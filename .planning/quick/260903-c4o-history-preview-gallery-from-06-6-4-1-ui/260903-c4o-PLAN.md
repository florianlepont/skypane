---
phase: quick-260903-c4o
plan: 260903-c4o
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/pages/history_page.py
  - companion/test_view_pages.py
  - companion/app.py
  - companion/static/style.css
  - companion/test_companion_app.py
  - server/panel_preview.py
autonomous: true
requirements: [QUICK-260903-c4o]

must_haves:
  truths:
    - "UIR-09 (developer override): History no longer renders the current panel enlarged and separate. `preview_section()` is deleted outright from `companion/pages/history_page.py`, and the newest render reaches the page only as `gallery_tiles(ctx)`'s FIRST tile — identical markup, identical sizing, identical `<a href>` open-full-size behaviour as every other tile. The audit's own suggested fix (keep the frame, serve it at higher resolution) is NOT implemented."
    - "The safety argument, restated so it is not re-litigated at execution time: `server/poll_loop.py::_save_to_gallery()` runs on every `panel_changed: True` write of `panel.bin`, saving the same canvas at full resolution, so `ctx[\"gallery_entries\"][0]` (newest-first, from `companion/app.py::gallery_entries()`) IS the image currently on the panel. The accepted edge case — a hand-placed `panel.bin` with no gallery entry now showing the no-renders empty state instead of a lone big image — is deliberate and is NOT engineered around."
    - "The section is a single, ALWAYS-VISIBLE `<section class=\"page-section\">` with exactly ONE `<h2 class=\"text-heading\">`. There is no second heading, and the gallery grid is NOT wrapped in `<details class=\"readings-disclosure\">` — it sits directly in the page flow as this section's primary content."
    - "Heading and caption copy is decided here, not left open: `NOW_SHOWING_HEADING` is superseded by `RENDER_GALLERY_HEADING = \"Recent renders\"`, and `RECENT_RENDERS_SUMMARY_TEMPLATE` is superseded by `RENDER_GALLERY_CAPTION_TEMPLATE = \"Newest first — the newest render is what the panel is showing now. Showing %d.\"`, emitted as a `<p class=\"text-label section-caption\">` sibling BELOW the `<h2>` (the `config_page.py` L196/L338/L390 pattern), never as an inline `<span>` inside the heading (that inline form is exactly the hanging-dash phone defect UIR-24 reports, and UIR-24 is out of scope — do not introduce a new instance of it). Renaming both constants is grep-verified safe: no consumer outside `history_page.py` exists."
    - "The em dash in the caption carries a space on BOTH sides — UIR-12 already fixed one glued em dash in this codebase; do not ship a second one."
    - "`COLOUR_CAVEAT` renders EXACTLY ONCE when the gallery is non-empty and NOT AT ALL when it is empty (nothing to caveat when nothing is shown). The count caption follows the same rule — a caption reading \"Showing 0.\" above an empty state is noise."
    - "`gallery_tiles(ctx)`'s existing empty-state branch (`_NO_RENDERS_HEADING`/`_NO_RENDERS_BODY`, copy UNCHANGED) is now the only empty state for this section. `_NO_PANEL_CAPTION`'s panel-file-based branch is gone with `preview_section()`."
    - "`gallery_tiles()`'s per-tile markup, `_PANEL_WIDTH`/`_PANEL_HEIGHT` sizing hints, `loading=\"lazy\"`, `decoding=\"async\"`, the `<a href>` wrapper and the caption fallback are all UNCHANGED. `GALLERY_DISPLAY_LIMIT` keeps its value of 12 — do not change this constant."
    - "Every \"View panel near this time\" symbol is untouched: `_view_panel_button_html()`, `_lightbox_html()`, `nearest_gallery_entry()`, `VIEW_PANEL_LABEL`, `LIGHTBOX_DIALOG_ID`, the three `data-view-panel-*` attribute constants, and the three-file DOM-contract guard that pins them. `git diff` on those functions must be empty."
    - "`from server import panel_preview` is removed from `history_page.py` — ruff's F (pyflakes) family is a BLOCKING CI lint (`.github/workflows/ci.yml` L42), so a surviving unused import fails CI, not just review."
    - "Dead-code retirement in `companion/app.py` is grep-verified before deletion, not assumed: `PREVIEW_IMAGE_ROUTE` (L118), `PREVIEW_THUMB_WIDTH` (L69), `_serve_preview_image()` (L712-731) and the `do_GET` dispatch branch (L935-938) all go, and `panel_preview` is dropped from the L61 `from server import ...` list (its only two uses are inside the deleted method). `PREVIEW_PAGE_ROUTE` (the `/preview` -> `/history` 303 redirect) STAYS — it is a different route with a different job."
    - "`companion/static/style.css`: `.preview-image` (L2208-2212, with its `/* Image preview (CFG-10) */` comment) and `.preview-frame` (L2947-2957, with its three-line preceding comment block) are deleted together with their own comments. `.readings-disclosure` (L2204-2206) STAYS — `health_page.py` L1674 and L1731 are live consumers and `test_status_pages.py` pins them. `.gallery-grid`, `.gallery-tile a` and `.gallery-grid img` are untouched: this task reuses the existing component, it does not restyle it."
    - "No new design tokens, no new fonts, no new JS file, no change to style.css's accent-reservation header comment, and zero external references added to the stylesheet — the `sketch-findings-skypane` skill's standing constraints."
    - "`server/panel_preview.py` and `server/test_panel_preview.py` (11 checks) are NOT deleted. `panel_preview.py` receives exactly ONE informational comment line near the top of its module docstring noting it currently has no production caller (companion/app.py's route, removed by this task, was the only one). Zero functional change to that file. `server/test_panel_preview.py` is not edited at all and must still report 11/11."
    - "ANTICIPATED surviving references, found during planning — the executor must retarget these rather than halt: `companion/test_companion_app.py` L1397-1399 (`unauthenticated GET /preview.png redirects to /login`), L1973-1978 (`_preview_missing`), L1980-1992 (`_preview_real_panel`), and `companion/test_view_pages.py` L1828-1858 (Section 3's end-to-end check asserting `/preview.png` returns a real PNG). These are TEST references, not production consumers. If ANY reference OUTSIDE this list survives the repo-wide grep, STOP and report it in SUMMARY.md instead of deleting anyway."
    - "Behaviour change that must be pinned, not discovered in production: `do_GET`'s unknown-path fallthrough (`companion/app.py` L957) is NOT session-gated, so once the `/preview.png` branch is removed an UNAUTHENTICATED `GET /preview.png` returns the generic 404 page instead of a 303 to `/login`. That is correct — it is exactly what every other unknown path already does and it leaks nothing beyond \"this route does not exist\" — but it is a real, observable contract change and gets its own replacement check."
    - "The five checks the task_spec names are replaced, not deleted, EXCEPT `_now_showing_no_preview_freshness_apparatus`, whose guarantee (no `data-loaded-at`, no `data-stale-banner` on History) is unaffected by this restructure and is RETAINED in place — only its now-pointless `_write_panel_file(tmp)` seeding and its comment are updated. Net for `test_view_pages.py`: 4 checks removed, 5 added, `EXPECTED_CHECK_COUNT` 49 -> 50, with a dated provenance comment in the file's existing idiom."
    - "Net for `test_companion_app.py`: 3 checks removed, 2 added (authenticated `/preview.png` 404 WITH a real `panel.bin` present; unauthenticated `/preview.png` 404 with no login redirect), `EXPECTED_CHECK_COUNT` 108 -> 107, same provenance-comment idiom. `IMAGE_BYTES` stays in use because the replacement check still writes a real 960,000-byte panel — proving the route 404s even when a panel genuinely exists, which is the whole point."
    - "No tombstone comments naming a deleted identifier are left in any edited source file (a comment reading \"_NO_PANEL_CAPTION removed by ...\" would defeat its own dead-code gate). Removals are described in the commit message, not in the file. The verify gates below are comment-immune (Python via `ast`, CSS via a comment-stripping regex) specifically so this rule is enforceable rather than aspirational."
    - "`scripts/run-all-tests.sh` reports ALL 16 canonical harnesses green with their exact pass counts recorded in SUMMARY.md — none skipped, `server/` included. Coverage stays above `fail_under = 83`; the exact reported percentage is recorded, because `panel_file_mtime_iso()` loses its only caller AND has no coverage of its own in `server/test_panel_preview.py` (grep-verified during planning), so a small drop is expected and must be measured rather than assumed."
    - "The browser verification runs against a COPY of `/tmp/skypane-prod-state`, never the original: `cp -R /tmp/skypane-prod-state/. \"$SCRATCH/state\"` first, then `companion/app.py --state-dir \"$SCRATCH/state\"` with `SKYPANE_COMPANION_PASSWORD` set for the session. Do not start the server against the original snapshot under any circumstance."
    - "A BEFORE baseline is captured in a real browser BEFORE the first edit lands (Task 1 step 0), so the page-height before/after is measured on this machine rather than quoted from the audit's own figures. Without it there is no honest 'after' number to report."
  artifacts:
    - path: "companion/pages/history_page.py"
      provides: "The single always-visible render-gallery section: one <h2>, a count caption, the colour caveat once when non-empty, and gallery_tiles() directly in page flow"
      contains: "RENDER_GALLERY_HEADING"
    - path: "companion/test_view_pages.py"
      provides: "Five replacement checks pinning the new single-section structure, plus the retargeted end-to-end route check"
      contains: "RENDER_GALLERY_CAPTION_TEMPLATE"
    - path: "companion/app.py"
      provides: "The retired /preview.png route — constants, handler, dispatch branch and the panel_preview import all gone"
      contains: "PREVIEW_PAGE_ROUTE"
    - path: "companion/static/style.css"
      provides: "The stylesheet with .preview-frame and .preview-image retired and .readings-disclosure/.gallery-grid intact"
      contains: ".gallery-grid"
    - path: "companion/test_companion_app.py"
      provides: "Two replacement route checks pinning /preview.png's 404 in both auth states"
      contains: "EXPECTED_CHECK_COUNT"
    - path: "server/panel_preview.py"
      provides: "One informational no-production-caller note; zero functional change"
      contains: "def panel_png_bytes"
  key_links:
    - "history_page.render()'s section <-> gallery_tiles()'s empty-state branch — the caveat and the count caption must be emitted by the SAME non-empty condition the grid is, or an empty deployment shows a caveat about images it is not showing."
    - "companion/app.py's deleted PREVIEW_IMAGE_ROUTE branch <-> do_GET's unsession-gated 404 fallthrough — the pre-auth response changes from 303 to 404, and only the new replacement check makes that visible."
    - "history_page.py's deleted `from server import panel_preview` <-> ruff's blocking F401 in CI — the same coupling exists for app.py's own import list."
    - "server/panel_preview.py::panel_file_mtime_iso() <-> its now-zero callers and its zero coverage in server/test_panel_preview.py — this is the one place the coverage number can move, so it is measured, not assumed."
    - "style.css `.preview-frame` <-> the `sketch-findings-skypane` skill's `--radius-card` reserved-consumer list, which names it — SUMMARY.md must carry `affects: [sketch-findings-skypane]` so the skill's list is corrected on its next maintenance pass."
---

<objective>
Fold History's separate, enlarged "Now showing" frame into the same uniform gallery grid as every other historical render — same tile size, same full-resolution `/gallery/{name}.png` source, no special treatment — and retire the low-resolution `/preview.png` route and its stylesheet rules behind it.

Purpose: UIR-09 measured the old frame at 1,325px tall, pushing the flight list ~1,400px down a page named History, and the image inside it was a 600px nearest-neighbour thumbnail stretched to 956px — visible dither static beside a crisp lightbox showing the same render. The developer's decision supersedes the audit's suggested fix: rather than serve that one frame at higher resolution, stop treating the newest render as special at all. The gallery already carries it, at full resolution, first in the list.

Output: a single always-visible section on `/history` with one heading, one count caption, one colour caveat and the existing gallery grid; four dead symbols gone from `companion/app.py`; two dead rules gone from `companion/static/style.css`; nine rewritten/retargeted harness checks across two files; and a SUMMARY.md carrying real before/after page-height and image-crispness numbers in quick task 260902-w4t's Browser Measurement format.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.planning/quick/260902-w4t-history-table-content-from-06-6-4-1-ui-r/260902-w4t-SUMMARY.md
@companion/pages/history_page.py
@companion/app.py
@companion/static/style.css
@companion/test_view_pages.py
@companion/test_companion_app.py
</context>

<!-- planner-discipline-allow: preview_section -->
<!-- planner-discipline-allow: _NO_PANEL_CAPTION -->
<!-- planner-discipline-allow: _PREVIEW_IMAGE_ROUTE -->
<!-- planner-discipline-allow: PREVIEW_IMAGE_ROUTE -->
<!-- planner-discipline-allow: PREVIEW_THUMB_WIDTH -->
<!-- planner-discipline-allow: _serve_preview_image -->
<!-- planner-discipline-allow: preview-frame -->
<!-- planner-discipline-allow: preview-image -->
<!-- planner-discipline-allow: /preview.png -->
<!-- planner-discipline-allow: panel_preview -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fold the live panel into the render gallery (history_page.py + its render-level checks)</name>
  <files>companion/pages/history_page.py, companion/test_view_pages.py</files>
  <behavior>
    - With 3 gallery entries and no flight rows: the page carries exactly one `<h2`, exactly one occurrence of `COLOUR_CAVEAT`, the caption `RENDER_GALLERY_CAPTION_TEMPLATE % 3`, exactly 3 `<img` elements, and every `alt` value starts with `Rendered panel `.
    - With a real `panel.bin` on disk AND gallery entries seeded: the rendered output still contains zero occurrences of `/preview.png`, `preview-frame`, `preview-image`, and the old no-panel caption sentence.
    - With `gallery_entries=[]`: `_NO_RENDERS_HEADING` present, no `gallery-grid`, `COLOUR_CAVEAT` count is 0, no count caption, the single `<h2` still present.
    - With 3 gallery entries AND one seeded flight row: `<details class="readings-disclosure"` appears zero times, while `<details class="history-card__details"` still appears (History's unrelated disclosures survive).
    - With `GALLERY_DISPLAY_LIMIT + 3` entries: exactly `GALLERY_DISPLAY_LIMIT` tiles render, the caption's count equals that limit, and the first tile's href names `entries[0]` (newest-first preserved).
  </behavior>
  <action>
**Step 0 — capture the BEFORE baseline before touching anything.** This produces no repo diff and must happen on the clean tree:
`cp -R /tmp/skypane-prod-state/. "$SCRATCH/state-before"`, then run `SKYPANE_COMPANION_PASSWORD=... server/.venv/bin/python3 companion/app.py --state-dir "$SCRATCH/state-before" --port <free>`, log in via Playwright (`mcp__plugin_playwright_playwright__browser_run_code_unsafe`), load `/history`, and record to a scratch JSON: `document.documentElement.scrollHeight` at 1440/1280/375px in both themes; the `getBoundingClientRect().height` of the `.preview-frame` element; the `.preview-image` element's `naturalWidth`/`naturalHeight` vs its rendered `width`/`height`; and the `offsetTop` of the flight list (`.history-cards` or `.data-table-wrap`). Stop the server. If this baseline is skipped, the only recovery is `git archive origin/main | tar -x -C "$SCRATCH/before-tree"` and running the app from that copy with this repo's venv interpreter — do not fabricate the numbers from the audit text.

**Step 1 — `companion/pages/history_page.py`.**
1. Delete `preview_section()` in full (L137-176).
2. Delete the `_NO_PANEL_CAPTION` (L69) and `_PREVIEW_IMAGE_ROUTE` (L82-84, including its two-line trailing comment) constants. Grep the file after the deletion to confirm no reference survives.
3. Remove `panel_preview` from the module's imports (L45). Nothing else in this file uses it — confirm by grep, then delete the import line.
4. Supersede the two heading constants (L105-110), keeping this codebase's documented-supersession comment idiom:
   `RENDER_GALLERY_HEADING = "Recent renders"` and
   `RENDER_GALLERY_CAPTION_TEMPLATE = "Newest first — the newest render is what the panel is showing now. Showing %d."`
   The comment above them explains WHY there is now one heading rather than two: the newest render is the first tile in this same grid, so a separate "Now showing" heading would name a section that no longer exists. Keep the "count of what is shown, not the ceiling constant" convention the old summary template documented. The em dash keeps a space on both sides.
5. Update the `_PANEL_WIDTH`/`_PANEL_HEIGHT` comment (L87-93): its sole consumer is now `gallery_tiles()`, not two call sites.
6. Update `COLOUR_CAVEAT`'s comment block (L58-67): the caveat now applies to the gallery images rather than to a separate live-preview PNG. The sentence's VALUE is unchanged — a user comparing any of these renders to the glass on the wall still needs it.
7. In `render()` (L926-948), replace the `now_showing_html` block with one always-visible section:
   `<section class="page-section">` + `<h2 class="text-heading">{escaped heading}</h2>` + (when the gallery is non-empty) `<p class="text-label section-caption">{escaped caption}</p>` + (when non-empty) `<p class="text-body">{escaped COLOUR_CAVEAT}</p>` + `gallery_tiles(ctx)` + `</section>`.
   No `<details>`, no `<summary>`, no second `<h2>`. `shown_count` keeps its existing `min(len(entries), GALLERY_DISPLAY_LIMIT)` computation and drives the caption. Both the caption and the caveat are gated on the SAME non-empty condition, with a comment stating why (nothing to caveat and nothing to count when the empty state is showing). Rewrite the block's `# D-18/D-19:` comment to describe what the section now is — do not leave prose describing a Now-showing frame that no longer exists, and do not name any deleted identifier in that prose.
   Everything below in `render()` — the `_DB_UNAVAILABLE` branch, the per-row `nearest_gallery_entry()` loop, `has_view_panel_button`, `lightbox_html`, the filter-bar/cards/table ordering — is byte-identical.

**Step 2 — `companion/test_view_pages.py`.**
Delete these four checks and their `check(...)` registrations: `_now_showing_panel_present_one_image_one_caveat` (L1173-1192), `_now_showing_no_panel_no_image_element` (L1194-1212), `_recent_renders_disclosure_closed_by_default_correct_count` (L1214-1244), `_recent_renders_disclosure_empty_gallery` (L1246-1262). RETAIN `_now_showing_no_preview_freshness_apparatus` (L1264-1283) — its guarantee is unrelated to this restructure — but drop its `_write_panel_file(tmp)` line (no longer meaningful) and update its comment and check description to stop referencing a ported preview section.
Add five replacement checks in the same Section 1b block, matching this harness's existing style (nested function returning `(ok, message)`, `_mkstate()` + `try/finally shutil.rmtree`), asserting against module constants (`history_page.RENDER_GALLERY_HEADING`, `escape_html(history_page.RENDER_GALLERY_CAPTION_TEMPLATE % n)`, `history_page.COLOUR_CAVEAT`, `history_page._NO_RENDERS_HEADING`, `history_page.GALLERY_DISPLAY_LIMIT`) and NEVER against re-typed string literals:
   a. non-empty structure: `rendered.count("<h2") == 1`, heading text present, caveat count == 1, caption present, `_img_tag_count(rendered) == 3`, and every value from `_img_alt_values(rendered)` starts with the tile alt prefix. (Both helpers already exist at L192/L196 and are currently unused — put them to work rather than writing new ones. Seed gallery entries but NO runway events, so no lightbox `<img>` is emitted and the image count is unambiguous.)
   b. no preview apparatus ever: write a real `panel.bin` via `_write_panel_file(tmp)` AND seed gallery entries, then assert the rendered output contains none of the three dead markers and none of the old no-panel sentence — the panel file existing changes nothing about the markup any more, which is the point.
   c. empty gallery: empty-state heading present, no `gallery-grid`, caveat count == 0, no caption, exactly one `<h2`.
   d. not a disclosure: with entries AND one seeded flight row, `'<details class="readings-disclosure"' not in rendered` while `'<details class="history-card__details"' in rendered`.
   e. display limit: seed `GALLERY_DISPLAY_LIMIT + 3` entries, assert exactly `GALLERY_DISPLAY_LIMIT` `class="gallery-tile"` occurrences, the caption's count equals that limit, and the first `href` in document order names `entries[0]`.
Update `EXPECTED_CHECK_COUNT` (L69) from 49 to 50 with a dated provenance comment in this file's existing running-arithmetic idiom (`# 50 = 49 - 4 (quick task 260903-c4o: the two Now-showing checks and the two Recent-renders disclosure checks, whose structure no longer exists) + 5 (...)`). Update the module docstring's "Covers:" paragraph (L8-24) so it describes the single gallery section instead of a Now-showing image plus a Recent-renders disclosure; leave its Section 3 sentence alone, Task 2 owns that one.

Do NOT touch `gallery_tiles()`, `_gallery_name_to_iso()`, `nearest_gallery_entry()`, `_view_panel_button_html()`, `_lightbox_html()`, or any check that exercises them.
  </action>
  <verify>
    <automated>server/.venv/bin/ruff check companion/ && server/.venv/bin/python3 -c "import ast; t=ast.parse(open('companion/pages/history_page.py').read()); f={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; a={x.id for n in ast.walk(t) if isinstance(n,ast.Assign) for x in n.targets if isinstance(x,ast.Name)}; m={al.name for n in ast.walk(t) if isinstance(n,ast.ImportFrom) for al in n.names}; assert 'preview_section' not in f, 'dead function survives'; assert not ({'_NO_PANEL_CAPTION','_PREVIEW_IMAGE_ROUTE'} & a), 'dead constants survive'; assert 'panel_preview' not in m, 'dead import survives'; assert {'RENDER_GALLERY_HEADING','RENDER_GALLERY_CAPTION_TEMPLATE','GALLERY_DISPLAY_LIMIT'} <= a, 'new constants missing'; print('history_page structure OK')" && server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>`companion/test_view_pages.py` reports 50/50. The AST gate confirms `preview_section`, both dead constants and the `panel_preview` import are gone and the two new constants exist. `ruff check companion/` is clean. `git diff` shows zero change to `gallery_tiles()` and to every View-panel/lightbox symbol. One commit: `refactor(quick-260903-c4o): fold History's live panel into the render gallery`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: retire the /preview.png route, its stylesheet rules, and their harness checks</name>
  <files>companion/app.py, companion/static/style.css, server/panel_preview.py, companion/test_companion_app.py, companion/test_view_pages.py</files>
  <behavior>
    - Authenticated `GET /preview.png`, with a real 960,000-byte `panel.bin` present in the state dir, returns 404 carrying the generic `Page not found.` copy — the route is gone, and a present panel does not resurrect it.
    - Unauthenticated `GET /preview.png` returns 404, NOT a 303 to `/login` — the branch that carried the session gate is gone and the request falls through to `do_GET`'s ungated unknown-path handler, exactly as any other unknown path does.
    - `GET /preview` still returns 303 to `/history`; `GET /history` still returns 200; a seeded `GET /gallery/{name}.png` still returns 200 `image/png` with a real PNG signature — the full-resolution source every tile now points at.
  </behavior>
  <action>
**Step 1 — grep first, delete second.** Run the repo-wide sweep and compare against the plan's anticipated list before removing anything:
`grep -rn "preview\.png\|PREVIEW_IMAGE_ROUTE\|PREVIEW_THUMB_WIDTH\|preview-frame\|preview-image" --include='*.py' --include='*.css' --include='*.js' --include='*.html' --include='*.sh' . | grep -v '^\./\.planning/' | grep -v '^\./\.claude/'`
Expected survivors, all of them tests this task retargets: `companion/test_companion_app.py` L1397-1399 / L1973-1978 / L1980-1992, and `companion/test_view_pages.py` L22 (docstring) and L1828-1858 (Section 3). Anything OUTSIDE that list means the plan missed a consumer: STOP, do not delete, and record the finding in SUMMARY.md.

**Step 2 — `companion/app.py`.** Delete `PREVIEW_THUMB_WIDTH` (L69), `PREVIEW_IMAGE_ROUTE` (L118), the whole `_serve_preview_image()` method (L712-731), and the four-line dispatch branch in `do_GET` (L935-938). Drop `panel_preview` from the L61 `from server import device_config, history_db, panel_preview` list, and update the L45 comment that enumerates the server modules this file reaches into. `PREVIEW_PAGE_ROUTE` and its `/preview` -> `/history` redirect branch STAY untouched, as do `GALLERY_ROUTE_PREFIX`, `gallery_entries()`, `gallery_bytes()` and `_serve_gallery_image()`.

**Step 3 — `companion/static/style.css`.** Delete the `.preview-image` rule (L2208-2212) together with its own `/* Image preview (CFG-10) */` comment line, and the `.preview-frame` rule (L2947-2957) together with its three-line preceding comment block. Do not touch the `.readings-disclosure` rule immediately above the first one (live: `health_page.py` L1674/L1731) or the `.gallery-grid` / `.gallery-tile a` / `.gallery-grid img` rules immediately below the second one. Add no new rule: the gallery grid already sizes these tiles, and this task's whole premise is that no bespoke framing is needed.

**Step 4 — `server/panel_preview.py`.** Add exactly one informational sentence inside the existing module docstring (near the top, after the opening paragraph) recording that this module currently has no production caller — the companion route that used to serve its output was removed by quick task 260903-c4o — and that it is kept as tested infrastructure. Zero functional change: no code line is added, removed or reordered, and `server/test_panel_preview.py` is not edited and must still report 11/11.

**Step 5 — `companion/test_companion_app.py`.** Replace the `_unauth_redirects_to_login("GET", "/preview.png")` registration (L1395-1399) with a check asserting an unauthenticated `GET /preview.png` returns 404 and an empty-of-page-content-shaped generic not-found body, with a comment recording WHY the expectation changed (the session-gated branch is gone; `do_GET`'s unknown-path fallthrough is deliberately ungated and leaks nothing beyond route non-existence). Leave the surrounding four-tab `?next=` loop, the `/preview` redirect-source check and the gallery-image check untouched. In the authenticated block, replace `_preview_missing` (L1973-1978) and `_preview_real_panel` (L1980-1992) with ONE check that writes the same real `b"\x11" * IMAGE_BYTES` panel and then asserts `GET /preview.png` returns 404 with the exact `Page not found.` copy — keeping `IMAGE_BYTES` in use and proving the route is gone rather than merely empty. Also correct the L1790-1800 explanatory comment block that currently cites `_preview_real_panel`/`_preview_missing` as live coverage. Move `EXPECTED_CHECK_COUNT` (L68) from 108 to 107 with a dated provenance comment in this file's existing idiom.

**Step 6 — `companion/test_view_pages.py` Section 3.** Retarget the existing end-to-end check in place (still one check, so `EXPECTED_CHECK_COUNT` stays 50): keep the `/history` 200 and `/preview` 303 -> `/history` assertions verbatim, replace the `/preview.png` real-PNG assertion with a 404 assertion, and add a `GET /gallery/{name}.png` assertion that returns 200, `Content-Type: image/png` and a body starting with `_PNG_SIGNATURE` — seeded via the existing `_seed_gallery(harness.tmpdir, [...])` helper using a plain name with no URL-reserved characters (the `"20260827T100002Z.png"` shape already used elsewhere in this file). That keeps `_PNG_SIGNATURE` in use and upgrades the check into a proof that the route every tile now links to genuinely serves full-resolution bytes. Keep the `_write_panel_file(harness.tmpdir)` seeding — a present panel that changes nothing is part of what this check proves. Update the check's description string and the module docstring's Section 3 sentence (L21-24) accordingly.

**Step 7 — full suite.** `scripts/run-all-tests.sh`. Record the exact per-harness counts and the reported coverage percentage; `panel_file_mtime_iso()` loses its last caller and has no coverage of its own, so a small dip is expected — confirm it stays above `fail_under = 83` rather than assuming it.
  </action>
  <verify>
    <automated>server/.venv/bin/ruff check . && server/.venv/bin/python3 -c "import ast,re; t=ast.parse(open('companion/app.py').read()); f={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; a={x.id for n in ast.walk(t) if isinstance(n,ast.Assign) for x in n.targets if isinstance(x,ast.Name)}; m={al.name for n in ast.walk(t) if isinstance(n,ast.ImportFrom) for al in n.names}; assert '_serve_preview_image' not in f, 'dead handler survives'; assert not ({'PREVIEW_IMAGE_ROUTE','PREVIEW_THUMB_WIDTH'} & a), 'dead constants survive'; assert 'panel_preview' not in m, 'dead import survives'; assert 'PREVIEW_PAGE_ROUTE' in a, 'the /preview redirect constant was deleted by mistake'; css=re.sub(r'/\*.*?\*/','',open('companion/static/style.css').read(),flags=re.S); assert '.preview-frame' not in css and '.preview-image' not in css, 'dead CSS rules survive'; assert '.gallery-grid' in css and '.readings-disclosure' in css, 'a live rule was deleted by mistake'; print('route + css retirement OK')" && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 server/test_panel_preview.py</automated>
  </verify>
  <done>`test_companion_app.py` reports 107/107, `test_view_pages.py` 50/50, `server/test_panel_preview.py` 11/11 with zero diff to that harness. The AST/CSS gate confirms the four dead app.py symbols and the two dead CSS rules are gone while `PREVIEW_PAGE_ROUTE`, `.gallery-grid` and `.readings-disclosure` survive. `scripts/run-all-tests.sh` is green across all 16 harnesses with coverage above 83. One commit: `refactor(quick-260903-c4o): retire the /preview.png route and its stylesheet rules`.</done>
</task>

<task type="auto">
  <name>Task 3: real-browser verification against production data, and SUMMARY</name>
  <files>.planning/quick/260903-c4o-history-preview-gallery-from-06-6-4-1-ui/260903-c4o-SUMMARY.md</files>
  <action>
1. Re-run `scripts/run-all-tests.sh` on the final tree and record EVERY harness's exact pass count — all 16, `server/` included, none skipped — plus the coverage percentage. If a harness fails, fix it before proceeding; if a failure is genuinely pre-existing and unrelated, say so with evidence rather than omitting the harness.

2. Stand up a throwaway state dir — never the original: `cp -R /tmp/skypane-prod-state/. "$SCRATCH/state-after"`, then run `companion/app.py --state-dir "$SCRATCH/state-after"` with `SKYPANE_COMPANION_PASSWORD` set, and drive it with `mcp__plugin_playwright_playwright__browser_run_code_unsafe`. Do not start the server against `/tmp/skypane-prod-state` itself under any circumstance.

3. At 1440px, 1280px and 375px, in BOTH themes, load `/history` and record:
   - **Zero `/preview.png` requests.** Capture the network log for the page load and assert no entry whose URL ends in that route; also assert `document.querySelectorAll('.preview-frame, .preview-image').length === 0`. Report both, not one.
   - **Exactly one heading, gallery directly below.** Count `<h2>` elements inside the section, confirm the next element sequence is caption -> caveat -> `.gallery-grid`, and confirm `document.querySelectorAll('details.readings-disclosure').length === 0` on this page while History's own `details.history-card__details` count is non-zero at 375px.
   - **The colour caveat appears exactly once** (count occurrences of its text in `document.body.innerText`).
   - **Page height before/after.** `document.documentElement.scrollHeight` at each width/theme, next to Task 1 step 0's baseline numbers. Also record the flight list's `offsetTop` before/after — the audit's actual complaint was the ~1,400px push-down, so the delta on that number is the headline result, not just total height.
   - **First tile crispness.** For the first `.gallery-tile img`: `naturalWidth`/`naturalHeight` and `getBoundingClientRect()` width/height, plus its resolved `currentSrc`. Confirm the source is a `/gallery/...png` URL and that `naturalWidth` is the panel's real 1200px rather than the old 600px thumbnail, and state the resulting upscale factor (it should now be < 1, i.e. downscaled, where the old frame upscaled ~1.6x).
   - **Uniformity.** Confirm the first tile's rendered box matches the other tiles' (compare `getBoundingClientRect().width` across the first three tiles) — the entire point of the change is that the newest render gets no special treatment.

4. Screenshot `/history` at 1440px and 375px in both themes for the visual record.

5. Write `260903-c4o-SUMMARY.md` with: frontmatter carrying `affects: [sketch-findings-skypane]` (the skill's `--radius-card` reserved-consumer list names `.preview-frame`, which no longer exists, and its Data/tables section describes History's two-part render area); a **Browser Measurement (before/after)** table matching quick task 260902-w4t's format exactly (`| Measurement | Viewport | Before (this branch, pre-edit baseline) | After (this branch, 2026-09-03) |`); a **Test Harness Results** table listing all 16 harnesses with counts and an explicit overall PASS/FAIL; the grep sweep's result stated plainly (either "no unanticipated survivors" or the exact finding); and a short note recording the accepted edge case (a `panel.bin` with no gallery entry now shows the no-renders empty state) and the deliberate non-deletion of `server/panel_preview.py`.
  </action>
  <verify>
    <automated>grep -q "Browser Measurement (before/after)" .planning/quick/260903-c4o-history-preview-gallery-from-06-6-4-1-ui/260903-c4o-SUMMARY.md && grep -q "sketch-findings-skypane" .planning/quick/260903-c4o-history-preview-gallery-from-06-6-4-1-ui/260903-c4o-SUMMARY.md && grep -c "test_" .planning/quick/260903-c4o-history-preview-gallery-from-06-6-4-1-ui/260903-c4o-SUMMARY.md | awk '$1>=16{exit 0} {exit 1}'</automated>
  </verify>
  <done>All 16 harnesses reported with exact counts, a coverage percentage and an overall verdict. Zero `/preview.png` requests confirmed from a real network log at all three widths in both themes. Exactly one heading, one caveat occurrence and no disclosure wrapper confirmed in-browser. Before/after `scrollHeight` and flight-list `offsetTop` numbers recorded from this machine, not quoted from the audit. The first tile's `naturalWidth` confirmed at the panel's full resolution with its rendered box matching its siblings. One commit: `docs(quick-260903-c4o): complete the History render-gallery consolidation`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> companion HTTP router | Every `do_GET` path decision, including which branches carry `require_session()` |
| state_dir filesystem -> rendered HTML | Gallery filenames become `<a href>`/`<img src>` values and caption text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-c4o-01 | Information disclosure | `do_GET`'s unknown-path fallthrough after the `/preview.png` branch is removed | low | accept | Pre-auth `GET /preview.png` changes from a 303-to-login into the generic 404 that every unknown path already returns. It reveals only route non-existence, discloses no panel bytes and no state-dir content, and is pinned by a dedicated replacement check in `test_companion_app.py` so the change is recorded rather than silent. |
| T-c4o-02 | Information disclosure | Gallery images becoming this section's only image source | low | mitigate | No new route and no widened access: `/gallery/{name}.png` keeps its existing `require_session()` gate and `gallery_bytes()`'s exact-match-against-a-real-scandir-listing rule (T-06-05-02), both untouched by this task. The retargeted end-to-end check re-proves the gated 200 and the unauthenticated redirect. |
| T-c4o-03 | Tampering | Gallery filenames reaching the DOM | low | mitigate | Unchanged code path: `gallery_tiles()` already routes every filename through `escape_html()` once at the point of interpolation. This task adds no new interpolation site — the heading, caption and caveat are module constants, still escaped on the way out. |
| T-c4o-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package is installed, added or upgraded by this task. `server/requirements.txt` and `server/requirements-dev.txt` carry zero diff, so no legitimacy gate applies. |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` green across all 16 harnesses; `companion/test_view_pages.py` 50/50, `companion/test_companion_app.py` 107/107, `server/test_panel_preview.py` 11/11 with zero diff to that file's harness; coverage above `fail_under = 83`.
- `server/.venv/bin/ruff check .` clean (the blocking CI lint).
- The repo-wide grep sweep returns no consumer of `/preview.png`, `PREVIEW_IMAGE_ROUTE`, `PREVIEW_THUMB_WIDTH`, `preview-frame` or `preview-image` outside the four anticipated test sites, all of which are retargeted.
- `git diff origin/main --stat` shows exactly the six files in `files_modified` plus the SUMMARY: no `server/` change beyond `panel_preview.py`'s one docstring sentence, and no change to `server/test_panel_preview.py`, `companion/pages/health_page.py`, `companion/static/list-filter.js` or `companion/static/panel-lookup.js`.
- Real-browser confirmation at 1440/1280/375px in both themes, against a COPY of the production snapshot, with before/after numbers recorded.
</verification>

<success_criteria>
- History shows one always-visible section: one `<h2>`, one count caption, the colour caveat exactly once when the gallery is non-empty and never when it is empty, and the existing `.gallery-grid` directly in page flow with no disclosure wrapper.
- The newest render is the grid's first tile at full `/gallery/{name}.png` resolution, rendered in a box identical to its siblings — no enlarged frame, no low-resolution thumbnail, no special treatment anywhere in the markup.
- `/preview.png` no longer exists as a route; its constants, handler, dispatch branch, stylesheet rules and harness checks are all retired or retargeted, with `server/panel_preview.py` and its 11-check harness deliberately kept and annotated.
- `GALLERY_DISPLAY_LIMIT` is still 12, `gallery_tiles()`'s tile markup is byte-identical, and every View-panel/lightbox symbol is untouched.
- Three commits, one per task, in the repo's conventional-commit house style with the `(quick-260903-c4o)` scope.
</success_criteria>

<output>
Create `.planning/quick/260903-c4o-history-preview-gallery-from-06-6-4-1-ui/260903-c4o-SUMMARY.md` when done.
</output>
