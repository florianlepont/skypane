---
quick_id: 260923-9fe
slug: public-repo-hygiene-licences-readme-secu
date: 2026-09-23
type: quick
---

# Quick 260923-9fe: public-repo hygiene — licences, README, security

The repository went public. An audit (2026-09-23) found the full git history
free of real secrets (only test fixtures and placeholders), but found licensing,
documentation and repo-security gaps.

## Decisions (from the user, 2026-09-23)

- Airline/state liveries in `server/assets/icons/illustrations/`: keep them, add a
  trademark/no-affiliation disclaimer, and correct the VENDOR.md claim that AI
  generation "sidesteps" the trademark constraint (it does not).
- `firmware/` (and `stub-server/byos_server.py`): Apache-2.0, satisfying all four
  Apache §4 conditions (licence copy, NOTICE, retained notices, modified-file notices).
- README: English, rewritten for a public visitor.
- GitHub settings (approved in chat): Dependabot alerts + security updates, private
  vulnerability reporting, ruleset blocking force-push/deletion on `main`,
  description + topics, fork-PR CI approval for all external contributors.

## Tasks

1. **LICENSE / NOTICE** — `LICENSE` becomes the verbatim MIT text only (GitHub then
   detects MIT; today it reports "Other"). The per-directory scope note moves to a new
   top-level `NOTICE` and to the README's licence section.
2. **Apache-2.0 compliance in firmware** — add `firmware/LICENSE` (verbatim from the
   pinned upstream commit) and `firmware/NOTICE` (upstream NOTICE + SkyPane line).
   SPDX headers: SkyPane-original files (battery*, led*, test_battery_math.c,
   secrets.example.h) are copyright Florian Lepont, not YODE; modified vendored files
   gain a second `SPDX-FileCopyrightText: 2026 Florian Lepont` line plus a
   "modified — see firmware/VENDOR.md" notice; verbatim files untouched. Add `led.*`
   and `test_battery_math.c` to VENDOR.md's originals list.
3. **Trademarks** — disclaimer in README, NOTICE and illustrations VENDOR.md.
4. **README** — public-facing rewrite; keep the adsb.fi citation sentence verbatim
   (COMPLIANCE.md requires the two copies agree).
5. **Repo security files** — `SECURITY.md`, `.github/dependabot.yml` (pip +
   github-actions), `permissions: contents: read` on both workflows,
   `webfactory/ssh-agent` pinned to a commit SHA (it handles the deploy key).
6. **`.claude/CLAUDE.md`** — replace the stale AeroDataBox/PRIM stack text with the
   stack actually shipped (ADS-B aggregators, stdlib companion, OVH-hosted VPS …).
7. **GitHub settings** via `gh api` (user-approved list above).

## Verification

- `./scripts/check-attribution.sh` passes; `firmware/tests/run_host_tests.sh` passes.
- `gh repo view --json licenseInfo` reports MIT after merge.
- `git grep -L SPDX-FileCopyrightText firmware/main/*.[ch]` is empty.
