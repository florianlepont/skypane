---
phase: quick-260902-w4t
plan: 260902-w4t
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/pages/history_page.py
  - companion/static/style.css
  - companion/layout.py
  - companion/test_view_pages.py
autonomous: true
requirements: [QUICK-260902-w4t]

must_haves:
  truths:
    - "UIR-05: a row with no airline no longer borrows the ROUTE fallback. `history_page.py` gains its own module-level `AIRLINE_FALLBACK_TEXT = \"Airline unknown\"` constant, and `format_event_row()`'s `airline_label` fallback (L498-501) uses it instead of `panel_render.ROUTE_FALLBACK_TEXT`. The `route_label` fallback four lines below (L505-507) is left byte-identical — the two nouns stop sharing one string."
    - "`server/plane/render.py` is NOT touched. `ROUTE_FALLBACK_TEXT` (render.py:301) keeps its value and all four of its live in-module callers (render.py:1200/1329/1338/1349) plus `server/test_render.py`'s six assertions on it are unaffected — grep-verified before editing, not assumed."
    - "UIR-05: `_type_airline_cell()` (L623-637) keys the unresolved-airline link on `AIRLINE_FALLBACK_TEXT`, not `panel_render.ROUTE_FALLBACK_TEXT`, and its docstring's own sentence naming the compared constant is corrected in the same edit. `_history_cards_html()`'s mirrored comparison (L787-790) and its neighbouring `# D-21:` comment are updated identically, so the desktop cell and the mobile `<dl>` can never key on different constants."
    - "UIR-05: the \"View unresolved prefixes\" anchor is visibly separated from the airline text it follows. `_unresolved_link_html()` emits `class=\"text-label cell-unresolved-link\"` (a new class constant `UNRESOLVED_LINK_CLASS` alongside the existing `UNRESOLVED_LINK_HREF`/`UNRESOLVED_LINK_TEXT` pair), and style.css gains `.cell-unresolved-link { margin-left: var(--space-sm); }` — an existing spacing token, following the `margin-left: var(--space-xs)` precedent `.nav-notification` (style.css L655-661) already sets for an inline appended element. One class covers both the desktop `<td>` and the mobile `<dd>`, because both call the same builder."
    - "UIR-06: `_callsign_hex_cell()` (L594-611) gains a hex-only branch. When `callsign` is falsy and `hex_value` is truthy it emits the hex inside `<span class=\"cell-primary\">` followed by exactly one copy button — the existing `_copy_button_html(hex_value, _COPY_HEX_LABEL)` call, not a new one — then the existing `cell-inline-sep` separator span and `<span class=\"cell-secondary\">no callsign</span>` with NO copy button on that presentational text. The cell carries exactly 1 `data-copy-value` occurrence."
    - "UIR-06: the callsign-present branch of `_callsign_hex_cell()` is byte-identical after this task. `test_view_pages.py`'s `_desktop_callsign_hex_cell_two_copy_buttons` check (L893-929, fixture callsign `CDONE` + hex `cd01`) still finds exactly 2 copy buttons and 2 feedback-sibling pairs without being edited."
    - "UIR-06: the both-falsy case (no callsign, no hex) still returns a `<td>` and does not raise — it renders an empty primary span with no copy button, no separator and no secondary, which is the same shape `_merged_cell()`'s own docstring already reasons about for a dangling separator."
    - "UIR-06: `_history_cards_html()`'s primary line (L760-769) mirrors the same rule. When `row[\"callsign\"]` is falsy and `row[\"hex\"]` is truthy the `<span class=\"cell-primary mono\">` carries the hex, followed by a `<span class=\"cell-secondary\">no callsign</span>` note, so the card's primary line is never blank. `.history-card__primary` is `display: flex; gap: var(--space-sm); justify-content: space-between` and `.history-card__time` carries `margin-left: auto`, so the extra span needs no new CSS."
    - "UIR-06: the mobile `<details>` disclosure is unchanged. Its `Callsign` `<dt>/<dd>` pair keeps rendering the literal (possibly empty) value with its copy button, so `test_view_pages.py`'s `_mobile_details_three_copy_buttons` check (L931-960, fixture callsign `CMONE`) still finds exactly 3 — the plan deliberately does not extend UIR-06's no-copy-for-empty-value rule into the disclosure, because no production row reaches it with an empty callsign in a way the audit flagged."
    - "UIR-04: `_CORROBORATION_LABELS[\"None\"]` (L262-267) becomes `(\"ok\", \"Single-source\")`. The long form survives as a tooltip: a new `_CORROBORATION_TITLES` dict maps `\"None\"` to `\"Single-source (uncorroborated)\"`, and `format_event_row()` returns it under a new `corroboration_title` key (empty string for `True`/`False`, which need no tooltip)."
    - "UIR-04: `layout.status_dot(state, label)` gains an optional third parameter `title=None`, appended after the two existing positional ones. Every existing call site keeps working unchanged — grep-verified: `history_page.py:702` and `:809`, `health_page.py:1752`, and `test_companion_app.py:568/571/702` all pass exactly 2 positional args. When `title` is falsy the emitted markup is byte-identical to today's; when truthy the `<span class=\"dot-label\">` gains an `escape_html()`-escaped `title` attribute (the same single-escaping discipline `concise_timestamp_html()` at layout.py:543/546 already uses for its own `title`)."
    - "UIR-04: History's desktop Corroboration cell (`_history_table_html()` L702-703) and the mobile card's Corroboration `<dd>` (`_history_cards_html()` L809) both pass `row[\"corroboration_title\"]` as that third argument, so the tooltip exists in both renderings."
    - "UIR-04: `health_page.py` is NOT touched. `_CORROBORATION_ROWS` (L136-147) keeps `\"Single-source (uncorroborated)\"` as its own visible label — Health's version is explicitly out of scope."
    - "UIR-04: the cross-page drift guard survives as a real guard rather than being deleted. `test_view_pages.py`'s `_corroboration_copy_agrees_with_health_page` (L751-785) currently asserts `history_labels[key] == health_rows[key]` for all three keys and WILL fail on the shortened label — it is restated, not removed: statuses must still agree key-by-key for all three; visible labels must still be identical for `True`/`False`; and for `None` the guard asserts History's visible label is the documented short form AND `history_page._CORROBORATION_TITLES[\"None\"]` equals Health's full label exactly, so a future edit to Health's copy still fails loudly. The existing `\"ok\"`-not-a-failure assertions for both pages are kept verbatim."
    - "UIR-04: `.data-table-wrap` gains a CSS-only, JS-free, pointer-inert scroll-edge affordance using the layered `background-attachment: local`/`scroll` gradient technique (the `background-attachment` half of the two options UIR-04's own fix note names). This is chosen over a bare `mask-image` fade because `.data-table` declares `width: 100%` (style.css L1669-1673), so an unconditional right-edge mask would fade the last column of a table that fits — the exact regression the task forbids. With `local`-attached opaque cover gradients over `scroll`-attached shadow gradients, the shadow is geometrically invisible when there is nothing to scroll, which is a property of the technique rather than an assumption, and is confirmed in-browser on Airlines' and Health's own tables in Task 4."
    - "UIR-04: the fade is theme-aware with no new tokens and no hardcoded colours. The cover gradients use the surface token the wrap actually sits on, and the shadow gradients use the `color-mix(in srgb, var(--color-text) N%, transparent)` muted idiom this stylesheet already uses for quiet treatments (`.section-caption` at L281-283, the `.copy-btn` washes). The accent-reservation list in style.css's header comment is NOT extended — no accent is involved."
    - "UIR-04: the correct surface token per call site is determined by grep, not guessed. History's table is returned by `render()` directly onto the page background (`--color-canvas`), while Health's `_registry_table_html()` (health_page.py:1961) and Airlines' own wrap may sit inside a `.page-section` card (`--color-dominant`, style.css L2628+). The executor greps both containers before writing the rule and adds a `.page-section .data-table-wrap` surface override ONLY if a wrap genuinely sits on a card surface; a mismatched cover colour would read as a coloured band, which Task 4's screenshots must rule out."
    - "New regression coverage lands with the fixes, in `companion/test_view_pages.py`, and the harness's own check-count constant is moved with a dated provenance comment in the file's existing idiom (view-pages was 43/43 at the head of this branch)."
    - "`scripts/run-all-tests.sh` reports ALL 16 canonical harnesses, server/ included, with their exact pass counts recorded in SUMMARY.md — no harness skipped."
    - "The browser verification runs against a COPY of `/tmp/skypane-prod-state`, never the original: `cp -R /tmp/skypane-prod-state/. <scratch>/` first, then `companion/app.py --state-dir <scratch>`."
  artifacts:
    - path: "companion/pages/history_page.py"
      provides: "AIRLINE_FALLBACK_TEXT, UNRESOLVED_LINK_CLASS, _CORROBORATION_TITLES, the hex-only branches in _callsign_hex_cell()/_history_cards_html(), and the shortened Single-source label"
      contains: "AIRLINE_FALLBACK_TEXT"
    - path: "companion/static/style.css"
      provides: ".cell-unresolved-link spacing rule and the .data-table-wrap scroll-edge gradient stack"
      contains: "cell-unresolved-link"
    - path: "companion/layout.py"
      provides: "status_dot()'s backward-compatible optional title parameter"
      contains: "def status_dot(state, label, title=None)"
    - path: "companion/test_view_pages.py"
      provides: "the restated corroboration drift guard plus new coverage for the three fixes"
      contains: "AIRLINE_FALLBACK_TEXT"
  key_links:
    - "history_page.AIRLINE_FALLBACK_TEXT <-> _type_airline_cell()'s comparison <-> _history_cards_html()'s mirrored comparison — all three must move together, or the link silently disappears from one rendering."
    - "history_page._CORROBORATION_TITLES['None'] <-> health_page._CORROBORATION_ROWS' 'None' label — the restated drift guard is the only thing keeping the tooltip honest once the visible labels diverge."
    - "layout.status_dot()'s new third parameter <-> its six existing 2-arg call sites — a non-default or repositioned parameter breaks History, Health and two harness checks at once."
    - ".data-table-wrap's cover-gradient colour <-> the surface each of the three tables actually sits on — a wrong token produces a visible coloured band instead of an invisible cover."
---

<objective>
Ship the three History-table findings from `06.6.4.1-UI-REVIEW.md` — UIR-05 (the airline fallback borrowing the route's wording, and the glued "View unresolved prefixes" link), UIR-06 (hex-only rows rendering a dead copy button and a blank primary slot), and UIR-04's History half (an overlong corroboration label plus a horizontally-scrollable table with no scroll affordance) — then measure, in a real browser, whether the desktop table now fits at 1280px and 1440px, a re-verification `06.6.1-UI-SPEC.md` explicitly deferred.

Purpose: three of History's seven columns are invisible at 1440px and the two worst offenders are content defects, not layout ones — a 434px Type column carrying a doubled "Route unavailable" and a 253px Corroboration column carrying a parenthetical. Fixing the copy is the real fix; the scroll-edge affordance is the safety net for whatever width remains.

Output: edits confined to `companion/pages/history_page.py`, `companion/static/style.css`, one backward-compatible signature addition in `companion/layout.py`, and the `companion/test_view_pages.py` assertions that pin them; plus a SUMMARY.md carrying before/after browser numbers in quick task 260902-v2v's Browser Measurement format.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-REVIEW.md
@.planning/quick/260902-v2v-ship-the-five-one-line-ui-fixes-from-06-/260902-v2v-SUMMARY.md
@companion/pages/history_page.py
@companion/static/style.css
@companion/layout.py
@companion/test_view_pages.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UIR-05 — an airline-specific fallback and a link that is not glued to it</name>
  <files>companion/pages/history_page.py, companion/static/style.css, companion/test_view_pages.py</files>
  <behavior>
    - A seeded row with no airline renders `Airline unknown` in the Type+Airline cell and `Route unavailable` in the Route cell — two different strings, in two different columns.
    - A seeded row with a resolved airline but no origin/destination still renders `Route unavailable` in the Route cell and still carries NO unresolved-airline link (the existing `_unresolved_link_keyed_on_airline_not_route` check, unedited, proves this).
    - A seeded row with no airline carries exactly one unresolved-airline anchor in the desktop `<tr>` and exactly one in the mobile `<li>` (the existing count check, unedited).
    - The rendered anchor carries the new spacing class, and that class name appears in style.css — the same cross-file drift discipline `_merged_cell_classes_agree_with_stylesheet` already applies to the `CELL_*` constants.
  </behavior>
  <action>
Read `companion/pages/history_page.py` L240-330 and L490-640 before editing.

First, confirm by grep that `ROUTE_FALLBACK_TEXT` stays correct where it lives: `grep -n "ROUTE_FALLBACK_TEXT" server/plane/render.py server/test_render.py`. It is defined at render.py:301 and consumed by that module's own route-line builders — this task must not touch `server/` at all.

1. Add a module-level constant next to History's other copy constants (near `UNRESOLVED_LINK_HREF`/`UNRESOLVED_LINK_TEXT` around L316-319):
   `AIRLINE_FALLBACK_TEXT = "Airline unknown"`, with a short comment stating it exists specifically so a missing AIRLINE stops borrowing `panel_render.ROUTE_FALLBACK_TEXT`, which describes a different noun and made the same phrase appear twice per unresolved row (UIR-05).

2. In `format_event_row()` change only the `airline_label` fallback to `AIRLINE_FALLBACK_TEXT`. Leave the `route_label` fallback below it using `panel_render.ROUTE_FALLBACK_TEXT` exactly as it is.

3. In `_type_airline_cell()`, change the comparison to `if row["airline_label"] == AIRLINE_FALLBACK_TEXT:` and correct the docstring sentence that currently names `panel_render.ROUTE_FALLBACK_TEXT` as the compared constant, so the prose and the code agree.

4. In `_history_cards_html()`, change the mirrored `unresolved_link` comparison to `AIRLINE_FALLBACK_TEXT` and update the `# D-21:` comment above it that names the old constant. Both renderings must key on the same constant.

5. Add `UNRESOLVED_LINK_CLASS = "text-label cell-unresolved-link"` beside the other two link constants and have `_unresolved_link_html()` emit it as the anchor's `class` (escaped through the same `escape_html()` the function already applies to its href and text). Add the spacing rule to `companion/static/style.css` immediately after the `.cell-inline-sep` rule (around L1898-1901, the block that already owns inline cell spacing):
   `.cell-unresolved-link { margin-left: var(--space-sm); }`
   with a comment noting it uses an existing spacing token and follows `.nav-notification`'s own `margin-left: var(--space-xs)` precedent for an appended inline element — the separator itself uses `xs` on both sides, so the link takes the next step up to read as a distinct affordance rather than another inline part. Do not introduce a new token.

6. Add one check to `companion/test_view_pages.py` in the existing `check(...)` idiom covering: the two fallback strings are distinct and appear in their own columns for a no-airline row; the anchor's class name is present both in the rendered page and in style.css; and `panel_render.ROUTE_FALLBACK_TEXT` still appears exactly where the Route column puts it. Move the harness's check-count constant by the number of checks added, with a dated `quick task 260902-w4t` provenance comment in the file's existing style.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && git diff --stat -- server/ | grep -c . | grep -qx 0 && echo "server/ untouched"</automated>
  </verify>
  <done>An unresolved row's desktop cell reads "B77L · Airline unknown" followed by a visibly separated "View unresolved prefixes" link; the Route column independently reads "Route unavailable"; `server/` has zero diff; view-pages is green with its new check counted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UIR-06 — promote the hex to the primary slot on callsign-less rows</name>
  <files>companion/pages/history_page.py, companion/test_view_pages.py</files>
  <behavior>
    - Seeded row with `callsign: ""` and `hex: "34560d"`: the desktop Callsign+Hex `<td>` contains the hex inside `cell-primary`, the literal text `no callsign` inside `cell-secondary`, and exactly 1 `data-copy-value` occurrence whose value is the hex.
    - The same row's mobile `<li>` primary line contains the hex inside its `cell-primary mono` span and the `no callsign` note — the primary line is never empty.
    - Seeded row with both callsign and hex present: the desktop cell still contains exactly 2 `data-copy-value` occurrences and 2 `</button><span ... data-copy-feedback` pairs (the existing check, unedited).
    - Seeded row with `callsign: ""` and no hex: `render()` returns without raising and the cell contains 0 `data-copy-value` occurrences.
  </behavior>
  <action>
Read `_callsign_hex_cell()` (L594-611) and `_history_cards_html()`'s `primary` block (L760-769) before editing.

1. Restructure `_callsign_hex_cell(callsign, hex_value)` into three explicit branches, keeping the callsign-present path byte-identical to what it emits today:
   - `callsign` truthy: unchanged — primary span + `_copy_button_html(callsign, _COPY_CALLSIGN_LABEL)`, then, when `hex_value` is truthy, the separator span + secondary span + `_copy_button_html(hex_value, _COPY_HEX_LABEL)`.
   - `callsign` falsy and `hex_value` truthy: the hex goes in `<span class="%s">` with `CELL_PRIMARY_CLASS` followed by the SAME `_copy_button_html(hex_value, _COPY_HEX_LABEL)` call the secondary path already uses (reuse it, do not write a second call with a re-typed label), then the existing `CELL_SEPARATOR_CLASS` span carrying `CELL_SEPARATOR_TEXT` and a `CELL_SECONDARY_CLASS` span carrying the literal `no callsign` with NO copy button attached — it is presentational text, not data.
   - both falsy: an empty primary span, no copy button, no separator, no secondary.
   Extend the function's docstring to state the three branches and why the `no callsign` note gets no copy button (nothing to copy) and why the empty callsign gets none either (a button copying `""` is the dead affordance UIR-06 reported).

2. Define the note's text as a module-level constant next to the other copy constants (e.g. `NO_CALLSIGN_NOTE_TEXT = "no callsign"`) so the desktop cell and the mobile card cannot drift, and have both renderers read it by symbol.

3. Mirror the rule in `_history_cards_html()`'s `primary` block: when `row["callsign"]` is falsy and `row["hex"]` is truthy, the `cell-primary mono` span carries `row["hex"]` and a `<span class="cell-secondary">` carrying the note follows it, before the `history-card__time` span. Escape through the existing `escape_html()` calls, do not pre-escape at the call site. Add a comment tying this to the desktop branch so a future edit to one is visibly obliged to touch the other. No CSS change is needed — `.history-card__primary` is a flex row with `gap: var(--space-sm)` and `.history-card__time` keeps `margin-left: auto`.

4. Leave the `<details>` disclosure's `Callsign` `<dt>/<dd>` pair and its copy button exactly as they are, so the existing 3-copy-button mobile check keeps passing untouched.

5. Add one check to `companion/test_view_pages.py` covering all four behaviours listed above (hex-only desktop cell, hex-only mobile primary line, unchanged callsign-present counts, and the no-crash empty/empty case). Move the check-count constant again with the same dated provenance comment.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>A hex-only row shows the hex in bold mono as its primary value with a muted "no callsign" beside it and exactly one copy button, on both the desktop cell and the mobile card; the callsign-present branch is unchanged; view-pages is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UIR-04 — shorten the corroboration label and give an overflowing table an edge</name>
  <files>companion/pages/history_page.py, companion/layout.py, companion/static/style.css, companion/test_view_pages.py</files>
  <behavior>
    - `layout.status_dot("ok", "X")` returns markup byte-identical to today's (no `title` attribute) — the existing 2-arg harness checks in `test_companion_app.py` pass unedited.
    - `layout.status_dot("ok", "X", "Long form")` returns the same markup with an escaped `title="Long form"` on the `dot-label` span; a hostile title value renders escaped.
    - History's rendered Corroboration cell for a `None` row contains the visible text `Single-source`, does NOT contain the parenthetical as visible text, and DOES carry it inside a `title` attribute — in both the desktop `<td>` and the mobile `<dd>`.
    - `health_page._CORROBORATION_ROWS`' own `None` label still reads `Single-source (uncorroborated)` and Health's rendered page is unchanged.
    - `.data-table-wrap` carries the scroll-edge gradient declarations in style.css and no `pointer-events`-blocking overlay element is introduced.
  </behavior>
  <action>
Read `layout.status_dot()` (layout.py:1002-1012), `_CORROBORATION_LABELS` (history_page.py:262-267), the drift guard (`test_view_pages.py:751-785`), and `.data-table-wrap`/`.data-table` (style.css:1656-1673) before editing.

**3a — status_dot's optional title.** Grep every call site first: `grep -rn "status_dot(" companion/ server/ | grep -v "def status_dot"`. Confirm all six live call sites (`history_page.py:702`, `history_page.py:809`, `health_page.py:1752`, `test_companion_app.py:568/571/702`) pass exactly two positional arguments. Change the signature to `def status_dot(state, label, title=None):` and emit the `title` attribute only when `title` is truthy, escaping it with the same `escape_html()` the function already applies to `label` — the identical single-escaping discipline `concise_timestamp_html()` uses for its own `title` at layout.py:543/546. When `title` is falsy the returned string must be character-for-character what it is today. Document the backward-compatibility contract in the docstring.

**3b — the shortened label plus its tooltip.** In `history_page.py`:
- Change `_CORROBORATION_LABELS["None"]` to `("ok", "Single-source")`. Leave `True`/`False` untouched.
- Add `_CORROBORATION_TITLES = {"None": "Single-source (uncorroborated)"}` (other keys absent, resolved via `.get(key, "")`), with a comment stating this is the long form deliberately moved out of the visible label per UIR-04, that `health_page._CORROBORATION_ROWS` still carries it as ITS visible label by design, and that the restated drift guard in `test_view_pages.py` is what keeps the two in sync.
- Have `format_event_row()` return `"corroboration_title": _CORROBORATION_TITLES.get(row.get("corroborated"), "")` alongside the existing status/label keys.
- Pass `row["corroboration_title"]` as `status_dot()`'s third argument at both `_history_table_html()` L702-703 and `_history_cards_html()` L809.
- Do NOT touch `companion/pages/health_page.py`.

**3c — restate the drift guard.** `test_view_pages.py`'s `_corroboration_copy_agrees_with_health_page` currently asserts label equality for all three keys and WILL fail. Restate it (do not delete it) so it asserts: statuses agree key-by-key for all three; visible labels are still identical for `True` and `False`; History's `None` visible label is the short form; and `history_page._CORROBORATION_TITLES["None"] == health_rows["None"][1]` so a future edit to Health's copy still fails loudly. Keep both existing `"ok"`-is-not-a-failure assertions verbatim. Update the `check(...)` description sentence to describe the new contract accurately.

**3d — the scroll-edge affordance.** Determine the surface each `.data-table-wrap` sits on before writing the rule: History's comes straight out of `render()` onto the page background; grep `health_page.py` around its `_registry_table_html()` (L1961) and the Airlines page for a wrapping `.page-section` (style.css L2628+ gives that class `--color-dominant`). Then add to `.data-table-wrap` (style.css L1664-1667) a four-layer background stack:
- two opaque cover gradients (`linear-gradient` fading the surface colour to `transparent`, one anchored `left`, one `right`, each ~24px wide, `no-repeat`, `background-attachment: local`)
- two shadow gradients underneath them (`radial-gradient(farthest-side at 0 50% ...)` / `at 100% 50%`, colour `color-mix(in srgb, var(--color-text) 15%, transparent)` fading to `transparent`, ~12px wide, `no-repeat`, `background-attachment: scroll`)
Cover layers are listed FIRST so they paint above the shadows. Use `var(--color-canvas)` for the covers in the base rule, and add a `.page-section .data-table-wrap` override using `var(--color-dominant)` ONLY if the grep above shows a wrap genuinely sitting inside a `.page-section`.

Write a head comment on the rule explaining, in this file's existing explanatory register: why `background-attachment` was chosen over `mask-image` (`.data-table` declares `width: 100%`, so an unconditional right-edge mask would fade the last column of a table that already fits — the exact regression the fix must avoid); that the `local`-attached covers sit over the `scroll`-attached shadows so a non-scrollable table shows no shadow at all, by geometry rather than by a media query; that this needs no pseudo-element and therefore cannot intercept pointer events or interfere with the wrap's own scrolling; and that the shadow colour is a `color-mix` on `--color-text`, never a hardcoded grey, so both themes work with no per-theme value to keep in sync. Do not add JS, a new token, or an accent colour.

**3e — coverage.** Add checks to `companion/test_view_pages.py` for: `status_dot()`'s two-arg output being unchanged and its three-arg output carrying an escaped title; History's `None` row rendering `Single-source` visibly with the long form in a `title` in BOTH renderings; and `.data-table-wrap`'s rule in style.css declaring both `background-attachment` values and containing no `pointer-events` overlay. Move the check-count constant with the same dated provenance comment.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_status_pages.py && git diff --stat -- companion/pages/health_page.py | grep -c . | grep -qx 0 && echo "health_page untouched"</automated>
  </verify>
  <done>The Corroboration column reads "Single-source" with the parenthetical available as a tooltip in both renderings; Health's own label is unchanged and its page renders identically; `.data-table-wrap` carries a CSS-only, pointer-inert, theme-aware scroll-edge affordance; the restated drift guard still fails on a real drift.</done>
</task>

<task type="auto">
  <name>Task 4: full suite, real-browser measurement, and the deferred 1280/1440px fit verdict</name>
  <files>.planning/quick/260902-w4t-history-table-content-from-06-6-4-1-ui-r/260902-w4t-SUMMARY.md</files>
  <action>
1. Run the full suite and record EVERY harness's exact pass count — all 16, `server/` included, none skipped:
   `scripts/run-all-tests.sh`
   If any harness fails, fix it before proceeding; if a failure is genuinely pre-existing and unrelated, say so explicitly with the evidence rather than omitting the harness.

2. Stand up a throwaway state dir — never the original:
   `cp -R /tmp/skypane-prod-state/. "$SCRATCH/state"` then run `companion/app.py --state-dir "$SCRATCH/state"`.
   Point Playwright (`mcp__plugin_playwright_playwright__browser_run_code_unsafe`) at it. Do not start the server against `/tmp/skypane-prod-state` itself under any circumstance.

3. Measure and record, at 1280px and 1440px, in BOTH themes:
   - `/history` `.data-table-wrap` `scrollWidth` vs `clientWidth` — record both numbers at both widths whether or not the table now fits. This is the re-verification `06.6.1-UI-SPEC.md` deferred. If it still overflows, that is an ACCEPTABLE, DOCUMENTED outcome: the scroll-edge fade is the safety net. Do NOT invent a column-hiding or truncation mechanism to force a fit.
   - The rendered `<td>` width of the Type+Airline and Corroboration columns, against the audit's own 434px / 253px before-numbers.
   - That no row's text contains `Route unavailable` twice (count occurrences within a single `<tr>`).
   - The computed pixel gap between the airline text and the "View unresolved prefixes" link — report the measured number, not "looks separated".
   - A hex-only row (there are ~4 in the snapshot; find one by querying for a `<td>` whose `cell-primary` matches a hex pattern): confirm the hex renders as `cell-primary`, `no callsign` renders as `cell-secondary`, and the cell contains exactly one `[data-copy-value]`. Repeat for its mobile card at 375px, and confirm the card's primary line is non-empty.
   - The Corroboration cell's visible `textContent` is `Single-source` and its `title` attribute carries the full parenthetical form.

4. Screenshot the scroll-edge fade on any table that still overflows, and screenshot `/airlines` and `/health` to confirm no fade band appears on a table that fits — the "must not visually break a non-overflowing table" requirement is verified here, not assumed. Check both themes for the fade's colour match against its surface.

5. Write `260902-w4t-SUMMARY.md` with a **Browser Measurement (before/after)** table matching quick task 260902-v2v's SUMMARY format exactly (`| Measurement | Viewport | Before (from 06.6.4.1-UI-REVIEW.md) | After (this branch, 2026-09-02) |`), a **Test Harness Results** table listing all 16 harnesses with their counts and an explicit overall PASS/FAIL, and a short verdict paragraph on the 1280/1440px fit question stating plainly whether the table now fits, with the numbers behind it.
  </action>
  <verify>
    <automated>grep -q "Browser Measurement (before/after)" .planning/quick/260902-w4t-history-table-content-from-06-6-4-1-ui-r/260902-w4t-SUMMARY.md && grep -c "test_" .planning/quick/260902-w4t-history-table-content-from-06-6-4-1-ui-r/260902-w4t-SUMMARY.md | awk '$1>=16{exit 0} {exit 1}'</automated>
  </verify>
  <done>All 16 harnesses reported with exact counts and an overall verdict; the `.data-table-wrap` scrollWidth/clientWidth numbers at 1280px and 1440px are recorded whether or not the table fits; every UIR-05/06/04 acceptance point is confirmed in a real browser at 1280px, 1440px and 375px in both themes; SUMMARY.md follows 260902-v2v's format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite `runway_events` row -> rendered History HTML | Stored callsign/hex/airline values, originally sourced from third-party ADS-B/enrichment APIs, are interpolated into attributes and element bodies |
| Local filesystem state dir -> running companion server | The verification step points a live server at a directory copy; pointing it at the real snapshot would let a POST mutate the developer's captured production state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w4t-01 | Tampering | `layout.status_dot()`'s new `title` attribute | medium | mitigate | `title` is routed through the existing `escape_html()` exactly once, matching `concise_timestamp_html()`'s own `title` handling; Task 3's new harness check feeds a hostile value and asserts the escaped form |
| T-w4t-02 | Tampering | `_unresolved_link_html()`'s new `class` attribute | low | mitigate | The class is a module-level constant, never a row-derived value; it goes through the same `escape_html()` call the href and text already use |
| T-w4t-03 | Tampering | `_callsign_hex_cell()`'s hex-only branch | medium | mitigate | The promoted hex reuses the existing `_copy_button_html()` and `escape_html()` call paths (T-06.6.3-11's original mitigation), introducing no second, unescaped derivation of the same value; the existing hostile-value merged-cell check covers the branch |
| T-w4t-04 | Tampering | Verification against `/tmp/skypane-prod-state` | high | mitigate | Task 4 mandates `cp -R` to a scratch dir first and forbids starting the server against the original path; the companion's Settings page POSTs into its state dir |
| T-w4t-05 | Denial of Service | `.data-table-wrap` four-layer background stack | low | accept | Static CSS gradients on one scroll container; no JS, no scroll listener, no layout-thrashing observer — cost is a single compositor paint |
| T-w4t-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this task installs no packages; the stdlib-only companion and the existing `server/.venv` are unchanged |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` green across all 16 harnesses, with counts recorded (view-pages moves above its 43 baseline by the number of checks added in Tasks 1-3).
- `git diff --stat -- server/ companion/pages/health_page.py` is empty — neither `ROUTE_FALLBACK_TEXT` nor Health's `_CORROBORATION_ROWS` is touched.
- `grep -rn "status_dot(" companion/ server/ | grep -v "def status_dot"` shows every pre-existing call site still passing two positional arguments.
- Real-browser confirmation of all six UIR acceptance points at 1280px, 1440px and 375px in both themes, with numbers in SUMMARY.md.
</verification>

<success_criteria>
- An unresolved row reads `B77L · Airline unknown` + a measurably separated `View unresolved prefixes` link; `Route unavailable` appears at most once per row, in the Route column.
- A hex-only row shows the hex as its bold-mono primary value with a muted `no callsign` secondary and exactly one copy button, on desktop and on the mobile card.
- The Corroboration column reads `Single-source`, with `Single-source (uncorroborated)` reachable as a tooltip; Health's own label is unchanged and its drift guard still guards.
- `.data-table-wrap` announces horizontal overflow with a CSS-only, pointer-inert, theme-aware edge treatment that is invisible on a table that fits — confirmed by screenshot on Airlines and Health, not by assumption.
- The deferred 1280/1440px fit question is answered with measured numbers, whatever the answer is.
- Four commits, one per task, conventional-commit style with the `(quick-260902-w4t)` scope.
</success_criteria>

<output>
Create `.planning/quick/260902-w4t-history-table-content-from-06-6-4-1-ui-r/260902-w4t-SUMMARY.md` when done.
</output>
