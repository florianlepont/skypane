---
phase: quick-260902-l0b
plan: 260902-l0b
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - server/history_db.py
  - server/test_config_history.py
  - companion/pages/health_page.py
  - companion/test_status_pages.py
  - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
autonomous: true
requirements: [QUICK-260902-l0b]

must_haves:
  truths:
    - "The developer's request has two halves and both must land: the chart shows the evolution over MANY DAYS (`ce qui m'intéresse, c'est globalement l'évolution sur plusieurs jours`), and the latest computed reading is STILL SHOWN (`Et la dernière valeur calculée` — additive, not a replacement). A plan that delivers the 3-month chart but drops the resting readout has failed half the request."
    - "The window is locked at 3 months / 90 days. The developer rejected 7-day and 30-day alternatives out loud (`Mais j'imaginais quand même quelque chose à trois mois`). Do not narrow it."
    - "Aggregation is DAILY AVERAGE, server-side, one point per calendar day. Intra-day min/max amplitude is explicitly NOT shown. Do not add a band, a bar chart, or a second series."
    - "No schema migration is needed and none may be written. D-13 already keeps `device_health` forever; `BATTERY_TREND_LIMIT` is a DISPLAY limit, not a retention policy (its own comment and `battery_trend_rows()`'s docstring both say so). This task adds one aggregate SELECT and changes what the page plots — nothing is deleted, no table changes."
    - "VERIFIED DURING PLANNING against a real SQLite: `date(ts)` on this project's stored timestamp shapes returns the right UTC calendar day for every form the writer can produce — `2026-09-02T02:00:00+00:00` (the `utc_now_iso()` / epoch-converted form) -> `2026-09-02`; the `Z` suffix, the naive form, the space-separated form and a fractional-seconds form all parse; and an offset timestamp is CONVERTED to UTC first (`2026-09-02T01:30:00+02:00` -> `2026-09-01`). An unparseable string (`not-a-timestamp`, which the Caddy tailer can legitimately store when an entry's own `ts` field is a hostile string) returns NULL and must be filtered out, or it becomes a phantom NULL day bucket. Re-run this yourself before trusting it."
    - "The cutoff must be applied as `ts >= ?` (a plain string comparison against the indexed column), never as `date(ts) >= ?` — `idx_device_health_ts` exists and a function call on the left-hand side would discard it. `route_source_counts()`/`corroboration_counts()` already establish exactly this `since=` shape and their two-branch (with/without `since`) structure is the precedent to copy verbatim."
    - "`server/test_config_history.py`'s `_all_sql_uses_placeholders_not_string_formatting()` greps `history_db.py` for `%`-formatted and f-string SQL. The new query must be a literal string with `?` placeholders in both branches, exactly like its two neighbours, or that pre-existing check fails."
    - "REGRESSION RISK, the single most important thing in this plan: `_battery_section()` emits the readout ONLY when `sparkline_html` is non-empty, and the sparkline needs at least TWO plotted points. A device whose entire history sits inside one calendar day therefore collapses to ONE daily bucket, which would mean no chart AND no readout — a page that is strictly worse than today's for a freshly-deployed device on day 1. This is why Task 2 keeps a fallback to the raw-readings series whenever fewer than two day buckets exist. That fallback is NOT a simplified v1 of the feature: the 90-day daily chart ships complete in this task and renders the moment two days of history exist. The fallback fires only when the DATA cannot support a daily series at all."
    - "The fallback is also what keeps the blast radius small, and that is a consequence rather than the reason: every existing direct-call harness fixture (`_sparkline_axis_labels_present_with_real_min_max`, `_sparkline_svg_has_per_point_interactive_markup`, `_sparkline_scale_bounded_at_one_across_real_container_widths`, `_sparkline_axis_chrome_present`) passes rows explicitly and takes the non-daily path unchanged, and every seeded-render fixture (`_battery_trend_shows_all_readings_and_one_sparkline`, `_battery_readings_collapsed_behind_closed_disclosure_after_chart`, `_battery_readout_seeded_with_latest_reading_not_placeholder`, `_single_reading_still_no_chart_no_readout_no_script`, `_page_allows_exactly_one_scoped_script_no_inline_handlers`) seeds readings minutes apart on one day and therefore takes the fallback path unchanged. Confirm that by running the harness, not by trusting this sentence."
    - "`battery_status()` — the abnormal-drop anomaly signal feeding `collect_anomalies()` and the nav-tab dot — must KEEP scanning the raw `trend_rows`, never the daily averages. Averaging a day's readings is precisely the operation that hides a sudden drop, and `_battery_section()` hands its second return value straight to `collect_anomalies()`. Changing that input would silently disarm a health signal while every test still passed."
    - "The raw-readings `<details>` disclosure (`View N readings`) and its `Timestamp`/`Battery (mV)` table must KEEP rendering the raw rows. It is the only place the exact per-reading values remain visible once the chart is aggregated, so it becomes MORE load-bearing after this task, not less."
    - "CONFIRMED, answering the question CONTEXT.md asked: `_battery_readout_block()` is keyed off `_latest_numeric_battery_reading(trend_rows)` — the first numerically-valid row of `recent_device_health(limit=20)`, newest-first — which is the same row `latest_device_health()` returns. It is NOT keyed off the chart series and must not become so. It stays the raw latest reading, humanised by `_battery_reading_parts()`, exactly as today."
    - "`companion/static/battery-trend.js` needs NO edit, and this is a planning FINDING that contradicts the task brief's own file list. `reveal()` reads `data-mv`/`data-ts`/`data-when` off whichever point is hovered and writes them through `textContent`; quick task 260901-uzi already moved all formatting server-side and shares the humanised string via `data-when` precisely so a server-side format change lands without touching this file. Daily points carry a day string in `data-ts` and a daily-average label in `data-when`, and the script is agnostic to both. Verify this by reading `reveal()` before concluding it; if you find a reason it must change, say so rather than editing it silently."
    - "`companion/static/style.css` needs NO edit either. Marker radii are `r` attributes emitted by Python, not CSS; the dense-series change emits fewer `.sparkline-dot` circles rather than restyling them; `.sparkline`'s grid, `.sparkline__x`'s space-between and `.sparkline-axis-label`'s 10px sizing all already do the right thing for two endpoint labels of any text. Confirm from source; report either way."
    - "Two harness gates block the plumbing and BOTH must be retargeted in place, never deleted. (1) `_battery_trend_timestamps_show_concise_format()` asserts `len(inspect.signature(health_page._battery_section).parameters) != 1`, pinning 06.5-02's call site while two phases executed concurrently — a concurrency that ended long ago. Adding a defaulted keyword parameter cannot break a call site, which is what that gate actually protects, so the gate is retargeted onto the property it means: the function stays callable with exactly one positional argument. (2) `_read_health_inputs_gained_no_new_key()` pins an exact five-key set; its stated intent is D-11, that the migrated REGISTRY/STATS reads stay separate calls in `render()`. A battery-chart read is neither, and belongs in the same atomic snapshot as `trend_rows`, so the set grows to six and the D-11 intent is restated explicitly in the check's own comment."
    - "`_battery_trend_section_html()`'s hard-coded `— Latest %d readings` caption becomes a lie the moment the chart plots days instead of readings, and it is also the ONLY on-page text that tells the developer what window they are looking at. It must state the real window AND the aggregation (`— Last 3 months, daily average`), and it must fall back to today's byte-identical `— Latest %d readings` string whenever the chart is actually showing raw readings. A caption that says one thing while the chart shows another is worse than no caption."
    - "At ~90 points the existing per-point markers stop working as a visual language rather than merely looking busy: `x` is a PERCENTAGE of the canvas width, so on a narrow mobile card 90 points sit roughly 3px apart while every cosmetic dot is 6px wide and every hit target is 16px wide. The dots merge into a caterpillar and the hit circles overlap almost completely. The developer asked for the same thin-line treatment, and preserving that treatment at 90 points MEANS suppressing the dots — keeping them is what would change the visual language."
    - "The server cannot know the canvas width — that is the whole point of quick task 260902-ep7's percentage-coordinate rework, and no plan may reintroduce a hand-estimated pixel width. The density rule must therefore key on POINT COUNT alone, with its threshold derived from the narrowest real canvas and that derivation written down beside the constant, exactly as `_SPARKLINE_VERTICAL_INSET_PERCENT` documents its own."
    - "Locale: the app's own UI text is English throughout (`Battery trend`, `Latest 20 readings`, `No battery readings yet.`, `HH:MM UTC`) even though the developer writes in French. Date labels follow the app, not the conversation. CONTEXT.md leaves the exact format to discretion and asks that existing precedent be checked first — the only precedent is `_axis_clock_label()`/`_battery_reading_parts()`'s `strftime(\"%H:%M\")`, which says nothing about dates."
    - "`strftime`'s no-pad day directive (the dash-prefixed one) is a glibc/BSD extension, absent on Windows and not guaranteed by the C standard. A day-of-month label must be composed from the parsed `datetime`'s own `.day` integer plus a `%b` month abbreviation, never from that directive."
    - "`companion/test_status_pages.py` runs at `EXPECTED_CHECK_COUNT` and `server/test_config_history.py` at its own; both are exact-equality gates, so every added check must move its file's constant by exactly the number added, with a provenance line in the comment block, and every retarget must move it by zero."
    - "Both harnesses green at their moved counts, and `scripts/run-all-tests.sh`'s FAILED list identical to the baseline measured on this tree before any edit — never compared against a remembered expectation."
  artifacts:
    - path: "server/history_db.py"
      provides: "`daily_battery_averages(conn, since=None)` — one newest-first row per UTC calendar day carrying the rounded mean `battery_mv` and the contributing reading count, NULL batteries and unparseable-timestamp buckets excluded, `?`-parameterised in both branches"
      contains: "260902-l0b"
    - path: "server/test_config_history.py"
      provides: "A check covering multi-day grouping, the rounded average, newest-first ordering, the `since` cutoff, and both exclusion paths (NULL battery, unparseable timestamp), with EXPECTED_CHECK_COUNT moved by exactly one"
      contains: "260902-l0b"
    - path: "companion/pages/health_page.py"
      provides: "A 90-day daily-average series driving the chart, with the raw-readings series still driving the readout, the anomaly scan and the disclosure table; a window/aggregation-honest heading caption; date endpoint labels and daily-average point labels; and a documented density rule for the dense series"
      contains: "260902-l0b"
    - path: "companion/test_status_pages.py"
      provides: "Two gates retargeted in place (the single-argument gate, the five-key gate) plus new checks for the daily series end-to-end, the day-1 fallback, the honest caption, the date labels and the dense-series marker rule"
      contains: "260902-l0b"
    - path: ".claude/skills/sketch-findings-skypane/references/visual-direction-typography.md"
      provides: "The Battery trend section entry updated to record what the chart now shows — a 90-day daily-average series, endpoint date labels, dots suppressed above the density threshold — under the file's own SUPERSEDED convention"
      contains: "260902-l0b"
  key_links:
    - from: "the new daily series feeding `battery_sparkline_svg()`"
      to: "`_battery_section()`'s `if sparkline_html:` guard, which gates the readout AND the script tag. One day bucket means an empty sparkline, which means no readout — the exact regression the fallback exists to prevent. Any change to the series selection must be re-checked against this guard."
    - from: "`daily_battery_averages()`'s returned row dicts"
      to: "`battery_sparkline_svg()`'s `row.get(\"battery_mv\")` / `row.get(\"ts\")` reader and its `isinstance(..., int)` filter. The day string must be returned under the key `ts` and the average must be a real `int`, or the plotting function silently drops every point and returns `\"\"` — a blank card with no error anywhere."
    - from: "`_read_health_inputs()` gaining a sixth key"
      to: "`_read_health_inputs_gained_no_new_key()`'s exact five-key set assertion, which fails immediately. Retarget it and restate the D-11 intent in its comment, or the next reader will re-narrow it."
    - from: "widening `_battery_section()` with a defaulted keyword parameter"
      to: "`_battery_trend_timestamps_show_concise_format()`'s `len(signature.parameters) != 1` assertion. It fails on a defaulted parameter that cannot break any call site — retarget it onto the positional-arity property it actually means."
    - from: "the heading caption's mode-dependent text"
      to: "`_battery_trend_heading_shows_d10_window_label()`, which renders an EMPTY state dir and greps for `Latest 20 readings`. An empty database has no chart at all, so the caption must state the default framing (the 3-month window) and that check must be retargeted onto it."
    - from: "`battery_status(trend_rows)`"
      to: "`collect_anomalies()` -> `overall_severity()` -> the Health nav-tab dot and the on-page anomaly banner. Feeding it daily averages instead of raw readings would disarm the abnormal-drop signal without failing a single existing test."
    - from: "suppressing cosmetic dots above the density threshold"
      to: "`_battery_empty_state_no_sparkline()` and every fixture asserting `SPARKLINE_DOT_CLASS`. All current fixtures are 3-row, far below the threshold, so they must stay green untouched — which is itself the assertion that the rule is threshold-gated and not unconditional."
---

<objective>
Redesign Health's battery-trend chart from "the latest 20 raw readings" to "the last 3 months, one server-aggregated daily average per calendar day", while keeping the latest computed reading on the page. The developer's own framing: `sur la courbe de la batterie, tu affiches seulement les dernières valeurs, alors que moi, ce qui m'intéresse, c'est globalement l'évolution sur plusieurs jours. Et la dernière valeur calculée.`

| # | What | Why |
|---|------|-----|
| 1 | Add `daily_battery_averages()` to `server/history_db.py` — a `GROUP BY date(ts)` aggregate over the last N days | The window is 90 days of history; only the server can collapse it to ~90 points, and `route_source_counts()`/`corroboration_counts()` already establish the exact query shape to follow |
| 2 | Feed the chart from that series, keep the readout, the anomaly scan and the raw table on the raw series, and make the heading caption honest about which window is on screen | This is the request. The split matters: averaging is right for a 3-month trend and wrong for an abnormal-drop alarm |
| 3 | Make the chart readable as a day series — endpoint labels as dates, point labels naming the daily average, and cosmetic dots suppressed once points outnumber the space for them | Same thin-line treatment the developer asked to keep, at 4.5x the point count. At 90 points the dots and hit circles overlap; preserving the treatment means dropping them |

Purpose: turn a chart that answers "what did the battery do in the last hour" into one that answers "is the battery draining, and how fast" — which is the question the developer actually has — without losing the exact latest reading they explicitly asked to keep.

Output: one new aggregate query with its own check, a page that plots ~90 daily points with a graceful fallback for a device younger than two days, two harness gates retargeted in place, new coverage for every new behaviour, and a design-system record of what the chart now shows.

**The multi-source audit for this quick task.** GOAL (the developer's two sentences) -> Tasks 2 and 3. CONTEXT decisions -> daily average / server-side `GROUP BY` (Task 1), 90-day window (Task 2), same sparkline treatment (Task 3), two date endpoint labels via the existing `<span>` axis mechanism (Task 3), readout unchanged (Task 2, asserted). CONTEXT discretion items -> query name and shape (Task 1), constant strategy (Task 2 — a new `BATTERY_TREND_WINDOW_DAYS` alongside a retained, re-scoped `BATTERY_TREND_LIMIT`), date format (Task 3), caption rewording (Task 2), and the ship-as-one-quick-task question, answered YES below. No item is unplanned; nothing is deferred.

**Scope verdict, since CONTEXT.md left it open.** This fits one quick task. It is a new SELECT plus a series swap in one page module — no migration, no new dependency, no new file, no JavaScript, no stylesheet change, and four source files. It does NOT need a roadmap phase. The one thing that could have forced an escalation — a retention change — is already ruled out by D-13.

**Non-goals — verified during planning, deliberately NOT touched.**
- **`companion/static/battery-trend.js`.** The task brief lists it; planning found it needs no edit, and Task 3 requires the executor to confirm that from `reveal()`'s source rather than accept it.
- **`companion/static/style.css`.** Marker radii are Python-emitted `r` attributes; the axis-label spans already size themselves to any text.
- **Intra-day min/max amplitude, a band, a bar chart, a second series, month-by-month axis ticks.** All explicitly excluded by CONTEXT.md.
- **`battery_status()`'s thresholds and `BATTERY_DROP_WARN_MV`.** The anomaly scan keeps its raw input and its provisional threshold; nothing here reopens phase 5's pending measurement.
- **The `<details>` disclosure, its table, its `Timestamp` column format, and `_battery_reading_parts()`'s existing clock shape.** Unchanged.
- **Retention.** Nothing is deleted from `device_health`, here or anywhere.
- **Every page other than Health**, and every other consumer of `recent_device_health()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@.planning/quick/260902-l0b-redesign-the-companion-health-page-batte/260902-l0b-CONTEXT.md
@.claude/skills/sketch-findings-skypane/SKILL.md

@server/history_db.py
@companion/pages/health_page.py
@companion/static/battery-trend.js
@companion/test_status_pages.py
@server/test_config_history.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the daily-average aggregate query to history_db.py</name>
  <files>server/history_db.py, server/test_config_history.py</files>
  <read_first>
    - `server/history_db.py`'s `route_source_counts()` and `corroboration_counts()` in full (around lines 226-257). These are the two-branch `since=None` / `since=<iso>` pattern to copy verbatim: a literal SQL string per branch, `?` placeholders, no concatenation, no formatting. Note that both compare `ts >= ?` directly against the indexed column.
    - `recent_device_health()` and `latest_device_health()` (around lines 260-272) — the `ORDER BY ts DESC, id DESC` newest-first convention and the `[dict(row) for row in rows]` return shape the new function must match.
    - `init_schema()`'s `device_health` table and `idx_device_health_ts` (around lines 122-135). Confirm for yourself that `battery_mv` is nullable and that the `ts` index exists — both drive the query's shape.
    - The module docstring's D-13 / Pitfall 1 paragraph (lines 13-19), so the new function's own docstring says the right thing about retention: this is a read, and it deletes nothing.
    - `tail_caddy_battery_log()`'s `ts` handling (around lines 351-357). This is the proof that a stored `ts` can be an arbitrary attacker-influenceable string from a log entry, which is exactly why the query must exclude buckets whose `date(ts)` is NULL.
    - `server/test_config_history.py`'s `EXPECTED_CHECK_COUNT` (line 26), the `check(name, fn)` helper (around line 47), one nearby `device_health` check for the fixture idiom (`_ingest_caddy_battery_log_is_idempotent()`, around line 657), and `_all_sql_uses_placeholders_not_string_formatting()` (around line 690) — the pre-existing gate the new SQL must satisfy.
  </read_first>
  <action>
**A. Write `daily_battery_averages(conn, since=None)` into the Readers section, immediately after `latest_device_health()`.**

It returns one dict per UTC calendar day that has at least one numeric battery reading, newest day first, each carrying: the day itself under the key `ts` (a `YYYY-MM-DD` string), `battery_mv` as a real Python `int` (the rounded mean of that day's readings), and `reading_count` as the number of readings that contributed.

The key is named `ts` deliberately, not `day`, and that choice must be explained in the docstring: it makes these rows structurally interchangeable with `recent_device_health()`'s rows for `battery_sparkline_svg()`, which reads exactly `battery_mv` and `ts` off whatever it is given. One plotting function, one row contract, no adapter layer.

The query selects `date(ts)` as the day, `AVG(battery_mv)` and a count, filters `battery_mv IS NOT NULL` and `date(ts) IS NOT NULL`, groups by `date(ts)`, and orders by the day descending. Two literal-string branches, with and without the `since` predicate, mirroring `route_source_counts()` exactly. The `since` predicate is `ts >= ?` against the raw indexed column — never a function call on the left-hand side, which would discard `idx_device_health_ts`. Rounding happens in Python (`int(round(...))` on the float `AVG` returns), not in SQL.

**B. Write the docstring around the four facts planning verified against a real SQLite, so the next reader does not have to re-derive them.** That `date()` converts an offset timestamp to UTC before taking the calendar day, so buckets are UTC days — consistent with every other timestamp on the Health page, which is labelled UTC. That `date()` returns NULL for an unparseable string, that the Caddy tailer can legitimately store such a string, and that this is why the NULL filter exists rather than being defensive noise. That `AVG` ignores NULLs but the explicit `battery_mv IS NOT NULL` filter is kept anyway so the count means "readings that contributed to this average" rather than "rows on this day". And that D-13's keep-forever retention is untouched: this function reads, and the `since` bound is a display window exactly as `BATTERY_TREND_LIMIT` is.

Re-verify each of those yourself against the real library before writing them down as fact. Where your result differs from this plan's, trust yours and say so.

**C. Add one check to `server/test_config_history.py`,** beside the existing `device_health` checks and using the same `tempfile.mkdtemp` / `history_db.open_db()` idiom. Seed several readings per day across three consecutive days with values whose daily means are unambiguous, plus three rows that must each be excluded for a different reason: one with a NULL battery, one with an unparseable timestamp, and one real reading older than the cutoff. Then assert, one failure message per property: exactly three buckets come back; they are newest-first; the day strings are the expected calendar days; each `battery_mv` is a real `int` equal to the hand-computed mean; each `reading_count` is the real number of contributing readings; no bucket carries a NULL or absent day; and the out-of-window and NULL-battery values appear nowhere in the result. Add a second assertion pass with `since=None` proving the out-of-window row DOES come back when no bound is given, and a third proving an empty database returns an empty list rather than raising.

Move `EXPECTED_CHECK_COUNT` from its real on-disk value by exactly one and add a provenance line naming this quick task, following the file's existing comment style.

**D. Falsifiability pass.** Break the new check's exclusion half (assert the NULL-timestamp bucket IS present), run the harness, confirm exactly that one check reports FAIL, then restore it. An assertion never observed failing is not an assertion.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys; sys.path.insert(0, '.')
import tempfile, shutil, inspect
from datetime import datetime, timezone, timedelta
from server import history_db
assert hasattr(history_db, 'daily_battery_averages'), 'the new reader must exist'
params = inspect.signature(history_db.daily_battery_averages).parameters
assert list(params) == ['conn', 'since'], 'signature must be (conn, since=None), got %r' % list(params)
assert params['since'].default is None, 'since must default to None like route_source_counts()'
d = tempfile.mkdtemp()
try:
    now = datetime(2026, 9, 2, 12, 0, 0, tzinfo=timezone.utc)
    with history_db.open_db(d) as conn:
        assert history_db.daily_battery_averages(conn) == [], 'an empty database must return an empty list'
        for day in range(3):
            for hour, mv in ((2, 4000 + day), (14, 4100 + day), (23, 4200 + day)):
                ts = (now - timedelta(days=day)).replace(hour=hour).isoformat(timespec='seconds')
                history_db.record_device_health(conn, ts, battery_mv=mv)
        history_db.record_device_health(conn, 'not-a-timestamp', battery_mv=9999)
        history_db.record_device_health(conn, now.isoformat(timespec='seconds'), battery_mv=None)
        history_db.record_device_health(conn, (now - timedelta(days=200)).isoformat(timespec='seconds'), battery_mv=1234)
        cutoff = (now - timedelta(days=90)).isoformat(timespec='seconds')
        rows = history_db.daily_battery_averages(conn, since=cutoff)
        unbounded = history_db.daily_battery_averages(conn)
    assert len(rows) == 3, 'expected exactly three day buckets inside the window, got %r' % (rows,)
    days = [r['ts'] for r in rows]
    assert days == ['2026-09-02', '2026-09-01', '2026-08-31'], 'newest-first UTC calendar days expected, got %r' % days
    for r, expected in zip(rows, (4100, 4101, 4102)):
        assert isinstance(r['battery_mv'], int) and not isinstance(r['battery_mv'], bool), 'battery_mv must be a real int (the plotting filter rejects anything else): %r' % (r,)
        assert r['battery_mv'] == expected, 'daily mean wrong: %r expected %d' % (r, expected)
        assert r['reading_count'] == 3, 'reading_count must be the contributing reading count: %r' % (r,)
    plotted = [r['battery_mv'] for r in rows]
    assert 9999 not in plotted, 'an unparseable-timestamp row must not form a bucket'
    assert 1234 not in plotted, 'a row older than the cutoff must be excluded'
    assert None not in [r['ts'] for r in rows], 'no NULL day bucket may survive'
    assert len(unbounded) == 4 and unbounded[-1]['battery_mv'] == 1234, 'since=None must return every day, got %r' % (unbounded,)
    print('daily_battery_averages ok: 3 bounded buckets, 4 unbounded, all exclusions hold')
finally:
    shutil.rmtree(d, ignore_errors=True)
" && server/.venv/bin/python3 -c "
import re
src = open('server/history_db.py').read()
assert '260902-l0b' in src, 'the new reader must be greppable to this quick task'
i = src.index('def daily_battery_averages')
body = src[i:src.index('\ndef ', i + 1)]
assert 'date(ts) >= ?' not in body, 'the cutoff must compare the raw indexed ts column, not a function call on it'
assert 'ts >= ?' in body, 'the cutoff must use the indexed ts >= ? form route_source_counts() established'
assert 'IS NOT NULL' in body, 'NULL batteries and unparseable-timestamp buckets must both be filtered'
assert body.count('conn.execute(') >= 2, 'two literal-string branches expected, mirroring route_source_counts()'
for line in body.splitlines():
    if 'execute(' in line or 'SELECT' in line:
        assert 'f\'' not in line and 'f\"' not in line, 'no f-string SQL: %r' % line
        assert ' % ' not in line, 'no %-formatted SQL: %r' % line
        assert '+' not in line, 'no concatenated SQL: %r' % line
print('query shape ok')
" && server/.venv/bin/python3 server/test_config_history.py && server/.venv/bin/python3 -c "
import re, subprocess
src = open('server/test_config_history.py').read()
expected = int(re.search(r'^EXPECTED_CHECK_COUNT = (\d+)', src, re.M).group(1))
assert '260902-l0b' in src, 'the new check must record its provenance'
out = subprocess.run(['server/.venv/bin/python3', 'server/test_config_history.py'], capture_output=True, text=True).stdout
passed, total = map(int, re.findall(r'(\d+)/(\d+)', out)[-1])
assert passed == total == expected, 'printed %d/%d against EXPECTED_CHECK_COUNT %d' % (passed, total, expected)
print('config-history green at %d checks' % expected)
"</automated>
  </verify>
  <done>
`server/history_db.py` exposes `daily_battery_averages(conn, since=None)` returning newest-first, one dict per UTC calendar day, with the day under the key `ts`, a real `int` rounded mean under `battery_mv`, and the contributing `reading_count` — NULL batteries, unparseable-timestamp buckets and out-of-window rows all excluded, both branches `?`-parameterised against the raw indexed `ts` column. Its docstring records the four SQLite facts (UTC conversion, NULL on unparseable input, why the explicit NULL filter is kept, and that D-13 retention is untouched), each re-verified by the executor. `server/test_config_history.py` covers grouping, rounding, ordering, the cutoff and all three exclusion paths, its `EXPECTED_CHECK_COUNT` moved by exactly one with a provenance line, the pre-existing placeholder-discipline check still green, and the new check observed failing under mutation before restoration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plot the 90-day daily series, keep the readout and the anomaly scan on the raw one, and make the caption honest</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - `health_page.py`'s `BATTERY_TREND_LIMIT` and its comment (around lines 94-99), and `battery_trend_rows()` (around line 424). Both already state that the limit is a display choice and not a retention policy — that is the sentence this task extends rather than contradicts.
    - `_cutoff_iso()` (around line 386) and the `_CORROBORATION_WINDOW_DAYS` line in `_read_health_inputs()` (around line 1765). This is the in-file precedent for turning an ISO `now` plus a day count into a `since=` bound, and the new read sits directly beside it.
    - `resolution_stats()` (around line 1540) as the second precedent — a windowed reader in this same module, with its own `window_days` constant.
    - `_battery_section()` in full (around lines 1327-1416). Trace every one of its four consumers of `trend_rows` separately and decide, one at a time, which series each takes after this task: `battery_status()` (raw — the anomaly scan), the `table_rows` comprehension and the `View %d readings` summary (raw), `battery_sparkline_svg()` (the chart — this is the one that changes), and `_latest_numeric_battery_reading()` feeding the readout (raw). Also read the `if sparkline_html:` guard carefully: it gates the readout AND the script tag together.
    - `compute_health_state()` (around line 856) and `render()`'s unpacking of its dict (around lines 1822-1834), plus `_battery_trend_section_html()`'s call site near the end of `render()`. Note that `source_fault_raw` is already an input passed straight through the state dict — the precedent for the caption doing the same.
    - `_battery_trend_section_html()` in full (around lines 1245-1324), especially its own note that a grep confirmed nothing pins its arity, and the `— Latest %d readings` caption span.
    - `companion/test_status_pages.py`: `_battery_trend_timestamps_show_concise_format()`'s first two lines (around line 662) — the single-argument gate; `_read_health_inputs_gained_no_new_key()` (around line 2200) — the five-key gate; `_battery_trend_heading_shows_d10_window_label()` (around line 722) — the empty-render caption grep; and `_battery_trend_shows_all_readings_and_one_sparkline()` (around line 614) plus `_battery_readings_collapsed_behind_closed_disclosure_after_chart()` (around line 689), whose same-day fixtures are what this task's fallback keeps green.
    - `EXPECTED_CHECK_COUNT` (line 224) and its provenance block. Read the real value.
  </read_first>
  <action>
**A. Constants.** Add `BATTERY_TREND_WINDOW_DAYS = 90` beside `BATTERY_TREND_LIMIT`, with a comment recording that 3 months is the developer's explicitly locked window (7-day and 30-day alternatives were rejected in discussion), that D-13's keep-forever retention makes this a display window and not a retention bound — the same distinction the neighbouring constant already draws — and that it bounds a `ts >= ?` read, deleting nothing.

Keep `BATTERY_TREND_LIMIT` at its current value and RE-SCOPE its comment: after this task it no longer bounds the chart at all. It bounds the raw-readings disclosure table, the abnormal-drop scan, and the fallback chart. Say which three, so the next reader does not assume the chart is still keyed off it.

**B. The reader.** Add `battery_daily_rows(conn, now)` beside `battery_trend_rows()`, calling Task 1's aggregate with `since=_cutoff_iso(now, BATTERY_TREND_WINDOW_DAYS)`. Its docstring states the row shape it returns, that the rows are structurally interchangeable with `battery_trend_rows()`'s for plotting purposes, and that `_cutoff_iso()` returning `None` on an unparseable `now` degrades to an unbounded read rather than raising — decide deliberately whether that is the behaviour you want and write down which you chose and why. (Unbounded is the safer degradation here: it shows more history, never less, and never an empty card.)

**C. Plumbing.** Add `daily_rows` to `_read_health_inputs()`'s dict via `_safe_query()`, directly beside `trend_rows`, so both battery reads share the one atomic snapshot the function exists to provide. Extend the docstring's "five reads" language to six and say why this read belongs here while D-11's registry/stats reads deliberately do not: this is a battery-health read consumed by the same section builder, from the same table, in the same request.

Widen `_battery_section(trend_rows, daily_rows=None)`. A defaulted keyword parameter is chosen specifically because it cannot break the call site the existing gate was written to protect. Record that reasoning in the function's own docstring, in the same voice `_battery_trend_section_html()` used for its own widening.

**D. Series selection, and the fallback that prevents a day-1 regression.** Add a small module-level predicate — one place, called from both consumers — that answers "can a daily chart be drawn?": the daily rows are a real list (not the `_DB_UNAVAILABLE` sentinel), and there are at least two of them. Two is not arbitrary: it is exactly `battery_sparkline_svg()`'s own minimum, and duplicating that threshold anywhere else would let the two drift.

`_battery_section()` plots the daily rows when that predicate holds and the raw `trend_rows` otherwise. Everything else in the function keeps the raw series, unchanged: the anomaly scan, the table, the summary count, and the readout.

Write the fallback's reasoning into the docstring as a decision, not as an apology, and be precise about what it is and is not. It is NOT a reduced first version of the feature — the 90-day daily chart is complete in this task and renders the moment two calendar days of history exist. It fires only when the DATA cannot support a daily series, and its purpose is that a device on its first day keeps the chart and the readout it has today instead of losing both to an empty-sparkline branch. Name that branch (`if sparkline_html:`) explicitly so the coupling is discoverable from here.

**E. The honest caption.** Compute the caption from the same predicate, so the heading can never describe a window the chart is not showing: `— Last 3 months, daily average` when the daily series is on screen (and on an empty database, which is the default framing), and today's byte-identical `— Latest %d readings` string when the fallback series is. Carry it through `compute_health_state()`'s returned dict — `source_fault_raw` is the existing precedent for an input passed straight through — and give `_battery_trend_section_html()` a third parameter with a default that reproduces today's string, so any caller or check still passing two arguments keeps working.

Before adding the key, grep the harness for anything pinning `compute_health_state()`'s returned key set. If such a check exists, retarget it in place with the same reasoning as step F, do not work around it.

**F. Retarget two gates in place. Delete neither.**

`_battery_trend_timestamps_show_concise_format()`'s first assertion pins an exact parameter count. Retarget it onto the property that gate actually protects — that the function stays callable with exactly one positional argument, i.e. every parameter after the first carries a default — and say in its comment why the exact-count form was too strong: 06.5-02's concurrent-execution window closed long ago, and a defaulted keyword cannot break a call site. The rest of that check is untouched.

`_read_health_inputs_gained_no_new_key()` pins an exact five-key set. Grow it to six and restate D-11's real intent in the comment: the migrated REGISTRY and STATS reads stay separate calls in `render()`, which is unchanged and still asserted by the set's exactness. Rename the function and rewrite its `check(...)` description so neither still says "no new key", which will have stopped being true.

**G. New coverage — three checks.**

One: the daily series end-to-end. Seed several readings per day across three calendar days with distinct daily means, render, and assert the chart plots the three DAILY AVERAGES — assert each average appears as a point's `data-mv` value, and assert a raw value that is not any day's mean appears as no point's `data-mv` at all. That negative half is the check's substance; without it the check passes on a chart still plotting raw readings. Assert two trend-line segments for three points, and assert the raw values are still all present on the page (the disclosure table keeps them).

Two: the day-1 fallback. Seed three readings minutes apart on ONE day, render, and assert the chart still exists, the readout element is still present, and the caption reads the raw-readings form — i.e. the page a freshly-deployed device shows is not worse than today's.

Three: the caption is mode-honest. On the empty render it names the 3-month window; on the multi-day render it names the 3-month window; on the same-day render it names the readings count. One check, three renders.

Retarget `_battery_trend_heading_shows_d10_window_label()` onto the 3-month caption (its empty-state render has no chart, so the default framing is what it must assert) or fold it into check three — if you fold, the count math must account for the removal and the comment block must say so.

Move `EXPECTED_CHECK_COUNT` by exactly the number of checks added, with a provenance line naming this quick task.

**H. Green-suite pass.** Run the whole companion harness set. Every same-day fixture named in this plan's must-haves should still pass untouched; if one does not, do not adjust the fixture to make it pass — work out which of this plan's assumptions was wrong and say so.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys, re, inspect, tempfile, shutil; sys.path.insert(0, '.')
from datetime import datetime, timezone, timedelta
from companion.pages import health_page as h
from server import history_db

assert h.BATTERY_TREND_WINDOW_DAYS == 90, 'the window is locked at 3 months'
assert hasattr(h, 'battery_daily_rows'), 'the windowed daily reader must exist'
params = list(inspect.signature(h._battery_section).parameters.values())
assert len(params) >= 2, 'the section builder must now take the daily series too'
assert all(p.default is not inspect.Parameter.empty for p in params[1:]), 'every parameter after the first must have a default so the pinned call site cannot break'

def _render(readings, now):
    d = tempfile.mkdtemp()
    try:
        with history_db.open_db(d) as conn:
            for ts, mv in readings:
                history_db.record_device_health(conn, ts, battery_mv=mv)
        return h.render({'state_dir': d, 'now': now})
    finally:
        shutil.rmtree(d, ignore_errors=True)

def _mvs(html):
    return sorted(set(int(x) for x in re.findall(r'data-mv=\"(\d+)\"', html)))

now = datetime(2026, 9, 2, 12, 0, 0, tzinfo=timezone.utc)
iso = lambda dt: dt.isoformat(timespec='seconds')

multiday = []
for day in range(3):
    for hour, mv in ((2, 4000 + day), (14, 4100 + day), (23, 4200 + day)):
        multiday.append((iso((now - timedelta(days=day)).replace(hour=hour)), mv))
r = _render(multiday, iso(now))
plotted = _mvs(r)
assert plotted == [4100, 4101, 4102], 'the chart must plot the three DAILY AVERAGES, got %r' % (plotted,)
for raw in (4000, 4200, 4001, 4202):
    assert raw not in plotted, 'raw reading %d must not be a plotted point once the chart is aggregated' % raw
assert r.count(h.SPARKLINE_LINE_CLASS) == 2, 'three daily points means two trend-line segments, got %d' % r.count(h.SPARKLINE_LINE_CLASS)
for _ts, mv in multiday:
    assert str(mv) in r, 'the raw readings must still be visible in the disclosure table (%d missing)' % mv
assert 'Last 3 months' in r, 'the heading must name the real window when the daily series is on screen'
assert h.BATTERY_READOUT_ID in r, 'the latest computed reading must still be on the page'

sameday = [(iso(now - timedelta(minutes=m)), 4200 - m) for m in (2, 1, 0)]
f = _render(sameday, iso(now))
assert f.count(h.SPARKLINE_LINE_CLASS) == 2, 'a device with only one day of history must still get its raw-readings chart (day-1 regression guard), got %d segments' % f.count(h.SPARKLINE_LINE_CLASS)
assert h.BATTERY_READOUT_ID in f, 'the readout must not disappear on a device younger than two days'
assert ('Latest %d readings' % h.BATTERY_TREND_LIMIT) in f, 'the fallback chart must be captioned as readings, not as 3 months'
assert 'Last 3 months' not in f, 'the caption must never describe a window the chart is not showing'
assert _mvs(f) == sorted(set(mv for _t, mv in sameday)), 'the fallback must plot the raw readings, got %r' % (_mvs(f),)

e = _render([], iso(now))
assert 'Last 3 months' in e, 'an empty database must show the default 3-month framing'

d = tempfile.mkdtemp()
try:
    keys = set(h._read_health_inputs(d, iso(now)).keys())
finally:
    shutil.rmtree(d, ignore_errors=True)
assert 'daily_rows' in keys, 'the daily read belongs in the one atomic snapshot beside trend_rows'
assert not any('registr' in k or 'stat' in k for k in keys), 'D-11 unchanged: the registry/stats reads stay separate calls in render()'

spiky = []
for day in range(3):
    for hour, mv in ((2, 4100 + day), (14, 4100 + day), (23, 4100 + day)):
        spiky.append((iso((now - timedelta(days=day)).replace(hour=hour)), mv))
spiky.append((iso(now.replace(hour=3)), 4100 - h.BATTERY_DROP_WARN_MV - 50))
spiky.append((iso(now.replace(hour=4)), 4100))
a = _render(spiky, iso(now))
assert 'abnormal drop' in a, 'the anomaly scan must still see RAW consecutive readings — these daily averages are flat, so an averaged input would silently disarm it'

src = open('companion/pages/health_page.py').read()
assert '260902-l0b' in src, 'the change must be greppable to this quick task'
print('page ok: daily averages plotted, raw series still drives readout/table/anomaly, caption honest in all three modes')
" && server/.venv/bin/python3 -c "
import re, subprocess
src = open('companion/test_status_pages.py').read()
expected = int(re.search(r'^EXPECTED_CHECK_COUNT = (\d+)', src, re.M).group(1))
assert '260902-l0b' in src, 'the retargets and new checks must record their provenance'
# No negative grep for the old assertion text here, deliberately: the retarget's own
# comment legitimately quotes what it replaced. The real gate is the harness run below —
# a gate left at its exact-parameter-count form fails against the widened signature.
out = subprocess.run(['server/.venv/bin/python3', 'companion/test_status_pages.py'], capture_output=True, text=True).stdout
passed, total = map(int, re.findall(r'(\d+)/(\d+)', out)[-1])
assert passed == total == expected, 'printed %d/%d against EXPECTED_CHECK_COUNT %d' % (passed, total, expected)
print('status-pages green at %d checks' % expected)
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_contrast_check.py</automated>
  </verify>
  <done>
Health's battery chart plots one point per UTC calendar day over the last 90 days, sourced from Task 1's aggregate through a new `battery_daily_rows()` reader and a sixth key in `_read_health_inputs()`'s atomic snapshot. The raw series still drives all three of its other consumers, provably: the readout is still the latest raw reading, the disclosure table still lists the raw rows, and `battery_status()`'s abnormal-drop scan still never sees an average. A device with fewer than two calendar days of history keeps exactly the chart and readout it has today, and the heading caption names whichever window is genuinely on screen. Both blocking gates are retargeted in place onto the properties they actually meant, three new checks cover the daily series (including the negative half proving raw values are no longer plotted), the day-1 fallback and the mode-honest caption, `EXPECTED_CHECK_COUNT` moved by exactly the number added, and every companion harness is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make the chart read as a day series — date endpoint labels, daily-average point labels, and a density rule</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py, .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md</files>
  <read_first>
    - `battery_sparkline_svg()` in full (around lines 540-745), especially: the `pairs` construction and its `isinstance(..., int)` filter, `_point_x()`/`_point_y()`, the hard-coded `r=\"3\"` cosmetic marker and `r=\"8\"` hit target inside the point loop, the roving-tabindex `is_latest` condition, and `x_labels_html`'s two `<span class=\"sparkline-axis-label\">` endpoints. Note that `x` is a PERCENTAGE and every radius is an absolute pixel value — that asymmetry is the whole density problem.
    - `_axis_clock_label()` (around line 486) and `_battery_reading_parts()` (around line 498). These are the two label builders, their graceful-degradation contracts (fall back to the raw `ts` string, never raise), and the plain-unescaped-text contract every caller must keep honouring.
    - `_SPARKLINE_CANVAS_HEIGHT_PX` and `_SPARKLINE_VERTICAL_INSET_PERCENT`'s derivations (around lines 460-483). These are the model for how a chart constant is justified in this file: derived from a named criterion, with a re-derivation trigger written down.
    - The long comment above them (around lines 435-458) recording why hand-estimated pixel geometry was deleted outright by quick task 260902-ep7. Nothing in this task may reintroduce an assumed canvas width.
    - `companion/static/battery-trend.js`'s `reveal()` (lines 56-102) — enough to confirm for yourself that it reads `data-mv`/`data-ts`/`data-when` and writes them through `textContent` without parsing or reformatting either, and therefore needs no edit for a day series. Report your conclusion either way.
    - `companion/static/style.css`'s `.sparkline-dot`, `.sparkline-hit`, `.sparkline-hit:hover`, `.sparkline-hit:focus-visible` and `.sparkline-axis-label` rules (around lines 2983-3032), plus `.sparkline__x` (around line 2950). Confirm from source that no radius lives in CSS and that the endpoint label row sizes itself to its own text.
    - `companion/test_status_pages.py`'s four direct-call sparkline checks (around lines 1179, 1194, 1248, 1414). All four pass 3-row fixtures, so all four must stay green untouched — that is itself the proof the density rule is threshold-gated.
    - `.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md`'s `Battery trend section` bullet (around line 16) and the file's SUPERSEDED convention, plus `references/data-density.md`. Decide from their actual content which file the chart's new data-density record belongs in; put the treatment note where it fits and cross-reference rather than duplicating.
  </read_first>
  <action>
**A. A daily label mode on the chart.** Give `battery_sparkline_svg()` a third parameter, defaulted so every existing call site and every direct-call harness fixture keeps today's behaviour byte-for-byte, that says the rows being plotted are daily aggregates rather than individual readings. `_battery_section()` passes it alongside the series it already chose in Task 2 — one predicate decides both, so the labels can never disagree with the data.

**B. Date endpoint labels.** Add a sibling to `_axis_clock_label()` that renders a day-of-month plus an abbreviated month, and inherits the same never-raise fallback to the raw string. Compose the day from the parsed `datetime`'s own `.day` integer and the month from a `%b` directive — do NOT use `strftime`'s no-pad day flag (the dash-prefixed variant), which is a glibc/BSD extension and not portable; a verify gate greps the whole module for its absence. That gate is self-invalidating if you quote the directive in a comment explaining why you avoided it — describe it by name, as this sentence does, and never write the literal into the source. English, matching every other visible string on this page, and noted in the docstring as following the app rather than the conversation. The labels flow through the existing `<span>`-based `x_labels_html` mechanism untouched — no SVG text node, no gutter estimate, per 260902-ep7's own record.

Once the daily mode is the only real caller of the clock variant, decide deliberately whether the clock variant survives: it does, because the fallback series still uses it, and that is worth one sentence rather than a silent retention.

**C. Daily point labels.** Add a sibling to `_battery_reading_parts()` for aggregated points, returning the same plain-unescaped `(value, when)` tuple contract so the existing escape-at-interpolation discipline is unchanged and `battery-trend.js` keeps working with no edit. The `when` half names the day and, in the house parenthetical shape `_battery_reading_parts()` already uses for `HH:MM UTC (3m ago)`, says the point is a daily average and how many readings it averages — singular and plural both handled. When the count is missing, degrade to the date alone rather than printing an empty parenthetical.

This is what stops the readout from lying on hover: at rest it shows the latest raw reading, and hovering a chart point replaces it with an average — the label is the only thing that tells the developer which of the two they are looking at, so it must say so.

**D. The density rule.** Replace the two hard-coded radii in the point loop with named constants and add a point-count threshold above which the cosmetic dots are not emitted at all and the hit target uses a smaller radius. Name the threshold and the reduced radius so that both contain `DENSE` — this task's gate discovers them by that substring rather than by a hard-coded name, so it does not pin your naming beyond that one token; a literal left inline in the loop fails the gate.

Derive the threshold rather than picking it: at the narrowest real canvas (measure it — the mobile viewport minus the page and card padding, from the stylesheet, not from memory), find the point count at which the spacing between adjacent points equals the cosmetic dot's own diameter. Below that they read as separate marks; at or above it they merge into a continuous band, which is a different visual language from the thin line the developer asked to keep. Write the derivation and its re-derivation trigger beside the constant, in the style `_SPARKLINE_VERTICAL_INSET_PERCENT` already sets. Record the honest trade in the smaller hit radius's own comment: it costs tap-target size and buys pointing precision among heavily overlapping targets, the keyboard path (roving tabindex plus arrow keys, unchanged) is unaffected, and it is on the human-verification list because only a real device settles it.

Under no circumstances derive anything from an assumed rendered canvas width in the emitted geometry — the threshold is a design judgement about a worst case, the coordinates stay percentages.

**E. New coverage.** Add checks for: the date endpoint labels in daily mode (assert the real expected label strings for a known fixture, and assert the clock form does NOT appear); a daily point's `data-when` naming both the day and the daily average with its reading count; the density rule at and just below its threshold, asserting no cosmetic dot class survives above it, that the hit targets do (an unreachable chart would also have no dots), and that the smaller radius is in use; and one regression assertion that a below-threshold non-daily call is byte-for-byte what it is today. Move `EXPECTED_CHECK_COUNT` by exactly the number added, with a provenance line.

**F. Design-system record.** Update the `Battery trend section` bullet so it describes what the chart now shows — a 90-day daily-average series, two date endpoint labels, cosmetic dots suppressed above the density threshold, the same thin-line treatment and the same nested-card contract — under the file's own SUPERSEDED convention, naming what it showed before (the latest 20 raw readings with clock endpoint labels) and the developer's own reason for the change. Put the density threshold and its derivation wherever the two reference files' actual content says it belongs, and cross-reference rather than restating it in both.

**G. Full suite, against a measured baseline.** Before this task's first edit, run `scripts/run-all-tests.sh` and write its FAILED list to a file. After, run it again and diff. The two must be identical — no harness that passed may fail, and no pre-existing failure may be reported as this task's. Do not assert a remembered expectation of which harnesses fail on this tree; assert equality with what you measured.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys, re, inspect; sys.path.insert(0, '.')
from companion.pages import health_page as h

src = open('companion/pages/health_page.py').read()
assert '%-d' not in src, 'the no-pad day directive is not portable — compose the day from the datetime .day integer'

sig = list(inspect.signature(h.battery_sparkline_svg).parameters.values())
assert len(sig) >= 3 and all(p.default is not inspect.Parameter.empty for p in sig[1:]), 'the daily-mode flag must be defaulted so every existing call site is unchanged'
kw = sig[2].name

daily = [
    {'ts': '2026-09-02', 'battery_mv': 4100, 'reading_count': 12},
    {'ts': '2026-09-01', 'battery_mv': 4101, 'reading_count': 9},
    {'ts': '2026-08-31', 'battery_mv': 4102, 'reading_count': 1},
]
svg = h.battery_sparkline_svg(daily, now='2026-09-02T12:00:00+00:00', **{kw: True})
labels = re.findall(r'<span class=\"sparkline-axis-label\"[^>]*>([^<]*)</span>', svg)
assert len(labels) == 4, 'still exactly four axis labels, got %r' % (labels,)
x_labels = labels[2:]
assert all(re.fullmatch(r'\d{1,2} [A-Za-z]{3}', s) for s in x_labels), 'the X endpoints must be day-plus-month date labels, got %r' % (x_labels,)
assert x_labels == ['31 Aug', '2 Sep'], 'oldest-then-newest date endpoints expected, got %r' % (x_labels,)
assert '00:00' not in svg, 'a day string rendered through the clock formatter would print midnight — the daily mode must not do that'
whens = re.findall(r'data-when=\"([^\"]*)\"', svg)
assert len(whens) == 3, 'one humanised label per point, got %r' % (whens,)
assert '2 Sep' in whens[-1] and '12' in whens[-1], 'a daily point must name its day and its contributing reading count: %r' % whens[-1]
assert 'average' in whens[-1].lower(), 'a daily point must say it is an average, or the readout silently mixes averages with raw readings'
assert re.search(r'\b1 reading\b', whens[0]), 'a single-reading day must read singular, got %r' % whens[0]
for forbidden in ('url(', '<image', '<script'):
    assert forbidden not in svg, 'the no-external-reference guarantee must survive: found %r' % forbidden

sparse = [{'ts': '2024-01-01T0%d:00:00' % i, 'battery_mv': 4200 - i * 10} for i in range(3)]
base = h.battery_sparkline_svg(sparse)
assert h.SPARKLINE_DOT_CLASS in base, 'a below-threshold chart keeps its cosmetic dots — the rule must be threshold-gated'
assert 'r=\"8\"' in base, 'a below-threshold chart keeps its full-size hit target'
assert re.search(r'>\d{2}:\d{2}<', base), 'a non-daily chart keeps its clock endpoint labels'

thr = [c for c in dir(h) if 'DENSE' in c]
assert thr, 'the density threshold must be a named, documented constant, not a literal in the loop'
n = max(getattr(h, c) for c in thr if isinstance(getattr(h, c), int) and getattr(h, c) > 10)
dense = [{'ts': '2026-06-%02d' % ((i % 28) + 1), 'battery_mv': 4000 + i} for i in range(n + 5)]
dsvg = h.battery_sparkline_svg(dense, now='2026-09-02T12:00:00+00:00', **{kw: True})
assert h.SPARKLINE_DOT_CLASS not in dsvg, 'above the threshold the cosmetic dots must not be emitted'
assert dsvg.count(h.SPARKLINE_HIT_CLASS) == len(dense), 'every point must stay reachable — hit targets are not what gets dropped'
assert 'r=\"8\"' not in dsvg, 'the dense hit radius must be the smaller one'
assert dsvg.count(h.SPARKLINE_LINE_CLASS) == len(dense) - 1, 'the thin line still carries the trend, one segment per pair'
just_under = [{'ts': '2026-06-%02d' % ((i % 28) + 1), 'battery_mv': 4000 + i} for i in range(n - 1)]
assert h.SPARKLINE_DOT_CLASS in h.battery_sparkline_svg(just_under, now='2026-09-02T12:00:00+00:00', **{kw: True}), 'just below the threshold the dots must survive — the boundary must be the documented one'
print('labels and density ok: date endpoints, averaged point labels, dots dropped only above the threshold')
" && server/.venv/bin/python3 -c "
import subprocess
d = subprocess.run(['git', 'diff', '--name-only'], capture_output=True, text=True).stdout.split()
for forbidden in ('companion/static/battery-trend.js', 'companion/static/style.css'):
    assert forbidden not in d, '%s was found to need no edit during planning — if that finding is wrong, say so in the SUMMARY rather than editing silently' % forbidden
ref = open('.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md').read()
assert '260902-l0b' in ref, 'the design-system record must name this quick task'
assert 'SUPERSEDED' in ref, 'the round trip belongs under the file own supersede convention'
print('non-goals held, design-system record updated')
" && server/.venv/bin/python3 -c "
import re, subprocess
src = open('companion/test_status_pages.py').read()
expected = int(re.search(r'^EXPECTED_CHECK_COUNT = (\d+)', src, re.M).group(1))
out = subprocess.run(['server/.venv/bin/python3', 'companion/test_status_pages.py'], capture_output=True, text=True).stdout
passed, total = map(int, re.findall(r'(\d+)/(\d+)', out)[-1])
assert passed == total == expected, 'printed %d/%d against EXPECTED_CHECK_COUNT %d' % (passed, total, expected)
print('status-pages green at %d checks' % expected)
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 server/test_config_history.py && scripts/run-all-tests.sh > /tmp/skypane-l0b-run-all-tests.log 2>&1; sed -n '/FAILED harnesses/,$p' /tmp/skypane-l0b-run-all-tests.log | sed -n 's/^    - //p' | sort > /tmp/skypane-l0b-after-failures.txt; diff -u /tmp/skypane-l0b-baseline-failures.txt /tmp/skypane-l0b-after-failures.txt && echo "suite matches the measured baseline"</automated>
    <human-check>
REQUIRED, not optional — but do NOT block plan completion on it. This project's memory records that computed-style and structural checks alone have already missed a real rendered bug (`feedback_real_device_ui_verification`), and this is a chart whose entire point is how it reads at a glance. Every recent Health quick task handed this pass back to the orchestrating session, which performed it successfully; assume the same division of labour — finish every automated half yourself, then hand these back.

Name these in the SUMMARY as outstanding for the orchestrating session's browser pass. Seed a realistic multi-week history first (the interesting failure modes only appear above ~30 points, and a fresh state directory will show the fallback chart instead):

1. **The whole point.** Does the 3-month chart actually answer "is the battery draining, and how fast" at a glance, in a way the old 20-reading chart did not? This is the developer's own stated need and nothing automated can judge it.
2. **Density at the real width.** At ~90 points, does the thin line still read as a line? Confirm the cosmetic dots are gone and that their absence looks intentional rather than broken. Then check the derived threshold at the boundary — if dots reappear where they visibly should not, the threshold is wrong and needs re-deriving against the measured canvas, not nudging.
3. **Hover and tap precision** with the reduced dense hit radius, on a real phone and not an emulator. Can a finger land on a specific day? If not, say so — the recorded fallback is a larger radius with the dots still suppressed, not a return to the raw-readings chart.
4. **The readout's two meanings.** At rest it shows the latest raw reading with a clock time; hovering a point replaces it with a daily average and a date. Read both out loud. Is the distinction obvious from the label alone, or does it read as one number changing inexplicably?
5. **The endpoint date labels.** Do they fit at the narrowest viewport without wrapping or colliding with the Y-column, and is the day-plus-month form the right one — or does the developer want the French convention after all? CONTEXT.md left the format to discretion and the app is otherwise English; this is the moment to confirm that call.
6. **The caption.** Does `— Last 3 months, daily average` sit correctly beside the heading, and does the fallback caption appear on a fresh state directory?
7. **Both themes, and a narrow viewport.**

Record what was actually observed rather than restating this list as if performed.
    </human-check>
  </verify>
  <done>
The chart reads as a day series: its two endpoint labels are portable day-plus-month dates flowing through the existing `<span>` axis mechanism, and each point's hover/tap label names its day, says it is a daily average, and gives the reading count that formed it — so the readout can never silently mix an average with a raw reading. Above a threshold derived from the narrowest real canvas and documented beside its constant, cosmetic dots are not emitted and the hit target shrinks, preserving the thin-line treatment the developer asked to keep; below it, everything is byte-for-byte what it is today, proven by the four untouched direct-call fixtures. `battery-trend.js` and `style.css` were confirmed from source to need no edit and were not edited. New checks cover the date labels, the averaged point labels, both sides of the density boundary and the unchanged sparse path, `EXPECTED_CHECK_COUNT` moved by exactly the number added, the design-system reference records the new treatment under its SUPERSEDED convention, and `scripts/run-all-tests.sh`'s FAILED list is identical to the baseline measured on this tree before the first edit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device -> Caddy -> `device_health.ts` | The stored timestamp originates in a Caddy access-log entry's own `ts` field and can be an arbitrary string (`tail_caddy_battery_log()` accepts any `str` unvalidated). It now reaches a new SQL function call, `date(ts)`, and a new date formatter. |
| browser -> `GET /health` | Server-rendered HTML for an authenticated operator. This task changes which rows are plotted, adds two date strings and one label string per point, and emits up to ~90 points instead of ~20. |
| companion service -> `history.db` | One additional read per authenticated page render (`compute_health_state()` runs for the nav dot on every page, not only Health). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l0b-01 | Tampering | SQL injection via the new aggregate's `since` bound | high | mitigate | Both branches are literal strings with `?` placeholders, copied from `route_source_counts()`. `server/test_config_history.py`'s pre-existing `_all_sql_uses_placeholders_not_string_formatting()` greps the whole module for `%`-formatted and f-string SQL and is re-run by Task 1's gate, which additionally asserts the query body contains no `%`-formatting. |
| T-l0b-02 | Tampering | A hostile stored timestamp reaching the new date formatter or forming a phantom day bucket | medium | mitigate | The query filters `date(ts) IS NOT NULL`, so an unparseable timestamp forms no bucket at all — asserted directly by Task 1's gate with a `not-a-timestamp` fixture. The new date label inherits `_axis_clock_label()`'s never-raise fallback to the raw string, and every label still passes through `escape_html()` at the existing interpolation points, so the pre-existing hostile-timestamp harness coverage stays meaningful. |
| T-l0b-03 | Denial of Service | An abnormal-drop alarm silently disarmed by averaging | high | mitigate | `battery_status()` keeps the raw `trend_rows` input. Task 2's gate greps its body for any daily reference and its must-have names the coupling to `collect_anomalies()` and the nav dot. Averaging a day's readings is precisely the operation that hides a sudden drop, so this is the one consumer that must not move. |
| T-l0b-04 | Denial of Service | The chart and the readout both vanishing on a device younger than two calendar days | high | mitigate | The fallback to the raw series when fewer than two day buckets exist, asserted end-to-end by Task 2's day-1 gate (chart present, readout present, caption honest) on a same-day fixture. |
| T-l0b-05 | Denial of Service | The new aggregate running on every authenticated page render | low | accept | `compute_health_state()` is called once per request for the nav dot, so this adds one aggregate read per page. It is bounded to 90 days and served through `idx_device_health_ts` via a raw `ts >= ?` comparison, over a table fed at most one row per device poll — a few thousand rows at the ceiling. Accepted rather than mitigated; if it ever shows up, the fix is to make the chart read lazy, not to narrow the window. |
| T-l0b-06 | Information Disclosure | A daily average presented as if it were a real reading | medium | mitigate | Every aggregated point's label states it is a daily average and how many readings formed it, and the heading caption names the aggregation. Asserted by Task 3's gate on `data-when`, and on the human-verification list because only a person can judge whether the distinction actually reads. |
| T-l0b-07 | Tampering | A retargeted gate silently asserting less than it did | medium | mitigate | Neither gate is deleted. The parameter-count gate is retargeted onto the positional-arity property it meant; the key-set gate keeps its exactness and adds an explicit registry/stats-absence assertion so D-11's real intent is strengthened rather than loosened. Task 1 requires its own new check to be observed failing under mutation. |
| T-l0b-08 | Repudiation | Reporting a pre-existing suite failure as caused by this task | medium | mitigate | Task 3 measures `scripts/run-all-tests.sh`'s FAILED list before its first edit and diffs against that measured file, never against a remembered expectation. |
| T-l0b-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install of any kind. Stdlib `sqlite3`/`datetime` only, no new dependency, no new file, no JavaScript. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 server/test_config_history.py` — green at its own `EXPECTED_CHECK_COUNT`, moved by exactly one, with the pre-existing SQL-placeholder discipline check still passing.
2. `server/.venv/bin/python3 companion/test_status_pages.py` — green at its own `EXPECTED_CHECK_COUNT`, moved by exactly the number of checks added across Tasks 2 and 3, with both retargeted gates counted as zero movement and nothing deleted.
3. `companion/test_view_pages.py`, `companion/test_companion_app.py`, `companion/test_config_page.py` and `companion/test_contrast_check.py` all green at their own unchanged counts.
4. A multi-day seeded render plots the daily averages and provably does NOT plot any raw reading value, while the raw readings all remain present in the disclosure table.
5. A same-day seeded render still produces a chart, still produces the readout element, and is captioned as readings — the day-1 regression guard.
6. An empty-database render shows the 3-month caption.
7. `_read_health_inputs()` carries the sixth battery key and still carries no registry or stats key.
8. `_battery_section()` and `battery_sparkline_svg()` are both still callable with their original positional arity.
9. `battery_status()`'s body references no aggregated series.
10. Direct-call sparkline fixtures below the density threshold are byte-for-byte unchanged; above it, cosmetic dots are absent, every hit target survives, and the smaller radius is in use.
11. `git diff --name-only` touches only the five files in `files_modified` — in particular neither `companion/static/battery-trend.js` nor `companion/static/style.css`.
12. `scripts/run-all-tests.sh`'s FAILED list is identical to the baseline measured on this tree before Task 3's first edit.
13. The SUMMARY records: the four re-verified SQLite facts, the measured narrow-canvas width and the density threshold derived from it, the confirmation that the JS and the stylesheet needed no edit (with the reason), the day-1 fallback recorded as a data-availability degradation rather than a reduced feature, the two gate retargets with their reasoning, the measured baseline comparison, and the pixel-level items outstanding.
</verification>

<success_criteria>
- The Health battery chart shows the evolution over many days — one server-aggregated daily-average point per UTC calendar day across a 90-day window — which is exactly what the developer asked for and at exactly the window they locked.
- The latest computed reading is still on the page, still the raw latest reading, still humanised by the same helper, and provably not sourced from the aggregate.
- Nothing was simplified, deferred or shipped as a first version: the daily chart is complete, and the raw-series fallback fires only when fewer than two calendar days of data exist.
- The abnormal-drop anomaly signal and the raw readings table both still see raw readings; averaging changed what is plotted, not what is alarmed on or what is auditable.
- The heading caption always names the window actually on screen, in both modes.
- The chart still reads as the same thin-line sparkline at 4.5x the point count, which is what suppressing the cosmetic dots above a derived threshold buys.
- No schema migration, no retention change, no new dependency, no JavaScript change, no stylesheet change.
- No check was deleted. Two gates were retargeted in place onto the properties they actually protected, and both harness counts moved by exactly the number of checks added.
- `scripts/run-all-tests.sh`'s FAILED list is identical to the baseline measured before the first edit.
</success_criteria>

<commits>
Focused and atomic, matching this session's established style (`git log --oneline -10`), referencing the quick task id rather than a phase-plan number:
- `feat(quick-260902-l0b): add a daily battery-average aggregate to history_db` — body naming the UTC-day bucketing, the two exclusion filters, and that D-13 retention is untouched
- `feat(quick-260902-l0b): plot the last 3 months of daily battery averages on Health` — body naming the developer's own request, the raw series retained for the readout/table/anomaly scan, the day-1 fallback and why it is not a reduced feature, and the two gate retargets
- `refactor(quick-260902-l0b): make the battery chart read as a day series` — body naming the date endpoint labels, the averaged point labels, the derived density threshold, and the confirmation that the JS and stylesheet needed no edit
</commits>

<output>
Create `.planning/quick/260902-l0b-redesign-the-companion-health-page-batte/260902-l0b-SUMMARY.md` when done.

Beyond the standard sections it must contain, under findable headings: the four SQLite `date()` facts as re-verified by the executor (and any disagreement with this plan's recorded results); the measured narrow-canvas width and the density threshold derived from it, with its re-derivation trigger; the confirmation that `companion/static/battery-trend.js` and `companion/static/style.css` needed no edit, with the source-level reason for each; the day-1 fallback recorded as a data-availability degradation with the `if sparkline_html:` coupling named; the two gate retargets with the reasoning for each; whether the measured `run-all-tests.sh` FAILED set matched the baseline; a "Handed Forward" section for anything noticed but deliberately not fixed; and a "Pixel-Level Items Outstanding" section reproducing the live-browser list for the orchestrating session's pass.
</output>
