---
phase: 05-low-battery-indicator
plan: 260828-0qo
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - server/plane/render.py
  - server/test_render.py
  - server/test_pipeline_e2e.py
  - .planning/phases/05-low-battery-indicator/05-UI-SPEC.md
autonomous: true
requirements: [DEVICE-04]

must_haves:
  truths:
    - "The bottom-left low-battery glyph renders at 70% of its former linear size in all three states (departing/arriving/empty) — nominal 51x22px instead of 72x32px — anchored at the exact same bottom-left corner it always occupied, because only the size constants moved and BATTERY_ICON_LEFT/BATTERY_ICON_BOTTOM did not."
    - "The icon still reads as a battery: a hollow outlined body, a solid terminal nub on its right edge vertically centred on the body, and a left-aligned solid partial fill occupying BATTERY_ICON_FILL_FRAC of the post-stroke interior — the same four-part shape contract, just smaller."
    - "battery_low=True still changes zero pixels outside the icon's own (now smaller) bounding box, and battery_low=False is still pixel-identical to omitting the kwarg entirely — the containment and default-off guarantees 05-02 established survive the resize."
    - "Every automated assertion about the icon's geometry is recomputed from the new BATTERY_ICON_* constants rather than carried over as a stale literal — no test, comment, or docstring anywhere in server/ still names the superseded bounding box."
    - "The full 9-harness suite, ruff, and scripts/check-attribution.sh are green after the change, at the same 46 render checks as before (this is a retune of existing behaviour, not new behaviour — the check count must not move)."
    - "05-UI-SPEC.md records the new geometry as an explicit, dated live on-glass correction — the same pattern Phase 3's 03-CONTEXT.md D-26/D-27 corrections used — rather than silently overwriting the original figures as if they had always been right."
  artifacts:
    - path: "server/plane/render.py"
      provides: "The five rescaled BATTERY_ICON_* size constants and a draw_battery_icon() docstring/comment block that describes the geometry it actually produces"
      contains: "BATTERY_ICON_BODY_W"
    - path: "server/test_render.py"
      provides: "Checks 44/45/46 repinned to the new bounding box, pixel probes, and constant-derivation rule"
      contains: "BATTERY_ICON_BOX"
    - path: "server/test_pipeline_e2e.py"
      provides: "The end-to-end battery panel-diff window repinned to the new icon row range and 4bpp byte-column range"
      contains: "icon_row_start"
    - path: ".planning/phases/05-low-battery-indicator/05-UI-SPEC.md"
      provides: "The amended geometry contract plus a dated on-glass revision record"
      contains: "on-glass"
  key_links:
    - from: "render.BATTERY_ICON_BODY_W/BODY_H/NUB_W/NUB_H/STROKE_PX"
      to: "draw_battery_icon()'s derived body/nub/fill boxes and its returned total bounding box"
    - from: "draw_battery_icon()'s returned total bounding box"
      to: "test_render.py's BATTERY_ICON_BOX containment window (Check B) and Check D's literal geometry pin"
    - from: "draw_battery_icon()'s rendered pixel footprint"
      to: "test_pipeline_e2e.py's icon_row_start/icon_row_end and icon_byte_start/icon_byte_end panel-diff window (x/2, since the panel is 4bpp, 2 pixels per byte)"
---

<objective>
Shrink the shipped bottom-left low-battery icon to 70% of its linear size, following live on-glass feedback: the developer saw the real Spectra 6 panel render it (05-02, merged and deployed earlier this session) and judged it too large.

Purpose: this is a live tuning iteration on already-shipped, already-deployed code — the same preview-then-correct discipline Phase 3 used for D-20/D-21/D-22 and D-26/D-27, applied to a real physical panel rather than a preview render. Nothing about the trigger logic, hysteresis, telemetry path, or per-state ink changes; only the glyph's size.

Output: five rescaled constants in `server/plane/render.py`, every dependent assertion in `server/test_render.py` and `server/test_pipeline_e2e.py` recomputed from those constants, and a dated on-glass revision record in `05-UI-SPEC.md`.

**Follow-up (not this plan's work):** once these changes are committed, the developer wants the server redeployed immediately so the smaller icon reaches the real panel. That is a separate deploy step after this plan lands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@server/plane/render.py
@server/test_render.py
@.planning/phases/05-low-battery-indicator/05-UI-SPEC.md
</context>

<reference_geometry>
Every number below was derived by hand from `draw_battery_icon()`'s existing arithmetic and is stated here so the executor never has to guess. The executor must still confirm each value empirically (the Task 1 verify block does exactly that) rather than trusting this table blind.

**Spacing scale already in `render.py`:** `SPACE_XS = 8`, `SPACE_SM = 16`, `SPACE_MD = 32`, `SPACE_LG = 64`, `MARGIN = SPACE_LG = 64`, `WIDTH = 1200`, `HEIGHT = 1600`, `FRAME_STROKE_PX = 2`.

**Constant changes:**

| Constant | Old | New | Derivation |
|---|---|---|---|
| `BATTERY_ICON_BODY_W` | `SPACE_LG` (64) | `45` | `round(64 * 0.7)` = `round(44.8)` |
| `BATTERY_ICON_BODY_H` | `SPACE_MD` (32) | `22` | `round(32 * 0.7)` = `round(22.4)` |
| `BATTERY_ICON_NUB_W` | `SPACE_XS` (8) | `6` | `round(8 * 0.7)` = `round(5.6)` |
| `BATTERY_ICON_NUB_H` | `SPACE_SM` (16) | `11` | `round(16 * 0.7)` = `round(11.2)` |
| `BATTERY_ICON_STROKE_PX` | `3` | `2` | `round(3 * 0.7)` = `round(2.1)`; also the floor for e-ink legibility — one flat 2px stroke, matching `FRAME_STROKE_PX`, never a 1px hairline |
| `BATTERY_ICON_FILL_FRAC` | `0.22` | `0.22` | **unchanged** — a ratio of the interior width, not a pixel size |
| `BATTERY_ICON_LEFT` | `MARGIN` (64) | `MARGIN` (64) | **unchanged** — position, not size |
| `BATTERY_ICON_BOTTOM` | `HEIGHT - MARGIN` (1536) | `HEIGHT - MARGIN` (1536) | **unchanged** — position, not size |

None of the four `round()` inputs lands on a `.5` tie, so Python's banker's rounding is not in play and each result is unambiguous.

**Resulting boxes** (Pillow inclusive-corner convention, exactly as `draw_battery_icon()` already computes them):

- `body_top` = `1536 - 22` = `1514`; `body_right` = `64 + 45` = `109` → body `(64, 1514, 109, 1536)`
- `nub_top` = `1514 + (22 - 11) // 2` = `1514 + 5` = `1519`; `nub_bottom` = `1530`; `nub_right` = `109 + 6` = `115` → nub `(109, 1519, 115, 1530)`
- interior after the 2px stroke inset: `(66, 1516, 107, 1534)` — 41px wide
- `fill_w` = `round(41 * 0.22)` = `round(9.02)` = `9` → fill `(66, 1516, 75, 1534)`
- **total bounding box: `(64, 1514, 115, 1536)`** — nominal 51x22px, rendered inclusive footprint 52x23px (was nominal 72x32, rendered 73x33)

**Nub centring is no longer symmetric.** `BODY_H - NUB_H` = `11`, an odd leftover, so floor division gives `gap_above = 5` and `gap_below = 6`. The old test asserted both gaps equalled exactly 8; that assertion is now factually wrong and must become a centred-to-within-one-pixel assertion, not be deleted.

**4bpp panel byte columns** (`test_pipeline_e2e.py` only): the packed panel is 2 pixels per byte, `row_bytes = 600`. Icon x-range `64..115` → byte columns `64 // 2 = 32` through `115 // 2 = 57` (was `32..68`).
</reference_geometry>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rescale the battery-icon constants and repin every dependent assertion</name>
  <files>server/plane/render.py, server/test_render.py, server/test_pipeline_e2e.py</files>

  <behavior>
Work this task as a genuine RED then GREEN cycle — update the test expectations first, watch them fail against the still-old constants, then change the constants and watch them pass. Do not change `render.py` first.

RED (expectations, written before the constants move):
  - `test_render.py` Check B: `BATTERY_ICON_BOX` becomes `(64, 1514, 115, 1536)`.
  - `test_render.py` Check C pixel probes land on: body-outline corner `(64, 1514)` = ink; a fill-interior pixel = ink; a nub pixel = ink; a hollow-interior pixel right of the fill = the state's background.
  - `test_render.py` Check D: the four size constants equal `round(SPACE_LG * 0.7)` / `round(SPACE_MD * 0.7)` / `round(SPACE_XS * 0.7)` / `round(SPACE_SM * 0.7)`; `BATTERY_ICON_STROKE_PX == 2`; `BATTERY_ICON_LEFT is MARGIN` and `BATTERY_ICON_BOTTOM == HEIGHT - MARGIN` both still hold unchanged; the nub is centred to within one pixel; the computed total box equals `(64, 1514, 115, 1536)`.
  - `test_pipeline_e2e.py`: the panel-diff window is rows `1514..1536` and byte columns `32..57`.

GREEN: the five constant edits in `render.py` make all of the above pass, with `render: 46/46 checks pass` and the e2e harness exiting 0.
  </behavior>

  <action>
**Step 1 — repin `server/test_render.py` (RED first).**

Rewrite the module-level `BATTERY_ICON_BOX` (currently a hand-written literal near the start of the battery-icon check block) so it is *computed from `render`'s own constants* rather than restated by hand — build it exactly the way `draw_battery_icon()` does: left = `render.BATTERY_ICON_LEFT`, top = `render.BATTERY_ICON_BOTTOM - render.BATTERY_ICON_BODY_H`, right = `render.BATTERY_ICON_LEFT + render.BATTERY_ICON_BODY_W + render.BATTERY_ICON_NUB_W`, bottom = `render.BATTERY_ICON_BOTTOM`. Check B's containment guarantee then follows the constants automatically and can never go stale again; Check D remains the one place that pins the literal values, which is the correct division of labour.

Update Check B's `check(...)` description string so the bounding box it names matches the new one.

In Check C (`_battery_icon_ink_and_hollow_interior`), all four `getpixel(...)` probes and their four failure-message strings must be recomputed. Derive each probe from the constants (or state it as a literal only after confirming it against the derived boxes in `<reference_geometry>`):
  - body-outline corner: the box's own top-left, `(64, 1514)`
  - fill interior: any pixel strictly inside `(66, 1516, 75, 1534)` — e.g. `(70, 1525)`
  - nub: any pixel strictly inside `(109, 1519, 115, 1530)` and clear of the body's right outline columns 108-109 — e.g. `(112, 1524)`
  - hollow interior right of the fill: any pixel with x in `76..107` and y in `1516..1534` — e.g. `(95, 1525)`
The semantic each probe asserts (ink / ink / ink / background) does not change — only the coordinates do.

In Check D (`_battery_icon_geometry_derives_from_spacing_scale`), the four `is not render.SPACE_*` identity assertions are now factually false and must be replaced. The spacing-scale lineage is preserved by asserting the *derivation* instead of the identity: `render.BATTERY_ICON_BODY_W == round(render.SPACE_LG * 0.7)`, and the same shape for `BODY_H`/`SPACE_MD`, `NUB_W`/`SPACE_XS`, `NUB_H`/`SPACE_SM`. Add an assertion that `render.BATTERY_ICON_STROKE_PX == 2` and that it is `>= render.FRAME_STROKE_PX` (the icon's stroke must never drop below the frame's own weight — that is the legibility floor this reduction stops at). Keep the two position assertions (`BATTERY_ICON_LEFT is render.MARGIN`, `BATTERY_ICON_BOTTOM == render.HEIGHT - render.MARGIN`) byte-for-byte — those constants did not move and the test must keep saying so. Replace the `gap_above != gap_below or gap_above != 8` condition with a centred-to-within-one-pixel condition: `gap_below - gap_above` must be `0` or `1` and neither gap may be negative — floor division puts the odd leftover pixel below the nub, which is the intended behaviour, not a defect. Repin the total-box comparison to the new box, and rewrite Check D's leading comment and its `check(...)` description string so both describe the reduction rule and the new box rather than the superseded 8px-gap/spacing-scale story.

Rename the Check D function only if its current name becomes actively misleading; if you rename it, update the call site in the same edit.

Do **not** add or remove any check. `EXPECTED_CHECK_COUNT` stays at 46 — this task retunes existing behaviour, it does not introduce new behaviour.

**Step 2 — repin `server/test_pipeline_e2e.py` (still RED).**

In the battery end-to-end check, the panel-diff window is currently two hardcoded pairs: an icon row range and an icon 4bpp byte-column range. Set the row range to the new `body_top`..`BATTERY_ICON_BOTTOM` and the byte-column range to the new icon x-range halved (see `<reference_geometry>`). Both failure-message strings interpolate the old ranges as literal text — rewrite them to interpolate the variables instead of restating numbers, so this window can never drift out of sync with its own bounds again.

The single-pixel nibble probe further down (a byte offset into row 1520, read as a high nibble) must be re-derived, not assumed: confirm that the pixel it samples still lands inside the new fill rectangle `(66, 1516, 75, 1534)` and therefore still reads as ink. If it does, leave it and add a brief comment naming the fill box it depends on; if it does not, move it to a pixel that does and update the surrounding comment.

**Step 3 — apply the constant changes in `server/plane/render.py` (GREEN).**

Change the five constants to the values in `<reference_geometry>`. Leave `BATTERY_ICON_FILL_FRAC`, `BATTERY_ICON_LEFT`, and `BATTERY_ICON_BOTTOM` untouched. Write each new size constant as a plain integer with its `round(SPACE_* * 0.7)` derivation stated in its own trailing comment, so the spacing-scale ancestry stays readable even though the values are no longer raw scale steps.

**Step 4 — make the prose in `render.py` true again.** Three places currently describe geometry that will no longer exist, and each is load-bearing documentation, not decoration:
  - The `# --- D-04/D-06/D-07 battery-low icon geometry` header comment block above the constants claims every dimension derives from the spacing scale with only two bespoke exceptions, and names the old total bounding box. Rewrite it: the two *position* constants still derive from `MARGIN`; the four *size* constants are now a uniform 0.7 reduction of their former spacing-scale values, applied as a live on-glass correction after the developer saw the shipped icon on the real Spectra 6 panel; name the new total bounding box.
  - `BATTERY_ICON_NUB_H`'s trailing comment currently justifies its value by grid alignment against the spacing scale. That justification no longer applies — replace it with the 0.7 derivation, and note that the resulting odd leftover puts the nub's centring one pixel low.
  - `draw_battery_icon()`'s docstring states both the rendered-vs-nominal footprint and the returned bounding box. Update both to the new figures. Also update `BATTERY_ICON_STROKE_PX`'s own comment: its former rationale ("thicker than the frame") is no longer the reason for the value — the stroke is now equal to `FRAME_STROKE_PX`, held there as the legibility floor rather than reduced further toward a hairline.

No other line of `draw_battery_icon()` changes — the arithmetic already derives everything from the constants, which is exactly why this is a constants-only change. Do not touch the `_assert_within_canvas()` guard, the drawing primitives, the call sites in `_build_empty_canvas()`/`_build_active_canvas()`, or the `--battery-low` CLI flag.

**Step 5 — sweep for stale literals.** After the three files are edited, scan all three for any surviving occurrence of the superseded top-edge coordinate (the old body top, which appears in every stale reference — comments, docstrings, message strings, and code alike). There must be none left in `server/`. The value is deliberately *kept* in `05-UI-SPEC.md` by Task 2 as a historical record, so restrict this sweep to the three code files.

<!-- planner-discipline-allow: 1504 -->
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
    <automated>server/.venv/bin/python3 server/test_pipeline_e2e.py</automated>
    <automated>server/.venv/bin/python3 -c "
import sys; sys.path.insert(0, 'server')
from PIL import Image, ImageDraw
from plane import render as r
c = Image.new('P', (r.WIDTH, r.HEIGHT), 1)
box = r.draw_battery_icon(c, ImageDraw.Draw(c), 0)
assert box == (64, 1514, 115, 1536), 'total box %r' % (box,)
assert (r.BATTERY_ICON_BODY_W, r.BATTERY_ICON_BODY_H, r.BATTERY_ICON_NUB_W, r.BATTERY_ICON_NUB_H, r.BATTERY_ICON_STROKE_PX) == (45, 22, 6, 11, 2)
assert r.BATTERY_ICON_FILL_FRAC == 0.22 and r.BATTERY_ICON_LEFT == 64 and r.BATTERY_ICON_BOTTOM == 1536
assert c.getpixel((64, 1514)) == 0 and c.getpixel((70, 1525)) == 0 and c.getpixel((112, 1524)) == 0
assert c.getpixel((95, 1525)) == 1, 'hollow interior right of the fill is not background'
print('geometry ok', box)
"</automated>
    <automated>server/.venv/bin/python3 -c "
import sys
paths = ['server/plane/render.py', 'server/test_render.py', 'server/test_pipeline_e2e.py']
stale = [(p, i + 1, l.rstrip()) for p in paths for i, l in enumerate(open(p)) if '1504' in l]
for row in stale: print('STALE:', row)
sys.exit(1 if stale else 0)
"</automated>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>

  <done>
`server/test_render.py` prints `render: 46/46 checks pass` and exits 0; `server/test_pipeline_e2e.py` exits 0; `draw_battery_icon()` returns `(64, 1514, 115, 1536)` with the four probe pixels reading ink/ink/ink/background as asserted; zero occurrences of the superseded top-edge coordinate remain in any of the three code files; `scripts/run-all-tests.sh` reports all 9 harnesses green; `ruff check .` is clean. `BATTERY_ICON_FILL_FRAC`, `BATTERY_ICON_LEFT`, and `BATTERY_ICON_BOTTOM` are byte-for-byte unchanged in `git diff`, and `git diff --name-only` for this task lists exactly the three code files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the reduction in 05-UI-SPEC.md as a dated on-glass correction</name>
  <files>.planning/phases/05-low-battery-indicator/05-UI-SPEC.md</files>

  <action>
Amend the `## Canvas and Geometry — new zone only` section and the `## Battery Icon — glyph contract` section so the document describes the icon that now ships, while preserving the record of what it superseded — the same pattern Phase 3's `03-CONTEXT.md` used for its D-26/D-27 on-glass corrections (a dated, explicitly-labelled correction entry rather than a silent overwrite that reads as if the original figures had always been right).

**1. Add a revision record.** Immediately under the `## Canvas and Geometry — new zone only` heading, before the existing prose, add a short dated block — a `### Revision 2 — live on-glass size correction (2026-08-28)` heading and two or three sentences. It must state: the original geometry shipped in 05-02 and was deployed to the real hardware; the developer then saw the glyph rendered on the physical 13.3" Spectra 6 panel and judged it too large; every size dimension was therefore reduced to 70% of its original value (`round(original * 0.7)`), with position and fill-fraction deliberately untouched. Name the superseded total bounding box and the new one so the delta is legible. Cite this as a live on-glass correction in the same lineage as Phase 3's D-20/D-21/D-22 and D-26/D-27, and cite this quick task's ID (`260828-0qo`) as the change record. Note explicitly that the original section's own instruction — confirm against a real render before treating the figures as final — is exactly what produced this revision, so the process worked as designed.

**2. Update the constant table.** Set the `BATTERY_ICON_BODY_W`/`BODY_H`/`NUB_W`/`NUB_H`/`STROKE_PX` rows to their new values (45 / 22 / 6 / 11 / 2), each Notes cell naming its `round(original * 0.7)` derivation. Leave the `BATTERY_ICON_LEFT`, `BATTERY_ICON_BOTTOM`, and `BATTERY_ICON_FILL_FRAC` rows unchanged — they did not move, and the table must keep saying so. Note that the pre-revision `BATTERY_ICON_NUB_H` row in this document still reads 14 while the shipped code has used 16 since 05-02 resolved that discretion item; correct it to the new 11 and mention the 16 (not the 14) as the value actually superseded, so the document stops carrying a figure that never shipped.

**3. Update the derived bounding boxes.** Replace the body, nub, total, and fill/interior boxes with the values from this plan's `<reference_geometry>`: body `(64, 1514, 109, 1536)`, nub `(109, 1519, 115, 1530)`, **total `(64, 1514, 115, 1536)` — 51x22px**, interior `(66, 1516, 107, 1534)` (41x18px after the 2px stroke inset), fill width `round(41 * 0.22)` = 9px → fill box `(66, 1516, 75, 1534)`. The old total-box bullet compared the size against the session sketch's "~72x34px, sized like B" reference from `05-CONTEXT.md` D-04; that comparison is now superseded by direct observation of the real panel — say so rather than deleting it, since the sketch reference is what the on-glass feedback overrode.

**4. Correct the nub-centring statement.** The nub is no longer symmetrically centred: `(22 - 11) // 2` leaves an odd pixel, giving a 5px gap above and 6px below. State this, so a future reader does not treat the asymmetry as a bug.

**5. Correct the stroke rationale in both places it appears** — the `BATTERY_ICON_STROKE_PX` table row and the `**Line weight:**` paragraph in the glyph contract section. Both currently justify the value as being *thicker* than the frame's 2px `FRAME_STROKE_PX`. At 2px that is no longer true: the stroke now equals the frame's. Rewrite the rationale honestly — 2px is where the proportional 0.7 reduction lands (`round(3 * 0.7)` = 2) and is simultaneously the floor the reduction stops at, because dropping to a 1px hairline would cost the glyph its legibility as a distinct shape at e-ink resolution.

**6. Refresh the collision check.** The `**Collision check against the existing locked layout**` bullets cite the icon's x-range as 64-136 and its clearances from the frame. The conclusions all still hold — the box only shrank while its anchor stayed put, so every clearance strictly increased — but the cited numbers are stale. Update the x-range to 64-115 and the frame/previous-card clearance figures accordingly, and state plainly that a uniform shrink at a fixed anchor cannot introduce a collision.

Do not touch the `## Color`, `## Trigger and Data Contract`, `## Copywriting Contract`, `## Registry Safety`, or `## Checker Sign-Off` sections — none of them says anything about size. Do not touch the `supersedes:` frontmatter line (it is about the Yellow/D-12 reservation, unrelated to this change); do add or update a dated marker in the frontmatter recording that the document was revised on 2026-08-28, matching whatever field convention the existing frontmatter already uses.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys, re
p = '.planning/phases/05-low-battery-indicator/05-UI-SPEC.md'
t = open(p).read()
fails = []
for lit in ['(64, 1514, 115, 1536)', '(64, 1514, 109, 1536)', '(109, 1519, 115, 1530)', '(66, 1516, 75, 1534)', '2026-08-28', '260828-0qo']:
    if lit not in t: fails.append('missing new geometry/provenance: ' + lit)
for added in ['D-26', 'D-27']:
    if added not in t: fails.append('missing required Phase 3 on-glass precedent citation: ' + added)
if not re.search(r'on.glass', t, re.I): fails.append('missing: on-glass correction framing')
if not re.search(r'###\s*Revision\b', t): fails.append('missing: dated Revision heading')
for stale in ['64px | Icon body', '| 14px |', 'thicker than the frame']:
    if stale in t: fails.append('stale text still present: ' + stale)
for keep in ['BATTERY_ICON_FILL_FRAC', '0.22', 'MARGIN', 'D-20/D-21/D-22']:
    if keep not in t: fails.append('unexpectedly removed (must survive the revision): ' + keep)
for f in fails: print('FAIL:', f)
sys.exit(1 if fails else 0)
"</automated>
    <automated>server/.venv/bin/python3 -c "
import subprocess, sys
out = subprocess.run(['git','diff','--name-only','HEAD','--','server/'], capture_output=True, text=True).stdout.strip()
print('server/ diff for this task:', repr(out))
sys.exit(1 if out else 0)
" </automated>
  </verify>

  <done>
`05-UI-SPEC.md` carries a dated `### Revision` block framing the change as a live on-glass correction citing the Phase 3 D-26/D-27 precedent and this task's ID; its constant table, derived bounding boxes, nub-centring statement, stroke rationale (both occurrences), and collision-check figures all match the shipped constants; the unchanged position and fill-fraction rows are still present and still stated as unchanged; no `server/` file was modified by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device→server `X-Battery-Mv` header | Untrusted device-supplied millivolt reading crosses here. **Not touched by this plan** — parsing, validation, hysteresis, and persistence all live in `byos_server.py`/`poll_loop.py` and are unchanged. |
| server→panel rendered image | Server-controlled bytes only. The battery glyph carries no external data: it is a fixed shape drawn from compile-time integer constants, with no text, no font, and no flight/telemetry value rendered into it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0qo-01 | Denial of Service | `draw_battery_icon()` geometry | low | mitigate | New constants could in principle place the glyph off-canvas or produce a degenerate box. The existing `_assert_within_canvas(total, "battery icon")` guard is retained untouched and still runs on every draw; Task 1's verify block additionally asserts the exact returned box, and the full 9-harness suite must be green. |
| T-0qo-02 | Information Disclosure | rendered panel image | low | accept | The glyph is a fixed "low" shape, not a live gauge (`BATTERY_ICON_FILL_FRAC` unchanged at 0.22) — it discloses only the boolean already disclosed before this change. Shrinking it cannot widen the disclosure. |
| T-0qo-03 | Tampering | supply chain (npm/pip/cargo) | low | accept | Zero package installs. `server/requirements.txt` and `server/requirements-dev.txt` are not in `files_modified`; no dependency surface changes, so the package-legitimacy gate does not apply to this plan. |
| T-0qo-04 | Tampering | test assertions weakened during repin | medium | mitigate | Repinning geometry tests is the classic path to accidentally deleting coverage. Mitigated structurally: `EXPECTED_CHECK_COUNT` stays at 46 and the harness fails if the executed count differs; Check B's window is made *derived* rather than literal so it cannot silently drift; Check D retains the literal pin; the stale-literal sweep proves no superseded coordinate survives anywhere in `server/`. |
</threat_model>

<verification>
1. `scripts/run-all-tests.sh` — all 9 harnesses green, `render: 46/46 checks pass` among them.
2. `server/.venv/bin/ruff check .` — clean.
3. `scripts/check-attribution.sh` — passes (no asset changes, but the gate runs in CI and must stay green).
4. `git diff --stat` across both tasks touches exactly four files and no others.
5. Human on-glass confirmation, after the follow-up deploy:
   <human-check>
   Render a preview with the icon forced on (`server/.venv/bin/python3 -m plane.render --state arriving --battery-low --out /tmp/preview.bin` or the equivalent PNG output flag), then — after the server is redeployed — look at the real 13.3" panel on the next wake. Confirm the glyph still reads unambiguously as a battery (body, nub, and partial fill all distinguishable at a glance from normal viewing distance) at its reduced size, and that 70% is the right amount of reduction rather than too much. If it now reads too small, that is another tuning iteration, not a defect in this one.
   </human-check>
</verification>

<success_criteria>
- `BATTERY_ICON_BODY_W`/`BODY_H`/`NUB_W`/`NUB_H` are 45/22/6/11 and `BATTERY_ICON_STROKE_PX` is 2, each with its `round(original * 0.7)` derivation stated in code.
- `BATTERY_ICON_FILL_FRAC`, `BATTERY_ICON_LEFT`, and `BATTERY_ICON_BOTTOM` are byte-for-byte unchanged.
- `draw_battery_icon()` returns `(64, 1514, 115, 1536)` and every comment and docstring in `render.py` describing its geometry matches what it actually produces.
- `test_render.py` still runs exactly 46 checks, all passing, with Check B's window derived from the constants and Check D pinning the literal values and the derivation rule.
- `test_pipeline_e2e.py`'s battery panel-diff window matches the new icon rows and 4bpp byte columns, with its failure messages interpolating those bounds rather than restating them.
- No superseded geometry literal survives anywhere under `server/`.
- `05-UI-SPEC.md` records the change as a dated live on-glass correction citing the Phase 3 D-26/D-27 precedent, with every geometry figure, the nub-centring note, the stroke rationale, and the collision-check numbers brought in line.
- Full 9-harness suite, ruff, and attribution check all green.
</success_criteria>

<output>
Create `.planning/quick/260828-0qo-reduce-the-low-battery-icon-s-size-by-30/260828-0qo-SUMMARY.md` when done.

In the summary, record explicitly: the exact old→new constant values, the old→new total bounding box, that `FILL_FRAC`/`LEFT`/`BOTTOM` were deliberately held constant, that the nub's centring became asymmetric by one pixel as a consequence of the odd `BODY_H - NUB_H` leftover, that the stroke now equals rather than exceeds `FRAME_STROKE_PX`, and that a redeploy to the production server is the immediate expected next step so the developer can judge the new size on real glass.
</output>
