---
quick_id: 260923-em4
slug: fix-flaky-fallback-save-browser-check-ba
date: 2026-09-23
type: quick
---

# Quick 260923-em4: fix the flaky fallback-Save browser check

`companion/test_browser_ux.py`'s "with scripts blocked at 360px ... the fallback
Save ... still saves to disk" check fails CI on `main` (run after #89) and on both
open Dependabot PRs (#86, #87). 260923-9na (#82) fixed one race but called itself
partial.

## Task

Find the actual mechanism and fix it in the check without weakening what it asserts
(fallback visible, real box, saves to disk in both languages).

## Verification

Full `test_browser_ux.py` harness: the check must pass on repeated runs where it
previously failed every time.
