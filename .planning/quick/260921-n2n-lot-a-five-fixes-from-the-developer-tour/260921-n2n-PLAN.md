---
phase: quick-260921-n2n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/pages/history_page.py
  - companion/pages/airlines_page.py
  - companion/pages/health_page.py
  - companion/pages/config_page.py
  - companion/i18n_fr/display.py
  - companion/static/panel-lookup.js
  - companion/static/style.css
  - companion/test_view_pages.py
  - companion/test_status_pages.py
autonomous: true
requirements:
  - QUICK-260921-n2n
must_haves:
  truths:
    - "Tapping the Flights or Compagnies filter field on iOS Safari offers no contact/phone-number autofill, and the on-screen keyboard opens shifted for a callsign."
    - "Opening an ordinary illustration in the Compagnies lightbox shows NO Prefix/First seen/Last seen/Times seen/Example callsign block at all — not an empty one."
    - "Opening an unidentified-prefix (gap) card in the same lightbox still shows that block fully populated, with a real example callsign under 'Example callsign'."
    - "The runway card's caption, in both English and French, no longer describes a schematic diagram; it states which runway is watched and when the change applies, and nothing else."
    - "The French runway caption still resolves through the catalogue (the page does not silently fall back to English)."
    - "No new script, route, CSP directive, CSS custom property or CSS class family is introduced anywhere by this batch."
    - "A future fourth filter bar cannot reintroduce the Safari autofill defect without a harness failure."
    - "`.flight-detail-row__grid`'s bottom margin can never be reduced below the two copy buttons' combined 11px reach without a harness failure (Phase 27 CFG-70's measured finding, now executable)."
  artifacts:
    - companion/pages/history_page.py
    - companion/pages/airlines_page.py
    - companion/pages/health_page.py
    - companion/pages/config_page.py
    - companion/i18n_fr/display.py
    - companion/static/panel-lookup.js
    - companion/static/style.css
    - companion/test_view_pages.py
    - companion/test_status_pages.py
  key_links:
    - "companion/i18n_fr/display.py's CATALOG key is byte-identical to companion/pages/config_page.py's RUNWAY_SECTION_CAPTION — companion/test_i18n.py Checks 1 and 2 already enforce this in both directions (missing translation / dead translation)."
    - "panel-lookup.js's contextCallsign assignment is gated on the SAME `count` variable that gates `resolveContext.hidden` — one condition, never two."
    - "`.resolve-context[hidden] { display: none; }` sits after `.resolve-context { display: grid; ... }` in style.css, joining the file's existing .banner__pill / .refresh-pill / .dirty-bar / .login-reveal [hidden]-versus-display guards."
    - "The new exhaustive `<input type=\"search\">` scan (Task 4) is what proves Task 1 missed no site, including health_page.py's third filter bar."
---

<objective>
"Lot A" — the five small, independently-shippable fixes the developer found on a live tour of the deployed companion app on 2026-09-21. These ship AHEAD of Phase 29, whose own ROADMAP.md entry declares "quick task Lot A merged first (its five fixes are assumed present)".

Purpose: five one-line-class defects should not be held hostage by a planning cycle. Each is a pure string / attribute / CSS / one-expression change with no new script, no new route and no new token.

Output: three filter inputs that no longer trigger Safari contact autofill; an illustration lightbox that hides its resolve-context block instead of showing it empty, and that stops printing the picture's own caption under "Example callsign"; a runway caption that no longer describes the map Phase 27 removed, in both languages; two new permanent harness checks that make two of these fixes impossible to regress; and a measured verdict on the "écart bizarre" gap rather than a guessed change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Notes for the executor — read all of these before Task 1:

- **Run everything from the worktree** `/Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703`. Never `cd` to the main checkout. Do NOT use worktree isolation for this work: this repo's isolation forks from `main` rather than from the current branch HEAD, which would place the commits on the wrong base.
- **Base commit for this batch:** `4900256` (HEAD at planning time). Task 4 diffs against it. Export it once: `BASE=4900256`.
- **There is no pytest in this project.** Every harness is a stdlib-only script with its own `check(name, fn)` helper, a module-level `EXPECTED_CHECK_COUNT`, and a `main()` returning 0/1. Invoke one harness directly:
  `server/.venv/bin/python3 companion/test_view_pages.py`
  The whole suite is `scripts/run-all-tests.sh` (18 harnesses, ~90s wall clock).
- **`playwright` is NOT installed in `server/.venv`** (confirmed: `ModuleNotFoundError: No module named 'playwright'`). `companion/test_browser_ux.py` therefore exits 0 with a printed `SKIP` line. **Do not attempt `pip install playwright` or `playwright install chromium`.** No task in this plan may depend on a live browser run succeeding. Where a live browser is the only possible proof, the task says so in its `<done>` and hands the developer an exact, short visual check.
- **Measured baselines, taken during planning on this exact tree (all green):**
  | harness | baseline |
  |---|---|
  | `companion/test_config_page.py` | 266/266 |
  | `companion/test_view_pages.py` | 164/164 |
  | `companion/test_status_pages.py` | 306/306 |
  | `companion/test_i18n.py` | 24/24 |
  | `companion/test_browser_ux.py` | SKIP (exit 0, no playwright) |
- **`EXPECTED_CHECK_COUNT` is never trusted from arithmetic.** This project's own convention (see the comment stacks at the top of every harness) is: add a new `EXPECTED_CHECK_COUNT = N` assignment BELOW the existing ones, with a comment naming this quick task and the task number and what the delta is, and **re-derive N by actually running the harness**, not by adding one in your head. Follow it exactly.
- **Every new check must be mutation-proven.** Break the thing the check protects, run the harness, quote the real failure message in the SUMMARY, then revert the mutation with `git checkout-index -f -- <path>` (this project's standing revert idiom — never `git stash`, the stash stack is shared with other worktrees and other sessions).
- **Commit message form:** `fix(quick-260921-n2n): <what>`. These are code fixes, never `docs(...)`. One commit per task. End every commit message with:
  `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
- **Do NOT touch** `.planning/ROADMAP.md` or `.planning/STATE.md` (the orchestrator owns STATE.md). Do NOT touch `companion/app.py` (no route change, no CSP change is needed by any task here). Do NOT touch anything in Phase 29's or Phase 30's own scope: no editorial-floor harness, no quiet-hours/`_normalised_time_html()` change, no "Modifier les images" / `edit_mode` change, no Aspect / theme-carousel change, no pagination.
- **Project skill check, done during planning:** `.claude/skills/sketch-findings-skypane/` is the companion app's design-system reference (colour, typography, spacing, cards, control density, navigation, page patterns). Nothing in it governs an input autofill attribute, a `[hidden]` display guard, or a JS textContent gate — those are correctness, not visual design. It IS relevant to Task 5 in one respect only: the `--space-*` token scale (`xs: 4px, sm: 8px, md: 16px, lg: 24px`) is the design system's, and any spacing change must come from that scale. No other part of the skill applies; do not load it for Tasks 1-4.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/CLAUDE.md

Source under change — read each once, extract everything needed in that pass:
@companion/pages/history_page.py
@companion/pages/airlines_page.py
@companion/pages/health_page.py
@companion/pages/config_page.py
@companion/i18n_fr/display.py
@companion/static/panel-lookup.js
@companion/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1 (FIX 1): the three filter search inputs stop offering Safari contact autofill</name>
  <files>companion/pages/history_page.py, companion/pages/airlines_page.py, companion/pages/health_page.py, companion/test_view_pages.py, companion/test_status_pages.py</files>
  <action>
The developer's screenshot: tapping the Flights filter field on Safari popped up "09 51 90 38 96 – domicile" / "06 15 82 72 84 – iPhone". Cause: a bare `<input type="search">` with no `name` and no `autocomplete`, which Safari heuristically treats as a contact field.

There are THREE independent copies of the same literal, in three independent files — verified by grep during planning, not assumed:

- `companion/pages/history_page.py:979`, inside `_filter_bar_html(total)` (Flights)
- `companion/pages/airlines_page.py:1738`, inside `_filter_bar_html(total, summary_html="")` (Compagnies)
- `companion/pages/health_page.py:3354`, inside that page's own filter-bar builder (the unidentified-prefix registry filter)

The third site was NOT in the developer's report and was found during planning. It is the identical literal with the identical defect; fixing two of three would leave a known-remaining instance of the exact bug being reported, and Task 4's exhaustive scan would fail on it anyway. Fix all three.

In each of the three files, replace the single occurrence of the search-input literal so that it reads exactly:

`'<input type="search" id="%s" autocomplete="off" spellcheck="false" autocapitalize="characters" data-filter-input>'`

Attribute order is load-bearing for the harness checks below and for readability: the three new attributes sit after `id="%s"` and before `data-filter-input`, so the `data-filter-input` marker stays the last token in the tag, as it is today. Add no `name` attribute (a `name` on a filter input would be submitted by any enclosing form and is not needed by `list-filter.js`, which reads the element by attribute selector).

`autocapitalize="characters"` per the developer decision: all three filters search callsigns, hex codes and company names, which are conventionally upper-case. Verified safe during planning — `companion/static/list-filter.js:122` matches on `input.value.toLowerCase()` against server-lowercased haystacks, so the keyboard's shift state cannot affect what matches; `autocapitalize` changes only the on-screen keyboard, never the filter result. Say this in the code comment you add, so nobody later "fixes" it back out of a fear it breaks matching.

Add a short provenance comment at ONE of the three sites (history_page.py, the page the developer reported) naming this quick task, the Safari symptom, and the fact that the same three attributes are repeated verbatim at the other two sites because these three builders are deliberately NOT a shared helper — extracting one is a refactor, explicitly out of scope for a quick task. At the other two sites, a one-line comment pointing back at history_page.py's is enough. Do not extract a shared function.

Then retarget the ONE existing check that asserts the old literal byte-for-byte: `companion/test_status_pages.py:10898` (inside `_gallery_filter_label_for_matches_input_id()`) builds `'<input type="search" id="%s" data-filter-input>' % airlines_page._FILTER_INPUT_ID` and asserts it is present. Update that construction to the new full literal in place — same check, same name, same purpose (it is proving the label's `for=` equals the input's `id`), just the current literal. Do not delete it and do not add a second one beside it.

Then add exactly TWO new checks, in this project's own style (a nested `def`, a `(bool, message)` return, a `check(name, fn)` registration, a failure message that explains the defect rather than restating the assertion):

- In `companion/test_view_pages.py`, beside the existing `_filter_bar_markers_present_once()` check: assert History's rendered filter bar carries `autocomplete="off"`, `spellcheck="false"` and `autocapitalize="characters"` on its search input, with a failure message naming the Safari contact-autofill defect these prevent.
- In `companion/test_status_pages.py`, beside the existing gallery filter-bar checks: assert the SAME three attributes on both the Compagnies gallery filter input and the Health registry filter input, in one check over both rendered pages, with the same shape of failure message.

Bump `EXPECTED_CHECK_COUNT` in both harnesses per the convention in `<execution_context>` — new assignment below the existing stack, comment naming `quick task 260921-n2n Task 1`, value re-derived by running.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 -c "
import sys
NEW = '&lt;input type=\"search\" id=\"%s\" autocomplete=\"off\" spellcheck=\"false\" autocapitalize=\"characters\" data-filter-input&gt;'
OLD = '&lt;input type=\"search\" id=\"%s\" data-filter-input&gt;'
bad = []
for p in ('companion/pages/history_page.py','companion/pages/airlines_page.py','companion/pages/health_page.py'):
    s = open(p, encoding='utf-8').read()
    if s.count(NEW) != 1: bad.append('%s: new literal count %d, want 1' % (p, s.count(NEW)))
    if OLD in s: bad.append('%s: old bare literal still present' % p)
if bad: sys.exit('FAIL\n' + '\n'.join(bad))
print('OK 3/3 filter inputs carry the three attributes, no old literal left')
"</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_view_pages.py 2&gt;&amp;1 | tail -1   # expect 165/165 (baseline 164 + 1), re-derived by running</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py 2&gt;&amp;1 | tail -1   # expect 307/307 (baseline 306 + 1), re-derived by running</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_i18n.py 2&gt;&amp;1 | tail -1   # expect 24/24 — the new attributes are not in Check 5's title/alt/aria-label/placeholder set, so no French entry is required; this run proves it</automated>
  </verify>
  <done>
All three search inputs carry `autocomplete="off" spellcheck="false" autocapitalize="characters"`; the old bare literal exists nowhere in `companion/pages/`; `test_status_pages.py:10898`'s exact-literal assertion was retargeted in place rather than deleted; two new checks (one per harness) pin the attributes on all three pages; both new checks mutation-proven by stripping the attributes from one site and quoting the real failure message; `test_view_pages.py` 165/165, `test_status_pages.py` 307/307, `test_i18n.py` 24/24.

FULLY AUTOMATED. The only thing a machine cannot prove here is Safari's own behaviour (whether iOS actually stops offering contacts) — that is a browser-vendor heuristic, not a property of this code. `autocomplete="off"` on a non-credential field is the documented suppression for it; the developer can confirm on the deployed app in one tap. No live-browser harness run would prove it either, since Chromium's autofill heuristics differ from Safari's.
  </done>
</task>

<task type="auto">
  <name>Task 2 (FIX 2): the illustration lightbox hides its resolve-context block, and stops printing the picture's caption as an example callsign</name>
  <files>companion/static/style.css, companion/static/panel-lookup.js, companion/test_status_pages.py, companion/test_view_pages.py</files>
  <action>
One defect report ("une bonne part de ces infos ne servent à rien"), two independent root causes, both inside the Compagnies illustration lightbox. Fix both; one commit.

**Root cause A — the block shows EMPTY instead of hidden (CSS).**
`companion/static/panel-lookup.js:510-511` already does the right thing: `resolveContext.hidden = !count;`, so for a plain art card (no `data-view-panel-count`) the element IS marked `hidden`. But `companion/static/style.css:4893` declares `.resolve-context { display: grid; ... }`, an author rule that sets `display` with no `[hidden]` guard — and an author `display` declaration beats the user-agent stylesheet's `[hidden] { display: none }` regardless of source order. So the native `hidden` attribute is silently inert and every ordinary illustration paints five empty label/value pairs.

This exact trap is already named four times in this same file. Read all four before writing:
- `.banner__pill[hidden]` (~line 2404, with the fullest explanatory comment)
- `.refresh-pill[hidden]` (~line 2465 — hides by `visibility`, deliberately, and declares no `display` at all; a check in `test_status_pages.py` pins that difference, so do not copy this one's mechanism)
- `.dirty-bar[hidden]` (~line 2775)
- `.login-reveal[hidden]` (~line 7490)

Add a fifth, immediately AFTER the `.resolve-context { ... }` block and before `.resolve-context dt, .resolve-context dd { ... }`:

```
.resolve-context[hidden] {
  display: none;
}
```

with a comment in this file's own register that: names this quick task and the developer's report; states the collision in the same terms the four siblings' comments use; and records why the `[hidden]`-suffix form is used here rather than the `:not([hidden])` form the neighbouring lightbox rules use (`.lightbox__replace:not([hidden])` et al., ~line 7816) — namely that `.resolve-context`'s base rule carries five declarations that all belong to the visible state, and moving the whole selector to `:not([hidden])` would restructure a rule Phase 22's B5 deliberately laid out, where a two-line additive guard changes nothing about the visible state. Both idioms are correct; the additive one is the smaller change. Say so, so the inconsistency reads as a decision rather than an oversight.

`.resolve-context[hidden]` is (0,2,0) against the base rule's (0,1,0), so it wins on specificity alone — but place it after the base rule anyway, matching all four siblings' placement, so the file reads top-down.

**Root cause B — "Example callsign" always shows the image's own caption (JS).**
`companion/static/panel-lookup.js:528-530` assigns `captionText` into `contextCallsign` unconditionally, and `captionText` is `trigger.getAttribute("data-view-panel-caption")` (read once at line 429). For an art card that attribute is "Illustration Transavia France" — the picture's caption, not a callsign. Verified during planning that for a gap card (`airlines_page._gap_card_html()`) the caption attribute IS the example callsign, so the resolve-mode behaviour is already correct and must not change.

Gate the assignment on the SAME `count` variable that already gates `resolveContext.hidden` twenty lines above — do not invent a second condition, do not add a new `data-view-panel-*` attribute, do not add a server-side field. `count` is already in scope at that point. Replace the body of the existing `if (contextCallsign)` block so it reads exactly:

```
    if (contextCallsign) {
      // Quick task 260921-n2n Task 2: gated on the SAME `count` that
      // gates resolveContext.hidden above, never a second independent
      // condition — the field and the container it lives in can then
      // never disagree. In art mode `captionText` is the illustration's
      // own caption, which is not a callsign at all; writing it here
      // printed the picture's title under the "Example callsign" label
      // on every ordinary picture. In the resolve modes the caption
      // attribute genuinely IS the example callsign
      // (airlines_page._gap_card_html()), so this branch is unchanged
      // there.
      contextCallsign.textContent = count ? captionText : "";
    }
```

Clear to `""` rather than leaving the previous open's value behind: the dialog is a single shared `<dialog>` reused across every trigger, so a stale value would survive from one open to the next.

**Scope boundary, hard.** Do NOT touch the page-wide `edit_mode` gate, the "Modifier les images" / "Change pictures" toggle, or `_lightbox_replace_form_html() if edit_mode else ""`. Making the Replace/Delete forms always-render is Phase 29's CFG-81 and is deliberately out of this task. This task fixes only the two bugs inside the CURRENT gating model.

**Checks.** Add exactly two, each in the harness that owns that kind of assertion (verified during planning):
- `companion/test_status_pages.py` owns style.css source-level `[hidden]`-guard assertions (see its existing `.banner__pill[hidden]` block-parsing check ~line 14154 and the lightbox `:not([hidden])` check ~line 13146). Add one check there: `.resolve-context[hidden] {` exists, appears exactly once, sits at a later source offset than `.resolve-context {`, and its block declares `display: none`. The failure message must explain the collision (an author `display` beats the UA `[hidden]` rule), not merely restate the selector.
- `companion/test_view_pages.py` owns panel-lookup.js source-level contract pins without a live DOM (see its own header comment block ~line 118-130). Add one check there: the gated assignment form is present exactly once, and no ungated assignment into `contextCallsign` survives — assert the gated form's presence and that the count of `contextCallsign.textContent` assignments in the file is exactly one. The failure message must name what the ungated form did wrong (printed a picture caption under a callsign label).

Bump `EXPECTED_CHECK_COUNT` in both harnesses per the convention — new assignment below Task 1's, comment naming `quick task 260921-n2n Task 2`, value re-derived by running.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 -c "
import sys
s = open('companion/static/style.css', encoding='utf-8').read()
base = s.index('.resolve-context {')
if s.count('.resolve-context[hidden] {') != 1: sys.exit('guard count != 1')
guard = s.index('.resolve-context[hidden] {')
if guard &lt;= base: sys.exit('guard must come AFTER the base rule')
block = s[guard:s.index('}', guard)]
if 'display: none' not in block: sys.exit('guard must declare display: none, got %r' % block)
print('OK css guard at offset', guard, 'after base rule at', base)
"</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 -c "
import sys
s = open('companion/static/panel-lookup.js', encoding='utf-8').read()
n = s.count('contextCallsign.textContent')
if n != 1: sys.exit('expected exactly one contextCallsign.textContent assignment, got %d' % n)
if 'contextCallsign.textContent = count ? captionText : \"\";' not in s: sys.exit('gated assignment form not found')
print('OK js assignment is gated on count, exactly once')
"</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py 2&gt;&amp;1 | tail -1   # expect 308/308 (307 after Task 1, + 1), re-derived by running</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_view_pages.py 2&gt;&amp;1 | tail -1   # expect 166/166 (165 after Task 1, + 1), re-derived by running</automated>
  </verify>
  <done>
`.resolve-context[hidden] { display: none; }` exists exactly once, after the base rule, declaring `display: none`, with a comment that explains the collision AND why the additive `[hidden]` form was chosen over the neighbouring `:not([hidden])` form. `contextCallsign.textContent` is assigned exactly once in `panel-lookup.js`, gated on the same `count` as `resolveContext.hidden`. Two new checks added (one per harness), each mutation-proven with the real failure message quoted. `test_status_pages.py` 308/308, `test_view_pages.py` 166/166. `edit_mode`, the "Modifier les images" toggle and the replace/delete form gating are byte-unchanged — prove it with `git diff --stat companion/pages/airlines_page.py` showing that file is NOT in this task's diff at all.

NEEDS A HUMAN / LIVE-BROWSER FOLLOW-UP for the rendered result. The static checks prove the guard exists in the right place with the right declaration and that the JS assignment is gated — they cannot prove the browser's resolved cascade actually hides the element, because that needs a real layout engine and `playwright` is not installed here. The paired check that WOULD prove it already exists as a pattern: `test_browser_ux.py`'s `.banner__pill[hidden]` check (~line 6330) asserts a real `getComputedStyle().display === "none"`. Record in the SUMMARY, verbatim, the two-step check for the developer or a playwright-capable CI run:
  1. On Compagnies, click any ordinary airline illustration. The dialog must show the picture, its caption, and NOTHING between the caption and the close control — no Prefix / First seen / Last seen / Times seen / Example callsign labels, not even empty ones.
  2. On the same page, click an unidentified-prefix (gap) card. That block must appear, fully populated, and "Example callsign" must read a real callsign (e.g. `TVF37GK`), never "Illustration ...".
  </done>
</task>

<task type="auto">
  <name>Task 3 (FIX 3): the runway caption stops describing the map Phase 27 removed — both languages, one atomic edit</name>
  <files>companion/pages/config_page.py, companion/i18n_fr/display.py</files>
  <action>
The developer quoted this caption as his example of useless descriptive text. It is worse than verbose: two of its three clauses describe a schematic runway diagram that Phase 27 (CFG-66) removed outright. `companion/i18n_fr/display.py`'s own comment above the French entry still says the group "now draws a map". Both languages still ship it.

Two files must move together, because `companion/i18n.py`'s lookup is keyed by the exact English source string. If the English constant changes and the French dict KEY does not, the key becomes dead and the French page silently falls back to English. Edit both in one commit.

**`companion/pages/config_page.py`, `RUNWAY_SECTION_CAPTION` (~line 468).** Drop ONLY the two clauses about the drawing. Keep the three surviving facts — which runway, what it does not claim about timing, when it applies — with nothing added:

```
RUNWAY_SECTION_CAPTION = (
    "Which Orly runway the device watches. Applies on the next "
    "scheduled poll, not immediately.")
```

Also rewrite the ~14-line comment block immediately above it (~lines 455-467). That block currently argues at length for the clause you are deleting ("the group now draws a diagram, and a diagram that implies a scale it does not have..."). Replace that argument with a short one naming this quick task, Phase 27's CFG-66 removal of the map, and the developer's 2026-09-21 report — and do NOT quote the deleted clause verbatim in the new comment; name it by concept ("the two schematic-drawing clauses"). The one-caption-per-section rule the surrounding comment establishes still holds and must survive in the rewritten text. Leave `LED_SECTION_CAPTION` below it untouched; note that it already ends on the same "Applies on the next scheduled poll." clause, which is why the shortened runway caption reads consistently with its sibling.

Do NOT touch `RUNWAY_SECTION_CAPTION_ID`, `_with_next_wake()`, or any render site. Verified during planning: `config_page.py:2398` wraps this constant in `_with_next_wake(i18n.t(...), next_wake_clock)`, which appends the "(next wake ≈ HH:MM)" suffix — that mechanism is indifferent to the caption's length and needs no change.

**`companion/i18n_fr/display.py`, the runway-card entry (~lines 152-164).** The dict KEY must become byte-identical to the new English constant, and the VALUE must drop exactly the corresponding French clause:

```
    "Which Orly runway the device watches. Applies on the next "
    "scheduled poll, not immediately.":
        "Quelle piste d’Orly l’appareil surveille. S’applique lors "
        "de la prochaine vérification programmée, pas immédiatement.",
```

Write the French with the real characters, not escapes — they are shown escaped here only so the plan is unambiguous. **Three typography facts, verified against the live catalogue during planning, all enforced by `companion/test_i18n.py` Check 4 (D-09):**
  - The apostrophes are U+2019 (`’`), never ASCII `'`. The current entry already uses them (`d’Orly`, `l’appareil`, `S’applique`); keep them.
  - The current entry also carries U+00A0 before its colon (`indicatif\xa0:`). The new sentence has no colon, semicolon, question mark or exclamation mark at all, so no NBSP is required anywhere in it.
  - Nothing else in either sentence changes. Do not re-translate, do not "improve" the wording.

Also rewrite the 5-line comment above that entry. It currently explains the schematic clause "because the group now draws a map" — a statement that has been false since Phase 27. Replace it with a short note naming this quick task and the fact that the clause described a removed drawing, again without quoting the deleted clause verbatim.

**No new check is added by this task, deliberately.** The drift guard already exists and is stronger than a grep: `companion/test_i18n.py`'s ast-based Check 1 requires every producible English module constant to have a French catalogue entry, and Check 2 requires every catalogue entry to be read by some scanned source. Changing one side without the other fails BOTH directions automatically. Prove that claim by mutation rather than asserting it: change only the English constant, run `companion/test_i18n.py`, confirm it fails, quote the real failure message in the SUMMARY, then revert with `git checkout-index -f -- companion/pages/config_page.py` and apply the real two-file edit.

Verified during planning: all seven existing `RUNWAY_SECTION_CAPTION` references in `companion/test_config_page.py` (lines 2230, 3475, 3510, 3768, 9381, 9770) read the constant BY REFERENCE via `config_page.RUNWAY_SECTION_CAPTION`, never as a hardcoded literal — so none needs editing. Confirm by running the harness, not by trusting this paragraph; if any check does turn out to hardcode the old wording, retarget it in place.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 -c "
import sys
import companion.pages.config_page as cp
import companion.i18n_fr as fr
c = cp.RUNWAY_SECTION_CAPTION
if c != 'Which Orly runway the device watches. Applies on the next scheduled poll, not immediately.':
    sys.exit('EN constant is not the agreed text: %r' % c)
if c not in fr.CATALOG: sys.exit('FR key drift — the catalogue has no entry for the new EN constant, the page will fall back to English')
v = fr.CATALOG[c]
for word in ('schéma', 'échelle', 'indicatif'):
    if word in v: sys.exit('FR value still describes the removed drawing: %r' % v)
if '’' not in v: sys.exit('FR value must use the typographic apostrophe U+2019: %r' % v)
if \"'\" in v: sys.exit('FR value contains a straight apostrophe: %r' % v)
print('OK EN:', c); print('OK FR:', v)
"</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_i18n.py 2&gt;&amp;1 | tail -1   # expect 24/24 — Checks 1 and 2 are the key/constant drift guard, Check 4 is the apostrophe/NBSP guard</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_config_page.py 2&gt;&amp;1 | tail -1   # expect 266/266 unchanged — every existing RUNWAY_SECTION_CAPTION check reads the constant by reference</automated>
  </verify>
  <done>
Both files edited in one commit; the French dict key byte-equals the new English constant (proved at runtime, not by grep); the French value carries U+2019 and no straight apostrophe and no longer mentions the drawing; both stale comment blocks rewritten without quoting the deleted clause; `test_i18n.py` 24/24; `test_config_page.py` 266/266 with no check edited. The drift guard is mutation-proven: the English-only mutation made `test_i18n.py` fail, and that failure message is quoted in the SUMMARY.

FULLY AUTOMATED. No human follow-up needed — the rendered French page is already covered by `test_i18n.py` Check 3, which renders every page in French against a running service.
  </done>
</task>

<task type="auto">
  <name>Task 4 (FIX 4): the batch's containment gate, made executable — every search input in the app, forever, plus proof this batch added no script, route, CSP directive or token</name>
  <files>companion/test_status_pages.py</files>
  <action>
FIX 4 in the source data is a constraint, not a sixth defect: "the autocomplete popup fix and FIX 3 are unrelated to CSP/routes/scripts; both are pure string/attribute/CSS edits with no new script, no new route, no new custom property." This task turns that constraint into two things a machine enforces, rather than a sentence somebody has to remember.

**Part A — make Task 1 permanent, and prove it missed no site.**
Task 1 fixed three hand-copied literals. A fourth filter bar added in a later phase (Phase 29's CFG-83 touches the Flights list; CFG-82 touches Compagnies) would reintroduce the Safari autofill defect with nothing to catch it. This project's own sixth standing contract is "assert relationships, not endpoints" — so assert the relationship: *every* `<input type="search"` this app can render carries the three attributes.

Add ONE check to `companion/test_status_pages.py` (the harness that already owns cross-page source scans over `companion/pages/`). Implement it as a source scan, not a render scan, because the literal is a `%s`-template and one of the three sites lives on a page whose filter bar renders only when the unresolved registry is non-empty:

- Enumerate `companion/pages/*.py` plus `companion/app.py` from disk with `glob`/`os.listdir` — never a hand-written file list, which is exactly what would go stale when a page module is added.
- **Scan with `ast`, not with a raw substring search over the file text, and exclude docstrings.** This is not fussiness: `companion/pages/history_page.py:947` — `_filter_bar_html()`'s own docstring — describes the markup and therefore contains the very substring you are matching, without any attributes on it. A naive text scan fails on that docstring forever. Follow `companion/test_i18n.py`'s own methodology (`ast.parse()` of the source, never an import of the scanned module): walk every `ast.Constant` string node, skip any node that is the first statement expression of a `Module`/`FunctionDef`/`AsyncFunctionDef`/`ClassDef` (that is a docstring), and search only the surviving literals. Verified during planning that this yields exactly 3 hits today — one per builder — and correctly excludes the docstring. Note in the check's comment that `ast` folds adjacent string literals, so each hit arrives inside the builder's whole concatenated markup literal, not as a bare tag.
- Within each surviving literal, for every occurrence of `<input type="search"`, take the tag text up to the next `>` and require all three of `autocomplete="off"`, `spellcheck="false"` and `autocapitalize="characters"` in it.
- Require the total occurrence count across all scanned files to be at least 3 (so an accidental deletion of all of them cannot make the check vacuously pass), and report the real count in the check's own PASS name or message so a future reader sees the number grow.
- Failure message must name the defect being prevented: iOS Safari offers contact/phone-number autofill on a bare `type="search"` field with no `name` and no `autocomplete`, as the developer photographed on 2026-09-21.

Mutation-prove it two ways, quoting both real failure messages in the SUMMARY: (1) strip one attribute from `companion/pages/health_page.py`'s input and confirm the failure names that file; (2) confirm the ≥3 floor bites, by pointing the scan at an empty file list in a scratch run or by exercising the count assertion directly — then revert with `git checkout-index -f -- companion/pages/health_page.py`.

Bump `EXPECTED_CHECK_COUNT` per the convention — new assignment below Task 2's, comment naming `quick task 260921-n2n Task 4`, value re-derived by running.

**Part B — prove the batch's containment over its own diff.** No source edit; this is a gate, run and recorded. Against `BASE=4900256`, assert every one of these and paste the actual command output into the SUMMARY:
- `companion/app.py` is not in the diff at all → no new route, no CSP change, no new flash message.
- No new file anywhere → in particular nothing new under `companion/static/`, so no new script and no new stylesheet.
- `companion/static/style.css`'s added lines declare no new `--custom-property` and no new class selector (the only addition is `.resolve-context[hidden]`, a guard on a class that already exists).
- No `<script` substring is added to any file.
- The full 18-harness suite runs, and `companion/test_browser_ux.py` prints its `SKIP` line and exits 0 rather than failing.

**Baseline honesty.** `.planning/STATE.md` records "exactly the 5 sandbox baseline failures by NAME" for the full-suite run in this environment. Run the full suite, and in the SUMMARY name each failing harness and state whether it is one of those 5 known sandbox failures or something this batch introduced. Do not report "suite green" if it is not; do not absorb a new failure into the known list.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py 2&gt;&amp;1 | tail -1   # expect 309/309 (308 after Task 2, + 1), re-derived by running</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; BASE=4900256 &amp;&amp; test -z "$(git diff --name-only $BASE..HEAD -- companion/app.py)" &amp;&amp; echo "OK app.py untouched — no route, no CSP change" || (echo "FAIL app.py is in the diff"; exit 1)</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; BASE=4900256 &amp;&amp; test -z "$(git diff --diff-filter=A --name-only $BASE..HEAD)" &amp;&amp; echo "OK no new files — no new script, no new stylesheet" || (echo "FAIL new file(s) added"; exit 1)</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; BASE=4900256 &amp;&amp; N=$(git diff -U0 $BASE..HEAD -- companion/static/style.css | grep '^+' | grep -cE '^\+\s*--[a-z]') ; test "$N" = "0" &amp;&amp; echo "OK no new CSS custom property" || (echo "FAIL $N new custom propert(ies)"; exit 1)</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; BASE=4900256 &amp;&amp; N=$(git diff $BASE..HEAD | grep '^+' | grep -c '&lt;script') ; test "$N" = "0" &amp;&amp; echo "OK no new script tag" || (echo "FAIL $N added &lt;script"; exit 1)</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; scripts/run-all-tests.sh 2&gt;&amp;1 | tail -30   # every failing harness named in the SUMMARY and classified as known-sandbox or new; test_browser_ux.py must print SKIP and exit 0</automated>
  </verify>
  <done>
One new check in `test_status_pages.py` scans every `companion/pages/*.py` and `companion/app.py` for `<input type="search"`, requires all three attributes on each, refuses to pass on fewer than 3 matches, and is mutation-proven both ways with the real failure messages quoted. `test_status_pages.py` 309/309. All five containment assertions pass and their literal output is pasted into the SUMMARY. The full 18-harness suite was run; every failing harness is named and classified as one of the 5 known sandbox baseline failures or as new (and if new, this batch does not ship until it is fixed). `companion/test_browser_ux.py` printed SKIP and exited 0 — recorded as a skip, never as a pass.

FULLY AUTOMATED. No human follow-up needed; this task is itself the machine-checkable half of what a human would otherwise have to remember.
  </done>
</task>

<task type="auto">
  <name>Task 5 (FIX 5): measure the "écart bizarre" before changing anything — and pin Phase 27's measured floor so it cannot be undone</name>
  <files>companion/static/style.css, companion/test_status_pages.py</files>
  <action>
**This task is allowed to conclude "no defect, closing as verified-fine". It is not allowed to guess a pixel fix.**

The developer's screenshot of an expanded Flights detail row reads as a hex code, a full timestamp, a runway, a large blank gap, then "Voir l'image". The orchestrator explicitly could not confirm the resolved pixel gap without a live render and said so.

**Read the whole structure first — every value below was verified from source during planning, so do not re-derive it, verify it and move on:**
- `companion/pages/history_page.py:_flight_detail_row_html()` (~line 1142-1148) emits, inside one `<td colspan="6">`: `.flight-detail-row__reveal` &gt; `.flight-detail-row__reveal-inner` &gt; `<dl class="flight-detail-row__grid">`, then the callsign `_copy_button_html()` output, then `row["view_panel_html"]`.
- `.flight-detail-row__reveal-inner` (style.css ~4393) is a plain block: `overflow: hidden; min-height: 0; padding-left: var(--space-md)`. No flex, no gap.
- `.flight-detail-row__grid` (style.css ~4269) carries `margin: 0 0 var(--space-lg)` = 24px bottom.
- The callsign copy button is `.copy-btn`: `width: 22px; height: 22px; display: inline-flex`, with `::before { inset: -11px }` synthesizing a 44x44 pointer target. Its `[data-copy-feedback]` sibling is `.visually-hidden` (absolutely positioned, 1x1, clipped) and occupies no layout space.
- The "View picture" trigger is `_view_panel_button_html()` → `<button class="calendar-disconnect-btn">`, which declares `min-height: 30px` and **no `display`**, so it is the base `button` rule's default inline-block.
- **Therefore the copy button and the "Voir l'image" button are two inline-level siblings in ONE line box** immediately after the grid — not two stacked blocks.

**The hard constraint, which the source-data's suggested remedy would violate.** That 24px bottom margin is not slack: the comment directly above `.flight-detail-row__grid` records Phase 27 / CFG-70 / `27-08-PLAN.md` Task 3 MEASURING the prior 0-margin state, finding the grid's last-row copy button resolved to 34x26 against its declared 44x44 because the trailing button's own 11px reach won the overlap, and choosing `var(--space-lg)` because two adjacent 11px reaches need 22px of clearance. `var(--space-md)` is 16px, which is **below that 22px floor** — reducing the margin to `--space-md` would silently re-break CFG-70's mutation-tested 44x44 hit-target contract. **That remedy is forbidden.** Any margin value below 22px is forbidden. `.copy-btn`'s own geometry (22x22 visual, `inset: -11px`) is out of scope and must not be touched.

**Step 1 — compute the static bound and write it down.** Using only the declarations above and the token scale (`--space-xs: 4px, --space-sm: 8px, --space-md: 16px, --space-lg: 24px`, verified at style.css:141-144), state in the SUMMARY the exact vertical distance the stylesheet itself contributes between the grid's last baseline and the buttons' line box, and name every contributor. Verified during planning that **no offline CSS-cascade resolver exists anywhere in this repo** (`companion/test_browser_ux.py` measures via a real `getComputedStyle()`; there is no static resolver to reuse) — so the one quantity you cannot compute is the inline line box's own leading, i.e. how much space baseline-aligning a 22px `inline-flex` button beside a 30px text button adds below the baseline. Say that plainly; do not estimate it as if it were measured.

**Step 2 — decide, with the burden of proof on changing something.**
- If the statically-attributable gap alone exceeds ~36px (1.5x `--space-lg`), there is a real excess with a real cause in the stylesheet: fix THAT cause, never by dropping the grid's margin below 22px. Name the cause and the fix in the SUMMARY.
- If it does not (the grid contributes exactly `--space-lg` = 24px, which is CFG-70's mandated floor plus 2px), then **make no CSS change**, and say so explicitly: the 24px is a measured, load-bearing value, and the screenshot's visual impression is not evidence of a defect in it. Record the leading hypothesis for what the developer actually saw — that the low-contrast 22px icon-only copy button, transparent at rest with a 14px glyph, reads as blank space to the left of "Voir l'image" rather than as an affordance — as a FINDING for the developer, and note that it is a control-visibility question (Phase 29 territory, CFG-81/CFG-83 touch this row), not a spacing bug. Do not fix it here.

**Step 3 — ship the durable deliverable either way.** Whatever Step 2 decides, add ONE check to `companion/test_status_pages.py` that makes CFG-70's measurement executable instead of a comment, asserting the RELATIONSHIP rather than the endpoint:
- Parse `.flight-detail-row__grid`'s `margin` shorthand's bottom value out of style.css's source, resolve the `var(--space-*)` token through the `:root` block parsed from the same file (do not hardcode 24), parse `.copy-btn::before`'s `inset` magnitude from the same file (do not hardcode 11), and assert `margin_bottom >= 2 * inset_magnitude`.
- The failure message must state the measured finding in full: two adjacent synthesized 44x44 pointer targets need their owners' visual boxes at least 2x11px apart, CFG-70 measured the earlier control at 34x26 when they were not, and a smaller margin here silently shrinks a hit target nothing else in the suite would catch.
- This is the check that would have failed if anyone had taken the source data's "reduce to `--space-md`" suggestion. Say that in the check's comment.
- Mutation-prove it: set the margin to `var(--space-md)`, run the harness, quote the real failure message, revert with `git checkout-index -f -- companion/static/style.css`.

Bump `EXPECTED_CHECK_COUNT` per the convention — new assignment below Task 4's, comment naming `quick task 260921-n2n Task 5`, value re-derived by running.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 -c "
import re, sys
s = open('companion/static/style.css', encoding='utf-8').read()
tok = dict(re.findall(r'--(space-[a-z]+):\s*(\d+)px', s))
i = s.index('.flight-detail-row__grid {')
blk = s[i:s.index('}', i)]
m = re.search(r'margin:\s*0\s+0\s+var\(--(space-[a-z]+)\)', blk)
if not m: sys.exit('could not parse the grid margin shorthand: %r' % blk)
mb = int(tok[m.group(1)])
j = s.index('.copy-btn::before {')
ib = s[j:s.index('}', j)]
n = re.search(r'inset:\s*-(\d+)px', ib)
if not n: sys.exit('could not parse .copy-btn::before inset: %r' % ib)
reach = int(n.group(1))
if mb &lt; 2 * reach: sys.exit('CFG-70 floor violated: margin-bottom %dpx &lt; 2 x %dpx reach' % (mb, reach))
print('OK margin-bottom %dpx (var(--%s)) &gt;= 2 x %dpx reach' % (mb, m.group(1), reach))
"</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py 2&gt;&amp;1 | tail -1   # expect 310/310 (309 after Task 4, + 1), re-derived by running</automated>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 &amp;&amp; server/.venv/bin/python3 companion/test_view_pages.py 2&gt;&amp;1 | tail -1   # expect 166/166 unchanged — the detail row's markup is not edited by this task</automated>
  </verify>
  <done>
The static bound is computed, written down with every contributor named, and the one uncomputable quantity (inline line-box leading) is declared uncomputable rather than estimated. A decision is recorded with its reasoning; if it is "no change", `git diff --stat $BASE..HEAD -- companion/static/style.css` shows only Task 2's `[hidden]` guard, proving no spacing was touched. The new relationship check is in place, resolves both numbers from the stylesheet rather than hardcoding them, and is mutation-proven by setting the margin to `var(--space-md)` with the real failure message quoted. `test_status_pages.py` 310/310.

NEEDS A HUMAN / LIVE-BROWSER FOLLOW-UP for the numeric gap itself. `playwright` is not installed in `server/.venv`, and this repo has no offline cascade resolver, so the resolved pixel distance cannot be measured here at all. Record in the SUMMARY, verbatim, the exact measurement a developer or a playwright-capable run should take, so it is a one-paste job rather than a re-investigation:
  - On Vols at 390px width, expand one flight row with a callsign AND a matching panel render, then evaluate, for the `.flight-detail-row__grid` and the trailing `.calendar-disconnect-btn` in the same `.flight-detail-row__reveal-inner`: `btn.getBoundingClientRect().top - grid.getBoundingClientRect().bottom`.
  - Report that number against 24px (the stylesheet's own contribution). Anything materially above it is inline line-box leading, and the remedy lives in how those two inline siblings are aligned — never in the grid's margin, which the new check now pins at &gt;= 22px.
  </done>
</task>

</tasks>

<source_coverage_audit>
Every item from every source artifact, mapped to the task that delivers it. No item is deferred, simplified, or split into a "v1".

| # | Source | Item | Task | Status |
|---|--------|------|------|--------|
| GOAL-1 | ROADMAP Phase 29 §"Lot A" | Five one-line fixes ship AHEAD of Phase 29 so it can assume them present | 1-5 | COVERED |
| REQ-1 | QUICK-260921-n2n | The batch is atomic, one commit per fix, no `docs(...)` prefix | 1-5 | COVERED |
| CONTEXT-FIX1 | Developer tour, screenshot 1 | Flights/Airlines filter inputs offer Safari phone-number autofill | 1 | COVERED (+ health_page.py's third site, found in planning) |
| CONTEXT-FIX2a | Developer tour, screenshot 4 | resolve-context block shows EMPTY on every ordinary illustration | 2 | COVERED (CSS `[hidden]` guard) |
| CONTEXT-FIX2b | Developer tour, screenshot 4 | "Example callsign" prints the image's own caption | 2 | COVERED (JS gate on the same `count`) |
| CONTEXT-FIX3 | Developer tour, quote 1 | Runway caption still describes the map Phase 27 removed, both languages | 3 | COVERED (both files, key + value) |
| CONTEXT-FIX4 | Orchestrator constraint | No new CSP directive, route, script or custom property | 4 | COVERED (made executable, not just asserted) |
| CONTEXT-FIX5 | Developer tour, screenshot 5 | "écart bizarre entre le texte et le bouton" — measure, never guess | 5 | COVERED (investigate-then-decide + CFG-70 floor pinned) |

**Explicitly out of scope, by developer decision recorded in ROADMAP Phase 29/30** — not gaps: the `edit_mode` / "Modifier les images" toggle and always-rendered Replace/Delete forms (CFG-81); the quiet-hours component and its normalised twin (CFG-80); the site-wide editorial floor and its harness (CFG-79); Compagnies gallery-first reordering (CFG-82); Vols pagination (CFG-83); État's battery-trend title (CFG-84); the whole Aspect section (Phase 30, CFG-85/86); the 17-09 audit's 44px tap-target item (not selected); the companion's real domain name (operations, no code).

**One planning-time addition, flagged for the developer rather than absorbed silently:** `companion/pages/health_page.py:3354` carries the identical bare `<input type="search">` literal with the identical Safari autofill defect. It was not in the developer's report. Task 1 fixes it and Task 4's exhaustive scan requires it; if the developer wants the Health filter left alone, that is a one-line revert plus loosening Task 4's floor from 3 to 2.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `<input type="search">` | Untrusted user keystrokes enter the filter field. Handled entirely client-side by `list-filter.js`; nothing is submitted, stored, or reflected server-side. |
| server-rendered `data-view-panel-*` attributes → `panel-lookup.js` DOM writes | Registry-derived values (callsign prefix, example callsign, counts, timestamps) cross from Python into the DOM as attribute values and are then copied into the dialog. |
| `RUNWAY_SECTION_CAPTION` / French catalogue → rendered HTML | Static developer-authored copy, interpolated through `escape_html()` at the render site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-n2n-01 | Information disclosure | The three `<input type="search">` filter fields | low | mitigate | This is the reported defect itself: Safari's contact-autofill heuristic offers the household's own phone numbers into a field on screen. `autocomplete="off"` is the documented suppression. No `name` attribute is added, so the value can never be submitted anywhere even if a future enclosing form appears. (Task 1) |
| T-n2n-02 | Tampering | `panel-lookup.js`'s `contextCallsign` write | low | accept | The write remains `textContent` on a leaf `<span>` — never `innerHTML`, never a new sink. Gating it narrows what is written; it cannot widen it. The value's own provenance (`escape_html(example_callsign)` at `airlines_page._gap_card_html()`, with an explicit non-string coercion guard already in place) is unchanged by this plan. No new attribute and no new server-side field are introduced, so no new untrusted path is created. |
| T-n2n-03 | Tampering | `.resolve-context[hidden]` CSS guard | low | accept | An additive `display: none` rule cannot reveal anything. It makes a block that is currently visible-but-empty actually hidden — strictly less rendered, never more. The registry values inside it were already server-escaped. |
| T-n2n-04 | Information disclosure | Runway caption copy, both languages | low | accept | The change only DELETES two clauses of static developer-authored prose. It introduces no interpolation, no new `%s`, and no new data source. `escape_html()` at the render site is untouched. |
| T-n2n-SC | Tampering | npm/pip/cargo installs | n/a | accept | **No package install occurs anywhere in this plan.** No dependency is added, upgraded or removed; `server/requirements.txt` and `server/requirements-dev.txt` are not in `files_modified`. The `playwright` package is deliberately NOT installed (see `<execution_context>`). RESEARCH.md's Package Legitimacy Audit is therefore not required, and Task 4's containment gate asserts no new file appears anywhere in the diff, which mechanically confirms it. |
| T-n2n-05 | Elevation of privilege | CSP / routes / scripts | low | mitigate | Task 4 asserts mechanically that `companion/app.py` is absent from the whole batch diff (no route, no CSP directive change), that no file is added anywhere (no new script, no new stylesheet), and that no `<script` substring is added to any file. Enforced by command output pasted into the SUMMARY, not by assertion. |
</threat_model>

<verification>
Run from the worktree, after all five tasks:

1. `server/.venv/bin/python3 companion/test_view_pages.py` → 166/166 (baseline 164 + Task 1 + Task 2)
2. `server/.venv/bin/python3 companion/test_status_pages.py` → 310/310 (baseline 306 + Task 1 + Task 2 + Task 4 + Task 5)
3. `server/.venv/bin/python3 companion/test_config_page.py` → 266/266 (unchanged — no check edited by Task 3)
4. `server/.venv/bin/python3 companion/test_i18n.py` → 24/24 (Checks 1/2 = the EN-constant/FR-key drift guard; Check 4 = apostrophe + NBSP)
5. `scripts/run-all-tests.sh` → every failing harness named and classified as one of the 5 known sandbox baseline failures or as new; `companion/test_browser_ux.py` prints SKIP and exits 0
6. All five Task 4 containment assertions pass, output pasted into the SUMMARY
7. Every count above **re-derived by running**, never by arithmetic. If a number differs from the prediction, report the real number and explain the difference — do not adjust `EXPECTED_CHECK_COUNT` to match a prediction.
8. Four new checks, each mutation-proven with the real failure message quoted verbatim; every mutation reverted with `git checkout-index -f -- <path>`.

Human / live-browser follow-ups to hand the developer (none of them blocks the merge):
- Safari: tap the Vols filter field — no contact suggestions, keyboard opens shifted (Task 1).
- Compagnies: an ordinary illustration shows no resolve-context block at all; a gap card shows it populated with a real callsign (Task 2).
- Vols at 390px: the measured `btn.top - grid.bottom` for an expanded row, reported against 24px (Task 5).
</verification>

<success_criteria>
- All five of the developer's 2026-09-21 Lot A items are addressed; Phase 29's "assumed present" precondition holds.
- Five commits, `fix(quick-260921-n2n): ...`, each independently revertable, each with the attribution line.
- Three filter inputs (not two) carry the three attributes, and a machine now prevents a fourth from shipping without them.
- The illustration lightbox hides its resolve-context block and never prints a picture caption under "Example callsign", with the resolve-mode behaviour byte-unchanged.
- The runway caption is short and true in both languages, and the FR catalogue key cannot drift from the EN constant without `test_i18n.py` failing.
- CFG-70's measured 22px hit-target floor is now an executable check rather than a comment — and no spacing was changed on a guess.
- No new script, route, CSP directive, CSS custom property, CSS class family, package, or file anywhere in the batch.
- Nothing in Phase 29's or Phase 30's scope was touched. `ROADMAP.md` and `STATE.md` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260921-n2n-lot-a-five-fixes-from-the-developer-tour/260921-n2n-SUMMARY.md` when done.

The SUMMARY must carry, at minimum:
- Per task: the files touched, the commit SHA, and the harness counts before/after.
- Every new check's mutation proof, with the real failure message quoted verbatim.
- Task 4's five containment command outputs, pasted.
- Task 5's static bound with every contributor named, its decision, and its reasoning — including, if the decision was "no change", why the 24px is load-bearing.
- The full-suite run: every failing harness named and classified known-sandbox vs new.
- The three human/live-browser follow-ups, written so the developer can run them without re-reading this plan.
</output>
