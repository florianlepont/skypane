---
phase: quick/260907-d7e
plan: 01
type: execute
wave: 1
depends_on: []
branch: claude/roadmap-dashboard-6d4c9b   # STAY HERE. Do not create or switch branches.
files_modified:
  - .planning/notes/skypane-ops-board.html
autonomous: true
requirements: []           # Decision-tracked quick task, same precedent as every quick plan in
                           # this repo (see 260905-e04-PLAN.md). Traceability is via `decisions`
                           # below, cited inline in every task action.
decisions:
  - D-01  # CONTEXT.md is the ONLY source of truth. Transcribe verbatim, never re-verify the facts.
  - D-02  # The base file is d46e22a: no "Avertissements de revue" section exists; build it fresh.
  - D-03  # The single existing P14 row is MISLABELED (it holds Phase 15's content) — replace it
          # with two rows (a real P14 + a corrected/updated P15), do not layer on top of it.
  - D-04  # Pill split inside the new section is authoritative: 14 neutral / 5 warn / 2 accent = 21.
  - D-05  # Only .planning/notes/skypane-ops-board.html changes. No source, no ROADMAP, no STATE.
  - D-06  # Artifact republish is ORCHESTRATOR-ONLY, after the commit. The executor must NOT do it.
  - D-07  # One atomic commit, with the Sonnet 5 trailer (different from prior commits in this repo).

must_haves:
  truths:
    - "The Roadmap section carries exactly two rows for the newly merged work — a `P14` row for the real Phase 14 (gallery-lightbox resolve) and a `P15` row for the per-direction themes work, both pilled good/Terminé — and the single mislabeled row is gone, replaced rather than supplemented (D-03)"
    - "A new `Avertissements de revue` section sits between the Bugs section and the footer, with six group headers and 21 `.warn-row` entries whose pills tally exactly 14 neutral / 5 warn / 2 accent (D-04)"
    - "The stat strip shows five tiles instead of four, and both the `.stats` grid and the 680px media query are updated so the odd fifth tile does not strand on mobile (D-01)"
    - "Every French string the refresh touches is byte-identical to the copy locked in CONTEXT.md — guillemets, curly apostrophes, `&nbsp;`, `<code>` tags and `&lt;`/`&gt;` escapes included, with no escaped tag accidentally becoming a live one (D-01)"
    - "The page's tag balance is still intact (div/p/section/span opens equal closes) and the commit touches exactly one file (D-05)"
  artifacts:
    - ".planning/notes/skypane-ops-board.html — the only file touched: refreshed header comment, masthead, stat strip, Roadmap section, Seeds section and footer, plus one new section and its three CSS rules"
  key_links:
    - "New `.warn-row`/`.warn-tag`/`.warn-name` CSS rules -> the 21 rows of the new section (rows reuse the pre-existing `.bug-desc` class nested inside `.warn-name`, same as the Bugs section)"
    - "`.stats` grid-template-columns repeat(5,1fr) -> the fifth stat tile -> the 680px `.stat:last-child` span-2 rule that keeps the odd tile from stranding"
    - "The new section's AVERTISSEMENTS banner comment -> the `sed -n '/AVERTISSEMENTS/,/footer>/p'` scoping every pill-count gate in this plan depends on"
---

<objective>
Bring `.planning/notes/skypane-ops-board.html` (a single self-contained French HTML board, published
as a Claude artifact) up to date with `origin/main` @ `d46e22a`: reflect Phases 14 and 15, fix the
mislabeled `P14` roadmap row, and add a brand-new "Avertissements de revue" section covering the 21
review/security warnings still open across Phases 3 through 15.

Purpose: the board is the developer's single glanceable status page for SkyPane. It is two phases
stale and currently shows Phase 15's content under a `P14` tag, which is actively misleading. The
new warnings section puts the open hardening backlog in the one place it will actually be seen.

Output: one modified file, one atomic commit. No source code, no `ROADMAP.md`, no `STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260907-d7e-refresh-the-skypane-ops-board-for-phases/260907-d7e-CONTEXT.md
@.planning/notes/skypane-ops-board.html
</context>

<transcription_contract>
READ THIS BEFORE TOUCHING THE FILE. It governs all three tasks.

**CONTEXT.md is the only source of truth (D-01).** Every fact, count, status, tag, pill class and
French sentence this plan needs already exists — final and locked — in `260907-d7e-CONTEXT.md`. The
orchestrator verified all of it directly against the code on `origin/main` @ `d46e22a` on
2026-09-07.

- Do NOT open `*-REVIEW.md`, `*-SECURITY.md`, `*-VERIFICATION.md`, `ROADMAP.md`, `REQUIREMENTS.md`,
  the seed files, or any Python/CSS source to check a claim. Re-verification is out of scope and
  burns context this plan does not have.
- Do NOT reword, shorten, "improve", re-tally or correct anything. If a status, a count or a
  sentence looks wrong to you, **transcribe it anyway** and record the observation in the SUMMARY's
  Deviations section. That is the only sanctioned outlet for a disagreement.
- Transcribe character-for-character: French guillemets, curly apostrophes, non-breaking entities,
  inline `<code>` tags, and escaped angle brackets. Never substitute a straight quote for a curly
  one, and never let an escaped tag inside a description become a live tag.
- Line references below point into `260907-d7e-CONTEXT.md`. They are a navigation aid; the section
  headings are authoritative if the numbers drift.

**Scope fence (D-05).** The one and only file this plan may modify is
`.planning/notes/skypane-ops-board.html`. Not the seed files (CONTEXT.md explicitly says the
SEED-003 frontmatter drift gets footnoted on the board and is NOT fixed at the source), not
`STATE.md` (the orchestrator updates it afterwards), not `ROADMAP.md`.

**Do not republish the artifact (D-06).** The board's own header comment tells a reader to republish
the file to its Claude artifact URL. That step belongs to the orchestrator, after this plan's commit
lands. The executor must not invoke the Artifact tool at all.

**Editing technique.** Use `Edit` with tight, unique anchors throughout. Never rewrite the whole
722-line file with `Write` — a full-file rewrite of a document this dense is how transcription
errors get introduced silently.
</transcription_contract>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the existing page — CSS, stat strip, Roadmap rows, Seeds, chrome</name>
  <files>.planning/notes/skypane-ops-board.html</files>
  <action>
Apply the seven scoped edits below to the existing page, top-to-bottom so line numbers stay
predictable. Every string comes verbatim from CONTEXT.md per the transcription contract (D-01).

(a) CSS — three new rules. In the `<style>` block, immediately after the Bugs block's `.bug-date`
rule (around line 305) and before the `footer` rule, add the three rules given in CONTEXT.md "CSS
and new-section structure" (lines 239-246): `.warn-row` (a three-column grid, 96px / 1fr / auto,
plus its `:last-child` border reset), `.warn-tag` (IBM Plex Mono, faint ink, nowrap) and
`.warn-name` (weight 600). Copy the declarations exactly as written there. The row description
deliberately reuses the pre-existing `.bug-desc` class nested inside `.warn-name` — do not define a
new description class.

(b) CSS — stat grid widened. Change the `.stats` rule's grid-template-columns (line ~150) from four
equal columns to five, per CONTEXT.md line 249.

(c) CSS — responsive. Inside the existing 680px media block (starts ~line 323), add the two lines
from CONTEXT.md lines 247-248: a `.stat:last-child` span-2 rule and a `.warn-row` single-column
collapse. Put them alongside the existing rules, not in a new media block.

(d) Stat strip markup. The strip currently holds four tiles (lines ~349-368). Update tile 1's value
and note, update tile 4's value and note, and append a fifth tile — all five exactly as specified in
CONTEXT.md "Stat strip" (lines 110-118), keeping the established tile shape (a `stat-value` div with
its colour class, a `stat-label`, a `stat-note`). Tiles 2 and 3 are unchanged. Give the new fifth
tile the `warn` value colour, matching how the board already colours a non-zero open-work count.

(e) Roadmap section. Four edits, all specified in CONTEXT.md "Roadmap section — exact replacement
text" (lines 120-170):
  1. the `board-head` count span (line ~376) — 26 phases becomes 28, date range unchanged;
  2. the `board-intro` paragraph (line ~378) — thirteen main phases becomes fifteen;
  3. **the row replacement (D-03)** — the row block at lines ~533-545 is tagged `P14` but carries
     Phase 15's content, and there is no row for the real Phase 14 anywhere. Delete that entire
     `<div class="row">` block and put TWO rows in its place, in this order: CONTEXT.md's "Row 1
     (new, real Phase 14)" then its "Row 2" (the same subject matter, retagged `P15`, retitled and
     updated to shipped). Follow the markup shape the neighbouring P13 row already uses — a
     `row-tag` span, a `row-main` div holding `row-title`, `row-desc`, an optional `row-note` and
     the accent variant `<p class="row-desc" style="margin-top:5px;color:var(--accent-ink);">`, then
     the pill span and the `row-date` span. Both new rows are pilled good/Terminé, dated 6 sept.
     Row 1's description contains an escaped tag reference — keep it escaped;
  4. the SEUL POINT OUVERT callout (line ~549) — swap only the one sentence about Phase 15 for the
     replacement at CONTEXT.md lines 168-169. The sentences before and after it stay untouched.

(f) Seeds section. Two edits from CONTEXT.md "Seeds section — exact replacement text" (lines
172-197): the `board-intro` at line ~560 (the promoted-phase list becomes 10, 11, 12, 13, 15 — not
14, since Phase 14 was developer-raised rather than seed-promoted), and the SEED-003 card's
`seed-trigger` paragraph at line ~570, replaced wholesale by CONTEXT.md's "new:" version. The
SEED-003 card's title, pill and body paragraph are already correct — leave them alone. No other seed
card changes.

(g) Header comment, masthead, footer. From CONTEXT.md lines 205-236: replace the header comment's
`Dernière génération` line (HTML lines 34-35); append the two new entries to the header comment's
SOURCES list, placed after the `.planning/debug/` entry and before the VÉRIFIER paragraph,
preserving that list's hanging-indent alignment; replace the `masthead-sub` paragraph (line ~341);
and extend the footer's first span (line ~718) by appending the two new code spans right after
`54 quick tasks`, inside the span. One transcription note: CONTEXT.md frames the footer edit as
generalizing an existing single-phase SECURITY.md reference, but this base file has no such
reference in the footer — so it is a pure append of the two wildcard code spans, nothing to
generalize. Record that in the SUMMARY.
  </action>
  <verify>
    <automated>f=.planning/notes/skypane-ops-board.html; test "$(grep -c 'div class="stat"' $f)" = 5 && test "$(grep -c 'repeat(5,1fr)' $f)" = 1 && test "$(grep -c 'stat:last-child' $f)" = 1 && test "$(grep -c '\.warn-row' $f)" -ge 2 && test "$(grep -c '\.warn-tag{' $f)" = 1 && test "$(grep -c '\.warn-name{' $f)" = 1 && echo "T1a css+stats OK"</automated>
    <automated>f=.planning/notes/skypane-ops-board.html; test "$(grep -o 'row-tag">P14' $f | wc -l | tr -d ' ')" = 1 && test "$(grep -o 'row-tag">P15' $f | wc -l | tr -d ' ')" = 1 && test "$(grep -c 'pill accent">Cadrée' $f)" = 0 && test "$(grep -c '28 phases · 2026-08-26' $f)" = 1 && echo "T1b roadmap rows OK"</automated>
    <automated>f=.planning/notes/skypane-ops-board.html; test "$(grep -c 'le cadrage a gardé les volets' $f)" = 0 && test "$(grep -c 'en cadrage) est ouverte mais indépendante' $f)" = 0 && test "$(grep -c '(10, 11, 12, 13, 15), toutes livrées' $f)" = 1 && test "$(grep -c '2026-09-07, Phases 14 et 15 intégrées' $f)" = 1 && test "$(grep -c 'phases/\*-SECURITY.md' $f)" = 1 && echo "T1c seeds+chrome OK"</automated>
  </verify>
  <done>
Five stat tiles render; the `.stats` grid and the 680px media block carry the two new lines; the
three new CSS rules exist. The Roadmap section shows exactly one `P14` row (gallery-lightbox
resolve) and exactly one `P15` row (per-direction themes), both Terminé, and the stale scoping pill
no longer appears anywhere on the page. Seeds intro, SEED-003 trigger, header comment, masthead-sub
and footer all carry the new copy. `git diff --name-only` still lists only the board file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the "Avertissements de revue" section (6 groups, 21 rows)</name>
  <files>.planning/notes/skypane-ops-board.html</files>
  <action>
Insert one brand-new `<section class="board">` between the closing tag of the Bugs section and the
footer element. Build it from scratch (D-02) — no partial version of this section exists on this
base, so there is nothing to merge into. Structure per CONTEXT.md "CSS and new-section structure"
(lines 250-254):

- A section-delimiter HTML comment in the board's established banner style, and it MUST contain the
  token AVERTISSEMENTS in the same all-caps form the other banners use. Every pill-count gate in
  this plan scopes on that token via `sed -n '/AVERTISSEMENTS/,/footer>/p'`, so the section is
  unverifiable without it.
- A `board-head` div holding an `h2` reading `Avertissements de revue` plus the `count` span whose
  text is given at CONTEXT.md lines 251-252.
- The `board-intro` paragraph verbatim from CONTEXT.md lines 255-258.
- Six `<p class="backlog-group-name">` group headers, each immediately followed by its own rows. The
  six headers, in this exact order, are the bolded ones at CONTEXT.md lines 276, 322, 335, 345, 353
  and 361 — the parenthesised per-group counts are part of the header text.
- The 21 rows, in the numbered order given at CONTEXT.md lines 274-394. Each row is a
  `<div class="warn-row">` containing, in order: a `<span class="warn-tag">` with the tag, a
  `<span class="warn-name">` holding the title with a nested `<span class="bug-desc">` for the
  description (the same nesting the Bugs section already uses for `.bug-name`/`.bug-desc`), then a
  `<span class="pill …">` with the class and label stated on that row. There is no date column.
- A closing `<p class="note-box">` with the text verbatim from CONTEXT.md lines 260-272.

Row-by-row mapping, so nothing is invented: CONTEXT.md numbers the rows 1-21 and states for each a
tag, a pill class plus its label, a title and a description. Rows 1-9 are group 1 (Phase 13 revue de
code), 10-11 group 2 (Phase 13 audit sécurité), 12 group 3 (Phase 14 audit sécurité), 13 group 4
(Phase 15 revue de code), 14 group 5 (Phase 15 audit sécurité), 15-21 group 6 (Revues antérieures).

Three transcription traps in this block, all of which the gates below will catch:
- Row 17 (tag `6.2 · WR-01`) is followed in CONTEXT.md by a bolded parenthetical flagging that its
  description changed since the last publish. That parenthetical is editorial commentary addressed
  to you — it is NOT part of the row's copy. Transcribe the description, drop the parenthetical.
- The pill split is authoritative and pre-tallied (D-04): 14 neutral, 5 warn, 2 accent, totalling
  21. CONTEXT.md lines 396-400 enumerate which row numbers carry which class. If your rows do not
  tally to 14/5/2, you mis-transcribed a pill class — fix the row, never the tally.
- Descriptions and the closing note-box carry heavy inline markup: `<code>` spans, escaped angle
  brackets, non-breaking entities, guillemets, curly apostrophes and one `<em>`. Every escaped tag
  must stay escaped so it renders as literal text rather than injecting an element into the page.
  </action>
  <verify>
    <automated>f=.planning/notes/skypane-ops-board.html; test "$(grep -o 'class="warn-row"' $f | wc -l | tr -d ' ')" = 21 && test "$(grep -o 'p class="backlog-group-name"' $f | wc -l | tr -d ' ')" = 9 && test "$(grep -c 'section class="board"' $f)" = 5 && echo "T2a structure OK — 21 rows, 3 baseline + 6 new group headers, 5 sections"</automated>
    <automated>f=.planning/notes/skypane-ops-board.html; S=$(sed -n '/AVERTISSEMENTS/,/footer>/p' $f); test "$(printf '%s' "$S" | grep -o 'pill neutral' | wc -l | tr -d ' ')" = 14 && test "$(printf '%s' "$S" | grep -o 'pill warn' | wc -l | tr -d ' ')" = 5 && test "$(printf '%s' "$S" | grep -o 'pill accent' | wc -l | tr -d ' ')" = 2 && echo "T2b pill split OK — 14/5/2 = 21"</automated>
    <automated>f=.planning/notes/skypane-ops-board.html; test "$(grep -o '&nbsp;' $f | wc -l | tr -d ' ')" = 6 && test "$(grep -c '&lt;script&gt;' $f)" = 1 && test "$(grep -c '&lt;form&gt;' $f)" = 1 && test "$(grep -c '&lt;dialog&gt;' $f)" = 1 && echo "T2c entity transcription OK"</automated>
  </verify>
  <done>
The new section renders between Bugs and the footer with its head, intro, six group headers, 21
rows and closing note-box. Scoped pill counts inside the section are exactly 14 neutral, 5 warn and
2 accent. The six escaped-entity gates pass, proving no escaped tag was flattened into live markup
during transcription.
  </done>
</task>

<task type="auto">
  <name>Task 3: Structural verification and the single atomic commit</name>
  <files>.planning/notes/skypane-ops-board.html</files>
  <action>
First run the structural gates below and only then commit (D-07). If a gate fails, fix the HTML —
never relax the gate.

Structural checks, in order: tag balance across div, p, section and span (opens must equal closes —
the base file was balanced at 92/92, 73/73, 4/4 and 130/130, so any mismatch is something this task
introduced); scope confirmation that the working tree carries exactly one modified path and nothing
is already staged; and a re-run of the full gate set from Tasks 1 and 2 against the final file, so
a late edit cannot silently undo an earlier one.

Then stage only `.planning/notes/skypane-ops-board.html` and make one commit whose subject line is
exactly `docs(quick-260907-d7e): ops board — Phases 14/15 and the review-warnings refresh` and whose
final line is the trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. That trailer
differs from the one on prior commits in this repo — use exactly this one, it reflects the current
session's attribution instructions. Do not create or switch branches: the commit lands on
`claude/roadmap-dashboard-6d4c9b`, where the tree already is.

Do NOT republish the Claude artifact (D-06). The republish step belongs to the orchestrator and runs
after this commit.

In the SUMMARY, record every observation the transcription contract routed here: the footer's
missing SECURITY.md reference noted in Task 1(g), and anything in CONTEXT.md's copy that looked off
but was transcribed anyway.
  </action>
  <verify>
    <automated>python3 -c "import re; o=chr(60); s=open('.planning/notes/skypane-ops-board.html',encoding='utf-8').read(); bad=[t for t in ('div','p','section','span') if len(re.findall(o+t+r'[ >]',s))!=len(re.findall(o+'/'+t+'>',s))]; print('unbalanced tags:', bad or 'none'); raise SystemExit(1 if bad else 0)"</automated>
    <automated>test "$(git diff --name-only)" = ".planning/notes/skypane-ops-board.html" && test -z "$(git diff --cached --name-only)" && echo "scope OK — one modified path, nothing pre-staged"</automated>
    <automated>test "$(git show --pretty=format: --name-only HEAD | grep -c .)" = 1 && git log -1 --pretty=%s | grep -qF 'docs(quick-260907-d7e): ops board' && git log -1 --pretty=%B | tail -1 | grep -qF 'Co-Authored-By: Claude Sonnet 5' && test "$(git rev-parse --abbrev-ref HEAD)" = "claude/roadmap-dashboard-6d4c9b" && echo "commit OK — 1 file, right subject, right trailer, right branch"</automated>
  </verify>
  <done>
Tag balance holds for div, p, section and span. HEAD is a single commit touching exactly one file,
on `claude/roadmap-dashboard-6d4c9b`, with the required subject and the Sonnet 5 trailer as its last
line. The working tree is clean. The artifact has NOT been republished — that is the orchestrator's
next step.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal `*-REVIEW.md`/`*-SECURITY.md` findings -> a published Claude artifact URL | Open, unfixed security findings for a live VPS-hosted companion service cross from the private repo into a shareable artifact page |
| CONTEXT.md escaped markup -> rendered HTML page | Escaped angle brackets inside 21 row descriptions become live DOM if mis-transcribed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d7e-01 | Information Disclosure | The 21 warning rows published to the board's artifact URL | low | accept | Developer-locked decision: this board exists to make the open hardening backlog visible, and the URL is an unlisted per-account artifact link. The row copy is behavioural (what is unguarded and why), naming only repo-relative filenames and symbol names already public in the repo — no credentials, no hostnames, no endpoint paths, no exploit steps. |
| T-d7e-02 | Tampering | Escaped-tag transcription in row descriptions and the note-box | low | mitigate | Task 2's third gate asserts the exact expected counts of each escaped construct plus `&nbsp;`, so any escape flattened into live markup during transcription fails the build before commit. Task 3's tag-balance check catches the structural symptom independently. |
| T-d7e-03 | Tampering | Scope creep beyond the single board file | low | mitigate | Task 3 gates on `git diff --name-only` equalling exactly one path with nothing pre-staged, and re-checks post-commit that HEAD touches exactly one file (D-05). |

No package-manager installs in this task, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. All three Task 1 gates pass on the final file (CSS rules, five stat tiles, one P14 + one P15 row,
   stale scoping pill gone, seeds/header/masthead/footer copy in place).
2. All three Task 2 gates pass: 21 `.warn-row` entries, 9 total `backlog-group-name` headers (3
   pre-existing in Backlog v2 + 6 new), 5 board sections, scoped pills 14/5/2, entity counts intact.
3. Tag balance holds for div, p, section and span.
4. HEAD is one commit, one file, right subject, Sonnet 5 trailer, on
   `claude/roadmap-dashboard-6d4c9b`, working tree clean.
5. The artifact is NOT republished by this plan.
</verification>

<success_criteria>
- The board reads as current for `main` @ `d46e22a`: 27/28 phases, Phases 14 and 15 both Terminé,
  21 open warnings surfaced in their own section, 5 stat tiles.
- Zero re-verification of CONTEXT.md's facts happened; any doubts are recorded in the SUMMARY
  instead of being acted on.
- Exactly one file changed, in exactly one commit, on the existing branch.
</success_criteria>

<output>
Create `.planning/quick/260907-d7e-refresh-the-skypane-ops-board-for-phases/260907-d7e-SUMMARY.md`
when done, including a Deviations section covering the footer SECURITY.md note from Task 1(g) and
any other transcribed-but-questionable item.
</output>
