---
phase: quick
plan: 260924-u7n
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plane/render.py
  - server/test_render.py
  - server/test_fault_screen_mask.py
  - firmware/tools/gen_fault_screen.py
  - firmware/main/fault_screen_mask.h
  - firmware/main/fault_screen.h
  - firmware/main/fault_screen.c
  - firmware/tests/test_fault_screen.c
  - firmware/main/app_main.c
  - firmware/main/nvs_schema.h
  - firmware/VENDOR.md
  - ARCHITECTURE.md
  - .planning/REQUIREMENTS.md
  - .planning/seeds/on-device-fault-icon.md
  - .planning/quick/260924-u7n-device-06-local-no-connection-fault-scre/no-connection-preview.png
autonomous: true
requirements: [DEVICE-06]

must_haves:
  truths:
    - "On the 2nd consecutive failed wake (next_backoff_n >= 2) for a comm/data step, the device draws a NO CONNECTION screen from firmware alone, with no server round-trip"
    - "The screen is the fourth sibling of DISPLAY OFF / QUIET HOURS / BATTERY EMPTY: dark dithered field (~40% white), white alert triangle, tracked NO CONNECTION label, rule, two body lines"
    - "The screen is drawn once per outage (sentinel in FP_NVS_IMAGE_HASH), not on every failing wake, and a panel-guard deferral leaves the sentinel unwritten so the next failing wake retries"
    - "The first successful poll after recovery redraws the real server picture, because the sentinel never equals a server sha256: hash"
    - "blit, reset and deadline failures never trigger the fault screen"
    - "The `poll fail step=` Log Line Contract line is byte-identical and still emitted before any fault-screen work"
    - "A render.py copy/glyph change that is not regenerated into fault_screen_mask.h fails the pytest suite"
  artifacts:
    - path: "server/plane/render.py"
      provides: "draw_alert_icon(), NO_CONNECTION_* copy, _build_no_connection_canvas(flat=False)"
      contains: "def _build_no_connection_canvas"
    - path: "firmware/tools/gen_fault_screen.py"
      provides: "Header generator + firmware-equivalent preview (Python port of the C dither)"
    - path: "firmware/main/fault_screen_mask.h"
      provides: "Generated 1-bpp ink mask of the content block (~32 KB)"
      contains: "FP_FAULT_MASK_W"
    - path: "firmware/main/fault_screen.c"
      provides: "Pure (no ESP-IDF) fp_fault_screen_render() + fp_fault_screen_should_draw()"
    - path: "firmware/tests/test_fault_screen.c"
      provides: "Host test, auto-discovered by run_host_tests.sh"
    - path: "server/test_fault_screen_mask.py"
      provides: "Byte-for-byte drift test for the committed header"
  key_links:
    - from: "firmware/main/app_main.c fail_and_sleep()"
      to: "fp_fault_screen_should_draw / fp_fault_screen_render / fp_panel_draw"
      via: "call after NVS backoff update and after the poll fail log line, before enter_deep_sleep"
      pattern: "fp_fault_screen_should_draw\\(step, plan\\.next_backoff_n"
    - from: "firmware/main/fault_screen.c"
      to: "firmware/main/fault_screen_mask.h"
      via: "#include of generated mask"
      pattern: "#include \"fault_screen_mask.h\""
    - from: "server/test_fault_screen_mask.py"
      to: "firmware/tools/gen_fault_screen.py"
      via: "importlib load + render_header_text() == committed file"
      pattern: "render_header_text"
---

<objective>
Finish seed `.planning/seeds/on-device-fault-icon.md` by shipping its open half, DEVICE-06: a firmware-local "NO CONNECTION" hold screen drawn when the frame has failed to reach the server twice in a row.

Purpose: during an outage the server can never bake an alert into anything, so the frame today just freezes on its last picture. The device must say so itself.
Output: render.py composition + glyph (the source of truth for the artwork), a generator producing a committed 1-bpp mask header, a pure C module that dithers the field on-device and stamps the mask, wiring in app_main.c's single failure exit, tests on both sides, docs and planning closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/seeds/on-device-fault-icon.md
@.planning/quick/260923-fr4-battery-empty-screen-before-the-pack-die/260923-fr4-PLAN.md

Relevant source (read the named ranges, not whole files):
- server/plane/render.py: BATTERY_EMPTY_* copy (~L355-367), DIMMED_* constants (~L398-405), draw_source_fault_badge (~L781-860, triangle shape at ~L830-858), _build_battery_empty_canvas (~L2019), draw_empty_battery_icon (~L2182), _build_hold_canvas / _build_dimmed_hold_canvas (~L2215-2308). `_build_hold_canvas(..., field_idx, ink, dithered)` with `dithered=False` ALREADY yields the flat-field variant — reuse it, do not duplicate the layout.
- server/plane/dither.py dithered_state_background(): blend = round(black + (255-black)*0.4) = 102 per channel, Pillow FLOYDSTEINBERG quantize against {black, white}.
- server/test_render.py: tests near L3442-3470 (battery-empty copy + shared-composition tests) are the style to mirror; imports via REPO_ROOT on sys.path, `import server.plane.render as render`.
- firmware/main/app_main.c fail_and_sleep() (~L159-178), enter_deep_sleep() (~L119), on_wake_deadline() (~L183).
- firmware/main/state_machine.c ~L130-175: PSRAM malloc pattern, fp_api_release(); fp_wifi_stop(); ordering, deferral handling of fp_panel_draw.
- firmware/tests/test_sleep_decision.c: host-test style (SPDX header, HOST_TEST_DEPS comment, plain assert()).

<interfaces>
Facts established by the planner (no need to re-derive):
- fp_sleep_decide(FAILED, backoff_n, ...) returns next_backoff_n = backoff_n+1 saturating at UINT8_MAX. 1st failure: read 0 -> next 1. 2nd consecutive failure: read 1 -> next 2. So `next_backoff_n >= 2` == "this is the 2nd+ consecutive failure" (the seed's `backoff_n >= 2` rule, counted after increment). A healthy wake resets it to 0.
- Fail step tokens reachable today: wifi, http, status, json, auth, enrol, secret, config, download, verify, blit, reset, deadline (grep `fail_and_sleep(` and `fail_step_out = ` / step_for() in state_machine.c to confirm the full list).
- firmware/main/CMakeLists.txt uses `SRC_DIRS "."` — a new main/fault_screen.c is picked up automatically; NO CMake edit needed.
- run_host_tests.sh compiles tests/test_<name>.c + main/<name>.c with `cc -Wall -Wextra -std=c11`; extra sources via `/* HOST_TEST_DEPS: a.c */`.
- epd13in3e.h includes esp_err.h — fault_screen.h must NOT include it (or panel.h). Define its own FP_FAULT_SCREEN_WIDTH 1200, HEIGHT 1600, BYTES 960000; app_main.c adds `_Static_assert(FP_FAULT_SCREEN_BYTES == EPD_BYTES, ...)`.
- nvs_util.h: esp_err_t fp_nvs_get_str(const char *key, char *out, size_t cap); esp_err_t fp_nvs_set_str(const char *key, const char *value).
- panel.h: esp_err_t fp_panel_draw(const uint8_t *buf) — ESP_ERR_INVALID_STATE / ESP_ERR_TIMEOUT mean "deferred by the panel guard", not failure. fp_panel_on_boot() already ran before any fail_and_sleep except possibly "reset" (excluded anyway).
- wake_guard.h: fp_wake_feed() resets the task watchdog and is safe anywhere; fp_wake_checkpoint() can call on_wake_deadline() -> fail_and_sleep() — NEVER call fp_wake_checkpoint() anywhere in the fault-screen path (re-entrancy).
- Buffer format (PROTOCOL §1): 1600 rows x 600 bytes, left pixel in HIGH nibble; black = 0x0, white = 0x1.
- Pillow is 12.3.0 (server/requirements.txt), so ImageDraw.polygon(..., width=N) is available.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Artwork source of truth in render.py, mask generator, committed header, drift test</name>
  <files>server/plane/render.py, server/test_render.py, firmware/tools/gen_fault_screen.py, firmware/main/fault_screen_mask.h, server/test_fault_screen_mask.py, .planning/quick/260924-u7n-device-06-local-no-connection-fault-scre/no-connection-preview.png</files>
  <behavior>
    - NO_CONNECTION_HEADING_TEXT == "NO CONNECTION"; NO_CONNECTION_BODY_LINES == ("The frame can't reach its server.", "It will try again on its own.") by exact equality
    - _build_no_connection_canvas() is produced by _build_hold_canvas with DIMMED_FIELD_IDX/DIMMED_INK (dithered=True by default; flat=True passes dithered=False) and draw_alert_icon at ALERT_ICON_HEIGHT_PX — monkeypatch/spy like the battery-empty shared-composition test
    - the dithered canvas passes the same legal-palette / black-dominant checks the other hold screens pass; the flat canvas contains only indices {black, white}
    - build_canvas() never dispatches it: grep-level/inspect assertion that build_canvas's source does not reference _build_no_connection_canvas
    - draw_alert_icon returns ALERT_ICON_HEIGHT_PX and draws only the given ink index, with inked pixels above AND below the exclamation gap (dot present — guards the WR-02 regression class)
    - drift test: loading firmware/tools/gen_fault_screen.py via importlib.util.spec_from_file_location and calling render_header_text() returns a string equal to firmware/main/fault_screen_mask.h read from disk (byte-for-byte, compare encoded bytes)
  </behavior>
  <action>
In render.py (per the orchestrator design, resolving the seed's open question: same glyph for CFG-05 and DEVICE-06):
- Add NO_CONNECTION_HEADING_TEXT / NO_CONNECTION_BODY_LINES / NO_CONNECTION_BODY_TEXT next to the BATTERY_EMPTY_* block, with a comment in the same voice: English per the locked panel-copy decision, second sentence is true because the device retries on its own backoff and the server picture returns on the first healthy wake; note this copy is baked into firmware via gen_fault_screen.py, so changing it requires regenerating the header (the drift test enforces it).
- Add ALERT_ICON_HEIGHT_PX = 76 (same scale as power ring / crescent / runway / battery), ALERT_ICON_WIDTH_PX = 88, ALERT_ICON_STROKE_PX = 8 (Bold-class, safe against dither, matching POWER_ICON_STROKE_PX), and `draw_alert_icon(draw, center_x, top_y, ink_idx)` returning ALERT_ICON_HEIGHT_PX: an outline triangle (apex at top centre, base at top_y+height-1, polygon outline with width=stroke), an exclamation stroke (vertical line, same stroke width, roughly 0.35..0.65 of the height — keep it inside the triangle interior clear of the outline) and a round dot drawn as a filled ellipse (never a zero-length line, WR-02), radius ~5, at ~0.8 of the height. Same shape family as draw_source_fault_badge; do NOT modify draw_source_fault_badge itself (its pixels are covered by existing tests). Docstring in the style of draw_empty_battery_icon.
- Add `_build_no_connection_canvas(flat=False)` directly after `_build_battery_empty_canvas()`: calls `_build_hold_canvas(draw_alert_icon, ALERT_ICON_HEIGHT_PX, NO_CONNECTION_HEADING_TEXT, NO_CONNECTION_BODY_LINES, DIMMED_FIELD_IDX, DIMMED_INK, not flat)`. No theme/battery_low/source_fault params. Docstring: DEVICE-06, drawn by the firmware not the server (build_canvas deliberately never dispatches it — the device can only need it when it cannot reach the server); `flat=True` exists solely for gen_fault_screen.py to extract the ink mask; the firmware reproduces the dithered field itself. Check `_build_hold_canvas`'s safe-box assertion still passes with an 88 px wide glyph (it uses the power/moon half-width; that is fine, but confirm no assertion trips).

Create firmware/tools/gen_fault_screen.py (stdlib + Pillow; SPDX Apache-2.0 header like firmware sources; add REPO_ROOT to sys.path and `import server.plane.render as render`, `server.panel_format as pf`):
- `render_mask()` -> (x, y, w, h, bits bytes): render `_build_no_connection_canvas(flat=True)`, mask = pixel index == pf.IDX_WHITE, bbox via Image.getbbox() on the mask; pack each row MSB-first, row stride = (w+7)//8, padding bits 0.
- `render_header_text()` -> str: deterministic header: SPDX lines, a "GENERATED by firmware/tools/gen_fault_screen.py from server/plane/render.py _build_no_connection_canvas(flat=True) - DO NOT EDIT; regenerate with: server/.venv/bin/python3 firmware/tools/gen_fault_screen.py" comment, include guard FP_FAULT_SCREEN_MASK_H, `#include <stdint.h>`, `#define FP_FAULT_MASK_X/Y/W/H/STRIDE` values, and `static const uint8_t fp_fault_mask_bits[FP_FAULT_MASK_STRIDE * FP_FAULT_MASK_H] = { ... };` 16 bytes per line as `0x%02x,`. Trailing newline. No timestamps, no absolute paths (must be reproducible on CI).
- `firmware_equivalent_image()` -> PIL image: Python port of the EXACT C algorithm from Task 2 (integer Floyd–Steinberg, see Task 2 for the spec — implement both from that single spec) plus the mask stamp, as a 1200x1600 black/white image. Keep it vectorless pure-Python; 1.9 M pixels is fine for an offline tool.
- `main()`: writes firmware/main/fault_screen_mask.h, and (with `--preview PATH`, default the quick-task dir's no-connection-preview.png) writes the preview PNG side by side with the server's dithered `_build_no_connection_canvas()` for comparison (reuse pf palette conversion as the 260923-fr4 side-by-side did). Print bbox and header byte size. Expect roughly 765x340 px and ~32 KB of mask data; if the bbox is wildly larger (e.g. > 100 KB), stop and report instead of committing.

Run the generator with server/.venv/bin/python3 (fall back to python3 if the venv is absent and Pillow imports) to create the header and preview. Because the preview port and Task 2's C are written from one spec, write the port after reading Task 2's algorithm paragraph.

Create server/test_fault_screen_mask.py (pytest, same REPO_ROOT import prelude as test_render.py) with the drift test from <behavior>; failure message must say to rerun the generator. Add the render tests from <behavior> to server/test_render.py following the numbered-section comment convention near the battery-empty tests.
  </action>
  <verify>
    <automated>cd /home/user/skypane && server/.venv/bin/python3 -m pytest -q -p no:cacheprovider server/test_fault_screen_mask.py server/test_render.py -k "no_connection or alert or fault_screen or battery_empty or dimmed" --no-cov && server/.venv/bin/python3 -m ruff check server/plane/render.py server/test_render.py server/test_fault_screen_mask.py firmware/tools/gen_fault_screen.py</automated>
  </verify>
  <done>Header committed-ready and reproducible (rerunning the generator leaves `git diff --stat firmware/main/fault_screen_mask.h` empty); drift + render tests pass; ruff clean; preview PNG exists and visually matches the sibling hold screens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure fault_screen module, host test, and wiring into fail_and_sleep()</name>
  <files>firmware/main/fault_screen.h, firmware/main/fault_screen.c, firmware/tests/test_fault_screen.c, firmware/main/app_main.c, firmware/main/nvs_schema.h</files>
  <behavior>
    - render: every nibble of the 960000-byte buffer is 0x0 or 0x1
    - render: every pixel whose mask bit is set is 0x1 (white)
    - render: in a region well outside the mask (e.g. rows 100..299, all columns) the white fraction is within 0.37..0.43, and black is the dominant value over the whole buffer
    - render: two renders into separately 0xAA-filled buffers are byte-identical (deterministic, fully overwrites)
    - render: the tick callback is invoked (count > 0) and NULL tick is accepted
    - should_draw: ("wifi",1,false)=false; ("wifi",2,false)=true; ("wifi",UINT8_MAX,false)=true; ("wifi",2,true)=false; ("blit",2,false)=false; ("reset",2,false)=false; ("deadline",5,false)=false; (NULL,2,false)=false; ("",2,false)=false; unknown token ("bogus",2,false)=false; every included token (wifi http status json auth enrol secret config download verify) at 2 = true; next_backoff_n 0 = false
    - FP_FAULT_SCREEN_HASH == "fault:no-connection" and does not start with "sha256:"
  </behavior>
  <action>
fault_screen.h (SPDX header; includes only stdint.h/stdbool.h/stddef.h): FP_FAULT_SCREEN_WIDTH 1200, FP_FAULT_SCREEN_HEIGHT 1600, FP_FAULT_SCREEN_BYTES (W*H/2), FP_FAULT_SCREEN_HASH "fault:no-connection", FP_FAULT_SCREEN_MIN_BACKOFF_N 2, `typedef void (*fp_fault_screen_tick_fn)(void);`, `void fp_fault_screen_render(uint8_t *buf, fp_fault_screen_tick_fn tick);`, `bool fp_fault_screen_should_draw(const char *step, uint8_t next_backoff_n, bool already_shown);`. Header comment: DEVICE-06, why a mask + on-device dither (a full image would cost 960 KB of the 2.4 MB app partition — rejected by the seed; Pillow's FS field is aperiodic so it cannot be tiled), counter semantics (next_backoff_n is the value AFTER fp_sleep_decide's increment, so 2 = second consecutive failure), and the excluded steps with reasons: blit (the panel itself failed), reset (abnormal reset may be a brownout during a blit — redrawing could loop), deadline (the wake budget is already spent; a draw is ~30 s more).

fault_screen.c (pure C11, no ESP-IDF; `#include "fault_screen_mask.h"`; _Static_assert that mask X+W <= width and Y+H <= height):
- Dither spec (the ONE spec Task 1's Python port also implements): target level L = 102 on a 0..255 scale (the server's round(255*0.4)). Work in 1/16 units: two file-scope `static int16_t` error rows of WIDTH+2 entries (index x+1, so x-1 and x+1 never go out of bounds), zeroed at the start of every render (static, not stack — the app_main task stack is small). Plain left-to-right raster, no serpentine. For each pixel: v16 = L*16 + cur[x+1]; white if v16 >= 128*16, quantised value q16 = white ? 255*16 : 0; e = v16 - q16; cur[x+2] += e*7/16; nxt[x] += e*3/16; nxt[x+1] += e*5/16; nxt[x+2] += e*1/16 (use integer arithmetic with the remainder assigned so the four shares sum exactly to e: compute 7/3/5 shares with `/16` and give the last share `e - (s7+s3+s5)`). After each row swap cur/nxt and zero the new nxt. Confirm int16 range holds (|e| <= 4080, accumulated cell <= ~2*4080) or use int32 if in doubt — document.
- Pixel value: if inside the mask bbox and the MSB-first bit ((row*STRIDE + col/8) bit 7-(col%8)) is set → 0x1, else the dither result (0x1 white / 0x0 black). Still advance the dither for masked pixels (keeps the field identical to the flat unmasked dither everywhere — simplest, and matches the Python port). Pack two pixels per byte, left pixel high nibble.
- Call `tick()` (if non-NULL) every 64 rows.
- should_draw: false if step NULL, already_shown, or next_backoff_n < FP_FAULT_SCREEN_MIN_BACKOFF_N; true only if strcmp matches one of the included list (allow-list, so a future new token defaults to not drawing).

test_fault_screen.c: style of test_sleep_decision.c (SPDX, run comment, assert()); covers every <behavior> bullet. Buffer via malloc (960 KB, not stack). Uses fault_screen_mask.h macros for the mask check.

app_main.c fail_and_sleep(step): keep the existing NVS read/update and the `poll fail step=%s backoff_n=%u sleep_s=%` ESP_LOGW and log_wake_timing() exactly as-is and in order; then, before enter_deep_sleep(plan.sleep_s), call a new static helper `maybe_draw_fault_screen(step, plan.next_backoff_n)`:
  - already_shown = fp_nvs_get_str(FP_NVS_IMAGE_HASH, ...) == ESP_OK && strcmp(value, FP_FAULT_SCREEN_HASH) == 0 (buffer char[80] like state_machine.c).
  - If !fp_fault_screen_should_draw(...) return.
  - fp_api_release() (check api_client.c that it is safe/idempotent when no session exists — it is called unconditionally in state_machine.c paths; if not safe, skip it) and fp_wifi_stop() — radio down before the panel, same rule as state_machine.c.
  - buf = heap_caps_malloc(EPD_BYTES, MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT); on NULL log `ESP_LOGW("fp_diag", "fault screen skipped: no PSRAM")` and return.
  - fp_fault_screen_render(buf, fp_wake_feed); err = fp_panel_draw(buf); heap_caps_free(buf).
  - ESP_OK → fp_nvs_set_str(FP_NVS_IMAGE_HASH, FP_FAULT_SCREEN_HASH), log `ESP_LOGI("fp_diag", "fault screen drawn step=%s backoff_n=%u", ...)`. ESP_ERR_TIMEOUT / ESP_ERR_INVALID_STATE → do NOT write the sentinel (next failing wake retries), log `fault screen deferred err=%s`. Other errors → log `fault screen failed err=%s`, no sentinel, no extra backoff change.
  - NEVER call fp_wake_checkpoint() in this path (it can re-enter fail_and_sleep via on_wake_deadline).
  - Add includes (fault_screen.h, epd13in3e.h for EPD_BYTES, esp_heap_caps.h, nvs_util.h, api_client.h, <string.h>) and `_Static_assert(FP_FAULT_SCREEN_BYTES == EPD_BYTES, "...")`. Add a comment block above the helper explaining why no new NVS key: the sentinel in FP_NVS_IMAGE_HASH both suppresses redraws during the outage and, since it never equals a server `sha256:` hash, guarantees the first healthy poll's hash-skip does not fire so the real picture comes back; and that fp_panel_draw may light-sleep up to CONFIG_FP_MAX_GUARD_WAIT_S for refresh spacing (accepted).
nvs_schema.h: extend the FP_NVS_IMAGE_HASH comment: it may also hold FP_FAULT_SCREEN_HASH ("fault:no-connection") after the DEVICE-06 fault screen was drawn (quick task 260924-u7n).

Then try the real build: `command -v idf.py || command -v docker`; if either works run `firmware/build.sh` (copy secrets.example.h to secrets.h only if secrets.h is absent, and delete it afterwards if you created it). If neither exists, compile-check the pure module standalone with `cc -Wall -Wextra -Werror -std=c11 -c firmware/main/fault_screen.c` and record in the SUMMARY that the full ESP-IDF build was NOT run (CI's firmware.yml `build` job will run it).
  </action>
  <verify>
    <automated>cd /home/user/skypane && sh firmware/tests/run_host_tests.sh && sh firmware/tests/check_log_contract.sh && sh firmware/tests/check_production_config.sh static && cc -Wall -Wextra -Werror -std=c11 -c firmware/main/fault_screen.c -o /dev/null</automated>
  </verify>
  <done>test_fault_screen passes among all host suites; log contract check passes with the `poll fail step=` line untouched; fail_and_sleep draws only via the allow-listed, once-per-outage path; ESP-IDF build either passed or its absence is explicitly recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Docs, requirement and seed closure, full-suite verification</name>
  <files>firmware/VENDOR.md, ARCHITECTURE.md, .planning/REQUIREMENTS.md, .planning/seeds/on-device-fault-icon.md</files>
  <action>
- firmware/VENDOR.md: add fault_screen.c / fault_screen.h / fault_screen_mask.h (generated) / tools/gen_fault_screen.py to the "Original To This Repository" section in its existing style; append a quick task 260924-u7n note to app_main.c's row in the Vendored Files table (fault-screen draw in fail_and_sleep, contract line unchanged and emitted first); add the three new `fp_diag` lines (`fault screen drawn …`, `fault screen deferred err=…`, `fault screen failed err=…`, `fault screen skipped: no PSRAM`) to "Diagnostic lines (outside the contract)". Do not touch the Log Line Contract table itself.
- ARCHITECTURE.md: near the hold-screens section (~L340, BATTERY EMPTY / DISPLAY OFF), add a short paragraph: NO CONNECTION is the fourth dimmed hold screen, the only one drawn by the device; trigger (2nd consecutive comm/data failure), once-per-outage sentinel, recovery via hash mismatch, render.py as the artwork source with the generated mask + drift test. If a device failure-path section exists, add a one-line pointer there too.
- .planning/REQUIREMENTS.md: mark DEVICE-06 `[x]` in place in its v2 "On-Device Fault Fallback" section (grep for the checkbox form used by other done items) and append "— done in quick task 260924-u7n (2026-09-24): dithered-field NO CONNECTION hold screen + CFG-05 alert glyph, drawn by firmware; see .planning/quick/260924-u7n-…/". Update the traceability/coverage line near L2126 only if it states DEVICE-06 is unbuilt.
- .planning/seeds/on-device-fault-icon.md: frontmatter `status: fulfilled`, add `fulfilled_date: 2026-09-24` (keep resolved_date). Insert a new "## Fully fulfilled 2026-09-24" section ABOVE "## Partially fulfilled 2026-09-02": what shipped (files, trigger next_backoff_n >= 2 after increment, allow-listed steps and the three exclusions with reasons, mask + on-device integer FS field instead of a 960 KB image, sentinel/recovery mechanism, drift test), and the resolved open questions: glyph — YES, the same alert triangle as CFG-05 at hold-glyph scale (draw_alert_icon); backoff reset — fp_sleep_decide resets the counter to 0 on the first healthy wake and the healthy poll overwrites the sentinel with the real hash, so the next outage shows the screen again after two failures. Leave everything below unchanged as history.

Then run the full verification: `./scripts/run-all-tests.sh` (whole suite, coverage gate enforced), `firmware/tests/run_host_tests.sh`, `firmware/tests/check_log_contract.sh`, `server/.venv/bin/python3 -m ruff check .`, rerun `firmware/tools/gen_fault_screen.py` and confirm `git status --porcelain firmware/main/fault_screen_mask.h` shows no change vs. the staged/committed version. Commit on the current branch claude/festive-lovelace-575ocm (no branch switch).
  </action>
  <verify>
    <automated>cd /home/user/skypane && ./scripts/run-all-tests.sh && sh firmware/tests/run_host_tests.sh && server/.venv/bin/python3 -m ruff check . && grep -q "status: fulfilled" .planning/seeds/on-device-fault-icon.md && grep -n "DEVICE-06" .planning/REQUIREMENTS.md | grep -q "\[x\]"</automated>
  </verify>
  <done>Full pytest suite (incl. coverage gate) green, host tests green, ruff clean; VENDOR.md, ARCHITECTURE.md, REQUIREMENTS.md and the seed all describe the shipped DEVICE-06; work committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device ↔ server | Not crossed: the fault screen is drawn with zero network input. |
| build tooling → firmware | Generated header compiled into firmware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u7n-01 | Denial of Service (battery) | fail_and_sleep redraw loop | mitigate | Once-per-outage sentinel in FP_NVS_IMAGE_HASH; blit/reset/deadline excluded so a brownout-during-blit cannot loop; deferral does not write sentinel but next attempt is a backoff interval away |
| T-u7n-02 | Denial of Service | wake budget / watchdog | mitigate | render ticks fp_wake_feed every 64 rows; no fp_wake_checkpoint in the path (no re-entrant fail_and_sleep) |
| T-u7n-03 | Tampering | fault_screen_mask.h drifting from render.py | mitigate | byte-for-byte pytest drift test; header marked GENERATED |
| T-u7n-04 | Spoofing | sentinel colliding with a server hash | accept | server hashes are validated as `sha256:`+64 hex (validate.c); sentinel cannot match |
| T-u7n-05 | Denial of Service | PSRAM exhaustion | mitigate | malloc NULL → skip with fp_diag log, sleep proceeds normally |
</threat_model>

<verification>
- firmware/tests/run_host_tests.sh passes including test_fault_screen
- firmware/tests/check_log_contract.sh and check_production_config.sh static pass
- ./scripts/run-all-tests.sh passes (drift test + new render tests included)
- ruff clean on changed Python
- ESP-IDF build run if toolchain available, otherwise explicitly recorded as not run
</verification>

<success_criteria>
- DEVICE-06 behaviour implemented exactly as in must_haves; seed marked fulfilled with resolved questions; DEVICE-06 checked in REQUIREMENTS.md
- Preview PNG in the quick-task directory shows the firmware-equivalent screen next to the server-dithered one
</success_criteria>

<output>
Create `.planning/quick/260924-u7n-device-06-local-no-connection-fault-scre/260924-u7n-SUMMARY.md` when done
</output>
