---
phase: quick-260902-j8w
plan: 260902-j8w
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/pages/health_page.py
  - companion/test_status_pages.py
  - companion/static/style.css
  - companion/layout.py
  - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
autonomous: true
requirements: [QUICK-260902-j8w]

must_haves:
  truths:
    - "BLOCKER, discovered during planning and not present in the task brief: this worktree has an UNRESOLVED `git merge` in progress. `git rev-parse --git-dir`/MERGE_HEAD points at `2de17d3` (`Merge remote-tracking branch 'origin/main' into claude/ui-config-panel-improvement-3c2b80`), and three paths are unmerged (`UU`): `.planning/STATE.md`, `companion/pages/config_page.py`, `companion/test_config_page.py`. `companion/pages/config_page.py` line 17 contains a literal conflict marker, so it is not valid Python — and `companion/test_status_pages.py` imports it, so the Health harness this task must keep green currently dies with `SyntaxError` before running a single check. Task 1 gates on this. The executor must NOT resolve someone else's merge."
    - "None of this task's own target files are in conflict. `companion/pages/health_page.py`, `companion/static/style.css` and `companion/test_status_pages.py` all merged cleanly. The blocker is purely that the harness cannot import, and that `.planning/STATE.md` cannot be appended to."
    - "This is a developer instruction, not a bug report to diagnose. The developer's words were `supprime le logo de la batterie, car c'est inconsistant avec le reste` — remove the battery logo, it is inconsistent with the rest. There is no open question about WHETHER to remove it. The open questions are the second-order ones Tasks 2 and 3 answer: what happens to the now-unused constant, to the two harness assertions that pin the glyph's presence, and to the CSS and prose that describe it."
    - "The inconsistency is real and was verified by rendering the page, not assumed. `health_page.render()` emits exactly five `<h2>` elements — `Screen`, `Battery trend`, `Server & data`, `Unresolved prefixes`, `Resolution statistics` — and exactly one of them contains an `<svg>`. The other four are bare `<h2 class=\"text-heading\">Text</h2>`. Re-render and confirm this five-headings/one-glyph split yourself before editing; it is the whole premise, and it is also the shape of the new assertion Task 2 adds."
    - "The glyph is emitted from `_battery_trend_section_html()` (around line 1291), interpolated as `layout.icon_html(ICON_BATTERY)` directly into the `<h2>` format string ahead of the escaped heading text, with NO separator character between them. The three sibling constants (`ICON_DEVICE`, `ICON_PIPELINE`, `ICON_CORROBORATION`, around lines 265-267) are passed as the `icon=` keyword to `layout.stat_tile()` (lines 1816, 1828, 1831) and render inside a tile's own `.stat-tile__caption` flex row. That difference in call shape IS the difference the developer is looking at. The three tile icons are explicitly out of scope."
    - "The validated sketch agrees with the developer, and this is corroborating evidence worth recording. `.claude/skills/sketch-findings-skypane/sources/001-health-page-direction.html` defines an `#icon-battery` glyph in its own `<defs>` but references it from nowhere — every `<use href=\"#icon-...\">` in all three variants sits inside a `.stat-tile__head`, and the sketch's own battery section renders a bare `<h2>Battery trend</h2>` beside a status dot, with no glyph. The heading placement was invented by plan 06.6.1-04, not carried over from the validated direction. Verify this by grep before writing it down."
    - "`ICON_BATTERY` has exactly two live references outside its own definition: `companion/pages/health_page.py:1291` (the emission this task removes) and `companion/test_status_pages.py` lines 3524 and 3543 (inside one check, `_health_page_four_icons_correctly_placed_and_tinted`). A repo-wide grep finds nothing else outside `.planning/` history. Re-run that grep yourself; act on what it returns, not on this sentence."
    - "Both harness references live in ONE check, so retargeting them in place nets to no `EXPECTED_CHECK_COUNT` movement. The real on-disk value is to be read at execution time and left alone. That check currently asserts the four Health-signal constants are distinct and whitelisted, that the empty render emits exactly five `<use>` instances, that exactly three carry `STAT_TILE_ICON_CLASS`, and that the battery glyph falls between the battery section's opening tag and its `</h2>`. The last of those becomes the exact opposite of what must now be true."
    - "The retarget must not merely delete the two battery assertions — that would leave a silent coverage hole where a real assertion used to be. It must assert the NEW state positively, and the strongest available form of that is the developer's own complaint rendered testable: no `<h2>` on the Health page carries an `<svg>` at all. That invariant is currently false in exactly one place and will be true everywhere after this change."
    - "The constant's removal must be pinned by a runtime attribute check (`hasattr(health_page, 'ICON_BATTERY')`), never by a source-text grep. A SUPERSEDED comment recording the removal necessarily names the constant, so a negative grep over the source would fail on the very comment explaining the change."
    - "`layout.ICON_IDS` and `ICON_DEFS_HTML` are DELIBERATELY NOT pruned, and that decision must be written down rather than left as an apparent oversight. Removing `icon-battery` from the whitelist would force a matching `<symbol>` deletion plus four assertion edits in `companion/test_companion_app.py` (which pins `len(ICON_IDS) == 14`, no duplicates, sprite-symbol-ids/ICON_IDS set equality, and `ICON_DEFS_HTML.count('<symbol') == 14`) — a cross-page change to a shared component, which is exactly the scope expansion the brief forbids. `layout.py` already carries the precedent for documenting rather than pruning a whitelist member (see `icon-nav-preview`'s own comment). The sprite is `display: none` and costs about 200 bytes; the whitelist is an injection guard, not a usage index."
    - "`companion/static/style.css` needs NO structural cleanup, and this was checked in full rather than assumed. The heading rule `.page-section--nested > h2, .battery-trend-section > h2` carries exactly one declaration after quick task 260902-iag — `margin-bottom: var(--space-md)` — and no `gap`, no `margin-left`, no `display: flex`, so nothing there compensates for the glyph. The `<h2>` is normal inline flow; the glyph is an inline `<svg class=\"icon\">` sized 20x20 by the global `.icon` rule with `vertical-align: middle`, and removing an inline child from inline flow leaves no hole to close. (`display: flex` + `gap: var(--space-xs)` DOES exist — on `.stat-tile__caption`, which is the tile icons' row and is out of scope.) Confirm each of these by reading the real rules before concluding no cleanup is owed; report the conclusion either way."
    - "`.battery-trend-section svg:not(.icon)` MUST stay byte-identical. Its `:not(.icon)` guard exists precisely to keep the heading glyph out of the sparkline sizing rule, so it becomes vestigial — but `companion/test_status_pages.py` pins that exact selector string in three separate assertions (around lines 1367, 1369 and 1398, including a check that no `@media` block ever redeclares its height), and the selector's own comment records its (0,2,1) specificity as load-bearing against a bare `.sparkline__canvas` rule. Its comment's factual claim that the heading `also carries an icon-battery <svg class=\"icon\"> now` becomes false and must be corrected; the selector itself must not be touched."
    - "Three other CSS comments mention `.battery-trend-section svg:not(.icon)` by name (around lines 1090, 2304 and 2912) and stay TRUE — they document the `.icon` sizing-collision class and the selector's ownership of the canvas height, neither of which depends on a glyph existing in the heading. Do not rewrite them. Only the claim that the heading carries a glyph is false."
    - "Two prose sites make now-false factual claims and must be corrected in the file's own SUPERSEDED voice, not deleted: `_battery_trend_section_html()`'s docstring (its 06.6.1-04 / D-02 paragraph asserting the glyph sits inside this `<h2>`, carries no tint class deliberately, and that `All four Health signals still carry their icon — three on tiles, one here on the section heading`), and the tail of `.page-section--nested > h2`'s own comment, where quick task 260902-iag wrote that at 20px `the icon and the text are the same size again`."
    - "`companion/layout.py::icon_html()`'s docstring names `a section heading` as a live example of where its icons sit beside visible text. That becomes the only place in the app where no such example remains true for Health. This is a comment-only correction in a shared module — permitted, but gated: `git diff companion/layout.py` must contain no executable line."
    - "The design-system skill's record must be checked by grep rather than assumed. `references/visual-direction-typography.md` line 13 states `Icons on stat tiles: still current`, and line 16 describes the Battery trend section as having `a serif <h2> heading` without claiming a glyph — so neither line is false today, but the file has no entry stating that headings carry no glyphs, which is what this task makes a rule. `SKILL.md` documents the accent-reservation contract including `a neutral stat tile's icon tint` and needs amendment only if the grep finds it claims something now untrue."
    - "This task does not touch the three tile icons, `stat_tile()`, `layout.ICON_IDS`, `ICON_DEFS_HTML`, the `— Latest N readings` caption span, the battery readout, the sparkline, the heading's 20px `.text-heading` treatment, its retained 16px bottom margin, or any page other than Health."
    - "`companion/test_status_pages.py` passes at its unchanged real on-disk `EXPECTED_CHECK_COUNT`, with both battery assertions retargeted in place and none deleted."
    - "`scripts/run-all-tests.sh`'s FAILED list after this task is byte-identical to the baseline Task 1 captured on the same tree. The brief's expectation is that the list is exactly `server/test_poll_loop.py` (the known, pre-existing, unrelated panel.bin digest mismatch), but the in-progress merge is pulling `origin/main` changes into several `server/test_*.py` files, so the baseline must be MEASURED after the merge is resolved and compared against, never assumed from this sentence."
  artifacts:
    - path: "companion/pages/health_page.py"
      provides: "The battery-trend `<h2>` reduced to heading text plus its existing caption span, the `ICON_BATTERY` constant gone, and `_battery_trend_section_html()`'s docstring corrected so no paragraph still asserts a glyph that is not emitted"
      contains: "260902-j8w"
    - path: "companion/test_status_pages.py"
      provides: "The single icon check retargeted in place onto three tile-only Health icons, plus the positive invariant that no Health `<h2>` carries an `<svg>`, asserted on both the empty and the seeded render, with `EXPECTED_CHECK_COUNT` unmoved"
      contains: "260902-j8w"
    - path: "companion/static/style.css"
      provides: "The `.battery-trend-section svg:not(.icon)` comment corrected — the selector itself byte-identical — and 260902-iag's now-stale icon-size clause superseded in the nested-heading rule's comment"
      contains: "260902-j8w"
    - path: ".claude/skills/sketch-findings-skypane/references/visual-direction-typography.md"
      provides: "A recorded rule that glyphs are a tile/control treatment and never a heading one, with the sketch's own evidence and the 06.6.1-04 round trip under the file's SUPERSEDED convention"
      contains: "260902-j8w"
  key_links:
    - from: "the unresolved merge in this worktree"
      to: "`companion/test_status_pages.py`'s import of `companion.pages.config_page` — a file carrying a literal conflict marker at line 17. Every verify gate in this plan runs that harness, so the whole plan is unrunnable until the merge is resolved by whoever owns it. Task 1 exists solely to make that a loud STOP instead of a confusing cascade of SyntaxErrors."
    - from: "the deleted `layout.icon_html(ICON_BATTERY)` interpolation"
      to: "`_battery_trend_section_html()`'s `%` format tuple — the format string drops one `%s`, so the tuple must drop `layout.icon_html(ICON_BATTERY)` in the same edit or the interpolation raises `TypeError: not enough arguments` at render time, on every Health request"
    - from: "the removed `ICON_BATTERY` constant"
      to: "`_health_page_four_icons_correctly_placed_and_tinted()` lines 3524 and 3543, which reference `health_page.ICON_BATTERY` as a module attribute. Removing the constant without editing that check turns a deliberate removal into an `AttributeError` inside the harness, not a clean assertion failure"
    - from: "the SUPERSEDED comment recording the constant's removal"
      to: "any gate that greps `health_page.py` for the constant's absence — such a gate would fail on the comment explaining the change. This is why the constant's removal is pinned by `hasattr()` at runtime and the glyph's removal by the rendered markup, never by source text"
    - from: "`:not(.icon)` in `.battery-trend-section svg:not(.icon)`"
      to: "three assertions in `companion/test_status_pages.py` (around lines 1367/1369/1398) that key on that selector as a literal string, one of which walks every `@media` block. Tidying the now-vestigial guard away breaks all three at once, for a reason unrelated to the change being made"
    - from: "`icon-battery` staying in `layout.ICON_IDS`"
      to: "`companion/test_companion_app.py`'s four sprite assertions (`len(ICON_IDS) == 14`, no duplicates, symbol-id/ICON_IDS set equality, `count('<symbol') == 14`). Pruning the whitelist member is a four-assertion cross-page change; leaving it is the scoped choice, and it only stays defensible if it is written down as a decision rather than left looking like an oversight"
---

<!-- planner-discipline-allow: ICON_BATTERY -->
<!-- planner-discipline-allow: icon-battery -->
<!-- planner-discipline-allow: icon_html -->

<objective>
Remove the battery glyph from Health's `Battery trend` heading, at the developer's direct instruction — `supprime le logo de la batterie, car c'est inconsistant avec le reste`. Health renders five `<h2>` elements and exactly one of them carries a glyph; after this task, none does.

| # | What | Why |
|---|------|-----|
| 1 | Gate on the unresolved merge in this worktree before touching anything | `companion/pages/config_page.py` carries a literal conflict marker, so `companion/test_status_pages.py` cannot import and every verify gate in this plan is currently unrunnable. This is not this task's merge to resolve |
| 2 | Delete the `icon_html()` interpolation from the battery `<h2>`, delete the now-unused `ICON_BATTERY` constant, and retarget the one harness check that pins the glyph's presence | The developer's own instruction. The retarget turns two presence assertions into the positive invariant the complaint actually names: no Health heading carries a glyph |
| 3 | Correct every record the removal falsifies, confirm the CSS needs no cleanup, and hand forward what was deliberately left alone | A stylesheet comment, a docstring, another docstring in `layout.py` and 260902-iag's own icon-size clause each assert a glyph that will no longer exist. `layout.ICON_IDS` keeps its member on purpose, and that has to read as a decision |

Purpose: close a one-line developer instruction cleanly, without leaving a coverage hole where two real assertions used to be, and without letting a five-character deletion sprawl into the shared icon component.

Output: one interpolation and one constant removed, one harness check retargeted in place with no count movement, four prose corrections, one recorded non-decision about the sprite whitelist, and a SUMMARY naming the live-browser items and the two things deliberately not fixed.

**Evidence the developer is right, gathered during planning and worth putting in the SUMMARY.** The validated Merged Health Sketch (`sources/001-health-page-direction.html`) defines an `#icon-battery` glyph in its `<defs>` and then references it from nowhere: every `<use>` in all three variants sits inside a `.stat-tile__head`, and the sketch's own battery section is a bare `<h2>Battery trend</h2>`. The heading placement came from plan 06.6.1-04's own reading of D-02, not from the validated direction. This task does not merely satisfy a preference; it returns the page to what the sketch always showed.

**Non-goals — verified during planning, deliberately NOT touched.**
- **The three tile glyphs (`ICON_DEVICE` / `ICON_PIPELINE` / `ICON_CORROBORATION`) and their `stat_tile(icon=...)` call sites.** Confirmed consistent with each other and with the sketch, and explicitly excluded by the brief.
- **`layout.ICON_IDS` and `ICON_DEFS_HTML`.** `icon-battery` stays a whitelist member and a sprite symbol. See the must-have above for the full reasoning; the point is recorded in Task 3, not silently skipped.
- **The `.battery-trend-section svg:not(.icon)` selector.** Byte-identical. Three harness assertions key on it as a literal.
- **The `— Latest N readings` caption span, the battery readout, the sparkline, the section's status modifier.** Untouched. Note in passing: that span's text opens with an em dash and abuts the heading text with no separating space (`Battery trend— Latest 20 readings`). That is pre-existing, independent of the glyph, and is a hand-forward observation for the SUMMARY — NOT a fix to make here.
- **The heading's own 20px `.text-heading` treatment and its retained `margin-bottom: var(--space-md)`.** Quick task 260902-iag settled both one commit ago; nothing here reopens them.
- **`.planning/phases/06.6.1-.../06.6.1-VERIFICATION.md`'s criterion 26** (`Battery trend heading carries the battery icon — VERIFIED`). A completed phase's historical verification record. It stays as written; the SUMMARY notes it now describes a superseded state.
- **Every page other than Health.** No shared component's behaviour changes; the only shared-module edit is a docstring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.planning/quick/260902-iag-revert-the-health-nested-card-heading-de/260902-iag-SUMMARY.md

@companion/pages/health_page.py
@companion/layout.py
@companion/static/style.css
@companion/test_status_pages.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate on the unresolved merge and capture the real green baseline</name>
  <files>(no file edits — this task either unblocks the plan or stops it)</files>
  <read_first>
    - `git status --short`, `git diff --name-only --diff-filter=U`, and whether `$(git rev-parse --git-dir)/MERGE_HEAD` exists. At planning time all three said the same thing: a merge of `origin/main` into `claude/ui-config-panel-improvement-3c2b80` is in progress with `.planning/STATE.md`, `companion/pages/config_page.py` and `companion/test_config_page.py` unmerged. Read the CURRENT state; it may have been resolved between planning and execution, which is the good outcome.
    - `companion/pages/config_page.py` around line 17, only far enough to confirm whether a conflict marker is still there. Do not resolve it.
  </read_first>
  <action>
**A. Check for an in-progress merge.** If `MERGE_HEAD` exists, or `git diff --name-only --diff-filter=U` returns any path, or any tracked source file contains a line beginning with a conflict marker: **STOP THIS PLAN IMMEDIATELY.** Make no edits. Do not resolve the conflicts, do not `git checkout --ours`/`--theirs`, do not `git merge --abort`, do not stash. This merge belongs to whoever started it and touches Phase 8 panel-theme work and a Settings-page branch, neither of which this quick task knows anything about.

Report back, in the SUMMARY and in your return message: that the plan is BLOCKED, which paths are unmerged, that `companion/pages/config_page.py` is not importable so `companion/test_status_pages.py` cannot run, and that the merge must be resolved (or the work moved to a clean tree) before Task 2 can begin. Then stop.

**B. If and only if the tree is clean, capture the real baseline.** Do this BEFORE any edit, so the comparison at the end is against a measured fact rather than an assumption:

Run `server/.venv/bin/python3 companion/test_status_pages.py` and record the printed passed/total and the on-disk `EXPECTED_CHECK_COUNT`. All three numbers must agree. If they do not, the tree was already red before this task touched it — say so and stop rather than editing on top of a broken baseline.

Run `scripts/run-all-tests.sh`, capture the whole run, and write the FAILED-harness list to `/tmp/skypane-j8w-baseline-failures.txt` (one path per line, sorted, empty file if none failed). The brief expects that list to be exactly `server/test_poll_loop.py`. It probably is — but this merge is pulling `origin/main` changes into `server/test_poll_loop.py`, `server/test_render.py`, `server/test_enrich.py`, `server/test_pipeline_e2e.py` and `server/test_config_history.py`, so the post-merge failure set is a thing to MEASURE, not to assume. If the measured list differs from the brief's expectation, record the difference in the SUMMARY as a pre-existing condition this task inherited and did not cause; do not try to fix it.

**C. Re-verify the premise while the tree is green.** Render Health and confirm the five-headings/one-glyph split with your own eyes: `health_page.render()` on an empty state dir returns exactly five `<h2>` elements and exactly one contains an `<svg>`. Also re-run the two greps this plan rests on — every reference to the constant across the repo, and every `<use href=` in the sketch source at `.claude/skills/sketch-findings-skypane/sources/001-health-page-direction.html`. Record both results. If either disagrees with this plan's must-haves, trust what you found and say so before proceeding.
  </action>
  <verify>
    <automated>test ! -f "$(git rev-parse --git-dir)/MERGE_HEAD" && test -z "$(git diff --name-only --diff-filter=U)" && server/.venv/bin/python3 -c "
import re, subprocess
src = open('companion/test_status_pages.py').read()
expected = int(re.search(r'^EXPECTED_CHECK_COUNT = (\d+)', src, re.M).group(1))
out = subprocess.run(['server/.venv/bin/python3', 'companion/test_status_pages.py'], capture_output=True, text=True).stdout
passed, total = map(int, re.findall(r'(\d+)/(\d+)', out)[-1])
assert passed == total == expected, 'baseline is not green: printed %d/%d against EXPECTED_CHECK_COUNT %d' % (passed, total, expected)
print('baseline green at %d checks' % expected)
" && server/.venv/bin/python3 -c "
import sys, re, tempfile, shutil; sys.path.insert(0, '.')
from companion.pages import health_page as h
d = tempfile.mkdtemp()
r = h.render({'state_dir': d}); shutil.rmtree(d, ignore_errors=True)
heads = re.findall(r'<h2\b.*?</h2>', r, re.S)
assert len(heads) == 5, 'expected five Health headings before the change, got %d' % len(heads)
glyphed = [x for x in heads if '<svg' in x]
assert len(glyphed) == 1, 'the premise is that exactly one heading carries a glyph today, found %d' % len(glyphed)
assert h.BATTERY_SECTION_HEADING in glyphed[0], 'the one glyphed heading must be the battery-trend one'
print('premise confirmed: 5 headings, 1 glyph')
" && test -s /tmp/skypane-j8w-baseline-failures.txt -o -f /tmp/skypane-j8w-baseline-failures.txt</automated>
  </verify>
  <done>
Either the plan is STOPPED with a written BLOCKED report naming the unmerged paths and the harness import failure they cause — no file edited, no merge resolved — or the tree is confirmed clean, `companion/test_status_pages.py` is green at its own real `EXPECTED_CHECK_COUNT`, the measured `scripts/run-all-tests.sh` FAILED list is on disk at `/tmp/skypane-j8w-baseline-failures.txt`, and the five-headings/one-glyph premise plus both supporting greps have been re-verified against the current tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Take the glyph out of the heading and retarget the check that pinned it</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - `health_page.py::_battery_trend_section_html()` in full — the whole docstring and the whole return expression (around lines 1234-1292). This function has been edited by several quick tasks today (260901-tsa, 260902-gjj twice, 260902-ep7), so read the real current shape rather than working from any description. Note precisely: the format string's placeholder count, the `%` tuple's argument order, and that the glyph and the escaped heading text are interpolated back to back with no separator character between them.
    - The four icon constants and their shared comment (around lines 258-268). That comment explains that these are `ICON_IDS` members and that the harness asserts membership so a typo fails loudly. After this task there are three, and the comment's own count language must still be true.
    - The three `icon=` call sites (around lines 1816, 1828, 1831) and `layout.py::stat_tile()`'s `icon` parameter handling. Confirm for yourself that these render inside `.stat-tile__caption` and are untouched by anything here.
    - `layout.py::icon_html()` in full — its whitelist behaviour and its `%` format. Confirm that an unrecognised id returns the empty string, which is why nothing here can fail silently-but-visibly.
    - `companion/test_status_pages.py::_health_page_four_icons_correctly_placed_and_tinted()` in full, including its `check(...)` description string (around lines 3513-3553). Identify each assertion individually and decide, one at a time, whether it survives, inverts, or needs a new number: the four-constant distinctness tuple, the `ICON_IDS` membership loop, the `<use` count of five, the `STAT_TILE_ICON_CLASS` count of three, and the two battery-position assertions.
    - The `check(name, fn)` helper's return-tuple contract and the `_mkstate` / `_ctx` / `_iso` / `_now` / `_seed_device_health` / `_seed_runway_events` fixtures, so the retargeted check can assert against a seeded render as well as an empty one. Note that a seeded Health render also emits the `icon-search` glyph in the unresolved-prefixes filter bar, so any `<use` count must state which state it is counting.
    - `EXPECTED_CHECK_COUNT` and its provenance comment block (around line 224). Read the real value; this task must not move it, because the whole retarget is in place.
  </read_first>
  <action>
**A. `health_page.py` — remove the interpolation.** In `_battery_trend_section_html()`, drop `layout.icon_html(ICON_BATTERY)` from the `<h2>` format string and drop the matching argument from the `%` tuple in the same edit. The two must move together: the format string loses one placeholder, so a tuple left at its old length raises at render time on every Health request. Everything else in the heading stays exactly as it is — the `text-heading` class, the escaped heading text, and the trailing `<span class="text-label section-caption">— Latest %d readings</span>`. The heading becomes structurally identical to `Unresolved prefixes` and `Resolution statistics`, which is the point.

**B. `health_page.py` — remove the now-unused constant.** Delete `ICON_BATTERY`. Your Task 1 grep is the authority on whether anything else still references it; if it turned up a site this plan does not name, handle that site before deleting. Then reread the shared comment above the remaining three constants: it currently speaks of four constants and of the harness asserting all four are whitelist members. Correct its count and its claim so it describes three, and record — in that comment, in the file's SUPERSEDED voice — that a fourth existed, what it did, and why it went. Reference this quick task id so the removal is greppable from the site of the removal.

**C. `health_page.py` — correct the docstring, do not delete it.** `_battery_trend_section_html()`'s docstring carries a 06.6.1-04 (D-02) paragraph asserting the glyph sits inside this `<h2>` before the heading text, that it deliberately carries no tint class because there is no status modifier for a tint rule to hang off, and that "All four Health signals still carry their icon — three on tiles, one here on the section heading". Every clause of that is about to be false. Supersede it: state what the heading emits today (text plus its caption span, structurally the same as this page's other nested card headings), then keep the old reasoning readable as history with the developer's instruction as the reason it changed, and record the sketch evidence your Task 1 grep produced — the validated sketch's own battery section never had a glyph, so this is a return to the validated direction rather than a departure from it. Note also that the 06.6.1-UI-SPEC wording this paragraph was already reconciling is now further out of date, and that `06.6.1-VERIFICATION.md`'s criterion 26 is a historical record left as written. The 260902-gjj paragraph about the status modifier on the section, and the paragraph about `battery_html` passing through unescaped, are both unaffected — leave them alone. Fix any other comment in the module that your `grep -n "icon"` over `health_page.py` shows making a now-false claim; do not stop at the ones this plan names.

**D. `test_status_pages.py` — retarget the one check IN PLACE. Delete nothing.**

`_health_page_four_icons_correctly_placed_and_tinted()` keeps its position in the file and its slot in the count. Rework it assertion by assertion:

The constant tuple drops to the three tile constants, still asserted distinct and still asserted to be `layout.ICON_IDS` members — that loop is the typo guard and stays.

The `<use` count on the empty render moves from five to four (three tile glyphs plus the auto-refresh pill), with a message that names which four so a future reader does not have to re-derive the arithmetic. If you also assert a count on a seeded render, state separately that it is five and name `icon-search` as the fifth, rather than leaving an unexplained different number.

The `STAT_TILE_ICON_CLASS` count of three is unchanged in value, but its MEANING changes and the message should say so: it used to mean "three of the four Health glyphs are tile glyphs", it now means "every Health-signal glyph on this page is a tile glyph, with none left over".

The two battery-position assertions invert into the positive invariant the developer's complaint actually names, and this is the substance of the retarget. Assert, on both an empty render and a seeded one: every `<h2>` the page emits contains no `<svg>` at all. Find the headings by regex over the rendered body rather than by positional index, assert the expected number of them is still five (so a heading silently disappearing fails here too), and include the offending heading's own text in the failure message. Also assert the retired glyph id is referenced nowhere in the rendered body — note that `render()` returns the page body only, so the sprite `<symbol>` that `page_shell()` emits is correctly out of scope for this assertion and the whitelist member's survival cannot mask a regression here.

Add one more assertion pinning the constant's removal: `health_page` must no longer define `ICON_BATTERY`. Do this with a runtime attribute check, never a source-text grep — the SUPERSEDED comment Step B writes necessarily names the constant, and a grep-based gate would fail on the comment explaining the change.

Rename the function so its name is true (it is no longer about four icons), rewrite its `check(...)` description string to describe the tile-only state and the no-glyph-in-any-heading invariant, and reference this quick task id in the check's own comment so the retarget's provenance sits beside the assertions it changed. Write the failure messages to say what a failure MEANS: a glyph reappearing inside a heading is the inconsistency quick task 260902-j8w removed at the developer's explicit instruction, not merely a count drifting.

`EXPECTED_CHECK_COUNT` does not move. Leave the number and its provenance block alone; the retarget is in place and adds no check.

**E. Falsifiability pass.** Mutate the retargeted check so the heading-glyph half asserts against something that is not true of the current source, run the harness, and confirm exactly that one check reports FAIL and nothing else does. Do the same for the `hasattr` assertion. Then restore both. An assertion that cannot be observed failing is not an assertion.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys, re, tempfile, shutil; sys.path.insert(0, '.')
from companion.pages import health_page as h
from companion import layout
if hasattr(h, 'ICON_BATTERY'):
    raise SystemExit('FAIL: the unused constant must be gone from the module namespace')
d = tempfile.mkdtemp()
r = h.render({'state_dir': d}); shutil.rmtree(d, ignore_errors=True)
heads = re.findall(r'<h2\b.*?</h2>', r, re.S)
assert len(heads) == 5, 'expected Health to still render five headings, got %d' % len(heads)
for x in heads:
    assert '<svg' not in x, 'no Health heading may carry a glyph any more: %r' % x[:140]
assert '#icon-battery' not in r, 'the retired glyph must not be referenced anywhere in the page body'
assert r.count('<use') == 4, 'expected four glyph instances in the empty render (three tiles + the refresh pill), got %d' % r.count('<use')
assert r.count(layout.STAT_TILE_ICON_CLASS) == 3, 'the three tile glyphs must survive untouched, got %d' % r.count(layout.STAT_TILE_ICON_CLASS)
assert '>' + h.BATTERY_SECTION_HEADING in r, 'the heading text itself must survive the glyph removal'
assert 'section-caption' in r[r.index('>' + h.BATTERY_SECTION_HEADING):r.index('</h2>', r.index('>' + h.BATTERY_SECTION_HEADING))], 'the trailing readings caption must survive'
for const in ('ICON_DEVICE', 'ICON_PIPELINE', 'ICON_CORROBORATION'):
    assert getattr(h, const) in layout.ICON_IDS, '%s must stay a whitelist member' % const
src = open('companion/pages/health_page.py').read()
assert '260902-j8w' in src, 'the removal must be recorded at the site of the removal'
print('markup ok: 5 headings, 0 glyphs in headings, 3 tile glyphs intact')
" && server/.venv/bin/python3 -c "
import re, subprocess
src = open('companion/test_status_pages.py').read()
expected = int(re.search(r'^EXPECTED_CHECK_COUNT = (\d+)', src, re.M).group(1))
assert '260902-j8w' in src, 'the retargeted check must record this quick task'
# No source-text grep for the retired constant here, deliberately: the retarget's own
# comment legitimately names it while explaining the change. A check still READING it off
# the module would raise AttributeError, so the harness run below is the real gate.
out = subprocess.run(['server/.venv/bin/python3', 'companion/test_status_pages.py'], capture_output=True, text=True).stdout
passed, total = map(int, re.findall(r'(\d+)/(\d+)', out)[-1])
assert passed == total == expected, 'printed %d/%d against EXPECTED_CHECK_COUNT %d — the retarget must be in place, adding no check' % (passed, total, expected)
print('harness ok at %d checks, unmoved' % expected)
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_contrast_check.py</automated>
  </verify>
  <done>
Health renders five `<h2>` elements and none of them contains an `<svg>`; the battery-trend heading is now text plus its existing readings caption, structurally identical to `Unresolved prefixes` and `Resolution statistics`. The `ICON_BATTERY` constant is gone from the module namespace, its removal recorded in the constants comment in the file's SUPERSEDED voice with the sketch evidence and the developer's instruction, and `_battery_trend_section_html()`'s docstring no longer asserts a glyph that is not emitted. The three tile glyphs and their `stat_tile()` call sites are untouched. The one harness check that pinned the glyph is retargeted in place — renamed, redescribed, and now asserting the positive no-glyph-in-any-heading invariant on both an empty and a seeded render, plus the constant's absence by runtime attribute check — with `EXPECTED_CHECK_COUNT` unmoved, no check deleted, and both new assertions observed failing under mutation before being restored.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the records, settle the sprite question in writing, and confirm the suite against Task 1's measured baseline</name>
  <files>companion/static/style.css, companion/layout.py, .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md, .claude/skills/sketch-findings-skypane/SKILL.md</files>
  <read_first>
    - `style.css`'s `.battery-trend-section svg:not(.icon)` rule and its ENTIRE comment (around lines 2353-2432). Read the whole thing before typing: the first paragraph makes the factual claim about to become false, the specificity note about `(0,2,1)` beating a bare `.sparkline__canvas` rule is live and load-bearing, and the long HISTORY block below it is explicitly marked as evidence rather than constraint. Know which paragraph you are correcting.
    - `companion/test_status_pages.py` around lines 1360-1405 — the three assertions that key on that selector as a literal string, including the one that walks every `@media` block asserting the height is declared nowhere else. These are why the selector must stay byte-identical, and they must stay green with no edit.
    - `style.css`'s `.page-section--nested > h2, .battery-trend-section > h2` rule and its comment (around lines 2623-2699). Confirm for yourself that the rule carries exactly one declaration and that nothing in it compensates for a glyph — no `gap`, no `margin-left`, no `display: flex`. Then find the clause at the tail of its comment, written by quick task 260902-iag, stating that at 20px the glyph and the text are the same size again. That clause is about to describe nothing.
    - `style.css`'s global `.icon` rule (around line 2217) and `.stat-tile__caption` (around line 2194). The first is what sized the glyph; the second is the one place in this region that does declare `display: flex` and a `gap`, and it belongs to the tile glyphs, which are out of scope. Seeing both is how you conclude, from source, that removing an inline child from the heading's normal inline flow leaves no spacing hole to close.
    - The three other comments naming `.battery-trend-section svg:not(.icon)` (around lines 1090, 2304 and 2912). Read enough of each to satisfy yourself they document the `.icon` sizing-collision class and the canvas-height ownership, neither of which depends on a glyph in the heading — so all three stay true and must NOT be rewritten.
    - `companion/layout.py::icon_html()`'s docstring, specifically its `aria-hidden` paragraph naming `a tile caption, a section heading` as the places its glyphs sit beside visible text.
    - `companion/layout.py`'s `ICON_IDS` block and `icon-nav-preview`'s own comment (around lines 137-144), which is this file's existing precedent for documenting a whitelist member's status instead of pruning it. Then `companion/test_companion_app.py` around lines 789-807 — the four assertions that would all have to move if `icon-battery` were pruned. Read both before writing the decision down; the decision is only defensible if you have seen its cost.
    - `.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md` in full, and grep the whole skill directory for `icon`. Establish by grep — not assumption — whether anything in the skill claims Health's heading carries a glyph. At planning time nothing did: line 13 documents glyphs as a stat-tile treatment, line 16 describes the Battery trend section without mentioning one. Act on what your grep returns.
    - `.claude/skills/sketch-findings-skypane/sources/001-health-page-direction.html`'s `<defs>` block and every `<use href=` in it, so the sketch evidence you record is something you read rather than inherited from this plan.
  </read_first>
  <action>
**A. `style.css` — correct the one false comment, leave the selector alone.** In `.battery-trend-section svg:not(.icon)`'s comment, the opening paragraph states that the section's heading also carries a glyph now and that `:not(.icon)` excludes it explicitly rather than relying on the sparkline being the only match by luck. Rewrite that paragraph to say what is true after this task: the heading glyph it was written to exclude has been removed, so the guard now matches nothing and is retained purely as belt-and-braces against any future `.icon` element appearing inside this section. Then state plainly why the guard is retained rather than tidied away — three assertions in `test_status_pages.py` key on this selector as a literal string, one of them scanning every `@media` block, and the specificity arithmetic in the next paragraph is stated against this exact selector form. Reference this quick task id. Do not touch a single character of the selector itself, and do not touch the specificity paragraph or the HISTORY block below it.

**B. `style.css` — supersede 260902-iag's now-stale clause.** At the tail of the nested-heading rule's comment, the clause explaining that the note about the glyph rendering larger than its text was removed because at 20px the two are the same size again now refers to a glyph that does not exist. Correct it in place, in the file's SUPERSEDED voice: keep the record that such a note once existed and why 260902-iag retired it, and add that the glyph itself was removed one task later at the developer's own instruction, so the question the note asked can no longer arise. Reference this quick task id. Change no declaration in that rule — it still carries exactly one, and a verify gate asserts it.

**C. `style.css` — record the CSS-cleanup verdict.** The brief asks explicitly whether any spacing or sizing rule was compensating for the glyph. Answer it from source and write the answer down: which rules you checked, what each declares, and the conclusion. If your reading agrees with this plan's — that the heading rule carries only a bottom margin, that the heading is normal inline flow with no flex container or gap, and that the only `display: flex` + `gap` in this region belongs to `.stat-tile__caption` and the tile glyphs — then no cleanup is owed and the rules degrade gracefully with one fewer inline child. Put that verdict in the SUMMARY. If your reading DISAGREES, fix what you actually found rather than what this plan predicted, and say so.

**D. `companion/layout.py` — one docstring correction, and one decision written down.** In `icon_html()`'s docstring, the `aria-hidden` paragraph names a section heading as a live example of a glyph sitting beside its own visible text label. Correct that example; after this task, glyphs in this app sit beside tile captions, nav labels, filter-bar and pill text — not headings. Keep the paragraph's actual argument (the glyph is decorative, do not add a `<title>`) intact.

Then, in the `ICON_IDS` block, record the deliberate non-removal beside the whitelist itself. State that `icon-battery` no longer has any consumer as of this quick task, that it is retained anyway, and give both halves of the reason: pruning it would force a matching `<symbol>` deletion plus four assertion edits in `test_companion_app.py` (the fourteen-member count, the duplicate check, the symbol-id/`ICON_IDS` set equality and the symbol count), which is a cross-page change to a shared component well outside this task's scope; and the whitelist is an injection guard on `icon_html()`'s fragment reference, not an index of what is currently used — `icon-nav-preview`'s own comment a few lines above already establishes exactly that reading. Note that the sprite is `display: none` and the retained symbol costs roughly 200 bytes, and hand the pruning forward in the SUMMARY as a real, optional follow-up the developer can take or decline. Reference this quick task id.

**This edit is comment- and docstring-only.** A verify gate asserts `git diff companion/layout.py` contains no changed executable line. If you find yourself wanting to change `ICON_IDS` itself, stop — that is the scope expansion this step exists to refuse in writing.

**E. Design-system record.** Act on what your grep found, and record the finding either way — if nothing in the skill ever claimed Health's heading carried a glyph, say so in the SUMMARY rather than implying a correction was made that was not needed.

In `references/visual-direction-typography.md`, the `Icons on stat tiles` entry currently documents the treatment without stating its boundary. Extend it into a rule: glyphs are a tile and control affordance in this app — stat tiles, nav links, the copy button, the filter bar, the refresh pill — and headings carry none. Record the round trip under the file's own SUPERSEDED convention: plan 06.6.1-04 (D-02) placed a glyph inside Health's `Battery trend` heading, it was the only glyph inside any `<h2>` anywhere on that page, and quick task 260902-j8w removed it at the developer's explicit instruction as inconsistent with the rest. Add the sketch evidence you read in Step read_first: the sketch defines an `#icon-battery` glyph and references it from nowhere, its own battery section being a bare heading — so the removal returns the app to the validated direction rather than departing from it. Also amend the `Battery trend section` entry, which describes the section's heading, so it states the heading carries no glyph.

In `SKILL.md`, amend only what your grep shows is now untrue, in the file's existing voice and at its existing altitude — it summarises and points at the reference files, so the detail belongs in the reference. If the accent-reservation contract's `a neutral stat tile's icon tint` clause is still accurate (it should be — tile tinting is untouched), leave it. Reference this quick task id wherever you do edit.

**F. Full suite, compared against the measured baseline.** Run `scripts/run-all-tests.sh` and diff its FAILED list against `/tmp/skypane-j8w-baseline-failures.txt` from Task 1. The two must be identical: no harness that passed before this task may fail after it, and no pre-existing failure may be reported as though this task caused it. Do not assert a hard-coded expectation of "exactly `server/test_poll_loop.py`" — assert equality with what was actually measured on this tree, and note in the SUMMARY whether the measured set matched the brief's expectation. If any harness regressed, stop and fix it; do not record a green result over a new failure.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
s = open('companion/static/style.css').read()
sel = '.battery-trend-section svg:not(.icon) {'
assert sel in s, 'the pinned selector must stay byte-identical — three harness assertions key on it as a literal'
at = s.index(sel)
comment = s[s.rindex('/*', 0, at):at]
assert '260902-j8w' in comment, 'the vestigial-guard correction must be recorded above the selector'
body = s[s.index('{', at):s.index('}', s.index('{', at))]
assert 'height: 160px' in body, 'the canvas height must stay declared here and only here'
nat = s.index('.page-section--nested > h2,')
nbody = s[s.index('{', nat):s.index('}', s.index('{', nat))]
assert nbody.count(':') == 1 and 'margin-bottom: var(--space-md)' in nbody, 'the nested-heading rule must still carry exactly its one retained declaration'
for prop in ('gap', 'display', 'margin-left'):
    assert prop not in nbody, 'the heading rule must declare no %s — nothing there compensated for a glyph' % prop
ncomment = s[s.rindex('/*', 0, nat):nat]
assert '260902-j8w' in ncomment, 'the stale icon-size clause must be superseded in place, not left describing nothing'
assert '260902-iag' in ncomment and '260901-uzi' in ncomment, 'the prior reversal record must stay readable, not be overwritten'
print('css ok: selector byte-identical, heading rule untouched, both comments corrected')
" && server/.venv/bin/python3 -c "
import subprocess
d = subprocess.run(['git', 'diff', '-U0', '--', 'companion/layout.py'], capture_output=True, text=True).stdout
bad = []
for line in d.splitlines():
    if line[:1] not in ('+', '-') or line[:3] in ('+++', '---'):
        continue
    t = line[1:].strip()
    if not t or t.startswith('#') or t.startswith('*') or t.startswith('\"\"\"') or t.endswith('\"\"\"'):
        continue
    bad.append(line)
assert not bad, 'layout.py must be comment/docstring-only in this task; executable lines changed:\n' + '\n'.join(bad[:10])
src = open('companion/layout.py').read()
assert '260902-j8w' in src, 'the deliberate non-removal of the whitelist member must be recorded beside the whitelist'
assert 'icon-battery' in src, 'the whitelist member is retained on purpose — see the recorded decision'
print('layout.py ok: comment-only, decision recorded')
" && server/.venv/bin/python3 -c "
ref = open('.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md').read()
assert '260902-j8w' in ref, 'the typography reference is where this rule belongs'
assert 'SUPERSEDED' in ref, 'the round trip must use the file own supersede convention'
assert '06.6.1-04' in ref or '06.6.1' in ref, 'the record must name the plan that placed the glyph'
print('skill record ok')
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_contrast_check.py && scripts/run-all-tests.sh > /tmp/skypane-j8w-run-all-tests.log 2>&1; sed -n '/FAILED harnesses/,$p' /tmp/skypane-j8w-run-all-tests.log | sed -n 's/^    - //p' | sort > /tmp/skypane-j8w-after-failures.txt; diff -u /tmp/skypane-j8w-baseline-failures.txt /tmp/skypane-j8w-after-failures.txt && echo "suite matches the measured baseline"</automated>
    <human-check>
REQUIRED, not optional — but do NOT block plan completion on it. This project's memory records that computed-style and structural checks alone have already missed a real rendered bug (`feedback_real_device_ui_verification`), and the last ten Health quick tasks each had no browser-automation tooling bound to their executor and handed this pass back to the orchestrating session, which performed it successfully every time. Assume the same division of labour: complete every automated half yourself, then hand the pixel-level items back. The scope here is small, so this list is deliberately short.

Name these in the SUMMARY as outstanding for the orchestrating session's browser pass:

1. Does the `Battery trend` heading read cleanly without the glyph, and does it now look like a sibling of `Unresolved prefixes` and `Resolution statistics` rather than a special case? This is the whole request.
2. Spacing: with one fewer inline child, does the heading's left edge align with the other card headings and with the card's own 16px padding, and is the gap below it still the same 16px the 260902-bl2 pass confirmed live?
3. Nothing else moved: the three tile glyphs are still present and still status-tinted, the sparkline still fills its card, and the `— Latest N readings` caption still sits where it did.
4. Both themes, and a narrow viewport.
5. Pre-existing and NOT fixed here, worth a look while you are on the page: the readings caption's em dash abuts the heading text with no separating space (`Battery trend— Latest 20 readings`). Confirm whether that reads as a defect at 20px; if it does, it is a separate quick task, not this one.

Record what was actually observed rather than restating this list as if performed.
    </human-check>
  </verify>
  <done>
The `.battery-trend-section svg:not(.icon)` selector is byte-identical and its comment now says the glyph it excluded is gone and why the vestigial guard is retained anyway; the three other comments naming that selector are untouched because they stayed true. 260902-iag's icon-size clause is superseded in place with its prior record intact, and the nested-heading rule still carries exactly one declaration. The CSS-cleanup question the brief asked is answered from source with the rules named, and the verdict is in the SUMMARY. `layout.py` carries a corrected `icon_html()` docstring example and a written decision explaining why `icon-battery` stays in the whitelist and what pruning it would cost, with `git diff` proving no executable line moved. The design-system reference records glyphs as a tile/control-only treatment with the 06.6.1-04 round trip under its SUPERSEDED convention and the sketch's own evidence. `scripts/run-all-tests.sh`'s FAILED list is identical to Task 1's measured baseline, and the SUMMARY says whether that baseline matched the brief's expectation of a lone `server/test_poll_loop.py` failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `GET /health` | Server-rendered HTML for an authenticated operator. This task removes one decorative, `aria-hidden` inline `<svg>` from one `<h2>`. No new markup, no new attribute, no dynamic content, no change to any escaping path. |
| browser -> `GET /static/style.css` | A pre-auth static asset. Comment corrections only; not one declaration or selector changes. |
| repo -> `companion/layout.py` `ICON_IDS` | The whitelist is `icon_html()`'s guard against an attacker-influenceable fragment reference reaching a `<use href>`. This task deliberately does not shrink it, so that guard is provably unchanged. |
| repo -> `.claude/skills/sketch-findings-skypane/` | A Claude-discoverable design-system reference auto-loaded during UI implementation. Stale content misleads future automated work, which is why the record correction is in scope. |
| developer -> this worktree's git index | An unresolved `origin/main` merge is in progress, owned by another workstream. Task 1's gate is what keeps this task from writing into, resolving, or committing over it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j8w-01 | Tampering | An executor resolving or aborting the in-progress `origin/main` merge to make its own gates run | critical | mitigate | Task 1 is a dedicated STOP gate placed before any edit. It names the forbidden recoveries explicitly (`--ours`/`--theirs`/`merge --abort`/stash) and requires a written BLOCKED report instead. Its automated half fails hard if `MERGE_HEAD` exists or any path is `UU`, so the plan cannot proceed past it silently. |
| T-j8w-02 | Denial of Service | A `%`-interpolation arity mismatch taking the whole Health page down | high | mitigate | Dropping the format placeholder and the tuple argument are one edit, called out as inseparable in Task 2 Step A and in a key_link. Task 2's gate renders the page, so an arity error surfaces as an immediate crash in the gate rather than on a live request. |
| T-j8w-03 | Tampering | A source-text negative grep failing on the comment that explains the change | medium | mitigate | The constant's removal is pinned by `hasattr()` at runtime and the glyph's removal by the rendered body, never by grepping the module source — so the SUPERSEDED comment naming the removed constant cannot invalidate its own gate. Stated as a must-have and as a key_link. |
| T-j8w-04 | Tampering | Tidying away the now-vestigial `:not(.icon)` guard | high | mitigate | Three assertions in `test_status_pages.py` key on that selector as a literal string, one scanning every `@media` block, and its `(0,2,1)` specificity is stated against this exact form. Task 3 Step A forbids touching the selector, its gate asserts the literal is byte-identical and the height still declared in its body, and the harness itself fails loudly if it is not. |
| T-j8w-05 | Repudiation | A deliberate non-removal reading as an oversight | medium | mitigate | `icon-battery` staying in `ICON_IDS` with zero consumers is exactly the kind of thing a later contributor "cleans up" without knowing the four-assertion cost. Task 3 Step D requires the decision, its cost and its `icon-nav-preview` precedent to be written beside the whitelist and handed forward as an optional follow-up; the gate asserts both the record and the member survive. |
| T-j8w-06 | Tampering | Scope creep from a five-character deletion into the shared icon component | high | mitigate | `layout.py` is declared comment/docstring-only and gated by a `git diff` parse that fails on any changed executable line. The three tile constants' membership is asserted positively. `stat_tile()`, `ICON_DEFS_HTML` and every non-Health page are outside every file list in this plan. |
| T-j8w-07 | Tampering | A retargeted assertion silently asserting nothing | medium | mitigate | The two battery assertions invert into a positive invariant (no `<h2>` carries an `<svg>`, asserted on both empty and seeded renders with the heading count pinned) rather than being deleted, and Task 2 Step E requires both new assertions to be observed failing under mutation before restoration. |
| T-j8w-08 | Repudiation | Reporting a pre-existing suite failure as caused by this task, or vice versa | medium | mitigate | The merge is pulling `origin/main` changes into five `server/test_*.py` files, so the brief's "only `server/test_poll_loop.py`" expectation may not survive it. Task 1 measures the real FAILED set before any edit; Task 3's gate diffs against that measured file rather than a hard-coded name, and the SUMMARY states whether the measurement matched the brief. |
| T-j8w-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install of any kind. This task edits one Python module's markup and comments, one harness, one stylesheet's comments, one shared module's docstrings and two Markdown records — all stdlib-only, no dependency change. |
</threat_model>

<verification>
1. `git rev-parse --git-dir`/MERGE_HEAD absent and `git diff --name-only --diff-filter=U` empty at the start of Task 2 — the plan never edits a tree mid-merge.
2. `server/.venv/bin/python3 companion/test_status_pages.py` — all checks pass; the printed total equals the real on-disk `EXPECTED_CHECK_COUNT`, which is UNCHANGED from Task 1's baseline reading.
3. `companion/test_config_page.py`, `companion/test_view_pages.py`, `companion/test_companion_app.py` and `companion/test_contrast_check.py` all pass at their own unchanged counts — `test_companion_app.py` in particular proves the fourteen-member sprite contract survived untouched.
4. `scripts/run-all-tests.sh`'s FAILED list is byte-identical to `/tmp/skypane-j8w-baseline-failures.txt` measured in Task 1, and the coverage gate reports no new shortfall.
5. `git diff --stat` touches only `companion/pages/health_page.py`, `companion/test_status_pages.py`, `companion/static/style.css`, `companion/layout.py` and the design-system Markdown files. No other page module, no JavaScript, no `config_page.py`, no `.planning/` file outside this quick task's own directory.
6. `git diff companion/pages/health_page.py` shows exactly one removed interpolation, one removed argument, one removed constant, and comment/docstring changes. No control-flow change, no other emitted string altered.
7. `git diff companion/static/style.css` shows comment changes only — zero declaration changes, zero selector changes, and `.battery-trend-section svg:not(.icon)` byte-identical.
8. `git diff companion/layout.py` contains no changed executable line, asserted mechanically by Task 3's gate.
9. `health_page.render()` emits five `<h2>` elements, none containing an `<svg>`, on both an empty and a seeded state dir — the developer's complaint, rendered as a test.
10. The SUMMARY records: the CSS-cleanup verdict with the rules named, the sketch-evidence grep result, the design-system grep result, the deliberate `ICON_IDS` non-removal with its cost and as an optional follow-up, whether the measured baseline matched the brief's expectation, the two hand-forwards (`06.6.1-VERIFICATION.md` criterion 26, and the em-dash spacing in the readings caption), and the pixel-level items outstanding.
</verification>

<success_criteria>
- No `<h2>` on the Health page carries a glyph. The `Battery trend` heading is text plus its existing readings caption, structurally identical to `Unresolved prefixes` and `Resolution statistics` — which is precisely what the developer asked for.
- The three tile glyphs, their `stat_tile()` call sites, their status tinting, and `layout.stat_tile()` itself are provably untouched.
- The `ICON_BATTERY` constant is gone from the module namespace, and its removal is recorded at the site of the removal rather than concealed.
- No check was deleted. The one check that pinned the glyph is retargeted in place into a positive invariant, `EXPECTED_CHECK_COUNT` never moved, and both new assertions were observed failing under mutation.
- `.battery-trend-section svg:not(.icon)` is byte-identical, and the three harness assertions keying on it stayed green with no edit.
- The CSS-cleanup question the brief asked is answered from source, with the rules named and the verdict written down — not assumed either way.
- `icon-battery`'s survival in `layout.ICON_IDS` reads as a decision with a stated cost and a stated precedent, and is handed forward as an optional follow-up rather than left as an apparent oversight.
- `scripts/run-all-tests.sh`'s FAILED list is identical to the baseline measured on this tree before any edit.
- The in-progress `origin/main` merge was not resolved, aborted, stashed, or committed over by this task.
</success_criteria>

<commits>
Focused and atomic, matching this session's established style (`git log --oneline -10`), referencing the quick task id rather than a phase-plan number. The change is small, so this is two code/doc commits plus the workflow's own record commits:
- `refactor(quick-260902-j8w): drop the battery glyph from Health's section heading` — body naming the developer's own instruction, that this was the only glyph inside any `<h2>` on the page, that the validated sketch never had one, and that the harness check was retargeted in place onto the no-glyph-in-any-heading invariant with no count movement
- `docs(quick-260902-j8w): correct the records the heading glyph's removal falsifies` — the stylesheet comments, `icon_html()`'s docstring example, the deliberate `ICON_IDS` non-removal with its cost, and the design-system typography reference

If Task 1 STOPS the plan, commit nothing. A BLOCKED report is not a code change, and this worktree's index belongs to an in-progress merge that is not ours to touch.
</commits>

<output>
Create `.planning/quick/260902-j8w-remove-the-battery-icon-from-the-health-/260902-j8w-SUMMARY.md` when done.

Beyond the standard sections it must contain, under findable headings: the merge-gate outcome from Task 1 (BLOCKED with the unmerged paths, or clean with the measured baseline numbers); the CSS-cleanup verdict naming every rule checked and what each declares; the sketch-evidence grep result (`#icon-battery` defined in the sketch's `<defs>` and referenced from nowhere); the design-system grep result and whether any correction was actually needed; the `layout.ICON_IDS` non-removal recorded as a decision with its four-assertion cost and offered as an optional follow-up; whether the measured `run-all-tests.sh` FAILED set matched the brief's expectation of a lone `server/test_poll_loop.py`; a "Handed Forward" section covering `06.6.1-VERIFICATION.md` criterion 26 (now describing a superseded state, left as written) and the readings caption's missing space before its em dash; and a "Pixel-Level Items Outstanding" section reproducing the live-browser list for the orchestrating session's pass.
</output>
