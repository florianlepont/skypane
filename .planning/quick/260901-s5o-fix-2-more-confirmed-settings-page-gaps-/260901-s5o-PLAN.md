---
phase: quick-260901-s5o
plan: 260901-s5o
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/test_config_page.py
autonomous: true
requirements: [QUICK-260901-s5o]

must_haves:
  truths:
    - "The Poll section renders exactly one `<p class=\"text-label section-caption\">` paragraph, directly under its `<h2 class=\"text-heading\">Poll</h2>` heading and BEFORE the `<form method=\"post\" action=\"/poll-now\">` trigger — the same one-caption-under-the-heading shape Theme, Runway and Diagnostic LED already carry from quick task 260901-re6. It is the fourth and last section on this page to get one; the page now has zero heading-without-caption sections."
    - "The caption text is the validated copy VERBATIM, with no rewording and no re-punctuation: \"Manually trigger an immediate poll cycle instead of waiting for the next scheduled one.\" It lives in exactly one module-level constant, `POLL_SECTION_CAPTION`, named into the same `*_SECTION_CAPTION` family as the three that already exist."
    - "The caption renders on BOTH of `poll_trigger_section()`'s branches — the zero-cooldown enabled branch AND the non-zero-cooldown disabled branch — computed once into a local `caption_html` and interpolated into both return expressions, the exact two-branch idiom `theme_fieldset()` already uses. A caption that renders only at zero cooldown would silently vanish for the whole cooldown window."
    - "The caption reuses the existing `.section-caption` markup pattern and CSS rule verbatim. No new class, no new helper function, no new CSS rule, and no new custom property is introduced for it — `companion/static/style.css` is not modified for fix 1 at all."
    - "`poll_trigger_section()`'s disabled branch still emits its own separate `<p class=\"text-body\" id=\"poll-cooldown-text\">` remaining-seconds paragraph AFTER the button, unchanged. That branch legitimately has two paragraphs; only the `.section-caption` count is one."
    - "Nothing else about `poll_trigger_section()` changes: both branches' `<form method=\"post\" action=\"/poll-now\">`, the `POLL_TRIGGER_BUTTON_ID`/`POLL_COOLDOWN_TEXT_ID` id attributes, the native `disabled` attribute, `POLL_COOLDOWN_HELPER_TEXT`'s `{n}` formatting, `_poll_cooldown_script()` and `_poll_submit_script()` are all byte-identical to before."
    - "At the >=960px breakpoint `.dirty-bar` is a floating, inset, rounded-on-all-four-corners pop-up card: inset by `var(--space-md)` from the content column's own left, right and bottom edges, `border-radius: var(--radius-card)` on every corner, a full `border: 1px solid var(--color-border)` on all four sides, and a shadow that surrounds the whole card (`var(--shadow-card-hover)` plus one larger ambient layer) rather than an upward-only toolbar shadow."
    - "The inset is genuinely visible at EVERY viewport width >=960px, including widths where the 1440px content cap binds. `max-width` is reduced by twice the inset (`calc(min(1440px, 100%) - var(--space-md) * 2)`) so the bar stays 16px narrower than `.dashboard-main` once both are clamped — without that, a >=1712px viewport (e.g. a 16-inch MacBook Pro's 1728px default) would render the bar exactly flush with the content column's edges, which is the very treatment this change exists to retire."
    - "The symmetric left/right inset preserves the bar's horizontal centring exactly: adding `var(--space-md)` to `left` and subtracting the same from `right` leaves the centre of the positioning region unchanged, so the bar stays centred on `.dashboard-main`'s own centre line at every width."
    - "The underlying positioning MECHANISM is unchanged: still `position: fixed` at >=960px (never sticky, never absolute), still deriving its `left` offset from `.dashboard-shell`'s `240px` sidebar track plus its `var(--space-xl)` column gap, still `margin: 0 auto` to zero the base rule's `margin-top` and centre under the cap, still no `z-index`. Only the inset/rounding/shadow/border values change."
    - "The retired toolbar treatment is gone with no residue: the base `.dirty-bar` rule body carries no `border-top:` declaration and no upward-only y-negated `box-shadow` literal, and the >=960px rule body no longer squares the corners off — the base rule's `border-radius: var(--radius-card)` now governs at every width, one radius for one component."
    - "The shadow's first layer is the real `var(--shadow-card-hover)` theme token, which has its own `prefers-color-scheme: dark` and `[data-ui-theme]` variants — a strict improvement over the theme-neutral y-negated black-alpha literal it replaces, which was near-invisible in dark mode and leaned entirely on the top hairline for separation."
    - "Everything established by quick task 260901-re6 outside the four changed visual values still holds: `position: fixed` at >=960px, the save button's `form=\"{SETTINGS_FORM_ID}\"` wiring, the DOM placement of the bar after `</form>` and after the Poll `</section>`, `.dirty-bar[hidden] { display: none }` still winning over the base `display: flex`, and normal non-fixed in-flow behavior below 960px."
    - "`companion/static/dirty-state.js` is NOT modified. `companion/pages/config_page.py`'s `render()`, `dirty_bar_html`, `handle_post()`, `SETTINGS_FORM_ID` and every dirty-bar-related attribute are NOT modified — fix 2 is pure CSS plus the harness retargets that pin it."
    - "`companion/test_config_page.py` passes after EACH of the two commits, with `EXPECTED_CHECK_COUNT` moved from its real on-disk baseline (read at execution time, never taken from this plan's text) to that baseline plus exactly 1."
    - "`scripts/run-all-tests.sh` reports no failing harness other than the known, pre-existing, unrelated `server/test_poll_loop.py` digest mismatch, and no coverage-threshold failure."
  artifacts:
    - path: "companion/pages/config_page.py"
      provides: "`POLL_SECTION_CAPTION` added to the `*_SECTION_CAPTION` family with the shared comment updated to describe four captions; `poll_trigger_section()` computing `caption_html` once and emitting it first on both branches"
      contains: "POLL_SECTION_CAPTION"
    - path: "companion/static/style.css"
      provides: "The base `.dirty-bar` rule restyled as a fully-bordered floating card (full border, token-based surrounding shadow); the >=960px `.dirty-bar` rule inset by `var(--space-md)` on left/right/bottom with a correspondingly reduced `max-width` and no corner squaring"
      contains: "calc(min(1440px, 100%) - var(--space-md) * 2)"
    - path: "companion/test_config_page.py"
      provides: "One new check (Poll caption on both branches, positioned under the Poll heading), two in-place retargets of the cross-file CSS guard onto the floating-card treatment and the new left offset, one in-place retarget of the caption-verbatim check to cover all four captions, and `EXPECTED_CHECK_COUNT` at on-disk-baseline + 1"
      contains: "POLL_SECTION_CAPTION"
  key_links:
    - from: "`poll_trigger_section()`'s new `<p class=\"text-label section-caption\">` markup"
      to: "style.css's existing `.text-label` (supplies `var(--font-label-size)`) plus `.section-caption` (supplies only the 70% muted colour) — the class pair is inert markup without both rules. Both already exist and are already guarded by the pre-existing cross-file CSS check; this fix adds a fourth consumer and must not add a fourth rule."
    - from: "`render()`'s `<h2 class=\"text-heading\">Poll</h2>` heading, which lives in `render()` and NOT in `poll_trigger_section()`"
      to: "`poll_trigger_section()`'s return value, which `render()` interpolates immediately after that heading — this is the only reason emitting the caption first inside `poll_trigger_section()` lands it directly under the heading. A future refactor that moves the heading into the section function, or that inserts anything between them in `render()`, breaks the position silently; Task 1's new harness check asserts the rendered order, not just the presence."
    - from: "style.css's >=960px `.dirty-bar { left: calc(240px + var(--space-xl) + var(--space-md)) }`"
      to: "`.dashboard-shell`'s `grid-template-columns: 240px minmax(0, 1fr)` and its `column-gap: var(--space-xl)` — still the same duplicated-not-imported must-equal pair 260901-re6 documented, now with the inset addend on top. The existing harness guard matches the OLD three-term literal exactly and WILL fail until retargeted onto the new four-term one; that failure is the guard working, not a regression."
    - from: "style.css's >=960px `.dirty-bar { max-width: calc(min(1440px, 100%) - var(--space-md) * 2) }`"
      to: "`.dashboard-main`'s own `max-width: min(1440px, 100%)` — the bar must stay exactly twice the inset narrower than the content column once the 1440px cap binds on both. If either cap changes, both sites change together; there is no shared token, so Task 2's guard pins the pair."
    - from: "the base `.dirty-bar` rule's `box-shadow: var(--shadow-card-hover), ...`"
      to: "`:root`'s `--shadow-card-hover` and its three theme variants (`prefers-color-scheme: dark`, `[data-ui-theme=\"light\"]`, `[data-ui-theme=\"dark\"]`) — using the token instead of a literal is what gives the bar a correct dark-mode shadow for the first time, and is why the base rule's head comment must stop claiming no suitable token exists."
---

<objective>
Fix 2 more confirmed Settings-page (`/settings`) gaps, found continuing phase 06.6.4.1's closing checkpoint. Both were validated by the developer against the "Settings Save Bar Sketch" artifact and approved verbatim.

| # | Gap | Root cause | Fix |
|---|-----|------------|-----|
| 1 | The Poll section has NO description/caption text at all, while Theme, Runway and Diagnostic LED each render exactly one muted `.section-caption` sentence under their heading | Quick task 260901-re6 merged the description/helper paragraph pairs for the three `.theme-status` groups but never touched the Poll section, which is a `.page-section` and had no paragraph of its own to merge — so it was silently skipped, leaving one of the page's four sections with a bare heading | Add a `POLL_SECTION_CAPTION` constant and render it through the existing `.section-caption` pattern, first, on BOTH of `poll_trigger_section()`'s branches |
| 2 | At >=960px the unified save bar reads as a flush edge-to-edge docked toolbar: pinned to the content column's exact edges with no side margin, square corners, a `border-top` hairline only, and an upward-only shadow | 260901-re6 correctly fixed the bar's *behaviour* (sticky → fixed, moved out of the form) and treated it as a toolbar while doing so. The validated sketch wants a floating pop-up, not a dock | Inset the fixed box by `var(--space-md)` on left/right/bottom, restore `--radius-card` on all four corners, swap the `border-top` for a full border, and swap the upward-only literal shadow for `var(--shadow-card-hover)` plus one larger ambient layer |

Purpose: close the last two deltas between the shipped Settings page and its validated sketch, so 06.6.4.1's closing checkpoint can sign off on the page as built rather than as intended. This is the third quick task in that same closing-checkpoint context this session, after 260901-qif (3 visual gaps) and 260901-re6 (2 issues).

Output: one new constant and one two-branch markup edit in `config_page.py`, two rule edits in `style.css`, one new harness check, three in-place harness retargets, and two focused commits.

**Approach note — reuse, never invent.** Every value used here already exists in the repository and is taken from the site that established it: the caption markup and class pair from `theme_fieldset()`/`runway_fieldset()`/`led_group()`'s own `<p class="text-label section-caption">`; the two-branch `caption_html`-computed-once idiom from `theme_fieldset()`; the inset amount from `--space-md`; the radius from `--radius-card` (already declared by the base `.dirty-bar` rule); the shadow's first layer from the real `--shadow-card-hover` token; the border from `--color-border`. The single genuinely new literal is the ambient shadow layer `0 8px 24px rgba(18, 21, 27, 0.10)`, taken from the sketch and consistent with this file's existing raw-rgba shadow precedent. Do not invent a new token, a new class, a new muted strength, a new radius, or a new helper function.

**Non-goals.**
- Do NOT modify `companion/static/style.css` for fix 1. `.section-caption` and `.text-label` already exist and already do everything the Poll caption needs; adding a Poll-specific rule would be a second definition site for one concept.
- Do NOT modify `companion/static/dirty-state.js`, `render()`, `dirty_bar_html`, `SETTINGS_FORM_ID`, the save button's `form=` attribute, the bar's DOM position after `</form>` and after the Poll `</section>`, `.dirty-bar[hidden]`, `.dirty-bar__cancel`, or the `button:hover` rule that follows them. Fix 2 is a pure visual-treatment change to two CSS rule bodies plus their head comments.
- Do NOT change `.dirty-bar`'s `padding` at either breakpoint. The >=960px `padding: var(--space-md) var(--space-3xl)` was chosen by 260901-re6 to align the bar's controls with `.dashboard-main`'s own content gutter, and the developer's approved change list is inset/radius/shadow/border only. The sketch's own `var(--space-md) var(--space-lg)` is a scoped-demo value inside a 720px shell, not a page-scale one — do not port it.
- Do NOT change `.dirty-bar`'s positioning mechanism. It stays `position: fixed` at >=960px, never `absolute` (the sketch uses `absolute` only because it is a scoped demo inside a `.bar-demo` box) and never sticky again.
- Do NOT add a `z-index` to `.dirty-bar`. 260901-re6's stated reason still holds: it is the last positioned element in document order and nothing it overlaps creates a competing stacking context.
- Do NOT extend the existing `_each_group_emits_exactly_one_caption_between_heading_and_control()` check to cover Poll. It asserts `rendered.count("<p") == 1` (false for Poll's disabled branch, which legitimately also emits the cooldown paragraph) and indexes `"</h2>"` inside the group's own output (the Poll heading lives in `render()`, not in `poll_trigger_section()`, so that index call would raise `ValueError`). Poll gets its own new check instead.
- Do NOT touch `handle_post()`, `poll_trigger_section()`'s cooldown branching logic, `_poll_cooldown_script()`, `_poll_submit_script()`, or `_js_literal()`.
- Do NOT reintroduce a `.config-form` column rule in the >=960px block (06.6.4.1 D-01 removed it outright and its comment says so).

**Note on the sketch.** The validated artifact is `settings-savebar-poll-sketch.html` in this session's scratchpad, not an on-disk artifact under `.planning/sketches/` (only 001-health, 002-mobile-nav, 003-history-density are). Its validated values are therefore stated inline in the tasks below and are the authority for this plan. The developer approved it verbatim ("oui c'est top"). Do not reword the caption copy and do not "improve" the treatment during execution.

**Note on file freshness.** `companion/pages/config_page.py` and `companion/static/style.css` were both heavily modified by the two immediately-prior quick-fix cycles this session (260901-qif and 260901-re6). Read them fresh; do not act on remembered content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

@companion/pages/config_page.py
@companion/static/style.css
@companion/test_config_page.py
</context>

<!--
Comment-text discipline: four literals below are negative-greped by this
plan's acceptance criteria AND must be named in the task actions, because
in every case the instruction is "this declaration is retired, remove it".
All four gates are region-scoped (awk/regex) to a single CSS rule BODY,
and Task 2 explicitly forbids placing any comment inside those rule
bodies, so none of the gates is self-invalidating. Allowlisted
deliberately:
-->
<!-- planner-discipline-allow: border-top: -->
<!-- planner-discipline-allow: box-shadow: 0 - -->
<!-- planner-discipline-allow: border-radius: 0 -->
<!-- planner-discipline-allow: position: sticky -->

<tasks>

<task type="auto">
  <name>Task 1: Give the Poll section the caption the other three sections already have</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    - `companion/pages/config_page.py`'s `*_SECTION_CAPTION` constant block and the shared comment above it (the quick-task-260901-re6 comment explaining the description/helper merge). That comment currently describes exactly three captions and asserts all five superseded constants are retired — it needs a fourth entry that does not pretend Poll was part of that merge.
    - `theme_fieldset()`'s first four lines — the `caption_html = ('<p class="text-label section-caption">%s</p>' % escape_html(THEME_SECTION_CAPTION))` computed-once-reused-by-both-branches idiom. This is the pattern to reuse; `poll_trigger_section()` is the only other two-branch renderer in the module.
    - `runway_fieldset()`'s and `led_group()`'s return expressions — the inline single-branch form of the same markup, for the exact class-attribute spelling and the `escape_html` routing.
    - `poll_trigger_section()` in full, BOTH branches and its whole docstring. Note the `> 0`-not-truthy comment above the branch (load-bearing, do not disturb) and note that the disabled branch already emits a second paragraph after the button.
    - `render()`'s return expression — specifically that `<h2 class="text-heading">Poll</h2>` is emitted by `render()` inside `<section class="page-section">`, with `poll_trigger_section()`'s output interpolated immediately after it. This is why emitting the caption first inside the section function lands it under the heading.
    - `companion/test_config_page.py`'s `_section_captions_appear_escaped_verbatim_exactly_once()`, `_each_group_emits_exactly_one_caption_between_heading_and_control()` (the one this task must NOT extend, and why), the four existing `poll_trigger_section()` checks (`_poll_trigger_enabled_at_zero_cooldown`, `_poll_trigger_disabled_with_remaining_seconds`, `_poll_trigger_live_countdown_seeded_from_server_value`, `_poll_trigger_zero_cooldown_ships_submit_affordance_script`), and `EXPECTED_CHECK_COUNT` plus the full change-log comment block above it.
  </read_first>
  <action>
**A. `companion/pages/config_page.py` — one new constant.**

Add `POLL_SECTION_CAPTION` immediately after `LED_SECTION_CAPTION` and before the `LED_SECTION_HEADING` comment, so all four `*_SECTION_CAPTION` names stay contiguous and the "every section on this page has exactly one caption" invariant is visible in one place. Carry the validated copy VERBATIM — do not reword, do not re-punctuate, do not expand or contract anything:

`Manually trigger an immediate poll cycle instead of waiting for the next scheduled one.`

Wrap it across source lines the same way the three neighbouring constants are wrapped, so the file's line-width convention holds.

Extend the shared comment above the group with a `quick task 260901-s5o:` sentence recording what is genuinely different about this fourth one: Poll was never part of the description/helper merge those three came out of, because it is a `<section class="page-section">` rather than a `.theme-status` group and had no paragraph at all to merge — it was simply skipped, leaving the page's fourth section as the only one with a bare heading. This caption is new copy, validated against the Settings Save Bar Sketch, not merged from anything. Also note that unlike the other three, this constant is consumed by a two-branch renderer, so it must be interpolated on both branches.

**B. `poll_trigger_section()` — emit the caption first, on both branches.**

Compute the markup once, before the `if cooldown_remaining > 0:` branch and after the existing `> 0`-not-truthy comment (leave that comment attached to the `if` where it is — it explains the branch condition, not the caption). Use the identical idiom `theme_fieldset()` uses: assign a local `caption_html` built from the same `<p class="text-label section-caption">%s</p>` template with the constant routed through `escape_html`. Do not add a new module-level helper function for this — three existing call sites already inline this template and a fourth is not the threshold at which extracting one earns its keep.

Interpolate `caption_html` as the FIRST element of BOTH return expressions, before each branch's `<form method="post" action="/poll-now">`:

1. Disabled branch (`cooldown_remaining > 0`): the element order becomes caption, poll form, cooldown paragraph, countdown script. Add the leading `"%s"` slot to the format string and `caption_html` as the first member of the interpolation tuple, keeping the existing four members in their current order after it.
2. Enabled branch (zero cooldown): the element order becomes caption, poll form, submit-affordance script. Same edit — leading `"%s"` slot, `caption_html` first in the tuple.

Nothing else in either branch changes. The `<form method="post" action="/poll-now">`, the button's `id` and `disabled` attributes, `POLL_COOLDOWN_HELPER_TEXT.format(n=cooldown_remaining)`, the `<p class="text-body" id="...">` cooldown paragraph, and both script calls are byte-identical.

Add a paragraph to `poll_trigger_section()`'s docstring stating: the section's single muted caption (`POLL_SECTION_CAPTION`) renders first on both branches, landing directly under the `<h2 class="text-heading">Poll</h2>` heading that `render()` — not this function — emits immediately before this function's output; and that a caption emitted on only one branch would silently disappear for the whole cooldown window, which is why `caption_html` is computed once above the branch rather than inline in each return.

**C. `companion/test_config_page.py` — one new check (+1).**

Add ONE new check, placed directly after `_poll_trigger_disabled_with_remaining_seconds()` so it sits with the other `poll_trigger_section()` coverage. It must assert all three of:

1. **Both branches carry exactly one caption.** Call `config_page.poll_trigger_section(0)` and `config_page.poll_trigger_section(17)`. For each, assert `rendered.count("section-caption") == 1` and that `escape_html(config_page.POLL_SECTION_CAPTION)` appears exactly once. Do NOT assert a `<p` count on the disabled branch — it legitimately emits two paragraphs (the caption and the cooldown copy).
2. **The caption precedes the trigger on both branches.** For each branch assert the index of `"section-caption"` is less than the index of `'<form method="post" action="/poll-now">'`.
3. **The caption lands under the Poll heading in the real page.** Call `config_page.render({...})` with `poll_cooldown_remaining: 0` (mirroring the ctx shape the neighbouring render-level checks use) and assert the escaped caption appears exactly once, with its index falling after `'<h2 class="text-heading">Poll</h2>'` and before `'action="/poll-now"'`. This is the assertion that actually pins the position, since the heading is emitted by `render()` and the caption by `poll_trigger_section()` — two different functions whose relative order nothing else guards.

Write its `check(...)` description to name all three properties and tag it `(quick task 260901-s5o)`, matching the description style of the checks around it.

**D. `companion/test_config_page.py` — one in-place retarget (no count change).**

Extend `_section_captions_appear_escaped_verbatim_exactly_once()` to cover `POLL_SECTION_CAPTION` alongside the existing three, keeping the exactly-once assertion and its per-constant failure message shape. Update its `check(...)` description from "the theme, runway, and LED section captions" to name all four. Do not add a check for this — it is the same check, widened.

Leave `_each_group_emits_exactly_one_caption_between_heading_and_control()` completely alone. See the Non-goals: adding Poll to it would raise `ValueError` on `rendered.index("</h2>")` and false-fail its `<p`-count assertion.

**E. `EXPECTED_CHECK_COUNT`.**

Read the REAL on-disk value at execution time and set it to that value plus exactly 1. Do not take any number from this plan's text. Append a `quick task 260901-s5o:` line to the change-log comment block above it recording the baseline you actually observed, the +1 (the Poll-caption both-branches-and-position check), and that the caption-verbatim check was widened in place with no count change.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.config_page as c
from companion.layout import escape_html
assert c.POLL_SECTION_CAPTION == 'Manually trigger an immediate poll cycle instead of waiting for the next scheduled one.', repr(c.POLL_SECTION_CAPTION)
for n in (0, 17):
    r = c.poll_trigger_section(n)
    assert r.count('section-caption') == 1, (n, r.count('section-caption'))
    assert r.count(escape_html(c.POLL_SECTION_CAPTION)) == 1, n
    assert r.index('section-caption') < r.index('<form method=\"post\" action=\"/poll-now\">'), n
page = c.render({'device_config': {'theme': 'sky', 'tracked_runway': '3', 'led_enabled': True}, 'poll_cooldown_remaining': 0})
cap = escape_html(c.POLL_SECTION_CAPTION)
assert page.count(cap) == 1
assert page.index('<h2 class=\"text-heading\">Poll</h2>') < page.index(cap) < page.index('action=\"/poll-now\"')
print('poll caption OK')
" && test "$(git diff --stat companion/static/style.css | wc -l)" -eq 0 && server/.venv/bin/python3 companion/test_config_page.py</automated>
    <human-check>Optional. Load `/settings` and confirm the Poll section now shows one grey caption sentence directly under its "Poll" heading, above the Trigger Poll Now button, at the same size and lightness as the Theme/Runway/Diagnostic LED captions — so all four sections read identically.</human-check>
  </verify>
  <done>
`companion/pages/config_page.py` defines `POLL_SECTION_CAPTION` once, verbatim, contiguous with the other three `*_SECTION_CAPTION` constants and covered by an updated shared comment; `poll_trigger_section()` computes `caption_html` once above its branch and emits it first on BOTH branches with everything else in the function byte-identical; its docstring records the two-branch requirement and the render()-owns-the-heading relationship; `companion/static/style.css` shows zero diff; `companion/test_config_page.py` carries exactly one new check covering both branches plus the render-level position, has `_section_captions_appear_escaped_verbatim_exactly_once()` widened in place to four constants, has `EXPECTED_CHECK_COUNT` at the real on-disk baseline plus exactly 1 with the bump recorded in the change-log, and passes. Committed as one focused commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign the save bar from a flush toolbar into a floating rounded pop-up</name>
  <files>companion/static/style.css, companion/test_config_page.py</files>
  <read_first>
    - `companion/static/style.css`'s base (column-0) `.dirty-bar` rule and its ENTIRE head comment. That comment's final paragraph is a long justification of the upward-only shadow literal ("a bar pinned to the viewport bottom is overlapped by content ABOVE it... no upward-shadow token exists and adding one would need four definition sites"). That whole premise is retired by this task and must be rewritten, not left standing beside contradicting declarations.
    - `.dirty-bar[hidden]`, `.dirty-bar__cancel` and the `button:hover` rule immediately after them, plus their comments — all three are untouched, and the `button:hover` placement is explicitly documented as load-bearing source-order. Read them so you edit around them, not through them.
    - The `@media (min-width: 960px)` `.dirty-bar` rule and its ENTIRE head comment. The fixed-not-sticky rationale paragraph stays true and stays; the content-column-geometry paragraph and the `240px` must-equal paragraph both need amending for the inset; the no-`z-index` paragraph stays.
    - `.dashboard-shell` (`grid-template-columns: 240px minmax(0, 1fr)`, `column-gap: var(--space-xl)`) and `.dashboard-main` (`max-width: min(1440px, 100%)`, `margin: 0 auto`, `padding: var(--space-2xl) var(--space-3xl)`) in that same media block — the geometry the bar reproduces, now inset.
    - `:root`'s `--radius-card` / `--radius-control` declarations and its `--shadow-card-hover` definition, plus the three theme-variant redefinitions of `--shadow-card-hover` (the `prefers-color-scheme: dark` block and both `[data-ui-theme]` blocks). Confirm for yourself that `--radius-card` is a real token and that `--shadow-card-hover` really does have dark-mode variants before relying on either.
    - `.lightbox`'s comment ("box-shadow: var(--shadow-card-hover) at rest is this phase's one deliberate exception to the hairline-at-rest contract... this is a floating modal overlay, not a page-flow card, so its resting state should already read as elevated") and `.lightbox::backdrop`'s raw-rgba comment. `.lightbox` is the file's existing precedent for a floating element carrying a resting shadow, and is the rule this one should cite rather than re-deriving the argument.
    - `.login-card` — note it carries a comment INSIDE its rule body. That is the one place in this file that does so, and Task 2 forbids copying that habit into the two rules it edits (see the action).
    - `companion/test_config_page.py`'s `_style_css_carries_section_caption_and_restyled_fixed_dirty_bar()` in full — its `border-top:` assertion (part b) and its `calc(240px + var(--space-xl))` assertion (part d) will BOTH fail against the new CSS. That is the guard doing its job; both must be retargeted in the same commit as the CSS so the harness is green at every commit.
  </read_first>
  <action>
**A. `companion/static/style.css` — restyle the base `.dirty-bar` rule.**

Three declaration changes, nothing else:

1. Replace the top-only hairline (`border-top: 1px solid var(--color-border)`) with a full four-sided `border: 1px solid var(--color-border)`. The bar is now a bordered floating card at both breakpoints, not a toolbar whose only edge is the one facing the content.
2. Replace the upward-only y-negated shadow literal with `box-shadow: var(--shadow-card-hover), 0 8px 24px rgba(18, 21, 27, 0.10);` — the real theme token first, then one larger ambient layer for the floating read. Take the ambient layer's numbers from the validated sketch.
3. Leave `display: flex`, `align-items: center`, `gap: var(--space-md)`, `padding: var(--space-md)`, `background: var(--color-dominant)`, `border-radius: var(--radius-card)` and `margin-top: var(--space-md)` exactly as they are. `border-radius: var(--radius-card)` in particular is now load-bearing at BOTH breakpoints, not just below 960px — see part B.

Rewrite the rule's head comment. Keep the first paragraph's factual frame (a JS-only enhancement layered over the always-server-rendered bottom `Save Settings` button; full-width and in normal flow below 960px; the >=960px variant lives in that breakpoint's own block). Replace the surface/shadow paragraph entirely. State: the bar is a floating rounded pop-up card, not a docked toolbar — hence a full border rather than a single top hairline, and a shadow that surrounds the whole box rather than projecting upward only. State that the first shadow layer is now the real `--shadow-card-hover` token rather than the y-negated black-alpha literal it replaces, and say why that is a strict improvement: the token carries its own dark-mode and `[data-ui-theme]` variants, whereas the retired literal was theme-neutral and near-invisible on a dark surface, leaning entirely on the hairline for separation. State that the second, larger ambient layer is a raw rgba literal deliberately, consistent with this file's existing raw-rgba shadow precedent, and that it exists to push the card further off the page than a card-hover shadow alone does. Cite `.lightbox` as the file's existing precedent for a floating element that carries a resting shadow rather than revealing one on hover — this bar is the second, and for the same reason.

**B. `companion/static/style.css` — inset the >=960px `.dirty-bar` rule.**

Four declaration changes plus one deletion:

1. `bottom` becomes `var(--space-md)` instead of zero.
2. `right` becomes `var(--space-md)` instead of zero.
3. `left` gains the same inset as a third addend, becoming `calc(240px + var(--space-xl) + var(--space-md))`.
4. `max-width` becomes `calc(min(1440px, 100%) - var(--space-md) * 2)` instead of the bare `min(1440px, 100%)` copied from `.dashboard-main`.
5. Delete the corner-squaring declaration (`border-radius: 0`) outright. Do not replace it with a restatement of the card radius — the base rule already declares `border-radius: var(--radius-card)`, and deleting the override is what makes one radius govern one component at every width. Deleting is the point; restating it here would create a second definition site that could silently drift from the base rule.

`position: fixed`, `margin: 0 auto` and `padding: var(--space-md) var(--space-3xl)` are unchanged.

The `max-width` reduction is load-bearing, not decorative, and must not be dropped as "redundant with left/right". Reason it through before writing it: for a fixed-position box, `100%` resolves against the viewport, so `min(1440px, 100%)` is a flat 1440px on any viewport at least 1440px wide. Once the viewport passes roughly 1712px (240px sidebar + 32px gap + 1440px cap), the cap binds and the box stops being sized by `left`/`right` at all — it would render exactly 1440px wide, exactly as wide as `.dashboard-main`, i.e. visually flush with the content column on both sides and showing no inset whatsoever. That is the exact treatment this task exists to retire, and 1728px is a 16-inch MacBook Pro's default logical width, so it is a width this project is actively developed at. Subtracting twice the inset from the cap keeps the bar exactly `var(--space-md)` narrower than the content column on each side at every width from 960px up.

Note for the comment (verify it yourself rather than taking it on faith): adding the inset to `left` and subtracting the same amount from `right` leaves the midpoint of the positioning region unchanged, so `margin: 0 auto` still centres the bar on exactly the same line `.dashboard-main` centres on. The inset is symmetric by construction.

Amend the rule's head comment. KEEP the fixed-not-sticky paragraph verbatim — it is still the reason this rule exists and is still true. KEEP the no-`z-index` paragraph verbatim. Rewrite the geometry paragraph: say the rule reproduces `.dashboard-main`'s content-column geometry INSET by `var(--space-md)` on left, right and bottom, so the bar reads as a floating pop-up card rather than a strip docked to the column's edges; record that the symmetric left/right inset preserves the centring; and record the `max-width` reduction with its reason (the cap binds above roughly 1712px and would otherwise render the bar flush with the content column, cancelling the inset at exactly the widths where it is most visible). Amend the `240px` must-equal paragraph to describe the now-three-term `left` expression while keeping its existing point intact — `240px` must still equal `.dashboard-shell`'s `grid-template-columns` first track and the `var(--space-xl)` addend its `column-gap`, with no token for either, guarded by a harness check. Add one sentence recording that the corner-squaring override is deliberately deleted rather than re-set, so the base rule's `--radius-card` is the single definition site for this component's radius.

**C. Comment placement — a hard constraint for both rules.**

Every explanatory comment for both edited rules goes ABOVE the selector, in the head comment. Do not place any comment INSIDE either `.dirty-bar` rule body. `.login-card` further down this file does carry an in-body comment; that is the file's lone exception and must not be copied here, because Task 2's harness guard greps these two rule bodies for the retired declarations by name, and a comment mentioning a retired declaration inside the body would false-fail the guard against correct CSS.

**D. `companion/test_config_page.py` — retarget the cross-file CSS guard in place (no count change).**

Edit `_style_css_carries_section_caption_and_restyled_fixed_dirty_bar()`. Its part (a) `.section-caption` assertions and its part (c) fixed-not-sticky assertions are unchanged. Change and extend the rest:

Part (b), the base rule body. Keep the `var(--color-dominant)` assertion and the "no `var(--color-secondary)`" assertion. Replace the top-hairline assertion with a full-border one: assert the body carries `border: 1px solid var(--color-border)` AND that it no longer carries a top-only hairline declaration — assert the substring `border-top:` is absent from the body. Add two assertions pinning the floating-card treatment: the body carries `border-radius: var(--radius-card)` (which is what the deleted >=960px override now defers to), and the body carries `box-shadow: var(--shadow-card-hover)` while no longer carrying the retired upward-only literal (assert the substring `box-shadow: 0 -` is absent). Give each its own explicit failure message in the style of the ones already there.

Part (d), the must-equal pair. Keep the `grid-template-columns: 240px` assertion against the whole source. Retarget the `left`-offset assertion from the old three-term literal onto the new four-term one, `calc(240px + var(--space-xl) + var(--space-md))`, so the pair stays pinned.

Add a part (e) for the inset itself, asserted against the media-query rule body: it carries `bottom: var(--space-md)`, it carries `right: var(--space-md)`, it carries the reduced cap `calc(min(1440px, 100%) - var(--space-md) * 2)`, and it no longer squares the corners (assert the substring `border-radius: 0` is absent from that body, so a future edit cannot silently re-dock the bar). Comment part (e) with one sentence recording why the reduced cap is asserted rather than left implicit: `left`/`right` alone stop sizing the box once the 1440px cap binds, so without this the inset would silently vanish above roughly 1712px.

Update the check's `check(...)` description string to describe the floating-card treatment rather than the toolbar one, and tag the change `(quick task 260901-s5o)`.

Append a `quick task 260901-s5o:` line to the change-log comment block above `EXPECTED_CHECK_COUNT` recording that this task retargeted and extended one check in place with no count change. Leave `EXPECTED_CHECK_COUNT`'s value exactly as Task 1 left it.

**E. Full suite.**

Run `scripts/run-all-tests.sh` and confirm the only failing harness is the known, pre-existing, unrelated `server/test_poll_loop.py` digest mismatch, and that no coverage-threshold failure is reported. No harness that passed before this plan may fail after it.
  </action>
  <verify>
    <automated>test "$(awk '/^\.dirty-bar \{/,/^\}/' companion/static/style.css | grep -cE 'border: 1px solid var\(--color-border\);|border-radius: var\(--radius-card\);|box-shadow: var\(--shadow-card-hover\), 0 8px 24px rgba\(18, 21, 27, 0\.10\);|background: var\(--color-dominant\);')" = 4 && test "$(awk '/^\.dirty-bar \{/,/^\}/' companion/static/style.css | grep -cE 'border-top:|box-shadow: 0 -|var\(--color-secondary\)')" = 0 && test "$(awk '/^  \.dirty-bar \{/,/^  \}/' companion/static/style.css | grep -cE 'position: fixed;|bottom: var\(--space-md\);|right: var\(--space-md\);|left: calc\(240px \+ var\(--space-xl\) \+ var\(--space-md\)\);|max-width: calc\(min\(1440px, 100%\) - var\(--space-md\) \* 2\);|margin: 0 auto;')" = 6 && test "$(awk '/^  \.dirty-bar \{/,/^  \}/' companion/static/style.css | grep -cE 'border-radius|position: sticky')" = 0 && grep -q 'grid-template-columns: 240px' companion/static/style.css && test "$(git diff --stat companion/static/dirty-state.js companion/pages/config_page.py | grep -c 'dirty-state.js')" = 0 && server/.venv/bin/python3 companion/test_config_page.py && LOG="${TMPDIR:-/tmp}/s5o-all-tests.log"; scripts/run-all-tests.sh > "$LOG" 2>&1; grep -E '^    - ' "$LOG" | grep -qv 'server/test_poll_loop\.py' && { echo "UNEXPECTED FAILING HARNESS:"; grep -E '^    - ' "$LOG"; exit 1; }; grep -q 'Coverage failure' "$LOG" && { echo "COVERAGE THRESHOLD REGRESSED"; exit 1; }; echo "SUITE OK (only the known poll_loop failure, if any)"</automated>
    <human-check>Optional but recommended, since the >=1712px behaviour is the one thing no source-level check can see rendered. Start a throwaway app (`SKYPANE_COMPANION_PASSWORD=<any> server/.venv/bin/python3 companion/app.py --port <free> --state-dir <tmpdir>`), sign in, dirty one field so the bar appears, and screenshot at two viewport widths: one around 1280x800 and one at least 1800 wide. At BOTH widths the bar must read as a floating rounded card — visibly detached from the viewport bottom edge, rounded on all four corners, bordered all the way around, with a shadow on every side and a clear gap between its left/right edges and the content column's edges. The wide shot is the one that matters: if the bar's edges line up exactly with the cards above it there, the `max-width` reduction was dropped. Stop the process and delete the tmpdir afterwards.</human-check>
  </verify>
  <done>
`companion/static/style.css`'s base `.dirty-bar` rule carries a full four-sided `--color-border` border, `border-radius: var(--radius-card)`, and a surrounding `var(--shadow-card-hover)` plus ambient-layer shadow, with no top-only hairline and no upward-only shadow literal remaining; its >=960px rule is inset by `var(--space-md)` on bottom, right and left (as a third addend on the existing `240px + var(--space-xl)` offset), caps its width at `calc(min(1440px, 100%) - var(--space-md) * 2)`, and no longer squares its corners, with `position: fixed`, `margin: 0 auto` and the `var(--space-md) var(--space-3xl)` padding unchanged; both head comments are rewritten to describe the floating-card treatment with the retired upward-shadow justification removed and the fixed-not-sticky and no-z-index rationales preserved; no comment sits inside either rule body; `companion/pages/config_page.py` and `companion/static/dirty-state.js` show zero diff for this task; `companion/test_config_page.py`'s cross-file CSS guard is retargeted and extended in place with no count change and passes; and `scripts/run-all-tests.sh` shows no failing harness other than the known `server/test_poll_loop.py` digest mismatch and no coverage-threshold failure. Committed as one focused commit.
  </done>
</task>

</tasks>

<commits>
Two focused commits, one per task, in task order — matching this session's established pattern (`61e4a03`/`9855cff`/`b4aa48d` for 260901-qif and `68272fd`/`9cad339`/`f92f34f` for 260901-re6). Each commit includes its own harness changes so `server/.venv/bin/python3 companion/test_config_page.py` is green at every commit, not just at the end.

1. After Task 1 — `feat(quick-260901-s5o): add a Poll section caption` (config_page.py + test_config_page.py).
2. After Task 2 — `fix(quick-260901-s5o): float the save bar as a rounded pop-up instead of a flush toolbar` (style.css + test_config_page.py).

Follow the repo's existing body style: a short prose paragraph or a tight bullet list explaining what changed and WHY (see `9cad339` and `9855cff` for the two shapes in use), ending with a line noting this is the third quick task in the 06.6.4.1-09 closing-checkpoint context this session, after 260901-qif and 260901-re6. No `Co-Authored-By` line unless the repo's own recent commits carry one — check `git log` before writing.
</commits>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → companion HTTP (`POST /settings`, `POST /poll-now`) | Untrusted form data crosses here; the session cookie gate in `companion/app.py`, `config_page.handle_post()`'s registry validation, and app.py's `_POLL_LOCK` + cooldown re-check are the controls. Neither change in this plan touches any of them. |
| `config_page` module constants → rendered HTML | Copy strings cross into a markup sink; `layout.escape_html()` is the control. |
| `config_page.py` ⇄ `style.css` | Class names duplicated by hand across two files that never import one another; the cross-file harness guard is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-s5o-01 | Tampering | `POLL_SECTION_CAPTION` rendered by `poll_trigger_section()` | low | mitigate | Routed through `layout.escape_html()` exactly as the four constants already rendered by this module are. It is a module-level literal never derived from request data, a session, the filesystem, or `cooldown_remaining`. Task 1's new check asserts it appears escaped-verbatim exactly once on both branches and once in `render()`. |
| T-s5o-02 | Information Disclosure | The Poll caption's copy | low | accept | The sentence describes only the control's own already-visible behaviour ("trigger an immediate poll cycle instead of waiting for the next scheduled one"). It names no interval, no upstream API, no credential, no route, and no internal identifier — nothing an unauthenticated observer could learn from it that the button's own label does not already say. The page is session-gated regardless. |
| T-s5o-03 | Denial of Service | The floating `position: fixed` bar overlaying the bottom of the page at >=960px | low | accept | Same accepted risk 260901-re6 recorded (T-re6-03), marginally larger: the bar now sits `var(--space-md)` off the bottom edge, so it can occlude roughly 16px more of the page's trailing content than the docked version did. It remains `hidden` unless the form is dirty, so it never occludes anything during normal reading, and both Cancel and Save remove it. Reserving permanent bottom padding for a conditionally-visible bar would cost every page load a visible gap to protect an edge case. Recorded as a Non-goal, not an oversight. |
| T-s5o-04 | Tampering | Hand-duplicated must-equal literals across `config_page.py` and `style.css` (`section-caption`, `240px`, the `min(1440px, 100%)` cap) | medium | mitigate | Task 2's retargeted cross-file guard pins the `240px` ⇄ `grid-template-columns` pair through the new three-term `left` expression, pins the reduced `max-width` cap against its `.dashboard-main` counterpart, and pins the absence of both retired toolbar declarations. Task 1's new check pins `.section-caption`'s fourth consumer against the rule that styles it. Neither file imports the other; these guards are the only thing preventing silent drift. |
| T-s5o-05 | Repudiation | `EXPECTED_CHECK_COUNT` drift | low | mitigate | The count is computed from the real on-disk baseline at execution time, never from this plan's text, and the observed baseline plus the reason for the +1 is recorded in the file's own change-log comment block — the same audit discipline every prior task on this file has followed. |
| T-s5o-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no dependency of any kind. All three files edited are already in the repository; `companion/` remains stdlib-only. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_config_page.py` passes after EACH commit — after Task 1 at the on-disk baseline plus exactly 1, and after Task 2 at that same value (Task 2 changes no count).
2. `git diff --stat companion/static/dirty-state.js` is empty for the whole plan.
3. `git diff --stat companion/static/style.css` is empty after Task 1's commit (fix 1 is Python-only), and `git diff --stat companion/pages/config_page.py` shows no Task 2 changes (fix 2 is CSS-plus-harness only).
4. The base `.dirty-bar` rule body contains no `border-top:` declaration and no upward-only y-negated shadow literal; the >=960px rule body contains no corner-squaring declaration and no sticky positioning.
5. `scripts/run-all-tests.sh` reports no failing harness other than the known, pre-existing, unrelated `server/test_poll_loop.py` digest mismatch, and no coverage-threshold failure.
6. Two commits exist, one per task, each self-contained and each leaving the harness green.
</verification>

<success_criteria>
- The Poll section renders exactly one muted `.section-caption` sentence directly under its heading and above the Trigger Poll Now button, carrying the validated copy verbatim, on both the enabled and the cooldown-disabled branch — so all four sections on `/settings` now read identically.
- No new CSS rule, class, custom property or helper function was introduced for that caption; it reuses `.text-label` + `.section-caption` and the existing markup pattern exactly.
- The save bar renders at >=960px as a floating rounded pop-up card: inset `var(--space-md)` from the content column's left, right and bottom edges at every width including above the 1440px cap, `--radius-card` on all four corners, a full `--color-border` border, and a surrounding token-based shadow plus one ambient layer.
- The bar's positioning mechanism, `form=` wiring, DOM placement, hidden-state rule and sub-960px in-flow behavior are all unchanged; `dirty-state.js` and `config_page.py` are untouched by fix 2.
- `companion/test_config_page.py` passes at its real on-disk baseline plus exactly 1, with the cross-file CSS guard retargeted onto the floating-card treatment, and `scripts/run-all-tests.sh` shows only the known `server/test_poll_loop.py` failure.
</success_criteria>

<output>
Create `.planning/quick/260901-s5o-fix-2-more-confirmed-settings-page-gaps-/260901-s5o-SUMMARY.md` when done.
</output>
