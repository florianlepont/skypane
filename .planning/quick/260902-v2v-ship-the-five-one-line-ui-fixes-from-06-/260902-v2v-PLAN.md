---
phase: quick-260902-v2v
plan: 260902-v2v
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/static/style.css
  - companion/pages/health_page.py
  - companion/pages/config_page.py
  - companion/app.py
  - companion/test_status_pages.py
  - companion/test_config_page.py
  - companion/test_contrast_check.py
autonomous: true
requirements: [QUICK-260902-v2v]

must_haves:
  truths:
    - "A 375px-wide /health render no longer scrolls horizontally. `document.documentElement.scrollWidth` reads 375 (it read 421 before, measured in 06.6.4.1-UI-REVIEW's `health-banner-mobile` screenshot run), and every `.banner__pill` element's `getBoundingClientRect().right` is <= 375."
    - "The mechanism for that is three declarations, not a rewrite: `flex-wrap: wrap` added to `.banner` (style.css L978-988, currently `display: flex; align-items: center; gap: 8px` with no wrap), `min-width: 0` added to `.banner__pill` (L1057-1069, currently `flex: none`, which is what refuses to let a pill shrink), and `white-space: nowrap` on a NEW `.banner__label` class carried by the count-and-noun lead span that `health_page.py`'s `_anomaly_banner_html()` emits (currently a bare `<span>` built from `lead_html`)."
    - "`.banner__pill`'s own head comment (style.css L1050-1056) stops claiming a literal-identity with `.airline-card__chip` that the `min-width: 0` addition makes false. The comment currently states the two rules' declarations are 'intentionally identical ... kept literally identical so a future consolidation into one shared class is a mechanical edit'. After this task it names the single deliberate divergence and why the banner call site needs it (the banner is a wrapping flex row at 375px; the chips row is not). No test pins that identity, so this is a comment-truth fix, not a harness-driven one."
    - "The accessible tail span (`<span class=\"visually-hidden\">` carrying `_anomaly_category_text()` + `ANOMALY_BANNER_TEXT`) is byte-identical after this task. Every existing `ANOMALY_BANNER_TEXT in rendered` presence/count check in `test_status_pages.py` keeps passing without being touched, which is the exact property `_anomaly_banner_html()`'s own docstring says that span exists to preserve."
    - "Airlines illustrations stop being letterboxed. `.airline-card__image` (style.css L2911-2918) gains `height: auto;`, which is what lets its already-declared `aspect-ratio: 900 / 263` apply at all — `aspect-ratio` is ignored while both dimensions are resolved, and the `<img width=\"900\" height=\"263\">` attributes that `airlines_page.py`'s `_airline_card_html()` emits map to presentational height. The HTML attributes STAY: with `height: auto` they still reserve the correct box for `loading=\"lazy\"`, which is why UIR-07's own fix note says to keep them."
    - "The measured result of that one declaration: the first `.airline-card__image` renders at a height of roughly width x 263/900 (about 65px at a 224px-wide card, about 95px at the 325px mobile card) instead of a flat 263px, and `.airline-card`'s own height drops from about 317px toward about 120px. Before/after numbers for both are recorded in SUMMARY.md from a real browser, not computed from the ratio."
    - "The Battery trend heading reads with a space before its em dash. `_battery_trend_section_html()` (health_page.py L1571-1580) currently emits `...Battery trend<span class=\"text-label section-caption\">— %s</span>`; the caption string gains a leading space inside the span so the rendered text content is `Battery trend — Last 3 months, daily average`. The raw HTML therefore contains the literal `section-caption\"> — ` and that is what the automated guard pins, since the DOM text and the HTML source differ here by the span tag."
    - "The markup fix is chosen over UIR-12's alternative (`margin-left: var(--space-xs)` on `h2 > .section-caption`) for two concrete reasons, both verified in source rather than assumed. First, `test_status_pages.py:2416` locates the base rule with `css_source.index(\".section-caption {\")` and asserts its body declares exactly one property — a new `h2 > .section-caption {` rule placed anywhere before it would be found by that same substring search and fail the check against the wrong rule body. Second, a CSS margin leaves the accessible name glued ('trend—Last') for a screen reader; a real space fixes both renderings at once."
    - "All three existing Battery-trend caption assertions keep passing untouched, verified by reading them before editing: `test_status_pages.py:777` and `:875/:882` check `\"Last 3 months\" in rendered`, `:893` checks `\"Latest %d readings\"`, and `:2395` locates the heading via `rendered.index(\">%s\" % BATTERY_SECTION_HEADING)` then requires `class=\"text-label section-caption\"` inside it. A leading space inside the span body changes none of those four anchors."
    - "The Resolution-statistics Source column stops wrapping on desktop. A new rule `.data-table--prose th:first-child, .data-table--prose td:first-child { white-space: nowrap; }` is placed IMMEDIATELY AFTER the existing `.data-table--prose { min-width: 0; }` rule (style.css L1688), so that `test_status_pages.py:3459`'s `css_source.index(\".data-table--prose {\")` still resolves to the base rule and its `base_at < prose_css_at` ordering assertion against `.data-table {` still holds. At 1440px 'Fresh lookup' and 'Airline only' each render on one line."
    - "This task changes nothing about that table below 960px. UIR-10 (the mobile Health tables) is explicitly out of scope and no media-query-scoped rule is added."
    - "Button copy is sentence case in all three remaining Title Case sites: `app.py`'s `_login_body` submit button, and `config_page.py`'s two `poll_trigger_section()` buttons (the cooldown-disabled one at L572 and the enabled one at L586) plus `render()`'s no-JS fallback submit button at L655. The dirty bar's own save button (L644) is already sentence case and is not touched — it is the register the other three are being brought into line with."
    - "Every harness assertion pinning the old copy is retargeted, located by grep before editing rather than by memory: `test_config_page.py` L309-310, L313 (check description), L481-482, L828-831 (a `re.escape`'d regex whose literal tail is the button text), L836 (check description), L909-910 and L1053-1054. `test_companion_app.py` has ZERO assertions on the login button copy — grep-verified — so no login-side test edit is needed."
    - "`companion/test_contrast_check.py`'s module docstring (L21) mentions the old Title Case label in prose only; it is a docstring, not an assertion, and is updated for consistency in the same commit. No planning or skill doc is edited, because no harness reads one for this copy."
    - "New regression coverage lands with the fixes, not after them: one new check in `test_status_pages.py` pins all four style/markup contracts at once (`.banner`'s wrap, `.banner__label`'s nowrap plus the class actually appearing on the rendered lead span, `.banner__pill`'s `min-width: 0`, `.airline-card__image`'s `height: auto` alongside its surviving `aspect-ratio`, the prose first-column nowrap rule and its placement after the base rule, and the space before the em dash in the rendered Battery trend heading). `EXPECTED_CHECK_COUNT` moves 112 to 113 with a dated provenance comment in the file's own idiom."
    - "The browser verification runs against a COPY of `/tmp/skypane-prod-state`, never the original — the Settings page POSTs into its state dir and the original is the developer's real captured production state."
    - "`scripts/run-all-tests.sh` reports every companion harness green (companion-app, config-page, contrast-check, status-pages, view-pages). `server/test_poll_loop.py`'s pre-existing state is not this task's concern and is not touched."
  artifacts:
    - path: "companion/static/style.css"
      provides: "`.banner` flex-wrap, `.banner__label` nowrap rule, `.banner__pill` min-width with a corrected head comment, `.airline-card__image` height: auto, and the `.data-table--prose` first-column nowrap rule placed after the base rule"
      contains: "flex-wrap: wrap"
    - path: "companion/pages/health_page.py"
      provides: "`_anomaly_banner_html()`'s lead span carrying `class=\"banner__label\"`, and `_battery_trend_section_html()`'s caption span opening with a space before its em dash"
      contains: "banner__label"
    - path: "companion/app.py"
      provides: "Sentence-case login submit button copy"
    - path: "companion/pages/config_page.py"
      provides: "Sentence-case poll-trigger buttons (both cooldown branches) and the no-JS fallback save button"
    - path: "companion/test_status_pages.py"
      provides: "One new check pinning the four UIR-03/07/12/13 contracts together, with EXPECTED_CHECK_COUNT at 113"
      contains: "EXPECTED_CHECK_COUNT = 113"
    - path: "companion/test_config_page.py"
      provides: "Seven retargeted assertions/descriptions on the sentence-case button copy"
  key_links:
    - "`.banner__label` in style.css <-> the class emitted on `lead_html`'s span in `health_page.py::_anomaly_banner_html()` — the nowrap rule does nothing if the class is only on one side, which is why the new harness check pins both halves in one check."
    - "`.airline-card__image { height: auto }` <-> the `<img width height>` attributes in `airlines_page.py::_airline_card_html()` — the attributes must survive for lazy-loading box reservation; removing them would 'fix' the letterbox while reintroducing layout shift."
    - "The new `.data-table--prose th:first-child` rule's SOURCE POSITION <-> `test_status_pages.py:3459`'s `css_source.index(\".data-table--prose {\")` — placing the new rule before the base rule does not change specificity but does change which rule body that index finds."
    - "`config_page.py` L655's fallback button text <-> `test_config_page.py:828`'s `r'<button\\b[^>]*%s[^>]*>...</button>'` regex — the button copy is baked into the regex literal, so the copy change and the regex change are one edit, not two."
---

<objective>
Ship the five one-line UI fixes from `06.6.4.1-UI-REVIEW.md` that need no design decision: the Health anomaly banner's 375px horizontal overflow (UIR-03, the review's only BLOCKER), the letterboxed Airlines illustrations (UIR-07), the glued em dash in the Battery trend heading (UIR-12), the wrapping Resolution-statistics Source column (UIR-13), and three Title Case button labels in a sentence-case UI (UIR-15).

Purpose: UIR-03 makes every phone load of the page whose whole job is "is everything fine?" horizontally scrollable and triggers mobile-browser zoom-out; the other four are visible-on-every-load polish defects that each cost one declaration or one string. Shipping them together keeps five separate review items from each needing their own task cycle.

Output: five defects fixed in four source files, one new regression check pinning four of them at once, seven retargeted copy assertions, and real before/after browser measurements at 375px and 1440px recorded in SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-REVIEW.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@companion/static/style.css
@companion/pages/health_page.py
@companion/pages/config_page.py

Design-system boundaries that apply to every task below (from `sketch-findings-skypane` and `style.css`'s own header comment): no new design tokens, no new font, no change to the accent-reservation list, and zero external references in `style.css`. Every declaration this plan adds is a layout/typography primitive (`flex-wrap`, `min-width`, `white-space`, `height`) — none of them is a colour, and none of them touches the accent.

Branch `claude/ui-audit-quick-fixes-fa542d` is already checked out. Do not create a branch or a worktree.

Single-harness invocation for the fast loop: `server/.venv/bin/python3 companion/test_status_pages.py` (each harness is a standalone `__main__` script; `scripts/run-all-tests.sh` runs the canonical 16 under coverage).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the banner overflow, the letterboxed illustrations, the glued em dash and the wrapping Source column</name>
  <files>companion/static/style.css, companion/pages/health_page.py, companion/test_status_pages.py</files>
  <behavior>
    New check in `companion/test_status_pages.py`, added next to the existing stylesheet-contract checks and pinning all four contracts in ONE check so a partial fix cannot satisfy it:
    - Reading `companion/static/style.css` as source text: `.banner {`'s rule body declares a wrapping flex; a `.banner__label` rule exists and declares the no-wrap white-space; `.banner__pill {`'s body declares `min-width: 0` while still declaring `flex: none`; `.airline-card__image {`'s body declares `height: auto` AND still declares its `aspect-ratio: 900 / 263`; a `.data-table--prose`-scoped `:first-child` rule exists, declares the no-wrap white-space, and its index is GREATER than `css_source.index(".data-table--prose {")` (the placement half — see key_links).
    - Rendering `health_page.render()` on a fixture seeded to produce an anomaly banner: the lead span carries `class="banner__label"`, and the count-and-noun text is inside that span rather than a bare one.
    - Rendering `health_page.render()` on any fixture: the raw markup contains the caption span opening with a space before its em dash, i.e. the `section-caption"> ` + em-dash sequence.
    - `.banner__pill {`'s index in the source is still LESS than `.refresh-pill {`'s (re-asserting the neighbouring source-order contract that check at L4084 already owns, so an edit near this rule cannot silently reorder it).
    Bump `EXPECTED_CHECK_COUNT` 112 -> 113 with a `# quick task 260902-v2v (2026-09-02): 1 new` provenance comment in the file's existing idiom.
  </behavior>
  <action>
Four independent fixes, one commit. Write the new harness check FIRST and watch it fail, then apply the four fixes and watch it pass.

**UIR-03 — banner overflow (the BLOCKER).** In `companion/static/style.css`:
(a) Add `flex-wrap: wrap;` to the `.banner` rule (around L978-988). Leave `display: flex`, `align-items: center`, `gap: 8px`, the padding/margin, the `--color-dominant` background, the border pair and the font-size exactly as they are — the accent left edge is on the accent-reservation list and must not move.
(b) Add `min-width: 0;` to the `.banner__pill` rule (around L1057-1069). Keep `flex: none` — it is what keeps the pill from stretching; `min-width: 0` is what lets it participate in wrapping instead of forcing the row wider than the viewport.
(c) That rule's head comment (around L1050-1056) currently asserts its declarations are kept literally identical to `.airline-card__chip`'s so a future consolidation is mechanical. That claim is now false. Rewrite the relevant sentence to name the one divergence and its reason: the banner pill lives in a wrapping flex row that must survive a 375px viewport, the chip does not, so the two rules now differ by exactly one declaration and a consolidation would need to carry it. Do not silently leave the stale claim.
(d) Add a new `.banner__label` rule near `.banner__pill` declaring `white-space: nowrap;` and nothing else.
(e) In `companion/pages/health_page.py`'s `_anomaly_banner_html()`, change `lead_html` so the span carries `class="banner__label"`. Change NOTHING else in that function: the per-anomaly `banner__pill` spans and, critically, the trailing `<span class="visually-hidden">` accessible tail stay byte-identical — that tail is what keeps every existing `ANOMALY_BANNER_TEXT` assertion in this harness passing untouched, as the function's own docstring records.

**UIR-07 — letterboxed illustrations.** Add `height: auto;` to the `.airline-card__image` rule (around L2911-2918). Do not remove or alter the rule's `width: 100%`, `aspect-ratio: 900 / 263`, `object-fit: contain`, `display: block`, radius or margin. Do not touch `airlines_page.py` — the `<img width height>` attributes must stay; with `height: auto` the browser resolves the box from `aspect-ratio` instead, which is exactly what `loading="lazy"` needs to avoid layout shift. Extend the rule's existing head comment with one sentence recording why `height: auto` is load-bearing (the attributes otherwise resolve both dimensions and `aspect-ratio` never applies), so the next reader does not "clean up" the declaration.

**UIR-12 — glued em dash.** In `_battery_trend_section_html()` (around L1571-1580), add a leading space inside the caption span's text so it emits the space-then-em-dash sequence rather than opening on the dash. Do NOT take UIR-12's CSS alternative: a new `h2 > .section-caption` rule would be found by `test_status_pages.py:2416`'s `css_source.index(".section-caption {")` substring search if placed before the base rule, and a margin leaves the accessible name glued for a screen reader. Verify before editing that the three caption assertions at L777, L875/L882/L893 and L2395 anchor on `"Last 3 months"`, `"Latest %d readings"`, `">" + BATTERY_SECTION_HEADING` and `class="text-label section-caption"` respectively — none of which a leading space disturbs.

**UIR-13 — wrapping Source column.** Add `.data-table--prose th:first-child, .data-table--prose td:first-child { white-space: nowrap; }` IMMEDIATELY AFTER the existing `.data-table--prose { min-width: 0; }` rule (around L1688), never before it: `test_status_pages.py:3459` finds the base rule by substring index and asserts it follows `.data-table {`, and a new rule placed earlier would be found in its place. Add nothing media-query-scoped — the mobile behaviour of this table is UIR-10 and is out of scope.

Nothing in this task adds a design token, a font, a colour or an accent use.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && grep -c "EXPECTED_CHECK_COUNT = 113" companion/test_status_pages.py && grep -c "banner__label" companion/pages/health_page.py && grep -c "height: auto" companion/static/style.css</automated>
  </verify>
  <done>`test_status_pages.py` reports PASS with 113/113 checks; the new check fails if any one of the four fixes is reverted in isolation; `.banner__pill`'s head comment no longer claims declaration-identity with `.airline-card__chip`; `airlines_page.py` is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Sentence-case the three Title Case button labels</name>
  <files>companion/app.py, companion/pages/config_page.py, companion/test_config_page.py, companion/test_contrast_check.py</files>
  <action>
UIR-15. The UI's register is sentence case — the dirty bar's own save button (`config_page.py` L644), "Sign out", "Clear", "Close", "More details", "View 20 readings" — and three labels predate it. Bring all three into line; touch no other copy.

Source edits:
1. `companion/app.py`, `_login_body()` (around L611): the login form's submit button label becomes sentence case.
2. `companion/pages/config_page.py`, `poll_trigger_section()`: BOTH poll-trigger button labels — the `disabled` one in the cooldown branch (around L572) and the enabled one (around L586). Missing one leaves the copy inconsistent across the cooldown state.
3. `companion/pages/config_page.py`, `render()` (around L655): the no-JS fallback submit button label becomes sentence case, matching the dirty bar's existing one exactly.
4. `companion/pages/config_page.py`'s module docstring (L2, L8) and the comments at L613/L622 quote the old labels as the shipped copy; update those quotes so the file does not contradict itself. Same for `companion/static/style.css`'s comments at L562, L1271-1272 and L1334, which enumerate the primary-action buttons by name.

Harness edits — grep `companion/test_*.py` for the three old literals first and retarget every hit found, not only the ones listed here (the list below is the grep result at plan time):
- `test_config_page.py` L309-310 (presence check + its failure message), L313 (the `check(...)` description string), L481-482 (the in-form fallback presence check + message), L828-831 (the `re.search` pattern's literal button text, which is interpolated with `re.escape(STATIC_SAVE_FALLBACK_ATTR)` — change the literal tail only, not the escaping), L836 (check description), L909-910 and L1053-1054 (both poll-trigger presence checks + messages).
- `test_contrast_check.py` L21 is a module docstring mentioning the old label in prose. It is not an assertion; update it for consistency in this same commit.
- `test_companion_app.py` has no assertion on the login button copy (grep-verified at plan time) — confirm that is still true, and if so leave it unmodified rather than inventing a check for it here.

Do NOT edit any file under `.planning/` or `.claude/skills/` for this: `references/settings-page-patterns.md` quotes the old label in a markup excerpt, but no harness reads that file, and the task spec scopes doc edits to harness-read docs only.

Add no new checks in this task — the retargeted assertions already fail on the old copy and pass on the new, which is the regression coverage.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_contrast_check.py && grep -c ">Sign in</button>" companion/app.py && grep -c ">Trigger poll now</button>" companion/pages/config_page.py</automated>
  </verify>
  <done>`test_config_page.py` reports PASS at its unchanged 64/64 check count (assertions retargeted, none added or removed); the login button grep returns 1; the poll-trigger grep returns 2 (both cooldown branches); `config_page.py`'s own docstring and comments no longer quote the retired Title Case labels.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the fixes in a real browser and record before/after numbers</name>
  <files>companion/ (read-only), .planning/quick/260902-v2v-ship-the-five-one-line-ui-fixes-from-06-/260902-v2v-SUMMARY.md</files>
  <action>
Computed-style inspection alone already missed a real mobile bug on this project once (quick task 260902-qkm), so UIR-03 and UIR-07 get measured in a running browser at a real viewport width, not asserted from CSS text.

Set up, in this order:
1. `cp -a /tmp/skypane-prod-state /tmp/skypane-v2v-verify` — measure against the COPY. NEVER point the verification server at `/tmp/skypane-prod-state` itself: the Settings page POSTs into its state dir, and that directory is the developer's real captured production state.
2. Start the companion exactly as `scripts/run-local-verify.sh` does but against the copy: `SKYPANE_COMPANION_PASSWORD=local-verify-only server/.venv/bin/python3 companion/app.py --state-dir /tmp/skypane-v2v-verify`, in the background.
3. Drive it with the Playwright MCP tool (`mcp__plugin_playwright_playwright__browser_run_code_unsafe`), logging in with that same password. If Playwright is unavailable, use the in-app browser and read the same numbers.

Measure and record, at a 375px-wide viewport:
- `/health`: `document.documentElement.scrollWidth` (expected 375; the review measured 421 before the fix).
- `/health`: `[...document.querySelectorAll('.banner__pill')].map(el => el.getBoundingClientRect().right)` — every value <= 375.
- `/airlines`: the first `.airline-card__image`'s `getBoundingClientRect()` width and height, and its `.airline-card`'s height.

Then at 1440px:
- `/airlines`: the same three numbers (the review measured 224x263 with a 317px card before the fix).
- `/health`: that the Resolution-statistics table's first-column cells render on one line — read `getBoundingClientRect().height` for the "Fresh lookup" and "Airline only" cells and compare against a single-line cell's height in the same table.
- `/health`: the Battery trend `h2`'s `textContent`, confirming the space around the em dash survives into the DOM.

Write every one of those numbers into SUMMARY.md as a before/after table, using the review's own measured values as the "before" column and citing the review as their source (do not re-measure the pre-fix state by reverting).

Stop the background server and `rm -rf /tmp/skypane-v2v-verify` when done. Leave `/tmp/skypane-prod-state` untouched — confirm with `ls -la` that its mtime is unchanged from before the run.

Finally run the full suite: `scripts/run-all-tests.sh`. Every companion harness (companion-app, config-page, contrast-check, status-pages, view-pages) must be green. `server/test_poll_loop.py`'s pre-existing state is not this task's concern — report it if it fails, do not fix it here.
  </action>
  <verify>
    <automated>scripts/run-all-tests.sh 2>&1 | tail -30</automated>
    <human-check>Developer confirms on a real phone (or a 375px browser window) that the /health anomaly banner no longer hangs off-screen and the page does not scroll sideways, and that the Airlines cards read as illustration cards rather than letterboxed boxes.</human-check>
  </verify>
  <done>SUMMARY.md carries a before/after table with real measured numbers for /health scrollWidth, `.banner__pill` right edges, the airline image box and the airline card height; the five companion harnesses are green in `scripts/run-all-tests.sh`; `/tmp/skypane-prod-state` is byte-unchanged and the verify copy is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> companion HTTP (authenticated session) | The only untrusted input path this task's rendered markup reaches; unchanged in shape by this task. |
| Verification harness -> developer's captured production state dir | Task 3 runs a real, write-capable server whose Settings POST target is a directory on disk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2v-01 | Tampering | Task 3's local verification server, `--state-dir` | medium | mitigate | Run only against `cp -a` copy `/tmp/skypane-v2v-verify`; verify `/tmp/skypane-prod-state` mtime is unchanged after the run and delete the copy. |
| T-v2v-02 | Information disclosure | `SKYPANE_COMPANION_PASSWORD` in the verification run | low | mitigate | Use the throwaway `local-verify-only` value `scripts/run-local-verify.sh` already uses; never the production password, never committed. |
| T-v2v-03 | Tampering (XSS) | `_anomaly_banner_html()` lead span, `_battery_trend_section_html()` caption | low | mitigate | Both edits add a static attribute/literal space only. Every interpolated value stays inside its existing `escape_html()` call; no new interpolation site and no raw-HTML path is introduced. |
| T-v2v-04 | Tampering | `.airline-card__image` sizing change | low | accept | A purely presentational declaration on an already-served, already-normalized image route; no new source, origin or attribute is introduced. |
| T-v2v-SC | Tampering | npm/pip/cargo installs | n/a | accept | This task installs no packages from any package manager — no dependency file is touched, so the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` shows all five companion harnesses green: companion-app, config-page, contrast-check, status-pages, view-pages.
- `companion/test_status_pages.py` runs 113/113 (112 + 1 new).
- `companion/test_config_page.py` runs at its unchanged 64/64 — this task retargets assertions, it does not add or remove any.
- Real-browser numbers at 375px and 1440px are recorded in SUMMARY.md with the review's measurements as the before column.
- `git diff --stat` touches only: `companion/static/style.css`, `companion/pages/health_page.py`, `companion/pages/config_page.py`, `companion/app.py`, `companion/test_status_pages.py`, `companion/test_config_page.py`, `companion/test_contrast_check.py`, plus `.planning/`. In particular `companion/pages/airlines_page.py` is NOT in the diff.
- No new design token, font, colour or accent use anywhere in the diff; `style.css` still has zero external references.
</verification>

<success_criteria>
- UIR-03: `/health` at 375px has `document.documentElement.scrollWidth === 375` and every `.banner__pill` right edge <= 375.
- UIR-07: the first `.airline-card__image` renders at approximately width x 263/900 and `.airline-card`'s height drops from about 317px toward about 120px.
- UIR-12: the rendered Battery trend heading's `textContent` contains the heading, a space, an em dash, a space, then the caption.
- UIR-13: "Fresh lookup" and "Airline only" each render on one line at 1440px; nothing media-query-scoped was added.
- UIR-15: all three buttons read in sentence case, every harness assertion pinning the old copy is retargeted, and no doc outside the harnesses was edited.
- One commit per task, conventional-commit style with the `(quick-260902-v2v)` scope.
</success_criteria>

<output>
Create `.planning/quick/260902-v2v-ship-the-five-one-line-ui-fixes-from-06-/260902-v2v-SUMMARY.md` when done, including the before/after measurement table from Task 3.
</output>
