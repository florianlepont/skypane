---
phase: quick-260901-re6
plan: 260901-re6
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/test_config_page.py
autonomous: true
requirements: [QUICK-260901-re6]

must_haves:
  truths:
    - "Each of the three Settings groups (Theme, Runway, Diagnostic LED) renders EXACTLY ONE paragraph element — a single muted caption sitting directly under the group's `<h2 class=\"text-heading\">` heading and BEFORE that group's control (the swatch row / the `.runway-row` / the `.led-checkbox` label). Theme and Runway go from two paragraphs to one; LED keeps its single paragraph but moves it above the checkbox label and restyles it."
    - "The three captions carry the validated copy verbatim, with no rewording: Theme = \"Panel colors for departing/arriving flights. More themes appear here once validated on real hardware.\"; Runway = \"Which Orly runway the device watches. Applies on the next scheduled poll, not immediately.\"; Diagnostic LED = \"Lit only during the device's brief wake window, not visible from the wall side. Applies on the next scheduled poll.\""
    - "The Theme group's heading is still the word \"Theme\" — the section heading is NOT renamed."
    - "Each caption computes to `font-size: var(--font-label-size)` (14px) and `color: color-mix(in srgb, var(--color-text) 70%, transparent)` — the 70% muted strength this stylesheet already uses for secondary copy (`.data-table-wrap thead`, the filter-bar hint, the empty-state copy). No new muted strength, no new font-size token, no new custom property is introduced."
    - "All five superseded constants (`THEME_HELPER_TEXT`, `THEME_SECTION_DESCRIPTION`, `RUNWAY_HELPER_TEXT`, `RUNWAY_SECTION_DESCRIPTION`, `LED_HELPER_TEXT`) are removed outright from `companion/pages/config_page.py` — no definition, no reference, no dead second-paragraph markup, and no docstring or comment left describing markup that no longer exists."
    - "`companion/pages/config_page.py`'s `<form class=\"config-form\">` carries an `id` attribute whose value is defined exactly once in the module, as `SETTINGS_FORM_ID`, and referenced (never re-typed) by both the form tag and the dirty-bar's save button — the same one-definition-site discipline the module already applies to `RUNWAY_IMAGE_ROUTE_PREFIX` and `SETTINGS_ROUTE`."
    - "`dirty_bar_html` is constructed and emitted AFTER `</form>` closes AND after the Poll `<section>` — it is the last element `render()` returns, a sibling of the form rather than a descendant of it."
    - "The dirty-bar's own `<button type=\"submit\" class=\"dirty-bar__save\">` carries `form=\"{SETTINGS_FORM_ID}\"`, so it natively submits the merged Theme+Runway+LED settings form in one POST despite now living outside it in the DOM. Proven by a real browser click against a live `companion/app.py` process, not by a source-level assertion alone."
    - "`companion/static/dirty-state.js` is NOT modified. Its `bar`/`countEl`/`cancelBtn` lookups are already document-wide `document.querySelector` calls and its cancel handler already calls `form.reset()` directly, so the bar functions unchanged as a sibling of the form."
    - "`.dirty-bar` no longer uses `position: sticky` anywhere. At >=960px it is `position: fixed; bottom: 0` with `right: 0` and a `left` offset equal to `.dashboard-shell`'s own sidebar grid track, plus `.dashboard-main`'s own `max-width: min(1440px, 100%)` / `margin: 0 auto` / `var(--space-3xl)` horizontal padding — so the bar's content box lines up with the page content column above it at every width, instead of reading as a full-bleed strip. Below 960px it stays full-width and non-fixed in normal document flow, unchanged."
    - "`.dirty-bar` visually stands out from the page: `background: var(--color-dominant)` (the same card surface every other card on the page uses, replacing the muted `--color-secondary`), `border-top: 1px solid var(--color-border)`, and a genuinely present UPWARD box-shadow (the y-negated mirror of this file's own `--shadow-card-hover` light-mode pair) — never `box-shadow: none`, and never a downward shadow that would fall off-screen below a viewport-pinned bar."
    - "`.dirty-bar[hidden] { display: none }` still wins over the base rule's `display: flex`, so a non-dirty form shows no bar at any width."
    - "A real browser, driving a live `companion/app.py` process at a >=960px viewport, dirties two fields (Runway card + LED checkbox), clicks the dirty-bar's Save button, and the on-disk `device_config.json` afterwards shows BOTH new values — proving the cross-DOM `form=` association submits the whole merged form in one POST."
    - "`companion/test_config_page.py` passes, with `EXPECTED_CHECK_COUNT` moved from its real on-disk baseline (read at execution time, never guessed) to that baseline plus exactly 3."
    - "`scripts/run-all-tests.sh` reports no failing harness other than the known, pre-existing, unrelated `server/test_poll_loop.py` digest mismatch, and no coverage-threshold failure."
  artifacts:
    - path: "companion/pages/config_page.py"
      provides: "Three merged caption constants; `SETTINGS_FORM_ID`; `theme_fieldset()`/`runway_fieldset()`/`led_group()` each emitting exactly one caption paragraph; `render()` emitting the dirty bar after `</form>` and after the Poll section, with `form=` on its save button"
      contains: "SETTINGS_FORM_ID"
    - path: "companion/static/style.css"
      provides: "A `.section-caption` colour modifier composing with `.text-label`; the restyled `.dirty-bar` base rule (dominant surface, top hairline, upward shadow); the >=960px `.dirty-bar` fixed/content-column-aligned rule replacing the sticky one"
      contains: ".section-caption {"
    - path: "companion/test_config_page.py"
      provides: "Five in-place check retargets (no count change) plus 3 new checks and `EXPECTED_CHECK_COUNT` at on-disk-baseline + 3"
      contains: "section-caption"
  key_links:
    - from: "`config_page.SETTINGS_FORM_ID` interpolated into the `<form id=\"%s\">` tag"
      to: "the same constant interpolated into the dirty-bar save button's `form=\"%s\"` attribute — if these two ever drift, the bar's Save button silently submits nothing; Task 3's live-browser smoke test is the only check that actually catches that at runtime"
    - from: "`config_page`'s `<p class=\"text-label section-caption\">` markup"
      to: "style.css's `.text-label` (supplies `var(--font-label-size)`) plus `.section-caption` (supplies only the 70% muted colour) — the class pair is inert markup without both rules, and Task 3's cross-file guard is what keeps them from drifting"
    - from: "style.css's >=960px `.dirty-bar { left: calc(240px + var(--space-xl)) }`"
      to: "`.dashboard-shell`'s `grid-template-columns: 240px minmax(0, 1fr)` and its `column-gap: var(--space-xl)` — the same duplicated-not-imported must-equal discipline `DIRTY_SECTION_ATTR` already documents; Task 3's guard asserts both literals still agree"
    - from: "`render()`'s dirty bar now being a sibling of the form"
      to: "`companion/static/dirty-state.js`'s document-wide `document.querySelector(\"[data-dirty-bar]\")` / `[data-dirty-count]` / `[data-dirty-cancel]` lookups and its direct `form.reset()` call — this is exactly why the JS file needs no edit; if any of those three lookups were ever narrowed to `form.querySelector`, the bar would silently stop working"
---

<objective>
Fix 2 more confirmed issues on the companion Settings page (`/settings`), found continuing phase 06.6.4.1's closing checkpoint by comparing the real rendered page against real-browser testing and a validated typography sketch artifact.

| # | Issue | Root cause | Fix |
|---|-------|------------|-----|
| 1 | Theme and Runway each render TWO paragraphs of prose around their control (a description above, a helper below) at full body size and full opacity, so the page reads as a wall of text with no visual hierarchy between control and explanation | The D-02 "one description sentence per group" change (06.6.4.1) ADDED a paragraph above the control without retiring the pre-existing helper paragraph below it, leaving both. LED escaped the doubling but kept the un-muted full-body-size style | Merge each pair into ONE muted caption directly under the heading, at `--font-label-size` and the file's existing 70% muted text strength; restyle (not merge) LED's single paragraph and move it above its control |
| 2 | The unified save bar detaches from the viewport bottom and visibly stops right above the Poll section when scrolling a page much taller than the form | `.dirty-bar` is `position: sticky; bottom: 0` AND lives inside `<form class="config-form">`. A sticky element's containing block is its nearest scrolling ancestor's *box* — here the short 3-section form — so it stops sticking at the form's own bottom edge. It also blends into the page as a muted `--color-secondary` surface with no border and no shadow | Move the bar out of the form (last element on the page), give the form an `id` and the bar's save button a matching `form=` attribute, switch to `position: fixed` at the same >=960px breakpoint, and restyle it onto the dominant card surface with a top hairline and an upward shadow |

Purpose: close the last two deltas between the shipped Settings page and its validated typography/interaction target, so 06.6.4.1's closing checkpoint can sign off on the page as built rather than as intended.

Output: five retired constants and three new caption constants in `config_page.py`, one restructured `render()`, three CSS rule edits/additions in `style.css`, five in-place harness retargets, three new harness checks, and one real-browser live-process smoke test.

**Approach note — reuse, never invent.** Every value used here already exists in `companion/static/style.css` and is taken from the rule that established it: the 14px size from `.text-label`'s `var(--font-label-size)`; the muted strength from the `color-mix(in srgb, var(--color-text) 70%, transparent)` idiom `.data-table-wrap thead`, the filter-bar hint and the empty-state copy all already use; the card surface and hairline from `.page-section` / `.theme-status`; the shadow numbers from `--shadow-card-hover`'s own light-mode pair (only the y-offsets are negated, so a bottom-pinned bar's shadow projects up onto the content it covers rather than off-screen); and the content-column geometry from `.dashboard-shell`'s `grid-template-columns` plus `.dashboard-main`'s `max-width` / `margin` / `var(--space-3xl)` padding. Do not invent a new token, a new muted strength, a new spacing value, or a new radius.

**Non-goals.**
- Do NOT modify `companion/static/dirty-state.js`. Its three bar-related lookups are already document-wide `document.querySelector` calls (lines 48-50) and its cancel handler already calls `form.reset()` directly (line 177), so the bar keeps working unmodified as a sibling of the form. This was independently verified before planning; re-confirm by reading, do not "fix" it.
- Do NOT rename the "Theme" section heading.
- Do NOT touch `handle_post()`, the LED checkbox's `type`/`name`/`value`/`checked` attribute sequence, `LED_CHECKBOX_VALUE`, `DIRTY_SECTION_ATTR`, `STATIC_SAVE_FALLBACK_ATTR`, `DIRTY_BAR_INITIAL_TEXT`, or the always-rendered bottom `Save Settings` no-JS fallback button (which stays inside the form, still carrying `STATIC_SAVE_FALLBACK_ATTR`).
- Do NOT touch `.theme-status`, `.theme-status__row`, `.theme-swatch`, `.runway-row`, `.runway-card`, or `.led-checkbox` — all landed correctly by quick task 260901-qif.
- Do NOT add `z-index` to `.dirty-bar`. It is the last positioned element in document order and nothing it overlaps creates a competing stacking context, so a z-index would be an unearned magic number. If a future overlap appears, add it then with a stated reason.
- Do NOT add bottom padding to `.dashboard-main` to reserve space under the fixed bar. The bar is `hidden` unless the form is dirty, and dismissing it (Cancel) or saving restores the last ~14px of page content immediately. This is recorded as an accepted risk in the threat model below, not an oversight.
- Do NOT reintroduce a `.config-form` column rule in the >=960px block (06.6.4.1 D-01 removed it outright and its comment says so).

**Note on the sketch.** The validated typography sketch is not an on-disk artifact under `.planning/sketches/` (only 001-health, 002-mobile-nav, 003-history-density are). Its validated copy is therefore stated verbatim in Task 1 below and is the authority for this plan — do not reword it further, and do not "improve" it during execution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

@companion/pages/config_page.py
@companion/static/style.css
@companion/static/dirty-state.js
@companion/layout.py
@companion/test_config_page.py
</context>

<!--
Comment-text discipline: two literals below are negative-greped by acceptance
criteria AND must appear in the task actions, because in both cases the
instruction is "do not declare this property here" / "replace this declaration".
Both gates are region-scoped with awk to a single rule body, and in both cases
the CSS comment the action asks for sits ABOVE the selector — outside the awk
range — so neither gate is self-invalidating. Allowlisted deliberately:
-->
<!-- planner-discipline-allow: font-size -->
<!-- planner-discipline-allow: position: sticky -->

<tasks>

<task type="auto">
  <name>Task 1: Merge each settings group's two paragraphs into one muted section caption</name>
  <files>companion/pages/config_page.py, companion/static/style.css, companion/test_config_page.py</files>
  <read_first>
    - `companion/pages/config_page.py`'s module-level constant block (roughly lines 20-70): the five constants being retired and the comments above them, which state the D-02/D-04 rationale this task supersedes.
    - `theme_fieldset()` — BOTH branches. The single-theme read-only branch (`len(device_config.THEME_IDS) == 1`, the live one today) and the `>1`-theme radio-group fallback each emit their own pair of paragraphs, and both must be merged or the retired constants stay referenced.
    - `runway_fieldset()`'s return expression and its long docstring — the docstring names the description constant by name and describes the paragraph that follows the `.runway-row`, so it goes stale with this edit.
    - `led_group()`'s return expression and docstring. Note the docstring already claims the LED paragraph "renders directly under the heading here" while the markup actually places it AFTER the checkbox label — that stale sentence is the source of the position ambiguity; this task makes the markup match the claim.
    - `companion/static/style.css`'s "Typography roles" block (`.text-label`, `.text-body`, `.text-heading`) — `.text-label` already supplies `var(--font-label-size)`, so the new rule must add only colour. Also read the two LATER `.text-heading` rules (the serif-family one and the `h1, h2, h3, .text-heading` heading-rhythm one) so you anchor the insertion at the right one of the three.
    - Any one existing 70%-muted rule in `companion/static/style.css` (e.g. the `.data-table-wrap thead` rule, the filter-bar hint rule, or the empty-state copy rule) to copy the `color-mix(in srgb, var(--color-text) 70%, transparent)` idiom character-for-character. Note these `color` declarations carry no rgba fallback — the WR-02 fallback pattern in this file is scoped to `background`/`border` on the `button` rule only. Match that precedent: no fallback here.
    - `companion/test_config_page.py`'s three checks that reference the retired constants — the runway-row ordering check, the section-descriptions-appear-once check, and the helper-texts-appear-verbatim check — plus `EXPECTED_CHECK_COUNT` and the change-log comment block directly above it.
  </read_first>
  <action>
**A. `companion/pages/config_page.py` — retire five constants, add three.**

Delete the five module-level constants outright: the theme helper sentence, the theme section description, the runway helper sentence, the runway section description, and the LED helper sentence. Delete the comment blocks above them that only exist to justify them (the D-04 phase-number-leak note, the D-02 "one description sentence per settings group" note). Do not leave any of the five names behind as an alias, a re-export, or a commented-out line.

Add three new module-level constants in their place, one per group, carrying the validated copy VERBATIM. Do not reword, do not re-punctuate, do not expand contractions:

- Theme: `Panel colors for departing/arriving flights. More themes appear here once validated on real hardware.`
- Runway: `Which Orly runway the device watches. Applies on the next scheduled poll, not immediately.`
- Diagnostic LED: `Lit only during the device's brief wake window, not visible from the wall side. Applies on the next scheduled poll.`

Name them `THEME_SECTION_CAPTION`, `RUNWAY_SECTION_CAPTION`, `LED_SECTION_CAPTION` — one naming shape for all three, matching the `.section-caption` class they render into. Write one shared comment above the trio explaining the merge: each settings group used to render a description sentence above its control and a helper sentence below it; both now collapse into a single caption rendered once, directly under the group heading and before the control. Note that the LED group is the odd one out only in that it never had a pair — its single paragraph is reworded and relocated, not merged.

**B. `theme_fieldset()` — one caption, both branches.**

Single-theme read-only branch: keep the wrapper `<div class="theme-status" data-dirty-section="Theme">` and the `<h2 class="text-heading">Theme</h2>` heading exactly as they are. Replace the `<p class="text-body">` that currently sits between the heading and `.theme-status__row` with `<p class="text-label section-caption">` carrying `THEME_SECTION_CAPTION` (still routed through `escape_html`, same as every other string this module emits). Delete the trailing `<p class="text-label">` that currently follows `.theme-status__row`, and drop its argument from the interpolation tuple. The branch must end up emitting exactly one `<p` element.

Multi-theme radio-group fallback branch (dead today, live the moment a second theme is registered): same merge. Keep the `<fieldset>`/`<legend>Theme</legend>` shape it already has — this branch is deliberately different from the read-only branch and is not in scope to restructure. Replace its `<p class="text-body">` description with the same `<p class="text-label section-caption">` caption, and delete its trailing `<p class="text-label">` helper. This branch must also end up emitting exactly one `<p` element. Missing this branch leaves the retired constants referenced and the module will not import.

**C. `runway_fieldset()` — one caption.**

Replace the `<p class="text-body">` between the `<h2 class="text-heading">Runway</h2>` heading and `<div class="runway-row">` with `<p class="text-label section-caption">` carrying `RUNWAY_SECTION_CAPTION`. Delete the `<p class="text-label">` that currently follows the closing `</div>` of `.runway-row`, and drop its argument from the interpolation tuple. The group's element order becomes: heading, caption, `.runway-row`, close wrapper — nothing after the row.

Rewrite the paragraph of the docstring that describes the old two-paragraph shape (the passage explaining that the helper renders once after the card list, and the passage describing the row as sitting "between the description paragraph and the helper paragraph"). It must describe the shipped shape: a single caption under the heading, then the row, then nothing. Leave every other paragraph of that docstring alone — the D-01 single-top-level-element rationale, the `images_available` contract, and the `<h2>`-not-`<legend>` reasoning are all still true and still load-bearing.

**D. `led_group()` — restyle and relocate the single caption.**

Move the paragraph from after the `<label class="led-checkbox">` to directly after the `<h2 class="text-heading">` heading, and change it from `<p class="text-label">` to `<p class="text-label section-caption">` carrying `LED_SECTION_CAPTION`. Element order becomes: heading, caption, label. The `<label class="led-checkbox">` and its `<input>`'s `type`/`name`/`value`/`checked` attribute sequence are untouched, byte for byte.

Rewrite the docstring paragraph that explains why this group needs no separate description sentence. It currently asserts the LED paragraph "already renders directly under the heading here" — that was never true of the markup, and it is the reason this position drift went unnoticed. Replace it with a statement of what is now actually shipped: one caption, under the heading, above the control, styled identically to Theme's and Runway's.

**E. `companion/static/style.css` — add `.section-caption`.**

Add one rule declaring only the colour. `.text-heading` appears in THREE separate rules in this file — anchor precisely: place the new rule immediately after the FIRST one, the `.text-heading` in the "Typography roles" block that declares `font-size: var(--font-heading-size)`. NOT after the serif-family rule further down, and NOT after the `h1, h2, h3, .text-heading` heading-rhythm rule. This keeps the four-size typography-role group contiguous with its modifier directly following it.

`.section-caption` sets `color` to the file's existing `color-mix(in srgb, var(--color-text) 70%, transparent)` value. Nothing else — no font-size, no font-weight, no line-height, and deliberately no margin.

Write a head comment above it stating three things: (1) this is the single muted sentence now sitting directly under each Settings group heading, replacing the old description-above / helper-below pair; (2) it deliberately COMPOSES with `.text-label` rather than restating it, because `.text-label` already supplies `var(--font-label-size)`, so the markup always carries both classes together — and the 70% strength is the one this file already uses for secondary copy, not a new one; (3) the absence of a `margin` declaration is a decision, not an omission — unlike the `h1, h2, h3, .text-heading` heading-rhythm rule above, this role keeps the UA default paragraph margin, because the description and helper paragraphs it replaces already relied on that default and the validated sketch was measured against it. Do not "complete" this rule by adding a margin.

**F. `companion/test_config_page.py` — three in-place retargets, no count change.**

Retarget these three pre-existing checks onto the new markup shape, keeping the total unchanged (this file's own established discipline — see the change-log comment above `EXPECTED_CHECK_COUNT`). Do not add or delete any check in this task.

1. The runway-row containment/ordering check: it currently locates the runway helper text and asserts it renders AFTER `.runway-row` closes. Invert it — assert `RUNWAY_SECTION_CAPTION` renders BEFORE `<div class="runway-row">` opens, and additionally assert that no `<p` element appears anywhere after the row closes inside the wrapper (which is the actual proof the second paragraph is gone, not merely moved). Its three-cards-inside-the-row assertions are untouched. Update its `check(...)` description string to match.

2. The section-descriptions-appear-exactly-once check: retarget onto `THEME_SECTION_CAPTION` and `RUNWAY_SECTION_CAPTION`, keeping the exactly-once assertion for both. Update its description string.

3. The helper-texts-appear-escaped-verbatim check: retarget onto all three caption constants, and strengthen "is present" to "appears exactly once" for each. Update its description string.

Also update the change-log comment block above `EXPECTED_CHECK_COUNT` with a `quick task 260901-re6:` line recording that these three were retargeted in place with no count change (Task 3 of this plan will append the count bump to the same block).

Leave `EXPECTED_CHECK_COUNT`'s value untouched in this task. Leave the four-groups-named-once check alone — its `'<p class="text-label">Theme</p>'` negative assertion still holds under the new class pair.
  </action>
  <verify>
    <automated>test "$(grep -c 'section-caption' companion/pages/config_page.py)" = 3 && test "$(grep -cE '^(THEME|RUNWAY|LED)_(HELPER_TEXT|SECTION_DESCRIPTION|SECTION_CAPTION) = ' companion/pages/config_page.py)" = 3 && test "$(grep -cE 'escape_html\((THEME|RUNWAY|LED)_(HELPER_TEXT|SECTION_DESCRIPTION)\)' companion/pages/config_page.py)" = 0 && grep -q '^\.section-caption {' companion/static/style.css && test "$(awk '/^\.section-caption \{/,/^\}/' companion/static/style.css | grep -c 'color-mix(in srgb, var(--color-text) 70%, transparent)')" = 1 && test "$(awk '/^\.section-caption \{/,/^\}/' companion/static/style.css | grep -c 'font-size')" = 0 && server/.venv/bin/python3 companion/test_config_page.py</automated>
    <human-check>Load `/settings` in a browser and confirm each of Theme, Runway and Diagnostic LED shows exactly one sentence of grey caption text directly under its heading and above its control, at noticeably smaller and lighter type than the headings, with no second sentence below any control.</human-check>
  </verify>
  <done>
`companion/pages/config_page.py` defines exactly three `*_SECTION_CAPTION` constants carrying the validated copy verbatim and zero `*_HELPER_TEXT`/`*_SECTION_DESCRIPTION` constants; `theme_fieldset()` (both branches), `runway_fieldset()` and `led_group()` each emit exactly one `<p class="text-label section-caption">` element positioned between the group heading and the group control, with the retired second paragraph and its interpolation argument removed and the affected docstrings rewritten to describe the shipped shape; `companion/static/style.css` declares a colour-only `.section-caption` rule using the file's existing 70% `color-mix` idiom, placed after `.text-heading`; the three affected harness checks are retargeted in place; and `server/.venv/bin/python3 companion/test_config_page.py` passes at its unchanged `EXPECTED_CHECK_COUNT`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the save bar outside the form and restyle it as a fixed, page-width, raised bar</name>
  <files>companion/pages/config_page.py, companion/static/style.css, companion/test_config_page.py</files>
  <read_first>
    - `companion/static/dirty-state.js` lines 43-53 and 175-180. Confirm before changing anything: `bar`, `countEl` and `cancelBtn` are resolved with `document.querySelector`, not `form.querySelector`, and the cancel handler calls `form.reset()` on the separately-resolved form. Also note `dirtySectionLabels()` DOES use `form.querySelectorAll("[data-dirty-section]")` — the three group wrappers stay inside the form, so that walk is unaffected. This read is what licenses leaving the JS file untouched.
    - `companion/pages/config_page.py`'s `render()` — the `dirty_bar_html` assignment, the long comment above it (which explicitly asserts the bar is "a genuine descendant of this `<form>` ... never a sibling, so its own `<button type="submit">` submits natively via normal DOM nesting, no `form=` attribute needed"; that premise is exactly what this task inverts), and the final return expression's element order.
    - `companion/pages/config_page.py`'s `RUNWAY_IMAGE_ROUTE_PREFIX` / `SETTINGS_ROUTE` comments — the module's stated one-definition-site discipline for literals read in more than one place, which `SETTINGS_FORM_ID` must follow.
    - `companion/static/style.css`'s `.dirty-bar` base rule, its head comment, `.dirty-bar[hidden]`, `.dirty-bar__cancel`, and the `.dirty-bar` rule inside the `@media (min-width: 960px)` block plus its comment.
    - `companion/static/style.css`'s `.dashboard-shell` rule (`grid-template-columns`, `column-gap`) and `.dashboard-main` rule (`max-width`, `margin`, `padding`) inside that same `@media` block — these supply the exact content-column geometry the fixed bar must reproduce.
    - `companion/static/style.css`'s `--shadow-card-hover` definitions in `:root` (the light-mode pair is the one whose numbers get mirrored) and the `button[type="submit"]` comment documenting this file's precedent for theme-neutral black-alpha shadow literals that need no dark-mode variant.
    - `companion/test_config_page.py`'s form-class-hook check (its `expected_tag` is an exact full-tag string that adding an `id` will break) and its dirty-bar-nested-inside-form check (whose entire assertion set inverts).
  </read_first>
  <action>
**A. `companion/pages/config_page.py` — one new constant.**

Add a module-level `SETTINGS_FORM_ID = "settings-form"` alongside `SETTINGS_ROUTE`, with a comment following the module's existing one-definition-site pattern: this value is interpolated twice — once as the settings form's `id`, once as the dirty-bar save button's `form` attribute — and the two must never be re-typed as literals, because a mismatch produces a Save button that looks correct in markup and silently submits nothing.

**B. `render()` — restructure.**

1. Give the form an `id`, placed immediately after its `class` attribute so the tag reads `<form class="config-form" id="{SETTINGS_FORM_ID}" data-dirty-form method="post" action="{SETTINGS_ROUTE}">`. The value comes from the constant, interpolated — never typed as a literal in the tag string.

2. Give the dirty bar's save button a `form` attribute sourced from the same constant, so it reads `<button type="submit" class="dirty-bar__save" form="{SETTINGS_FORM_ID}">Save settings</button>`. Leave the cancel button alone: it is `type="button"` and is driven entirely by `dirty-state.js`'s explicit `form.reset()` call, so a form association would be inert noise.

3. Move `dirty_bar_html` out of the form. In the return expression, the bar's interpolation slot moves from between the LED group and the bottom Save Settings button to the very end — after `</form>` closes AND after the Poll `</section>` closes. The bar becomes the last element `render()` returns. Reorder the interpolation tuple to match. The always-rendered bottom Save Settings button stays inside the form, still carrying `STATIC_SAVE_FALLBACK_ATTR`, in its current position.

4. Rewrite the comment above `dirty_bar_html`. Its current text asserts the bar is a genuine form descendant and that no `form=` attribute is needed. Replace that with the real reason for the move: `position: sticky` resolves against the nearest scrolling ancestor's box, which was the short three-section form, so the bar detached from the viewport bottom and visibly stopped above the Poll section on a page much taller than the form. State that the bar is now a sibling emitted last on the page; that `companion/static/dirty-state.js` needs no change because its `[data-dirty-bar]` / `[data-dirty-count]` / `[data-dirty-cancel]` lookups are document-wide and its cancel handler calls `form.reset()` directly; and that the save button's `form` attribute is what preserves native submission of the merged settings form. Add the standing warning that narrowing any of those three JS lookups to a form-scoped query would silently break the bar.

**C. `companion/static/style.css` — restyle the `.dirty-bar` base rule.**

Edit the base rule (the column-0 one, outside any media query) in place. Keep `display: flex`, `align-items: center`, `gap: var(--space-md)`, `padding: var(--space-md)`, `border-radius: var(--radius-card)` and `margin-top: var(--space-md)` — these govern the sub-960px in-flow form of the bar, which is explicitly unchanged.

Change the surface from the muted `--color-secondary` to `var(--color-dominant)`, the same card surface every other card on the page uses — this is the whole point: the bar currently reads as a secondary, receding strip when it should read as the raised action surface. Add `border-top: 1px solid var(--color-border)`. Add an UPWARD `box-shadow` whose numbers are `--shadow-card-hover`'s own light-mode pair with the y-offsets negated: `0 -4px 12px rgba(18, 21, 27, 0.08), 0 -2px 4px rgba(18, 21, 27, 0.05)`.

Comment the shadow: a bar pinned to the viewport bottom is overlapped by content ABOVE it, so a downward shadow would fall off-screen and the bar would read as flat; the direction is the only thing changed from the token's own values. State that it is a literal rather than a new custom property because no upward-shadow token exists and adding one would need four definition sites (`:root`, the `prefers-color-scheme: dark` block, and both `[data-ui-theme]` blocks) for a single consumer; the black-alpha literal follows this file's documented `button[type="submit"]` precedent for theme-neutral shadows, and the top hairline (a real theme token) is what carries the separation in dark mode where a black shadow is near-invisible.

Rewrite the rule's head comment: it currently describes the bar as full-width/non-sticky below 960px with "the >=960px sticky variant" added below. Say fixed, not sticky, and say why (see part D).

`.dirty-bar[hidden]`, `.dirty-bar__cancel` and the `button:hover` rule that follows them are untouched — their source-order relationships are load-bearing and documented in place.

**D. `companion/static/style.css` — replace the >=960px `.dirty-bar` rule.**

Inside the `@media (min-width: 960px)` block, replace the two-declaration sticky rule with a fixed one that reproduces the page content column:

- `position: fixed` and `bottom: 0` — immune to any container-height issue, unlike sticky, and the genuine pinned-to-viewport behaviour intended.
- `right: 0`.
- `left: calc(240px + var(--space-xl))` — the sidebar grid track plus the shell's column gap, i.e. exactly where `.dashboard-main`'s own track starts. The region to the left of that is opaque sidebar and empty gutter, so nothing scrolls through it.
- `max-width: min(1440px, 100%)` and `margin: 0 auto`, copied verbatim from `.dashboard-main`. With `left` and `right` both set and `width` auto, an over-constrained box with auto inline margins centres itself once `max-width` clamps it — which reproduces `.dashboard-main`'s own centring at every width. The `margin` shorthand also zeroes the base rule's `margin-top`, which is meaningless for a fixed box.
- `padding: var(--space-md) var(--space-3xl)` — vertical unchanged from the base rule, horizontal matched to `.dashboard-main`'s own `var(--space-3xl)` gutter so the bar's controls line up with the content above them instead of reading as a full-bleed strip.
- `border-radius: 0` — the bar is flush to the viewport bottom edge at this breakpoint; the base rule's card radius belongs to the in-flow sub-960px form only.

Comment the `240px` literal explicitly as a duplicated-not-imported must-equal value: it has to stay equal to `.dashboard-shell`'s `grid-template-columns` first track and the `var(--space-xl)` addend equal to its `column-gap`, and there is no token for either, so the pair is guarded by a harness check instead. Comment the deliberate absence of `z-index` and why (last positioned element in document order, no competing stacking context).

**E. `companion/test_config_page.py` — two in-place retargets, no count change.**

1. The form-class-hook check: its `expected_tag` full-tag string must gain the `id` attribute, built by interpolating BOTH `config_page.SETTINGS_FORM_ID` and `config_page.SETTINGS_ROUTE` — never re-typing either literal, matching what that check already does for the route. Its `class="config-form"` presence assertion and its exactly-one-form count assertion are unchanged.

2. The dirty-bar-nested-inside-form check: invert it wholesale and rename the function and its description to say the bar is a SIBLING of the form. New assertions: the `</form>` closing tag exists; `data-dirty-bar` appears in the rendered output AFTER that closing tag, not before it; `data-dirty-bar` appears after the Poll section's `<h2 class="text-heading">Poll</h2>` heading too, so the bar is genuinely last on the page and not merely outside the form; the bottom `Save Settings` fallback button still appears INSIDE the form segment; and the bar's own save button carries `form="{config_page.SETTINGS_FORM_ID}"` (interpolated from the constant). Keep the exactly-one-form assertion.

Append to the change-log comment block above `EXPECTED_CHECK_COUNT` that these two were retargeted in place with no count change. Leave `EXPECTED_CHECK_COUNT`'s value untouched in this task.
  </action>
  <verify>
    <automated>test "$(awk '/^\.dirty-bar \{/,/^\}/' companion/static/style.css | grep -cE 'background: var\(--color-dominant\);|border-top: 1px solid var\(--color-border\);')" = 2 && test "$(awk '/^\.dirty-bar \{/,/^\}/' companion/static/style.css | grep -c 'box-shadow: 0 -')" = 1 && test "$(awk '/^  \.dirty-bar \{/,/^  \}/' companion/static/style.css | grep -cE 'position: fixed;|bottom: 0;|right: 0;|max-width: min\(1440px, 100%\);')" = 4 && test "$(awk '/^  \.dirty-bar \{/,/^  \}/' companion/static/style.css | grep -c 'position: sticky')" = 0 && test "$(grep -c 'SETTINGS_FORM_ID' companion/pages/config_page.py)" -ge 3 && server/.venv/bin/python3 companion/test_config_page.py</automated>
    <human-check>Deferred to Task 3's live-browser smoke test, which is the only check that can prove the moved submit button actually submits.</human-check>
  </verify>
  <done>
`companion/pages/config_page.py` defines `SETTINGS_FORM_ID` once and interpolates it into both the form's `id` and the dirty-bar save button's `form` attribute; `render()` emits the dirty bar as the last element on the page, after both `</form>` and the Poll section; `companion/static/style.css`'s base `.dirty-bar` rule uses `var(--color-dominant)`, a `--color-border` top hairline and a present upward box-shadow, and its >=960px rule is `position: fixed` with the content-column geometry copied from `.dashboard-shell`/`.dashboard-main` and zero remaining `position: sticky`; `companion/static/dirty-state.js` is byte-identical to before this task; the two affected harness checks are retargeted in place; and `server/.venv/bin/python3 companion/test_config_page.py` passes at its unchanged `EXPECTED_CHECK_COUNT`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add three guard checks, prove the moved Save button really submits in a real browser, and clear the full suite</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - `companion/test_config_page.py`'s `EXPECTED_CHECK_COUNT` and the full change-log comment block above it — read the REAL on-disk value at execution time and compute the new value from it. Do not take any number from this plan text.
    - The existing cross-file DOM-contract guard that asserts style.css declares `.theme-status` / `.runway-row` / `.led-checkbox` — the new CSS guard must follow its `_read_static` + index-plus-window technique exactly, never a regex CSS parser.
    - The `Harness` class (subprocess lifecycle, `_pick_free_port`, `state_path`), `TEST_PASSWORD`, `_login`, and `auth.PASSWORD_ENV_VAR` — the live-process pattern the smoke test reuses for its own out-of-harness app instance.
    - `server/device_config.py`'s `RUNWAY_IDS`, `DEFAULT_RUNWAY_ID` and `DEFAULT_LED_ENABLED`, so the smoke test picks a genuinely different runway rather than re-saving the current one.
  </read_first>
  <action>
**A. Three new harness checks (+3, no more, no fewer).**

Add these to `companion/test_config_page.py`, then set `EXPECTED_CHECK_COUNT` to the real on-disk baseline you read plus exactly 3. Record the bump in the change-log comment block with a `quick task 260901-re6:` line that states the baseline you actually observed and names all three additions.

1. **One-caption-per-group.** Call `theme_fieldset()`, `runway_fieldset()` and `led_group()` directly and assert each returns markup containing exactly one `<p` occurrence and exactly one `section-caption` occurrence. Also assert, for each, that the caption's index falls after the group's `</h2>` and before the group's control (`theme-status__row` / `runway-row` / `led-checkbox` respectively). This is the direct proof of the merge and of the position — the check that would have caught this bug.

2. **Retired-symbol source assertion.** Assert `config_page` exposes none of the five retired constant names, using `hasattr` against the imported module. Follow the precedent 06.6.4.1-07 set with its own retired-symbol check.

3. **Cross-file CSS DOM contract.** Using `_read_static("style.css")`, assert: (a) a `.section-caption {` rule exists and its body carries `color-mix(in srgb, var(--color-text) 70%, transparent)`; (b) the base `.dirty-bar {` rule's body carries `var(--color-dominant)` and `border-top:`, and does NOT carry `var(--color-secondary)`; (c) a `.dirty-bar` rule inside the >=960px block carries `position: fixed` and that the string `position: sticky` does not appear anywhere in any `.dirty-bar` rule body; and (d) the `240px` literal the fixed rule's `left` uses still equals `.dashboard-shell`'s `grid-template-columns` first track — assert both `grid-template-columns: 240px` and `calc(240px + var(--space-xl))` are present, so a future sidebar-width change that misses one of the two sites fails here.

**B. Live-process real-browser smoke test (the load-bearing one).**

A `form=` association on a submit button moved outside its form is exactly the change that looks correct in markup and silently fails at runtime, and no stdlib-only Python harness can exercise it — only a real browser can. Run this by hand, once, and record the evidence in the SUMMARY.

1. Create a throwaway state dir and start the real app on a free port:
   `SKYPANE_COMPANION_PASSWORD=<any> server/.venv/bin/python3 companion/app.py --port <free> --state-dir <tmpdir>`
2. Drive a real browser with the `chrome-devtools` MCP skill (`Skill("chrome-devtools-mcp:chrome-devtools")`). Set the viewport to at least 1280x800 so the >=960px fixed branch and the `.js [data-static-save-fallback]` hide-rule are both active — at that width the bar's Save button is the ONLY visible save control, which is precisely what must be proven.
3. Navigate to `/settings`, follow the login redirect, sign in with the password from step 1, and land back on `/settings`.
4. Read the on-disk `device_config.json` BEFORE interacting, and note its `tracked_runway` and `led_enabled` values.
5. In the browser: click a runway card that is NOT the currently selected one, and toggle the Diagnostic LED checkbox to the opposite of its current state. Two fields from two different groups, both dirty. Confirm the bar appears and its copy names both groups (`dirty-state.js`'s section-aware text should read "Runway and Diagnostic LED changed").
6. Screenshot with the page scrolled to the TOP and again scrolled to the very BOTTOM. Both must show the bar still pinned to the viewport bottom — the bottom-scrolled shot is the direct regression evidence, since that is exactly where the sticky bar used to detach and stop above the Poll section.
7. Click the bar's "Save settings" button. Confirm the browser navigates to `/settings?flash=saved` and the confirmation banner renders.
8. Re-read `device_config.json` and assert BOTH values changed, scripted rather than eyeballed:
   `server/.venv/bin/python3 -c "import json,sys; d=json.load(open(sys.argv[1])); print(d)" <tmpdir>/device_config.json`
   Compare against the step-4 values. If only one changed, or neither, the `form=` association is broken — stop and fix, do not proceed.
9. Stop the app process and delete the throwaway state dir.

Record in the SUMMARY: the two before/after `device_config.json` values, the observed bar copy, and a one-line confirmation for each of — bar pinned at top-scroll, bar pinned at bottom-scroll, bar visibly raised off the page (white surface, top hairline, upward shadow) rather than blending in, and the bar's left/right edges aligned with the content column rather than full-bleed.

**C. Full suite.**

Run `scripts/run-all-tests.sh` and confirm the only failing harness is the known, pre-existing, unrelated `server/test_poll_loop.py` digest mismatch, and that no coverage-threshold failure is reported. No harness that passed before this plan may fail after it.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py && LOG="${TMPDIR:-/tmp}/re6-all-tests.log"; scripts/run-all-tests.sh > "$LOG" 2>&1; grep -E '^    - ' "$LOG" | grep -qv 'server/test_poll_loop\.py' && { echo "UNEXPECTED FAILING HARNESS:"; grep -E '^    - ' "$LOG"; exit 1; }; grep -q 'Coverage failure' "$LOG" && { echo "COVERAGE THRESHOLD REGRESSED"; exit 1; }; echo "SUITE OK (only the known poll_loop failure, if any)"</automated>
    <human-check>The live-browser smoke test in part B: with two fields dirtied at a >=960px viewport, clicking the bar's Save settings button navigates to `/settings?flash=saved` AND the on-disk `device_config.json` shows BOTH the new runway and the new LED value. The bar stays pinned to the viewport bottom when scrolled to the very bottom of the page, reads as a raised white surface with a top hairline and a visible upward shadow, and its edges align with the content column rather than running full-bleed.</human-check>
  </verify>
  <done>
`companion/test_config_page.py` carries exactly 3 new checks (one-caption-per-group with position assertions, retired-symbol source assertion, and the cross-file CSS DOM contract covering `.section-caption`, the restyled `.dirty-bar`, the fixed-not-sticky positioning and the `240px` must-equal pair), `EXPECTED_CHECK_COUNT` equals the real on-disk baseline plus exactly 3 with the bump recorded in the change-log comment, the harness passes, the live-browser smoke test proved a single POST from the out-of-form Save button persisted BOTH dirtied fields with the before/after values recorded in the SUMMARY, and `scripts/run-all-tests.sh` shows no failing harness other than the known `server/test_poll_loop.py` digest mismatch and no coverage-threshold failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → companion HTTP (`POST /settings`) | Untrusted form data crosses here; the session cookie gate in `companion/app.py` and `config_page.handle_post()`'s registry validation are the two controls |
| `config_page` module constants → rendered HTML | Copy strings cross into a markup sink; `layout.escape_html()` is the control |
| `config_page.py` ⇄ `style.css` / `dirty-state.js` | Class names and data attributes duplicated by hand across three files that never import one another; harness guards are the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-re6-01 | Tampering | The three new `*_SECTION_CAPTION` constants rendered by `theme_fieldset()`/`runway_fieldset()`/`led_group()` | low | mitigate | Every caption is routed through `layout.escape_html()` exactly as the five constants it replaces were; all three are module-level literals never derived from request data, a session, or the filesystem. Task 1's retargeted "appears exactly once, escaped-verbatim" harness check pins this. |
| T-re6-02 | Elevation of Privilege | The dirty-bar save button's cross-DOM `form="settings-form"` association | low | mitigate | The association changes only which DOM node owns the submit; it introduces no new route, no new field, no client-side serialization, and no JavaScript. `handle_post()` is untouched and remains the sole server-side boundary — membership tests against `THEME_IDS`/`RUNWAY_IDS`, exact equality against `LED_CHECKBOX_VALUE`, all-or-nothing rejection before any `save_device_config()` call. The pre-existing live-HTTP "unauthenticated POST /settings redirects to /login and writes nothing" check must stay green (Task 3 part C). |
| T-re6-03 | Denial of Service | The `position: fixed` bar overlaying the bottom of the page at >=960px | low | accept | The bar is `hidden` unless the form is dirty, so it never occludes anything during normal reading. When dirty it can cover roughly the last 14px of content past `.dashboard-main`'s 48px bottom padding; Cancel dismisses it and Save removes it. Reserving permanent bottom padding for a conditionally-visible bar would cost every page-load a visible gap to protect an edge case. Recorded as a Non-goal above, not an oversight. |
| T-re6-04 | Tampering | Hand-duplicated literals across `config_page.py`, `style.css` and `dirty-state.js` (`section-caption`, `settings-form`, `240px`, `data-dirty-bar`) | medium | mitigate | `SETTINGS_FORM_ID` is defined once and interpolated twice (a mismatch would produce a Save button that submits nothing — the exact silent-failure mode this task risks). Task 3's cross-file CSS guard pins `.section-caption`, the `.dirty-bar` surface/positioning, and the `240px` ⇄ `grid-template-columns` pair. Task 3's live-browser smoke test is the runtime backstop that a source-level guard cannot provide. |
| T-re6-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no dependency of any kind. All three files edited are already in the repository; `companion/` remains stdlib-only. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_config_page.py` passes after EACH task (Task 1 and Task 2 at the unchanged on-disk `EXPECTED_CHECK_COUNT`; Task 3 at baseline + 3).
2. `companion/static/dirty-state.js` shows zero diff for the whole plan (`git diff --stat companion/static/dirty-state.js` is empty).
3. The live-browser smoke test in Task 3 part B persisted BOTH dirtied fields in one POST from the out-of-form Save button, with the before/after `device_config.json` values recorded in the SUMMARY.
4. `scripts/run-all-tests.sh` reports no failing harness other than the known, pre-existing, unrelated `server/test_poll_loop.py` digest mismatch, and no coverage-threshold failure.
</verification>

<success_criteria>
- Theme, Runway and Diagnostic LED each render exactly one caption paragraph, under the heading and above the control, carrying the validated copy verbatim at 14px and the file's existing 70% muted text strength.
- All five superseded copy constants and their second-paragraph markup are gone, with no stale docstring or comment describing markup that no longer exists.
- The unified save bar is the last element on the page, outside the form, submits the merged settings form natively via a `form=` attribute proven at runtime in a real browser, stays pinned to the viewport bottom at >=960px no matter how far the page is scrolled, and visibly reads as a raised white surface with a top hairline and an upward shadow aligned to the page content column.
- `companion/static/dirty-state.js` is unmodified.
- `companion/test_config_page.py` passes at its real on-disk baseline plus exactly 3, and `scripts/run-all-tests.sh` shows only the known `server/test_poll_loop.py` failure.
</success_criteria>

<output>
Create `.planning/quick/260901-re6-fix-2-more-confirmed-issues-on-the-compa/260901-re6-SUMMARY.md` when done.
</output>
