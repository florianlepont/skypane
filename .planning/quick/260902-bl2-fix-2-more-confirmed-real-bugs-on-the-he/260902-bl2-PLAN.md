---
phase: quick-260902-bl2
plan: 260902-bl2
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - companion/layout.py
  - companion/pages/health_page.py
  - companion/static/style.css
  - companion/test_status_pages.py
autonomous: true
requirements: [QUICK-260902-bl2]

must_haves:
  truths:
    - "Bug 1's root cause is that `layout.data_table()` has no column-role class hook of any kind for anything but monospace: its cell loop emits `' class=\"mono\"' if column_index in mono_columns else ''`, a single-class ternary, so every Description `<td>` in the Resolution-statistics table ships with NO class attribute at all and inherits full-strength `--color-text`. The live `getComputedStyle()` reading of `rgb(23, 25, 31)` with `classList: []` is exactly what that ternary produces — there is no CSS rule to blame, because there is no element hook for one to target."
    - "The validated sketch answers this with `table.data-table td.desc { color: var(--color-text-muted); min-width: 220px; }` (cached artifact line 109). Exactly HALF of that rule is carried over. The colour half is carried, translated to this stylesheet's own muted idiom. The `min-width: 220px` half is deliberately NOT carried: quick task 260901-uzi's `.data-table--prose` comment already argued, in this same file, that a fixed pixel floor on this exact column re-creates the horizontal overflow it had just removed one breakpoint down. Carrying only half of a sketch rule is the decision here, and it must be written down as one."
    - "The muted value is this stylesheet's existing 70% strength, `color-mix(in srgb, var(--color-text) 70%, transparent)` — the same literal `.section-caption`, `.data-table th`, `.filter-bar__count`, `.empty-state`'s copy and `.battery-readout__detail` already carry. The sketch's `--color-text-muted` token is NOT introduced: style.css's own `.cell-primary`/`.cell-secondary` comment states in writing that no such token exists in this file and that the sketch's pattern naming one is deliberately not carried over. That comment is a standing decision this task obeys rather than reopens."
    - "The property is `color`, not `opacity`. `.cell-secondary` and `.cell-inline-sep` use `opacity: 0.7` for the same 70% read, but a `<td>` is not a bare span: `.data-table td` declares `border-bottom: 1px solid var(--color-border)`, and `opacity` would fade that hairline along with the text, breaking the table's own row separators on exactly four rows and nowhere else. Same strength, different property, for a concrete reason."
    - "The mechanism is a new optional `desc_columns=()` keyword on `data_table()`, the direct sibling of the `mono_columns` keyword that already does precisely this job for a different column role — same zero-based-index shape, same default-inert contract, same 'adds a class, changes no content and no escaping' boundary. Scoping is carried by the opt-in, exactly as `.mono`'s is: History and the unresolved-prefix registry hand-roll their own tables and never call this builder, and the battery readings table is the only other production caller and does not pass the new keyword, so their cells cannot change colour."
    - "The cell-class construction changes from a single-class ternary to a joined class list, which is what lets one cell hold both roles. `mono` is emitted first so the existing single-role output is byte-identical for every current caller — `test_view_pages.py::_mono_columns_present()` asserts the merged-cell mono output and must stay green with no edit."
    - "Bug 2's root cause is (b) from the report, confirmed against source: nothing in this stylesheet has ever governed the gap between a card heading and its first real content, so that gap is whatever the composition of one stated margin and one UNSTATED UA default happens to produce. Resolution-statistics puts a `<div class=\"data-table-wrap\">` (no margin at all) directly under its `<h2>`, so the gap is exactly the heading-rhythm rule's own `margin: 0 0 var(--space-sm)` = 8px. Unresolved-prefixes puts two `<p class=\"text-body\">` elements there, and `.text-body` declares size/weight/line-height but NO margin, so each paragraph falls through to the UA `margin-block: 1em` this file never states. The 8px number is fully explained by source; the two cards differ because one is followed by a div and the other by UA-margined paragraphs."
    - "The fix adopts the sketch's own two spacing declarations rather than inventing a middle value. The sketch's card-title role, `.wide-card__caption`, declares `margin: 0 0 var(--space-md)` — 16px under every card title, unconditionally, in BOTH cards (cached artifact line 90). The sketch's intervening prose role, `.read-only-note`, declares `margin: 0 0 var(--space-sm)` — zero top, 8px bottom (line 110). Those two values, applied to the demoted nested-card heading tier and to the direct-child `p.text-body` elements under it, are the whole fix."
    - "That composition is deterministic, not coincidental: with the heading owning 16px below and every direct-child paragraph owning zero above, the heading-to-first-element gap is 16px in all three nested cards no matter which element type follows, because every OTHER element type that can follow a heading in these cards (`div.data-table-wrap`, `div.filter-bar`, `div.empty-state`, `p.battery-readout`, `svg`, `details`) already carries no top margin. A `.page-section--nested > h2 + * { margin-top: 0 }` catch-all is therefore not added — but a markup check pins the allowlist, so a future element that DOES carry a UA top margin fails the harness instead of silently reintroducing the inconsistency."
    - "The rhythm rule reaches three cards, not two: `.battery-trend-section` renders the identical `<h2>` then `<p class=\"text-body\">` dot-label shape, and its heading is already in the demotion rule's own selector list from 260901-uzi. Fixing two of three instances of one pattern would ship a new inconsistency to close two old ones."
    - "The `> p.text-body` scoping is exact, verified against a seeded render: the only direct-child `p.text-body` elements under these three cards are the registry's Coverage dot line, the registry's read-only note, and the battery card's Battery-readings dot line — three elements on the whole page. `.battery-readout` is `<p id=\"battery-readout\" class=\"battery-readout\" role=\"status\">` and carries no `text-body` class, so the rule cannot reach it and cannot silently override its own `margin: 0 0 var(--space-sm)`; the `.empty-state` paragraphs are not siblings of any `<h2>` and are unreachable through a child combinator."
    - "The Coverage status dot is NOT moved into the heading row, even though the sketch puts it there (`.wide-card__caption` is a space-between flex row holding the title and the status dot). That is a structural change to three cards, a new flex heading treatment and a set of harness retargets, for a complaint that was about spacing — it is recorded as a considered rejection with its sketch line reference so a later task can pick it up deliberately, not discovered as an omission."
    - "The existing `_nested_heading_tier_demoted_to_emphasis_role()` check reads the demotion rule's declaration block and fails if the literal `font-family` appears anywhere inside it. The new `margin-bottom` declaration is safe, but every word of new reasoning must live in the comment ABOVE the rule, outside the braces, as this file's whole comment style already does — a comment moved inside the block would fail a check that has nothing to do with margins."
    - "`companion/test_status_pages.py` passes with `EXPECTED_CHECK_COUNT` moved from its real on-disk baseline to that baseline plus exactly 2, with the live-HTTP check extended IN PLACE, and with no existing check deleted."
    - "`scripts/run-all-tests.sh` reports exactly one failing harness, `server/test_poll_loop.py` (the known, pre-existing, unrelated digest mismatch). No harness that passed before this task fails after it."
    - "A real `companion/app.py` process was started, signed into, and both `GET /health` AND `GET /static/style.css` fetched over HTTP, confirming that the new `desc` cells reach a genuine response body and that the two new stylesheet rules reach a genuine served asset — not only an in-process `render()` call and an on-disk file read."
  artifacts:
    - path: "companion/layout.py"
      provides: "`data_table()`'s optional `desc_columns=()` keyword, the joined cell-class list that lets one cell carry both roles, and the docstring paragraph placing it beside `mono_columns` as the same kind of column-role hook"
      contains: "desc_columns"
    - path: "companion/static/style.css"
      provides: "`.data-table td.desc` carrying only the colour half of the sketch's rule; `margin-bottom: var(--space-md)` added to the nested-card heading tier; `.page-section--nested > p.text-body, .battery-trend-section > p.text-body` carrying the sketch's read-only-note rhythm"
      contains: "td.desc"
    - path: "companion/pages/health_page.py"
      provides: "`desc_columns=(1,)` on the stats table's `data_table()` call, and the written record that the Coverage dot was deliberately not moved into the heading row"
      contains: "desc_columns"
    - path: "companion/test_status_pages.py"
      provides: "2 new checks (the muted Description column end to end; the heading-to-content rhythm markup + stylesheet guard), the in-place live-HTTP extension covering both the rendered page and the served stylesheet, and `EXPECTED_CHECK_COUNT` at on-disk-baseline + 2"
      contains: "td class=\\\"desc\\\""
  key_links:
    - from: "`layout.data_table()`'s new `desc_columns` keyword"
      to: "style.css's `.data-table td.desc` rule — the class is the ONLY thing connecting them, and the muting is invisible without both halves. A stylesheet rule with no emitter, or an emitted class with no rule, each fails silently and looks exactly like the bug being fixed"
    - from: "style.css's `.page-section--nested > h2` gaining `margin-bottom`"
      to: "`_nested_heading_tier_demoted_to_emphasis_role()`'s `\"font-family\" not in demotion_body` assertion — that check slices the rule's declaration block, so a reasoning comment written inside the braces rather than above the selector fails a check about a completely different property"
    - from: "the new `> p.text-body` rhythm rule"
      to: "`.battery-readout`'s own `margin: 0 0 var(--space-sm)` — the readout is a direct-child `<p>` of `.battery-trend-section` and is spared only because it carries no `text-body` class. Broadening the selector to a bare `> p` would silently take ownership of a margin another rule already documents owning"
    - from: "the four `<td class=\"desc\">` cells"
      to: "`test_contrast_check.py`'s already-pinned `muted detail text on card surface` pairs — the Description cells render the same 70% strength on the same `--color-dominant` card surface those two pairs already cover, so this task adds no pair and instead extends that comment to name its second consumer"
---

<objective>
Fix two confirmed, live-measured bugs on the shipped Health page, found by the developer testing the real page in Safari against the validated Merged Health Sketch hours after quick task 260901-uzi landed.

| # | Bug | Confirmed root cause | Fix |
|---|-----|----------------------|-----|
| 1 | Resolution-statistics' Description column renders at full-strength `--color-text` (measured `rgb(23, 25, 31)`, `classList: []`) where the sketch renders it muted | `data_table()` has no column-role class hook except `mono_columns`, so those `<td>`s carry no class at all and no rule can target them | A `desc_columns` keyword — `mono_columns`' direct sibling — plus `.data-table td.desc` carrying only the COLOUR half of the sketch's `td.desc` rule |
| 2 | Heading-to-content spacing differs between the two migrated nested cards (measured 8px vs 56px total) | Nothing in this stylesheet governs that gap: one card is followed by a zero-margin `<div>`, the other by `<p class="text-body">` elements whose margin comes from a UA default this file has never stated | Adopt the sketch's own two values: the card heading owns `var(--space-md)` below it, and every direct-child prose paragraph under it owns `0 0 var(--space-sm)` |

Purpose: close the developer's second live Safari pass over the page 06.6.4.1's closing checkpoint is still waiting on.

Output: three stylesheet changes (one new rule, one new declaration on an existing rule, one new rule), one new optional `layout` keyword, one call-site change plus two written decisions in `health_page.py`, 2 new harness checks, an in-place live-HTTP extension that now also proves the stylesheet itself is served, and a SUMMARY naming exactly what still needs live-browser pixel confirmation.

**Approach note — the sketch is authority for the VALUES, not for wholesale copying.** Both fixes take their numbers and their names directly from the cached Merged Health Sketch, and both deliberately decline part of what the sketch does. Bug 1 takes `td.desc`'s colour and refuses its `min-width: 220px`, because 260901-uzi's own comment in this same file already argued that a fixed floor on that exact column re-creates the overflow it had just fixed. Bug 2 takes `.wide-card__caption`'s 16px and `.read-only-note`'s `0 0 8px` and refuses the sketch's structural move of the status dot into the caption row, because that is a markup change to three cards for a complaint that was about spacing. Write both refusals down where the next reader will trip over them; a future "finish matching the sketch" edit must argue with a written decision, not fill in an apparent omission.

**Non-goals — verified, deliberately NOT touched.**
- **`--color-text-muted`.** The sketch names this token; style.css's own `.cell-primary`/`.cell-secondary` comment states in writing that no such token exists here and that the sketch's pattern naming one is deliberately not carried over, because a hard-coded grey would be wrong in one of the two themes. This task uses the file's single existing 70% `color-mix` strength and introduces no fourth muted value and no new token.
- **`min-width` on the Description column.** The sketch's `td.desc` declares `min-width: 220px`. `.data-table--prose`'s comment, six lines away from where the new rule lands, already explains why a fixed floor on this column is the defect and not the fix. The new rule declares no minimum width and the verify gate asserts it does not.
- **History's table and the unresolved-prefix registry table.** Both hand-roll their own markup (`history_page.py::_history_table_html()`, `health_page.py::_registry_table_html()`) and never call `data_table()`, so they cannot receive a `desc` class. Their cells' colour is unchanged and a harness check proves it.
- **The battery readings table.** The only other production `data_table()` caller. It passes `mono_columns`/`raw_columns` and does not pass `desc_columns`; its output is byte-identical.
- **Moving the Coverage / Battery-readings status dot into its card's heading row.** The sketch's own arrangement (`.wide-card__caption`, a space-between flex row). Rejected for this task, recorded in `health_page.py` with the sketch line reference.
- **`.page-section`, `.section-intro`, and every page other than Health.** Both new selectors are scoped through `.page-section--nested` and `.battery-trend-section`, the two classes only `health_page.py` emits. Settings' groups, History's card and Health's own top-level source-fault block keep their current rhythm.
- **`.stat-tile__value`'s size, `.data-table`'s shared `min-width: max-content` floor, and `companion/static/battery-trend.js`.** All three were settled by 260901-uzi and neither finding here touches them. No JavaScript change of any kind.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.planning/quick/260901-uzi-fix-4-confirmed-real-bugs-on-the-health-/260901-uzi-SUMMARY.md

@companion/pages/health_page.py
@companion/layout.py
@companion/static/style.css
@companion/test_status_pages.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the Resolution-statistics Description column its own muted column role</name>
  <files>companion/layout.py, companion/pages/health_page.py, companion/static/style.css</files>
  <read_first>
    - `layout.data_table()` in full — signature, both the `mono_columns` and `raw_columns` docstring paragraphs, and the cell loop. The loop's cell-class line is a single-class ternary; look at it before writing anything, because turning it into a class list is the one behavioural change in this file and the byte-identical claim for existing callers rests on getting the join order right.
    - Every `data_table(` call site. Grep `companion/` for it: there are exactly two production callers, both in `health_page.py` (the battery readings table and the stats table), plus harness callers in `test_view_pages.py` and `test_companion_app.py`. Confirm this yourself — the "scoping is carried by the opt-in" claim depends on it.
    - `health_page.py::_stats_table_html()` and `_SOURCE_ROWS`, plus the `rows` construction inside the resolution-stats reader. The row tuples are `(label, gloss, count)` against headers `["Source", "Description", "Count"]`, so the Description column's zero-based index is the middle one. Derive that index from the code you read, not from this sentence.
    - `health_page.py::_registry_table_html()`, the hand-rolled registry table this task must leave alone, so you can see for yourself that it never goes through the builder being changed.
    - style.css's `.data-table`, `.data-table--prose`, `.data-table th`, `.data-table-wrap th`, `.data-table td` and `.data-table tbody tr:hover` rules with every comment above them. Read `.data-table--prose`'s comment in full: its "no fixed pixel floor" paragraph is the standing argument this task obeys when it declines the sketch's `min-width: 220px`.
    - style.css's `.cell-primary`/`.cell-secondary`/`.cell-inline-sep` block and the comment above it. That comment is a written decision that this file has no muted token and will not gain one; it also shows the `opacity: 0.7` idiom, which is the alternative this task rejects for a `<td>`.
    - Grep style.css for the exact literal `color-mix(in srgb, var(--color-text) 70%, transparent)` to see every existing consumer of the muted strength, and confirm for yourself there is exactly one muted strength in the file before reusing it.
    - The cached Merged Health Sketch at `/Users/florian/.claude/projects/-Users-florian-Projects-skypane--claude-worktrees-airplanes-api-sustainability-a4b703/e1c689bd-9161-44a8-a403-c6b1d0a720e9/tool-results/artifact-3303f9eb-1788256096-c307.html` — its `table.data-table td.desc` rule and the Resolution-statistics table markup that uses it, so you are matching a real artifact rather than this plan's paraphrase of it.
    - `companion/test_contrast_check.py`'s `live_pairs` tuple, specifically the two `muted detail text on card surface` entries 260901-uzi added. Satisfy yourself that the Description cells render the same foreground strength on the same card surface those pairs already pin, so this task adds no pair.
    - Grep `companion/test_status_pages.py`, `companion/test_view_pages.py` and `companion/test_companion_app.py` for `<td>` and for `mono_columns` before editing, so you find every check that keys on a bare cell literal.
  </read_first>
  <action>
**A. `layout.py` — one more optional column-role keyword.** Add `desc_columns=()` to `data_table()`'s signature, after `raw_columns`, defaulting to an empty tuple exactly as `mono_columns` does.

Change the cell loop's class construction from a single-class ternary into a joined list of role classes: the monospace role when the column index is in `mono_columns`, the description role (class name exactly `desc`, the sketch's own name) when it is in `desc_columns`, and no `class` attribute at all when the list is empty. Put the monospace class first in the join, so a cell with only the monospace role emits the identical attribute string it emits today — that byte-identical property is what the verify gate below asserts and what keeps `test_view_pages.py`'s merged-cell mono check green without an edit.

Document it in the docstring as its own paragraph, after the `raw_columns` one, in that paragraph's voice. Say what it is: the same kind of hook `mono_columns` is — zero-based column indices, adds a class, changes no cell content and no escaping — for a different column role. Say what the role means: a column holding descriptive prose rather than data, which the validated sketch renders in the muted secondary strength so the eye lands on the values beside it. Name the defect that prompted it: the Resolution-statistics Description column measured `rgb(23, 25, 31)` — full-strength body text — with an empty `classList`, because before this keyword the builder had no way to emit a hook for any column role but monospace. State that `mono_columns`, `raw_columns` and `desc_columns` are mutually orthogonal (one controls a class, one controls escaping, one controls a class) and may safely name the same index.

**B. `health_page.py` — opt exactly one column in.** Pass the new keyword at `_stats_table_html()`'s `data_table()` call, naming the Description column's index. Leave the battery readings table's call untouched.

Extend `_stats_table_html()`'s docstring with one short paragraph beside its existing `prose` one: the same reason this is the only table that opts out of the no-crop floor is the reason it is the only one whose middle column opts into the description role — it is the only table in the app whose cells hold prose rather than values, and the sketch's own Resolution-statistics table (cached artifact, `td.desc`) mutes exactly that column so the Source labels and Counts stay the scannable part.

**C. `style.css` — the muted column-role rule.** Add `.data-table td.desc` immediately after the existing `.data-table td` rule, so the base cell rule and its column-role variant read together — the same "base rule, then its scoped variant" pairing `.data-table` / `.data-table--prose` already uses higher in the file. Declare exactly one thing: the colour, set to this file's existing 70% `color-mix` muted strength.

Comment it ABOVE the selector, never inside the braces (this file's universal style, and the demotion rule two hundred lines away has a harness check that reads declaration blocks for stray property names). Cover four things, in this order.

First, the measured defect and its real cause: the Description cells rendered at full `--color-text` with no class at all, because `data_table()` had no column-role hook except the monospace one — there was no element for a rule to target, so this is a missing hook plus a missing rule, not a rule that was written wrong.

Second, the value's provenance: the sketch names a `--color-text-muted` token; this file's own `.cell-primary` comment already decided in writing that no such token will exist here, so the strength used is the file's single existing 70% `color-mix` — the same literal `.section-caption`, `.data-table th`, `.filter-bar__count` and `.battery-readout__detail` carry. Not a fourth muted value, and not a new token.

Third, why `color` and not `opacity`, since `.cell-secondary` reaches the same 70% read the other way: `.data-table td` declares a `border-bottom` hairline, and `opacity` would fade that separator along with the text on exactly these four rows and nowhere else in the table.

Fourth, the two boundaries. The sketch's own rule carries a second declaration, a fixed minimum width on this column; it is deliberately not carried, because `.data-table--prose`'s comment higher in this same file already argues that a fixed floor on this exact column re-creates the horizontal overflow that modifier had just removed, one breakpoint down. And the scoping: this rule is unscoped by design because the CLASS is the scope — `desc` is emitted only where a caller passes the new keyword, exactly as `mono` is, and the only caller that passes it is the stats table. History's table and the unresolved-prefix registry hand-roll their own markup and never reach this builder at all.

**D. Fix in place anything the harness greps in `read_first` surfaced.** If a check asserts a bare `<td>` literal against a stats-table cell, retarget it onto the class-bearing form; if it is reading History's or the battery readings table, leave it and say so in the commit message. No `EXPECTED_CHECK_COUNT` movement in this task. Add no contrast pair — record in your commit message which existing `live_pairs` entries already cover this foreground on this surface.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys; sys.path.insert(0, '.')
from companion import layout
plain = layout.data_table(['A', 'B'], [['1', '2']])
assert '<td>1</td><td>2</td>' in plain, 'default output must carry no cell class at all'
mono = layout.data_table(['A', 'B'], [['1', '2']], mono_columns=(0,))
assert '<td class=\"mono\">1</td>' in mono, 'the mono-only output must be byte-identical to today'
desc = layout.data_table(['A', 'B'], [['1', '2']], desc_columns=(1,))
assert '<td class=\"desc\">2</td>' in desc, 'desc_columns must class the named column'
assert desc.replace(' class=\"desc\"', '') == plain, 'the keyword must change the class list and nothing else'
both = layout.data_table(['A', 'B'], [['1', '2']], mono_columns=(0,), desc_columns=(0,))
assert '<td class=\"mono desc\">1</td>' in both, 'one cell must be able to carry both roles, mono first'
raw = layout.data_table(['A'], [['<b>x</b>']], raw_columns=(0,))
assert '<td><b>x</b></td>' in raw, 'raw_columns must be unaffected'
esc = layout.data_table(['A'], [['<b>x</b>']], desc_columns=(0,))
assert '&lt;b&gt;' in esc, 'desc_columns must not bypass escaping'
print('builder ok')
" && server/.venv/bin/python3 -c "
import sys; sys.path.insert(0, '.')
s = open('companion/static/style.css').read()
td_at = s.index('.data-table td {')
desc_at = s.index('.data-table td.desc {')
assert td_at < desc_at, 'the column-role variant belongs after the base cell rule'
body = s[desc_at:s.index('}', desc_at)]
assert 'color: color-mix(in srgb, var(--color-text) 70%, transparent)' in body, 'reuse the file existing muted strength'
assert 'opacity' not in body, 'colour, not opacity — opacity would fade the cell border hairline too'
assert 'min-width' not in body, 'the sketch fixed floor is deliberately not carried; see the .data-table--prose comment'
assert 'var(--color-text-muted)' not in s, 'this file has no muted token and must not gain one'
assert '--color-text-muted:' not in s, 'no muted token may be declared here either'
print('css ok')
" && server/.venv/bin/python3 -c "
import sys, tempfile, shutil; sys.path.insert(0, '.')
from datetime import datetime, timedelta, timezone
from server import history_db as hd
from companion.pages import health_page as h
d = tempfile.mkdtemp(); n = datetime.now(timezone.utc)
with hd.open_db(d) as conn:
    hd.record_device_health(conn, n.isoformat(timespec='seconds'), battery_mv=4200)
    hd.record_runway_event(conn, ts=n.isoformat(timespec='seconds'), hex='abc123', route_source='fresh_hit')
r = h.render({'state_dir': d}); shutil.rmtree(d, ignore_errors=True)
expected = len(h._SOURCE_ROWS)
assert r.count('<td class=\"desc\">') == expected, 'one desc cell per source row, got %d want %d' % (r.count('<td class=\"desc\">'), expected)
stats_at = r.index(h.STATS_SECTION_HEADING)
assert r.index('<td class=\"desc\">') > stats_at, 'every desc cell must live in the Resolution-statistics table'
unresolved_at = r.index(h.UNRESOLVED_SECTION_HEADING)
assert not (unresolved_at < r.index('<td class=\"desc\">') < stats_at), 'the registry table must carry no desc cell'
print('markup ok')
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_contrast_check.py</automated>
  </verify>
  <done>
`data_table()` carries one optional, default-inert `desc_columns` keyword built as `mono_columns`' sibling, with a joined cell-class list that leaves every existing caller's output byte-identical and lets one cell hold both roles. Only the Resolution-statistics table's Description column passes it. `.data-table td.desc` sits after `.data-table td` declaring only the file's existing 70% muted colour, with the missing-hook diagnosis, the no-new-token decision, the colour-not-opacity reason and the declined `min-width` all written above the selector. No contrast pair was added and the reason is recorded. Every companion harness passes at its unchanged `EXPECTED_CHECK_COUNT`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give every nested card one heading-to-content rhythm, from the sketch's own values</name>
  <files>companion/static/style.css, companion/pages/health_page.py</files>
  <read_first>
    - Render the page yourself before forming any hypothesis and read the ACTUAL markup between each `</h2>` and its first real content, for all three nested cards, in BOTH the seeded and the empty state. Use `health_page.render({'state_dir': <tmpdir>})` with and without seeded device-health / runway-event / unresolved-prefix fixtures. The developer's report names one intervening paragraph in the Unresolved-prefixes card; the source has two (a status-dot line and the read-only note) plus a filter bar in the populated state. Trust what you render over any number in this plan or in the report.
    - `health_page.py::_registry_section()`, `_stats_table_html()`, `_battery_section()` and the two `<section class="page-section page-section--nested">` lines in `render()`. Note that `_battery_section()` emits the same `<h2>` then `<p class="text-body">` dot-label shape the registry card does — the pattern this task fixes has three instances, not two.
    - `health_page.py::_battery_readout_block()`'s emitted element, so you can confirm for yourself that `.battery-readout` carries no `text-body` class. The new rule's scoping depends on that being true, and if it has changed since this plan was written the selector must change with it.
    - style.css's `.text-label`, `.text-body`, `.text-heading` typography-role rules and the `h1, h2, h3, .text-heading { margin: 0 0 var(--space-sm) }` heading-rhythm rule with its comment. Note what `.text-body` does NOT declare: no margin at all, which is the whole mechanism — those paragraphs fall through to the UA `margin-block: 1em` this stylesheet has never stated.
    - style.css's `.section-intro` and `.section-intro > p { margin: 0 }` rules and the long comment above the second one. That is this file's own precedent for zeroing a UA paragraph margin at a specific structural position with a child combinator, and for explaining why the zeroing belongs on the container's scoped selector rather than inside the typography role — the new rule below is the same move and should read as its sibling.
    - style.css's `.page-section`, `.page-section--nested > h2, .battery-trend-section > h2` (the 260901-uzi demotion rule, comment and all), `.battery-trend-section`, `.battery-readout`, `.filter-bar`, `.empty-state` and `summary`. Confirm for yourself which of these declare a top margin — the decision not to add a catch-all rule depends on the answer being "none of them".
    - `companion/test_status_pages.py::_nested_heading_tier_demoted_to_emphasis_role()` in full. It slices the demotion rule's declaration block and fails if the literal `font-family` appears inside it. Your new declaration is safe; a comment written inside the braces would not be.
    - The cached Merged Health Sketch at `/Users/florian/.claude/projects/-Users-florian-Projects-skypane--claude-worktrees-airplanes-api-sustainability-a4b703/e1c689bd-9161-44a8-a403-c6b1d0a720e9/tool-results/artifact-3303f9eb-1788256096-c307.html` — read `.wide-card`, `.wide-card__caption`, `.resolution-grid` and `.read-only-note` in its `<style>` block, and both `.wide-card` bodies inside `.resolution-grid` in its markup. Those two margin declarations are the values this task adopts, and the caption's flex row holding the status dot is the structural move it declines. Read them before writing either.
  </read_first>
  <action>
**A. Diagnose against the real source and record what you found.** Before editing, write down (for the SUMMARY) the actual current gap chain in each of the three cards, derived from the cascade you just read: which declaration produces each gap, and which of them is a stated rule in this file versus an unstated UA default. Expect the Resolution-statistics 8px to be exactly the heading-rhythm rule's `var(--space-sm)`, and expect the registry card's gaps to be the UA paragraph margin `.text-body` never overrides.

If your arithmetic does not reproduce the reported live numbers (8px / 40px / 16px / 56px total), do NOT invent a rule to hit them and do NOT quietly ignore the difference: fix the mechanism you can prove from source, and record the discrepancy in the SUMMARY as an item for the orchestrating session's browser pass, naming both the number you derived and the number that was reported. A fix aimed at an unexplained number is a guess.

**B. `style.css` — the card heading owns the gap below it.** Add one declaration to the existing `.page-section--nested > h2, .battery-trend-section > h2` demotion rule: the bottom margin, set to the medium space token. Use the longhand bottom property, not the `margin` shorthand — the heading-rhythm rule's own `margin: 0 0 var(--space-sm)` still owns the zeroed top and sides, and a shorthand here would restate three values it does not mean to change.

Extend that rule's comment ABOVE the selector — never inside the braces — with a fifth numbered paragraph in its existing voice. Say: the two migrated cards measured wildly different heading-to-content gaps in a real browser, and the cause was that nothing in this file had ever governed that gap, so it was whatever the composition of the heading-rhythm rule and an unstated UA paragraph margin happened to produce. Say where the value comes from: the validated sketch's own card-title role declares the same medium space below every card title unconditionally, in both cards, and this is that declaration rather than a middle value chosen to split the difference. Say what makes the gap deterministic rather than coincidental: paired with the paragraph rule below, the heading owns the space and no following element contributes any, so the gap is the same regardless of whether a paragraph, a table wrapper, a filter bar or an empty state comes next.

**C. `style.css` — direct-child prose under a card heading owns the sketch's own rhythm.** Add one rule immediately after the demotion rule, with the selector list `.page-section--nested > p.text-body` and `.battery-trend-section > p.text-body`, declaring the margin shorthand as zero top, zero sides, and the small space token below — the exact shape the sketch's own intervening-prose role declares.

Child combinators on both, and the `text-body` class qualifier on both, are load-bearing and must be commented as such: the child combinator keeps the rule off the paragraphs inside a nested `.empty-state` (which are not siblings of any heading), and the class qualifier keeps it off `.battery-readout`, which is a direct-child `<p>` of the battery card and already documents owning its own margin in its own rule. A bare `> p` selector would silently take that ownership away.

Comment the rule with, in order: the defect (composed margins with no governing rule, measured live as a large inconsistency between two cards that render the same structural pattern); the provenance of the value (the sketch's own intervening-prose role, zero above and the small space below, which is what makes a card's whole pre-content stack add up the way the sketch's does); the scoping reasoning above; and the file's own precedent, naming `.section-intro > p` as the same move made for the same reason at a different structural position.

Then record the one thing this rule deliberately does not do: no catch-all is added for "whatever follows the heading". Say why it is not needed today — every other element type that can sit directly under one of these headings (the table wrapper, the filter bar, the empty state, the readout, the chart, the disclosure) already carries no top margin, which you verified by reading them — and say what pins that instead: a markup check in the harness asserts the allowlist of element types that may follow one of these headings, so a future element carrying a UA top margin fails a test rather than silently reintroducing the inconsistency.

**D. `health_page.py` — record the structural move that was NOT made.** Add a short comment at `_registry_section()` (the builder that emits the status-dot line and the read-only note) stating that the sketch places the card's status dot inside the card-title row as a space-between flex pair, and that this task deliberately kept it as its own line below the heading: the developer's finding was about spacing, the spacing is now the sketch's, and moving the dot would restructure three cards, introduce a new flex heading treatment and retarget several harness checks for a change nobody asked for. Name the sketch's role by its class name so the next reader can find it. Add the same one-line cross-reference at `_battery_section()`'s status-line construction, pointing at the `_registry_section()` comment rather than repeating it.

**E. Fix in place any check the changes break.** The demotion-rule check reads a declaration block for a font-family literal and must still pass unedited — verify by running, not by reading. No `EXPECTED_CHECK_COUNT` movement in this task.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys; sys.path.insert(0, '.')
s = open('companion/static/style.css').read()
sel = '.page-section--nested > h2,'
at = s.index(sel); open_b = s.index('{', at); close_b = s.index('}', open_b)
body = s[open_b:close_b]
assert 'font-size: var(--font-body-size)' in body, 'the demotion must keep the Body size'
assert 'font-weight: var(--weight-semibold)' in body, 'the demotion must keep the semibold weight'
assert 'margin-bottom: var(--space-md)' in body, 'the card heading must own the sketch medium gap below it'
assert 'margin:' not in body, 'longhand only — the heading-rhythm rule still owns the zeroed top and sides'
assert 'font-family' not in body, 'the existing harness guard forbids a family here, comments included'
p_at = s.index('.page-section--nested > p.text-body')
assert p_at > close_b, 'the prose rhythm rule belongs after the heading rule it pairs with'
p_open = s.index('{', p_at)
p_list = s[p_at:p_open]
p_body = s[p_open:s.index('}', p_open)]
assert 'margin: 0 0 var(--space-sm)' in p_body, 'intervening prose owns zero above and the small space below'
assert '.battery-trend-section > p.text-body' in p_list, 'the battery card renders the same pattern and must be covered'
assert p_list.count('p.text-body') == 2, 'both selectors keep the text-body qualifier: a bare > p would capture .battery-readout'
print('css ok')
" && server/.venv/bin/python3 -c "
import sys, tempfile, shutil, re; sys.path.insert(0, '.')
from datetime import datetime, timedelta, timezone
from server import history_db as hd
from server import poll_loop
from companion.pages import health_page as h
ALLOWED = ('<p class=\"text-body\">', '<div ', '<details', '<svg ')
for seeded in (False, True):
    d = tempfile.mkdtemp(); n = datetime.now(timezone.utc)
    if seeded:
        with hd.open_db(d) as conn:
            for off, mv in ((3, 4200), (1, 4190)):
                hd.record_device_health(conn, (n - timedelta(minutes=off)).isoformat(timespec='seconds'), battery_mv=mv)
            hd.record_runway_event(conn, ts=n.isoformat(timespec='seconds'), hex='abc123', route_source='fresh_hit')
        poll_loop.save_poll_state(d, {'unresolved_prefixes': {'JAF': {'count': 4, 'first_seen': n.isoformat(timespec='seconds'), 'last_seen': n.isoformat(timespec='seconds'), 'example_callsign': 'JAF412'}}})
    r = h.render({'state_dir': d}); shutil.rmtree(d, ignore_errors=True)
    for heading in (h.BATTERY_SECTION_HEADING, h.UNRESOLVED_SECTION_HEADING, h.STATS_SECTION_HEADING):
        at = r.index('>%s' % heading)
        after = r[r.index('</h2>', at) + len('</h2>'):]
        if not after.startswith('</section>'):
            assert after.startswith(ALLOWED), 'seeded=%s %r is followed by %r, which is outside the no-top-margin allowlist' % (seeded, heading, after[:40])
print('markup ok')
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>
The actual per-card gap chain was derived from the real cascade and written down, with any disagreement against the reported live numbers recorded rather than papered over. The nested card heading tier declares a longhand bottom margin at the sketch's card-title value, and one new rule gives every direct-child `p.text-body` under those three cards the sketch's intervening-prose rhythm — zero above, the small space below. Both rules carry their provenance, their scoping reasoning and the declined catch-all in comments placed above their selectors. `health_page.py` records that the sketch's status-dot-in-the-caption-row structure was considered and declined. Every companion harness passes at its unchanged `EXPECTED_CHECK_COUNT`, including the demotion-rule guard, unedited.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pin both fixes, prove the stylesheet is really served, and hand off the pixel pass</name>
  <files>companion/test_status_pages.py</files>
  <read_first>
    - `companion/test_status_pages.py`'s `EXPECTED_CHECK_COUNT` and the whole provenance comment block above it — read the REAL on-disk value at execution time; this plan deliberately names no number.
    - The `check(name, fn)` helper at the top of `main()` for the return-tuple contract, and `_mkstate` / `_ctx` / `_iso` / `_now` / `_seed_device_health` / `_seed_meta` / `_seed_unresolved_prefixes` / `_seed_runway_events`.
    - The five checks 260901-uzi added (`_dashboard_grid_stretches_same_row_tiles`, `_nested_heading_tier_demoted_to_emphasis_role`, `_prose_table_opts_out_alone`, the humanised-readout end-to-end check, the readout typographic-split guard). The two new checks below extend the same markup-half-plus-stylesheet-half idiom and belong beside them; read how they locate rule bodies and how they word their failure messages before writing new ones.
    - Section 3's `Harness` block, its `http_request` helper and `_both_tabs_ok_end_to_end()` in full, including the `/health` assertions 260901-uzi added in place and the fixture's resolved runway event.
    - `companion/app.py`'s static-asset route constant for the stylesheet and the comment noting it is served pre-auth. That route is what lets the live check prove the CSS itself is served, not merely present on disk.
  </read_first>
  <action>
**Add exactly two checks, extend one in place, and bump the count.**

**Check 1 — the Description column is the only muted column, end to end.** One check with three halves, beside `_prose_table_opts_out_alone()`.

Markup: on a fixture seeded with a resolved runway event (so the stats table actually renders) and an unresolved prefix (so the registry table renders too), assert that the number of description-class cells equals `len(health_page._SOURCE_ROWS)` — derived from the module, never re-typed — that every one of them falls inside the Resolution-statistics section's own slice located from `health_page.STATS_SECTION_HEADING`, and that neither the registry table's slice nor the battery readings table's slice contains one.

Builder: call `layout.data_table()` directly and assert the default output carries no cell class, that a monospace-only call is byte-identical to the pre-keyword form, that a description-only call differs from the plain call by exactly the added class, and that a cell named by both keywords carries both classes.

Stylesheet: locate `.data-table td.desc`'s declaration block and assert it declares the file's existing 70% `color-mix` muted strength, declares no fixed minimum width, and does not use opacity; and assert the file contains no muted-token custom property.

Fail with messages that say what the failure MEANS: an unclassed Description cell is the original full-black bug returning; a `min-width` in that block is the horizontal overflow `.data-table--prose` removed returning one breakpoint down; a second muted value is the thing this stylesheet's own comments forbid.

**Check 2 — one heading-to-content rhythm across all three nested cards.** One check with two halves, beside `_nested_heading_tier_demoted_to_emphasis_role()`.

Markup: render both the empty and the seeded state. For each of the battery, unresolved-prefixes and resolution-statistics cards, located from their own heading constants, assert that the element immediately following `</h2>` is either a `p` carrying the body-text class (governed by the new rhythm rule) or a member of the no-top-margin allowlist you verified in Task 2 — or that the card is empty. This is the check that replaces the catch-all rule Task 2 declined; its failure message must say exactly that, so a future contributor who adds an element with a UA top margin is told which rule to add rather than being told a number is wrong.

Stylesheet: assert the demotion rule declares the medium bottom margin as a longhand, that the prose rhythm rule exists with both card selectors and declares zero above and the small space below, and that the prose rule sits after the heading rule in source order.

**In-place extension — the live-HTTP check (no count change).** Extend Section 3's `_both_tabs_ok_end_to_end()` where 260901-uzi already extended it:

- For the `/health` response body, assert the description-class cells appear exactly `len(health_page._SOURCE_ROWS)` times and that they fall after the Resolution-statistics heading in the served body.
- Fetch the stylesheet's own route from the same running service with the same helper, assert a 200, and assert the served CSS contains the description-column rule, the demotion rule's new bottom margin and the prose rhythm rule's selector. This is the automated half of "the new CSS is actually served" — the on-disk assertions in Checks 1 and 2 prove the file says the right thing; only this one proves the running service hands it to a browser.

Update the check's description to say it now also covers the served stylesheet.

**Falsifiability pass.** Before finalizing, mutate both new checks so each asserts on a name that does not exist in the source it reads, run the harness, and confirm the output reports exactly those two as FAIL and nothing else. Then restore them. Do the same for the live-HTTP additions by temporarily pointing the stylesheet fetch at a nonexistent route. A check that cannot be observed failing is not a check.

**Count bump.** Read the current on-disk `EXPECTED_CHECK_COUNT` and set it to that value plus exactly 2. Do not carry a number over from this plan text. Extend the provenance comment block with one new entry in its established format: name this quick task, list the two checks added, and record that Tasks 1 and 2's in-place retargets and this task's live-HTTP extension were all no-count-change edits.

**Full suite.** Run `scripts/run-all-tests.sh`. The only harness in its FAILED list must be `server/test_poll_loop.py` — the known, pre-existing, unrelated digest mismatch. If any other harness fails, or the coverage gate reports a new shortfall, stop and fix it; do not record a green result over a new failure.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 -c "
import re, subprocess, sys
src = open('companion/test_status_pages.py').read()
expected = int(re.search(r'^EXPECTED_CHECK_COUNT = (\d+)', src, re.M).group(1))
out = subprocess.run(['server/.venv/bin/python3', 'companion/test_status_pages.py'], capture_output=True, text=True).stdout
passed, total = map(int, re.findall(r'(\d+)/(\d+)', out)[-1])
assert passed == total == expected, 'printed %d/%d against EXPECTED_CHECK_COUNT %d' % (passed, total, expected)
print('count ok: %d' % expected)
" && scripts/run-all-tests.sh > /tmp/skypane-bl2-run-all-tests.log 2>&1; test "$(sed -n '/FAILED harnesses/,$p' /tmp/skypane-bl2-run-all-tests.log | grep -c '^    - ')" = 1 && sed -n '/FAILED harnesses/,$p' /tmp/skypane-bl2-run-all-tests.log | grep -q 'server/test_poll_loop.py'</automated>
    <human-check>
REQUIRED, not optional — but do NOT block plan completion on it. This project's memory records that computed-style and structural checks alone have already missed a real rendered bug (`feedback_real_device_ui_verification`), and both immediately-prior Health quick tasks (260901-tsa, 260901-uzi) had no browser tooling bound to their executor and handed this pass back to the orchestrating session, which performed it successfully both times. Assume the same division of labour.

The live-HTTP half is already automated and is not optional: the extended `_both_tabs_ok_end_to_end()` starts a real `companion/app.py` subprocess against a real seeded state directory, signs in, fetches `/health`, and separately fetches the stylesheet's own route. Read what those assertions actually matched and record it in the SUMMARY — specifically that the served HTML carried the description-class cells and the served CSS carried all three new/changed rules.

Then name these pixel-level items in the SUMMARY as outstanding for the orchestrating session's browser pass:
1. The Resolution-statistics Description column now reads as muted secondary text beside full-strength Source labels and Counts, in BOTH themes, and is still comfortably legible at the dark theme's card surface.
2. The three nested cards ("Battery trend", "Unresolved prefixes", "Resolution statistics") now show the same gap between their heading and the first thing under it, measured live rather than derived — and the Unresolved-prefixes card's total pre-table stack is visibly tighter than the 56px that was reported.
3. Whether the derived gap arithmetic in the SUMMARY matches the live measurement. If the reported 40px between that card's heading and its first paragraph is still there after the fix, the cause is something not visible in the source read and must be measured, not guessed.
4. Nothing else on the page moved: Settings, History and Health's own source-fault block keep their previous heading rhythm, and History's and the registry table's cells keep full-strength text.
5. Still open from 260901-uzi and not touched here: the Safari readings-disclosure header clipping (finding 5), with both candidates and their trade-offs as recorded in that task's SUMMARY.

Record what was actually observed rather than restating this list as if performed, and stop the service and delete the tmpdir afterwards.
    </human-check>
  </verify>
  <done>
`companion/test_status_pages.py` passes with every check green, its printed total equals the new `EXPECTED_CHECK_COUNT`, and that value is the real on-disk baseline plus exactly 2. The provenance comment records the two additions and the in-place edits as no-count-change. Both new checks and the live stylesheet fetch were each observed failing under mutation before being restored. `scripts/run-all-tests.sh` lists exactly one failing harness, `server/test_poll_loop.py`. A real `companion/app.py` process served both a `/health` body carrying the description-class cells and a stylesheet response carrying all three rule changes, and the SUMMARY records what those responses contained plus the precise list of pixel-level items still outstanding.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `GET /health` | Server-rendered HTML reaching an authenticated operator's browser. Every dynamic value on this page passes through `escape_html()` or one of `companion/layout.py`'s escaping component builders; this task changes only a class attribute the builder emits from its own literals. |
| `history_db` state files -> `render()` | Device telemetry and route-source counts feeding the stats table. No read path, query or parse is changed. |
| browser -> `GET /static/style.css` | A pre-auth static asset. This task adds two rules and one declaration to it and adds a live fetch of it to the harness; it introduces no dynamic content into that response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-bl2-01 | Tampering (XSS) | `data_table()`'s new `desc_columns` keyword | low | mitigate | The keyword's only effect is a class name chosen from the builder's own literals; it does not touch `raw_columns`, does not bypass `escape_html()`, and cannot make a cell's value unescaped. Task 1's verify gate asserts directly that a hostile cell value passed through `desc_columns` is still escaped and that `raw_columns` behaviour is unchanged. |
| T-bl2-02 | Tampering | The new keyword reaching callers this task does not intend to change | low | mitigate | The parameter defaults to an empty tuple and the cell-class join is ordered so that a monospace-only cell emits the byte-identical attribute string it emits today. Task 1's gate asserts the default output carries no cell class and that the mono-only output is unchanged, and runs `test_view_pages.py` (which owns the merged-cell mono assertions) and `test_companion_app.py` (which owns this builder's escaping checks). |
| T-bl2-03 | Information Disclosure | The muted Description column | low | mitigate | Uses this file's single existing 70% `color-mix` strength on the `--color-dominant` card surface — the exact foreground/background pair `test_contrast_check.py`'s `live_pairs` already pins in both themes for `.battery-readout__detail`. No new value, no new token, no new pair needed; Task 1 runs that harness and records which entries cover it. Task 1's gate also forbids `opacity`, which would additionally fade the cell's border hairline. |
| T-bl2-04 | Denial of Service | Accessibility/readability regression from the spacing change | low | mitigate | Both new declarations are margins on existing elements, taken from the validated sketch's own two values. No element, role, `id`, heading level or live region changes, so the document outline, heading navigation and every id anchor — including `#server-data`, which `history_page.py` links to — are untouched. Task 2's gate re-runs the demotion-rule guard unedited and asserts the heading rule still carries its size and weight. |
| T-bl2-05 | Tampering | A future edit silently undoing either fix | medium | mitigate | Both fixes depend on facts no comment can enforce: that `desc` is emitted by exactly one call site and targeted by exactly one rule, and that nothing following one of these headings carries a top margin. Task 3 pins both as harness checks — including a markup allowlist that fails loudly if a future element reintroduces a UA margin — and proves each observably fails under mutation before restoring it. |
| T-bl2-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install of any kind. This task edits one stylesheet, two Python modules and one harness, all stdlib-only, with no dependency change. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_status_pages.py` — all checks pass; the printed total equals the new `EXPECTED_CHECK_COUNT`, which is the real on-disk baseline plus exactly 2.
2. `companion/test_config_page.py`, `companion/test_view_pages.py`, `companion/test_companion_app.py` and `companion/test_contrast_check.py` — all pass at their own unchanged `EXPECTED_CHECK_COUNT`s, proving the `data_table()` keyword and the two spacing declarations reached no other page.
3. `scripts/run-all-tests.sh` — exactly one harness in the FAILED list, `server/test_poll_loop.py` (known pre-existing digest mismatch). Coverage gate reports no new shortfall.
4. `git diff --stat` touches only `companion/layout.py`, `companion/pages/health_page.py`, `companion/static/style.css` and `companion/test_status_pages.py`. No other page module and no JavaScript file appears.
5. `git diff companion/static/style.css` shows exactly three changes: `.data-table td.desc` new, after `.data-table td`; one longhand bottom-margin declaration added to the existing nested-heading demotion rule; and the `> p.text-body` rhythm rule new, after it. No other rule's declarations change, and no new custom property, size, muted strength or `:has()` selector appears anywhere.
6. `git diff companion/layout.py` shows only the new signature parameter, the cell-class join, and the new docstring paragraph — no change to escaping, to `raw_columns`, to `mono_columns` or to the wrapper markup.
7. The extended `_both_tabs_ok_end_to_end()` passed against a real `companion/app.py` subprocess, and the SUMMARY records both what the `/health` body contained and what the served `/static/style.css` contained.
8. The SUMMARY names, explicitly and separately, the pixel-level items still outstanding for the orchestrating session's browser pass, including any disagreement between the gap arithmetic derived from source and the numbers the developer measured live.
</verification>

<success_criteria>
- The Resolution-statistics Description column renders in the same muted secondary strength every other muted element on the page uses, and no other table's cells change colour.
- All three nested cards on the Health page show one consistent heading-to-content rhythm, built from the validated sketch's own two margin declarations rather than a value chosen to split the difference.
- Every value introduced comes from this stylesheet's existing token set and its single existing muted strength. No new custom property, no new size, no second muted value, no `--color-text-muted`.
- Both places where this task takes only part of what the sketch does — `td.desc`'s declined `min-width`, and the declined move of the status dot into the card-title row — are recorded as written decisions in the files they affect, with their sketch references, not left as apparent omissions.
- The scoping of both fixes is carried by opt-in mechanisms (`desc_columns`, and the `page-section--nested` / `battery-trend-section` classes only `health_page.py` emits), so Settings, History, Preview and the source-fault block are provably untouched.
- `data_table()`'s existing callers are byte-identical, proven by assertion rather than by inspection.
- Every check the two fixes break is retargeted in place; none is deleted.
- `EXPECTED_CHECK_COUNT` moved to the real on-disk baseline plus exactly 2, with the in-place retargets and the live-HTTP extension recorded as no-count-change edits.
- The running service was proven to serve both the new markup and the new stylesheet, not merely to have them on disk.
- `scripts/run-all-tests.sh` shows only the pre-existing `server/test_poll_loop.py` failure.
</success_criteria>

<commits>
Focused and atomic, matching this session's established style (`git log --oneline -10`), referencing the quick task id rather than a phase-plan number:
- `fix(quick-260902-bl2): mute the resolution-statistics description column`
- `fix(quick-260902-bl2): give every nested Health card one heading-to-content rhythm`
- `test(quick-260902-bl2): pin both Health fixes, EXPECTED_CHECK_COUNT +2`
- `docs(quick-260902-bl2): record the muted-description and card-rhythm fixes`
</commits>

<output>
Create `.planning/quick/260902-bl2-fix-2-more-confirmed-real-bugs-on-the-he/260902-bl2-SUMMARY.md` when done.
</output>
