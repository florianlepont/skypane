---
phase: quick-260902-i4f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plane/enrich.py
autonomous: true
requirements: [QT-lgt-D-06]
must_haves:
  truths:
    - "The comment block immediately above `\"KLJ\": \"KlasJet\",` opens with a dated 2026-09-02 note recording that a real KLJ-prefixed flight was observed and confirmed at Orly."
    - "The original lower-confidence / never-live-confirmed framing no longer reads as a live claim: everything describing it sits below an explicit uppercase `SUPERSEDED` marker that names 2026-09-02 as the resolution date."
    - "The original investigation record survives verbatim in substance — the ~25 adsbdb queries returning `unknown callsign`, the `QT-lgt-D-06` citation, and the ACMI/wet-lease reasoning for why a KLJ callsign might never have appeared — as historical context, not deleted."
    - "The Remediation pointer survives as a standing safeguard (re-verify this row if a KLJ callsign ever resolves to a DIFFERENT carrier), no longer presented as an open or unresolved item."
    - "The line `    \"KLJ\": \"KlasJet\",` is byte-identical to before the task — the mapping value is confirmed, not changed."
    - "`server/plane/enrich.py` is the only modified tracked file: no test file, no `illustrations.py`, no VENDOR.md/HANDOFF.md, no `.planning/` file other than this task's own artifacts."
    - "`server/test_enrich.py` passes unmodified at its existing check count."
  artifacts:
    - "server/plane/enrich.py: rewritten KLJ comment block (currently 15 comment lines, `server/plane/enrich.py:584-598`) carrying the 2026-09-02 confirmation on top, the SUPERSEDED historical record below it, and the standing remediation pointer last"
  key_links:
    - "2026-09-02 confirmation note -> the SUPERSEDED marker -> the retained pre-2026-09-02 adsbdb/wet-lease investigation record (this top-to-bottom order is what makes the historical text unmistakably historical, and is machine-checked)"
    - "This comment block -> `\"KLJ\": \"KlasJet\",` (the row the comment documents, which this task must not touch)"
    - "This comment's supersession shape -> `server/test_poll_loop.py`'s `_DEFAULT_CONFIG_DIGEST` dated-addendum convention (the in-repo precedent being followed)"
---

<objective>
Record, in `server/plane/enrich.py`'s ICAO-prefix table, that the `KLJ`
(KlasJet) prefix has now been **genuinely observed in real traffic** —
developer-confirmed 2026-09-02 ("vol réel observé") — and retire the
never-live-confirmed framing the row has carried since quick task 260827-lgt.

The existing 15-line comment at `server/plane/enrich.py:584-598` was written
when `KLJ` had only ever been corroborated by lookup sources: ~25 adsbdb probes
across plausible flight-number ranges all returned `unknown callsign`, and there
was a real structural reason (KlasJet is a Lithuanian ACMI/wet-lease and VIP
charter operator, and wet-lease flights typically broadcast the CONTRACTING
airline's callsign rather than the operator's own) to suspect a `KLJ` callsign
might rarely or never appear at Orly at all. That open question is now closed —
and closed in the direction that CONFIRMS the mapping, not one that contradicts
it.

Purpose: this comment is the single strongest "do not trust this row" warning in
the whole table. Leaving it standing after the uncertainty resolved actively
misleads: a future reader debugging a caption would treat a now-verified row as
the prime suspect, and a future planner would re-scope a live-confirmation
investigation that has already happened.

Output: one comment-only edit to `server/plane/enrich.py`. The
`"KLJ": "KlasJet"` mapping value itself does not change — the observation
confirmed it. No test changes. No other file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Read before editing:
- `server/plane/enrich.py:550-600` — the quick-task-260827-lgt block (`HOP`,
  `WMT`, `KLJ`). Read all three rows, not just `KLJ`: they share a preamble
  ("HOP, WMT, KLJ added by quick task 260827-lgt...") that must survive, and the
  `HOP`/`WMT` comments establish the house voice this rewrite must match
  (uppercase for load-bearing warnings, dashes not em-dashes, ~72-column wrap,
  explicit decision-ID citations).
- `server/test_poll_loop.py:150-225` — the `_DEFAULT_CONFIG_DIGEST` comment
  history. This is the in-repo precedent for the shape being asked for here:
  successive dated addenda ("Fifth re-pin: ...", "Sixth re-pin: ...") that
  record what changed and when, stacked on top of the original rationale rather
  than replacing it. Match that spirit, not its literal wording.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supersede the KLJ never-live-confirmed comment with a dated 2026-09-02 confirmation</name>
  <files>server/plane/enrich.py</files>
  <action>
Rewrite ONLY the comment lines between `"WMT": "Wizz Air",` and
`"KLJ": "KlasJet",` (currently `server/plane/enrich.py:584-598`, 15 comment
lines). Do not touch the `"KLJ": "KlasJet",` line itself, the `HOP`/`WMT` rows,
the shared 260827-lgt preamble at lines 550-551, or anything else in the file.

The replacement block has exactly three segments, in this order — the ordering
is load-bearing and machine-checked, because it is what makes the historical
text unmistakably historical to a reader who stops after the first sentence:

Segment 1 — the current truth, first. Open with the row identifier
(`KLJ: KlasJet.`) followed by a dated confirmation stating that a real
KLJ-prefixed flight was observed and confirmed at Orly, developer-confirmed
2026-09-02. Include the literal date string `2026-09-02`. State plainly that
this resolves the open question the rest of this comment records, and that the
observation CONFIRMED the KlasJet attribution rather than contradicting it —
which is why the mapping value is unchanged and only this comment moved. Record
honestly that no specific callsign or flight number was captured at observation
time; do NOT invent, guess, or reconstruct one, and do not imply a fixture or
curl transcript exists for it (none does — the evidence of record is the
developer's own in-session confirmation, and this comment should say so).

Segment 2 — the superseded historical record. Introduce it with a line carrying
the uppercase marker `SUPERSEDED` and the 2026-09-02 resolution date, framing
everything that follows as the pre-2026-09-02 investigation record and stating
explicitly that the original lower-confidence framing no longer applies. Then
retain, as history, the substance the original comment carried: the `QT-lgt-D-06`
citation; that approximately 25 adsbdb queries across plausible flight-number
ranges all returned `unknown callsign` (zero live confirmation at the time); and
the ACMI/wet-lease reasoning — KlasJet is a Lithuanian ACMI/wet-lease and VIP
charter operator, and wet-lease flights typically broadcast the contracting
airline's callsign rather than the operator's own, which is why a real
KLJ-prefixed callsign was expected to appear rarely or never at Orly. Note that
the developer chose to include the row anyway with that uncertainty in hand, and
that the 2026-09-02 observation vindicated that call. Keep the original's
comparison to the `KMM` row above only if it still reads cleanly as history —
it is optional, and a shorter honest paragraph beats a padded one.

Segment 3 — the standing safeguard, last. Keep the Remediation pointer, lightly
reworded so it reads as a permanent invariant rather than an open item: if a
future KLJ callsign is ever seen resolving to a DIFFERENT carrier, this row is
the first thing to re-verify. Frame it as still valid precisely BECAUSE the
2026-09-02 observation confirmed KlasJet and not some other operator.

Style: match the surrounding rows exactly — `# ` prefix at four-space
indentation, wrapped at roughly 72 columns, plain ASCII hyphens (the file uses
`-`, never em-dashes), uppercase reserved for genuinely load-bearing emphasis.
Ruff's `E501` is deliberately disabled in `pyproject.toml`, so wrap width is a
house-style convention here, not a lint gate — follow it anyway. Target roughly
18-24 comment lines; do not pad to hit a count.

Explicitly out of scope for this task (record them in the SUMMARY as follow-up
candidates, change none of them): the same never-live-confirmed framing also
appears in `server/plane/illustrations.py` (module docstring near line 145, the
`"KlasJet"` provenance string near lines 382-405, and a cross-reference comment
near line 507), in `server/assets/icons/illustrations/VENDOR.md` (~line 414),
in `server/assets/icons/illustrations/HANDOFF.md` (~line 359), and in two
`server/test_enrich.py` check DESCRIPTION strings (lines 795-805). The
`test_enrich.py` occurrences were grepped during planning and confirmed to be
descriptive prose only — no test asserts on this comment's wording, and check
38's actual assertion is `airline_from_callsign("KLJ123") == "KlasJet"`, which
this comment-only change cannot affect. That grep settles the planning
assumption; leave all six sites alone.
  </action>
  <verify>
    <automated>python3 - <<'PY'
src = open("server/plane/enrich.py", encoding="utf-8").read().splitlines()
mapping = '    "KLJ": "KlasJet",'
fails = []
if mapping not in src:
    raise SystemExit("FAIL: the KLJ mapping line was changed or removed - it must be byte-identical")
start = src.index('    "WMT": "Wizz Air",')
end = src.index(mapping)
block = "\n".join(src[start + 1:end])
for tok in ("2026-09-02", "SUPERSEDED", "adsbdb", "wet-lease", "Remediation", "QT-lgt-D-06"):
    if tok not in block:
        fails.append("missing required token %r" % (tok,))
if not fails:
    order = [block.index(t) for t in ("2026-09-02", "SUPERSEDED", "adsbdb")]
    if order != sorted(order):
        fails.append("wrong order: the dated confirmation must precede the SUPERSEDED marker, which must precede the historical adsbdb text")
if not all(ln.strip().startswith("#") for ln in src[start + 1:end]):
    fails.append("non-comment line found between the WMT row and the KLJ row")
print("FAIL: " + "; ".join(fails) if fails else "PASS: KLJ comment block (%d lines)" % (end - start - 1,))
raise SystemExit(1 if fails else 0)
PY</automated>
    <automated>server/.venv/bin/python3 server/test_enrich.py</automated>
    <automated>test "$(git diff --name-only HEAD -- . ':(exclude).planning')" = "server/plane/enrich.py"</automated>
  </verify>
  <done>
- The KLJ comment block opens with a `2026-09-02` dated confirmation that a real
  KLJ-prefixed flight was observed at Orly, and states no specific callsign was
  captured.
- An uppercase `SUPERSEDED` marker sits between that confirmation and the
  retained historical record, so no reader can mistake the old framing for a
  live claim.
- The ~25-adsbdb-queries finding, the `QT-lgt-D-06` citation and the wet-lease
  reasoning all still appear, below the marker.
- The Remediation pointer still appears, scoped to a DIFFERENT-carrier
  resolution, phrased as a standing safeguard.
- The gate script above exits 0; `server/test_enrich.py` passes unmodified;
  `server/plane/enrich.py` is the only modified tracked file outside
  `.planning/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Comment-only edit inside a static in-repo lookup table. No input parsing, no I/O, no network, no control flow touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-i4f-01 | Tampering | `_ICAO_AIRLINE_PREFIXES["KLJ"]` | medium | mitigate | A comment edit that slipped into the mapping value would silently change every KlasJet caption AND, via `illustrations.normalise_airline_key()`, the illustration filename resolved for the row. The gate asserts the exact literal line `    "KLJ": "KlasJet",` is present unchanged, and asserts every line in the edited region starts with `#`. |
| T-quick-i4f-02 | Repudiation | provenance of the 2026-09-02 confirmation | low | mitigate | The comment must state plainly that the evidence of record is the developer's in-session confirmation with no captured callsign, rather than implying a fixture or curl transcript that does not exist. The `<action>` forbids inventing a callsign; the SUMMARY records the same. |
| T-quick-i4f-03 | Information disclosure | — | low | accept | No credentials, tokens, hostnames or personal data are involved; the edit adds only a date and an airline name already present in the file. |
| T-quick-i4f-04 | Tampering | scope creep into the five other stale KLJ sites | low | mitigate | `illustrations.py`, VENDOR.md, HANDOFF.md and `test_enrich.py` all carry the same stale framing and are tempting to "finish". The third `<automated>` gate asserts `server/plane/enrich.py` is the sole modified tracked file outside `.planning/`; the follow-up is recorded in the SUMMARY instead. |
</threat_model>

<verification>
1. Run the gate script in Task 1's first `<automated>` block — it must print
   `PASS` and exit 0. It was executed against the pre-change file during
   planning and correctly reported `FAIL` on the two tokens this task adds
   (`2026-09-02`, `SUPERSEDED`) while the three history tokens (`adsbdb`,
   `wet-lease`, `Remediation`) already passed — so it has teeth in both
   directions: it fails if the confirmation is missing, and it fails if the
   historical record is deleted rather than superseded.
2. Read the rewritten block top to bottom as a first-time reader: the first
   sentence must leave no doubt the row is now confirmed, and nothing below the
   `SUPERSEDED` marker may read as a live warning.
3. `git diff HEAD -- server/plane/enrich.py` — every changed line begins with
   `#` (or is whitespace); no line of executable Python appears in the diff.
4. `server/.venv/bin/python3 server/test_enrich.py` passes at its existing check
   count, unmodified.
5. `git diff --name-only HEAD` shows `server/plane/enrich.py` plus only this
   quick task's own `.planning/quick/260902-i4f-*/` artifacts.
</verification>

<success_criteria>
- A reader of `server/plane/enrich.py`'s KLJ row learns, in the first sentence,
  that a real KLJ-prefixed flight was observed at Orly and confirmed
  2026-09-02.
- The never-live-confirmed / materially-lower-confidence framing survives only
  as explicitly-marked history, never as a current claim.
- The original investigation (~25 failed adsbdb probes, the wet-lease callsign
  reasoning, `QT-lgt-D-06`) is preserved, not erased — a future reader can see
  both what was uncertain and how and when it resolved.
- The Remediation pointer remains as a standing safeguard for the
  different-carrier case.
- `"KLJ": "KlasJet"` is byte-identical; zero behavioural change; zero test
  changes; one file touched.
</success_criteria>

<output>
Create `.planning/quick/260902-i4f-update-klj-klasjet-confidence-comment-in/260902-i4f-SUMMARY.md` when done.

Record in it: the six out-of-scope sites that still carry the stale
never-live-confirmed framing (`server/plane/illustrations.py` x3,
`server/assets/icons/illustrations/VENDOR.md`,
`server/assets/icons/illustrations/HANDOFF.md`, `server/test_enrich.py` check
descriptions x2), as an explicit follow-up candidate for a future quick task.
</output>
