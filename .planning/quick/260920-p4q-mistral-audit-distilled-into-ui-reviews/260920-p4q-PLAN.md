---
phase: quick-260920-p4q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ui-reviews/2026-09-20-mistral-companion-audit.md
  - audits/COMPANION_AUDIT_2024.md
  - audits/COMPANION_AUDIT_2026_PART1.md
  - audits/COMPANION_AUDIT_2026_PART2.md
autonomous: true
requirements:
  - QUICK-260920-p4q
must_haves:
  truths:
    - "The repo has exactly one companion-audit document from 2026-09-20, in .planning/ui-reviews/, in French, carrying only code-verified findings."
    - "No fabricated 2024 baseline, no numeric ratings, no star efforts and no invented UX metrics survive anywhere in the repo."
    - "Every claim the orchestrator marked as contradicted by the code is listed once, as closed, so nobody re-opens it."
    - "The audits/ directory no longer exists."
    - "No file outside .planning/ and audits/ is touched."
  artifacts:
    - .planning/ui-reviews/2026-09-20-mistral-companion-audit.md
  key_links:
    - "The distilled document names .planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md as the measured audit whose open findings it corroborates, and states that file is not versioned."
    - "Retained findings point at the existing UI backlog, not at a new phase."
---

<objective>
Replace Mistral's three-file companion audit (commit `d288e27`, pushed straight to `main` on 2026-09-20 with no PR) with ONE distilled French document under `.planning/ui-reviews/`, then delete `audits/`.

Purpose: the audit mixes a handful of usable observations with a fabricated "2024 baseline", figures that confuse bytes with lines, and several claims the code directly contradicts. Left as-is it will be re-read as fact. Distilled, it becomes a short record of what actually holds, plus an explicit closed list so the bad claims are never re-litigated.

Output: `.planning/ui-reviews/2026-09-20-mistral-companion-audit.md` (76 lines of French prose), and `audits/` gone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Notes for the executor:

- Run everything from the worktree `/Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703`. Do NOT use worktree isolation for this work: this repo's isolation forks from `main` rather than from the current branch HEAD, which would place the commits on the wrong base.
- This is a DOC-ONLY change. Do not modify anything under `companion/`, `server/`, `stub-server/`, `hardware/`, `scripts/`, `tests/` or any source file. No code, no tests, no config.
- Do not touch `ROADMAP.md` or `.planning/STATE.md`. The orchestrator owns STATE.md.
- Do not touch `.planning/ui-reviews/.gitignore`.
- Project skill check: `.claude/skills/sketch-findings-skypane/` is the companion's UI-implementation design-system reference (colour, type, spacing, components). It does not apply to a documentation-only task and must not be loaded for this plan.
- Task 1 authors nothing. The complete final text of the document is given verbatim below; write it exactly as given, including accents, French quotation marks and the em dashes.
- One atomic commit per task, conventional message, in task order.
</execution_context>

<context>
@.planning/STATE.md

Style register this document must match (read-only reference, lives in the MAIN checkout and not in this worktree, so it may be unavailable — the text below already conforms and needs no adjustment):
`/Users/florian/Projects/skypane/.planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md`

Source files being retired (no need to read them; everything usable from them is already in Task 1's text):
`audits/COMPANION_AUDIT_2024.md`, `audits/COMPANION_AUDIT_2026_PART1.md`, `audits/COMPANION_AUDIT_2026_PART2.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the distilled French review into .planning/ui-reviews/</name>
  <files>.planning/ui-reviews/2026-09-20-mistral-companion-audit.md</files>
  <action>
Create `.planning/ui-reviews/2026-09-20-mistral-companion-audit.md` with the Write tool, using EXACTLY the text in the fenced block below, byte for byte, with no additions, no reordering, no reformatting and no editorial improvement. Do not append a signature, a date footer or a table of contents. Do not convert the em dashes or the French quotation marks. The text has already been fact-checked against the codebase at `d288e27` and every figure in it was re-measured with `wc`; changing a number would reintroduce the error this task exists to remove.

Two things to preserve deliberately, because they look like omissions and are not: there is no rating, no score, no priority emoji and no effort rating anywhere (the source audit's numeric ratings were invented), and the "Écarté" table keeps claims the source audit made even though they are false, precisely so that nobody re-opens them later.

Write the file at the path above and nowhere else. Do not create any other file. Do not modify `.planning/ui-reviews/.gitignore`.

```markdown
# Audit Mistral du companion — 20 septembre 2026

## Provenance

Cet audit n'a pas été produit par le projet. Il a été écrit par « Vibe Code » (Vibe Nuage Agent, Mistral AI) et poussé directement sur `main` le 20 septembre 2026, commit `d288e27`, sans pull request ni relecture.

Il arrivait en trois fichiers sous `audits/` : une partie 2026 en deux morceaux et un prétendu audit de référence « 2024 ». Le présent document les remplace : tout ce qui suit est ce qui reste après vérification contre le code, et le répertoire `audits/` a été supprimé.

## Fiabilité

Trois réserves, qui expliquent pourquoi cet audit est condensé ici plutôt que conservé tel quel.

D'abord la ligne de base. Le fichier « 2024 » a été créé dans le même commit que les deux autres, et le premier commit du dépôt date du 2026-08-04 : il n'a jamais existé de code SkyPane en 2024, donc aucun audit de 2024. Toute la narration « évolution depuis 2024 », les notes chiffrées, les gains de points et la liste des « problèmes résolus depuis 2024 » sont une mise en scène et ont été retirées en bloc — avec les notes sur dix, les efforts en étoiles, et les métriques UX (satisfaction, taux d'erreur, temps par tâche) qui ne reposent sur aucune mesure.

Ensuite les chiffres, remesurés un par un. `companion/pages/config_page.py` fait 6 460 lignes, et non « 355 794 lignes » : l'audit a pris des octets pour des lignes, 356 Ko étant juste et sa conversion en lignes fausse. Les tests font 66 622 lignes sur 7 fichiers, et non « environ 85 000 ». Il y a 17 fichiers JavaScript, et non 14. En revanche `companion/static/style.css` fait bien 10 546 lignes et 506 Ko, et l'internationalisation compte bien 13 modules.

Enfin la méthode. L'audit ne mesure rien lui-même : aucun viewport, aucune hauteur de page, aucune cible tactile, aucune capture. Plusieurs de ses constats sont contredits par le code du dépôt, listés plus bas sous « Écarté ». Ce qui suit est ce qui survit à la relecture.

## Constats retenus

### Vols n'a pas de pagination

La page liste tous les vols sans découpage. Retenu parce que le constat est vérifiable et déjà mesuré ailleurs : l'audit du 17 septembre 2026 le classe P1, avec 36 cartes et une page de 6 710 px sur téléphone, et recommande 10 à 15 vols suivis d'un bouton d'affichage progressif. L'audit Mistral confirme ce constat sans y ajouter de mesure.

### Appareil et Affichage exposent trop de prose avant l'action

Formulé comme « interface un peu technique » pour Appareil, et trop d'explications pour Affichage. Retenu parce qu'il recoupe exactement le P1 « trop d'explications avant les actions » de l'audit du 17 septembre, qui relève deux écrans de lecture avant les deux réglages d'Appareil.

### Compagnies reste difficile à parcourir

Formulé comme « interface un peu complexe ». Retenu au titre de corroboration du P2 du 17 septembre : sur téléphone, la galerie des compagnies connues arrive après le titre, deux textes d'introduction, l'action d'édition et la section des compagnies non identifiées.

### Deux fichiers sont devenus monolithiques

`companion/pages/config_page.py` (6 460 lignes, 356 Ko) et `companion/static/style.css` (10 546 lignes, 506 Ko). Retenu comme dette de maintenabilité, pas comme défaut critique : aucun seuil n'a été fixé pour ce projet, aucune régression n'est attribuée à ces tailles, et un découpage n'apporte rien à l'utilisateur. À traiter quand une phase touche déjà largement l'un des deux, pas pour lui-même. La remarque voisine sur la duplication de motifs HTML relève du même sac, elle aussi sans mesure derrière elle.

### Peu d'états de chargement visibles — mineur, non vérifié

Trois occurrences seulement de `aria-busy`, spinner ou loading dans les assets statiques, et `quick-switch.js` procède par bascule optimiste. Retenu comme note mineure, à confirmer par une mesure réelle avant toute action : ce n'est pas un défaut établi.

## Ce qui fonctionne déjà bien

L'audit est juste sur les forces, et elles sont vérifiables dans le code :

- aucune dépendance externe ; serveur en bibliothèque standard Python, HTML rendu côté serveur, pas d'étape de build ;
- une seule source de navigation, `NAV_GROUPS`, rendue par `sidebar_nav()` en desktop et `_tab_bar_html()` en mobile, complétée par le panneau compte et préférences ;
- des tokens CSS — échelle d'espacement par pas de 4 px, quatre tailles de texte, tokens de mouvement — les thèmes clair et sombre, et `prefers-reduced-motion` respecté ;
- `companion/contrast_check.py`, qui impose les contrastes WCAG AA et une distance d'au moins 28 dE76 entre l'accent et les couleurs de statut ;
- une CSP en place, avec `script-src 'self'` et aucun script inline ;
- l'internationalisation FR et EN sur 13 modules ;
- la limitation des tentatives de connexion (`LoginThrottle`).

## Écarté

Ces constats de l'audit sont clos. Ils ne doivent pas être rouverts sans élément nouveau.

| Constat de l'audit | Pourquoi il est écarté |
|---|---|
| « Pas de rate limiting » | Faux : `companion/auth.py` implémente `LoginThrottle`, avec un seuil d'échecs et un verrouillage de 300 s. |
| « CSP trop permissive, retirer `unsafe-inline` » | `script-src 'self'` ne porte déjà aucun `unsafe-inline` ; seul `style-src` en a un, documenté comme délibéré dans `companion/app.py` pour sept styles inline. |
| « Pas de focus trap pour les modales » | `panel-lookup.js` documente l'emploi de l'élément natif `<dialog>` précisément pour ses sémantiques natives de fermeture au clavier, d'arrière-plan et de piégeage du focus. Choix assumé. |
| « Pas de tooltips » | Quinze attributs `title` existent dans `layout.py` et les pages. |
| « Peu de régions `aria-live` » | Dix occurrences dans le rendu Python ; l'affirmation n'est étayée par aucun relevé. |
| « ES5 seulement, passer à ES6+ » | Le sous-ensemble ES5 est une contrainte documentée du projet — pas d'étape de build, pas de bundler — rappelée en tête de plusieurs scripts. Refus assumé, pas défaut. |
| « Breadcrumbs partiellement implémentés » | Aucun fil d'Ariane n'existe dans `companion/`. L'audit se contredit d'ailleurs lui-même en déplorant ailleurs leur absence complète. |
| « Pas de breadcrumbs », « pas de bouton Retour » | Sans objet : six pages de premier niveau, aucune imbrication, barre latérale permanente en desktop et barre d'onglets en mobile. |
| « Health remonté dans la barre mobile » et « bouton More pour Health et Device » | Les deux affirmations se contredisent dans le même document ; aucune n'est retenue. |
| Toute ligne « problème résolu depuis 2024 » | Ligne de base inventée, voir « Fiabilité ». |
| « Pas de raccourcis clavier » | Vrai, mais sans valeur ici : application de réglages sur six pages, avec navigation permanente. |
| « Adopter BEM », « JSDoc ou TypeScript », « tests unitaires JS », « bibliothèque utilitaire JS », « moderniser le design » | Recommandations génériques, sans mesure ni symptôme observé dans ce dépôt. |

## Suite

Les constats retenus ne justifient pas une phase à eux seuls. Vols, la prose d'Appareil et d'Affichage, et l'ordre de Compagnies sont déjà les trois premières lignes de l'ordre recommandé par l'audit du 17 septembre 2026 (`.planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md`, relevé mesuré, non versionné à ce jour) : ils rejoignent cet arriéré-là au lieu d'en ouvrir un second.

La dette de maintenabilité des deux gros fichiers reste hors de cet arriéré d'interface, à traiter par opportunité.
```

Then commit, staging only this one file:

`git add .planning/ui-reviews/2026-09-20-mistral-companion-audit.md`
`git commit -m "docs(quick-260920-p4q): distill Mistral companion audit into ui-reviews"`
  </action>
  <verify>
    <automated>F=.planning/ui-reviews/2026-09-20-mistral-companion-audit.md; test -f "$F" && L=$(wc -l < "$F") && echo "lines=$L" && [ "$L" -ge 70 ] && [ "$L" -le 120 ] && [ "$(grep -c 'd288e27' "$F")" -ge 1 ] && [ "$(grep -c '2026-08-04' "$F")" -ge 1 ] && [ "$(grep -ci 'sur 10\|/10' "$F")" -eq 0 ] && [ "$(grep -c '6 460' "$F")" -ge 1 ] && [ "$(grep -c '66 622' "$F")" -ge 1 ] && [ "$(grep -c '355 794' "$F")" -eq 1 ] && [ "$(grep -cE '^## (Provenance|Fiabilité|Constats retenus|Ce qui fonctionne déjà bien|Écarté|Suite)$' "$F")" -eq 6 ] && [ "$(grep -c '2026-09-17-companion-ui-ux-audit.md' "$F")" -ge 1 ] && [ "$(grep -c '⭐\|✅\|⚠️\|🔴\|🟡\|🟢\|📊\|🎯' "$F")" -eq 0 ] && echo GATE-OK</automated>
    <automated>git show --stat --name-only --format= HEAD | grep -v '^$' | grep -vc '^\.planning/ui-reviews/2026-09-20-mistral-companion-audit\.md$' | grep -qx 0 && echo COMMIT-SCOPE-OK</automated>
  </verify>
  <done>`.planning/ui-reviews/2026-09-20-mistral-companion-audit.md` exists with 76 lines; the six required section headings are present in order; the corrected figures (6 460, 66 622, 17, 10 546) appear and `355 794` appears only once, inside the sentence that corrects it; no rating, priority emoji or star effort survives; the commit touches that file and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the audits/ directory</name>
  <files>audits/COMPANION_AUDIT_2024.md, audits/COMPANION_AUDIT_2026_PART1.md, audits/COMPANION_AUDIT_2026_PART2.md</files>
  <action>
Remove the three retired source files and the directory that holds them, then commit. The directory contains exactly these three tracked files and nothing untracked, so `git rm -r` clears it completely:

`git rm -r audits/`

Confirm the directory itself is gone from the working tree (git removes an empty directory after the last tracked file leaves it; if an empty `audits/` remains for any reason, `rmdir audits` it — do not use `rm -rf` on anything else).

Then commit:

`git commit -m "docs(quick-260920-p4q): remove audits/, superseded by the distilled review"`

Do not delete, move or rewrite anything else. `.planning/ui-reviews/.gitignore` stays exactly as it is.
  </action>
  <verify>
    <automated>test ! -d audits && [ "$(git ls-files audits/ | wc -l | tr -d ' ')" -eq 0 ] && test -f .planning/ui-reviews/.gitignore && test -f .planning/ui-reviews/2026-09-20-mistral-companion-audit.md && echo REMOVAL-OK</automated>
    <automated>test -z "$(git status --porcelain)" && git diff --name-only HEAD~2 HEAD | grep -cvE '^(\.planning/ui-reviews/2026-09-20-mistral-companion-audit\.md|audits/)' | grep -qx 0 && echo DOC-ONLY-OK</automated>
  </verify>
  <done>`audits/` does not exist as a directory and has no tracked files; `git status --porcelain` is empty; the two commits together touch only the new review file and the three removed audit files — no source file, no `ROADMAP.md`, no `STATE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Documentation-only change. No runtime code, no request handling, no authentication path, no dependency install, no new input is introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260920-p4q-01 | Information disclosure | `.planning/ui-reviews/2026-09-20-mistral-companion-audit.md` | low | mitigate | The document names files, a CSP directive and a throttle constant, but no secret, no token, no host and no credential. Task 1's text is fixed and reviewed; the executor authors nothing. |
| T-quick-260920-p4q-02 | Tampering | working tree outside `.planning/` and `audits/` | low | mitigate | Both tasks pin an explicit file list, Task 2 forbids `rm -rf`, and each task's second automated gate fails if any commit touches a path outside the two allowed prefixes. |
| T-quick-260920-p4q-03 | Repudiation | audit provenance | low | accept | The retired files are removed from the tip but stay recoverable in history at `d288e27`; the distilled document records author, commit and push route, so provenance is not lost. |

No package-manager install occurs in this plan, so the package legitimacy gate does not apply and no `T-*-SC` entry is required.
</threat_model>

<verification>
Run from the worktree root, after both commits:

1. `test -f .planning/ui-reviews/2026-09-20-mistral-companion-audit.md && wc -l .planning/ui-reviews/2026-09-20-mistral-companion-audit.md` — file exists, 76 lines.
2. `test ! -d audits && echo "audits gone"` — the directory is gone.
3. `grep -ci 'sur 10\|/10' .planning/ui-reviews/2026-09-20-mistral-companion-audit.md` — returns 0.
4. `grep -c 'd288e27' .planning/ui-reviews/2026-09-20-mistral-companion-audit.md` and `grep -c '2026-08-04' ...` — each at least 1.
5. `git status --porcelain` — empty.
6. `git log --oneline -2` — two commits, both prefixed `docs(quick-260920-p4q):`.
7. `git diff --name-only HEAD~2 HEAD` — exactly four paths: the new review file and the three removed audit files.
</verification>

<success_criteria>
- One French document at `.planning/ui-reviews/2026-09-20-mistral-companion-audit.md`, written verbatim from Task 1, 76 lines, sections Provenance / Fiabilité / Constats retenus / Ce qui fonctionne déjà bien / Écarté / Suite.
- Zero ratings, zero priority emoji, zero star efforts, zero invented UX metrics, zero surviving reference to a 2024 baseline other than the paragraph that debunks it.
- Every figure matches the re-measured values; `355 794` appears only as the corrected error.
- Every code-contradicted claim appears once in the "Écarté" table with its one-line reason.
- `audits/` removed, directory included, with no tracked files left under it.
- Two atomic commits, conventional messages, nothing outside `.planning/ui-reviews/` and `audits/` modified.
</success_criteria>

<output>
Create `.planning/quick/260920-p4q-mistral-audit-distilled-into-ui-reviews/260920-p4q-SUMMARY.md` when done.
</output>
