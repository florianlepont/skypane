---
phase: quick-260902-j21
plan: 01
subsystem: ui
tags: [companion, device_config, runway-picker, static-assets]

requires:
  - phase: 06.4
    provides: "companion/static/RUNWAY-IMAGES.md's drop-in asset contract, runway_images_available(), runway_fieldset()'s conditional <img>"
provides:
  - "Three tracked runway diagram PNGs at companion/static/runway-{3,06-24,02-20}.png, now shipped by deploy/deploy.sh's wholesale companion/ rsync"
  - "server/device_config.py RUNWAYS[id]['label'] values relabelled to the official Aeroport de Paris Piste numbers (Piste 3/4/2)"
affects: [companion-config-page, device_config]

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - companion/static/runway-3.png
    - companion/static/runway-06-24.png
    - companion/static/runway-02-20.png
  modified:
    - server/device_config.py

key-decisions:
  - "Runway dict KEYS (3/06-24/02-20) stay byte-identical to before — they are the persisted tracked_runway config value, the RUNWAY_IDS validation set, the CFG-12 geofence consistency check, and the runway-{id}.png filename stem; only the label field text changed."
  - "tag_text/empty_heading (the panel-facing English strings rendered by server/plane/render.py) are deliberately left untouched — the French Piste vocabulary is scoped to the companion web picker only."

requirements-completed: [QT-j21-01, QT-j21-02]

coverage:
  - id: D1
    description: "Three pre-verified runway diagram PNGs tracked in git so deploy/deploy.sh ships them to the VPS"
    requirement: "QT-j21-01"
    verification:
      - kind: unit
        ref: "Pillow decode + non-degenerate size assertion (plan Task 1 verify block)"
        status: pass
      - kind: unit
        ref: "git ls-files / git status / git show --name-only (plan Task 1 verify block, staged-set + commit-set exactness)"
        status: pass
    human_judgment: false
  - id: D2
    description: "RUNWAYS registry labels relabelled to official Piste 3/4/2 numbers, with keys/tag_text/empty_heading byte-identical"
    requirement: "QT-j21-02"
    verification:
      - kind: unit
        ref: "server/device_config.py data-level assertions (plan Task 2 verify block, inline script)"
        status: pass
      - kind: unit
        ref: "server/test_config_history.py"
        status: pass
      - kind: unit
        ref: "companion/test_config_page.py"
        status: pass
    human_judgment: false

duration: ~15min
completed: 2026-09-02
status: complete
---

# Quick Task 260902-j21: Commit the 3 runway diagram PNGs and simplify runway labels to Piste numbers Summary

**Tracked three previously-untracked runway diagram PNGs and relabelled the Config page's runway picker from aviation heading-pair IDs to Orly's own Piste 3/4/2 signage vocabulary, leaving all storage keys and panel-facing strings untouched.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-09-02T11:52:35Z
- **Tasks:** 2/2 completed
- **Files modified:** 4 (3 created, 1 modified)

## Accomplishments
- `companion/static/runway-3.png`, `companion/static/runway-06-24.png`, `companion/static/runway-02-20.png` committed byte-for-byte (each re-verified as a genuine, non-degenerate PNG via Pillow before staging), closing `companion/static/RUNWAY-IMAGES.md`'s drop-in asset contract — the Config page's runway picker now renders a real `<img>` for all three registered runways instead of the D-03 text-only fallback.
- `server/device_config.py`'s `RUNWAYS` dict `label` values changed to `Piste 3` / `Piste 4` / `Piste 2` (from `Runway 3 (07/25)` / `Runway 06/24` / `Runway 02/20`), matching the official Aeroport de Paris runway-works diagram's own vocabulary.
- Dict keys (`3`/`06-24`/`02-20`), `RUNWAY_IDS`, `DEFAULT_RUNWAY_ID`, and all six `tag_text`/`empty_heading` values confirmed byte-identical before and after — the panel-facing render surface (`server/plane/render.py`) is unaffected by design.
- Extended the module's dated provenance comment block (matching the `THEMES` registry's own established convention) with a 2026-09-02 paragraph recording the Piste mapping, the key-stability rationale, and the deliberate panel-field exclusion.

## Task Commits

1. **Task 1: Track and commit the three runway diagram PNGs** - `0725196` (feat)
2. **Task 2: Simplify the RUNWAYS registry labels to official Piste numbers** - `353b95f` (feat)

**Plan metadata:** committed via `gsd_run query commit` alongside this summary.

## Files Created/Modified
- `companion/static/runway-3.png` - Runway diagram for the default/tracked runway 07-25 (Piste 3); 1672x941 PNG
- `companion/static/runway-06-24.png` - Runway diagram for runway 06-24 (Piste 4); 1672x941 PNG
- `companion/static/runway-02-20.png` - Runway diagram for runway 02-20 (Piste 2); 1672x940 PNG
- `server/device_config.py` - `RUNWAYS` dict `label` values relabelled; dated provenance comment extended

## Decisions Made
- Kept all three `RUNWAYS` dict keys, `RUNWAY_IDS`, `DEFAULT_RUNWAY_ID`, `tag_text`, and `empty_heading` values byte-identical — only `label` changed, per the plan's explicit scope boundary and the key-to-asset-filename dependency it documents.
- Extended the existing dated-provenance-comment convention (matching the `THEMES` registry's own pattern) rather than replacing the prior CFG-12/D-26/D-27/D-28 paragraph, to preserve the historical record.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched their `<action>` and `<verify>` blocks with no auto-fixes required.

## Issues Encountered

None. `server/test_poll_loop.py` (the one harness this plan's own `<verification>` block flagged as carrying a known pre-existing macOS/Linux digest-mismatch risk) reported a clean 44/44 pass locally when run standalone and as part of the full suite — the same result before and after this task's two commits (neither change touches rendering code or any `RUNWAYS` field `render.py` reads), so no regression occurred. The full `scripts/run-all-tests.sh` run confirms `Result: PASS` across all 15 harnesses at 91% coverage.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

This closes the last two loose ends from Phase 06.4 (`companion/static/RUNWAY-IMAGES.md`'s asset contract and the runway-label vocabulary mismatch). No follow-on work identified; the project's roadmap shows Phase 9 (Diagonal band theme) as the last phase, already complete.

---
*Quick task: 260902-j21*
*Completed: 2026-09-02*

## Self-Check: PASSED

All created files verified present on disk; both task commit hashes (`0725196`, `353b95f`) verified present in git log.
