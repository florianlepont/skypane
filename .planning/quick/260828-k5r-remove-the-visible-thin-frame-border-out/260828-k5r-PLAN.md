---
phase: quick-260828-k5r
plan: 260828-k5r
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - server/plane/render.py
  - server/test_render.py
  - server/test_poll_loop.py
  - .planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md
autonomous: false
requirements: [QUICK-260828-k5r]

must_haves:
  truths:
    - "A real `build_canvas()` departing render and a real arriving render both show the state's own flat background colour everywhere along the former D-26 outline (the 30px inset rectangle `(30, 30, 1170, 1570)`, 2px stroke) — no ink line anywhere on that band, at either stroke row, on all four edges, and at both sampled corners."
    - "The empty state is byte-identical to before this change: it never called the frame helper, so `render_panel(flight, 'empty')` must still hash to the exact pre-edit macOS value `657b80f53098430e367f819831c390f15e49b703b4f4ca47138772be31491e78`."
    - "The ONLY pixels that moved in the departing/arriving renders lie on that 2px frame band — illustration sizing and centring, previous-card right-alignment, both text blocks, the top labels, the CFG-05 badge, and the battery icon are all provably untouched, because re-drawing the frame back on top of a post-edit canvas reproduces the pre-edit digest exactly."
    - "`draw_frame()` itself, `FRAME_INSET_FRAC` (0.025) and `FRAME_STROKE_PX` (2) all survive unmodified — the constants are still load-bearing for `_build_active_canvas()`'s `inner_width` derivation and for `draw_source_fault_badge()`'s own bottom-anchoring, and the two existing harness checks that call the helper directly on a throwaway canvas (checks 16 and 70) still pass with zero edits."
    - "`server/test_render.py` gained exactly one new check proving the border is genuinely absent from the REAL render path (not the helper in isolation), `EXPECTED_CHECK_COUNT` moved 71 -> 72, and that new check was observed FAILING against the unmodified code before the removal landed."
    - "`server/test_poll_loop.py`'s pinned `_DEFAULT_CONFIG_DIGEST` is re-pinned to a value read out of a real GitHub Actions ubuntu-latest FAIL line — never from a local macOS run and never from a container — honouring that file's own standing rule, with a dated re-pin note explaining that FLIGHT1's pixels genuinely moved because the outline was removed."
    - "`.planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md` records the removal as a dated amendment rather than silently continuing to describe a frame the panel no longer draws."
  artifacts:
    - path: "server/plane/render.py"
      provides: "`_build_active_canvas()` with the frame-drawing side-effect call deleted and its stale D-26 comment replaced by one describing what the code now actually does"
      contains: "FRAME_INSET_FRAC"
    - path: "server/test_render.py"
      provides: "Check 72 — the real-render-path absent-border regression, sampling seven points on the former outline across both active states — and EXPECTED_CHECK_COUNT = 72"
      contains: "EXPECTED_CHECK_COUNT = 72"
    - path: "server/test_poll_loop.py"
      provides: "A CI-derived `_DEFAULT_CONFIG_DIGEST` plus a dated re-pin note naming this quick task as the cause"
      contains: "_DEFAULT_CONFIG_DIGEST"
    - path: ".planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md"
      provides: "A dated amendment under Canvas and Geometry recording that the outline is no longer drawn while the inset constant survives as pure layout geometry"
      contains: "no longer drawn"
  key_links:
    - from: "_build_active_canvas()'s deleted frame call"
      to: "the seven sampled pixels in test_render.py check 72, which must read state_background_index(state) instead of state_ink_index(state)"
    - from: "_build_active_canvas()'s rendered pixel output"
      to: "test_poll_loop.py's _DEFAULT_CONFIG_DIGEST byte-identity gate (check 10), which run_once() feeds via panel.bin"
    - from: "FRAME_INSET_FRAC (retained)"
      to: "_build_active_canvas()'s inner_width -> main illustration width, AND draw_source_fault_badge()'s frame_inset/frame_bottom anchoring — both must keep producing identical geometry"
---

<objective>
Remove the visible thin outline ("le liseret") from the rendered e-ink panel by deleting the single production call to `draw_frame()` in `_build_active_canvas()`, and prove — with new automated coverage on the REAL render path, not on the helper in isolation — that the line is genuinely gone and that nothing else about the composition moved.

Purpose: the developer judged the border visually unwanted on the physical Spectra 6 panel. Today no test asserts the border's presence or absence in a real `build_canvas()` output — only `draw_frame()` in isolation is exercised — so the removal would otherwise ship unguarded.
Output: a borderless active-state panel, one new regression check, a CI-re-pinned render digest, and a dated UI-spec amendment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@server/plane/render.py
@server/test_render.py

Verified facts established during planning — treat these as measured, not assumed:

- `draw_frame()` (render.py lines 399-407) is called exactly ONCE in production code: `_build_active_canvas()` line 1148, purely for its side effect. The call site discards the returned box.
- `_build_active_canvas()` line 1156 recomputes `inner_width` from `FRAME_INSET_FRAC` directly, and `draw_source_fault_badge()` line 429 recomputes `frame_inset`/`frame_bottom` from `FRAME_INSET_FRAC` directly. Neither depends on the helper being called or on its return value. Removing the call therefore cannot move any layout.
- `_build_empty_canvas()` (line 1029) never calls the helper — the empty state already renders borderless today.
- Geometry: inset = round(1200 x 0.025) = 30; box = (30, 30, 1170, 1570); stroke 2px, drawn inward.
- Pre-edit pixel probe on this macOS checkout, default theme: for departing, `state_background_index` = 4 and `state_ink_index` = 1; for arriving, background = 5 and ink = 1. All seven of these points currently read 1 (ink) in BOTH states: (600, 30), (600, 31), (30, 800), (1170, 800), (600, 1570), (30, 30), (1170, 1570). After the removal every one of them must read the state's own background index. This is what makes the new check provably meaningful rather than vacuous.
- Pre-edit macOS `render_panel()` sha256 baselines: empty `657b80f53098430e367f819831c390f15e49b703b4f4ca47138772be31491e78`, departing `f7edbb7faa13c51837ca40222ae5c2bf689c2436acfb172f840f4cb892072a1d`, arriving `60c42f0d2ca60aed74b2bad411ea5ba06f126d6984603ccfeae8e4f05e48ab7c`.
- Pre-edit harness baselines: `server/test_render.py` is 71/71 green. `server/test_poll_loop.py` is 42/43 and exits 1, with exactly one FAIL — the pinned-digest check, reporting local macOS digest `ea555e8b68e9387bef30dd93088c712707b0c30ad1bdf53a336841c04ea8e5b3` against the pinned Linux value `45b17a3e...`. That single failure is the PRE-EXISTING, documented macOS-vs-Linux FreeType rasterization split (test_poll_loop.py lines 102-136), not something this change introduces. Do not "fix" it locally.
- `_DEFAULT_CONFIG_DIGEST` (test_poll_loop.py line 137) is the only pinned 64-hex literal in any harness in this repo. `server/test_pipeline_e2e.py` and `companion/test_companion_app.py` compute hashes dynamically and need no re-pin.
- All harnesses run under `server/.venv/bin/python3`, never bare system python3. `scripts/run-all-tests.sh` drives all 15.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the border is drawn, then remove it</name>
  <files>server/plane/render.py, server/test_render.py</files>
  <behavior>
    New check 72, appended to `server/test_render.py` after the existing runway/theme matrix check (currently the last check, ending around line 1753) and before the `total = len(results)` tally:

    - For each of the two active states, "departing" and "arriving": build a real canvas via `render.build_canvas(TEST_FLIGHT, state, route=TEST_ROUTE)` — the default theme, no source_fault, no battery_low.
    - Derive the sample points from the module constants, never from hardcoded pixel literals, so a future change to the inset can never make this check silently vacuous. Compute inset as `round(panel_format.WIDTH * render.FRAME_INSET_FRAC)`, then probe: the top edge at both stroke rows (WIDTH//2 at inset, and WIDTH//2 at inset+1), the left and right edges at mid-height (inset at HEIGHT//2, and WIDTH-inset at HEIGHT//2), the bottom edge (WIDTH//2 at HEIGHT-inset), and the two diagonal corners (inset/inset, and WIDTH-inset/HEIGHT-inset).
    - Every probe must equal `render.state_background_index(state)` for that state. Assert equality against the background index rather than mere inequality against the ink index — that is the stronger claim and it is what "the mood colour shows through" actually means.
    - On failure the message must name the offending point, the state, the index read, and the expected background index, matching the surrounding checks' diagnostic style.
    - Expected RED behaviour before the removal lands: this check FAILS for both states at the very first probe, reading index 1 (ink) where background was expected.
    - Expected GREEN behaviour after the removal: 72/72.
  </behavior>
  <action>
    Work in this order — the RED observation is the point of the task and must not be skipped.

    Step 1 (RED). Add check 72 to `server/test_render.py` exactly as the behavior block describes, and bump `EXPECTED_CHECK_COUNT` on line 38 from 71 to 72. Follow this harness's own house idioms: an inner `def _...()` returning a `(bool, reason)` tuple, registered through the existing `check(name, fn)` helper, using the existing module-level `TEST_FLIGHT`/`TEST_ROUTE` fixtures and the `canvas.getpixel(...)` sampling style already used by the battery-icon probes around line 1144. Do not invent a new spy or helper class. Run `server/.venv/bin/python3 server/test_render.py` and CONFIRM it reports 71/72 with the new check as the sole FAIL. Record the exact FAIL line in the summary — that line is the evidence the border was really being drawn on the real render path.

    Step 2 (GREEN). In `server/plane/render.py`, delete the one-line frame-drawing side-effect call on line 1148 inside `_build_active_canvas()` — the statement that passes the canvas and `fg_idx` to the frame helper — and replace the now-stale comment on line 1147 ("D-26: thin frame, inset ~2.5% of canvas width from every edge.") with a comment saying the D-26 outline is no longer drawn (removed 2026-08-28 by developer request, quick task 260828-k5r), while noting that `FRAME_INSET_FRAC` deliberately survives below as pure layout geometry feeding `inner_width`, and that `draw_source_fault_badge()` independently derives its own bottom anchor from the same constant. Write that replacement comment referring to the helper by its bare name only — do NOT write its call form (name immediately followed by an open paren and the canvas argument) anywhere in the comment, because the verification greps for exactly that call form and a comment containing it would self-invalidate the gate. <!-- planner-discipline-allow: draw_frame(canvas -->

    Step 3. Do NOT delete or edit the frame helper function itself (lines 399-407), and do NOT change `FRAME_INSET_FRAC` or `FRAME_STROKE_PX` (lines 257-258). Leave the two existing harness checks that call the helper directly on their own throwaway canvas — check 16 ("draw_frame() draws a thin outline at the ~2.5%-of-width inset (D-26)", around line 531) and check 70 ("draw_source_fault_badge()'s bounding box stays inside the drawn frame", around line 1729) — completely untouched; they exercise the helper in isolation and must keep passing with zero edits.

    Step 4. Re-run the render harness and confirm 72/72, with checks 16 and 70 both still PASS.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py 2>&1 | tail -1 | grep -q 'render: 72/72 checks pass' &amp;&amp; server/.venv/bin/python3 server/test_render.py 2>&1 | grep -q "PASS draw_frame() draws a thin outline" &amp;&amp; server/.venv/bin/python3 server/test_render.py 2>&1 | grep -q "PASS draw_source_fault_badge()'s bounding box stays inside the drawn frame" &amp;&amp; test "$(grep -c 'draw_frame(canvas' server/plane/render.py)" = "0" &amp;&amp; grep -q 'EXPECTED_CHECK_COUNT = 72' server/test_render.py &amp;&amp; grep -q 'FRAME_INSET_FRAC = 0.025' server/plane/render.py &amp;&amp; grep -q 'FRAME_STROKE_PX = 2' server/plane/render.py</automated>
  </verify>
  <done>
    The render harness is 72/72. The new check was observed failing 71/72 against the unmodified code first, and that FAIL line is recorded. No call form of the frame helper remains anywhere in render.py. The helper definition and both frame constants are unchanged, and the two isolation checks still pass unedited.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove zero collateral change, re-pin the CI digest, amend the UI spec</name>
  <files>server/test_poll_loop.py, .planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md</files>
  <action>
    Step 1 — prove nothing but the outline moved. Write a throwaway verification script in the scratchpad directory (NOT in the repo, NOT a committed test) that imports `server.plane.render` and asserts three things.

    First: `render_panel(TEST_FLIGHT, 'empty')` still hashes to `657b80f53098430e367f819831c390f15e49b703b4f4ca47138772be31491e78`. Byte-identity here is the proof that empty-state rendering is untouched.

    Second: the departing and arriving digests have MOVED away from `f7edbb7faa13c51837ca40222ae5c2bf689c2436acfb172f840f4cb892072a1d` and `60c42f0d2ca60aed74b2bad411ea5ba06f126d6984603ccfeae8e4f05e48ab7c` respectively. They must differ — if either still matches, the removal did not take effect on the real path.

    Third, and this is the load-bearing one: for each active state, build a post-edit canvas, call the frame helper on it with `state_ink_index(state)` to paint the outline back on top, pack the result with the same packing function `render_panel` uses, and confirm the sha256 now equals the pre-edit baseline for that state exactly. An exact match proves the frame band was the ONLY difference and that nothing had been painted over the band — i.e. illustration sizing and centring, previous-card alignment, both text blocks, the top labels, the badge anchor and the battery icon are all provably unaffected. If either state does NOT match, fall back to a per-pixel diff between the reconstructed and baseline canvases and confirm every differing coordinate lies on the band (x in 30..31 or 1169..1170 for any y in 30..1570, or y in 30..31 or 1569..1570 for any x in 30..1170); report the finding either way and do not proceed silently.

    Step 2 — establish the local suite baseline honestly. Run `./scripts/run-all-tests.sh`. Expect `server/test_poll_loop.py` to be the ONLY red harness, at 42/43, with the sole FAIL being the pinned-digest check now reporting a NEW local macOS digest different from the pre-edit `ea555e8b68e9387bef30dd93088c712707b0c30ad1bdf53a336841c04ea8e5b3`. That change is the confirmation that the production poll path really renders borderless now. Every other harness must be green; any NEW red harness is a genuine regression and must be investigated before going further. Do not attempt to re-pin the digest from this local macOS value.

    Step 3 — re-pin from real CI, per that file's own standing rule. `server/test_poll_loop.py` lines 102-136 forbid pinning from any local or containerized computation: the value must come from an actual GitHub Actions ubuntu-latest FAIL line, because macOS and Linux FreeType rasterize text differently and a prior attempt to shortcut this through a `python:3.12-slim` container produced a third wrong value. So: commit Task 1's work plus this task's UI-spec amendment, then ASK THE DEVELOPER for explicit approval to push branch `claude/debug-skypane-0c21a9` (pushing is what triggers `.github/workflows/ci.yml` on ubuntu-latest). After approval, push, wait for the run, and read the failing line with `gh run view --log-failed` (or `gh run view <id> --log`), extracting the new digest from the harness's own message of the form "panel.bin digest NEW != pinned 45b17a3e...". Update line 137 to that CI-reported value and append a dated re-pin note to the comment block directly above it, in the same voice as the existing notes: RE-PINNED 2026-08-28 for quick task 260828-k5r, the D-26 outline was removed from the active render path so the FLIGHT1 fixture's pixels genuinely move — an expected new digest, not a regression — and the value below was read from the CI FAIL output, not computed locally. Then push again and confirm CI goes green.

    Step 4 — amend the design record. In `.planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md`, directly under the Canvas and Geometry constants table (the table on lines 44-48 listing `FRAME_INSET_FRAC` and `FRAME_STROKE_PX`), add a short dated amendment: as of 2026-08-28 (quick task 260828-k5r) the outline is no longer drawn on the active-state panel at the developer's request; `FRAME_INSET_FRAC` survives purely as layout geometry — it still derives the main illustration's `inner_width` and still anchors the CFG-05 source-fault badge — and `draw_frame()` itself is retained but no longer called from the render path. Keep it to an appended note. Do NOT rewrite or delete the surrounding prose that describes the frame historically (lines 50, 172, 185): this project's established practice is that point-in-time records stay intact and corrections are appended, dated.
  </action>
  <verify>
    <automated>test "$(grep -c '45b17a3e46e02f67575206ea2946633210b557a61bee5cd9bcc1ffd8ac654b5d' server/test_poll_loop.py)" = "1" &amp;&amp; grep -q '260828-k5r' server/test_poll_loop.py &amp;&amp; grep -qi 'no longer drawn' .planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md &amp;&amp; grep -q 'FRAME_INSET_FRAC' .planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md &amp;&amp; server/.venv/bin/python3 server/test_render.py 2>&amp;1 | tail -1 | grep -q 'render: 72/72 checks pass'</automated>
    <human-check>
      The old digest literal now appears only once — inside the historical comment block, never as the live pin on line 137. Confirm the GitHub Actions run on ubuntu-latest is GREEN after the second push, and that the value on line 137 was transcribed from that run's own FAIL output rather than from any local or container computation.
    </human-check>
  </verify>
  <done>
    The empty state is byte-identical to its pre-edit baseline; both active states moved, and re-drawing the outline back onto each post-edit canvas reproduces its pre-edit digest exactly, proving the 2px band was the only change. The local suite shows no new red harness. `_DEFAULT_CONFIG_DIGEST` holds a CI-derived value with a dated re-pin note naming this task, and CI is green. 03-UI-SPEC.md carries the dated amendment with its historical prose intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change deletes one drawing call and adds test coverage. No input crosses a new boundary, no endpoint, parser, credential, or network path is added or altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k5r-01 | Tampering | `server/test_poll_loop.py` `_DEFAULT_CONFIG_DIGEST` | medium | mitigate | The pinned digest is this repo's byte-identity gate against silent render drift; re-pinning it is exactly the moment that guard is weakest. Mitigated by requiring the new value to come from a real CI FAIL line (never a local or container computation, per that file's own standing rule), by keeping the old value present in the comment history, and by the independent reconstruct-and-compare proof in Task 2 Step 1 that bounds the pixel change to the 2px frame band before any re-pin happens. |
| T-k5r-02 | Denial of Service | CI pipeline | low | accept | The prescribed flow knowingly pushes a commit that fails CI once, in order to read the Linux digest out of the failure. Accepted: it is the only way to obtain a Linux-rasterized value, it is the documented procedure in the file itself, and it is resolved by the immediately following commit. |
| T-k5r-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan; no new dependency is introduced. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_render.py` reports 72/72, including both untouched frame-helper isolation checks.
- The new check was demonstrated RED (71/72) against unmodified code before the removal.
- `./scripts/run-all-tests.sh` shows exactly one red harness — `server/test_poll_loop.py` at 42/43 on macOS, the pre-existing documented platform split — and no new failures.
- `render_panel(flight, 'empty')` still hashes to `657b80f5...`; departing and arriving hashes moved; re-drawing the outline onto each post-edit canvas reproduces the pre-edit hashes exactly.
- `grep -c 'draw_frame(canvas' server/plane/render.py` returns 0, while the helper definition and both frame constants remain.
- GitHub Actions ubuntu-latest is green after the re-pin.
</verification>

<success_criteria>
The panel no longer shows the thin outline in either active state; the empty state is provably byte-identical; the only pixels that changed anywhere are the 2px frame band; automated coverage now proves the absence on the real render path and would have caught the border's presence; CI is green with a legitimately CI-sourced digest pin; and the design record says what the code actually does.
</success_criteria>

<output>
Create `.planning/quick/260828-k5r-remove-the-visible-thin-frame-border-out/260828-k5r-SUMMARY.md` when done.
</output>
