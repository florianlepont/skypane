---
phase: quick-260901-t00
plan: 260901-t00
subsystem: docs

tags: [companion, design-system, docs, skill]

requires:
  - phase: 06.6.4.1-companion-page-by-page-ia-consolidation
    provides: "the current Settings rename, Preview retirement, and unified save bar this skill now documents (still open)"
  - phase: quick-260901-qif
    provides: ".theme-status card treatment, .runway-row, .led-checkbox — folded into control-density.md / settings-page-patterns.md"
  - phase: quick-260901-re6
    provides: "section-caption merge, save bar moved outside form + fixed positioning — folded into settings-page-patterns.md"
  - phase: quick-260901-s5o
    provides: "Poll section caption, save bar floating-pop-up restyle — folded into settings-page-patterns.md"
provides:
  - "The project skill at .claude/skills/sketch-findings-skypane/ as the companion app's continuously-current design-system reference, current through 06.6.4.1 and this session's three quick tasks plus direct fix e87d46d"
  - "The SUPERSEDED line-marker convention for distinguishing deliberately-historical statements from claims that must be current"
  - "Three new reference files: accessibility-contrast.md, control-density.md, settings-page-patterns.md"
affects: [companion-ui-implementation, future-claude-sessions-on-skypane]

tech-stack:
  added: []
  patterns:
    - "SUPERSEDED line-marker convention: any markdown line stating a no-longer-current value carries the literal uppercase token SUPERSEDED, mechanically enforced by a throwaway cross-check script rather than trusted from a human read"
    - "Current-rule-first-then-superseded sentence form, applied consistently across all seven skill files"

key-files:
  created:
    - .claude/skills/sketch-findings-skypane/references/accessibility-contrast.md
    - .claude/skills/sketch-findings-skypane/references/control-density.md
    - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md
  modified:
    - .claude/skills/sketch-findings-skypane/SKILL.md
    - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
    - .claude/skills/sketch-findings-skypane/references/mobile-navigation.md
    - .claude/skills/sketch-findings-skypane/references/data-density.md
    - .claude/CLAUDE.md

key-decisions:
  - "Read companion/static/style.css in full, in one pass, before writing any skill content — a sampled/grepped read of this specific file would have surfaced a superseded rule's own retired rationale reading as current fact, since the file cites its own D-numbers and phase names inline next to values that have since moved"
  - "Adopted the SUPERSEDED line-marker convention (current-rule-first, historical value named on the same line, marked with the literal uppercase token) as the mechanism that lets a reversal stay documented (the whole point of recording it) without being mistaken for current fact by either a human or the cross-check script"
  - "Built the cross-check as a throwaway stdlib-only Python script at ${TMPDIR}/t00-verify-skill.py — never committed, never added to scripts/ — per the plan's explicit non-goal against adding a permanent CI gate to a documentation file whose value depends on being edited freely"
  - "Where a value never existed in any phase (e.g. the sketch's own --color-text-muted token, or its .dropdown/.nav-link/.theme-row class names that never shipped), documented it as a SUPERSEDED sketch-only artifact rather than deleting the mention outright — these are legitimate 'this was proposed, never adopted, do not reach for it' warnings that help a future implementer, not fabrications laundered into the record"
  - "Left one genuine open conflict unresolved rather than silently picking a side: 06.6.1's sketch said drop the anomaly banner's detail-text list, 06.6.3 (UXA-06) re-added it as prose naming real failing categories — visual-direction-typography.md now states this is a still-open developer decision, not a documentation-correctable inconsistency"

requirements-completed: [QUICK-260901-t00]

coverage:
  - id: D1
    description: "The skill's markdown states the CURRENT design system as authoritative, with every reversal since 06.6.1 recorded current-rule-first and what it replaced, verified against style.css/layout.py/pages/*.py/static/*.js at execution time rather than trusted from planning documents"
    requirement: QUICK-260901-t00
    verification:
      - kind: static
        ref: "${TMPDIR}/t00-verify-skill.py — extracts every hex colour, --custom-property, NNpx value, and .class-selector across all seven skill markdown files and confirms each is present verbatim in the real companion sources, or sits on a SUPERSEDED-marked line"
        status: pass
    human_judgment: true
    rationale: "The script's own printed historical-marked list (25 entries) was read entry by entry and each confirmed genuinely no longer current — the one class of error (a current value mislabelled as historical) the script cannot catch on its own."
  - id: D2
    description: "Three new reference files (accessibility-contrast.md, control-density.md, settings-page-patterns.md) exist, follow the existing files' section shape, and cover the three areas the skill previously had no account of at all"
    requirement: QUICK-260901-t00
    verification:
      - kind: static
        ref: "grep assertions confirming each file is non-empty and carries ## Origin / ## What to Avoid sections"
        status: pass
    human_judgment: false
  - id: D3
    description: "Zero source-code changes across the whole plan; sources/ byte-identical; exactly two focused commits, the first touching only the seven skill markdown files and the second touching only .claude/CLAUDE.md"
    requirement: QUICK-260901-t00
    verification:
      - kind: unit
        ref: "git diff --quiet HEAD~2 -- companion server firmware scripts; git diff --quiet HEAD~2 -- .claude/skills/sketch-findings-skypane/sources/; combined two-commit file-list scope assertion"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-09-01
status: complete
---

# Quick Task 260901-t00: Consolidate the Companion App's Design System into a Living Skill Summary

**Turned `.claude/skills/sketch-findings-skypane/` from a frozen 2026-08-29 snapshot of Phase 06.6.1's three sketch experiments into the companion app's continuously-current design-system reference — rewriting SKILL.md, adding three new reference files, correcting three existing ones in place, and mechanically cross-checking every stated literal against the real stylesheet and page modules before committing.**

## Performance

- **Duration:** ~12 min
- **Completed:** 2026-09-01T19:16Z
- **Tasks:** 3/3 completed
- **Files modified:** 8 (7 skill markdown files + `.claude/CLAUDE.md`)

## Accomplishments

- Read `companion/static/style.css` in full (2,458 lines, one pass) plus `companion/contrast_check.py`, `companion/pages/config_page.py`, `companion/layout.py`, `companion/static/dirty-state.js`, `companion/static/nav-dropdown.js`, and `companion/pages/history_page.py`'s relevant sections, along with the 06.6.2/06.6.4/06.6.1 planning context docs and the three 260901 quick-task SUMMARYs, before writing any skill content — extracting both the current values and their inline origin citations so conflicting statements across phases could be correctly time-ordered.
- Rewrote `SKILL.md`'s `description`, `<context>`, `<design_direction>` (colour, typography, spacing, cards/borders/radii/shadows, controls/density, navigation, data/tables — each a summary pointing at its reference file), `<findings_index>` (six Design Areas rows, per-sketch invalidation notes in Source Files), and extended the append-only `<metadata>` log with every phase/quick-task/direct-fix folded in since 2026-08-29, with 06.6.4.1 explicitly marked as still open.
- Wrote three new reference files: `accessibility-contrast.md` (06.6.2's WCAG-AA accent darkening, the three-level surface split, the executable colour-separation contract in `contrast_check.py`, focus/skip-link/reduced-motion/no-JS floors), `control-density.md` (06.6.4's button/nav/card density pass, the wash-strength register, the touch-target floor register with its gave-up/kept/relocated split), and `settings-page-patterns.md` (the one-caption-per-section rule, and the floating save bar's current mechanics plus its four-iteration history as a worked example).
- Corrected the three pre-existing reference files in place: `visual-direction-typography.md` no longer presents the sketch's `#E8622C` accent, warm-tint background pair, or shadow-at-rest card treatment as current; `mobile-navigation.md` now documents the shipped `.mobile-nav`/`.mobile-nav__link`/`.mobile-nav__footer`/`.site-nav-toggle` classes and the real in-flow `flex-basis` push-down mechanism (recording the `position: absolute` version as a found-and-fixed real-device defect, not an alternative); `data-density.md` no longer claims a `--color-text-muted` token or a horizontal-scroll mobile fallback, both retired/replaced.
- Adopted the SUPERSEDED line-marker convention throughout: any line stating a no-longer-current value carries the literal uppercase token `SUPERSEDED` on that same line.
- Built a throwaway, stdlib-only cross-check script (`${TMPDIR}/t00-verify-skill.py`, never committed) that extracted every hex colour, `--custom-property`, `NNpx` value, and `.class-selector` across all seven skill markdown files and confirmed each is present verbatim in the real `companion/static/style.css` / `companion/layout.py` / `companion/pages/*.py` / `companion/static/*.js`, or sits on a `SUPERSEDED`-marked line. Ran it, fixed six unmarked-historical-value failures it found (mostly lines that named a retired sketch class or hex without the literal marker), re-ran to a clean pass, and read the resulting 25-entry historical-marked list end to end to confirm none were current values mislabelled as historical.
- Updated `.claude/CLAUDE.md`'s Project Skills one-liner from the single-phase sketch-round framing to the continuously-current design-system framing, keeping the `Skill("sketch-findings-skypane")` invocation unchanged.

## Task Commits

Each task's content was verified before committing; per the plan, only Task 3 commits (Tasks 1 and 2 build content, Task 3 cross-checks then commits):

1. **Commit 1 (Tasks 1+2 content):** `30c172e` — `docs(quick-260901-t00): make the design-system skill current through 06.6.4.1` — all seven skill markdown files.
2. **Commit 2 (Task 3, pointer update):** `a7be166` — `docs(quick-260901-t00): point CLAUDE.md at the continuously-current design skill` — `.claude/CLAUDE.md` only.

## Files Created/Modified

- `.claude/skills/sketch-findings-skypane/SKILL.md` — rewritten description, `<context>`, `<design_direction>`, `<findings_index>`, extended `<metadata>` log
- `.claude/skills/sketch-findings-skypane/references/accessibility-contrast.md` — new
- `.claude/skills/sketch-findings-skypane/references/control-density.md` — new
- `.claude/skills/sketch-findings-skypane/references/settings-page-patterns.md` — new
- `.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md` — corrected in place
- `.claude/skills/sketch-findings-skypane/references/mobile-navigation.md` — corrected in place
- `.claude/skills/sketch-findings-skypane/references/data-density.md` — corrected in place
- `.claude/CLAUDE.md` — Project Skills one-liner rewritten

## Decisions Made

- Read `style.css` in full rather than sampling/grepping it — the file's own header comment warns that a superseded rule's retired rationale, quoted out of context, reads exactly like current fact; a sampled read was the specific failure mode this plan existed to prevent.
- Applied the SUPERSEDED marker even to cases where a value was *proposed but never shipped* (the sketch's `--color-text-muted` token, its `.dropdown`/`.nav-link`/`.theme-row` class names) rather than silently deleting the mention — these function as "do not reach for this, it never existed" warnings for a future implementer, which is more useful than omission, and is consistent with the marker's stated meaning ("was true, no longer is" extends naturally to "was proposed, never adopted").
- Left the anomaly-banner detail-text-list question explicitly unresolved in `visual-direction-typography.md` — 06.6.1's sketch and 06.6.3's UXA-06 genuinely disagree, and picking a side here would have been inventing a decision rather than documenting one already made.
- Did not cite the ~368px rendered save-bar width from commit `e87d46d`'s own message anywhere in the skill — it is a runtime measurement, not a re-verifiable declaration, per the plan's explicit non-goal.

## Deviations from Plan

None — Rules 1-4 auto-fix triggers. The plan's own iterative cross-check-and-fix loop (Task 3B) surfaced six lines needing a `SUPERSEDED` marker added (five in the newly-written Task 1/2 content, one a line I'd have missed without the mechanical sweep); each was fixed in place as intended by the plan's own design, not treated as a deviation from it.

## Issues Encountered

None. No package installs, no auth gates, no architectural questions.

## User Setup Required

None.

## Next Phase Readiness

The skill now auto-loads a continuously-current design-system reference into future Claude sessions on this project, satisfying both halves of the developer's original ask (the definition exists in the project, and it is kept for later use). No blockers. 06.6.4.1's own closing checkpoint remains open and is correctly marked as in-progress in the skill's `<metadata>` log — the skill should be re-verified against `${TMPDIR}/t00-verify-skill.py`'s pattern (rebuilt fresh, since this run's copy was never committed) the next time 06.6.4.1 or any later phase changes `companion/static/style.css`.

---
*Phase: quick-260901-t00*
*Completed: 2026-09-01*

## Self-Check: PASSED

All 8 modified/created files verified on disk (`SKILL.md`, `accessibility-contrast.md`, `control-density.md`, `settings-page-patterns.md`, `visual-direction-typography.md`, `mobile-navigation.md`, `data-density.md`, `.claude/CLAUDE.md`), plus this SUMMARY.md. Both commits (`30c172e`, `a7be166`) verified in `git log --oneline -2`.
