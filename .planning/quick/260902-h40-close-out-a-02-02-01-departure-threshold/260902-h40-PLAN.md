---
phase: quick-260902-h40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [A-02-02-01]
must_haves:
  truths:
    - "Every line in .planning/STATE.md that mentions A-02-02-01 also carries the closure marker `CLOSED 2026-09-02` — no occurrence is left standing as an open or carried-forward item."
    - "The Blockers/Concerns entry for A-02-02-01 is the single evidence of record: it names both independent production sources, the 20-row `runway_events` sample with its real UTC timestamp span and its real callsign/hex list, and the 13327-arriving / 4183-departing `journalctl` counts."
    - "The climb (departure) side of D-P2-04's +-200 ft/min deadband is recorded as validated against real sensor data, no longer as provisional/symmetry-derived or visual-path-only."
    - "No file other than .planning/STATE.md is modified — .planning/ROADMAP.md, server/, firmware/, hardware/ and companion/ are byte-identical after the task."
    - "STATE.md's YAML frontmatter and its Quick Tasks Completed table are left untouched by this task (the /gsd-quick orchestrator owns those updates)."
  artifacts:
    - ".planning/STATE.md: rewritten `### Blockers/Concerns` entry for A-02-02-01 carrying the full 2026-09-02 evidence"
    - ".planning/STATE.md: closure annotations on the 5 secondary A-02-02-01 mentions (Phase 7 block, Phase 8 block, D-P2-04 decision entry, Phase 02 / 02-05 Task 3 entry, Session Continuity carried-into-Phase-3 note)"
  key_links:
    - "Blockers/Concerns A-02-02-01 entry -> the two independent production evidence sources (`runway_events` SQLite table + `journalctl -u skypane-poll` confirmed_state counts)"
    - "Each of the 5 secondary A-02-02-01 mentions -> the Blockers/Concerns entry (each annotation cross-references it rather than duplicating the evidence)"
---

<objective>
Close out open item **A-02-02-01** — the departure-side (+200 ft/min) half of
D-P2-04's vertical-rate deadband in `server/plane/detect.py` — in
`.planning/STATE.md`, using real production evidence gathered live with the
developer on 2026-09-02.

A-02-02-01 has been carried as "never observed against a real departure /
unvalidated, visual-path only" since Phase 2, and repeated verbatim in the
Phase 7 and Phase 8 carried-forward-items notes plus older Decision Log and
Blockers/Concerns entries. That is now false: two independent real sources on
the live OVH VPS show thousands of real departure detections flowing through the
actually-deployed deadband logic.

Purpose: STATE.md is the project's live truth. Leaving a closed item described as
open in five places means the next planner re-scopes work that is already done,
and re-reads a stale "visual path only" caveat as a live risk.

Output: one docs-only edit to `.planning/STATE.md`. No code changes. No
ROADMAP.md changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
**Do NOT `@`-include `.planning/STATE.md`** — it is ~211 KB / 481 lines and
`@`-including it will consume most of the context budget for no benefit.

Instead, locate the edit sites surgically:

```
grep -n "A-02-02-01" .planning/STATE.md
```

As of 2026-09-02 that returns exactly **6 lines**: 33, 63, 217, 225, 314, 379.
Re-run the grep rather than trusting those numbers — read each matched line with
a narrow `Read` (`offset`/`limit`) before editing it. STATE.md's prose style is
one long single line per bullet/paragraph, so each match is a whole paragraph.

The established in-file convention for closing an item is an inline marker on the
existing entry (see the `- Spectra 6 dual-chip display driver — RESOLVED
2026-08-25 (01-06): ...` bullet in `### Blockers/Concerns`). Follow that pattern:
**annotate in place, preserve the historical narrative, do not delete history.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record A-02-02-01 as closed in STATE.md with the 2026-09-02 production evidence</name>

  <files>.planning/STATE.md</files>

  <action>
Edit `.planning/STATE.md` only. Six sites, located by `grep -n "A-02-02-01"`.

**Marker contract (load-bearing — the verify gate depends on it):** every line in
STATE.md that contains the token `A-02-02-01` must, after this task, also contain
the exact literal `CLOSED 2026-09-02` **on that same physical line**. Since each
site is a single long line, appending the annotation to that line satisfies this
naturally. Do not split an existing long line into multiple lines.

---

**Site 1 — `### Blockers/Concerns` (currently line 314). This is the evidence of
record; rewrite this bullet in full.** Replace the existing bullet (which begins
`- A-02-02-01 (departure-side D-03 threshold, +200 ft/min) has never been
observed against a real runway-3 departure ...`) with a single long line reading
substantially as follows — keep it as ONE line, matching the surrounding bullets'
style:

`- A-02-02-01 (departure-side D-03 threshold, +200 ft/min) — CLOSED 2026-09-02: validated against real production sensor data, superseding every "never observed" / "visual path only" framing of this item elsewhere in this file. Evidence gathered live with the developer over SSH on the production VPS (ubuntu@92.222.92.167), from two independent real sources. (1) server/history_db.py's runway_events table in /opt/skypane/state/history.db, queried with SELECT ts, hex, callsign, confirmed_state FROM runway_events WHERE confirmed_state='departing' ORDER BY ts DESC LIMIT 20 — returned 20 real departing rows spanning 2026-09-01T20:54:47Z to 2026-09-02T09:44:48Z across many distinct real aircraft: FPO683 (399264), TVF62GV (39de44), TVF65SU (39cea9), TVF18NK (3964e9), EJU9041 (44083b), TVF15ER (39dd00), TVF55ZF (39de4c), EJU408P (4401d1, twice), TAP441 (4952d1), FGN9372 (3aac0c), QAF10 (06a3d5, twice), DAH1007 (4d2190), VLG33BP (34560d), plus a few rows with callsign=None — an enrichment miss only; the hex, timestamp and confirmed departing state on those rows are real. (2) journalctl -u skypane-poll on the same VPS, grepped for confirmed_state=[a-z_]* and counted across the service's full log history: 13327 confirmed_state=arriving, 4183 confirmed_state=departing. This is real ADS-B vertical-rate data flowing through the actually-deployed +200 ft/min deadband in server/plane/detect.py (D-P2-04) — thousands of real departure detections, not the forced/synthetic render path Phase 7's on-glass session exercised. The climb side of D-P2-04 is therefore no longer provisional/symmetry-derived. ROADMAP.md is deliberately left unchanged: it is historical record for phases that are already complete.`

Preserve the immediately-following `(Post-rename: this is now the skypane-poll
unit, per quick-task 260826-vlq.)` continuation line as-is.

---

**Sites 2-6 — annotate in place.** For each of the five remaining matched lines,
append (or insert at the natural end of the relevant clause) this exact
annotation, adapting only the leading punctuation/spacing to read cleanly:

`[CLOSED 2026-09-02 — validated against real production departure data; see Blockers/Concerns.]`

The five sites and the clause each annotation attaches to:

- **Line ~33, the Phase 7 (Final On-Glass Verification) block** — the
  carried-forward-items clause reading `A-02-02-01's real +200ft/min departure
  threshold still unvalidated against real sensor data (visual path only)`.
- **Line ~63, the Phase 8 (Panel theme rework) block** — the
  carried-forward-items clause reading `A-02-02-01's real +200ft/min departure
  threshold still unvalidated (every real detection observed so far has been an
  arrival)`.
- **Line ~217, `### Decisions`, the Phase 02 D-P2-04 entry** — the clause reading
  `climb side documented as provisional/symmetry-derived per A-02-02-01`. Here,
  additionally make the closure explicit in prose: the climb side is now backed by
  real production data rather than symmetry alone.
- **Line ~225, `### Decisions`, the Phase 02 / 02-05 Task 3 entry** — the clause
  reading `Step 6 (real-departure threshold, A-02-02-01) explicitly deferred by
  developer choice`.
- **Line ~379, `## Session Continuity`, the end-of-2026-08-26 session block** —
  the bullet reading `Open item carried into Phase 3: A-02-02-01's real-departure
  threshold has never been observed (see Blockers/Concerns above).`

For sites 2, 3, 5 and 6, keep the original historical sentence intact and append
the annotation after it — these are dated session/decision records, and rewriting
them would falsify what was true at the time.

---

**Explicitly out of scope for this task — do not do any of these:**

- Do not modify `.planning/ROADMAP.md`. It is historical record for completed
  phases and stays as-is by developer instruction.
- Do not modify any file under `server/`, `firmware/`, `hardware/` or
  `companion/`. This is a documentation-only change; the +200 ft/min threshold
  itself is confirmed correct and needs no code change.
- Do not edit STATE.md's YAML frontmatter (`last_updated`, `last_activity`,
  `progress`, etc.). The `/gsd-quick` orchestrator owns those via its own state
  update.
- Do not add a row to STATE.md's `### Quick Tasks Completed` table. The
  `/gsd-quick` orchestrator appends that row itself; adding one here produces a
  duplicate.
  </action>

  <verify>
    <automated>bash -c 'S=.planning/STATE.md; N=$(grep -c "A-02-02-01" "$S"); M=$(grep "A-02-02-01" "$S" | grep -c "CLOSED 2026-09-02"); test "$N" -ge 6 && test "$N" -eq "$M" && grep -q "runway_events" "$S" && grep -q "13327" "$S" && grep -q "4183" "$S" && grep -q "2026-09-01T20:54:47Z" "$S" && grep -q "2026-09-02T09:44:48Z" "$S" && grep -q "TVF62GV" "$S" && grep -q "EJU408P" "$S" && grep -q "92.222.92.167" "$S" && test -z "$(git diff HEAD --name-only -- server firmware hardware companion .planning/ROADMAP.md)" && echo PASS'</automated>
  </verify>

  <done>
The verify command prints `PASS`, meaning:
- all 6 (or more) A-02-02-01 lines in STATE.md carry the `CLOSED 2026-09-02` marker;
- the evidence details are present verbatim (`runway_events`, both journalctl
  counts 13327 / 4183, both real UTC timestamps bounding the sample, at least two
  real callsigns, the production host);
- `git diff HEAD` shows zero changes under `server/`, `firmware/`, `hardware/`,
  `companion/` or in `.planning/ROADMAP.md`.

Additionally, `git diff HEAD --name-only` lists `.planning/STATE.md` as the only
modified tracked file (the plan/summary files under
`.planning/quick/260902-h40-.../` are expected new files, not modifications).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none introduced)* | Documentation-only edit to a local planning file. No new code paths, no network surface, no user input, no dependency installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-h40-01 | Tampering | `.planning/STATE.md` | low | mitigate | Scope gate in `<verify>` asserts `git diff HEAD` is empty for `server/`, `firmware/`, `hardware/`, `companion/` and `.planning/ROADMAP.md`, so the edit cannot silently widen into code or into the historical roadmap. |
| T-quick-h40-02 | Information disclosure | production VPS details recorded in STATE.md | low | accept | The host IP (`92.222.92.167`) and unit name are already recorded throughout `.planning/` and `deploy/`; no credentials, bearer tokens or API keys are copied into STATE.md by this task. |
| T-quick-h40-03 | Repudiation | evidence provenance | low | mitigate | Both queries are recorded verbatim (SQL statement + `journalctl` grep) alongside their results, so the closure claim is independently re-runnable rather than asserted. |
</threat_model>

<verification>
1. `grep -n "A-02-02-01" .planning/STATE.md` — every returned line contains
   `CLOSED 2026-09-02`.
2. Read the rewritten `### Blockers/Concerns` bullet: it names both sources, the
   SQL query, the 20-row span, the callsign/hex sample, and both journalctl
   counts.
3. `git diff HEAD --name-only` — `.planning/STATE.md` is the only modified
   tracked file.
4. `git diff HEAD -- .planning/STATE.md | grep '^-'` — the removed lines are
   confined to the single Blockers/Concerns bullet being rewritten; the Phase 7,
   Phase 8, Decisions and Session Continuity sites show additive annotation only
   (their original sentences survive in the `+` side).
</verification>

<success_criteria>
- A-02-02-01 no longer reads as an open or carried-forward item anywhere in
  `.planning/STATE.md`.
- The 2026-09-02 evidence (two independent production sources, with counts,
  timestamps and real callsigns) is recorded once, in `### Blockers/Concerns`,
  and cross-referenced from the five secondary mentions.
- D-P2-04's climb side is documented as validated against real sensor data.
- Zero code files and zero ROADMAP.md changes.
</success_criteria>

<output>
Create `.planning/quick/260902-h40-close-out-a-02-02-01-departure-threshold/260902-h40-SUMMARY.md` when done.
</output>
