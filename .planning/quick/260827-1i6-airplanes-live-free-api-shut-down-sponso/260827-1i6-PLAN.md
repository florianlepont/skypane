---
phase: 04-ci-cd-documentation-legal-compliance-github-actions-ci-tests
plan: 260827-1i6
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - server/plane/detect.py
  - server/test_plane_detection.py
  - README.md
  - COMPLIANCE.md
  - ARCHITECTURE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PLANE-03]

must_haves:
  truths:
    - "A default poll — `detect.poll_current_aircraft(geofence)` with no providers argument, which is exactly how `server/poll_loop.py:165` calls it in production — issues one aggregator request, to adsb.fi, and never reaches api.airplanes.live."
    - "`--provider airplaneslive` and `--provider both` still work as explicit, human-invoked opt-ins: the provider entry is demoted out of the default path, not deleted from the code."
    - "The 9-harness suite still passes, and now contains a regression check that fails if airplanes.live is ever put back into the default provider order."
    - "COMPLIANCE.md records the 2026-08-27 reply, the same-day live HTTP 403 against the exact production endpoint, and the resulting demotion — and no source in that document is left carrying an open-item marker."
    - "Every document describing current runtime behaviour (README.md, ARCHITECTURE.md, PROJECT.md's context bullet, REQUIREMENTS.md's PLANE-03) names adsb.fi as the provider actually queried."
    - "Historical records are left intact and are not retro-edited: adsb-test/RESULTS.md, PROJECT.md's now-superseded Key Decisions row, REQUIREMENTS.md's rejected-alternatives table, and server/fixtures/README.md's provenance notes all keep their original wording."
  artifacts:
    - server/plane/detect.py
    - server/test_plane_detection.py
    - COMPLIANCE.md
    - README.md
    - ARCHITECTURE.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "`server/poll_loop.py:165` calls `detect.poll_current_aircraft(geofence_data)` with no providers argument. The new default-order constant is therefore what production actually uses. If the constant is added but `poll_current_aircraft` still falls back to `list(PROVIDERS.keys())`, production behaviour is unchanged and every documentation claim in Tasks 2 and 3 becomes false."
    - "README.md's adsb.fi citation sentence and COMPLIANCE.md's citation blockquote are the same citation deliberately mirrored in two places — COMPLIANCE.md's adsb.fi entry says so in its own text. Updating one without the other leaves the project's attribution self-contradictory."
    - "README.md states a total check count that equals the sum of every harness's `EXPECTED_CHECK_COUNT`. Adding checks without updating that number makes the README false, and nothing in CI catches it — Task 1 must add the gate that does."
---

<objective>
airplanes.live shut down its free API tier on 2026-08-27. Access now requires running an ADS-B feeder, a paid sponsorship, or a commercial licence. This project's production server calls it as its *primary* aggregator on a 30-second cadence, and its terms status was already the single open item in COMPLIANCE.md — the shutdown notice is, in substance, the reply to the clarification email that item was waiting on.

This plan promotes adsb.fi to sole default provider, demotes airplanes.live to an opt-in code path that is never called automatically, and reconciles every document that currently tells a reader otherwise.

Purpose: keep the shipped server on a free, terms-clean data source and stop it making automated requests to a service that has withdrawn free access — which is both a compliance fix and a correctness fix, since the production endpoint now returns HTTP 403 on every call.
Output: a one-provider default path in `server/plane/detect.py` with a regression check that pins it; a COMPLIANCE.md with zero open items; and README/ARCHITECTURE/PROJECT/REQUIREMENTS describing what the code now actually does.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@server/plane/detect.py
@server/test_plane_detection.py
@COMPLIANCE.md
@adsb-test/RESULTS.md
</context>

<decision_already_made>
**The developer has decided: switch to adsb.fi as primary rather than pay for airplanes.live access. Do not re-open this.**

Do not propose, cost out, or add a task for an airplanes.live sponsorship or commercial licence. Do not add a task to stand up an ADS-B feeder. Those three routes are the only ways to regain free-tier-equivalent access and all three were considered and declined.

Do not touch the production VPS. Deployment happens through the existing CI/CD pipeline on merge — this plan changes repository contents only.
</decision_already_made>

<ground_truth_corrections>
Two things the executor must get right, because the obvious summary of them is wrong:

1. **Sample-error counts are the opposite of what a casual reading suggests.** `adsb-test/RESULTS.md`'s per-provider table records **adsbfi: 145 samples, 1 error** and **airplaneslive: 144 samples, 0 errors**. adsb.fi is the one with the error. Do not write "adsb.fi had zero sample errors" anywhere — read the table and quote it.

2. **adsb.fi's real advantage is coverage breadth, and its real cost is cadence.** RESULTS.md: adsbfi saw **38** distinct aircraft at or below the 3000ft ceiling and **2** on-ground detections; airplaneslive saw **37** and **2**. Both **PASS**ed the two coverage conditions. adsb.fi's median position-update gap for its best-tracked aircraft was **36.2s** vs airplanes.live's **22.4s** — and *both* providers **FAIL**ed the 15-second cadence threshold, a miss RESULTS.md's own verdict already judged immaterial against the device's multi-minute refresh cycle.

The honest rationale to write into the docs is therefore: *adsb.fi independently cleared the same coverage bar; its slower cadence was already on the record as immaterial for this display.* Not: *adsb.fi was better.* Any doc sentence that cherry-picks in adsb.fi's favour should be rewritten before commit.
</ground_truth_corrections>

<scope_boundary>
**In scope** — the seven files in `files_modified`, and nothing else.

**Explicitly out of scope, do not edit:**

| File | Why it stays as-is |
|---|---|
| `adsb-test/RESULTS.md`, `adsb-test/query_aggregator.py`, `adsb-test/README.md` | A frozen Phase 1 spike and its measurement record. Rewriting a record of what was measured in August to match a decision made later is falsification, not maintenance. |
| `server/fixtures/README.md` | Correctly states the committed fixtures were captured from airplanes.live samples. That remains true regardless of which provider ships. |
| `server/poll_loop.py` | Its line-51 comment maps response keys (`ac` vs `aircraft`) to providers. Still accurate; makes no ordering claim. |
| `.planning/REQUIREMENTS.md` rejected-alternatives table (the RTL-SDR row) | Names both aggregators without asserting an order. Nothing stale in it. |
| `.planning/PROJECT.md` Key Decisions row for the original aggregator choice | Gets a supersession note appended to its Outcome cell — its Decision and Rationale cells keep their original wording, matching how the physical-button row was superseded in this same table. |
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make adsb.fi the sole default provider, pin it with a regression check</name>
  <files>server/plane/detect.py, server/test_plane_detection.py, README.md</files>
  <behavior>
    Two new checks in `server/test_plane_detection.py`, added after the existing six, following that harness's exact `check(name, fn)` / `(ok, reason)` convention:

    - Check 7 — default poll queries adsb.fi only: monkeypatch `detect.query_provider` with a recorder that appends the provider name to a list and returns `[]`, save the original first and restore it in a `finally` so later checks are unaffected. Call `detect.poll_current_aircraft(geofence)` with no `providers` argument. Assert the recorded list equals exactly `["adsbfi"]`. Fails loudly if the list has two entries, or the wrong single entry.
    - Check 8 — the opt-in path survives: assert `"airplaneslive"` is still a key of `detect.PROVIDERS` (so `--provider airplaneslive` remains selectable) while `detect.DEFAULT_PROVIDER_ORDER` does not contain it.

    Check 7 is behavioural — it pins the call sequence regardless of which mechanism holds the order, so it stays valid if the ordering is later refactored.
  </behavior>
  <action>
Edit `server/plane/detect.py`:

1. Reorder the `PROVIDERS` dict literal so the `adsbfi` entry precedes the `airplaneslive` entry. Change only the ordering — both entries keep their exact `url_template` and `aircraft_key` values.

2. Add a module-level constant `DEFAULT_PROVIDER_ORDER = ("adsbfi",)` directly after the `PROVIDERS` dict, replacing the block comment that currently sits above `PROVIDERS`. Rewrite that comment so it does two things at once, since both halves are still load-bearing:
   - Records that the Phase 1 decision to use free ADS-B aggregators at all still stands — that part is not being reversed.
   - Records that the *ordering* half of it was reversed on 2026-08-27 when airplanes.live withdrew free API access (feeder, paid sponsorship, or commercial licence now required), and that the exact production endpoint template returned HTTP 403 on live verification the same day while adsb.fi returned HTTP 200.
   - States the tradeoff plainly: a single default provider means there is no automatic fallback, so an adsb.fi outage renders the Empty state rather than silently switching sources. Note that the `airplaneslive` entry is retained for explicit `--provider` use only — by a feeder operator, sponsor, or licensee — and is not queried automatically.
   Use the figures from `<ground_truth_corrections>` if the comment cites any.

3. In `poll_current_aircraft()`, change the default so `provider_names` falls back to `DEFAULT_PROVIDER_ORDER` instead of `list(PROVIDERS.keys())` when `providers is None`. Leave the `MIN_SECONDS_BETWEEN_CALLS` sleep-on-`i > 0` logic untouched — it still applies whenever a caller passes an explicit multi-provider sequence. Update the docstring's first line so it no longer implies a multi-provider default.

4. In `build_parser()`, change the `--provider` argument's `default` from the both-providers value to `"adsbfi"`, and rewrite its `help=` text to say the default queries adsb.fi only and that the other choices are explicit opt-ins. Leave `choices=sorted(PROVIDERS) + ["both"]` as-is.

5. In `main()`, the both-providers branch currently resolves to `None` and thereby inherits the default; make it resolve to `list(PROVIDERS)` so it genuinely means both, adsb.fi first, now that `None` means adsb.fi alone.

6. In the module docstring's `Usage:` block, change the second example's `--provider` value to `adsbfi` so the documented example matches a call that will actually succeed.

Then edit `server/test_plane_detection.py`: add the two checks described in `<behavior>` and raise `EXPECTED_CHECK_COUNT` from 6 to 8.

Then edit `README.md`: the Tests section's parenthetical states a harness count and a total check count. The harness count is unchanged; the total is now the sum of every `EXPECTED_CHECK_COUNT` across `server/test_*.py` and `stub-server/test_*.py`. Compute that sum from the files — do not assume the arithmetic — and update the number to match. Change nothing else in that section.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); import server.plane.detect as d; assert d.DEFAULT_PROVIDER_ORDER == ('adsbfi',), d.DEFAULT_PROVIDER_ORDER; assert 'airplaneslive' in d.PROVIDERS, 'opt-in provider entry was deleted'; assert list(d.PROVIDERS)[0] == 'adsbfi', list(d.PROVIDERS); assert d.build_parser().parse_args([]).provider == 'adsbfi'; print('detect.py defaults OK')"</automated>
    <automated>server/.venv/bin/python3 -c "
import glob, pathlib, re
tot = sum(int(re.search(r'EXPECTED_CHECK_COUNT = (\d+)', pathlib.Path(p).read_text()).group(1)) for p in sorted(glob.glob('server/test_*.py') + glob.glob('stub-server/test_*.py')))
rd = pathlib.Path('README.md').read_text()
assert ('%d checks total' % tot) in rd, 'README check count is stale; harnesses now sum to %d' % tot
print('README check count matches harnesses:', tot)"</automated>
    <automated>./scripts/run-all-tests.sh</automated>
  </verify>
  <done>`poll_current_aircraft()` called with no providers argument queries adsb.fi and only adsb.fi; `--provider airplaneslive` and `--provider both` still resolve; `server/test_plane_detection.py` reports 8/8; `./scripts/run-all-tests.sh` exits 0 with all 9 harnesses green and coverage above threshold; README's stated check total equals the computed sum.</done>
</task>

<task type="auto">
  <name>Task 2: Close the compliance item and correct the shipped repo docs</name>
  <files>COMPLIANCE.md, README.md, ARCHITECTURE.md</files>
  <action>
Edit `COMPLIANCE.md`:

1. **airplanes.live entry.** Replace its `Terms checked`, `What the terms require`, `Verdict`, and status lines. It must now record, as plain factual paraphrase and never as a quoted email:
   - What the 2026-08-27 reply said — the free API tier was discontinued for cost-sustainability reasons, and access is now gated behind running an ADS-B feeder, a paid sponsorship, or a commercial licence for any revenue-generating use. Paraphrase in a few sentences; do not reproduce the email text.
   - The live verification performed the same day: a GET against the exact endpoint template `server/plane/detect.py` used in production returned HTTP 403, while the adsb.fi endpoint in the same file returned HTTP 200.
   - The resulting decision: adsb.fi promoted to sole default provider; the airplanes.live provider entry retained in `detect.py` for explicit `--provider` use by a feeder operator, sponsor, or licensee, and not queried by any automated poll. Because shipped behaviour no longer reaches the service, its terms are no longer a blocker on anything this repository does — and the item that was waiting on a clarification-email reply has now received one.
   - Update the `Used in shipped code` line from a plain yes: it is present in the code but unreached by the default path.
   - Keep the record that `/api-guide/` returned 403 on automated fetch — that is still true and still explains why the terms text itself was never read directly.

2. **adsb.fi entry.** It currently describes adsb.fi as the secondary aggregator, in the `Used in shipped code` line and inside the citation blockquote. Both must now describe it as the primary and, since 2026-08-27, the only aggregator an automated poll queries. The upstream requirement itself (cite adsb.fi, link to their home page) is unchanged and still met — only our description of the role changes. Note in this entry that the same citation sentence is mirrored in `README.md` and that both must agree.

3. **Runtime behaviour section.** Its poll-cadence bullet describes a per-cycle call to each provider tried and describes `MIN_SECONDS_BETWEEN_CALLS` as throttling a fallback call to a second provider. Rewrite so it states that a production cycle now issues a single aggregator request, that the throttle still governs any explicit multi-provider invocation, and that both figures stay comfortably inside the documented 1 req/s limit.

4. **Status table and closing line.** Update the airplanes.live row's terms-status and action cells to reflect a resolved item, and its used-in-shipped-code cell to reflect the demotion. Update the adsb.fi row to primary. Then rewrite the sentence beneath the table — it currently asserts that exactly one row is marked as an outstanding item; no row is, now. PRIM's "revisit at v2 planning" note is a forward-looking reminder, not an outstanding item, and stays as it is.

Edit `README.md`'s Data sources section:

- Lead with adsb.fi as the primary and sole automatically-queried source, carrying the same citation sentence as COMPLIANCE.md's blockquote, with its live link intact.
- Describe airplanes.live factually as documented-but-not-called-by-default following the 2026-08-27 free-tier closure, pointing at `COMPLIANCE.md` for the detail. Remove the good-faith-attribution-pending-confirmation framing — the confirmation arrived.
- Keep the adsbdb.com sentence and the no-raw-data-is-republished paragraph. In that paragraph, delete the trailing clause that advertises an outstanding compliance item; the reference to `COMPLIANCE.md` stays.

Edit `ARCHITECTURE.md`:

- In the end-to-end ASCII data-flow diagram, the top-left box's two label lines name the two aggregators and their roles. Relabel to name adsb.fi as the queried source and mark airplanes.live as opt-in/unused-by-default. **Preserve the box's column alignment** — the diagram's `│` and `▼` characters below must still line up.
- In the Detection paragraph, rewrite the sentence describing which aggregator is queried first and which serves as fallback. It must now state that a poll queries adsb.fi's geofenced bounding box, that there is no automatic fallback provider, and that an adsb.fi failure yields no detection for that cycle — which the pipeline already handles as the Empty state. Leave the rest of that paragraph, including the `select_runway3_aircraft()` total-order description, untouched.
  </action>
  <verify>
    <automated>test "$(grep -c 'OPEN' COMPLIANCE.md)" = "0" && echo "COMPLIANCE.md: no open-item markers remain"</automated>
    <automated>test "$(grep -c 'primary aggregator source' README.md)" = "0" && echo "README.md: stale primary-source claim removed"</automated>
    <automated>test "$(grep -c 'airplanes.live primary' ARCHITECTURE.md)" = "0" && echo "ARCHITECTURE.md: stale diagram label removed"</automated>
    <automated>server/.venv/bin/python3 -c "
import pathlib
c = pathlib.Path('COMPLIANCE.md').read_text(); r = pathlib.Path('README.md').read_text(); a = pathlib.Path('ARCHITECTURE.md').read_text()
for f, t in (('COMPLIANCE.md', c), ('README.md', r)):
    assert 'https://adsb.fi' in t, f + ' lost the required adsb.fi link'
assert '403' in c, 'COMPLIANCE.md does not record the live verification result'
assert '2026-08-27' in c, 'COMPLIANCE.md does not date the reply'
for f, t in (('COMPLIANCE.md', c), ('README.md', r), ('ARCHITECTURE.md', a)):
    assert 'adsb.fi' in t, f
print('shipped docs OK')"</automated>
    <automated>./scripts/check-attribution.sh</automated>
  </verify>
  <done>COMPLIANCE.md carries no open item, records the 2026-08-27 reply as paraphrase plus the same-day live 403 against the production endpoint template, and describes adsb.fi as primary in both its entry and its citation blockquote; README.md's Data sources section leads with adsb.fi carrying the identical citation sentence and no longer advertises an outstanding item; ARCHITECTURE.md's diagram and Detection paragraph describe a single-provider poll with no automatic fallback, and the diagram's column alignment still holds.</done>
</task>

<task type="auto">
  <name>Task 3: Record the reversal in the planning artifacts</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
Edit `.planning/PROJECT.md`:

1. **Context bullet (the plane-detection-approach bullet, above the Constraints section).** Its parenthetical names both aggregators and their order. Rewrite it to name adsb.fi as the sole default provider, and append a short dated correction note in the same style the bullet already uses for its 2026-08-26 RTL-SDR correction — this file has an established convention of correcting current-state claims in place with a dated note rather than leaving them stale. Point the reader at Key Decisions.

2. **Key Decisions table — annotate the existing row.** Find the row whose Decision cell describes choosing free public ADS-B aggregators over a schedule API or a local RTL-SDR receiver. Leave its Decision and Rationale cells exactly as written — they are the historical record of what was decided in August, and the aggregators-over-schedule-API core of that decision still stands. Append to its **Outcome** cell only: a note that the provider-ordering half was superseded on 2026-08-27, see the row below. Match the phrasing style of the physical-button row's existing supersession note.

3. **Key Decisions table — add a new row** at the end, in the table's existing three-column shape:
   - *Decision:* adsb.fi promoted to sole default ADS-B provider; the airplanes.live entry retained in `server/plane/detect.py` as an explicit `--provider` opt-in but never queried by an automated poll.
   - *Rationale:* airplanes.live discontinued its free API tier on 2026-08-27, gating access behind running a feeder, a paid sponsorship, or a commercial licence — all three declined. adsb.fi independently cleared the same Phase 1 coverage bar (see `adsb-test/RESULTS.md`), and staying on a free source preserves the original reason for choosing free aggregators over a paid schedule API. Cite the coverage figures and the cadence figure exactly as `<ground_truth_corrections>` states them, including that adsb.fi's slower cadence was already judged immaterial for this display in RESULTS.md's own verdict. Do not claim adsb.fi was the better provider.
   - *Outcome:* Decided 2026-08-27 and live-verified the same day — the production airplanes.live endpoint returns HTTP 403, adsb.fi returns HTTP 200. Tradeoff accepted: one default provider, no automatic fallback, so an adsb.fi outage renders the Empty state.

Edit `.planning/REQUIREMENTS.md`:

- The validated PLANE-03 requirement line carries a parenthetical naming both aggregators and their order. Update it to name adsb.fi as the provider actually queried. The requirement's substance — plane view updates one flight at a time from free public ADS-B aggregator APIs geofenced to the runway, not a fixed timetable — is unchanged and stays validated; only the provider parenthetical moves.
- Leave the rejected-alternatives table's RTL-SDR row alone. It names both aggregators without asserting an order and is a historical record.
  </action>
  <verify>
    <automated>test "$(awk '/^## Key Decisions/{exit} {print}' .planning/PROJECT.md | grep -c 'airplanes.live primary')" = "0" && echo "PROJECT.md: pre-Key-Decisions prose carries no stale ordering claim"</automated>
    <automated>test "$(grep -c 'airplanes.live primary' .planning/REQUIREMENTS.md)" = "0" && echo "REQUIREMENTS.md: PLANE-03 ordering claim updated"</automated>
    <automated>server/.venv/bin/python3 -c "
import pathlib
p = pathlib.Path('.planning/PROJECT.md').read_text()
head, _, tail = p.partition('## Key Decisions')
assert tail, 'Key Decisions heading missing'
assert '2026-08-27' in tail, 'no dated 2026-08-27 entry in Key Decisions'
assert tail.count('| ') > head.count('## Key Decisions'), 'Key Decisions table malformed'
assert 'airplanes.live primary' in tail, 'the superseded historical row was rewritten instead of annotated'
r = pathlib.Path('.planning/REQUIREMENTS.md').read_text()
assert 'PLANE-03' in r and 'adsb.fi' in r
print('planning artifacts OK')"</automated>
    <automated>test "$(git -C . diff --numstat -- adsb-test/ server/fixtures/README.md server/poll_loop.py | wc -l | tr -d ' ')" = "0" && echo "out-of-scope files untouched"</automated>
  </verify>
  <done>PROJECT.md's context bullet names adsb.fi with a dated correction note; the original aggregator-choice row keeps its Decision and Rationale text with a supersession note appended to Outcome only; a new dated Key Decisions row records the promotion with RESULTS.md's real figures and the accepted no-fallback tradeoff; REQUIREMENTS.md's PLANE-03 names adsb.fi; nothing under `adsb-test/`, `server/fixtures/README.md`, or `server/poll_loop.py` was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → adsb.fi (`opendata.adsb.fi`) | Unauthenticated third-party HTTPS feed. Untrusted JSON crosses into the render pipeline. |
| server → airplanes.live (`api.airplanes.live`) | Same shape, but after this plan it is unreachable from any automated code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1i6-01 | Denial of Service | `detect.poll_current_aircraft()` default path | medium | accept | Removing the second provider removes the automatic fallback: an adsb.fi outage yields no detection for that cycle. Already-safe behaviour — `poll_loop.py` renders the Empty state rather than crashing or showing stale data. Accepted and documented in Task 1's rationale comment, Task 2's ARCHITECTURE.md paragraph, and Task 3's Key Decisions Outcome cell, so the tradeoff is on the record rather than discovered during an outage. |
| T-1i6-02 | Tampering | untrusted aggregator JSON → `filter_in_geofence()` | low | mitigate | Unchanged and already mitigated: `filter_in_geofence()` isinstance-checks lat/lon and `alt_baro`, skips malformed records instead of raising, and never claims below-ceiling on an unknown altitude. This plan changes which host the JSON comes from, not how it is validated. Task 1's regression checks run against the existing harness, which covers this path. |
| T-1i6-03 | Repudiation | automated requests to a service that withdrew free access | medium | mitigate | Continuing 30-second automated polls against airplanes.live after its free tier closed would be use without the now-required feeder/sponsorship/licence. Mitigated by Task 1 removing it from the default order — verified by check 7 asserting the recorded call list is adsb.fi alone, so the compliance claim written in Task 2 is machine-backed rather than asserted. |
| T-1i6-04 | Information Disclosure | documentation edits | low | accept | No credential, host, or token literal is touched. Every edited file is already public in the pushed repository; the added text is a terms paraphrase and two HTTP status codes. |

No package-manager install is performed by this plan and no dependency is added or changed, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
Run from the repository root after all three tasks:

1. `./scripts/run-all-tests.sh` exits 0 — 9 harnesses, `server/test_plane_detection.py` reporting 8/8, coverage above the `pyproject.toml` threshold.
2. `./scripts/check-attribution.sh` exits 0.
3. `server/.venv/bin/python3 -m ruff check .` exits 0.
4. `server/.venv/bin/python3 server/plane/detect.py --json` against the live network prints either a selection or `null` — and its stderr contains no failure line naming `airplaneslive`, confirming the demoted provider is not being reached.
5. `git diff --stat` lists exactly the seven files in `files_modified` and no others.

<human-check>
Read the rewritten airplanes.live entry in `COMPLIANCE.md` end to end and confirm it reads as a factual paraphrase of the 2026-08-27 reply rather than a reproduction of it, and that nothing in it overstates what the reply actually said. Then read the new `.planning/PROJECT.md` Key Decisions row and confirm its rationale does not cherry-pick the Phase 1 figures in adsb.fi's favour — RESULTS.md records adsb.fi with the broader coverage (38 vs 37 distinct aircraft ≤3000ft) but also the slower cadence (36.2s vs 22.4s median update gap) and the one sample error (1 vs 0).
</human-check>
</verification>

<success_criteria>
- A production-shaped poll (`detect.poll_current_aircraft(geofence)`, no providers argument) issues exactly one request and it goes to adsb.fi.
- `--provider airplaneslive` and `--provider both` still resolve and run, so the opt-in path for a feeder operator, sponsor, or licensee is retained rather than deleted.
- `server/test_plane_detection.py` fails if airplanes.live is ever returned to the default provider order.
- All 9 harnesses, the attribution check, and ruff pass; README's stated check total matches the harnesses' actual sum.
- COMPLIANCE.md has zero open items and records the reply, the live 403, and the demotion.
- README.md, ARCHITECTURE.md, PROJECT.md, and REQUIREMENTS.md each describe adsb.fi as the queried provider, with identical adsb.fi citation text in COMPLIANCE.md and README.md.
- Historical records under `adsb-test/`, `server/fixtures/README.md`, and the superseded PROJECT.md decision row are unmodified in substance.
- No sponsorship purchased, no feeder provisioned, no change made outside this repository.
</success_criteria>

<output>
Create `.planning/quick/260827-1i6-airplanes-live-free-api-shut-down-sponso/260827-1i6-SUMMARY.md` when done.
</output>
