---
phase: 04-ci-cd-documentation-legal-compliance-github-actions-ci-tests
plan: 260827-1i6
subsystem: compliance
tags: [ads-b, adsb.fi, airplanes.live, compliance, plane-detection]

requires:
  - phase: 01-hardware-firmware-bringup
    provides: adsb-test/RESULTS.md's Phase 1 aggregator viability figures (coverage, cadence, sample-error counts)
provides:
  - adsb.fi as the sole default ADS-B provider in server/plane/detect.py, machine-pinned by a regression check
  - COMPLIANCE.md with zero open items (airplanes.live's terms status resolved)
  - README.md, ARCHITECTURE.md, PROJECT.md, REQUIREMENTS.md all describing the provider actually queried
affects: [plane-detection, compliance, documentation]

tech-stack:
  added: []
  patterns:
    - "DEFAULT_PROVIDER_ORDER constant separates 'providers registered in code' (PROVIDERS dict, still includes an opt-in-only entry) from 'providers an automated poll actually queries' (a narrower tuple) — the same shape used for the demoted airplanes.live entry"

key-files:
  created: []
  modified:
    - server/plane/detect.py
    - server/test_plane_detection.py
    - README.md
    - COMPLIANCE.md
    - ARCHITECTURE.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "adsb.fi promoted to sole default ADS-B provider; airplanes.live retained in server/plane/detect.py as an explicit --provider opt-in (for a feeder operator, sponsor, or licensee) but never queried automatically"
  - "No sponsorship purchased, no feeder provisioned, no commercial licence bought — all three declined per the developer's prior decision"

patterns-established:
  - "Machine-pinned provider-order regression check: server/test_plane_detection.py check 7 monkeypatches query_provider and asserts the default poll's recorded call list, so a future PR that re-adds airplanes.live to the default order fails the suite instead of silently regressing"

requirements-completed: [PLANE-03]

coverage:
  - id: D1
    description: "A production-shaped poll (poll_current_aircraft(geofence), no providers argument) issues exactly one request, and it goes to adsb.fi"
    requirement: "PLANE-03"
    verification:
      - kind: unit
        ref: "server/test_plane_detection.py#default poll (no providers arg) queries adsb.fi only"
        status: pass
    human_judgment: false
  - id: D2
    description: "--provider airplaneslive and --provider both still resolve and run (opt-in path retained, not deleted)"
    verification:
      - kind: unit
        ref: "server/test_plane_detection.py#airplaneslive remains a selectable opt-in, absent from the default order"
      - kind: manual_procedural
        ref: "server/.venv/bin/python3 -c \"import server.plane.detect as d; assert 'airplaneslive' in d.PROVIDERS\""
        status: pass
    human_judgment: false
  - id: D3
    description: "server/test_plane_detection.py contains a regression check that fails if airplanes.live is ever returned to the default provider order"
    verification:
      - kind: unit
        ref: "server/test_plane_detection.py (8/8, EXPECTED_CHECK_COUNT=8)"
        status: pass
    human_judgment: false
  - id: D4
    description: "All 9 test harnesses, the attribution check, and ruff pass; README's stated check total matches the harnesses' actual sum"
    verification:
      - kind: integration
        ref: "./scripts/run-all-tests.sh (9/9 harnesses, 119/119 checks, 80% coverage)"
        status: pass
      - kind: other
        ref: "./scripts/check-attribution.sh"
        status: pass
      - kind: other
        ref: "server/.venv/bin/python3 -m ruff check ."
        status: pass
    human_judgment: false
  - id: D5
    description: "COMPLIANCE.md has zero open items and records the 2026-08-27 reply, the live 403, and the demotion"
    requirement: "PLANE-03"
    verification:
      - kind: other
        ref: "grep -c 'OPEN' COMPLIANCE.md == 0"
        status: pass
    human_judgment: true
    rationale: "The plan's own human-check asks a reader to confirm the rewritten airplanes.live entry reads as factual paraphrase rather than reproduction, and that it does not overstate what the reply said — a judgment call the automated OPEN-marker grep cannot make."
  - id: D6
    description: "README.md, ARCHITECTURE.md, PROJECT.md, and REQUIREMENTS.md each describe adsb.fi as the queried provider, with identical adsb.fi citation text in COMPLIANCE.md and README.md"
    verification:
      - kind: other
        ref: "grep -c 'primary aggregator source' README.md == 0; grep -c 'airplanes.live primary' ARCHITECTURE.md == 0; grep -c 'airplanes.live primary' .planning/REQUIREMENTS.md == 0"
        status: pass
    human_judgment: false
  - id: D7
    description: "Historical records under adsb-test/, server/fixtures/README.md, and the superseded PROJECT.md decision row are unmodified in substance"
    verification:
      - kind: other
        ref: "git diff --numstat -- adsb-test/ server/fixtures/README.md server/poll_loop.py (empty output)"
        status: pass
    human_judgment: false
  - id: D8
    description: "PROJECT.md's new Key Decisions row states the rationale honestly (coverage + cadence + sample-error figures) without cherry-picking in adsb.fi's favour"
    requirement: "PLANE-03"
    verification: []
    human_judgment: true
    rationale: "The plan's human-check explicitly asks a reader to confirm the new row does not cherry-pick RESULTS.md's figures — this is a qualitative honesty check on prose, not something a grep/assert can certify."

duration: ~25min
completed: 2026-08-27
status: complete
---

# Quick Task 260827-1i6: Demote airplanes.live to opt-in, promote adsb.fi to sole default provider Summary

**adsb.fi is now the sole default ADS-B provider in production; airplanes.live is retained as an explicit, opt-in-only `--provider` path after its 2026-08-27 free-tier shutdown, with a machine-pinned regression check and zero open compliance items.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-27
- **Tasks:** 3/3 completed
- **Files modified:** 7

## Accomplishments

- `server/plane/detect.py`: `PROVIDERS` dict reordered (adsb.fi first), new `DEFAULT_PROVIDER_ORDER = ("adsbfi",)` constant, `poll_current_aircraft()` now defaults to that constant instead of every registered provider, `--provider` CLI default changed to `adsbfi`, and `--provider both` now resolves explicitly to `list(PROVIDERS)` (adsb.fi first) instead of inheriting the old both-providers default via `None`.
- `server/test_plane_detection.py`: two new regression checks (8/8 total) — one monkeypatches `query_provider` and asserts a default (no-`providers`-argument) poll records exactly `["adsbfi"]`; the other asserts `airplaneslive` stays a key of `PROVIDERS` (selectable) while being absent from `DEFAULT_PROVIDER_ORDER` (not automatic). This pins the demotion so a future PR silently re-adding airplanes.live to the default path fails the suite.
- `COMPLIANCE.md`: the airplanes.live entry rewritten from "OPEN — unconfirmed terms" to "resolved" — records the 2026-08-27 reply (free tier discontinued for cost-sustainability reasons; access now requires a feeder, a paid sponsorship, or a commercial licence) as plain paraphrase, plus the same-day live verification (production endpoint template returns HTTP 403, adsb.fi's returns HTTP 200). The adsb.fi entry, runtime-behaviour section, and status table were all updated to reflect its new primary/sole-default role. Zero `OPEN` markers remain.
- `README.md`, `ARCHITECTURE.md`: Data sources section and end-to-end diagram/Detection paragraph rewritten to describe a single-provider poll with no automatic fallback; README's citation sentence for adsb.fi is now byte-identical to COMPLIANCE.md's blockquote (both read "...the sole aggregator source an automated poll queries as of 2026-08-27"). README's stated test-check total was recomputed and updated (117 → 119, matching the two new plane-detection checks).
- `.planning/PROJECT.md`: context bullet corrected with a dated 2026-08-27 note; the original aggregator-choice Key Decisions row kept its Decision/Rationale cells untouched (per the plan's scope boundary) with only a supersession note appended to its Outcome cell; a new dated row records the promotion, citing RESULTS.md's real coverage (38 vs 37 distinct aircraft ≤3000ft), cadence (36.2s vs 22.4s median update gap — adsb.fi is slower, judged immaterial by RESULTS.md's own verdict), and sample-error (1 vs 0, adsb.fi is the one with the error) figures without claiming adsb.fi is the better provider.
- `.planning/REQUIREMENTS.md`: PLANE-03's provider parenthetical updated to name adsb.fi; requirement substance and validated status unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make adsb.fi the sole default provider, pin it with a regression check** - `f2b9a30` (feat)
2. **Task 2: Close the compliance item and correct the shipped repo docs** - `5bfbeb6` (docs)
3. **Task 3: Record the reversal in the planning artifacts** - `93d05af` (docs)

**Follow-up completeness fix (Rule 2):** `bc2cc98` (docs) — added the RESULTS.md sample-error figure (adsbfi: 1, airplaneslive: 0) to PROJECT.md's new Key Decisions rationale, alongside the coverage and cadence figures already cited, so the row cites all three recorded metrics rather than two of three.

## Files Created/Modified

- `server/plane/detect.py` - PROVIDERS reordered, DEFAULT_PROVIDER_ORDER added, poll/CLI defaults changed to adsb.fi-only, rewritten dual-purpose block comment
- `server/test_plane_detection.py` - two new regression checks (7, 8), EXPECTED_CHECK_COUNT 6 → 8
- `README.md` - Data sources section rewritten, Tests section check-count updated 117 → 119
- `COMPLIANCE.md` - airplanes.live entry resolved, adsb.fi entry/runtime-behaviour/status table updated to primary+sole
- `ARCHITECTURE.md` - end-to-end diagram relabeled (alignment preserved), Detection paragraph rewritten
- `.planning/PROJECT.md` - context bullet corrected, original Key Decisions row annotated (Outcome only), new dated row added
- `.planning/REQUIREMENTS.md` - PLANE-03 provider parenthetical updated

## Decisions Made

- adsb.fi promoted to sole default ADS-B provider; airplanes.live retained in code as an explicit `--provider` opt-in (feeder operator / sponsor / licensee use), never queried automatically. No sponsorship, feeder, or commercial licence purchased — all three were considered and declined per the developer's prior instruction (`decision_already_made`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - missing completeness] PROJECT.md's Key Decisions rationale omitted the sample-error figure on first pass**
- **Found during:** Post-Task-3 self-review against the plan's human-check instructions
- **Issue:** The new Key Decisions row cited RESULTS.md's coverage figures (38 vs 37) and cadence figures (36.2s vs 22.4s) but not the sample-error count (adsbfi: 1, airplaneslive: 0) — the plan's `ground_truth_corrections` block specifically flagged this figure as easy to get backwards or drop, and the human-check asks a reader to confirm all three metrics are represented honestly.
- **Fix:** Added a clause citing the sample-error figures in the same sentence as the cadence figures.
- **Files modified:** `.planning/PROJECT.md`
- **Verification:** Re-ran Task 3's Python verification block (`Key Decisions heading`, dated entry, `airplanes.live primary` still present in the historical row) — still passes.
- **Committed in:** `bc2cc98`

---

**Total deviations:** 1 auto-fixed (Rule 2)
**Impact on plan:** Strengthens the honesty of a documentation sentence per the plan's own human-check instruction; no scope creep, no code behaviour change.

## Issues Encountered

The worktree's `server/.venv` did not exist (each git worktree needs its own virtualenv, and this sandbox has no network access to PyPI to build one from `server/requirements.txt`/`server/requirements-dev.txt`). Resolved by pointing `PYTHON=` at the main checkout's already-provisioned `server/.venv` (`/Users/florian/Projects/skypane/server/.venv/bin/python3`) for every verification command and `scripts/run-all-tests.sh` invocation in this session — no interpreter, dependency, or version drift from what CI/the main checkout uses, since it is literally the same venv. No files were left behind in the worktree from this workaround (a symlink attempt was made and then removed rather than committed).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 4 (CI/CD, Documentation & Legal Compliance) remains fully closed; this quick task only corrected drift discovered after that phase shipped (airplanes.live's free-tier shutdown, 2026-08-27). No blockers. The next time CI runs on this repository, it will exercise the same 8/8 `plane-detection` checks and the updated 119-check total via `scripts/run-all-tests.sh`, unchanged in structure from what plan 04-04 already wired up.

---
*Quick task: 260827-1i6*
*Completed: 2026-08-27*

## Self-Check: PASSED

All 8 claimed files confirmed present on disk (`server/plane/detect.py`, `server/test_plane_detection.py`, `README.md`, `COMPLIANCE.md`, `ARCHITECTURE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, this SUMMARY.md). All 4 claimed commit hashes confirmed present in `git log --oneline --all` (`f2b9a30`, `5bfbeb6`, `93d05af`, `bc2cc98`). No missing items.
