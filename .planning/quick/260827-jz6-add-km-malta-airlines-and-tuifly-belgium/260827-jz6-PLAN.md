---
phase: quick-260827-jz6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plane/enrich.py
  - server/plane/illustrations.py
  - server/test_enrich.py
  - server/test_illustrations.py
  - server/assets/icons/illustrations/HANDOFF.md
  - server/assets/icons/illustrations/VENDOR.md
autonomous: true
requirements: [PLANE-01, PLANE-02]
user_setup: []

must_haves:
  truths:
    - "A real KM Malta Airlines flight over runway 3 (callsign prefix KMM) resolves the airline name 'KM Malta Airlines' with zero network call, even though adsbdb has no record of the carrier."
    - "A real TUIfly Belgium flight (callsign prefix JAF) resolves the airline name 'TUIfly Belgium' through the airline-only fallback path, with the adsbdb-hit divergence to 'Jetairfly' recorded as a known, accepted consequence rather than a defect."
    - "Both new carriers appear in the machine-reported illustration hand-off plan (`illustrations.py --targets`) as outstanding targets the developer can generate art for."
    - "Adding two not-yet-delivered targets leaves the full test suite, ruff, check-attribution.sh, and `illustrations.py --validate` green — nothing enforces a file that does not exist on disk yet."
    - "A future reader can tell from HANDOFF.md alone why TUIfly Belgium is filed under its current brand name while Europe Airpost/Corsairfly/CCM Airlines are not."
  artifacts:
    - server/plane/enrich.py  # two new _ICAO_AIRLINE_PREFIXES rows with live-curl evidence comments
    - server/plane/illustrations.py  # two new _ILLUSTRATION_TARGETS primary entries
    - server/test_enrich.py  # two new airline_from_callsign() checks, EXPECTED_CHECK_COUNT 25 -> 27
    - server/test_illustrations.py  # one new target_airline_names() drift guard, EXPECTED_CHECK_COUNT 43 -> 44
    - server/assets/icons/illustrations/HANDOFF.md  # required-files list, naming-rules exception, coverage caveat, 2 new prompts
    - server/assets/icons/illustrations/VENDOR.md  # outstanding-items record updated 1 -> 3, KM Malta coverage note corrected
  key_links:
    - "enrich._ICAO_AIRLINE_PREFIXES values -> illustrations.target_airline_names() (existing check 24 drift guard: adding a prefix value with no illustration target fails the suite)"
    - "illustrations._ILLUSTRATION_TARGETS -> target_filenames() -> outstanding_filenames() -> HANDOFF.md prompt ordering (HANDOFF's own stated invariant: prompts are printed in --targets order)"
    - "normalise_airline_key('KM Malta Airlines') -> 'km-malta-airlines' and normalise_airline_key('TUIfly Belgium') -> 'tuifly-belgium' — the filenames are derived, never hand-typed"
    - "scripts/check-attribution.sh walks files on disk only — a filename named in VENDOR.md with no file behind it is not a gap"
---

<objective>
Add **KM Malta Airlines** (ICAO `KMM`) and **TUIfly Belgium** (ICAO `JAF`) as two
new target airlines across the existing, already-correct Phase 3.1 D-09 hand-off
machinery: the illustration target table, the callsign-prefix airline resolution
table, the hand-off spec, the provenance record, and regression coverage for
both.

Purpose: both carriers really fly Orly (KM Malta scheduled, TUIfly Belgium on
winter Orly↔Morocco charter), and since quick task 260827-hyy the panel no
longer needs an adsbdb hit to identify an airline — `airline_from_callsign()`
resolves the carrier straight from the ICAO callsign prefix. That change retired
the original reason KM Malta was excluded from the Phase 3.1 target set ("adsbdb
never resolves it, so no illustration can ever be reached"), which is now false.

Output: two new outstanding illustration targets (`km-malta-airlines.png`,
`tuifly-belgium.png`), two new prefix-table rows with live-curl evidence
comments, updated hand-off + provenance docs, and three new automated checks.
**No PNG artwork is produced by this plan** — the two files are named,
specified, and reported as outstanding so the developer can generate them
externally afterward (D-09, unchanged).

## Decisions this plan implements

| ID | Decision |
|---|---|
| **QT-jz6-D-01** | KM Malta Airlines is filed under its **real current brand name**. Live-verified 2026-08-27: `curl https://api.adsbdb.com/v0/callsign/KMM466` returns `"unknown callsign"` — adsbdb was never updated for the 2023 Air Malta → KM Malta Airlines rebrand and can never produce a competing string. The stale-brand-mirroring rule has nothing to mirror here, so this is *not* an exception to it. Same class as the existing `EJU` → easyJet row. |
| **QT-jz6-D-02** | TUIfly Belgium is filed under its **real current brand name** — a deliberate, explicitly-named **EXCEPTION** to the precedent that governs `FPO`→"Europe Airpost", `CRL`→"Corsairfly", and `CCM`→"CCM Airlines". Live-verified 2026-08-27: `curl https://api.adsbdb.com/v0/callsign/JAF7521` **does** resolve, returning `"Jetairfly"` (the pre-2016 legacy brand). The developer chose the current name knowing the consequence: an adsbdb hit renders "Jetairfly" and falls through to a lower illustration tier, while the airline-only fallback renders "TUIfly Belgium" and reaches `tuifly-belgium.png`. **This is known and accepted — it is not a bug to solve in this plan, and it must be documented as a decision, not as an inconsistency to fix later.** |
| **QT-jz6-D-03** | Illustrated aircraft: KM Malta → **Airbus A320neo** (100% A320neo fleet, A320-251N); TUIfly Belgium → **Boeing 737 MAX 8** specifically (distinctive split-tip winglets), not the 737-800, despite the near-even split on the Orly–Morocco route. |
| **QT-jz6-D-04** | **No secondary-variant (`{slug}-{shape}.png`) files** for either carrier. Both map to a single existing shape bucket (`a320` via `A20N`, `b737` via `B38M`/`B738`), and HANDOFF.md's own suffix rule already states one illustration covers a whole family. Two new files total, both unsuffixed primaries. |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@server/plane/enrich.py
@server/plane/illustrations.py
@server/test_enrich.py
@server/test_illustrations.py
@server/assets/icons/illustrations/HANDOFF.md
@server/assets/icons/illustrations/VENDOR.md
</context>

<interface_context>
Verified against the live tree before planning — treat these as facts, do not
re-derive them:

- `illustrations._ILLUSTRATION_TARGETS` is a list of
  `(resolved_airline_name, shape_slug_or_None, note)` tuples. Its current
  layout is: 22 primary entries (`shape=None`), then a trailing
  `# --- P-04 secondary-variant files for mixed-fleet airlines ---` block of
  4 entries. `target_filenames()` walks it in list order, then appends one
  `generic-{shape}.png` per `SHAPE_SLUGS`, then `generic-fallback.png`.
  Current totals: **34 targets, 1 outstanding** (`royal-air-maroc-embraer.png`).
- `required_filenames()` = the pre-3.1 baseline ∪ *only those* `target_filenames()`
  entries that already exist on disk. **Adding a target for a file that does not
  exist cannot fail `--validate` or the suite.** `_validate_directory()` only
  prints `OUTSTANDING <name>` lines and a count for them (`strict_targets` is
  False by default). Confirmed by reading the code — do not re-litigate.
- `_validate_directory()` also fails on any `.png` on disk that is *not* in
  `target_filenames()`. Adding targets can only ever relax that check.
- `enrich._ICAO_AIRLINE_PREFIXES` currently holds 23 rows, each with a trailing
  evidence comment in one of three established forms: `# callsign XXXNNNN`
  (a real live callsign), `# airline endpoint XXX`, or `# cited callsign XXXNNN`.
  The one multi-line entry is `EJU`, which carries a block comment above the row
  explaining why it is a brand-level exception — **that is the precedent to
  follow for both new rows**, since both need a real explanation rather than a
  one-line citation.
- `test_enrich.py` check **24** already asserts
  `set(_ICAO_AIRLINE_PREFIXES.values()) ⊆ set(illustrations.target_airline_names())`.
  This means Task 1's two halves are load-bearing on each other: adding a prefix
  row without the matching illustration target **will** fail the suite. Land both
  in the same commit.
- Both harnesses gate on a module-level `EXPECTED_CHECK_COUNT` constant
  (`test_enrich.py` = **25**, `test_illustrations.py` = **43**) and fail if the
  actual check count differs. Adding a check without bumping the constant fails.
- `scripts/check-attribution.sh` enumerates **files found on disk** under
  `server/assets/` and requires each to be named in some `VENDOR.md`. It never
  performs the reverse check. Verified by reading the script: naming a
  not-yet-delivered filename in `VENDOR.md` is safe and is exactly the discipline
  HANDOFF.md already states ("whatever remains outstanding when you stop is
  recorded by name in `VENDOR.md`").
- HANDOFF.md's prompt sections are `### N. \`filename.png\` (optional note)`,
  numbered 1–34, and the file explicitly claims "Prompts below are grouped in the
  same order `--targets` prints them." **This invariant currently holds** —
  verified by diffing the extracted heading filenames against `--targets` output
  before planning. Task 2 must keep it holding.
- Test runner: `scripts/run-all-tests.sh` (9 harnesses under coverage,
  `fail_under=75`). Lint is separate: `server/.venv/bin/ruff check .`.
- No change to `render.py` or `poll_loop.py` is needed or permitted.
  `_TYPE_SHAPE_BUCKETS` already contains `A20N`→`a320` and `B38M`/`B738`→`b737`,
  and `render._TYPE_DISPLAY_LABELS` already labels all three designators.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire both carriers into the illustration target table and the ICAO-prefix airline table, with regression coverage</name>
  <files>server/plane/illustrations.py, server/plane/enrich.py, server/test_illustrations.py, server/test_enrich.py</files>
  <behavior>
    - `illustrations.target_airline_names()` contains both `"KM Malta Airlines"` and `"TUIfly Belgium"`.
    - `illustrations.target_airline_names()` contains neither `"Air Malta"` (KM Malta's superseded predecessor) nor `"Jetairfly"` (the stale adsbdb string deliberately not adopted, per QT-jz6-D-02) — asserted as Python value membership, not as a text search over any source file.
    - `illustrations.target_filenames()` contains `"km-malta-airlines.png"` and `"tuifly-belgium.png"`, and its length is 36.
    - `illustrations.outstanding_filenames()` is exactly `["royal-air-maroc-embraer.png", "km-malta-airlines.png", "tuifly-belgium.png"]` in target order (nothing on disk changed, so all three are still missing).
    - `enrich.airline_from_callsign("KMM466")` returns `"KM Malta Airlines"`.
    - `enrich.airline_from_callsign("JAF7521")` returns `"TUIfly Belgium"`.
    - The pre-existing drift guard (test_enrich check 24) still passes: every prefix-table value is a `target_airline_names()` member.
    - The pre-existing shape guard (test_enrich check 25) still passes: both new keys are exactly 3 uppercase A–Z characters.
  </behavior>
  <action>
Land all four files in one commit — test_enrich check 24 makes the enrich.py and illustrations.py halves mutually load-bearing, so splitting them would leave a red intermediate state.

**1. `server/plane/illustrations.py` — two new `_ILLUSTRATION_TARGETS` entries.**
Append them at the **end of the primary block**, immediately after the
`"Corsairfly"` entry and **before** the `# --- P-04 secondary-variant files for
mixed-fleet airlines ---` comment. This placement keeps `target_filenames()`
ordered as 24 primaries → 4 secondaries → 7 generics → universal fallback, which
is what HANDOFF.md's "Required files" categorical listing mirrors. Introduce them
under a new section comment naming this quick task (`260827-jz6`) and the date,
matching the existing `# --- Step-C airlines newly live-resolved this phase ---`
style.

Both entries are primaries: `shape` is `None` (QT-jz6-D-04). Use the multi-line
tuple form the existing `Europe Airpost` / `Corsairfly` entries use, since both
notes are long:

- `("KM Malta Airlines", None, <note>)` — the note must state that this carrier
  is a confirmed permanent adsbdb miss (live-verified 2026-08-27, callsign
  `KMM466` returns `unknown callsign`), that it is reachable only via
  `enrich.airline_from_callsign()`'s prefix path (quick task 260827-hyy), and
  that the real current brand name is correct here precisely *because* no adsbdb
  string exists to mirror (QT-jz6-D-01). Carry a `[VERIFIED-CALLSIGN-MISS]`
  verdict token, a deliberately new token distinct from the existing
  `[VERIFIED-CALLSIGN]` / `[VERIFIED-AIRLINE-ENDPOINT-ONLY]` / `[CITED: ...]`
  ones, because this is a genuinely different evidence class — a confirmed
  negative, not a confirmed positive.
- `("TUIfly Belgium", None, <note>)` — the note must state that this is a
  deliberate, developer-chosen **exception** to the stale-brand-mirroring rule
  that produced `Europe Airpost` and `Corsairfly` two entries above; that
  adsbdb *does* resolve `JAF7521` (live-verified 2026-08-27) to the pre-2016
  legacy brand; and that the accepted consequence is a divergence between the
  adsbdb-hit render and the airline-only fallback render for the same real
  carrier. Carry a `[VERIFIED-CALLSIGN-STALE-NAME-OVERRIDDEN]` verdict token.
  Point the reader at `HANDOFF.md`'s "Naming rules" section for the full record
  (Task 2 writes it).

Also extend the module docstring's "Filenames mirror the data source, never the
current public brand name" section with a short closing paragraph noting that
`260827-jz6` introduced exactly one developer-approved override of that rule
(TUIfly Belgium) and why the rule still stands everywhere else. Do not rewrite
the existing paragraph — append to it.

Optionally (cosmetic, keep it to one line each) extend the airline lists in
`_TYPE_SHAPE_BUCKETS`' `# A320 family (D-03: ...)` and `# B737 family (D-03: ...)`
comments to mention KM Malta and TUIfly Belgium respectively. No table values
change — `A20N`, `B738`, and `B38M` are already mapped.

**2. `server/plane/enrich.py` — two new `_ICAO_AIRLINE_PREFIXES` rows.**
Append both after the existing final `"FBU"` row, each preceded by a block
comment in the same shape as the existing `EJU` entry (the established precedent
for a row that needs an explanation rather than a one-line citation):

- `"KMM": "KM Malta Airlines"` — the comment cites this session's live
  verification by naming the exact command run (`curl` against
  `https://api.adsbdb.com/v0/callsign/KMM466`) and its exact result
  (`unknown callsign`), dated 2026-08-27; states the carrier replaced Air Malta
  (ICAO `AMC`, ceased March 2024) and that adsbdb was never updated for the 2023
  rebrand; and concludes that, exactly like `EJU` above, this value can never be
  contradicted by a live adsbdb hit. **Cite this session's live curl, not
  `03.1-LIVE-RESOLUTION.md`** — neither carrier appears in that file.
- `"JAF": "TUIfly Belgium"` — the comment cites the same-dated live `curl`
  against `https://api.adsbdb.com/v0/callsign/JAF7521`, states that it **does**
  resolve and returns the pre-2016 legacy brand string, and records QT-jz6-D-02
  in full: the developer's explicit choice of the current brand name, that this
  is a *named exception* to the `FPO`/`CRL`/`CCM` precedent above it and not an
  oversight, and the accepted consequence (adsbdb-hit path renders the legacy
  name and drops to a lower illustration tier; the airline-only fallback path
  renders `TUIfly Belgium` and reaches its own art).

Both rows are pure static-table data. Do not touch `airline_from_callsign()`,
`_AIRLINE_PREFIX_SHAPE_RE`, `airline_only_route()`, or `resolve_route()` — the
existing 3-uppercase-letter prefix shape already admits `KMM` and `JAF`.

**3. `server/test_enrich.py` — two new checks.**
Add checks **26** and **27** immediately after check 25
(`_prefix_table_keys_are_three_uppercase_letters`) and before the
`total = len(results)` block, following the file's exact
`def _name(): ... return True, ""` + `check("<description>", _name)` convention
with a numbered lead comment. Check 26 asserts
`airline_from_callsign("KMM466") == "KM Malta Airlines"`; check 27 asserts
`airline_from_callsign("JAF7521") == "TUIfly Belgium"`. Use the real callsigns
this session actually curled, not invented ones, and say so in the lead comment.
Bump `EXPECTED_CHECK_COUNT` from 25 to 27.

**4. `server/test_illustrations.py` — one new drift-guard check.**
Add check **44** immediately after check 43
(`_target_airline_names_carries_resolved_not_brand_names`), mirroring that
check's structure exactly. It must assert, against `ill.target_airline_names()`,
that both new names are present and that neither `"Air Malta"` nor `"Jetairfly"`
is — the second half is the guard that keeps QT-jz6-D-02's deliberate override
from silently being "corrected" later by someone applying the stale-brand rule
mechanically. Additionally assert both new filenames are in
`ill.target_filenames()`, proving `normalise_airline_key()` derives the slugs the
hand-off spec and the developer's generation batch will use. Bump
`EXPECTED_CHECK_COUNT` from 43 to 44.

Do not add a check that asserts a specific total target count — no such check
exists today and hardcoding 36 would need editing on every future addition.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_enrich.py &amp;&amp; server/.venv/bin/python3 server/test_illustrations.py &amp;&amp; test "$(server/.venv/bin/python3 server/plane/illustrations.py --targets | wc -l | tr -d ' ')" = "36" &amp;&amp; test "$(server/.venv/bin/python3 server/plane/illustrations.py --targets | grep -cE '^(km-malta-airlines|tuifly-belgium)\.png$')" = "2" &amp;&amp; test "$(server/.venv/bin/python3 server/plane/illustrations.py --outstanding | wc -l | tr -d ' ')" = "3" &amp;&amp; server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane.enrich import airline_from_callsign as a; assert a('KMM466')=='KM Malta Airlines', a('KMM466'); assert a('JAF7521')=='TUIfly Belgium', a('JAF7521'); print('prefix-rows-live-OK')" &amp;&amp; server/.venv/bin/ruff check .</automated>
  </verify>
  <done>
`test_enrich.py` reports 27/27 and `test_illustrations.py` reports 44/44.
`--targets` prints 36 filenames including both new ones; `--outstanding` prints
exactly 3. `airline_from_callsign()` returns the two new names for the two real
curled callsigns. `ruff check .` is clean. All four files are in one commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the D-09 hand-off spec and the provenance record to cover both new outstanding targets</name>
  <files>server/assets/icons/illustrations/HANDOFF.md, server/assets/icons/illustrations/VENDOR.md</files>
  <action>
Both documents currently assert things Task 1 just made false. Every edit below
is a correction of a now-stale claim or an addition at the same detail level as
the surrounding entries — do not restructure either file.

**A. `HANDOFF.md` — required-files list.**
- Header paragraph (the "Phase 3.1 scaled this hand-off…" block): append a
  sentence recording that quick task `260827-jz6` (2026-08-27) added two further
  carriers, taking the plan from 34 to 36 files. Leave the Phase 3/3.1 history
  sentences intact — this file is cumulative.
- `## Required files (34 total, 8 already vendored)` → `36 total, 8 already vendored`.
- `**Airline primary files (22)**` → `(24)`, and append two lines to that fenced
  block in the same aligned style as the existing rows:
  `km-malta-airlines.png` (with a short `see Coverage caveat` pointer) and
  `tuifly-belgium.png` (with a `see Naming rules — this is the one approved
  current-brand override` pointer). Neither carries an asterisk; both are new.
- Leave the secondary-variant (4) and neutral-fallback (8) counts untouched —
  QT-jz6-D-04 adds no files to either group.

**B. `HANDOFF.md` — Naming rules.** This section currently reads as an absolute
rule ("**never** from the carrier's current public brand name"). Task 1 created
one approved override of it, so leaving the section as-is would make the file
lie. Append a clearly-headed subsection after the existing Europe Airpost /
Corsairfly bullets — proposed heading: **"The one approved override
(`tuifly-belgium.png`)"** — stating: the ICAO prefix; that adsbdb *does* resolve
a real `JAF` callsign, live-verified this session, to the pre-2016 legacy brand
name; that the developer chose the current brand name anyway, deliberately and
with the tradeoff in hand (QT-jz6-D-02); the exact accepted consequence in
selection terms (adsbdb-hit → the legacy string → no matching file → falls to
Tier 3 `generic-b737.png`; airline-only fallback → `TUIfly Belgium` →
`tuifly-belgium.png`); and an explicit instruction that this is a recorded
decision, **not** an inconsistency for a future reader to "fix" by renaming the
file. Close by reaffirming that the rule still governs every other entry,
naming Europe Airpost / Corsairfly / CCM Airlines as the cases where it holds.

**C. `HANDOFF.md` — Coverage caveat section.** The bullet reading
"**KM Malta Airlines (`KMM`) remains excluded**, unchanged from Phase 3 — same
confirmed-miss status" is now factually wrong and must be **replaced**, not
supplemented. The replacement bullet states: KM Malta Airlines is now a target
(`km-malta-airlines.png`); it is still a confirmed permanent adsbdb miss
(live-verified 2026-08-27, `KMM466` → `unknown callsign`); and the reason that no
longer excludes it is that quick task 260827-hyy's
`enrich.airline_from_callsign()` resolves the carrier straight from the ICAO
prefix, so an adsbdb miss no longer costs the airline identity. Then update the
adjacent `EJU` bullet: it still says no file is requested *because* the airline
name is never available, which the same 260827-hyy change also falsified — `EJU`
maps to `easyJet` in the prefix table and reaches the existing `easyjet.png`, so
correct that bullet's stated reason (no *separate* file is requested, because
`EJU` and `EZY` are the same brand and share one asset) rather than deleting it.
Add a new bullet introducing TUIfly Belgium: winter Orly↔Morocco charter
service, `JAF` prefix, and a pointer to the Naming rules override subsection.

**D. `HANDOFF.md` — two new prompt sections, and renumbering.** The file states
that prompts appear in `--targets` order; Task 1 placed both new entries at the
end of the primary block, so they become **#23** (`km-malta-airlines.png`) and
**#24** (`tuifly-belgium.png`), and every existing section from the current #23
through #34 shifts to #25–#36. Insert the two new sections between the current
#22 (`corsairfly.png`) and #23 (`ccm-airlines-atr72.png`), then renumber the
twelve sections below them. Write both prompts at the same level of detail as
the surrounding ones, reusing the file's fixed closing boilerplate verbatim
(nose pointing LEFT / transparent background with real alpha / no ground, sky or
shadow / clean flat illustration style, crisp hard edges, vintage aviation
poster plate):
- `km-malta-airlines.png` — an **Airbus A320neo** (QT-jz6-D-03) in KM Malta
  Airlines livery: white fuselage, the red two-tone Maltese Cross on the tail,
  blue and red accents. Note in the section heading that this is the post-2023
  livery, explicitly **not** the superseded Air Malta red-tail scheme, so the
  generator does not produce the predecessor airline's art.
- `tuifly-belgium.png` — a **Boeing 737 MAX 8** (QT-jz6-D-03, name the split-tip
  winglets explicitly since they are what distinguishes it from the 737-800) in
  the TUI Group "Dynamic Wave" livery: light blue and white fuselage, blue wave
  sweep along the body, red TUI fuselage titles, red "smile" logo on the tail.
  Note in the heading that this is the current TUI livery, not the superseded
  Jetairfly scheme.

**E. `VENDOR.md` — coverage records.**
- `## Coverage note`: the bullet "**KM Malta Airlines (`KMM`) remains
  excluded**, unchanged from Phase 3 — same confirmed-miss status, not re-probed
  this phase" is stale twice over (it *was* re-probed, and it is no longer
  excluded). Replace it with the same corrected status Task 2.C writes into
  HANDOFF.md, citing this session's live curl and quick task 260827-hyy as the
  reason the exclusion lifted. Apply the same reason-correction to the `EJU`
  bullet as in 2.C, so the two documents agree.
- Add a new `### Quick task 260827-jz6 (2026-08-27) — two new targets, both
  outstanding` subsection under the Phase 3.1 coverage material. It records:
  target count 34 → 36; outstanding 1 → 3; both new filenames **by name**
  (`km-malta-airlines.png`, `tuifly-belgium.png`) with the aircraft type
  specified for each (A320neo / 737 MAX 8) and the livery in one line each; the
  live-curl evidence for both ICAO prefixes, dated; and QT-jz6-D-02's named
  override with its accepted consequence. State plainly that **no artwork was
  generated by this task** and that both files await an external developer-side
  generation batch per D-09, so neither has a sha256/dimensions row yet — a
  digest row gets added to the Phase 3.1-style table only when a real file lands
  on disk.
- Update the existing `**Outstanding:** 1 file — royal-air-maroc-embraer.png`
  line in the `## Phase 3.1 coverage` section so it no longer contradicts the new
  count. Keep it accurate to its own scope (it is the one file outstanding *from
  the Phase 3.1 batch*) and add a pointer to the new subsection for the current
  total of 3.

Do not add sha256 or dimension rows for either new file — neither exists.
Do not touch `server/assets/icons/VENDOR.md` (the parent), which records only the
Phase 3 baseline eight.
  </action>
  <verify>
    <automated>diff &lt;(grep -oE '^### [0-9]+\. `[^`]+`' server/assets/icons/illustrations/HANDOFF.md | sed -E 's/.*`([^`]+)`.*/\1/') &lt;(server/.venv/bin/python3 server/plane/illustrations.py --targets) &amp;&amp; test "$(grep -oE '^### [0-9]+\.' server/assets/icons/illustrations/HANDOFF.md | grep -oE '[0-9]+' | tr '\n' ' ')" = "$(seq 1 36 | tr '\n' ' ')" &amp;&amp; for f in km-malta-airlines.png tuifly-belgium.png; do grep -qF "$f" server/assets/icons/illustrations/HANDOFF.md || { echo "HANDOFF missing $f"; exit 1; }; grep -qF "$f" server/assets/icons/illustrations/VENDOR.md || { echo "VENDOR missing $f"; exit 1; }; done &amp;&amp; test "$(grep -c 'remains excluded' server/assets/icons/illustrations/HANDOFF.md server/assets/icons/illustrations/VENDOR.md | grep -c ':0$')" = "2" &amp;&amp; scripts/check-attribution.sh &amp;&amp; server/.venv/bin/python3 server/plane/illustrations.py --validate &amp;&amp; echo DOCS-OK</automated>
  </verify>
  <done>
HANDOFF.md's 36 prompt headings are numbered 1–36 with no gaps and their
filenames match `--targets` output line-for-line. Both new filenames appear in
HANDOFF.md and VENDOR.md. Neither document still contains the stale
"remains excluded" claim about KM Malta. `check-attribution.sh` passes (it walks
disk files only, so the two named-but-undelivered PNGs are correctly not a gap)
and `--validate` exits 0 while reporting 3 outstanding targets.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-gate verification and developer hand-off report</name>
  <files>(no source edits — verification and reporting only)</files>
  <action>
Run the project's three canonical gates end to end and confirm nothing regressed:
`scripts/run-all-tests.sh` (all 9 harnesses under coverage, `fail_under=75`),
`server/.venv/bin/ruff check .`, and `scripts/check-attribution.sh`. Confirm the
enrich harness reports 27/27 and the illustrations harness 44/44 in the aggregate
run output, not just in isolation — the aggregate run is what CI executes.

If coverage drops below the 75 floor, do **not** lower the floor. Two static-table
rows plus three checks should not move coverage measurably; a real drop means
something else changed and must be investigated before commit.

Then produce the hand-off report in the SUMMARY, since the artwork is the
developer's next real-world action and this environment cannot produce it:
- the exact two filenames to generate, with their target directory
  (`server/assets/icons/illustrations/`);
- the exact aircraft type and livery for each (QT-jz6-D-03);
- the copy-pasteable commands the developer runs after dropping files in:
  `--outstanding`, then `--validate`, then `--outstanding` again, then the
  by-eye nose-left and type-matches-filename confirmation, exactly as
  HANDOFF.md's "After generating the files" section already prescribes;
- a reminder that `royal-air-maroc-embraer.png` is still outstanding from the
  Phase 3.1 batch, so the current outstanding list is 3 files, not 2.

Record in the SUMMARY that no PNG artwork was generated, faked, or placed by
this task, and that `render.py` / `poll_loop.py` were deliberately untouched
(quick tasks 260827-hyy and 260827-itz already wired that machinery correctly;
this task only added entries to it).
  </action>
  <verify>
    <automated>scripts/run-all-tests.sh &amp;&amp; server/.venv/bin/ruff check . &amp;&amp; scripts/check-attribution.sh &amp;&amp; test "$(server/.venv/bin/python3 server/plane/illustrations.py --outstanding | sort | tr '\n' ' ')" = "km-malta-airlines.png royal-air-maroc-embraer.png tuifly-belgium.png "</automated>
  </verify>
  <done>
All 9 harnesses pass in the aggregate run with `enrich: 27/27` and
`illustrations: 44/44` visible in its output; coverage is at or above 75.
`ruff check .` and `scripts/check-attribution.sh` both pass. `--outstanding`
lists exactly the three expected filenames. The SUMMARY carries the developer
hand-off report and an explicit statement that no artwork was produced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregator → `detect.py` → `enrich.airline_from_callsign()` | An attacker-influenced or malformed ADS-B `flight`/callsign string crosses into the static prefix table lookup. |
| resolved `airline_name` → `illustrations.normalise_airline_key()` → filesystem path | An airline-name string reaches path construction under `ILLUSTRATION_DIR`. |
| rendered panel → the developer's own eyes | The panel asserts a real-world carrier identity for a real, physical aircraft overhead. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-jz6-01 | Tampering | `illustrations.illustration_path_for_key()` fed a new `_ILLUSTRATION_TARGETS` airline name | low | mitigate | Both new names are plain ASCII and slug to `km-malta-airlines` / `tuifly-belgium`. No new code path is introduced — the existing `_UNSAFE_KEY_RE` boundary in `illustration_path_for_key()` and `test_illustrations.py`'s pre-existing `^[a-z0-9-]+\.png$` assertion over the whole of `target_filenames()` both cover the two new entries automatically. Task 1's verify runs that harness. |
| T-jz6-02 | Spoofing | `enrich.airline_from_callsign()` — hostile callsign reaching the two new prefix rows | low | mitigate | No change to the lookup function, its `_AIRLINE_PREFIX_SHAPE_RE` gate, or `normalise_callsign()`. The function still can only ever return a fixed table value or `None`, never anything derived from its argument (T-hyy-01, preserved). `test_enrich.py` check 25 (unchanged) asserts both new keys are exactly 3 uppercase A–Z characters. |
| T-jz6-03 | Repudiation | Brand misattribution on the panel — TUIfly Belgium rendered as its legacy brand on an adsbdb hit, and as its current brand on the fallback path, for the same real aircraft | low | **accept** | Explicit developer decision (QT-jz6-D-02), made with the tradeoff stated. The divergence is cosmetic (a caption string and which of two correct-shape illustrations is shown), never a wrong-carrier claim: the adsbdb-hit path renders adsbdb's own answer, and the fallback path renders the prefix table's. Accepted rather than mitigated; recorded in `enrich.py`, `illustrations.py`, `HANDOFF.md`, and `VENDOR.md` so it cannot be mistaken for a defect. Task 1's check 44 prevents it being silently "corrected". |
| T-jz6-04 | Spoofing | A generated PNG misrepresenting a carrier's identity (wrong predecessor livery — Air Malta or Jetairfly art landing under the new filenames) | medium | mitigate | Task 2's prompt sections name the current livery explicitly and name the superseded scheme as the thing **not** to produce, for both files. HANDOFF.md's existing "After generating the files" step 4 already requires the developer to confirm type-matches-filename by eye before the file is recorded in `VENDOR.md`. |
| T-jz6-SC | Tampering | npm/pip/cargo installs | n/a | **accept** | No package-manager install is in scope for this task — no dependency is added, removed, or upgraded. `server/requirements.txt` and `server/requirements-dev.txt` are not touched, so the Package Legitimacy Gate does not apply and no blocking human checkpoint is required. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 server/test_enrich.py` → `enrich: 27/27 checks pass`.
2. `server/.venv/bin/python3 server/test_illustrations.py` → `illustrations: 44/44 checks pass`.
3. `scripts/run-all-tests.sh` → all 9 harnesses pass, coverage ≥ 75.
4. `server/.venv/bin/ruff check .` → clean.
5. `scripts/check-attribution.sh` → PASS (unchanged asset-file count; the two
   named-but-undelivered PNGs are correctly not counted, because the script
   walks disk files only).
6. `server/plane/illustrations.py --validate` → exits 0, reports
   `3 outstanding target file(s)`.
7. `server/plane/illustrations.py --targets | wc -l` → 36.
8. HANDOFF.md prompt headings 1–36, in `--targets` order (diff-verified).
9. `git diff --stat` touches exactly the six files in `files_modified` — no
   change to `render.py`, `poll_loop.py`, `detect.py`, or any `.png`.
</verification>

<success_criteria>
- KM Malta Airlines (`KMM`) and TUIfly Belgium (`JAF`) both resolve through
  `enrich.airline_from_callsign()` with zero network call, proven against the
  two real callsigns live-curled this session.
- Both are outstanding illustration targets reported by
  `illustrations.py --outstanding`, alongside the pre-existing
  `royal-air-maroc-embraer.png` — three files total.
- `HANDOFF.md` specifies both new files at the same detail level as the existing
  34: filename, exact aircraft type/variant, and a livery description sufficient
  to drive an AI image-generation prompt unaided.
- `HANDOFF.md`'s Naming rules section documents QT-jz6-D-02 as a named,
  developer-approved override with its accepted consequence spelled out — not as
  an inconsistency awaiting a fix.
- Every stale "KM Malta remains excluded" claim is gone from both documents.
- `VENDOR.md` records both new files by name as outstanding, with no invented
  digest or dimension data.
- Full test suite, ruff, `check-attribution.sh`, and `--validate` are all green.
- No PNG artwork was generated, faked, or placed; `render.py` and `poll_loop.py`
  are untouched.
</success_criteria>

<output>
Create `.planning/quick/260827-jz6-add-km-malta-airlines-and-tuifly-belgium/260827-jz6-SUMMARY.md` when done.
</output>