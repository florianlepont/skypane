---
phase: quick-260902-v26
plan: 03
type: execute
wave: 3
depends_on: ["260902-v26-02"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/static/style.css
  - companion/test_status_pages.py
  - companion/test_view_pages.py
autonomous: true
requirements: [D-01, D-03, D-04]
must_haves:
  truths:
    - "Every Airlines card carries a replace-image control that posts to that card's own illustration route"
    - "The control works with JavaScript disabled — it is a plain form, no script involvement"
    - "After a successful upload the card shows the new image immediately, not a stale cached one"
    - "No revert-to-original control is rendered anywhere (D-04, explicitly out of scope)"
    - "airlines_page.render() still works when ctx carries no state_dir"
  artifacts:
    - "companion/pages/airlines_page.py: the replace disclosure + form markup, and the override-aware image URL"
    - "companion/static/style.css: the .airline-card__replace rules"
    - "companion/test_status_pages.py + companion/test_view_pages.py: markup and escaping checks"
  key_links:
    - "form action == the same /illustration/{key}.png URL the card's <img> and zoom trigger already point at, minus the cache-busting query"
    - "ctx['state_dir'] -> override mtime -> the ?v= cache buster on the <img> src and the zoom trigger's data-view-panel-src"
---

<objective>
Give the Airlines gallery a per-card "replace this image" control, wired to plan
02's `POST /illustration/{key}.png` route, styled to this project's shipped design
system, and make a freshly uploaded replacement visible immediately rather than
after the route's 5-minute private cache window expires.

Purpose: this is the only part of the feature the developer actually touches. It
must fit the page's existing card/disclosure/button patterns rather than
introducing a new one.

Output: markup in `airlines_page.py`, rules in `style.css`, harness checks, and a
real-browser sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260902-v26-add-the-ability-to-replace-an-airline-s-/260902-v26-CONTEXT.md
@.planning/quick/260902-v26-add-the-ability-to-replace-an-airline-s-/260902-v26-02-SUMMARY.md
@companion/pages/airlines_page.py
@companion/test_status_pages.py

Load the `sketch-findings-skypane` skill before writing any CSS — it is this
project's live design-system reference and is auto-loaded for UI work.

Verified ground truth (re-checked live against the working tree, 2026-09-02):

- `_airline_card_html(index, airline_name, shapes)` (airlines_page.py:139) builds
  `image_url = "%s%s.png" % (ILLUSTRATION_ROUTE_PREFIX, escape_html(key))` **once**
  and interpolates it into both the `<img src>` and the zoom button's
  `data-view-panel-src`, with a comment saying the two must never drift apart.
  Preserve that single-construction property.
- `render(ctx)` (airlines_page.py:302) currently ignores `ctx` entirely, and its
  docstring says so. **`companion/test_view_pages.py:1365` calls
  `airlines_page.render({})` with a literal empty dict.** Any new ctx read must
  therefore be `ctx.get("state_dir")` with a `None`-tolerant path, or that harness
  breaks.
- `companion/app.py`'s `page_context()` does provide `"state_dir"` (app.py:521),
  so the real render path has it.
- `Handler.send_bytes(..., cache_seconds=300)` on the illustration route emits
  `Cache-Control: private, max-age=300`. Without a cache-busting change to the
  URL, a browser can show the pre-upload image for up to five minutes after a
  successful replace.
- `do_GET` parses `urlsplit(self.path).path`, so a `?v=` query string on the
  image URL is already ignored by the route — no route change is needed to
  support a cache buster.
- `companion/app.py` already has the precedent for a page-render-time filesystem
  probe: `runway_images_available()` stats a fixed registry of ids per request and
  feeds `ctx["runway_images"]`.
- Design system facts that constrain the CSS (from `sketch-findings-skypane`):
  buttons are 30px tall and 13px; `button[type="submit"]` is the accent-filled
  primary and is already on the stylesheet's accent-reservation list, so a submit
  button here introduces no new accent use; base `button` is the near-invisible
  quiet variant; `summary` **keeps** the 44px touch-target floor and native
  `input` does too; `<details>` disclosures are an established 06.6.3 pattern;
  cards use `--radius-control` (8px), never `--radius-card`; `.airline-card` is
  hairline-at-rest with `--shadow-card-hover` on `:hover`/`:focus-within`; the
  sub-scale label tier (11-13px) exists and `.airline-card__chip` already lives in
  it. Do not introduce a fifth type role and do not add a new accent use.
- `.airline-card`, `.airline-card__image`, `.airline-card__zoom`,
  `.airline-card__name`, `.airline-card__chips`, `.airline-card__chip` rules live
  at style.css:2883-3000.
- `escape_html()` is applied exactly once at each point of interpolation
  (T-06.6.4.1-05). Keep that discipline — every new interpolated value goes
  through it, and nothing goes through it twice.
- D-04 is a **negative** requirement: no revert-to-original UI is in scope. The
  vendored original stays on disk and is recoverable by deleting the override
  file, but nothing user-facing may offer that.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The per-card replace control and the override-aware image URL</name>
  <files>companion/pages/airlines_page.py</files>
  <behavior>
    - Each rendered card contains one form whose action is that card's /illustration/{key}.png URL, method post, enctype multipart/form-data.
    - The form contains one file input with a per-card unique id, an associated label, and one submit button.
    - render({}) with no state_dir produces the same image URLs it produces today, with no query string.
    - render(ctx) with a state_dir whose override directory holds {key}.png appends a cache-busting query to that card's image URL and to its zoom trigger's data-view-panel-src, identically.
    - A hostile airline name still produces escaped output at every new interpolation point.
    - No revert/reset control is rendered.
  </behavior>
  <action>
Part A — the override-aware image URL.

Add a small module-level helper that takes the airline key and the optional
state_dir and returns the cache-busting suffix for that key's image URL: an empty
string when there is no state_dir or no override file, otherwise a query carrying
the override file's integer mtime. Resolve the override path through
`illustrations.override_path_for_key(key, state_dir)` — do not rebuild the path
here, and do not reach for `ILLUSTRATION_OVERRIDE_DIRNAME` directly; plan 01's
function is the one place that join lives. Wrap the `os.stat` in a try/except so a
vanished or unreadable file degrades to an empty suffix rather than raising, the
same never-raises posture `runway_images_available()` documents.

Explain in its docstring why the buster is needed at all: the illustration route
serves `Cache-Control: private, max-age=300`, so without a URL change a
freshly-replaced image stays stale in the developer's browser for up to five
minutes after the upload they just performed — which reads as "the upload didn't
work". Keying the buster on the override's mtime means the URL changes exactly
when the bytes change, and never otherwise.

Thread `state_dir` from `render(ctx)` down through `_gallery_grid_html()` into
`_airline_card_html()` as an explicit parameter with a `None` default. In
`render()`, read it as `ctx.get("state_dir")`, **not** `ctx["state_dir"]` — and
put a comment on that line naming the reason: `companion/test_view_pages.py` calls
`render({})` with an empty dict, and this page's own docstring has always
advertised that it reads nothing from ctx. Update that docstring: it now reads
exactly one optional key.

In `_airline_card_html()`, keep building `image_url` once. Append the suffix to
produce the URL used by both the `<img src>` and the zoom trigger's
`data-view-panel-src`, preserving the existing "these two can never drift apart"
property. The form's `action` must use the **un-busted** URL — a query string on a
POST target is pointless and would make the action and the src look gratuitously
different; add a comment saying so.

Part B — the replace control markup.

Inside the card, after the chips block, render a `<details>` disclosure whose
`<summary>` is the replace affordance and whose content is the upload form.
Use `<details>` rather than an always-visible form because this page renders 27
cards and 27 permanently-open file pickers would dominate a gallery whose whole
purpose is looking at pictures; `<details>` is already this codebase's established
06.6.3 disclosure pattern and needs no JavaScript.

The form: `method="post"`, `enctype="multipart/form-data"`,
`action="{un-busted image_url}"`. Inside it, one `<label>` bound by `for` to one
`<input type="file">` carrying `name="image"`, `accept="image/png"`, `required`,
and an id derived from the airline key so it is unique across the 27 cards. Then
one `<button type="submit">`.

State in a comment that `accept` is a browser-side file-picker hint only and is
never trusted server-side — plan 02's route decides what an image is by parsing
the real PNG header, and this attribute exists purely to save the developer
scrolling past their photo library.

Copy: the summary text, the label text, and the button text each get their own
module-level constant beside the existing `ZOOM_LABEL_TEMPLATE` /
`CARD_IMAGE_ALT_TEMPLATE` constants, so the harness can assert against the
constant rather than a duplicated literal. The summary must name the airline in its
accessible text the way `ZOOM_LABEL_TEMPLATE` does, so a screen-reader user moving
between 27 identical disclosures can tell them apart.

Every interpolated value — the key inside both URLs, the airline name inside the
summary and label text, the input id — goes through `escape_html()` exactly once,
at the point of interpolation.

Render **no** revert, reset, or restore-original control (D-04). Add a short
comment recording that this is a deliberate scope decision, not an omission: the
vendored original is never modified by this feature and stays recoverable, but no
user-facing revert is in scope for this task.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>Both harnesses exit 0 at their current counts (44 / 116) before any new checks are added in Task 3 — proving the markup addition and the `ctx.get()` read broke nothing. `airlines_page.render({})` returns without raising.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Style the replace control to the shipped design system</name>
  <files>companion/static/style.css</files>
  <behavior>
    - The collapsed disclosure reads as a quiet secondary affordance, not competing with the illustration.
    - The summary keeps a 44px-or-greater touch target.
    - The open form's controls stack and stay usable at a 375px viewport.
    - No new accent use is introduced beyond the already-listed type="submit" primary.
  </behavior>
  <action>
Add rules for the new elements immediately after the existing
`.airline-card__chip` block in `companion/static/style.css`, so the Airlines
gallery's rules stay contiguous.

Load `sketch-findings-skypane` first and follow it. Concretely:

- Give the disclosure a top margin from the spacing scale so it separates from the
  chips row without inventing a new gap value.
- The summary sits in the existing sub-scale label tier alongside
  `.airline-card__chip` — a quiet, muted treatment using a `color-mix` against
  `--color-text` rather than a new colour token. It must not be accent-coloured:
  the stylesheet header's accent-reservation list is exhaustive and this control is
  not on it. Keep the global 44px touch floor that `summary` already has; if any
  new padding would fight it, state in a comment that the floor wins.
- Style the open state's form as a simple vertical stack with a spacing-scale gap.
  The native file input keeps its global 44px floor — do not override it. The
  submit button inherits the global 30px primary treatment; do not restyle it.
- Constrain the file input so a long filename cannot force the card wider than its
  grid column (`.illustration-grid` is `repeat(auto-fill, minmax(200px, 1fr))`, so
  a 200px column is a real case) — a `max-width: 100%` plus a `min-width: 0` on the
  stack is the usual fix; verify at 375px.
- Do not add a resting box-shadow anywhere here. `.airline-card`'s 06.6.4 D-03
  hairline-at-rest contract governs this card and the existing rule block says so
  verbatim.

Write the block's comment the way this file's neighbours are written: say what the
rule does and why the obvious alternative was rejected, and reference quick task
260902-v26.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 && server/.venv/bin/python3 companion/test_contrast_check.py && server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <done>Both harnesses exit 0 (contrast check at 36; companion app at plan 02's count) — the contrast harness proving no colour-separation or accent-reservation rule was violated, and the app harness proving the stylesheet still serves and still satisfies its DOM/CSS contract guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harness checks, full-suite pass, and browser sign-off</name>
  <files>companion/test_status_pages.py, companion/test_view_pages.py</files>
  <behavior>
    - Every rendered card carries exactly one replace form pointing at that card's own key.
    - The form's method, enctype and action are asserted, not assumed.
    - Each file input's id is unique across the page and is referenced by a label's for attribute.
    - A hostile airline name cannot escape any new interpolation point.
    - The cache buster is absent with no state_dir and present, keyed on mtime, when an override exists.
    - No revert/reset control appears anywhere in the rendered page.
  </behavior>
  <action>
Add checks to `companion/test_status_pages.py` in its Section 2
(`companion/pages/airlines_page.py`) block, following the file's existing
`check(...)` convention:

1. The rendered page contains exactly as many replace forms as
   `illustrations.target_airline_names()` has entries, and each form's `action`
   equals `ILLUSTRATION_ROUTE_PREFIX + key + ".png"` for a real key — assert every
   action's filename is a member of `illustrations.target_filenames()`, mirroring
   the existing check at test_status_pages.py:4420 that proves every rendered URL
   passes the route's own membership test. Reuse that check's shape.
2. Each form declares `method="post"` and `enctype="multipart/form-data"`; a form
   missing the enctype would silently send the file as a filename string, so this
   is a real failure mode, not a formality.
3. Every file input id is unique across the whole rendered page, and each is the
   target of a `for` attribute on a label.
4. Cache buster: `render({})` produces image URLs with no query string; a
   `render({"state_dir": tmp})` where `{tmp}/illustration_overrides/{key}.png`
   exists produces exactly one busted URL for that key, the buster matches the
   file's mtime, and every other card's URL is unbusted. Assert the busted URL
   appears in **both** the `<img src>` and the zoom trigger's
   `data-view-panel-src`, pinning the existing "these must never drift apart"
   property across this change. Create and clean up the tmp dir with
   `tempfile.mkdtemp` + try/finally, as this file's other filesystem checks do.
5. Escaping: extend the page's existing hostile-value discipline to the new
   interpolation points — render with a hostile airline name and assert no raw
   angle bracket or raw quote from the input survives into the summary text, the
   label text, or the input id.
6. Absence of a revert control (D-04): assert the rendered page contains no
   control whose accessible text offers restoring or resetting the original image.
   Write this as a membership test over the specific copy constants this feature
   introduces plus a small list of revert-shaped words, not as a bare negative grep
   over the whole document — a bare negative grep over rendered HTML is brittle
   against unrelated future copy.

Add one cross-module check to `companion/test_view_pages.py`: `render({})` still
succeeds with a literal empty dict and its output still contains the gallery grid —
pinning the `ctx.get()` tolerance that this harness's own line 1365 depends on.

Raise each file's `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)`
calls added there, extending each file's running comment in its established
"N = previous + k (reason)" style and naming quick task 260902-v26.

Finally run the whole suite once through `scripts/run-all-tests.sh`, which is this
project's single entry point and additionally enforces the coverage threshold from
`pyproject.toml`. All 16 harnesses must pass and the coverage gate must hold.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/skypane/.claude/worktrees/airplanes-api-sustainability-a4b703 && scripts/run-all-tests.sh && git status --porcelain server/assets/icons/illustrations/ | wc -l</automated>
    <human-check>
Real-browser sign-off (this project's own lesson: computed-style checks alone have
already missed a real mobile nav bug, so a live look is budgeted deliberately).

1. Start the companion locally against a scratch state dir:
   `SKYPANE_COMPANION_PASSWORD=... server/.venv/bin/python3 companion/app.py --port 8643 --state-dir /tmp/skypane-v26-state`
   (use the real env var name from `companion/auth.py`).
2. Sign in and open `http://127.0.0.1:8643/airlines`.
3. On the Air France card, open the replace disclosure, choose a transparent PNG
   at least 1200px wide and landscape, and submit.
4. Confirm: the success flash appears; the card's picture is the new image
   immediately, with no manual reload or cache clear; the enlarge lightbox opens
   the new image too.
5. Try a non-image file (any small text file renamed to .png) — confirm the
   rejection flash explains what is required and the card's picture is unchanged.
6. Check the page on a phone-width viewport (375px): the disclosure, file input
   and button all fit inside the card without overflowing the grid column.
7. Confirm `server/assets/icons/illustrations/air-france.png` is still the original
   file on disk, and that the replacement landed at
   `/tmp/skypane-v26-state/illustration_overrides/air-france.png`.

Report anything that looks wrong rather than approving around it.
    </human-check>
  </verify>
  <done>`scripts/run-all-tests.sh` prints `==> Result: PASS` with all 16 harnesses green and the coverage gate satisfied. `git status --porcelain server/assets/icons/illustrations/ | wc -l` prints 0. The browser sign-off above is recorded in the SUMMARY with the developer's response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| airline_name / airline key -> rendered HTML | Curated in-repo data, but interpolated into new attributes (a form action, an input id, a label `for`) that did not exist before. |
| filesystem override state -> rendered URL | An override file's mtime is read at render time and interpolated into a URL. |
| browser form -> POST route | The form is the client-side half of plan 02's upload boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v26-03-01 | Tampering | new HTML interpolation points | medium | mitigate | Every new interpolated value goes through `escape_html()` exactly once at the point of interpolation, per T-06.6.4.1-05. Task 3 check 5 asserts it against a hostile airline name for the summary text, the label text and the input id. |
| T-v26-03-02 | Tampering | form `action` attribute | high | mitigate | The action is the same server-built `ILLUSTRATION_ROUTE_PREFIX + key + ".png"` string the card's `<img src>` already uses, with the key coming from `normalise_airline_key()` over a curated in-repo list. Task 3 check 1 asserts every rendered action's filename is a member of `illustrations.target_filenames()` — the route's own membership set. |
| T-v26-03-03 | Information Disclosure | cache-buster value | low | accept | The buster exposes an override file's mtime to an already-authenticated session that just performed the upload. It reveals nothing the developer does not already know, and the route is session-gated. |
| T-v26-03-04 | Denial of Service | per-render filesystem probes | low | accept | Adds up to 27 `os.stat` calls per Airlines render. `runway_images_available()` already establishes this pattern per request, and the page is one authenticated household user's occasional page view. |
| T-v26-03-05 | Spoofing | the `accept="image/png"` attribute | low | mitigate | Documented in-code as a browser-side picker hint only. The server never reads it; plan 02's route decides what an image is from the real PNG header. |
| T-v26-03-06 | Elevation of Privilege | control visible to an unauthenticated viewer | high | mitigate | `/airlines` is behind `do_GET`'s `require_session()` gate (app.py:891), so the form is never rendered to an unauthenticated caller, and plan 02's POST route re-checks the session independently regardless. |
| T-v26-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. No build step exists for `style.css` — it is hand-written with zero external references, per its own header comment. Nothing to audit. |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` — `==> Result: PASS`, all 16 harnesses, coverage gate held.
- `git status --porcelain server/assets/icons/illustrations/` — empty.
- The `<human-check>` browser sign-off above, with the developer's response recorded.
</verification>

<success_criteria>
- Every Airlines card offers a replace control that posts to its own illustration
  route, works with JavaScript disabled, and fits the shipped design system.
- A replaced image appears immediately in the browser, with no stale-cache window.
- `airlines_page.render({})` still works with an empty ctx.
- No revert-to-original control exists anywhere (D-04).
- The full 16-harness suite and the coverage gate pass.
</success_criteria>

<output>
Create `.planning/quick/260902-v26-add-the-ability-to-replace-an-airline-s-/260902-v26-03-SUMMARY.md` when done.
</output>
