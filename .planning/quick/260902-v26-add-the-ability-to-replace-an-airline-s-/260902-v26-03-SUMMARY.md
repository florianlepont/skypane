---
phase: quick-260902-v26
plan: 03
subsystem: ui
tags: [companion-app, html-forms, multipart-upload, css, cache-busting, escaping]

requires:
  - phase: 260902-v26-01
    provides: "server/plane/illustrations.py override-resolution API (override_path_for_key(), override_dir_for_state_dir(), resolved_illustration_path())"
  - phase: 260902-v26-02
    provides: "companion/app.py POST /illustration/{key}.png (Handler._handle_illustration_replace()) and the three FLASH_ILLUSTRATION_* keys on companion/pages/airlines_page.py"
provides:
  - "companion/pages/airlines_page.py: _illustration_cache_buster() and _replace_control_html() — the per-card <details> upload-form disclosure and the override-mtime cache-busting query appended to both the <img src> and the zoom trigger's data-view-panel-src"
  - "companion/pages/airlines_page.py: render(ctx) now reads ctx.get('state_dir') (still tolerant of a literal empty dict), threaded through _gallery_grid_html()/_airline_card_html()"
  - "companion/static/style.css: .airline-card__replace / .airline-card__replace-form rules — a quiet, non-accent disclosure styled to the shipped design system"
affects: []

tech-stack:
  added: []
  patterns:
    - "Cache-busting query keyed on an override file's own os.stat() mtime, never a random token or timestamp-at-render-time — the URL only changes when the underlying bytes actually change"
    - "A form's action uses the un-busted image URL (a query string on a POST target is pointless); only the <img src> and the zoom trigger's data-view-panel-src carry the buster, and they must always carry the identical value"
    - "Scoped-negative-assertion pattern for a non-goal (D-04): the no-revert-control check greps within the feature's own <details> markup slice and against its own copy constants, never the whole rendered document, to avoid brittleness against unrelated future copy"

key-files:
  created: []
  modified:
    - companion/pages/airlines_page.py
    - companion/static/style.css
    - companion/test_status_pages.py
    - companion/test_view_pages.py

key-decisions:
  - "The replace disclosure's copy (REPLACE_SUMMARY_TEMPLATE/REPLACE_LABEL_TEMPLATE/REPLACE_BUTTON_TEXT) are module-level constants, matching this module's existing ZOOM_LABEL_TEMPLATE/CARD_IMAGE_ALT_TEMPLATE convention, so the harness asserts against the constant rather than a duplicated literal"
  - "The global `summary` rule's accent colour (this file's own documented accent-reservation use) is overridden to a neutral color-mix tone scoped to `.airline-card__replace summary` and its two marker pseudo-elements — the accent-reservation list stays exhaustive and unchanged, per the plan's explicit instruction not to add a new accent use"
  - "No revert/reset/restore-original control anywhere (D-04) — enforced by construction (no such markup was written) and pinned by a harness check scoped to each card's own replace-disclosure markup plus this feature's own copy constants, not a whole-document grep"

requirements-completed: [D-01, D-03, D-04]

coverage:
  - id: D1
    description: "Every Airlines card carries a replace-image control (a <details>/<summary> disclosure, JS-free) whose form posts to that card's own /illustration/{key}.png route with method=post and enctype=multipart/form-data, and a real upload through it changes what the GET route serves"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py#_replace_form_action_matches_route_membership, _replace_form_declares_post_and_multipart_enctype, _replace_form_file_input_ids_are_unique_and_labelled"
        status: pass
      - kind: e2e
        ref: "live curl multipart POST (identical field name/enctype/method the rendered form itself declares) against a real running companion/app.py subprocess on 127.0.0.1:8643 — a valid PNG upload to /illustration/air-france.png redirected 303 to /airlines?flash=illustration_replaced, and the override file at {state_dir}/illustration_overrides/air-france.png was confirmed byte-identical to the uploaded file"
        status: pass
    human_judgment: false
  - id: D2
    description: "A freshly-replaced image is visible immediately (no stale 5-minute private-cache window) — the cache-busting query is absent with no override and present, keyed on the override's own mtime, identically on both the <img src> and the zoom trigger's data-view-panel-src"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py#_cache_buster_absent_with_no_state_dir_and_keyed_on_mtime_with_an_override"
        status: pass
      - kind: e2e
        ref: "live GET /airlines?flash=illustration_replaced against the real subprocess after the upload above: the Air France card's <img src> and data-view-panel-src both carried /illustration/air-france.png?v=1788384138 (the override file's real mtime), and every sibling card's URL stayed unbusted"
        status: pass
    human_judgment: false
  - id: D3
    description: "A rejected upload (non-image bytes) leaves the card showing the previously-accepted image, unchanged, with a rejection flash explaining what is required"
    requirement: "D-01"
    verification:
      - kind: e2e
        ref: "live curl multipart POST of a plain-text file renamed .png to /illustration/air-france.png against the real subprocess: redirected to /airlines?flash=illustration_rejected, rendered flash text 'Couldn't use that image — upload a transparent PNG that's at least 1200 pixels wide and landscape (wider than tall).', and the override file's md5 was confirmed unchanged (still the prior successful upload)"
        status: pass
    human_judgment: false
  - id: D4
    description: "A hostile airline name cannot escape any new interpolation point (the summary text, the label text, or the input id)"
    requirement: "D-03"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py#_replace_control_escapes_hostile_airline_name"
        status: pass
    human_judgment: false
  - id: D5
    description: "airlines_page.render({}) with a literal empty dict still works and still contains the gallery grid — the ctx.get('state_dir') tolerance test_view_pages.py's own call site depends on"
    requirement: "D-03"
    verification:
      - kind: unit
        ref: "companion/test_view_pages.py#_airlines_render_empty_ctx_still_contains_gallery_grid"
        status: pass
    human_judgment: false
  - id: D6
    description: "No revert-to-original control is rendered anywhere (D-04, explicitly out of scope)"
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py#_replace_disclosure_contains_no_revert_or_reset_control"
        status: pass
    human_judgment: false
  - id: D7
    description: "The replace control fits the shipped design system: quiet/non-accent disclosure, 44px+ summary touch target, no resting shadow on .airline-card, and the open form's controls stack and stay usable at a 375px viewport without overflowing the card's grid column"
    verification:
      - kind: automated_ui
        ref: "companion/test_contrast_check.py (36/36 — colour-separation/accent-reservation contract unbroken), companion/test_companion_app.py (125/125 — stylesheet DOM/CSS contract guards)"
        status: pass
      - kind: e2e
        ref: "Real browser (orchestrating session, Claude_Browser pane) at a 375x812 viewport against a real running companion/app.py: opened the Air France card's replace disclosure by a real .click() on <summary>, measured every control's (details/form/file input/submit button) bounding rect against the card's own — zero overflow in either direction. Screenshot confirms the disclosure reads quiet (muted grey text, no accent wash) and the sibling card's collapsed summary is equally unobtrusive."
        status: pass
    human_judgment: false
  - id: D8
    description: "Real-browser, real-form sign-off: opening the disclosure by click, submitting a real file through the real <input type=file>, and seeing the lightbox open the new image, all in an actual browser tab"
    verification:
      - kind: e2e
        ref: "Real browser (orchestrating session, Claude_Browser pane) against a real running companion/app.py: opened the disclosure with a real click on <summary> (details.open flipped false->true); assigned a real File to the file input via the standard DataTransfer technique (the only spec-compliant way to place a File on an <input type=file> without an OS file-picker dialog, since browsers block scripted .value on file inputs) and dispatched a real change event; clicked the actual <button type=submit>, producing a genuine browser-native multipart form submission (not fetch/XHR) that navigated to /airlines?flash=illustration_replaced. Confirmed: the flash banner rendered the exact success copy; the card's <img src> and the zoom trigger's data-view-panel-src both updated to the new mtime-keyed ?v= query; clicking the zoom trigger opened the lightbox showing the SAME versioned URL, and a screenshot confirms it visually renders the just-uploaded replacement image (a synthetic blue rectangle), not the original Air France aircraft. Then repeated the same real-click/real-file/real-submit sequence with a non-image file renamed .png: redirected to ?flash=illustration_rejected with the exact user-facing copy, and the card's image URL was confirmed unchanged (still the successful replacement, not reverted and not further corrupted). Override file verified on disk at {state_dir}/illustration_overrides/air-france.png; server/assets/icons/illustrations/ confirmed untouched via git status --porcelain."
        status: pass
    human_judgment: false

duration: ~50min
completed: 2026-09-02
status: complete
---

# Quick Task 260902-v26 Plan 03: Airlines Replace-Image UI Summary

**A per-card `<details>`/`<summary>` upload disclosure on the Airlines gallery, wired to plan 02's `POST /illustration/{key}.png` route with a mtime-keyed cache-busting query so a fresh upload appears immediately — code complete and live-verified end-to-end, including the real-browser click-through pass (performed by the orchestrating session after this executor's environment lacked browser tooling).**

## Performance

- **Duration:** ~50 min (executor) + a real-browser follow-up pass by the orchestrating session
- **Completed:** 2026-09-02
- **Tasks:** 3/3 code-complete; Task 3's `<human-check>` step also complete (see below)
- **Files modified:** 4

## Accomplishments
- `_illustration_cache_buster(key, state_dir)` (companion/pages/airlines_page.py): resolves an override's own `os.stat().st_mtime` through plan 01's `illustrations.override_path_for_key()` (never rebuilding that join locally), returning `""` for no state_dir/no override/a vanished file (never raises). Appended to both the `<img src>` and the zoom trigger's `data-view-panel-src` — never to the form's `action`, which stays the un-busted URL.
- `_replace_control_html(key, airline_name, action_url)`: a `<details class="airline-card__replace">` disclosure per card, containing a plain `method="post" enctype="multipart/form-data"` form with one file input (`accept="image/png"`, a browser-hint only, never trusted server-side), one label bound by `for`, and one submit button. Renders no revert/reset control (D-04). Every interpolated value (summary text, label text, input id) goes through `escape_html()` once.
- `render(ctx)` now reads `ctx.get("state_dir")` — never `ctx["state_dir"]` — threaded through `_gallery_grid_html()`/`_airline_card_html()`; `render({})` still works and still renders the full gallery, pinned by a cross-module check in `test_view_pages.py`.
- `companion/static/style.css`: `.airline-card__replace` (top-margin spacing only), a scoped override of the global accent-coloured `summary`/`::marker`/`::-webkit-details-marker` rule to a neutral `color-mix` tone at the existing 13px sub-scale label tier (this control is not on the file's own accent-reservation list), and `.airline-card__replace-form` (a `min-width: 0` vertical stack with a `max-width: 100%` file input so a long filename can't force the card wider than its 200px grid column).
- 6 new checks in `companion/test_status_pages.py` (116 → 122) and 1 cross-module check in `companion/test_view_pages.py` (44 → 45), covering: replace-form action membership against `illustrations.target_filenames()`, method/enctype declaration, unique labelled file-input ids, the cache buster's absent/present-and-mtime-keyed contract identically on both URLs, hostile-airline-name escaping (via a monkeypatched `target_variants_by_airline()`, following `test_config_page.py`'s own monkeypatch-and-restore technique), and D-04's no-revert-control guarantee (scoped to each disclosure's own markup plus this feature's copy constants, not a whole-document grep).
- Full `scripts/run-all-tests.sh` run: all 16 harnesses green, coverage gate held (92% total). `git status --porcelain server/assets/icons/illustrations/` confirms the vendored illustration tree was never touched.
- **Live HTTP end-to-end verification** against a real running `companion/app.py` subprocess (port 8643, a scratch `--state-dir`): logged in via `POST /login`; confirmed the rendered `/airlines` page carries the expected `<details class="airline-card__replace">` markup for every card (spot-checked Air France's exact HTML); POSTed a real 1400×400 semi-transparent PNG as `multipart/form-data` (field name `image`, matching the rendered form exactly) to `/illustration/air-france.png` — got the `illustration_replaced` flash, the override file landed at `{state_dir}/illustration_overrides/air-france.png` byte-identical to the upload (lossless re-encode, matching plan 02's own documented finding), the vendored `server/assets/icons/illustrations/air-france.png` stayed byte-identical (confirmed via `git status --porcelain`), and the re-fetched `/airlines` page showed Air France's `<img src>` and its zoom trigger's `data-view-panel-src` both carrying the identical `?v={override mtime}` cache-busting query while every sibling card's URL stayed unbusted; then POSTed a renamed `.txt` file to the same route — got the `illustration_rejected` flash with the exact user-facing copy plan 02 shipped, and confirmed the override file's md5 was unchanged (the rejected upload never touched disk).

## Task Commits

1. **Task 1: The per-card replace control and the override-aware image URL** - `7390fa4` (feat)
2. **Task 2: Style the replace control to the shipped design system** - `c840f74` (feat)
3. **Task 3: Harness checks and full-suite pass** - `cec88cd` (test)

_Note: all three tasks were `tdd="true"` per the plan; each task's own verify step was harness-execution against the existing/extended `check()` suite (this module's established TDD discipline, not pytest), so behavior was implemented directly and proven green in the same commit, matching plan 02's own precedent._

## Files Created/Modified
- `companion/pages/airlines_page.py` - `_illustration_cache_buster()`, `_replace_control_html()`, `REPLACE_SUMMARY_TEMPLATE`/`REPLACE_LABEL_TEMPLATE`/`REPLACE_BUTTON_TEXT`, `render(ctx)`/`_gallery_grid_html()`/`_airline_card_html()` all threading an optional `state_dir`
- `companion/static/style.css` - `.airline-card__replace`, `.airline-card__replace summary`/`::marker`/`::-webkit-details-marker`, `.airline-card__replace-form`, `.airline-card__replace-form input[type="file"]`
- `companion/test_status_pages.py` - 6 new checks (`EXPECTED_CHECK_COUNT` 116 → 122)
- `companion/test_view_pages.py` - 1 new cross-module check (`EXPECTED_CHECK_COUNT` 44 → 45)

## Decisions Made
- Kept the replace-form's copy plain and generic ("New image for {airline}" / "Upload") rather than restating the server's own validation requirements in the label — the rejection flash (plan 02's own copy) already explains exactly what's required when an upload fails, so the form itself doesn't need to pre-empt it.
- Scoped the D-04 no-revert-control harness check to each card's own `<details class="airline-card__replace">` markup slice plus this feature's own copy constants, rather than a whole-document negative grep — the plan explicitly called out that a bare whole-document grep would be brittle against unrelated future copy (e.g. a filter "Clear"/"Reset" control elsewhere on the page).
- Verified the live upload's re-encode is lossless for the synthetic test PNG (md5-identical upload vs. override file) rather than assuming it — matches plan 02's own documented finding for its own live-verification pass.

## Deviations from Plan

None — every behavior bullet, artifact name, and verification command in the plan was implemented and passed as specified. Task 3's `<automated>` verify step and its `<human-check>` step are treated separately below (see "Next Phase Readiness" / the open item).

## Issues Encountered
- A leftover `companion/app.py` process from an earlier manual session was still running on port 8645 with a different `--state-dir` (`/tmp/skypane-prod-state-tli`) — unrelated to this plan, not touched, and did not interfere with this plan's own instance on port 8643. This session's own instance (pid 67739) was stopped cleanly after verification.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness — real-browser checkpoint closed by the orchestrating session

This executor's environment had no browser-automation tooling bound (the `chrome-devtools-mcp:chrome-devtools` skill returned only usage documentation, not a live tool binding). The orchestrating session, which does have browser tooling, performed the plan's Task 3 `<human-check>` pass directly afterward, against a fresh `companion/app.py` instance:

- Opened the Air France card's replace `<details>` with a real `.click()` on `<summary>` (`details.open` flipped `false` -> `true`).
- Assigned a real `File` (a synthetic 1400x400 landscape PNG with partial transparency) to the file input via the standard `DataTransfer` technique — the spec-compliant way to place a file on an `<input type=file>` without an OS-level file-picker dialog, since browsers block scripted `.value` on file inputs — and dispatched a real `change` event.
- Clicked the actual `<button type=submit>`, producing a genuine browser-native `multipart/form-data` submission (not `fetch`/XHR) that navigated to `/airlines?flash=illustration_replaced`.
- Confirmed the success flash rendered, the card's `<img src>` and the zoom trigger's `data-view-panel-src` both updated to the new mtime-keyed `?v=` query, and clicking the zoom trigger opened the lightbox showing that same versioned URL — a screenshot confirms it visually renders the just-uploaded replacement image, not the original aircraft art.
- Repeated the same real-click/real-file/real-submit sequence with a non-image file renamed `.png`: redirected to `?flash=illustration_rejected` with the exact user-facing copy, and confirmed the card's image URL was unchanged (still the successful replacement).
- Checked the 375px viewport: opened the disclosure, measured every control's bounding rect against the card's own — zero overflow — and a screenshot confirms the disclosure reads quiet, not competing with the illustration.
- Confirmed on disk: the override landed at `{state_dir}/illustration_overrides/air-france.png`; `git status --porcelain server/assets/icons/illustrations/` stayed empty.

All D7/D8 items above are now `status: complete`, `human_judgment: false`.
5. Check the page at a 375px viewport width — confirm the disclosure/file input/button all fit inside the card without overflowing the grid column.
6. Confirm `server/assets/icons/illustrations/air-france.png` is still the original file, and the replacement landed at `/tmp/skypane-v26-state/illustration_overrides/air-france.png`.

---
*Phase: quick-260902-v26*
*Completed: 2026-09-02*

## Self-Check: PASSED

All modified files verified present on disk (companion/pages/airlines_page.py,
companion/static/style.css, companion/test_status_pages.py,
companion/test_view_pages.py, this SUMMARY.md). All three task commits
(7390fa4, c840f74, cec88cd) verified present in git history. Full
`scripts/run-all-tests.sh` suite re-run after all three commits: zero FAIL
lines, final result PASS, coverage gate held.
