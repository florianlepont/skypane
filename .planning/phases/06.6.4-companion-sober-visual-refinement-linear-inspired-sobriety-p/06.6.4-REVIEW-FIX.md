---
phase: 06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p
fixed_at: 2026-09-01T08:28:25Z
review_path: .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 06.6.4: Code Review Fix Report

**Fixed at:** 2026-09-01T08:28:25Z
**Source review:** .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (fix_scope: critical_warning — WR-01 through WR-05; the 4 Info findings were out of scope and not attempted)
- Fixed: 5
- Skipped: 0

## Fixed Issues

### WR-01: Generic `a:hover` overrides nav-link text colour on hover, including the active link

**Files modified:** `companion/static/style.css`
**Commit:** `4e7ce6f`
**Applied fix:** Added `.sidebar-link:hover, .mobile-nav__link:hover { color: var(--color-text); }` and `.sidebar-link--active:hover, .mobile-nav__link--active:hover { color: var(--color-accent); }` at (0,2,0) specificity, matching the review's suggested fix verbatim. These now outrank the generic `a:hover` rule's (0,1,1) `color` declaration, so inactive nav-link text stays neutral on hover and the active link's accent cue no longer drifts to `--color-accent-hover`. Placed immediately after the existing D-05 `:not()`-scoped background-hover rules for both nav renderers.

### WR-02: Extensive `color-mix()` usage with no fallback anywhere in the file

**Files modified:** `companion/static/style.css`
**Commit:** `e53be7c`
**Applied fix:** Added solid-colour `rgba(23, 25, 31, ...)` fallback declarations (matching `--color-text`'s light-mode value, `#17191F`) immediately before the `color-mix()` declarations on the base `button` rule's `background` and `border` properties, per the review's explicit "at minimum" scope. Browsers without `color-mix()` support now render a visible neutral wash/border instead of nothing. The review's other ~12 cited `color-mix()` call sites (active-pill tints, other button washes, muted text) were not touched — the review's own Fix section scoped the concrete patch to the base `button` rule only ("at minimum"), so extending it further would go beyond the finding's stated guidance.

### WR-03: `.filter-bar__field input:focus` border-color contradicts the "border is structural only" contract

**Files modified:** `companion/static/style.css`
**Commit:** `5e453d5`
**Applied fix:** Replaced `border-color: var(--color-accent)` with `outline: 2px solid var(--color-accent); outline-offset: 1px;` on `.filter-bar__field input:focus`, matching the file's own existing "focused-input outline" sanctioned accent use (same idiom as `.sparkline-hit:focus-visible`). This resolves the contract violation without needing to update the header's accent-use list or 06.6.1-UI-SPEC.md, since the outline pattern was already the documented sanctioned use.

### WR-04: Dark-mode contrast test suite missing the primary-button-label pair

**Files modified:** `companion/test_contrast_check.py`
**Commit:** `2cdfc6e`
**Applied fix:** Added `("dark: primary-button label on accent fill", "#151922", "#FF8A5C")` to `live_pairs` (values confirmed against the actual `--color-on-accent`/`--color-accent` dark-theme tokens in `style.css`), and bumped `EXPECTED_CHECK_COUNT` from 33 to 34. Ran the test module directly: `contrast-check: 34/34 checks pass`, exit 0.

### WR-05: Clear-control regex only handles single-line selectors

**Files modified:** `companion/test_view_pages.py`
**Commit:** `9d7dc99`
**Applied fix:** Added `declarations = re.sub(r",\s*\n\s*", ", ", declarations)` immediately after comment-stripping in `_clear_control_shared_attribute_contract`, joining multi-line comma-grouped selectors onto one line before the rule-matching regex runs. Verified standalone against synthetic multi-line CSS (confirms the full selector list — including earlier lines that were previously dropped — is now captured) and against the real `style.css` (confirms no false positive/regression: the check still correctly finds no competing class-keyed Clear-control rule).

Note: full end-to-end execution of `test_view_pages.py` was not possible in the isolated fix worktree — importing it pulls in `server.plane.detect`, which requires the `requests` package that is only installed in the main checkout's untracked `server/.venv` (not replicated into the isolated worktree, and not present on the system Python). This is a pre-existing environment gap unrelated to the fix; `ast.parse()` syntax verification passed, and the regex logic itself was independently verified as described above.

## Skipped Issues

None — all in-scope findings were fixed.

---

_Fixed: 2026-09-01T08:28:25Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
