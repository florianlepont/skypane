---
phase: 06.6.4
slug: companion-sober-visual-refinement-linear-inspired-sobriety-p
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-31
---

# Phase 06.6.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Custom stdlib `check(name, fn)` harness (not pytest/unittest) — each `companion/test_*.py` is directly executable and self-verifies against its own `EXPECTED_CHECK_COUNT` constant |
| **Config file** | none — `pyproject.toml`'s `[tool.coverage.*]` sections configure coverage measurement only (`fail_under = 83`), not test discovery |
| **Quick run command** | `server/.venv/bin/python3 companion/test_status_pages.py` (or whichever single file was touched) |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~60 seconds (full suite, per this project's own established precedent) |

---

## Sampling Rate

- **After every task commit:** Run the single touched harness directly.
- **After every plan wave:** Run `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite must be green (single accepted exception: `server/test_poll_loop.py`'s pre-existing, unrelated macOS-local Pillow/FreeType `panel.bin` digest mismatch — confirmed unrelated whenever zero `server/` files are touched, which is true for this entire phase).
- **Max feedback latency:** ~60s

---

## Per-Task Verification Map

| Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------|----------|-----------|-------------------|-------------|--------|
| D-08 | Airlines' filter Clear control still has no `<button>`/`<form>` anywhere (D-16 constraint) | markup regression | `server/.venv/bin/python3 companion/test_status_pages.py` — `_filter_bar_absent_with_no_button_or_form_present` (line 1680) | ✅ existing | ⬜ pending |
| D-08 | History's filter bar still carries exactly one each of `data-filter-input`/`-count`/`-clear`/`-empty` | markup regression | `server/.venv/bin/python3 companion/test_view_pages.py` — `_filter_bar_markers_present_once` (line 744) | ✅ existing | ⬜ pending |
| D-03/D-09 | Icon/class-name DOM contract with `style.css` (`STAT_TILE_ICON_CLASS`, etc.) still agrees after CSS value edits | drift guard | `server/.venv/bin/python3 companion/test_companion_app.py` — checks around lines 761-769, 1021-1056 | ✅ existing | ⬜ pending |
| D-06a | If `_theme_form_html()` is edited, "both theme-form copies posting to /ui-theme" check | markup regression | `server/.venv/bin/python3 companion/test_companion_app.py` — check around line 644-648 | ✅ existing | ⬜ pending |
| D-04 | Banner text-on-surface contrast meets WCAG AA after background changes from a 10%-wash to `var(--color-dominant)` | contrast regression | `server/.venv/bin/python3 companion/test_contrast_check.py` (optional new check — not a gap in existing coverage) | ⚠️ optional addition | ⬜ pending |
| All | Full composed suite green | full suite | `scripts/run-all-tests.sh` | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*None — this phase edits CSS values and at most 2 small markup class-name additions inside files every touched harness already exercises. No new test file or fixture is required. Existing infrastructure covers all phase decisions.*

**`EXPECTED_CHECK_COUNT` note (compute at execution time, not planning time — this project's own established precedent):** current on-disk baselines, for reference only — `companion/test_contrast_check.py` = 31, `companion/test_companion_app.py` = 88, `companion/test_config_page.py` = 47, `companion/test_status_pages.py` = 59, `companion/test_view_pages.py` = 41.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Visual fidelity to the validated design-system artifact (hairline cards, flattened banners, 30px buttons with inset highlight, 28px icon-buttons with a 44px hit area, dense nav with single active signal, segmented theme picker, quiet table header, tightened filter bar) | D-01 through D-09 | No visual regression test exists in this stdlib-only test suite; computed-style/structural checks alone previously missed a real mobile-nav bug on this project (see project memory) | Run the companion service locally, sign in, and visually compare each touched page/component against the validated artifact in both light and dark theme; confirm the icon-button's invisible `::before` hit-area actually registers clicks 8px outside its visible edge (not just visually present) |
| WCAG 2.5.5 AAA touch-target trade-off (D-01, 44px→30px) reads as an acceptable, deliberate choice on a real device/trackpad | D-01 | Subjective ergonomics judgment, not something a unit test can assert | Developer's own real-device pass, same discipline as every prior `06.6.x` closing checkpoint |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (Wave 0 not required — see above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none missing)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
