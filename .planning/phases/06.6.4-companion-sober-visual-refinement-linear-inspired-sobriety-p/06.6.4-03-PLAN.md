---
phase: 06.6.4
plan: 03
type: execute
wave: 3
depends_on: ["06.6.4-01", "06.6.4-02"]
files_modified:
  - companion/static/style.css
  - companion/test_contrast_check.py
autonomous: true
requirements: [D-04, D-04a]

must_haves:
  truths:
    - "A banner renders as a near-neutral card surface with a hairline border, a 3px coloured left edge and a small coloured dot — colour is a punctual signal, not a background wash (D-04)."
    - "All three severities (anomaly/warn/flash) keep their own existing colour on both the edge and the dot; only the mechanism changed."
    - "Health's source-fault block, which is simultaneously a card and a banner, keeps its multi-paragraph block layout and its error edge — the banner's flex row and dot never reflow it (D-04a)."
    - "Body text on the card surface every banner now uses is proven to meet WCAG AA in both themes by an automated check, not by inspection."
  artifacts: []
  key_links:
    - "The banner's flex/dot treatment reaches the combined page-section banner selector by inheritance; that selector must explicitly opt out of both, or Health's source-fault section reflows into a single row."
    - "The combined selector's restated left border is still load-bearing after plan 01 gave the page-section rule a real border shorthand that resets it."
---

<objective>
Replace the banner system's 10% colour-wash background with D-04's near-neutral surface plus hairline border, slimmer 3px coloured edge and small status dot, across the base banner rule and all three severity modifiers, and reconcile the combined card-and-banner selector Health's source-fault block uses so it keeps its own block layout (D-04a).

Purpose: The wash made every banner a large tinted region competing with the accent-filled primary button for attention; D-04 demotes colour to a punctual signal (edge plus dot) on the same neutral card surface plan 01 established, which is what lets a real alert read as an alert.
Output: `companion/static/style.css` — the banner rule rebuilt with a flex row and a dot pseudo-element, three severity modifiers each gaining a dot colour, and the combined card-and-banner selector opting out of the flex row and the dot; `companion/test_contrast_check.py` — two new live token pairs pinning body text on the card surface in both themes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-CONTEXT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md
@companion/static/style.css
@companion/test_contrast_check.py
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04 — flat banner surface, 3px edge, status dot, across all three severities</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 714-766 (the banner rule, its long comment justifying the 10% wash, and the three severity modifiers with their own comments);
    companion/static/style.css lines 687-712 (the existing status-dot component — read to confirm the banner's dot is a NEW pseudo-element, not a reuse of that class, since banner markup carries no dot element);
    companion/layout.py lines 889-922 (the two banner builders — confirm both emit a single `<div class="banner ...">` whose only child is escaped plain text, with no block-level element inside, so a flex row is safe for them);
    companion/pages/health_page.py line 850 (the one call site that combines the card class with both banner classes on a `<section>` — this is the exception Task 2 handles)
  </read_first>
  <action>
    Rebuild the base `.banner` rule per D-04. Declare, in this order so the shorthand cannot clobber the edge: `display: flex;`, `align-items: center;`, `gap: 8px;`, `padding: var(--space-sm) var(--space-md);`, `margin-bottom: var(--space-lg);`, `background: var(--color-dominant);`, `border: 1px solid var(--color-border);`, `border-left: 3px solid var(--color-accent);`, `font-size: var(--font-body-size);`. The left-edge declaration must follow the border shorthand within the same rule — the shorthand resets all four sides, so reversing them silently reduces the alert edge to a 1px hairline. Remove the `color-mix` wash entirely; it is replaced, not re-tinted.

    Add the dot as a pseudo-element on the base rule: `.banner::before { content: ""; flex: none; width: 6px; height: 6px; border-radius: 50%; background: var(--color-accent); }`. `flex: none` is required or the dot collapses under the flex container's default shrinking. Deliberately do NOT reuse the existing status-dot class here — the banner builders emit no dot element and D-04a forbids touching banner content, so the dot has to be presentational.

    Update the three severity modifiers so each keeps exactly the colour it has today and gains a matching dot: the anomaly modifier keeps the error token, the warn modifier keeps the warn token, the flash modifier keeps the accent token. For each, replace the `background` wash declaration with nothing (the base rule's neutral surface now applies to all three) and keep only `border-left-color`, then add a sibling `::before` rule setting `background` to the same token. Preserve the flash modifier's existing comment explaining why it is kept rather than deleted as redundant — that reasoning is unchanged and still correct.

    Rewrite the long comment above the base rule. Its entire second paragraph exists to justify the 10% wash and to reassure a reader that the mix stays 90% secondary surface so text contrast holds — every sentence of which this task falsifies. Replace it with the new contract: neutral card surface, hairline boundary, a 3px coloured edge and a 6px dot as the only two carriers of severity; note that the surface is now the same card token every card uses (plan 01, D-03), and that the text-on-that-surface contrast is pinned by an automated check added in Task 2 rather than argued in prose.
  </action>
  <acceptance_criteria>
    - The base banner block declares `display: flex`, `align-items: center`, `gap: 8px`, `background: var(--color-dominant)`, a 1px `var(--color-border)` shorthand border and a `border-left: 3px` declaration, with the left-edge declaration positioned after the shorthand.
    - With comments stripped, no rule whose selector starts with `.banner` contains `color-mix`.
    - A `.banner::before` rule exists declaring `content: ""`, `width: 6px`, `height: 6px`, `border-radius: 50%` and `flex: none`.
    - Each of the three severity modifiers has a matching `::before` rule whose `background` names the same status/accent token as its own `border-left-color`.
    - `server/.venv/bin/python3 companion/test_status_pages.py` and `companion/test_config_page.py` pass at their existing on-disk EXPECTED_CHECK_COUNT — banner markup is unchanged, so every existing banner assertion must pass untouched.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
b = block('.banner')
for needle in ('display: flex', 'align-items: center', 'gap: 8px', 'background: var(--color-dominant)', 'border: 1px solid var(--color-border)', 'border-left: 3px'):
    assert needle in b, 'banner missing ' + needle
assert b.index('border: 1px solid') < b.index('border-left: 3px'), 'shorthand would clobber the alert edge'
dot = block('.banner::before')
for needle in ('content:', 'width: 6px', 'height: 6px', 'border-radius: 50%', 'flex: none'):
    assert needle in dot, 'banner dot missing ' + needle
pairs = {'.banner--anomaly': 'var(--color-status-error)', '.banner--warn': 'var(--color-status-warn)', '.banner--flash': 'var(--color-accent)'}
for sel, token in pairs.items():
    edge = block(sel)
    assert 'border-left-color: ' + token in edge, sel + ' lost its edge colour'
    assert 'color-mix' not in edge, sel + ' still carries the wash'
    assert 'background: ' + token in block(sel + '::before'), sel + ' dot colour missing'
assert 'color-mix' not in block('.banner'), 'base banner still carries a wash'
print('OK D-04')
" && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <done>All three banner severities render as one neutral surface distinguished only by a 3px edge and a 6px dot in their own existing colour; no wash remains anywhere in the banner rule set.</done>
</task>

<task type="auto">
  <name>Task 2: D-04a — combined card-and-banner reconciliation, plus a contrast guard for the new surface</name>
  <files>companion/static/style.css, companion/test_contrast_check.py</files>
  <read_first>
    companion/static/style.css lines 1437-1462 (the combined card-and-banner selector and its three-paragraph comment explaining the border-left width, colour and wash restatements, all written for the pre-06.6.4 cascade);
    companion/static/style.css as edited by plan 01 (the card rule's new border shorthand — read the current on-disk state, not the pre-plan-01 line numbers);
    companion/pages/health_page.py around line 850 (the source-fault section: a `<section>` carrying the card class plus both banner classes, containing a heading and multiple paragraphs — the reason the flex row must not reach it);
    companion/test_contrast_check.py lines 96-131 (the live_pairs tuple and the check factory around it);
    companion/test_contrast_check.py line 49 (the EXPECTED_CHECK_COUNT constant — read its REAL current on-disk value; do not trust any number written into this plan)
  </read_first>
  <action>
    Reconcile the combined card-and-banner selector, which Health's source-fault block is the only user of. Three changes, each with a distinct reason to state in the comment.

    First, narrow its restated left border from 4px to 3px so it matches D-04's new edge width. This restatement is still load-bearing and must not be deleted: plan 01 gave the card rule a real `border: 1px solid var(--color-border)` shorthand, which resets the left side at equal specificity, so without this higher-specificity restatement the app's single most severe state would draw a 1px neutral edge while every other anomaly banner draws a 3px error edge.

    Second, replace its restated wash background with `var(--color-dominant)`. Keep the declaration rather than deleting it as now-redundant, for exactly the cascade-determinism reason its existing comment already gives — but rewrite that reason, since it no longer describes a colour conflict, only a guarantee that the block cannot drift onto a different surface than the banners it shares a severity language with.

    Third — and this is the new finding, not something CONTEXT.md could have anticipated — add `display: block;` and a `.page-section.banner--anomaly::before { display: none; }` rule. Task 1 made the base banner a centred flex row, and that reaches this selector by inheritance: the source-fault block contains a heading and several paragraphs, so without opting out it would reflow into a single horizontal row with the 6px dot as its first flex item. The block already carries a heading and its own 3px error edge, so it needs no dot. Document both opt-outs as scoped exceptions that preserve D-04a's "container treatment only, never banner content" boundary rather than violating it.

    Then add a contrast guard in `companion/test_contrast_check.py`. Read the file's `live_pairs` tuple: it pins accent-on-canvas, accent-on-card-surface, accent-on-secondary, the primary-button label, and body-text-on-canvas — but it does not pin body text on the primary card surface in either theme, which is now the surface every banner and every card renders text on after this phase. Add exactly two entries following the existing tuple's shape and hard-coded-hex convention (the module's own comment explains why hexes are literals rather than parsed from the CSS — a token change must surface as a regression, not silently re-validate itself): light body text on the light card surface, and dark body text on the dark card surface. Use the values the token blocks actually declare today. Then bump `EXPECTED_CHECK_COUNT` — compute the new value from the file's real on-disk baseline plus the number of checks you actually added, at execution time; this project's every prior phase records that a number written into a plan at planning time goes stale.
  </action>
  <acceptance_criteria>
    - The combined card-and-banner block declares `border-left: 3px`, `background: var(--color-dominant)` and `display: block`.
    - A `.page-section.banner--anomaly::before` rule exists declaring `display: none`.
    - `companion/test_contrast_check.py`'s `live_pairs` gains exactly two entries covering body text on the primary card surface in light and in dark, using hard-coded hex literals consistent with the surrounding entries.
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 with its bumped, execution-time-computed `EXPECTED_CHECK_COUNT`, and every new pair meets the module's AA threshold.
    - Health's source-fault section still renders as a block with its heading and paragraphs stacked — confirmed by the closing plan's browser checkpoint, and structurally by the CSS opt-outs above.
    - `git diff --stat` across this plan's commits touches exactly `companion/static/style.css` and `companion/test_contrast_check.py`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
c = block('.page-section.banner--anomaly')
assert 'border-left: 3px' in c, 'combined selector edge width not reconciled'
assert 'background: var(--color-dominant)' in c, 'combined selector not on the card surface'
assert 'display: block' in c, 'combined selector would inherit the banner flex row'
assert 'display: none' in block('.page-section.banner--anomaly::before'), 'combined selector would render a stray dot'
src = open('companion/test_contrast_check.py').read()
assert src.count('card surface') >= 2, 'expected two new card-surface contrast pairs'
print('OK D-04a')
" && server/.venv/bin/python3 companion/test_contrast_check.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>Health's source-fault block keeps its stacked layout and a full-width 3px error edge; body text on the surface every banner and card now uses is pinned to WCAG AA in both themes by an executable check rather than a paragraph of prose.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — plan 03

**New Python symbols:** none. The only Python file touched is a test harness, which gains two tuple entries, not a new function or constant.
**New CSS class names:** none — the banner dot is a pseudo-element precisely because the banner builders emit no dot element and D-04a forbids touching banner content.
**New design tokens:** none.

**New CSS rules (pseudo-element rules on existing selectors only):**
- `.banner::before` — the 6px status dot
- `.banner--anomaly::before`, `.banner--warn::before`, `.banner--flash::before` — per-severity dot colour
- `.page-section.banner--anomaly::before` — the opt-out that keeps Health's source-fault block dot-free

**Modified in place:** `.banner`, the three severity modifiers, `.page-section.banner--anomaly` (gaining `display: block`), the banner rule's justification comment, and `companion/test_contrast_check.py`'s `live_pairs` tuple plus its `EXPECTED_CHECK_COUNT`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→static asset | `GET /static/style.css`, pre-auth by design; property values only |
| server→rendered alert content | The banner builders escape their message and choose their ARIA role from a whitelist; this plan changes the container's appearance only and touches neither |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4-03-01 | Repudiation | banner severity legibility | medium | mitigate | Demoting a full-region colour wash to a 3px edge plus a 6px dot reduces the visual weight of a genuine alert; a missed error banner means an operator cannot later account for having seen a fault. Mitigated three ways: the ARIA roles (`alert` for error, `status` otherwise) are untouched so assistive tech is unaffected; the dot and edge use the unmodified `--color-status-*` tokens whose perceptual separation from the accent is already pinned by the separation section of the contrast harness; and Task 2 adds an executable AA guard for text on the new surface. The residual subjective question — does an alert still read as an alert — is an explicit item on the closing plan's blocking human-verify checklist |
| T-06.6.4-03-02 | Information Disclosure | `.banner::before` presentational dot | low | accept | A CSS-generated pseudo-element with empty content carries no data and is not exposed to the accessibility tree as content; the banner's own escaped text remains the sole information carrier |
| T-06.6.4-03-03 | Tampering | rendered markup | low | accept | Zero Python page/layout files in `files_modified`; the only Python file touched is a test harness, which renders nothing to a browser |
| T-06.6.4-SC | Tampering | package-manager installs | low | accept | Zero installs; RESEARCH.md records the phase's Package Legitimacy Audit as not-applicable |
</threat_model>

<verification>
1. Both task gates pass.
2. `scripts/run-all-tests.sh` is green apart from the single pre-existing, accepted `server/test_poll_loop.py` macOS-local digest mismatch (this plan touches zero `server/` files).
3. The contrast harness's `EXPECTED_CHECK_COUNT` was computed from its real on-disk baseline at execution time and is recorded in the SUMMARY alongside the before/after numbers.
</verification>

<success_criteria>
- Three banner severities, one neutral surface, colour only at the edge and the dot.
- Health's combined card-and-banner section is visually and structurally unbroken.
- Body-text contrast on the new banner/card surface is machine-verified in both themes.
</success_criteria>

<output>
Create `.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-03-SUMMARY.md` when done. Record explicitly: the flex-inheritance finding on the combined selector and its two opt-outs, and the real before/after `EXPECTED_CHECK_COUNT` values.
</output>
