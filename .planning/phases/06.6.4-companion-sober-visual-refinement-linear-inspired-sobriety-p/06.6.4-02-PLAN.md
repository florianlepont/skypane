---
phase: 06.6.4
plan: 02
type: execute
wave: 2
depends_on: ["06.6.4-01"]
files_modified:
  - companion/static/style.css
autonomous: true
requirements: [D-01, D-01a, D-01b, D-01c, D-02]

must_haves:
  truths:
    - "Text buttons render at 30px tall with 13px semibold labels and 6px corners (D-01), a deliberate WCAG 2.5.5 AAA trade-off that stays 2.5.8 AA-compliant."
    - "The primary submit button carries an inset top highlight plus a soft drop shadow at rest and a deeper pair on hover (D-01a)."
    - "Quiet/secondary buttons are near-invisible at rest and reveal a wash plus a stronger edge on hover (D-01b)."
    - "Every button depresses by 1px while active."
    - "The copy button renders as a 28x28 visual box with a real 44x44 pointer/touch hit area synthesized by an invisible pseudo-element — no accessibility trade-off (D-02)."
    - "The 44px hit-target floor is still genuinely intact for the hamburger toggle, native inputs/selects, and disclosure summaries (D-01c)."
  artifacts: []
  key_links:
    - "The quiet-hover rule must sit after both quiet-button overrides in source order, or those equal-specificity rules silently defeat the hover state."
    - "The primary-button rules must explicitly neutralize the new base border, or an accent fill picks up a text-tinted hairline."
    - "The theme-picker buttons are submit buttons and would inherit the new inset highlight; plan 06.6.4-04 neutralizes that in the same edit that builds the segmented control."
---

<objective>
Apply the phase's button system (D-01, D-01a, D-01b, D-01c) and the icon-only button pattern (D-02) to `companion/static/style.css`: text buttons drop from 44px to 30px with semibold 13px labels, the primary action gains a subtle inset highlight instead of a flat fill, secondary actions become near-invisible until hovered, and the copy button shrinks to a 28x28 glyph while keeping a genuine 44x44 hit area.

Purpose: Oversized, uniformly-filled controls are the second structural cause (after permanent card relief, closed by plan 01) of the "catastrophique" density the developer reacted to. D-01's touch-target reduction was shown to and accepted by the developer three times and must be implemented, not re-litigated.
Output: `companion/static/style.css` — the base button rule, both primary-button rules, both quiet-button overrides, a new quiet-hover rule, a new pressed rule, and the icon-button rule with its pseudo-element hit area; plus a 44px floor register comment recording which selectors deliberately kept the floor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-CONTEXT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md
@companion/static/style.css
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01/D-01a — base button density, primary inset highlight, pressed state</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 795-831 (the global input/select rule and the base button rule, read together — the input/select 44px floor is explicitly NOT changed here);
    companion/static/style.css lines 833-852 (the primary submit rule, its comment about theme-picker exclusion, and the submit hover rule);
    companion/static/style.css lines 916-921 (the disabled-button rule);
    companion/static/style.css lines 563-592 (the theme-picker rules — read only to understand which declarations of this task's changes will and will not reach them);
    .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md (Pattern 1 — the color-mix idiom that removes the need for a dark-mode override block; the "D-01: button base rule" code example showing the exact current declarations)
  </read_first>
  <action>
    Rewrite the base `button` rule per D-01 and the resting half of D-01b. Remove both the `min-height` and `min-width` 44px declarations — this is the deliberate WCAG 2.5.5 AAA trade-off, and leaving either one in place would silently defeat the whole decision, since a minimum beats a height. Set `height: 30px;`, `padding: 0 12px;`, `font-size: 13px;`, `font-weight: var(--weight-semibold);`, `letter-spacing: -0.01em;`, `border-radius: 6px;`. Keep `font-family: var(--font-ui);`, `cursor: pointer;` and `color: var(--color-text);` unchanged.

    Replace the rule's `background: var(--color-secondary)` and `border: none` with D-01b's near-invisible resting treatment, expressed through the file's own established two-theme idiom rather than the light-only literals CONTEXT.md quotes: `background: color-mix(in srgb, var(--color-text) 4.5%, transparent);` and `border: 1px solid color-mix(in srgb, var(--color-text) 9%, transparent);`. This is RESEARCH Pattern 1 — the text token resolves near-black in light and near-white in dark, so one declaration produces a dark wash on light surfaces and a light wash on dark surfaces with no per-theme override block to keep in sync. It is also numerically equivalent to the values CONTEXT.md specifies for light mode, and it closes D-01b's own explicitly-open "dark-mode analogue needed" question. Record that reasoning in a comment on the rule.

    Extend the rule's `transition` list with `box-shadow .15s ease, transform .15s ease` so D-01a's shadow swap and the pressed state animate rather than snap. Do NOT add a `display` declaration to the base rule — native buttons already centre their label at any height, and introducing `display` on a base rule creates the same `[hidden]` collision the dirty-bar rule already documents. Icon centring is handled on the icon button itself in Task 3.

    In `button[type="submit"]`, keep the accent fill and on-accent foreground, and add D-01a's resting shadow: `box-shadow: inset 0 1px 0 rgba(255,255,255,.14), 0 1px 2px rgba(18,21,27,.14);`. Also add `border-color: transparent;` — the base rule now paints a text-tinted hairline that would sit on top of an accent fill and read muddy; this is a new consequence of D-01b, not a value CONTEXT.md had to anticipate. In `button[type="submit"]:hover`, keep the accent-hover background and add both the deeper shadow pair `inset 0 1px 0 rgba(255,255,255,.16), 0 2px 5px rgba(18,21,27,.18);` and a repeated `border-color: transparent;`, so the primary button's edge stays neutralized even once Task 2's equal-specificity quiet-hover rule exists. These shadow values are theme-neutral black/white alphas by design, so no dark-mode variant is needed (D-01a says so explicitly).

    Add D-01b's pressed state as one rule covering both variants: `button:active { transform: translateY(1px); }`. Place it immediately after the submit-hover rule and therefore before the disabled rule, so a disabled button's own treatment stays the later, winning declaration set.

    Note for the SUMMARY, no code change required here: `.theme-form .theme-option` still carries its own 44px minimum at this point in the phase, so theme-picker buttons keep their current size through waves 2 and 3 and only change in plan 06.6.4-04 — an intentional intermediate state, not a regression. Those buttons are also submit buttons and will inherit the inset highlight added here; plan 04 neutralizes it in the same edit that builds the segmented control.
  </action>
  <acceptance_criteria>
    - With comments stripped, the base `button` block declares neither `min-height` nor `min-width`, and does declare `height: 30px`, `padding: 0 12px`, `font-size: 13px`, `var(--weight-semibold)`, `letter-spacing: -0.01em` and `border-radius: 6px`.
    - The base `button` block's `background` and `border` both resolve through `color-mix(in srgb, var(--color-text)` — no raw rgba literal, no per-theme override block added.
    - The base `button` block's `transition` list includes both `box-shadow` and `transform`.
    - `button[type="submit"]` declares an `inset 0 1px 0` shadow and `border-color: transparent`; `button[type="submit"]:hover` declares a deeper `inset 0 1px 0` shadow and `border-color: transparent`.
    - A `button:active` rule declaring `translateY(1px)` exists and appears earlier in the file than the `button:disabled` rule.
    - The global `input, select` rule still declares `min-height: 44px` (untouched).
    - All five companion harnesses pass at their existing on-disk EXPECTED_CHECK_COUNT.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
b = block('button')
for needle in ('height: 30px', 'padding: 0 12px', 'font-size: 13px', 'var(--weight-semibold)', 'letter-spacing: -0.01em', 'border-radius: 6px', 'color-mix(in srgb, var(--color-text)'):
    assert needle in b, 'base button missing ' + needle
assert 'min-height' not in b and 'min-width' not in b, 'base button kept a 44px floor'
for prop in ('box-shadow', 'transform'):
    assert prop in b[b.index('transition'):], 'transition list missing ' + prop
p = block('button[type=\"submit\"]')
assert 'inset 0 1px 0' in p and 'border-color: transparent' in p, 'primary rest state incomplete'
h = block('button[type=\"submit\"]:hover')
assert 'inset 0 1px 0' in h and 'border-color: transparent' in h, 'primary hover state incomplete'
assert 'translateY(1px)' in block('button:active'), 'pressed state missing'
assert d.index('\nbutton:active {') < d.index('\nbutton:disabled {'), 'pressed rule must precede disabled rule'
assert 'min-height: 44px' in block('input,\nselect') or 'min-height: 44px' in d[d.index('\ninput,'):d.index('}', d.index('\ninput,'))], 'native input floor was disturbed'
print('OK D-01/D-01a')
" && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <done>Text buttons are 30px semibold controls with 6px corners; the primary action reads as a lit, slightly raised accent chip; every button depresses on press; the native input floor is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: D-01b/D-01c — quiet-button hover reveal and the 44px floor register</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 854-866 (the sign-out quiet override and its source-order comment);
    companion/static/style.css lines 895-904 (the dirty-bar cancel quiet override and its source-order comment);
    companion/static/style.css lines 400-418 (the hamburger toggle — a selector that deliberately KEEPS its 44px floor);
    companion/static/style.css lines 1164-1184 (the disclosure summary rule — also deliberately keeps its floor);
    companion/static/style.css lines 470-486 and 646-658 (the two nav-link rules that still carry a 44px floor at this point in the phase — plan 06.6.4-04 changes them, this task must not)
  </read_first>
  <action>
    Update the two quiet-button overrides so they express D-01b's new resting treatment instead of the superseded transparent-plus-structural-border one, and so they still defeat the primary accent fill they exist to defeat. Both the sign-out override and the dirty-bar cancel override should declare: the same `color-mix(in srgb, var(--color-text) 4.5%, transparent)` background as the base rule, `border: 1px solid color-mix(in srgb, var(--color-text) 9%, transparent)`, `box-shadow: none` (this is newly required — Task 1 gave every submit button an inset highlight, and both of these are submit buttons that must not read as primary actions), and the existing `color: var(--color-text)`. Preserve each rule's existing comment about equal specificity and source-order placement, extending it to mention the shadow neutralization.

    Add D-01b's hover reveal as a single base rule: `button:hover { background: color-mix(in srgb, var(--color-text) 7.5%, transparent); border-color: color-mix(in srgb, var(--color-text) 14%, transparent); }`. Placement is load-bearing and must be stated in a comment on the rule: it has to sit AFTER both quiet overrides in source order (i.e. immediately after the dirty-bar cancel rule and before the icon-button rule), because those overrides share its specificity and would otherwise win on `background` even while the pointer is over them, leaving the two quietest controls in the app as the only two with no hover feedback. The primary button is unaffected by placement because its own hover rule carries higher specificity and, per Task 1, restates the neutralized border explicitly.

    Then complete D-01c's audit by adding a short "44px floor register" comment immediately above the base `button` rule. Enumerate, exhaustively, which interactive selectors deliberately gave the floor up in 06.6.4 and which deliberately keep it. Gave it up: text buttons at 30px (D-01), nav links at 32px (D-05, plan 04), theme-picker segments at 28px (D-06, plan 04), the filter input at 32px (D-08, plan 05) — and note that the icon button (D-02) is not in this list because it keeps a genuine 44x44 hit area. Keeps it: the hamburger toggle, the global native input/select rule, and the disclosure summary — none of which CONTEXT.md names, and all of which must stay intact. Also state the criterion the trade-off rests on: WCAG 2.5.8 (Level AA) requires 24px and is still met everywhere; it is 2.5.5 (Level AAA, 44px) that was consciously given up on the named selectors only, with the developer's recorded acceptance.

    Do not modify the nav-link, theme-picker or filter-input rules in this task even though the register comment forward-references them — they belong to plans 04 and 05, which own the single-file edit sequence for those selectors.
  </action>
  <acceptance_criteria>
    - Both quiet-button override blocks declare the text-based `color-mix` background, a `color-mix` border, and `box-shadow: none`.
    - A `button:hover` rule exists declaring both a `background` and a `border-color` through `color-mix(in srgb, var(--color-text)`.
    - With comments stripped, the `button:hover` rule appears LATER in the file than both quiet-button override rules.
    - The hamburger-toggle rule, the global input/select rule and the disclosure-summary rule each still declare `min-height: 44px`.
    - A comment above the base button rule names all four selectors that gave up the floor and all three that keep it.
    - All five companion harnesses pass at their existing on-disk EXPECTED_CHECK_COUNT.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def start(sel):
    return d.index('\n' + sel + ' {')
def block(sel):
    i = start(sel)
    return d[i:d.index('}', i)]
for sel in ('.logout-form button', '.dirty-bar__cancel'):
    b = block(sel)
    assert 'color-mix(in srgb, var(--color-text)' in b, sel + ' not on the quiet wash'
    assert 'box-shadow: none' in b, sel + ' would inherit the primary inset highlight'
hv = block('button:hover')
assert 'background' in hv and 'border-color' in hv and 'color-mix(in srgb, var(--color-text)' in hv, 'quiet hover incomplete'
assert start('button:hover') > start('.logout-form button'), 'quiet hover is defeated by the sign-out override'
assert start('button:hover') > start('.dirty-bar__cancel'), 'quiet hover is defeated by the cancel override'
for sel in ('.site-nav-toggle', 'summary'):
    assert 'min-height: 44px' in block(sel), sel + ' lost its deliberate 44px floor'
print('OK D-01b/D-01c')
" && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>Sign out and Cancel are near-invisible at rest and light up under the pointer like every other quiet button; the file carries an explicit, exhaustive record of which 44px floors were traded and which were kept.</done>
</task>

<task type="auto">
  <name>Task 3: D-02 — icon-only button, 28x28 visual box with a real 44x44 hit area</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 906-914 (the icon-button rule and the comment whose premise Task 1 just invalidated);
    companion/static/style.css lines 1269-1288 (the icon sizing rule and the icon-tint rules);
    companion/pages/history_page.py lines 285-311 (the copy-button builder and the merged Callsign/Hex cell that renders two of them in one cell — read to confirm the two hit areas are separated by the hex text and separator, not adjacent);
    .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md (Pitfall 3 — why the existing comment must be rewritten as part of this edit; Security Domain section's hit-area collision note)
  </read_first>
  <action>
    Implement D-02 on `.copy-btn`, keeping the existing class name (no rename — the name is already referenced by `history_page.py`'s builder and by the DOM-contract guards, and D-02's "planner's call on a rename" resolves toward the lower-blast-radius option; state that in the SUMMARY). Declare: `width: 28px; height: 28px; padding: 0; position: relative; border: none; border-radius: 6px; background: transparent;` plus `display: inline-flex; align-items: center; justify-content: center;`. The flex centring is needed here specifically (unlike the base rule, where it was deliberately avoided): the button's only child is an SVG glyph, and a fixed 28x28 box with zero padding needs explicit centring rather than relying on inline layout. `border: none` and the transparent background are what keep an icon affordance from reading as a filled control now that the base rule paints a wash and a hairline.

    Add the invisible hit-area pseudo-element: `.copy-btn::before { content: ""; position: absolute; inset: -8px; }`. 28 plus 8 on each side is exactly 44, centred on the visible glyph — this is the whole reason D-02 carries no accessibility trade-off, unlike D-01. The pseudo-element must have no background, no border and no content; it is a pointer target only.

    Add `.copy-btn:hover { background: color-mix(in srgb, var(--color-text) 7%, transparent); }` — the 7% strength D-02 specifies for light mode, expressed through the same two-theme idiom the rest of the phase uses. This rule must come after the base quiet-hover rule Task 2 added; since the icon-button rules already sit later in the file than that placement, no reordering is needed, but verify it rather than assuming.

    Rewrite the comment above the rule. Its current text asserts that this button inherits a 44x44 floor from the base button rule and instructs a reader never to reduce below it — a mechanism Task 1 removed. The replacement must state the actual new mechanism: a 28x28 visual box, a 44x44 pointer/touch target synthesized by the pseudo-element's negative inset, and the fact that this is the pattern to reuse for any future icon-only button rather than reintroducing a minimum-size floor. Also record the adjacency finding: History's merged Callsign/Hex cell renders two of these in one cell, separated by the inline separator span and the hex value itself (roughly 50px), so the two enlarged hit areas do not overlap; a future layout that places two icon buttons closer than 16px apart would need to revisit the inset.
  </action>
  <acceptance_criteria>
    - The icon-button block declares `width: 28px`, `height: 28px`, `padding: 0`, `position: relative`, `border: none`, `background: transparent`, `border-radius: 6px` and `display: inline-flex`.
    - A `.copy-btn::before` rule exists declaring `content: ""`, `position: absolute` and `inset: -8px`, and declares no background, border or box-shadow.
    - A `.copy-btn:hover` rule exists using the text-based `color-mix` wash, appearing later in the file than the base `button:hover` rule.
    - With comments stripped, the icon-button block declares no `min-height` and no `min-width`.
    - History still renders exactly two copy buttons in a populated Callsign/Hex cell, and `server/.venv/bin/python3 companion/test_view_pages.py` passes at its existing on-disk count.
    - `git diff --stat` across this plan's three commits touches exactly `companion/static/style.css`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def start(sel):
    return d.index('\n' + sel + ' {')
def block(sel):
    i = start(sel)
    return d[i:d.index('}', i)]
b = block('.copy-btn')
for needle in ('width: 28px', 'height: 28px', 'padding: 0', 'position: relative', 'border: none', 'background: transparent', 'border-radius: 6px', 'display: inline-flex'):
    assert needle in b, 'icon button missing ' + needle
assert 'min-height' not in b and 'min-width' not in b, 'icon button kept a size floor'
pb = block('.copy-btn::before')
assert 'inset: -8px' in pb and 'position: absolute' in pb and 'content:' in pb, 'hit area incomplete'
for forbidden in ('background', 'border', 'box-shadow'):
    assert forbidden not in pb, 'hit area must be invisible, found ' + forbidden
assert start('.copy-btn:hover') > start('button:hover'), 'icon hover is defeated by the base quiet hover'
print('OK D-02')
" && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>The copy affordance reads as a small quiet glyph while remaining a full 44x44 target for a thumb or an imprecise click; the file no longer claims a floor-inheritance mechanism that does not exist.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — plan 02

**New Python symbols:** none.
**New CSS class names:** none — the icon button deliberately keeps its existing class name rather than being generalized/renamed.
**New design tokens:** none.

**New CSS rules (state and pseudo-element rules on existing selectors only):**
- `button:hover` — the quiet reveal, placed after both quiet-button overrides
- `button:active` — the pressed state, placed before the disabled rule
- `.copy-btn::before` — the invisible 44x44 hit area
- `.copy-btn:hover`

**Modified in place:** the base `button` rule, `button[type="submit"]`, `button[type="submit"]:hover`, `.logout-form button`, `.dirty-bar__cancel`, `.copy-btn`, and the comment blocks above the base button rule and the icon button rule.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→static asset | `GET /static/style.css`, served pre-auth by design; only property values change |
| user pointer→interactive control | The icon button's hit area is enlarged past its visible bounds — the one place this plan changes what a click at a given coordinate activates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4-02-01 | Tampering | `.copy-btn::before` enlarged hit area | low | mitigate | The pseudo-element extends 8px past the glyph. Task 3's `read_first` requires confirming against `history_page.py`'s merged cell that no second interactive element sits within that margin, and the finding is recorded in the rule's comment. The closing plan's blocking human-verify checkpoint re-tests it on a real pointer, since a mis-sized hit area could route a click to the wrong copy action (wrong value written to the clipboard) |
| T-06.6.4-02-02 | Elevation of Privilege | button visual hierarchy | low | accept | D-01b makes destructive/quiet actions (Sign out, Cancel) visually quieter than the primary action. Both remain fully visible, keyboard-focusable, and carry an unchanged visible focus outline; no action is hidden or made harder to identify, only less loud. Accepted per the developer's explicit validation of the button hierarchy |
| T-06.6.4-02-03 | Tampering | rendered markup | low | accept | Zero Python files in `files_modified`; enforced by Task 3's `git diff --stat` acceptance criterion |
| T-06.6.4-SC | Tampering | package-manager installs | low | accept | Zero installs in this plan; RESEARCH.md records the phase's Package Legitimacy Audit as not-applicable |
</threat_model>

<verification>
1. All three task gates pass.
2. `scripts/run-all-tests.sh` is green apart from the single pre-existing, accepted `server/test_poll_loop.py` macOS-local digest mismatch (confirmed unrelated — this plan touches zero `server/` files).
3. `git diff --stat` across this plan's commits touches exactly `companion/static/style.css`.
4. No `EXPECTED_CHECK_COUNT` bump is expected from this plan; if one becomes necessary, compute it from the file's real on-disk baseline at execution time, never from a number written here.
</verification>

<success_criteria>
- Text buttons are 30px, semibold, 6px-cornered, and depress on press.
- The primary action is the only lit control on any given screen.
- Quiet controls are near-invisible at rest and legible on hover in both themes.
- The copy glyph is 28px but still a 44px target.
- The three deliberately-retained 44px floors are provably intact.
</success_criteria>

<output>
Create `.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-02-SUMMARY.md` when done. Record explicitly: the class-rename decision for the icon button, the color-mix resolution of D-01b/D-02's open dark-mode question, and the source-order requirement discovered for the quiet-hover rule.
</output>
