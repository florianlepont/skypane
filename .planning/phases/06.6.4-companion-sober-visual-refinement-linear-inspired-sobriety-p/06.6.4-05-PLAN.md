---
phase: 06.6.4
plan: 05
type: execute
wave: 5
depends_on: ["06.6.4-01", "06.6.4-02", "06.6.4-03", "06.6.4-04"]
files_modified:
  - companion/static/style.css
  - companion/test_view_pages.py
autonomous: true
requirements: [D-07, D-07a, D-08]

must_haves:
  truths:
    - "Table headers read as quiet uppercase labels rather than filled colour blocks, both when static and when stuck to the top of a scrolled table (D-07)."
    - "Zebra striping is gone; a row answers the pointer with a subtle hover tint instead."
    - "The last row of a table has no trailing separator line."
    - "The filter input is a 32px transparent field whose search glyph never sits under its own text."
    - "Both the History Clear button and the Airlines Clear link render as the same quiet underlined text link, through one shared rule and zero Python changes — Airlines still contains no button or form element anywhere (D-08 reconciled without reopening D-16)."
    - "Table column content is unchanged — no new pills, dots or columns (D-07a)."
  artifacts: []
  key_links:
    - "The base header rule and the sticky header rule carry equal specificity and must both be edited in one task, or the sticky rule silently repaints the filled block the base rule just gave up."
    - "The global native-input minimum height outranks a plain height on the filter field; the minimum must be explicitly cleared or the 32px value has no effect."
    - "The Clear control converges via the existing data attribute both pages already emit, so neither page module is touched and the D-16 no-button regression check passes unmodified."
---

<objective>
Restyle the data-table shell — quiet uppercase headers (both the base and the sticky rule), no zebra striping, a hover row tint, roomier cells and a clean bottom edge — and tighten the filter bar's input, count and Clear control, converging History's Clear button and Airlines' Clear link on one shared rule without touching either page module (D-07, D-07a, D-08).

Purpose: History and Airlines are the two densest screens in the app; the filled header block plus alternating row fills gave every table three competing background colours. Reducing that to one surface with a hover tint is the last structural piece of the sobriety pass.
Output: `companion/static/style.css` — both header rules, the cell rule, the removed stripe rule, three new table rules, and four filter-bar rules; `companion/test_view_pages.py` — one new drift guard pinning the shared Clear-control contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-CONTEXT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md
@companion/static/style.css
@companion/pages/history_page.py
@companion/pages/airlines_page.py
@companion/test_view_pages.py
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 — table shell, both header rules changed together</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 985-1038 (the wrap, the table, the base header rule, the sticky header rule with its scroll-opacity comment, the cell rule with its separator-colour comment, and the stripe rule);
    companion/static/style.css lines 1040-1062 (the merged-cell parts, whose 0.7 opacity is this file's single muted-secondary strength — the same strength the header colour should match);
    companion/layout.py `data_table()` around line 1068 (the server-side alternating-class computation this task deliberately leaves in place);
    companion/pages/history_page.py line 357 and companion/pages/airlines_page.py line 232 (the two other sites computing the same alternating class — also deliberately untouched)
  </read_first>
  <action>
    Implement D-07's header change in BOTH header rules in this single task. They carry equal specificity and the sticky one sits later in source order, so editing only the base rule leaves the visible header exactly as it is today — a filled block that reappears the moment a long table is scrolled. This is RESEARCH.md's Pitfall 1 and it is the one implementation trap in this phase that a screenshot-based review could not have caught.

    Base header rule: remove the `background` declaration entirely and declare `font-size: 11px;`, `font-weight: var(--weight-semibold);`, `letter-spacing: 0.06em;`, `text-transform: uppercase;`, `color: color-mix(in srgb, var(--color-text) 70%, transparent);`, `text-align: left;`, `padding: 0 var(--space-md) 10px;`, `border-bottom: 1px solid var(--color-border);`. The 70% muted strength is the same value plan 04 used for the theme segments and the same read as this file's existing 0.7 secondary opacity — one muted strength across the whole app, not a third one invented here.

    Sticky header rule: keep `position: sticky`, `top: 0` and `z-index: 1` exactly as they are — D-07 says the sticky positioning is unchanged, only restyled — and change its `background` from the elevated secondary surface to `var(--color-canvas)`. The declaration must stay (an opaque background is the only thing stopping body rows showing through a stuck header) but it must stop being the tinted elevated surface, or the quiet-label treatment above is defeated the instant the table scrolls. Record in that rule's comment both the source-order/specificity reason the two rules must always be edited together, and the token choice: canvas is chosen because History's table — the only table in the app long enough for the sticky state to actually engage — renders directly on the page background rather than inside a card. RESEARCH.md flags this specific token as the one value in the phase that was never live-validated; if the in-card tables read as a visibly mismatched strip against the white card surface, the primary card surface token is the equally-valid alternative and this is a one-declaration change. That comparison is an explicit item on the closing plan's checklist.

    Cell rule: change `padding` to `13px var(--space-md);` and keep the border-bottom separator exactly as it is.

    Add `.data-table tbody tr:last-child td { border-bottom: none; }` for D-07's clean bottom edge, and `.data-table tbody tr:hover { background: color-mix(in srgb, var(--color-text) 4%, transparent); }` for the hover-only row highlight — the 4% strength D-07 itself specifies, in the same idiom every other hover wash in this phase uses.

    Delete the alternating-row background rule. Deliberately leave the three server-side sites that compute and emit the alternating class name untouched: this phase's stated goal is minimal blast radius, the class becomes inert markup with no visual or functional effect, and removing it would mean three Python edits and three harness updates for zero user-visible gain. Record that decision in a comment where the deleted rule was, so a future reader sees an intentional choice rather than a half-finished refactor. Also rewrite the cell rule's own comment, whose entire explanation is about how the separator colour used to vanish against the alternating fill — a conflict that no longer exists.

    D-07a: change nothing about column content. No new pill, dot or badge treatment on any cell, no column added or removed, in either page module.
  </action>
  <acceptance_criteria>
    - The base header block declares `font-size: 11px`, `text-transform: uppercase`, `letter-spacing: 0.06em`, a 70% text `color-mix`, `padding: 0 var(--space-md) 10px` and a `var(--color-border)` bottom border, and declares no `background`.
    - The sticky header block still declares `position: sticky`, `top: 0` and `z-index: 1`, and its `background` names `var(--color-canvas)` rather than the elevated secondary surface.
    - With comments stripped, `var(--color-secondary)` appears in no rule whose selector contains `.data-table`.
    - The cell block declares `padding: 13px var(--space-md)`.
    - A last-child rule clearing the bottom border and a `tbody tr:hover` rule with a 4% text `color-mix` both exist.
    - With comments stripped, no `.data-table tr.row-alt` rule remains.
    - `git diff --stat` for this task shows zero changes under `companion/pages/`.
    - All five companion harnesses pass at their existing on-disk EXPECTED_CHECK_COUNT.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
th = block('.data-table th')
for needle in ('font-size: 11px', 'text-transform: uppercase', 'letter-spacing: 0.06em', 'color-mix(in srgb, var(--color-text) 70%', 'padding: 0 var(--space-md) 10px', 'border-bottom: 1px solid var(--color-border)'):
    assert needle in th, 'base header missing ' + needle
assert 'background' not in th, 'base header still fills a block'
sticky = block('.data-table-wrap th')
for needle in ('position: sticky', 'top: 0', 'z-index: 1', 'background: var(--color-canvas)'):
    assert needle in sticky, 'sticky header missing ' + needle
for m in re.finditer(r'\n(\.data-table[^{]*)\{([^}]*)\}', d):
    assert 'var(--color-secondary)' not in m.group(2), 'a table rule still paints the elevated surface: ' + m.group(1)
assert 'padding: 13px var(--space-md)' in block('.data-table td'), 'cell padding not increased'
assert 'border-bottom: none' in block('.data-table tbody tr:last-child td'), 'last row keeps a separator'
assert 'color-mix(in srgb, var(--color-text) 4%' in block('.data-table tbody tr:hover'), 'row hover tint missing'
assert '.data-table tr.row-alt' not in d, 'zebra stripe rule survives'
print('OK D-07/D-07a')
" && git diff --quiet -- companion/pages/ && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>Every table renders one surface, a quiet uppercase label row, roomier cells, a hover-only row cue and no trailing rule — and stays that way when the header sticks.</done>
</task>

<task type="auto">
  <name>Task 2: D-08 — filter bar input, count, and one shared Clear-control rule</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 1123-1156 (the filter bar container, the field wrapper, the absolutely-positioned search glyph, the input's current left-padding calculation, and the count rule);
    companion/static/style.css lines 795-817 (the global native input/select rule — read specifically for its 44px minimum, which this task must clear on the filter field only and must NOT change globally);
    companion/static/style.css lines 1274-1279 (the icon sizing rule, whose 20px default is what makes D-08's stated left padding collide with the glyph);
    companion/pages/history_page.py `_filter_bar_html()` (the Clear button element and its data attribute);
    companion/pages/airlines_page.py `_filter_bar_html()` and its docstring (the Clear link element, and the explicit constraint that this page carries no button or form element anywhere);
    companion/static/list-filter.js lines 30-40 and 88-96 (the Clear-control lookup is by data attribute and never inspects the element's tag name)
  </read_first>
  <action>
    Rewrite the filter input rule, scoped to the field wrapper's descendant input only — never the global native input rule, which keeps its 44px floor for every other form field in the app. Declare `height: 32px;`, `min-height: 0;`, `padding: 0 10px 0 26px;`, `font-size: 13px;`, `background: transparent;`, `border: 1px solid var(--color-border);`, and add a sibling `:focus` rule declaring `border-color: var(--color-accent);`. The `min-height: 0` is load-bearing and must be commented as such: the global rule's 44px minimum outranks any plain height regardless of specificity, so without clearing it the 32px value is inert and D-08 silently does nothing. This also replaces the rule's existing computed left-padding declaration, which D-08's literal value supersedes.

    D-08's 26px left padding assumes a smaller glyph than this codebase renders, so make it work rather than shipping text under the icon: add `width: 14px; height: 14px;` to the field wrapper's icon rule. The glyph is positioned 8px from the left edge, so at 14px it ends at 22px and the text starts at 26px with a 4px gap; at the current 20px it would end at 28px, two pixels past where the text begins. A 14px glyph is also the proportionate size for a 32px field with a 13px label. Record this as a planner adjustment made in service of D-08's own stated value, not a deviation from it. The rule outranks the base icon sizing rule and also beats the inline width/height attributes the icon builder emits, so no Python change is needed.

    Shrink the count rule to `font-size: 12px;` and give it the same 70% text `color-mix` muted colour the table header and theme segments use; keep its `white-space: nowrap`.

    Resolve D-08's Clear-control fork with a single rule targeting the existing data attribute both pages already emit: `.filter-bar [data-filter-clear]`. This is strictly better than adding a shared class, which is what CONTEXT.md and RESEARCH.md both assumed would be needed: the attribute is already on both elements, so the two genuinely converge on identical CSS with zero Python edits, zero markup change, zero risk to the filter script's element-agnostic lookup, and — the decisive point — zero risk of reopening Airlines' no-button-no-form constraint, whose regression check must keep passing completely unmodified. Its specificity comfortably outranks both the base button rule and the base anchor rule, and History's Clear is not a submit button so the primary-fill rule never reaches it.

    Declare on that rule: `height: auto;`, `min-height: 0;`, `min-width: 0;`, `padding: 0;`, `background: none;`, `border: none;`, `box-shadow: none;`, `border-radius: 0;`, `font-family: var(--font-ui);`, `font-size: 12px;`, `font-weight: var(--weight-regular);`, `letter-spacing: normal;`, `color: color-mix(in srgb, var(--color-text) 70%, transparent);`, `text-decoration: underline;`, `text-underline-offset: 2px;`, `cursor: pointer;`. The size, weight, letter-spacing and shadow resets are each undoing a specific declaration plan 02 gave the base button rule, so that the button variant and the link variant land on pixel-identical output. Add a hover rule on the same selector declaring `color: var(--color-text);`. Do not change either page's Clear element type.
  </action>
  <acceptance_criteria>
    - The filter input rule declares `height: 32px`, `min-height: 0`, `padding: 0 10px 0 26px`, `font-size: 13px`, `background: transparent` and a `var(--color-border)` border, and a matching `:focus` rule declares `border-color: var(--color-accent)`.
    - The global native input/select rule still declares `min-height: 44px`.
    - The field wrapper's icon rule declares `width: 14px` and `height: 14px`.
    - The count rule declares `font-size: 12px` and a 70% text `color-mix` colour.
    - A `.filter-bar [data-filter-clear]` rule exists declaring `background: none`, `border: none`, `box-shadow: none`, `text-decoration: underline`, `text-underline-offset: 2px` and a 70% text `color-mix` colour, plus a hover rule on the same selector.
    - `git diff --stat` for this task shows zero changes under `companion/pages/` and none to `companion/static/list-filter.js`.
    - Airlines' rendered output still contains no button and no form element, and History's still contains exactly one of each filter data marker — both asserted by the existing, unmodified checks in the status-pages and view-pages harnesses.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
inp = block('.filter-bar__field input')
for needle in ('height: 32px', 'min-height: 0', 'padding: 0 10px 0 26px', 'font-size: 13px', 'background: transparent', 'border: 1px solid var(--color-border)'):
    assert needle in inp, 'filter input missing ' + needle
assert 'var(--color-accent)' in block('.filter-bar__field input:focus'), 'focus edge missing'
g = d[d.index('\ninput,'):]
assert 'min-height: 44px' in g[:g.index('}')], 'global native input floor was disturbed'
ic = block('.filter-bar__field .icon')
assert 'width: 14px' in ic and 'height: 14px' in ic, 'glyph would overlap the input text'
cnt = block('.filter-bar__count')
assert 'font-size: 12px' in cnt and 'color-mix(in srgb, var(--color-text) 70%' in cnt, 'count not muted/shrunk'
cl = block('.filter-bar [data-filter-clear]')
for needle in ('background: none', 'border: none', 'box-shadow: none', 'text-decoration: underline', 'text-underline-offset: 2px', 'color-mix(in srgb, var(--color-text) 70%'):
    assert needle in cl, 'clear control missing ' + needle
assert 'color: var(--color-text)' in block('.filter-bar [data-filter-clear]:hover'), 'clear hover missing'
print('OK D-08')
" && git diff --quiet -- companion/pages/ companion/static/list-filter.js && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>The filter bar is a 32px transparent field with a proportionate glyph, a small muted count, and a Clear control that reads as the same quiet text link on both pages — achieved with no markup change on either.</done>
</task>

<task type="auto">
  <name>Task 3: drift guard for the shared Clear-control contract</name>
  <files>companion/test_view_pages.py</files>
  <read_first>
    companion/test_view_pages.py lines 744-767 (the existing filter-marker check — the new check belongs beside it and must follow the same `check(name, fn)` shape, tmpdir seeding and cleanup idiom);
    companion/test_view_pages.py lines 620-655 (the existing style.css drift-guard check — copy its comment-free file-read and class-lookup pattern);
    companion/test_view_pages.py line 61 (the EXPECTED_CHECK_COUNT constant — read its REAL current on-disk value and its running-total comment convention; do not trust any number written into this plan);
    companion/static/style.css as edited by Task 2 (the shared Clear-control selector)
  </read_first>
  <action>
    Add one new check to `companion/test_view_pages.py`, placed immediately after the existing filter-marker check and following the same construction idiom. It pins the three-way contract Task 2's zero-Python-change resolution depends on, which nothing currently guards: that History's rendered filter bar still carries an element with the Clear data attribute, that the attribute-based selector styling it is present in `companion/static/style.css`, and that the stylesheet contains no Clear-control rule keyed on a class name instead — because a future author adding such a class to one page and not the other is exactly how the two Clear controls would silently diverge again. Read the stylesheet with comments stripped, using the same regular-expression comment removal the app harness's single-error-token check already establishes, so explanatory prose can never satisfy or trip the assertion.

    Then bump `EXPECTED_CHECK_COUNT` and extend its running-total comment in the file's existing style. Compute the new value from the file's real on-disk baseline plus the one check you added, at execution time — every prior phase in this project records the same instruction, and a number written into a plan at planning time is stale by the time it is read.

    Do not add a matching check to the status-pages harness: Airlines' half of the contract is already covered by that harness's existing no-button-no-form check, which this plan requires to pass completely unmodified and which would be weakened, not strengthened, by a parallel assertion.
  </action>
  <acceptance_criteria>
    - `companion/test_view_pages.py` gains exactly one new `check(...)` call.
    - The new check reads `companion/static/style.css` with comments stripped and asserts the attribute-based Clear selector is present.
    - `EXPECTED_CHECK_COUNT` is bumped by exactly the number of checks added, computed from the file's real on-disk baseline at execution time, with its running-total comment extended.
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0.
    - `git diff --stat` across this plan's three commits touches exactly `companion/static/style.css` and `companion/test_view_pages.py`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_companion_app.py && git diff --quiet -- companion/pages/ companion/layout.py && echo 'page modules and layout untouched'</automated>
  </verify>
  <done>The shared Clear-control contract is machine-enforced rather than resting on the fact that both pages happen to emit the same attribute today.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — plan 05

**New Python symbols:** none. The only Python file touched is a test harness, which gains one nested check function inside the existing suite body — no module-level constant, no exported name. Both page modules and the filter script are covered by empty-diff gates.
**New CSS class names:** none — and this is the load-bearing part of D-08's resolution. The Clear control converges through `[data-filter-clear]`, an attribute both pages already emit, rather than through a new shared class that would have required editing two page modules.
**New design tokens:** none.

**New CSS rules (state and attribute rules on existing structures only):**
- `.data-table tbody tr:last-child td`
- `.data-table tbody tr:hover`
- `.filter-bar__field input:focus`
- `.filter-bar [data-filter-clear]`
- `.filter-bar [data-filter-clear]:hover`

**Removed:** the `.data-table tr.row-alt` rule. The server-side class computation that emits `row-alt` is deliberately retained at all three sites and becomes inert markup — a documented decision, not an incomplete refactor.

**Modified in place:** `.data-table th`, `.data-table-wrap th`, `.data-table td`, `.filter-bar__field .icon`, `.filter-bar__field input`, `.filter-bar__count`, and the two comment blocks those changes falsify.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→static asset | `GET /static/style.css`, pre-auth by design; property values only |
| read-only page constraint (Airlines) | A prior decision forbids any button or form element on Airlines; D-08's Clear-control reconciliation is the one change in this phase that could plausibly reopen it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4-05-01 | Elevation of Privilege | Airlines' read-only-by-design constraint | high | mitigate | Converting Airlines' Clear link into a button, or introducing a form to style it, would put a submitting control on a page whose whole contract is that it exposes no state-changing affordance. Mitigated by construction: the reconciliation targets the data attribute both pages already emit, so `companion/pages/airlines_page.py` is not in `files_modified` at all, enforced by an empty-diff gate on the pages directory in two separate task verify commands, and the existing no-button-no-form regression check must pass completely unmodified |
| T-06.6.4-05-02 | Tampering | `companion/static/list-filter.js` Clear handler | medium | mitigate | Changing History's Clear from a button to a link (the alternative D-08 offers) would risk the script's click wiring. Avoided entirely — no element type changes, and the script file is covered by the same empty-diff gate. The script's lookup is by data attribute and never inspects a tag name, verified during `read_first` |
| T-06.6.4-05-03 | Repudiation | sticky-header opacity | low | mitigate | A see-through stuck header would let scrolled rows bleed into the label row and let an operator misread which column a value belongs to. The sticky rule keeps an explicit opaque background; only the token changes. Verified by the acceptance criteria and re-checked on a long, populated History table at the closing checkpoint |
| T-06.6.4-05-04 | Tampering | rendered markup | low | accept | Zero page/layout modules in `files_modified`; the only Python file touched is a test harness |
| T-06.6.4-SC | Tampering | package-manager installs | low | accept | Zero installs; RESEARCH.md records the phase's Package Legitimacy Audit as not-applicable |
</threat_model>

<verification>
1. All three task gates pass.
2. `git diff --quiet -- companion/pages/ companion/layout.py companion/static/list-filter.js` succeeds across the whole plan.
3. `scripts/run-all-tests.sh` is green apart from the single pre-existing, accepted `server/test_poll_loop.py` macOS-local digest mismatch (this plan touches zero `server/` files).
4. The view-pages `EXPECTED_CHECK_COUNT` was computed from its real on-disk baseline at execution time and both before/after values are recorded in the SUMMARY.
</verification>

<success_criteria>
- Table headers read as labels, not blocks, static and stuck alike.
- One row surface plus a hover cue; no stripes; no trailing rule.
- A 32px filter field whose glyph and text do not collide.
- One Clear-control treatment across two different element types, with zero markup change and the read-only page constraint provably intact.
</success_criteria>

<output>
Create `.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-05-SUMMARY.md` when done. Record explicitly: the attribute-selector resolution of D-08's fork and why it beat the shared-class option, the sticky-header token choice and its open alternative, the decision to leave the alternating-row class computation in place, and the real before/after `EXPECTED_CHECK_COUNT` values.
</output>
