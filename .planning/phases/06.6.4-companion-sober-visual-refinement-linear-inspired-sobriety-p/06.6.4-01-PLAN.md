---
phase: 06.6.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/static/style.css
autonomous: true
requirements: [D-03, D-03a, D-09]

must_haves:
  truths:
    - "Every card component renders a hairline --color-border edge at rest with no resting shadow; the shadow appears only on hover/focus-within (D-03)."
    - "A selected runway card keeps its 2px accent border even while hovered — the hover treatment can never reach it (D-03a)."
    - "In explicit dark mode (data-ui-theme=\"dark\") a hovered card's border genuinely clears; no higher-specificity theme override pins it back on."
    - "The Emphasis role (Body size + semibold) replaces the retired 24px display size on the two values that used it (D-09)."
    - "The stylesheet's typography scale is back to exactly 4 sizes, matching its own header comment."
  artifacts: []
  key_links:
    - "The 6 card selectors and the 2 former dark-mode card-edge override blocks are one interlocking set — the overrides must be removed in the same edit that gives the base rules a real border, or hover cannot clear it in explicit dark mode."
    - ".runway-card's hover rule is scoped with :not(.runway-card--selected) so it is structurally incapable of overriding the selected state, independent of source order."
---

<objective>
Replace 06.6.1's shadow-at-rest card treatment with a hairline border at rest plus a hover/focus-within shadow reveal across all six card components (D-03, D-03a), and retire the 24px display size in favour of the Emphasis role (Body size + semibold) on its two consumers (D-09).

Purpose: This is the structural half of the phase's "sober, Linear-inspired" direction — permanent relief on every surface is what made the shipped UI read as busy. It lands first because every later plan (banners, nav, tables) styles components that sit inside or beside these cards, and because D-09's Emphasis role is the type precedent D-06 and D-07's later `--weight-semibold` label treatments reuse.
Output: `companion/static/style.css` — 6 card rules restyled, 4 new hover/focus-within rules, 2 obsolete dark-mode override blocks removed, 2 font-size consumers retargeted, 1 token retired, and every superseded explanatory comment rewritten rather than left describing the opposite of what the file now does.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-CONTEXT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md
@companion/static/style.css

<!-- .planning/STATE.md is deliberately NOT referenced by any plan in this phase:
     it is ~513 lines / ~93k tokens of prose session history and would consume
     most of an executor's context budget on its own. Everything this phase
     needs from it (the EXPECTED_CHECK_COUNT precedent, the accepted
     server/test_poll_loop.py exception, the requirements.mark-complete
     not_found precedent) is restated in 06.6.4-RESEARCH.md and in this plan. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-09 — retire the display size, move both consumers to the Emphasis role</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 65-96 (the :root typography scale block and its "exactly 4 sizes, 2 weights" comment);
    companion/static/style.css lines 327-337 (the .site-title comment, whose closing sentence attributes the display size to two named rules);
    companion/static/style.css lines 956-960 (.runway-card__number);
    companion/static/style.css lines 1302-1306 (.stat-tile__value);
    .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md ("D-09: the two --font-display-size consumers" code example — RESEARCH verified by grep that these are the only two consumers in the whole file)
  </read_first>
  <action>
    Implement D-09. In `.runway-card__number` and `.stat-tile__value`, change the `font-size` declaration from the display-size token to `var(--font-body-size)`. Leave `font-weight: var(--weight-semibold)` and `line-height: 1.2` exactly as they are in both rules — the Emphasis role is precisely "an existing size, made bold", and the weight half is already correct.

    Then delete the `--font-display-size: 24px;` declaration from the `:root` block (line 80). It is the only one of the phase's "planner's call" token questions with a positive reason to resolve toward removal rather than retention: the scale block's own comment above it claims exactly four sizes while five were declared, and retiring this one makes that comment literally true again (label 14 / body 16 / heading 20 / page-title 30). This also matches the file's own established precedent of deleting superseded rules outright rather than leaving unreferenced bloat (see the `.runway-option` removal note in the `.runway-card` comment, and 06.6.1-05's deletion of the horizontal nav renderer).

    Rewrite the closing sentence of the `.site-title` comment block, which currently asserts that the retired token "now belongs to" two named rules. State instead that both of those rules moved to the Emphasis role in 06.6.4 (D-09) — Body size plus semibold, the same size-that-already-exists-made-bold combination the table header rule uses — and that the display-size token was retired with them, restoring the four-size scale. Do not restate the token name anywhere as a live declaration; a comment mentioning it is fine and expected (every gate in this phase strips comments before asserting).

    Do not touch `--font-page-title-size`, `--font-heading-size`, `--font-label-size`, `--font-body-size`, the weight tokens, or any serif rule (D-10/D-11).
  </action>
  <acceptance_criteria>
    - With CSS comments stripped, the string `--font-display-size` does not appear anywhere in `companion/static/style.css` (neither as a declaration nor as a `var()` reference).
    - The `.stat-tile__value` rule block contains `var(--font-body-size)` and `var(--weight-semibold)`.
    - The `.runway-card__number` rule block contains `var(--font-body-size)` and `var(--weight-semibold)`.
    - With comments stripped, exactly four `--font-*-size` tokens are declared in `:root`.
    - `git diff --stat` for this task touches only `companion/static/style.css`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
assert 'font-display-size' not in d, 'retired token still live in declarations'
for sel in ('.stat-tile__value', '.runway-card__number'):
    b = block(sel)
    assert 'var(--font-body-size)' in b, sel + ' is not on the Body size'
    assert 'var(--weight-semibold)' in b, sel + ' lost its semibold weight'
root = d[d.index(':root {'):d.index('}', d.index(':root {'))]
sizes = re.findall(r'--font-[a-z-]*size', root)
assert len(sizes) == 4, 'expected exactly 4 size tokens, got %r' % sizes
print('OK D-09')
"</automated>
  </verify>
  <done>Both former display-size consumers render at Body size with semibold weight; the token is gone from the declarations; the scale block's own four-size claim is true again.</done>
</task>

<task type="auto">
  <name>Task 2: D-03/D-03a — hairline at rest, shadow on hover, across all six cards</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 930-954 (.runway-card, its comment, and .runway-card--selected);
    companion/static/style.css lines 1082-1092 (.history-card and its comment);
    companion/static/style.css lines 1196-1228 (.stat-tile, its two comment blocks including the 06.6.1 D-02 "traded for a resting shadow" paragraph, and the existing .stat-tile hover/focus-within rule);
    companion/static/style.css lines 1321-1337 (.battery-trend-section and its "two deliberate omissions" comment);
    companion/static/style.css lines 1352-1385 (the two dark-mode card-edge override blocks and the comment explaining why they enumerate all six selectors);
    companion/static/style.css lines 1404-1435 (.page-section and its comment);
    companion/static/style.css lines 1663-1688 (.login-shell / .login-card and its comment);
    companion/static/style.css lines 91-96 (the --radius-card / --radius-control token definitions);
    .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-RESEARCH.md (Pattern 2 — why the card set is 6 selectors not 4; Pattern 3 — why --radius-card itself must not be redefined)
  </read_first>
  <action>
    Implement D-03 and D-03a across the six card selectors RESEARCH.md verified share the identical resting treatment: `.runway-card`, `.history-card`, `.stat-tile`, `.battery-trend-section`, `.page-section`, `.login-card`. This is the codebase's own existing six-member card set — the same list the two dark-mode override blocks already enumerate verbatim — not four named selectors plus improvised additions. `.login-card` is an explicit, deliberate inclusion (it shares 100% of the pattern; excluding it would leave the pre-auth page as the one card in the app still carrying resting relief); flag it as such in the SUMMARY.

    In each of the six rules: change the transparent 1px border to `1px solid var(--color-border)`; change the resting `box-shadow` to `none`; and change `border-radius` from the card-radius token to `var(--radius-control)`. Use the control-radius token rather than a bare `8px` literal — it already equals 8px, and per RESEARCH Pattern 3 the card-radius token must NOT itself be redefined, because it is also consumed by the mobile-nav dropdown corners, the dirty bar and the preview frame, none of which are in D-03's scope. Leave those three consumers, and the card-radius token itself, untouched.

    Add the hover/focus-within reveal. `.stat-tile` already has a combined hover/focus-within rule with a shadow swap and a translate — extend that existing rule with `border-color: transparent;` rather than writing a second one. For `.page-section`, `.history-card` and `.login-card`, add new `:hover, :focus-within` rules declaring `border-color: transparent;` and `box-shadow: var(--shadow-card-hover);` only (no transform — the lift is a stat-tile affordance, not a global one). For `.battery-trend-section`, add the same shadow/border-color reveal but no transform, and amend that rule's own "two deliberate omissions" comment so it still reads correctly: the transform omission stands and is still deliberate (it holds an interactive chart, and moving the container under a pointer aimed at a small chart target is unhelpful), while the shadow reveal is new in 06.6.4 and changes no geometry.

    For `.runway-card`, the hover rule MUST be written as `.runway-card:not(.runway-card--selected):hover, .runway-card:not(.runway-card--selected):focus-within`. A bare `.runway-card:hover` carries the same specificity as `.runway-card--selected` and would clear the selected card's 2px accent border on hover — the exact signal D-03a says is unaffected. The `:not()` form cannot match a selected card at all, so the guarantee holds independent of source order rather than depending on where the rule is placed. Leave `.runway-card--selected` itself completely unmodified.

    Then delete both dark-mode card-edge override blocks outright — the `@media (prefers-color-scheme: dark)` one and the `html[data-ui-theme="dark"]` one — and replace them with a comment recording why. Two reasons, both load-bearing: they are now redundant (the base rules carry a real `--color-border` hairline in every theme, which is what they existed to synthesize for dark mode only), and — the reason they cannot merely be left in place — the attribute-scoped variant outranks a bare `.stat-tile:hover` on specificity, so leaving it would pin the border back on during hover for any user who has explicitly chosen dark mode via the theme picker, while `prefers-color-scheme` users saw the correct behaviour. That silent per-theme divergence is precisely the class of bug this file's comments exist to prevent.

    Finally, rewrite every comment this change falsifies rather than leaving it describing the opposite of the code. At minimum: the `.stat-tile` paragraph that instructs a future reader never to restore a border on top of the shadow (06.6.1's D-02 direction, which this phase deliberately reverses — say so, and say that the status-coloured top border still coexists as a separate property, unchanged); the `.page-section` paragraph explaining why it carries a transparent border and a resting shadow; the `.runway-card` and `.history-card` sentences about reserving a width for the now-deleted dark-mode override; and the `.login-card` sentence making the same claim. Each rewrite should state the new contract (hairline at rest, shadow on interaction) and name 06.6.4 D-03 as the supersession, in the same voice the rest of the file uses.
  </action>
  <acceptance_criteria>
    - With comments stripped, each of the six card rule blocks contains `border: 1px solid var(--color-border)`, `box-shadow: none`, and `border-radius: var(--radius-control)`.
    - With comments stripped, no rule block among the six declares `var(--shadow-card)` as a resting shadow.
    - A hover/focus-within rule exists for each of the six selectors and declares `var(--shadow-card-hover)`.
    - The runway-card hover selector is `:not()`-scoped against the selected modifier; `.runway-card--selected`'s own block is byte-identical to before this task.
    - With comments stripped, neither `html[data-ui-theme="dark"] .stat-tile` nor a dark-media card-edge override block remains anywhere in the file.
    - The card-radius token is still declared and still consumed by the mobile-nav, dirty-bar and preview-frame rules (3 remaining consumers).
    - `server/.venv/bin/python3 companion/test_companion_app.py` and `server/.venv/bin/python3 companion/test_status_pages.py` both pass at their existing on-disk EXPECTED_CHECK_COUNT — no class names change in this task, so the DOM-contract drift guards must pass unmodified.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
def block(sel):
    i = d.index('\n' + sel + ' {')
    return d[i:d.index('}', i)]
cards = ('.runway-card', '.history-card', '.stat-tile', '.battery-trend-section', '.page-section', '.login-card')
for sel in cards:
    b = block(sel)
    assert 'border: 1px solid var(--color-border)' in b, sel + ' has no hairline'
    assert 'box-shadow: none' in b, sel + ' still has a resting shadow'
    assert 'border-radius: var(--radius-control)' in b, sel + ' radius not tightened'
    assert 'var(--shadow-card)' not in b, sel + ' still references the resting shadow token'
for sel in cards:
    assert re.search(re.escape(sel) + r'[^{]*:(hover|focus-within)[^{]*\{[^}]*shadow-card-hover', d), sel + ' has no hover reveal'
assert ':not(.runway-card--selected):hover' in d, 'runway hover is not :not()-scoped'
assert 'html[data-ui-theme=\"dark\"] .stat-tile' not in d, 'obsolete dark card-edge override survives'
assert d.count('var(--radius-card)') == 3, 'card-radius consumers changed: %d' % d.count('var(--radius-card)')
print('OK D-03/D-03a')
" && server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>All six cards read as hairline-bounded surfaces at rest and reveal a shadow on hover or focus-within, in both themes and in both theme-selection mechanisms; a selected runway card keeps its accent border under the pointer; no comment in the file still describes the superseded shadow-at-rest contract as current.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — plan 01

**New Python symbols:** none.
**New CSS class names:** none. Every selector below targets a class that already exists.

**New CSS rules (state rules on existing selectors only):**
- `.page-section:hover, .page-section:focus-within`
- `.history-card:hover, .history-card:focus-within`
- `.login-card:hover, .login-card:focus-within`
- `.battery-trend-section:hover, .battery-trend-section:focus-within`
- `.runway-card:not(.runway-card--selected):hover, .runway-card:not(.runway-card--selected):focus-within`

**Removed:**
- the `--font-display-size` custom property (declaration and both `var()` references)
- the `@media (prefers-color-scheme: dark)` card-edge override block
- the `html[data-ui-theme="dark"]` card-edge override block

**Modified in place:** the six card rules, `.stat-tile:hover, .stat-tile:focus-within`, `.stat-tile__value`, `.runway-card__number`, and the six explanatory comment blocks those changes falsify.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→static asset | `GET /static/style.css` is served pre-auth (deliberately, so the login page is styled); this plan changes only property values inside that already-public file |
| server→rendered HTML | Untouched by this plan — zero Python files are modified, so no escaping call site, route, or session gate is reached |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4-01-01 | Information Disclosure | `companion/static/style.css` (pre-auth readable) | low | accept | The file already contains the full design system and is already served unauthenticated by design; this plan adds no selector that reveals authenticated content structure beyond what 06.6.2/06.6.3 already published |
| T-06.6.4-01-02 | Tampering | rendered markup | low | accept | Zero Python files in `files_modified`; every `escape_html()` call site and every markup builder is untouched, so no new user-influenced data reaches the DOM. Enforced by the `git diff --stat` acceptance criterion |
| T-06.6.4-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. RESEARCH.md's Package Legitimacy Audit section records the phase as not-applicable (zero external packages); there is no `[ASSUMED]`/`[SUS]` entry to gate |
</threat_model>

<verification>
1. Both task gates above pass.
2. `server/.venv/bin/python3 companion/test_contrast_check.py`, `companion/test_view_pages.py` and `companion/test_config_page.py` all still pass at their existing on-disk counts (no `EXPECTED_CHECK_COUNT` bump is expected from this plan — it adds no checks).
3. `git diff --stat` across this plan's commits touches exactly `companion/static/style.css`.
4. `grep -c 'url(' companion/static/style.css` returns 0 — the file's zero-external-reference constraint is untouched.
</verification>

<success_criteria>
- Six card components carry a hairline at rest and a shadow only on hover/focus-within.
- The selected-runway signal survives hover in every theme.
- Explicit dark mode and `prefers-color-scheme` dark behave identically on hover.
- The typography scale is back to four sizes with the Emphasis role carrying both former display-size values.
- No comment in `style.css` still asserts the superseded shadow-at-rest contract.
</success_criteria>

<output>
Create `.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-01-SUMMARY.md` when done. Record explicitly: the `.login-card` inclusion decision, the token-retirement decision, and the dark-mode-override removal with its specificity rationale.
</output>
