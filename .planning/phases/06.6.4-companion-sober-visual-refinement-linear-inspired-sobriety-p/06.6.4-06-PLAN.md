---
phase: 06.6.4
plan: 06
type: execute
wave: 6
depends_on: ["06.6.4-01", "06.6.4-02", "06.6.4-03", "06.6.4-04", "06.6.4-05"]
files_modified:
  - .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-VALIDATION.md
autonomous: false
requirements: [D-10, D-11]

must_haves:
  truths:
    - "The typeface and every accent/status colour token are provably byte-identical to what the heading-color-consistency commit left them (D-10, D-11) — the phase delivered structural restraint, not a rebrand."
    - "The stylesheet still contains zero external references — no remote asset, no web font — after five plans of edits."
    - "The full composed suite is green apart from the single pre-documented, accepted macOS-local exception."
    - "The developer has personally seen every applied change in a real browser, in BOTH light and dark theme, and signed off — not a computed-style check standing in for that."
  artifacts:
    - ".planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-VALIDATION.md with a completed Per-Task Verification Map and a resolved Approval line"
  key_links:
    - "This project's own history records that structural and computed-style checks alone previously missed a real mobile navigation bug; the browser checkpoint is the control for that failure mode and must not be inferred, summarised, or answered by the executor."
---

<objective>
Close the phase: prove the two non-goals held (D-10, D-11) and the zero-external-reference constraint survived, run the full composed suite, complete the phase's validation record, and then obtain the developer's own real-browser sign-off across both themes before anything is marked complete.

Purpose: Every decision in this phase was validated against a standalone HTML mockup, not against the real rendered application — the mockup's DOM shape already proved to differ from the real renderer once (D-06a). This plan is where the applied result meets a real browser. This project's own memory records a case where computed-style and structural checks alone passed while a genuine visual bug shipped; that is precisely the failure mode this checkpoint exists to catch.
Output: an updated `06.6.4-VALIDATION.md`, a recorded full-suite result, and a developer verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-CONTEXT.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-VALIDATION.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-01-SUMMARY.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-02-SUMMARY.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-03-SUMMARY.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-04-SUMMARY.md
@.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: non-goal regression gate, full-suite gate, and validation record</name>
  <files>.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-VALIDATION.md</files>
  <read_first>
    .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-VALIDATION.md (the Per-Task Verification Map with its pending status column, the Manual-Only Verifications table, and the Approval line);
    all five of this phase's SUMMARY files (the real task list, the real EXPECTED_CHECK_COUNT before/after values, and every recorded deviation — the map must reflect what shipped, not what was planned);
    scripts/run-all-tests.sh (the canonical harness array — the single source of truth for what "full suite" means);
    companion/static/style.css lines 65-199 (the four token blocks) and the headings serif rule near the top (read only to confirm the gate below is asserting against the real current shape)
  </read_first>
  <action>
    Run the non-goal regression gate below. It is the only automated coverage D-10 and D-11 have, and it is genuinely load-bearing: five consecutive plans edited the same 1689-line stylesheet, and a token or serif-rule change slipped in among them is exactly the kind of drift that no other check in this repository would catch. It asserts that each accent, accent-hover and status token still declares the exact value the heading-color-consistency commit left it at, in all four token blocks; that the headings selector still includes the legend element (the specific fix that commit shipped); that the serif family token is unchanged; and that the file still contains no external reference of any kind — no remote asset, no font-face declaration — which is a standing constraint of this file stated in its own header.

    Then run `scripts/run-all-tests.sh` against the fully composed five-plan worktree. Exactly one non-zero harness is acceptable: the pre-documented `server/test_poll_loop.py` macOS-local Pillow/FreeType panel digest mismatch. Confirm it is genuinely unrelated by verifying that this phase touched zero files under `server/` — true for all five plans by construction — and record the confirmation rather than asserting it. Any other failure is a real regression and must be fixed before proceeding to Task 2. Record the coverage percentage and confirm it clears the configured threshold.

    Rewrite `06.6.4-VALIDATION.md`'s Per-Task Verification Map so it lists the real tasks that shipped across plans 01 through 05 plus this plan's own two, one row each, with the decision ID, the behaviour, the test type, the automated command actually run, whether the file existed, and a resolved status. Tick the Sign-Off checklist items that the real plans now satisfy, and leave the final Approval line unresolved until Task 2 returns a verdict. Also record, in the Manual-Only Verifications table, the three specific items this phase deliberately deferred to human judgment: the D-05a nav-weight fork, the sticky-header surface-token choice, and the icon-button hit-area behaviour under a real pointer.
  </action>
  <acceptance_criteria>
    - The non-goal gate passes: every accent/status token value appears exactly twice (once per light block, once per dark block), the headings selector still includes the legend element, the serif token is unchanged, and the file contains zero external references.
    - `scripts/run-all-tests.sh` reports every harness green except the single pre-documented `server/test_poll_loop.py` digest mismatch, and `git diff --stat` over the phase's commits confirms zero files under `server/` were touched.
    - Coverage is at or above the threshold configured in `pyproject.toml`.
    - `06.6.4-VALIDATION.md`'s Per-Task Verification Map contains a row per shipped task with a resolved status; the Approval line remains pending.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import re
css = open('companion/static/style.css').read()
d = re.sub(r'/\*.*?\*/', '', css, flags=re.S)
frozen = {
 '--color-accent: #B13F16': 2, '--color-accent-hover: #963610': 2,
 '--color-status-ok: #16A34A': 2, '--color-status-warn: #D97706': 2, '--color-status-error: #BE123C': 2,
 '--color-accent: #FF8A5C': 2, '--color-accent-hover: #FF9E77': 2,
 '--color-status-ok: #4ADE80': 2, '--color-status-warn: #FBBF24': 2, '--color-status-error: #FB7185': 2,
}
for decl, n in frozen.items():
    got = d.count(decl)
    assert got == n, 'D-11 violated: %r declared %d times, expected %d' % (decl, got, n)
assert '--font-serif: Georgia,' in d, 'D-10 violated: the serif family token changed'
i = d.index('.text-heading {')
sel = d[d.rindex('\n', 0, d.rindex('{', 0, i + 1)) - 200:i]
assert 'legend' in sel, 'D-11 violated: the headings serif selector lost the legend element'
assert 'url(' not in d and '@font-face' not in d, 'zero-external-reference constraint broken'
print('OK D-10/D-11 non-goals held')
" && scripts/run-all-tests.sh; echo "full-suite exit: $? (one accepted failure: server/test_poll_loop.py digest mismatch)"</automated>
  </verify>
  <done>Both non-goals are machine-proven intact, the composed suite is green apart from the one accepted exception, and the phase's validation record reflects what actually shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: blocking developer real-browser verification, light AND dark</name>
  <files>none — verification only, no file is written by this task</files>
  <read_first>
    all five of this phase's SUMMARY files (so the checklist below can be annotated with any deviation actually recorded during execution before it is presented);
    .planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-CONTEXT.md (the Explicitly OUT OF SCOPE list — three of those four deferred items are visible on the same screens the developer is about to inspect, and must be named as known-deferred rather than treated as new findings)
  </read_first>
  <action>
    Do not implement anything in this task. Start the companion service locally so the developer can reach it, then present the verification checklist below verbatim and stop. Wait for the developer's own reply.

    Before presenting, do two things. First, annotate any checklist item whose corresponding plan recorded a deviation in its SUMMARY, so the developer is inspecting what actually shipped rather than what was planned. Second, state up front which known issues are deliberately out of this phase's scope and should NOT be reported as failures: Config's runway-card grid misalignment, Health's anomaly-banner detail-text redundancy (a genuine unresolved conflict between two prior decisions, needing a developer decision rather than CSS), Airlines' empty-state rendering above real data, and the nested horizontal scroll inside Airlines' unresolved-prefix card. All four are already queued for the page-by-page pass the developer asked for after this phase closes.

    This checkpoint must not be answered, inferred, summarised or self-approved by the executor under any circumstances, and `auto_advance` does not apply to it. Two items in particular (17 and 20) are open decisions the plan deliberately did not make; a verdict that does not answer them is incomplete, not an approval.
  </action>
  <what-built>
    Five plans of applied visual refinement, all in `companion/static/style.css` plus one test harness each in plans 03 and 05, with zero changes to any page module, to `companion/layout.py`, or to `companion/static/list-filter.js`:

    - **Plan 01 (D-03, D-03a, D-09):** all six card components traded their resting shadow for a hairline border, with the shadow revealed only on hover/focus-within and corners tightened to 8px; the two obsolete dark-mode card-edge override blocks removed (they would have pinned the border back on during hover for explicit-dark-mode users); the 24px display size retired in favour of the Emphasis role.
    - **Plan 02 (D-01, D-01a, D-01b, D-01c, D-02):** text buttons 44px to 30px with 13px semibold labels and 6px corners; the primary action gained an inset highlight plus drop shadow; quiet buttons became near-invisible at rest with a hover reveal; every button depresses 1px on press; the copy button became a 28x28 glyph with a synthesized 44x44 hit area.
    - **Plan 03 (D-04, D-04a):** banners lost their colour wash for a neutral card surface, a hairline, a 3px coloured edge and a 6px dot; Health's combined card-and-banner block opted out of the flex row and the dot to keep its stacked layout.
    - **Plan 04 (D-05, D-05a, D-06, D-06a):** both nav renderers tightened to 32px Label-size links with a single tinted-pill active signal and a new hover wash; the theme picker became one shared segmented control, CSS-only.
    - **Plan 05 (D-07, D-07a, D-08):** table headers became quiet uppercase labels (both the base and the sticky rule), zebra striping replaced by a hover tint, cells roomier, last row unruled; the filter field tightened to 32px with a proportionate glyph, and both Clear controls converged on one rule.
  </what-built>
  <how-to-verify>
    Start the companion service locally and sign in. Work through the list below **twice — once with the theme picker set to Light and once set to Dark** (use the picker itself, not the OS preference, since plan 01 specifically removed the explicit-dark-mode override blocks and that path must be checked directly). Note any item that looks wrong rather than approving a whole group at once.

    **A. Cards (D-03) — Health, Config, History, Preview, and the login page after signing out**
    1. Every card sits inside a thin hairline with no shadow at rest.
    2. Hovering a stat tile reveals a shadow, clears the hairline, and lifts by 1px.
    3. Hovering a page section, a History mobile card and the login card reveals a shadow with no lift.
    4. On Config, hover a runway card: unselected cards reveal a shadow; **the selected card keeps its 2px accent border throughout** — this is the specific cascade risk plan 01 guarded against.
    5. Stat tiles keep their colour-coded top edge.

    **B. Buttons (D-01, D-01a, D-01b, D-02) — mainly Config, plus the sidebar**
    6. Save Settings reads as a lit accent chip with a subtle top highlight; hovering deepens it; pressing depresses it 1px.
    7. Sign out and the dirty-bar Cancel are near-invisible at rest and reveal a wash and a stronger edge on hover.
    8. Buttons at 30px still feel comfortably clickable — this is the WCAG AAA touch-target trade-off you accepted; say so now if it reads wrong on a trackpad or a phone.
    9. On History, the copy glyphs are small and quiet. **Click roughly 6px outside the visible glyph edge and confirm the copy still fires**, then confirm the Callsign and Hex copy buttons in the same cell never trigger each other.

    **C. Banners (D-04) — Config after saving, Health with a warning or anomaly**
    10. A save confirmation reads as a flat neutral strip with a 3px accent edge and a small accent dot, not a tinted region.
    11. Health's anomaly/warn banner shows its own colour on the edge and the dot only. **Does a genuine error still read as an alert at a glance?** If it now reads too quietly, say so — that is a legitimate finding, not a nitpick.
    12. Health's source-fault block (the section that is both a card and a banner) still renders as stacked heading-and-paragraphs, **not** squashed into one horizontal row, and shows no stray dot.

    **D. Navigation and theme picker (D-05, D-06)**
    13. Sidebar links are dense 32px pills; the current page is signalled by a tinted accent pill and nothing else — no left rule anywhere.
    14. Hovering a non-current link shows a faint neutral wash; **hovering the current link never replaces its accent tint**.
    15. Narrow the window below 960px, open the hamburger dropdown, and confirm the mobile links match the sidebar exactly; the dropdown opens and closes normally and no link is clipped.
    16. The theme picker is one bordered pill with three flush segments; the active one is a tinted accent pill; segments are evenly sized with no gap or stray border between them.
    17. **D-05a needs your decision.** Nav links currently use the existing two weights — regular when inactive, semibold when active — rather than the uniform 500 weight the reference artifact used. That choice preserves the two-weight type system closed earlier and matches the theme picker's own locked semibold active state. Approve it, or ask for the 500-weight token instead; either answer is fine, but it must be yours.

    **E. Tables and filter bar (D-07, D-08) — History and Airlines**
    18. Column headers read as small uppercase muted labels above a hairline, with no filled background.
    19. **Scroll a populated History table past its header** and confirm the header stays stuck, stays opaque (no rows bleeding through), and still reads as a quiet label rather than reverting to a filled block.
    20. **The sticky header's surface colour is the one value in this phase never validated against a real render.** On Airlines and Health, where tables sit inside a white card, does the stuck header read as a mismatched strip? If so, say so — it is a one-line change to the card surface token.
    21. Rows have no alternating stripes; hovering a row shows a faint tint; the last row has no line beneath it.
    22. The filter field is a slim transparent box; the search glyph sits clear of the typed text with no overlap.
    23. Clear reads as a quiet underlined text link on **both** History and Airlines and looks identical on the two pages; clicking it on each actually clears the filter.

    **F. Non-goals (D-10, D-11)**
    24. Headings are still the warm serif; the accent is still the same terracotta/coral; error, warning and ok signals are still their existing colours and still read apart from the accent. Nothing about this phase should feel like a rebrand.
  </how-to-verify>
  <verify>
    <human-check>The developer works through all 24 numbered items above in a real local browser, twice (theme picker set to Light, then to Dark), and replies with a verbatim verdict that explicitly answers items 17 (nav-link weight) and 20 (sticky-header surface token). No automated command can substitute for this: the applied spec was only ever validated against a standalone HTML mockup whose DOM shape already proved to differ from the real renderer once, and this project's own history records a real visual bug shipping past passing structural checks.</human-check>
  </verify>
  <done>The developer has replied with a verbatim verdict covering both themes, including explicit resolutions for the D-05a nav-weight fork and the sticky-header surface token; any failed items are recorded with what was seen.</done>
  <resume-signal>
    Reply with either "approved" (optionally noting the D-05a verdict from item 17 and the sticky-header verdict from item 20), or a list of the specific numbered items that failed and what you saw. Do not treat silence or a partial answer as approval — this checkpoint must be answered by the developer, in a real browser, in both themes, before `06.6.4-06-SUMMARY.md` is written or the phase is marked complete.
  </resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — plan 06

**New Python symbols:** none. **New CSS:** none. **New design tokens:** none.

**Files written:** `06.6.4-VALIDATION.md` (Per-Task Verification Map completed, Sign-Off items resolved) and, only after the checkpoint returns a verdict, `06.6.4-06-SUMMARY.md`.

## Phase-wide symbol inventory (all six plans)

This phase creates **zero new Python symbols, zero new CSS class names and zero new design tokens.** Every edit is a property-value change, a state/pseudo-element rule on a selector that already exists, or a deletion. The complete set of genuinely new selectors across the phase:

| Selector | Plan | Why it is new rather than a value change |
|----------|------|------------------------------------------|
| `.page-section:hover, :focus-within` + 3 sibling card hover rules | 01 | D-03 moves the shadow from rest to interaction; no card but the stat tile had an interaction state before |
| `.runway-card:not(.runway-card--selected):hover, :focus-within` | 01 | `:not()`-scoped so it cannot erase D-03a's selected border |
| `button:hover`, `button:active` | 02 | D-01b's reveal and pressed state |
| `.copy-btn::before`, `.copy-btn:hover` | 02 | D-02's synthesized 44x44 hit area |
| `.banner::before` + 4 severity/opt-out `::before` rules | 03 | D-04's status dot, which has no markup element to hook |
| 3 `:not()`-scoped nav/theme hover rules | 04 | D-05/D-06's hover washes, scoped so they cannot outrank an active state |
| `.data-table tbody tr:last-child td`, `.data-table tbody tr:hover` | 05 | D-07's clean bottom edge and hover-only row cue |
| `.filter-bar__field input:focus`, `.filter-bar [data-filter-clear]`(+`:hover`) | 05 | D-08's focus edge and the shared Clear-control treatment |

**Deletions:** the `--font-display-size` token, two dark-mode card-edge override blocks (plan 01), and the zebra-stripe rule (plan 05).

**The one conditional artifact:** if the developer chooses option (a) at checkpoint item 17, a `--weight-medium: 500` token is added to `:root`. As planned and implemented, it does not exist.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact→repository | This plan writes only to a `.planning/` markdown file; no application code, no configuration, no deployment artifact |
| developer judgment→phase completion | The blocking checkpoint is the sole gate between the applied CSS and the phase being declared done |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4-06-01 | Repudiation | phase sign-off | high | mitigate | An executor inferring, summarising or self-answering the checkpoint would produce a recorded developer approval that never happened — the exact failure this project's own memory note about real-device verification warns against. Mitigated by `gate="blocking"`, by the resume signal explicitly forbidding inferred approval, and by requiring the verdict before the SUMMARY file may be created |
| T-06.6.4-06-02 | Tampering | design tokens across five sequential edits to one file | medium | mitigate | Five plans edited the same stylesheet in sequence; a token or serif-rule change slipping in would silently violate D-10/D-11 with no other check in the repository catching it. Task 1's automated gate pins every accent/status value in all four token blocks, the serif family, the legend element's membership in the headings selector, and the zero-external-reference constraint |
| T-06.6.4-06-03 | Denial of Service | interactive affordance reachability | medium | mitigate | The phase reduced several controls below the 44px AAA floor. Checkpoint items 8 and 9 verify real-pointer reachability, including the synthesized hit area, on real hardware rather than by computed style |
| T-06.6.4-SC | Tampering | package-manager installs | low | accept | Zero installs anywhere in this phase; RESEARCH.md records the Package Legitimacy Audit as not-applicable |
</threat_model>

<verification>
1. Task 1's non-goal gate exits 0.
2. `scripts/run-all-tests.sh` shows the single accepted exception and nothing else.
3. `06.6.4-VALIDATION.md` has a completed Per-Task Verification Map.
4. A verbatim developer verdict for Task 2 is recorded, including explicit answers to items 17 and 20.
</verification>

<success_criteria>
- D-10 and D-11 are machine-proven to have held across all five plans.
- The composed suite is green apart from the one pre-documented exception.
- The developer has personally verified the applied result in a real browser in both themes and given a verbatim verdict, and the two deferred judgment calls (nav weight, sticky-header surface) are resolved by that verdict.
</success_criteria>

<output>
Create `.planning/phases/06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p/06.6.4-06-SUMMARY.md` ONLY after the Task 2 checkpoint returns a developer verdict. Record the verdict verbatim, the D-05a and sticky-header resolutions, the full-suite result with its accepted exception, and any follow-up items raised during verification.

Note for whoever closes the phase: `requirements.mark-complete` will return `not_found` for every `D-NN` identifier in this phase's plans. That is expected and correct — these are `06.6.4-CONTEXT.md` Decision IDs, not `REQUIREMENTS.md` entries, matching every prior `06.6.x` decimal phase's precedent. `REQUIREMENTS.md` should be left untouched.
</output>
