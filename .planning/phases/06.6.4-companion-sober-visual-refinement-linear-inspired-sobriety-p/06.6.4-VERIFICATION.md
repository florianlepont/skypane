---
phase: 06.6.4-companion-sober-visual-refinement-linear-inspired-sobriety-p
verified: 2026-09-01T08:01:49Z
status: passed
score: 16/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 06.6.4: Companion sober visual refinement Verification Report

**Phase Goal:** Apply the already-fully-validated "sober, Linear-inspired" visual spec to the real companion codebase. Structural restraint only, not a rebrand: cards trade their resting shadow for a hairline with the shadow revealed on interaction; text buttons drop 44px→30px (a deliberate WCAG 2.5.5 AAA trade-off the developer accepted three times, still 2.5.8 AA-compliant) with an inset-highlighted primary and near-invisible quiet variants; icon buttons shrink to 28px while keeping a real 44px hit area via a pseudo-element (later shrunk again to 22px/14px-icon post-checkpoint per real developer feedback — the hit area stayed 44px); banners lose their colour wash for a neutral surface with a 3px edge and a status dot; both nav renderers tighten to 32px with a single tinted-pill active signal; the theme picker becomes one segmented control; tables lose zebra striping for a hover tint under quiet uppercase headers; and the filter bar tightens with both Clear controls converged on one rule.
**Verified:** 2026-09-01T08:01:49Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 6 cards (`.runway-card`, `.history-card`, `.stat-tile`, `.battery-trend-section`, `.page-section`, `.login-card`) render a hairline `--color-border` edge at rest, `box-shadow: none`, and reveal `--shadow-card-hover` only on `:hover`/`:focus-within` (D-03) | ✓ VERIFIED | `companion/static/style.css:1149-1176` (`.runway-card`), `:1351-1364` (`.history-card`), `:1556-1572` (`.stat-tile`), `:1681-1692` (`.battery-trend-section`), `:1755-1776` (`.page-section`), `:2045-2062` (`.login-card`) — all 6 selectors confirmed with the hairline/shadow-reveal pattern; both dark-mode override blocks confirmed absent (no `html[data-ui-theme="dark"] .stat-tile`-style overrides remain) |
| 2 | `.runway-card--selected` keeps its 2px accent border even while hovered (D-03a) | ✓ VERIFIED | `style.css:1168-1184` — hover rule is scoped `.runway-card:not(.runway-card--selected):hover`, structurally incapable of matching the selected modifier; `.runway-card--selected { border: 2px solid var(--color-accent); }` is a separate, higher-specificity rule |
| 3 | Text buttons render 30px/13px-semibold/6px-radius; primary gets an inset highlight, quiet buttons are near-invisible until hover, all buttons press 1px (D-01, D-01a, D-01b) | ✓ VERIFIED | `style.css:930-995` — base `button` has no `min-height`/`min-width`, declares `height: 30px; padding: 0 12px; font-size: 13px; font-weight: var(--weight-semibold); letter-spacing: -0.01em; border-radius: 6px`; `button[type="submit"]` carries `box-shadow: inset 0 1px 0 rgba(255,255,255,.14), 0 1px 2px rgba(18,21,27,.14)` (hover variant deeper); `button:active { transform: translateY(1px) }` |
| 4 | The 44px touch-target floor genuinely remains for the hamburger toggle, native `input`/`select`, and disclosure `summary` (D-01c) | ✓ VERIFIED | `style.css:898-913` (`input, select { min-height: 44px; min-width: 44px }` untouched); floor-register comment at `:915-929` documents exactly which selectors kept vs. gave up the floor |
| 5 | Icon-only buttons (`.copy-btn`) render as a small visual box with a real 44×44 hit area synthesized by an invisible `::before` pseudo-element (D-02) | ✓ VERIFIED | `style.css:1095-1125` — `.copy-btn { width: 22px; height: 22px; ... }`, `.copy-btn .icon { width: 14px; height: 14px }`, `.copy-btn::before { position: absolute; inset: -11px }` — 22 + 11×2 = 44, confirming the hit area math; post-checkpoint shrink from 28px→22px documented and applied (commit `ee7235a`) |
| 6 | Banners render as a near-neutral card surface with a hairline border, a 3px coloured left edge, and a small status dot instead of a colour wash; all three severities keep their own colour (D-04) | ✓ VERIFIED | `style.css:795-858` — `.banner { display:flex; background: var(--color-dominant); border: 1px solid var(--color-border); border-left: 3px solid var(--color-accent) }`, `.banner::before` 6×6px dot; `.banner--anomaly`/`--warn`/`--flash` each restate only `border-left-color`/`::before` background with their own existing colour token |
| 7 | Health's combined card-and-banner selector (source-fault block) keeps its stacked block layout and error edge, unaffected by the banner's new flex row/dot (D-04a) | ✓ VERIFIED | `style.css:1823-1830` — `.page-section.banner--anomaly { display: block; border-left: 3px solid var(--color-status-error); background: var(--color-dominant) }` plus `.page-section.banner--anomaly::before { display: none }` opt-out |
| 8 | Body text on the card surface every banner now renders on is proven WCAG AA in both themes by an automated check | ✓ VERIFIED | `companion/test_contrast_check.py` — `EXPECTED_CHECK_COUNT = 33` (was 31), new `live_pairs` entries for `--color-dominant` card surface; ran live: `contrast-check: 33/33 checks pass` |
| 9 | Both nav renderers (sidebar, mobile dropdown) render 32px links with a single tinted-pill active signal and no left rule, plus a `:not()`-scoped inactive hover (D-05) | ✓ VERIFIED | `style.css:485-517` (`.mobile-nav__link`/`--active`/hover), `:704-740` (`.sidebar-link`/`--active`/hover) — both at `height: 32px`, both `--active` modifiers use only `color-mix(in srgb, var(--color-accent) 12%, transparent)` + accent text + semibold (no `border-left`), both hover rules `:not()`-scoped against their own active modifier |
| 10 | The theme picker renders as one shared bordered segmented control with borderless pills, the active segment reusing the nav-pill language, with zero markup change (D-06, D-06a) | ✓ VERIFIED | `style.css:591-650` — `.theme-form` is the bordered container (`border`, `padding: 2px`, `gap: 2px`); `.theme-form .theme-option` is borderless/28px/flex; `.theme-form .theme-option--active` byte-identical to the nav active-pill declarations; `companion/layout.py:613-625` confirms `_theme_form_html()` already wraps all three buttons in one `<form class="theme-form">` — no Python edit was needed or made |
| 11 | Table headers read as quiet uppercase labels (static and sticky), zebra striping is gone in favour of a hover tint, and the last row has no trailing separator (D-07) | ✓ VERIFIED | `style.css:1243-1296` — `.data-table th` (11px/uppercase/letter-spacing/70%-muted/no background), `.data-table-wrap th` (sticky, background moved to `--color-canvas`), `.data-table tbody tr:hover` (4% tint), `.data-table tbody tr:last-child td { border-bottom: none }`; `tr.row-alt` rule confirmed removed (only a removal-rationale comment remains at the old location) |
| 12 | Table column content is unchanged — no new pills/dots/columns (D-07a) | ✓ VERIFIED | SUMMARY 05 documents `git diff --quiet -- companion/pages/` gate passing in both tasks; independently confirmed no pill/dot markup was added by grepping the table cell rules — only shell (header/row/border) selectors were touched |
| 13 | The filter input is 32px/transparent, and both History's `<button>` Clear control and Airlines' `<a>` Clear control render identically via one shared rule with zero Python changes; Airlines still has no `<button>`/`<form>` (D-08) | ✓ VERIFIED | `style.css:1443-1499` — `.filter-bar__field input { height: 32px; min-height: 0; background: transparent; ... }`; `.filter-bar [data-filter-clear]` undoes button defaults uniformly; `companion/pages/history_page.py:333` emits `<button type="button" data-filter-clear>`, `companion/pages/airlines_page.py:205` emits `<a href="#..." data-filter-clear>` — both unmodified by this phase, styled via the same attribute selector; live run confirms `view-pages: 42/42` and `status-pages: 59/59` including the Airlines no-button/no-form guard |
| 14 | `--font-display-size` is retired; `.stat-tile__value`/`.runway-card__number` render at Body size + semibold (Emphasis role) (D-09) | ✓ VERIFIED | `grep -n "font-display-size" style.css` returns only the retirement rationale comment, no live declaration; `.stat-tile__value` (`:1650`) and `.runway-card__number` (`:1186`) both use `var(--font-body-size)` + `var(--weight-semibold)` |
| 15 | Typeface and every accent/status colour token are unchanged from the `heading-color-consistency` commit; zero external stylesheet references remain (D-10, D-11 non-goals) | ✓ VERIFIED | `--color-accent`/`--color-status-ok`/`--color-status-warn`/`--color-status-error` values (`#B13F16`/`#16A34A`/`#D97706`/`#BE123C` light, `#FF8A5C`/`#4ADE80`/`#FBBF24`/`#FB7185` dark) match the commit's documented values in both `:root` and explicit `[data-ui-theme]` blocks; the serif+`legend` heading rule (`h1, h2, h3, legend, .text-heading { font-family: var(--font-serif) }`) is intact; `grep -n "http://\|https://\|url(" style.css` returns no matches |
| 16 | The developer personally verified the applied result in a real browser, both light and dark theme, across all 24 checklist items, and approved — with one real finding (copy button) fixed and re-verified | ✓ VERIFIED | `06.6.4-06-SUMMARY.md` records the developer's verbatim checkpoint verdict ("A - validé", "B - Validé", "C - validé", "D - validé...", "E. - ...", "F - validé") and the copy-button finding's fix/re-verification (commit `ee7235a`); `06.6.4-VALIDATION.md`'s Approval line: "approved 2026-09-01 ... All 24 checklist items validated across both themes; D-05a resolved (kept option (b)); sticky-header surface token confirmed correct; one finding (copy button size) fixed post-verdict and re-verified." |

**Score:** 16/16 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `companion/static/style.css` | All 11 CSS surfaces (cards, buttons, icon buttons, banners, nav, theme picker, table, filter bar) restyled per D-01–D-09 | ✓ VERIFIED | 2065 lines; every selector inspected directly (see Observable Truths table) |
| `companion/test_contrast_check.py` | 2 new `live_pairs` entries for the card surface, `EXPECTED_CHECK_COUNT` 31→33 | ✓ VERIFIED | `EXPECTED_CHECK_COUNT = 33`; live run: 33/33 pass |
| `companion/test_view_pages.py` | New drift guard for the shared `[data-filter-clear]` contract, `EXPECTED_CHECK_COUNT` 41→42 | ✓ VERIFIED | Live run: 42/42 pass, including `_clear_control_shared_attribute_contract` |
| `06.6.4-VALIDATION.md` | Completed Per-Task Verification Map, resolved Approval line | ✓ VERIFIED | Approval line present and resolved, all 24 checklist rows shown ✅ green |
| `companion/layout.py` | Untouched (D-06a resolved CSS-only) | ✓ VERIFIED | `_theme_form_html()` at line 613 already wraps all three buttons in one `<form>`; no phase commit touches `companion/layout.py`'s theme-form logic |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `.runway-card--selected` | hover rule | `:not()` scoping | WIRED | `.runway-card:not(.runway-card--selected):hover` structurally cannot match the selected modifier |
| `button[type="submit"]` (theme segments) | `.theme-form .theme-option` | specificity override | WIRED | `.theme-form .theme-option` is (0,2,0), outranks `button[type="submit"]` (0,1,1); `box-shadow: none` explicitly neutralizes the inset highlight |
| `.data-table th` | `.data-table-wrap th` (sticky) | shared header restyle | WIRED | Both rules edited together; sticky rule's background moved off the old filled surface to `--color-canvas`, confirmed not repainting the quiet header |
| `history_page.py` `<button data-filter-clear>` / `airlines_page.py` `<a data-filter-clear>` | `.filter-bar [data-filter-clear]` | attribute-selector convergence | WIRED | Both page modules unmodified; both elements confirmed present via grep; CSS rule confirmed styling the shared attribute |
| `.copy-btn::before` | 44×44 hit area | negative inset | WIRED | `inset: -11px` on a 22×22 box = 44×44 effective, confirmed by arithmetic and unchanged from the original 28px/-8px (28+16=44) formula |

### Behavioral Spot-Checks / Full Suite Run

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full composed test suite | `scripts/run-all-tests.sh` (run live by verifier, not trusted from SUMMARY) | 16 harnesses; 15 pass fully, `server/test_poll_loop.py` 42/43 (single pre-documented, accepted, unrelated macOS-local exception — zero `server/` files touched by this phase) | ✓ PASS (matches documented exception) |
| `companion-app` | (part of full suite) | 88/88 | ✓ PASS |
| `config-page` | (part of full suite) | 47/47 | ✓ PASS |
| `contrast-check` | (part of full suite) | 33/33 | ✓ PASS |
| `status-pages` | (part of full suite) | 59/59 | ✓ PASS |
| `view-pages` | (part of full suite) | 42/42 | ✓ PASS |
| Coverage threshold | pyproject.toml `fail_under = 83` | 91% | ✓ PASS |

### Requirements Coverage

No formal REQUIREMENTS.md IDs are mapped to this phase — confirmed by `grep -n "06.6.4\|D-0[1-9]" .planning/REQUIREMENTS.md` returning zero matches. This matches ROADMAP.md's own note for Phase 06.6.4 ("Requirements: None — unmapped backlog phase, matching every prior `06.6.x` decimal phase's precedent — `requirements.mark-complete` returning `not_found` for these IDs is expected, not an error") and the verification task's own instruction. Traceability instead runs through `06.6.4-CONTEXT.md`'s Decision IDs D-01 through D-11 (plus sub-decisions a), all six of which are declared across the 6 plans' `requirements:` frontmatter (`06.6.4-01`: D-03, D-03a, D-09; `06.6.4-02`: D-01, D-01a, D-01b, D-01c, D-02; `06.6.4-03`: D-04, D-04a; `06.6.4-04`: D-05, D-05a, D-06, D-06a; `06.6.4-05`: D-07, D-07a, D-08; `06.6.4-06`: D-10, D-11) — every Decision ID from CONTEXT.md is accounted for in exactly one plan. No orphaned Decision IDs found.

### Anti-Patterns Found

None. Scanned `companion/static/style.css`, `companion/test_contrast_check.py`, `companion/test_view_pages.py`, and `companion/layout.py` for `TBD`/`FIXME`/`XXX`/`TODO`/`placeholder`/`not yet implemented` — zero matches. No external stylesheet references (`http://`, `https://`, `url(`) introduced.

### Human Verification Required

None. The phase's own closing plan (06.6.4-06) already performed the real-browser, both-theme, 24-item checkpoint that this project's precedent requires for CSS-only visual changes (per project memory: "computed-style checks alone missed a real mobile nav bug"). The developer's verbatim verdict is recorded in `06.6.4-06-SUMMARY.md` and the resolved Approval line in `06.6.4-VALIDATION.md`, with one real finding (copy button still read too large) found and fixed post-verdict (commit `ee7235a`), re-verified against the full suite and a fresh screenshot per the SUMMARY. This satisfies the phase's own Nyquist/human-verification contract; no further human check is being deferred to this verification report.

### Gaps Summary

No gaps found. All 16 observable truths derived from the phase goal and the 6 plans' `must_haves` are verified directly against `companion/static/style.css`, `companion/layout.py`, `companion/pages/history_page.py`, `companion/pages/airlines_page.py`, and the live test suite — not inferred from SUMMARY prose. The full test suite was re-run independently by this verifier (not trusted from SUMMARY claims) and reproduces the exact pass counts documented in every plan's SUMMARY (companion-app 88/88, config-page 47/47, contrast-check 33/33, status-pages 59/59, view-pages 42/42, 91% coverage, single pre-documented `server/test_poll_loop.py` exception). All git commit hashes cited across the 6 SUMMARYs resolve to real commits in this branch. The developer's real-browser sign-off (the one thing no automated check can substitute for) is documented with verbatim quotes and a resolved Approval line, matching this project's own established precedent for CSS-only visual-change closing checkpoints.

---

_Verified: 2026-09-01T08:01:49Z_
_Verifier: Claude (gsd-verifier)_
