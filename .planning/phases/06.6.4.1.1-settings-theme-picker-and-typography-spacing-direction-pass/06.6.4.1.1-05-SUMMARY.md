---
phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass
plan: 05
subsystem: ui
tags: [css, python, http-server, image-preview, config-page]

# Dependency graph
requires:
  - phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass (plan 01)
    provides: "GET /theme-preview/{id}.png route and THEME_PREVIEW_ROUTE_PREFIX/THEME_PREVIEW_ALT_TEMPLATE constants this plan's chip markup embeds"
  - phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass (plan 03)
    provides: "the spacing tokens (--space-xs/--space-sm) the new .theme-chip component follows"
provides:
  - "companion/static/style.css: the .theme-chip component (.theme-chip-grid/.theme-chip/.theme-chip--selected/.theme-chip__preview/__body/__name/__swatches/__dot/__check), reusing .runway-card's hidden-radio/server-computed --selected/always-present check-glyph mechanism verbatim"
  - "companion/pages/config_page.py: theme_fieldset()'s multi-theme branch rewritten to emit a .theme-status card holding one .theme-chip per THEME_IDS entry, each carrying a real /theme-preview/{id}.png rendered preview, two real-palette swatch dots, and the theme name"
  - "companion/test_config_page.py: the harness retargeted onto the chip contract (69 checks, up from 65), with a new cross-file CSS guard and 4 new markup-contract checks"
affects: ["06.6.4.1.1-06 (any remaining plan in this phase's typography/spacing pass — Settings now renders zero <fieldset>/<legend> anywhere)"]

tech-stack:
  added: []
  patterns:
    - "Second selectable-card component (.theme-chip) deliberately built as a sibling/neighbour of .runway-card in style.css, reusing its hidden-radio-in-label + server-computed --selected modifier + always-present check-glyph mechanism verbatim rather than inventing a new selectable-card idiom"
    - "Mechanism-owned route-prefix rebinding consumed a second time: companion/pages/config_page.py rebinds THEME_PREVIEW_ROUTE_PREFIX/THEME_PREVIEW_ALT_TEMPLATE from companion/theme_preview.py, exactly the reversed-definition-site pattern plan 01's SUMMARY predicted this plan would need"

key-files:
  created: []
  modified:
    - companion/static/style.css
    - companion/pages/config_page.py
    - companion/test_config_page.py
    - .planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md

key-decisions:
  - "Applied the plan's own instruction to extend 06.6.1-UI-SPEC.md's Color section with the same one-line accent-reservation broadening added to style.css's header comment, since that file was still present on disk (not just recorded in the SUMMARY as the plan's fallback option)"
  - "Widened the CSS cross-file guard's .theme-chip window from 400 to 700 bytes after the load-bearing margin/font-size comment pushed 'background: var(--color-dominant)' past the original window size — a test-harness fix, not a CSS change"

requirements-completed: []

coverage:
  - id: D1
    description: "Settings' Theme group renders as a wrapping grid of 16 compact .theme-chip cards inside a .theme-status card, each with a real rendered theme preview, two real-palette swatch dots, and the theme name — no <fieldset>/<legend> radio list remains anywhere on the page"
    verification:
      - kind: unit
        ref: "companion/test_config_page.py#_render_shape_theme_chip_grid_runway_cards_led_group_and_save_button"
        status: pass
      - kind: unit
        ref: "companion/test_config_page.py#_settings_page_has_zero_fieldsets_and_three_dirty_sections"
        status: pass
      - kind: integration
        ref: "manual: real running companion instance, GET /settings after POST /login — grep confirmed 16 theme-chip__preview, 1 theme-chip-grid, 0 <fieldset>, 0 <legend>"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every chip's <img src> points at THEME_PREVIEW_ROUTE_PREFIX + the theme's own id, and every chip's swatch dots equal _palette_hex() of that theme's departing_index/arriving_index — real rendered previews and real panel colours, not placeholders"
    verification:
      - kind: unit
        ref: "companion/test_config_page.py#_theme_chip_preview_src_points_at_the_real_route_prefix_for_every_theme"
        status: pass
      - kind: unit
        ref: "companion/test_config_page.py#_theme_chip_swatch_dots_carry_real_palette_hex_values"
        status: pass
      - kind: integration
        ref: "manual: GET /theme-preview/blue.png against a real running instance returned a real 320x120 PNG"
        status: pass
    human_judgment: false
  - id: D3
    description: "The currently-saved theme's chip carries an accent border (.theme-chip--selected) and a check glyph; keyboard and no-JS selection still work because the radio is visually-hidden (never display:none), and handle_post()'s existing THEME_IDS membership validation is unchanged"
    verification:
      - kind: unit
        ref: "companion/test_config_page.py#_theme_fieldset_default_selects_exactly_the_white_option"
        status: pass
      - kind: unit
        ref: "companion/test_config_page.py#_current_theme_and_runway_are_selected"
        status: pass
      - kind: unit
        ref: "companion/test_config_page.py#_theme_chip_radio_hidden_and_check_glyph_present_on_every_chip"
        status: pass
      - kind: unit
        ref: "companion/test_config_page.py (handle_post non-member theme -> FLASH_SAVE_FAILED, unchanged)"
        status: pass
    human_judgment: true
    rationale: "Keyboard-only tab/space selection and the visual accent border/check glyph in both light and dark mode are the plan's own <verification> item still owed to a human at the checkpoint in plan 06 — this plan's harness proves the markup contract but not the live rendered/keyboard experience."

duration: 55min
completed: 2026-09-04
status: complete
---

# Phase 06.6.4.1.1 Plan 05: Settings theme picker chip grid Summary

**Settings' 16 stacked 44px theme radios replaced with a wrapping grid of `.theme-chip` cards — each showing a real `/theme-preview/{id}.png` render, two real-palette swatch dots, and the theme name, reusing `.runway-card`'s hidden-radio/accent-border/check-glyph mechanism verbatim.**

## Performance

- **Duration:** ~55 min
- **Completed:** 2026-09-04T05:10:00Z
- **Tasks:** 3
- **Files modified:** 4 (3 planned + 1 UI-SPEC.md extension per the plan's own instruction)

## Accomplishments
- Added a `.theme-chip` component family to `companion/static/style.css` (sketch 004 variant B, the developer-confirmed winner): `.theme-chip-grid`, `.theme-chip`, `.theme-chip--selected`, `.theme-chip__preview`, `__body`, `__name`, `__swatches`, `__dot`, `__check` — placed as a direct neighbour of `.runway-card`, reusing its hidden-radio-in-label + server-computed `--selected` modifier + always-present check-glyph mechanism verbatim rather than inventing a second selectable-card idiom.
- Extended `style.css`'s header accent-reservation list to cover the selected theme chip's border and check glyph, and applied the same one-line extension to `06.6.1-UI-SPEC.md`'s Color section (the plan's own instruction, applied rather than merely recorded since that file is still present on disk).
- Rewrote `theme_fieldset()`'s multi-theme branch in `companion/pages/config_page.py`: it now emits a `.theme-status` card (the same wrapper Runway/Diagnostic LED use) holding a `.theme-chip-grid` of one chip per `THEME_IDS` entry, each with a real rendered preview `<img>`, two real-palette swatch dots, and the theme name. The radio's `name`/`value`/`checked` semantics are byte-for-byte unchanged, so `handle_post()`'s existing membership validation keeps working untouched. Settings now renders zero `<fieldset>` and zero `<legend>` elements anywhere.
- Rebound `THEME_PREVIEW_ROUTE_PREFIX`/`THEME_PREVIEW_ALT_TEMPLATE` in `config_page.py` from `companion/theme_preview.py` (plan 01), mirroring `RUNWAY_IMAGE_ROUTE_PREFIX`'s own rebinding discipline with the definition site inverted.
- Retargeted 7 pre-existing checks in `companion/test_config_page.py` that encoded the retired fieldset/radio-list contract (page shape, group naming, the multi-theme fallback, default selection, section-caption position, current-theme/runway-selected needles, and the runway-image `<img>` count now scoped to `runway-card__image`), extended the cross-file CSS guard with four new `.theme-chip*` assertions, and added 4 brand-new checks (preview-route contract, real-palette swatch values, hidden-radio + check-glyph markup, zero-fieldset/three-dirty-section page shape). `EXPECTED_CHECK_COUNT` moved 65 → 69; no check was weakened.
- Verified against a real running companion instance (temp state dir, real login): `GET /settings` renders exactly 16 `theme-chip__preview` occurrences, 1 `theme-chip-grid`, 0 `<fieldset>`, 0 `<legend>`; `GET /theme-preview/blue.png` returned a real 320×120 PNG.

## Task Commits

Each task was committed atomically:

1. **Task 1: The .theme-chip component in style.css (D-01, D-02, D-07)** - `a98cf10` (feat)
2. **Task 2: Rewrite theme_fieldset()'s multi-theme branch as the chip grid (D-01, D-02, D-03, D-08)** - `cf05e8c` (feat)
3. **Task 3: Retarget companion/test_config_page.py onto the chip contract** - `3d4b6e9` (test)

_No separate TDD test-then-implementation split: this codebase's `check()`-harness convention bundles test additions with the code they cover, matching plan 01's own precedent._

## Files Created/Modified
- `companion/static/style.css` - new `.theme-chip*` component family (9 selectors), placed as a neighbour of `.runway-card`; extended the header's accent-reservation list
- `companion/pages/config_page.py` - rebinds `THEME_PREVIEW_ROUTE_PREFIX`/`THEME_PREVIEW_ALT_TEMPLATE`; `theme_fieldset()`'s multi-theme branch rewritten as the D-01 chip grid; docstring updated
- `companion/test_config_page.py` - 7 checks retargeted in place onto the chip contract, the cross-file CSS guard extended with 4 `.theme-chip*` assertions, 4 new checks added; `EXPECTED_CHECK_COUNT` 65 → 69
- `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md` - Accent row's Usage cell extended with the same one-line broadening note added to style.css's header comment (plan's own instruction, file confirmed still present)

## Decisions Made
- Applied (not just recorded) the plan's UI-SPEC.md accent-reservation broadening, since `.planning/phases/06.6.1-.../06.6.1-UI-SPEC.md` is still present on disk — the plan's fallback ("record in the SUMMARY... apply it if that file is still present") pointed at applying it directly.
- Widened the harness's `.theme-chip` cross-file CSS-guard window from 400 to 700 bytes: the load-bearing comment explaining the margin/font-size overrides pushed `background: var(--color-dominant)` past the original window size. This is a test-harness sizing fix, not a CSS behavior change — confirmed by re-running the full harness after the widening.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Comment text collided with its own acceptance-criteria grep guard**
- **Found during:** Task 1 acceptance-criteria verification
- **Issue:** The first draft of `.theme-chip`'s explanatory comment used the literal string "margin-bottom: 0" inside prose, which the plan's own acceptance criterion (`awk '/^\.theme-chip \{/,/^}/' | grep -c 'margin-bottom: 0'` must equal 1) then counted twice — once in the comment, once in the real declaration.
- **Fix:** Reworded the comment to describe the same fact without repeating the literal declaration text.
- **Files modified:** `companion/static/style.css`
- **Verification:** The acceptance-criteria grep now returns exactly 1.
- **Committed in:** `a98cf10` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug, cosmetic-only)
**Impact on plan:** No scope creep, no behavior change — purely a comment-wording fix to satisfy the plan's own acceptance-criteria grep guard exactly, the same class of issue plan 01's own SUMMARY recorded.

## Issues Encountered
None beyond the deviation above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Settings' Theme group is now a compact, informative chip grid; the whole rendered page carries zero `<fieldset>`/`<legend>` elements, matching this phase's broader typography/spacing direction (all four groups named by one consistent `<h2 class="text-heading">` level).
- The plan's own `<verification>` section calls for a keyboard/no-JS and light-vs-dark visual check by a human — that remains for plan 06 (or a later checkpoint in this phase) since this plan was fully autonomous (no `checkpoint:*` tasks) and the harness/manual-curl checks above cover markup correctness, not the live interactive/visual experience.
- No blockers. All five companion test harnesses (`test_companion_app.py`, `test_config_page.py`, `test_contrast_check.py`, `test_status_pages.py`, `test_view_pages.py`) exit 0.

---
*Phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: companion/static/style.css
- FOUND: companion/pages/config_page.py
- FOUND: companion/test_config_page.py
- FOUND: .planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md
- FOUND: .planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-05-SUMMARY.md
- FOUND commit: a98cf10
- FOUND commit: cf05e8c
- FOUND commit: 3d4b6e9
