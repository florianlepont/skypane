---
phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/static/style.css
  - companion/test_status_pages.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "On an Apple device the serif headings render in Iowan Old Style or Charter, not Georgia."
    - "A page title, a section heading and a nested card title inside that section read as three distinct tiers, not one."
    - "Every label in the app — stat-tile captions, table headers, mobile card labels, filter counts, pills and chips — reads as ONE voice: 12px sans, semibold, uppercase, +0.06em."
    - "No serif text remains at Label size anywhere in the companion."
  artifacts:
    - "companion/static/style.css: the reordered --font-serif stack"
    - "companion/static/style.css: the sans-semibold nested-card-title tier on .page-section--nested > h2 / .battery-trend-section > h2"
    - "companion/test_status_pages.py: the two retargeted type-contract checks"
  key_links:
    - ".stat-tile__caption and .data-table th and .data-card__label must all state the same label voice — test_status_pages.py already asserts .data-card__label's font-size/color are STRING-EQUAL to .data-table th's, so those two move together or the harness fails"
    - ".page-section--nested > h2 overrides the shared h1/h2/h3/legend/.text-heading serif rule's family, size and letter-spacing — the base rule itself must stay untouched (test_companion_app.py pins its exact selector string and its two declarations)"
---

<objective>
Land the type half of the direction pass in `companion/static/style.css`: the serif-stack
reorder (D-12), the three-tier heading ladder (D-09, D-10, D-11), and the single unified
label voice (D-13) — plus the harness retargeting those changes require.

Purpose: UIR-21's finding is that hierarchy is carried by size alone and collapses inside
a card, and UIR-23's is that two label voices coexist. CONTEXT locks the full audit
ladder, not a weight-only or size-only subset. This plan is the foundation the spacing
pass (plan 03) and the theme chips (plan 05) both sit on top of.

Output: a revised type contract in `style.css`, and `companion/test_status_pages.py`'s two
type-contract checks rewritten to assert the new contract instead of the old one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-CONTEXT.md
@.planning/sketches/005-type-ladder-health-page/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serif stack, page title, section heading and wordmark (D-12, D-10, D-11)</name>

  <files>companion/static/style.css</files>

  <read_first>
  - `Skill("sketch-findings-skypane")` — read the skill in full, and specifically
    `references/visual-direction-typography.md`'s "Nested card title tier" and
    "Stat-tile captions" entries. They record the round trip this phase's D-09 reverses a
    second time; do not start editing before you have read them.
  - `companion/static/style.css` lines 1-70 — the header comment's design-system boundary
    statement, including the zero-external-reference rule (the serif stack must stay a
    system stack; no web font, no `url()`).
  - `companion/static/style.css` lines 82-96 — the four-size/two-weight token block and
    `--font-serif`.
  - `companion/static/style.css` lines 247-450 — `.text-label`, `.text-body`,
    `.text-heading`, `.section-caption`, `.section-intro`, the shared serif rule
    (`h1, h2, h3, legend, .text-heading`), the heading-rhythm rule, `.page-title`, and
    `.site-title` (note its retired-`--font-display-size` comment right above it).
  - `.planning/phases/06.6.4.1-.../06.6.4.1-UI-REVIEW.md` — the "Proposed type ladder"
    table at the end of the Design-direction section. That table is this phase's starting
    spec, not inspiration.
  </read_first>

  <action>
  Four edits, each in place on an existing rule — never by appending a new rule that
  restates an existing selector.

  1. **D-12, serif stack.** Reorder `--font-serif` to
     `"Iowan Old Style", Charter, "Palatino Linotype", Georgia, "Book Antiqua", serif`.
     Extend the existing comment above it: Georgia first meant every Mac and iPhone
     rendered Georgia even though Iowan Old Style and Charter are bundled there (UIR-20);
     Windows now falls through Palatino Linotype to Georgia. Restate that this is still a
     system stack, so the file's zero-external-reference rule is untouched.

  2. **D-10, page title.** Change the token `--font-page-title-size` from `30px` to
     `32px`. In `.page-title`, change `font-weight` to `var(--weight-semibold)`,
     `letter-spacing` to `-0.015em`, and `line-height` to `1.15`. Keep the existing
     `margin` and `font-family` declarations as they are.

  3. **D-10, section heading.** Change the token `--font-heading-size` from `20px` to
     `22px`, and change `.text-heading`'s `line-height` from `1.2` to `1.25`.
     `.text-heading` must keep inheriting `var(--weight-regular)` from the shared serif
     rule and must NOT gain a weight of its own — the section-heading tier stays regular,
     which is what makes the semibold page title and the semibold nested card title read
     as different tiers rather than as more of the same. Note in a comment that `legend`
     also consumes `--font-heading-size`; that is fine and intentional.

  4. **D-11, wordmark.** In `.site-title`, set `font-size: 24px`,
     `font-weight: var(--weight-semibold)`, `letter-spacing: -0.01em` (replacing the
     current `0.02em`), keeping `font-family: var(--font-serif)` and `line-height: 1.2`.
     The `24px` is a literal, not a token, and needs a comment saying why: the file's
     stated scale is four content sizes, the retired `--font-display-size` was 24px and
     was deliberately retired by 06.6.4 D-09, and the wordmark is a brand role rather than
     a content role — so it takes a literal, the way the documented sub-scale exception
     tier (11/12/13px) already does, instead of resurrecting a fifth content token.
     `.sidebar-title` needs no rule of its own; it is always co-classed with `.site-title`.

  Do not touch the shared `h1, h2, h3, legend, .text-heading {` rule's selector string,
  its `font-family` declaration or its `font-weight` declaration. `test_companion_app.py`
  finds that block by the exact literal `h1,\nh2,\nh3,\nlegend,\n.text-heading {` and
  asserts both declarations are present; reformatting or reordering that selector list
  breaks it.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py && server/.venv/bin/python3 companion/test_contrast_check.py</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c 'Iowan Old Style", Charter' companion/static/style.css` is 1.
  - `grep -c -- '--font-page-title-size: 32px' companion/static/style.css` is 1.
  - `grep -c -- '--font-heading-size: 22px' companion/static/style.css` is 1.
  - `grep -c -- '-0.015em' companion/static/style.css` is at least 1.
  - `grep -c 'url(' companion/static/style.css` is 0 — the zero-external-reference rule
    still holds after touching the font stack.
  - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 (the shared serif
    rule's own contract check still passes).
  - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0.
  </acceptance_criteria>

  <done>The serif stack leads with Iowan Old Style/Charter; page title is 32px semibold with -0.015em; section headings are 22px regular; the wordmark is 24px serif semibold.</done>
</task>

<task type="auto">
  <name>Task 2: Nested card titles become the sans-semibold Emphasis tier (D-09) — the deliberate second reversal</name>

  <files>companion/static/style.css, companion/test_status_pages.py</files>

  <read_first>
  - `companion/static/style.css` lines 2930-3005 — the `.page-section--nested > h2,
    .battery-trend-section > h2` rule and the long comment block above it recording quick
    task 260902-iag's revert, plus the `> p.text-body` rhythm rule below it.
  - `companion/test_status_pages.py` lines 3040-3149 —
    `_nested_heading_tier_reverted_to_standard_heading_role`. It currently HARD-FAILS if
    the nested-heading rule declares any `font-size`, `font-weight` or `font-family`, and
    asserts `--font-heading-size` is `20px` in `:root`. Every one of those assertions is
    now wrong and must be rewritten, not deleted.
  - `companion/test_status_pages.py` lines 3151-3253 —
    `_stat_tile_caption_weight_reverted_and_four_role_scale_hold`. It re-asserts the same
    nested-heading constraints, pins `.stat-tile__caption` as serif with no size/weight,
    and pins the three `:root` tokens by value. Task 3 finishes rewriting this one; this
    task fixes its nested-heading and `--font-heading-size` halves.
  - `companion/static/style.css` — the `.stat-tile__value` rule (the existing 16px-semibold
    "Emphasis role" this tier reuses, so no fifth size is introduced).
  </read_first>

  <action>
  **Read this paragraph before editing, and carry its substance into the CSS comment you
  write.** This change moves Health's nested card titles ("Battery trend", "Unresolved
  prefixes", "Resolution statistics") from 20px serif regular to 16px sans semibold. That
  is exactly the change quick task 260901-uzi made and quick task 260902-iag then reverted
  at the developer's own direct instruction, after a same-day screenshot comparison. It is
  being re-applied deliberately, with the developer's explicit confirmation recorded in
  CONTEXT D-09, because the circumstances changed: 260901-uzi demoted this one tier in
  isolation, against a 20px section heading it then matched too closely; here it lands as
  the bottom rung of a real three-tier ladder (32px semibold page title / 22px regular
  section heading / 16px semibold sans card title) validated on a full Health composition
  in sketch 005, variant B. This is not a mistake to second-guess and not a revert to undo
  — it is the third and final state of that round trip, and the comment you write must say
  so and must name 260901-uzi and 260902-iag by id so the next reader can follow the
  whole sequence.

  In `companion/static/style.css`, extend the existing
  `.page-section--nested > h2, .battery-trend-section > h2` rule (do not create a second
  rule with the same selector) with:
  - `font-family: var(--font-ui);`
  - `font-size: var(--font-body-size);`  (16px — the existing Emphasis role
    `.stat-tile__value` already uses; no new size, no new token)
  - `font-weight: var(--weight-semibold);`
  - `letter-spacing: normal;`  (clears the shared serif rule's `-0.01em`, which is tuned
    for large serif text and reads wrong on 16px sans)
  and keep the existing `margin-bottom: var(--space-md);` exactly as it is — that
  declaration is quick task 260902-bl2's independently-justified spacing fix and has
  nothing to do with the tier question.

  Rewrite the CSS comment block above the rule: keep the historical record (it is the
  evidence trail for why this looks like a flip-flop and is not), mark the
  260902-iag-revert paragraphs SUPERSEDED in the file's own established style rather than
  deleting them, and state the current contract plainly at the top.

  Then retarget `_nested_heading_tier_reverted_to_standard_heading_role` in
  `companion/test_status_pages.py`. Keep its structure, its `check()` registration and its
  position (so `EXPECTED_CHECK_COUNT` is unchanged by this task); invert its assertions so
  they now require the rule to declare the sans family, the Body size and the semibold
  weight, still require the retained `margin-bottom: var(--space-md)`, still require
  `.battery-trend-section > h2` in the selector list, and require `--font-heading-size` to
  be `22px` in `:root`. Rewrite its docstring/comment and its `check()` description string
  to describe the new contract — a check whose prose still describes the reverted state
  while asserting the opposite is worse than no check.

  In `_stat_tile_caption_weight_reverted_and_four_role_scale_hold`, fix the two halves
  that belong to this task: its `nested_body` assertions (now: the rule must declare
  `font-size: var(--font-body-size)` and `font-weight: var(--weight-semibold)`), and the
  `--font-heading-size` entry in its token table (`20px` → `22px`). Leave its
  `.stat-tile__caption` assertions alone; Task 3 owns those.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <acceptance_criteria>
  - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and its printed total
    equals `EXPECTED_CHECK_COUNT` (unchanged by this task — checks were retargeted in
    place, not added).
  - `awk '/^\.page-section--nested > h2,/,/^}/' companion/static/style.css | grep -c 'var(--weight-semibold)'` is 1.
  - `awk '/^\.page-section--nested > h2,/,/^}/' companion/static/style.css | grep -c 'var(--font-ui)'` is 1.
  - `awk '/^\.page-section--nested > h2,/,/^}/' companion/static/style.css | grep -c 'margin-bottom: var(--space-md)'` is 1 — 260902-bl2's spacing fix survived.
  - `grep -c '260902-iag' companion/static/style.css` is at least 1 — the round-trip
    history is still traceable in the file.
  - `server/.venv/bin/python3 companion/test_companion_app.py` still exits 0 — the shared
    serif rule was not touched to achieve the override.
  </acceptance_criteria>

  <done>Nested card titles render 16px sans semibold, the two harness checks assert that contract, and the file records why this is the third state of a deliberate round trip.</done>
</task>

<task type="auto">
  <name>Task 3: One label voice — retire the serif Label role (D-13)</name>

  <files>companion/static/style.css, companion/test_status_pages.py</files>

  <read_first>
  - `companion/static/style.css` — `.stat-tile__caption` (with its long
    260902-dng/260902-iag adjudication comment above it), `.data-table th`,
    `.data-card__label`, `.filter-bar__count`, `.banner__pill`, `.airline-card__chip`.
    Note which of these already state the target voice: `.data-table th` and
    `.data-card__label` already carry `var(--weight-semibold)`, `letter-spacing: 0.06em`,
    `text-transform: uppercase` and the 70% `color-mix` — only their `font-size` moves.
  - `companion/test_status_pages.py` around line 3745 — the check asserting
    `.data-card__label`'s `font-size` and `color` declarations are **string-equal** to
    `.data-table th`'s. This is the trap: changing one of those two rules without the
    other fails the harness. They move together.
  - `companion/test_status_pages.py` lines 3151-3253 —
    `_stat_tile_caption_weight_reverted_and_four_role_scale_hold`, whose
    `.stat-tile__caption` assertions (no font-size, no font-weight, must keep
    `var(--font-serif)`) all invert with this task.
  - `companion/test_status_pages.py` around lines 4440-4460 and 4660-4670 — the
    `.banner__pill` source-ORDER assertions. They compare rule positions, not values, so
    editing declarations in place is safe; MOVING the `.banner__pill` rule is not.
  - `companion/layout.py` line ~1092 — `<p class="text-label stat-tile__caption">`, the
    only emitter. The markup does not change; `.text-label` still supplies the base and
    `.stat-tile__caption` overrides it.
  - `companion/static/style.css` `.section-caption` and its comment — this rule composes
    with `.text-label` too, but it is a muted sentence-case *description*, NOT a label.
    It must NOT be swept into the uppercase voice, and `.text-label` itself must NOT be
    changed (that would drag every Settings section caption into uppercase).
  </read_first>

  <action>
  Converge five consumers on one voice: sans, 12px, `var(--weight-semibold)`, uppercase,
  `letter-spacing: 0.06em`, colour `color-mix(in srgb, var(--color-text) 70%, transparent)`
  — the file's existing muted strength, not a new one.

  1. `.stat-tile__caption` — this is the whole substance of D-13. Replace
     `font-family: var(--font-serif)` with `font-family: var(--font-ui)`; replace
     `text-transform: none` with `text-transform: uppercase`; replace
     `letter-spacing: normal` with `letter-spacing: 0.06em`; add `font-size: 12px`,
     `font-weight: var(--weight-semibold)` and the 70% `color-mix` colour. Keep the
     existing `display: flex; align-items: center; gap: var(--space-xs); margin: 0 0
     var(--space-sm);` untouched. Rewrite the comment above it: mark the
     260902-dng/260902-iag weight adjudication SUPERSEDED (its whole premise was a
     four-role scale in which this caption was serif and 14px; both facts are now gone),
     and state the new contract — this rule is no longer a serif exception at all.

  2. **Update the file's serif-boundary header comment.** `style.css`'s comment above the
     shared serif rule names `.stat-tile__caption` as "the one named Label-role
     exception". That exception no longer exists — the serif set is now exactly
     `h1, h2, h3, legend, .text-heading, .page-title, .site-title`, minus the nested
     card-title override plan Task 2 added. Correct that comment; a stale allow-list in a
     comment is precisely the defect the heading-color-consistency debug session was
     caused by.

  3. `.data-table th` — `font-size: 11px` → `12px`. Nothing else changes.

  4. `.data-card__label` — `font-size: 11px` → `12px`, in the same edit. Its `color`
     already matches. These two are pinned string-equal by the harness; if you change one
     alone the suite fails.

  5. `.filter-bar__count` — already 12px and already the 70% mix; add
     `font-weight: var(--weight-semibold)`, `text-transform: uppercase` and
     `letter-spacing: 0.06em`.

  6. `.banner__pill` and `.airline-card__chip` — bring each to `font-size: 12px`,
     `font-weight: var(--weight-semibold)`, `text-transform: uppercase`,
     `letter-spacing: 0.06em`. Edit declarations in place; do not move, merge or reorder
     either rule (source-order assertions key on `.banner__pill`'s position).

  Then rewrite `_stat_tile_caption_weight_reverted_and_four_role_scale_hold` in
  `companion/test_status_pages.py` to assert the new contract: the caption declares the
  uppercase voice's four properties and the 12px size, the caption's rule body no longer
  names the serif token, `.stat-tile__value` keeps its own untouched Emphasis size and
  weight, the shared serif rule keeps `var(--weight-regular)`, and the token table reads
  14/16/22px. Retarget its comment and `check()` description string too. Keep it a single
  check so `EXPECTED_CHECK_COUNT` is unchanged; if you judge the contract now needs a
  second check, add one and bump the count with the usual inline arithmetic comment.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
  - `awk '/^\.stat-tile__caption \{/,/^}/' companion/static/style.css | grep -c 'text-transform: uppercase'` is 1.
  - `awk '/^\.stat-tile__caption \{/,/^}/' companion/static/style.css | grep -c 'font-size: 12px'` is 1.
  - `awk '/^\.data-table th \{/,/^}/' companion/static/style.css | grep -c 'font-size: 12px'` is 1.
  - `awk '/^\.data-card__label \{/,/^}/' companion/static/style.css | grep -c 'font-size: 12px'` is 1.
  - `awk '/^\.filter-bar__count \{/,/^}/' companion/static/style.css | grep -c 'letter-spacing: 0.06em'` is 1.
  - `awk '/^\.section-caption \{/,/^}/' companion/static/style.css | grep -vc 'uppercase'` confirms the
    Settings section captions were NOT swept into the label voice (the rule body contains
    no uppercase transform).
  - All five companion harnesses exit 0:
    `for f in companion/test_companion_app.py companion/test_config_page.py companion/test_contrast_check.py companion/test_status_pages.py companion/test_view_pages.py; do server/.venv/bin/python3 "$f" >/dev/null || echo "FAIL $f"; done` prints nothing.
  </acceptance_criteria>

  <done>Stat-tile captions, table headers, mobile card labels, filter counts, pills and chips all state one 12px uppercase sans-semibold voice; no serif text remains at Label size; the file's serif allow-list comment is accurate again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is presentation-only: no route, no input parsing, no filesystem path, no new data flow. The only boundary it touches is the browser's own rendering of an already-served stylesheet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1.1-06 | Information disclosure | `--font-serif` stack | low | mitigate | The reordered stack must stay a system font stack — no `@font-face`, no `url()`. A remote font would leak every page view to a third party and break the file's stated zero-external-reference rule. Asserted by the `grep -c 'url('` criterion in Task 1 |
| T-06.6.4.1.1-07 | Denial of service (accessibility) | 12px uppercase label voice | medium | mitigate | Shrinking text is a legibility risk, not a security one, but it is a real user-harm regression path. Mitigated by keeping the existing 70% `color-mix` muted strength (measured ≥6.2:1 in the 06.6.4.1 audit) rather than muting further, by moving `.data-table th`/`.data-card__label` UP from 11px to 12px, and by the blocking developer verification in plan 06 |
| T-06.6.4.1.1-08 | Tampering | harness retargeting | high | mitigate | The two rewritten checks are the executable contract for this whole ladder. Retarget them — never delete or `return True` them. Every acceptance criterion above re-asserts the CSS value independently of the harness, so a check that was gutted rather than rewritten still fails the plan |
| T-06.6.4.1.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan |
</threat_model>

<verification>
- All five companion harnesses exit 0.
- `git diff --stat companion/static/style.css` shows edits concentrated in the rules named
  above — no rule was moved, duplicated, or removed.
- No new CSS selector was created for a role that already had one (every change in this
  plan is an in-place declaration edit).
</verification>

<success_criteria>
- Three visually distinct heading tiers exist on Health and Settings.
- Exactly one label voice exists across the whole companion.
- The design-system comments inside `style.css` describe what the file actually does.
</success_criteria>

## Artifacts this plan produces

**Changed CSS token values:** `--font-serif` (reordered), `--font-page-title-size`
(30px → 32px), `--font-heading-size` (20px → 22px)

**Changed CSS rules (no new selectors):** `.page-title`, `.text-heading`, `.site-title`,
`.page-section--nested > h2, .battery-trend-section > h2`, `.stat-tile__caption`,
`.data-table th`, `.data-card__label`, `.filter-bar__count`, `.banner__pill`,
`.airline-card__chip`

**New CSS declarations of note:** the sans-semibold Emphasis-role override on the nested
card-title selector; the 12px uppercase label voice on `.stat-tile__caption`

**Retargeted harness checks:** `test_status_pages.py::_nested_heading_tier_reverted_to_standard_heading_role`
(contract inverted, name should be updated to describe the new state),
`test_status_pages.py::_stat_tile_caption_weight_reverted_and_four_role_scale_hold`
(contract inverted, name should be updated)

<output>
Create `.planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-02-SUMMARY.md` when done.
</output>
