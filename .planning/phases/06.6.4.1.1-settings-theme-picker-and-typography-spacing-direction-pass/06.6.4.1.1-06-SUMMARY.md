---
phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass
plan: 06
subsystem: ui
tags: [verification, coverage, http-routes, checkpoint, design-system, css, accessibility]

# Dependency graph
requires:
  - phase: 06.6.4.1.1-01
    provides: "GET /theme-preview/{id}.png route and disk cache this plan's live-route gate exercises end to end"
  - phase: 06.6.4.1.1-02
    provides: "the type ladder and unified label voice this plan's checkpoint asked the developer to inspect on real pages"
  - phase: 06.6.4.1.1-03
    provides: "the spacing/density pass, including the .battery-trend-section padding extension flagged for this plan's checkpoint"
  - phase: 06.6.4.1.1-04
    provides: "the flash-slot move (D-17) this plan's checkpoint asked the developer to confirm on Settings"
  - phase: 06.6.4.1.1-05
    provides: "the Settings theme-chip grid this plan's checkpoint presented as its headline change, and the .runway-card selectable-card mechanism the chip reuses verbatim"
provides:
  - "Full-suite (16/16 harnesses) and 92% coverage pass recorded against the phase's final code state"
  - "Live end-to-end verification of all 16 /theme-preview/{id}.png routes, their disk cache, and their refusal paths (unknown id, traversal-shaped id, unauthenticated request)"
  - "Pairwise mean-RGB distinctness proof across all 16 theme previews, fetched over HTTP"
  - "The developer's verbatim checkpoint response, approving the entire revised visual contract with one real fix requested and applied: a background-wash addition to the selected-state treatment of .runway-card and .theme-chip"
  - "companion/static/style.css: .runway-card--selected and .theme-chip--selected .theme-chip__body both carry a 12%-accent color-mix background wash, matching .theme-form .theme-option--active's established active-state idiom"
  - "companion/test_config_page.py: a new harness check (70th) proving the background-wash rule exists on both selectors, with the wash correctly scoped away from .theme-chip's own preview band"
  - "sketch-findings-skypane skill (SKILL.md + 3 reference files) brought current with everything 06.6.4.1.1 shipped, including the checkpoint's own fix, with every superseded value marked in place rather than deleted"
affects: ["06.6.4.1.1 phase closure — this was the phase's final plan; the phase is now complete"]

tech-stack:
  added: []
  patterns:
    - "Live-route verification against a disposable local state dir (matching plan 05's own precedent for manual verification depth)"
    - "Real-browser visual verification via the cached Playwright Chromium binary driven over raw CDP (a hand-rolled minimal WebSocket client, no external deps), used here because no MCP browser tool was reachable from inside this subagent — see Decisions Made for the full mechanism"
    - "Selected-card idiom unification: border + check glyph + 12%-accent background wash, now the single 'this is the current selection' visual language shared by .theme-form .theme-option--active, the nav-pill active-link rules, .runway-card--selected, and .theme-chip--selected"

key-files:
  created: []
  modified:
    - companion/static/style.css
    - companion/test_config_page.py
    - .claude/skills/sketch-findings-skypane/SKILL.md
    - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
    - .claude/skills/sketch-findings-skypane/references/control-density.md
    - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md
    - .planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md

key-decisions:
  - "The developer's checkpoint response approved every lettered/numbered item in Task 2's <how-to-verify> list (theme picker mechanism, the full heading ladder including D-09's deliberate second reversal, label voice, spacing/density) — no rollback, no further changes to any of those. The one real problem reported was site-wide: the selected-state visual treatment (a thin border plus a small stroke-only check glyph) was too weak to notice at a glance. This was diagnosed as a PRE-EXISTING weak pattern .theme-chip inherited faithfully from .runway-card per CONTEXT.md D-01's own explicit instruction, made newly visible by the chip grid's greater density (16 small photo-heavy chips vs. 3 large runway cards) — not a new defect plan 05 introduced."
  - "Fix: added a color-mix(in srgb, var(--color-accent) 12%, transparent) background wash to BOTH .runway-card--selected (whole card) and .theme-chip--selected .theme-chip__body (body only, not the whole chip, so the wash never sits behind and tints the rendered theme-preview band) — reusing the exact idiom .theme-form .theme-option--active and the nav-pill active-link rules already use, rather than inventing a new one. This is additive: the existing 2px accent border and check glyph on both components are unchanged."
  - "No production state-dir snapshot (/tmp/skypane-prod-state) was present in this environment for the real-browser visual verification, matching Task 1's own prior finding. A fresh, disposable local state dir was used instead, which is sufficient for verifying the selected-state contrast fix (a rendering/CSS property, independent of what data the state dir holds)."
  - "Real-browser verification used a hand-rolled minimal raw-WebSocket CDP client (no external Python deps available in this environment) driving the cached Playwright Chromium binary headless, per this execution's own environment note that no MCP browser tool is reachable from inside a subagent here. Login was performed via a real form submission inside the browser context (fill password field, .submit()) rather than manually replaying cookies, so Chrome's own Secure-cookie-over-http-on-127.0.0.1 handling was exercised exactly as a real user's browser would, not worked around."
  - "Task 3 (skill update) was executed strictly after Task 2's checkpoint resolved and the fix was applied and committed, per the plan's own instruction to record the shipped state rather than the proposed one — the skill's Nested card title tier, Label voice, and new Selected-card-treatment entries all reflect the developer's actual sign-off and the actual fix, not a prediction of either."

requirements-completed: []

coverage:
  - id: D1
    description: "scripts/run-all-tests.sh (the canonical 16-harness suite) prints ==> Result: PASS with the coverage threshold enforced"
    verification:
      - kind: automated_ui
        ref: "scripts/run-all-tests.sh — 16/16 harnesses pass, combined coverage 92% (TOTAL 3385 stmts, 271 miss), re-run clean after both the CSS fix and the test-suite extension"
        status: pass
    human_judgment: false
  - id: D2
    description: "All 16 registered themes' /theme-preview/{id}.png routes return 200 image/png against a running companion; unknown-id and traversal-shaped id both 404; an unauthenticated request returns no image bytes"
    verification:
      - kind: e2e
        ref: "Task 1 (prior execution): manual curl session against a local companion instance — all 16 THEME_IDS returned 200/image/png; unknown-id and two traversal-shaped variants returned 404; unauthenticated request returned 303 with 0 response bytes"
        status: pass
    human_judgment: false
  - id: D3
    description: "{state_dir}/theme_previews/ holds exactly one file per registered theme after the first Settings-driven load, and a second load adds no new files"
    verification:
      - kind: e2e
        ref: "Task 1 (prior execution): theme_previews/ held exactly 16 files after the first pass over all 16 routes; a second full pass added zero new files"
        status: pass
    human_judgment: false
  - id: D4
    description: "The 16 fetched theme-preview PNGs have pairwise-distinct mean RGB values, proving the chips are visually discriminating rather than decorative"
    verification:
      - kind: e2e
        ref: "Task 1 (prior execution): all 120 pairwise comparisons distinct, including the closest-looking pairs (White vs Band Blue Light, the five Band-family members)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The developer has looked at the real pages in both themes at desktop and phone width and signed off — including on D-09's deliberate second reversal"
    verification:
      - kind: human_judgment
        ref: "Developer's verbatim response (two messages, French): 'Oui, c'est parfait !. Par contre, il y a un souci sur l'ensemble du site, c'est que l'element selectionne est tres peu visible. On ne comprend pas exactement quel est le nouveau parametre qu'on a selectionne par rapport a l'ancien. Il y a seulement un jeu de nombre qui change.' Then, confirming items 2-5 individually: '2. paerfait / 3. parfait / 4. oui / 5. parfait'. Interpretation: every checkpoint item is approved as shipped (theme picker mechanism, the heading ladder including D-09's reversal, label voice, spacing/density) — no rollback. The one real problem (selected-state visibility) was diagnosed and fixed in this same plan (see Deviations)."
        status: pass
    human_judgment: true
  - id: D6
    description: "The design-system skill describes what the stylesheet now actually does, with superseded values marked rather than deleted"
    verification:
      - kind: automated
        ref: "python3 -c check confirming SKILL.md mentions 06.6.4.1.1 (17x), 004-theme-picker-chips, 005-type-ladder-health-page, and theme-chip; SUPERSEDED count in visual-direction-typography.md rose from 13 (git HEAD~ baseline) to 18 after this plan's edits, proving prior values were marked, not deleted"
        status: pass
      - kind: manual
        ref: "Spot-checked every quoted token value (--font-label-size 14px, --font-body-size 16px, --font-heading-size 22px, --font-page-title-size 32px, --font-serif stack, all seven --space-* tokens) against companion/static/style.css's own :root declarations — all match exactly"
        status: pass
    human_judgment: false
  - id: D7
    description: "(New, this continuation) The selected-state background-wash fix is verified both by an automated harness check and by real-browser visual confirmation in both themes, at desktop and mobile widths"
    verification:
      - kind: unit
        ref: "companion/test_config_page.py#_selected_runway_card_and_theme_chip_carry_a_background_wash — new check, 70th of 70"
        status: pass
      - kind: e2e
        ref: "Real running companion instance, real login (form submission inside a headless-Chromium browser context over raw CDP), screenshots captured in light/dark x desktop/mobile — the selected White theme chip and the selected Piste 3 runway card are both clearly, immediately distinguishable from unselected siblings at a glance, in every combination"
        status: pass
    human_judgment: false

duration: ~55min (this continuation session; Task 1 was ~12min in a prior session)
completed: 2026-09-04
status: complete
---

# Phase 06.6.4.1.1 Plan 06: Full-suite gates, developer checkpoint, and design-system close-out Summary

**Full-suite/live-route gates passed (Task 1, prior session); the developer's blocking checkpoint approved the entire revised visual contract with one real fix requested — a background-wash addition to the selected theme chip and runway card, applied and verified live in both themes (Task 2); the sketch-findings-skypane design-system skill brought fully current with everything this phase shipped, including the fix (Task 3). Phase 06.6.4.1.1 is now complete.**

## Performance

- **Duration:** ~55 min (this continuation session, resuming from Task 1's prior ~12 min run)
- **Started:** 2026-09-04T05:15:00Z (Task 1, prior session) / this session began at the checkpoint
- **Completed:** 2026-09-04T05:48:00Z
- **Tasks:** 3 of 3 complete
- **Files modified:** 7 (companion/static/style.css, companion/test_config_page.py, 4 skill files, 06.6.1-UI-SPEC.md)

## Accomplishments

**Task 1 (prior session, unchanged):** Full 16-harness suite passed at 92% coverage; all 16 theme-preview routes verified live end to end (200/image/png, correct refusals, working disk cache, pairwise-distinct mean RGB).

**Task 2 (this session) — developer checkpoint recorded and resolved:**
- Recorded the developer's verbatim response (see `coverage` D5 above and Decisions Made). Every lettered/numbered checkpoint item was approved as shipped — the theme-chip grid, the full heading ladder (including D-09's deliberate second reversal of the nested-card-title tier, specifically confirmed "oui"), the unified label voice, and the spacing/density pass. No item was rejected or rolled back.
- The developer flagged one real, site-wide problem: the selected-state visual treatment (a 2px border plus a small stroke-only check glyph, with zero background difference from unselected siblings) was too weak to notice at a glance — confirmed via `getComputedStyle` inspection showing the selected chip's background was identical to an unselected chip's.
- Diagnosed as a pre-existing weak idiom `.theme-chip--selected` inherited verbatim from `.runway-card--selected` per this phase's own CONTEXT.md D-01 instruction — made newly visible by the chip grid's greater density (16 small photo-heavy chips vs. 3 large runway cards), not a new defect plan 05 introduced.
- **Fix applied:** added a `color-mix(in srgb, var(--color-accent) 12%, transparent)` background wash to both `.runway-card--selected` (whole card — no separate body wrapper exists, and the wash does not obscure the card's number/optional airport-diagram image) and `.theme-chip--selected .theme-chip__body` (body only — deliberately NOT the whole chip, since the chip's `.theme-chip__preview` band is a real rendered theme image that must stay visually untinted). This reuses `.theme-form .theme-option--active`'s and the nav-pill active-link rules' own already-established 12%-accent wash idiom, giving the app exactly one "this is the current selection" visual language instead of two. The existing border and check glyph on both components are unchanged — the fix is additive.
- Extended `companion/test_config_page.py` with one new harness check (`_selected_runway_card_and_theme_chip_carry_a_background_wash`) proving the wash exists on both selectors and is correctly NOT present on the whole `.theme-chip--selected` rule itself. `EXPECTED_CHECK_COUNT` moved from the real on-disk value (69) to 70, re-derived by running the harness, not by arithmetic.
- Extended `companion/static/style.css`'s header accent-reservation comment and `06.6.1-UI-SPEC.md`'s Color section with a note that both selected cards gained this third accent property, per the header comment's own "extend it there first" instruction.
- **Verified visually, not just via harness assertions:** started a real local companion instance against a fresh, disposable state dir (no production snapshot present in this environment, matching Task 1's own finding), logged in via a real form submission inside a headless-Chromium browser context (driven over raw CDP with a hand-rolled minimal WebSocket client, since no external Python websocket library or MCP browser tool was available/reachable), and captured screenshots of `/settings` in light and dark mode, at desktop (1440px) and mobile (390px) widths. In every combination the selected White theme chip and the selected Piste 3 runway card are now clearly, immediately distinguishable from unselected siblings at a glance.
- Ran the full suite again (`scripts/run-all-tests.sh`): 16/16 harnesses pass, 92% coverage, `==> Result: PASS`, unchanged from Task 1.

**Task 3 (this session) — design system of record brought current:**
- `SKILL.md`'s `<design_direction>`: Typography (four sizes now 14/16/22/32px, reordered serif stack, page-title semibold weight, `.site-title`'s 24px brand role, the retired serif Label-role exception, the new unified 12px label voice, the nested-card-title tier's third round-trip step explained so it doesn't read as a loop, the now-moot stat-tile-caption weight adjudication); Spacing (one 24px sibling-card gap, roomier card padding including `.battery-trend-section`, mobile-first `.section-intro`); Cards (`.theme-chip` added to the card set, the new selected-card background-wash treatment on both `.runway-card` and `.theme-chip`); Controls and density (sub-960px button growth as a D-01 extension, the theme-chip radios joining the exempt-by-delegation register); Accent reservation (the wash noted as a third accent property on the two already-listed components); Navigation (06.6.4.1 marked closed).
- `SKILL.md`'s `<metadata>`: appended one chronological Folded-In Work entry for phase 06.6.4.1.1 summarising everything above; added `004-theme-picker-chips (winner: B)` and `005-type-ladder-health-page (winner: B)` to Processed Sketches; updated "Current as of" to 2026-09-04.
- `references/visual-direction-typography.md`: rewrote the Headings, `.site-title`, Stat-tile-captions and Nested-card-title-tier entries to describe the current state, with the three-step round trip's third step explicit about what makes it not a repeat of the first; updated the CSS Patterns code block's serif-stack/size tokens and the Origin footer.
- `references/control-density.md`: added the sub-960px button-growth entry, extended the hidden-form-controls and card-hairline-at-rest entries to include `.theme-chip`, added the new Selected-card-treatment entry documenting the background-wash fix, extended the CSS Patterns block and What-to-Avoid list, updated the Origin footer.
- `references/settings-page-patterns.md`: added "The Theme group is a chip grid, not a fieldset" and "The flash-banner slot" design-decision entries, updated the Origin footer.
- Automated verification passed: `SKILL.md` mentions `06.6.4.1.1` (17x), `004-theme-picker-chips`, `005-type-ladder-health-page`, and `theme-chip`; SUPERSEDED count in `visual-direction-typography.md` rose from 13 to 18. Hand spot-check confirmed every quoted token value (font sizes, serif stack, all seven spacing tokens) matches `companion/static/style.css`'s own `:root` declarations exactly.

## Task Commits

1. **Task 1: Full-suite and live-route gates** — `110f1c7` (docs, prior session; no source-file changes per its own `<files>` spec)
2. **Task 2: Developer checkpoint resolution — background-wash fix** — `d4fb3ec` (fix)
3. **Task 3: Bring the design system of record up to date** — `4f32eb2` (docs)

_The blocking checkpoint itself (Task 2's `<action>`) produced no code by design; `d4fb3ec` is the code change the checkpoint's own resume-signal requested and this execution applied per the plan's instruction: "if they reject any numbered item, make the requested change."_

## Files Created/Modified

- `companion/static/style.css` — `.runway-card--selected` and `.theme-chip--selected .theme-chip__body` gained a 12%-accent `color-mix()` background wash; header accent-reservation comment extended
- `companion/test_config_page.py` — one new harness check proving the wash exists on both selectors and is correctly scoped; `EXPECTED_CHECK_COUNT` 69 → 70
- `.claude/skills/sketch-findings-skypane/SKILL.md` — Typography/Spacing/Cards/Controls/Accent/Navigation entries updated, metadata appended
- `.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md` — Headings, site-title, stat-tile-caption, nested-card-title-tier entries rewritten; CSS Patterns and Origin updated
- `.claude/skills/sketch-findings-skypane/references/control-density.md` — sub-960px button growth, `.theme-chip` additions, new selected-card-treatment entry, CSS Patterns and Origin updated
- `.claude/skills/sketch-findings-skypane/references/settings-page-patterns.md` — two new design-decision entries (theme-chip grid, flash-banner slot), Origin updated
- `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md` — Accent row extended with the background-wash note, per style.css's own "extend it there first" instruction

## Decisions Made

See `key-decisions` in the frontmatter above for the full text. Summarised: the developer approved the entire revised visual contract; the one real fix requested (selected-state background wash) was diagnosed as a pre-existing weak idiom made newly visible by the chip grid's density, fixed by reusing an idiom already established elsewhere in the app (`.theme-form .theme-option--active`), verified both by an automated harness check and by real-browser screenshots in both themes/both widths; Task 3 was executed only after the checkpoint resolved, per the plan's own sequencing requirement.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1/2 — developer-requested fix, applied per the plan's own checkpoint-rejection protocol] Selected-state background wash added to `.runway-card--selected` and `.theme-chip--selected`**
- **Found during:** Task 2's checkpoint response
- **Issue:** the border-only + check-glyph selected-state treatment was too subtle to notice at a glance, site-wide, per the developer's real-device review
- **Fix:** added a 12%-accent `color-mix()` background wash to both components, additive to their existing border/check glyph, matching `.theme-form .theme-option--active`'s established idiom
- **Files modified:** `companion/static/style.css`, `companion/test_config_page.py`, `.claude/skills/sketch-findings-skypane/*` (documentation of the fix), `06.6.1-UI-SPEC.md`
- **Verification:** new harness check (70/70 pass), full suite still PASS at 92% coverage, real-browser screenshots in light/dark × desktop/mobile confirm clear visual distinction
- **Committed in:** `d4fb3ec` (fix), `4f32eb2` (docs)

This is not a Rule 1-3 auto-fix in the strict sense (it was explicitly requested by the developer at a blocking checkpoint, per the plan's own text: "if they reject any numbered item, make the requested change and re-present that item before continuing") — logged here for completeness since it is the one piece of new code this plan produced.

---

**Total deviations:** 1 (developer-requested fix at the blocking checkpoint, applied per the plan's own protocol — not a defect in prior plans' work).
**Impact on plan:** No scope creep beyond what the checkpoint's own resume-signal explicitly authorized.

## Issues Encountered

None. The local companion instance and the headless-Chromium browser process used for real-device visual verification were both started, exercised, and cleanly stopped after verification completed.

## User Setup Required

None.

## Next Phase Readiness

**Phase 06.6.4.1.1 is now complete.** All three tasks of its final plan (06) are done: the full-suite/live-route gates hold, the blocking developer checkpoint approved the entire revised visual contract with one fix requested and applied, and the design system of record (`sketch-findings-skypane` skill) accurately describes the shipped stylesheet with every superseded value marked in place. No blockers remain. STATE.md, ROADMAP.md and REQUIREMENTS.md are updated as part of this plan's own metadata commit, following this SUMMARY.

---
*Phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: .planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-06-SUMMARY.md
- FOUND commit: 110f1c7
- FOUND commit: d4fb3ec
- FOUND commit: 4f32eb2
- VERIFIED: companion/static/style.css declares `.runway-card--selected { ... background: color-mix(in srgb, var(--color-accent) 12%, transparent); }` and `.theme-chip--selected .theme-chip__body { background: color-mix(in srgb, var(--color-accent) 12%, transparent); }`
- VERIFIED: `server/.venv/bin/python3 companion/test_config_page.py` exits 0, config-page: 70/70 checks pass
- VERIFIED: `scripts/run-all-tests.sh` exited 0 with `==> Result: PASS` printed (16/16 harnesses, 92% coverage)
- VERIFIED: SKILL.md mentions 06.6.4.1.1 (17x), 004-theme-picker-chips, 005-type-ladder-health-page, theme-chip
- VERIFIED: SUPERSEDED count in visual-direction-typography.md rose from 13 to 18
- VERIFIED: real-browser screenshots (light/dark × desktop/mobile) show the selected theme chip and runway card clearly distinguishable from unselected siblings
