---
phase: 06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass
plan: 05
type: execute
wave: 3
depends_on: ["06.6.4.1.1-01", "06.6.4.1.1-03"]
files_modified:
  - companion/static/style.css
  - companion/pages/config_page.py
  - companion/test_config_page.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settings' Theme group renders 16 compact selectable chips in a wrapping grid, not 16 stacked 44px native radios."
    - "Each chip shows a real rendered preview of that theme's panel, plus the theme's colour swatches and its name."
    - "All 16 previews show the same fictional scene, so the only visible difference between chips is the theme."
    - "The currently-saved theme's chip carries an accent border and a check glyph."
    - "The Theme group sits in the same rounded, hairline-bordered card Runway and Diagnostic LED use — no bare square fieldset remains on the page."
    - "Selecting a chip and saving still persists the theme, with keyboard and no-JS selection working natively."
  artifacts:
    - "companion/static/style.css: the .theme-chip component rules"
    - "companion/pages/config_page.py: the rewritten multi-theme branch of theme_fieldset()"
  key_links:
    - "config_page.py rebinds THEME_PREVIEW_ROUTE_PREFIX / THEME_PREVIEW_ALT_TEMPLATE from companion/theme_preview.py — one definition site, never a re-typed literal"
    - "The chip's <input type=radio name=theme value={id}> is unchanged in name/value/checked semantics, so handle_post()'s existing THEME_IDS membership validation keeps working untouched"
    - "The visually-hidden radio's 44px touch-target floor is delegated to the wrapping .theme-chip label, exactly as quick task 260902-l9w recorded for the Runway radios"
---

<objective>
Replace Settings' 16 stacked native radios with the sketch-004-B chip grid: a wrapping
row of ~160px selectable cards, each carrying a real rendered theme preview band, the
theme's two colour swatches and its name, wrapped in the same `.theme-status` card idiom
Runway and Diagnostic LED already use.

Purpose: UIR-01 is the audit's only BLOCKER and the first thing an operator sees after
login — sixteen 44px radio circles in a bare square fieldset, giving no colour information
for a colour setting. D-01/D-02/D-08 lock the chip treatment; D-03 through D-07 add the
rendered preview, which plan 01 already built and serves.

Output: `.theme-chip*` rules in `style.css`, a rewritten `theme_fieldset()` multi-theme
branch, and a retargeted `companion/test_config_page.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-CONTEXT.md
@.planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-01-SUMMARY.md
@.planning/sketches/004-theme-picker-chips/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The .theme-chip component in style.css (D-01, D-02, D-07)</name>

  <files>companion/static/style.css</files>

  <read_first>
  - `.planning/sketches/004-theme-picker-chips/index.html` — variant B's CSS block
    (`#variant-b`, lines ~47-57). This is the developer-confirmed winner and the concrete
    reference for the chip's geometry: 160px width, 56px preview band, 8px/10px body
    padding, 13px semibold name, 12px swatch dots. Adapt its raw values onto this file's
    tokens where a token exists; keep its literals where none does.
  - `companion/static/style.css` — the `.runway-card` block (~lines 1543-1621) in full,
    including `.runway-card:not(.runway-card--selected):hover/:focus-within`,
    `.runway-card--selected`, `.runway-card__check` and
    `.runway-card--selected .runway-card__check`. The chip reuses this mechanism
    verbatim in shape; read the `:not()`-scoping comment (D-03a) before writing the hover
    rule.
  - `companion/static/style.css` — the global `label { display: block; font-size: 14px;
    margin-bottom: 8px }` rule (~line 1226) and the global `input, select { min-height:
    44px; … }` rule (~line 1239). Both reach an unclassed label/radio; the chip must
    override the first and the radio must escape the second via `.visually-hidden`.
  - `companion/static/style.css` lines 10-26 — the accent-reservation contract in the
    file's header comment, which currently names "the selected runway card's border and
    check glyph". It is stated to be exhaustive on purpose.
  - `companion/static/style.css` — `.theme-form .theme-option` (the light/dark UI theme
    segmented control in the nav footer). Different component, confusingly adjacent name;
    make sure your new rules cannot reach it.
  </read_first>

  <action>
  Add a `.theme-chip` component block to `companion/static/style.css`, placed immediately
  after the `.runway-card` family so the two selectable-card components read as neighbours
  in the file. Open it with a comment stating: this is sketch 004 variant B, the
  developer-confirmed winner; it deliberately reuses `.runway-card`'s hidden-radio /
  server-computed `--selected` modifier / always-present check-glyph mechanism rather than
  inventing a second selectable-card idiom; and it is NOT related to `.theme-form
  .theme-option`, which picks the companion's own light/dark UI theme.

  Rules:

  - `.theme-chip-grid` — `display: flex; flex-wrap: wrap; gap: var(--space-sm);`
  - `.theme-chip` — `display: block; position: relative; width: 160px; margin-bottom: 0;
    font-size: var(--font-body-size); background: var(--color-dominant); border: 1px solid
    var(--color-border); border-radius: var(--radius-control); box-shadow: none; overflow:
    hidden; cursor: pointer;` The `margin-bottom: 0` and `font-size` overrides are
    load-bearing — without them the global `label` rule gives every chip an 8px bottom
    margin on top of the grid's own gap and shrinks its text to Label size. Say so in a
    comment. `overflow: hidden` is what clips the preview image to the chip's rounded
    corners.
  - `.theme-chip:not(.theme-chip--selected):hover, .theme-chip:not(.theme-chip--selected):focus-within`
    — `border-color: transparent; box-shadow: var(--shadow-card-hover);` The `:not()`
    scoping is the same D-03a property `.runway-card` documents: hovering a selected chip
    must never be able to clear its accent border, independent of source order.
  - `.theme-chip--selected` — `border: 2px solid var(--color-accent);` (the global
    `box-sizing: border-box` reset keeps unselected siblings from shifting).
  - `.theme-chip__preview` — `display: block; width: 100%; height: 56px; object-fit: cover;`
    Comment that 56px is sketch B's validated band height and that `object-fit: cover`
    absorbs the small ratio difference between the served 320x120 PNG and the chip's own
    ~156px-wide box, so the band never letterboxes (the UIR-07 defect).
  - `.theme-chip__body` — `display: block; padding: var(--space-sm);`
  - `.theme-chip__name` — `display: block; font-size: 13px; font-weight:
    var(--weight-semibold); line-height: 1.2; margin-bottom: var(--space-xs);` The 13px is
    the documented sub-scale exception tier, not a new content size.
  - `.theme-chip__swatches` — `display: flex; gap: var(--space-xs); align-items: center;`
  - `.theme-chip__dot` — `width: 12px; height: 12px; border-radius: 3px; border: 1px solid
    var(--color-border); display: inline-block;` Carry over `.theme-swatch__chip`'s own
    comment reasoning: the fill is an inline style holding a raw panel `PALETTE_RGB` value,
    the one colour in this file the stylesheet cannot reason about, and the structural
    hairline is what keeps a white swatch visible on a white card surface.
  - `.theme-chip__check` — `position: absolute; top: var(--space-sm); right: var(--space-sm);
    color: var(--color-accent); display: none;` and
    `.theme-chip--selected .theme-chip__check { display: inline-flex; }`

  Then extend the file's header accent-reservation list: the entry currently reading "the
  selected runway card's border and check glyph" must also cover the selected theme chip's
  border and check glyph. This is a genuine broadening of an exhaustive list, so make it
  an explicit edit with the phase id in the wording, in the same style as the 260902-ep7
  broadening note already in that comment. The comment also says not to broaden the list
  without updating `06.6.1-UI-SPEC.md`'s Color section — record in the plan SUMMARY that
  the same one-line extension belongs there, and apply it if that file is still present at
  `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md`.

  Touch-target note for the comment: `.theme-chip` is ~160px wide and roughly 100px tall,
  far above the 44px floor, and the visually-hidden radio inside it is exempt-by-delegation
  exactly as quick task 260902-l9w recorded for the Runway radios. This adds no entry to
  the traded-floor register.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_contrast_check.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c '^\.theme-chip' companion/static/style.css` is at least 7.
  - `grep -c '\.theme-chip:not(\.theme-chip--selected):hover' companion/static/style.css` is 1.
  - `awk '/^\.theme-chip \{/,/^}/' companion/static/style.css | grep -c 'margin-bottom: 0'` is 1 —
    the global `label` margin is neutralised.
  - `awk '/^\.theme-chip__preview \{/,/^}/' companion/static/style.css | grep -c 'object-fit: cover'` is 1.
  - `grep -c 'url(' companion/static/style.css` is 0.
  - `server/.venv/bin/python3 companion/test_contrast_check.py` and
    `server/.venv/bin/python3 companion/test_status_pages.py` both exit 0.
  </acceptance_criteria>

  <done>A .theme-chip component exists in style.css, reusing .runway-card's selection mechanism, and the accent-reservation list names its new accent use.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite theme_fieldset()'s multi-theme branch as the chip grid (D-01, D-02, D-03, D-08)</name>

  <files>companion/pages/config_page.py</files>

  <read_first>
  - `companion/pages/config_page.py` lines 167-345 — `_palette_hex()`, `theme_fieldset()`
    (both branches) and `runway_fieldset()` in full. `runway_fieldset()` is the structural
    template: the `.theme-status` wrapper carrying `DIRTY_SECTION_ATTR`, the `<h2
    class="text-heading">` group name, the single `.section-caption` paragraph, the inner
    layout row, and the per-card `<label>` holding a `class="visually-hidden"` radio, a
    server-computed `--selected` modifier and an always-present check glyph.
  - `companion/pages/config_page.py` lines 20-47 — the route-prefix / one-definition-site
    rebinding discipline (`RUNWAY_IMAGE_ROUTE_PREFIX`, `SETTINGS_ROUTE`,
    `SETTINGS_FORM_ID`).
  - `companion/pages/config_page.py` lines 674-751 — `handle_post()`. Its theme validation
    is a membership test on `form.get("theme")` against `device_config.THEME_IDS`. Nothing
    in this task may change the radio's `name`, `value` or `checked` semantics, or that
    validation silently stops matching.
  - `.planning/phases/06.6.4.1.1-.../06.6.4.1.1-01-SUMMARY.md` — the exact names plan 01
    exported (`THEME_PREVIEW_ROUTE_PREFIX`, `THEME_PREVIEW_ALT_TEMPLATE`).
  - `companion/layout.py::icon_html` — how `runway_fieldset()` gets its check glyph.
  </read_first>

  <action>
  Rebind at module level in `companion/pages/config_page.py`:
  `THEME_PREVIEW_ROUTE_PREFIX = theme_preview.THEME_PREVIEW_ROUTE_PREFIX` and
  `THEME_PREVIEW_ALT_TEMPLATE = theme_preview.THEME_PREVIEW_ALT_TEMPLATE`, importing
  `from companion import theme_preview`. Comment that this mirrors
  `RUNWAY_IMAGE_ROUTE_PREFIX`'s own rebinding discipline with the definition site inverted:
  the mechanism module owns the prefix here, and both this module and `companion/app.py`
  rebind from it, so the literal is still typed exactly once in the repository.

  Rewrite `theme_fieldset()`'s multi-theme branch (the `<fieldset>/<legend>` path) to emit:

  ```
  <div class="theme-status" data-dirty-section="Theme">
    <h2 class="text-heading">Theme</h2>
    {caption_html}
    <div class="theme-chip-grid">
      ...one chip per THEME_IDS entry...
    </div>
  </div>
  ```

  and per chip:

  ```
  <label class="theme-chip[ theme-chip--selected]">
    <input type="radio" name="theme" value="{id}" class="visually-hidden"[ checked]>
    <img class="theme-chip__preview" src="{THEME_PREVIEW_ROUTE_PREFIX}{id}.png"
         alt="{THEME_PREVIEW_ALT_TEMPLATE % label}" width="320" height="120"
         loading="lazy" style="background:{departing_hex}">
    <span class="theme-chip__body">
      <span class="theme-chip__name">{label}</span>
      <span class="theme-chip__swatches" aria-hidden="true">
        <span class="theme-chip__dot" style="background:{departing_hex}"></span>
        <span class="theme-chip__dot" style="background:{arriving_hex}"></span>
      </span>
    </span>
    <span class="theme-chip__check">{icon}<span class="visually-hidden">Selected</span></span>
  </label>
  ```

  Details that are decisions, not incidental — each needs a comment:
  - The `caption_html` local computed at the top of `theme_fieldset()` is already shared by
    both branches (quick task 260901-re6). Keep using it; do not give the new branch its
    own copy of that markup.
  - `<span>`, never `<p>`, for the chip body and name: a `<p>` inside a `<label>` is
    invalid markup, and the surrounding `.theme-chip` is a `<label>`.
  - `aria-hidden="true"` on the swatch row and no alt-bearing role for the dots: the theme's
    name in `.theme-chip__name` is the accessible label, and the dots are decoration
    duplicating what the `<img>`'s alt text already conveys. This mirrors the read-only
    branch's own `<span class="theme-swatch" aria-hidden="true">`.
  - `loading="lazy"` on the preview `<img>` mirrors the Airlines gallery's own precedent
    and keeps the cold-cache render cost off the critical path for chips below the fold.
  - `width="320" height="120"` are the real served pixel dimensions, so the browser reserves
    the correct box before the image arrives (this is the attribute pair `loading="lazy"`
    needs — and note UIR-07's lesson: these attributes beat a CSS `aspect-ratio`, which is
    why `.theme-chip__preview` states an explicit `height` and `object-fit` instead).
  - The `style="background:{departing_hex}"` on the `<img>` is the graceful-degradation
    path: if a single theme's render ever fails and its route 404s, the chip still shows
    that theme's own colour instead of a broken-image glyph.
  - Every interpolated value goes through `escape_html()`, including the theme id in the
    `src`. `_palette_hex()`'s output is server-computed from `PALETTE_RGB` and is not user
    input, but escape it anyway for consistency with the read-only branch.
  - The `--selected` modifier and the radio's `checked` attribute are computed from the
    SAME server-side membership comparison, never a client-side `:has()` trick — the
    property `runway_fieldset()`'s docstring states.

  Update `theme_fieldset()`'s docstring: the single-theme read-only branch is unchanged,
  the multi-theme branch is now the D-01 chip grid inside the D-08 `.theme-status` wrapper,
  and the `<fieldset>`/`<legend>` pair is gone — which means the whole Settings page now
  renders zero `<fieldset>` and zero `<legend>` elements, all four groups named by an `<h2
  class="text-heading">` at one consistent heading level.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "import companion.pages.config_page as c; from server import device_config as d; r=c.theme_fieldset('grey'); assert '<fieldset' not in r; assert '<legend>' not in r; assert 'theme-chip-grid' in r; assert r.count('theme-chip--selected') == 1; assert r.count('/theme-preview/') == len(d.THEME_IDS); assert '/theme-preview/grey.png' in r; print('chip grid OK')"</automated>
  </verify>

  <acceptance_criteria>
  - The verify command above exits 0.
  - `grep -c 'THEME_PREVIEW_ROUTE_PREFIX' companion/pages/config_page.py` is at least 2
    (rebinding plus the `src` interpolation), and
    `grep -c '"/theme-preview/"' companion/pages/config_page.py` is 0 — the literal is
    typed once, in `companion/theme_preview.py`, and nowhere else.
  - `server/.venv/bin/python3 -c "import companion.pages.config_page as c; r=c.render({'device_config': {'theme':'blue','tracked_runway':'3','led_enabled':True},'poll_cooldown_remaining':0}); assert r.count('class=\"theme-status\"') == 3; assert '<fieldset' not in r; print('page shape OK')"` exits 0 — Theme, Runway and LED are all `.theme-status` cards and no bare fieldset remains.
  - `server/.venv/bin/python3 -c "import companion.pages.config_page as c; assert c.handle_post({'theme':'nope','tracked_runway':'3'}, {'state_dir':'/tmp'}) == c.FLASH_SAVE_FAILED; print('validation intact')"` exits 0 — the POST-side membership validation still rejects a non-member theme.
  </acceptance_criteria>

  <done>Settings' Theme group renders 16 preview-bearing chips inside a .theme-status card; no fieldset or legend remains on the page; the radio's name/value/checked contract and the POST validation are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget companion/test_config_page.py onto the chip contract</name>

  <files>companion/test_config_page.py</files>

  <read_first>
  - Run `server/.venv/bin/python3 companion/test_config_page.py` FIRST and read every
    failure before editing anything. The failures are the work list; this task's job is to
    retarget each one onto the new contract, never to weaken or delete a check.
  - `companion/test_config_page.py` lines 40-125 — the `EXPECTED_CHECK_COUNT` comment
    block and its inline-arithmetic convention.
  - The checks known to encode the retired fieldset/radio-list contract:
    `_render_shape_theme_radio_group_runway_cards_led_group_and_save_button` (~line 287,
    asserts exactly one `<fieldset`, a `<legend>Theme</legend>`, and exactly two
    `class="theme-status"` groups); the `<legend>Theme</legend>` count check (~line 349);
    `_theme_fieldset_one_radio_per_registry_entry` (~494);
    `_theme_fieldset_falls_back_to_radio_group_when_multiple_themes_registered` (~557);
    `_theme_fieldset_covers_every_registered_theme_with_own_id_and_label` (~601);
    `_theme_fieldset_default_selects_exactly_the_white_option` (~628);
    `_current_theme_and_runway_are_selected` (~869);
    `_style_css_carries_theme_status_runway_row_and_led_checkbox_selectors` (~1629).
  - `_theme_fieldset_single_theme_renders_read_only_status_with_real_swatch_hex` (~514) —
    this one covers the UNCHANGED single-theme branch and must keep passing as-is. If it
    fails, the read-only branch was damaged; fix the code, not the check.
  </read_first>

  <action>
  Retarget the failing checks in place, keeping each one's identity, position and intent:

  - Fieldset/legend counts become zero, and the `class="theme-status"` group count becomes
    three (Theme, Runway, Diagnostic LED). Rewrite each affected check's name, comment and
    `check()` description string so none of them still claims Theme is a `<fieldset>` —
    a check whose prose contradicts its assertions is worse than no check, and this file
    already carries a long comment trail about exactly that.
  - The radio-per-registry-entry, covers-every-theme, default-selection and
    current-theme-selected checks should keep asserting the same underlying facts (one
    `<input type="radio" name="theme" value="{id}">` per `device_config.THEME_IDS` entry,
    every id and label present, exactly one `checked`, the right one selected) — those
    facts are unchanged by the redesign and are what keeps `handle_post()` working. Only
    the surrounding markup expectations move.
  - Extend `_style_css_carries_theme_status_runway_row_and_led_checkbox_selectors` with the
    new component's own cross-file guard, following its existing index-plus-window
    technique (never a regex CSS parser): `.theme-chip-grid {` declares `display: flex`;
    `.theme-chip {` declares the card surface token and `width: 160px`;
    `.theme-chip--selected {` declares `var(--color-accent)`;
    `.theme-chip__preview {` declares `height: 56px`.

  Then add these NEW checks, bumping `EXPECTED_CHECK_COUNT` with an inline arithmetic
  comment in the file's established style:

  1. Every chip's `<img src>` points at `theme_preview.THEME_PREVIEW_ROUTE_PREFIX` +
     the theme's own id + `.png`, for every entry in `device_config.THEME_IDS` — the
     cross-module route contract, asserted against the constant rather than a re-typed
     literal.
  2. Each chip carries exactly two `.theme-chip__dot` spans whose inline background values
     equal `_palette_hex(THEMES[id]["departing_index"])` and
     `_palette_hex(THEMES[id]["arriving_index"])` — the swatches show real panel palette
     colours, computed, never hardcoded.
  3. Every chip's radio carries `class="visually-hidden"` and every chip carries a
     `.theme-chip__check` glyph with its visually-hidden "Selected" text, present on all 16
     chips regardless of selection — the markup half of the CSS-only selection reveal.
  4. The rendered Settings page contains no `<fieldset` and no `<legend`, and exactly three
     `data-dirty-section` groups — so `dirty-state.js`'s section-aware walk still finds
     Theme as one addressable unit after the rewrite.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>

  <acceptance_criteria>
  - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 and its printed total
    equals the bumped `EXPECTED_CHECK_COUNT`.
  - `grep -c 'theme-chip' companion/test_config_page.py` is at least 6.
  - `grep -c 'THEME_PREVIEW_ROUTE_PREFIX' companion/test_config_page.py` is at least 1 —
    the route contract is asserted against the constant, not a re-typed literal.
  - No check was neutralised:
    `git diff companion/test_config_page.py | grep -c '^+.*return True, ""$'` is 0 outside
    of genuinely new checks' normal terminations — inspect the diff by hand and record in
    the SUMMARY that every removed assertion was replaced by an equivalent one.
  - All five companion harnesses exit 0:
    `for f in companion/test_companion_app.py companion/test_config_page.py companion/test_contrast_check.py companion/test_status_pages.py companion/test_view_pages.py; do server/.venv/bin/python3 "$f" >/dev/null || echo "FAIL $f"; done` prints nothing.
  </acceptance_criteria>

  <done>The Settings harness asserts the chip contract — preview route, real palette swatches, hidden radios, three dirty-sections, zero fieldsets — with no check weakened to get there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /settings` | The submitted `theme` value crosses into `save_device_config()` |
| server-built markup → browser | 16 `<img src>` values and 32 inline `background:` styles are interpolated into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1.1-16 | Tampering | `handle_post()` theme validation | high | mitigate | The redesign must not change the radio's `name`/`value` semantics. `handle_post()`'s membership test against `device_config.THEME_IDS` is unchanged and re-asserted by an acceptance criterion that submits a non-member theme and requires `FLASH_SAVE_FAILED` |
| T-06.6.4.1.1-17 | Tampering (XSS) | chip markup interpolation | medium | mitigate | Every interpolated value goes through `escape_html()`, including the theme id in the `src` and the alt text. All values originate in the server-controlled `THEMES` registry and `_palette_hex()`, never in a request — escaping is defence in depth, not the primary control |
| T-06.6.4.1.1-18 | Information disclosure | 16 preview requests per Settings load | low | accept | Each `<img>` hits the plan-01 route, which is session-gated and membership-validated. `loading="lazy"` keeps below-the-fold chips off the critical path. No new information is exposed that the authenticated Settings page does not already render in full |
| T-06.6.4.1.1-19 | Denial of service (accessibility) | hidden radio inside the chip label | medium | mitigate | The radio is `class="visually-hidden"`, never `display: none`, so keyboard and no-JS selection still work natively — the property `runway_fieldset()` already documents. The chip label is the 44px+ hit target (exempt-by-delegation, quick task 260902-l9w). Asserted by a new harness check and re-verified by keyboard at the plan-06 checkpoint |
| T-06.6.4.1.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan |
</threat_model>

<verification>
- All five companion harnesses exit 0.
- A local companion run against a copy of the production state dir renders Settings with
  16 chips, each showing a distinguishable preview band, in both light and dark.
- Selecting a different chip marks the Theme section dirty in the save bar, and saving
  persists the new theme.
</verification>

<success_criteria>
- UIR-01 and UIR-02 are closed: no oversized radio list, no bare square fieldset, real
  colour information for a colour setting.
- The chip picker is keyboard- and no-JS-operable.
- The Settings harness asserts the new contract without any check having been weakened.
</success_criteria>

## Artifacts this plan produces

**New CSS classes:** `.theme-chip-grid`, `.theme-chip`, `.theme-chip--selected`,
`.theme-chip__preview`, `.theme-chip__body`, `.theme-chip__name`, `.theme-chip__swatches`,
`.theme-chip__dot`, `.theme-chip__check`

**Extended CSS contract:** the accent-reservation list in `style.css`'s header comment now
covers the selected theme chip's border and check glyph

**New Python constants in `companion/pages/config_page.py`:** `THEME_PREVIEW_ROUTE_PREFIX`,
`THEME_PREVIEW_ALT_TEMPLATE` (both rebound from `companion/theme_preview.py`)

**Rewritten Python function:** `config_page.theme_fieldset()` multi-theme branch

**New harness checks in `companion/test_config_page.py`:** preview-route contract, real
palette swatch values, hidden-radio + check-glyph markup, zero-fieldset / three-dirty-section
page shape, and the `.theme-chip*` cross-file CSS guard

<output>
Create `.planning/phases/06.6.4.1.1-settings-theme-picker-and-typography-spacing-direction-pass/06.6.4.1.1-05-SUMMARY.md` when done.
</output>