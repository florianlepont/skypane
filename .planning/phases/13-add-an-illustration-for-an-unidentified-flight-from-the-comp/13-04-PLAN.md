---
phase: 13-add-an-illustration-for-an-unidentified-flight-from-the-comp
plan: 04
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-03
files_modified:
  - companion/pages/airlines_page.py
  - companion/static/style.css
  - companion/test_status_pages.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Arriving at /airlines?resolve=XYZ shows that prefix's own sighting context — first seen, last seen, times seen, example callsign — re-read server-side from the unresolved-prefix registry, with nothing displayed taken from the query string (D-12)."
    - "A prefix that is not a member of the live unresolved-prefix registry shows the stale-gap card and no form at all (D-11)."
    - "The name field offers the 27 airlines that already have artwork through a native datalist, so choosing a suggestion guarantees the uploaded-or-vendored art actually attaches (D-13)."
    - "The upload step appears only when the named airline has no artwork under its normalised key (D-03), and posts to the existing /illustration/{key}.png route unchanged (D-04)."
    - "Every manual resolution is listed with its state, active or superseded, and can be deleted — and a superseded entry says in words why it no longer applies (D-06, D-07)."
    - "The page gains no JavaScript: the datalist is native and every control is an ordinary form or link."
  artifacts:
    - companion/pages/airlines_page.py
    - companion/static/style.css
  key_links:
    - "unresolved_row_for_prefix() is the single D-11 membership test — companion/app.py's POST handler (plan 13-06) imports and re-runs this exact function rather than re-implementing the check."
    - "manual_resolutions.illustration_key_for_name() is the only way a stored name becomes an illustration key; the page must never slug a name itself."
    - "enrich.static_airline_name_for_prefix() is the supersession oracle — a truthy return is what makes an entry Superseded."
---

<objective>
Build the whole operator-facing surface on Airlines: the conditional resolve section (D-10/D-11/D-12/D-03/D-13), and the always-present manual-resolutions management list with its delete control and superseded marker (D-06/D-07).

Purpose: this is where the coverage gap actually closes. Everything before it was plumbing; everything after it is wiring. The page is written to be defensive about `ctx` — every new key is read with `ctx.get()` — so it renders correctly the moment plan 13-06 starts supplying them, and renders the unchanged gallery until then.

Output: one rewritten `render()`, roughly a dozen new module constants and helpers, and four CSS additions.

**Copy is binding.** `13-UI-SPEC.md`'s Full Copy Deck was approved 6/6 by the UI checker. Use its strings byte for byte. Three strings and two flash keys not in that deck are specified below and are flagged as such — they cover states the deck did not enumerate, which `13-CONTEXT.md` explicitly leaves to Claude's discretion ("All copy: ... the form's labels and hints, the success/rejection flashes").
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-CONTEXT.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UI-SPEC.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-PATTERNS.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-01-SUMMARY.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-03-SUMMARY.md
@.claude/skills/sketch-findings-skypane/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The resolve section — server-side prefix validation, sighting context, datalist name form, conditional upload step (D-03, D-11, D-12, D-13)</name>

  <files>companion/pages/airlines_page.py, companion/static/style.css, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/airlines_page.py` in full — especially the module docstring's note on why it duplicates rather than imports `ILLUSTRATION_ROUTE_PREFIX` (app.py imports this module, so the reverse would be a cycle), the `FLASH_ILLUSTRATION_*` constants at lines 138-190 and the comment explaining why they live here rather than in `app.py`, `REPLACE_HINT_TEXT` at line 187, `_lightbox_replace_form_html()` at line 270, `_filter_bar_html()` at line 507, and `render(ctx)` at line 544 with its `ctx.get()`-not-`ctx[...]` comment.
    - `companion/pages/health_page.py` lines 1843-1880 — `unresolved_rows()`. Do **not** import it: `companion/pages/__init__.py` forbids a page module importing another page module. Reimplement the single-entry lookup locally against `server.poll_loop.load_poll_state()`, which is the sanctioned crossing point (`health_page.py` itself reaches `poll_loop` exactly this way).
    - `companion/layout.py` — `escape_html()` (429), `concise_timestamp_html()` (536), `empty_state()` (1163), `icon_html()` (388), `page_header()` (1176).
    - `server/plane/manual_resolutions.py` as shipped by 13-01 — `load_manual_resolutions()`, `illustration_key_for_name()`, `entry_rows()`.
    - `server/plane/illustrations.py` lines 668-682 (`resolved_illustration_path()`) and 812-830 (`target_airline_names()`).
    - `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UI-SPEC.md` § Page Composition — Airlines item 1, § Full Copy Deck, § Accessibility & JS-Free Degradation Notes.
    - `companion/static/style.css` lines 4355-4510 — the `.lightbox__replace-zone` rule and its five descendant rules.
  </read_first>

  <behavior>
    - `render({})` (a literal empty dict, as `companion/test_view_pages.py` already calls it) renders the gallery exactly as before and no resolve section.
    - `render(ctx)` with `resolve_prefix="XYZ"` and a state dir whose `poll_state.json` has no `XYZ` entry renders the stale card: the back link plus the approved stale sentence, and no name field and no file input.
    - `render(ctx)` with `resolve_prefix="XYZ"` and a seeded registry entry for `XYZ` renders Step A: the heading, the caption naming the prefix, the five-row context list populated from the *registry* values, a hidden prefix field, the name input wired to the datalist, the hint, and the submit button.
    - The rendered datalist contains exactly 27 option elements, one per `illustrations.target_airline_names()`, each value escaped once.
    - With a manual entry for `XYZ` naming an airline that has **no** artwork, Step B renders: the heading naming that airline, the caption, the same context list, the upload zone posting to `/illustration/{key}.png` with the key recomputed server-side from the stored name, and the skip link.
    - With a manual entry for `XYZ` naming an airline that **does** have artwork, the already-resolved card renders instead — no upload zone.
    - A hostile prefix or a hostile stored airline name (angle brackets, quotes, an ampersand) renders fully escaped everywhere it appears, including inside the form action and the hidden field value.
    - The page contains no script element and no inline event-handler attribute.
  </behavior>

  <action>
**Imports.** Add `from server.plane import manual_resolutions`, `from server.plane import enrich`, and `import server.poll_loop as poll_loop` alongside the existing `from server.plane import illustrations`. `health_page.py` already reaches `server.poll_loop` this way and `companion/pages/__init__.py` sanctions it; a page-to-page import is what is forbidden, and none is added.

**Flash constants.** Following this module's existing `FLASH_ILLUSTRATION_*` convention and the comment that explains why they live here (app.py imports this module; the reverse would be a cycle), add:
- `FLASH_MANUAL_RESOLVED = "manual_resolved"`
- `FLASH_MANUAL_NAME_EMPTY = "manual_name_empty"`
- `FLASH_MANUAL_NAME_TOO_LONG = "manual_name_too_long"`
- `FLASH_MANUAL_NAME_RESERVED = "manual_name_reserved"`
- `FLASH_MANUAL_PREFIX_STALE = "manual_prefix_stale"`
- `FLASH_MANUAL_REGISTRY_FULL = "manual_registry_full"`
- `FLASH_MANUAL_SAVE_FAILED = "manual_save_failed"` — **planner addition, not in the UI-SPEC deck.** Justification, to be written into the comment above it: `manual_resolutions.add_entry()` returns `ADD_FAILED` on an unwritable state dir and never raises; without this key that failure would redirect with no message at all, and `13-CONTEXT.md`'s governing constraint is that the operator is never silently misled. It mirrors `FLASH_ILLUSTRATION_REPLACE_FAILED`'s existing role and voice exactly.
- `FLASH_MANUAL_DELETE_FAILED = "manual_delete_failed"` — same justification, for `delete_entry()` returning `False` after a write failure.

Note in a comment that deleting a manual resolution deliberately produces **no success flash**: the UI-SPEC deck has none, and the row disappearing from the list is the confirmation. Only the failure path speaks.

**Route constants** (duplicated-not-imported, exactly as `ILLUSTRATION_ROUTE_PREFIX` already is, with the same explanatory comment style):
- `RESOLVE_ROUTE = "/airlines/resolve"`
- `MANUAL_DELETE_ROUTE_PREFIX = "/airlines/manual-resolutions/"`
- `MANUAL_DELETE_ROUTE_SUFFIX = "/delete"`
- `AIRLINES_ROUTE = "/airlines"`
- `RESOLVE_QUERY_PARAM = "resolve"`

**Copy constants** — byte-identical to `13-UI-SPEC.md`'s Full Copy Deck (real U+2014 em dashes, real U+2019 apostrophes, matching every other string in this module):
- `RESOLVE_BACK_LINK_TEXT = "← Back to Health"`
- `RESOLVE_STALE_BODY = "That coverage gap isn't there anymore — it may already be resolved. Check Health for current gaps."`
- `RESOLVE_HEADING = "Resolve an unidentified flight"`
- `RESOLVE_CAPTION_TEMPLATE = "Prefix %s has been detected but never matched a known airline. Give it a name below."`
- `RESOLVE_CONTEXT_LABELS = ("Prefix", "First seen", "Last seen", "Times seen", "Example callsign")`
- `NAME_LABEL_TEXT = "Airline name"`
- `NAME_HINT_TEXT = "Start typing — pick a suggestion if the airline already has artwork, so this reuses it instead of asking for a new upload."`
- `SAVE_BUTTON_TEXT = "Save airline name"`
- `STEP_B_HEADING_TEMPLATE = "Add an illustration for %s"`
- `STEP_B_CAPTION = "Saved. This airline doesn't have artwork yet — add one below, or skip for now."`
- `STEP_B_SKIP_TEXT = "Skip — I'll add artwork later"`
- `RESOLVE_ALREADY_DONE_TEMPLATE = "%s is already named for this prefix and has artwork — nothing more to do here."` — **planner addition, not in the UI-SPEC deck**, covering the fourth reachable state (a bookmark or a Back press landing on a prefix that is still listed as a gap but is already fully resolved). Comment it as such; the deck enumerated three states and this is the fourth. Saying "that gap isn't there anymore" here would be false, and false is the one thing `13-CONTEXT.md` forbids.

Element ids and the datalist id: `MANUAL_NAME_INPUT_ID = "manual-airline-name"`, `MANUAL_UPLOAD_INPUT_ID = "manual-illustration-input"`, `MANUAL_DATALIST_ID = "known-airlines"`.

**`unresolved_row_for_prefix(state_dir, prefix)`** — public, because `companion/app.py` imports and re-runs it as its own D-11 membership test in plan 13-06. It calls `poll_loop.load_poll_state(state_dir)`, reads the `unresolved_prefixes` key, returns `None` unless that value is a dict, unless `prefix` is a string matching exactly three uppercase ASCII letters, unless the entry for it is a dict, and unless its `count` is an int that is not a bool. Otherwise it returns the five-tuple `(prefix, count, first_seen, last_seen, example_callsign)` with the last three defaulting to `""`, mirroring `health_page.unresolved_rows()`'s field handling exactly. Never raises; a missing or unreadable poll state yields `None`. Docstring must state that this is D-11's validate-then-join membership test — the same shape `_serve_illustration_image()` uses against a closed server-controlled set, applied here to the unresolved-prefix registry, which is server-written from genuinely detected traffic — and that it is deliberately shared with the POST handler so the read path and the write path can never diverge.

**`_resolve_context_html(row, now)`** — a definition list carrying `class="resolve-context"`, one `dt`/`dd` pair per `RESOLVE_CONTEXT_LABELS` entry. `dt` carries `class="text-label"`; `dd` carries `class="text-body"`, plus `mono` for the prefix and the example callsign. First seen and Last seen render through `layout.concise_timestamp_html(value, now, fallback="")`, whose return value is already-safe markup and is interpolated verbatim, never re-escaped — the same discipline `health_page._registry_row_html()` documents. Every other value goes through `escape_html()` exactly once.

**`_known_airlines_datalist_html()`** — a datalist element with `id` from `MANUAL_DATALIST_ID` containing one option per `illustrations.target_airline_names()`, each `value` attribute escaped once. Docstring: this is D-13's whole mechanism and it is native — choosing a suggestion is what guarantees the slug derived downstream lands on art the fallback ladder already ships; typing anything else stays available for a genuinely new carrier. No script is involved and none may be added.

**`_resolve_section_html(ctx)`** — returns `""` when `ctx.get("resolve_prefix")` is falsy. Otherwise it opens a `div` carrying `class="page-section"` (an ordinary card: no accent border, no status modifier — its mere presence is the emphasis, per UI-SPEC Autonomous Decision 1), whose first child is an anchor carrying `class="text-label"` and `href="/health"` with `RESOLVE_BACK_LINK_TEXT`. Then it branches on **server-derived state only**, in this order:
1. `unresolved_row_for_prefix(state_dir, prefix)` is `None` -> render `RESOLVE_STALE_BODY` in a `p` carrying `class="text-body"`, and stop. No context block, no controls.
2. Look up `manual_resolutions.load_manual_resolutions(state_dir).get(prefix)`. If absent -> **Step A**: the heading (an `h2` carrying `class="text-heading"`), the caption built from `RESOLVE_CAPTION_TEMPLATE` in a `p` carrying `class="text-label section-caption"`, the context block, then a form with `method="post"` and `action` from `RESOLVE_ROUTE`, containing a hidden input named `prefix` whose value is the *validated* prefix from the tuple (never `ctx`'s raw string), a `div` carrying `class="resolve-name-field"` holding the label, a text input with `id`/`name="airline_name"`, `list` pointing at `MANUAL_DATALIST_ID`, `maxlength="100"`, `required`, `autocomplete="off"` and `autofocus`, the datalist, and the hint paragraph; then a submit button with `SAVE_BUTTON_TEXT`.
3. Entry present. Recompute `key = manual_resolutions.illustration_key_for_name(entry["airline_name"])` — **never take a key from the query string, and never derive a slug locally; this module owns no slug logic of its own and must not call the slug helper in `illustrations` directly.** If `key` is falsy, fall back to the stale card (a stored entry whose name no longer slugs is a corrupt-file case that must not render a form). Then check `illustrations.resolved_illustration_path(key, state_dir)`:
   - `None` -> **Step B**: heading from `STEP_B_HEADING_TEMPLATE` filled with the escaped stored name, `STEP_B_CAPTION`, the same context block, then a `div` carrying `class="resolve-upload-zone"` containing `layout.icon_html("icon-upload", extra_class=REPLACE_ICON_CLASS)`, a label bound to `MANUAL_UPLOAD_INPUT_ID`, a hint paragraph carrying `class` from `REPLACE_HINT_CLASS` whose text is `REPLACE_HINT_TEXT` **reused from the existing constant, never re-typed**, and a form with `method="post"`, `enctype="multipart/form-data"` and `action` built as `ILLUSTRATION_ROUTE_PREFIX + key + ".png"` holding the file input (`name="image"`, `accept="image/png"`, `required`) and its submit button labelled with `REPLACE_BUTTON_TEXT`; then, after the zone, an anchor carrying `class="text-label"` and `href` from `AIRLINES_ROUTE` with `STEP_B_SKIP_TEXT`.
   - not `None` -> the already-resolved card: the heading, then `RESOLVE_ALREADY_DONE_TEMPLATE` filled with the escaped stored name, in a `p` carrying `class="text-body"`. No controls.

Every interpolated prefix, airline name, callsign and key passes through `escape_html()` exactly once, at the point of interpolation.

**`render(ctx)`** — add `_resolve_section_html(ctx)` immediately after `layout.page_header(...)` and before `filter_html`, per UI-SPEC's declared top-to-bottom order. Extend `render()`'s docstring to name the three new ctx keys it now reads with `ctx.get()` (`resolve_prefix`, `state_dir`, `now`) and to record that the page still opens no database. (`manual_resolutions` as a ctx key is read in Task 2.)

**CSS** — three additions to `companion/static/style.css`, each with a one-line comment naming phase 13 and the decision it serves:
- `.resolve-context { margin: 0 0 var(--space-md); }` placed beside `.data-card__details`. Comment: the `dt`/`dd` visual treatment is deliberately inherited, because this file declares no `dl`/`dt`/`dd` rule at all — Health's own registry disclosure renders its definition list on browser defaults, so matching it means adding nothing but the block's own bottom margin, from the existing `md` token.
- `.resolve-name-field { margin-bottom: var(--space-md); }` beside it, same token, no other property.
- Extend the existing `.lightbox__replace-zone` selector group rather than duplicating any declaration: add `.resolve-upload-zone` as a second selector to **all six** rules whose selector currently begins with `.lightbox__replace-zone` — the base rule, the `.lightbox__replace-icon` descendant, the `label` descendant, the `input[type="file"]` descendant, the `::file-selector-button` rule, and its `:hover` rule. Change no declaration inside any of them.

**Harness.** Add three checks to `companion/test_status_pages.py`, moving `EXPECTED_CHECK_COUNT` in this same edit:
5. State machine: with a `tempfile.TemporaryDirectory()` state dir, assert all four states render the right thing and only the right thing — absent-from-registry -> stale sentence present and the name input id absent; seeded gap, no entry -> Step A heading and the name input id present, no file input; seeded gap plus an entry naming an airline with no artwork -> the Step B heading naming that airline and a form action ending `/{key}.png`; seeded gap plus an entry naming one of `target_airline_names()` -> the already-resolved sentence and no file input.
6. Datalist contract: the rendered Step A markup contains exactly `len(illustrations.target_airline_names())` option elements inside the datalist, the datalist id matches the input's `list` attribute, and every airline name appears escaped exactly once.
7. Escaping and query-string distrust: with a stored airline name and an example callsign both containing an angle bracket, a double quote and an ampersand, assert no raw `<`, no raw unescaped `"` inside an attribute, and no raw `&` that is not part of an entity appears in the output; and assert that a `ctx` whose `resolve_prefix` differs in case or padding from the stored registry key renders the stale card rather than the form — nothing displayed comes from the query string (D-12).

Bump `EXPECTED_CHECK_COUNT` from `140` to `143`, extend the ledger comment in this file's established style, and re-derive by running the harness rather than by arithmetic.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `143/143`.
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 — its existing `render({})` call still produces the unchanged gallery.
    - `grep -c '<script' companion/pages/airlines_page.py` returns `0` and `grep -c ' onclick=\| onchange=\| onsubmit=' companion/pages/airlines_page.py` returns `0` — this phase adds zero JavaScript.
    - `grep -c "datalist" companion/pages/airlines_page.py` is at least `2`.
    - `grep -c "def unresolved_row_for_prefix" companion/pages/airlines_page.py` returns `1`.
    - `grep -cE "^(from|import) +companion\.pages" companion/pages/airlines_page.py` returns `0` — the page-module boundary holds; no page module imports another.
    - `grep -cE "^ *(from|import).*normalise_airline_key|illustrations\.normalise_airline_key" companion/pages/airlines_page.py` returns `0` — the page never derives a slug itself; it always goes through `manual_resolutions.illustration_key_for_name()`.
    - `grep -c "resolve-upload-zone" companion/static/style.css` returns `6`.
    - `grep -c "resolve-context\|resolve-name-field" companion/static/style.css` returns `2`.
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0.
  </acceptance_criteria>

  <done>All four resolve states render from server-derived data alone, the native datalist offers all 27 known airlines, the upload step reuses the existing route and hint constant, and the page still ships no script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The manual-resolutions management list — list, status, delete (D-06, D-07, D-08)</name>

  <files>companion/pages/airlines_page.py, companion/static/style.css, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` lines 1975-2115 — `_REGISTRY_HEADERS`, `_registry_row_html()`, `_registry_table_html()`, `_registry_cards_html()`. UI-SPEC directs this list to mirror that desktop-table / mobile-cards sibling pairing structurally: same `.data-table-wrap` / `.data-table` wrapper, same `.data-cards` list, same `row`/`row-alt` alternation convention.
    - `companion/layout.py` `empty_state()` (1163) and `card_status_class()` (1132) — the latter's docstring on why the absence of a coloured edge is itself the signal.
    - `companion/static/style.css` lines 2610-2625 (`.data-card__label`, which carries the label voice by value) and 2929-2942 (`.stat-tile__caption`, the same voice again).
    - `.claude/skills/sketch-findings-skypane/SKILL.md` — the Label voice bullet: six selectors, one shared declaration set, **each written out separately, not consolidated into one rule**. The new selector follows that same pattern.
    - `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UI-SPEC.md` § Page Composition — Airlines item 2, § Copywriting Contract (the destructive-confirmation row), § Autonomous Decisions Log items 2-5.
    - `server/plane/enrich.py` `static_airline_name_for_prefix()` as shipped by 13-03.
  </read_first>

  <behavior>
    - With no manual resolutions, the section renders its heading, its caption, and `layout.empty_state()` with the approved heading and body — and no table, no card list, no filter bar.
    - With entries, the section renders both a desktop table and a mobile card list covering the same rows in the same order (prefix ascending).
    - An entry whose prefix is absent from the static prefix table renders with an empty status cell — no marker, no colour.
    - An entry whose prefix IS in the static prefix table renders the Superseded marker with its `title` explanation, and the explanatory caption appears once below the list.
    - When no entry is superseded, the explanatory caption is absent.
    - Each row carries a delete form posting to `/airlines/manual-resolutions/{prefix}/delete` with a single submit control and no confirmation dialog.
    - A stored airline name containing hostile characters renders escaped in both representations and inside the `title` attribute.
  </behavior>

  <action>
Add the section's copy constants, byte-identical to the UI-SPEC deck:
- `MANUAL_SECTION_HEADING = "Manually resolved prefixes"`
- `MANUAL_SECTION_CAPTION = "Airlines you've named by hand for a prefix the frame couldn't otherwise identify."`
- `MANUAL_EMPTY_HEADING = "No manual resolutions yet."`
- `MANUAL_EMPTY_BODY = "Resolve an unidentified flight from Health's coverage-gap list to add one here."`
- `MANUAL_RESOLUTION_HEADERS = ("Prefix", "Airline name", "Added", "Status", "Delete")`
- `SUPERSEDED_MARKER_TEXT = "Superseded"`
- `SUPERSEDED_MARKER_TITLE = "The frame's built-in airline list now also recognizes this prefix — its entry wins, and this manual name is no longer used."`
- `SUPERSEDED_CAPTION = "A prefix is marked Superseded once the frame's built-in list also recognizes it — the built-in entry wins. Delete and re-add to point it somewhere else."`
- `DELETE_BUTTON_TEXT = "Delete"`
- `SUPERSEDED_STATUS_CLASS = "manual-resolution__status--superseded"`

Add `_manual_delete_action(prefix)` returning `MANUAL_DELETE_ROUTE_PREFIX + escaped_prefix + MANUAL_DELETE_ROUTE_SUFFIX`, so the two representations build the identical action string from one place and can never diverge.

Add `_manual_superseded_marker_html()` returning a `span` carrying `SUPERSEDED_STATUS_CLASS` and the `title` attribute from `SUPERSEDED_MARKER_TITLE`, with `SUPERSEDED_MARKER_TEXT` as its content, everything escaped once. The marker carries text, never colour alone — this app's Label-voice convention and UI-SPEC's no-colour-only-signal rule.

Add `_manual_resolution_rows(state_dir, registry)` returning a list of `(prefix, airline_name, created_at, superseded)` by calling `manual_resolutions.entry_rows(registry)` and setting `superseded = bool(enrich.static_airline_name_for_prefix(prefix))`. This is D-06's oracle: the static table has caught up, so its entry wins at runtime and the operator's uploaded art is no longer reachable under this prefix. Nothing here mutates the registry — flagging is the whole of D-06's UI obligation, and repairing is delete-and-re-add.

Add the paired renderers, structurally mirroring `health_page`'s registry pair:
- `_manual_resolution_row_html(index, prefix, airline_name, created_at, superseded, now)` — a `tr` carrying `row` or `row-alt` by index parity, with five cells: the prefix (`mono`), the airline name, `layout.concise_timestamp_html(created_at, now, fallback="")` interpolated verbatim as already-safe markup, the status cell (the marker when superseded, empty otherwise — an active entry is the normal state and gets no visual noise), and the delete cell holding a `method="post"` form whose action comes from `_manual_delete_action()` and whose only child is a submit control labelled `DELETE_BUTTON_TEXT`.
- `_manual_resolution_table_html(rows, now)` — the same `.data-table-wrap` / `.data-table` / `thead` / `tbody` structure `health_page._registry_table_html()` uses, headers from `MANUAL_RESOLUTION_HEADERS`, each escaped once.
- `_manual_resolution_cards_html(rows, now)` — one `li` carrying `class="data-card"` per row: a `data-card__primary` block with the prefix in a `span` carrying `class="cell-primary mono"` and the airline name in a `span` carrying `class="data-card__value"`, with the superseded marker appended inline in that same primary block when applicable; a `data-card__secondary` block labelled with `MANUAL_RESOLUTION_HEADERS[2]` carrying the same `concise_timestamp_html()` output; and the same delete form. Returns `""` for an empty list, matching the table's no-chrome-with-no-data rule. Emit **none** of the filter-pairing data attributes the Health registry's own rows and cards carry — this list has **no filter bar** (UI-SPEC Autonomous Decision 3), and the distinct-group counting `list-filter.js` performs must not be invited to see a list it does not drive.
- `_manual_resolutions_section_html(ctx)` — a `div` carrying `class="page-section"` holding an `h2` carrying `class="text-heading"` with `MANUAL_SECTION_HEADING`, a `p` carrying `class="text-label section-caption"` with `MANUAL_SECTION_CAPTION`, then either `layout.empty_state(MANUAL_EMPTY_HEADING, MANUAL_EMPTY_BODY)` when there are no rows, or the table plus the card list, plus — only when at least one row is superseded — a trailing `p` carrying `class="text-label section-caption"` with `SUPERSEDED_CAPTION`. Read the registry from `ctx.get("manual_resolutions")` when present, falling back to `manual_resolutions.load_manual_resolutions(state_dir)` when the key is absent and `state_dir` is truthy, and to `{}` otherwise — so `render({})` still works and plan 13-06's ctx key is used when supplied.

In `render(ctx)`, append `_manual_resolutions_section_html(ctx)` last, after the lightbox, per UI-SPEC's declared order. Extend `render()`'s docstring accordingly and record UI-SPEC Autonomous Decision 2's reasoning in one sentence: this list is reference and cleanup material, not the page's purpose, so it sits below the gallery.

**CSS** — one new rule in `companion/static/style.css`, placed beside the other Airlines-section rules. `.manual-resolution__status--superseded` declares the project's unified label voice **by value, not by selector reuse** — `font-size: 12px`, `font-weight: var(--weight-semibold)`, `text-transform: uppercase`, `letter-spacing: 0.06em`, `color: color-mix(in srgb, var(--color-text) 70%, transparent)` — exactly as `.data-card__label` already does, with a comment saying so and naming the SKILL.md rule that these six (now seven) selectors are written out separately rather than consolidated. No background, no border, no new colour token, no icon: a superseded entry is an expected, harmless outcome of the built-in list catching up, not a fault, so it must not reach for a warn or error colour.

**Harness.** Add three checks to `companion/test_status_pages.py`, moving `EXPECTED_CHECK_COUNT` in this same edit:
8. Empty and populated states: with an empty registry the section renders the approved empty heading and body and contains no table element; with two entries it renders both representations covering the same prefixes in the same order.
9. Supersession contract: an entry whose prefix is a real member of the static prefix table renders the marker, its `title`, and exactly one trailing explanatory caption; an entry whose prefix is not renders neither, and the caption is absent entirely when no row is superseded.
10. Delete-control and design-system contract: each row's delete form action is exactly `/airlines/manual-resolutions/{prefix}/delete`, the control is a submit control inside that form and not an anchor, the section emits no filter-bar markup, and `.manual-resolution__status--superseded` exists in `style.css` declaring the same five label-voice values `.data-card__label` declares (assert the declared values, so the two can never drift).

Bump `EXPECTED_CHECK_COUNT` from `143` to `146`, extend the ledger comment, and re-derive by running the harness.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `146/146`.
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0.
    - `grep -c "manual-resolution__status--superseded" companion/static/style.css` returns `1`.
    - `grep -c 'data-filter-group="' companion/pages/airlines_page.py` returns `0` — the management list stays out of the filter mechanism.
    - `grep -c 'class="filter-bar' companion/pages/airlines_page.py` returns `3` — unchanged from before this plan; the gallery's own filter bar is the page's only one.
    - `grep -c '<script' companion/pages/airlines_page.py` returns `0`.
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0.
    - `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <done>Every manual resolution is listed in both representations with an honest active/superseded state and a one-click delete, using only existing design-system vocabulary.</done>
</task>

</tasks>

## Artifacts this phase produces

**Owned by this plan (13-04):**

| Symbol | Kind | Location |
|---|---|---|
| `FLASH_MANUAL_RESOLVED` `FLASH_MANUAL_NAME_EMPTY` `FLASH_MANUAL_NAME_TOO_LONG` `FLASH_MANUAL_NAME_RESERVED` `FLASH_MANUAL_PREFIX_STALE` `FLASH_MANUAL_REGISTRY_FULL` `FLASH_MANUAL_SAVE_FAILED` `FLASH_MANUAL_DELETE_FAILED` | flash keys | `companion/pages/airlines_page.py` |
| `RESOLVE_ROUTE` = `/airlines/resolve`, `MANUAL_DELETE_ROUTE_PREFIX` = `/airlines/manual-resolutions/`, `MANUAL_DELETE_ROUTE_SUFFIX` = `/delete`, `AIRLINES_ROUTE`, `RESOLVE_QUERY_PARAM` | route constants | same |
| `RESOLVE_BACK_LINK_TEXT` `RESOLVE_STALE_BODY` `RESOLVE_HEADING` `RESOLVE_CAPTION_TEMPLATE` `RESOLVE_CONTEXT_LABELS` `NAME_LABEL_TEXT` `NAME_HINT_TEXT` `SAVE_BUTTON_TEXT` `STEP_B_HEADING_TEMPLATE` `STEP_B_CAPTION` `STEP_B_SKIP_TEXT` `RESOLVE_ALREADY_DONE_TEMPLATE` `MANUAL_SECTION_HEADING` `MANUAL_SECTION_CAPTION` `MANUAL_EMPTY_HEADING` `MANUAL_EMPTY_BODY` `MANUAL_RESOLUTION_HEADERS` `SUPERSEDED_MARKER_TEXT` `SUPERSEDED_MARKER_TITLE` `SUPERSEDED_CAPTION` `DELETE_BUTTON_TEXT` | copy constants | same |
| `MANUAL_NAME_INPUT_ID` `MANUAL_UPLOAD_INPUT_ID` `MANUAL_DATALIST_ID` `SUPERSEDED_STATUS_CLASS` | element ids / class name | same |
| `unresolved_row_for_prefix()` (public) `_resolve_context_html()` `_known_airlines_datalist_html()` `_resolve_section_html()` `_manual_delete_action()` `_manual_superseded_marker_html()` `_manual_resolution_rows()` `_manual_resolution_row_html()` `_manual_resolution_table_html()` `_manual_resolution_cards_html()` `_manual_resolutions_section_html()` | functions | same |
| `.resolve-context` `.resolve-name-field` `.resolve-upload-zone` `.manual-resolution__status--superseded` | CSS classes | `companion/static/style.css` |

**Produced by sibling plans (reference only — do not create here):** `server/plane/manual_resolutions.py`'s full surface (13-01); `health_page` `RESOLVE_LINK_*`, `_SOURCE_ROWS[4]`, `.data-card__action` (13-02); `enrich.airline_source_from_callsign()`, `enrich.static_airline_name_for_prefix()`, `enrich.clear_resolved_unresolved_prefix()`, the `"manual"` source (13-03); the `poll_loop.run_once()` call sites (13-05); `app._illustration_filenames(state_dir)`, `Handler._handle_manual_resolve_post()`, `Handler._handle_manual_resolution_delete()`, ctx keys `resolve_prefix` / `manual_resolutions`, and the `FLASH_KEY_MANUAL_*` rebindings with their `FLASH_MESSAGES` / `FLASH_ROLES` entries (13-06).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string (`?resolve=XYZ`) -> rendered page | The prefix arrives from a link, a bookmark, or a hand-typed URL. It is a hint only. |
| `poll_state.json` (ADS-B-derived, spoofable callsigns) -> rendered sighting context | The five displayed values originate in an unauthenticated feed. |
| `manual_resolutions.json` (operator-written) -> rendered name, `title` attribute, form action | A stored airline name reaches four distinct interpolation sites, one of them inside a URL path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Spoofing / Tampering | `?resolve={prefix}` deep link | high | mitigate | `unresolved_row_for_prefix()` membership-tests the prefix against the live `unresolved_prefixes` registry before anything renders (D-11) — validate-then-join over a closed, server-written set, the identical shape `_serve_illustration_image()` already uses. Every displayed value comes from the returned tuple, never from `ctx`'s raw string (D-12); Task 1 check 7 asserts a mismatched-case or padded query value renders the stale card. |
| T-13-01 | Tampering | Step B's upload form action | high | mitigate | The `{key}` in `/illustration/{key}.png` is recomputed server-side by `manual_resolutions.illustration_key_for_name()` from the *stored* name, never taken from the query string and never slugged in this module. A falsy key falls back to the stale card rather than emitting a form. This is what makes plan 13-06's widened membership check safe: by the time the browser posts, the key is already-persisted server state. |
| T-13-05 | Tampering (stored XSS) | prefix, airline name, callsign, `title` attribute, form actions | medium | mitigate | Single escaping choke point: `companion.layout.escape_html()`, exactly once per interpolation, at the point of interpolation. `concise_timestamp_html()`'s already-safe markup is interpolated verbatim and never re-escaped, matching `health_page`'s documented discipline. Task 1 check 7 exercises angle brackets, quotes and ampersands through every new site. |
| T-13-03 | Tampering | reserved-key names reaching the page | medium | transfer | The page never accepts a name; it only displays stored ones. `manual_resolutions.add_entry()` and `load_manual_resolutions()` (plan 13-01) both refuse `generic-*` slugs, so a reserved name can neither be stored nor loaded, and therefore never renders. |
| T-13-07 | Spoofing (CSRF) | the resolve form and each delete form | medium | accept | `SameSite=Strict` on the session cookie is this site's whole CSRF control (`companion/auth.py:132`) and no route carries a token. The two new state-changing forms follow that single established posture rather than inventing a second mechanism, exactly as `_handle_illustration_replace()` documents for itself (T-v26-02-07, accepted risk). |
| T-13-18 | Tampering | client-side script tampering with the flow | low | mitigate | There is none to tamper with: the datalist is native, no script is added, and every control degrades to an ordinary form or link. Asserted by a zero-count grep for script elements and inline event-handler attributes. |
| T-13-10 | Tampering | the delete control | medium | mitigate | The form posts to a delete route that (plan 13-06) calls `manual_resolutions.delete_entry()`, which never touches the override image (D-08). No confirmation dialog is a deliberate UI-SPEC call justified by that recoverability; the page must not invent one. |
| T-13-SC | Tampering | package installs | n/a | accept | Zero package installs in this phase; `13-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at `146/146`.
- `server/.venv/bin/python3 companion/test_view_pages.py` and `companion/test_contrast_check.py` both exit 0.
- `scripts/run-all-tests.sh` exits 0.
- **D-09 standing gate:** `git diff --stat -- server/plane/illustrations.py` is empty and `server/.venv/bin/python3 server/test_illustrations.py` exits 0 with `EXPECTED_CHECK_COUNT` still `58`.
- **Zero-JavaScript gate:** `git diff --stat -- companion/static/` shows only `style.css`, and `grep -c '<script' companion/pages/airlines_page.py` returns `0`.
- **Copy fidelity:** every string added to `airlines_page.py` that appears in `13-UI-SPEC.md`'s Full Copy Deck matches it byte for byte; the three deviations (the two failure flash keys and `RESOLVE_ALREADY_DONE_TEMPLATE`) are commented in-source as planner additions with their justification.
</verification>

<success_criteria>
- `/airlines?resolve={prefix}` renders one of exactly four server-derived states, and only the stale state is reachable for a prefix that is not in the live unresolved-prefix registry.
- The sighting context shown is re-read from the registry; nothing displayed originates in the query string.
- The name field is backed by a native datalist offering all 27 airlines that already have artwork.
- The upload step appears only when no artwork resolves for the stored name's key, and posts to the existing `/illustration/{key}.png` route with a server-recomputed key.
- The management list renders in both representations, flags superseded entries in words, and offers a one-click delete per row.
- No JavaScript, no new colour token, no new spacing value, no new icon.
</success_criteria>

<output>
Create `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-04-SUMMARY.md` when done.
</output>
