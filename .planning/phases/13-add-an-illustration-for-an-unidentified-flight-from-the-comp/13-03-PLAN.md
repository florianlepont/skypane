---
phase: 13-add-an-illustration-for-an-unidentified-flight-from-the-comp
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - server/plane/enrich.py
  - server/test_enrich.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A callsign whose prefix carries only a manual resolution resolves to the operator's airline name, and every existing caller of airline_from_callsign() sees that transparently with no signature change (D-01)."
    - "The static _ICAO_AIRLINE_PREFIXES table is consulted first and always wins; a prefix present in both tables reports the static name and the static source (D-06)."
    - "resolve_route() returns the source string \"manual\" — never \"airline_only\" — when the runtime registry, not the static table, did the resolving (D-02)."
    - "A prefix that has since become resolvable can be removed from the unresolved-prefix registry by an explicit, never-raising helper, independent of what resolve_route() returned for adsbdb this cycle (D-14)."
    - "_ICAO_AIRLINE_PREFIXES, _AIRLINE_NAME_CORRECTIONS, _ILLUSTRATION_TARGETS and the drift guard coupling them are byte-for-byte untouched."
  artifacts:
    - server/plane/enrich.py
  key_links:
    - "airline_from_callsign() becomes a one-line wrapper over airline_source_from_callsign() — this is what keeps its ~10 call sites and 15+ existing tests unchanged."
    - "note_unresolved_prefix()'s existing gate (stop recording once airline_from_callsign() returns non-None) becomes correct for manual resolutions with zero code change there, but its docstring's claim about _ICAO_AIRLINE_PREFIXES goes stale and must move."
---

<objective>
Thread the manual-resolution registry into the enrichment seam without widening any existing signature: add `airline_source_from_callsign()` (provenance-aware, static-first), turn `airline_from_callsign()` into its wrapper, extend `resolve_route()` to a fifth `"manual"` source, add `static_airline_name_for_prefix()` for the companion's supersession check, and add `clear_resolved_unresolved_prefix()` as D-14's whole implementation.

Purpose: this is the phase's purity break, and it is deliberate. After this plan `airline_from_callsign()`'s returnable set is operator-supplied rather than fixed-table-only, which is the single most security-relevant consequence of the phase (T-13-02). The plan handles it by keeping the *shape* of every public contract intact, documenting the change precisely everywhere the old claim is written down, and leaning on plan 13-01's write-time allowlist rather than on the old "table values or None" invariant.

Output: four new functions in `enrich.py`, one rewritten fallback branch, a docstring sweep, and matching harness checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-CONTEXT.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-RESEARCH.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-PATTERNS.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add airline_source_from_callsign() and static_airline_name_for_prefix(); make airline_from_callsign() their wrapper (D-01, D-06)</name>

  <files>server/plane/enrich.py, server/test_enrich.py</files>

  <read_first>
    - `server/plane/enrich.py` lines 90-120 (`_AIRLINE_PREFIX_SHAPE_RE`, `normalise_callsign()`) and lines 615-640 (`airline_from_callsign()` in full, including the T-hyy-01 / T-hyy-02 paragraphs and the "Pure, no I/O, no network" sentence).
    - `server/plane/manual_resolutions.py` as shipped by plan 13-01 — specifically `airline_name_for_prefix()` and `set_manual_registry_state_dir()`, and the docstring note that a process which never calls the setter sees an empty registry.
    - `server/test_enrich.py` lines 1-90 and the "260827-hyy" battery around lines 380-413 — the existing tests that assert `airline_from_callsign()`'s never-raises and hostile-input behaviour. None of them may change.
    - `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-RESEARCH.md` § Pattern 1 and § Pitfall 6.
  </read_first>

  <behavior>
    - With no registry configured (the default in every existing test): `airline_source_from_callsign("AFR1234")` returns `("Air France", "static")` — or whatever the static table holds — and `airline_source_from_callsign("ZZZ1234")` returns `(None, None)`.
    - `airline_from_callsign()` returns exactly what it returned before this change for every input in the existing test battery — non-strings, ints, empty strings, bare 3-letter strings, and callsigns containing a path separator all still return `None` without raising.
    - After `manual_resolutions.set_manual_registry_state_dir(tmp)` where `tmp` holds an entry `ZZZ -> "Zephyr Air"`: `airline_source_from_callsign("ZZZ1234")` returns `("Zephyr Air", "manual")` and `airline_from_callsign("ZZZ1234")` returns `"Zephyr Air"`.
    - D-06 collision: with a manual entry for a prefix that IS in the static table, `airline_source_from_callsign()` returns the *static* name and `"static"`. The manual entry is never consulted for that prefix.
    - The shape gate runs before any registry read: `airline_source_from_callsign("ZZ")`, `("ZZZ")` (no flight suffix), `("../x")`, `(None)`, `(42)` all return `(None, None)` without touching the registry.
    - `static_airline_name_for_prefix("AFR")` returns the static name; `static_airline_name_for_prefix("ZZZ")` returns `None` even when `ZZZ` has a manual entry; `static_airline_name_for_prefix("afr")`, `("AF")`, `(None)`, `(42)` are handled without raising.
  </behavior>

  <action>
Add `from server.plane import manual_resolutions` to `enrich.py`'s imports, beside its existing `from server.plane import runway_config` line, keeping the same import style. Confirm no cycle: `manual_resolutions.py` must not import `enrich` (plan 13-01 forbids it and its acceptance criteria assert it).

Add `static_airline_name_for_prefix(prefix)` immediately above `airline_from_callsign()`. It returns `_ICAO_AIRLINE_PREFIXES.get(prefix)` for a string matching exactly three uppercase ASCII letters, and `None` for everything else — non-string, wrong length, lowercase, or anything containing a non-letter. Never raises. Its docstring must state that this is the *static-table-only* accessor, that it deliberately does not consult the runtime registry, and that its one consumer is the companion's D-06 supersession check on the Airlines management list, which needs to know "has the built-in list caught up with this prefix?" — a question `airline_from_callsign()` can no longer answer once the registry is in play.

Add `airline_source_from_callsign(callsign)` returning a 2-tuple `(airline_name, source)` where `source` is `"static"`, `"manual"`, or `None`. Body order, which is the whole security-relevant part and must not be reordered:
1. `normalise_callsign(callsign)`; `None` -> return `(None, None)`.
2. `_AIRLINE_PREFIX_SHAPE_RE.match(normalised)` fails -> return `(None, None)`.
3. `prefix = normalised[:3]`; look up `_ICAO_AIRLINE_PREFIXES.get(prefix)` — a hit returns `(static_name, "static")`. **The static table is consulted first and always wins (D-06); a prefix present in both tables never reports `"manual"`.**
4. Only then `manual_resolutions.airline_name_for_prefix(prefix)` — a hit returns `(manual_name, "manual")`.
5. Otherwise `(None, None)`.

Rewrite `airline_from_callsign()`'s body to the single line `return airline_source_from_callsign(callsign)[0]`, keeping its name, its signature, and its position in the file. Rewrite its docstring precisely — this is the sweep RESEARCH.md Pitfall 6 calls for, and it must be exact rather than softened:
- Keep the never-raises guarantee (T-hyy-02) and say it now derives from `airline_source_from_callsign()`'s own gates plus `manual_resolutions.airline_name_for_prefix()`'s never-raises contract.
- Replace the T-hyy-01 paragraph. The old claim — that the only strings this can return are fixed `_ICAO_AIRLINE_PREFIXES` values — is no longer true. State plainly that after D-01 the returnable set additionally includes operator-supplied airline names from the runtime registry, and that the property which now protects `illustrations.py`'s path construction is different and lives elsewhere: `manual_resolutions.add_entry()` refuses at write time any name whose `normalise_airline_key()` slug fails its `^[a-z0-9][a-z0-9-]*$` allowlist, and `load_manual_resolutions()` re-applies that allowlist on every read, so a hostile name can never be *stored*, let alone returned. Name `illustrations._UNSAFE_KEY_RE`, `illustration_path_for_key()` and `override_path_for_key()` explicitly as the defence-in-depth second line they now are rather than the first.
- Delete the existing purity sentence — the short one asserting the function does no I/O and no network work — and put an accurate statement in its place: the function performs no network access and opens no file of its own; it reads a process-scoped dict that `manual_resolutions.set_manual_registry_state_dir()` populates once per poll cycle. A process that never calls that setter — every existing harness included — sees an empty registry and therefore today's exact behaviour.
- Note that `illustrations.classify_aircraft_type()`, which the old paragraph cited as a mirror, still holds the fixed-table property and is no longer a mirror of this function.

Also update `enrich.py`'s module docstring (around line 15), which currently presents the airline-only case as the last of a closed set of four outcomes, so that it presents five.

Add three checks to `server/test_enrich.py` and move `EXPECTED_CHECK_COUNT` in this same edit:
1. Static-path parity: `airline_source_from_callsign()` returns `("<name>", "static")` for a known static prefix, `(None, None)` for an unknown one, and `(None, None)` for the full existing hostile-input list (reuse the same inputs the 260827-hyy battery already uses, so the two can never drift).
2. Manual-path resolution: using `tempfile.TemporaryDirectory()` plus `manual_resolutions.add_entry()` and `set_manual_registry_state_dir()`, a manually-registered prefix resolves through both `airline_source_from_callsign()` (as `"manual"`) and `airline_from_callsign()`; then `set_manual_registry_state_dir(None)` restores `(None, None)`. The check must reset the process cache in a `finally` so it cannot leak into other checks in the same run.
3. D-06 collision + `static_airline_name_for_prefix()`: a manual entry for a prefix already in the static table reports the static name and `"static"`; `static_airline_name_for_prefix()` returns the static name for that prefix and `None` for a manual-only prefix, and handles a non-string and a wrong-length argument without raising.

Bump `EXPECTED_CHECK_COUNT` from `52` to `55` with a ledger comment in this file's style, and re-derive by running the harness rather than by arithmetic.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_enrich.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_enrich.py` exits 0 and reports `55/55`.
    - `git diff -- server/plane/enrich.py | grep -c "^[-+].*_ICAO_AIRLINE_PREFIXES = \|^[-+].*_AIRLINE_NAME_CORRECTIONS = "` returns `0` — neither static table's literal was touched.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import enrich; n,s = enrich.airline_source_from_callsign('AFR1234'); assert s == 'static' and n; assert enrich.airline_source_from_callsign('../x') == (None, None); assert enrich.airline_from_callsign(42) is None"` exits 0.
    - `grep -c "Pure, no I/O, no network" server/plane/enrich.py` returns `0`.
    - `grep -c "def airline_source_from_callsign\|def static_airline_name_for_prefix" server/plane/enrich.py` returns `2`.
    - `server/.venv/bin/python3 server/test_illustrations.py` exits 0 with `EXPECTED_CHECK_COUNT` still `58` (the drift guard coupling `_ICAO_AIRLINE_PREFIXES` to `illustrations.target_airline_names()` still passes untouched).
  </acceptance_criteria>

  <done>The registry is consulted after the static table, every existing caller and test is unchanged, and no docstring in the module still claims the old fixed-table-only property.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend resolve_route() to the fifth "manual" source (D-02)</name>

  <files>server/plane/enrich.py, server/test_enrich.py</files>

  <read_first>
    - `server/plane/enrich.py` lines 663-696 — `resolve_route()` in full, including the docstring's four-way `source` enumeration and the "D-04: unchanged, not loosened" note on `was_cached`.
    - `server/plane/enrich.py` lines 639-661 — `airline_only_route()`, the sole construction site for the airline-only route shape.
    - `companion/pages/health_page.py` `_SOURCE_ROWS` as edited by plan 13-02 — the fifth `"manual"` row already exists there and its label/gloss must stay consistent with this docstring.
    - `server/test_enrich.py` — the existing `resolve_route()` battery; none of its assertions may change.
  </read_first>

  <behavior>
    - With no registry: `resolve_route()` returns exactly the same `(route, source)` pairs it returns today for every existing test input — `"fresh_hit"`, `"cache_hit"`, `"airline_only"`, `"miss"` all unchanged.
    - With a manual registry entry for a prefix absent from the static table and an adsbdb transport that returns nothing: `resolve_route()` returns `(airline_only_route("<manual name>"), "manual")`.
    - With a manual registry entry for a prefix that IS in the static table: the source is `"airline_only"`, never `"manual"` (D-06 flows through unchanged from Task 1).
    - adsbdb still wins by construction: a transport that returns a full route yields `"fresh_hit"`/`"cache_hit"` and the registry is never consulted.
  </behavior>

  <action>
Replace `resolve_route()`'s fallback branch. Instead of `airline_name = airline_from_callsign(callsign)`, call `airline_name, airline_source = airline_source_from_callsign(callsign)`. When `airline_name` is truthy, return `airline_only_route(airline_name)` paired with `"airline_only"` when `airline_source` is `"static"` and `"manual"` when it is `"manual"`. Otherwise return `(None, "miss")` exactly as today.

Do not construct a second route shape: D-02 changes only the *source label*, and `airline_only_route()` stays the sole construction site for that dict — every downstream consumer (`city_for_state()`, `render._flight_line1_text()`, `render._flight_line2_text()`, `illustrations.select_illustration()`) keeps working unchanged precisely because the shape is identical.

Rewrite the docstring's `source` enumeration from four values to five. The `"airline_only"` entry's wording must be tightened, not merely kept: it now means specifically that the **static** prefix table identified the carrier. Add the fifth entry for `"manual"`, worded consistently with `health_page._SOURCE_ROWS`'s gloss ("the operator resolved this callsign's prefix by hand, from the companion web interface"). State in the docstring why the two are separate rather than folded together (D-02): Health renders the `"airline_only"` bucket with a gloss that names the static prefix table, and folding manual resolutions into it would make that sentence false and the resolution rate dishonest. Keep the never-raises note and the `was_cached` paragraph verbatim.

Then grep the whole repository for prose that enumerates the pre-existing source values as an exhaustive set and fix each occurrence outside this file's own docstrings that this plan owns — at minimum `server/plane/enrich.py`'s module docstring (already handled in Task 1) — and record in the SUMMARY any occurrence you find in a file this plan does not own (`server/poll_loop.py` lines ~1029 and ~1042 are known and belong to plan 13-05; do not edit them here).

Add two checks to `server/test_enrich.py`, moving `EXPECTED_CHECK_COUNT` in this same edit:
4. Manual source: with a temp state dir holding a manual entry for a prefix absent from the static table and a transport stub returning no route, `resolve_route()` returns `"manual"` and a route dict whose `airline_name` is the operator's name and whose other five keys are `None`. Reset the process cache in a `finally`.
5. Static/manual precedence and adsbdb precedence together: a manual entry for a static-table prefix still yields `"airline_only"`; and with a transport stub returning a full route, the source is `"fresh_hit"` even though a manual entry exists for that prefix.

Bump `EXPECTED_CHECK_COUNT` from `55` to `57`, extend the ledger comment, and re-derive by running the harness.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_enrich.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_enrich.py` exits 0 and reports `57/57`.
    - `grep -c '"manual"' server/plane/enrich.py` is at least `2` (the branch and the docstring entry).
    - `grep -c "fourth outcome\|four sources\|one of four" server/plane/enrich.py` returns `0`.
    - `server/.venv/bin/python3 server/test_render.py` exits 0 (the route shape is unchanged, so the renderer is unaffected).
    - `server/.venv/bin/python3 server/test_pipeline_e2e.py` exits 0.
  </acceptance_criteria>

  <done>`resolve_route()` classifies into five sources, the route dict shape is unchanged, and the renderer and end-to-end harnesses still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add clear_resolved_unresolved_prefix() — D-14's whole implementation</name>

  <files>server/plane/enrich.py, server/test_enrich.py</files>

  <read_first>
    - `server/plane/enrich.py` lines 743-820 — `UNRESOLVED_PREFIX_MAX_ENTRIES`, `note_unresolved_prefix()` in full (its gate order, its `isinstance(registry, dict)` guard, its docstring's claim about `_ICAO_AIRLINE_PREFIXES`), and `trim_unresolved_prefixes()`.
    - `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-RESEARCH.md` § Pattern 6 and § Pitfall 1 and § Pitfall 2 — in particular why the cleanup must NOT be gated on `route_source`.
  </read_first>

  <behavior>
    - `clear_resolved_unresolved_prefix("ZZZ1234", registry)` where `registry` holds `"ZZZ"` and the prefix now resolves (statically or manually) removes the entry and returns `"ZZZ"`.
    - The same call when the prefix does NOT resolve returns `None` and leaves the registry untouched.
    - The same call when the prefix is absent from the registry returns `None` and does not raise.
    - A non-dict `registry`, a non-string callsign, an int, an empty string, a bare 3-letter callsign, and a callsign containing a path separator all return `None` without raising and without mutating anything.
    - Calling it twice in a row is safe: the second call returns `None`.
  </behavior>

  <action>
Add `clear_resolved_unresolved_prefix(callsign, registry)` immediately after `note_unresolved_prefix()`, written as that function's structural inverse with the identical gate order so the two can never drift:
1. `if not isinstance(registry, dict): return None`
2. `normalised = normalise_callsign(callsign)`; `None` -> return `None`
3. `_AIRLINE_PREFIX_SHAPE_RE.match(normalised)` fails -> return `None`
4. `prefix = normalised[:3]`; `prefix not in registry` -> return `None`
5. `airline_from_callsign(callsign) is None` -> return `None`
6. `del registry[prefix]`; return `prefix`

Never raises. The docstring must say all of the following explicitly, because each is a pitfall RESEARCH.md names:
- This is D-14's whole implementation. `note_unresolved_prefix()` already stops *recording* a prefix that resolves; nothing in this module ever *removed* an entry that predates the resolution, and a dict entry persists until something deletes it.
- The resolution test is `airline_from_callsign()`, i.e. either table (D-01), never a `route_source` value. `route_source` describes this cycle's specific callsign's adsbdb outcome and says nothing about whether the prefix is now resolvable — gating on it would leave a stale entry uncleaned whenever adsbdb happened to answer.
- Ordering requirement for the caller: it must run before `trim_unresolved_prefixes()` and before the `poll_state["unresolved_prefixes"]` write-back. Name `server/poll_loop.py`'s `run_once()` as the sole caller (wired by plan 13-05).

Also fix `note_unresolved_prefix()`'s own docstring in this same task. Its parenthetical gloss on the recording gate currently names only the static prefix table as the place the prefix must be missing from; after D-01 that is stale, and it must instead say the prefix is absent from both the static table and the runtime manual registry. Keep the rest of that docstring, including the paragraph explaining that both decisions derive from the single `airline_from_callsign()` call so the function can never drift from that seam's verdict; that reasoning is now *more* load-bearing, not less, and should say so.

Add two checks to `server/test_enrich.py`, moving `EXPECTED_CHECK_COUNT` in this same edit:
6. Happy path and idempotence: a registry entry for a manually-resolved prefix is removed and the prefix returned; a second call returns `None`; a still-unresolved prefix's entry survives untouched (assert the exact entry dict is unchanged, not merely present).
7. Hostile-input sweep: non-dict registry, `None`, `42`, `""`, `"ZZ"`, `"ZZZ"`, `"../x"` — every one returns `None`, raises nothing, and leaves a seeded registry byte-identical. One `check()` covering the whole list.

Bump `EXPECTED_CHECK_COUNT` from `57` to `59`, extend the ledger comment, and re-derive by running the harness.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_enrich.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_enrich.py` exits 0 and reports `59/59`.
    - `grep -c "def clear_resolved_unresolved_prefix" server/plane/enrich.py` returns `1`.
    - `grep -c "route_source" server/plane/enrich.py` is at least `1` and every occurrence is inside the new docstring explaining why the cleanup is NOT gated on it (inspect the matches).
    - `grep -c "genuinely absent from \`_ICAO_AIRLINE_PREFIXES\`" server/plane/enrich.py` returns `0`.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import enrich; r={'ZZZ':{'count':1}}; assert enrich.clear_resolved_unresolved_prefix('ZZZ1234', r) is None and r == {'ZZZ':{'count':1}}; assert enrich.clear_resolved_unresolved_prefix('ZZZ1234', None) is None"` exits 0.
    - `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <done>The inverse of `note_unresolved_prefix()` exists, is never-raising, is gated on resolvability rather than on `route_source`, and both functions' docstrings tell the truth after D-01.</done>
</task>

</tasks>

## Artifacts this phase produces

**Owned by this plan (13-03):**

| Symbol | Kind | Location |
|---|---|---|
| `airline_source_from_callsign(callsign) -> (name, source)` | function | `server/plane/enrich.py` |
| `static_airline_name_for_prefix(prefix) -> str \| None` | function | same |
| `clear_resolved_unresolved_prefix(callsign, registry) -> str \| None` | function | same |
| `airline_from_callsign(callsign)` | rewritten as a one-line wrapper, signature unchanged | same |
| `"manual"` | new `resolve_route()` source string / `runway_events.route_source` value | same, and `history.db` at runtime |

**Produced by sibling plans (reference only — do not create here):** the whole of `server/plane/manual_resolutions.py` (13-01); `health_page` `RESOLVE_LINK_*` constants, `_SOURCE_ROWS[4]`, `.data-card__action` (13-02); `airlines_page` `FLASH_MANUAL_*`, `RESOLVE_ROUTE`, `MANUAL_DELETE_ROUTE_PREFIX`, `unresolved_row_for_prefix()`, `.resolve-context`, `.resolve-name-field`, `.resolve-upload-zone`, `.manual-resolution__status--superseded` (13-04); the `poll_loop.run_once()` setter and cleanup call sites (13-05); `app._illustration_filenames(state_dir)`, `Handler._handle_manual_resolve_post()`, `Handler._handle_manual_resolution_delete()`, ctx keys `resolve_prefix` / `manual_resolutions` (13-06).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `manual_resolutions.json` (operator-written) -> `airline_from_callsign()` -> `illustrations.select_illustration()` -> filesystem path | **The phase's central boundary.** Before this plan, everything downstream of `airline_from_callsign()` could rely on "fixed-table values or `None`". After it, that invariant is gone. |
| ADS-B callsign (unauthenticated, spoofable) -> `airline_source_from_callsign()` prefix gate | Unchanged in kind, but now selects between two tables instead of one. |
| `poll_state.json` `unresolved_prefixes` dict -> `clear_resolved_unresolved_prefix()` mutation | The dict is documented as hand-editable; a malformed entry must not crash the poll cycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Tampering | `airline_from_callsign()`'s widened returnable set (breaks T-hyy-01) | high | mitigate | The invariant moves upstream rather than disappearing: plan 13-01's `add_entry()` refuses at write time any name whose `normalise_airline_key()` slug fails `^[a-z0-9][a-z0-9-]*$`, and `load_manual_resolutions()` re-applies the same allowlist on every read, so no traversal-shaped or separator-bearing name can ever be stored. Task 1 rewrites the T-hyy-01 paragraph to state this precisely instead of leaving a stale "fixed table values only" claim. `illustrations._UNSAFE_KEY_RE` / `illustration_path_for_key()` / `override_path_for_key()` are re-designated as defence in depth. |
| T-13-15 | Spoofing | a manual entry shadowing a real carrier's prefix | medium | mitigate | D-06 precedence is implemented as branch order, not as a comparison: the static table is read first and returns immediately, so a manual entry can never override a shipped mapping. Task 1 check 3 asserts it directly. |
| T-13-16 | Tampering | stale gap data after resolution (`coverage_status()` reporting a gap that no longer exists) | medium | mitigate | `clear_resolved_unresolved_prefix()`, gated on `airline_from_callsign()` rather than on `route_source` (RESEARCH.md Pitfall 2) so a cycle where adsbdb answered still cleans up. Wired by plan 13-05. |
| T-13-12 | Denial of Service | a raise inside the enrichment seam | high | mitigate | Every new function keeps the module's never-raises discipline (T-hyy-02): `isinstance` guards first, shape gate second, registry read last, and `manual_resolutions.airline_name_for_prefix()` reads only a process dict and never touches disk. Task 3 check 7's hostile sweep is the proof. A raise here would abort the poll cycle mid-render. |
| T-13-17 | Repudiation | `route_source` gaining a fifth value in `runway_events` | low | accept | `runway_events.route_source` is an unconstrained `TEXT` column with no `CHECK` and `route_source_counts()` groups generically, so no schema migration and no storage-layer risk. The only real exposure was a display-layer omission, closed by plan 13-02's fifth `_SOURCE_ROWS` tuple. |
| T-13-SC | Tampering | package installs | n/a | accept | Zero package installs in this phase; `13-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_enrich.py` exits 0 at `59/59`.
- `scripts/run-all-tests.sh` exits 0 (17 harnesses).
- **D-09 standing gate:** `git diff --stat -- server/plane/illustrations.py` is empty and `server/.venv/bin/python3 server/test_illustrations.py` exits 0 with `EXPECTED_CHECK_COUNT` still `58` — this is the phase's regression proof that `select_illustration()` and `resolved_illustration_path()` were not touched.
- Drift guard intact: the `test_enrich.py` check asserting every `_ICAO_AIRLINE_PREFIXES` value is a member of `illustrations.target_airline_names()` still passes, unmodified.
- No new dependency: `git diff -- server/requirements.txt server/requirements-dev.txt` is empty.
</verification>

<success_criteria>
- `airline_source_from_callsign()`, `static_airline_name_for_prefix()` and `clear_resolved_unresolved_prefix()` exist with the exact signatures above.
- `airline_from_callsign()`'s signature and behaviour for every pre-existing input are unchanged; no existing test in `server/test_enrich.py` was modified.
- `resolve_route()` returns `"manual"` only when the runtime registry resolved the prefix, and `"airline_only"` only when the static table did.
- No docstring anywhere in `enrich.py` still claims the function is pure/no-I/O or that it can only return fixed-table values.
- `_ICAO_AIRLINE_PREFIXES`, `_AIRLINE_NAME_CORRECTIONS` and `server/plane/illustrations.py` are byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-03-SUMMARY.md` when done.
</output>
