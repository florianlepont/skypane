---
phase: 13-add-an-illustration-for-an-unidentified-flight-from-the-comp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/pages/health_page.py
  - companion/static/style.css
  - companion/test_status_pages.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every row of Health's unresolved-prefix registry carries a link that opens the Airlines page pre-targeted at that prefix, in both the desktop table and the mobile card list (D-10)."
    - "Health's read-only note still tells the truth: the list is read-only here, and it now says where resolution happens instead of pointing at the manual runbook."
    - "Health's registry section still contains no HTML form element and no submit control — decision D-11/D-12 of 06.6.4.1-04 stands, unreopened."
    - "The resolution-rate breakdown offers a fifth row, Manual, so the D-02 source never lands in the airline_only bucket whose gloss says the static prefix table did the work."
  artifacts:
    - companion/pages/health_page.py
    - companion/static/style.css
  key_links:
    - "_REGISTRY_HEADERS is index-addressed by _registry_cards_html() (indices 1, 2, 3, 4) — the new entry must be APPENDED so those lookups stay correct."
    - "resolution_stats() iterates _SOURCE_ROWS generically, so the fifth tuple needs no new rendering code — but any harness assertion counting four sources must move with it."
---

<objective>
Make Health's existing coverage-gap surface actionable without giving it any state-changing control: reword `_READ_ONLY_NOTE`, add a per-row deep link to `/airlines?resolve={prefix}` in both the desktop table and the mobile card list, and append the fifth `"manual"` row to the resolution-rate breakdown (D-02, D-10).

Purpose: this is the entry point the whole phase exists to create — the operator sees the gap on Health and reaches the resolution form in one click, instead of hand-copying a prefix onto another page. It is presentation-only: no new data source, no new ctx key, no server dependency, which is why it ships in wave 1 alongside plan 13-01 rather than waiting for the registry.

Output: a re-worded constant, a sixth registry column plus its mobile counterpart, a fifth source row, and one new CSS class.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-CONTEXT.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UI-SPEC.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reword _READ_ONLY_NOTE and append the fifth "manual" source row (D-02, D-10)</name>

  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` lines 340-400 — `_READ_ONLY_NOTE` (line 352) and `_SOURCE_ROWS` (line 368) in full, including the surrounding comments that enumerate the four historical source values in prose.
    - `companion/pages/health_page.py` lines 1889-1930 — `resolution_stats()`, which iterates `_SOURCE_ROWS` generically to compute `total` and build its rows.
    - `companion/pages/health_page.py` line 2138 — the single rendering call site for `_READ_ONLY_NOTE`.
    - `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md` — D-11/D-12, the read-only-by-design decision this task must NOT reopen.
    - `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UI-SPEC.md` § Page Composition — Health, and § Full Copy Deck. Its strings are binding.
    - `companion/test_status_pages.py` lines 275-300 — the `EXPECTED_CHECK_COUNT` ledger comment convention.
  </read_first>

  <behavior>
    - `health_page.render(ctx)` output contains the new note string and does not contain the substring `coverage-gap runbook`.
    - With a seeded `route_source` count for `"manual"`, the rendered resolution-statistics table contains a row labelled `Manual` and the total includes that count.
    - `len(health_page._SOURCE_ROWS) == 5` and the fifth tuple's first element is `"manual"`.
  </behavior>

  <action>
Replace the body of `_READ_ONLY_NOTE` with the UI-SPEC's approved string, byte for byte:

`This list is read-only here — each row's Resolve link opens the Airlines page to name that prefix's airline (and add artwork, if it needs one).`

Note the em dash is a real U+2014, matching every other string in this module. Do not change the constant's name, its rendering call site, or its `<p class="text-body section-caption">` wrapper. Update the comment above the constant so it records that D-10 of phase 13 re-worded it and that D-11/D-12 of `06.6.4.1-04` therefore stands rather than being reopened — the list is still read-only on this page, the action lives on Airlines.

Append a fifth tuple to `_SOURCE_ROWS`, after the existing `"miss"` entry:

`("manual", "Manual", "The operator resolved this callsign's prefix by hand, from the companion web interface.")`

Then sweep this module for prose that enumerates exactly four source values as if exhaustive — RESEARCH.md Pitfall 5 names this as the change's real blast radius. Grep `companion/pages/health_page.py` for `airline_only` and for `four` and update every comment or docstring that reads as a closed four-way enumeration, including the block immediately above `_SOURCE_ROWS` (around lines 365-367) and `resolution_stats()`'s own docstring. Say explicitly in the updated comment that `"airline_only"` still means *the static prefix table* answered, and that `"manual"` is the new fifth value covering the runtime registry — that distinction is the entire reason D-02 refused to fold the two together.

Add two checks to `companion/test_status_pages.py` and move its `EXPECTED_CHECK_COUNT` in this same edit:
1. A copy check: `_READ_ONLY_NOTE` equals the exact new string, the rendered page contains it, and the rendered page no longer contains the old note's closing phrase, the one that named the existing manual runbook. Recover its exact wording from git history; that literal belongs in the test file only, never back in `health_page.py`, since the acceptance gate greps the page module for it.
2. A source-row check: `_SOURCE_ROWS` has five entries, the fifth is the `"manual"` tuple, and a `render()` with a seeded `"manual"` count produces a row whose label cell reads `Manual` and whose total accounts for it.

Bump `EXPECTED_CHECK_COUNT` from `136` to `138` and extend the ledger comment above it in this file's established style (`138 = 136 + 2 (phase 13 plan 02 Task 1: ...)`), then re-derive the number by **running the harness**, not by arithmetic — this file's own comments state that discipline twice.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `138/138`.
    - `grep -c "coverage-gap runbook" companion/pages/health_page.py` returns `0`.
    - `grep -c "each row's Resolve link opens the Airlines page" companion/pages/health_page.py` returns `1`.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import health_page as h; assert len(h._SOURCE_ROWS) == 5 and h._SOURCE_ROWS[4][0] == 'manual'"` exits 0.
    - `grep -c "EXPECTED_CHECK_COUNT = 138" companion/test_status_pages.py` returns `1`.
  </acceptance_criteria>

  <done>Health's note names Airlines as the resolution surface, the breakdown offers five sources, and no four-source prose remains in the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the per-row Resolve deep link to both registry representations (D-10)</name>

  <files>companion/pages/health_page.py, companion/static/style.css, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` lines 1975-2115 — `_REGISTRY_HEADERS`, `_registry_filter_text()`, `_registry_row_html()`, `_registry_table_html()`, `_registry_cards_html()`. Note that `_registry_cards_html()` index-addresses `_REGISTRY_HEADERS[1]`, `[2]`, `[3]`, `[4]`.
    - `companion/static/style.css` around line 2620-2645 — `.data-card__label`, `.data-card__secondary`, `.data-card__details`, and the surrounding `.data-card__*` block where the new class belongs.
    - `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UI-SPEC.md` § Page Composition — Health item 2 (exact markup, exact link text per representation, the `aria-label` requirement) and § Accessibility & JS-Free Degradation Notes.
    - `companion/layout.py` line 429 — `escape_html()`, the single escaping choke point every interpolated value must pass through exactly once.
  </read_first>

  <behavior>
    - A rendered Health page with a seeded registry entry for prefix `XYZ` contains `href="/airlines?resolve=XYZ"` exactly twice (once in the table row, once in the mobile card).
    - Both occurrences carry `aria-label="Resolve prefix XYZ"`.
    - The desktop link's visible text is `Resolve`; the mobile link's visible text is `Resolve this prefix`.
    - A registry prefix containing HTML-hostile characters renders escaped in both the `href` and the `aria-label`, with no raw angle bracket or quote reaching the output.
    - The rendered registry section still contains no HTML form element and no submit control.
  </behavior>

  <action>
Add four module-level copy/route constants next to the other registry constants, so both representations and the harness read the same strings:
- `RESOLVE_LINK_HREF_TEMPLATE = "/airlines?resolve=%s"`
- `RESOLVE_LINK_ARIA_TEMPLATE = "Resolve prefix %s"`
- `RESOLVE_LINK_TEXT = "Resolve"`
- `RESOLVE_CARD_LINK_TEXT = "Resolve this prefix"`

**Desktop table.** Append `"Resolve"` as a sixth entry to `_REGISTRY_HEADERS` — appended, never inserted, because `_registry_cards_html()` addresses indices 1 through 4 positionally and an insertion would silently relabel every mobile card field. Add a matching sixth `<td>` to `_registry_row_html()` containing a plain anchor element built from the two templates above, with the prefix passed through `escape_html()` once for the `href` and once for the `aria-label`, and `RESOLVE_LINK_TEXT` as its content. It is a navigation control, so it is an anchor — never a submit control, never a control styled to look like a link.

**Mobile card.** In `_registry_cards_html()`, add a new block between the existing `secondary` line and the `<details>` disclosure, so the row's one actionable affordance is visible at rest and never behind a tap-to-reveal: a `div` carrying `class="data-card__action"` wrapping an anchor with the same `href` and the same `aria-label`, and `RESOLVE_CARD_LINK_TEXT` as its content. The two representations deliberately differ in visible text and are deliberately identical in `href` and `aria-label`; say so in the function's docstring.

Update both functions' docstrings to record that this column/block was added by phase 13 D-10, that Health remains read-only (the link navigates, it changes nothing), and that `_REGISTRY_HEADERS`'s positional lookups are why the new header is appended.

**CSS.** Add exactly one new rule to `companion/static/style.css`, placed with the other `.data-card__*` rules (immediately after `.data-card__details`): `.data-card__action { margin-top: var(--space-xs); }`. No colour, no size, no new token — the link inherits the page's default anchor styling. Add a one-line comment naming phase 13 D-10 and stating that the link's styling is deliberately inherited so the project's exhaustive accent-reservation list is not extended.

Add two checks to `companion/test_status_pages.py`, moving `EXPECTED_CHECK_COUNT` in this same edit:
3. A deep-link pairing check: with a seeded registry entry, the rendered page contains the `href` exactly twice and the `aria-label` exactly twice, with the two distinct visible link texts present once each; and a hostile prefix (e.g. one containing an angle bracket and a double quote) renders fully escaped in both representations.
4. A read-only-guard check, asserting on the module source that `companion/pages/health_page.py` still contains zero HTML form elements and exactly one occurrence of the button-element literal — the single pre-existing docstring reference around line 1928 that states D-16 forbids one on this page. Read the file, filter out nothing, and assert the two counts directly; this check is the machine-readable form of D-10's promise not to reopen `06.6.4.1-04`'s D-11/D-12.

Bump `EXPECTED_CHECK_COUNT` from `138` to `140`, extend the ledger comment in this file's style, and re-derive by running the harness.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `140/140`.
    - `grep -c '<form' companion/pages/health_page.py` returns `0`.
    - `grep -c '<button' companion/pages/health_page.py` returns `1` (the pre-existing docstring reference only).
    - `grep -c '<script' companion/pages/health_page.py` returns `1` (the pre-existing battery-trend script tag only — this phase adds zero JavaScript).
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import health_page as h; assert len(h._REGISTRY_HEADERS) == 6 and h._REGISTRY_HEADERS[5] == 'Resolve' and h._REGISTRY_HEADERS[1] == 'Count'"` exits 0.
    - `grep -c "data-card__action" companion/static/style.css` returns `1`.
    - `grep -c "margin-top: var(--space-xs);" companion/static/style.css` is at least `1` and the new rule declares no other property.
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 (the CSS edit did not disturb the contrast harness).
  </acceptance_criteria>

  <done>Both registry representations carry an accessible, escaped deep link to the Airlines resolve surface; Health still has no state-changing control and no new script.</done>
</task>

</tasks>

## Artifacts this phase produces

**Owned by this plan (13-02):**

| Symbol | Kind | Location |
|---|---|---|
| `RESOLVE_LINK_HREF_TEMPLATE` `RESOLVE_LINK_ARIA_TEMPLATE` `RESOLVE_LINK_TEXT` `RESOLVE_CARD_LINK_TEXT` | module constants | `companion/pages/health_page.py` |
| `_REGISTRY_HEADERS[5] == "Resolve"` | sixth column | same |
| `_SOURCE_ROWS[4] == ("manual", "Manual", ...)` | fifth source row | same |
| `_READ_ONLY_NOTE` (re-worded body) | constant | same |
| `.data-card__action` | CSS class | `companion/static/style.css` |
| `/airlines?resolve={prefix}` | outbound route contract | consumed by plan 13-04 / 13-06 |

**Produced by sibling plans (reference only — do not create here):** everything in `server/plane/manual_resolutions.py` (13-01); `enrich.airline_source_from_callsign()`, `enrich.static_airline_name_for_prefix()`, `enrich.clear_resolved_unresolved_prefix()`, the `"manual"` `route_source` value (13-03); `airlines_page` `FLASH_MANUAL_*`, `RESOLVE_ROUTE`, `MANUAL_DELETE_ROUTE_PREFIX`, `unresolved_row_for_prefix()`, `.resolve-context`, `.resolve-name-field`, `.resolve-upload-zone`, `.manual-resolution__status--superseded` (13-04); `app._illustration_filenames(state_dir)`, `Handler._handle_manual_resolve_post()`, `Handler._handle_manual_resolution_delete()`, ctx keys `resolve_prefix` / `manual_resolutions` (13-06).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADS-B feed -> `poll_state.json` `unresolved_prefixes` -> Health render | Prefixes originate in an unauthenticated, trivially spoofable callsign field and are now interpolated into an `href` and an `aria-label`, not only into a text cell. |
| Health page -> Airlines page (via query string) | The deep link hands a prefix to another page. It is a hint, never an authorisation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering (stored XSS) | `_registry_row_html()` / `_registry_cards_html()` new link | medium | mitigate | The prefix passes through `companion.layout.escape_html()` exactly once at each of the four new interpolation points (`href` and `aria-label`, in each representation). Proved by Task 2's hostile-prefix check, not by inspection. |
| T-13-08 | Spoofing | the `?resolve={prefix}` query value | medium | transfer | This page only *emits* the link. The receiving side (plan 13-04's render gate and plan 13-06's POST handler) membership-tests the prefix against the live unresolved registry before anything is shown or written (D-11). Nothing on Health may be treated as having validated it. |
| T-13-13 | Elevation of Privilege | Health acquiring a state-changing control | high | mitigate | The new affordance is a navigation anchor only. Task 2's read-only-guard check asserts on source that the module still contains zero HTML form elements and exactly one button-element literal (a docstring mention), which is the machine-readable form of D-10's promise that `06.6.4.1-04`'s D-11/D-12 is not reopened. |
| T-13-14 | Information disclosure | Health page access | low | accept | Unchanged: `/health` already sits behind `require_session()`. This plan adds no new route and no new data source. |
| T-13-SC | Tampering | package installs | n/a | accept | Zero package installs in this phase; `13-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at `140/140`.
- `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0.
- `scripts/run-all-tests.sh` exits 0.
- **D-09 standing gate:** `git diff --stat -- server/plane/illustrations.py` is empty and `server/.venv/bin/python3 server/test_illustrations.py` exits 0 with `EXPECTED_CHECK_COUNT` still `58`.
- **Zero-JavaScript gate:** `git diff -- companion/static/*.js` is empty and no new script tag appears in `companion/pages/health_page.py`.
</verification>

<success_criteria>
- Health's read-only note names Airlines as the resolution surface and no longer points at the manual runbook.
- Every registry row, in both representations, carries an escaped, `aria-label`led anchor to `/airlines?resolve={prefix}`.
- The resolution-rate breakdown offers five sources including `Manual`.
- Health gains no HTML form element, no submit control, and no script.
- `server/plane/illustrations.py` is byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-02-SUMMARY.md` when done.
</output>
