---
phase: 35-comment-purge-in-english-and-dead-code
plan: 07
type: execute
wave: 4
depends_on: [35-06]
files_modified:
  - stub-server/byos_server.py
  - stub-server/devices_cli.py
  - stub-server/make_test_panel.py
  - stub-server/test_devices_registry.py
  - stub-server/test_poll_cycle.py
  - stub-server/.gitignore
  - scripts/comment-history-pending.txt
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-01, HYG-03]

must_haves:
  truths:
    - "Every tracked stub-server/ file has 0 guard hits and is out of the pending list"
    - "The device-protocol security invariants in byos_server.py (token checks, enrolment refusal, input bounds) keep their why-comments"
    - "same-code passes with no --allow, and the full suite is green"
  artifacts:
    - path: "stub-server/byos_server.py"
      provides: "purged comments"
  key_links:
    - from: "stub-server/byos_server.py comments"
      to: "firmware/main/api_client.c protocol"
      via: "protocol facts kept as spec pointers"
      pattern: "byos"
---

<objective>
Purge stub-server/ (production and tests) and close group 3 in the same plan (HYG-01, HYG-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-06-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. close_procedure is as in 35-06-PLAN.md, with
  GROUP_PATHS = `stub-server/`, no ALLOW, and group number 3.
- Branch `claude/phase-35-g3-stub-server`, cut from `origin/main`. If the group-2 PR is not
  merged yet, stack it on the group-2 branch and set the draft PR's base to that branch.
- Phases 36 and 37-11 also edit `byos_server.py`. Merge `origin/main` first, then compute BASE.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge stub-server production files</name>
  <files>stub-server/byos_server.py, stub-server/devices_cli.py, stub-server/make_test_panel.py, stub-server/.gitignore</files>
  <read_first>
    - `CLI check --paths` output for these files, then byos_server.py bottom-up
  </read_first>
  <action>
Apply purge_rules. In `byos_server.py`, keep and tighten the why of:
- every authentication or token check;
- the MAC re-enrolment refusal;
- request-size and time-out bounds;
- the hash-to-image contract.
Each one gets a single sentence, plus the protocol pointer where there is one. Record the ratios
before and after.
  </action>
  <verify>
    <automated>git fetch -q origin main && B="$(git merge-base HEAD origin/main)" && F="stub-server/byos_server.py stub-server/devices_cli.py stub-server/make_test_panel.py stub-server/.gitignore" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest stub-server -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and `CLI check --paths` reports 0 hits
    - `pytest stub-server` passes, and ruff is clean on the files
    - `grep -ciE "token|enrol" stub-server/byos_server.py` counts comment lines that still explain the auth checks (spot-checked, listed in SUMMARY)
  </acceptance_criteria>
  <done>stub-server production is purged, with its security whys intact.</done>
</task>

<task type="auto">
  <name>Task 2: Purge stub-server tests and close group 3</name>
  <files>stub-server/test_devices_registry.py, stub-server/test_poll_cycle.py, scripts/comment-history-pending.txt, .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - `CLI check --paths` output for the two tests
  </read_first>
  <action>
Apply purge_rules to the two tests. Test names and assertion strings stay unchanged, and each
test docstring becomes one line. Then run close_procedure steps 1-7 for group 3.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $(git diff --name-only "$B" -- stub-server/) && server/.venv/bin/python scripts/check_comment_history.py check --paths $(git ls-files stub-server/) && ! grep -q '^stub-server/' scripts/comment-history-pending.txt && server/.venv/bin/python scripts/check_comment_history.py check && ./scripts/run-all-tests.sh && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - same-code over every changed stub-server/ file exits 0 with no --allow
    - `grep -c '^stub-server/' scripts/comment-history-pending.txt` prints 0, and `CLI check` exits 0
    - 35-COMMENT-RATIO.md has a stub-server section
    - The full suite and ruff are green after merging main, and a draft PR is open
  </acceptance_criteria>
  <done>Group 3 is closed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Information loss | the why of an auth or enrolment check in the device protocol is dropped | mitigate | Task 1 names these checks explicitly; the SUMMARY lists the kept comments |
| T-35-05 | Tampering | an accidental code edit on a concurrently changed file | mitigate | merge main before BASE; AST same-code |
</threat_model>

<verification>
Both tasks' commands pass.
</verification>

<success_criteria>
stub-server/ is purged and CI-enforced, its ratios are recorded, and the PR is open.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-07-SUMMARY.md`
</output>
