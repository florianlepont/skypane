---
phase: 35-comment-purge-in-english-and-dead-code
plan: 18
type: execute
wave: 9
depends_on: [35-17]
files_modified:
  - companion/static/battery-trend.js
  - companion/static/confirm-submit.js
  - companion/static/copy-button.js
  - companion/static/dirty-state.js
  - companion/static/flash-cleanup.js
  - companion/static/flight-rows.js
  - companion/static/freshness.js
  - companion/static/list-filter.js
  - companion/static/login-card.js
  - companion/static/nav-dropdown.js
  - companion/static/panel-lookup.js
  - companion/static/poll-cooldown.js
  - companion/static/quick-switch.js
  - companion/static/relative-time.js
  - companion/static/submit-guard.js
  - companion/static/theme-preview.js
  - companion/static/value-controls.js
  - scripts/comment-history-pending.txt
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-02]

must_haves:
  truths:
    - "Every companion/static/*.js file has 0 guard hits and is out of the pending list"
    - "The JS token stream (comments removed) is identical to the base for every file"
    - "Every JS file passes node --check, and the browser tests pass"
  artifacts:
    - path: "companion/static/freshness.js"
      provides: "purged comments (was 645 comment lines)"
  key_links:
    - from: "companion/static/*.js"
      to: "companion/test_browser_ux*.py"
      via: "browser tests exercise the shipped scripts"
      pattern: "static"
---

<objective>
Purge the comments of the companion JavaScript and close group 6 (HYG-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-06-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md; read "docstring" as "JSDoc block". The
  close_procedure is as in 35-06-PLAN.md, with GROUP_PATHS = `companion/static/*.js`, no ALLOW,
  and group number 6.
- Branch `claude/phase-35-g6-js`, cut from `origin/main` or stacked on the group-5 branch.
- JS specifics:
  - keep `'use strict'` (it is code);
  - keep `/*! … */` licence blocks and `// eslint-…`/`@ts-…` directives verbatim;
  - keep the why of event-ordering, debounce and time-out values, and of accessibility
    (ARIA/focus) choices;
  - `//` inside strings, template literals and regex literals is code.
- If `git ls-files 'companion/static/*.js'` lists a file not in files_modified, it is owned here
  too; record it.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge the four heaviest scripts</name>
  <files>companion/static/freshness.js, companion/static/value-controls.js, companion/static/dirty-state.js, companion/static/panel-lookup.js</files>
  <read_first>
    - `CLI check --paths` output for the four files, then each file bottom-up
  </read_first>
  <action>
Apply purge_rules and the JS specifics. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="companion/static/freshness.js companion/static/value-controls.js companion/static/dirty-state.js companion/static/panel-lookup.js" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && for f in $F; do node --check "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - same-code (token stream) exits 0 with no --allow, and there are 0 hits
    - `node --check` passes for each file; skip this only if node is absent, and record that
  </acceptance_criteria>
  <done>The four heaviest scripts are purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the remaining scripts and run the browser tests</name>
  <files>companion/static/battery-trend.js, companion/static/confirm-submit.js, companion/static/copy-button.js, companion/static/flash-cleanup.js, companion/static/flight-rows.js, companion/static/list-filter.js, companion/static/login-card.js, companion/static/nav-dropdown.js, companion/static/poll-cooldown.js, companion/static/quick-switch.js, companion/static/relative-time.js, companion/static/submit-guard.js, companion/static/theme-preview.js</files>
  <read_first>
    - `CLI check --paths` output for these files
  </read_first>
  <action>
Apply purge_rules and the JS specifics. Record the ratios before and after. Then run the browser
tests.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files 'companion/static/*.js' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && for f in $F; do node --check "$f" || exit 1; done && SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python -m pytest companion -q -n auto -k browser</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for every JS file with no --allow, and there are 0 hits
    - `node --check` passes for every file, and the browser tests pass with the browser required
  </acceptance_criteria>
  <done>All JS is purged.</done>
</task>

<task type="auto">
  <name>Task 3: Close group 6</name>
  <files>scripts/comment-history-pending.txt, .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - scripts/comment-history-pending.txt
  </read_first>
  <action>
Run close_procedure steps 1-7 for group 6.
  </action>
  <verify>
    <automated>! grep -qE '^companion/static/.*\.js$' scripts/comment-history-pending.txt && server/.venv/bin/python scripts/check_comment_history.py check && SKYPANE_REQUIRE_BROWSER=1 ./scripts/run-all-tests.sh && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - No JS path remains in the pending list, and `CLI check` exits 0
    - 35-COMMENT-RATIO.md has a JS section, and the full suite and ruff are green after merging main
    - A draft PR is open
  </acceptance_criteria>
  <done>Group 6 is closed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-11 | Tampering | a JS regex or URL mangled by a comment strip (`//` inside a literal) | mitigate | the string/regex-aware tokenizer, token-stream same-code, node --check and the browser tests |
| T-35-06 | Information loss | the why of a client-side guard is dropped (double-submit, CSRF token handling) | mitigate | purge_rules keep security invariants; the server still enforces them |
</threat_model>

<verification>
All three tasks' commands pass.
</verification>

<success_criteria>
The JS is purged and CI-enforced, the ratios are recorded, and the PR is open.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-18-SUMMARY.md`
</output>
