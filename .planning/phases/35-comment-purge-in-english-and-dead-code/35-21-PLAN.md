---
phase: 35-comment-purge-in-english-and-dead-code
plan: 21
type: execute
wave: 12
depends_on: [35-20]
files_modified:
  - firmware/main/*.c
  - firmware/main/*.h
  - firmware/main/Kconfig.projbuild
  - firmware/main/CMakeLists.txt
  - firmware/CMakeLists.txt
  - firmware/sdkconfig.defaults
  - firmware/sdkconfig.dev.defaults
  - firmware/sdkconfig.ee02.defaults
  - firmware/partitions.csv
  - firmware/.gitignore
  - firmware/build.sh
  - firmware/flash.sh
  - firmware/monitor.sh
  - firmware/provision.sh
  - firmware/tests/*.c
  - firmware/tests/*.sh
  - firmware/VENDOR.md
  - scripts/comment-history-pending.txt
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-03]

must_haves:
  truths:
    - "Gate G-34 is proven (34-11-SUMMARY.md on main, Phase 34 complete) before any firmware edit"
    - "Every firmware C/H file preprocesses to identical output (cpp -fpreprocessed same-code)"
    - "Every sdkconfig `# CONFIG_X is not set` line and every Kconfig symbol line is preserved"
    - "SPDX and Apache-2.0 attribution headers are verbatim; VENDOR.md keeps the upstream attribution, the licence facts and the list of modifications, stated concisely"
    - "The host tests, check_log_contract.sh, check_production_config.sh static and the firmware build pass"
  artifacts:
    - path: "firmware/VENDOR.md"
      provides: "concise provenance"
      contains: "Apache"
  key_links:
    - from: "firmware/tests/check_log_contract.sh"
      to: "app_main.c, state_machine.c, VENDOR.md"
      via: "grep of code lines and VENDOR rows, never comments"
      pattern: "grep"
---

<objective>
Purge the firmware comments, the last purge wave, behind gate G-34 (HYG-03). Rewrite the
provenance in `firmware/VENDOR.md` concisely. Close group 9's purge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-06-PLAN.md
@firmware/VENDOR.md
@firmware/NOTICE

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. close_procedure is as in 35-06-PLAN.md, with
  GROUP_PATHS = `firmware/`, no ALLOW, and group number 9.
- Branch `claude/phase-35-g9-firmware`.
- files_modified uses globs because Phase 34 (still in flight at planning time) may add or rename
  firmware sources. The glob set at execution time is authoritative.
- Firmware specifics:
  - SPDX lines and the Apache-2.0 / FlightPortrait / YODE PTE LTD headers stay verbatim;
  - keep datasheet/register pointers, timing units (ms/us), power-state whys and ESP-IDF quirk
    notes;
  - Kconfig `help` text may be tightened, but symbol, `default` and `depends on` lines stay;
  - `LICENSE`, `NOTICE` and `certs/*.pem` are not touched.
- Before editing a line grepped by `check_log_contract.sh`, confirm that it is code.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate G-34, then purge the firmware C and H sources</name>
  <files>firmware/main/*.c, firmware/main/*.h, firmware/tests/*.c</files>
  <read_first>
    - firmware/tests/check_log_contract.sh (what it greps)
    - `CLI check --paths $(git ls-files 'firmware/*.c' 'firmware/*.h')`
  </read_first>
  <action>
(1) G-34. After `git fetch origin main`, check that `origin/main` has
`.planning/phases/34-firmware-resilience-power-security-cleanup/34-11-SUMMARY.md` and that
ROADMAP marks Phase 34 complete. If either is missing, STOP and report "G-34 not met". Do not
edit anything.

(2) Apply purge_rules and the firmware specifics to every `.c`/`.h` under `firmware/main` and
`firmware/tests`, including `secrets.example.h` (keep its setup instructions, trimmed). Record
the ratios before and after.
  </action>
  <verify>
    <automated>git ls-tree --name-only origin/main .planning/phases/34-firmware-resilience-power-security-cleanup/ | grep -q '34-11-SUMMARY.md' && B="$(git merge-base HEAD origin/main)" && F="$(git ls-files 'firmware/*.c' 'firmware/*.h' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && ./firmware/tests/run_host_tests.sh && sh firmware/tests/check_log_contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the G-34 evidence
    - same-code (`cpp -fpreprocessed -P`) exits 0 for every C/H file with no --allow, and there are 0 hits
    - SPDX/copyright multiset preserved (same-code), and `git diff "$BASE" -- firmware | grep -E '^-.*SPDX'` is empty
    - Host tests and the log contract pass
  </acceptance_criteria>
  <done>The firmware C/H sources are purged, with identical preprocessor output.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the hash-comment files and scripts, and rewrite VENDOR.md</name>
  <files>firmware/main/Kconfig.projbuild, firmware/main/CMakeLists.txt, firmware/CMakeLists.txt, firmware/sdkconfig.defaults, firmware/sdkconfig.dev.defaults, firmware/sdkconfig.ee02.defaults, firmware/partitions.csv, firmware/.gitignore, firmware/build.sh, firmware/flash.sh, firmware/monitor.sh, firmware/provision.sh, firmware/tests/*.sh, firmware/VENDOR.md</files>
  <read_first>
    - `CLI check --paths` output for these files
    - firmware/VENDOR.md, whole file (the rows check_log_contract.sh greps)
  </read_first>
  <action>
Apply purge_rules and the firmware specifics.

For `firmware/VENDOR.md`, rewrite the provenance concisely:
- upstream project, URL and commit;
- licence: Apache-2.0, with a pointer to `LICENSE`/`NOTICE`;
- a list of the files that were modified, with one line each;
- no plan IDs.
Keep every row or string that `check_log_contract.sh` greps.

Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files firmware | grep -vE '\.(c|h|pem|md)$|/(LICENSE|NOTICE)$' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F firmware/VENDOR.md && sh firmware/tests/check_log_contract.sh && sh firmware/tests/check_production_config.sh static && grep -q "Apache" firmware/VENDOR.md</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and every `# CONFIG_… is not set` line is preserved (the same-code check treats it as code)
    - `CLI check --paths` reports 0 hits, including VENDOR.md
    - check_log_contract.sh and check_production_config.sh static pass
    - VENDOR.md names the upstream project, the Apache-2.0 licence and the modified files
  </acceptance_criteria>
  <done>The firmware config, scripts and provenance are purged.</done>
</task>

<task type="auto">
  <name>Task 3: Build proof and close group 9's purge</name>
  <files>scripts/comment-history-pending.txt, .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - .github/workflows/firmware.yml
  </read_first>
  <action>
(1) Run `./firmware/build.sh`. It needs Docker and the IDF container. If Docker is unavailable,
record that, and make the PR's `firmware.yml` run the gate.

(2) Run close_procedure steps 1-7 for group 9. The pending list is then empty except for its
header line. The PR title is "Phase 35 group 9: firmware comment purge".
  </action>
  <verify>
    <automated>test -z "$(grep -v '^#' scripts/comment-history-pending.txt)" && server/.venv/bin/python scripts/check_comment_history.py check && ./firmware/tests/run_host_tests.sh && ./scripts/run-all-tests.sh && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - The pending list has no path left, and `CLI check` exits 0
    - The firmware build passes locally, or the PR's firmware.yml run is green (URL in SUMMARY)
    - 35-COMMENT-RATIO.md has a firmware section; the full suite and ruff are green; a draft PR is open
  </acceptance_criteria>
  <done>Firmware is purged, and every tracked code file is clean.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-14 | Tampering | a macro or `#if` changed along with a comment | mitigate | `cpp -fpreprocessed -P` same-code, host tests and the build |
| T-35-15 | Tampering | an sdkconfig `is not set` line removed, changing the production build | mitigate | same-code treats it as code; check_production_config.sh static |
| T-35-16 | Repudiation | licence attribution lost | mitigate | SPDX multiset in same-code; VENDOR.md content check; LICENSE/NOTICE untouched |
</threat_model>

<verification>
All three tasks' commands pass, and the firmware build or firmware.yml run is green.
</verification>

<success_criteria>
The firmware is purged with identical preprocessed code, the provenance is concise, and nothing
is pending.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-21-SUMMARY.md`
</output>
