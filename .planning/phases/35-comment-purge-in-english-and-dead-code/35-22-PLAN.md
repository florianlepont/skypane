---
phase: 35-comment-purge-in-english-and-dead-code
plan: 22
type: execute
wave: 13
depends_on: [35-21]
files_modified:
  - scripts/check_comment_history.py
  - scripts/comment-history-pending.txt
  - test-support/test_check_comment_history.py
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03, HYG-04, HYG-05, HYG-06]

must_haves:
  truths:
    - "The pending mechanism is gone: the guard scans every tracked code file, and CI is green"
    - "The mutation proof re-run on the final tree still fails for each removed alternative, and a planted ID in any code file fails `check`"
    - "35-COMMENT-RATIO.md has the final table: every file before/after, per-group totals, overall totals and the style.css raw/gzip shipped size"
    - "Every HYG requirement is confirmed with evidence"
  artifacts:
    - path: ".planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md"
      provides: "final ratio table"
      contains: "Total"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check_comment_history.py check"
      via: "unchanged step, now with no pending list"
      pattern: "check_comment_history.py check"
---

<objective>
Close Phase 35: remove the ratchet so the guard covers everything, re-prove it by mutation,
assemble the final ratio table and confirm HYG-01..06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-01-SUMMARY.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-13-SUMMARY.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-BASELINE/INDEX.md

The CLI is as in 35-06-PLAN.md. Branch `claude/phase-35-close`, cut from `origin/main` or stacked
on the group-9 branch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the pending mechanism and re-prove the guard</name>
  <files>scripts/check_comment_history.py, scripts/comment-history-pending.txt, test-support/test_check_comment_history.py</files>
  <read_first>
    - scripts/check_comment_history.py (the pending-list code path)
    - test-support/test_check_comment_history.py (the pending tests)
  </read_first>
  <action>
(1) Run `git rm scripts/comment-history-pending.txt`.

(2) Remove the pending-list reading from the tool, along with its tests. `check` with no args now
scans every tracked file with a known comment syntax, and `--paths` keeps working.

(3) Re-run the 35-01 mutation proof on this tree: remove each regex alternative in a scratch
copy, and confirm a test fails. Plant `# see D-06` (or the equivalent in the file's syntax) in one
file of each language (Python, C, JS, CSS, shell, YAML) and confirm `check` exits non-zero each
time. Revert every plant. Record everything in the SUMMARY.
  </action>
  <verify>
    <automated>test ! -e scripts/comment-history-pending.txt && ! grep -q "comment-history-pending" scripts/check_comment_history.py && server/.venv/bin/python scripts/check_comment_history.py check && server/.venv/bin/python -m pytest test-support/test_check_comment_history.py -q && git diff --quiet -- server companion stub-server deploy firmware</automated>
  </verify>
  <acceptance_criteria>
    - The pending file and every reference to it are gone. `grep -rn "comment-history-pending" scripts .github test-support` prints nothing
    - `CLI check` exits 0 over the whole tree
    - The mutation and plant results are in the SUMMARY: each alternative is killed, and each language plant exits non-zero
  </acceptance_criteria>
  <done>The guard covers every tracked code file, with no exceptions.</done>
</task>

<task type="auto">
  <name>Task 2: Final ratio table, requirement confirmation and the phase PR</name>
  <files>.planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md (the group sections)
  </read_first>
  <action>
(1) Run `CLI ratio --before 35-BASELINE/ratio-before.tsv --markdown` over all tracked files and
write a "Final" section:
- every file before and after;
- per-group totals and the overall total;
- the style.css raw and gzip bytes before and after;
- the list of files still above 35 %, with their justifications.
Keep the group sections above it.

(2) Add a "Requirements" section with the evidence for each requirement:
- HYG-01/02/03: 0 hits over the whole tree, and same-code passed per group (PR links);
- HYG-04: the lines in `.claude/CLAUDE.md` and `CONTRIBUTING.md`;
- HYG-05: the deleted names and `git grep` output from 35-13;
- HYG-06: the CI step, and the mutation proof from Task 1.

(3) Run `./scripts/run-all-tests.sh`, `ruff check .`, the firmware host tests,
`check_log_contract.sh` and `check_production_config.sh static`.

(4) Merge `origin/main`, push, and open a draft PR titled "Phase 35 close: comment guard covers
the whole tree".
  </action>
  <verify>
    <automated>grep -q "Final" .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md && grep -q "HYG-06" .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md && grep -qi "gzip" .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md && ./scripts/run-all-tests.sh && server/.venv/bin/ruff check . && ./firmware/tests/run_host_tests.sh && sh firmware/tests/check_log_contract.sh && sh firmware/tests/check_production_config.sh static</automated>
  </verify>
  <acceptance_criteria>
    - 35-COMMENT-RATIO.md has the Final table (every file, group and overall totals, the style.css shipped size) and the Requirements evidence for HYG-01..06
    - The full suite, ruff and the firmware checks are green; the PR's CI (lint job with the guard) is green; a draft PR is open
  </acceptance_criteria>
  <done>Phase 35 is closed with evidence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | the guard is weakened while the pending code is removed | mitigate | the mutation proof and per-language plants re-run on the final tree |
| T-35-04 | Denial of service | a file missed by every group makes CI red after removal | mitigate | `CLI check` runs over the whole tree before push; any hit is purged here and recorded |
</threat_model>

<verification>
Both tasks' commands pass, and the PR CI is green.
</verification>

<success_criteria>
Every tracked code file is guarded, the ratio table is final, and HYG-01..06 are confirmed.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-22-SUMMARY.md`
</output>
