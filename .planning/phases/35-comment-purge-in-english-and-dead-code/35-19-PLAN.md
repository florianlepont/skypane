---
phase: 35-comment-purge-in-english-and-dead-code
plan: 19
type: execute
wave: 10
depends_on: [35-18]
files_modified:
  - companion/static/style.css
  - scripts/comment-history-pending.txt
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-02]

must_haves:
  truths:
    - "style.css has 0 guard hits and is out of the pending list"
    - "The CSS token stream (comments removed) is identical to the base"
    - "The shipped size of style.css is reported raw and gzip, before and after"
    - "Section-heading comments that aid navigation and the why of design-token values survive"
  artifacts:
    - path: "companion/static/style.css"
      provides: "purged comments (was 6 842 comment lines, ~512 KB)"
  key_links:
    - from: "style.css tokens"
      to: ".claude/skills/sketch-findings-skypane"
      via: "the design system stays documented in the skill, not in CSS comments"
      pattern: "--"
---

<objective>
Purge the comments of `companion/static/style.css` in sections and close group 7 (HYG-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-06-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-BASELINE/INDEX.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. close_procedure is as in 35-06-PLAN.md, with
  GROUP_PATHS = `companion/static/style.css`, no ALLOW, and group number 7.
- Branch `claude/phase-35-g7-css`.
- CSS specifics:
  - keep one short heading comment per major section;
  - keep the why of contrast values (WCAG ratio), of e-ink/print or `prefers-*` media choices,
    and of specificity hacks;
  - drop plan/UI-SPEC narratives and restated property lists;
  - `/*` inside strings or `url()` is code.
- Sections: at plan start, split the file into halves S1/S2 at a top-level section heading
  comment near the middle (record the line). Work bottom-up (S2, then S1).
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge the lower half of style.css (S2)</name>
  <files>companion/static/style.css</files>
  <read_first>
    - `CLI check --paths companion/static/style.css`, filtered to S2
  </read_first>
  <action>
Before editing, record the raw and gzip bytes (`wc -c`; `gzip -9c | wc -c`). They should match
INDEX.md unless main changed the file; if so, note it. Apply purge_rules and the CSS specifics to
S2, then commit.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - The token-stream same-code exits 0 with no --allow
    - `CLI check --paths` shows no hit in S2
  </acceptance_criteria>
  <done>S2 is purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the upper half of style.css (S1) and run the browser tests</name>
  <files>companion/static/style.css</files>
  <read_first>
    - `CLI check --paths companion/static/style.css`
  </read_first>
  <action>
Apply purge_rules and the CSS specifics to S1, including the file header. Record the raw and gzip
bytes after, and the ratio.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/static/style.css && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/static/style.css && SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python -m pytest companion -q -n auto -k "browser or contrast or theme"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and there are 0 hits
    - The browser, contrast and theme tests pass
  </acceptance_criteria>
  <done>style.css is purged.</done>
</task>

<task type="auto">
  <name>Task 3: Close group 7 with the shipped-size report</name>
  <files>scripts/comment-history-pending.txt, .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - scripts/comment-history-pending.txt
  </read_first>
  <action>
Run close_procedure steps 1-7 for group 7. In the style.css section of 35-COMMENT-RATIO.md, add
the shipped size: raw bytes and gzip -9 bytes, before and after, with the percentage saved.
  </action>
  <verify>
    <automated>! grep -q '^companion/static/style.css' scripts/comment-history-pending.txt && server/.venv/bin/python scripts/check_comment_history.py check && grep -qi "gzip" .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md && SKYPANE_REQUIRE_BROWSER=1 ./scripts/run-all-tests.sh && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - style.css is not in the pending list, and `CLI check` exits 0
    - 35-COMMENT-RATIO.md reports the style.css raw and gzip bytes before and after
    - The full suite and ruff are green after merging main, and a draft PR is open
  </acceptance_criteria>
  <done>Group 7 is closed, with the size reported.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-11 | Tampering | a rule mangled by a comment strip (`/*` in a string or `url()`) | mitigate | the string-aware tokenizer, token same-code and the browser tests |
</threat_model>

<verification>
All three tasks' commands pass.
</verification>

<success_criteria>
style.css is purged, its size is reported, and the PR is open.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-19-SUMMARY.md`
</output>
