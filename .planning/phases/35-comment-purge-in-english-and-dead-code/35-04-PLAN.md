---
phase: 35-comment-purge-in-english-and-dead-code
plan: 04
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - server/poll_loop.py
  - server/device_config.py
  - server/history_db.py
  - server/wake.py
  - server/notify.py
  - server/panel_format.py
  - server/panel_preview.py
  - server/requirements.in
  - server/requirements-dev.in
  - server/.gitignore
  - server/state/.gitignore
autonomous: true
requirements: [HYG-01, HYG-03]

must_haves:
  truths:
    - "No comment or docstring in server top-level production files carries a history ID"
    - "The code is unchanged: same-code passes with no --allow"
    - "run_once keeps only the comments that explain ordering, locking or failure-handling constraints"
  artifacts:
    - path: "server/poll_loop.py"
      provides: "purged comments (run_once no longer mostly comments)"
  key_links:
    - from: "server/requirements*.in comments"
      to: "scripts/lock-deps.sh"
      via: "the .in files feed the hash lock"
      pattern: "lock-deps"
---

<objective>
Purge the comments and docstrings of the server's top-level production modules, plus the
hash-comment files `requirements*.in` and `.gitignore` (HYG-01, HYG-03). Do not touch
`server/requirements*.txt`: those files are generated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md

<conventions>
The CLI, group branch (`claude/phase-35-g2-server`), BASE and purge_rules are as in 35-02-PLAN.md
`<context>`.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge poll_loop.py</name>
  <files>server/poll_loop.py</files>
  <read_first>
    - `CLI check --paths server/poll_loop.py`, then the file bottom-up (`run_once` is the largest block)
  </read_first>
  <action>
Apply purge_rules. In `run_once`, keep the comments that state:
- ordering constraints (what must happen before what, and why);
- failure handling (which errors are swallowed, and why a cycle still completes);
- file-format and units facts.
Drop the step-by-step narration of the code. Record the ratio before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" server/poll_loop.py && server/.venv/bin/python scripts/check_comment_history.py check --paths server/poll_loop.py && server/.venv/bin/python -m pytest server/test_poll_loop.py server/test_pipeline_e2e.py -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - `CLI same-code --base "$BASE" server/poll_loop.py` exits 0 with no --allow
    - `CLI check --paths server/poll_loop.py` reports 0 hits
    - test_poll_loop and test_pipeline_e2e pass, and ruff is clean
  </acceptance_criteria>
  <done>poll_loop.py is purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the other server production modules and hash-comment files</name>
  <files>server/device_config.py, server/history_db.py, server/wake.py, server/notify.py, server/panel_format.py, server/panel_preview.py, server/requirements.in, server/requirements-dev.in, server/.gitignore, server/state/.gitignore</files>
  <read_first>
    - `CLI check --paths` output for these files, then each file
  </read_first>
  <action>
Apply purge_rules.
- `device_config.py`: keep the validation-bound whys (why a range or format is enforced).
- `history_db.py`: keep the schema, units and retention facts.
- The `.in` files: keep one comment per dependency only where it explains why the dependency or
  pin exists.
Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="server/device_config.py server/history_db.py server/wake.py server/notify.py server/panel_format.py server/panel_preview.py server/requirements.in server/requirements-dev.in server/.gitignore server/state/.gitignore" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest server -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for all ten files with no --allow, so the non-comment lines of the .in/.gitignore files are identical
    - `CLI check --paths` reports 0 hits for the ten files
    - `pytest server` passes and `git diff --stat -- server/requirements*.txt` is empty
  </acceptance_criteria>
  <done>The server's top-level production files are purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | an accidental code or dependency-line edit | mitigate | AST and non-comment-line same-code, the server suite, and a check that the .txt locks are untouched |
| T-35-06 | Information loss | a why behind a config validation bound is dropped | mitigate | purge_rules: rewrite security invariants, never drop them |
</threat_model>

<verification>
The commands pass, and `pytest server -q -n auto` is green.
</verification>

<success_criteria>
All 11 files have 0 hits and same-code passes. The SUMMARY records the per-file ratios and
justifies any file still above 35 %.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-04-SUMMARY.md`
</output>
