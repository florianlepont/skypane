---
phase: 35-comment-purge-in-english-and-dead-code
plan: 15
type: execute
wave: 7
depends_on: [35-13]
files_modified:
  - companion/test_status_pages.py
  - companion/test_status_pages_01.py
  - companion/test_status_pages_02.py
  - companion/test_status_pages_03.py
  - companion/test_status_pages_04.py
  - companion/test_status_pages_helpers.py
  - companion/test_view_pages_01.py
  - companion/test_view_pages_02.py
  - companion/test_view_pages_03.py
  - companion/test_view_pages_04.py
  - companion/test_view_pages_helpers.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "Every companion/test_status_pages*.py and test_view_pages*.py file has 0 guard hits"
    - "Code is unchanged relative to the post-35-13 tree (same-code with no --allow)"
  artifacts:
    - path: "companion/test_status_pages.py"
      provides: "purged test comments"
  key_links:
    - from: "test node ids"
      to: "33-MIGRATION-LEDGER.md"
      via: "unchanged names"
      pattern: "def test_"
---

<objective>
Purge the comments and docstrings of the status_pages and view_pages test families (~4 000
comment lines) (HYG-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-14-PLAN.md

<conventions>
- The CLI, purge_rules, test-docstring convention and group-5 branch are as in 35-14-PLAN.md.
- Ownership: every file matching `companion/test_status_pages*.py` or
  `companion/test_view_pages*.py`.
- 35-13 edited some of these files for HYG-05. BASE is the merge-base with a main that already
  contains group 4. If group 4 is not yet merged, use BASE = the tip of the group-4 branch, so
  that same-code compares against post-deletion code.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge the status_pages family</name>
  <files>companion/test_status_pages.py, companion/test_status_pages_01.py, companion/test_status_pages_02.py, companion/test_status_pages_03.py, companion/test_status_pages_04.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - `CLI check --paths` output for the family
  </read_first>
  <action>
Record the collected count first. Apply purge_rules. `test_status_pages.py` (~7 000 lines) is
done bottom-up. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files 'companion/test_status_pages*.py' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest $F -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow against the post-35-13 base, and there are 0 hits
    - The family passes, and the count is unchanged
  </acceptance_criteria>
  <done>status_pages is purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the view_pages family</name>
  <files>companion/test_view_pages_01.py, companion/test_view_pages_02.py, companion/test_view_pages_03.py, companion/test_view_pages_04.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - `CLI check --paths` output for the family
  </read_first>
  <action>
Apply purge_rules. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files 'companion/test_view_pages*.py' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest $F -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and there are 0 hits
    - The family passes
  </acceptance_criteria>
  <done>view_pages is purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | a test weakened by an edit | mitigate | AST same-code against the post-deletion base, and an unchanged count |
</threat_model>

<verification>
Both families pass.
</verification>

<success_criteria>
Both families have 0 hits and their code is unchanged. The ratios are recorded.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-15-SUMMARY.md`
</output>
