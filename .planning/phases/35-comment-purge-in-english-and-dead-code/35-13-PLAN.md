---
phase: 35-comment-purge-in-english-and-dead-code
plan: 13
type: execute
wave: 6
depends_on: [35-08, 35-09, 35-10, 35-11, 35-12]
files_modified:
  - companion/pages/health_page.py
  - companion/draw.py
  - companion/app.py
  - companion/auth.py
  - companion/pages/__init__.py
  - companion/test_companion_app_02.py
  - companion/test_status_pages.py
  - companion/test_status_pages_01.py
  - companion/test_status_pages_04.py
  - scripts/comment-history-pending.txt
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-05, HYG-01]

must_haves:
  truths:
    - "Each HYG-05 candidate that has no production caller on current main is deleted, together with the tests that exercise only it"
    - "A test that uses a dead function as an oracle for live behaviour is re-pointed to the live function, keeping its name, or deleted when no live equivalent exists; each case is recorded"
    - "No comment in companion production code describes a removed feature"
    - "Group-4 same-code passes, with --allow listing exactly the files where code was deleted, plus app.py for __doc__"
    - "Every companion production .py file is out of the pending list, and the full suite is green"
  artifacts:
    - path: "companion/pages/health_page.py"
      provides: "without health_severity/anomaly_active (if confirmed dead)"
  key_links:
    - from: "companion/app.py"
      to: "health_state['severity']"
      via: "the live severity path that replaces health_severity"
      pattern: "severity"
---

<objective>
Delete the HYG-05 dead code that is confirmed dead on current main, with its test-only tests, and
close group 4 (companion Python production).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-06-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-08-SUMMARY.md

<planner_findings>
The state of each candidate at planning time (`git grep -nw`):
- `health_severity`: defined in `pages/health_page.py` (~2364). It is called by
  `anomaly_active`, and by `test_status_pages.py:~5982`. Mentioned in comments in `app.py` and
  `pages/__init__.py`.
- `anomaly_active`: defined at ~2390. It is called by `test_status_pages_01.py` (the
  degrade-safely root-safety test, ~455-502) and `test_status_pages_04.py` (a banner-agreement
  test, ~629-670). `test_suite_guards.py:~373` holds a string sample: that is a code literal,
  so leave it.
- `usable_pairs`: `draw.py:294`. Tests in `test_companion_app_02.py`.
- `label_grid`: `draw.py:692`. No test.
- Do NOT confuse the dead function with the live context key: `ctx["health_severity"]` in
  `app.py` (~1627, ~2928), fed from `health_state["severity"]`, is live code and stays. The
  `pages/__init__.py` docstring describing that key stays too, rewritten under purge_rules.
- The `.claude/skills/sketch-findings-skypane` mentions are out of scope. Note them in the
  SUMMARY and leave them.
</planner_findings>

The CLI, close_procedure (35-06-PLAN.md) and group branch `claude/phase-35-g4-companion-py` apply.
GROUP_PATHS = the companion production Python, i.e. `companion/*.py` (not `test_*`/`conftest.py`),
`companion/pages/` and `companion/i18n_fr/`. Group number 4.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-check the candidates and delete the dead code</name>
  <files>companion/pages/health_page.py, companion/draw.py, companion/app.py, companion/auth.py, companion/pages/__init__.py, companion/test_companion_app_02.py, companion/test_status_pages.py, companion/test_status_pages_01.py, companion/test_status_pages_04.py</files>
  <read_first>
    - `git grep -nw -e health_severity -e anomaly_active -e usable_pairs -e label_grid -- ':!.planning' ':!.claude'`
    - app.py, only where it builds `health_state` (grep `severity`) to find the live severity function
  </read_first>
  <action>
(1) For each candidate, list its non-test, non-comment callers. A candidate with a production
caller is NOT dead: keep it and record why.

(2) For each dead candidate:
- delete the def;
- delete any helper that becomes unreferenced only because of this deletion, after confirming
  with `git grep -nw`;
- delete the comments that describe it.

(3) For each test that calls a deleted function:
- (a) If it only tests that function, delete the test.
- (b) If it uses the function as an oracle for live behaviour (the root-safety degrade-safely
  check, or banner agreement), re-point the call to the live function app.py uses for
  `health_state` severity. Keep the test name, and keep the assertion intent and message.
- (c) If there is no live equivalent, delete the test.
Record each test node id with a, b or c in the SUMMARY.

(4) Confirm that no comment in `app.py` (~3450/3514) or `auth.py` (~63) still narrates a removed
feature. Keep any why that is still true.

(5) Deleting a whole test module is not expected. If it happens, record it.
  </action>
  <verify>
    <automated>! git grep -nE "def (health_severity|anomaly_active|usable_pairs|label_grid)\b|[.](health_severity|anomaly_active|usable_pairs|label_grid)[(]|^from .* import .*\b(usable_pairs|label_grid|health_severity|anomaly_active)\b" -- companion ':!companion/test_suite_guards.py' && server/.venv/bin/python -m pytest companion -q -n auto && server/.venv/bin/ruff check companion</automated>
  </verify>
  <acceptance_criteria>
    - For every candidate deleted, no `def`, attribute call or import of it remains in companion (except the string sample in test_suite_guards.py); the live `ctx["health_severity"]` key is untouched; any kept candidate has a recorded production caller
    - `pytest companion` passes, and the SUMMARY lists each affected test id as a/b/c
    - The coverage gate still holds; verified by the full run in Task 2
  </acceptance_criteria>
  <done>HYG-05 dead code is gone, and live behaviour stays covered.</done>
</task>

<task type="auto">
  <name>Task 2: Group-4 proof, ratchet and PR</name>
  <files>scripts/comment-history-pending.txt, .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - The 35-08..35-12 SUMMARYs (ratios, justifications)
  </read_first>
  <action>
Run close_procedure steps 1-7 for group 4.
- ALLOW is exactly `--allow companion/app.py`, plus `--allow` for each production file where
  Task 1 deleted code (health_page.py and/or draw.py).
- The test files changed by Task 1 belong to group 5's paths. Check them separately with
  `CLI same-code --base "$BASE" --allow <each test file changed in Task 1>`, and list them in
  the SUMMARY.
- Remove the companion production paths from the pending list, but keep `companion/test_*`,
  `companion/conftest.py` and `companion/static/`.
  </action>
  <verify>
    <automated>git fetch -q origin main && B="$(git merge-base HEAD origin/main)" && P="$(git ls-files 'companion/*.py' 'companion/pages/*.py' 'companion/i18n_fr/*.py' | grep -vE '^companion/(test_|conftest)')" && server/.venv/bin/python scripts/check_comment_history.py check --paths $P && ! grep -qE '^companion/(pages/|i18n_fr/|app\.py|auth\.py|layout\.py|draw\.py)' scripts/comment-history-pending.txt && server/.venv/bin/python scripts/check_comment_history.py check && ./scripts/run-all-tests.sh && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `CLI same-code --base "$BASE" --allow companion/app.py [--allow deleted-code files] <changed companion production files>` exits 0, and the allow list in the SUMMARY matches Task 1 exactly
    - `CLI check --paths` over the companion production .py reports 0 hits, and `CLI check` exits 0
    - 35-COMMENT-RATIO.md has a companion-Python section with a group total
    - The full suite (coverage gate included) and ruff are green after merging main, and a draft PR is open
  </acceptance_criteria>
  <done>Group 4 is closed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Denial of service | deleting a function that is still reachable (dynamic lookup, template) | mitigate | `git grep -nw` on current main plus the full suite; keep and record any candidate with a caller |
| T-35-10 | Information loss | root-safety coverage lost with the anomaly_active test | mitigate | re-point oracle tests to the live severity function rather than deleting them |
</threat_model>

<verification>
The full suite is green, and grep proves the deleted names are absent.
</verification>

<success_criteria>
HYG-05 is delivered, group 4 is enforced by CI, and the PR is open.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-13-SUMMARY.md`
</output>
