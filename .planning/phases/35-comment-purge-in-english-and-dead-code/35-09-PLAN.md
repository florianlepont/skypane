---
phase: 35-comment-purge-in-english-and-dead-code
plan: 09
type: execute
wave: 5
depends_on: [35-07]
files_modified:
  - companion/pages/config_page.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "companion/pages/config_page.py has 0 guard hits"
    - "same-code passes with no --allow"
    - "Form-validation and bound-enforcement whys survive; UI string literals (EN and FR) are untouched"
  artifacts:
    - path: "companion/pages/config_page.py"
      provides: "purged comments (was ~67 % comments)"
  key_links:
    - from: "config_page.py validation comments"
      to: "server/device_config.py bounds"
      via: "the bound's why kept in one place, pointed to from the other"
      pattern: "device_config"
---

<objective>
Purge the comments and docstrings of the largest companion module,
`companion/pages/config_page.py` (~4 400 comment lines), in three bottom-up chunks (HYG-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-08-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. The group branch, BASE and the "comments only,
  no dead-code deletion" rule are as in 35-08-PLAN.md.
- Chunking: at plan start, run `grep -n '^def \|^class ' companion/pages/config_page.py` and pick
  two top-level definitions near 1/3 and 2/3 of the list (at planning time, ~line 2598 and ~line
  4157). Name them in the SUMMARY: C1 = start to before the first, C2 = the first to before the
  second, C3 = the second to the end. Go bottom-up (C3, then C2, then C1) so the unprocessed line
  numbers do not move.
- Keep HTML inside Python strings exactly as it is: it is shipped markup.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge chunk C3 (last third)</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    - `CLI check --paths companion/pages/config_page.py`, filtered to C3's line range
  </read_first>
  <action>
Apply purge_rules to C3. Keep the why of every value bound, clamp and cross-field rule in a single
sentence. Commit.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/pages/config_page.py && server/.venv/bin/python -c "import companion.pages.config_page"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow
    - `CLI check --paths companion/pages/config_page.py` shows no hit at or after C3's first line
  </acceptance_criteria>
  <done>C3 is purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge chunk C2 (middle third)</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    - `CLI check --paths companion/pages/config_page.py`, filtered to C2's range
  </read_first>
  <action>
Apply purge_rules to C2, then commit.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/pages/config_page.py && server/.venv/bin/python -c "import companion.pages.config_page"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow
    - `CLI check --paths` shows no hit at or after C2's first line
  </acceptance_criteria>
  <done>C2 is purged.</done>
</task>

<task type="auto">
  <name>Task 3: Purge chunk C1 (first third, including the module docstring), then run the tests</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    - `CLI check --paths companion/pages/config_page.py`
  </read_first>
  <action>
Apply purge_rules to C1. The module docstring becomes a short summary of what the page does, with
no history. Record the ratio before and after, and justify it if it is still above 35 %.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/pages/config_page.py && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/pages/config_page.py && server/.venv/bin/python -m pytest companion -q -n auto -k "config"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow
    - `CLI check --paths companion/pages/config_page.py` reports 0 hits
    - The config-page tests pass (`-k config`); the full companion run happens in 35-13
  </acceptance_criteria>
  <done>config_page.py is purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | an edit to a UI string or HTML-in-string that looked like a comment | mitigate | AST same-code compares every string constant |
| T-35-06 | Information loss | the why of an input bound is dropped | mitigate | Task 1 requires a one-sentence why per bound |
</threat_model>

<verification>
same-code, check and the config tests pass.
</verification>

<success_criteria>
config_page.py has 0 hits, its code is unchanged, and the ratio is recorded.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-09-SUMMARY.md`
</output>
