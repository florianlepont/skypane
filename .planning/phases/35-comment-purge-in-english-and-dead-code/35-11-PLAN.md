---
phase: 35-comment-purge-in-english-and-dead-code
plan: 11
type: execute
wave: 5
depends_on: [35-07]
files_modified:
  - companion/layout.py
  - companion/draw.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "layout.py and draw.py have 0 guard hits"
    - "same-code passes with no --allow; usable_pairs and label_grid still exist (deleted in 35-13)"
    - "Geometry units (px, panel coordinates) and palette constraints keep their why"
  artifacts:
    - path: "companion/layout.py"
      provides: "purged comments (was ~65 % comments)"
  key_links:
    - from: "draw.py palette comments"
      to: "E Ink Spectra 6 panel palette"
      via: "datasheet pointer kept"
      pattern: "Spectra"
---

<objective>
Purge the comments and docstrings of `companion/layout.py` and `companion/draw.py` (HYG-01).
Dead functions stay for 35-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-08-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. The group branch, BASE and the comments-only
  rule are as in 35-08-PLAN.md.
- layout.py (~2 700 comment lines) is split in two at a top-level def near the middle. Do the
  lower half first.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge the lower half of layout.py</name>
  <files>companion/layout.py</files>
  <read_first>
    - `CLI check --paths companion/layout.py`, filtered to the lower half
  </read_first>
  <action>
Apply purge_rules. Keep units, coordinate conventions and the why of the magic numbers. Commit.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/layout.py && server/.venv/bin/python -c "import companion.layout"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow
    - `CLI check --paths` shows no hit in the lower half
  </acceptance_criteria>
  <done>The lower half is purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the upper half of layout.py</name>
  <files>companion/layout.py</files>
  <read_first>
    - `CLI check --paths companion/layout.py`
  </read_first>
  <action>
Apply purge_rules to the rest of the file, including the module docstring. Record the ratio
before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/layout.py && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/layout.py</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and there are 0 hits
  </acceptance_criteria>
  <done>layout.py is purged.</done>
</task>

<task type="auto">
  <name>Task 3: Purge draw.py, then run the tests</name>
  <files>companion/draw.py</files>
  <read_first>
    - `CLI check --paths companion/draw.py`, then the file bottom-up
  </read_first>
  <action>
Apply purge_rules, and keep the panel palette and datasheet pointers. `usable_pairs` and
`label_grid` stay defined, though their docstrings may shrink. Record the ratio before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/layout.py companion/draw.py && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/layout.py companion/draw.py && git grep -qw "def usable_pairs" companion/draw.py && server/.venv/bin/python -m pytest companion -q -n auto -k "not browser"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for both files with no --allow, and there are 0 hits
    - `usable_pairs` and `label_grid` are still defined
    - The non-browser companion tests pass
  </acceptance_criteria>
  <done>layout.py and draw.py are purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | an accidental geometry-constant edit | mitigate | AST same-code compares every constant |
</threat_model>

<verification>
same-code, check and the tests pass.
</verification>

<success_criteria>
Both files have 0 hits and their code is unchanged. The ratios are recorded.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-11-SUMMARY.md`
</output>
