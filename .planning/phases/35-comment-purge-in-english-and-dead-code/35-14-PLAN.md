---
phase: 35-comment-purge-in-english-and-dead-code
plan: 14
type: execute
wave: 7
depends_on: [35-13]
files_modified:
  - companion/test_browser_ux.py
  - companion/test_browser_ux_01.py
  - companion/test_browser_ux_02.py
  - companion/test_browser_ux_health_drawings.py
  - companion/test_browser_ux_helpers.py
  - companion/test_browser_ux_quiet_wake.py
  - companion/test_browser_origin.py
  - companion/test_browser_policy.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "Every companion/test_browser_*.py file has 0 guard hits"
    - "Test names and assertion strings are unchanged (AST same-code), and the collected browser test count is unchanged"
  artifacts:
    - path: "companion/test_browser_ux.py"
      provides: "purged test comments"
  key_links:
    - from: "test node ids"
      to: "33-MIGRATION-LEDGER.md"
      via: "unchanged names"
      pattern: "def test_"
---

<objective>
Purge the comments and docstrings of the browser test family (~5 200 comment lines) (HYG-01, tests
in scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-05-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md, and the test-docstring convention is as in
  35-05-PLAN.md.
- Group branch `claude/phase-35-g5-companion-tests`, cut from `origin/main` or stacked on the
  group-4 branch. BASE = `git merge-base HEAD origin/main`.
- Ownership: this plan owns every file matching `git ls-files 'companion/test_browser_*.py'`. If
  that list differs from files_modified (for example after a rename on main), the glob wins;
  record the difference in the SUMMARY.
- JS/CSS strings passed to `page.evaluate` are string literals. Leave them.
- Before editing, record `pytest --collect-only -q companion -k browser | tail -1`.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge test_browser_ux.py and test_browser_ux_helpers.py</name>
  <files>companion/test_browser_ux.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - `CLI check --paths` output for the two files
  </read_first>
  <action>
Apply purge_rules. Keep the why of waits, time-outs and viewport sizes, because they explain
flake-avoidance. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="companion/test_browser_ux.py companion/test_browser_ux_helpers.py" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and `CLI check --paths` reports 0 hits
  </acceptance_criteria>
  <done>The two largest browser files are purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the other browser test files and run them</name>
  <files>companion/test_browser_ux_01.py, companion/test_browser_ux_02.py, companion/test_browser_ux_health_drawings.py, companion/test_browser_ux_quiet_wake.py, companion/test_browser_origin.py, companion/test_browser_policy.py</files>
  <read_first>
    - `CLI check --paths` output for these files
  </read_first>
  <action>
Apply purge_rules and record the ratios before and after. Then run the browser family and check
that the collected count equals the recorded one.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files 'companion/test_browser_*.py' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python -m pytest $F -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for every browser test file with no --allow, and there are 0 hits
    - The browser tests pass with SKYPANE_REQUIRE_BROWSER=1 (no skips), and the count is unchanged
  </acceptance_criteria>
  <done>The browser family is purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | a test weakened by an edit | mitigate | AST same-code and an unchanged collected count |
</threat_model>

<verification>
The browser tests pass with the browser required.
</verification>

<success_criteria>
The browser family has 0 hits and its code is unchanged. The ratios are recorded.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-14-SUMMARY.md`
</output>
