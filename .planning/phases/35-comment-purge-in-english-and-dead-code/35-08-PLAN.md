---
phase: 35-comment-purge-in-english-and-dead-code
plan: 08
type: execute
wave: 5
depends_on: [35-07]
files_modified:
  - companion/app.py
  - companion/auth.py
  - companion/__init__.py
  - companion/battery.py
  - companion/frame_state.py
  - companion/i18n.py
  - companion/prefs.py
  - companion/screens.py
  - companion/wake.py
  - companion/illustration_normalize.py
  - companion/contrast_check.py
  - companion/theme_preview.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "No comment or docstring in these companion modules carries a history ID"
    - "same-code passes, with only companion/app.py allowed and only for its argparse module docstring"
    - "Auth, session, CSRF/origin and throttling security invariants in app.py/auth.py keep a one-line why"
    - "No function is deleted here (HYG-05 belongs to 35-13)"
  artifacts:
    - path: "companion/app.py"
      provides: "purged comments; --help text accurate"
  key_links:
    - from: "companion/app.py module docstring"
      to: "argparse --help"
      via: "ArgumentParser(description=__doc__)"
      pattern: "description=__doc__"
---

<objective>
Purge the comments and docstrings of `companion/app.py`, `auth.py` and the small companion
modules (HYG-01). Code is untouched, and dead code is left for 35-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md.
- Group branch `claude/phase-35-g4-companion-py`, cut from `origin/main`, or stacked on the
  group-3 branch if that PR is unmerged. BASE = `git merge-base HEAD origin/main`.
- Group 4 rule: purge comments only. Do not delete `health_severity`, `anomaly_active`,
  `usable_pairs` or `label_grid`, or their tests. Comments about removed features (the
  display-mode cookie in `auth.py`, and in `app.py`) are change narratives, so drop them here
  under purge_rules; 35-13 re-checks.
- Quick proof per task: AST same-code plus an import smoke test. The plan's last task runs
  `pytest companion`.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge companion/app.py</name>
  <files>companion/app.py</files>
  <read_first>
    - `CLI check --paths companion/app.py`, then the file bottom-up
  </read_first>
  <action>
Apply purge_rules. The module docstring is `--help` text: trim it to accurate usage text and
record the old and new versions in the SUMMARY. Keep a one-line why for:
- every auth, session, origin/CSRF and rate-limit check;
- security headers;
- the reasons for the request-size limits.
A multi-line docstring over a one-line function becomes one line. Record the ratio before and
after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" --allow companion/app.py companion/app.py && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/app.py && server/.venv/bin/python -m companion.app --help | head -1 && server/.venv/bin/ruff check companion/app.py</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with `--allow companion/app.py`. A second run of same-code without --allow fails only on the module docstring, or passes; the SUMMARY records which
    - `CLI check --paths companion/app.py` reports 0 hits
    - `python -m companion.app --help` prints usage
  </acceptance_criteria>
  <done>app.py is purged, and its --help text is accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Purge auth.py and the small modules, then run the companion suite</name>
  <files>companion/auth.py, companion/__init__.py, companion/battery.py, companion/frame_state.py, companion/i18n.py, companion/prefs.py, companion/screens.py, companion/wake.py, companion/illustration_normalize.py, companion/contrast_check.py, companion/theme_preview.py</files>
  <read_first>
    - `CLI check --paths` output for these files, then each file
  </read_first>
  <action>
Apply purge_rules.
- `auth.py`: keep the why of password hashing parameters, constant-time comparison, cookie flags
  and session expiry. Drop the display-mode-cookie history (lines ~63-70).
- `contrast_check.py`: keep the WCAG formula pointer.
- `battery.py`: keep the voltage/percent units.
Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="companion/auth.py companion/__init__.py companion/battery.py companion/frame_state.py companion/i18n.py companion/prefs.py companion/screens.py companion/wake.py companion/illustration_normalize.py companion/contrast_check.py companion/theme_preview.py" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest companion -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for the eleven files with no --allow, and `CLI check --paths` reports 0 hits
    - `pytest companion` passes
    - `git diff "$BASE" -- companion/ | grep -E '^-def (health_severity|anomaly_active|usable_pairs|label_grid)'` is empty
  </acceptance_criteria>
  <done>app.py, auth.py and the small modules are purged, and no dead code has been deleted yet.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Information loss | the why of an auth, CSRF or throttle check is dropped, so a later refactor removes the check | mitigate | Task 1/2 name these checks; the SUMMARY lists the kept security comments |
| T-35-05 | Tampering | an accidental code edit | mitigate | AST same-code; app.py is allowed for its module docstring only (verified by the second run) |
</threat_model>

<verification>
`pytest companion -q -n auto` is green, and same-code and check pass.
</verification>

<success_criteria>
The twelve files have 0 hits and their code is unchanged. The SUMMARY records the ratios and
justifies any file still above 35 %.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-08-SUMMARY.md`
</output>
