---
phase: 35-comment-purge-in-english-and-dead-code
plan: 03
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - server/plane/calendar_rules.py
  - server/plane/enrich.py
  - server/plane/detect.py
  - server/plane/manual_resolutions.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "No comment or docstring in these files carries a plan/ticket/decision/review/phase ID"
    - "The code is unchanged: same-code against the group base passes with no --allow"
    - "Detection and enrichment invariants (corroboration, runway gating, upstream API quirks) survive as why-comments"
  artifacts:
    - path: "server/plane/calendar_rules.py"
      provides: "purged comments"
  key_links:
    - from: "server/plane/detect.py comments"
      to: "COMPLIANCE.md / upstream ADS-B API behaviour"
      via: "spec pointers kept"
      pattern: "adsb"
---

<objective>
Purge the comments and docstrings of `server/plane` part B (HYG-01). Code is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md

<conventions>
The CLI, group branch (`claude/phase-35-g2-server`), BASE and purge_rules are exactly those in
35-02-PLAN.md `<context>`. Read that block once and apply it as written.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge calendar_rules.py</name>
  <files>server/plane/calendar_rules.py</files>
  <read_first>
    - `CLI check --paths server/plane/calendar_rules.py`, then the file bottom-up
  </read_first>
  <action>
Apply purge_rules. The rule docstrings often restate the calendar semantics at length: keep one
summary line, plus the rule's boundary behaviour (inclusive/exclusive dates, time zone) when it
is not obvious from the code. Record the ratio before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" server/plane/calendar_rules.py && server/.venv/bin/python scripts/check_comment_history.py check --paths server/plane/calendar_rules.py && server/.venv/bin/python -m pytest server/test_calendar_rules.py -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - `CLI same-code --base "$BASE" server/plane/calendar_rules.py` exits 0 with no --allow
    - `CLI check --paths server/plane/calendar_rules.py` reports 0 hits
    - `server/test_calendar_rules.py` passes and ruff is clean on the file
  </acceptance_criteria>
  <done>calendar_rules.py is purged, with its code unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge enrich.py, detect.py, manual_resolutions.py</name>
  <files>server/plane/enrich.py, server/plane/detect.py, server/plane/manual_resolutions.py</files>
  <read_first>
    - `CLI check --paths` output for the three files, then each file
  </read_first>
  <action>
Apply purge_rules. Keep the why behind corroboration between the two ADS-B sources, the
callsign→route lookup caching, and any upstream API quirk, each as one or two sentences with the
upstream pointer. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" server/plane/enrich.py server/plane/detect.py server/plane/manual_resolutions.py && server/.venv/bin/python scripts/check_comment_history.py check --paths server/plane/enrich.py server/plane/detect.py server/plane/manual_resolutions.py && server/.venv/bin/python -m pytest server/test_enrich.py server/test_plane_detection.py server/test_manual_resolutions.py -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for the three files with no --allow
    - `CLI check --paths` reports 0 hits for the three files
    - The three test modules pass and ruff is clean
  </acceptance_criteria>
  <done>Part B of server/plane is purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | an accidental code edit | mitigate | AST same-code against BASE, plus the module tests |
| T-35-06 | Information loss | an input-validation why is dropped (for example, why an upstream field is untrusted) | mitigate | purge_rules: rewrite security invariants, never drop them |
</threat_model>

<verification>
Both tasks' commands pass, and `pytest server -q -n auto` is green.
</verification>

<success_criteria>
The four files have 0 hits and same-code passes. The SUMMARY records the ratios and justifies any
file still above 35 %.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-03-SUMMARY.md`
</output>
