---
phase: 35-comment-purge-in-english-and-dead-code
plan: 05
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - server/test_calendar_rules.py
  - server/test_colour_rules.py
  - server/test_config_history.py
  - server/test_dither.py
  - server/test_enrich.py
  - server/test_illustrations.py
  - server/test_manual_resolutions.py
  - server/test_notify.py
  - server/test_panel_preview.py
  - server/test_pipeline_e2e.py
  - server/test_plane_detection.py
  - server/test_poll_loop.py
  - server/test_render.py
  - server/test_runway_config.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "No comment or docstring in server tests carries a history ID"
    - "Test function names and assertion-message strings are unchanged"
    - "The server test count is unchanged, and same-code passes"
  artifacts:
    - path: "server/test_poll_loop.py"
      provides: "purged test comments"
  key_links:
    - from: "test node ids"
      to: ".planning/phases/32-*/32-MIGRATION-LEDGER.md"
      via: "unchanged test names"
      pattern: "def test_"
---

<objective>
Purge the comments and docstrings of the server tests (HYG-01, tests in scope per the developer
decision). Test names and assertion strings are untouched. `server/fixtures/` has only data
files (json, ics) and one md, so it is out of scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-01-SUMMARY.md

<conventions>
- The CLI, group branch (`claude/phase-35-g2-server`), BASE and purge_rules are as in
  35-02-PLAN.md `<context>`.
- Test docstrings: one line saying what behaviour is checked. Drop the "ported from…" or
  "ledger row…" narration.
- If 35-01-SUMMARY lists a class (b) source-read hit in a server test (a test reading a comment),
  fix that test first so it asserts behaviour, and note the fix in the SUMMARY.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge the large server test files</name>
  <files>server/test_poll_loop.py, server/test_render.py, server/test_calendar_rules.py, server/test_config_history.py</files>
  <read_first>
    - `CLI check --paths` output for the four files
  </read_first>
  <action>
Before editing, record `pytest --collect-only -q server | tail -1`. Apply purge_rules and the
test-docstring convention. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="server/test_poll_loop.py server/test_render.py server/test_calendar_rules.py server/test_config_history.py" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest $F -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow; since names and asserts are code, AST equality proves they are unchanged
    - `CLI check --paths` reports 0 hits for the four files
    - The four modules pass
  </acceptance_criteria>
  <done>The large server tests are purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the remaining server test files</name>
  <files>server/test_colour_rules.py, server/test_dither.py, server/test_enrich.py, server/test_illustrations.py, server/test_manual_resolutions.py, server/test_notify.py, server/test_panel_preview.py, server/test_pipeline_e2e.py, server/test_plane_detection.py, server/test_runway_config.py</files>
  <read_first>
    - `CLI check --paths` output for the ten files
  </read_first>
  <action>
Apply purge_rules and the test-docstring convention. If `git ls-files 'server/test_*.py'` lists a
file that is in neither task (added on main since planning), purge it here too and name it in the
SUMMARY. Then confirm that the collected count equals the Task 1 figure.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files 'server/test_*.py' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest server -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for every `server/test_*.py` with no --allow
    - `CLI check --paths` reports 0 hits for every `server/test_*.py`
    - `pytest server` passes, with the collected count equal to the Task 1 figure
  </acceptance_criteria>
  <done>Every server test file is purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | a test weakened by an edit that looked like a comment edit | mitigate | AST same-code (asserts are code) and an unchanged collected count |
</threat_model>

<verification>
`pytest server -q -n auto` is green, with the count unchanged.
</verification>

<success_criteria>
Every server test has 0 hits and same-code passes. The SUMMARY records the per-file ratios.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-05-SUMMARY.md`
</output>
