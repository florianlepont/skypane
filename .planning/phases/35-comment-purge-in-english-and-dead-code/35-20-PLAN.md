---
phase: 35-comment-purge-in-english-and-dead-code
plan: 20
type: execute
wave: 11
depends_on: [35-19]
files_modified:
  - deploy/.gitignore
  - deploy/Caddyfile
  - deploy/activate.sh
  - deploy/deploy.sh
  - deploy/harden_sshd.sh
  - deploy/provision.sh
  - deploy/render_caddyfile.sh
  - deploy/skypane-backup.service
  - deploy/skypane-backup.timer
  - deploy/skypane-byos.service
  - deploy/skypane-companion.service
  - deploy/skypane-poll.service
  - deploy/skypane-poll.timer
  - deploy/skypane.env.example
  - deploy/backup/backup_gate.py
  - deploy/backup/skypane_backup.py
  - deploy/backup/install-backup-key.sh
  - deploy/backup/mac/install-launchagent.sh
  - deploy/backup/mac/skypane-backup-pull.sh
  - deploy/backup/mac/skypane-backup-pull.plist.template
  - deploy/tests/conftest.py
  - deploy/tests/test_activate.py
  - deploy/tests/test_backup.py
  - deploy/tests/test_backup_gate.py
  - deploy/tests/test_caddyfile.py
  - deploy/tests/test_ci_secrets.py
  - deploy/tests/test_deploy.py
  - deploy/tests/test_docs.py
  - deploy/tests/test_install_backup_key.py
  - deploy/tests/test_mac_pull.py
  - deploy/tests/test_provision.py
  - deploy/tests/test_units.py
  - scripts/check-attribution.sh
  - scripts/lock-deps.sh
  - scripts/run-all-tests.sh
  - scripts/run-local-verify.sh
  - .github/dependabot.yml
  - .github/workflows/ci.yml
  - .github/workflows/firmware.yml
  - pyproject.toml
  - conftest.py
  - .gitignore
  - adsb-test/.gitignore
  - adsb-test/analyze_samples.py
  - adsb-test/query_aggregator.py
  - adsb-test/sample_window.py
  - hardware/logtools.py
  - scripts/comment-history-pending.txt
  - .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md
autonomous: true
requirements: [HYG-03, HYG-01]

must_haves:
  truths:
    - "Every tracked code/config file under deploy/, scripts/ (except the guard tool and its pending list), .github/, adsb-test/ and hardware/, plus pyproject.toml, conftest.py and .gitignore, has 0 hits and is out of the pending list"
    - "Non-comment lines of every unit, Caddyfile, env example, shell, YAML and TOML file are identical (same-code); only the two backup modules may differ, and only in their argparse __doc__"
    - "systemd hardening directives and Caddy security headers keep a one-line why"
    - "The deploy tests, shellcheck, the systemd-analyze score and the full suite are green"
  artifacts:
    - path: "deploy/skypane-companion.service"
      provides: "purged comments, identical directives"
  key_links:
    - from: "deploy/tests/test_units.py"
      to: "deploy/*.service"
      via: "the tests parse directives, not comments"
      pattern: "service"
---

<objective>
Purge the comments of deploy/, scripts/, .github/, the root config, adsb-test/ and the hardware
scripts, and close group 8 (HYG-03, HYG-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-06-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-01-SUMMARY.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. close_procedure is as in 35-06-PLAN.md, with
  GROUP_PATHS = `deploy/ scripts/ .github/ adsb-test/ hardware/logtools.py pyproject.toml
  conftest.py .gitignore`, group number 8, and
  ALLOW = `--allow deploy/backup/backup_gate.py --allow deploy/backup/skypane_backup.py`
  (argparse `__doc__` only).
- Do not touch `scripts/check_comment_history.py` (35-01/35-22) or `hardware/fixtures/**` and
  `hardware/logs/**` (data).
- Branch `claude/phase-35-g8-deploy-root`.
- Specifics:
  - `# shellcheck` directives and shebangs are verbatim;
  - systemd units: keep one line of why per hardening directive group;
  - `ci.yml`: keep the why of the paths filter, the concurrency groups, `--require-hashes`,
    the deploy guard and the reviewer gate. Drop the plan references;
  - `pyproject.toml`: keep the `fail_under` derivation as one or two lines (method and figure,
    no plan ids);
  - `.plist.template` XML comments follow the same rules.
- If 35-01-SUMMARY lists a class (b) source-read hit in deploy/tests, fix that test first.
- `shellcheck` may be missing locally, in which case the CI lint job is the gate. Do not edit
  `ci.yml` steps.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge deploy production files</name>
  <files>deploy/.gitignore, deploy/Caddyfile, deploy/activate.sh, deploy/deploy.sh, deploy/harden_sshd.sh, deploy/provision.sh, deploy/render_caddyfile.sh, deploy/skypane-backup.service, deploy/skypane-backup.timer, deploy/skypane-byos.service, deploy/skypane-companion.service, deploy/skypane-poll.service, deploy/skypane-poll.timer, deploy/skypane.env.example, deploy/backup/backup_gate.py, deploy/backup/skypane_backup.py, deploy/backup/install-backup-key.sh, deploy/backup/mac/install-launchagent.sh, deploy/backup/mac/skypane-backup-pull.sh, deploy/backup/mac/skypane-backup-pull.plist.template</files>
  <read_first>
    - `CLI check --paths $(git ls-files deploy | grep -v '^deploy/tests/' | grep -v '\.md$')`
  </read_first>
  <action>
Apply purge_rules and the specifics. The two backup modules' module docstrings stay accurate
`--help` text; record the old and new first lines. Record the ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files deploy | grep -vE '^deploy/tests/|\.md$' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" --allow deploy/backup/backup_gate.py --allow deploy/backup/skypane_backup.py $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python deploy/backup/backup_gate.py --help >/dev/null && server/.venv/bin/python deploy/backup/skypane_backup.py --help >/dev/null && server/.venv/bin/python -m pytest deploy -q -n auto</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0. The only allows are the two backup modules, and the SUMMARY states whether their docstring changed
    - `CLI check --paths` reports 0 hits for the deploy production files
    - Both `--help` commands exit 0, and `pytest deploy` passes
    - If `systemd-analyze` is present, `systemd-analyze security --offline=true --threshold=20` passes for each deploy/*.service
  </acceptance_criteria>
  <done>deploy production is purged, with identical directives.</done>
</task>

<task type="auto">
  <name>Task 2: Purge deploy tests, scripts, .github, root config, adsb-test and hardware/logtools.py</name>
  <files>deploy/tests/conftest.py, deploy/tests/test_activate.py, deploy/tests/test_backup.py, deploy/tests/test_backup_gate.py, deploy/tests/test_caddyfile.py, deploy/tests/test_ci_secrets.py, deploy/tests/test_deploy.py, deploy/tests/test_docs.py, deploy/tests/test_install_backup_key.py, deploy/tests/test_mac_pull.py, deploy/tests/test_provision.py, deploy/tests/test_units.py, scripts/check-attribution.sh, scripts/lock-deps.sh, scripts/run-all-tests.sh, scripts/run-local-verify.sh, .github/dependabot.yml, .github/workflows/ci.yml, .github/workflows/firmware.yml, pyproject.toml, conftest.py, .gitignore, adsb-test/.gitignore, adsb-test/analyze_samples.py, adsb-test/query_aggregator.py, adsb-test/sample_window.py, hardware/logtools.py</files>
  <read_first>
    - `CLI check --paths` output for these files
  </read_first>
  <action>
Apply purge_rules and the specifics. Test names and assertion strings stay unchanged. Record the
ratios before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && F="$(git ls-files deploy/tests scripts .github adsb-test hardware/logtools.py pyproject.toml conftest.py .gitignore | grep -vE '\.md$|\.json$|check_comment_history\.py$|comment-history-pending\.txt$' | tr '\n' ' ')" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" $F && server/.venv/bin/python scripts/check_comment_history.py check --paths $F && server/.venv/bin/python -m pytest deploy test-support -q -n auto && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, so the YAML/TOML/shell non-comment lines are identical and CI behaviour is unchanged
    - `CLI check --paths` reports 0 hits
    - `pytest deploy test-support` and ruff are green
  </acceptance_criteria>
  <done>The rest of group 8 is purged.</done>
</task>

<task type="auto">
  <name>Task 3: Close group 8</name>
  <files>scripts/comment-history-pending.txt, .planning/phases/35-comment-purge-in-english-and-dead-code/35-COMMENT-RATIO.md</files>
  <read_first>
    - scripts/comment-history-pending.txt
  </read_first>
  <action>
Run close_procedure steps 1-7 for group 8. After this plan, only firmware/ paths should remain in
the pending list. List any other remaining path in the SUMMARY and explain it.
  </action>
  <verify>
    <automated>test -z "$(grep -v '^#' scripts/comment-history-pending.txt | grep -v '^firmware/')" && server/.venv/bin/python scripts/check_comment_history.py check && ./scripts/run-all-tests.sh && server/.venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - The pending list holds firmware/ paths only, and `CLI check` exits 0
    - 35-COMMENT-RATIO.md has a group-8 section
    - The full suite and ruff are green after merging main; the PR's CI lint job (shellcheck, systemd-analyze) is green; a draft PR is open
  </acceptance_criteria>
  <done>Group 8 is closed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Elevation of privilege | a systemd hardening directive dropped along with its comment | mitigate | non-comment-line same-code, the systemd-analyze threshold and test_units |
| T-35-13 | Tampering | a CI step or deploy guard altered | mitigate | same-code on ci.yml non-comment lines; the PR CI run |
| T-35-06 | Information loss | the why of a security header or hardening directive is dropped | mitigate | the specifics require a one-line why per group |
</threat_model>

<verification>
All three tasks' commands pass, and the PR CI is green.
</verification>

<success_criteria>
Group 8 is purged and CI-enforced, and only firmware remains pending.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-20-SUMMARY.md`
</output>
