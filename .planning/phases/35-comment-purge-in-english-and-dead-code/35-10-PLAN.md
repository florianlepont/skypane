---
phase: 35-comment-purge-in-english-and-dead-code
plan: 10
type: execute
wave: 5
depends_on: [35-07]
files_modified:
  - companion/pages/health_page.py
  - companion/pages/history_page.py
autonomous: true
requirements: [HYG-01]

must_haves:
  truths:
    - "health_page.py and history_page.py have 0 guard hits"
    - "same-code passes with no --allow; health_severity and anomaly_active still exist (deleted in 35-13)"
    - "Severity thresholds and their units keep their why"
  artifacts:
    - path: "companion/pages/health_page.py"
      provides: "purged comments (was ~68 % comments)"
  key_links:
    - from: "health_page.py thresholds"
      to: "health_state severity consumed by app.py"
      via: "comment states the threshold's unit and source"
      pattern: "severity"
---

<objective>
Purge the comments and docstrings of the health and history pages (HYG-01). Dead functions stay
for 35-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-CONTEXT.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-02-PLAN.md
@.planning/phases/35-comment-purge-in-english-and-dead-code/35-08-PLAN.md

<conventions>
- The CLI and purge_rules are as in 35-02-PLAN.md. The group branch, BASE and the comments-only
  rule are as in 35-08-PLAN.md.
- health_page.py (~3 200 comment lines) is split in two at a top-level def near the middle
  (`grep -n '^def \|^class '`). Do the lower half first.
- The `health_severity` and `anomaly_active` docstrings may be shortened, but the functions are
  not deleted.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge the lower half of health_page.py</name>
  <files>companion/pages/health_page.py</files>
  <read_first>
    - `CLI check --paths companion/pages/health_page.py`, filtered to the lower half
  </read_first>
  <action>
Apply purge_rules. Keep each threshold's unit and the why of its value in one sentence. Commit.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/pages/health_page.py && server/.venv/bin/python -c "import companion.pages.health_page"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow
    - `CLI check --paths` shows no hit in the lower half
  </acceptance_criteria>
  <done>The lower half is purged.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the upper half of health_page.py</name>
  <files>companion/pages/health_page.py</files>
  <read_first>
    - `CLI check --paths companion/pages/health_page.py`
  </read_first>
  <action>
Apply purge_rules to the rest of the file, including the module docstring. Record the ratio before
and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/pages/health_page.py && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/pages/health_page.py && git grep -qw "def health_severity" companion/pages/health_page.py</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 with no --allow, and `CLI check --paths` reports 0 hits
    - `health_severity` and `anomaly_active` are still defined
  </acceptance_criteria>
  <done>health_page.py is purged.</done>
</task>

<task type="auto">
  <name>Task 3: Purge history_page.py, then run the status tests</name>
  <files>companion/pages/history_page.py</files>
  <read_first>
    - `CLI check --paths companion/pages/history_page.py`, then the file bottom-up
  </read_first>
  <action>
Apply purge_rules. Keep the time-zone, retention and aggregation-unit facts. Record the ratio
before and after.
  </action>
  <verify>
    <automated>B="$(git merge-base HEAD origin/main)" && server/.venv/bin/python scripts/check_comment_history.py same-code --base "$B" companion/pages/health_page.py companion/pages/history_page.py && server/.venv/bin/python scripts/check_comment_history.py check --paths companion/pages/health_page.py companion/pages/history_page.py && server/.venv/bin/python -m pytest companion -q -n auto -k "status or health or history"</automated>
  </verify>
  <acceptance_criteria>
    - same-code exits 0 for both files with no --allow, and there are 0 hits
    - The status/health/history tests pass
  </acceptance_criteria>
  <done>Both pages are purged.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | an accidental code edit | mitigate | AST same-code after every task |
| T-35-06 | Information loss | threshold rationale is lost | mitigate | Task 1 keeps the unit and the why per threshold |
</threat_model>

<verification>
same-code, check and the tests pass.
</verification>

<success_criteria>
Both files have 0 hits and their code is unchanged. The ratios are recorded.
</success_criteria>

<output>
Create `.planning/phases/35-comment-purge-in-english-and-dead-code/35-10-SUMMARY.md`
</output>
