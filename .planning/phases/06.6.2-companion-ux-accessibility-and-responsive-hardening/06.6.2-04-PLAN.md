---
phase: 06.6.2
plan: 04
type: execute
wave: 3
depends_on: ["06.6.2-03"]
files_modified:
  - companion/layout.py
  - companion/static/style.css
  - companion/pages/config_page.py
  - companion/pages/health_page.py
  - companion/pages/history_page.py
  - companion/pages/airlines_page.py
  - companion/pages/preview_page.py
  - companion/test_companion_app.py
  - companion/test_config_page.py
  - companion/test_status_pages.py
  - companion/test_view_pages.py
autonomous: true
requirements: [D-15, D-16]

must_haves:
  truths:
    - "A distinct page-title role exists, visually larger than the existing 20px section-heading role, and every one of the five authenticated pages' top-level heading uses it (D-15)."
    - "All five authenticated pages open with the same shared layout.page_header() call instead of five independent bare-<h1> patterns (D-16)."
    - "layout.page_header()'s signature accepts an optional one-sentence purpose, an optional already-safe freshness fragment, and an optional already-safe single contextual action, so Phase 06.6.3's per-page recomposition can pass those without changing this function's shape."
  artifacts:
    - companion/layout.py
    - companion/static/style.css
  key_links:
    - "Phase 06.6.3's five per-page redesign plans call layout.page_header(title, purpose=None, freshness_html=None, action_html=None) by this exact name and signature — do not rename or reorder its parameters after this plan ships."
---

<objective>
Ship the one shared page-header component (D-16) every authenticated page adopts for its title, and the distinct page-title typography role (D-15) that separates a page's own identity from its 20px section headings — replacing today's inconsistent "bare `<h1>` on some pages, second heading immediately below on others" pattern with one component all five pages call the same way.

Purpose: this is the shared primitive Phase 06.6.3's per-page redesign plans depend on by name — its signature must be settled and stable before that phase's planner reads it as a literal contract. This plan applies it minimally (title only) across all five pages; 06.6.3 is the phase that adds each page's purpose sentence, freshness indicator and contextual action, since those are per-page content decisions this phase's CONTEXT.md explicitly moved out of scope.
Output: `layout.page_header()`, a new `.page-title` CSS role, and all five page modules' `render()` calling it in place of their bare `<h1>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.2-companion-ux-accessibility-and-responsive-hardening/06.6.2-CONTEXT.md
@.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UX-AUDIT.md
</context>

## Artifacts this plan produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `layout.page_header(title, purpose=None, freshness_html=None, action_html=None)` | new function | returns escaped/composed header markup; `purpose` is escaped here, `freshness_html`/`action_html` are already-safe markup passed through verbatim (same contract as `stat_tile()`'s `content_html`) |
| `.page-title` | new CSS class | 28-32px serif page-title role, distinct from the existing 20px `.text-heading` |
| `PAGE_HEADER_CLASS` | new constant | the wrapping `<header>`/`<div>` element's class, for test assertions |

<tasks>

<task type="auto" id="06.6.2-04-01">
  <name>Task 1: layout.page_header() component + .page-title typography role</name>

  <files>companion/layout.py, companion/static/style.css</files>

  <read_first>
    - `companion/layout.py` lines 615-666 (`stat_tile()`/`empty_state()` — the exact "escape the caption, pass already-built content through verbatim" contract `page_header()` must follow) and lines 218-230 (`escape_html()`).
    - `companion/static/style.css` lines 156-201 (the typography-role rules: `.text-label`/`.text-body`/`.text-heading`, the `h1,h2,h3,.text-heading` serif rule, `.site-title`) and lines 37-61 (`--font-heading-size`/`--font-display-size` — the two existing size tokens to sit between).
    - `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UX-AUDIT.md` lines 236-258 (Typography hierarchy table — the suggested 30-34px page-title size) and lines 292-308 (Page-header pattern — title/purpose/freshness/action, the exact per-page purpose-sentence examples for 06.6.3's later reference).
    - `.planning/phases/06.6.2-companion-ux-accessibility-and-responsive-hardening/06.6.2-CONTEXT.md` decisions D-15 and D-16 in full.
  </read_first>

  <action>
1. In `companion/static/style.css`, add a new `--font-page-title-size: 30px;` custom property to the `:root` block's typography scale, placed between `--font-heading-size: 20px;` and `--font-display-size: 24px;` in the file (value ordering, not necessarily numeric — 30px is larger than both existing tokens, name it clearly as the page-title role). Add a new rule `.page-title { font-family: var(--font-serif); font-size: var(--font-page-title-size); font-weight: var(--weight-regular); line-height: 1.2; letter-spacing: -0.01em; margin: 0 0 var(--space-xs); }` placed immediately after the existing `h1, h2, h3, .text-heading { ... }` rule. Add `.page-header__purpose { margin: 0 0 var(--space-lg); }` (reuses `.text-body` sizing via the markup, not a duplicate font-size rule) and `.page-header { margin-bottom: var(--space-lg); }` for the wrapping element's spacing.

2. In `companion/layout.py`, add a new function `page_header(title, purpose=None, freshness_html=None, action_html=None)` placed after `empty_state()`. It must: escape `title` and wrap it in `<h1 class="page-title">`; when `purpose` is truthy, escape it and add `<p class="page-header__purpose text-body">...</p>`; when `freshness_html` is truthy, interpolate it verbatim (already-safe markup, same "no re-escaping" contract `stat_tile()`'s `content_html` uses — document this explicitly in the docstring, since it is the one place in this function that deviates from "escape everything"); when `action_html` is truthy, interpolate it verbatim too. Wrap the whole thing in `<div class="page-header">...</div>`. The docstring must state this function's signature is the literal contract Phase 06.6.3's per-page plans consume by name — do not rename or reorder parameters after this plan ships, and every pre-existing call site (added by Task 2/3 of this plan, title-only) must remain byte-compatible with later calls that also pass `purpose`/`freshness_html`/`action_html`.
  </action>

  <acceptance_criteria>
    - Source: `companion/layout.py` defines `def page_header(title, purpose=None, freshness_html=None, action_html=None):`.
    - Source: `page_header("X")` (title only) returns markup containing `<div class="page-header">`, `<h1 class="page-title">X</h1>`, and no `<p class="page-header__purpose` (since `purpose` was omitted).
    - Source: `page_header("X", purpose="Y")` returns markup containing `<p class="page-header__purpose text-body">Y</p>`.
    - Source: `page_header("<b>", freshness_html="<span>raw</span>")` HTML-escapes the title (`&lt;b&gt;`) but interpolates `freshness_html` verbatim (`<span>raw</span>` unescaped) — proving the two parameters have different escaping contracts, as documented.
    - Source: `style.css` defines `.page-title` at `var(--font-page-title-size)` and `--font-page-title-size: 30px;` in `:root`.
    - Behavior: `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys
sys.path.insert(0, '.')
from companion.layout import page_header
out = page_header('X')
assert '<div class=\"page-header\">' in out and '<h1 class=\"page-title\">X</h1>' in out
assert 'page-header__purpose' not in out
out2 = page_header('X', purpose='Y')
assert '<p class=\"page-header__purpose text-body\">Y</p>' in out2
out3 = page_header('<b>', freshness_html='<span>raw</span>')
assert '&lt;b&gt;' in out3
assert '<span>raw</span>' in out3
print('ok')
" && server/.venv/bin/python3 -c "
import re
src = open('companion/static/style.css').read()
assert '--font-page-title-size: 30px;' in src
m = re.search(r'^\.page-title\s*\{([^}]*)\}', src, re.M)
assert m and 'var(--font-page-title-size)' in m.group(1) and 'var(--font-serif)' in m.group(1)
print('ok')
" && scripts/run-all-tests.sh</automated>
  </verify>

  <done>layout.page_header() exists with the exact four-parameter signature Phase 06.6.3 will consume; .page-title renders larger and distinctly from the existing 20px section-heading role.</done>
</task>

<task type="auto" id="06.6.2-04-02">
  <name>Task 2: Apply page_header() to Config and Health</name>

  <files>companion/pages/config_page.py, companion/pages/health_page.py, companion/test_config_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/config_page.py` `render()` (its current `'<h1 class="text-heading">Config</h1>'` opening literal).
    - `companion/pages/health_page.py` `render()` (its current `'<h1 class="text-heading">Health</h1>'` opening literal — note it is followed immediately by `_source_fault_block()`/`banner_html`/a second `<h2>Overview</h2>` heading; do not touch anything after the opening `<h1>` literal).
  </read_first>

  <action>
In `config_page.py`'s `render()`: replace the leading `'<h1 class="text-heading">Config</h1>'` string with `layout.page_header("Config")` (title-only call — no purpose/freshness/action in this plan; 06.6.3 owns adding those). `config_page.py` must import `layout` if it does not already (check the existing `from companion.layout import escape_html` import line and add `import companion.layout as layout` alongside it, matching `history_page.py`'s existing dual-import convention).

In `health_page.py`'s `render()`: replace the leading `'<h1 class="text-heading">Health</h1>'` string with `layout.page_header("Health")` (this module already does `import companion.layout as layout`).

In `test_config_page.py` and `test_status_pages.py`: update or add a check asserting each page's rendered output contains `<h1 class="page-title">Config</h1>` / `<h1 class="page-title">Health</h1>` respectively, and no longer contains `<h1 class="text-heading">`. Bump each file's `EXPECTED_CHECK_COUNT` from its real current on-disk value.
  </action>

  <acceptance_criteria>
    - Source: `config_page.py`'s `render()` output starts with `layout.page_header("Config")`'s markup, not a literal `<h1 class="text-heading">`.
    - Source: `health_page.py`'s `render()` output starts with `layout.page_header("Health")`'s markup, not a literal `<h1 class="text-heading">`.
    - Behavior: `server/.venv/bin/python3 companion/test_config_page.py` exits 0; `server/.venv/bin/python3 companion/test_status_pages.py` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>

  <done>Config and Health both open with the shared page_header() component instead of an independent bare-H1 literal.</done>
</task>

<task type="auto" id="06.6.2-04-03">
  <name>Task 3: Apply page_header() to History, Airlines and Preview</name>

  <files>companion/pages/history_page.py, companion/pages/airlines_page.py, companion/pages/preview_page.py, companion/test_view_pages.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/history_page.py` `render()` (its current `'<h1 class="text-heading">History</h1>'` opening literal — this module already does `import companion.layout as layout`).
    - `companion/pages/airlines_page.py` `render()` (locate its current opening `<h1>` literal; confirm its exact class/text before editing).
    - `companion/pages/preview_page.py` `render()` (locate its current opening `<h1>` literal; confirm its exact class/text before editing).
  </read_first>

  <action>
In `history_page.py`'s `render()`: replace the leading `'<h1 class="text-heading">History</h1>'` string with `layout.page_header("History")`.

In `airlines_page.py`'s `render()`: replace its opening `<h1 class="text-heading">Airlines</h1>` with `layout.page_header("Airlines")` — this module already does `import companion.layout as layout` (line 23), no import change needed.

In `preview_page.py`'s `render()`: replace its opening `<h1 class="text-heading">Preview</h1>` with `layout.page_header("Preview")`. This module currently only does `from companion.layout import absolute_and_relative, empty_state, escape_html` (line 20) — add `import companion.layout as layout` as a new line immediately after it, matching `history_page.py`'s existing dual-import convention (`from companion.layout import escape_html` plus `import companion.layout as layout`).

In `test_view_pages.py`: update/add checks asserting History's and Preview's rendered output each contain `<h1 class="page-title">` with the correct title text and no longer a `<h1 class="text-heading">`. In `test_status_pages.py` (which also covers Airlines): add the equivalent check for Airlines. Bump `EXPECTED_CHECK_COUNT` in each touched file from its real current on-disk value.
  </action>

  <acceptance_criteria>
    - Source: `history_page.py`, `airlines_page.py`, `preview_page.py` each open `render()`'s return with `layout.page_header(...)`, not a literal `<h1 class="text-heading">`.
    - Behavior: `server/.venv/bin/python3 companion/test_view_pages.py` exits 0; `server/.venv/bin/python3 companion/test_status_pages.py` exits 0.
    - Behavior: `scripts/run-all-tests.sh` exits 0 (full-suite gate for this plan).
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_status_pages.py && scripts/run-all-tests.sh</automated>
  </verify>

  <done>All five authenticated pages (Config, Health, History, Airlines, Preview) now open with the same shared layout.page_header() call instead of five independent bare-H1 literals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page module → `layout.page_header()` | Internal, server-side only — `title`/`purpose` are always fixed string literals in this plan (page names), never user-influenced input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.2-08 | Tampering (XSS, forward-looking) | `page_header()`'s `freshness_html`/`action_html` parameters are documented as "interpolated verbatim, not escaped" — a future caller could pass unescaped user-influenced data through them | medium | mitigate | The docstring added in Task 1 explicitly states these two parameters carry the same "already-safe, caller-escaped markup" contract as `stat_tile()`'s `content_html`, and names that contract by its existing precedent so a future 06.6.3 call site follows the established discipline rather than inventing a new one. No call site in this plan passes anything but a static title string. |
| T-06.6.2-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero packages installed. |
</threat_model>

<verification>
1. `scripts/run-all-tests.sh` exits 0.
2. `git diff --stat` touches exactly the eleven files in this plan's `files_modified`.
3. `grep -rn '<h1 class="text-heading">' companion/pages/` returns no matches — every page's top-level heading now goes through `page_header()`.
</verification>

<success_criteria>
- D-15: a distinct ~30px serif page-title role exists, separate from the 20px section-heading role, used by every page's top-level heading.
- D-16: every authenticated page opens with the same shared `layout.page_header()` component instead of five independent patterns.
</success_criteria>

<output>
Create `.planning/phases/06.6.2-companion-ux-accessibility-and-responsive-hardening/06.6.2-04-SUMMARY.md` when done
</output>
