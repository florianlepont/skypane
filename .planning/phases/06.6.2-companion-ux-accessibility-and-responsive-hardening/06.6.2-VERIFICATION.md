---
phase: 06.6.2-companion-ux-accessibility-and-responsive-hardening
verified: 2026-08-31T11:24:33Z
status: passed
score: 17/17 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 06.6.2: Companion UX, accessibility, and responsive hardening — Verification Report

**Phase Goal:** Ship the shared UX/accessibility/design-token foundation the rest of the companion redesign builds on: color/surface/typography/motion tokens, a WCAG contrast checker, the joint mobile-nav closed-state + no-JS-fallback fix, desktop table containment, a shared page-header pattern and refined sidebar (incl. sticky), warning/error severity propagation, a dedicated login shell with deep-link return and logout→POST, poll-trigger concurrency locking, and programmatic feedback roles.

**Verified:** 2026-08-31T11:24:33Z
**Status:** passed
**Re-verification:** No — initial verification

## Method

This is an unmapped backlog phase (no formal REQUIREMENTS.md REQ-IDs). Must-haves were derived from `06.6.2-CONTEXT.md`'s locked decisions (D-01, D-06 accent-color half, D-11, D-14–D-17, D-19, D-21) and `06.6.1-UX-AUDIT.md`'s owned findings (UXA-01 desktop-containment half, UXA-02/03/04/07/09/10/14/15, UXA-08 accent-color half), cross-referenced against all 8 plans' `must_haves.truths`/`key_links` and the REVIEW.md/REVIEW-FIX.md code-review cycle that landed after the human checkpoint. Every claim below was independently re-derived from the current on-disk code (not read from SUMMARY.md prose) via direct file reads, `grep`, and live `server/.venv/bin/python3` execution of the actual functions.

The plan 08 human checkpoint (real local browser) is accepted as satisfying the human-verification requirement for behaviors source alone cannot prove (accessibility-tree exposure, keyboard focus order, rendered contrast, sticky sidebar, severity propagation, login deep-link, poll lock) — its verdict in `06.6.2-08-SUMMARY.md` is itemized per checklist group, quotes the developer's own words, and includes two live-investigated observations (one correctly deferred to 06.6.3's D-07, one resolved as a wrong-URL testing mistake) rather than a blanket "approved," so it is treated as genuine evidence, not a rubber stamp.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `companion/contrast_check.py` reproduces the audit's own published contrast numbers exactly | ✓ VERIFIED | Zero-import module; live-executed all 8 fixtures from plan 01 (`contrast_ratio("#E8622C","#FBF9F6")==3.22` etc.) — all pass. `companion/test_contrast_check.py` registered in `scripts/run-all-tests.sh`'s `HARNESSES`. |
| 2 | Light mode's accent/text pairs reach ≥4.5:1 (UXA-04) | ✓ VERIFIED | `--color-accent: #B13F16` (darkened from `#E8622C`) confirmed in all light token blocks; `test_contrast_check.py`'s 16 checks (8 formula + 8 live token-pair) pass. |
| 3 | Three genuinely distinct surface levels exist in both themes (D-14) | ✓ VERIFIED | `--color-canvas`/`--color-dominant`/`--color-secondary` present with distinct hex values in all 4 token blocks (`grep` confirmed 3 distinct light values `#F7F4EF`/`#FFFFFF`/`#EEE8DE` and 3 distinct dark values `#0C0F14`/`#151922`/`#1C222D`); `body` background repointed to `--color-canvas`. |
| 4 | Dark-mode cards carry a restrained visible border, not shadow alone (D-14) | ✓ VERIFIED | `.stat-tile, .page-section, .battery-trend-section { border-color: var(--color-border); }` present both inside `@media (prefers-color-scheme: dark)` and under `html[data-ui-theme="dark"]` (style.css:971-979). |
| 5 | Every native radio/checkbox renders with the terracotta accent (D-06) | ✓ VERIFIED | `accent-color: var(--color-accent);` present on the shared `input, select` rule (style.css:658). |
| 6 | 120-180ms transitions with one global reduced-motion override (D-19) | ✓ VERIFIED | `.15s ease` transitions on `a`/`button`/nav links/`input`/`select`; one global `@media (prefers-reduced-motion: reduce)` rule (style.css:160-166) sets `transition-duration: 0.01ms !important` for `*, *::before, *::after`. |
| 7 | Two concurrent `POST /poll-now` requests can never both call `run_once()`; loser gets an honest "already running" response (UXA-15) | ✓ VERIFIED | `_POLL_LOCK = threading.Lock()`; `_handle_poll_now()` acquires `blocking=False` as its first statement, wraps cooldown-check→`run_once()`→`mark_poll_triggered()` in `try/finally: _POLL_LOCK.release()`. Genuine two-thread test (`threading.Event`-synchronized) in `test_companion_app.py` passes live (80/80 suite). |
| 8 | Populated History never grows the document wider than the viewport (UXA-01 desktop half) | ✓ VERIFIED | `body { overflow-x: hidden; }` (guaranteed fix) plus `.dashboard-main { max-width: min(1440px, 100%); width: 100%; }` and `.data-table-wrap { max-width: 100%; overflow-x: auto; }` all confirmed present in style.css. |
| 9 | Closed mobile nav is absent from the accessibility tree/tab order; no-JS renders it fully visible (UXA-02/UXA-12) | ✓ VERIFIED | `.js .mobile-nav`/`.js .mobile-nav--open` scoped clipping rules (base `.mobile-nav` rule carries no `max-height`/`overflow: hidden`); `nav-dropdown.js` adds `.js` marker unconditionally as its first statement, sets `panel.hidden = true` initially, toggles `hidden` in step with the transition (`transitionend` on close, immediate on open). Confirmed live in the human checkpoint (item 2, "Pass"). |
| 10 | One shared `layout.page_header()` component backs every page's title, with a distinct ~30px page-title role (D-15/D-16) | ✓ VERIFIED | `layout.page_header(title, purpose=None, freshness_html=None, action_html=None)` exists; all five page modules (`config_page.py`, `health_page.py`, `history_page.py`, `airlines_page.py`, `preview_page.py`) call it; zero remaining `<h1 class="text-heading">` in `companion/pages/`. `.page-title` at `--font-page-title-size: 30px` confirmed distinct from the 20px `.text-heading`. |
| 11 | Sidebar: tinted-pill active state + per-label icon, sticky on long pages, theme+logout grouped in a separated footer (D-17/D-21) | ✓ VERIFIED | `ICON_IDS` grew to 10 (5 new `icon-nav-*`); `sidebar_nav("health")` emits `aria-current="page"` exactly once; `.sidebar-link--active` carries `color-mix(...)` + `border-radius`; `.dashboard-sidebar` (inside `@media (min-width: 960px)`) carries `position: sticky; top: 0;`; `.sidebar-footer`/`.mobile-nav__footer` group `_theme_form_html()` + `_logout_form_html()`. Confirmed live in checkpoint (item 4, "Pass," including a live-investigated sticky-behavior false alarm resolved as a wrong-URL mistake, not a defect). |
| 12 | `/logout` only accepts POST; GET no longer ends a session (D-11) | ✓ VERIFIED | `do_GET` contains no `LOGOUT_ROUTE` branch; `do_POST` contains exactly one. Checkpoint item 5 confirms `GET /logout` → 404, `POST /logout` → 303 + session invalidated. |
| 13 | A warning-only Health state renders warn (not error) styling on both nav dot and banner; error still promotes to error; all-clear stays silent (UXA-14) | ✓ VERIFIED | `overall_severity()` precedence live-executed: `("ok","ok","ok",False)→"ok"`, `("ok","ok","ok",True)→"warn"`, `("warn","ok","ok",False)→"warn"`, `("error","warn","ok",True)→"error"` — all match. `_health_alert_markup(severity)` looks up `dot--warn`/`dot--error` via the existing whitelist dict; `sidebar_nav(health_alert="warn")` renders `dot--warn` only, `"error"` renders `dot--error` only, `None` renders neither. WR-04's post-checkpoint fix confirmed: `page_context()` computes `health_state` once (`safe_health_state()`), reused by both the nav dot (`health_state["severity"]`) and `health_page.render()`. Checkpoint item 7 confirms live against two seeded fixtures, banner/dot never disagreed. |
| 14 | Save/poll outcomes and the anomaly banner carry the correct role (status vs alert) by real severity (UXA-07) | ✓ VERIFIED | `flash_banner(message, role="status")` validates against `{"status","alert"}`; `anomaly_banner(message, severity="error")` picks `banner--anomaly`/`role="alert"` for error, `banner--warn`/`role="status"` otherwise. `app.FLASH_ROLES` maps every `FLASH_KEY_*` to status/alert; all 6 `flash_banner(ctx["flash"], role=ctx["flash_role"])` call sites confirmed present (zero bare `flash_banner(ctx["flash"])` calls remain). |
| 15 | Login renders a dedicated nav-free shell; no protected navigation implied pre-auth (UXA-03) | ✓ VERIFIED | `layout.login_shell()` live-executed: contains `<html lang="en"`, `.login-shell`/`.login-card`, and none of `dashboard-shell`/`sidebar-nav`/`mobile-nav`/`site-nav-toggle`. Checkpoint item 6 confirms live: login page shows no sidebar/nav/theme selector. |
| 16 | Deep-link return is allowlisted; open redirect is structurally impossible | ✓ VERIFIED | `_validated_next_route()` live-executed against `/health` (passes), `https://evil.example`, `//evil.example`, `/health/../../etc/passwd`, `http://evil.example/health`, `None` — all correctly rejected except the exact-member case. Exact-membership equality only, never prefix/URL-parsed. |
| 17 | Document language is English-only and mechanically guarded (D-01/UXA-09) | ✓ VERIFIED | `<html lang="en"` hard-coded in both `page_shell()` and `login_shell()`; no French-language strings found in `companion/pages/*.py`, `layout.py`, `app.py`. |

**Score:** 17/17 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `companion/contrast_check.py` | pure-stdlib WCAG contrast utility | ✓ VERIFIED | Zero imports; 3 functions + 3 constants; live-executed correctly. |
| `companion/static/style.css` | reworked token system, containment, sidebar, banners, login CSS | ✓ VERIFIED | All rules present and correctly scoped (see truths 2-6, 8, 9, 11, 14, 15 evidence above). |
| `companion/layout.py` | `page_header()`, sidebar icons/footer/skip-link/favicon, `login_shell()`, severity-aware banner/dot | ✓ VERIFIED | All functions exist with documented signatures; live-executed. |
| `companion/app.py` | poll lock, `/logout` POST-only, `health_severity`/`flash_role` ctx wiring, `_validated_next_route()`, hardening headers, socket timeout | ✓ VERIFIED | All present and live-verified. |
| `companion/pages/health_page.py` | `overall_severity()`, `health_severity()`, `compute_health_state()`, `safe_health_state()` | ✓ VERIFIED | All present; precedence table live-executed correctly. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| Plans 02-08 | Plan 01's tokens/reduced-motion override | inherited, not redeclared | WIRED | No plan re-declares `@media (prefers-reduced-motion: reduce)`; only one instance of the global override exists (plus the pre-existing mobile-nav-specific one, now also `.js`-scoped). |
| `companion/pages/*.py` render() | `layout.page_header()` | direct call, 5 sites | WIRED | Confirmed via grep — all 5 page modules call it by name. |
| `require_session()` | `_validated_next_route()` → `?next=` → login → post-auth redirect | allowlist round-trip | WIRED | `_validated_next_route()` used on both the GET query-param path and the POST form path; success redirects to `next_route or CONFIG_ROUTE`. |
| `page_context()` | `health_page.safe_health_state()` (WR-04) | single computation, 2 consumers | WIRED | `ctx["health_severity"]` and `health_page.render()`'s internal computation both source from the same `ctx["health_state"]`/`safe_health_state()` call, closing the WR-04 duplicate-DB-read finding. |
| `flash_key` | `FLASH_ROLES` → `ctx["flash_role"]` → `flash_banner(role=...)` | 6 call sites | WIRED | All 6 confirmed. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Contrast formula fidelity | live Python execution of 8 audit fixtures | all match | ✓ PASS |
| Poll-lock concurrency | `server/.venv/bin/python3 companion/test_companion_app.py` (genuine 2-thread test) | 80/80 pass | ✓ PASS |
| Severity precedence | live Python execution of `overall_severity()` fixtures | all match | ✓ PASS |
| Open-redirect rejection | live Python execution of `_validated_next_route()` against 5 adversarial inputs | all correctly rejected | ✓ PASS |
| `login_shell()` nav-free contract | live Python execution, negative-assertion set | all absent as required | ✓ PASS |
| Full test suite | `scripts/run-all-tests.sh` | 15/16 harnesses green, 91% coverage (threshold met); sole failure is the pre-existing, independently-reproduced (via `git stash` before any 06.6.2 commit) macOS-local Pillow/FreeType `panel.bin` digest mismatch in `server/test_poll_loop.py`, unrelated to this phase (zero `server/` files touched) | ✓ PASS (with documented pre-existing exception) |

### Code Review Findings — Fix Verification

06.6.2-REVIEW.md found 1 critical + 4 warning findings after plan 08's checkpoint closed. All 5 were fixed in commits landed after the checkpoint; each is independently re-verified in code here (not merely trusted from REVIEW-FIX.md's narrative):

| Finding | Fix | Verified in code |
|---------|-----|-------------------|
| CR-01 (skip-link target not focusable) | `tabindex="-1"` on `<main id="main-content">` | ✓ Confirmed: `'<main class="page-content dashboard-main" id="%s" tabindex="-1">\n'` in layout.py:767 |
| WR-01 (rAF re-open race) | `isOpen()` re-check inside the scheduled `requestAnimationFrame` callback | ✓ Confirmed present in nav-dropdown.js:99 |
| WR-02 (missing hardening headers) | `_send_hardening_headers()` called from both `send_html()`/`send_bytes()` | ✓ Confirmed: nosniff/X-Frame-Options/Referrer-Policy all present, called at app.py:345,369 |
| WR-03 (unbounded slow-body read) | `Handler.timeout = 30`, `except socket.timeout` in `read_form()` | ✓ Confirmed present at app.py:79,322,447 |
| WR-04 (duplicate Health DB reads) | `compute_health_state()`/`safe_health_state()`, single computation reused by nav dot and page render | ✓ Confirmed: `page_context()` computes once, `health_page.render()` reuses `ctx["health_state"]` when present |

This directly addresses the task's flagged concern: CR-01's fix changes behavior that the checkpoint's own item 9 ("keyboard-only pass," recorded "Pass") covered — the fix lands *after* that checkpoint recorded its verdict, so item 9's intent (skip link actually moves focus) is now more soundly satisfied than what the checkpoint itself could observe at the time, not merely left untested. No regression: full suite re-run post-fix is green (80/80 `test_companion_app.py`, including the new CR-01 regression check and the corrected `EXPECTED_CHECK_COUNT` mechanical fix in `2dfaedf`).

### Requirements Coverage

No formal REQUIREMENTS.md REQ-IDs are claimed or expected for this phase (unmapped backlog phase, same precedent as 06.5/06.6/06.6.1). Confirmed: `grep -n "06.6.2" .planning/REQUIREMENTS.md` returns zero matches — REQUIREMENTS.md is correctly untouched. All 8 plans' frontmatter `requirements:` fields reference only CONTEXT.md decision IDs (D-01/D-06/D-11/D-14-D-17/D-19/D-21) and UX-AUDIT.md finding IDs (UXA-01/02/03/04/07/09/10/14/15/UXA-08), none of which are formal REQ-IDs — this matches ROADMAP.md's own stated "Requirements: None — unmapped backlog phase" line exactly, and no orphaned REQ-ID exists.

### Anti-Patterns Found

None blocking. No `TBD`/`FIXME`/`XXX` markers found in any file this phase modified. Two INFO-level findings from REVIEW.md (IN-01: mobile-nav 420px height budget is tight but currently fits; IN-02: an intentionally-unused, documented `noqa`'d import) are explicitly low-priority/deliberate and out of the fix scope per REVIEW-FIX.md's own frontmatter (`findings_in_scope: 5`, IN-01/IN-02 excluded) — correctly not required for this phase's goal.

### Minor Documentation Note (non-blocking)

`.planning/STATE.md`'s frontmatter (`status: executing`, `stopped_at: "...blocked on Task 2's human-verify checkpoint"`) is stale relative to the actual git history — the checkpoint was approved, a full code-review cycle ran, and 5 findings were fixed afterward (commits `f03f7cd` through `9110f7f`), none of which is reflected in STATE.md's top summary or appended to its running log. This is a state-tracking/bookkeeping gap only (consistent with this project's previously-documented `state.update-progress` percent-computation bug), not a code or goal-achievement gap — ROADMAP.md itself correctly shows "Plans: 8/8 plans complete" with all 8 plan checkboxes ticked. Does not affect this phase's status determination.

## Gaps Summary

None. All 17 derived must-have truths verified against live code execution and direct source inspection, not SUMMARY.md narrative. The full test suite is green except the single pre-existing, independently-reproduced, unrelated `server/test_poll_loop.py` exception. The post-checkpoint code-review fix pass (5 findings) is independently re-verified in the current codebase, and does not regress anything the human checkpoint covered — if anything it more fully satisfies the checkpoint's own keyboard-only-pass item (CR-01). REQUIREMENTS.md correctly has zero entries for this phase.

---

*Verified: 2026-08-31T11:24:33Z*
*Verifier: Claude (gsd-verifier)*
