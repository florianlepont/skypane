---
phase: 08-panel-theme-rework-white-default-theme-black-yellow-red-blue
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - server/plane/render.py
  - server/test_render.py
autonomous: true
requirements: [D-08, D-09, D-10, D-12]

must_haves:
  truths:
    - "The raw ADS-B ICAO callsign no longer appears anywhere on the panel, on either card, at any information tier, including every fallback case (D-08)."
    - "When adsbdb resolved both an IATA flight identifier and a city, the main line reads as that identifier followed by to-or-from and the city (D-10 tier 1)."
    - "When only a city is known, the main line names the city with a capitalised direction word and shows no flight identifier at all (D-10 tier 2)."
    - "When only the airline is known, the card's first line is omitted entirely and the airline-and-type line moves up to where the first line would have started — on both the main card and the previous card, which have separate positioning maths (D-10 tier 3)."
    - "When nothing resolved, the first line reads Departing or Arriving in title case and the existing route-unavailable text stays on the second line unchanged (D-10 tier 4)."
    - "The previous card's two text lines are right-aligned 20px left of the aircraft's measured opaque right edge — an intentional optical correction on top of a measurement that is already exact, not a change to which edge is measured (D-12)."
  artifacts:
    - server/plane/render.py
    - server/test_render.py
  key_links:
    - "enrich._parse_route()/_route_from_entry() write the `callsign_iata` key (plan 08-02); render._flight_line1_text() reads it by exactly that name. A rename on either side silently drops every card to tier 2 with no error."
    - "_flight_line1_text() is called independently by draw_main_text_block() and draw_previous_text_block(). They are two separate functions with separate positioning maths and no shared helper, so tier 3's omitted-line handling must be implemented twice — implementing it once leaves the other card drawing an empty string at full size."
    - "draw_previous_text_block()'s line 2 is positioned from line 1's TOP (not its bottom), unlike the main block which uses line 1's bottom. The omitted-line branch must reuse each function's own `content[3] + gap` expression rather than copying the other's."
    - "right_x feeds both `available_width` and both lines' anchor. Shifting it left by 20px narrows the fit_text_size() budget by 20px as a harmless side effect; it is not a separate bug to fix."
---

<objective>
Rewrite the panel's flight-identifier line as the four-tier content ladder the spike locked, so the raw ICAO callsign is never displayed again, and apply the previous card's fixed 20px optical alignment correction.

Purpose: D-08, D-10 and D-12 from `08-CONTEXT.md`, consuming the `callsign_iata` field plan 08-02 threaded through. Every value here came from live iteration with the developer reacting to real renders — the tier definitions, the direction-word casing, and the 20px offset are locked outputs, not starting points to re-tune.
Output: a main line that degrades gracefully across four genuinely different information states, with the omitted-line case handled correctly in both of the two independent text blocks, and a previous card whose text reads as visually aligned to the aircraft it belongs to.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-PATTERNS.md
@.planning/spikes/001-panel-theme-colours/README.md
@.planning/notes/adsbdb-callsign-lookup-legacy-vs-rotating.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-02-SUMMARY.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-03-SUMMARY.md
@server/plane/render.py
</context>

<rendered_targets>
The spike produced developer-confirmed renders for every tier and for the optical
offset. Treat these as the literal target output, not merely as illustrations of a
description — open them before implementing and compare against them afterwards:

- `.planning/spikes/001-panel-theme-colours/renders/91-*.png` — tier 1, IATA identifier plus city, with the aircraft type label present on line 2
- `.planning/spikes/001-panel-theme-colours/renders/92-*.png` — tier 4, full miss
- `.planning/spikes/001-panel-theme-colours/renders/93-*.png` and `94-*.png` — tier 3, line 1 omitted, shown separately for the previous-card slot and the main-card slot
- `.planning/spikes/001-panel-theme-colours/renders/95-alignment-guide-and-bigger-subline.png` — the vertical guide line drawn at the aircraft's measured opaque right edge, proving the unshifted anchor was already exact
- `.planning/spikes/001-panel-theme-colours/renders/96-prev-text-nudged-left-15px.png` and `97-prev-text-nudged-left-20px.png` — the two live iterations; 20px is the confirmed value, 15px was rejected as not quite enough
</rendered_targets>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite _flight_line1_text() as the four-tier content ladder</name>

  <files>server/plane/render.py, server/test_render.py</files>

  <read_first>
    - `server/plane/render.py` lines 847-865 — the current `_flight_line1_text()` in full: its signature, its docstring (which explains the airline-only case and will need rewriting), its `flight.get("callsign") or hex or "?"` derivation, and the `direction = "to" if state == runway_config.STATE_DEPARTING else "from"` conditional that is the correct analog for deriving both the lowercase and title-case direction words.
    - `server/plane/render.py` lines 932-970 — `_flight_line2_text()`, this file's established pattern for a multi-source fallback ladder that floors at `ROUTE_FALLBACK_TEXT`. Match its defensive shape: `isinstance()` guards, `try`/`except` around a `.get()` that could be hostile, and a never-raises contract. Note that line 2's own behaviour is completely unchanged by this phase — only its vertical position moves, and only in tier 3.
    - `server/plane/render.py` — `ROUTE_FALLBACK_TEXT`'s definition and value, and `enrich.city_for_state()`'s contract (which end of the route it returns for each state, and that it returns `None` for a `None` route).
    - `server/plane/enrich.py` after plan 08-02 — `_parse_route()`'s six-key return dict and `airline_only_route()`'s all-`None`-but-airline shape, confirming the exact key name to read and that it is `None` rather than absent on an airline-only route.
    - `server/test_render.py` — `TEST_ROUTE`, `TEST_PREVIOUS_ROUTE`, `TEST_LONG_ROUTE`, `TEST_FLIGHT`, `TEST_PREVIOUS_FLIGHT` (roughly lines 46-75), the `_TextSpy` helper, and every check that builds an expected line-1 string by interpolating a callsign — there are seven of them, at roughly lines 551, 565, 591, 620, 667, 1268 and 1486.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-08, D-09 and D-10 in full, plus the `<specifics>` block recording the developer's own words about tier 3.
    - `.planning/spikes/001-panel-theme-colours/README.md` — investigation steps 11 through 13, including the record that an earlier draft still fell back to the bare callsign and was pushed back on.
  </read_first>

  <action>
**A. Rewrite `_flight_line1_text(flight, state, route)` in `server/plane/render.py`.** The signature does not change. The `flight` argument stays in the signature even though the new body no longer reads a callsign from it — both call sites pass it positionally, and removing it would be a signature change `08-CONTEXT.md` never asked for. Note in the docstring that it is retained deliberately.

Evaluate four tiers strictly in this order and return on the first match. Derive the direction word from the same `state == runway_config.STATE_DEPARTING` test the current body already uses; do not invent a second way to ask that question.

- **Tier 1 — identifier and city both known.** The route carries a usable `callsign_iata` value AND `enrich.city_for_state(route, state)` returns a city. Return the identifier, then a lowercase direction word (to for departing, from for arriving), then the city, single-space separated. Example shape: an identifier like AF1234, then to, then New York.
- **Tier 2 — city known, no identifier.** Return a title-case direction word (To for departing, From for arriving), then the city. No flight identifier appears at all. The casing difference from tier 1 is deliberate and confirmed: tier 1's word sits mid-sentence after an identifier, tier 2's word starts the line.
- **Tier 3 — airline known, no city, no identifier.** The route carries a truthy `airline_name` but nothing else usable. Return an empty string. This is the sentinel meaning *omit line 1 entirely*; Task 2 implements the positioning consequence in both drawing functions. Return an empty string rather than `None` so the function's return type stays consistently a string and a caller that forgets the guard degrades to drawing nothing rather than raising on a `None`.
- **Tier 4 — nothing resolved.** The route is `None`, or is not a dict, or carries no airline name either. Return the title-case state word: Departing for departing, Arriving for arriving. This is title case, deliberately unlike the all-caps DEPARTING/ARRIVING label in the panel's top-left corner, which is a different element and is not changed by this plan. Line 2 independently falls to `ROUTE_FALLBACK_TEXT` in this case, which is existing behaviour and needs no change.

Two constraints that are the whole point of the rewrite:

- **The raw callsign must not be reachable at any tier.** Delete the `flight.get("callsign") or hex or "?"` derivation entirely rather than keeping it for a fifth fallback. The spike record shows an earlier draft of this ladder kept a bare-callsign floor and the developer explicitly asked for it removed even from the fallback cases. There is no tier below tier 4.
- **Never raise.** Read `callsign_iata` and `airline_name` through a guarded `.get()` on a value confirmed to be a dict, exactly as `_flight_line2_text()` already does, and treat any non-string or blank-after-stripping value as absent. A hostile cache entry must degrade a tier, not crash a render cycle.

Note the residual ordering case in the docstring so it is a decision rather than an accident: a route carrying an identifier but no city falls through tier 1 and lands on tier 3 if it has an airline, or tier 4 if it does not. That combination cannot arise from `_parse_route()`, which requires all five core fields, but can arise from a hand-edited or corrupt persisted cache entry, so the behaviour should be stated rather than discovered.

Rewrite the docstring completely. The current one describes the callsign-based behaviour and its airline-only special case; every sentence of it is about to be false. The new one should name each tier, give its condition and its output shape, and cite D-08/D-09/D-10.

**B. Reconcile the seven existing line-1 expectations in `server/test_render.py`.** Each builds its expected string by interpolating a flight's callsign. First give the shared route fixtures the new field:

- `TEST_ROUTE` is documented as coming from the real TVF16VB adsbdb fixture — give it that fixture's own `callsign_iata` value so the default test render exercises tier 1 with a genuinely real value.
- `TEST_PREVIOUS_ROUTE` and `TEST_LONG_ROUTE` are hand-built; give each a plausible IATA-format identifier and mark it in a comment as synthetic. Giving `TEST_LONG_ROUTE` one matters: without it the long-name shrink check would stress a *shorter* string than before, quietly weakening an existing guard rail.

Then update all seven expectations to the tier-1 shape. Do not weaken any of them into a substring or "contains a city" check — the exact-string comparison is what makes them catch a ladder regression.

**C. Add tier coverage.** Add checks in the file's existing style, calling `_flight_line1_text()` directly for the unit-level cases and driving a real `build_canvas()` through `_TextSpy` for the end-to-end ones:

1. Tier 1 for both states, asserting the exact string including the lowercase direction word and correct city end (destination for departing, origin for arriving).
2. Tier 2 for both states, from a route with cities but a `None` identifier, asserting the title-case direction word and that no identifier appears.
3. Tier 3, from an `enrich.airline_only_route(...)` result, asserting an empty string is returned.
4. Tier 4, from `route=None` and separately from a dict with no airline name, asserting the title-case state word for both states.
5. **The D-08 guard, end-to-end.** Render a full two-flight canvas through `_TextSpy` for each of the four tiers in turn, and assert that no drawn text anywhere on the panel contains the flight's raw callsign or its hex code. Run it for both cards, and give the fixture flights callsigns that are distinctive enough not to collide with a city or airline name. This is the check that actually enforces D-08 rather than merely describing it.
6. Hostile input: a route whose `callsign_iata` is a non-string, an empty string, or whitespace-only degrades to the next matching tier without raising; a route that is not a dict at all lands on tier 4 without raising.

Raise `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added. Read the on-disk value first — plan 08-03 will have raised it twice — and preserve the increment rather than any literal.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 reporting `N/N checks pass` with N equal to `EXPECTED_CHECK_COUNT` and strictly greater than the value committed by plan 08-03.
    - The four tiers behave exactly as specified: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render as r; from server.plane import enrich; base={'airline_name':'Air France','origin_iata':'ORY','origin_city':'Paris','destination_iata':'JFK','destination_city':'New York','callsign_iata':'AF1234'}; f={'hex':'3985a7','callsign':'AFR1234'}; t2=dict(base, callsign_iata=None); ao=enrich.airline_only_route('Air France'); assert r._flight_line1_text(f,'departing',base)=='AF1234 to New York'; assert r._flight_line1_text(f,'arriving',base)=='AF1234 from Paris'; assert r._flight_line1_text(f,'departing',t2)=='To New York'; assert r._flight_line1_text(f,'arriving',t2)=='From Paris'; assert r._flight_line1_text(f,'departing',ao)==''; assert r._flight_line1_text(f,'departing',None)=='Departing'; assert r._flight_line1_text(f,'arriving',None)=='Arriving'; print('OK')"` prints `OK`.
    - The raw callsign is structurally unreachable: `grep -nE '_flight_line1_text' -A 40 server/plane/render.py | grep -cE "flight\.get\(.callsign.\)|flight\.get\(.hex.\)"` is 0 — the new body does not read either field from the flight dict.
    - Hostile values never raise: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render as r; f={'hex':'x','callsign':'Y'}; [r._flight_line1_text(f, s, v) for s in ('departing','arriving') for v in (None, {}, [], 'not-a-dict', 42, {'callsign_iata': 7, 'airline_name':'A'}, {'callsign_iata':'  ', 'origin_city':'Paris','destination_city':'Rome'})]; print('no raise')"` exits 0.
    - The D-08 end-to-end guard bites — demonstrate once, do not commit: temporarily add a bare-callsign fallback as a fifth tier, confirm `server/.venv/bin/python3 server/test_render.py` exits non-zero naming the D-08 check, then revert with `git checkout -- server/plane/render.py` and re-confirm the suite exits 0. Record the observed failure message in the plan summary.
    - `server/.venv/bin/python3 server/test_pipeline_e2e.py &amp;&amp; server/.venv/bin/python3 server/test_panel_preview.py &amp;&amp; server/.venv/bin/python3 server/test_enrich.py &amp;&amp; server/.venv/bin/python3 server/test_illustrations.py &amp;&amp; server/.venv/bin/python3 server/test_dither.py` all exit 0.
  </acceptance_criteria>

  <done>The main line resolves through four ordered tiers with no path back to the raw ICAO callsign, hostile route values degrade a tier instead of raising, all seven previously-callsign-based expectations have moved to the tier-1 shape without being weakened, and an end-to-end check proves no drawn text on any panel contains the raw callsign or hex.</done>
</task>

<task type="auto">
  <name>Task 2: Omit line 1 when the ladder returns empty, in both text blocks, and apply the previous card's 20px optical offset</name>

  <files>server/plane/render.py, server/test_render.py</files>

  <read_first>
    - `server/plane/render.py` lines 972-1010 — `draw_main_text_block()` in full: `center_x`, `safe_width`, the two `fit_text_size()` calls, `top_y = main_placement.content[3] + MAIN_TEXT_GAP_PX`, line 1's `textbbox`/`_assert_within_canvas`/`draw.text` sequence with anchor `ma`, and `line2_top = line1_bbox[3] + MAIN_LINE_GAP_PX` — note it derives line 2 from line 1's BOTTOM.
    - `server/plane/render.py` lines 1013-1057 — `draw_previous_text_block()` in full: its docstring explaining why the horizontal anchor reads the measured opaque right edge rather than the source rectangle, `right_x = prev_placement.content[2]`, `available_width = right_x - SAFE_BOX[0]`, the two `fit_text_size()` calls, `top_y = prev_placement.content[3] + PREVIOUS_TEXT_GAP_PX`, line 1's sequence with anchor `ra`, and `line2_top = line1_bbox[1] + PREVIOUS_LINE_GAP_PX` — note it derives line 2 from line 1's TOP, deliberately, for tighter stacking. This asymmetry is the reason the two omitted-line branches cannot share code.
    - `server/plane/render.py` lines 1200-1250 — the two call sites in `_build_active_canvas()`, confirming both discard the returned bboxes.
    - `server/plane/render.py` lines 295-315 — `MAIN_TEXT_GAP_PX` and `PREVIOUS_TEXT_GAP_PX` and the comment deriving both from the developer-confirmed render, so the new constant added below matches that documentation style.
    - `server/test_render.py` roughly lines 1240-1290 — the gap-measurement helper that locates a line-1 draw call by its exact text and measures its y against the illustration's opaque bottom.
    - `server/test_render.py` roughly lines 1470-1500 — the check asserting the previous text's anchor x equals the previous aircraft's visible right edge exactly. This check is correct today and must be updated, not deleted, to expect the new offset.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-10's tier 3 (including the exact expressions to use for each card's promoted line-2 top), D-12 in full, and the bullet stating the main card is centre-anchored and gets no equivalent nudge.
    - The spike renders listed in this plan's `<rendered_targets>` block — specifically the alignment-guide render and the 15px-versus-20px pair.
  </read_first>

  <action>
**A. Omitted-line handling, implemented twice.** `_flight_line1_text()` now returns an empty string for tier 3. Both drawing functions must detect that and skip line 1 entirely.

In `draw_main_text_block()`: when line 1's text is falsy, do not compute its font, do not compute its bbox, do not assert it within the canvas, and do not draw it. Set line 2's top to the same `main_placement.content[3] + MAIN_TEXT_GAP_PX` expression that seeds line 1's `top_y` today — line 2 starts exactly where line 1 would have. Return `None` in the first slot of the returned pair; both call sites discard the return value, so this is safe, and `None` is more honest than an invented empty bbox.

In `draw_previous_text_block()`: the same, using that function's own `prev_placement.content[3] + PREVIOUS_TEXT_GAP_PX` expression. Do not copy the main block's expression here and do not factor the two branches into a shared helper — the two functions position line 2 from opposite edges of line 1 (bottom versus top) and use different gap constants, so a shared helper would have to carry both behaviours anyway and would make the asymmetry harder to see, not easier.

Implementing this in only one of the two functions is the failure mode `08-CONTEXT.md` calls out by name: the other card would draw an empty string at full size, silently reserving vertical space for nothing and pushing its second line down. The tests below cover both cards specifically for that reason.

**B. The previous card's optical offset (D-12).** Introduce a named module-level constant for the offset — a bare `20` in an expression would be indistinguishable from a typo six months from now, and this file names every other geometry value. Put it beside `PREVIOUS_TEXT_GAP_PX` with a comment recording, in this order: that the unshifted anchor was confirmed by direct pixel instrumentation to land exactly on the aircraft's measured opaque right edge with zero delta, so this is not a bug fix; that the offset compensates for a human-perception effect, because the aircraft's rightmost pixel sits on a thin raked tail-fin tip rather than on the visual mass of the fuselage the eye anchors on; that the value was iterated live with the developer from 15px, which was judged not quite enough, to 20px, which was confirmed; and that it was tuned against one illustration file, with a wider spot-check landing in plan 08-05 and real-glass confirmation in plan 08-06.

Then subtract that constant when assigning `right_x` in `draw_previous_text_block()`. That single assignment feeds both of the card's lines, since both already read `right_x` for their right-aligned anchor, so no second edit is needed to keep the two lines aligned with each other.

`available_width` is derived from `right_x` and therefore narrows by the same 20px. That is a harmless side effect, not a separate defect: it makes `fit_text_size()`'s budget marginally tighter, which is correct given the text now starts 20px further left. Leave it deriving from `right_x` and do not add a compensating term.

**Do not touch the main card's `center_x`.** D-12 is explicit that only the previous card gets this offset: the main card's lines are centre-anchored and do not exhibit the single-raked-edge failure mode. And do not change which edge `prev_placement.content[2]` measures — the measurement was already correct, and "fixing" it would silently revert the debug session that established it.

**C. Harness updates in `server/test_render.py`.**

Update the existing anchor-alignment check to expect the shifted anchor. Keep its two existing guard clauses intact — the fixture-drift guard that fails if the two illustrations' right paddings converge, and the assertion that the two aircraft's visible right edges coincide. Only the final text-anchor comparison changes, and it should be written against the new named constant rather than a literal, so the check and the code cannot drift apart.

Add checks, in the file's existing style:

1. **Tier-3 promotion on the main card.** Render an active canvas whose main route is airline-only, capture text draws, and assert exactly one text draw exists in the main block region, that it is the airline-and-type line, and that its y equals the main illustration's opaque bottom plus the main text gap — the y line 1 would have used. Assert no empty-string draw call was made.
2. **Tier-3 promotion on the previous card.** The same for the previous card, using that card's own gap constant. This must be a separate check, not a parameterisation of the first, because it is the one that catches the change being implemented in only one function.
3. **Tier-3 on both cards simultaneously**, confirming the two independent branches compose without interfering.
4. **The optical offset**, asserted against the named constant: for a two-flight render, both of the previous card's text draws share one anchor x, and that x equals the previous aircraft's measured opaque right edge minus the constant.
5. **The main card is not offset**: its line draws remain centred on the canvas midpoint with the centre anchor, unchanged.

Raise `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added. Read the on-disk value first and preserve the increment.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 reporting `N/N checks pass`, with N strictly greater than the value committed by Task 1.
    - The offset is a named constant, equals 20, and is actually applied: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render as r; c=[n for n in dir(r) if 'OFFSET' in n and 'PREVIOUS' in n]; assert len(c)==1, c; assert getattr(r,c[0])==20, c; import inspect; src=inspect.getsource(r.draw_previous_text_block); assert c[0] in src, 'constant not used in draw_previous_text_block'; print('OK', c[0])"` prints `OK` and the constant's name.
    - The main card is untouched: `server/.venv/bin/python3 -c "import sys,inspect; sys.path.insert(0,'.'); from server.plane import render as r; src=inspect.getsource(r.draw_main_text_block); assert 'WIDTH // 2' in src; assert 'OFFSET' not in src; print('main card unshifted')"` exits 0.
    - Both cards omit line 1 on an airline-only route — assert both, not one: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render as r; from server.plane import enrich; ao=enrich.airline_only_route('Air France'); f={'hex':'3985a7','callsign':'AFR1234','aircraft_type':'B738'}; p={'hex':'4a1b02','callsign':'VLG6PD','aircraft_type':'A320'}; ao2=enrich.airline_only_route('Vueling Airlines'); r.build_canvas(f,'departing',route=ao,previous_flight=p,previous_route=ao2,previous_state='arriving'); print('both-card tier 3 renders clean')"` exits 0, and the two dedicated per-card checks added in this task both pass.
    - The tier-3 promotion checks bite — demonstrate once, do not commit: remove the omitted-line branch from `draw_previous_text_block()` only, leaving the main card's branch in place, confirm `server/.venv/bin/python3 server/test_render.py` exits non-zero naming the previous-card promotion check, then revert with `git checkout -- server/plane/render.py` and re-confirm. Record the observed failure message in the plan summary. This is the specific proof that the two implementations are independently covered.
    - Visual confirmation against the spike's own targets: run `server/.venv/bin/python3 server/plane/render.py --state departing --callsign AF1380 --previous-callsign VY1234 --preview /tmp/08-04-tier1.png` and open it beside `.planning/spikes/001-panel-theme-colours/renders/97-prev-text-nudged-left-20px.png`. Confirm the previous card's text sits visibly left of the aircraft's tail tip and reads as aligned to the fuselage mass. Monitor check only — it does not satisfy D-13.
    - `server/.venv/bin/python3 server/test_pipeline_e2e.py &amp;&amp; server/.venv/bin/python3 server/test_panel_preview.py &amp;&amp; server/.venv/bin/python3 server/test_illustrations.py &amp;&amp; server/.venv/bin/python3 server/test_dither.py &amp;&amp; server/.venv/bin/python3 server/test_config_history.py &amp;&amp; server/.venv/bin/python3 server/test_enrich.py` all exit 0.
  </acceptance_criteria>

  <done>Both text blocks independently promote line 2 into line 1's slot when the ladder omits line 1, using each function's own positioning expression; the previous card's two lines share one anchor 20px left of the measured opaque edge via a documented named constant; the main card is provably unshifted; and a deliberately half-applied implementation has been observed failing the per-card checks.</done>
</task>

<task type="auto">
  <name>Task 3: Make every ladder tier reachable from the render CLI for the on-glass session</name>

  <files>server/plane/render.py, server/test_render.py</files>

  <read_first>
    - `server/plane/render.py` lines 1360-1385 — `_PREVIEW_ROUTE` and `_PREVIEW_PREVIOUS_ROUTE`, the two hand-built sample routes the CLI uses so a preview shows resolved-route layout rather than always previewing the fallback.
    - `server/plane/render.py` lines 1384-1465 — `build_parser()` in full: `--state`, `--callsign`, `--hex`, `--previous-callsign`, `--previous-hex`, `--out`, `--preview`, `--no-route`, `--airline`, `--city`, `--calibration-preview`, `--preview-airline-only`, `--theme`, `--runway`, `--source-fault`, `--battery-low`. Note that `--theme`'s choices are drawn from the registry, so plan 08-01's four new themes are already selectable with no change here.
    - `server/plane/render.py` lines 1463-1560 — `main()`, especially the precedence chain where `--preview-airline-only` wins over `--no-route`, where `--airline`/`--city` copy and override the preview route without mutating it, and the reminder line printed when a route override is combined with `--out`.
    - `server/plane/render.py` — the `--preview-airline-only` help text, which describes the airline-only render as showing a bare callsign on line 1. That description is now wrong: after Task 1 that case omits line 1 entirely.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-13's list of what the on-glass pass must be able to exercise, which includes the content ladder's tiers.
    - `.planning/phases/07-final-on-glass-verification/07-01-PLAN.md` — Task 1's precedent for adding CLI forcing flags specifically so an on-glass step is one copy-pasteable command rather than an inline snippet typed on a production host.
  </read_first>

  <action>
Plan 08-06's on-glass session has to display each of the four content tiers on real glass. Three of the four are already reachable from the CLI — the default preview route gives a full hit, `--preview-airline-only` gives the airline-only case, and `--no-route` gives the total miss — but tier 2 (city known, no identifier) has no forcing path at all, and tier 1 currently cannot show a real identifier because neither preview route carries the field. Close both gaps. This mirrors Phase 7's own precedent, where forcing flags were added in a preparatory task precisely so the on-glass steps were single commands.

**A. Give both preview routes an identifier.** Add `callsign_iata` to `_PREVIEW_ROUTE` and `_PREVIEW_PREVIOUS_ROUTE`, using an IATA-format value consistent with each route's airline, so a plain preview now exercises tier 1 end to end. Mark them in the existing comment as synthetic sample values, in the same terms that comment already uses for the rest of these dicts.

**B. Add one flag that suppresses the identifier,** so tier 2 is forceable. It should strip the identifier from whichever preview route is in play, leaving cities intact. Define its interaction with the existing flags explicitly in its help text and implement that interaction in `main()`'s existing precedence chain rather than bolting on a separate branch: it has no effect when the route is already `None` (because `--no-route` won) and no effect on the airline-only preview route (which has no cities and therefore lands on tier 3 regardless). Both of those are no-ops rather than errors — a flag combination that quietly does nothing is better on a production host than one that raises.

**C. Correct the stale help text.** `--preview-airline-only`'s help describes that case as rendering a bare callsign on line 1. After Task 1 it omits line 1 entirely and shows only the airline-and-type line. Rewrite the help to describe what it now does and to name the tier it exercises, so an operator reading `--help` on the VPS during the on-glass session gets the truth.

**D. Add harness coverage** in `server/test_render.py`, driving `main()` through its argument list the way the existing CLI checks already do:

1. A default preview render draws a tier-1 line containing the preview route's identifier.
2. The new flag produces a tier-2 line — title-case direction word plus city, no identifier.
3. The new flag combined with `--no-route` still produces tier 4, and combined with `--preview-airline-only` still produces tier 3 with no line 1 — the two no-op interactions, pinned so a later refactor cannot make them raise.
4. No CLI path at any tier draws the raw callsign passed via `--callsign`. Run this across all four tier-forcing flag combinations in one check; it is the CLI-level counterpart of Task 1's D-08 guard, and the on-glass session drives the CLI, not the library.

Raise `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added. Read the on-disk value first and preserve the increment.

**Scope boundary.** Add exactly one new flag. Do not restructure `build_parser()`, do not change any existing flag's name or semantics, and do not touch `--theme`, whose registry-driven choices already cover the four new themes.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 reporting `N/N checks pass`, with N strictly greater than the value committed by Task 2.
    - All four tiers are reachable from the command line and each writes a PNG. Run each and open it: the default preview (tier 1, identifier plus city), the new suppression flag (tier 2, title-case direction plus city, no identifier), `--preview-airline-only` (tier 3, no first line, airline and type only), and `--no-route` (tier 4, title-case state word plus the route-unavailable line). All four commands exit 0. Record the four exact commands in the plan summary — plan 08-06 reuses them verbatim.
    - Every new theme is still reachable alongside them with no parser change: `for t in white black yellow red sky; do server/.venv/bin/python3 server/plane/render.py --state departing --callsign AF1380 --previous-callsign VY1234 --theme "$t" --preview "/tmp/08-04-theme-$t.png" || exit 1; done` exits 0 and writes five PNGs.
    - The stale help text is gone: `server/.venv/bin/python3 server/plane/render.py --help | grep -c 'bare callsign'` is 0, and reading the new help text confirms it describes an omitted first line.
    - The no-op combinations really are no-ops: running the new flag together with `--no-route`, and again together with `--preview-airline-only`, both exit 0 and produce the tier-4 and tier-3 renders respectively.
    - The raw callsign appears on none of the four renders — confirmed by the harness check added in D above, and spot-confirmed by eye on the four PNGs.
    - `git diff --stat` across this plan's three tasks touches exactly `server/plane/render.py` and `server/test_render.py`.
  </acceptance_criteria>

  <done>Each of the four content tiers is a single copy-pasteable command against the production renderer, the preview routes carry a real identifier so tier 1 is genuinely exercised, the airline-only flag's help no longer describes behaviour that was just removed, and no CLI path at any tier can draw the raw callsign.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (08-04)

**New symbols**
- A named module-level constant in `server.plane.render` for the previous card's optical left offset, value `20` (D-12). Its exact name is Claude's Discretion but must contain `PREVIOUS` and `OFFSET` so the acceptance criterion locates it; it must be documented in place with the measurement-was-already-exact rationale.
- One new `server.plane.render` CLI flag suppressing the preview routes' IATA identifier, so tier 2 is forceable. Exact flag name is Claude's Discretion; record it in the summary because plan 08-06 depends on it.
- `callsign_iata` keys on `_PREVIEW_ROUTE` and `_PREVIEW_PREVIOUS_ROUTE` (synthetic sample values)
- `callsign_iata` keys on `server.test_render`'s `TEST_ROUTE` (real, from the TVF16VB fixture), `TEST_PREVIOUS_ROUTE` and `TEST_LONG_ROUTE` (synthetic)

**Changed behaviour**
- `server.plane.render._flight_line1_text()` — rewritten as a four-tier ladder; signature unchanged; returns an empty string for tier 3
- `server.plane.render.draw_main_text_block()` — new omitted-line branch; returns `None` in the first slot when line 1 is omitted
- `server.plane.render.draw_previous_text_block()` — new omitted-line branch; `right_x` shifted left by the new constant
- `server.plane.render`'s `--preview-airline-only` help text — corrected
- `server.test_render.EXPECTED_CHECK_COUNT` — raised three times

**Removed symbols**
- None. The raw-callsign derivation inside `_flight_line1_text()` is deleted, but it was a local expression, not a module-level name.

**Deliberately unchanged**
- `_flight_line2_text()` and `ROUTE_FALLBACK_TEXT` — line 2's content is identical at every tier; only its vertical position moves, and only in tier 3
- `draw_main_text_block()`'s `center_x` — the main card gets no optical offset (D-12)
- `prev_placement.content[2]` — which edge is measured is unchanged; the offset is added on top of it
- The companion web app — its History page still shows the raw callsign and hex by design (`08-CONTEXT.md` Phase Boundary)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route dict (from a third-party adsbdb response or a persisted, possibly hand-edited cache file) -> `_flight_line1_text()` -> rasterised glyphs on a 960,000-byte device buffer | This plan adds one more untrusted field crossing this boundary and makes the boundary's branching more complex. |
| Operator's CLI arguments on a production host -> forced panel written to the device's state path | Pre-existing; this plan adds one flag to that surface. |

No new network call, no new authentication surface, no new external dependency, and zero package-manager installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | The raw ICAO callsign surviving at some tier — a retained fallback, a hostile-input path, or a CLI combination — silently reversing D-08 | high | mitigate | Task 1 deletes the callsign derivation outright and forbids a fifth tier; an end-to-end check asserts no drawn text on any panel contains the raw callsign or hex across all four tiers and both cards; Task 3 repeats the same guard at the CLI level across all four forcing combinations; plan 08-02 already removed the raw callsign from the route dict itself, so there is no nearby source to reach for. An acceptance criterion requires adding a bare-callsign fallback, observing the failure, and reverting. |
| T-08-04-02 | Tampering | The omitted-line branch implemented in only one of the two independent drawing functions, leaving the other card silently reserving vertical space for an empty string | high | mitigate | Named as the expected failure mode in the action text and in `key_links`; covered by two separate per-card checks plus a both-cards-at-once check; and an acceptance criterion requires removing the previous card's branch specifically, observing that card's own check fail, and reverting. |
| T-08-04-03 | Denial of Service | A hostile or malformed route value (non-dict, non-string identifier, blank identifier) raising inside `_flight_line1_text()` and aborting a render cycle | medium | mitigate | Task 1 requires the never-raises discipline `_flight_line2_text()` already uses — guarded `.get()` on a confirmed dict, non-string and blank values treated as absent. A dedicated check exercises a non-dict, a list, a string, an int, a non-string identifier and a whitespace identifier across both states and asserts no raise. ASVS L1 V5. |
| T-08-04-04 | Denial of Service | An extremely long identifier or city from a third-party response breaching the canvas | low | accept | Unchanged from before this plan: `fit_text_size()` already shrinks against a per-role floor and `_assert_within_canvas()` already fails loudly, and the existing long-name stress check is strengthened here rather than weakened, because `TEST_LONG_ROUTE` gains an identifier so the shrink path is stressed with a longer string than before. |
| T-08-04-05 | Tampering | "Fixing" the previous card's alignment by changing which edge is measured, silently reverting the debug session that established the current measurement | medium | mitigate | Task 2 forbids it explicitly, the new constant's in-code comment records that direct pixel instrumentation confirmed a zero delta, and the existing anchor check is updated rather than deleted so it still asserts the two aircraft's visible right edges coincide — only the final text comparison moves, and it is written against the named constant so code and test cannot drift. |
| T-08-04-06 | Repudiation | The 20px offset being recorded as a general fix when it was tuned against a single illustration file | medium | mitigate | The constant's comment must state it was tuned against one file and name plan 08-05's wider spot-check and plan 08-06's real-glass confirmation as the open follow-ups. `08-CONTEXT.md` already carries the same caveat in its deferred block. |
| T-08-04-SC | Tampering | npm/pip/cargo supply chain | n/a | accept | Not applicable — zero package-manager installs, zero new dependencies. No `## Package Legitimacy Audit` is required because no install task exists in this plan. |
</threat_model>

<verification>
- `server/test_render.py` exits 0 at its raised check count after each of the three tasks.
- All four tiers return their exact locked strings for both states, driven directly against `_flight_line1_text()`.
- No drawn text on any panel, at any tier, on either card, through either the library or the CLI, contains the raw ICAO callsign or hex.
- Both cards independently promote line 2 into line 1's slot on an airline-only route, each using its own positioning expression, proven by two separate checks and by a deliberate half-removal demonstration.
- The previous card's two lines share one anchor at the measured opaque right edge minus the named constant; the main card's lines remain centred.
- Every hostile route shape returns a string without raising.
- Four copy-pasteable CLI commands render the four tiers, and all five themes render alongside them.
- `server/test_pipeline_e2e.py`, `server/test_panel_preview.py`, `server/test_enrich.py`, `server/test_illustrations.py`, `server/test_dither.py`, `server/test_config_history.py` and `companion/test_config_page.py` all exit 0.
</verification>

<success_criteria>
- The panel never shows the raw ADS-B callsign again, at any information tier, on either card.
- Each of the four tiers renders exactly what the spike's own confirmed renders show.
- The airline-only case genuinely omits its first line on both cards rather than drawing an empty one.
- The previous card's text reads as belonging to its aircraft, via a documented optical correction that does not disturb the underlying measurement.
- Every tier is one command away for the operator running plan 08-06's on-glass session.
</success_criteria>

<output>
Create `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-04-SUMMARY.md` when done. Record: the on-disk `EXPECTED_CHECK_COUNT` baseline at execution time versus the value this plan cites; the exact name chosen for the optical-offset constant and for the new CLI flag; the four verbatim CLI commands that force the four tiers, which plan 08-06 will reuse; the identifier values chosen for both preview routes and all three test routes, marking which are real fixture values and which are synthetic; the two observed failure messages from the deliberate-regression demonstrations in Tasks 1 and 2; and a note on whether the rendered output matched the spike's `9x-*.png` targets or differed in any way.
</output>
