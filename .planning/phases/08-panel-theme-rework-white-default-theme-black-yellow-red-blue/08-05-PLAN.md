---
phase: 08-panel-theme-rework-white-default-theme-black-yellow-red-blue
plan: 05
type: execute
wave: 4
depends_on: ["08-03", "08-04"]
files_modified:
  - server/test_render.py
  - server/test_poll_loop.py
  - server/plane/render.py
autonomous: true
requirements: [D-12]

must_haves:
  truths:
    - "The previous card's 20px optical offset has been checked against a diverse sample of the vendored illustration set, not only the single Air France / Vueling fixture pair it was tuned on — so it is either confirmed as safe across differing tail geometries or the outlier is named before real glass is involved (D-12's own caveat)."
    - "The pinned panel.bin digest guard reflects the panel this phase actually produces, so it is once again a working regression detector rather than a permanently-red check everyone learns to ignore."
    - "The whole 15-harness suite passes under the configured coverage threshold, with every failure either fixed or explicitly accounted for."
    - "The restart reminder the render CLI prints when a forced panel is written names the systemd unit that actually exists on the deployed host."
  artifacts:
    - server/test_render.py
    - server/test_poll_loop.py
    - server/plane/render.py
  key_links:
    - "server/test_poll_loop.py's pinned digest is computed by CI's Linux container, not by a developer Mac — a documented FreeType rendering difference means a locally-recomputed value will be wrong in CI and vice versa. Three prior re-pins in this file's own comment history record the same constraint; the value must be read from CI's own failure output."
    - "draw_previous_text_block()'s right_x offset interacts with `available_width`, which feeds fit_text_size()'s budget. On a wide illustration with little right padding, a 20px leftward shift plus a long line could cross the safe-box left edge — the spot-check is what proves it does not."
    - "The render CLI's forced-panel reminder is the operator's only in-tool prompt to restore live polling after plan 08-06's session. Naming a unit that does not exist means the frame silently sits on a synthetic panel indefinitely."
---

<objective>
Close the three loose ends this phase's render changes leave behind before the on-glass session can run: widen D-12's single-illustration validation to a real sample, restore the pinned panel digest to a working state, and make the render CLI's restart reminder name the unit the deployed host actually runs.

Purpose: D-12's own explicit caveat about the 20px value being tuned against one illustration file, plus the suite reconciliation that plans 08-01 through 08-04 defer here on purpose because the digest re-pin needs a CI round trip that no single earlier plan could complete.
Output: a green 15-harness suite at the configured coverage threshold, a digest guard that guards again, and a preview sweep the developer can look at before committing an hour of panel refreshes to plan 08-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-03-SUMMARY.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-04-SUMMARY.md
@server/plane/render.py
@scripts/run-all-tests.sh
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spot-check the previous card's 20px optical offset across a diverse illustration sample</name>

  <files>server/test_render.py</files>

  <read_first>
    - `server/plane/render.py` `draw_previous_text_block()` after plan 08-04 — the `right_x` assignment with the new named offset constant, the `available_width = right_x - SAFE_BOX[0]` derivation, both `fit_text_size(...)` calls with their per-role floors, and both `_assert_within_canvas(...)` assertions.
    - `server/plane/render.py` `draw_illustration()` and the `IllustrationPlacement` named tuple — specifically the difference between `rect` (the full source rectangle) and `content` (the measured opaque-pixel bbox), because the whole offset question is about where the aircraft's visible right edge falls relative to its visual mass.
    - `server/test_render.py` — the existing alignment check updated by plan 08-04, the `GAP_SPREAD_ILLUSTRATIONS` list and the `_measured_gaps(...)` helper (roughly lines 1240-1290), which already parameterise a measurement across differing illustration files. Reuse that machinery rather than building a second one.
    - `server/plane/illustrations.py` — the airline-name-to-file selection table, to confirm which airline-name strings actually resolve to which vendored files before hardcoding any into a test.
    - `server/assets/icons/illustrations/` — 43 vendored PNGs. Look at a handful directly to pick a sample with genuinely different tail geometry rather than six near-identical narrowbodies.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-12's final bullet, which asks for exactly this spot-check and permits either outcome (confirm the value, or accept it with a note that an outlier may need per-file re-tuning later).
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-04-SUMMARY.md` — the name plan 08-04 chose for the offset constant.
  </read_first>

  <action>
D-12's 20px value was tuned against one illustration pair and CONTEXT.md explicitly asks for a wider check before treating it as final. Do that check two ways — programmatically, so it becomes a permanent guard rail, and visually, so a human can actually judge it.

**A. Add a parameterised check to `server/test_render.py`.** Choose a sample of six airline names whose vendored illustrations have genuinely different silhouettes — a mix of narrowbody jets, at least one turboprop, at least one regional jet, and at least one small twin, since the whole premise of the offset is that a thin raked tail tip sits far from the fuselage mass and that geometry varies by airframe. Verify each name actually resolves to the file you expect by reading the selection table first; do not guess names.

For each sampled airline, render a two-flight canvas with that airline on the previous card and assert three invariants:

1. Both of the previous card's drawn text lines share one anchor x, and it equals that aircraft's measured opaque right edge minus the offset constant. Written against the constant, never a literal.
2. Neither line's bounding box crosses the safe box's left edge. This is the real risk the offset introduces: shifting the anchor left also narrows the width budget `fit_text_size()` is given, so a wide illustration paired with a long line is where an overflow would first appear.
3. The render completes without raising — the existing canvas assertion inside the drawing function is the guard, and this simply exercises it across the sample.

Also record, as part of the same check or a sibling one, the per-file distance between each aircraft's full source rectangle right edge and its measured opaque right edge. If any sampled file's value is a large outlier relative to the rest, that is the signal D-12's caveat anticipates. Do not silently "fix" it by changing the offset — the value is locked and was confirmed live with the developer. Report it in the plan summary as a named candidate for the on-glass session to look at specifically.

**B. Write a visual sweep.** Render one preview PNG per sampled airline into a temp directory using the existing CLI, with the previous card carrying that airline. Also render the same sample at tier 3 (line 1 omitted), because that is the case where line 2 moves up into line 1's slot and the alignment reads differently. Record every written path in the plan summary; plan 08-06 uses them as the shortlist for which illustrations are worth spending a panel refresh on.

Raise `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added. Read the on-disk value first and preserve the increment.

**Scope boundary.** This task adds test coverage and produces preview images. It must not change the offset constant's value, must not make the offset per-file or configurable, and must not touch `draw_previous_text_block()`. If the sweep finds a genuine outlier, the correct output is a named finding for plan 08-06, not a code change here — `08-CONTEXT.md`'s deferred block already records per-illustration re-tuning as an unclosed risk to watch, explicitly not a task to schedule now.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 reporting `N/N checks pass`, with N strictly greater than the value committed by plan 08-04.
    - The sample is genuinely diverse and its members really resolve to distinct files: the plan summary lists the six airline names, the vendored filename each one resolved to, and the airframe class of each. Six near-identical narrowbodies is not a sample and does not satisfy this criterion.
    - The offset constant's value was not changed: `git diff server/plane/render.py` is empty for this task.
    - The overflow invariant genuinely bites — demonstrate once, do not commit: temporarily raise the offset constant to a value large enough to push a line past the safe box's left edge (try 400), confirm `server/.venv/bin/python3 server/test_render.py` exits non-zero naming the safe-box invariant, then revert with `git checkout -- server/plane/render.py` and re-confirm the suite exits 0. Record the observed failure message and the value used.
    - The visual sweep exists: the temp directory holds one PNG per sampled airline for the normal case plus one per sampled airline for the omitted-line case, and every path is listed in the plan summary.
    - The summary states plainly whether the sweep confirms the 20px value across the sample or names a specific outlier file for plan 08-06 to inspect on glass. "Looks fine" is not an outcome; name what was measured.
  </acceptance_criteria>

  <done>The optical offset is validated against a deliberately varied illustration sample by a permanent, parameterised check that also guards the width-budget side effect, a visual sweep exists for the developer to review before spending panel refreshes, and any outlier is named rather than absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin the panel.bin digest from CI and bring the full 15-harness suite green</name>

  <files>server/test_poll_loop.py</files>

  <read_first>
    - `server/test_poll_loop.py` lines 150-180 — the pinned digest constant and, crucially, the multi-paragraph comment above it recording three prior re-pins. Read all of it. It documents the standing rule, the reason each earlier re-pin happened, and the specific caveat that fired on the third one: CI's Linux container computes a different digest than a developer Mac for byte-identical code, because of a real font-rendering difference, so the value must be read verbatim from CI's own failure output rather than recomputed locally.
    - `server/test_poll_loop.py` around line 1120 — the check that consumes the constant, so its failure message format is recognisable in CI output.
    - `scripts/run-all-tests.sh` — the canonical 15-harness array, the parallel-mode coverage invocation, and the threshold enforcement step.
    - `pyproject.toml`'s coverage section — the `source`/`omit` lists and the `fail_under` value, plus the dated derivation comment explaining that the number is only meaningful under exactly that scope.
    - `.planning/STATE.md` — the recurring account of this exact digest situation across Phases 6, 7 and the 06.x backlog, including the note that the local failure has been independently confirmed out of scope four separate times and validated clean by real GitHub Actions CI.
    - The four preceding summaries in this phase directory, for the list of every harness whose check count moved.
  </read_first>

  <action>
This phase changes what the default panel looks like in four independent ways — a different default background, a heavier font at every role, no backing plates, and different line-1 text — so the pinned digest is genuinely stale, not merely environment-skewed. Restore it to a working guard.

**A. Establish the true starting state.** Run `scripts/run-all-tests.sh` and record the complete result: every harness, its pass count, its expected count, and every failure verbatim. Do this before changing anything, so the summary can distinguish what this phase caused from what was already red. Expect the digest check to fail; expect nothing else to.

**B. Re-pin from CI, not from this machine.** Push the branch and let GitHub Actions run, then read the new digest verbatim out of CI's own failure message. Do not recompute it locally and do not copy a locally-computed value — the constant's own comment records that doing so is what caused the third re-pin, and this machine and CI genuinely disagree for identical code.

If CI is not reachable in this session, do not guess and do not delete or weaken the check. Leave the constant as it is, record the situation explicitly in the plan summary as an open item with the exact command a later session needs to run, and let the phase's own verification carry the one known failure forward as a named exception rather than a surprise. A weakened digest check is worse than a temporarily stale one: the whole point of the guard is that it fires when rendered pixels move unexpectedly, and this phase moved them on purpose four times.

**C. Extend the standing-rule comment** with a fourth dated entry, in the same style as the three that precede it. Record what changed the pixels this time — name all four causes, because a future reader hitting a fifth re-pin needs to know this one was expected and multi-causal rather than mysterious — and record whether the new value came from CI or was left stale pending a CI run.

**D. Bring the suite green and confirm the coverage threshold.** Re-run `scripts/run-all-tests.sh` and confirm all 15 harnesses pass and the aggregate coverage meets the configured threshold. This phase adds new branches — four ladder tiers, two omitted-line branches, a new CLI flag — and each is covered by tests added in the plans above, so coverage should hold or improve. If it drops below the threshold, add coverage for the specific uncovered lines rather than lowering the number: the threshold's own derivation comment states it is set four points below a real measurement and that changing the scope invalidates its meaning.

Do not change `pyproject.toml`'s `source`, `omit` or `fail_under` values, and do not add a harness to `scripts/run-all-tests.sh` — this phase creates no new test file.
  </action>

  <verify>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/run-all-tests.sh` exits 0 with all 15 harnesses reporting `N/N checks pass` and the coverage threshold met — OR, if CI was unreachable this session, it exits non-zero with exactly one failure, the pinned digest check, and the plan summary carries the open item with the exact re-pin command for a later session. Any other failure is a regression and blocks this task.
    - The pinned constant changed (or is documented as deliberately unchanged pending CI): `git diff server/test_poll_loop.py` shows either a new digest value plus a fourth dated comment entry, or no value change plus a comment entry explaining why.
    - The comment records all four pixel-moving causes by name — the new default theme, the font-weight switch, the removed backing plates, and the rewritten line-1 text — so the next reader can tell an expected re-pin from an unexplained one. `grep -c 'Fourth re-pin\|fourth re-pin' server/test_poll_loop.py` is at least 1.
    - The digest guard was not weakened: reading the diff confirms the comparison is still an exact equality against a single pinned value, with no tolerance, no skip, no environment conditional and no removal.
    - `pyproject.toml` shows no diff, and `scripts/run-all-tests.sh`'s harness array shows no diff.
    - The plan summary records the pre-change full-suite result and the post-change full-suite result side by side, including every harness's check count, so the phase's total test growth is legible in one place.
  </acceptance_criteria>

  <done>The full suite is green at the configured coverage threshold with the digest guard restored to a real, exact comparison against the panel this phase actually produces — or the single remaining failure is the digest check, documented as pending a CI run with the exact command to complete it.</done>
</task>

<task type="auto">
  <name>Task 3: Point the forced-panel restart reminder at the systemd unit that actually exists</name>

  <files>server/plane/render.py</files>

  <read_first>
    - `server/plane/render.py` roughly lines 1525-1545 — the reminder line printed whenever a route override is combined with writing a packed panel buffer, and the comment above it explaining that a human forgetting the restart is the most common failure mode of a forced render.
    - `server/plane/render.py`'s module docstring `Usage (manual QA)` block near the top, and every other place in the file that names a service, path or venv, so all of them are checked rather than just the one that is obviously wrong.
    - `deploy/skypane-poll.timer`, `deploy/skypane-poll.service`, `deploy/deploy.sh` and `deploy/provision.sh` — the real unit names, the real service user, the real application root, and the real interpreter path on the deployed host.
    - `.planning/STATE.md` — the Phase 7 paragraph recording both that a legacy pre-rename timer was found running and failing on the VPS and had to be stopped, and that the naming drift inside 07-01's own example commands was carried forward as an open documentation item. This task closes that item.
    - `.planning/phases/07-final-on-glass-verification/07-01-PLAN.md` — the expectation note explaining why the timer's final state is an acceptance criterion rather than an instruction buried in a step.
  </read_first>

  <action>
The render CLI prints a reminder naming the systemd unit an operator must restart after writing a forced panel. That reminder names a unit from before the project was renamed. The unit it names does not exist on the deployed host — and worse, a legacy unit under that old name was found running and failing on the VPS during Phase 7 and had to be stopped and disabled, so the name is not merely stale, it is actively misleading.

Plan 08-06 drives this exact CLI to force a dozen panels on the real host and depends on this reminder to restore live polling at the end. Fix it now, before that session, not during it.

Replace the unit name in the reminder string with the real one from the deploy directory. While in the file, read every other place it names a service, a path or an interpreter — including the module docstring's manual-QA usage block — and correct any that carry the same pre-rename drift, so an operator copy-pasting from `--help` or from the docstring during the session gets commands that work. Check each against the deploy directory rather than assuming; some may already be correct.

Keep the reminder's structure and its trigger condition exactly as they are — this is a string correction plus any parallel corrections in nearby comments, not a redesign of when the reminder fires.

Extend `server/test_render.py`'s existing reminder check only if it asserts the old unit name literally; read it first and confirm whether it does. This phase's earlier plans may already have touched it. Do not add a new check if the existing one already covers the corrected string.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/plane/render.py --state departing --callsign AF1380 --no-route --out /tmp/08-05-forced.bin | grep -c 'skypane-poll.timer'</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` outputs at least 1 — the reminder now names the real unit.
    - The old unit name is gone from the file entirely: `grep -c 'inkframe' server/plane/render.py` is 0.
    - Every unit, path and interpreter the file names matches the deploy directory: cross-check each occurrence against `deploy/skypane-poll.timer`, `deploy/skypane-poll.service` and `deploy/deploy.sh`, and list every corrected occurrence in the plan summary.
    - The reminder still fires under exactly the same condition as before: reading the diff confirms only string content changed, not the surrounding condition or control flow.
    - `server/.venv/bin/python3 server/test_render.py` exits 0, and `scripts/run-all-tests.sh` exits 0 (or with only the documented digest exception from Task 2).
    - The plan summary notes that this closes a documentation open item carried forward from `07-01-SUMMARY.md`.
  </acceptance_criteria>

  <done>The forced-panel reminder, and every other host-facing string in the render module, names the units and paths the deployed host actually has — so the operator running plan 08-06's session can copy-paste from the tool itself and restore live polling correctly.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (08-05)

**New symbols**
- None.

**Changed values**
- `server.test_poll_loop._DEFAULT_CONFIG_DIGEST` — re-pinned to the panel this phase produces, read verbatim from CI's own failure output (or documented as pending a CI run)
- `server.test_render.EXPECTED_CHECK_COUNT` — raised
- `server.plane.render`'s forced-panel restart reminder string, plus any other pre-rename service/path strings in that module — corrected to the deployed host's real unit names and paths

**New content**
- A fourth dated re-pin entry in `server/test_poll_loop.py`'s standing-rule comment, naming all four causes of this phase's pixel movement
- A preview sweep of previous-card renders across a diverse illustration sample, written to a temp directory and enumerated in the plan summary for plan 08-06's use

**Deliberately unchanged**
- The optical offset constant's value — locked by D-12, confirmed live with the developer; an outlier found by the sweep is reported, not absorbed
- `pyproject.toml`'s coverage `source`, `omit` and `fail_under`
- `scripts/run-all-tests.sh`'s harness array — this phase creates no new test file
- The digest check's exact-equality comparison — never weakened, skipped or made conditional
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine's font rasteriser versus CI container's | Two environments produce different pixels for identical code, so a value copied from the wrong one silently disarms a regression guard in the other. |
| Render CLI's printed instructions -> operator's commands on a live production host | The tool's own output is treated as authoritative during a manual session; a wrong unit name propagates directly into a wrong command. |

No new network surface, no new external input path, no new authentication surface, and zero package-manager installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Tampering | The digest guard being weakened, skipped or deleted instead of re-pinned, permanently disarming the phase's only whole-panel byte-level regression detector | high | mitigate | Task 2 forbids weakening in the action text, requires the comparison to remain an exact equality with no tolerance or conditional, and provides an explicit sanctioned alternative — leave the value stale and carry it as a named open item with the exact completion command — so there is never pressure to disarm it just to get a green run. An acceptance criterion requires reading the diff to confirm the comparison's shape survived. |
| T-08-05-02 | Denial of Service | Plan 08-06's on-glass session ending with live polling not restored, leaving the frame frozen on a synthetic panel indefinitely and silently | high | mitigate | Task 3 corrects the reminder the CLI prints, which is the operator's in-tool prompt; an acceptance criterion requires the old name to be absent from the module entirely, not merely replaced in one string; and plan 08-06 independently carries a teardown step plus an acceptance criterion asserting a real poll cycle after the restart, so the mitigation is not single-point. Rated high because the failure is silent — the panel shows a plausible flight while detection has been dead for days. |
| T-08-05-03 | Repudiation | A locally-computed digest committed as if it came from CI, making the guard permanently wrong in the environment that actually runs it | medium | mitigate | The constant's own comment already records this exact mistake happening once; Task 2 requires the value be read verbatim from CI's failure output and requires the new comment entry to state which source it came from. |
| T-08-05-04 | Tampering | The 20px optical offset being quietly re-tuned to make a spot-check pass, reversing a value confirmed live with the developer | medium | mitigate | Task 1's scope boundary forbids changing the constant, forbids making it per-file or configurable, and forbids touching the drawing function; an acceptance criterion requires an empty diff on the render module for that task; and the sanctioned outcome for an outlier is a named finding for the on-glass session, matching `08-CONTEXT.md`'s own deferred-risk framing. |
| T-08-05-05 | Tampering | Lowering the coverage threshold to absorb a drop rather than covering the new branches | medium | mitigate | Task 2 forbids editing the threshold and its scope lists, cites the derivation comment explaining the number stops meaning anything if the scope changes, and directs any shortfall to be closed by covering the specific uncovered lines. An acceptance criterion requires an empty diff on the configuration file. |
| T-08-05-06 | Information Disclosure | A real SSH target, hostname or credential leaking into a committed string while correcting host-facing text | low | mitigate | Task 3 corrects only unit names, application paths and interpreter paths — all already public in the committed deploy directory. No credential or host address is introduced; the environment file remains gitignored, exactly as the deploy scripts already arrange. |
| T-08-05-SC | Tampering | npm/pip/cargo supply chain | n/a | accept | Not applicable — zero package-manager installs, zero new dependencies. No `## Package Legitimacy Audit` is required because no install task exists in this plan. |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` exits 0 with all 15 harnesses green and the configured coverage threshold met — or with exactly one documented failure, the pinned digest check, carried as a named open item with its completion command.
- The digest comparison is still an exact equality against a single pinned value, and its standing-rule comment carries a fourth dated entry naming all four causes of this phase's pixel movement.
- The previous card's offset holds its anchor and safe-box invariants across a deliberately varied six-file illustration sample, with any outlier named rather than absorbed.
- A visual sweep of that sample exists, in both the normal and omitted-line cases, with every path enumerated for plan 08-06.
- The render module names no pre-rename unit or path; the forced-panel reminder names the real timer and still fires under the same condition.
- `pyproject.toml` and `scripts/run-all-tests.sh` show no diff.
</verification>

<success_criteria>
- The phase's four independent pixel-moving changes are reconciled into one green suite rather than four separately-deferred failures.
- The digest guard guards again, with an honest record of why it moved and where the value came from.
- D-12's single-illustration caveat is closed with a real sample and a permanent check, not with an assurance.
- The operator running the on-glass session can trust the commands the tool itself prints.
</success_criteria>

<output>
Create `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-05-SUMMARY.md` when done. Record: the full-suite result before and after, harness by harness with check counts, so this phase's total test growth is legible in one place; the six sampled airline names with the vendored file and airframe class each resolved to, and the explicit verdict on the 20px offset across that sample including any named outlier; every preview path written by the visual sweep; where the new digest value came from (CI run URL, or the open-item command if CI was unreachable); the observed failure message from Task 1's deliberate over-offset demonstration; and every pre-rename string corrected in the render module, noting that this closes an open item carried forward from `07-01-SUMMARY.md`.
</output>
