---
phase: 08-panel-theme-rework-white-default-theme-black-yellow-red-blue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/device_config.py
  - server/test_config_history.py
  - server/test_render.py
  - companion/test_config_page.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "The panel's default appearance is a white field with black ink, for BOTH the departing and arriving states, with no code change anywhere outside server/device_config.py's registry (D-01)."
    - "A user opening the companion Config page sees five theme options — White, Black, Yellow, Red, Sky — with White pre-selected on a fresh install, and the companion code was not modified to make that happen (D-01/D-02/D-04)."
    - "Black, Yellow and Red each render as one single colour shared by both states, with contrast-correct ink (black ink on yellow; white ink on black and red) — not as a two-tone departing/arriving pair (D-02)."
    - "The existing Sky (Blue departing / Green arriving) theme still exists, still renders exactly as before, and is now labelled 'Sky' rather than 'Sky (default)' (D-03/D-04)."
    - "Every one of the five themes still satisfies render._assert_legal_palette()'s legality-plus-dominance guard rail on a real two-flight render, in both states."
  artifacts:
    - server/device_config.py
    - server/test_config_history.py
    - server/test_render.py
    - companion/test_config_page.py
  key_links:
    - "device_config.THEMES -> THEME_IDS = tuple(THEMES) -> normalise_theme_id() / theme_background_index() / theme_ink_index() / theme_label(): every consumer derives from the dict, so a malformed entry breaks the render path, not this module."
    - "device_config.DEFAULT_THEME_ID -> render.STATE_BACKGROUND / render.STATE_INK (evaluated once at render.py import time): flipping the default silently rewrites those two module-level dicts, which is why test_render.py's dominant-nibble checks are in this plan's scope and not a later one."
    - "device_config.THEME_IDS -> companion/pages/config_page.py theme_fieldset() -> the CFG-01 radio group: the picker iterates the registry generically, so the four new entries must appear with zero companion-side code change. Confirm, do not assume."
---

<objective>
Add the four new single-colour themes (White, Black, Yellow, Red) to `server/device_config.py`'s `THEMES` registry, make White the new default, and relabel the retained Sky theme — then prove the three claims the rest of this phase depends on: the registry alone drives everything, the companion picker needs no code change, and every new theme still passes the palette guard rail.

Purpose: D-01/D-02/D-03/D-04 from `08-CONTEXT.md`. This is the first of two Wave 1 plans and is a prerequisite for every render-side plan that follows, because flipping `DEFAULT_THEME_ID` rewrites `render.STATE_BACKGROUND`/`STATE_INK` at import time.
Output: a five-entry `THEMES` registry, `DEFAULT_THEME_ID = "white"`, and regression coverage in three harnesses proving the change propagated without any call-site fanout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-PATTERNS.md
@server/device_config.py
@server/panel_format.py
</context>

<source_audit>
## Multi-Source Coverage Audit (whole phase, all six plans)

There is no RESEARCH.md for Phase 8 — research was deliberately skipped because the
interactive spike `.planning/spikes/001-panel-theme-colours/` already validated every
decision against real rendered output. The spike README + `MANIFEST.md` are therefore
the RESEARCH source row below. There are no REQ-IDs mapped to Phase 8 in
`REQUIREMENTS.md` (unmapped polish/refinement phase, same precedent as 06.4); the
`requirements:` frontmatter field on every plan in this set carries `08-CONTEXT.md`
decision IDs instead, matching the project's own established 06.x practice.

| # | Source | Item | Covered by |
|---|--------|------|------------|
| 1 | GOAL (ROADMAP Phase 8) | White default theme | 08-01 |
| 2 | GOAL | Black/Yellow/Red/Blue/Green as optional CFG-01 themes | 08-01 |
| 3 | GOAL | PT Serif Bold replaces the text backing-plate | 08-03 |
| 4 | GOAL | `callsign_iata` threaded through so the raw ADS-B callsign never displays | 08-02 (producer) + 08-04 (consumer) |
| 5 | GOAL | Previous-card text sizing/alignment fix | 08-03 (size) + 08-04 (alignment) |
| 6 | GOAL | Required on-glass verification pass | 08-06 |
| 7 | REQ | *(none mapped — unmapped phase, see note above)* | n/a |
| 8 | RESEARCH (spike MANIFEST Round 1) | White default; Sky retained; backing plate removed; PT Serif Bold universally; palette-only colours | 08-01, 08-03 |
| 9 | RESEARCH (spike MANIFEST Round 2) | Raw ICAO callsign never displayed; 4-tier ladder; `PREVIOUS_LINE2_FONT` 16->20; 20px optical nudge; Black/Yellow/Red approved | 08-01, 08-03, 08-04 |
| 10 | RESEARCH (spike, still open) | 20px nudge validated against one illustration only | 08-05 (automated spot-check) + 08-06 (on glass) |
| 11 | RESEARCH (spike, still open) | Black/Yellow/Red never got an aesthetic reaction | 08-06 |
| 12 | CONTEXT D-01 | `"white"` entry + new `DEFAULT_THEME_ID` | 08-01 |
| 13 | CONTEXT D-02 | `"black"`/`"yellow"`/`"red"` single-colour entries, contrast-correct ink | 08-01 |
| 14 | CONTEXT D-03 | `"sky"` kept unchanged, no longer default | 08-01 |
| 15 | CONTEXT D-04 | Plain descriptive labels; `"Sky (default)"` -> `"Sky"` | 08-01 |
| 16 | CONTEXT D-05 | Text backing-plate rectangle removed entirely | 08-03 |
| 17 | CONTEXT D-06 | Every text role switches to `PTSerif-Bold.ttf` | 08-03 |
| 18 | CONTEXT D-07 | `VENDOR.md` Supersession note for PT Serif Regular | 08-03 |
| 19 | CONTEXT D-08 | Raw ICAO callsign never displayed at any tier | 08-04 |
| 20 | CONTEXT D-09 | `callsign_iata` threaded through the enrich chain | 08-02 |
| 21 | CONTEXT D-10 | 4-tier `_flight_line1_text()` ladder + line-1-omitted handling in both draw blocks | 08-04 |
| 22 | CONTEXT D-11 | `PREVIOUS_LINE2_FONT` 16 -> 20 | 08-03 |
| 23 | CONTEXT D-12 | Previous-card 20px optical left offset | 08-04 |
| 24 | CONTEXT D-13 | Blocking on-glass verification pass | 08-06 |

**Result: 0 unplanned items.** Excluded as out of scope by CONTEXT.md's own
`<deferred>` block (not gaps): a live schedule/FIDS API for rotating-callsign
carriers; additional background colours beyond the six real palette entries;
per-illustration re-tuning of the 20px nudge.

**Explicit non-scope boundary for every plan in this set:** `08-CONTEXT.md`'s
Phase Boundary limits D-08 ("raw callsign never displayed") to the physical e-ink
panel. The companion web app's History page deliberately still shows the raw
callsign and hex, and the CFG-09 companion light/dark theme is a different,
unrelated setting. No plan in this phase may change either.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add the four new THEMES entries, flip DEFAULT_THEME_ID to white, and relabel Sky</name>

  <files>server/device_config.py</files>

  <read_first>
    - `server/device_config.py` in full (~270 lines). Specifically: the module docstring's "Adding a theme (Phase 7 and beyond)" paragraph (lines 20-27), which states the extension contract this task discharges; the `from server.panel_format import ...` line (48); `DEFAULT_THEME_ID`/`DEFAULT_RUNWAY_ID`/`DEFAULT_LED_ENABLED` (50-52); the theme-registry block comment (56-70); the `THEMES` dict (71-78); the `RUNWAYS` dict for the sibling label style (85-104); `THEME_IDS = tuple(THEMES)` (106); `normalise_theme_id()` (117-126); `theme_background_index()`/`theme_ink_index()`/`theme_label()` (235-256).
    - `server/panel_format.py` lines 55-80 — the `PALETTE_RGB` triples and the six named `IDX_*` constants, including the comment recording which triples were tuned on real glass in Phase 7 and which are still interim.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — the `### Theme registry` subsection (D-01 through D-04) and the `### Claude's Discretion` bullet about entry ordering.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-PATTERNS.md` — the `server/device_config.py — THEMES registry` section, which carries this file's exact current state and the concrete entries to add.
  </read_first>

  <action>
Edit `server/device_config.py` only. Four changes, all inside the existing structure — no new function, no signature change, no new module-level name other than the dict keys themselves.

**1. Widen the palette import.** The existing `from server.panel_format import IDX_BLUE, IDX_GREEN, IDX_WHITE` must also bring in `IDX_BLACK`, `IDX_YELLOW` and `IDX_RED`. All six are already defined in `panel_format.py` as `IDX_BLACK = 0`, `IDX_WHITE = 1`, `IDX_YELLOW = 2`, `IDX_RED = 3`, `IDX_BLUE = 4`, `IDX_GREEN = 5` — define nothing new. Keep the import alphabetically consistent with how the line reads today.

**2. Add four entries to `THEMES`,** each a flat dict with exactly the same four keys the `"sky"` entry already uses (`departing_index`, `arriving_index`, `ink_index`, `label`) and no others. Use the named `IDX_*` constants only — writing a bare palette integer here violates this module's own stated discipline in the registry block comment. The four entries, values locked by D-01/D-02/D-04:

- id `white` — departing and arriving both `IDX_WHITE`, ink `IDX_BLACK`, label `White`
- id `black` — departing and arriving both `IDX_BLACK`, ink `IDX_WHITE`, label `Black`
- id `yellow` — departing and arriving both `IDX_YELLOW`, ink `IDX_BLACK`, label `Yellow`
- id `red` — departing and arriving both `IDX_RED`, ink `IDX_WHITE`, label `Red`

Every one of the four is a single-colour theme: `departing_index` equals `arriving_index`. That is deliberate and is the whole point of D-01/D-02 — with one field colour for both states, DEPARTING versus ARRIVING is carried by the label text alone, exactly the pattern the empty state already uses. Do not invent a second hue for the arriving state on any of these four.

Insertion order is Claude's Discretion per D-01's own note (`THEME_IDS = tuple(THEMES)` derives either way). Put `white` first so source order mirrors its new default status, then `black`, `yellow`, `red`, and leave `sky` last.

**3. Relabel the retained Sky entry** (D-04). Its `label` value changes from the current parenthetical-default wording to plain `Sky`. Its three index values are untouched — D-03 keeps that theme byte-identical in behaviour, it just stops being the default.

**4. Flip the default.** `DEFAULT_THEME_ID` becomes `white` (D-01). This is the single most load-bearing line in the plan: `server/plane/render.py` evaluates `STATE_BACKGROUND` and `STATE_INK` from `DEFAULT_THEME_ID` once at import time, so this one assignment silently changes the default panel from a dithered Blue/Green field to a white field. Task 3 is where that consequence is pinned by tests.

**Comment maintenance (required, not cosmetic).** The registry block comment above `THEMES` currently narrates a history in which `"sky"` is "the *only* theme today" and its Blue/Green hues were the ones Phase 7 tuned on real glass. That claim goes stale the moment this task lands. Extend the comment — do not rewrite or delete the existing Phase 7 provenance, which is still accurate for Sky — with a short dated Phase 8 note stating: the four new single-colour entries were chosen from an interactive spike judged on on-screen preview PNGs only; none of the four has been seen on real Spectra 6 ink yet; plan 08-06's blocking on-glass session is where that happens; and the same "validate on real glass before trusting the hue" expectation the Blue/Green history establishes applies to them. Also correct the module docstring's "Adding a theme (Phase 7 and beyond)" paragraph where it frames Phase 7 as the place additional entries get added — Phase 8 is where that actually happened; keep the extension-contract sentences themselves, which are still exactly right and are what this task relies on.

Do not touch `RUNWAYS`, `DEFAULT_RUNWAY_ID`, `DEFAULT_LED_ENABLED`, `load_device_config()`, `save_device_config()`, `normalise_theme_id()`, or any accessor body. The whole point of the registry design is that none of them need to change.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server import device_config as d; from server import panel_format as p; assert d.DEFAULT_THEME_ID=='white', d.DEFAULT_THEME_ID; assert set(d.THEME_IDS)=={'white','black','yellow','red','sky'}, d.THEME_IDS; assert d.THEMES['sky']['label']=='Sky'; expect={'white':(p.IDX_WHITE,p.IDX_BLACK,'White'),'black':(p.IDX_BLACK,p.IDX_WHITE,'Black'),'yellow':(p.IDX_YELLOW,p.IDX_BLACK,'Yellow'),'red':(p.IDX_RED,p.IDX_WHITE,'Red')}; [(_ for _ in ()).throw(AssertionError(t)) for t,(bg,ink,lab) in expect.items() if not (d.THEMES[t]['departing_index']==d.THEMES[t]['arriving_index']==bg and d.THEMES[t]['ink_index']==ink and d.THEMES[t]['label']==lab and set(d.THEMES[t])=={'departing_index','arriving_index','ink_index','label'})]; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` prints `OK` and exits 0.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server import device_config as d; print(sorted({v for t in d.THEMES.values() for k,v in t.items() if k.endswith('_index')}))"` prints a subset of `[0, 1, 2, 3, 4, 5]` — every index in the registry is a real Spectra 6 palette index.
    - No bare palette integer is written in the registry: `grep -nE '"(departing|arriving|ink)_index":\s*[0-9]' server/device_config.py` returns no matches (exit 1).
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server import device_config as d; print(d.normalise_theme_id('not-a-theme'), d.normalise_theme_id(7), d.normalise_theme_id(None))"` prints `white white white` — the unrecognised-value contract still holds and now degrades to the new default.
    - `git diff --stat server/device_config.py` shows changes only in that one file, and reading the diff confirms `RUNWAYS`, `DEFAULT_RUNWAY_ID`, `DEFAULT_LED_ENABLED`, `load_device_config()`, `save_device_config()` and every accessor body are untouched.
    - The registry block comment carries a dated Phase 8 note stating the four new hues are screen-confirmed only and awaiting the 08-06 on-glass pass: `grep -c 'Phase 8' server/device_config.py` is at least 1, and reading the comment confirms the existing Phase 7 Blue/Green provenance paragraph survives alongside it rather than being replaced.
  </acceptance_criteria>

  <done>`THEMES` holds five entries whose four new members are single-colour, contrast-correct and built only from named `IDX_*` constants; `DEFAULT_THEME_ID` is `white`; Sky is retained, behaviourally unchanged, and relabelled; and the registry's provenance comment tells the truth about which hues have and have not met real ink.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the new registry and the new load-time defaults in test_config_history.py</name>

  <files>server/test_config_history.py</files>

  <read_first>
    - `server/test_config_history.py` — the whole `device_config.py` section (roughly lines 26-270). Specifically the three checks that assert a literal default config dict (`_missing_state_dir_yields_defaults`, `_malformed_file_yields_defaults`, `_hostile_values_yield_defaults`, around lines 67-110), the `save_device_config(theme="sky", ...)` call sites further down, `EXPECTED_CHECK_COUNT` at line 26, and the `check(...)` helper's `(bool, message)` return convention so the new checks match the file's own style.
    - `server/device_config.py` after Task 1 — the shipped registry this task pins.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-01 through D-04.
  </read_first>

  <action>
Edit `server/test_config_history.py` only.

**Correct the three stale default-config assertions.** Three checks compare `load_device_config()`'s result against a literal dict whose theme value is the old default. Each of those literals must carry the new default theme id instead. Do not weaken them into a "starts with" or key-subset comparison — the exact-dict equality is what makes them useful, and the runway and LED values in those same literals are unchanged and must stay exact.

**Leave the explicit `save_device_config(theme=...)` call sites alone** where they name the retained Sky theme deliberately — those exercise the round-trip of a non-default value and are now *more* meaningful than before, because the value they save genuinely differs from the load-time default. Read each one and confirm that is what it is doing before deciding; if a call site was only naming Sky because Sky happened to be the default, it may keep naming Sky and still be correct.

**Add four new checks** in this file's existing style, appended to the `device_config.py` section, each with a descriptive `check(...)` label that names the decision it pins:

1. **Registry shape (D-02).** Every entry in `THEMES` carries exactly the four contract keys and no extras; every `departing_index`, `arriving_index` and `ink_index` value is a member of the six named `panel_format.IDX_*` constants; every `label` is a non-empty string.
2. **Single-colour contract for the four new themes (D-01/D-02).** For each of the four new ids, `departing_index` equals `arriving_index`. Assert this only for the four new ids — the retained Sky theme intentionally differs, and that difference is itself pinned by asserting Sky's two indices are *not* equal, so a future edit that accidentally flattens Sky is caught.
3. **Ink contrast pairing (D-02).** White and Yellow backgrounds carry black ink; Black and Red backgrounds carry white ink. Express this as a literal expected mapping from theme id to `(background index, ink index)` so the check fails loudly on a swap rather than silently accepting any dark-on-light combination.
4. **Default and labels (D-01/D-03/D-04).** `DEFAULT_THEME_ID` is the white id and is a member of `THEMES`; `theme_label()` returns the exact plain descriptive string for each of the five ids, including the relabelled Sky value with no parenthetical suffix. Assert the absence of the parenthetical form on Sky's label specifically, since that is the one string D-04 changes rather than adds.

Raise `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added. **Read the constant's real on-disk value first and increment from that** — it was 21 at planning time, but this project's history records several plans whose literal target counts had gone stale between authoring and execution. Preserve the increment, not the literal.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_config_history.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_config_history.py` exits 0 and its final line reports `N/N checks pass` with N equal to the file's `EXPECTED_CHECK_COUNT` and strictly greater than the value committed before this task.
    - The three default-config literals name the new default theme: `grep -c '"theme": "sky", "tracked_runway"' server/test_config_history.py` is 0.
    - The four new checks are present and named: `grep -cE 'check\("' server/test_config_history.py` increased by exactly 4 relative to the pre-task value recorded in the commit.
    - Deliberately corrupting the registry makes the new checks fail rather than pass silently — demonstrate once, do not commit: temporarily swap Yellow's `ink_index` to the white constant in `server/device_config.py`, confirm `server/.venv/bin/python3 server/test_config_history.py` exits non-zero naming the ink-contrast check, then revert with `git checkout -- server/device_config.py` and re-confirm the suite exits 0. Record the observed failure message in the plan summary.
  </acceptance_criteria>

  <done>The config harness pins the five-entry registry's shape, the four new themes' single-colour and ink-contrast contracts, the new load-time default, and every plain label — and a deliberately broken registry has been observed failing it.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the three theme-default checks in test_render.py and confirm the companion picker needs no code change</name>

  <files>server/test_render.py, companion/test_config_page.py</files>

  <read_first>
    - `server/test_render.py` lines 280-345 — `_departing_packs_correctly` / `_arriving_packs_correctly` (which stash bytes into `ctx`) and the two dominant-nibble checks at roughly lines 304-323 that consume them. These are two of the exactly three checks that break from the default flip.
    - `server/test_render.py` lines 1525-1590 — the CFG-01 theme block: the byte-identity check between an implicit and an explicit default theme, the Sky-equals-default check at roughly lines 1537-1545 (the third breaking check), the unknown-theme-degrades check, and `_legal_palette_holds_for_every_theme` which already loops `THEME_IDS` and therefore now exercises all five ids for free.
    - `server/test_render.py` lines 30-70 — `EXPECTED_CHECK_COUNT`, the local `IDX_*`/`NIBBLE_*` constants, `LEGAL_NIBBLES`, `TEST_FLIGHT`/`TEST_PREVIOUS_FLIGHT`/`TEST_ROUTE`/`TEST_PREVIOUS_ROUTE`, and the `dominant_nibble()`/`nibble_counts()` helpers.
    - `companion/pages/config_page.py` `theme_fieldset()` (roughly lines 89-113) — it iterates `device_config.THEME_IDS` and renders `device_config.theme_label(theme_id)` per option, with no per-theme special casing. This is the code CONTEXT.md asks be *confirmed* rather than assumed.
    - `companion/test_config_page.py` — `EXPECTED_CHECK_COUNT` at line 50 and the existing theme-fieldset checks, so new checks match the harness's conventions.
    - `server/plane/render.py` lines 160-210 — `STATE_BACKGROUND`/`STATE_INK` (evaluated at import from `DEFAULT_THEME_ID`) and `state_background_index()`/`state_ink_index()`, to understand why the dominant-nibble expectation moved.
  </read_first>

  <action>
Two harnesses, no production code.

**A. `server/test_render.py` — exactly three checks break from Task 1, and all three are stale expectations rather than regressions.** This has been measured, not guessed: with the five-entry registry and the white default in place, this harness goes from 76/76 to 73/76, and the three failures are the two dominant-nibble checks and the Sky-equals-default check. Reconcile each by moving the expectation, and strengthen rather than delete:

1. The departing dominant-nibble check currently expects the Blue nibble. The default field is now white, so it must expect the White nibble — and its `check(...)` label, which cites the old flat Blue background, must be reworded to describe the White default and cite D-01 instead of the retired flat-field decision it names today.
2. The arriving dominant-nibble check gets the same treatment: it must expect the White nibble too. Note in the label that both states now share one field colour, which is exactly D-01's point and not a copy-paste error — a reader seeing two identical expectations needs that stated.
3. The Sky-equals-default check asserted that an explicit Sky theme id produced the same canvas as no theme id at all. That is now false by design. Rewrite it as two assertions in one check: an explicit White theme id produces a canvas byte-identical to no theme id, AND an explicit Sky theme id produces a canvas that genuinely *differs* from it. The second half is what keeps D-03's "Sky is retained and still distinct" claim honest; without it the check would pass even if Sky had been deleted.

Then **add two new checks** in the file's existing style:

4. **Per-theme dominant background across both states (D-01/D-02).** For each of the five registered ids and each of the two active states, render a real two-flight panel and assert the dominant nibble is the nibble corresponding to that theme's own background index for that state. Drive the loop from `device_config.THEME_IDS` and `device_config.theme_background_index()` so a future sixth theme is exercised automatically. Map palette index to wire nibble through this harness's existing local nibble constants, not by recomputing the mapping. This check is the concrete answer to CONTEXT.md's flagged uncertainty about whether background dominance survives on a flat Black/Yellow/Red field with a large livery area: measured at planning time, the background index holds roughly a 1.08M-to-0.78M margin over White on Black, Yellow and Red, so the guard rail does hold — this check is what stops that quietly regressing.
5. **Ink index is the theme's own (D-02).** For each registered id, assert `render.state_ink_index(state, theme_id=...)` equals `device_config.theme_ink_index(...)` for both states, so a future per-state ink split cannot silently bypass the registry.

Raise `EXPECTED_CHECK_COUNT` by exactly 2 (three checks changed in place, two added). Read the on-disk value first — it was 76 at planning time — and preserve the increment rather than the literal.

**B. `companion/test_config_page.py` — confirm the picker needs no code change.** Do not edit `companion/pages/config_page.py` or `companion/app.py`. Add checks proving the CFG-01 picker absorbed the four new themes purely through the registry:

6. Rendering the theme fieldset produces one radio input per registered theme id, five in total, each carrying that theme's own id as its `value` and that theme's plain label as its visible text — drive the expectation from `device_config.THEME_IDS` and `device_config.theme_label()`, not from a hardcoded list, so this stays true for a future sixth theme.
7. Rendering the fieldset with the new default theme id as the current value marks exactly one option selected, and it is the white one.

If either check cannot be made to pass without touching `companion/pages/config_page.py`, stop and report it in the plan summary rather than editing the companion module — CONTEXT.md's `code_context` predicts no companion change is needed, and a genuine need for one is a finding worth surfacing, not silently absorbing.

Raise `companion/test_config_page.py`'s `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added. Read its real on-disk value first — it was 37 at planning time, and this project's history records this exact constant having drifted between plan authoring and execution twice before.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py &amp;&amp; server/.venv/bin/python3 companion/test_config_page.py &amp;&amp; server/.venv/bin/python3 server/test_config_history.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0, reporting `N/N checks pass` with N equal to its `EXPECTED_CHECK_COUNT` and exactly 2 greater than the value committed before this task.
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with its own count raised by exactly the number of checks added.
    - `git diff --stat companion/` shows `companion/test_config_page.py` as the only changed file under `companion/` — `companion/pages/config_page.py` and `companion/app.py` are untouched, proving the registry-driven claim rather than asserting it.
    - The old Sky-equals-default assertion is gone and its replacement covers both halves: `grep -c "build_canvas(theme_id='sky') differs from build_canvas() with no theme_id" server/test_render.py` is 0, and reading the replacement confirms it asserts White-equals-default AND Sky-differs-from-default.
    - `server/.venv/bin/python3 server/test_pipeline_e2e.py &amp;&amp; server/.venv/bin/python3 server/test_panel_preview.py &amp;&amp; server/.venv/bin/python3 server/test_dither.py &amp;&amp; server/.venv/bin/python3 server/test_illustrations.py &amp;&amp; server/.venv/bin/python3 server/test_runway_config.py &amp;&amp; server/.venv/bin/python3 server/test_plane_detection.py` all exit 0 — the default flip did not reach any other harness.
    - `server/.venv/bin/python3 server/test_poll_loop.py` reports exactly one failing check, and it is the pinned `panel.bin` digest comparison. That check was **already** failing on this machine before this phase began (a documented, CI-validated macOS-versus-Linux FreeType difference), and this plan additionally invalidates its pinned value for real by changing the default theme. Record the observed count and message; the re-pin is plan 08-05's job, not this one's. Any *second* failure in that harness is a genuine regression and must be investigated here.
  </acceptance_criteria>

  <done>The three theme-default expectations in the render harness have moved to the White default and now also pin Sky's continued distinctness; five-theme background-dominance and ink-index coverage is in place; and the companion picker's registry-driven behaviour is proven by a passing test against an unmodified `config_page.py`.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (08-01)

**New symbols**
- `server.device_config.THEMES["white"]` — `departing_index`/`arriving_index` = `IDX_WHITE`, `ink_index` = `IDX_BLACK`, `label` = `"White"`
- `server.device_config.THEMES["black"]` — both indices `IDX_BLACK`, ink `IDX_WHITE`, label `"Black"`
- `server.device_config.THEMES["yellow"]` — both indices `IDX_YELLOW`, ink `IDX_BLACK`, label `"Yellow"`
- `server.device_config.THEMES["red"]` — both indices `IDX_RED`, ink `IDX_WHITE`, label `"Red"`

**Changed values**
- `server.device_config.DEFAULT_THEME_ID` — `"sky"` -> `"white"`
- `server.device_config.THEMES["sky"]["label"]` — `"Sky (default)"` -> `"Sky"`
- `server.device_config.THEME_IDS` — derived, grows from a 1-tuple to a 5-tuple
- `server.plane.render.STATE_BACKGROUND` / `STATE_INK` — derived at import from `DEFAULT_THEME_ID`; values change with no edit to `render.py`
- `server.test_config_history.EXPECTED_CHECK_COUNT`, `server.test_render.EXPECTED_CHECK_COUNT`, `companion.test_config_page.EXPECTED_CHECK_COUNT` — raised

**Removed symbols**
- None.

**Deliberately unchanged**
- `companion/pages/config_page.py`, `companion/app.py` — the CFG-01 picker absorbs all four new themes through the registry alone.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted `device_config.json` -> `load_device_config()` -> `normalise_theme_id()` | A user- or attacker-writable JSON file supplies a theme id that is used as a dict key. Pre-existing boundary; this plan widens the set of accepted values from one to five. |
| `THEMES` registry -> `render._assert_legal_palette()` | Registry values become palette indices painted onto a 960,000-byte device buffer. A malformed index escapes into the wire format. |

No new network surface, no new authentication surface, no new external input path, and zero package-manager installs are introduced by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | A `THEMES` entry written with a bare integer or an out-of-range palette index, escaping into `pack_panel()`'s wire nibbles | high | mitigate | Task 1 forbids bare integers and uses only named `IDX_*` constants; an acceptance criterion greps for the bare-integer form and must find none; Task 2's registry-shape check asserts every index is a member of the six named constants; Task 3's per-theme dominance check renders every theme in both states through `_assert_legal_palette()`'s guard rail. |
| T-08-01-02 | Tampering | Flipping `DEFAULT_THEME_ID` silently rewrites `render.STATE_BACKGROUND`/`STATE_INK` at import time, changing the shipped panel with no edit to `render.py` | high | mitigate | The coupling is named explicitly in this plan's `key_links` and in Task 1's action text; Task 3 pins the consequence with the two dominant-nibble checks plus a five-theme dominance loop, and an acceptance criterion runs every other server harness to prove the flip did not reach them. The one harness it legitimately does reach (the pinned `panel.bin` digest) is called out by name with its re-pin assigned to plan 08-05. |
| T-08-01-03 | Denial of Service | A hostile or stale theme id in `device_config.json` reaching a dict lookup and raising inside the poll cycle | low | accept | `normalise_theme_id()`'s membership-test-before-lookup contract is pre-existing, unchanged by this plan, and re-verified by an acceptance criterion against a string, an int and `None`. The residual risk is unchanged from before this plan. |
| T-08-01-04 | Repudiation | The registry's provenance comment claiming the new hues were hardware-validated when they were only screen-confirmed | medium | mitigate | Task 1 requires a dated Phase 8 note recording that all four are preview-only and awaiting plan 08-06's on-glass pass, and requires the existing Phase 7 Blue/Green provenance paragraph to survive alongside it. Enforced by an acceptance criterion. |
| T-08-01-SC | Tampering | npm/pip/cargo supply chain | n/a | accept | Not applicable — zero package-manager installs, zero new dependencies. No `## Package Legitimacy Audit` is required because no install task exists in this plan. |
</threat_model>

<verification>
- `server/test_config_history.py`, `server/test_render.py`, `companion/test_config_page.py` all exit 0 at their raised check counts.
- `server/test_dither.py`, `server/test_illustrations.py`, `server/test_pipeline_e2e.py`, `server/test_panel_preview.py`, `server/test_runway_config.py`, `server/test_plane_detection.py`, `server/test_enrich.py` all exit 0 — untouched by the default flip.
- `server/test_poll_loop.py` fails exactly one check (the pinned `panel.bin` digest, already red on this machine before the phase started, and now genuinely stale). No second failure.
- Every registered theme, in both active states, renders a real two-flight panel whose dominant nibble is that theme's own background — verified by a loop over `THEME_IDS`, not by hand.
- `companion/pages/config_page.py` and `companion/app.py` show zero diff.
</verification>

<success_criteria>
- Five themes exist in the registry, four of them new, all built from named palette constants and all single-colour with contrast-correct ink.
- White is the default; a fresh install renders a white field with black ink in both states.
- Sky survives, renders exactly as it did before, is labelled plainly, and is provably distinct from the new default.
- The companion CFG-01 picker offers all five options with no companion-side code change.
- The registry comment honestly records that the four new hues have not yet met real ink.
</success_criteria>

<output>
Create `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-01-SUMMARY.md` when done. Record the real on-disk `EXPECTED_CHECK_COUNT` baselines found at execution time versus the values this plan cites, the observed failure message from Task 2's deliberate-corruption demonstration, the exact `server/test_poll_loop.py` result (count and message), and confirmation that `companion/pages/config_page.py` was not modified.
</output>
