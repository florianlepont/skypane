---
phase: 08-panel-theme-rework-white-default-theme-black-yellow-red-blue
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - server/plane/render.py
  - server/test_render.py
  - server/assets/fonts/VENDOR.md
autonomous: true
requirements: [D-05, D-06, D-07, D-11]

must_haves:
  truths:
    - "Every text run on an active-state panel is drawn in PT Serif Bold, at every role and every size, on every theme including the White default — not just on the coloured themes that originally needed a legibility fix (D-06)."
    - "No solid rectangle is painted behind any text run anywhere on the panel, on any theme — the heavier stroke weight is what carries legibility now, with no box, outline or shadow substituted (D-05)."
    - "The previous-flight card's secondary caption renders at 20px instead of 16px (D-11)."
    - "PTSerif-Regular.ttf remains vendored on disk with its provenance record intact, marked superseded in the same way the file already marks Zilla Slab and Inter (D-07)."
    - "The font file's documented e-ink hairline-legibility risk no longer claims the Regular weight is the active one, because it no longer is."
  artifacts:
    - server/plane/render.py
    - server/test_render.py
    - server/assets/fonts/VENDOR.md
  key_links:
    - "render._font_cache is keyed by (path, size), so swapping the path string in a role-constant tuple is a complete change with no cache invalidation needed — but the two fit_text_size() call sites read the module-level PT_SERIF_REGULAR name DIRECTLY, not the role tuple's path element, so editing the tuples alone leaves the main and previous text blocks still rendering in Regular."
    - "_paint_text_backing() has four call sites across three drawing functions; removing the definition without removing all four raises NameError at render time, and removing the call sites without the definition leaves dead code the next reader will re-wire."
    - "bg_idx becomes an unused parameter on three drawing functions once the backing plate is gone. PATTERNS.md's instruction is to retain the parameter rather than change three signatures and two call sites for a change CONTEXT.md never asked for."
---

<objective>
Replace the panel's text-legibility mechanism: delete the solid backing-plate rectangle entirely, switch every active-state text role to the already-vendored PT Serif Bold, grow the previous card's caption from 16px to 20px, and record the supersession in the font provenance file.

Purpose: D-05, D-06, D-07 and D-11 from `08-CONTEXT.md`. The spike explored a stroke outline at three widths and an offset drop-shadow, found both legible, and the developer rejected both on visual grounds before font weight was tried — so this plan must not reintroduce either as a "safer" substitute.
Output: a panel whose text is legible on a dithered field with no visual treatment at all, and a font-provenance record that tells the truth about which weight is active.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-PATTERNS.md
@.planning/spikes/001-panel-theme-colours/README.md
@.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-01-SUMMARY.md
@server/plane/render.py
@server/assets/fonts/VENDOR.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch every active-state text role to PT Serif Bold and grow the previous card's caption to 20px</name>

  <files>server/plane/render.py, server/test_render.py</files>

  <read_first>
    - `server/plane/render.py` lines 114-140 — the typography block in full: `FONT_DIR`, `PT_SERIF_REGULAR`, `PT_SERIF_BOLD`, the comment explaining that the third tuple element is documentation-only because the path already selects the static weight file, and all eight role constants. Note that `EMPTY_HEADING_FONT` is already `(PT_SERIF_BOLD, 72, 700)` — it is the exact target shape for the six roles being changed.
    - `server/plane/render.py` lines 140-150 — the per-role `*_MIN_SIZE` overflow floors, which `fit_text_size()` respects and which this task does not change.
    - `server/plane/render.py` lines 988-1010 — `draw_main_text_block()`, specifically the two `fit_text_size(...)` calls that pass `PT_SERIF_REGULAR` as a bare module-level name rather than reading it out of the role tuple.
    - `server/plane/render.py` lines 1035-1045 — `draw_previous_text_block()`'s equivalent pair of `fit_text_size(...)` calls.
    - `server/plane/render.py` lines 30-62 — the module docstring's typography paragraph (which flags the Regular weight's unverified e-ink hairline risk and points at a checkpoint that has since happened) and its CFG-01 paragraph (which states the theme registry holds exactly one entry — already false after plan 08-01).
    - `server/plane/render.py` `_font()` and `_font_cache` — confirm the cache key is `(path, size)` so a path swap needs no invalidation.
    - `server/test_render.py` around lines 642-656 — the check asserting every active-state role uses the Regular file while the empty-state heading uses Bold. This check is the direct assertion of the behaviour being inverted.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-06, D-07 and D-11, plus the `### Established Patterns` bullet naming the two direct `fit_text_size` call sites.
    - `.planning/spikes/001-panel-theme-colours/README.md` — investigation steps 7 through 9, which record why Bold was chosen over Zilla Slab and why the switch is universal rather than coloured-themes-only.
  </read_first>

  <action>
**A. Role constants (`server/plane/render.py`).** Six role-constant tuples currently point at the Regular font file with a documented weight number of 400: `STATE_LABEL_FONT`, `TOP_TAG_FONT`, `MAIN_LINE1_FONT`, `MAIN_LINE2_FONT`, `PREVIOUS_LINE1_FONT`, `PREVIOUS_LINE2_FONT`. Repoint all six at `PT_SERIF_BOLD` and change the documented weight number from 400 to 700, so each matches `EMPTY_HEADING_FONT`'s existing shape exactly.

Sizes stay as they are for five of the six. `PREVIOUS_LINE2_FONT`'s size changes from 16 to 20 (D-11) — the only size change in this phase, folded in here because it is the same line. Its `PREVIOUS_LINE2_MIN_SIZE` floor is unchanged.

`EMPTY_BODY_FONT` is deliberately NOT changed. D-06 scopes the switch to the active-state render path; `08-CONTEXT.md`'s Claude's Discretion section explicitly puts the empty state out of scope, and the spike never rendered it. `EMPTY_HEADING_FONT` is already Bold and needs no edit. `PT_SERIF_REGULAR` itself stays defined — `EMPTY_BODY_FONT` still uses it, so it is not dead.

Update the comment above the role block. It currently states that every active-state role is Regular and explains that as a deliberate choice from an earlier phase. Replace that claim with a dated Phase 8 statement: every active-state role is now Bold; this is the functional replacement for the removed backing plate (Task 2), not a taste change; the spike confirmed legibility at every size on the panel including the smallest caption; and stroke-outline and drop-shadow alternatives were both tried, both read as legible, and were both rejected by the developer on visual grounds — recorded so a future reader does not re-litigate them without new information. Keep the existing sentence explaining that the third tuple element is documentation-only, which is still true.

**B. The two direct `fit_text_size` call site pairs.** `draw_main_text_block()` and `draw_previous_text_block()` each call `fit_text_size()` twice, passing the module-level Regular path name as the first argument and reading only element `[1]` (the size) out of the role tuple. All four of those first arguments must become `PT_SERIF_BOLD`. This is the step that actually changes what the main and previous flight text look like — editing the tuples in part A alone would change the top-row labels only and leave the two flight text blocks silently still rendering in Regular, which is the most plausible way this task ships half-done and passes a careless review.

**C. Module docstring corrections (same file, no behaviour).** Two paragraphs are now stale and both are load-bearing for a future reader:
- The typography paragraph flags the Regular weight's unverified e-ink hairline risk and defers it to a checkpoint. That checkpoint happened in Phase 7 and reported the Regular weight legible on real glass; the weight is now changing anyway, for a different reason (it replaces the removed backing plate). Rewrite the paragraph to say what is true now, pointing at the font provenance file for the full supersession record and at plan 08-06 for this phase's own on-glass re-check.
- The CFG-01 paragraph states the theme registry holds exactly one entry. Plan 08-01 made that five. Correct the count and the accompanying "adding Phase 7's validated variants is a single dict entry" framing, which describes work that has now been done.

**D. Invert the font-weight assertion (`server/test_render.py`).** The existing check asserting every active-state role points at the Regular file must assert Bold instead, and its `check(...)` label must cite D-06 rather than the earlier decision it names today. Keep the half of the check that pins `EMPTY_HEADING_FONT` to Bold. Extend the same check, or add one beside it, to assert `EMPTY_BODY_FONT` still points at Regular — that is the one active use keeping the Regular constant alive, and pinning it prevents a future "tidy-up" from deleting the constant and breaking the empty state.

Add two more checks in the file's existing style:
- `PREVIOUS_LINE2_FONT`'s size element equals 20 and its floor constant is unchanged (D-11).
- No text is rendered in the Regular file anywhere on an active-state panel. Assert this behaviourally rather than by reading constants: monkeypatch `render._font` (or the `fit_text_size` seam, whichever the harness can wrap without changing production code — read the module and pick the one that captures every path actually used), render a full two-flight active canvas in both states, collect every font path requested, and assert the Regular filename appears in none of them while the Bold filename appears in all of them. This is the check that catches part B being skipped, which no constant-reading assertion can catch.

Raise `EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added (checks changed in place do not count). Read the on-disk value first — plan 08-01 raised it to 78 from a planning-time baseline of 76 — and preserve the increment rather than any literal target.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 reporting `N/N checks pass` with N equal to `EXPECTED_CHECK_COUNT` and strictly greater than the value committed by plan 08-01.
    - All six active-state role constants point at the Bold file with a documented weight of 700, and the two empty-state constants are correct: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render as r; roles=['STATE_LABEL_FONT','TOP_TAG_FONT','MAIN_LINE1_FONT','MAIN_LINE2_FONT','PREVIOUS_LINE1_FONT','PREVIOUS_LINE2_FONT']; [(_ for _ in ()).throw(AssertionError(n)) for n in roles if not (getattr(r,n)[0].endswith('PTSerif-Bold.ttf') and getattr(r,n)[2]==700)]; assert r.PREVIOUS_LINE2_FONT[1]==20, r.PREVIOUS_LINE2_FONT; assert r.EMPTY_HEADING_FONT[0].endswith('PTSerif-Bold.ttf'); assert r.EMPTY_BODY_FONT[0].endswith('PTSerif-Regular.ttf'); print('OK')"` prints `OK`.
    - Neither drawing function passes the Regular constant to `fit_text_size()` any more: `grep -c 'fit_text_size(PT_SERIF_REGULAR' server/plane/render.py` is 0, and `grep -c 'fit_text_size(PT_SERIF_BOLD' server/plane/render.py` is exactly 4.
    - The behavioural no-Regular-on-an-active-panel check is present and genuinely bites — demonstrate once, do not commit: revert one of the four `fit_text_size` first arguments to the Regular constant, confirm `server/.venv/bin/python3 server/test_render.py` exits non-zero naming that check, then restore it and re-confirm the suite exits 0. Record the observed failure message in the plan summary.
    - The stale module-docstring claims are gone: `grep -c 'currently holds exactly' server/plane/render.py` is 0, and reading the corrected paragraphs confirms the theme count now matches the registry and the typography paragraph describes Bold as the active weight.
    - `server/.venv/bin/python3 server/test_dither.py &amp;&amp; server/.venv/bin/python3 server/test_illustrations.py &amp;&amp; server/.venv/bin/python3 server/test_pipeline_e2e.py &amp;&amp; server/.venv/bin/python3 server/test_panel_preview.py &amp;&amp; server/.venv/bin/python3 server/test_enrich.py &amp;&amp; server/.venv/bin/python3 server/test_runway_config.py &amp;&amp; server/.venv/bin/python3 server/test_plane_detection.py &amp;&amp; server/.venv/bin/python3 server/test_config_history.py` all exit 0.
    - `server/.venv/bin/python3 server/test_render.py --help >/dev/null 2>&1; server/.venv/bin/python3 server/plane/render.py --state departing --callsign AF1380 --previous-callsign VY1234 --preview /tmp/08-03-t1.png` exits 0 and writes the PNG. Open it and confirm by eye that the flight text is visibly heavier than before and that the previous card's second line is larger. This is a monitor check only and does not satisfy D-13.
  </acceptance_criteria>

  <done>Every active-state text role — including both flight text blocks, which are fed through a separate code path from the role constants — renders in PT Serif Bold at weight 700, the previous card's caption is 20px, the empty state is untouched, and the module docstring no longer makes two claims that stopped being true.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the solid text backing-plate rectangle entirely, on every theme</name>

  <files>server/plane/render.py, server/test_render.py</files>

  <!-- planner-discipline-allow: _paint_text_backing -->

  <read_first>
    - `server/plane/render.py` lines 542-557 — the backing-plate helper's definition and its docstring, which records the exact Phase 7 on-glass finding that motivated it. Read this before deleting it: the reason it existed is real, and Task 1's font-weight change is what replaces it. Understanding that is what stops a future reader restoring it.
    - `server/plane/render.py` lines 559-587 — `draw_top_labels()`, with two call sites (one behind the state label, one behind the top-right runway tag) and the surrounding `textbbox` / `_assert_within_canvas` / `draw.text` sequence that must survive unchanged.
    - `server/plane/render.py` lines 988-1010 — `draw_main_text_block()`, with two more call sites.
    - `server/plane/render.py` lines 1035-1057 — `draw_previous_text_block()`, with the final two call sites.
    - `server/plane/render.py` lines 1170-1250 — `_build_active_canvas()`, specifically the `bg_idx = state_background_index(...)` assignment and the three calls that pass `bg_idx` into the drawing functions above, so the consequence of the parameter becoming unused is understood before deciding what to do about it.
    - `server/plane/dither.py` `dithered_state_background()` — the mechanism whose White speckle the backing plate was fighting. It is NOT being changed by this phase; the panel background stays a dithered lighten-toward-White blend for every theme, including the four new single-colour ones.
    - `server/test_render.py` — the `_TextSpy` helper, which monkeypatches the text-drawing seam. The new check below needs the same technique applied to the rectangle-drawing seam.
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-05, including its explicit statement that no replacement box, outline or shadow is wanted.
    - `.planning/spikes/001-panel-theme-colours/README.md` — investigation steps 2 through 6, which document the outline and drop-shadow experiments and the developer's rejection of both.
  </read_first>

  <action>
Delete the backing-plate mechanism from `server/plane/render.py`: the helper function itself, together with all six of its call sites — two in `draw_top_labels()`, two in `draw_main_text_block()`, two in `draw_previous_text_block()`. (PATTERNS.md counted four; the file has six. Verify the real count with a grep before editing and record the discrepancy in the summary rather than trusting either number.) The definition and every call site go together: leaving the definition behind creates dead code the next reader will re-wire, and leaving a call site behind raises at render time on the very first panel.

At each call site, delete only the backing-plate line. The surrounding `textbbox` computation, the `_assert_within_canvas` assertion, and the `draw.text` call are all unchanged and all still needed — the bbox is still used to position the next line, and the canvas assertion is still the layout guard rail.

Substitute nothing. No outline, no `stroke_width`/`stroke_fill`, no offset shadow copy, no thinner or lighter box, no per-theme conditional that keeps the plate on the dithered themes and drops it on White. D-05 is explicit that the removal is universal, and the spike record shows outline (at 1, 2 and 3 pixel stroke widths) and drop-shadow were both built, both read as legible, and were both rejected by the developer on visual grounds before font weight was tried. Task 1's Bold switch is the replacement. If the resulting render looks wrong to you, that is a finding for plan 08-06's on-glass session to adjudicate, not a licence to reintroduce a rejected technique here.

**Leave a removal-rationale comment** where the helper used to sit, in the same style this codebase already uses for a deleted-not-hidden helper elsewhere. State: what was removed, that it was added in Phase 7 to fix a real on-glass legibility regression against the dithered background, that Phase 8 supersedes that fix with a heavier font weight rather than re-tuning it, and that outline and shadow were both explored and rejected. Do not write the removed function's name in a form that reads like a call or a definition — describe it in prose, so a future grep for live references does not match a comment.

**The `bg_idx` parameter becomes unused** on all three drawing functions once their backing-plate calls are gone. Keep the parameter on all three signatures and keep the three call sites passing it. `08-CONTEXT.md` does not ask for a signature change, PATTERNS.md explicitly instructs retaining it, and a future theme with a per-state or per-role background could need it back. Add one short comment per signature marking it as deliberately retained and pointing at this decision, so it is not mistaken for an oversight and quietly removed.

**Add two checks to `server/test_render.py`,** in the file's existing style:

1. **The helper is gone.** Assert the render module has no attribute of that name. One line, and it is what makes a partial deletion impossible to miss.
2. **No background-filled rectangle is painted behind text, on any theme.** Build a rectangle-drawing spy using the same monkeypatch technique `_TextSpy` uses for the text seam. Render a full two-flight active canvas and capture every rectangle call with its bounds and fill. First, run this once and read the captured list before writing the assertion — record the complete observed set in the plan summary, so the assertion is written against what the renderer actually does rather than an assumption. Then assert that no captured rectangle is filled with the state's own background index. If a legitimate background-filled rectangle turns out to exist elsewhere in the render path, narrow the assertion to rectangles whose bounds enclose or overlap a text bbox captured by `_TextSpy` in the same render, and say so in the summary. Run the check across every registered theme and both active states, driving the loop from the theme registry, so the White default and the three other new single-colour themes are all covered rather than just the dithered Sky case the plate was originally built for.

Raise `EXPECTED_CHECK_COUNT` by exactly 2. Read the on-disk value first and preserve the increment.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 reporting `N/N checks pass` with N exactly 2 greater than the value committed by Task 1.
    - No live definition or call of the removed helper survives: `grep -cE '^def _paint_text_backing|^\s+_paint_text_backing\(' server/plane/render.py` is 0. (This form is anchored so a prose removal-rationale comment mentioning the mechanism does not match — the comment is required, and a bare whole-file grep would be self-invalidating.)
    - No rejected substitute was introduced: `grep -cE 'stroke_width|stroke_fill' server/plane/render.py` is 0, and reading the diff confirms no offset duplicate `draw.text` call and no replacement rectangle were added at any of the six sites.
    - The three drawing functions still accept and are still passed `bg_idx`: `grep -c 'bg_idx' server/plane/render.py` is at least 6, and reading the diff confirms no signature changed and no call site in `_build_active_canvas()` changed.
    - The removal is visible on a real render: `server/.venv/bin/python3 server/plane/render.py --state departing --callsign AF1380 --previous-callsign VY1234 --theme sky --preview /tmp/08-03-t2-sky.png` exits 0, and opening the PNG shows no rectangular plate behind the state label, the runway tag, or either flight text block. Compare against `/tmp/08-03-t1.png` from Task 1 to see the difference. Monitor check only — it does not satisfy D-13.
    - Every theme still holds the palette guard rail with the plate gone: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server import device_config as d; from server.plane import render as r; f={'hex':'3985a7','callsign':'AF1380','aircraft_type':'B738'}; rt={'airline_name':'Air France','origin_iata':'ORY','origin_city':'Paris','destination_iata':'JFK','destination_city':'New York'}; [r.build_canvas(f, s, route=rt, theme_id=t) for t in d.THEME_IDS for s in ('departing','arriving')]; print('all themes OK')"` exits 0.
    - `server/.venv/bin/python3 server/test_dither.py &amp;&amp; server/.venv/bin/python3 server/test_pipeline_e2e.py &amp;&amp; server/.venv/bin/python3 server/test_panel_preview.py &amp;&amp; server/.venv/bin/python3 server/test_illustrations.py` all exit 0.
  </acceptance_criteria>

  <done>The backing-plate mechanism is gone in one piece — definition and all its call sites — with nothing substituted, the reason for its removal recorded in place, the retained `bg_idx` parameters marked deliberate, and a test that fails if a background-filled rectangle ever reappears behind text on any theme.</done>
</task>

<task type="auto">
  <name>Task 3: Record the PT Serif Regular supersession in VENDOR.md and correct its now-stale risk note</name>

  <files>server/assets/fonts/VENDOR.md</files>

  <read_first>
    - `server/assets/fonts/VENDOR.md` in full. Specifically: the Inter entry's dated "Correction" paragraph (roughly lines 23-30), which is this file's established pattern for a claim that went stale; the Zilla Slab entry's `### Supersession (Phase 3, later in the same session — D-20/D-27)` subsection (roughly lines 101-107), which is the exact three-sentence shape to mirror; the `## PTSerif-Regular.ttf / PTSerif-Bold.ttf` entry heading (roughly line 109) and everything under it, including the pinned upstream commit, the per-file sha256 digests, the licence block, the `### Local modifications` subsection, and the `### Known risk — Regular weight is active (D-27, deliberate, flagged)` subsection (roughly lines 142-157).
    - `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-CONTEXT.md` — D-07 in full, including its instruction to mirror the Zilla Slab treatment rather than delete anything, and the bullet recording that Zilla Slab was re-spiked as an alternative and explicitly not chosen.
    - `.planning/spikes/001-panel-theme-colours/README.md` — investigation steps 7 through 9, the source for why Bold was chosen and why the choice is universal.
    - `hardware/BRINGUP-LOG.md` — the Phase 7 on-glass entry's legibility finding for the Regular weight, so the correction paragraph states accurately what real glass actually said rather than implying Regular failed.
  </read_first>

  <action>
Edit `server/assets/fonts/VENDOR.md` only. Two additions, no deletions.

**1. Add a supersession subsection** under the existing PT Serif entry, titled in the same form the Zilla Slab entry uses, naming Phase 8 and decisions D-06/D-07. Mirror that subsection's three-part shape:

- **What stopped referencing what.** Name the six active-state role constants and the two `fit_text_size()` call sites that moved from Regular to Bold, and state that `EMPTY_BODY_FONT` is the one remaining active reference to the Regular file, so the file is not fully unreferenced.
- **Why.** This is where this note must differ from the Zilla Slab precedent rather than copy it. Zilla Slab was replaced because the developer did not like its look. PT Serif Regular is being replaced for a *functional* reason: the solid backing plate behind every text run was removed on visual grounds, and the heavier Bold stroke is what takes over the legibility job that plate was doing against the dithered background. Say that plainly. Add that a stroke outline at three widths and an offset drop-shadow were both built and both read as legible in the spike, and were rejected by the developer before font weight was tried — and that Zilla Slab Bold/SemiBold was re-tried at the same time and again not chosen, because PT Serif keeps the panel's existing typographic family. Record that all of this was judged on preview PNGs and that plan 08-06's on-glass session is where it meets real ink.
- **Disposition.** Both PT Serif files stay vendored, with their pinned upstream commit, digests and licence record intact — the same "retained for provenance" treatment this file already gives Inter and Zilla Slab. Do not delete the file, the entry, the digests or the licence block.

**2. Correct the stale risk subsection.** The `### Known risk — Regular weight is active` subsection asserts in bold that the Regular file IS the active weight for every text role, and defers verification to a checkpoint. Both halves are now wrong: the weight changed, and the checkpoint happened. Follow the Inter entry's own precedent for this situation — add a short dated correction paragraph inside that subsection rather than deleting or silently rewriting it, because the risk record is itself useful history: it documents that the project knowingly accepted a hairline-legibility risk, then Phase 7's on-glass session found the Regular weight legible on real glass anyway, and Phase 8 changed the weight for an unrelated reason. State those three facts in that order and point the reader at the new supersession subsection. Keep the original paragraph's text intact above the correction so the sequence stays readable.

Do not restructure the file, do not touch any other font entry, and do not change any digest, commit SHA or licence text.
  </action>

  <verify>
    <automated>test "$(grep -cE '^### Supersession' server/assets/fonts/VENDOR.md)" -ge 2 &amp;&amp; grep -q 'Phase 8' server/assets/fonts/VENDOR.md &amp;&amp; grep -q 'a4951fade06ff8f09b7673aa81ffb65a8cd409e24d3289a6dc670bc4dda2557a' server/assets/fonts/VENDOR.md &amp;&amp; grep -q '038ba7336bd7ea14f12ad155bed51a4345cac5153275d521dec3ba04021c526e' server/assets/fonts/VENDOR.md &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` prints `OK` and exits 0 — at least two supersession subsections now exist in the file (Zilla Slab's and the new PT Serif one), Phase 8 is named, and both pinned sha256 digests survive untouched.
    - The new subsection sits under the PT Serif entry, not appended to the end of the file or misfiled under Zilla Slab: read the rendered order and confirm it appears after the `## PTSerif-Regular.ttf / PTSerif-Bold.ttf` heading and before the next `## ` heading (or end of file).
    - The stale risk claim carries a dated correction rather than a deletion: the `### Known risk` subsection still exists (`grep -c '### Known risk' server/assets/fonts/VENDOR.md` is at least 1) and now contains a Phase 8 correction paragraph naming both what Phase 7's on-glass session actually found about the Regular weight and why Phase 8 changed the weight regardless.
    - Both font files are still on disk and still byte-identical to their recorded digests: `shasum -a 256 server/assets/fonts/PTSerif-Regular.ttf server/assets/fonts/PTSerif-Bold.ttf` and confirm both values appear verbatim in `VENDOR.md`.
    - The note is honest about the functional-versus-taste distinction and about what has not been verified: reading it confirms it states the Bold switch replaces the removed backing plate's legibility job, that outline and shadow were tried and rejected, that Zilla Slab was re-tried and again not chosen, and that everything was judged on preview PNGs pending plan 08-06.
    - `git diff --stat server/assets/fonts/` shows `VENDOR.md` as the only changed path — no font file added, removed or modified.
  </acceptance_criteria>

  <done>The font provenance record explains why the Regular weight stopped being active, distinguishes this functional supersession from the earlier taste-driven one, records the rejected alternatives so they are not re-litigated blind, corrects the stale risk claim without erasing its history, and leaves both vendored files and their digests untouched.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (08-03)

**New symbols**
- None. This plan adds no constant and no function.

**Changed values**
- `server.plane.render.STATE_LABEL_FONT`, `TOP_TAG_FONT`, `MAIN_LINE1_FONT`, `MAIN_LINE2_FONT`, `PREVIOUS_LINE1_FONT`, `PREVIOUS_LINE2_FONT` — font path moves from `PTSerif-Regular.ttf` to `PTSerif-Bold.ttf`, documented weight `400` -> `700`
- `server.plane.render.PREVIOUS_LINE2_FONT` — size `16` -> `20` (D-11)
- The four `fit_text_size(...)` first arguments inside `draw_main_text_block()` and `draw_previous_text_block()` — `PT_SERIF_REGULAR` -> `PT_SERIF_BOLD`
- `server.test_render.EXPECTED_CHECK_COUNT` — raised

**Removed symbols**
- `server.plane.render._paint_text_backing` — the helper and every one of its call sites (D-05). Nothing replaces it.

**New content**
- `server/assets/fonts/VENDOR.md` — a new `### Supersession (Phase 8 — D-06/D-07)` subsection under the PT Serif entry, plus a dated correction paragraph inside the existing `### Known risk` subsection

**Deliberately unchanged**
- `server.plane.render.PT_SERIF_REGULAR` (still referenced by `EMPTY_BODY_FONT`), `EMPTY_HEADING_FONT`, `EMPTY_BODY_FONT`, every `*_MIN_SIZE` floor
- `server.plane.dither.dithered_state_background()` — the panel background stays a dithered lighten-toward-White blend on every theme
- The `bg_idx` parameter on `draw_top_labels()`, `draw_main_text_block()`, `draw_previous_text_block()` — retained deliberately, now commented as such
- Both vendored PT Serif font files and their pinned digests
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored font file on disk -> `PIL.ImageFont.truetype()` -> rendered panel | A binary asset is loaded and used to rasterise glyphs onto a 960,000-byte device buffer. Pre-existing boundary; this plan changes which of two already-vendored, already-digest-recorded files is loaded. |
| Removed legibility mechanism -> human readability of a device that cannot be inspected remotely | The panel is judged by eye, on hardware, and a regression here is invisible to every automated check. |

No new network surface, no new external input path, no new authentication surface, and zero package-manager installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Tampering | Half-applied font switch: the six role constants repointed at Bold while the four `fit_text_size()` call sites still pass the Regular constant, leaving both flight text blocks rendering in the old weight while every constant-reading assertion passes | high | mitigate | Task 1 names the two call-site pairs explicitly and states this as the most likely half-done failure; a grep acceptance criterion requires exactly four Bold-fed `fit_text_size` calls and zero Regular-fed ones; and a behavioural check captures every font path actually requested during a full two-flight render in both states, which no constant assertion can substitute for. An acceptance criterion requires reverting one call site, observing the failure, and restoring. |
| T-08-03-02 | Tampering | Partial removal of the backing-plate mechanism — a surviving call site raising at render time on the first real panel, or a surviving definition being re-wired later | high | mitigate | Task 2 removes definition and all call sites in one edit, requires verifying the real call-site count by grep rather than trusting either document's number, adds a check asserting the module no longer carries the attribute, and adds a rectangle-spy check across every theme and both states. The line-anchored grep criterion is deliberately written so the required removal-rationale comment cannot self-invalidate it. |
| T-08-03-03 | Tampering | A rejected legibility technique (stroke outline or drop shadow) reintroduced as a well-meaning substitute, silently reversing a developer decision | medium | mitigate | Task 2's action forbids it and cites the spike record showing both were built, both read as legible, and both were rejected on visual grounds. An acceptance criterion greps for the stroke parameters and requires zero. The reasoning is also written into the removal-rationale comment and into `VENDOR.md`, so the next reader finds it without re-running the spike. |
| T-08-03-04 | Repudiation | `VENDOR.md` continuing to assert the Regular weight is active, or the supersession note implying a taste change when the real driver was functional | medium | mitigate | Task 3 requires both a new supersession subsection stating the functional reason explicitly and a dated correction inside the stale risk subsection, following the Inter entry's own precedent for a claim that went stale. Acceptance criteria check both survive alongside the original text rather than replacing it. |
| T-08-03-05 | Tampering | Losing or altering a vendored font binary or its provenance record while editing the file that documents it | medium | mitigate | Task 3 forbids deletion of the files, digests, commit SHA and licence text; the verify command greps for both recorded digests; an acceptance criterion re-computes both digests from disk and compares against the file; and `git diff --stat` must show `VENDOR.md` as the only changed path under the fonts directory. |
| T-08-03-06 | Denial of Service | Text becoming illegible on real ink once its background plate is gone, invisible to every automated check | high | transfer | Not resolvable in software: the spike confirmed legibility at every size on preview PNGs, and this project's own history records preview judgments being overturned by real ink twice. The residual risk is transferred to plan 08-06's blocking on-glass session, which D-13 makes a gate rather than a follow-up, and which names the smallest caption size as a required observation. Both this plan's monitor-only preview criteria say explicitly that they do not satisfy D-13. |
| T-08-03-SC | Tampering | npm/pip/cargo supply chain | n/a | accept | Not applicable — zero package-manager installs, zero new dependencies. Both font files were already vendored, with pinned upstream commit and sha256 digests recorded, before this plan. No `## Package Legitimacy Audit` is required because no install task exists in this plan. |
</threat_model>

<verification>
- `server/test_render.py` exits 0 at its raised check count, including a behavioural check proving no Regular-weight glyph is drawn anywhere on an active-state panel in either state.
- `server/test_dither.py`, `server/test_illustrations.py`, `server/test_pipeline_e2e.py`, `server/test_panel_preview.py`, `server/test_enrich.py`, `server/test_runway_config.py`, `server/test_plane_detection.py`, `server/test_config_history.py` and `companion/test_config_page.py` all exit 0.
- All five registered themes build a canvas in both active states without raising, with the backing plate removed.
- No live definition or call of the removed helper remains, and no stroke or shadow substitute was introduced.
- `VENDOR.md` carries the new supersession subsection and the dated risk correction, with both font digests re-verified against the files on disk.
- Preview PNGs from Tasks 1 and 2 show heavier text, a larger previous-card caption, and no plate — recorded as a monitor check that explicitly does not discharge D-13.
</verification>

<success_criteria>
- Every text run an active panel draws is PT Serif Bold, on every theme, at every role, through both the role constants and the two independent sizing call sites.
- Nothing is painted behind text any more, and nothing was substituted for it.
- The previous card's caption is 20px.
- The Regular font file is still vendored with its provenance intact and is honestly marked superseded, with the reason distinguished from the earlier taste-driven supersession the file already records.
- Every remaining stale claim in `render.py`'s module docstring and in `VENDOR.md`'s risk note has been corrected rather than left to mislead the next reader.
</success_criteria>

<output>
Create `.planning/phases/08-panel-theme-rework-white-default-theme-black-yellow-red-blue/08-03-SUMMARY.md` when done. Record: the real on-disk `EXPECTED_CHECK_COUNT` baseline at execution time versus the value this plan cites; the actual number of backing-plate call sites found by grep versus PATTERNS.md's stated four and this plan's stated six; the complete set of rectangle-drawing calls captured by Task 2's spy, and whether the assertion had to be narrowed as a result; the observed failure message from Task 1's deliberate call-site revert; and the two preview PNG paths written, with the explicit note that they are monitor checks and do not discharge D-13.
</output>
