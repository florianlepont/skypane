---
phase: 06-companion-configuration-web-interface-visual-settings-view-s
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - adsb-test/runway3.json
  - server/plane/detect.py
  - server/test_plane_detection.py
autonomous: true
requirements: [CFG-05, CFG-12]
must_haves:
  truths:
    - "The detection pipeline can positively track any one of Orly's three runways, selected by id, using the same corridor + track-alignment gate discipline runway 3 already uses."
    - "Every existing runway-3 caller keeps working byte-for-byte through an unchanged entry point — no historical test, CLI invocation, or deployed unit changes behaviour by default."
    - "An unrecognised runway id degrades to the documented default instead of raising or silently widening the gate."
    - "A poll in which every aggregator errored is distinguishable from a poll in which no aircraft was on the runway."
  artifacts:
    - adsb-test/runway3.json
    - server/plane/detect.py
    - server/test_plane_detection.py
  key_links:
    - "geofence['runways'] key set must equal device_config.RUNWAYS key set — asserted by plan 06-10's harness."
    - "poll_current_aircraft()'s diagnostics dict is the sole signal CFG-05's fault icon is derived from; without it 'source down' and 'no aircraft' are indistinguishable."
    - "The corridor thresholds for 06/24 and 02/20 are copied from runway 3's measured values and are explicitly unvalidated — plan 06-12 is the live-capture gate that closes 06-RESEARCH.md Assumption A1."
---

<objective>
Generalise `server/plane/detect.py` from a runway-3-only detector into a runway-parameterised one (D-26/D-27/D-28), and give `adsb-test/runway3.json` a `runways` block carrying positive-tracking geometry for all three of Orly's runways. Additionally, make an all-providers-failed poll observable to the caller so CFG-05's fault icon has a real trigger signal.

Purpose: CFG-12 is the one requirement in this phase that changes what the detection pipeline actually does rather than displaying data it already produces. Getting the parameterisation and the back-compat wrapper right here is what lets plans 06-07, 06-09 and 06-10 treat runway selection as an ordinary setting.
Output: an extended geofence file, a runway-parameterised detect module with an unchanged default path, and a widened regression harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-CONTEXT.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-RESEARCH.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new, not pre-existing — do not flag as drift):

`server/plane/detect.py`: `DEFAULT_RUNWAY_ID`, `runway_ids()`, `runway_block()`, `select_aircraft_for_runway()`, and the new keyword parameter `runway_id` on `runway_axis()`, `corridor_params()`, `along_cross_track_m()`, `track_axis_deviation_deg()`, `filter_in_geofence()`, `poll_current_aircraft()`; the new `diagnostics` keyword parameter on `poll_current_aircraft()`; the new `--runway` CLI flag; the new per-record tag `on_runway` and the new selection-dict key `selected_runway`.

`adsb-test/runway3.json`: new top-level `runways` object with keys `3`, `06-24`, `02-20`, each carrying a `runway` block (with a `thresholds` two-element array, `icao_heading_designators`, `length_m`) and a `corridor` block.

<tasks>

<task type="auto">
  <name>Task 1: Add a `runways` block to adsb-test/runway3.json covering all three Orly runways</name>
  <read_first>
    - `adsb-test/runway3.json` — the whole 56-line file. Its `bbox.correction_2026_08_27`, `corridor.threshold_derivation`, `corridor.why_both_gates`, `corridor.known_residuals`, `runway.correction_2026_08_27` and `runway.neighbouring_runways` blocks carry the measured evidence and the real published OurAirports coordinates for 06/24 and 02/20. Every coordinate this task needs already exists in this file — none is to be invented.
    - `server/plane/detect.py` lines 157-230 — `load_geofence()`, `runway_axis()`, `corridor_params()`: the exact keys currently read, so the new block stays consumable by the same code shape.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-RESEARCH.md` — Pattern 7 and Pitfall 7.
  </read_first>
  <files>adsb-test/runway3.json</files>
  <action>
Edit `adsb-test/runway3.json` **additively**. Do not delete, rename, or reword any existing key. In particular the existing top-level `runway` and `corridor` blocks, and every `source` / `correction_2026_08_27` / `threshold_derivation` / `why_both_gates` / `known_residuals` string inside them, stay byte-for-byte unchanged — they are the point-in-time evidence record for the runway3-false-positive fix and `detect.py` keeps reading them as the legacy fallback path.

Add one new top-level key, `runways`, an object with exactly three keys: `3`, `06-24`, `02-20`. These three ids are the contract with `server/device_config.py`'s `RUNWAYS` registry (plan 06-01) and with the companion Config page's radio group (plan 06-07); they must match exactly, including the hyphen form.

Each entry is an object with two sub-objects:

- `runway`: `local_designation` (for `3` reuse the existing string verbatim; for the others use `Orly runway 06/24` and `Orly runway 02/20`), `icao_heading_designators` (`07/25`, `06/24`, `02/20`), `length_m` (`3320`, `3650`, `2400` — the values already recorded in this file), and `thresholds`: an array of exactly two objects, each `{"lat": ..., "lon": ...}`. Source the coordinates from this file itself: for `3`, the existing `runway.threshold_07` then `runway.threshold_25`; for `06-24`, `runway.neighbouring_runways["06/24"].threshold_06` then `threshold_24`; for `02-20`, `runway.neighbouring_runways["02/20"].threshold_02` then `threshold_20`. Order matters — the first element defines the axis origin and the along-track sign, exactly as threshold 07 does today.
- `corridor`: `half_width_m`, `extension_m`, `axis_tolerance_deg`. For `3`, copy the existing top-level values (500 / 2500 / 30) so the default path is provably identical. For `06-24` and `02-20`, use the same three numbers, and add a `threshold_status` string on those two entries only, stating plainly that these values are **copied from runway 3's measured derivation and have never been checked against a single real capture on this runway**, that runway 3's own numbers came from comparing real runway-3 traffic (measured at or below 31 m cross-track) against real wrong-runway traffic (measured at or above 611 m), that no equivalent dataset exists for this runway, and that plan 06-12 is the live-capture pass that either confirms them or replaces them. Cross-reference 06-RESEARCH.md's Assumption A1 by name.

Add a `source` string on the new `runways` object recording that it was added by phase 6 plan 06-02 for CFG-12, that it supersedes the flat `runway`/`corridor` pair as the live source while those two are preserved as the legacy fallback and historical record, and that runway 3's entry duplicates the legacy coordinates deliberately so a drift check is possible.

Also add, on each of the `06-24` and `02-20` entries, a `geometry_caveat` string recording the real reason the two gates behave differently per runway, taken from the existing `corridor.why_both_gates` text: 06/24 sits only 12 degrees off runway 3's heading so a track gate cannot separate the two, while 02/20 physically crosses runway 3's centreline so a corridor gate cannot. When the tracked runway is 06/24 or 02/20 the same asymmetry applies in reverse, and that is precisely why plan 06-12's live capture matters.

Keep the file valid JSON with the existing two-space indentation.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json;d=json.load(open('adsb-test/runway3.json'));print(sorted(d['runways']))"` prints the three ids `02-20`, `06-24`, `3`.
    - `d["runways"]["3"]["runway"]["thresholds"]` equals `[d["runway"]["threshold_07"], d["runway"]["threshold_25"]]` (checked in Task 3's harness, not by eye).
    - `d["runways"]["3"]["corridor"]` equals `{k: d["corridor"][k] for k in ("half_width_m","extension_m","axis_tolerance_deg")}`.
    - Each of `d["runways"]["06-24"]` and `d["runways"]["02-20"]` carries a non-empty `corridor.threshold_status` string and a non-empty `geometry_caveat` string.
    - `git diff adsb-test/runway3.json` shows **only** additions — no deleted or reworded line in the pre-existing `runway`, `corridor`, `bbox`, `center`, `radius_nm` or `alt_ceiling_ft` blocks. Confirm with `git diff --numstat adsb-test/runway3.json` showing 0 deletions apart from the one line that gained a trailing comma.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 server/test_plane_detection.py</automated>
  </verify>
  <done>The geofence file carries positive-tracking geometry for all three runways, the legacy blocks are untouched, and the unvalidated-threshold status is recorded in the file itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterise detect.py by runway id, keep the runway-3 entry point unchanged, and expose an all-providers-failed signal</name>
  <read_first>
    - `server/plane/detect.py` — the whole file. Specifically: `runway_axis()` (162-205), `corridor_params()` (208-228), `along_cross_track_m()` (231-248), `track_axis_deviation_deg()` (251-273), `filter_in_geofence()` (292-382), `select_runway3_aircraft()` (399-483), `poll_current_aircraft()` (486-578), `build_parser()` (581-614), `main()` (617-651). Every docstring in this file carries measured evidence; preserve that evidence and extend it, do not replace it.
    - `adsb-test/runway3.json` — the `runways` block Task 1 just added.
    - `server/poll_loop.py` lines 160-166 — the two current call sites (`select_runway3_aircraft` and `poll_current_aircraft`) that must keep working unchanged until plan 06-10 threads the runway through.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-RESEARCH.md` — Pattern 7 (the parameterisation sketch and the back-compat wrapper) and the Security Domain table's CFG-12 row.
  </read_first>
  <files>server/plane/detect.py</files>
  <behavior>
    - `runway_ids(geofence)` returns the three ids for the new-shape file, and the single default id for a hand-written geofence carrying no `runways` key.
    - `runway_axis(geofence)` with no `runway_id` returns exactly the same dict as before this change (same origin, same unit vector, same bearing at 74.41 degrees true).
    - `runway_axis(geofence, runway_id="06-24")` returns a bearing matching that runway's published 062/242 true heading pair to within a degree.
    - `runway_axis(geofence, runway_id="not-a-runway")` returns the runway-3 axis, not `None` and not an exception.
    - `corridor_params(geofence, runway_id="02-20")` returns the three numbers from that entry; a malformed or negative entry falls back to the module defaults exactly as today.
    - `select_runway3_aircraft(aircraft, geofence)` returns byte-identical results to the pre-change implementation for every fixture in the existing harness.
    - `select_aircraft_for_runway(aircraft, geofence, runway_id="02-20")` positively selects a real 02/20-aligned record that `select_runway3_aircraft()` correctly rejects, and rejects the runway-3 records the current fixtures accept.
    - `poll_current_aircraft(geofence, providers=[...], diagnostics=d)` fills `d` with the queried and failed provider lists and a disagreement flag; when every queried provider raised, `d` shows queried == failed and the return value is `None`.
  </behavior>
  <action>
Extend `server/plane/detect.py` in place. Keep it dependency-free — it must not import `server.device_config`, `server.history_db`, or anything else from this repo; the runway id set comes from the geofence file's own `runways` keys, and the id-set agreement with `device_config.RUNWAYS` is asserted by plan 06-10's harness instead.

Add `DEFAULT_RUNWAY_ID = "3"` next to the existing `DEFAULT_CORRIDOR_*` constants, with a comment stating it exists so every generalised function's default keeps the pre-CFG-12 behaviour.

Add two small helpers:
- `runway_ids(geofence)`: returns a tuple of the `runways` dict's keys when that key is a dict with at least one entry, else `(DEFAULT_RUNWAY_ID,)`. Never raises.
- `runway_block(geofence, runway_id=DEFAULT_RUNWAY_ID)`: returns the `runways[runway_id]` sub-dict when the `runways` key is a dict and `runway_id` is a member; otherwise, and this is the security-relevant branch, falls back to a synthetic block built from the legacy flat `runway`/`corridor` keys. An unrecognised `runway_id` must land on the default runway's geometry, never raise, and never widen a gate — mirror `corridor_params()`'s existing "malformed config falls back rather than raising" discipline and say so in the docstring (T-06-02-01, ASVS V5).

Give `runway_axis`, `corridor_params`, `along_cross_track_m`, `track_axis_deviation_deg` and `filter_in_geofence` a trailing keyword parameter `runway_id=DEFAULT_RUNWAY_ID`. Each reads its geometry via `runway_block()`. `runway_axis()` must accept both the new two-element `thresholds` array and the legacy `threshold_07`/`threshold_25` pair — read `thresholds` first, fall back to the legacy keys, and keep every existing guard (non-dict, missing key, non-numeric, zero-length axis all return `None`).

Update the docstrings that currently say "runway 3" to say "the selected runway" where the statement is now general, but **preserve verbatim** every sentence carrying measured evidence (the 71.9% / 80.5% bbox containment figures, the 611 m and 750 m wrong-runway measurements, the 21/21 numeric-track observation, the true-versus-magnetic correction). Add a new paragraph to `filter_in_geofence()`'s docstring recording that the gate is now parameterised and that the two neighbouring runways' thresholds are copied placeholders pending plan 06-12's live capture.

In `filter_in_geofence()`, set a new tag `on_runway` to the real gate result for the requested `runway_id`, and keep the existing `on_runway3` tag as a deprecated alias set to `on_runway` when `runway_id` is the default and `False` otherwise. Document that alias and its retirement condition in the docstring — several existing checks read the old tag name and must keep passing.

Rename the selection body: create `select_aircraft_for_runway(aircraft, geofence, runway_id=DEFAULT_RUNWAY_ID)` carrying the current `select_runway3_aircraft()` implementation, gating candidates on `on_runway` instead of `on_runway3`, and adding one new key to the returned normalised dict: `selected_runway`, set to the effective runway id it gated on (resolved through `runway_block()`, so an unrecognised input reports the default it actually fell back to, not the string it was handed). Then redefine `select_runway3_aircraft(aircraft, geofence)` as a thin wrapper delegating with the default id, whose docstring says it is preserved so every pre-CFG-12 caller — `poll_loop.py`, the CLI, and the existing checks in `server/test_plane_detection.py` — keeps working unchanged.

Give `poll_current_aircraft()` two new keyword parameters:
- `runway_id=DEFAULT_RUNWAY_ID`, threaded into its `select_aircraft_for_runway()` call and echoed into the returned dict alongside `sources` and `corroborated`.
- `diagnostics=None`. When a dict is passed, populate it with `queried` (the provider names actually attempted, in order), `failed` (those that raised), `selected` (those that returned a selection), `disagreement` (a bool set only on the disagree branch) and `runway_id`. This is the sole signal CFG-05 needs to tell "every ADS-B source is down" apart from "nothing is on the runway right now" — both of which return `None` today. Do not change any existing return value or any existing stderr line; the disagreement print stays exactly as it is.

Add a `--runway` CLI flag to `build_parser()` whose `choices` are computed at parse time is not possible (the geofence path is itself a flag), so instead accept a free string with `default=DEFAULT_RUNWAY_ID` and validate it in `main()` against `runway_ids(geofence)` after the geofence is loaded, printing a clear error and returning a non-zero exit code on an unknown id. Update `main()`'s human-readable output line to include the selected runway, and its "no aircraft" message to name the runway rather than hardcoding runway 3.
  </action>
  <acceptance_criteria>
    - All 28 pre-existing checks in `server/test_plane_detection.py` pass unchanged before Task 3 adds any new ones — run the harness after editing detect.py and before touching the test file.
    - `grep -vE '^\s*#' server/plane/detect.py | grep -c "from server import\|import server\." == 0` (detect.py stays repo-dependency-free).
    - `server/.venv/bin/python3 server/plane/detect.py --runway nope --geofence adsb-test/runway3.json` exits non-zero and prints a message naming the three legal ids.
    - `select_runway3_aircraft` still exists as a public name and its body is a single delegating call.
    - `poll_current_aircraft`'s signature includes both `runway_id` and `diagnostics` keyword parameters with the defaults named above.
    - `git diff server/plane/detect.py` shows no deleted sentence containing any of the measured figures 71.9, 80.5, 611, 750, 31.0, 74.41, 21/21.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 server/test_plane_detection.py</automated>
  </verify>
  <done>detect.py is runway-parameterised, the default path is provably unchanged, an unknown runway id degrades to the default, and an all-sources-failed poll is observable through the diagnostics dict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend server/test_plane_detection.py with positive-tracking checks for all three runways</name>
  <read_first>
    - `server/test_plane_detection.py` — the whole file, especially `EXPECTED_CHECK_COUNT` (line 42), the `check()` closure in `main()` (line 60), and the existing neighbouring-runway *exclusion* checks that use the 06/24 and 02/20 coordinates as negative fixtures. Those exclusion checks are what the new positive checks mirror.
    - `server/fixtures/` — the committed geofence fixtures (`39d300`, `39dd01`, `3985a7`) and their recorded cross-track offsets, plus the two real wrong-runway records (`3964e8` at -611.3 m, `39de4a` at -750.4 m) referenced in `runway3.json`'s `threshold_derivation`.
    - `adsb-test/runway3.json` — the `runways` block from Task 1.
  </read_first>
  <files>server/test_plane_detection.py</files>
  <action>
Extend the existing harness in place. Do not restructure it, do not renumber existing checks, and do not weaken any existing assertion. Raise `EXPECTED_CHECK_COUNT` from 28 to the new exact total.

Add checks covering, at minimum:

1. `runways["3"]["runway"]["thresholds"]` equals the legacy `[threshold_07, threshold_25]` pair, and `runways["3"]["corridor"]` equals the legacy corridor's three numbers — the drift guard for Task 1's deliberate duplication.
2. `runway_axis(geofence)` and `runway_axis(geofence, runway_id="3")` return identical dicts.
3. `runway_axis(geofence, runway_id="06-24")`'s bearing is within 1.0 degree of 62, and `runway_axis(geofence, runway_id="02-20")`'s is within 1.0 degree of 18 — cross-checking the computed axes against the published true headings already recorded in the file.
4. `runway_axis(geofence, runway_id="totally-unknown")` returns the same dict as the default — the never-raise, never-widen fallback (T-06-02-01).
5. `corridor_params(geofence, runway_id="02-20")` returns the three expected numbers, and a geofence whose `runways["02-20"].corridor.half_width_m` has been hand-mutated to a negative number falls back to `DEFAULT_CORRIDOR_HALF_WIDTH_M` rather than accepting it.
6. Positive tracking: build a synthetic record placed on the 06/24 centreline (interpolate between that runway's two thresholds) with a track equal to its own bearing, and assert `select_aircraft_for_runway(..., runway_id="06-24")` selects it while `select_runway3_aircraft()` rejects it. Repeat symmetrically for 02/20.
7. Reverse direction: the real committed runway-3 fixtures are still selected with the default id and are **not** selected with `runway_id="06-24"` — the gate must be exclusive in both directions, not merely permissive.
8. `select_aircraft_for_runway()`'s returned dict carries `selected_runway` equal to the requested id, and equal to the default id when an unrecognised id was requested.
9. `filter_in_geofence()` tags carry both `on_runway` and the deprecated `on_runway3` alias, with the alias `False` when a non-default runway is requested.
10. A `poll_current_aircraft()` diagnostics check driven entirely by monkeypatching `detect.query_provider` with a double that raises for every provider — assert the return value is `None`, `diagnostics["queried"]` equals `diagnostics["failed"]`, and no network call is made. Add a second variant where one double raises and the other returns a selection, asserting `failed` has one entry and `corroborated` is the single-source unknown value.

Every synthetic coordinate must be derived arithmetically from coordinates already in `runway3.json` — do not paste new latitude/longitude literals into the test file. State that rule in a comment so a future reader knows the fixtures are not hand-guessed.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_plane_detection.py` exits 0 and prints `plane-detection: N/N checks pass` where N equals the new `EXPECTED_CHECK_COUNT` and N is at least 38.
    - The 28 original check names still appear in the output, unmodified.
    - `grep -cE '4[89]\.7[0-9]{4}' server/test_plane_detection.py` does not increase relative to the pre-change file — no new raw coordinate literals were introduced.
    - Deliberately reverting one line of Task 2's `runway_block()` fallback makes check 4 fail; the revert is undone before commit.
    - `ruff check server/plane/detect.py server/test_plane_detection.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 server/test_plane_detection.py && ruff check server/plane/detect.py server/test_plane_detection.py</automated>
  </verify>
  <done>The harness covers positive tracking on all three runways in both directions, the unknown-id fallback, and the diagnostics signal, at a declared and exact check count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| companion Config form → `device_config.json` → `detect.select_aircraft_for_runway(runway_id=...)` | A user-chosen runway id travels from an HTTP form through a file into the geometry that decides which aircraft the frame shows. |
| ADS-B aggregator JSON → `filter_in_geofence()` | Positions, tracks and altitudes are untrusted external input already handled by explicit isinstance guards; the new runway parameter must not create a path that bypasses them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering | `runway_block()` / `runway_axis()` / `corridor_params()` | high | mitigate | An unrecognised or malformed `runway_id` falls back to the default runway's geometry and never raises, mirroring `corridor_params()`'s existing malformed-config discipline. A negative or non-numeric corridor number still falls back to the module default so a hand-edited geofence cannot widen the gate to infinity. Checks 4 and 5 in Task 3 assert both. |
| T-06-02-02 | Spoofing | corridor thresholds for 06/24 and 02/20 | high | mitigate | The copied thresholds are recorded as unvalidated in the geofence file itself, the exclusive-in-both-directions checks (Task 3 check 7) prove the gate is not merely permissive, and plan 06-12 performs the live-capture confirmation that closes 06-RESEARCH.md Assumption A1. Shipping unvalidated geometry silently is exactly the failure the runway3-false-positive session fixed. |
| T-06-02-03 | Repudiation | all-sources-failed poll | medium | mitigate | The `diagnostics` dict makes "every aggregator errored" distinguishable from "nothing on the runway", so CFG-05's fault icon fires on a real outage and not on Orly's ordinary quiet periods — the exact false-alarm trap `.planning/seeds/on-device-fault-icon.md` warns about. |
| T-06-02-04 | Tampering | back-compat wrapper | medium | mitigate | `select_runway3_aircraft()` remains a public, single-delegation wrapper; all 28 pre-existing checks must pass unchanged, which is the regression gate against a silent behaviour change on the deployed default path. |
| T-06-02-SC | Tampering | package installs | high | accept | No package manager runs in this plan. Zero new dependencies; the geofence file and detect.py use stdlib plus the already-pinned `requests`. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_plane_detection.py` exits 0 at the new declared check count.
- `scripts/run-all-tests.sh` passes all 9 registered harnesses — in particular `server/test_pipeline_e2e.py` and `server/test_poll_loop.py`, which exercise `select_runway3_aircraft()` through `poll_loop.run_once()` and must be unaffected.
- `ruff check .` is clean.
- `python3 -c "import json;json.load(open('adsb-test/runway3.json'))"` succeeds.
- `git status --porcelain stub-server/` is empty.
</verification>

<success_criteria>
- All three Orly runways are positively trackable by id, each with its own corridor and track-alignment gate.
- The runway-3 default path is provably byte-identical in behaviour: the 28 original checks pass unmodified.
- An unknown runway id lands on the default geometry without raising and without widening any gate.
- An all-providers-failed poll is distinguishable from a no-aircraft poll through the diagnostics dict.
- The unvalidated status of the two new runways' corridor thresholds is recorded in the geofence file and carried into plan 06-12.
</success_criteria>

<output>
Create `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-02-SUMMARY.md` when done.
</output>
