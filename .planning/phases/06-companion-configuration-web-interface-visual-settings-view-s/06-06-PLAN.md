---
phase: 06-companion-configuration-web-interface-visual-settings-view-s
plan: 06
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - server/plane/render.py
  - server/test_render.py
autonomous: true
requirements: [CFG-01, CFG-05, CFG-12]
must_haves:
  truths:
    - "The panel's DEPARTING and ARRIVING background colours come from a named theme rather than a hardcoded constant, so a new theme is one dict entry and no call-site change."
    - "The panel's top-right runway tag and the empty-state heading name whichever runway is actually being tracked."
    - "When every ADS-B source is failing, the rendered panel carries a small alert prompting the user to check the companion page — and it never appears during Orly's ordinary quiet periods."
    - "Every rendered panel still contains only the six legal Spectra 6 palette indices, with the state's flat background field still dominant."
  artifacts:
    - server/plane/render.py
    - server/test_render.py
  key_links:
    - "render.py reads themes and runway labels from server/device_config.py — that module is the single registry both the panel and the companion Config page agree on."
    - "The fault badge is driven only by the source_fault flag poll_loop derives from detect's diagnostics dict; deriving it from an empty render instead would make it fire constantly."
    - "_assert_legal_palette() must still pass with the badge drawn — a new glyph that used an illegal index would break every panel."
---

<objective>
Parameterise `server/plane/render.py` by theme (CFG-01) and by tracked runway (CFG-12), and add CFG-05's source-fault alert badge. All three are small, surgical changes to a module whose existing geometry, typography and palette guard rails must survive untouched.

Purpose: CFG-01's picker and CFG-12's runway selector are only real if the render pipeline actually consumes them. CFG-05 is the one requirement that changes what the frame draws, and its trigger has to be the genuine "all sources down" signal plan 06-02 exposed — not the ordinary Empty state, which `.planning/seeds/on-device-fault-icon.md` explicitly warns would train the user to ignore the icon.
Output: a theme-aware, runway-aware renderer with a fault badge, and a widened render harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-CONTEXT.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-RESEARCH.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-PATTERNS.md
@.planning/seeds/on-device-fault-icon.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-01-SUMMARY.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new, not pre-existing — do not flag as drift):

`server/plane/render.py`: `state_background_index()`, `state_ink_index()`, `runway_tag_text()`, `empty_heading_text()`, `draw_source_fault_badge()`, `SOURCE_FAULT_TEXT`, `SOURCE_FAULT_GLYPH_PX`, and the new keyword parameters `theme_id`, `runway_id`, `source_fault` on `build_canvas()` and `render_panel()`, plus `runway_id` on `draw_top_labels()`, plus the new CLI flags `--theme`, `--runway`, `--source-fault`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the panel's background and ink colours a function of the selected theme</name>
  <read_first>
    - `server/plane/render.py` lines 60-160 — the module docstring, the `sys.path` bootstrap, the spacing and font constants, and above all the Colour section at lines 124-143 (`STATE_BACKGROUND`, `STATE_INK`, `STATE_LABEL_TEXT`, `EMPTY_HEADING_TEXT`, `TOP_RIGHT_TAG_TEXT`) with its comment explaining why those dicts are keyed on `runway_config`'s constants rather than bare literals.
    - `server/plane/render.py` lines 605-690 — `_LEGAL_PANEL_INDICES`, `_assert_legal_palette()` and `_build_active_canvas()`'s opening lines, which are the two places `STATE_BACKGROUND` and `STATE_INK` are actually read.
    - `server/device_config.py` from plan 06-01 — `THEMES`, `DEFAULT_THEME_ID`, `theme_background_index()`, `theme_ink_index()`.
    - `server/panel_format.py` lines 27-77 — the D-P2-03 / D-13 / D-21 palette comment block, which records that blue and green are confirmed against on-screen previews only.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-CONTEXT.md` — D-09, D-10, D-11.
  </read_first>
  <files>server/plane/render.py</files>
  <behavior>
    - `render_panel(flight, "departing", route=...)` with no `theme_id` produces byte-identical output to the pre-change implementation.
    - `build_canvas(..., theme_id="sky")` and `build_canvas(...)` with no theme produce identical canvases.
    - `build_canvas(..., theme_id="not-a-theme")` produces the default theme's canvas rather than raising.
    - `build_canvas(flight, "nonsense-state")` still raises `ValueError` naming the three legal states — an unknown *state* stays loud even though an unknown *theme* is forgiving.
    - `_assert_legal_palette()` still passes for every theme.
  </behavior>
  <action>
Add `server.device_config` to `server/plane/render.py`'s imports, alongside its existing `server.plane.runway_config` import and after the same `sys.path` bootstrap. `device_config` is a leaf module and importing it here creates no cycle.

Replace the two direct reads of `STATE_BACKGROUND` and `STATE_INK` in `_build_active_canvas()` with calls to two new module functions:
- `state_background_index(state, theme_id=device_config.DEFAULT_THEME_ID)` — normalise the theme id through `device_config.normalise_theme_id()` first (so an unrecognised theme silently lands on the default, matching how an unrecognised runway id behaves in `detect.py`), then delegate to `device_config.theme_background_index()`. Preserve the existing behaviour that an unknown *state* raises `ValueError` with the message naming the three legal states; that check is a real caller-bug detector and stays loud.
- `state_ink_index(state, theme_id=...)` — same normalisation, delegating to `device_config.theme_ink_index()`.

Keep `STATE_BACKGROUND` and `STATE_INK` as module-level names, redefined as the default theme's mapping, with a comment stating they are retained for the existing checks in `server/test_render.py` that read them directly and that new code must call the two functions instead. Do not delete them; do not change their values.

Give `build_canvas()` and `render_panel()` a trailing keyword parameter `theme_id=device_config.DEFAULT_THEME_ID`, threaded through to `_build_active_canvas()`. Every existing positional and keyword call site keeps working unchanged.

Add a `--theme` flag to `build_parser()` whose choices are `device_config.THEME_IDS`, defaulting to the default theme, and thread it into `main()`'s render call.

Extend the module docstring's colour paragraph with a short block recording D-09/D-10/D-11: the panel's colours have never been calibrated against real glass; Phase 7 is the first place that happens; CFG-01 therefore ships a picker whose registry currently holds exactly one entry; and the extension procedure lives in `server/device_config.py`'s own docstring so that adding Phase 7's validated variants is one dict edit with no change here.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from server.plane import render as r;a=r.render_panel(None,'empty');b=r.render_panel(None,'empty',theme_id='sky');print(a==b)"` prints `True`.
    - A departing render with no theme argument and one with the default theme id hash-match under `hashlib.sha256`.
    - `build_canvas(TEST_FLIGHT, "departing", theme_id="nope")` returns a canvas rather than raising, and that canvas equals the default-theme canvas.
    - `build_canvas(TEST_FLIGHT, "sideways")` still raises `ValueError` and the message names departing, arriving and empty.
    - `grep -c "STATE_BACKGROUND" server/plane/render.py >= 1` — the constant survives for the existing checks.
    - `grep -vE '^\s*#' server/plane/render.py | grep -c "device_config" >= 4`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>
  <done>Panel colours resolve through the theme registry, the default path is byte-identical, an unknown theme degrades to the default, and an unknown state still raises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the runway tag and empty-state heading follow the tracked runway</name>
  <read_first>
    - `server/plane/render.py` lines 140-143 — `EMPTY_HEADING_TEXT`, `EMPTY_BODY_TEXT`, `TOP_RIGHT_TAG_TEXT`, `ROUTE_FALLBACK_TEXT`.
    - `server/plane/render.py` lines 241-266 — `draw_top_labels()`, including the comment explaining why `_assert_within_canvas()` and not the stricter `_assert_in_safe_box()` is used for these two small strings (real glyph bearings at small sizes).
    - `server/plane/render.py` lines 573-602 — `_build_empty_canvas()`, where the heading and the wrapped body are laid out and both are safe-box-asserted.
    - `server/device_config.py` from plan 06-01 — `RUNWAYS`, `runway_tag_text()`, `runway_empty_heading()`, `normalise_runway_id()`.
    - `server/test_render.py` lines 438-460 — the two existing checks that read `render.TOP_RIGHT_TAG_TEXT` directly and assert its draw position; they must keep passing.
  </read_first>
  <files>server/plane/render.py</files>
  <behavior>
    - `runway_tag_text()` with no argument returns exactly the current top-right tag string, so the default render is unchanged.
    - `runway_tag_text("06-24")` and `runway_tag_text("02-20")` return the strings from the runway registry.
    - `runway_tag_text("unknown")` returns the default runway's tag rather than raising.
    - `build_canvas(None, "empty", runway_id="02-20")` draws that runway's heading and the heading still passes the safe-box assertion, including the longest of the three headings.
    - `build_canvas(flight, "departing", runway_id="06-24")` draws that runway's tag at the same anchor and the tag still passes the within-canvas assertion.
  </behavior>
  <action>
Add two module functions:
- `runway_tag_text(runway_id=device_config.DEFAULT_RUNWAY_ID)` — normalise through `device_config.normalise_runway_id()` then return `device_config.runway_tag_text()`.
- `empty_heading_text(runway_id=...)` — same shape, returning `device_config.runway_empty_heading()`.

Redefine the existing `TOP_RIGHT_TAG_TEXT` and `EMPTY_HEADING_TEXT` module constants as calls to those two functions with the default runway id, so their values are unchanged and the two existing checks in `server/test_render.py` that read them keep working. Add a comment saying new code must call the functions.

Give `draw_top_labels()` a trailing `runway_id=device_config.DEFAULT_RUNWAY_ID` parameter and use `runway_tag_text(runway_id)` for the string it draws and measures. Give `_build_empty_canvas()` the same parameter and use `empty_heading_text(runway_id)`. Thread `runway_id` through `build_canvas()`, `_build_active_canvas()` and `render_panel()` as a keyword parameter defaulting to the same constant.

The empty-state heading is drawn at the largest font in the module and is safe-box-asserted, so a longer runway label could in principle overflow. Do not widen or weaken `_assert_in_safe_box()`. Instead, run the heading through the module's existing `fit_text_size()` helper against the safe-box width before drawing, with the empty-heading font's own size as the initial size and a floor no lower than the module's existing minimum-size discipline — the same treatment long route text already receives. Document that this is why the three registry headings are all short.

Add a `--runway` flag to `build_parser()` whose choices are `device_config.RUNWAY_IDS`, defaulting to the default runway, threaded into `main()`.
  </action>
  <acceptance_criteria>
    - `render.TOP_RIGHT_TAG_TEXT` and `render.EMPTY_HEADING_TEXT` still evaluate to their pre-change values.
    - The two existing top-right-tag checks in `server/test_render.py` pass unmodified.
    - `runway_tag_text("nope")` equals `runway_tag_text()`.
    - `render_panel(None, "empty")` and `render_panel(None, "empty", runway_id="3")` hash-match.
    - `build_canvas(None, "empty", runway_id="06-24")` and `runway_id="02-20"` both complete without an assertion error.
    - `server/.venv/bin/python3 server/plane/render.py --state empty --runway 02-20` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>
  <done>The runway tag and empty heading follow the tracked runway for all three ids, the default render is byte-identical, and the safe-box assertions still hold for every heading.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CFG-05's source-fault badge and widen server/test_render.py</name>
  <read_first>
    - `.planning/seeds/on-device-fault-icon.md` — the whole file, in particular the section scoping CFG-05 to avoid a false-alarm trap: the ordinary Empty state must never trigger the icon, only a genuine upstream failure, because conflating "no plane right now" with "something is broken" would fire it through every quiet period at Orly.
    - `server/plane/render.py` lines 230-266 — `draw_frame()` and `draw_top_labels()`, for the frame inset geometry the badge sits inside and the anchor/bbox-assertion idiom to copy.
    - `server/plane/render.py` lines 605-636 — `_assert_legal_palette()`, whose two assertions (no illegal index anywhere; the state background field is the single most common index) the badge must not break.
    - `server/plane/render.py` lines 100-122 — the font specs, so the badge caption reuses an existing spec rather than introducing a new size.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-02-SUMMARY.md` — the `diagnostics` dict plan 06-02 added to `poll_current_aircraft()`, which is the only legitimate source of the fault flag.
  </read_first>
  <files>server/plane/render.py, server/test_render.py</files>
  <behavior>
    - `render_panel(flight, "arriving", route=..., source_fault=False)` is byte-identical to the same call without the argument.
    - `render_panel(flight, "arriving", route=..., source_fault=True)` differs from the same call with the flag false.
    - The fault badge is drawn on the active canvas and on the empty canvas alike, so it is visible whichever state the panel is in.
    - The badge's caption text is present among the drawn strings when the flag is set and absent when it is not.
    - `_assert_legal_palette()` passes with the badge drawn, in both states and for every theme.
    - The badge's bounding box stays inside the drawn frame.
  </behavior>
  <action>
Add `SOURCE_FAULT_TEXT` and `SOURCE_FAULT_GLYPH_PX` module constants. The caption must be short, English (D-23), and must direct the user to the companion page — 06-UI-SPEC.md establishes that the companion service is where an outage is diagnosed, and CFG-05's whole purpose is to point there. Use the caption `ADS-B source unavailable — check the companion page`. Reuse the existing small tag font spec rather than adding a fifth size.

Add `draw_source_fault_badge(canvas, ink_idx)`: draw a small outlined triangle with an exclamation stroke inside it, using `ImageDraw.polygon` and `ImageDraw.line` with the ink index only — never a new palette index. Place it bottom-centre, inside the frame that `draw_frame()` returns, with the caption to its right on the same baseline. Measure the combined bounding box with `textbbox` and assert it with the module's existing `_assert_within_canvas()` helper before drawing, exactly as `draw_top_labels()` does. The badge must occupy a small enough area that `_assert_legal_palette()`'s background-dominance assertion still holds — state that constraint in the docstring so a later enlargement is a conscious decision.

Give `_build_active_canvas()`, `_build_empty_canvas()`, `build_canvas()` and `render_panel()` a trailing `source_fault=False` keyword parameter, drawing the badge only when it is true. For the empty canvas use the black ink the rest of that canvas uses; for the active canvas use the state's resolved ink index.

Add a `--source-fault` store-true flag to `build_parser()` for manual QA, threaded into `main()`.

Document, in `render_panel()`'s docstring, the rule that makes this requirement correct rather than annoying: the flag is set by `poll_loop.py` **only** when every queried aggregator raised, derived from `detect.poll_current_aircraft()`'s diagnostics dict. It is never derived from "no aircraft was selected", because that is Orly's ordinary quiet state and firing on it is exactly the false-alarm trap the seed document rejects.

Then extend `server/test_render.py` in place — do not restructure it, do not weaken any existing check, and raise `EXPECTED_CHECK_COUNT` to the new exact total. Add one check per behavior bullet in all three tasks of this plan, plus:
- A check that a fault-badged departing render still satisfies `_assert_legal_palette()` for the default theme, asserted by calling `build_canvas()` (which runs the assertion internally) rather than by re-implementing it.
- A check that the badge's drawn strings are absent from a normal render, using the existing text-draw spy idiom already present in this harness for the top-right tag.
- A check that all three runway ids and the single theme id combine without error across both states — a small matrix, so a future theme addition is immediately exercised.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 and prints its `N/N checks pass` line with N equal to the new `EXPECTED_CHECK_COUNT`; N is at least 44 and every one of the 35 pre-existing check names still appears.
    - `render_panel(TEST_FLIGHT, "arriving", route=TEST_ROUTE)` and the same call with the fault flag false hash-match; with the flag true they differ.
    - The badge caption string appears exactly once as a module constant and is not duplicated as a literal anywhere else in `render.py`.
    - `grep -vE '^\s*#' server/plane/render.py | grep -c "fill=IDX_YELLOW\|fill=IDX_RED" == 0` inside the badge function — the badge uses the state ink only, adding no new colour to the panel.
    - `server/.venv/bin/python3 server/plane/render.py --state arriving --callsign AF1380 --source-fault --out /tmp/fault.bin` exits 0 and writes 960000 bytes.
    - `ruff check server/plane/render.py server/test_render.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py && ruff check server/plane/render.py server/test_render.py</automated>
  </verify>
  <done>The fault badge renders on both canvas states without breaking the palette guard rails, is absent by default, is documented as driven only by the all-sources-failed signal, and the harness covers the theme, runway and fault matrix at an exact declared check count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `device_config.json` (companion-service-written) → `render.py` | A user-chosen theme id and runway id cross into the code that decides what the physical frame draws. |
| `detect.py` diagnostics → `source_fault` flag → rendered panel | An inference about third-party API availability changes what a wall-mounted display shows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Tampering | `state_background_index()` / `runway_tag_text()` | high | mitigate | Both normalise their id through `device_config`'s membership test before any dict lookup, so a hand-edited or spoofed config value lands on the default rather than raising inside the render path — a raise here would blank the frame until the next successful cycle. |
| T-06-06-02 | Tampering | fault badge false-positive | medium | mitigate | The badge is driven only by `poll_loop.py`'s all-providers-failed classification derived from `detect.poll_current_aircraft()`'s diagnostics dict, never from an empty selection. The rule is stated in `render_panel()`'s docstring and enforced at the single call site in plan 06-10. |
| T-06-06-03 | Denial of Service | palette guard rails | medium | mitigate | `_assert_legal_palette()` is unchanged and still runs on every canvas; the badge uses the state ink index only and is small enough that the background-dominance assertion still holds, asserted by a check that goes through `build_canvas()` rather than re-implementing the assertion. |
| T-06-06-04 | Denial of Service | empty-state heading overflow | low | mitigate | The runway-dependent heading is passed through the module's existing `fit_text_size()` before drawing, so a longer label shrinks rather than tripping the safe-box assertion and crashing the render. |
| T-06-06-SC | Tampering | package installs | high | accept | No package manager runs. Pillow is already pinned and already imported by this module. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_render.py` exits 0 at the new declared check count with all 35 original checks still named in the output.
- `scripts/run-all-tests.sh` passes all 9 registered harnesses, including `server/test_pipeline_e2e.py` and `server/test_poll_loop.py`, which call `render_panel()` with the old signature and must be unaffected.
- `ruff check .` is clean.
- `git diff --stat` touches only `server/plane/render.py` and `server/test_render.py`.
</verification>

<success_criteria>
- Panel colours and the runway tag are functions of the selected theme and runway, with the default path byte-identical to before.
- Adding Phase 7's validated colour variants is one dict entry in `server/device_config.py` with no change to `render.py`.
- A genuine all-sources outage puts a small alert on the panel pointing at the companion page; an ordinary quiet period does not.
- Every legal palette guard rail still passes for every theme, runway and fault combination.
</success_criteria>

<output>
Create `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-06-SUMMARY.md` when done.
</output>
