---
phase: 06-companion-configuration-web-interface-visual-settings-view-s
plan: 08
type: execute
wave: 3
depends_on: ["06-05"]
files_modified:
  - companion/pages/health_page.py
  - companion/pages/airlines_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-03, CFG-04, CFG-05, CFG-08]
must_haves:
  truths:
    - "The user can see, without SSH, when the device last checked in and when the ADS-B pipeline last ran — as two separately-labelled signals, because they fail independently."
    - "Battery readings are shown as a trend over time, not just a current number, so gradual degradation is visible."
    - "An abnormal condition — a stale poll, a corroboration failure streak, an unusual battery drop — is visually flagged rather than left for the user to spot in raw numbers."
    - "The unresolved-prefix registry is visible read-only, with no in-page action, exactly as the existing manual runbook expects."
    - "Route-resolution performance is visible as a rate over time, beyond the raw unresolved list."
  artifacts:
    - companion/pages/health_page.py
    - companion/pages/airlines_page.py
    - companion/test_status_pages.py
  key_links:
    - "The Health page is CFG-05's landing destination — the panel's fault badge points here, so a real outage must be legible on this page within seconds of arriving."
    - "The corroboration signal is read verbatim from what detect.py already computes; this page performs no new inference (D-15)."
    - "Airlines page reads poll_state.json's unresolved_prefixes through poll_loop.load_poll_state() — it never re-derives enrich.py's logic (D-16)."
---

<objective>
Complete the two read-only status pages: `companion/pages/health_page.py` (CFG-03's device and pipeline health with trend and anomaly flagging, and CFG-05's landing context) and `companion/pages/airlines_page.py` (CFG-04's unresolved-prefix registry and CFG-08's resolution statistics).

Purpose: these two pages are the reason the phase exists as an operational tool rather than just a settings form. When the frame goes wrong, this is where the user finds out why — and CFG-05's on-panel badge sends them straight here.
Output: two complete pages and their shared harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-CONTEXT.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-RESEARCH.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-UI-SPEC.md
@.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-05-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new, not pre-existing — do not flag as drift):

`companion/pages/health_page.py`: `render()` (completed), `STALE_DEVICE_WARN_S`, `STALE_DEVICE_ERROR_S`, `STALE_PIPELINE_WARN_S`, `STALE_PIPELINE_ERROR_S`, `BATTERY_DROP_WARN_MV`, `BATTERY_TREND_LIMIT`, `staleness_status()`, `battery_status()`, `corroboration_status()`, `battery_trend_rows()`, `battery_sparkline_svg()`, `collect_anomalies()`.

`companion/pages/airlines_page.py`: `render()` (completed), `RESOLUTION_WINDOW_DAYS`, `unresolved_rows()`, `resolution_stats()`, `coverage_status()`.

New file paths: `companion/test_status_pages.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the Health page — two independent freshness signals, battery trend, corroboration, and anomaly flagging</name>
  <read_first>
    - `companion/pages/health_page.py` — the stub from plan 06-05 and its documented `ctx` contract.
    - `companion/layout.py` — `status_dot()`, `anomaly_banner()`, `data_table()`, `empty_state()`, `escape_html()`. All markup comes from here.
    - `server/history_db.py` from plan 06-01 — `recent_device_health()`, `latest_device_health()`, `corroboration_counts()`, `recent_runway_events()`, `get_meta()`, and the meta key constants for the last pipeline run, last detection and source-fault flag.
    - `server/plane/detect.py` lines 486-578 — `poll_current_aircraft()`'s three-state corroboration outcome (agreement, single-source unknown, disagreement returns nothing) so the page describes what the flag actually means rather than treating unknown as failure.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-UI-SPEC.md` — the Color section's Status colours table, the Component Patterns rows for the status dot, the anomaly banner and the trend/chart, and the health-unavailable error copy.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-RESEARCH.md` — Open Question 2's recommendation to show both freshness signals with distinct labels.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-CONTEXT.md` — D-12, D-13, D-14, D-15.
  </read_first>
  <files>companion/pages/health_page.py</files>
  <behavior>
    - The page renders two separately-labelled freshness rows — one for the device's last check-in, one for the ADS-B pipeline's last run — each with its own status dot.
    - A device check-in older than the warning threshold shows the warning status; older than the error threshold shows the error status; recent shows the healthy status.
    - The two thresholds are independent: a healthy pipeline and a stale device produce one warning row and one healthy row, not a single blended verdict.
    - The battery section renders a trend of recent readings, not only the latest value, and shows the good-news empty state when no reading exists yet.
    - A drop between consecutive readings larger than the configured threshold raises the battery anomaly.
    - The corroboration section reports the three states distinctly and never presents the unknown single-source state as a failure.
    - When any anomaly is present the anomaly banner appears at the top of the page; when none is present no banner is rendered at all.
    - A database that is missing or unreadable renders the health-unavailable copy instead of raising.
  </behavior>
  <action>
Complete `companion/pages/health_page.py`. It imports `server.history_db`, `server.poll_loop` (for `load_poll_state`) and `companion.layout` only.

**Two freshness signals, separately labelled.** 06-RESEARCH.md's Open Question 2 established that CFG-03's "last successful poll" is ambiguous between two genuinely different signals with different failure modes and different data sources. Render both, with the labels `Device last checked in` and `ADS-B pipeline last ran`. The device signal comes from `latest_device_health()`'s timestamp (populated by the Caddy log tailer); the pipeline signal comes from `history_db.get_meta()`'s last-pipeline-run key (written by `poll_loop.py`). Each renders a status dot plus the absolute timestamp in the monospace style plus a relative age.

`staleness_status(age_seconds, warn_s, error_s)`: return one of the three status names. Define four module constants — a warning and an error threshold for each signal — and document how each was chosen rather than leaving them as magic numbers. Ground them in real, recorded project values: the server's ADS-B cadence is 30 seconds (`poll_loop.POLL_INTERVAL_S` and the systemd timer), so a pipeline gap of a few minutes is already abnormal; the device's own sleep interval is currently 30 seconds but is explicitly a bring-up default that STATE.md records as expected to lengthen substantially once Phase 5's battery data lands, so the device thresholds must be generous and must be stated as tunable, with a comment pointing at the environment variable that sets the device sleep interval. Do not encode a device threshold so tight that lengthening the sleep interval turns the page permanently red.

**Battery trend (D-12/D-13).** `battery_trend_rows(conn)` reads the most recent readings up to `BATTERY_TREND_LIMIT` and returns rows of timestamp and millivolts. Render them with `layout.data_table()` using the monospace column for both. Additionally render a dependency-free inline SVG sparkline built server-side by `battery_sparkline_svg(rows)` — no charting library, consistent with the zero-new-dependency constraint. Keep the SVG small and purely presentational: fixed viewBox, one polyline, no text, no external reference. When there are fewer than two readings, omit the sparkline and render only the table or the empty state. Note in the docstring that D-13 keeps history forever but the page deliberately shows only recent readings for readability, which is a display choice and not a retention policy.

`battery_status(rows)`: compare consecutive readings and return the warning or error status when a drop exceeds `BATTERY_DROP_WARN_MV`. Pick that threshold from the project's own recorded battery protocol rather than inventing one — read `hardware/BATTERY-RUN.md` if present for the pre-registered thresholds, and if it is absent state plainly in a comment that the threshold is provisional pending Phase 5's measured discharge curve, because Phase 5 has not produced one yet. Do not present a provisional number as measured.

**Corroboration (D-15).** `corroboration_status(counts)` reads `history_db.corroboration_counts()` over a recent window and renders the three outcomes with distinct labels and distinct status colours: agreement is healthy, the single-source unknown state is informational and must **not** be shown as an error, and a run of the disagreement state is a warning. Add a short explanatory sentence on the page saying what each means, sourced from `detect.poll_current_aircraft()`'s own documented semantics — this page performs no new inference, it exposes an already-computed signal, which is exactly what D-15 asks for.

**CFG-05 landing context.** When `history_db.get_meta()`'s source-fault key is set, render a prominent block at the top explaining that the panel's alert badge refers to this condition and naming which providers failed. This closes the loop the on-panel badge opens: a user who walks to their phone because the frame showed an alert must find the explanation here, not another vague indicator.

**Anomaly flagging (D-14).** `collect_anomalies(...)` returns a list of short strings for every non-healthy condition found. When the list is non-empty, `render()` puts `layout.anomaly_banner()` at the top carrying 06-UI-SPEC.md's anomaly-banner copy; when empty it renders no banner at all — the absence of a banner is the all-clear, which keeps the page uncluttered per D-21.

Wrap every database access in a try that catches the database and operating-system error classes and renders 06-UI-SPEC.md's health-unavailable copy for that section, so a locked or missing database degrades one section rather than faulting the page.
  </action>
  <acceptance_criteria>
    - The rendered page contains both freshness labels as distinct strings.
    - With a seeded database where the device timestamp is old and the pipeline timestamp is fresh, the output contains one warning-or-error status class and one healthy status class.
    - With no battery rows, the output contains the empty-state copy and no `<svg`.
    - With three battery rows, the output contains a `<svg` with exactly one `<polyline`.
    - The sparkline SVG contains no `url(`, no `<image`, and no `<script`.
    - With a corroboration count of unknown-state entries only, the output contains no error status class.
    - With no anomalies, `grep`-ing the output for the anomaly-banner copy finds nothing; with a stale pipeline it finds it exactly once.
    - Pointing the page at a directory containing no database renders the health-unavailable copy and does not raise.
    - `grep -c "import html" companion/pages/health_page.py == 0`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>The Health page shows two independent freshness signals, a battery trend with a dependency-free sparkline, the three corroboration states without mislabelling the unknown one, CFG-05's landing explanation, and D-14 anomaly flagging that stays silent when everything is fine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the Airlines page — read-only unresolved-prefix registry and resolution statistics</name>
  <read_first>
    - `companion/pages/airlines_page.py` — the stub from plan 06-05, whose docstring records the D-16 read-only constraint.
    - `server/plane/enrich.py` — `note_unresolved_prefix()`'s docstring (lines 708-745) for the exact registry entry shape (count, first-seen, last-seen, example callsign) and `resolve_route()`'s docstring (lines 617-640) for the four legal source values and what each one means.
    - `server/poll_loop.py` lines 86-95 and 244-250 — `load_poll_state()` and the block that writes `unresolved_prefixes`, so this page reads through the existing loader rather than opening the file itself.
    - `server/history_db.py` from plan 06-01 — `route_source_counts()`.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-UI-SPEC.md` — the good-news empty state copy for coverage gaps, and the Data table component pattern including which columns take the monospace treatment.
    - `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-CONTEXT.md` — D-16, D-19.
  </read_first>
  <files>companion/pages/airlines_page.py</files>
  <behavior>
    - With an empty registry the page renders the good-news empty state copy and no table.
    - With registry entries the page renders one row per prefix showing the prefix, the count, both timestamps and the example callsign, sorted by count descending then prefix ascending so the order is deterministic.
    - A registry entry whose value is malformed — not a dict, or with a non-integer count — is skipped rather than crashing the page.
    - A prefix or example callsign containing markup characters is rendered escaped.
    - The resolution statistics section shows one row per route source over the configured window, with a resolved percentage computed from the four documented source categories.
    - With no history rows the statistics section renders its own empty state rather than dividing by zero.
    - The page contains no form and no button anywhere.
  </behavior>
  <action>
Complete `companion/pages/airlines_page.py`.

`unresolved_rows(state_dir)`: call `server.poll_loop.load_poll_state()` and read the `unresolved_prefixes` key. Never open `poll_state.json` directly and never re-derive `enrich.py`'s prefix logic — D-16 scopes this page to displaying an already-computed registry, and duplicating the derivation would create a second source of truth that could disagree with the runbook the user already follows. Guard each entry with an isinstance check and skip malformed ones; the registry is written by production code but is also hand-editable, so the page must tolerate a bad edit.

Render the rows with `layout.data_table()`, monospace on the prefix and example-callsign columns per 06-UI-SPEC.md's Typography rules. Above the table render `layout.status_dot()` summarising coverage: healthy when the registry is empty, warning when it has entries. When empty, render the good-news empty state copy verbatim.

Add a short explanatory line stating that this list is read-only by design and that resolving a prefix is a manual step done elsewhere, referencing the existing runbook rather than restating it. Explicitly do not add a resolve, dismiss, or clear control — D-16 rejected in-page actions, and the stub's docstring already records that.

`resolution_stats(conn, window_days)`: call `history_db.route_source_counts()` with a `since` bound of `RESOLUTION_WINDOW_DAYS` before now. Render one row per source using the four category names `enrich.resolve_route()` documents, with a short plain-English gloss for each so the page is readable without the source code: a fresh lookup that resolved a full route, a cached route that spared a request, an airline identified from the static prefix table with no route available, and a full miss. Compute a resolved percentage as the share of entries that produced any usable airline or route — that is, everything except the miss category — and render it as a headline figure. Note in a comment that Phase 2 measured this at roughly 52.6 percent against real traffic, so a figure in that region is expected rather than a defect; that context is what turns a bare number into a signal.

Guard the whole statistics section against a missing or unreadable database, rendering the health-unavailable copy for that section only. Guard against a zero total before computing the percentage.
  </action>
  <acceptance_criteria>
    - `grep -c "<form" companion/pages/airlines_page.py == 0` and `grep -c "<button" companion/pages/airlines_page.py == 0`.
    - `grep -c "open(" companion/pages/airlines_page.py == 0` — the registry is read through `load_poll_state()`.
    - `grep -c "note_unresolved_prefix\|_AIRLINE_PREFIX_SHAPE_RE" companion/pages/airlines_page.py == 0` — no re-derivation of enrich logic.
    - With an empty registry the output contains the good-news empty-state copy and no `<table`.
    - With two entries of unequal count the higher count appears first in the output.
    - A registry entry whose value is a string instead of a dict does not raise and does not appear as a row.
    - A prefix rendered from a value containing angle brackets appears escaped in the output.
    - With zero history rows the statistics section renders without a division error.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>The Airlines page displays the existing unresolved-prefix registry read-only with deterministic ordering and malformed-entry tolerance, plus a windowed resolution-rate breakdown over the four documented source categories, with no in-page action of any kind.</done>
</task>

<task type="auto">
  <name>Task 3: Create companion/test_status_pages.py</name>
  <read_first>
    - `companion/test_companion_app.py` — the harness skeleton and the subprocess launcher from plan 06-05.
    - `server/history_db.py` — the writer functions used to seed fixtures.
    - `server/poll_loop.py` — `save_poll_state()`, used to seed a registry fixture.
    - The two completed page modules from Tasks 1 and 2.
  </read_first>
  <files>companion/test_status_pages.py</files>
  <action>
Create `companion/test_status_pages.py` following the project's harness skeleton (shebang, docstring naming coverage and the invocation `server/.venv/bin/python3 companion/test_status_pages.py`, stdlib-only imports plus the repo-root `sys.path` bootstrap, `EXPECTED_CHECK_COUNT`, a `main()` with a local `check()` closure, a `status-pages: N/M checks pass` summary, `sys.exit(main())`). No pytest.

Seed every fixture programmatically into a temporary state directory using `history_db`'s own writer functions and `poll_loop.save_poll_state()` — never a committed fixture file, so the harness cannot drift from the schema.

Include one check per behavior bullet from Tasks 1 and 2, plus:
- A check that a battery reading sequence with a large drop between two consecutive rows produces the battery anomaly, and that a gentle monotonic decline does not.
- A check that an unresolved-prefix entry whose example callsign contains a script tag renders escaped, asserting the raw tag does not appear in the output.
- A check that with the source-fault meta key set, the Health page's output contains the CFG-05 landing explanation, and that with it unset the explanation is absent.
- One end-to-end check launching `companion/app.py`, logging in, and fetching both tab routes with a seeded database, asserting each returns 200 and contains its own page heading — proving the router and both page modules agree.

Set `EXPECTED_CHECK_COUNT` to the exact number of `check()` calls.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and prints `status-pages: N/N checks pass` with N equal to `EXPECTED_CHECK_COUNT`.
    - The escaping check asserts the absence of an unescaped script tag in the rendered output.
    - Deliberately making `staleness_status()` always return the healthy value makes the stale-pipeline check fail; the change is reverted before commit.
    - No stray subprocess remains after the run and the temporary directory is removed.
    - `grep -vE '^\s*#' companion/test_status_pages.py | grep -c "import pytest" == 0`.
    - `ruff check companion/` is clean.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && ruff check companion/</automated>
  </verify>
  <done>The harness covers both pages' happy paths, empty states, malformed-input tolerance, escaping, anomaly detection, the CFG-05 landing block, and one HTTP round trip per page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADS-B and adsbdb feed values stored in `history.db` and `poll_state.json` → rendered HTML | Callsigns, airline names, aircraft types and unresolved ICAO prefixes all originate from untrusted external feeds and are displayed inside the authenticated admin page. |
| hand-editable `poll_state.json` registry → page rendering | The registry is documented as a manually-inspectable file, so a malformed hand edit must not fault the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08-01 | Tampering | stored XSS via feed-sourced callsigns, airline names and prefixes | high | mitigate | Every value reaches HTML only through `layout.data_table()` and `layout.escape_html()`; neither page imports `html` or builds markup by hand. A dedicated check asserts a script-tag-shaped example callsign renders escaped. This is Pitfall 2's exact scenario and these two pages are its largest surface. |
| T-06-08-02 | Denial of Service | malformed registry entry or missing database | medium | mitigate | Every entry is isinstance-guarded and skipped when malformed; every database access is wrapped so a locked or absent database degrades one section to the health-unavailable copy rather than faulting the page. |
| T-06-08-03 | Information Disclosure | health page content | medium | accept | The page exposes battery voltages, poll timings and aggregator reliability — operational data about a single household device, behind the D-02 whole-site password gate. No credential, token or hostname is rendered. |
| T-06-08-04 | Repudiation | mislabelled corroboration state | medium | mitigate | The single-source unknown state is rendered as informational, never as an error, matching `detect.poll_current_aircraft()`'s own documented semantics. Presenting unknown as failure would train the user to ignore a real disagreement, which is the same false-alarm trap CFG-05 is scoped to avoid. |
| T-06-08-05 | Repudiation | provisional battery threshold presented as measured | medium | mitigate | The battery-drop threshold is sourced from the project's pre-registered protocol where one exists and is otherwise labelled provisional in a comment, because Phase 5 has not yet produced a measured discharge curve. |
| T-06-08-SC | Tampering | package installs | high | accept | No package manager runs. The sparkline is hand-built inline SVG specifically to avoid adding a charting dependency. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at its declared check count.
- `server/.venv/bin/python3 companion/test_companion_app.py` still passes unchanged.
- `ruff check .` is clean.
- `scripts/run-all-tests.sh` passes all 9 registered harnesses.
- `git diff --stat` touches only the two page modules and the new harness.
</verification>

<success_criteria>
- Device check-in freshness and ADS-B pipeline freshness are visible as two separately-labelled signals with their own thresholds.
- Battery readings render as a trend with a dependency-free sparkline, and an abnormal drop is flagged.
- The three corroboration states are distinguishable and the unknown state is not mislabelled as failure.
- A real source outage is explained on the Health page, closing the loop the on-panel badge opens.
- The unresolved-prefix registry is visible read-only with no in-page action, and route-resolution performance is visible as a windowed rate.
</success_criteria>

<output>
Create `.planning/phases/06-companion-configuration-web-interface-visual-settings-view-s/06-08-SUMMARY.md` when done.
</output>
