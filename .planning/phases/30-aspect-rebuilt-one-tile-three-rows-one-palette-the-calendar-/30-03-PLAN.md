---
phase: 30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-
plan: 03
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - companion/test_config_page.py
  - companion/test_browser_ux.py
  - companion/test_companion_app.py
autonomous: true
requirements: [CFG-85]
user_setup: []

must_haves:
  truths:
    - "Every check that pins a mechanism CFG-85 deletes is retired, and every PROPERTY those checks proved is either re-homed here or carries a named replacement check and the plan number that owes it."
    - "A machine guard fails the harness if any ledger row's replacement check name is neither present in the file nor listed as owed by a named later plan — so a forgotten re-pin is a red build, not a silent coverage loss."
    - "`_NO_JS_CONTROL_REGISTRY` no longer names the theme carousel's pagers, and its own contract machine is still proven non-vacuous by its four fixtures."
    - "The scripts-blocked-save proof for `theme` and `theme_arriving` survives this plan — narrowed to its markup-agnostic half (operate → submit → read back from disk, both languages, 360px), never deleted."
    - "All three harnesses are green at counts re-derived by running, at every commit in this plan."
  artifacts:
    - "companion/test_config_page.py :: _ASPECT_REPIN_LEDGER"
    - "companion/test_config_page.py :: _every_aspect_repin_ledger_row_names_a_live_or_owed_replacement()"
    - "companion/test_browser_ux.py :: _ASPECT_REPIN_LEDGER"
    - ".planning/phases/30-…/30-03-SUMMARY.md :: the derived affected-check inventory"
  key_links:
    - "`_no_js_control_violation()` (test_companion_app.py:7083) loops the registry only — there is NO completeness guard demanding a row per gated element, which is why deleting the pagers row while the pagers still ship keeps the harness green."
    - "`_persist_without_js()` is the markup-agnostic half of the two scripts-blocked-save checks; `_STRIP_PROBE` is the markup-specific half. Keep the first, retire the second."
    - "`EXPECTED_CHECK_COUNT` in all three files must be re-derived by RUNNING, per ROADMAP's own carried discipline for this phase."
---

<objective>
Remove the pins before removing the thing they pin, and make the one-wave coverage gap that creates
a machine-tracked debt rather than a hope.

Twenty-four check functions in `companion/test_config_page.py` and thirteen blocks in
`companion/test_browser_ux.py` reference identifiers CFG-85 deletes (`frame-colours*`,
`theme-carousel*`, `colour_usage`, `THEME_CAROUSEL_*`, `FRAME_COLOURS_*`, `usage-panel`). They
cannot all be rewritten in the same commit that changes the markup — that commit would be the
whole phase. And they cannot be left in place, because the markup change makes them fail, which
would put the next three plans behind a wall of expected red and destroy the sampling contract
30-VALIDATION.md depends on.

So: retire them here, in one deliberate, ledgered move, while the old markup still ships and every
harness is green. Each retired check's PROPERTY gets a row in a `_ASPECT_REPIN_LEDGER` tuple naming
the replacement check and the plan that owes it, and a guard check fails the harness if a row's
replacement is neither present nor owed. The coverage gap is one wave wide, named, and impossible
to forget.

Purpose: keep every commit in this phase green without losing a single proven property.
Output: three harnesses lighter and still green; two ledgers; one guard; a derived inventory in the
SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-RESEARCH.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-VALIDATION.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-02-SUMMARY.md
</context>

<derivation_first>
**Derive the inventory, do not trust this plan's list.** Run this first and record its output
verbatim in the SUMMARY — the routing table below is a cross-check on your derivation, not a
substitute for it. If your derivation and the table disagree, the derivation wins and the
disagreement goes in the SUMMARY.

```
server/.venv/bin/python3 - <<'PY'
import re
PAT = re.compile(r'frame[-_]colours|FRAME_COLOURS|theme[-_]carousel|THEME_CAROUSEL|colour_usage|COLOUR_USAGE|usage-panel')
for path in ("companion/test_config_page.py", "companion/test_browser_ux.py",
             "companion/test_companion_app.py"):
    lines = open(path).read().split("\n")
    cur, curline, order, seen = None, 0, [], {}
    for i, ln in enumerate(lines, 1):
        m = re.match(r'^\s*def (_\w+)\(', ln)
        if m:
            cur, curline = m.group(1), i
        if PAT.search(ln) and not ln.strip().startswith('#'):
            key = (cur, curline)
            seen.setdefault(key, []) or order.append(key) if key not in seen else None
            seen.setdefault(key, []).append(i)
    print("===", path, len(order), "blocks")
    for fn, dl in order:
        print("   %-70s def@%-6d hits=%d" % (fn, dl, len(seen[(fn, dl)])))
PY
```

Expected at the SHA this plan starts from: **24** blocks in `test_config_page.py`, **13** in
`test_browser_ux.py`, **1** (attributed to `None`, i.e. the module-level
`_NO_JS_CONTROL_REGISTRY`) in `test_companion_app.py`.

**One known mis-attribution in that script, do not be misled by it.** In `test_browser_ux.py` the
script attributes the shared selector block at lines 13391-13500 (`THEME_STRIP_SEL`,
`THEME_PAGERS_SEL`, `THEME_DETAILS_SEL`, `THEME_PREVIEW_SEL`, `_STRIP_PROBE`) to
`_the_slider_meets_its_floors_at_360px_in_both_themes`, because that block sits after the slider
check's own registration and the script walks backwards to the nearest `def`. **The slider check
itself is unaffected by this phase** — do not touch it. The block is a shared four-check selector
set and it is retired by Task 3.
</derivation_first>

<routing_table>
Cross-check for your derivation. "Retire" = delete the check outright, its property is genuinely
gone with the mechanism. "Ledger → NN" = delete the check now, add a `_ASPECT_REPIN_LEDGER` row
naming its replacement and plan NN. "Re-home here" = the property survives and is re-proven in
THIS plan against the still-shipping markup, expressed so it survives the rebuild too.

**`companion/test_config_page.py`**

| Check | Disposition |
|---|---|
| `_the_departures_grid_is_the_one_renderer_presented_as_a_strip` | Retire — "presented as a strip" is the thing CFG-85 deletes |
| `_the_carousel_dots_are_real_colours_and_the_strip_rules_are_declared` | Retire — no dots row, no strip rules |
| `_the_full_grid_sits_behind_a_native_details_and_the_pagers_behind_the_gate` | Retire, **but re-home here** its one surviving property: the whole Display page carries exactly `len(THEME_IDS)` radios named `theme` (ONE set, so the page can never show one setting in two disagreeing places). Re-express it with no reference to a strip, a pager or a gate, so the rebuild does not break it again. |
| `_the_rendered_settings_page_carries_no_duplicate_id` | **Keep unchanged** — page-wide id uniqueness is generic and must hold across the rebuild. Only its registered check-name string mentions the carousel; leave even that alone this plan (plan 30-05 may reword it). |
| `_frame_colours_card_covers_every_registered_theme_with_own_id_and_label` | Ledger → 30-05 (`_aspect_card_covers_every_registered_theme_with_own_id_and_label`) |
| `_frame_colours_card_default_selects_exactly_the_white_departures_option` | Ledger → 30-05 (`_aspect_card_default_selects_exactly_the_white_departures_option`) |
| `_frame_colours_arrivals_grid_carries_leading_chip_no_checkbox` | Ledger → 30-05 (`_aspect_arrivals_row_carries_leading_option_no_checkbox`) |
| `_frame_colours_arrivals_override_preselects_the_override_not_same_as_departures` | Ledger → 30-05 (same name, `_aspect_` prefix) |
| `_frame_colours_card_full_shape_checklist` | Ledger → 30-05 (`_aspect_card_full_shape_checklist`) |
| `_rules_section_renders_inside_frame_colours_after_form` | Ledger → 30-05 (`_rules_row_renders_inside_aspect_after_form`) |
| `_frame_colours_rules_row_label_locked_verbatim` | Ledger → 30-05 (`_aspect_rules_row_label_locked_verbatim`) |
| `_rules_panel_segment` (helper) / `_rules_copy_appears_escaped_verbatim` | Ledger → 30-05 — the helper's `COLOUR_USAGE_PANEL_TARGET_ATTR` locator becomes the row's `data-usage` locator |
| `_calendar_theme_chip_grid_exactly_one_compact_radiogroup_populated_in_order` | Ledger → 30-05 (`_aspect_calendar_row_palette_populated_in_order`) |
| `_display_render_has_exactly_one_live_preview_figure_eager_with_dimensions` | Ledger → 30-05 — one figure, eager, with dimensions; only its wrapper class changes |
| `_scoped_render_carries_hidden_fields_and_omits_other_groups` | Ledger → 30-05 — a one-symbol swap |
| `_english_display_render_still_carries_every_pinned_english_string` | Ledger → 30-05 — the pinned set loses two captions and one heading, gains one |
| `_title_form_inventory_classifies_every_h2_text_heading_on_both_routes` | Ledger → 30-06 — the h2 inventory changes twice (once here, once when the Calendar h2 goes) |
| `_no_card_builder_function_ever_calls_section_intro_html` | Ledger → 30-06 — the builder name list loses two and gains one |
| `_display_h2_order_matches_d12_after_calendar_placement_fix` | Ledger → 30-06 |
| `_calendar_placement_after_display_form_close_with_dirty_attr` | Ledger → 30-06 |
| `_calendar_group_no_inline_js_and_chip_grid_cross_submits_form` | Ledger → 30-06 |
| `_destructive_disconnect_is_secondary_and_selection_is_free_and_focusable` | Ledger → 30-07 — it carries TWO properties: the Disconnect button's secondary treatment (30-06's subject) and an `input:checked + .frame-colours__row` CSS-selector assertion (30-07's). Ledger it ONCE to 30-07 and say in the ledger row's own comment that 30-06 owes the disconnect half — the replacement check must re-prove both. |
| `_segmented_control_resets_the_global_label_margin_and_the_legend_leaves_the_serif` | Ledger → 30-07 — the `.frame-colours__panel-legend` serif-leak assertion has no legend to point at once the usage panels go |

**`companion/test_browser_ux.py`**

| Check | Disposition |
|---|---|
| `_display_page_height` (helper, line 2870) | **Do not touch.** Its `FRAME_COLOURS_HEADING_ID` guard is updated by plan **30-04**, in the same commit as the heading rename (30-RESEARCH.md Pitfall 1). Touching it here would leave the instrument pointing at a constant that still exists. |
| `_displays_page_height_is_recorded_at_both_phone_widths` | **Do not touch** — plan 30-04 rewords, plan 30-08 extends |
| `_the_theme_still_saves_with_scripts_blocked_through_the_carousel` | **Narrow here**, do not delete: keep the `_persist_without_js()` half (target theme reaches disk in both languages at 360px, restore leg puts the old value back, the neighbour setting is unchanged), delete the trailing `_STRIP_PROBE` block. Rename to `_the_theme_still_saves_with_scripts_blocked`. Ledger → 30-08 the extra property the deleted half proved (every registry theme is server-rendered and owes nothing to a script) |
| `_arrivals_still_saves_with_scripts_blocked_through_its_own_carousel` | **Narrow here**, same treatment, rename to `_arrivals_still_saves_with_scripts_blocked` |
| `_each_carousels_own_disclosure_toggles_only_its_own_strip` | Retire — no per-carousel disclosure exists in the accordion |
| `_arrivals_and_calendar_keep_the_focused_chip_in_view_when_keyed` | Retire — scroll-into-view has no meaning in a wrapping grid |
| `_keying_the_strip_selects_scrolls_into_view_and_moves_the_preview` | Retire, **ledger → 30-08** the surviving half: arrow-keying the radiogroup still moves the preview |
| `_scrolling_a_strip_moves_its_own_preview_to_the_centered_chip_and_selects_nothing` | Retire, **ledger → 30-08** the surviving property, re-keyed: preview-is-not-selection (hover/focus previews a theme and never writes a radio, a change event or a form value) |
| `_the_carousel_meets_its_floors_at_360px_in_both_themes` | Retire, **ledger → 30-08** (`_the_palette_meets_its_floors_at_360px_in_both_themes`) — the replacement measures the four controls in 30-UI-SPEC.md's Touch Targets table by real hit-testing in their own containers |
| `_selecting_a_theme_chip_answers_and_moves_no_layout_box` | Ledger → 30-08 — re-points at `.palette-chip` and the open row |
| `_the_live_preview_crossfade_settles_correct_through_its_own_listener` | Ledger → 30-08 — same |
| `_cancel_restores_the_field_the_preview_and_the_dial_from_the_resulting_dom` | Ledger → 30-08 — this is the ONE check proving `window.SkyPaneLivePreview.refresh()` still works after `dirty-state.js`'s `form.reset()`; it must come back |
| `_images_hold_their_place_before_they_arrive` | Ledger → 30-08 with a recorded decision to make: its `/display` surface is `.theme-chip` + `.theme-chip__preview`, and after the rebuild the only surviving `.theme-chip__preview` on Display is inside the rule-add form's nested, closed-by-default disclosure. 30-08 must EITHER open that disclosure before measuring (preferred — it keeps the surface) OR drop the `/display` row from the `surfaces` tuple with the reason written at the tuple. It must not silently keep a row that now measures a laid-out-zero box. |
| `_the_no_js_floor_holds_for_both_settings_pages` | Ledger → 30-08 — see `<no_js_floor_note>` below; its third assertion's PROPERTY changes shape and the replacement must be stronger, not weaker |
| the shared selector block (lines 13391-13500: `THEME_STRIP_SEL`/`THEME_PAGERS_SEL`/`THEME_DETAILS_SEL`/`_STRIP_PROBE`) | Retire with its last consumer. `THEME_PREVIEW_SEL` (`.theme-live-preview__image`) SURVIVES — keep it, the live preview element is unchanged by this phase |

**`companion/test_companion_app.py`**

| Item | Disposition |
|---|---|
| `_NO_JS_CONTROL_REGISTRY`'s `"the theme carousel's two pagers (D5)"` row (lines 995-1021, comment block included) | Delete outright — 30-RESEARCH.md Pitfall 2. Do NOT repoint it. |
</routing_table>

<no_js_floor_note>
**A requirement-text vs. approved-design discrepancy the executor must NOT paper over.**

CFG-85's own text includes "every row open with scripts blocked", and ROADMAP's floors paragraph
repeats it. The developer-approved sketch and 30-UI-SPEC.md ship a native
`<details name="aspect-rows">` accordion with row 1 `open` and rows 2-4 closed, and a grouped
`<details>` set is mutually exclusive in the browser by construction — so "every row open" is not
achievable while keeping the zero-script accordion the developer signed off on (30-UI-SPEC.md
Floor #1, and §0's rule that the approved sketch's markup wins over ROADMAP prose).

The property that CFG-85 is actually protecting — no control is unreachable or unsaveable with
scripts blocked — is met, and met more strongly than by a visible stack:

1. A closed `<details>`'s form controls remain in the DOM and DO participate in form submission.
2. `<summary>` is natively activatable by pointer and keyboard with scripts blocked, so a visitor
   can open any row themselves.
3. The preview is server-rendered for the SAVED theme in either case.

Do NOT silently re-word the retired assertion into something weaker. The ledger row for
`_the_no_js_floor_holds_for_both_settings_pages` must state this discrepancy in its own comment, and
30-08's replacement must prove all three points above, with scripts blocked, at 360px, in both
languages, with the verdict read back from disk. Record the discrepancy in this plan's SUMMARY as
well, flagged for the developer, so the phase's own verification meets the argument rather than
finding a quietly narrowed claim.
</no_js_floor_note>

<tasks>

<task type="auto">
  <name>Task 1: The ledger, its guard, and test_config_page.py's retirements</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - `companion/test_config_page.py` lines 1090-1120 — the `EXPECTED_CHECK_COUNT` history block and
      its final pin (274). Append a new pinned value with a one-line reason, matching the file's own
      established comment convention; never edit an earlier line in that history.
    - `companion/test_companion_app.py` lines 930-1052 — `_NO_JS_CONTROL_REGISTRY`'s shape: a
      module-level tuple of dicts with a per-row `"plan"` key and a comment block above each row
      arguing it. Mirror that shape for `_ASPECT_REPIN_LEDGER`.
    - `companion/test_config_page.py` — the `_no_card_builder_function_ever_calls_section_intro_html`
      check (around line 9390): the established idiom for a check that reads its own project's
      Python source. The ledger guard reads THIS file's source the same way.
    - Every check named in the routing table above, at the line numbers your derivation reports.
  </read_first>
  <action>
    Add a module-level `_ASPECT_REPIN_LEDGER` tuple near the `EXPECTED_CHECK_COUNT` block, one dict
    per retired check:

      `{"retired": "<old check function name>", "property": "<one sentence naming the PROPERTY, not
      the markup>", "replacement": "<new check function name>", "owed_by": "30-05", "why": "<one
      sentence: which CFG-85 mechanism removal makes the old shape unassertable>"}`

    Write a header comment above the tuple stating what it is for, that it is EXPECTED TO EMPTY (the
    same self-documenting convention `ASPECT_CAPTION_EXEMPTIONS` already uses in
    `companion/pages/config_page.py`), and which plans owe which rows.

    Add `_every_aspect_repin_ledger_row_names_a_live_or_owed_replacement()` and register it with
    `check(...)`. It reads THIS file's own source (`__file__`) and, for each ledger row:
      - if `owed_by` names a plan, asserts the row is well-formed (all five keys present, non-empty,
        `property` at least eight words so it names a property rather than restating a selector);
      - if `owed_by` is empty/None, asserts `re.search(r"def %s\(" % row["replacement"], own_source)`
        matches — i.e. the replacement genuinely exists in this file.
    It must also assert the ledger is non-empty, so it cannot pass vacuously, and that no `retired`
    name still has a `def` in this file — a retired check that was left behind is the other failure
    shape.

    **Make the guard non-vacuous before committing it.** Run it three times against deliberately
    wrong ledgers and quote all three failure messages in the SUMMARY: (a) a row whose `owed_by` is
    cleared while its `replacement` does not exist → must FAIL; (b) a row whose `retired` check is
    still present in the file → must FAIL; (c) an empty ledger → must FAIL. Revert each with
    `git checkout-index -f -- companion/test_config_page.py` and clear `__pycache__`.

    Then apply the routing table's `test_config_page.py` dispositions:
      - delete the three "Retire" checks and their `check(...)` registrations and their comment
        blocks (a comment arguing a deleted check is a comment about nothing);
      - **re-home here**, as a new check, the one surviving property from
        `_the_full_grid_sits_behind_a_native_details_and_the_pagers_behind_the_gate`: a rendered
        Display page carries exactly `len(device_config.THEME_IDS)` radios named `theme`, computed
        from the registry at check time. Write it with no reference to a strip, a pager, a gate, a
        disclosure or a dots row, so plan 30-04's rebuild leaves it green — that is the point of
        re-homing it now rather than ledgering it;
      - delete every "Ledger" check and add its ledger row.

    Re-derive `EXPECTED_CHECK_COUNT` by RUNNING the harness and reading its tally. The count will go
    DOWN substantially and that is correct; ROADMAP's carried discipline for this phase says re-derive
    by running, never hand-increment, and it applies to decrements identically.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_config_page.py").read()
ns = {}
m = re.search(r"^_ASPECT_REPIN_LEDGER = \((.*?)^\)", src, re.S | re.M)
assert m, "no _ASPECT_REPIN_LEDGER tuple"
exec("_ASPECT_REPIN_LEDGER = (" + m.group(1) + ")", ns)
led = ns["_ASPECT_REPIN_LEDGER"]
assert len(led) >= 15, "expected at least 15 ledgered rows, got %d" % len(led)
for r in led:
    for k in ("retired", "property", "replacement", "owed_by", "why"):
        assert k in r and r[k], (r.get("retired"), k)
    assert len(r["property"].split()) >= 8, r["retired"]
    assert not re.search(r"def %s\(" % re.escape(r["retired"]), src), \
        "retired check still defined: " + r["retired"]
    assert r["owed_by"] in ("30-05", "30-06", "30-07", "30-08"), (r["retired"], r["owed_by"])
assert re.search(r"def _every_aspect_repin_ledger_row_names_a_live_or_owed_replacement\(", src)
for gone in ("_the_departures_grid_is_the_one_renderer_presented_as_a_strip",
             "_the_carousel_dots_are_real_colours_and_the_strip_rules_are_declared",
             "_the_full_grid_sits_behind_a_native_details_and_the_pagers_behind_the_gate"):
    assert not re.search(r"def %s\(" % gone, src), gone
assert re.search(r"def _the_rendered_settings_page_carries_no_duplicate_id\(", src), \
    "the generic duplicate-id check must survive untouched"
print("OK ledger rows:", len(led))
PY</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_config_page.py` reports `N/N checks pass` with
      `EXPECTED_CHECK_COUNT` equal to the tally the run reported, appended to the file's own history
      block with a one-line reason.
    - The inline verify script exits 0: at least 15 well-formed ledger rows, every `retired` name has
      no surviving `def`, every `owed_by` is one of 30-05/30-06/30-07/30-08, the guard check exists,
      the three retired carousel-shape checks are gone, the generic duplicate-id check survives.
    - The re-homed "exactly `len(THEME_IDS)` radios named `theme` page-wide" check passes AND its
      source contains none of the words that name a retired mechanism — prove it:
      `server/.venv/bin/python3 -c "import re;s=open('companion/test_config_page.py').read();b=re.search(r'def _[a-z_]*radios_named[a-z_]*\(.*?(?=\n    def |\n    check\()',s,re.S).group(0);print([w for w in ('strip','pager','carousel','dots','usage-panel','disclosure') if w in b])"`
      prints `[]`.
    - Three guard-mutation failure messages quoted in the SUMMARY.
    - `git diff --stat` shows exactly one changed file.
  </acceptance_criteria>
  <done>
    `test_config_page.py` is green at a re-derived count, carries a non-vacuous ledger guard proven
    against three deliberately wrong ledgers, and has re-homed the one property whose retirement
    would otherwise have left the page able to show one setting in two places.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the no-JS registry's carousel-pagers row</name>
  <files>companion/test_companion_app.py</files>
  <read_first>
    - `companion/test_companion_app.py` lines 930-1052 — `_NO_JS_CONTROL_REGISTRY` in full, and
      specifically lines 995-1021: the pagers row plus the comment block that argues it. Note the
      comment's own sentence naming the pagers as "the only part of this control that cannot work
      without a script" — that is the precondition CFG-85 removes, which is why the row goes rather
      than moves.
    - `companion/test_companion_app.py` lines 7083-7240 — `_no_js_control_violation()` and
      `_no_js_control_contract_holds_for_every_registered_control()`. Confirm for yourself before
      deleting: the check loops `_NO_JS_CONTROL_REGISTRY` and separately exercises four hand-built
      fixtures; there is NO completeness guard demanding a registry row per `layout.JS_GATE_CLASS`
      element. That is why deleting a row while the pagers still ship leaves the harness green.
    - `.planning/phases/30-…/30-RESEARCH.md` "Pitfall 2" (lines 362-388) in full.
  </read_first>
  <action>
    Delete the `"the theme carousel's two pagers (D5)"` dict from `_NO_JS_CONTROL_REGISTRY`, together
    with the comment block above it that argues its `wrapper_attr` choice and its one-row-not-two
    arithmetic.

    Do NOT repoint it at the palette radios. The registry's purpose is to prove that every element
    carrying a `.js`-gated wrapper attribute also has a native input reachable when the gate is
    absent — it registers JS-REQUIRED affordances only. The accordion has none: `<details name>` is
    complete from first paint with zero script, so there is nothing to gate. Registering the palette
    radios here would claim a JS-optionality property that was never at risk for them, and — per
    30-RESEARCH.md's Pitfall 2 — would either fail immediately (no wrapper attribute exists on the
    new markup) or pass vacuously against a leftover attribute. Both outcomes are wrong; deletion is
    correct.

    Two independent reasons this row must go in THIS plan and not later: its `wrapper_attr` reads
    `config_page.THEME_CAROUSEL_WRAPPER_ATTR` and its `render` lambda calls
    `config_page._frame_colours_card_html(...)` — plan 30-04 deletes both, so leaving the row would
    turn this harness red at import time, not at assertion time.

    Add a one-paragraph note in the registry's own header comment recording that Phase 30 removed a
    row and that the accordion is deliberately unregistered because it gates nothing — so a future
    reader finds the argument at the registry rather than in a planning file, matching the file's own
    convention.

    Re-derive `EXPECTED_CHECK_COUNT` by RUNNING. Expect it to be UNCHANGED at 317: deleting a
    registry row removes an assertion from inside one check, not a check.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_companion_app.py").read()
assert "THEME_CAROUSEL_WRAPPER_ATTR" not in src, "the retired wrapper attr is still referenced"
assert "_frame_colours_card_html" not in src, "the retiring builder is still called from this harness"
assert re.search(r"^_NO_JS_CONTROL_REGISTRY = \(", src, re.M), "registry tuple missing"
body = re.search(r"^_NO_JS_CONTROL_REGISTRY = \((.*?)^\)", src, re.S | re.M).group(1)
rows = body.count('"control":')
assert rows == 4, "expected 4 registry rows after the deletion, got %d" % rows
assert re.search(r"def _no_js_control_contract_holds_for_every_registered_control\(", src)
print("OK rows:", rows)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_companion_app.py` prints `317/317 checks pass` —
      unchanged, because a registry row is an assertion inside a check, not a check.
    - The inline verify script exits 0: `THEME_CAROUSEL_WRAPPER_ATTR` and `_frame_colours_card_html`
      no longer appear anywhere in this harness, and exactly 4 registry rows remain (the two
      quiet-hours handles, the wake-interval slider, the artwork drop zone).
    - `server/.venv/bin/python3 -c "import re;s=open('companion/test_companion_app.py').read();print(s.count('accordion'))"`
      prints at least 1 — the header-comment note explaining the deliberate non-registration exists.
    - The four fixtures are untouched: `git diff` shows no hunk inside lines 7083-7240.
  </acceptance_criteria>
  <done>
    The pagers row and its comment are gone, no reference to the two retiring `config_page` symbols
    remains in this harness, the registry's header records why the accordion is deliberately
    unregistered, and the count is still 317 with the contract machine still proven against its four
    fixtures.
  </done>
</task>

<task type="auto">
  <name>Task 3: Retire the carousel's browser checks; narrow the two scripts-blocked-save proofs</name>
  <files>companion/test_browser_ux.py</files>
  <read_first>
    - `companion/test_browser_ux.py` lines 13391-13500 — the shared selector block and `_STRIP_PROBE`.
      Establish which of its four selector constants have consumers OUTSIDE the checks being retired
      (`THEME_PREVIEW_SEL` does; the other three do not) before deleting anything.
    - `companion/test_browser_ux.py` lines 13554-13712 —
      `_the_theme_still_saves_with_scripts_blocked_through_the_carousel`. Note the split: the
      `_persist_without_js()` loop over `("en", "fr")` at `VIEWPORT_MIN_SUPPORTED` is entirely
      markup-agnostic (it drives by field NAME and reads back from disk) and must survive verbatim;
      only the trailing `with _no_js_page(...)` strip-probe block is markup-specific.
    - `companion/test_browser_ux.py` lines 13714-13861 — the arrivals twin, same split.
    - `companion/test_browser_ux.py` lines 13863-13933, 13935-14066, 14390-14598, 14600-15060,
      15226-end-of-that-check — the five retirees.
    - `companion/test_browser_ux.py` lines 950-980 — the `EXPECTED_CHECK_COUNT` history block (final
      pin 96).
  </read_first>
  <action>
    Add a `_ASPECT_REPIN_LEDGER` tuple to THIS file too, with the same five-key row shape and the
    same header-comment convention as Task 1's, plus its own registered guard check
    (`_every_aspect_repin_ledger_row_names_a_live_or_owed_replacement`, same name, this file's own
    source). Two ledgers rather than one shared import: each guard reads its OWN file's source, which
    is what makes it a guard rather than a cross-file assumption, and this file's rows are all owed
    by 30-08 while the other file's spread across three plans.

    Apply the routing table's `test_browser_ux.py` dispositions:

    **Narrow, do not delete, the two scripts-blocked-save checks.** Keep the `_persist_without_js()`
    half verbatim — that is CFG-85's own named proof ("the value read back from disk after an
    operate-submit round trip with scripts blocked at 360px in both languages") and it must not have a
    single-wave gap. Delete only the trailing strip-probe block. Rename both to drop
    `_through_the_carousel` / `_through_its_own_carousel`, and reword their registered `check(...)`
    names to describe the surviving proof without naming a strip. Ledger the deleted half's property
    to 30-08.

    **Retire the five carousel-only checks** with their comment blocks and `check(...)`
    registrations, adding a ledger row for each surviving property named in the routing table.

    **Retire the shared selector block's three carousel constants and `_STRIP_PROBE`.** Keep
    `THEME_PREVIEW_SEL` — `.theme-live-preview__image` is unchanged by this phase and other checks
    read it. Before deleting each of the three, grep the whole file for its remaining consumers and
    record the count per constant in the SUMMARY: this is the same
    grep-before-delete discipline ROADMAP's carried discipline demands for CSS rules, applied to test
    constants, and the two `.theme-chip`-based helper probes elsewhere in the file are exactly the
    kind of surviving consumer a blind delete would break.

    **Do NOT touch** `_display_page_height` (line 2870),
    `_displays_page_height_is_recorded_at_both_phone_widths`, or
    `_the_slider_meets_its_floors_at_360px_in_both_themes`. The first two belong to plan 30-04 and
    30-08 respectively; the third only appeared in the derivation because of the known
    mis-attribution documented in `<derivation_first>`.

    Re-derive `EXPECTED_CHECK_COUNT` by RUNNING. The count drops by five and gains one (the ledger
    guard), so it moves 96 → 92 — but take the number from the run, not from this arithmetic.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_browser_ux.py 2>&amp;1 | tail -3 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_browser_ux.py").read()
fail = []
for gone in ("_each_carousels_own_disclosure_toggles_only_its_own_strip",
             "_arrivals_and_calendar_keep_the_focused_chip_in_view_when_keyed",
             "_keying_the_strip_selects_scrolls_into_view_and_moves_the_preview",
             "_scrolling_a_strip_moves_its_own_preview_to_the_centered_chip_and_selects_nothing",
             "_the_carousel_meets_its_floors_at_360px_in_both_themes"):
    if re.search(r"def %s\(" % gone, src): fail.append("still defined: " + gone)
for token in ("THEME_STRIP_SEL", "THEME_PAGERS_SEL", "THEME_DETAILS_SEL", "_STRIP_PROBE"):
    if token in src: fail.append("still referenced: " + token)
if "THEME_PREVIEW_SEL" not in src: fail.append("THEME_PREVIEW_SEL was deleted but has other consumers")
for kept in ("_the_theme_still_saves_with_scripts_blocked",
             "_arrivals_still_saves_with_scripts_blocked"):
    if not re.search(r"def %s\(" % kept, src): fail.append("missing narrowed check: " + kept)
if not re.search(r"def _display_page_height\(", src): fail.append("_display_page_height was touched")
if "FRAME_COLOURS_HEADING_ID" not in src: fail.append("the CFG-86 guard was changed here; it belongs to 30-04")
if not re.search(r"^_ASPECT_REPIN_LEDGER = \(", src, re.M): fail.append("no ledger in this file")
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_browser_ux.py` prints a `N/N checks pass` tally with
      `EXPECTED_CHECK_COUNT` equal to it, appended to the history block with a one-line reason.
    - The inline verify script exits 0 on every clause, including the two negative ones that would
      catch touching `_display_page_height` or the CFG-86 heading-id guard (both belong to 30-04).
    - Per-constant grep counts for the three retired selector constants recorded in the SUMMARY,
      taken BEFORE deletion.
    - `scripts/run-all-tests.sh` green end to end.
  </acceptance_criteria>
  <done>
    Five browser checks and the shared strip-selector block are retired, the two scripts-blocked-save
    proofs survive in narrowed form under names that no longer mention a strip, this file carries its
    own ledger and guard, the CFG-86 instrument is untouched, and the harness is green at a re-derived
    count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → local test server | unchanged: every browser check navigates only to `Harness.base_url()`, enforced by `test_browser_ux.py`'s own code |
| test suite → shipped behaviour | this plan changes ONLY assertions; no production code path is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-10 | Tampering | retiring security-relevant assertions | medium | mitigate | The one security-relevant proof in scope is CFG-85's scripts-blocked save round trip (the no-JS **control** contract, whose verdict is the value read back from disk). It is explicitly NARROWED rather than deleted, and the narrowed half is the markup-agnostic one — so the property is continuously proven across every commit in this phase. The `_no_js_control_violation()` machine and its four non-vacuous fixtures are untouched (asserted by Task 2's acceptance criteria). |
| T-30-11 | Repudiation | a silently forgotten re-pin | medium | mitigate | `_ASPECT_REPIN_LEDGER` + `_every_aspect_repin_ledger_row_names_a_live_or_owed_replacement()` in both harness files. The guard is proven non-vacuous against three deliberately wrong ledgers with all three failure messages quoted. A row whose `owed_by` is cleared without a real replacement `def` turns the harness red. |
| T-30-12 | Tampering | `_NO_JS_CONTROL_REGISTRY` row deletion leaving a gated element unproven | low | mitigate | Verified by reading the machine: it loops registered rows only, and after the deletion four genuine rows remain (two quiet-hours handles, wake-interval slider, artwork drop zone) — none of which this phase touches. The accordion introduces zero `layout.JS_GATE_CLASS` elements (30-UI-SPEC.md Floor #1), so there is nothing new owed a row; the registry's header comment records that argument in place. |
| T-30-13 | Elevation of Privilege | new route / auth surface / CSP | low | accept | None. This plan edits three test files and no production file. The CSP, the session gate and the `/settings` POST validation are untouched. |
</threat_model>

<verification>
- All three harnesses green at counts re-derived by RUNNING, at each of the plan's three commits.
- `scripts/run-all-tests.sh` green.
- `git diff --stat` across the plan: exactly three changed files, all under `companion/`, all test
  harnesses. No production file.
- The derived affected-check inventory (the `<derivation_first>` script's output) recorded verbatim in
  the SUMMARY, with any disagreement against `<routing_table>` called out.
- The CFG-85 requirement-text discrepancy from `<no_js_floor_note>` recorded in the SUMMARY, flagged
  for the developer.
</verification>

<success_criteria>
- Three retired + one re-homed in `test_config_page.py`; ~18 rows ledgered across 30-05/06/07.
- Pagers row gone from `_NO_JS_CONTROL_REGISTRY`; count still 317; four fixtures untouched.
- Five retired in `test_browser_ux.py`, two narrowed and renamed, shared strip-selector block gone,
  `THEME_PREVIEW_SEL` and `_display_page_height` untouched.
- Two ledgers, two guards, three quoted mutation messages.
- Every commit green.
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by this plan:
- `companion/test_config_page.py :: _ASPECT_REPIN_LEDGER` — tuple of five-key rows
  (`retired`, `property`, `replacement`, `owed_by`, `why`).
- `companion/test_config_page.py :: _every_aspect_repin_ledger_row_names_a_live_or_owed_replacement()`
- `companion/test_config_page.py ::` one re-homed check asserting the page carries exactly
  `len(device_config.THEME_IDS)` radios named `theme`, written with no retired-mechanism vocabulary.
- `companion/test_browser_ux.py :: _ASPECT_REPIN_LEDGER`
- `companion/test_browser_ux.py :: _every_aspect_repin_ledger_row_names_a_live_or_owed_replacement()`
- `companion/test_browser_ux.py ::` `_the_theme_still_saves_with_scripts_blocked` and
  `_arrivals_still_saves_with_scripts_blocked` (renamed, narrowed).

Retired by this plan: three `test_config_page.py` checks, five `test_browser_ux.py` checks, the
`test_browser_ux.py` shared strip-selector block (`THEME_STRIP_SEL`, `THEME_PAGERS_SEL`,
`THEME_DETAILS_SEL`, `_STRIP_PROBE`), and `_NO_JS_CONTROL_REGISTRY`'s carousel-pagers row.
No production symbol is created or retired.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-03-SUMMARY.md` when done. It must carry: the derivation script's verbatim output, the three ledger-guard mutation messages, the per-constant pre-delete grep counts, the three re-derived `EXPECTED_CHECK_COUNT` values, and the `<no_js_floor_note>` discrepancy flagged for the developer.
</output>
