---
phase: 30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-
plan: 07
type: execute
wave: 6
depends_on: ["30-06"]
files_modified:
  - companion/static/style.css
  - companion/test_config_page.py
  - companion/test_companion_app.py
  - .planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-CSS-AUDIT.md
autonomous: true
requirements: [CFG-85]
user_setup: []

must_haves:
  truths:
    - "The Aspect tile is styled: one card surface, four rows with the app's existing shared `<summary>` chevron, a wrapping palette of square swatches, a full-width leading option, a nested add-rule disclosure."
    - "Every retired CSS rule was grepped for a surviving consumer BEFORE deletion and the per-selector count is on record."
    - "Zero new custom property, colour literal, font family and font size — every declaration spends an existing token."
    - "The selected palette chip reuses the existing `:has(input:checked)` mechanism inside the ONE existing feature-query block; no second block is opened."
    - "The accent-reservation list in the stylesheet's own header names `.palette-chip` AND closes the three-phase-old Phase-23 gap (the switch's on-state track fill and the new-row arrival wash)."
    - "The motion budget is untouched: 4 keyframes, 2 reduce blocks, 1 no-preference block, all comment-stripped."
  artifacts:
    - "companion/static/style.css :: .aspect-card, .aspect-card__preview, .usage-row (+ --secondary, __name, __meta, __swatch), .palette, .palette-chip (+ __swatch, __name, __check), .palette-swatch (+ __band), .leading-option (+ __name), .rule-add"
    - ".planning/phases/30-…/30-CSS-AUDIT.md"
  key_links:
    - "`_exactly_one_has_feature_query_block_survives()` (test_companion_app.py:6780) — the new selected-chip rule must JOIN the existing `@supports selector(:has(*))` block, not open a second."
    - "`.theme-chip` has 55 live (comment-stripped) occurrences and `.theme-chip--compact` 8 — the rule-add form's compact grid is a surviving consumer. Only the carousel-specific and full-size-departures-specific rules are candidates for deletion."
    - "`.calendar-actions`, `.calendar-masked-url` and `.calendar-disconnect-btn` gained a new home in plan 30-06 and are live consumers — do not delete them."
    - "`button.calendar-disconnect-btn`'s element-qualified `(0,1,1)` specificity is what keeps the app's one destructive control off the primary-accent fill. Phase 22's own record says it was broken for a phase and a half; do not weaken it."
---

<objective>
Give the tile its appearance, remove the ~37 rules the old one needed, and — because this is the plan
that edits `companion/static/style.css` — pay a debt that has been outstanding since Phase 23.

The deletions are the half of CFG-85 that actually shrinks Display: the strip's `overflow-x`, its
`scroll-snap`, the fixed-width nowrap chips, two pager rules, a dots row, a disclosure and the
four-panel layout grid. ROADMAP's carried discipline for this phase says every removed rule is grepped
for a surviving consumer before deletion, and the measured occurrence counts make that checkable
rather than promised: `.theme-chip` alone has 55 live occurrences and most of them are still needed.

Purpose: land the visual contract with zero new tokens, and leave the stylesheet's own accent list
exhaustive again for the first time since Phase 23.
Output: the new rules, the audited deletions, `30-CSS-AUDIT.md`, and a repaired header comment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-UI-SPEC.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-PATTERNS.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-04-SUMMARY.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-06-SUMMARY.md
</context>

<project_skill>
Read `Skill("sketch-findings-skypane")` before writing a declaration, and specifically its Colour,
Typography, Spacing, Motion and Controls sections plus `references/control-density.md`. Three of its
standing contracts bind this plan directly:

1. **The accent-reservation list lives in `companion/static/style.css`'s own header comment and
   nowhere else.** Extend it there. `.palette-chip`'s selected border/check-glyph/background-wash is a
   new SELECTOR consuming the already-listed selected-selectable-card USE — name the selector, do not
   duplicate the list entry.
2. **The three-phase-old open gap is this plan's to close.** The skill records, re-verified at Phase
   25's close and unchanged since, that Phase 23 added two genuine accent consumers without extending
   the header list, and that "whichever plan next edits `companion/static/style.css` extends the list
   by those two, with Phase 23's reasoning". This plan edits that file. The two are
   `.switch[aria-checked="true"] .switch__track { background: var(--color-accent); border-color:
   var(--color-accent); }` — a switch's on-state, an author fill on a custom button and therefore NOT
   covered by the existing native-`accent-color` entry — and `@keyframes skypane-row-arrive`'s
   `color-mix(in srgb, var(--color-accent) 22%, transparent)` — an ARRIVAL wash, a use the list has
   never carried, distinct from the selected-card SELECTION wash. Add both, with that reasoning, in the
   header comment. Confirm both are still present and still unlisted before adding them; if a plan
   between Phase 25 and now closed the gap, say so and skip.
3. **Minimum supported viewport is 360px; the 320px assertions already in the harness stay.** Do not
   delete a 320px assertion to make a new layout pass.
</project_skill>

<tasks>

<task type="auto">
  <name>Task 1: The new rules, on existing tokens only</name>
  <files>companion/static/style.css</files>
  <read_first>
    - `companion/static/style.css` lines 1-80 — the header comment, including the accent-reservation
      list and its "do not broaden this list without updating 06.6.1-UI-SPEC.md's Color section"
      instruction.
    - `companion/static/style.css` lines 5334-5389 — the GLOBAL `summary` / `summary::before` rule:
      `min-height: 44px`, the 6px rotated-border chevron, the `[open]` rotation. The usage rows inherit
      this; do NOT draw a second chevron.
    - `companion/static/style.css` lines 4863-4899 — `.history-card__summary` / `::before`: the
      established override-only-what-differs idiom for a `<summary>` inside a card.
    - `companion/static/style.css` lines 3560-3694 — the ONE `@supports selector(:has(*))` block, with
      `.theme-chip:has(input:checked)` and `.runway-card`'s equivalents: border-colour, a 2px inset
      ring, a 1.02 scale, a 12% accent `color-mix` body wash, and the check glyph switching to
      `inline-flex`. The palette chip's selected state joins THIS block with the same three
      consequences at a smaller box.
    - `companion/static/style.css` lines 9983-10025 — `.theme-chip--compact`'s size-only rules, as the
      model for a size-only modifier that inherits every selected-state rule for free.
    - `companion/static/style.css` lines 10027-10070 — `.theme-live-preview*`, which is unchanged.
    - `.planning/phases/30-…/30-UI-SPEC.md` Spacing Scale (46-63), Typography (66-84), Color (87-124)
      and Touch Targets (324-336) tables — the declared contract for every value below.
  </read_first>
  <action>
    Add the new rules. Every declaration spends an existing token; the only literals permitted are the
    three the UI-SPEC argues explicitly, and each needs a written argument beside it:

    - `.aspect-card` — the card surface. It already gets `--color-dominant`, its hairline and its
      responsive `--space-lg`/`--space-md` padding from `.page-section`; add only what differs. Do not
      restate `.page-section`'s declarations.
    - `.aspect-card__preview` — the live preview's position: full width on phones, and at the
      `>=960px` breakpoint the left column of the card's own two-column layout. Use the existing
      breakpoint; do not introduce a new one.
    - `.usage-row` / `.usage-row--secondary` — the row container and the one-notch-quieter modifier.
      Per 30-UI-SPEC.md the `--secondary` difference is regular-weight summary text instead of
      semibold: ONE declaration, not a second treatment.
    - `.usage-row > summary` — row padding `var(--space-md) 0` and the swatch/label/meta layout around
      the INHERITED global chevron. The 44px floor is met directly by the global `summary` rule; do not
      re-declare `min-height` and do not add a second `::before`.
    - `.usage-row__name` at `--font-body-size` / `--weight-semibold`; `.usage-row__meta` at
      `--font-label-size` with the file's existing 70% `color-mix` muted strength. Both are existing
      tokens and an existing muted idiom.
    - `.palette` — `display: grid; grid-template-columns: repeat(auto-fill, minmax(64px, 1fr)); gap:
      var(--space-sm);`. **No `overflow-x`, no `scroll-snap-type`, no `flex-wrap: nowrap`, no fixed
      child width.** The `64px` is a track-size literal, the same category as
      `.theme-chip--compact`'s existing `width: 104px`, and 30-UI-SPEC.md's Spacing Scale exceptions
      table says so — write that one-line argument beside it.
    - `.palette-chip` — the `<label>`: column layout, swatch above caption, the existing card hairline
      at rest, the existing radius token, and `position: relative` for the check glyph. `cursor:
      pointer`.
    - `.palette-chip__swatch` — `aspect-ratio: 1` inside the 64px-minimum column, so the swatch is
      square at every width without a second literal.
    - `.palette-swatch` — `position: relative; overflow: hidden;` plus the radius token. The inline
      `background` arrives from the server; this rule contributes shape only.
    - `.palette-swatch__band` — `position: absolute; top: 33%; height: 34%; left: 0; right: 0;`. These
      four are geometry from 30-UI-SPEC.md's locked Swatch Rendering Contract, not spacing values;
      state that beside them.
    - `.palette-chip__name` — 10px / line-height 1.2. This is the one typographic exception and it
      needs its written argument: it WIDENS the existing sub-scale exception tier that
      `.sparkline-axis-label` and `.drawing-axis-label` already occupy at exactly 10px/1.2, which the
      skill file records as a deliberate per-selector literal convention rather than a token. Do NOT
      invent a `--font-micro-size`.
    - `.palette-chip__check` — `display: none` at rest, matching `.theme-chip__check`'s existing
      mechanism, so the feature-query block can switch it to `inline-flex`.
    - `.leading-option` / `.leading-option__name` — a full-row-width selectable control, ≥44px tall,
      reusing the existing `.theme-form .theme-option--active` active-state idiom (12% accent wash +
      accent text) for its checked state. That is a re-keyed consumer of an already-listed accent use,
      not a new one.
    - `.rule-add` — the nested disclosure. Its `<summary>` keeps the global 44px floor; add spacing
      only.

    **The selected state joins the existing feature query.** Inside the ONE
    `@supports selector(:has(*))` block at ~3560, add `.palette-chip:has(input:checked)` and its two
    descendants with the same three consequences `.theme-chip` already uses: accent border-colour plus
    the inset ring, the 12% accent `color-mix` wash, and the check glyph to `inline-flex`. Do not open
    a second `@supports` block — `_exactly_one_has_feature_query_block_survives()` pins the count at
    one and that pin is the reason the file has one feature query rather than a family of them.

    **Extend the header comment's accent-reservation list**, in one edit, with three things: the new
    `.palette-chip` selector named against the existing selected-selectable-card use, and the two
    Phase-23 consumers from `<project_skill>` item 2 with Phase 23's own reasoning. Verify both
    Phase-23 consumers are still present and still unlisted before adding them.

    **Do not touch** `.theme-live-preview*`, `.theme-chip` base, `.theme-chip--compact`,
    `.theme-chip__body--placeholder`, `.calendar-actions`, `.calendar-masked-url`,
    `button.calendar-disconnect-btn`, the motion blocks, or any 320px/360px media query.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
raw = open("companion/static/style.css").read()
s = re.sub(r"/\*.*?\*/", "", raw, flags=re.S)   # comment-stripped: the header comment may name anything
fail = []
for sel in (".aspect-card", ".aspect-card__preview", ".usage-row", ".usage-row--secondary",
            ".usage-row__name", ".usage-row__meta", ".palette", ".palette-chip",
            ".palette-chip__swatch", ".palette-chip__name", ".palette-chip__check",
            ".palette-swatch", ".palette-swatch__band", ".leading-option", ".rule-add"):
    if sel not in s: fail.append("missing rule: " + sel)
if len(re.findall(r"@supports selector\(:has", s)) != 1:
    fail.append("the :has() feature-query block count moved off 1")
if ".palette-chip:has(input:checked)" not in s.replace(" ", ""):
    fail.append("the selected-chip rule is not keyed on :has(input:checked)")
if len(re.findall(r"@keyframes", s)) != 4: fail.append("keyframes count moved off 4")
if len(re.findall(r"@media \(prefers-reduced-motion: reduce\)", s)) != 2: fail.append("reduce blocks moved off 2")
if len(re.findall(r"@media \(prefers-reduced-motion: no-preference\)", s)) != 1: fail.append("no-preference moved off 1")
pal = re.search(r"\.palette\s*\{[^}]*\}", s)
if pal:
    for banned in ("overflow-x", "scroll-snap", "nowrap"):
        if banned in pal.group(0): fail.append(".palette declares " + banned)
# the header comment must now name the new selector and both Phase-23 consumers
head = raw[:raw.index("*/", raw.index("Accent") if "Accent" in raw else 0) + 2] if "Accent" in raw else raw[:8000]
for want in (".palette-chip", "switch__track", "skypane-row-arrive"):
    if want not in head: fail.append("accent list does not name: " + want)
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY
server/.venv/bin/python3 companion/test_companion_app.py 2>&amp;1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - The inline verify script exits 0. It measures comment-stripped source for every rule gate, so the
      header comment's prose cannot satisfy or break one.
    - `server/.venv/bin/python3 companion/test_companion_app.py` green at 317, including
      `_exactly_one_has_feature_query_block_survives()` and
      `_motion_budget_is_enforced_in_the_stylesheet()`.
    - **Zero new custom property, colour literal, family or size.** Prove all four by arithmetic
      against the pre-task file and record the numbers in the SUMMARY:
      `server/.venv/bin/python3 -c "import re,subprocess;cur=re.sub(r'/\*.*?\*/','',open('companion/static/style.css').read(),flags=re.S);old=re.sub(r'/\*.*?\*/','',subprocess.run(['git','show','HEAD:companion/static/style.css'],capture_output=True,text=True).stdout,flags=re.S);f=lambda t,p:len(set(re.findall(p,t)));print('props',f(cur,r'--[a-z-]+:'),f(old,r'--[a-z-]+:'));print('hex',f(cur,r'#[0-9A-Fa-f]{3,8}'),f(old,r'#[0-9A-Fa-f]{3,8}'));print('families',f(cur,r'font-family:[^;]+'),f(old,r'font-family:[^;]+'));print('sizes',f(cur,r'font-size:[^;]+'),f(old,r'font-size:[^;]+'))"`
      — the custom-property, hex and family sets must not grow. The font-size set may grow by at most
      one (the 10px caption) and only if that value already exists elsewhere in the file for
      `.sparkline-axis-label`/`.drawing-axis-label`, in which case it grows by zero. State which.
    - The three permitted literals (`minmax(64px, …)`, the band's four percentages, the 10px caption)
      each carry a written argument on an adjacent line.
    - The accent-list edit quoted verbatim in the SUMMARY, with confirmation that both Phase-23
      consumers were still present and unlisted beforehand.
  </acceptance_criteria>
  <done>
    The tile is styled from existing tokens, the selected state joins the single feature query, the
    motion budget is unmoved, and the accent-reservation list is exhaustive again for the first time
    since Phase 23.
  </done>
</task>

<task type="auto">
  <name>Task 2: The audited deletions</name>
  <files>companion/static/style.css, .planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-CSS-AUDIT.md</files>
  <read_first>
    - `companion/static/style.css` lines 10072-10233 — the `.frame-colours*` block: `__layout` (and its
      `>=960px` override), `__panels`, `__preview`, `__list`, `__row` (+ `:hover`, the
      `input:checked +` rule, `:focus-within`), `__swatch`, `__label`, `__meta`, `__panel-legend`,
      `__usage-panel` (+ `:last-child`, `--collapsed`).
    - `companion/static/style.css` lines 10288-10405 — `.theme-chip-grid--strip` (+ its `> .theme-chip`
      rule), `.theme-carousel__all`, `__pagers`, `__pager::after`, `__pager--prev::after`, `__dots`.
    - `companion/static/style.css` lines 3757-3830 — the two rules inside the feature query that name
      retired markup: `.frame-colours__list li:has(input:checked) .frame-colours__row` and
      `.theme-carousel:has(.theme-carousel__all[open]) .theme-chip-grid--strip`. Both go; the BLOCK
      stays (it now holds `.theme-chip`, `.runway-card` and the new `.palette-chip`).
    - `companion/static/style.css` line 2917 — a comment referencing `.frame-colours__panel-legend`
      from another rule's argument. A comment is not a consumer; update the prose, do not delete the
      rule it belongs to.
    - `companion/static/style.css` lines 3299-3830 and 9983-10025 — `.theme-chip` base and
      `--compact`. **Both stay.**
  </read_first>
  <action>
    For EVERY selector you intend to delete, in this order and recorded per selector in
    `30-CSS-AUDIT.md`:

    1. Count its surviving consumers across the whole repository BEFORE deleting, separately for
       markup/Python (`companion/`, `server/`), for JavaScript (`companion/static/*.js`) and for the
       harnesses (`companion/test_*.py`). Distinguish a real consumer from a COMMENT mentioning the
       name — a comment is not a consumer, and line 2917 is exactly that case.
    2. Delete only if the markup/Python and JavaScript counts are both zero. If a harness still
       references it, that reference belongs to a 30-07 ledger row and Task 3 handles it — note the
       dependency in the audit row rather than deleting the harness reference here.
    3. If any count is non-zero in markup or JS, do NOT delete. Record why and what still uses it.

    Expected deletions: the whole `.frame-colours*` block, `.theme-chip-grid--strip` and its child
    rule, all five `.theme-carousel*` rules, and the two feature-query rules naming retired markup.

    Expected NON-deletions, and the audit must say so explicitly for each, because each is the kind of
    thing a block-level delete takes out by accident: `.theme-chip` base and every rule under it,
    `.theme-chip--compact` and its six descendants, `.theme-chip__body--placeholder`,
    `.theme-live-preview*`, `.calendar-actions`, `.calendar-masked-url`,
    `button.calendar-disconnect-btn` (plan 30-06 gave the last three a new home inside the Aspect
    card — they are live consumers now, not orphans), and the `@supports selector(:has(*))` block
    itself.

    `30-CSS-AUDIT.md` carries, per selector: the selector, the pre-delete counts in the three
    categories, the verdict (deleted / kept), and the reason. Front matter: `phase: 30`, `plan: 07`,
    `sha` of the commit the counts were taken at. The audit IS the evidence ROADMAP's carried
    discipline asks for; a SUMMARY sentence saying "grepped before deleting" is not.

    Where a deleted rule's argument comment explained something still true of the app (for example the
    `min-width: 0` load-bearing note, or `.frame-colours__panel-legend`'s serif-leak fix and why no new
    rule block was needed), carry the SURVIVING half of that reasoning to whichever rule now owns the
    concern rather than dropping it with the selector. Note each carry-over in the audit.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 - &lt;&lt;'PY'
import pathlib, re, sys
raw = open("companion/static/style.css").read()
s = re.sub(r"/\*.*?\*/", "", raw, flags=re.S)   # a historical note in a comment must not fail this gate
fail = []
for dead in (".frame-colours", ".theme-carousel", ".theme-chip-grid--strip"):
    n = s.count(dead)
    if n: fail.append("%s still has %d live occurrence(s)" % (dead, n))
for kept in (".theme-chip--compact", ".theme-chip__body--placeholder", ".theme-live-preview",
             ".calendar-actions", ".calendar-masked-url", "button.calendar-disconnect-btn"):
    if kept not in s: fail.append("wrongly deleted, still has a consumer: " + kept)
if s.count(".theme-chip") < 30: fail.append(".theme-chip base rules look over-deleted (%d occurrences)" % s.count(".theme-chip"))
if len(re.findall(r"@supports selector\(:has", s)) != 1: fail.append("the feature-query block was deleted or duplicated")
a = pathlib.Path(".planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-CSS-AUDIT.md")
if not a.exists(): fail.append("no 30-CSS-AUDIT.md")
else:
    t = a.read_text()
    if not re.search(r"^sha:\s*[0-9a-f]{40}", t, re.M): fail.append("audit has no 40-char sha")
    rows = [l for l in t.split("\n") if l.strip().startswith("|") and "." in l]
    if len(rows) < 20: fail.append("audit has only %d selector rows; the deletion set is larger" % len(rows))
    for must in (".theme-chip--compact", ".calendar-disconnect-btn", ".theme-live-preview"):
        if must not in t: fail.append("audit does not record the NON-deletion of " + must)
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY
server/.venv/bin/python3 companion/test_companion_app.py 2>&amp;1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - The inline verify script exits 0. It measures comment-stripped CSS, so a historical comment
      naming a deleted mechanism does not fail its own gate — the deliberate alternative to a bare
      `grep -c … == 0` on the raw file, which any explanatory note would break.
    - `30-CSS-AUDIT.md` exists with a 40-character SHA, at least 20 selector rows, and an explicit
      NON-deletion row for each of the six kept selectors.
    - `server/.venv/bin/python3 companion/test_companion_app.py` green at 317.
    - `wc -l companion/static/style.css` before and after, recorded in the SUMMARY with the net line
      delta. This is the CSS half of what CFG-86's page-height reduction depends on, so the number is
      worth having.
  </acceptance_criteria>
  <done>
    Every retired rule was grepped in three categories before deletion and the evidence is on record;
    six selectors with surviving consumers are explicitly kept; the feature-query block survives with
    its count at one.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the CSS-declaration checks and clear the 30-07 ledger rows</name>
  <files>companion/test_config_page.py, companion/test_companion_app.py</files>
  <read_first>
    - the `owed_by == "30-07"` rows of `_ASPECT_REPIN_LEDGER`: the
      disconnect-is-secondary-plus-checked-row check and the
      segmented-control-and-panel-legend-serif check.
    - `companion/test_config_page.py` at the sites plan 30-03's derivation reported for
      `.frame-colours__panel-legend` (~11531) and `input:checked + .frame-colours__row` (~6912-6921),
      and the `.frame-colours__layout`/`__usage-panel` `min-width: 0` declaration assertions (~12912).
    - `companion/test_companion_app.py` lines 1054-1090 and 6780-6801 and 7300-7340 — the motion pins,
      the feature-query-block pin, and the reduce/no-preference assertions. None of the three numbers
      moves this phase; confirm rather than adjust.
    - `.planning/phases/30-…/30-CSS-AUDIT.md` from Task 2 — any audit row whose deletion was deferred
      pending a harness reference is this task's work.
  </read_first>
  <action>
    Write the replacements for the two 30-07 ledger rows.

    **The disconnect-is-secondary check's two halves.** Re-prove BOTH: (a) the destructive control's
    quiet treatment — `button.calendar-disconnect-btn` is element-qualified so its `(0,1,1)`
    specificity beats `button[type="submit"]`'s accent fill, and the rule's declarations are therefore
    live rather than dead. Assert the SPECIFICITY RELATIONSHIP and the source order, not merely that
    the rule exists: the recorded defect was a rule whose every declaration was dead for a phase and a
    half while the control rendered as the page's primary CTA. (b) the selection-is-free-and-focusable
    half, re-keyed from `input:checked + .frame-colours__row` to the new selected-state mechanism —
    which is now `:has(input:checked)` inside the feature query, so the replacement asserts the
    palette chip's checked-state declarations (border-colour, inset ring, wash, check glyph) exist
    inside that ONE block. Note in the check's own comment that the mechanism changed from an
    adjacent-sibling selector to `:has()`, and why.

    **The segmented-control / panel-legend check.** Its `.frame-colours__panel-legend` serif-leak half
    has no legend to point at — the usage panels and their `<legend>` elements are gone. Retire that
    half, and keep the segmented-control half (the global label-margin reset and the legend staying off
    the serif) pointed at the control that still exists: the rule-add form's "Match by" segmented
    control, which 30-UI-SPEC.md says is reused verbatim. Record in the check's comment that the serif
    boundary's named `.frame-colours__panel-legend` exception is retired with its selector, which
    NARROWS the serif exception set by one — a design-system fact the skill file's Typography section
    tracks and the SUMMARY should state so it can be folded in.

    Also re-point the `min-width: 0` declaration assertions from `.frame-colours__layout` /
    `.frame-colours__usage-panel` to whichever new rule now owns that concern (the card's own
    two-column layout at `>=960px`). `min-width: 0` on a grid child is load-bearing against a grid item
    refusing to shrink below its content; if the new layout does not need it, say so explicitly with the
    reason rather than silently dropping the assertion.

    Clear only the `30-07` ledger rows. Leave the `30-08` rows. Re-derive both harnesses'
    `EXPECTED_CHECK_COUNT` by RUNNING.

    Mutation-test two: remove the element qualifier from `button.calendar-disconnect-btn` (the
    specificity relationship must fail), and move the palette chip's checked-state rules outside the
    feature query (the single-block pin must fail). Quote both messages.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 companion/test_companion_app.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
cp = open("companion/test_config_page.py").read()
ca = open("companion/test_companion_app.py").read()
ns = {}
m = re.search(r"^_ASPECT_REPIN_LEDGER = \((.*?)^\)", cp, re.S | re.M)
assert m, "ledger missing"
exec("_L = (" + m.group(1) + ")", ns); led = ns["_L"]
fail = []
if [r for r in led if r.get("owed_by") == "30-07"]:
    fail.append("rows still owed by 30-07: %r" % [r["retired"] for r in led if r.get("owed_by") == "30-07"])
for r in led:
    if not r.get("owed_by") and not re.search(r"def %s\(" % re.escape(r["replacement"]), cp):
        fail.append("cleared row without a live replacement: " + r["retired"])
for dead in ("frame-colours", "theme-carousel", "theme-chip-grid--strip"):
    if dead in cp: fail.append("harness still asserts against retired CSS: " + dead)
if "EXPECTED_REDUCED_MOTION_REDUCE_BLOCKS = 2" not in ca: fail.append("reduce-block pin moved")
if "EXPECTED_REDUCED_MOTION_NO_PREFERENCE_BLOCKS = 1" not in ca: fail.append("no-preference pin moved")
if "button.calendar-disconnect-btn" not in cp: fail.append("the destructive-control specificity relationship is no longer asserted")
print("FAIL: " + "; ".join(fail) if fail else "OK still-owed=%d" % len([r for r in led if r.get("owed_by")]))
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - No ledger row owed by 30-07; only 30-08 rows remain; every cleared row resolves to a live `def`.
    - No harness reference to any deleted selector remains, so Task 2's deferred audit rows (if any)
      are now closed — update `30-CSS-AUDIT.md` accordingly.
    - Both motion pins unchanged at 2 and 1; the feature-query pin unchanged at 1.
    - Two mutation failure messages quoted in the SUMMARY.
    - `scripts/run-all-tests.sh` green end to end.
    - The narrowed serif-exception set stated in the SUMMARY as a design-system fact to fold into the
      `sketch-findings-skypane` skill.
  </acceptance_criteria>
  <done>
    The destructive control's specificity relationship and the palette chip's checked-state
    declarations are both pinned, the retired legend exception is retired with its selector and
    recorded, the motion and feature-query pins are unmoved, and the ledger owes nothing to this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet → rendered appearance | `companion/static/style.css` is served from the existing already-allowlisted route; no new asset, no external reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-33 | Tampering | `button.calendar-disconnect-btn` losing its element qualifier | high | mitigate | This app's one destructive control rendered as the page's primary accent-filled CTA for the whole of Phase 21 and most of Phase 22, because the rule was `(0,1,0)` against `button[type="submit"]`'s `(0,1,1)` and every declaration in it was dead. Task 3's replacement asserts the specificity RELATIONSHIP and source order, and mutation-proves it by removing the qualifier. Colour is not the safety mechanism here — the two-step confirm is — but a destructive control painted as the primary action is a real misclick risk and this is the phase most likely to disturb it, since the button just moved into a new container. |
| T-30-34 | Tampering | CSP / external reference | low | accept | `companion/static/style.css` has no external reference by standing convention and this plan adds none: no `@import`, no `url(` to a remote host, no webfont. The `url(` ban the drawing contract records is unaffected. `companion/app.py` untouched. |
| T-30-35 | Denial of Service | a deletion taking out a live consumer | medium | mitigate | Three-category pre-delete grep per selector (markup/Python, JavaScript, harness), recorded in `30-CSS-AUDIT.md` with a SHA, plus six explicit NON-deletion rows machine-checked by the verify script. A comment mentioning a name is explicitly not counted as a consumer. |
| T-30-36 | Tampering | reduced-motion floor | medium | accept | Unchanged and asserted: 4 keyframes, 2 reduce blocks, 1 no-preference block, all comment-stripped, all pinned in `companion/test_companion_app.py`. This plan adds no `animation` declaration, so the global override continues to cover everything it needs to. A per-rule reduce block would be dead code, not a safety net — the file's own recorded verdict. |
| T-30-37 | Information Disclosure | accent-reservation list drifting from exhaustive | low | mitigate | The list's promise to be exhaustive is how an accidental accent use is told from an intended one, and that promise has been false since Phase 23. Task 1 closes it with Phase 23's own reasoning and quotes the edit in the SUMMARY. Registered here rather than dropped as cosmetic because the Disconnect-button defect above is precisely what a non-exhaustive list let survive for a phase and a half. |
| T-30-38 | Elevation of Privilege | new route, field or auth surface | low | accept | None. CSS and test files only. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_companion_app.py` — 317, with the motion budget, the
  feature-query-block pin and the reduce/no-preference pins all green.
- `server/.venv/bin/python3 companion/test_config_page.py` — green at a re-derived count.
- `scripts/run-all-tests.sh` — green end to end.
- Four token-growth arithmetic results (custom properties, hex literals, families, sizes) recorded in
  the SUMMARY.
- `wc -l companion/static/style.css` before and after, with the net delta.
- `30-CSS-AUDIT.md` complete, SHA-stamped, with the six NON-deletion rows.
- Visual, recorded in the SUMMARY: run the app and look at `/display` at 360px and at desktop, in both
  light and dark. State plainly what is right and what is not. Plan 30-08 carries the measured
  touch-target and both-theme proofs; this is the eyes-on pass that catches what a measurement will
  not, such as a swatch that is legible in light mode and invisible in dark.
</verification>

<success_criteria>
- Fifteen-plus new rules, all on existing tokens, with the three permitted literals each argued.
- Selected state inside the ONE feature query; block count still 1.
- `.frame-colours*`, `.theme-carousel*` and `.theme-chip-grid--strip` gone from live CSS; six named
  selectors with live consumers explicitly kept.
- `30-CSS-AUDIT.md` with three-category pre-delete counts per selector.
- Accent-reservation list exhaustive again: `.palette-chip` plus Phase 23's two.
- Motion budget and 320px/360px assertions unmoved.
- Zero ledger rows owed by 30-07. Full suite green.
</success_criteria>

<artifacts_this_phase_produces>
Created in `companion/static/style.css`: `.aspect-card`, `.aspect-card__preview`, `.usage-row`,
`.usage-row--secondary`, `.usage-row > summary`, `.usage-row__name`, `.usage-row__meta`,
`.usage-row__swatch`, `.palette`, `.palette-chip`, `.palette-chip__swatch`, `.palette-chip__name`,
`.palette-chip__check`, `.palette-swatch`, `.palette-swatch__band`, `.leading-option`,
`.leading-option__name`, `.rule-add`, plus `.palette-chip:has(input:checked)` and its two descendants
inside the existing `@supports selector(:has(*))` block, plus three header-comment
accent-reservation-list entries.

Created: `.planning/phases/30-…/30-CSS-AUDIT.md`.

Created in the harnesses: the replacement destructive-control-specificity check, the replacement
palette-checked-state-declaration check, and the narrowed segmented-control check.

Retired from `companion/static/style.css`: the whole `.frame-colours*` block (`__layout` and its
`>=960px` override, `__panels`, `__preview`, `__list`, `__row` + `:hover` + `input:checked +` +
`:focus-within`, `__swatch`, `__label`, `__meta`, `__panel-legend`, `__usage-panel` + `:last-child` +
`--collapsed`), `.theme-chip-grid--strip` and its `> .theme-chip` rule, `.theme-carousel__all`,
`.theme-carousel__pagers`, `.theme-carousel__pager::after`, `.theme-carousel__pager--prev::after`,
`.theme-carousel__dots`, and the two feature-query rules naming retired markup. The serif-exception set
narrows by one (`.frame-colours__panel-legend`).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-07-SUMMARY.md` when done. It must carry: the four token-growth arithmetic results, the style.css line delta, the verbatim accent-list edit with pre-check confirmation, the two mutation messages, the narrowed serif-exception fact, and the eyes-on observations at 360px and desktop in both themes.
</output>
