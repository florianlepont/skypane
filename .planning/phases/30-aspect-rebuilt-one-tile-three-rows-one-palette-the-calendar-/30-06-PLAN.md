---
phase: 30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-
plan: 06
type: execute
wave: 5
depends_on: ["30-05"]
files_modified:
  - companion/pages/config_page.py
  - companion/i18n_fr/display.py
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-85]
user_setup: []

must_haves:
  truths:
    - "The calendar's connection lives under the Calendar row: status line, masked URL, Replace-URL disclosure, Disconnect button and How-it-works, all inside `<details data-usage=\"calendar\">`, directly and with no extra wrapping disclosure."
    - "There is no second `page-section` card for the calendar on Display, no `<h2>Calendar</h2>` and no calendar caption — Display's Look supersection holds exactly one card."
    - "The cross-DOM disconnect `<form>` is still a separate fragment, and the two-step disconnect gate (native confirm + server-rendered confirmation page) is byte-for-byte unchanged."
    - "The merged card's render is gated on ONE screen-group condition, with the registry invariant that justifies the collapse written at the code."
    - "`handle_post()`'s two separate validation gates are untouched."
    - "`ASPECT_CAPTION_EXEMPTIONS` is down to two members and the tuple survives."
  artifacts:
    - "companion/pages/config_page.py :: _calendar_connection_html()"
    - "companion/test_config_page.py :: the replacement calendar-placement, h2-order, title-form and card-builder checks"
  key_links:
    - "`calendar_group()` returns `card_html + disconnect_form_html` — two concatenated top-level fragments (30-PATTERNS.md Pattern 2). The button renders inside the row; the `<form id=\"calendar-disconnect-form\">` stays its own fragment."
    - "30-RESEARCH.md Pitfall 3: the two `screens.GROUP_*` render gates must collapse to one for one card, and `handle_post()`'s gates at config_page.py:6167 and :6535 are a SEPARATE concern and out of scope."
    - "30-UI-SPEC.md §6: the sketch builds NO third wrapping \"Gérer\" disclosure. Build the flatter, approved shape."
---

<objective>
Fold the calendar's connection into the row that already sets the calendar's colour — the developer's
own complaint, verbatim: *"je comprends pas pourquoi calendrier ne fait pas partie de la même tuile
que les autres, au moins intégré dans « vols du calendrier »."*

Nothing about the calendar's behaviour changes. The four status branches, the write-only URL field,
the masked host-only line, the Replace-URL disclosure, the quiet grey Disconnect button, the
cross-DOM form, the native `confirm()` misclick guard and the server-rendered confirmation page that
is the real control — all of it relocates, none of it is rewritten. What changes is where it sits and
that its card, its heading and its colour-talking caption stop existing.

Purpose: one place for the calendar's colour and its connection; Display's Look supersection down to
one card.
Output: `_calendar_connection_html()` inside the Calendar row, `calendar_group()` retired, one render
gate, two caption exemptions left.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-UI-SPEC.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-RESEARCH.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-PATTERNS.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-04-SUMMARY.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-05-SUMMARY.md
@companion/pages/config_page.py
</context>

<derivation_first>
Plan 30-03's derivation grepped for `frame-colours`/`carousel`/`colour_usage` tokens and therefore
did NOT see the checks that pin the Calendar CARD by its own constants. Those break in this plan, and
this plan owns both the production file and the harness, so it retires and replaces them in the same
commit. Derive the list before writing anything:

```
server/.venv/bin/python3 - <<'PY'
import re
PAT = re.compile(r'CALENDAR_SECTION_HEADING|CALENDAR_HEADING_ID|CALENDAR_CAPTION|calendar_group|'
                 r'calendar-heading|calendar-masked-url|calendar-actions|calendar-url-disclosure')
for path in ("companion/test_config_page.py", "companion/test_companion_app.py",
             "companion/test_browser_ux.py"):
    lines = open(path).read().split("\n")
    cur, curline, order, seen = None, 0, [], {}
    for i, ln in enumerate(lines, 1):
        m = re.match(r'^\s*def (_\w+)\(', ln)
        if m: cur, curline = m.group(1), i
        if PAT.search(ln) and not ln.strip().startswith('#'):
            k = (cur, curline)
            if k not in seen: order.append(k)
            seen.setdefault(k, []).append(i)
    print("===", path, len(order), "blocks")
    for fn, dl in order:
        print("   %-70s def@%-6d hits=%d first=%d" % (fn, dl, len(seen[(fn, dl)]), seen[(fn, dl)][0]))
PY
```

At the SHA this plan starts from, `companion/test_config_page.py` carries at least these reference
sites: 7377, 7728, 7902, 7945-7946, 8209, 8217, 8533, 8647, 9157, 9301, 9820. Record the derived
block list verbatim in the SUMMARY and route each one: **keep** (its property survives the merge
untouched), **repoint** (same property, new locator), or **replace** (the property itself changes
shape, e.g. "two dirty sections under Look" becomes "one").

**One behaviour change you must find in that list and record rather than paper over.** Display's
Look supersection currently contains TWO `data-dirty-section` cards ("Frame colours" and
"Calendar"); after this plan it contains ONE ("Aspect"). The save bar counts unsaved sections by that
attribute, so its count for a calendar-only edit changes from "1 of 2 sections" to "1 of 1". That is
correct — one tile is one section — and it is exactly what CFG-85 asked for. The check at ~8533 that
counts `data-dirty-section="Calendar"` must be REPLACED (not repointed to Aspect and left asserting
the old arithmetic), and the SUMMARY must state the new section count for Display explicitly.
</derivation_first>

<tasks>

<task type="auto">
  <name>Task 1: _calendar_connection_html() inside the Calendar row</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    - `companion/pages/config_page.py` lines 4459-4734 — `calendar_group()` in full, docstring
      included. The docstring's five labelled paragraphs (D-14b the status row, D-13/D-14 the state
      branch, D-14 the cross-form Disconnect button, D-14a How-it-works, plus the drift-first
      ordering argument) are the specification for what must survive; carry them into the new
      function's docstring rather than writing a shorter one.
    - `companion/pages/config_page.py` lines 1240-1260 — `CALENDAR_SECTION_HEADING`,
      `CALENDAR_HEADING_ID`, `CALENDAR_CAPTION`.
    - `companion/pages/config_page.py` lines 1336-1347 — `ASPECT_CAPTION_EXEMPTIONS` as plan 30-04
      left it (three members) and its rewritten header comment.
    - `companion/pages/config_page.py` — `_aspect_card_html()`'s Calendar row as plan 30-04 built it,
      and its own `<verify>`-proven shape.
    - 30-UI-SPEC.md §6 (lines 352-363) — why there is no third "Gérer" wrapper.
    - 30-PATTERNS.md "Disconnect-form sibling-fragment pattern (must preserve)".
  </read_first>
  <action>
    Extract `calendar_group()`'s body into
    `_calendar_connection_html(configured, drift, last_synced_at, last_attempt_at, now, entry_count,
    errors=None, submitted=None, state_dir=None)` returning a `(row_body_html,
    disconnect_form_html)` **tuple** rather than one concatenated string. The tuple is the honest
    shape now: the row body nests inside a `<details>` and the disconnect `<form>` must not, so a
    caller that receives one string would have to split it again. Keep the existing function's own
    argument order and defaults so the call site is a rename.

    `row_body_html` is, in this order and with NO extra wrapping element of any kind:
      1. `layout.status_row("", verdict, detail, state)` — the four-branch selection unchanged,
         `drift` still evaluated FIRST (a drifted file makes `configured` already False, so checking
         `not configured` first would make the drift state unreachable — that reasoning is in the
         existing docstring and must survive). The empty label argument stays correct: the row's own
         `<summary>` now names the subject where the `<h2>` used to.
      2. the masked-URL paragraph, when and only when a mask can be computed — OMITTED entirely
         rather than fabricated, unchanged.
      3. the Replace-URL `<details class="calendar-url-disclosure">` holding the connect form
         relabelled "Replace", plus the Disconnect button, in the existing
         `<p class="calendar-actions">` — unchanged, including the drift branch's own solo-Disconnect
         variant and the not-connected branch's unwrapped connect form.
      4. the How-it-works `<details>`, always, in every state.

    **Do not add a wrapping "Gérer"/"Manage" `<details>`.** ROADMAP's point 4 describes one; the
    developer-approved sketch does not build one and 30-UI-SPEC.md §6 records the resolution: two of
    these four pieces are already `<details>` elements, so a third layer would be a disclosure around
    two disclosures. Build the flatter, approved shape, and put §6's one-sentence reason in the
    function's docstring so a future reader meets the argument instead of filing the absence as a gap.

    Place the tuple's first element into the Calendar row's body in `_aspect_card_html()`, directly
    after that row's palette and its field error — per 30-UI-SPEC.md's row-3 contract, "directly
    beneath the palette, not behind any further disclosure". Concatenate the second element onto
    `_aspect_card_html()`'s overall return value as a sibling of the whole `.aspect-card` div,
    preserving the existing proven three-part shape: the Disconnect BUTTON inside, the `<form
    id="calendar-disconnect-form">` outside. `<details>` is not a `<form>`, so the form could legally
    nest — but matching the already-proven pattern is the lower-risk choice and 30-RESEARCH.md
    Pattern 2 says so in as many words.

    `_aspect_card_html()` gains the calendar-status parameters. Give them the same names and defaults
    `calendar_group()` used, so the call site reads as a merge rather than a re-interface.

    Retire `calendar_group()`, `CALENDAR_SECTION_HEADING`, `CALENDAR_HEADING_ID` and
    `CALENDAR_CAPTION`, each with its comment. Grep each for surviving consumers first and record the
    counts in the SUMMARY. Remove `CALENDAR_CAPTION` from `ASPECT_CAPTION_EXEMPTIONS`, taking it to
    two members (`DISPLAY_LOOK_INTRO`, `CALENDAR_URL_HINT`), and update the header comment again: the
    tuple does not empty this phase and the two survivors belong to a later editorial pass, not to
    this one. Do NOT delete the tuple or its harness wiring — plan 30-04 already corrected
    30-UI-SPEC.md on this point and the correction still holds.

    **Do NOT shorten `CALENDAR_URL_HINT`.** It is 22 words, deliberately over CFG-79's floor, and
    30-UI-SPEC.md's copy table says to reuse it verbatim. It is in the exemptions tuple precisely so
    the floor check passes against its exact, unshortened text.

    **Do NOT touch `handle_post()`.** Its two `screens.GROUP_*` validation gates (around lines 6167
    and 6535) decide which POSTed fields are validated, which is a different concern from which card
    renders. 30-RESEARCH.md Pitfall 3 puts them explicitly out of scope; changing them here would
    alter what the server accepts, not what it draws.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys; sys.path.insert(0, ".")
from companion.pages import config_page as cp
src = open("companion/pages/config_page.py").read()
fail = []
for gone in ("CALENDAR_SECTION_HEADING", "CALENDAR_HEADING_ID", "CALENDAR_CAPTION"):
    if re.search(r"^%s = " % gone, src, re.M): fail.append("still declared: " + gone)
if re.search(r"^def calendar_group\(", src, re.M): fail.append("calendar_group() still defined")
if not re.search(r"^def _calendar_connection_html\(", src, re.M): fail.append("no _calendar_connection_html")
if len(cp.ASPECT_CAPTION_EXEMPTIONS) != 2: fail.append("exemptions should be 2, got %d" % len(cp.ASPECT_CAPTION_EXEMPTIONS))
if cp.CALENDAR_URL_HINT not in cp.ASPECT_CAPTION_EXEMPTIONS: fail.append("CALENDAR_URL_HINT must remain exempt")
if len(cp.CALENDAR_URL_HINT.split()) < 20: fail.append("CALENDAR_URL_HINT was shortened")
for state in ((True, False), (False, False), (False, True)):
    configured, drift = state
    h = cp._aspect_card_html({}, "red", None, None, calendar_configured=configured,
                             calendar_drift=drift, calendar_last_synced_at=None,
                             calendar_last_attempt_at=None, now=None, calendar_entry_count=0) \
        if "calendar_configured" in src else None
    if h is None: fail.append("could not call _aspect_card_html with calendar args; check the signature")
h = cp._aspect_card_html({}, "red", None, None, calendar_configured=True, calendar_drift=False,
                         calendar_last_synced_at=None, calendar_last_attempt_at=None,
                         now=None, calendar_entry_count=0)
row = h.split('data-usage="calendar"')[1].split('data-usage="rules"')[0]
for want in ("status-row", "calendar-url-disclosure", "calendar-disconnect-btn"):
    if want not in row: fail.append("calendar row is missing: " + want)
if row.count('class="palette"') != 1: fail.append("calendar row lost its palette")
if h.count('class="page-section') != 1: fail.append("more than one page-section card rendered")
if 'id="calendar-heading"' in h: fail.append("the calendar h2 survived")
if h.index('class="palette"') > h.index("status-row") and 'data-usage="calendar"' in h:
    pass
if 'id="calendar-disconnect-form"' not in h: fail.append("the cross-DOM disconnect form is missing")
if row.count('id="calendar-disconnect-form"') != 0: fail.append("the disconnect form must be a sibling fragment, not inside the row")
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - The inline verify script exits 0 on every clause, including that the disconnect `<form>` is
      OUTSIDE the calendar row and the Disconnect button is inside it.
    - All three calendar states render: connected, not connected, and permission-drifted. Assert each
      shape separately; the drift branch is the one a merge most easily loses because it is the only
      one with a solo Disconnect and no Replace disclosure.
    - The two-step disconnect gate is byte-identical: `git diff` shows no hunk inside
      `calendar_disconnect_confirm_page()`, and `grep -c 'CALENDAR_DISCONNECT_CONFIRM_VALUE' companion/pages/config_page.py`
      is unchanged.
    - `git diff` shows NO hunk at `handle_post()`'s two `screens.GROUP_*` validation gates — quote
      both line contexts in the SUMMARY as evidence.
    - Pre-delete consumer grep counts recorded for `calendar_group`, `CALENDAR_SECTION_HEADING`,
      `CALENDAR_HEADING_ID` and `CALENDAR_CAPTION`.
    - `server/.venv/bin/python3 companion/test_companion_app.py` green at 317, including the CFG-79
      caption-floor check recomputing against a two-member exemptions tuple.
  </acceptance_criteria>
  <done>
    The calendar's connection renders inside the Calendar row in the flatter approved shape, its card,
    heading and caption are gone, the disconnect fragment and the two-step gate are unchanged,
    `handle_post()` is untouched, and the exemptions tuple is at two members with a corrected comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: One render gate, with the registry invariant written at the code</name>
  <files>companion/pages/config_page.py, companion/i18n_fr/display.py</files>
  <read_first>
    - `companion/pages/config_page.py` lines 5660-5700 — the Display branch as plan 30-04 left it:
      `aspect_section_html` gated on `screens.GROUP_THEME`, `display_calendar_card_html` gated
      separately on `screens.GROUP_CALENDAR`, and lines 5878-5900 where both interpolate.
    - `companion/pages/config_page.py` lines 5740-5780 — the two other scopes setting both to `""`.
    - `companion/screens.py` — the whole file, and specifically the single registered screen type's
      `everyday_groups` tuple (around lines 86-87) which lists `GROUP_THEME` and `GROUP_CALENDAR`
      together, plus the module docstring's own statement that the registry is designed for future
      multi-screen-type extension.
    - 30-RESEARCH.md Pitfall 3 and Assumption A1 in full.
    - `companion/i18n_fr/display.py` — the Calendar block, for the caption entry to delete.
  </read_first>
  <action>
    Collapse the two gates into one for the one merged card. Delete `display_calendar_card_html`
    entirely (all three scopes' assignments and its interpolation slot), and thread the calendar status
    arguments into the single `_aspect_card_html(...)` call already gated on
    `screens.GROUP_THEME in groups`.

    **Write the justification at the code, not only in the SUMMARY.** 30-RESEARCH.md Assumption A1 is
    explicit that this collapse is safe today and could stop being safe: `screens.py`'s one registered
    screen type lists both groups together in `everyday_groups`, so the two conditions are always both
    true or both false — but the registry exists to be extended, and a future screen type supporting
    theme-only would then render a merged card whose calendar half has no group backing it. Put that in
    a comment at the gate naming: the invariant relied on (`everyday_groups` lists both together), the
    file and tuple to check (`companion/screens.py`), and what a future reader must do if they add a
    screen type with only one of the two. A silent collapse is the version of this that a later reader
    cannot find.

    Leave `handle_post()`'s two validation gates alone — again, and for the same reason as Task 1.

    Delete `CALENDAR_CAPTION`'s French entry from `companion/i18n_fr/display.py` in this same commit,
    and the Calendar block's own comment reference to the retired caption. Keep every other calendar
    string: the status verdicts and details, the URL field label, the URL hint, the Replace summary and
    button, the Disconnect button, the How-it-works pair and all three disconnect-confirmation strings
    still have live English constants. If the Calendar `<h2>`'s own string
    (`CALENDAR_SECTION_HEADING`, "Calendar") had a French entry, delete that too — Task 1 retired the
    constant, and `companion/test_i18n.py`'s dead-translation check fires on an orphan.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys; sys.path.insert(0, ".")
src = open("companion/pages/config_page.py").read()
fr = open("companion/i18n_fr/display.py").read()
fail = []
if "display_calendar_card_html" in src: fail.append("the second card variable survives")
if src.count("screens.GROUP_CALENDAR in groups") != 0: fail.append("the second render gate survives")
if src.count("screens.GROUP_THEME in groups") < 1: fail.append("the merged gate is missing")
if "screens.GROUP_CALENDAR not in scope_groups" not in src and "GROUP_CALENDAR not in" not in src:
    fail.append("handle_post()'s own calendar validation gate appears to have been removed")
m = re.search(r"screens\.GROUP_THEME in groups", src)
ctx = src[max(0, m.start() - 1400):m.start()]
if "screens.py" not in ctx or "everyday_groups" not in ctx:
    fail.append("the gate-collapse rationale does not name companion/screens.py and everyday_groups")
if "Flights from your calendar get their own colour" in fr: fail.append("orphaned FR caption entry")
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY
server/.venv/bin/python3 companion/test_i18n.py 2>&amp;1 | tail -2
server/.venv/bin/python3 companion/test_companion_app.py 2>&amp;1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - One render gate; `display_calendar_card_html` gone from all three scopes and from the
      interpolation list; `handle_post()`'s two gates still present (asserted, not assumed).
    - The gate's comment names `companion/screens.py`, `everyday_groups`, and the action a future
      reader must take — machine-checked by the verify script searching the 1400 characters before
      the gate.
    - `server/.venv/bin/python3 companion/test_i18n.py` green; no orphaned French entry.
    - A rendered Display page contains exactly one `data-dirty-section` card under the Look
      supersection. State the new page-wide `data-dirty-section` count in the SUMMARY alongside the
      old one.
  </acceptance_criteria>
  <done>
    One card, one gate, the invariant documented at the code, the French catalogue clean, and the new
    dirty-section arithmetic stated rather than discovered.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the calendar's properties and clear the 30-06 ledger rows</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - the `<derivation_first>` script's output from the start of this plan — the authoritative list.
    - the `owed_by == "30-06"` rows of `_ASPECT_REPIN_LEDGER`.
    - `companion/test_config_page.py` at each reference site the derivation reports (7377, 7728, 7902,
      7945, 8209, 8217, 8533, 8647, 9157, 9301, 9820 and any others it finds), to decide keep /
      repoint / replace per site.
  </read_first>
  <action>
    Work the derived list. For each block:
      - **repoint** where the property is unchanged and only the locator moved — e.g. a check that
        located the calendar's status row via the Calendar card's `<h2>` now locates it via
        `data-usage="calendar"`;
      - **replace** where the property itself changed shape — the `data-dirty-section="Calendar"`
        count check is the clear case: its replacement asserts that Display's Look supersection carries
        exactly one dirty section and that its value is the translated Aspect heading;
      - **keep** untouched anything whose property the merge does not affect.

    Then write the replacements for the `owed_by == "30-06"` ledger rows:
      - the calendar-placement check → the calendar connection block renders inside the row whose
        `data-usage` is the calendar constant, AFTER that row's palette, with the disconnect `<form>`
        outside the card and the Disconnect button inside the row. Assert the relationship (button
        inside, form outside, button's `form=` attribute naming that form's id) rather than the three
        endpoints separately — a check that only asserts all three exist passes while the button
        points at nothing.
      - the h2-order check → Display's `<h2>` sequence, with the Calendar heading removed and the
        Aspect heading in the Frame-colours slot. Derive the expected sequence from the module's own
        heading constants, not from a retyped list of strings.
      - the title-form inventory → the classification still holds with one fewer form-A card title.
        Recompute the form-A/form-B tallies inside the check; do not restate the old 7/3/2 split.
      - `_no_card_builder_function_ever_calls_section_intro_html` → the AST-based builder-name list
        loses `_frame_colours_card_html` and `calendar_group` and gains `_aspect_card_html`. Assert the
        list's length against `len()` of the real builder set rather than a literal, so a later card
        merge does not need to edit a number.
      - the disconnect-is-secondary check → re-prove BOTH halves its ledger row names: the Disconnect
        button's quiet, non-accent treatment (its own rule is element-qualified
        `button.calendar-disconnect-btn` at `(0,1,1)`, which is what beats `button[type="submit"]`'s
        accent fill — that specificity fact was a real, phase-and-a-half-long defect and the check
        exists because of it), and the selection-is-free-and-focusable half. The `input:checked +
        .frame-colours__row` CSS-selector half of that old check belongs to plan 30-07 and stays
        ledgered there; say so in the row you clear.

    Clear only the `30-06` rows. Leave `30-07` and `30-08` rows untouched. Do not delete a row to make
    the guard pass.

    Mutation-test three: move the disconnect `<form>` inside the calendar row; remove the Aspect
    heading from the h2 sequence; add a second `data-dirty-section` card under Look. Quote all three
    messages. Re-derive `EXPECTED_CHECK_COUNT` by running.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_config_page.py").read()
ns = {}
m = re.search(r"^_ASPECT_REPIN_LEDGER = \((.*?)^\)", src, re.S | re.M)
assert m, "ledger missing"
exec("_L = (" + m.group(1) + ")", ns); led = ns["_L"]
fail = []
if not led: fail.append("ledger emptied instead of cleared")
if [r for r in led if r.get("owed_by") == "30-06"]:
    fail.append("rows still owed by 30-06: %r" % [r["retired"] for r in led if r.get("owed_by") == "30-06"])
for r in led:
    if not r.get("owed_by") and not re.search(r"def %s\(" % re.escape(r["replacement"]), src):
        fail.append("cleared row without a live replacement: " + r["retired"])
    if r.get("owed_by") and r["owed_by"] not in ("30-07", "30-08"):
        fail.append("unexpected owed_by %r" % r["owed_by"])
for gone in ("CALENDAR_SECTION_HEADING", "CALENDAR_HEADING_ID", "CALENDAR_CAPTION"):
    if gone in src: fail.append("harness still references retired constant: " + gone)
if "calendar_group" in src: fail.append("harness still references calendar_group()")
print("FAIL: " + "; ".join(fail) if fail else "OK still-owed=%d" % len([r for r in led if r.get("owed_by")]))
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - No ledger row owed by 30-06; every cleared row resolves to a live `def`; only 30-07 and 30-08
      rows remain.
    - No reference anywhere in the harness to the four retired calendar symbols.
    - Three mutation failure messages quoted in the SUMMARY.
    - `scripts/run-all-tests.sh` green end to end.
    - The derived block list from `<derivation_first>` recorded verbatim in the SUMMARY with each
      block's keep/repoint/replace disposition.
  </acceptance_criteria>
  <done>
    Every calendar-card property is re-proven against the merged row, the dirty-section arithmetic
    change is asserted rather than absorbed, the ledger owes nothing to this plan, and the full suite
    is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /settings/calendar/connect` | the write-only calendar feed URL crosses here |
| browser → `POST /settings/calendar/disconnect` | a destructive action, gated twice |
| stored calendar secret → rendered page | read once, host-only, for the masked line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-26 | Information Disclosure | the calendar feed URL | high | mitigate | The write-only contract is preserved verbatim by relocation, not rewritten: the `<input>` never carries a `value` attribute (T-16/17/20-SECRET), and the masked line is a wholly separate host-only fragment via `_masked_calendar_url()` that is OMITTED rather than fabricated when a mask cannot be computed. `_calendar_connection_html()` keeps the single `calendar_rules.configured_calendar_url(state_dir)` read at the same point in the same `configured` branch, behind the same upstream permission-drift guard. Assert in Task 1 that the connect/replace input emits no `value=` in any of the three states. |
| T-30-27 | Tampering | the destructive Disconnect losing its server-side gate | high | mitigate | The two-step gate is the security control and it is untouched: the hidden confirm field ships EMPTY, so a bare POST lands on `calendar_disconnect_confirm_page()` rather than erasing anything; `confirm-submit.js`'s native `confirm()` is a misclick guard layered on top, never the gate. Asserted by `git diff` showing no hunk in `calendar_disconnect_confirm_page()` and an unchanged `CALENDAR_DISCONNECT_CONFIRM_VALUE` reference count. The button's cross-DOM `form=` attribute is asserted to name the real form id, so relocation cannot leave a button that submits nothing — or, worse, submits the enclosing connect form. |
| T-30-28 | Tampering | HTML `<form>` nesting | medium | mitigate | The calendar row now holds the connect/replace `<form>`; the disconnect `<form>` stays a sibling fragment of the whole `.aspect-card`. Task 1's verify asserts `id="calendar-disconnect-form"` appears in the card's output but NOT inside the calendar row's slice. A nested form is silently dropped by the parser, which is a failure with no error message. |
| T-30-29 | Tampering / XSS | relocated interpolation sites | medium | mitigate | Every `escape_html()` call in `calendar_group()` relocates verbatim; the acceptance criteria compare counts before and after. The masked URL, the field label, the hint, both button labels and all three confirmation strings keep their existing escaping. |
| T-30-30 | Access Control | the gate collapse rendering a half-backed card | medium | mitigate | 30-RESEARCH.md Assumption A1. Mitigated by documenting the relied-on registry invariant at the gate (machine-checked: the comment must name `companion/screens.py` and `everyday_groups`) and by leaving `handle_post()`'s two independent validation gates untouched, so what the server ACCEPTS is unchanged regardless of what it draws. A future screen type supporting only one of the two groups is a rendering concern a reader can now find, not a silent authorization change. |
| T-30-31 | Spoofing / Elevation of Privilege | new route or auth surface | low | accept | None. Same two already-existing calendar POST routes, same session gate, same CSRF posture (session cookie + same-origin POST). No new endpoint. |
| T-30-32 | Tampering | CSP | low | accept | Unchanged: no script edited in this plan, no inline script, no `on*` attribute, `img-src 'self' data:` untouched. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` — green at a re-derived count.
- `server/.venv/bin/python3 companion/test_companion_app.py` — `317/317`, CFG-79 floor check
  recomputing against two exemptions.
- `server/.venv/bin/python3 companion/test_i18n.py` — green.
- `server/.venv/bin/python3 companion/test_browser_ux.py` — green.
- `scripts/run-all-tests.sh` — green end to end.
- All three calendar states rendered and asserted: connected, not connected, permission-drifted.
- `git diff` evidence quoted in the SUMMARY for the two untouched `handle_post()` gates and for
  `calendar_disconnect_confirm_page()`.
</verification>

<success_criteria>
- One card under Look; the calendar's connection inside the Calendar row in the flatter approved
  shape; no third "Gérer" wrapper.
- `calendar_group()` and three calendar constants retired with pre-delete consumer counts.
- One render gate with the registry invariant documented at the code; `handle_post()` untouched.
- `ASPECT_CAPTION_EXEMPTIONS` at two members; `CALENDAR_URL_HINT` unshortened.
- Dirty-section arithmetic change asserted and stated.
- Zero ledger rows owed by 30-06. Full suite green.
</success_criteria>

<artifacts_this_phase_produces>
Created: `_calendar_connection_html(...)` returning `(row_body_html, disconnect_form_html)`
(`companion/pages/config_page.py`); calendar-status parameters added to `_aspect_card_html()`; the
replacement calendar-placement, h2-order, title-form-inventory, card-builder-AST,
disconnect-is-secondary and single-dirty-section checks (`companion/test_config_page.py`).

Retired: `calendar_group()`, `CALENDAR_SECTION_HEADING`, `CALENDAR_HEADING_ID`, `CALENDAR_CAPTION`,
`display_calendar_card_html` (render-local), the second `screens.GROUP_CALENDAR` render gate, and the
French entries for the retired English constants.

No new CSS class is emitted — the relocated calendar markup keeps `calendar-url-disclosure`,
`calendar-actions`, `calendar-masked-url` and `calendar-disconnect-btn`, all of which already have
rules in `companion/static/style.css`. Plan 30-07 must therefore NOT delete them; it inherits them as
surviving consumers.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-06-SUMMARY.md` when done. It must carry: the `<derivation_first>` output with per-block dispositions, the four pre-delete consumer counts, the old and new Display `data-dirty-section` counts, the three mutation messages, and the `git diff` evidence for `handle_post()` and `calendar_disconnect_confirm_page()`.
</output>
