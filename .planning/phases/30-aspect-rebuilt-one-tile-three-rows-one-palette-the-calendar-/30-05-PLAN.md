---
phase: 30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-
plan: 05
type: execute
wave: 4
depends_on: ["30-04"]
files_modified:
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-85]
user_setup: []

must_haves:
  truths:
    - "`companion/test_config_page.py` is GREEN again — every check that plan 30-03 ledgered to 30-05 has a live replacement asserting the same property against the accordion."
    - "The Aspect card's shape is pinned as a RELATIONSHIP, not as a list of endpoints: four rows in `COLOUR_USAGES` order, exactly one open, one palette per theme row, one radio set per saved field, the rules row last and secondary."
    - "Every count in every new check is derived from `device_config` / `COLOUR_USAGES` at check time — no restated literal that a nineteenth theme would silently falsify."
    - "`_ASPECT_REPIN_LEDGER` carries no row still owed by 30-05, and the ledger guard proves it."
  artifacts:
    - "companion/test_config_page.py :: _aspect_card_full_shape_checklist()"
    - "companion/test_config_page.py :: _aspect_card_covers_every_registered_theme_with_own_id_and_label()"
    - "companion/test_config_page.py :: _aspect_card_default_selects_exactly_the_white_departures_option()"
    - "companion/test_config_page.py :: _aspect_arrivals_row_carries_leading_option_no_checkbox()"
    - "companion/test_config_page.py :: _aspect_arrivals_override_preselects_the_override_not_same_as_departures()"
    - "companion/test_config_page.py :: _aspect_calendar_row_palette_populated_in_order()"
    - "companion/test_config_page.py :: _rules_row_renders_inside_aspect_after_form()"
    - "companion/test_config_page.py :: _aspect_rules_row_label_locked_verbatim()"
  key_links:
    - "`_ASPECT_REPIN_LEDGER` rows with `owed_by == \"30-05\"` are this plan's exact contract — the guard check turns a forgotten one red."
    - "`_the_rendered_settings_page_carries_no_duplicate_id` and the re-homed radios-named-theme check from 30-03 must both still pass; the three palettes each carry a `role=\"radiogroup\"` and NO `id`, which is what keeps them from colliding."
---

<objective>
Turn the harness green again by re-proving every property plan 30-03 retired, against the accordion
this time.

This is the plan that decides whether Phase 30 traded coverage for structure. The ledger names the
debt; this plan pays it. Two rules govern every replacement check:

1. **Assert the relationship, not the endpoint** — this app's own sixth standing contract, from
   Phase 27. "Four rows exist" is an endpoint. "Four rows in `COLOUR_USAGES` order, exactly one of
   them `open`, each theme row holding exactly one palette whose radios all carry the same `name`,
   and the rules row last and marked secondary" is the relationship, and it is the thing a later
   refactor can actually break.
2. **Derive every count from the registry at check time.** A check that says 18 is a check that goes
   quiet the day a nineteenth theme ships. `_frame_colours_rules_row_label_locked_verbatim()`'s
   neighbours were written this way on purpose and the replacements must keep the habit.

Purpose: no property proven before this phase is unproven after it.
Output: `test_config_page.py` green at a re-derived count, with zero ledger rows owed by 30-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-UI-SPEC.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-03-SUMMARY.md
@.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-04-SUMMARY.md
@companion/pages/config_page.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-pin the card's shape and its three palettes</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - `companion/test_config_page.py` — the `_ASPECT_REPIN_LEDGER` tuple plan 30-03 added. Filter it
      to `owed_by == "30-05"` and work that list; each row's `property` sentence is the assertion the
      replacement owes, written deliberately in property language rather than markup language.
    - `companion/pages/config_page.py` — `_aspect_card_html()`, `_usage_row_html()`,
      `_palette_grid_html()`, `_palette_chip_html()`, `_usage_row_summary_html()` as plan 30-04 and
      30-02 left them. The checks address the real emitted strings, so read the emitters rather than
      the plans that described them.
    - `.planning/phases/30-…/30-04-SUMMARY.md` — the locked markup contract as actually shipped, and
      any deviation the executor recorded.
    - `companion/test_config_page.py` — the surviving re-homed radios-named-theme check and
      `_the_rendered_settings_page_carries_no_duplicate_id`, so the new checks complement them rather
      than restate them.
  </read_first>
  <action>
    Add these replacement checks, each registered with `check(...)` and each named in a ledger row so
    Task 3 can clear it.

    **`_aspect_card_full_shape_checklist()`** — the relationship check, replacing the old card's own.
    Against a real authenticated Display render (not just the builder's output — the old check ran
    against the page for a reason: it also pinned the card's POSITION):
      - exactly one `class="page-section aspect-card` on the page, and its index is greater than the
        `DISPLAY_LOOK_SECTION_ID` intro's index (the card is first under "Look");
      - exactly `len(config_page.COLOUR_USAGES)` `<details` elements carrying
        `name="aspect-rows"`, and the `data-usage` values in document order equal
        `list(config_page.COLOUR_USAGES)` — this is the locked-order property the retired
        `colour_usage` radiogroup check used to hold, re-expressed;
      - exactly ONE of those four carries ` open`, and it is the `departures` one;
      - the last one, and only it, carries `usage-row--secondary`;
      - each of the three theme rows contains exactly one `class="palette"` and the rules row
        contains none;
      - zero occurrences, page-wide, of the retired mechanisms' markers. Assert this against a set
        built from the COUNT of each token rather than a bare `not in`, and put the token list in a
        module-level tuple with a comment, so a later reader can see what is being excluded and why.
      - the `<h2>` immediately inside the card is `ASPECT_HEADING` at `ASPECT_HEADING_ID`, and the
        element immediately following it is NOT a `section-caption` paragraph — the no-caption half
        of CFG-85, asserted as a relationship between two adjacent elements rather than as a global
        count.

    **`_aspect_card_covers_every_registered_theme_with_own_id_and_label()`** — for each of the three
    saved fields (`theme`, `theme_arriving`, `calendar_theme_id`), the row's palette renders exactly
    one radio per `device_config.THEME_IDS` entry, in registry order, each carrying its own
    `value="{theme_id}"`, its own translated label, its own `data-preview-src` naming that same id,
    and `form="settings-form"`. Derive the expected count from the registry. Assert that the arrivals
    and calendar palettes additionally carry exactly one leading option each, and the departures
    palette exactly zero — departures has no "same as" concept and a leading option there would be a
    control that means nothing.

    **`_aspect_card_default_selects_exactly_the_white_departures_option()`** — rendered with
    `device_config.DEFAULT_THEME_ID` and no overrides: exactly one checked radio named `theme` and it
    is the default one; exactly one checked radio named `theme_arriving` and it is the leading option
    (empty-string value); same for `calendar_theme_id`. Assert "exactly one checked per group",
    derived per group, rather than a page-wide checked count — a page-wide count passes while two
    groups are wrong in opposite directions.

    **`_aspect_arrivals_row_carries_leading_option_no_checkbox()`** — the arrivals row's leading
    control is a native `<input type="radio" name="theme_arriving" value="">` wearing
    `class="leading-option"` on its wrapping label, carrying `form="settings-form"`, and there is no
    `<input type="checkbox">` anywhere in the Aspect card. The old check existed because this control
    used to be a checkbox and the empty-string radio is what made the clear-signal honest; keep that
    reasoning in the check's own comment.

    **`_aspect_arrivals_override_preselects_the_override_not_same_as_departures()`** — with a stored
    `theme_arriving` override: the override's chip is checked and the leading option is not, and the
    arrivals row's summary meta reads the override's own translated label rather than
    `SAME_AS_DEPARTURES_LABEL`. Locate the row by its `data-usage` attribute, and assert the calendar
    row is unaffected in the same render — that cross-row independence is the relationship the old
    check was really protecting.

    **`_aspect_calendar_row_palette_populated_in_order()`** — the calendar row's palette is a real
    `role="radiogroup"` populated in registry order with `name="calendar_theme_id"`, and it carries no
    `id` attribute. The no-`id` clause is load-bearing: `_palette_grid_html()` has three call sites on
    one page and an `id` emitted inside it would be three identical ids, which is exactly the trap the
    retired `_theme_carousel_html()`'s own docstring recorded and
    `_the_rendered_settings_page_carries_no_duplicate_id` would then catch as a symptom rather than a
    cause.

    Mutation-test at least four of the six before committing, and quote every failure message in the
    SUMMARY. Good mutations, each of which a shape-only check would miss: swap rows 2 and 3 at the
    call site (order); add ` open` to row 2 (exactly-one-open); give `_palette_grid_html()` an `id`
    (duplicate id); drop the arrivals leading option. Revert each with
    `git checkout-index -f -- companion/pages/config_page.py` and clear `__pycache__`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_config_page.py").read()
need = ("_aspect_card_full_shape_checklist",
        "_aspect_card_covers_every_registered_theme_with_own_id_and_label",
        "_aspect_card_default_selects_exactly_the_white_departures_option",
        "_aspect_arrivals_row_carries_leading_option_no_checkbox",
        "_aspect_arrivals_override_preselects_the_override_not_same_as_departures",
        "_aspect_calendar_row_palette_populated_in_order")
fail = [n for n in need if not re.search(r"def %s\(" % n, src)]
# every new check must derive its counts, never restate 18
for n in need:
    m = re.search(r"def %s\(\).*?(?=\n    def |\n    check\()" % n, src, re.S)
    if m and re.search(r"[^\w.]18[^\w]", m.group(0)):
        fail.append("%s restates the literal 18 instead of deriving it" % n)
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - All six replacement checks exist and pass; none contains the literal `18`.
    - `server/.venv/bin/python3 companion/test_config_page.py` reports `N/N checks pass` with
      `EXPECTED_CHECK_COUNT` re-derived by running.
    - Four mutation failure messages quoted in the SUMMARY, one per mutation listed in the action.
    - `git diff --stat` shows one changed file. `companion/pages/config_page.py` is byte-identical to
      the end of plan 30-04 — confirm with `git diff --exit-code companion/pages/config_page.py`
      against 30-04's final commit; a replacement check that needed a production change means the
      markup contract drifted and belongs in an escalation, not in a quiet edit.
  </acceptance_criteria>
  <done>
    The card's shape, order, open-row uniqueness, three palettes, leading options and override
    behaviour are pinned as relationships derived from the registry, and four mutations prove the
    checks non-vacuous.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-pin the page-level properties the Aspect rename moved</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - the remaining `owed_by == "30-05"` ledger rows: the rules-row placement check, the rules-row
      label lock, the live-preview-figure check, the scoped-render check, the pinned-English-strings
      check, and the `_rules_panel_segment` helper plus its escaped-copy consumer.
    - `companion/pages/config_page.py` — `render()`'s Display branch as plan 30-04 left it, for where
      `aspect_section_html` lands relative to `</form>`.
    - `companion/test_config_page.py` — the deleted checks' git history
      (`git log -p -S'_rules_section_renders_inside_frame_colours_after_form' -- companion/test_config_page.py`)
      so the replacement keeps the ORIGINAL reasoning rather than inventing a new rationale for an
      old property.
  </read_first>
  <action>
    **`_rules_row_renders_inside_aspect_after_form()`** — the rules row is inside the Aspect card, and
    the Aspect card's own index on the rendered page is after `</form>`'s. Both halves matter and for
    the same original reason: the rules row holds real `<form>` elements (the add form), HTML forbids
    a nested `<form>`, so the whole card must be a sibling of `#settings-form` while every theme radio
    still reaches it through `form="settings-form"`. Assert all three facts — card after
    `</form>`, rules row inside the card, and every theme radio carrying the `form=` attribute — as
    one relationship. Replace the old `COLOUR_USAGE_PANEL_TARGET_ATTR` locator with the row's
    `data-usage` attribute, and update `_rules_panel_segment` (rename it to `_rules_row_segment`) to
    slice the page by that attribute so `_rules_copy_appears_escaped_verbatim` keeps working
    unchanged.

    **`_aspect_rules_row_label_locked_verbatim()`** — `FRAME_COLOURS_ROW_LABELS[COLOUR_USAGE_RULES]`
    is still exactly `"Per-flight rules"`. Keep the lock: 30-UI-SPEC.md's copy table pins this string
    and ROADMAP's prose paraphrases it differently ("Règles par vol" happens to be the shipped French
    but the ROADMAP's empty-state paraphrase "Aucune règle · Ajouter" is NOT a shipped string). Add an
    assertion that the rules row's empty-state meta reads `FRAME_COLOURS_RULES_EMPTY_META`'s real
    value and not the ROADMAP paraphrase — this is a correction 30-UI-SPEC.md explicitly records and
    it deserves a check, because it is precisely the kind of plausible-sounding copy a later editorial
    pass would "restore".

    **The live-preview figure check** — one `.theme-live-preview` figure on Display, eager, with
    width and height, now wearing `aspect-card__preview`. One figure, not three: three rows sharing
    ONE preview is the whole point of the new shape, and a second figure would mean a row grew its
    own. Assert the count from the class, and assert the figure's index precedes the first
    `name="aspect-rows"` row's — the preview is ABOVE the rows (ROADMAP point 1), which is a
    relationship a class-presence check would miss.

    **The scoped-render check** — a one-symbol swap: the Display scope renders the Aspect card and the
    Device/SCOPE_ALL scopes do not. Keep its existing hidden-fields assertions untouched.

    **The pinned-English-strings check** — the pinned set loses `FRAME_COLOURS_HEADING` and
    `FRAME_COLOURS_CAPTION` and gains `ASPECT_HEADING`. Do not thin the rest of the set while you are
    in there; it is a pin, and a pin that shrinks for convenience is not a pin.

    Mutation-test the two with real geometry claims — move the preview below the rows, and move the
    Aspect card before `</form>` — and quote both messages.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_config_page.py").read()
fail = []
for n in ("_rules_row_renders_inside_aspect_after_form", "_aspect_rules_row_label_locked_verbatim"):
    if not re.search(r"def %s\(" % n, src): fail.append("missing " + n)
if "_rules_panel_segment" in src: fail.append("_rules_panel_segment should be renamed to _rules_row_segment")
if not re.search(r"def _rules_row_segment\(", src): fail.append("missing _rules_row_segment helper")
if "COLOUR_USAGE_PANEL_TARGET_ATTR" in src: fail.append("still references a retired attribute constant")
if "FRAME_COLOURS_HEADING" in src: fail.append("still references the retired heading constant")
if "ASPECT_HEADING" not in src: fail.append("the pinned English set does not include ASPECT_HEADING")
if "FRAME_COLOURS_RULES_EMPTY_META" not in src: fail.append("the real empty-state constant is not pinned")
if "aspect-card__preview" not in src: fail.append("the preview check was not repointed")
print("FAIL: " + "; ".join(fail) if fail else "OK")
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - The inline verify script exits 0; no reference to `COLOUR_USAGE_PANEL_TARGET_ATTR` or
      `FRAME_COLOURS_HEADING` survives anywhere in the harness.
    - Both mutation failure messages quoted in the SUMMARY.
    - `server/.venv/bin/python3 companion/test_config_page.py` green at a re-derived count.
  </acceptance_criteria>
  <done>
    The rules row's placement and copy lock, the single-preview-above-the-rows relationship, the
    scoped-render gate and the pinned English set are all re-proven against the accordion, with the
    ROADMAP's own empty-state paraphrase now guarded against.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clear the 30-05 ledger rows and prove nothing was skipped</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - `companion/test_config_page.py` — `_ASPECT_REPIN_LEDGER` and
      `_every_aspect_repin_ledger_row_names_a_live_or_owed_replacement()`, and the three mutation
      messages plan 30-03's SUMMARY quoted for that guard, so you know exactly which failure shapes
      it can already catch.
  </read_first>
  <action>
    For each ledger row with `owed_by == "30-05"`: clear `owed_by` (set it to `""` or `None`, whichever
    shape the guard was written against) so the guard switches from "well-formed and owed" to "the
    replacement must genuinely exist in this file", and confirm the guard then resolves each row's
    `replacement` to a real `def`.

    Where a replacement's name differs from what plan 30-03 predicted (for example the
    `_rules_panel_segment` → `_rules_row_segment` rename, or a check you merged into another), update
    the row's `replacement` field to the real name AND add a one-line note in the row explaining the
    divergence. Do NOT delete a row to make the guard pass — a deleted row is exactly the silent
    coverage loss the ledger exists to prevent, and the guard's own non-emptiness assertion is there
    because of it.

    Leave every `30-06`, `30-07` and `30-08` row exactly as it is.

    Then re-derive `EXPECTED_CHECK_COUNT` by RUNNING, and record in the SUMMARY the count trajectory
    across this phase so far: 274 before the phase, the value after 30-03's retirements, and the value
    now. ROADMAP's carried discipline asks for the count to be re-derived by running; recording the
    trajectory is what makes a later reader able to see that the dip was paid back.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py 2>&amp;1 | tail -2 &amp;&amp; server/.venv/bin/python3 - &lt;&lt;'PY'
import re, sys
src = open("companion/test_config_page.py").read()
ns = {}
m = re.search(r"^_ASPECT_REPIN_LEDGER = \((.*?)^\)", src, re.S | re.M)
assert m, "ledger missing"
exec("_L = (" + m.group(1) + ")", ns)
led = ns["_L"]
fail = []
if not led: fail.append("ledger emptied; rows must be cleared, not deleted")
owed05 = [r for r in led if r.get("owed_by") == "30-05"]
if owed05: fail.append("rows still owed by 30-05: %r" % [r["retired"] for r in owed05])
for r in led:
    if not r.get("owed_by"):
        if not re.search(r"def %s\(" % re.escape(r["replacement"]), src):
            fail.append("cleared row has no live replacement: %s -> %s" % (r["retired"], r["replacement"]))
    elif r["owed_by"] not in ("30-06", "30-07", "30-08"):
        fail.append("unexpected owed_by %r on %s" % (r["owed_by"], r["retired"]))
print("FAIL: " + "; ".join(fail) if fail else "OK cleared=%d still-owed=%d" % (
    len([r for r in led if not r.get("owed_by")]), len([r for r in led if r.get("owed_by")])))
sys.exit(1 if fail else 0)
PY</automated>
  </verify>
  <acceptance_criteria>
    - No ledger row is owed by 30-05; every cleared row resolves to a real `def`; the ledger is not
      empty; all remaining rows are owed by 30-06, 30-07 or 30-08.
    - `server/.venv/bin/python3 companion/test_config_page.py` green at a re-derived count, recorded
      with its trajectory (274 → post-30-03 → now).
    - `scripts/run-all-tests.sh` green end to end — this is the commit where the whole suite comes
      back from the one-wave dip, and that must be demonstrated, not assumed.
  </acceptance_criteria>
  <done>
    The ledger owes nothing to this plan, the guard proves every cleared row has a live replacement,
    and the full suite is green for the first time since plan 30-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → shipped behaviour | assertions only; no production file is edited by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-21 | Repudiation | a replacement check that passes while proving less than the one it replaces | medium | mitigate | Six mutations across Tasks 1-2, each chosen to be invisible to a shape-only check (row order swap, a second open row, a duplicated palette id, a dropped leading option, the preview moved below the rows, the card moved inside the form). Every failure message quoted in the SUMMARY. Plus the machine rule that no new check may contain the literal `18`. |
| T-30-22 | Repudiation | a ledger row deleted rather than paid | medium | mitigate | The guard asserts the ledger is non-empty and that every cleared row resolves to a real `def`; Task 3's action forbids deletion in as many words and requires a divergence note when a replacement's name differs from the prediction. |
| T-30-23 | Tampering | duplicate `id` on the page | low | mitigate | `_aspect_calendar_row_palette_populated_in_order()` asserts the palette carries no `id`, and the pre-existing `_the_rendered_settings_page_carries_no_duplicate_id` catches the symptom independently — cause and symptom are proven by two different checks, which is what makes this one non-vacuous. |
| T-30-24 | Information Disclosure / XSS | escaped-copy coverage | low | mitigate | `_rules_copy_appears_escaped_verbatim` keeps working unchanged because its `_rules_row_segment` helper is re-pointed rather than rewritten; asserted by the verify script requiring both the new helper name and the absence of the old one. |
| T-30-25 | Elevation of Privilege | new route / auth / CSP | low | accept | None. Test-only plan. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` — green at a re-derived count.
- `scripts/run-all-tests.sh` — green end to end.
- `git diff --exit-code` against plan 30-04's final commit for every file EXCEPT
  `companion/test_config_page.py` — this plan must not touch production code.
- Six mutation messages and the count trajectory in the SUMMARY.
</verification>

<success_criteria>
- Eight-plus replacement checks live, each asserting a relationship and deriving its counts.
- Zero ledger rows owed by 30-05; none deleted.
- Full suite green.
- No production file touched.
</success_criteria>

<artifacts_this_phase_produces>
Created in `companion/test_config_page.py`: `_aspect_card_full_shape_checklist()`,
`_aspect_card_covers_every_registered_theme_with_own_id_and_label()`,
`_aspect_card_default_selects_exactly_the_white_departures_option()`,
`_aspect_arrivals_row_carries_leading_option_no_checkbox()`,
`_aspect_arrivals_override_preselects_the_override_not_same_as_departures()`,
`_aspect_calendar_row_palette_populated_in_order()`, `_rules_row_renders_inside_aspect_after_form()`,
`_aspect_rules_row_label_locked_verbatim()`, `_rules_row_segment()` (renamed from
`_rules_panel_segment`), plus the repointed live-preview, scoped-render and pinned-English-strings
checks and a module-level tuple of retired-mechanism tokens asserted absent from the page.

Retired: `_rules_panel_segment` (renamed). No production symbol created or retired.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/30-aspect-rebuilt-one-tile-three-rows-one-palette-the-calendar-/30-05-SUMMARY.md` when done. It must carry the six mutation failure messages, the `EXPECTED_CHECK_COUNT` trajectory (274 → post-30-03 → now), and the cleared/still-owed ledger tallies.
</output>
