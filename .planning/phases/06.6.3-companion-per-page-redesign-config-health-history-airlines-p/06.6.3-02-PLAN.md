---
phase: 06.6.3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/static/style.css
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-08, D-18, D-20, D-22, D-23, UXA-06, UXA-08, UXA-16, UXA-18]

must_haves:
  truths:
    - "Every new CSS class this whole phase's five page plans reference by exact name already exists before any of those plans execute, so no page plan needs to touch companion/static/style.css."
    - "The dead .runway-option* rules D-05's card redesign supersedes are removed outright (not left as unreferenced bloat), matching this project's established 06.6.1-05 precedent."
    - "Native <details>/<summary> disclosures get a real 44px hit-target floor and a cross-browser marker color, since no prior rule in this file touches those elements at all."
  artifacts: []
  key_links:
    - ".dashboard-grid's new align-items: start applies to every existing consumer (Health, Airlines) without visually regressing either."
    - ".data-table-wrap's new sticky-header rule is scoped to its own scroll container, not the page, so it does not collide with the >=960px sticky sidebar."
---

<objective>
Land every new CSS rule this phase's five page-redesign plans (Config, Health, History, Airlines, Preview) need, in one pass, before any of them execute — so that no later plan in this phase touches `companion/static/style.css` and all five page plans can run with zero file-ownership conflicts on this shared stylesheet.

Purpose: `companion/static/style.css` is a single file; if multiple later plans each added their own slice of CSS, every pair of them would conflict on `files_modified` and be forced into separate waves, serializing work that is otherwise fully parallel (five independent Python page modules). Front-loading all CSS here removes that constraint entirely.
Output: Every class name named in `06.6.3-UI-SPEC.md`'s "New Component Contracts" and "Page-by-Page Design Contract" sections exists in `companion/static/style.css`, using only tokens already defined by Phase 06.6.2 (no new custom properties).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md
@companion/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config CSS (theme status, runway cards, dirty-state bar) + shared disclosure/grid CSS</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css (fieldset/legend/input/button rules near lines 626-716; the existing .runway-option*/.runway-option__number/.runway-option__image rules near lines 719-736 — the exact dead CSS this task removes; .visually-hidden near line 454, the reusable hidden-but-focusable utility class — note its real name is `.visually-hidden`, NOT `.sr-only`, correcting the UI-SPEC's own prose reference; .dashboard-grid near line 988; .theme-form/.theme-option near lines 486-513 for the sibling visual-weight precedent);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("New Component Contracts" — Native disclosure section; "Config" page section's Read-only Theme status markup, Runway selectable card markup, Dirty-state bar markup; "Color" section's accent-reserved-for list)
  </read_first>
  <action>
    Delete the three existing dead rules `.runway-option`, `.runway-option__number`, `.runway-option__image` outright (not commented out, not left in place) — D-05's card redesign in a later plan supersedes this markup entirely, matching this project's own `06.6.1-05` precedent of removing a superseded rule rather than leaving unreferenced bloat.

    Add `.theme-status` (a `<div>` replacing the old `<fieldset>`; give it its own padding/margin-bottom matching what the removed `fieldset` rule used to provide for this slot — `var(--space-md)` padding, `var(--space-lg)` margin-bottom — since a plain `<div>` inherits none of `fieldset`'s box styling and this must not visually collapse to zero spacing), `.theme-status__row` (flex row, `align-items: center`, `gap: var(--space-sm)`), `.theme-swatch` (inline flex, `gap: var(--space-xs)`), `.theme-swatch__chip` (a small ~16px rounded square or circle: `width: 16px; height: 16px; border-radius: 4px; display: inline-block;` — no border needed beyond the existing card surface's own contrast, per the UI-SPEC).

    Add `.runway-card` (the full `<label>` hit target: `display: block; position: relative;` — the `position: relative` anchor is required so the visually-hidden native radio absolutely positioned inside it stays contained rather than positioning against a further ancestor; plus the existing `.stat-tile`-style card treatment: `background: var(--color-dominant); border-radius: var(--radius-card); box-shadow: var(--shadow-card); padding: var(--space-md); margin-bottom: var(--space-md); cursor: pointer;`), `.runway-card--selected` (`border: 2px solid var(--color-accent);` — a real border, not merely a class toggle with no visual difference; adjust padding by `1px` less or use `box-sizing: border-box` so the added border doesn't shift layout against unselected siblings), `.runway-card__number` (reuse the same declarations the old `.runway-option__number` had: `font-size: var(--font-display-size); font-weight: var(--weight-semibold); line-height: 1.2;`), `.runway-card__image` (reuse the old `.runway-option__image`'s declarations: `display: block; max-width: 100%; height: auto; margin-top: var(--space-sm);`), `.runway-card__check` (positions the check glyph, e.g. `position: absolute; top: var(--space-sm); right: var(--space-sm); color: var(--color-accent); display: none;` shown only via `.runway-card--selected .runway-card__check { display: inline-flex; }` — the check icon should only be visually present on the selected card, even though its markup exists on every card, since the icon's own visibility must follow selection state without a second server-side conditional).

    Add `.dirty-bar` (`display: flex; align-items: center; gap: var(--space-md); padding: var(--space-md); background: var(--color-secondary); border-radius: var(--radius-card); margin-top: var(--space-md);` plus, scoped inside the existing `@media (min-width: 960px)` block, `position: sticky; bottom: 0;` — full-width and non-sticky below that breakpoint, per the UI-SPEC's explicit "sticky at >=960px only, full-width non-sticky in normal flow on mobile" instruction), `.dirty-bar__save` (no new rule needed — it is a bare `<button type="submit">`, which already gets the accent fill from the existing `button[type="submit"]` rule for free), `.dirty-bar__cancel` (`background: transparent; border: 1px solid var(--color-border); color: var(--color-text);` — an explicit override is required here since `.dirty-bar__cancel`'s own class-selector specificity is not automatically higher than the bare-element `button` rule's `background: var(--color-secondary)`; give this rule at least equal-or-later cascade weight using a class selector, matching the existing `.logout-form button` override's precedent immediately below `button[type="submit"]` in source order).

    Add the disclosure rules this codebase has never needed before (`grep` confirms zero existing `details`/`summary` rules): `summary { cursor: pointer; min-height: 44px; display: flex; align-items: center; }`, `summary::marker { color: var(--color-accent); }`, `summary::-webkit-details-marker { color: var(--color-accent); }` (both marker rules are required — Safari supports only the `-webkit-` form on `<summary>` specifically, unlike `<li>`). Add `.readings-disclosure` only if a wrapper-level rule is actually needed beyond the bare `summary` rule (e.g. `margin-top: var(--space-md);` on the `<details>` element itself, matching the section's existing rhythm) — do not invent additional properties beyond spacing.

    Locate the existing `.dashboard-grid` rule (~line 988) and add exactly one new declaration to it: `align-items: start;` — this is a one-line addition to the existing rule block, not a new rule, and it prevents Health's shorter Device/Pipeline tiles from stretching to the height of a taller sibling (UXA-06).
  </action>
  <acceptance_criteria>
    - grep for `.runway-option ` (with the trailing space, to avoid matching `.runway-option__*`) in companion/static/style.css finds zero matches.
    - grep for `.theme-status`, `.theme-swatch__chip`, `.runway-card--selected`, `.dirty-bar__cancel`, `summary::-webkit-details-marker` each finds at least one match.
    - The `.dashboard-grid { ... }` rule block contains the literal declaration `align-items: start;`.
    - `python3 -c "import re; s=open('companion/static/style.css').read(); assert '.runway-card {' in s and 'position: relative' in s.split('.runway-card {')[1].split('}')[0]"` exits 0 (confirms the position:relative anchor lands inside the .runway-card rule specifically, not some other rule).
  </acceptance_criteria>
  <verify>
    <automated>grep -c '\.runway-option ' companion/static/style.css | grep -qx 0 && grep -q '\.theme-status' companion/static/style.css && grep -q '\.runway-card--selected' companion/static/style.css && grep -q '\.dirty-bar__cancel' companion/static/style.css && grep -q 'summary::-webkit-details-marker' companion/static/style.css && echo OK</automated>
  </verify>
  <done>All Config-page and shared disclosure/grid CSS classes named in 06.6.3-UI-SPEC.md exist; the superseded .runway-option* rules are removed.</done>
</task>

<task type="auto">
  <name>Task 2: History/Airlines/Preview CSS (cards, sticky header, filter bar, copy button, matte frame)</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css (.data-table/.data-table-wrap/.data-table th rules near lines 745-767; .mobile-nav/.dashboard-sidebar's `display: none` + `@media (min-width: 960px)` breakpoint-toggle idiom near lines 353-385 and 1087-1172 — the exact pattern History's card/table toggle must reuse verbatim; .gallery-grid/.gallery-grid img near lines 1018-1028; input/select rule near lines 645-660 for the 44px floor and icon-affordance sizing precedent);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("History" page section's mobile card markup and desktop sticky-header note; "Preview" page section's matte-frame/gallery description; "Registry Safety"/"Color" for the filter input's accent focus ring)
  </read_first>
  <action>
    Add `.history-cards` (`list-style: none; margin: 0; padding: 0;` — a plain `<ul>` reset) and, at base (mobile) width, ensure `.data-table-wrap` (History's desktop table's own wrapper) gets `display: none;` while `.history-cards` stays visible by default; inside the existing `@media (min-width: 960px)` block, add the inverse: `.history-cards { display: none; } .data-table-wrap { display: block; }` — this must be the exact same `display`-based hide-not-clip technique `.dashboard-sidebar`/`.mobile-nav` already use, never a second toggling mechanism (e.g. never `visibility`/`opacity`). Add `.history-card` (the card surface: `background: var(--color-dominant); border-radius: var(--radius-card); box-shadow: var(--shadow-card); padding: var(--space-md); margin-bottom: var(--space-sm);`), `.history-card__primary` (flex row, `align-items: center; gap: var(--space-sm); justify-content: space-between;`), `.history-card__secondary` (flex row, `gap: var(--space-sm); opacity: 0.85; margin-top: var(--space-xs);`), `.history-card__time` (`margin-left: auto; font-size: var(--font-label-size);`), `.history-card__details` (`margin-top: var(--space-sm);` — the wrapper around the `<details>` element, if a wrapper is needed beyond the bare element; otherwise apply this spacing directly to the `<details>` selector scoped under `.history-card`).

    Add the desktop sticky table header, scoped to the History/Airlines table's own scroll container so it does not collide with the >=960px sticky sidebar: `.data-table-wrap th { position: sticky; top: 0; background: var(--color-secondary); z-index: 1; }` (the explicit `background` prevents body rows from visibly scrolling underneath a see-through sticky header; `z-index: 1` keeps it above scrolled row content without needing to compete with any higher-stacked element on the page).

    Add `.filter-bar` (flex row, `gap: var(--space-sm); align-items: center; margin-bottom: var(--space-md); flex-wrap: wrap;`), `.filter-bar__field` (a relatively-positioned wrapper around the `<input>` + decorative search icon, if the icon is rendered inside the input's visual bounds rather than beside it — `position: relative;` with the icon absolutely positioned `left: var(--space-sm); top: 50%; transform: translateY(-50%); pointer-events: none; opacity: 0.6;` and the `<input>` itself gaining left padding to clear the icon), `.filter-bar__count` (`font-size: var(--font-label-size); white-space: nowrap;`), `[data-filter-empty]` (reuses the existing `.empty-state` classes at the markup level — no new CSS rule needed here beyond ensuring `[hidden]` behaves correctly, which every browser already does natively for the `hidden` attribute; do not add a redundant `display: none` override).

    Add `.copy-btn` (icon-only compact button: inherits the global `button` rule's 44x44 floor — do not reduce `min-height`/`min-width` below 44px; add `padding: 0 var(--space-xs); background: transparent;` so it reads as a quiet inline affordance rather than a filled action button) and `.copy-feedback` (reuses `.visually-hidden`'s exact declarations at the markup level via the `visually-hidden` class rather than a second CSS rule — this class needs no new CSS entry, only correct markup usage in the page plans that consume it).

    Add `.preview-frame` (`display: flex; justify-content: center; background: var(--color-secondary); border: 1px solid var(--color-border); border-radius: var(--radius-card); padding: var(--space-lg); margin-bottom: var(--space-md);` — a bounded, centered container; the `<img>` inside it keeps its own existing `.preview-image` sizing rule, add `max-width: 100%; height: auto;` to `.preview-image` if that constraint is not already present, confirmed by reading the current rule first) and `.gallery-tile a` (`display: block;` — the existing `.gallery-grid img { width:100%; height:auto; display:block }` rule is a descendant selector and still applies through this new `<a>` wrapper, but the anchor's own default inline display can introduce a small baseline gap under the image without this explicit block rule).
  </action>
  <acceptance_criteria>
    - grep for `.history-cards`, `.history-card__details`, `.filter-bar__count`, `.copy-btn`, `.preview-frame`, `.gallery-tile a` each finds at least one match in companion/static/style.css.
    - The `@media (min-width: 960px)` block contains both `.history-cards { display: none; }` and a rule restoring `.data-table-wrap` to visible, confirming the breakpoint toggle is genuinely inverted between the two widths.
    - `.data-table-wrap th` (or an equivalently scoped selector, not the bare `.data-table th`) carries `position: sticky;`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '\.history-cards' companion/static/style.css && grep -q '\.filter-bar__count' companion/static/style.css && grep -q '\.copy-btn' companion/static/style.css && grep -q '\.preview-frame' companion/static/style.css && grep -q '\.gallery-tile a' companion/static/style.css && echo OK</automated>
  </verify>
  <done>All History/Airlines/Preview CSS classes named in 06.6.3-UI-SPEC.md exist, including the mobile-card/desktop-table breakpoint toggle and the sticky table header scoped to its own scroll container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is CSS-only; no new data enters the page, no new script executes, no new route is added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-04 | Denial of Service | None | low | accept | A CSS-only change carries no injection or availability surface; not applicable beyond the file growing in size, which is negligible |
</threat_model>

<verification>
`grep` the specific class names listed in each task's acceptance criteria against `companion/static/style.css`. Run `scripts/run-all-tests.sh` afterward (should be unaffected, since no Python or JS file changed) and confirm the same single pre-existing, unrelated `server/test_poll_loop.py` exception documented throughout STATE.md's history.
</verification>

<success_criteria>
- Every CSS class name referenced by 06.6.3-UI-SPEC.md's Config/Health/History/Airlines/Preview sections exists in companion/static/style.css before any later plan in this phase executes.
- The dead `.runway-option*` rules are removed, not left in place.
- No later plan in this phase needs to touch companion/static/style.css.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-02-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `companion/static/style.css` new rules: `.theme-status`, `.theme-status__row`, `.theme-swatch`, `.theme-swatch__chip`, `.runway-card`, `.runway-card--selected`, `.runway-card__number`, `.runway-card__image`, `.runway-card__check`, `.dirty-bar`, `.dirty-bar__cancel`, `summary`, `summary::marker`, `summary::-webkit-details-marker`, `.history-cards`, `.history-card`, `.history-card__primary`, `.history-card__secondary`, `.history-card__time`, `.history-card__details`, `.filter-bar`, `.filter-bar__field`, `.filter-bar__count`, `.copy-btn`, `.preview-frame`, `.gallery-tile a`
- `companion/static/style.css` removed rules: `.runway-option`, `.runway-option__number`, `.runway-option__image`
- `companion/static/style.css` modified rule: `.dashboard-grid` gains `align-items: start;`
</output>
