---
phase: 06.6.3
plan: 01
subsystem: ui
tags: [python-stdlib-html, es5-javascript, clipboard-api, html-escaping, static-assets]

# Dependency graph
requires:
  - phase: 06.6.2
    provides: page_header(), sidebar footer region, severity function, icon sprite convention
provides:
  - "layout.concise_timestamp_html(ts, now_ts, fallback) — D-09's concise clock-time + relative-age span"
  - "layout.data_table(raw_columns=()) — opt-in already-safe-markup cell bypass"
  - "layout.ICON_IDS grown 10->14 (icon-check/icon-copy/icon-refresh/icon-search)"
  - "4 new pre-auth static JS files: dirty-state.js, list-filter.js, copy-button.js, freshness.js"
  - "4 new companion/app.py routes serving those files, cross-file-guarded against layout.py's *_SCRIPT_SRC constants"
  - "page_shell() emits all 4 new script tags unconditionally on every authenticated page"
affects: ["06.6.3-02 (Config)", "06.6.3-03 (Health)", "06.6.3-04 (History)", "06.6.3-05 (Airlines)", "06.6.3-06 (Preview)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "raw_columns=() on data_table(): opt-in per-column escape bypass for already-safe builder output only, never a bare string"
    - "concise_timestamp_html(): full ISO demoted to title attribute, visible text reduced to HH:MM UTC (relative)"
    - "ES5-safe 'use strict' IIFE static JS files with early-return guard on an absent DOM marker (dirty-state.js/list-filter.js/copy-button.js/freshness.js join nav-dropdown.js/battery-trend.js)"

key-files:
  created:
    - companion/static/dirty-state.js
    - companion/static/list-filter.js
    - companion/static/copy-button.js
    - companion/static/freshness.js
  modified:
    - companion/layout.py
    - companion/app.py
    - companion/test_companion_app.py

key-decisions:
  - "Executed Task 3 (create the 4 static JS files) before finishing Task 2 (wire routes + add regression checks): Task 2's own <verify> command requires the 4 files to exist and return 200, but Task 2 is the only task that creates the routes serving files Task 3 creates — a plan-internal ordering dependency, not a scope change. All three tasks still ended up as their own atomic commits, in the order layout.py -> static JS files -> app.py+tests."
  - "Task 2's stated arithmetic ('5 route checks + 1 contract-guard check = 6') doesn't match the literal '4 new routes' this task and Task 3 actually ship (4, not 5) — added the real count (4 route checks + 1 contract-guard check = 5 new checks), bumping EXPECTED_CHECK_COUNT 80->85, not 80->86."

requirements-completed: [D-03, D-09, D-12, D-20, D-23, UXA-18]

coverage:
  - id: D1
    description: "layout.concise_timestamp_html() returns a title-carrying span with concise HH:MM UTC (relative) visible text for a real ISO pair, an escaped bare fallback for a falsy ts, and a raw-value-in-both-slots span for an unparseable ts — never raises"
    requirement: "D-09"
    verification:
      - kind: unit
        ref: "server/.venv/bin/python3 companion/test_companion_app.py — direct in-process call verified at implementation time (python -c script in plan's own <verify>)"
        status: pass
    human_judgment: false
  - id: D2
    description: "layout.data_table(raw_columns=) bypasses escaping only for named columns; every pre-existing call site (no raw_columns argument) stays byte-identical"
    requirement: "D-09"
    verification:
      - kind: unit
        ref: "companion/test_companion_app.py::_data_table_escapes_and_empty_state, _data_table_wrapped_for_horizontal_scroll (existing checks, unmodified — confirm backward compatibility) + plan's own inline verify script for the new raw_columns behavior"
        status: pass
    human_judgment: false
  - id: D3
    description: "ICON_IDS grows to 14 members with icon-check/icon-copy/icon-refresh/icon-search rendering via icon_html(); page_shell() emits exactly one sprite with all 14 symbols"
    requirement: "D-05, D-23, D-12, D-20"
    verification:
      - kind: unit
        ref: "companion/test_companion_app.py::_icon_sprite_integrity, _icon_html_whitelist_enforcement, _page_shell_emits_sprite_once_no_inline_styles"
        status: pass
    human_judgment: false
  - id: D4
    description: "4 new pre-auth static JS files (dirty-state.js/list-filter.js/copy-button.js/freshness.js) exist, ES5-safe, guard-on-absent-marker, and implement exactly the DOM-attribute contracts named in the UI-SPEC"
    requirement: "D-03, D-20, D-23, D-12"
    verification:
      - kind: unit
        ref: "plan's own inline shell verify script (use-strict/no-backtick/literal-substring/no-innerHTML/no-setInterval checks) — all passed"
        status: pass
    human_judgment: false
  - id: D5
    description: "companion/app.py serves the 4 new files pre-auth with the shared caching contract (text/javascript, max-age=300), cross-file-guarded against layout.py's matching *_SCRIPT_SRC constants"
    requirement: "D-03, D-20, D-23, D-12"
    verification:
      - kind: integration
        ref: "companion/test_companion_app.py::_static_script_public (x4) and _four_new_static_routes_dom_contract_guard, run against a real subprocess"
        status: pass
    human_judgment: false

# Metrics
duration: 45min
completed: 2026-08-31
status: complete
---

# Phase 06.6.3 Plan 01: Shared Cross-Page Foundation Summary

**layout.concise_timestamp_html()/data_table(raw_columns=), 4 new icon glyphs, and 4 new ES5-safe static JS files (dirty-state, list-filter, copy-button, freshness) wired pre-auth through app.py and page_shell()**

## Performance

- **Duration:** 45 min
- **Started:** 2026-08-31T13:52:29Z (STATE.md's "Phase 06.6.3 execution started")
- **Completed:** 2026-08-31T14:00:47Z
- **Tasks:** 3/3
- **Files modified:** 7

## Accomplishments
- `companion/layout.py` gained `concise_timestamp_html(ts, now_ts, fallback="no reading yet")` — D-09's raw-markup-producing "clock UTC (relative)" span with the full ISO demoted to `title`, matching `absolute_and_relative()`'s absolute-first ordering convention without deleting that function
- `data_table()` extended with `raw_columns=()` — a column-scoped escape bypass for already-safe builder output only, fully backward-compatible (both pre-existing Airlines call sites unchanged)
- `ICON_IDS` grew 10->14 (`icon-check`, `icon-copy`, `icon-refresh`, `icon-search`), with matching `ICON_DEFS_HTML` symbols following the sprite's exact existing visual language
- `page_shell()` now emits 5 deferred `<script>` tags (the pre-existing `nav-dropdown.js` plus 4 new ones) unconditionally on every authenticated page
- 4 new pre-auth static JS files shipped, each an ES5-safe `"use strict"` IIFE mirroring `nav-dropdown.js`/`battery-trend.js`'s guard-and-early-return convention: `dirty-state.js` (D-03 unsaved-changes bar), `list-filter.js` (D-20 generic row/card filter), `copy-button.js` (D-23 Clipboard API + `execCommand` fallback, no `innerHTML`), `freshness.js` (D-12/UXA-13 one-shot/one-deferred-`setTimeout` staleness banner, never `setInterval`, never recomputes server severity)
- `companion/app.py` gained 4 new route constants, path constants, one-line `_serve_*_script()` delegates, and pre-auth `do_GET` branches — all mirroring `_serve_nav_dropdown_script()`'s established shape exactly
- `companion/test_companion_app.py` grew 80->85 checks: 4 new public-route regression checks (200/`text/javascript`/non-empty/`max-age=300`) plus 1 new cross-file `*_SCRIPT_ROUTE`/`*_SCRIPT_SRC` DOM-contract-guard check; two pre-existing icon-count checks were updated in place (10->14, not counted as new)

## Task Commits

Each task was committed atomically, with Task 3 executed before Task 2 finished (see Deviations):

1. **Task 1: layout.py — concise_timestamp_html(), data_table(raw_columns=), 4 new icons, 4 script tags** - `9f178a4` (feat)
2. **Task 3: 4 new static JS files** - `4b15db7` (feat)
3. **Task 2: app.py — 4 new pre-auth static script routes** - `81ef8a7` (feat)

**Plan metadata:** commit pending (this SUMMARY + STATE.md update)

_Note: Task 1 was tdd="true"; RED was verified via the plan's own inline `<verify>` python script (confirmed `AttributeError: module 'companion.layout' has no attribute 'concise_timestamp_html'` against the unmodified file) before implementing GREEN — no separate test-file commit was made since Task 1's own `<files>` scope names only `companion/layout.py`._

## Files Created/Modified
- `companion/layout.py` - `concise_timestamp_html()`, `data_table(raw_columns=)`, 4 new icon ids/symbols, 4 new `*_SCRIPT_SRC` constants, `page_shell()` emits 4 more `<script>` tags
- `companion/app.py` - 4 new `*_SCRIPT_ROUTE` constants, path constants, `_serve_*_script()` delegates, pre-auth `do_GET` branches
- `companion/static/dirty-state.js` - D-03 unsaved-changes save/cancel bar
- `companion/static/list-filter.js` - D-20 generic client-side row/card filter (History + Airlines)
- `companion/static/copy-button.js` - D-23 Clipboard API + `execCommand` fallback copy buttons
- `companion/static/freshness.js` - D-12/UXA-13 one-shot staleness banner
- `companion/test_companion_app.py` - 4 new route checks + 1 cross-file contract-guard check; 2 pre-existing icon-count checks updated in place; `EXPECTED_CHECK_COUNT` 80->85

## Decisions Made
- Executed Task 3 before finishing Task 2 (see Deviations) — a plan-internal ordering fix, not a scope change; all three tasks still landed as their own atomic, correctly-attributed commits.
- Kept `absolute_and_relative()` fully intact (not deleted, not modified) per the plan's explicit instruction — `concise_timestamp_html()` is a sibling, not a replacement.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Reordered Task 3 (static JS files) ahead of finishing Task 2's test/verify work**
- **Found during:** Task 2 (app.py routes + regression checks)
- **Issue:** Task 2's own `<verify>` step (`server/.venv/bin/python3 companion/test_companion_app.py`, requiring `passed == total == EXPECTED_CHECK_COUNT`) adds checks that GET each of the 4 new routes and assert HTTP 200 with a real body. Those routes 404 until the files they serve exist — and Task 3, not Task 2, creates those files. Running Task 2's verification in the plan's literal task order would fail through no defect in Task 2's own code.
- **Fix:** Wrote `companion/app.py`'s route wiring first (constants, delegates, `do_GET` branches), then created all 4 static JS files (Task 3's full scope) and committed them as their own atomic `feat` commit, then returned to finish Task 2's test additions and `EXPECTED_CHECK_COUNT` bump and committed that separately. Each task's own `<files>` scope was respected exactly — only the *order* of the two commits changed, not their content or boundaries.
- **Files modified:** No file's final content changed as a result — same diff as if the tasks had executed in their stated order, just committed in file-creation order (Task 1 -> Task 3 -> Task 2) rather than numeric order (Task 1 -> Task 2 -> Task 3).
- **Verification:** `server/.venv/bin/python3 companion/test_companion_app.py` — 85/85 pass after both commits landed.
- **Committed in:** `4b15db7` (Task 3, JS files), `81ef8a7` (Task 2, routes + tests)

**2. [Rule 1 - Bug] Corrected Task 2's own stated check-count arithmetic**
- **Found during:** Task 2 (app.py routes + regression checks)
- **Issue:** The plan's action text computes "5 route checks + 1 contract-guard check = 6" as the `EXPECTED_CHECK_COUNT` bump, but only 4 new routes/files exist in this plan (dirty-state.js, list-filter.js, copy-button.js, freshness.js) — "one per new route" is therefore 4 checks, not 5.
- **Fix:** Added the real count — 4 route-check calls (one per new file) plus 1 cross-file contract-guard check = 5 new checks — and bumped `EXPECTED_CHECK_COUNT` 80->85 (not the plan's stated 86), documenting the corrected arithmetic in the file's own trailing comment.
- **Files modified:** `companion/test_companion_app.py`
- **Verification:** `passed == total == EXPECTED_CHECK_COUNT` (85/85) after the fix.
- **Committed in:** `81ef8a7` (Task 2 commit)

**3. [Rule 1 - Bug] Updated two pre-existing icon-count regression checks to match the new 14-icon contract**
- **Found during:** Task 1 (layout.py)
- **Issue:** `_icon_sprite_integrity()` and `_page_shell_emits_sprite_once_no_inline_styles()` (both pre-existing, from 06.6.1-04/06.6.2-05) hard-coded an expectation of exactly ten `ICON_IDS`/`<symbol>` occurrences — a correct assertion before this plan, now stale after `ICON_IDS` legitimately grew to 14.
- **Fix:** Updated both checks' hard-coded `10` to `14` and their failure-message text to match, following the exact precedent this project's own history documents for prior icon-count growth (06.6.2-05's own three similar updates).
- **Files modified:** `companion/test_companion_app.py`
- **Verification:** Both checks pass against the new 14-icon sprite; no other check in the suite hard-codes an icon or `<script>` tag count.
- **Committed in:** `9f178a4` (Task 1 commit)

---

**Total deviations:** 3 auto-fixed (1 blocking-issue task reordering, 2 bug fixes to stale/incorrect test assertions)
**Impact on plan:** All three are self-inflicted friction in the plan's own task ordering and arithmetic, not gaps in the plan's actual design intent. No scope creep — every file touched was already named in the plan's frontmatter `files_modified` list.

## Issues Encountered
None beyond the deviations documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Every downstream page plan (Config, Health, History, Airlines, Preview) can now call `layout.concise_timestamp_html()`, `layout.data_table(raw_columns=...)`, and reference the 4 new icon ids (`icon-check`/`icon-copy`/`icon-refresh`/`icon-search`) and 4 new static JS files' documented DOM-attribute contracts (`data-dirty-*`, `data-filter-*`, `data-copy-*`, `data-loaded-at`/`data-stale-banner`) by name — all shipped and regression-tested.
- `scripts/run-all-tests.sh`: 15/16 harnesses green, coverage 90% (above the pinned threshold); the sole failing harness is the single pre-existing, unrelated `server/test_poll_loop.py` macOS-local Pillow/FreeType `panel.bin` digest mismatch documented throughout STATE.md's history (this plan touches zero `server/` files, confirmed via `git diff --stat`).
- No blockers for 06.6.3-02 through 06.6.3-06.

---
*Phase: 06.6.3-companion-per-page-redesign-config-health-history-airlines-p*
*Completed: 2026-08-31*

## Self-Check: PASSED
All 7 created/modified files verified present on disk; all 3 task commit hashes (`9f178a4`, `4b15db7`, `81ef8a7`) verified present in git log.
