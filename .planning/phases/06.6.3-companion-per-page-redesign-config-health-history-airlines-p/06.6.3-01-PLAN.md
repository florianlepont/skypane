---
phase: 06.6.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/layout.py
  - companion/app.py
  - companion/static/dirty-state.js
  - companion/static/list-filter.js
  - companion/static/copy-button.js
  - companion/static/freshness.js
  - companion/test_companion_app.py
autonomous: true
requirements: [D-03, D-09, D-12, D-20, D-23, UXA-18]

must_haves:
  truths:
    - "layout.concise_timestamp_html() exists and returns already-safe HTML with the full ISO demoted to a title attribute (D-09)."
    - "layout.data_table() accepts a raw_columns=() parameter that bypasses escaping only for the named columns, with every pre-existing call site byte-identical (Pattern 3)."
    - "Four new whitelisted icon ids (icon-check, icon-copy, icon-refresh, icon-search) render via icon_html() and nowhere else."
    - "Four new pre-auth static JS files are served with the same shared-cacheable, ES5-safe, guard-and-early-return convention as nav-dropdown.js/battery-trend.js — none of them execute any logic on a page that doesn't render their marker attribute."
  artifacts:
    - companion/static/dirty-state.js
    - companion/static/list-filter.js
    - companion/static/copy-button.js
    - companion/static/freshness.js
  key_links:
    - "companion/app.py's 4 new *_SCRIPT_ROUTE constants == companion/layout.py's 4 new *_SCRIPT_SRC constants (cross-file contract, guarded by a test)."
    - "companion/layout.py's page_shell() emits exactly one <script defer> tag per new JS file, on every authenticated page."
---

<objective>
Ship the shared, cross-page foundation every one of this phase's five per-page redesign plans depends on: two extended `companion/layout.py` builders (`concise_timestamp_html()` for D-09's relative+concise timestamp format, and `data_table(raw_columns=)` for embedding that pre-built markup inside a table cell without double-escaping), four new whitelisted icon symbols, and four new pre-auth static JavaScript files (`dirty-state.js`, `list-filter.js`, `copy-button.js`, `freshness.js`) wired through `companion/app.py`'s existing static-script-serving pattern and emitted from every page via `page_shell()`.

Purpose: Every downstream page plan (Config, Health, History, Airlines, Preview) references these exact function signatures, icon ids, and DOM marker-attribute contracts by name — building them here, once, correctly, is what keeps the five page plans from re-deriving (and drifting on) the same primitives.
Output: `companion/layout.py` (concise_timestamp_html, data_table raw_columns, 4 icons, 4 script tags), `companion/app.py` (4 new routes), 4 new static JS files, `companion/test_companion_app.py` extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md
@companion/layout.py
@companion/app.py
@companion/static/nav-dropdown.js
@companion/static/battery-trend.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: layout.py — concise_timestamp_html(), data_table(raw_columns=), 4 new icons, 4 script tags</name>
  <files>companion/layout.py</files>
  <read_first>
    companion/layout.py (full file — absolute_and_relative() near line 364, data_table() near line 939, ICON_IDS/ICON_DEFS_HTML near line 93, page_shell() near line 678, NAV_DROPDOWN_SCRIPT_SRC near line 63);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md ("Code Examples" section: concise_timestamp_html()'s literal body, Pattern 3's data_table() diff);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("New Component Contracts" — New icons table, layout.concise_timestamp_html() section)
  </read_first>
  <behavior>
    - Test 1: concise_timestamp_html("2026-08-30T19:20:42+00:00", "2026-08-30T19:22:42+00:00") returns a span with the full ISO in the title attribute and a concise "HH:MM UTC (relative)" visible text.
    - Test 2: concise_timestamp_html(None, now) returns the escaped fallback string with no markup at all (bare text, not wrapped in a span).
    - Test 3: concise_timestamp_html("not-a-date", now) (unparseable ts) returns a span with the raw value in both the title and visible-text slots, never raises.
    - Test 4: data_table(["A"], [["<x>"]]) (no raw_columns) still contains the escaped entity form of "<x>" — existing behavior unchanged.
    - Test 5: data_table(["A"], [["<b>raw</b>"]], raw_columns=(0,)) contains the literal, unescaped "<b>raw</b>" — the new bypass works and is column-scoped.
  </behavior>
  <action>
    Add concise_timestamp_html(ts, now_ts, fallback="no reading yet") immediately after absolute_and_relative(): guard on a falsy ts by returning escape_html(fallback); parse ts via parse_iso() and compute age via age_seconds(); when either is None, return a span whose title and visible text are both the escaped raw ts value; otherwise format the parsed value's clock time as "%H:%M" and return a span whose title is the escaped full ISO and whose visible text is "{clock} UTC ({relative_age_text(age)})". Document explicitly in the docstring: this is a RAW-MARKUP-PRODUCING function — callers interpolate the return value verbatim, never re-escape it, and place it only in data_table()'s new raw_columns or directly in already-safe markup (never in a column outside raw_columns). Preserve absolute_and_relative()'s absolute-first ordering convention — do not reverse to relative-first, and do not delete absolute_and_relative() (it stays the right choice for plain-text-only call sites, e.g. Preview's no-panel caption).

    Extend data_table(headers, rows, mono_columns=()) to data_table(headers, rows, mono_columns=(), raw_columns=()). Inside the per-cell loop, replace the unconditional escape_html(cell) call with a branch: use the cell's raw value verbatim when its column_index is a member of raw_columns, otherwise escape_html(cell) as before. Update the docstring to name the raw_columns contract explicitly, matching stat_tile()'s content_html docstring language: only ever place the output of a builder that already escapes internally (concise_timestamp_html(), status_dot()) in a raw_columns cell — never a bare string. Confirm both of Airlines' existing data_table() call sites need zero changes (they pass no raw_columns argument, defaulting to the empty tuple).

    Extend ICON_IDS (currently a 10-entry tuple ending in "icon-nav-preview") with exactly 4 new members, appended in this order: "icon-check", "icon-copy", "icon-refresh", "icon-search" — the tuple grows to 14. Add 4 matching symbol blocks to ICON_DEFS_HTML's defs, each following the file's exact existing visual language (viewBox 0 0 20 20, fill none, stroke currentColor, stroke-width 1.5, round linecap/linejoin, built only from path/line/circle/rect primitives, no detailed illustration): icon-check a simple checkmark stroke; icon-copy two overlapping rounded rectangles (the standard "copy" glyph); icon-refresh a circular arrow (an open circle with an arrowhead); icon-search a magnifying glass (circle plus a short diagonal line). Exact path coordinates are your discretion within that constraint set — match the existing symbols' proportions (glyph roughly centered in the 20x20 viewBox, ~2-3px inset).

    Add 4 new route-src constants near the existing NAV_DROPDOWN_SCRIPT_SRC ("/static/nav-dropdown.js"): DIRTY_STATE_SCRIPT_SRC ("/static/dirty-state.js"), LIST_FILTER_SCRIPT_SRC ("/static/list-filter.js"), COPY_BUTTON_SCRIPT_SRC ("/static/copy-button.js"), FRESHNESS_SCRIPT_SRC ("/static/freshness.js") — each documented as "must equal companion/app.py's matching route constant exactly", mirroring NAV_DROPDOWN_SCRIPT_SRC's own comment.

    Modify page_shell()'s final return format string: immediately after the existing deferred nav-dropdown script tag and before the closing body tag, add 4 more deferred script tags, one per new file, and append the 4 new constants to the trailing format-argument tuple in the same order. Every one of these 4 new scripts is emitted unconditionally on every authenticated page (matching nav-dropdown.js/battery-trend.js's own "served everywhere, no-ops via guard clause" convention) — do not conditionally include per page.
  </action>
  <acceptance_criteria>
    - A direct call to concise_timestamp_html with a real ISO pair returns the exact "clock UTC (relative)" shape with the full ISO in the title attribute.
    - A direct call to concise_timestamp_html(None, anything) returns the bare fallback string with no HTML tags.
    - data_table(...) with no raw_columns still HTML-escapes every cell; the same call with raw_columns=(0,) leaves column 0 unescaped.
    - len(layout.ICON_IDS) == 14 and icon_html("icon-check")/icon_html("icon-copy")/icon_html("icon-refresh")/icon_html("icon-search") each render a non-empty svg (not the empty-string unrecognized-id fallback).
    - grep for the 4 new *_SCRIPT_SRC constant names in companion/layout.py finds each defined once and referenced once more inside page_shell()'s format arguments.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "import companion.layout as l; assert l.concise_timestamp_html(None,'x')=='no reading yet'; assert len(l.ICON_IDS)==14; assert l.icon_html('icon-check') and l.icon_html('icon-copy') and l.icon_html('icon-refresh') and l.icon_html('icon-search'); r=l.data_table(['A'],[['<b>x</b>']],raw_columns=(0,)); assert '<b>x</b>' in r; r2=l.data_table(['A'],[['<x>']]); assert '<b>x</b>' not in r2 and 'x' in r2; print('OK')"</automated>
  </verify>
  <done>concise_timestamp_html() and data_table(raw_columns=) exist with the exact contracts documented above; ICON_IDS has 14 members with matching symbols; page_shell() emits 4 additional deferred script tags for the new static files.</done>
</task>

<task type="auto">
  <name>Task 2: app.py — 4 new pre-auth static script routes</name>
  <files>companion/app.py, companion/test_companion_app.py</files>
  <read_first>
    companion/app.py (lines 80-100: route constants; lines 573-630: _serve_stylesheet/_serve_script_file/_serve_battery_trend_script/_serve_nav_dropdown_script; lines 700-730: do_GET's pre-auth branches for STYLE_ROUTE/SCRIPT_ROUTE/NAV_SCRIPT_ROUTE);
    companion/test_companion_app.py (the EXPECTED_CHECK_COUNT constant near line 64; the _stylesheet_public/_battery_trend_script_public/_nav_dropdown_script_public checks near lines 1090-1150 and the three-file DOM-contract-guard check near lines 913-948, which cross-checks companion.app.NAV_SCRIPT_ROUTE against layout.NAV_DROPDOWN_SCRIPT_SRC — the pattern this task's new checks must mirror);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md ("New static JS files — shared convention", Route wiring analog)
  </read_first>
  <action>
    Add 4 new route constants near NAV_SCRIPT_ROUTE ("/static/nav-dropdown.js"): DIRTY_STATE_SCRIPT_ROUTE, LIST_FILTER_SCRIPT_ROUTE, COPY_BUTTON_SCRIPT_ROUTE, FRESHNESS_SCRIPT_ROUTE — values must equal companion/layout.py's 4 new *_SCRIPT_SRC constants exactly (Task 1's output). Add 4 new path constants near _NAV_DROPDOWN_JS_PATH, each os.path.join(_HERE, "static", "<filename>") for dirty-state.js/list-filter.js/copy-button.js/freshness.js. Add 4 new one-line delegate methods mirroring _serve_nav_dropdown_script()'s exact shape (each calls self._serve_script_file(<its own path constant>) and nothing else). Add 4 new do_GET branches, each "if path == <ROUTE>: return self.<serve method>()", placed immediately alongside the existing NAV_SCRIPT_ROUTE branch — pre-auth, before any require_session() gate, matching that branch's own documented reasoning (a static asset carries no per-user data, so gating it would add a session round-trip for zero benefit).

    Add regression checks to companion/test_companion_app.py, one per new route, each mirroring _nav_dropdown_script_public()'s exact shape: GET the route against the running subprocess, assert status 200, assert "text/javascript" in the Content-Type header, assert a non-empty body, assert "max-age=300" in Cache-Control (the shared-cacheable contract _serve_script_file() already provides). Add one more check mirroring _three_file_nav_dom_contract_guard()'s cross-file-equality half: assert each of the 4 new companion.app.*_SCRIPT_ROUTE constants equals its matching companion.layout.*_SCRIPT_SRC constant, and assert layout.page_shell(title="T", active="health", body="<p>b</p>") contains a <script src="..."> tag for each of the 4 new routes. Read the file's current EXPECTED_CHECK_COUNT value and bump it by the exact number of new check(...) calls this task adds (5 route checks + 1 contract-guard check = 6), documenting the arithmetic in the existing trailing-comment style this file already uses.
  </action>
  <acceptance_criteria>
    - The 4 new *_SCRIPT_ROUTE constants in companion/app.py are byte-identical to companion/layout.py's 4 new *_SCRIPT_SRC constants.
    - Each of the 4 new routes, requested pre-auth against a running companion/app.py subprocess (no session cookie), returns 200 with a text/javascript content type and a non-empty body.
    - server/.venv/bin/python3 companion/test_companion_app.py exits 0 with passed == total == the file's own EXPECTED_CHECK_COUNT.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <done>4 new pre-auth static routes exist, serve their files with the established caching/content-type contract, and are cross-file-guarded against companion/layout.py's matching constants; the full harness passes at its own pinned count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 4 new static JS files (dirty-state.js, list-filter.js, copy-button.js, freshness.js)</name>
  <files>companion/static/dirty-state.js, companion/static/list-filter.js, companion/static/copy-button.js, companion/static/freshness.js</files>
  <read_first>
    companion/static/nav-dropdown.js (full file — the "use strict" IIFE, early-return-on-absent-marker, addEventListener conventions);
    companion/static/battery-trend.js (full file — the same conventions, plus its getAttribute()-not-dataset discipline);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("New static JS files" table — the per-file purpose and guard contract)
  </read_first>
  <behavior>
    - dirty-state.js: on load, snapshot every named form.elements value inside the single form[data-dirty-form] on the page; on any change/input event on that form, recompute how many fields differ from their snapshot; when the count is > 0, remove the hidden attribute from [data-dirty-bar] and set [data-dirty-count]'s textContent to "1 unsaved change" (count == 1) or "{count} unsaved changes" (count > 1); when the count returns to 0 (user reverted the value by hand), re-add hidden. [data-dirty-cancel]'s click calls form.reset() then re-hides the bar. Absent form[data-dirty-form] anywhere on the page: no-op.
    - list-filter.js: on [data-filter-input]'s input event, lowercase the query and, for every element carrying a data-filter-text attribute anywhere on the page, toggle its hidden attribute based on a plain substring match (indexOf) against that attribute's already-lowercased value; update [data-filter-count]'s textContent to "{visible} of {total} shown"; toggle [data-filter-empty]'s hidden attribute (shown only when the query is non-empty and zero rows match). [data-filter-clear]'s click empties the input and re-runs the same match logic. Absent [data-filter-input] anywhere on the page: no-op.
    - copy-button.js: each element carrying a data-copy-value attribute gets a click listener that tries navigator.clipboard.writeText(value) first (when the API exists), falling back to a hidden off-screen <textarea> + document.execCommand("copy") otherwise; on success, if the button's very next sibling element carries a data-copy-feedback attribute, set its textContent to "Copied" and clear it back to "" after 2000ms via setTimeout — never innerHTML, never a browser alert()/toast. Absent every data-copy-value element on the page: no-op.
    - freshness.js: reads the single [data-loaded-at] element's attribute value as an ISO timestamp; if [data-stale-banner] is absent or the timestamp fails to parse, no-op; otherwise compare against a named 600000ms (10 minute) threshold constant — reveal the banner (remove hidden) immediately if already past threshold, or schedule exactly one setTimeout for the remaining time until the threshold and reveal then. Never setInterval, never a network call, never recomputes any server-authoritative severity.
  </behavior>
  <action>
    Write all four files as a "use strict" IIFE matching nav-dropdown.js/battery-trend.js's exact opening shape (early return on the file's own absent marker query, ES5 only — var/function, no let/const/arrow functions/template literals/backticks, no framework, no network call beyond the Clipboard API). Each file's top-of-file comment states its purpose, its guard condition, and — for copy-button.js and freshness.js specifically — the exact security-relevant constraint it must never violate (copy-button.js: no innerHTML/HTML-writing sink anywhere in the file, only textContent and attribute reads; freshness.js: never recomputes Health's authoritative ok/warn/error severity client-side, only toggles a server-rendered banner's visibility).

    dirty-state.js: use form.elements (a live HTMLFormControlsCollection) to snapshot every element with a name attribute; re-scan the same collection on each change/input event rather than caching a stale element list.

    list-filter.js: query document.querySelectorAll('[data-filter-text]') fresh on every input event (do not cache the NodeList once at load, since the two responsive representations toggle visibility via CSS display, not DOM removal, so both a desktop <tr> and a mobile <li> for the same row exist simultaneously and both need filtering).

    copy-button.js: query document.querySelectorAll('[data-copy-value]') once at load (buttons are not dynamically added after render). The execCommand fallback must select() the temporary textarea's content before calling document.execCommand("copy"), and must remove the textarea from the DOM afterward regardless of success/failure (wrap in try/finally or an equivalent unconditional cleanup).

    freshness.js: parse the ISO string via `new Date(value)` and check `isNaN(parsed.getTime())` for the unparseable case — this file has no access to companion/layout.py's parse_iso(), so it must do its own defensive parse-or-noop rather than assuming the attribute is always well-formed.
  </action>
  <acceptance_criteria>
    - Each file, when read from disk, contains the literal string `"use strict"` and no backtick character (ES5-safety regression guard, matching this project's existing check on nav-dropdown.js).
    - dirty-state.js contains the literal substrings `data-dirty-form`, `data-dirty-bar`, `data-dirty-count`, `data-dirty-cancel`.
    - list-filter.js contains the literal substrings `data-filter-input`, `data-filter-text`, `data-filter-count`, `data-filter-clear`, `data-filter-empty`.
    - copy-button.js contains the literal substrings `data-copy-value`, `data-copy-feedback`, `clipboard`, `execCommand` and contains no occurrence of `.innerHTML`.
    - freshness.js contains the literal substrings `data-loaded-at`, `data-stale-banner`, `setTimeout` and contains no occurrence of `setInterval`.
  </acceptance_criteria>
  <verify>
    <automated>for f in dirty-state list-filter copy-button freshness; do grep -q '"use strict"' companion/static/$f.js && ! grep -q '`' companion/static/$f.js || (echo "FAIL $f" && exit 1); done; grep -c innerHTML companion/static/copy-button.js | grep -qx 0; grep -c setInterval companion/static/freshness.js | grep -qx 0; echo OK</automated>
  </verify>
  <done>Four new pre-auth static JS files exist under companion/static/, each following the project's established ES5-safe guard-and-early-return convention, implementing exactly the DOM-attribute contracts named above and nothing more.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| companion Python → data_table() raw_columns | An internal API surface: a future caller could pass a not-actually-pre-escaped string into raw_columns, reopening an XSS-shaped defect this phase's own design otherwise closes |
| browser clipboard fallback | copy-button.js's hidden `<textarea>` fallback path touches the DOM directly, though only ever with attribute-sourced values already rendered (and therefore already escaped) by the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-01 | Tampering | layout.data_table(raw_columns=) | medium | mitigate | Docstring states the contract explicitly (only pre-escaped builder output, never a bare string); the only two callers this phase adds (health_page.py, airlines_page.py) are reviewed in their own plans against this exact rule |
| T-06.6.3-02 | Information Disclosure | copy-button.js's execCommand fallback `<textarea>` | low | accept | The textarea is populated only from data-copy-value, an attribute the server already escaped when rendering it — no new data reaches the DOM that wasn't already visible on the page |
| T-06.6.3-03 | Denial of Service | 4 new pre-auth static routes | low | accept | Fixed-size static files served from disk, identical risk profile to the existing pre-auth /static/style.css and /static/nav-dropdown.js routes this phase's routes are modeled on directly |
</threat_model>

<verification>
Run `server/.venv/bin/python3 companion/test_companion_app.py` and confirm `passed == total == EXPECTED_CHECK_COUNT`. Run `scripts/run-all-tests.sh` and confirm no new failures beyond the single pre-existing, unrelated `server/test_poll_loop.py` macOS-local digest mismatch documented throughout STATE.md's history (this plan touches zero `server/` files).
</verification>

<success_criteria>
- companion/layout.py exposes concise_timestamp_html() and data_table(raw_columns=) with the exact contracts every downstream page plan references by name.
- 14 icon ids are whitelisted and render via icon_html().
- 4 new pre-auth static JS files exist, are served by companion/app.py with the established caching contract, and are emitted from every page by page_shell().
- companion/test_companion_app.py passes at its own newly-pinned EXPECTED_CHECK_COUNT.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-01-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `companion/layout.py::concise_timestamp_html(ts, now_ts, fallback="no reading yet")` — new function
- `companion/layout.py::data_table(headers, rows, mono_columns=(), raw_columns=())` — extended signature (new `raw_columns` parameter)
- `companion/layout.py::ICON_IDS` — 4 new members: `icon-check`, `icon-copy`, `icon-refresh`, `icon-search`
- `companion/layout.py::DIRTY_STATE_SCRIPT_SRC`, `LIST_FILTER_SCRIPT_SRC`, `COPY_BUTTON_SCRIPT_SRC`, `FRESHNESS_SCRIPT_SRC` — new constants
- `companion/app.py::DIRTY_STATE_SCRIPT_ROUTE`, `LIST_FILTER_SCRIPT_ROUTE`, `COPY_BUTTON_SCRIPT_ROUTE`, `FRESHNESS_SCRIPT_ROUTE` — new constants
- `companion/app.py::Handler._serve_dirty_state_script/_serve_list_filter_script/_serve_copy_button_script/_serve_freshness_script` — new methods
- `companion/static/dirty-state.js`, `companion/static/list-filter.js`, `companion/static/copy-button.js`, `companion/static/freshness.js` — new files
</output>
