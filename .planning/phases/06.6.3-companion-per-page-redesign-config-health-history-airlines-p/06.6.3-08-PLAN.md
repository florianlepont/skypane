---
phase: 06.6.3
plan: 08
type: execute
wave: 4
depends_on: ["06.6.3-03", "06.6.3-04", "06.6.3-05", "06.6.3-06", "06.6.3-07"]
files_modified: []
autonomous: false
requirements: [D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-12, D-13, D-18, D-20, D-22, D-23, UXA-01, UXA-05, UXA-06, UXA-08, UXA-11, UXA-13, UXA-16, UXA-17, UXA-18]

must_haves:
  truths:
    - "The full automated test suite is green across all seven prior plans composed together, with no new failure beyond the single pre-existing, unrelated server/test_poll_loop.py digest mismatch."
    - "A real human, on a real browser/phone, confirms every behavior this phase's automated suite structurally cannot verify: rendered visual hierarchy, real keyboard focus order, real screen-reader announcement, real clipboard writes, and real assistive-technology operability — closing this phase's own carried-forward deferred items."
  artifacts: []
  key_links:
    - "Every one of this phase's 16 decisions and 9 owned audit findings maps to at least one verified, checked-off item in the closing checklist below — none are silently skipped."
---

<objective>
Close Phase 06.6.3: confirm the fully-composed seven-plan worktree passes its own automated suite, then run the one verification this project's stdlib-only test harness structurally cannot automate — a real local browser/keyboard/phone pass across all five redesigned pages, plus the two items this phase's own CONTEXT.md explicitly deferred here (real assistive-technology testing and the production authenticated read-only walkthrough).

Purpose: Every prior plan in this phase (03-07) declared its own real-browser-dependent acceptance criteria as `human_judgment: true` scope, matching this project's own established closing-plan precedent (06.3-05, 06.6.1-06, 06.6.2-08). This plan is where that judgment is finally exercised, once every page in the phase is composed together.
Output: A full green suite run, and a recorded, itemized developer verdict in `06.6.3-08-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.2-companion-ux-accessibility-and-responsive-hardening/06.6.2-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite automated gate</name>
  <files></files>
  <read_first>
    scripts/run-all-tests.sh (the canonical harness enumeration);
    STATE.md (the documented, accepted, pre-existing server/test_poll_loop.py macOS-local digest-mismatch exception — confirm it is still the only non-zero harness before treating any other failure as pre-existing)
  </read_first>
  <action>
    Run `scripts/run-all-tests.sh` against the fully composed worktree (all seven prior plans of this phase merged/committed). Confirm every companion/test_*.py harness this phase touched (test_companion_app.py, test_config_page.py, test_status_pages.py, test_view_pages.py) passes at its own file's real, currently-pinned EXPECTED_CHECK_COUNT — not a value copied from any plan's draft text, since each prior plan computed its own bump from the file's real starting count at execution time. Confirm coverage stays at or above the project's pinned threshold (pyproject.toml). If the only non-zero harness is the single, already-documented, unrelated server/test_poll_loop.py macOS-local Pillow/FreeType digest mismatch (confirmed unrelated whenever this phase's plans touched zero server/ files — true for every plan in this phase), treat the run as green. Any other failure is a real gap: stop, do not proceed to Task 2, and report it instead of asking the developer to verify around a known-broken build.
  </action>
  <acceptance_criteria>
    - `scripts/run-all-tests.sh` exits with no unexpected non-zero harness (the single documented server/test_poll_loop.py exception is acceptable and expected).
    - Coverage is at or above the pinned threshold in pyproject.toml.
    - Every companion/test_*.py file's actual on-disk EXPECTED_CHECK_COUNT matches its own real passed/total count from this run.
  </acceptance_criteria>
  <verify>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>
  <done>The fully composed worktree's automated suite is green, with only the single pre-existing, unrelated, documented exception.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" id="06.6.3-08-02">
  <name>Task 2: Local browser/keyboard/phone verification pass across all five redesigned pages</name>

  <files>.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-08-SUMMARY.md</files>

  <action>
Present the 20 checklist items below (grouped by page, plus a cross-cutting group) to the developer, wait for their verdict, and record it. This is a blocking checkpoint: **stop and wait**. Do not proceed, do not infer answers, and do not fill in items the developer did not answer.

When the verdict arrives:
1. Record every item's outcome — pass, fail, or marginal — verbatim in the developer's own words, not paraphrased.
2. Turn each failed or marginal item into a discrete, actionable gap entry in `06.6.3-08-SUMMARY.md` — what was expected, what was seen, and which of this phase's prior plans (03-07) it traces to — so `/gsd-plan-phase --gaps` has something concrete to plan against if needed.
3. **Fix nothing.** A checkpoint that edits the thing it is verifying stops being a checkpoint.
4. Item 20 (production walkthrough) is optional if production access isn't available in this session — note that explicitly rather than skipping it silently.
  </action>

  <what-built>
    Every page in the companion web app has been recomposed per `06.6.3-UI-SPEC.md`: Config (dirty-state save bar, read-only Theme swatch, Runway selectable cards, renamed/de-duplicated Diagnostic LED section), Health (roving-tabindex battery chart, chart-before-collapsed-table ordering, specific anomaly summary, corroboration copy fix, Refresh/stale-view banner), History (mobile compact cards with a per-card details disclosure, human-readable state/runway labels, client-side filter, copy-to-clipboard), Airlines (promoted resolved-rate headline, simplified hierarchy, client-side filter), Preview (centered matte frame, task-oriented gallery captions, sized/lazy-loaded images, gallery-tile links, Refresh/stale-view banner).
  </what-built>
  <how-to-verify>
    Run the companion service locally (`server/.venv/bin/python3 companion/app.py --state-dir <a populated state dir with real history_db/device_health/gallery data> --geofence <geofence file>`) and sign in with a real browser. Work through each group below and note a genuine Pass/Fail/Observation for each — do not rubber-stamp:

    **Config**
    1. Change the Runway selection: confirm the dirty-state bar appears with "1 unsaved change", Save settings and Cancel both work as expected, and clicking anywhere on a runway card (not just the radio) selects it.
    2. Confirm the Theme section shows a read-only swatch (two color chips) with no radio buttons, reflecting the real on-glass panel colors.
    3. With JavaScript disabled (or devtools' "disable JavaScript"), confirm the page still renders correctly and the bottom Save Settings button still works — the dirty-state bar should simply never appear.
    4. Confirm "Diagnostic LED" appears once (not "Bring-up LED" anywhere), with one accessible group name, not two.

    **Health**
    5. Tab into the battery chart: confirm exactly one Tab stop lands on it (not 20); confirm Left/Right/Home/End move between readings and update the readout line, with no wrap past the first/last point.
    6. Confirm the readings table starts collapsed behind "View N readings" and the chart appears above it.
    7. Seed or observe a real warning-only anomaly (e.g. a corroboration disagreement) and confirm the banner names the actual category, not a generic message, and reads as a warning (not error-red) when nothing has actually failed.
    8. Click Refresh: confirm the page reloads with fresh data. Leave the tab open past 10 minutes (or temporarily lower the threshold for this one check, then revert) and confirm the stale-view banner appears without a page reload.

    **History**
    9. At a phone-width viewport, confirm one compact card renders per flight (not a cropped table), with callsign + relative time as the primary line and route + state as the secondary line; open a card's "More details" and confirm aircraft/airline, corroboration, runway, hex, and full timestamp are all present.
    10. Confirm confirmed_state renders as "Departing"/"Arriving" (not raw text) and the runway renders as its full label (not a bare id).
    11. Type into the filter box: confirm rows/cards filter live, the count updates, Clear resets it, and an unmatched query shows the empty-result message. Disable JavaScript and confirm the full unfiltered table/cards still work.
    12. Click a copy-callsign/copy-hex button: confirm the value lands on the real system clipboard and a transient "Copied" announcement appears.

    **Airlines**
    13. Confirm the resolved-rate percentage is the single most prominent figure near the top, with the unresolved-prefix list appearing before the resolution-statistics breakdown.
    14. Confirm the filter box works the same way History's does.

    **Preview**
    15. Confirm the live preview renders inside a centered, framed/matted treatment (not a raw edge-to-edge image), and the caption reads "Captured HH:MM UTC (relative)".
    16. Confirm gallery thumbnails show "Captured ..." captions (never raw filenames), each thumbnail opens/zooms to the full image when clicked, and scrolling the gallery shows no visible layout jump as images load.
    17. Confirm Refresh and the stale-view banner behave the same way Health's do.

    **Cross-cutting**
    18. A full keyboard-only pass across all five pages: confirm every new control (dirty-bar buttons, runway cards, filter inputs, copy buttons, disclosure summaries, Refresh links) is reachable and operable via keyboard alone, with a visible focus indicator at every stop.
    19. Real assistive-technology pass (VoiceOver, NVDA, or TalkBack — whichever is available): confirm the disclosures, filters, and copy-feedback announce sensibly and nothing announces silently or twice. (Carried forward from `06.6.1-UX-AUDIT.md`'s Wave 4 closing-verification step, explicitly deferred to this phase's own closing checkpoint per `06.6.3-CONTEXT.md`.)
    20. Production authenticated read-only walkthrough: if the developer has access to the live deployed companion service, a brief pass confirming the same five pages render correctly there too. (Also carried forward from the audit's Wave 4 step; skip with a note if production access isn't available in this session — not a hard blocker on closing the phase, since 03-07's own automated suites already prove correctness against the same code that would be deployed.)
  </how-to-verify>
  <resume-signal>Report each numbered item's Pass/Fail/Observation (a summary verdict per group is fine if every item in the group passed cleanly), or describe what needs fixing.</resume-signal>

  <verify>
    <human-check>The developer has returned a verdict covering all 20 checklist items across Config/Health/History/Airlines/Preview and the cross-cutting group, and every item's outcome is recorded verbatim in `06.6.3-08-SUMMARY.md`. `git status --porcelain -- companion/` is empty, proving nothing was fixed inside the checkpoint.</human-check>
    <automated>git status --porcelain -- companion/ | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>A human has confirmed, in a real local browser/phone, every behavior this project's stdlib test harness cannot verify on its own: dirty-state/theme/runway-card behavior, roving-tabindex keyboard nav, disclosure operability, mobile card layout, client-side filtering, copy-to-clipboard, matte-frame/gallery rendering, freshness/stale banners, a full keyboard-only pass, and (per this phase's own carried-forward deferred items) a real assistive-technology pass and, where available, a production authenticated walkthrough. Every outcome is recorded verbatim in `06.6.3-08-SUMMARY.md`, and no failed item was fixed inside this checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan verifies prior work; it introduces no new code, route, or data path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-16 | Repudiation | This checkpoint's own verdict | low | accept | The developer's verbatim, itemized verdict is recorded in `06.6.3-08-SUMMARY.md`, matching this project's own established checkpoint-recording precedent (06.6.2-08, 06.6.1-06) |
</threat_model>

<verification>
Task 1's automated suite result plus Task 2's recorded developer verdict together constitute this phase's full verification. Do not write `06.6.3-08-SUMMARY.md` until Task 2's checkpoint has actually resolved with the developer's own words — per this project's own established discipline, the executor must not infer or fabricate this verdict.
</verification>

<success_criteria>
- Full automated suite green (with only the single documented pre-existing exception).
- Every one of the 20 checklist items above has a genuine, recorded verdict — not a rubber stamp.
- Any Fail is either fixed directly (developer's choice, mirroring 06.6.1-06's precedent) or captured as an explicit, tracked follow-up — never silently dropped.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-08-SUMMARY.md` when done, itemizing every checklist group's verdict in the developer's own words, matching `06.6.2-08-SUMMARY.md`'s established format.

## Artifacts this phase produces (this plan's share)
None — this plan is verification-only and modifies no files.
</output>
