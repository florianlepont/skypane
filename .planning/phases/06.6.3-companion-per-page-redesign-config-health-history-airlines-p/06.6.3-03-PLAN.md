---
phase: 06.6.3
plan: 03
type: execute
wave: 2
depends_on: ["06.6.3-01", "06.6.3-02"]
files_modified:
  - companion/pages/config_page.py
  - companion/test_config_page.py
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-06, UXA-08]

must_haves:
  truths:
    - "Diagnostic LED replaces every user-facing occurrence of the internal identifier Bring-up LED, while Python identifiers stay unchanged (D-02)."
    - "The LED section's heading and fieldset legend no longer duplicate the same accessible group name (D-06)."
    - "Theme renders as a read-only status swatch reflecting the real on-glass-calibrated panel colors when exactly one theme exists, and automatically reverts to the editable radio group the moment a second theme is registered (D-04)."
    - "Runway options render as selectable cards where the entire card is the hit target, the selected card is visually distinct, and 0/1/2/3 supplied runway images all render correctly (D-05)."
    - "A dirty-state action bar appears only once a Theme/Runway value genuinely changes, offers Save settings and Cancel, and the pre-existing always-available server-rendered submit button at the bottom of the form is untouched (D-03)."
  artifacts: []
  key_links:
    - "config_page.py's runway_fieldset() output uses only CSS classes 06.6.3-02 already defined — no new class name invented here."
    - "The dirty-state bar's Save settings button submits the same <form action=\"/config\"> the bottom button submits — no second endpoint, no fetch()/XHR call."
---

<objective>
Recompose the Config page's Theme, Runway, LED, and Save affordances per D-02 through D-06: rename the LED section's user-facing copy, remove its duplicate heading, replace the single-option Theme fieldset with a read-only status swatch, rebuild the Runway fieldset as selectable cards, and add a JS-enhanced dirty-state save bar layered on top of the existing always-working no-JS submit path.

Purpose: These are the developer's own original three complaints from the phase's originating conversation (oversized/inconsistent runway radios, an awkwardly-placed Save button, a desire for a real runway diagram) plus the audit's UXA-08 heading-hierarchy findings, all landing in the one page file that owns them.
Output: `companion/pages/config_page.py` recomposed per `06.6.3-UI-SPEC.md`'s Config section; `companion/test_config_page.py` extended to cover every new branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md
@companion/pages/config_page.py
@companion/layout.py
@server/device_config.py
@server/panel_format.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 LED copy rename + D-06 heading dedup</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    companion/pages/config_page.py (led_fieldset(), led_section(), LED_HELPER_TEXT — lines 33-37, 177-210);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md (D-02, D-06)
  </read_first>
  <behavior>
    - Test 1: led_section(True) does not contain the substring "Bring-up LED" anywhere in its rendered output (case-sensitive; internal Python identifiers are unaffected since they never render).
    - Test 2: led_section(True) contains exactly one heading-or-legend-shaped accessible group name for the LED control, not two — i.e. the string "Diagnostic LED" appears exactly once as either an h2 or a legend, never both.
    - Test 3: led_fieldset(True)'s checkbox label text no longer reads "Enable bring-up LED" — it reads "Enable diagnostic LED".
  </behavior>
  <action>
    Rename every user-facing string containing "bring-up LED"/"Bring-up LED" to "diagnostic LED"/"Diagnostic LED": LED_HELPER_TEXT's "Controls the board's built-in bring-up LED..." becomes "Controls the board's built-in diagnostic LED..."; led_fieldset()'s hardcoded legend text "Bring-up LED" becomes "Diagnostic LED"; its checkbox label "Enable bring-up LED" becomes "Enable diagnostic LED". Do not rename the Python identifiers LED_HELPER_TEXT, led_fieldset, led_section, LED_CHECKBOX_VALUE, LED_ROUTE — this is a copy-only change (D-02's own explicit scope boundary).

    In led_section(), remove the `<h2 class="text-heading">Bring-up LED</h2>` line entirely, keeping only the `<fieldset><legend>Diagnostic LED</legend>...` structure as the section's sole accessible group name (D-06 — the fieldset/legend pair is the surviving one, matching the sibling Theme/Runway fieldsets inside `.config-form`, which already have no independent h2 of their own).
  </action>
  <acceptance_criteria>
    - "Bring-up LED" (any casing of the two words together) does not appear anywhere in config_page.py's rendered output for any render() call.
    - "Diagnostic LED" appears exactly once in led_section(True)'s output as an accessible group name (inside a `<legend>`), and zero times as an `<h2>`.
    - The Python identifier `led_fieldset` and the module constant `LED_HELPER_TEXT` are unchanged by name.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.config_page as c
out = c.led_section(True)
assert 'bring-up' not in out.lower(), 'stale internal identifier leaked into copy'
assert out.count('Diagnostic LED') == 1, 'expected exactly one accessible group name'
assert '<h2' not in out, 'expected the duplicate h2 removed'
assert 'Enable diagnostic LED' in out
print('OK')
"</automated>
  </verify>
  <done>Every user-facing LED string reads "Diagnostic LED"/"diagnostic LED"; the section has exactly one accessible group name (the fieldset legend), not two.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 read-only theme status + D-05 runway selectable cards</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    companion/pages/config_page.py (theme_fieldset(), runway_fieldset() — lines 105-174);
    server/device_config.py (THEMES, THEME_IDS, RUNWAYS, RUNWAY_IDS, theme_label(), runway_label() — confirm THEME_IDS has exactly one member today: "sky");
    server/panel_format.py (PALETTE_RGB — confirm it is a FLAT list of ints, not a list of (r,g,b) tuples: PALETTE_RGB[index*3 : index*3+3] is the (r,g,b) slice for palette index `index`; IDX_BLUE/IDX_GREEN);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("Config" page section — the literal Read-only Theme status markup and Runway selectable card markup);
    companion/static/style.css (.theme-status*, .runway-card* rules 06.6.3-02 already added — confirm the exact class names before using them)
  </read_first>
  <behavior>
    - Test 1: theme_fieldset("sky") — with device_config.THEME_IDS having exactly one member — renders the read-only .theme-status block (no `<input type="radio">` anywhere in its output), showing "Sky (default) · current" and two swatch chips whose inline background-color values are the real hex-encoded panel RGB for that theme's departing_index/arriving_index (not hardcoded literals).
    - Test 2: A synthetic THEME_IDS with 2+ members (monkeypatched or parameterized in the test) makes theme_fieldset() fall back to the original editable radio-group markup — this is a len() check, not a hardcoded single-theme assumption.
    - Test 3: runway_fieldset("3", images_available=()) renders three .runway-card elements, each a `<label>` wrapping a visually-hidden (not display:none) `<input type="radio">`, with the "3" card carrying runway-card--selected and the other two not.
    - Test 4: runway_fieldset("3", images_available=("3","06-24")) renders an `<img>` inside exactly the "3" and "06-24" cards, and no `<img>` inside the "02-20" card.
  </behavior>
  <action>
    Rewrite theme_fieldset(current_theme_id) to branch on `len(device_config.THEME_IDS) == 1`. In the single-theme branch, render the UI-SPEC's literal read-only markup: a `<p class="text-label">Theme</p>` legend-equivalent, a `.theme-status__row` holding a `.theme-swatch` with two `.theme-swatch__chip` spans (inline `style="background:#RRGGBB"` computed server-side — never hardcode the two hex strings; add a small local helper that slices `panel_format.PALETTE_RGB[index*3:index*3+3]` into an (r,g,b) tuple and formats it as `#%02X%02X%02X`, called once for `THEMES[theme_id]["departing_index"]` and once for `["arriving_index"]`), a `<span class="text-body">{theme_label} · current</span>`, and a `<p class="text-label">Additional themes will appear here once more are validated on real hardware.</p>` (this exact sentence replaces THEME_HELPER_TEXT's current "...once Phase 7 validates..." wording — update THEME_HELPER_TEXT's constant value itself, since UXA-05 flags the old wording as a leaked internal planning reference; keep using the constant, just change its text). In the `len() > 1` branch (unreachable today, reachable automatically the moment a second theme is registered), keep the exact original radio-group markup this function already emits — no logic change to that branch beyond it now living inside the `else`.

    Rewrite runway_fieldset(current_runway_id, images_available=()) to emit one `.runway-card` per `device_config.RUNWAY_IDS` entry: a `<label class="runway-card{ ' runway-card--selected' if selected else '' }">` wrapping a visually-hidden `<input type="radio" name="tracked_runway" value="{id}" class="visually-hidden"{checked}>` (use the exact existing `.visually-hidden` utility class from companion/static/style.css — it keeps the input in the DOM and keyboard-operable while hiding it visually, unlike `display:none`; do NOT invent or reference a `.sr-only` class, which does not exist in this codebase), a `<span class="runway-card__number">{label}</span>`, the existing conditional `<img class="runway-card__image" ...>` (unchanged wiring — reuse `RUNWAY_IMAGE_ROUTE_PREFIX`/`RUNWAY_IMAGE_ALT_TEMPLATE`/`images_available` exactly as today), and a `<span class="runway-card__check">{icon-check via layout.icon_html("icon-check")}<span class="visually-hidden">Selected</span></span>` (present in every card's markup; CSS shows it only on the selected card, so no server-side conditional is needed for the icon itself, only for the `runway-card--selected` class). Import `companion.layout` (already imported as `layout` in this file) to call `layout.icon_html("icon-check")`. The `runway-card--selected` class is computed server-side from the same `runway_id == current_runway_id` comparison the existing radio's `checked` attribute already uses — never a CSS `:has()` selector.

    Keep the module-level `RUNWAY_HELPER_TEXT` paragraph, rendered once after the card list (unchanged position/wording).
  </action>
  <acceptance_criteria>
    - With the real (unmodified) `device_config.THEME_IDS` (one member), `theme_fieldset("sky")`'s output contains zero `<input` occurrences and contains "Sky (default) · current".
    - The two swatch chip hex values in the rendered output match `panel_format.PALETTE_RGB`'s real departing/arriving triples for the "sky" theme, computed at test time from the module's own data (not hardcoded expected strings in the test).
    - `runway_fieldset("3")`'s output contains exactly three occurrences of `class="runway-card` and exactly one occurrence of `runway-card--selected`, on the "3" card.
    - `runway_fieldset("3", images_available=())`'s output contains zero `<img` occurrences; `runway_fieldset("3", images_available=("3",))`'s output contains exactly one.
    - THEME_HELPER_TEXT's rendered text no longer contains the substring "Phase 7".
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.config_page as c
from server import device_config, panel_format
out = c.theme_fieldset(device_config.DEFAULT_THEME_ID)
assert '<input' not in out
assert 'Sky (default) · current' in out
assert 'Phase 7' not in c.THEME_HELPER_TEXT
theme = device_config.THEMES[device_config.DEFAULT_THEME_ID]
dep = panel_format.PALETTE_RGB[theme['departing_index']*3: theme['departing_index']*3+3]
hexval = '#%02X%02X%02X' % tuple(dep)
assert hexval in out, 'expected departing swatch hex %r in output' % hexval
out2 = c.runway_fieldset('3')
assert out2.count('class=\"runway-card') == 3
assert out2.count('runway-card--selected') == 1
assert 'runway-card--selected' in out2.split('value=\"3\"')[0][-400:] or True
print('OK')
"</automated>
  </verify>
  <done>theme_fieldset() renders the read-only swatch when THEME_IDS has one member, falls back to radios otherwise; runway_fieldset() renders three selectable cards with server-computed selected state and correct 0/1/2/3-image rendering.</done>
</task>

<task type="auto">
  <name>Task 3: D-03 dirty-state action bar</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    companion/pages/config_page.py (render() — lines 390-424, the current `<form class="config-form" method="post" action="/config">...</form>` composition);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("Config" page section's Dirty-state bar markup and composition list — item 2's "sits directly below the two columns, above the always-visible Save Settings submit button — not replacing it");
    companion/static/dirty-state.js (06.6.3-01's output — confirm the exact data-dirty-* attribute contract it reads: form[data-dirty-form], [data-dirty-bar], [data-dirty-count], [data-dirty-cancel])
  </read_first>
  <action>
    In render(), add `data-dirty-form` to the existing `<form class="config-form" method="post" action="/config">` opening tag (a bare boolean attribute, no value needed). Immediately after the theme_fieldset()/runway_fieldset() output and before the always-visible `<button type="submit">Save Settings</button>`, insert the dirty-state bar markup as a genuine descendant of this same `<form>` (not a sibling, not positioned via a portal) so its own `<button type="submit">` submits the form natively via normal DOM nesting — no `form=` attribute needed, no new `id` needed on the `<form>` element: `<div class="dirty-bar" data-dirty-bar hidden role="status"><span data-dirty-count>1 unsaved change</span><button type="submit" class="dirty-bar__save">Save settings</button><button type="button" class="dirty-bar__cancel" data-dirty-cancel>Cancel</button></div>`. This is static, non-dynamic markup — no escaping needed, no server-side count computation (dirty-state.js computes and writes the count client-side; the server-rendered "1 unsaved change" is only the pre-JS/first-paint placeholder, immediately overwritten by the script once any field changes). The existing bottom `<button type="submit">Save Settings</button>` (Title Case, unchanged) remains exactly where it is — this is the no-JS fallback path and must not be removed or altered.
  </action>
  <acceptance_criteria>
    - render(ctx)'s output contains `data-dirty-form` on the `<form class="config-form"` opening tag.
    - render(ctx)'s output contains the `data-dirty-bar`, `data-dirty-count`, `data-dirty-cancel` markers, all inside the same `<form>` element as the existing `Save Settings` button (verify by index position: the dirty-bar markers appear before the closing `</form>` tag that follows the existing bottom button).
    - The existing bottom `<button type="submit">Save Settings</button>` (Title Case) is still present, unchanged, and appears after the dirty-bar markup in document order.
    - render(ctx)'s output still contains exactly one `<form class="config-form"` (no second/duplicate form was introduced).
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.config_page as c
ctx = {'device_config': {}, 'poll_cooldown_remaining': 0, 'runway_images': ()}
out = c.render(ctx)
assert 'data-dirty-form' in out
form_start = out.index('<form class=\"config-form\"')
form_end = out.index('</form>', form_start)
segment = out[form_start:form_end]
assert 'data-dirty-bar' in segment
assert 'data-dirty-count' in segment
assert 'data-dirty-cancel' in segment
assert 'Save Settings' in segment
assert out.count('<form class=\"config-form\"') == 1
print('OK')
"</automated>
  </verify>
  <done>The dirty-state bar is nested inside the Theme/Runway form, wired to dirty-state.js's exact attribute contract; the always-available no-JS submit button is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Runway/theme radio submission | Client-controlled form values (tracked_runway, theme) reach handle_post()'s existing server-side membership validation — unchanged by this plan |
| Runway card's server-computed selected class | Never a client-side `:has()` CSS trick; the class is derived from the same trusted `current_runway_id` value the existing radio `checked` attribute already used |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-05 | Tampering | dirty-state bar's Save settings button | low | accept | Submits the exact same pre-existing `<form action="/config">` handle_post() already validates server-side (membership test against THEME_IDS/RUNWAY_IDS) — the dirty-state bar adds zero new submission path |
| T-06.6.3-06 | Spoofing | Theme swatch hex derivation | low | accept | Hex values are computed server-side from `panel_format.PALETTE_RGB`, a trusted constant, never from client input |
</threat_model>

<verification>
`server/.venv/bin/python3 companion/test_config_page.py` — confirm `passed == total == EXPECTED_CHECK_COUNT` (the file's own real starting count plus this plan's new checks). Manual grep for `Bring-up LED`/`bring-up` across `companion/pages/config_page.py`'s rendered output finds zero matches.
</verification>

<success_criteria>
- LED copy renamed, duplicate heading removed.
- Theme renders read-only with real panel-color swatches when exactly one theme is registered; reverts to radios automatically with 2+.
- Runway renders as selectable cards, selected state server-computed, 0/1/2/3-image rendering all correct.
- Dirty-state bar present, wired to dirty-state.js's contract, no-JS fallback untouched.
- companion/test_config_page.py passes at its own newly-pinned EXPECTED_CHECK_COUNT.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-03-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `companion/pages/config_page.py::theme_fieldset()` — rewritten (read-only status when THEME_IDS has one member)
- `companion/pages/config_page.py::runway_fieldset()` — rewritten (selectable card markup)
- `companion/pages/config_page.py::led_section()` / `led_fieldset()` — copy renamed, duplicate heading removed
- `companion/pages/config_page.py::render()` — dirty-state bar markup added to the Theme/Runway form
- `companion/pages/config_page.py` new private helper for deriving a swatch hex from `panel_format.PALETTE_RGB`
</output>
