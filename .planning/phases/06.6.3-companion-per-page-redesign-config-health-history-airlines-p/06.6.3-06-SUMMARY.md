---
phase: 06.6.3
plan: 06
subsystem: ui
tags: [python-stdlib-html, es5-javascript, list-filter, data-density]

# Dependency graph
requires:
  - phase: 06.6.3-01
    provides: "layout.concise_timestamp_html(), layout.data_table(raw_columns=), layout.page_header(purpose=), icon-search, companion/static/list-filter.js"
  - phase: 06.6.3-02
    provides: ".filter-bar/.filter-bar__field/.filter-bar__count CSS rules (shared with History)"
  - phase: 06.6.3-04
    provides: "shared test_status_pages.py harness baseline (EXPECTED_CHECK_COUNT=55) this plan extends"
provides:
  - "airlines_page._resolved_headline_html()/render(): the resolved-rate figure is promoted to a bare .stat-tile__value figure directly under page_header(), no longer buried inside the demoted resolution-statistics stat_tile() (D-18)"
  - "airlines_page._registry_section()/_registry_table_html()/_registry_row_html(): the unresolved-prefix registry is the primary actionable list, hand-rolled (not layout.data_table()) so each row carries data-filter-text; First seen/Last seen render via layout.concise_timestamp_html() (D-09)"
  - "airlines_page._filter_bar_html(): D-20's client-side filter over the registry — the second consumer of companion/static/list-filter.js after History, with a non-<button> Clear control preserving D-16's read-only-by-design constraint"
affects: ["06.6.3-07 (Preview, next wave)", "06.6.3-08 (phase closing verification)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A page-scoped filter bar's Clear affordance implemented as a plain <a href=\"#{input_id}\"> rather than list-filter.js's own reference <button type=\"button\">, when the page carries a hard no-<button>-anywhere constraint (D-16) that a shared script's default markup shape would otherwise violate — the script's [data-filter-clear] attribute selector is element-agnostic, so no script change was needed."
    - "Splitting a stat_tile()'s content into two independently-computed halves (a promoted headline + a demoted table) that both consume one already-computed dict passed in by render(), rather than each half re-querying the database — extends this phase's established single-computation discipline (06.6.3-04's compute_health_state()) to a second page."

key-files:
  modified:
    - companion/pages/airlines_page.py
    - companion/test_status_pages.py

key-decisions:
  - "Used a plain <a href=\"#airlines-filter-input\" data-filter-clear>Clear</a> instead of History's <button type=\"button\" data-filter-clear>Clear</button> for the filter bar's Clear control — D-16's read-only-by-design constraint (no <button> anywhere on this page, unlike History) is unchanged and not reopened by this plan, and the plan's own files_modified scope excludes companion/static/style.css, so no new CSS could be added for an alternative styled control. Pointing the link at the filter input's own id doubles as a UX bonus: clicking Clear scrolls to and (per standard browser fragment-navigation focus behaviour) refocuses the input, ready for the next query."
  - "Dropped the standalone <h2 class=\"text-heading\">Coverage</h2> heading (the plan's Task 1 explicitly left this as an implementor's call) rather than keeping it above the reordered content — the promoted resolved-rate headline now serves the same orienting role, and a second heading directly above it would be redundant chrome, consistent with D-18's stated goal of simplifying the page's hierarchy."
  - "Hand-rolled the registry table's own <tr> markup (_registry_table_html()/_registry_row_html()) instead of extending layout.data_table() with a new row-attributes parameter, mirroring companion/pages/history_page.py's own precedent (_history_table_html()) for exactly the same reason: data_table()'s two other call sites (this page's own resolution-statistics table, and Health's battery table) needed to stay untouched, and a per-row data-filter-text attribute has no natural home in data_table()'s existing column-oriented API."

requirements-completed: [D-09, D-18, D-20, UXA-18]

coverage:
  - id: D1
    description: "The resolved-rate percentage is promoted out of the demoted stats tile to a standalone .stat-tile__value figure directly under page_header(), computed once via resolution_stats() and threaded to both the headline and the demoted table (no second query); the redundant Coverage heading is dropped."
    requirement: "D-18"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py::_promoted_headline_precedes_registry_table"
        status: pass
      - kind: unit
        ref: "companion/test_status_pages.py::_airlines_page_renders_one_dashboard_grid_of_two_tiles (retargeted)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The unresolved-prefix registry (now hand-rolled, not layout.data_table()) is the primary actionable list, rendered before the demoted resolution-statistics breakdown; First seen/Last seen switch to the D-09 concise timestamp format."
    requirement: "D-09, D-18"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py::_registry_timestamps_use_concise_format_with_filter_markers"
        status: pass
    human_judgment: false
  - id: D3
    description: "A client-side filter bar (label, live count, empty-result state, Clear action) exists over the unresolved-prefix registry, the second consumer of companion/static/list-filter.js; every row carries an escaped, lowercased data-filter-text attribute; the Clear control is a non-<button> element preserving D-16's read-only-by-design constraint (zero <form>/<button> anywhere on the page)."
    requirement: "D-20"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py::_registry_timestamps_use_concise_format_with_filter_markers, _filter_bar_absent_with_no_button_or_form_present, _airlines_page_only_icon_is_filter_search"
        status: pass
    human_judgment: false

# Metrics
duration: 25min
completed: 2026-08-31
status: complete
---

# Phase 06.6.3 Plan 06: Airlines Recomposition Summary

**Airlines' resolved-rate figure promoted to the page's single most prominent value, the unresolved-prefix registry now the primary actionable list with D-09 concise timestamps and a D-20 client-side filter bar whose Clear control is a plain link (not a `<button>`) to keep D-16's read-only-by-design constraint intact**

## Performance

- **Duration:** 25 min
- **Started:** 2026-08-31T15:07:36Z (STATE.md's "Completed 06.6.3-05-PLAN.md")
- **Completed:** 2026-08-31T15:22:41Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments
- `render()` now opens with `layout.page_header("Airlines", purpose="Route-resolution coverage and unresolved callsign prefixes.")` followed immediately by `_resolved_headline_html()` — a bare `<p class="stat-tile__value">{pct}% resolved</p>` figure, no longer wrapped inside the demoted stats tile — computed once from `resolution_stats()`'s result and threaded to both the headline and the demoted table (`_stats_table_html()`), never a second database query (D-18)
- The standalone `<h2 class="text-heading">Coverage</h2>` heading was dropped: the promoted headline now serves that orienting role, and a second heading above it would be redundant chrome
- The unresolved-prefix registry table is now hand-rolled (`_registry_table_html()`/`_registry_row_html()`, mirroring `history_page.py`'s own precedent) instead of `layout.data_table()`, so each row carries its own escaped, lowercased `data-filter-text` attribute — `data_table()`'s two other call sites (this page's own stats table, Health's battery table) stay byte-identical
- First seen/Last seen cells switch to `layout.concise_timestamp_html()` — already-safe markup interpolated verbatim, never re-escaped (D-09)
- A new `_filter_bar_html()` adds D-20's client-side filter over the registry (label "Filter by prefix", live `{N} of {total} shown` count, "No matching prefixes" empty state) — the second consumer of `companion/static/list-filter.js` after History. Its Clear control is a plain `<a href="#airlines-filter-input" data-filter-clear>Clear</a>` rather than History's `<button type="button">`, since D-16 forbids any `<button>` on this page and the plan's file scope excludes `style.css` for a new styled alternative
- `companion/test_status_pages.py` grew 55→58 checks: 3 new (promoted-headline document order, concise-timestamp + all-four-filter-markers presence, no-button/no-form regression over the filter bar) plus 2 pre-existing checks retargeted in place (the iconless check now expects exactly one `icon-search` instance once the registry has rows; the two-tiles check no longer asserts a Coverage heading)

## Task Commits

Each task was committed atomically:

1. **Task 1: D-18 headline promotion + simplified hierarchy** - `fde519a` (feat)
2. **Task 2: D-09 raw_columns timestamps + D-20 filter bar** - `674bc6b` (feat)

**Plan metadata:** commit pending (this SUMMARY + STATE.md update)

_Note: only Task 1 was `tdd="true"`. RED was verified by running Task 1's own inline `<verify>` script against the pre-implementation file state (the D-16 form/button assertion), confirmed it already passed trivially against the unmodified file (no behavior to make fail first — the check pins an invariant, not a new feature), then implemented the headline promotion and re-ran the same script GREEN; the new permanent regression checks mirroring the plan's `<behavior>` list were added to `companion/test_status_pages.py` in Task 2's own commit (not Task 1's, since Task 1's `<files>` scope excludes the test file)._

## Files Created/Modified
- `companion/pages/airlines_page.py` - `_resolved_headline_html()`/`_stats_table_html()` (split from the old `_stats_section()`), `render()` reordered + `purpose=` added, `_filter_bar_html()`/`_registry_row_html()`/`_registry_table_html()` (new), `_registry_section(rows, now)` signature extended
- `companion/test_status_pages.py` - 3 new checks, 2 pre-existing checks retargeted, `EXPECTED_CHECK_COUNT` 55→58

## Decisions Made
- Used a plain `<a href="#airlines-filter-input" data-filter-clear>Clear</a>` instead of History's `<button type="button" data-filter-clear>Clear</button>` for the filter bar's Clear control, since D-16's read-only-by-design constraint (zero `<button>` anywhere on this page) is unchanged and not reopened by this plan, and the plan's own `files_modified` scope excludes `companion/static/style.css` — no budget for new CSS to style an alternative control. Pointing the link at the filter input's own id is also a small UX bonus: clicking Clear scrolls to and refocuses the input for the next query.
- Dropped the standalone `<h2 class="text-heading">Coverage</h2>` heading (the plan's Task 1 explicitly left this as an implementor's call) rather than keeping it — the promoted resolved-rate headline now serves the same orienting role, consistent with D-18's simplification goal.
- Hand-rolled the registry table's own `<tr>` markup instead of extending `layout.data_table()` with a new row-attributes parameter, mirroring `history_page.py::_history_table_html()`'s own precedent for the identical reason (a per-row attribute need with no natural home in `data_table()`'s column-oriented API, and a requirement to keep that builder's other two call sites untouched).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Reworded a docstring comment to avoid tripping the plan's own literal D-16 verification grep**
- **Found during:** Task 2 (filter bar Clear control)
- **Issue:** The plan's own `<verification>` section requires `grep -c '<form\|<button' companion/pages/airlines_page.py` to report zero occurrences. An early draft of `_filter_bar_html()`'s docstring explained the Clear-control design choice using the literal substrings `` `<button>` `` and `` `<button type="button" data-filter-clear>` `` (prose, not rendered output) — which the literal grep command still matches, since it operates on the source file, not just render() output.
- **Fix:** Reworded the docstring to describe the constraint in prose ("a button element", "submit-type button") with zero literal `<button`/`<form` substrings anywhere in the file, while keeping the same explanatory content.
- **Files modified:** `companion/pages/airlines_page.py`
- **Verification:** `grep -c '<form\|<button' companion/pages/airlines_page.py` reports 0; `server/.venv/bin/python3 companion/test_status_pages.py` — 58/58 pass.
- **Committed in:** `674bc6b` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — self-inflicted friction in the plan's own literal verification command against explanatory prose, not a behavioral gap)
**Impact on plan:** No scope creep — both files touched were already named in the plan's frontmatter `files_modified` list, and the fix was a wording-only change with no markup or behavior impact.

## Issues Encountered
None beyond the deviation documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Airlines' D-09/D-18/D-20/UXA-18 scope for this phase is fully shipped and regression-tested; `grep -c '<form\|<button' companion/pages/airlines_page.py` confirmed zero.
- `scripts/run-all-tests.sh`: 15/16 harnesses green, coverage 91% (above the pinned threshold); the sole failing harness is the single pre-existing, unrelated `server/test_poll_loop.py` macOS-local Pillow/FreeType `panel.bin` digest mismatch documented throughout STATE.md's history (this plan touches zero `server/` files, confirmed via `git diff --stat fde519a~1 674bc6b`).
- `git diff --stat` across both task commits confirms exactly the plan's two declared `files_modified` were touched.
- No blockers for 06.6.3-07 (Preview) or 06.6.3-08 (the phase's closing verification plan) — neither depends on this plan's own output; Preview only shares the 06.6.3-01/02 foundation this plan also consumed.

---
*Phase: 06.6.3-companion-per-page-redesign-config-health-history-airlines-p*
*Completed: 2026-08-31*

## Self-Check: PASSED
Both modified files (`companion/pages/airlines_page.py`, `companion/test_status_pages.py`) verified present on disk; both task commit hashes (`fde519a`, `674bc6b`) verified present in git log.
