---
phase: 06.6.3
plan: 07
type: execute
wave: 3
depends_on: ["06.6.3-01", "06.6.3-02", "06.6.3-05"]
files_modified:
  - companion/pages/preview_page.py
  - companion/test_view_pages.py
autonomous: true
requirements: [D-09, D-10, D-12, D-18, D-22, UXA-16, UXA-17, UXA-18]

must_haves:
  truths:
    - "The live preview renders inside a centered, bounded matte frame evoking the panel's physical aspect ratio, not a raw <img> dropped in a card (D-18)."
    - "Every gallery caption reads as a captured time/state, never a raw filename (D-22)."
    - "The live-preview image and every gallery thumbnail carry explicit width/height plus the correct eager-vs-lazy loading hint, so populated Preview has negligible layout shift (UXA-16)."
    - "Preview shows an explicit Refresh action and, past the same 10-minute client-side threshold Health uses, a stale-view banner — independently gated from Health's own (D-12)."
    - "The Gallery heading states its display window (Latest 12 renders, D-10)."
  artifacts: []
  key_links:
    - "The gallery filename-to-timestamp reversal never raises on a malformed/foreign filename — it degrades to the raw-filename caption instead (Pitfall 2)."
    - "Each gallery tile's image is now wrapped in a same-src <a> for native open/zoom, reusing the existing /gallery/{name}.png route — no new route added."
---

<objective>
Recompose Preview per D-18's matte-frame treatment, D-22's task-oriented gallery captions (derived from the gallery filename's own embedded timestamp, not the raw filename), D-12's freshness/stale-view signal, D-10's window label, and UXA-16's image loading/sizing hints.

Purpose: Preview is the one page whose only content is images — UXA-16's layout-shift and lazy-loading findings apply here uniquely, and D-22's caption fix directly follows from RESEARCH.md's own Pitfall 2 finding (a naive filename-to-timestamp reversal mangles the date portion).
Output: `companion/pages/preview_page.py` recomposed per `06.6.3-UI-SPEC.md`'s Preview section; `companion/test_view_pages.py` extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md
@companion/pages/preview_page.py
@companion/layout.py
@server/poll_loop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-18 matte frame + UXA-16 preview-image sizing + D-09 caption fork resolution</name>
  <files>companion/pages/preview_page.py</files>
  <read_first>
    companion/pages/preview_page.py (full file — preview_section() lines 54-82);
    companion/static/style.css (.preview-frame — 06.6.3-02's output, confirm its exact class name);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md ("Preview page" section — the exact Required changes list for preview_section(), including the caption plain-text-vs-raw-HTML fork noted under "No Analog Found")
  </read_first>
  <behavior>
    - Test 1: preview_section() with a real panel present wraps its `<img class="preview-image">` inside a `<div class="preview-frame">`, and that img carries `width="1200" height="1600" loading="eager" decoding="async"`.
    - Test 2: preview_section() with a real panel present renders a caption reading "Captured {clock} UTC ({relative})" (the concise D-09 format), never the old bare "Captured {ISO} ({relative} ago)" plain-text shape, and never double-escaped.
    - Test 3: preview_section() with no panel yet renders the unchanged "No panel has been rendered yet." sentence, with no `<img>` and no `.preview-frame` wrapper at all (an honest sentence, not an empty/broken frame).
  </behavior>
  <action>
    Wrap the existing `<img class="preview-image" src="..." alt="Current panel preview">` in a `<div class="preview-frame">...</div>` (only in the branch where `mtime_iso` is truthy — the no-panel branch keeps its current img-less behavior, unchanged). Add `width="1200" height="1600" loading="eager" decoding="async"` to the `<img>` tag itself (the source panel's real pixel dimensions, matching `server/panel_format.py`'s documented 1200x1600 output size — reserves the aspect ratio before decode and marks this above-the-fold image as eagerly loaded and non-blocking-decode).

    Resolve the D-09 caption fork PATTERNS.md flags: today `caption_text = PREVIEW_CAPTION % absolute_and_relative(mtime_iso, ctx.get("now"))` produces a plain string that then passes through `escape_html()` at the `caption_html` line — this cannot host `concise_timestamp_html()`'s pre-built markup without double-encoding it. Restructure so the two branches build `caption_html` directly instead of going through the shared `caption_text`/`escape_html()` path: in the `mtime_iso`-present branch, build `caption_html = '<p class="text-label mono">Captured %s</p>' % layout.concise_timestamp_html(mtime_iso, ctx.get("now"))` (raw interpolation, no `escape_html()` wrapper — `concise_timestamp_html()` already returns safe markup); in the no-panel branch, keep `caption_html = '<p class="text-label mono">%s</p>' % escape_html(_NO_PANEL_CAPTION)` exactly as today (plain text, still escaped, since `_NO_PANEL_CAPTION` carries no markup of its own). The `PREVIEW_CAPTION` module constant's `"Captured %s"` template is now only meaningful in the mtime_iso branch's inline format string — update or retire the constant so it isn't left referencing dead code; document your choice in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `preview_section(ctx)` with a real `mtime_iso` present contains `<div class="preview-frame">` wrapping the `<img class="preview-image"` element, and that `<img>` tag contains `width="1200"`, `height="1600"`, `loading="eager"`, `decoding="async"`.
    - The same call's caption contains `class="mono" title=` (the concise-timestamp markup) and no double-escaped `&amp;lt;span` artifact.
    - `preview_section(ctx)` with no panel present contains no `<img` and no `.preview-frame` occurrence, and its caption is unchanged plain text.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import unittest.mock as m, companion.pages.preview_page as p
with m.patch('server.panel_preview.panel_file_mtime_iso', return_value='2026-08-30T19:00:00Z'):
    out = p.preview_section({'state_dir': '/tmp', 'now': '2026-08-30T19:05:00+00:00'})
    assert 'class=\"preview-frame\"' in out
    assert 'width=\"1200\"' in out and 'loading=\"eager\"' in out
    assert 'class=\"mono\" title=' in out
    assert '&amp;lt;span' not in out
with m.patch('server.panel_preview.panel_file_mtime_iso', return_value=None):
    out2 = p.preview_section({'state_dir': '/tmp', 'now': '2026-08-30T19:05:00+00:00'})
    assert '<img' not in out2 and 'preview-frame' not in out2
print('OK')
"</automated>
  </verify>
  <done>The live preview renders inside a matte frame with correct sizing/loading hints and the concise D-09 caption format; the no-panel state stays honest and img-less.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-22 gallery caption reverse-parse + D-10 window label + UXA-16 gallery sizing/links</name>
  <files>companion/pages/preview_page.py</files>
  <read_first>
    companion/pages/preview_page.py (gallery_tiles() lines 85-109);
    server/poll_loop.py (`_save_to_gallery()`'s filename-sanitisation contract — `now_iso.replace(":", "-") + ".png"` — confirm the exact literal shape before writing the reversal regex);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md ("Pitfall 2: Gallery filename → timestamp reversal is not a simple .replace('-', ':')" — the exact regex and function to implement, verbatim);
    companion/layout.py (concise_timestamp_html())
  </read_first>
  <behavior>
    - Test 1: `_gallery_name_to_iso("2026-08-30T19-20-42+00-00.png")` returns "2026-08-30T19:20:42+00:00" (only the time+offset portion's hyphens convert back to colons, the date portion's hyphens are untouched).
    - Test 2: `_gallery_name_to_iso("not-a-real-name.png")` (no "T" separator) returns None, never raises.
    - Test 3: `_gallery_name_to_iso("2026-08-30Tgarbage.png")` (a "T" present but a malformed time portion) returns None, never raises.
    - Test 4: gallery_tiles() with 2 real gallery-shaped filenames renders each tile's caption as "Captured {concise timestamp}", not the raw filename; each `<img>` carries `width="1200" height="1600" loading="lazy" decoding="async"`, and each is wrapped in `<a href="{same src}">`.
    - Test 5: gallery_tiles() with one malformed/foreign filename in the list still renders that one tile with a raw-filename-derived caption fallback (never raises, never a blank caption).
  </behavior>
  <action>
    Add a new private, never-raising function `_gallery_name_to_iso(name)` implementing the exact regex-guarded reversal RESEARCH.md's Pitfall 2 specifies: strip the trailing ".png" (if present) to get `stem`; if `"T"` is not in `stem`, return `None`; otherwise split on the first `"T"` into `date_part`/`time_part`; match `time_part` against a compiled regex `^(\d{2})-(\d{2})-(\d{2})([+-]\d{2})-(\d{2})$`; on no match, return `None`; on a match, rejoin as `"%sT%s:%s:%s%s:%s" % (date_part, hh, mm, ss, tz_sign_hh, tz_mm)`. Add the `import re` this needs at the top of the file.

    In `gallery_tiles()`, replace the current `caption = name[:-4] if name.endswith(".png") else name` raw-filename caption with: call `_gallery_name_to_iso(name)`; when it returns a real ISO string, build the caption as `"Captured %s" % layout.concise_timestamp_html(iso, ctx.get("now"))` interpolated as raw markup (not escaped a second time); when it returns `None` (malformed/foreign filename), fall back to today's existing raw-filename-derived caption text, still escaped via `escape_html()` exactly as today — this is the honest degradation path, never a crash and never a blank caption.

    Add `width="1200" height="1600" loading="lazy" decoding="async"` to each gallery `<img>` tag (off-screen thumbnails — always lazy, never eager, the opposite of the live preview's own hints from Task 1). Wrap each tile's `<img>` in `<a href="%s%s">` using the exact same `_GALLERY_ROUTE_PREFIX + escaped_name` value the `<img src>` already uses (UXA-16's "linking each gallery tile for native open/zoom" — no new route, the existing `/gallery/{name}.png` route already serves the full-size file).

    Add the D-10 window label to the Gallery section's own heading in `render()`: change `<h2 class="text-heading">Gallery</h2>` to include a label span stating the real `GALLERY_DISPLAY_LIMIT` value, e.g. `<h2 class="text-heading">Gallery <span class="text-label">— Latest %d renders</span></h2> % GALLERY_DISPLAY_LIMIT`.
  </action>
  <acceptance_criteria>
    - `_gallery_name_to_iso("2026-08-30T19-20-42+00-00.png")` returns exactly `"2026-08-30T19:20:42+00:00"`.
    - `_gallery_name_to_iso("garbage.png")` and `_gallery_name_to_iso("2026-08-30Tzzzz.png")` both return `None` without raising.
    - `gallery_tiles()`'s output for a real, well-formed gallery filename contains "Captured " followed by a `class="mono" title=` shaped substring, never the raw filename as the caption.
    - `gallery_tiles()`'s output for a deliberately malformed filename in the list still renders a non-empty caption for that tile and does not raise.
    - Every `<img>` inside `gallery_tiles()`'s output carries `loading="lazy"`; every such `<img>` is wrapped in an `<a href="...">` pointing at the same path as its own `src`.
    - `render()`'s Gallery heading contains "Latest 12 renders" (the real `GALLERY_DISPLAY_LIMIT` value).
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.preview_page as p
assert p._gallery_name_to_iso('2026-08-30T19-20-42+00-00.png') == '2026-08-30T19:20:42+00:00'
assert p._gallery_name_to_iso('garbage.png') is None
assert p._gallery_name_to_iso('2026-08-30Tzzzz.png') is None
out = p.gallery_tiles({'gallery_entries': ['2026-08-30T19-20-42+00-00.png', 'garbage.png'], 'now': '2026-08-30T19:25:00+00:00'})
assert out.count('loading=\"lazy\"') == 2
assert out.count('<a href=') == 2
assert 'Captured' in out
print('OK')
"</automated>
  </verify>
  <done>Gallery captions derive from the filename's own embedded capture timestamp with a safe raw-filename fallback for malformed names; every gallery image carries the correct lazy-loading/sizing hints and is wrapped in a same-src link; the Gallery heading states its real display window.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-12 freshness action + stale-view banner (independently gated from Health)</name>
  <files>companion/pages/preview_page.py, companion/test_view_pages.py</files>
  <read_first>
    companion/pages/preview_page.py (render());
    companion/layout.py (page_header(title, purpose=None, freshness_html=None, action_html=None));
    companion/static/freshness.js (06.6.3-01's output — the same data-loaded-at/data-stale-banner contract Health's Task 3 already used, this is its second, independent consumer)
  </read_first>
  <action>
    In `render()`, change the `layout.page_header("Preview")` call to `layout.page_header("Preview", purpose="What the physical frame is currently showing.", freshness_html=<Refresh link with data-loaded-at>)`, using the same construction Health's Task 3 used: `<a href="/preview" class="freshness-refresh" data-loaded-at="{escaped now}">{layout.icon_html("icon-refresh")}Refresh</a>`. Immediately after the header, add the identical hidden-by-default stale-view banner markup Health's Task 3 used (same static-text constant shape, `href="/preview"` this time): `<p class="banner banner--warn" data-stale-banner hidden role="status">This view may be out of date. <a href="/preview">Refresh</a> to see the latest.</p>`. This page's `[data-loaded-at]`/`[data-stale-banner]` pair is independently gated from Health's own (freshness.js queries the single such pair present on whichever page is currently loaded — Health and Preview are never rendered simultaneously in one document, so there is no risk of the script confusing the two).

    Extend companion/test_view_pages.py with checks covering: Preview's `data-loaded-at`/`data-stale-banner` markers present exactly once each with the `hidden` attribute on the banner; the promoted matte-frame/sizing/caption behaviors from Tasks 1-2 re-asserted against the full `render()` output (not just the isolated `preview_section()`/`gallery_tiles()` calls); the `_gallery_name_to_iso()` fixtures from Task 2. Read the file's current EXPECTED_CHECK_COUNT and bump it by the real total number of new check(...) calls this plan's three tasks add, following this project's established trailing-comment arithmetic convention.
  </action>
  <acceptance_criteria>
    - `render(ctx)`'s output contains exactly one `data-loaded-at` and exactly one `data-stale-banner`, the latter carrying the bare `hidden` attribute.
    - `render(ctx)`'s output still contains exactly one `layout.page_header` call's worth of `<h1 class="page-title">` (no second/competing header).
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 with `passed == total == EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>Preview carries a real Refresh link with a live data-loaded-at timestamp and a hidden-by-default stale-view banner, independently gated from Health's own; the full harness passes at its own newly-pinned EXPECTED_CHECK_COUNT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Gallery filename reversal (_gallery_name_to_iso) | Reads filenames from the gallery directory on disk — a manually-dropped or renamed file is not attacker-reachable over the network, but the parser must still never raise on an unexpected shape |
| freshness.js's stale-banner reveal | Client-only visibility toggle over a server-rendered, hidden-by-default element; never recomputes any server-authoritative state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-14 | Denial of Service (page-level) | _gallery_name_to_iso() against a malformed/foreign filename | medium | mitigate | Regex-match-or-None contract, never raises — verified by two dedicated malformed-input regression checks in Task 2 |
| T-06.6.3-15 | Tampering | data_table-adjacent raw markup in preview_section()/gallery_tiles() captions | low | mitigate | Only concise_timestamp_html()'s own pre-escaped output is interpolated raw; the malformed-filename fallback path still routes through escape_html() as today |
</threat_model>

<verification>
`server/.venv/bin/python3 companion/test_view_pages.py` — confirm `passed == total == EXPECTED_CHECK_COUNT`. Confirm `_gallery_name_to_iso()`'s two malformed-input fixtures both return `None` without raising.
</verification>

<success_criteria>
- Live preview renders in a matte frame with correct sizing/loading hints and the concise D-09 caption.
- Gallery captions derive from the filename's embedded timestamp with a safe fallback; every gallery image is lazy-loaded, sized, and linked.
- Gallery heading states its real display window.
- Refresh action and hidden-by-default stale-view banner present, independently gated from Health.
- companion/test_view_pages.py passes at its own newly-pinned EXPECTED_CHECK_COUNT.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-07-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `companion/pages/preview_page.py::preview_section()` — matte frame, sizing/loading hints, D-09 caption
- `companion/pages/preview_page.py::_gallery_name_to_iso()` — new never-raising helper
- `companion/pages/preview_page.py::gallery_tiles()` — D-22 captions, UXA-16 sizing/links, D-10 window label
- `companion/pages/preview_page.py::render()` — freshness_html + stale-view banner
</output>
