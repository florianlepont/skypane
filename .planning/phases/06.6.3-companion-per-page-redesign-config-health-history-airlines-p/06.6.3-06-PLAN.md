---
phase: 06.6.3
plan: 06
type: execute
wave: 3
depends_on: ["06.6.3-01", "06.6.3-02", "06.6.3-04"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [D-09, D-18, D-20]

must_haves:
  truths:
    - "The resolved-rate percentage is the single most prominent value on the page, promoted directly under the header rather than buried inside a stat-tile wrapper (D-18)."
    - "The unresolved-prefix registry is the visually dominant, primary actionable element; the resolution-statistics breakdown is demoted below it (D-18)."
    - "A client-side filter over the unresolved-prefix registry exists with a visible label, a clear action, a live result count, and an empty-result state (D-20)."
    - "First seen/Last seen render in the concise D-09 format."
    - "No decorative donut chart or any new visualisation is added — an explicit anti-goal both the audit and D-18 state."
  artifacts: []
  key_links:
    - "airlines_page.py's data-filter-text attributes are consumed only by 06.6.3-01's list-filter.js — no new script is added for Airlines specifically."
    - "The unresolved-prefix table's First seen/Last seen columns route through layout.data_table()'s raw_columns parameter — never a bare unescaped timestamp string."
---

<objective>
Recompose Airlines per D-18's simplified hierarchy (one prominent resolved-rate figure, the unresolved-prefix registry as the primary actionable list, resolution statistics demoted below it, no decorative chart) and add D-20's client-side filter over the already-rendered registry rows, plus D-09's concise timestamp format for First seen/Last seen.

Purpose: Airlines shares the audit's UXA-18 "no acceleration layer" finding with History, and D-18's own composition instruction reframes the page around what an operator actually needs to act on (the unresolved prefixes), not the statistics that currently dominate it visually.
Output: `companion/pages/airlines_page.py` recomposed per `06.6.3-UI-SPEC.md`'s Airlines section; `companion/test_status_pages.py` extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md
@companion/pages/airlines_page.py
@companion/layout.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-18 headline promotion + simplified hierarchy</name>
  <files>companion/pages/airlines_page.py</files>
  <read_first>
    companion/pages/airlines_page.py (full file — _registry_section() lines 164-177, _stats_section() lines 180-194, render() lines 197-213);
    companion/static/style.css (.stat-tile__value — already existing at line ~915, 24px/600, no new CSS needed);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("Airlines" page section's full composition list)
  </read_first>
  <behavior>
    - Test 1: render() with real stats produces the resolved-rate percentage rendered as a bare p.stat-tile__value figure directly under the page header (not wrapped inside a stat_tile() card), with the existing "over the last N days, M events" subtext immediately below it.
    - Test 2: render()'s document order places the unresolved-prefix registry section before the resolution-statistics breakdown table (currently the reverse tile order in the dashboard-grid).
    - Test 3: render() never contains a form or button element anywhere (D-16's read-only-by-design constraint, unchanged and not reopened by this plan).
  </behavior>
  <action>
    In render(), remove the resolved-rate headline from inside `_stats_section()`'s stat_tile() wrapper and promote it to a standalone element rendered directly after `layout.page_header("Airlines", purpose="Route-resolution coverage and unresolved callsign prefixes.")`: `<p class="stat-tile__value">{pct}% resolved</p><p class="text-label">over the last {window_days} days, {total} events</p>` — reuse `resolution_stats()`'s already-computed `resolved_pct`/`total` values exactly as `_stats_section()` already computes them today (no new calculation, no second call to `resolution_stats()`; thread the computed dict through instead of calling it twice). When `resolution_stats()`'s `total` is 0 (no events yet), render the existing `_NO_STATS_HEADING`/`_NO_STATS_BODY` empty_state() in this promoted slot instead of a headline with no data.

    Restructure the rest of `render()`'s body so the unresolved-prefix registry section (`_registry_section()`'s output) renders first, immediately after the promoted headline, and the remaining resolution-statistics breakdown (the four-source-category table, minus the headline that moved) renders after it — both still wrapped in their existing `stat_tile()`/`page-section` card treatment (no visual container removed, only the vertical order and the headline's location changed). Keep the existing `<h2 class="text-heading">Coverage</h2>` heading if it still makes sense above the reordered content, or drop it if the promoted headline now serves that orienting role — your call, document the choice in the plan's own SUMMARY.
  </action>
  <acceptance_criteria>
    - render(ctx) with real stats contains exactly one `class="stat-tile__value"` occurrence holding a "{N.N}% resolved" shaped string, appearing before the registry's own table markup in document order.
    - render(ctx)'s registry-table markup (or its enclosing section) appears before the resolution-statistics breakdown table in document order.
    - render(ctx) contains zero `<form` and zero `<button` occurrences (D-16 unchanged).
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import tempfile, companion.pages.airlines_page as a
with tempfile.TemporaryDirectory() as d:
    out = a.render({'state_dir': d, 'now': '2026-08-30T19:00:00+00:00'})
    assert '<form' not in out and '<button' not in out, 'D-16 violated'
    print('OK')
"</automated>
  </verify>
  <done>The resolved-rate figure is the single most prominent value on the page, positioned before the registry; the registry precedes the demoted statistics breakdown; no form/button exists anywhere on the page.</done>
</task>

<task type="auto">
  <name>Task 2: D-09 raw_columns timestamps + D-20 filter bar</name>
  <files>companion/pages/airlines_page.py, companion/test_status_pages.py</files>
  <read_first>
    companion/pages/airlines_page.py (_registry_section(), unresolved_rows(), render());
    companion/layout.py (data_table(raw_columns=), concise_timestamp_html() — 06.6.3-01's output);
    companion/static/list-filter.js (06.6.3-01's output — the data-filter-input/data-filter-count/data-filter-clear/data-filter-empty/data-filter-text attribute contract, identical to History's Task 3 usage);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md ("Pattern 3" — the exact literal diff for _registry_section(rows, now) needing now threaded down, and the two-column raw_columns=(2, 3) call site)
  </read_first>
  <action>
    Thread `now` down into `_registry_section(rows, now)` (its current signature takes only `rows`) and into `render(ctx)`'s call site (pull `ctx.get("now")`, matching how other pages already do this). Inside `_registry_section()`, before calling `layout.data_table()`, map each `(prefix, count, first_seen, last_seen, example_callsign)` row into a new tuple where index 2 (`first_seen`) and index 3 (`last_seen`) are replaced with `layout.concise_timestamp_html(first_seen, now, fallback="")` / `layout.concise_timestamp_html(last_seen, now, fallback="")`. Change the `layout.data_table(["Prefix", "Count", "First seen", "Last seen", "Example callsign"], rows, mono_columns=(0, 4))` call to add `raw_columns=(2, 3)`.

    Add a filter bar directly above the registry table (reusing the exact `[data-filter-input]`/`[data-filter-count]`/`[data-filter-clear]`/`[data-filter-empty]` contract History's plan already wired list-filter.js against — this is the second, not the first, consumer of that shared script, so no script change is needed here): label "Filter by prefix", the empty-result copy "No matching prefixes" / "Try a different search, or Clear filter to see all N prefixes." (N = the real row count). Add `data-filter-text="{escaped, lowercased prefix}"` to each registry `<tr>` — since `_registry_section()` currently builds its table entirely via `layout.data_table()` (which has no per-row attribute hook), either extend the row tuples with the prefix pre-computed and hand-roll this one table's `<tr>` attributes the same way History's own precedent required, or confirm whether `data_table()` needs a small additional `row_attrs` parameter — pick whichever approach keeps `data_table()`'s existing two other call sites (Airlines' own stats table, and Health's battery table) untouched, and document the choice.

    Extend companion/test_status_pages.py: assert the registry table's First seen/Last seen cells contain the concise-timestamp markup shape; assert the filter bar's five markers are present exactly once; assert a registry row carries `data-filter-text` holding the lowercased prefix. Read the file's current EXPECTED_CHECK_COUNT and bump it by the real number of new check(...) calls this task (plus Task 1's own checks, if any were added there) contributes, following this project's established trailing-comment arithmetic convention.
  </action>
  <acceptance_criteria>
    - `_registry_section(rows, now)`'s First seen/Last seen cells contain a `class="mono" title=` shaped substring, not a bare ISO string.
    - `render(ctx)`'s output contains exactly one each of `data-filter-input`, `data-filter-count`, `data-filter-clear`, `data-filter-empty`.
    - A registry row's markup carries a `data-filter-text` attribute holding the lowercased prefix value.
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 with `passed == total == EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>Airlines' First seen/Last seen use the concise D-09 format via data_table's raw_columns; a working filter bar (additive, degrades to the full list with no JS) exists over the unresolved-prefix registry; the full harness passes at its own newly-pinned EXPECTED_CHECK_COUNT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| data-filter-text on registry rows | Built from the unresolved-prefix registry, which airlines_page.py's own docstring documents as "hand-editable" — a slightly higher-trust-required source than pure machine-generated ADS-B data |
| data_table() raw_columns usage for First seen/Last seen | Only concise_timestamp_html()'s own pre-escaped return value is placed there — never a bare first_seen/last_seen string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-12 | Tampering | data-filter-text on a hand-editable registry field (prefix) | high | mitigate | escape_html() applied to the lowercased prefix before attribute interpolation, identical discipline to History's Task 3 — verified by a dedicated regression check |
| T-06.6.3-13 | Tampering | data_table(raw_columns=(2,3)) call site | medium | mitigate | Only concise_timestamp_html()'s output is passed in those two columns, never the bare first_seen/last_seen strings — reviewed against 06.6.3-01's documented raw_columns contract |
</threat_model>

<verification>
`server/.venv/bin/python3 companion/test_status_pages.py` — confirm `passed == total == EXPECTED_CHECK_COUNT`. Confirm `grep -c '<form\|<button' companion/pages/airlines_page.py` reports zero occurrences inside any render() output (D-16 unchanged).
</verification>

<success_criteria>
- Resolved-rate figure is the single most prominent value; registry precedes demoted statistics.
- First seen/Last seen use the concise D-09 format.
- A working, additive client-side filter exists over the registry.
- No decorative chart added; D-16's read-only constraint unchanged.
- companion/test_status_pages.py passes at its own newly-pinned EXPECTED_CHECK_COUNT.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-06-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `companion/pages/airlines_page.py::render()` — promoted resolved-rate headline, reordered composition, filter bar
- `companion/pages/airlines_page.py::_registry_section(rows, now)` — signature extended with `now`, First seen/Last seen switched to `concise_timestamp_html()` via `data_table(raw_columns=)`, `data-filter-text` attributes added
</output>
