---
phase: 06.6.3
plan: 04
type: execute
wave: 2
depends_on: ["06.6.3-01", "06.6.3-02"]
files_modified:
  - companion/pages/health_page.py
  - companion/static/battery-trend.js
  - companion/test_status_pages.py
autonomous: true
requirements: [D-08, D-09, D-10, D-12, D-13, UXA-05, UXA-06, UXA-11, UXA-13]

must_haves:
  truths:
    - "The battery readings table is collapsed behind a closed-by-default disclosure, and the chart appears before it in both DOM and visual order (D-08)."
    - "The anomaly banner names the actual failing category or categories instead of a generic 'check the tiles below' (UXA-06)."
    - "The corroboration disagreement explanation no longer contains a bare decision-ID parenthetical (UXA-05)."
    - "The battery chart adds exactly one stop to the page's Tab order; arrow keys move chronologically between readings without wrapping at either end (D-13/UXA-11)."
    - "Health shows an explicit Refresh action and, past a 10-minute client-side threshold, a stale-view banner — with the authoritative ok/warn/error severity always computed server-side, never recomputed in the browser (D-12/UXA-13)."
  artifacts: []
  key_links:
    - "battery-trend.js's new keydown handler reuses the file's existing reveal()/focus listener rather than duplicating the readout-writing logic."
    - "The freshness banner's data-loaded-at value is read only by freshness.js (06.6.3-01's output) — no new script is added for Health specifically."
---

<objective>
Recompose Health's information hierarchy and interaction model per D-08, D-09, D-10, D-12, D-13, and this phase's UXA-05/UXA-06/UXA-11/UXA-13 findings: collapse the raw readings table behind a disclosure and reorder the chart ahead of it, make the anomaly banner name its real cause, fix the one remaining decision-ID leak in the corroboration copy, cut the chart's 20-stop Tab cost to exactly one via roving tabindex, switch every visible timestamp on this page to the concise D-09 format, and add an explicit Refresh action plus a client-only stale-view warning.

Purpose: Health is the audit's primary "does not lead the operator to the problem" finding (UXA-06) and its primary keyboard-cost finding (UXA-11) — both are fixed here, on the page that owns them, using only the shared primitives 06.6.3-01/02 already built.
Output: `companion/pages/health_page.py` recomposed per `06.6.3-UI-SPEC.md`'s Health section; `companion/static/battery-trend.js` extended with a roving-tabindex keydown handler; `companion/test_status_pages.py` extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-CONTEXT.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md
@.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-PATTERNS.md
@companion/pages/health_page.py
@companion/static/battery-trend.js
@companion/layout.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Readings disclosure + reorder, specific anomaly copy, corroboration copy fix, D-10 label, D-09 timestamps</name>
  <files>companion/pages/health_page.py</files>
  <read_first>
    companion/pages/health_page.py (full file — _battery_section() lines 656-715, _battery_trend_section_html() lines 621-653, _anomaly_banner_text()/ANOMALY_BANNER_TEXT/_SEVERITY_BANNER_PREFIXES lines 136-156, 570-576, collect_anomalies() lines 395-428, _CORROBORATION_ROWS lines 101-114, _device_section()/_pipeline_section() lines 583-603, BATTERY_TREND_LIMIT line 77);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-UI-SPEC.md ("Health" page section composition list items 4 and 6; Copywriting Contract's "anomaly banner, specific" row; New Component Contracts' Native disclosure section);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md (Pitfall 1: confirmed real confirmed_state values — not relevant to this task's own scope, but confirms the general "trust the real code over the audit's prose" discipline this task's corroboration-copy fix follows too); Pattern 3 (data_table raw_columns) code example for _battery_section()
  </read_first>
  <behavior>
    - Test 1: _battery_section() with 3+ numeric-mv rows returns markup where the sparkline `<svg>` appears before a `<details class="readings-disclosure">` element, and that `<details>` carries no `open` attribute (closed by default).
    - Test 2: The `<summary>` inside that `<details>` reads "View {N} readings" where N equals the number of rows actually plotted.
    - Test 3: _anomaly_banner_text("warn") with a disagreement-only anomaly set names "ADS-B sources disagreed" in its output, not the old generic "check the tiles below" text verbatim as the whole message (the ANOMALY_BANNER_TEXT constant itself may still exist as a fallback tail, but the visible banner text must include the specific category name(s)).
    - Test 4: The corroboration disagreement row's rendered explanation text contains no substring matching the pattern "(D-" (a decision-ID leak) anywhere.
    - Test 5: _battery_trend_section_html()'s output contains the literal text "Latest 20 readings" (D-10's window label, using BATTERY_TREND_LIMIT's real value, not a hardcoded "20").
    - Test 6: The Device check-in and ADS-B pipeline rows' timestamps render via the new concise format (a `<span class="mono" title="...">` shape), not the old bare "ISO (Nm ago)" plain-text shape.
  </behavior>
  <action>
    In _battery_section(), replace the table_html's direct concatenation into the return value with a wrap: build the existing `layout.data_table(["Timestamp", "Battery (mV)"], table_rows, ...)` call exactly as today but change its Timestamp column to raw HTML via `layout.concise_timestamp_html(row.get("ts"), now, fallback="")` (drop the old `mono_columns=(0, 1)` down to `mono_columns=(1,)` and add `raw_columns=(0,)` — `concise_timestamp_html()`'s own `<span class="mono">` already carries the mono styling for column 0), then wrap the resulting table string in `<details class="readings-disclosure"><summary>View {N} readings</summary>{table_html}</details>` where N is `len(trend_rows)`. Reorder _battery_section()'s return so the sparkline/readout/script chart_block is concatenated BEFORE the disclosure-wrapped table (today's order is badge, table, chart — the new order is badge, chart, collapsed table).

    In _battery_trend_section_html(), add the D-10 window label immediately after BATTERY_SECTION_HEADING inside the `<h2>`: `<span class="text-label">— Latest %d readings</span> % BATTERY_TREND_LIMIT`, matching the constant's real value rather than a hardcoded "20".

    Switch _device_section() and _pipeline_section() to the new concise format: replace `detail = escape_html(layout.absolute_and_relative(ts, now))` with `detail = layout.concise_timestamp_html(ts, now)` (drop the escape_html() wrapper entirely — concise_timestamp_html() already returns pre-escaped-safe markup, and wrapping it in escape_html() a second time would double-encode and print the raw tags as visible text).

    Rewrite _anomaly_banner_text(severity) to accept the already-computed `collect_anomalies()` list (thread it through from render()/compute_health_state() rather than recomputing) and build the message as the existing severity-count-lead-in prefix (`_SEVERITY_BANNER_PREFIXES`) followed by a comma-joined, human-readable naming of the actual anomaly categories present, e.g. "1 warning: ADS-B sources disagreed once." / "2 warnings: Device check-in is stale, a battery reading shows an abnormal drop." — derive the category names directly from `collect_anomalies()`'s own four existing literal strings (do not invent new copy independent of that list; reuse/lightly reword its existing four strings so the two never drift). `compute_health_state()` must therefore also route the anomalies list through to wherever `_anomaly_banner_text()` is called (render()) — thread it as an additional value in the state dict this function already returns, alongside `severity`.

    Fix the corroboration "Disagreement" row's explanation string in `_CORROBORATION_ROWS`: remove the parenthetical "(D-04: the panel is left unchanged, not blanked)" entirely, replacing it with plain language carrying the same meaning, e.g. "...so nothing was selected that cycle — the panel image was kept from the previous cycle." (matches the audit's own suggested direction verbatim: "The two sources disagreed. The current panel image was kept." reworded to fit this row's existing sentence structure, without a decision-ID reference).
  </action>
  <acceptance_criteria>
    - `_battery_section()`'s returned markup, when it contains a chart, places the `<svg` before `<details class="readings-disclosure"` in string index order, and the `<details` tag carries no ` open` attribute.
    - The rendered `<summary>` text matches `View {N} readings` where N is the real plotted-row count for the fixture used.
    - `_anomaly_banner_text` (or its replacement call site in render()) produces text containing a real anomaly category phrase, not only the generic ANOMALY_BANNER_TEXT sentence alone.
    - No occurrence of the substring `(D-04` or `(D-` remains anywhere in health_page.py's `_CORROBORATION_ROWS`.
    - `_battery_trend_section_html("")`'s output (or any battery_html input) contains "Latest 20 readings".
    - `_device_section()`'s output for a real timestamp contains a `<span class="mono" title=` shaped substring, not a bare `ISO (Nm ago)` plain-text pattern with no markup.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.health_page as h
rows = [{'ts':'2026-08-30T19:%02d:00+00:00' % i,'battery_mv':4000-i} for i in range(5)]
badge_table_chart, state = h._battery_section(rows)
i_svg = badge_table_chart.index('<svg')
i_det = badge_table_chart.index('<details class=\"readings-disclosure\"')
assert i_svg < i_det, 'chart must precede the collapsed table'
assert ' open' not in badge_table_chart.split('<details')[1].split('>')[0]
assert 'View 5 readings' in badge_table_chart
section = h._battery_trend_section_html(badge_table_chart)
assert 'Latest 20 readings' in section
for _key,_label,_st,expl in h._CORROBORATION_ROWS:
    assert '(D-' not in expl, 'decision-ID leak: %r' % expl
row, st = h._device_section({'ts':'2026-08-30T19:00:00+00:00'}, '2026-08-30T19:05:00+00:00')
assert '<span class=\"mono\" title=' in row
print('OK')
"</automated>
  </verify>
  <done>Chart precedes a closed-by-default readings disclosure; the anomaly banner names real categories; the corroboration copy has no decision-ID leak; the D-10 window label is present; Device/Pipeline rows use the concise timestamp format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-13/UXA-11 roving-tabindex battery chart</name>
  <files>companion/pages/health_page.py, companion/static/battery-trend.js</files>
  <read_first>
    companion/pages/health_page.py (battery_sparkline_svg() lines 274-351 — the hit_targets loop);
    companion/static/battery-trend.js (full file — the per-point IIFE loop at lines 78-105, the existing reveal()/getAttribute()/focus-listener conventions);
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-RESEARCH.md ("Pattern 4: Roving tabindex extension of battery-trend.js" — the exact literal diff for both files)
  </read_first>
  <behavior>
    - Test 1: battery_sparkline_svg() with 5 rows produces exactly one `tabindex="0"` occurrence among its hit-target circles, and that one belongs to the chronologically-last (rightmost/latest) point.
    - Test 2: battery-trend.js, read from disk, contains a keydown listener responding to ArrowRight/ArrowDown/ArrowLeft/ArrowUp/Home/End, and a moveFocusTo()-shaped function that clamps (does not wrap) at both array bounds.
  </behavior>
  <action>
    In battery_sparkline_svg()'s hit_targets loop, replace the hardcoded `tabindex="0"` with a per-index conditional: `is_latest = index == len(pairs) - 1`; `tabindex = "0" if is_latest else "-1"`; interpolate `tabindex` into the existing `<circle ... tabindex="%s" ...>` format string in place of the literal `"0"`. The loop already iterates `pairs` in chronological order and the x-coordinate math already places the last index rightmost, so this one condition correctly identifies both "latest" and "rightmost" simultaneously — no second computation needed.

    Extend battery-trend.js in place (do not create a second file): add a `moveFocusTo(index)` function that clamps `index` to `[0, points.length - 1]` (never wraps), sets every point's `tabindex` attribute to `"0"` at the new index and `"-1"` everywhere else via `setAttribute` (never the `dataset`/property form, matching this file's own documented `getAttribute()`-not-`dataset` discipline), then calls `.focus()` on the newly-current point — `.focus()` fires the file's existing `"focus"` event listener, which already calls `reveal()`, so no duplicate reveal call is needed here. Inside the existing per-point IIFE loop (alongside its current click/mouseenter/focus/keydown(Enter/Space) listeners — add this as a separate `keydown` listener registration, do not merge it into the existing Enter/Space keydown listener), add a `keydown` listener that reads `evt.key`: `ArrowRight`/`ArrowDown` calls `moveFocusTo(currentIndex + 1)`; `ArrowLeft`/`ArrowUp` calls `moveFocusTo(currentIndex - 1)`; `Home` calls `moveFocusTo(0)`; `End` calls `moveFocusTo(points.length - 1)` — each branch calls `evt.preventDefault()` first (so arrow keys don't also scroll the page). Compute `currentIndex` via `Array.prototype.indexOf.call(points, point)` (ES5-safe against a NodeList, no `Array.from()`).
  </action>
  <acceptance_criteria>
    - `battery_sparkline_svg()`'s output for N>=2 rows contains exactly one occurrence of `tabindex="0"` and N-1 occurrences of `tabindex="-1"` among its hit-target circles.
    - `battery-trend.js` contains the literal strings `ArrowRight`, `ArrowLeft`, `Home`, `End`, `moveFocusTo`, and does not contain `Array.from(`.
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at its own EXPECTED_CHECK_COUNT.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.health_page as h
rows = [{'ts':'2026-08-30T19:%02d:00+00:00' % i,'battery_mv':4000-i} for i in range(5)]
svg = h.battery_sparkline_svg(rows)
assert svg.count('tabindex=\"0\"') == 1
assert svg.count('tabindex=\"-1\"') == 4
print('OK')
"; grep -q 'ArrowRight' companion/static/battery-trend.js && grep -q 'moveFocusTo' companion/static/battery-trend.js && ! grep -q 'Array.from(' companion/static/battery-trend.js && echo OK</automated>
  </verify>
  <done>Exactly one chart point carries tabindex="0" (the latest reading); arrow/Home/End keys move focus chronologically with no wrap at either end, reusing the existing reveal() logic via .focus().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-12/UXA-13 freshness action + stale-view banner</name>
  <files>companion/pages/health_page.py</files>
  <read_first>
    companion/pages/health_page.py (render() lines 781-827, page_header() call site);
    companion/layout.py (page_header(title, purpose=None, freshness_html=None, action_html=None) — the literal signature contract this task must plug into, not build a second header element);
    companion/static/freshness.js (06.6.3-01's output — confirm the exact data-loaded-at / data-stale-banner attribute contract it reads)
  </read_first>
  <action>
    In render(), build a `freshness_html` fragment: `<a href="/health" class="freshness-refresh" data-loaded-at="{escaped now ISO}">{layout.icon_html("icon-refresh")}Refresh</a>` — pass `ctx.get("now")` (already computed once per request by page_context()) as the ISO value, escaped via `layout.escape_html()`. Pass this fragment into `layout.page_header("Health", freshness_html=freshness_html)` — do not add a second, competing header element. Immediately after the `page_header(...)` call's output (before `_source_fault_block(...)`), add a stale-view banner block, hidden by default: `<p class="banner banner--warn" data-stale-banner hidden role="status">This view may be out of date. <a href="/health">Refresh</a> to see the latest.</p>` — this is static, non-dynamic markup (contains no user-controlled or request-specific data), so it can be written directly as a module-level constant string rather than built with escape_html() at the render() call site. `freshness.js` (06.6.3-01's output) reveals this banner client-side past the 10-minute threshold — render() never computes or predicts staleness itself, and the authoritative `ok`/`warn`/`error` severity computed elsewhere on this page is never touched by this addition.
  </action>
  <acceptance_criteria>
    - render(ctx)'s output contains exactly one element with the `data-loaded-at` attribute, and its value is a real ISO timestamp string (not empty, not a literal placeholder).
    - render(ctx)'s output contains exactly one element with the `data-stale-banner` attribute, and that element carries the bare `hidden` attribute (hidden by default at render time).
    - The stale-view banner's visible text contains "may be out of date" and a `Refresh` link.
    - `layout.page_header(...)` is still called exactly once in render() (no second/competing `<h1 class="page-title">`).
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 -c "
import companion.pages.health_page as h
ctx = {'state_dir': '/tmp/does-not-exist-%d' % id(object()), 'now': '2026-08-30T19:00:00+00:00'}
out = h.render(ctx)
assert 'data-loaded-at=\"2026-08-30T19:00:00+00:00\"' in out
assert out.count('data-stale-banner') == 1
seg = out[out.index('data-stale-banner')-80:out.index('data-stale-banner')+40]
assert 'hidden' in seg
assert 'may be out of date' in out
assert out.count('<h1 class=\"page-title\"') == 1
print('OK')
"</automated>
  </verify>
  <done>Health carries a real Refresh link with a live data-loaded-at timestamp and a hidden-by-default stale-view banner; freshness.js is the sole consumer, never the server, of the staleness decision itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anomaly banner category naming | Sourced entirely from collect_anomalies()'s own fixed, hardcoded literal strings — no request-controlled input ever reaches this text |
| freshness.js's stale-banner reveal | Client-only visibility toggle over a server-rendered, hidden-by-default element; never recomputes or overrides the server-authoritative health severity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.3-07 | Tampering | data_table() raw_columns usage in _battery_section() | medium | mitigate | Only concise_timestamp_html()'s own pre-escaped return value is placed in the raw_columns cell — never a bare row.get("ts") string, matching Task 1's action text and the layout.py docstring contract from 06.6.3-01 |
| T-06.6.3-08 | Denial of Service (page-level) | freshness.js's one-shot setTimeout | low | accept | A single deferred timer per page load, never setInterval, never a network call — no resource-exhaustion surface |
</threat_model>

<verification>
`server/.venv/bin/python3 companion/test_status_pages.py` — confirm `passed == total == EXPECTED_CHECK_COUNT`. `grep -c '(D-' companion/pages/health_page.py` on the rendered corroboration explanations specifically (not docstrings) must be zero for the `_CORROBORATION_ROWS` tuple's own explanation strings.
</verification>

<success_criteria>
- Chart precedes a closed-by-default readings disclosure; Device/Pipeline/Battery timestamps use the concise D-09 format.
- Anomaly banner names real categories; corroboration copy has no decision-ID leak; D-10 window label present.
- Exactly one chart point is a normal Tab stop; arrow/Home/End move chronologically with no wrap.
- Refresh action and hidden-by-default stale-view banner present, driven entirely by freshness.js client-side.
- companion/test_status_pages.py passes at its own newly-pinned EXPECTED_CHECK_COUNT.
</success_criteria>

<output>
Create `.planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-04-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's share)
- `companion/pages/health_page.py::_battery_section()` — reordered (chart before collapsed table), Timestamp column switched to concise_timestamp_html() via data_table(raw_columns=)
- `companion/pages/health_page.py::_battery_trend_section_html()` — D-10 "Latest 20 readings" label
- `companion/pages/health_page.py::_device_section()` / `_pipeline_section()` — switched to concise_timestamp_html()
- `companion/pages/health_page.py::_anomaly_banner_text()` — rewritten to name real anomaly categories
- `companion/pages/health_page.py::_CORROBORATION_ROWS` — decision-ID leak removed from the Disagreement explanation
- `companion/pages/health_page.py::battery_sparkline_svg()` — single tabindex="0" on the latest point
- `companion/pages/health_page.py::render()` — freshness_html + stale-view banner
- `companion/static/battery-trend.js::moveFocusTo()` — new roving-tabindex keydown handling
</output>
