---
phase: 06.4-runway-picker-show-runway-number-and-airport-map
verified: 2026-08-28T23:59:00Z
status: human_needed
score: 5/6 must-haves verified
behavior_unverified: 1
overrides_applied: 0
behavior_unverified_items:
  - truth: "Dropping companion/static/runway-3.png onto the running server makes runway 3's airport diagram appear on the very next /config load, with zero code changes and no service restart (D-03 asset contract)."
    test: "Drop a real runway-3.png (and/or runway-06-24.png, runway-02-20.png) into companion/static/ on a running companion service, then reload /config with no restart and no code change."
    expected: "Only the runway(s) with a file present render an <img src=\"/runway-image/{id}.png\"> beside their number/heading; the others remain text-only. The image appears on the very next page load."
    why_human: "This phase deliberately ships with zero image files on disk (D-02 — the user supplies the asset). The code path (runway_images_available() recomputed per request, no caching, conditional <img> emission) is unit/e2e-tested against synthetic temp-dir files and a route-branch test that asserts whichever branch matches real on-disk state, but no test exercises an actual end-to-end 'drop a real file, reload the page, see the diagram' cycle because no real asset exists yet. The plan's own <human-check> defers this identically."
human_verification:
  - test: "Drop a real runway-3.png (and/or runway-06-24.png, runway-02-20.png) into companion/static/ on a running companion service, then reload /config with no restart and no code change."
    expected: "Only the runway(s) with a file present render an <img src=\"/runway-image/{id}.png\"> beside their number/heading, legible at the rendered size; at >=960px the image stays inside the Runway fieldset's half-width column without stretching or cropping."
    why_human: "Requires a real user-supplied image asset (D-02) and visual/layout judgment this phase's own plan explicitly defers until that asset exists — not something grep or a unit test can assess."
---

# Phase 06.4: Runway picker — show runway number and airport map Verification Report

**Phase Goal:** The Config page's runway picker shows each runway's number/heading in the large Display type instead of a small inline radio label, and — for any runway whose airport-diagram image the user has supplied — that real diagram beside it, served over a session-gated route that validates the requested id against `device_config.RUNWAY_IDS` before building any filesystem path. With no image files present (D-02's shipped state) it renders the upgraded text-only picker with zero `<img>` tags and no error.

**Verified:** 2026-08-28T23:59:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every runway option on `/config` shows its runway number/heading in Display-size type, not small inline radio-label text (D-01) | ✓ VERIFIED | `companion/pages/config_page.py:126-131` wraps each option in `<label class="runway-option">` with `<span class="runway-option__number">{label}</span>`; `companion/static/style.css:380-384` sets `.runway-option__number { font-size: var(--font-display-size); font-weight: var(--weight-semibold); ... }`, reusing the existing Display token, no new token. |
| 2 | With no runway image files on disk (the phase's shipped state), `/config` renders all three runway options, all three radios, zero `<img>` tags, no error, no broken image (D-03) | ✓ VERIFIED | Real repo state has 0 `.png` files in `companion/static/` (`ls companion/static/*.png` → no matches). `runway_fieldset(current_runway_id, images_available=())` default is the empty tuple; `render()` reads `ctx.get("runway_images") or ()`. Live test `PASS runway_fieldset(images_available=set()) renders zero <img tags and all three number/heading labels (D-03 graceful fallback)` passes in `companion/test_config_page.py`, run directly this session. |
| 3 | Dropping `companion/static/runway-3.png` onto the running server makes runway 3's airport diagram appear on the very next `/config` load, zero code changes, no restart (D-03 asset contract) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code is present and correctly wired: `runway_images_available()` is recomputed fresh inside `page_context()` on every request (no caching layer), and `runway_fieldset()` emits `<img src="/runway-image/{id}.png">` only when the id is in the (freshly computed) set. `companion/test_config_page.py`'s "a session-authenticated GET /runway-image/3.png returns the branch matching real on-disk state (never 500)" check proves the branch logic is honest for whatever the real on-disk state is, but by design (D-02) no real image file exists in this repo, so no test has ever actually exercised "file appears → next load shows `<img>`" end-to-end. This is exactly the plan's own deferred `<human-check>`, not a gap. |
| 4 | `/runway-image/{id}.png` requires a companion session: unauthenticated GET redirects to `/login`, unlike `/static/style.css` | ✓ VERIFIED | Live test `PASS an unauthenticated GET /runway-image/3.png redirects to /login` passes; `companion/app.py:509-513`'s route branch calls `self.require_session()` first, using its own `RUNWAY_IMAGE_ROUTE_PREFIX` rather than extending `/static/`. Live test `PASS GET /static/style.css succeeds without a session and returns a CSS content type` (in `test_companion_app.py`) confirms `/static/` was not widened. |
| 5 | A runway id not in `device_config.RUNWAY_IDS` returns 404 without any filesystem path ever being built from it | ✓ VERIFIED | `companion/app.py:405-406`: `if runway_id not in device_config.RUNWAY_IDS: return self.send_html(404, ...)` is the literal first statement in `_serve_runway_image()`, at line 405, strictly before `_runway_image_path(runway_id)` at line 407. Live test `PASS session-authenticated GET requests for three adversarial runway-image paths all return 404, never 200/500` (percent-encoded traversal, raw traversal, unregistered id) passes. |
| 6 | `companion/pages/config_page.py` performs no filesystem access — it learns image existence only through ctx | ✓ VERIFIED | `grep -n "^import\|^from" companion/pages/config_page.py` shows only `from companion.layout import escape_html` and `from server import device_config` — no `os` import, no filesystem call anywhere in the file. `render()` reads `ctx.get("runway_images")`, the only entry point. |

**Score:** 5/6 truths verified (1 present, behavior-unverified — an intentionally deferred human-check per D-02, not a defect)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `companion/app.py` | Registry-bounded runway-image existence check, ctx wiring, session-gated image route | ✓ VERIFIED | `def runway_images_available` present (×1); `_RUNWAY_IMAGE_DIR = ` present (×1); `"runway_images": runway_images_available()` wired into `page_context()`; `_serve_runway_image` method present and routed in `do_GET()`. |
| `companion/pages/config_page.py` | Number-prominent runway fieldset with conditional `<img>`, single definition of route prefix | ✓ VERIFIED | `RUNWAY_IMAGE_ROUTE_PREFIX = "/runway-image/"` defined once here; `runway_fieldset(current_runway_id, images_available=())` signature confirmed; conditional `<img>` logic confirmed. |
| `companion/static/style.css` | Runway option number/image presentation rules from existing tokens | ✓ VERIFIED | `.runway-option`, `.runway-option__number` (`var(--font-display-size)`), `.runway-option__image` (`var(--color-border, var(--color-secondary))` fallback) all present; `grep -c 'url('` on the file is 0 (zero-external-reference invariant holds). |
| `companion/static/RUNWAY-IMAGES.md` | The D-03 drop-in asset contract | ✓ VERIFIED | File exists, 66 lines (≥20 min), documents exact filenames, PNG-only, highlight-baked-in requirement, sizing guidance, deploy note, and access-control note. |
| `companion/test_config_page.py` | New checks covering detection, fallback rendering, auth, path safety | ✓ VERIFIED (with a noted literal-string deviation) | 10 new checks added (4 detection + 3 rendering + 3 route e2e) confirmed present and passing. `EXPECTED_CHECK_COUNT` is `34`, not the plan's literal `25` — this is a documented, reasoned baseline-drift correction (the plan assumed a stale 15-check baseline; the real on-disk baseline had already grown to 24 from an unrelated phase 06.2 feature before this phase executed). The harness's actual fail-closed invariant (`total == EXPECTED_CHECK_COUNT`) is satisfied at 34/34, confirmed by a fresh run this session. Documented identically in `06.4-REVIEW.md` IN-01 and `06.4-01-SUMMARY.md`. Not treated as a gap. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `companion/app.py` `page_context()` | `runway_images_available()` | Computed once per request, handed to every page module | ✓ WIRED | `companion/app.py:320`: `"runway_images": runway_images_available(),` present verbatim inside `page_context()`. |
| `companion/pages/config_page.py` `render()` | `ctx["runway_images"]` | Forwarded into `runway_fieldset()`, module never touches filesystem | ✓ WIRED | `companion/pages/config_page.py:231`: `runway_fieldset(current_runway_id, ctx.get("runway_images") or ())`. Live test `render()` ctx-forwarding check passes. |
| `companion/app.py` `do_GET()` | `Handler._serve_runway_image()` | Route branch on prefix, gated by `require_session()` first | ✓ WIRED | `companion/app.py:509-513`: prefix+suffix match, `require_session()` first statement, then `_serve_runway_image(runway_id)`. |
| `companion/app.py` | `companion/pages/config_page.py RUNWAY_IMAGE_ROUTE_PREFIX` | app.py rebinds the page module's constant (FLASH_KEY_* precedent) | ✓ WIRED | `companion/app.py:79`: `RUNWAY_IMAGE_ROUTE_PREFIX = config_page.RUNWAY_IMAGE_ROUTE_PREFIX`; the literal `"/runway-image/"` string does not appear a second time in `app.py` (`grep -c` → 0). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full config-page test suite | `server/.venv/bin/python3 companion/test_config_page.py` (run live this session) | `config-page: 34/34 checks pass`, exit 0 | ✓ PASS |
| Full companion-app test suite | `server/.venv/bin/python3 companion/test_companion_app.py` (run live this session) | `companion-app: 55/55 checks pass`, exit 0 | ✓ PASS |
| Whole-repo test harness | `scripts/run-all-tests.sh` (run live this session) | `FAILED harnesses (1): server/test_poll_loop.py`; `Result: FAIL` | ✗ FAIL (pre-existing, out-of-scope — see below) |
| WR-01 fix applied | `git show 4ec2990` (run live this session) | Diff confirms dead `try/except OSError` removed from `runway_images_available()`, docstring corrected | ✓ PASS |
| 06.4 commits touch nothing under `server/` | `git show 54a27af/40f6484/7835c7c --stat` (run live this session) | All three commits only touch `companion/app.py`, `companion/pages/config_page.py`, `companion/pages/__init__.py`, `companion/static/style.css`, `companion/static/RUNWAY-IMAGES.md`, `companion/test_config_page.py` | ✓ PASS |
| Zero PNG files present (D-02/D-03 shipped state) | `ls companion/static/*.png` (run live this session) | No matches, count 0 | ✓ PASS |
| No debt markers in phase files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 5 modified/created files (run live this session) | No matches | ✓ PASS |

**On the one failing harness:** `server/test_poll_loop.py` (42/43, confirmed live this session) fails on the same pre-existing macOS-local-vs-Linux-CI panel-rendering digest mismatch documented repeatedly across 06.2-01/06.3-02/06.3-04/06.3-05. Confirmed via `git show <06.4 commit> --stat` for all three of 06.4's own commits (`54a27af`, `40f6484`, `7835c7c`) that none touch anything under `server/` — the branch's `server/` changes visible in a broader `git diff` against an older base come from the independent, already-merged Phase 7 (on-glass hardware verification), not from this phase. Not a 06.4 regression; out of this phase's scope.

### Anti-Patterns Found

None. Scanned all 5 phase-modified/created files (`companion/app.py`, `companion/pages/config_page.py`, `companion/pages/__init__.py`, `companion/static/style.css`, `companion/static/RUNWAY-IMAGES.md`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` and "coming soon"/"not yet implemented" phrasing — zero matches. `companion/pages/config_page.py` remains filesystem-free (no `os` import). WR-02 (shared `Cache-Control: public` on `send_bytes()`) is a real, pre-existing pattern inherited by the new route from `_serve_gallery_image()`/`_serve_stylesheet()` — confirmed by grep that all three call sites share the same helper — correctly scoped by the code review as out-of-phase, not a regression this phase introduced.

### Requirements Coverage

Not applicable — this is an unmapped backlog phase (promoted from 999.4), traced against locked decisions D-01/D-02/D-03 in `06.4-CONTEXT.md` rather than `REQUIREMENTS.md` REQ-IDs, per the phase's own ROADMAP.md entry. No REQ-ID cross-reference exists to check for orphans.

### Human Verification Required

### 1. Real-asset drop-in behavior (D-03 asset contract, end-to-end)

**Test:** Drop a real `companion/static/runway-3.png` (and optionally `runway-06-24.png`, `runway-02-20.png`) onto a running companion service, with no restart and no code change, then reload `/config` in a browser.

**Expected:** Only the runway(s) with a file present show an airport-diagram `<img>` beside their number/heading, legible at the rendered size; the others remain text-only with no broken image; at ≥960px the image stays inside the Runway fieldset's half-width column (06.3's 2-column form grid) without stretching or cropping.

**Why human:** This phase deliberately ships zero image files (D-02 — the user supplies the asset, not Claude). The code's logic for this exact scenario is unit/e2e-tested against synthetic temp-directory files and a route-branch test that asserts whichever branch matches real on-disk state (i.e., it's honest either way) — but no test in the suite has ever exercised the real "drop a file, reload, see the diagram, verify legibility and layout" cycle, because no real asset exists yet to exercise it with. The plan's own `<human-check>` defers this identically and explicitly states it "must not block this phase (D-02)."

## Gaps Summary

No gaps. All six derived observable truths hold except one, and that one exception is not a defect — it is the phase's own deliberately deferred `<human-check>`, required by design (D-02: the user, not Claude, supplies the image asset) and impossible to exercise until a real image file exists. The code paths that truth depends on (fresh-per-request existence detection, conditional `<img>` emission, no caching that would delay pickup) are all present, correctly wired, and covered by tests that prove the logic is honest for whatever the real on-disk state is — they just cannot prove the specific transition "file appears -> image renders" without a real file, which this phase is explicitly forbidden from creating.

The one review-flagged literal-string mismatch (`EXPECTED_CHECK_COUNT = 25` vs. the actual `34`) is a documented, reasoned baseline-drift correction with clean-room evidence (the harness's real invariant, `total == EXPECTED_CHECK_COUNT`, holds at 34/34), consistent with an identical, already-accepted precedent in phase 06.3. Not treated as a gap.

Both code-review warnings (WR-01, WR-02) were independently confirmed: WR-01's dead `except OSError` clause was actually removed in commit `4ec2990` (diff verified this session); WR-02 (shared-cache exposure via `Cache-Control: public`) was confirmed to be a pre-existing pattern inherited unmodified from `_serve_gallery_image()`/`_serve_stylesheet()`'s shared `send_bytes()` helper, not new attack surface this phase introduced — correctly left as a flagged, out-of-scope follow-up rather than a phase-blocking defect.

---

_Verified: 2026-08-28T23:59:00Z_
_Verifier: Claude (gsd-verifier)_
