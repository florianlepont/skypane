---
phase: 06.4
slug: runway-picker-show-runway-number-and-airport-map
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-28
---

# Phase 06.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Hand-rolled `check()`-based harness, stdlib only (no pytest) |
| **Config file** | none — `scripts/run-all-tests.sh`'s `HARNESSES` array is the canonical file list |
| **Quick run command** | `server/.venv/bin/python3 companion/test_config_page.py` |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~20-30 seconds |

---

## Sampling Rate

- **After every task commit:** `server/.venv/bin/python3 companion/test_config_page.py`
- **After every plan wave:** `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite green; also re-check `test_companion_app.py`'s existing `/static/style.css` unauthenticated-access assertion still passes unchanged, proving the new image route didn't accidentally widen `/static/`'s exposure
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.4-01-01 | 01 | 1 | D-03 (graceful fallback detection) | T-06.4-01 | `runway_images_available(image_dir=tmpdir)` returns empty set with no files; returns `{"3"}` with only `runway-3.png` present | unit | `server/.venv/bin/python3 companion/test_config_page.py` | ✅ file exists, extend | ⬜ pending |
| 06.4-01-02 | 01 | 1 | D-01/D-03 (fieldset rendering) | T-06.4-02 | `runway_fieldset()` emits `<img>` only for runway ids present in the available set; still emits number/heading label text when no image exists (no broken image, no crash) | unit | `server/.venv/bin/python3 companion/test_config_page.py` | ✅ file exists, extend | ⬜ pending |
| 06.4-01-03 | 01 | 1 | D-02 (image route, auth + path safety) | T-06.4-03 | New image route requires a session (matches `/preview.png`/`/gallery/*`); a runway id not in `RUNWAY_IDS` (path-traversal-shaped input) returns 404 via exact membership check, never touches the filesystem with unvalidated input; missing file returns 404, not 500 | end-to-end (subprocess) | `server/.venv/bin/python3 companion/test_config_page.py` | ✅ file exists, extend | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. `companion/test_config_page.py` already exists with the exact harness shape (`Harness` subprocess class, `check()` runner, `EXPECTED_CHECK_COUNT` guard) this phase's new checks slot into. The only Wave-0-shaped action is updating `EXPECTED_CHECK_COUNT` (currently 15) to match the real new check count — the harness fails closed if this constant drifts, so the plan must include updating it as an explicit task step.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Highlight visual treatment reads correctly once the user supplies real images | D-01 | Depends entirely on user-supplied image content, which does not exist at planning time | Once the user drops real `runway-*.png` files into `companion/static/`, visually confirm the highlighted runway is legible and the image doesn't stretch/crop badly against the fieldset's layout width (per `06.3-UI-SPEC.md`'s 2-column fieldset grid) |

---

## Validation Sign-Off

- [x] All tasks have automated verify (see table above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (all 3 tasks in the phase's single wave have direct coverage)
- [x] Wave 0 covers all MISSING references — N/A, no gaps
- [x] No watch-mode flags — all commands are one-shot script executions
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-28 by the planner. `06.4-01-PLAN.md` breaks the phase into exactly three tasks in one plan, one wave, with task ids `06.4-01-01` / `06.4-01-02` / `06.4-01-03` matching the map above row-for-row:

| Task ID | PLAN.md task | New checks | Harness total after task |
|---------|--------------|-----------|--------------------------|
| 06.4-01-01 | Task 1 — registry-bounded existence detection + ctx wiring | 4 (empty dir, single file present, non-existent dir, non-registry file ignored) | 19 |
| 06.4-01-02 | Task 2 — number-prominent fieldset, graceful fallback, asset contract | 3 (image emitted only for available ids, zero `<img>` with an empty set, `render()` forwards `ctx["runway_images"]`) | 22 |
| 06.4-01-03 | Task 3 — session-gated, membership-validated image route | 3 (unauthenticated → 303 `/login`; real GET is 200 `image/png` when the file exists else 404, never 500; three adversarial ids → 404) | 25 |

`EXPECTED_CHECK_COUNT` is raised explicitly inside each task (15 → 19 → 22 → 25), satisfying the Wave 0 requirement above. The manual-only row stays manual and stays non-blocking: it depends on user-supplied assets that do not exist (D-02), so the plan records it as a deferred `<human-check>` rather than a blocking checkpoint, consistent with `workflow.human_verify_mode: end-of-phase`.
