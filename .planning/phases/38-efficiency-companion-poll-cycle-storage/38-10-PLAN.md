---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 10
type: execute
wave: 5
depends_on: ["38-05", "38-08"]
files_modified:
  - companion/app.py
  - companion/test_page_context.py
autonomous: true
requirements: [EFF-04]

must_haves:
  truths:
    - "Display, Device, Flights and Airlines requests build no Health markup; Home and Health build it once"
    - "The nav-dot severity on every page comes from one health_signals snapshot, and equals the Health page banner's severity in the same request"
    - "Authenticated 404 and 403 pages compute the nav dot from health_signals only; unauthenticated ones compute nothing"
    - "Expensive context values (health state, gallery, manual resolutions, colour rules, calendar values, poll cooldown) are computed only when a page reads them, at most once per request"
    - "poll_cooldown_remaining degrades to 0 on a database error instead of a 500"
  artifacts:
    - path: "companion/app.py"
      provides: "_LazyContext and lazy page_context; 404/403 via safe_health_signals"
      contains: "class _LazyContext"
    - path: "companion/test_page_context.py"
      provides: "no-markup-on-non-Health-pages, once-per-request, dot-equals-banner, degrade tests"
  key_links:
    - from: "companion/app.py page_context"
      to: "health_page.safe_health_signals / health_state_from_signals"
      via: "lazy loaders sharing one signals snapshot"
      pattern: "health_state_from_signals"
---

<objective>
Lazy page context and markup-free severity (EFF-04, first part).

Purpose: `page_context()` builds the full Health state, markup included, for every tab and for
the 404/403 pages, only to draw a nav dot. After this plan, only the pages that render Health
markup pay for it, and every other expensive context value is computed on first read.
Output: `_LazyContext`, lazy `page_context()`, 404/403 via signals, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-05-SUMMARY.md
@.claude/CLAUDE.md

<conventions>
- No plan/phase/requirement IDs in comments. The context stays a dict (a dict subclass); a typed
  per-page context belongs to a later phase. Page unit tests that build plain dict contexts must
  keep working unchanged.
- Companion tests obey companion/test_suite_guards.py; use `app_server_in_process` for
  monkeypatch-visible counting.
- Claude's discretion (CONTEXT): `poll_cooldown_remaining` degrading to 0 on a database error is
  approved; do it inside the lazy loader.
- Merge note: Phase 42 edits companion/app.py.
</conventions>

<interfaces>
From plan 05 (companion/pages/health_page.py): `health_signals(state_dir, now=None) -> dict`
(states, `severity`, `anomalies`, `next_wake_iso`, `effective_interval_s`, `hold_reason`, `inputs`,
...), `safe_health_signals(state_dir, now=None) -> dict | None`,
`health_state_from_signals(signals) -> dict` (today's compute_health_state dict),
`compute_health_state`, `safe_health_state`.
companion/app.py (re-locate by name): `Handler.page_context()` returns a plain dict with keys
state_dir, ui_theme, lang, device_config, screen_id, last_checkin_ts, battery_critical,
wake_interval_env_default, flash, flash_role, poll_cooldown_remaining, gallery_entries,
runway_images, health_severity, health_state, now, resolve_prefix, flights_limit,
manual_resolutions, colour_rules, calendar_configured, calendar_last_synced_at,
calendar_last_attempt_at, calendar_entry_count, calendar_drift. It calls
`prefs.set_request_prefs(lang=...)` before any Health work (markup language). Consumers:
home_page reads `ctx.get("health_state") or {}`; health_page.render reads ctx["health_state"]
(falls back to a fresh compute when None); `_page_shell_for` reads ctx["health_severity"] after
render. `_not_found_page()` / `_forbidden_page()` call `health_page.safe_health_state(...)` when
`self._is_authenticated()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _LazyContext and a lazy page_context sharing one health snapshot</name>
  <files>companion/app.py, companion/test_page_context.py</files>
  <read_first>
    - companion/app.py (page_context, poll_cooldown_remaining, gallery_entries, _not_found_page, _forbidden_page, _page_shell_for, _render_tab)
    - companion/pages/health_page.py (health_signals, safe_health_signals, health_state_from_signals, render's use of ctx["health_state"])
    - companion/pages/home_page.py (ctx.get("health_state"))
    - companion/pages/__init__.py (documented ctx keys)
    - companion/test_request_connections.py (plan 08 fixtures and counting patterns)
  </read_first>
  <behavior>
    - In-process server, logged in, seeded state: with `health_page.health_state_from_signals` wrapped by a counter, GET /display, /device, /flights, /airlines -> 0 calls; GET / and /health -> exactly 1 call; `health_page.health_signals` -> exactly 1 call on every tab route
    - Seeded warn scenario (stale device check-in): the nav dot on /flights shows the warn state, and on /health the nav dot severity equals the banner severity in the same response (parse both from the HTML)
    - Authenticated GET /no-such-page -> 404 with the warn dot, `health_state_from_signals` 0 calls; unauthenticated -> 404, `health_signals` 0 calls
    - `gallery_entries`, `manual_resolutions.load_manual_resolutions`, `colour_rules.load_colour_rules` and `calendar_rules.load_calendar_registry` are each called 0 times on /flights (if Flights does not read them) and at most once per request on any route (wrap each with a counter)
    - sqlite3.connect monkeypatched to raise sqlite3.OperationalError: GET /flights and GET / return 200 (poll cooldown degrades to 0; sections show their unavailable blocks)
    - A plain dict passed to a page module's render still works (existing page unit tests stay green)
    - `_LazyContext`: `ctx["k"]`, `ctx.get("k")`, `"k" in ctx` all resolve a lazy key once; `ctx.get("missing", 7) == 7`
  </behavior>
  <action>
Add `class _LazyContext(dict)` in companion/app.py: constructor takes eager values plus a
`loaders` mapping of key -> zero-argument callable; `__getitem__` resolves a pending loader once,
stores the value with `dict.__setitem__`, and returns it; `get(key, default=None)` and
`__contains__` treat pending loaders as present (dict.get never calls `__missing__`, so both must
be overridden). Rewrite `page_context()` so it still calls `prefs.set_request_prefs` first and
keeps cheap values eager (state_dir, ui_theme, lang, device_config, screen_id, last_checkin_ts,
battery_critical, wake_interval_env_default, flash, flash_role, runway_images, now,
resolve_prefix, flights_limit), and makes these lazy: `health_signals` (internal key
`_health_signals`, `health_page.safe_health_signals(state_dir, now)`), `health_state`
(`health_page.health_state_from_signals(signals)` when signals is not None, else None, wrapped in
the same broad fail-closed catch `safe_health_state` uses), `health_severity` (taken from
`health_state` if already built, else from the signals; "ok" when None), `gallery_entries`,
`manual_resolutions`, `colour_rules`, the five calendar values (one shared lazy registry load),
and `poll_cooldown_remaining` (returns 0 on `(sqlite3.Error, OSError)`; say why in one comment).
Keep `_resolve_flash_text`'s arguments eager as today. `_not_found_page` and `_forbidden_page`
use `health_page.safe_health_signals` for the severity (still only when authenticated). Update the
`page_context` docstring and companion/pages/__init__.py is not in this plan's files, so leave its
key list as is (the keys and meanings are unchanged). Write companion/test_page_context.py for
every behavior bullet.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto companion/test_page_context.py companion/test_request_connections.py companion/test_health_signals.py companion/test_suite_guards.py && server/.venv/bin/python -m pytest -q -n auto companion -m "not browser" && server/.venv/bin/ruff check companion && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - companion/test_page_context.py passes, including 0 markup builds on the four non-Health tabs and 1 on Home/Health
    - companion/test_request_connections.py still reports 1 connection per tab route
    - `server/.venv/bin/python -m pytest -q -n auto companion -m "not browser"` passes
  </acceptance_criteria>
  <done>Only pages that need Health markup build it; context values are computed on demand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated vs unauthenticated requests | Health severity is only for signed-in users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-31 | Information disclosure | Health severity on 404/403 | mitigate | computed only when `_is_authenticated()`; test asserts 0 signal computations for an unauthenticated 404 |
| T-38-32 | Repudiation (hidden problem) | nav dot vs banner disagreement | mitigate | one lazily shared signals snapshot per request; test compares both in one response |
| T-38-33 | Denial of service | DB error 500-ing every tab | mitigate | cooldown degrades to 0; test with a failing connect expects 200 |
| T-38-34 | Tampering | lazy value computed in the wrong language | mitigate | `set_request_prefs` still runs first in page_context; markup is built later on the same thread |
</threat_model>

<verification>
- Laziness and equivalence tests green; connection counts unchanged; companion suite green.
</verification>

<success_criteria>
Severity without markup on every page; lazy context in place for the freshness check.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-10-SUMMARY.md` when done.
</output>
