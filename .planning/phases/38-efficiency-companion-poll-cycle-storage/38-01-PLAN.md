---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test-support/efficiency_probe.py
  - test-support/test_efficiency_probe.py
  - scripts/measure_efficiency.py
  - .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md
autonomous: true
requirements: [EFF-01, EFF-02, EFF-03, EFF-04, EFF-05, EFF-06]

must_haves:
  truths:
    - "One stdlib probe module counts SQLite connections, init_schema runs, statements and COMMITs, script tags, identity and gzip bytes, time.sleep calls and poll_state.json writes, with no production change"
    - "scripts/measure_efficiency.py prints the per-route, static-revalidation, freshness-tick and per-branch poll-cycle tables in markdown, with no network access"
    - "38-EFF-BASELINE.md holds the BEFORE tables measured on the unmodified tree, with commit SHA, machine, Python version and repeat count, committed before any production file changes"
  artifacts:
    - path: "test-support/efficiency_probe.py"
      provides: "count_db, count_sleeps, count_poll_state_writes, fake_provider_latency, route_weight, cycle_probe"
      contains: "def cycle_probe"
    - path: "test-support/test_efficiency_probe.py"
      provides: "self-tests of the probe mechanics (not of efficiency)"
    - path: "scripts/measure_efficiency.py"
      provides: "baseline CLI"
      contains: "def main"
    - path: ".planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md"
      provides: "BEFORE tables"
      contains: "## Before"
  key_links:
    - from: "scripts/measure_efficiency.py"
      to: "test-support/efficiency_probe.py"
      via: "import efficiency_probe"
      pattern: "efficiency_probe"
    - from: "test-support/efficiency_probe.py"
      to: "server/history_db.py init_schema + sqlite3.connect"
      via: "attribute patching restored on exit"
      pattern: "init_schema"
---

<objective>
Build the measurement instruments and record the BEFORE numbers for all six efficiency items,
with no production change (instruments-first rule, CONTEXT "Locked by the ledger").

Purpose: ROADMAP criteria 1 and 4 need before/after numbers, and every later fix plan asserts
its invariant through these same counters. Timings are recorded, never asserted; counts are
asserted by the fix plans.
Output: probe module + its self-test, the CLI, and 38-EFF-BASELINE.md with the Before section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md
@.claude/CLAUDE.md

<conventions>
- English only; no plan, phase, requirement or decision IDs in code comments or docstrings
  (`python3 scripts/check_comment_history.py check` runs in CI).
- No network: pytest runs with pytest-socket (loopback only). The CLI is not under pytest, so it
  must install `skypane_test_support.guarded_resolvers()` onto `socket.getaddrinfo`,
  `socket.gethostbyname`, `socket.gethostbyname_ex` for its whole run, patch
  `server.plane.enrich.default_transport` to return `(404, None)`, and replace
  `server.plane.detect.query_provider` with the latency fake below.
- test-support/ is on `pythonpath` (pyproject) and outside the coverage `source`; scripts/ is
  also outside coverage. Keep both stdlib-only (plus repo modules).
- The probe must restore every attribute it patches on exit (it is used both under pytest and from
  the CLI, so it cannot rely on pytest's monkeypatch).
</conventions>

<interfaces>
Existing (re-locate by name, line numbers drift):
- `test-support/companion_app_server.py`: `InProcessAppServer(state_dir)` (`.url(path)`,
  `.base_url()`, `.stop()`), `login(server) -> cookie`, `get(server, path, cookie=None) ->
  (status, headers_dict, raw_bytes)`, `http_request(url, method="GET", data=None, cookie=None,
  timeout=10, content_type=None, extra_headers=None)`.
- `test-support/companion_markup.py`: `parse_html(text)` -> Node with `.select(selector)`;
  supports tag, `.class`, `#id`, `[attr]`, `[attr="v"]`.
- `test-support/skypane_test_support.py`: `guarded_resolvers()` returns a dict with
  `getaddrinfo`, `gethostbyname`, `gethostbyname_ex`; `NetworkAccessBlocked`.
- `server/history_db.py`: `connect(state_dir)` calls module-global `sqlite3.connect` then
  `init_schema(conn)` by global name; `open_db(state_dir)`; writers `record_device_health(conn,
  ts, battery_mv=...)`, `record_runway_event(conn, **fields)`, `set_meta(conn, key, value)`;
  `META_LAST_PIPELINE_RUN`, `utc_now_iso()`.
- `server/poll_loop.py`: `run_once(snapshot=None, state_dir=None, geofence=None, caddy_log=None,
  lock_timeout_s=None)` returns a dict with `"state"`; writes poll_state.json only through
  `atomic_io.atomic_write(path, text)` (module attribute call `atomic_io.atomic_write`).
- `server/plane/detect.py`: `query_provider(name, lat, lon, radius_nm, timeout=...)`,
  `DEFAULT_PROVIDER_ORDER = ("adsbfi", "adsblol")`, `MIN_SECONDS_BETWEEN_CALLS = 1.1`, uses
  `time.sleep` via `import time`.
- `server/device_config.py`: `save_device_config(state_dir, **fields)` (e.g. `display_enabled=False`).
- A detected-flight record the default geofence accepts (used during research):
  `{"hex": "39a1b2", "flight": "AFR123  ", "lat": 48.7233, "lon": 2.3794, "alt_baro": 450,
  "gs": 137.1, "baro_rate": 1500, "seen_pos": 1.0}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Probe module test-support/efficiency_probe.py and its self-test</name>
  <files>test-support/efficiency_probe.py, test-support/test_efficiency_probe.py</files>
  <read_first>
    - test-support/companion_app_server.py (InProcessAppServer, login, get, http_request)
    - test-support/companion_markup.py (parse_html, select)
    - test-support/skypane_test_support.py (guarded_resolvers, FakeProviders)
    - server/history_db.py (connect, init_schema, open_db, writers)
    - server/poll_loop.py (run_once, save_poll_state, _poll_state_path)
    - server/plane/detect.py (query_provider, MIN_SECONDS_BETWEEN_CALLS)
    - test-support/test_test_support.py (style of existing test-support tests)
  </read_first>
  <behavior>
    - count_db(): opening `history_db.open_db(tmp)` and calling `set_meta` inside it counts connections == 1, init_schema >= 1 (today) and commits >= 1; counts from a second thread are included
    - count_db() restores `sqlite3.connect` and `history_db.init_schema` on exit (identity check after the block)
    - count_sleeps(): `time.sleep(0)` inside the block is recorded as [0]; outside it is not
    - count_poll_state_writes(): `poll_loop.save_poll_state(tmp, {"a": 1})` counts 1 write and records its byte length; an `atomic_io.atomic_write` to another filename is not counted
    - fake_provider_latency(0.0, records): `detect.query_provider("adsbfi", ...)` returns the records and logs (name, start, end, thread name)
    - route_weight(server, "/", cookie) on an InProcessAppServer over a seeded state returns status 200, identity_bytes > gzip_bytes > 0, a non-empty script_srcs list and ms >= 0
    - cycle_probe(state_dir, latency_s=0) with MIN_SECONDS_BETWEEN_CALLS monkeypatched to 0 returns a dict with keys wall_s, sleeps, connections, init_schema, commits, poll_state_writes, poll_state_bytes, state
  </behavior>
  <action>
Create `test-support/efficiency_probe.py` (stdlib + repo modules, no pytest import) with:
- a private `_patched(obj, name, value)` context manager that swaps an attribute and always restores it;
- `count_db()`: context manager yielding a `DbCounts` object (`connections`, `init_schema`,
  `statements`, `commits`, `by_thread` optional) guarded by a `threading.Lock`. It wraps
  `sqlite3.connect` (patch the attribute on the `sqlite3` module, forwarding `*args, **kwargs`
  unchanged so a later `factory=` argument still works) and installs `conn.set_trace_callback`
  counting every statement and every statement whose stripped upper text starts with "COMMIT";
  it wraps `server.history_db.init_schema` counting calls;
- `count_sleeps()`: captures the real `time.sleep` at import time as `REAL_SLEEP`, patches
  `time.sleep` with a recorder that appends the duration then calls `REAL_SLEEP`; yields the list;
- `count_poll_state_writes()`: wraps `server.atomic_io.atomic_write`, counting calls whose path
  basename is `poll_state.json` and recording each payload's encoded byte length;
- `fake_provider_latency(latency_s, records_by_provider)`: patches `server.plane.detect.query_provider`
  with a fake that records `(name, time.time() at entry, time.time() at exit, thread name)` in a
  lock-guarded list, sleeps `latency_s` with `REAL_SLEEP`, and returns a copy of
  `records_by_provider.get(name, [])`; yields the call log;
- `seed_history(state_dir, n=30)`: 30 `device_health` rows and 30 `runway_events` rows plus
  `META_LAST_PIPELINE_RUN`, the same seed the research used (see 38-RESEARCH.md "Baseline numbers");
- `route_weight(server, path, cookie=None, repeats=5, extra_headers=None)`: one warm-up request,
  then `repeats` timed requests via `companion_app_server.http_request`; returns a dict with
  `status`, `headers`, `identity_bytes`, `gzip_bytes` (gzip level 6), `ms` (mean), `script_srcs`
  (ordered `src` values of `script[src]` via `parse_html`, empty for non-HTML), and the
  `count_db()` counts averaged per request;
- `cycle_probe(state_dir, latency_s=0.25, records=None, **run_once_kwargs)`: patches
  `server.plane.enrich.default_transport` to `(404, None)`, runs `poll_loop.run_once` under
  `count_db`, `count_sleeps`, `count_poll_state_writes` and `fake_provider_latency`, times it with
  `time.perf_counter`, and returns the dict listed in the behavior block (sleeps as a list).
Then write `test-support/test_efficiency_probe.py` covering each behavior bullet (pytest,
`tmp_path`, `monkeypatch` for `MIN_SECONDS_BETWEEN_CALLS` only; stop every InProcessAppServer in a
`finally`). These tests assert probe mechanics only and must pass before and after every later
plan, so never assert today's inefficiency (no `connections > 1`, no `sleeps == [1.1]`).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto test-support/test_efficiency_probe.py && server/.venv/bin/ruff check test-support && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python -m pytest -q test-support/test_efficiency_probe.py` passes with at least 7 tests collected
    - `git diff --name-only` for this task lists no file under server/, companion/, stub-server/ or deploy/
    - `server/.venv/bin/ruff check test-support` and `python3 scripts/check_comment_history.py check` exit 0
  </acceptance_criteria>
  <done>The probe counts every quantity the phase measures and restores all patches.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/measure_efficiency.py and the BEFORE section of 38-EFF-BASELINE.md</name>
  <files>scripts/measure_efficiency.py, .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md</files>
  <read_first>
    - test-support/efficiency_probe.py (Task 1)
    - .planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md (document pattern)
    - 38-RESEARCH.md §Validation Architecture "Baseline numbers measured during research" (cross-check values)
  </read_first>
  <action>
Create `scripts/measure_efficiency.py` (executable, `#!/usr/bin/env python3`, stdlib; inserts the
repo root and `test-support/` on `sys.path`; `argparse` with `--label` (free text, e.g. before /
after), `--repeats` default 5, `--latency` default 0.25). It installs the resolver guard and the
enrich/provider fakes (see conventions), creates a scratch state dir with `tempfile.mkdtemp()`,
seeds it with `efficiency_probe.seed_history`, starts one `InProcessAppServer`, logs in, and prints
markdown to stdout:
1. Header: label, `git rev-parse --short HEAD` output, `platform.node()`, `platform.platform()`,
   `sys.version.split()[0]`, repeats, latency, UTC timestamp.
2. Table "Routes": `/`, `/display`, `/device`, `/flights`, `/health`, `/airlines`, `/login`,
   `/static/style.css`, and one row per distinct script src seen on any page. Columns: status,
   identity bytes, gzip-6 bytes, mean ms, `<script src>` count, sqlite connections, init_schema,
   COMMITs per request.
3. Table "First-load weight": per tab route, HTML + `/static/style.css` + every script the page
   references, identity and gzip-6 totals.
4. Table "Static revalidation": for `/static/style.css` and `/static/freshness.js`, the first
   response's `ETag`, `Last-Modified`, `Cache-Control`, then the status and body bytes of a second
   request sending `If-None-Match` equal to the first ETag (or no header when there was no ETag).
5. Table "Freshness tick": for `/`, `/display`, `/flights`, `/health`, a first GET, then a GET with
   `X-Requested-With: freshness` and `If-None-Match` set to the first response's `ETag` if any:
   status and body bytes of the second response.
6. Table "Poll cycle" (fresh state dir, providers faked at `--latency`): the research's seven
   branches in order: empty sky first, empty sky repeat, flight detected (the record in
   `<interfaces>` returned by both providers), same flight again, nothing new with flight on
   screen (providers return []), display_off hold entry (`device_config.save_device_config(state,
   display_enabled=False)`), display_off hold repeat. Columns: wall s, sleeps list, connections,
   init_schema, COMMITs, poll_state writes, poll_state bytes, returned state.
7. One extra line: empty-sky wall time at latency 0.
The script must not write anything outside its temp dir and must remove it at exit.

Run it on the unmodified tree:
`server/.venv/bin/python scripts/measure_efficiency.py --label before`
and create `38-EFF-BASELINE.md` with: a title and one-paragraph purpose (ROADMAP criteria 1-4);
"Measurement setup" (command line, what is faked, that timings are informative); "## Before"
with the verbatim markdown output; "## After" containing only the sentence "Recorded by the
final plan of this phase with the same command."; "## Live compression (VPS)" with an empty
table (columns: when, URL, Accept-Encoding, Content-Encoding, size_download) and the sentence
that the developer records it at the final checkpoint. Cross-check the Before numbers against the
research table and note any difference larger than 20 % in one sentence (the research used the
same seed; a difference is recorded, not tuned away).
  </action>
  <verify>
    <automated>server/.venv/bin/python scripts/measure_efficiency.py --label check --repeats 1 --latency 0 | grep -c "^|" && grep -c "^## Before$" .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md && server/.venv/bin/ruff check scripts && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python scripts/measure_efficiency.py --label check --repeats 1 --latency 0` exits 0 and prints at least 25 lines starting with `|`
    - 38-EFF-BASELINE.md has exactly one line equal to `## Before`, and the Before tables show `/static/style.css` with no ETag and the poll-cycle rows with a 1.1 entry in the sleeps column for the live branches (the unmodified tree)
    - `git log --format=%H -1 -- .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md` is a commit whose diff touches no file under server/, companion/, stub-server/ or deploy/
  </acceptance_criteria>
  <done>Before numbers are committed and reproducible with one command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement CLI -> network | the CLI runs outside pytest-socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Denial of service (provider ToS) | scripts/measure_efficiency.py | mitigate | `detect.query_provider` and `enrich.default_transport` are replaced before any cycle runs, and the resolver guard raises `NetworkAccessBlocked` on any non-loopback lookup, so the CLI can never call adsb.fi, adsb.lol or adsbdb (COMPLIANCE.md) |
| T-38-02 | Information disclosure | 38-EFF-BASELINE.md | accept | Contains only synthetic seed data, byte counts and the dev machine's hostname; no secret, cookie or password is printed (the session cookie is never output) |
| T-38-03 | Tampering | probe patches leaking into other tests | mitigate | every patch goes through `_patched`, restored in `finally`; Task 1 asserts restoration |
</threat_model>

<verification>
- test-support/test_efficiency_probe.py green; CLI runs offline; Before section committed with no production diff.
</verification>

<success_criteria>
Instruments exist and the BEFORE baseline is committed ahead of every production change.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-01-SUMMARY.md` when done.
</output>
