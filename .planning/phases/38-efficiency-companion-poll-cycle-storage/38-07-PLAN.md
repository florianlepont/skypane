---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 07
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - server/poll_loop.py
  - server/test_poll_efficiency.py
autonomous: true
requirements: [EFF-03]

must_haves:
  truths:
    - "Every poll-cycle branch opens exactly one SQLite connection"
    - "Every branch's history writes land in exactly one COMMIT"
    - "No write transaction is open while the ntfy notification is sent"
    - "run_once keeps its signature and shape: lock, then the cycle body"
  artifacts:
    - path: "server/poll_loop.py"
      provides: "cycle connection scope inside the lock; _record_history in one write_batch"
      contains: "connection_scope"
    - path: "server/test_poll_efficiency.py"
      provides: "per-branch connection/commit counts and the no-transaction-during-notify test"
  key_links:
    - from: "server/poll_loop.py run_once"
      to: "history_db.connection_scope"
      via: "with block nested inside poll_cycle_lock"
      pattern: "history_db\\.connection_scope\\("
    - from: "server/poll_loop.py _record_history"
      to: "history_db.write_batch"
      via: "with block around all writes"
      pattern: "history_db\\.write_batch\\("
---

<objective>
One SQLite connection and one transaction per poll cycle (EFF-03, ROADMAP criterion 3 for cycles).

Purpose: today a cycle opens 3 connections, runs the schema 3 times and commits 2-4 times. With the
storage scope from plan 03, the cycle entry opens one scope and `_record_history` wraps its writes
in one batch, committed before the silence notification's HTTP call.
Output: two small poll_loop changes and server/test_poll_efficiency.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-03-SUMMARY.md
@.claude/CLAUDE.md

<conventions>
- No plan/phase/requirement IDs in comments. No `CycleContext`, no run_once split, no state_store:
  a later phase owns those; this plan only changes how often the database is opened and committed.
- Merge note: Phase 42 reads poll_state in poll_loop (battery_low_active) and Phase 39 rewrites
  run_once; keep the diff small and local.
- Tests: pytest, no network (`fake_providers` fixture zeroes MIN_SECONDS_BETWEEN_CALLS), use
  `efficiency_probe.count_db()` / `cycle_probe()`; timings are never asserted.
</conventions>

<interfaces>
From plan 03 (server/history_db.py): `connection_scope(state_dir)` (thread-bound, re-entrant,
lazy open, closes at outermost exit, rolls back a pending transaction), `open_db(state_dir)`
(returns the scoped connection when a scope for the same path is active), `write_batch(conn)`
(writers defer their commit; one COMMIT at the outermost exit; rollback + re-raise on exception).
server/poll_loop.py (re-locate by name):
- `run_once(snapshot=None, state_dir=None, geofence=None, caddy_log=None, lock_timeout_s=None)`:
  `with poll_cycle_lock(state_dir, lock_timeout_s): return _run_once_locked(...)`.
- `_last_source_fault(state_dir)`, `_record_history(state_dir, flight, confirmed_state,
  route_source, route, tracked_runway_id, source_fault, record_event, now_iso, caddy_log=None,
  wake_interval_s=_NO_WAKE_EPOCH, detected=False)` (one `with history_db.open_db` block, catches
  `(sqlite3.Error, OSError)` and logs).
- Two `with history_db.open_db(state_dir) as conn: _notify_silence_transition(state_dir,
  poll_state, conn, device_cfg)` sites (hold branch and shared tail); the notify helper calls
  `notify.send_notification(topic_url, title, body)` through `sender or notify.send_notification`.
- Research scenarios (efficiency_probe / measure_efficiency): empty sky first/repeat, flight
  detected, same flight again, nothing new with flight on screen, display_off hold entry/repeat.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cycle scope in run_once and one write_batch in _record_history</name>
  <files>server/poll_loop.py, server/test_poll_efficiency.py</files>
  <read_first>
    - server/poll_loop.py (run_once, _run_once_locked's four open_db sites, _record_history, _notify_silence_transition, _last_source_fault)
    - server/history_db.py (connection_scope, write_batch from plan 03)
    - test-support/efficiency_probe.py (count_db, cycle_probe, fake_provider_latency)
    - server/test_poll_loop.py (how cycles and notifications are set up in tests; device_config notification fields)
  </read_first>
  <behavior>
    - Parametrised over the seven research branches (run in sequence on one state dir, as measure_efficiency does): count_db().connections == 1 per cycle and commits == 1 per cycle (a hold-repeat cycle with nothing to write still writes the pipeline-run meta, so 1)
    - init_schema runs at most once across the whole sequence (schema once per file)
    - The injected-snapshot path (`run_once(snapshot=...)`) also opens exactly one connection
    - A cycle where `_notify_silence_transition` fires (notifications topic_url + frame_silent enabled, a device_health row older than the warn threshold): a fake `notify.send_notification` records `captured_conn.in_transaction` (connection captured through a sqlite3.connect wrapper); the recorded value is False, and the batch's rows are visible from a second connection opened inside the fake sender
    - A history write failure (monkeypatch `history_db.set_meta` to raise sqlite3.OperationalError) is logged and the cycle still returns its normal dict; no row from that batch is persisted
    - POST-style nesting: calling run_once inside an already active `history_db.connection_scope(state_dir)` on the same thread opens no additional connection
  </behavior>
  <action>
In `run_once`, nest `with history_db.connection_scope(state_dir):` inside the existing
`with poll_cycle_lock(...)` and return `_run_once_locked(...)` from inside it; nothing else in
run_once changes. In `_record_history`, keep the single `with history_db.open_db(state_dir) as
conn:` and put every write inside `with history_db.write_batch(conn):`, so the event row, the
meta updates, the Caddy ingest and the wake epoch commit once; the batch exits (committing)
before `_record_history` returns, which is before either `_notify_silence_transition` call, so no
write transaction is ever open during the ntfy HTTP request. Update `_record_history`'s docstring
("one connection, one transaction") and `_notify_silence_transition`'s "must be called only after
... has committed" sentence stays true. The other open_db sites (`_last_source_fault`, the two
notify wrappers) need no change: inside the scope they reuse the cycle connection. Write
server/test_poll_efficiency.py for every behavior bullet.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto server/test_poll_efficiency.py server/test_poll_loop.py server/test_pipeline_e2e.py server/test_poll_lock.py server/test_state_writers.py && server/.venv/bin/ruff check server && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - server/test_poll_efficiency.py passes: 1 connection and 1 COMMIT for each of the seven branches, init_schema <= 1 overall, in_transaction False during the notify send
    - `server/.venv/bin/python -m pytest -q -n auto server stub-server companion/test_poll_now_lock.py` passes
  </acceptance_criteria>
  <done>A poll cycle uses one connection and one transaction, committed before any notification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poll process -> history.db | writer shared with the companion process |
| poll process -> ntfy | outbound HTTP during the cycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-22 | Denial of service | SQLite write lock held across the ntfy call | mitigate | the batch commits inside `_record_history`, before any notify; test asserts `in_transaction` is False inside the fake sender |
| T-38-23 | Tampering | partial history on failure | mitigate | the batch rolls back as a whole; the existing catch logs and the cycle continues; tested |
| T-38-24 | Denial of service | connection left open after a crash | mitigate | `connection_scope` closes in `finally`; the poll lock and scope nest so the lock is released after the scope |
</threat_model>

<verification>
- Per-branch counts asserted; server, stub-server and poll-now tests green.
</verification>

<success_criteria>
ROADMAP criterion 3 holds for poll cycles: one connection per cycle.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-07-SUMMARY.md` when done.
</output>
