---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 09
type: execute
wave: 4
depends_on: ["38-07"]
files_modified:
  - server/poll_loop.py
  - server/test_poll_state_writes.py
autonomous: true
requirements: [EFF-05]

must_haves:
  truths:
    - "A poll cycle writes poll_state.json at most once, on every branch"
    - "An unchanged repeat cycle (hold repeat, empty-sky repeat) writes it zero times"
    - "poll_state.json is written compact (no indentation, no newlines) and every reader still parses it"
    - "panel.bin is still written before poll_state.json in a cycle that changes both"
  artifacts:
    - path: "server/poll_loop.py"
      provides: "_serialize_poll_state, _persist_poll_state, compact save_poll_state"
      contains: "def _persist_poll_state"
    - path: "server/test_poll_state_writes.py"
      provides: "per-branch write counts, zero on unchanged repeat, compact format, ordering"
  key_links:
    - from: "_run_once_locked (both exits)"
      to: "_persist_poll_state(state_dir, poll_state, baseline)"
      via: "comparison with the serialisation taken right after load_poll_state"
      pattern: "_persist_poll_state\\("
---

<objective>
Save poll_state.json once per cycle, only if it changed, in compact JSON (EFF-05).

Purpose: today a cycle saves 1-2 times, including an unconditional final save even when nothing
changed, at `indent=1`. Writes are already atomic (atomic_io); this plan changes only when and how
compactly they happen.
Output: write-once logic in `_run_once_locked`, compact `save_poll_state`, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.claude/CLAUDE.md

<conventions>
- No plan/phase/requirement IDs in comments. `save_poll_state(state_dir, state)` stays in
  poll_loop as an always-write public seam (tests seed with it); no state_store module.
- Readers (load_poll_state, wake.read_battery_critical, stub-server/byos_server.py, companion
  health/airlines pages) all use json.load, so the compact format is invisible to them; do not
  touch stub-server/byos_server.py.
- Merge note: Phase 39 rewrites run_once and Phase 42 adds a poll_state read; keep the diff local.
</conventions>

<interfaces>
server/poll_loop.py (re-locate by name): `save_poll_state(state_dir, state)` ->
`atomic_io.atomic_write(_poll_state_path(state_dir), json.dumps(state, indent=1))`;
`load_poll_state(state_dir)`; `write_panel_atomic(state_dir, rendered)`.
Save sites inside `_run_once_locked`: hold branch conditional save (after the render,
`if was_hold != hold_kind or battery_changed or legacy_present or battery_critical_changed`) and
an unconditional save after the hold-branch notify; flight-on-display-changed branch
unconditional save before `_record_history`; held-flight branch conditional save
(`if battery_changed or queue_dirty or hold_exited`); empty branch conditional save
(`if battery_changed or hold_exited`); final unconditional save after the shared notify. The
notify helpers mutate `poll_state["notifications"]`, which is why a final save exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write-once, only-if-changed, compact poll_state</name>
  <files>server/poll_loop.py, server/test_poll_state_writes.py</files>
  <read_first>
    - server/poll_loop.py (save_poll_state, load_poll_state, every save site in _run_once_locked, the two notify helpers)
    - test-support/efficiency_probe.py (count_poll_state_writes, cycle_probe)
    - server/test_poll_loop.py and server/test_state_writers.py (tests that rely on save timing or format)
  </read_first>
  <behavior>
    - Over the seven research branches in sequence: count_poll_state_writes() <= 1 for every cycle
    - Hold repeat and empty-sky repeat: 0 writes, and the file's mtime_ns and bytes are unchanged
    - Flight detected: exactly 1 write, after panel.bin was replaced (wrap `atomic_io.atomic_write` to log the basename order: "panel.bin" precedes "poll_state.json")
    - A silence notification transition on an otherwise unchanged cycle: exactly 1 write (the notifications mutation is persisted)
    - The written file contains no "\n" and no ": " after a key, `json.loads` round-trips it to the in-memory state, and its length is <= the `indent=1` serialisation of the same state
    - `save_poll_state(tmp, state)` always writes (public seam), compact
    - The first cycle on a file written in the old `indent=1` format rewrites it once (compact), then repeats write 0
  </behavior>
  <action>
Add `_serialize_poll_state(state)` returning `json.dumps(state, separators=(",", ":"))` and make
`save_poll_state` write that. In `_run_once_locked`, right after `load_poll_state`, capture
`poll_state_baseline = _serialize_poll_state(poll_state)` (a string, because the dict is mutated
in place and cannot serve as its own baseline). Remove every mid-branch save listed in the
interfaces, and at the two exits (the hold-branch return and the final return) call one
`_persist_poll_state(state_dir, poll_state, poll_state_baseline)` that serialises once and writes
through `atomic_io.atomic_write` only when the text differs from the baseline. Keep the existing
order guarantee explicit in a comment: the save now happens later than before, never earlier, so
panel.bin is always written first. Keep the comments that explain why particular fields must be
persisted (hysteresis memory, queue persistence) next to the field assignments, reworded to say the
single end-of-cycle save persists them. Write server/test_poll_state_writes.py for every behavior
bullet (fake providers, `efficiency_probe.count_poll_state_writes`), and update any existing test
that pinned the old `indent=1` text or an intermediate save (record each in the SUMMARY).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto server/test_poll_state_writes.py server/test_poll_efficiency.py server/test_poll_loop.py server/test_state_writers.py server/test_pipeline_e2e.py && server/.venv/bin/python -m pytest -q -n auto server stub-server companion -m "not browser" && server/.venv/bin/ruff check server && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - server/test_poll_state_writes.py passes: <= 1 write per cycle on all seven branches, 0 on the two unchanged repeats, compact format, panel.bin first
    - `server/.venv/bin/python -m pytest -q -n auto server stub-server companion -m "not browser"` passes (readers unaffected)
  </acceptance_criteria>
  <done>poll_state.json is written once, only when changed, compact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poll process -> poll_state.json <- byos / companion readers | shared JSON state file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-28 | Tampering | torn or partial state file | mitigate | writes still go through `atomic_io.atomic_write` (mkstemp + os.replace) |
| T-38-29 | Repudiation (lost state) | a crash between the panel write and the single end-of-cycle save | accept | the save moved later, never earlier; a crash before it loses at most that cycle's queue/hysteresis update, which the next cycle recomputes, and panel.bin-before-state ordering is kept |
| T-38-30 | Denial of service | reader failing on the new format | mitigate | all readers use json.load; companion/stub-server suites run in the verify step |
</threat_model>

<verification>
- Per-branch write counts asserted; full non-browser suite green.
</verification>

<success_criteria>
At most one compact poll_state write per cycle, zero when nothing changed.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-09-SUMMARY.md` when done.
</output>
