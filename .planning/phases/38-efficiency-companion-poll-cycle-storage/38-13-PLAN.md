---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 13
type: execute
wave: 7
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05", "38-06", "38-07", "38-08", "38-09", "38-10", "38-11", "38-12"]
files_modified:
  - .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md
autonomous: false
requirements: [EFF-01, EFF-02, EFF-03, EFF-04, EFF-05, EFF-06]

must_haves:
  truths:
    - "38-EFF-BASELINE.md has Before and After tables produced by the same command, for every route and every poll-cycle branch (ROADMAP criteria 1 and 4)"
    - "The After tables show 304 on static revalidation (criterion 2), 1 SQLite connection per tab request and per cycle (criterion 3), and no 1.1 entry in any live cycle's sleeps (criterion 4)"
    - "The live Caddy compression check is recorded (before, and after deploy when available) or explicitly marked pending deploy"
  artifacts:
    - path: ".planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md"
      provides: "Before/After tables, deltas, live compression rows"
      contains: "## After"
  key_links:
    - from: "38-EFF-BASELINE.md After section"
      to: "scripts/measure_efficiency.py --label after"
      via: "verbatim output"
      pattern: "after"
---

<objective>
Record the AFTER numbers with the same instrument, check the four ROADMAP criteria against them,
and record the live compression check from the VPS (manual; Caddy is not available in CI).

Purpose: close ROADMAP criteria 1-4 with evidence. The VPS check can only be completed after the
phase is deployed, so it is a human checkpoint that accepts "before only" and leaves the after row
marked pending deploy.
Output: 38-EFF-BASELINE.md completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-VALIDATION.md
@.planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure AFTER and check the four criteria</name>
  <files>.planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md</files>
  <read_first>
    - .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md (Before section and setup)
    - scripts/measure_efficiency.py
  </read_first>
  <action>
Run `./scripts/run-all-tests.sh` first; it must pass (coverage floor included). Then run
`server/.venv/bin/python scripts/measure_efficiency.py --label after` on the same machine class
and replace the After section's sentence with its verbatim output. Add a "## Deltas" section with
one table: per tab route, HTML identity bytes, first-load identity/gzip total, script count,
connections, ms, Before -> After; per poll branch, wall s, sleeps, connections, COMMITs,
poll_state writes, Before -> After. Add a "## Criteria" checklist quoting the numbers that prove
each ROADMAP criterion: (1) bytes and ms before/after on every route; (2) static revalidation
status 304 with 0 body bytes for style.css and freshness.js, plus the freshness-tick 304 rows;
(3) connections = 1 on every tab route and every poll branch; (4) poll wall before/after and no
1.1 in the sleeps column. If any criterion does not hold, stop and report it rather than editing
numbers. Record the commit SHA the After run used.
  </action>
  <verify>
    <automated>./scripts/run-all-tests.sh && grep -c "^## After$" .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md && grep -c "^## Criteria$" .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/run-all-tests.sh` exits 0 (full suite with the coverage floor)
    - 38-EFF-BASELINE.md has one `## After` line followed by the script's tables and one `## Criteria` line
    - In the After "Routes" table every tab route shows 1 connection; in the After "Poll cycle" table every branch shows 1 connection and no sleeps entry equal to 1.1; the "Static revalidation" rows show 304
  </acceptance_criteria>
  <done>After numbers recorded and the four criteria checked against them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live compression check on the VPS (before now; after deploy, or "before-only")</name>
  <files>none (VPS action; recorded in Task 3)</files>
  <read_first>
    - 38-VALIDATION.md "Manual-Only Verifications"
    - deploy/Caddyfile (companion block now has encode)
  </read_first>
  <action>The developer runs the curl commands against production. Production still runs the pre-phase code until this branch is merged and deployed, so the first run is the BEFORE row; the AFTER row is taken after the normal deploy pipeline has run (activate.sh validates and reloads the Caddy site file). If the deploy has not happened yet, the developer answers "before-only" and the after row is recorded as pending deploy; this does not block the phase.</action>
  <what-built>`encode zstd gzip` in the companion site block; ETag/304 on static files; freshness 304s.</what-built>
  <how-to-verify>
    From any machine (replace the host with SKYPANE_COMPANION_HOST, e.g. skypane.algernon.ovh):
    ```bash
    H=https://skypane.algernon.ovh
    curl -s -o /dev/null -D - -w 'size_download=%{size_download}\n' -H 'Accept-Encoding: zstd, gzip' "$H/static/style.css" | grep -iE '^(content-encoding|etag|cache-control|vary)|size_download'
    curl -s -o /dev/null -D - -w 'size_download=%{size_download}\n' -H 'Accept-Encoding: gzip' "$H/static/freshness.js" | grep -iE '^(content-encoding|etag|cache-control)|size_download'
    ETAG=$(curl -s -o /dev/null -D - -H 'Accept-Encoding: gzip' "$H/static/style.css" | awk -F': ' 'tolower($1)=="etag"{print $2}' | tr -d '\r')
    curl -s -o /dev/null -w 'revalidate=%{http_code} size=%{size_download}\n' -H 'Accept-Encoding: gzip' -H "If-None-Match: $ETAG" "$H/static/style.css"
    ```
    Expected BEFORE (old code): no Content-Encoding, no ETag, `public, max-age=300`, size about 140 KB for style.css; the revalidate line prints 200.
    Expected AFTER (deployed): `content-encoding: zstd` (or gzip), an ETag ending in `-zstd`/`-gzip`, `public, no-cache`, `vary: Accept-Encoding`, size about 37 KB or less; the revalidate line prints `revalidate=304 size=0`.
    Also confirm the device host is not compressed: `curl -s -o /dev/null -D - -H 'Accept-Encoding: gzip' https://<SKYPANE_PUBLIC_HOST>/device/v1/display | grep -i content-encoding` prints nothing (an auth error status is fine; only the absence of Content-Encoding matters).
  </how-to-verify>
  <acceptance_criteria>
    - The developer pasted the BEFORE output, and either the AFTER output or the word "before-only"
  </acceptance_criteria>
  <verify>
    <human-check>Developer runs the commands and pastes the output; Claude compares it with the Expected lines.</human-check>
  </verify>
  <done>Live compression evidence captured (after row possibly pending deploy).</done>
  <resume-signal>Paste the outputs, or type "before-only" with the BEFORE output</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the live compression rows</name>
  <files>.planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md</files>
  <read_first>
    - The developer's Task 2 reply
    - 38-EFF-BASELINE.md "Live compression (VPS)" table
  </read_first>
  <action>Fill the "Live compression (VPS)" table: one row per URL and run (when: before / after, UTC date, Accept-Encoding sent, Content-Encoding received, size_download, revalidate status). If the reply was "before-only", add after rows whose cells read "pending deploy" and add one sentence saying the developer re-runs the Task 2 commands after deploy and records them here. No secret, cookie or host key goes into the file.</action>
  <verify>
    <automated>grep -c "^## Live compression (VPS)$" .planning/phases/38-efficiency-companion-poll-cycle-storage/38-EFF-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - The Live compression table has at least two before rows (style.css, freshness.js) and either measured after rows or rows marked "pending deploy"
  </acceptance_criteria>
  <done>The baseline document is the single before/after record for the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> production over HTTPS | read-only curl requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-43 | Information disclosure | baseline document | mitigate | only public static-asset headers and sizes are recorded; no cookie, password, env value or key |
| T-38-44 | Tampering | device protocol compressed by mistake | mitigate | the checkpoint includes a device-host curl expecting no Content-Encoding |
</threat_model>

<verification>
- Full suite green; After and Criteria sections present; live rows recorded or marked pending deploy.
</verification>

<success_criteria>
ROADMAP criteria 1-4 are evidenced in 38-EFF-BASELINE.md.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-13-SUMMARY.md` when done.
</output>
