---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 08
type: execute
wave: 4
depends_on: ["38-03", "38-06"]
files_modified:
  - companion/app.py
  - companion/test_request_connections.py
autonomous: true
requirements: [EFF-03]

must_haves:
  truths:
    - "A GET of any tab route opens exactly one SQLite connection"
    - "A static or login request opens none"
    - "A second request in the same process runs the schema DDL zero times"
    - "POST /poll-now runs the whole poll cycle on the request's single connection"
    - "A database failure behaves exactly as before at each call site, with a single connect attempt per request (no retry storm)"
  artifacts:
    - path: "companion/app.py"
      provides: "request connection scope around do_GET and do_POST dispatch"
      contains: "connection_scope"
    - path: "companion/test_request_connections.py"
      provides: "per-route connection and schema counts"
  key_links:
    - from: "companion/app.py Handler.do_GET / do_POST"
      to: "history_db.connection_scope(self.args.state_dir)"
      via: "with block around the existing dispatch"
      pattern: "history_db\\.connection_scope\\("
---

<objective>
One SQLite connection per companion request (EFF-03, ROADMAP criterion 3 for page requests).

Purpose: a page request opens 9-12 connections today. With the storage scope, every existing
`open_db()` call site in app.py and the page modules shares one lazily opened connection, with no
signature change.
Output: the scope in do_GET/do_POST, companion/test_request_connections.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-03-SUMMARY.md
@.claude/CLAUDE.md

<conventions>
- No plan/phase/requirement IDs in comments. Companion tests obey companion/test_suite_guards.py;
  use `app_server_in_process` (monkeypatch-visible counting) and `efficiency_probe.count_db()`.
- Keep the if-chain routes and the dict context; no route table, no ctx plumbing of `conn`.
- Merge note: Phase 42 edits companion/app.py (Update route); resolve conflicts by keeping both.
</conventions>

<interfaces>
From plan 03: `history_db.connection_scope(state_dir)` (thread-bound via threading.local,
re-entrant, lazy, remembers an open failure and re-raises it at each open_db), `open_db(state_dir)`.
companion/app.py: `Handler.do_GET(self)` (large if-chain), `Handler.do_POST(self)` (Origin check
first, then if-chain; `_handle_poll_now` calls `poll_loop.run_once(..., lock_timeout_s=0)` which
now opens its own nested `connection_scope` for the same state dir, reusing the request's).
`ThreadingHTTPServer` runs one thread per request. `self.args.state_dir` is the state dir.
Call sites that will share the connection: app.py `poll_cooldown_remaining`, `_safe_last_checkin_ts`,
`_safe_latest_runway_event`, `mark_poll_triggered`; home_page, health_page `_safe_query`,
history_page, config_page (three sites).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Request scope around the GET/POST dispatch</name>
  <files>companion/app.py, companion/test_request_connections.py</files>
  <read_first>
    - companion/app.py (do_GET, do_POST, _handle_poll_now, poll_cooldown_remaining, mark_poll_triggered)
    - server/history_db.py (connection_scope)
    - companion/conftest.py (app_server_in_process)
    - test-support/efficiency_probe.py (count_db, seed_history)
    - companion/test_poll_now_lock.py (how /poll-now is exercised in-process)
  </read_first>
  <behavior>
    - Seeded state (efficiency_probe.seed_history), logged in: GET each of the six NAV_TABS routes -> count_db().connections == 1 per request
    - GET /static/style.css, GET /login (unauthenticated), GET of an unauthenticated tab (303) -> 0 connections
    - After one warm request, a second GET of `/` -> init_schema == 0
    - POST /poll-now (fake providers, cooldown elapsed) -> exactly 1 connection for the whole request including the cycle, and the cooldown meta is persisted (a following GET shows the cooldown)
    - Database unavailable (sqlite3.connect monkeypatched to raise sqlite3.OperationalError): one GET /flights attempts the connect exactly once, however many call sites read the database (do not assert the status here: `poll_cooldown_remaining` is unguarded today and returns 500; the lazy-context plan makes it degrade and asserts 200)
    - Two concurrent GETs (two threads) each get their own connection (2 connections total, no sqlite ProgrammingError)
  </behavior>
  <action>
Rename the current bodies to `_dispatch_get(self)` and `_dispatch_post(self)` unchanged, and make
`do_GET`/`do_POST` each `with history_db.connection_scope(self.args.state_dir): return
self._dispatch_get()` (resp. post). Keep the Origin check as the first statement of the POST
dispatch, unchanged. Add one comment: one connection per request, opened lazily on the first
database read, so static and login requests never open one. Keep `poll_cooldown_remaining`'s
behaviour as is in this plan (its degrade-to-0 change belongs to the lazy-context plan). Write
companion/test_request_connections.py covering every behavior bullet with `app_server_in_process`.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto companion/test_request_connections.py companion/test_poll_now_lock.py companion/test_suite_guards.py && server/.venv/bin/python -m pytest -q -n auto companion -m "not browser" && server/.venv/bin/ruff check companion && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - companion/test_request_connections.py passes: 1 connection for each of the six tab routes, 0 for static/login/unauthenticated, 0 init_schema on the second request, 1 for POST /poll-now
    - `server/.venv/bin/python -m pytest -q -n auto companion -m "not browser"` passes
  </acceptance_criteria>
  <done>Each page request uses one connection; the poll-now cycle shares it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request threads -> history.db | one connection per request thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-25 | Tampering / DoS | a connection shared across request threads | mitigate | scope is thread-local; concurrency test with two threads |
| T-38-26 | Elevation of privilege | auth order changed by the wrapper | mitigate | the scope only wraps the unchanged dispatch; require_session and the POST Origin check run exactly as before; existing auth tests stay green |
| T-38-27 | Denial of service | open-failure retry storm | mitigate | the scope remembers the failure; each call site degrades via its existing catch; test counts one attempt per request |
</threat_model>

<verification>
- Per-route connection counts asserted; companion suite green.
</verification>

<success_criteria>
ROADMAP criterion 3 holds for page requests: one connection per request.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-08-SUMMARY.md` when done.
</output>
