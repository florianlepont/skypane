---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 06
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - companion/layout.py
  - companion/app.py
  - companion/test_page_scripts.py
autonomous: true
requirements: [EFF-02]

must_haves:
  truths:
    - "Each authenticated page references only the global scripts plus its own declared scripts, in today's relative order, with no build step"
    - "For every tab route in layout.NAV_TABS and several seeded states, every DOM hook present in the served HTML has its script among the page's script tags"
    - "A tab route without a declared script tuple fails the suite (so a later page cannot silently lose its scripts)"
    - "404/403 pages carry the global set only; the login page keeps login-card.js only; Health keeps battery-trend.js in its body"
    - "No script route, serve method or file is removed"
  artifacts:
    - path: "companion/layout.py"
      provides: "GLOBAL_PAGE_SCRIPTS, SHELL_SCRIPT_ORDER, page_shell(scripts=...)"
      contains: "SHELL_SCRIPT_ORDER"
    - path: "companion/app.py"
      provides: "_PAGE_SCRIPTS per route next to _PAGE_TITLES"
      contains: "_PAGE_SCRIPTS"
    - path: "companion/test_page_scripts.py"
      provides: "hook-coverage and exact-set tests"
  key_links:
    - from: "companion/app.py _page_shell_for"
      to: "layout.page_shell(scripts=_PAGE_SCRIPTS[route])"
      via: "keyword argument"
      pattern: "scripts=_PAGE_SCRIPTS"
---

<objective>
Load only the scripts each page uses (EFF-02), with no build step.

Purpose: today every authenticated page emits 15 `<script defer>` tags (16 on Health). Each page
should reference its own set, a superset over every state of that page including regions that
freshness.js may swap in later.
Output: `page_shell(scripts=...)`, per-route tuples, a behaviour-level hook-coverage test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.claude/CLAUDE.md

<conventions>
- No plan/phase/requirement IDs in comments. Companion tests obey companion/test_suite_guards.py:
  the hook map lives in the test as selectors -> layout `*_SCRIPT_SRC` constants; never read JS
  source text.
- Named templates stay a later phase: replace the 15 positional `%s` script slots with one `%s`
  filled by a `"".join(...)`, nothing more.
- Merge note: Phase 42 (OTA) adds an Update page and a nav entry in companion/layout.py and
  companion/app.py; the coverage test iterates `layout.NAV_TABS`, so that page fails CI until it
  declares its tuple (it needs confirm-submit.js for its `form[data-confirm]`).
</conventions>

<interfaces>
companion/layout.py: the 16 `*_SCRIPT_SRC` constants (NAV_DROPDOWN, DIRTY_STATE, LIST_FILTER,
COPY_BUTTON, FRESHNESS, PANEL_LOOKUP, FLASH_CLEANUP, POLL_COOLDOWN, CONFIRM_SUBMIT, THEME_PREVIEW,
FLIGHT_ROWS, LOGIN_CARD, SUBMIT_GUARD, RELATIVE_TIME, QUICK_SWITCH, VALUE_CONTROLS);
`NAV_TABS` (route, label) tuples; `page_shell(title, active, body, ui_theme="auto", flash=None,
banner=None, health_alert=None, lang=None, device_config=None)` whose template ends with 15
`'<script src="%s" defer></script>\n'` lines filled in this order: nav-dropdown, dirty-state,
list-filter, copy-button, freshness, panel-lookup, flash-cleanup, poll-cooldown, confirm-submit,
theme-preview, flight-rows, submit-guard, relative-time, quick-switch, value-controls.
companion/app.py: `_PAGE_TITLES = {layout.HOME_ROUTE: "Home", ...}` (six tabs);
`Handler._page_shell_for(route, body, ctx)` is the single page_shell call site for every tab GET
and the rejected settings POST; `_not_found_page()` / `_forbidden_page()` call page_shell directly.
DOM hooks per script (from each script's own guard clause; declare these in the test):
nav-dropdown `#site-nav-toggle`; dirty-state `form[data-dirty-form]`; list-filter
`[data-filter-input]`; copy-button `[data-copy-value]`; freshness `[data-loaded-at]`; panel-lookup
`#panel-lookup-dialog`; flash-cleanup `.banner--flash`; poll-cooldown `#poll-trigger-btn`;
confirm-submit `form[data-confirm]`; theme-preview `.theme-live-preview__image`; flight-rows
`[data-row-toggle]` and `.flight-detail-row`; submit-guard `form`; relative-time
`time[data-relative]`; quick-switch `form[data-quick-switch]`; value-controls `[data-value-control]`.
Research hook scan (a hint, the test is authoritative): Home freshness, quick-switch; Display
dirty-state, freshness, quick-switch, theme-preview, value-controls; Device dirty-state,
poll-cooldown, confirm-submit (calendar connected), quick-switch, value-controls; Flights
copy-button, flight-rows, freshness, list-filter (+ panel-lookup if the dialog renders); Health
freshness (+ copy-button, list-filter with registry rows); Airlines list-filter, panel-lookup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook-coverage test first, then page_shell(scripts=...) and per-route tuples</name>
  <files>companion/test_page_scripts.py, companion/layout.py, companion/app.py</files>
  <read_first>
    - companion/layout.py (the *_SCRIPT_SRC constants and page_shell's template and argument tuple)
    - companion/app.py (_PAGE_TITLES, _page_shell_for, _not_found_page, _forbidden_page)
    - companion/test_browser_ux_helpers.py (seed_state_dir) and companion/test_status_pages_helpers.py, companion/test_view_pages_helpers.py (seeding patterns: empty, flights, registry rows, calendar connected, flash query)
    - test-support/companion_markup.py (parse_html, select)
  </read_first>
  <behavior>
    - For each route in layout.NAV_TABS x states {empty state dir, seeded history + unresolved registry rows + calendar connected + gallery file, and `?flash=saved`}: every selector in HOOKS that matches in the served HTML has its script src in the page's `script[src]` list
    - For each tab route and state: the set of shell script srcs (excluding battery-trend.js, which Health emits in its body) equals set(layout.GLOBAL_PAGE_SCRIPTS) | set(app._PAGE_SCRIPTS[route])
    - Every route in layout.NAV_TABS has an entry in app._PAGE_SCRIPTS (a missing tuple fails with the route named)
    - The shell script srcs appear in layout.SHELL_SCRIPT_ORDER's relative order on every page
    - Authenticated 404 (`/no-such-page`) carries exactly GLOBAL_PAGE_SCRIPTS; `/login` carries exactly [login-card.js]
    - No page repeats a script src
  </behavior>
  <action>
Write companion/test_page_scripts.py first (it fails today on the exact-set and missing-tuple
tests; that is the RED step). Use `make_app_server(seed=...)` or `app_server_in_process` from
companion/conftest.py and `companion_markup.parse_html(...).select("script[src]")`.
Then in companion/layout.py add `SHELL_SCRIPT_ORDER`, a tuple of today's 15 shell script srcs in
today's emission order, and `GLOBAL_PAGE_SCRIPTS = (NAV_DROPDOWN_SCRIPT_SRC,
FLASH_CLEANUP_SCRIPT_SRC, SUBMIT_GUARD_SCRIPT_SRC, RELATIVE_TIME_SCRIPT_SRC)` (the hamburger on
every page, a flash on any page, every form, the nav status line's relative time). Add a
`scripts=()` keyword to `page_shell`; emit `"".join('<script src="%s" defer></script>\n' % src
for src in SHELL_SCRIPT_ORDER if src in wanted)` where `wanted = set(GLOBAL_PAGE_SCRIPTS) |
set(scripts)`, through one `%s` slot, replacing the 15 positional slots and the 15 per-script
comments with one comment explaining the rule (global set on every shell page, each page adds
what its hooks need across every state, order fixed by SHELL_SCRIPT_ORDER). A src in `scripts`
that is not in SHELL_SCRIPT_ORDER raises ValueError (fail fast on a typo).
In companion/app.py add `_PAGE_SCRIPTS` next to `_PAGE_TITLES`, one tuple per tab route, starting
from the research hook scan above and taking the superset over every state the test seeds (for
example Flights keeps flight-rows, copy-button and list-filter even with zero rows, since rows
arrive by freshness swap). Pass `scripts=_PAGE_SCRIPTS[route]` in `_page_shell_for`. Leave
`_not_found_page`/`_forbidden_page` on the default (global set). Do not delete any script route,
delegate or file.
Then run the whole companion suite and update only those existing tests whose assertion is the
old serve-everywhere convention (a script expected on a page that has no hook for it); record each
changed test and why in the SUMMARY.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto companion/test_page_scripts.py companion/test_suite_guards.py && server/.venv/bin/python -m pytest -q -n auto companion -m "not browser" && server/.venv/bin/ruff check companion && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - companion/test_page_scripts.py passes; its per-route exact-set test is parametrised over all six NAV_TABS routes and at least two states
    - `server/.venv/bin/python -m pytest -q -n auto companion -m "not browser"` passes
    - `server/.venv/bin/python -m pytest -q companion/test_browser_ux_01.py companion/test_browser_ux_02.py companion/test_browser_ux_03.py companion/test_browser_ux_04.py companion/test_browser_ux_health_drawings.py companion/test_browser_ux_quiet_wake.py` passes in CI (behaviour holds with fewer scripts: swaps, flight rows, theme preview, dial)
    - companion/test_page_scripts.py also asserts that every tab route serves fewer than 15 shell script tags in the seeded state (the measurable drop the baseline records)
  </acceptance_criteria>
  <done>Each page loads only the scripts its hooks need; a missing tuple fails CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered HTML -> browser | script inclusion decides which client behaviour exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-20 | Tampering (lost client safeguard) | confirm-submit / submit-guard missing on a page | mitigate | submit-guard is global; confirm-submit is required wherever `form[data-confirm]` renders, enforced by the hook-coverage test; server-side confirmation still applies where it exists |
| T-38-21 | Elevation of privilege | new script src from a variable | mitigate | srcs come only from layout constants; `page_shell` rejects any src not in SHELL_SCRIPT_ORDER; CSP unchanged |
</threat_model>

<verification>
- Hook-coverage and exact-set tests green; companion suite and CI browser suite green.
</verification>

<success_criteria>
Per-page scripts with no build step; coverage enforced for current and future tab routes.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-06-SUMMARY.md` when done.
</output>
