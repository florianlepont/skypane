---
phase: 38-efficiency-companion-poll-cycle-storage
plan: 05
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - companion/pages/health_page.py
  - companion/test_health_signals.py
autonomous: true
requirements: [EFF-04]

must_haves:
  truths:
    - "health_signals(state_dir, now) returns every health state, the severity and the anomaly list without building any markup"
    - "compute_health_state(state_dir, now) returns exactly today's dict and is built from one health_signals snapshot, so the nav dot and the Health banner cannot disagree"
    - "For every existing severity fixture, health_signals()['severity'] and ['anomalies'] equal compute_health_state()'s"
  artifacts:
    - path: "companion/pages/health_page.py"
      provides: "health_signals, safe_health_signals, health_state_from_signals, state-only helpers"
      contains: "def health_signals"
    - path: "companion/test_health_signals.py"
      provides: "equivalence and no-markup tests"
  key_links:
    - from: "compute_health_state"
      to: "health_state_from_signals(health_signals(...))"
      via: "composition"
      pattern: "health_state_from_signals\\("
---

<objective>
Split Health's severity computation from its markup (EFF-04, "severity computed without markup").

Purpose: every authenticated page needs the nav-dot severity, but only Home and Health need the
Health markup. This plan adds the markup-free path; the lazy page context (later plan) uses it.
No app.py change here.
Output: `health_signals`, `safe_health_signals`, `health_state_from_signals`, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-CONTEXT.md
@.planning/phases/38-efficiency-companion-poll-cycle-storage/38-RESEARCH.md
@.claude/CLAUDE.md

<conventions>
- No plan/phase/requirement IDs in comments. Companion tests obey companion/test_suite_guards.py.
- Do not change the pinned tuple returns of the `_x_section()` builders (a harness unpacks
  `_battery_section`'s 2-tuple); add state-only siblings and have the builders reuse them.
</conventions>

<interfaces>
companion/pages/health_page.py (re-locate by name):
- `_read_health_inputs(state_dir, now) -> dict` (device_health, pipeline_ts, last_detection,
  source_fault_raw, trend_rows, daily_rows, corroboration_counts, device_config, registry_rows);
  `_DB_UNAVAILABLE` sentinel; `_safe_query(state_dir, fn)`.
- `compute_health_state(state_dir, now=None)` returns keys: now, source_fault_raw, registry_rows,
  offbox, wake_interval_s, device_html, device_state, device_detail_html, pipeline_html,
  pipeline_state, pipeline_detail_html, battery_html, battery_state, battery_caption,
  corroboration_html, disagreement_warn, anomalies, severity.
- `safe_health_state(state_dir, now=None)` -> None on any exception.
- Builders: `_device_section(device_health, now, warn_s=, error_s=, next_wake_iso=,
  effective_interval_s=, hold_reason=) -> (html, state)`, `_device_timestamp_only`,
  `_pipeline_section(pipeline_ts, last_detection, now) -> (html, state)`,
  `_pipeline_timestamp_only`, `_battery_section(trend_rows, daily_rows=None) -> (html, state)`,
  `_battery_trend_caption`, `_corroboration_section(counts) -> (html, disagreement_warn)`;
  existing state helpers `battery_status(rows)`, `corroboration_status(counts)`,
  `coverage_status(rows)`, `staleness_status(age, warn_s, error_s)`, `_meta_flag_true`,
  `offbox_backup_status(now)`, `overall_severity(...)`, `collect_anomalies(...)`.
- `wake.effective_wake_interval_s(cfg)`, `wake.device_staleness_thresholds(s)`,
  `wake.next_wake_status(device_ts, cfg) -> (next_wake_iso, effective_interval_s, hold_reason)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: health_signals + health_state_from_signals, compute_health_state as their composition</name>
  <files>companion/pages/health_page.py, companion/test_health_signals.py</files>
  <read_first>
    - companion/pages/health_page.py (compute_health_state, safe_health_state, _read_health_inputs, the four _x_section builders and their state logic, overall_severity, collect_anomalies)
    - companion/test_status_pages_helpers.py and companion/test_status_pages_0*.py (existing severity fixtures and how state dirs are seeded)
    - companion/test_suite_guards.py
  </read_first>
  <behavior>
    - For each seeded scenario (fresh ok state; stale device check-in; stale pipeline; source_fault meta "True"; battery drop; disagreement counts; unresolved registry rows; database unavailable): health_signals(...)["severity"] == compute_health_state(...)["severity"] and ["anomalies"] are equal, same `now`
    - health_signals builds no markup: with `_device_section`, `_pipeline_section`, `_battery_section`, `_corroboration_section`, `_device_timestamp_only`, `_pipeline_timestamp_only` monkeypatched to raise AssertionError, health_signals still returns and its values contain no "<"
    - compute_health_state(...) returns the same key set as before (assert the exact key list above) and equal values to health_state_from_signals(health_signals(...))
    - safe_health_signals returns None when `_read_health_inputs` raises RuntimeError
    - health_signals exposes next_wake_iso, effective_interval_s, hold_reason (for the frame strip and the freshness token later)
  </behavior>
  <action>
Add state-only helpers that hold the exact state logic now inside the builders:
`_device_state(device_health, now, warn_s, error_s, hold_reason)`,
`_pipeline_state(pipeline_ts, last_detection, now)`, `_battery_state(trend_rows, daily_rows)`,
`_disagreement_warn(counts)` (reuse existing `battery_status`/`corroboration_status` where they
already are the state logic). Make each `_x_section` builder call its sibling for the state so the
two can never diverge, keeping their return shapes. Add `health_signals(state_dir, now=None)`:
reads `_read_health_inputs` once and returns a dict with `now`, `inputs` (the raw inputs dict,
consumed by the markup step), `wake_interval_s`, `warn_s`, `error_s`, `next_wake_iso`,
`effective_interval_s`, `hold_reason`, `device_state`, `pipeline_state`, `battery_state`,
`disagreement_warn`, `coverage_state`, `source_fault`, `source_fault_raw`, `registry_rows`,
`offbox`, `severity`, `anomalies`. Add `health_state_from_signals(signals)` that builds all the
`*_html` values and `battery_caption` from `signals["inputs"]` and returns exactly today's
`compute_health_state` dict (states, severity and anomalies copied from the signals, never
recomputed). `compute_health_state(state_dir, now=None)` becomes
`health_state_from_signals(health_signals(state_dir, now))`; keep its docstring's invariant
(one snapshot feeds dot and banner). Add `safe_health_signals(state_dir, now=None)` mirroring
`safe_health_state`'s broad fail-closed catch. Write companion/test_health_signals.py for every
behavior bullet, seeding under `tmp_path` with the existing helpers' patterns.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q -n auto companion/test_health_signals.py companion/test_status_pages_01.py companion/test_status_pages_02.py companion/test_status_pages_03.py companion/test_status_pages_04.py companion/test_status_pages_05.py companion/test_status_pages_05b.py companion/test_status_pages_06.py companion/test_status_pages_07.py companion/test_health_offbox.py companion/test_suite_guards.py && server/.venv/bin/ruff check companion && python3 scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - companion/test_health_signals.py passes with at least 10 tests (8 scenarios parametrised + no-markup + key-set + safe wrapper)
    - `server/.venv/bin/python -m pytest -q -n auto companion -m "not browser"` passes (Health and Home render unchanged)
  </acceptance_criteria>
  <done>Severity and anomalies are computable without markup and equal to today's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | pure computation over existing reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-18 | Repudiation (a hidden problem) | severity divergence between nav dot and banner | mitigate | both come from one `health_signals` snapshot; equivalence test over eight scenarios |
| T-38-19 | Denial of service | exception in signals 500-ing every page | mitigate | `safe_health_signals` keeps the fail-closed broad catch; tested |
</threat_model>

<verification>
- Equivalence tests green; all Health/Home page tests unchanged and green.
</verification>

<success_criteria>
Health severity has a markup-free path equal to the current computation.
</success_criteria>

<output>
Create `.planning/phases/38-efficiency-companion-poll-cycle-storage/38-05-SUMMARY.md` when done.
</output>
