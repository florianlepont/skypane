---
phase: 28-companion-review-feedback-round-2-five-more-findings-from-th
plan: 09
type: execute
wave: 8
depends_on: ["28-05", "28-08"]
files_modified:
  - companion/test_browser_ux.py
  - companion/test_config_page.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .claude/skills/sketch-findings-skypane/SKILL.md
autonomous: true
requirements: [CFG-77, CFG-78]

must_haves:
  truths:
    - "CFG-78: exactly ONE save affordance is associated with the settings form — asserted by resolving each submit-shaped control's own `.form` property in a live browser, not by counting buttons on the page, so the calendar/airlines/quick-switch/rules forms are excluded by construction rather than by a hand-maintained allow-list"
    - "CFG-78: no orphaned CSS rule is left pointing at the retired `.js`-hide mechanism, and no second submit-shaped control sits inside the settings form's DOM subtree"
    - "CFG-78: the runway radios' cross-tree `form=\"settings-form\"` wiring drives the SAME bar-appears -> Enregistrer -> persisted-to-disk path every natively-nested control does — closing the path CFG-74(c) named before the reversal, now against the real POST instead of the fetch"
    - "CFG-78: the phase gate re-derives EVERY check count by RUNNING, never by arithmetic; the sandbox baseline failures are named BY NAME and their count re-verified rather than carried forward from 28-04's SUMMARY; the full-suite runner's exit status is recorded"
    - "CFG-78: the ledger states plainly which of CFG-74's original four clauses were genuinely built (NONE — it was superseded before implementation) versus what replaced them"
    - "CFG-74 must NOT be ticked, and nothing in this plan may imply it became satisfied. It stays unticked-and-superseded exactly as REQUIREMENTS.md's own note already records"
    - "The ledger is written against the CODE on the finished tree, not against what the plans intended — the standard Phase 27's own ledger set, and the standard CFG-65's deliberately unticked row demonstrates"
  artifacts:
    - path: "companion/test_browser_ux.py"
      provides: "the single-affordance audit and the runway form=-path regression check against the restored bar"
      contains: "settings-form"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the Phase 28 coverage ledger with per-clause evidence for CFG-77/CFG-78, and CFG-74 recorded as superseded-never-built"
      contains: "CFG-77"
    - path: ".claude/skills/sketch-findings-skypane/SKILL.md"
      provides: "the 'Settings auto-save' standing paragraph superseded in writing by the restored-bar account"
      contains: "Settings auto-save"
  key_links:
    - from: "the runway radios (rendered outside the settings form)"
      to: "the settings form's on-disk state"
      via: "form=\"settings-form\" -> dirty-state.js's document-level delegated listener -> the bar -> a real native POST"
      pattern: "settings-form"
    - from: ".planning/REQUIREMENTS.md's Phase 28 coverage ledger"
      to: "the finished tree"
      via: "each clause's evidence being a named check or a measured figure, never a plan's intention"
      pattern: "CFG-78"
---

<objective>
The phase's closing plan. Three jobs, in this order: prove the consolidation, prove the
path CFG-74 named before it was superseded, and write the record.

**Prove the consolidation (CFG-78).** 28-08 relocated the native submit into the bar and
deleted the `.js`-hide rule. The claim "exactly one save affordance on the settings pages"
is now true by construction — but "by construction" is what 27-06 said about the title
form, and it was wrong. So it gets an executable contract, shaped so it cannot drift:
resolve every submit-shaped control's own `.form` property in a live browser and require
exactly one whose form is `settings-form`. That excludes Calendar's
connect/replace/disconnect, Airlines' "Enregistrer le nom", the three `role="switch"`
instant toggles, the per-flight rules add/delete, the notifications test and the quick-LED
form automatically, because each genuinely belongs to a different form — no hand-maintained
allow-list to rot.

**Prove the runway path.** This was the developer's ORIGINAL, narrower report before
everything else happened — *"Quand je fais un changement de paramètre (comme la piste) je
ne vois pas le bouton enregistrer apparaître"* — and it is the one clause of the superseded
CFG-74 that carries forward with its value intact. The runway radios live OUTSIDE the
settings form and reach it only through a `form="settings-form"` attribute; a form element
never receives a `change` event from a control that is merely `form=`-associated with it,
which is exactly the B1 defect `dirty-state.js`'s document-level delegation exists to
handle. That path deserves the same pass/fail contract every natively-nested control meets,
and it has never had one. Now it is proven against the restored bar's real POST, end to
end, to disk.

**Write the record, against the code.** The Phase 28 coverage ledger, the ROADMAP entry,
and the design-system skill's now-false "Settings auto-save" standing paragraph.

**What this plan must NOT do.** CFG-74 is not ticked and is not rounded up. Nothing here
was built for it, because it was superseded before implementation — and the reason is worth
writing down accurately rather than glossing. The requirement's own text named "a
regression from Phase 27's removal of the manual save button" as its root cause, and with
that button restored, the resilience machinery it specified has no fetch left to wrap. The
real Safari Network-tab data the developer captured while testing the silent auto-save (a
genuine 204 success, the value persisted, with zero visible confirmation) is what led
directly to the reversal, and it stands as CFG-74's own evidence for why
resilience-around-a-silent-mechanism was the wrong fix for the symptom it correctly
diagnosed. Say that; do not say "fixed", and do not say "not met".
</objective>

<decisions_covered>
CFG-78's remaining clauses (the single-affordance proof, the runway `form=` regression
check, and the closing gate's three obligations), plus the phase-owner's duty to leave the
written record consistent: REQUIREMENTS.md's coverage ledger, ROADMAP.md's Phase 28 entry,
and the design-system skill's standing paragraph.

**Explicitly NOT this plan's:** CFG-74 (superseded before implementation, ticked by nobody)
and CFG-63 (retired in place; its tick stands because the work it records really was done).
Neither may be reported as satisfied by this phase's restoration.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-CONTEXT.md
@.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan is the only writer of all five of its files in Wave 8. It runs strictly after
28-08 (which owns every source file this phase still touches) and after 28-05.
</files_owned>

<interfaces>
Measured live at planning time (2026-09-16). **Re-derive every number by running before you
use it — 28-05 and 28-08 both land between this measurement and your execution.**

  companion/pages/config_page.py:114   SETTINGS_FORM_ID = "settings-form"
  companion/pages/config_page.py:2315  the runway radios' own form= interpolation
  companion/pages/config_page.py:2332  the Runway group's DIRTY_SECTION_ATTR wrapper,
                                       labelled i18n.t("Runway")
  companion/i18n_fr/display.py:151     "Runway": "Piste"

  Rendered [data-dirty-section] labels in document order, from calling render() directly:
    Display -> Frame colours, Calendar, Runway, Quiet hours
    Device  -> Wake interval, Diagnostic LED, Notifications

  EXPECTED_CHECK_COUNT, pre-28-05 and pre-28-08 (all three WILL have moved):
    companion/test_browser_ux.py      91  (last assignment, line 910)
    companion/test_config_page.py    265  (last assignment, line 1020)
    companion/test_companion_app.py  316  (last assignment, line 857)

  style.css literal counts, pre-28-08 (both WILL have moved; recorded so you can tell a
  real change from a miscount):
    grep -c 'data-static-save-fallback' companion/static/style.css  ->  2
        (one rule at line 1648, one prose mention inside the superseded-bar comment at
         line 2681 — a criterion of "0 occurrences" would have been wrong even before)
    grep -c 'save-status' companion/static/style.css  ->  5
        (the five comment blocks 28-08 was required to supersede in writing)

  Sandbox baseline, as recorded by 28-04-SUMMARY.md: "exactly the documented 5-check
  root-sandbox baseline, verified BY NAME: 4x WR-11 (manual_save_failed /
  manual_delete_failed flash keys, add_entry() / delete_entry() read-only-dir simulation)
  + 1x anomaly_active() for a non-existent state_dir. Coverage 93%."
  *** That is 28-04's observation, not this plan's. Re-derive it by RUNNING. ***

  Suites: companion/test_browser_ux.py, companion/test_companion_app.py,
          companion/test_config_page.py, companion/test_contrast_check.py,
          companion/test_i18n.py, companion/test_status_pages.py,
          companion/test_view_pages.py
  Runners: scripts/run-all-tests.sh, scripts/run_all_tests.py, scripts/run-local-verify.sh

  Ledger format to match, read before writing anything:
    .planning/REQUIREMENTS.md line 991 onward — "## Phase 27 coverage ledger (...)", its
    opening paragraph explaining what the ledger is and is not, then per-correction
    "### Correction N — title (CFG-xx, CFG-yy)" sections carrying clause-by-clause
    evidence.
    .planning/REQUIREMENTS.md lines 222-240 — the per-requirement traceability TABLE rows,
    a separate and additional record from the ledger section.
    .planning/REQUIREMENTS.md lines 89, 100, 103-104 — CFG-63's retirement note, CFG-74's
    superseded note, and CFG-77/CFG-78's binding per-clause wording. CFG-77 and CFG-78 are
    the target wording; tick each clause only where evidence exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: One affordance, proven by form association — and the runway's cross-tree path proven to disk</name>
  <files>companion/test_browser_ux.py, companion/test_config_page.py</files>
  <behavior>
    - Exactly one submit-shaped control on the whole settings page resolves its own form to the settings form, on both /display and /device, in both languages
    - Every OTHER submit-shaped control resolves to a different form, and each is NAMED in the check's failure message so a regression says WHICH one drifted
    - No orphaned CSS rule targets the retired `.js`-hide mechanism
    - Selecting a runway radio reveals the bar naming "Runway"/"Piste"; clicking Enregistrer persists the runway to disk through a real navigation
  </behavior>
  <read_first>
    - `.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-08-SUMMARY.md` — what 28-08 actually shipped: its measured clearance figures, its EXPECTED_CHECK_COUNT numbers, the names of the bar helpers it added, and whether CONTEXT.md's dial-repaints-for-free claim held. Everything below is written against 28-08's intent; the SUMMARY is written against what happened
    - `companion/pages/config_page.py` — `runway_fieldset()` in full (around lines 2237-2340): its docstring on why the card is a SIBLING of the settings form, the `form="%s"` interpolation on each radio, and its `DIRTY_SECTION_ATTR` wrapper carrying the `Runway` label
    - `companion/pages/config_page.py` — every other form-emission site reachable from the settings pages: `calendar_group()`'s connect/replace and disconnect forms, the notifications test form, the quick-LED form, the per-flight rules add/delete forms inside `_frame_colours_card_html()`; plus `companion/layout.py`'s `frame_strip_html()` quick-switch forms. You change none of them — you need to know they exist so the audit's exclusions hold BY CONSTRUCTION and the failure message can name them
    - `companion/test_browser_ux.py` — `_login()`, `_no_js_page()`, `_click_control()` (read its own docstring about coordinate clicks not landing reliably on these controls), and whatever bar helpers 28-08 added; grep for them, do not assume their names
    - `companion/test_browser_ux.py` — `_the_floor_saves_to_disk_with_scripts_blocked_after_the_gate_simplifies` and the retargeted `_fallback_save_hides_immediately_once_script_runs`, so this plan's new checks complement rather than duplicate them
    - `companion/test_config_page.py` — the retargeted `_style_css_references_static_save_fallback_attr()` from 28-08 Task 2, so the orphan clause here asserts something it does not already cover
    - `companion/static/style.css` — grep `data-static-save-fallback` and `save-status` on the FINISHED tree and read every hit. 28-08 was required to leave comment prose standing while removing every rule selector, so the orphan check must distinguish the two
    - `git show 6dea46a:companion/pages/config_page.py` — the `dirty_bar_html` block around lines 3430-3468 and the return tuple at 3700-3770, for what the bar's own Save looked like before, so the audit's expectations match the restored shape rather than an invented one
  </read_first>
  <action>
    **The single-affordance audit.** Add one check to `companion/test_browser_ux.py` that,
    on `/display` and on `/device`, in both site languages, evaluates in the page: collect
    every `input[type="submit"]`, every `button[type="submit"]`, AND every `button` inside a
    form carrying no explicit `type` attribute — the HTML default IS submit, and a check
    that only looked at explicit `type="submit"` would miss the exact regression it exists
    to catch. Map each to its own resolved `.form && .form.id`. Require exactly ONE whose
    form id equals `config_page.SETTINGS_FORM_ID`, and require that one to carry
    `data-static-save-fallback` and to be a descendant of `[data-dirty-bar]`.

    Make the failure message NAME the offenders: report every collected control as a
    tagName / type / form-id / truncated-textContent tuple. A check that says "expected 1,
    got 2" costs the next reader a browser session; one that says which second control
    appeared and which form it belongs to does not.

    Assert the complement too — that is the half that makes this a relationship rather than
    an endpoint: the calendar, airlines, rules, notifications-test, quick-LED and
    quick-switch controls each resolve to a form id that is NOT the settings form. That is
    the developer's own confirmed scope boundary ("laisser ces deux-là tels quels")
    expressed as a contract instead of a comment.

    **The orphan-rule clause.** Add a check to `companion/test_config_page.py` asserting
    `companion/static/style.css` contains no RULE (as distinct from comment prose) targeting
    the retired hide mechanism: strip comment lines first, then require zero selectors
    containing `data-static-save-fallback` and zero `.save-status` rule blocks — while
    separately requiring that the superseded comment blocks 28-08 amended DO still carry
    their original sentences. Re-derive both literal counts on the finished tree before
    writing any number into the check; the planning-time figures in the interfaces block are
    a baseline for spotting a miscount, never an expectation to paste in.

    **The runway `form=` regression check.** Add one check to `companion/test_browser_ux.py`
    proving the whole cross-tree path in one sequence, on `/display`, in BOTH languages:
      1. Load the page. Assert the bar is hidden — script has run and nothing is dirty.
      2. Select a runway radio whose value differs from what is on disk, using the same real
         interaction the existing dial/slider checks use. Read `_click_control()`'s own
         docstring warning about coordinate clicks before choosing how.
      3. Assert the bar becomes visible AND that `[data-dirty-count]`'s ACTUAL text is the
         Runway section label plus the changed-suffix — `Runway changed` in English,
         `Piste modifié` in French. Asserting the bar is merely visible is not enough: the
         section-naming is what proves the `form=`-associated control was resolved into the
         right `[data-dirty-section]` wrapper, which is the actual relationship under test.
      4. Click the bar's Enregistrer and wait for the real navigation.
      5. Re-read `tracked_runway` OFF DISK via `device_config.load_device_config()` and
         assert it is the selected value. Then reload the page and assert the radio's
         checked state reflects it — the round trip, not just the write.

    **Mutation-test all three.** For the audit, temporarily add a second submit button
    inside the settings form and confirm the audit fails NAMING it. For the runway check,
    temporarily narrow `dirty-state.js`'s delegated listener from `document` to the form
    element — reproducing the exact B1 defect this path exists to guard — and confirm the
    bar never appears for the runway radio; that mutation is the one that proves the check
    would have caught the original defect class, and the SUMMARY must say so. For the orphan
    check, re-add a hide rule. Quote every failure message verbatim. Commit before mutating;
    revert every mutation with `git checkout-index -f --`, never `git checkout --`.

    Re-derive `EXPECTED_CHECK_COUNT` in both edited harnesses by RUNNING each suite and
    reading its printed total.
  </action>
  <verify>
    <automated>cd /home/user/skypane &amp;&amp; PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py &amp;&amp; "$PY" companion/test_browser_ux.py</automated>
  </verify>
  <acceptance_criteria>
    - Both suites exit 0
    - The audit resolves each control's own `.form` property in the live browser — a check that counts submit-button occurrences in the HTML string, or that maintains a hard-coded list of expected buttons, is a fail
    - The audit covers buttons with no explicit `type` attribute inside a form, and its own description says why
    - The audit's failure message enumerates every collected control with its resolved form id; proven by reading the mutation's actual output and quoting it verbatim in the SUMMARY
    - The audit asserts the one settings-form control carries `data-static-save-fallback` AND is a descendant of `[data-dirty-bar]`
    - The audit asserts the calendar / airlines / rules / notifications-test / quick-LED / quick-switch controls each resolve to a NON-settings-form form id
    - The orphan check strips comment lines BEFORE grepping, and its expectations are re-derived on the finished tree; the planning-time figures (2 and 5) appear nowhere as a hard-coded expectation
    - The runway check asserts `[data-dirty-count]`'s actual text equals the Runway label plus the changed-suffix in BOTH languages — `Runway changed` / `Piste modifié` — not merely that the bar is visible
    - The runway check reads `tracked_runway` back OFF DISK after a real Enregistrer navigation, then reloads the page and asserts the radio reflects it
    - Three mutations run (second submit button; document-to-form listener narrowing; re-added hide rule), all three fail, all three messages quoted verbatim in the SUMMARY; every revert via `git checkout-index -f --`
    - The listener-narrowing mutation's SUMMARY entry states explicitly that it reproduces the B1 defect class and that the check caught it
    - Both `EXPECTED_CHECK_COUNT` values re-derived by RUNNING; the numbers stated in the SUMMARY alongside the statement that they were read off a run
    - `ruff check .` clean
  </acceptance_criteria>
  <done>One save affordance, proven by the browser's own form association; and the runway radio's path from a cross-tree click to a value on disk, proven end to end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The gate — every count re-derived by running, the sandbox baseline named by name</name>
  <files>companion/test_browser_ux.py, companion/test_config_page.py</files>
  <behavior>
    - Every suite runs to completion and its check count is read off the run, never computed
    - The full-suite runner's exit status is recorded, and every failure it reports is named individually and attributed either to the sandbox baseline or to this phase
    - The sandbox baseline's size and membership are re-verified, not carried forward from an earlier plan's SUMMARY
  </behavior>
  <read_first>
    - `scripts/run-all-tests.sh` in full — what it runs, in what order, and how it reports
    - `scripts/run_all_tests.py` and `scripts/run-local-verify.sh` — whether either is the canonical entry point this project's prior gates used
    - `.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-04-SUMMARY.md` — its gate section, for the FORM a gate report takes in this project, and for the 5-check sandbox baseline it names (a datum to re-verify, never to copy)
    - `.planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve/` — the closing plan's own SUMMARY gate section, for the same reason
    - The `EXPECTED_CHECK_COUNT` assignment nearest the END of each of `companion/test_browser_ux.py`, `companion/test_config_page.py`, `companion/test_companion_app.py` — those files carry a long historical chain of assignments and only the last one is live
  </read_first>
  <action>
    Run every suite individually and record the printed "N/N checks pass" line verbatim for
    each: `companion/test_browser_ux.py`, `companion/test_companion_app.py`,
    `companion/test_config_page.py`, `companion/test_contrast_check.py`,
    `companion/test_i18n.py`, `companion/test_status_pages.py`,
    `companion/test_view_pages.py`. Where a suite's `EXPECTED_CHECK_COUNT` is stale after
    28-05, 28-08 and this plan's own Task 1, correct it to the number the run printed.
    **Never compute a count by adding up the checks you think you added.** That is the
    specific failure mode this project's gate discipline exists to stop, and this phase has
    already moved these numbers several times.

    Then run `scripts/run-all-tests.sh` (with the venv Python if present) and record its
    exit status.

    **Name every failure individually.** 28-04's SUMMARY recorded a 5-check root-sandbox
    baseline: four WR-11 failures (the `manual_save_failed` / `manual_delete_failed` flash
    keys, and the `add_entry()` / `delete_entry()` read-only-directory simulations) plus one
    `anomaly_active()` against a non-existent `state_dir`. **Re-derive that list by running,
    do not copy it.** Four plans have edited these suites since. If the count is still five
    and the names still match, say so AND say that you verified it by running. If it has
    moved, say what moved and why — and do NOT widen the baseline to absorb a failure this
    phase caused. That is the same rounding-up the CFG-65 precedent forbids.

    Record, in a form the SUMMARY can carry verbatim: each suite's count, the runner's exit
    status, the baseline failures by name, and an explicit statement that every number was
    read off a run.

    If any failure is attributable to 28-08 or to this plan's Task 1, FIX it here rather
    than recording it — a gate that reports a known-broken check is not a gate.
  </action>
  <verify>
    <automated>cd /home/user/skypane &amp;&amp; PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); for f in companion/test_config_page.py companion/test_companion_app.py companion/test_browser_ux.py companion/test_i18n.py companion/test_contrast_check.py companion/test_status_pages.py companion/test_view_pages.py; do "$PY" "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the seven suites exits 0 and its printed "N/N checks pass" line is quoted verbatim in the SUMMARY
    - Every `EXPECTED_CHECK_COUNT` this phase moved is set to a number READ OFF A RUN; the SUMMARY states this explicitly for each, and no number in it was reached by arithmetic
    - `scripts/run-all-tests.sh` was executed and its exit status is recorded in the SUMMARY
    - Every failure the full-suite runner reports is named individually and attributed to the sandbox baseline or to this phase; the baseline's count and membership are stated as RE-DERIVED, with the derivation method named
    - If the baseline is no longer exactly the five named in the interfaces block, the difference is explained and no phase-caused failure is absorbed into it
    - Zero failures are attributable to 28-08 or to Task 1 — any such failure is fixed here, not recorded
  </acceptance_criteria>
  <done>Numbers that came from running, and a baseline named out loud rather than inherited.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The record — coverage ledger, roadmap entry, and the design-system paragraph that is now false</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .claude/skills/sketch-findings-skypane/SKILL.md</files>
  <behavior>
    - REQUIREMENTS.md carries a Phase 28 coverage ledger in the same shape as Phase 27's, written against the finished tree
    - CFG-77 and CFG-78 are ticked clause by clause with per-clause evidence; CFG-74 stays unticked and its supersession is stated plainly
    - ROADMAP.md's Phase 28 entry reflects the restoration and the final plan list
    - SKILL.md's "Settings auto-save" standing paragraph is superseded in writing, not deleted
  </behavior>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 89, 100 and 103-104 in full — CFG-63's retirement note, CFG-74's superseded note, and CFG-77/CFG-78's exact binding wording. CFG-77 and CFG-78 are your per-clause targets; every clause needs its own evidence or its own honest shortfall
    - `.planning/REQUIREMENTS.md` lines 222-240 — the per-requirement traceability TABLE rows and their exact prose register
    - `.planning/REQUIREMENTS.md` lines 991-1110 — the Phase 27 coverage ledger's opening paragraph and its first two "### Correction N" sections, for the exact format to match
    - `.planning/ROADMAP.md` lines 1308-1359 — the whole Phase 28 entry including both addenda, the plan checklist and the replacement scope
    - `.claude/skills/sketch-findings-skypane/SKILL.md` line 123 — the "Settings auto-save (new, Phase 27, CFG-63/CFG-64)" standing paragraph, in full. Also line 11 (the "Current as of" header) and line 208 (Phase 27's Folded-In Work entry), both of which describe the auto-save as current
    - Every SUMMARY in this phase: 28-01 through 28-05, plus 28-08-SUMMARY.md, plus this plan's own Task 1 and Task 2 findings. The ledger is written against those AND against the code, never against what the plans intended
    - `28-06-PLAN.md.superseded` and `28-07-PLAN.md.superseded` — their header notes, so the ledger's account of what was superseded matches what is on disk
  </read_first>
  <action>
    **Write the Phase 28 coverage ledger** into `.planning/REQUIREMENTS.md`, immediately
    after the Phase 27 ledger, matching its shape: an opening paragraph stating what the
    ledger is and is not and that it was written against the finished tree, then one
    "### Correction N — title (CFG-xx)" section per item. Six sections: CFG-72 (titles,
    28-04), CFG-73 (the dial's two bugs, 28-02 and 28-03), CFG-75 (carousel preview,
    28-05), CFG-76 (gear icon, 28-01), CFG-74 (superseded, never built), and the
    restoration (CFG-77/CFG-78, 28-08 and 28-09).

    **The CFG-74 section is the one that matters most to get right.** It must say, without
    hedging: none of its four clauses was built; the plans that would have built them
    (28-06, 28-07) never executed and are on disk with superseding header notes rather than
    deleted; and the requirement was superseded — not failed — because the developer's own
    real-device evidence showed the mechanism it wrapped was working correctly and the
    developer rejected the model rather than the feedback. Do not tick it. Do not retire it
    as met. Do not fold its clauses into CFG-78's evidence. Its row stays unticked and its
    existing note stands; the ledger explains it, it does not revise it.

    **Tick CFG-77 and CFG-78 clause by clause.** Walk each requirement's own sentence and
    pair each clause with named evidence: the check function name that proves it, or the
    measured figure, or the file and mechanism. CFG-77's clauses: the bar appears on a
    field change; it names the changed sections in document order from the surviving
    `data-dirty-section` wrappers; Enregistrer is a genuine native submission; Annuler
    restores every field via `form.reset()` and re-triggers BOTH the dial's click-delegated
    repaint and `window.SkyPaneLivePreview.refresh()`; the leave-guard is not permanently
    disarmed and stays exactly where CFG-63's carve-out put it. CFG-78's clauses: the
    already-existing AST-provably-unconditional native submit BECAME the bar's Save (one
    element under two rendering conditions, not two implementations); the runway `form=`
    path has its own check; the gate re-derived counts by running, named the baseline, and
    stated CFG-74's status. If any clause is short of its full wording, record it as an
    outcome in its own words — the CFG-65 precedent — never round it up.

    **Add the two traceability table rows** for CFG-77 and CFG-78 in the register the
    existing CFG-62..CFG-67 rows use, and update CFG-63's and CFG-64's rows only insofar as
    they now point forward to the restoration. CFG-63's tick stands (the work it records
    really was done; it is the requirement that was withdrawn). CFG-64's tick stands and its
    substance is now MORE load-bearing, not less — its native submit is the visible Save.
    Say both in as many words.

    **Update `.planning/ROADMAP.md`'s Phase 28 entry.** Mark 28-05 through 28-09 in the plan
    checklist according to what actually shipped, replace the 28-06/28-07 checklist lines
    with 28-08/28-09 lines describing what they really did, update the Plans count and the
    wave description, and update the Requirements line to include CFG-77 and CFG-78 while
    recording CFG-74 as superseded. Leave both existing addenda intact — they are the
    narrative record of why this happened and must not be edited away.

    **Supersede the design-system skill's standing paragraph.** SKILL.md line 123's
    "Settings auto-save (new, Phase 27, CFG-63/CFG-64)" paragraph is now false in almost
    every sentence: there IS a save button, `change` no longer drives a fetch,
    `.save-status` no longer exists, and the `.js` hide rule is gone. Keep the paragraph
    verbatim, mark it SUPERSEDED by Phase 28 (CFG-77/CFG-78), and follow it with the new
    standing account: the restored dirty bar; the bar's Save IS the AST-unconditional native
    submit (one element, two rendering conditions); the visibility polarity (server-rendered
    visible, script hides at init, reveals on `countDifferences() > 0`) and why that is what
    keeps CFG-64's floor intact with only one button; `DIRTY_SECTION_ATTR` having a reader
    again; `.quick-toast` and `announceFailure()` remaining exclusively `quick-switch.js`'s;
    and `window.SkyPaneDirtyState.hasUncommittedEdits()` surviving for `freshness.js`. Also
    correct line 11's "Current as of" header and append this phase's entry to the Folded-In
    Work list in the same register the Phase 27 entry uses — including the counts Task 2
    re-derived by running. Do not delete Phase 27's Folded-In Work entry; it is history and
    it was true when written.

    **State plainly, in the SUMMARY and in the ledger both**, which of CFG-74's original
    four clauses were genuinely built. The answer is none, and REQUIREMENTS.md's CFG-78
    wording requires that sentence to exist.
  </action>
  <verify>
    <automated>cd /home/user/skypane &amp;&amp; grep -c 'Phase 28 coverage ledger' .planning/REQUIREMENTS.md &amp;&amp; grep -c 'CFG-77' .planning/ROADMAP.md &amp;&amp; grep -c 'SUPERSEDED' .claude/skills/sketch-findings-skypane/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` carries a "## Phase 28 coverage ledger" section in the same shape as Phase 27's, with six per-item sections and an opening paragraph stating it was written against the finished tree
    - CFG-77's row and CFG-78's row are both ticked, and EVERY clause of each requirement's own wording has named evidence beside it (a check function name, a measured figure, or a file and mechanism) — a clause with no evidence is recorded as a shortfall in its own words, never rounded up
    - CFG-74's row is still UNTICKED; `grep -c '^- \[x\] \*\*CFG-74\*\*' .planning/REQUIREMENTS.md` is 0. The ledger states in as many words that none of its four clauses was built, that 28-06 and 28-07 never executed, and that it was superseded rather than failed
    - The ledger names the developer's own real-device evidence (POST /settings answering 204, the value persisted, no visible confirmation) as CFG-74's reason for supersession — the record must explain the reversal, not merely assert it
    - CFG-63's tick and CFG-64's tick both still stand, each now pointing forward to the restoration; CFG-64's row says explicitly that its native submit became the bar's visible Save
    - Two new traceability TABLE rows exist for CFG-77 and CFG-78 in the same register the CFG-62..CFG-67 rows use
    - `.planning/ROADMAP.md`'s Phase 28 entry lists 28-08 and 28-09 in place of the superseded 28-06/28-07 lines, has an accurate Plans count and wave description, and names CFG-77/CFG-78 on its Requirements line with CFG-74 recorded as superseded. Both existing 2026-09-16 addenda survive verbatim — `git diff` shows no hunk inside either
    - SKILL.md's "Settings auto-save" paragraph still contains its original sentences (grep a distinctive phrase and confirm it survives), is marked SUPERSEDED by Phase 28, and is followed by the new standing account naming: the restored bar, the one-element-two-conditions Save, the visibility polarity and why it keeps CFG-64's floor intact, `DIRTY_SECTION_ATTR` having a reader again, `.quick-toast` remaining quick-switch.js's alone, and `hasUncommittedEdits()` surviving for freshness.js
    - SKILL.md's "Current as of" header names Phase 28; Phase 27's Folded-In Work entry is untouched and a Phase 28 entry is appended after it, carrying the counts Task 2 re-derived by running
    - The SUMMARY states plainly which of CFG-74's four clauses were genuinely built (none) versus what replaced them — the sentence CFG-78's own wording requires
    - No sentence anywhere in the ledger, the roadmap, the skill, the commit messages or the SUMMARY claims a Safari defect was found or fixed, or that the auto-save was broken. It worked; the model was rejected
  </acceptance_criteria>
  <done>A record a reader can trust: what shipped, what was superseded and why, and one requirement deliberately left unticked because nothing was built for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to POST /settings | the runway radio's cross-tree `form=` association carries untrusted input across it; unchanged in shape by this plan, only newly proven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09-01 | Spoofing | a second, unnoticed submit control appearing inside the settings form | mitigate | the single-affordance audit resolves every submit-shaped control's own `.form` property in a live browser and requires exactly one — mutation-proven against a deliberately added second button |
| T-28-09-02 | Tampering | the runway radio's `form=`-attribute path silently ceasing to reach the settings form | mitigate | the runway regression check proves the whole path to disk, mutation-proven against the exact B1 defect (narrowing the delegated listener from `document` to the form element) |
| T-28-09-03 | Repudiation | the phase record claiming more than the evidence supports | mitigate | every clause of CFG-77/CFG-78 is paired with a named check or a measured figure; CFG-74 stays unticked; counts are read off runs, never computed |
| T-28-09-SC | Tampering | npm/pip/cargo installs | mitigate | not applicable — this plan installs no package of any kind and adds no dependency |
</threat_model>

<verification>
1. All seven suites exit 0, each `EXPECTED_CHECK_COUNT` set to a number read off a run.
2. `scripts/run-all-tests.sh` executed; its exit status and every reported failure recorded
   and attributed by name.
3. The single-affordance audit and the runway regression check both pass in both languages
   on both settings scopes, and both are mutation-proven.
4. `git diff` over `.planning/ROADMAP.md` shows both 2026-09-16 addenda untouched.
5. `git diff` over `.claude/skills/sketch-findings-skypane/SKILL.md` shows the original
   "Settings auto-save" sentences retained, not rewritten in place.
6. `ruff check .` clean.
</verification>

<success_criteria>
- Exactly one save affordance is proven, by form association, on both settings scopes in
  both languages — with the failure message naming any offender.
- The runway radio's cross-tree path is proven from click to bar-naming to disk to reload.
- Every check count in the repository was re-derived by running; the sandbox baseline is
  named by name and re-verified rather than inherited.
- The Phase 28 coverage ledger, the ROADMAP entry and the design-system skill all agree
  with the code on the finished tree.
- CFG-74 is unticked, its supersession explained, and none of its four clauses claimed.
- Nothing claims a Safari defect was found or fixed.
</success_criteria>

<output>
Create `.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-09-SUMMARY.md` when done.
</output>
