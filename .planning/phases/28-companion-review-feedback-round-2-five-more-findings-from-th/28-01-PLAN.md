---
phase: 28-companion-review-feedback-round-2-five-more-findings-from-th
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/layout.py
  - companion/test_companion_app.py
autonomous: true
requirements: [CFG-76]

must_haves:
  truths:
    - "CFG-76: `#site-nav-toggle` renders a gear glyph, not a hamburger — the icon now matches what the panel actually holds (language switch, theme switch, sign-out), which is what the developer reported as confusing"
    - "CFG-76: `NAV_TOGGLE_LABEL` (\"Account and preferences\", layout.py:147) is UNCHANGED and `_mobile_nav_html()`'s panel body is UNCHANGED — only the glyph moves, because the label was already accurate"
    - "CFG-76: the new symbol joins the ONE shared sprite by the file's own append-never-reorder convention, and `ICON_IDS` stays in 1:1 correspondence with the sprite's symbol ids"
    - "CFG-76: `_icon_sprite_integrity()`'s hard-coded count moves 22 → 23 in BOTH its numeric literals and its English message text — a count bumped in one place and not the other is a check that passes for the wrong reason"
    - "Phase 28 discipline: every new check is mutation-tested with its failure message QUOTED, and the harness's own expected-check total is re-derived by RUNNING, never by arithmetic"
  artifacts:
    - path: "companion/layout.py"
      provides: "the icon-gear <symbol> in ICON_DEFS_HTML, its ICON_IDS whitelist entry, and the toggle's changed icon_html() argument"
      contains: "icon-gear"
    - path: "companion/test_companion_app.py"
      provides: "the sprite-integrity count bumped to 23 plus the new toggle-glyph check"
      contains: "icon-gear"
  key_links:
    - from: "companion/layout.py"
      to: "ICON_DEFS_HTML"
      via: "the toggle's icon_html() reference resolving to a symbol that exists in the sprite"
      pattern: "icon_html\\(\"icon-gear\""
---

<objective>
The developer's words: *"Sur mobile le hamburger est perturbant car cela ressemble à
la navigation. Remplacer par un engrenage ?"*

This is the smallest item in the phase and the developer proposed the fix themselves.
`#site-nav-toggle` wears the universal "site navigation" glyph while opening a panel
that contains **zero page-navigation links** — real navigation moved to the bottom tab
bar in 22-14. The label (`"Account and preferences"`) has been accurate the whole time;
only the picture lied.

**This plan changes one argument and adds one symbol.** It does not touch the label, the
panel body, or any other icon consumer. Its only real hazard is the one PATTERNS.md
flagged: `_icon_sprite_integrity()` hard-codes the member count, so adding a symbol
without bumping that literal FAILS the suite rather than silently passing — which is the
check working correctly, and is why that file is in this plan's `files_modified`.

**Why it is Wave 1 and parallel with 28-02:** its two files are disjoint from every
other plan in this phase's first wave. Nothing else writes `layout.py` or
`test_companion_app.py` in Wave 1.
</objective>

<decisions_covered>
CONTEXT.md "Mobile hamburger → gear icon (CFG-76)" in full, including the explicit
"keep `aria-label` and `_mobile_nav_html()` completely unchanged" boundary and the
Claude's-Discretion allowance on the gear artwork itself.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-CONTEXT.md
@.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan is the only writer of `companion/layout.py` and
`companion/test_companion_app.py` in Wave 1. Later waves (28-03 writes `layout.py`,
28-06 writes `test_companion_app.py`) depend on this plan having landed first.
</files_owned>

<interfaces>
RE-DERIVE by running before editing — the line numbers below were measured at planning
time and this file moves:

  grep -n 'ICON_IDS' companion/layout.py
  grep -n 'NAV_TOGGLE_ID\|NAV_TOGGLE_LABEL\|site-nav-toggle' companion/layout.py
  grep -n 'icon-hamburger' companion/layout.py companion/pages/*.py companion/static/*.js companion/static/style.css
  grep -n '_icon_sprite_integrity' companion/test_companion_app.py
  python3 -c "import sys; sys.path.insert(0,'companion'); import layout; print(len(layout.ICON_IDS))"

Measured at planning time (verify, do not trust):
  layout.py:147            `NAV_TOGGLE_LABEL = "Account and preferences"` — UNTOUCHED
  layout.py:~632-822       `ICON_IDS` whitelist + `ICON_DEFS_HTML` sprite
  layout.py:~678-704       the append-never-reorder comment convention for every prior
                           `ICON_IDS = ICON_IDS + (...)` extension
  layout.py:~807-814       `icon-power` / `icon-moon` — the two closest existing symbols
                           by stroke weight, the visual template for the gear
  layout.py:~2415-2420     the toggle render site; `icon_html("icon-hamburger", size=24)`
                           is the ONLY expression in `_mobile_nav_html()` that changes
  layout.py:~2348-2444     `_mobile_nav_html()`'s panel body — UNTOUCHED
  test_companion_app.py:~2283-2313  `_icon_sprite_integrity()`, with `!= 22` twice and
                           the literal English words "twenty-two" in its message

Binding constraints:
- Every interpolated string goes through `escape_html(...)`; the gear adds no new
  user-visible copy at all, so it adds no catalogue entry and `companion/test_i18n.py`
  must stay green unchanged.
- The sprite's symbols all use `viewBox="0 0 20 20" fill="none" stroke="currentColor"
  stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"` — match it exactly
  so the gear carries the same visual weight as its neighbours.
- `ICON_IDS` is APPENDED, never reordered. Every prior extension is its own trailing
  tuple-concat carrying a one-line comment naming the old and new member counts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The gear joins the sprite and the toggle stops pointing at the hamburger</name>
  <files>companion/layout.py</files>
  <behavior>
    - `ICON_IDS` gains exactly one member, `"icon-gear"`, appended as its own trailing tuple-concat
    - `ICON_DEFS_HTML` gains exactly one `<symbol id="icon-gear">`, matching its neighbours' viewBox/stroke attributes byte-for-byte apart from the path data
    - `#site-nav-toggle` renders `icon_html("icon-gear", size=24)`
    - `NAV_TOGGLE_LABEL` and every other line of `_mobile_nav_html()` are byte-identical
  </behavior>
  <read_first>
    - companion/layout.py: the whole `ICON_IDS` / `ICON_DEFS_HTML` region, including every prior `ICON_IDS = ICON_IDS + (...)` append and its comment, so the new one matches the convention rather than inventing a second
    - companion/layout.py: the `icon-power` and `icon-moon` `<symbol>` entries in full — the stroke-weight template for the gear
    - companion/layout.py: the `#site-nav-toggle` render expression and `NAV_TOGGLE_LABEL`'s definition, so the boundary of this edit is visible before it is made
    - companion/layout.py: `_mobile_nav_html()` in full — to confirm by reading that nothing else in it names an icon
    - .planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-PATTERNS.md: the CFG-76 section
  </read_first>
  <action>
    Append `"icon-gear"` to `ICON_IDS` as its OWN trailing tuple-concat statement —
    `ICON_IDS = ICON_IDS + ("icon-gear",)` — immediately after the last existing append,
    carrying a one-line comment in the same shape every prior append uses, naming this
    phase/plan (28-01, CFG-76) and stating the member count moving from 22 to 23. Do not
    reorder, re-sort, or fold existing appends into the new one.

    Add one `<symbol id="icon-gear" viewBox="0 0 20 20" fill="none"
    stroke="currentColor" stroke-width="1.6" stroke-linecap="round"
    stroke-linejoin="round">` entry to `ICON_DEFS_HTML`, placed adjacent to the
    `icon-power` / `icon-moon` entries. The artwork is Claude's Discretion per
    CONTEXT.md: draw a clean, recognisable gear — a centre circle plus a toothed outer
    ring, or a circle plus radial spokes — using `<circle>` / `<path>` elements only,
    with no `fill` other than the `none` already on the symbol and no `stroke-width`
    other than the symbol's own 1.6. Keep the path count comparable to its neighbours
    (2-3 shapes); a 12-tooth photorealistic gear at 20×20 renders as a grey blob.

    At the toggle's render site, change `icon_html("icon-hamburger", size=24)` to
    `icon_html("icon-gear", size=24)`. **That expression is the entire code change in
    `_mobile_nav_html()`.** Do not touch `NAV_TOGGLE_ID`, `NAV_TOGGLE_LABEL`,
    `MOBILE_NAV_ID`, `aria-expanded`, `aria-controls`, the panel body, or the
    `.site-nav-toggle` class.

    **Do NOT remove the `icon-hamburger` symbol or its `ICON_IDS` entry.** Grep its
    remaining consumers first and record the count in the SUMMARY. If the count is zero,
    still keep it — removing it would take the member count back to 22, put this plan in
    the business of pruning a shared registry it was not asked to prune, and lose the
    glyph for any future consumer. Record the zero-consumer finding as an observation for
    a later phase instead of acting on it.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" -c "import sys; sys.path.insert(0,'companion'); import layout; ids=layout.ICON_IDS; assert len(ids)==23, len(ids); assert len(set(ids))==23; assert 'icon-gear' in ids; assert 'icon-hamburger' in ids; assert 'icon_html(\"icon-gear\"' in open('companion/layout.py').read(); print('ok', len(ids))"</automated>
  </verify>
  <acceptance_criteria>
    - `len(layout.ICON_IDS) == 23` and `len(set(layout.ICON_IDS)) == 23` — no duplicate
    - `"icon-gear" in layout.ICON_IDS` and `"icon-hamburger" in layout.ICON_IDS` (the old glyph is kept, not pruned)
    - `grep -c 'icon_html("icon-hamburger"' companion/layout.py` is 0
    - `grep -c 'icon_html("icon-gear"' companion/layout.py` is 1
    - `grep -c 'id="icon-gear"' companion/layout.py` is 1, and that symbol's opening tag contains all of `viewBox="0 0 20 20"`, `fill="none"`, `stroke="currentColor"`, `stroke-width="1.6"`, `stroke-linecap="round"`, `stroke-linejoin="round"`
    - `grep -n 'NAV_TOGGLE_LABEL = ' companion/layout.py` still returns the line reading `"Account and preferences"` — unchanged
    - `git diff companion/layout.py` shows NO edit inside `_mobile_nav_html()`'s panel body — the only changed line in that function is the `icon_html(...)` argument
    - The new `ICON_IDS` append is its own statement with a comment naming "22" and "23"; no existing append line appears in the diff
    - The SUMMARY states the measured remaining `icon-hamburger` consumer count and that the symbol was deliberately kept
    - `ruff check .` clean
  </acceptance_criteria>
  <done>The mobile toggle shows a gear, the sprite still matches its whitelist, and nothing else in the panel moved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The count check moves to 23 and one new check proves the glyph swap end to end</name>
  <files>companion/test_companion_app.py</files>
  <behavior>
    - `_icon_sprite_integrity()` asserts 23, in both numeric literals AND in its English message text
    - ONE new check asserts the toggle renders `icon-gear`, does NOT render `icon-hamburger`, still carries the translated `NAV_TOGGLE_LABEL` in BOTH shipped languages, and the panel it opens still holds zero page-navigation links
    - Both are mutation-tested with their failure messages quoted
  </behavior>
  <read_first>
    - companion/test_companion_app.py: `_icon_sprite_integrity()` in full, including its message strings, so both the literals and the words move together
    - companion/test_companion_app.py: an existing check that renders the authenticated shell in both languages, for the idiom this new check reuses rather than inventing
    - companion/test_companion_app.py: the file's `EXPECTED_CHECK_COUNT` (or equivalent expected-total) declaration and how prior plans bumped it
    - companion/layout.py: the toggle render site and `_mobile_nav_html()` as edited by Task 1
  </read_first>
  <action>
    Update `_icon_sprite_integrity()`: change both `!= 22` comparisons to `!= 23`, change
    `"expected exactly twenty-two ICON_IDS, got %d"` to name twenty-three, and change the
    duplicate-count message's wording if it names a number. Leave the
    `sorted(symbol_ids) != sorted(layout.ICON_IDS)` correspondence assertion structurally
    unchanged — it is the part that actually proves the sprite and the whitelist agree,
    and it needs no number at all.

    Add ONE new check, `_the_nav_toggle_wears_the_gear_and_opens_the_same_panel()`
    (or a name in this file's own convention). It must assert, over a rendered
    authenticated shell, all of:
    (a) the rendered `#site-nav-toggle` element's inner markup references `icon-gear`;
    (b) it references `icon-hamburger` zero times;
    (c) its `aria-label` equals `i18n.t(layout.NAV_TOGGLE_LABEL)` in EN and in FR — read
        through the same translation call the renderer uses, never a hardcoded French
        literal, so a catalogue change can never silently desync this check;
    (d) the panel `_mobile_nav_html()` emits still contains the language switch, the
        theme switch and the sign-out form, and contains no `<a href>` pointing at an
        in-app page route — the reason the label is accurate and therefore the reason
        this phase changes only the glyph.

    Clause (d) is the RELATIONSHIP clause: without it, this check proves a glyph changed
    and nothing about whether the glyph now tells the truth. State that reasoning in the
    check's registered description.

    MUTATION-TEST BOTH and QUOTE EVERY FAILURE MESSAGE VERBATIM in the SUMMARY:
    - M-A: revert the toggle to `icon_html("icon-hamburger", size=24)`. The new check
      must fail on clause (a) or (b).
    - M-B: delete the `"icon-gear"` entry from `ICON_IDS` while leaving the `<symbol>` in
      the sprite. `_icon_sprite_integrity()` must fail on BOTH the count clause and the
      correspondence clause — quote both messages.
    - M-C: add a page-navigation `<a href="/flights">` to `_mobile_nav_html()`'s panel.
      The new check must fail on clause (d). If it does NOT fail, clause (d) is not
      actually asserting the relationship and must be rewritten until it does.

    Revert every mutation with `git checkout-index -f -- <path>`, never `git checkout --`.

    Re-derive this file's expected-check total by RUNNING the suite and reading the
    printed total. Never compute it by arithmetic.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_companion_app.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'twenty-two' companion/test_companion_app.py` is 0 within `_icon_sprite_integrity()`; the message names twenty-three
    - `grep -cE '!= 22' companion/test_companion_app.py` returns 0 for the two `_icon_sprite_integrity()` lines (verify by reading the function, not the whole file)
    - The new check asserts all four clauses (a)-(d); clause (c) reads the label through the translation call, and `grep -c 'Compte et préférences' companion/test_companion_app.py` is 0 — no hardcoded French literal
    - All three mutation failure messages are QUOTED VERBATIM in the SUMMARY; M-C's outcome is stated either way
    - Every mutation was reverted with `git checkout-index -f --`; `git status --porcelain` is clean of unintended changes at the end
    - The file's expected-check total was re-derived BY RUNNING, and the SUMMARY states both the old and new numbers alongside the command that produced them
    - `companion/test_companion_app.py` and `companion/test_i18n.py` both exit 0
    - `ruff check .` clean
  </acceptance_criteria>
  <done>The sprite's own integrity check counts the icon that was actually added, and one check proves the new glyph names what the panel really is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → browser | the icon sprite is server-rendered inline SVG emitted into every authenticated page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-A | Tampering | new inline SVG markup in `ICON_DEFS_HTML` | mitigate | the symbol is a module-level literal with zero interpolation — no user-controlled value reaches it, so there is no injection surface to escape; the acceptance criteria pin its attribute set exactly |
| T-28-01-B | Spoofing | an icon that misrepresents what a control does | mitigate | clause (d) of the new check asserts the panel still holds no page-navigation links, so the gear cannot start lying the way the hamburger did without a check failing |
| T-28-01-SC | Tampering | npm/pip/cargo installs | n/a | zero packages installed; this plan adds no dependency, no build step and no new file |
</threat_model>

<verification>
- `companion/test_companion_app.py` → exits 0, expected-check total re-derived by running
- `companion/test_i18n.py` → exits 0, no English leak and no new catalogue entry
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → exactly the known sandbox baseline failures, named
- `ruff check .` clean
- `git diff --stat` names exactly two files
</verification>

<success_criteria>
- `#site-nav-toggle` renders `icon-gear`; `aria-label` and the panel body are unchanged
- `ICON_IDS` is 23 members, appended not reordered, in 1:1 correspondence with the sprite
- `_icon_sprite_integrity()`'s literals AND its message text both say twenty-three
- One new check proves the swap AND that the label is still accurate (the panel has no page-nav links)
- Three mutations quoted verbatim; every revert used `git checkout-index -f --`
- Expected-check total re-derived by running, both numbers stated
- One commit per task, no model identifier in any artifact
</success_criteria>

<output>
Create `.planning/phases/28-companion-review-feedback-round-2-five-more-findings-from-th/28-01-SUMMARY.md` when done.
</output>
