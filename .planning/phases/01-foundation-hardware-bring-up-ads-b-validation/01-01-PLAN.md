---
phase: 01-foundation-hardware-bring-up-ads-b-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hardware/BOM.md
autonomous: false
requirements: [DEVICE-05]
user_setup:
  - service: seeed-studio
    why: "Purchase of the XIAO ePaper DIY Kit EE02 (XIAO ESP32-S3 Plus + 13.3in Spectra 6 panel) — the only hardware that can satisfy DEVICE-03/DEVICE-05 (D-05)"
    dashboard_config:
      - task: "Place the order and capture the order number + estimated delivery date"
        location: "seeedstudio.com (or an EU distributor listed in hardware/BOM.md)"
      - task: "Place the battery-pack order from a supplier whose listing states the connector pitch and polarity"
        location: "Any EU LiPo supplier (see hardware/BOM.md)"

must_haves:
  truths:
    - "A committed bill of materials names the exact hardware to buy, its unit prices, and whether the total clears the EUR 300 display+compute ceiling"
    - "The battery pack line records the verified connector pitch and polarity for the XIAO ESP32-S3 Plus, so a reversed-polarity pack cannot be ordered by accident"
    - "The conditional RTL-SDR fallback hardware is tracked on a separate budget line and is explicitly not ordered during this plan (D-04)"
    - "Orders are placed and their order numbers plus estimated delivery dates are recorded, so the hardware-dependent waves have a known unblock date"
  artifacts:
    - path: "hardware/BOM.md"
      provides: "Phase 1 bill of materials, budget ceiling check, connector-polarity verification, order tracking"
      contains: "## Budget Ceiling Check"
  key_links:
    - from: "hardware/BOM.md"
      to: ".planning/PROJECT.md"
      via: "EUR 300 display+compute hardware ceiling is restated and checked against the actual quoted total"
      pattern: "300"
---

<objective>
Turn the Phase 1 hardware requirement into a concrete, priced, ordered bill of materials, and get the orders placed on day one so that shipping lead time overlaps with the software-only work in plans 01-02, 01-03, and 01-04 (D-08).

Nothing has been purchased yet. Every hardware-dependent plan in this phase (01-05, 01-06, 01-07) is blocked until the XIAO ESP32-S3 Plus + EE02 kit and a LiPo battery pack physically arrive. This plan exists so that blocking clock starts immediately rather than after the software is done.

Purpose: DEVICE-05 ("device runs on battery power only") cannot be validated without a battery pack, and DEVICE-03's wake/poll/display/sleep loop cannot be validated without the panel. This plan procures both, inside the stated budget, with the one irreversible mistake (reversed battery polarity) explicitly guarded.
Output: `hardware/BOM.md` — priced, budget-checked, order-tracked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-hardware-bring-up-ads-b-validation/01-CONTEXT.md
@.planning/phases/01-foundation-hardware-bring-up-ads-b-validation/01-RESEARCH.md
@.planning/phases/01-foundation-hardware-bring-up-ads-b-validation/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the priced, budget-checked bill of materials</name>
  <files>hardware/BOM.md</files>
  <read_first>
    - `.claude/CLAUDE.md` — the "Device Hardware" table holds the already-researched SKUs and indicative prices (XIAO ePaper DIY Kit EE02 bundle ~$163.90 / ~EUR 152; XIAO ESP32-S3 Plus board $14.90 + 13.3in panel $149 sold separately; battery ~EUR 10-15; reTerminal E1004 all-in-one $279.90 as the rejected alternative)
    - `.planning/PROJECT.md` — the EUR 300 constraint and its exact scope ("display + compute", not accessories)
    - `.planning/phases/01-foundation-hardware-bring-up-ads-b-validation/01-CONTEXT.md` — D-04 (RTL-SDR tracked on a separate budget line), D-05 (board choice is locked to XIAO ESP32-S3 Plus + EE02), D-08 (nothing purchased yet)
    - `.planning/phases/01-foundation-hardware-bring-up-ads-b-validation/01-RESEARCH.md` — "Environment Availability" table (kit and battery both marked not purchased) and Assumptions Log A4 (shipping lead time reported anywhere from 5-10 business days to 4-6 weeks depending on distributor — confirm at order time)
  </read_first>
  <action>
    Create `hardware/BOM.md` with these exact sections, in this order.

    `# Ink Frame — Phase 1 Bill of Materials` followed by a one-line statement that this BOM covers only what Phase 1 needs on the bench: no enclosure, no solar, no RTL-SDR.

    `## Required Now` — a markdown table with columns `Item | SKU / Model | Vendor | URL | Unit price (excl. VAT) | Qty | Subtotal`. Rows:
    1. `XIAO ePaper DIY Kit EE02` — the bundle containing the XIAO ESP32-S3 Plus driver board and the 13.3in E Ink Spectra 6 1200x1600 six-colour panel. Locked by D-05; do not substitute the reTerminal E1004 or an Arduino Nano ESP32. Price the bundle and, on a second row, price the separate board + panel path, then mark which one is being ordered.
    2. `LiPo battery pack` — 3.7V, 5000-6000 mAh, with an integrated protection circuit, terminated in the connector the XIAO ESP32-S3 Plus actually accepts.
    3. `USB-C data cable` — must carry data, not power only; the EE02's XIAO exposes native USB Serial/JTAG and that is the flashing and console channel on this board.

    Populate the URL and price columns by looking up the live Seeed Studio product pages (and at least one EU distributor) at execution time. Do not copy the CLAUDE.md figures forward unchecked — they are dated indicative prices; record the price you actually see, with the date you saw it.

    `## Battery Connector Verification` — a short subsection recording three facts, each with the source URL you confirmed them against: (a) the battery connector pitch the XIAO ESP32-S3 Plus board carries (Seeed's XIAO battery connector and a generic "JST-PH 2.0" pack are not necessarily the same pitch — 1.25 mm and 2.0 mm are both commonly sold as "JST"), (b) the pin order / polarity on the board's connector, and (c) the polarity of the pack being ordered. State plainly that a reversed-polarity pack destroys the board on first connection and is not recoverable, so the ordered pack must be confirmed against the board's documented polarity before purchase. If the supplier listing does not state polarity, choose a different supplier or record that a polarity check with a multimeter is required before first connection.

    `## Budget Ceiling Check` — restate PROJECT.md's EUR 300 ceiling and its scope (display + compute), then a table with rows for `Kit subtotal`, `Estimated EU VAT`, `Estimated shipping and customs`, `Total`, and a final `Headroom vs EUR 300 ceiling` row. Use the VAT rate for France (20%). If the total exceeds the ceiling, do not silently drop items — record the overrun explicitly and flag it for the checkpoint in Task 2. Note in this section that the battery pack and USB cable are accessories rather than "display + compute", and state whether you are counting them inside or outside the ceiling (count them inside, and say so, so the number is conservative).

    `## Separate Budget Line — Conditional, Not Ordered Now (D-04)` — a table for the RTL-SDR fallback hardware (RTL-SDR v4 dongle, 1090 MHz antenna, and if a permanent receiver is later wanted, a Raspberry Pi plus microSD). Give indicative prices. State that per D-02 this hardware is ordered only if plan 01-04's aggregator-API validation comes back insufficient, that per D-04 its cost is tracked separately and does not count against the EUR 300 display+compute ceiling, and that nothing in this section is being purchased during this plan.

    `## Deliberately Not In Phase 1` — a short list with one-line reasons: enclosure (no confirmed public EE02 design exists; the panel sits on the bench for validation), solar (out of scope for v1), wall power (battery-only by constraint), USB inline power meter (rejected by D-07 in favour of the time-to-depletion method).

    `## Order Tracking` — a table with columns `Item | Vendor | Order number | Ordered on | Estimated delivery | Arrived on`, pre-filled with the item rows and the value `PENDING` in the tracking columns. Task 2 fills these in.
  </action>
  <verify>
    <automated>test -f hardware/BOM.md && for h in "## Required Now" "## Battery Connector Verification" "## Budget Ceiling Check" "## Separate Budget Line" "## Deliberately Not In Phase 1" "## Order Tracking"; do grep -qF "$h" hardware/BOM.md || { echo "MISSING SECTION: $h"; exit 1; }; done; grep -qF "EE02" hardware/BOM.md && grep -qE '300' hardware/BOM.md && grep -qiE 'polarit' hardware/BOM.md && grep -qF "D-04" hardware/BOM.md && echo "BOM structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `hardware/BOM.md` exists and contains all six required headings verified by the command above.
    - The `## Required Now` table has a non-empty URL and a non-empty price for every row, and each price carries the date it was looked up.
    - `## Battery Connector Verification` names a specific connector pitch in millimetres and a specific polarity, each with a source URL.
    - `## Budget Ceiling Check` contains a computed `Total` and a `Headroom vs EUR 300 ceiling` figure; if headroom is negative the file says so explicitly rather than omitting the row.
    - `## Separate Budget Line` cites `D-04` and states the RTL-SDR items are not being ordered in this plan.
    - `## Order Tracking` exists with one row per item from `## Required Now` and `PENDING` placeholders.
    - The document never proposes the reTerminal E1004 or any board other than the XIAO ESP32-S3 Plus + EE02 as the item to order (D-05 is locked; E1004 may appear only inside `## Deliberately Not In Phase 1` or as a priced-but-rejected comparison).
  </acceptance_criteria>
  <done>The developer can read one file, see exactly what to buy, what it costs, whether it fits the budget, and which battery connector will not destroy the board.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Place the hardware orders and record delivery estimates</name>
  <files>hardware/BOM.md</files>
  <read_first>
    - `hardware/BOM.md` — the BOM written in Task 1, including the connector-polarity verification and the budget ceiling check
  </read_first>
  <what-built>A priced, budget-checked bill of materials at `hardware/BOM.md` naming the exact kit, battery pack, and cable to order, with the battery connector pitch and polarity already verified against the board's documentation.</what-built>
  <how-to-verify>
    1. Open `hardware/BOM.md` and read `## Budget Ceiling Check`. Confirm the total (including VAT and shipping) is acceptable. If headroom is negative, decide whether to proceed anyway or change the order.
    2. Read `## Battery Connector Verification`. Confirm the battery pack you are about to order matches the pitch and polarity recorded there. This is the one irreversible step in the phase — a reversed-polarity pack destroys the board.
    3. Place the order for the XIAO ePaper DIY Kit EE02 at the vendor URL in the BOM.
    4. Place the order for the LiPo battery pack.
    5. Order a data-capable USB-C cable if you do not already have one.
    6. For each order, note the order number and the vendor's estimated delivery date.
  </how-to-verify>
  <action>
    After the developer confirms the orders are placed, fill in the `## Order Tracking` table in `hardware/BOM.md`: replace the `PENDING` placeholders with the order number, the ordering date, and the vendor's estimated delivery date for each item. Leave the `Arrived on` column as `PENDING` — plan 01-05's arrival checkpoint fills that in.

    Then append a `## Unblock Date` line stating the latest of the estimated delivery dates and naming the plans it gates: 01-05, 01-06, and 01-07 cannot start before that date. Record any vendor-reported lead time that differs materially from what the BOM assumed, since RESEARCH.md flagged reported lead times ranging from 5-10 business days to 4-6 weeks depending on distributor.
  </action>
  <resume-signal>Type "ordered" with the order numbers and estimated delivery dates, or describe what blocked the order.</resume-signal>
  <acceptance_criteria>
    - `hardware/BOM.md` `## Order Tracking` table contains a real order number and a real estimated delivery date for the EE02 kit row and the battery row (no remaining `PENDING` in the `Order number` or `Estimated delivery` columns for those two rows).
    - `hardware/BOM.md` contains a `## Unblock Date` section naming a date and listing plans 01-05, 01-06, 01-07 as gated by it.
    - Verified by: `grep -A20 '## Order Tracking' hardware/BOM.md` shows populated order numbers, and `grep -q '## Unblock Date' hardware/BOM.md` succeeds.
  </acceptance_criteria>
  <done>Hardware is on its way, the phase has a known unblock date, and the software-only plans (01-02, 01-03, 01-04) can proceed in parallel with shipping.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|---|---|---|
| `hardware/BOM.md` | new file | Phase 1 bill of materials |
| `hardware/BOM.md` § `## Required Now` | section | Priced item table with vendor URLs |
| `hardware/BOM.md` § `## Battery Connector Verification` | section | Connector pitch + polarity, sourced |
| `hardware/BOM.md` § `## Budget Ceiling Check` | section | Total vs EUR 300 ceiling |
| `hardware/BOM.md` § `## Separate Budget Line` | section | Conditional RTL-SDR items (D-04), not ordered |
| `hardware/BOM.md` § `## Order Tracking` | section | Order numbers, dates, arrival column |
| `hardware/BOM.md` § `## Unblock Date` | section | Gates plans 01-05, 01-06, 01-07 |
| `hardware/` | new directory | Hardware-facing docs and measurement logs for this phase |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor website -> developer's payment details | The developer transacts with an external commerce site; Claude never handles payment data |
| supplier -> physical hardware -> developer's board | An incorrectly specified battery pack is a physically destructive input to the device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | Physical device (XIAO ESP32-S3 Plus battery input) | high | mitigate | `## Battery Connector Verification` in `hardware/BOM.md` records the board's documented connector pitch and polarity against a source URL, and the Task 2 checkpoint requires the developer to confirm the ordered pack matches before purchase. A reversed-polarity pack is an unrecoverable hardware kill. |
| T-01-01-02 | Information Disclosure | Order records in a git-tracked file | low | accept | Order numbers and estimated delivery dates are recorded in `hardware/BOM.md`, which is committed. These are low-value identifiers tied to a personal hobby order in a repository the developer controls. No payment details, addresses, or account credentials are ever written to the repo — the checkpoint asks only for order number and date. |
| T-01-01-03 | Spoofing | Vendor URLs in the BOM | medium | mitigate | Item URLs must point at the official Seeed Studio product page or a named EU distributor, captured at execution time by direct lookup rather than recalled. The checkpoint puts a human between the recorded URL and the actual purchase, so a wrong or typosquatted link is caught by the person reading the page before paying. |
| T-01-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages of any kind — it writes one markdown file and pauses for a human purchase. RESEARCH.md's Package Legitimacy Audit records that Phase 1 requires zero external package installations. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist to gate. |
</threat_model>

<verification>
- `test -f hardware/BOM.md` succeeds.
- All six required section headings present (see Task 1 verify command).
- `## Order Tracking` has real order numbers for the EE02 kit and battery rows.
- `## Unblock Date` names a date and lists the three gated plans.
- The BOM's `Total` line and `Headroom vs EUR 300 ceiling` line are both present and numeric.
</verification>

<success_criteria>
- The XIAO ePaper DIY Kit EE02 and a polarity-verified LiPo battery pack are ordered, with order numbers and delivery estimates recorded in git.
- The phase has a documented unblock date for its hardware-dependent waves.
- The EUR 300 display+compute ceiling has been checked against real, dated quotes rather than assumed.
- No RTL-SDR hardware has been purchased (D-02 says that decision belongs to plan 01-04).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-hardware-bring-up-ads-b-validation/01-01-SUMMARY.md` when done.
</output>
