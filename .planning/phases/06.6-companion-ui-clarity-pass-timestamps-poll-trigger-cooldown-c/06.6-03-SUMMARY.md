---
phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c
plan: 03
subsystem: ui
tags: [python, http.server, stdlib-templating, timestamp-formatting]

# Dependency graph
requires:
  - phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c (plan 01)
    provides: companion/layout.py's shared absolute_and_relative()/parse_iso()/age_seconds()/relative_age_text() helpers
provides:
  - History's Timestamp column reads "ISO (Nm ago)" through the shared helper
  - Preview's Captured caption reads "Captured ISO (Nm ago)" through the same shared helper
  - A permanent cross-module regression check pinning History's corroboration copy to Health's
affects: []

tech-stack:
  added: []
  patterns:
    - "Page modules degrade timestamp formatting gracefully: format_event_row(row, now=None) and preview_section()'s caption both fall through to absolute-only text when now is unavailable, matching absolute_and_relative()'s own missing-now_ts contract."

key-files:
  created: []
  modified:
    - companion/pages/history_page.py
    - companion/pages/preview_page.py
    - companion/test_view_pages.py

key-decisions:
  - "D-03 closed as verified, not rewritten: health_page._CORROBORATION_ROWS and history_page._CORROBORATION_LABELS already agreed key-by-key (True/None/False, single-source 'None' already 'ok' in both) before this plan ran — no copy change made, only a permanent check added."
  - "Plan's literal <verify> grep counts for layout.status_dot(/layout.data_table(/companion.app/history_db in history_page.py and preview_page.py over-count because both modules' docstrings mention those symbols by name in prose; the real code-level invariant (checked by excluding backtick-quoted docstring lines, and by the pre-existing narrower _preview_page_never_imports_app/history_db==0 checks) holds unchanged both before and after this plan."

requirements-completed: [D-02, D-03]

coverage:
  - id: D1
    description: "History's Timestamp column shows the stored ISO timestamp followed by its relative age (D-02)"
    requirement: "D-02"
    verification:
      - kind: unit
        ref: "companion/test_view_pages.py#History's Timestamp column reads 'ISO (Nm ago)'..."
        status: pass
    human_judgment: false
  - id: D2
    description: "Preview's Captured caption shows the panel file's ISO mtime followed by its relative age, keeping the existing wording (D-02)"
    requirement: "D-02"
    verification:
      - kind: unit
        ref: "companion/test_view_pages.py#Preview's Captured caption reads 'Captured ISO (Nm ago)'..."
        status: pass
    human_judgment: false
  - id: D3
    description: "History and Health's ADS-B corroboration copy verified consistent, single-source unknown state confirmed 'ok' not a failure, and a permanent cross-page drift guard added (D-03)"
    requirement: "D-03"
    verification:
      - kind: unit
        ref: "companion/test_view_pages.py#history_page._CORROBORATION_LABELS agrees with health_page._CORROBORATION_ROWS..."
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-08-30
status: complete
---

# Phase 06.6 Plan 03: History Timestamp + Preview Caption + Corroboration Copy Guard Summary

**History's Timestamp column and Preview's Captured caption now both read `"ISO (Nm ago)"` through `companion/layout.py`'s shared `absolute_and_relative()` helper, and D-03's corroboration-copy consistency is confirmed and permanently pinned by a new cross-module regression check — no copy change was needed.**

## Performance

- **Duration:** ~20 min (including a required worktree sync — see Issues Encountered)
- **Completed:** 2026-08-30T07:14Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- `history_page.format_event_row(row, now=None)` routes the `"ts"` cell through `layout.absolute_and_relative()`, degrading to the raw timestamp with one argument and to an empty string for a missing timestamp; `render(ctx)` now reads `ctx["now"]` with a `history_db.utc_now_iso()` fallback, mirroring `health_page.render()`'s own idiom.
- `preview_page.preview_section()` interpolates `absolute_and_relative(mtime_iso, ctx.get("now"))` into the unchanged `"Captured %s"` format; the no-panel-yet branch, the module's two-import contract (`server.panel_preview` + `companion.layout` only), and the mandatory colour caveat are all untouched.
- D-03 verified: `health_page._CORROBORATION_ROWS` and `history_page._CORROBORATION_LABELS` already agreed key-by-key across all three stored states (`"True"`, `"None"`, `"False"`), and the single-source `"None"` state was already `"ok"` in both — no copy change was made. `companion/test_view_pages.py` now imports `health_page` and carries a permanent check pinning that agreement, including the specific "unknown is never a failure" rule.
- `companion/test_view_pages.py` grew 25 → 28 checks (one per task), each bump computed from the file's real on-disk value at execution time.

## Task Commits

Each task was committed atomically:

1. **Task 1: History's Timestamp column renders "ISO (Nm ago)" (D-02)** - `60fe5ec` (feat)
2. **Task 2: Preview's Captured caption renders "Captured ISO (Nm ago)" (D-02)** - `e1f5f76` (feat)
3. **Task 3: Verify the ADS-B corroboration copy and make the cross-page consistency permanent (D-03)** - `36d0ef3` (test)

_No REFACTOR commits needed - none of the three tasks required cleanup beyond their own GREEN step._

## Files Created/Modified

- `companion/pages/history_page.py` - `format_event_row(row, now=None)` now returns `layout.absolute_and_relative(row.get("ts"), now, fallback="")`; `render(ctx)` reads `ctx.get("now")` with an `history_db.utc_now_iso()` fallback and threads it into every `format_event_row()` call. `_history_table_html()`, `_CORROBORATION_LABELS`, `_HEADERS`, `_safe_query()`, `history_rows()` all untouched.
- `companion/pages/preview_page.py` - `absolute_and_relative` added to the `companion.layout` import; the panel-present branch's caption now interpolates `absolute_and_relative(mtime_iso, ctx.get("now"))` instead of the raw `mtime_iso`, with a comment recording the verified Z-suffix/`+00:00` compatibility finding. No-panel branch, `image_html`, `escape_html(caption_text)`, `caveat_html`, `gallery_tiles()`, `render()` all untouched.
- `companion/test_view_pages.py` - `health_page` and `companion.layout` (as `layout`) added to the import block; `_history_ctx(state_dir, now=None)` and `_preview_ctx(state_dir, gallery_entries=None, now=None)` both gained the documented `now` key (existing single/two-argument call sites keep working unchanged); three new `check(...)` calls (History timestamp format + graceful degradation, Preview caption format + no-panel branch + mixed-suffix subtraction, corroboration cross-page agreement); `EXPECTED_CHECK_COUNT` 25 → 28.

## Decisions Made

- **D-03 verification finding (full):** Compared `health_page._CORROBORATION_ROWS` (the canonical `(stored, label, status, explanation)` table) against `history_page._CORROBORATION_LABELS` (the `{stored: (status, label)}` mirror table) as they actually stand on this branch's HEAD (post-Wave-1 merge, see Issues Encountered). Both tables cover exactly the three stored strings `"True"`, `"None"`, `"False"`; the `(status, label)` pairs are identical key-by-key (`"True"` → `("ok", "Agreement")`, `"None"` → `("ok", "Single-source (uncorroborated)")`, `"False"` → `("warn", "Disagreement")`); and the `"None"` single-source state maps to `"ok"` in both, never a warning or an error. All three checks held — **no copy change was made**, matching both `06.6-CONTEXT.md`'s and `06.6-RESEARCH.md`'s independent findings during planning. `history_page._DEFAULT_CORROBORATION` (`("warn", "Unknown")`, the unrecognised-value fallback) is confirmed unchanged. `companion/pages/health_page.py` was read only, never modified.
- **Sibling phase 06.3 state when read fresh:** Phase 06.3 had already fully executed against `companion/pages/history_page.py` by the time this plan ran (confirmed via `STATE.md` and `git blame`: `9199101 fix(06.3-03): wrap History's hand-built table in .data-table-wrap (D-03)`, further merged-cell work by `06.6.1-02`). `_history_table_html()`'s return block already wraps the table in `<div class="data-table-wrap">`, matching `layout.data_table()`'s own wrap literal; the Corroboration cell's unescaped `layout.status_dot()` embedding was untouched by this plan, per cross-phase constraints 2 and 3. This plan's edits to `history_page.py` were confined entirely to `format_event_row()` and `render()`, upstream of `_history_table_html()` as the plan specified.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree branch was forked before Wave 1's plan-01/plan-02 merges landed**
- **Found during:** Pre-task setup, before Task 1
- **Issue:** This worktree's branch (`worktree-agent-ac28221004b1a4537`) was created off commit `fa6dacc`, which predates `8dbb697` ("docs(state): begin Phase 06.6 execution, sync post-merge base") and both Wave 1 merge commits (`984d306` "Merge 06.6-01", `cd1d3aa` "Merge 06.6-02"). As a result, `companion/layout.py` on this worktree's HEAD had none of `absolute_and_relative()`/`parse_iso()`/`age_seconds()`/`relative_age_text()` — the exact dependency this plan (`depends_on: ["06.6-01"]`) requires — and `companion/pages/config_page.py` lacked 06.6-02's D-01 poll-cooldown work.
- **Fix:** Ran `git merge --no-edit cd1d3aa` (the local `claude/backlog-6x-phases-d6bb33` branch tip, which already unifies both Wave 1 plans). Fast-forwarded cleanly (`fa6dacc..cd1d3aa`, 9 files changed, no conflicts), bringing in both `06.6-01-SUMMARY.md`/`06.6-02-SUMMARY.md`, `companion/layout.py`'s new helpers, and `config_page.py`'s D-01 work.
- **Files modified:** None beyond the merge itself (fast-forward, no manual resolution).
- **Verification:** `grep -n 'def absolute_and_relative' companion/layout.py` confirmed present post-merge; full plan then proceeded and all verification gates passed.
- **Committed in:** Not a separate commit — the merge itself is `cd1d3aa` (pre-existing on the integration branch), fast-forwarded into this worktree's branch tip before Task 1's commit `60fe5ec`.

**2. [Rule 3 - Blocking] Worktree has no `server/.venv`**
- **Found during:** Task 1, first verification attempt
- **Issue:** `server/.venv/bin/python3` does not exist in this git worktree (worktrees don't share untracked directories with the main checkout); every `<verify>` command in the plan assumes it.
- **Fix:** Used the main checkout's provisioned interpreter directly (`/Users/florian/Projects/skypane/server/.venv/bin/python3` / `ruff`) for every verification command in this session, and `PYTHON=<that path> scripts/run-all-tests.sh` for the full-suite gate. No project files were changed for this.
- **Files modified:** None.
- **Verification:** All `<verify>` commands ran successfully with the substituted interpreter path.
- **Committed in:** N/A (environment workaround only, not a code change).

**3. [Rule 3 - Blocking, documented finding] Plan's literal shell `grep -c` verify assertions over-count due to pre-existing docstring prose**
- **Found during:** Task 1's `<verify>` (expected `layout.status_dot(` count `1`/`layout.data_table(` count `0` in `history_page.py`) and Task 2's `<verify>` (expected `companion.app` count `0`/`history_db` count `0` in `preview_page.py`)
- **Issue:** Both modules' own docstrings mention these symbols by name in prose (e.g. `history_page.py`'s module docstring says `` `layout.data_table()` cannot host the Corroboration column's `layout.status_dot()` markup ``; `preview_page.py`'s docstring explains it "must never import `companion.app`"). A whole-file `grep -c 'layout.status_dot('` therefore counts both the one real code call site (line 200) and the docstring's own mention (2 total), and `grep -c 'companion.app'` in `preview_page.py` counts 5 prose mentions even though the module genuinely imports neither `companion.app` nor `history_db`. Confirmed via `git diff`/`git blame` that this pre-dates this plan entirely (the docstring lines are unchanged by this plan's commits, originally authored in `06-09`/`d74f6445`).
- **Fix:** No code change — verified the actual behavioural intent directly: `grep -n 'layout\.status_dot(\|layout\.data_table(' companion/pages/history_page.py | grep -v '`'` shows exactly one real call site (`layout.status_dot(` at line 200) and zero `layout.data_table(` calls; `grep -n 'import companion.app\|from companion import app'` and `grep -c history_db` on `preview_page.py` both confirm zero real imports. The pre-existing, narrower automated checks (`_history_page_never_imports_html_module`-style discipline, `_preview_page_never_imports_app`) already assert the real invariant and pass. This is the same category of plan-vs-reality drift documented repeatedly elsewhere in this project's `STATE.md` (e.g. 06.3-03's stale `action="/config"` literal count).
- **Files modified:** None — this is a verification-methodology note, not a functional change.
- **Verification:** Both narrower/real-intent checks pass; both `_preview_page_never_imports_app` and `_history_page_never_imports_html_module` (pre-existing, unmodified) pass in the full 28/28 `test_view_pages.py` run.
- **Committed in:** N/A (verification note only).

---

**Total deviations:** 3 auto-fixed (2 blocking-environment, 1 blocking-verification-methodology). None required a code change beyond the plan's own scope.
**Impact on plan:** Zero scope creep. All three tasks landed exactly as the plan specified; deviations were entirely about getting the worktree and its verification commands into a state where the plan's own acceptance criteria could be meaningfully evaluated.

## Issues Encountered

- **Worktree base drift (see Deviation 1):** resolved by fast-forward merge before any task began.
- **Missing worktree venv (see Deviation 2):** resolved by pointing at the main checkout's interpreter for all verification in this session; no code or config change was needed since `server/.venv` is gitignored infrastructure, not a plan deliverable.
- No other issues. `server/test_poll_loop.py` reports its one already-documented, pre-existing failure (macOS-local Pillow/FreeType `panel.bin` digest vs. the pinned Linux-CI-computed value) — confirmed unrelated via `git diff --stat cd1d3aa HEAD` showing zero `server/` files touched by this plan's three commits, consistent with every prior 06.x session's identical finding recorded throughout `STATE.md`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 06.6 (Companion UI clarity pass — timestamps, poll-trigger cooldown, corroboration copy) is now **complete, 3/3 plans**: 06.6-01 (shared timestamp helpers + Battery Trend), 06.6-02 (live poll-trigger cooldown countdown, D-01), 06.6-03 (this plan — History + Preview timestamps, D-02; corroboration copy verification, D-03).
- All five of the companion app's timestamp-bearing locations (Device check-in, ADS-B pipeline, Battery Trend, History, Preview) now read `"ISO (Nm ago)"` from the single shared `companion/layout.py::absolute_and_relative()` implementation.
- `companion/test_view_pages.py`: 28/28. `companion/test_status_pages.py`: 47/47 (unaffected, `health_page.py` not modified). `scripts/run-all-tests.sh`: 14/15 harnesses pass under coverage 91% (threshold met); the one failure is the pre-existing, already-documented, unrelated `server/test_poll_loop.py` digest mismatch.
- No blockers for closing out the phase.

---
*Phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c*
*Completed: 2026-08-30*

## Self-Check: PASSED

- FOUND: companion/pages/history_page.py
- FOUND: companion/pages/preview_page.py
- FOUND: companion/test_view_pages.py
- FOUND: .planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-03-SUMMARY.md
- FOUND commit: 60fe5ec
- FOUND commit: e1f5f76
- FOUND commit: 36d0ef3
