---
phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/layout.py
  - companion/pages/health_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [D-02]

must_haves:
  truths:
    - "Health's Battery Trend table shows each reading's absolute ISO timestamp followed by its relative age, matching the Device check-in and ADS-B pipeline rows on the same page (D-02)."
    - "The Device check-in and ADS-B pipeline rows render byte-identical text after the timestamp helpers move to companion/layout.py as before the move."
    - "The absolute-first ordering — the ISO string first, the relative age in parentheses — is unchanged everywhere it already exists."
    - "A malformed or empty stored timestamp degrades to the raw string (or a caller-chosen fallback) instead of raising during a page render."
  artifacts:
    - "companion/layout.py — public parse_iso(), age_seconds(), relative_age_text(), absolute_and_relative()"
    - "companion/pages/health_page.py — the three private timestamp helpers removed, all call sites rewired to layout, Battery Trend cells reformatted"
    - "companion/test_status_pages.py — 3 new checks, EXPECTED_CHECK_COUNT bumped by exactly 3"
  key_links:
    - "companion/pages/health_page.py -> companion.layout is the only legal import path for these helpers; companion/pages/__init__.py forbids one page module importing another, which is why the helpers move to layout.py rather than being imported across pages."
    - "layout.data_table() escapes every cell it is given, so absolute_and_relative() must return plain (unescaped) text — the same contract status_dot()'s label parameter already has."
    - "_battery_section() keeps exactly one parameter: sibling phase 06.5's 06.5-02-PLAN.md pins the literal call-site text in its own automated gate."
---

<objective>
Promote `health_page.py`'s already-shipped "absolute + relative" timestamp helpers into `companion/layout.py` as public, shared functions, then use them to fix the first of D-02's three inconsistent spots: Health's own Battery Trend table, which today prints a raw ISO string while the Device check-in and ADS-B pipeline rows two sections above it print `"ISO (Nm ago)"`.

Purpose: D-02 is a consistency pass, not a new design. The target format already exists, already ships, and is already the house style on this exact page — the only real work is putting the logic somewhere all four page modules can legally reach it, and then applying it. This plan owns the shared helper, so plan 06.6-03 (History + Preview, wave 2) can consume it without duplicating the logic a third time.

Output: four new public functions in `companion/layout.py`; `health_page.py` reduced to a consumer of them; a Battery Trend table whose Timestamp column reads like every other timestamp on the page; three new automated checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-CONTEXT.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-RESEARCH.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-PATTERNS.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-VALIDATION.md

@companion/layout.py
@companion/pages/health_page.py
@companion/test_status_pages.py

# ROADMAP.md and STATE.md are deliberately not attached: this phase carries no
# REQUIREMENTS.md requirement id (it is an unmapped backlog phase, promoted from
# 999.6) and everything it needs is captured in the four phase artifacts above.
</context>

<cross_phase_constraints>
**Three sibling phases are fully planned but NOT yet executed against these same files: 06.3 (health_page render() restructured into `layout.stat_tile()` cards), 06.4 (config_page runway picker), 06.5 (health_page `_battery_section()` gains a badge, per-point hit targets and a `<script src>` tag).** They may execute before or after this plan, in any order.

1. **Locate every edit target by grepping for the function/symbol name. Never by line number.** Every line number quoted in `06.6-CONTEXT.md`'s canonical_refs describes the pre-06.3/06.4/06.5 snapshot and will have drifted if any sibling ran first. Read the target function fresh and target whatever structure is actually there.

2. **Do NOT change `_battery_section()`'s parameter list.** `06.5-02-PLAN.md`'s own automated gate greps `companion/pages/health_page.py` for the exact call-site text `battery_html, battery_state = _battery_section(trend_rows)`. Adding a `now` parameter breaks that gate in whichever order the two phases run. Compute `now` as a local inside `_battery_section()` via `history_db.utc_now_iso()` instead — the same call `render()` already makes for its own `now`. The resulting sub-millisecond clock skew is immaterial at this helper's second/minute/hour/day granularity.

3. **Do not touch `render()`, `battery_sparkline_svg()`, `battery_status()`, `collect_anomalies()`, `_section()`, `_CORROBORATION_ROWS` or `corroboration_status()`** beyond what Task 1's helper rewiring strictly requires. 06.3 and 06.5 both restructure `render()`; 06.6-03 owns the corroboration verification.

4. **Do not change the output ordering of the existing format.** The ISO string comes first, the relative age follows in parentheses. `06.3-UI-SPEC.md`'s Typography table shows a relative-first example; `06.6-RESEARCH.md` Open Question 1 resolved that (by exhaustive grep across every 06.3 artifact) as illustrative prose that no 06.3 plan task implements or depends on. Do not chase it.
</cross_phase_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the timestamp helpers into companion/layout.py and rewire health_page.py onto them</name>

  <files>companion/layout.py, companion/pages/health_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/layout.py` in full (208 lines). Note especially: the module docstring's single-escaping-choke-point discipline; `escape_html()` and its never-raises contract; `status_dot()`, whose `label` parameter is *plain text the helper itself escapes* — that is the contract the new helpers must match; `data_table()`, which escapes every cell value via `escape_html(cell)` and therefore accepts a pre-formatted plain-text string as a drop-in replacement for a raw value.
    - `companion/pages/health_page.py` in full. Locate by grep, not line number: `_parse_iso`, `_age_seconds`, `_cutoff_iso`, `_relative_age_text`, `_device_section`, `_pipeline_section`, `staleness_status`, and the `from datetime import ...` line.
    - `companion/test_status_pages.py` — its `check(name, fn)` helper inside `main()`, the `EXPECTED_CHECK_COUNT` module constant, the `_ctx(state_dir, now=None)` / `_iso()` / `_now()` / `_ago()` / `_seed_device_health()` fixture helpers, the existing `_staleness_status_boundaries` check (the closest style analog), and the import block's `# noqa: E402` convention.
    - `.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-PATTERNS.md` — the section "`companion/layout.py` (new shared helpers)", which carries the verbatim source to promote and the placement rationale.
  </read_first>

  <behavior>
    - `layout.absolute_and_relative("2026-08-28T13:58:02+00:00", "2026-08-28T14:01:02+00:00")` returns `"2026-08-28T13:58:02+00:00 (3m ago)"` — absolute first, relative in parentheses.
    - `layout.absolute_and_relative("2026-08-28T13:58:02Z", "2026-08-28T13:58:32+00:00")` returns a string ending in `"(30s ago)"` — the `Z`-suffixed form `server/panel_preview.panel_file_mtime_iso()` produces parses and subtracts cleanly against the `+00:00` form `history_db.utc_now_iso()` produces. (Confirmed during planning on this project's own interpreter, `server/.venv/bin/python3` = CPython 3.11.15.)
    - `layout.absolute_and_relative(None, now)` returns the fallback, whose default is the string `"no reading yet"` already used by `_device_section()`.
    - `layout.absolute_and_relative("", now, fallback="")` returns `""`.
    - `layout.absolute_and_relative("not-a-date", now)` returns `"not-a-date"` — absolute only, no relative suffix, no exception.
    - `layout.absolute_and_relative("2026-08-28T13:58:02+00:00", None)` returns the ISO string unchanged — a caller with no `now` still gets the absolute value.
    - `layout.relative_age_text()` keeps the existing s/m/h/d thresholds and clamps a negative age to 0.
    - `health_page` no longer defines its own `_relative_age_text` attribute; a rendered Health page's Device row is textually unchanged.
  </behavior>

  <action>
In `companion/layout.py`:

1. Add `from datetime import datetime` immediately after `import html`, and update the module docstring's opening constraint sentence so it reads "stdlib `html` and `datetime` only" — keeping the rest of that sentence (no imports from server/, nothing from companion.auth beyond the UI-theme cookie name) exactly as written.

2. Immediately after `escape_html()`, under a short section banner comment, add four public functions. The first three are promoted verbatim in body from `companion/pages/health_page.py` — copy the logic, do not re-derive it:

   - `parse_iso(ts)` — returns `datetime.fromisoformat(ts)`, or `None` when `ts` is not a `str` or does not parse. Never raises.
   - `age_seconds(ts, now_ts)` — parses both through `parse_iso()` and returns `(now - parsed).total_seconds()`, or `None` when either side fails to parse.
   - `relative_age_text(age_seconds)` — the existing s/m/h/d threshold ladder, clamping a negative age to 0. Its parameter shadows the module-level `age_seconds` function inside its own body; that shadowing already exists in `health_page.py` today and is harmless because this function never calls it. Keep the parity rather than renaming.
   - `absolute_and_relative(ts, now_ts, fallback="no reading yet")` — new. Returns `fallback` when `ts` is falsy; returns `ts` unchanged when `age_seconds(ts, now_ts)` is `None` (unparseable timestamp, or no `now_ts` supplied); otherwise returns `"%s (%s)" % (ts, relative_age_text(age))`.

3. Give `absolute_and_relative()` a docstring recording three things a future reader needs: (a) the return value is **plain, unescaped text**, matching `status_dot()`'s `label` convention — every caller keeps calling `escape_html()` on the result, or hands it to a builder such as `data_table()` that already escapes; (b) absolute-first ordering is this app's shipped, canonical convention (D-02) and must not be reversed — `06.3-UI-SPEC.md`'s relative-first Typography example is illustrative prose that no plan implements, resolved in `06.6-RESEARCH.md` Open Question 1; (c) an unparseable timestamp degrades to the absolute string rather than raising, which is a deliberate hardening over the pre-existing `health_page.py` path where an empty-string timestamp would have reached the relative-age helper as `None` and raised a `TypeError` mid-render.

In `companion/pages/health_page.py`:

4. Delete the three private helpers `_parse_iso`, `_age_seconds` and `_relative_age_text` outright. This module already does `import companion.layout as layout`, so every use becomes a `layout.`-prefixed call. Do not leave delegating aliases behind — a grep across `companion/`, `server/`, `scripts/` and every sibling-phase plan file was run during planning and found no reference to any of the three outside this module.

5. Rewire the three surviving call sites:
   - `_cutoff_iso()` calls `layout.parse_iso(now_ts)`.
   - `_device_section()` keeps `age = layout.age_seconds(ts, now)` (its `staleness_status()` argument is unchanged) and builds its detail text as `escape_html(layout.absolute_and_relative(ts, now))`. This is byte-identical output: `html.escape()` touches only `& < > " '`, so escaping the joined string equals escaping the two parts separately, and the `ts is None` branch already produced the same `"no reading yet"` string the helper's default fallback returns.
   - `_pipeline_section()` gets the same treatment against `pipeline_ts`.

6. Change the import line `from datetime import datetime, timedelta` to import `timedelta` only — `datetime` was used exclusively by the deleted `_parse_iso`, and leaving it would fail `ruff`'s unused-import rule.

In `companion/test_status_pages.py`:

7. Add `import companion.layout as layout  # noqa: E402` to the post-`sys.path` import block, alongside the existing `from companion.pages import airlines_page, health_page` line.

8. Add two `check(...)` calls in the same style as the surrounding checks, placed near the existing `_staleness_status_boundaries` check:
   - One exercising `layout.absolute_and_relative()` against every case in this task's behavior list above — the `+00:00`/`+00:00` pair, the `Z`/`+00:00` pair, the `None` fallback, the explicit empty-string fallback, the unparseable-timestamp degradation, and the missing-`now_ts` degradation.
   - One regression check proving the promotion is a *move* and not a copy: seed one `device_health` reading through `_seed_device_health()`, render Health through `health_page.render(_ctx(tmp, now=...))`, assert the seeded ISO string and a `" ago)"` suffix both appear on the page, and assert `hasattr(health_page, "_relative_age_text")` is false.

9. Bump `EXPECTED_CHECK_COUNT` by exactly 2 — computed from the file's **current** value read at execution time, not from any number quoted in a planning document. Sibling phases 06.3 and 06.5 also add checks to this harness and may have run first.
  </action>

  <acceptance_criteria>
    - `companion/layout.py` defines `parse_iso`, `age_seconds`, `relative_age_text` and `absolute_and_relative` as public (non-underscore) module functions.
    - `companion/layout.py`'s module docstring names `datetime` alongside `html` in its stdlib-only constraint sentence, and still contains the "no imports from server/" clause.
    - `absolute_and_relative()`'s docstring states that its return value is plain, unescaped text.
    - `health_page._relative_age_text`, `health_page._age_seconds` and `health_page._parse_iso` no longer exist as attributes of the module.
    - `layout.absolute_and_relative('2026-08-28T13:58:02+00:00', '2026-08-28T14:01:02+00:00')` equals `'2026-08-28T13:58:02+00:00 (3m ago)'`.
    - `layout.absolute_and_relative('not-a-date', '2026-08-28T14:01:02+00:00')` equals `'not-a-date'` and raises nothing.
    - A Health render with a seeded `device_health` row still shows the stored ISO string followed by a parenthesised `" ago)"` suffix on the Device check-in row.
    - `companion/test_status_pages.py` passes with `EXPECTED_CHECK_COUNT` exactly 2 higher than its pre-task value, and every check green.
    - `server/.venv/bin/ruff check companion/` is clean.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/ruff check companion/ && [ "$(grep -v '^#' companion/layout.py | grep -c 'def absolute_and_relative')" = "1" ] && [ "$(grep -v '^#' companion/layout.py | grep -c 'def relative_age_text')" = "1" ] && server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); import companion.layout as l; from companion.pages import health_page as h; assert not hasattr(h,'_relative_age_text') and not hasattr(h,'_age_seconds') and not hasattr(h,'_parse_iso'), 'helpers were copied, not promoted'; assert l.absolute_and_relative('2026-08-28T13:58:02+00:00','2026-08-28T14:01:02+00:00') == '2026-08-28T13:58:02+00:00 (3m ago)'; assert l.absolute_and_relative('2026-08-28T13:58:02Z','2026-08-28T13:58:32+00:00').endswith('(30s ago)'); assert l.absolute_and_relative(None,'2026-08-28T14:01:02+00:00') == 'no reading yet'; assert l.absolute_and_relative('',  '2026-08-28T14:01:02+00:00', fallback='') == ''; assert l.absolute_and_relative('not-a-date','2026-08-28T14:01:02+00:00') == 'not-a-date'; assert l.absolute_and_relative('2026-08-28T13:58:02+00:00', None) == '2026-08-28T13:58:02+00:00'; assert 'unescaped' in (l.absolute_and_relative.__doc__ or ''), 'the plain-text contract must be stated in the docstring'; print('ok')"</automated>
  </verify>

  <done>`companion/layout.py` owns the four shared timestamp helpers with a documented plain-text return contract; `health_page.py` is a pure consumer of them with no private copy left behind and no unused `datetime` import; the Device and ADS-B pipeline rows render exactly what they rendered before; `test_status_pages.py` carries two new green checks and a correctly bumped `EXPECTED_CHECK_COUNT`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Battery Trend table renders "ISO (Nm ago)" without changing _battery_section()'s signature (D-02)</name>

  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` — re-read `_battery_section` **fresh** by grepping for `def _battery_section`, not by line number. Sibling phase 06.5's `06.5-02-PLAN.md` rewrites this exact function (adding a status badge block, a readout paragraph and a `<script src>` tag) and sibling 06.3's `06.3-04-PLAN.md` rewrites `render()` around it; if either has already executed, the function will not match any snippet quoted in this phase's planning documents. Target whatever structure is actually there. Also re-read `render()`'s own `now = ctx.get("now") or history_db.utc_now_iso()` line as the pattern for computing a local `now`.
    - `companion/layout.py` — `data_table()` and `absolute_and_relative()` as written by Task 1.
    - `.planning/phases/06.5-interactive-battery-trend-chart-with-overall-status/06.5-02-PLAN.md` — its Task 1 acceptance criteria and automated verify block, which is where the pinned single-argument call-site literal lives. Read it so the constraint is concrete rather than hearsay.
    - `companion/test_status_pages.py` — the existing `_battery_trend_shows_all_readings_and_one_sparkline` and `_battery_empty_state_no_svg` checks (the two this task must not break) and `_seed_device_health()`.
  </read_first>

  <behavior>
    - With three seeded `device_health` readings, a rendered Health page's Battery Trend table cell for each row contains the stored ISO timestamp followed by a parenthesised relative age ending in `" ago)"`.
    - The existing battery checks still pass unchanged: all three `battery_mv` values still appear, there is still exactly one `<svg>` and one `<polyline>`, and zero rows still render the good-news empty state with no `<svg>`.
    - `_battery_section` still accepts exactly one parameter.
    - A row whose `ts` is missing renders an empty cell, exactly as it does today — not the words "no reading yet".
  </behavior>

  <action>
In `companion/pages/health_page.py`, change only the Battery Trend table's cell construction inside `_battery_section()`'s data branch:

1. Compute a local `now` at the top of that branch via `history_db.utc_now_iso()` — `history_db` is already imported by this module. Add a one-line comment stating why it is computed locally instead of threaded in as a parameter: `_battery_section()`'s single-argument call site is pinned by sibling phase 06.5's own automated gate, so widening the signature would break that gate in whichever order the two phases execute.

2. Change the `table_rows` list comprehension so the first element of each tuple becomes `layout.absolute_and_relative(row.get("ts"), now, fallback="")` instead of the raw `row.get("ts")`. The explicit empty-string fallback preserves today's behaviour for a row with no stored timestamp — `escape_html(None)` already renders as an empty cell, and the helper's default `"no reading yet"` would be a visible change nobody asked for.

3. Leave the `layout.data_table([...], table_rows, mono_columns=(0, 1))` call exactly as it is. `data_table()` escapes every cell it receives, so handing it a pre-formatted plain-text string is a drop-in substitution requiring no change to that builder and no change to the `mono_columns` indices.

4. Change nothing else in the function — not the empty-history branch, not the unavailable branch, not the second return value, not `battery_sparkline_svg()`'s call, and nothing that 06.5 may have added around it.

In `companion/test_status_pages.py`:

5. Add one `check(...)` beside the existing battery checks: seed three `device_health` readings at known offsets through `_seed_device_health()`, render with `health_page.render(_ctx(tmp, now=...))` using a `now` that makes the ages deterministic, and assert that for each seeded reading the page contains that reading's exact ISO string immediately followed by `" ("` and that the page contains `" ago)"`. Additionally assert, via `inspect.signature(health_page._battery_section)`, that the function still has exactly one parameter — this is the machine-readable form of the cross-phase constraint and it is why the check belongs here rather than only in the shell gate.

6. Bump `EXPECTED_CHECK_COUNT` by exactly 1, computed from the file's current value at execution time (Task 1 already bumped it by 2 in this same plan; read, do not assume).
  </action>

  <acceptance_criteria>
    - A rendered Health page with three seeded battery readings contains each reading's ISO timestamp followed by a parenthesised relative age.
    - `companion/pages/health_page.py` still contains the text `battery_html, battery_state = _battery_section(trend_rows)` exactly once.
    - `inspect.signature(health_page._battery_section)` reports exactly one parameter.
    - The pre-existing `_battery_trend_shows_all_readings_and_one_sparkline` and `_battery_empty_state_no_svg` checks are unmodified and still pass.
    - `companion/test_status_pages.py` passes with `EXPECTED_CHECK_COUNT` exactly 3 higher than its value before this plan started.
    - `scripts/run-all-tests.sh` exits 0 (all 15 harnesses green, coverage threshold met).
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/ruff check companion/ && [ "$(grep -c 'battery_html, battery_state = _battery_section(trend_rows)' companion/pages/health_page.py)" = "1" ] && server/.venv/bin/python3 -c "import sys, inspect; sys.path.insert(0,'.'); from companion.pages import health_page as h; assert len(inspect.signature(h._battery_section).parameters) == 1, '_battery_section must stay single-argument (06.5-02 pins its call site)'; print('ok')" && scripts/run-all-tests.sh</automated>
  </verify>

  <done>Health's Battery Trend table reads `"ISO (Nm ago)"` in its Timestamp column, matching the Device and ADS-B pipeline rows on the same page; `_battery_section()`'s call site and parameter list are byte-identical to what sibling phase 06.5 expects; the whole suite is green.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol / file | Kind | Location |
|---|---|---|
| `parse_iso(ts)` | public function (promoted) | `companion/layout.py` |
| `age_seconds(ts, now_ts)` | public function (promoted) | `companion/layout.py` |
| `relative_age_text(age_seconds)` | public function (promoted) | `companion/layout.py` |
| `absolute_and_relative(ts, now_ts, fallback="no reading yet")` | new public function | `companion/layout.py` |
| `_parse_iso` / `_age_seconds` / `_relative_age_text` | **removed** | `companion/pages/health_page.py` |
| `_battery_section(trend_rows)` | changed output, unchanged signature | `companion/pages/health_page.py` |
| `_device_section()` / `_pipeline_section()` / `_cutoff_iso()` | rewired onto `layout.*`, output unchanged | `companion/pages/health_page.py` |
| 3 new `check(...)` calls + `EXPECTED_CHECK_COUNT` +3 | test coverage | `companion/test_status_pages.py` |

No new files. No new packages. No new routes.

## Multi-Source Coverage Audit (plan 06.6-01 slice)

| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL (ROADMAP) | Companion UI clarity pass — timestamps | Tasks 1-2 | COVERED |
| REQ (REQUIREMENTS.md) | none — unmapped backlog phase (promoted from 999.6) | n/a | N/A |
| CONTEXT | D-02 — Health's Battery Trend table gains the absolute+relative format | Task 2 | COVERED |
| CONTEXT | Claude's Discretion — factor out a shared helper rather than duplicate the relative-time logic three times | Task 1 (promoted to `companion/layout.py`) | COVERED |
| RESEARCH | Primary recommendation: promote the helpers to `companion/layout.py` | Task 1 | COVERED |
| RESEARCH | Pitfall 1 — do not widen `_battery_section()`'s signature | Task 2 + `<cross_phase_constraints>` | COVERED |
| RESEARCH | Pitfall 2 — locate by name, never by line number | `<cross_phase_constraints>` + every `read_first` | COVERED |
| RESEARCH | Pitfall 3 — bump `EXPECTED_CHECK_COUNT` in the same commit | Tasks 1-2 step 9 / step 6 | COVERED |
| RESEARCH | Pitfall 4 — do not reverse the format ordering for 06.3-UI-SPEC's example | `<cross_phase_constraints>` item 4 + Task 1 docstring | COVERED |
| VALIDATION | 06.6-01-01 (helper promotion), 06.6-01-02 (Battery Trend) | Tasks 1, 2 | COVERED |

D-01 and D-03 are covered by plans 06.6-02 and 06.6-03 respectively. No item is unplanned; no item is deferred.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `history.db` -> page render | Stored `device_health.ts` strings are read back and interpolated into HTML. They are server-written by `poll_loop.py`, not client-supplied, but they are still untrusted-by-discipline at the escaping choke point. |
| server -> browser | Server-rendered HTML only. This plan adds no client-side code, no new route, no form field and no new input parameter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6-01 | Tampering (XSS) | `layout.absolute_and_relative()`'s return value reaching Health's HTML | medium | mitigate | The helper returns plain, unescaped text — a contract stated in its docstring and machine-asserted by Task 1's automated verify gate. Every call site keeps its escaping: `_device_section()`/`_pipeline_section()` wrap the result in `escape_html()`, and the Battery Trend cell is escaped by `layout.data_table()`'s own per-cell `escape_html(cell)`. Task 2 does not touch `data_table()`. |
| T-06.6-02 | Denial of Service (unhandled exception during page render) | `parse_iso()` / `absolute_and_relative()` on a malformed or empty stored timestamp | low | mitigate | `absolute_and_relative()` returns the fallback for a falsy timestamp and the raw string when parsing fails, never raising. This closes a real latent defect: today an empty-string `ts` would reach `_relative_age_text(None)` and raise `TypeError` mid-render. Task 1's behavior list and automated verify gate both pin the non-raising cases. |

## Not applicable at ASVS level 1 for this plan

- **V2 Authentication / V3 Session Management / V4 Access Control** — unchanged. These pages already sit behind `companion/app.py`'s existing per-route session gate; no route is added, removed or re-gated.
- **V6 Cryptography** — untouched.
- **Supply chain (no `T-06.6-SC` row).** This plan installs nothing. `06.6-RESEARCH.md`'s Package Legitimacy Audit section states the gate is not applicable: zero new `pip`/`npm`/`cargo` packages, stdlib `datetime` only.
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_status_pages.py` — every check green and the total equal to the bumped `EXPECTED_CHECK_COUNT`.
2. `server/.venv/bin/ruff check companion/` — clean (catches the now-unused `datetime` import if step 6 of Task 1 was missed).
3. `scripts/run-all-tests.sh` — all 15 harnesses green, coverage threshold (83) met.
4. Cross-phase gate: `grep -c 'battery_html, battery_state = _battery_section(trend_rows)' companion/pages/health_page.py` returns exactly 1.
5. Promotion-not-duplication gate: `health_page` exposes none of `_parse_iso`, `_age_seconds`, `_relative_age_text`.
</verification>

<success_criteria>
- Health's Battery Trend Timestamp column reads `"ISO (Nm ago)"`, the same shape as the Device check-in and ADS-B pipeline rows on the same page.
- Exactly one implementation of the relative-age logic exists in the codebase, in `companion/layout.py`, reachable by every page module without violating `companion/pages/__init__.py`'s no-page-imports-a-page contract.
- The existing Device/Pipeline rendered text is unchanged, and the format ordering is unchanged.
- `_battery_section()`'s parameter list and call-site text are byte-identical to what sibling phase 06.5's pinned gate expects.
- `companion/test_status_pages.py` carries 3 new checks with `EXPECTED_CHECK_COUNT` bumped by exactly 3; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-01-SUMMARY.md` when done.

Record in it: the pre-task and post-task `EXPECTED_CHECK_COUNT` values for `companion/test_status_pages.py`; whether any sibling phase (06.3/06.4/06.5) had already executed and, if so, what `_battery_section()` actually looked like when read fresh; and the fact that `companion/layout.py` now exports four public timestamp helpers that plan 06.6-03 consumes.
</output>
