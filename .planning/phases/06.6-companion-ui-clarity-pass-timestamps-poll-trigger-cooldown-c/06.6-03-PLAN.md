---
phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c
plan: 03
type: execute
wave: 2
depends_on: ["06.6-01"]
files_modified:
  - companion/pages/history_page.py
  - companion/pages/preview_page.py
  - companion/test_view_pages.py
autonomous: true
requirements: [D-02, D-03]

must_haves:
  truths:
    - "History's Timestamp column shows the stored ISO timestamp followed by its relative age, matching Health's Device and pipeline rows (D-02)."
    - "Preview's Captured caption shows the panel file's ISO mtime followed by its relative age, keeping the existing 'Captured ' wording (D-02)."
    - "The no-panel-yet Preview branch is unchanged: still an honest sentence, still no image element, still no relative-age suffix."
    - "The corroboration copy on Health and History is confirmed consistent, and the single-source unknown state is confirmed still labelled ok rather than a failure (D-03)."
  artifacts:
    - "companion/pages/history_page.py — format_event_row(row, now=None), render() reads ctx['now']"
    - "companion/pages/preview_page.py — preview_section() formats the caption through layout.absolute_and_relative()"
    - "companion/test_view_pages.py — 3 new checks, EXPECTED_CHECK_COUNT bumped by exactly 3, _history_ctx()/_preview_ctx() carry the documented now key"
  key_links:
    - "companion/layout.py::absolute_and_relative() (created by plan 06.6-01) is the single shared implementation both page modules consume — companion/pages/__init__.py forbids either of them importing health_page directly."
    - "ctx['now'] is populated for every page by companion/app.py::page_context(); history_page falls back to history_db.utc_now_iso() (which it already imports), preview_page degrades to absolute-only rather than widening its documented import contract."
    - "history_page._CORROBORATION_LABELS must stay equal, key by key, to the (status, label) pairs in health_page._CORROBORATION_ROWS — a permanent cross-module check replaces the one-off manual inspection."
---

<objective>
Finish D-02's timestamp consistency pass by applying `companion/layout.py`'s shared `absolute_and_relative()` helper to the two remaining spots — History's Timestamp column and Preview's Captured caption — and close D-03 by verifying the ADS-B corroboration copy against the live modules and converting that one-off verification into a permanent regression guard.

Purpose: after plan 06.6-01, three of the app's five timestamp-bearing locations already read `"ISO (Nm ago)"`. These are the last two. D-03 is deliberately verify-and-polish: `06.6-CONTEXT.md` and `06.6-RESEARCH.md` both independently found the existing copy already correct, so the honest deliverable is a confirmation plus a check that stops the two copies drifting — not an invented rewrite.

Output: `history_page.py` and `preview_page.py` reduced to consumers of the shared helper; three new automated checks; a documented D-03 finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-CONTEXT.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-RESEARCH.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-PATTERNS.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-VALIDATION.md
@.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-01-SUMMARY.md

@companion/layout.py
@companion/pages/history_page.py
@companion/pages/preview_page.py
@companion/test_view_pages.py

# The full Multi-Source Coverage Audit for phase 06.6 lives in 06.6-01-PLAN.md.
# ROADMAP.md/STATE.md are deliberately not attached — this phase carries no
# REQUIREMENTS.md requirement id and its context lives in the artifacts above.
</context>

<cross_phase_constraints>
**Sibling phase 06.3 is fully planned but NOT yet executed against `companion/pages/history_page.py`,** and may run before or after this plan.

1. **Locate every edit target by grepping for the function name, never by line number.** `06.3-03-PLAN.md` rewrites `_history_table_html()`'s return expression to wrap the table in a `.data-table-wrap` div.

2. **Do not touch `_history_table_html()`'s return block, its Corroboration cell, or its escaping.** `06.3-03-PLAN.md` owns the return block, and its own threat mitigation machine-checks that `layout.status_dot(` occurs exactly once and `layout.data_table(` zero times in `history_page.py`. This plan adds `layout.absolute_and_relative(` — a different symbol — and must leave both of those counts exactly as they are. In particular: do not take the opportunity to refactor `_history_table_html()` into a `layout.data_table()` call; that would double-escape the status dot into visible tag text, the exact defect the module docstring warns about.

3. **`_history_table_html()` needs no change at all for D-02.** Its existing `escape_html(cell["ts"])` already escapes whatever plain text `format_event_row()` put in that key. The change is entirely upstream, in `format_event_row()`.

4. **This plan does not modify `companion/pages/health_page.py`.** Task 3 reads `_CORROBORATION_ROWS` from it, nothing more. That file belongs to plan 06.6-01 (already complete when this wave runs) and to sibling phases 06.3/06.5.
</cross_phase_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: History's Timestamp column renders "ISO (Nm ago)" (D-02)</name>

  <files>companion/pages/history_page.py, companion/test_view_pages.py</files>

  <read_first>
    - `companion/pages/history_page.py` in full (165 lines). Note especially: the module docstring's explanation of why this table is hand-built rather than delegated to `layout.data_table()` (the unescaped `status_dot()` cell) and its statement that no page module imports another page module — the reason the shared timestamp helper had to move to `companion/layout.py` in the first place; `format_event_row()`; `_history_table_html()`'s `escape_html(cell["ts"])` cell; and `render(ctx)`, which today reads only `ctx["state_dir"]`. The file already does `import companion.layout as layout` and `from server import history_db`.
    - `companion/layout.py` — `absolute_and_relative()` as created by plan 06.6-01, especially its plain-text return contract and its `fallback` parameter.
    - `companion/pages/health_page.py` — `render()`'s `now = ctx.get("now") or history_db.utc_now_iso()` line, read only. That is the exact fallback idiom this task replicates.
    - `companion/pages/__init__.py` — the ctx contract, specifically the documented `now` key.
    - `companion/test_view_pages.py` — its `check(name, fn)` helper inside `main()`, `EXPECTED_CHECK_COUNT`, the `_mkstate()` / `_seed_runway_events()` / `_history_ctx()` fixture helpers, and the import block's `# noqa: E402` convention.
  </read_first>

  <behavior>
    - With a `runway_events` row seeded at a known ISO timestamp and a `ctx["now"]` three minutes later, the rendered History page contains that ISO string immediately followed by `" (3m ago)"`.
    - `format_event_row(row)` called with one argument still works and returns the raw stored timestamp with no relative suffix — the second parameter is optional so no hypothetical existing caller breaks.
    - A row with no stored timestamp still produces an empty Timestamp cell, not the words "no reading yet".
    - `render(ctx)` with a ctx that has no `now` key still renders without raising and still shows a relative suffix, because it falls back to `history_db.utc_now_iso()`.
  </behavior>

  <action>
In `companion/pages/history_page.py`:

1. Change `format_event_row(row)` to `format_event_row(row, now=None)`. The parameter is optional on purpose: any caller that has not been updated degrades to an absolute-only timestamp rather than raising, matching `absolute_and_relative()`'s own missing-`now_ts` behaviour. Extend the function's docstring with one sentence naming `now` as the reference instant used for the relative-age suffix.

2. Change that function's `"ts"` entry from `row.get("ts") or ""` to `layout.absolute_and_relative(row.get("ts"), now, fallback="")`. The explicit empty-string fallback preserves today's exact behaviour for a row with no stored timestamp — the helper's default `"no reading yet"` would be a visible change nobody asked for.

3. In `render(ctx)`, add `now = ctx.get("now") or history_db.utc_now_iso()` as the second line, mirroring `health_page.render()`'s own line character for character, and pass it through: `_history_table_html([format_event_row(row, now) for row in rows])`. `history_db` is already imported by this module, so no import changes.

4. Change nothing else. `_history_table_html()`, `_CORROBORATION_LABELS`, `_HEADERS`, `_safe_query()` and `history_rows()` are all untouched (see cross-phase constraints 2 and 3).

In `companion/test_view_pages.py`:

5. Extend the `_history_ctx()` fixture helper to `_history_ctx(state_dir, now=None)`, returning `{"state_dir": state_dir, "now": now or history_db.utc_now_iso()}`. This brings the fixture into line with `companion/pages/__init__.py`'s documented ctx contract (every page's ctx carries `now`) and matches `companion/test_status_pages.py`'s own `_ctx(state_dir, now=None)` helper. Existing single-argument call sites keep working unchanged.

6. Add one `check(...)` asserting every bullet in this task's behavior list above: seed one `runway_events` row at a known ISO timestamp via `_seed_runway_events()`, render through `history_page.render(_history_ctx(tmp, now=<that timestamp plus three minutes>))`, and assert the page contains the seeded ISO followed by `" (3m ago)"`; separately assert `format_event_row({"ts": "<iso>"})` called with one argument returns that raw timestamp in its `"ts"` key, and that `format_event_row({}, <now>)["ts"]` is the empty string.

7. Bump `EXPECTED_CHECK_COUNT` by exactly 1, computed from the file's **current** value read at execution time. Sibling phase 06.3 also adds checks to this harness and may have run first — do not hardcode a number quoted in any planning document.
  </action>

  <acceptance_criteria>
    - A rendered History page with one seeded event contains the stored ISO timestamp immediately followed by `" (3m ago)"` when `ctx["now"]` is three minutes later.
    - `history_page.format_event_row` accepts two parameters, the second with a default, and a one-argument call returns the raw timestamp unchanged.
    - `history_page.format_event_row({}, now)["ts"]` equals `""`.
    - `companion/pages/history_page.py` contains `layout.status_dot(` exactly once and `layout.data_table(` zero times — sibling phase 06.3's pinned counts are unchanged.
    - `companion/pages/history_page.py` does not import the stdlib `html` module directly (the existing check in `companion/test_view_pages.py` still passes).
    - `companion/test_view_pages.py` passes with `EXPECTED_CHECK_COUNT` exactly 1 higher than its pre-task value.
    - `server/.venv/bin/ruff check companion/` is clean.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/ruff check companion/ && [ "$(grep -c 'layout.status_dot(' companion/pages/history_page.py)" = "1" ] && [ "$(grep -c 'layout.data_table(' companion/pages/history_page.py)" = "0" ] && server/.venv/bin/python3 -c "import sys, inspect; sys.path.insert(0,'.'); from companion.pages import history_page as h; sig = inspect.signature(h.format_event_row); assert len(sig.parameters) == 2 and list(sig.parameters.values())[1].default is None, 'format_event_row must gain an optional now parameter'; assert h.format_event_row({'ts': '2026-08-28T13:58:02+00:00'})['ts'] == '2026-08-28T13:58:02+00:00', 'a one-argument call must degrade to the raw timestamp'; assert h.format_event_row({'ts': '2026-08-28T13:58:02+00:00'}, '2026-08-28T14:01:02+00:00')['ts'] == '2026-08-28T13:58:02+00:00 (3m ago)'; assert h.format_event_row({}, '2026-08-28T14:01:02+00:00')['ts'] == '', 'a missing timestamp must stay an empty cell'; print('ok')"</automated>
  </verify>

  <done>History's Timestamp column reads `"ISO (Nm ago)"`, sourced from the one shared helper in `companion/layout.py`; `_history_table_html()` and its unescaped status-dot cell are untouched, so sibling phase 06.3's pinned symbol counts and its own planned wrap-div change both still land cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preview's Captured caption renders "Captured ISO (Nm ago)" (D-02)</name>

  <files>companion/pages/preview_page.py, companion/test_view_pages.py</files>

  <read_first>
    - `companion/pages/preview_page.py` in full (116 lines). Note especially: the module docstring's import contract — this module imports `server.panel_preview` and `companion.layout` only, and must never import `companion.app`; `PREVIEW_CAPTION = "Captured %s"` and `_NO_PANEL_CAPTION`; `preview_section(ctx)`'s two branches and its `escape_html(caption_text)` wrapper; and the standing instruction not to add any form control to this page.
    - `server/panel_preview.py` — `panel_file_mtime_iso()`, read only. Confirm for yourself that it returns a `Z`-suffixed ISO-8601 string (`"YYYY-MM-DDTHH:MM:SSZ"`), which is a different suffix from the `+00:00` form `history_db.utc_now_iso()` produces.
    - `companion/layout.py` — `absolute_and_relative()` and `parse_iso()` as created by plan 06.6-01.
    - `companion/test_view_pages.py` — the `_write_panel_file()` and `_preview_ctx()` fixture helpers, `_img_tag_count()`, and the existing Preview checks (especially the no-panel-yet branch, which must stay green).
  </read_first>

  <behavior>
    - With a panel file present, the rendered Preview page's caption begins with `"Captured "` and contains a parenthesised relative age ending in `" ago)"`.
    - With no panel file, the caption is still exactly `_NO_PANEL_CAPTION`, there is still no image element for the live preview, and the caption contains no relative-age suffix.
    - `layout.absolute_and_relative("2026-08-28T13:58:02Z", "2026-08-28T13:58:32+00:00")` — the mixed `Z` / `+00:00` pair this page actually produces — yields a relative suffix rather than degrading to absolute-only.
    - A ctx with no `now` key still renders without raising; the caption degrades to the absolute timestamp alone.
  </behavior>

  <action>
In `companion/pages/preview_page.py`:

1. Add `absolute_and_relative` to the existing `from companion.layout import empty_state, escape_html` line, keeping alphabetical order. Do **not** add an import of `server.history_db` — that would widen this module's documented two-import contract for no benefit. `absolute_and_relative()` degrades to the absolute string when no reference instant is supplied, which is exactly the graceful behaviour that lets this module stay `history_db`-free.

2. In `preview_section(ctx)`'s panel-present branch, change `caption_text = PREVIEW_CAPTION % mtime_iso` to interpolate `absolute_and_relative(mtime_iso, ctx.get("now"))` instead. `PREVIEW_CAPTION`'s literal `"Captured %s"` wording is unchanged — D-02 extends the app's existing format onto the existing caption, it does not rewrite the caption's words.

3. Leave the no-panel branch, `image_html`, the `escape_html(caption_text)` wrapper, `caveat_html`, `gallery_tiles()` and `render()` untouched.

4. Add a short comment above the changed line recording the format-compatibility finding so a future reader does not re-investigate it: `panel_file_mtime_iso()` returns a `Z`-suffixed ISO string while `ctx["now"]` is `+00:00`-suffixed; `datetime.fromisoformat()` parses both into timezone-aware values on this project's interpreter, so subtracting them raises nothing and no normalising shim is needed. This was verified during planning against `server/.venv/bin/python3`, which is CPython 3.11.15 — the `Z` suffix has been accepted since 3.11.

In `companion/test_view_pages.py`:

5. Extend `_preview_ctx()` to `_preview_ctx(state_dir, gallery_entries=None, now=None)`, adding `"now": now or history_db.utc_now_iso()` to the returned dict so the fixture matches `companion/pages/__init__.py`'s documented ctx contract. Existing call sites keep working unchanged.

6. Add one `check(...)` asserting every bullet in this task's behavior list above: write a panel file with `_write_panel_file()`, render through `preview_page.render(_preview_ctx(tmp))`, assert the caption contains `"Captured "` and `" ago)"`; then, against a fresh state directory with no panel file, assert the rendered page contains `_NO_PANEL_CAPTION` and no `" ago)"`; and assert directly that `layout.absolute_and_relative` handles the mixed `Z` / `+00:00` pair.

7. Bump `EXPECTED_CHECK_COUNT` by exactly 1, computed from the file's current value at execution time (Task 1 already bumped it by 1 within this same plan — read the file, do not assume).
  </action>

  <acceptance_criteria>
    - A rendered Preview page with a panel file present contains a caption starting `"Captured "` and containing `" ago)"`.
    - A rendered Preview page with no panel file contains `_NO_PANEL_CAPTION`, contains no `" ago)"`, and still emits no live-preview image element.
    - `PREVIEW_CAPTION` is still exactly `"Captured %s"`.
    - `companion/pages/preview_page.py` imports nothing from `server` other than `panel_preview`, and imports nothing from `companion` other than `layout` — in particular it does not import `server.history_db` or `companion.app`.
    - `companion/test_view_pages.py` passes with `EXPECTED_CHECK_COUNT` exactly 2 higher than its value before this plan started.
    - `server/.venv/bin/ruff check companion/` is clean.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/ruff check companion/ && [ "$(grep -c 'history_db' companion/pages/preview_page.py)" = "0" ] && [ "$(grep -c 'companion.app' companion/pages/preview_page.py)" = "0" ] && server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import preview_page as p; import companion.layout as l; assert p.PREVIEW_CAPTION == 'Captured %s', 'the caption wording must not change'; assert l.absolute_and_relative('2026-08-28T13:58:02Z','2026-08-28T13:58:32+00:00').endswith('(30s ago)'), 'the Z-suffixed mtime form must subtract cleanly against the +00:00 now form'; print('ok')"</automated>
  </verify>

  <done>Preview's Captured caption reads `"Captured ISO (Nm ago)"` through the same shared helper the other four timestamp sites now use; the caption's wording, the honest no-panel branch and this module's narrow two-import contract are all unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the ADS-B corroboration copy and make the cross-page consistency permanent (D-03)</name>

  <files>companion/test_view_pages.py, companion/pages/history_page.py</files>

  <read_first>
    - `companion/pages/health_page.py` — `_CORROBORATION_ROWS` and `corroboration_status()`, **read only, do not edit** (that file is not this plan's to modify). Locate both by grep. `_CORROBORATION_ROWS` is the canonical copy: a tuple of `(stored_string, display_label, status, explanation)` entries.
    - `companion/pages/history_page.py` — `_CORROBORATION_LABELS` and `_DEFAULT_CORROBORATION`, and the comment above them stating they mirror `health_page.py`'s table so the two pages read consistently.
    - `.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-CONTEXT.md` — D-03 in full, including the explicit instruction not to invent copy changes without a concrete found problem.
    - `.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-RESEARCH.md` — the D-03 paragraph in the Summary, which independently reached the same finding.
    - `companion/test_view_pages.py` — its import block (currently `from companion.pages import history_page, preview_page`) and `EXPECTED_CHECK_COUNT`.
  </read_first>

  <action>
This task is **verify-and-polish, not build-from-scratch**. Both `06.6-CONTEXT.md` (D-03) and `06.6-RESEARCH.md` independently examined this copy and found it already correct. Confirm that against the live modules, then convert the one-off confirmation into a permanent guard.

1. **Verify.** Read `health_page._CORROBORATION_ROWS` and `history_page._CORROBORATION_LABELS` as they actually are right now (sibling phases may have run since this plan was written). Check three things: that both tables cover exactly the three stored strings `"True"`, `"None"` and `"False"`; that the `(status, label)` pair each table assigns to a given key is identical across the two; and that the `"None"` key — the single-source, uncorroborated, genuinely-unknown state — maps to the `"ok"` status in both, never to a warning or an error.

2. **Change nothing if nothing is wrong.** If the three checks above hold, make no copy change of any kind. Record the finding in the SUMMARY: what you compared, what you found, and that D-03 therefore closes as verified. Do not invent an improvement.

3. **Fix only a concrete finding.** If and only if a real inconsistency exists, correct `companion/pages/history_page.py`'s `_CORROBORATION_LABELS` to match `health_page._CORROBORATION_ROWS` — `health_page.py` is canonical because it carries the four-field table with the per-state explanations, and because `corroboration_status()` documents the "unknown is not a failure" rule alongside it. Do not edit `health_page.py` from this plan. Describe any change made, and the defect that justified it, in the SUMMARY.

4. **Do not change `_DEFAULT_CORROBORATION`.** Its `("warn", "Unknown")` value is the fallback for an unrecognised or legacy value found in the database, which is a genuinely different situation from the `"None"` single-source state and is correctly a warning. Leave it exactly as it is.

5. **Make it permanent.** Add `health_page` to `companion/test_view_pages.py`'s existing `from companion.pages import history_page, preview_page` import line. A test module importing two page modules is fine — `companion/pages/__init__.py`'s no-page-imports-a-page rule binds page modules to each other, not the harness that exercises them.

6. Add one `check(...)` that, for each of `"True"`, `"None"` and `"False"`, looks up the matching entry in `health_page._CORROBORATION_ROWS` and asserts `history_page._CORROBORATION_LABELS[key]` equals that entry's `(status, label)` pair; that both tables have exactly those three keys and no others; and, as a separately-named assertion with its own failure message, that `history_page._CORROBORATION_LABELS["None"][0] == "ok"` — the D-15/D-03 rule that the unknown state is never labelled a failure. This is the check that stops the two hand-maintained tables from ever silently drifting again.

7. Bump `EXPECTED_CHECK_COUNT` by exactly 1, computed from the file's current value at execution time (Tasks 1 and 2 already bumped it by 2 within this same plan — read the file, do not assume).
  </action>

  <acceptance_criteria>
    - For each of `"True"`, `"None"` and `"False"`, `history_page._CORROBORATION_LABELS[key]` equals the `(status, label)` pair carried by the matching `health_page._CORROBORATION_ROWS` entry.
    - `history_page._CORROBORATION_LABELS["None"][0]` equals `"ok"` and `health_page._CORROBORATION_ROWS`'s `"None"` entry carries the status `"ok"`.
    - Both tables cover exactly the three keys `"True"`, `"None"`, `"False"`.
    - `history_page._DEFAULT_CORROBORATION` is unchanged.
    - `companion/pages/health_page.py` is not modified by this plan.
    - `companion/test_view_pages.py` passes with `EXPECTED_CHECK_COUNT` exactly 3 higher than its value before this plan started.
    - `scripts/run-all-tests.sh` exits 0.
    - The SUMMARY records the D-03 finding explicitly — either "verified consistent, no change made" with what was compared, or the concrete defect found and the fix applied.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import health_page as hp, history_page as yp; rows = {k: (s, l) for k, l, s, _e in hp._CORROBORATION_ROWS}; assert set(rows) == {'True','None','False'} == set(yp._CORROBORATION_LABELS), 'the two corroboration tables must cover exactly the same three states'; assert all(yp._CORROBORATION_LABELS[k] == rows[k] for k in rows), 'health_page and history_page corroboration copy has drifted: %r vs %r' % (rows, yp._CORROBORATION_LABELS); assert yp._CORROBORATION_LABELS['None'][0] == 'ok' and rows['None'][0] == 'ok', 'the unknown single-source state must never be labelled a failure (D-03/D-15)'; assert yp._DEFAULT_CORROBORATION == ('warn','Unknown'), 'the unrecognised-value fallback must stay a warning'; print('ok')" && server/.venv/bin/ruff check companion/ && scripts/run-all-tests.sh</automated>
  </verify>

  <done>D-03 is closed with a documented finding rather than an invented rewrite, and the two hand-maintained corroboration tables are now pinned to each other by an automated check — including the specific rule that the single-source unknown state stays `"ok"`.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol / file | Kind | Location |
|---|---|---|
| `format_event_row(row, now=None)` | changed signature (optional second parameter) + changed output | `companion/pages/history_page.py` |
| `render(ctx)` | now reads `ctx["now"]` with a `history_db.utc_now_iso()` fallback | `companion/pages/history_page.py` |
| `preview_section(ctx)` | changed caption output; `absolute_and_relative` added to the `companion.layout` import | `companion/pages/preview_page.py` |
| `_history_ctx(state_dir, now=None)` | fixture helper gains the documented `now` key | `companion/test_view_pages.py` |
| `_preview_ctx(state_dir, gallery_entries=None, now=None)` | fixture helper gains the documented `now` key | `companion/test_view_pages.py` |
| 3 new `check(...)` calls + `EXPECTED_CHECK_COUNT` +3 | test coverage | `companion/test_view_pages.py` |

No new files. No new packages. No new routes. No `health_page.py` change.

## Artifacts across all of phase 06.6

For the executor's convenience, the complete set of symbols this phase creates or changes:

- `companion/layout.py`: `parse_iso()`, `age_seconds()`, `relative_age_text()`, `absolute_and_relative()` *(plan 01)*
- `companion/pages/health_page.py`: `_parse_iso`/`_age_seconds`/`_relative_age_text` **removed**; `_battery_section()`, `_device_section()`, `_pipeline_section()`, `_cutoff_iso()` rewired *(plan 01)*
- `companion/pages/config_page.py`: `POLL_TRIGGER_BUTTON_ID`, `POLL_COOLDOWN_TEXT_ID`, `POLL_COOLDOWN_TEMPLATE_TOKEN`, `_js_literal()`, `_poll_cooldown_script()`, rewritten `poll_trigger_section()` *(plan 02)*
- `companion/pages/history_page.py`: `format_event_row(row, now=None)` *(plan 03)*
- `companion/pages/preview_page.py`: `preview_section()` caption format *(plan 03)*
- Test harnesses: `test_status_pages.py` +3 checks, `test_config_page.py` +3 checks, `test_view_pages.py` +3 checks, each with `EXPECTED_CHECK_COUNT` bumped by the same amount in the same commit
- No new files, no new packages, no new routes, no `companion/app.py` change anywhere in the phase.

## Multi-Source Coverage Audit (plan 06.6-03 slice)

| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL (ROADMAP) | Companion UI clarity pass — timestamps, corroboration copy | Tasks 1-3 | COVERED |
| REQ (REQUIREMENTS.md) | none — unmapped backlog phase (promoted from 999.6) | n/a | N/A |
| CONTEXT | D-02 — History's table gains the absolute+relative format | Task 1 | COVERED |
| CONTEXT | D-02 — Preview's Captured caption gains the absolute+relative format | Task 2 | COVERED |
| CONTEXT | D-03 — corroboration copy is verify-and-polish; confirm against the live page, do not invent changes | Task 3 (verify first, change only on a concrete finding, plus a permanent drift guard) | COVERED |
| CONTEXT | Claude's Discretion — do not duplicate the relative-time logic three times | Tasks 1-2 consume plan 01's `layout.absolute_and_relative()` | COVERED |
| RESEARCH | `panel_file_mtime_iso()`'s `Z` suffix parses and subtracts against the `+00:00` form | Task 2 steps 4 + behavior list + automated verify assertion | COVERED |
| RESEARCH | `history_page.render()` does not currently read `ctx["now"]` and must start doing so | Task 1 step 3 | COVERED |
| RESEARCH | Pitfall 2 — locate by name, never by line number | `<cross_phase_constraints>` + every `read_first` | COVERED |
| RESEARCH | Pitfall 3 — bump `EXPECTED_CHECK_COUNT` in the same commit | Tasks 1, 2, 3 final steps | COVERED |
| VALIDATION | 06.6-01-03 (History table), 06.6-01-04 (Preview caption), 06.6-03-01 (corroboration) | Tasks 1, 2, 3 | COVERED |

D-01 is covered by plan 06.6-02. No item is unplanned; no item is deferred.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `history.db` -> page render | Stored `runway_events.ts` strings are read back and interpolated into HTML. Server-written by `poll_loop.py`, still untrusted-by-discipline at the escaping choke point. |
| filesystem -> page render | `panel.bin`'s mtime, formatted by `server/panel_preview.panel_file_mtime_iso()`, reaches the Preview caption. |
| server -> browser | Server-rendered HTML only. This plan adds no client-side code, no new route, no form field and no new input parameter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6-07 | Tampering (XSS) | History's Timestamp cell and Preview's Captured caption | medium | mitigate | Both call sites keep their existing escaping unchanged: `_history_table_html()` still wraps the cell in `escape_html(cell["ts"])`, and `preview_section()` still wraps the whole caption in `escape_html(caption_text)`. `layout.absolute_and_relative()` returns plain text by documented contract, so escaping happens exactly once, at the same point it does today. Cross-phase constraint 2 forbids touching `_history_table_html()`'s return block or its single deliberately-unescaped `status_dot()` cell, and Task 1's automated verify gate machine-checks that `layout.status_dot(` still occurs exactly once and `layout.data_table(` zero times. |
| T-06.6-08 | Repudiation / misinformation | corroboration copy labelling the unknown ADS-B state as a failure | low | mitigate | Task 3 verifies both tables against each other at execution time and then adds a permanent cross-module equality check plus a dedicated assertion that the `"None"` state's status is `"ok"`. A future edit that turns the unknown state into a warning — telling a single-source deployment its two sources disagreed, which they did not — fails the harness rather than shipping. |
| T-06.6-09 | Denial of Service (unhandled exception during page render) | parsing the `Z`-suffixed panel mtime against the `+00:00` `now` | low | mitigate | Verified during planning on this project's own interpreter (`server/.venv/bin/python3`, CPython 3.11.15): `datetime.fromisoformat()` accepts both suffixes and returns timezone-aware values, so the subtraction cannot raise `TypeError`. Task 2's behavior list and automated verify gate both pin the mixed-suffix case, and `absolute_and_relative()` returns the absolute string rather than raising for anything it cannot parse. |

## Not applicable at ASVS level 1 for this plan

- **V2 Authentication / V3 Session Management / V4 Access Control** — unchanged. `/history` and `/preview` already sit behind `companion/app.py`'s existing per-route session gate; no route is added, removed or re-gated. The Preview page still builds every gallery URL only from a name in `ctx["gallery_entries"]` and still joins no path itself — untouched by this plan.
- **V5 Input Validation** — no new input reaches the server. `ctx["now"]` is server-generated.
- **V6 Cryptography** — untouched.
- **Supply chain (no `T-06.6-SC` row).** This plan installs nothing. `06.6-RESEARCH.md`'s Package Legitimacy Audit section states the gate is not applicable: zero new `pip`/`npm`/`cargo` packages.
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_view_pages.py` — every check green and the total equal to the bumped `EXPECTED_CHECK_COUNT`.
2. `server/.venv/bin/python3 companion/test_status_pages.py` — still green (Task 3 imports `health_page` but modifies nothing in it).
3. `server/.venv/bin/ruff check companion/` — clean.
4. Cross-phase gates: `layout.status_dot(` occurs exactly once and `layout.data_table(` zero times in `companion/pages/history_page.py`.
5. Import-contract gate: `companion/pages/preview_page.py` references neither `history_db` nor `companion.app`.
6. Corroboration gate: the two tables agree key-by-key, both cover exactly three states, `"None"` is `"ok"` in both, and `_DEFAULT_CORROBORATION` is still `("warn", "Unknown")`.
7. `scripts/run-all-tests.sh` — all 15 harnesses green, coverage threshold (83) met.
</verification>

<success_criteria>
- All five of the companion app's timestamp-bearing locations — Device check-in, ADS-B pipeline, Battery Trend, History, Preview — now read `"ISO (Nm ago)"` in the same absolute-first order, from one shared implementation in `companion/layout.py`.
- Preview's caption keeps its existing "Captured " wording and its honest no-panel-yet fallback.
- `preview_page.py` still imports only `server.panel_preview` and `companion.layout`; `history_page.py` still hand-builds its table with exactly one unescaped `status_dot()` cell and zero `data_table()` calls.
- D-03 is closed with a recorded finding, no invented copy change, and a permanent check that the Health and History corroboration tables cannot drift apart and cannot relabel the unknown state as a failure.
- `companion/test_view_pages.py` carries 3 new checks with `EXPECTED_CHECK_COUNT` bumped by exactly 3; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-03-SUMMARY.md` when done.

Record in it: the pre-plan and post-plan `EXPECTED_CHECK_COUNT` values for `companion/test_view_pages.py`; the D-03 verification finding in full (what was compared, what was found, and whether any change was made — if none, say so explicitly, since "no diff" is this task's expected and correct outcome); and whether sibling phase 06.3 had already executed against `history_page.py` and what `_history_table_html()` looked like when read fresh.
</output>
