---
phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c
plan: 01
subsystem: ui
tags: [companion, html-templating, timestamps, python]

# Dependency graph
requires: []
provides:
  - "companion/layout.py public timestamp helpers: parse_iso(), age_seconds(), relative_age_text(), absolute_and_relative()"
  - "Health's Battery Trend table Timestamp column in the house 'ISO (Nm ago)' format"
affects: ["06.6-03 (History + Preview, wave 2, consumes layout.absolute_and_relative())"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared cross-page helpers live in companion/layout.py, never duplicated into a second page module — companion/pages/__init__.py forbids one page module importing another, so a helper every page needs must live in the shared layout layer."
    - "A builder's 'plain, unescaped text' return contract is stated in its own docstring (status_dot()'s label precedent, now also absolute_and_relative())."

key-files:
  created: []
  modified:
    - companion/layout.py
    - companion/pages/health_page.py
    - companion/test_status_pages.py

key-decisions:
  - "absolute_and_relative() hardens the pre-existing empty-string-timestamp path: health_page.py's prior _relative_age_text(None) call would have raised TypeError mid-render; the promoted helper degrades to a caller-chosen fallback instead."
  - "_battery_section() keeps its single-argument signature; `now` is computed locally via history_db.utc_now_iso() rather than threaded in as a parameter, to avoid breaking sibling phase 06.5-02's own automated gate on the exact call-site text."

patterns-established:
  - "New layout.py builders that return unescaped text must state that contract in their own docstring, matching status_dot()'s precedent, and callers escape it themselves or pass it to a builder (e.g. data_table()) that already escapes."

requirements-completed: [D-02]

coverage:
  - id: D1
    description: "companion/layout.py exports four public timestamp helpers (parse_iso, age_seconds, relative_age_text, absolute_and_relative); health_page.py's three private copies are removed, not duplicated."
    requirement: "D-02"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py#layout.absolute_and_relative() covers every documented case: ordering, Z-suffix parsing, default/explicit fallback, unparseable-timestamp degradation, missing now_ts"
        status: pass
      - kind: unit
        ref: "companion/test_status_pages.py#health_page's private timestamp helpers are gone (a move, not a copy) and the Device row still renders the absolute-plus-relative format"
        status: pass
    human_judgment: false
  - id: D2
    description: "Health's Battery Trend table Timestamp column reads 'ISO (Nm ago)', matching the Device/ADS-B pipeline rows; _battery_section() keeps its single-argument signature."
    requirement: "D-02"
    verification:
      - kind: unit
        ref: "companion/test_status_pages.py#Battery Trend's Timestamp column shows 'ISO (Nm ago)', matching the Device/pipeline rows (D-02), and _battery_section() stays single-argument"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-30
status: complete
---

# Phase 06.6 Plan 01: Shared timestamp helpers + Battery Trend "ISO (Nm ago)" Summary

**Promoted `health_page.py`'s absolute+relative timestamp formatting into four public `companion/layout.py` functions, then applied `absolute_and_relative()` to Health's Battery Trend table so its Timestamp column matches the Device/ADS-B pipeline rows.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-30T07:01:39Z
- **Tasks:** 2/2 completed
- **Files modified:** 3 (`companion/layout.py`, `companion/pages/health_page.py`, `companion/test_status_pages.py`)

## Accomplishments
- `companion/layout.py` now owns `parse_iso()`, `age_seconds()`, `relative_age_text()` (promoted verbatim from `health_page.py`) and a new `absolute_and_relative()` — the single shared implementation every page module can legally reach.
- `health_page.py`'s three private helpers (`_parse_iso`, `_age_seconds`, `_relative_age_text`) are gone; `_device_section()`, `_pipeline_section()`, and `_cutoff_iso()` are rewired onto `layout.*` with byte-identical rendered output.
- Health's Battery Trend table's Timestamp column now reads `"ISO (Nm ago)"`, matching the Device check-in and ADS-B pipeline rows two sections above it (D-02).
- `_battery_section()`'s single-argument signature and its call-site text (`battery_html, battery_state = _battery_section(trend_rows)`) are byte-identical to what sibling phase 06.5-02's own automated gate expects.
- Three new automated checks added to `companion/test_status_pages.py`; `EXPECTED_CHECK_COUNT` bumped 44 → 47 (44 pre-plan + 2 Task 1 + 1 Task 2).

## Task Commits

1. **Task 1: Promote the timestamp helpers into companion/layout.py and rewire health_page.py onto them** - `5a13d21` (feat)
2. **Task 2: Battery Trend table renders "ISO (Nm ago)" without changing _battery_section()'s signature (D-02)** - `8035117` (feat)

**Plan metadata:** (this commit)

## Files Created/Modified
- `companion/layout.py` - Added `datetime` import and four public timestamp helpers (`parse_iso`, `age_seconds`, `relative_age_text`, `absolute_and_relative`), placed immediately after `escape_html()`; docstring's stdlib-only sentence updated to name `datetime` alongside `html`.
- `companion/pages/health_page.py` - Removed the three private timestamp helpers and the now-unused `datetime` import; `_device_section()`/`_pipeline_section()`/`_cutoff_iso()` rewired onto `layout.*`; `_battery_section()`'s data branch now computes `now` locally and formats each row's Timestamp cell via `layout.absolute_and_relative(row.get("ts"), now, fallback="")`.
- `companion/test_status_pages.py` - Added `import inspect`; added two Task 1 checks (`absolute_and_relative()`'s documented cases; the promotion-not-duplication regression guard) and one Task 2 check (Battery Trend absolute+relative rendering plus a machine-readable single-argument signature assertion); `EXPECTED_CHECK_COUNT` 44 → 46 → 47.

## Decisions Made
- Kept `relative_age_text(age_seconds)`'s parameter name shadowing the module-level `age_seconds` function, per the plan's explicit instruction to preserve parity with the pre-existing `health_page.py` code rather than rename it away.
- `absolute_and_relative()`'s docstring states the plain-text/unescaped-return contract explicitly (mirroring `status_dot()`'s `label` convention) — this is asserted by an automated gate, not just documented.

## Deviations from Plan

None - plan executed exactly as written. One out-of-scope pre-existing test failure was discovered and logged rather than fixed (see below).

### Out-of-scope discovery (logged, not fixed)

`scripts/run-all-tests.sh`'s `server/test_poll_loop.py` harness has one failing check in this sandbox environment — `"a default config against the FLIGHT1 fixture reproduces the pinned pre-06-10 panel.bin digest"` — unrelated to this plan's files. `git diff HEAD --stat` at the time confirmed only `companion/pages/health_page.py` and `companion/test_status_pages.py` were touched by Task 2 (Task 1's `companion/layout.py` change was already committed and is also unrelated to `server/plane/render.py` or `server/test_poll_loop.py`). Logged to `.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/deferred-items.md` per the executor's scope-boundary rule rather than investigated or fixed — most likely a font/Pillow-rendering environment difference from the digest's pinning environment (see the file for detail). All 4 `companion/*` harnesses (`test_companion_app.py`, `test_config_page.py`, `test_status_pages.py`, `test_view_pages.py`) — the harnesses this plan's files can affect — pass in full.

## Issues Encountered

Server/.venv did not exist in this fresh worktree (no network access available to `pip install` from `server/requirements.txt`/`server/requirements-dev.txt`). Resolved by symlinking `server/.venv` to the sibling main-repo checkout's already-provisioned venv (`/Users/florian/Projects/skypane/server/.venv`, Python 3.11.15, same interpreter version pinned by this project) — `server/.venv` is gitignored and the symlink was never staged or committed.

## Sibling-phase state observed (per plan's `<output>` instructions)

Read fresh (never by line number) per the plan's cross-phase constraints. `_battery_section()` already carried sibling phase 06.5-02's shipped changes when this plan started: a status badge block (`_battery_badge_block()`), a readout paragraph (`_battery_readout_block()`), and a `<script src>` tag — i.e. 06.5-02 had already executed against these files before this plan ran. `render()` already used `layout.stat_tile()` cards with icon parameters (06.6.1-04's icon work), consistent with 06.6.1 having executed. This plan targeted the actual as-read structure rather than any pre-06.3/06.4/06.5 snapshot quoted in planning documents, per the cross-phase constraint. `EXPECTED_CHECK_COUNT` was 44 before this plan (not the 42 some planning documents' comments referenced) — bumped by exactly 3 to 47, computed from the value read at execution time.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`companion/layout.py` now exports four public timestamp helpers (`parse_iso`, `age_seconds`, `relative_age_text`, `absolute_and_relative`) that sibling plan 06.6-03 (History + Preview, wave 2) can import directly rather than duplicating the logic a third time. D-02's Health/Battery Trend inconsistency is resolved; D-02's remaining two inconsistent spots (History, Preview) are 06.6-03's scope. No blockers for wave 2.

---
*Phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c*
*Plan: 01*
*Completed: 2026-08-30*

## Self-Check: PASSED

- FOUND: companion/layout.py
- FOUND: companion/pages/health_page.py
- FOUND: companion/test_status_pages.py
- FOUND: .planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-01-SUMMARY.md
- FOUND commit: 5a13d21 (Task 1)
- FOUND commit: 8035117 (Task 2)
