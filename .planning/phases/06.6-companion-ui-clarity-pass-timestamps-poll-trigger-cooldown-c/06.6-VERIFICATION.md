---
phase: 06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c
verified: 2026-08-30T09:45:00Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 06.6: Companion UI clarity pass — timestamps, poll-trigger cooldown, corroboration copy — Verification Report

**Phase Goal:** Three independent clarity fixes to the companion web app: (1) standardize timestamp presentation across all five timestamp-bearing pages (D-02), (2) make the poll-trigger cooldown a live countdown instead of a page-load-frozen number (D-01), (3) verify/polish ADS-B corroboration copy consistency (D-03).
**Verified:** 2026-08-30T09:45Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: Config page's poll-trigger cooldown is a live, self-decrementing client-side countdown, not a static page-load-frozen number | ✓ VERIFIED | `companion/pages/config_page.py::_poll_cooldown_script()` emits a `setInterval(fn, 1000)` IIFE that decrements `remaining` once per second and rewrites `text.textContent`. `_poll_cooldown_script()` is only appended on `poll_trigger_section()`'s disabled branch. |
| 2 | D-01: the button re-enables itself client-side at zero | ✓ VERIFIED | Inside the `setInterval` callback: on `remaining <= 0`, the script calls `clearInterval(timer)`, `btn.removeAttribute("disabled")`, and clears the paragraph text — confirmed by reading `_poll_cooldown_script()` (lines 215-265) and by `companion/test_config_page.py`'s 37/37 passing checks including the sink-safety and zero-cooldown regression checks added for this phase. |
| 3 | D-01: the server independently re-validates the cooldown regardless of client state | ✓ VERIFIED | `companion/app.py::_handle_poll_now()` (lines 643-659) calls `poll_cooldown_remaining(state_dir)` fresh on every POST and redirects with the cooldown flash if `remaining > 0`, **before** ever calling `poll_loop.run_once()` — independent of any client-side button/script state. This logic and file are unmodified by this phase (not in any plan's `files_modified`), confirming it was a pre-existing, independently-operating trust boundary the phase correctly left alone. |
| 4 | D-02: all five timestamp-bearing locations (Device check-in, ADS-B pipeline, Battery Trend, History's Timestamp column, Preview's Captured caption) render "ISO (Nm ago)" via one shared `companion/layout.py` implementation | ✓ VERIFIED | All five call sites read: `health_page._device_section()`, `health_page._pipeline_section()`, `health_page._battery_section()` (all three call `layout.absolute_and_relative`), `history_page.format_event_row()` (`layout.absolute_and_relative`), `preview_page.preview_section()` (imports and calls `absolute_and_relative` from `companion.layout`). Grep for `def _relative_age_text\|def _age_seconds\|def _parse_iso\b` across `companion/`, `server/`, `scripts/` returns zero matches outside `layout.py` — no duplicate relative-time logic exists anywhere else in the codebase. |
| 5 | D-03: ADS-B corroboration copy is confirmed consistent between `health_page.py` and `history_page.py` | ✓ VERIFIED | Read both tables directly: `health_page._CORROBORATION_ROWS` and `history_page._CORROBORATION_LABELS` agree key-by-key across all three states (`True`→`("ok","Agreement")`, `None`→`("ok","Single-source (uncorroborated)")`, `False`→`("warn","Disagreement")`). |
| 6 | D-03: a permanent automated check (not a one-off manual comparison) pins the two tables together | ✓ VERIFIED | `companion/test_view_pages.py` imports `health_page` and carries a check ("history_page._CORROBORATION_LABELS agrees with health_page._CORROBORATION_ROWS key-by-key...") that programmatically asserts equality of both tables and specifically that `_CORROBORATION_LABELS["None"][0] == "ok"`. Confirmed passing in the live 28/28 test run. |
| 7 | Code-review warnings WR-01 (age_seconds TypeError on tz-naive/aware mismatch) and WR-02 (poll_trigger_section truthy-vs->0 mismatch) are genuinely fixed in current file contents | ✓ VERIFIED | `companion/layout.py::age_seconds()` now wraps the subtraction in `try/except TypeError: return None` (confirmed by reading current source, lines 260-274). `companion/pages/config_page.py::poll_trigger_section()` now branches on `if cooldown_remaining > 0:` (not truthy), matching `_poll_cooldown_script()`'s own `remaining <= 0` guard, with an explanatory comment (lines 294-298). Both fixes land in commit `223882e`, which follows the review commit `dc562e8` in `git log`. |
| 8 | The full test suite passes except the pre-existing, documented, unrelated `server/test_poll_loop.py` panel.bin digest mismatch | ✓ VERIFIED | Ran `scripts/run-all-tests.sh` live: 14/15 harnesses pass, coverage 91% (threshold met); the sole failure is `server/test_poll_loop.py`'s "a default config against the FLIGHT1 fixture reproduces the pinned pre-06-10 panel.bin digest" check (macOS-local Pillow/FreeType rendering vs. a Linux-CI-pinned digest — unrelated to any file this phase touched, none of which are under `server/plane/` or `server/panel_format.py`). No other harness failed. |

**Score:** 8/8 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `companion/layout.py` | `parse_iso()`, `age_seconds()`, `relative_age_text()`, `absolute_and_relative()` public helpers | ✓ VERIFIED | All four present, public (non-underscore), documented, and used by all 5 call sites. |
| `companion/pages/health_page.py` | Private timestamp helpers removed; call sites rewired to `layout.*`; Battery Trend cells reformatted | ✓ VERIFIED | `_parse_iso`/`_age_seconds`/`_relative_age_text` no longer exist as module attributes (grep confirms). `_device_section`, `_pipeline_section`, `_battery_section` all call `layout.absolute_and_relative`/`layout.age_seconds`. |
| `companion/pages/config_page.py` | `POLL_TRIGGER_BUTTON_ID`, `POLL_COOLDOWN_TEXT_ID`, `POLL_COOLDOWN_TEMPLATE_TOKEN`, `_js_literal()`, `_poll_cooldown_script()`, rewritten `poll_trigger_section()` | ✓ VERIFIED | All present and correctly wired; `_js_literal()` wraps `json.dumps()` + `</`-breaking; script contains no forbidden sinks (`innerHTML`, `eval(`, `fetch(`, etc. — confirmed absent by grep and by the passing sink-safety test). |
| `companion/pages/history_page.py` | `format_event_row(row, now=None)`, `render()` reads `ctx["now"]` | ✓ VERIFIED | Confirmed by reading source; `_history_table_html()`, `_CORROBORATION_LABELS`, and the single `layout.status_dot(` / zero `layout.data_table(` counts are untouched per cross-phase constraints. |
| `companion/pages/preview_page.py` | `preview_section()` formats caption through `layout.absolute_and_relative()` | ✓ VERIFIED | Confirmed; module still imports only `panel_preview` and `companion.layout` (no `history_db`, no `companion.app`). |
| `companion/test_status_pages.py`, `test_config_page.py`, `test_view_pages.py` | +3 checks each, `EXPECTED_CHECK_COUNT` bumped accordingly | ✓ VERIFIED | Live run: 47/47, 37/37, 28/28 — all pass, matching the bumped counts recorded in each SUMMARY. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `health_page.py` | `companion.layout` | `layout.absolute_and_relative()` calls in `_device_section`/`_pipeline_section`/`_battery_section` | ✓ WIRED | Confirmed by direct source read. |
| `history_page.py` | `companion.layout` | `layout.absolute_and_relative()` in `format_event_row()` | ✓ WIRED | Confirmed by direct source read. |
| `preview_page.py` | `companion.layout` | `absolute_and_relative()` imported and used in `preview_section()` | ✓ WIRED | Confirmed by direct source read. |
| `config_page.py::render()` | `_poll_cooldown_script()` | `ctx["poll_cooldown_remaining"]` → `poll_trigger_section(cooldown_remaining)` → `_poll_cooldown_script(cooldown_remaining)` → `_js_literal()` | ✓ WIRED | Full chain confirmed by source read; no reference to `POLL_COOLDOWN_S` constant anywhere in `config_page.py` (grep confirms it lives only in `app.py`). |
| `app.py::_handle_poll_now()` | server-side cooldown gate | `poll_cooldown_remaining(state_dir)` re-check before `poll_loop.run_once()` | ✓ WIRED | Confirmed unmodified and independent of client state. |
| `history_page._CORROBORATION_LABELS` | `health_page._CORROBORATION_ROWS` | permanent cross-module equality check in `test_view_pages.py` | ✓ WIRED | Confirmed passing in live test run. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full companion test suites pass | `server/.venv/bin/python3 companion/test_status_pages.py` | `status-pages: 47/47 checks pass` | ✓ PASS |
| | `server/.venv/bin/python3 companion/test_config_page.py` | `config-page: 37/37 checks pass` | ✓ PASS |
| | `server/.venv/bin/python3 companion/test_view_pages.py` | `view-pages: 28/28 checks pass` | ✓ PASS |
| Full workspace suite | `scripts/run-all-tests.sh` | 14/15 harnesses pass, only pre-existing `server/test_poll_loop.py` digest mismatch fails | ✓ PASS (expected single pre-existing failure) |
| Lint | `server/.venv/bin/ruff check companion/` | `All checks passed!` | ✓ PASS |
| No duplicate relative-time logic | `grep -rn "def _relative_age_text\|def _age_seconds\|def _parse_iso\b" companion/ server/ scripts/` | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| D-01 | 06.6-02 | Live, self-decrementing poll-trigger cooldown countdown | ✓ SATISFIED | `_poll_cooldown_script()`, server re-validation confirmed. |
| D-02 | 06.6-01, 06.6-03 | All 5 timestamp locations render "ISO (Nm ago)" via one shared implementation | ✓ SATISFIED | All 5 call sites confirmed wired to `layout.absolute_and_relative()`; no duplicate logic. |
| D-03 | 06.6-03 | Corroboration copy verified consistent, permanent guard added | ✓ SATISFIED | Tables agree key-by-key; permanent cross-module check passing. |

No orphaned requirements — REQUIREMENTS.md does not map this backlog phase to any requirement id (confirmed by each plan's frontmatter: `requirements: [D-0x]` referencing CONTEXT.md decision ids, not REQUIREMENTS.md ids, consistent with this being an unmapped backlog phase promoted from 999.6).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | none found | — | Grep for `TODO\|FIXME\|XXX\|TBD\|HACK\|PLACEHOLDER` across all 8 touched files returned only two legitimate, non-stub uses of the word "placeholder" as English prose describing UI copy (`BATTERY_READOUT_PLACEHOLDER`), not a debt marker. |

**Debt marker gate:** clean — no unresolved `TBD`/`FIXME`/`XXX` markers in any file this phase touched.

**Note (non-blocking, informational):** the WR-01/WR-02 fix commit (`223882e`) changed only `companion/layout.py` and `companion/pages/config_page.py` — it did not add a dedicated regression test pinning either fix (e.g. a tz-naive-vs-aware `age_seconds()` case, or a negative-cooldown `poll_trigger_section()` case). The fixes themselves are directly confirmed correct by reading the current source (try/except TypeError in `age_seconds()`; `> 0` comparison in `poll_trigger_section()`, matching `_poll_cooldown_script()`'s own guard), and the full 47/37/28 check counts still pass with no regression. This is a code-quality gap (missing regression coverage for a bug fix), not a functional gap — the review's two warnings were about robustness, not about test coverage of the fix, and both were explicitly "not known to be triggered by any current code path" per the review itself. Recorded here for visibility; does not block phase completion.

### Human Verification Required

None. All must-haves resolve to VERIFIED via direct source inspection and passing automated tests. The one behavior the plan itself flagged as human-check (visually watching the countdown tick and the button re-enable in a real browser, `06.6-02-PLAN.md` Task 1's `<human-check>`) is explicitly marked "Optional, non-blocking" in the plan — the automated checks fully cover the seeded literal values, the sink-safety of the script body, and the zero-cooldown regression, and direct source reading confirms the `setInterval`/`clearInterval`/`removeAttribute` logic is correct and matches the documented behavior. No `<human-check>` block from any of this phase's three plans was left unaddressed or requires deferral.

### Gaps Summary

No gaps. All 8 derived observable truths (roadmap goal decomposed via the three CONTEXT.md decisions D-01/D-02/D-03, plus the explicit review-fix and test-suite verification asks) are VERIFIED against live source code and passing automated tests — not SUMMARY.md claims. Both post-review warning fixes (WR-01, WR-02) are confirmed present in the commit that follows the review commit. The only test failure in the full suite is the pre-existing, already-documented, unrelated `server/test_poll_loop.py` panel.bin digest mismatch, confirmed by direct execution to be the sole failure and unrelated to any file this phase modified.

---

_Verified: 2026-08-30T09:45:00Z_
_Verifier: Claude (gsd-verifier)_
