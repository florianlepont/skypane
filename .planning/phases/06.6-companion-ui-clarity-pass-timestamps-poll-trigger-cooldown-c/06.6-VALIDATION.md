---
phase: 06.6
slug: companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-28
---

# Phase 06.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Hand-rolled stdlib test harnesses (no pytest, no unittest) — each file defines a local `check(name, fn)` helper and a pinned `EXPECTED_CHECK_COUNT` module constant |
| **Config file** | none — each `companion/test_*.py` file is directly executable |
| **Quick run command** | `server/.venv/bin/python3 companion/test_status_pages.py` (or `test_view_pages.py` / `test_config_page.py`, matching whichever file a given task touches) |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~20-30 seconds |

---

## Sampling Rate

- **After every task commit:** run the single affected file's harness directly — each completes in low single-digit seconds against a fresh tempdir
- **After every plan wave:** `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.6-01-01 | 01 | 1 | D-02 (shared helper promotion) | T-06.6-01 | `_relative_age_text()`/`_age_seconds()`/`_parse_iso()` promoted to `companion/layout.py` (page modules cannot import each other); `health_page.py`'s existing call sites still produce identical output after the move | unit | `server/.venv/bin/python3 companion/test_status_pages.py` | ✅ file exists, extend | ⬜ pending |
| 06.6-01-02 | 01 | 1 | D-02 (Battery Trend table) | T-06.6-01 | Battery Trend table cell renders `"ISO (Nm ago)"`, not raw ISO; `_battery_section()` computes `now` internally (`history_db.utc_now_iso()`), does NOT add a parameter (would break 06.5's pinned single-argument grep gate) | unit | `server/.venv/bin/python3 companion/test_status_pages.py` | ✅ file exists, extend | ⬜ pending |
| 06.6-01-03 | 01 | 1 | D-02 (History table) | T-06.6-01 | History table's Timestamp column renders `"ISO (Nm ago)"` | unit | `server/.venv/bin/python3 companion/test_view_pages.py` | ✅ file exists, extend | ⬜ pending |
| 06.6-01-04 | 01 | 1 | D-02 (Preview caption) | T-06.6-01 | Preview's caption renders `"Captured ISO (Nm ago)"` | unit | `server/.venv/bin/python3 companion/test_view_pages.py` | ✅ file exists, extend | ⬜ pending |
| 06.6-02-01 | 02 | 1 | D-01 (live countdown) | T-06.6-02 | Disabled-cooldown branch of `poll_trigger_section()` emits an inline `<script>` block seeded from the server-persisted remaining-seconds value via `json.dumps()` (never raw string interpolation); ticks down and re-enables the button at zero | unit (string/DOM-marker assertions) | `server/.venv/bin/python3 companion/test_config_page.py` | ✅ file exists, extend | ⬜ pending |
| 06.6-02-02 | 02 | 1 | D-01 (regression + sink safety) | T-06.6-03 | `poll_trigger_section(0)` still renders no `<script>` and an enabled button (existing `_poll_trigger_enabled_at_zero_cooldown` check still passes); the inline script contains no `innerHTML`/`document.write`/`eval(`/`fetch(`/`XMLHttpRequest`, mirroring 06.5's own sink-safety grep gate | unit (source-grep style) | `server/.venv/bin/python3 companion/test_config_page.py` | ✅ file exists, extend | ⬜ pending |
| 06.6-03-01 | 03 | 1 | D-03 (corroboration copy, verify-only) | — | `_CORROBORATION_ROWS`/`_CORROBORATION_LABELS` remain consistent — regression guard, not new behavior; no code change expected unless a concrete problem is found | unit | `server/.venv/bin/python3 companion/test_status_pages.py` / `test_view_pages.py` (existing coverage) | ✅ existing coverage | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `companion/layout.py` — promote `_relative_age_text()`/`_age_seconds()`/`_parse_iso()` from `health_page.py`; new checks needed for the promoted location, plus a regression check that `health_page.py`'s existing call sites are unaffected.
- [ ] `companion/test_status_pages.py` — new check for Battery Trend's relative-age format.
- [ ] `companion/test_view_pages.py` — two new checks (History table, Preview caption).
- [ ] `companion/test_config_page.py` — two new checks (countdown script presence + seeding, sink-safety grep).
- [ ] All `EXPECTED_CHECK_COUNT` bumps computed from each file's **current** value at task-execution time, not hard-coded — sibling phases 06.3/06.4/06.5 also touch these harnesses and may execute in any order relative to 06.6.
- [ ] No new test framework or fixture infrastructure needed — extends the existing `check()` pattern already established in each harness.

---

## Manual-Only Verifications

*All phase behaviors have automated verification.* The live countdown's actual visual ticking (not just the script's presence/content) is a natural candidate for a quick real-browser glance, but is not required as a blocking manual step since the automated checks fully cover correctness of the seeded value and the absence of dangerous sinks.

---

## Validation Sign-Off

- [x] All tasks have automated verify (see table above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references — 6 new checks + 1 promoted-helper regression check still to be written by the planner/executor (see Wave 0 Requirements)
- [x] No watch-mode flags — all commands are one-shot script executions
- [x] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter — left `false` pending planner's actual task breakdown matching this map exactly

**Approval:** pending — planner must confirm task IDs above match its actual PLAN.md breakdown before flipping `nyquist_compliant: true`.
