---
phase: 23-companion-dynamism-live-updates-real-switches-motion-budge
plan: 11
type: execute
wave: 9
depends_on: [23-01, 23-02, 23-03, 23-04, 23-05, 23-06, 23-07, 23-08, 23-09, 23-10]
files_modified:
  - .claude/skills/sketch-findings-skypane/SKILL.md
  - .claude/skills/sketch-findings-skypane/references/accessibility-contrast.md
  - .claude/skills/sketch-findings-skypane/references/control-density.md
  - .claude/skills/sketch-findings-skypane/references/data-density.md
  - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CFG-38, CFG-32, CFG-33, CFG-34, CFG-35, CFG-36, CFG-37]

must_haves:
  truths:
    - "The design system is updated IN STEP with what this phase changed — a motion row where there was none, the app's first @keyframes, the view-transition gap and its closure, the switch pattern, and the live-refresh convention"
    - "Every supersession is marked IN PLACE with a stated reason and nothing is deleted, the discipline 22-16 established and verified by diff"
    - "The phase's coverage ledger names all seven D-items and, for each clause not built, the reason and the decision it needs — three findings carry forward to the developer rather than disappearing"
    - "The three items the roadmap struck before planning (D9 SSE, D12 service worker, D3's overlay drawer) are recorded as struck with their grounds, so a future audit does not re-propose them as unexamined ideas"
    - "The full suite is green at the phase gate, INCLUDING test_browser_ux.py not skipping — a SKIP line means this phase's interaction contracts were never checked, and this phase is almost entirely interaction"
  artifacts:
    - path: ".claude/skills/sketch-findings-skypane/SKILL.md"
      provides: "the motion row, the folded-in-work entry for Phase 23, and every widened register"
      contains: "Phase 23"
  key_links:
    - from: "the design system"
      to: "companion/static/style.css"
      via: "every value read live from the stylesheet at execution time, never recalled"
      pattern: "motion"
---

<objective>
`sketch-findings-skypane` is the authority and it must be updated in step with
whatever a phase changes — that is D-08's own framing and 22-16 is the worked
example. This phase gave the app its first duration tokens, its first
`@keyframes`, its first `<time>` elements, its first `role="switch"` controls, a
refresh loop on four pages instead of one, and a cross-document view transition
with a reduced-motion gap the global override does not reach. None of that is in
the design system yet, and "zero new custom properties" has been a stated
achievement of two prior phases that this one deliberately broke by two.

Purpose: this plan closes the phase. It records what changed where the next phase
will look for it, it writes the coverage ledger so the developer can see exactly
which D-clauses landed and which three need a decision, and it runs the human
sweep that `workflow.human_verify_mode: end-of-phase` defers to the end.
Output: an updated authority, a ledger, and a green suite that did not skip.
</objective>

<decisions_covered>
`22-CONTEXT.md` D-08's standing framing (the skill is the authority and moves in
step) applied to Phase 23, plus the phase's own regression floor (D-09). This plan
implements no feature and changes no companion source file: if a design-system row
cannot be written truthfully because the code does not match it, that is a finding
about the code and it is recorded, not smoothed over — 22-16 found two such rows
and corrected them rather than applying them mechanically.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-companion-dynamism-live-updates-real-switches-motion-budge/23-RESEARCH.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-CONTEXT.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-AUDIT.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan writes documentation only. It is the ONLY writer of every file in
`files_modified`, and it must not edit a single file under `companion/`,
`server/` or `deploy/`. If the sweep finds a defect, it is recorded as a finding
with its evidence — a closing plan that starts fixing code is how a phase stops
closing.
</files_owned>

<no_js_floor>
Not applicable to the documentation edits, and re-asserted for the phase as a
whole by Task 3: the sweep's own checklist includes running every page with
scripts blocked at 360 px and confirming that every setting this phase touched is
still saveable. The phase-level claim "the no-JS floor holds" is only true if
someone checked it after all ten plans landed, and that check is this plan's.
</no_js_floor>

<interfaces>
What each plan in this phase changed, and therefore what the design system owes a
row for (verify each against the code before writing — the plans state intent, the
code states fact):

  23-01  two motion duration tokens (the first new custom properties since Phase 19;
         Phases 20 and 21 each shipped zero, deliberately), the app's first and only
         `@keyframes`, and the executable motion guard
  23-04  `@view-transition` wrapped in `@media (prefers-reduced-motion: no-preference)`,
         three `view-transition-name` participants, and the recorded fact that the
         global `*, *::before, *::after` override does NOT reach view-transition
         pseudo-elements
  23-03  the `<time data-relative>` element convention and the future form
  23-05  the thirteenth script, the breathing dot as a further `.dot--off` consumer,
         and D22's completion
  23-06  the per-page refresh registry, the three swap skips, the picture fade
  23-07  the fourteenth script, `role="switch"` controls, the 204/303 content
         negotiation, `led_enabled`'s absent->unchanged move, and the toast
  23-08  the detail-row height animation, the chevron transition, the whole-card tap,
         the one-shot new-row highlight
  23-09  the save bar's entrance and its in-flight label
  23-10  the selection transition, the crossfade, the dialog entrances, the skeletons

Registers that move, and where they live:
  - the accent-reservation list lives in `companion/static/style.css`'s OWN header
    comment, never in the skill — extend it there first if this phase added a
    consumer (it should not have; verify)
  - `.dot--off`'s consumer list is in `SKILL.md`'s Navigation section
  - the touch-target register is in `references/control-density.md` — the three
    switches and the whole-card summary belong in it
  - the floating-overlay shadow exception list is in `SKILL.md`'s Cards paragraph
  - `references/data-density.md` carries the SUPERSEDED sticky-header entry that
    plan 23-08's finding refers to
  - `references/accessibility-contrast.md` carries the reduced-motion floor, which
    now has a named exception (view-transition pseudo-elements) for the first time
  - `references/settings-page-patterns.md` carries the save bar's history

Binding constraints:
- Read every value live from the code at execution time. `SKILL.md` says so in as
  many words and 22-16 found three stale load-bearing numbers by doing it.
- Mark supersessions IN PLACE with a stated reason. Delete nothing. Verify by diff.
- Record corrections of record where a previous entry was wrong, rather than
  quietly overwriting — the corrected `.calendar-disconnect-btn` history is the
  model.
- "Zero new custom properties" is broken by two, deliberately. Say so, with the
  reason, in the same paragraph that has claimed the achievement for two phases.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The design system gains a motion contract</name>
  <files>.claude/skills/sketch-findings-skypane/SKILL.md, .claude/skills/sketch-findings-skypane/references/accessibility-contrast.md, .claude/skills/sketch-findings-skypane/references/control-density.md, .claude/skills/sketch-findings-skypane/references/data-density.md, .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md</files>
  <behavior>
    - `SKILL.md` carries a motion entry: the two duration tokens with their values and purposes, the one `@keyframes` and its consumers, and the rule that new motion spends tokens rather than literals
    - `references/accessibility-contrast.md`'s reduced-motion floor gains its first named exception — view-transition pseudo-elements — with the mechanism that closes it and the reason that mechanism beats zeroing durations
    - `references/control-density.md`'s touch-target register gains the three switches and the whole-card summary, in the correct category, with measurements
    - `references/data-density.md`'s sticky-header entry gains a note that Phase 23 considered re-adding it and did not, with the reason
    - `references/settings-page-patterns.md`'s save-bar history gains a fifth entry for its entrance and in-flight label
    - Every value in every new row was read from the code during this task, and every supersession is marked in place
  </behavior>
  <read_first>
    - .claude/skills/sketch-findings-skypane/SKILL.md in full, including the Folded-In Work list's own format — each entry states what shipped, what it superseded and why, and the Phase 22 entry is the length and register to match
    - all five reference files being edited, in full
    - companion/static/style.css: the token block, the keyframes, the view-transition rules, and the file's own header comment (the accent-reservation list — check whether this phase added a consumer; if it did, that list is extended THERE, not here)
    - every 23-*-SUMMARY.md in this phase directory — they are the record of what actually happened, including the three findings
  </read_first>
  <action>
    Write the motion contract into `SKILL.md` as its own short paragraph in
    `<design_direction>`, beside spacing and controls: the two tokens with their
    real values, what each is for, the single `@keyframes` and the surfaces that
    consume it, and the standing rule that the global reduce block is the
    enforcement mechanism and a per-rule block is dead code. Name the harness check
    that makes it executable, the way the colour-separation contract names
    `contrast_check.py`.

    Then state plainly, in the paragraph that has carried the claim: "zero new
    custom properties" held for Phases 20 and 21 and does NOT hold for Phase 23 —
    two duration tokens were added deliberately, because a motion budget with no
    named durations is a budget in prose. That is a decision, and a phase that
    breaks a stated achievement should say so in the place the achievement is
    stated.

    Give `references/accessibility-contrast.md` its first named exception to the
    reduced-motion floor. The floor's own entry currently reads as universal
    ("every element… for free"), and it is not: the pseudo-element tree is outside
    it. Record the gap, the fix, and why the media wrapper is preferred to an
    `animation: none` override.

    Update the touch-target register, the sticky-header entry and the save-bar
    history as described above. Add the Folded-In Work entry for Phase 23 in the
    established format.

    Where a row cannot be written truthfully because the code does not match what a
    plan intended, do NOT write it. Record it as a finding in the SUMMARY, the way
    22-16 corrected two §4 rows that were wrong against the code rather than
    applying them mechanically.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); PYTHON="$PY" bash scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'motion' .claude/skills/sketch-findings-skypane/SKILL.md` is at least `3` and the new paragraph names both token values verbatim as they appear in `companion/static/style.css` — verified by diffing the two values, not by reading
    - `grep -c 'view-transition' .claude/skills/sketch-findings-skypane/references/accessibility-contrast.md` is at least `1`
    - `git diff --stat` on the skill shows only additions and in-place supersessions; `git diff` contains no deleted entry (state the deleted-line count, which must be zero except inside superseded sentences that were amended in place)
    - Every numeric value written into the skill is reproduced by a command in the SUMMARY (the token values, the keyframes count, the script count, the reduce-block count, the `:has()` block count, the check counts)
    - `scripts/run-all-tests.sh` still green — this task changes no code, and proving that is the point
  </acceptance_criteria>
  <done>The next phase's UI work will meet this phase's motion decisions where it looks for them.</done>
</task>

<task type="auto">
  <name>Task 2: The coverage ledger, and the three questions it hands back</name>
  <files>.planning/REQUIREMENTS.md</files>
  <behavior>
    - Every one of the phase's seven D-items has a status line naming the plans that served it
    - Every clause NOT built has its reason and, where a decision is needed, the decision that is needed
    - The three items struck before planning are recorded as struck, with their grounds, so they are not re-proposed as unexamined ideas
    - The requirement rows for this phase reflect what actually landed, verified by grepping the plans that claim them
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md — the row format, and Phase 22's rows as the model for how much evidence a status line carries here
    - every 23-*-SUMMARY.md in this phase directory
    - .planning/ROADMAP.md's Phase 23 entry, including its three pre-planning decisions
  </read_first>
  <action>
    Write the requirement rows for this phase in the established format: what
    landed, in which plan, with the evidence that proves it — `grep -l CFG-NN
    *-PLAN.md` is the convention this file uses to show a requirement's real span.

    Then write the ledger of what did not land, with three entries that are
    questions for the developer rather than omissions:

    1. **D2's fourth switch (notifications).** Not built. Its two checkboxes share
       a card with a Save-governed topic-URL field; converting only the checkboxes
       makes a card where some controls apply instantly and one needs Save, a
       pattern no locked source covers; converting the field too would mean an
       instant-apply text input the save bar exists to avoid; leaving the
       checkboxes and adding switches would be two controls for one setting, the
       defect X1/D-04 removed. Needs a decision.
    2. **D7's sticky day headers.** Not built. T4 removed the previous sticky claim
       on the structural ground that a sticky `th` inside a wrapper with no height
       can never engage, and that ground has not changed; making it engage means
       giving the Flights table a bounded-height scroll region, a layout decision
       with a measured width budget behind it. Needs a decision.
    3. **D22's orange reconnecting state.** Deliberately not implemented as worded.
       22-15 shipped it neutral, on the argued ground that a browser that lost its
       connection is not a device fault. Recorded as superseded by a shipped
       decision, not as an omission.

    Also record the three items the roadmap struck before planning — D9's SSE, D12's
    service worker and D3's overlay drawer — with their grounds in one line each, so
    the next audit meets the reasoning rather than the idea.

    If a `relative_age_text()` call site from plan 23-03's inventory is still
    unconverted, list it here too: the phase's own record of which visible ages
    tick and which do not belongs in one place.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); grep -c 'CFG-3' .planning/REQUIREMENTS.md && "$PY" -c "print('ledger written')"</automated>
  </verify>
  <acceptance_criteria>
    - Every requirement ID this phase's plans declare appears in `.planning/REQUIREMENTS.md` with a status line: `for id in CFG-32 CFG-33 CFG-34 CFG-35 CFG-36 CFG-37 CFG-38; do grep -c "$id" .planning/REQUIREMENTS.md; done` outputs a non-zero count for each
    - For each requirement, `grep -l "$id" .planning/phases/23-*/23-*-PLAN.md` returns the exact plan set the status line claims — run it and paste the output in the SUMMARY
    - The three not-built entries each name a reason AND the decision needed
    - The three struck items each carry their ground in one line
    - No requirement row claims a clause no plan implemented — verified by walking the seven D-items against the ten SUMMARYs, with the walk recorded
  </acceptance_criteria>
  <done>The developer can see, in one place, everything that shipped and the three things that need them.</done>
</task>

<task type="auto">
  <name>Task 3: The phase gate — the full suite, and the human sweep</name>
  <files>.planning/REQUIREMENTS.md</files>
  <behavior>
    - The full suite runs green, and `companion/test_browser_ux.py` does NOT skip
    - Every harness's final check count is recorded against its Phase 22 starting value
    - The no-JS floor is re-asserted across the whole phase's surface, not per plan
    - The human sweep's checklist is written out for the developer to walk
  </behavior>
  <read_first>
    - every 23-*-SUMMARY.md's own verification section, so the sweep covers what each plan actually shipped rather than what it planned to
    - `23-RESEARCH.md`'s "Sampling rate" section: the phase gate is the full suite green INCLUDING the browser harness not skipping, because a SKIP means the interaction contracts were never checked
  </read_first>
  <action>
    Run the full suite serially and in parallel, and record every harness's final
    `EXPECTED_CHECK_COUNT` beside its value at the start of this phase
    (browser_ux 26, companion_app 272, config_page 233, status_pages 268,
    view_pages 143, i18n 24, contrast_check 43). A count that moved without a plan
    claiming it is a finding.

    Confirm `companion/test_browser_ux.py` ran and did not SKIP. If it skipped,
    the phase is not verified — say so plainly rather than reporting the suite as
    green.

    Then write the human sweep's checklist into the SUMMARY, drawn from every
    plan's own `<human-check>` line, grouped so the developer can walk it in one
    pass rather than eleven:

    - **Motion, default settings.** Navigate Home → Display → Flights and watch the
      page transition. Change a setting and watch the save bar arrive and its count
      move. Click through theme chips and watch the selection answer and the
      preview cross-fade. Open and close both dialogs and a Flights detail row.
    - **Motion, reduce-motion on.** Repeat all of the above: navigation is instant,
      the dot is static, nothing animates, and everything still works.
    - **Live.** Leave Home open for two minutes: the countdown falls, the tiles
      update, the page does not jump. Leave Flights open until a detection lands.
      Watch the Health freshness age tick and the dot breathe. Stop the server and
      watch the dot stop and the neutral Reconnecting badge appear.
    - **Switches.** Flip Screen, Quiet hours and the LED on a phone-sized window.
      Stop the server and flip one: it comes back and says so, in French too.
    - **The floor.** With scripts blocked, at 360 px, in both languages: save a
      setting on Display, save one on Device, flip all three switches, open a
      Flights detail row, and read Home. Everything works; nothing renders a
      control that does nothing.
    - **Layout.** Hard-reload Home on a throttled connection and confirm nothing
      jumps when the picture arrives.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_browser_ux.py && PYTHON="$PY" JOBS=1 bash scripts/run-all-tests.sh && PYTHON="$PY" bash scripts/run-all-tests.sh && ruff check .</automated>
    <human-check>Walk the six grouped checklists above on a real phone and a desktop browser, in both languages. Report anything that moves when it should not, anything that does not move when it should, and anything that cannot be done with scripts blocked.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/run-all-tests.sh` green both serially and in parallel, with coverage at or above 83
    - `companion/test_browser_ux.py` reports N/N with no SKIP line — paste the last line of its output in the SUMMARY
    - Every harness's start and end `EXPECTED_CHECK_COUNT` recorded, with each delta attributed to the plan that claimed it; an unattributed delta is recorded as a finding
    - `ruff check .` clean
    - The six checklists are written out in the SUMMARY in walkable form
  </acceptance_criteria>
  <done>The phase is verified by machine where a machine can see it, and handed to a human where only a human can.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the design system → every future UI plan | a wrong row in the authority becomes a wrong implementation later |
| the coverage ledger → the developer's decisions | an omission recorded as "done" is a decision taken by silence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-40 | Tampering | a design-system row that contradicts the code | mitigate | every value read live at execution time and reproduced by a command in the SUMMARY; rows that cannot be written truthfully become findings, following 22-16's precedent |
| T-23-41 | Repudiation | a clause quietly dropped and reported as shipped | mitigate | the coverage ledger walks all seven D-items against ten SUMMARYs and records the walk; three not-built clauses are named with their reasons and the decisions they need |
| T-23-42 | Information Disclosure | none — this plan writes documentation only | accept | no code, no route, no runtime change |
| T-23-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `PYTHON=/home/user/skypane/server/.venv/bin/python JOBS=1 bash scripts/run-all-tests.sh` → green
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → green, coverage ≥ 83
- `companion/test_browser_ux.py` → N/N, NOT skipped
- `ruff check .` clean
- `<human-check>` → the six grouped checklists, walked in one pass
</verification>

<success_criteria>
- A motion contract in the design system, with real values read from the code
- The reduced-motion floor's first named exception, recorded with its mechanism
- The touch-target, sticky-header and save-bar entries updated in place
- A Folded-In Work entry for Phase 23 in the established format
- A coverage ledger naming all seven D-items, three not-built clauses with the decisions they need, and the three struck items with their grounds
- Every harness's count delta attributed to a plan
- The browser harness proven to have run
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/23-companion-dynamism-live-updates-real-switches-motion-budge/23-11-SUMMARY.md` when done.
</output>
