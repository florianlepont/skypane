---
phase: 06.3-companion-ui-visual-design-and-desktop-layout-pass
plan: 05
type: execute
wave: 3
depends_on: ["06.3-01", "06.3-02", "06.3-03", "06.3-04"]
files_modified:
  - .planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md
autonomous: true
requirements: [D-01, D-02, D-03, D-22]
user_setup: []

must_haves:
  truths:
    - "Every registered test harness in the repository passes together after all four implementation plans have landed, not just each plan's own file in isolation."
    - "06.3-VALIDATION.md's Per-Task Verification Map records the phase's actual plan/task breakdown and actual automated commands, so the validation contract is not a stale draft."
    - "The dashboard's visual correctness at 960px, 960-1200px, 1440px+ and phone width is confirmed in a real browser — the one thing this codebase's string-assertion harness structurally cannot verify."
    - "The Battery-trend tile is confirmed to stay inside its grid track and not force page-level horizontal scroll (the CSS Grid blowout risk min-width: 0 is there to prevent)."
    - "Keyboard tab order at desktop width is confirmed to reach the visible sidebar with no stops on the hidden nav copy (the duplicate-landmark risk display: none is there to prevent)."
  artifacts:
    - ".planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md (Per-Task Verification Map rewritten to the shipped breakdown, nyquist_compliant flipped)"
  key_links:
    - "scripts/run-all-tests.sh -> the four companion harnesses plus every server harness: the single command that proves the four implementation plans compose."
    - "The manual browser runbook -> 06.3-RESEARCH.md Pitfall 3: this project has no browser automation anywhere, so breakpoint behaviour is verified by a human or not at all."
    - "This plan's sign-off -> /gsd-verify-work: the phase gate this phase's core value depends on."
---

<objective>
Close the phase: prove the four implementation plans compose by running every registered harness together, bring `06.3-VALIDATION.md` in line with the breakdown that actually shipped, and carry out the browser-based visual sign-off that this codebase's test harness structurally cannot substitute for.

Purpose: this phase's entire value proposition — "does the dashboard actually look right at desktop width" — lives in rendered CSS behaviour. `06.3-RESEARCH.md`'s Pitfall 3 establishes that no browser automation exists anywhere in this repository (no `package.json`, no Playwright, no Selenium), and warns explicitly against writing a `check()` that greps the stylesheet for a breakpoint string and calls that coverage. The honest answer is a human at a real browser, with a specific, ordered list of things to look at.

Output: an updated `06.3-VALIDATION.md` and a recorded visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-CONTEXT.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-01-SUMMARY.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-02-SUMMARY.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-03-SUMMARY.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

Modified:
- `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md` — Per-Task Verification Map rewritten against the shipped plan/task IDs, `EXPECTED_CHECK_COUNT` deltas recorded, `nyquist_compliant` set to `true`, Validation Sign-Off checklist completed

No source file is modified by this plan. It produces no new symbol, route, or class name.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full suite and reconcile 06.3-VALIDATION.md with the shipped breakdown</name>

  <read_first>
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md` in full — the draft map to reconcile, and its Approval note stating the planner must confirm the task IDs match the actual PLAN.md breakdown before `nyquist_compliant` can be flipped
    - All four SUMMARY files (`06.3-01-SUMMARY.md` through `06.3-04-SUMMARY.md`) — the tasks that actually shipped and the final `EXPECTED_CHECK_COUNT` in each harness
    - `scripts/run-all-tests.sh` — the canonical harness list, to confirm all four companion test files are registered
  </read_first>

  <files>.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md</files>

  <action>
Run `scripts/run-all-tests.sh` and confirm it exits 0. This is the wave gate: it runs every registered harness including all four companion test files, so it proves the four implementation plans compose rather than each passing only against its own file.

If it fails, stop and fix the failure before touching `06.3-VALIDATION.md` — a validation contract marked compliant against a red suite is worse than a draft one.

Then rewrite `06.3-VALIDATION.md`'s **Per-Task Verification Map** table so its Task ID and Plan columns name the tasks that actually shipped, rather than the draft's three-plan guess. The shipped structure is five plans across three waves:

| Shipped task | Plan | Wave | Decision covered | Automated command |
|---|---|---|---|---|
| sidebar nav + shared `_nav_links()` | 01 | 1 | D-02 | `server/.venv/bin/python3 companion/test_companion_app.py` |
| `stat_tile()` builder | 01 | 1 | D-01/D-02 | same |
| `page_shell()` dashboard shell | 01 | 1 | D-02 | same |
| token refresh (`--color-border`, `--space-3xl`, revised hexes) | 02 | 1 | D-01 | same, plus the grep and brace-balance criteria in `06.3-02-PLAN.md` |
| stat-tile / dashboard-grid / page-section / sidebar-link CSS | 02 | 1 | D-01/D-02 | same |
| 960px breakpoint block | 02 | 1 | D-02/D-22 | same |
| History `.data-table-wrap` fix | 03 | 1 | D-03 | `server/.venv/bin/python3 companion/test_view_pages.py` |
| Config `.config-form` class hook | 03 | 1 | D-01 | `server/.venv/bin/python3 companion/test_config_page.py` |
| Health 4-tile dashboard grid | 04 | 2 | D-02 | `server/.venv/bin/python3 companion/test_status_pages.py` |
| Airlines 2-tile dashboard grid | 04 | 2 | D-02 | same |
| visual sign-off | 05 | 3 | D-01/D-02/D-03/D-22 | manual — no automated equivalent (Pitfall 3) |

Keep the table's existing column set (Task ID, Plan, Wave, Requirement, Threat Ref, Secure Behavior, Test Type, Automated Command, File Exists, Status) and fill every cell. Populate the Threat Ref column from the threat IDs the plans actually declared: `T-06.3-01` and `T-06.3-02` for the layout.py builders, `T-06.3-04` and `T-06.3-06`/`T-06.3-07` for the stylesheet, `T-06.3-08`/`T-06.3-10` for History, `T-06.3-09` for Config, `T-06.3-01`/`T-06.3-03`/`T-06.3-11` for the two page dashboards. Mark every automated row's Status green only if the corresponding harness is green in this run.

Update the **Wave 0 Requirements** section's parenthetical to record the actual `EXPECTED_CHECK_COUNT` deltas taken from the four SUMMARY files (`test_companion_app.py` 51 → 55, `test_view_pages.py` 19 → 20, `test_config_page.py` 15 → 16, `test_status_pages.py` 25 → 28), replacing the draft's "exact deltas depend on how many check() calls the plan adds" placeholder.

Add a row to the **Manual-Only Verifications** table for the keyboard tab-order check at desktop width, alongside the two rows already there. Its "Why Manual" is the same structural reason: this harness asserts markup strings and cannot observe focus order or which element CSS has removed from the accessibility tree.

Tick the final Validation Sign-Off checkbox and set `nyquist_compliant: true` in the front matter, replacing the Approval line's "pending" with a note that the map now matches the shipped five-plan breakdown. Do not flip `nyquist_compliant` if `scripts/run-all-tests.sh` did not exit 0.

Change no source file in this task.
  </action>

  <verify>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/run-all-tests.sh` exits 0.
    - `server/.venv/bin/python3 companion/test_companion_app.py`, `companion/test_config_page.py`, `companion/test_status_pages.py` and `companion/test_view_pages.py` each exit 0 at 55/55, 16/16, 28/28 and 20/20 respectively.
    - `test "$(grep -c 'nyquist_compliant: true' .planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md)" = "1"` succeeds.
    - `test "$(grep -c '| 05 |' .planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md)" -ge 1` succeeds — the map covers all five shipped plans, not the draft's three.
    - `git diff --name-only` for this task lists only `06.3-VALIDATION.md`.
  </acceptance_criteria>

  <done>The full suite is green and `06.3-VALIDATION.md` describes the breakdown that actually shipped, with `nyquist_compliant: true`.</done>
</task>

<task type="auto">
  <name>Task 2: Visual sign-off in a real browser at 900px / 960px / 1200px / 1440px+ / phone width (D-01, D-02, D-03, D-22)</name>

  <read_first>
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Layout Contract" and "Color" — the contract the eyes are checking against
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-RESEARCH.md` "Pitfall 1" (grid track blowout), "Pitfall 2" (focusable hidden duplicates) and "Pitfall 3" (why this step cannot be automated here)
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md` "Manual-Only Verifications" — the two rows already written plus the tab-order row added in Task 1
  </read_first>

  <files>.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-VALIDATION.md</files>

  <action>
Start the companion service locally so the developer has something to look at, using a throwaway password:

`SKYPANE_COMPANION_PASSWORD='local-visual-check' server/.venv/bin/python3 companion/app.py --port 8643`

It serves on `http://127.0.0.1:8643/`; sign in with that same password. Note in the summary that this is a local, disposable credential for the visual pass only and must not be committed anywhere or reused for the deployed service.

Present the developer with the ordered checklist in `<verify><human-check>` below, then record their verdict in the summary. Where a real database is not seeded locally, the pages will render their empty-state blocks — that is still a valid check of the tile frames, the grid, and the shell; note in the summary which checks were exercised against real data and which against empty states.

Do not attempt to substitute a scripted check for any item below. `06.3-RESEARCH.md`'s Pitfall 3 is explicit that a Python string assertion against the stylesheet tests only that a string exists in a file, not that a layout behaves — and there is no browser-automation dependency anywhere in this repository to fall back on. If the developer reports a defect, capture it verbatim in the summary as a follow-up rather than marking the phase visually signed off.
  </action>

  <verify>
    <automated>scripts/run-all-tests.sh</automated>
    <human-check>
Open `http://127.0.0.1:8643/` in a real browser, sign in, and work through these in order. Resize using the browser's own window or its responsive-design mode.

**A. Below the breakpoint — 900px wide**
1. The horizontal top nav bar is visible with all five tabs; no sidebar column is present anywhere.
2. The page is a single fluid column, visually the same layout as before this phase (colours will differ — that is D-01's refresh).

**B. At and above the breakpoint — 960px wide**
3. The horizontal top nav bar is gone entirely and a 240px sidebar has appeared on the left, with a hairline border separating it from the content.
4. The sidebar reads, top to bottom: the "SkyPane" title, the five nav links stacked vertically, and the Light/Dark/Auto theme buttons pinned at the bottom.
5. The current page's sidebar link carries an accent-coloured left border and accent text; the other four do not.
6. On **Health**: an "Overview" heading sits above a multi-column grid of four cards (Device / Pipeline / Battery / Corroboration), each with a coloured top border. Two or more cards sit side by side — not a single stacked column.
7. Each Health card's top-border colour agrees with the status dot inside it (green/amber/red, or the neutral accent where no status applies). A card claiming a healthy dot with a red border, or the reverse, is a defect.
8. On **Airlines**: a "Coverage" heading above two cards.
9. On **Config**: the Theme and Runway fieldsets sit side by side; "Save Settings" is a full-width row beneath them, not squeezed into a third grid cell.

**C. The narrow-desktop stress range — drag slowly from 960px to 1200px**
10. The Battery-trend card stays inside its own grid column at every width in this range. It must not push the other cards off the grid or widen its row.
11. The **page itself** never gains a horizontal scrollbar. A horizontal scrollbar *inside* the Battery card's own table area is correct and expected; one on the page is the grid-blowout defect.
12. The battery sparkline shrinks to fit its card rather than being clipped or overflowing it.

**D. Wide desktop — 1440px and wider, ideally 1920px**
13. The main content column stops growing and centres itself; content does not run edge-to-edge.
14. There is a clear gutter (roughly 64px) between the content and the window edge on the right.

**E. Phone width — around 390px**
15. The sidebar is completely absent; the top nav bar is back and scrolls horizontally when the five tabs do not fit.
16. On **History**: the wide flight table scrolls sideways *within its own area*. The page itself must not scroll sideways, and no column may be cropped off-screen with no way to reach it. This is D-03, the bug this phase closes — check it specifically, not just in passing.
17. Nav links, theme buttons, form inputs and the Save button are all comfortably tappable (roughly a fingertip tall, ~44px).

**F. Keyboard and theme, at 1440px**
18. Click into the address bar, then press Tab repeatedly into the page. Focus must land on a **visible** sidebar link first. If focus disappears — an outline moving somewhere invisible before reaching the sidebar — that is the hidden-duplicate-nav defect.
19. Every focused link and button shows a visible focus outline.
20. Click Dark, then Light, then Auto in the sidebar's theme toggle. Each applies immediately and persists across a page navigation.
21. Repeat step 20 at phone width using the header's theme toggle — both copies must work identically, since both post to the same endpoint.

**G. Overall visual judgement (D-01)**
22. In **both** light and dark mode: body text is comfortably readable against the new page background, card borders are visible but not heavy, and the accent colour reads as an accent rather than shouting.
23. The Health cards' captions repeat the wording of the status-dot label inside them (for Device check-in and ADS-B pipeline). This follows the approved UI-SPEC's copy contract literally. Confirm it reads acceptably; if the repetition looks redundant on screen, say so and it will be captured as a follow-up rather than silently changed.
24. The page still reads as the SkyPane companion tool — simple title, no logo, five pages, English copy (D-23/D-24/D-25).

Reply `approved` if all of A-G hold, or describe each defect with the viewport width and page where you saw it.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `scripts/run-all-tests.sh` exits 0 immediately before the browser pass, so what the developer looks at is the same code the suite certified.
    - The developer has been shown all seven groups A-G and has responded with either `approved` or a specific defect list.
    - The summary records the verdict verbatim, states which pages were exercised against seeded data versus empty states, and lists any defect as a named follow-up.
    - Items 10, 11 and 18 are answered explicitly, since they are the two rendering-only failure modes (`min-width: 0` grid blowout, `display: none` hidden-duplicate focus) that no automated check in this repository can reach.
    - No source file is modified by this task.
  </acceptance_criteria>

  <done>The dashboard shell, stat-tile grid, Config two-column form, History table scroll and keyboard tab order are all confirmed in a real browser across the five named viewport widths, and the verdict is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local operator → locally-launched `companion/app.py` on 127.0.0.1 | A throwaway password is set in the launching shell's environment for the duration of the visual pass. The service binds locally and is stopped afterwards; no deployed service, credential store, or committed file is involved. |

This plan modifies no source file and changes no runtime behaviour. Its only artifact is a planning document.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-12 | Information Disclosure | The throwaway `SKYPANE_COMPANION_PASSWORD` used to launch the app for the visual pass | medium | mitigate | The value is supplied inline on the launch command for one local session, never written to `deploy/skypane.env`, never committed, and never reused for the deployed service — recorded explicitly in the summary. The service binds to the local port only for the duration of the check and is stopped afterwards. |
| T-06.3-13 | Repudiation | Marking the phase visually signed off without the checks actually being performed | high | mitigate | The verdict must be recorded verbatim in the summary, and items 10, 11 and 18 must be answered individually because they are the only evidence that exists for the two rendering-only failure modes. `nyquist_compliant` may not be flipped while the suite is red, and a reported defect is captured as a follow-up rather than being marked approved. |
| T-06.3-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No package-manager install occurs anywhere in this phase — `06.3-RESEARCH.md`'s Package Legitimacy Audit records it as installing zero external packages. This plan runs only in-repo scripts against the existing `server/.venv`. No legitimacy checkpoint is required. |

Categories not applicable: Spoofing, Tampering (beyond supply chain), Denial of Service, Elevation of Privilege — no code, no route, no data path and no privilege boundary changes here.
</threat_model>

<verification>
1. `scripts/run-all-tests.sh` exits 0.
2. `06.3-VALIDATION.md` has `nyquist_compliant: true` and a Per-Task Verification Map covering all five shipped plans with real automated commands.
3. The developer's browser verdict is recorded verbatim in `06.3-05-SUMMARY.md`, with items 10, 11 and 18 answered explicitly.
4. `git status` shows no modified source file attributable to this plan.
</verification>

<success_criteria>
- Every registered test harness passes together after all four implementation plans have landed.
- `06.3-VALIDATION.md` matches the shipped five-plan, three-wave breakdown and is marked Nyquist-compliant.
- The visual sign-off has been performed at 900px, 960px, the 960-1200px stress range, 1440px+ and phone width, and its verdict is recorded.
- The grid-blowout and hidden-duplicate-focus risks are each explicitly answered rather than assumed clean.
</success_criteria>

<output>
Create `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-05-SUMMARY.md` when done.
</output>
