---
phase: 06.3-companion-ui-visual-design-and-desktop-layout-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/layout.py
  - companion/test_companion_app.py
autonomous: true
requirements: [D-01, D-02, D-24, D-25]
user_setup: []

must_haves:
  truths:
    - "Every authenticated page's HTML contains a vertical sidebar navigation landmark carrying all five NAV_TABS routes, marked with aria-label=\"Primary navigation\" (D-02)."
    - "Exactly one sidebar link is marked active per request, matching the `active` slug the router already passes to page_shell()."
    - "A stat_tile() builder wraps already-built section markup in a status-coloured card without altering that markup by a single byte (status_dot()'s raw <span> survives intact)."
    - "stat_tile()'s own caption parameter is HTML-escaped, so a hostile caption cannot inject markup."
    - "Both the existing horizontal nav-bar and the new sidebar render into every response's DOM, wrapped in a single .dashboard-shell container, so CSS alone decides which is visible."
    - "Both theme-toggle form copies post to the unchanged /ui-theme action with the unchanged ui_theme field name, so companion/app.py's existing POST handler keeps working."
    - "The site title stays the plain \"SkyPane\" string in both nav copies — no logo, no branding markup (D-24)."
  artifacts:
    - "companion/layout.py::_nav_links"
    - "companion/layout.py::sidebar_nav"
    - "companion/layout.py::stat_tile"
    - "companion/layout.py::_STAT_TILE_BORDER_CLASSES"
    - "companion/layout.py::_DEFAULT_STAT_TILE_CLASS"
    - "companion/test_companion_app.py (4 new check() calls, EXPECTED_CHECK_COUNT 51 -> 55)"
  key_links:
    - "page_shell() -> sidebar_nav(active): the single choke point where the sidebar reaches all five pages, using the same `active` slug _nav_html() already consumes."
    - "_nav_links() -> both _nav_html() and sidebar_nav(): one escaping/active-state implementation feeding two renderers, so NAV_TABS is never iterated twice with drifting logic."
    - "stat_tile(content_html) -> health_page/airlines_page section builders (plan 06.3-04): the raw-markup pass-through contract those pages depend on."
    - "CSS class names emitted here -> companion/static/style.css rules added by plan 06.3-02: the two plans run in the same wave and must agree exactly (see <interface_contract>)."
---

<objective>
Add the two new `companion/layout.py` component builders this phase's dashboard depends on — `sidebar_nav()` and `stat_tile()` — and extend `page_shell()` into a `.dashboard-shell` container that renders both the existing horizontal nav and the new vertical sidebar into every response.

Purpose: `page_shell()` is the single place that assembles page chrome (every page module returns body-only markup per `companion/pages/__init__.py`'s contract), so D-02's sidebar navigation has exactly one integration point instead of five. `stat_tile()` is the card wrapper that plan 06.3-04 needs before Health and Airlines can become real dashboards.

Output: three new/reworked functions in `companion/layout.py`, an extended `page_shell()`, and four new `check()` calls in `companion/test_companion_app.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-CONTEXT.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-PATTERNS.md
@companion/layout.py
</context>

<interface_contract>
## CSS class names this plan emits (plan 06.3-02 styles exactly these)

Plan 06.3-02 runs in the same wave and writes `companion/static/style.css` against this list. Neither plan may rename a class without the other. Emit these names verbatim:

| Element | Class attribute emitted here |
|---------|------------------------------|
| Outer shell wrapper (wraps header + sidebar + main) | `dashboard-shell` |
| Desktop sidebar column | `dashboard-sidebar` |
| Sidebar site title | `site-title sidebar-title` |
| Sidebar nav landmark | `sidebar-nav` (on a `<nav>` with `aria-label="Primary navigation"`) |
| Sidebar link, inactive | `sidebar-link` |
| Sidebar link, active | `sidebar-link sidebar-link--active` |
| Main content column | `page-content dashboard-main` |
| Stat tile card, neutral status | `stat-tile stat-tile--accent` |
| Stat tile card, ok / warn / error | `stat-tile stat-tile--ok` / `stat-tile stat-tile--warn` / `stat-tile stat-tile--error` |
| Stat tile caption line | `text-label stat-tile__caption` |

Unchanged and still emitted exactly as today: `site-header`, `site-title`, `nav-bar`, `nav-tab`, `nav-tab--active`, `theme-form`, `theme-option`, `theme-option--active`.

Two other classes are emitted by sibling plans against the same stylesheet: `dashboard-grid` and `stat-tile__value` (plan 06.3-04), `config-form` (plan 06.3-03).
</interface_contract>

<artifacts_this_phase_produces>
## Artifacts this plan produces

New symbols in `companion/layout.py`:
- `_nav_links(active)` — private helper returning `(is_active, escaped_route, escaped_label)` tuples for every `NAV_TABS` entry
- `sidebar_nav(active)` — public builder returning the vertical `<nav aria-label="Primary navigation">` block
- `stat_tile(caption, content_html, status=None)` — public builder returning a status-classed card `<div>`
- `_STAT_TILE_BORDER_CLASSES` — module constant mapping `"ok"`/`"warn"`/`"error"` to CSS class suffixes
- `_DEFAULT_STAT_TILE_CLASS` — module constant holding the accent-neutral fallback class

Reworked (signature unchanged, output changed):
- `page_shell(title, active, body, ui_theme="auto", flash=None, banner=None)` — now wraps its `<header>` + `<main>` in `<div class="dashboard-shell">` and prepends an `<aside class="dashboard-sidebar">`

Reworked (signature and output byte-identical):
- `_nav_html(active)` — now consumes `_nav_links()` instead of iterating `NAV_TABS` itself

New CSS class names entering the DOM (styled by plan 06.3-02): `dashboard-shell`, `dashboard-sidebar`, `sidebar-title`, `sidebar-nav`, `sidebar-link`, `sidebar-link--active`, `dashboard-main`, `stat-tile`, `stat-tile--ok`, `stat-tile--warn`, `stat-tile--error`, `stat-tile--accent`, `stat-tile__caption`

Test file changes: `companion/test_companion_app.py` — `EXPECTED_CHECK_COUNT` 51 → 55, four new `check()` calls in Section 2.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _nav_links() and add sidebar_nav() (D-02, D-25)</name>

  <read_first>
    - `companion/layout.py` lines 20-31 (`SITE_TITLE`, `NAV_TABS`) and lines 43-80 (`escape_html()`, `_nav_html()`) — the exact loop shape and escaping discipline to preserve
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-PATTERNS.md` section "`companion/layout.py` — add `sidebar_nav()`" — the analog and the "no duplicated route list" requirement
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Component Patterns" table, "Sidebar nav (new, ≥960px only)" row
    - `companion/test_companion_app.py` lines 48-63 (module constants incl. `EXPECTED_CHECK_COUNT`) and lines 503-530 (two adjacent `check()` calls — the exact harness shape to copy)
  </read_first>

  <files>companion/layout.py, companion/test_companion_app.py</files>

  <behavior>
    - `_nav_links("health")` returns 5 tuples, one per `NAV_TABS` entry, in `NAV_TABS` order, with exactly one tuple whose first element is True.
    - `_nav_links("")` returns 5 tuples with zero active entries.
    - `sidebar_nav("history")` output contains `aria-label="Primary navigation"`, all five hrefs (`/config`, `/health`, `/airlines`, `/history`, `/preview`), all five labels, and exactly one occurrence of `sidebar-link--active`.
    - `sidebar_nav("<script>x</script>")` matches no tab, so the output contains zero `sidebar-link--active` occurrences and no raw `<script>` substring.
    - `_nav_html("health")` output is byte-identical to its pre-change output (the refactor must not alter the horizontal nav at all).
  </behavior>

  <action>
Insert a new private helper `_nav_links(active)` in `companion/layout.py` immediately above the existing `_nav_html()` (currently line 72). It returns a list of `(is_active, escaped_route, escaped_label)` tuples built by iterating the existing `NAV_TABS` constant — one tuple per entry, in `NAV_TABS` order. `is_active` is `route.lstrip("/") == active` (the exact comparison `_nav_html()` uses today). `escaped_route` is `escape_html(route)`; `escaped_label` is `escape_html(label)`. All escaping for both nav renderers now lives in this one function, per `06.3-RESEARCH.md`'s Anti-Patterns entry on duplicated `NAV_TABS` iteration logic.

Rewrite `_nav_html(active)` to consume `_nav_links(active)`: for each tuple, pick `"nav-tab nav-tab--active"` when `is_active` else `"nav-tab"`, format `'<a class="%s" href="%s">%s</a>'` with the already-escaped route and label, and join with `"\n"` exactly as today. This refactor must be output-preserving — do not change the class strings, the element order, or the join separator.

Add a new public builder `sidebar_nav(active)` after `_nav_html()`. It consumes the same `_nav_links(active)`, picks `"sidebar-link sidebar-link--active"` when `is_active` else `"sidebar-link"`, formats the same `'<a class="%s" href="%s">%s</a>'` anchor shape, joins with `""` (no separator — the sidebar stacks via CSS flex-direction, not whitespace), and returns the joined links wrapped in `'<nav class="sidebar-nav" aria-label="Primary navigation">%s</nav>'`. The `aria-label` value is exactly `Primary navigation` per `06.3-UI-SPEC.md`'s Copywriting Contract row "Sidebar nav accessible label" — it is a screen-reader-only string, not visible copy, and stays English (D-23). Give `sidebar_nav()` a docstring noting it renders the same `NAV_TABS` route set as `_nav_html()` in a vertical arrangement, and that `companion/static/style.css` decides which copy is visible at a given viewport width.

Do not add an icon glyph, an SVG, or any image to the sidebar links — `06.3-UI-SPEC.md`'s Design System row "Icon library" specifies text labels only, consistent with D-24's no-logo/no-elaborate-branding constraint.

In `companion/test_companion_app.py` Section 2 (the `companion/layout.py` unit checks, immediately after the existing `_data_table_wrapped_for_horizontal_scroll()` check that ends at line 530), add two new `check()` calls following that file's exact `def _name(): ... return True, ""` / `check("description", _name)` shape:
1. A check that `layout.sidebar_nav("history")` contains `aria-label="Primary navigation"`, contains every `route` and every `label` from `layout.NAV_TABS`, and that its `.count("sidebar-link--active")` equals 1.
2. A check that `layout.sidebar_nav("<script>alert(1)</script>")` contains no `<script>` substring and that its `.count("sidebar-link--active")` equals 0.

Raise `EXPECTED_CHECK_COUNT` (line 63) from 51 to 53 for these two checks. Task 2 and Task 3 in this plan raise it further; the final value at the end of this plan is 55.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
    - `companion/test_companion_app.py` exits 0 with 53 of 53 checks passed.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion import layout; assert layout.sidebar_nav('health').count('sidebar-link--active') == 1; assert 'aria-label=\"Primary navigation\"' in layout.sidebar_nav('health'); print('ok')"` prints `ok`.
    - `test "$(grep -c 'for route, label in NAV_TABS' companion/layout.py)" = "1"` succeeds — `NAV_TABS` is iterated in exactly one place (`_nav_links()`), not once per renderer.
    - `test "$(grep -c 'sidebar-link' companion/layout.py)" -ge 2` succeeds.
  </acceptance_criteria>

  <done>`sidebar_nav()` exists, emits the Primary-navigation landmark with all five NAV_TABS links and exactly one active link, escapes through the shared `_nav_links()` helper, and `_nav_html()`'s output is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stat_tile() with never-re-escape content passthrough (D-01, D-02)</name>

  <read_first>
    - `companion/pages/health_page.py` lines 334-344 (`_unavailable_block()`, `_section()`) — the exact raw-`content_html` convention `stat_tile()` must replicate
    - `companion/layout.py` lines 35-40 (`_STATUS_DOT_CLASSES`, `_DEFAULT_STATUS_DOT_CLASS`) and lines 151-174 (`status_dot()`, `empty_state()`) — the fixed-class-lookup-with-fallback pattern to mirror
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-PATTERNS.md` section "`companion/layout.py` — add `stat_tile()`", including its anti-pattern warning
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Component Patterns" table, "Stat tile (new)" row
  </read_first>

  <files>companion/layout.py, companion/test_companion_app.py</files>

  <behavior>
    - `stat_tile("Battery trend", "<p>x</p>", "ok")` output contains `stat-tile--ok` and the substring `<p>x</p>` unchanged.
    - `stat_tile("c", "x", "warn")` contains `stat-tile--warn`; `status="error"` contains `stat-tile--error`.
    - `stat_tile("c", "x")` and `stat_tile("c", "x", "not-a-real-state")` both contain `stat-tile--accent` (the neutral fallback).
    - `stat_tile("<b>hi</b>", "x")` output contains no `<b>` substring — the caption is escaped.
    - `stat_tile("c", layout.status_dot("ok", "All good"))` output still contains `dot--ok` and does not contain the double-encoded form of that span.
  </behavior>

  <action>
Add a module-level constant `_STAT_TILE_BORDER_CLASSES` to `companion/layout.py`, placed directly beneath the existing `_DEFAULT_STATUS_DOT_CLASS` (line 40), mapping `"ok" -> "stat-tile--ok"`, `"warn" -> "stat-tile--warn"`, `"error" -> "stat-tile--error"`, plus a companion constant `_DEFAULT_STAT_TILE_CLASS = "stat-tile--accent"`. This mirrors the fixed-class-lookup discipline `_STATUS_DOT_CLASSES` already establishes: an unrecognised or `None` status resolves to a known class name, never an attacker-influenceable one interpolated from the caller's value.

Add the public builder `stat_tile(caption, content_html, status=None)` after `status_dot()` and before `empty_state()`. It computes `css_class = "stat-tile " + _STAT_TILE_BORDER_CLASSES.get(status, _DEFAULT_STAT_TILE_CLASS)` and returns:

- an outer `<div>` carrying `class="%s"` with that computed class,
- a caption line `<p class="text-label stat-tile__caption">%s</p>` fed `escape_html(caption)`,
- then `content_html` interpolated verbatim,
- then the closing `</div>`.

The `content_html` parameter is interpolated with **no** call to `escape_html()` and no other transformation. This is deliberate and load-bearing: every caller passes pre-built markup that already escaped its own dynamic parts — `layout.status_dot()`'s raw `<span class="dot ...">` pair, `layout.data_table()`'s table, `layout.empty_state()`'s block, or hand-built `<p>` strings from `health_page.py`. Re-encoding it would print the tag characters as visible text instead of rendering a coloured dot, which is exactly the failure `06.3-CONTEXT.md`'s canonical refs flag and the same reason `history_page.py` could never reuse `data_table()` directly. Document this in the docstring: `caption` is escaped here; `content_html` is the caller's already-safe markup and is never touched. Use the same wording style as `page_shell()`'s existing docstring, which already documents the identical split for its `body`/`flash`/`banner` parameters.

Do not add a `<h2>` heading to the tile. `06.3-UI-SPEC.md`'s Typography section assigns Heading (20/600) to the dashboard *group* label above the grid, and Label (14/400) to the tile's own caption line — the tile caption is a `<p class="text-label ...">`, not a heading, so the page keeps exactly one heading level per group instead of four sibling `<h2>`s.

In `companion/test_companion_app.py` Section 2, immediately after Task 1's two new checks, add one `check()` call verifying all of: the `"ok"`/`"warn"`/`"error"` status-to-class mapping; that both `status=None` and an unrecognised status string resolve to `stat-tile--accent`; that a caption containing `<b>hi</b>` emits no `<b>` substring; and that `stat_tile("Device", layout.status_dot("ok", "All good"))` still contains `dot--ok` (proving `content_html` reached the output unmodified).

Raise `EXPECTED_CHECK_COUNT` from 53 to 54.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
    - `companion/test_companion_app.py` exits 0 with 54 of 54 checks passed.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion import layout; t = layout.stat_tile('Device', layout.status_dot('ok','All good'), 'ok'); assert 'dot--ok' in t and 'stat-tile--ok' in t; assert '<b>' not in layout.stat_tile('<b>x</b>','y'); assert 'stat-tile--accent' in layout.stat_tile('c','y'); print('ok')"` prints `ok`.
    - `test "$(grep -c 'escape_html(content_html)' companion/layout.py)" = "0"` succeeds — the content parameter is never routed through the escaper.
    - `test "$(grep -c 'stat-tile--accent' companion/layout.py)" -ge 1` succeeds.
  </acceptance_criteria>

  <done>`stat_tile()` exists in `companion/layout.py`, escapes its caption, maps the four status states to fixed class names with an accent-neutral fallback, and passes `content_html` through byte-for-byte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap page_shell() in the dashboard shell with a sidebar column (D-02, D-24, D-25)</name>

  <read_first>
    - `companion/layout.py` lines 83-138 (`_theme_form_html()`, `page_shell()`) — the exact document string to extend, not replace
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-RESEARCH.md` "Pattern 1: Single-choke-point page shell extension" and the "Anti-Patterns to Avoid" list (same-action/same-field-name requirement for the duplicate theme form)
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Layout Contract" → "Dashboard shell (≥960px only)" — sidebar contents and their top-to-bottom order
    - `companion/test_companion_app.py` lines 532-540 (`_page_shell_escapes_hostile_body()`) — the existing page_shell check whose contract must keep holding
  </read_first>

  <files>companion/layout.py, companion/test_companion_app.py</files>

  <behavior>
    - `page_shell("Health", "health", "<p>b</p>")` output contains `<div class="dashboard-shell">`, `<aside class="dashboard-sidebar">`, `<header class="site-header">`, and `<main class="page-content dashboard-main">`.
    - That same output contains `aria-label="Primary navigation"` exactly once and `class="nav-bar"` exactly once — both nav copies present, neither duplicated.
    - It contains `action="/ui-theme"` exactly twice and `name="ui_theme"` six times (three theme options in each of the two form copies).
    - It contains the literal `SkyPane` twice in the body region (header title and sidebar title) and no `<img`/`<svg` branding element.
    - The existing hostile-body check still passes: an escaped hostile body produces no unescaped `<script>` tag.
  </behavior>

  <action>
Extend `page_shell()` in `companion/layout.py` (currently lines 97-138). Keep the signature, the `resolved_theme` membership guard, the `<!DOCTYPE html>`/`<head>` block, and every existing `escape_html()` call exactly as they are. Change only the `<body>` region's structure, per `06.3-RESEARCH.md`'s Pattern 1.

Compute `sidebar_html = sidebar_nav(active)` alongside the existing `nav_html` / `theme_form_html` locals. Then restructure the body so it reads, in this exact DOM order:

1. `<body>`
2. `<div class="dashboard-shell">` — the new outer wrapper
3. `<aside class="dashboard-sidebar">` containing, top to bottom per `06.3-UI-SPEC.md`'s Dashboard-shell contract: a `<span class="site-title sidebar-title">` holding `escape_html(SITE_TITLE)`, then `sidebar_html`, then a second `theme_form_html` copy
4. `</aside>`
5. The existing `<header class="site-header">` block, unchanged: `<span class="site-title">`, `<nav class="nav-bar">`, and the first `theme_form_html` copy
6. `</header>`
7. `<main class="page-content dashboard-main">` — the existing `page-content` class stays, `dashboard-main` is added alongside it so the mobile padding rule keeps applying and the desktop max-width/centering rule has its own hook
8. The unchanged `flash_html`, `banner_html`, `body` interpolations
9. `</main>`, `</div>`, `</body>`, `</html>`

The `<aside>` deliberately precedes the `<header>` in source order so that at desktop width — where CSS hides the header entirely — a keyboard user tabs into the visible sidebar navigation first, with no invisible stops before it.

Both theme-form copies come from the same `_theme_form_html(resolved_theme)` call result, so both post to `action="/ui-theme"` with `name="ui_theme"` and identical option values. Do not give either copy a distinct action, field name, or id — `companion/app.py`'s existing `/ui-theme` POST handler is unchanged by this phase and must keep working regardless of which copy the user submitted (`06.3-RESEARCH.md` Anti-Patterns, third bullet). Reusing the one computed string, rather than calling the helper twice, makes that impossible to break by accident.

Both nav copies and both theme forms are always present in the DOM; nothing here decides visibility. That decision belongs entirely to `companion/static/style.css`'s new `@media (min-width: 960px)` block (plan 06.3-02), which uses `display: none` on the losing copy so the hidden one leaves both the tab order and the accessibility tree — the duplicate-landmark mitigation. Do not add inline styles, an HTML boolean-hidden attribute, an ARIA visibility-state attribute, or any server-side viewport guess to this function: no viewport information exists at render time in a stdlib HTTP server, and an ARIA-level hint would conflict with the CSS-driven state rather than reinforce it.

Keep the sidebar title as the plain `SITE_TITLE` string in a `<span>` (D-24: a simple "SkyPane" title, no logo, no elaborate branding, even under the fuller visual refresh). Add no image, SVG, or icon element anywhere in the shell.

In `companion/test_companion_app.py` Section 2, immediately after Task 2's check and before the existing `_page_shell_escapes_hostile_body()` check, add one `check()` call asserting on a single `layout.page_shell(title="Health", active="health", body="<p>b</p>")` render: that it contains each of `<div class="dashboard-shell">`, `<aside class="dashboard-sidebar">`, `<main class="page-content dashboard-main">`; that `.count('aria-label="Primary navigation"')` equals 1 and `.count('class="nav-bar"')` equals 1; and that `.count('action="/ui-theme"')` equals 2 (both theme-form copies present and pointing at the one unchanged endpoint).

Raise `EXPECTED_CHECK_COUNT` from 54 to 55 — the plan's final value.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
    - `companion/test_companion_app.py` exits 0 with 55 of 55 checks passed. This harness's Section 3 launches `companion/app.py` as a real subprocess and drives all nine authenticated routes, so a shell that fails to render on any page fails this command.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion import layout; h = layout.page_shell('Health','health','<p>b</p>'); assert h.count('aria-label=\"Primary navigation\"') == 1; assert h.count('class=\"nav-bar\"') == 1; assert h.count('action=\"/ui-theme\"') == 2; assert '<div class=\"dashboard-shell\">' in h; print('ok')"` prints `ok`.
    - `test "$(grep -c 'aria-hidden' companion/layout.py)" = "0"` succeeds — visibility is CSS's job, not the shell's.
    - `test "$(grep -c '<img\|<svg' companion/layout.py)" = "0"` succeeds — D-24's no-logo constraint holds.
  </acceptance_criteria>

  <done>Every page renders inside `<div class="dashboard-shell">` with a sidebar column (title, Primary-navigation landmark, theme form) preceding the untouched header, and the main column carries both `page-content` and `dashboard-main`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADS-B upstream / SQLite `history.db` → server-rendered HTML | Callsigns, hex codes, airline names and route strings originate from third-party aggregators, are persisted, then interpolated into markup by `companion/layout.py`'s builders. This is the only untrusted-data boundary this plan touches. |
| Browser → `companion/app.py` POST `/ui-theme` | Unchanged by this plan. A second theme-form copy now posts to the same endpoint with the same field name and the same three option values; no new endpoint, parameter, or accepted value is introduced. |

No new trust boundary is created: this plan adds no route, no request parameter, no auth surface, and no client-side script.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-01 | Tampering (stored XSS) | `layout.stat_tile()`'s `caption` parameter | high | mitigate | `caption` is routed through the module's single `escape_html()` choke point (Task 2). Regression-asserted by the new `check()` feeding `<b>hi</b>` as a caption and requiring no `<b>` in the output. |
| T-06.3-02 | Tampering (stored XSS) | `layout.sidebar_nav()`'s route/label interpolation | medium | mitigate | Both values are escaped once inside the shared `_nav_links()` helper (Task 1), so the sidebar cannot diverge from `_nav_html()`'s already-audited escaping. The `active` argument is only ever string-compared, never interpolated into markup — asserted by the `sidebar_nav("<script>alert(1)</script>")` check. |
| T-06.3-03 | Tampering (output-encoding correctness) | `stat_tile()`'s `content_html` passthrough | low | mitigate | `content_html` is never re-encoded (Task 2). Double-encoding would be a rendering defect rather than a vulnerability, but it is asserted directly: `stat_tile("Device", status_dot("ok","All good"))` must still contain `dot--ok`. The negative direction — that no *unescaped* caller value reaches this parameter — remains guaranteed by `companion/pages/__init__.py`'s existing single-escaper contract, which this plan does not alter. |
| T-06.3-04 | Denial of Service (assistive-technology access, not availability) | Duplicate nav landmark and theme-form controls rendered in every response by the extended `page_shell()` | medium | mitigate | Both copies are always in the DOM because no viewport information exists server-side. The mitigation lives in plan 06.3-02's CSS: the losing copy is hidden with `display: none`, which removes it from both the tab order and the accessibility tree — never `visibility: hidden` or `opacity: 0`, which leave duplicate landmarks focusable. This plan's contribution is structural: the `<aside>` precedes the `<header>` in source order so the desktop-visible sidebar is reached first, and no `aria-hidden` attribute is emitted that could conflict with the CSS-driven state. Verified at 960px/1440px/mobile in plan 06.3-05's keyboard-tab check. |
| T-06.3-05 | Spoofing / Tampering | Second theme-toggle `<form>` posting to `/ui-theme` | low | accept | Both copies are the same computed string: identical `action`, identical `name="ui_theme"`, identical option values drawn from `UI_THEME_CHOICES`. The server-side control is unchanged — `layout.ui_theme_from_cookie()` and `companion/app.py`'s handler already validate membership before use. Accepted because the duplicate introduces no value the single form could not already submit. |
| T-06.3-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this plan or anywhere in phase 06.3 — `06.3-RESEARCH.md`'s Package Legitimacy Audit records the phase as installing zero external packages. `companion/layout.py` imports stdlib `html` plus one in-repo constant, unchanged. No legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 with 55/55 checks.
2. `companion/layout.py` still imports nothing beyond stdlib `html` and `companion.auth.UI_THEME_COOKIE_NAME` — verify with `grep -c '^import\|^from' companion/layout.py` returning 2.
3. `NAV_TABS` is iterated in exactly one function; both nav renderers consume `_nav_links()`.
4. No new file is created; only `companion/layout.py` and `companion/test_companion_app.py` change.
</verification>

<success_criteria>
- `sidebar_nav()`, `stat_tile()`, `_nav_links()`, `_STAT_TILE_BORDER_CLASSES`, and `_DEFAULT_STAT_TILE_CLASS` all exist in `companion/layout.py`.
- `page_shell()` emits the `.dashboard-shell` wrapper, the `.dashboard-sidebar` aside, and `page-content dashboard-main` on `<main>`.
- Every CSS class name in this plan's `<interface_contract>` table appears verbatim in `page_shell()`/`sidebar_nav()`/`stat_tile()` output.
- `companion/test_companion_app.py` passes 55/55, including its real-subprocess route sweep.
</success_criteria>

<output>
Create `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-01-SUMMARY.md` when done.
</output>
