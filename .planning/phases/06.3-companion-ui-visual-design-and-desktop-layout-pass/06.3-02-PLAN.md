---
phase: 06.3-companion-ui-visual-design-and-desktop-layout-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/static/style.css
autonomous: true
requirements: [D-01, D-02, D-22]
user_setup: []

must_haves:
  truths:
    - "The companion app's page background, nav/sidebar background, accent and border colours all come from revised token values in both light and dark mode — the visual refresh is a token-value change, not a per-rule rewrite (D-01)."
    - "A --color-border token exists in all four theme declaration sites, so card outlines and the sidebar divider follow the light/dark toggle like every other colour."
    - "--space-3xl (64px) is declared and genuinely consumed as the desktop main-column side gutter — it is no longer a reserved, unused token."
    - "At 960px and above the page becomes a two-column CSS Grid: a 240px sidebar with a 1px border divider, and a main column capped at 1440px and horizontally centred."
    - "At 960px and above the horizontal .site-header is removed with display: none, so the sidebar is the only navigation landmark present in the accessibility tree and the only one in the tab order (D-02)."
    - "Below 960px nothing in the new breakpoint applies: the page renders as a fluid single column with the top .nav-bar, whose own 480px horizontal-scroll fallback is untouched (D-22)."
    - "A .stat-tile grid item can shrink below the intrinsic width of a nested data table, so the Battery-trend tile cannot force its dashboard-grid track past the 240px floor."
    - "Sidebar links keep a 44px minimum tap target at every width (D-22)."
  artifacts:
    - "companion/static/style.css::--color-border (4 declaration sites)"
    - "companion/static/style.css::--space-3xl"
    - "companion/static/style.css::.dashboard-sidebar, .sidebar-nav, .sidebar-link, .sidebar-link--active"
    - "companion/static/style.css::.stat-tile, .stat-tile--ok, .stat-tile--warn, .stat-tile--error, .stat-tile--accent, .stat-tile__caption, .stat-tile__value"
    - "companion/static/style.css::.dashboard-grid"
    - "companion/static/style.css::.page-section"
    - "companion/static/style.css::@media (min-width: 960px) block (.dashboard-shell, .dashboard-main, .config-form)"
  key_links:
    - "Token values -> every existing rule that already reads var(--color-dominant)/var(--color-secondary)/var(--color-accent): the refresh propagates without touching those rules."
    - ".stat-tile { min-width: 0 } -> .data-table-wrap { overflow-x: auto } (existing, line 322): the wrap absorbs table width instead of the grid track growing."
    - "@media (min-width: 960px) display: none on .site-header -> companion/layout.py's always-rendered duplicate nav (plan 06.3-01): CSS is the only thing choosing between the two copies."
    - "Class names styled here -> the exact names emitted by plans 06.3-01 (shell/sidebar/tile), 06.3-03 (.config-form) and 06.3-04 (.dashboard-grid, .stat-tile__value)."
---

<objective>
Deliver D-01's visual refresh and D-02's desktop dashboard entirely inside `companion/static/style.css`: revised colour/spacing tokens, the new card and grid component rules, and the single new 960px breakpoint that turns the always-rendered dashboard markup into an actual two-column layout.

Purpose: this stylesheet is the one place the app's visual identity lives. Because every existing rule already reads its colours from custom properties, changing token *values* refreshes the whole app without rewriting rules — and the new 960px block is the only thing standing between the markup plan 06.3-01 emits and a real dashboard.

Output: one modified file, `companion/static/style.css`, with no new external reference of any kind — no CSS url function, no custom font-face declaration, no remote asset. That constraint is stated in this file's own header comment today and stays true after this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-CONTEXT.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md
@.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-PATTERNS.md
@companion/static/style.css
</context>

<interface_contract>
## Class names this stylesheet must target

These are emitted by sibling plans in the same or a later wave. Do not rename, and do not invent additional class names beyond this list plus the ones already in the file.

| Class | Emitted by |
|-------|-----------|
| `dashboard-shell`, `dashboard-sidebar`, `sidebar-title`, `sidebar-nav`, `sidebar-link`, `sidebar-link--active`, `dashboard-main` | plan 06.3-01 (`companion/layout.py::page_shell`, `::sidebar_nav`) |
| `stat-tile`, `stat-tile--ok`, `stat-tile--warn`, `stat-tile--error`, `stat-tile--accent`, `stat-tile__caption` | plan 06.3-01 (`companion/layout.py::stat_tile`) |
| `dashboard-grid`, `stat-tile__value` | plan 06.3-04 (`health_page.py`, `airlines_page.py`) |
| `config-form` | plan 06.3-03 (`config_page.py`) |

Already in the file and unchanged: `site-header`, `site-title`, `nav-bar`, `nav-tab`, `nav-tab--active`, `theme-form`, `theme-option`, `theme-option--active`, `page-content`, `page-section`, `data-table-wrap`, `data-table`, `dot`, `dot-label`, `banner`, `empty-state`, `gallery-grid`, `preview-image`, `mono`, `text-label`, `text-body`, `text-heading`.
</interface_contract>

<artifacts_this_phase_produces>
## Artifacts this plan produces

New CSS custom properties in `companion/static/style.css`:
- `--space-3xl` (declared once, in `:root`)
- `--color-border` (declared four times: `:root`, `@media (prefers-color-scheme: dark) :root`, `html[data-ui-theme="light"]`, `html[data-ui-theme="dark"]`)

Revised values for existing custom properties (same four-site declaration pattern): `--color-dominant`, `--color-secondary`, `--color-accent`.

New CSS rules: `.dashboard-sidebar`, `.sidebar-nav`, `.sidebar-link`, `.sidebar-link--active`, `.stat-tile`, `.stat-tile--ok`, `.stat-tile--warn`, `.stat-tile--error`, `.stat-tile--accent`, `.stat-tile__caption`, `.stat-tile__value`, `.stat-tile svg`, `.dashboard-grid`, `.page-section`.

New at-rule: `@media (min-width: 960px)` containing `.dashboard-shell`, `.dashboard-sidebar`, `.dashboard-sidebar .theme-form`, `.site-header`, `.dashboard-main`, `.config-form`, `.config-form fieldset`, `.config-form > button`.

Modified existing rules: the file header comment, `.site-title` (letter-spacing), `fieldset` (border colour token).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Revise the colour tokens and activate --space-3xl / --color-border (D-01)</name>

  <read_first>
    - `companion/static/style.css` lines 1-88 in full — the header comment plus all four token declaration sites (`:root`, the `prefers-color-scheme: dark` override, and both `html[data-ui-theme=...]` overrides)
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Spacing Scale" and "Color" sections — the exact hex values and the explicit accent reserved-for list
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-PATTERNS.md` section "`companion/static/style.css` — add `--space-3xl`, `--color-border` tokens" — the three-place (four-site) duplication convention
  </read_first>

  <files>companion/static/style.css</files>

  <action>
Edit only the token blocks (lines 23-88) and the file header comment (lines 1-21). Do not touch any rule below line 88 in this task.

Add `--space-3xl: 64px;` to the `:root` spacing scale, immediately after the existing `--space-2xl` declaration. Spacing tokens are not theme-dependent in this file, so it is declared exactly once — do not copy it into any theme override block.

Set the light-mode colour values in `:root`:
- `--color-dominant: #FAFBFD;`
- `--color-secondary: #EEF1F7;`
- `--color-accent: #3454D1;`
- add a new `--color-border: #E2E6EE;` declared directly after `--color-secondary`

Set the dark-mode colour values in **both** dark declaration sites — the `@media (prefers-color-scheme: dark) { :root { ... } }` block and the `html[data-ui-theme="dark"]` block — with identical values in each:
- `--color-dominant: #0D0F14;`
- `--color-secondary: #1A1E27;`
- `--color-accent: #6E93FF;`
- add `--color-border: #262B36;`

Set the `html[data-ui-theme="light"]` block to the same four light values as `:root`, including `--color-border: #E2E6EE;`. That block already duplicates `:root`'s defaults verbatim; keep that convention exactly — it is what makes the CFG-09 explicit-light override beat the `prefers-color-scheme` media query.

Leave `--color-destructive`, `--color-text`, `--color-status-ok`, `--color-status-warn`, `--color-status-error` and every typography/font token at their current values in all four sites. `06.3-UI-SPEC.md` revises Dominant, Secondary, Accent and adds Border; the status triad and the text colour are explicitly unchanged.

Update the file header comment so it stays truthful after this change, keeping its existing voice and structure:
- The "Accent is reserved to exactly four uses" paragraph now has five: primary buttons, the active nav-tab/sidebar-link indicator, links, the focused-input outline, and a stat tile's top border when no ok/warn/error status applies to that tile. Point the "do not broaden without updating" reference at `06.3-UI-SPEC.md`'s Color section.
- Add a short paragraph stating that `--color-border` is structural only — card outlines, the sidebar/content divider, and fieldset borders — and is never used as an interactive-state signal and never substitutes for the accent or status colours.
- The opening paragraph currently describes this stylesheet as a deliberately lower-investment visual register per `06-CONTEXT.md`'s D-21. Replace that framing: `06.3-CONTEXT.md`'s D-01 supersedes D-21 for the companion app, so the comment should say the companion app now carries its own full visual identity (colour, type, spacing, card treatment, dashboard layout) while remaining entirely separate from the physical frame's design system — the frame's vendored fonts and PALETTE_RGB values must still never be pulled in here.
- Leave the theme-resolution paragraph and the zero-external-references paragraph unchanged; both are still exactly true.

Do not add a CSS url function, a custom font-face declaration, or any remote asset reference — keep the header comment's existing wording for that paragraph verbatim, since it already states the constraint without naming the at-rule. The system font stacks are unchanged: `06.3-UI-SPEC.md`'s Design System row keeps zero web-font loading as a hard constraint, and the extra visual personality comes from colour, spacing and card treatment instead.

Do not add a `{` or `}` character inside any CSS comment: the acceptance check below uses brace balance as a cheap syntax smoke test for this hand-written, build-step-free file.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
    - `test "$(grep -cE '^[[:space:]]*--color-border:' companion/static/style.css)" = "4"` succeeds — the token is declared at every theme site, matching the file's existing convention.
    - `test "$(grep -cE '^[[:space:]]*--space-3xl:' companion/static/style.css)" = "1"` succeeds.
    - `test "$(grep -c '#3454D1' companion/static/style.css)" = "2"` and `test "$(grep -c '#6E93FF' companion/static/style.css)" = "2"` both succeed — the new accent is set in the `:root`/explicit-light pair and the two dark sites respectively.
    - `test "$(grep -c '#FAFBFD' companion/static/style.css)" = "2"` and `test "$(grep -c '#0D0F14' companion/static/style.css)" = "2"` both succeed.
    - `test "$(grep -c '#2563EB' companion/static/style.css)" = "0"` and `test "$(grep -c '#5B8DEF' companion/static/style.css)" = "0"` both succeed — no superseded accent value survives.
    - `test "$(grep -c '#1C2028' companion/static/style.css)" = "0"` and `test "$(grep -c '#FFFFFF' companion/static/style.css)" = "0"` both succeed.
    - `test "$(grep -c 'url(' companion/static/style.css)" = "0"`, `test "$(grep -c '@font-face' companion/static/style.css)" = "0"` and `test "$(grep -c '@import' companion/static/style.css)" = "0"` all succeed — the zero-external-reference constraint holds. (All three pass against the file as it stands today; the header comment is deliberately worded to describe these forms without spelling them out.)
    - `server/.venv/bin/python3 -c "s=open('companion/static/style.css').read(); assert s.count('{')==s.count('}'), 'unbalanced braces'; print('css ok')"` prints `css ok`.
    - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 — its Section 3 serves this stylesheet over a real HTTP request and asserts the CSS content type.
  </acceptance_criteria>

  <done>All four theme declaration sites carry the revised Dominant/Secondary/Accent values plus a `--color-border`; `--space-3xl` is declared; the header comment describes the current, post-D-01 reality.</done>
</task>

<task type="auto">
  <name>Task 2: Add the stat-tile, dashboard-grid, sidebar-link and page-section component rules (D-01, D-02, D-22)</name>

  <read_first>
    - `companion/static/style.css` lines 128-142 (`.site-title`, the unconditional `:focus-visible` rule), 173-214 (`.nav-tab`, `.theme-form`, `.theme-option`, `.page-content`), 261-272 (`fieldset`, `legend`), 317-373 (`.data-table-wrap`, `.data-table`, `.empty-state`, `.gallery-grid`) — the idioms and spacing conventions to match
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Component Patterns" (Stat tile, Sidebar nav rows), "Typography" (the letter-spacing accent and its two-location limit), "Spacing Scale"
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-RESEARCH.md` "Pitfall 1" (grid track blowout) and "Pitfall 4" (`.page-section` currently has no rule at all)
    - `companion/pages/health_page.py` lines 253-271 (`battery_sparkline_svg()`) — the fixed `width`/`height` attributes the tile must override
  </read_first>

  <files>companion/static/style.css</files>

  <action>
Add new rules to `companion/static/style.css`. Place the sidebar rules directly after the existing `.theme-option--active` rule (around line 210), and the stat-tile / dashboard-grid / page-section rules directly before the existing `/* Gallery grid (CFG-11) */` comment near the end of the file. Keep the file's existing comment style: a short `/* ... */` block above each group explaining what it is and, where relevant, why.

**Sidebar component rules (base, all widths).** Add:
- `.dashboard-sidebar { display: none; }` — the base state. The sidebar is desktop-only; the 960px block in Task 3 turns it on. Using `display` (rather than a transparency or CSS-visibility approach) is what keeps the hidden copy out of the tab order and out of the accessibility tree at mobile width, so a phone user tabbing through the page never lands on an invisible link and the page never exposes two navigation landmarks at once.
- `.sidebar-nav { display: flex; flex-direction: column; gap: var(--space-xs); }`
- `.sidebar-link` — `display: flex; align-items: center; min-height: 44px; padding: 0 var(--space-sm); color: var(--color-text); text-decoration: none; font-size: var(--font-body-size); border-left: 3px solid transparent; letter-spacing: 0.02em;`. The 44px floor is D-22's tap target, kept unconditional so there is one rule instead of a breakpoint-conditional target size. The transparent 3px left border reserves the space the active indicator occupies, so activating a link does not shift the text — the same technique `.nav-tab`'s `border-bottom: 2px solid transparent` already uses.
- `.sidebar-link--active { border-left-color: var(--color-accent); color: var(--color-accent); }` — a left border rather than the horizontal nav's bottom border, since the list is vertical.

Add `letter-spacing: 0.02em;` to the existing `.site-title` rule (line 128). Combined with `.sidebar-link` above, that is the complete set of locations for this accent. `06.3-UI-SPEC.md`'s Typography section is explicit that it is targeted, not page-wide: do not add `letter-spacing` to any other selector, and do not put it on `body`, a heading role class, or a typography role class.

Add no rule for `.sidebar-title` beyond what `.site-title` already provides — the sidebar title carries both classes precisely so it inherits the shared treatment.

**Stat tile.** Add `.stat-tile` with `min-width: 0;` as its **first** declaration, then `padding: var(--space-md); background: var(--color-dominant); border: 1px solid var(--color-border); border-top: 3px solid var(--color-accent); border-radius: 2px;`. The `min-width: 0` is load-bearing and must not be dropped as redundant-looking: a grid item's default minimum size is its content's intrinsic width, and the Battery-trend tile contains a `.data-table` carrying `min-width: max-content` (line 328, deliberately, so tables never crop). Without `min-width: 0` that intrinsic width propagates up and stretches the tile's whole dashboard-grid track far past the 240px floor, pushing sibling tiles off-grid and forcing horizontal scroll on the page itself. With it, the existing `.data-table-wrap { overflow-x: auto }` (line 322) absorbs the width instead, which is exactly what that wrapper is for. State this reason in the rule's comment.

Add the four status modifiers, each setting only `border-top-color`: `.stat-tile--ok` → `var(--color-status-ok)`, `.stat-tile--warn` → `var(--color-status-warn)`, `.stat-tile--error` → `var(--color-status-error)`, `.stat-tile--accent` → `var(--color-accent)`. The accent modifier is the neutral/informational case — this is the fifth entry on the accent reserved-for list added to the header comment in Task 1.

Add `.stat-tile__caption { margin: 0 0 var(--space-sm); }` (the caption is a `<p>` whose default browser margin would otherwise fight the card's 16px padding) and `.stat-tile__value { font-size: var(--font-display-size); font-weight: var(--weight-semibold); line-height: 1.2; }` — the Display role, reused for a tile's headline value, using the existing type tokens rather than new sizes.

Add `.stat-tile svg { width: 100%; height: auto; }`. The battery sparkline is emitted with fixed `width="300" height="60"` attributes alongside its `viewBox`; without this override it cannot shrink inside a 240px-floor tile.

**Dashboard grid.** Add `.dashboard-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: var(--space-lg); margin-bottom: var(--space-2xl); }`. This is the same `auto-fit`/`minmax` idiom `.gallery-grid` already establishes as this file's responsive-grid precedent, with deliberately different values — `auto-fit` (not `auto-fill`), a 240px floor (not 160px), and the `lg` gap. The `2xl` bottom margin is the vertical gap between dashboard-grid groups. No breakpoint is needed for the grid itself: it collapses to one column on narrow viewports on its own.

**Page section card treatment.** Add `.page-section { padding: var(--space-md); margin-bottom: var(--space-lg); border: 1px solid var(--color-border); border-radius: 2px; }`. This class is used by four page modules today but has never had a rule of its own — its current spacing comes entirely from default browser margins on the elements inside it. The rule gives the below-breakpoint stacked sections the same card treatment as the dashboard's tiles, per `06.3-UI-SPEC.md`'s Mobile/tablet floor. Note in the comment that `health_page.py`'s source-fault block combines `page-section` with `banner banner--anomaly`, so its 4px destructive left border keeps overriding this rule's left edge — that is intended, not a conflict.

Change the existing `fieldset` rule's border colour from the secondary token to `var(--color-border)`. Fieldset borders are on `06.3-UI-SPEC.md`'s Border reserved-for list. Leave `input`/`select`/`.theme-option`/`.data-table td` borders on their current tokens — those are field and row treatments, not card edges.

Add nothing to this task's rules that depends on viewport width; every declaration here applies at all widths. Task 3 owns the entire breakpoint.

Do not add a `{` or `}` character inside any CSS comment.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
    - `test "$(grep -A10 '^\.stat-tile {' companion/static/style.css | grep -c 'min-width: 0')" -ge 1` succeeds — the grid-blowout fix is inside the `.stat-tile` rule, not merely mentioned in a comment above it.
    - `test "$(grep -c 'repeat(auto-fit, minmax(240px, 1fr))' companion/static/style.css)" = "1"` succeeds.
    - `test "$(grep -c '^\.dashboard-grid {' companion/static/style.css)" = "1"`, `test "$(grep -c '^\.page-section {' companion/static/style.css)" = "1"`, and `test "$(grep -c '^\.sidebar-link {' companion/static/style.css)" = "1"` all succeed.
    - `test "$(grep -c 'min-height: 44px' companion/static/style.css)" = "5"` succeeds — the four existing declarations (nav tab, theme option, input/select, button) plus the new sidebar link, all keeping D-22's tap-target floor.
    - `test "$(grep -c 'letter-spacing: 0.02em' companion/static/style.css)" = "2"` succeeds — exactly the two locations `06.3-UI-SPEC.md` permits.
    - `test "$(grep -c '^\.stat-tile svg {' companion/static/style.css)" = "1"` succeeds.
    - `server/.venv/bin/python3 -c "s=open('companion/static/style.css').read(); assert s.count('{')==s.count('}'), 'unbalanced braces'; print('css ok')"` prints `css ok`.
    - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0.
  </acceptance_criteria>

  <done>The stylesheet defines the stat-tile card (with the `min-width: 0` grid-blowout fix), the dashboard grid, the sidebar link treatment, the `.page-section` card treatment, and the two permitted letter-spacing locations.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 960px dashboard breakpoint (D-02, D-22)</name>

  <read_first>
    - `companion/static/style.css` lines 165-171 — the existing `@media (max-width: 480px)` block, this file's only width breakpoint so far, and the comment style to mirror
    - `companion/static/style.css` lines 149-163 (`.site-header`, `.nav-bar`) and line 212-214 (`.page-content`) — the rules the breakpoint overrides
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-UI-SPEC.md` "Layout Contract" in full — breakpoint value, sidebar width, divider, max-width cap, gutter, and the Form (Config page) row of "Component Patterns"
    - `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-RESEARCH.md` "Pitfall 2" — why the losing nav copy must be removed from layout flow rather than merely made invisible
    - `companion/pages/config_page.py` lines 127-149 (`render()`) — the form whose two fieldsets and Save button the config grid rule targets
  </read_first>

  <files>companion/static/style.css</files>

  <action>
Append a single `@media (min-width: 960px) { ... }` block at the end of `companion/static/style.css`. This is the file's second and last width breakpoint; the 480px nav-scroll rule stays exactly as it is and is not nested, merged, or otherwise touched. Below 960px, nothing in this new block applies and the page renders exactly as it did before this phase — fluid single column, top `.nav-bar`, with its own 480px horizontal-scroll fallback intact (D-22's mobile floor).

Precede the block with a comment, in this file's existing voice, covering three points: (1) it is the desktop dashboard shell from `06.3-UI-SPEC.md`'s Layout Contract, D-02; (2) `companion/layout.py`'s `page_shell()` renders both navigation copies into every response because a stdlib HTTP server has no viewport information at render time, so this block is what chooses between them; (3) the losing copy is removed with `display: none`, which takes it out of the layout flow, the tab order and the accessibility tree together — that is what keeps a keyboard user from landing on invisible nav links and keeps exactly one navigation landmark exposed at any given width.

Inside the block, add exactly these rules:

- `.dashboard-shell` — `display: grid; grid-template-columns: 240px minmax(0, 1fr); column-gap: var(--space-xl); align-items: start;`. The `minmax(0, 1fr)` on the main track (rather than a bare `1fr`) is the same intrinsic-minimum defence as `.stat-tile { min-width: 0 }`, applied one level up: it stops a wide table anywhere in the page body from stretching the main column and squeezing the sidebar. The `xl` column gap is the sidebar-to-main-column gap from the spacing scale.
- `.dashboard-sidebar` — `display: flex; flex-direction: column; gap: var(--space-lg); min-height: 100vh; padding: var(--space-lg) var(--space-md); background: var(--color-secondary); border-right: 1px solid var(--color-border);`. This overrides the base `display: none` from Task 2. The column order it lays out is fixed by the markup: title, then nav, then theme form.
- `.dashboard-sidebar .theme-form` — `margin-top: auto;`, which pins the CFG-09 theme toggle to the bottom of the sidebar column per the Layout Contract's stated top-to-bottom order.
- `.site-header` — `display: none;`. The sidebar carries the title, the nav and the theme toggle at this width, so the entire header row is removed rather than just its nav; leaving the header's title visible alongside the sidebar's would duplicate it on screen. `06.3-UI-SPEC.md` is explicit that the sidebar **replaces** the horizontal nav-bar here — a supplement was considered and rejected.
- `.dashboard-main` — `min-width: 0; max-width: 1440px; margin: 0 auto; padding: var(--space-lg) var(--space-3xl) var(--space-2xl);`. The cap and `margin: 0 auto` centre the content column; the `3xl` horizontal padding is the 64px minimum side gutter, so content never runs edge-to-edge on an ultra-wide monitor. Note in the comment that `3xl` is serving as the main column's horizontal padding rather than as a separate outer gutter element: expressing the cap and the gutter as two independent mechanisms would need an extra wrapper element in `page_shell()`, and this single-element form delivers the same visible result. Below the breakpoint the base `.page-content` padding rule (line 212) governs unchanged.
- `.config-form` — `display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-lg); align-items: start;`, plus `.config-form fieldset { margin-bottom: 0; }` (the grid gap replaces the stacked margin) and `.config-form > button { grid-column: 1 / -1; justify-self: start; }` so Save Settings stays a full-width row beneath the two side-by-side fieldsets rather than becoming a third grid cell. Below 960px the form has no grid rule at all and the fieldsets stack exactly as they do today.

Add no rule for `.dashboard-grid` inside this block — its `auto-fit` behaviour already handles every width, and duplicating it here would create a second place to keep in sync.

Add no rule for `.gallery-grid` inside this block. `06.3-UI-SPEC.md` lists widening the Preview gallery as optional and cosmetic; its existing `auto-fill` rule already adds columns as the viewport grows, so it needs nothing.

Add nothing to this block for the History page: `06.3-UI-SPEC.md` keeps History a single full-width table because data density matters more than tile framing there.

Do not add a `{` or `}` character inside any CSS comment.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>

  <acceptance_criteria>
    - `test "$(grep -c '@media (min-width: 960px)' companion/static/style.css)" = "1"` succeeds — exactly one new breakpoint.
    - `test "$(grep -c '@media (max-width: 480px)' companion/static/style.css)" = "1"` succeeds — the existing mobile nav breakpoint is still present and untouched.
    - `test "$(grep -A60 '@media (min-width: 960px)' companion/static/style.css | grep -c 'display: none')" -ge 1` succeeds — the losing nav copy is removed from flow, not merely made invisible.
    - `test "$(grep -c 'grid-template-columns: 240px minmax(0, 1fr)' companion/static/style.css)" = "1"` succeeds.
    - `test "$(grep -c 'max-width: 1440px' companion/static/style.css)" = "1"` and `test "$(grep -c 'var(--space-3xl)' companion/static/style.css)" -ge 1` both succeed — the cap and the newly-active 64px gutter token are both consumed, not just declared.
    - `test "$(grep -c 'grid-template-columns: 1fr 1fr' companion/static/style.css)" = "1"` succeeds — the Config page's desktop two-column fieldset rule exists.
    - `server/.venv/bin/python3 -c "s=open('companion/static/style.css').read(); assert s.count('{')==s.count('}'), 'unbalanced braces'; print('css ok')"` prints `css ok`.
    - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0.
  </acceptance_criteria>

  <done>A single `@media (min-width: 960px)` block turns `.dashboard-shell` into a 240px-sidebar two-column grid, removes the horizontal header with `display: none`, caps and centres the main column at 1440px with a 64px gutter, and lays the Config fieldsets side by side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `GET /static/style.css` | The only boundary this plan touches. The stylesheet is a static, unauthenticated asset served by `companion/app.py`'s existing static handler; this plan changes its bytes, not how it is served. |

No untrusted input reaches this file: CSS is fully static, contains no interpolated value, and the phase adds no `url()`, `@import`, `@font-face`, or any other external reference.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-04 | Denial of Service (assistive-technology access, not availability) | The 960px breakpoint's choice between the two always-rendered nav/theme-form copies | medium | mitigate | Task 3 hides the losing copy with `display: none`, which removes it from layout flow, tab order and the accessibility tree in one step; Task 2 uses the same mechanism for the sidebar's base mobile state. Asserted by the `display: none`-inside-the-breakpoint check, and confirmed end-to-end by plan 06.3-05's keyboard-tab pass at 900px/960px/1440px. |
| T-06.3-06 | Information Disclosure | External asset reference in the stylesheet — a CSS url function, an import at-rule, or a custom font-face declaration would leak the companion app's referrer or a device identifier to a third-party host on every page load | medium | mitigate | The zero-external-reference constraint stated in this file's own header comment is preserved and machine-checked by Task 1's acceptance criteria, which require zero occurrences of each of those three forms. The visual refresh is delivered entirely through colour, spacing, and card treatment with the existing system font stacks (`06.3-UI-SPEC.md` Design System, Font row). |
| T-06.3-07 | Tampering | CSS syntax error silently disabling downstream rules (a hand-written, build-step-free stylesheet has no parser gate, and one unbalanced brace can drop every subsequent rule including the focus-visible outline) | low | mitigate | Brace-balance smoke check in every task's acceptance criteria, plus the existing unconditional `:focus-visible` rule (lines 136-142) is deliberately left above all new rules so it cannot be orphaned by a late syntax error. Visual confirmation of focus outlines is part of plan 06.3-05's keyboard pass. |
| T-06.3-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No package-manager install occurs. `06.3-RESEARCH.md`'s Package Legitimacy Audit records this phase as installing zero external packages in any ecosystem; there is no build step, no preprocessor, and no dependency for this file. No legitimacy checkpoint is required. |

Categories not applicable to this plan: Spoofing, Repudiation, Elevation of Privilege — no identity, no logged action, and no privilege boundary exists in a static stylesheet.
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 after each task (its Section 3 fetches the stylesheet over real HTTP).
2. Brace balance holds: `server/.venv/bin/python3 -c "s=open('companion/static/style.css').read(); assert s.count('{')==s.count('}')"`.
3. Exactly two width breakpoints exist in the file: the pre-existing 480px one and the new 960px one.
4. The stylesheet still references zero external assets — no CSS url function, no import at-rule, no custom font-face declaration.
5. Only `companion/static/style.css` is modified — no Python file, no new file.
</verification>

<success_criteria>
- `--color-border` is declared at all four theme sites and `--space-3xl` once, with the revised Dominant/Secondary/Accent values in place and no superseded hex value surviving.
- `.stat-tile` carries `min-width: 0` as its first declaration.
- `.dashboard-grid` uses `repeat(auto-fit, minmax(240px, 1fr))` with the `lg` gap.
- One `@media (min-width: 960px)` block defines the 240px-sidebar grid shell, removes `.site-header` with `display: none`, caps `.dashboard-main` at 1440px with a `3xl` gutter, and lays out `.config-form` in two columns.
- `letter-spacing: 0.02em` appears at exactly two locations.
- `companion/test_companion_app.py` passes.
</success_criteria>

<output>
Create `.planning/phases/06.3-companion-ui-visual-design-and-desktop-layout-pass/06.3-02-SUMMARY.md` when done.
</output>
