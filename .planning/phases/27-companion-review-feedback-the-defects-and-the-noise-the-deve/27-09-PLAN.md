---
phase: 27-companion-review-feedback-the-defects-and-the-noise-the-deve
plan: 09
type: execute
wave: 9
depends_on: ["27-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .claude/skills/sketch-findings-skypane/SKILL.md
  - .planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve/deferred-items.md
  - companion/test_browser_ux.py
autonomous: true
requirements: [CFG-68, CFG-71]

must_haves:
  truths:
    - "CFG-71: EVERY EXPECTED_CHECK_COUNT is re-derived by RUNNING, never by arithmetic. A count reached by adding and subtracting is a count nobody verified, and this is the phase whose whole lesson is that unverified relationships ship"
    - "CFG-71: the sandbox baseline is exactly 5 failing checks and they are verified BY NAME. 'Five failures' is a number; five NAMES are evidence. If the names differ from the ones this phase started with, that is a regression this phase caused and it is reported as one"
    - "CFG-68: Display's height is MEASURED at 390 px and reported against 27-07's STATED prediction. A prediction that was wrong is information; a prediction quietly adjusted to match the measurement is nothing"
    - "CFG-71: no new script and no new route in the whole phase — ls companion/static/*.js unchanged, the deferred-script pin still 15, and the set of routes reachable WITHOUT a session unchanged. Verified by running, at the phase's close, across every plan's work together"
    - "the two standing refusals stay refused: the overlay drawer and sticky day headers. Neither was reopened, and this plan proves it rather than asserting it"
    - "the human sweep is the developer's and is NOT claimed here. This gate reports what a machine can see; it says plainly what it cannot"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "the Phase 27 coverage ledger, written by walking the code rather than the plans"
      contains: "Phase 27 coverage ledger"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve"
      via: "every CFG-62..71 verdict cites the SUMMARY and the code, never the plan that intended it"
      pattern: "CFG-6"
---

<objective>
The closing gate. This phase opened because three passing checks shipped a lying arc,
so its own close cannot be a narrative — it has to be a set of numbers somebody
re-derived by running, and a set of names somebody read.

Four jobs:

1. **Re-derive every count by running**, and verify the sandbox baseline is still
   exactly five failing checks, **by name**.
2. **Measure Display's height at 390 px** and report it against the figure 27-07
   stated before anyone measured. 27-07 predicts the target is not reached and
   explains why; this plan finds out whether that was right.
3. **Prove the phase's structural pins together**, across every plan's work at once —
   no new script, no new route, the motion budget, the feature-query count, the
   stylesheet's cleanliness, and the two standing refusals.
4. **Write the ledger**: CFG-62…71 walked clause by clause against the code and the
   SUMMARYs, CFG-47's retirement confirmed to have survived the phase intact, the
   deferred item recorded, and the design authority updated in step.

And one thing this gate must NOT do: claim the human sweep. What a machine can see
is reported here. What only the developer can judge is named and left to them.
</objective>

<decisions_covered>
D-15 (CFG-47's retirement, confirmed intact at close), D-16 and D-31 (counts
re-derived by running; baseline verified by name), D-21 (the height reported against
a stated prediction), D-26 (no new script, no new route), D-33 (the design authority
updated; the two standing refusals hold). D-32 is the convention the whole ledger is
written in.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve/27-RESEARCH.md
@.planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve/27-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
Only writer of every listed file in wave 9. Every earlier plan's SUMMARY is INPUT
here and is never edited — a gate that edits the evidence is not a gate.
</files_owned>

<no_js_floor>
The floor is not re-argued here; it is re-run. Every scripts-blocked save-to-disk
check in the suite must pass, and this plan enumerates them BY NAME in the SUMMARY —
the runway radios (25-03), the quiet window (25-04), the wake interval (25-05), the
theme (25-06), the artwork upload (25-07), 27-03's floor-under-auto-save proof, and
27-07's newly wrapped arrivals grid.

`grep -n 'java_script_enabled' companion/test_browser_ux.py` must still return exactly
one line: one scripts-blocked context, composed with, never duplicated.
</no_js_floor>

<interfaces>
RE-DERIVE by running — this plan measures, it does not cite:

  PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh
  for f in companion/test_*.py; do echo "$f"; grep -n 'EXPECTED_CHECK_COUNT = ' "$f" | tail -1; done
  ls companion/static/*.js | wc -l
  grep -c 'defer' <the authenticated shell's rendered head>        # the pin: 15
  grep -c '@keyframes' companion/static/style.css                   # comment-stripped: 4
  grep -cE '@supports selector\(:has\(\*\)\)\s*\{' companion/static/style.css   # 1
  grep -cE 'interpolate-size|calc-size\(' companion/static/style.css            # 0
  grep -ci 'drawer' <comment-stripped style.css>                    # 0
  grep -c 'position: sticky' <comment-stripped style.css>           # 1, and not a day header

Pre-phase baselines, to be reported against (re-derive each by running, do not trust):
  test_browser_ux.py 81 · test_companion_app.py 314 · test_config_page.py 259 ·
  test_status_pages.py 305 · test_i18n.py 24 · test_view_pages.py 164 ·
  test_contrast_check.py 49

**A bare grep lies about two of these and the trap is documented:** a bare
`grep -c '@supports selector(:has(\*))'` returns **6** and a bare `grep -c '@keyframes'`
returns **5**, because comments quote the at-rule. Always count comment-stripped and
brace-anchored. Record the bare counts alongside the real ones so the next reader does
not read them as a regression.

Binding constraints:
- The suite must report exactly the **5** sandbox baseline failures, and this plan
  lists their NAMES. If a name changed, the phase caused it and it is reported as a
  regression, not normalised.
- `grep -c SKIP` over the whole run is **0**. A SKIP means nothing was checked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run everything; name the five</name>
  <files>companion/test_browser_ux.py</files>
  <behavior>
    - Every harness runs; every `EXPECTED_CHECK_COUNT` is re-derived by running and recorded against its pre-phase baseline
    - The five sandbox baseline failures are enumerated BY NAME and compared to the names the phase started with
    - Zero SKIPs across the whole run
    - Every scripts-blocked save-to-disk check is named and confirmed passing
  </behavior>
  <read_first>
    - every 27-0X SUMMARY in this phase directory, for the counts each plan recorded when it ran
    - .planning/phases/25-.../25-08-SUMMARY.md: the pre-phase count table and the five no-JS persistence check names
  </read_first>
  <action>
    Run the whole suite. Record, per harness, the pre-phase count, the final count, and
    the pass/fail split — each obtained by RUNNING, never by adding up what the plans
    said they added.

    Enumerate the **five sandbox baseline failures by name**. This is the part most
    likely to be skipped and it is the part that matters: the phase's standing
    constraint is "exactly 5, verified by NAME", and no artifact in the tree currently
    writes those five names down. Write them down here. If the set differs from the set
    this phase started with — by name, not by count — **that is a regression this phase
    caused**, and it is reported as one and investigated, never absorbed into a
    still-five tally.

    Confirm `grep -c SKIP` over the run is 0. A harness that skips reports nothing, and
    this phase's central claim is executable.

    Enumerate every scripts-blocked save-to-disk check by name and confirm each passes:
    the five from Phase 25, 27-03's floor proof, and 27-07's arrivals grid.

    Confirm `grep -n 'java_script_enabled' companion/test_browser_ux.py` returns exactly
    one line.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); PYTHON="$PY" bash scripts/run-all-tests.sh; true</automated>
  </verify>
  <acceptance_criteria>
    - A per-harness table: pre-phase count, final count, pass/fail — every figure obtained by RUNNING
    - The five baseline failures listed BY NAME, and compared by name to the phase's starting set
    - Any change in that set reported as a regression, not normalised
    - `grep -c SKIP` over the whole run is 0
    - Every scripts-blocked save-to-disk check named and confirmed passing
    - `grep -n 'java_script_enabled' companion/test_browser_ux.py` returns exactly one line
    - `ruff check .` clean
  </acceptance_criteria>
  <done>The phase's numbers are numbers somebody ran, and the five known failures are five names somebody read.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Measure Display's height against the stated prediction</name>
  <files>companion/test_browser_ux.py</files>
  <behavior>
    - Display's page height is measured at 390 px, in the same conditions 25-06 used
    - It is reported against 27-07's stated prediction and against X6's 2600 px target
    - The no-JS page height is measured too, because that is where the carousel extension's real saving lands
    - No horizontal body scroll at 360 px on any page this phase touched
  </behavior>
  <read_first>
    - 27-07's SUMMARY: the predicted figure, its arithmetic, and its explicit yes/no on reaching 2600 px
    - .planning/phases/25-.../25-06-SUMMARY.md: the measurement conditions that produced 4276 px and 3743 px, so this measurement is comparable rather than merely similar
    - companion/static/theme-preview.js: the collapse behaviour, which is why the scripted and no-JS numbers should differ this time
  </read_first>
  <action>
    Measure Display's rendered page height at **390 px**, reproducing 25-06's
    conditions. Report three numbers side by side: 25-06's **3743 px**, 27-07's
    **prediction**, and **today's measurement** — plus the distance from X6's
    **2600 px**.

    Then measure the **scripts-blocked** height too. 27-07 predicts the carousel
    extension saves ≈0 px on the scripted page because `theme-preview.js` already
    collapses the non-selected panels, and that the saving is real for a no-JS reader.
    That claim is testable and this is where it gets tested. If the two numbers do not
    behave as predicted, say so.

    **Do not adjust the prediction.** Report the delta. A prediction that missed is the
    most useful line in this SUMMARY, because it tells the next phase which model of
    the page was wrong.

    Confirm no horizontal body scroll at **360 px** on every page this phase touched,
    in both themes.

    If 2600 px is not reached — which 27-07 expects — state what the remainder actually
    is, measured, in the same voice 25-06 used when it said "four more cards, not a
    grid". That sentence is what makes the number actionable for whoever picks this up.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_browser_ux.py</automated>
  </verify>
  <acceptance_criteria>
    - Scripted height measured at 390 px and reported against 25-06's 3743 px, 27-07's prediction, and the 2600 px target
    - Scripts-blocked height measured too, and the predicted scripted-versus-no-JS difference confirmed or refuted
    - The prediction is NOT adjusted; the delta is reported
    - No horizontal body scroll at 360 px on every touched page, both themes
    - If the target is missed, the remainder is named and measured
    - `ruff check .` clean
  </acceptance_criteria>
  <done>Display's height is a measured fact reported against an expectation somebody committed to in advance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the structural pins and the two standing refusals, together</name>
  <files>companion/test_browser_ux.py</files>
  <behavior>
    - No new script and no new route across the whole phase, verified at close
    - The motion budget, the feature-query count and the stylesheet's cleanliness all hold
    - The overlay drawer and sticky day headers are both still absent
    - The set of routes reachable WITHOUT a session is unchanged
  </behavior>
  <read_first>
    - every 27-0X SUMMARY's claim about the pin, the script count and the route set — this task checks those claims TOGETHER, which no single plan could
    - .planning/phases/25-.../25-08-SUMMARY.md: the pin table and the two-refusals paragraph, the shape this task reproduces
  </read_first>
  <action>
    Verify, in one place and at the end, what each plan could only claim for itself:

    - `ls companion/static/*.js | wc -l` — unchanged from the phase's start. **This
      phase repurposed `dirty-state.js` rather than adding a file, and that is the
      whole reason the count can hold.**
    - the deferred `<script src=` count on the authenticated shell — still **15**.
    - the set of routes reachable **without a session** — unchanged. Enumerate it and
      diff it rather than asserting it.
    - `@keyframes` **4**, comment-stripped. Record the bare count (**5**) beside it.
    - brace-anchored `@supports selector(:has(*))` **1**. Record the bare count (**6**)
      beside it.
    - `interpolate-size` / `calc-size(` **0**.
    - stray comment terminators in `style.css` **0** — this phase deleted more CSS than
      it added (the save bar, the runway map), and an orphaned terminator is the most
      likely way that breaks.
    - the **overlay drawer**: absent, comment-stripped grep returns 0. Three recorded
      rejections plus locked decision D-10; it must not have crept back in as a
      "transient status" surface while correction 1 was rebuilding save feedback.
    - **sticky day headers**: `position: sticky` appears once, comment-stripped, at its
      pre-phase value, and that one is not a day header.

    Report every number with its pre-phase value beside it. A pin that held is one
    line; a pin that moved is the headline.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_browser_ux.py && "$PY" companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - Script file count and deferred-script pin unchanged, with their pre-phase values beside them
    - The unauthenticated route set enumerated and diffed, not asserted
    - Motion and feature-query counts correct comment-stripped, with the bare counts recorded beside them
    - Zero stray comment terminators
    - Both standing refusals proven absent, each with its grep
    - `ruff check .` clean
  </acceptance_criteria>
  <done>The claims every plan made about the whole phase are checked against the whole phase, once, at the end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: The ledger, the design authority, the deferred item, and the roadmap</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .claude/skills/sketch-findings-skypane/SKILL.md, .planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve/deferred-items.md</files>
  <behavior>
    - A Phase 27 coverage ledger is written by walking CFG-62…71 against the CODE and the SUMMARYs, clause by clause
    - Each requirement is ticked only on evidence, and a clause that was not met is recorded as not met rather than reworded
    - CFG-47's retirement is confirmed to have survived the phase in all three places
    - The design authority is updated in step; the deferred item is recorded; the roadmap's plan list is filled in
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md: the Phase 23, 24 and 25 coverage ledgers — the voice, the clause-by-clause discipline, and the precedent that a requirement is checked against the code at close rather than against the plans
    - every 27-0X SUMMARY, especially every decision marked PROVISIONAL
    - .claude/skills/sketch-findings-skypane/SKILL.md: the five contracts (motion, colour, spacing, drawing, controls), which this phase changed in the controls and spacing contracts most
  </read_first>
  <action>
    **The ledger.** Write a Phase 27 coverage ledger in `.planning/REQUIREMENTS.md`,
    following the Phase 23/24/25 sections' discipline: walk CFG-62 through CFG-71
    **clause by clause against the code**, not against the plans that intended it. Tick
    a row only where every clause is met; where a clause is not met, say so in the row
    and say what is missing. The precedent here is strong and it is the reason these
    ledgers are worth reading — Phase 24's own ledger records what was NOT built as
    plainly as what was.

    Give the dial defect its own paragraph. It is the reason the phase exists, and the
    ledger should record the lesson in its executable form: three passing checks, two
    correct halves, one unmeasured relationship — and the one check that now measures
    it, named.

    **Confirm CFG-47's retirement survived.** All three records — the ticked row, the
    traceability row, the D16 section — still present, still consistent with a tree
    that no longer has a runway map. 27-05 verified this mid-phase; confirm it again
    at close, because the intervening plans edited the same file.

    **The design authority.** Update `sketch-findings-skypane` in step. This phase
    changed what the controls contract says (there is no save button; saving is
    automatic and announced transiently; icon controls resolve to the 44 px floor in
    their own containers) and what the spacing contract says (one title form; the
    disclosure sits below the strip). Update the affected contracts with the measured
    values this phase produced, and record the two standing refusals as still standing.

    **The deferred item.** Create this phase's `deferred-items.md` and record the
    **"règles par vol"** view: the developer's words (*"pas propre, manque de
    simplicité, trop de texte"*), why it was not planned (too vague to plan against,
    needs a conversation first), and that the rules add-form's colour grid was
    deliberately left unwrapped by 27-07 for the same reason — so the two decisions are
    findable together. Also record any PROVISIONAL decision from any plan that the
    developer rejects or has not yet ruled on.

    **The roadmap.** Fill in Phase 27's `**Plans:**` count and its plan checklist,
    replacing the `TBD` placeholder, and set `**Requirements**` to CFG-62…71.

    **What this gate does NOT cover, stated plainly.** The human sweep is the
    developer's: auto-save judged on a real phone (does "Sauvegardé" arrive reassuringly
    or invisibly?), the dial's handles when the window's ends are close, the shortened
    battery wording, the one title form, the runway card without its map, and whether
    the disappearing duration during a drag is acceptable. Name these. Do not claim
    them.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - A Phase 27 coverage ledger walking CFG-62…71 clause by clause against the code
    - Rows ticked only on evidence; unmet clauses recorded as unmet
    - The dial defect's lesson recorded with the name of the check that now measures the relationship
    - CFG-47's three retirement records confirmed present and consistent at close
    - `sketch-findings-skypane` updated, with the two standing refusals recorded as standing
    - `deferred-items.md` created, carrying the "règles par vol" deferral, the unwrapped rules grid, and every open PROVISIONAL decision
    - ROADMAP's Phase 27 entry carries the real plan count, the plan list, and CFG-62…71
    - The human sweep named and explicitly NOT claimed
    - `ruff check .` clean
  </acceptance_criteria>
  <done>The phase closes with a record a future reader can check, and an honest statement of what only the developer can judge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's claims → the phase's record | the ledger is the artifact a future audit trusts; a tick written from a plan rather than from the code is the failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09-A | Repudiation | a count reached by arithmetic would make the record unverifiable | mitigate | every count re-derived by RUNNING, and the five baseline failures verified BY NAME rather than by tally |
| T-27-09-B | Tampering | a gate that edits earlier SUMMARYs could make a missed prediction disappear | mitigate | earlier SUMMARYs are INPUT and are never written by this plan; the prediction delta is reported, not adjusted |
| T-27-09-C | Information Disclosure | enumerating the unauthenticated route set could itself be treated as the check | mitigate | the set is diffed against its pre-phase value; unchanged is the assertion, and the enumeration is the evidence |
| T-27-09-SC | Tampering | supply chain | n/a | zero packages installed |
</threat_model>

<verification>
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → exactly the 5 baseline failures, listed BY NAME; 0 SKIP
- every harness's `EXPECTED_CHECK_COUNT` re-derived by RUNNING and tabled against its pre-phase value
- `ruff check .` clean
- `ls companion/static/*.js | wc -l` unchanged; deferred-script pin 15; unauthenticated route set unchanged
- `<human-check>`: **this is the phase's real gate and it is the developer's.** On a real phone at 360 px and on a desktop, in both themes and both languages — auto-save's two words, the dial's arc and caption following both a drag and a preset (including when the two ends are close), the runway card without its map, the shortened wake and battery wording, the one title form, all three colour strips and their pagers, and the Quiet hours link from both pages that show the Frame strip.
</verification>

<success_criteria>
- Every count re-derived by running; the five baseline failures named and compared by name
- Display's height measured at 390 px and at the no-JS floor, reported against 27-07's unadjusted prediction and the 2600 px target
- No new script, no new route, pin 15, motion and feature-query budgets intact, zero stray comment terminators
- Both standing refusals proven absent
- CFG-62…71 walked clause by clause against the code; unmet clauses recorded as unmet
- CFG-47's retirement confirmed intact in all three places
- Design authority updated; deferred item recorded; roadmap filled in
- The human sweep named and NOT claimed
- One commit per task, no model identifier in any artifact
</success_criteria>

<output>
Create `.planning/phases/27-companion-review-feedback-the-defects-and-the-noise-the-deve/27-09-SUMMARY.md` when done.
</output>
