---
phase: 06.6.4.1
plan: 04
type: execute
wave: 2
depends_on: ["06.6.4.1-01"]
files_modified:
  - companion/pages/health_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "The anomaly banner names each failing category as a short label pill instead of repeating the Overview tiles' full sentences (D-07)."
    - "Corroboration's three status rows stay always-visible and compact; their three explanation sentences are preserved behind a collapsed native disclosure (D-08)."
    - "The battery chart shows a real reading before any interaction and carries four axis labels; hover/click/keyboard reveal still works and reverts to the latest reading, not to a prompt (D-09)."
    - "Health's body reads as two headed, id-anchored sections — Screen and Server & data — with Server & data carrying three tiles in one grid row and two full-width stacked cards below it (D-10, D-11)."
    - "The migrated Unresolved-prefixes and Resolution-statistics cards render in full-width .page-section containers, never inside .dashboard-grid or a .stat-tile, and each keeps its own independent database-failure isolation (D-11, D-12)."
    - "The Unresolved-prefixes card's filter bar, read-only note, and non-button Clear control are byte-identical in behaviour to what the old Airlines page rendered (D-12)."
  artifacts:
    - "companion/pages/health_page.py::SCREEN_SECTION_ID, SCREEN_SECTION_HEADING, SERVER_DATA_SECTION_ID, SERVER_DATA_SECTION_HEADING, RESOLUTION_RATE_LABEL, RESOLUTION_WINDOW_DAYS, UNRESOLVED_SECTION_HEADING, STATS_SECTION_HEADING"
    - "companion/pages/health_page.py::unresolved_rows(), coverage_status(), resolution_stats(), _registry_section(), _registry_table_html(), _registry_row_html(), _registry_filter_bar_html(), _stats_table_html(), _resolution_rate_tile_html(), _anomaly_banner_html(), _corroboration_details_html()"
  key_links:
    - "D-11 explicitly warns against folding the migrated reads into _read_health_inputs()'s single dict. Each migrated section must keep its own isolated read — the registry through poll_loop.load_poll_state() (a filesystem/JSON failure mode) and the stats through _safe_query() (a SQLite failure mode) — so one failing source degrades one card, exactly as it does on the page they come from today."
    - "The .stat-tile rule's own CSS comment documents the exact failure mode D-11 diagnoses: a wide table inside a 240px-floor grid track drags the whole grid off-page. The migrated cards must therefore use .page-section, which is already outside the grid and already carries the 06.6.4 card contract — no new CSS is needed and none may be added here (plan 01 is this phase's only stylesheet write site)."
    - "This plan deliberately leaves companion/pages/airlines_page.py untouched, so for one wave both pages render the registry. Plan 06 (the very next wave) removes it from Airlines — the sequencing RESEARCH.md prescribes to avoid an intermediate state where the two pages disagree about who owns this content."
---

<objective>
Restructure Health into the two-section Screen / Server & data shape and absorb the old Airlines page's two diagnostics sections into it as full-width cards, while making the anomaly banner, the Corroboration tile, and the battery chart do what D-07/D-08/D-09 ask.

Purpose: three of this phase's real bug fixes live here — an empty-state that read as a page-wide contradiction, a five-column table forced into a 240px grid track, and a chart that showed no number until you touched it. All three are fixed by choosing the right container and the right default, not by new CSS.
Output: a Health page module that owns every server-and-data diagnostic signal the companion app has.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/pages/health_page.py
@companion/pages/airlines_page.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: anomaly-banner category pills (D-07) and the Corroboration details disclosure (D-08)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    companion/pages/health_page.py lines 140-160 and 405-470 (`ANOMALY_BANNER_TEXT`, `_SEVERITY_BANNER_NOUNS`, `collect_anomalies()`, `overall_severity()` — the four literal anomaly strings and the severity mapping the pills are derived from);
    companion/pages/health_page.py lines 586-660 (`_starts_with_acronym()`, `_anomaly_category_text()`, `_anomaly_banner_text()` — the existing comma-joined derivation, including the deliberate reason the trailing period is dropped and why the ADS-B acronym is special-cased; the pill derivation reuses the same period-stripping and must keep that acronym behaviour);
    companion/layout.py lines 907-923 (`layout.anomaly_banner()` — it escapes its whole message, so it structurally cannot carry pill markup; read this before deciding how the new builder emits the banner);
    companion/pages/health_page.py lines 97-125 and 820-842 (`_CORROBORATION_ROWS` and `_corroboration_section()` — the three rows, their explanations, and the `_DB_UNAVAILABLE`/empty-counts branches that must both survive unchanged);
    companion/pages/health_page.py lines 796-801 (the existing `readings-disclosure` details/summary block — the disclosure idiom D-08 reuses verbatim);
    companion/static/style.css (`.banner`, its three severity modifiers, its `::before` dot, and the `.banner__pill` rule plan 01 landed — confirm the class exists before emitting markup for it; this plan must not edit the stylesheet);
    companion/test_status_pages.py — every check naming `ANOMALY_BANNER_TEXT`, the corroboration explanations, or the banner role/class, plus the file's real `EXPECTED_CHECK_COUNT`
  </read_first>
  <action>
    Add `_anomaly_category_labels(anomalies)` returning one short label string per anomaly — reuse `_anomaly_category_text()`'s existing period-stripping and its `_starts_with_acronym()` guard, but return the list rather than a joined clause, and keep each label sentence-cased (no mid-sentence lower-casing, since a pill is not mid-sentence). Keep `_anomaly_category_text()` itself in the file and still call it for the banner's accessible/plain-text tail, so no existing presence check on `ANOMALY_BANNER_TEXT` breaks.

    Add `_anomaly_banner_html(severity, anomalies)` returning the banner element directly instead of routing through `layout.anomaly_banner()`. It must reproduce that helper's exact class/role mapping — the error severity gets the anomaly modifier class and the alert role, anything else gets the warn modifier and the status role — then emit, as flex children of the existing `.banner` row: one escaped `&lt;span&gt;` carrying the count-and-noun lead ("N warning(s)" / "N error(s)", from `_SEVERITY_BANNER_NOUNS`), then one `&lt;span class="banner__pill"&gt;` per label from `_anomaly_category_labels()`, each escaped once at interpolation. Document in the docstring why this page now builds its own banner rather than calling the shared helper: that helper escapes its whole message by design, which is correct for a plain-text banner and structurally incompatible with pill markup — this is a deliberate local builder, not a duplication to be "cleaned up" by re-routing it through the shared helper later.

    Rewrite `_corroboration_section()` so the three always-visible rows carry only the status dot, the label, and the monospaced count — drop the explanation clause and its separator from each row. Append one `&lt;details class="readings-disclosure"&gt;&lt;summary&gt;More details&lt;/summary&gt;` block holding a `&lt;dl&gt;` with one `&lt;dt&gt;`/`&lt;dd&gt;` pair per `_CORROBORATION_ROWS` entry (label, explanation), each escaped once. The explanations are preserved verbatim — D-08 collapses them, it does not delete them. Leave both early-return branches (`_DB_UNAVAILABLE`, and the no-counts empty state) exactly as they are, and leave the function's second return value (the disagreement flag) unchanged, since `collect_anomalies()` consumes it.

    Then update `companion/test_status_pages.py`: retarget the existing anomaly-banner checks onto the new pill markup (assert the banner carries the same class and role for each severity, one pill per anomaly, and that each pill's text is a real category label rather than one of `collect_anomalies()`'s full literal sentences); retarget the corroboration checks that assert an explanation appears inline onto asserting it appears inside the disclosure instead; and add new checks for the disclosure being closed by default (no `open` attribute) and for all three explanations still being present in the rendered output. Bump `EXPECTED_CHECK_COUNT` by the number of genuinely NEW checks only, computed from the file's real on-disk starting value at execution time.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: for a fixture producing two anomalies, the rendered Health page contains exactly two `banner__pill` elements inside one banner element.
    - Behaviour assertion: the error severity still yields the anomaly banner modifier class and `role="alert"`; the warn severity still yields the warn modifier and `role="status"` — identical to the mapping `layout.anomaly_banner()` applies today.
    - Behaviour assertion: the rendered page still contains `ANOMALY_BANNER_TEXT` when severity is non-ok (every existing presence check keeps passing).
    - Behaviour assertion: all three `_CORROBORATION_ROWS` explanation strings appear in the rendered page, inside a `readings-disclosure` details element that carries no `open` attribute.
    - Behaviour assertion: `_corroboration_section()`'s second return value is unchanged for the same input counts as before this task.
    - Test command: `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The banner names its categories as pills, the Corroboration explanations are collapsed rather than deleted, and every pre-existing severity/anomaly contract still holds.</done>
</task>

<task type="auto">
  <name>Task 2: battery chart default latest reading and labeled axes (D-09)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    companion/pages/health_page.py lines 274-365 (`battery_sparkline_svg()` in full — the 300x60 viewBox, the padding/step maths, the per-point label format string, the roving-tabindex logic, and the document-order comment explaining why hit targets must be painted last; the axis labels must not break any of these);
    companion/pages/health_page.py lines 692-706 (`_battery_readout_block()` and `BATTERY_READOUT_PLACEHOLDER` — the element `battery-trend.js` writes into and the prompt text D-09 replaces);
    companion/static/battery-trend.js (the whole file — specifically what it writes on pointer/keyboard reveal and what, if anything, it writes on blur/pointer-leave; D-09 requires reverting to the latest reading rather than blanking, so confirm whether the JS already reverts or whether the resting text simply persists);
    companion/layout.py::concise_timestamp_html (the clock-format convention the X-axis labels follow);
    companion/static/style.css `.sparkline-axis-label` (landed by plan 01 — confirm it exists and sets `fill`; this plan must not edit the stylesheet);
    companion/test_status_pages.py — the checks pinning the sparkline's viewBox, its polyline count, its no-external-reference guarantee, and `BATTERY_READOUT_PLACEHOLDER`
  </read_first>
  <action>
    Extend `battery_sparkline_svg()` to emit four `&lt;text class="sparkline-axis-label"&gt;` nodes and grow the viewBox to make room for them: reserve a left gutter for the two Y-axis labels (the maximum and minimum plotted millivolt values, each suffixed " mV") and a bottom strip for the two X-axis labels (the oldest and newest plotted reading's clock time). Derive the plot area from the new dimensions so the polyline, the cosmetic markers, and the hit targets all stay in correct relative position — do not hardcode the old padding against a larger box. Place the Y max label at the top of the plot area and the Y min label at its bottom, both left-aligned in the gutter; place the X oldest label at the plot area's left edge and the X newest at its right edge, both in the bottom strip with the newest right-anchored via `text-anchor`. No tick marks and no axis lines: UI-SPEC §5.3 states four text labels are sufficient.

    Every axis label carries `aria-hidden="true"` — the exact readings are already announced by each point's own `aria-label` and `&lt;title&gt;`, and duplicating them as loose text nodes would make a screen reader read the chart's extremes twice. Keep the function's existing guarantees intact and unweakened: still exactly one polyline, still zero external references of any kind, still no script tag and no readout element emitted from here. Derive the X-axis clock strings from the same `(value, ts)` pairs the points are built from, so a dropped non-numeric row cannot shift an axis label away from the point it describes.

    Change `_battery_readout_block()` to take the latest reading's label as an argument and seed the readout element's text with it, using the exact same per-point label format `battery_sparkline_svg()` already builds ("{value} mV — {ts}") so the resting text and the hover/tap text are word-identical. Retire `BATTERY_READOUT_PLACEHOLDER` as the seeded value; if any check or comment still references it, retarget rather than leave it pointing at dead code. Update `_battery_section()`'s call site to pass the chronologically-latest numeric row's label, and only on the branch where a chart actually exists — the no-chart branch still emits neither readout nor script, which is what keeps the "exactly one script tag, zero on the no-chart path" check true.

    If `companion/static/battery-trend.js` currently blanks the readout on blur or pointer-leave, that is out of this plan's `files_modified` scope; instead confirm by reading it whether the resting text simply persists (in which case D-09's "reverts to the latest reading" is already satisfied by seeding alone) and record the finding in the SUMMARY. Do not edit that file here.

    Then update `companion/test_status_pages.py`: retarget the viewBox check onto the new dimensions, retarget any `BATTERY_READOUT_PLACEHOLDER` check onto the seeded latest-reading text, and add new checks for exactly four axis-label text nodes, each carrying the axis-label class and `aria-hidden`, for the Y labels carrying the real min/max of the fixture's plotted values, and for the no-external-reference guarantee still holding on the extended SVG. Bump `EXPECTED_CHECK_COUNT` by the number of genuinely NEW checks only, from the real on-disk starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: `battery_sparkline_svg(rows)` for a multi-row fixture returns markup containing exactly four elements carrying the `sparkline-axis-label` class, each with `aria-hidden="true"`.
    - Behaviour assertion: two of those four labels contain the fixture's real minimum and maximum millivolt values.
    - Behaviour assertion: the returned SVG still contains exactly one `<polyline` and no `url(`, no `<image`, and no script tag.
    - Behaviour assertion: the rendered Health page's readout element's initial text equals the latest plotted point's own label, and is not the retired prompt text.
    - Behaviour assertion: for a fixture with fewer than two numeric readings, `battery_sparkline_svg()` still returns the empty string and the page emits neither a readout element nor a chart script tag.
    - Test command: `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The chart carries four correctly-positioned, screen-reader-silent axis labels and shows a real reading before any interaction, with every prior sparkline guarantee intact.</done>
</task>

<task type="auto">
  <name>Task 3: Screen / Server & data restructure, migrated diagnostics cards, and the Resolution-rate tile (D-10, D-11, D-12)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    companion/pages/airlines_page.py in full (the source of every migrated symbol: the no-gaps and no-stats copy constants, the read-only note, the filter-bar copy constants, `_safe_query()`, `unresolved_rows()`, `coverage_status()`, `resolution_stats()` with its window constant and source-row table, `_filter_bar_html()` with its deliberate non-button Clear control and the D-16 comment explaining it, `_registry_row_html()`, `_registry_table_html()`, `_registry_section()`, `_resolved_headline_html()`, `_stats_table_html()`);
    companion/pages/health_page.py lines 206-230 and 857-896 (`_DB_UNAVAILABLE`, `_safe_query()`, `_read_health_inputs()` — the single-sourced input dict D-11 explicitly forbids folding the migrated reads into, and the stale-view banner constant);
    companion/pages/health_page.py lines 898-957 (`render()` in full — the current header, stale banner, source-fault block, anomaly banner, single Overview heading, one dashboard-grid, and the battery section; this is the function being restructured);
    companion/layout.py::stat_tile and ::page_header (the tile signature including its status and icon keywords, and the header signature including its freshness keyword);
    companion/static/style.css `.stat-tile` lines 1584-1600 (read the load-bearing `min-width: 0` comment — it documents the exact horizontal-scroll failure mode D-11 diagnoses) and `.page-section` lines 1783-1810 (the container the migrated cards use instead);
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md §5.2, §5.5, §5.6 (the exact section order, the Resolution-rate tile's content and its icon-free/status-free choice, and the migrated cards' headings and order);
    companion/test_status_pages.py — the Airlines-page checks covering the registry, the stats table, the resolved headline, and the filter bar; these describe behaviour that now belongs to Health, and Health-side equivalents are what this task adds
  </read_first>
  <action>
    Copy — do not re-derive — the following from `companion/pages/airlines_page.py` into `companion/pages/health_page.py`: the no-gaps heading/body constants, the read-only note, the no-stats heading/body constants, the resolution window constant, the four-entry source-row table, `unresolved_rows()`, `coverage_status()`, `resolution_stats()`, `_registry_row_html()`, `_registry_table_html()`, `_registry_section()`, `_stats_table_html()`, and the filter bar builder (rename it `_registry_filter_bar_html()` so it does not collide with anything, and keep its `&lt;a href="#..." data-filter-clear&gt;` Clear control and its explanatory comment exactly as written — D-12 says the registry card's content and behaviour are unchanged, and `list-filter.js` keys on the attribute, not the element type). Preserve every function body's logic byte-for-byte; only the module they live in changes. `health_page.py` already defines an identically-shaped `_safe_query()` and `_DB_UNAVAILABLE`, so reuse Health's rather than copying Airlines' second copy, but keep the distinction D-11 warns about: the registry read goes through `poll_loop.load_poll_state()` and is NOT a `_safe_query()` call, because its failure mode is a filesystem/JSON one, not a SQLite one.

    Do NOT fold either migrated read into `_read_health_inputs()`. Add the registry read and the stats read as their own independent calls inside `render()`, each isolated exactly as it is on the page it comes from, and say so in a comment naming D-11 — merging them would make one query's failure take down sections that used to fail independently.

    Add `_resolution_rate_tile_html(stats)` producing the tile body UI-SPEC §5.5 specifies: the unavailable paragraph when the stats read failed, the existing no-stats empty state when the total is zero, and otherwise a `&lt;p class="stat-tile__value"&gt;` carrying the percentage plus a `&lt;p class="text-label"&gt;` carrying the window and event count — the same two lines the old promoted headline built, reused verbatim rather than reworded.

    Add the section constants: `SCREEN_SECTION_ID = "screen"`, `SCREEN_SECTION_HEADING = "Screen"`, `SERVER_DATA_SECTION_ID = "server-data"`, `SERVER_DATA_SECTION_HEADING = "Server & data"` (escaped once at interpolation, so the ampersand renders correctly), `RESOLUTION_RATE_LABEL = "Resolution rate"`, `UNRESOLVED_SECTION_HEADING = "Unresolved prefixes"`, `STATS_SECTION_HEADING = "Resolution statistics"`. Comment on `SERVER_DATA_SECTION_ID` that `companion/pages/history_page.py` links to this exact anchor for D-21 and that renaming it silently breaks that link — this is the one cross-page coupling this phase introduces.

    Restructure `render()` to emit, in order: the existing header with its freshness link, the stale-view banner, the source-fault block, and the anomaly banner (all four unchanged, all four above both sections); then an `&lt;h2&gt;` carrying the Screen id and heading, followed by the Device-freshness stat tile rendered standalone with no `dashboard-grid` wrapper (a single-tile grid row and a bare block-level tile render identically at full column width — skip the wrapper), followed by the battery-trend section exactly where it sits today; then an `&lt;h2&gt;` carrying the Server-and-data id and heading, followed by one `dashboard-grid` holding the Pipeline-freshness tile, the Corroboration tile, and the new Resolution-rate tile (built with `layout.stat_tile()` passing no status and no icon, matching the old Airlines stats tile's own precedent), followed by two `&lt;section class="page-section"&gt;` cards — Unresolved prefixes first, Resolution statistics second, each opening with an `&lt;h2 class="text-heading"&gt;` carrying its heading constant and holding the migrated section markup. Remove the now-inaccurate single "Overview" heading. Neither migrated card may be wrapped in `stat_tile()` or placed inside a `dashboard-grid` — that is the bug D-11 fixes.

    Leave `companion/pages/airlines_page.py` completely untouched by this plan. For exactly one wave both pages will render the registry; plan 06 removes it from Airlines next wave. Record that deliberate transient in the SUMMARY.

    Then update `companion/test_status_pages.py`: add Health-side checks for the two section headings existing with their expected id anchors and in the expected document order; for the Server-and-data grid holding exactly three tiles; for the Resolution-rate tile rendering the percentage and the window/event line for a seeded fixture, and rendering the no-stats empty state for an empty one; for each migrated card being a `page-section` and for neither appearing inside a `dashboard-grid` or a `stat-tile` in the rendered output; for the registry card still carrying its filter bar, its read-only note, and a non-button Clear control; and for per-section isolation — a fixture where the SQLite read fails but the registry file is readable must still render the registry rows, and vice versa. Leave the existing Airlines-page checks alone; plan 06 owns their retargeting. Bump `EXPECTED_CHECK_COUNT` by the number actually added, from the real on-disk starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: the rendered Health page contains exactly two `<h2` elements carrying `id` attributes equal to `SCREEN_SECTION_ID` and `SERVER_DATA_SECTION_ID`, in that document order, and contains no "Overview" heading.
    - Behaviour assertion: the page contains exactly one `dashboard-grid`, and it holds exactly three `stat-tile` elements.
    - Behaviour assertion: for a fixture with route-source counts seeded, the Resolution-rate tile renders a `stat-tile__value` containing a percentage and a `text-label` naming the window in days and the event count.
    - Behaviour assertion: the Unresolved-prefixes and Resolution-statistics blocks each render inside a `page-section` element, and neither substring appears anywhere between the `dashboard-grid` opening tag and its matching close.
    - Behaviour assertion: the registry card renders its filter bar, its read-only note, and a Clear control that is not a `button` element.
    - Behaviour assertion (isolation): with a fixture whose history database is unavailable but whose poll-state file is readable, the registry rows still render and only the stats card shows the unavailable copy; with the inverse fixture, the stats table still renders and only the registry degrades.
    - Source assertion: `_read_health_inputs()` gained no new key — the migrated reads are separate calls in `render()`.
    - Test command: `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>Health owns every server-and-data diagnostic, in the right containers, with per-section failure isolation preserved and the anchor History will link to in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→page (poll state) | `unresolved_prefixes` is read from a documented hand-editable JSON file and rendered as table rows. |
| database→page (history db) | Route-source counts and corroboration counts are read from SQLite and rendered as figures. |
| server→browser | Anomaly labels, prefixes, example callsigns, and timestamps are interpolated into HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-13 | Tampering (XSS) | migrated registry rows and new banner pills in `health_page.py` | medium | mitigate | Every migrated builder keeps its existing single-escaping-choke-point discipline — `escape_html()` exactly once at the point of interpolation, including the `data-filter-text` attribute value and each new pill's text. Already-safe markup returned by `layout.concise_timestamp_html()` and `layout.status_dot()` stays interpolated verbatim and is never re-escaped. |
| T-06.6.4.1-14 | Denial of Service | per-section database isolation | medium | mitigate | Keep the registry read (`poll_loop.load_poll_state()`) and the stats read (`_safe_query()`) independent, as D-11 requires. A locked, missing, or corrupt database must degrade one card, never raise out of `render()` and blank the whole page. Verified by Task 3's two inverse-fixture isolation checks. |
| T-06.6.4.1-15 | Information Disclosure | Health page content surface | low | accept | The page now aggregates more operational detail (prefixes, example callsigns, resolution rates) on one screen, but every item was already rendered to the same authenticated audience on the Airlines page; the merge changes location, not audience. The route stays behind the existing session gate. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; see `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit (not applicable). |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped count after each task.
2. The other three companion harnesses pass unmodified — this plan touches no route, no shared layout helper, and no other page module.
3. `git diff --stat` across this plan's commits touches exactly `companion/pages/health_page.py` and `companion/test_status_pages.py`; `git diff --quiet companion/pages/airlines_page.py companion/static/style.css companion/app.py companion/layout.py` reports no change.
4. `scripts/run-all-tests.sh` green apart from the single pre-existing `server/test_poll_loop.py` digest mismatch.
</verification>

<success_criteria>
- Two id-anchored sections, three tiles in one grid, two full-width migrated cards, in the documented order.
- Anomaly pills, collapsed Corroboration explanations, a chart that shows a number before you touch it and carries four axis labels.
- Per-section failure isolation demonstrably preserved by two inverse fixtures.
- Zero CSS edited, zero routes edited, `airlines_page.py` untouched.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan** (all in `companion/pages/health_page.py`): `SCREEN_SECTION_ID`, `SCREEN_SECTION_HEADING`, `SERVER_DATA_SECTION_ID`, `SERVER_DATA_SECTION_HEADING`, `RESOLUTION_RATE_LABEL`, `UNRESOLVED_SECTION_HEADING`, `STATS_SECTION_HEADING`, `RESOLUTION_WINDOW_DAYS`; `_anomaly_category_labels()`, `_anomaly_banner_html()`, `_corroboration_details_html()`, `_resolution_rate_tile_html()`; the symbols migrated in from the Airlines page — `unresolved_rows()`, `coverage_status()`, `resolution_stats()`, `_registry_row_html()`, `_registry_table_html()`, `_registry_section()`, `_registry_filter_bar_html()`, `_stats_table_html()` and their copy/window/source-row constants; `_battery_readout_block()`'s new latest-reading argument and `battery_sparkline_svg()`'s four axis labels and larger viewBox.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-04-SUMMARY.md` when done
</output>
