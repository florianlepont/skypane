# Phase 06.6.4.1 — Artifacts this phase produces

Phase-wide symbol inventory. Every plan in this phase references this file from its own
`## Artifacts this phase produces` section. Each entry is tagged with the plan that owns it.

## New files

- `companion/static/panel-lookup.js` — plan 02

## Deleted files

- `companion/pages/preview_page.py` — plan 08

## Routes

| Route | Change | Plan |
|---|---|---|
| `GET /settings` | New path for the Settings page (renames the old settings page route; the old path 404s by design — D-26 declines a redirect) | 07 |
| `GET /illustration/{key}.png` | New, authenticated, membership-validated image route | 02 |
| `GET /static/panel-lookup.js` | New pre-auth static asset route | 02 |
| `POST /config-led` | Retired — falls through to 404 | 07 |
| `GET /preview` | Retired as a page — fixed 302 to `/history` | 08 |
| `GET /preview.png`, `GET /gallery/{name}.png`, `GET /runway-image/{id}.png` | Unchanged and still authenticated | — |

## `server/plane/illustrations.py`

- `target_variants_by_airline()` — plan 02

## `companion/app.py`

- `ILLUSTRATION_IMAGE_ROUTE_PREFIX`, the module-level illustration filename membership frozenset, `Handler._serve_illustration_image()` — plan 02
- `PANEL_LOOKUP_SCRIPT_ROUTE`, `_PANEL_LOOKUP_JS_PATH`, `Handler._serve_panel_lookup_script()` — plan 02
- `SETTINGS_ROUTE` (rebound from `config_page.SETTINGS_ROUTE`, replacing the old settings route constant); `_PAGE_TITLES` settings key retargeted — plan 07
- Removed: the separate LED route constant and its `do_POST` branch — plan 07
- Preview page path retained solely as a fixed-redirect source; `_PAGE_TITLES` preview entry removed; the preview page module import removed — plan 08

## `companion/layout.py`

- `PANEL_LOOKUP_SCRIPT_SRC`; a sixth deferred `<script>` tag in `page_shell()` — plan 02
- `NAV_TABS` first entry retargeted to the settings route and the "Settings" label; `NAV_ICON_IDS` first key retargeted to the settings slug — plan 07
- `NAV_TABS` shrunk from five entries to four; `NAV_ICON_IDS` preview key removed. The eye glyph stays in `ICON_IDS` — its consumer is now History's View-panel trigger, not a nav tab — plan 08

## `companion/pages/config_page.py`

- `SETTINGS_ROUTE` (the single definition of this path in the repository; `app.py` rebinds it), `THEME_SECTION_DESCRIPTION`, `RUNWAY_SECTION_DESCRIPTION`, `LED_SECTION_HEADING`, `DIRTY_SECTION_ATTR`, `STATIC_SAVE_FALLBACK_ATTR`, `DIRTY_BAR_INITIAL_TEXT`, `led_group()`; `handle_post()` extended to validate and persist the LED field in the same single save call — plan 03
- Removed: `led_fieldset()`, `led_section()`, `handle_led_post()` — plan 07

## `companion/pages/health_page.py`

- `SCREEN_SECTION_ID`, `SCREEN_SECTION_HEADING`, `SERVER_DATA_SECTION_ID`, `SERVER_DATA_SECTION_HEADING`, `RESOLUTION_RATE_LABEL`, `UNRESOLVED_SECTION_HEADING`, `STATS_SECTION_HEADING`, `RESOLUTION_WINDOW_DAYS` — plan 04
- `_anomaly_category_labels()`, `_anomaly_banner_html()`, `_corroboration_details_html()`, `_resolution_rate_tile_html()` — plan 04
- Migrated in from the Airlines page: `unresolved_rows()`, `coverage_status()`, `resolution_stats()`, `_registry_row_html()`, `_registry_table_html()`, `_registry_section()`, `_registry_filter_bar_html()`, `_stats_table_html()`, and their copy, window and source-row constants — plan 04
- `_battery_readout_block()` gains a latest-reading argument; `BATTERY_READOUT_PLACEHOLDER` retired as the seeded value; `battery_sparkline_svg()` emits four axis labels and a larger viewBox — plan 04

## `companion/pages/history_page.py`

- `NOW_SHOWING_HEADING`, `RECENT_RENDERS_SUMMARY_TEMPLATE`, `VIEW_PANEL_LABEL`, `LIGHTBOX_DIALOG_ID`, `LIGHTBOX_CAPTION_TEMPLATE`, `LIGHTBOX_NOTE`, `UNRESOLVED_LINK_HREF`, `UNRESOLVED_LINK_TEXT` — plan 05
- `nearest_gallery_entry()`, `_view_panel_button_html()`, `_lightbox_html()`, `_type_airline_cell()` — plan 05
- Migrated in from the Preview page: `COLOUR_CAVEAT`, `GALLERY_DISPLAY_LIMIT`, `_gallery_name_to_iso()`, `preview_section()`, `gallery_tiles()`, and their panel-dimension, route-literal, no-panel and no-renders constants — plan 05

## `companion/pages/airlines_page.py`

- `ILLUSTRATION_ROUTE_PREFIX`, `GALLERY_PURPOSE_TEXT`, `CARD_IMAGE_ALT_TEMPLATE`, `variant_chip_label()`, `_airline_card_html()`, `_gallery_grid_html()`, and a gallery-variant `_filter_bar_html()` with its four copy constants — plan 06
- Removed: every diagnostics symbol migrated to `health_page.py`, plus the now-unused database, poll-state, sqlite and datetime imports — plan 06

## `companion/static/style.css`

All plan 01 — the phase's only stylesheet write site. No other plan may edit this file.

- `.illustration-grid`, `.airline-card`, `.airline-card__image`, `.airline-card__name`, `.airline-card__chips`, `.airline-card__chip`
- `.lightbox`, `.lightbox__image`, `.lightbox__caption`, `.lightbox__note`, `.lightbox::backdrop`
- `.sparkline-axis-label`, `.banner__pill`, and the `.js`-gated static-save-fallback hide rule
- Removed: the `.config-form` two-column grid rule group

## New cross-file HTML attribute contracts

| Attribute | Emitted by | Consumed by | Plans |
|---|---|---|---|
| `data-dirty-section` | `config_page.py` | `dirty-state.js` | 03 |
| `data-static-save-fallback` | `config_page.py` | a `style.css` rule gated on the `js` class | 01, 03 |
| `data-view-panel-src` | `history_page.py` | `panel-lookup.js` | 02, 05 |
| `data-view-panel-caption` | `history_page.py` | `panel-lookup.js` | 02, 05 |
| `data-view-panel-close` | `history_page.py` | `panel-lookup.js` | 02, 05 |

## Test harnesses touched

`server/test_illustrations.py` (plan 02), `companion/test_companion_app.py` (plans 02, 07, 08),
`companion/test_config_page.py` (plans 03, 07), `companion/test_status_pages.py` (plans 04, 06),
`companion/test_view_pages.py` (plans 05, 08). Every `EXPECTED_CHECK_COUNT` bump is computed from
the file's real on-disk value at execution time — never from a number predicted at planning time.
