---
phase: 06.6.4.1
plan: 06
type: execute
wave: 3
depends_on: ["06.6.4.1-01", "06.6.4.1-02", "06.6.4.1-04"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [D-13, D-14, D-15, D-16, D-17]

must_haves:
  truths:
    - "The /airlines route renders one card per airline in the illustration target list — 27 today — each showing that airline's real already-shipped PNG through the session-gated illustration route, its name, and one chip per fleet-type variant it has (D-13, D-14)."
    - "An airline with no variant entries renders no chip container at all, and the a350-1000 variant survives — chips are never validated against the seven-member shape-slug classification (D-14)."
    - "A filter bar searches the cards by airline name using the existing shared attribute contract, with no script change (D-16)."
    - "The old diagnostics content is GONE from this page — after this plan exactly one page in the app renders the unresolved-prefix registry and the resolution-statistics breakdown (D-13)."
    - "The gallery shows the full static curated list and performs no detection-history cross-reference — the page module opens no database and reads no poll state (D-17)."
  artifacts:
    - "companion/pages/airlines_page.py::ILLUSTRATION_ROUTE_PREFIX, GALLERY_PURPOSE_TEXT, CARD_IMAGE_ALT_TEMPLATE, variant_chip_label(), _airline_card_html(), _gallery_grid_html(), _filter_bar_html()"
  key_links:
    - "This plan closes the one-wave window plan 04 deliberately opened: for wave 2 both Health and Airlines rendered the registry. D-13 forbids that steady state, so removing the migrated functions here is not cleanup — it is the second half of the move, and it must land before this phase's closing checkpoint."
    - "ILLUSTRATION_ROUTE_PREFIX must equal companion/app.py's ILLUSTRATION_IMAGE_ROUTE_PREFIX exactly. app.py imports page modules, so the reverse import is a cycle — the equality is held by the same duplicated-not-imported discipline this codebase already uses for its static-script route constants, and must be pinned by a check."
    - "Chips come from server/plane/illustrations.py's new public accessor, never from a private-name read and never from a second hand-typed per-airline shape table — that accessor is the reason plan 02 exists."
---

<objective>
Replace the Airlines page's entire content: out goes the diagnostics that moved to Health in the previous wave, in comes a visual gallery of the panel renderer's own already-shipped illustration set, with a filter bar over it.

Purpose: the nav slot survives repurposed rather than removed, so navigation stays at four items instead of shrinking to three — and the gallery answers a question nothing in the app answered before: which airlines does this frame actually have art for?
Output: an Airlines page module that reads one static in-memory list and renders images, with no database access of any kind.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/pages/airlines_page.py
@companion/pages/history_page.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: gallery grid, cards, and fleet-variant chips (D-13, D-14, D-15 consumption)</name>
  <files>companion/pages/airlines_page.py, companion/test_status_pages.py</files>
  <read_first>
    server/plane/illustrations.py — `target_airline_names()`, `target_filenames()`, `normalise_airline_key()`, and the `target_variants_by_airline()` accessor plan 02 added (read the new function's real signature and return shape, not an assumed one), plus the `SHAPE_SLUGS` tuple and the surrounding comments explaining that it is a DIFFERENT domain from the free-text variant strings;
    companion/app.py — `ILLUSTRATION_IMAGE_ROUTE_PREFIX` and `_serve_illustration_image()` as plan 02 landed them (take the route prefix's real value and confirm the membership set is built from full filenames including the extension, since the card's URL must produce a key that passes that test);
    companion/pages/config_page.py lines 176-247 (`runway_fieldset()` — this codebase's existing precedent for rendering a session-gated image route into a card, including its alt-text template constant and its escaping discipline);
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md §7.0 and §7.1 (the page header's purpose sentence, the exact card markup, the image loading hints, and the chip display transform rules);
    companion/static/style.css — the illustration grid and airline card classes plan 01 landed (confirm all six exist; this plan must not edit the stylesheet);
    companion/test_status_pages.py — its Airlines-page section in full, and the file's real `EXPECTED_CHECK_COUNT`
  </read_first>
  <action>
    Rewrite `companion/pages/airlines_page.py`'s module docstring first, because the current one states a constraint this decision retires: the old D-16 read-only rule forbidding any form or button element applied to the diagnostics registry, not to a gallery, and UI-SPEC §7.2 explicitly directs this page to use the standard clear-control button. Replace it with an accurate statement of what the module now is — a presentation-only gallery over the panel renderer's own illustration target list, reading one public accessor and touching no database and no filesystem — and record that the registry and statistics content moved to `companion/pages/health_page.py` in this same phase, so a reader hunting for it knows where it went.

    Change the imports to match: `from server.plane import illustrations` plus `companion.layout` and its escaping helper. Every database and poll-state import this module currently carries becomes unused and is removed with the functions in Task 3.
    <!-- planner-discipline-allow: history_db -->
    <!-- planner-discipline-allow: poll_loop -->
    <!-- planner-discipline-allow: sqlite3 -->

    Add `ILLUSTRATION_ROUTE_PREFIX = "/illustration/"` carrying the duplicated-not-imported must-equal comment (naming `companion/app.py`'s own prefix constant as the other side, and the import cycle as the reason it is duplicated rather than imported), `GALLERY_PURPOSE_TEXT = "Illustration reference for every airline this frame can recognize."`, and `CARD_IMAGE_ALT_TEMPLATE = "%s illustration"`.

    Add `variant_chip_label(shape)` implementing UI-SPEC §7.1's display transform: an alphanumeric type code uppercases verbatim, so the A320/ATR72/A330/B737 forms and the hyphenated A350-1000 form all render as their upper-cased selves; a word-form shape title-cases, so the Embraer and Beechcraft-1900D forms read as words. Derive the branch from the string's own shape (whether it is a manufacturer word or a type code), and state in the docstring the trap this function must not fall into: these strings are free-text filename suffixes from the illustration target list, a different domain from the seven-member shape-slug classification used for aircraft-type mapping, so no membership test against that tuple may gate a chip — doing so silently drops the A350-1000 variant, which is a real entry.

    Add `_airline_card_html(airline_name, shapes)` producing one card: an image with the card image class, a source built from `ILLUSTRATION_ROUTE_PREFIX` joined with the escaped key `illustrations.normalise_airline_key()` returns for that airline plus the PNG extension, the panel-appropriate lazy-loading and async-decoding hints, and an alt built from `CARD_IMAGE_ALT_TEMPLATE`; a name paragraph with the card name class; and, only when the shapes list is non-empty, a chips container holding one chip span per shape. An airline with no variants renders no chips container at all, not an empty one. Every interpolated value — the key, the name, each chip label, the alt text — goes through the escaping helper exactly once at the point of interpolation. Skip (do not crash on) any airline whose normalised key comes back falsy, mirroring `target_filenames()`'s own documented skip discipline.

    Add `_gallery_grid_html(pairs)` wrapping the cards in one element carrying the illustration grid class, and rewrite `render(ctx)` to emit the page header with the title "Airlines" and `GALLERY_PURPOSE_TEXT` as its purpose, followed by the grid built from `illustrations.target_variants_by_airline()`. Task 2 inserts the filter bar between them.

    Then add checks to `companion/test_status_pages.py`: the rendered page contains exactly as many card elements as `illustrations.target_airline_names()` has entries; every card's image source starts with the route prefix and ends with the PNG extension, and the filename portion of every one is a member of `illustrations.target_filenames()` (so every rendered URL provably passes the route's own membership test); the Air Caraïbes card carries exactly three chips reading as the upper-cased A330, A350-1000 and ATR72 forms; a single-entry airline's card carries no chips container; every airline name appears exactly once; and `airlines_page.ILLUSTRATION_ROUTE_PREFIX` equals `app.ILLUSTRATION_IMAGE_ROUTE_PREFIX`. Bump `EXPECTED_CHECK_COUNT` by the number of genuinely NEW checks only, computed from the file's real on-disk starting value at execution time.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: `airlines_page.render(ctx)` contains exactly `len(illustrations.target_airline_names())` card elements — 27 against today's data.
    - Behaviour assertion: for every rendered image source, stripping the route prefix yields a string that is a member of `illustrations.target_filenames()`.
    - Behaviour assertion: the Air Caraïbes card renders exactly three chips whose texts are the upper-cased A330, A350-1000 and ATR72 forms; the A350-1000 chip is present (the shape-slug-validation trap is not fallen into).
    - Behaviour assertion: an airline with no variant entries renders no chips-container element.
    - Behaviour assertion: `variant_chip_label` maps the Embraer and Beechcraft forms to word-cased labels and every alphanumeric type code to its upper-cased self.
    - Cross-module assertion: `airlines_page.ILLUSTRATION_ROUTE_PREFIX == app.ILLUSTRATION_IMAGE_ROUTE_PREFIX`.
    - Test command: `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>27 cards render real, route-servable images with correct names and chips; every rendered URL provably passes the image route's own membership test.</done>
</task>

<task type="auto">
  <name>Task 2: gallery filter bar and its empty-result state (D-16)</name>
  <files>companion/pages/airlines_page.py, companion/test_status_pages.py</files>
  <read_first>
    companion/pages/history_page.py lines 314-346 (`_filter_bar_html()` — the shared attribute contract's canonical shape, including the search input, the live count span, the button-type clear control, and the hidden empty-state block; this is the variant UI-SPEC §7.2 directs this page to copy);
    companion/static/list-filter.js in full (the generic attribute contract: which selectors it queries, how it counts distinct groups rather than raw elements, and its early-return guard — confirm no script change is needed);
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md §7.2 (the exact five copy strings and the input id);
    companion/layout.py::empty_state and ::icon_html (the empty-state builder's signature and the search glyph id)
  </read_first>
  <action>
    Add the filter copy constants matching UI-SPEC §7.2 exactly: an input id of `airlines-gallery-filter-input`, a label of "Filter by airline name", an empty heading of "No matching airlines", and an empty-body template producing "Try a different search, or Clear filter to see all {N} airlines." with the real card count substituted. Add `_filter_bar_html(total)` copying History's variant of the shared contract — the label bound to the input id, the search input carrying the filter-input attribute with the decorative search glyph beside it, the live count span carrying the filter-count attribute seeded with the existing "N of N shown" wording, a clear control that is a plain button carrying the filter-clear attribute, and a hidden empty-state block carrying the filter-empty attribute. Use the button variant, not the anchor variant the old diagnostics page used: that anchor existed only because the old page was forbidden any button element, and that constraint is retired with the page it applied to.

    Give every card a filter group: each card element carries a filter-text attribute holding the lower-cased airline name, escaped before interpolation, and a filter-group attribute holding that card's index. This page renders one representation per airline, but the shared script counts distinct groups rather than raw elements, so every filterable card must still carry one — the same reasoning the old registry rows' own comment records.

    Emit the filter bar between the page header and the grid in `render()`. Render no filter chrome at all when there are zero cards, matching this codebase's consistent "no chrome with no data" rule — though with a static curated list that branch is unreachable today, so keep it a genuine guard and not a claim that the list can be empty.

    Then add checks to `companion/test_status_pages.py`: the filter bar renders with all four contract attributes present exactly once each; the clear control is a button element; the label's for-attribute equals the input's id; the count text and the empty-body text both name the real card total; and every card carries a filter-text attribute equal to its own lower-cased airline name plus a distinct filter-group value. Bump `EXPECTED_CHECK_COUNT` by the number actually added, from the real on-disk starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: the rendered page contains exactly one element carrying each of the filter-input, filter-count, filter-clear and filter-empty attributes.
    - Behaviour assertion: the clear control is a `button` element with `type="button"`.
    - Behaviour assertion: the label's `for` attribute equals the search input's `id`, and that id is the value UI-SPEC §7.2 names.
    - Behaviour assertion: the count text and the empty-state body both contain the real number of rendered cards.
    - Behaviour assertion: every card carries a filter-text attribute equal to its own lower-cased airline name, and the set of filter-group values has the same size as the card count.
    - Source assertion: `companion/static/list-filter.js` is byte-identical to its pre-task state — no script change was needed.
    - Test command: `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The gallery filters by airline name through the existing shared script with zero script changes, and its empty state names the real total.</done>
</task>

<task type="auto">
  <name>Task 3: remove the migrated diagnostics content and pin the D-17 and D-13 non-goals</name>
  <files>companion/pages/airlines_page.py, companion/test_status_pages.py</files>
  <read_first>
    companion/pages/health_page.py — the migrated symbols as plan 04 actually landed them (confirm each one this task deletes has a live counterpart there BEFORE deleting it; a symbol that failed to migrate must be reported, not silently dropped);
    companion/pages/airlines_page.py — the full current set of diagnostics symbols: the coverage and stats copy constants, the read-only note, the shared unavailable text, the resolution window constant, the source-row table, the database sentinel and its query helper, the registry row/table/section builders, the old filter bar, the resolved headline, and the statistics table builder;
    companion/test_status_pages.py — every check that exercises an Airlines-page diagnostics symbol (search for the registry, the statistics table, the resolved headline, the coverage status, and the old filter input id) and the file's own EXPECTED_CHECK_COUNT history comment block, which documents this project's discipline for distinguishing new, retargeted and deleted checks
  </read_first>
  <action>
    Delete from `companion/pages/airlines_page.py` every symbol whose live counterpart now exists in `companion/pages/health_page.py`: the coverage-gap heading and body, the read-only note, the shared unavailable copy, the no-stats heading and body, the resolution window constant, the four-entry source-row table, the database sentinel and the query helper, the registry rows reader, the coverage status helper, the resolution statistics computation, the registry row builder, the registry table builder, the registry section builder, the old prefix filter bar and its four copy constants, the resolved headline builder, and the statistics table builder. Delete the now-unused database, poll-state, sqlite and datetime imports along with them. This is not cleanup — it is the second half of a move, and leaving them behind would leave two pages rendering the same registry, which is the steady state D-13 forbids.

    Before deleting each one, confirm its counterpart is live in `health_page.py`. If any symbol turns out not to have migrated, stop and report it in the SUMMARY rather than deleting it — a silently dropped diagnostic is a regression, not a simplification.

    Add a short module-level note recording where the deleted content went and which decision moved it, so `git log` is not the only trail.

    Then update `companion/test_status_pages.py`: delete every check that exercised a now-deleted Airlines symbol (their Health-side equivalents were added by plan 04 — verify that before deleting, do not assume it), and add two non-goal regression guards. First, D-17: read `companion/pages/airlines_page.py` from disk and assert it contains no import of the history database module, no import of the poll-state module, and no sqlite import — the gallery shows the full static curated list and performs no detection-history cross-reference. Second, D-13: assert the rendered Airlines page contains none of the registry table's column headers and none of the statistics table's own headers, so the two pages provably do not both render that content. Recompute `EXPECTED_CHECK_COUNT` from the file's real on-disk value at execution time, subtracting deletions and adding new checks, and extend the file's own history-comment block in its established format recording which checks were new, which were retargeted in place, and which were deleted.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (D-17 non-goal): `companion/pages/airlines_page.py` contains no import of the history-database module, no import of the poll-state module, and no sqlite import.
    - Behaviour assertion (D-13 non-goal): the rendered Airlines page contains none of the unresolved-prefix table's column headers and none of the resolution-statistics table's column headers.
    - Behaviour assertion: the rendered Health page still contains both of those header sets — the content moved, it was not lost.
    - Source assertion: importing `companion.pages.airlines_page` raises no error and the module exposes none of the deleted diagnostics symbols.
    - Test command: `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the recomputed `EXPECTED_CHECK_COUNT`, and the file's history comment records the new/retargeted/deleted split.
  </acceptance_criteria>
  <done>Exactly one page in the app renders the registry and the statistics; the gallery page opens no database; both non-goals are pinned by guards rather than by intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static data→page | Airline names and variant shape strings come from a hardcoded, developer-controlled list in `server/plane/illustrations.py` and are interpolated into HTML. |
| page→image route | Each card emits a URL whose key portion is consumed by the session-gated illustration route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-05 | Tampering (XSS) | airline names and chip labels in `_airline_card_html()` | medium | mitigate | Every interpolated value — the normalised key inside the URL, the display name, each chip label, and the alt text — goes through `escape_html()` exactly once at the point of interpolation, matching every other page module's documented discipline. The data is developer-controlled today, but a future edit to that table is still "just Python source" a reviewer might not re-audit for HTML safety, so the escaping is unconditional rather than justified away. |
| T-06.6.4.1-18 | Information Disclosure | rendered image URLs | low | mitigate | The page emits only keys derived from `normalise_airline_key()` over the same closed list the route's own membership set is built from — asserted by Task 1's "every rendered filename is a member of `target_filenames()`" check. The page itself never joins a filesystem path and never reads a directory. |
| T-06.6.4.1-19 | Information Disclosure | D-17 non-goal | low | mitigate | The gallery deliberately performs no detection-history cross-reference, so the page discloses nothing about what this specific installation has actually observed. Pinned by Task 3's import guard rather than left as an intention. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; see `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit (not applicable). |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 at the bumped/recomputed count after each task.
2. The other three companion harnesses pass unmodified — this plan touches no route, no shared script, and no other page module.
3. `git diff --stat` across this plan's commits touches exactly `companion/pages/airlines_page.py` and `companion/test_status_pages.py`; `git diff --quiet companion/static/list-filter.js companion/static/style.css companion/app.py companion/pages/health_page.py` reports no change.
4. Live spot check against a running local instance: request one card's image URL as an authenticated session and confirm 200 with a PNG body, then request the same URL with the key altered and confirm 404 — the route the cards point at behaves as plan 02 built it.
5. `scripts/run-all-tests.sh` green apart from the single pre-existing `server/test_poll_loop.py` digest mismatch.
</verification>

<success_criteria>
- 27 cards, correct chips including the A350-1000 variant, every image URL provably servable by the illustration route.
- A working filter bar over the cards with zero changes to the shared script.
- Every migrated diagnostics symbol deleted here and confirmed live in Health first.
- Both non-goals (no history cross-reference, no duplicated registry) pinned by guards.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan** (all in `companion/pages/airlines_page.py`): `ILLUSTRATION_ROUTE_PREFIX`, `GALLERY_PURPOSE_TEXT`, `CARD_IMAGE_ALT_TEMPLATE`, `variant_chip_label()`, `_airline_card_html()`, `_gallery_grid_html()`, and a gallery-variant `_filter_bar_html()` with its four copy constants. Removed by this plan: every diagnostics symbol migrated to `health_page.py` in plan 04, plus the now-unused database, poll-state, sqlite and datetime imports.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-06-SUMMARY.md` when done
</output>
