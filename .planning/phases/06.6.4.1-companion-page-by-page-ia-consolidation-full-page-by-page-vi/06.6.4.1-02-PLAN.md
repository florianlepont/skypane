---
phase: 06.6.4.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plane/illustrations.py
  - server/test_illustrations.py
  - companion/app.py
  - companion/layout.py
  - companion/static/panel-lookup.js
  - companion/test_companion_app.py
autonomous: true
requirements: [D-14, D-15, D-20]

must_haves:
  truths:
    - "A public accessor on server/plane/illustrations.py returns one (airline_name, [variant_shape, ...]) pair per distinct airline, derived from the single source-of-truth target list — no second hand-typed per-airline shape table exists anywhere (D-14)."
    - "GET /illustration/{key}.png returns a real PNG only for a key whose filename is a member of the module's own precomputed filename list; an unknown key and a real key with no file on disk both return the identical 404, and an unauthenticated request gets the login redirect, never the image (D-15)."
    - "A new ES5-safe static script is served pre-auth and is inert on every page that carries no lightbox dialog — exactly like the five static scripts already shipped."
  artifacts:
    - "server/plane/illustrations.py::target_variants_by_airline()"
    - "companion/app.py::ILLUSTRATION_IMAGE_ROUTE_PREFIX, _illustration_filenames(), Handler._serve_illustration_image()"
    - "companion/app.py::PANEL_LOOKUP_SCRIPT_ROUTE, _PANEL_LOOKUP_JS_PATH, Handler._serve_panel_lookup_script()"
    - "companion/layout.py::PANEL_LOOKUP_SCRIPT_SRC and a sixth deferred <script> tag in page_shell()"
    - "companion/static/panel-lookup.js"
  key_links:
    - "The illustration route's membership test must happen BEFORE any path is constructed — validate-then-join, never sanitise-then-join. companion/app.py::_serve_runway_image() is the exact shape to mirror; illustration_path_for_key()'s own _UNSAFE_KEY_RE is defence in depth, never a substitute for the route-level membership check."
    - "This plan is purely additive: it adds two routes, one accessor, one static file, and one script tag. It renames nothing and deletes nothing, so it can land in wave 1 before any consumer exists — the same shape 06.6.3-01 used when it wired four new static JS routes ahead of the pages that consume them."
---

<objective>
Land the three shared, purely-additive pieces waves 2 and 3 depend on: the illustrations data accessor the Airlines gallery renders from (D-14), the session-gated validate-then-join image route that serves its PNGs (D-15), and the lightbox script plus its pre-auth static route that History's per-row panel lookup drives (D-20).

Purpose: `companion/app.py` and `companion/layout.py` are wanted by three different page plans. Landing every additive route/constant here, first, is what lets waves 2 and 3 run three page plans in parallel; the destructive route work (rename, retirement, nav shrink) is deliberately deferred to plans 07 and 08 so nothing in this plan can leave the running service inconsistent.
Output: two new routes, one new public accessor, one new static JS file, one new script tag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-RESEARCH.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/app.py
@server/plane/illustrations.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: public per-airline fleet-variant accessor on server/plane/illustrations.py (D-14)</name>
  <files>server/plane/illustrations.py, server/test_illustrations.py</files>
  <read_first>
    server/plane/illustrations.py lines 700-755 (`target_airline_names()` and `target_filenames()` — the two existing public accessors whose de-dup, order-preserving discipline and docstring shape the new function must match exactly);
    server/plane/illustrations.py lines 241-465 (`_ILLUSTRATION_TARGETS` — the real flat list of (airline_name, shape_or_None, note) tuples, so the grouping is written against the real data, not an assumed shape);
    server/plane/illustrations.py lines 465-482 (`_UNSAFE_KEY_RE` and `SHAPE_SLUGS` — read specifically to confirm SHAPE_SLUGS is NOT the validation set for variant shapes; `a350-1000` is a real variant that is deliberately not a SHAPE_SLUGS member);
    server/test_illustrations.py (the whole file — its `check(name, fn)` harness shape and its `EXPECTED_CHECK_COUNT` constant's real on-disk value at execution time)
  </read_first>
  <behavior>
    - `target_variants_by_airline()` returns a list of `(airline_name, [shape, ...])` pairs.
    - Exactly 27 pairs, one per distinct airline, in `_ILLUSTRATION_TARGETS` first-appearance order — the same order and length `target_airline_names()` produces.
    - Air Caraïbes' pair carries exactly three shapes, in list order: `a330`, `a350-1000`, `atr72`.
    - Air France (a single-entry airline with a `None` shape) carries an empty list, not a list holding `None`.
    - No returned shape string is dropped for failing a `SHAPE_SLUGS` membership test — `a350-1000` must survive.
  </behavior>
  <action>
    Add a new public function `target_variants_by_airline()` to `server/plane/illustrations.py`, placed immediately after `target_filenames()` so the three read-only accessors sit together. It groups `_ILLUSTRATION_TARGETS` by its first tuple element, preserving first-appearance order, and collects each entry's non-`None` second element (the shape) into that airline's list. Return a list of two-tuples, not a dict, so ordering is part of the contract rather than an implementation detail of dict iteration.

    Write the docstring in the same register as its two neighbours: state that it derives from `_ILLUSTRATION_TARGETS` directly and never from a second hardcoded list, and state the one real trap explicitly — the shape values here are free-text filename suffixes, a different domain from `SHAPE_SLUGS`' seven-member ICAO-type classification, so a caller must not validate a variant against `SHAPE_SLUGS` before displaying it; `a350-1000` is a real entry that such a check would silently drop.

    This function is the sole entry point `companion/pages/airlines_page.py` will use in plan 06, so `_ILLUSTRATION_TARGETS` stays private and this project's consistent "no cross-module private-name reach" pattern holds.

    Then add checks to `server/test_illustrations.py` covering every bullet in this task's behaviour block. Bump `EXPECTED_CHECK_COUNT` by the number of checks actually added, computed from the file's real on-disk starting value at execution time — never a predicted final number.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_illustrations.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: `target_variants_by_airline()` returns 27 pairs and `[n for n, _ in target_variants_by_airline()] == target_airline_names()`.
    - Behaviour assertion: the Air Caraïbes pair's shape list equals `["a330", "a350-1000", "atr72"]`.
    - Behaviour assertion: no returned shape list contains `None`, and an airline with no variant entry maps to `[]`.
    - Source assertion: `_ILLUSTRATION_TARGETS` is referenced by the new function and no new hardcoded airline-to-shape literal table was introduced.
    - Test command: `server/.venv/bin/python3 server/test_illustrations.py` exits 0 and its reported count equals the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>One new public accessor exists alongside the existing two, returns the real grouped data in the real order, and is covered by new checks in the canonical illustrations harness.</done>
</task>

<task type="auto">
  <name>Task 2: session-gated validate-then-join illustration image route (D-15)</name>
  <files>companion/app.py, companion/test_companion_app.py</files>
  <read_first>
    companion/app.py lines 699-715 (`_serve_runway_image()` — the exact validate-then-join shape this route mirrors, including the deliberate identical-404 comment);
    companion/app.py lines 297-335 (`_runway_image_filename()`, `_runway_image_path()`, `runway_images_available()` — the module-level helper layering the new route copies);
    companion/app.py lines 852-868 (do_GET's tail: the three byte-serving route blocks, each preceded by its own `require_session()` gate, and the final 404 — the new block goes here, inside the gated region, never in the pre-auth static-asset list above);
    companion/app.py lines 100-115 (the route-constant block, including `RUNWAY_IMAGE_ROUTE_PREFIX`'s rebinding comment — the naming convention the new prefix constant follows);
    server/plane/illustrations.py lines 579-592 (`illustration_path_for_key()` and its `_UNSAFE_KEY_RE` guard — the defence-in-depth layer, read so the route is written to use it rather than joining a path itself);
    companion/test_companion_app.py — the existing live-HTTP runway-image route checks (grep for the runway image route prefix) and the file's real `EXPECTED_CHECK_COUNT`
  </read_first>
  <action>
    Add to `companion/app.py`'s route-constant block: `ILLUSTRATION_IMAGE_ROUTE_PREFIX = "/illustration/"`, matching `RUNWAY_IMAGE_ROUTE_PREFIX`'s existing naming convention.

    Add a module-level helper `_illustration_filenames()` that returns the known-safe membership set by calling `illustrations.target_filenames()` and wrapping it in a `frozenset`. Compute it once at module import into a module-level constant rather than per request — `target_filenames()` performs no I/O, but a single materialised set makes the "one closed, server-controlled list" property visible at a glance. Import the module as `from server.plane import illustrations`; `companion/app.py` already imports four `server.*` modules, and `companion/pages/__init__.py`'s only documented import boundary is page-module-to-page-module, so this crosses no rule.

    Add `Handler._serve_illustration_image(self, key)` immediately after `_serve_runway_image()`. Its body mirrors that method statement for statement: build the candidate filename as the key plus the `.png` suffix, membership-test it against the precomputed set FIRST and return `self.send_html(404, self._not_found_page())` on a miss, only then call `illustrations.illustration_path_for_key(key)`, open the file in binary mode inside a `try` guarding `OSError`, return the same 404 on failure, and finally `self.send_bytes(200, "image/png", payload, cache_seconds=300)`. Carry a comment stating the same two facts the runway method's comment states: membership test before any path is ever constructed, and an unknown key and an unreadable file returning the identical 404 so a caller cannot distinguish them. Note that `illustration_path_for_key()` runs its own independent unsafe-key regex check and that this is defence in depth, never a substitute for the membership test above it.

    Wire the dispatch in `do_GET`, immediately after the runway-image block and before the final 404: match on the path starting with the new prefix and ending with `.png`, call `require_session()` and return `None` if it fails, slice the key out between prefix and suffix, and delegate. Do not add this route to the pre-auth static-asset region higher up in `do_GET` — that region is for assets carrying no per-user data, and this one sits behind the same gate the runway image already does.

    Then add checks to `companion/test_companion_app.py`, modelled directly on the existing live-HTTP runway-image checks: an authenticated request for a real key returns 200 with a `image/png` content type and a non-empty body; an authenticated request for a key that is not in the membership set returns 404; an authenticated request whose key contains a traversal sequence returns 404 with no filesystem read; and an unauthenticated request returns the login redirect rather than image bytes. Bump `EXPECTED_CHECK_COUNT` by the number of checks actually added, computed from the file's real on-disk starting value at execution time.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion (live HTTP, in-harness): authenticated `GET /illustration/air-france.png` returns 200, `Content-Type: image/png`, and a body longer than zero bytes.
    - Behaviour assertion (live HTTP): authenticated `GET /illustration/not-a-real-airline.png` returns 404.
    - Behaviour assertion (live HTTP): authenticated `GET` for a key carrying a path-traversal sequence returns 404 and does not return any file content.
    - Behaviour assertion (live HTTP): unauthenticated `GET /illustration/air-france.png` returns a redirect to the login route, not 200 and not image bytes.
    - Source assertion: inside `_serve_illustration_image`, the membership test appears at a lower line number than the first call to `illustration_path_for_key`.
    - Test command: `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The route exists, is authenticated, validates membership before constructing any path, 404s identically on both failure modes, and is covered by four live-HTTP checks.</done>
</task>

<task type="auto">
  <name>Task 3: panel-lookup.js lightbox script, its pre-auth static route, and its page_shell script tag (D-20)</name>
  <files>companion/static/panel-lookup.js, companion/app.py, companion/layout.py, companion/test_companion_app.py</files>
  <read_first>
    companion/static/nav-dropdown.js (the whole file — the ES5-safe `"use strict"` IIFE convention, the early-return guard idiom, and the `document.documentElement.className += " js"` first statement this codebase's client scripts follow);
    companion/static/copy-button.js (the whole file — the closest existing analogue: a delegated click handler over a set of attribute-marked buttons, no network call, no timer);
    companion/app.py lines 87-101 and 160-167 (the five existing static-script route constants and their five `_*_JS_PATH` siblings) and lines 597-668 (`_serve_script_file()` plus the six one-line delegate methods — the exact shape the new delegate copies);
    companion/app.py lines 757-786 (the pre-auth static-asset region of do_GET, with its own justifying comment — where the new route's dispatch goes);
    companion/layout.py lines 57-73 (the five `*_SCRIPT_SRC` constants and their duplicated-not-imported must-equal contract) and lines 845-885 (`page_shell()`'s five deferred script tags and their argument tuple)
  </read_first>
  <action>
    Create `companion/static/panel-lookup.js` as a single `"use strict"` IIFE in the same ES5-safe subset every existing static script in this directory uses — `var` only, `function` expressions only, no `let`/`const`, no arrow functions, no template literals, no network call, no timer, no persistent state. Open with a header comment in the same register as `dirty-state.js`'s, naming the decision it implements (D-20) and stating the standing constraints as constraints, not as a description of this version.

    Its contract, which plan 05 emits markup against:
    - Resolve the dialog by `document.getElementById("panel-lookup-dialog")`; early-return if absent, since this script is served to every page and only History carries the dialog.
    - Early-return if the resolved element has no `showModal` function, so a browser without native dialog support degrades to no lightbox rather than to a JavaScript error — the underlying page stays fully usable.
    - Resolve the dialog's image, caption, and note elements by their `lightbox__image` / `lightbox__caption` / `lightbox__note` class names, scoped inside the dialog with `querySelector`.
    - Attach one delegated `click` listener on `document`, walking up from the event target to find an ancestor carrying the `data-view-panel-src` attribute (an ES5-safe manual parent walk with a `nodeType` guard, not `Element.closest`, matching this codebase's transpiler-free constraint). On a hit, read `data-view-panel-src` and `data-view-panel-caption` off that element, assign them to the image's `src` and the caption's `textContent`, and call `showModal()`. Set the image's `alt` from the same caption text so the modal is not an unlabelled image.
    - Attach a `click` listener on the element carrying `data-view-panel-close` inside the dialog that calls `close()`.
    - Add no Escape handler and no focus-management code: the native dialog element already provides Escape-to-close, a backdrop, and focus-trap semantics, which is exactly why UI-SPEC §8.3 chose it. State that reasoning in a comment so nobody adds a redundant handler later.
    - The note text is server-rendered by plan 05 and never written by this script; do not add an innerHTML write anywhere in this file.

    In `companion/app.py`: add `PANEL_LOOKUP_SCRIPT_ROUTE = "/static/panel-lookup.js"` beside the existing five script-route constants, `_PANEL_LOOKUP_JS_PATH` beside the existing five path constants, a one-line `_serve_panel_lookup_script()` delegate to `_serve_script_file()`, and a dispatch line in the pre-auth static region of `do_GET` alongside the other four 06.6.3-era static scripts — same reasoning, no second justification comment needed beyond a pointer to the existing one.

    In `companion/layout.py`: add `PANEL_LOOKUP_SCRIPT_SRC = "/static/panel-lookup.js"` beside the existing four 06.6.3 constants, carrying the same duplicated-not-imported must-equal comment, and emit a sixth deferred `<script>` tag in `page_shell()` alongside the existing five, adding the constant to that call's argument tuple in matching position.

    Then add checks to `companion/test_companion_app.py` mirroring the existing static-script checks: the route serves the file pre-auth with a JavaScript content type; the served source contains no `let `, no `const `, no `=>`, and no backtick (the ES5-dialect gate the existing static-script checks already apply); and `layout.PANEL_LOOKUP_SCRIPT_SRC` equals `app.PANEL_LOOKUP_SCRIPT_ROUTE`. Bump `EXPECTED_CHECK_COUNT` by the number actually added, from the real on-disk starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion (live HTTP): unauthenticated `GET /static/panel-lookup.js` returns 200 with a JavaScript content type (matching the existing five static scripts' pre-auth behaviour).
    - Source assertion: `companion/static/panel-lookup.js` contains `"use strict"`, contains none of `let `, `const `, `=>`, or a backtick character, and contains no `innerHTML`, no `fetch`, no `XMLHttpRequest`, and no `setInterval`/`setTimeout`.
    - Source assertion: `companion.layout.PANEL_LOOKUP_SCRIPT_SRC == companion.app.PANEL_LOOKUP_SCRIPT_ROUTE`.
    - Behaviour assertion: a rendered authenticated page contains exactly six deferred `<script src=` tags before the closing body tag.
    - Test command: `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The script file exists in the project's ES5-safe dialect, is served pre-auth, is referenced once from page_shell, is inert on every page carrying no dialog, and its route/src constants provably agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→`/illustration/{key}.png` | An authenticated caller supplies an arbitrary key string that, unguarded, would reach a filesystem path join. |
| client→`/static/panel-lookup.js` | Pre-auth static asset, served to anyone; its contents are public. |
| server→browser DOM (panel-lookup.js) | The script writes attribute-sourced values into DOM properties. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-01 | Tampering / Information Disclosure | `Handler._serve_illustration_image()` | high | mitigate | Membership-test `key + ".png"` against the frozenset built from `illustrations.target_filenames()` BEFORE any path is constructed; only then call `illustration_path_for_key()`, whose own `_UNSAFE_KEY_RE` is a second independent boundary. Unknown key and unreadable file return the identical 404 via `_not_found_page()`. Verified by Task 2's traversal-key live-HTTP check. |
| T-06.6.4.1-03 | Elevation of Privilege | `do_GET` dispatch placement | high | mitigate | Dispatch the illustration route inside the `require_session()`-gated region, alongside `/preview.png`, `/gallery/`, and `/runway-image/` — never in the pre-auth static-asset region. Verified by Task 2's unauthenticated-request check. |
| T-06.6.4.1-09 | Tampering (DOM XSS) | `companion/static/panel-lookup.js` | medium | mitigate | The script writes only to `src`, `alt`, and `textContent` — never `innerHTML`, never `document.write`, never `eval`. Its inputs are server-rendered attribute values built from `ctx["gallery_entries"]` filenames, which `app.py`'s own listing helper produced. Asserted by Task 3's source-assertion criteria. |
| T-06.6.4.1-10 | Denial of Service | illustration route caching | low | accept | The route serves at most 43 small static PNGs from a closed list, behind a session gate, with a 300-second cache header matching the runway route. No amplification path exists; no rate limit is added. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs. The new JS file is hand-rolled ES5 with no dependency and no build step, matching the six static scripts already shipped; `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit records this phase as not-applicable. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 server/test_illustrations.py` and `server/.venv/bin/python3 companion/test_companion_app.py` both exit 0 at their bumped counts.
2. The other three companion harnesses (`test_config_page.py`, `test_status_pages.py`, `test_view_pages.py`) pass unmodified — this plan renames and deletes nothing, so none of them may need editing.
3. `git diff --stat` across this plan's commits touches exactly the six files in `files_modified`.
4. `scripts/run-all-tests.sh` green apart from the single pre-existing `server/test_poll_loop.py` digest mismatch — re-confirm that no rendering-pipeline file under `server/plane/` other than `illustrations.py` was touched, and that `illustrations.py`'s change is purely additive, before accepting it.
</verification>

<success_criteria>
- `target_variants_by_airline()` exists, is public, returns 27 correctly-ordered pairs, and preserves `a350-1000`.
- `/illustration/{key}.png` is authenticated, membership-validated before any path join, and 404s identically on both failure modes.
- `panel-lookup.js` exists in the project's ES5-safe dialect, is served pre-auth, and is emitted once by `page_shell()`.
- Nothing was renamed, retired, or deleted by this plan.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan:** `server/plane/illustrations.py::target_variants_by_airline()`; `companion/app.py::ILLUSTRATION_IMAGE_ROUTE_PREFIX`, the illustration filename membership frozenset, `Handler._serve_illustration_image()`, `PANEL_LOOKUP_SCRIPT_ROUTE`, `_PANEL_LOOKUP_JS_PATH`, `Handler._serve_panel_lookup_script()`; `companion/layout.py::PANEL_LOOKUP_SCRIPT_SRC` plus a sixth deferred script tag in `page_shell()`; the new file `companion/static/panel-lookup.js` and its `data-view-panel-src` / `data-view-panel-caption` / `data-view-panel-close` attribute contract; the new routes `GET /illustration/{key}.png` (authenticated) and `GET /static/panel-lookup.js` (pre-auth).

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-02-SUMMARY.md` when done
</output>
