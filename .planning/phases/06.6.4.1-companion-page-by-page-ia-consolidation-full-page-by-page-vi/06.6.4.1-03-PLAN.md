---
phase: 06.6.4.1
plan: 03
type: execute
wave: 2
depends_on: ["06.6.4.1-01"]
files_modified:
  - companion/pages/config_page.py
  - companion/static/dirty-state.js
  - companion/test_config_page.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-26]

must_haves:
  truths:
    - "Settings renders three sibling groups — Theme, Runway, Diagnostic LED — as a single-column stack inside ONE form posting to /settings, each group a single wrapped top-level element with one heading and one short description (D-01, D-02, D-05, D-26)."
    - "The Runway group is wrapped in the same container idiom the Theme group already uses; it no longer emits five flat top-level siblings into the page (D-01's root cause)."
    - "One page-level unsaved-changes bar names which section(s) changed rather than counting raw field diffs, and the form's always-rendered bottom Save button is marked so the JS-gated CSS rule can hide it (D-03, D-04)."
    - "Submitting the merged form persists theme, tracked runway, and LED state in a single save_device_config() call; a crafted value in ANY of the three rejects the whole submission with no partial write (D-05)."
    - "With JavaScript disabled the page still renders correctly and the bottom Save button still submits the whole form — the bar simply never appears (D-06)."
  artifacts:
    - "companion/pages/config_page.py::SETTINGS_ROUTE, THEME_SECTION_DESCRIPTION, RUNWAY_SECTION_DESCRIPTION, LED_SECTION_HEADING, DIRTY_SECTION_ATTR, STATIC_SAVE_FALLBACK_ATTR, DIRTY_BAR_INITIAL_TEXT, led_group()"
    - "companion/static/dirty-state.js section-aware [data-dirty-count] copy"
  key_links:
    - "companion/app.py still dispatches the OLD settings path and still owns the separate LED POST route until plan 07 flips them. This plan therefore MUST NOT delete led_fieldset() or handle_led_post() — app.py calls the latter, and deleting it here would break a live route and its passing live-HTTP checks two waves before its replacement lands. Plan 07 removes both together with the route."
    - "The section-naming mechanism must be driven by markup (a data attribute on each group wrapper), not by a hardcoded three-name list in the JS — the Theme group renders no form control at all while exactly one theme is registered, so a hardcoded list would name a section that structurally cannot change."
---

<objective>
Rebuild Settings as a single-column, single-form page: three wrapped and described sections, one unified save bar that names what changed, one merged POST handler, and a no-JS path that still works.

Purpose: the broken Runway section is not a styling bug — it is an unwrapped fieldset placed into a grid that was never designed to hold it. D-01 fixes the container (plan 01 deleted the grid; this plan adds the wrapper), and the LED merge (D-05) removes the very reason that grid constraint existed.
Output: a Settings page module whose render() emits one form, three named sections, and one fallback-marked save button.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/pages/config_page.py
@companion/static/dirty-state.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: single-column wrapped sections, descriptions, LED group, and the /settings form action (D-01, D-02, D-05 form half, D-26)</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    companion/pages/config_page.py lines 122-173 (`theme_fieldset()` — both branches; the single-theme branch's `&lt;div class="theme-status"&gt;` wrap is the idiom Runway copies, and the multi-theme fallback branch must keep working);
    companion/pages/config_page.py lines 176-294 (`runway_fieldset()`'s docstring and its five-flat-elements return, plus `led_fieldset()`/`led_section()` — read the docstrings, they explain WHY the current shapes exist and which of those reasons this plan retires);
    companion/pages/config_page.py lines 474-526 (`render()` — the existing form, the dirty-bar markup, the Poll section, and the comment block explaining why LED was kept separate; that comment's own premise is what D-05 retires);
    companion/static/style.css (the `.theme-status` rule, and confirm plan 01 already deleted the two-column grid — this task must not add any CSS);
    companion/test_config_page.py — every check that names the form action, the form class, the fieldset count, or the two-form shape (search for the form-class marker and for the count-of-fieldsets check); and the file's real `EXPECTED_CHECK_COUNT`
  </read_first>
  <action>
    Add module constants: `SETTINGS_ROUTE = "/settings"` (the single definition of this path in the repository, carrying the same rebinding comment `RUNWAY_IMAGE_ROUTE_PREFIX` already carries — `companion/app.py` imports this module, so app.py rebinds this constant rather than re-typing the literal, and the reverse import would be a cycle); `THEME_SECTION_DESCRIPTION = "The color palette used for departing and arriving flights on the physical frame."`; `RUNWAY_SECTION_DESCRIPTION = "Which Orly runway the device watches for departures and arrivals."`; `LED_SECTION_HEADING = "Diagnostic LED"`; `DIRTY_SECTION_ATTR = "data-dirty-section"`; `STATIC_SAVE_FALLBACK_ATTR = "data-static-save-fallback"`; `DIRTY_BAR_INITIAL_TEXT = "Unsaved changes"`.

    Note in a comment beside `DIRTY_SECTION_ATTR` and `STATIC_SAVE_FALLBACK_ATTR` that both are read elsewhere — the first by `companion/static/dirty-state.js`, the second by a `.js`-gated rule in `companion/static/style.css` — following this module's existing duplicated-not-imported must-equal discipline.

    Change `theme_fieldset()` so both branches return exactly one top-level element carrying `DIRTY_SECTION_ATTR` set to the escaped literal "Theme": the single-theme branch's existing `theme-status` div gains the attribute, and its heading is followed by a `&lt;p class="text-body"&gt;` holding `THEME_SECTION_DESCRIPTION` (escaped once, at interpolation); the multi-theme fallback branch's existing element gains the same attribute and the same description paragraph. Do not otherwise restructure either branch — the read-only swatch markup and the helper text are unchanged.

    Change `runway_fieldset()` to wrap its whole return value in `&lt;div class="theme-status" ...&gt;` carrying `DIRTY_SECTION_ATTR` set to "Runway", with the description paragraph holding `RUNWAY_SECTION_DESCRIPTION` placed directly after the existing `&lt;h2 class="text-heading"&gt;Runway&lt;/h2&gt;` and before the card list. Reuse the `theme-status` class name verbatim — do not invent a second wrapper class; the two sections share one wrapping idiom, which is the whole point of D-01. Rewrite the paragraph of the function's docstring that explains the missing wrapper and the `&lt;h2&gt;`-versus-`&lt;legend&gt;` reasoning so it describes what the function now actually does; leave the visually-hidden-radio and selected-card reasoning intact.

    Add a NEW function `led_group(current_led_enabled)` returning one top-level `&lt;div class="theme-status" ...&gt;` carrying `DIRTY_SECTION_ATTR` set to "Diagnostic LED", holding: `&lt;h2 class="text-heading"&gt;` with `LED_SECTION_HEADING`; the existing checkbox `&lt;label&gt;` markup and its `LED_CHECKBOX_VALUE`/`checked` logic copied from `led_fieldset()` byte-for-byte in behaviour; and the existing `LED_HELPER_TEXT` paragraph. Per UI-SPEC §5.2's table, LED needs no new description sentence — `LED_HELPER_TEXT` already reads as one. Document in the docstring that this group deliberately carries no fieldset/legend now that it is a sibling of two `&lt;h2&gt;`-headed groups in one single-column stack, so all three groups read at one heading level.

    Leave `led_fieldset()` and `led_section()` in the file, unreferenced by `render()` and otherwise untouched. `companion/app.py` still routes the separate LED POST to `handle_led_post()` until plan 07; removing either function here would break a live route two waves early. Add a one-line comment above each saying so, naming plan 07 as their removal site.

    Rewrite `render()` to emit, in order: `layout.page_header("Settings")`; one `&lt;form class="config-form" data-dirty-form method="post" action="{SETTINGS_ROUTE}"&gt;` containing the theme group, the runway group, the LED group, the existing dirty-bar div (with `[data-dirty-count]`'s seeded text changed to `DIRTY_BAR_INITIAL_TEXT`), and the bottom `&lt;button type="submit" {STATIC_SAVE_FALLBACK_ATTR}&gt;Save Settings&lt;/button&gt;`; then the existing Poll `&lt;section class="page-section"&gt;`, unchanged. Nothing follows the Poll section — the old LED sibling section is gone from the rendered page.
    <!-- planner-discipline-allow: action="/config-led" -->
    Replace the obsolete comment block above the old LED placement with a short note recording that D-05 retired its premise (the two-column grid that would have made a third fieldset an orphan row no longer exists), so nobody restores the separate section by reading a stale rationale.

    Then update `companion/test_config_page.py`: retarget the form-shape checks onto the new single-form shape (one form carrying the config-form class, `data-dirty-form`, and the new action, with the dirty bar nested inside it before the bottom button), retarget or replace the check asserting a second distinct LED form action in render()'s output, retarget the fieldset-count check onto the new heading-level contract (three group headings, one per section, no duplicate accessible name), and add new checks for: each of the three groups being a single top-level element carrying the section attribute with the expected label; each of Theme and Runway carrying its description sentence; and the bottom submit button carrying the fallback attribute. Bump `EXPECTED_CHECK_COUNT` by the number of genuinely NEW checks only, computed from the file's real on-disk starting value at execution time — retargeted-in-place checks are not counted as new, following this file's own documented history-comment discipline.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: `config_page.render(ctx)` output contains exactly one occurrence of the config-form class and exactly one form whose action equals `config_page.SETTINGS_ROUTE`.
    - Behaviour assertion: the rendered output contains exactly three elements carrying `data-dirty-section`, whose attribute values in document order are "Theme", "Runway", "Diagnostic LED".
    - Behaviour assertion: `runway_fieldset(...)` returns a string that starts with a single opening `div` tag and ends with its matching closing tag — one top-level element, not five siblings.
    - Behaviour assertion: the rendered output contains `THEME_SECTION_DESCRIPTION` and `RUNWAY_SECTION_DESCRIPTION` exactly once each.
    - Behaviour assertion: the rendered output contains exactly one element carrying `data-static-save-fallback`, and it is a `type="submit"` button inside the settings form.
    - Behaviour assertion: the rendered output contains no `action` attribute pointing at the retired separate LED path.
    - Test command: `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>Settings renders one form, three wrapped and named sections with descriptions, and one fallback-marked bottom button; the old LED sibling section is gone from render() while its route-serving functions stay alive for plan 07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handle_post() absorbs LED validation as one all-or-nothing submission (D-05)</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    companion/pages/config_page.py lines 529-608 (`handle_post()` and `handle_led_post()` in full — both docstrings state the asymmetric-validation discipline that must survive the merge verbatim: the read path forgives, the write path rejects and never silently coerces);
    server/device_config.py (`save_device_config()`'s signature and its carry-forward-on-None semantics for each keyword — read to confirm a single call can persist all three fields and that `None` still means "leave unchanged");
    companion/test_config_page.py — the three existing `handle_led_post` checks (unchecked persists False, checked persists True, crafted value rejected and leaves the config file byte-identical) — these are the exact behaviours the merged handler must now also exhibit
  </read_first>
  <behavior>
    - `handle_post({}, ctx)` — the shape a browser sends when nothing is checked and nothing is selected — persists `led_enabled` False and returns the saved flash key.
    - `handle_post({"led_enabled": LED_CHECKBOX_VALUE}, ctx)` persists `led_enabled` True.
    - `handle_post({"led_enabled": "<crafted>"}, ctx)` returns the save-failed flash key and leaves `device_config.json` byte-identical — no partial write.
    - `handle_post({"theme": "<not a registered theme>", "led_enabled": LED_CHECKBOX_VALUE}, ctx)` returns save-failed and leaves the file byte-identical — an invalid theme rejects the LED half too.
    - `handle_post({"tracked_runway": <a real runway id>, "led_enabled": LED_CHECKBOX_VALUE}, ctx)` persists both in one call and returns the saved flash key.
  </behavior>
  <action>
    Extend `handle_post()` so it resolves the LED field with exactly the three shapes `handle_led_post()` documents — absent means False (an unchecked HTML checkbox is omitted from the POST body entirely), equal to `LED_CHECKBOX_VALUE` means True, anything else rejects the whole submission with the save-failed flash key before any write. Place that resolution alongside the existing theme and runway membership tests, before the single `device_config.save_device_config()` call, and pass all three values to that one call.

    Two properties are load-bearing and must be stated in the docstring, not just implemented. First, the LED field's absent-means-False semantics is deliberately different from theme's and runway's absent-means-unchanged semantics — a checkbox and a radio group encode "no submission" differently, and conflating them would silently re-enable a disabled LED on every save. Second, rejection stays all-or-nothing across all three fields: because there is now one form and one save call, a crafted value in any field must abort before the call, never persist the valid remainder. Update the docstring's closing paragraph, which currently names the old settings path as the redirect target, to name `SETTINGS_ROUTE` instead.

    Leave `handle_led_post()` itself untouched — `companion/app.py` still calls it until plan 07.

    Then add checks to `companion/test_config_page.py` covering every bullet in this task's behaviour block, modelled on the three existing LED checks' fixture setup (a temp state dir, a byte-identical-file assertion on the reject paths). Bump `EXPECTED_CHECK_COUNT` by the number actually added, from the file's real starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: every bullet in this task's `<behavior>` block passes as a check in `companion/test_config_page.py`.
    - Source assertion: `handle_post` contains exactly one call to `save_device_config`, and every membership/shape test appears at a lower line number than that call.
    - Behaviour assertion: on each reject path, the on-disk `device_config.json` is byte-identical before and after the call.
    - Test command: `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>One handler validates and persists all three settings in one call, with no partial-save path and the checkbox/radio asymmetry documented.</done>
</task>

<task type="auto">
  <name>Task 3: section-aware unified save-bar copy in dirty-state.js (D-03, D-04, D-06)</name>
  <files>companion/static/dirty-state.js, companion/test_config_page.py</files>
  <read_first>
    companion/static/dirty-state.js (the whole file — its header's standing constraints, `snapshotValues()`/`countDifferences()`, and `updateBar()`, whose only DOM mutation today is toggling the bar's hidden property; that fact is the bug D-04 names);
    companion/static/nav-dropdown.js line 31 (the unconditional `js` class append — the mechanism plan 01's CSS rule is gated on; read it so this task does not duplicate that work);
    companion/static/panel-lookup.js (created in plan 02 — reuse its ES5-safe ancestor-walk idiom if a walk is needed, so the two files share one shape);
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md §5.1 (the exact three-row table of `[data-dirty-count]` copy this task implements)
  </read_first>
  <action>
    Change only `updateBar()` and add one helper to `companion/static/dirty-state.js`; leave the header, the guards, `snapshotValues()`, `countDifferences()`, the two event listeners, and the cancel handler as they are. Stay inside the file's stated ES5-safe subset — `var` and `function` only, no `let`/`const`, no arrow functions, no template literals — and introduce no network call, no timer, and no persistent state, as the header requires.

    Add a helper that returns an array of dirty section labels: query the form for every element carrying the `data-dirty-section` attribute (document order, which makes the output order deterministic and independent of which field the user touched first); for each such wrapper, re-run the same per-field comparison `countDifferences()` already performs but scoped to fields the wrapper `contains()`; push the wrapper's attribute value when any of its fields differs from the load-time snapshot. Do not hardcode the three section names in this file — the Theme group renders no form control at all while exactly one theme is registered, so a hardcoded list would name a section that structurally cannot change.

    Change `updateBar()` to compose the count element's text from that label array: one label reads "{Label} changed"; two read "{A} and {B} changed"; three or more join every label but the last with ", " and prefix the last with ", and " — matching UI-SPEC §5.1's table exactly. Keep the existing zero-difference branch (hide the bar) unchanged. Keep a fallback: if the total difference count is greater than zero but the label array is empty (a differing field sits outside every section wrapper), fall back to the existing raw-count copy so the bar can never go silent while unsaved edits exist. Write into `textContent` only — never `innerHTML`.

    Add a header note recording that this file deliberately does NOT hide the static fallback Save button: that fix is the `.js`-gated CSS rule plan 01 landed, driven by the class `nav-dropdown.js` already sets, so no JavaScript in this file needs to know the button exists (D-04). This closes the real bug where the old per-section bars and the bottom button used to show at the same time.

    Then add checks to `companion/test_config_page.py` — this file already owns Settings' cross-file DOM-contract guards. Add a guard reading `companion/static/dirty-state.js` and `companion/static/style.css` from disk and asserting: the JS references the same `data-dirty-section` attribute `config_page.DIRTY_SECTION_ATTR` names; the JS contains no `innerHTML`, no `let `, no `const `, no `=>`, and no backtick; and the stylesheet's fallback-hide rule references the same attribute `config_page.STATIC_SAVE_FALLBACK_ATTR` names. Bump `EXPECTED_CHECK_COUNT` by the number actually added, from the file's real starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `companion/static/dirty-state.js` references the literal value of `config_page.DIRTY_SECTION_ATTR` and contains none of `innerHTML`, `let `, `const `, `=>`, or a backtick character.
    - Source assertion: `companion/static/style.css` contains a rule referencing the literal value of `config_page.STATIC_SAVE_FALLBACK_ATTR`.
    - Source assertion: `dirty-state.js` contains no hardcoded occurrence of the strings "Theme", "Runway", or "Diagnostic LED" — the labels come from the DOM.
    - Source assertion: the file still contains no `fetch`, `XMLHttpRequest`, `setInterval`, or `setTimeout`.
    - Test command: `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The bar names which sections changed, in deterministic document order, with a never-silent fallback; the fallback-button fix stays in CSS where UI-SPEC put it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client→settings POST | One merged form now submits theme, tracked runway, and LED state in a single request. |
| server→browser (rendered settings markup) | Registry-derived labels and helper copy are interpolated into HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-04 | Tampering | `config_page.handle_post()` after the D-05 merge | medium | mitigate | The merged handler validates the union of the two previous field sets with the same per-field tests each previously had — theme and runway by explicit membership against `device_config.THEME_IDS`/`RUNWAY_IDS`, LED by explicit equality against `LED_CHECKBOX_VALUE`. A crafted value in any field aborts before the single `save_device_config()` call, so the merge does not widen what one authenticated POST can persist; it narrows the partial-write surface from two independent saves to one atomic save. Verified by Task 2's cross-field reject check. |
| T-06.6.4.1-11 | Tampering (XSS) | new description/heading/attribute interpolation in `config_page.py` | low | mitigate | Every new interpolated value goes through `escape_html()` exactly once at the point of interpolation, including the `data-dirty-section` attribute values, matching this module's single-escaping-choke-point discipline. |
| T-06.6.4.1-12 | Tampering (DOM XSS) | `companion/static/dirty-state.js` | low | mitigate | The section-label composition writes to `textContent` only; `innerHTML` is absent from the file and asserted absent by Task 3. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; see `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit (not applicable — stdlib Python and hand-rolled ES5, no build step). |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at the bumped count after each task.
2. `server/.venv/bin/python3 companion/test_companion_app.py` passes UNMODIFIED — the live LED POST route and the settings GET tab still work, because this plan deletes no function `companion/app.py` calls.
3. `git diff --stat` across this plan's commits touches exactly `companion/pages/config_page.py`, `companion/static/dirty-state.js`, and `companion/test_config_page.py`; `git diff --quiet companion/app.py companion/layout.py companion/static/style.css` reports no change.
4. `scripts/run-all-tests.sh` green apart from the single pre-existing `server/test_poll_loop.py` digest mismatch (zero `server/` files touched here).
</verification>

<success_criteria>
- One form, one action, three wrapped and named sections, three descriptions where D-02 asks for them.
- One save call, all-or-nothing rejection across all three fields.
- Section-named bar copy driven by markup, never a hardcoded list.
- No CSS edited, no route edited, no function deleted that `companion/app.py` still calls.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan:** `companion/pages/config_page.py::SETTINGS_ROUTE` (the single definition of this path in the repository — `app.py` rebinds it in plan 07), `THEME_SECTION_DESCRIPTION`, `RUNWAY_SECTION_DESCRIPTION`, `LED_SECTION_HEADING`, `DIRTY_SECTION_ATTR`, `STATIC_SAVE_FALLBACK_ATTR`, `DIRTY_BAR_INITIAL_TEXT`, `led_group()`, and `handle_post()`'s extension to validate and persist the LED field in the same single save call; the section-aware `[data-dirty-count]` copy in `companion/static/dirty-state.js`; the `data-dirty-section` and `data-static-save-fallback` attribute contracts.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-03-SUMMARY.md` when done
</output>
