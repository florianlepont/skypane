---
phase: 06.6.4.1
plan: 08
type: execute
wave: 5
depends_on: ["06.6.4.1-05", "06.6.4.1-07"]
files_modified:
  - companion/app.py
  - companion/layout.py
  - companion/pages/__init__.py
  - companion/pages/preview_page.py
  - companion/test_companion_app.py
  - companion/test_view_pages.py
autonomous: true
requirements: [D-22]

must_haves:
  truths:
    - "The Preview HTML page route returns a fixed 302 to the History page — never a 404, and never a redirect to any caller-supplied value (D-22)."
    - "The live-panel image route and the per-render gallery image routes are unchanged and still authenticated — History now depends on both of them (D-22)."
    - "Navigation renders exactly four items: Settings, Health, Airlines, History — in both the sidebar and the mobile dropdown."
    - "companion/pages/preview_page.py no longer exists on disk, nothing imports it, and its content lives in companion/pages/history_page.py."
    - "The eye glyph symbol survives the nav shrink because History's View-panel trigger uses it — only the nav slug's mapping entry is removed, never the icon whitelist member."
  artifacts:
    - "companion/app.py::PREVIEW_PAGE_ROUTE (retained solely as the redirect source) and its fixed 302 branch"
    - "deleted: companion/pages/preview_page.py"
  key_links:
    - "The redirect target must be a hardcoded literal. companion/app.py already has a caller-supplied-next-route mechanism for the login flow — that is a DIFFERENT, separately-allowlisted mechanism and must not be reached for or conflated here. A redirect that consults any request value on this route is an open redirect."
    - "The icon whitelist entry for the eye glyph is consumed by History's View-panel trigger button (plan 05). Removing the nav slug's entry from the slug-to-icon MAP is correct; removing the glyph from the icon WHITELIST would make every trigger button render an empty box with no error."
    - "Plan 07 installed a standing guard asserting the nav tuple, the page-titles dict, and the slug-to-icon map agree. That guard is what makes this plan's four-collection edit fail loudly if any one of them is missed — run it, do not weaken it."
---

<objective>
Retire the Preview page: redirect its HTML route to History, shrink navigation from five items to four, delete the now-superseded page module, and leave both byte-serving image routes exactly as they are because History depends on them.

Purpose: this is the last structural change of the phase. After it, the app has the four-page information architecture every earlier plan was building toward, and no module or route exists whose content lives somewhere else.
Output: four nav items, one redirect, one deleted file, and two untouched image routes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/app.py
@companion/layout.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixed 302 from the Preview page route to History, image routes untouched (D-22)</name>
  <files>companion/app.py, companion/test_companion_app.py</files>
  <read_first>
    companion/app.py lines 385-395 (`redirect()` — its signature, the status code it emits, and its optional cookie argument) and lines 839-868 (do_GET's preview page block, then the preview-image block, the gallery-prefix block, the runway-image block, and the illustration block plan 02 added — this task edits only the first and must leave the other four byte-identical);
    companion/app.py lines 199-225 (`_validated_next_route()` — read it specifically to confirm this task does NOT touch it; that mechanism belongs to the login flow and reaching for it here would turn a fixed redirect into an open one);
    companion/app.py lines 106-110 (the preview-image route constant and the gallery route prefix — both stay);
    companion/pages/history_page.py (confirm the moved preview and gallery content is genuinely live there before removing the page block, and confirm which image route literals it emits, since those are the routes this task must not disturb);
    companion/test_companion_app.py — the existing checks covering the preview page route, the preview image route, and the gallery image route, and the file's real `EXPECTED_CHECK_COUNT`
  </read_first>
  <action>
    In `companion/app.py`, rename the preview page path's role: keep a module constant for it, named to say what it now is (a redirect source rather than a page route), and replace the whole preview page block in do_GET with a two-line branch — the session gate, then a redirect to a hardcoded literal history-route string. Emit the same status code `redirect()` already emits for every other redirect on this site; D-22 asks for a 302-class response and this method's existing behaviour is the site's one answer to that.

    The redirect target is a fixed literal. Do not derive it from a query parameter, a form value, a referer header, or the login flow's allowlisted next-route mechanism. Add a comment saying exactly that, and saying why: the login flow's mechanism exists to honour a caller's requested destination and is allowlisted for that reason, whereas this route has exactly one correct destination and consulting any request value would turn a fixed redirect into an open one.

    Keep the session gate. The retired page was authenticated and the page it redirects to is authenticated; dropping the gate here would let an unauthenticated caller probe the redirect and learn that the route exists at all.

    Remove the module's import of the preview page module — it now has no call site. Leave the preview-image route block, the gallery-prefix block, the runway-image block, and the illustration block completely untouched: History's Now-showing section and its Recent-renders disclosure both depend on the first two, so this is not dead plumbing.

    Then update `companion/test_companion_app.py`: retarget the preview-page checks onto the redirect — an authenticated GET of the preview page path returns the redirect status with a location header equal to the history route exactly, and an unauthenticated GET redirects to login rather than to history. Add a check that the redirect location is unaffected by a query string on the request, so a crafted next-style parameter provably cannot influence it. Verify the existing preview-image and gallery-image checks still pass untouched, and add one asserting both routes still require a session. Bump `EXPECTED_CHECK_COUNT` by the number of genuinely NEW checks only, from the file's real on-disk starting value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion (live HTTP): authenticated GET of the preview page path returns a redirect whose location header equals the history route string exactly.
    - Behaviour assertion (live HTTP): the same request carrying an arbitrary query string still redirects to the identical location — no request value influences the target.
    - Behaviour assertion (live HTTP): unauthenticated GET of the preview page path redirects to login, not to history.
    - Behaviour assertion (live HTTP): the live-panel image route and a real gallery image route both still return 200 with a PNG body for an authenticated session, and both still redirect an unauthenticated one.
    - Source assertion: `companion/app.py` no longer imports the preview page module.
    - Test command: `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The retired page route lands somewhere useful, provably cannot be steered, and both image routes it used to share a module with are untouched and still gated.</done>
</task>

<task type="auto">
  <name>Task 2: shrink navigation from five items to four</name>
  <files>companion/layout.py, companion/app.py, companion/test_companion_app.py</files>
  <read_first>
    companion/layout.py lines 26-32 (the nav tuple as plan 07 left it) and lines 94-125, 251-262 (the icon whitelist — note which member the eye glyph is — and the slug-to-icon map);
    companion/pages/history_page.py (the View-panel trigger builder plan 05 added — confirm which icon id it passes to the icon helper; that id must stay in the whitelist after this task);
    companion/layout.py lines 493-520 and 561-610 and the mobile dropdown builder (`_nav_links()`, `sidebar_nav()`, and the dropdown renderer — confirm all three derive from the tuple and need no edit);
    companion/app.py lines 185-196 (the page-titles dict, still holding a preview entry) and lines 199-225, 717-724 (the two runtime allowlists derived from the tuple, which self-adjust);
    companion/test_companion_app.py — the nav-count assertions and the tab-iteration tuple plan 07 partially updated, plus the standing route-contract guard plan 07 added
  </read_first>
  <action>
    Remove the preview entry from the nav tuple in `companion/layout.py`, and remove the preview key from the slug-to-icon map. Leave the eye glyph in the icon whitelist and leave its symbol definition in the sprite: History's View-panel trigger renders it, and an id that falls outside the whitelist renders nothing at all — a silently invisible button with no error. Add a comment on the whitelist entry recording that its consumer is now that trigger rather than a nav tab, so nobody removes it as apparently-orphaned later.

    Remove the preview entry from the page-titles dict in `companion/app.py`. The two runtime allowlists derived from the tuple need no edit — say so in the SUMMARY rather than touching them.

    Then update `companion/test_companion_app.py`: retarget every assertion that counts nav links or iterates the tab set onto four items, including the tab-iteration tuple's preview pair and any nearby prose comment stating a count. Run plan 07's standing route-contract guard unchanged — it should now assert four-way agreement across the tuple, the page-titles dict, and the icon map, and it must pass without being weakened. Add one check asserting the eye glyph id is still a whitelist member and that the icon helper returns non-empty markup for it, so the nav shrink provably did not break History's trigger. Recompute `EXPECTED_CHECK_COUNT` from the file's real on-disk value.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion: `len(layout.NAV_TABS) == 4` and its routes are the settings, health, airlines and history paths in that order.
    - Behaviour assertion: a rendered authenticated page contains exactly four sidebar nav links and exactly four mobile dropdown links, with exactly one marked active in each.
    - Contract assertion: plan 07's standing guard passes unmodified — the nav tuple, the page-titles dict, and the slug-to-icon map all agree at size four.
    - Behaviour assertion: the eye glyph id is still a member of the icon whitelist and `layout.icon_html()` returns non-empty markup for it.
    - Behaviour assertion: a rendered History page still contains at least one View-panel trigger carrying a non-empty icon (for a fixture with gallery entries).
    - Test command: `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 at the recomputed `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>Four nav items in both renderers, all four route collections agree, and the glyph History borrowed from the retired tab survives.</done>
</task>

<task type="auto">
  <name>Task 3: delete companion/pages/preview_page.py and repurpose its test section</name>
  <files>companion/pages/preview_page.py, companion/pages/__init__.py, companion/test_view_pages.py</files>
  <read_first>
    companion/pages/history_page.py — every symbol plan 05 moved in (the colour caveat, the no-panel caption, the no-renders copy, the display limit, the two route literals, the panel dimensions, the filename-time regex, the timestamp-recovery helper, the preview section builder, and the gallery tiles builder). Confirm each one is genuinely live and behaviourally equivalent BEFORE deleting its original;
    companion/pages/preview_page.py in full, one last time, to enumerate exactly what is being deleted;
    companion/pages/__init__.py in full (its documented page-module contract and any reference to the preview module — the registry note and the context-key rationale both mention it);
    companion/test_view_pages.py — its Preview-specific section in full, and the History-side checks plan 05 added, so each deleted check is confirmed to have a live History-side equivalent rather than assumed to;
    a repository-wide grep for any remaining import of the preview page module (companion/, server/, scripts/, deploy/) — the deletion must not be the thing that discovers a call site
  </read_first>
  <action>
    Delete `companion/pages/preview_page.py` outright. This project's stated convention is to delete what is certainly unused rather than leave a backwards-compatibility shim, and every one of this module's functions now has a live home in `companion/pages/history_page.py`. Before deleting, run the repository-wide import grep and confirm the only references left are the ones this plan removes; if any other call site exists, stop and report it rather than deleting.

    Update `companion/pages/__init__.py`: remove the preview module from its documented page-module registry and rewrite the two passages that explain the gallery-entries context key by reference to that module, so they name the history module instead. The context key itself and the reasoning behind it are unchanged — only the consumer's name changes. Do not remove the key's own rationale; it is still exactly why the key exists.

    Then update `companion/test_view_pages.py`: delete the Preview-specific section, having first confirmed for each check that plan 05 added a History-side equivalent covering the same behaviour. Two checks deserve particular care rather than mechanical deletion — the filename-to-timestamp recovery helper's degrade-safely cases (a name with no separator, a name whose time portion does not match the expected shape) and the gallery tiles builder's raw-filename caption fallback. Both describe robustness against a hand-dropped or renamed file in the gallery directory; if plan 05's History-side checks do not already cover them, add them there rather than losing them. Recompute `EXPECTED_CHECK_COUNT` from the file's real on-disk value at execution time, subtracting deletions and adding anything carried over, and extend the file's own history-comment block in its established format recording the split.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py &amp;&amp; server/.venv/bin/python3 -c "import companion.pages, companion.app, companion.layout; import os; assert not os.path.exists('companion/pages/preview_page.py'); print('module deleted, package imports clean')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `companion/pages/preview_page.py` does not exist on disk.
    - Source assertion: a repository-wide grep finds no import of the deleted module anywhere under `companion/`, `server/`, `scripts/`, or `deploy/`.
    - Behaviour assertion: `import companion.app` succeeds and a live authenticated walk of all four page routes returns 200 for each.
    - Behaviour assertion: the timestamp-recovery helper's two degrade-safely cases and the gallery caption fallback are each covered by a live check in `companion/test_view_pages.py` (carried over if plan 05 did not already add them).
    - Test command: `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 at the recomputed `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>The module is gone, nothing imports it, its package registry entry is corrected, and none of its robustness coverage was lost in the move.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→retired page route | An authenticated caller reaches a route whose only job is now to redirect. |
| client→byte-serving image routes | The live-panel and gallery image routes remain, now consumed from a different page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-02 | Spoofing (open redirect) | the retired page route's redirect | high | mitigate | The target is a hardcoded literal history-route string. No query parameter, form value, referer header, or the login flow's allowlisted next-route mechanism is consulted. Verified by Task 1's check that an arbitrary query string leaves the location header byte-identical. |
| T-06.6.4.1-23 | Elevation of Privilege | session gate on the retired route and the two image routes | high | mitigate | The redirect branch keeps its `require_session()` call, and the preview-image, gallery, runway-image and illustration blocks are left byte-identical with their own gates intact. Verified by Task 1's unauthenticated checks on all three. |
| T-06.6.4.1-24 | Denial of Service | deletion of a still-imported module | medium | mitigate | A repository-wide import grep precedes the deletion, and the verify command imports the package to prove no dangling reference survives — the deletion is proven safe rather than assumed safe. |
| T-06.6.4.1-25 | Tampering | icon whitelist shrink | low | mitigate | Only the nav slug's map entry is removed; the glyph stays in the whitelist because History's trigger renders it, and an out-of-whitelist id renders silently empty. Pinned by Task 2's whitelist-membership and non-empty-markup checks. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; see `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit (not applicable). |
</threat_model>

<verification>
1. All four companion harnesses exit 0 at their recomputed counts.
2. `scripts/run-all-tests.sh` green apart from the single pre-existing `server/test_poll_loop.py` digest mismatch (confirm zero `server/` files touched by this plan before accepting it).
3. `git diff --stat` across this plan's commits touches exactly the six files in `files_modified`, with the preview page module recorded as a deletion.
4. Live manual smoke against a locally-run instance: sign in, confirm four nav items in both the sidebar and the mobile dropdown, visit the retired page path and confirm it lands on History, and confirm History's Now-showing image and at least one Recent-renders thumbnail both load.
</verification>

<success_criteria>
- Retired page route redirects to a hardcoded target that no request value can influence, behind the session gate.
- Both byte-serving image routes are byte-identical and still gated.
- Four nav items, four page titles, four icon-map entries, agreeing under plan 07's standing guard.
- The page module is deleted, nothing imports it, and none of its robustness coverage was lost.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan:** `companion/app.py`'s preview page path retained solely as a fixed-redirect source plus its 302 branch; the removal of the `_PAGE_TITLES` preview entry and of the preview page module import; `companion/layout.py::NAV_TABS` shrunk from five entries to four and `NAV_ICON_IDS`' preview key removed (the eye glyph stays in `ICON_IDS` — its consumer is now History's View-panel trigger); the corrected page-module registry in `companion/pages/__init__.py`. Deleted by this plan: `companion/pages/preview_page.py`.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-08-SUMMARY.md` when done
</output>
