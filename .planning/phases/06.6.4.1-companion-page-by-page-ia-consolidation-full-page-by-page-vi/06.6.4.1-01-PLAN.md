---
phase: 06.6.4.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/static/style.css
autonomous: true
requirements: [D-01, D-04, D-07, D-09, D-14, D-20]

must_haves:
  truths:
    - "Settings' three sections stack single-column at every viewport width — the >=960px two-column grid that the unwrapped Runway fieldset was never designed to sit in no longer exists (D-01 root-cause fix)."
    - "A page whose <html> carries the .js class hides any element marked as the static save fallback, so the unified save bar is the only visible save affordance once JS is confirmed running (D-04)."
    - "Every CSS class the Airlines gallery (D-14), the History lightbox (D-20), Health's battery-chart axis labels (D-09), and Health's anomaly-banner category pills (D-07) reference exists in the stylesheet before any page emits markup for them."
    - "The stylesheet still declares zero external references (no remote asset, no @font-face, no url() to anything off-origin) and introduces zero new design tokens."
  artifacts:
    - "companion/static/style.css carrying .illustration-grid, .airline-card and its four BEM parts, .lightbox and its three parts plus ::backdrop, .sparkline-axis-label, .banner__pill, and the .js-gated static-save-fallback rule"
  key_links:
    - "This plan is the single write site for companion/static/style.css this phase — plans 03/04/05/06 each consume classes landed here and must not edit the stylesheet themselves. This mirrors 06.6.3-02's own precedent ('landed once so no later plan touches style.css')."
    - "The new card rules must carry 06.6.4's hairline-at-rest / shadow-on-hover contract, not 06.6.1's shadow-at-rest treatment — a card that keeps a resting shadow is a visible regression against the phase this one builds on."
---

<objective>
Land every CSS rule the four page-module plans in waves 2 and 3 depend on, in one write site, plus the one deletion (D-01's two-column grid) that is the actual root-cause fix for Settings' broken Runway section.

Purpose: `companion/static/style.css` is wanted by Settings, Health, Airlines, and History simultaneously. Landing it once, first, and additively is this project's own established answer to that (06.6.3-02) and is what lets waves 2 and 3 run three page plans in parallel with zero file contention.
Output: an updated stylesheet whose new rules are inert until the page plans emit markup for them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings single-column stack (D-01) and the JS-gated static-save-fallback rule (D-04)</name>
  <files>companion/static/style.css</files>
  <read_first>
    companion/static/style.css lines 1-80 (the file header's stated constraints — zero external references, the token block, and the four-size type scale this plan must not extend);
    companion/static/style.css lines 2015-2045 (the &gt;=960px media query holding the rule this task deletes — read the whole media block so the deletion does not orphan a brace);
    companion/static/style.css lines 440-470 (the existing `.js`-prefixed rules `nav-dropdown.js` already relies on — the exact idiom this task's new rule must copy);
    companion/static/style.css lines 1044-1096 (`.dirty-bar` and its two modifier rules — read to confirm no change is needed there; the unified bar reuses this container unmodified per UI-SPEC §5.1)
  </read_first>
  <action>
    Delete the entire `.config-form` grid rule group from inside the &gt;=960px media query: the `.config-form` rule itself (display/grid-template-columns/gap/align-items), the `.config-form fieldset` margin-bottom reset, and the `.config-form > button` grid-column/justify-self pair. Nothing replaces them — normal block flow already produces the single-column stack D-01 asks for, and the class hook itself stays alive because `companion/pages/config_page.py` still emits it and `companion/test_config_page.py` pins it.

    Leave a short removal-rationale comment in place of the deleted group, in the same shape 06.6.1-05 used when it deleted `_nav_html()`: state that the two-column grid was the container the Runway group's five unwrapped top-level elements were never designed to sit in, that plan 03 of this phase wraps that group instead, and that this is a supersede, not a re-tune — do not reintroduce a column rule here.
    <!-- planner-discipline-allow: grid-template-columns: 1fr 1fr -->
    <!-- planner-discipline-allow: .config-form > button -->

    Then add, near the existing `.js`-prefixed mobile-nav rules so the whole `.js`-gated family reads together, a single new rule selecting an element carrying the static-save-fallback data attribute under a `.js` ancestor and setting `display: none`. Use the attribute name `data-static-save-fallback` — plan 03 puts that exact attribute on the form's always-rendered bottom submit button. Comment it with the real mechanism: `companion/static/nav-dropdown.js`'s first statement appends the `js` class to `document.documentElement` unconditionally on every authenticated page, so this rule applies if and only if JavaScript actually ran; without JS the class never appears, the rule never matches, and the bottom button remains the only way to save (D-06's graceful degradation). Name the bug this closes: `dirty-state.js`'s only DOM mutation is toggling the bar's `hidden` property, so it never had any reference to the fallback button and both used to show at once (D-04).
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "d=open('companion/static/style.css').read(); assert 'grid-template-columns: 1fr 1fr' not in d, 'two-column config grid still present'; assert 'data-static-save-fallback' in d and 'display: none' in d.split('data-static-save-fallback')[1][:120], 'fallback-hide rule missing'; assert d.count('{')==d.count('}'), 'brace mismatch'; print('T1 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: no `grid-template-columns` declaration remains anywhere inside a `.config-form`-selected rule in `companion/static/style.css`.
    - Source assertion: a rule whose selector combines the `js` class with the `data-static-save-fallback` attribute exists and declares `display: none`.
    - Source assertion: the file's `{` and `}` counts are equal (the media-query deletion did not orphan a brace).
    - Test command: `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 and reports its unchanged `EXPECTED_CHECK_COUNT` (no colour pairing changed).
  </acceptance_criteria>
  <done>The Settings form stacks single-column at every width, the fallback-hide rule exists and is gated on the real `.js` class, and the stylesheet still parses with balanced braces.</done>
</task>

<task type="auto">
  <name>Task 2: Airlines illustration-gallery card CSS (D-14, UI-SPEC §7.1)</name>
  <files>companion/static/style.css</files>
  <read_first>
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md §7.1 (the exact declarations this task lands — treat it as the contract, not a suggestion);
    companion/static/style.css lines 1876-1900 (`.gallery-grid` / `.gallery-tile` — the existing auto-fill idiom this new grid follows without reusing, since those two stay reserved for History's bare render thumbnails);
    companion/static/style.css lines 1584-1610 (`.stat-tile` and its `:hover, :focus-within` rule — the canonical 06.6.4 hairline-at-rest / shadow-on-hover card contract every new card must copy);
    companion/static/style.css lines 1255-1275 (`.data-table th` — the existing 11px quiet-label sub-scale precedent the chip text reuses)
  </read_first>
  <action>
    Add a new section to the stylesheet, placed immediately after the existing `.gallery-grid`/`.gallery-tile` block so the two grid idioms sit together, holding six new rules:

    `.illustration-grid` — `display: grid`, `grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))`, `gap: var(--space-lg)`. Comment why this is a new class rather than a reuse of `.gallery-grid`: those tiles carry only an image and a caption, these carry a name and a chip row too, so they need the wider 200px column floor and the roomier gap; same auto-fill idiom, not a new pattern.

    `.airline-card` — `background: var(--color-dominant)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-control)`, `box-shadow: none`, `padding: var(--space-sm)`. Then `.airline-card:hover, .airline-card:focus-within` — `border-color: transparent`, `box-shadow: var(--shadow-card-hover)`. This is the 06.6.4 D-03 contract verbatim; comment it as such so a later reader does not "restore" a resting shadow.

    `.airline-card__image` — `width: 100%`, `height: auto`, `display: block`, `border-radius: var(--radius-control)`, `margin-bottom: var(--space-sm)`.

    `.airline-card__name` — `font-size: var(--font-body-size)`, `font-weight: var(--weight-semibold)`, `margin: 0 0 var(--space-xs)`. This is the existing Emphasis role, not a new type role.

    `.airline-card__chips` — `display: flex`, `flex-wrap: wrap`, `gap: var(--space-xs)`.

    `.airline-card__chip` — `display: inline-flex`, `align-items: center`, `height: 20px`, `padding: 0 8px`, `border-radius: 999px`, `font-size: 11px`, `font-weight: var(--weight-regular)`, `background: color-mix(in srgb, var(--color-text) 6%, transparent)`, `border: 1px solid color-mix(in srgb, var(--color-text) 12%, transparent)`, `color: color-mix(in srgb, var(--color-text) 70%, transparent)`. Comment the two deliberate constraints UI-SPEC pins: the 11px matches `.data-table th`'s already-shipped quiet-label tier (not a fifth content type role), and the chip is neutral-tinted rather than accent-tinted specifically so the accent-reserved list in this file's own header stays exhaustive and unchanged.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "d=open('companion/static/style.css').read(); [print(s,'ok') or None for s in ['.illustration-grid','.airline-card{'.replace('{',''),'.airline-card__image','.airline-card__name','.airline-card__chips','.airline-card__chip'] if s in d]; missing=[s for s in ['.illustration-grid','.airline-card__image','.airline-card__name','.airline-card__chips','.airline-card__chip'] if s not in d]; assert not missing, missing; blk=d[d.index('.airline-card {'):]; assert 'box-shadow: none' in blk[:400] and 'var(--shadow-card-hover)' in blk[:900], 'hairline-at-rest/shadow-on-hover contract missing'; assert '@font-face' not in d and 'https://' not in d, 'external reference introduced'; print('T2 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all six selectors (`.illustration-grid`, `.airline-card`, `.airline-card__image`, `.airline-card__name`, `.airline-card__chips`, `.airline-card__chip`) are present in `companion/static/style.css`.
    - Source assertion: within the `.airline-card` rule group, `box-shadow: none` appears at rest and `var(--shadow-card-hover)` appears in the `:hover, :focus-within` rule.
    - Source assertion: no `@font-face` declaration and no `https://` string exists anywhere in the file (the standing zero-external-reference constraint).
    - Source assertion: no new custom property (a line matching `--` followed by a name and a colon inside `:root`) was added — the `:root` declaration count is unchanged from the pre-task value.
  </acceptance_criteria>
  <done>All six gallery classes exist, carry the 06.6.4 card contract, use only already-shipped tokens, and the stylesheet still has zero external references.</done>
</task>

<task type="auto">
  <name>Task 3: History lightbox CSS (D-20, §8.3), Health battery-chart axis-label class (D-09, §5.3), and the anomaly-banner pill (D-07)</name>
  <files>companion/static/style.css</files>
  <read_first>
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md §8.3 and §5.3, plus the Color section's explicit ruling that this phase's pills and chips use neutral treatments and add zero new accent consumers;
    companion/static/style.css lines 1864-1875 (`.preview-frame` — the bounded, centered matte treatment History's Now-showing section reuses unchanged; read to confirm no edit is needed here);
    companion/static/style.css lines 1902-1930 (`.sparkline-dot` / `.sparkline-hit` and its focus rules — the existing sparkline class family the new axis-label class joins);
    companion/static/style.css — the `.banner` base rule and its three severity modifiers plus the `::before` dot pseudo-element (06.6.4 D-04's flattened banner: a flex row with an 8px gap), so the new pill is written as a flex child of that existing row rather than as a new container;
    companion/static/style.css lines 1783-1810 (`.page-section` and its hover rule — the full-width card class the lightbox deliberately does NOT copy at rest, and the class Health's migrated cards will reuse verbatim in plan 04)
  </read_first>
  <action>
    Add a `.lightbox` block near the end of the stylesheet's component section, holding five rules:

    `.lightbox` (a native `dialog` element, styled by class) — `background: var(--color-dominant)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-card)`, `padding: var(--space-lg)`, `max-width: 480px`, and `box-shadow: var(--shadow-card-hover)` at rest. Comment this last one as the phase's one deliberate exception to the hairline-at-rest contract, with the reason UI-SPEC gives: this is a floating modal overlay, not a page-flow card, so its resting state should already read as elevated.

    A max-width media rule (or a plain `width: calc(100% - var(--space-lg) * 2)` on the base rule, whichever keeps the block simplest) so the dialog does not exceed the viewport on a phone.

    `.lightbox__image` — `width: 100%`, `height: auto`, `display: block`, `border-radius: var(--radius-control)`, `margin-bottom: var(--space-sm)`.

    `.lightbox__caption` and `.lightbox__note` — margin-only rules using `var(--space-xs)` / `var(--space-sm)`; both elements already carry `text-label mono` / `text-body` utility classes from plan 05's markup, so neither rule sets a font-size or a colour.

    `.lightbox::backdrop` — `background: rgba(0, 0, 0, 0.5)`. Comment that no scrim token exists and that a raw rgba value here is consistent with this file's existing raw-rgba shadow values, so this is not a new token.

    Then add `.sparkline-axis-label` to the existing sparkline class family — `font-size: 10px`, `font-weight: var(--weight-regular)`, `fill: color-mix(in srgb, var(--color-text) 70%, transparent)`. `fill`, not `color`: these are SVG `text` nodes. Comment that 10px is a reuse of this file's already-documented sub-scale exception tier (`.data-table th` at 11px, `.cell-secondary` at 12px), one step further down because chart-axis micro-labels sit below secondary table text in the hierarchy — explicitly not a fifth content type role.

    Finally add `.banner__pill`, placed directly after the existing `.banner` severity modifiers so the banner family reads together. Give it the identical neutral-pill declarations `.airline-card__chip` carries in Task 2 — `display: inline-flex`, `align-items: center`, `height: 20px`, `padding: 0 8px`, `border-radius: 999px`, `font-size: 11px`, `font-weight: var(--weight-regular)`, the same 6% background / 12% border / 70% text `color-mix(...)` triple — plus `flex: none` so it never shrinks inside `.banner`'s flex row. Comment the two constraints: it is a flex child of the already-flattened `.banner` row (06.6.4 D-04), not a new container; and it is deliberately neutral-tinted rather than severity-tinted, because the banner's own left edge and dot already carry the severity signal and a second coloured token per pill would dilute it. The declarations are intentionally identical to `.airline-card__chip`'s — the two are the same visual primitive at two call sites, and keeping them literally identical is what makes a future consolidation into one shared class a mechanical edit rather than a redesign.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "d=open('companion/static/style.css').read(); missing=[s for s in ['.lightbox','.lightbox__image','.lightbox__caption','.lightbox__note','.lightbox::backdrop','.sparkline-axis-label','.banner__pill'] if s not in d]; assert not missing, missing; b=d[d.index('.lightbox {'):]; assert 'var(--shadow-card-hover)' in b[:400], 'lightbox missing at-rest elevation'; assert 'fill:' in d[d.index('.sparkline-axis-label'):d.index('.sparkline-axis-label')+220], 'axis label must set fill, not color'; p=d[d.index('.banner__pill'):d.index('.banner__pill')+420]; assert 'border-radius: 999px' in p and 'var(--color-accent)' not in p, 'pill must be a neutral 999px pill'; assert d.count('{')==d.count('}'), 'brace mismatch'; print('T3 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `.lightbox`, `.lightbox__image`, `.lightbox__caption`, `.lightbox__note`, `.lightbox::backdrop`, `.sparkline-axis-label`, and `.banner__pill` all exist in `companion/static/style.css`.
    - Source assertion: the `.lightbox` rule declares `var(--shadow-card-hover)` at rest (the documented deliberate exception), and `.sparkline-axis-label` declares `fill` rather than `color`.
    - Source assertion: the `.banner__pill` rule declares `border-radius: 999px` and references no accent or status colour token — the phase adds zero new accent consumers.
    - Source assertion: `{` and `}` counts are equal.
    - Test command: `server/.venv/bin/python3 companion/test_companion_app.py`, `server/.venv/bin/python3 companion/test_config_page.py`, `server/.venv/bin/python3 companion/test_status_pages.py`, and `server/.venv/bin/python3 companion/test_view_pages.py` each exit 0 with their existing check counts unmodified — this plan changes no markup, so no harness may need editing.
  </acceptance_criteria>
  <done>Every lightbox, axis-label, and banner-pill class exists, the deliberate exceptions are commented as exceptions, and all four companion harnesses pass unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→stylesheet | `companion/static/style.css` is served pre-auth by `app.py`'s `_serve_stylesheet()`; its contents are public. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-07 | Information Disclosure | `companion/static/style.css` (pre-auth route) | low | mitigate | Add no comment naming a deployment path, credential, hostname, or internal URL; every comment this plan adds describes CSS mechanism only. |
| T-06.6.4.1-08 | Tampering | stylesheet external references | medium | mitigate | Keep the file's standing zero-external-reference constraint: no `@font-face`, no `url()` to an off-origin target, no `https://` string. Asserted by Task 2's verify. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this phase; `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit records this as not-applicable (stdlib Python plus hand-rolled ES5, no build step). Nothing to verify, nothing installed. |
</threat_model>

<verification>
1. `server/.venv/bin/python3 companion/test_contrast_check.py` — 0 exit, unchanged check count.
2. All four `companion/test_*.py` harnesses pass with no edit to any of them.
3. `git diff --stat` across this plan's commits touches exactly `companion/static/style.css`.
4. `scripts/run-all-tests.sh` green apart from the single pre-existing, unrelated `server/test_poll_loop.py` macOS-local Pillow/FreeType digest mismatch (confirm zero `server/` files were touched before accepting it).
</verification>

<success_criteria>
- The two-column Settings grid is gone; the fallback-hide rule exists and is `.js`-gated.
- All six Airlines-gallery classes, all five lightbox rules, `.sparkline-axis-label`, and `.banner__pill` exist.
- Zero new custom properties, zero external references, balanced braces.
- No page module, test harness, or JS file was edited by this plan.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan** (`companion/static/style.css`, this phase's only stylesheet write site):
`.illustration-grid`, `.airline-card`, `.airline-card__image`, `.airline-card__name`, `.airline-card__chips`, `.airline-card__chip`, `.lightbox`, `.lightbox__image`, `.lightbox__caption`, `.lightbox__note`, `.lightbox::backdrop`, `.sparkline-axis-label`, `.banner__pill`, and the `.js`-gated `data-static-save-fallback` hide rule. Removed: the `.config-form` two-column grid rule group.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-01-SUMMARY.md` when done
</output>
