---
phase: 06.6.4.1
slug: companion-page-by-page-ia-consolidation-full-page-by-page-vi
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-01
---

# Phase 06.6.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Custom stdlib `check(name, fn)` harness (not pytest/unittest) — each `companion/test_*.py` is directly executable and self-verifies against its own `EXPECTED_CHECK_COUNT` constant |
| **Config file** | none — `pyproject.toml`'s `[tool.coverage.*]` sections configure coverage measurement only (`fail_under = 83`), not test discovery |
| **Quick run command** | `server/.venv/bin/python3 companion/test_<touched_file>.py` (whichever single file was touched) |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~60 seconds (full suite, this project's own established precedent) |

---

## Sampling Rate

- **After every task commit:** Run the single touched harness directly.
- **After every plan wave:** Run `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite must be green (single accepted exception: `server/test_poll_loop.py`'s pre-existing, unrelated macOS-local Pillow/FreeType `panel.bin` digest mismatch — confirmed unrelated whenever zero `server/` files are touched, which is true for this entire phase; D-14/D-15's illustration route reads `server/plane/illustrations.py` and `server/assets/` but does not modify either).
- **Max feedback latency:** ~60s

---

## Per-Decision Verification Map

| Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------|----------|-----------|-------------------|-------------|--------|
| D-01 | Settings single-column stacked sections (Theme/Runway/LED), fixing the unwrapped-runway-fieldset grid bug | source assertion + browser | `companion/test_config_page.py` DOM-contract checks, checkpoint item A1 | ✅ existing, extended | ⬜ pending |
| D-02 | Per-section description sentences | source assertion | `companion/test_config_page.py` | ✅ existing, extended | ⬜ pending |
| D-03/D-04 | Unified Discord-style save bar; real fix for `dirty-state.js` never hiding the static fallback button | source assertion + browser | `companion/test_config_page.py`, `companion/test_companion_app.py` (dirty-state.js contract), checkpoint item A1/A3 | ✅ existing, extended | ⬜ pending |
| D-05 | LED form backend-merged into `/config` → `/settings`'s POST route; `/config-led` retired | source assertion + markup regression | `companion/test_config_page.py` — every call site asserting the separate `/config-led` route/form must be updated, not just added to (research-flagged: ~10 call sites) | ✅ existing, extended | ⬜ pending |
| D-06 | No-JS graceful degradation preserved | browser only | checkpoint item A3 | n/a | ⬜ pending |
| D-26 | Route rename `/config` → `/settings` everywhere (app.py, layout.py nav) | source assertion + markup regression | `companion/test_companion_app.py` (route dispatch, nav link contract) | ✅ existing, extended | ⬜ pending |
| D-07/D-08 | Health anomaly banner category pills; Corroboration `<details>` disclosure | source assertion + browser | `companion/test_status_pages.py`, checkpoint item B1 | ✅ existing, extended | ⬜ pending |
| D-09 | Battery trend: full-width interactive chart, labeled axes, default latest-reading value | source assertion + browser | `companion/test_status_pages.py`, checkpoint item B2 | ✅ existing, extended | ⬜ pending |
| D-10/D-11/D-12 | Health restructure into Screen/Server-and-data; old Airlines diagnostics sections promoted to full-width cards under Server-and-data | source assertion + browser | `companion/test_status_pages.py` (dual health+airlines module — every Airlines-diagnostics assertion moves here), checkpoint items B3-B4 | ✅ existing, extended | ⬜ pending |
| D-13/D-14/D-15/D-16 | Airlines reborn as illustration gallery: 27 cards, real PNGs via new validate-then-join route, filter bar | source assertion + browser | new `companion/test_status_pages.py` (or a new dedicated harness — planner's call) checks for the gallery markup + route membership-validation contract, checkpoint item C1-C3 | ✅ existing infra, new checks | ⬜ pending |
| D-17 | Gallery shows the static curated list only, no "seen at Orly" cross-reference | non-goal regression | inline verify: confirm no `history_db` import/query appears in the new Airlines gallery page module | n/a | ⬜ pending |
| D-18/D-19 | History "Now showing" section (live image + caveat); Recent-renders gallery demoted to collapsed `<details>` | source assertion + browser | `companion/test_view_pages.py` (Preview-specific Section 2 repurposed onto History), checkpoint item D1-D2 | ✅ existing, repurposed | ⬜ pending |
| D-20 | Per-row nearest-render lookup button + lightbox | source assertion + browser | `companion/test_view_pages.py`, checkpoint item D3 (click-tested, not just markup-present) | ✅ existing, extended | ⬜ pending |
| D-21 | Unresolved-airline rows link to Health instead of duplicating the prefixes table | source assertion | `companion/test_view_pages.py` | ✅ existing, extended | ⬜ pending |
| D-22 | `/preview.png`/`/gallery/*` byte routes unchanged; `/preview` page route redirects to `/history` (302) | source assertion + markup regression | `companion/test_companion_app.py` (route dispatch) | ✅ existing, extended | ⬜ pending |
| Claude's Discretion (preview_page.py deletion) | File deleted outright, not orphaned | non-goal regression | inline verify: `test -f companion/pages/preview_page.py` fails after this phase | n/a | ⬜ pending |
| D-23/D-25 | Closing checkpoint translates 06.6.3-08's 20 items onto the new page structure; full keyboard-only pass including new controls | browser only | checkpoint items across all groups | n/a | ⬜ pending |
| D-24 | Real assistive-technology pass (VoiceOver/NVDA/TalkBack) + production authenticated walkthrough — done for real this time, no further deferral | browser/AT tool only | checkpoint items E1 (AT pass), E2 (production walkthrough) | n/a | ⬜ pending |
| All | Full composed suite green apart from the one pre-documented exception | full suite | `scripts/run-all-tests.sh` — coverage must stay ≥83% (pyproject.toml threshold) | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**`EXPECTED_CHECK_COUNT` note (compute at execution time, not planning time — this project's own established precedent):** current on-disk baselines, for reference only — `companion/test_config_page.py` = 47, `companion/test_status_pages.py` = 59, `companion/test_view_pages.py` = 42, `companion/test_companion_app.py` = 88, `companion/test_contrast_check.py` = 34 (unaffected unless a new colour pairing is introduced, which no decision here calls for).

---

## Wave 0 Requirements

*None — this phase restructures existing page markup, merges two routes into two others, adds one new route, and deletes one page module. No new test file or fixture is required; every touched harness (`test_config_page.py`, `test_status_pages.py`, `test_view_pages.py`, `test_companion_app.py`) already exercises the relevant page module or route dispatch table. A planner may choose to add a small dedicated test section for the new Airlines-gallery route's membership-validation contract (mirroring `test_companion_app.py`'s existing runway-image-route tests) rather than growing `test_status_pages.py` further — either is Wave-0-free either way.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Full visual fidelity to this session's validated sketch artifacts (Settings single-column + save bar, Health Screen/Server-and-data split, Airlines gallery grid, History Now-showing + lightbox) | D-01 through D-22 | No visual regression test exists in this stdlib-only suite; computed-style/structural checks alone previously missed a real mobile-nav bug on this project (project memory: [[feedback-real-device-ui-verification]]) | Run the companion service locally, sign in, and visually compare each of the 4 pages against this session's validated Artifact URLs (recorded in `06.6.4.1-CONTEXT.md`'s `<specifics>` section) in both light and dark theme |
| History's per-row "View panel near this time" lightbox, clicked with a real pointer | D-20 | A lightbox open/close interaction and the nearest-timestamp match it displays can only be confirmed by actually clicking it against real gallery data | Click the button on a handful of rows spanning different times of day; confirm the lightbox opens, shows a real timestamp close to (before) the row's own time, and closes cleanly |
| Airlines gallery's illustration route, requested with an invalid/hostile key | D-15 | The validate-then-join safety contract (`_serve_runway_image()`'s own precedent) needs a live request against a real server process, not just a source read, to confirm the 404-on-both-cases behavior holds | Request `/airline-illustration/../../etc/passwd.png` (or the planner's actual chosen route shape) and confirm a clean 404, not a path-traversal read or a 500 |
| A real assistive-technology pass (VoiceOver/NVDA/TalkBack) | D-24 | Screen-reader announcement quality cannot be asserted by a stdlib test harness | Navigate all 4 pages with a real AT tool; confirm disclosures, the save bar, the gallery filter, and the lightbox all announce sensibly, nothing silently or twice |
| Production authenticated walkthrough | D-24 | Confirms the same code that passed locally also renders correctly against the real deployed environment | Sign in to the live deployed companion service and browse all 4 pages |
| WCAG keyboard-only pass across all 4 pages including this phase's new controls | D-25 | Subjective reachability/focus-visibility judgment, not something a unit test can assert | Tab through Settings' save bar, Airlines' filter input, and History's lightbox button/close control; confirm every stop has a visible focus indicator |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (Wave 0 not required — see above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (source-assertion coverage spans every decision; only the closing checkpoint plan is manual-only, matching every prior 06.6.x closing-plan precedent)
- [x] Wave 0 covers all MISSING references (none missing)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — this phase's own closing checkpoint (mirroring 06.6.4-06's precedent) is where the developer's real-device sign-off happens, per D-23/D-24/D-25.
