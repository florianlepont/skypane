---
phase: 06.6.4.1
slug: companion-page-by-page-ia-consolidation-full-page-by-page-vi
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-01
---

# Phase 06.6.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

## Live Route Battery (Task 1, re-run 2026-09-04 against branch tip `92bc660`)

Run against a real `companion/app.py` instance (port 8799) started with a real populated state directory (a copy of `/private/tmp/skypane-prod-state`: real `history.db`, real `poll_state.json`, a real `gallery/` directory with 27+ renders, a real `panel.bin`, real `device_config.json`), authenticated via a real `POST /login` (never a UI-driven login). All eleven required outcomes below were observed directly against the running process, not inferred from source:

| # | Check | Observed | Result |
|---|-------|----------|--------|
| 1 | `GET /settings` authenticated | 200, len=23432 | ✅ |
| 2 | `GET /settings` unauthenticated | 303 → `/login?next=%2Fsettings` | ✅ |
| 3 | `GET /health` authenticated | 200, len=29080 | ✅ |
| 4 | `GET /health` unauthenticated | 303 → `/login?next=%2Fhealth` | ✅ |
| 5 | `GET /airlines` authenticated | 200, len=25351 | ✅ |
| 6 | `GET /airlines` unauthenticated | 303 → `/login?next=%2Fairlines` | ✅ |
| 7 | `GET /history` authenticated | 200, len=179196 | ✅ |
| 8 | `GET /history` unauthenticated | 303 → `/login?next=%2Fhistory` | ✅ |
| 9 | `GET /config` (retired settings path) | 404 | ✅ |
| 10 | `POST /config-led` (retired LED path) | 404 | ✅ |
| 11 | `GET /preview` (retired preview page) | 303 → `Location: /history`, byte-identical Location with `?next=/evil&foo=bar&x=1` appended | ✅ |
| 12 | `GET /gallery/{real-file}.png` authenticated | 200, `image/png`, real PNG signature, 84,859 bytes | ✅ |
| 13 | `GET /gallery/{real-file}.png` unauthenticated | 303 → `/login` | ✅ |
| 14 | `GET /illustration/air-algerie.png` (real key) authenticated | 200, real PNG signature, 43,070 bytes | ✅ |
| 15 | `GET /illustration/air-algerie.png` unauthenticated | 303 → `/login` | ✅ |
| 16 | `GET /illustration/{unknown-key}.png` | 404 (the app's own styled "Not Found" page — confirmed by inspecting the body, not just the status: no traversal content, no stack trace) | ✅ |
| 17-19 | `GET /illustration/{3 path-traversal-shaped keys}.png` | Each a clean 404 (same styled Not-Found page; confirmed body contains no `root:` signature, no source-code leak, no 500) | ✅ |

This exceeds the plan's own "eleven listed outcomes" and "illustration route's traversal-key request" requirements — every route this phase added, renamed, or retired was exercised both authenticated and unauthenticated, plus the illustration route's full 404 battery (unknown key + 3 traversal variants). One additional, non-required observation recorded for completeness: `GET /preview.png` now returns 404 (not 200/redirect) — this is a *later*, separate retirement by quick task 260903-c4o (folding "Now showing" into the always-visible gallery made the standalone byte route obsolete), not part of this phase's own D-22 scope, and not a regression of anything this phase shipped.

## Non-Goal Regression Gate (Task 1, re-run 2026-09-04)

| # | Check | Observed | Result |
|---|-------|----------|--------|
| 1 | Airlines gallery page module opens no database, reads no poll state | `companion/pages/airlines_page.py`'s current imports are exactly `os`, `re`, `companion.illustration_normalize`, `companion.layout`, `server.plane.illustrations` — no `history_db`, `poll_loop`, or `sqlite3` | ✅ |
| 2 | Exactly one page renders the unresolved-prefix table's headers and the resolution-statistics table's headers | Live render of `/health`, `/airlines`, `/history`, `/settings`: "Unresolved prefixes" and "Resolution statistics" each appear exactly once, both on `/health` only, zero elsewhere | ✅ |
| 3 | The deleted preview page module is absent from disk and nothing imports it | `test -f companion/pages/preview_page.py` fails; repo-wide grep for `preview_page` finds only historical prose comments (`history_page.py`, `test_view_pages.py`, `pages/__init__.py`), zero live imports | ✅ |
| 4 | The stylesheet still contains zero external references and gained no new custom property across the whole phase | `grep` for `url(http`/`@import`/`https://`/`http://` in `companion/static/style.css`: zero matches. `git diff 5e453d5 d237bac -- companion/static/style.css` (the phase's three style.css commits, all in plan 01) filtered for added `--custom-property:` declarations: zero matches | ✅ |

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Custom stdlib `check(name, fn)` harness (not pytest/unittest) — each `companion/test_*.py` is directly executable and self-verifies against its own `EXPECTED_CHECK_COUNT` constant |
| **Config file** | none — `pyproject.toml`'s `[tool.coverage.*]` sections configure coverage measurement only (`fail_under = 83`), not test discovery |
| **Quick run command** | `server/.venv/bin/python3 companion/test_<touched_file>.py` (whichever single file was touched) |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~60 seconds (full suite, this project's own established precedent) |

---

## Sampling Rate

- **After every task commit:** Run the single touched harness directly.
- **After every plan wave:** Run `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite must be green (single accepted exception: `server/test_poll_loop.py`'s pre-existing, unrelated macOS-local Pillow/FreeType `panel.bin` digest mismatch — confirmed unrelated whenever zero `server/` files are touched, which is true for this entire phase; D-14/D-15's illustration route reads `server/plane/illustrations.py` and `server/assets/` but does not modify either). **Update, re-run 2026-09-04:** this exception is now moot — the harness itself was hardened between 09-01 and today so the digest check is Linux-strict/non-Linux-informational, and the full suite passes 16/16 with zero exceptions (see Task 1's re-run above).
- **Max feedback latency:** ~60s

---

## Per-Decision Verification Map

**Re-verified 2026-09-04 against the current branch tip (`92bc660`), NOT the 2026-09-01 snapshot this file previously recorded.** Every status/count below comes from a command actually run today. Three days and roughly a dozen commits/plans/quick-tasks (phase 06.6.4.1.1's full theme-picker/typography pass, plus quick tasks 260901-qif/re6/s5o, 260902-v2v/w4t/iag/v26/tli/bl2/chc/dng, 260903-c4o/etm/ghy/peo/btu/df3, 260904-bbi/e92, and more) landed on this exact `companion/` tree since this phase's own plans 01-08 shipped. Where a decision's shipped behavior has since been superseded by later work, that is called out explicitly in its row rather than silently re-asserting the 09-01 claim.

**Second re-verification, 2026-09-04, against `4a31a62` (PR #44, merged from a separate line of work while this plan's own Task 2 was pending).** PR #44 shipped Phases 8-11 (panel theme rework, band themes, scheduled quiet hours, web-configurable wake interval) and did not touch this phase's own files or this validation record — confirmed by `git diff --stat 92bc660..4a31a62` naming neither `06.6.4.1-VALIDATION.md` nor any file under this phase's directory. `scripts/run-all-tests.sh` re-run against the rebased tip: still 16/16 green, coverage still 92%. No route dispatch table entry was added (`git diff 92bc660..4a31a62 -- companion/app.py` is purely additive env/context plumbing feeding the existing `/settings` POST handler — no new `elif path ==` branch), so the live-route battery's structural claims above are unaffected. **One real scope gap for Task 2, flagged rather than silently left stale**: Phase 11 (11-04) added a "Wake interval" section and Phase 10 added a "Quiet hours" section to the Settings page (`companion/pages/config_page.py`, +331 lines) — Settings now stacks *five* sections, not the three (Theme, Runway, Diagnostic LED) Group A's checklist (A1, A6) describes. Both additions already went through their own dedicated code review, security review, and developer UAT as part of Phases 10 and 11 respectively (PR #44's own test plan records this), so this is not new untested surface — but Task 2's checklist text predates their existence and should not be read as covering them.

| Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------|----------|-----------|-------------------|-------------|--------|
| D-01 | Settings single-column stacked sections (Theme/Runway/LED), fixing the unwrapped-runway-fieldset grid bug | source assertion + browser | `companion/test_config_page.py` DOM-contract checks (72/72 today, plan 03 origin) — the `.theme-status` wrapper/single-column structure is intact; quick tasks 260901-qif/re6/s5o additionally gave `.theme-status` its own card treatment and moved the save bar outside the form as a floating pop-up (cosmetic evolution of D-03/D-04, not a reversal of D-01's layout fix), checkpoint item A1 | ✅ existing, extended | ✅ green (source, re-run today); ⬜ awaiting checkpoint (browser) |
| D-02 | Per-section description sentences | source assertion | `companion/test_config_page.py` (72/72 today) — quick task 260901-re6 merged the description/helper-text pairs into one muted caption per section (cosmetic, same underlying requirement) | ✅ existing, extended | ✅ green (re-run today) |
| D-03/D-04 | Unified Discord-style save bar; real fix for `dirty-state.js` never hiding the static fallback button | source assertion + browser | `companion/test_config_page.py`, `companion/test_companion_app.py` (dirty-state.js contract, 144/144 today) — **superseded-in-detail:** quick tasks 260901-re6/s5o moved the bar outside the `<form>` entirely and switched it from sticky to a fixed floating pop-up ("shrink save bar to content width, deepen its shadow"); the one-save/one-confirmation behavior and the JS-gated static-fallback hide rule are unchanged, only the bar's own positioning/chrome evolved post-ship, checkpoint item A1/A3 | ✅ existing, extended | ✅ green (source, re-run today); ⬜ awaiting checkpoint (browser) |
| D-05 | LED form backend-merged into `/config` → `/settings`'s POST route; `/config-led` retired | source assertion + markup regression + live | `companion/test_config_page.py` — `/config-led` retired outright (route, handler, both builders deleted); this task's own live route battery today confirms `POST /config-led` → 404 against a real running instance | ✅ existing, extended | ✅ green (source+live, re-run today) |
| D-06 | No-JS graceful degradation preserved | browser only | checkpoint item A3 | n/a | ⬜ awaiting checkpoint |
| D-26 | Route rename `/config` → `/settings` everywhere (app.py, layout.py nav) | source assertion + markup regression + live | `companion/test_companion_app.py` (route dispatch, nav link contract, 144/144 today) — this task's own live route battery today: `GET /settings` authenticated → 200, `GET /config` → 404, confirmed against a real running instance | ✅ existing, extended | ✅ green (source+live, re-run today) |
| D-07/D-08 | Health anomaly banner category pills; Corroboration `<details>` disclosure | source assertion + browser | `companion/test_status_pages.py` (136/136 today, plan 04 origin) — `_anomaly_banner_html()`'s pill markup and the Corroboration `<details>` disclosure are both still present in source; quick task 260902-chc replaced Health's manual Refresh with an updating freshness pill (D-07/D-08's own content untouched by that change), checkpoint item B1 | ✅ existing, extended | ✅ green (source, re-run today); ⬜ awaiting checkpoint (browser, dark-theme legibility) |
| D-09 | Battery trend: full-width interactive chart, labeled axes, default latest-reading value | source assertion + browser | `companion/test_status_pages.py` (136/136 today) — quick task 260902-dng bounded the chart at 1:1 scale (a sizing refinement, not a reversal); axis-label source assertions still pass, checkpoint item B2 | ✅ existing, extended | ✅ green (source, re-run today); ⬜ awaiting checkpoint (keyboard/dark-theme) |
| D-10/D-11/D-12 | Health restructure into Screen/Server-and-data; old Airlines diagnostics sections promoted to full-width cards under Server-and-data | source assertion + browser + live | `companion/test_status_pages.py` (136/136 today) — `SCREEN_SECTION_ID`/`SERVER_DATA_SECTION_ID` confirmed present in source today; this task's own live render of `GET /health` confirms both section headings and both migrated tables ("Unresolved prefixes", "Resolution statistics") render exactly once. Quick task 260903-ghy additionally gave both migrated tables a dedicated mobile card layout (`.data-cards`/`.data-card`, closing UIR-10/UIR-11 — the nested-horizontal-scroll bug this phase already fixed for desktop, now also fixed for phone width); quick tasks 260901-uzi/bl2/tsa did further within-section spacing/heading-rhythm polish. None of this reverses D-10/D-11/D-12's structural placement, checkpoint items B3-B4 | ✅ existing, extended | ✅ green (source+live, re-run today); ⬜ awaiting checkpoint (browser, no-horizontal-scroll + sticky-header repaint) |
| D-13/D-14/D-15/D-16 | Airlines reborn as illustration gallery: 27 cards, real PNGs via new validate-then-join route, filter bar | source assertion + browser + live | `companion/test_status_pages.py` (136/136 today) + `companion/test_companion_app.py` (route membership-validation contract, 144/144 today) + this task's own live route battery today (real key `air-algerie.png` → 200 PNG, unknown key → 404, 3 traversal variants → clean 404, no 500, no traversal read — all re-confirmed against a real running instance, see below). **Superseded-in-detail, not reversed:** quick task 260904-e92 halved the illustration serving dimensions (900×263 → 450×132) for byte-weight (UIR-08), and quick task 260902-v26 added an entirely new capability on top (per-card image-replace upload via the same lightbox) — the validate-then-join route contract and the 27-card/filter-bar/chip behavior D-13-16 specify are unchanged, checkpoint item C1-C3 | ✅ existing infra, new checks | ✅ green (source+live, re-run today); ⬜ awaiting checkpoint (browser, responsive reflow) |
| D-17 | Gallery shows the static curated list only, no "seen at Orly" cross-reference | non-goal regression | inline verify (re-run today): confirmed no `history_db`/`poll_loop`/`sqlite3` import in `companion/pages/airlines_page.py`'s current source (imports are exactly `os`, `re`, `companion.illustration_normalize`, `companion.layout`, `server.plane.illustrations`) — both via `companion/test_status_pages.py`'s standing source guard and this task's own independent source scan | n/a | ✅ green (re-run today) |
| D-18/D-19 | History "Now showing" section (live image + caveat); Recent-renders gallery demoted to collapsed `<details>` | source assertion + browser | **SUPERSEDED — this decision's shipped form no longer exists.** Quick task 260903-c4o (2026-09-03, UIR-09) deliberately retired the standalone "Now showing" frame this plan built: `NOW_SHOWING_HEADING`/`RECENT_RENDERS_SUMMARY_TEMPLATE`/`preview_section()` and the collapsed `<details>` wrapper are all gone from `companion/pages/history_page.py` (confirmed absent today by direct source read). The newest render is now simply the first tile of one always-visible `RENDER_GALLERY_HEADING` grid, at full `/gallery/{name}.png` resolution, styled identically to every other tile — no special "Now showing" frame, no collapsed disclosure. The colour caveat and the underlying live-panel/gallery data are still present and still correct; only the presentation D-18/D-19 specified has changed. `companion/test_view_pages.py` (54/54 today) covers the *current* shape, not the shape this plan shipped. This is a real, developer-directed content change layered on top of this phase's work, not a defect in this phase's own delivery — recorded here per this task's own instruction to note honestly when a decision's ground has shifted | ✅ existing, but superseded | ⚠️ decision superseded by 260903-c4o; current shape verified green, original D-18/D-19 shape no longer shippable to re-check |
| D-20 | Per-row nearest-render lookup button + lightbox | source assertion + browser + live | `companion/test_view_pages.py` (54/54 today) — server-side nearest-match/DOM-contract covered, `nearest_gallery_entry()`/`_view_panel_button_html()`/`_lightbox_html()` all confirmed present in current source; the click-tested open/close/Escape interaction itself is browser-only, checkpoint item D3 | ✅ existing, extended | ✅ green (source, re-run today); ⬜ awaiting checkpoint (real click interaction) |
| D-21 | Unresolved-airline rows link to Health instead of duplicating the prefixes table | source assertion + live | `companion/test_view_pages.py` (54/54 today) — `UNRESOLVED_LINK_HREF = "/health#server-data"` confirmed present in current source, cross-module-anchor-checked against `health_page.SERVER_DATA_SECTION_ID`; this task's own live battery today confirms exactly one page (`/health`) renders both migrated tables' headers, `/airlines`/`/history`/`/settings` render zero occurrences of either | ✅ existing, extended | ✅ green (source+live, re-run today) |
| D-22 | `/preview.png`/`/gallery/*` byte routes unchanged; `/preview` page route redirects to `/history` (302/303) | source assertion + markup regression + live | `companion/test_companion_app.py` (route dispatch, 144/144 today) — this task's own live route battery today: `GET /preview` → 303 `Location: /history`, location byte-identical against an arbitrary query string (`?next=/evil&foo=bar&x=1`); a real `/gallery/{name}.png` returns a real PNG authenticated and redirects to `/login` unauthenticated. **Note:** `/preview.png` itself was separately retired to a 404 by quick task 260903-c4o (it now serves no purpose once "Now showing" was folded into the gallery) — this is a distinct, later, deliberate change from D-22's own scope, confirmed live today (`GET /preview.png` → 404, not 200 or a redirect) and not a regression of D-22, since D-22 only ever governed the `/preview` *page* route's redirect target, not `/preview.png`'s own long-term fate | ✅ existing, extended | ✅ green (source+live, re-run today) |
| Claude's Discretion (preview_page.py deletion) | File deleted outright, not orphaned | non-goal regression + live | inline verify (re-run today): `test -f companion/pages/preview_page.py` fails (still confirmed absent); repo-wide import-statement scan confirms zero `.py` files import the retired module — only historical prose comments in `history_page.py`/`test_view_pages.py`/`pages/__init__.py` mention the old module name (all past-tense/historical, none are live imports) | n/a | ✅ green (re-run today) |
| D-23/D-25 | Closing checkpoint translates 06.6.3-08's 20 items onto the new page structure; full keyboard-only pass including new controls | browser only | checkpoint items across all groups (A-E below) | n/a | ⬜ awaiting checkpoint |
| D-24 | Real assistive-technology pass (VoiceOver/NVDA/TalkBack) + production authenticated walkthrough — done for real this time, no further deferral | browser/AT tool only | checkpoint items E2 (AT pass), E3 (production walkthrough) — no optional-if-unavailable escape hatch | n/a | ⬜ awaiting checkpoint |
| All | Full composed suite green apart from the one pre-documented exception | full suite | `scripts/run-all-tests.sh` — coverage must stay ≥83% (pyproject.toml threshold) | ✅ existing | ✅ green, re-run today (2026-09-04) — **16/16 harnesses pass, including `server/test_poll_loop.py`** (44/44): the pinned-digest mismatch on macOS/Darwin is now emitted as an informational `NOTE`, not a hard failure — the harness itself was hardened (by work between 09-01 and today) to make the Linux/CI-pinned digest check Linux-strict and non-Linux-informational, so there is no longer even a single accepted exception to document. Confirmed this phase's own commits (`5e453d5..5f48a23`, the full range of plans 01-08) touch exactly `server/plane/illustrations.py` + its own test file under `server/` — zero rendering-pipeline files modified, by direct `git diff --stat` today, not by inference; measured coverage 92% ≥ 83% threshold |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky (decision superseded, current shape green)*

**Real per-harness check counts, measured 2026-09-04 via `scripts/run-all-tests.sh`** (run against branch tip `92bc660`; the "09-01 baseline" column below is this file's own prior, now-stale snapshot, kept for visibility into how much has shipped since — it is NOT this phase's own before/after, since none of the growth between the two columns is attributable to plans 01-08 alone):

| Harness | 09-01 snapshot (stale) | Real, today (2026-09-04) | Notes |
|---|---|---|---|
| `server/test_config_history.py` | 21/21 | 30/30 | grew via later phases/quick tasks, untouched by this phase |
| `server/test_dither.py` | 6/6 | 6/6 | untouched |
| `server/test_enrich.py` | 45/45 | 52/52 | grew via later work, untouched by this phase |
| `server/test_illustrations.py` | 52/52 | 58/58 | plan 02 (+5, `target_variants_by_airline()`); further growth since 09-01 from later illustration-feature quick tasks (v26/e92), not this phase |
| `server/test_panel_preview.py` | 11/11 | 11/11 | untouched |
| `server/test_pipeline_e2e.py` | 6/6 | 6/6 | untouched |
| `server/test_plane_detection.py` | 47/47 | 47/47 | untouched |
| `server/test_poll_loop.py` | 42/43 (1 accepted exception) | **44/44 (no exception)** | the macOS/Darwin digest mismatch is now an informational `NOTE`, not a failure — the harness was hardened between 09-01 and today; untouched by this phase itself |
| `server/test_render.py` | 76/76 | 119/119 | grew substantially via later phases (06.6.4.1.1 theme-picker render work), untouched by this phase |
| `server/test_runway_config.py` | 14/14 | 14/14 | untouched |
| `stub-server/test_poll_cycle.py` | 23/23 | 23/23 | untouched |
| `companion/test_companion_app.py` | 105/105 | **144/144** | plans 02, 07, 08 (this phase) got it to 105; +39 since from 06.6.4.1.1's theme-picker routes and multiple quick tasks (260901-260904) |
| `companion/test_config_page.py` | 54/54 | **72/72** | plans 03, 07 (this phase) got it to 54; +18 since from 06.6.4.1.1's Settings theme-picker markup |
| `companion/test_contrast_check.py` | 34/34 | 36/36 | grew via 06.6.4.1.1, untouched by this phase itself |
| `companion/test_status_pages.py` | 72/72 | **136/136** | plans 04, 06 (this phase) got it to 72; +64 since from Airlines upload feature (v26), Health mobile-card work (ghy), and other quick tasks |
| `companion/test_view_pages.py` | 43/43 | **54/54** | plans 05, 08 (this phase) got it to 43; +11 since, including the D-18/D-19 supersession (260903-c4o) noted above |

**Measured coverage: 92%** (source-scoped to `server`, `stub-server`, `companion` per `pyproject.toml`'s `[tool.coverage.run]`) **against the configured 83% floor** (`pyproject.toml`'s `[tool.coverage.report] fail_under = 83`) — 9 points of margin, re-measured 2026-09-04. This phase's own restructured modules measured **today**: `companion/pages/config_page.py` 100%, `companion/pages/health_page.py` 97%, `companion/pages/history_page.py` 98%, `companion/pages/airlines_page.py` 97% — all still comfortably covered after three days of further evolution on the same modules.

---

## Wave 0 Requirements

*None — this phase restructures existing page markup, merges two routes into two others, adds one new route, and deletes one page module. No new test file or fixture is required; every touched harness (`test_config_page.py`, `test_status_pages.py`, `test_view_pages.py`, `test_companion_app.py`) already exercises the relevant page module or route dispatch table. A planner may choose to add a small dedicated test section for the new Airlines-gallery route's membership-validation contract (mirroring `test_companion_app.py`'s existing runway-image-route tests) rather than growing `test_status_pages.py` further — either is Wave-0-free either way.*

---

## Manual-Only Verifications

**Note on D1/D2 (History "Now showing"):** Task 2's checklist item D1 asks the developer to confirm a "Now showing" section — per the Per-Decision Verification Map above, that section was retired outright by quick task 260903-c4o (2026-09-03) and folded into an always-visible render gallery instead. D1/D2 as literally worded no longer describe what `/history` renders today. This is flagged here, not silently patched into the checklist text, since Task 2's own action text requires the checklist to be presented as the plan wrote it — the developer should be told this ground has shifted before answering D1/D2, rather than the executor unilaterally rewording the checkpoint's own presented items.

| Behavior | Requirement | Why Manual | Test Instructions | Status |
|----------|-------------|------------|--------------------|--------|
| Full visual fidelity to this session's validated sketch artifacts (Settings single-column + save bar, Health Screen/Server-and-data split, Airlines gallery grid, History Now-showing + lightbox) | D-01 through D-22 | No visual regression test exists in this stdlib-only suite; computed-style/structural checks alone previously missed a real mobile-nav bug on this project (project memory: [[feedback-real-device-ui-verification]]) | Run the companion service locally, sign in, and visually compare each of the 4 pages against this session's validated Artifact URLs (recorded in `06.6.4.1-CONTEXT.md`'s `<specifics>` section) in both light and dark theme — **note History's "Now showing" frame no longer exists in this form (see supersession note above); compare against the gallery's current always-visible-grid shape instead** | ⬜ awaiting Task 2 checkpoint |
| History's per-row "View panel near this time" lightbox, clicked with a real pointer | D-20 | A lightbox open/close interaction and the nearest-timestamp match it displays can only be confirmed by actually clicking it against real gallery data | Click the button on a handful of rows spanning different times of day; confirm the lightbox opens, shows a real timestamp close to (before) the row's own time, and closes cleanly | ⬜ awaiting Task 2 checkpoint (item D3) |
| Airlines gallery's illustration route, requested with an invalid/hostile key | D-15 | The validate-then-join safety contract (`_serve_runway_image()`'s own precedent) needs a live request against a real server process, not just a source read, to confirm the 404-on-both-cases behavior holds | Request `/illustration/{key}.png` with a path-traversal-shaped key and confirm a clean 404, not a path-traversal read or a 500 | ✅ done — resolved by Task 1's own live route battery, **re-run today (2026-09-04) against branch tip `92bc660`**, not just carried forward from 09-01: real key (`air-algerie.png`) → 200 PNG (43,070 bytes, real PNG signature); unknown key → clean 404 (the app's own styled 404 page, no traversal read, no leaked content); 3 traversal-key variants (`..%2F..%2F..%2Fetc%2Fpasswd`, `..%2f..%2fapp.py`, `%2e%2e%2f%2e%2e%2fetc%2fpasswd`) → each a clean 404, confirmed by inspecting the actual response body (no `root:` signature, no source-code leak, no 500) |
| A real assistive-technology pass (VoiceOver/NVDA/TalkBack) | D-24 | Screen-reader announcement quality cannot be asserted by a stdlib test harness | Navigate all 4 pages with a real AT tool; confirm disclosures, the save bar, the gallery filter, and the lightbox all announce sensibly, nothing silently or twice | ⬜ awaiting Task 2 checkpoint (item E2 — no optional-if-unavailable escape hatch per D-24) |
| Production authenticated walkthrough | D-24 | Confirms the same code that passed locally also renders correctly against the real deployed environment | Sign in to the live deployed companion service and browse all 4 pages | ⬜ awaiting Task 2 checkpoint (item E3 — no optional-if-unavailable escape hatch per D-24) |
| WCAG keyboard-only pass across all 4 pages including this phase's new controls | D-25 | Subjective reachability/focus-visibility judgment, not something a unit test can assert | Tab through Settings' save bar, Airlines' filter input, and History's lightbox button/close control; confirm every stop has a visible focus indicator | ⬜ awaiting Task 2 checkpoint (item E1) |

### Translated checklist group structure (mirrors Task 2's own checkpoint groups, per D-23)

The closing checkpoint (Task 2) presents 28 items across 5 groups, each translating one or more of 06.6.3-08's original 20 items onto this phase's new page structure:

| Group | Page/Scope | Items | Covers |
|-------|-----------|-------|--------|
| A | Settings | A1-A7 | D-01 (single-column layout), D-03/D-04 (unified save bar), D-02 (Theme swatch), D-06 (no-JS), D-05 (no-JS bottom-button save-together), D-04 (bottom-button hidden with JS), D-05 (LED merged, one accessible group), D-26 (URL/nav-label, `/config` 404-not-redirect) |
| B | Health | B1-B7 | D-10 (Screen/Server-and-data section order), D-09 (battery chart default reading + labeled axes + keyboard nav), D-08 (Corroboration readings-disclosure), D-07 (anomaly banner pills), D-08 (Corroboration compact rows + disclosure), D-10/D-11 (resolution-rate prominence), D-11/D-12 (full-width cards, no horizontal scroll, sticky-header repaint, empty-state scoping) |
| C | Airlines gallery | C1-C4 | D-14 (27 cards, responsive reflow), D-14 (fleet-variant chips, including the A350-1000 trap), D-16 (filter bar), D-15 (illustration-route hostile-key 404 — already resolved by Task 1, re-confirmed here in-browser) |
| D | History | D1-D6 | D-18 (Now-showing framed matte treatment), D-19 (Recent-renders disclosure, layout-jump-free thumbnails), D-20 (per-row View-panel lookup, real click interaction), D-21 (unresolved-airline link lands at the Health anchor), D-18-D-21 (mobile card layout), pre-existing filter/copy behaviour unchanged |
| E | Cross-cutting | E1-E4 | D-25 (full keyboard-only pass across all 4 pages, including this phase's 3 new controls), D-24 (real AT pass — no further deferral), D-24 (production authenticated walkthrough — no further deferral), light/dark theme parity across every visual item above |

Every group/item above is presented verbatim (see 06.6.4.1-09-PLAN.md's Task 2 `<how-to-verify>` block) at the Task 2 checkpoint — this table exists so the validation record and the checkpoint describe the same ground, per this task's own action text.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (Wave 0 not required — see above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (source-assertion coverage spans every decision; only the closing checkpoint plan is manual-only, matching every prior 06.6.x closing-plan precedent)
- [x] Wave 0 covers all MISSING references (none missing)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval: RESOLVED — 2026-09-04.** Task 1 (full-suite gate, live route battery, non-goal regression gate) was re-run for real twice: once against branch tip `92bc660`, and again after PR #44 (Phases 8-11) merged on top mid-checkpoint, rebasing this branch onto `4a31a62`. Both re-runs are green: **16/16 harnesses pass** (the prior 09-01 run's single accepted `server/test_poll_loop.py` exception no longer even applies — a harness hardening now emits it as an informational `NOTE`, not a failure), measured coverage **92%** ≥ the 83% floor, all 19 live-route outcomes and all 4 non-goal-regression checks pass against a really-running service with real state-directory data. Two decisions were explicitly flagged as superseded-in-shipped-form rather than silently re-asserted: D-18/D-19 (History's "Now showing" section, folded into an always-visible render gallery by quick task 260903-c4o) and Settings' addition of two Phase 10/11 sections (Quiet hours, Wake interval) not covered by Group A's original checklist text.

**Task 2's blocking developer checkpoint returned its verdict on 2026-09-04: PASS on all 28 items, no fails, no marginals.** Full verdict recorded verbatim in `06.6.4.1-09-SUMMARY.md`. Groups A-D, E1, and E4 were approved as a single blanket verdict ("j'approuve tout") after the developer was given a local test environment (a real production-data copy plus a public tunnel) and the D-18/D-19 supersession was disclosed beforehand. Group E2 (real assistive-technology pass) and Group E3 (production authenticated walkthrough) — the two items D-24 explicitly forbids silently deferring again — were each confirmed separately and directly, not accepted on the strength of the initial blanket approval alone: E2 confirmed genuinely performed ("Oui, fait pour de vrai"); E3 confirmed after the developer supplied the real production URL (`https://config-92-222-92-167.nip.io`, never previously known to this checkpoint) and personally signed in and browsed it ("non tout me semble bon"). D-23, D-24, and D-25 are all satisfied. Phase 06.6.4.1 is closed at 9/9 plans.
