---
phase: 06.6.4.1
plan: 07
type: execute
wave: 4
depends_on: ["06.6.4.1-02", "06.6.4.1-03"]
files_modified:
  - companion/app.py
  - companion/layout.py
  - companion/pages/config_page.py
  - companion/test_companion_app.py
  - companion/test_config_page.py
autonomous: true
requirements: [D-05, D-26]

must_haves:
  truths:
    - "The Settings page is served at /settings, its nav label reads Settings, its page title reads Settings, and every internal redirect that used to land on the old path now lands there (D-26)."
    - "The old settings path returns the standard 404 — D-26 explicitly declines a redirect, since this is a fresh URL at inception rather than a deprecated bookmark (unlike the Preview page route, which plan 08 does redirect)."
    - "The separate LED POST route no longer exists, its handler no longer exists, and its two markup builders no longer exist — one form, one route, one handler (D-05)."
    - "Theme, Runway, and LED are saved by a single authenticated POST whose rejection is all-or-nothing, and no second settings-writing route remains reachable."
    - "The nav slug-to-icon map's keys still correspond one-to-one with the nav route slugs after the rename, so no nav link silently loses its glyph."
  artifacts:
    - "companion/app.py::SETTINGS_ROUTE (rebound from config_page.SETTINGS_ROUTE), updated _PAGE_TITLES"
    - "companion/layout.py::NAV_TABS first entry and NAV_ICON_IDS first key retargeted to the settings slug"
  key_links:
    - "companion/pages/config_page.py already defines SETTINGS_ROUTE as the single definition of this path (plan 03). app.py must REBIND it, not re-type the literal — app.py imports config_page, so the reverse import is a cycle; this is the same discipline already applied to the runway-image route prefix and the flash-key constants."
    - "layout.py cannot import config_page (page modules import layout, so the reverse is a cycle). Its NAV_TABS entry therefore duplicates the literal under the same documented must-equal contract its script-source constants already carry — and this plan pins that equality with a check rather than leaving it to discipline alone."
    - "Most of app.py's own allowlists derive from NAV_TABS at runtime and self-adjust. The test harnesses do NOT: they hold hardcoded route literals that will silently keep asserting against the old path. Task 3 sweeps them explicitly."
---

<objective>
Flip the Settings route from the old path to `/settings` everywhere it is dispatched, linked, titled, or redirected to, and retire the separate LED POST route together with its handler and its two now-unused markup builders.

Purpose: plan 03 already made the page render a form pointing at the new path and made one handler capable of saving all three settings. This plan is the other half — until it lands, the form's target is not dispatched and a second settings-writing route is still reachable.
Output: one settings route, one settings handler, one nav label, and no orphaned route constants.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
@companion/app.py
@companion/layout.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: rename the settings route across the router and the nav (D-26)</name>
  <files>companion/app.py, companion/layout.py, companion/test_companion_app.py</files>
  <read_first>
    companion/app.py lines 100-125 (the route-constant block, including the comment on the runway-image prefix explaining the rebind-rather-than-retype discipline this rename must follow) and lines 185-196 (the page-titles dict, which is NOT derived from the nav tuple and must be hand-edited);
    companion/app.py lines 199-225 (`_validated_next_route()` — read its docstring, which names a count of known routes and names the old path as the post-login default; both statements become inaccurate) and lines 717-738 (`_referring_tab()` and `_render_tab()` — the second allowlist derived from the nav tuple, and the unreferenced tab renderer that reads the page-titles dict);
    companion/app.py lines 742-800 (do_GET's login-redirect branch and the settings page block) and lines 872-975 (do_POST in full, plus `_handle_poll_now()`'s three redirect targets and `_handle_login_post()`'s default);
    companion/app.py lines 526-534 (`_not_found_page()` — it builds a link back to the settings route);
    companion/layout.py lines 26-32 (the nav tuple) and lines 251-262 (the slug-to-icon map, whose keys must track the slug because `_nav_links()` derives the slug from the route) and lines 493-520, 561-610 (`_nav_links()` and `sidebar_nav()` — confirm they derive everything from the tuple and need no edit);
    companion/pages/config_page.py (the settings-route constant plan 03 added — take its real name and value);
    companion/test_companion_app.py — every hardcoded occurrence of the old settings path (there are several: a tab-iteration tuple, two redirect-location assertions, a login next-route assertion, and a direct GET) and the file's real `EXPECTED_CHECK_COUNT`
  </read_first>
  <action>
    In `companion/app.py`, replace the settings route constant with `SETTINGS_ROUTE = config_page.SETTINGS_ROUTE` — a rebind, not a re-typed literal, carrying the same explanatory comment the runway-image prefix constant already carries. Update every reference to the old constant name across the module: the login-authenticated redirect, the not-found page's back-link, the referring-tab fallback, the login POST's default target, and all three poll-trigger redirect targets. Change the do_GET page block's path comparison to the new constant and its two page-shell arguments to the Settings title and the settings slug. Update the page-titles dict's settings key and its value. Leave the `/preview` entry and the preview page block alone — plan 08 owns those.
    <!-- planner-discipline-allow: CONFIG_ROUTE -->

    Rewrite `_validated_next_route()`'s docstring so it no longer states a route count that the nav tuple is about to change again in plan 08, and so it names the new default. Its body needs no change — it derives its allowlist from the nav tuple at runtime, which is exactly why it self-adjusts.

    In `companion/layout.py`, change the nav tuple's first entry to the settings route and the label "Settings", and change the slug-to-icon map's first key to the settings slug. Leave the SVG symbol id itself unchanged: it is a gear glyph, visually correct for Settings, and renaming the symbol is cosmetic churn UI-SPEC §5.0 explicitly marks optional — the icon whitelist stays at its current membership. Add a comment on the nav tuple entry recording the must-equal relationship with the page module's own route constant and the cycle that prevents importing it.

    Then update `companion/test_companion_app.py`: retarget every hardcoded old-path literal onto the new one — the tab-iteration tuple's first pair (route and heading both), the two redirect-location assertions, the login next-route round-trip, and the direct authenticated GET. Add new checks pinning the contract: the module's route constant is the same object as the page module's; the nav tuple's first route equals it; the page-titles dict has a key for it; the old path now returns 404 for an authenticated session (D-26 declines a redirect, so a 404 is the correct and intended outcome, not an oversight); and every key of the slug-to-icon map corresponds to a nav tuple route slug and vice versa. Bump `EXPECTED_CHECK_COUNT` by the number of genuinely NEW checks only, computed from the file's real on-disk starting value at execution time.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion (live HTTP): authenticated `GET /settings` returns 200 and its body contains the Settings page heading.
    - Behaviour assertion (live HTTP): authenticated `GET` of the old settings path returns 404.
    - Behaviour assertion (live HTTP): a successful login with no next-route redirects to `/settings`; a successful settings POST redirects to `/settings` with a flash query.
    - Behaviour assertion (live HTTP): an unauthenticated `GET /settings` redirects to login carrying `/settings` as its next value, and that value survives the round-trip as a rendered hidden field.
    - Cross-module assertion: `app.SETTINGS_ROUTE == config_page.SETTINGS_ROUTE == layout.NAV_TABS[0][0]`, and `set(layout.NAV_ICON_IDS) == {r.lstrip("/") for r, _ in layout.NAV_TABS}`.
    - Test command: `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`.
  </acceptance_criteria>
  <done>Settings is served, linked, titled, and redirected to at its new path; the icon map and the nav tuple provably agree; the old path 404s by design.</done>
</task>

<task type="auto">
  <name>Task 2: retire the separate LED POST route, its handler, and its two orphaned builders (D-05)</name>
  <files>companion/app.py, companion/pages/config_page.py, companion/test_config_page.py, companion/test_companion_app.py</files>
  <read_first>
    companion/app.py lines 100-110 (the LED route constant) and lines 939-975 (do_POST in full — the LED branch, its form read, its handler call, and its redirect);
    companion/pages/config_page.py — the LED markup builder, the section wrapper that emits the separate form, and the LED post handler, each carrying the removal note plan 03 left on it naming this plan as their removal site; and `handle_post()` as plan 03 extended it, to confirm the LED validation genuinely lives there before deleting the old handler;
    companion/test_config_page.py — the three checks exercising the old LED handler and the two checks exercising the old section wrapper (their behaviour is now covered by the merged-handler checks plan 03 added; verify that before deleting, do not assume it);
    companion/test_companion_app.py — the live-HTTP checks that POST to the separate LED path, both authenticated and unauthenticated
  </read_first>
  <action>
    Delete the LED route constant from `companion/app.py` and delete its whole branch from do_POST — the form read, the handler call, and the redirect. Nothing replaces it: the merged settings POST already reads and validates the LED field, so the path simply becomes unrouted and falls through to the standard 404.
    <!-- planner-discipline-allow: LED_ROUTE -->
    <!-- planner-discipline-allow: /config-led -->

    Delete from `companion/pages/config_page.py` the LED post handler, the LED markup builder, and the section wrapper that emitted the separate form. Before deleting the handler, confirm by reading `handle_post()` that all three of its documented submitted-value shapes — absent means off, the accepted checkbox value means on, anything else rejects the whole submission — are genuinely implemented there. If any of the three is missing, stop and report it rather than deleting the handler; losing a validation branch is a security regression, not a simplification. Leave the LED helper text and the accepted-checkbox-value constants alone — the surviving group builder and the merged handler both still use them.
    <!-- planner-discipline-allow: handle_led_post -->
    <!-- planner-discipline-allow: led_section -->
    <!-- planner-discipline-allow: led_fieldset -->

    Then update the two harnesses. In `companion/test_config_page.py`, delete the checks that exercise the now-deleted handler and the now-deleted section wrapper, after confirming the merged-handler checks plan 03 added cover the same three value shapes plus the cross-field rejection. In `companion/test_companion_app.py`, retarget the two live-HTTP LED checks: the authenticated one now posts the LED field to the settings route and asserts the same persisted outcome and the same redirect-with-flash shape; the unauthenticated one now posts to the settings route and asserts no config file was created or modified. Add one new check asserting the retired LED path returns 404 for an authenticated session. Recompute each file's `EXPECTED_CHECK_COUNT` from its real on-disk value at execution time, subtracting deletions and adding new checks, and extend each file's own history-comment block in its established format.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py &amp;&amp; server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour assertion (live HTTP): an authenticated POST to the retired LED path returns 404.
    - Behaviour assertion (live HTTP): an authenticated POST to `/settings` with the LED field present persists the enabled state and redirects with the saved flash key; the same POST with the field absent persists the disabled state.
    - Behaviour assertion (live HTTP): an unauthenticated POST to `/settings` creates no config file and modifies no existing one.
    - Behaviour assertion: an authenticated POST to `/settings` carrying a crafted LED value returns the save-failed flash and leaves the config file byte-identical — the all-or-nothing rejection survives the route retirement.
    - Source assertion: `companion.pages.config_page` imports cleanly and exposes neither the deleted handler nor the deleted section wrapper nor the deleted markup builder.
    - Source assertion: `companion.app` exposes no LED route constant.
    - Test command: both harnesses exit 0 at their recomputed check counts.
  </acceptance_criteria>
  <done>One settings route, one settings handler, no second settings-writing path reachable, and the all-or-nothing rejection demonstrably intact.</done>
</task>

<task type="auto">
  <name>Task 3: literal sweep and a standing route-contract guard</name>
  <files>companion/test_companion_app.py, companion/test_config_page.py</files>
  <read_first>
    companion/test_companion_app.py in full for hardcoded route strings — search for every occurrence of the old settings path, the retired LED path, and any prose comment asserting a count of nav routes (the research for this phase identified several such call sites plus comments that state a route count nearby, which is a signal that the surrounding code assumes it);
    companion/test_config_page.py for the same;
    companion/test_status_pages.py and companion/test_view_pages.py — confirm by grep that neither contains a hardcoded settings-route literal (this plan does not own those files, so if either DOES, stop and report rather than editing them);
    companion/layout.py::NAV_TABS and companion/app.py::_PAGE_TITLES as this plan just left them
  </read_first>
  <action>
    Sweep both harnesses this plan owns for any remaining hardcoded occurrence of the old settings path or the retired LED path and retarget or delete each one — the runtime allowlists derive from the nav tuple and self-adjust, but these literals do not, and a stale literal that still passes is worse than one that fails, because it silently asserts against a route nobody serves. Also correct any nearby prose comment that states a nav-route count, since that count changes again in plan 08.

    Add one standing contract guard to `companion/test_companion_app.py`, in the shape of this codebase's existing three-file DOM-contract guards: assert that for every entry in the nav tuple there is a page-titles entry with the same key, that the two collections have the same size, that every nav slug has a slug-to-icon entry and vice versa, and that the page module's own route constant is the nav tuple's first route. This guard is what makes the remaining nav change in plan 08 fail loudly rather than silently if any of the four collections is missed.

    Confirm by grep that the two harnesses this plan does not own carry no settings-route literal. If either does, do not edit it — report it in the SUMMARY as a finding for plan 08, which owns one of them.

    Recompute both files' `EXPECTED_CHECK_COUNT` from their real on-disk values at execution time.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py &amp;&amp; server/.venv/bin/python3 companion/test_companion_app.py &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py &amp;&amp; server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: neither harness this plan owns contains a hardcoded occurrence of the old settings path or the retired LED path.
    - Contract assertion: the nav tuple and the page-titles dict have equal length and identical key sets; the slug-to-icon map's key set equals the set of nav slugs.
    - Test command: all four companion harnesses exit 0 at their recorded check counts.
    - Behaviour assertion: a full authenticated walk of all five currently-routed page paths returns 200 for each (the preview page is still routed until plan 08).
  </acceptance_criteria>
  <done>No stale route literal survives in the two harnesses this plan owns, and a standing guard now fails loudly if any of the four route collections drifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client→settings route | The renamed route must stay behind the same session gate the old one had. |
| client-supplied next-route value→post-login redirect | The login flow round-trips a caller-supplied redirect target. |
| authenticated client→settings POST | Now the only route that writes device configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-20 | Elevation of Privilege | renamed settings route's session gate | high | mitigate | The renamed GET block and the renamed POST branch each keep their existing `require_session()` call in the same position; the rename changes only the compared literal. Verified by Task 1's unauthenticated-GET check and Task 2's unauthenticated-POST check, which asserts no config file is created or modified. |
| T-06.6.4.1-21 | Spoofing (open redirect) | `_validated_next_route()` after the nav tuple changes | high | mitigate | The function keeps deriving its allowlist from the nav tuple at runtime and keeps its exact-membership equality test — no parsing, no prefix check, no regex. Only its docstring changes. The renamed route enters the allowlist through the tuple, and the old path leaves it, both automatically. Verified by Task 1's next-route round-trip check. |
| T-06.6.4.1-04 | Tampering | retirement of the second settings-writing route | medium | mitigate | Removing the separate LED route reduces the config-writing surface from two authenticated routes to one, and the surviving handler validates the union of both former field sets with all-or-nothing rejection. Task 2's crafted-value check asserts the rejection survived the retirement rather than assuming it. |
| T-06.6.4.1-22 | Denial of Service | orphaned route constants | low | accept | The retired path becomes unrouted and falls through to the existing 404 handler, which is already the behaviour for every unknown path; no new code path and no new failure mode is introduced. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; see `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit (not applicable). |
</threat_model>

<verification>
1. All four companion harnesses exit 0 at their recomputed counts.
2. Live manual smoke against a locally-run instance: sign in, land on `/settings`, change the LED checkbox and the runway together, save once, confirm one redirect with the saved flash and both values persisted in `device_config.json`.
3. `git diff --stat` across this plan's commits touches exactly the five files in `files_modified`; `git diff --quiet companion/pages/health_page.py companion/pages/airlines_page.py companion/pages/history_page.py companion/static/` reports no change.
4. `scripts/run-all-tests.sh` green apart from the single pre-existing `server/test_poll_loop.py` digest mismatch.
</verification>

<success_criteria>
- Settings served, linked, titled, and redirected to at `/settings`; old path 404s by design.
- Exactly one config-writing route, one handler, all-or-nothing rejection proven post-retirement.
- Nav tuple, page-titles dict, icon map, and page-module constant provably agree, pinned by a standing guard.
- No stale route literal in either harness this plan owns.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan:** `companion/app.py::SETTINGS_ROUTE` (rebound from `config_page.SETTINGS_ROUTE`, replacing the old settings route constant) and the retargeted `_PAGE_TITLES` settings key; `companion/layout.py::NAV_TABS`' first entry and `NAV_ICON_IDS`' first key retargeted to the settings route and slug; the new route `GET /settings` (the old path 404s by design). Removed by this plan: `companion/app.py`'s separate LED route constant and its `do_POST` branch, and `companion/pages/config_page.py`'s `led_fieldset()`, `led_section()`, and `handle_led_post()`.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-07-SUMMARY.md` when done
</output>
