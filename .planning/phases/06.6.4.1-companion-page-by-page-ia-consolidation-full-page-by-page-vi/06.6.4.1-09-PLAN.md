---
phase: 06.6.4.1
plan: 09
type: execute
wave: 6
depends_on: ["06.6.4.1-01", "06.6.4.1-02", "06.6.4.1-03", "06.6.4.1-04", "06.6.4.1-05", "06.6.4.1-06", "06.6.4.1-07", "06.6.4.1-08"]
files_modified:
  - .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-VALIDATION.md
autonomous: false
requirements: [D-23, D-24, D-25]

must_haves:
  truths:
    - "Every decision row in this phase's validation map carries a real, recorded status — not the pending placeholder it was drafted with."
    - "The full composed suite is green apart from the single pre-documented, accepted macOS-local exception, and coverage still meets the configured threshold."
    - "The four page routes, the two retired routes, and the one new image route all behave as specified against a really-running service, not only against source assertions."
    - "The developer has personally worked through the translated checklist in a real browser, in BOTH light and dark theme, and signed off — including the real assistive-technology pass and the production walkthrough that were each deferred twice before this phase (D-24)."
    - "D-25: a full keyboard-only pass across all four pages has been performed and confirmed reachable/operable with a visible focus indicator at every stop, explicitly including this phase's new controls — the unified Settings save bar's Save/Cancel, the Airlines gallery's filter input, and History's View-panel trigger plus the dialog's Close control."
  artifacts:
    - "06.6.4.1-VALIDATION.md with every Per-Decision Verification Map row resolved and a completed Approval line"
    - "06.6.4.1-09-SUMMARY.md carrying the developer's verbatim verdict per checklist item and a discrete gap entry for every failure or marginal"
  key_links:
    - "This project's own memory records a case where computed-style and structural checks alone passed while a genuine mobile navigation bug shipped. Nine plans of source assertions do not substitute for the developer's own eyes on a real browser — that failure mode is precisely what this checkpoint controls for."
    - "D-24's two items (a real assistive-technology pass, a production authenticated walkthrough) were each deferred once from the original UX audit and again at the previous phase's closing plan. The developer's explicit choice this session is that they are done for real here, with no further deferral — they do NOT carry the optional-if-unavailable escape hatch the previous phase's checklist gave them."
---

<objective>
Close the phase: prove the automated gates hold against the fully-assembled result, complete the phase's validation record, and then obtain the developer's own real-browser sign-off across the translated checklist — including the two items that have now been deferred twice.

Purpose: every decision in this phase was validated against standalone HTML sketch artifacts, not against the real rendered application. This plan is where nine plans of applied change meet a real browser, a real screen reader, and the real deployed service.
Output: a completed validation record and a recorded developer verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-CONTEXT.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-VALIDATION.md
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: full-suite gate, live route battery, non-goal regression gate, and the validation record</name>
  <files>.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-VALIDATION.md</files>
  <read_first>
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-VALIDATION.md (the Per-Decision Verification Map with its pending status column, the Manual-Only Verifications table, and the Approval line — the three things this task resolves);
    all eight of this phase's SUMMARY files (the real task list, the real check-count before and after values per harness, and every recorded deviation — the map must reflect what actually shipped, not what was planned);
    scripts/run-all-tests.sh (the canonical harness array — the single source of truth for what "full suite" means, and confirm the array still lists every harness this phase touched);
    pyproject.toml's coverage configuration (the configured minimum, so the coverage claim below is checked against the real number rather than an assumed one)
  </read_first>
  <action>
    Run the full composed suite and record the real result. The single accepted exception is the pre-existing, unrelated macOS-local Pillow/FreeType panel-digest mismatch in the poll-loop harness; before accepting it, confirm from this phase's own commits that no rendering-pipeline file under the server package was modified — this phase reads the illustrations module and adds one function to it, and modifies nothing else there. If any other failure appears, it is a real regression and this task stops.

    Run the live route battery against a locally-started companion service with a populated state directory (real history data, real device-health rows, a real gallery directory, a real panel file). Record each result, pass or fail, in the validation record:
    - Each of the four page routes returns 200 for an authenticated session and redirects an unauthenticated one.
    - The retired settings path returns 404; the retired LED path returns 404.
    - The retired preview page path returns a redirect whose location is the history route, and the location is unchanged by an arbitrary query string.
    - The live-panel image route and a real gallery image route both return a PNG for an authenticated session and redirect an unauthenticated one.
    - The illustration image route returns a PNG for a real key, 404 for an unknown key, and 404 for a key carrying a path-traversal sequence — with no traversal read and no 500 in either failure case. This is the manual-only verification the phase's validation record names for this decision; run it against a real server process, not against a source read.

    Run the phase's non-goal regression gate and record it:
    - The Airlines gallery page module opens no database and reads no poll state (the deferred cross-reference stayed deferred).
    - Exactly one page renders the unresolved-prefix table's headers and the resolution-statistics table's headers.
    - The deleted preview page module is absent from disk and nothing imports it.
    - The stylesheet still contains zero external references and gained no new custom property across the whole phase.

    Then complete `06.6.4.1-VALIDATION.md`: resolve every Per-Decision Verification Map row's status column from the real evidence above and from each SUMMARY's recorded outcome; record the real per-harness check counts this phase ended at, alongside the baselines already noted in the file; record the measured coverage against the configured minimum; and leave the Approval line pending, since Task 2 is what resolves it. Add the translated checklist's group structure to the Manual-Only Verifications table so the record and the checkpoint describe the same ground.
  </action>
  <verify>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `scripts/run-all-tests.sh` reports every harness green except the single pre-documented poll-loop digest mismatch, and that exception is justified in the record by a confirmed empty diff over the server rendering pipeline.
    - CLI/behaviour assertion: the live route battery's eleven listed outcomes are each recorded with a real observed status.
    - Behaviour assertion: the illustration route's traversal-key request returns 404 with no file content and no server error, observed against a running process.
    - Source assertion: the four non-goal regression items are each recorded as verified.
    - Artifact assertion: `06.6.4.1-VALIDATION.md` has no remaining pending status in its Per-Decision Verification Map, and its Approval line is explicitly marked as awaiting Task 2.
  </acceptance_criteria>
  <done>Every automated and live-service gate this phase can assert is asserted and recorded, and the validation record reflects what shipped rather than what was planned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: blocking developer verification pass across the four consolidated pages</name>
  <files>.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-VALIDATION.md</files>
  <read_first>
    .planning/phases/06.6.3-companion-per-page-redesign-config-health-history-airlines-p/06.6.3-08-PLAN.md (Task 2's original 20-item checklist — the ground this phase's checkpoint formally closes, per D-23; read it so the translation below is checked against the original rather than assumed faithful);
    .planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-RESEARCH.md (its mapping table from those 20 items onto this phase's new page structure);
    all eight of this phase's SUMMARY files (so any recorded deviation can be raised with the developer at the checkpoint rather than discovered by them)
  </read_first>
  <action>
    Present the checklist below to the developer, wait for their verdict, and record it. This is a blocking checkpoint: stop and wait. Do not proceed, do not infer answers, and do not fill in items the developer did not answer.

    When the verdict arrives:
    1. Record every item's outcome — pass, fail, or marginal — verbatim in the developer's own words, not paraphrased.
    2. Turn each failed or marginal item into a discrete, actionable gap entry in this plan's SUMMARY: what was expected, what was seen, and which of this phase's plans 01 through 08 it traces to, so a gap-closure pass has something concrete to plan against.
    3. Fix nothing. A checkpoint that edits the thing it is verifying stops being a checkpoint.
    4. Resolve the Approval line in `06.6.4.1-VALIDATION.md` from the developer's verdict.
    5. Group E items 2 and 3 (the assistive-technology pass and the production walkthrough) carry no optional-if-unavailable escape hatch. Each was deferred once from the original UX audit and again at the previous phase's closing plan; the developer's explicit choice this session is that both are done for real here. If either genuinely cannot be run in this session, that is a blocking outcome to be raised and decided by the developer, not a note to be silently written.
  </action>
  <what-built>
    All four companion pages were reorganized per this phase's twenty-six locked decisions. Settings (renamed from Config, now served at its own route) is a single-column stack of three described sections — Theme, Runway, Diagnostic LED — inside one form with one unified unsaved-changes bar that names which sections changed, one merged save handler, and a bottom fallback Save button that now hides once JavaScript is confirmed running. Health restructured into two headed sections: Screen (device freshness plus a battery chart that shows a real reading before you touch it and carries labeled axes) and Server & data (pipeline freshness, a corroboration tile whose explanations collapsed behind a disclosure, a new resolution-rate tile, and the old Airlines page's two diagnostics sections promoted into full-width cards); its anomaly banner now names failing categories as short pills. Airlines was reborn as an illustration gallery — one card per airline in the panel renderer's own art set, with fleet-variant chips and a filter bar, served through a new session-gated image route. History absorbed Preview entirely: a Now-showing section at the top, the render gallery demoted into a collapsed disclosure, a per-row View-panel button that opens the nearest recorded render in a native dialog, and an inline link to Health for rows whose airline could not be resolved. The Preview page module was deleted and its route now redirects to History; navigation is four items.
  </what-built>
  <how-to-verify>
    Start the companion service locally against a populated state directory (real history rows, real device-health rows, a real gallery directory with several renders spanning different times of day, and a real current panel file) and sign in with a real browser. Work through each group and note a genuine Pass / Fail / Observation per item — do not rubber-stamp. **Check every visual item in BOTH light and dark theme.**

    **Group A — Settings**
    A1. The three sections stack in one column at every width, each with its own heading and a short description sentence under it. The Runway cards render as a proper grouped block, not as loose elements floating outside a container — this is the specific bug this phase set out to root-cause.
    A2. Change the Runway selection: the unified bar appears at the page level naming "Runway changed". Now also toggle the LED checkbox: the same single bar updates to name both sections. Save once — confirm both changes persist in one action and one confirmation. Then repeat, and confirm Cancel reverts both without saving.
    A3. The Theme section still shows a read-only swatch of two colour chips reflecting the real on-glass panel colours, with no radio buttons.
    A4. With JavaScript disabled: the page still renders correctly, the unified bar never appears, and the bottom Save Settings button is visible and still saves the Runway and LED changes together.
    A5. With JavaScript enabled: the bottom Save Settings button is hidden from page load onward, so only the unified bar can save. Before this phase both were visible at once — confirm that is fixed.
    A6. "Diagnostic LED" appears once, with one accessible group name, and renders correctly as a third sibling section inside the merged form with no orphaned or mismatched styling against Theme and Runway.
    A7. The URL reads as the settings path; the nav label reads Settings. Visiting the old config path returns a 404 — this is intended, not a bug: this decision deliberately declined a redirect because it is a fresh URL at inception rather than a bookmarked page being removed.

    **Group B — Health**
    B1. The body reads as two headed sections in order — Screen, then Server & data — with device freshness and the battery chart under the first, and pipeline freshness, corroboration and resolution rate under the second.
    B2. The battery chart shows a real millivolt reading and timestamp before you touch anything. Both axes carry legible labels (min/max millivolts, oldest/newest clock times) — check legibility in dark theme specifically, since these are the smallest text in the app. Tab into the chart: exactly one Tab stop lands on it; Left/Right/Home/End move between readings and update the readout; moving away leaves the latest reading showing rather than a blank or a prompt.
    B3. The readings table still starts collapsed behind its "View N readings" summary, with the chart above it.
    B4. Observe or seed a real anomaly: the banner names the failing category or categories as short label pills rather than repeating the tiles' full sentences, and a warning-only state reads as a warning rather than as an error.
    B5. The Corroboration tile shows three compact rows (dot, label, count) always visible, with a "More details" disclosure holding all three explanation sentences — collapsed by default.
    B6. The resolution-rate figure is prominent and legible within Server & data, and reads correctly against the real data.
    B7. Unresolved prefixes and Resolution statistics render as full-width stacked cards. The five-column prefix table has real room and does NOT force the page itself to scroll horizontally — this is the second bug this phase set out to root-cause. Scroll a long table and confirm its sticky header repaints correctly against the page background rather than as a solid contrasting band; this specific token was flagged in the previous phase as never having been checked against a real browser. Confirm the "No resolution data yet" empty state, if you can produce it, reads as scoped to its own card rather than as a page-wide contradiction sitting above real data.

    **Group C — Airlines gallery**
    C1. All twenty-seven cards render with their real illustration art and correct airline names. Resize from desktop to tablet to phone width and confirm the grid reflows sensibly with no clipped or stretched images.
    C2. Fleet-variant chips are correct — the Air Caraïbes card carries three, including the A350-1000 variant; an airline with no variants carries no chip row at all rather than an empty one.
    C3. The filter box works the same way History's does: rows filter live, the count updates, Clear resets, and an unmatched query shows the "No matching airlines" message.
    C4. Request an illustration URL with a hostile or nonsense key (including one containing a path-traversal sequence) and confirm a clean 404 — not a file read, not a server error.

    **Group D — History**
    D1. The Now showing section renders the current panel inside the centered, framed matte treatment, with a "Captured ..." caption and the colour-accuracy caveat present.
    D2. Recent renders is collapsed by default; its summary count matches the number of thumbnails inside; expanding it shows captioned thumbnails that load with no visible layout jump, each opening full size when clicked.
    D3. Click the View-panel button on several rows spanning different times of day. Confirm the dialog opens, shows a real render whose labeled timestamp is at or before that row's own time, carries the one-line note that this is the nearest recorded render rather than a guaranteed match, and closes cleanly both via its Close button and via the Escape key.
    D4. Find a row whose airline could not be resolved: confirm it carries the inline link, and that clicking it lands on Health's Server & data section rather than at the top of the page.
    D5. At phone width, confirm one compact card renders per flight with its details disclosure intact, human-readable state and runway labels, and the View-panel button present and comfortably tappable.
    D6. The filter box and the copy-to-clipboard buttons still behave exactly as they did before this phase.

    **Group E — Cross-cutting**
    E1. A full keyboard-only pass across all four pages, explicitly including this phase's three new controls: the unified Settings save bar's Save and Cancel, the Airlines gallery filter input, and History's View-panel trigger and the dialog's Close control. Every stop reachable, operable, and carrying a visible focus indicator.
    E2. A real assistive-technology pass (VoiceOver, NVDA, or TalkBack) across all four pages. Confirm the Corroboration and Recent-renders disclosures, the unified save bar's changed-sections announcement, the two filter inputs' live counts, the anomaly banner's pills, and the View-panel dialog all announce sensibly — nothing silent, nothing announced twice, and the dialog traps and restores focus correctly. This item was deferred twice before; it is done for real now.
    E3. A production authenticated walkthrough: sign in to the live deployed companion service and browse all four pages, confirming the same code that passed locally also renders correctly there. This item was deferred twice before; it is done for real now.
    E4. Confirm every visual item above was checked in both light and dark theme, and name any item where the two differ.
  </how-to-verify>
  <verify>
    <human-check>The developer's own verdict on all twenty-eight items, recorded verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    - Every one of the twenty-eight items has a recorded outcome in the developer's own words — no item silently skipped, no item answered by the executor.
    - Every failed or marginal item has a discrete gap entry in the SUMMARY naming what was expected, what was seen, and which plan (01-08) it traces to.
    - Group E items 2 and 3 are recorded as genuinely performed, or explicitly raised to the developer as a blocking decision — never quietly noted as skipped.
    - `06.6.4.1-VALIDATION.md`'s Approval line is resolved from the verdict.
    - No source file was modified by this task.
  </acceptance_criteria>
  <resume-signal>Report each item's Pass / Fail / Observation (a summary verdict per group is fine when every item in that group passed cleanly), or describe what needs fixing.</resume-signal>
  <done>The developer has personally verified the consolidated four-page app in a real browser in both themes, with a real screen reader and against production, and the verdict is recorded item by item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→every route this phase added, renamed, or retired | The live route battery in Task 1 is the phase's only end-to-end confirmation that each gate landed where the source says it did. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.4.1-01 | Tampering / Information Disclosure | illustration image route | high | mitigate | Task 1's live traversal-key request against a running process is the phase's end-to-end confirmation of the validate-then-join boundary — the source assertion in plan 02 proves the code shape, this proves the running behaviour. |
| T-06.6.4.1-02 | Spoofing (open redirect) | retired preview page route | high | mitigate | Task 1's live check that an arbitrary query string leaves the redirect location byte-identical confirms the target is genuinely fixed and not derived from any request value. |
| T-06.6.4.1-03 | Elevation of Privilege | session gates across every touched route | high | mitigate | Task 1's route battery exercises each of the four page routes, the two retired paths, and all three image routes unauthenticated as well as authenticated, so no gate can have been dropped during nine plans of route edits without this catching it. |
| T-06.6.4.1-26 | Repudiation | phase sign-off record | low | mitigate | The developer's verdict is recorded verbatim per item in the SUMMARY and reflected in the validation record's Approval line, so what was and was not verified is legible after the fact rather than reconstructed. |
| T-06.6.4.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurred anywhere in this phase; `06.6.4.1-RESEARCH.md`'s Package Legitimacy Audit records it as not applicable (stdlib Python, hand-rolled ES5, no build step). Task 1 re-confirms by checking that no dependency manifest was modified by any of this phase's commits. |
</threat_model>

<verification>
1. `scripts/run-all-tests.sh` green apart from the single pre-documented, justified exception.
2. Measured coverage meets the configured minimum.
3. The live route battery's eleven outcomes and the four non-goal regression items are all recorded.
4. `06.6.4.1-VALIDATION.md` carries no pending status and a resolved Approval line.
5. `git diff --stat` for this plan touches only planning artifacts — no source file.
</verification>

<success_criteria>
- Every validation-map row resolved from real evidence.
- Every route this phase added, renamed, or retired confirmed against a running service, both authenticated and not.
- The developer's own verdict recorded item by item, in both themes, including the two twice-deferred items.
- Every failure or marginal converted into an actionable gap entry rather than a note.
</success_criteria>

## Artifacts this phase produces

Full phase-wide symbol inventory, with every entry tagged by the plan that owns it:
@.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-ARTIFACTS.md

**Owned by THIS plan:** planning artifacts only — a completed `06.6.4.1-VALIDATION.md` (every Per-Decision Verification Map row resolved, real per-harness check counts and coverage recorded, Approval line resolved) and `06.6.4.1-09-SUMMARY.md` carrying the developer's verbatim per-item verdict and one discrete gap entry per failure or marginal. This plan creates no source symbol and modifies no source file; that constraint is itself an acceptance criterion.

<output>
Create `.planning/phases/06.6.4.1-companion-page-by-page-ia-consolidation-full-page-by-page-vi/06.6.4.1-09-SUMMARY.md` when done
</output>
