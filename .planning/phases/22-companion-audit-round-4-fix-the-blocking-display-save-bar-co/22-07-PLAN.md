---
phase: 22-companion-audit-round-4-fix-the-blocking-display-save-bar-co
plan: 07
type: execute
wave: 4
depends_on: [22-02, 22-03, 22-04]
files_modified:
  - companion/pages/home_page.py
  - companion/static/style.css
  - companion/i18n_fr/home.py
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-26, CFG-30]

must_haves:
  truths:
    - "X2: Home's Frame tile reads the one state result — it shows the same clock time and the same state as the strip above it, and never a warning while the strip is neutral"
    - "B2: Home renders the verdict-free `pipeline_detail_html` under its OWN verdict, instead of re-embedding Health's verdict and stacking two"
    - "B2: Home's three status tiles share one height on a phone (they measure 112 / 112 / 131 today)"
    - "B18: the recent-flight time is one line — the clock and the relative age on the same line, the age in the muted label voice, and the clock out of the monospace family"
    - "X4: airline names on Home come from display_airline_name(), so Home and Flights render the same string for the same row"
    - "X4: exactly one element on the page is named 'Frame' — the collision between the strip heading and the tile caption is resolved"
    - "X4: ordering and naming defects only — the hero redesign is D4, Phase 23, and the hero's composition is not touched"
  artifacts:
    - path: "companion/pages/home_page.py"
      provides: "a Home that reads one frame state, one pipeline detail and one airline-name function"
      contains: "display_airline_name"
  key_links:
    - from: "companion/pages/home_page.py"
      to: "companion/frame_state.py"
      via: "the one state resolution the strip also reads"
      pattern: "frame_state\\."
    - from: "companion/pages/home_page.py"
      to: "companion/pages/health_page.py"
      via: "the published verdict-free pipeline detail key"
      pattern: "pipeline_detail_html"
---

<objective>
Home currently disagrees with the strip printed directly above it and with the
Flights page listing the same aircraft. Its Frame tile derives its own staleness
verdict; its Flight-data tile re-embeds Health's whole verdict block under its
own, producing "A little stale / A little behind · no reading yet"; its recent
flights name airlines from the raw row ("CCM Airlines") while Flights resolves
them ("Air Corsica"); its recent-flight time wraps onto two monospace lines; and
"Frame" is simultaneously the strip's heading and a tile's caption.

Purpose: this is the last consumer of the one state truth, and the one page where
a household member looks first. Everything here is an ordering or naming
correction — the hero redesign is Phase 23's D4 and is explicitly out of scope,
so a plan that starts recomposing the hero has gone wrong.
Output: a Home whose three tiles agree with the strip, with Health and with
Flights, and whose recent-flight rows read on one line.
</objective>

<decisions_covered>
D-03's Home consumer, D-07's B2 (Home half), B18 and X4 rows, and UI-SPEC §2's
B2, B18 and X4 contracts plus the X8 note that Home's status grid gains
`align-items: stretch`. The hero redesign, the day timeline and the countdown are
D4/D13/D1 — Phase 23, and named in UI-SPEC §6 so their absence is not a miss.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-CONTEXT.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-AUDIT.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-UI-SPEC.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-03-SUMMARY.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-04-SUMMARY.md
@.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md

<files_owned>
This plan is the ONLY writer, in its wave, of every file in `files_modified`.
`companion/pages/health_page.py`, `companion/layout.py`, `server/history_db.py`,
`companion/static/battery-trend.js` and `companion/test_status_pages.py` belong to
plan 22-06 in this same wave — do not touch them. In particular, do not change
`pipeline_detail_html`'s value or `frame_strip_html()`'s behaviour: consume both
as plans 22-03 and 22-04 published them.
</files_owned>

<interfaces>
Facts read live on 2026-09-12 (re-verify before editing; line numbers drift):

  companion/pages/home_page.py:114,150  the comments already naming `device_detail_html` as "a single
                                  bare <span>, no blocks at all" — the shape `pipeline_detail_html`
                                  now matches
  companion/pages/home_page.py:209      the raw `row["airline"]` read — the X4 naming defect
  companion/pages/home_page.py:239-317  `_status_card_html()`, including the verdict re-embedding at
                                  :257-288 and `_plain_text_from_markup()` at :271
  companion/pages/home_page.py:421      the existing `wake.next_wake_at_iso(...)` call — migrate it to the
                                  richer accessor plan 22-02 added
  companion/pages/history_page.py       READ-ONLY: `display_airline_name()`'s import path and call idiom,
                                  as Flights uses it
  companion/static/style.css      `.home-status-grid` (the three tiles), `.now-showing__image` and
                                  `.preview-frame__image` (the shared white-backing / hairline / radius
                                  rule), `.airline-card__placeholder` (the shipped dashed treatment),
                                  `.cell-inline-sep` (the existing middle dot) and `.time-value`
                                  (added by plan 22-04)
  companion/test_view_pages.py:440      EXPECTED_CHECK_COUNT (re-read; plan 22-02 bumped it)

Measured today, from 22-AUDIT.md's pixel table: Home's status tiles are
112 / 112 / 131 px on a phone; the recent-flight time wraps to two mono lines
("17:25" / "(25m ago)"); the Vueling thumbnail placeholder differs in weight from
the real thumbnails.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home reads one frame state, one pipeline detail and one airline name</name>
  <files>companion/pages/home_page.py, companion/i18n_fr/home.py, companion/test_view_pages.py</files>
  <behavior>
    - For a held fixture, Home's Frame tile renders the same clock time and the same state as `frame_strip_html()` renders for the same inputs, and contains no warn or error token
    - For a late fixture, both report late — the tile and the strip flip together, not at different thresholds
    - The Flight-data tile renders exactly one verdict: Home's own, with Health's verdict-free detail beneath it
    - A row whose stored airline is an alias renders the same display string on Home as it does on Flights
    - Exactly one element on the page carries the name "Frame"
    - The hero's composition, its element order and its picture are unchanged
  </behavior>
  <read_first>
    - companion/pages/home_page.py in full (430 lines) — it is short enough to read once, and every one of this task's four changes is in it
    - companion/pages/health_page.py's published health-state dict as plan 22-03 left it, plus 22-03-SUMMARY.md for the final key name
    - companion/frame_state.py and companion/wake.py as plan 22-02 left them, plus 22-04-SUMMARY.md for how the strip consumes them
    - companion/pages/history_page.py: the `display_airline_name()` import and one call site — READ-ONLY
    - companion/i18n_fr/home.py in full; companion/test_view_pages.py's existing Home checks
  </read_first>
  <action>
    Migrate Home's `wake.next_wake_at_iso(...)` call at :421 to the richer accessor
    plan 22-02 added, and render the Frame tile's state from
    `companion/frame_state.py`'s resolution — the same one the strip reads. Delete
    any locally derived staleness decision for the frame's own lateness. The tile
    and the strip must be unable to disagree because neither computes anything.

    Replace the re-embedded pipeline verdict with Health's verdict-free
    `pipeline_detail_html`, rendered under Home's own verdict — the pattern the
    file's own comments at :114 and :150 already describe for
    `device_detail_html`. Home keeps exactly one verdict per tile.

    Replace the raw `row["airline"]` read at :209 with `display_airline_name()`,
    imported the same way Flights imports it. Do not fork a second resolution
    path; if the import creates a cycle, stop and record it in the SUMMARY rather
    than copying the function.

    Resolve the "Frame" collision: the strip's heading and a tile's caption cannot
    both be "Frame" on one page. Rename the tile's caption (not the strip's
    heading — the strip is the shared component plan 22-04 owns) to something that
    names what the tile reports, and ship its French entry in the same commit.

    Touch nothing else on Home. The hero redesign, the countdown, the day timeline
    and the self-refresh are D1/D4/D13, Phase 23 — a change to the hero's
    composition here is out of scope by the boundary rule.

    Tests: pin every behaviour above, including one check that renders the strip
    and the tile from the same fixture and asserts their clock strings are equal,
    and one that asserts exactly one occurrence of the "Frame" name. Re-derive
    `EXPECTED_CHECK_COUNT` by RUNNING the harness and append a NEW last assignment
    commented `22-07-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'row\["airline"\]' companion/pages/home_page.py` outputs `0`
    - `grep -c "pipeline_detail_html" companion/pages/home_page.py` is non-zero
    - a held fixture renders Home with zero `dot--warn`, `dot--error`, `stat-tile--warn` tokens
    - the strip's and the tile's clock strings are equal for the same fixture
    - `companion/test_view_pages.py` reports M/M at its new pin; `companion/test_i18n.py` exits 0
    - `ruff check .` clean
  </acceptance_criteria>
  <done>Home agrees with the strip above it, with Health beside it and with Flights below it.</done>
</task>

<task type="auto">
  <name>Task 2: One line for the time, one height for the tiles, one weight for the thumbnails</name>
  <files>companion/pages/home_page.py, companion/static/style.css, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/home_page.py: the recent-flights row builder and its time cell, and the thumbnail/placeholder markup
    - companion/static/style.css: `.home-status-grid`, `.cell-inline-sep`, `.time-value` (plan 22-04), `.airline-card__placeholder`, `.now-showing__image` and `.preview-frame__image`
    - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md: the rule reserving monospace for identifiers
    - companion/static/style.css:2081 — the single `@supports selector(:has(*))` block; the file is pinned at exactly one by `test_config_page.py:3760`
  </read_first>
  <action>
    B18: make the recent-flight time one line. The clock takes the `.time-value`
    role plan 22-04 defined and leaves `--font-mono` — monospace is reserved for
    identifiers (callsign, ICAO24 hex, the masked calendar URL), and a clock time
    is not one. The relative age sits on the same line as a sibling in the muted
    label voice, separated by the existing `.cell-inline-sep` middle dot. Apply
    `white-space: nowrap` to the pair so it cannot wrap.

    B2's phone half: give `.home-status-grid` `align-items: stretch` so the three
    tiles stop measuring 112 / 112 / 131 and report one height.

    The Vueling placeholder: reuse the shipped `.airline-card__placeholder` dashed
    treatment so a missing thumbnail matches the real ones in weight, and put the
    real thumbnails on the same white-backing / hairline / radius rule
    `.now-showing__image` and `.preview-frame__image` already share — one rule, an
    additional selector, no new colour literal and no new radius.

    Add no new off-scale literal: the spacing tokens, the wash register and the
    four type sizes are the contract. Take a `T16` debt item only if it is inside a
    rule this task already edits, and record it in the SUMMARY.

    Do not open a second `@supports selector(:has(*))` block.

    Tests: add source-scanning checks for the nowrap pair, the stretched status
    grid and the placeholder's reuse of the shipped class, plus a markup check
    that the time cell contains no `--font-mono` class. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `22-07-PLAN.md Task 2`. Run the full suite and record every harness's old and
    new pin in the SUMMARY.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_config_page.py && PYTHON="$PY" bash scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@supports selector(:has(\*)) {' companion/static/style.css` outputs `1`
    - the recent-flight time cell markup contains no monospace class and carries a nowrap pair
    - `.home-status-grid` declares `align-items: stretch`
    - `companion/test_config_page.py` still reports M/M — the `:has()` gate is untouched
    - `scripts/run-all-tests.sh` shows no new failure and coverage stays at or above 83
    - `ruff check .` clean
  </acceptance_criteria>
  <done>Home's rows read on one line, its tiles line up on a phone, and a missing thumbnail no longer looks like a rendering fault.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored airline string → rendered display name | a resolution function now sits between the database row and the page |
| health state dict → Home's renderer | a verdict-free fragment produced by another module is interpolated here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-24 | Information Disclosure | `display_airline_name()` output rendered on Home | mitigate | the value crosses `escape_html()` at its interpolation site exactly as the raw row value did; the function is the same one Flights already ships, adding no new sink |
| T-22-25 | Spoofing | Home reporting a neutral state for a genuinely late frame | mitigate | Home derives nothing: it renders the shared resolution, whose held-then-late bound is pinned in plans 22-02 and 22-04. The strip/tile equality assertion here makes a divergence a test failure |
| T-22-26 | Tampering | `pipeline_detail_html` crossing a module boundary | accept | it is produced by `health_page.compute_health_state()` from already-escaped values, the same contract `device_detail_html` has shipped under since phase 20 |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_view_pages.py` → M/M at its new pin
- `companion/test_config_page.py` → M/M (the `:has()` gate unmoved)
- `companion/test_i18n.py` → green
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure, coverage ≥ 83
- `ruff check .` clean
- `<human-check>` (folded into the phase-closing sweep, plan 22-16): on a 390 px phone the three Home tiles are the same height; a recent flight's time sits on one line; Home and Flights show the same airline name for the same flight.
</verification>

<success_criteria>
- Home's Frame tile and the strip cannot disagree
- One verdict per tile, with Health's verdict-free detail beneath it
- Airline names come from `display_airline_name()`
- Exactly one element named "Frame" per page
- One-line recent-flight time, out of the monospace family, with the age in the muted voice
- Three status tiles of one height on a phone; the placeholder matches real thumbnails in weight
- The hero's composition untouched (D4 is Phase 23)
- `EXPECTED_CHECK_COUNT` re-derived by running the harness, appended as new last assignments citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-07-SUMMARY.md` when done.
</output>
