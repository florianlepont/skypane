---
phase: 22-companion-audit-round-4-fix-the-blocking-display-save-bar-co
plan: 16
type: execute
wave: 12
depends_on: [22-04, 22-09, 22-10, 22-11, 22-12, 22-13, 22-14, 22-15]
files_modified:
  - .claude/skills/sketch-findings-skypane/SKILL.md
  - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
  - .claude/skills/sketch-findings-skypane/references/control-density.md
  - .claude/skills/sketch-findings-skypane/references/mobile-navigation.md
  - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md
  - .claude/skills/sketch-findings-skypane/references/data-density.md
autonomous: true
requirements: [CFG-31]

must_haves:
  truths:
    - "D-08: every one of 22-UI-SPEC.md §4's twenty rows is applied, so the design system does not contradict the code this phase shipped"
    - "D-08: superseded guidance is marked SUPERSEDED IN PLACE with a stated reason, following the skill's own established convention — never deleted"
    - "The recorded rejection of the icon-only pattern for the row toggle is reversed explicitly, with its reasoning recorded, not quietly rewritten"
    - "The rejected verdicts on the full-screen overlay and the backdrop drawer stay UNCHANGED and UNREVERSED — bottom tabs were chosen so they never had to be touched"
    - "Three stale, load-bearing numbers are corrected: the :has() block count (one, not two), the floating-overlay exception count (four, not two), and the compact-chip usage sentence"
    - "The three UI-checker follow-up notes are folded in: both new .calendar-disconnect-btn call sites are named, the day-separator format is stated as the Paris-day formatter's output, and the row toggle's accessible name is recorded as advertising the picture inside"
    - "The phase's regression floor holds: every harness green at its pin, coverage at or above 83, and the browser harness having run rather than skipped at least once"
  artifacts:
    - path: ".claude/skills/sketch-findings-skypane/references/control-density.md"
      provides: "the register changes this phase forces, including one recorded reversal"
      contains: "SUPERSEDED"
    - path: ".claude/skills/sketch-findings-skypane/references/mobile-navigation.md"
      provides: "the bottom tab bar as a third nav rendering, with the rejected verdicts intact"
      contains: "tab bar"
  key_links:
    - from: "the skill's touch-target register"
      to: "the shipped code"
      via: "one entry per control this phase moved between categories"
      pattern: "row-toggle|tab-bar|login-card"
    - from: "references/settings-page-patterns.md"
      to: "companion/test_config_page.py's pinned count"
      via: "the corrected :has() block number"
      pattern: ":has"
---

<objective>
Update `sketch-findings-skypane` in step with everything waves 1 through 11
shipped, and close the phase. This lands last because it codifies decisions the
earlier waves make — and it is not optional: a plan that ships a change without
its skill edit leaves the design system contradicting the shipped code, which is
exactly how X9's audit row came to propose a pattern the skill had already
rejected.

Purpose: the skill is the authority the next phase reads. Three of its numbers are
already stale and load-bearing — one of them is where an earlier draft of this
phase's own UI spec took the wrong `:has()` count from. Correcting stale guidance
is the same work as recording new guidance.
Output: six updated skill files, every superseded entry marked in place with its
reason, and a phase-closing verification sweep.
</objective>

<decisions_covered>
D-08's "the design system must be updated in step" requirement, 22-UI-SPEC.md §4's
twenty rows in full, and the three follow-up notes the UI checker left for the
planner. No code file is edited by this plan — the `style.css` header comment's
accent-reservation edit that C2 requires already shipped with plan 22-04, because
that comment is the authoritative list and had to move with the rule it describes.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-CONTEXT.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-AUDIT.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-UI-SPEC.md
@.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-VALIDATION.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.claude/skills/sketch-findings-skypane/references/control-density.md
@.claude/skills/sketch-findings-skypane/references/mobile-navigation.md
@.claude/skills/sketch-findings-skypane/references/settings-page-patterns.md
@.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
@.claude/skills/sketch-findings-skypane/references/data-density.md

<files_owned>
This plan edits ONLY the six skill files, and it is the only plan in its wave.
No file under `companion/`, `server/` or `scripts/` is this plan's to change. If
the verification sweep in Task 3 finds a code defect, record it in the SUMMARY as
a finding for a gap-closure plan — do not fix it here.
</files_owned>

<interfaces>
The twenty §4 rows, grouped by the file each lands in. Read 22-UI-SPEC.md §4 for
each row's full framing; the list below is the routing, not the content.

  SKILL.md
    C1        Typography: the second non-serif exception, plus the compact empty_state variant
    C2        the pointer to style.css's own authoritative accent list (already edited by plan 22-04)
    C6        mark the Phase 21 `d9e0338` polish decision SUPERSEDED, with its reason
    X9        Navigation: a third nav rendering
    X9        Cards: the floating-overlay exception count is FOUR, not two
    T9        Cards: hover transparency now applies to three edges; the reveal is scoped off the strip
    X2/X8/T13 Navigation: `.dot--off` gains three consumers
    B4/C5     the Phase 18 Paris-local-time rule restated as exhaustive

  references/visual-direction-typography.md
    C1        the label-voice legend exception; `legend` STAYS in the shared serif selector
    C5        one new "time value" role replacing four treatments; monospace reserved for
              identifiers; no new token, family or size

  references/control-density.md
    C4        the new composition rule, stated narrowly: controls that read as ONE object share
              height and radius; controls that read as SEPARATE objects on a shared row are
              centre-aligned and keep their own registered geometry. Two worked examples: the login
              card's field plus Sign in, and the rules add-form's four controls
    X9        the tab bar joins the MET-DIRECTLY category (78x56 px), NOT the traded-away list
    X3        the login submit button is a named 44 px exception joining the KEPT category, with its
              three-point justification
    X7        `.calendar-disconnect-btn` gains its second consumer — name BOTH new call sites
    T6        constant 1 px border; the 2 px signal moves to an inset ring; the hover restore moved too
    X5        the recorded rejection of the icon-only pattern for the row toggle is REVERSED
    T9        the card hairline-at-rest contract's hover amendment
    X6        the compact-chip usage sentence is no longer exhaustive

  references/mobile-navigation.md
    X9        the bottom tab bar as a THIRD rendering, all three still fed by the one link iteration;
              the "What to Avoid" rejected verdicts stay UNCHANGED AND UNREVERSED; one clarifying
              note that the More `<details>` panel's upward absolute positioning is NOT a reversal of
              that verdict; the dropdown's reduction, its renamed toggle label and its single max-height
    X2/X8/T13 `.dot--off`'s three consumers

  references/settings-page-patterns.md
    X3        `.field-error` gains a second consumer, with its placement rule stated
    T7        TWO entries superseded in place: the dedicated "No z-index, and why" paragraph and its
              matching "What to Avoid" entry, using the paragraph's own escape clause as written
    Gate      line 47's ":has() pinned to exactly two such blocks" is WRONG — it is ONE, at
              style.css:2081, asserted by test_config_page.py:3760

  references/data-density.md
    T4        mark the sticky-header entry SUPERSEDED, and record that its never-live-validated
              canvas-colour background note is moot once sticky is removed

The three UI-checker follow-up notes, to fold in where they belong:
  1. X7's control-density row must name BOTH new `.calendar-disconnect-btn` call sites — Airlines'
     per-card Replace AND the Flights picture control — not one.
  2. The day-separator format needs a half-sentence stating that its shape is the output of the
     Paris-day formatter and is not a licence to call `strftime` directly.
  3. The row toggle's accessible name must be recorded as advertising that a picture is reachable
     inside the detail row — an icon-only control whose name understates its contents is the same
     defect class it was meant to fix.

Every plan SUMMARY from 22-01 through 22-15 carries the measurements and
consequences this plan records. Read them all before writing: the accent
arithmetic (22-04), the register consequence for `.row-toggle` (22-09), the crop
re-measurement and the touch-target confirmations (22-10), the second
`.calendar-disconnect-btn` call site (22-11), the `.dot--off` consumers and the
compact `empty_state()` variant (22-12), the login button's justification and its
contrast measurements (22-13), the tab bar's measurements and the save-bar
stacking reason (22-14), and the T4 removal (22-15).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md, the type ladder and the control-density register</name>
  <files>.claude/skills/sketch-findings-skypane/SKILL.md, .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md, .claude/skills/sketch-findings-skypane/references/control-density.md</files>
  <read_first>
    - .claude/skills/sketch-findings-skypane/SKILL.md in full (126 lines) — every paragraph, especially the Phase 18 and Phase 21 entries, the Cards paragraph's floating-overlay enumeration, the Navigation paragraph, and the pointer to style.css's own accent list
    - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md in full (79 lines)
    - .claude/skills/sketch-findings-skypane/references/control-density.md in full (150 lines) — the button-geometry register, the wash register, the selected-card contract, the touch-target register's three categories, the `.calendar-disconnect-btn` entry and its own prediction, the compact-chip entry, and the recorded rejection of the icon-only pattern for the row toggle
    - the SUMMARY files of plans 22-04, 22-09, 22-10, 22-11, 22-12, 22-13 and 22-15
  </read_first>
  <action>
    Apply the eight SKILL.md rows, the two typography rows and the eight
    control-density rows routed above, following the skill's own established
    convention: mark superseded guidance SUPERSEDED IN PLACE with a stated reason,
    never delete it.

    Three edits need particular care.

    First, the row-toggle reversal. `references/control-density.md` currently
    REJECTS the icon-only pattern for that control on the stated ground that,
    unlike the copy button, the row toggle carries visible text. Plan 22-09 removed
    that text, which dissolves the rejection's own ground — so the reversal is
    defensible, but only if the reasoning is RECORDED rather than the entry quietly
    rewritten. Mark it SUPERSEDED in place with that reasoning, move `.row-toggle`
    from the traded-away category to the relocated category, and note that it had
    no CSS rule at all before this phase, so a new rule block was created rather
    than an existing one edited. Fold in the checker's note: its accessible name
    advertises the picture reachable inside.

    Second, the `.calendar-disconnect-btn` entry. It gains a second consumer, and
    the row must name BOTH new call sites — Airlines' per-card Replace control and
    the Flights picture control — not one. This confirms the file's own prediction
    that it is "the pattern to reuse the next time this app needs a small,
    deliberately de-emphasized secondary action", and confirms it is still not a
    `.btn` family.

    Third, the Cards paragraph's floating-overlay enumeration. It says TWO
    exceptions and names the lightbox and the save bar; it was already stale before
    this phase, because the mobile dropdown carries an unconditional resting shadow
    too. With the tab bar it is FOUR. Correct the count and the list in one edit
    rather than adding a fourth to a wrong total.

    Take every measurement from the plan SUMMARYs rather than restating a target
    from the UI spec — the spec stated what should be true, the SUMMARYs record
    what is. Where a SUMMARY reports a value that differs from the spec's target,
    record the SUMMARY's value and note the difference.
  </action>
  <verify>
    <automated>cd /home/user/skypane && grep -c "SUPERSEDED" .claude/skills/sketch-findings-skypane/references/control-density.md && grep -c "row-toggle" .claude/skills/sketch-findings-skypane/references/control-density.md && grep -c "time value" .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md</automated>
  </verify>
  <acceptance_criteria>
    - `references/control-density.md` carries the row-toggle reversal marked SUPERSEDED in place, with its reasoning, and the control appears in the relocated category
    - the `.calendar-disconnect-btn` entry names two new call sites
    - `SKILL.md`'s Cards paragraph enumerates four floating-overlay exceptions
    - `SKILL.md`'s Phase 21 entry carries the C6 supersession with its reason; its Phase 18 entry restates the Paris-local rule as exhaustive
    - `references/visual-direction-typography.md` carries the one time-value role and the legend exception, and still states that `legend` stays in the shared serif selector
    - no entry was deleted rather than superseded (verify by diff: every removal is a replacement carrying a SUPERSEDED marker)
  </acceptance_criteria>
  <done>The type ladder and the density register describe the app that now exists, with every reversal argued rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation, the settings patterns and the three stale numbers</name>
  <files>.claude/skills/sketch-findings-skypane/references/mobile-navigation.md, .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md, .claude/skills/sketch-findings-skypane/references/data-density.md</files>
  <read_first>
    - .claude/skills/sketch-findings-skypane/references/mobile-navigation.md in full (112 lines) — the in-flow `flex-basis: 100%` mechanism, the structural guarantee that all nav renderings share one iteration, and every entry in "What to Avoid" including the two rejected verdicts and their 06.6.1-06 real-device history
    - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md in full (148 lines) — line 47's `:has()` count claim, the "No z-index, and why" paragraph and its escape clause, the matching "What to Avoid" entry, the one-caption-per-section rule, and the save bar's four-iteration history
    - .claude/skills/sketch-findings-skypane/references/data-density.md in full (143 lines) — the sticky-header entry and its canvas-colour note
    - the SUMMARY files of plans 22-09, 22-13, 22-14 and 22-15, for the measured dropdown max-height, the remaining push, the save-bar padding and the T4 removal
  </read_first>
  <action>
    Record the bottom tab bar in `references/mobile-navigation.md` as a THIRD nav
    rendering alongside the sidebar and the dropdown, all three still fed by the one
    link iteration. Leave the two rejected verdicts UNCHANGED AND UNREVERSED, and
    say why they did not need touching: bottom tabs were chosen specifically to
    avoid reopening a verdict established by real-device testing. Add one clarifying
    note that the More panel's upward absolute positioning is not a reversal of that
    verdict — the verdict is about the PRIMARY nav's push-versus-overlay behaviour
    on a component that must be able to push content, and the More panel is a
    two-item secondary sheet anchored to an already-fixed bar. Record the dropdown's
    reduction to the state reminder plus the footer, its renamed toggle label, and
    its single measured max-height. Add `.dot--off`'s three consumers and the one
    rule they share.

    In `references/settings-page-patterns.md`, record `.field-error`'s second
    consumer with its placement rule, and supersede IN PLACE the two entries the
    tab bar contradicts — the "No z-index, and why" paragraph and its matching
    "What to Avoid" entry. Use the paragraph's own escape clause exactly as it is
    written: record the stated reason (the save bar is the active task, the tab bar
    is ambient chrome, and the blocked save was this phase's P0) and the new
    sub-960 px bottom offset, rather than deleting the paragraph.

    Correct line 47's `:has()` count. It says the file is pinned to exactly two
    blocks; it is ONE, at `style.css:2081`, asserted by `test_config_page.py:3760`,
    because 21-07 retired the Calendar-card fusion rule and deleted its block. This
    correction is required whether or not any plan added a `:has()` rule — the stale
    line is what an earlier draft of this phase's own UI spec copied instead of
    checking the code, and leaving it would set the same trap again. Verify the
    number against the code before writing it, and record in the SUMMARY that you
    did.

    In `references/data-density.md`, mark the sticky-header entry SUPERSEDED with
    its reason (the wrap has no height, so sticky can never engage; sticky day
    headers are Phase 23's D7) and record that its never-live-validated
    canvas-colour background note is moot once sticky is removed. Add the
    day-separator half-sentence from the checker's second note: the label's shape
    is the Paris-day formatter's output, not a licence to call `strftime` directly.
  </action>
  <verify>
    <automated>cd /home/user/skypane && grep -c "still rejected" .claude/skills/sketch-findings-skypane/references/mobile-navigation.md && grep -c "SUPERSEDED" .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md && grep -c "SUPERSEDED" .claude/skills/sketch-findings-skypane/references/data-density.md && grep -c '@supports selector(:has(\*)) {' companion/static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - the two rejected nav verdicts are present and unchanged; the clarifying note about the More panel is added
    - `references/settings-page-patterns.md` states the `:has()` count as one, at `style.css:2081`, and the live file still has exactly one such block
    - both contradicted save-bar entries are superseded in place, with the stated reason and the new offset
    - `references/data-density.md`'s sticky entry is superseded, and the day-separator sentence is present
    - the measured dropdown max-height, remaining push and save-bar padding recorded here match the SUMMARY values
  </acceptance_criteria>
  <done>The navigation and settings guidance describe the shipped behaviour, and the number that misled this phase's own spec is corrected at its source.</done>
</task>

<task type="auto">
  <name>Task 3: The phase-closing regression sweep</name>
  <files>.claude/skills/sketch-findings-skypane/SKILL.md</files>
  <read_first>
    - every SUMMARY from 22-01 to 22-15, for each harness's final `EXPECTED_CHECK_COUNT` and for every finding deferred to another plan
    - .planning/phases/22-.../22-VALIDATION.md — the sign-off list and the manual-only table
    - .planning/phases/22-.../22-AUDIT.md — the full finding list, used as the checklist for the coverage confirmation below
  </read_first>
  <action>
    Run the full suite and confirm the phase's regression floor: every harness green
    at its final pin, only the documented pre-existing root-sandbox failures, and
    coverage at or above 83. Confirm the browser harness RAN rather than skipped at
    least once, and record how it was confirmed.

    Then confirm coverage of the ledger. Walk `22-AUDIT.md`'s four tables — B1-B18,
    X1-X9, C1-C6, T1-T16 — and record, one line each, which plan closed it and how
    it is verified. Anything not closed, and anything a SUMMARY deferred to another
    plan, is recorded as a finding for gap closure rather than quietly dropped. Do
    NOT fix a code defect here: this plan owns only the skill files.

    Confirm the no-JS floor and the dependency floor explicitly, because both are
    phase-wide invariants rather than any one plan's: every page usable and every
    setting saveable with scripts blocked; nothing added to
    `server/requirements.txt`; no vendored library; the CSP still `script-src
    'self'` with no unsafe-inline and no nonce.

    Add one dated entry to `SKILL.md`'s phase log recording what this phase changed
    in the design system, in the same shape as its existing entries — the accent
    arithmetic, the one time-value role, the compact empty state, the third nav
    rendering, the three `.dot--off` consumers, and the two reversals (the row
    toggle's icon-only pattern and the save bar's z-index).

    Write the human-verification list for phase-level UAT into the SUMMARY, pulled
    from every plan's own deferred `<human-check>` plus `22-VALIDATION.md`'s
    manual-only table: the pixel-measurement table re-walked at 1280 px and 390 px
    in both themes and both languages against the audit's seeded state directory;
    the segmented control's hover legibility after a click reload; real-device
    rendering of the bottom tab bar; the "within about 5 minutes" claim against the
    firmware; and one real overnight quiet-hours window.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); PYTHON="$PY" bash scripts/run-all-tests.sh && "$PY" companion/test_browser_ux.py && grep -c "playwright" server/requirements.txt; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run-all-tests.sh` passes with only the documented pre-existing failures, and coverage at or above 83
    - `companion/test_browser_ux.py` ran rather than skipped, and the evidence is recorded
    - `grep -c playwright server/requirements.txt` outputs `0`
    - `grep -v "^ *#" companion/app.py | grep -cE "script-src[^;]*(unsafe-inline|nonce-)"` outputs `0`, and `grep -c "script-src 'self';" companion/app.py` outputs `1` — scoped to `script-src` AND comment-filtered, both deliberately: a bare `unsafe-inline` count returns `3` on the live tree (two CSP comment lines at `:113` and `:119`, plus the real header at `:137`, whose `style-src 'self' 'unsafe-inline'` is the documented exception for the seven inline style attributes), and the unfiltered scoped form still returns `1`, because the comment at `:113` states "script-src 'self' — deliberately no 'unsafe-inline' and no nonce" on one line and so matches the very pattern that is meant to catch its opposite
    - every finding in `22-AUDIT.md`'s four tables is recorded in the SUMMARY with its closing plan and its verification, or listed as a gap-closure finding
    - `SKILL.md` carries one dated phase entry summarising the design-system changes
  </acceptance_criteria>
  <done>The design system and the code agree, and the phase's own ledger has a line-by-line record of where each of its ~49 findings was closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-system documentation → the next phase's implementation | the skill is read as authority; a stale entry becomes a shipped defect one phase later |
| plan SUMMARYs → recorded measurements | the numbers recorded here are what future work will trust without re-measuring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-60 | Repudiation | a reversed verdict recorded as if it had never existed | mitigate | every reversal is marked SUPERSEDED in place with its reasoning, and an acceptance criterion checks by diff that no entry was deleted rather than superseded — this is exactly how X9's audit row came to propose an already-rejected pattern |
| T-22-61 | Tampering | a stale number propagating into a future spec | mitigate | the `:has()` count is verified against the live file before being written, and the acceptance criterion re-greps the stylesheet rather than trusting the document |
| T-22-62 | Denial of Service | a code defect discovered during the sweep being fixed here | mitigate | this plan's file ownership excludes every code file; a finding is recorded for gap closure instead, so a last-wave edit cannot bypass the wave's own verification |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → only the documented pre-existing failures, coverage ≥ 83
- `companion/test_browser_ux.py` → ran, not skipped, with evidence recorded
- `grep -c playwright server/requirements.txt` → 0; no vendored library; CSP unchanged
- Every `22-AUDIT.md` finding recorded with its closing plan, or listed for gap closure
- `<human-check>` (phase-level UAT, listed in the SUMMARY): the pixel-measurement table re-walked at 1280 px and 390 px, light and dark, FR and EN, against the audit's seeded state directory; the segmented control's hover after a click reload; the bottom tab bar on a real iPhone and Android handset; the "within about 5 minutes" claim against the firmware; one real overnight quiet-hours window.
</verification>

<success_criteria>
- All twenty §4 rows applied across the six skill files
- Every supersession marked in place with a stated reason; nothing deleted
- The row-toggle rejection reversed explicitly; the nav rejected verdicts untouched
- The `:has()` count, the floating-overlay count and the compact-chip sentence corrected
- The three UI-checker notes folded in
- The full suite green at every pin, coverage at or above 83, the browser harness having run
- A line-by-line coverage record for every finding in the audit's four tables
- One dated phase entry in `SKILL.md`
- One commit per task, `docs(22): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/22-companion-audit-round-4-fix-the-blocking-display-save-bar-co/22-16-SUMMARY.md` when done.
</output>
