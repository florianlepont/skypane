---
phase: 04-ci-cd-documentation-legal-compliance-github-actions-ci-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: false
requirements: [D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "A repo-root .gitignore exists and `git status --porcelain` reports no ignorable cruft (.DS_Store, caches, venvs, build dirs)."
    - "Neither the real VPS IPv4 nor the real VPS DNS hostname appears in the diff of any commit reachable from any ref."
    - "The developer has explicitly decided, on the record, which additional sensitive literals (home Wi-Fi SSID/BSSID, home street address) are in or out of the scrub."
    - "Every literal the developer approved for scrubbing is absent from all commit diffs and from the working tree."
    - "Live docs read naturally after the rewrite — sensitive values are replaced by placeholder tokens matching the project's existing `<vps-ip>` / `<public-host>` convention, not by mangled sentinel strings."
    - "The pre-rewrite history is recoverable from a verified backup taken before filter-repo ran."
  artifacts:
    - .gitignore
    - .git/inkframe-scrub-expressions.txt
    - .git/inkframe-scrub-literals.txt
    - .planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/SCRUB-RECORD.md
  key_links:
    - "scrub-expressions.txt (filter-repo `literal==>replacement` format) -> scrub-literals.txt (bare literals, one per line) — the literals file is the ONLY input to every verification grep, so no plan or doc ever needs to restate a sensitive value."
    - ".gitignore -> the pre-existing per-subdir .gitignore files (server/, deploy/, firmware/, stub-server/, adsb-test/, server/state/) — the root file is additive and must not duplicate or supersede them."
    - "git worktree registration under .claude/worktrees/ -> object reachability: a live worktree pinned to a pre-rewrite commit keeps the old (unscrubbed) objects reachable, so `git gc --prune` silently leaves them in the local object store."
---

<objective>
Make this repository safe to publish, before it ever touches a remote. Two things must be true when this plan ends: (1) a repo-root `.gitignore` enforces the secrets discipline the project has so far maintained by hand (D-06, D-03), and (2) the real VPS IPv4 and DNS hostname — plus any additional sensitive literal the developer approves — exist in **no commit reachable from any ref** (D-05, D-04).

Purpose: D-05 is the one irreversible-if-botched operation in Phase 4. Every later plan in this phase commits onto whatever history this plan produces, and plan 04-06 pushes that history publicly. Getting this wrong is unrecoverable once pushed.

Output: a root `.gitignore`, a rewritten local history, a scrub record documenting exactly what was replaced and why, and two machine-readable scrub input files under `.git/` (never committed) that every downstream verification grep reads from.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-CONTEXT.md
@.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-RESEARCH.md

# Existing .gitignore convention to extend (grouped entries under short "why" comments)
@deploy/.gitignore
@server/.gitignore
@firmware/.gitignore
</context>

<planner_findings>
Findings established live during planning. Treat these as verified facts, not guesses.

- **F1 — Scale of the leak.** The two D-04 literals appear in **8 tracked files at HEAD** (`.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/phases/02-plane-view-end-to-end-slice/02-05-SUMMARY.md`, `.planning/phases/02-plane-view-end-to-end-slice/02-VERIFICATION.md`, this phase's `04-CONTEXT.md`, `04-DISCUSSION-LOG.md`, `04-RESEARCH.md`, and `deploy/README.md`) across **~13 commits** in a **150-commit** history.
- **F2 — Two more sensitive-literal classes exist that D-05 does not name.** Both were found by grep during planning, both are already committed, and both are arguably more sensitive than a VPS IP:
  - The **home Wi-Fi network name in plaintext**, its **BSSID (router MAC)**, and the **device's own MAC address** all appear in `hardware/logs/first-light.log` and `hardware/logs/backoff-run.log` — both tracked. STATE.md already records the SSID leak as a known, unfixed 01-07 finding, but never connects it to "this repo is about to become public." Of the three, the BSSID is the sensitive one: it resolves to a street-level location through public Wi-Fi-geolocation databases. The device MAC is a hardware identifier of much lower consequence.
  - The **home street address** appears in 4 tracked files (`.planning/PROJECT.md`, `.planning/ROADMAP.md`, `01-CONTEXT.md`, `01-RESEARCH.md`).
  D-05's literal wording covers only the VPS IP and hostname; its stated intent is that sensitive information not be visible once public. Resolving that gap is a developer decision, not a planner one — hence Task 2.
- **F3 — `git-filter-repo` is not installed** on this machine (`command -v git-filter-repo` → not found). `gh` **is** installed and authenticated (account `florianlepont`, scopes include `repo`).
- **F4 — A live git worktree exists** at `.claude/worktrees/faience-blanche-mate-434eeb`, detached at a pre-rewrite commit. It will pin the old, unscrubbed objects as reachable and defeat `git gc --prune`. It must be removed and pruned before the final gc.
- **F5 — The working tree has uncommitted modifications** (`.planning/config.json`, `hardware/logs/backoff-powercycle.log`) and untracked cruft (`.DS_Store` at three paths, `.planning/research/.cache/`). `git reset --hard` in Task 3 discards uncommitted tracked changes, so Task 1 must resolve them first.
- **F6 — `deploy/inkframe.env.example` is a clean placeholder template** (`INK_BYOS_SECRET=replace-with-a-long-random-secret`, an RFC-5737 example host). It contains no real secret and must not be scrubbed. Likewise `adsb-test/runway3.json`'s coordinates are the **airport's**, public infrastructure, not the home address — leave them alone.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: Root .gitignore, worktree removal, and a clean pre-rewrite working tree</name>
  <files>.gitignore</files>
  <read_first>
    - `deploy/.gitignore` and `server/.gitignore` — the exact comment-and-grouping convention to follow: short `#` headers that explain *why* an entry exists, not just what it matches.
    - `firmware/.gitignore` — note its explicit rationale for `main/secrets.h` ("the ignore rule is added the moment this directory is created ... so there is no window in which a credential could land in git history"). The root file extends that same reasoning repo-wide.
    - 04-RESEARCH.md "Pitfall 5" — why the belt-and-suspenders secret entries are needed even though history is clean.
  </read_first>
  <action>
    Create a repo-root `.gitignore` implementing D-06, following the grouped-entries-under-a-why-comment convention the five existing per-subdirectory ignore files already use.

    Cover, at minimum, grouped by rationale:
    - **OS cruft the subdir files do not reach:** `.DS_Store` (matched at any depth).
    - **Python artifacts at repo-root scope:** `__pycache__/` at any depth, `*.pyc`, and any `.venv/` at any depth.
    - **Tooling caches this phase introduces:** the Ruff cache directory and coverage's data files, including its parallel-mode per-process data files. Plan 04-02 adds the tools that generate these; the ignore entries must land here, before those tools ever run in the working tree.
    - **ESP-IDF build output at root scope:** `firmware/build*/`.
    - **Agent/worktree scratch:** the nested-worktree directory under `.claude/` (see F4) — a nested checkout must never be stageable from the root.
    - **Credential belt-and-suspenders (D-03/D-06):** `deploy/inkframe.env` and `firmware/main/secrets.h`, each with a comment stating that per-subdir rules already cover them and that this duplication is deliberate defence-in-depth.
    - **Local planning scratch:** `.planning/research/.cache/`.

    Do NOT ignore `deploy/inkframe.env.example` or `firmware/main/secrets.example.h` — those templates are tracked on purpose (F6). Do NOT delete or weaken any existing per-subdirectory ignore file; the root file is strictly additive.

    Then get the tree into a state `git reset --hard` can safely be run against in Task 3 (F5):
    - Remove the nested worktree (`git worktree remove --force` on the path under `.claude/worktrees/`, then `git worktree prune`) and confirm `git worktree list` reports only the main working tree.
    - Resolve the two uncommitted tracked modifications — commit them if they are intended changes, restore them if they are incidental. Nothing tracked may remain dirty.
    - Delete the untracked `.DS_Store` files so the new ignore rules are verified against a genuinely clean tree rather than merely masking existing junk.

    Commit `.gitignore` (plus any intended content commits) before Task 2.
  </action>
  <verify>
    <automated>test -f .gitignore &amp;&amp; git status --porcelain | grep -v '^$' | wc -l | grep -qx '0' &amp;&amp; [ "$(git worktree list | wc -l | tr -d ' ')" = "1" ] &amp;&amp; git check-ignore -q deploy/inkframe.env firmware/main/secrets.h .DS_Store &amp;&amp; ! git check-ignore -q deploy/inkframe.env.example &amp;&amp; echo GITIGNORE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` produces zero lines (no modified tracked files, no untracked non-ignored files).
    - `git check-ignore` confirms `deploy/inkframe.env`, `firmware/main/secrets.h`, and `.DS_Store` are ignored.
    - `git check-ignore` confirms `deploy/inkframe.env.example` is NOT ignored.
    - `git worktree list` reports exactly one entry (the main working tree).
    - `git ls-files server/.gitignore deploy/.gitignore firmware/.gitignore stub-server/.gitignore adsb-test/.gitignore server/state/.gitignore` still lists all six pre-existing files — none were removed.
  </acceptance_criteria>
  <done>A committed root `.gitignore` enforces the credential and cruft rules repo-wide, the nested worktree is gone, and the working tree is clean enough for a history rewrite.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the scrub scope before the rewrite runs</name>
  <files>.git/inkframe-scrub-expressions.txt</files>
  <action>Present the three options below to the developer and record the selection. The chosen categories become the lines of `.git/inkframe-scrub-expressions.txt` that Task 3 executes against. Do not proceed to Task 3 without an explicit selection — the rewrite is a single irreversible pass, and adding a literal afterwards means redoing the whole operation.</action>
  <decision>Which sensitive literals does the pre-publish history scrub replace?</decision>
  <context>
D-05 names two literals: the VPS public IPv4 and the VPS DNS hostname. During planning, grep found two further classes of already-committed sensitive data that D-05's wording does not cover but its stated intent ("j'aimerais que ces informations sensibles ne soient pas visibles") plausibly does — see finding F2 above.

This matters now because the rewrite is a single irreversible pass. Adding a literal later means doing the whole operation again, and if the repo has been pushed by then, the old objects are already public and permanently archived by third parties.

Category A (D-05, locked, not in question): the VPS public IPv4 and the VPS DNS hostname. 8 files at HEAD, ~13 commits.

Category B (found during planning, NOT named by D-05): the home Wi-Fi network name in plaintext, the router BSSID, and the device's own MAC address, in two tracked firmware capture logs under `hardware/logs/`. The BSSID is the one that matters — it is resolvable to a street-level location via public Wi-Fi-geolocation databases. The device MAC is a low-consequence hardware identifier, included here only so the decision covers everything those two files expose.

A related check came back clean and is worth recording so nobody re-raises it: `stub-server/byos_state.json` holds issued device bearer tokens, but it is correctly covered by `stub-server/.gitignore` and has never appeared in a single commit. D-03's clean-history claim survives scrutiny beyond the two paths it originally checked.

Category C (found during planning, NOT named by D-05): the home street address, in 4 tracked planning documents.

Note that the airport geofence coordinates in `adsb-test/runway3.json` are deliberately excluded from all options — they describe Orly's runway 3, public infrastructure, and are load-bearing for the shipped detection code.
  </context>
  <options>
    <option id="a-only">
      <name>Scrub Category A only — D-05 exactly as written</name>
      <pros>Strictly honours the locked decision's literal scope; smallest, most reviewable rewrite; no interpretation of intent.</pros>
      <cons>Publishes the home Wi-Fi name, the router BSSID, and the home street address. Together these locate the developer's residence far more precisely than the VPS IP ever could.</cons>
    </option>
    <option id="a-b-c">
      <name>Scrub Categories A + B + C — D-05's intent applied to everything grep found</name>
      <pros>Nothing that identifies the developer's home network or address survives into public history. Same single pass, same tooling, marginal extra cost: three more lines in the replacement file.</pros>
      <cons>Rewrites more commits than A alone. Some prose in `.planning/` and the two hardware logs will read with placeholder tokens where a real value used to be.</cons>
    </option>
    <option id="a-b">
      <name>Scrub Categories A + B, leave the street address</name>
      <pros>Removes the machine-readable, geolocatable identifiers (BSSID, Wi-Fi name) while leaving planning prose untouched.</pros>
      <cons>The street address is the most directly sensitive of the three and appears in the very first file a visitor to a public repo is likely to open.</cons>
    </option>
  </options>
  <resume-signal>Select: a-only, a-b-c, or a-b. If you add or remove a specific literal beyond these groupings, state it explicitly — it goes into the replacement file verbatim and is recorded in SCRUB-RECORD.md.</resume-signal>
  <verify>
    <automated>test -s .git/inkframe-scrub-expressions.txt &amp;&amp; grep -c '==>' .git/inkframe-scrub-expressions.txt | grep -qE '^[1-9]' &amp;&amp; test -z "$(git ls-files .git/inkframe-scrub-expressions.txt)" &amp;&amp; echo SCRUB_SCOPE_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - The developer has selected one of the three options, or stated an explicit variant.
    - `.git/inkframe-scrub-expressions.txt` exists, is non-empty, and every line uses filter-repo's replacement separator.
    - The file is untracked — `git ls-files` returns nothing for it.
    - The number of lines matches the selected categories, and that mapping is recorded for SCRUB-RECORD.md.
  </acceptance_criteria>
  <done>The scrub scope is decided on the record and encoded in the replacement file Task 3 will execute against.</done>
</task>

<task type="auto">
  <name>Task 3: Execute and verify the history rewrite</name>
  <files>.git/inkframe-scrub-expressions.txt, .git/inkframe-scrub-literals.txt, .planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/SCRUB-RECORD.md</files>
  <read_first>
    - 04-RESEARCH.md "Pattern 3: git filter-repo history scrub" — the canonical command shape and the fresh-clone safety rail.
    - 04-RESEARCH.md "Pitfall 2" — why filter-repo refuses to run on a non-fresh clone, and why `--force` must never be the first thing tried.
    - `deploy/README.md` around the provisioning section — the project's existing placeholder convention (`<vps-ip>`, `<public-host>`). The replacement tokens chosen below must match this convention so post-rewrite prose still reads correctly.
  </read_first>
  <action>
    Install the tool and build the two input files, then rewrite, verify, and re-home the history. Do not deviate from this order — each step is a precondition for the next.

    **Step 1 — install.** Install `git-filter-repo` (Homebrew or pip; it is not present, per F3). Confirm it runs.

    **Step 2 — build the replacement file.** Write `.git/inkframe-scrub-expressions.txt` in filter-repo's `literal==>replacement` format, one line per approved literal from the Task 2 decision. Place it under `.git/` deliberately: that directory is never tracked, so the sensitive values are never stageable, and no plan or summary in `.planning/` ever needs to restate them.

    Choose replacement tokens that match the project's own existing placeholder style rather than sentinel words — the VPS IPv4 becomes the same angle-bracket ip placeholder `deploy/README.md` already uses, and the DNS hostname becomes the same angle-bracket host placeholder. Do the equivalent for any Category B/C literals approved in Task 2 (a redacted-network placeholder, a redacted-address placeholder). This choice means post-rewrite prose reads naturally and needs no follow-up cleanup commit, which is exactly what D-05 asks for when it says live docs should reference the VPS by a placeholder.

    **Step 3 — derive the verification file.** Write `.git/inkframe-scrub-literals.txt` containing only the left-hand side of each line in the expressions file (cut at the `==>` separator), one bare literal per line. Every verification in this plan and in plan 04-04 greps with `-F -f` against this file, so correctness of the rewrite is checked without any sensitive value ever appearing in a plan, a command line, or a summary.

    **Step 4 — back up.** Produce a verified, restorable backup of the current history before touching it: a `git bundle create` of all refs, stored outside the repository tree. Confirm the bundle verifies (`git bundle verify`) and record its path. This is the only route back if the rewrite goes wrong.

    **Step 5 — rewrite in a scratch clone.** Clone the repo to a scratch directory and run `git filter-repo --replace-text` with the expressions file there. Working on a clone is filter-repo's documented safety model; do not reach for `--force` against the working repo instead.

    **Step 6 — verify in the clone.** In the scratch clone, confirm zero occurrences across every commit diff of every ref, and zero occurrences in the working tree, using only the derived literals file. If any count is non-zero, stop and diagnose — a non-zero count here means a literal appears in a form the replacement did not match (different case, split across a line, URL-encoded), and the expressions file needs another entry.

    **Step 7 — re-home the rewritten history.** Bring the rewritten branch back into the real working repository rather than relocating the project: fetch the scrubbed branch from the scratch clone into a temporary ref, hard-reset `main` onto it, then drop the temporary ref. Untracked and ignored working-tree content (the virtualenv, agent state) is preserved by this route; the Task 1 clean-tree precondition is what makes it safe.

    **Step 8 — purge the old objects locally.** Expire all reflogs and garbage-collect with pruning so the pre-rewrite objects leave the local object store. This only works because Task 1 removed the worktree that pinned them (F4).

    **Step 9 — re-verify in the real repo** with the same grep used in Step 6, then delete the scratch clone.

    **Step 10 — record it.** Write `SCRUB-RECORD.md` in this phase's directory documenting: the Task 2 decision and which categories it covered, the count of literals scrubbed (a count, never the values), the replacement-token style used, the backup bundle's path, the commit range affected, and the verification commands with their zero results. Anyone auditing this repo later needs to see that the scrub happened and was checked — without the record re-leaking what was scrubbed.
  </action>
  <verify>
    <automated>L=.git/inkframe-scrub-literals.txt; test -s "$L" &amp;&amp; test "$(git log --all -p | grep -c -F -f "$L")" = "0" &amp;&amp; test "$(git grep -c -F -f "$L" HEAD -- . | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git worktree list | wc -l | tr -d ' ')" = "1" &amp;&amp; test -f .planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/SCRUB-RECORD.md &amp;&amp; echo SCRUB_VERIFIED</automated>
  </verify>
  <acceptance_criteria>
    - `.git/inkframe-scrub-expressions.txt` and `.git/inkframe-scrub-literals.txt` both exist, are non-empty, and are untracked (`git ls-files` returns nothing for either).
    - `git log --all -p | grep -c -F -f .git/inkframe-scrub-literals.txt` prints exactly `0`.
    - `git grep -F -f .git/inkframe-scrub-literals.txt HEAD -- .` returns no matches.
    - A `git bundle verify` on the pre-rewrite backup bundle succeeds, and its path is recorded in SCRUB-RECORD.md.
    - `deploy/README.md` still reads as coherent English after the rewrite, using the same angle-bracket placeholder style it already used elsewhere — not a sentinel word like an all-caps REDACTED token.
    - `git rev-list --count HEAD` is within one or two commits of the pre-rewrite count (the rewrite replaces text; it must not drop commits). The exact pre- and post- counts are recorded in SCRUB-RECORD.md.
    - `SCRUB-RECORD.md` contains no scrubbed literal — verified by the same `git grep -F -f` check above passing.
  </acceptance_criteria>
  <done>Every approved sensitive literal is absent from all commit diffs and the working tree, a verified backup of the pre-rewrite history exists outside the repo, and SCRUB-RECORD.md documents the operation without restating what it removed.</done>
</task>

</tasks>

<artifacts_produced>
Artifacts this plan produces:

| Path | Kind | Note |
|------|------|------|
| `.gitignore` | new file, tracked | Repo-root, additive to the six existing per-subdirectory ignore files (D-06) |
| `.git/inkframe-scrub-expressions.txt` | new file, never tracked | filter-repo `literal==>replacement` input |
| `.git/inkframe-scrub-literals.txt` | new file, never tracked | Bare literals; sole input to every scrub verification grep, here and in 04-04 |
| `.planning/phases/04-.../SCRUB-RECORD.md` | new file, tracked | Audit record of the rewrite; contains counts and method, never values |
| local git history | rewritten in place | ~13 commits' content altered; commit count, messages, authors, and dates preserved |
| pre-rewrite backup bundle | new file, outside the repo | The only rollback path; path recorded in SCRUB-RECORD.md |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public GitHub (crossed in 04-06) | Everything reachable from any ref becomes world-readable and third-party-archivable the instant the first push lands. This plan is the last point at which that content can still be changed. |
| working tree → git object store | `git add` promotes a file from private-on-disk to permanently-in-history; the root `.gitignore` is the enforcement point. |
| pre-rewrite objects → post-rewrite repo | Old objects survive in the local store via reflogs, worktree pins, and unpruned packs until explicitly expired. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | VPS IPv4/hostname in ~13 historical commits | high | mitigate | Task 3 filter-repo rewrite; gated on `git log --all -p` grep returning 0 against the derived literals file before this plan can close |
| T-04-01-02 | Information Disclosure | Home Wi-Fi name + BSSID in `hardware/logs/*.log`; home street address in 4 planning docs (F2) | high | mitigate | Task 2 puts the scope decision on the record with the developer; approved literals go into the same single rewrite pass and are covered by the same zero-match gate |
| T-04-01-03 | Information Disclosure | Pre-rewrite objects retained locally via reflog / the nested worktree pin (F4) | medium | mitigate | Task 1 removes and prunes the worktree; Task 3 Step 8 expires all reflogs and gc-prunes, then Step 9 re-verifies against the real repo, not just the scratch clone |
| T-04-01-04 | Information Disclosure | The plan, its SUMMARY, and SCRUB-RECORD.md re-leaking the very literals being removed | medium | mitigate | Sensitive values live only in two untracked files under `.git/`; every verification greps `-F -f` from the derived file, so no value is ever typed into a plan, a command, or a summary |
| T-04-01-05 | Denial of Service | Botched rewrite destroying 150 commits of project history with no way back | high | mitigate | Task 3 Step 4 takes a `git bundle` of all refs, verifies it, and stores it outside the repo tree before filter-repo runs; Step 5 operates on a scratch clone, never the only copy |
| T-04-01-06 | Tampering | Future `git add -A` committing a real credential for the first time, undoing the clean-history property | high | mitigate | Root `.gitignore` explicitly lists `deploy/inkframe.env` and `firmware/main/secrets.h`; Task 1's verify asserts `git check-ignore` matches both, and asserts the `.example` templates are NOT ignored |
| T-04-01-07 | Tampering | `git-filter-repo` itself, installed fresh from a package manager to process the whole repo | low | accept | Officially recommended by git's own documentation as the successor to `filter-branch`; installed from Homebrew core or PyPI under its canonical name. Task 3 Step 6/9 verify the *output* independently of the tool's own claims, so a misbehaving tool fails the gate rather than passing silently. |
</threat_model>

<verification>
1. `git status --porcelain` is empty.
2. `git check-ignore -q deploy/inkframe.env firmware/main/secrets.h .DS_Store` succeeds; the same command against `deploy/inkframe.env.example` fails.
3. `git log --all -p | grep -c -F -f .git/inkframe-scrub-literals.txt` prints `0`.
4. `git grep -F -f .git/inkframe-scrub-literals.txt HEAD -- .` returns nothing.
5. `git worktree list` prints exactly one line.
6. `git bundle verify <backup-path>` succeeds.
7. All 9 test harnesses still pass after the rewrite — the rewrite must not have altered any executable code. Run each of `server/test_dither.py`, `server/test_enrich.py`, `server/test_illustrations.py`, `server/test_pipeline_e2e.py`, `server/test_plane_detection.py`, `server/test_poll_loop.py`, `server/test_render.py`, `server/test_runway_config.py`, `stub-server/test_poll_cycle.py` under `server/.venv/bin/python3` from the repo root and confirm each exits 0.
</verification>

<success_criteria>
- A root `.gitignore` exists, is committed, and machine-verifiably ignores the credential files and OS/tooling cruft while leaving the `.example` templates tracked.
- The developer's scrub-scope decision is recorded, and every literal it approved is absent from all commit diffs and the working tree, proven by a grep that reads only from an untracked derived file.
- A verified, restorable backup of the pre-rewrite history exists outside the repo tree.
- Post-rewrite documentation reads naturally using the project's pre-existing angle-bracket placeholder convention.
- All 9 test harnesses still pass, proving the rewrite touched documentation prose and log captures only.
</success_criteria>

<output>
Create `.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-01-SUMMARY.md` when done.

The SUMMARY must not restate any scrubbed literal. Refer to them by category and count, exactly as SCRUB-RECORD.md does.
</output>
