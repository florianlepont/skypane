---
phase: 04-ci-cd-documentation-legal-compliance-github-actions-ci-tests
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - server/requirements-dev.txt
  - pyproject.toml
  - scripts/run-all-tests.sh
autonomous: true
requirements: [D-07, D-08, D-09]
must_haves:
  truths:
    - "A single command runs all 9 test harnesses, aggregates coverage across them, and fails if any harness fails or coverage falls below the threshold."
    - "`ruff check .` exits 0 against the current codebase with no source-code changes."
    - "The coverage threshold is derived from a recorded measurement of this suite, not chosen as a round number."
    - "CI-only tooling is pinned in a file separate from the production dependency list, so a production install never pulls lint/coverage tooling."
  artifacts:
    - server/requirements-dev.txt
    - pyproject.toml
    - scripts/run-all-tests.sh
  key_links:
    - "scripts/run-all-tests.sh -> .github/workflows/ci.yml (plan 04-04): CI invokes this script rather than restating the 9-file list, so the list can never drift between local and CI."
    - "scripts/run-all-tests.sh -> README.md (plan 04-05): the README tells a contributor to run the same command CI runs."
    - "pyproject.toml [tool.coverage.run] source/omit -> the fail_under number: change the scope and the threshold stops meaning what it was measured to mean."
---

<objective>
Build the local quality baseline that plan 04-04's CI workflow will invoke: pinned dev tooling, a lint configuration that is green on the existing codebase without rewriting working code, a coverage configuration scoped to this project's own modules, and one script that runs the whole suite the way both a contributor and CI will run it.

Purpose: D-09 makes lint and coverage blocking. A blocking gate is only useful if it is green on day one for the right reasons — a gate that is red from the moment it lands gets disabled, and a gate whose threshold was picked as a round number blocks nothing real. This plan establishes both by measurement. D-08 governs how CI installs dependencies at all: `server/requirements.txt`'s existing `==`-pinned versions (Pillow, requests) are what CI installs from directly — this plan's own `server/requirements-dev.txt` follows the identical pinned-version convention rather than re-deriving it.

Output: `server/requirements-dev.txt`, a root `pyproject.toml` carrying Ruff and coverage configuration, and `scripts/run-all-tests.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-CONTEXT.md
@.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-RESEARCH.md
@.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-PATTERNS.md

# The convention every test harness follows, and the dependency file this plan sits beside
@server/README.md
@server/requirements.txt

# The stage-echo / fail-fast shell convention scripts/run-all-tests.sh must match
@deploy/deploy.sh
</context>

<planner_findings>
Measured live during planning on this exact working tree. These are results, not estimates — do not re-derive them from scratch, but do confirm they still hold.

- **M1 — The suite is 9 files and all 9 pass.** `server/test_dither.py` (6 checks), `server/test_enrich.py` (16), `server/test_illustrations.py` (22), `server/test_pipeline_e2e.py` (5), `server/test_plane_detection.py` (6), `server/test_poll_loop.py` (5), `server/test_render.py` (26), `server/test_runway_config.py` (14), `stub-server/test_poll_cycle.py` (17). 117 checks, every file exit 0. This confirms 04-RESEARCH.md Pitfall 1: D-07's 7-file list in CONTEXT.md is stale and must never be the source of the list.
- **M2 — 04-RESEARCH.md's coverage invocation is broken as written.** Its Pattern 1 and full-workflow skeleton both pass parallel-mode and append together. coverage.py rejects that combination outright (`Can't append to data files in parallel mode.`) and exits non-zero on every single file. Parallel mode already writes one data file per process; append is what you use *instead*, not alongside. Use parallel mode alone, then combine.
- **M3 — Ruff's out-of-the-box behaviour at 0.16.4 is not usable as a blocking gate here.** Run with no configuration it reports **397 errors**, of which **328 are `UP031` printf-string-formatting** — the `"%s: %d/%d checks pass" % (...)` idiom this codebase uses consistently in every harness and module. Silencing that would mean rewriting 328 call sites in working, tested code for zero correctness benefit.
- **M4 — Ruff's classic correctness ruleset is nearly clean.** Restricted to `E4`, `E7`, `E9`, `F` (undefined names, unused imports, syntax and logic errors) the same tree reports **8 errors, all `E402`** — and all 8 are the project's deliberate, documented `sys.path` bootstrap: `server/poll_loop.py` (4), `server/plane/render.py` (3), `server/plane/enrich.py` (1), each preceded by a comment explaining that the repo root must be inserted into `sys.path` before the absolute `server.*` imports can resolve under direct script execution. "Fixing" these breaks direct script execution, which is how every harness and `poll_loop.py` itself are run.
- **M5 — Measured coverage.** With parallel-mode runs over all 9 files, `--source=server,stub-server`, then combine: **76%** including the harnesses themselves; **65%** production-only; **79%** production-only once the two subprocess-launched stub-server scripts are excluded. `stub-server/byos_server.py` and `stub-server/make_test_panel.py` both measure 0% not because they are untested but because `test_poll_cycle.py` launches the server as a **child process**, which an uninstrumented coverage run cannot see. Per-module production figures: `panel_format.py` 100%, `dither.py` 100%, `runway_config.py` 100%, `enrich.py` 93%, `render.py` 84%, `poll_loop.py` 71%, `illustrations.py` 66%, `detect.py` 59%.
- **M6 — Both tools are already installed into `server/.venv`** at the versions 04-RESEARCH.md verified (`ruff` 0.16.4, `coverage` 7.15.4), and `pip show` resolves them to their canonical upstream projects: ruff → `docs.astral.sh/ruff` with repository `github.com/astral-sh/ruff`; coverage → `github.com/coveragepy/coveragepy`.
- **M7 — `cairosvg` is present in `server/.venv` but imported by no shipped module.** It is Phase 2 residue. A clean venv built from `server/requirements.txt` alone runs all 9 harnesses green (confirmed on both Python 3.11 and 3.14), so it must not be added to either requirements file.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: Pin CI-only tooling in a separate dev requirements file</name>
  <files>server/requirements-dev.txt</files>
  <read_first>
    - `server/requirements.txt` — the exact format to mirror: one package per line, `==` pinned, nothing else.
    - `server/README.md`'s Setup section — the venv creation and install commands, and its standing rule that any new package goes through a legitimacy check before entering a requirements file.
    - 04-RESEARCH.md "Package Legitimacy Audit" — both tools were flagged by the automated checker and both overrides are documented false positives driven by an unreachable download-stats endpoint in a sandboxed environment, not by anything about the packages.
  </read_first>
  <action>
    Create `server/requirements-dev.txt` pinning exactly the two tools this phase's quality gate needs, at the versions 04-RESEARCH.md verified against the PyPI registry and M6 confirmed installed: the linter at 0.16.4 and the coverage engine at 7.15.4. Use the same one-package-per-line `==` format as `server/requirements.txt`.

    Add a short header comment stating why this file is separate from `server/requirements.txt`: production installs — including the one `deploy/deploy.sh` performs on the VPS when the requirements hash changes — must never pull lint or coverage tooling onto the production host. `deploy.sh` installs from `server/requirements.txt` only, and this separation is what keeps that true.

    Do not add `cairosvg` or anything else currently sitting in the local virtualenv to either file (M7).

    Install this file into `server/.venv` and confirm both tools resolve to their genuine upstream projects, closing the legitimacy item 04-RESEARCH.md left open. The confirmation is a provenance check on installed metadata, not a trust assertion: read each package's recorded project URL and check it points at the expected upstream repository under the expected organisation.
  </action>
  <verify>
    <automated>server/.venv/bin/pip install -r server/requirements-dev.txt &amp;&amp; server/.venv/bin/python3 -c "
import importlib.metadata as m, sys
want = {'ruff': 'astral-sh/ruff', 'coverage': 'coveragepy/coveragepy'}
for pkg, frag in want.items():
    md = m.metadata(pkg)
    urls = ' '.join([v for k, v in md.items() if k == 'Project-URL'] + [md.get('Home-page') or ''])
    assert frag in urls, (pkg, urls)
    print('provenance ok:', pkg, md['Version'])
" &amp;&amp; grep -c '==' server/requirements-dev.txt | grep -qx 2 &amp;&amp; ! grep -qi cairosvg server/requirements.txt server/requirements-dev.txt &amp;&amp; echo DEVREQS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `server/requirements-dev.txt` exists and contains exactly two `==`-pinned entries.
    - Installed metadata for both packages carries a project URL pointing at the expected upstream organisation and repository; the assertion above passes for both.
    - Neither requirements file mentions `cairosvg`.
    - `server/requirements.txt` is byte-identical to its pre-task content — production pins were not touched.
  </acceptance_criteria>
  <done>CI-only tooling is pinned separately from production dependencies, installed, and provenance-checked against its upstream projects.</done>
</task>

<task type="auto">
  <name>Task 2: Lint and coverage configuration, green by measurement</name>
  <files>pyproject.toml</files>
  <read_first>
    - `server/poll_loop.py` lines ~28-45, `server/plane/render.py` lines ~66-80, and `server/plane/enrich.py` lines ~34-43 — read the actual bootstrap blocks and the comments above them before deciding how to configure the import rule. These are the 8 findings from M4.
    - 04-RESEARCH.md "Pitfall 3" — why an unscoped coverage run measures library internals and makes any threshold meaningless.
    - `server/requirements.txt` — confirms Pillow and requests are the only production dependencies, which is what makes source-scoping necessary.
  </read_first>
  <action>
    Create a repo-root `pyproject.toml` holding this project's Ruff and coverage configuration. Include tool sections only — deliberately no project metadata table, because this repository is a deployed application rather than an installable distribution, and adding one would misrepresent it to packaging tooling.

    **Lint configuration.** Select the correctness families explicitly rather than relying on the tool's evolving defaults: the pycodestyle import-placement and statement families, the syntax/runtime-error family, and the pyflakes family (undefined names, unused imports, unreachable logic). M3 is the reason this is pinned rather than left to defaults — the unconfigured tool reports 397 findings dominated by a stylistic modernisation rule that would require rewriting 328 working call sites of the project's consistent printf-style formatting idiom. That is change-risk with no correctness benefit, so the rule family that produces it is out of the selected set.

    Then handle M4's 8 module-level-import-placement findings. All 8 sit immediately after the documented `sys.path` bootstrap that lets these modules be executed directly as scripts — the exact invocation style `server/README.md` prescribes for the whole test suite. Suppress that one rule repo-wide via the ignore list, with a comment recording that the bootstrap is intentional, that the alternative breaks direct script execution, and that the rule is suppressed rather than the code changed for that reason.

    Add a note in the same comment block that broadening the selected rule set later is a deliberate follow-up, and that the printf-formatting family is the specific one held back and why. Someone reading this config in six months must be able to tell the difference between "we never thought about it" and "we measured it and chose".

    **Coverage configuration.** Configure the run section so every invocation is scoped identically without repeating flags at each call site:
    - Source-scope measurement to this project's own two code directories, so library internals are never counted (Pitfall 3).
    - Enable parallel data files, since the suite is 9 independent processes that each need their own data file before being combined. Do not also enable appending — M2 established the two are mutually exclusive and the combination fails every run.
    - Omit the test harnesses themselves from the report: they are the measuring instrument, not the thing being measured.
    - Omit the two `stub-server/` scripts that M5 identified as structurally unmeasurable, each with a comment naming the real reason — `test_poll_cycle.py` exercises the server by launching it as a child process, which an uninstrumented run cannot observe. Leaving them in would permanently pin roughly 180 statements at 0% and turn the threshold into noise.

    **Threshold.** Set the report's failure threshold to **75**. Derivation, which must be recorded in a comment beside the number: M5 measured this suite at **79%** under exactly this source-and-omit scope. 75 sits four points below the measurement — tight enough that a genuine regression trips it, loose enough that a legitimate refactor which shifts a few statements does not red-line the build and train the developer to bypass the gate. Note in the same comment that the number is a floor to ratchet upward as coverage improves, and that changing the source or omit lists invalidates the measurement the number came from.

    Finally, confirm the configuration is actually being discovered and that lint is genuinely green — a config that is green because it was not loaded is worse than no config.
  </action>
  <verify>
    <automated>test -f pyproject.toml &amp;&amp; ! grep -qE '^\[project\]' pyproject.toml &amp;&amp; server/.venv/bin/ruff check . &amp;&amp; test -z "$(git diff --name-only -- '*.py')" &amp;&amp; server/.venv/bin/python3 -c "
import tomllib
c = tomllib.load(open('pyproject.toml','rb'))
run = c['tool']['coverage']['run']; rep = c['tool']['coverage']['report']
assert run.get('parallel') is True, run
assert not run.get('append'), run
assert set(run['source']) == {'server','stub-server'}, run['source']
assert rep['fail_under'] == 75, rep
sel = c['tool']['ruff']['lint']['select']
assert 'F' in sel and 'E9' in sel, sel
assert 'E402' in c['tool']['ruff']['lint']['ignore'], c['tool']['ruff']['lint']
print('config ok')
" &amp;&amp; echo LINTCOV_CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists at the repo root, contains `[tool.ruff]` and `[tool.coverage]` sections, and contains no `[project]` table.
    - `server/.venv/bin/ruff check .` exits 0 with zero findings, and no `.py` file under `server/`, `stub-server/`, or `adsb-test/` was modified to achieve that — confirmed by `git diff --stat -- '*.py'` being empty for this task.
    - The coverage run section sets parallel data files on and does not also set appending; the assertion above proves it.
    - Coverage source scope is exactly the two project directories; the report omits the test harnesses and the two subprocess-launched stub scripts.
    - The failure threshold is 75, and a comment beside it states the 79% measurement it was derived from and the scope that measurement assumed.
    - A comment records which lint rule family is deliberately excluded and why, so the choice is auditable.
  </acceptance_criteria>
  <done>Lint is configured to a correctness-focused rule set that is green on the untouched codebase, and coverage is scoped so its blocking threshold measures this project's own code.</done>
</task>

<task type="auto">
  <name>Task 3: One command that runs the whole suite, for contributors and CI alike</name>
  <files>scripts/run-all-tests.sh</files>
  <read_first>
    - `deploy/deploy.sh` — the shell convention to match: strict mode at the top, one `==>` stage echo per logical stage, all paths resolved from the repo root regardless of the caller's working directory.
    - `server/README.md`'s "Running the tests" section — the rule that harnesses must run from the repository root so relative fixture and geofence paths resolve, and under the virtualenv interpreter rather than a bare system interpreter.
    - 04-RESEARCH.md "Pattern 1" — the intended shape, but read it against M2 in this plan's findings before copying the invocation.
  </read_first>
  <action>
    Create `scripts/run-all-tests.sh`, the single entry point that runs the full suite with coverage and enforces the threshold. Plan 04-04's CI workflow will call this script rather than restating the file list, and plan 04-05's README will tell contributors to run the same thing — one list, one place, no drift.

    Follow `deploy/deploy.sh`'s conventions: strict shell mode, self-locating so it resolves the repo root from its own path and works when invoked from anywhere, and one `==>` stage echo per stage so a CI log reads the same way a local run does.

    Behaviour:
    - Resolve the interpreter to the project virtualenv's Python by default, but let the caller override it through an environment variable so CI can point at the interpreter it provisioned. Fail with a clear message if the resolved interpreter does not exist, rather than falling through to a system interpreter that lacks Pillow.
    - Clear any stale coverage data files from a previous run before starting, so a partial earlier run cannot inflate or corrupt this run's combined result.
    - Enumerate all **9** harness files explicitly, in a single list at the top of the script: the eight under `server/` and the one under `stub-server/`. M1 is the authority for this list, not D-07's stale 7-file enumeration in CONTEXT.md. Add a comment on the list stating that it is the canonical enumeration and that CONTEXT.md's shorter list is known-stale, so nobody "corrects" it back.
    - Run each file under coverage as its own process, letting each write its own parallel-mode data file, and surface each file's own exit code as the pass/fail signal — that is the convention every harness already implements with its `N/M checks pass` line and 0/1 exit. Do not pass an append flag alongside parallel mode; M2 documents why that combination fails.
    - Continue through the whole list rather than aborting on the first failure, then report which files failed and exit non-zero. A contributor fixing a broken change wants the full picture in one run; strict mode's default abort-on-first-error would hide the rest.
    - After the runs, combine the parallel data files and produce the report. The threshold lives in `pyproject.toml` from Task 2, so the report invocation must not restate it — one source of truth.
    - Exit non-zero if any harness failed or the threshold was not met; exit 0 only when both hold.
    - Make the script executable.

    Then confirm it works end to end, and confirm it genuinely fails when it should: temporarily raise the threshold above the measured figure, observe a non-zero exit, and restore it. A gate that has never been seen to fail has not been tested.
  </action>
  <verify>
    <automated>test -x scripts/run-all-tests.sh &amp;&amp; ./scripts/run-all-tests.sh &amp;&amp; ./scripts/run-all-tests.sh 2>&amp;1 | grep -c 'checks pass' | grep -qx 9 &amp;&amp; echo RUNNER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run-all-tests.sh` is executable and exits 0 from the repo root.
    - Its output contains exactly 9 `checks pass` lines — one per harness — proving no file was silently skipped.
    - It runs correctly when invoked from a different working directory (`cd /tmp && /Users/florian/Projects/ink-frame/scripts/run-all-tests.sh` exits 0).
    - The 9-file list appears exactly once in the script, carrying a comment marking it canonical over CONTEXT.md's stale list.
    - The threshold is read from `pyproject.toml`; the script does not restate a threshold number.
    - Temporarily raising the configured threshold above the measured coverage makes the script exit non-zero, and the original value is restored afterwards — recorded in the SUMMARY as evidence the gate actually gates.
    - A run leaves no stray coverage data files in the working tree (`git status --porcelain` is empty afterwards, since `.gitignore` from 04-01 covers them).
  </acceptance_criteria>
  <done>One executable command runs all 9 harnesses, aggregates coverage, enforces the configured threshold, and has been demonstrated to fail when the threshold is not met.</done>
</task>

</tasks>

<artifacts_produced>
Artifacts this plan produces:

| Path | Kind | Note |
|------|------|------|
| `server/requirements-dev.txt` | new file | Two `==`-pinned CI-only tools; deliberately not in `server/requirements.txt` so `deploy.sh` never installs them on the VPS |
| `pyproject.toml` | new file, repo root | `[tool.ruff]` + `[tool.coverage]` only; no `[project]` table |
| `scripts/run-all-tests.sh` | new file, executable | Canonical 9-file runner; sole test entry point for CI (04-04) and contributors (04-05) |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → developer machine and CI runner | Two new third-party packages enter the toolchain and execute with full access to the checkout. |
| dev tooling → production host | `deploy/deploy.sh` installs from a requirements file onto the real VPS; whatever lands in that file runs in production. |
| quality gate → merge decision | Once CI is blocking, this configuration decides what is allowed into `main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-SC | Tampering | `pip install -r server/requirements-dev.txt` — supply chain for the two new packages | high | mitigate | Both `==`-pinned to registry-verified versions; Task 1's verify asserts installed metadata resolves to the expected upstream organisation and repository, so a name-squatted substitute fails the gate. 04-RESEARCH.md's checker flags are documented false positives from an unreachable download-stats endpoint, not package signals. |
| T-04-02-01 | Tampering | Lint/coverage tooling reaching the production VPS through the deploy path | medium | mitigate | Dev tooling is pinned in a separate file; `deploy/deploy.sh` installs from `server/requirements.txt` only. Task 1's acceptance criteria require `server/requirements.txt` to be byte-identical after the task. |
| T-04-02-02 | Repudiation | A coverage threshold that measures the wrong scope, so a passing gate certifies nothing | medium | mitigate | Source scope pinned to the two project directories (Pitfall 3); unmeasurable subprocess-launched scripts omitted with a stated reason; the threshold carries a comment recording the measurement and scope it was derived from. |
| T-04-02-03 | Repudiation | A blocking gate that has never been observed to fail | medium | mitigate | Task 3 requires demonstrating a real failure by temporarily raising the threshold, restoring it, and recording the evidence in the SUMMARY. |
| T-04-02-04 | Denial of Service | A lint gate red on arrival, prompting the gate to be disabled or bypassed rather than fixed | medium | mitigate | Rule set chosen from a measurement (M3/M4) so it is green on the untouched codebase; the deliberately-excluded family and the suppressed rule each carry a written rationale in the config. |
| T-04-02-05 | Information Disclosure | Coverage HTML/XML output or data files being committed | low | mitigate | Coverage data files and the lint cache are covered by the root `.gitignore` created in 04-01; Task 3's acceptance criteria require a clean `git status --porcelain` after a run. |
</threat_model>

<verification>
1. `server/.venv/bin/pip install -r server/requirements-dev.txt` succeeds and both packages' installed metadata resolves to their expected upstream repositories.
2. `server/.venv/bin/ruff check .` exits 0 with no findings, and `git diff --stat -- '*.py'` shows no source file was modified to get there.
3. `./scripts/run-all-tests.sh` exits 0 and prints exactly 9 `checks pass` lines.
4. `./scripts/run-all-tests.sh` invoked from an unrelated working directory also exits 0.
5. Raising the configured threshold above the measured figure makes the script exit non-zero; restoring it makes it pass again.
6. `git status --porcelain` is empty after a full run.
7. `server/requirements.txt` is unchanged from its pre-plan content.
</verification>

<success_criteria>
- Lint runs blocking-green over the whole repository against a correctness-focused rule set, with zero changes to working source code and a written rationale for every rule family excluded or suppressed.
- Coverage is scoped to this project's own modules, aggregates correctly across 9 independent processes, and enforces a threshold traceable to a recorded measurement of this suite.
- One executable command reproduces the entire gate locally, is proven to fail when it should, and is ready for plan 04-04's CI workflow to call without restating the file list.
- Production dependency pins are untouched, so nothing in this plan can reach the VPS through the deploy path.
</success_criteria>

<output>
Create `.planning/phases/04-ci-cd-documentation-legal-compliance-github-actions-ci-tests/04-02-SUMMARY.md` when done.

Record in it: the confirmed 9-file list, the measured coverage figure and the scope it was measured under, the threshold chosen and its margin, the lint rule families selected/excluded/suppressed with reasons, and the evidence that the threshold gate was observed failing and then restored.
</output>
