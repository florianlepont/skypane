---
phase: 31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu
plan: 04
type: execute
wave: 4
depends_on: ["31-02", "31-03"]
files_modified:
  - scripts/run_all_tests.py
  - pyproject.toml
  - .planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-TIMINGS.md
autonomous: true
requirements: [D-01, D-03, D-05, D-06, D-07]
user_setup: []

must_haves:
  truths:
    - "The two extracted harnesses are entries in scripts/run_all_tests.py's HARNESSES list, so CI actually runs them — a file that exists but is not listed there silently never runs (D-07)."
    - "Both new entries also appear near the top of EXPECTED_SLOWEST, so _submission_order() puts them in flight from the first moment rather than queueing them behind short harnesses on a 4-worker pool (D-07)."
    - "No .github/workflows/ci.yml change was needed or made — the extracted files join the existing local worker pool and CI still just calls ./scripts/run-all-tests.sh (D-07's explicit constraint)."
    - "The full suite passes end-to-end at JOBS=4 with the new entries, and the coverage threshold in pyproject.toml still passes — splitting a file must not move coverage, because the harnesses themselves are omitted from measurement."
    - "The post-split JOBS=4 total wall time is recorded next to plan 01's pre-split baseline in 31-TIMINGS.md, giving D-05's 30-40% gate a real number instead of a static proxy (RESEARCH.md Pitfall 4 and Open Question 1)."
    - "Every stale harness-count figure in the files this plan touches is corrected to match the live AST count, per this repo's own norm of not leaving a number that contradicts the code (RESEARCH.md Corrections to CONTEXT.md item 1)."
  artifacts:
    - "scripts/run_all_tests.py with 24 HARNESSES entries and 10 EXPECTED_SLOWEST entries"
    - ".planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-TIMINGS.md with pre-split and post-split sections"
  key_links:
    - "HARNESSES is the single source of truth CI and README both defer to; a new harness file is invisible to CI until its path is a string in that list."
    - "EXPECTED_SLOWEST feeds _submission_order(), which submits listed harnesses first in list order and then everything else — placement near the top is what keeps the critical path saturated on a 4-worker pool."
    - "_run_one()'s subprocess, timeout, process-group and coverage-parallel plumbing is already generic over any path in HARNESSES — no new mechanism is added, only list entries, which is precisely what D-07 mandates."
---

<objective>
Wire the two extracted harnesses into the suite's existing worker pool and
measure what the split actually bought.

This is the plan where D-07's mechanism choice becomes real: the extracted files
join `scripts/run_all_tests.py`'s `HARNESSES` list and are picked up by the
`ThreadPoolExecutor` pool that already exists, with zero changes to
`.github/workflows/ci.yml`, zero new scheduling code, and zero GitHub Actions
matrix strategy. `_run_one()`'s subprocess/timeout/coverage-parallel plumbing is
already generic over any path in the list.

It is also where D-05 stops being a guess. RESEARCH.md's Open Question 1 states
plainly that the wall-time split cannot be determined by static analysis, and
Pitfall 4 warns that its own `.goto()`-count proxy probably undercounts the two
extracted groups because both are `_in_both_themes()`-heavy. This plan produces
the measured number, compared against plan 01's pre-split baseline taken on the
same host with the same `JOBS=4` pinning.

Purpose: make the split take effect, and produce the evidence plan 05's decision
checkpoint needs.
Output: a wired `scripts/run_all_tests.py`, a green full suite at `JOBS=4`, and
`31-TIMINGS.md` carrying a real before-and-after.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-CONTEXT.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-RESEARCH.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-PATTERNS.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-02-SUMMARY.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-03-SUMMARY.md
</context>

## Artifacts this phase produces

Created or modified by **this plan** (plan 04):

| Artifact | Kind | Notes |
|---|---|---|
| `"companion/test_browser_ux_health_drawings.py"` in `HARNESSES` | NEW list entry | String entry with its own explanatory inline comment, following the repo's convention of commenting every non-self-explanatory entry. |
| `"companion/test_browser_ux_quiet_wake.py"` in `HARNESSES` | NEW list entry | Same. |
| `"companion/test_browser_ux_health_drawings.py"` in `EXPECTED_SLOWEST` | NEW tuple entry | Placed immediately after `"companion/test_browser_ux.py"`, at the top of the longest-first submission order. |
| `"companion/test_browser_ux_quiet_wake.py"` in `EXPECTED_SLOWEST` | NEW tuple entry | Same. |
| Corrected harness-count prose | MODIFIED comments | Three stale figures in `scripts/run_all_tests.py` (module docstring, the `HARNESSES` header comment, and the coverage-parallel comment inside `main()`) and one in `pyproject.toml`'s `[tool.coverage.run]` header comment. |
| `31-TIMINGS.md` post-split section | MODIFIED measurement artifact | Post-split `JOBS=4` per-harness table, total wall time, delta against the pre-split baseline, and the computed position against D-05's 30-40% gate. |

No new function, class, module, application symbol, route, endpoint, env var or
config key. `.github/workflows/ci.yml` is deliberately untouched (D-07).

<tasks>

<task type="auto">
  <name>Task 1: Register the two extracted harnesses and correct every stale harness count</name>
  <files>scripts/run_all_tests.py, pyproject.toml</files>
  <read_first>
    - `scripts/run_all_tests.py` lines 1-35 — the module docstring, which opens with a stale harness count and documents the env-var contract (`PYTHON`, `JOBS`, `HARNESS_TIMEOUT_S`, `COVERAGE_CORE`).
    - `scripts/run_all_tests.py` lines 49-90 — the `HARNESSES` header comment and the list itself. Read the header comment carefully: it records that a prior canonical list in `04-CONTEXT.md` is known-stale and must NOT be "corrected" back down to match, and it declares this list the single source of truth CI and README both defer to. Note also the existing inline comment on the `companion/test_browser_ux.py` entry explaining why that entry needs explaining — new entries follow that convention.
    - `scripts/run_all_tests.py` lines 92-119 — the `EXPECTED_SLOWEST` tuple with its measured-timings comment, and `_submission_order()`, which submits every `EXPECTED_SLOWEST` member present in `HARNESSES` first in tuple order and then everything else in `HARNESSES` order.
    - `scripts/run_all_tests.py` lines 185-195 — the coverage-parallel comment inside `main()`, which states a harness count as part of its reasoning and is the third stale figure.
    - `scripts/run_all_tests.py` lines 246-285 — the reporting block that emits the timing table, the total wall time and the `GITHUB_STEP_SUMMARY` markdown table. Confirm it iterates `HARNESSES` and therefore picks up new entries with no further change.
    - `pyproject.toml` `[tool.coverage.run]` header comment — it opens by stating the suite is a specific number of independent processes, the fourth stale figure. Read the whole block to confirm `parallel = true` genuinely needs no change for two more concurrent processes, and that the `omit` list's `companion/test_*.py` glob already covers both new harnesses and the helper module.
    - `.github/workflows/ci.yml` lines 63-92 — confirm for yourself that the test step is an unconditional `./scripts/run-all-tests.sh` with no harness list and no `JOBS` override, which is why D-07 says this file needs no change.
    - `.planning/phases/31-.../31-RESEARCH.md` "Corrections to CONTEXT.md" item 1 and "Code Examples" section "scripts/run_all_tests.py wiring (exact entries to add)".
  </read_first>
  <action>
Add the two extracted harness paths to `HARNESSES`, immediately after the
existing `companion/test_browser_ux.py` entry so the three browser harnesses read
as one family. Give each a short inline comment in the style of its neighbour:
name the phase and plan that created it, say it was split out of
`companion/test_browser_ux.py` to let the worker pool run it concurrently, and
note that like its parent it skips cleanly with exit 0 when playwright or
Chromium is unavailable — that last point is what the existing entry's comment
establishes and it remains true of the new files.

Add both paths to `EXPECTED_SLOWEST`, immediately after the existing
`companion/test_browser_ux.py` entry and before `server/test_render.py`. They
belong at the top because each still launches its own Chromium plus its own
`companion/app.py` subprocess, which is the property that made the parent the
slowest file by construction in the first place. Extend that tuple's comment to
record the actual standalone wall times plans 02 and 03 measured for the two new
files, so the ordering rationale stays evidence-backed rather than assumed — the
comment already carries measured numbers for the existing entries and that
convention should not lapse.

Do NOT reorder, rename or remove any existing entry, and do not touch
`_submission_order()`, `_run_one()`, the `JOBS` handling or the timeout/SIGKILL
logic. That plumbing is already generic over any path in `HARNESSES`; D-07's
whole point is that this phase adds list entries and nothing else.

Then correct the stale harness counts. Three live in `scripts/run_all_tests.py`
— the module docstring's opening sentence, the `HARNESSES` header comment's
opening phrase, and the coverage-parallel comment inside `main()` — and a fourth
lives in `pyproject.toml`'s `[tool.coverage.run]` header comment. All four state
a count that was already wrong before this phase (the live list held 22 entries
while the prose said 18) and would be wrong twice over after it. Set each to the
live count, which after this task is 24. Derive that number by AST-counting the
list rather than by eye, and phrase each correction so it matches the list rather
than restating a number that can drift again. Leave the `HARNESSES` header
comment's warning about the known-stale `04-CONTEXT.md` list intact — that
warning is still load-bearing.

Make no other change to `pyproject.toml`. Its coverage `omit` list already covers
all three browser harnesses and the shared helper module through the existing
`companion/test_*.py` glob, which is exactly why plan 01 chose a `test_`-prefixed
helper module name (RESEARCH.md Pitfall 3, option 1). `parallel = true` already
gives every process its own data file and needs no adjustment for two more
concurrent processes. `fail_under` stays where it is: the harnesses are omitted
from measurement, so splitting one into three moves no covered statement.
  </action>
  <verify>
    <automated>server/.venv/bin/ruff check scripts/ &amp;&amp; server/.venv/bin/python3 -c "
import ast,re
s=open('scripts/run_all_tests.py').read(); t=ast.parse(s)
H=[x.value for x in [a for a in t.body if isinstance(a,ast.Assign) and getattr(a.targets[0],'id','')=='HARNESSES'][0].elts]
E=[x.value for x in [a for a in t.body if isinstance(a,ast.Assign) and getattr(a.targets[0],'id','')=='EXPECTED_SLOWEST'][0].elts]
assert len(H)==24, len(H)
assert len(set(H))==24, 'duplicate HARNESSES entry'
for p in ('companion/test_browser_ux.py','companion/test_browser_ux_health_drawings.py','companion/test_browser_ux_quiet_wake.py'):
    assert p in H, p
    assert p in E, p
assert 'companion/test_browser_ux_helpers.py' not in H, 'helper module must never be a harness'
assert E.index('companion/test_browser_ux_health_drawings.py') < E.index('server/test_render.py')
assert E.index('companion/test_browser_ux_quiet_wake.py') < E.index('server/test_render.py')
import os
for p in H: assert os.path.exists(p), p
stale=[n for n in re.findall(r'\b(1[0-9]|2[0-9])\b(?=[^\n]*harness)', s, re.I) if n!='24']
print('wiring-ok', len(H), len(E), 'lingering-count-tokens:', stale)
" &amp;&amp; server/.venv/bin/python3 -c "
import ast,tomllib
c=tomllib.load(open('pyproject.toml','rb'))['tool']['coverage']
assert c['run']['parallel'] is True
assert 'companion/test_*.py' in c['run']['omit']
assert c['report']['fail_under']==83
print('coverage-config-unchanged-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - AST-parsing `scripts/run_all_tests.py` yields a `HARNESSES` list of exactly 24 unique entries, every one of which is a path that exists on disk.
    - `companion/test_browser_ux_health_drawings.py` and `companion/test_browser_ux_quiet_wake.py` are present in both `HARNESSES` and `EXPECTED_SLOWEST`, and in `EXPECTED_SLOWEST` both sit before `server/test_render.py`.
    - `companion/test_browser_ux_helpers.py` appears in neither list — it is a module, not a harness.
    - Each new `HARNESSES` entry carries an inline comment naming phase 31 and stating the skip-cleanly property.
    - The `EXPECTED_SLOWEST` comment records the measured standalone wall times for the two new files, taken from the plan 02 and plan 03 summaries.
    - All four stale harness-count figures now read 24, matching the AST count; the `stale` list printed by the verify command is empty.
    - `pyproject.toml` retains `parallel = true`, the `companion/test_*.py` omit entry and `fail_under = 83`; the only change to that file is the corrected count in a comment.
    - `git status --porcelain .github/` reports no change — D-07's no-YAML-change constraint is honoured.
    - `server/.venv/bin/ruff check scripts/` exits 0.
  </acceptance_criteria>
  <done>Both extracted harnesses are registered, ordered longest-first, and every harness-count figure in the touched files agrees with the code. CI will now run them with no workflow change.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full suite at JOBS=4 and record the measured post-split wall time</name>
  <files>.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-TIMINGS.md</files>
  <read_first>
    - `.planning/phases/31-.../31-TIMINGS.md` — plan 01's pre-split section: the per-harness table, the total wall time, the commit SHA it was taken at, the host core count, and D-01's recorded CI figures. This task appends a directly comparable post-split section.
    - `.planning/phases/31-.../31-RESEARCH.md` Pitfall 5 — why the run must pin `JOBS=4` rather than let the host's own core count apply, and why local numbers are a proxy for CI rather than a substitute.
    - `.planning/phases/31-.../31-RESEARCH.md` Pitfall 4 and Open Question 1 — why the static `.goto()`-count proxy probably undercounts the two extracted groups, and why only a real run can answer the gate question.
    - `scripts/run_all_tests.py` lines 246-285 — the exact output lines to harvest: the `==> Timing (slowest first)` table and the `==> Total wall time: N.Ns (JOBS=N)` line.
    - `scripts/run_all_tests.py` around the coverage-report invocation — the suite fails as a whole if `coverage report` returns non-zero against `fail_under`, so a green run also proves the coverage threshold survived the split.
  </read_first>
  <action>
Run the complete suite from the repository root with the worker count pinned to 4
via the `JOBS` environment variable, invoking `./scripts/run-all-tests.sh`. Pin
to 4 for the same reason plan 01 did: this host has more cores than the CI
runner, and an unpinned run hides contention that CI will actually experience,
understating the gain.

Run it at least twice and keep both numbers. A single wall-clock sample of a
browser-driving suite is noisy, and D-05's gate is a percentage — a 5% sampling
swing changes the answer near the boundary. Report the range, not a single
figure.

Append a post-split section to `31-TIMINGS.md` containing: the full per-harness
timing table as printed; the total wall time from each run; the current commit
SHA; the host core count; and the three browser harnesses' individual times
called out, since they are the whole story.

Then compute and record the comparison against the pre-split baseline: absolute
seconds saved, percentage reduction in total suite wall time, and where that
percentage falls relative to D-05's 30-40% bar. State the arithmetic explicitly
so a reader can check it.

Record the interpretation honestly and separately from the raw numbers. Three
caveats belong in writing. First, the local total is a proxy — D-05's gate is
defined against the CI "test" job measured by `gh run view`, exactly as D-01
measured the baseline, and that number only exists after this branch runs in CI.
Second, the suite total is not the CI job total: the job also pays for checkout,
venv install and the Chromium download, which the split does not touch, so the
job-level percentage will be smaller than the suite-level percentage. Compute the
job-level estimate too, using D-01's ~5min40 job average against its ~300-320s
file contribution, and label it as an estimate. Third, per D-06 there is no fixed
target time; "materially faster than today" is the bar, and the point of stopping
is diminishing returns rather than an arbitrary number.

If the suite fails, do not adjust a counter or a threshold to make it pass.
A harness failure here after three green standalone runs most likely means
resource contention at `JOBS=4` with three concurrent Chromium instances —
investigate and report it, because that is a real finding about the mechanism
D-07 chose, not a number to tune away.
  </action>
  <verify>
    <automated>JOBS=4 ./scripts/run-all-tests.sh > /tmp/31-04-suite.txt 2>&amp;1; echo "suite-exit=$?"; grep -E '^==> Result:' /tmp/31-04-suite.txt; grep -E 'Total wall time' /tmp/31-04-suite.txt; grep -cE '^==> PASS ' /tmp/31-04-suite.txt; grep -E '^\s+companion/test_browser_ux' /tmp/31-04-suite.txt; python3 -c "
import re
s=open('/tmp/31-04-suite.txt').read()
assert '==> Result: PASS' in s, 'suite did not pass'
m=re.search(r'Total wall time: ([\d.]+)s \(JOBS=(\d+)\)', s); assert m, 'no total wall time line'
assert m.group(2)=='4', 'JOBS was not pinned to 4: '+m.group(2)
assert s.count('==> PASS ')==24, s.count('==> PASS ')
t=open('.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-TIMINGS.md').read()
for k in ('pre-split','post-split','JOBS=4'):
    assert k.lower() in t.lower(), k
print('suite-green', m.group(1)+'s', 'timings-recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - `JOBS=4 ./scripts/run-all-tests.sh` prints `==> Result: PASS` and exits 0, with 24 `==> PASS` lines — every harness including the two new ones.
    - The printed `Total wall time` line reports `(JOBS=4)`, proving the pin took effect.
    - The coverage threshold still passes: the suite's own `coverage report` step returns 0, which is a precondition of the `Result: PASS` line above.
    - `31-TIMINGS.md` contains a post-split section with the full timing table, at least two independent total-wall-time samples, the commit SHA and the host core count, alongside the pre-split section plan 01 wrote.
    - `31-TIMINGS.md` states the absolute seconds saved, the suite-level percentage reduction, the estimated CI-job-level percentage reduction derived from D-01's figures, and where each falls relative to D-05's 30-40% bar, with the arithmetic shown.
    - `31-TIMINGS.md` records all three caveats in writing: local-proxy-versus-CI, suite-total-versus-job-total, and D-06's no-fixed-target rule.
    - The three browser harnesses' individual wall times are called out explicitly, so the remaining critical path is visible to plan 05's decision.
  </acceptance_criteria>
  <done>The suite is green end-to-end with the new entries and the split's real cost saving is on record with its arithmetic and its caveats.</done>
</task>

<task type="auto">
  <name>Task 3: Record the D-05 gate position and the follow-up recommendation</name>
  <files>.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-TIMINGS.md</files>
  <read_first>
    - `.planning/phases/31-.../31-CONTEXT.md` D-04, D-05 and D-06 verbatim — the exact wording of the gate, the deferral it controls, and the explicit instruction to stop at diminishing returns rather than chase a number.
    - `.planning/phases/31-.../31-CONTEXT.md` "Deferred Ideas" — what a follow-up phase would and would not cover, so the recommendation does not quietly re-scope it.
    - `.planning/phases/31-.../31-RESEARCH.md` "Why the mega-cluster is not this phase's target" — the ~70% of check bodies left behind, the 34 Display-page and 7 Device-page checks, and the handful written to span both pages in one check body, which is what makes the remainder hard.
    - `.planning/phases/31-.../31-TIMINGS.md` as written by task 2.
    - `.planning/phases/31-.../31-02-SUMMARY.md` and `31-03-SUMMARY.md` — the per-file standalone times, which show where the remaining critical path sits.
  </read_first>
  <action>
Write the gate verdict into `31-TIMINGS.md` as a clearly-labelled section that
plan 05's decision checkpoint reads directly.

State three things without hedging. First, the measured suite-level reduction and
the estimated job-level reduction, each as a number. Second, whether that clears,
approaches or misses D-05's ~30-40% bar, judged against the job-level estimate
since that is what D-05 is written about, with the suite-level figure given
alongside. Third, what the critical path is now: with the drawings and quiet-wake
groups gone, whether the reduced `companion/test_browser_ux.py` still dominates
the suite or whether some other harness has become the bound. That last point is
what determines whether any further extraction can help at all — if the reduced
parent is no longer the longest pole, extracting a third group buys nothing
regardless of how the percentage came out, and D-06's diminishing-returns
instruction applies directly.

Then write a recommendation for plan 05's checkpoint covering both branches.
If the gate is cleared comfortably, recommend skipping the optional Flights
extraction in this phase and opening the D-05 follow-up phase for the settings
mega-cluster instead — the incremental approach is validated and the remaining
work is better done with its own planning pass. If the gate is missed or
marginal, recommend the Flights extraction as the cheapest remaining win, and
flag what plan 05 must handle that RESEARCH.md did not: the early Flights cluster
is not contiguous. A quiet-hours-caption check sits between the first and second
Flights checks in file order, so the Flights extraction must be anchored on check
names rather than on a line range, and that check must be left behind.

Record whether any harness other than the three browser files showed a wall-time
change, which would indicate pool contention rather than a real cost shift.

Keep this section factual. The decision belongs to the developer at plan 05's
checkpoint; this section's job is to make that decision cheap to take.
  </action>
  <verify>
    <automated>python3 -c "
t=open('.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-TIMINGS.md').read().lower()
need=['d-05','d-06','gate','critical path','recommend','flights','follow-up']
missing=[k for k in need if k not in t]
assert not missing, missing
import re
assert re.search(r'\d+(\.\d+)?\s*%', t), 'no percentage figure recorded'
print('gate-section-ok')" &amp;&amp; server/.venv/bin/python3 companion/test_browser_ux.py > /tmp/31-04-parent.txt 2>&amp;1; grep -E 'checks pass$' /tmp/31-04-parent.txt</automated>
  </verify>
  <acceptance_criteria>
    - `31-TIMINGS.md` contains a clearly-labelled gate section naming D-05 and D-06, quoting at least one percentage figure, and stating a clear cleared / marginal / missed verdict against the ~30-40% bar.
    - That section names the current critical path by harness path and states whether the reduced `companion/test_browser_ux.py` still dominates the suite.
    - It carries a recommendation for plan 05 covering both branches, and where it recommends the Flights extraction it flags the non-contiguity of the early Flights cluster and the check that must be left behind.
    - It notes whether any non-browser harness's wall time moved, distinguishing pool contention from a real cost shift.
    - `companion/test_browser_ux.py` still reports `76/76 checks pass` — this plan changed no harness content, and the re-run proves it.
  </acceptance_criteria>
  <done>Plan 05's checkpoint has a factual, arithmetic-backed basis for the extract-or-stop decision, and the D-05 gate position is on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → harness subprocesses | `_run_one()` launches each harness as a child process in its own process group under `coverage run`, with a wall-clock cap and a SIGKILL escalation, writing stdout to a real file rather than a pipe to avoid a full-pipe deadlock. Two new children join that existing, unchanged mechanism. |
| Harness subprocess → local HTTP server | Unchanged from plans 02 and 03: each browser harness drives only `127.0.0.1:<ephemeral-port>` origins it launched itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-10 | Tampering | `HARNESSES` / `EXPECTED_SLOWEST` edits | low | mitigate | Task 1's AST assertions require exactly 24 unique entries, every path existing on disk, no removal or duplication of an existing entry, and the helper module absent from both lists. A dropped entry would silently stop running a harness in CI, which is the real risk here and is what the count-and-existence assertion catches. |
| T-31-11 | Denial of Service | four concurrent harness children at `JOBS=4` | medium | mitigate | Three of the 24 harnesses now each launch a Chromium plus a `companion/app.py` subprocess, so a 4-worker pool can run all three at once. `_run_one()`'s existing `HARNESS_TIMEOUT_S` cap with process-group SIGKILL bounds any hang. Task 2's full-suite run at `JOBS=4` is the empirical check that the machine tolerates the concurrency; a failure there is explicitly called out as a finding to report rather than a number to tune. |
| T-31-12 | Repudiation | measurement integrity | low | mitigate | Every recorded figure is accompanied by the commit SHA, the host core count and the pinned `JOBS` value, and at least two independent samples are kept, so the D-05 verdict can be re-derived rather than taken on trust. |
| T-31-SC | Tampering | dependency installation | low | accept | No package install occurs in this plan. The suite runs against the venv plan 01 provisioned from the repository's own pinned requirements files, identical to what `.github/workflows/ci.yml` installs. No new dependency is introduced anywhere in phase 31, so RESEARCH.md's Package Legitimacy Gate has nothing to audit. |

ASVS level 1, blocking on `high`. RESEARCH.md's Security Domain section found V2,
V3, V4, V5 and V6 all inapplicable: this plan adds two strings to a list and
corrects four stale comments. No application code, endpoint, auth path or input
handler is touched, and `.github/workflows/ci.yml` is deliberately not modified at
all. A documented no-applicable-threats finding is the correct threat model here,
not an empty one. Nothing in this register is rated `high` or above, so nothing
blocks.
</threat_model>

<verification>
- `server/.venv/bin/ruff check .` exits 0.
- `HARNESSES` AST-counts to 24 unique existing paths; `EXPECTED_SLOWEST` contains all three browser harnesses ahead of `server/test_render.py`.
- `companion/test_browser_ux_helpers.py` is in neither list.
- `JOBS=4 ./scripts/run-all-tests.sh` prints `==> Result: PASS` with 24 `==> PASS` lines, and the coverage threshold holds.
- `31-TIMINGS.md` carries comparable pre-split and post-split sections, the delta arithmetic, the D-05 gate verdict and a plan-05 recommendation.
- `git status --porcelain .github/` reports no change (D-07).
- `pyproject.toml`'s `parallel`, `omit` and `fail_under` values are unchanged; only a comment count was corrected.
</verification>

<success_criteria>
- The two extracted harnesses run in CI through the existing worker pool with zero workflow, scheduling or coverage-config change — D-07 satisfied exactly as written.
- The full suite is green at `JOBS=4` with three concurrent browser harnesses.
- D-05's gate has a measured number with shown arithmetic and stated caveats, not a static proxy.
- Every harness-count figure in the touched files agrees with the live list.
</success_criteria>

<output>
Create `.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-04-SUMMARY.md` when done.
</output>
