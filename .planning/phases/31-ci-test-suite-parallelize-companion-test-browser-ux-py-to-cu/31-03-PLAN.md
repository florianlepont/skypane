---
phase: 31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - companion/test_browser_ux_quiet_wake.py
  - companion/test_browser_ux.py
autonomous: true
requirements: [D-04, D-07]
user_setup: []

must_haves:
  truths:
    - "companion/test_browser_ux_quiet_wake.py runs standalone, owns its own companion/app.py subprocess and its own Chromium, and passes 9/9 checks (D-04's second mandatory extraction — RESEARCH.md's largest fully-contiguous block outside the settings mega-cluster)."
    - "The new file skips cleanly with exit 0 when playwright or Chromium is unavailable, matching every other browser harness this phase creates."
    - "companion/test_browser_ux.py passes 76/76 after this extraction, with EXPECTED_CHECK_COUNT re-derived by RUNNING (RESEARCH.md Pitfall 1)."
    - "Every one of the 9 moved checks reports the same PASS verdict standalone as it did inside the monolith, proven by diffing against 31-BASELINE-CHECKS.txt (RESEARCH.md Pitfall 2)."
    - "The settings dirty-bar audit that stays in companion/test_browser_ux.py still resolves _quiet_arc_minutes and _handle_sel — the two cross-group-coupled helpers plan 01 promoted to the shared module precisely so this extraction would not strand them."
    - "18 of the original 96 checks now run in two extra processes rather than sequentially inside one, which is the concrete mechanism behind D-04's incremental wall-time bet and the input to D-05's gate."
  artifacts:
    - "companion/test_browser_ux_quiet_wake.py"
  key_links:
    - "companion/test_browser_ux_quiet_wake.py imports _quiet_arc_minutes, _quiet_caption_minutes, _handle_sel and the quiet-dial decoder cluster from companion.test_browser_ux_helpers rather than defining them — plan 01's promotion is what makes this import legal from both sides."
    - "_quiet_hours_on_disk, _set_window, _wake_interval_on_disk and _set_interval are main()-local closures over `harness` and must land inside the new file's main(), closing over ITS harness, never be imported."
    - "The three checks in the file that still reference the quiet dial from outside this block (the settings dirty-bar audit) must keep passing in the reduced parent — that is the proof the promotion worked."
---

<objective>
Extract the quiet-hours dial and wake-interval slider scenario group — the
25-04/25-05 widget family — out of `companion/test_browser_ux.py` into its own
independently-runnable harness.

This is D-04's second mandatory extraction and RESEARCH.md's rank-2 candidate:
the single largest fully-contiguous block outside the settings mega-cluster, one
coherent widget family, nine checks, three `_in_both_themes()` loops.

**Boundary correction against RESEARCH.md.** RESEARCH.md scopes this group at
pre-plan-01 lines 11516-13379. Line 11516 is a `page.goto()` call in the middle
of the `_set_window()` fixture, not a block boundary — cutting there would leave
the block's local fixtures behind and the moved checks would die on `NameError`.
The real boundary starts at the `25-04-PLAN.md Task 4 (CFG-48)` banner
(pre-plan-01 line 11444), which is where the block's local selector constants and
its four disk-and-UI fixtures begin. Verified against the live file: with that
start point, `QUIET_DIAL_SEL`, `QUIET_ARC_SEL`, `QUIET_HANDLES_SEL`,
`QUIET_READOUT_SEL`, `_quiet_hours_on_disk`, `_set_window`, `_SETTLE_DIAL`,
`_wake_interval_on_disk` and `_set_interval` all have exactly zero references
outside the block. The one name that did leak — `_handle_sel`, referenced by the
settings dirty-bar audit that stays behind — was promoted to the shared helper
module by plan 01 for exactly this reason.

Purpose: complete D-04's "2-3 largest logical scenario groups" mandate and put 18
of the original 96 checks into their own processes, which is the entire mechanism
by which this phase buys wall time.
Output: `companion/test_browser_ux_quiet_wake.py` at 9/9, and
`companion/test_browser_ux.py` reduced to 76/76.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-CONTEXT.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-RESEARCH.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-PATTERNS.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-01-SUMMARY.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-02-SUMMARY.md
</context>

## Artifacts this phase produces

Created by **this plan** (plan 03):

| Artifact | Kind | Notes |
|---|---|---|
| `companion/test_browser_ux_quiet_wake.py` | NEW harness | Own `main()`, own `results`/`check()` closure, own `EXPECTED_CHECK_COUNT` (expected 9, set by running), own `Harness()` + `seed_state_dir()` + Chromium, own two-gate Playwright skip preamble. |
| `EXPECTED_CHECK_COUNT` in that file | NEW module-level constant | One counter per file (Phase-4 D-07 convention). |
| `main()` in that file | NEW module-level function | Same shape as every other browser harness in this phase. |
| `QUIET_DIAL_SEL`, `QUIET_ARC_SEL`, `QUIET_HANDLES_SEL`, `QUIET_READOUT_SEL`, `_SETTLE_DIAL` | RELOCATED `main()`-local constants | Not new — relocated from `companion/test_browser_ux.py`'s `main()`. |
| `_quiet_hours_on_disk`, `_set_window`, `_wake_interval_on_disk`, `_set_interval` | RELOCATED `main()`-local closures | Close over the harness; they must be redefined inside the NEW file's `main()` over ITS harness, never imported. |
| 9 relocated check callables | RELOCATED | `_the_window_still_saves_with_scripts_blocked_through_the_dial`, `_dragging_and_keying_a_handle_reach_disk`, `_the_arc_the_handles_and_the_caption_agree_after_an_interaction`, `_the_dial_caption_keeps_its_form_after_every_interaction_kind`, `_the_dial_meets_its_floors_at_360px_in_both_themes`, `_the_dial_handle_stays_on_its_ring_for_the_whole_of_a_held_press`, `_the_interval_still_saves_with_scripts_blocked_through_the_slider`, `_dragging_and_keying_the_range_reach_disk`, `_the_slider_meets_its_floors_at_360px_in_both_themes`. |

No orchestration or coverage-config change here — that is plan 04.
No new application symbol, route, endpoint, env var or config key.

<tasks>

<task type="auto">
  <name>Task 1: Create companion/test_browser_ux_quiet_wake.py with the 9 dial and slider checks moved verbatim</name>
  <files>companion/test_browser_ux_quiet_wake.py</files>
  <read_first>
    - `companion/test_browser_ux_health_drawings.py` (created by plan 02) — the finished, working precedent for exactly this task. Its `main()` skeleton, its import block shape, its module docstring shape and its skip-gate wording are what this file copies. Reading it is faster and safer than re-deriving the pattern from the parent.
    - `companion/test_browser_ux.py`, the block to move, anchored by content: it starts at the `# ---` rule line above the banner reading `25-04-PLAN.md Task 4 (CFG-48): D17's quiet-hours dial, measured at 360px, from the keyboard, and with its fallback proven by SAVING rather than by rendering` and ends at the line `_the_slider_meets_its_floors_at_360px_in_both_themes)`. The next banner after it reads `25-06-PLAN.md Tasks 1 and 4 (CFG-50): D5's theme carousel` and is NOT part of this group. Pre-plan-01 coordinates were 11444-13389; plan 01 and plan 02 have both shifted every number, so anchor on the banner text.
    - Inside that block, specifically read the fixture cluster at its head: the four selector constants, then `_quiet_hours_on_disk()` (loads the device config from `harness.tmpdir` and returns the stored start/end pair), then `_set_window()` (drives both fields through the real UI, commits each with `_commit_field`, waits for the save bar and saves via it — with an idempotence guard that returns early when disk already matches, because under the save bar disk only moves on an explicit Save and a no-difference fill never reveals the bar), then `_SETTLE_DIAL`. Further down read the matching `_wake_interval_on_disk()` and `_set_interval()` pair.
    - `companion/test_browser_ux_helpers.py` — confirm `_quiet_arc_minutes`, `_quiet_caption_minutes`, `_quiet_caption_shape`, `_quiet_duration_span_text`, `_expected_quiet_duration_text`, `_fraction_to_minute`, `_fraction_pair_minutes`, `_QUIET_ARC_SELECTOR`, `_QUIET_READOUT_SELECTOR`, `_QUIET_DURATION_SELECTOR`, `MINUTES_PER_DAY` and `_handle_sel` are all exported there.
    - `.planning/phases/31-.../31-RESEARCH.md` "Helper Coupling" table — the row explaining why `_quiet_arc_minutes` and its decoder cluster had to be promoted rather than moved into this file.
  </read_first>
  <action>
Create `companion/test_browser_ux_quiet_wake.py` following
`companion/test_browser_ux_health_drawings.py` as the working precedent.

Give it a module docstring stating what it covers (the quiet-hours dial and the
wake-interval slider — the 25-04 and 25-05 widget family on the Display and
Device settings pages), that it was split out of `companion/test_browser_ux.py`
in phase 31 so the suite's worker pool can run it concurrently, that it drives a
real headless Chromium against a real `companion/app.py` subprocess, and that it
never constructs or navigates to any URL outside `Harness.base_url()`.

Copy the `sys.path` bootstrap and a pruned project import block, plus a
`from companion.test_browser_ux_helpers import (...)` line with the `# noqa: E402`
marker. Derive that list mechanically from the finished file's AST rather than by
eye. It will include the quiet-dial decoder cluster —`_quiet_arc_minutes`,
`_quiet_caption_minutes` and the constants they read — and `_handle_sel`, all of
which plan 01 placed in the shared module precisely so this file and the reduced
parent can each import one definition instead of duplicating it. `config_page`
from `companion.pages` is also needed, since the dial radius the arc decoder
reads comes from there.

Reproduce `main()` with the identical nesting: two skip gates returning 0 and
naming this file's own path, `results = []`, the `check(name, fn)` closure,
`harness = Harness()`, `seed_state_dir(harness.tmpdir)`, `harness.start()`, then
`try:` / `with sync_playwright() as p:` / `browser = p.chromium.launch()` /
`try:`. That puts the moved bodies at exactly 16 spaces — **move them with zero
reindentation**; if the text needs reindenting, the skeleton is wrong.

Move the whole block verbatim in file order, including its head fixture cluster.
`_quiet_hours_on_disk`, `_set_window`, `_wake_interval_on_disk` and `_set_interval`
are closures over `harness` and must be redefined inside the new file's `main()`
so they close over ITS harness — do not try to import them and do not lift them
to module level. The nine callables passed to `check()` are, in file order:
`_the_window_still_saves_with_scripts_blocked_through_the_dial`,
`_dragging_and_keying_a_handle_reach_disk`,
`_the_arc_the_handles_and_the_caption_agree_after_an_interaction`,
`_the_dial_caption_keeps_its_form_after_every_interaction_kind`,
`_the_dial_meets_its_floors_at_360px_in_both_themes`,
`_the_dial_handle_stays_on_its_ring_for_the_whole_of_a_held_press`,
`_the_interval_still_saves_with_scripts_blocked_through_the_slider`,
`_dragging_and_keying_the_range_reach_disk`,
`_the_slider_meets_its_floors_at_360px_in_both_themes`.

Preserve every comment verbatim — several of them record measured browser
behaviour that is not obvious from the code, notably the note that Playwright's
own `.fill()` dispatches only an `input` event and never the `change` event the
save bar's document-level listener needs, which is why `_commit_field()` exists.
Deleting or paraphrasing that comment invites a future reader to "simplify"
`_set_window` back into a broken shape.

This group is the one RESEARCH.md flagged as most likely to expose a hidden state
dependency, because several of its checks deliberately mutate persisted config
and read it back, and one of them explicitly restores what it changed before
returning. In the monolith those checks ran after roughly sixty earlier checks
had already moved the shared fixture around; here they start from a fresh seed.
Expect that to be MORE correct, not less — but expect it to be where a surprise
shows up, and treat task 3's diff as the arbiter.

Set `EXPECTED_CHECK_COUNT` by RUNNING the file and reading the printed tally, not
by counting `check(` occurrences.
  </action>
  <verify>
    <automated>server/.venv/bin/ruff check companion/test_browser_ux_quiet_wake.py &amp;&amp; server/.venv/bin/python3 companion/test_browser_ux_quiet_wake.py > /tmp/31-03-new.txt 2>&amp;1; echo "exit=$?"; grep -cE '^PASS ' /tmp/31-03-new.txt; grep -cE '^FAIL ' /tmp/31-03-new.txt; grep -E 'checks pass$' /tmp/31-03-new.txt; server/.venv/bin/python3 -c "import ast; t=ast.parse(open('companion/test_browser_ux_quiet_wake.py').read()); v=[s.value.value for s in t.body if isinstance(s,ast.Assign) and getattr(s.targets[0],'id','')=='EXPECTED_CHECK_COUNT']; assert len(v)==1 and v[0]==9, v; assert any(getattr(n,'name','')=='main' for n in t.body); print('shape-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_browser_ux_quiet_wake.py` exits 0, prints 9 lines beginning with `PASS ` and zero beginning with `FAIL `.
    - Its printed tally reports `9/9 checks pass`.
    - Its AST has exactly one `EXPECTED_CHECK_COUNT` assignment with value 9, read off a real run; the SUMMARY quotes the run output that produced it.
    - `server/.venv/bin/ruff check companion/test_browser_ux_quiet_wake.py` exits 0.
    - Both SKIP messages name this file's own path: `grep -c 'test_browser_ux_quiet_wake.py — ' companion/test_browser_ux_quiet_wake.py` prints 2.
    - The skip path is exercised: running with a deliberately unavailable browser exits 0 and prints a line beginning with `SKIP `.
    - The file imports the quiet-dial decoder cluster rather than redefining it: `server/.venv/bin/python3 -c "import ast; t=ast.parse(open('companion/test_browser_ux_quiet_wake.py').read()); d={n.name for n in t.body if isinstance(n,ast.FunctionDef)}; assert '_quiet_arc_minutes' not in d and '_handle_sel' not in d; src=open('companion/test_browser_ux_quiet_wake.py').read(); assert 'from companion.test_browser_ux_helpers import' in src; print('imports-shared-helpers-ok')"` succeeds.
    - `git diff --stat companion/test_browser_ux.py` reports no change — the parent is reduced in task 2.
  </acceptance_criteria>
  <done>The second extracted harness exists, runs standalone at 9/9, lints clean, skips cleanly, and sources its shared decoders from the helper module.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the 9 moved checks from companion/test_browser_ux.py and re-derive its counter by running</name>
  <files>companion/test_browser_ux.py</files>
  <read_first>
    - `companion/test_browser_ux_quiet_wake.py` (as just written) — the authoritative record of what moved.
    - `companion/test_browser_ux.py`, the span between the `25-04-PLAN.md Task 4 (CFG-48)` banner and the `25-06-PLAN.md Tasks 1 and 4 (CFG-50): D5's theme carousel` banner — the exact region to delete.
    - `companion/test_browser_ux.py`, the settings dirty-bar audit check that calls both `_quiet_arc_minutes` and `_handle_sel` (pre-plan-01 lines 15073-15195, inside the un-splittable settings mega-cluster). This check STAYS and is the live proof that plan 01's helper promotion was necessary and correct. Read it so you can confirm after deletion that it still resolves both names through the import rather than through a definition that just left.
    - `companion/test_browser_ux.py` lines 111-996 — the `EXPECTED_CHECK_COUNT` changelog, which plan 02 has already appended to once. Append again; do not edit prior entries.
    - `.planning/phases/31-.../31-RESEARCH.md` Pitfall 1.
  </read_first>
  <action>
Delete from `companion/test_browser_ux.py` the exact span now living in
`companion/test_browser_ux_quiet_wake.py`: from the `# ---` rule line above the
`25-04-PLAN.md Task 4 (CFG-48)` banner through the line closing the last moved
`check(...)` call, plus the blank line separating it from the following banner.
The `25-06-PLAN.md Tasks 1 and 4 (CFG-50)` theme-carousel banner and everything
after it stays — that block is part of the settings mega-cluster D-04 explicitly
leaves for a possible follow-up phase.

Append a new `EXPECTED_CHECK_COUNT` assignment to the changelog, following the
file's established shape: a comment block naming phase 31 plan 03, naming the
destination file, and recording that the number was re-derived by running.

Determine the value by running the reduced file and reading its tally. The
expected number is 76 (85 minus the 9 that left, and 96 minus all 20 moved so
far). The number that goes in the file is the one the run printed; anything else
is a finding to report, not a number to accept.

After deletion, pay particular attention to the settings dirty-bar audit check
that calls `_quiet_arc_minutes` and `_handle_sel`. It must still pass. If it
raises `NameError`, the import line plan 01 added is missing one of those names
and must be corrected in the import list — not by copying a definition back into
this file, which would recreate the duplication the promotion existed to
prevent.

Do not touch `scripts/run_all_tests.py` or `pyproject.toml` in this task.
  </action>
  <verify>
    <automated>server/.venv/bin/ruff check companion/ &amp;&amp; server/.venv/bin/python3 companion/test_browser_ux.py > /tmp/31-03-reduced.txt 2>&amp;1; echo "exit=$?"; grep -cE '^PASS ' /tmp/31-03-reduced.txt; grep -cE '^FAIL ' /tmp/31-03-reduced.txt; grep -E 'checks pass$' /tmp/31-03-reduced.txt; server/.venv/bin/python3 -c "
import ast
g=lambda p:[s.value.value for s in ast.parse(open(p).read()).body if isinstance(s,ast.Assign) and getattr(s.targets[0],'id','')=='EXPECTED_CHECK_COUNT'][-1]
a=g('companion/test_browser_ux.py'); b=g('companion/test_browser_ux_health_drawings.py'); c=g('companion/test_browser_ux_quiet_wake.py')
assert a==76,(a,); assert a+b+c==96,(a,b,c); print('conserved',a,b,c)"</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_browser_ux.py` exits 0, prints 76 `PASS ` lines, zero `FAIL ` lines, and a tally reading `browser-ux: 76/76 checks pass`.
    - The last `EXPECTED_CHECK_COUNT` assignment in the file evaluates to 76, and that value matches the number the run printed (the SUMMARY quotes both).
    - A new comment block above that assignment names phase 31 plan 03, names the destination file, and records that the number was re-derived by running.
    - The three-file total is conserved at 96: the command above prints `conserved 76 11 9`.
    - `server/.venv/bin/ruff check companion/` exits 0.
    - The settings dirty-bar audit check that calls `_quiet_arc_minutes` and `_handle_sel` is among the 76 passing checks — proving plan 01's helper promotion held across the extraction. Confirm by name in the run transcript.
  </acceptance_criteria>
  <done>The parent harness is reduced to 76/76 with a run-derived counter, the 96-check total is conserved across three files, and the cross-group helper promotion is proven load-bearing.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no moved check changed verdict — diff all three files against the pre-split transcript</name>
  <files>.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-03-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/31-.../31-BASELINE-CHECKS.txt` — the pre-split 96-check transcript from plan 01 task 1.
    - `.planning/phases/31-.../31-02-SUMMARY.md` — plan 02's verdict on RESEARCH.md Assumption A3 and the comparison script it used, which this task extends from two files to three.
    - `.planning/phases/31-.../31-RESEARCH.md` Pitfall 2 and Assumptions Log row A3.
    - `.planning/phases/31-.../31-RESEARCH.md` Open Questions item 2 — the specific question this task answers for the quiet-hours group, which is the group most likely to surface a hidden dependency because several of its checks mutate persisted config and read it back.
  </read_first>
  <action>
Run all three harnesses and prove the two-stage split preserved every verdict.

Produce four sets of printed check-name lines: the 96 from
`31-BASELINE-CHECKS.txt`, the 11 from `companion/test_browser_ux_health_drawings.py`,
the 9 from `companion/test_browser_ux_quiet_wake.py`, and the 76 from the reduced
`companion/test_browser_ux.py`.

Three properties must hold. The union of the three post-split name sets equals
the baseline name set exactly. The three post-split sets are pairwise disjoint.
Every name's verdict is PASS everywhere it appears. Run this as a script — the
names are multi-sentence strings and a visual scan will miss a single-character
difference.

Then run all three files concurrently and confirm all three still pass. This is
the real contention the worker pool will create once plan 04 lands: three
Chromium instances and three `companion/app.py` subprocesses on three
OS-assigned ephemeral ports, plus a fourth concurrent slot for whatever else the
pool schedules. Proving it here rather than discovering it in CI is the point.

If any check that passed in the baseline now fails, treat it exactly as plan 02
did: make the check seed the state it needs, or widen the extraction boundary to
include whatever produced that state. Never delete the check and never adjust a
counter to absorb it.

Record in the SUMMARY: the 9 moved check names, the four-way comparison result,
the concurrent three-file run result, each file's standalone wall time, and a
final verdict on RESEARCH.md Assumption A3 and Open Question 2 now that both
mandatory extractions have landed.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_browser_ux_health_drawings.py > /tmp/31-03-hd.txt 2>&amp;1 &amp; P1=$!; server/.venv/bin/python3 companion/test_browser_ux_quiet_wake.py > /tmp/31-03-qw.txt 2>&amp;1 &amp; P2=$!; server/.venv/bin/python3 companion/test_browser_ux.py > /tmp/31-03-red.txt 2>&amp;1 &amp; P3=$!; wait $P1; R1=$?; wait $P2; R2=$?; wait $P3; R3=$?; echo "concurrent-exits=$R1/$R2/$R3"; python3 -c "
def names(p): return [l[5:].rstrip() for l in open(p) if l.startswith('PASS ')]
def fails(p): return [l.rstrip() for l in open(p) if l.startswith('FAIL ')]
base=names('.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-BASELINE-CHECKS.txt')
hd=names('/tmp/31-03-hd.txt'); qw=names('/tmp/31-03-qw.txt'); red=names('/tmp/31-03-red.txt')
for p in ('/tmp/31-03-hd.txt','/tmp/31-03-qw.txt','/tmp/31-03-red.txt'):
    assert not fails(p), (p, fails(p)[:2])
assert (len(base),len(hd),len(qw),len(red))==(96,11,9,76), (len(base),len(hd),len(qw),len(red))
assert set(base)==set(hd)|set(qw)|set(red), 'check-name set changed across the split'
assert not (set(hd)&set(qw)) and not (set(hd)&set(red)) and not (set(qw)&set(red)), 'a check appears in two files'
print('PITFALL-2-CLEAN: 96 == 11 + 9 + 76, names conserved, all PASS, three-way concurrent run green')
"</automated>
  </verify>
  <acceptance_criteria>
    - All three files run concurrently to exit 0 — no ephemeral-port, temp-dir or fixture collision under three-way contention.
    - The union of the three post-split PASS name sets equals the baseline's 96-name set exactly, and the three sets are pairwise disjoint. The command above prints `PITFALL-2-CLEAN`.
    - No post-split run emits any line beginning with `FAIL `.
    - The SUMMARY lists all 9 moved check names verbatim and gives a final PASS or FAIL verdict on RESEARCH.md Assumption A3 and Open Question 2.
    - The SUMMARY records each file's standalone wall time, giving plan 04 the per-file numbers it needs alongside the suite total.
    - If any divergence was found and fixed, the SUMMARY names the check, the state it depended on, and which permitted fix was applied.
  </acceptance_criteria>
  <done>Both mandatory extractions are empirically proven verdict-preserving under real concurrency, and RESEARCH.md's highest-risk assumption is settled with evidence rather than argument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright/Chromium → local HTTP server | `companion/test_browser_ux_quiet_wake.py` launches `companion/app.py` as its own subprocess on an OS-assigned ephemeral port and navigates exclusively to `Harness.base_url()` — a `127.0.0.1:<port>` origin the test process itself created. No external URL is constructed or navigated to. |
| Test fixture writer → temp state dir | This file's checks write quiet-hours and wake-interval values through the real UI into `harness.tmpdir`'s device config, then read them back from disk. All writes are to a `tempfile.mkdtemp()` directory owned by this process and destroyed by `cleanup()` — and now to a FRESH one rather than one 60 earlier checks already mutated, which is strictly more isolated than before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06 | Tampering | 9 relocated check bodies | low | accept | Relocation is verbatim with zero reindentation; task 3's four-way transcript comparison against `31-BASELINE-CHECKS.txt` fails loudly on any verdict change. |
| T-31-07 | Tampering | shared helper resolution for the staying dirty-bar audit | low | mitigate | `_quiet_arc_minutes` and `_handle_sel` resolve through the plan-01 import in the reduced parent, not through a definition that left with this block. Task 2's acceptance criteria require that audit check to be among the 76 passing, which is the direct proof. |
| T-31-08 | Denial of Service | three concurrent `Harness()` instances | low | mitigate | `Harness._pick_free_port()` binds `("127.0.0.1", 0)`; the OS never double-issues an in-use ephemeral port. Task 3 confirms empirically with a three-way concurrent run rather than resting on the argument. |
| T-31-09 | Information Disclosure | test fixture data | low | accept | Only synthetic device-config values are written, into per-process temp dirs. `TEST_PASSWORD` is set per-subprocess by `Harness.start()` and never leaves the local process tree. |

ASVS level 1, blocking on `high`. RESEARCH.md's Security Domain section found V2,
V3, V4, V5 and V6 all inapplicable: this plan changes which Python file a block of
test code lives in and touches no application code, endpoint, auth path or input
handler. A documented no-applicable-threats finding is the correct threat model
here, not an empty one. Nothing in this register is `high` or above, so nothing
blocks.
</threat_model>

<verification>
- `server/.venv/bin/ruff check .` exits 0.
- `companion/test_browser_ux_quiet_wake.py` runs standalone at 9/9, exit 0.
- `companion/test_browser_ux_health_drawings.py` still runs standalone at 11/11, exit 0.
- `companion/test_browser_ux.py` runs standalone at 76/76, exit 0.
- 11 + 9 + 76 = 96: no check lost or duplicated across the two-stage split.
- The three post-split PASS name sets are pairwise disjoint and their union equals the pre-split 96-name set.
- All three files run concurrently without collision.
- `git status --porcelain scripts/ pyproject.toml` reports no changes.
</verification>

<success_criteria>
- Both of D-04's mandatory extractions have landed: 20 of the original 96 checks now run in two additional processes.
- The quiet-dial decoder cluster and `_handle_sel` have exactly one definition each, in the shared helper module, imported by both consumers.
- All three `EXPECTED_CHECK_COUNT` values were derived by running, never by arithmetic.
- RESEARCH.md Pitfall 2, Assumption A3 and Open Question 2 all have empirical answers recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-03-SUMMARY.md` when done.
</output>