---
phase: 15-per-direction-themes-per-flight-colour-rules-and-roster-link
plan: 03
type: execute
wave: 2
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - server/poll_loop.py
  - server/test_poll_loop.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ONE effective-theme computation feeds both flight-displaying build_canvas() call sites in run_once() — the flight-detected branch and the held/source-fault/battery re-render branch that draws the same flight again from current_route (D-13)."
    - "The identical flight, across two run_once() calls differing only in battery state, produces the identical effective theme id — an override can never flip the panel's theme on a battery-icon repaint (D-13)."
    - "The four flight-less build_canvas() call sites — both empty states and both hold screens — never consult a rule or the arrivals override, even when one is configured that would otherwise match (D-09)."
    - "The rules registry is loaded exactly once per cycle, so a companion-side rule save landing mid-cycle can never split one rendered panel across two configurations."
    - "The resolver is never called before render_state and current_flight are settled — it is not hoisted beside the top-of-cycle base-theme read (RESEARCH Pitfall 1)."
  artifacts:
    - server/poll_loop.py
    - server/test_poll_loop.py
  key_links:
    - "colour_rules.set_colour_rules_state_dir(state_dir) must sit beside the existing illustrations/manual_resolutions priming calls, or resolve_effective_theme_id() reads an empty cache and every rule silently does nothing."
    - "run_once()'s result dict gains effective_theme — the observable the both-branches invariant test asserts on, and the only place the panel's actual theme is reported."
---

<objective>
Insert the D-13 resolution seam into `server/poll_loop.py`'s `run_once()`: one cache-priming call at the top, and one effective-theme computation feeding exactly the two `build_canvas()` call sites that display a flight — leaving the other four untouched.

Purpose: this is the phase's correctness core. `run_once()` has six `build_canvas()` call sites; two display a flight and two of the remaining four draw an empty state while two draw hold screens. An override applied in one flight-displaying branch and not the other would flip the panel's theme on a battery-icon repaint of the *same* aircraft — the exact failure the roadmap flagged before this phase was discussed.

Output: one modified poll-loop module, one extended integration harness.

Depends on 15-01 (the `colour_rules` module and its resolver) and 15-02 (the `theme_arriving` key the resolver reads off `device_cfg`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-CONTEXT.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-RESEARCH.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-PATTERNS.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-VALIDATION.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-01-SUMMARY.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the resolver into run_once()'s two flight-displaying render branches (D-13)</name>

  <files>server/poll_loop.py</files>

  <read_first>
    - `server/poll_loop.py` **lines 640-760** — `run_once()`'s docstring including its documented result-dict shape, the existing `illustrations.set_override_state_dir(state_dir)` / `manual_resolutions.set_manual_registry_state_dir(state_dir)` priming pair, and the once-per-cycle `device_cfg` / `theme_id` read.
    - `server/poll_loop.py` **lines 800-880** — the hold early-return branch: its `build_canvas()` call and its own result dict.
    - `server/poll_loop.py` **lines 900-1040** — where `current_flight`, `battery_low`/`battery_changed`, the pacing/promotion logic and `confirmed_state` are actually settled, plus the flight-detected branch's own empty-state `build_canvas()` call.
    - `server/poll_loop.py` **lines 1090-1250** — the flight-detected `build_canvas()` call, the held/re-render branch's two `build_canvas()` calls and the `source_fault != previous_source_fault or battery_changed or hold_exited` gate above them, and the nothing-ever-detected `build_canvas()` call.
    - `server/poll_loop.py` **lines 1300-1340** — the main result dict.
    - `server/plane/colour_rules.py` as delivered by plan 15-01 — `set_colour_rules_state_dir()` and `resolve_effective_theme_id()`'s exact signature and contract.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-RESEARCH.md` § Pattern 1 (the six-call-site table and the ordering trap) and § Pitfall 1.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-PATTERNS.md` § "`server/poll_loop.py` (resolver seam...)" — the same table with the two flagged lines.
  </read_first>

  <behavior>
    - With no rules and no arrivals override configured, every render in every branch uses the base `theme` exactly as it does today — the whole existing `server/test_poll_loop.py` and `server/test_pipeline_e2e.py` suites pass unchanged.
    - With an arrivals override configured and a detected arriving flight, the flight-detected render uses the override.
    - With an arrivals override configured and a detected departing flight, the flight-detected render uses the base theme.
    - With a matching rule configured, the flight-detected render uses the rule's theme regardless of direction, and regardless of whether an arrivals override is also set.
    - A flight rendered on one cycle and then re-rendered on a later cycle by the battery-transition path uses the **same** effective theme id on both cycles.
    - With a rule configured that would match nothing currently displayed, and with an arrivals override configured, both hold screens and both empty states still render with the bare base theme.
    - `run_once()`'s returned result dict carries `effective_theme`, equal to the base `theme` on any cycle that displayed no flight, and equal to the resolved id on any cycle that did.
  </behavior>

  <action>
Four edits to `server/poll_loop.py`. Every one is additive or a single-argument substitution; do not restructure any branch, do not move any existing statement, and do not change any branch's gating condition.

**1. Import and cache priming.** Add `colour_rules` to the existing `server.plane` import group. Then add exactly one line beside the existing `illustrations.set_override_state_dir(state_dir)` and `manual_resolutions.set_manual_registry_state_dir(state_dir)` priming pair: `colour_rules.set_colour_rules_state_dir(state_dir)`. This is the ONLY piece of this feature safe to place at the top of `run_once()` — loading a small JSON file unconditionally, even on cycles that never need it, is cheap and matches the established per-cycle-cache-priming precedent exactly. Comment it with the same reasoning the neighbouring calls already carry: a companion-side rule save landing mid-cycle must never split one rendered panel across two registries.

**2. The base default.** Immediately after the existing `theme_id = device_cfg["theme"]` read, add `effective_theme_id = theme_id`. Comment this as a **default assignment, not a resolution**: it exists so every branch — including the four that display no flight and the early returns — can reference the name without an `UnboundLocalError`, in exactly the way `unknown_prefix` and `event_recorded` are already predefined before any branching for the same reason. Add an explicit warning in that comment that the resolver itself must NOT be called here: `render_state` and `current_flight` are not settled at this point in the function (`render_state` is not known until `runway_config.infer_from_flight()` returns a non-`None` `confirmed_state`, or until `current_confirmed_state` is read back from `poll_state`; `current_flight` is not final until after the pacing/promotion logic), so a resolver call here would either raise or — worse, if written defensively — silently resolve against the previous cycle's stale values.

**3. The two flight-displaying call sites.** There are six `render.build_canvas(...)` call sites in `run_once()`. Exactly two of them display a flight and get the resolver; the other four keep `theme_id=theme_id` completely unchanged and must not be touched at all.

- The **flight-detected branch's** confirmed-state call — the one that passes `route=route` and `previous_state=previous_confirmed_state` (approximately line 1109). Immediately before it, on its own line, compute `effective_theme_id = colour_rules.resolve_effective_theme_id(render_state, current_flight, device_cfg)`. Change that call's `theme_id=theme_id` argument to `theme_id=effective_theme_id`.
- The **held/re-render branch's** confirmed-state call — the one assigned to `held_canvas` that passes `route=current_route`, inside the `if confirmed_state is not None:` arm of the `source_fault != previous_source_fault or battery_changed or hold_exited` gate (approximately line 1188). Immediately before it, on its own line, compute `effective_theme_id` with the identical call. Change that call's `theme_id=theme_id` argument to `theme_id=effective_theme_id`.

Write the two calls out explicitly rather than factoring them into a shared local closure: `run_once()` already repeats near-identical code across its branches (the history-recording calls, the poll-state saves), and this codebase's style favours explicit, greppable repetition over a closure inside an already-long function.

Add a short comment above each of the two calls naming it as one of D-13's two required application points, and naming the invariant the pair exists to hold: the same flight drawn again from `current_route` on a battery-icon or source-fault repaint must get the same theme it got on the cycle that first displayed it.

Leave untouched, with `theme_id=theme_id` exactly as it stands today: the hold early-return call, the flight-detected branch's own empty-state call, the held branch's empty-state call in the `else` arm, and the nothing-ever-detected call. Rules and the arrivals override are consulted only when a flight is displayed, never for an empty state or a hold screen (D-09).

**4. Report the effective theme (`15-CONTEXT.md`'s discretion note — taking the recommended option).** Add an `effective_theme` key to `run_once()`'s result dict, carrying `effective_theme_id`. Add the same key to the hold early-return's result dict, carrying `theme_id` there — a hold screen's effective theme *is* the base theme, and reporting it uniformly is what makes the key trustworthy. Leave the existing `theme` key alone in both dicts: it keeps meaning the configured base theme, which existing checks depend on. Update `run_once()`'s docstring where it enumerates the result dict's shape to name the new key and state the distinction between the two in one sentence. Also add `effective_theme_id` to the shared log statement at the bottom of the function, beside whatever it already reports for the theme, so an operator reading the service log can see what the panel actually rendered with.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_poll_loop.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "theme_id=effective_theme_id" server/poll_loop.py` returns exactly `2`.
    - `grep -c "theme_id=theme_id" server/poll_loop.py` returns exactly `4`.
    - `grep -c "colour_rules.resolve_effective_theme_id(render_state, current_flight, device_cfg)" server/poll_loop.py` returns exactly `2`.
    - `grep -c "colour_rules.set_colour_rules_state_dir(state_dir)" server/poll_loop.py` returns exactly `1`.
    - `grep -c "render.build_canvas(" server/poll_loop.py` returns exactly `6` — no call site was added or removed.
    - `grep -c "effective_theme" server/poll_loop.py` is at least `8`.
    - The priming call is before the resolver calls and the resolver calls are late in the function: `server/.venv/bin/python3 -c "s=open('server/poll_loop.py').read().splitlines(); prime=[i for i,l in enumerate(s) if 'set_colour_rules_state_dir' in l][0]; calls=[i for i,l in enumerate(s) if 'resolve_effective_theme_id(render_state' in l]; assert len(calls)==2 and prime < calls[0]; assert all(c > prime + 200 for c in calls), 'resolver hoisted too close to the top-of-cycle read'"` exits 0.
    - `server/.venv/bin/python3 server/test_poll_loop.py` exits 0 with its `EXPECTED_CHECK_COUNT` still at its pre-edit value, proving this task broke no existing poll-loop behaviour before Task 2 extends the ledger.
    - `server/.venv/bin/python3 server/test_pipeline_e2e.py` exits 0 — the end-to-end pipeline is unaffected when no rule and no override are configured.
    - `git diff --stat -- server/plane/render.py` is empty.
  </acceptance_criteria>

  <done>`run_once()` primes the rules cache once, resolves the effective theme at exactly the two flight-displaying `build_canvas()` call sites, leaves the other four passing the bare base theme, and reports `effective_theme` in both result dicts and in the log line.</done>
</task>

<task type="auto">
  <name>Task 2: Extend server/test_poll_loop.py with the both-branches invariant and the flight-less-sites check (15-VALIDATION.md rows 8 and 9)</name>

  <files>server/test_poll_loop.py</files>

  <read_first>
    - `server/test_poll_loop.py` — its module docstring's note about the process boundary between two separate `run_once()` invocations; `_write_battery_state(state_dir, mv)` (around line 302); `_snapshot(...)` and the `CLIMB`/`GEOFENCE_PATH` fixtures; the existing multi-cycle checks that drive `run_once()` twice against one persistent state dir (around lines 500-600 and 920-950); the comment around line 96 recording how the `set_manual_registry_state_dir()` call was covered when it was added — the closest precedent for testing a new per-cycle priming call; its live `EXPECTED_CHECK_COUNT` assignment and the terminating comparison.
    - `server/poll_loop.py` as modified in Task 1 — in particular the `source_fault != previous_source_fault or battery_changed or hold_exited` gate, which is how a test forces the held/re-render branch.
    - `server/plane/colour_rules.py` and `server/device_config.py` as delivered by plans 15-01 and 15-02 — the test writes rules via `add_rule()` and the override via `save_device_config()`, never by hand-writing JSON.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-VALIDATION.md` — the two Per-Task Verification Map rows whose Plan column reads `poll-loop`.
  </read_first>

  <action>
Extend `server/test_poll_loop.py` in place. Use the file's existing fixture helpers and its existing persistent-temp-dir idiom; add no new import beyond what the file already has, and do not convert anything to pytest.

Write these checks:

1. **The both-branches invariant (`15-VALIDATION.md` row 8) — the plan's headline proof.** In one persistent state dir: configure a rule via `colour_rules.add_rule()` that matches the fixture aircraft (use a `callsign` rule so the match is unambiguous), then drive `run_once()` once with a snapshot containing that aircraft so the flight-detected branch renders it, capturing the returned `effective_theme`. Then change only the battery state — write a `battery_state.json` whose millivolts cross the low threshold via `_write_battery_state()` — and drive `run_once()` again with a snapshot that detects nothing, so the held branch's battery-transition gate fires and re-renders the same flight from `current_route`. Assert the second call's `effective_theme` is identical to the first's, and assert it is the rule's theme rather than the base theme (so the check would still fail if both branches were wrong in the same direction). Name the check so a failure says plainly that a battery-icon repaint flipped the panel's theme.

2. **The four flight-less call sites (`15-VALIDATION.md` row 9).** With both a matching rule AND an arrivals override configured — configured such that either one, if wrongly consulted, would produce a theme different from the base — assert that a cycle which displays no flight reports `effective_theme` equal to the configured base `theme`. Cover the nothing-ever-detected case and the held-with-no-confirmed-state case as separate checks so a failure names which branch leaked. For the hold screens, drive a cycle under quiet hours (or display-off) with the same rule and override configured and assert the same equality against the hold early-return's result dict.

3. **Direction sensitivity through the real loop.** With no rule but an arrivals override set via `save_device_config()`, assert a detected arriving flight's cycle reports the override as `effective_theme` and a detected departing flight's cycle reports the base theme. This is the D-04 half of the seam proved end-to-end rather than only at the resolver's unit level.

4. **Per-cycle priming.** Assert that a rule added to the state dir *after* a `run_once()` call is picked up by the *next* `run_once()` call — proving the priming call runs every cycle rather than once per process, which is the failure mode a module-level cache invites.

Then raise the live `EXPECTED_CHECK_COUNT` by exactly the number of checks added and **re-derive it by running the harness**, not by arithmetic. Follow this file's existing ledger-comment style: previous value, delta, and that phase 15 plan 03 added them for D-13.

If the pre-existing macOS-versus-Linux `panel.bin` digest NOTE fires, confirm it is already present on a clean pre-Phase-14 checkout before treating it as anything to fix — it is an accepted environment-specific non-regression, not a Phase 15 gate failure (`15-RESEARCH.md` Pitfall 5).
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_poll_loop.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_poll_loop.py` exits 0 and prints an `N/N checks pass`-shaped line whose `N` is strictly greater than the pre-edit `EXPECTED_CHECK_COUNT`.
    - `grep -c "effective_theme" server/test_poll_loop.py` is at least `8`.
    - `grep -c "_write_battery_state" server/test_poll_loop.py` is at least `1` more than its pre-edit count (the both-branches invariant drives the battery transition).
    - `grep -c "colour_rules" server/test_poll_loop.py` is at least `4`.
    - `grep -c "total == EXPECTED_CHECK_COUNT" server/test_poll_loop.py` returns `1`.
    - Deliberately breaking the invariant proves the check is live: temporarily change one of the two `theme_id=effective_theme_id` arguments in `server/poll_loop.py` back to `theme_id=theme_id`, confirm the harness exits non-zero and that the failing check is the both-branches one, then restore it.
    - `scripts/run-all-tests.sh` exits 0 (the pre-existing digest NOTE, if present on a clean checkout, is not a failure).
  </acceptance_criteria>

  <done>The harness proves the both-branches invariant, proves the four flight-less call sites never consult a rule or the override, proves direction sensitivity end-to-end, and proves the registry is primed every cycle — and its ledger matches the new check count.</done>
</task>

</tasks>

## Artifacts this phase produces

**Owned by this plan (15-03):**

| Symbol | Kind | Location |
|---|---|---|
| `effective_theme_id` | local in `run_once()`, defaulted to the base theme and reassigned at exactly two call sites | `server/poll_loop.py` |
| `effective_theme` | new key in both of `run_once()`'s result dicts, and a new field in its log line | same |
| the `colour_rules.set_colour_rules_state_dir(state_dir)` priming call | new per-cycle call beside the two existing priming calls | same |

**Produced by sibling plans (reference only — do not create here):** the whole of `server/plane/colour_rules.py` (15-01); `device_config.CLEAR_THEME_ARRIVING`, `normalise_theme_arriving()`, the `theme_arriving` key (15-02); `config_page.ARRIVING_CHECKBOX_VALUE`, `THEME_ARRIVING_TOGGLE_ID`, `THEME_ARRIVING_CHECKBOX_LABEL`, `THEME_DIRECTION_LABEL`, `.theme-direction-label`, `[data-arrival-grid]` (15-04); the `FLASH_RULE_*` keys, `RULES_ADD_ROUTE`, `RULES_DELETE_ROUTE_PREFIX`, `RULES_DELETE_ROUTE_SUFFIX`, `_handle_rule_add()`, `_handle_rule_delete()`, the `colour_rules` ctx key, `.rule-add-form` (15-05).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `state_dir` JSON files (rules registry, device config) -> `run_once()` -> `render.build_canvas(theme_id=...)` | Operator-curated durable state decides what the physical panel renders on every cycle. |
| one poll cycle's in-memory cache -> two mutually exclusive render branches | The same displayed aircraft is drawn by two different code paths on different cycles; a divergence between them is invisible in any single-cycle test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Tampering (of displayed information) | the resolver's placement in `run_once()` | high | mitigate | Enforced by construction: the resolver is invoked at exactly the two call sites that display a flight, immediately before each `build_canvas()` call, never hoisted to a point where `render_state`/`current_flight` could be stale or `None`. The other four call sites keep the bare base theme, so a rule or the arrivals override can never leak onto a hold screen or an empty state and make the operator believe a rule is broken when it is working — or the reverse. Proved by Task 2's row-9 checks and pinned by the exact `theme_id=theme_id`-count-of-4 and `theme_id=effective_theme_id`-count-of-2 acceptance criteria, which fail if any call site drifts. |
| T-15-10 | Tampering (of displayed information) | divergence between the two flight-displaying branches | high | mitigate | Both branches call the identical function with the identically-shaped inputs, and `resolve_effective_theme_id()` is a pure function of `(state, flight.callsign, flight.hex, device_cfg, cached registry)` with no cycle-local mutable state. Proved by Task 2's both-branches invariant, which drives two real `run_once()` calls differing only in battery state and asserts the reported `effective_theme` is identical. |
| T-15-02 | Tampering / Repudiation | mid-cycle registry mutation | medium | mitigate | The registry is loaded exactly once per cycle by the priming call, so a companion-side add or delete landing between two branches of one cycle cannot split a single rendered panel across two configurations. Proved by Task 2's per-cycle-priming check, which additionally proves the cache is not stale across cycles. |
| T-15-05 | Tampering | a crafted theme id reaching `build_canvas()` | high | transfer | Fully mitigated upstream in `server/plane/colour_rules.py` (plan 15-01, T-15-05): the resolver re-checks membership against `device_config.THEMES` before returning, so this plan's call sites cannot receive an unregistered id. Transferred rather than re-implemented — a second membership test here would be the one place in the codebase that diverges from the established single-gate discipline. |
| T-15-SC | Tampering | package installs | n/a | accept | This plan installs zero packages. `15-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, and `./.claude/CLAUDE.md`'s stdlib-only server posture forbids adding one. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_poll_loop.py` exits 0.
- `server/.venv/bin/python3 server/test_pipeline_e2e.py` exits 0.
- `scripts/run-all-tests.sh` exits 0.
- **D-07 standing gate (checked in every plan of this phase):** `git diff --stat -- server/plane/render.py` is empty for this plan's commits, and `server/.venv/bin/python3 server/test_render.py` exits 0 with its `EXPECTED_CHECK_COUNT` unedited — `build_canvas()`'s signature and composition are untouched; only which id is passed in changed.
- `git diff --stat -- companion/` is empty for this plan's commits.
- No new third-party dependency: `git diff -- server/requirements.txt server/requirements-dev.txt` is empty.
</verification>

<success_criteria>
- `run_once()` calls `colour_rules.set_colour_rules_state_dir()` exactly once, at the top, beside the two existing priming calls.
- `resolve_effective_theme_id()` is called at exactly two places, both immediately before a `build_canvas()` call that displays a flight.
- Exactly four `build_canvas()` call sites still pass the bare base theme; the total call-site count is still six.
- The identical flight re-rendered by the battery-transition path gets the identical effective theme id.
- Both result dicts and the log line report `effective_theme`, and `theme` keeps its existing meaning.
</success_criteria>

<output>
Create `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-03-SUMMARY.md` when done.
</output>
