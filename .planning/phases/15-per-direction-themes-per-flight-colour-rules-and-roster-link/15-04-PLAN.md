---
phase: 15-per-direction-themes-per-flight-colour-rules-and-roster-link
plan: 04
type: execute
wave: 2
depends_on:
  - "15-02"
files_modified:
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/test_config_page.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A checkbox under the Theme group reveals a second, identical 18-chip grid for the arrivals theme, and unchecking it at save time genuinely CLEARS a previously-set override (D-05)."
    - "Both grids are always present in the HTML and the reveal is CSS-only, so the page keeps working with JavaScript disabled — the second grid is reachable and selectable without any script (D-05)."
    - "Both values travel in the one existing Settings form and its unified save bar; the all-or-nothing save_device_config() contract is untouched (D-05)."
    - "The second grid reuses the existing /theme-preview/{id}.png route as-is — previews are per theme id, never re-rendered per direction (D-05)."
    - "handle_post() branches on the CHECKBOX field, never on theme_arriving's presence, because the always-rendered second grid means theme_arriving is always submitted (RESEARCH Pitfall 3)."
  artifacts:
    - companion/pages/config_page.py
    - companion/static/style.css
    - companion/test_config_page.py
  key_links:
    - "The unchecked-checkbox path must reach device_config.CLEAR_THEME_ARRIVING, not None — None means carry forward, so a field that silently fails to clear would look exactly like a working feature until an operator tries to undo an override."
    - "#theme-arriving-toggle is an element id, not a class, because the :has() reveal selector must reference one specific control rather than a class three other checkboxes on this page already share."
---

<objective>
Extend the Settings page's Theme group with the arrivals-override control: a `settings-checkbox` that reveals a second, identical theme-chip grid (D-05), and the `handle_post()` path that makes unchecking it genuinely clear the override (D-04).

Purpose: this is where the clearable-field mechanic plan 15-02 built becomes an operator-visible feature. It is also this phase's only new CSS and its only no-JS-correctness surface — the second grid is always in the DOM and hidden by a `:has()` rule, so a browser with scripting disabled sees a denser page, never a broken one.

Output: one extended page module, one extended stylesheet, one extended harness.

Depends on 15-02 (`CLEAR_THEME_ARRIVING` and the `theme_arriving` config key). Independent of 15-01 and 15-03 — no rules code is touched here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-CONTEXT.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-RESEARCH.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-PATTERNS.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-UI-SPEC.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-VALIDATION.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the arrivals checkbox and the second chip grid to the Theme group, plus their CSS (D-05)</name>

  <files>companion/pages/config_page.py, companion/static/style.css</files>

  <read_first>
    - `companion/pages/config_page.py` lines 26-230 (the route/constant/copy block, including `THEME_PREVIEW_ROUTE_PREFIX`, `THEME_PREVIEW_ALT_TEMPLATE`, `LED_CHECKBOX_VALUE`/`QUIET_HOURS_CHECKBOX_VALUE`/`DISPLAY_CHECKBOX_VALUE`, `THEME_SECTION_CAPTION`, `DIRTY_SECTION_ATTR`) and `_palette_hex()`.
    - `companion/pages/config_page.py` lines 241-362 — `theme_fieldset()` in full: its single-theme read-only branch, its `caption_html`, the per-`THEME_IDS` chip `<label>` loop, and the `.theme-status` wrapper it returns.
    - `companion/pages/config_page.py` lines 521-576 (`quiet_hours_group()`) and `display_group()` — the `settings-checkbox` idiom and the absent-means-off submission semantics, verbatim.
    - `companion/pages/config_page.py` lines 885-992 — `render()`'s per-field `device_cfg.get(...)` reads and its returned section-assembly tuple.
    - `companion/static/style.css` — its header comment's accent-reservation list (this phase adds **zero** entries to it); the `.theme-chip-grid` / `.theme-chip` / `.theme-status` rules; the existing `@supports selector(:has(*))` block and the comment above it explaining the progressive-enhancement contract; `.text-label` and `.section-caption`.
    - `.claude/skills/sketch-findings-skypane/references/settings-page-patterns.md` — the one-caption-per-section rule (the Theme group keeps its single existing caption; the checkbox and the grid label are not new captions).
    - `.claude/skills/sketch-findings-skypane/references/control-density.md` — the touch-target floor register and the `:has()`-keyed selected-card mechanism the chips already use.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-UI-SPEC.md` § Section Anatomy §1, § Reveal Mechanism, § Copywriting Contract (Theme group additions), § Control Density, § Accessibility Floors.
  </read_first>

  <action>
**Copy constants** (locked-English module constants, defined once, exactly as `15-UI-SPEC.md`'s Copywriting Contract states them — do not paraphrase):
- `THEME_ARRIVING_CHECKBOX_LABEL = "Use a different theme for arrivals"`
- `THEME_DIRECTION_LABEL = "Arrivals theme"`
- `ARRIVING_CHECKBOX_VALUE = "on"` — comment it exactly the way `QUIET_HOURS_CHECKBOX_VALUE` and `DISPLAY_CHECKBOX_VALUE` are already commented, as a fourth consumer of the same absent-means-off idiom.
- `THEME_ARRIVING_TOGGLE_ID = "theme-arriving-toggle"` — comment that this is an element `id` rather than a class because the CSS reveal selector must reference this one specific control, unlike the three existing checkboxes which need no `id`.
- `ARRIVAL_GRID_ATTR = "data-arrival-grid"` — the attribute-as-CSS-hook, following this app's existing `data-dirty-section` / `data-static-save-fallback` / `data-filter-group` convention.

`THEME_SECTION_CAPTION` stays byte-for-byte unchanged: it already frames the departing/arriving split, and the one-caption-per-section rule means the Theme group gains no second caption.

**Extract the chip grid into a shared helper.** Factor the existing per-`THEME_IDS` chip `<label>` loop out of `theme_fieldset()` into a module-level helper taking the radio group's field name, the currently-selected theme id, and an optional extra grid class plus an optional extra grid attribute; it returns the complete `<div class="theme-chip-grid ...">` element. `theme_fieldset()` then calls it twice. The two grids must be byte-identical apart from the radio `name`, the computed `checked`/`--selected` state, and the second grid's extra class and attribute — extracting rather than copy-pasting is what guarantees they cannot drift, and it keeps the chips' hidden-radio selectable-card idiom, their `/theme-preview/{id}.png` sources, their `_palette_hex()` dots and their check glyph as one definition. Do not change the emitted markup for the existing grid in any way: the first grid must still render exactly `<div class="theme-chip-grid">` with `name="theme"` radios.

**Extend `theme_fieldset()`.** Change its signature to also take the current `theme_arriving` value (an id or `None`). Leave the single-theme read-only branch untouched — it is a `len(device_config.THEME_IDS) == 1` guard and gains nothing here. In the multi-theme branch, after the existing first grid and still **inside the same `.theme-status` card** (not a new section, not a new `<div>` wrapper of its own), append in this order:
1. The checkbox, using the `settings-checkbox` label idiom exactly as `quiet_hours_group()` uses it — a native `<input type="checkbox">` nested inside `<label class="settings-checkbox">`, carrying `name="theme_arriving_enabled"`, `value=ARRIVING_CHECKBOX_VALUE`, `id=THEME_ARRIVING_TOGGLE_ID`, and `checked` when the passed `theme_arriving` is not `None`. Label text is `THEME_ARRIVING_CHECKBOX_LABEL`, escaped.
2. A `<p class="text-label theme-direction-label">` carrying `THEME_DIRECTION_LABEL`, escaped.
3. The second grid, via the shared helper: field name `theme_arriving`, extra class `theme-chip-grid--arrivals`, extra attribute `ARRIVAL_GRID_ATTR`, and a selected id computed as the **effective** arrivals theme — the passed `theme_arriving` when it is set, otherwise the same `current_theme_id` the first grid has selected, so an operator who ticks the box starts from the theme already in use rather than from nothing.

Add a comment above the label explaining why only the revealed grid carries one and the first does not: the `<h2>` heading plus the checkbox's own wording already disambiguate the first grid as the default/departures one, and a second "Departures" label would be an extra line of chrome the checkbox's wording makes redundant.

**Wire `render()`.** Read `current_theme_arriving` from `device_cfg` with `.get("theme_arriving")` — an explicit `.get()` with no `or` fallback and no default, because `None` is a meaningful value here (no override), exactly the reasoning the `current_wake_interval_s` read above it already carries. Pass it as `theme_fieldset()`'s new argument. Change nothing else in `render()` — the section order, the form, the Poll section and the dirty bar all stay exactly as they are.

**CSS (`companion/static/style.css`).** Two additions, both placed with the existing theme-chip rules, each preceded by a comment naming phase 15 D-05 as its reason:
1. `.theme-direction-label` — the "Arrivals theme" line. It composes the existing Label voice verbatim: 12px, `--font-ui`, `--weight-semibold`, uppercase, `0.06em` letter-spacing, the file's existing 70% `color-mix` muted strength — the same declaration set `.data-table th` and `.stat-tile__caption` already carry. Write the declarations out rather than adding this selector to any existing rule's selector list, matching how those six label-voice selectors are already written out separately. Add no new size, weight, family or colour token.
2. An `@supports selector(:has(*))` block hiding both the `[data-arrival-grid]` element and the `.theme-direction-label` line when the toggle is present and unchecked, scoped through `.theme-status:has(#theme-arriving-toggle)` so the attribute selector can never match an unrelated future component. Use a plain `display: none` — no transition, no animation, so `prefers-reduced-motion` is not implicated. The comment above it must state the progressive-enhancement contract explicitly: both grids are always rendered, and a browser without `:has()` support simply always shows both — denser, never broken, never dependent on script.

Add **no** new accent use, no destructive-action styling, no new spacing token, no new form-control CSS: the checkbox gets the site-wide native `accent-color` for free, and the native `input`/`select` 44px floor and the `.settings-checkbox` 44px label are both already in place. This phase adds zero entries to the stylesheet header's accent-reservation list.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; from server import device_config as dc; h=c.theme_fieldset('white', None); assert h.count('theme-chip-grid') >= 2; assert 'name=\"theme_arriving\"' in h; assert 'name=\"theme_arriving_enabled\"' in h; assert 'theme-arriving-toggle' in h; assert 'data-arrival-grid' in h; assert h.count('/theme-preview/') == 2 * len(dc.THEME_IDS); assert '<div class=\"theme-chip-grid\">' in h; print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; from server import device_config as dc; h=c.theme_fieldset('white', None); assert h.count('/theme-preview/') == 2 * len(dc.THEME_IDS)"` exits 0 — the second grid renders all 18 chips and reuses the existing preview route with no per-direction variant.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; h=c.theme_fieldset('white', None); assert 'name=\"theme_arriving_enabled\"' in h; assert 'data-arrival-grid' in h; assert 'Use a different theme for arrivals' in h; assert 'Arrivals theme' in h"` exits 0.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; unchecked=c.theme_fieldset('white', None); checked=c.theme_fieldset('white', 'black'); import re; box=lambda h: re.search(r'<input[^>]*theme-arriving-toggle[^>]*>', h).group(0); assert 'checked' not in box(unchecked); assert 'checked' in box(checked)"` exits 0 — the checkbox's state follows whether an override is set.
    - `server/.venv/bin/python3 -c "import sys,re; sys.path.insert(0,'.'); from companion.pages import config_page as c; h=c.theme_fieldset('white', None); grid=h.split('data-arrival-grid')[1]; assert 'value=\"white\"' in grid and re.search(r'value=\"white\"[^>]*checked', grid)"` exits 0 — with no override set, the second grid pre-selects the base theme rather than nothing.
    - `grep -c "theme-direction-label" companion/static/style.css` is at least `2` (the standalone rule plus the reveal block).
    - `grep -c "data-arrival-grid" companion/static/style.css` is at least `1`.
    - `grep -c "theme-arriving-toggle" companion/static/style.css` is at least `2`.
    - `grep -c "@supports selector(:has(\*))" companion/static/style.css` is exactly one more than its pre-edit count.
    - The first grid's markup is unchanged: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; h=c.theme_fieldset('white', None); assert '<div class=\"theme-chip-grid\">' in h"` exits 0.
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 — no new colour was introduced, so the executable colour-separation contract still holds.
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with its `EXPECTED_CHECK_COUNT` still at its pre-edit value, OR fails only on count-shaped assertions that this task's new chips legitimately changed — in which case repair those counts in Task 3 and note each one in the summary.
  </acceptance_criteria>

  <done>The Theme group renders the checkbox, the "Arrivals theme" label and a second identical 18-chip grid inside its existing `.theme-status` card; the reveal is CSS-only and both grids are always in the DOM; the first grid's markup is byte-identical to before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the unchecked checkbox genuinely clear theme_arriving in handle_post() (D-04/D-05)</name>

  <files>companion/pages/config_page.py</files>

  <read_first>
    - `companion/pages/config_page.py` lines 994-1160 — `handle_post()` in full: its docstring's statement of the validation contract, the `submitted_theme not in device_config.THEME_IDS` membership test, the three existing three-way checkbox branches (absent -> `False`; equals its own `*_CHECKBOX_VALUE` -> `True`; anything else -> reject the whole submission), and the `try: device_config.save_device_config(...) except (ValueError, OSError): return FLASH_SAVE_FAILED` call.
    - `server/device_config.py` as delivered by plan 15-02 — `CLEAR_THEME_ARRIVING`'s exact semantics and `save_device_config()`'s new parameter.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-RESEARCH.md` § Pattern 2's `handle_post()` half, § Pitfall 2 and § Pitfall 3 — the two specific ways this task can be got wrong.
  </read_first>

  <behavior>
    - Submitting with the checkbox present at `ARRIVING_CHECKBOX_VALUE` and `theme_arriving` set to a valid theme id persists that id.
    - Submitting with the checkbox **absent** — the ordinary browser semantics for an unchecked box — clears a previously-set `theme_arriving` back to `None`, even though `theme_arriving` itself is still present in the form with a valid theme id (because the second grid is always rendered).
    - Submitting with the checkbox present but carrying a value other than `ARRIVING_CHECKBOX_VALUE` rejects the whole submission and returns the save-failure flash key, writing nothing — matching the three existing checkboxes' behaviour for a crafted value.
    - Submitting with `theme_arriving` set to a value outside `device_config.THEME_IDS` rejects the whole submission and returns the save-failure flash key, writing nothing — including a path-traversal-shaped and a SQL-shaped payload, matching the existing adversarial checks for `theme`.
    - Every other field's behaviour is unchanged: a partial-field post still carries the other settings forward, and a rejection still writes nothing at all.
  </behavior>

  <action>
Extend `handle_post()` with the two new fields, using the file's own membership-check-then-explicit-branch shape verbatim.

Read `theme_arriving_enabled` and `theme_arriving` off the form. Validate `theme_arriving` first with the same membership test the existing `theme` field uses — when it is not `None` and not a member of `device_config.THEME_IDS`, return `FLASH_SAVE_FAILED` immediately, writing nothing.

Then branch on the **checkbox field**, following the three existing checkboxes' three-way shape: absent means unchecked; equal to `ARRIVING_CHECKBOX_VALUE` means checked; anything else is a crafted value and rejects the whole submission with `FLASH_SAVE_FAILED`.

The value handed to `save_device_config()` is the already-membership-checked `theme_arriving` when the box was checked, and `device_config.CLEAR_THEME_ARRIVING` when it was not. Add the resulting value as one extra keyword argument to the existing `save_device_config()` call inside the existing `try`/`except (ValueError, OSError)` block — do not add a second call, a second `try`, or any new exception type. The all-or-nothing rejection contract is unchanged.

Add a comment at the branch stating the trap it exists to avoid, because this is the one place in this file where the obvious reading is wrong: because D-05 requires the second grid to be always rendered in the HTML for no-JS correctness, `theme_arriving` is essentially **always** present in a real browser submission with a valid theme id — so a branch keyed on that field's presence or absence would never fire the clear path. The checkbox is the only signal that distinguishes set from clear. Add a second sentence naming why `None` is not the clear value: `None` already means "not supplied, carry forward" for every parameter of `save_device_config()` including this one, so passing it here would make a partial-field save silently wipe a previously-set override.

Update `handle_post()`'s docstring to name the two new form fields and to state the clear-versus-carry-forward distinction in one sentence, matching how it already documents the other checkbox fields.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>

  <acceptance_criteria>
    - **The clearable contract, proved end to end** (`15-VALIDATION.md` row 7 — this is the criterion the whole plan exists for): `server/.venv/bin/python3 -c "import sys,tempfile; sys.path.insert(0,'.'); from companion.pages import config_page as c; from server import device_config as dc; d=tempfile.mkdtemp(); ctx={'state_dir':d}; c.handle_post({'theme':'white','tracked_runway':dc.DEFAULT_RUNWAY_ID,'theme_arriving_enabled':'on','theme_arriving':'black'}, ctx); assert dc.load_device_config(d)['theme_arriving']=='black'; c.handle_post({'theme':'white','tracked_runway':dc.DEFAULT_RUNWAY_ID,'theme_arriving':'black'}, ctx); assert dc.load_device_config(d)['theme_arriving'] is None, 'unchecked checkbox failed to clear the override'; print('ok')"` exits 0. Adjust the `ctx`/form shape to whatever `handle_post()` actually takes as read in `<read_first>`; the assertion itself must not be weakened.
    - `grep -c "CLEAR_THEME_ARRIVING" companion/pages/config_page.py` is at least `1`.
    - `grep -c "ARRIVING_CHECKBOX_VALUE" companion/pages/config_page.py` is at least `2` (the constant's definition in Task 1 and its use in the branch).
    - `grep -c "theme_arriving=" companion/pages/config_page.py` is at least `1` — the single extra keyword argument on the existing save call.
    - `grep -c "save_device_config(" companion/pages/config_page.py` is unchanged from its pre-edit value — exactly one call, no second call added.
    - A crafted checkbox value rejects the whole submission and writes nothing: assert `handle_post()` returns `FLASH_SAVE_FAILED` for a `theme_arriving_enabled` of `"yes"` and that `device_config.json`'s bytes are unchanged afterwards.
    - A non-member `theme_arriving` rejects the whole submission and writes nothing: same assertion shape, for `"chartreuse"` and for a path-traversal-shaped payload.
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0.
  </acceptance_criteria>

  <done>An unchecked arrivals checkbox clears `theme_arriving` to `None`; a checked one persists the submitted id; a crafted checkbox value or a non-member theme id rejects the whole submission writing nothing; no other field's behaviour changed.</done>
</task>

<task type="auto">
  <name>Task 3: Extend companion/test_config_page.py with the arrivals-override checks (15-VALIDATION.md row 7)</name>

  <files>companion/test_config_page.py</files>

  <read_first>
    - `companion/test_config_page.py` — its module docstring, its `check()` helper, the existing theme-chip-grid markup checks and any count-shaped assertion over `THEME_IDS` (Task 1 doubled the chip count, so some of these legitimately need repair), the existing `handle_post()` membership-validation and adversarial-payload checks, the existing end-to-end HTTP round-trip check (the machinery a raw-POST no-JS check reuses), its live `EXPECTED_CHECK_COUNT` assignment and the terminating comparison.
    - `companion/pages/config_page.py` as modified in Tasks 1 and 2.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-VALIDATION.md` — the Per-Task Verification Map row whose Plan column reads `config` and whose Automated Command is this harness.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-UI-SPEC.md` § Copywriting Contract — the two new strings must be asserted escaped-verbatim, matching how this harness already pins the existing helper texts.
  </read_first>

  <action>
First, repair any existing count-shaped assertion that Task 1's second grid legitimately changed — for instance a check asserting the rendered Settings page contains exactly `len(THEME_IDS)` preview images or exactly one `.theme-chip-grid`. Repair means updating the expected number and appending a short comment naming phase 15 plan 04 as the reason, never loosening the assertion into a range or an inequality. If a repaired check no longer distinguishes the two grids, split it into one assertion per grid instead.

Then add one `check()` per behaviour in Task 2's `<behavior>` block, plus these:

1. **Markup checks.** The rendered Settings page contains both grids; the second carries `name="theme_arriving"` radios and the `data-arrival-grid` attribute; both new copy strings appear escaped-verbatim; the checkbox carries the `theme-arriving-toggle` id and the `settings-checkbox` label class; the total preview-image count is exactly twice `len(device_config.THEME_IDS)`.
2. **Pre-selection checks.** With no override stored, the second grid's checked radio is the base theme and the checkbox is unchecked. With an override stored, the second grid's checked radio is the override and the checkbox is checked.
3. **The clearable contract (`15-VALIDATION.md` row 7).** Its own named check, asserting the full sequence: save with the box checked and a chosen arrivals theme, confirm it persisted; save again with the checkbox key simply absent from the form and `theme_arriving` still present with a valid id, confirm `theme_arriving` came back `None`; confirm every other setting survived that save unchanged. Name the check so a failure says plainly that unchecking the box failed to clear the override.
4. **The no-JS Settings-form half (`15-VALIDATION.md` row 11, the part this plan owns).** Using this file's existing end-to-end HTTP round-trip machinery, POST a raw URL-encoded body to the Settings route — no client script involved — once with the checkbox key present and once with it absent, and assert the persisted `theme_arriving` is the chosen id and then `None`. This is the automated half of the no-JS verification; the browser half is this phase's closing manual check.

Then raise the live `EXPECTED_CHECK_COUNT` by exactly the net number of checks added and **re-derive it by running the harness**, not by arithmetic. Follow this file's existing ledger-comment style: previous value, delta, and that phase 15 plan 04 added them for D-04/D-05.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 and prints an `N/N checks pass`-shaped line whose `N` is strictly greater than the pre-edit `EXPECTED_CHECK_COUNT`.
    - `grep -c "theme_arriving" companion/test_config_page.py` is at least `12`.
    - `grep -c "data-arrival-grid" companion/test_config_page.py` is at least `1`.
    - `grep -c "Use a different theme for arrivals" companion/test_config_page.py` is at least `1` and `grep -c "Arrivals theme" companion/test_config_page.py` is at least `1` — both copy strings pinned escaped-verbatim.
    - `grep -c "total == EXPECTED_CHECK_COUNT" companion/test_config_page.py` returns `1`.
    - Deliberately breaking the clear path proves the check is live: temporarily change Task 2's unchecked branch to pass `None` instead of `device_config.CLEAR_THEME_ARRIVING`, confirm the harness exits non-zero and that the failing check is the clearable-contract one, then restore it.
    - `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <done>The harness pins both grids' markup, both copy strings, the checkbox's pre-selection states, the clearable contract as its own named check, and the raw-HTTP-POST no-JS path for the Settings form — and its ledger matches the new check count.</done>
</task>

</tasks>

## Artifacts this phase produces

**Owned by this plan (15-04):**

| Symbol | Kind | Location |
|---|---|---|
| `THEME_ARRIVING_CHECKBOX_LABEL` | copy constant `"Use a different theme for arrivals"` | `companion/pages/config_page.py` |
| `THEME_DIRECTION_LABEL` | copy constant `"Arrivals theme"` | same |
| `ARRIVING_CHECKBOX_VALUE` | constant `"on"` | same |
| `THEME_ARRIVING_TOGGLE_ID` | constant `"theme-arriving-toggle"` | same |
| `ARRIVAL_GRID_ATTR` | constant `"data-arrival-grid"` | same |
| the shared theme-chip-grid helper | new module-level function called twice by `theme_fieldset()` | same |
| `theme_fieldset(current_theme_id, current_theme_arriving)` | changed signature (second parameter added) | same |
| `theme_arriving_enabled` / `theme_arriving` | new Settings form fields | same (markup + `handle_post()`) |
| `.theme-direction-label` | new CSS class | `companion/static/style.css` |
| `.theme-chip-grid--arrivals` | new CSS modifier class (grid hook, no new declarations required beyond the reveal) | same |
| `[data-arrival-grid]` | new attribute-as-CSS-hook | same |
| `#theme-arriving-toggle` | new element id referenced by the `:has()` reveal selector | same |

**Produced by sibling plans (reference only — do not create here):** the whole of `server/plane/colour_rules.py` (15-01); `device_config.CLEAR_THEME_ARRIVING`, `normalise_theme_arriving()`, the `theme_arriving` config key (15-02); `poll_loop`'s `effective_theme_id` and the `effective_theme` result key (15-03); `config_page.RULES_ADD_ROUTE`, `RULES_DELETE_ROUTE_PREFIX`, `RULES_DELETE_ROUTE_SUFFIX`, `RULES_SECTION_HEADING`, `RULES_SECTION_CAPTION`, `RULE_VALUE_HINT`, `RULES_EMPTY_HEADING`, `RULES_EMPTY_BODY`, `RULE_HEADERS`, `RULE_KIND_LABELS`, the seven `FLASH_RULE_*` keys, `app.Handler._handle_rule_add()` / `_handle_rule_delete()`, the `colour_rules` ctx key, `.rule-add-form`, `.rule-add-form__field` (15-05).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated operator -> `POST /settings` -> `handle_post()` -> `save_device_config()` | Two new form fields cross here and become durable config the poll loop reads on every arrival render. |
| a crafted request body (no browser, no client script) -> `handle_post()` | The form's own `required`/checkbox semantics are browser conveniences, not controls — any field can be absent, duplicated, or carry an arbitrary value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Tampering | `handle_post()`'s `theme_arriving` validation | high | mitigate | Membership test against `device_config.THEME_IDS` before the value reaches `save_device_config()`, returning `FLASH_SAVE_FAILED` and writing nothing — the same gate the existing `theme` field already uses, including its adversarial path-traversal and SQL-shaped payload coverage. `save_device_config()`'s own `ValueError` is the second, independent line (plan 15-02). Proved by Task 2's rejection criteria, which assert the config file's bytes are unchanged after each rejection. |
| T-15-11 | Tampering | `handle_post()`'s `theme_arriving_enabled` branch | medium | mitigate | Three-way explicit branch matching the three existing checkboxes: absent means off, the exact `ARRIVING_CHECKBOX_VALUE` means on, anything else rejects the whole submission. A crafted checkbox value therefore cannot produce a third, undefined behaviour. |
| T-15-09 | Tampering | the clear-versus-carry-forward decision | medium | mitigate | The branch keys off the checkbox field alone and passes `CLEAR_THEME_ARRIVING`, never `None`. Keying off `theme_arriving`'s presence would never fire the clear path in a real browser (the second grid is always rendered), and passing `None` would make any partial-field save silently wipe the override. Both failure modes are covered by Task 3's clearable-contract check and its deliberate-break criterion. |
| T-15-12 | Denial of Service (of function, not availability) | the CSS-only reveal | low | accept | A browser without `:has()` support renders both grids unconditionally. Accepted, not mitigated: the outcome is a denser page, never a broken or unsubmittable one, and both grids remain reachable, selectable and correctly submitted with scripting fully disabled. Adding a JavaScript fallback would introduce this codebase's first script dependency for form correctness, which `15-UI-SPEC.md` explicitly forbids. |
| T-15-SC | Tampering | package installs | n/a | accept | This plan installs zero packages, adds no component library, no CSS framework and no build step. `15-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable; `15-UI-SPEC.md` § Registry Safety records no registry of any kind is in use. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` exits 0.
- `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 — zero new colours, so the executable colour-separation contract is untouched.
- `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 — the label-voice selectors it pins string-equal are unaffected by the new `.theme-direction-label` rule.
- `scripts/run-all-tests.sh` exits 0.
- **D-07 standing gate (checked in every plan of this phase):** `git diff --stat -- server/plane/render.py` is empty for this plan's commits, and `server/.venv/bin/python3 server/test_render.py` exits 0 with its `EXPECTED_CHECK_COUNT` unedited.
- `git diff --stat -- server/` is empty for this plan's commits apart from nothing at all — this plan touches `companion/` only.
- Accent-reservation contract: `git diff -- companion/static/style.css` shows no change to the header comment's accent-reservation list. This phase adds zero entries to it.
</verification>

<success_criteria>
- The Theme group renders a `settings-checkbox`, an "Arrivals theme" label and a second identical 18-chip grid inside its existing `.theme-status` card, with the single existing `THEME_SECTION_CAPTION` unchanged.
- Both grids are always in the DOM; the reveal is a CSS-only `:has()` rule inside an `@supports` gate with no animation and no script.
- Unchecking the box at save time genuinely clears `theme_arriving`; checking it persists the chosen id; a crafted value in either field rejects the whole submission writing nothing.
- The second grid reuses `/theme-preview/{id}.png` unchanged — no per-direction preview variant exists.
- Zero new colours, zero new spacing or type tokens, zero new accent-reservation entries, zero new touch-target-register entries.
</success_criteria>

<output>
Create `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-04-SUMMARY.md` when done.
</output>
