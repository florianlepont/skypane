---
phase: 21
plan: 07
type: execute
wave: 5
depends_on: ["21-05"]
files_modified:
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/i18n_fr/calendar_group.py
  - companion/i18n_fr/display.py
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-21]

must_haves:
  truths:
    - "D-13: Calendar is ONE <section class=\"page-section\"> — status row, then the URL field and Connect button when not connected or the masked URL and the replace/disconnect line when connected, then the 'How it works' disclosure; there is no second Calendar page-section and no trailing disconnect card"
    - "D-13: the connect form still posts to POST /settings/calendar/connect and its input still renders with no value attribute in either state — the write-only contract is unchanged"
    - "R-10: once connected the card shows the feed URL's host plus '…', derived from a real urlsplit() parse, never a byte-offset truncation, and never the full token-bearing URL"
    - "D-14: 'Replace the feed URL' is a <details> whose <summary> is link-styled, its inner button reads 'Replace', and 'Disconnect' is a small grey button on the same line, right-aligned, still carrying the data-confirm mechanism and still going through the existing confirm route"
    - "R-08: the two CSS fusion rules (.page-section:has(+ .calendar-disconnect-form) and .calendar-disconnect-form) are deleted, and the pinned @supports selector(:has(*)) block count is re-derived by running the harness"
    - "R-09: the small grey button is one new additive rule block on the bare <button> idiom (.calendar-disconnect-btn) — this codebase has no .btn/.btn--secondary/.btn--small family"
    - "The disconnect <form> is never nested inside another form: it renders as a data-only sibling with an id, and its button submits cross-DOM via form=\"…\""
  artifacts:
    - path: "companion/pages/config_page.py"
      provides: "the merged Calendar card and the masked-URL helper"
      contains: "calendar-masked-url"
    - path: "companion/static/style.css"
      provides: ".calendar-actions, .calendar-disconnect-btn, .calendar-masked-url, .text-link"
      contains: "calendar-disconnect-btn"
  key_links:
    - from: "companion/pages/config_page.py"
      to: "POST /settings/calendar/disconnect"
      via: "the cross-DOM form= attribute on the small grey button"
      pattern: "form=\"calendar-disconnect-form\"|CALENDAR_DISCONNECT_ROUTE"
    - from: "companion/pages/config_page.py"
      to: "the stored calendar URL"
      via: "a host-only mask derived from urlsplit()"
      pattern: "urlsplit\\("
---

<objective>
Fuse the Calendar status card, its connect form and its disconnect form into one
tile: status, then either the URL field with "Connect calendar" or the masked
feed URL with a quiet "Replace the feed URL" link and a small grey "Disconnect"
button, then "How it works".

Purpose: the developer's sixth and seventh feedback points, with a photo of what
the connected state should look like. Today these are three separate render calls
glued at the edges by a `:has()` CSS trick; D-13/D-14 want one real card, which
means retiring that trick rather than building on it (Pitfall 2).
Output: one card builder, a host-only URL mask that can never leak a token, the
small-button and layout CSS, the French copy, and a re-derived `:has()` block-count
pin.
</objective>

<decisions_covered>
D-13, D-14, R-08, R-09, R-10, R-14 (this is the sequenced-after-B half: plan
21-05 rewrote the theme machinery in `config_page.py` in wave 4, this plan
rewrites the adjacent calendar slice in wave 5 — never in parallel).
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-PATTERNS.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan is the only plan in wave 5 and owns every file in `files_modified`.
It must NOT touch `companion/app.py`, `companion/layout.py`, any other page
module, any other static script, `companion/i18n_fr/__init__.py`,
`companion/test_i18n.py` (run it, never edit it) or `scripts/run_all_tests.py`.
Inside `config_page.py` it restructures ONLY the calendar slice — the Frame
colours card (plan 21-05) and the Frame strip call site (plan 21-04) are already
in place and must survive untouched.

</files_owned>

<interfaces>
Current sites, file:line as read on 2026-09-12 (re-verify before editing — plans
21-01, 21-04 and 21-05 have since edited this file):

  config_page.py:2143-2292   calendar_group(configured, drift, last_synced_at, …)
      — heading, caption, the `layout.status_row("", verdict, detail, state)` line
      (:2244) and the "How it works" <details> (:2263-2273), wrapped in .page-section.
      Its compact chip grid was already deleted by plan 21-05 (D-06).
  config_page.py:2295-2369   calendar_connect_section(configured, errors=None)
      — its own <form action="/settings/calendar/connect">, the write-only URL field
      whose docstring (:2311-2320) states nothing is ever derived from the stored URL
      for display, and the <details><summary>Replace the feed URL</summary> wrap
      when configured (:2363-2366)
  config_page.py:2372-2418   calendar_disconnect_section(configured, drift)
      — its own <form class="calendar-disconnect-form"> with data-confirm /
      data-confirm-value / the hidden confirm field (:2404-2418)
  config_page.py:3327-3399   render()'s Display assembly, which emits all three
      as separate siblings today
  config_page.py:654-787     the CALENDAR_* constants
  style.css:2193-2197        .page-section:has(+ .calendar-disconnect-form) { … }   — DELETE
  style.css:5369-5381        .calendar-disconnect-form { … }                        — DELETE
  test_config_page.py ~3712-3760  the pinned `@supports selector(:has(*)) {` block
      count (`supports_marker`, `_rule_body(".theme-chip:has(input:checked) {")`),
      with its history comments at ~172 and ~215 — RE-DERIVE it here (R-08)

Checks to retarget (21-PATTERNS.md Area C):
  _calendar_placement_after_display_before_form_close_with_dirty_attr (~4719)
  _calendar_group_no_inline_js_and_chip_grid_within_form (~4748)
  _calendar_connect_field_never_carries_value_in_either_state (~4872)
  _calendar_connect_wraps_in_details_only_when_configured (~4893)
  _calendar_containment_at_the_renderer_five_needles (~4913)
  _calendar_disconnect_checkbox_never_appears_in_calendar_group (~4945)
  _calendar_disconnect_section_appears_only_when_expected (~4963)
  _calendar_disconnect_form_is_not_inside_settings_form_on_display_scope (~5014)
  _calendar_connect_form_appears_before_the_runway_card_on_display_scope (~5036)
  _calendar_secret_never_reaches_render_function (~4543)
  _calendar_secret_never_reaches_served_http_bytes (~6325)

New copy (UI-SPEC Copywriting Contract §E). "Replace the feed URL" →
"Remplacer l'URL du flux" already exists in `companion/i18n_fr/calendar_group.py`.
Add to `companion/i18n_fr/calendar_group.py`:
  "Replace"    → "Remplacer"      (CALENDAR_REPLACE_BUTTON_TEXT, the connected-state
                                   button inside the disclosure — Structural Note 6)
  "Disconnect" → "Déconnecter"    (the short label for the new small grey button;
                                   the existing longer "Disconnect calendar" /
                                   "Disconnect this calendar…" strings stay exactly
                                   as they are for the confirmation page)
A key may live in only ONE i18n_fr module — `companion/i18n_fr/__init__.py` raises
on a duplicate. Check both modules before adding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge the three calendar builders into one card</name>
  <files>companion/pages/config_page.py, companion/i18n_fr/calendar_group.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py `calendar_group()`, `calendar_connect_section()`, `calendar_disconnect_section()`, `calendar_disconnect_confirm_page()` and `render()`'s Display assembly — each in full, as they stand after plans 21-04 and 21-05
    - .planning/phases/21-.../21-UI-SPEC.md §E: the full markup for both states, the cross-form Disconnect reasoning and the "one card" bullet list
    - .planning/phases/21-.../21-RESEARCH.md Area C.1-C.3 and Pitfall 2
    - companion/static/confirm-submit.js — enough to confirm the `data-confirm`/`data-confirm-value`/hidden-field mechanism works on a form whose submit button lives outside it via `form="…"`; if it does NOT, keep the button inside its own form and place that form inline instead, and record the deviation in the SUMMARY
    - companion/i18n_fr/calendar_group.py and companion/i18n_fr/display.py (to place the two new keys in exactly one module)
    - companion/test_config_page.py: every check listed in the interfaces block, and the EXPECTED_CHECK_COUNT trail (read the LAST assignment)
  </read_first>
  <action>
    Collapse the three builders into ONE function returning ONE
    `<section class="page-section" data-dirty-section="Calendar">` (keep whatever
    wrapper class and dirty-section attribute the current card uses so the dirty
    bar keeps naming it): heading, caption, `layout.status_row(...)`, then the
    state branch, then the "How it works" `<details>`.

    Not-connected branch: the connect `<form method="post"
    action="/settings/calendar/connect">` renders inline, unwrapped — the URL
    field, its hint and the primary "Connect calendar" button. The input keeps its
    write-only contract verbatim: no `value` attribute, ever, in either state.

    Connected branch: a `<p class="text-body calendar-masked-url">` (Task 2 fills
    it), then a `<p class="calendar-actions">` holding
    `<details class="calendar-url-disclosure"><summary class="text-link">Replace the
    feed URL</summary>` wrapping the same connect form with its button reading the
    new `CALENDAR_REPLACE_BUTTON_TEXT` ("Replace"), and — right-aligned on the same
    line — a bare `<button type="submit" form="calendar-disconnect-form"
    class="calendar-disconnect-btn">Disconnect</button>`. The disconnect `<form
    id="calendar-disconnect-form" method="post" action="{CALENDAR_DISCONNECT_ROUTE}"
    data-confirm=… data-confirm-value=…>` renders as a data-only sibling of the card
    carrying only its hidden confirm field — HTML forbids nested forms, and the
    button reaches it by `form=` exactly as the dirty-bar Save button already does.
    The confirm step (`confirm=yes`, the existing confirm page and route) is
    unchanged; `calendar_disconnect_confirm_page()` is not edited.

    Update `render()`'s Display assembly to emit the single card (plus the
    data-only disconnect form) where the three pieces used to be, keeping the
    card's position relative to its neighbours.

    Add the two new keys to `companion/i18n_fr/calendar_group.py` (after checking
    neither already exists in another module), under a comment banner naming
    D-13/D-14. Delete any entry orphaned by the merge in this same commit.

    Checks — retarget, do not delete, the checks listed in the interfaces block:
    the Display render contains exactly ONE Calendar `page-section`; the connect
    input carries no `value` in either state; the `<details>` wrap appears only
    when configured; the disconnect form is not a descendant of
    `<form id="settings-form">` and is not nested in any form; the whole rendered
    body still contains no nested `<form>`; the Disconnect button carries
    `form="calendar-disconnect-form"` and `class="calendar-disconnect-btn"`, and
    the confirm attributes still live on the form; a not-connected render shows the
    URL field and no Replace/Disconnect control at all; the drifted/unsafe state
    still renders its existing error-state detail inside the merged card. Re-derive
    `EXPECTED_CHECK_COUNT` by RUNNING the harness and appending a NEW last
    assignment commented `21-07-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py; "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def calendar_connect_section\|def calendar_disconnect_section" companion/pages/config_page.py` outputs `0` (or, if one function name is retained as the merged builder, exactly `1` — state which in the SUMMARY)
    - `grep -c 'form="calendar-disconnect-form"' companion/pages/config_page.py` outputs `1`
    - `grep -c "calendar-disconnect-btn" companion/pages/config_page.py` outputs `1`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Replace'], '|', c.CATALOG['Disconnect'])"` prints `Remplacer | Déconnecter`
    - `companion/test_config_page.py` reports M/M against its new last `EXPECTED_CHECK_COUNT`; `companion/test_i18n.py` exits 0
  </acceptance_criteria>
  <done>Calendar is one card in both states, the disconnect button sits on the replace line and still confirms, and no form is nested inside another.</done>
</task>

<task type="auto">
  <name>Task 2: The host-only masked feed URL</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py: how the stored calendar URL reaches this page today (the `configured`/`drift` flags and whatever loader supplies them) — confirm whether the raw URL is available at render time at all, and if it is NOT, read how to obtain it without widening any other seam
    - the write-only docstring at `calendar_connect_section()`'s field (:2311-2320) and 21-RESEARCH.md Area C.4 + the Security Domain row for the mask
    - 21-CONTEXT.md R-10 and 21-PATTERNS.md's `_masked_calendar_url()` sketch
    - companion/test_config_page.py: `_calendar_secret_never_reaches_render_function` (~4543) and `_calendar_secret_never_reaches_served_http_bytes` (~6325) in full — these are the checks to EXTEND, never to weaken
  </read_first>
  <action>
    Add a small helper beside the merged builder that returns the host-only mask:
    parse with `urlsplit(url)` (import from `urllib.parse`), take `netloc`, and
    return `"%s…" % netloc`. On ANY failure — falsy url, `ValueError`, empty
    netloc — return the empty string, and have the caller omit the
    `<p class="calendar-masked-url">` line entirely rather than render a
    fabricated placeholder (the "omit, don't fabricate" convention in the
    UI-SPEC's Empty/Error table; this deliberately avoids inventing a new string).
    Never build the mask by slicing or truncating the raw URL — a byte-offset mask
    leaks path/query characters for a short host. The value is escaped with
    `escape_html()` like every other interpolation.

    Document the deviation explicitly in the helper's docstring: phase 20's
    convention for secret URLs was write-only and never read back for display;
    D-14/R-10 deliberately narrows that to "host only, parsed, never the path,
    query, fragment or userinfo", and this helper is the only place that reads the
    stored URL back for rendering.

    Checks — extend the two secret-leak checks rather than adding a parallel pair:
    with a seeded URL such as
    `https://calendar.google.com/calendar/ical/abc123secret/basic.ics?token=deadbeef#frag`,
    the rendered page and the real served HTTP bytes contain the host, contain the
    ellipsis, and contain NONE of the path segments, the query string, the token,
    the fragment or any userinfo; a stored URL with no parseable netloc renders no
    masked-URL line at all; a hostile stored value (`"not a url"`, `""`,
    `"javascript:alert(1)"`) renders no line and raises nothing. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `21-07-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "urlsplit" companion/pages/config_page.py` outputs at least `1` and no masking is done by slicing (inspect the helper)
    - `grep -c "calendar-masked-url" companion/pages/config_page.py` outputs `1`
    - the extended secret-leak checks pass with the seeded token-bearing URL
    - `companion/test_config_page.py` reports M/M against its new last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>A connected calendar shows its host and an ellipsis, and no path, query, token or fragment character can reach the page or the wire.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the fusion CSS, style the small grey button, re-derive the :has() pin</name>
  <files>companion/static/style.css, companion/pages/config_page.py, companion/i18n_fr/display.py, companion/test_config_page.py</files>
  <read_first>
    - companion/static/style.css:2178-2198 and :5356-5381 — both `@supports selector(:has(*))` blocks with their own pin comments, `.page-section:has(+ .calendar-disconnect-form)` and `.calendar-disconnect-form`
    - companion/static/style.css: the bare `button` and `button[type="submit"]` rules (this codebase has no `.btn` family), `summary`/`summary::marker`, `.rule-add-form`, `--radius-control`, `--font-mono`
    - .planning/phases/21-.../21-UI-SPEC.md §E's CSS block (`.calendar-actions`, `.calendar-disconnect-btn`, `.calendar-masked-url`, the one-line `.text-link` rule) and the Accessibility Floors note on button geometry
    - companion/test_config_page.py ~3712-3760 (the `@supports selector(:has(*))` block-count check) and its history comments at ~172 and ~215
    - .claude/skills/sketch-findings-skypane/SKILL.md — the touch-target register and the control-density rules the small button must be checked against
  </read_first>
  <action>
    Delete both fusion rules from `companion/static/style.css`:
    `.page-section:has(+ .calendar-disconnect-form)` and `.calendar-disconnect-form`.
    They exist only to fake one card out of two adjacent siblings, which Task 1
    replaced with real nesting (Pitfall 2) — deleting them is part of the change,
    not a cleanup to defer. If deleting the first one empties an
    `@supports selector(:has(*))` block, delete the now-empty block too.

    Add the UI-SPEC §E rules: `.calendar-actions` (flex, baseline, space-between,
    `gap: var(--space-md)`), `.calendar-disconnect-btn` (the one additive block —
    30 px min-height, 4px/`--space-sm` padding, 12px text, a 6% text-colour wash
    with a 20% hairline, `--radius-control`) and its hover/focus-visible rule,
    `.calendar-masked-url` (`--font-mono`), and the one-line `.text-link`
    (accent colour, underline) applied to the disclosure summary. Tokens only —
    zero new colour literals, zero new accent-reservation entries. Record in the
    SUMMARY that the small button is a deliberate 30 px control on the existing
    quiet-button geometry register, not a new touch-target exception.

    Re-derive the pinned `@supports selector(:has(*))` block count in
    `companion/test_config_page.py`: RUN the harness, read the actual count from
    the stylesheet, update the expected number and extend the check's own history
    comment with a `21-07-PLAN.md Task 3` line stating the old count, the new count
    and which block was retired. Add a check that neither retired selector appears
    anywhere in the stylesheet.

    If a French entry in `companion/i18n_fr/display.py` was orphaned by Task 1's
    merge (for example a heading or caption the merged card no longer renders),
    delete it here, in the same wave that owns the file. Then run every harness and
    `ruff check .`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py && "$PY" companion/test_i18n.py && "$PY" companion/test_contrast_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "calendar-disconnect-form" companion/static/style.css` outputs `0`
    - `grep -c "calendar-disconnect-btn\|calendar-actions\|calendar-masked-url\|text-link" companion/static/style.css` outputs at least `4`
    - `grep -c "@supports selector(:has(\*)) {" companion/static/style.css` equals the number now pinned in `companion/test_config_page.py`
    - `companion/test_config_page.py`, `companion/test_i18n.py` and `companion/test_contrast_check.py` all report M/M against their last `EXPECTED_CHECK_COUNT`; `ruff check .` is clean
  </acceptance_criteria>
  <done>The card's shape comes from real nesting, the Disconnect button is small and grey without a .btn family, and the feature-query pin matches the stylesheet again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored calendar URL (a secret token-bearing string) → rendered page | the one place in this codebase that reads a stored secret back for display |
| browser → POST /settings/calendar/connect | the write-only URL field, unchanged |
| browser → POST /settings/calendar/disconnect | a destructive state change whose submit button now lives outside its own form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-24 | Information Disclosure | the masked feed URL (R-10) | mitigate | the mask is `urlsplit(url).netloc` plus an ellipsis — a parse, never a byte-offset truncation, so path, query, fragment and userinfo characters can never appear; any parse failure renders no line at all; pinned by the two extended secret-leak checks against both the render function and the real served HTTP bytes |
| T-21-25 | Tampering (XSS) | the masked host, the status detail and every merged-card string | mitigate | every interpolation crosses `escape_html()`; the masked value is additionally constrained to a parsed netloc |
| T-21-26 | Tampering (accidental destructive action) | the small grey Disconnect button | mitigate | the confirmation is unchanged: `data-confirm`/`data-confirm-value` on the form plus the existing server-rendered confirm page and `confirm=yes` route — the button got smaller, the safety net did not |
| T-21-27 | Tampering (invalid DOM / cross-form submission) | the `form="calendar-disconnect-form"` button | mitigate | the disconnect form is a data-only sibling with an id; the whole-body "no `<form>` inside a `<form>`" check stays green, and the disconnect form is explicitly asserted not to be a descendant of `#settings-form` |
| T-21-28 | Tampering | the connect field ever pre-filled | mitigate | the write-only contract is re-pinned in both states by `_calendar_connect_field_never_carries_value_in_either_state` |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_config_page.py`, `companion/test_i18n.py`, `companion/test_contrast_check.py` → M/M
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure
- `ruff check .` clean
- `<human-check>` (folded into plan 21-08's sweep): the connected Calendar card shows one status line, the masked host, a quiet "Replace the feed URL" link with a small grey Disconnect button on the same line; clicking Disconnect still asks for confirmation; the not-connected state shows the field and the primary button.
</verification>

<success_criteria>
- One Calendar `page-section` in both states; no trailing disconnect card and no `:has()`-based fusion
- The masked URL is host-only and parse-derived; no path, query, token or fragment reaches the page or the wire
- "Replace the feed URL" is a `<details>` link whose button reads "Replace"; Disconnect is small, grey, right-aligned and still confirms
- The write-only connect field and the confirm route are unchanged
- Both fusion CSS rules deleted and the `@supports selector(:has(*))` pin re-derived by running the harness
- Three `EXPECTED_CHECK_COUNT` re-derivations by running the harness, appended citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-07-SUMMARY.md` when done.
</output>
</content>
