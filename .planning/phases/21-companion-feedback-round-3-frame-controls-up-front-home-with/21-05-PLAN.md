---
phase: 21
plan: 05
type: execute
wave: 4
depends_on: ["21-04"]
files_modified:
  - companion/pages/config_page.py
  - companion/static/theme-preview.js
  - companion/static/style.css
  - companion/i18n_fr/display.py
  - companion/test_config_page.py
  - companion/test_companion_app.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-20]

must_haves:
  truths:
    - "D-06: Display's Look supersection opens with ONE 'Frame colours' card; the old Theme card, the arrivals checkbox, the Calendar card's compact chip grid and the separate 'Flight colours' card are gone"
    - "D-07: at ≥960 px the card is preview left (never narrower than 360 px) and a four-row assignment list right, in the order Departures, Arrivals, Calendar flights, Per-flight rules; below 960 px the preview stacks on top"
    - "D-08: the rows are a visually-hidden radiogroup named colour_usage whose whole row is the label; one usage panel is shown at a time by theme-preview.js"
    - "D-08/R-05 no-JS floor: the server renders all four usage panels with NO hidden attribute and no inline style — three labelled <fieldset> chip grids plus the rules block — and the script hides the unselected ones at load; every value is still saveable without script"
    - "D-09: Arrivals and Calendar each get a leading 'Same as departures' chip that submits the empty string; the theme_arriving_enabled checkbox and ARRIVING_CHECKBOX_VALUE are deleted"
    - "R-07: handle_post()'s membership gates exempt the empty string for theme_arriving AND calendar_theme_id, and the resolution block maps '' → CLEAR_THEME_ARRIVING, absent → carry forward, member → value — both sites change together"
    - "D-10: the rules row's panel holds the existing rule list, the existing add form with its own compact chip grid, and the 'How rules combine' disclosure, relocated verbatim"
    - "D-11: the three theme radiogroups still carry form='settings-form', so Display's single Save persists them and the dirty bar covers them; rule add/delete keep their own POSTs"
    - "D-12/R-11: theme-preview.js is rewritten to scope its lookups to the Frame colours card — no page-wide querySelector — and still writes only an <img src> and class/hidden toggles"
  artifacts:
    - path: "companion/pages/config_page.py"
      provides: "the Frame colours card and the empty-string clear signal"
      contains: "colour_usage"
    - path: "companion/static/theme-preview.js"
      provides: "row → panel → preview wiring, ES5, no HTML sink"
      contains: "data-usage-panel"
  key_links:
    - from: "companion/pages/config_page.py"
      to: "#settings-form"
      via: "cross-DOM submission of the three theme radiogroups"
      pattern: "radio_form_id|SETTINGS_FORM_ID"
    - from: "companion/static/theme-preview.js"
      to: "companion/pages/config_page.py"
      via: "the data-usage-panel / data-preview-src attribute contract"
      pattern: "data-usage-panel|data-preview-src"
---

<objective>
Replace the four theme chip grids with one "Frame colours" card: live preview on
the left, a four-row assignment list on the right, one chip grid for the selected
row, "Same as departures" for Arrivals and Calendar, and the rule list and add
form under the rules row — with a no-JS floor that still saves every value and a
server handler that actually accepts the new clear signal.

Purpose: the developer's fourth and fifth feedback points — the same picker
rendered four times in four places, and no way to say "same as departures" except
a checkbox. The trap here is server-side: today's clear signal is a checkbox this
decision deletes, and the membership gate rejects the empty string before the
resolution block ever runs (Pitfall 1). Both sites change together or the option
can never be saved.
Output: one new card builder, a rewritten `theme-preview.js`, the empty-string
clear path through `handle_post()`, the card's CSS and its French copy.
</objective>

<decisions_covered>
D-06, D-07, D-08, D-09, D-10, D-11, D-12, R-05, R-07, R-11.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-PATTERNS.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan owns, for wave 4: `companion/pages/config_page.py`,
`companion/static/theme-preview.js`, `companion/static/style.css`,
`companion/i18n_fr/display.py`, `companion/test_config_page.py`,
`companion/test_companion_app.py` and `companion/test_status_pages.py`. Plan
21-06 runs concurrently and owns `companion/pages/airlines_page.py` and
`companion/test_view_pages.py` — never edit either here. `companion/test_i18n.py`
is RUN, never edited. Inside `config_page.py`, this plan restructures the theme
machinery and deletes the Calendar card's compact chip grid (D-06) — it does NOT
otherwise restructure the Calendar card: plan 21-07 (wave 5) merges it.
</files_owned>

<interfaces>
Current sites, file:line as read on 2026-09-12 (re-verify before editing):

  config_page.py:966-1070   _theme_chip_grid_html(field_name, selected_theme_id,
      extra_class="", extra_attr="", chip_extra_class="", radio_form_id=None)
      — the ONE grid builder; each chip label already carries
      data-preview-src="/theme-preview/{id}.png?live=1" (:1041)
  config_page.py:1073-1135  _theme_live_preview_html(current_theme_id, state_dir)
  config_page.py:1138-1356  theme_fieldset() — the card to retire: preview +
      departures grid (:1293), the arrivals checkbox + second grid (:1313)
  config_page.py:222-235    THEME_ARRIVING_TOGGLE_ID, THEME_ARRIVING_CHECKBOX_LABEL,
      ARRIVING_CHECKBOX_VALUE — deleted
  config_page.py:2252-2261  calendar_group()'s compact calendar_theme_id grid — deleted
  config_page.py:2581-2605  _rule_kind_radio_html() — the hidden-radio + styled-label
      idiom the colour_usage rows copy
  config_page.py:2608-2685  _rule_add_form_html()  (its own <form>, its own compact grid)
  config_page.py:2785-2803  _rule_list_html()
  config_page.py:2805-2869  _rules_section_html() — its outer card/heading retires;
      its contents relocate into the rules usage panel
  config_page.py:3912-3923  the two membership gates (calendar_theme_id, theme_arriving)
  config_page.py:3941-3960  the checkbox-keyed theme_arriving resolution block
  config_page.py:3327-3399  render()'s Display-scope assembly
  static/theme-preview.js:41-42  `document.querySelector(".theme-live-preview img")`
      and `document.querySelector(".theme-chip-grid")` — the single-grid assumption
      that breaks with four grids on one page (R-11: a real rewrite, not a patch)
  style.css:2178-2198, 5356-5368  the two `@supports selector(:has(*))` blocks whose
      COUNT is pinned by companion/test_config_page.py ~3712-3760
      (`supports_marker = "@supports selector(:has(*)) {"`). If this plan adds or
      removes such a block, re-derive that check's expected count here; plan 21-07
      re-derives it again when it retires the calendar fusion rules.
  server/device_config.py   normalise_theme_arriving() and normalise_calendar_theme_id()
      already degrade "" to None and need NO change; CLEAR_THEME_ARRIVING is their
      existing sentinel.

Exact new copy (UI-SPEC Copywriting Contract §D) for `companion/i18n_fr/display.py`:
  "Frame colours"        → "Couleurs du cadre"
  "Departures"           → "Départs"
  "Arrivals"             → "Arrivées"
  "Calendar flights"     → "Vols du calendrier"
  "Per-flight rules"     → "Règles par vol"
  "Same as departures"   → "Comme les départs"      (row meta AND chip label)
  "%d rules"             → "%d règles"
  "1 rule"               → "1 règle"
  "No rules yet"         → "Aucune règle pour l'instant"
  "Choose the colour theme for departures, arrivals, calendar flights and your own rules."
                         → "Choisissez le thème de couleurs pour les départs, les arrivées,
                            les vols du calendrier et vos propres règles."
      (the card's section caption — the only string in §D's markup that the copy
       table does not itself tabulate; this French value is locked here, same voice,
       sentence case, U+2019 apostrophes, U+00A0 before `:` `;` `?` `!`)
Reused verbatim: the preview captions
(THEME_LIVE_PREVIEW_CAPTION_WITH_FLIGHT_TEMPLATE / _SAMPLE), every theme name,
every chip label, and every rule-form string.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Frame colours card — one preview, four rows, four usage panels</name>
  <files>companion/pages/config_page.py, companion/static/style.css, companion/i18n_fr/display.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py: `_theme_chip_grid_html()`, `_theme_live_preview_html()`, `theme_fieldset()`, `_rule_kind_radio_html()`, `_rule_add_form_html()`, `_rule_list_html()`, `_rules_section_html()`, `calendar_group()`'s chip-grid block, and `render()`'s Display-scope assembly — each in full
    - .planning/phases/21-.../21-UI-SPEC.md §D: the full markup block, every CSS rule, the layout/selection/row-meta/chip-grid/rules/no-JS bullets, and Structural Notes 2 and 3
    - companion/static/style.css: `.theme-status`, `.theme-chip*`, `.theme-chip-grid[--compact]`, `.theme-option`, `.runway-card` (the selectable-card idiom), `.visually-hidden`, `.page-section`, and both `@supports selector(:has(*))` blocks with their pin comments
    - companion/test_config_page.py: every check naming `theme_fieldset`, `theme-chip-grid`, `THEME_ARRIVING_`, `_rules_section_html`, `RULES_SECTION_HEADING`, or the calendar chip grid (`_calendar_theme_chip_grid_*`, ~4656-4718), the `@supports` block-count check (~3712-3760), and the EXPECTED_CHECK_COUNT trail (read the LAST assignment)
  </read_first>
  <action>
    Add one builder — e.g. `_frame_colours_card_html(ctx, …)` — rendering the
    UI-SPEC §D markup as `<div class="page-section frame-colours"
    data-dirty-section="Frame colours">`: heading, section caption, then
    `.frame-colours__layout` holding the existing `_theme_live_preview_html()`
    output on the left and `.frame-colours__assign` on the right. The assign
    column holds a `role="radiogroup"` list of four rows — each a
    `<label class="frame-colours__row">` wrapping a `class="visually-hidden"`
    `<input type="radio" name="colour_usage" value="departures|arrivals|calendar|rules">`
    (departures checked), a swatch built from the existing
    `.theme-chip__swatches`/`.theme-chip__dot` markup, the row label and the row
    meta ("theme name" / "Same as departures" / "N rules" / "No rules yet") — then
    four `.frame-colours__usage-panel` divs carrying `data-usage-panel-target`.

    Panels one to three each wrap a `<fieldset>` with a `<legend>` matching its
    row's label around one `_theme_chip_grid_html()` call — `theme_id`,
    `theme_arriving`, `calendar_theme_id` — every one with
    `radio_form_id=SETTINGS_FORM_ID` (D-11). Panel four holds
    `_rule_list_html()` (or the existing empty-state block), `_rule_add_form_html()`
    and the existing "How rules combine" `<details>`, relocated verbatim — its own
    `<form>` is legal here because the parent is a `<div>`, and the card itself
    renders as a SIBLING of `<form id="settings-form">` (Structural Note 2; confirm
    `_scope_fields_html()`'s hidden inputs still render correctly inside the
    now-visibly-empty form and say so in the SUMMARY).

    Extend `_theme_chip_grid_html()` with one additive parameter,
    `leading_chip_html=""`, interpolated before the chip loop (Structural Note 3):
    Arrivals and Calendar pass a "Same as departures" chip that submits the EMPTY
    STRING for their field; Departures and the rule form pass nothing and are
    byte-identical. The same-as-departures chip reuses `.theme-chip`'s markup shape
    with no fabricated theme-coloured preview.

    NO-JS FLOOR (R-05, locked): the server renders all four panels with NO `hidden`
    attribute and no inline style — a script-free page shows four clearly labelled
    stacked sections and can still save every value. The collapsing is entirely the
    script's job at load (Task 3), exactly like `flight-rows.js`'s class-at-load
    pattern. Do not emit `hidden` server-side and do not key any hide off a
    page-wide `.js` class.

    Retire `theme_fieldset()` (its preview and departures grid now live in the new
    card), the arrivals checkbox and its constants (`THEME_ARRIVING_TOGGLE_ID`,
    `THEME_ARRIVING_CHECKBOX_LABEL`, `ARRIVING_CHECKBOX_VALUE`) together with the
    `:has()` reveal rule that served the checkbox, `_rules_section_html()`'s outer
    card and `RULES_SECTION_HEADING`, and `calendar_group()`'s compact
    `calendar_theme_id` grid (leave the rest of the Calendar card alone — plan
    21-07 owns it). Wire the new card in as the first item of the "Look"
    supersection in `render()`.

    Add the UI-SPEC §D CSS: `.frame-colours__layout` (one column, then
    `minmax(360px, 1fr) minmax(0, 1fr)` at ≥960 px), `.frame-colours__preview`,
    `.frame-colours__list`, `.frame-colours__row` (min-height 44px — the whole row
    is the label), the `:checked`/`:focus-visible` sibling rules reusing the exact
    `.theme-chip--selected` accent values, `.frame-colours__label` and
    `.frame-colours__meta`. Zero new colour tokens, zero new accent-reservation
    entries. If your `:has()` belt-and-braces rule adds or removes an
    `@supports selector(:has(*))` block, re-derive the pinned block count in
    `test_config_page.py` in this same task.

    Add the new French entries listed in the interfaces block to
    `companion/i18n_fr/display.py`, under a comment banner naming D-06..D-12.
    Delete any entry orphaned by the retired checkbox/heading in the same commit.

    Checks — `companion/test_config_page.py`: exactly one `.frame-colours` card on
    Display and it is the first card under the "Look" section intro; exactly four
    `input[name="colour_usage"]` radios in the locked order with departures
    checked; exactly four `.frame-colours__usage-panel` elements and NONE carries
    `hidden` or `style=`; each of the three chip grids carries
    `form="settings-form"` on every radio; the arrivals and calendar grids each
    have a leading chip whose value is the empty string; the rules panel contains
    the rule list, the add form and the "How rules combine" details; the page
    contains no `THEME_ARRIVING_CHECKBOX_LABEL` markup, no `RULES_SECTION_HEADING`
    card and no chip grid inside the Calendar card; the rendered page still has no
    nested `<form>` (keep that pinned check green); a French render shows the four
    row labels and "Comme les départs". Re-derive `EXPECTED_CHECK_COUNT` by RUNNING
    the harness and appending a NEW last assignment commented `21-05-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py; "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "colour_usage" companion/pages/config_page.py` outputs at least `4`
    - `grep -c "THEME_ARRIVING_TOGGLE_ID\|ARRIVING_CHECKBOX_VALUE\|THEME_ARRIVING_CHECKBOX_LABEL\|RULES_SECTION_HEADING" companion/pages/config_page.py` outputs `0`
    - `grep -c "leading_chip_html" companion/pages/config_page.py` outputs at least `3`
    - `grep -c "frame-colours" companion/static/style.css` outputs at least `6`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Frame colours'], '|', c.CATALOG['Same as departures'])"` prints `Couleurs du cadre | Comme les départs`
    - `companion/test_config_page.py` reports M/M against its new last `EXPECTED_CHECK_COUNT`; `companion/test_i18n.py` exits 0
  </acceptance_criteria>
  <done>Display's Look section opens with one Frame colours card whose four usage panels all render without script, and no other theme chip grid survives on the page.</done>
</task>

<task type="auto">
  <name>Task 2: Make "Same as departures" actually saveable — handle_post's two sites</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py `handle_post()` in full from the submitted-field reads through the save call — specifically the membership gates at :3912-3923 and the resolution block at :3941-3960, and how `submitted_calendar_theme_id` reaches `save_device_config()`
    - server/device_config.py: `normalise_theme_arriving()` (:503-523), `normalise_calendar_theme_id()` (:526-547), the `CLEAR_THEME_ARRIVING` sentinel contract (:95-111, :890-895) — read to confirm no change is needed there
    - .planning/phases/21-.../21-RESEARCH.md Area B.3 and Pitfall 1, and 21-PATTERNS.md's before/after code for both sites; 21-CONTEXT.md R-07
    - companion/test_config_page.py: `_theme_arriving_markup_both_grids_copy_and_checkbox_present` (~3327), `_theme_arriving_override_preselects_second_grid_and_checks_the_box` (~3394), `_handle_post_theme_arriving_checked_persists_chosen_id` (~3416), `_handle_post_theme_arriving_checkbox_absent_clears_previous_override` (~3440), `_handle_post_crafted_theme_arriving_checkbox_value_rejected` (~3476), `_handle_post_nonmember_theme_arriving_rejected` (~3498), `_handle_post_theme_arriving_partial_post_still_carries_other_fields` (~3527), `_theme_arriving_clearable_contract_full_round_trip` (~3558), `_settings_form_raw_post_no_js_clears_and_sets_theme_arriving` (~6164), and the calendar-grid checks (~4656-4718)
  </read_first>
  <action>
    Change BOTH sites in the same commit (Pitfall 1 — changing one alone ships an
    option that always fails to save):

    1. The membership gates: both `submitted_calendar_theme_id` and
       `submitted_theme_arriving` are now valid when they equal `""`, i.e. test
       against `("",) + device_config.THEME_IDS`. Anything else non-member still
       raises `ERROR_INVALID_CHOICE` and still returns `FLASH_SAVE_FAILED` —
       the empty string is carved out, the gate is not weakened.
    2. The resolution block loses its `submitted_theme_arriving_enabled` branch
       entirely and becomes: out of scope → `None`; field absent → `None` (carry
       forward); `""` → `device_config.CLEAR_THEME_ARRIVING`; otherwise the
       already-membership-checked value. Delete
       `ERROR_UNEXPECTED_SWITCH_VALUE`'s use here if nothing else raises it.
       `calendar_theme_id` needs no second block: once its gate accepts `""`, the
       existing pass-through plus `normalise_calendar_theme_id("")`'s documented
       `None` degrade already does the right thing — confirm by reading, and say
       so in the SUMMARY.
    `server/device_config.py` is NOT edited by this plan.

    Checks — rewrite the checkbox-era checks named in read_first rather than
    deleting them wholesale: `theme_arriving=""` saves successfully and reads back
    as `None`; `calendar_theme_id=""` does the same; a POST omitting the field
    entirely carries the previous value forward; a crafted non-member value
    (`"nope"`, `" "`, `"WHITE "`) is still rejected with `ERROR_INVALID_CHOICE` and
    the whole save is refused; the full no-JS round trip (a raw form POST with no
    script) both sets and clears `theme_arriving` and `calendar_theme_id`; and the
    three theme fields still save together from one Display Save. Delete
    `_handle_post_crafted_theme_arriving_checkbox_value_rejected` (its field no
    longer exists) and say so in the SUMMARY. Re-derive `EXPECTED_CHECK_COUNT` and
    append a NEW last assignment commented `21-05-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "theme_arriving_enabled" companion/pages/config_page.py` outputs `0`
    - `grep -c "CLEAR_THEME_ARRIVING" companion/pages/config_page.py` outputs at least `1`
    - `grep -c "(\"\",) + device_config.THEME_IDS\|('',) + device_config.THEME_IDS" companion/pages/config_page.py` outputs `2`
    - `grep -rn "normalise_theme_arriving\|normalise_calendar_theme_id" server/device_config.py` is unchanged (`git diff --stat server/` shows no server file touched)
    - `companion/test_config_page.py` reports M/M against its new last `EXPECTED_CHECK_COUNT`, including the new empty-string round-trip checks
  </acceptance_criteria>
  <done>Selecting "Same as departures" for Arrivals or Calendar saves and reads back as no override, and a crafted non-member value is still refused.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite theme-preview.js for four panels, one preview</name>
  <files>companion/static/theme-preview.js, companion/pages/config_page.py, companion/test_config_page.py, companion/test_companion_app.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/static/theme-preview.js in full (81 lines) — its header comment, its IIFE guard clauses and the two page-wide `querySelector` calls at :41-42
    - companion/static/flight-rows.js (added by plan 21-03) — the class-at-load no-JS-floor pattern to mirror
    - .planning/phases/21-.../21-UI-SPEC.md §D's selection-mechanism, preview-linkage and no-JS bullets; 21-CONTEXT.md R-11; 21-RESEARCH.md Area B.5
    - companion/test_companion_app.py: the theme-preview route==src check and the served-bytes check (~3491-3535); companion/test_status_pages.py: any check asserting the served stylesheet or script contract that names theme-preview
  </read_first>
  <action>
    Rewrite `companion/static/theme-preview.js`'s core: scope every lookup to the
    Frame colours card (query the card element once, then `querySelectorAll`
    within it) instead of the two page-wide `document.querySelector` calls.
    Behaviour:
    - at load, hide every `.frame-colours__usage-panel` whose
      `data-usage-panel-target` does not match the checked `colour_usage` radio, by
      adding a collapsing class the stylesheet owns (never by writing `hidden`
      server-side — the server must keep rendering all four visible);
    - on a `colour_usage` change, show that row's panel and hide the others, and
      set the preview `<img>`'s `src` to that usage's currently-effective
      `data-preview-src` (for Arrivals/Calendar with "Same as departures" selected
      this is the departures theme's preview; for the rules row it is the rule
      form's currently-selected chip, defaulting to the first theme);
    - on a chip change inside the visible grid, set the preview `src` to that
      chip's `data-preview-src` — the phase-20 behaviour, now per-usage.
    Hard requirements: ES5 only (no `let`/`const`/arrow/template literal), one
    `"use strict"` IIFE, guard-clause out when the card is absent (every other page
    must be unaffected), and the ONLY DOM writes are the `<img src>` assignment,
    class toggles and `aria`/checked-state reads — no `innerHTML`,
    `insertAdjacentHTML`, `document.write` or inline handler. Update the header
    comment to name D-08/D-12/R-11, the attribute contract it depends on, and the
    no-JS floor. The route, the `*_SCRIPT_ROUTE`/`*_SCRIPT_SRC` pair and the
    `<script>` tag are unchanged — this is a body rewrite of an already-registered
    script.

    If the card needs extra attributes for this (e.g. one `data-preview-src` per
    row for the effective theme), add them in `config_page.py` in this task, each
    value escaped server-side.

    Checks — `companion/test_config_page.py`: the rendered card carries a
    `data-usage-panel-target` for each of the four usages and a `data-preview-src`
    on every chip and on every row; no rendered panel carries `hidden`; the page
    has no inline `<script>` and no `on*=` attribute. `companion/test_companion_app.py`:
    a real GET of the theme-preview script route returns 200 whose body contains
    `data-usage-panel`, contains no `document.querySelector(".theme-chip-grid")`
    page-wide lookup, and contains none of `innerHTML`, `document.write`, `=>`,
    ` let `, ` const `. `companion/test_status_pages.py`: only touch it if an
    existing check there names the theme-preview script's contract — otherwise
    leave it unedited and say so. Re-derive every edited harness's
    `EXPECTED_CHECK_COUNT` and append NEW last assignments commented
    `21-05-PLAN.md Task 3`. Finish with the full suite and `ruff check .`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py && "$PY" companion/test_companion_app.py && "$PY" companion/test_status_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "document.querySelector(\".theme-chip-grid\")" companion/static/theme-preview.js` outputs `0`
    - `grep -cE "\b(let|const)\b|=>|innerHTML|document\.write|insertAdjacentHTML" companion/static/theme-preview.js` outputs `0`
    - `grep -c "data-usage-panel" companion/static/theme-preview.js` outputs at least `1` and the same token appears in `companion/pages/config_page.py`
    - `grep -c "hidden" companion/pages/config_page.py` shows no `hidden` attribute on a usage panel (inspect the builder, do not just count)
    - all four harnesses report M/M against their last `EXPECTED_CHECK_COUNT` (with only the documented pre-existing failures) and `ruff check .` is clean
  </acceptance_criteria>
  <done>Clicking a row swaps the visible chip grid and the preview with no reload, and a browser with the script blocked still shows and saves all four usages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /settings (scope=display) | the batched settings form now carries three theme fields whose valid domain includes the empty string |
| browser → POST /settings/rules/add and /settings/rules/delete | unchanged routes, relocated forms |
| browser → GET /static/theme-preview.js | a rewritten static script served to every authenticated page |
| theme-preview.js → the preview <img> src | a client-side URL write derived from server-rendered attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-16 | Tampering (input validation, ASVS V5) | the widened `theme_arriving`/`calendar_theme_id` domain | mitigate | both gates test membership against `("",) + device_config.THEME_IDS` and still reject everything else with `ERROR_INVALID_CHOICE` + `FLASH_SAVE_FAILED`; the storage layer's `normalise_*()` functions independently degrade any non-member (including `""`) to `None`, so a crafted POST can neither crash nor write garbage — pinned by Task 2's crafted-value checks |
| T-21-17 | Tampering (silent state loss) | the batched save now covering three theme fields from a card that sits outside `<form id="settings-form">` | mitigate | every radio carries `form="settings-form"`, so the submitted field set is unchanged in shape; absent-field carry-forward is explicitly re-pinned by a round-trip check |
| T-21-18 | Tampering (XSS via script sink) | the rewritten `theme-preview.js` | mitigate | the script writes an `<img src>` from a server-rendered, server-escaped `data-preview-src` and toggles classes — no HTML sink, no inline handler, no `eval`; pinned by a served-bytes check |
| T-21-19 | Tampering (XSS) | the card's row labels, meta text, theme names and swatch colours | mitigate | every interpolation crosses `escape_html()`; swatch colours come from `_palette_hex(...)` over `server.panel_format.PALETTE_RGB`, never from user input |
| T-21-20 | Denial of Service (usability) | a browser with the script blocked | mitigate | the no-JS floor is server-rendered: all four panels visible, every radio submitting its real value — the failure mode is a longer page, never an unsaveable one |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_config_page.py`, `companion/test_i18n.py` → M/M
- `companion/test_companion_app.py` → only the two documented root-sandbox FAILs
- `companion/test_status_pages.py` → only the documented `anomaly_active()` FAIL
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure
- `ruff check .` clean
- `<human-check>` (folded into plan 21-08's sweep): clicking each of the four rows swaps the panel and the preview with no console error; saving Display persists a "Same as departures" choice; with JavaScript disabled the card shows four labelled sections and still saves.
</verification>

<success_criteria>
- One "Frame colours" card; no arrivals checkbox, no Calendar-card chip grid, no separate "Flight colours" card anywhere
- Four `colour_usage` rows, one visible chip grid at a time under script, all four panels visible and saveable without script
- Arrivals and Calendar offer a leading "Same as departures" chip submitting the empty string, and that choice saves and reads back as no override
- Both `handle_post()` sites changed together; a crafted non-member value is still rejected; `server/device_config.py` untouched
- `theme-preview.js` scoped to the card, still ES5, still no HTML sink, still one registered route
- Zero new colour tokens; any change to the `@supports selector(:has(*))` block count re-derived in the pinned check
- Three `EXPECTED_CHECK_COUNT` re-derivations by running the harnesses, appended citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-05-SUMMARY.md` when done.
</output>
</content>
