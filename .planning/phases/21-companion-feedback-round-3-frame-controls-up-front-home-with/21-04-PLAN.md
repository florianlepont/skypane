---
phase: 21
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - companion/layout.py
  - companion/app.py
  - companion/pages/home_page.py
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/i18n_fr/nav.py
  - companion/i18n_fr/home.py
  - companion/test_view_pages.py
  - companion/test_status_pages.py
  - companion/test_companion_app.py
  - companion/test_config_page.py
  - companion/test_contrast_check.py
autonomous: true
requirements: [CFG-19]

must_haves:
  truths:
    - "D-01/R-01: one function, layout.frame_strip_html(ctx, return_to, next_wake_iso), renders the Frame strip; Home and Display each call it once, so the two copies cannot diverge"
    - "D-01: the strip holds, left to right, the Screen switch (state + instant button), the Quiet hours switch (state + instant button) and the next update, with the next update as the strip's only Emphasis-weight line"
    - "D-02: the Screen on/off and Quiet hours cards on Display keep their presets, custom times and Save button and contain no .quick-action markup — exactly one .quick-action--on/--off pair exists per page, inside the strip"
    - "D-01/R-02: each switch form carries a hidden return_to field; _handle_quick_toggle() validates it against {HOME_ROUTE, DISPLAY_ROUTE} and falls back to DISPLAY_ROUTE, and all three redirects in the handler use the resolved target"
    - "D-03/R-03: one layout function computes the nav reminder from ctx['device_config'], both nav renderers call it, and page_shell(device_config=None) renders no reminder at all — the login shell and error pages are unchanged"
    - "D-03: the reminder is an <a href='/'> with two dot+word segments and no form and no button"
    - "D-04/R-06: Home renders header → strip → three stat_tile() tiles (Frame/Battery/Flight data) + the Health link → a two-column picture/recent-flights row; the phase-20 status card, .home-hero and .status-card rows are deleted"
    - "R-06: the tiles are fed by the phase-20 bug-fixed verdict/detail derivation — the frame verdict appears exactly once on the page; the phase-19 function body is never resurrected verbatim"
    - "D-05: the strip reuses .stat-tile/.stat-tile--accent and adds zero colour tokens and zero accent-reservation entries"
  artifacts:
    - path: "companion/layout.py"
      provides: "frame_strip_html(), nav_status_html(), page_shell(device_config=...)"
      contains: "frame_strip_html"
    - path: "companion/pages/home_page.py"
      provides: "the rebuilt Home: strip, three tiles, picture/recent-flights row"
      contains: "layout.stat_tile"
    - path: "companion/static/style.css"
      provides: ".frame-strip*, .nav-status*, .dot--off, .home-picture-row"
      contains: "frame-strip__cells"
  key_links:
    - from: "companion/pages/home_page.py"
      to: "companion/layout.py"
      via: "the one shared strip helper"
      pattern: "layout\\.frame_strip_html\\("
    - from: "companion/pages/config_page.py"
      to: "companion/layout.py"
      via: "the same shared strip helper on the Display scope"
      pattern: "layout\\.frame_strip_html\\("
    - from: "companion/app.py"
      to: "companion/layout.py"
      via: "the return_to whitelist and the device_config keyword on page_shell()"
      pattern: "return_to|device_config="
---

<objective>
Put the frame's two everyday controls and its next update in one shared strip at
the top of Home and Display, add a state-only reminder to the nav, and rebuild
Home as strip → three tiles → picture beside recent flights.

Purpose: the developer asked for the Screen and Quiet hours switches to have a
"place prépondérante" and for the next update to stand out, and for Home to be
"3 tuiles séparées" again rather than one dense card. One helper renders the
strip for both pages so the two can never drift apart.
Output: two new shared helpers in `layout.py`, a `return_to`-validating quick
toggle, a Display whose schedule cards no longer carry an instant switch, a
rebuilt Home, and the CSS the UI-SPEC specifies.
</objective>

<decisions_covered>
D-01, D-02, D-03, D-04, D-05, R-01, R-02, R-03, R-04, R-06.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-PATTERNS.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan is the only plan in wave 3 and owns every file in `files_modified`.
It must NOT touch: `companion/pages/history_page.py`, `companion/pages/health_page.py`,
`companion/pages/airlines_page.py`, `companion/static/flight-rows.js`,
`companion/static/freshness.js`, `companion/static/theme-preview.js`,
`companion/i18n_fr/display.py`, `companion/i18n_fr/flights.py`,
`companion/i18n_fr/health.py`, `companion/i18n_fr/calendar_group.py`,
`companion/i18n_fr/rules.py`, `companion/i18n_fr/__init__.py`,
`companion/test_i18n.py` (run it, never edit it) or `scripts/run_all_tests.py`.
Within `companion/pages/config_page.py` this plan touches ONLY the two
quick-action slots, the Display `render()` call site and whatever import the strip
needs — the theme/Frame-colours machinery belongs to plan 21-05 and the Calendar
card to plan 21-07.
</files_owned>

<interfaces>
`companion/layout.py` imports stdlib plus `companion.auth`, `companion.i18n` and
`companion.prefs` ONLY — its own docstring states "no imports from server/".
Do not add `from server import wake` there. The strip helper therefore takes the
already-resolved `next_wake_iso` from its caller; both callers already import
`companion.wake` (`home_page.py:34`, `config_page.py:23`).

Markup to relocate verbatim (PATTERNS gives both blocks in full):
  companion/pages/config_page.py:1718-1754  quiet_hours_group()'s quick_action_html
  companion/pages/config_page.py:2090-2120  display_group()'s quick_action_html
  companion/pages/config_page.py:433-446    QUICK_ACTION_SCREEN_LABEL, _ON_TEXT,
      _OFF_TEXT, _SWITCH_ON_BUTTON, _SWITCH_OFF_BUTTON, QUICK_ACTION_QUIET_LABEL,
      _QUIET_ON_TEMPLATE, _QUIET_OFF_TEXT, _QUIET_TURN_ON_BUTTON,
      _QUIET_TURN_OFF_BUTTON, QUICK_ACTION_APPLIES_SENTENCE
  companion/pages/home_page.py:254-274      the next-update headline computation
  companion/pages/home_page.py:82-97        STATUS_HEADING, FRAME_ROW_LABEL,
      HEALTH_LINK_TEXT, NEXT_UPDATE_TEMPLATE, EXPECTED_SINCE_TEMPLATE

Constants that must MOVE to `companion/layout.py` (a page module may never import
another page module, and both pages need them): the eleven `QUICK_ACTION_*`
constants, `NEXT_UPDATE_TEMPLATE`, `EXPECTED_SINCE_TEMPLATE`, and a strip heading
constant whose English value is `"Frame"`. Every English VALUE stays
byte-identical, so every French entry keeps working unchanged — the catalogue is
keyed by English string and `layout.py` is already in `test_i18n.py`'s
`_SCAN_RELATIVE_PATHS`. Do not fork a second copy of any template text.

The handler to change — companion/app.py:2787-2826 `_handle_quick_toggle()`:
all three `self.redirect("%s?flash=%s" % (layout.DISPLAY_ROUTE, ...))` calls take
the resolved `return_to` instead. The validation shape to copy is
`_referring_tab()`'s (app.py:2376-2383) membership-test-then-fallback — but R-02
locks the SOURCE as a hidden form field, not the Referer header:
    return_to = form.get("return_to")
    if return_to not in (layout.HOME_ROUTE, layout.DISPLAY_ROUTE):
        return_to = layout.DISPLAY_ROUTE      # unchanged pre-phase-21 behaviour

The phase-19 tile structure to restore (structure only — R-06) is
`git show 614d41e~1:companion/pages/home_page.py` lines 166-218. The CURRENT,
bug-fixed content derivation to keep is `_status_card_html()`'s
`frame_verdict`/`frame_detail` pair (home_page.py:276-297), built from
`FRAME_STATE_TEXT` plus `health["device_detail_html"]` — never
`health["device_html"]`, which carries the verdict a second time.

New copy (UI-SPEC §B, R-04) for `companion/i18n_fr/nav.py`:
  "Screen on"       → "Écran allumé"
  "Screen off"      → "Écran éteint"
  "Quiet hours on"  → "Heures calmes activées"
  "Quiet hours off" → "Heures calmes désactivées"
  "Screen and quiet hours status — go to Home"
                    → "État de l'écran et des heures calmes — aller à l'accueil"
Everything else in §A and §C is reused copy: no other new key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: layout.frame_strip_html(), the return_to redirect, and Display's two switch cards</name>
  <files>companion/layout.py, companion/app.py, companion/pages/config_page.py, companion/static/style.css, companion/test_config_page.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/pages/config_page.py `quiet_hours_group()` (:1597-1789) and `display_group()` (:2033-2140) in full, plus the `QUICK_ACTION_*` constants (:433-446) and the Display-scope assembly in `render()` (:3327-3399)
    - companion/pages/home_page.py:239-317 (`_status_card_html()`) — only the headline computation at :254-274 is needed here
    - companion/layout.py: `stat_tile()` (:1460-1517), `status_dot()` (:1435-1457), `icon_html()`, `parse_iso()`, `age_seconds()`, `local_clock_text()`, the `QUICK_STATE_*` constants, `HOME_ROUTE`/`DISPLAY_ROUTE`, and the module docstring's import rule
    - companion/app.py `_handle_quick_toggle()` (:2787-2826) and `_referring_tab()` (:2376-2383) in full
    - .planning/phases/21-.../21-UI-SPEC.md §A (markup block, CSS block, the accent-surface and focus-order notes) and Structural Note 1
    - companion/static/style.css: `.stat-tile`, `.stat-tile--accent`, `.quick-action*`, `.quick-action-slot`, `.status-card__headline[--warn]`
    - companion/test_companion_app.py: the quick-toggle redirect checks (grep `QUICK_DISPLAY_ROUTE`/`QUICK_QUIET_HOURS_ROUTE`, ~2900-2965) and the EXPECTED_CHECK_COUNT trail (read the LAST assignment)
    - companion/test_config_page.py: every check naming `quick-action`, `QUICK_ACTION_`, or the two schedule cards, and its EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    Move the eleven `QUICK_ACTION_*` constants, `NEXT_UPDATE_TEMPLATE`,
    `EXPECTED_SINCE_TEMPLATE` and a new `FRAME_STRIP_HEADING = "Frame"` into
    `companion/layout.py` (values byte-identical; delete them from their old
    modules in this same commit and update every reference, including test
    references, to the `layout.` names).

    Add `layout.frame_strip_html(ctx, return_to, next_wake_iso=None)` — R-01 puts
    it here, not in a new page module. It renders exactly the UI-SPEC §A markup:
    `<div class="frame-strip stat-tile stat-tile--accent" aria-labelledby=…>` with
    an `<h2 class="text-heading">` carrying the strip heading, then
    `.frame-strip__cells` holding (1) the Screen `.quick-action` cell relocated
    byte-for-byte from `display_group()`, (2) the Quiet hours `.quick-action` cell
    relocated byte-for-byte from `quiet_hours_group()`, and (3)
    `.frame-strip__cell--update` carrying the next-update line. Each switch form
    gains ONE new hidden field, `<input type="hidden" name="return_to" value="…">`,
    with the value escaped. The next-update line reuses
    `.status-card__headline`/`--warn` verbatim, including the leading
    `<span class="dot dot--warn"></span>` on the overdue branch, and renders
    NOTHING when `next_wake_iso` is falsy or unparseable (never a fabricated
    "Next update ≈ —"). Do not route the strip through `layout.stat_tile()` — it
    borrows that function's CSS classes, not its builder (UI-SPEC §A). Do not add
    a second on/off dot inside a switch cell: `.quick-action--on/--off`'s left edge
    already is that signal. Every interpolated value crosses `escape_html()`, and
    every user-visible string crosses `i18n.t()` at its interpolation site. The
    strip is pure markup — no script, no inline handler.

    In `companion/app.py`'s `_handle_quick_toggle()`: read `return_to` from the
    form, membership-test it against `(layout.HOME_ROUTE, layout.DISPLAY_ROUTE)`,
    fall back to `layout.DISPLAY_ROUTE` when absent or unrecognised, and use the
    resolved value in ALL THREE `self.redirect(...)` calls (including the
    invalid-state early return and the save-failure branch). Nothing else in the
    handler changes; `_referring_tab()` is untouched.

    In `companion/pages/config_page.py`: delete the `quick_action_html` block and
    its `%`-slot from BOTH `quiet_hours_group()` and `display_group()` — presets,
    custom times, the scheduled checkboxes with their `form="settings-form"`
    attributes, the captions and the Save button all stay exactly as they are —
    and call `layout.frame_strip_html(ctx, return_to=layout.DISPLAY_ROUTE,
    next_wake_iso=wake.next_wake_at_iso(ctx.get("last_checkin_ts"),
    ctx.get("device_config")))` from `render()`'s Display scope, immediately after
    the page header and before the "Look" `section_intro_html()`. If the
    `QUICK_ACTION_APPLIES_SENTENCE` caption existed only inside the removed slots,
    it moves with them into the strip; do not leave an orphaned sentence in either
    card.

    In `companion/static/style.css`, add the UI-SPEC §A rules verbatim in intent:
    `.frame-strip`, `.frame-strip__cells`, `.frame-strip__cell.quick-action`,
    `.frame-strip__cell--update` and the `max-width: 699.98px` stacking rule —
    tokens only, zero new colour literals, zero new accent-reservation entries.

    Checks — `companion/test_config_page.py`: a Display render contains exactly one
    `.quick-action--on`/`--off` pair and it is inside `.frame-strip`; neither
    schedule card contains `quick-action`; both `/quick/display` and
    `/quick/quiet-hours` forms appear exactly once each and each carries a
    `return_to` hidden input whose value is the Display route; the rendered page
    still contains no nested `<form>` (keep the existing pinned check green); the
    strip renders immediately after the page header and before the first
    `section-intro`. `companion/test_companion_app.py`: a POST to `/quick/display`
    with `return_to=/` redirects to `/`; with `return_to=/display` redirects to
    `/display`; with a hostile value (`https://evil.example/`, `//evil.example`,
    `/flights`) or with the field absent redirects to `/display`; the same three
    assertions for `/quick/quiet-hours`; and the invalid-`state` early return also
    honours `return_to`. Re-derive both pins by RUNNING the harnesses and appending
    NEW last assignments commented `21-04-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py; "$PY" companion/test_companion_app.py; "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "frame_strip_html" companion/layout.py` outputs at least `1`; `grep -c "layout.frame_strip_html(" companion/pages/config_page.py` outputs `1`
    - `grep -c "quick-action" companion/pages/config_page.py` outputs `0`
    - `grep -c "layout.DISPLAY_ROUTE" companion/app.py` shows the handler's three literals replaced (inspect `_handle_quick_toggle` directly)
    - `grep -c "from server import wake\|server.wake" companion/layout.py` outputs `0`
    - `grep -c "frame-strip" companion/static/style.css` outputs at least `4`
    - `companion/test_config_page.py` and `companion/test_companion_app.py` report M/M against their new last `EXPECTED_CHECK_COUNT` (the latter with only its two documented root-sandbox FAILs); `companion/test_i18n.py` exits 0
  </acceptance_criteria>
  <done>One helper renders the strip, Display shows it once at the top, its schedule cards hold no instant switch, and a switch returns the user to the page they pressed it on.</done>
</task>

<task type="auto">
  <name>Task 2: The nav state reminder</name>
  <files>companion/layout.py, companion/app.py, companion/static/style.css, companion/i18n_fr/nav.py, companion/test_status_pages.py, companion/test_contrast_check.py</files>
  <read_first>
    - companion/layout.py: `sidebar_nav()` (:906-967), `_mobile_nav_html()` (:1057-1126), `page_shell()` (:1202-1274 as left by plan 21-01), `login_shell()` (:1129-1201), `_health_alert_markup()` (:871-903 — the keyword-with-default precedent and its docstring shape), `status_dot()` and `_STATUS_DOT_CLASSES`
    - companion/app.py `_page_shell_for()` (:2385-2405) and `page_context()`'s `device_config` key — the one call site that has the config available
    - .planning/phases/21-.../21-UI-SPEC.md §B (both markup blocks, the CSS, the contrast computation, the `.dot--off` value) and 21-CONTEXT.md R-03/R-04
    - companion/static/style.css: `.sidebar-title`, `.sidebar-link`, `.mobile-nav`, `.dot`, `.dot--ok`, `.dot-label`, and the `.quick-action--off` left-edge colour this reuses
    - companion/i18n_fr/nav.py (whole file) and companion/test_contrast_check.py (how a pairing is registered and its EXPECTED_CHECK_COUNT trail)
    - companion/test_status_pages.py: the nav/shell checks (including the footer-order check plan 21-01 rewrote) and its EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    Add `layout.nav_status_html(device_cfg)`: returns `""` when `device_cfg` is
    falsy; otherwise an `<a class="nav-status text-label" href="/"
    aria-label="…">` holding two dot+word segments separated by
    `<span class="nav-status__sep"> · </span>` — `.dot--ok` + "Screen on" or
    `.dot--off` + "Screen off", then `.dot--ok` + "Quiet hours on" or `.dot--off`
    + "Quiet hours off". Read the same two fields the strip reads
    (`display_enabled`, `quiet_hours_enabled`); every value crosses
    `escape_html()` and every string crosses `i18n.t()`. Its docstring must state
    that both nav renderers call this one body, so the strip and the reminder can
    never disagree (the convention `sidebar_nav()`'s own docstring already uses).
    No form, no button, no script (D-03).

    Thread `device_config=None` through `sidebar_nav()`, `_mobile_nav_html()` and
    `page_shell()` as a keyword-with-default — `None` means "no request context"
    and renders no reminder at all, so `login_shell()` and the error pages are
    byte-identical (the exact degrade contract `health_alert` already documents).
    Pass `device_config=ctx["device_config"]` from `app.py`'s `_page_shell_for()`
    — the one call site that covers both the GET path and the rejected-POST
    redisplay path.

    Place the reminder in the sidebar between the brand and `<nav class="sidebar-nav">`,
    and in the mobile dropdown as the first child of `#mobile-nav` before its
    `<nav>` — exactly UI-SPEC §B.

    In `companion/static/style.css` add `.nav-status`, `.nav-status:hover`,
    `.nav-status__sep` and the new `.dot--off` state, whose value is the same
    `color-mix(in srgb, var(--color-text) 30%, transparent)` `.quick-action--off`'s
    left edge already uses — a fourth entry in the existing dot vocabulary, never
    a new colour token.

    Add the five new keys to `companion/i18n_fr/nav.py` under a comment banner
    naming D-03/R-04, with the exact French values in the interfaces block above
    (fully French — "Heures calmes activées/désactivées", never "off").

    Checks — `companion/test_status_pages.py`: the sidebar and the mobile dropdown
    each contain exactly one `.nav-status` link, it is an `<a href="/">` with no
    `<form>` and no `<button>` inside, it sits after the brand and before the
    primary nav list in document order, its dot classes follow the four
    on/off combinations, and a French render reads "Écran allumé · Heures calmes
    désactivées" for a screen-on/quiet-off device; `page_shell(..., device_config=None)`
    renders no `.nav-status` at all and the login shell is unchanged.
    `companion/test_contrast_check.py`: register the reminder's text pairing
    (full-strength `--color-text` on `--color-secondary`) if that harness's shape
    expects every new text pairing to be listed — read it first; if `.dot--off` is
    a non-text indicator accompanied by its own word (it is), record in the SUMMARY
    that WCAG 1.4.11 is not the binding gate and add no bogus entry. Re-derive both
    pins by running the harnesses and appending NEW last assignments commented
    `21-04-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py; "$PY" companion/test_contrast_check.py; "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nav_status_html" companion/layout.py` outputs at least `3` (definition plus both renderer call sites)
    - `grep -c "device_config=" companion/app.py` shows `_page_shell_for()` passing it (inspect the call)
    - `grep -c "dot--off" companion/static/style.css` outputs `1`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Quiet hours off'])"` prints `Heures calmes désactivées`
    - `PY=...; "$PY" -c "import companion.layout as l; print(repr(l.nav_status_html(None)))"` prints `''`
    - `companion/test_status_pages.py` reports only the documented `anomaly_active()` FAIL; `companion/test_contrast_check.py` and `companion/test_i18n.py` exit 0
  </acceptance_criteria>
  <done>Both navs show one state reminder line linking to Home, computed from the same two fields the strip reads, and the login shell still renders without one.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild Home as strip → three tiles → picture beside recent flights</name>
  <files>companion/pages/home_page.py, companion/static/style.css, companion/i18n_fr/home.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/home_page.py in full (the current `_status_card_html()`, `_hero_figure_html()`, `_recent_flights_html()` and `render()`), noting which constants survive
    - `git show 614d41e~1:companion/pages/home_page.py` lines 166-218 — the phase-19 three-tile STRUCTURE to restore (never the body: it built the frame tile from `health["device_html"]`, which duplicated the verdict)
    - .planning/phases/21-.../21-UI-SPEC.md §C (the markup order, the `.home-columns.home-picture-row` 3fr/2fr rule, the deletion list and the "pretty" checklist) and 21-CONTEXT.md D-04/D-05/R-06
    - companion/layout.py `stat_tile()`'s signature and the `.dashboard-grid`/`.home-status-grid` breakpoint rules in style.css, plus `.home-hero`, `.status-card*`, `.home-columns`
    - companion/test_view_pages.py: the `.preview-frame`-before-`.status-card` document-order check (~3595-3603), the `_status_card_html()` headline checks (~3812-3838), the `FRAME_ROW_LABEL`/`BATTERY_ROW_LABEL`/`DATA_ROW_LABEL` count checks (~3683-3684, ~3789-3793), and the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    Replace `_status_card_html()` with a `_status_tiles_html(ctx)` that restores
    the phase-19 LAYOUT — three `layout.stat_tile(label, content_html, state,
    icon=…)` calls for Frame, Battery and Flight data inside
    `<div class="dashboard-grid home-status-grid">`, wrapped in
    `<section class="home-section" aria-labelledby="home-status">` with its
    `<h2 class="text-heading">` and the "See details on Health" link below the
    grid — while keeping the CURRENT, bug-fixed content derivation: each tile's
    `content_html` is built from the verdict/detail PAIR the status card computes
    today (`FRAME_STATE_TEXT`/`BATTERY_STATE_TEXT`/`DATA_STATE_TEXT` for the
    verdict, `health["…_detail_html"]`-derived text for the detail) and never from
    `health["device_html"]`. The frame verdict must appear exactly once in the
    whole page (this is the phase-20 regression this rebuild must not undo). The
    Health link is unconditional (plan 21-01 already removed its gate — do not
    re-add it).

    Rewrite `render()` as: page header → `layout.frame_strip_html(ctx,
    return_to=layout.HOME_ROUTE, next_wake_iso=wake.next_wake_at_iso(
    ctx.get("last_checkin_ts"), ctx.get("device_config")))` →
    `_status_tiles_html(ctx)` → `<div class="home-columns home-picture-row">` +
    `_hero_figure_html(...)` + `_recent_flights_html(...)` + `</div>`.
    `_hero_figure_html()` and `_recent_flights_html()` bodies are unchanged — only
    their wrapper and their neighbour change.

    Delete: `_status_card_html()`, Home's own next-update computation (it now
    lives in the strip helper), and any constant left with no consumer. If a
    `companion/i18n_fr/home.py` entry is orphaned by those deletions, delete it in
    THIS commit (Pitfall 5); the "Frame"/"Next update ≈ %s"/"Expected since %s"
    entries stay alive because Task 1 moved their English constants into
    `layout.py`, which `test_i18n.py` already scans — verify, do not assume.

    In `companion/static/style.css`: delete `.home-hero`, `.status-card__rows` and
    the rest of the `.status-card*` block EXCEPT `.status-card__headline` and
    `.status-card__headline--warn` (the strip reuses both, with their contrast
    gate untouched), and delete Home's `.preview-frame` rule only if the UI-SPEC's
    deletion list says so and nothing else consumes it (grep both the stylesheet
    and every page module first — `history_page.py` has an unrelated string guard).
    Add the one new modifier `.home-columns.home-picture-row` with
    `grid-template-columns: 3fr 2fr` at `min-width: 1024px` and one column below it.

    Checks — `companion/test_view_pages.py`: rewrite the document-order check to
    assert header → `.frame-strip` → `.home-status-grid` → `.home-picture-row`,
    with `.preview-frame` before `.recent-flight` inside the last one; retarget the
    headline checks (future/past/missing-checkin/missing-interval) at
    `layout.frame_strip_html()` instead of the deleted `_status_card_html()`; assert
    a rendered Home contains no `status-card__rows`, no `home-hero` and no
    `.quick-action` outside `.frame-strip`; assert exactly three `stat-tile`
    elements in `.home-status-grid` with the Frame/Battery/Flight data labels;
    assert the frame verdict sentence appears exactly once in the whole body; assert
    the Health link renders. Re-derive the pin by running the harness and appending
    a NEW last assignment commented `21-04-PLAN.md Task 3`. Finish by running every
    harness and `ruff check .`, and record in the SUMMARY which `.status-card*`
    rules you kept and why.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_status_pages.py && "$PY" companion/test_config_page.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_status_card_html\|home-hero" companion/pages/home_page.py` outputs `0`
    - `grep -c "layout.stat_tile(" companion/pages/home_page.py` outputs `3`
    - `grep -c "device_html" companion/pages/home_page.py` outputs `0`
    - `grep -c "layout.frame_strip_html(" companion/pages/home_page.py` outputs `1`
    - `grep -c "home-picture-row" companion/static/style.css` outputs at least `1`; `grep -c "status-card__headline" companion/static/style.css` outputs at least `2`
    - all four harnesses report M/M against their last `EXPECTED_CHECK_COUNT` (with only the documented pre-existing failures) and `ruff check .` is clean
  </acceptance_criteria>
  <done>Home reads strip, three tiles, then picture beside recent flights, with the frame verdict stated once and no quick-action widget outside the strip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /quick/display, POST /quick/quiet-hours | two instant state changes now posted from two different pages, carrying a client-supplied redirect target |
| `_handle_quick_toggle()` → HTTP 302 Location | a redirect target derived from form input |
| `ctx["device_config"]` → nav reminder and strip markup | stored device state rendered into every page's shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Tampering (open redirect) | the new `return_to` field in both quick-toggle forms | mitigate | the value is membership-tested against the two-element tuple `(layout.HOME_ROUTE, layout.DISPLAY_ROUTE)` before any use and falls back to `DISPLAY_ROUTE` otherwise — never string-prefix-matched, never parsed as a URL; pinned by checks for `https://evil.example/`, `//evil.example` and `/flights` in Task 1 |
| T-21-13 | Tampering (XSS) | the strip's and the reminder's interpolated state text, times and the escaped `return_to` value | mitigate | every interpolation crosses `layout.escape_html()`, translated strings included (a `t()` result is plain text, never pre-escaped); the quiet-hours times come from `device_config`'s already write-validated fields |
| T-21-14 | Elevation of Privilege | `/quick/*` posted from Home as well as Display | accept | both routes keep their existing `require_session()` gate and their existing field validation; only the page rendering the form and the redirect target changed |
| T-21-15 | Information Disclosure | the nav reminder exposing screen/quiet-hours state on every page | accept | the same two values already render on Home and Display for the same authenticated session; the shell is never rendered pre-session (`page_shell(device_config=None)` renders no reminder) |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_view_pages.py`, `companion/test_config_page.py`, `companion/test_contrast_check.py`, `companion/test_i18n.py` → M/M
- `companion/test_companion_app.py` → only the two documented root-sandbox FAILs
- `companion/test_status_pages.py` → only the documented `anomaly_active()` FAIL
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure
- `ruff check .` clean
- `<human-check>` (folded into plan 21-08's sweep): at 1280 px Home reads strip → three tiles → picture (wider) beside recent flights; at 390 px the same in one column; pressing a switch on Home returns to Home with the state changed, and on Display returns to Display; the nav shows one state line under the brand.
</verification>

<success_criteria>
- One `frame_strip_html()` body, called once by Home and once by Display, with a `return_to` hidden field in both switch forms
- `_handle_quick_toggle()` redirects to the whitelisted `return_to` in all three of its exits, defaulting to Display
- Exactly one `.quick-action--on/--off` pair per page, inside the strip; the two schedule cards keep everything else
- One `nav_status_html()` body feeding both nav renderers; `page_shell(device_config=None)` renders no reminder
- Home renders three `stat_tile()` tiles with the frame verdict stated exactly once, and a 3:2 picture/recent-flights row
- `.home-hero`, `.status-card__rows` and `_status_card_html()` are gone; `.status-card__headline[--warn]` survives for the strip
- Zero new colour tokens and zero new accent-reservation entries
- Five `EXPECTED_CHECK_COUNT` re-derivations by running the harnesses, appended citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-04-SUMMARY.md` when done.
</output>
</content>
