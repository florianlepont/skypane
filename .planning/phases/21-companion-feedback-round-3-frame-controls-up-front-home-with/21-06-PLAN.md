---
phase: 21
plan: 06
type: execute
wave: 4
depends_on: ["21-01", "21-04"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-24]

must_haves:
  truths:
    - "D-19: step B of the resolve flow (name saved, no artwork yet) renders the upload drop zone unconditionally — `upload_zone = _resolve_upload_form_html(upload_action, \"\")` with no `if edit_mode` guard"
    - "D-19: the lightbox's own no-artwork-yet upload affordance follows the same rule — `resolve_upload_html = _resolve_upload_form_html(\"\", \"-dialog\")` with no guard"
    - "D-20: the replace form, the lightbox replace form and both delete forms stay behind `edit_mode` — 'Change pictures' keeps only replace/delete of existing artwork"
    - "D-20/Pitfall 6: `_edit_toggle_html()` is NOT edited by this plan — plan 21-01 already deleted its simple-mode early-return, which is the single deletion site serving both D-17 and D-20"
    - "No markup, CSS or copy changes: `_resolve_upload_form_html()`'s own drop-zone markup, its surrounding card and every string inside it are untouched, and `panel-lookup.js` needs no change"
  artifacts:
    - path: "companion/pages/airlines_page.py"
      provides: "an unconditional upload affordance in both step-B surfaces"
      contains: "_resolve_upload_form_html"
  key_links:
    - from: "companion/pages/airlines_page.py"
      to: "the artwork upload route"
      via: "the unchanged upload form action"
      pattern: "_resolve_upload_form_html\\("
---

<objective>
Restore the picture upload as part of naming an unrecognised airline: drop the
two `if edit_mode` guards phase 19 put on the step-B upload zone (in the no-JS
resolve panel and in the lightbox), and leave every replace/delete affordance
gated exactly as it is.

Purpose: the developer's fifteenth feedback point — naming an airline and giving
it a picture is one job, and phase 19's `?edit=1` gating split it in two. The
"Change pictures" toggle keeps its own, narrower job.
Output: two one-line gate removals and the retargeted checks that pin the new
behaviour in both surfaces.
</objective>

<decisions_covered>
D-19, D-20 (the `_edit_toggle_html` gate half of D-20 was already delivered by
plan 21-01 — Pitfall 6: exactly one plan owns that deletion, and it is not this
one).
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-PATTERNS.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md

<files_owned>
This plan owns, for wave 4: `companion/pages/airlines_page.py` and
`companion/test_view_pages.py`. Plan 21-05 runs concurrently and owns
`companion/pages/config_page.py`, `companion/static/theme-preview.js`,
`companion/static/style.css`, `companion/i18n_fr/display.py`,
`companion/test_config_page.py`, `companion/test_companion_app.py` and
`companion/test_status_pages.py` — never edit any of those here.
`companion/test_i18n.py` is RUN, never edited. This plan adds no string, so no
`companion/i18n_fr/*` module is touched at all.
</files_owned>

<interfaces>
The four guards, file:line as read on 2026-09-12 (re-verify before editing):

  companion/pages/airlines_page.py:1727   delete_form = _manual_delete_form_html(...) if edit_mode else ""   # STAYS
  companion/pages/airlines_page.py:1733   upload_zone = _resolve_upload_form_html(upload_action, "") if edit_mode else ""   # GUARD DROPS (D-19)
  companion/pages/airlines_page.py:1228   resolve_upload_html = _resolve_upload_form_html("", "-dialog") if edit_mode else ""   # GUARD DROPS (D-19)
  companion/pages/airlines_page.py:1229-1230  replace_html / delete_html ... if edit_mode else ""   # STAY (D-20)

  companion/pages/airlines_page.py:1608-1743  _resolve_section_html(ctx, edit_mode=False)
  companion/pages/airlines_page.py:1160-1240  _lightbox_html(...)
  companion/pages/airlines_page.py:1829-1870  _edit_toggle_html() — DO NOT EDIT
  companion/static/panel-lookup.js             needs no change: its lookups are each
      behind their own `if (form)` guard and tolerate a form being present or absent

Checks to retarget in `companion/test_view_pages.py`:
  `_airlines_default_render_has_no_edit_only_forms` (~2903-2916) — currently asserts
      `airlines_page.RESOLVE_UPLOAD_ZONE_CLASS` is ABSENT from a default render;
      that token must now be PRESENT in step B
  `_airlines_edit_mode_render_has_exactly_one_of_each_edit_only_form` (~2932-2951) —
      verify its upload-zone count expectation still holds under edit_mode=True
  the lightbox-dialog equivalents (grep for the `-dialog` suffixed class names near
      ~2394-2398 and ~2725-2727)
Live EXPECTED_CHECK_COUNT: read the LAST assignment in the file — plans 21-01,
21-03 and 21-04 each appended one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the step-B upload guard in the resolve panel</name>
  <files>companion/pages/airlines_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/airlines_page.py `_resolve_section_html()` (:1608-1743) in full — both its step-A and step-B branches, the `edit_mode` parameter and each of the four things that parameter gates
    - companion/pages/airlines_page.py `_resolve_upload_form_html()` and the `RESOLVE_UPLOAD_ZONE_CLASS` constant (its markup and strings must not change)
    - .planning/phases/21-.../21-UI-SPEC.md §H (the exact before/after lines and the "no markup, CSS or copy changes" statement)
    - companion/test_view_pages.py: `_airlines_default_render_has_no_edit_only_forms` and `_airlines_edit_mode_render_has_exactly_one_of_each_edit_only_form` in full, plus the EXPECTED_CHECK_COUNT trail (read the LAST assignment)
  </read_first>
  <action>
    In `_resolve_section_html()`, change the step-B line
    `upload_zone = _resolve_upload_form_html(upload_action, "") if edit_mode else ""`
    to `upload_zone = _resolve_upload_form_html(upload_action, "")` — nothing else
    in the function changes. The `delete_form` line immediately above it keeps its
    `if edit_mode` guard (D-20). Leave a comment naming D-19 and stating that
    uploading a picture is part of naming an airline again, so the reason survives
    the next audit.

    In `companion/test_view_pages.py`: drop `RESOLVE_UPLOAD_ZONE_CLASS` from
    `_airlines_default_render_has_no_edit_only_forms`' asserted-absent tuple
    (leaving every other token in it asserted absent), and add a check that a
    default render (`edit_mode` absent) of an entry that has a saved name and no
    artwork contains exactly one upload zone and still contains zero manual-delete
    forms and zero replace forms. Confirm
    `_airlines_edit_mode_render_has_exactly_one_of_each_edit_only_form` still passes
    unchanged (the upload zone was already present under `edit_mode=True`); if its
    count assertion now double-counts, fix the count, not the intent, and say so in
    the SUMMARY. Re-derive `EXPECTED_CHECK_COUNT` by RUNNING the harness and
    appending a NEW last assignment commented `21-06-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_resolve_upload_form_html(upload_action" companion/pages/airlines_page.py` shows no `if edit_mode else` on that line
    - `grep -c "if edit_mode else" companion/pages/airlines_page.py` is exactly one lower than before this task
    - `grep -c "_manual_delete_form_html(_manual_delete_action(prefix)) if edit_mode else" companion/pages/airlines_page.py` outputs `1` (the delete guard survives)
    - `companion/test_view_pages.py` reports M/M against its new last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Naming an unrecognised airline offers the drop zone in step B without visiting `?edit=1`, and the delete form is still edit-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the same guard in the lightbox, keep replace and delete gated</name>
  <files>companion/pages/airlines_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/airlines_page.py `_lightbox_html()` (:1160-1240) in full, including the comment block at :1193-1200 describing `panel-lookup.js`'s own `if (form)` guards
    - companion/static/panel-lookup.js — enough to confirm every form lookup is guarded and tolerates presence or absence (no change is expected; if one is NOT guarded, stop and record it in the SUMMARY rather than editing a file this plan does not own)
    - companion/test_view_pages.py: the lightbox checks around ~2394-2398 and ~2725-2727 and the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    In `_lightbox_html()`, change
    `resolve_upload_html = _resolve_upload_form_html("", "-dialog") if edit_mode else ""`
    to `resolve_upload_html = _resolve_upload_form_html("", "-dialog")`. The
    `replace_html` and `delete_html` lines immediately after it keep their
    `if edit_mode` guards (D-20). Do not touch `_edit_toggle_html()` — plan 21-01
    already removed its only phase-21 change (Pitfall 6); confirm by reading that
    the function has no `simple_mode` reference left and record that confirmation
    in the SUMMARY.

    In `companion/test_view_pages.py`: retarget the lightbox check that asserted the
    dialog upload form's absence in a default render so it asserts presence; add a
    check that the same default render still contains zero lightbox replace forms
    and zero delete forms, and that an `edit_mode=True` render contains exactly one
    of each of the three. Re-derive `EXPECTED_CHECK_COUNT` and append a NEW last
    assignment commented `21-06-PLAN.md Task 2`. Finish by running the full suite
    and `ruff check .`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '_resolve_upload_form_html("", "-dialog")' companion/pages/airlines_page.py` shows no `if edit_mode else` on that line
    - `grep -c "simple_mode" companion/pages/airlines_page.py` outputs `0`
    - `grep -c "_lightbox_replace_form_html() if edit_mode else" companion/pages/airlines_page.py` outputs `1`
    - `git diff --stat` shows exactly two files changed by this plan
    - `companion/test_view_pages.py` and `companion/test_i18n.py` report M/M; `ruff check .` is clean
  </acceptance_criteria>
  <done>The lightbox offers the upload for an airline with no artwork yet, while replace and delete stay behind "Change pictures".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → the artwork upload POST | a multipart upload route whose form is now rendered in two more situations |
| rendered upload form → panel-lookup.js | a client script that looks the form up by selector and tolerates its absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-21 | Elevation of Privilege | the upload route reachable without `?edit=1` | accept | `?edit=1` was never an authorisation boundary — it is a query-string view toggle a user can type; the upload route keeps its existing `require_session()` gate and its existing file-type/size validation, neither of which this plan touches |
| T-21-22 | Tampering (malicious upload) | the artwork upload handler | accept | unchanged server-side validation: this plan only changes whether the FORM renders, never what the handler accepts; the handler's own rejection path (`_resolve_upload_form_html()`'s existing error rendering) is simply reachable more often |
| T-21-23 | Tampering (XSS) | the rendered upload form | mitigate | `_resolve_upload_form_html()`'s markup and escaping are byte-identical — this plan writes no new markup and no new string |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_view_pages.py` → M/M against its new last `EXPECTED_CHECK_COUNT`
- `companion/test_i18n.py` → M/M (this plan adds no string)
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure
- `ruff check .` clean
- `<human-check>` (folded into plan 21-08's sweep): on Airlines, naming an unrecognised airline shows the picture drop zone in step B without `?edit=1`; "Change pictures" still reveals replace and delete on an airline that already has artwork.
</verification>

<success_criteria>
- Both step-B upload guards dropped; both replace guards and both delete guards intact
- `_edit_toggle_html()` untouched by this plan and free of any `simple_mode` reference
- No markup, CSS, copy or `panel-lookup.js` change
- `companion/test_view_pages.py`'s pin re-derived by running the harness, appended once per task citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-06-SUMMARY.md` when done.
</output>
</content>
