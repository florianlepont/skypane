---
phase: 21
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - companion/pages/history_page.py
  - companion/layout.py
  - companion/app.py
  - companion/static/flight-rows.js
  - companion/static/list-filter.js
  - companion/static/style.css
  - companion/i18n_fr/flights.py
  - companion/test_view_pages.py
  - companion/test_companion_app.py
autonomous: true
requirements: [CFG-22]

must_haves:
  truths:
    - "D-15: the desktop Flights table has five data columns — When, Flight, Route, State, Corroboration — plus one toggle column whose header is a visually-hidden 'Details'"
    - "D-15: the When cell is a two-line cell-primary/cell-secondary pair (clock, then relative age); the Flight cell is a two-line pair (callsign, then 'airline · aircraft type')"
    - "D-15: the Corroboration cell renders the dot only — its label span carries visually-hidden, its accessible name and title are unchanged"
    - "D-15: the ICAO24 hex, the full ISO timestamp, the runway and the copy buttons render in a sibling detail <tr>, never in the summary row"
    - "D-15 no-JS floor: every detail <tr> renders with no hidden attribute and no inline style; flight-rows.js adds the collapsing class at load, so a page where this specific script never runs shows every detail row"
    - "R-12: flight-rows.js is registered through all six touch points and is ES5, with no inline handler, no inline <script> and no HTML-writing sink"
    - "R-12: list-filter.js hides and shows a filtered row's sibling detail row in lockstep with the summary row"
    - "D-16: the phone cards, the filter bar and the View-panel lightbox are byte-identical"
  artifacts:
    - path: "companion/static/flight-rows.js"
      provides: "the detail-row disclosure behaviour"
      contains: "data-row-toggle"
    - path: "companion/pages/history_page.py"
      provides: "the five-column table and its detail rows"
      contains: "flight-detail-row"
    - path: "companion/layout.py"
      provides: "status_dot()'s visually_hide_label keyword and the new script src constant"
      contains: "visually_hide_label"
  key_links:
    - from: "companion/app.py"
      to: "companion/layout.py"
      via: "the duplicated-not-imported static-script route/src pair"
      pattern: "FLIGHT_ROWS_SCRIPT_(ROUTE|SRC)"
    - from: "companion/pages/history_page.py"
      to: "companion/static/flight-rows.js"
      via: "the data-row-toggle / aria-controls contract"
      pattern: "data-row-toggle|aria-controls=\"flight-detail-"
---

<objective>
Compact the desktop Flights table to five columns that fit the real 880 px
content box in French and English, move the hex, the full ISO timestamp, the
runway and the copy buttons into an expandable detail row, and ship the small ES5
script that collapses those rows — with the no-JS floor being a fully visible
detail row, never a hidden one.

Purpose: the developer's twelfth feedback point — at 1280 px the table scrolls
sideways, which makes the one page a household member actually reads feel broken.
The budget is 880 px, not 1280 px (`1280 − 240 sidebar − 32 gap − 128 main
padding`), and French labels are the wider ones.
Output: a rewritten `_history_table_html()`, a new `companion/static/flight-rows.js`
registered through the six-touch-point contract, one scoped table-padding rule,
the detail-row styles, the French header entries, and retargeted checks.
</objective>

<decisions_covered>
D-15, D-16, R-12.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-PATTERNS.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan owns, for wave 2: `companion/pages/history_page.py`,
`companion/layout.py`, `companion/app.py`, `companion/static/flight-rows.js`,
`companion/static/list-filter.js`, `companion/static/style.css`,
`companion/i18n_fr/flights.py`, `companion/test_view_pages.py` and
`companion/test_companion_app.py`. Plan 21-02 runs concurrently and owns
`companion/pages/health_page.py`, `companion/static/freshness.js`,
`companion/i18n_fr/health.py` and `companion/test_status_pages.py` — never edit
any of those here. `companion/test_i18n.py` is RUN by both plans and edited by
neither. If a French key in `i18n_fr/health.py` ("Timestamp", "Corroboration") or
`i18n_fr/display.py` ("Callsign") turns out to be orphaned by this plan's header
change, STOP and record it in the SUMMARY — do not edit those files. (Expected
result: none is orphaned; Health's own table and Display still use them.)
</files_owned>

<interfaces>
Current shape, file:line as read on 2026-09-12 (re-verify before editing):

  companion/pages/history_page.py:141-143   _HEADERS = ("Timestamp", "Callsign",
        "Type", "Route", "State", "Corroboration")
  companion/pages/history_page.py:565-589   _merged_cell(primary, secondary) — the
        two-line cell-primary/cell-secondary pattern to reuse for When and Flight
  companion/pages/history_page.py:644-687   _callsign_hex_cell() — its copy buttons
        move into the detail row
  companion/pages/history_page.py:703-724   _type_airline_cell()
  companion/pages/history_page.py:777-805   _clock_cell_html() — the clock-only
        primary line; combine with layout.relative_age_text() for the second line
  companion/pages/history_page.py:807+      _history_table_html()
  companion/pages/history_page.py:867-991   _history_cards_html() — UNCHANGED (D-16);
        its <details> block at :952-978 is the exact content shape the detail row reuses
  companion/layout.py:1435-1457             status_dot(state, label, title=None)
  companion/layout.py:615                   relative_age_text(age_seconds, lang=None)
  companion/static/list-filter.js:82-93     applyFilter()'s per-row loop
        (`row.hidden = !matched;`, keyed on data-filter-group)

The six touch points to copy from theme-preview.js (PATTERNS, worked example):
  1. companion/app.py:187      THEME_PREVIEW_SCRIPT_ROUTE  → add FLIGHT_ROWS_SCRIPT_ROUTE = "/static/flight-rows.js"
  2. companion/app.py:582,1672 _THEME_PREVIEW_JS_PATH + _serve_theme_preview_script()
  3. companion/app.py:2479     the do_GET() dispatch line
  4. companion/layout.py:167   THEME_PREVIEW_SCRIPT_SRC    → add FLIGHT_ROWS_SCRIPT_SRC (same literal, duplicated-not-imported)
  5. companion/layout.py:1392  page_shell()'s script-tag tuple
  6. companion/test_companion_app.py:3491-3529  the route==src agreement check and
     the `<script src="%s" defer></script>` tag check to mirror

Flights/History checks live in `companion/test_view_pages.py` (152 `history_page.`
references). `test_status_pages.py:4995` renders history_page for a generic
secret-leak check and must keep passing untouched.

Exact new copy (UI-SPEC Copywriting Contract §F) — English constant, then the
French value for `companion/i18n_fr/flights.py`:
  "When"    → "Quand"
  "Flight"  → "Vol"
  "Details" → "Détails"   (the visually-hidden toggle-column header)
  "More"    → "Plus"
  "Less"    → "Moins"
Reused, already in the catalogue: "Route" → "Trajet", "State" → "Sens",
"Corroboration" → "Corroboration" (health.py), "Hex" → "Code hex",
"Full timestamp" → "Horodatage complet".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five columns, two-line cells, a dot-only Corroboration column</name>
  <files>companion/layout.py, companion/pages/history_page.py, companion/static/style.css, companion/i18n_fr/flights.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/history_page.py in full for `_HEADERS`, `_merged_cell()`, `_copy_button_html()`, `_callsign_hex_cell()`, `_type_airline_cell()`, `_clock_cell_html()`, `_history_table_html()` and `_history_cards_html()` (the last for reference only — it does not change)
    - the comment near `_HEADERS` stating the 880 px column budget and the A-36/D-19 history (the relative-age suffix was once removed for width — Pitfall 4: the new second line is STACKED, not an inline suffix; never reach for `layout.concise_timestamp_html()` here)
    - companion/layout.py `status_dot()` (:1435-1457) and `stat_tile()` (:1460-1517) — the latter for the "keyword-with-default, byte-identical when falsy" docstring contract to copy
    - .planning/phases/21-.../21-UI-SPEC.md §F: the column-width table, the markup block and the scoped padding rule
    - companion/static/style.css: the `.data-table` td/th padding rule, `.cell-primary`/`.cell-secondary`/`.cell-inline-sep`, `.visually-hidden`, `.data-table-wrap`
    - companion/i18n_fr/flights.py in full (its header comment records which shared keys live in other modules)
    - companion/test_view_pages.py: every check naming `_HEADERS`, the column count, `_clock_cell_html`, `_callsign_hex_cell` or `_type_airline_cell`, plus the EXPECTED_CHECK_COUNT trail (read the LAST assignment — plan 21-01 appended one)
  </read_first>
  <action>
    In `companion/layout.py`, add the keyword `visually_hide_label=False` to
    `status_dot()`: when true the label span's class becomes
    `"dot-label visually-hidden"`, everything else byte-identical (same dot class
    lookup, same escaped label, same optional title). Document it as
    "byte-identical when falsy" and name the one call site that passes True
    (Flights' desktop corroboration cell, UI-SPEC Structural Note 4). Health's
    call sites are not touched.

    In `companion/pages/history_page.py`, set
    `_HEADERS = ("When", "Flight", "Route", "State", "Corroboration")` and build
    the sixth `<th>` directly in `_history_table_html()` as
    `<th><span class="visually-hidden">Details</span></th>` (it has no data column
    and must not go through the `i18n.t(h)` header loop as a visible label).

    Rewrite `_history_table_html()`'s summary row to five data cells:
    - When: `_merged_cell()` with `_clock_cell_html()`'s clock text as the primary
      line and `layout.relative_age_text(layout.age_seconds(raw_ts, now))` as the
      secondary line — two stacked lines, never an inline suffix (Pitfall 4).
    - Flight: `_merged_cell()` with the callsign (mono) as the primary line and
      `"%s · %s" % (airline_label, aircraft_type_label)` as the secondary line.
      The copy buttons that `_callsign_hex_cell()` rendered inline leave this cell
      (Task 2 puts them in the detail row).
    - Route and State: unchanged escaped cells.
    - Corroboration: the existing `layout.status_dot(...)` call, now with
      `visually_hide_label=True` — the dot stays, the visible word goes, the
      `title` and the accessible name are unchanged.
    Retire `_callsign_hex_cell()` and `_type_airline_cell()` only if nothing else
    calls them (grep first — `_history_cards_html()` may); if the cards use them,
    leave both functions untouched and simply stop calling them from the table.
    `_history_cards_html()`, the filter bar and the lightbox are NOT edited (D-16).

    In `companion/static/style.css`, add the one scoped rule from UI-SPEC §F:
    `table.data-table--flights td, table.data-table--flights th { padding:
    var(--space-sm) var(--space-sm); }` — tokens only, scoped to this table so
    Health's and Airlines' tables are unaffected — and add `data-table--flights`
    to the table's class attribute in `history_page.py`.

    Add the new keys to `companion/i18n_fr/flights.py`: `"When": "Quand"`,
    `"Flight": "Vol"`, `"Details": "Détails"` (sentence case, U+2019 apostrophes,
    U+00A0 before `:` `;` `?` `!`), under a comment banner naming D-15. If
    `"Type": "Modèle"` or any other existing entry becomes orphaned because the
    table stopped using it, delete it from THIS file only, in this same commit;
    if the orphan is in a file this plan does not own, stop and report (see
    files_owned).

    In `companion/test_view_pages.py`: rewrite every check asserting the old
    six-header tuple or the old cell composition. Add checks — a seeded render's
    `<thead>` carries exactly six `<th>` in the order When/Flight/Route/State/
    Corroboration/visually-hidden Details; the corroboration cell contains
    `dot-label visually-hidden` and no visible corroboration word in the table
    slice (the phone cards may still show it); the When and Flight cells each
    carry one `cell-primary` and one `cell-secondary`; a French render's headers
    read Quand/Vol/Trajet/Sens/Corroboration; `status_dot()` without the keyword
    is byte-identical to its pre-change output (pin the default). Re-derive
    `EXPECTED_CHECK_COUNT` by RUNNING the harness and appending a NEW last
    assignment commented `21-03-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py; "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `PY=...; "$PY" -c "from companion.pages import history_page as h; print(h._HEADERS)"` prints `('When', 'Flight', 'Route', 'State', 'Corroboration')`
    - `grep -c "concise_timestamp_html" companion/pages/history_page.py` outputs `0`
    - `grep -c "visually_hide_label" companion/layout.py` outputs at least `2` and `grep -c "visually_hide_label=True" companion/pages/history_page.py` outputs `1`
    - `grep -c "data-table--flights" companion/static/style.css` outputs at least `1`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['When'], c.CATALOG['Flight'], c.CATALOG['More'] if 'More' in c.CATALOG else '-')"` prints `Quand Vol …`
    - `companion/test_view_pages.py` reports M/M with M equal to its new last `EXPECTED_CHECK_COUNT`; `companion/test_i18n.py` exits 0
  </acceptance_criteria>
  <done>The desktop table has five data columns with two-line When/Flight cells and a dot-only Corroboration column, and the phone cards are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The detail row and companion/static/flight-rows.js</name>
  <files>companion/pages/history_page.py, companion/static/flight-rows.js, companion/app.py, companion/layout.py, companion/static/style.css, companion/i18n_fr/flights.py, companion/test_view_pages.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/static/theme-preview.js in full (81 lines) — the IIFE/"use strict"/guard-clause header convention and the six-touch-point example
    - companion/pages/history_page.py `_history_cards_html()`'s `<details>` block (:952-978) — the exact dt/dd pairs, escaping and copy-button calls the detail row reuses
    - companion/app.py:187, :582, :1672-1678, :2479-2480 (theme-preview's four sites) and companion/layout.py:167, :1392
    - companion/test_companion_app.py:3480-3535 (the route==src check and the script-tag check to mirror) and the EXPECTED_CHECK_COUNT trail
    - .planning/phases/21-.../21-UI-SPEC.md §F: the detail-row markup, `.flight-detail-row*` CSS, the `--collapsed` class and the full no-JS-floor reasoning
  </read_first>
  <action>
    In `companion/pages/history_page.py`, emit one detail `<tr>` immediately after
    each summary row: `<tr class="flight-detail-row" id="flight-detail-{n}"
    data-row-detail><td colspan="6"><dl class="flight-detail-row__grid">…</dl></td></tr>`,
    where `{n}` is the same index the summary row's `data-filter-group` uses. Its
    content is the mobile card's own set, re-wrapped: Hex + its copy button, Full
    timestamp (the raw ISO) + its copy button, Runway, and the copy-name button —
    each `<dt>`/`<dd>` pair OMITTED entirely when its value is absent (never a
    fabricated "—", per the UI-SPEC's Empty/Error table). Reuse
    `_copy_button_html()` verbatim; every interpolated value keeps its
    `escape_html()` wrap.

    The summary row gains a final cell:
    `<button type="button" class="row-toggle" data-row-toggle aria-expanded="false"
    aria-controls="flight-detail-{n}" data-more-text="More" data-less-text="Less">More</button>`
    with both label strings escaped server-side through `i18n.t()` — the script
    only ever writes back a string the server already escaped. The detail `<tr>`
    carries NO `hidden` attribute and NO inline style: the no-JS floor is a
    visible detail row (D-15, locked).

    Create `companion/static/flight-rows.js` (R-12 — this exact filename): an ES5
    IIFE with `"use strict"`, a header comment naming D-15/R-12, the no-JS-floor
    reasoning and the served route. On load it adds `flight-detail-row--collapsed`
    to every `.flight-detail-row`; on click of a `[data-row-toggle]` it toggles
    that class on the row named by `aria-controls`, flips `aria-expanded`, and
    swaps the button's text between its own `data-more-text`/`data-less-text`
    attributes. Guard-clause out when either collection is empty. Hard
    requirements: no `let`/`const`/arrow function/template literal; no inline
    handler and no inline `<script>`; no `innerHTML`/`outerHTML`/`insertAdjacentHTML`/
    `document.write` (use `textContent` for the label swap); never key off a
    page-wide `.js` class — this script's own class-at-load is the only hide
    mechanism, so a page where only this script is blocked still shows every
    detail row.

    Register it through all six touch points, mirroring theme-preview.js exactly:
    `FLIGHT_ROWS_SCRIPT_ROUTE` and `_FLIGHT_ROWS_JS_PATH` + a
    `_serve_flight_rows_script()` delegate and its `do_GET()` dispatch line in
    `companion/app.py`; `FLIGHT_ROWS_SCRIPT_SRC` (the same literal, duplicated not
    imported) and its entry in `page_shell()`'s script-tag tuple in
    `companion/layout.py`; and the cross-file agreement check in
    `companion/test_companion_app.py`.

    In `companion/static/style.css`, add `.flight-detail-row td`,
    `.flight-detail-row__grid` (+ its `dt`/`dd` rules) and
    `.flight-detail-row--collapsed { display: none; }` exactly as UI-SPEC §F
    specifies — tokens only, no new colour literal.

    Add `"More": "Plus"` and `"Less": "Moins"` to `companion/i18n_fr/flights.py`.

    Checks — `companion/test_companion_app.py`: `layout.FLIGHT_ROWS_SCRIPT_SRC ==
    app.FLIGHT_ROWS_SCRIPT_ROUTE`; `page_shell()` emits
    `<script src="/static/flight-rows.js" defer></script>`; a real GET of the route
    returns 200 whose body contains `data-row-toggle` and `flight-detail-row--collapsed`
    and contains none of `innerHTML`, `document.write`, `=>`, ` let `, ` const `.
    `companion/test_view_pages.py`: a seeded render emits one detail `<tr>` per
    summary row with matching `aria-controls`/`id`; no detail row carries `hidden`
    or `style=`; the hex, the raw ISO timestamp and the runway appear inside the
    detail row and NOT in the summary row's slice; the toggle's `aria-expanded`
    starts `"false"`; the rendered page contains no inline `<script>` and no
    `on*=` handler attribute. Re-derive both harnesses' `EXPECTED_CHECK_COUNT` by
    running them and appending NEW last assignments commented `21-03-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py; "$PY" companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `ls companion/static/flight-rows.js` succeeds and `grep -cE "\b(let|const)\b|=>|innerHTML|document\.write|insertAdjacentHTML" companion/static/flight-rows.js` outputs `0`
    - `PY=...; "$PY" -c "import companion.app as a, companion.layout as l; print(a.FLIGHT_ROWS_SCRIPT_ROUTE == l.FLIGHT_ROWS_SCRIPT_SRC)"` prints `True`
    - `grep -c "flight-detail-row" companion/pages/history_page.py` outputs at least `2` and `grep -c 'hidden' companion/pages/history_page.py` shows no `hidden` on a detail row (inspect, do not just count)
    - `grep -c "flight-detail-row--collapsed" companion/static/style.css` outputs `1`
    - `companion/test_view_pages.py` and `companion/test_companion_app.py` report M/M (the latter with only its two documented root-sandbox FAILs)
  </acceptance_criteria>
  <done>Every flight row has a detail row carrying the hex, the ISO timestamp, the runway and the copy buttons; the new script collapses them at load and is served at its own route.</done>
</task>

<task type="auto">
  <name>Task 3: Filter the detail rows in lockstep, and prove the 880 px fit</name>
  <files>companion/static/list-filter.js, companion/pages/history_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/static/list-filter.js in full — `applyFilter()`'s per-row loop, its `data-filter-group` keying and its own header comment
    - companion/pages/history_page.py `_filter_bar_html()` and the summary row's `data-filter-text`/`data-filter-group` attributes
    - .planning/phases/21-.../21-UI-SPEC.md §F's column-width table (the 590 px content + 96 px padding = 686 px arithmetic against the 880 px ceiling) and 21-RESEARCH.md "Headless sweep mechanism"
  </read_first>
  <action>
    Extend `applyFilter()` in `companion/static/list-filter.js` so that, for each
    summary row it hides or shows, it applies the same `hidden` state to the
    sibling `tr.flight-detail-row` whose `id` matches that row's group
    (`flight-detail-{group}`) — looked up by id, never by DOM adjacency, and
    guarded with an `if (detail)` so every other page that uses this script is
    unaffected. ES5 only; no new sink; update the file's header comment to name
    D-15/R-12 and the one-line reason.

    Then measure, do not assume. Start the companion service against the seeded
    state directory and run the headless overflow probe described in
    21-RESEARCH.md's "Headless sweep mechanism" (adapt the scratchpad's
    `probe-ovf.js`/`probe-ovf-fr.js`: Playwright + the pinned Chromium
    executablePath, log in, switch language through the real
    `form[action="/ui-lang"]` control, load `/flights` at 1280×900 and assert
    `document.documentElement.scrollWidth <= window.innerWidth` and that the
    `.data-table-wrap` element's `scrollWidth <= clientWidth`) in BOTH languages.
    Record the exact command, the measured numbers and the verdict in the SUMMARY.
    If the probe cannot run in this worktree (no browser, no server), say so
    explicitly in the SUMMARY and fall back to the arithmetic check below — plan
    21-08 owns the binding sweep and needs to know which of the two happened.

    In `companion/test_view_pages.py`, add the static half of the same guarantee:
    a check pinning that `companion/static/style.css` contains the
    `table.data-table--flights` padding rule using `var(--space-sm)` on both axes
    (not a literal px value), and a check that the rendered table's `<thead>`
    contains exactly six cells in both `en` and `fr` — the two facts the width
    budget depends on that a Python harness can actually assert. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `21-03-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_companion_app.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flight-detail-" companion/static/list-filter.js` outputs at least `1`
    - `grep -cE "\b(let|const)\b|=>" companion/static/list-filter.js` outputs `0`
    - the SUMMARY records either the headless probe's measured `scrollWidth`/`innerWidth` pair per language, or an explicit statement that the probe could not run here
    - all three harnesses report M/M against their last `EXPECTED_CHECK_COUNT` (with only the documented pre-existing failures)
    - `ruff check .` clean
  </acceptance_criteria>
  <done>Filtering hides a row and its detail row together, and the 1280 px fit is measured or explicitly deferred to the phase sweep with a reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite history rows → table markup | callsigns, hex codes, airline names and aircraft types are third-party API strings rendered into both the summary and the detail row |
| browser → GET /static/flight-rows.js | a new static asset served to every authenticated page |
| flight-rows.js → table DOM | the script writes a class, an aria attribute and a button label |
| client filter input → row visibility | `list-filter.js` now also acts on a second row per group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering (XSS) | hex, callsign, airline, aircraft type, runway and ISO timestamp in the new detail row | mitigate | every value crosses `layout.escape_html()` at interpolation, exactly as the mobile card's existing `<details>` already does; the detail row adds no new value source, only a new render site |
| T-21-09 | Tampering (XSS via script sink) | `flight-rows.js`'s label swap | mitigate | the script writes `textContent` only, from `data-more-text`/`data-less-text` values the server already escaped; no `innerHTML`/`insertAdjacentHTML`/`document.write` — pinned by a served-bytes check in Task 2 |
| T-21-10 | Tampering (CSP) | the new `<script src>` registration | mitigate | registered through the same six-touch-point contract as every existing script, served from `companion/static/` by the existing `_serve_script_file()` delegate; no inline script and no inline handler is introduced anywhere (pinned by a render-side check) |
| T-21-11 | Information Disclosure | the detail row rendering the full ISO timestamp and hex by default when no script runs | accept | both values are already shown on the phone card and were previously visible desktop columns; the no-JS floor shows strictly less-hidden, never more, data than today |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_view_pages.py` → M/M
- `companion/test_companion_app.py` → only the two documented root-sandbox FAILs
- `companion/test_i18n.py` → M/M (new flights keys present, none orphaned)
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure
- `ruff check .` clean
- `<human-check>` (folded into plan 21-08's sweep): at 1280 px in FR and EN the Flights table has no horizontal scrollbar; clicking "More"/"Plus" expands one detail row and flips its label; the phone cards are unchanged.
</verification>

<success_criteria>
- Five data columns plus a visually-hidden-header toggle column; hex, ISO timestamp, runway and copy buttons only in the detail row
- Corroboration renders a dot with no visible word, and `status_dot()`'s default output is byte-identical for every other caller
- `companion/static/flight-rows.js` exists, is ES5, has no HTML-writing sink, and is registered at all six touch points with a passing route==src check
- Detail rows render visible without script; the collapsing class is added by that script at load
- `list-filter.js` hides a detail row with its summary row
- The phone cards, filter bar and lightbox are unchanged
- Three `EXPECTED_CHECK_COUNT` re-derivations by running the harnesses, each appended citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-03-SUMMARY.md` when done.
</output>
</content>
