---
phase: 21
slug: companion-feedback-round-3-frame-controls-up-front-home-with
status: draft
shadcn_initialized: false
preset: none
created: 2026-09-12
---

# Phase 21 — UI Design Contract

> Visual and interaction contract for: the Frame strip (Screen/Quiet-hours switches +
> next update) at the top of Home and Display, a nav state reminder, Home rebuilt as
> strip → three tiles → picture/recent-flights row, one "Frame colours" view replacing
> four theme chip grids, the Calendar card fused into one tile, a compact Flights
> table with an expandable detail row, Simple mode and the Health pause button
> removed, and the Airlines resolve-flow upload restored. Generated by
> `gsd-ui-researcher`, verified by `gsd-ui-checker`. Authoritative source of
> decisions: `21-CONTEXT.md` (D-01..D-20). This spec does not re-litigate any locked
> decision — it specifies the *shape* those decisions take in markup, copy, CSS and
> structure, resolves what `21-CONTEXT.md` left to "Claude's Discretion," and
> **supersedes** the parts of `20-UI-SPEC.md` named in each section below. Where the
> design-system skill (`sketch-findings-skypane`) and a prior spec disagree, the skill
> wins — every token, class and idiom below is read live from
> `companion/static/style.css` and the current `companion/pages/*.py`/`companion/
> layout.py` source, not from the phase 20 document's own prose.

**Non-interactive run:** every open question below is resolved with a stated default
and a one-line rationale, not left for the user. Seven are flagged explicitly (one of them, return_to, because
it touches server logic, not just markup) — see "Structural Notes for the Planner."

---

## What This Phase Supersedes in `20-UI-SPEC.md`

| Phase 20 artifact | Phase 21 disposition |
|---|---|
| Home hero row (`.home-hero`, `_hero_figure_html()` + `_status_card_html()`, `.status-card*` CSS) | **Deleted.** Replaced by §C below: strip → three `stat_tile()` tiles → a new two-column picture/recent-flights row. `.status-card__headline`/`.status-card__headline--warn` are **not** deleted — they relocate onto the Frame strip's next-update line (§A). `.status-card__rows`, `.home-hero`, `.preview-frame` (Home's copy) are deleted. |
| Display's Screen on/off and Quiet hours `.quick-action-slot` (inside `display_group()`/`quiet_hours_group()`) | **Moved**, not deleted — the same `.quick-action` markup relocates into the Frame strip (§A); the two cards keep everything else (presets, custom times, Save) unchanged. |
| Theme's `theme_fieldset()` card, the arrivals checkbox (`THEME_ARRIVING_CHECKBOX_LABEL`), the Calendar card's own compact chip grid, the separate `_rules_section_html()` "Flight colours" card | **Merged** into one new "Frame colours" card (§D). |
| Calendar's two-card split (`calendar_group()`'s `.page-section` + `calendar_connect_section()`'s own second `.page-section`, fused only at the bottom edge via `:has(+ .calendar-disconnect-form)`) | **Merged into one card** (§E) — status, URL/replace, how-it-works all in the same `.page-section`; the chip grid leaves this card entirely (moved to §D). |
| Flights desktop table (`_HEADERS`, `_history_table_html()`) | **Compacted** to 5 visible columns + a detail-row toggle (§F). Mobile cards (`_history_cards_html()`) are untouched. |
| Nav-footer "Simple · Full" switch (`layout._mode_form_html()`), `POST /ui-mode` | **Deleted** (§G). |
| Health "Pause updates" button, `freshness.js`'s `paused` flag | **Deleted** (§G). |
| Airlines resolve flow's `if edit_mode` gate on the Step-B upload zone | **Removed** (§H) — the gate itself, not the surrounding function. |

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-written, build-step-free, stdlib-only Python HTTP app |
| Preset | not applicable |
| Component library | none — `companion/static/style.css` is the sole authority for every token and component class |
| Icon library | this app's own inline `<svg>` symbol sheet (`layout.ICON_DEFS_HTML`/`layout.icon_html()`). Reused verbatim: `icon-power`, `icon-moon` (already used by the quick-action rows this phase relocates), `icon-check` (theme-chip/frame-colours check glyph). **No new icon.** |
| Font | `--font-ui` for every new control and row; `--font-serif` only on the Home/Display `<h1>` page titles (unchanged) and the section `<h2>`s per the existing serif-headings-only boundary — nothing here breaks it. The Frame strip's own heading ("Frame"/"Cadre") is a `.text-heading` like every other section heading; the next-update line uses the Emphasis role (Body 16px + semibold), never the retired `--font-display-size`. |

No shadcn gate applies (no `components.json`, not a React/Next/Vite stack). Do not propose a component library, CSS framework, or build step at plan or execute time.

---

## Spacing Scale

Reused as-is — zero new tokens:

| Token | Value | Usage in this phase |
|-------|-------|----------------------|
| xs | 4px | Frame-colours row internal gaps, dot margin, detail-row cell gaps |
| sm | 8px | Frame strip cell internal gap; Flights table's new reduced horizontal cell padding (§F); Frame-colours list row padding |
| md | 16px | Card padding below 960px; Frame-colours preview/list column gap on phones |
| lg | 24px | **The one universal sibling-card gap** — strip-to-tiles gap, tiles-to-picture-row gap, Frame-colours preview/list column gap at ≥960px |
| xl | 32px | Not used directly by this phase's new markup |
| 2xl | 48px | Not used directly by this phase's new markup |
| 3xl | 64px | Not used directly by this phase's new markup (referenced only in the Flights column-width arithmetic, §F, as `.dashboard-main`'s existing side padding) |

Exceptions: none new. This phase reuses the established 2px hairline-adjacent micro-tier nowhere new.

---

## Typography

Reused as-is — zero new sizes, zero new weights. Four sizes (14/16/22/32px) and two weights (400/600) remain exhaustive.

| Role | Size | Weight | Family | Where it appears in this phase |
|------|------|--------|--------|----------------------------------|
| Page title | 32px | semibold | serif | Unchanged — "Home", "Display" `<h1>`s |
| Section heading | 22px | regular | serif | The Frame strip's own `<h2>` ("Frame"/"Cadre"); "Frame colours" card `<h2>`; the merged Calendar card `<h2>` (unchanged) |
| Nested card title | 16px | semibold | sans | Unaffected — Display's three supersections (Look/What it watches/When it is on) and their nested cards keep the phase 20 tier exactly |
| Emphasis | 16px | semibold | sans | **The strip's next-update line** (reusing `.status-card__headline`/`--warn` verbatim — see §A); the Frame-colours row's theme name/"Same as departures"/"N rules" text; the Flights table's Flight-column callsign line |
| Body | 16px | regular | sans | Frame-colours preview caption; Calendar's masked-URL line; Flights table's Route/State cell text |
| Label voice | 12px, uppercase, 0.06em tracking | semibold | sans | `.quick-action__label` (unchanged, third+fourth consumers now both inside the strip); Frame-colours row meta text is NOT this role (see Emphasis above) — only the row's own small caption-weight elements (e.g. the swatch's `aria-hidden` dots have no text) |
| Section caption | 14px (`.text-label.section-caption`) | regular, 70%-muted | sans | "Applies the next time the frame wakes up." sentences inside the strip's two switch cells; every existing group caption unaffected |
| Sub-scale (`.cell-secondary`) | 13px | regular, 75%-opacity | mono | Flights table's Flight-column second line (airline · aircraft type) and When-column second line (relative age) — the exact existing `.cell-secondary` token, no new sub-scale value |

**Warn-state colour gate, inherited unchanged.** The strip's next-update line reuses `.status-card__headline--warn` byte-for-byte from phase 20's own shipped CSS (`companion/static/style.css`, already-run `contrast_check.py` gate: light measured 3.19:1 — below WCAG AA — so the rule stays on `--color-text` in both themes; the overdue state is carried by the wording ("Expected since…") plus a leading `dot--warn`, never by text colour). Nothing in this phase reopens that gate.

---

## Color

Reused as-is — zero new colors, zero new accent uses, zero new reservations.

| Role | Value | Usage in this phase |
|------|-------|----------------------|
| Dominant (60%) | `--color-dominant` | Frame-colours card surface, merged Calendar card surface, Flights detail-row surface |
| Secondary (30%) | `--color-secondary` | Sidebar/nav surface, now also the backdrop the new nav-status reminder sits on |
| Accent (10%) | `--color-accent` | **Zero new reservations.** The Frame strip's own accent-bordered surface reuses `stat-tile--accent`'s existing top-border/accent-surface entry verbatim (D-05's own instruction: "no new colour tokens; the strip uses the accent-bordered surface"). The Frame-colours radiogroup rows reuse the exact `.theme-form .theme-option--active` 12%-wash idiom (already reserved) for the *selected row*, and the chip grid's own selected-chip mechanism (already reserved) for the selected theme — no third accent use is introduced. |
| Destructive | not used | Calendar's small "Disconnect" button and every rule "Remove" button stay on the plain quiet-button treatment (no red) — matching this app's unbroken precedent (15-UI-SPEC.md, reaffirmed by 20-UI-SPEC.md's own Color table) |
| Status ok/warn/error | `--color-status-ok/warn/error` | The strip's per-switch on/off dot (reusing `.quick-action--on/--off`'s existing left-edge colours, unchanged); the nav-status reminder's two dots (reusing `.dot--ok`/a new grey "off" dot — see §B); the Corroboration column's dot (unchanged, now the column's *only* visible content — see §F) |
| Theme swatches | `_palette_hex(theme[...])` | Every swatch in the Frame-colours row list and the compact chip grid — computed from `server.panel_format.PALETTE_RGB`, never a hardcoded hex |

**Accent-reservation contract:** this phase adds **zero new entries** to `style.css`'s header-comment list. Every accent use above is a pre-existing consumer of an already-closed entry.

---

## Copywriting Contract

Locked-English module constants (source language), each with its French catalogue entry per the established D-09 discipline (sentence case, typographic apostrophe `'`, non-breaking space before `:` `;` `?` `!`). Existing catalogue keys are marked **(reused, no new key)** — this phase reuses far more copy than it invents, since D-01/D-02/D-13..D-16 are explicitly relocations of existing strings.

### A. Frame strip (D-01..D-05)

| Element | English | French | Source |
|---|---|---|---|
| Strip heading | "Frame" | "Cadre" | **(reused)** `home_page.FRAME_ROW_LABEL` |
| Screen label | "Screen" | "Écran" | **(reused)** `config_page.QUICK_ACTION_SCREEN_LABEL` |
| Screen state, on | "On" | "Allumé" | **(reused)** `config_page.QUICK_ACTION_ON_TEXT` |
| Screen state, off | "Off" | "Éteint" | **(reused)** `config_page.QUICK_ACTION_OFF_TEXT` |
| Screen button, currently on | "Switch off" | "Éteindre" | **(reused)** `QUICK_ACTION_SWITCH_OFF_BUTTON` |
| Screen button, currently off | "Switch on" | "Allumer" | **(reused)** `QUICK_ACTION_SWITCH_ON_BUTTON` |
| Quiet-hours label | "Quiet hours" | "Heures calmes" | **(reused)** `QUICK_ACTION_QUIET_LABEL` |
| Quiet-hours state, on | "On — %s to %s" | "Allumé — de %s à %s" | **(reused)** `QUICK_ACTION_QUIET_ON_TEMPLATE`; the exact French value already shipped — verified in `companion/i18n_fr/display.py` |
| Quiet-hours state, off | "Off" | "Éteint" | **(reused)** `QUICK_ACTION_QUIET_OFF_TEXT` |
| Quiet-hours button, currently on | "Turn off" | "Désactiver" | **(reused)** `QUICK_ACTION_QUIET_TURN_OFF_BUTTON` |
| Quiet-hours button, currently off | "Turn on" | "Activer" | **(reused)** `QUICK_ACTION_QUIET_TURN_ON_BUTTON` |
| Applies sentence | "Applies the next time the frame wakes up." | "S'applique la prochaine fois que le cadre se réveille." | **(reused)** `QUICK_ACTION_APPLIES_SENTENCE` |
| Next update, on time | "Next update ≈ %s" | "Prochaine mise à jour ≈ %s" | **(reused)** `home_page.NEXT_UPDATE_TEMPLATE` |
| Next update, overdue | "Expected since %s" | "Attendue depuis %s" | **(reused)** `home_page.EXPECTED_SINCE_TEMPLATE` |

Every string in this table already exists in the catalogue — the strip is a **relocation**, not new copy. No `test_i18n.py` growth is expected from §A alone.

### B. Nav state reminder (D-03) — new copy

| Element | English | French | Note |
|---|---|---|---|
| Screen segment, on | "Screen on" | "Écran allumé" | New composed key (`layout.NAV_SCREEN_ON_TEXT`) |
| Screen segment, off | "Screen off" | "Écran éteint" | New composed key (`layout.NAV_SCREEN_OFF_TEXT`) |
| Quiet-hours segment, on | "Quiet hours on" | "Heures calmes activées" | New composed key (`layout.NAV_QUIET_ON_TEXT`) — see rationale below |
| Quiet-hours segment, off | "Quiet hours off" | "Heures calmes désactivées" | New composed key (`layout.NAV_QUIET_OFF_TEXT`) |
| Accessible name (link) | "Screen and quiet hours status — go to Home" | "État de l'écran et des heures calmes — aller à l'accueil" | `aria-label` on the `<a>` (see §B markup) — the visible text is the two dot+word segments; the link's accessible name states where it goes, since "Screen on · Quiet hours off" alone does not read as a link destination |

**Assumption, stated explicitly (no user available to confirm):** `21-CONTEXT.md`'s own D-03 text gives the French rendering as "Écran allumé · Heures calmes **off**" — leaving the second word untranslated. That is inconsistent with this app's own shipped French catalogue, where "Screen"/"Quiet hours" state pairs are always fully translated (`QUICK_ACTION_QUIET_OFF_TEXT` → "Éteint", never "off"). This spec corrects that apparent drafting artifact and uses fully-French copy throughout, choosing "activées/désactivées" (rather than "allumées/éteintes") for the Quiet-hours pair specifically because it agrees with the verb pair this app already ships for the same toggle ("Activer"/"Désactiver" — `QUICK_ACTION_QUIET_TURN_ON_BUTTON`/`_OFF_BUTTON`), keeping one consistent "activate/deactivate" metaphor for Quiet hours and one consistent "on/off, lit/unlit" metaphor for Screen, rather than forcing both toggles onto the same verb.

### C. Home rebuild (D-04) — reused copy only

No new strings. `STATUS_HEADING`/`FRAME_ROW_LABEL`/`BATTERY_ROW_LABEL`/`DATA_ROW_LABEL`/`FRAME_STATE_TEXT`/`BATTERY_STATE_TEXT`/`DATA_STATE_TEXT`/`HEALTH_LINK_TEXT`/`RECENT_FLIGHTS_HEADING`/`RECENT_FLIGHTS_LINK_TEXT`/`NO_FLIGHTS_HEADING`/`NO_FLIGHTS_BODY`/`RENDERED_CAPTION_TEMPLATE`/`NO_PANEL_HEADING`/`NO_PANEL_BODY` are the exact phase-19/20 constants, re-consumed by the phase-19-shaped `_status_tiles_html()` this phase restores (git `614d41e~1:companion/pages/home_page.py:166-218`) and the unchanged `_hero_figure_html()`/`_recent_flights_html()` bodies.

### D. Frame colours (D-06..D-12) — new row copy, reused chip/theme copy

| Element | English | French | Source |
|---|---|---|---|
| Card heading | "Frame colours" | "Couleurs du cadre" | New — matches CFG-20's own PRD wording exactly |
| Row: departures | "Departures" | "Départs" | New |
| Row: arrivals | "Arrivals" | "Arrivées" | New |
| Row: calendar | "Calendar flights" | "Vols du calendrier" | New |
| Row: rules | "Per-flight rules" | "Règles par vol" | New |
| Row meta, unset (arrivals/calendar) | "Same as departures" | "Comme les départs" | New — also the chip label (below) |
| Row meta, rules, N > 0 | "%d rules" (singular "1 rule") | "%d règles" (singular "1 règle") | New |
| Row meta, rules, empty | "No rules yet" | "Aucune règle pour l'instant" | New |
| Chip: same-as-departures | "Same as departures" | "Comme les départs" | New — first chip in the Arrivals/Calendar grids only |
| Preview caption, live flight | "Preview with your last flight: %s" | "Aperçu avec votre dernier vol : %s" | **(reused)** `config_page.THEME_LIVE_PREVIEW_CAPTION_WITH_FLIGHT_TEMPLATE` |
| Preview caption, sample | "Preview with a sample flight" | "Aperçu avec un vol d'exemple" | **(reused)** `THEME_LIVE_PREVIEW_CAPTION_SAMPLE` |
| Every theme name, chip label, "Selected" glyph text | unchanged | unchanged | **(reused)** `_theme_chip_grid_html()` output verbatim |
| Rule add form, kind labels, "Add rule", kind badges, "Remove", empty-state, "How rules combine" | unchanged | unchanged | **(reused)** `_rule_add_form_html()`/`_rule_row_html()`/`_rules_section_html()` constants verbatim — this content *relocates* into the rules row's panel, it is not rewritten |

### E. Calendar (D-13/D-14) — reused status/URL copy, one relocated string

| Element | English | French | Source |
|---|---|---|---|
| Status verdicts/detail, "Connect calendar", the "How it works" disclosure, the field label/hint | unchanged | unchanged | **(reused)** `calendar_group()`/`calendar_connect_section()` constants verbatim |
| "Replace the feed URL" (disclosure summary) | "Replace the feed URL" | "Remplacer l'URL du flux" | **(reused)** `CALENDAR_REPLACE_URL_SUMMARY` — already shipped with exactly this French text |
| Connect/Replace button, connected state | "Replace" | "Remplacer" | **New label variant** — see markup note in §E: the button already reads "Connect calendar"/"Connecter le calendrier" when not connected; inside the Replace disclosure it must say the shorter "Replace"/"Remplacer" per D-14's own wording, so `calendar_connect_section()` needs a second button-text constant, `CALENDAR_REPLACE_BUTTON_TEXT` |
| "Disconnect" | "Disconnect" | "Déconnecter" | **(reused)** `CALENDAR_DISCONNECT_CHECKBOX_LABEL`'s existing button text verbatim |

### F. Compact Flights table (D-15/D-16)

| Element | English | French | Source / note |
|---|---|---|---|
| Column: When | "When" | "Quand" | New header key |
| Column: Flight | "Flight" | "Vol" | New header key — matches `_rule_kind_radio_html()`'s existing "Flight"/"Vol" pair used elsewhere on Display, so this is the same word, not a new coinage |
| Column: Route | "Route" | "Trajet" | **(reused)** — `companion/i18n_fr/flights.py` already maps `"Route": "Trajet"` |
| Column: State | "State" | "Sens" | **(reused, corrected against the task brief)** — `companion/i18n_fr/flights.py` already maps `"State": "Sens"` (this column shows Departing/Arriving, i.e. *direction*, so the shipped translator chose "Sens" over "État" — this spec keeps the real shipped word, not the task brief's own guessed "État") |
| Column: Corroboration | "Corroboration" | "Corroboration" | **(reused)** `companion/i18n_fr/health.py` — identical word in both languages |
| Detail toggle, closed | "More" | "Plus" | New |
| Detail toggle, open (accessible-name change only — see §F) | "Less" | "Moins" | New |
| Detail row labels: Hex, Full timestamp, Runway, Copy | unchanged | unchanged | **(reused)** `_COPY_HEX_LABEL`, `history_page`'s existing `"Hex"`/`"Full timestamp"`/`runway_label()` output, `_copy_button_html()` |

### G. Removals (D-17/D-18)

No new copy. Deleted strings (must be removed from both the module and every `companion/i18n_fr/*` catalogue entry that names them): `layout._mode_form_html()`'s "Simple mode"/"Simple"/"Full" trio; `health_page.REFRESH_PAUSE_TEXT`/`REFRESH_RESUME_TEXT`; `CALENDAR_HOW_IT_WORKS_SIMPLE`/`RULES_HOW_RULES_COMBINE_SIMPLE`.

### H. Airlines upload restore (D-19/D-20)

No new copy — `_resolve_upload_form_html()`'s own existing strings render unconditionally now; nothing in the string itself changes.

### Destructive confirmations

| Action | Confirmation copy |
|---|---|
| Remove a flight-colour rule | Unchanged from phase 20 — no dialog, immediately reversible (re-adding the same key restores it) |
| Disconnect calendar | Unchanged from phase 20 (two-step confirm: native `confirm()` plus the server-rendered fallback page) — this phase only changes the button's surrounding card, never the confirmation mechanism |

---

## Section Anatomy

### A. The Frame strip (D-01, D-02, D-05) — new, shared

One helper, one write site: **`layout.frame_strip_html(ctx, return_to)`** (Claude's Discretion resolved: `layout.py`, not a new `pages/frame_strip.py` — the strip reads `ctx["device_config"]` exactly like `status_dot()`/`stat_tile()` already do from this module, and a new page module would need to import `wake.next_wake_at_iso()`, which `layout.py` can do without creating a page-to-page import; `home_page.py` and `config_page.py` both already import `layout` for everything else the strip needs). Called identically by `home_page.render()` (with `return_to=layout.HOME_ROUTE`) and `config_page.render()`'s Display scope (with `return_to=layout.DISPLAY_ROUTE`) — one function body, so the two pages' copies can never diverge (D-01's own requirement).

```html
<div class="frame-strip stat-tile stat-tile--accent" aria-labelledby="frame-strip-heading">
  <h2 id="frame-strip-heading" class="text-heading">Frame</h2>
  <div class="frame-strip__cells">

    <div class="frame-strip__cell quick-action quick-action--on">
      <div class="quick-action__text">
        <span class="text-label quick-action__label">{icon-power}Screen</span>
        <span class="text-body quick-action__state">On</span>
      </div>
      <form method="post" action="/quick/display" class="quick-action__form">
        <input type="hidden" name="state" value="off">
        <input type="hidden" name="return_to" value="/">
        <button type="submit">Switch off</button>
      </form>
    </div>

    <div class="frame-strip__cell quick-action quick-action--off">
      <div class="quick-action__text">
        <span class="text-label quick-action__label">{icon-moon}Quiet hours</span>
        <span class="text-body quick-action__state">Off</span>
      </div>
      <form method="post" action="/quick/quiet-hours" class="quick-action__form">
        <input type="hidden" name="state" value="on">
        <input type="hidden" name="return_to" value="/">
        <button type="submit">Turn on</button>
      </form>
    </div>

    <div class="frame-strip__cell frame-strip__cell--update">
      <p class="status-card__headline">Next update ≈ 22:25</p>
    </div>

  </div>
</div>
```

```css
/* Reuses .stat-tile/.stat-tile--accent for the card surface itself
 * (padding, border-radius, hairline, accent top-border) — zero new card
 * declarations. This rule adds only the internal 3-cell flex layout. */
.frame-strip {
  margin-bottom: var(--space-lg);
}
.frame-strip__cells {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: var(--space-lg);
}
.frame-strip__cell.quick-action {
  flex: 1 1 220px;   /* the two switch cells share the row, wrap to their own line first */
}
.frame-strip__cell--update {
  flex: 1 1 160px;
  min-width: 0;
}
@media (max-width: 699.98px) {
  .frame-strip__cells {
    flex-direction: column;
    align-items: stretch;
  }
  .frame-strip__cell.quick-action,
  .frame-strip__cell--update {
    flex-basis: auto;
  }
}
```

- **Card surface:** `.stat-tile.stat-tile--accent` reused verbatim — this is the literal instruction in D-05 ("the strip uses the accent-bordered surface (`stat-tile--accent`) so it reads as the 'control' area"). The strip's own `<h2>` sits where `.stat-tile__caption` normally would, but at the Section-heading role (22px serif), not the Label-voice caption role — this is a deliberate, singular exception: the strip is not one stat tile among three (it has its own two interactive controls and a headline, not one verdict), so it earns the same heading tier every other `.page-section`/`.theme-status` card uses, not the smaller caption tier a plain `stat_tile()` call would give it. This is why the strip is a hand-built `<div>`, not a `layout.stat_tile()` call — it borrows that function's CSS class, not its Python builder.
- **Switch cells:** `.quick-action`/`.quick-action--on`/`--off` reused byte-for-byte from the relocated `display_group()`/`quiet_hours_group()` markup (D-05's explicit instruction to "reuse the existing quick-action button styles") — same left-edge colour, same icon, same label/state/button anatomy. The only addition is the `return_to` hidden field (see Structural Notes) and the `.frame-strip__cell` layout-only class alongside `.quick-action`.
- **Next-update cell:** reuses `.status-card__headline`/`.status-card__headline--warn` verbatim from phase 20's Home status card (now deleted elsewhere, but its CSS rule and contrast gate are kept — see Typography above). This is the "largest text in the strip" D-01 asks for: 16px semibold versus the switch cells' 16px-regular state text and 12px label — the visual hierarchy comes from *font-weight and being the only un-buttoned cell*, not from a bigger font size (there is no larger role left in the four-size scale to reach for).
- **On/off dot idiom:** the switch cells do **not** carry a separate dot — `.quick-action--on`/`--off`'s own 4px left-edge colour (status-ok / 30%-muted-text) already *is* this app's on/off idiom for this exact component, unchanged since phase 18. Do not add a second dot inside `.quick-action__text`; that would be two competing state signals on one control (the same anti-duplication principle `status_row()`'s own docstring already states for verdict/detail).
- **Placement:** directly under `page_header()`'s output, before anything else, on both Home (`home_page.render()`) and Display (`config_page.render()`, `scope=SCOPE_DISPLAY`, before the "Look" `section_intro_html()`).
- **Focus order:** Screen's switch button, then Quiet hours' switch button, then nothing (the next-update cell is read-only text, no focusable element) — matching source order top-to-bottom, left-to-right, the same order a sighted user scans the row in.

### B. Nav state reminder (D-03) — new

One line, both nav renderers, between the brand (`site-title`) and the primary nav list:

```html
<!-- sidebar_nav(), inside <aside class="dashboard-sidebar"> -->
<span class="site-title sidebar-title">SkyPane</span>
<a class="nav-status text-label" href="/"
   aria-label="Screen and quiet hours status — go to Home">
  <span class="dot dot--ok"></span><span class="dot-label">Screen on</span>
  <span class="nav-status__sep"> · </span>
  <span class="dot dot--off"></span><span class="dot-label">Quiet hours off</span>
</a>
<nav class="sidebar-nav" aria-label="Primary navigation">…</nav>
```

```html
<!-- _mobile_nav_html(), first child of the dropdown panel, before <nav> -->
<div id="mobile-nav" class="mobile-nav">
  <a class="nav-status text-label" href="/" aria-label="…">…</a>
  <nav class="mobile-nav__nav" aria-label="Primary navigation">…</nav>
  …
</div>
```

```css
.nav-status {
  display: block;
  padding: 0 var(--space-sm) var(--space-sm);
  color: var(--color-text);
  text-decoration: none;
}
.nav-status:hover { text-decoration: underline; }
.nav-status__sep {
  color: color-mix(in srgb, var(--color-text) 70%, transparent);
  margin: 0 2px;
}
```

- **New dot state:** `.dot--off` — a grey/muted dot for "quiet hours off" and "screen off," visually distinct from the existing three status dots (ok/warn/error) since "off" here is a *neutral*, not a *problem*, state. Reuses the same 30%-muted-text mix `.quick-action--off`'s own left edge already uses (`color-mix(in srgb, var(--color-text) 30%, transparent)`), so this is styling continuity, not a fourth genuinely new colour: `.dot--off { background: color-mix(in srgb, var(--color-text) 30%, transparent); }`. "Screen on"/"Quiet hours on" use the existing `.dot--ok` verbatim.
- **Contrast:** the reminder's visible text is full-strength `--color-text` on `--color-secondary` (the sidebar surface) — the same pairing `.sidebar-link`'s own unmodified colour rule already relies on site-wide. Computed (WCAG relative-luminance formula, both themes' `:root` hex literals): **light `#17191F` on `#EEE8DE` = 14.42:1**; **dark `#F1F3F6` on `#1C222D` = 14.35:1**. Both clear the ≥4.5:1 floor with wide margin; no muted/70% treatment is used for this text specifically because that would fail to reuse an already-validated pairing for no reason.
- **Mobile dropdown placement:** first child inside `#mobile-nav`, above `<nav class="mobile-nav__nav">` — mirrors the sidebar's "under the brand, above the list" position exactly, since the mobile header's own brand (`<span class="site-title">`) sits outside the dropdown panel in the header row; the reminder is the first thing a user sees on opening the dropdown.
- **No form, no button** — a plain `<a>`, matching the phase-18 audit finding D-03 cites ("no actions in the nav").
- **Server-side only:** built by the same helper both `page_shell()` call sites already invoke (`sidebar_nav()`, `_mobile_nav_html()`) — reads `ctx["device_config"]`'s `display_enabled`/`quiet_hours_enabled` fields, the exact same two fields the strip reads, so the two can never disagree (there is only one source of truth).

### C. Home rebuild (D-04) — restores the phase-19 shape, replaces the phase-20 hero

```html
{page_header("Home", purpose="Your frame at a glance.")}
{frame_strip_html(ctx, return_to="/")}

<section class="home-section" aria-labelledby="home-status">
  <h2 class="text-heading" id="home-status">Status</h2>
  <div class="dashboard-grid home-status-grid">
    <!-- three stat_tile() calls: Frame / Battery / Flight data,
         byte-identical to git 614d41e~1:companion/pages/home_page.py
         _status_tiles_html(), lines 166-218, with phase 20's own
         FRAME_STATE_TEXT/BATTERY_STATE_TEXT/DATA_STATE_TEXT wording -->
  </div>
  <p class="text-label"><a href="/health">See details on Health</a></p>
</section>

<div class="home-columns home-picture-row">
  {_hero_figure_html(ctx, current_flight_row)}   <!-- unchanged builder -->
  {_recent_flights_html(rows, now, state_dir)}   <!-- unchanged builder -->
</div>
```

```css
/* .home-columns already exists (1fr, gap: lg, no changes needed) — this
 * phase adds ONE modifier for the 3:2 desktop split D-04 asks for
 * ("the picture column is the wider one, about 3:2"). */
.home-columns.home-picture-row {
  grid-template-columns: 1fr;
}
@media (min-width: 1024px) {
  .home-columns.home-picture-row {
    grid-template-columns: 3fr 2fr;
    align-items: start;
  }
}
```

- **Order (≥1024px):** strip → three tiles → picture/recent-flights row. This is a literal restore of the phase-19 shape (`_status_tiles_html()`) plus a second row that phase 19 never had (phase 19's own second row was Quick-actions + panel + flights, all separate; D-04 asks specifically for picture-beside-flights, two columns, no third widget).
- **Tiles:** `layout.stat_tile(label, value_html, state, icon=...)`, three calls, in a `.dashboard-grid.home-status-grid` (both classes already exist and already collapse to one column below 480px per the pre-existing `.dashboard-grid` breakpoint rule — no new CSS needed for the tile grid itself).
- **Picture/flights row:** reuses `.home-columns` (existing 1-column-mobile / grid-desktop wrapper) with one new modifier, `.home-picture-row`, that sets the desktop track ratio to 3:2 (`3fr 2fr`) instead of `.home-columns`' other consumers' own ratios — scoped by the compound selector so no other `.home-columns` use anywhere else in the app is affected.
- **Deleted:** `.home-hero`, `.preview-frame` (Home's copy — `history_page.py`'s own unrelated string-guard is untouched, see the phase-20 spec's own naming-caution note, still valid), `.status-card*`, `_status_card_html()`, `_hero_figure_html()`'s caller site inside the old hero (its own function body is unchanged, only its wrapper and neighbour change).
- **Phone stacking (<700px):** strip cells wrap to one per line (§A); the three tiles collapse to one column below 480px (unchanged phase-19 breakpoint); the picture/flights row collapses to one column (picture first, by source order) below 1024px.
- **"Pretty" checklist (D-05):** surface = `.stat-tile`/`.page-section` hairline-at-rest cards throughout, no ad hoc surfaces; border-radius = `--radius-control` (8px) on every card, `--radius-card` (10px) nowhere on this page (no overlay/floating element on Home); spacing = the one universal 24px sibling-card gap between the strip, the tiles section and the picture row; the strip's accent-bordered surface is the only accent-tinted region on the page, reading as "the control area" against the three neutral/ok/warn tiles below it — matching D-05's own explicit look description.

### D. "Frame colours" (D-06..D-12) — new card, replaces four chip grids

One card, `.frame-colours`, at the top of the "Look" supersection, **rendered as a sibling of `<form id="settings-form">`** (see Structural Notes — this follows the exact precedent `calendar_group()` already establishes: a card whose *saved* fields post via `form="settings-form"` on each control, while the card's own DOM position is free of the form-nesting constraint the rules row's real `<form>`s would otherwise violate).

```html
<div class="page-section frame-colours" data-dirty-section="Frame colours">
  <h2 class="text-heading">Frame colours</h2>
  <p class="text-label section-caption">Choose the colour theme for departures, arrivals, calendar flights and your own rules.</p>

  <div class="frame-colours__layout">
    <figure class="theme-live-preview frame-colours__preview">
      <img class="theme-live-preview__image" src="/theme-preview/white.png?live=1"
           width="320" height="480" loading="eager" alt="White theme preview">
      <figcaption class="text-label">Preview with your last flight: AFR1380</figcaption>
    </figure>

    <div class="frame-colours__assign">
      <ul class="frame-colours__list" role="radiogroup" aria-labelledby="frame-colours-heading">
        <li>
          <label class="frame-colours__row">
            <input type="radio" name="colour_usage" value="departures" class="visually-hidden" checked
                   data-usage-panel="departures">
            <span class="frame-colours__swatch theme-chip__swatches" aria-hidden="true">
              <span class="theme-chip__dot" style="background:#FFFFFF"></span>
              <span class="theme-chip__dot" style="background:#111111"></span>
            </span>
            <span class="frame-colours__label">Departures</span>
            <span class="frame-colours__meta">White</span>
          </label>
        </li>
        <li>
          <label class="frame-colours__row">
            <input type="radio" name="colour_usage" value="arrivals" class="visually-hidden"
                   data-usage-panel="arrivals">
            <span class="frame-colours__swatch theme-chip__swatches" aria-hidden="true">…</span>
            <span class="frame-colours__label">Arrivals</span>
            <span class="frame-colours__meta">Same as departures</span>
          </label>
        </li>
        <li>
          <label class="frame-colours__row">
            <input type="radio" name="colour_usage" value="calendar" class="visually-hidden"
                   data-usage-panel="calendar">
            <span class="frame-colours__swatch theme-chip__swatches" aria-hidden="true">…</span>
            <span class="frame-colours__label">Calendar flights</span>
            <span class="frame-colours__meta">Same as departures</span>
          </label>
        </li>
        <li>
          <label class="frame-colours__row">
            <input type="radio" name="colour_usage" value="rules" class="visually-hidden"
                   data-usage-panel="rules">
            <span class="frame-colours__swatch theme-chip__swatches" aria-hidden="true">…</span>
            <span class="frame-colours__label">Per-flight rules</span>
            <span class="frame-colours__meta">3 rules</span>
          </label>
        </li>
      </ul>

      <div class="frame-colours__usage-panel" data-usage-panel-target="departures">
        <div class="theme-chip-grid" role="radiogroup" aria-labelledby="frame-colours-heading">
          <!-- _theme_chip_grid_html("theme_id", ..., radio_form_id="settings-form") -->
        </div>
      </div>
      <div class="frame-colours__usage-panel" data-usage-panel-target="arrivals" hidden>
        <div class="theme-chip-grid theme-chip-grid--compact" role="radiogroup" aria-labelledby="frame-colours-heading">
          <!-- "Same as departures" chip FIRST, then _theme_chip_grid_html("theme_arriving", ..., radio_form_id="settings-form") -->
        </div>
      </div>
      <div class="frame-colours__usage-panel" data-usage-panel-target="calendar" hidden>
        <div class="theme-chip-grid theme-chip-grid--compact" role="radiogroup" aria-labelledby="frame-colours-heading">
          <!-- "Same as departures" chip FIRST, then _theme_chip_grid_html("calendar_theme_id", ..., radio_form_id="settings-form") -->
        </div>
      </div>
      <div class="frame-colours__usage-panel" data-usage-panel-target="rules" hidden>
        <!-- _rule_list_html(rows) or the empty-state-plain block, then
             _rule_add_form_html() (its own <form>, a legal descendant of
             this <div> — only <form>-in-<form> is forbidden, and this
             div is not a form), then the "How rules combine" <details> -->
      </div>
    </div>
  </div>
</div>
```

```css
.frame-colours__layout {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-md);
}
@media (min-width: 960px) {
  .frame-colours__layout {
    grid-template-columns: minmax(360px, 1fr) minmax(0, 1fr);
    gap: var(--space-lg);
    align-items: start;
  }
}
.frame-colours__preview { margin: 0; }

.frame-colours__list {
  list-style: none;
  margin: 0 0 var(--space-md);
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}
.frame-colours__row {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-sm) var(--space-md);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-control);
  background: var(--color-dominant);
  cursor: pointer;
  min-height: 44px;   /* the whole row is the label — clears the touch-target floor directly, no delegation needed */
}
.frame-colours__row:hover {
  border-color: color-mix(in srgb, var(--color-text) 14%, transparent);
}
input:checked + .frame-colours__row,
.frame-colours__list li:has(input:checked) .frame-colours__row {
  border: 2px solid var(--color-accent);
  background: color-mix(in srgb, var(--color-accent) 12%, transparent);
}
input:focus-visible + .frame-colours__row {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
.frame-colours__label { flex: 1 1 auto; font-weight: var(--weight-regular); }
.frame-colours__meta {
  flex: 0 0 auto;
  font-weight: var(--weight-semibold);   /* Emphasis role */
  color: color-mix(in srgb, var(--color-text) 70%, transparent);
}
```

- **Layout (D-07):** two columns ≥960px, preview column `minmax(360px, 1fr)` — literally D-07's own "never narrower than 360px" instruction — assignment list `minmax(0, 1fr)`, roughly 1:1 as specified. Below 960px, one column, preview first (source order, no CSS `order`).
- **Selection mechanism (D-08):** `<input type="radio" name="colour_usage">` — visually hidden via the existing `.visually-hidden` utility (already exempt-by-delegation from the 44px floor, per the established `input.visually-hidden` register entry) — inside a `<label class="frame-colours__row">` that *is* the visible row, matching the site's one already-established "hidden radio + whole-element label" idiom (`.runway-card`, `.theme-chip`) verbatim rather than inventing a fourth selectable-card mechanism. The selected row's strong treatment (2px accent border + 12%-accent wash) reuses the *exact* colour values `.theme-chip--selected`/`.runway-card--selected` already use — not new literals — via a plain `:checked` sibling selector plus a `:has()` progressive-enhancement layer for older browsers exactly mirroring this file's own documented `:has()`-fallback convention (a browser without `:has()` still gets the `input:checked + label` rule, which alone is sufficient for a same-parent radio+label pair — the `:has()` form here is a belt-and-braces duplicate for a future markup change, not load-bearing today).
- **Row meta text (D-07):** the small swatch (existing `.theme-chip__dot`/`.theme-chip__swatches` reused verbatim) plus the theme name — or "Same as departures" for Arrivals/Calendar when unset, or "N rules"/"No rules yet" for the rules row — rendered in the Emphasis role (16px semibold), matching the row's own visual weight against the plain-regular row label to its left.
- **Chip grid per usage (D-08/D-09):** exactly one `.theme-chip-grid` visible at a time (`data-usage-panel-target` matching the checked radio's `value`), the other three carrying a plain `hidden` attribute the script (`theme-preview.js`, extended) removes/restores on `change`. Arrivals' and Calendar's grids gain one extra leading chip, "Same as departures" (submits the empty string) — this chip reuses `.theme-chip`'s own markup shape with no `<img>` preview (a plain `--color-secondary`-tinted placeholder square with a centred icon or just the swatch-less name text is acceptable; do not fabricate a fake theme-coloured preview for "no override").
- **Rules panel (D-10):** the fourth panel is not a chip grid at all — it is `_rule_list_html()`'s existing `<ul class="rule-list">` (or the empty-state block), `_rule_add_form_html()`'s existing inline add form (own `<form>`, a legal descendant here since the parent is a `<div>`, not a `<form>`), and the existing "How rules combine" `<details>` — all three relocate here verbatim from the deleted standalone `_rules_section_html()` card.
- **One save (D-11):** the three theme radiogroups (`theme_id`/`theme_arriving`/`calendar_theme_id`) each carry `form="settings-form"` on every radio (the exact `_theme_chip_grid_html(radio_form_id=...)` parameter this file already has, today used by Calendar's own grid) — Display's single Save button and the dirty-bar both cover them, unchanged. Rule add/delete keep their own immediate POSTs, unchanged.
- **Preview linkage (D-12):** clicking a row updates the preview to that usage's *currently selected* theme (departures' own selection for the departures row, the effective arrivals/calendar theme — which is the departures theme when unset — for those rows, the rule-form's own currently-selected chip for the rules row, defaulting to the first theme). Clicking a chip afterward updates the preview to that chip, exactly as phase 20's `theme-preview.js` already does for the single always-visible grid — this phase's own addition to that script is only the row→panel→chip-defaults wiring, not a new preview mechanism.
- **No-JS floor (D-08, locked):** without script, all four `<div class="frame-colours__usage-panel">` render **without** the `hidden` attribute (i.e., the server only ever omits `hidden` when it can rely on the script to manage visibility — see Structural Notes for exactly how this is decided server-side) and each panel is wrapped in its own `<fieldset>` with a `<legend>` matching its row's own label ("Departures"/"Arrivals"/"Calendar flights"/"Per-flight rules") so a no-JS user reads four clearly-labelled stacked sections in the same order as the (still-functional, still-selectable, but visually inert without script) radiogroup above them. Every radio still submits its real, correct value regardless of script — D-08's own "every value can still be saved" guarantee.

### E. Calendar, one tile (D-13/D-14) — merges two cards into one

```html
<div class="page-section" data-dirty-section="Calendar">
  <h2 class="text-heading">Calendar</h2>
  <p class="text-label section-caption">Flights from your calendar get their own colour on the frame.</p>

  <div class="status-row status-row--ok">
    <span class="dot dot--ok"></span>
    <span class="status-row__verdict">Connected</span>
    <span class="status-row__detail">12 upcoming flights · checked 10 min ago</span>
  </div>

  <!-- NOT CONNECTED branch: the URL field renders unconditionally, no <details> -->
  <form method="post" action="/settings/calendar/connect" class="rule-add-form">
    <div class="rule-add-form__field">
      <label for="calendar-connect-url">Calendar feed URL</label>
      <input type="text" id="calendar-connect-url" name="calendar_url" autocomplete="off"
             spellcheck="false" maxlength="2048">
      <p class="text-label section-caption" id="calendar-url-hint">Paste the private "secret" link your calendar app gives you for sharing a read-only feed.</p>
    </div>
    <button type="submit">Connect calendar</button>
  </form>

  <!-- CONNECTED branch, replaces the block above -->
  <p class="text-body calendar-masked-url">calendar.google.com/…</p>
  <p class="calendar-actions">
    <details class="calendar-url-disclosure">
      <summary class="text-link">Replace the feed URL</summary>
      <form method="post" action="/settings/calendar/connect" class="rule-add-form">
        <div class="rule-add-form__field">
          <label for="calendar-connect-url">Calendar feed URL</label>
          <input type="text" id="calendar-connect-url" name="calendar_url" autocomplete="off"
                 spellcheck="false" maxlength="2048">
        </div>
        <button type="submit">Replace</button>
      </form>
    </details>
    <button type="submit" form="calendar-disconnect-form" class="calendar-disconnect-btn">Disconnect</button>
  </p>

  <details><summary>How it works</summary>
    <p class="text-body">It can only colour a flight that happens to be on screen — it does not track or announce anything on its own. Applies on the frame's next scheduled poll, not immediately.</p>
  </details>
</div>

<!-- The Disconnect action's own form — a sibling, id-linked via form= above
     so its button can render INLINE on the calendar-actions line instead of
     needing its own trailing card (D-14's "right-aligned on the same line
     as the replace link"). -->
<form id="calendar-disconnect-form" method="post" action="/settings/calendar/disconnect"
      data-confirm="Disconnect this calendar?" data-confirm-value="yes">
  <input type="hidden" name="confirm" value="" data-confirm-field>
</form>
```

```css
.calendar-actions {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: var(--space-md);
  margin: var(--space-sm) 0 0;
}
.calendar-disconnect-btn {
  /* R-09: the one additive rule block for the small grey secondary button —
   * the base `button` rule already supplies the quiet 4.5%-wash grey; this
   * only makes it small and keeps it off the primary accent. */
  min-height: 30px;
  padding: 4px var(--space-sm);
  font-size: 12px;
  color: var(--color-text);
  background: color-mix(in srgb, var(--color-text) 6%, transparent);
  border: 1px solid color-mix(in srgb, var(--color-text) 20%, transparent);
  border-radius: var(--radius-control);
}
.calendar-disconnect-btn:hover, .calendar-disconnect-btn:focus-visible {
  background: color-mix(in srgb, var(--color-text) 12%, transparent);
}
.calendar-masked-url {
  font-family: var(--font-mono);
  margin: var(--space-sm) 0 0;
}
```

- **One card (D-13):** status row → (not connected: URL field + Connect button) or (connected: masked URL line + the replace/disconnect line) → "How it works" — all inside the *same* `<div class="page-section">`, superseding the phase-20 two-card split. The chip grid that used to sit in this card is gone (moved to §D).
- **Replace as a text link (D-14):** `<details class="calendar-url-disclosure"><summary class="text-link">Replace the feed URL</summary>` — reusing the exact existing class name (`calendar-url-disclosure`) and exact existing copy (`CALENDAR_REPLACE_URL_SUMMARY`), with one new small CSS rule for `.text-link` if it does not already exist as a named class (verified: it does not — this app's plain prose links inherit the base `a` rule's accent colour with no dedicated class; `.text-link` here is a **new, trivial** one-line rule: `.text-link { color: var(--color-accent); text-decoration: underline; }`, applied to the `<summary>` only so it reads as a link-styled disclosure trigger rather than the default bold `<summary>` treatment `summary::marker`'s own accent-coloured triangle already signals as interactive).
- **Cross-form Disconnect button (D-14):** the small grey "Disconnect" button must sit on the same visual line as the "Replace the feed URL" link — but Disconnect is a real state-changing action needing its own confirmed `<form>`, and that form cannot be a descendant of the `<details>` element's own sibling flow without an awkward nested-forms problem. Resolution: give the Disconnect `<form>` an `id`, render it as a data-only sibling (its one hidden field, no visible content), and give its **button** — rendered inline on the `.calendar-actions` line — a `form="calendar-disconnect-form"` attribute, the exact same cross-DOM submission idiom this codebase already uses for the dirty-bar's Save button. The button itself is a bare `<button>` carrying one new class, `.calendar-disconnect-btn` (**this codebase has no `.btn`/`.btn--secondary`/`.btn--small` class family** — every button is styled by the element selector), whose single rule block above (R-09) makes it small (30px, 12px text) and grey (a 6% text-colour wash with a 20% hairline), never the accent primary. `data-confirm`/`data-confirm-value`/the hidden `confirm` field are unchanged from phase 20's `calendar_disconnect_section()` — same confirmation mechanism, new position only.
- **Masked URL (D-13):** `host + "…"` on one plain-body line, never the full token-bearing URL — unchanged write-only contract from phase 20, just relocated onto its own line inside the merged card.

### F. Compact Flights table (D-15/D-16)

**Column-width arithmetic (both languages), from the real tokens:**

| Measurement | Value | Source |
|---|---|---|
| Sidebar width | 240px | `.dashboard-shell` grid-template-columns, `style.css` |
| Sidebar↔content gap | 32px (`--space-xl`) | `.dashboard-shell` column-gap |
| `.dashboard-main` side padding, ≥960px | 64px × 2 (`--space-3xl`) | `.dashboard-main`'s own ≥960px rule |
| **Content column at 1280px viewport** | **880px** | 1280 − 240 − 32 − 128 = 880 — the exact figure `history_page.py`'s own existing comment already states ("a 1,305px table that had to fit an 880px column") |
| Existing `.data-table td/th` horizontal padding | 16px × 2 per cell (`--space-md`) | `style.css` |
| **New, scoped** `.data-table--flights td/th` horizontal padding | 8px × 2 per cell (`--space-sm`) | new rule, this phase — reuses an existing token, does not invent one |

With 6 columns (5 data + the "More" toggle) at the new 8px-horizontal padding, total padding = 6 × 16px = **96px**, leaving a **784px** content budget inside 880px.

| Column | Widest expected content (both languages) | Allocated width | Basis |
|---|---|---|---|
| When | FR "31 sept. 22:14" (14 chars, mono, non-today row) / "il y a 59 min" (13 chars, `.cell-secondary` 13px) | 130px | Worst-case date+clock line; ordinary same-day rows ("22:14"/"il y a 12 min") are far shorter |
| Flight | Callsign "TVF7412" (7 chars, mono) / airline·type data line, e.g. "Turkish Airlines · A321" (24 chars, `.cell-secondary`, **data, not translated** — airline/type text length is independent of UI language) | 170px | Widest realistic airline name observed in ADS-B/adsbdb data; short callsigns (AFR1234) are the common case |
| Route | "ORY → XXX" (9 chars, mono, fixed 3-letter-code shape, data) | 90px | Format is fixed-width by construction |
| State | FR "À l'arrivée" (11 chars) vs EN "Arriving" (8 chars) — **French is the wider driver** | 100px | `home_page.DIRECTION_ARRIVING_TEXT`'s French value, reused verbatim here via the same translation key |
| Corroboration | dot only (12px circle) + `.visually-hidden` label (no visible text) | 40px | See dot-only decision below |
| More/Plus toggle | "More" (4) / "Plus" (4) | 60px | Small quiet button, matches `.copy-btn`-adjacent sizing |
| **Content subtotal** | | **590px** | |
| **+ padding (96px)** | | **686px** | **194px of margin below the 880px ceiling** — a wide safety buffer against font-metric variance across browsers, confirmed by the headless sweep, not by arithmetic alone (D-15's own verification method) |

**Corroboration becomes dot-only (explicit reading of D-15, not a re-guess):** today's desktop cell already renders `layout.status_dot(status, label, title)`, and `.dot-label` is a **visible** 14px text span (confirmed in `style.css`) — so "dot only, as today" in the CONTEXT text is read here as the *intended target shape* the developer is describing, not a literal claim about the current render (the current render is dot **plus visible label**, e.g. "Both agree"/"Ils sont d'accord"). This phase converts the label span to `.visually-hidden` for this column specifically (a new, narrow `status_dot(..., visually_hide_label=True)` keyword, defaulted `False` so every other call site — Health's own table — is byte-identical), keeping the exact same accessible name and `title` tooltip. This one change is what makes the 590px column-content budget realistic; keeping a visible ~90px-wide label here would blow the budget on the one column that carries the least essential at-a-glance information (its own dot colour already answers "agree/disagree/unknown" visually, same as the Frame/Battery/Data tiles' own dot-first pattern elsewhere in this app).

```html
<div class="data-table-wrap" tabindex="0" role="region" aria-label="Recent flights table, scrollable">
  <table class="data-table data-table--flights">
    <thead><tr><th>When</th><th>Flight</th><th>Route</th><th>State</th><th>Corroboration</th><th><span class="visually-hidden">Details</span></th></tr></thead>
    <tbody>
      <tr class="row" data-filter-text="afr1234 3944f2" data-filter-group="0" title="Runway 3 (07/25)">
        <td><span class="cell-primary mono">22:14</span><span class="cell-inline-sep">·</span><span class="cell-secondary">12 min ago</span></td>
        <td><span class="cell-primary mono">AFR1234</span><span class="cell-inline-sep">·</span><span class="cell-secondary">Air France · A320</span></td>
        <td>ORY → TLS</td>
        <td>Departing</td>
        <td>{layout.status_dot("ok", "Both agree", visually_hide_label=True)}</td>
        <td>
          <button type="button" class="row-toggle" data-row-toggle aria-expanded="false"
                  aria-controls="flight-detail-0">More</button>
        </td>
      </tr>
      <tr class="flight-detail-row" id="flight-detail-0" data-row-detail>
        <td colspan="6">
          <dl class="flight-detail-row__grid">
            <div><dt class="text-label">Hex</dt><dd class="mono">3944F2</dd>{copy button}</div>
            <div><dt class="text-label">Full timestamp</dt><dd class="mono">2026-09-12T22:14:03+00:00</dd></div>
            <div><dt class="text-label">Runway</dt><dd>Runway 3 (07/25)</dd></div>
          </dl>
          {copy-name button, unchanged _copy_button_html()}
        </td>
      </tr>
    </tbody>
  </table>
</div>
```

```css
/* Scoped to Flights' own table only — no other .data-table consumer
 * (Health, Airlines) gains this modifier. */
table.data-table--flights td,
table.data-table--flights th {
  padding: var(--space-sm) var(--space-sm); /* 8px both ways — token, not a literal; horizontal budget unchanged */
}

.flight-detail-row td {
  background: color-mix(in srgb, var(--color-text) 3%, transparent);
  padding: var(--space-md);
}
.flight-detail-row__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: var(--space-sm) var(--space-md);
  margin: 0;
}
.flight-detail-row__grid dt { margin: 0; }
.flight-detail-row__grid dd { margin: 0 0 var(--space-xs); }

/* D-15's own no-JS floor: visible by default. The script (below) adds
 * this class to every .flight-detail-row at load, exactly mirroring
 * nav-dropdown.js's own "document.documentElement.className += ' js'"
 * convention for a component-scoped hide instead of a page-wide one. */
.flight-detail-row--collapsed { display: none; }
```

- **New script, `companion/static/flight-rows.js`** (Claude's Discretion resolved: a new file, not an extension of `list-filter.js`/`copy-button.js` — neither is a natural home, and this keeps the six-touch-point static-script contract's file count honest rather than overloading an unrelated script's responsibility). ES5, no inline handlers, matching every existing script's own IIFE/IIFE-guard convention: on load, adds `flight-detail-row--collapsed` to every `.flight-detail-row` and sets `aria-expanded="false"` on every `[data-row-toggle]` (both already the server-rendered default, so this is idempotent, not corrective); on click, toggles the class on the `aria-controls`-named row and flips `aria-expanded`, and swaps the button's own text between "More"/"Plus" and "Less"/"Moins" via a `data-more-text`/`data-less-text` attribute pair (the same "server escapes both strings once, script only ever writes back a value it already escaped" contract `freshness.js`'s pause labels and `copy-button.js`'s copied-text label both already establish).
- **No-JS floor (D-15, locked):** the detail `<tr>` renders with no `hidden` attribute and no inline style — fully visible, immediately below its own summary row — whenever no script runs. This is why the CSS hide is a *class* the script itself adds, never a server-rendered `hidden`/CSS-only rule keyed off `.js` alone: a page that never runs any script (including one where `nav-dropdown.js`'s own `.js`-marking IIFE never fires) must show every detail row, and a page where scripts DO run but `flight-rows.js` itself is blocked by a stricter CSP directive than the rest of the app must *also* show every detail row — keying off a page-wide `.js` class would collapse the rows in that second case even though this specific script never ran to attach the reveal-on-click behaviour, silently hiding data with no way to get it back. The literal per-script class-at-load pattern avoids that trap.
- **Mobile cards unchanged (D-16):** `_history_cards_html()`'s own `<details>`-based "More details" disclosure is untouched — this section only restructures the desktop `<table>`.
- **Filter bar and lightbox (D-16):** unchanged; `data-filter-text`/`data-filter-group` attributes on the main `<tr>` are unaffected by adding a sibling `.flight-detail-row` — `list-filter.js`'s own row-count logic already keys off `data-filter-group`, not row adjacency, so a bare, unfiltered detail row sitting next to a filtered-out main row needs its own `hidden`-on-filter treatment too: extend the filter script's existing hide/show call to also target `tr.flight-detail-row[id="flight-detail-{n}"]` for the same `n` (a one-line addition to `list-filter.js`, flagged in Structural Notes).

### G. Removals (D-17/D-18)

- **Simple/Full switch:** `layout._mode_form_html()` deleted outright; `sidebar_footer_html`/`_mobile_nav_html()`'s footer composition drops the `mode_form_html` argument and its slot, becoming `"%s%s%s" % (lang_form_html, theme_form_html, _logout_form_html())` — **resolved order: language, theme, Sign out** (was language, theme, simple mode, Sign out). No visual gap results: `.sidebar-footer`'s existing `gap: var(--space-sm)` rule needs no change, it simply has one fewer child.
- **Health pause button:** the button (`REFRESH_PAUSE_TEXT`/`_RESUME_TEXT`, `data-refresh-toggle`) is deleted from `health_page.py`'s freshness-clock markup; `freshness.js`'s `wireToggle()` function and its `paused` boolean are deleted — the freshness loop's own interval-refresh call simply always runs, unconditionally, matching every other always-on polling loop in this app (the poll-cooldown countdown, the battery chart).
- **Visual consequence:** the Health page's freshness-clock line shrinks from `{clock} {pause button}` to just `{clock}` — no replacement control, no placeholder, matching D-18's own "the button is unnecessary" framing (removal, not substitution).

### H. Airlines upload restore (D-19/D-20)

- **The change is a one-line gate removal, not a new component.** `airlines_page._resolve_section_html()`'s Step-B branch:
  ```python
  # Before (phase 19, D-22):
  upload_zone = _resolve_upload_form_html(upload_action, "") if edit_mode else ""
  # After (this phase, D-19):
  upload_zone = _resolve_upload_form_html(upload_action, "")
  ```
  The identical gate on `_lightbox_html()`'s own no-artwork-yet branch (`resolve_upload_html = _resolve_upload_form_html("", "-dialog") if edit_mode else ""`, line 1228) is removed the same way — D-19's own second sentence ("The lightbox's upload affordance … follows the same rule").
- **`_edit_toggle_html()`'s simple-mode gate is deleted, not relocated** — since D-17 removes `simple_mode` outright, the function's `if ctx.get("simple_mode"): return ""` early-return is dead code once phase 21's simple-mode removal lands; D-20's own text ("the button is always visible now that simple mode is gone") is the visual consequence, not a separate task — "Change pictures" simply always renders now.
- **No markup, CSS or copy changes** — `_resolve_upload_form_html()`'s own drop-zone/file-picker markup, the surrounding `<div class="page-section" data-resolve-fallback>` card, and every string inside it are untouched.

---

## Structural Notes for the Planner (flagged explicitly, not silently resolved)

1. **`return_to` for the strip's switches.** `app.py`'s `_handle_quick_toggle()` currently hardcodes its redirect target to `layout.DISPLAY_ROUTE` regardless of which page posted — correct for today's single call site (Display), but D-01/D-02 need Home's copy of the strip to redirect back to `/` and Display's copy to redirect back to `/display` ("redirect back to the page they were pressed on," acceptance criterion 2). **Required fix:** add one hidden `return_to` field to the strip's two `<form>`s, and in `_handle_quick_toggle()`, read `form.get("return_to")`, validate it against a two-member whitelist (`{layout.HOME_ROUTE, layout.DISPLAY_ROUTE}` — the exact "membership-test before use" discipline this file already applies to every other client-controlled enum field), and fall back to `layout.DISPLAY_ROUTE` (today's behaviour) when absent or unrecognised, so no existing test asserting the Display-only redirect breaks.
2. **Frame colours' DOM position relative to `<form id="settings-form">`.** Since Theme's own always-in-form chip grid is retired and folded into Frame colours (which must be a *sibling* of the form, per §D's own reasoning — the rules row's real `<form>`s cannot be descendants of another `<form>`), `<form id="settings-form">` on the Display scope will, after this phase, wrap **zero visible content** — only hidden scope fields. This is not a defect: every visible, saved control on Display already cross-submits via `form="settings-form"` (Runway's radios, Calendar's chip grid today, and now Frame colours' three radiogroups) — the physical `<form>` becomes a pure submission target, exactly the shape `runway_fieldset()`'s own docstring already normalises. Confirm at plan time that `_scope_fields_html()`'s hidden inputs still render correctly inside an otherwise-empty `<form>`.
3. **`_theme_chip_grid_html()`'s "same as departures" chip** needs one new, additive parameter (e.g. `leading_chip_html=""`) rather than a special-cased rebuild — Arrivals'/Calendar's calls pass a pre-built `"Same as departures"` chip fragment; Departures'/rules' calls pass nothing, unaffected.
4. **`status_dot()`'s new `visually_hide_label` keyword** (§F) — fully defaulted `False`, so Health's own call sites (the only other consumer) are byte-identical; only Flights' desktop-table call site passes `True`.
5. **`list-filter.js`'s row-hide logic** must also hide/show the sibling `.flight-detail-row` for a filtered-out main row (§F) — a one-line extension, flagged so it is not silently missed and left as a stray visible detail row under a hidden summary row.
6. **Two Calendar button-text constants now needed**, not one: `CALENDAR_CONNECT_BUTTON_TEXT` ("Connect calendar") for the not-connected state, and a new `CALENDAR_REPLACE_BUTTON_TEXT` ("Replace") for the connected state's disclosure — `calendar_connect_section()` must pick between them based on `configured`, matching D-14's literal wording that the *button* itself is renamed, not just its surrounding link.
7. **`_mode_form_html()`'s removal touches four files' pinned check counts** (`test_companion_app.py`, `test_config_page.py`, `test_status_pages.py`, `test_i18n.py`) per `21-CONTEXT.md` D-17's own list — re-derive each `EXPECTED_CHECK_COUNT` by running the harness, never by arithmetic on the old comment (matching `21-VALIDATION.md`'s own instruction).

---

## Accessibility Floors

- **Strip switch buttons:** the Screen/Quiet hours buttons reuse `.quick-action__form button` unchanged (`min-width: 96px`, the standard 30px/36px quiet-button height) — the same geometry they have on Display today, so no new touch-target floor applies to them; the 44px floor below concerns the new Frame colours rows only.

- **Radiogroup semantics, Frame colours (D-08).** The 4-row assignment list carries `role="radiogroup"` on its `<ul>` with `aria-labelledby` pointing at the card's own `<h2 id="frame-colours-heading">` — the same pattern `theme_fieldset()`'s two existing grids and Calendar's chip grid already use (third/fourth/fifth consumer, not a new pattern). Each row's native `<input type="radio">` supplies the group's real selection semantics to assistive tech; the visual "selected row" treatment is a CSS consequence of that same `checked` state, never a separate ARIA attribute needing to be kept in sync.
- **`aria-pressed`, deliberately not used on the strip's switches.** The Screen/Quiet-hours buttons are one-shot form submits that fully reload the page on the target state (`POST` → redirect → fresh server render) — they are not persistent client-side toggle buttons whose pressed-state must track live DOM changes (unlike Health's now-deleted pause button, which correctly used `aria-pressed` because it stayed on the page and changed state in place). The equivalent contract here is: the button's own visible text already names the action ("Switch off" when currently on), the adjacent `.quick-action__state` text already names the current state in plain words, and the full-page reload plus `flash_banner()` (already `role="status"`/`role="alert"`) announces the outcome after the redirect — three existing, already-audited mechanisms, not a fourth new one.
- **Focus order, the strip.** Screen's submit button, then Quiet hours' submit button — source order matches visual left-to-right order; the next-update cell has no focusable element (read-only text).
- **`aria-expanded`/`aria-controls`, the Flights detail-row toggle (D-15).** `data-row-toggle` buttons carry `aria-expanded="false"` (server-rendered default, kept in sync by the new script) and `aria-controls="flight-detail-{n}"` naming the exact sibling `<tr id="flight-detail-{n}">` it reveals — the standard disclosure-button pattern, matching this app's own `<details>`-based disclosures in spirit (table rows cannot use a real `<details>`/`<summary>` pair, per D-15's own stated constraint, which is why this is a scripted `aria-expanded` toggle instead).
- **Contrast, nav reminder dots.** The two dots (`dot--ok`, new `dot--off`) are supplementary to adjacent text stating the same state in words ("Screen on"/"Screen off") — never the sole carrier of the distinction, so the WCAG 1.4.11 non-text-contrast floor (3:1) is not the binding constraint here; the *text* contrast (≥4.5:1, computed above at 14.4:1/14.35:1) is what this spec gates on, since that is the actual information channel.
- **French string length, the strip and the reminder.** "Heures calmes désactivées" (25 chars) is the longest new string this phase introduces in a fixed-width context; the strip's switch cells use `flex: 1 1 220px` (no hard cap), so the cell grows rather than truncates. The nav reminder is a block-level `<a>` with no width constraint, so it wraps naturally rather than clipping on a narrow mobile dropdown.
- **Touch targets.** `.frame-colours__row` is a `min-height: 44px` label spanning the row's full width — clears the floor directly, not by delegation. The Flights "More"/"Plus" toggle is a plain `<button>` at the existing 30px/36px base geometry (WCAG 2.5.8 AA floor, the same accepted trade-off register every other quiet button in this app already uses) — not re-litigated here.

---

## Empty / Error States

| Context | State | Treatment |
|---|---|---|
| Frame strip | Device config entirely absent (fresh install, no check-in yet) | Screen/Quiet-hours cells fall back to their existing documented defaults (`is_on` resolution already handles `None` as on/off per each group's own established rule); the next-update cell renders nothing at all when `wake.next_wake_at_iso()` returns `None` — the cell's own `<p>` is simply omitted, matching phase 20's own `headline_html = ""` fallback verbatim (never a fabricated "Next update ≈ —"). |
| Frame colours preview | No theme ever selected on a brand-new install | Falls back to `device_config.DEFAULT_THEME_ID`, matching `_theme_live_preview_html()`'s existing fallback verbatim — this phase adds no new empty-preview state. |
| Frame colours, rules row | Zero rules registered | Row meta reads "No rules yet" (§D copy table); the panel below shows the existing `.empty-state-plain` block, unchanged copy. |
| Calendar | Not connected | Status row "Not connected" (existing), URL field + Connect button render unconditionally, no Replace/Disconnect controls at all (they only exist once `configured or drift`). |
| Calendar | Drifted (unsafe stored secret) | Status row shows the existing `CALENDAR_STATUS_PERMISSION_UNSAFE` detail in the error dot state — unchanged from phase 20, still inside the now-merged card. |
| Flights table | No flights at all | `layout.empty_state()` unchanged — the compacted-column table markup never renders when `formatted_rows` is empty, matching today's guard. |
| Flights detail row | A row with no hex/no runway | The corresponding `<dt>`/`<dd>` pair is omitted entirely (never "Hex: —") — matching this app's established "omit, don't fabricate" convention for every other optional field. |
| Airlines upload | Step B, upload rejected (bad file type/size) | Unchanged — `_resolve_upload_form_html()`'s own existing error rendering, now simply always reachable instead of edit-gated. |

---

## Registry Safety

Not applicable — no component registry of any kind. `flight-rows.js` (new) is hand-written, matching every other script in `companion/static/`.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Verification Checklist

Run the headless sweep at **1280px** and **390px**, in **EN and FR**, and confirm each row below from the screenshots (matching `21-VALIDATION.md`'s own Manual-Only Verifications table):

**Home (1280px)**
- [ ] Order top-to-bottom: page header → Frame strip (accent-bordered surface, Screen/Quiet-hours cells + next-update text visibly the largest/boldest text in the strip) → three tiles (Frame/Battery/Flight data) in one row → "See details on Health" link → picture (left, wider) beside Recent flights (right, narrower), roughly 3:2.
- [ ] No `.status-card`, no `.home-hero` class anywhere in the rendered HTML.
- [ ] No quick-action widget anywhere on the page except inside the strip.

**Home (390px)**
- [ ] Same order, one column throughout; strip's two switch cells and the next-update cell each on their own line; three tiles stacked; picture stacked above Recent flights.

**Display (1280px)**
- [ ] Frame strip renders identically (same markup shape) at the top, above "Look."
- [ ] Screen on/off and Quiet hours cards below "When it is on" contain their scheduled controls (checkbox/presets/time inputs) but **no** `.quick-action` element — exactly one `.quick-action--on`/`--off` pair exists on the whole page (inside the strip).
- [ ] "Look" shows exactly one "Frame colours" card: preview left (≥360px), 4-row list right; clicking each row swaps the visible panel below (chip grid ×3, or rule list+form for "Per-flight rules") and the preview image.
- [ ] No "Use a different theme for arrivals" checkbox, no Calendar-card chip grid, no separate "Flight colours" card anywhere.
- [ ] Calendar is one card: status row, then URL/replace/disconnect line, then "How it works" — no second `.page-section` for Calendar.
- [ ] Flights table (via the Flights page) has no horizontal scrollbar; 6 header cells (When/Flight/Route/State/Corroboration/blank-for-toggle); Corroboration column shows a dot with no adjacent visible word.

**Display (390px) / Frame colours**
- [ ] Preview stacks above the 4-row list.

**Nav (both widths)**
- [ ] Sidebar (≥960px) and mobile dropdown (<960px) each show one line under the brand, above the page list, reading "Screen {on/off} · Quiet hours {on/off}" with a coloured dot before each segment, as a link (no button, no form).

**FR-specific**
- [ ] Flights headers read "Quand / Vol / Trajet / Sens / Corroboration" (not "État").
- [ ] Frame colours rows read "Départs / Arrivées / Vols du calendrier / Règles par vol"; unset rows read "Comme les départs."
- [ ] Nav reminder reads "Écran {allumé/éteint} · Heures calmes {activées/désactivées}" — fully French, no stray English word.

**Cross-cutting**
- [ ] No inline `<script>`, no 404 for any new static asset (`flight-rows.js`), `test_i18n.py` green, no `simple_mode`/`/ui-mode` string anywhere in `companion/`.
- [ ] No "Pause updates" button on Health.
- [ ] Airlines: naming a new prefix's Step B shows the upload drop zone without visiting `?edit=1` first.

---

## Checker Sign-Off

Checked 2026-09-12 by gsd-ui-checker (sonnet); the two BLOCKs were fixed by the
orchestrator in place and the report re-read against the edited sections.

- [x] Dimension 1 Copywriting: PASS (FLAG accepted: "Replace" / "Disconnect" sit under their own disambiguating summary and card heading)
- [x] Dimension 2 Visuals: PASS ("accent-bordered surface" wording; strip switch geometry stated in Accessibility Floors)
- [x] Dimension 3 Color: PASS (nav reminder 14.42:1 / 14.36:1, next-update text ≈17.6:1 / ≈15.8:1 recomputed)
- [x] Dimension 4 Typography: PASS (new elements inherit the site line-height; zero new sizes)
- [x] Dimension 5 Spacing: PASS (`.data-table--flights` padding is `var(--space-sm)` both ways; horizontal budget unchanged)
- [x] Dimension 6 Registry Safety: PASS
- [x] `.calendar-disconnect-btn` has its one rule block (R-09); markup, prose and CSS agree
- [x] Script name reconciled to `flight-rows.js` (R-12)

**Verdict:** APPROVED for planning.
