---
phase: 21
plan: 08
type: execute
wave: 6
depends_on: ["21-02", "21-03", "21-05", "21-06", "21-07"]
files_modified:
  - .claude/skills/sketch-findings-skypane/SKILL.md
  - .claude/skills/sketch-findings-skypane/references/control-density.md
  - .claude/skills/sketch-findings-skypane/references/data-density.md
  - .claude/skills/sketch-findings-skypane/references/mobile-navigation.md
  - .planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md
autonomous: true
requirements: [CFG-19, CFG-20, CFG-21, CFG-22, CFG-23, CFG-24]

must_haves:
  truths:
    - "The design-system skill documents what phase 21 shipped — the Frame strip, the nav state reminder, the Frame colours card, the merged calendar tile, the compact Flights table with its detail row and the small grey secondary button — with every superseded phase-20 statement marked SUPERSEDED in place rather than deleted"
    - "D-05: Home and the strip are judged against the UI-SPEC's verification checklist from real screenshots at 1280 px and 390 px, in French and English"
    - "The headless sweep reports no horizontal overflow (including the Flights table at 1280 px in both languages), no inline script and no inline handler attribute, and no 404 for any static asset including /static/flight-rows.js"
    - "The full harness suite is green apart from the five documented root-sandbox failures, and ruff is clean"
    - "21-VALIDATION.md's per-task verification map is filled from the shipped plans and its frontmatter reads nyquist_compliant: true"
    - "No file under companion/ or server/ is edited by this plan — a defect found by the sweep is recorded as a finding, not fixed here"
  artifacts:
    - path: ".claude/skills/sketch-findings-skypane/SKILL.md"
      provides: "the Phase 21 entry in Folded-In Work and the corrected design-direction prose"
      contains: "Phase 21"
    - path: ".planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md"
      provides: "the filled per-task verification map"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".claude/skills/sketch-findings-skypane/SKILL.md"
      to: "companion/static/style.css"
      via: "the documented class names and the unchanged accent-reservation contract"
      pattern: "frame-strip|frame-colours|calendar-disconnect-btn|flight-detail-row"
---

<objective>
Close phase 21: record what it shipped in the design-system skill (so the next
phase reads a current contract, not a phase-20 one), run the bilingual headless
sweep at both widths against the UI-SPEC's own verification checklist, and gate
the phase on a green suite, clean ruff and a filled validation map.

Purpose: six surfaces changed, and three statements in the skill are now wrong
(the three-switch nav footer, the two-card calendar, the Home hero row). The
sweep is the only verification D-05 and D-15 actually accept — "pretty" and
"fits 1280 px" are screenshot facts, not harness facts.
Output: an updated skill, a recorded sweep with its exact commands and results,
and a validation document that says so.
</objective>

<decisions_covered>
D-05 (the screenshot criterion), R-14 (the B-before-C sequencing this phase
executed, recorded as shipped), and the phase-wide verification of D-01..D-20 /
R-01..R-13 against the UI-SPEC verification checklist.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<files_owned>
This plan is the only plan in wave 6. It owns the design-system skill files and
`21-VALIDATION.md`, and it must NOT edit a single file under `companion/` or
`server/` — if the sweep finds a defect, record it in the SUMMARY as a finding for
`/gsd:verify-work` to adjudicate rather than fixing it here (a fix would be an
unplanned edit to a file whose owning plan has already closed).
</files_owned>

<interfaces>
What each earlier plan shipped, to be documented:
  21-01  simple mode removed everywhere; the nav footer is now language → theme →
         Sign out (the skill's Navigation paragraph currently says three switches,
         "language / theme / simple mode / Sign out" — mark SUPERSEDED in place)
  21-02  the Health pause button and freshness.js's pause branch removed
  21-03  the Flights desktop table compacted to five data columns plus a detail-row
         toggle column (`.data-table--flights`, `.flight-detail-row*`,
         `.row-toggle`, `companion/static/flight-rows.js`), with the corroboration
         column dot-only via `status_dot(visually_hide_label=True)` — the skill's
         Data-density entry still calls the 9→7 merge current and must be marked
         SUPERSEDED in place
  21-04  `layout.frame_strip_html()` (`.frame-strip` on a `.stat-tile--accent`
         surface), `layout.nav_status_html()` (`.nav-status`, the new `.dot--off`
         state), Home rebuilt (three stat tiles + `.home-columns.home-picture-row`;
         `.home-hero` and `.status-card*` retired except
         `.status-card__headline[--warn]`, which the strip reuses)
  21-05  the Frame colours card (`.frame-colours*`, the `colour_usage` radiogroup as
         the next consumer of the hidden-radio/styled-label idiom, the
         "Same as departures" leading chip, the rewritten `theme-preview.js`)
  21-06  the Airlines resolve-flow upload restored unconditionally
  21-07  the merged Calendar card (both `:has()` fusion rules retired), the masked
         host-only feed URL, and `.calendar-disconnect-btn` — the first named small
         grey secondary button in a codebase with no `.btn` family (R-09)

Skill files and where each fact belongs:
  SKILL.md `<design_direction>`   the Navigation and the Data-and-tables paragraphs
  SKILL.md `<metadata>`           "Folded-In Work (append-only, chronological)" —
                                  one new Phase 21 entry at the end, in the same
                                  voice and density as the Phase 20 entry
  references/control-density.md   the small grey secondary button, the 44 px
                                  `.frame-colours__row` row-as-label, the 30 px
                                  "More"/"Plus" row toggle
  references/data-density.md      the Flights table's five-column shape and its
                                  detail row (the 9→7 entry becomes SUPERSEDED)
  references/mobile-navigation.md the footer's three→two switches and the new
                                  `.nav-status` line in both renderers

Sweep mechanism (21-RESEARCH.md "Headless sweep mechanism"): the scratchpad's
`shots20.js` / `probe-ovf.js` / `probe-ovf-fr.js` drive Playwright with
`chromium.launch({ executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome' })`
against the companion service started from `companion/app.py`'s argparse CLI on a
seeded state directory (the scratchpad's `state-main/` seed already carries
`device_config.json`, `history.db`, `manual_resolutions.json`, `poll_state.json`,
`theme_previews/` and `gallery/` — reuse it, do not re-derive one). They log in,
switch language through the real `form[action="/ui-lang"] button[value="fr"]`
control, visit `/`, `/display`, `/device`, `/flights`, `/airlines`, `/health` at
{1280×900} and {390×844}, and assert no
`document.documentElement.scrollWidth > window.innerWidth`, no
`script:not([src])` / `[onclick]` / `[onchange]` / `[onsubmit]` / `[oninput]`
match, and no response status ≥ 400.

The five pre-existing root-sandbox failures that must NOT be "fixed": two in
`companion/test_companion_app.py`, one in `companion/test_status_pages.py`
(`anomaly_active()`), two in `server/test_manual_resolutions.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the design-system skill to the shipped phase-21 contract</name>
  <files>.claude/skills/sketch-findings-skypane/SKILL.md, .claude/skills/sketch-findings-skypane/references/control-density.md, .claude/skills/sketch-findings-skypane/references/data-density.md, .claude/skills/sketch-findings-skypane/references/mobile-navigation.md</files>
  <read_first>
    - .claude/skills/sketch-findings-skypane/SKILL.md in full — especially the `<design_direction>` Navigation and Data-and-tables paragraphs and the Phase 20 entry at the end of Folded-In Work (the voice, density and SUPERSEDED convention to copy)
    - references/control-density.md (the button-geometry register, the wash-strength register, the touch-target floor register and how a traded selector is recorded)
    - references/data-density.md (the History/Flights table entry) and references/mobile-navigation.md (the footer-grouping entry)
    - every `21-0N-SUMMARY.md` in this phase directory — the shipped facts, including any deviation an executor recorded
    - .planning/phases/21-.../21-UI-SPEC.md §§A-H for the class names and the token/accent claims to restate
    - companion/static/style.css — enough to VERIFY the "zero new custom properties, zero new colour literals, zero new accent consumers" claim before writing it
  </read_first>
  <action>
    Append ONE Phase 21 entry to SKILL.md's "Folded-In Work" list, in the Phase 20
    entry's voice: what shipped per area, the new class names (`.frame-strip*`,
    `.nav-status*`, `.dot--off`, `.home-picture-row`, `.frame-colours*`,
    `.calendar-actions`, `.calendar-disconnect-btn`, `.calendar-masked-url`,
    `.text-link`, `.data-table--flights`, `.flight-detail-row*`, `.row-toggle`),
    what was retired (`.home-hero`, `.status-card__rows`, both calendar `:has()`
    fusion rules, the simple-mode switch, the Health pause button), and the
    standing claim that this phase added zero custom properties, zero colour
    literals and zero accent consumers — verify that claim against the shipped
    stylesheet first, and if a plan had to break it, write what actually happened
    instead.

    Correct the two `<design_direction>` paragraphs IN PLACE using this file's own
    SUPERSEDED convention (never delete an old statement):
    - Navigation: the footer's three segmented switches become two (language,
      theme, then Sign out) — mark the simple-mode switch SUPERSEDED with the
      phase and decision id — and add the new `.nav-status` reminder line under the
      brand in both renderers, stating that it is a link with no form and no button
      (the phase-18 "no actions in the nav" finding) and that `.dot--off` is a
      fourth, neutral entry in the existing dot vocabulary reusing
      `.quick-action--off`'s 30%-muted mix.
    - Data and tables: the Flights desktop table's merged 7-column shape becomes
      five data columns plus a detail-row toggle column, with the hex, full
      timestamp, runway and copy buttons in a sibling `<tr>` that is visible
      without script; the mobile card list is unchanged; the corroboration column
      is dot-only; the scoped `table.data-table--flights` padding is 8 px on both
      axes. Mark the 7-column statement SUPERSEDED with the phase reference.

    Update the three reference files in place, each with its own SUPERSEDED marker
    where a prior statement changed: `control-density.md` gains the small grey
    secondary button (30 px, 12 px text, 6 % wash, 20 % hairline — the first named
    secondary-button treatment in a codebase with no `.btn` family, R-09), the
    `.frame-colours__row` 44 px row-as-label (a floor met directly, not traded) and
    the 30 px row toggle (an existing trade, not a new one); `data-density.md`
    gains the Flights table's new shape and its detail row; `mobile-navigation.md`
    gains the two-switch footer and the reminder line's placement in both
    renderers.

    Do not restate anything this phase did not change, and do not rewrite history:
    every correction is an in-place SUPERSEDED annotation plus the new statement.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py && "$PY" companion/test_view_pages.py && grep -q "Phase 21" .claude/skills/sketch-findings-skypane/SKILL.md && echo SKILL_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 21" .claude/skills/sketch-findings-skypane/SKILL.md` outputs at least `1`
    - `grep -c "frame-strip\|frame-colours\|calendar-disconnect-btn\|flight-detail-row\|nav-status" .claude/skills/sketch-findings-skypane/SKILL.md` outputs at least `5`
    - `grep -c "SUPERSEDED" .claude/skills/sketch-findings-skypane/references/data-density.md` is higher than before this task
    - `git diff --stat` for this task shows only files under `.claude/skills/`
    - both harnesses report M/M against their last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>The skill describes the app as it now is: a two-switch nav with a state reminder, a five-column Flights table, one Frame colours card, one calendar tile and one named secondary button.</done>
</task>

<task type="auto">
  <name>Task 2: The FR/EN headless sweep at 1280 px and 390 px</name>
  <files>.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md</files>
  <read_first>
    - .planning/phases/21-.../21-RESEARCH.md "Headless sweep mechanism" (the exact script shapes, the Chromium executablePath, the seeded state directory and the three assertions)
    - .planning/phases/21-.../21-UI-SPEC.md "Verification Checklist" — every row is a line item this sweep must answer
    - .planning/phases/21-.../21-VALIDATION.md "Manual-Only Verifications"
    - 21-03-SUMMARY.md — whether that plan's own overflow probe ran or was deferred here
    - companion/app.py's argparse CLI block (how to start the service against a state directory and on which port)
  </read_first>
  <action>
    Start the companion service against the seeded state directory and run the
    sweep from the scratchpad (adapt `shots20.js`; do not reinvent it): both
    languages × both viewports × the six pages, capturing full-page screenshots
    and asserting no horizontal overflow, no inline `<script>`, no `on*` handler
    attribute, and no response status ≥ 400 (including `/static/flight-rows.js`,
    which must return 200).

    Then walk the UI-SPEC's Verification Checklist against the screenshots and
    answer every line item explicitly — Home's order and its 3:2 picture row at
    1280 px and its single column at 390 px; Display's strip, its single
    `.quick-action` pair, the Frame colours card's two-column layout and row→panel
    swap, the one-card Calendar; the Flights table's absence of a horizontal
    scrollbar and its dot-only corroboration column; the nav reminder in both
    renderers; the FR-specific header words ("Quand / Vol / Trajet / Sens /
    Corroboration", "Départs / Arrivées / Vols du calendrier / Règles par vol",
    "Comme les départs", "Écran allumé · Heures calmes désactivées"); no "Pause
    updates" on Health; Airlines' step-B drop zone without `?edit=1`.

    Record in the SUMMARY: the exact commands run, where the screenshots were
    written, a per-line-item PASS/FAIL table, and — for any FAIL — the page,
    viewport, language and a one-line description. Do not fix a failure here: this
    plan owns no code file. If the browser or the service cannot run in this
    worktree, say so plainly, state which line items therefore remain unverified,
    and mark them as such in `21-VALIDATION.md`'s Manual-Only table rather than
    claiming a pass.

    Update `21-VALIDATION.md`'s Manual-Only Verifications table with the outcome of
    each browser-only item (verified, failed, or not runnable here).
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_status_pages.py; grep -q "1280" .planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md && echo SWEEP_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - the SUMMARY contains the exact sweep command(s), the screenshot output path and a per-line-item table covering every row of the UI-SPEC Verification Checklist
    - every FAIL (if any) names page, viewport and language, and no companion/ or server/ file was edited to make it pass
    - `21-VALIDATION.md`'s Manual-Only table records an outcome for each of its six rows
    - `git diff --stat` for this task shows only `.planning/` files
  </acceptance_criteria>
  <done>Both languages at both widths have been looked at, and every UI-SPEC checklist line has a recorded answer rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: The phase gate — full suite, ruff, and the filled validation map</name>
  <files>.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md</files>
  <read_first>
    - all eight `21-0N-PLAN.md` files in this phase directory (their task names, requirements, threat ids and `<automated>` commands) and every `21-0N-SUMMARY.md` that exists
    - .planning/phases/21-.../21-VALIDATION.md — the empty Per-Task Verification Map's column set and the Validation Sign-Off checklist
    - scripts/run-all-tests.sh and scripts/run_all_tests.py (the canonical harness list — confirm no new harness file needs registering; none was added this phase)
  </read_first>
  <action>
    Run the full suite and the linter:
    `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh`
    and `ruff check .`. Confirm the only failures are the five documented
    root-sandbox cases, and record each harness's final `EXPECTED_CHECK_COUNT` and
    reported M in the SUMMARY, beside its phase-start baseline (view=107,
    config=212, companion_app=267, status=213, contrast=39, i18n=24).

    Fill `21-VALIDATION.md`'s Per-Task Verification Map: one row per task across all
    eight plans, with Task ID, Plan, Wave (derived from `depends_on`), Requirement,
    Threat Ref (the `T-21-xx` id the task's plan registers, or "—"), Secure
    Behavior (one clause), Test Type, the task's own `<automated>` command, File
    Exists (yes — every harness this phase uses already existed) and Status
    (pass/fail as observed).

    Tick the Validation Sign-Off checklist honestly and set
    `nyquist_compliant: true` in the frontmatter only if every task in the map has
    an `<automated>` command that ran against an existing file and no three
    consecutive tasks lack automated verification. If that is not true, leave it
    false and say exactly which task broke it.

    Close with a phase-level statement in the SUMMARY: which of D-01..D-20 and
    R-01..R-14 are verified by a harness, which by the sweep, and which remain
    human-judgement items for `/gsd:verify-work`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); PYTHON="$PY" bash scripts/run-all-tests.sh; "$PY" -m ruff check . || ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - the full suite shows only the five documented pre-existing failures, and `ruff check .` is clean
    - `21-VALIDATION.md`'s Per-Task Verification Map has one row per task across plans 21-01..21-08, each with a real `<automated>` command
    - `grep -c "nyquist_compliant: true" .planning/phases/21-.../21-VALIDATION.md` outputs `1` (or the SUMMARY states exactly why it is still false)
    - `git diff --stat` for this task shows only `.planning/` files
  </acceptance_criteria>
  <done>The phase is gated on a green suite, a clean linter and a validation document that matches the plans that were actually executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headless browser session → the running companion service | the sweep authenticates with the seeded password and drives real pages |
| seeded state directory → screenshots | rendered pages may contain seeded device state and airline data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-29 | Information Disclosure | sweep screenshots containing a seeded calendar host or device data | mitigate | the sweep runs against the scratchpad's synthetic seed, never a real household state directory, and the screenshots are written to the scratchpad, never committed — the SUMMARY records paths, not images |
| T-21-30 | Tampering (verification integrity) | claiming a checklist line passed without looking | mitigate | each line item gets a recorded PASS/FAIL/not-runnable verdict with page, viewport and language; "not runnable here" is an allowed and explicitly preferred answer over an assumed pass |
| T-21-31 | Tampering (scope) | fixing a defect in a closed plan's file from the closing plan | mitigate | this plan owns no `companion/`/`server/` file and its acceptance criteria assert `git diff --stat` stays inside `.claude/skills/` and `.planning/` |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages; the sweep uses the already-present Playwright/Chromium pair named in 21-RESEARCH.md |
</threat_model>

<verification>
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → only the five documented root-sandbox failures
- `ruff check .` clean
- the headless sweep (EN/FR × 1280/390) → no overflow, no inline script, no handler attribute, no status ≥ 400
- `21-VALIDATION.md` → per-task map filled, Manual-Only table answered, sign-off ticked
- `<human-check>`: the developer compares the Home and Display screenshots against 21-UI-SPEC.md's verification checklist and confirms the strip reads as the control area and Home is glanceable (D-05's "pretty" criterion).
</verification>

<success_criteria>
- The design-system skill and its three reference files describe the shipped phase-21 contract, with every changed prior statement marked SUPERSEDED in place
- The sweep ran in both languages at both widths, or its inability to run is recorded explicitly per line item
- Every UI-SPEC verification-checklist row has a recorded verdict
- Full suite green apart from the five documented failures; ruff clean
- `21-VALIDATION.md`'s per-task map is filled and `nyquist_compliant` is set honestly
- No file under `companion/` or `server/` was edited by this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-08-SUMMARY.md` when done.
</output>
</content>
