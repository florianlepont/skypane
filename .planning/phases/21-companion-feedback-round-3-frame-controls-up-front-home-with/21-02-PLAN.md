---
phase: 21
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - companion/pages/health_page.py
  - companion/static/freshness.js
  - companion/i18n_fr/health.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-23]

must_haves:
  truths:
    - "D-18: the Health page has no 'Pause updates' button — no [data-refresh-toggle] element, no data-pause-text/data-resume-text attribute, anywhere in its rendered HTML"
    - "D-18: freshness.js has no `paused` flag, no wireToggle(), no setToggleVisual() and no `if (paused)` guard — the freshness loop always runs"
    - "D-18: the tab-visibility mechanism (stopLoop/startLoop/intervalHandle, tick()'s `document.hidden` guard, the visibilitychange listener itself) survives byte-for-byte"
    - "D-18: the freshness clock line keeps its prefix text, its clock and its 'Updating…' pill — the countdown and the battery chart are untouched"
    - "R-13/Pitfall 5: REFRESH_PAUSE_TEXT and REFRESH_RESUME_TEXT are deleted in the same commit as their French catalogue entries in companion/i18n_fr/health.py"
  artifacts:
    - path: "companion/pages/health_page.py"
      provides: "a freshness line with clock and pill only"
      contains: "FRESHNESS_PREFIX_TEXT"
    - path: "companion/static/freshness.js"
      provides: "an unconditional refresh loop"
      contains: "visibilitychange"
  key_links:
    - from: "companion/static/freshness.js"
      to: "companion/pages/health_page.py"
      via: "the duplicated-not-imported swap-selector contract, unchanged"
      pattern: "REFRESH_SWAP_SELECTORS"
---

<objective>
Delete the Health page's "Pause updates" button and the pause branch of
`freshness.js`, leaving the freshness loop unconditional and every other part of
the freshness mechanism — the clock, the "Updating…" pill, the swap selectors and
the tab-visibility handling — exactly as it is.

Purpose: the developer's tenth feedback point — the button is unnecessary
furniture on a page that already refreshes politely. D-18 is a removal, not a
substitution: nothing replaces the button.
Output: a smaller `health_page.py` freshness line, a `freshness.js` with no
`paused` state, the two French entries gone, and the pinned script-contract
checks retargeted.
</objective>

<decisions_covered>
D-18 (and its half of R-13: the French entries go in the same commit as their
English constants).
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-CONTEXT.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-RESEARCH.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-PATTERNS.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-UI-SPEC.md
@.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-VALIDATION.md

<files_owned>
This plan owns, for wave 2: `companion/pages/health_page.py`,
`companion/static/freshness.js`, `companion/i18n_fr/health.py` and
`companion/test_status_pages.py`. Plan 21-03 runs concurrently in this wave and
owns `companion/pages/history_page.py`, `companion/static/flight-rows.js`,
`companion/static/list-filter.js`, `companion/app.py`, `companion/layout.py`,
`companion/static/style.css`, `companion/i18n_fr/flights.py`,
`companion/test_view_pages.py` and `companion/test_companion_app.py` — never edit
any of those here. `companion/test_i18n.py` is RUN by both plans and edited by
neither (no check is added or deleted there in this wave).
</files_owned>

<interfaces>
The sites to delete, file:line as read on 2026-09-12 (re-verify before editing):

  companion/pages/health_page.py:343-344   REFRESH_PAUSE_TEXT = "Pause updates"
                                           REFRESH_RESUME_TEXT = "Resume updates"
  companion/pages/health_page.py:2909-2925 the `toggle_html` build (a
      `<button type="button" data-refresh-toggle aria-pressed="false"
       data-pause-text="…" data-resume-text="…">…</button>`) and the comment above it
  companion/pages/health_page.py:2947-2949 `freshness_html`'s %-format tuple — drop
      the toggle slot only; FRESHNESS_PREFIX_TEXT, clock_html and pill_html stay
  companion/i18n_fr/health.py:34-35        "Pause updates"/"Resume updates" entries

  companion/static/freshness.js:163        `var paused = false;`
  companion/static/freshness.js:320-325    setToggleVisual()
  companion/static/freshness.js:327-341    wireToggle()
  companion/static/freshness.js:350        wireToggle() inside applySwap()
  companion/static/freshness.js:476        the bottom-of-file wireToggle() call
  companion/static/freshness.js:428-430    `if (paused) { return; }` inside tick()
  companion/static/freshness.js:458-462    `if (paused) { return; }` inside the
                                           visibilitychange listener, with its comment

  DO NOT TOUCH: stopLoop(), startLoop(), intervalHandle, tick()'s
  `if (document.hidden) { stopLoop(); return; }` guard, the visibilitychange
  registration itself, AUTO_REFRESH_INTERVAL_MS, REFRESH_SWAP_SELECTORS and every
  [data-loaded-at]/[data-refresh-pill] hook.

The pinned checks in companion/test_status_pages.py:
  ~3466        a comment naming the `<button type="button" data-refresh-toggle>` shape
  ~5669-5717   the freshness-wrapper check asserting exactly one
               `data-refresh-toggle` button and its `data-pause-text` attribute
  ~9303-9345   `_both_tabs_ok_end_to_end` — the real-HTTP needle loop over the served
               freshness.js bytes, whose tuple includes
               `("[data-refresh-toggle]", "the refresh-toggle attribute hook")`,
               plus the long check description naming that hook twice
Live EXPECTED_CHECK_COUNT: companion/test_status_pages.py = 213 (line 502) before
wave 1; plan 21-01 changed it — read the file's LAST assignment, never this number.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Pause updates button from the Health page</name>
  <files>companion/pages/health_page.py, companion/i18n_fr/health.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/pages/health_page.py lines 330-350 (both constants and the comment block explaining the data-pause-text/data-resume-text idiom) and lines 2895-2960 (the toggle build, the freshness wrapper and `freshness_html`'s assembly) — read in full
    - companion/i18n_fr/health.py (whole file, to delete exactly two entries and any banner comment that only introduces them)
    - companion/test_status_pages.py ~5650-5730 (the freshness-wrapper check in full, including its description string) and ~3450-3480 (the comment referencing the toggle)
    - companion/test_status_pages.py's EXPECTED_CHECK_COUNT trail (read the LAST assignment — plan 21-01 appended one)
  </read_first>
  <action>
    Delete `REFRESH_PAUSE_TEXT`, `REFRESH_RESUME_TEXT` and the comment block that
    exists only to explain them. Delete the `toggle_html` construction and its slot
    in `freshness_html`'s `%`-format tuple, leaving the prefix text, the clock and
    the "Updating…" pill in their current order and markup. The freshness line
    therefore shrinks from `{prefix} {clock} {pill} {button}` to
    `{prefix} {clock} {pill}` — no replacement control, no placeholder (UI-SPEC §G).

    Delete `"Pause updates"` and `"Resume updates"` from
    `companion/i18n_fr/health.py` in this same commit (Pitfall 5:
    `test_i18n.py`'s dead-translation check names orphaned keys by name).

    In `companion/test_status_pages.py`, rewrite the freshness-wrapper check in
    place — same check function, retargeted body and description: it now asserts
    the wrapper contains the readout span and the pill and contains ZERO
    occurrences of `data-refresh-toggle`, `data-pause-text` and
    `data-resume-text`. Update the stale comment near line 3466 so it no longer
    describes a button that does not exist. Do not delete the check — the
    freshness wrapper still needs its shape pinned.

    Re-derive `EXPECTED_CHECK_COUNT` by RUNNING the harness and appending a NEW
    last assignment commented `21-02-PLAN.md Task 1` (the count is unchanged if you
    only rewrote checks — say so in the comment and still append the assignment,
    matching the file's own convention).
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py; "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REFRESH_PAUSE_TEXT\|REFRESH_RESUME_TEXT\|data-refresh-toggle\|data-pause-text\|data-resume-text" companion/pages/health_page.py` outputs `0`
    - `grep -c "Pause updates\|Resume updates" companion/i18n_fr/health.py` outputs `0`
    - `grep -c "FRESHNESS_PREFIX_TEXT" companion/pages/health_page.py` is unchanged from before this task
    - `companion/test_i18n.py` exits 0; `companion/test_status_pages.py` reports only the documented `anomaly_active()` FAIL
  </acceptance_criteria>
  <done>Health's freshness line carries a clock and a pill and nothing else; no French entry is orphaned.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the pause branch from freshness.js and retarget the served-script contract</name>
  <files>companion/static/freshness.js, companion/test_status_pages.py</files>
  <read_first>
    - companion/static/freshness.js in full (489 lines) — every reference to `paused`, `wireToggle`, `setToggleVisual` and `[data-refresh-toggle]`, and the header comment block describing the script's contract
    - companion/test_status_pages.py ~9290-9350 (`_both_tabs_ok_end_to_end`'s needle tuple over the served freshness.js bytes and the check's own long description string)
  </read_first>
  <action>
    Delete from `companion/static/freshness.js`: the module-level `paused`
    variable, `setToggleVisual()`, `wireToggle()`, both `wireToggle()` call sites
    (inside `applySwap()` and at the bottom of the file), the `if (paused)` guard
    inside `tick()`, and the `if (paused)` guard inside the `visibilitychange`
    listener together with the comment that exists only to explain it. Update the
    file's header comment to state that the loop is unconditional and to name D-18.

    Leave untouched: `AUTO_REFRESH_INTERVAL_MS`, `stopLoop()`, `startLoop()`,
    `intervalHandle`, `tick()`'s `if (document.hidden) { stopLoop(); return; }`
    guard, the `visibilitychange` registration itself, the `[data-loaded-at]` and
    `[data-refresh-pill]` hooks and every `REFRESH_SWAP_SELECTORS` consumer. The
    file stays ES5 (no `let`/`const`/arrow functions/template literals), keeps its
    IIFE and its `"use strict"`, adds no inline handler and no HTML-writing sink —
    the CSP contract is unchanged by this plan.

    In `companion/test_status_pages.py`'s `_both_tabs_ok_end_to_end`, remove the
    `("[data-refresh-toggle]", "the refresh-toggle attribute hook")` entry from the
    needle tuple and remove that hook from the check's own description string,
    keeping every other needle (`AUTO_REFRESH_INTERVAL_MS`, `visibilitychange`,
    `[data-loaded-at]`, `[data-refresh-pill]`) and the whole
    `REFRESH_SWAP_SELECTORS` pass intact. Add one needle in the opposite
    direction: the served bytes must NOT contain `data-pause-text` or `wireToggle`
    — the pinned regression test that the branch cannot come back through the
    served file.

    Re-derive `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `21-02-PLAN.md Task 2`. Record in the SUMMARY that the freshness loop now runs
    unconditionally and that the tab-visibility mechanism was deliberately left
    alone.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "paused\|wireToggle\|setToggleVisual\|data-refresh-toggle" companion/static/freshness.js` outputs `0`
    - `grep -c "document.hidden\|visibilitychange\|stopLoop\|startLoop" companion/static/freshness.js` is unchanged from before this task
    - `grep -cE "\b(let|const)\b|=>|\`" companion/static/freshness.js` outputs `0` (ES5 floor held)
    - `companion/test_status_pages.py` reports only the documented `anomaly_active()` FAIL, with M equal to its new last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>The freshness loop has no pause state, the served script proves it, and the visibility mechanism still stops the loop on a hidden tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /static/freshness.js | a static script served to every authenticated page |
| freshness.js → Health DOM | the script rewrites server-rendered fragments into the page on each swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Tampering (XSS) | `freshness.js`'s DOM writes after the pause branch is removed | mitigate | no new sink is introduced: the deleted code is the only part being removed, and the surviving swap path writes only into the existing `REFRESH_SWAP_SELECTORS` targets from server-rendered bytes; no `innerHTML` call is added, no inline handler, no `eval` — pinned by the served-bytes check in Task 2 |
| T-21-06 | Denial of Service (self-inflicted) | an always-on refresh loop | accept | the loop still stops on `document.hidden` and still uses `AUTO_REFRESH_INTERVAL_MS`; removing a user-facing pause button does not change the request rate of a visible tab |
| T-21-07 | Information Disclosure | the deleted button's `data-*` label attributes | n/a | both labels were static UI copy, never device or session data |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | this plan installs zero packages in any ecosystem |
</threat_model>

<verification>
- `companion/test_status_pages.py` → only the documented `anomaly_active()` FAIL
- `companion/test_i18n.py` → M/M (no orphaned "Pause updates"/"Resume updates")
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh` → no new failure
- `ruff check .` clean
- `<human-check>` (folded into plan 21-08's sweep): the Health page shows the freshness clock with no button beside it, and the page still refreshes on its own.
</verification>

<success_criteria>
- No pause button, no `data-refresh-toggle`, no `data-pause-text` anywhere in the rendered Health page
- `freshness.js` has no `paused` flag and no toggle wiring, and is still ES5 with no inline handler
- The tab-visibility mechanism and the swap-selector contract are byte-identical
- Both French entries deleted in the same commit as their English constants; `test_i18n.py` green
- `companion/test_status_pages.py`'s pin re-derived by running the harness, appended once per task citing this plan
- One commit per task, `type(scope): summary` style, no model names, no `git stash`
</success_criteria>

<output>
Create `.planning/phases/21-companion-feedback-round-3-frame-controls-up-front-home-with/21-02-SUMMARY.md` when done.
</output>
</content>
