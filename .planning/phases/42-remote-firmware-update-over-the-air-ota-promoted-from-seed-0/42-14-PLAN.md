---
phase: 42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0
plan: 14
type: execute
wave: 3
depends_on: [42-09]
files_modified:
  - companion/app.py
  - companion/pages/update_page.py
  - companion/i18n_fr/update.py
  - companion/test_update_actions.py
  - companion/test_browser_update.py
autonomous: true
requirements: [OTA-08]
must_haves:
  truths:
    - "POST /update/install without confirm=yes changes nothing and renders a confirmation page naming the version and the expected next wake time; it works with JavaScript disabled (D-04)"
    - "POST /update/install with confirm=yes schedules the release through firmware_registry.schedule_release and redirects to /update; any published release at or above the floor that differs from the running version is accepted, including an older one, with no extra downgrade step (D-01, D-06, D-11)"
    - "POST /update/cancel cancels only while no offer has been served for the schedule; otherwise it changes nothing and flashes 'Couldn't cancel — the frame may have already started.' (D-05)"
    - "Both POST routes sit behind the existing session and Origin/Sec-Fetch-Site gate"
    - "With JavaScript the Install button's data-confirm guard asks first, and the server-side confirmation page still applies (D-04)"
    - "At 375 px and 390 px, in English and French, the tab bar's More sheet shows Health, Device and Update, each at least 44 px tall, without wrapping or truncation, and the tab bar cells keep their size (D-02)"
  artifacts:
    - path: companion/app.py
      provides: "POST /update/install and POST /update/cancel handlers with flash keys"
      contains: "/update/install"
    - path: companion/pages/update_page.py
      provides: "update_install_confirm_page(ctx, version, next_wake_text)"
      contains: "update_install_confirm_page"
    - path: companion/test_update_actions.py
      provides: "Plain-request (no-JS) tests of both POST flows"
    - path: companion/test_browser_update.py
      provides: "pytest-playwright tests: JS confirm guard, full flow, mobile-fit measurements"
  key_links:
    - from: companion/app.py
      to: server/firmware_registry.py
      via: "schedule_release / cancel_schedule"
      pattern: "schedule_release|cancel_schedule"
---

<objective>
Add the operator actions to the Update page: Install with a server-side confirmation that works without JavaScript, and Cancel while the frame has not yet picked up the offer; prove the navigation still fits on phones.

Purpose: OTA-08's action half (D-01, D-04, D-05, D-06) and the D-02 mobile-fit gate the UI-SPEC made a verification item.
Output: two POST handlers, the confirmation page, FR strings, plain-request and browser tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-UI-SPEC.md
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-09-SUMMARY.md

<interfaces>
From server/firmware_registry.py (plan 01): schedule_release(state_dir, version, running_version, now) -> "scheduled" | "unknown" | "below_floor" | "same_as_running" | "busy"; cancel_schedule(state_dir, now) -> "cancelled" | "not_cancellable" | "none"; update_view(...)
From companion/pages/update_page.py (plan 09): update_page(ctx, view, next_wake_text); the Install form posts `version`; the Cancel form posts nothing else.
Existing precedent (re-read on main): _handle_calendar_disconnect_post + calendar_disconnect_confirm_page (two-step confirm), _handle_quick_toggle / _handle_poll_now (plain POST + flash redirect), FLASH_MESSAGES and the "%s?flash=%s" redirect convention, read_form, the POST Origin/Sec-Fetch-Site gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install (two-step confirm) and Cancel POST handlers</name>
  <files>companion/app.py, companion/pages/update_page.py, companion/i18n_fr/update.py, companion/test_update_actions.py</files>
  <read_first>
    - companion/app.py (on current main after Phase 40: route table, do_POST dispatch, Origin gate, _handle_calendar_disconnect_post, _handle_quick_toggle, FLASH_MESSAGES, flash key naming)
    - companion/pages/config_page.py (calendar_disconnect_confirm_page: heading, sentence, re-post form with hidden fields, plain cancel link)
    - companion/pages/update_page.py and companion/i18n_fr/update.py (plan 09)
    - companion/test_post_origin.py (how cross-origin POST rejection is asserted)
  </read_first>
  <behavior>
    - POST /update/install version=fw-v1.1.0 (no confirm) -> 200, page with heading "Install firmware fw-v1.1.0?", the sentence naming the next wake time, a form re-posting version=fw-v1.1.0 and confirm=yes to /update/install, and a plain "Cancel" link to /update; registry unchanged
    - Same with confirm=yes -> 303 to /update; registry schedule version fw-v1.1.0 state scheduled; the next GET /update shows Scheduled and a Cancel form
    - confirm=yes for an older release than the running one -> scheduled, same wording as a newer one (no downgrade copy)
    - confirm=yes for an unknown version, a below-floor version, the running version, or while another update is in progress (schedule_release returns "busy") -> 303 to /update?flash=<key> with "Couldn't schedule that update — please try again." and registry unchanged
    - A version value that is not a plausible version (e.g. "<script>", 200 chars) -> treated as unknown, never echoed unescaped
    - POST /update/cancel with no offered event -> 303 /update, schedule null
    - POST /update/cancel after byos recorded an offered event for the schedule -> 303 /update?flash=<key> with "Couldn't cancel — the frame may have already started.", schedule unchanged
    - Both routes: no session -> login redirect; cross-origin Origin header -> rejected by the existing gate
    - lang=fr renders the confirm page and both flash messages in French
  </behavior>
  <action>
Re-read app.py on main. Add `UPDATE_INSTALL_ROUTE = "/update/install"` and `UPDATE_CANCEL_ROUTE = "/update/cancel"` (route table entries, session required, POST only). `_handle_update_install_post`: read_form; version must match firmware_registry's VERSION_RE else flash failure; if the `confirm` field is not exactly "yes", render `update_page.update_install_confirm_page(ctx, version, next_wake_text)` at 200 without touching state (the server-side page is the real gate per D-04; the client data-confirm is a misclick guard only); otherwise call schedule_release with the running version from update_view and redirect to /update, or to /update?flash=update_schedule_failed on any non-"scheduled" result or exception. `_handle_update_cancel_post`: cancel_schedule; "cancelled" or "none" -> /update; "not_cancellable" or exception -> /update?flash=update_cancel_failed. Add both flash keys and English messages (UI-SPEC wording) to FLASH_MESSAGES and French forms to the catalog. `update_install_confirm_page` follows calendar_disconnect_confirm_page's shape exactly: page header "Install firmware %s?", the body sentence "The frame will download and install this version at its next wake, around %s. It will keep the update after one successful check-in — otherwise it rolls back automatically.", a form re-posting hidden version and confirm=yes with one submit button "Install", and a plain link "Cancel" to /update (never a second form). The next-wake text uses the same helper plan 09 uses. Tests first, as plain requests (no browser).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q companion/test_update_actions.py companion/test_update_page.py companion/test_post_origin.py companion/test_suite_guards.py companion/test_i18n.py && server/.venv/bin/ruff check companion && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour case passes without a browser
    - The companion non-browser suite still passes: `server/.venv/bin/python -m pytest -q companion -m "not browser"`
    - If Phase 40 added a test that every non-public route requires a session, it covers both new routes without modification (they were declared in the route table)
  </acceptance_criteria>
  <done>Install and Cancel work with JavaScript off, with a real confirmation step and honest failure messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Browser tests: JS confirm, full flow, and the mobile-fit gate</name>
  <files>companion/test_browser_update.py</files>
  <read_first>
    - companion/test_browser_ux_helpers.py and companion/conftest.py (browser fixture, new_context, VIEWPORT constants, how hit-target boxes are measured in their own container)
    - companion/test_browser_ux_01.py (the measured tap-target pattern at the minimum supported viewport)
    - companion/static/confirm-submit.js (how data-confirm gates a submit)
    - .claude/skills/sketch-findings-skypane/references/mobile-navigation.md (More sheet structure and the 44 px floor)
    - 42-UI-SPEC.md "D-02's mobile-fit gate" checklist
  </read_first>
  <behavior>
    - With JS: clicking Install on a history row opens a native confirm dialog; dismissing it sends no request and the registry is unchanged; accepting it lands on the server confirmation page; submitting that page schedules the release and /update shows Scheduled with a Cancel button; clicking Cancel returns to Available
    - With JavaScript disabled (browser context java_script_enabled=False): the same Install click goes straight to the server confirmation page and completing it schedules
    - At viewports 375x812 and 390x844, lang en and fr: open the tab bar's More sheet; it lists exactly Health, Device, Update; each link's resolved hit box (measured in its own container) is at least 44 px tall and its text is not truncated (scrollWidth <= clientWidth) and on one line; the More summary cell's box has the same height as the other tab-bar cells (a relationship, not a stored number); the header dropdown contains no /update link
    - At 1280 px the sidebar Advanced group shows three links with no overlap
  </behavior>
  <action>
Write `companion/test_browser_update.py` marked `browser`, using the shared app-server fixture with a tmp state dir populated through firmware_registry (two releases, one running). Measure boxes with the project's existing helpers (resolved hit box in its own container, per the design system's review-feedback discipline: assert relationships, not magic endpoints). Record the measured heights and widths per viewport and language in the test output (print) so the SUMMARY can list them as the D-02 proof. If any measurement fails, stop and report "blocked: Update does not fit the More sheet at <viewport>/<lang>" — the fix is a layout decision to raise with the developer, never moving Update back into Health or Device (per D-02).
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python -m pytest -q companion/test_browser_update.py</automated>
  </verify>
  <acceptance_criteria>
    - The browser tests pass locally with the Playwright headless shell (a missing browser fails, as in CI)
    - The SUMMARY lists the measured More-sheet link heights for 375/390 px in en and fr, each >= 44
    - `./scripts/run-all-tests.sh` stays green with the coverage gate unchanged or higher
  </acceptance_criteria>
  <done>The confirm flows are proven in a real browser with and without JS, and the three-entry Advanced group is measured to fit on phones in both languages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> companion POST | operator-initiated state change that eventually flashes the frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-60 | Spoofing (CSRF) | /update/install, /update/cancel | mitigate | existing Origin/Sec-Fetch-Site gate on every POST plus session; tested |
| T-42-61 | Elevation of Privilege | scheduling without confirmation | mitigate | server-side confirm=yes gate; the confirmation page is the control, JS is only a misclick guard |
| T-42-62 | Tampering | arbitrary version string | mitigate | VERSION_RE validation, registry membership, floor check in schedule_release; escaped output |
| T-42-63 | Tampering | cancel after the device started | mitigate | cancel_schedule checks the served-offer event under the registry lock; honest flash message |
</threat_model>

<verification>
- `server/.venv/bin/python -m pytest -q companion` (browser tests included) and `./scripts/run-all-tests.sh`
</verification>

<success_criteria>
- OTA-08 fully holds: states, history, confirm with and without JS, cancel window, any release at or above the floor, and the measured phone fit
</success_criteria>

<output>
Create `.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-14-SUMMARY.md` when done
</output>
