---
phase: 42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0
plan: 09
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - companion/layout.py
  - companion/pages/update_page.py
  - companion/app.py
  - companion/i18n_fr/update.py
  - companion/i18n_fr/nav.py
  - companion/i18n_fr/__init__.py
  - companion/test_update_page.py
autonomous: true
requirements: [OTA-08]
must_haves:
  truths:
    - "The companion has an Update page ('Mise à jour' in French) at /update, the third entry of the Advanced nav group after Health and Device, added once through NAV_GROUPS so the sidebar, the tab bar's More sheet and every derived nav list pick it up (D-02)"
    - "The page shows the running version as last reported, the update state (Available / Scheduled / In progress / Installed / Failed) with its timestamp, a warn-toned rollback banner when the frame came back on the previous image, and every published release with date, generated notes and when it was installed (D-03, D-07)"
    - "A scheduled state names the expected next wake time (D-04)"
    - "Every release at or above the floor that differs from the running version shows an Install button; releases below the floor show 'Not installable' as plain text (D-06, D-11)"
    - "Cancel is shown only while the schedule is cancellable and is absent (not disabled) once the device acknowledged the offer (D-05)"
    - "Every user-facing string exists in English and French"
    - "The page requires the existing companion session like every non-public route"
  artifacts:
    - path: companion/pages/update_page.py
      provides: "update_page(ctx, view, next_wake) rendering the two cards from firmware_registry.update_view"
    - path: companion/layout.py
      provides: "UPDATE_ROUTE, NAV_GROUPS Advanced third entry, icon-nav-update symbol"
      contains: "UPDATE_ROUTE"
    - path: companion/test_update_page.py
      provides: "Plain-request behaviour tests of the served HTML in EN and FR for every state"
  key_links:
    - from: companion/pages/update_page.py
      to: server/firmware_registry.py
      via: "update_view(load_registry(state_dir), load_device_report(state_dir), now)"
      pattern: "update_view"
    - from: companion/layout.py
      to: "/update"
      via: "NAV_GROUPS Advanced group"
      pattern: "UPDATE_ROUTE"
---

<objective>
Add the read side of the companion Update page: navigation entry, icon, the Status card and the Version history card, in English and French, driven by `firmware_registry.update_view`.

Purpose: OTA-08's display half (D-02, D-03, D-05 visibility, D-06/D-11 installability, D-07 notes). The POST actions and their confirmation flow are plan 14, so this plan renders buttons as forms targeting routes plan 14 implements.
Output: nav + icon, page module, GET route, FR strings, plain-request tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-UI-SPEC.md
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-01-SUMMARY.md

<interfaces>
From server/firmware_registry.py (plan 01): load_registry(state_dir), load_device_report(state_dir), update_view(registry, device_report, now) -> {running_version, reported_at, state, state_at, cancellable, rollback: {version, back_on} | None, releases: [{version, released_at, notes, installed_at, bench, installable}]}
From server/wake.py: next_wake_at_iso(last_checkin_ts, device_cfg, battery_critical=False)
Companion layout helpers (re-read on main; Phase 40 may have moved them): page_shell, page_header, status_dot(state, label), stat_tile, data_table(headers, rows, mono_columns, raw_columns, desc_columns, prose, ...), empty_state(heading, body), relative_time_html, concise_timestamp_html, escape_html
Route contract used by plan 14: POST /update/install (fields: version, confirm), POST /update/cancel (no fields beyond the CSRF/Origin gate the app already enforces)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Navigation entry, icon and FR nav label</name>
  <files>companion/layout.py, companion/i18n_fr/nav.py, companion/test_update_page.py</files>
  <read_first>
    - companion/layout.py (on current main after Phase 40: NAV_GROUPS, route constants, ICON_IDS, ICON_DEFS_HTML symbols, the slug-to-icon map, and how the tab bar's More sheet derives from the Advanced group)
    - companion/i18n_fr/nav.py (on current main: catalog shape; after Phase 40 CMP-09 translations may be keyed by stable IDs — use whatever mechanism is on main)
    - companion/test_status_pages_helpers.py / companion/conftest.py / test-support/companion_app_server.py (the shared app-server fixtures and request helpers)
    - companion/test_suite_guards.py (tests must assert served HTML, never source text)
    - .claude/skills/sketch-findings-skypane/references/mobile-navigation.md (the More sheet contract)
  </read_first>
  <behavior>
    - GET /health (logged in) serves a sidebar whose Advanced group lists Health, Device, Update in that order, each a link; /update's link carries the active state when GET /update is served
    - The tab bar's More sheet in the served HTML lists exactly Health, Device, Update; the tab bar still has its existing fixed cells (Update is not a new cell)
    - The header dropdown preferences panel gains no Update link
    - With lang=fr the nav label reads "Mise à jour"
    - The served page includes a `<symbol id="icon-nav-update"` in the icon sprite and the Update link uses `href="#icon-nav-update"`
  </behavior>
  <action>
Re-read layout.py on main. Add `UPDATE_ROUTE = "/update"` and append `(UPDATE_ROUTE, "Update")` as the third entry of the Advanced group in NAV_GROUPS (per D-02; the single edit point, never hand-list the route elsewhere). Add the `icon-nav-update` symbol (20x20 viewBox, `fill="none" stroke="currentColor" stroke-width="1.5"`, a two-arrow cycle glyph per UI-SPEC, distinct from icon-refresh), its ICON_IDS entry and the slug map entry. If Phase 40 turned routes into a declared table with auth flags, declare /update there as session-required. Add "Update": "Mise à jour" to the FR nav catalog (or its stable-ID equivalent). Tests first, asserting the served HTML only.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q companion/test_update_page.py companion/test_suite_guards.py && server/.venv/bin/ruff check companion</automated>
  </verify>
  <acceptance_criteria>
    - The nav behaviour tests pass in EN and FR
    - The existing companion nav tests still pass: `server/.venv/bin/python -m pytest -q companion -k "nav or tab_bar or sidebar"`
  </acceptance_criteria>
  <done>Update is the third Advanced destination everywhere the nav is derived.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update page (Status and Version history cards) and GET route</name>
  <files>companion/pages/update_page.py, companion/app.py, companion/i18n_fr/update.py, companion/i18n_fr/__init__.py, companion/test_update_page.py</files>
  <read_first>
    - companion/pages/health_page.py (on current main: card-per-concern section builders, stat tile body, anomaly banner)
    - companion/pages/config_page.py (confirm-page and flash conventions plan 14 will reuse; here only for the button/form markup conventions)
    - companion/app.py (on current main after Phase 40: the route table or do_GET dispatch, page_context, _page_shell_for, how a page reads state_dir and device_cfg, FLASH_MESSAGES rendering)
    - companion/frame_state.py (the "Next update ≈ %s" register and its French form, reused for the scheduled sentence)
    - companion/i18n_fr/health.py and companion/i18n_fr/__init__.py (catalog registration)
    - server/firmware_registry.py (update_view output)
    - 42-UI-SPEC.md Surface 1 (card order, state-to-dot mapping, copy table, empty states, accent reservation)
  </read_first>
  <behavior>
    - With an empty registry: Status card shows "No version reported yet" when the device never reported, and Version history is the empty state "No releases yet" with its body text
    - With releases fw-v1.0.0 (installed at T1), fw-v1.1.0, and running fw-v1.1.0: rows are newest first; fw-v1.1.0 row has no Install button (it is running); fw-v1.0.0 row has an Install form posting to /update/install with a hidden version field fw-v1.0.0 and a `data-confirm` attribute; the Installed column shows a timestamp for fw-v1.0.0
    - A release below the floor shows the text "Not installable" and no form
    - Each of the five states renders its word with the dot class from UI-SPEC (Available/Scheduled -> dot--off, In progress -> dot--warn, Installed -> dot--ok, Failed -> dot--error) and a timestamp element
    - Scheduled and not yet offered: the sentence "Scheduled — installs at the next wake, around <time>." and a Cancel form posting to /update/cancel whose button is not the accent-styled primary (it carries the quiet button class the UI-SPEC names)
    - In progress (offered): no Cancel form anywhere in the page
    - Rollback outcome: a role="alert" warn banner "Firmware rolled back — the update to <v> failed on trial boot; the frame is back on <w>."
    - Notes render as text (HTML in a note is escaped), version tags in monospace cells
    - lang=fr renders every new string in French; the version tags are not translated
    - GET /update without a session redirects to login like other pages
  </behavior>
  <action>
Create `companion/pages/update_page.py` with `update_page(ctx, view, next_wake_text)` returning the page body: `layout.page_header` titled "Update", then card 1 "Status" (running version tile via the stat-tile verdict/detail pattern, state via `layout.status_dot`, timestamp via `concise_timestamp_html`, the scheduled sentence using `relative_time_html(..., countdown=True)` or the absolute clock per UI-SPEC, the rollback banner reusing the health page's warn anomaly banner markup, and the Cancel form only when `view["cancellable"]`), then card 2 "Version history" via `layout.data_table(headers=("Version","Date","Notes","Installed",""), mono_columns=(0,), desc_columns=(2,), prose=True, ...)`, or `layout.empty_state` when there are no releases. The per-row Install form posts `version` (no `confirm` field) to /update/install and carries `data-confirm` with the UI-SPEC wording; include the existing confirm-submit.js script the way other pages include per-page scripts. No new CSS token, colour or spacing value; if a class is missing, reuse an existing one (UI-SPEC Color/Spacing: nothing new). Define every English string as a module constant, and add `companion/i18n_fr/update.py` with the French forms (reuse the existing French for "Next update ≈ %s" register), registered wherever the catalogs are registered on main.

In companion/app.py (or Phase 40's route table) add GET /update: load the view via `firmware_registry.update_view(load_registry(state_dir), load_device_report(state_dir), now)`, compute the next-wake text from `server.wake.next_wake_at_iso` with the same inputs the Health page uses, and render through the standard page shell with the Update nav slug active. Tests assert served HTML through the shared app-server fixture with a tmp state dir populated via firmware_registry's own functions and hand-written device_report.json fixtures.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q companion/test_update_page.py companion/test_suite_guards.py companion/test_i18n.py && server/.venv/bin/ruff check companion && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - Every behaviour above is a passing plain-request test in EN, and the FR variants pass for the state words, the scheduled sentence, the rollback banner and the empty state
    - companion/test_i18n.py (or its Phase 40 successor) passes, proving no new English string lacks a French form
    - `grep -cE '#[0-9A-Fa-f]{6}' companion/pages/update_page.py` is 0 (no hard-coded colour)
    - The full companion suite still passes: `server/.venv/bin/python -m pytest -q companion -m "not browser"`
  </acceptance_criteria>
  <done>The Update page renders every state, the rollback warning and the release history in both languages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry/device report -> HTML | CI-generated notes and device-reported versions rendered to the operator |
| browser -> companion | authenticated operator session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-40 | Tampering (XSS) | notes / versions in the page | mitigate | every dynamic value through escape_html; a test injects `<script>` into a note and asserts it is escaped |
| T-42-41 | Elevation of Privilege | /update without login | mitigate | route declared session-required (Phase 40 route table) and tested for the login redirect |
| T-42-42 | Information Disclosure | firmware versions | accept | versions are not secret; page is behind the operator login |
</threat_model>

<verification>
- `server/.venv/bin/python -m pytest -q companion -m "not browser"` and ruff
</verification>

<success_criteria>
- OTA-08's display requirements hold in the served HTML for every state and both languages
</success_criteria>

<output>
Create `.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-09-SUMMARY.md` when done
</output>
