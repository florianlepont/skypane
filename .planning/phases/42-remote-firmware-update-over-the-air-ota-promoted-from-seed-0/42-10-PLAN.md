---
phase: 42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0
plan: 10
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - server/notify.py
  - server/test_notify.py
  - server/poll_loop.py
  - server/test_firmware_reconcile.py
autonomous: true
requirements: [OTA-06, OTA-09]
must_haves:
  truths:
    - "Every poll cycle, including battery-hold, quiet-hours and display-off cycles, folds new device reports into the registry: installed, rollback, and failed after the third counted attempt, which withdraws the offer (D-15)"
    - "A push notification fires once when an update is installed ('Firmware X installed') and once when it fails for good ('Update failed, back on Y'), through server/notify.py with the configured topic and language (D-08)"
    - "French bodies live in notify.py's _BODY_FR table keyed by the English string, and nothing logs the topic URL (D-08)"
    - "A reconcile error never breaks the poll cycle"
  artifacts:
    - path: server/notify.py
      provides: "FIRMWARE_INSTALLED_BODY, FIRMWARE_FAILED_BODY and their French forms"
      contains: "FIRMWARE_INSTALLED_BODY"
    - path: server/poll_loop.py
      provides: "The firmware reconcile step in every cycle"
    - path: server/test_firmware_reconcile.py
      provides: "Cycle-level tests with a fake notification sender"
  key_links:
    - from: server/poll_loop.py
      to: server/firmware_registry.py
      via: "apply_reconcile (locked), then send notifications outside the lock"
      pattern: "apply_reconcile"
    - from: server/poll_loop.py
      to: server/notify.py
      via: "send_notification with body_for_lang"
      pattern: "FIRMWARE_(INSTALLED|FAILED)_BODY"
---

<objective>
Close the outcome loop on the server: every poll cycle reconciles byos's device report into the release registry, and the two OTA outcomes produce push notifications in the operator's language.

Purpose: OTA-06's server half (three attempts, then failed and withdrawn) and OTA-09 (D-08 notifications).
Output: notify.py bodies, a reconcile step in the poll cycle, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-PATTERNS.md
@.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-01-SUMMARY.md

<interfaces>
From server/firmware_registry.py (plan 01): apply_reconcile(state_dir, now) -> [("installed", version, None) | ("failed", version, back_on)] (locked load + reconcile + save).
From server/notify.py: send_notification(topic_url, title, body, timeout=5, transport=None) -> bool; body_for_lang(text, lang); ALERT_TITLE; _BODY_FR.
From server/poll_loop.py: _notifications_group(device_cfg); the battery transition notifier is the model for topic/lang/enabled gating.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notification bodies EN/FR</name>
  <files>server/notify.py, server/test_notify.py</files>
  <read_first>
    - server/notify.py (on current main; Phase 36 may have changed transport/deadline handling)
    - server/test_notify.py (existing assertions on body_for_lang and _BODY_FR)
  </read_first>
  <behavior>
    - notify.FIRMWARE_INSTALLED_BODY == "Firmware %s installed" and notify.FIRMWARE_FAILED_BODY == "Update failed, back on %s"
    - body_for_lang(FIRMWARE_INSTALLED_BODY, "fr") % "fw-v1.4.0" == "Micrologiciel fw-v1.4.0 installé"
    - body_for_lang(FIRMWARE_FAILED_BODY, "fr") % "fw-v1.3.0" == "Échec de la mise à jour, retour à fw-v1.3.0"
    - body_for_lang with "en" returns the English string unchanged
  </behavior>
  <action>
Add the two English constants next to the existing bodies and their French forms to `_BODY_FR` keyed by the identical English string (French typography per the catalog conventions: typographic apostrophe, sentence case). No change to send_notification. Tests first.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_notify.py && server/.venv/bin/ruff check server/notify.py server/test_notify.py</automated>
  </verify>
  <acceptance_criteria>
    - The four behaviour cases pass alongside the existing notify tests
    - `grep -c 'FIRMWARE_INSTALLED_BODY\|FIRMWARE_FAILED_BODY' server/notify.py` is at least 2
  </acceptance_criteria>
  <done>Both OTA notification bodies exist in English and French.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile step in every poll cycle, with notifications</name>
  <files>server/poll_loop.py, server/test_firmware_reconcile.py</files>
  <read_first>
    - server/poll_loop.py (on current main after Phase 39: run_once's step list / context object, where hold branches return early, _notify_battery_transition's gating on notifications enabled/topic/lang)
    - server/firmware_registry.py (apply_reconcile from plan 01)
    - server/test_poll_loop.py (how run_once is driven with a fake snapshot and tmp state dir, and how a fake sender is injected)
  </read_first>
  <behavior>
    - A device report with an "installed;fw-v1.1.0" result for the scheduled release: after one run_once the registry shows fw-v1.1.0 installed, the schedule is null, and the fake sender received exactly one body "Firmware fw-v1.1.0 installed" (or the French form when the notifications language is fr)
    - Three "fail-hash;fw-v1.1.0" results for the current schedule with the device on fw-v1.0.0: after run_once, schedule state is "failed" and exactly one "Update failed, back on fw-v1.0.0" notification was sent; a second run_once sends nothing more
    - One "rollback;fw-v1.1.0" result: attempts 1, last_outcome kind rollback, no notification
    - The step runs when the cycle takes the battery-hold branch, the quiet-hours branch and the display-off branch (parametrised)
    - With notifications disabled or no topic, the registry still updates and nothing is sent
    - If reconcile raises (monkeypatched), run_once completes its normal work and logs one line with the exception type only
  </behavior>
  <action>
Re-read poll_loop.py on main. Add a `_reconcile_firmware(state_dir, device_cfg, now, sender=None)` step (or the equivalent named step in Phase 39's step list) that runs before any early-returning hold branch so every cycle executes it. It calls `firmware_registry.apply_reconcile(state_dir, now)`, then, outside the lock, sends each returned notification through `notify.send_notification(topic_url, notify.ALERT_TITLE, body)` with `notify.body_for_lang(...)` and the language from the notifications group, using exactly the enabled/topic gating `_notify_battery_transition` uses (per D-08). The whole step is wrapped so any exception logs `firmware reconcile failed: <ExceptionType>` to stderr and the cycle continues; never log the topic URL or exception text. Tests first, using the existing run_once test harness and an injected sender.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_firmware_reconcile.py server/test_poll_loop.py && server/.venv/bin/ruff check server && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - All behaviour cases pass and server/test_poll_loop.py still passes
    - `grep -c 'firmware_registry' server/poll_loop.py` is at least 1 (or in the Phase 39 step module that owns the cycle steps)
    - No function added exceeds the 80-code-line limit Phase 39 set (run the project's function-length check if one exists on main)
  </acceptance_criteria>
  <done>Outcomes reach the registry every cycle, and the operator gets exactly one push per installed or failed update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device report -> notifications | device-reported versions appear in push text |
| server -> push topic | outbound HTTPS to the configured topic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-43 | Information Disclosure | topic URL in logs | mitigate | reuse notify.send_notification (never logs URL); step logs exception type only |
| T-42-44 | Denial of Service | reconcile failure breaks polling | mitigate | step wrapped; cycle continues; tested with a raising reconcile |
| T-42-45 | Spoofing | notification text injection via version string | mitigate | versions validated by byos regex before storage; body is a fixed template with one %s |
| T-42-46 | Repudiation | duplicate or lost notifications | mitigate | reconcile processes each event once (reconciled_seq); tested that a second cycle sends nothing |
</threat_model>

<verification>
- `server/.venv/bin/python -m pytest -q server/test_notify.py server/test_firmware_reconcile.py server/test_poll_loop.py`
</verification>

<success_criteria>
- D-08 and D-15's server behaviour runs in every cycle and is proven by tests
</success_criteria>

<output>
Create `.planning/phases/42-remote-firmware-update-over-the-air-ota-promoted-from-seed-0/42-10-SUMMARY.md` when done
</output>
