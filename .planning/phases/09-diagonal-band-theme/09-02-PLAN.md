---
phase: 09-diagonal-band-theme
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - server/plane/render.py
  - server/test_render.py
autonomous: true
requirements: [PHASE9-1, PHASE9-3]

must_haves:
  truths:
    - "Calling draw_diagonal_band(canvas, band_idx, dithered=False/True) on a fresh White canvas paints the exact measured trapezoid (top spans 58.18%-85.23% of canvas width, bottom spans 7.42%-47.72%) in band_idx's colour, flat or dithered toward White depending on the dithered flag, using only legal panel indices."
    - "_build_active_canvas() draws the diagonal band immediately after the background fill and before the top labels/illustrations/text for every band theme (per device_config.theme_is_band()), and draws nothing extra for every non-band theme — the 11 existing themes render byte-identical to their pre-phase output."
    - "draw_top_labels() draws the merged 'DEPARTING FROM ORY' / 'ARRIVING TO ORY' label on the left and the split 'RWY 3' tag alone on the right when band_theme=True, reusing runway_tag_text()'s real output (partitioned on ' · ', never hardcoded or re-derived), and draws today's unchanged separate state-label / full-tag pair when band_theme=False (the default)."
  artifacts:
    - path: "server/plane/render.py"
      provides: "draw_diagonal_band(), band geometry constants, band-aware draw_top_labels(), _build_active_canvas() band dispatch for the background/label stage"
    - path: "server/test_render.py"
      provides: "New coverage for the band primitive's geometry/palette-legality and the split top-label text, plus regression proof the 11 existing themes are unaffected"
  key_links:
    - from: "device_config.theme_is_band(normalised_theme_id) / theme_band_index() / theme_band_dithered()"
      to: "render._build_active_canvas()'s new band-dispatch block"
      via: "direct accessor calls right after normalised_theme_id is resolved"
      pattern: "theme_is_band\\(normalised_theme_id\\)"
    - from: "render.draw_diagonal_band(canvas, band_idx, dithered=...)"
      to: "the canvas returned by pf.new_canvas(bg_idx) / dither.dithered_state_background(bg_idx)"
      via: "called once, directly after canvas creation, before draw_top_labels()"
      pattern: "draw_diagonal_band\\(canvas"
---

<objective>
Port the first half of spike `003-diagonal-band-theme`'s validated composition into production `server/plane/render.py`: the diagonal trapezoid band drawing primitive itself, and the band-aware top-label split (merged state label + separate short runway tag). Wire both into `_build_active_canvas()` for the 5 band themes registered in plan 09-01, with zero behavior change for the 11 existing themes.

This plan implements PHASE9-1 (band primitive) and PHASE9-3 (top-label split) of ROADMAP.md's Phase 9 Goal — see plan 09-01's objective for the full PHASE9-1..8 traceability table (this phase has no CONTEXT.md decisions to cite instead).

The main/previous flight-text three-tier hierarchy (PHASE9-4/5/6) is deliberately NOT in this plan — it lands in plan 09-03, which depends on this one because both touch the same `_build_active_canvas()` function and `draw_main_text_block()`/`draw_previous_text_block()` are edited there, not here. This plan's band themes will render correctly (band drawn, split labels) but with the OLD centred two-line text block still on top until 09-03 lands — that is an expected, tested intermediate state, not a bug.

Purpose: land the band geometry and the top-label change as one self-contained, independently testable unit before the more involved text-hierarchy port.
Output: `draw_diagonal_band()`, `_band_center_x()`-adjacent geometry constants, a band-aware `draw_top_labels()`, and `_build_active_canvas()`'s band/label dispatch, all covered by new `server/test_render.py` checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagonal-band-theme/09-PATTERNS.md
@.planning/phases/09-diagonal-band-theme/09-01-PLAN.md
@.planning/spikes/003-diagonal-band-theme/README.md
@.planning/spikes/003-diagonal-band-theme/explore_full_composition.py
</context>

<interfaces>
<!-- Exact current signatures/constants this plan reads or extends. Extracted
     from server/plane/render.py so no exploration is needed. -->

Existing (unchanged) imports already available at the top of render.py:
`from PIL import Image, ImageDraw, ImageFont`; `from server import device_config`; `from server import panel_format as pf`; `from server.panel_format import IDX_BLACK, IDX_BLUE, IDX_GREEN, IDX_RED, IDX_WHITE, IDX_YELLOW, WIDTH, HEIGHT`; `from server.plane import dither, enrich, illustrations, runway_config`.

Existing helper this plan reuses verbatim (do not modify): `dither.dithered_state_background(bg_idx, lighten_fraction=0.4)` in `server/plane/dither.py` — returns a full WIDTHxHEIGHT "P"-mode canvas of `bg_idx`'s ink dithered toward White, already used identically for the state background.

`server/plane/render.py`'s current `draw_top_labels()` (lines 720-772):
```
def draw_top_labels(canvas, state, ink_idx, bg_idx, weight, runway_id=device_config.DEFAULT_RUNWAY_ID):
    draw = ImageDraw.Draw(canvas)
    label_font = _role_font(STATE_LABEL_FONT, weight)
    tag_font = _role_font(TOP_TAG_FONT, weight)
    tag_text = runway_tag_text(runway_id)

    label_text = STATE_LABEL_TEXT[state]
    label_bbox = _tracked_text_bbox(label_font, (MARGIN, MARGIN), label_text, LABEL_TRACKING_PX)
    _assert_within_canvas(label_bbox, "state label")
    draw_tracked_text(draw, (MARGIN, MARGIN), label_text, label_font, ink_idx, tracking=LABEL_TRACKING_PX)

    tag_width = _tracked_text_width(tag_font, tag_text, LABEL_TRACKING_PX)
    tag_x = WIDTH - MARGIN - tag_width
    tag_bbox = _tracked_text_bbox(tag_font, (tag_x, MARGIN), tag_text, LABEL_TRACKING_PX)
    _assert_within_canvas(tag_bbox, "top-right tag")
    draw_tracked_text(draw, (tag_x, MARGIN), tag_text, tag_font, ink_idx, tracking=LABEL_TRACKING_PX)
```

`server/plane/render.py`'s current `_build_active_canvas()` (lines 1447-1568) — the relevant excerpt this plan edits:
```
    normalised_theme_id = device_config.normalise_theme_id(theme_id)
    theme_dithered = device_config.theme_dithered(normalised_theme_id)
    weight = device_config.theme_weight(normalised_theme_id)

    canvas = dither.dithered_state_background(bg_idx) if theme_dithered else pf.new_canvas(bg_idx)

    draw_top_labels(canvas, state, fg_idx, bg_idx, weight, runway_id=runway_id)
```

`server.plane.runway_config.STATE_DEPARTING = "departing"`, `STATE_ARRIVING = "arriving"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: draw_diagonal_band() and the band geometry constants</name>
  <files>server/plane/render.py</files>
  <read_first>
    - `.planning/spikes/003-diagonal-band-theme/explore_full_composition.py` lines 66-102 (`BAND_SHIFT_FRAC`/`BAND_TOP_LEFT_FRAC`/`BAND_TOP_RIGHT_FRAC`/`BAND_BOT_LEFT_FRAC`/`BAND_BOT_RIGHT_FRAC` and `draw_reference_band()`) — the exact geometry and drawing logic to port, renamed only where noted below.
    - `server/plane/render.py` lines 554-562 (`draw_frame()`) for this file's existing convention of a small drawing helper that takes `canvas` + a colour index and returns/paints directly — `draw_diagonal_band()` follows the same shape.
    - `server/plane/dither.py` lines 54-96 (`dithered_state_background()`) — confirm its full-canvas return shape before using it as the paste source below.
  </read_first>
  <action>
Add 5 new module-level constants directly after `PREVIOUS_TEXT_GAP_PX` (line 372) and before the battery-icon geometry block, matching the spike's exact measured values (per-row pixel scanning + linear regression against the developer's reference image, round 2/11 of the spike, confirmed final at round 15):

`BAND_SHIFT_FRAC = 0.0` (the reference's own unshifted position — confirmed clear of all text once the top tag is split, see Task 2 below); `BAND_TOP_LEFT_FRAC = 0.5818 + BAND_SHIFT_FRAC`; `BAND_TOP_RIGHT_FRAC = 0.8523 + BAND_SHIFT_FRAC`; `BAND_BOT_LEFT_FRAC = max(0.0, 0.0742 + BAND_SHIFT_FRAC)`; `BAND_BOT_RIGHT_FRAC = 0.4772 + BAND_SHIFT_FRAC`.

Add `draw_diagonal_band(canvas, band_idx, dithered=False)` directly after these constants: build the 4-point trapezoid polygon `[(BAND_TOP_LEFT_FRAC * w, 0), (BAND_TOP_RIGHT_FRAC * w, 0), (BAND_BOT_RIGHT_FRAC * w, h), (BAND_BOT_LEFT_FRAC * w, h)]` from `canvas.size`. When `dithered` is true: build `band_fill = dither.dithered_state_background(band_idx)`, an `"L"`-mode mask the same size as canvas with the polygon filled 255 (everything else 0), and `canvas.paste(band_fill, (0, 0), mask)` so only the trapezoid region receives the dithered fill. When `dithered` is false: draw the polygon directly onto `canvas` with `ImageDraw.Draw(canvas).polygon(poly, fill=band_idx)`. This is a verbatim, renamed port of the spike's `draw_reference_band()` — no geometry or logic change, just moving it from a monkeypatch into a real production function operating directly on the caller's canvas (no return value needed; the caller already holds the canvas reference).

Give the function a docstring naming: the geometry's provenance (measured from the developer's reference image, confirmed final at spike round 15), that it must be called BEFORE any text/illustration drawing so the band sits behind the aircraft (matching D-24's "illustrations occlude the band naturally" behavior already proven in the spike), and that `band_idx` must be one of the 6 legal `panel_format.IDX_*` values (never a bare integer) so `_assert_legal_palette()` downstream stays satisfied.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
from PIL import Image
from server import panel_format as pf
from server.plane import render
c = pf.new_canvas(pf.IDX_WHITE)
render.draw_diagonal_band(c, pf.IDX_BLUE, dithered=False)
colors = {v for _n, v in c.getcolors()}
assert colors <= {pf.IDX_WHITE, pf.IDX_BLUE}, colors
assert pf.IDX_BLUE in colors
c2 = pf.new_canvas(pf.IDX_WHITE)
render.draw_diagonal_band(c2, pf.IDX_GREEN, dithered=True)
colors2 = {v for _n, v in c2.getcolors()}
assert colors2 <= {pf.IDX_WHITE, pf.IDX_GREEN}, colors2
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `draw_diagonal_band(canvas, IDX_BLUE, dithered=False)` on a fresh White canvas paints only White and Blue pixels, with Blue occupying a trapezoid region matching the measured fractions (top edge 58.18%-85.23% of width at y=0, bottom edge 7.42%-47.72% of width at y=HEIGHT).
    - `draw_diagonal_band(canvas, IDX_GREEN, dithered=True)` on a fresh White canvas paints only White and Green pixels (the dithered blend's two-colour palette, per `dithered_state_background()`'s own contract), confined to the same trapezoid region.
    - Calling `draw_diagonal_band()` before any illustration/text draw leaves the band fully paintable-over by subsequent draws (no special masking needed by callers) — proven implicitly by plan 09-03's later full-composition checks, not required to be proven standalone here.
    - The 5 new geometry constants exist at module level with the exact values above; `git diff` shows only additive changes to `server/plane/render.py`.
  </acceptance_criteria>
  <done>draw_diagonal_band() paints the exact measured trapezoid in the requested colour, flat or dithered toward White, using only legal panel indices.</done>
</task>

<task type="auto">
  <name>Task 2: Band-aware draw_top_labels() and the _build_active_canvas() band/label dispatch</name>
  <files>server/plane/render.py</files>
  <read_first>
    - `.planning/spikes/003-diagonal-band-theme/explore_full_composition.py` lines 105-137 (`_MERGED_LABEL_DIRECTION` and `patched_draw_top_labels()`) — read the whole function including its own comment block (lines 105-121), which documents WHY `BAND_SHIFT_FRAC=0.0` is safe: with "RWY 3" alone starting 7 points further right than the full "ORY · RWY 3" tag did, the reference's own unshifted band position already clears both the shorter tag and the below-illustration text with margin.
    - `server/plane/render.py` lines 720-772 (`draw_top_labels()`, quoted in full in this plan's `<interfaces>` section above) — the exact function this task extends with a new trailing `band_theme` parameter, keeping every existing line's behavior byte-identical when `band_theme=False`.
    - `server/plane/render.py` lines 1447-1499 (`_build_active_canvas()`, the relevant excerpt already quoted in `<interfaces>` above) — the exact insertion points for this task's two changes.
    - `server/device_config.py`'s new `theme_is_band()`/`theme_band_index()`/`theme_band_dithered()` accessors (plan 09-01, already merged) — this task's only way to detect a band theme; never hardcode a band-theme id list in render.py.
  </read_first>
  <action>
Add a new module-level constant directly after the `LABEL_TRACKING_PX` block: `_BAND_TOP_LABEL_DIRECTION = {runway_config.STATE_DEPARTING: "FROM", runway_config.STATE_ARRIVING: "TO"}`.

Extend `draw_top_labels()`'s signature with one new trailing keyword parameter, default `False`: `def draw_top_labels(canvas, state, ink_idx, bg_idx, weight, runway_id=device_config.DEFAULT_RUNWAY_ID, band_theme=False):`. Inside the function, replace the two lines `tag_text = runway_tag_text(runway_id)` and the `label_text = STATE_LABEL_TEXT[state]` line with a small branch computed BEFORE the existing bbox/draw calls, so the rest of the function (the `_tracked_text_bbox()`/`_assert_within_canvas()`/`draw_tracked_text()` calls for both the left label and the right tag) runs completely unchanged, just fed different `label_text`/`tag_text` values:

- Always compute `full_tag = runway_tag_text(runway_id)` first (same call as today).
- When `band_theme` is true: split it with `airport_code, _sep, runway_part = full_tag.partition(" · ")`; set `label_text = "%s %s %s" % (STATE_LABEL_TEXT[state], _BAND_TOP_LABEL_DIRECTION[state], airport_code)` (e.g. "DEPARTING FROM ORY"); set `tag_text = runway_part` (e.g. "RWY 3" alone).
- When `band_theme` is false (the default): `label_text = STATE_LABEL_TEXT[state]` and `tag_text = full_tag` — today's exact values, unchanged.

This must reuse `runway_tag_text(runway_id)`'s real return value via `.partition(" · ")` — never hardcode "ORY" or "RWY 3" as literals, so a future runway selection (CFG-12, "06-24"/"02-20") splits correctly too. The existing `_assert_within_canvas()` calls on both bboxes already present in this function stay exactly where they are — they now guard whichever text was actually computed, band or not, with no separate assert path needed.

Then wire this into `_build_active_canvas()`, in the excerpt quoted in `<interfaces>` above:
- Immediately after `weight = device_config.theme_weight(normalised_theme_id)`, add: `is_band_theme = device_config.theme_is_band(normalised_theme_id)`; `band_idx = device_config.theme_band_index(normalised_theme_id) if is_band_theme else None`; `band_dithered_flag = device_config.theme_band_dithered(normalised_theme_id) if is_band_theme else False`.
- Immediately after the `canvas = dither.dithered_state_background(bg_idx) if theme_dithered else pf.new_canvas(bg_idx)` line, add: `if is_band_theme: draw_diagonal_band(canvas, band_idx, dithered=band_dithered_flag)` — this draws the band before `draw_top_labels()`/illustrations/text run, so it sits behind everything, matching PHASE9-1's "band behind the illustration" requirement.
- Change the existing `draw_top_labels(canvas, state, fg_idx, bg_idx, weight, runway_id=runway_id)` call to pass the new flag: `draw_top_labels(canvas, state, fg_idx, bg_idx, weight, runway_id=runway_id, band_theme=is_band_theme)`.

Do not touch the `draw_main_text_block()`/`draw_previous_text_block()` call sites in this same function yet — those gain their own `band_idx=` argument in plan 09-03, once those two functions are themselves made band-aware. Leaving them unchanged here means band themes render with the band drawn and the labels split, but the OLD centred 2-line text block still on top for this plan alone — an expected, deliberately incomplete intermediate state (see this plan's `<objective>`).
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
from server.plane import render
from server import device_config
for theme_id in device_config.THEME_IDS:
    for state in ('departing', 'arriving'):
        render.build_canvas({'hex':'39de41','callsign':'AFR1234','aircraft_type':'A320'}, state, route={'airline_name':'Air France','callsign_iata':'AF1234','origin_iata':'ORY','origin_city':'Paris','destination_iata':'JFK','destination_city':'New York'}, theme_id=theme_id)
print('OK all 16 themes render without exception')
"</automated>
  </verify>
  <acceptance_criteria>
    - `draw_top_labels(..., band_theme=False)` (the default, and every call site outside this phase's own new band-dispatch branch) draws exactly today's `STATE_LABEL_TEXT[state]` on the left and `runway_tag_text(runway_id)` in full on the right — byte-identical to pre-phase output, proven by re-running the 11 non-band themes through `build_canvas()` and confirming no pixel difference from a pre-phase reference (Task 3 makes this an automated regression check).
    - `draw_top_labels(..., band_theme=True)` draws `"DEPARTING FROM ORY"`/`"ARRIVING TO ORY"` on the left (state label + direction word + the airport-code half of `runway_tag_text()`'s real output) and `"RWY 3"` alone on the right (the runway-part half), for the default runway id — both halves derived from `runway_tag_text(runway_id).partition(" · ")`, never a hardcoded string.
    - Both bboxes (band and non-band paths alike) still pass `_assert_within_canvas()` — no new assertion failure introduced for any theme or runway id.
    - `_build_active_canvas()` calls `draw_diagonal_band()` if and only if `device_config.theme_is_band(normalised_theme_id)` is true, always before `draw_top_labels()`, and passes `band_theme=is_band_theme` through to `draw_top_labels()`.
    - All 16 registered themes (11 existing + 5 band) render through `build_canvas()` in both departing and arriving states without raising.
  </acceptance_criteria>
  <done>Band themes draw the trapezoid band and the split top-label pair; every existing theme's top labels are byte-identical to before this phase.</done>
</task>

<task type="auto">
  <name>Task 3: server/test_render.py coverage for the band primitive and the split top labels</name>
  <files>server/test_render.py</files>
  <read_first>
    - `server/test_render.py` lines 1-118 (module docstring, constants, `TEST_FLIGHT`/`TEST_ROUTE` fixtures) and lines 120-230 (the `_TextSpy`/`_RectangleSpy`/`_TextBBoxSpy` helper classes) — this task's new checks reuse `_TextSpy` exactly as check #65 (`_active_canvas_draws_selected_runways_tag`, ~line 2306) already does for glyph-by-glyph tracked-text reconstruction.
    - `server/test_render.py` lines 2137-2260 (checks #55/#58/#59/#60, the `THEME_IDS`-driven loops already quoted in this plan's context) — these loops will automatically start iterating this plan's 5 new band theme ids the moment plan 09-01 is merged. Confirm each still passes; do not weaken any of them to make a band theme pass artificially.
    - `server/test_render.py` line 43 (`EXPECTED_CHECK_COUNT = 107`) — the counter this task's new checks must push forward to the real total.
    - Plan 09-01's `<action>` (Task 1) for `device_config.THEME_IDS`'s exact 5 new band ids, and this plan's own Task 1/Task 2 `<action>` for `draw_diagonal_band()`'s and `draw_top_labels(band_theme=...)`'s exact contracts.
  </read_first>
  <action>
Add new checks (append after check #107, the file's current last check, before the `total = len(results)` line) proving what THIS plan adds — do not attempt to test plan 09-03's still-unbuilt three-tier text hierarchy here:

1. **Band geometry and palette legality, flat and dithered.** For each of the 5 band theme ids, call `render.build_canvas()` in both departing and arriving states and confirm no exception (the internal `_assert_legal_palette()` call already guards legality/dominance — this loop exists to force it to actually run for every band id, since the pre-existing checks #58/#59 already loop `THEME_IDS` generically but this makes band coverage explicit and independently readable).
2. **`draw_diagonal_band()` paints only `{IDX_WHITE, band_idx}` on a fresh White canvas**, for at least one flat (`IDX_BLUE`, `dithered=False`) and one dithered (`IDX_GREEN`, `dithered=True`) candidate — using `Image.getcolors()` the same way `_assert_legal_palette()` does internally, not a reimplementation of its logic.
3. **Band-theme top labels are genuinely split.** Using `_TextSpy` exactly as check #65 does: build a band-theme canvas (e.g. `theme_id="band_blue"`) and confirm the reconstructed top-row glyph run (single-character calls at `y == MARGIN`) equals `"DEPARTING FROM ORY"` for the departing state's default runway, with the right-hand run equal to `"RWY 3"` alone — both derived by calling `render.runway_tag_text()`/`render.STATE_LABEL_TEXT` in the check itself and partitioning on `" · "`, never a hardcoded literal duplicating the string in two places.
4. **Non-band-theme top labels are unaffected.** The same `_TextSpy` reconstruction, run against the existing default theme (`theme_id=device_config.DEFAULT_THEME_ID`, i.e. `"white"`), still reconstructs exactly `STATE_LABEL_TEXT["departing"]` on the left and the FULL `runway_tag_text()` string on the right — proving `band_theme` defaulting to `False` genuinely preserves today's behavior rather than accidentally applying the split everywhere.
5. **Non-band themes are pixel-identical to before this phase.** For one representative non-band theme already covered by an existing pinned expectation (`"white"`, the default), confirm `render.build_canvas(TEST_FLIGHT, "departing", route=TEST_ROUTE, theme_id="white")`'s pixel data is unchanged by comparing it against `render.build_canvas(TEST_FLIGHT, "departing", route=TEST_ROUTE)` (no explicit theme id) — the pre-existing check #55 already does exactly this comparison; this new check strengthens it into an explicit, named "band port did not touch the default path" regression guard by additionally asserting the White canvas's `getdata()` list length and `getcolors()` set are unchanged from a value the check computes fresh each run (never a hardcoded pixel dump).

Update `EXPECTED_CHECK_COUNT` (currently 107) to the real total once every new check above is written and the file actually runs.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 and prints `render: N/N checks pass` where N equals the file's own `EXPECTED_CHECK_COUNT`.
    - The 5 pre-existing `THEME_IDS`-driven loop checks (#55/#58/#59/#60, and the runway/theme matrix + badge-legality checks) all still pass now that `THEME_IDS` includes the 5 new band ids — none was edited to special-case or skip a band id.
    - The new split-label check demonstrably fails if `band_theme` defaults to `True` instead of `False` (spot-checked by temporarily flipping the default, observing the non-band-theme check fail, then reverting before commit).
    - `git diff --stat` touches only `server/test_render.py`.
  </acceptance_criteria>
  <done>server/test_render.py proves the band primitive's geometry/palette legality and the band-aware top-label split, and proves every non-band theme (including the default) is byte-identical to before this phase.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces (09-02)

**New symbols**
- `server.plane.render.draw_diagonal_band(canvas, band_idx, dithered=False)` — paints the trapezoid, no return value
- `server.plane.render.BAND_SHIFT_FRAC`, `BAND_TOP_LEFT_FRAC`, `BAND_TOP_RIGHT_FRAC`, `BAND_BOT_LEFT_FRAC`, `BAND_BOT_RIGHT_FRAC` — module-level geometry constants
- `server.plane.render._BAND_TOP_LABEL_DIRECTION` — `{state: "FROM"/"TO"}` dict

**Changed signatures**
- `server.plane.render.draw_top_labels(canvas, state, ink_idx, bg_idx, weight, runway_id=..., band_theme=False)` — new trailing kwarg, default preserves today's behavior exactly
- `server.plane.render._build_active_canvas()` — internal-only change (no signature change), now computes `is_band_theme`/`band_idx`/`band_dithered_flag` and calls `draw_diagonal_band()` for band themes

**No new files.**
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `theme_id` param (from device_config.json via the companion Config page, CFG-01) -> `_build_active_canvas()`'s band dispatch | An already-normalised theme id now also selects whether a band is drawn and which colour — the same trust boundary every existing theme field already crosses, just reading two more fields off the same validated registry entry. |

No new external input path, no new network surface, no package-manager installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Tampering | A band theme's `band_index` resolving to an illegal/out-of-range value and reaching `ImageDraw.polygon(fill=...)` un-validated | medium | mitigate | `band_index` is only ever read from `device_config.THEMES` via `theme_band_index()`, whose values were fixed and registry-shape-validated by plan 09-01's own test coverage (every band entry's `band_index` is asserted to be a member of the 6 legal `panel_format.IDX_*` values) — there is no live/external path that can set this value at runtime, only the registry itself. |
| T-09-02-02 | Denial of Service | `draw_diagonal_band()` raising for a malformed canvas or band index, breaking every band-theme render | high | mitigate | The function is exercised directly (Task 1's check) and transitively through every band theme's full `build_canvas()` call (Task 3's checks), across both active states — a raise anywhere in the band-theme render path fails the automated suite loudly before this plan can be considered done. |
| T-09-02-03 | Tampering | The band-theme top-label split accidentally applying to a non-band theme (regressing the 11 existing themes' top-row text) | high | mitigate | `band_theme` defaults to `False` and is only ever set `True` from `_build_active_canvas()`'s own `is_band_theme` computation, never a caller-supplied literal from outside this module. Task 3's non-band-theme check explicitly proves the default path reconstructs today's unsplit label/tag pair, and is demonstrated to actually catch a regression (default-flip spot-check) before commit. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install in this plan — pure Python edits to two already-vendored files. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_render.py` exits 0.
- All 16 registered themes render via `build_canvas()` in both states without exception.
- `git diff --stat` touches exactly `server/plane/render.py` and `server/test_render.py`.
</verification>

<success_criteria>
- The diagonal band primitive exists, is geometrically exact against the spike's measured values, and is wired into every band theme's render — and only those.
- The top-label split is genuinely conditional: band themes get the merged/split pair, every other theme is byte-identical to pre-phase output.
- The full automated suite passes with the 5 new band ids now live in `THEME_IDS`.
</success_criteria>

<output>
Create `.planning/phases/09-diagonal-band-theme/09-02-SUMMARY.md` when done.
</output>
