---
phase: 09-diagonal-band-theme
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - server/plane/render.py
  - server/test_render.py
autonomous: true
requirements: [PHASE9-4, PHASE9-5, PHASE9-6, PHASE9-7]

must_haves:
  truths:
    - "For a band theme, the main flight card shows a three-tier hierarchy (big identifier / dash rule / tracked route line / airline·type line) centred INSIDE the band, below the aircraft — built entirely from _flight_line1_text()/_flight_line2_text()'s real output, never re-derived content, with all three lines sharing one centre-x value computed once at the block's top y (not recomputed per line)."
    - "On the band_black theme specifically, the main card's text renders in white ink; every other band theme's main-card text renders in black ink — resolved per-call, never by changing a theme's registry ink_index."
    - "The previous (secondary) flight card shows the identical three-tier hierarchy, right-aligned, at its existing ~57% scale, in its unchanged position — never colliding with the band, and never ink-swapped."
    - "Every one of the 11 pre-band themes' main and previous card text is byte-identical to before this phase (band_idx=None preserves today's centred two-line block exactly)."
  artifacts:
    - path: "server/plane/render.py"
      provides: "_band_center_x(), band-only font/geometry constants, band-conditional draw_main_text_block()/draw_previous_text_block(), completed _build_active_canvas() wiring"
    - path: "server/test_render.py"
      provides: "Coverage for the tier-split content reuse, the centring-once regression guard, the black-band ink swap, previous-card clearance, and full-suite reconciliation"
  key_links:
    - from: "render._flight_line1_text()/_flight_line2_text()"
      to: "the band branches of draw_main_text_block()/draw_previous_text_block()"
      via: "called verbatim, exactly as the non-band path already calls them — never a separate content derivation"
      pattern: "_flight_line1_text\\(flight, state, route\\)"
    - from: "render._band_center_x(y, WIDTH)"
      to: "every line drawn in draw_main_text_block()'s band branch"
      via: "computed once before the first line is measured or drawn, reused for all three"
      pattern: "center_x = _band_center_x\\("
---

<objective>
Port the second half of spike `003-diagonal-band-theme`'s validated composition into `server/plane/render.py`: the three-tier flight-identifier hierarchy for both the main and previous cards, centred/right-aligned respectively, with the band's own contrast ink on the black candidate. Complete the `_build_active_canvas()` wiring plan 09-02 started.

Implements PHASE9-4 (main-card hierarchy + single computed centre-x), PHASE9-5 (band-aware ink), PHASE9-6 (previous-card hierarchy), and the remainder of PHASE9-7 (these new text roles render PT Serif Regular) from ROADMAP.md's Phase 9 Goal — see plan 09-01's objective for the full PHASE9-1..8 table.

Depends on plan 09-02 because both edit `_build_active_canvas()` and `server/test_render.py` — same-file conflict forces this wave after 09-02's, not just a data dependency.

Purpose: complete the band theme's visual composition exactly as the developer confirmed it at spike round 15 ("oui !"), preserving every non-band theme byte-for-byte.
Output: `_band_center_x()`, the band-only font constants, band-conditional `draw_main_text_block()`/`draw_previous_text_block()`, full `_build_active_canvas()` wiring, and `server/test_render.py` coverage plus full-suite reconciliation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagonal-band-theme/09-PATTERNS.md
@.planning/phases/09-diagonal-band-theme/09-01-PLAN.md
@.planning/phases/09-diagonal-band-theme/09-02-PLAN.md
@.planning/spikes/003-diagonal-band-theme/README.md
@.planning/spikes/003-diagonal-band-theme/explore_full_composition.py
</context>

<interfaces>
Current `draw_main_text_block()` (render.py lines 1224-1280) and `draw_previous_text_block()` (lines 1283-1350) both already compute, as their FIRST two lines:
```
line1_text = _flight_line1_text(flight, state, route)
line2_text = _flight_line2_text(route, flight.get("aircraft_type"))
```
and both anchor from `main_placement.content[3] + MAIN_TEXT_GAP_PX` / `prev_placement.content[3] + PREVIOUS_TEXT_GAP_PX` respectively (unchanged constants this plan reuses, never redefines).

`_role_font(role_spec, weight)` (render.py lines 491-498) resolves a role tuple's WEIGHT from the `weight` argument (via `_role_weight_path()`), discarding the tuple's own declared path — this is the existing, documented contract every active-state role already uses (`_role_font()`'s own docstring: "resolving the role's weight via `_role_weight_path()` rather than reading the tuple's own... path directly"). All 5 band themes registered in plan 09-01 have `weight: "regular"` — so `_role_font(<any band role tuple>, weight)` resolves to `PTSerif-Regular.ttf` for every band theme today, satisfying PHASE9-7 ("PT Serif stays Regular... for every new text role this phase adds") through the SAME mechanism the six pre-existing roles use, not a special case. This matches what the spike's own `patched_draw_main_text_block()`/`patched_draw_previous_text_block()` actually called (`render._role_font(FLIGHT_NUMBER_FONT, weight)`, etc.) and is what the developer's round-15 "oui !" approved — port this call shape verbatim, do not hardcode a bare `_font()` call that bypasses weight resolution.

`_tracked_text_width()`, `_tracked_text_bbox()`, `draw_tracked_text()`, `LABEL_TRACKING_PX` (already used by the top labels) are reused unchanged for the tracked route line in both cards.

Plan 09-02 already added `BAND_TOP_LEFT_FRAC`/`BAND_TOP_RIGHT_FRAC`/`BAND_BOT_LEFT_FRAC`/`BAND_BOT_RIGHT_FRAC` and `draw_diagonal_band()`, and threads `is_band_theme`/`band_idx`/`band_dithered_flag` through `_build_active_canvas()` already.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: _band_center_x() and the band-conditional main-card three-tier hierarchy</name>
  <files>server/plane/render.py</files>
  <read_first>
    - `.planning/spikes/003-diagonal-band-theme/explore_full_composition.py` lines 152-157 (font/dash constants), 200-211 (`_band_center_x()`), 214-220 (`_CURRENT_BAND_IDX`/ink-swap comment), and 223-309 (`patched_draw_main_text_block()` in full) — the exact logic this task ports.
    - `.planning/spikes/003-diagonal-band-theme/README.md`, Round 12/13/15 in the Investigation Trail — the black-band-total-failure finding (round 12), its white-ink fix (round 13), and the centring-once bug + fix (round 15). This task must preserve the round-15 fix; do not reintroduce the round-12 per-line recompute.
    - `server/plane/render.py` lines 1224-1280 (`draw_main_text_block()`, current form) and lines 491-507 (`_role_font()`/`_role_fit_text_size()`).
  </read_first>
  <action>
Add `_band_center_x(canvas_y, w)` next to plan 09-02's band geometry constants (immediately after `draw_diagonal_band()`'s definition): `f = canvas_y / HEIGHT`; `left_frac = BAND_TOP_LEFT_FRAC - (BAND_TOP_LEFT_FRAC - BAND_BOT_LEFT_FRAC) * f`; `right_frac = BAND_TOP_RIGHT_FRAC - (BAND_TOP_RIGHT_FRAC - BAND_BOT_RIGHT_FRAC) * f`; return `(left_frac + right_frac) / 2 * w`. Verbatim port of the spike's own `_band_center_x()` — the trapezoid's own linear interpolation, evaluated at a text y instead of the polygon's corners.

Add these constants directly above `draw_main_text_block()`'s definition, labelled as band-only text roles: `BAND_MAIN_NUMBER_FONT = (PT_SERIF_BOLD, 56, 700)`, `BAND_MAIN_ROUTE_FONT = (PT_SERIF_REGULAR, 22, 400)`, `BAND_MAIN_AIRLINE_FONT = (PT_SERIF_REGULAR, 20, 400)`, `BAND_MAIN_DASH_W = 24`, `BAND_MAIN_DASH_GAP = 10`. (Per this plan's `<interfaces>` note: these tuples' own declared paths are never read directly — `_role_font()` resolves the actual weight from the theme's `weight` field, same as every other active-state role.)

Extend `draw_main_text_block()`'s signature with one new trailing keyword, default `None`: `..., weight, band_idx=None):`. Wrap the function's ENTIRE current body in `if band_idx is None:` (today's unchanged centred two-line block — copy the existing body under this branch verbatim, no edits) `else:` (the new band branch, described below). This guarantees every non-band theme (`band_idx` always `None` for them, per plan 09-02's `_build_active_canvas()` wiring) is byte-for-byte unaffected.

**Band branch** (mirrors `patched_draw_main_text_block()`, lines 223-309 of the spike, verbatim in logic):
1. `effective_ink = IDX_WHITE if band_idx == IDX_BLACK else ink_idx` (round-13 fix — the black band's own contrast colour, not the theme's global ink).
2. `line1_full = _flight_line1_text(flight, state, route)`; `line2_full = _flight_line2_text(route, flight.get("aircraft_type"))` — the SAME two calls the non-band branch makes, never re-derived.
3. Extract `identifier`: `route.get("callsign_iata")` guarded for a non-dict `route`, `.strip()`'d, blank-after-strip treated as `None`.
4. Classify, in this order: (a) `line1_full == ""` → `number_text=None, tracked_text=None, plain_text=line2_full`; (b) `identifier` truthy and `line1_full.startswith(identifier + " ")` → `number_text=identifier, tracked_text=line1_full[len(identifier)+1:].upper(), plain_text=line2_full`; (c) otherwise → `number_text=None, tracked_text=line1_full.upper(), plain_text=line2_full`. This is `patched_draw_main_text_block()`'s exact 3-way split (lines 243-263) — port verbatim, do not invent a different rule.
5. `y = main_placement.content[3] + MAIN_TEXT_GAP_PX` (reuse the existing constant, same anchor the non-band branch uses). Compute `center_x = _band_center_x(y, WIDTH)` ONCE here, before any line is measured or drawn — the round-15 fix. Do not recompute `center_x` inside any of the following steps.
6. If `number_text`: font = `_role_font(BAND_MAIN_NUMBER_FONT, weight)`; measure+assert+draw anchor=`"ma"` at `(center_x, y)` in `effective_ink`; this is `first_bbox`. Draw a 2px-wide horizontal dash centred at `center_x`, `BAND_MAIN_DASH_W` px long, `BAND_MAIN_DASH_GAP` px below the number bbox's bottom, in `effective_ink`. Advance `y` to `dash_y + BAND_MAIN_DASH_GAP + 4`.
7. If `tracked_text`: font = `_role_font(BAND_MAIN_ROUTE_FONT, weight)`; `tracked_w = _tracked_text_width(font, tracked_text, LABEL_TRACKING_PX)`; left edge = `center_x - tracked_w / 2`; measure via `_tracked_text_bbox()`, assert, draw via `draw_tracked_text(..., tracking=LABEL_TRACKING_PX)` in `effective_ink`; if `first_bbox` still unset, use this bbox. Advance `y` to this bbox's bottom + 12.
8. Always draw `plain_text` last: font = `_role_font(BAND_MAIN_AIRLINE_FONT, weight)`; measure+assert+draw anchor=`"ma"` at `(center_x, y)` in `effective_ink`; if `first_bbox` still unset, use this bbox.
9. Add an `_assert_within_canvas()` call on every bbox measured in steps 6-8 BEFORE its corresponding draw call — the spike version omits this; every other drawing function in this file asserts before it paints, and this branch must match that discipline.
10. Return `(first_bbox, plain_bbox)` where `plain_bbox` is step 8's bbox — same 2-tuple shape the non-band branch returns, so existing callers (which discard the return value) see no shape change.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
from server.plane import render
from server import panel_format as pf
flight = {'hex':'39de41','callsign':'AFR1234','aircraft_type':'A320'}
route = {'airline_name':'Air France','callsign_iata':'AF1234','origin_iata':'ORY','origin_city':'Paris','destination_iata':'JFK','destination_city':'New York'}
for theme_id in ('band_blue','band_blue_light','band_green_light','band_red','band_black'):
    render.build_canvas(flight, 'departing', route=route, theme_id=theme_id)
    render.build_canvas(flight, 'arriving', route=route, theme_id=theme_id)
print('OK all 5 band themes render main-card hierarchy without exception')
"</automated>
  </verify>
  <acceptance_criteria>
    - `draw_main_text_block(..., band_idx=None)` (every non-band theme's call, per plan 09-02's wiring) produces pixel-identical output to this function's pre-this-plan body — proven by Task 3's regression check.
    - `draw_main_text_block(..., band_idx=IDX_BLACK)` draws every line in `IDX_WHITE`; every other `band_idx` value draws in the caller's `ink_idx` unchanged.
    - For a tier-1 route (identifier + city both known), the drawn number line's text equals `route["callsign_iata"]` exactly, and the tracked route line's reconstructed text equals `_flight_line1_text(flight, state, route)[len(identifier)+1:].upper()`.
    - For a tier-3 route (`_flight_line1_text()` returns `""`), no number line and no dash are drawn — only the promoted airline·type line.
    - `center_x` is identical across all three lines in a tier-1 render (spot-checked by capturing each `anchor="ma"` draw call's x-coordinate and confirming they match) — Task 3 makes this a permanent regression guard against the round-12 per-line-recompute bug.
    - All 5 band themes render both states without exception.
  </acceptance_criteria>
  <done>Band themes' main card shows the three-tier hierarchy centred inside the band with one shared centre-x and band-aware ink; every non-band theme's main card is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Band-conditional previous-card hierarchy and completed _build_active_canvas() wiring</name>
  <files>server/plane/render.py</files>
  <read_first>
    - `.planning/spikes/003-diagonal-band-theme/explore_full_composition.py` lines 312-377 (`PREV_NUMBER_FONT`/`PREV_ROUTE_FONT`/`PREV_AIRLINE_FONT`/`PREV_DASH_W`/`PREV_DASH_GAP` and `patched_draw_previous_text_block()` in full) — the exact logic this task ports, right-aligned mirror of Task 1's main-card branch.
    - `.planning/spikes/003-diagonal-band-theme/README.md`, Round 6 — confirms the previous card never needed to move: the band's rightmost extent at this card's height (~45% width) stays clear of this card's text (~89% width), so there is no ink-swap and no position change for this card, ever.
    - `server/plane/render.py` lines 1283-1350 (`draw_previous_text_block()`, current form) and the `_build_active_canvas()` excerpt quoted in plan 09-02's `<interfaces>` section (the two call sites this task finishes wiring).
  </read_first>
  <action>
Add these constants directly above `draw_previous_text_block()`'s definition: `BAND_PREV_NUMBER_FONT = (PT_SERIF_BOLD, 32, 700)`, `BAND_PREV_ROUTE_FONT = (PT_SERIF_REGULAR, 16, 400)`, `BAND_PREV_AIRLINE_FONT = (PT_SERIF_REGULAR, 14, 400)`, `BAND_PREV_DASH_W = 16`, `BAND_PREV_DASH_GAP = 6`.

Extend `draw_previous_text_block()`'s signature with one new trailing keyword, default `None`: `..., weight, band_idx=None):`. Wrap the function's ENTIRE current body in `if band_idx is None:` (today's unchanged right-aligned two-line block, copied verbatim) `else:` (the new band branch below) — same structural pattern as Task 1.

**Band branch** (mirrors `patched_draw_previous_text_block()`, lines 329-377 of the spike):
1. No ink override here — always use `ink_idx` as given, never swap to white even when `band_idx == IDX_BLACK`. This is deliberate: round 6 confirmed the band never reaches this card's position at any candidate geometry, so there is nothing for this card's text to contrast against.
2. `right_x = prev_placement.content[2] - PREVIOUS_TEXT_LEFT_OFFSET_PX` and `y = prev_placement.content[3] + PREVIOUS_TEXT_GAP_PX` — the SAME two existing constants the non-band branch already uses; do not introduce new ones.
3. Same content-classification logic as Task 1's main-card branch (steps 2-4 there), applied to `flight`/`route`/`state` as this function already receives them.
4. If `number_text`: font = `_role_font(BAND_PREV_NUMBER_FONT, weight)`; measure+assert+draw anchor=`"ra"` at `(right_x, y)` in `ink_idx`; draw a 2px dash `BAND_PREV_DASH_W` px long ending at `right_x`, `BAND_PREV_DASH_GAP` px below the number bbox's bottom; advance `y` to `dash_y + BAND_PREV_DASH_GAP + 3`.
5. If `tracked_text`: font = `_role_font(BAND_PREV_ROUTE_FONT, weight)`; `tracked_w = _tracked_text_width(...)`; left edge = `right_x - tracked_w`; draw via `draw_tracked_text(..., tracking=LABEL_TRACKING_PX)`; advance `y` to this bbox's bottom + 8.
6. Always draw `plain_text` last: font = `_role_font(BAND_PREV_AIRLINE_FONT, weight)`; anchor=`"ra"` at `(right_x, y)`.
7. `_assert_within_canvas()` on every bbox before its draw call, same discipline as Task 1.
8. Return `(first_bbox, plain_bbox)`, same 2-tuple shape as the non-band branch.

Finish `_build_active_canvas()`'s wiring (plan 09-02 already computes `band_idx`; this task threads it into the two text-block call sites): change `draw_main_text_block(canvas, flight, state, route, main_placement, fg_idx, bg_idx, weight)` to `draw_main_text_block(canvas, flight, state, route, main_placement, fg_idx, bg_idx, weight, band_idx=band_idx)`, and the equivalent for `draw_previous_text_block(...)`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "
from server.plane import render
flight = {'hex':'39de41','callsign':'AFR1234','aircraft_type':'A320'}
route = {'airline_name':'Air France','callsign_iata':'AF1234','origin_iata':'ORY','origin_city':'Paris','destination_iata':'JFK','destination_city':'New York'}
prev_flight = {'hex':'4a1b02','callsign':'VLG6PD','aircraft_type':'A320'}
prev_route = {'airline_name':'Vueling Airlines','callsign_iata':'VY1234','origin_iata':'ORY','origin_city':'Paris','destination_iata':'BCN','destination_city':'Barcelona'}
for theme_id in ('band_blue','band_blue_light','band_green_light','band_red','band_black'):
    render.build_canvas(flight, 'departing', route=route, previous_flight=prev_flight, previous_route=prev_route, previous_state='arriving', theme_id=theme_id)
print('OK all 5 band themes render the full two-flight composition without exception')
"</automated>
  </verify>
  <acceptance_criteria>
    - `draw_previous_text_block(..., band_idx=None)` (every non-band theme's call) produces pixel-identical output to this function's pre-this-plan body.
    - `draw_previous_text_block(..., band_idx=IDX_BLACK)` still draws in the caller's plain `ink_idx` — never white — proving no ink override leaked into this card.
    - The previous card's drawn text bboxes never overlap the band's own painted region at any of the 5 band colours, in a full two-flight render — Task 3 makes this an explicit geometric regression guard, not just an absence-of-exception check.
    - `_build_active_canvas()` passes `band_idx=band_idx` to both `draw_main_text_block()` and `draw_previous_text_block()`; for every non-band theme this value is `None`.
    - All 5 band themes render the full two-flight composition, both states, without exception.
  </acceptance_criteria>
  <done>Band themes' previous card shows the three-tier hierarchy right-aligned at its existing scale, in its unchanged position, with no ink override; every non-band theme's previous card is unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: server/test_render.py coverage for the full band composition, plus full-suite reconciliation</name>
  <files>server/test_render.py</files>
  <read_first>
    - `server/test_render.py` lines 120-230 (`_TextSpy`/`_RectangleSpy`/`_TextBBoxSpy`) and the check-writing pattern used throughout the file (`check(name, fn)` where `fn` returns `(bool, reason)`).
    - This plan's own Task 1/Task 2 `<action>` blocks for the exact band-branch behavior to assert against.
    - `server/test_poll_loop.py`'s pinned `panel.bin` digest check (grep for `_DEFAULT_CONFIG_DIGEST` or similar) — this phase's changes are additive-only (band themes never touch the default theme's render path), so this digest must pass UNCHANGED. This is different from Phase 8's precedent, which genuinely changed the default render output and required a re-pin each time — do not assume a re-pin is needed here; treat any digest mismatch as a real regression to root-cause, not a routine re-pin.
    - `companion/test_config_page.py` lines 240-270 (`theme_fieldset()`'s radio-count check, already driven by `len(device_config.THEMES)` dynamically) — confirm this passes unmodified once `THEMES` holds 16 entries; per `.planning/phases/09-diagonal-band-theme/09-PATTERNS.md`, zero edit is expected here.
  </read_first>
  <action>
Add new checks (append after this file's current last check, before `total = len(results)`):

1. **Non-band regression, both text blocks.** For each of the 11 pre-band theme ids, render a full two-flight canvas and confirm it is pixel-identical to the same render computed via a reference call that does not pass `band_idx` at all (i.e. confirm `draw_main_text_block()`/`draw_previous_text_block()`'s `band_idx=None` branch is genuinely a no-op wrapper around the pre-this-plan body) — reuse the existing `TEST_FLIGHT`/`TEST_ROUTE`/`TEST_PREVIOUS_ROUTE` fixtures already in this file.
2. **Tier-split content reuse, main card.** For a tier-1 route, capture text draws via `_TextSpy` on a band theme render and confirm: the big-number draw's text equals `route["callsign_iata"]` exactly; the tracked route line, reconstructed from consecutive single-character glyph draws (same reconstruction idiom check #65 already uses for the top row), equals `render._flight_line1_text(flight, state, route)[len(identifier)+1:].upper()` computed fresh in the check itself. Repeat for a tier-3 route (`enrich.airline_only_route(...)`) and confirm no number/dash/tracked-route draw occurs, only the promoted airline·type line.
3. **Centring-once regression guard (round-15 bug).** For a tier-1 band render, capture every `anchor="ma"` draw call's x-coordinate via `_TextSpy` (the number line and the airline·type line both use this anchor) and confirm they are numerically identical — proving `center_x` was computed once and reused, not recomputed per line. Demonstrate this check actually catches the round-12 regression by temporarily recomputing `center_x` inside the `plain_text` branch only, observing this check fail, then reverting before commit.
4. **Black-band ink swap.** Render `theme_id="band_black"` and, using `canvas.load()` pixel sampling inside the main card's drawn text bboxes (same idiom check #74 already uses), confirm `IDX_WHITE` pixels are present and `IDX_BLACK` text-ink pixels are not, within those bboxes. Render one other band theme (e.g. `"band_red"`) the same way and confirm the opposite: text pixels are `IDX_BLACK`, not `IDX_WHITE`.
5. **Previous-card band clearance.** For a full two-flight `"band_blue_light"` render (the widest dithered candidate), compute the band's rightmost x-fraction at the previous card's text y-range using the same linear interpolation `_band_center_x()` uses internally (inlined in the check, not calling a private helper by name if that feels fragile — either is acceptable as long as it is derived from `BAND_TOP_RIGHT_FRAC`/`BAND_BOT_RIGHT_FRAC`, never a hardcoded pixel literal) and confirm the previous card's drawn text bboxes' left edges sit to the right of that x-value for all 5 band themes.
6. **Full legal-palette + dominance sweep.** For all 5 band themes, both states, with and without `source_fault=True`, confirm `build_canvas()` raises no `AssertionError` (i.e. `_assert_legal_palette()` holds with the full three-tier text and the source-fault badge both present).

Update `EXPECTED_CHECK_COUNT` to the real total once every new check is written and the file runs.

Then run the full aggregate suite (`scripts/run-all-tests.sh`) and reconcile: confirm `server/test_poll_loop.py`'s pinned digest passes unchanged (see this task's `<read_first>` note — do not re-pin unless a real, root-caused default-path regression is found); confirm `companion/test_config_page.py` passes unmodified (the CFG-01 picker absorbs the 5 new themes automatically via `len(device_config.THEMES)`); confirm no other harness in the registered array needed a change. If any harness other than `server/test_render.py`/`server/test_config_history.py` needed an edit, that is a Rule 1 deviation to record in the plan summary, not a silent fix.
  </action>
  <verify>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0, `N/N` where N matches `EXPECTED_CHECK_COUNT`.
    - `scripts/run-all-tests.sh` exits 0 across every registered harness, with `server/test_poll_loop.py`'s digest passing unchanged (or, if it genuinely changed, a root-caused explanation recorded in the summary — never a silent re-pin).
    - The centring-once check (#3 above) is demonstrated to actually fail against a deliberately reintroduced round-12-style per-line recompute, then passes again once reverted.
    - The black-band ink-swap check (#4) distinguishes `band_black` from at least one non-black band theme by actual sampled pixel colour, not by inference.
    - `git diff --stat` touches only `server/test_render.py` (plus any genuinely-required, explicitly-recorded fix elsewhere).
  </acceptance_criteria>
  <done>server/test_render.py proves the full band composition (content reuse, centring-once, ink swap, previous-card clearance, palette legality) and the full suite passes with the default theme's output provably untouched.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces (09-03)

**New symbols**
- `server.plane.render._band_center_x(canvas_y, w)`
- `server.plane.render.BAND_MAIN_NUMBER_FONT`, `BAND_MAIN_ROUTE_FONT`, `BAND_MAIN_AIRLINE_FONT`, `BAND_MAIN_DASH_W`, `BAND_MAIN_DASH_GAP`
- `server.plane.render.BAND_PREV_NUMBER_FONT`, `BAND_PREV_ROUTE_FONT`, `BAND_PREV_AIRLINE_FONT`, `BAND_PREV_DASH_W`, `BAND_PREV_DASH_GAP`

**Changed signatures**
- `server.plane.render.draw_main_text_block(..., weight, band_idx=None)` — new trailing kwarg, default preserves today's behavior exactly
- `server.plane.render.draw_previous_text_block(..., weight, band_idx=None)` — same
- `server.plane.render._build_active_canvas()` — internal-only, now threads `band_idx=` into both text-block calls

**No new files.**
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `route`/`flight` dicts (ADS-B + adsbdb-derived, already untrusted input at this point in the pipeline) -> the band branches' `route.get("callsign_iata")` extraction | The same untrusted-data boundary `_flight_line1_text()` already crosses; this plan's tier-split logic reads the same field a second time for a different purpose (splitting the already-sanitised string), not a new read of raw external data. |

No new external input path, no new network surface, no package-manager installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Tampering | A hostile/malformed `route` dict (e.g. `callsign_iata` present but not a real prefix of `_flight_line1_text()`'s output) causing the band branch's tier classification to mis-split or raise | medium | mitigate | The classification mirrors `_flight_line1_text()`'s own guarded `.get()`/`.strip()` pattern and falls back to case (c) — "no identifier to split out, treat the whole line as the tracked run" — for any shape that does not exactly match tier 1's known format, never raising. Task 3's tier-1/tier-3 checks and the existing `_flight_line1_text()` test coverage together bound this. |
| T-09-03-02 | Denial of Service | The black-band ink override (`band_idx == IDX_BLACK`) silently failing to apply, regressing the confirmed round-12 total-text-invisibility bug | high | mitigate | Task 3's pixel-sampling ink-swap check directly asserts white pixels are present in the `band_black` render's text region and asserts the opposite for a non-black band theme, so a regression is caught by colour, not by absence of an exception (which would not have caught the original bug either — the round-12 render did not raise, it just drew invisible text). |
| T-09-03-03 | Tampering | The round-12 per-line-recompute centring bug reappearing silently (a cosmetic but developer-flagged-as-real defect) | medium | mitigate | Task 3's centring-once check is demonstrated, not assumed, to catch the exact regression pattern before commit (temporarily reintroduce it, observe failure, revert). |
| T-09-03-04 | Tampering | A band-branch text draw overlapping the previous card's own text at some band colour/geometry combination not exercised by the spike's own checks | low | accept | The spike's round 6 finding (band's rightmost extent ~45% width vs. this card's ~89% width text, at this card's height range) is geometry-derived and colour-independent — the band's shape does not vary by colour, only its fill does. Task 3's clearance check is a permanent regression guard against a future geometry change reopening this, at low residual risk given the geometry is fixed by this plan's own constants. |
| T-09-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install in this plan. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_render.py` exits 0.
- `scripts/run-all-tests.sh` exits 0, with `server/test_poll_loop.py`'s digest unchanged.
- `git diff --stat` touches exactly `server/plane/render.py` and `server/test_render.py` (plus any explicitly-recorded exception).
</verification>

<success_criteria>
- All 5 band themes render the full validated composition: three-tier hierarchy on both cards, single computed centre-x, band-aware ink, no previous-card collision.
- Every one of the 11 pre-band themes is byte-identical to before this phase, on both the main and previous cards.
- The full automated suite passes, with the default theme's render output provably untouched.
</success_criteria>

<output>
Create `.planning/phases/09-diagonal-band-theme/09-03-SUMMARY.md` when done.
</output>
