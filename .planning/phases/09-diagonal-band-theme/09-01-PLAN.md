---
phase: 09-diagonal-band-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/device_config.py
  - server/test_config_history.py
autonomous: true
requirements: [PHASE9-2, PHASE9-7]

must_haves:
  truths:
    - "device_config.THEMES contains exactly 5 new band theme entries (band_blue, band_blue_light, band_green_light, band_red, band_black), each selectable through the same registry mechanism the 11 existing themes already use, with zero change to any existing entry's values."
    - "Each band theme's base-canvas fields (departing_index/arriving_index/ink_index/dithered/weight) are identical to the existing white entry's own values (White base field, Black ink, undithered, Regular weight) — only label, band_index, and band_dithered vary between the 5 new entries, matching what the validated spike actually built (band colour was never a base-canvas property)."
    - "A caller can determine whether a theme id is a band theme, and if so its band colour index and whether that band is dithered, purely through new device_config accessor functions — never by inspecting THEMES directly."
  artifacts:
    - path: "server/device_config.py"
      provides: "THEMES band_blue/band_blue_light/band_green_light/band_red/band_black entries; theme_is_band()/theme_band_index()/theme_band_dithered() accessors"
    - path: "server/test_config_history.py"
      provides: "The 4 pre-existing exact-membership THEMES checks extended to the 5 new ids without weakening their coverage of the 11 existing ids, plus new coverage for the 3 new accessors"
  key_links:
    - from: "server/device_config.py THEMES['band_*']['band_index'] / ['band_dithered']"
      to: "server/plane/render.py's band dispatch in _build_active_canvas() (built in plans 09-02/09-03)"
      via: "device_config.theme_is_band()/theme_band_index()/theme_band_dithered() accessor calls"
      pattern: "theme_is_band\\(|theme_band_index\\(|theme_band_dithered\\("
---

<objective>
Add the registry half of Phase 9's new diagonal-band theme family to `server/device_config.py`: 5 new `THEMES` entries (one per validated band colour/treatment) and 3 new accessor functions the render pipeline will read in plans 09-02/09-03. This plan touches only the registry and its own test file — no drawing code changes yet.

**Traceability note (no CONTEXT.md this phase):** ROADMAP.md's Phase 9 goal has no `D-XX` decisions to cite (the developer skipped `/gsd-discuss-phase` and `/gsd-research-phase` for this phase, going straight from the validated spike to planning). This plan set instead cites the 8 numbered clauses of ROADMAP.md's own Phase 9 Goal paragraph as `PHASE9-1` through `PHASE9-8`:
PHASE9-1 diagonal trapezoid band primitive · PHASE9-2 the 5 band colour candidates · PHASE9-3 top-label split · PHASE9-4 main-card 3-tier hierarchy + single computed centre-x · PHASE9-5 band-aware ink colour · PHASE9-6 previous-card 3-tier hierarchy · PHASE9-7 PT Serif stays Regular, no italic · PHASE9-8 required blocking on-glass verification.
This plan implements PHASE9-2 (the 5 registered colour candidates) and half of PHASE9-7 (baking `weight: "regular"` into every band entry, matching the validated White-base render).

Purpose: give plans 09-02/09-03 a stable, already-tested data contract (`theme_is_band()` / `theme_band_index()` / `theme_band_dithered()`) to read from, so the render-side port never has to reach into `THEMES` directly.
Output: `server/device_config.py` with 16 total THEMES entries (11 unchanged + 5 new) and 3 new accessors; `server/test_config_history.py` with regression + new coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagonal-band-theme/09-PATTERNS.md
@.planning/spikes/003-diagonal-band-theme/README.md
</context>

<interfaces>
<!-- server/device_config.py, current THEMES shape (lines 104-193) and accessor
     pattern (lines 374-389) - the executor reads these directly, but the exact
     shape/contract is repeated here so no exploration is needed. -->

Existing entry shape (every one of the 11 current THEMES values):
```
"blue_light": {
    "departing_index": IDX_BLUE,
    "arriving_index": IDX_BLUE,
    "ink_index": IDX_WHITE,
    "label": "Blue Light",
    "dithered": True,
    "weight": "bold",
},
```

Existing accessor pattern (mirror exactly for the 3 new ones):
```
def theme_dithered(theme_id):
    return THEMES[theme_id]["dithered"]

def theme_weight(theme_id):
    return THEMES[theme_id]["weight"]
```

`IDX_BLACK, IDX_BLUE, IDX_GREEN, IDX_RED, IDX_WHITE, IDX_YELLOW` are already imported at the top of `server/device_config.py` from `server.panel_format` — no new import needed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 band THEMES entries and 3 accessor functions</name>
  <files>server/device_config.py</files>
  <read_first>
    - `server/device_config.py` lines 104-193 (the `THEMES` dict, every existing entry) and lines 342-402 (the presentation-accessor section, especially `theme_dithered()`/`theme_weight()` at lines 374-389, whose exact shape the 3 new accessors mirror).
    - `.planning/phases/09-diagonal-band-theme/09-PATTERNS.md`, the "`server/device_config.py` — `THEMES` dict" section, for the two candidate shapes considered and why shape 2 (5 separate entries) was chosen.
    - `.planning/spikes/003-diagonal-band-theme/README.md`, the "Developer confirmed round 15" Results section, for the exact 5 colours to register: blue light (dithered), blue (flat), green light (dithered), red (flat), black (flat).
  </read_first>
  <action>
Add 5 new entries to `THEMES`, inserted after the existing `"blue_light"` entry (end of the dict, before the closing brace), each following the existing entry shape (`departing_index`, `arriving_index`, `ink_index`, `label`, `dithered`, `weight`) plus 2 NEW fields this task introduces (`band_index`, `band_dithered`):

- `band_blue`: departing_index=IDX_WHITE, arriving_index=IDX_WHITE, ink_index=IDX_BLACK, label="Band Blue", dithered=False, weight="regular", band_index=IDX_BLUE, band_dithered=False
- `band_blue_light`: same base fields as band_blue, label="Band Blue Light", band_index=IDX_BLUE, band_dithered=True
- `band_green_light`: same base fields as band_blue, label="Band Green Light", band_index=IDX_GREEN, band_dithered=True
- `band_red`: same base fields as band_blue, label="Band Red", band_index=IDX_RED, band_dithered=False
- `band_black`: same base fields as band_blue, label="Band Black", band_index=IDX_BLACK, band_dithered=False

Every one of the 5 entries keeps `departing_index`/`arriving_index`/`ink_index`/`dithered`/`weight` IDENTICAL to the existing `white` entry's own values (White base field, Black ink, undithered field, Regular weight) — this matches exactly what the validated spike built: every band candidate rendered against `build_canvas(theme_id="white")`, with the band's own colour passed as a separate function parameter, never a base-canvas property. Only `label`, `band_index`, and `band_dithered` differ across the 5 new entries. Reference only the already-imported `panel_format.IDX_*` named constants — never a bare integer, matching this module's own stated discipline in its header comment (lines 20-24, 69-71).

Then add 3 new presentation accessors immediately after `theme_weight()` (before `runway_tag_text()`), mirroring `theme_dithered()`/`theme_weight()`'s exact one-line-body, never-raises-for-a-valid-id contract:

- `theme_is_band(theme_id)` — returns whether `"band_index"` is a key of `THEMES[theme_id]`. True for exactly the 5 new entries; False for all 11 existing entries, which never carry that key.
- `theme_band_index(theme_id)` — returns `THEMES[theme_id].get("band_index")`. Returns the entry's `IDX_*` band colour for a band theme, `None` for a non-band theme.
- `theme_band_dithered(theme_id)` — returns `THEMES[theme_id].get("band_dithered", False)`. Returns the entry's bool for a band theme, `False` for a non-band theme.

Give each a one-line docstring naming its contract (band-only field, absent-key-safe via `.get()`, mirrors `theme_dithered()`/`theme_weight()`'s placement and style).
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -c "from server import device_config as dc; assert len(dc.THEMES) == 16; assert dc.THEME_IDS == tuple(dc.THEMES); band_ids = [t for t in dc.THEMES if dc.theme_is_band(t)]; assert set(band_ids) == {'band_blue','band_blue_light','band_green_light','band_red','band_black'}, band_ids; assert all(dc.theme_band_index(t) is None for t in dc.THEMES if t not in band_ids); assert all(isinstance(dc.theme_band_dithered(t), bool) for t in dc.THEMES); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `device_config.THEMES` has exactly 16 keys: the 11 pre-existing ids, byte-identical to their pre-phase values, plus the 5 new band ids.
    - Each of the 5 new entries' `departing_index`/`arriving_index`/`ink_index`/`dithered`/`weight` match the `white` entry's own values exactly; only `label`, `band_index`, `band_dithered` vary between the 5.
    - `theme_is_band()` returns `True` for exactly the 5 new ids and `False` for all 11 existing ids.
    - `theme_band_index()` returns the correct `IDX_*` constant for each band id (`IDX_BLUE` for band_blue/band_blue_light, `IDX_GREEN` for band_green_light, `IDX_RED` for band_red, `IDX_BLACK` for band_black) and `None` for every non-band id.
    - `theme_band_dithered()` returns `True` for band_blue_light/band_green_light, `False` for band_blue/band_red/band_black, and `False` for every non-band id.
    - No existing `THEMES` entry, accessor, or module-level constant was modified — `git diff` shows insertions only in the `THEMES` dict and a new accessor block, nothing else.
  </acceptance_criteria>
  <done>THEMES holds 16 entries (11 unchanged + 5 new band entries with identical White-base fields and their own label/band_index/band_dithered), and theme_is_band()/theme_band_index()/theme_band_dithered() correctly resolve every id in the registry.</done>
</task>

<task type="auto">
  <name>Task 2: Extend server/test_config_history.py's THEMES contract checks and cover the 3 new accessors</name>
  <files>server/test_config_history.py</files>
  <read_first>
    - `server/test_config_history.py` lines 260-413 in full: `_theme_registry_shape_is_correct` (~272), `_every_theme_is_single_colour` (~301), `_ink_contrast_pairing_is_correct` (~320), `_dithered_and_weight_contract_is_correct` (~354), `_default_theme_and_labels_are_correct` (~390) — the exact functions this task edits, plus `EXPECTED_CHECK_COUNT` at line 26.
    - Task 1's own `<action>` in this plan, for the 5 new ids' exact field values this task must encode as expectations.
  </read_first>
  <action>
Four of the five functions listed above assert an EXACT id-set match against `device_config.THEMES` (`set(expected) != set(device_config.THEMES)` triggers a hard failure) and will break the moment Task 1 lands, unless updated. Update all four so they account for the 5 new band ids without weakening what they already check for the 11 existing ids — do not change any of the 11 existing ids' expected values in any of these functions, this task is additive only:

- `_theme_registry_shape_is_correct`: band-theme entries legitimately carry 8 keys (the existing 6 plus `band_index`/`band_dithered`), not 6. Change the per-entry key check so a non-band entry's key set (per `device_config.theme_is_band(theme_id)`) must equal exactly the existing `expected_keys` (6 keys), and a band entry's key set must equal exactly `expected_keys | {"band_index", "band_dithered"}`. Add a per-band-entry check that `band_index` is a member of this function's own already-defined `valid_indices` set, and that `band_dithered` is a bool.
- `_every_theme_is_single_colour`: no change needed — band entries' `departing_index`/`arriving_index` are both `IDX_WHITE`, so the existing single-colour assertion already holds for them. Confirm this by inspection; do not add special-casing that isn't needed.
- `_ink_contrast_pairing_is_correct`: extend the function's own `expected` dict with the 5 new ids, each mapped to `(panel_format.IDX_WHITE, panel_format.IDX_BLACK)` — the same pairing as `"white"`.
- `_dithered_and_weight_contract_is_correct`: extend the function's own `expected` dict with the 5 new ids, each mapped to `(False, "regular")` — the same pairing as `"white"`.
- `_default_theme_and_labels_are_correct`: extend the function's own `expected_labels` dict with the 5 new ids' exact label strings from Task 1 ("Band Blue", "Band Blue Light", "Band Green Light", "Band Red", "Band Black").

Then add 3 new `check(...)` calls (after the last one in this section, before the `history_db.py` import block) proving the new accessors' own contract, independent of the registry-shape checks above:
- `theme_is_band()` is `True` for exactly the 5 band ids (derived from `device_config.THEMES`, not a hardcoded list) and `False` for every other id.
- `theme_band_index()` returns the exact `IDX_*` value from Task 1's entries for each band id, and `None` for every non-band id.
- `theme_band_dithered()` returns the exact bool from Task 1's entries for each band id, and `False` for every non-band id.

Update `EXPECTED_CHECK_COUNT` (currently 26) to the real total once every new check is written and the file actually runs — count the real checks rather than pre-computing a number before writing them.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_config_history.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_config_history.py` exits 0 and prints `config-history: N/N checks pass` where N equals the file's own `EXPECTED_CHECK_COUNT`.
    - `_theme_registry_shape_is_correct` passes for all 16 entries (6-key contract for the 11 non-band ids, 8-key contract for the 5 band ids), and would genuinely fail if a band entry were missing `band_index` or `band_dithered` (spot-checked by temporarily deleting one field, observing the failure, then restoring it before commit).
    - `_ink_contrast_pairing_is_correct`, `_dithered_and_weight_contract_is_correct`, `_default_theme_and_labels_are_correct` each cover all 16 ids and still fail loudly on a mismatch for any of the original 11 (unchanged expected values for those 11).
    - The 3 new accessor checks correctly distinguish band ids from non-band ids using `device_config.THEMES`/`device_config.theme_is_band()` themselves, not a hardcoded id list that could drift from the registry.
    - `git diff --stat` touches only `server/test_config_history.py`.
  </acceptance_criteria>
  <done>server/test_config_history.py exercises all 16 THEMES entries through the existing contract checks (extended, not weakened) plus new coverage for theme_is_band()/theme_band_index()/theme_band_dithered(), and the full file passes.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces (09-01)

**New symbols**
- `server.device_config.theme_is_band(theme_id)` — returns bool
- `server.device_config.theme_band_index(theme_id)` — returns an `IDX_*` int or `None`
- `server.device_config.theme_band_dithered(theme_id)` — returns bool

**New THEMES entries**
- `band_blue`, `band_blue_light`, `band_green_light`, `band_red`, `band_black` — each with the White theme's own base-canvas fields plus a `band_index`/`band_dithered` pair

**No new files.**
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `device_config.json` on disk -> `THEMES` dict lookup | A stale or hand-edited config file's `theme` value reaches `normalise_theme_id()` before ever indexing `THEMES` — unchanged mechanism, now covering 5 more ids automatically. |

No new external input path, no new network surface, no package-manager installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Tampering | A hostile/stale `device_config.json` `"theme"` value naming a band id in a way that bypasses validation | medium | mitigate | `normalise_theme_id()` is untouched by this plan — its `isinstance(value, str) and value in THEMES` membership test automatically covers the 5 new ids the moment they're added to `THEMES`, with no new code path introduced. Regression-checked by Task 2's accessor tests confirming every registered id (band and non-band) round-trips correctly. |
| T-09-01-02 | Information Disclosure | A new accessor (`theme_band_index`/`theme_band_dithered`) raising on a malformed `theme_id` and leaking a stack trace to a caller that expected a safe default | low | accept | Both new accessors use `.get()` with an explicit default rather than `[...]` indexing for the two new optional fields, and are only ever called with an already-registry-validated `theme_id` (same calling convention as every existing accessor in this module, e.g. `theme_dithered()`). A raw `KeyError` from `THEMES[theme_id]` itself (unvalidated id) is the same pre-existing, accepted risk every other accessor in this file already carries — not a new surface this plan introduces. |
| T-09-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install in this plan — pure Python dict/function edits to two already-vendored files. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_config_history.py` exits 0.
- `server/.venv/bin/python3 -c "from server import device_config as dc; assert len(dc.THEMES) == 16"` succeeds.
- `git diff --stat` touches exactly `server/device_config.py` and `server/test_config_history.py`.
</verification>

<success_criteria>
- The 5 band themes exist in the registry with the exact base-canvas values the validated spike used, and are queryable by the 3 new accessors.
- The 11 pre-existing themes are byte-identical to their pre-phase values.
- server/test_config_history.py fully exercises the new registry shape without weakening any pre-existing coverage.
</success_criteria>

<output>
Create `.planning/phases/09-diagonal-band-theme/09-01-SUMMARY.md` when done.
</output>
