---
phase: 29-companion-review-feedback-round-3-the-developer-s-own-tour-o
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/static/value-controls.js
  - companion/i18n_fr/display.py
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-80]

must_haves:
  truths:
    - "Quiet hours reads as one object: Start and End render on one line, as one visual unit with the dial, not as two stacked full-width field groups (CFG-80)."
    - "The preset row is a segmented control whose labels are short — the hours are spoken once, by the dial's own readout, and never repeated on a button (CFG-80)."
    - "The normalised HH:MM twin beside each native time input is hidden at load when the native field already renders unambiguous 24 h, and is present and visible with scripts blocked (CFG-80)."
    - "B14's ground for the twin existing at all is unchanged: a browser that forces a 12 h rendering still gets the stored 24 h text beside the field."
    - "All four quiet-hours surfaces still agree after a preset click — the dial's arc, its readout caption, and both time fields decode to one pair of minute-of-day values (CFG-62, not reopened)."
  artifacts:
    - "companion/pages/config_page.py: quiet_hours_group() emitting a wrapping row for Start+End, short preset label constants, and the twin marked for script-driven hiding"
    - "companion/static/style.css: the Start/End row rule and the segmented preset-control rule, both from existing tokens"
    - "companion/static/value-controls.js: one load-time pass that hides the twin when the browser's own hour cycle is unambiguously 24 h"
    - "companion/i18n_fr/display.py: French twins of the shortened preset labels"
  key_links:
    - "The twin's hide direction is the OPPOSITE of layout.JS_GATE_CLASS: js-gate hides by default and reveals with .js, which would make a scripts-blocked browser lose the twin — the exact defect B14 exists to prevent. The twin must render VISIBLE server-side and be hidden by script only after a positive 24 h determination."
    - "config_page.py:3389-3391's docstring records 10-UI-SPEC.md as locking the four controls 'each on its own full-width line'. That locked rule is what CFG-80 relaxes for Start/End; it must be marked SUPERSEDED in place, in the same voice, never silently ignored."
    - "Phase 28's CFG-73 fixed the dial's minute-vs-HHMM readout bug. Do not reopen it, and do not replay the uncommitted main-checkout diff that predates it (CONTEXT.md CFG-80 forbids this explicitly)."
    - "value-controls.js is the quiet dial's own script and the one-script budget forbids a new file — extend it, never add companion/static/*.js."
---

<objective>
Make Quiet hours one visual object. Start and End move onto one line as a single unit with the dial instead of two independent full-width labels; the three presets become a segmented control with short labels; and the normalised HH:MM twin beside each native `<input type="time">` hides itself at load when the browser already renders the field in unambiguous 24 h, while staying present and visible with scripts blocked.

Purpose: the developer's own words on the quiet-hours screenshot — *"pas très joli ce composant"*. Four surfaces currently spell one value: the dial, the presets (each labelled with the hours it sets), the two native time inputs, and beside each a twin repeating what a correct browser already shows. The 2026-09-17 audit's P2 targets the twin specifically.

Output: one card that reads as one control, with the twin kept alive as the fallback it was built to be rather than as permanent noise — and the 24 h determination flagged honestly as the low-confidence mechanism it is.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-CONTEXT.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-RESEARCH.md
@.planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md
</context>

<environment>
**Python interpreter for every harness run: `server/.venv/bin/python`.** Bare `python3` has no Pillow and every `companion/test_*.py` fails at import.

**playwright is NOT installed.** `companion/test_browser_ux.py` SKIPs and exits 0. CFG-80's own named proofs are browser-shaped: "the surfaces-agree check reading the caption, the fields and the dial after a preset click" and "a check that the twin is hidden in a 24 h browser and visible with scripts blocked". Neither can be RUN here. This plan therefore:
- extends the existing browser-level surfaces-agree machinery in `companion/test_browser_ux.py` (written blind, never claimed as passing, reported as SKIPPED), AND
- lands the runnable half in `companion/test_config_page.py` as render-level and source-level checks that prove the two things a browser is not needed for: the server renders the twin VISIBLE (the scripts-blocked half, which is a fact about the served HTML), and the script's hide path is gated on a positive 24 h determination and on nothing else.

**LOW-CONFIDENCE MECHANISM — read RESEARCH.md assumption A1 before writing the script.** No 24 h-detection pattern exists anywhere in this codebase. `Intl.DateTimeFormat(navigator.language, {hour: "numeric"}).resolvedOptions().hour12 === false` measures the BROWSER's locale-derived preference, not what a specific `<input type="time">` will actually paint; no DOM API exposes the latter. A false negative — hiding the twin on a browser that is in fact painting 12 h — reintroduces exactly the defect B14 was built to fix. The hide must therefore be conservative (hide ONLY on a positive, unambiguous 24 h determination; on any uncertainty, leave the twin visible) and the real-device confirmation on a 12 h-locale browser is a blocking-in-spirit human follow-up recorded in Task 3.

**Do NOT replay the uncommitted `style.css` / `_normalised_time_html()` diff sitting in the MAIN CHECKOUT.** CONTEXT.md CFG-80 says it is moot and would conflict with Phase 28's shipped CFG-73 fix. Implement fresh in this worktree.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Start and End on one line with the dial, and the presets as a segmented control with short labels</name>
  <files>companion/pages/config_page.py, companion/static/style.css, companion/i18n_fr/display.py</files>
  <read_first>
    - `companion/pages/config_page.py` lines 3280-3443 — all of `quiet_hours_group()`: its docstring (including lines 3389-3391, the 10-UI-SPEC.md locked-order paragraph this task supersedes), the preset row build, the dial/readout build, and the return expression's two `<label>` blocks with their error siblings
    - `companion/pages/config_page.py` lines 556-590 — the six preset constants and the two `*_LABEL_TEMPLATE` forms, plus the comment explaining why the template is translated before substitution
    - `companion/static/style.css` — the `.quiet-dial*` block (grep `.quiet-dial {`) and `.runway-row` (the generic flex/wrap row the presets currently borrow), and the `.theme-form` / `.theme-option` pair (the app's existing segmented-button idiom: a bordered container holding borderless segments)
    - `companion/i18n_fr/display.py` — the French entries for the two preset label templates and the always-on label
    - `.claude/skills/sketch-findings-skypane/SKILL.md` — the Spacing entry (seven tokens, no new ones), the "Controls and density" entry (button geometry, the 36 px sub-960 px height, which class-scoped controls keep their own geometry), and the accent-reservation contract (a segmented control's selected state must reuse an already-listed accent use, or extend the list in `style.css`'s own header comment — never add a silent new accent consumer)
    - `companion/test_config_page.py` — grep `_no_card_builder_function_ever_calls_section_intro_html` and `theme-status__row` to learn which pinned properties constrain this card's markup before you change it
  </read_first>
  <action>
**A. Shorten the preset labels.** Replace the two hour-bearing label templates with bare labels: `QUIET_HOURS_PRESET_NIGHT_LABEL = "Night"` and `QUIET_HOURS_PRESET_WORKDAY_LABEL = "Day"`, and shorten the third to `QUIET_HOURS_PRESET_ALWAYS_ON_LABEL = "Always on"`. Delete the two `*_LABEL_TEMPLATE` constants and the derived `*_LABEL` assignments that formatted them, and delete the `%`-substitution at the render site. French twins in `companion/i18n_fr/display.py`: `"Nuit"`, `"Journée"`, `"Toujours actif"` — CONTEXT.md CFG-80 names "Nuit" and "Journée" explicitly. The hours the buttons used to spell are already spoken by `quiet_dial_readout_html()`'s own caption, which is the whole ground for the cut; record that ground in a comment at the constants, and note that `QUIET_HOURS_PRESET_NIGHT_START`/`_END` and `_WORKDAY_START`/`_END` are UNCHANGED — they still feed the buttons' `data-preset-*` attributes and still come from `device_config`'s own shipped defaults, so nothing about what a preset DOES changes.

Removing the `%s` templates also removes two catalogue keys; `test_i18n.py`'s Checks 1 and 2 pin the two sides equal, so the English deletions and the French additions/deletions must land together.

**B. The presets become a segmented control.** Replace the `.runway-row` wrapper with a dedicated class (name it for the role, e.g. `quiet-preset-row`, declared as a module-level constant next to the other `QUIET_*_CLASS` constants — this file's own convention is "a class that exists in Python and nowhere in style.css paints nothing, and a check scans the emitted markup's classes against the stylesheet for exactly that"). In `style.css`, give it the SAME bordered-container-plus-borderless-segment idiom `.theme-form`/`.theme-option` already use for the language and theme switches: one hairline border and radius on the container, segments with no border of their own, a hairline divider between them. Reuse existing tokens and existing wash strengths only. Do NOT introduce a selected/active state: these presets are momentary actions that write into the time fields, not a persistent choice, so there is nothing to keep selected — and that means this control adds no accent consumer and needs no change to the accent-reservation list. State that reasoning in the rule's comment, because a reader will otherwise expect a selected segment.

The three buttons stay `type="button"` with their existing `data-preset-*` attributes and no `form=` attribute, so a scripts-blocked browser still gets three inert buttons beside two fully usable time inputs — the established degradation for this card. `companion/static/dirty-state.js` reads those attributes and is NOT edited.

**C. Start and End on one line, as one unit with the dial.** Wrap the two `<label>` blocks (each with its native `<input type="time">`, its twin sibling and its error paragraph) in one container with its own class (again a Python constant, e.g. `quiet-times-row`). In `style.css` lay that container out as a two-column grid at the card's own width — `grid-template-columns: repeat(2, minmax(0, 1fr))` with an existing gap token — collapsing to one column below the narrowest supported width if and only if the two fields plus their twins cannot both fit at 360 px; if you add that fallback, add it as a `@media (max-width: …)` rule whose threshold you state and justify in the comment, and prefer NOT adding it if the fields fit, since a rule that never engages is dead code. Each label keeps its own error paragraph inside its own column so an error never displaces its sibling field. The container renders immediately after the preset row and immediately below the dial/readout pair, so dial → readout → presets → the one times row reads as one object.

**D. Supersede the locked layout rule in writing.** `quiet_hours_group()`'s docstring currently states that 10-UI-SPEC.md locks the order of the four controls "presets, then Start, then End, each on its own full-width line". Add a paragraph in the same voice: CFG-80 (this plan) SUPERSEDES the "each on its own full-width line" clause for Start and End specifically, because four surfaces stacked full-width is what made the card read as four controls for one value; the ORDER is unchanged (presets still precede Start, Start still precedes End in document order inside the row), and the ring's own placement argument from 25-04 is untouched. Keep the original sentence readable rather than deleting it — this file's SUPERSEDED-in-place convention.

**E. No new tokens.** No new custom property, no new colour literal, no new font size, no new family. Verify with `git diff -U0 companion/static/style.css | grep '^+' | grep -cE '^\+\s*--[a-z]'` returning 0.

Add checks to `companion/test_config_page.py`:
1. Every class the new markup emits exists in `style.css` (extend the existing emitted-class-vs-stylesheet scan if one already covers this card; read the file and reuse rather than duplicate).
2. The rendered card's document order is `quiet-dial` → readout → preset row → times row, asserted as chained `index()` comparisons with all four printed on failure.
3. Both `<input type="time">` elements are inside the times row's container slice, each with exactly one twin sibling and its own error paragraph slot.
4. The three preset buttons render with the short labels in both languages via `prefs.set_request_prefs(lang=...)`, and NO preset label contains a `:` character — the assertion that the hours are no longer on the buttons, stated as a property rather than as three literal comparisons.
5. The times row's CSS declares two tracks, parsed from `style.css`.

Append a new `EXPECTED_CHECK_COUNT` assignment below `test_config_page.py`'s existing stack (baseline 266 at planning time, unchanged by plans 29-01..29-03 — re-confirm by running before you edit). **Re-derive the new value by running**, never by arithmetic.

**Mutation-prove** the order check (swap the preset row and the times row in the return expression) and the no-colon-in-labels check (restore one hour-bearing label). Revert each with `git checkout-index -f -- companion/pages/config_page.py` after staging your correct version; quote both real failure messages verbatim in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -cE 'QUIET_HOURS_PRESET_(NIGHT|WORKDAY)_LABEL_TEMPLATE' companion/pages/config_page.py` returns 0.
    - No rendered preset button label contains `:` in either language; the three labels are Night/Day/Always on and Nuit/Journée/Toujours actif.
    - `QUIET_HOURS_PRESET_NIGHT_START`, `_NIGHT_END`, `_WORKDAY_START`, `_WORKDAY_END` and `QUIET_HOURS_PRESET_ATTR` are byte-identical to their pre-task values, and every `data-preset-start`/`data-preset-end`/`data-preset-enabled` attribute still renders with the same value.
    - `git diff --stat companion/static/dirty-state.js` is empty.
    - In the rendered card: `index(quiet-dial) < index(readout) < index(preset row class) < index(times row class)`, each class occurring exactly once.
    - Both `<input type="time">` elements and both twin spans fall inside the times-row container's slice of the markup.
    - The times-row rule in `style.css` declares exactly two grid tracks.
    - `git diff -U0 companion/static/style.css | grep '^+' | grep -cE '^\+\s*--[a-z]'` returns 0.
    - `quiet_hours_group()`'s docstring contains a new paragraph naming CFG-80 and marking the full-width-line clause SUPERSEDED, with the original sentence still present.
    - `server/.venv/bin/python companion/test_config_page.py` exits 0 with its total equal to the re-derived `EXPECTED_CHECK_COUNT`; `server/.venv/bin/python companion/test_i18n.py` green.
    - Two mutation proofs recorded with real failure messages quoted verbatim.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_config_page.py &amp;&amp; server/.venv/bin/python companion/test_i18n.py</automated>
  </verify>
  <done>Quiet hours renders dial, readout, a segmented preset row with short labels, then Start and End side by side as one unit; every preset still writes the same times; the locked full-width rule is superseded in writing; zero new tokens; two mutation proofs quoted.</done>
</task>

<task type="auto">
  <name>Task 2: the normalised twin hides itself only on a positive 24 h determination, and survives with scripts blocked</name>
  <files>companion/pages/config_page.py, companion/static/value-controls.js, companion/static/style.css</files>
  <read_first>
    - `companion/pages/config_page.py` lines 2665-2695 — all of `_normalised_time_html()` including its four-paragraph docstring: the ground for the element existing, the "not a placeholder, not a title" reasoning, the `aria-hidden` reasoning, and the omit-don't-fabricate rule. Read line 3933-3935 too — a SECOND emission site of the same `.field-inline-value` span for the wake-interval unit, which must NOT be affected by this change.
    - `companion/static/style.css` line ~8238 — the `.field-inline-value` rule and its comment
    - `companion/static/style.css` lines 1130-1165 — the `.js-gate` mechanism and its long comment. Read it to understand why it is the WRONG direction here: it hides by default and reveals with `.js`, so a scripts-blocked browser would lose the twin.
    - `companion/static/nav-dropdown.js` line 31 — `document.documentElement.className += " js"`, the one place the `.js` class is set
    - `companion/static/value-controls.js` lines 1-100 (the module's own preamble, the one-script budget note, the three taxes), 170-200 (what it already knows about the time inputs), 950-1046 (the delegation model and the deliberate absence of a `DOMContentLoaded` wrapper / load-time pass — this task adds the file's first load-time pass and must justify it against that stated choice)
    - `.planning/phases/29-.../29-RESEARCH.md` — the CFG-80 section's "No existing 24h-detection pattern found" paragraph and Assumptions Log A1
  </read_first>
  <action>
**A. Mark the twin without changing its default visibility.** In `_normalised_time_html()`, add a stable hook attribute to the emitted span — a `data-`attribute whose name is a module-level constant next to the other `QUIET_*` constants (e.g. `data-normalised-time`) — and nothing else. The span stays VISIBLE as served: no `hidden` attribute, no `.js-gate` class, no `style`. Do NOT add a `field=` parameter or any other signature widening (CONTEXT.md CFG-80 explicitly moots that approach).

Add a docstring paragraph to `_normalised_time_html()` in the file's own voice recording exactly this: B14's ground for the element existing is UNCHANGED — a browser that forces a 12 h rendering still needs the stored 24 h text beside the field — and what CFG-80 changes is only its DEFAULT VISIBILITY on a browser that is already unambiguous. State the direction and why it is that way round: the element is served visible and hidden by script only after a positive determination, because the opposite direction (`.js-gate`) would delete the fallback for precisely the readers who need it most. A future reader must not be able to mistake this for the whole element being second-guessed.

The wake-interval unit sibling at line ~3933 emits the same `.field-inline-value` class WITHOUT the new hook attribute, so it is untouched by the script. Assert that separation in Task 3.

**B. The script's hide pass.** In `companion/static/value-controls.js`, add one load-time pass — the file's first, so justify it in a comment against the module's own stated "no DOMContentLoaded wrapper, and no load-time pass" choice: that choice was about not duplicating LIVE state, and this pass writes no state, it makes a one-time environment determination. Write it in the same ES5-safe dialect the file already uses (no `let`/`const`/arrow functions/template literals/backticks — `companion/test_companion_app.py` pins this contract for the static scripts and will fail on a violation; a backtick inside a COMMENT is enough to fail it, as quick task 260921-n2n discovered the hard way).

The pass, conservatively:
1. Determine the hour cycle with a guarded feature test. Only proceed if `window.Intl`, `Intl.DateTimeFormat`, and `resolvedOptions` on a constructed formatter are all present; if any is missing, do nothing and leave the twin visible.
2. Read the resolved `hour12` (and, when the runtime exposes it, `hourCycle`). Hide the twins ONLY when the determination is unambiguously 24 h — `hour12 === false`, strictly, i.e. an explicit boolean false, never a falsy `undefined`. Any other value, including `undefined`, means uncertain, which means leave the twin visible.
3. When and only when that positive determination holds, set `hidden = true` on every element carrying the new hook attribute. Use the `hidden` property, not a class and not an inline `style` — `hidden` removes the element from the layout AND the accessibility tree, and the span is already `aria-hidden="true"`, so there is nothing to lose there and nothing new to keep in sync.
4. Wrap the whole pass so a throw cannot break the rest of the file's initialisation.
5. Write a comment above it recording, verbatim in substance, RESEARCH.md's A1 caveat: this measures the BROWSER's locale-derived preference, not what a given `<input type="time">` will actually paint, because no DOM API exposes the latter; the failure mode being guarded against is a FALSE NEGATIVE (hiding the twin on a browser that paints 12 h), which is why the test is strict-false and every uncertain case leaves the fallback on screen. Name the real-device confirmation as outstanding.

**C. CSS.** Add `.field-inline-value[hidden] { display: none; }` after the base `.field-inline-value` rule, with a comment citing the same trap quick task 260921-n2n's `.resolve-context[hidden]` guard fixed: an author `display`/`margin` declaration on the base class beats the user-agent `[hidden]` rule, so without this guard setting `hidden` would hide nothing. Verify the base rule's declarations against the UA rule before writing this — if the base rule declares no `display`, say so in the comment and add the guard anyway as defence in depth, matching the posture the stylesheet already takes for `.refresh-pill`, `.dirty-bar`, `.login-reveal` and `.resolve-context`.

Do NOT add a new `.js`-scoped selector, do NOT add a script file, do NOT touch `nav-dropdown.js`.
  </action>
  <acceptance_criteria>
    - `_normalised_time_html()`'s returned span carries the new hook attribute and carries NO `hidden` attribute, NO `js-gate` class and NO `style` attribute — asserted against the function's real output, not its source.
    - The wake-interval unit span at config_page.py ~3933 carries `.field-inline-value` and does NOT carry the hook attribute.
    - `companion/static/value-controls.js` contains zero occurrences of `` ` ``, ` let `, ` const ` and `=>` in the lines this task adds; `server/.venv/bin/python companion/test_companion_app.py` passes its static-script contract checks.
    - The hide condition in the added JS is a strict comparison against `false` — `grep -c 'hour12 === false' companion/static/value-controls.js` returns 1 (or the equivalent strict form, stated in the SUMMARY), and there is no truthiness test on the resolved option.
    - `grep -c 'field-inline-value\[hidden\]' companion/static/style.css` returns 1.
    - `ls companion/static/*.js | wc -l` is unchanged from its pre-task value (the one-script budget: extend, never add).
    - `git diff --stat companion/static/nav-dropdown.js companion/static/dirty-state.js` is empty.
    - `server/.venv/bin/python companion/test_config_page.py` and `companion/test_companion_app.py` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_companion_app.py &amp;&amp; server/.venv/bin/python companion/test_config_page.py</automated>
  </verify>
  <done>The twin is served visible, hidden only by a strict positive 24 h determination, with the uncertain and no-Intl cases both leaving it on screen; the CSS `[hidden]` guard is in place; the wake-interval sibling is provably unaffected; the script budget is unchanged and the ES5 contract holds.</done>
</task>

<task type="auto">
  <name>Task 3: prove the twin's two states, keep the four surfaces agreeing, and hand the 12 h-locale confirmation to the developer</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - `companion/test_config_page.py` — the file's `check()` helper, its `EXPECTED_CHECK_COUNT` stack near line 1086, and the checks that already read `companion/static/value-controls.js` from disk (grep `value-controls`) so this task extends an existing scanning idiom rather than inventing one
    - `companion/test_browser_ux.py` lines ~11540-11900 — the existing quiet-dial surfaces-agree machinery (CFG-62's one-check-decodes-all-four-surfaces shape). Read it so Task 3's addition to it is written in its own idiom, and so the SUMMARY can state exactly which assertion was added blind.
    - `.planning/quick/260921-n2n-.../260921-n2n-SUMMARY.md` — the mutation-proof format and the vacuity-floor shape
    - `.claude/skills/sketch-findings-skypane/SKILL.md` — the "review-feedback discipline" paragraph (assert relationships, not endpoints) and the no-JS control contract (the verdict is what the control DOES, not that it renders)
  </read_first>
  <action>
Add three checks to `companion/test_config_page.py`. Every one must run for real under `server/.venv/bin/python`; none may depend on a browser.

**Check A — the scripts-blocked state, proven from the served markup.** Render `/display`'s quiet-hours card in both languages. Assert: exactly two spans carry the hook attribute; each carries the `field-inline-value` class; each renders the stored `HH:MM` text of its own field, matching the corresponding `<input type="time">`'s `value` attribute as a RELATIONSHIP (parse both out of the markup and compare; never compare against a literal like `"23:00"`); and neither carries `hidden`, `js-gate` or an inline `style`. This IS the scripts-blocked proof: with no script running, the served HTML is what the reader sees, so a visible twin in the served HTML is the fallback working. Say so in the check's own docstring — do not describe it as a browser test.

**Check B — the hide path is gated, and gated on one thing only.** Read `companion/static/value-controls.js` from disk and assert, on the comment-stripped source (strip `/* … */` and `//` lines first, the way `test_companion_app.py`'s motion-budget check strips comments before measuring, and for the same reason: this file's comments quote the very tokens the scan counts):
1. Exactly one assignment sets `hidden = true` on elements selected by the hook attribute.
2. That assignment is inside a branch whose condition is a strict comparison of a resolved hour option against `false` — not a truthiness test, not `!= true`, not a negation of a truthy read. Print the condition text on failure.
3. There is no code path that sets `hidden = true` on the hook attribute outside that branch — assert the count of such assignments equals 1, so a second, ungated path cannot be added silently.
4. The `Intl`/`resolvedOptions` availability guard precedes the determination — assert its source index is lower than the branch's.
5. Vacuity floor: assert the comment-stripped source is at least some stated fraction of the raw file's length and that the hook-attribute literal appears at least twice (once in the selector, once in the assignment's own vicinity), printing both counts on failure, so a mangled strip or a renamed hook cannot make this check pass over nothing.

**Check C — the four surfaces still agree, at the render level.** CFG-62's own check is browser-level and cannot run here, but the SERVER's four surfaces can be compared: `quiet_dial_html()`'s arc presentation attributes, `quiet_dial_handles_html()`'s handle positions, `quiet_dial_readout_html()`'s caption text, and both `<input type="time">` `value` attributes are all computed in `quiet_hours_group()` from the same `effective_start`/`effective_end` pair. Decode all four back to a canonical minute-of-day pair from the rendered markup alone and assert the resulting set has exactly one member — the same shape CFG-62 established. Do this for at least three input pairs including one that wraps midnight (e.g. 23:00→07:00) and one that does not (08:00→18:00), plus one rejected-save echo case where `submitted` carries a value different from the stored one, to pin D-07's echo rule. This check is the guard that Task 1's markup restructuring did not silently break the agreement Phase 27 established; it is NOT a replacement for the browser-level preset-click check, and its docstring must say so.

**Also extend the browser-level check, written blind and reported as such.** In `companion/test_browser_ux.py`, extend the existing quiet-dial surfaces-agree machinery with one assertion: after a preset click, in addition to the four surfaces agreeing, each twin's visibility matches the browser's own resolved hour cycle — hidden when the resolved cycle is unambiguously 24 h, visible otherwise — read from the live DOM. Write it in that file's own idiom. **State plainly in the SUMMARY that this addition was written without ever being executed, because playwright is absent from this worktree, and that `companion/test_browser_ux.py` reported SKIPPED, not passed.** Do NOT append to `test_browser_ux.py`'s `EXPECTED_CHECK_COUNT` blind — if the file's convention requires a count change for a new `check(...)` call, add the assertion INSIDE the existing check rather than as a new one, so the pinned count does not move to a value nobody could verify. State which of those two you did and why.

Append a new `EXPECTED_CHECK_COUNT` assignment to `companion/test_config_page.py` below its existing stack. **Re-derive by running.**

**Mutation-prove all three**, reverting each with `git checkout-index -f -- <path>` after staging your correct version:
- A: add `hidden` to `_normalised_time_html()`'s emitted span. Check A must fail and name which twin.
- B: relax the script's strict `=== false` to a truthiness test. Check B must fail and print the condition it found.
- C: feed `quiet_dial_readout_html()` a minute value instead of the HH:MM pair (the exact shape of Phase 28's CFG-73 bug). Check C must fail with the disagreeing set printed — this is the proof that C would have caught CFG-73.
Quote all three real failure messages verbatim in the SUMMARY.
  </action>
  <acceptance_criteria>
    - Check A: exactly two hook-attribute spans per render, each matching its own field's `value` by relationship, none carrying `hidden`/`js-gate`/`style`, in both languages.
    - Check B: exactly one gated `hidden = true` assignment on the hook attribute; its condition is a strict comparison against `false`; the availability guard precedes it; the vacuity floor asserts the stripped-source length ratio and a hook-literal count of at least 2, both printed on failure.
    - Check C: for three input pairs including a midnight-wrapping one and a rejected-save echo, the decoded set of arc, handles, readout and both field values has exactly one member.
    - `server/.venv/bin/python companion/test_config_page.py` exits 0 with its total equal to the re-derived `EXPECTED_CHECK_COUNT`.
    - Three mutation proofs recorded with real failure messages quoted verbatim; `git status --porcelain` clean after each revert.
    - `companion/test_browser_ux.py`'s `EXPECTED_CHECK_COUNT` is either unchanged (assertion folded into an existing check) or changed with an explicit statement that the value could not be verified in this worktree — state which, and why.
    - `scripts/run-all-tests.sh` reports `companion/test_browser_ux.py` as SKIPPED; the SUMMARY records it as a skip, never as a pass.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_config_page.py &amp;&amp; server/.venv/bin/python companion/test_companion_app.py &amp;&amp; server/.venv/bin/python companion/test_i18n.py</automated>
    <human-check>On a browser whose OS region forces a 12 h clock (a macOS or iOS device set to United States), open /display and confirm each quiet-hours time field shows its 24 h twin BESIDE it — the twin must NOT be hidden there. Then on a French/European-locale browser, confirm the twin is hidden and the native field alone reads e.g. 23:00. Then disable JavaScript entirely and confirm the twin is visible in both locales. Finally click each preset and confirm the dial arc, the caption, both fields and (where visible) both twins all move together. Report any case where the twin vanished while the native field was still painting AM/PM — that is the false negative RESEARCH.md's A1 warns about and it reopens the mechanism.</human-check>
  </verify>
  <done>Three runnable checks green and mutation-proven, one of them proving it would have caught Phase 28's CFG-73 bug; the browser-level preset-click assertion extended and honestly reported as written-but-never-run; the 12 h-locale false-negative risk handed to the developer as a named, specific confirmation rather than assumed away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/display` POST (settings form) | Unchanged: `handle_post()`'s validation of the two time fields is not touched by this plan |
| browser environment → `value-controls.js` | NEW read of an ambient environment signal (`Intl.DateTimeFormat(...).resolvedOptions()`) drives a visibility decision. The signal is read-only and never persisted or submitted. |
| `value-controls.js` → the DOM | The script sets `hidden` on presentation-only spans; it writes no field value and no server state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04-01 | Information Disclosure | the normalised twin's script-driven hiding | high | mitigate | A false-negative determination hides the 24 h text on a browser that is in fact painting 12 h, so the reader sees "11:00 PM" beside a preset that means 23:00 and cannot tell they are the same value — the exact misreading B14 was built to prevent, and the one that could make someone configure the frame's quiet window wrong. Mitigated three ways: the determination is strict (`=== false` only; `undefined` and any missing API leave the twin visible), Check B pins that strictness and forbids a second ungated path, and the real-device 12 h-locale confirmation is a named human follow-up rather than an assumption. |
| T-29-04-02 | Denial of Service | the new load-time pass in `value-controls.js` | medium | mitigate | A throw in a load-time pass could abort the rest of the file's initialisation and take the dial's own interaction with it. The pass is wrapped so a throw cannot escape, every API access is feature-guarded, and Check B asserts the guard precedes the determination. |
| T-29-04-03 | Tampering | the preset buttons' `data-preset-*` values | medium | mitigate | Shortening the labels must not change what a preset WRITES. The four start/end constants and the attribute-name constant are asserted byte-identical to their pre-task values, and every rendered `data-preset-*` attribute value is asserted unchanged. `dirty-state.js`, which reads them, has a zero-line diff. |
| T-29-04-04 | Tampering | `handle_post()`'s time-field validation | low | accept | Not touched by this plan. The two inputs keep their `name`, `required`, `form=` and `lang` attributes; only their surrounding layout changes. Accepted with no new control; the existing server-side HH:MM validation remains the only writer. |
| T-29-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install occurs. No dependency manifest or lockfile is touched; `companion/` is stdlib-only and `companion/static/` carries no build step and no vendored dependency. `ls companion/static/*.js | wc -l` is asserted unchanged, so no new script enters the trust surface. The legitimacy gate does not apply and no audit table is required. |
</threat_model>

<artifacts>
## Artifacts this phase produces (this plan's share)

**New symbols (Python, `companion/pages/config_page.py`):**
- a preset-row class constant (segmented control container) and a times-row class constant — both declared beside the existing `QUIET_*_CLASS` constants
- a hook-attribute constant for the normalised twin (e.g. `QUIET_NORMALISED_TIME_ATTR`)
- `QUIET_HOURS_PRESET_NIGHT_LABEL` = `"Night"`, `QUIET_HOURS_PRESET_WORKDAY_LABEL` = `"Day"`, `QUIET_HOURS_PRESET_ALWAYS_ON_LABEL` = `"Always on"` (redefined as plain labels)

**Deleted:**
- `QUIET_HOURS_PRESET_NIGHT_LABEL_TEMPLATE`, `QUIET_HOURS_PRESET_WORKDAY_LABEL_TEMPLATE` and their derived formatted assignments, plus their two French catalogue keys
- `.runway-row` as the preset row's wrapper on this card (the class itself survives for the runway picker)

**New CSS:**
- the preset-row segmented-control rule (bordered container, borderless segments, no selected state)
- the times-row two-track grid rule
- `.field-inline-value[hidden] { display: none; }`

**New JS (`companion/static/value-controls.js`, extended — no new file):**
- one guarded load-time pass that sets `hidden` on every hook-attribute span when and only when the resolved hour cycle is strictly 24 h

**New French keys (`companion/i18n_fr/display.py`):** `"Nuit"`, `"Journée"`, `"Toujours actif"`

**Added checks:** five in `test_config_page.py` from Task 1, three more from Task 3, one assertion folded into `test_browser_ux.py`'s existing surfaces-agree check (written blind, not executed).

**Unchanged and asserted so (recorded so a reader does not look for changes that are not there):** `handle_post()`'s time validation, `dirty-state.js`, `nav-dropdown.js`, the four preset start/end constants, the `.js-gate` mechanism, the script file count, `quiet_dial_html()`'s own geometry constants.
</artifacts>

<verification>
- `server/.venv/bin/python companion/test_config_page.py` — green, re-derived count
- `server/.venv/bin/python companion/test_companion_app.py` — green; it pins the static scripts' ES5 contract and the stylesheet's token/motion budget, both touched here
- `server/.venv/bin/python companion/test_i18n.py` — green; re-derive, since three keys are replaced
- `server/.venv/bin/python companion/test_view_pages.py` / `companion/test_status_pages.py` — counts unchanged from plan 29-03's
- `scripts/run-all-tests.sh` with `PYTHON=server/.venv/bin/python` — full suite; `test_browser_ux.py` reported as SKIPPED, never as a pass
- `ls companion/static/*.js | wc -l` — unchanged
- `git diff -U0 companion/static/style.css | grep '^+' | grep -cE '^\+\s*--[a-z]'` — 0
</verification>

<success_criteria>
- Quiet hours renders as dial, readout, segmented presets, then Start and End on one line
- No preset label spells an hour in either language; every preset still writes its original times
- The twin is served visible and hidden only on a strict positive 24 h determination; uncertain and no-Intl cases leave it visible
- The `[hidden]` CSS guard is present so setting `hidden` actually hides
- The four server-rendered quiet-hours surfaces provably agree, including across a midnight wrap and a rejected-save echo
- Zero new custom properties, zero new scripts, `dirty-state.js` and `nav-dropdown.js` untouched
- Five mutation proofs across the three tasks, real failure messages quoted verbatim
- The 12 h-locale false-negative risk is a named human follow-up, and the browser-level addition is reported as written-but-never-run
</success_criteria>

<output>
Create `.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-04-SUMMARY.md` when done, including: the harness before/after table with every count re-derived by running, all five mutation proofs with real failure messages quoted verbatim, the recorded SUPERSEDED note for 10-UI-SPEC.md's full-width-line clause, an explicit statement of which `test_browser_ux.py` option was taken and why, and the A1 low-confidence caveat restated with the exact human confirmation still outstanding.
</output>
