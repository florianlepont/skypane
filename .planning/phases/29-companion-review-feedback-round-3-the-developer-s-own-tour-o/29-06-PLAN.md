---
phase: 29-companion-review-feedback-round-3-the-developer-s-own-tour-o
plan: 06
type: execute
wave: 6
depends_on: ["29-05"]
files_modified:
  - companion/pages/health_page.py
  - companion/pages/airlines_page.py
  - companion/i18n_fr/health.py
  - companion/i18n_fr/airlines.py
  - companion/test_status_pages.py
  - companion/test_companion_app.py
autonomous: true
requirements: [CFG-79, CFG-84]

must_haves:
  truths:
    - "État's battery-trend heading reads in the form 'Batterie · 3 mois' / 'Battery · 3 months' — a fixed short heading, no longer a heading with an inline caption span (CFG-84)."
    - "The precision the old heading carried ('moyenne quotidienne' / 'daily average', or the reading-count fallback) renders in the card's own caption element, a sibling of the heading, not inside it (CFG-84)."
    - "Every visible caption on all six authenticated routes, in both languages, is one sentence of at most 12 words — except the four Display Aspect captions Phase 30 owns and a named, pinned exemption list (CFG-79)."
    - "État's check-in caption keeps every honesty clause it carries today: the clauses that leave the visible sentence move into an existing disclosure, verbatim, and still render (CFG-79)."
    - "The site-wide floor is enforced by a check that renders each authenticated route over a real server in both languages — mutation-proven against a deliberately long caption."
  artifacts:
    - "companion/pages/health_page.py: _battery_trend_section_html() emitting <h2> plus a sibling caption paragraph, and a fixed short heading template using BATTERY_TREND_WINDOW_DAYS"
    - "companion/pages/health_page.py: the check-in caption reduced to one sentence with its honesty clauses moved into a readings-disclosure, and _READ_ONLY_NOTE shortened"
    - "companion/pages/airlines_page.py: five over-length caption constants shortened"
    - "companion/test_companion_app.py: the site-wide floor check over all six routes in both languages, with a pinned CAPTION_FLOOR_EXEMPTIONS list"
  key_links:
    - "config_page.ASPECT_CAPTION_EXEMPTIONS (created by plan 29-05) is the Display Aspect exemption. The site-wide check must import and reuse it, never re-list those four strings — one exemption, one home, so Phase 30 empties it in one place."
    - "test_i18n.py Checks 1 and 2 pin the English page-module string set equal to the French CATALOG key set; every English edit lands with its French twin or the harness fails."
    - "health_page.py's CHECK_IN_CAPTION_NOT_PROOF carries an explicit KEEP THIS CLAUSE comment naming threat T-24-07-A: the record cannot tell a missed wake from a lost log range. It is MOVED into a disclosure, never deleted."
    - "_battery_trend_caption()'s own two-branch logic (real daily average vs. reading-count fallback) survives unchanged and simply feeds the new sibling caption instead of the inline span."
---

<objective>
Two things, on the two pages that share these files.

**CFG-84:** État's battery-trend card stops carrying its precision inside its heading. The `<h2>` becomes a fixed short "Battery · 3 months" / "Batterie · 3 mois", and the precision `_battery_trend_caption()` computes moves into a sibling caption paragraph — the same heading-then-caption shape every other card on the site uses.

**CFG-79, second half:** the editorial floor reaches État and Compagnies, and then the whole site. État's check-in caption — today four clauses joined into one paragraph of roughly fifty words — becomes one sentence, with every honesty clause moved verbatim into the card's existing disclosure rather than cut. Compagnies' five over-length captions come down to twelve words. Then one check, over a real server, measures every caption on all six authenticated routes in both languages, with Display's four Aspect captions exempted through the tuple plan 29-05 already created.

Purpose: the 2026-09-17 audit's P2 for État ("le titre `Tendance de la batterie — 3 derniers mois, moyenne quotidienne` mérite d'être raccourci visuellement, par exemple `Batterie · 3 mois`, avec la précision dans une légende") and the developer's own *"il y a trop de texte descriptif qui servent à rien sur l'ensemble du site"* — made executable, site-wide, rather than left as an intention.

Output: a short État heading, a check-in caption that says one thing without losing an honesty statement, five shorter Compagnies captions, and the phase's one site-wide gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-CONTEXT.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-RESEARCH.md
@.planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-05-SUMMARY.md
</context>

<environment>
**Python interpreter for every harness run: `server/.venv/bin/python`.** Bare `python3` has no Pillow and every `companion/test_*.py` fails at import.

**playwright is NOT installed.** `companion/test_browser_ux.py` SKIPs and exits 0. CFG-79's "harness check measuring RENDERED caption length on every authenticated route" is satisfied here WITHOUT a browser, and legitimately: every caption in this app is a server-rendered Python string constant, no client script rewrites caption text, so the HTML the server returns over a real HTTP request IS the rendered text. Task 3 uses `test_companion_app.py`'s existing real `ThreadingHTTPServer` harness plus its `_login()` helper, and sets the language through `auth.UI_LANG_COOKIE_NAME`, the same cookie `companion/app.py`'s own `_lang_from_request()` reads. Describe this as an HTTP-level render measurement, never as a browser check.

**Word counting must match plan 29-05's rule exactly** (naive whitespace split, on unescaped text, after `%` substitution, with a leading em dash and its following whitespace stripped). Read `29-05-SUMMARY.md` and the check plan 29-05 landed, and reuse the SAME counting helper rather than writing a second one that could disagree. If plan 29-05's helper lives in `test_config_page.py` and is not importable, lift it into a shared position or duplicate it with a comment pinning the two equal and a check asserting they agree on a fixture string — state which you did.

**The French module for a given English string is NOT guessable.** For every string you change, find its French home with `grep -rn "<the exact English literal>" companion/i18n_fr/` and edit that module. `files_modified` lists the two most likely; if a string lives elsewhere, edit that module too and say so.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: CFG-84 — État's battery-trend heading becomes short, with its precision in a sibling caption</name>
  <files>companion/pages/health_page.py, companion/i18n_fr/health.py, companion/test_status_pages.py</files>
  <read_first>
    - `companion/pages/health_page.py` lines 2858-2948 — all of `_battery_trend_section_html()`: its long docstring recording four prior supersessions, the `state`/`caption` signature widenings, and the return expression that emits `<h2 class="text-heading">%s<span class="text-label section-caption"> — %s</span></h2>`
    - `companion/pages/health_page.py` lines 935-958 — `_battery_trend_caption()`'s three-case logic and its docstring
    - `companion/pages/health_page.py` lines 136-155 — `BATTERY_TREND_LIMIT` and `BATTERY_TREND_WINDOW_DAYS = 90`, with the comment distinguishing what each bounds
    - `companion/pages/health_page.py` lines 505-512 — `BATTERY_SECTION_HEADING = "Battery trend"`, `BATTERY_SECTION_CLASS = "battery-trend-section"`
    - `companion/pages/health_page.py` lines 2085-2100 and 4280-4292 — `render()`'s `battery_caption` computation and the single call site
    - `companion/i18n_fr/health.py` lines 160-176 — `"Battery trend"`, `"Last 3 months, daily average"`, `"Latest %d readings"`
    - `.claude/skills/sketch-findings-skypane/SKILL.md` — the Typography entry's "New non-serif exception" paragraph: `.battery-trend-section > h2` is in the selector list that renders this heading at the 16 px sans-semibold nested-card-title rung. Changing the heading's TEXT must not change its class or that selector's applicability.
    - `companion/test_status_pages.py` — grep `BATTERY_SECTION_HEADING`, `Last 3 months`, `battery_trend_caption` for every existing check that reads this heading or its span; several will need retargeting
  </read_first>
  <action>
Restructure the card's title, do not merely edit a string.

1. Replace `BATTERY_SECTION_HEADING = "Battery trend"` with a template that interpolates the real window: `BATTERY_SECTION_HEADING_TEMPLATE = "Battery · %d months"`, rendered with `BATTERY_TREND_WINDOW_DAYS // 30`. Use the existing constant, never a hardcoded `3` — `BATTERY_TREND_WINDOW_DAYS` is already 90 and already the chart's own window, which is what makes the heading honest if the window ever moves. The separator is a real U+00B7 middle dot, matching this codebase's real-Unicode punctuation convention (the same convention that puts a real en dash in the preset ranges). French twin: `"Batterie · %d mois"`. Translate the template, then substitute — this codebase's established order.
2. Change `_battery_trend_section_html()`'s return expression so the heading carries ONLY its own escaped text and the caption becomes a SIBLING paragraph immediately after it: `<h2 class="text-heading">…</h2><p class="text-label section-caption">…</p>`. Drop the inline `<span>` and the leading ` — ` entirely. `.text-label section-caption` is the composition every other card's caption uses and the one plan 29-05's floor check measures; using anything else would create a second "how a card names itself" idiom in the same phase.
3. Keep `_battery_trend_caption()` byte-identical. Its three cases already produce the right text for the new caption: the 3-month/daily-average framing, the same framing when there is nothing to plot, and the real reading count otherwise. The `caption=None` default in `_battery_trend_section_html()`'s signature still reproduces the `"Latest %d readings"` fallback for any caller passing two positional arguments — keep that behaviour and keep the docstring paragraph that documents it.
4. Extend the docstring with a new SUPERSEDED paragraph in the file's own voice, naming CFG-84 and this plan: the `<h2>`-with-inline-caption-span shape quick task 260902-j8w established is superseded; the heading is now fixed and short, the precision is a sibling, and the reason is the 2026-09-17 audit's P2 (a heading carrying its own qualification reads as a long title, not as a titled card). Keep the four earlier paragraphs readable rather than deleting them.
5. Confirm the `.battery-trend-section > h2` typography selector still matches: the `<h2>` is still the section's direct child and still carries `text-heading`. Do not add a class to the heading and do not change `BATTERY_SECTION_CLASS`.

Retarget every existing check in `test_status_pages.py` that reads the old combined heading. Retarget, do not delete — each was pinning a real property. Specifically:
- a check asserting the heading contains the caption text must become two assertions: the heading contains the short fixed text and NOT the precision; the sibling caption contains the precision.
- assert the ORDER as a relationship: `index(<h2 …>) < index(the caption paragraph) < index(the chart/table body)`, all printed on failure.
- assert in BOTH languages via `prefs.set_request_prefs(lang=...)` that the heading's rendered text equals `i18n.t_lang(BATTERY_SECTION_HEADING_TEMPLATE, lang) % (BATTERY_TREND_WINDOW_DAYS // 30)` — a relationship against the constants, never against a typed literal like `"Batterie · 3 mois"`.
- assert all three `_battery_trend_caption()` branches: drive the function with a usable daily series, with no rows at all, and with a sub-two-day raw series, and assert the rendered caption paragraph carries each branch's own text.

Append a new `EXPECTED_CHECK_COUNT` assignment to `test_status_pages.py` below its existing stack. **Re-derive by running.** Baseline: read it from `29-05-SUMMARY.md` (or from plan 29-02's, whichever last touched this file).

**Mutation-prove twice**, reverting with `git checkout-index -f -- companion/pages/health_page.py` after staging your correct version:
- Mutation A: put the caption back inside the `<h2>` as an inline span. The order/structure assertion must fail.
- Mutation B: hardcode `3` in place of `BATTERY_TREND_WINDOW_DAYS // 30`, then change `BATTERY_TREND_WINDOW_DAYS` to 60. The heading-equals-constant relationship must fail — proving the check pins the relationship and not the string.
Quote both real failure messages verbatim in the SUMMARY.
  </action>
  <acceptance_criteria>
    - The rendered battery-trend card contains exactly one `<h2 class="text-heading">` whose text equals `i18n.t_lang(BATTERY_SECTION_HEADING_TEMPLATE, lang) % (BATTERY_TREND_WINDOW_DAYS // 30)` in both languages, and contains no `<span>` inside that heading.
    - The heading is immediately followed by one `<p class="text-label section-caption">` carrying `_battery_trend_caption()`'s text for the branch in force, and the chart body follows the caption.
    - All three caption branches render their own text; the `caption=None` default still reproduces the reading-count fallback.
    - `BATTERY_SECTION_CLASS` is byte-identical and the `<h2>` is still the section's direct child (so the nested-card-title typography selector still matches).
    - `server/.venv/bin/python companion/test_i18n.py` exits 0.
    - `server/.venv/bin/python companion/test_status_pages.py` exits 0 with its total equal to the re-derived `EXPECTED_CHECK_COUNT`.
    - Two mutation proofs recorded with real failure messages quoted verbatim.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_status_pages.py &amp;&amp; server/.venv/bin/python companion/test_i18n.py</automated>
  </verify>
  <done>État's battery card carries a short fixed heading interpolated from the real window constant, with its precision in a sibling caption in the site's standard composition; all three caption branches still render; the typography selector still matches; both mutation proofs quoted.</done>
</task>

<task type="auto">
  <name>Task 2: the editorial floor on État and Compagnies, with every honesty clause moved rather than cut</name>
  <files>companion/pages/health_page.py, companion/pages/airlines_page.py, companion/i18n_fr/health.py, companion/i18n_fr/airlines.py, companion/test_status_pages.py</files>
  <read_first>
    - `companion/pages/health_page.py` lines 586-620 — `CHECK_IN_CAPTION_OBSERVED`, `CHECK_IN_CAPTION_CADENCE`, `CHECK_IN_CAPTION_CADENCE_FALLBACK`, `CHECK_IN_CAPTION_NOT_PROOF`, `CHECK_IN_CAPTION_EMPTY` — and READ THE COMMENTS BETWEEN THEM. Each one records why it exists; `CHECK_IN_CAPTION_NOT_PROOF`'s comment says in capitals that it is the clause a later editor will trim as noise, and names the threat (T-24-07-A) that trimming it would create.
    - `companion/pages/health_page.py` lines 3910-3975 — `_check_in_section()`'s clause assembly (`clauses = [...]`, the two conditional appends, the unconditional `NOT_PROOF` append, `" ".join(clauses)` into one `<p class="text-label section-caption">`) and `_check_in_key_html()`
    - `companion/pages/health_page.py` lines 3044-3056 and 3112-3130 — the two existing `<details class="readings-disclosure">` instances and their summary labels (`"View %d reading%s"`, `"More details"`), which are the disclosure idiom to reuse
    - `companion/pages/health_page.py` lines 688-700 and 3690-3700 — `_READ_ONLY_NOTE` (25 words) and its emission as `<p class="text-body section-caption">`
    - `companion/pages/airlines_page.py` lines 300-312 (`UPLOAD_PREVIEW_CAPTION_TEXT`), 450-485 (`RESOLVE_CAPTION_TEMPLATE`, `NAME_HINT_TEXT`, `STEP_B_CAPTION`), 540-560 (`MANUAL_DELETE_CAPTION`, `MANUAL_SUPERSEDED_NOTE_TEMPLATE`), 100-110 (`GAP_STRIP_BODY`), and each one's emission site (grep the constant name) so you know which CSS class each renders under
    - `.planning/phases/29-.../29-RESEARCH.md` — the CFG-79 offender table, and its recommendation that `MANUAL_SUPERSEDED_NOTE_TEMPLATE` (a `.lightbox__manual-note`, a status message naming a real conflict state) be treated as EXEMPT rather than shortened
    - `.planning/phases/29-.../29-05-SUMMARY.md` — plan 29-05's counting rule and the shape of its exemption pinning, to be matched here
  </read_first>
  <action>
**A. État's check-in caption: one visible sentence, the rest moved verbatim into a disclosure.**

`_check_in_section()` currently joins three or four clauses into one paragraph that renders at roughly fifty words. Restructure:

1. The visible `<p class="text-label section-caption">` carries `CHECK_IN_CAPTION_OBSERVED` ALONE. Measure it first — if it exceeds twelve words, shorten it too, and record its old → new count.
2. Every other clause the current assembly appends moves, unchanged in wording, into a new `<details class="readings-disclosure">` rendered immediately after the visible caption, reusing the existing disclosure idiom and the existing `"More details"` summary label constant. The clauses keep their conditional logic exactly: `CHECK_IN_CAPTION_EMPTY` only when nothing was observed; `CHECK_IN_CAPTION_CADENCE` with its duration substitution, or `CHECK_IN_CAPTION_CADENCE_FALLBACK`, as today; and `CHECK_IN_CAPTION_NOT_PROOF` unconditionally.
3. `CHECK_IN_CAPTION_NOT_PROOF` is NOT shortened, NOT paraphrased and NOT deleted. Its own comment names the threat that removing it creates: a grid without that sentence is a picture making a claim its own data cannot support. Moving it one tap away is the maximum this plan may do to it, and the comment must be extended to record that CFG-79 moved it into the disclosure and why that is not a weakening — the clause still renders on every request, in the same words, in the same document.
4. Do the same for `CHECK_IN_CAPTION_CADENCE`'s "not necessarily the cadence in force on an earlier day" qualifier: its comment explains it is what stops the caption making a claim about the past from a present-tense file. It moves, it does not shrink.
5. Add a check in `test_status_pages.py` asserting all of this as a relationship: for each of the four clause-combination cases (observed/not-observed × cadence-known/cadence-unknown), every clause that used to render still renders SOMEWHERE in the card, the visible caption contains exactly one of them, and each of the others is inside the disclosure's own markup slice. Failure message prints the case and which clause went missing. This is the check that makes "moved, not cut" an executable claim rather than a promise.

**B. `_READ_ONLY_NOTE` (25 words).** Shorten the visible sentence to at most twelve words naming what the list is. The "each row's Resolve link opens the Airlines page to name that airline (and add artwork, if it needs one)" instruction is genuine reference material — move it into the registry section's existing `<details class="data-card__details">`-adjacent disclosure if one is already on that card, or into a new `readings-disclosure` reusing the `"More details"` label if not. Read the section's markup first and reuse, never invent.

**C. Compagnies' five captions.** Shorten each to at most twelve words in both languages:
- `NAME_HINT_TEXT` (23 words) — keep the instruction ("start typing, pick a suggestion"); the "so this reuses it instead of asking for a new upload" explanation is a reason clause and moves into the resolve flow's own disclosure if one exists, or goes.
- `RESOLVE_CAPTION_TEMPLATE` (15 words) — keep the consequence, drop the conditional framing. It carries a `%s` for the prefix; preserve the placeholder and translate-then-substitute.
- `STEP_B_CAPTION` (15 words) — keep the state and the one action.
- `UPLOAD_PREVIEW_CAPTION_TEXT` (14 words) — keep what the preview is; "The server does the final crop" is a mechanism clause and goes.
- `MANUAL_DELETE_CAPTION` (13 words) — one clause over; trim to twelve.
- `GAP_STRIP_BODY` — measure it; RESEARCH.md's table does not list it, so it may already be under the floor. Record the measurement either way.

**D. Record `MANUAL_SUPERSEDED_NOTE_TEMPLATE` as exempt, with its reason.** It renders as `.lightbox__manual-note`, not `.section-caption`, so Task 3's selector will not match it at all — but write a comment at the constant stating that it is deliberately outside the editorial floor because it is a status message naming a real conflict state (which airline a prefix now resolves to, and that a manual name was superseded), not a description of a control, and that shortening it would cost information the reader needs at exactly the moment it renders. This is the one place in the phase where "long is correct" and it should be argued rather than left to look like an oversight.

For every string changed: record old-count → new-count, and find its French twin with `grep -rn "<exact English literal>" companion/i18n_fr/`. `test_i18n.py` is the gate.

Append a new `EXPECTED_CHECK_COUNT` assignment to `test_status_pages.py`. **Re-derive by running.**

**Mutation-prove** the moved-clause check: delete `CHECK_IN_CAPTION_NOT_PROOF`'s append from the disclosure assembly. The check must fail naming the clause and the case. Revert with `git checkout-index -f -- companion/pages/health_page.py`; quote the real failure verbatim.
  </action>
  <acceptance_criteria>
    - État's visible check-in caption is at most twelve words in both languages and contains exactly one of the five clause constants.
    - For all four clause-combination cases, every clause that rendered before this task still renders in the card, with the non-visible ones inside the disclosure's markup slice; the check names the case and the clause on failure.
    - `CHECK_IN_CAPTION_NOT_PROOF` and `CHECK_IN_CAPTION_CADENCE` are byte-identical in wording (moved, not edited), and both constants' comments are extended to record the move and why it is not a weakening.
    - `_READ_ONLY_NOTE`'s visible text is at most twelve words and its instruction text is PRESENT in the render inside a disclosure.
    - Each of `NAME_HINT_TEXT`, `RESOLVE_CAPTION_TEMPLATE`, `STEP_B_CAPTION`, `UPLOAD_PREVIEW_CAPTION_TEXT`, `MANUAL_DELETE_CAPTION` renders at most twelve words in both languages, with placeholders preserved.
    - `MANUAL_SUPERSEDED_NOTE_TEMPLATE` is unchanged and carries a new comment arguing its exemption.
    - `server/.venv/bin/python companion/test_i18n.py` exits 0.
    - `server/.venv/bin/python companion/test_status_pages.py` and `companion/test_view_pages.py` exit 0 with re-derived counts.
    - One mutation proof recorded with its real failure message quoted verbatim, plus the old → new count table and the French module for each twin.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_status_pages.py &amp;&amp; server/.venv/bin/python companion/test_view_pages.py &amp;&amp; server/.venv/bin/python companion/test_i18n.py</automated>
  </verify>
  <done>État's check-in caption says one thing with every honesty clause one tap away and provably still rendering; `_READ_ONLY_NOTE`'s instruction is in a disclosure; Compagnies' five captions are at or under twelve words in both languages; the one legitimately-long status message is exempt with its reason written down.</done>
</task>

<task type="auto">
  <name>Task 3: the site-wide floor — every authenticated route, both languages, over a real server, mutation-proven</name>
  <files>companion/test_companion_app.py</files>
  <read_first>
    - `companion/test_companion_app.py` lines 1120-1200 (the raw-request helper and the `Cookie` header threading, including the comment explaining why `HTTPCookieProcessor` is not used here), 1270-1330 (the real `ThreadingHTTPServer` harness), 1450-1475 (`_login()` and its `Set-Cookie` capture)
    - `companion/auth.py` — `UI_LANG_COOKIE_NAME` (grep it) and the session cookie's own name, so the language can be set on the same request as the session
    - `companion/app.py` lines 1378-1390 and 1500-1510 — `_lang_from_request()` reading `cookies.get(auth.UI_LANG_COOKIE_NAME)` and `prefs.set_request_prefs(lang=self._lang_from_request())`. This is the mechanism the check must drive; do not invent a second one.
    - `companion/layout.py` lines 71-78 — `HOME_ROUTE`, `DISPLAY_ROUTE`, `FLIGHTS_ROUTE`, `AIRLINES_ROUTE`, `HEALTH_ROUTE`, `DEVICE_ROUTE`
    - `companion/test_browser_ux.py` line 994 — `VIEW_TRANSITION_ROUTES`, the existing six-route tuple. Do NOT import from a test module; instead build the route list from `layout`'s own six constants and ADD an assertion that your list equals `test_browser_ux.VIEW_TRANSITION_ROUTES`'s literal membership read as text from that file, so the two enumerations cannot drift — the same reasoning that keeps `_nav_links()` the one source of truth for the nav set. State how you did it.
    - `companion/pages/config_page.py` — `ASPECT_CAPTION_EXEMPTIONS` as plan 29-05 created it; import and reuse it, never re-list its four strings
    - `.planning/phases/29-.../29-05-SUMMARY.md` — plan 29-05's exact counting rule and its helper's location
    - `companion/layout.py` lines 3671, 3776, 3904, 3932, 3953 — every `section-caption` emission in `layout.py`, including `empty_state()`'s `.empty-state__body text-label section-caption`, so the exemption reasoning accounts for compositions the page modules do not control
  </read_first>
  <action>
Add ONE check to `companion/test_companion_app.py` — the app-level harness, because "every authenticated route" is an app-level property and this is the only harness with a real server plus `_login()`.

1. Start the existing server harness, log in, capture the session cookie.
2. Build the route list from `layout`'s own six route constants. Assert it equals the six-route membership `companion/test_browser_ux.py` declares — read that file as text and extract the tuple's string literals, so a seventh route added to one enumeration and not the other fails here. Print both lists on mismatch.
3. For each route × each of `("en", "fr")`: issue an authenticated GET with `Cookie: <session>; <auth.UI_LANG_COOKIE_NAME>=<lang>`, assert a 200, and confirm the served document carries the expected `<html lang="…">` — otherwise the French pass could be silently measuring English and the check would be half vacuous.
4. Extract every element whose `class` attribute contains the token `section-caption`, with its full text content. Unescape HTML entities before counting. Use plan 29-05's counting rule and helper (see `<environment>` above for the duplicate-or-lift decision and the requirement to pin the two equal).
5. Skip a caption when its text matches, in the language being measured, a member of a single pinned exemption list:
   ```
   CAPTION_FLOOR_EXEMPTIONS = config_page.ASPECT_CAPTION_EXEMPTIONS + (<any further entry you must add>)
   ```
   Every entry beyond `ASPECT_CAPTION_EXEMPTIONS` must be argued in a comment beside it, naming the element it belongs to and why it is not "a caption under a card title" in CFG-79's sense. The only candidate this plan anticipates is `empty_state()`'s compact body, which composes `section-caption` onto `.empty-state__body` and is an empty-state sentence rather than a control description — include it ONLY if a real render actually trips on it, and if you do, exempt it by its CLASS composition (`empty-state__body`) rather than by its text, so the exemption cannot accidentally cover a future caption that happens to share a word. Prefer shortening over exempting in every ambiguous case, and record every judgement call you made.
6. Assert every remaining caption is at most 12 words. The failure message must print route, language, word count and the full text — fixable from the message alone.
7. Assert three anti-vacuity properties, each printed on failure:
   - a per-route minimum caption count (derive by running, pin a little below the observed figure, state both);
   - a site-wide total caption count minimum;
   - that the exemption skip count is exactly `len(ASPECT_CAPTION_EXEMPTIONS)` per language across the whole site plus whatever class-based exemptions you justified — so the exemption list cannot silently swallow a caption, and each exemption is proven reachable.
8. Assert the once-per-page apply-timing rule site-wide, the same relationship plan 29-05 asserted for two routes: on each route, at most one element's text contains an apply-timing sentence (matched against `layout`'s and `frame_state`'s own template constants, read from those modules — never a typed English phrase), and when present it is inside the Frame strip's markup slice. Print every match with 80 characters of context on failure.

Append a new `EXPECTED_CHECK_COUNT` assignment to `test_companion_app.py` below its long existing stack, following the file's own convention (a new assignment with a comment naming the plan and the delta). **Re-derive by running** — this file's total is large and arithmetic on it is exactly what the convention forbids.

**Mutation-prove four ways.** Stage your correct version first; revert each with `git checkout-index -f -- <path>`; quote every real failure message verbatim in the SUMMARY.
- Mutation A (the requirement's own named proof — a deliberately long caption): append a thirteen-word second sentence to one non-exempt caption on a route this plan did NOT edit (pick one on `/` or `/airlines`), so the mutation also proves the check reaches routes beyond the ones this phase touched. The check must fail naming route, language, count and text.
- Mutation B (French is really measured): change one FRENCH catalogue value to a long sentence while leaving its English twin short. The check must fail on the `fr` pass only — this is the proof that step 3's `<html lang>` assertion and the cookie threading actually work, and that the French pass is not silently re-measuring English.
- Mutation C (the exemption cannot grow silently): append an unrelated string to `CAPTION_FLOOR_EXEMPTIONS`. The skip-count assertion must fail.
- Mutation D (the once-per-page rule): add a second apply-timing sentence under a card on any route. The relationship assertion must fail with both matches printed.

**Record the control result explicitly:** with correct sources, the check PASSES against Display's four Aspect captions exactly as they are today — including `CALENDAR_URL_HINT` at 22 words. State that in the SUMMARY. This is the quality gate this phase is held to, and a check that failed against Aspect's current copy would be the wrong check.
  </action>
  <acceptance_criteria>
    - The check issues authenticated 200s for all six routes in both languages and asserts the served `<html lang>` matches the requested language on every one of the twelve requests.
    - Every non-exempt `section-caption` element across all twelve renders is at most 12 words.
    - `CAPTION_FLOOR_EXEMPTIONS` begins with `config_page.ASPECT_CAPTION_EXEMPTIONS` (imported, not re-listed) and every additional entry carries its own argued comment.
    - The exemption skip count equals the exemption list's own length per language, proving every exemption reachable; per-route and site-wide caption-count minimums are asserted and printed on failure.
    - The route list is asserted equal to `test_browser_ux.py`'s declared six-route membership, read as text.
    - At most one apply-timing element per route, and when present it is inside the Frame strip's slice.
    - `server/.venv/bin/python companion/test_companion_app.py` exits 0 with its total equal to the re-derived `EXPECTED_CHECK_COUNT`.
    - Four mutation proofs recorded with real failure messages quoted verbatim, including the French-only one.
    - An explicit control statement that the check passes against Display's four Aspect captions unchanged.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_companion_app.py &amp;&amp; server/.venv/bin/python companion/test_config_page.py &amp;&amp; server/.venv/bin/python companion/test_status_pages.py &amp;&amp; server/.venv/bin/python companion/test_view_pages.py &amp;&amp; server/.venv/bin/python companion/test_i18n.py</automated>
    <human-check>Open all six pages on a 390 px phone, in French then in English, light theme then dark. Confirm no card carries more than one sentence of description under its title, that the "applies at the next wake" sentence appears once per page (in the Frame strip) and nowhere else, and that État's battery card reads "Batterie · 3 mois" with "moyenne quotidienne" below it rather than inside the title. On État, open the check-in card's disclosure and confirm the "un jour sans enregistrement n'est pas la preuve…" sentence is still there, word for word. Report anything that now reads as too terse to act on — a caption cut past usefulness is a regression, not a win.</human-check>
  </verify>
  <done>One HTTP-level check measures every caption on all six authenticated routes in both languages, with a single pinned exemption list whose only unargued members are Phase 30's four, three anti-vacuity floors, the site-wide once-per-page apply-timing relationship, four mutation proofs including a French-only one, and an explicit control result against Aspect's current copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → the six authenticated routes | Unchanged: this plan edits copy constants, one card's heading/caption structure, and one clause assembly. No new input, no new parameter, no new route, no handler change. |
| test harness → a real local `ThreadingHTTPServer` | Task 3 starts the app for real, logs in, and threads a session cookie plus a language cookie. Test-only, bound to 127.0.0.1 by the existing harness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-06-01 | Repudiation | État's check-in honesty clauses | high | mitigate | `CHECK_IN_CAPTION_NOT_PROOF` exists because the record cannot distinguish a wake the frame missed from a log range this server lost; its own comment names threat T-24-07-A. A grid without it is a picture asserting more than its data supports — the same class of defect the battery honesty rule guards against. Mitigated by MOVING rather than cutting: the clause is byte-identical in wording, still renders on every request, and Task 2 adds a four-case check asserting every clause that rendered before still renders, with the non-visible ones located inside the disclosure's slice. `CHECK_IN_CAPTION_CADENCE`'s past-tense qualifier is treated identically. |
| T-29-06-02 | Repudiation | État's battery-trend heading honesty | medium | mitigate | A fixed "3 months" heading over a chart that is actually plotting the sub-two-day fallback series would be a claim the data does not support. Mitigated because `_battery_trend_caption()` is untouched and still chooses between the 3-month framing and the real reading count, and Task 1 asserts all three branches render their own caption text. The heading's "3" is interpolated from `BATTERY_TREND_WINDOW_DAYS`, so it cannot drift from the chart's own window; mutation B proves that binding. |
| T-29-06-03 | Information Disclosure | over-shortened captions hiding a fact the reader needs | medium | mitigate | An editorial pass can quietly remove the one sentence that made a control safe to use. Mitigated by the "move into an existing disclosure, do not delete" rule applied to every clause carrying reference material (`NOTIFICATIONS_URL_HINT` in plan 29-05, `_READ_ONLY_NOTE` and the check-in clauses here), by asserting the moved text is PRESENT in the render, and by the human follow-up explicitly asking the developer to report anything now too terse to act on. |
| T-29-06-04 | Tampering | the floor check's own exemption list | high | mitigate | An exemption list is the obvious place a future caption hides from the floor. `CAPTION_FLOOR_EXEMPTIONS` imports Phase 30's four from `config_page` rather than re-listing them (one home, emptied in one place), every additional entry needs an argued comment, class-based exemptions are keyed on class composition rather than on text, and the skip count is asserted equal to the list's own length — so an entry that matches nothing fails, and a caption that silently joins the list fails. Mutation C proves it. |
| T-29-06-05 | Denial of Service | the test harness's real server | low | accept | Task 3 starts `companion/app.py` on a loopback port inside the existing harness, which the file already does for other checks and already tears down. No production surface is involved. Accepted with no new control. |
| T-29-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install occurs. No dependency manifest or lockfile is touched; `companion/` is stdlib-only by contract and no file is added under `companion/static/`. The legitimacy gate does not apply and no audit table is required. |
</threat_model>

<artifacts>
## Artifacts this phase produces (this plan's share)

**New symbols (`companion/pages/health_page.py`):**
- `BATTERY_SECTION_HEADING_TEMPLATE` = `"Battery · %d months"`, replacing `BATTERY_SECTION_HEADING`, interpolated with `BATTERY_TREND_WINDOW_DAYS // 30`; French twin `"Batterie · %d mois"`
- a body constant (or constants) for the check-in clauses' new disclosure home, if the existing summary-label constants do not suffice — reusing `"More details"` where possible
- a body constant for `_READ_ONLY_NOTE`'s moved instruction text, plus its French twin

**Changed markup:**
- `_battery_trend_section_html()` — `<h2>` with an inline caption span becomes `<h2>` plus a sibling `<p class="text-label section-caption">`
- `_check_in_section()` — one joined multi-clause caption becomes one visible sentence plus a `<details class="readings-disclosure">` holding the remaining clauses, conditional logic preserved
- the registry section — `_READ_ONLY_NOTE`'s instruction moves into a disclosure

**Changed copy constants (English plus each one's French twin, in whichever `i18n_fr` module owns it):**
- `health_page`: `CHECK_IN_CAPTION_OBSERVED` (if over the floor), `_READ_ONLY_NOTE`
- `airlines_page`: `NAME_HINT_TEXT`, `RESOLVE_CAPTION_TEMPLATE`, `STEP_B_CAPTION`, `UPLOAD_PREVIEW_CAPTION_TEXT`, `MANUAL_DELETE_CAPTION`, and `GAP_STRIP_BODY` if measurement puts it over

**Unchanged in wording but relocated (and asserted so):** `CHECK_IN_CAPTION_CADENCE`, `CHECK_IN_CAPTION_CADENCE_FALLBACK`, `CHECK_IN_CAPTION_NOT_PROOF`, `CHECK_IN_CAPTION_EMPTY`

**Argued exemption, unchanged:** `airlines_page.MANUAL_SUPERSEDED_NOTE_TEMPLATE`, with a new comment stating why long is correct there

**New symbols (`companion/test_companion_app.py`):**
- `CAPTION_FLOOR_EXEMPTIONS` — built from `config_page.ASPECT_CAPTION_EXEMPTIONS` plus any argued additions
- the site-wide caption-floor check, and one new `EXPECTED_CHECK_COUNT` assignment

**Added checks:** the battery heading/caption structure and three-branch check plus the heading-equals-constant relationship (`test_status_pages.py`), the four-case moved-clause check (`test_status_pages.py`), the site-wide floor check with three anti-vacuity floors and the site-wide once-per-page apply-timing relationship (`test_companion_app.py`).
</artifacts>

<verification>
- `server/.venv/bin/python companion/test_companion_app.py` — green, re-derived count
- `server/.venv/bin/python companion/test_status_pages.py` — green, re-derived count
- `server/.venv/bin/python companion/test_view_pages.py` — green, re-derived count
- `server/.venv/bin/python companion/test_config_page.py` — green; plan 29-05's floor check must still pass unchanged
- `server/.venv/bin/python companion/test_i18n.py` — green, re-derived count
- `scripts/run-all-tests.sh` with `PYTHON=server/.venv/bin/python` — full suite; report `companion/test_browser_ux.py` as SKIPPED, never as a pass, and name the sandbox baseline failures honestly if any reappear
- `git diff --stat companion/layout.py companion/frame_state.py companion/static/` — empty
</verification>

<success_criteria>
- État's battery heading is short and fixed, interpolated from the real window constant, with its precision in a sibling caption
- Every honesty clause État's check-in card carried today still renders, word for word, one tap away — proven across all four clause-combination cases
- Compagnies' five captions and État's read-only note are at or under twelve words in both languages
- The one legitimately-long status message is exempt with its reason written at the constant
- One HTTP-level check measures every caption on all six authenticated routes in both languages and fails with route, language, count and text
- The exemption list has exactly Phase 30's four plus any argued addition, every one proven reachable
- The apply-timing sentence appears at most once per route, in the Frame strip
- Seven mutation proofs across the three tasks, real failure messages quoted verbatim, including a French-only one
- The control result — the check passes against Aspect's current, un-shortened copy — is stated explicitly
</success_criteria>

<output>
Create `.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-06-SUMMARY.md` when done, including: the old-count → new-count table for every constant touched with the `i18n_fr` module each French twin lived in, the harness before/after table with every count re-derived by running, all seven mutation proofs with real failure messages quoted verbatim, the explicit control statement that the site-wide floor passes against Aspect's current copy, every exemption-list judgement call recorded with its argument, the decision taken on plan 29-05's counting helper (lifted or duplicated-and-pinned), and the measured per-route caption counts behind the anti-vacuity floors.
</output>
