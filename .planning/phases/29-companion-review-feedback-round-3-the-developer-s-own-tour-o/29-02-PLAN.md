---
phase: 29-companion-review-feedback-round-3-the-developer-s-own-tour-o
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/static/style.css
  - companion/test_view_pages.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-82]

must_haves:
  truths:
    - "On Compagnies the filter bar and the known-airline gallery are the first two things under the page title — nothing else renders between the page header and the filter bar (CFG-82)."
    - "The unidentified-prefix strip renders BELOW the gallery, still announced by its own heading and body, with its overflow behaviour unchanged (CFG-82)."
    - "The mobile tab bar renders the word Compagnies whole at 360 px and at 390 px — the label's own scrollWidth fits inside its resolved pill box (CFG-82)."
    - "The tab cell keeps its 78x56 px tap area: only the inner pill's horizontal margin changes, never the cell (CFG-82)."
    - "The page renders no filter bar and no gallery chrome when there is nothing to filter — the existing no-data gate survives the reorder unchanged."
  artifacts:
    - "companion/pages/airlines_page.py with render()'s return expression reordered: page_header, filter, gallery, gap strip, lightbox, resolve"
    - "companion/static/style.css with .tab-bar__pill's horizontal margin component reduced to calc(var(--space-xs) / 2)"
    - "companion/test_view_pages.py with a section-order relationship check (index-of comparisons, never a literal offset)"
    - "companion/test_status_pages.py with a tab-label-fits relationship check computed from the stylesheet's own resolved values"
  key_links:
    - "render()'s existing no-chrome gate (total = len(gap_shown) + len(pairs); filter_html and lightbox_html both gated on `pairs or gap_shown`) must be preserved by the reorder, not rewritten."
    - ".tab-bar__pill is the INNER wrapper; .tab-bar__link is the 78x56 cell. The margin lives on the pill, so shrinking it gives the label room without touching the tap area — this is why the audit's fix is the pill's margin and not the cell's padding."
    - "CFG-81 (plan 29-01) already removed the toggle that used to be render()'s first term, so 'any remaining editing affordance' has nothing left to relocate — RESEARCH.md assumption A2, to be confirmed by inspection in Task 1 and recorded either way."
---

<objective>
Compagnies reads gallery-first, and its mobile tab label stops truncating. `render()`'s section order moves the filter bar and the known-airline gallery directly under the page title, and pushes the unidentified-prefix strip to a secondary position below the gallery. Separately, `.tab-bar__pill`'s horizontal margin drops from 8 px to 2 px so "Compagnies" renders whole at 360 px and 390 px without the tab cell's tap area shrinking.

Purpose: the 2026-09-17 audit's P2 ("Compagnies retarde sa galerie principale" — on mobile the title, two intro texts, the editing action, the unidentified section and the filter all precede the gallery) and its P1 ("`Compagnies` tronqué dans la navigation mobile" — the pill's horizontal inset left 62 px for a word needing 65).

Output: a Compagnies page whose main content arrives first, a tab bar that never ellipses a destination, and two relationship checks — one on section ORDER, one on the label's resolved fit — neither of which asserts an endpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-CONTEXT.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-RESEARCH.md
@.planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md
@.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-01-SUMMARY.md
</context>

<environment>
**Python interpreter for every harness run: `server/.venv/bin/python`.** Bare `python3` in this worktree has no Pillow and every `companion/test_*.py` fails at import.

**playwright is NOT installed.** `companion/test_browser_ux.py` SKIPs and exits 0. The audit's own P1 evidence was a real-browser `scrollWidth` measurement, which cannot be reproduced here. Task 2 therefore proves the fit ARITHMETICALLY from the stylesheet's own declared values and the label's own type metrics, and the real-browser confirmation is recorded as a human follow-up — exactly the shape quick tasks 260921-n2n and 260921-p2w already used for their own unrunnable confirmations.

**Do NOT pull or merge the uncommitted `style.css` diff sitting in the MAIN CHECKOUT.** CONTEXT.md CFG-82 forbids it explicitly. Re-derive the same one-value change in THIS worktree's file.

**Drift already ruled out:** RESEARCH.md ran `git log -p -L` on the `.tab-bar__pill` rule and confirmed it has been byte-identical since Phase 22 (commit `8a3520c`, 2026-09-13) — before the audit, not after. Phase 28's only tab-bar-adjacent change was the nav toggle's icon, which does not touch this rule's box model. Do NOT spend a task re-measuring the tab bar from scratch (RESEARCH.md Pitfall 3).
</environment>

<tasks>

<task type="auto">
  <name>Task 1: reorder render() so the filter and the gallery come first, and the unidentified strip second</name>
  <files>companion/pages/airlines_page.py, companion/test_view_pages.py</files>
  <read_first>
    - `companion/pages/airlines_page.py` lines 2389-2440 (`render()`'s docstring), 2460-2545 (the gap-strip build, the `total` computation, the lightbox gate), 2545-2590 (`summary_html`, `filter_html`, and the return expression as plan 29-01 left it)
    - `companion/pages/airlines_page.py` lines 100-110 (`GAP_STRIP_HEADING`, `GAP_STRIP_BODY`) and 1505-1555 (`_gap_strip_html()`)
    - `companion/pages/airlines_page.py` lines 1700-1760 (`_filter_bar_html(total, summary_html)` and `_manual_summary_html()`'s home inside it)
    - `.planning/phases/29-.../29-RESEARCH.md` section "CFG-82 — Compagnies gallery-first + tab label", including assumption A2
    - `.planning/phases/29-.../29-01-SUMMARY.md` — to confirm what plan 29-01 actually removed from the return expression
  </read_first>
  <action>
In `companion/pages/airlines_page.py`'s `render()`, change the return expression's term order to exactly:

```
page_header
+ filter_html
+ gallery_grid
+ gap_strip_html
+ lightbox_html
+ resolve_html
```

Rules that bind this edit:

1. **Move terms only.** Do not rewrite the builders, do not change any builder's arguments, and do not touch the existing no-chrome gate: `total = len(gap_shown) + len(pairs)` stays where it is, `filter_html = _filter_bar_html(total, summary_html) if (pairs or gap_shown) else ""` stays byte-identical, and `lightbox_html`'s `if (pairs or gap_shown)` gate stays byte-identical. The reorder must be provably a permutation of the same strings.
2. **The unidentified strip keeps its own announcement.** `_gap_strip_html()` already emits a `<section>` with its own `GAP_STRIP_HEADING` heading and `GAP_STRIP_BODY` body — that IS the "clearly announced secondary section" CFG-82 asks for. Do NOT wrap it in a new `<details>` disclosure: that would require inventing a default open/closed state the locked decision does not ask for, and CONTEXT.md leaves the framing to the planner, whose call this is. Record that call in the docstring.
3. **Resolve A2 by inspection and record the answer either way.** CFG-82's text says "unidentified prefixes and any remaining editing affordance move to a clearly announced secondary section below". After plan 29-01 there is no page-wide editing affordance left. Grep `render()`'s remaining terms for any editing control: the manual-resolutions summary button lives INSIDE the filter bar (it is a filter control, per the module's own 22-11 comment) and the resolve flow's controls render only under `?resolve=`, i.e. not on the default view at all. Write a docstring paragraph stating which of those two readings you adopted and why, so a reviewer can disagree with a recorded decision rather than guess at an omission. If you find any OTHER editing control on the default view that plan 29-01 did not remove, move it into the gap strip's section and say so — do not silently leave it above the gallery.
4. **Supersede the old order in writing, in place.** `render()`'s docstring and the two comment paragraphs immediately above the return expression currently record two prior orderings (Phase 14's D-11 filter-then-summary-then-gap-then-grid, and Phase 19's D-21 gap-strip-first). Add a third paragraph in the same voice naming CFG-82 and this plan, stating that D-21's "gap strip first" is SUPERSEDED because it put a diagnostic list ahead of the page's main content on a phone, and keeping the two earlier paragraphs readable rather than deleting them — this file's established SUPERSEDED-in-place convention.

In `companion/test_view_pages.py`, add ONE check that asserts the ORDER as a set of relationships, never as offsets:

- Render Compagnies with a fixture that produces BOTH at least one gap card and at least one gallery card (so every term is non-empty and the order is actually observable).
- Compute `rendered.index(...)` for one stable literal per section: the page title's own `<h1 class="page-title"` (or whatever `layout.page_header()` actually emits — read it, do not assume), the filter bar's `class="filter-bar"`, the gallery grid's own wrapper class, `GAP_STRIP_HEADING`'s escaped text, and the dialog's `<dialog class="lightbox`.
- Assert the chain `title < filter < gallery < gapstrip < lightbox`, with a failure message that prints all five indices so a regression says which pair inverted.
- Assert each literal occurs exactly once, so an index is unambiguous.
- Add a second assertion in the same check: with a fixture that has gap cards but the gallery's own `pairs` empty, and with a fixture that has neither, the existing no-chrome gate still holds (no `class="filter-bar"` when there is nothing to filter). This is the part that proves the reorder did not rewrite the gate.

Append a new `EXPECTED_CHECK_COUNT` assignment below the file's existing stack with a comment naming this plan and the delta. **Re-derive the value by running the harness**, never by arithmetic. Baseline after plan 29-01: read it from `29-01-SUMMARY.md`'s own table.

**Mutation-prove the order check**: swap two adjacent terms in `render()`'s return expression, run the harness, quote the real failure line verbatim into the SUMMARY, then `git checkout-index -f -- companion/pages/airlines_page.py`. Stage your correct version first.
  </action>
  <acceptance_criteria>
    - In a render with both gap cards and gallery cards: `index(filter-bar) < index(gallery wrapper) < index(GAP_STRIP_HEADING) < index('<dialog class="lightbox')`, and `index(page title) < index(filter-bar)`.
    - Each of those five literals occurs exactly once in that render.
    - A render with no gap cards and no gallery pairs contains zero occurrences of `class="filter-bar"`.
    - `git diff companion/pages/airlines_page.py` shows the return expression's terms permuted with no change to `total`, to `filter_html`'s gate expression, or to `lightbox_html`'s gate expression — verify by reading the diff, and state in the SUMMARY that it is a permutation.
    - `render()`'s docstring carries a new paragraph naming CFG-82, marking Phase 19's D-21 ordering SUPERSEDED, and recording the A2 decision.
    - One mutation proof recorded with its real failure message quoted verbatim.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_view_pages.py</automated>
  </verify>
  <done>Compagnies renders filter, gallery, then the unidentified strip; the no-chrome gate is provably untouched; one order-as-relationship check is green and mutation-proven; `EXPECTED_CHECK_COUNT` re-derived by running.</done>
</task>

<task type="auto">
  <name>Task 2: the tab bar's Compagnies label renders whole at 360 px and 390 px</name>
  <files>companion/static/style.css, companion/test_status_pages.py</files>
  <read_first>
    - `companion/static/style.css` lines 10390-10470 — the `.tab-bar`, `.tab-bar__link` (the 78x56 cell), `.tab-bar__pill` (the inner wrapper whose margin changes), `.tab-bar__label` (11px regular, sentence case) and the active/hover tint rules, including the comment at 10402-10404 that states WHY the tint is on the pill and not the cell
    - `companion/static/style.css` — the `--space-xs` / `--space-sm` token declarations (grep `--space-xs:` in the `:root` block) so the resolved pixel values are read from the file, never assumed
    - `companion/layout.py` lines 2198-2317 (`_tab_bar_html()` / the tab-bar renderer) — the cell structure and where the label text comes from; confirm the label is fed by `_nav_links()`/`NAV_GROUPS`, the one source of truth
    - `.planning/ui-reviews/2026-09-17-companion-ui-ux-audit.md` "P1 — Corrigé : `Compagnies` tronqué dans la navigation mobile" — the audit's own numbers: 62 px available for a word needing 65 px
    - `.claude/skills/sketch-findings-skypane/SKILL.md` — the "Navigation" entry (three renderings, the 78x56 cell, `_nav_links()` as the single source of truth) and the "review-feedback discipline" paragraph (measure the RESOLVED box in its own container)
  </read_first>
  <action>
In `companion/static/style.css`, in the `.tab-bar__pill` rule block only, change

`margin: var(--space-xs) var(--space-sm);`

to

`margin: var(--space-xs) calc(var(--space-xs) / 2);`

— vertical component unchanged at `--space-xs`, horizontal component from `--space-sm` (8 px) to 2 px. Add a comment above the declaration recording: the audit date (2026-09-17), the measured cause (the pill's 8 px horizontal margin on each side left 62 px of a 78 px cell for a label needing 65 px), that the margin is on the INNER pill so the cell's 78x56 tap area is unaffected, and that the vertical component is deliberately unchanged. Change nothing else in the tab-bar rules — not `.tab-bar__link`'s `height: 56px`, not the cell's width basis, not `.tab-bar__label`'s size.

Then add ONE check to `companion/test_status_pages.py` that proves the fit as a RELATIONSHIP computed from the stylesheet's own source, in the same style as quick task 260921-n2n Task 5's CFG-70 floor check (read that check for the idiom):

1. Parse `:root`'s `--space-xs` and `--space-sm` declarations out of `style.css` to get their real pixel integers — do not hardcode 4 and 8.
2. Parse `.tab-bar__pill`'s `margin` declaration and resolve its horizontal component through the same token map, including the `calc(var(--space-xs) / 2)` form. Parse `.tab-bar__link`'s own width basis and `height` from the same file.
3. Assert the available label width — cell width minus twice the pill's resolved horizontal margin — is at least the longest tab label's required width, where the requirement is derived from the longest label string in `layout.NAV_GROUPS` (read it, never type "Compagnies") at `.tab-bar__label`'s own parsed `font-size`, using a stated, conservative per-character advance for a sentence-case sans label. State the advance factor and its provenance in the check's own comment; if the audit's 65 px for "Compagnies" at 11 px gives a different factor than your formula, use the audit's measured number as the floor and say so — a measured number beats a modelled one.
4. Assert independently that the cell's own resolved box is unchanged by this edit: `.tab-bar__link`'s height parses to 56 and its width basis parses to the same value it had before this task. This is the half of the check that stops a future "fix" from buying label room out of the tap area.
5. Failure messages must print both sides of each comparison.

Append a new `EXPECTED_CHECK_COUNT` assignment below `test_status_pages.py`'s existing stack with a comment naming this plan. **Re-derive by running.** Baseline after plan 29-01: read it from `29-01-SUMMARY.md`.

**Mutation-prove it twice**, reverting with `git checkout-index -f -- companion/static/style.css` each time (stage your correct version first):
- Mutation A: restore the horizontal margin to `var(--space-sm)`. The fit assertion must fail and its message must print 62-ish available against the label's requirement.
- Mutation B: reduce `.tab-bar__link`'s height (or its width basis) by a few pixels. The tap-area assertion must fail independently of the fit assertion — proving the two halves are not one assertion wearing two hats.
Quote both real failure messages verbatim in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c 'var(--space-xs) var(--space-sm)' companion/static/style.css` inside the `.tab-bar__pill` block returns 0; the block's `margin` declaration resolves its horizontal component to 2 px through the file's own tokens.
    - `.tab-bar__link`'s parsed height is 56 and its parsed width basis is byte-identical to its pre-task value (state both in the SUMMARY).
    - The new check's computed available label width is >= the audit's measured 65 px requirement for the longest `NAV_GROUPS` label.
    - `server/.venv/bin/python companion/test_status_pages.py` exits 0 with its total equal to the re-derived `EXPECTED_CHECK_COUNT`.
    - Two mutation proofs recorded with real failure messages quoted verbatim, and `git status --porcelain companion/static/style.css` clean after each revert.
    - `git diff companion/static/style.css` touches exactly one declaration plus its new comment — no other tab-bar rule changes.
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python companion/test_status_pages.py &amp;&amp; server/.venv/bin/python companion/test_view_pages.py</automated>
  </verify>
  <done>The pill's horizontal margin is 2 px, derived from the file's own tokens; the longest tab label provably fits its resolved box; the 78x56 cell is provably unchanged; both halves mutation-proven independently. The real-browser `scrollWidth` confirmation at 360 px and 390 px is a human follow-up, recorded as such and NOT claimed as verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/airlines` GET | Unchanged by this plan: the reorder permutes already-built strings and introduces no new input, no new parameter and no new route |
| stylesheet → every page's tab bar | A shared nav rule changes; the blast radius is every sub-960px page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02-01 | Denial of Service | `.tab-bar__pill` margin change | medium | mitigate | Shrinking an inner margin to buy label room is exactly how a tap target silently shrinks — a nav that is hard to hit is a usability denial on the one nav every phone page carries. Task 2's check asserts `.tab-bar__link`'s resolved 78x56 cell is UNCHANGED as an assertion independent of the fit assertion, and mutation B proves the two are separable. The margin moves on the pill, which carries only the tint, never the pointer target. |
| T-29-02-02 | Information Disclosure | the unidentified-prefix strip's new position | low | accept | The strip lists observed callsign prefixes the registry cannot name. Moving it below the gallery changes only its position on an already session-gated page; the same data, the same audience. Accepted. |
| T-29-02-03 | Tampering | `render()`'s no-chrome gate | medium | mitigate | A reorder that accidentally rewrites `filter_html`'s or `lightbox_html`'s gate could render filter chrome over an empty registry or emit a dialog with no trigger. Task 1 asserts both empty-fixture cases explicitly and requires the diff to be read and reported as a permutation. |
| T-29-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install occurs. No dependency manifest or lockfile is touched; `companion/` is stdlib-only by contract. The legitimacy gate does not apply and no audit table is required. |
</threat_model>

<artifacts>
## Artifacts this phase produces (this plan's share)

**Changed:**
- `airlines_page.render()` — return-expression term order becomes page_header, filter, gallery, gap strip, lightbox, resolve; docstring gains a SUPERSEDED paragraph for Phase 19's D-21 ordering and the recorded A2 decision
- CSS rule `.tab-bar__pill` — horizontal `margin` component becomes `calc(var(--space-xs) / 2)`

**Added:**
- `companion/test_view_pages.py`: one section-order relationship check (five indices chained, plus the two empty-fixture no-chrome assertions); one new `EXPECTED_CHECK_COUNT` assignment
- `companion/test_status_pages.py`: one tab-label-fits relationship check with an independent tap-area-unchanged assertion, both resolved from `style.css`'s own tokens; one new `EXPECTED_CHECK_COUNT` assignment

**Not produced (recorded so a reader does not look for it):** no new CSS class, no new custom property, no new `<details>` wrapper for the gap strip, no change to `layout.py`.
</artifacts>

<verification>
- `server/.venv/bin/python companion/test_view_pages.py` — green, re-derived count
- `server/.venv/bin/python companion/test_status_pages.py` — green, re-derived count
- `server/.venv/bin/python companion/test_config_page.py` — 266/266 unchanged
- `server/.venv/bin/python companion/test_i18n.py` — 24/24 (no copy changed in this plan; a delta here means something leaked)
- `scripts/run-all-tests.sh` with `PYTHON=server/.venv/bin/python` — full suite; report `test_browser_ux.py` as SKIPPED, never as a pass
- `git diff companion/static/style.css` — exactly one declaration plus its comment
</verification>

<success_criteria>
- Filter and gallery are the first content under Compagnies' title
- The unidentified-prefix strip renders below the gallery with its heading and body intact
- The no-chrome-with-no-data gate is provably unchanged
- `.tab-bar__pill`'s horizontal margin is 2 px, resolved from the file's own tokens
- The longest `NAV_GROUPS` label provably fits the resolved pill box; the 78x56 cell provably unchanged
- Three mutation proofs total (one order, two tab-bar), real failure messages quoted verbatim
</success_criteria>

<human_follow_ups>
Not blocking; no playwright in this worktree, so these cannot be machine-verified here:
1. On a 360 px and a 390 px viewport, in French and in English, confirm the tab bar renders `Compagnies` whole with no ellipsis, and that each tab is still comfortable to hit. In the console: `[...document.querySelectorAll('.tab-bar__label')].map(e => [e.textContent, e.scrollWidth, e.clientWidth])` — no label may have `scrollWidth > clientWidth`.
2. On Compagnies at 390 px, confirm the filter field and the first gallery row are visible without scrolling past an unidentified-prefix list.
</human_follow_ups>

<output>
Create `.planning/phases/29-companion-review-feedback-round-3-the-developer-s-own-tour-o/29-02-SUMMARY.md` when done, including: the harness before/after table with every count re-derived by running, all three mutation proofs with real failure messages quoted verbatim, the recorded A2 decision, and the stated per-character advance factor with its provenance.
</output>
