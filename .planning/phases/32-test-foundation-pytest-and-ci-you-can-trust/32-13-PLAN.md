---
phase: 32-test-foundation-pytest-and-ci-you-can-trust
plan: 13
type: execute
wave: 3
depends_on: [32-03, 32-04, 32-05, 32-06, 32-07, 32-08, 32-09, 32-10, 32-11, 32-12]
files_modified:
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md
  - scripts/run_all_tests.py
  - scripts/run-all-tests.sh
  - pyproject.toml
  - server/test_calendar_rules.py
  - server/test_colour_rules.py
  - server/test_config_history.py
  - server/test_dither.py
  - server/test_enrich.py
  - server/test_illustrations.py
  - server/test_manual_resolutions.py
  - server/test_notify.py
  - server/test_panel_preview.py
  - server/test_pipeline_e2e.py
  - server/test_plane_detection.py
  - server/test_poll_loop.py
  - server/test_render.py
  - server/test_runway_config.py
  - stub-server/test_poll_cycle.py
autonomous: true
requirements: [TST-01, TST-02, TST-09]

must_haves:
  truths:
    - "32-MIGRATION-LEDGER.md accounts for every pre-migration check of the 15 server-side harnesses (row total == baseline total, 0 pending)"
    - "`./scripts/run-all-tests.sh` runs `pytest -n auto` with coverage as the single entry point and keeps the PYTHON= override contract"
    - "scripts/run_all_tests.py and its HARNESSES hand list no longer exist"
    - "The coverage gate is enforced by pytest-cov from pyproject.toml's fail_under, and a lower-than-gate run fails"
    - "Coverage data from subprocesses (legacy companion harnesses, companion/app.py and byos_server.py children) reaches the combined report via patch = [\"subprocess\"]"
    - "`pytest -n auto` over server/ and stub-server/ passes as root and as a non-root user"
  artifacts:
    - path: "scripts/run-all-tests.sh"
      provides: "Thin pytest wrapper (PYTHON=, JOBS=)"
      contains: "-m pytest"
    - path: "pyproject.toml"
      provides: "[tool.coverage.run] patch = [\"subprocess\"]"
      contains: "patch = [\"subprocess\"]"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md"
      provides: "Assembled ledger for 15 harnesses"
      contains: "| # | Old check label | Disposition | New node id / reason |"
  key_links:
    - from: "scripts/run-all-tests.sh"
      to: "pyproject.toml [tool.coverage.report] fail_under"
      via: "pytest --cov (pytest-cov reads fail_under from coverage config)"
      pattern: "--cov"
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-all-tests.sh"
      via: "unchanged CI step `./scripts/run-all-tests.sh`"
      pattern: "run-all-tests.sh"
---

<objective>
Finish the server-side migration and make pytest the single entry point: assemble and verify the full migration ledger, retire `scripts/run_all_tests.py` (its `HARNESSES` list and hand-run `coverage combine/report`), turn `scripts/run-all-tests.sh` into a thin `pytest -n auto --cov` wrapper that keeps the `PYTHON=` contract, move the coverage gate to pytest-cov, turn on `patch = ["subprocess"]`, and remove the temporary legacy-runner bridges from the 15 migrated files.

Purpose: TST-01 (coverage gate on pytest-cov; pytest discovery), TST-02 (ledger complete), TST-09 part 1 (subprocess coverage mechanism). The measured `fail_under` and the omit-list change are plan 32-15.
Output: assembled ledger, deleted runner, new wrapper, pyproject coverage config, 15 files without `__main__` bridges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-RESEARCH.md
@scripts/run-all-tests.sh
@scripts/run_all_tests.py
@pyproject.toml

<planner_findings>
- `scripts/run-all-tests.sh` owns the `PYTHON` contract (default `server/.venv/bin/python3`, error if not executable) and is what `.github/workflows/ci.yml` and README call; README/CONTRIBUTING also document `JOBS=1` and `HARNESS_TIMEOUT_S` (the shim from plan 32-01 still reads HARNESS_TIMEOUT_S).
- `run_all_tests.py` sets `COVERAGE_CORE=sysmon` on Python >= 3.12 unless already set (measured large speedup); keep that default in the wrapper.
- Coverage: pytest-cov 7 removed its `.pth` subprocess injection; coverage >= 7.10 `[tool.coverage.run] patch = ["subprocess"]` is the mechanism (it implies parallel = true). RESEARCH Open Question 1: confirm pytest-cov's end-of-session combine also picks up `.coverage.*` files written by subprocesses; fallback = the wrapper runs `coverage combine` + `coverage report` itself after pytest (with `--cov-report=` empty during pytest).
- The omit list still excludes companion/app.py, byos_server.py, make_test_panel.py here; plan 32-15 removes them and measures. The existing `fail_under = 83` must keep passing under the new runner.
- Each migrated file ends with `if __name__ == "__main__": raise SystemExit(pytest.main([...]))` (MR-3 bridge). Once the legacy runner is gone the bridge is dead code; remove it (and the then-unused `import pytest` only if nothing else uses it).
- The sandbox runs as root; `runuser -u nobody` works with server/.venv (verified by the planner: `runuser -u nobody -- server/.venv/bin/python3 -c "import PIL"` succeeds).
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble and verify the migration ledger</name>
  <files>.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md</files>
  <read_first>
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py (--all, --assemble)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/INDEX.md (grand total)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/ (15 fragments)
  </read_first>
  <action>
Run `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --all`; every harness must pass (fix a failing fragment only if the fix is bookkeeping, e.g. a typo in a node id; if a check is genuinely missing from the tests, stop and report which plan's harness is incomplete). Then run it with `--assemble` to regenerate `32-MIGRATION-LEDGER.md` (CONTEXT.md: the ledger lives in the phase directory, one row per pre-migration check). Confirm the assembled totals: sum of rows == INDEX.md grand total; ported + deleted == total; pending == 0.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --all && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --assemble</automated>
  </verify>
  <acceptance_criteria>
    - `32-ledger-check.py --all` exits 0 and prints 15 "baseline checks mapped" lines with 0 pending
    - `grep -c "^## server/test_\|^## stub-server/test_poll_cycle" .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md` prints 15
    - The ledger's grand-total row equals the grand total in 32-BASELINE/INDEX.md
    - `grep -c "| pending |" .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md` prints 0
  </acceptance_criteria>
  <done>The ledger accounts for every pre-migration server-side check.</done>
</task>

<task type="auto">
  <name>Task 2: pytest becomes the single entry point; coverage gate on pytest-cov with subprocess coverage</name>
  <files>scripts/run_all_tests.py, scripts/run-all-tests.sh, pyproject.toml</files>
  <read_first>
    - scripts/run-all-tests.sh (whole file)
    - scripts/run_all_tests.py (main(): COVERAGE_CORE default, coverage cleanup, GITHUB_STEP_SUMMARY timing table)
    - pyproject.toml ([tool.coverage.run] and [tool.coverage.report] with their derivation comments; [tool.pytest.ini_options] from plan 32-01)
    - .github/workflows/ci.yml (the `./scripts/run-all-tests.sh` step — must keep working unchanged)
  </read_first>
  <action>
Per the CONTEXT.md transition decision ("scripts/run_all_tests.py and its HARNESSES hand list are retired in this phase; scripts/run-all-tests.sh becomes a thin wrapper over pytest keeping the PYTHON= override contract") and D-A2 (coverage gate moves to pytest-cov):
1. `git rm scripts/run_all_tests.py`.
2. Rewrite `scripts/run-all-tests.sh` (bash, keep `set -uo pipefail`, cd to repo root, same PYTHON default and not-executable error, with the install hint updated to `server/.venv/bin/pip install --require-hashes -r server/requirements-dev.txt`): delete stale `.coverage` / `.coverage.*` files in the repo root; export `COVERAGE_CORE=sysmon` when unset and the interpreter is >= 3.12; `exec "${PYTHON}" -m pytest -n "${JOBS:-auto}" --cov --cov-report=term-missing:skip-covered --durations=15 "$@"`. Header comment: usage lines for `PYTHON=`, `JOBS=`, `HARNESS_TIMEOUT_S=` (read by the legacy companion shim until Phase 33) and "extra args go to pytest" (e.g. `-k`, a path). No plan history (D-A3).
3. In `pyproject.toml` `[tool.coverage.run]`: add `patch = ["subprocess"]` with a short why-comment (children — legacy companion harnesses and the servers they and the migrated tests start — are measured; pytest-cov 7 no longer does this itself); rewrite the `parallel = true` comment so it describes pytest-xdist workers + subprocesses instead of the retired runner. Leave `source`, `omit` and `fail_under = 83` unchanged (plan 32-15 changes them after measuring).
4. Run `./scripts/run-all-tests.sh`. Confirm: exit 0; the coverage total is >= 83; the report lists companion modules imported only by legacy harness processes (for example `companion/pages/config_page.py`) with non-zero coverage, which proves subprocess data files are combined (RESEARCH Open Question 1). If they show 0% or are missing, switch to the fallback: wrapper runs pytest with `--cov --cov-report=` and then `"${PYTHON}" -m coverage combine` and `"${PYTHON}" -m coverage report` (no exec for pytest; exit non-zero if either fails), and document why in the wrapper.
5. Gate proof: `./scripts/run-all-tests.sh server/test_dither.py --cov-fail-under=100` must exit non-zero with a "FAIL Required test coverage" message (pytest-cov enforces the gate), and a run of the full suite with no override exits 0.
  </action>
  <verify>
    <automated>test ! -e scripts/run_all_tests.py && bash -n scripts/run-all-tests.sh && grep -q -- "-m pytest" scripts/run-all-tests.sh && grep -q 'patch = \["subprocess"\]' pyproject.toml && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e scripts/run_all_tests.py` exits 0 and `git grep -n "run_all_tests.py" -- ':!.planning' ':!*.md'` prints nothing
    - `grep -c 'PYTHON="${PYTHON:-${REPO_ROOT}/server/.venv/bin/python3}"' scripts/run-all-tests.sh` prints 1
    - `grep -q -- '-n "${JOBS:-auto}"' scripts/run-all-tests.sh` and `grep -q -- '--cov' scripts/run-all-tests.sh` exit 0
    - `grep -q 'patch = \["subprocess"\]' pyproject.toml` exits 0 and `grep -q '^fail_under = 83' pyproject.toml` exits 0
    - `./scripts/run-all-tests.sh` exits 0, its output contains "Required test coverage of 83" and "reached", and the coverage table shows a non-zero percentage for at least one `companion/pages/` module
    - `./scripts/run-all-tests.sh server/test_dither.py --cov-fail-under=100` exits non-zero
    - `PYTHON=/nonexistent ./scripts/run-all-tests.sh` exits 1 with "interpreter not found"
    - This plan does not edit `.github/workflows/ci.yml` (plan 32-14 owns it and runs in the same wave); the CI step `./scripts/run-all-tests.sh` keeps working because the wrapper's name and PYTHON contract are unchanged
  </acceptance_criteria>
  <done>pytest -n auto with coverage is the single entry point; the gate is enforced by pytest-cov; subprocess coverage is combined.</done>
</task>

<task type="auto">
  <name>Task 3: Remove legacy-runner bridges; root and non-root runs</name>
  <files>server/test_calendar_rules.py, server/test_colour_rules.py, server/test_config_history.py, server/test_dither.py, server/test_enrich.py, server/test_illustrations.py, server/test_manual_resolutions.py, server/test_notify.py, server/test_panel_preview.py, server/test_pipeline_e2e.py, server/test_plane_detection.py, server/test_poll_loop.py, server/test_render.py, server/test_runway_config.py, stub-server/test_poll_cycle.py</files>
  <read_first>
    - The last 10 lines of each of the 15 files (the MR-3 `if __name__ == "__main__":` bridge)
  </read_first>
  <action>
Delete the `if __name__ == "__main__":` pytest.main bridge from each of the 15 migrated files (mechanical: only those lines, plus `import pytest` if it becomes unused — ruff F401 tells you). Nothing else changes. Then prove the success-criterion "pytest -n auto runs every migrated server/stub-server test" as root and as non-root:
- `server/.venv/bin/python3 -m pytest server/ stub-server/ -n auto -q -p no:cacheprovider -rs` exits 0 (as root: permission tests reported as skipped with the requires_non_root reason).
- `runuser -u nobody -- env PYTHONDONTWRITEBYTECODE=1 server/.venv/bin/python3 -m pytest server/ stub-server/ -n auto -q -p no:cacheprovider -rs` exits 0 with no requires_non_root skips (tmp_path goes to /tmp/pytest-of-nobody; a failure caused by a test writing into the read-only repo tree is a real MR-5 violation to fix in that test).
  </action>
  <verify>
    <automated>test "$(grep -l '__name__ == "__main__"' server/test_*.py stub-server/test_poll_cycle.py | wc -l)" -eq 0 && server/.venv/bin/ruff check server/ stub-server/ && server/.venv/bin/python3 -m pytest server/ stub-server/ -n auto -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l '__name__ == "__main__"' server/test_*.py stub-server/test_poll_cycle.py | wc -l` prints 0
    - `server/.venv/bin/ruff check .` exits 0
    - `server/.venv/bin/python3 -m pytest server/ stub-server/ -n auto -q -p no:cacheprovider` exits 0
    - `runuser -u nobody -- env PYTHONDONTWRITEBYTECODE=1 server/.venv/bin/python3 -m pytest server/ stub-server/ -n auto -q -p no:cacheprovider -rs` exits 0 and its output contains no "needs a non-root euid" skip line
    - `server/.venv/bin/python3 -m pytest --collect-only -q -p no:cacheprovider server/ stub-server/ | grep -c "::"` is >= the number of distinct ported node ids in 32-MIGRATION-LEDGER.md
  </acceptance_criteria>
  <done>Migrated files are plain pytest modules; the server-side suite passes under xdist as root and as non-root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → test runner | CI trusts the wrapper's exit code as "every check ran" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-13-01 | Repudiation | dropped checks during the runner switch | mitigate | Ledger assembled and verified (--all) before the runner is retired; row total equals baseline total |
| T-32-13-02 | Tampering | coverage gate silently not enforced | mitigate | Negative test: `--cov-fail-under=100` must fail; fail_under read from pyproject.toml by pytest-cov |
| T-32-13-03 | Information Disclosure | network use hidden by a runner change | mitigate | pytest addopts guard + shim child_env unchanged; the full suite runs under them |
| T-32-13-04 | Tampering | tests writing into the repo tree | mitigate | Non-root run against a read-only checkout (runuser nobody) |
</threat_model>

<verification>
- `./scripts/run-all-tests.sh` exits 0 with coverage >= 83 and companion modules measured
- `32-ledger-check.py --all` exits 0
</verification>

<success_criteria>
ROADMAP success criterion 1: `pytest -n auto` runs every migrated server/stub-server test and the ledger accounts for every pre-migration check; the legacy runner is gone and the coverage gate lives in pytest-cov.
</success_criteria>

<output>
Create `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-13-SUMMARY.md` when done (include total wall time and the coverage total of the full run)
</output>
