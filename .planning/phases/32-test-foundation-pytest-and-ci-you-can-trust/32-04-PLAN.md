---
phase: 32-test-foundation-pytest-and-ci-you-can-trust
plan: 04
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - server/test_manual_resolutions.py
  - server/test_colour_rules.py
  - server/test_enrich.py
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_manual_resolutions.md
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_colour_rules.md
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_enrich.md
autonomous: true
requirements: [TST-02, TST-03]

must_haves:
  truths:
    - "`pytest server/test_manual_resolutions.py server/test_colour_rules.py server/test_enrich.py -n 4` passes as root (permission checks skipped with a reason) and as non-root (they run)"
    - "Every baseline check of these three harnesses maps to a collected node id or a justified deletion"
    - "adsbdb access in test_enrich goes through transport injection or the fake_providers fixture, never the network"
  artifacts:
    - path: "server/test_manual_resolutions.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: "server/test_colour_rules.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: "server/test_enrich.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_manual_resolutions.md"
      provides: "ledger rows for server/test_manual_resolutions.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_colour_rules.md"
      provides: "ledger rows for server/test_colour_rules.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_enrich.md"
      provides: "ledger rows for server/test_enrich.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
  key_links:
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_manual_resolutions.md"
      to: "server/test_manual_resolutions.py"
      via: "every ported row names a collected node id in server/test_manual_resolutions.py"
      pattern: "server/test_manual_resolutions.py::test_"
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_colour_rules.md"
      to: "server/test_colour_rules.py"
      via: "every ported row names a collected node id in server/test_colour_rules.py"
      pattern: "server/test_colour_rules.py::test_"
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_enrich.md"
      to: "server/test_enrich.py"
      via: "every ported row names a collected node id in server/test_enrich.py"
      pattern: "server/test_enrich.py::test_"
---

<objective>
Migrate `server/test_manual_resolutions.py` (23 checks, first root-safety case), `server/test_colour_rules.py` (33) and `server/test_enrich.py` (60, adsbdb side of the fake provider) to pytest (RESEARCH batches B and D merged).

Purpose: TST-02 — server-side harnesses become real pytest tests with every pre-migration check accounted for in the migration ledger.
Output: migrated test module(s) and their ledger fragment(s) under `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-PATTERNS.md
@server/test_manual_resolutions.py
@server/test_colour_rules.py
@server/test_enrich.py
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md
@conftest.py
@test-support/skypane_test_support.py

<migration_rules>
Identical in every migration plan (32-03..32-10). "Harness" = one of the hand-rolled files; "baseline" = `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/<key>.txt` captured by plan 32-02.

- MR-1 Translation: each old `check(label, fn)` becomes one pytest test function `test_<descriptive_snake_case>`, or one id of a `@pytest.mark.parametrize` when the old check was emitted from a loop. The old label is the test docstring verbatim (a parametrized or consolidated test lists its labels in the docstring or carries them in readable param ids). `return False, reason` becomes `assert <condition>, reason` (or `pytest.fail(reason)` where no single boolean exists); `return True, ""` disappears; exceptions propagate — never wrap a test body in try/except that turns an error into a pass.
- MR-2 Remove `check()`, the `results` list, every `EXPECTED_CHECK_COUNT`, `main()`, the "N/M checks pass" summary and the import-failure guard at the top of `main()` (pytest reports collection errors itself).
- MR-3 Legacy-runner bridge (removed by plan 32-13): keep the file's existing repo-root `sys.path` bootstrap and end the file with an `if __name__ == "__main__":` block that does `raise SystemExit(pytest.main([__file__, "-q", "-p", "no:cacheprovider"]))`, so `scripts/run_all_tests.py` (still CI's runner until plan 32-13) keeps running the file under coverage.
- MR-4 Independence: no test depends on another test's side effects or on execution order. Shared expensive setup → a `scope="module"` fixture built with `tmp_path_factory`. A genuinely sequential scenario (a later old check asserts on state an earlier one created) → one test function whose asserts cover those old checks (N old checks → 1 node id in the ledger). The module must pass with `-n 0` and with `-n 4`.
- MR-5 Filesystem: every path lives under `tmp_path` / `tmp_path_factory`; the migrated file contains no `tempfile.` call; a test run leaves no new untracked file in the repo.
- MR-6 Global state (module attributes, env vars, cwd, `socket.getaddrinfo`): patch with `monkeypatch`, never manual save/restore.
- MR-7 Network: no test opens a non-loopback socket or resolves a non-loopback name — the pytest-socket allow-hosts guard and the autouse DNS guard from plan 32-01 fail such a test. ADS-B/adsbdb HTTP goes through the `fake_providers` fixture (repo-root conftest.py) or through the file's own function-level stubs converted to `monkeypatch.setattr`. Never add `@pytest.mark.enable_socket`. A fallback to the "real" `socket.getaddrinfo` inside a test now reaches the DNS guard: that is intended.
- MR-8 Root safety: every check whose outcome depends on permission bits (`os.chmod`, read-only files or directories, unwritable paths) gets `@requires_non_root` (`from skypane_test_support import requires_non_root`), with every path under tmp_path. A baseline FAIL line caused only by euid 0 is ported this way (the check runs in CI, where the runner is non-root).
- MR-9 Subprocesses: env built with `skypane_test_support.child_env()` (guard active in the child), loopback ports only, fixture teardown terminate → wait(timeout=5) → kill → wait.
- MR-10 Comments: keep only comments that explain why a behaviour is checked; drop phase/plan/ticket history (D-A3); English only.
- MR-11 No production code change. If the guard exposes a real network access, stub it in the test. Anything surprising (a check that cannot be ported as-is) goes into the SUMMARY with the reason.
- MR-12 Ledger fragment `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/<key>.md`, format exactly as described in `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md`: `# Ledger: <harness>`, a `Baseline:` line, then one table row per PASS/FAIL line of the baseline, in baseline order. `deleted` only with a concrete reason (for example: the check only asserted that a module imports, which pytest collection now guarantees). Verify with `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py <harness>`.
- MR-13 Test names are unique in the module and read as behaviour ("test_empty_state_is_white_dominant"), not as numbers.
</migration_rules>

<interfaces>
From test-support/skypane_test_support.py (plan 32-01; importable in tests because pyproject.toml sets `pythonpath = [".", "test-support"]`):
- `requires_non_root` — pytest skipif marker for euid 0
- `child_env(base=None, *, fake_providers=None, state_dir=None) -> dict[str, str]` — env for child processes: guard on, PYTHONPATH includes test-support; `fake_providers` = None | "default" | FakeProviders (written to state_dir)
- `class FakeProviders` — `respond(name, body, status=200)`, `fail(name, exc)`, `calls` (list of {"provider", "url"}); provider names "adsbfi", "adsblol", "airplaneslive", "adsbdb"; defaults: adsbfi (200, {"aircraft": []}), adsblol/airplaneslive (200, {"ac": []}), adsbdb (404, {"response": "unknown callsign"})
- `REPO_ROOT`, `TEST_SUPPORT_DIR`

From repo-root conftest.py (plan 32-01):
- fixture `fake_providers(monkeypatch) -> FakeProviders` — installed on `requests.get` (reaches `detect.query_provider()` and `enrich.default_transport()`), `detect.MIN_SECONDS_BETWEEN_CALLS` set to 0
- autouse DNS guard: `socket.getaddrinfo`/`gethostbyname`/`gethostbyname_ex` raise `NetworkAccessBlocked` for non-loopback names

From pyproject.toml (plan 32-01): `--import-mode=importlib --strict-markers`, markers `slow`, `legacy_harness`; pytest-socket `--allow-hosts=127.0.0.1,::1,localhost --allow-unix-socket`.
</interfaces>

<planner_notes>
- `server/test_manual_resolutions.py:437-474` has two `os.chmod(parent, 0o500)` checks with no root guard (they FAIL or pass for the wrong reason under euid 0). Port both with `@requires_non_root` and `tmp_path` (32-PATTERNS.md "Root-safety skip pattern" shows the exact translation). grep found 4 `os.chmod` lines in this file.
- `server/test_enrich.py` mostly injects a `transport=` callable into `enrich.lookup_route()` (the production seam `enrich.default_transport` documents). Keep that injection. Any check that exercises `enrich.default_transport` itself (the real `requests.get` wrapper) uses the `fake_providers` fixture (`fake_providers.respond("adsbdb", body, status)`), never a live call. A comment near line 990 cites a `.planning/notes/...` file: drop the path reference (D-A3), keep the why.
- `server/test_colour_rules.py` is pure CRUD over a state dir: tmp_path everywhere.
- Parallel execution: this plan shares wave 2 with other migration plans in the same working tree. Only touch the files in `files_modified`. Run only this plan's test files (never `./scripts/run-all-tests.sh`, which clears shared coverage data files). Do not reinstall or recreate `server/.venv`.
</planner_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test_manual_resolutions (root-safe) and test_colour_rules</name>
  <files>server/test_manual_resolutions.py, server/test_colour_rules.py, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_manual_resolutions.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_colour_rules.md</files>
  <read_first>
    - server/test_manual_resolutions.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_manual_resolutions.txt (the labels to map, in order)
    - server/test_colour_rules.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_colour_rules.txt (the labels to map, in order)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md (fragment format)
    - conftest.py and test-support/skypane_test_support.py (fixtures and helpers from plan 32-01)
  </read_first>
  <action>
Convert both harnesses. In `server/test_manual_resolutions.py`, every permission-dependent check (the two `os.chmod(..., 0o500)` read-only parent checks and any other you find) is decorated `@requires_non_root` per MR-8, with the chmod target under tmp_path and restored in a finally so pytest can clean tmp_path.

Apply MR-1..MR-13 (see `<migration_rules>`). Write the ledger fragment(s) .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_manual_resolutions.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_colour_rules.md with one row per baseline line. Per TST-02 and the CONTEXT.md ledger decision, every baseline check maps to a node id or to a deletion with a reason.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest server/test_manual_resolutions.py server/test_colour_rules.py -q -p no:cacheprovider && server/.venv/bin/python3 -m pytest server/test_manual_resolutions.py server/test_colour_rules.py -q -p no:cacheprovider -n 4 && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_manual_resolutions.py && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_colour_rules.py && server/.venv/bin/ruff check server/test_manual_resolutions.py server/test_colour_rules.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest server/test_manual_resolutions.py server/test_colour_rules.py -q -p no:cacheprovider` exits 0 (0 failed, 0 errors)
    - `server/.venv/bin/python3 -m pytest server/test_manual_resolutions.py server/test_colour_rules.py -q -p no:cacheprovider -n 4` exits 0
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_manual_resolutions.py` exits 0 and prints `server/test_manual_resolutions.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_manual_resolutions.py` prints 0
    - `server/.venv/bin/python3 server/test_manual_resolutions.py` exits 0 (legacy-runner bridge, MR-3)
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_colour_rules.py` exits 0 and prints `server/test_colour_rules.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_colour_rules.py` prints 0
    - `server/.venv/bin/python3 server/test_colour_rules.py` exits 0 (legacy-runner bridge, MR-3)
    - After this task's commit and a fresh test run, `git status --porcelain --untracked-files=all server/ stub-server/ | grep '^??' | grep -cvE 'server/requirements(-dev)?\.in$'` prints 0 (tests wrote nothing into the repo; the two `.in` files are plan 32-12's, created in the same wave)
    - `server/.venv/bin/ruff check server/test_manual_resolutions.py server/test_colour_rules.py` exits 0
    - `grep -c '@requires_non_root' server/test_manual_resolutions.py` prints at least 1, and when `id -u` is 0 a non-root run passes with no skips: `runuser -u nobody -- env PYTHONDONTWRITEBYTECODE=1 server/.venv/bin/python3 -m pytest server/test_manual_resolutions.py -q -p no:cacheprovider -rs` exits 0 (if runuser itself cannot start, record the reason in the SUMMARY — CI's non-root runner covers these tests)
    - `git diff --quiet HEAD -- server/plane server/device_config.py server/history_db.py server/notify.py server/panel_format.py server/panel_preview.py server/poll_loop.py server/wake.py stub-server/byos_server.py stub-server/make_test_panel.py` exits 0 (no production code change, MR-11)
  </acceptance_criteria>
  <done>server/test_manual_resolutions.py, server/test_colour_rules.py run as real pytest tests; the ledger fragment(s) map every baseline check.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate test_enrich (adsbdb seam via transport injection and fake_providers)</name>
  <files>server/test_enrich.py, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_enrich.md</files>
  <read_first>
    - server/test_enrich.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_enrich.txt (the labels to map, in order)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md (fragment format)
    - conftest.py and test-support/skypane_test_support.py (fixtures and helpers from plan 32-01)
    - server/plane/enrich.py (ADSBDB_URL, default_transport, lookup_route with transport=)
  </read_first>
  <action>
Convert `server/test_enrich.py`. Keep hermetic fixture replays through `lookup_route(..., transport=...)`. Where a check needs the default transport path, request the `fake_providers` fixture instead of monkeypatching `requests.get` by hand. Loop-emitted checks (per-fixture or per-callsign loops) become `@pytest.mark.parametrize` with readable ids.

Apply MR-1..MR-13 (see `<migration_rules>`). Write the ledger fragment(s) .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_enrich.md with one row per baseline line. Per TST-02 and the CONTEXT.md ledger decision, every baseline check maps to a node id or to a deletion with a reason.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest server/test_enrich.py -q -p no:cacheprovider && server/.venv/bin/python3 -m pytest server/test_enrich.py -q -p no:cacheprovider -n 4 && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_enrich.py && server/.venv/bin/ruff check server/test_enrich.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest server/test_enrich.py -q -p no:cacheprovider` exits 0 (0 failed, 0 errors)
    - `server/.venv/bin/python3 -m pytest server/test_enrich.py -q -p no:cacheprovider -n 4` exits 0
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_enrich.py` exits 0 and prints `server/test_enrich.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_enrich.py` prints 0
    - `server/.venv/bin/python3 server/test_enrich.py` exits 0 (legacy-runner bridge, MR-3)
    - After this task's commit and a fresh test run, `git status --porcelain --untracked-files=all server/ stub-server/ | grep '^??' | grep -cvE 'server/requirements(-dev)?\.in$'` prints 0 (tests wrote nothing into the repo; the two `.in` files are plan 32-12's, created in the same wave)
    - `server/.venv/bin/ruff check server/test_enrich.py` exits 0
    - `git diff --quiet HEAD -- server/plane server/device_config.py server/history_db.py server/notify.py server/panel_format.py server/panel_preview.py server/poll_loop.py server/wake.py stub-server/byos_server.py stub-server/make_test_panel.py` exits 0 (no production code change, MR-11)
  </acceptance_criteria>
  <done>server/test_enrich.py run as real pytest tests; the ledger fragment(s) map every baseline check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → network | Migrated tests run under the pytest-socket allow-hosts guard and the DNS guard |
| old harness → new tests | Checks could be dropped or weakened during translation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-01 | Repudiation | server/test_manual_resolutions.py, server/test_colour_rules.py, server/test_enrich.py | mitigate | Ledger fragment checked by 32-ledger-check.py: row count and label multiset equal the baseline, every ported node id is collected, every deletion has a reason |
| T-32-04-02 | Information Disclosure | tests reaching live ADS-B/adsbdb/calendar hosts | mitigate | MR-7: guard fails any non-loopback connect or DNS lookup; provider HTTP only via fake_providers or monkeypatched stubs; no enable_socket marker |
| T-32-04-03 | Tampering | tests writing outside tmp_path (repo tree, shared /tmp paths) | mitigate | MR-5: tmp_path only, no `tempfile.`, untracked-file check after the run; MR-8 @requires_non_root for permission-bit checks |
</threat_model>

<verification>
- `server/.venv/bin/python3 -m pytest server/test_manual_resolutions.py server/test_colour_rules.py server/test_enrich.py -q -p no:cacheprovider -n 4` exits 0; every `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py <harness>` for this plan exits 0
- `grep -rn "enable_socket" server/test_manual_resolutions.py server/test_colour_rules.py server/test_enrich.py` prints nothing
</verification>

<success_criteria>
server/test_manual_resolutions.py, server/test_colour_rules.py, server/test_enrich.py are pytest modules that pass under xdist, touch no network, write only under tmp_path, and every baseline check is mapped in the ledger fragment(s).
</success_criteria>

<output>
Create `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-04-SUMMARY.md` when done (include per-harness: baseline count, ported, deleted with reasons, consolidations, any root-only skips)
</output>
