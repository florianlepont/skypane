---
phase: 32-test-foundation-pytest-and-ci-you-can-trust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md
autonomous: true
requirements: [TST-02]

must_haves:
  truths:
    - "Every one of the 15 server-side harnesses has a captured pre-migration transcript whose PASS/FAIL line count equals the total the harness itself printed"
    - "A single command verifies, per harness, that a ledger fragment maps every baseline check to an existing pytest node id or to a deletion with a reason"
    - "The migration ledger exists before any harness is rewritten"
  artifacts:
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py"
      provides: "capture / check / assemble tool for the migration ledger"
      contains: "def main"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/INDEX.md"
      provides: "Per-harness baseline counts, interpreter, euid, capture commit"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md"
      provides: "Ledger scaffold: format, status table of the 15 harnesses"
      contains: "| # | Old check label | Disposition | New node id / reason |"
  key_links:
    - from: "32-ledger-check.py"
      to: "32-BASELINE/*.txt"
      via: "baseline label parsing (PASS <label> / FAIL <label> - <reason>)"
      pattern: "32-BASELINE"
    - from: "32-ledger-check.py"
      to: "pytest --collect-only"
      via: "node id existence check"
      pattern: "--collect-only"
---

<objective>
Capture the pre-migration baseline of the 15 server-side harnesses (per-check PASS/FAIL transcripts, the Phase 31 `31-BASELINE-CHECKS.txt` format), scaffold `32-MIGRATION-LEDGER.md`, and write the stdlib tool every migration plan uses to prove its ledger fragment is complete.

Purpose: CONTEXT.md requires "one row per pre-migration check... baseline captured before any rewrite". RESEARCH Pitfall 1: grepping `check(` call sites is wrong (loop-emitted checks), so the baseline is the harness's own stdout. Migration plans run in parallel, so each writes its own fragment `32-ledger/<dir>__<stem>.md` (disjoint files, no merge conflicts); plan 32-13 assembles them into `32-MIGRATION-LEDGER.md` with this tool's `--assemble` mode.
Output: `32-ledger-check.py`, `32-BASELINE/` (15 transcripts + INDEX.md), `32-MIGRATION-LEDGER.md` scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-RESEARCH.md
@.planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-BASELINE-CHECKS.txt
@server/test_dither.py

<planner_findings>
- Every server harness prints `PASS <label>` or `FAIL <label> - <reason>` per check and ends with `<name>: <passed>/<total> checks pass`. `server/test_config_history.py:1660` has an early-abort summary line mid-file; the LAST summary line printed is authoritative.
- `EXPECTED_CHECK_COUNT` values (first assignment) sum to 738, but several companion files reassign it many times; the printed total and the transcript are the ground truth, not a source read.
- The dev sandbox runs as root (euid 0). Permission-bit checks (`os.chmod` in test_manual_resolutions.py, test_calendar_rules.py, test_poll_loop.py) may print FAIL under root. Their labels are still captured; FAIL lines are recorded, not "fixed", in this plan.
- `server/.venv` is shared with plan 32-01 running in the same wave (it only adds packages); run harnesses with `server/.venv/bin/python3` from the repo root, exactly like the legacy runner does.
</planner_findings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ledger tool (capture, check, assemble)</name>
  <files>.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py</files>
  <read_first>
    - scripts/run_all_tests.py (HARNESSES list: the 15 server-side entries and their order)
    - server/test_dither.py (check() print format, summary line format)
    - .planning/phases/31-ci-test-suite-parallelize-companion-test-browser-ux-py-to-cu/31-BASELINE-CHECKS.txt (format precedent)
  </read_first>
  <behavior>
    - parse_baseline("PASS a\nFAIL b - boom\nnoise\nx: 1/2 checks pass") returns [("PASS","a"), ("FAIL","b - boom")] and total 2
    - a ledger row whose label is "b" matches the baseline line "FAIL b - boom" (FAIL lines match when the line starts with "FAIL " + label + " - "); a PASS line matches only on exact label equality after rstrip
    - labels containing "|" are written escaped as "\|" in fragments and unescaped when parsed
    - check fails (exit 1, message names the harness) when: row count != baseline count; the label multiset differs; a "ported" node id is absent from `pytest --collect-only -q` output; a "deleted" row has an empty reason; any row is "pending" and --allow-pending was not given; the migrated file still contains "EXPECTED_CHECK_COUNT" or "def check("
  </behavior>
  <action>
Write `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py`, stdlib only, runnable as `server/.venv/bin/python3 <path> ...` from the repo root (it locates the repo root and the phase dir from `__file__`). Constant `HARNESSES` = the 15 repo-relative paths (server/test_calendar_rules.py, server/test_colour_rules.py, server/test_config_history.py, server/test_dither.py, server/test_enrich.py, server/test_illustrations.py, server/test_manual_resolutions.py, server/test_notify.py, server/test_panel_preview.py, server/test_pipeline_e2e.py, server/test_plane_detection.py, server/test_poll_loop.py, server/test_render.py, server/test_runway_config.py, stub-server/test_poll_cycle.py). File key for a harness = path with "/" replaced by "__" and ".py" dropped (e.g. `server__test_dither`, `stub-server__test_poll_cycle`).

Modes (argparse):
- `--capture`: for each harness (or the ones given), refuse if `32-BASELINE/<key>.txt` already exists unless `--force`, refuse if the harness no longer contains `EXPECTED_CHECK_COUNT` (already migrated); run `[sys.executable, harness]` with cwd=repo root, stdout+stderr to the transcript file, timeout 900 s; then (re)write `32-BASELINE/INDEX.md` with a table: harness | PASS | FAIL | total | printed summary line | exit code, plus capture metadata (python version, `os.geteuid()`, `git rev-parse HEAD`, UTC timestamp) and the grand total.
- `<harness> [...]` / `--all` (default mode "check"): load `32-BASELINE/<key>.txt` and `32-ledger/<key>.md`; parse fragment rows from the markdown table with header `| # | Old check label | Disposition | New node id / reason |`; Disposition must be one of `ported`, `deleted`, `pending`; apply the rules in `<behavior>`; node ids are verified by one `[sys.executable, "-m", "pytest", "--collect-only", "-q", "-p", "no:cacheprovider", "server", "stub-server"]` run (a ported check may land in another file of the same directory); parse node ids from the output even when pytest exits non-zero, because a sibling plan may be mid-edit on another file in the same working tree: a collection error in a file OTHER than the harness being checked is printed as a warning, a collection error in the harness being checked is a failure; print `<harness>: <n>/<n> baseline checks mapped (<p> ported, <d> deleted, <q> pending)`; exit 1 on any failure.
- `--allow-pending`: pending rows count as mapped (used only by a plan that migrates a file in stages; none is planned).
- `--assemble`: requires all 15 fragments to pass check; writes `32-MIGRATION-LEDGER.md` = the scaffold header (format + rules), a summary table (harness | baseline | ported | deleted | fragment), grand totals, then every fragment's table under a `## <harness>` heading in HARNESSES order.
- `--self-test`: runs the `<behavior>` cases against in-memory strings (RED first, then implement) and exits 0 on success.
Keep it ruff-clean (it lives in the repo and `ruff check .` covers it). English only (D-A3).
  </action>
  <verify>
    <automated>server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --self-test && server/.venv/bin/ruff check .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --self-test` exits 0
    - `grep -c "server/test_\|stub-server/test_poll_cycle" .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py` prints at least 15
    - `grep -q -- "--collect-only" .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py` exits 0
    - `server/.venv/bin/ruff check .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py` exits 0
  </acceptance_criteria>
  <done>The tool self-tests green and implements capture, check, assemble.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the 15 baselines and scaffold the ledger</name>
  <files>.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md</files>
  <read_first>
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py (Task 1 output)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md (Migration ledger section)
  </read_first>
  <action>
Confirm no server-side harness has been rewritten yet: `grep -L EXPECTED_CHECK_COUNT server/test_*.py stub-server/test_poll_cycle.py` must print nothing. Then run `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --capture` from the repo root. For every harness whose transcript has FAIL lines, record in INDEX.md (a "Notes" column or section) whether the failure is a permission check failing only because euid is 0 (read the check's source to decide) or something else; do not modify any harness. If a transcript's PASS+FAIL count differs from the printed total, stop and report it (the harness aborted early).

Write `32-MIGRATION-LEDGER.md` scaffold: title; purpose (CONTEXT.md: one row per pre-migration check of the 15 server-side harnesses → pytest node id, or deleted + reason); the fragment format (path `32-ledger/<key>.md`, header line `# Ledger: <harness>`, a `Baseline:` line naming the transcript and its count, then the table header `| # | Old check label | Disposition | New node id / reason |` with `|` in labels escaped as `\|`); the rules (Disposition ∈ ported/deleted; ported cell = full node id such as `server/test_dither.py::test_panel_palette_image_is_unpadded` or a parametrized id `...::test_x[id]`; several old checks may map to one node id when consolidated; deleted needs a concrete reason); a status table listing the 15 harnesses with their baseline count from INDEX.md, the planned migrating plan (32-03: dither, runway_config, notify, panel_preview, pipeline_e2e; 32-04: manual_resolutions, colour_rules, enrich; 32-05: illustrations, plane_detection; 32-06: poll_cycle; 32-07: config_history; 32-08: calendar_rules; 32-09: render; 32-10: poll_loop) and status "pending"; the verification command `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py <harness>`; and a note that plan 32-13 regenerates this file with `--assemble`.
  </action>
  <verify>
    <automated>test "$(ls .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/*.txt | wc -l)" -eq 15 && test -f .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/INDEX.md && grep -q "| # | Old check label | Disposition | New node id / reason |" .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/*.txt | wc -l` prints 15
    - For each transcript, `grep -cE '^(PASS|FAIL) '` equals the `<total>` in its last `checks pass` line (checked with a one-line loop; INDEX.md shows the same numbers)
    - `grep -q 'server__test_dither' .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/INDEX.md` and INDEX.md contains a python version line and a euid line
    - `grep -c "pending" .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md` prints at least 15
    - `git diff --quiet server/ stub-server/` exits 0 (no harness modified)
  </acceptance_criteria>
  <done>Baselines captured for all 15 harnesses with verified counts; ledger scaffold committed before any rewrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness transcript → ledger | The baseline is the evidence that no check is silently dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Repudiation | migration silently dropping checks | mitigate | Baseline captured before rewrite, capture refuses to overwrite without --force and refuses already-migrated files; checker compares label multisets and verifies node ids exist |
| T-32-02-02 | Information Disclosure | baseline capture touching live ADS-B feeds | accept | Capture runs the unchanged harnesses exactly as CI does today; they already stub providers (network guard lands for pytest runs in plan 32-01, not for legacy script runs) |
</threat_model>

<verification>
- `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py --self-test` exits 0
- 15 transcripts + INDEX.md exist, counts consistent
</verification>

<success_criteria>
Baseline for 738-ish checks (exact number recorded in INDEX.md) exists before any migration, with a deterministic tool for per-harness and final ledger verification.
</success_criteria>

<output>
Create `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-02-SUMMARY.md` when done
</output>
