---
phase: 32-test-foundation-pytest-and-ci-you-can-trust
plan: 12
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - server/requirements.in
  - server/requirements-dev.in
  - server/requirements.txt
  - server/requirements-dev.txt
  - scripts/lock-deps.sh
  - deploy/deploy.sh
autonomous: true
requirements: [TST-08]

must_haves:
  truths:
    - "server/requirements.txt pins every runtime package including transitives (urllib3, certifi, idna, charset-normalizer) with sha256 hashes"
    - "server/requirements-dev.txt is a hash-locked superset (runtime + dev) with identical runtime pins"
    - "A clean Python 3.14 venv installs each lock with `pip install --require-hashes`, and a tampered hash is rejected"
    - "deploy/deploy.sh installs the runtime lock with --require-hashes and its requirements.sha256 change detection still hashes server/requirements.txt"
    - "Production still receives runtime dependencies only"
  artifacts:
    - path: "server/requirements.in"
      provides: "Direct runtime pins (lock source)"
      contains: "Pillow==12.3.0"
    - path: "server/requirements-dev.in"
      provides: "Direct dev pins, includes the runtime source"
      contains: "-r requirements.in"
    - path: "server/requirements.txt"
      provides: "Hash-locked runtime lock"
      contains: "--hash=sha256:"
    - path: "server/requirements-dev.txt"
      provides: "Hash-locked dev lock (superset)"
      contains: "pytest-socket=="
    - path: "scripts/lock-deps.sh"
      provides: "One command to regenerate both locks for Python 3.14"
      contains: "--generate-hashes"
    - path: "deploy/deploy.sh"
      provides: "Hash-enforced production install"
      contains: "--require-hashes"
  key_links:
    - from: "deploy/deploy.sh"
      to: "server/requirements.txt"
      via: "pip install --require-hashes -r ${APP_ROOT}/server/requirements.txt; sha256sum of the same file"
      pattern: "pip install --require-hashes"
    - from: "scripts/lock-deps.sh"
      to: "server/requirements*.in"
      via: "uv pip compile --generate-hashes --python-version 3.14"
      pattern: "uv pip compile"
---

<objective>
Hash-lock runtime and dev dependencies (TST-08): keep the direct pins as `.in` sources, generate `server/requirements.txt` (runtime lock) and `server/requirements-dev.txt` (dev lock, superset of runtime) with `uv pip compile --generate-hashes --python-version 3.14`, add a regeneration script, and make `deploy/deploy.sh` enforce hashes. Verified locally in clean venvs; no VPS access needed.

Purpose: the audit found transitive dependencies floating and no hashes; production (Python 3.14) must install exactly what CI tested.
Output: two `.in` files, two lock files, `scripts/lock-deps.sh`, edited `deploy/deploy.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-RESEARCH.md
@server/requirements.txt
@server/requirements-dev.txt
@deploy/deploy.sh

<planner_findings>
- Lock tool (Claude's discretion per CONTEXT.md): `uv pip compile` — RESEARCH verified `uv pip compile --generate-hashes --python-version 3.14` produces a correct lock with transitives; `uv` 0.8.17 is at `/root/.local/bin/uv`, and `uv python list` shows a local cpython-3.14.0rc2. `.in` sources are kept so the lock is reproducible.
- In-place lock (RESEARCH Pitfall 5 option a): the runtime lock stays at `server/requirements.txt`, so deploy.sh's `sha256sum "${REPO_ROOT}/server/requirements.txt"` and its `-r ${APP_ROOT}/server/requirements.txt` path stay correct; only `--require-hashes` is added. The first deploy after this change reinstalls once (the file hash changes) — expected.
- The dev lock is a superset (dev .in starts with `-r requirements.in`) so CI installs ONE file with `--require-hashes`; installing two separately-compiled hash files together risks duplicate/conflicting pins.
- `deploy/deploy.sh` rsyncs the whole `server/` dir (the `.in` and dev lock files ride along, as the dev file already does today) but only ever installs `server/requirements.txt`.
- `server/.venv` is shared with the parallel wave-2 migration plans: do NOT reinstall into it here; verify in scratch venvs only.
- Outbound HTTPS goes through an agent proxy; if uv or pip fails TLS, read /root/.ccr/README.md (for uv, `SSL_CERT_FILE=/root/.ccr/ca-bundle.crt` or `UV_NATIVE_TLS=1` are the usual fixes); never disable TLS verification.
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .in sources, hash-locked lock files, regeneration script</name>
  <files>server/requirements.in, server/requirements-dev.in, server/requirements.txt, server/requirements-dev.txt, scripts/lock-deps.sh</files>
  <read_first>
    - server/requirements.txt and server/requirements-dev.txt (current pins and comments; requirements-dev.txt includes the four pytest packages added by plan 32-01)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-RESEARCH.md ("Hash-locking with uv" code example, Pitfall 5)
  </read_first>
  <action>
Per TST-08 and D-A4 (locks generated for Python 3.14 / Linux):
1. Create `server/requirements.in` with `Pillow==12.3.0` and `requests==2.34.2` and a short header: runtime deps only; production is stdlib + Pillow + requests (CLAUDE.md constraint); edit this file then run `scripts/lock-deps.sh`.
2. Create `server/requirements-dev.in`: first line `-r requirements.in`, then the dev pins currently in server/requirements-dev.txt (ruff==0.16.8, coverage==7.16.1, playwright==1.63.0, pytest==9.1.1, pytest-xdist==3.8.0, pytest-cov==7.1.0, pytest-socket==0.8.1), carrying over the existing "why dev-only" comments in shortened form (D-A3: no plan history).
3. Create `scripts/lock-deps.sh` (bash, `set -euo pipefail`, executable, cd to repo root): requires `uv` on PATH (clear error otherwise); runs `uv pip compile --generate-hashes --python-version 3.14 --python-platform x86_64-manylinux_2_28 --custom-compile-command "scripts/lock-deps.sh" server/requirements.in -o server/requirements.txt` and the same for `server/requirements-dev.in -o server/requirements-dev.txt`. If uv 0.8.17 rejects `--python-platform x86_64-manylinux_2_28`, use `--python-platform linux` and note it in the script comment. Comment: production (OVH VPS, Ubuntu 26.04, Python 3.14) and CI (ubuntu-latest, 3.14) are both x86_64 Linux.
4. Run `scripts/lock-deps.sh`.
  </action>
  <verify>
    <automated>bash -n scripts/lock-deps.sh && test -x scripts/lock-deps.sh && grep -q "^urllib3==" server/requirements.txt && grep -q "^certifi==" server/requirements.txt && grep -q "^idna==" server/requirements.txt && grep -q "^charset-normalizer==" server/requirements.txt && grep -qi "^pillow==12.3.0" server/requirements-dev.txt && grep -q "^pytest-socket==0.8.1" server/requirements-dev.txt</automated>
  </verify>
  <acceptance_criteria>
    - Every non-comment, non-blank requirement line in server/requirements.txt and server/requirements-dev.txt is `name==version` followed by at least one `--hash=sha256:` continuation: `server/.venv/bin/python3 -c` one-liner (or awk) counting requirement blocks without a hash prints 0 for both files
    - server/requirements.txt contains pins for pillow, requests, urllib3, certifi, idna, charset-normalizer and nothing dev-only: `grep -ciE '^(pytest|ruff|coverage|playwright)' server/requirements.txt` prints 0
    - The runtime pins in server/requirements-dev.txt equal those in server/requirements.txt: for each `name==version` line of the runtime lock, the same line exists in the dev lock
    - `grep -c "python-version 3.14" scripts/lock-deps.sh` prints at least 1; `grep -q "generate-hashes" scripts/lock-deps.sh` exits 0
    - Both lock headers name `scripts/lock-deps.sh` as the compile command
  </acceptance_criteria>
  <done>Both locks hash every package (transitives included) for Python 3.14 Linux and are reproducible with one script.</done>
</task>

<task type="auto">
  <name>Task 2: deploy.sh enforces hashes; clean-venv and tamper verification on 3.14</name>
  <files>deploy/deploy.sh</files>
  <read_first>
    - deploy/deploy.sh (whole file; the "Checking whether requirements.txt changed" block)
    - deploy/provision.sh lines 55-75 (production venv from distro python3)
  </read_first>
  <action>
Per the CONTEXT.md locking decision ("deploy/deploy.sh installs the runtime lock with hashes enforced; its requirements.sha256 change-detection keeps working on the locked file"): in `deploy/deploy.sh` change only the remote install to `sudo -u skypane ${APP_ROOT}/venv/bin/pip install --require-hashes --quiet -r ${APP_ROOT}/server/requirements.txt` and update the two echo lines to say "requirements.txt (hash-locked)". Keep the sha256sum path, the remote `.requirements.sha256` handling and every other line unchanged.

Verify locally without the VPS (no checkpoint needed):
- `SCRATCH=$(mktemp -d)`; `uv venv --python 3.14 --seed "$SCRATCH/rt"`; `"$SCRATCH/rt/bin/pip" install --no-cache-dir --require-hashes -r server/requirements.txt` exits 0; `"$SCRATCH/rt/bin/python" -c "import PIL, requests"` exits 0; `"$SCRATCH/rt/bin/pip" list --format=freeze` contains no pytest/ruff/coverage/playwright.
- `uv venv --python 3.14 --seed "$SCRATCH/dev"`; `"$SCRATCH/dev/bin/pip" install --no-cache-dir --require-hashes -r server/requirements-dev.txt` exits 0; `"$SCRATCH/dev/bin/python" -m pytest --version` prints 9.1.1.
- Tamper test: copy server/requirements.txt to `$SCRATCH/tampered.txt`, flip one hex digit of the first `--hash=sha256:` of the requests pin, install into a fresh 3.14 venv with `--no-cache-dir --require-hashes -r "$SCRATCH/tampered.txt"`: must exit non-zero with "DO NOT MATCH THE HASHES" in the output.
- Record the 3.14 interpreter version used (RESEARCH Pitfall 4: the sandbox only has 3.14.0rc2; CI resolves the final 3.14.x in plan 32-14).
Remove `$SCRATCH` afterwards. Paste the three results in the SUMMARY.
  </action>
  <verify>
    <automated>bash -n deploy/deploy.sh && grep -q 'pip install --require-hashes --quiet -r ${APP_ROOT}/server/requirements.txt' deploy/deploy.sh && grep -q 'sha256sum "${REPO_ROOT}/server/requirements.txt"' deploy/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy/deploy.sh` exits 0
    - `grep -c -- '--require-hashes' deploy/deploy.sh` prints 1 and `grep -c 'requirements-dev' deploy/deploy.sh` prints 0
    - `git diff deploy/deploy.sh | grep -c '^[-+][^-+]'` prints at most 6 (only the install line and its two echo lines changed)
    - Clean 3.14 venv: `pip install --no-cache-dir --require-hashes -r server/requirements.txt` exits 0 and imports PIL and requests
    - Clean 3.14 venv: `pip install --no-cache-dir --require-hashes -r server/requirements-dev.txt` exits 0 and `python -m pytest --version` shows 9.1.1
    - Tampered copy: install exits non-zero and output contains `DO NOT MATCH THE HASHES`
  </acceptance_criteria>
  <done>Production install enforces hashes; both locks install cleanly on 3.14 and a tampered hash is rejected, all proven locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → production venv (VPS) | deploy.sh installs runtime packages on the production host |
| PyPI → CI venv | CI installs runtime + dev packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-SC | Tampering | dependency substitution / floating transitives (urllib3, certifi, idna, charset-normalizer) | mitigate | Hash-locked lock files for runtime and dev; `pip install --require-hashes` in deploy.sh (and in CI, plan 32-14); tamper test proves rejection |
| T-32-12-01 | Tampering | lock regenerated by hand-editing hashes | mitigate | `scripts/lock-deps.sh` is the only documented way; lock headers name it; hashes never hand-edited |
| T-32-12-02 | Elevation of Privilege | dev tooling reaching production | mitigate | Runtime lock contains no dev package (grep gate); deploy.sh never references requirements-dev |
| T-32-12-03 | Denial of Service | production install failing on the VPS (platform/wheel mismatch) | mitigate | Locks compiled for Python 3.14 x86_64 Linux and installed in a clean 3.14 venv with --require-hashes before shipping; deploy.sh unchanged otherwise |
</threat_model>

<verification>
- `bash -n deploy/deploy.sh && bash -n scripts/lock-deps.sh` exits 0
- Clean-venv and tamper results recorded in the SUMMARY
</verification>

<success_criteria>
TST-08: runtime and dev dependencies hash-locked with transitives; production install enforces hashes; verified without the VPS.
</success_criteria>

<output>
Create `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-12-SUMMARY.md` when done
</output>
