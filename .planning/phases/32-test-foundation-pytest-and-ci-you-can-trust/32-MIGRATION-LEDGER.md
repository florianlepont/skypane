# Phase 32 Migration Ledger

Generated by `32-ledger-check.py --assemble`. Do not hand-edit; re-run `--assemble` instead.

## Format

Each fragment lives at `32-ledger/<key>.md`, one per harness, with the header
`# Ledger: <harness>`, a `Baseline:` line naming the transcript and its check count, then a table:

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |

Disposition is one of `ported`, `deleted`, `pending`. A `|` inside a label is escaped as `\|`.

## Summary

| Harness | Baseline | Ported | Deleted | Fragment |
| --- | ---: | ---: | ---: | --- |
| server/test_calendar_rules.py | 113 | 113 | 0 | `32-ledger/server__test_calendar_rules.md` |
| server/test_colour_rules.py | 33 | 33 | 0 | `32-ledger/server__test_colour_rules.md` |
| server/test_config_history.py | 90 | 90 | 0 | `32-ledger/server__test_config_history.md` |
| server/test_dither.py | 6 | 6 | 0 | `32-ledger/server__test_dither.md` |
| server/test_enrich.py | 60 | 60 | 0 | `32-ledger/server__test_enrich.md` |
| server/test_illustrations.py | 60 | 60 | 0 | `32-ledger/server__test_illustrations.md` |
| server/test_manual_resolutions.py | 23 | 23 | 0 | `32-ledger/server__test_manual_resolutions.md` |
| server/test_notify.py | 8 | 8 | 0 | `32-ledger/server__test_notify.md` |
| server/test_panel_preview.py | 11 | 11 | 0 | `32-ledger/server__test_panel_preview.md` |
| server/test_pipeline_e2e.py | 7 | 7 | 0 | `32-ledger/server__test_pipeline_e2e.md` |
| server/test_plane_detection.py | 47 | 47 | 0 | `32-ledger/server__test_plane_detection.md` |
| server/test_poll_loop.py | 110 | 110 | 0 | `32-ledger/server__test_poll_loop.md` |
| server/test_render.py | 140 | 140 | 0 | `32-ledger/server__test_render.md` |
| server/test_runway_config.py | 15 | 15 | 0 | `32-ledger/server__test_runway_config.md` |
| stub-server/test_poll_cycle.py | 46 | 46 | 0 | `32-ledger/stub-server__test_poll_cycle.md` |

Grand totals: 769 baseline checks, 769 ported, 0 deleted.

## server/test_calendar_rules.py

# Ledger: server/test_calendar_rules.py

Baseline: 32-BASELINE/server__test_calendar_rules.txt (113 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | unfold_ics_lines() rejoins a SPACE continuation with the marker removed and nothing else lost | ported | server/test_calendar_rules.py::test_unfold_space_continuation |
| 2 | unfold_ics_lines() rejoins a HTAB continuation with the marker removed and nothing else lost | ported | server/test_calendar_rules.py::test_unfold_htab_continuation |
| 3 | unfold_ics_lines() folds three physical lines (a continuation of a continuation) into one logical line | ported | server/test_calendar_rules.py::test_unfold_three_physical_lines |
| 4 | unfold_ics_lines() round-trips a synthetic SUMMARY over 75 octets folded across two physical lines to its exact original value | ported | server/test_calendar_rules.py::test_unfold_long_summary_round_trips |
| 5 | unfold_ics_lines() on text whose very first line is a continuation does not raise and keeps that line as its own | ported | server/test_calendar_rules.py::test_unfold_leading_continuation_does_not_raise |
| 6 | unfold_ics_lines() unfolds the fixture's CRLF form to the identical list as its committed LF form | ported | server/test_calendar_rules.py::test_unfold_crlf_matches_lf |
| 7 | unfold_ics_lines() returns an empty list for non-string input (None, an int) without raising | ported | server/test_calendar_rules.py::test_unfold_non_string_input |
| 8 | split_property() on a parameter-bearing line yields the bare uppercased name and a value with no parameter residue | ported | server/test_calendar_rules.py::test_split_property_strips_parameters |
| 9 | split_property() partitions on the FIRST colon only, keeping a value's own embedded colon intact | ported | server/test_calendar_rules.py::test_split_property_value_with_colon |
| 10 | split_property() on a line with no colon at all returns (None, None, None) rather than raising | ported | server/test_calendar_rules.py::test_split_property_no_colon |
| 11 | the fixture's own X-SKYPANE-FIXTURE-EXPECTED-ENTRIES property agrees with this harness's FIXTURE_EXPECTED_ENTRIES constant | ported | server/test_calendar_rules.py::test_fixture_ledger_agrees_with_harness_constant |
| 12 | parse_ics_events() on the committed fixture returns exactly FIXTURE_EXPECTED_ENTRIES entries | ported | server/test_calendar_rules.py::test_fixture_parses_to_expected_count |
| 13 | no entry parsed from the fixture carries a 19th-century timestamp - the two STATUS:CANCELLED placeholder events were dropped | ported | server/test_calendar_rules.py::test_no_nineteenth_century_entries |
| 14 | no entry derives from the OFFD, CAHC or CPBL blocks - every route matches one of the four flight events and the count is exactly four | ported | server/test_calendar_rules.py::test_no_entries_from_non_flight_categories |
| 15 | parse_ics_datetime() returns a float epoch for the bare-UTC form | ported | server/test_calendar_rules.py::test_parse_ics_datetime_accepts_bare_utc |
| 16 | parse_ics_datetime() returns None for a VALUE=DATE value, a TZID-qualified value, a lowercase-z value, an empty string, a non-string, and a calendrically impossible value | ported | server/test_calendar_rules.py::test_parse_ics_datetime_rejection_sweep |
| 17 | the TZID tripwire is rejected loudly (a non-zero date-form rejection count on stderr) while the entry count stays four and no eight-digit date leaks | ported | server/test_calendar_rules.py::test_tzid_tripwire_rejected_loudly_without_leaking_a_date |
| 18 | parse_ics_events() never raises and returns an empty list for seven hostile bodies (empty, None, non-string, unterminated block, punctuation, no-colon lines, decoded random bytes) | ported | server/test_calendar_rules.py::test_parse_ics_events_never_raises |
| 19 | parse_ics_events() on a body with more VEVENT blocks than CALENDAR_MAX_RAW_EXAMINED returns exactly CALENDAR_MAX_RAW_EXAMINED entries | ported | server/test_calendar_rules.py::test_bounded_output_at_max_raw_examined |
| 20 | UAT-02: a feed listing more than CALENDAR_MAX_ENTRIES historical VEVENTs before a small number of in-window ones still surfaces every in-window entry through parse_ics_events() + select_window_entries() - the exact real-world failure reproduced | ported | server/test_calendar_rules.py::test_feed_history_before_window_still_surfaces_window_entries |
| 21 | every returned entry has exactly the five expected keys, and the serialised list carries no flight number, UID, or description text | ported | server/test_calendar_rules.py::test_record_shape_carries_no_schedule_text |
| 22 | D-01: writing and re-writing the calendar registry never touches colour_rules.json's content or bytes, and the two registries are distinct files | ported | server/test_calendar_rules.py::test_d01_calendar_never_touches_colour_rules |
| 23 | write_calendar_registry() replaces the whole file - a shorter second write leaves only that list, never a merge with the earlier one | ported | server/test_calendar_rules.py::test_d03_whole_file_rewrite_never_merges |
| 24 | write_calendar_registry([]) empties the registry rather than leaving the previous entries behind, with both timestamps still readable | ported | server/test_calendar_rules.py::test_d03_empty_write_empties_the_window |
| 25 | select_window_entries() keeps an already-landed-earlier-today entry, a later-today entry and a 47h-ahead entry, drops one ended yesterday and one 49h ahead, sorted ascending, without mutating its input | ported | server/test_calendar_rules.py::test_rolling_window_keeps_and_drops_the_expected_entries |
| 26 | select_window_entries() excludes an entry carrying a 19th-century timestamp for a present-day now | ported | server/test_calendar_rules.py::test_rolling_window_excludes_nineteenth_century_junk |
| 27 | load_calendar_registry() on a file holding more than CALENDAR_MAX_ENTRIES well-formed entries returns exactly CALENDAR_MAX_ENTRIES of them and prints a drop-count warning on stderr | ported | server/test_calendar_rules.py::test_load_caps_at_max_entries_with_a_warning |
| 28 | UAT-02: load_calendar_registry() on a raw entries list holding more than CALENDAR_MAX_ENTRIES out-of-window entries before a small number of in-window ones still surfaces every in-window entry - the exact real-world failure reproduced at the registry layer | ported | server/test_calendar_rules.py::test_registry_history_before_window_still_surfaces_window_entries |
| 29 | UF-16-05 closure: _resolve_retention_now() resolves None, a bool, a non-numeric value, NaN/+-Infinity, and a finite-but-absurd value (1e300) all to a real convertible clock, so none of them can erase a populated registry through load_calendar_registry() | ported | server/test_calendar_rules.py::test_resolve_retention_now_never_erases_a_populated_registry |
| 30 | load_calendar_registry() drops every one of eight hostile entry shapes (non-dict, short, over-long, lowercase airport, 3-char airline, string timestamp, boolean timestamp, inverted time pair) and raises nothing | ported | server/test_calendar_rules.py::test_load_drops_every_hostile_entry_shape |
| 31 | load_calendar_registry() degrades non-JSON bytes, a JSON array, a JSON string, and a dict whose entries is an integer all to the documented empty shape | ported | server/test_calendar_rules.py::test_load_degrades_to_empty_shape_for_hostile_files |
| 32 | calendar_fetch_is_due() returns the exact expected verdict for never-fetched, just-fetched, one-second-before/-after the interval, a future timestamp, a string, and a boolean - a broken feed must never be contacted on every 30-second cycle | ported | server/test_calendar_rules.py::test_throttle_exact_verdicts |
| 33 | the two persisted timestamps survive a round trip with distinct types (a number and an ISO string), and calendar_fetch_is_due() consults only last_attempt_at - changing last_synced_at alone never changes its verdict, changing last_attempt_at does | ported | server/test_calendar_rules.py::test_two_timestamps_are_distinct_in_type_and_role |
| 34 | a distinctive token set in the calendar secret file appears in neither calendar_rules.json's bytes, the loaded dict's serialisation, nor anything printed to stderr during a round trip | ported | server/test_calendar_rules.py::test_secret_never_reaches_the_file_or_the_log |
| 35 | _url_is_safe() refuses a non-https scheme (http, ftp, file, and a schemeless string) | ported | server/test_calendar_rules.py::test_scheme_gate_refuses_non_https |
| 36 | _url_is_safe() refuses a URL with no hostname | ported | server/test_calendar_rules.py::test_no_hostname_refused |
| 37 | _url_is_safe() refuses loopback (v4/v6), three private ranges, the link-local metadata address, and a reserved address | ported | server/test_calendar_rules.py::test_address_gate_refuses_every_unsafe_range |
| 38 | _url_is_safe() refuses a hostname that fails to resolve at all | ported | server/test_calendar_rules.py::test_unresolvable_hostname_refused |
| 39 | _url_is_safe() refuses a hostname whose resolved addresses are a MIX of public and private - the DNS-rebinding case a hostname-only check would miss | ported | server/test_calendar_rules.py::test_mixed_address_answer_refused |
| 40 | _url_is_safe() accepts the same hostname when every resolved address is public | ported | server/test_calendar_rules.py::test_all_public_answer_accepted |
| 41 | fetch_ics() refuses a redirect whose Location targets a loopback address, without ever fetching it | ported | server/test_calendar_rules.py::test_redirect_to_loopback_refused |
| 42 | fetch_ics() returns nothing for a redirect response with no Location header | ported | server/test_calendar_rules.py::test_redirect_without_location_returns_none |
| 43 | fetch_ics() resolves a relative Location against the current URL and re-validates it before following it | ported | server/test_calendar_rules.py::test_relative_redirect_resolved_and_refetched |
| 44 | fetch_ics() gives up after CALENDAR_MAX_REDIRECTS + 1 transport calls rather than looping forever | ported | server/test_calendar_rules.py::test_redirect_chain_bounded |
| 45 | fetch_ics() refuses a body one byte over CALENDAR_MAX_RESPONSE_BYTES | ported | server/test_calendar_rules.py::test_oversized_body_refused |
| 46 | fetch_ics() refuses an oversized body even when it declares a Content-Length of 1 - the cap is on streamed bytes, never the declared length | ported | server/test_calendar_rules.py::test_oversized_body_with_lying_content_length_refused |
| 47 | fetch_ics() accepts a body exactly at CALENDAR_MAX_RESPONSE_BYTES | ported | server/test_calendar_rules.py::test_body_exactly_at_cap_accepted |
| 48 | fetch_ics() returns nothing for a non-200 final status | ported | server/test_calendar_rules.py::test_non_200_status_refused |
| 49 | fetch_ics() invokes the transport with CALENDAR_FETCH_TIMEOUT_S as the timeout argument by default | ported | server/test_calendar_rules.py::test_default_timeout_passed_to_transport |
| 50 | across seven distinct fetch_ics() failure paths - including a transport exception whose message is the full secret URL - neither the token, the host, the path, nor the query-parameter name reaches stderr | ported | server/test_calendar_rules.py::test_secret_never_reaches_stderr_across_seven_failure_paths |
| 51 | neither the calendar URL's token, host, path, nor query-parameter name appears in calendar_rules.json after a full successful refresh_calendar_registry() cycle | ported | server/test_calendar_rules.py::test_secret_absent_from_persisted_registry_after_success |
| 52 | refresh_calendar_registry() makes no transport call and writes no last_attempt_at when the feature is unconfigured | ported | server/test_calendar_rules.py::test_refresh_unconfigured_makes_no_call_and_writes_nothing |
| 53 | refresh_calendar_registry() makes no transport call and prints nothing across twenty throttled cycles | ported | server/test_calendar_rules.py::test_refresh_throttled_path_is_silent |
| 54 | eleven refresh_calendar_registry() calls spaced 30 seconds apart perform exactly one transport call | ported | server/test_calendar_rules.py::test_refresh_throttle_holds_across_eleven_cycles |
| 55 | a failed refresh_calendar_registry() after a success moves last_attempt_at, leaves last_synced_at, and leaves the persisted entries unchanged | ported | server/test_calendar_rules.py::test_refresh_failure_after_success_preserves_the_window |
| 56 | a successful refresh_calendar_registry() cycle writes a windowed parse of the committed fixture, matching what is then readable on disk | ported | server/test_calendar_rules.py::test_refresh_success_writes_the_fixtures_windowed_parse |
| 57 | a successful fetch of a feed with nothing in the window reports FETCH_OK with an empty entry list, distinguished from a failure only by last_synced_at having moved | ported | server/test_calendar_rules.py::test_empty_window_success_distinguished_only_by_last_synced_at |
| 58 | refresh_calendar_registry() never raises - an unwritable registry path, a punctuation body, a raising transport, and a redirect-looping transport all return a result code | ported | server/test_calendar_rules.py::test_refresh_never_raises |
| 59 | match_calendar_theme() matches a departing detection whose route destination, airline and time all agree with an entry | ported | server/test_calendar_rules.py::test_match_truth_table_departure |
| 60 | match_calendar_theme() matches an arriving detection whose route origin, airline and time all agree with an entry | ported | server/test_calendar_rules.py::test_match_truth_table_arrival |
| 61 | match_calendar_theme() does not match when only the far-end airport differs | ported | server/test_calendar_rules.py::test_match_truth_table_different_far_end |
| 62 | match_calendar_theme() does not match when only the airline differs | ported | server/test_calendar_rules.py::test_match_truth_table_different_airline |
| 63 | match_calendar_theme() does not match when the time is just outside CALENDAR_MATCH_TOLERANCE_S | ported | server/test_calendar_rules.py::test_match_truth_table_time_outside_tolerance |
| 64 | match_calendar_theme() matches when the time is just inside CALENDAR_MATCH_TOLERANCE_S | ported | server/test_calendar_rules.py::test_match_truth_table_time_inside_tolerance |
| 65 | match_calendar_theme() direction symmetry: an arriving detection is never matched against an entry's departure-side far end | ported | server/test_calendar_rules.py::test_direction_symmetry_arrival_rejects_departure_side_far_end |
| 66 | match_calendar_theme() direction symmetry: a departing detection is never matched against an entry's arrival-side far end | ported | server/test_calendar_rules.py::test_direction_symmetry_departure_rejects_arrival_side_far_end |
| 67 | match_calendar_theme() derives the airline from callsign_iata at runtime - a designator no static table could contain still matches (CORRECTION 1) | ported | server/test_calendar_rules.py::test_airline_derived_not_tabulated |
| 68 | match_calendar_theme() never matches an airline_only-shaped route (the production bug the field-presence test prevents) | ported | server/test_calendar_rules.py::test_airline_only_shaped_route_never_matches |
| 69 | match_calendar_theme() never matches a manual-shaped route - the same shape as airline_only, a different provenance | ported | server/test_calendar_rules.py::test_manual_shaped_route_never_matches |
| 70 | match_calendar_theme() never matches a None route (the miss case) | ported | server/test_calendar_rules.py::test_none_route_never_matches |
| 71 | match_calendar_theme() matches a detection near the fixture's own first same-route (BBB-ORY) entry | ported | server/test_calendar_rules.py::test_ambiguity_near_first |
| 72 | match_calendar_theme() matches a detection near the fixture's own second same-route (BBB-ORY) entry | ported | server/test_calendar_rules.py::test_ambiguity_near_second |
| 73 | match_calendar_theme() matches neither of the fixture's own same-route entries when the detection sits midway, outside both tolerances | ported | server/test_calendar_rules.py::test_ambiguity_midway_matches_neither |
| 74 | match_calendar_theme() returns the identical result whether the fixture's same-route entries are listed forward or reversed (determinism, not iteration order) | ported | server/test_calendar_rules.py::test_ambiguity_deterministic_regardless_of_list_order |
| 75 | match_calendar_theme() returns no match when device_cfg carries no calendar_theme_id | ported | server/test_calendar_rules.py::test_no_calendar_theme_id_saved_yields_no_match |
| 76 | match_calendar_theme() never returns a calendar_theme_id that is not a member of device_config.THEMES (T-16-TAMPER) | ported | server/test_calendar_rules.py::test_tampered_calendar_theme_id_never_returned |
| 77 | match_calendar_theme() never raises for a non-dict registry, non-list entries, malformed entries, a non-dict route, a non-dict device_cfg, a non-numeric now, or an unconfirmed render state | ported | server/test_calendar_rules.py::test_matcher_never_raises |
| 78 | parse_ics_events() keeps a flight whose VEVENT contains a nested VALARM, and the nested component's properties never reach the parent entry (CR-01) | ported | server/test_calendar_rules.py::test_nested_component_does_not_drop_the_event |
| 79 | _normalise_calendar_entry() rejects NaN/Infinity timestamps while still accepting a finite entry (CR-02) | ported | server/test_calendar_rules.py::test_non_finite_timestamps_rejected |
| 80 | T-16-PRIV's own reproduction: an entry that ended ~10 days ago is absent from the RAW on-disk file after every one of three consecutive FAILING refresh_calendar_registry() cycles, and the returned registry matches the raw file on every cycle (D-04) - the verification the original goal check missed | ported | server/test_calendar_rules.py::test_failing_refresh_trims_the_raw_file_across_consecutive_cycles |
| 81 | a hand-written file mixing one out-of-window and one in-window entry loads to the in-window entry only, and the read does not rewrite the file (D-01/D-02) | ported | server/test_calendar_rules.py::test_loader_applies_the_window_on_read_without_rewriting |
| 82 | a write containing a stale entry and a current entry puts only the current entry in the raw file (D-01/D-03) | ported | server/test_calendar_rules.py::test_writer_refuses_to_persist_what_the_window_would_drop |
| 83 | for any list and any now, the loader's entries are exactly select_window_entries(list, now) - the anti-drift guard that goes red if a second window implementation ever appears (D-02) | ported | server/test_calendar_rules.py::test_loader_output_is_exactly_select_window_entries |
| 84 | save_calendar_url() passes 0o600 as os.open()'s own mode argument when creating its temporary file - not merely a file that happens to read 0o600 later - and the mode still reads 0o600 at the moment of the atomic rename (T-17-MODE) | ported | server/test_calendar_rules.py::test_tmp_file_mode_is_0600_at_creation_time |
| 85 | save_calendar_url() never calls os.chmod() at all - the mode is set once, at file-creation time, never as a follow-up permission change (T-17-MODE) | ported | server/test_calendar_rules.py::test_writer_never_calls_chmod |
| 86 | save_calendar_url() writes mode 0600 under a process umask of 022 | ported | server/test_calendar_rules.py::test_final_mode_is_0600_under_umask_022 |
| 87 | save_calendar_url() writes mode 0600 under a process umask of 027 | ported | server/test_calendar_rules.py::test_final_mode_is_0600_under_umask_027 |
| 88 | save_calendar_url() leaves the destination at 0600 even when the rename lands on a pre-existing group-readable file (T-17-MODE) | ported | server/test_calendar_rules.py::test_rename_does_not_inherit_destination_mode |
| 89 | calendar_secret_mode_is_unsafe() reports True for a file created through this codebase's ordinary umask-inheriting house idiom (0o644) and for 0o640/0o604/0o660, and False for 0o600/0o400 - the negative check proving the guard actually fires, not merely that the correct writer is correct (T-17-DRIFT) | ported | server/test_calendar_rules.py::test_guard_flags_files_created_through_the_house_idiom |
| 90 | calendar_secret_mode_is_unsafe() is a genuine bool False (is False, not merely falsy, never None) on a state dir with no secret file | ported | server/test_calendar_rules.py::test_guard_is_false_and_a_genuine_bool_when_absent |
| 91 | save_calendar_url(CLEAR_CALENDAR_URL) removes the secret file, returns True, and returns True again when called a second time with the file already gone | ported | server/test_calendar_rules.py::test_clear_branch_removes_the_file_and_is_idempotent |
| 92 | save_calendar_url() erases the fetched registry - zero entries and both timestamps None - on setting a URL for the first time or replacing one with a different URL (D-05) | ported | server/test_calendar_rules.py::test_erase_on_set_or_replace_d05 |
| 93 | save_calendar_url(CLEAR_CALENDAR_URL) erases the fetched registry and removes the secret file in the same call (D-04) | ported | server/test_calendar_rules.py::test_erase_on_clear_d04 |
| 94 | save_calendar_url() returns False and leaves an existing secret file's bytes and mode, and a seeded registry, untouched for None, an empty string, a whitespace-only string and a non-string value | ported | server/test_calendar_rules.py::test_rejected_values_change_nothing |
| 95 | save_calendar_url() strips leading/trailing whitespace and a trailing newline, writing the bare URL with no trailing newline byte, at mode 0600 | ported | server/test_calendar_rules.py::test_content_round_trips_stripped |
| 96 | no file matching the temporary-name shape remains in state_dir after a successful write or a rejected call | ported | server/test_calendar_rules.py::test_no_temp_file_survives |
| 97 | passing the literal string 'CLEAR_CALENDAR_URL' takes the ordinary write branch, not the sentinel's clear branch (identity comparison only) | ported | server/test_calendar_rules.py::test_sentinel_is_identity_only |
| 98 | configured_calendar_url() returns exactly the value written through save_calendar_url(), and calendar_is_configured() is True (identity) for the same state_dir (D-03) | ported | server/test_calendar_rules.py::test_accessor_reads_the_secret_file |
| 99 | neither configured_calendar_url() nor calendar_is_configured() ever calls os.environ.get() - the check that would catch a resurrected environment read, with and without a secret file present (D-03) | ported | server/test_calendar_rules.py::test_environment_is_never_consulted |
| 100 | configured_calendar_url() refuses a secret file whose permissions have drifted, and never opens it at all - proved by observing the file was not opened, not merely by observing the return value (D-02, T-17-DRIFT) | ported | server/test_calendar_rules.py::test_drifted_file_refuses_without_being_opened |
| 101 | a secret file written through this codebase's ordinary umask-inheriting house idiom (plain open(path, 'w'), not save_calendar_url()'s explicit 0600) is refused end to end by configured_calendar_url() and calendar_is_configured() - the negative counterpart to F1, going through the accessor rather than the mode predicate alone | ported | server/test_calendar_rules.py::test_ordinary_umask_file_refused_end_to_end |
| 102 | calendar_is_configured() returns a genuine bool - is True, is False, is False - across absent, configured, and permission-drifted secret files, never a truthy status string (D-08) | ported | server/test_calendar_rules.py::test_boolean_contract_in_three_states |
| 103 | configured_calendar_url() strips a trailing newline from a hand-written secret file, returning the bare URL | ported | server/test_calendar_rules.py::test_accessor_strips_hand_written_trailing_newline |
| 104 | refresh_calendar_registry(min_interval_s=0) bypasses the throttle and reaches the transport even when the recorded last attempt is well inside the standard interval, while the default (no interval argument) still honours the throttle (D-06) | ported | server/test_calendar_rules.py::test_min_interval_s_bypasses_the_throttle |
| 105 | save_calendar_url(CLEAR_CALENDAR_URL) returns False, not True, when os.remove() fails for a reason other than the file already being absent - a permission/immutable-flag/read-only-filesystem failure must never be reported as a successful disconnect (CR-02) | ported | server/test_calendar_rules.py::test_clear_branch_reports_failure_when_removal_actually_fails |
| 106 | a companion disconnect arriving while a (simulated) concurrent poll cycle is mid-fetch cannot have its registry erase overwritten by that poll cycle's later write - the cross-process registry lock, not either in-process threading.Lock, is what is exercised here (CR-01; simulates two OS processes with two threads racing the real fcntl-based lock - see comment above) | ported | server/test_calendar_rules.py::test_cross_process_lock_closes_the_disconnect_race |
| 107 | save_calendar_url() replacing a connected calendar's URL leaves the previous calendar's already-fetched flights untouched when the new secret's write fails before ever reaching the registry erase (WR-01) | ported | server/test_calendar_rules.py::test_write_failure_before_erase_preserves_the_previous_calendars_flights |
| 108 | _normalise_calendar_url() rewrites a webcal scheme (any case) to https, leaves every other scheme and an unparseable value unchanged, and never raises on None (UAT) | ported | server/test_calendar_rules.py::test_normalise_calendar_url_rewrites_only_webcal |
| 109 | fetch_ics() accepts a webcal:// URL - Apple Calendar's own share-link scheme - and the transport observes an https:// request (UAT) | ported | server/test_calendar_rules.py::test_fetch_ics_accepts_webcal_as_https |
| 110 | the webcal:// rewrite does not weaken the address gate - fetch_ics() still refuses a webcal:// URL pointing at a private address, at loopback (literal IP and "localhost"), and at the cloud metadata address (UAT) | ported | server/test_calendar_rules.py::test_webcal_does_not_weaken_the_address_gate |
| 111 | fetch_ics() still refuses a plain http:// URL - there is no webcal -> http downgrade path (UAT) | ported | server/test_calendar_rules.py::test_http_still_refused_no_downgrade_path |
| 112 | a redirect discovered while fetching a webcal:// URL (normalised to https:// first) is still re-validated per hop, refusing a Location that targets a loopback address (UAT) | ported | server/test_calendar_rules.py::test_redirect_from_normalised_webcal_still_revalidated |
| 113 | save_calendar_url() stores a webcal:// value already rewritten to its https:// form, so the secret file never holds a webcal:// string (UAT) | ported | server/test_calendar_rules.py::test_save_calendar_url_stores_the_normalised_form |

## server/test_colour_rules.py

# Ledger: server/test_colour_rules.py

Baseline: 32-BASELINE/server__test_colour_rules.txt (33 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | load_colour_rules() on a nonexistent state dir returns the empty registry shape without raising | ported | server/test_colour_rules.py::test_missing_state_dir_returns_empty_registry_shape |
| 2 | load_colour_rules() on a file containing invalid JSON returns the empty registry shape | ported | server/test_colour_rules.py::test_invalid_json_returns_empty_registry_shape |
| 3 | load_colour_rules() on a JSON list (non-dict top level) returns the empty registry shape | ported | server/test_colour_rules.py::test_non_dict_top_level_returns_empty_registry_shape |
| 4 | load_colour_rules() on {'callsign': 5} (non-dict kind value) returns the empty registry shape | ported | server/test_colour_rules.py::test_non_dict_kind_value_returns_empty_registry_shape |
| 5 | load_colour_rules() normalises a lowercase callsign key to uppercase and round-trips a valid entry | ported | server/test_colour_rules.py::test_lowercase_callsign_key_normalised_on_read_and_round_trips |
| 6 | load_colour_rules() normalises a lowercase hex key to uppercase on read | ported | server/test_colour_rules.py::test_lowercase_hex_key_normalised_on_read |
| 7 | load_colour_rules() drops a path-traversal-shaped callsign key, a 4-letter prefix, a non-hex hex key, and an unregistered theme_id | ported | server/test_colour_rules.py::test_malformed_entries_dropped_on_read |
| 8 | add_rule() returns ADD_OK_NEW and round-trips through load_colour_rules() with a non-empty created_at | ported | server/test_colour_rules.py::test_add_rule_round_trips_with_non_empty_created_at |
| 9 | add_rule() rejects an unrecognised kind with ADD_REJECTED_KIND | ported | server/test_colour_rules.py::test_add_rule_rejects_unrecognised_kind |
| 10 | add_rule() rejects a 4-letter prefix and a 5-character hex value with ADD_REJECTED_KEY | ported | server/test_colour_rules.py::test_add_rule_rejects_bad_key |
| 11 | add_rule() rejects an unregistered theme id with ADD_REJECTED_THEME | ported | server/test_colour_rules.py::test_add_rule_rejects_unregistered_theme |
| 12 | delete_rule() returns True once then False on a repeated call, without raising | ported | server/test_colour_rules.py::test_delete_rule_returns_true_once_then_false |
| 13 | rule_rows() returns (kind, value, theme_id, created_at) tuples ordered by kind then value, skipping a malformed entry | ported | server/test_colour_rules.py::test_rule_rows_ordered_and_skips_malformed_entry |
| 14 | set_colour_rules_state_dir(None) clears the cache and resolve_effective_theme_id() falls through to the base theme | ported | server/test_colour_rules.py::test_cache_reset_to_none_falls_through_to_base_theme |
| 15 | add_rule() rejects every hostile value in the sweep (write-side) and load_colour_rules() drops the same shapes written directly to disk (read-side), for all three kinds (T-15-01) | ported | server/test_colour_rules.py::test_hostile_input_sweep_rejected_write_and_read |
| 16 | add_rule() distinguishes ADD_OK_NEW from ADD_OK_REPLACED (a replace is not growth) and enforces COLOUR_RULE_MAX_ENTRIES only against new keys (D-09) | ported | server/test_colour_rules.py::test_add_rule_distinguishes_new_from_replaced_and_enforces_cap |
| 17 | no colour_rules.json.tmp file remains after a successful add_rule() or delete_rule() (atomicity proof) | ported | server/test_colour_rules.py::test_no_stray_tmp_file_after_add_and_delete |
| 18 | 20 concurrent add_rule() calls for 20 distinct prefixes (ThreadingHTTPServer's real concurrency shape) all persist durably with no lost update and no stray .tmp file left behind (T-15-02) | ported | server/test_colour_rules.py::test_concurrent_add_rule_calls_lose_no_updates |
| 19 | resolver truth table row 1: no rule, theme_arriving is None, state 'arriving' -> base theme | ported | server/test_colour_rules.py::test_resolver_row1_no_rule_no_override_arriving |
| 20 | resolver truth table row 2: no rule, theme_arriving set, state 'arriving' -> the override | ported | server/test_colour_rules.py::test_resolver_row2_no_rule_override_arriving |
| 21 | resolver truth table row 3: no rule, theme_arriving set, state 'departing' -> base theme | ported | server/test_colour_rules.py::test_resolver_row3_no_rule_override_departing |
| 22 | resolver truth table row 4: a matching prefix rule, no override -> the rule's theme | ported | server/test_colour_rules.py::test_resolver_row4_prefix_rule_no_override |
| 23 | resolver truth table row 5: a matching hex rule and a matching prefix rule -> the hex rule's theme | ported | server/test_colour_rules.py::test_resolver_row5_hex_beats_prefix |
| 24 | resolver truth table row 6: a matching callsign rule and a matching hex rule -> the callsign rule's theme | ported | server/test_colour_rules.py::test_resolver_row6_callsign_beats_hex |
| 25 | resolver truth table row 7: a matching rule of any kind AND theme_arriving set with state 'arriving' -> the rule's theme (a rule beats the override, D-09) | ported | server/test_colour_rules.py::test_resolver_row7_rule_beats_override |
| 26 | resolve_effective_theme_id() never raises for flight=None/{}/non-dict, a None callsign/hex, or a device_cfg with no theme_arriving key, falling through to the base theme | ported | server/test_colour_rules.py::test_resolver_never_raises_on_defensive_inputs |
| 27 | resolve_effective_theme_id() ignores a cached entry whose theme_id is not a member of device_config.THEMES rather than returning it (T-15-05) | ported | server/test_colour_rules.py::test_resolver_ignores_tampered_cache_theme_id |
| 28 | resolve_effective_theme_id() D-02: a calendar_theme_id beats even a matching exact-callsign rule (the accepted consequence) | ported | server/test_colour_rules.py::test_calendar_beats_exact_callsign_rule |
| 29 | resolve_effective_theme_id() D-02: a calendar_theme_id beats a matching hex rule | ported | server/test_colour_rules.py::test_calendar_beats_hex_rule |
| 30 | resolve_effective_theme_id() D-02: a calendar_theme_id beats a matching prefix rule | ported | server/test_colour_rules.py::test_calendar_beats_prefix_rule |
| 31 | resolve_effective_theme_id() D-02: a calendar_theme_id beats the arrivals override (theme_arriving) on an arriving state | ported | server/test_colour_rules.py::test_calendar_beats_arrivals_override |
| 32 | resolve_effective_theme_id() is byte-for-byte identical whether calendar_theme_id is omitted or passed explicitly as None, across a matrix of rule/override configurations | ported | server/test_colour_rules.py::test_backward_compatible_three_positional_call |
| 33 | resolve_effective_theme_id() ignores a calendar_theme_id that is not a member of device_config.THEMES, falling through to the matching rule rather than returning it or the base theme (T-16-TAMPER) | ported | server/test_colour_rules.py::test_tampered_calendar_theme_id_ignored |

## server/test_config_history.py

# Ledger: server/test_config_history.py

Baseline: 32-BASELINE/server__test_config_history.txt (90 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | load_device_config() on a missing state directory returns the documented defaults | ported | server/test_config_history.py::test_missing_state_dir_yields_defaults |
| 2 | load_device_config() on a JSON array, a truncated document, or a non-dict returns defaults instead of raising | ported | server/test_config_history.py::test_malformed_file_yields_defaults |
| 3 | load_device_config() replaces an unrecognised theme/runway value with the default rather than passing it through | ported | server/test_config_history.py::test_hostile_values_yield_defaults |
| 4 | save_device_config() followed by load_device_config() round-trips the saved theme and tracked_runway | ported | server/test_config_history.py::test_save_then_load_round_trips |
| 5 | save_device_config() with an unknown theme id raises ValueError and leaves the state directory file-free | ported | server/test_config_history.py::test_unknown_theme_rejected_without_touching_file |
| 6 | no .tmp file remains in the state directory after a successful save | ported | server/test_config_history.py::test_no_tmp_survives_a_successful_save |
| 7 | a legitimately saved config, hand-edited on disk to hostile values, still yields defaults from load_device_config() | ported | server/test_config_history.py::test_hostile_hand_edit_after_a_real_save_still_yields_defaults |
| 8 | normalise_led_enabled() returns the value only for real bools and degrades a string, int 0, int 1, None, and a list to DEFAULT_LED_ENABLED | ported | server/test_config_history.py::test_normalise_led_enabled_only_accepts_real_bools |
| 9 | save_device_config(led_enabled=False) round-trips through load_device_config() as False, with theme/tracked_runway still at their defaults | ported | server/test_config_history.py::test_save_led_enabled_false_round_trips |
| 10 | a hand-written device_config.json whose led_enabled is a hostile string yields DEFAULT_LED_ENABLED from load_device_config() | ported | server/test_config_history.py::test_hand_written_hostile_led_enabled_yields_default |
| 11 | save_device_config(led_enabled='off') raises ValueError and leaves a pre-existing, legitimately-saved file byte-identical | ported | server/test_config_history.py::test_save_led_enabled_off_rejected_without_touching_file |
| 12 | a subsequent theme-only save_device_config(theme='black') carries a previously-saved led_enabled=False forward unchanged | ported | server/test_config_history.py::test_theme_only_save_carries_led_enabled_false_forward |
| 13 | every THEMES entry carries exactly its contract keys (6 for non-band, 8 for band), real panel_format.IDX_* index values, a non-empty label, a bool dithered flag, a regular/bold weight, and (band entries only) a real band_index plus a bool band_dithered | ported | server/test_config_history.py::test_theme_registry_shape_is_correct |
| 14 | every registered theme is single-colour (departing_index == arriving_index); the retired two-tone 'sky' theme is gone | ported | server/test_config_history.py::test_every_theme_is_single_colour |
| 15 | every one of the 18 registered themes carries the exact background/ink pairing expected, pinned as an explicit id-to-(background,ink) mapping | ported | server/test_config_history.py::test_ink_contrast_pairing_is_correct |
| 16 | every registered theme's base-canvas dithered/weight pair matches the on-glass-confirmed values, including Yellow Light's Regular exception, the 5 White-base band themes, and the 2 tinted-field band themes' blue_light/red_light-derived values | ported | server/test_config_history.py::test_dithered_and_weight_contract_is_correct |
| 17 | DEFAULT_THEME_ID is 'white' and a THEMES member; theme_label() returns the exact plain label for all 18 ids | ported | server/test_config_history.py::test_default_theme_and_labels_are_correct |
| 18 | theme_is_band() returns True for exactly the ids device_config.THEMES itself marks as band entries (band_index present) and False for every other registered id | ported | server/test_config_history.py::test_theme_is_band_matches_registry_band_ids |
| 19 | theme_band_index() returns THEMES's own band_index for every band id (the exact spike-confirmed IDX_* per colour) and None for every non-band id | ported | server/test_config_history.py::test_theme_band_index_matches_registry_or_none |
| 20 | theme_band_dithered() returns THEMES's own band_dithered for every band id and False for every non-band id | ported | server/test_config_history.py::test_theme_band_dithered_matches_registry_or_false |
| 21 | band_blue_field/band_red_field are genuinely tone-on-tone (band_index equals departing_index equals arriving_index), dithered=True/band_dithered=False, and their base-canvas quadruple matches their tinted-field sibling's (blue_light/red_light) own registry row, read live rather than hardcoded | ported | server/test_config_history.py::test_tinted_field_band_themes_are_tone_on_tone |
| 22 | load_device_config() replaces hostile quiet_hours_enabled/quiet_hours_start/quiet_hours_end values with their documented defaults | ported | server/test_config_history.py::test_hostile_quiet_hours_values_yield_defaults |
| 23 | save_device_config(quiet_hours_enabled=True, quiet_hours_start='22:30', quiet_hours_end='06:15') round-trips through load_device_config() with theme/tracked_runway/led_enabled still at their prior (default) values | ported | server/test_config_history.py::test_save_quiet_hours_round_trips |
| 24 | save_device_config(quiet_hours_start='24:00') raises ValueError and leaves a pre-existing, legitimately-saved file byte-identical | ported | server/test_config_history.py::test_save_quiet_hours_start_invalid_rejected_without_touching_file |
| 25 | save_device_config(quiet_hours_enabled='on') raises ValueError | ported | server/test_config_history.py::test_save_quiet_hours_enabled_non_bool_rejected |
| 26 | normalise_quiet_hours_time() rejects an unpadded hour, an out-of-range minute, midnight-as-24:00, an empty string, None, an int, and a trailing newline - returning the supplied default for each | ported | server/test_config_history.py::test_normalise_quiet_hours_time_rejects_every_hostile_shape |
| 27 | seconds_until_quiet_hours_end() returns the verified wrap-midnight/DST anchors: 28000 mid-window, None just past end, 23400 across spring-forward (1h less than naive), 30600 across autumn fall-back (1h more than naive) | ported | server/test_config_history.py::test_wrap_midnight_window_returns_verified_dst_values |
| 28 | seconds_until_quiet_hours_end() handles a same-day (non-wrapping) window correctly and returns None for a zero-width start_hm == end_hm window | ported | server/test_config_history.py::test_same_day_window_and_zero_width_window |
| 29 | quiet_hours_status() returns (None, None) when quiet_hours_enabled is False, and (28000, '07:00') for an enabled 23:00-07:00 config at the verified anchor epoch | ported | server/test_config_history.py::test_quiet_hours_status_enabled_gate_and_verified_value |
| 30 | quiet_hours_status() returns (None, None) and never raises for a non-numeric string, None, NaN, or an absurdly large now_epoch | ported | server/test_config_history.py::test_quiet_hours_status_never_raises_for_hostile_now_epoch |
| 31 | normalise_wake_interval_s() returns None for True, False (the bool-is-an-int gotcha), a numeric string, a float, and every out-of-[60, 3600] int, and returns 60/3600/120 unchanged | ported | server/test_config_history.py::test_normalise_wake_interval_s_bounds_and_bool_gotcha |
| 32 | load_device_config() degrades a hand-written hostile wake_interval_s (JSON true, a numeric string, a float, or the below-minimum 30) to None, and lets an in-range 120 survive unchanged | ported | server/test_config_history.py::test_hand_written_hostile_wake_interval_s_yields_none |
| 33 | save_device_config(wake_interval_s=120) round-trips through load_device_config() as 120, with theme/tracked_runway/led_enabled and all three quiet-hours fields still at their prior (default) values | ported | server/test_config_history.py::test_save_wake_interval_s_round_trips |
| 34 | save_device_config() rejects wake_interval_s=59, 3601, True, False, '120', and 120.0 with ValueError, leaves a pre-existing, legitimately-saved file byte-identical across every rejection, and accepts the inclusive bounds 60 and 3600 | ported | server/test_config_history.py::test_save_wake_interval_s_rejects_out_of_bounds_and_bools |
| 35 | a subsequent theme-only save_device_config(theme='black') carries a previously-saved wake_interval_s=120 forward unchanged | ported | server/test_config_history.py::test_wake_interval_s_carries_forward_on_unrelated_save |
| 36 | normalise_theme_arriving() degrades a hostile string, an int, a bool, an empty string, a dict, and None to None - never to DEFAULT_THEME_ID, the wrong-shaped answer normalise_theme_id() would give - and returns a real THEMES member unchanged | ported | server/test_config_history.py::test_normalise_theme_arriving_degrades_hostile_values_never_to_default |
| 37 | CLEAR_THEME_ARRIVING is a distinct object - not None, and neither equal to nor a member of any registered theme id, so it can never collide with a real theme id or with unset | ported | server/test_config_history.py::test_clear_theme_arriving_sentinel_is_distinct_from_none_and_any_theme_id |
| 38 | load_device_config() degrades a hand-written hostile theme_arriving (an unregistered string, a JSON int, or JSON null) to None, never to DEFAULT_THEME_ID | ported | server/test_config_history.py::test_hand_written_hostile_theme_arriving_yields_none_never_default |
| 39 | a device_config.json written before this phase (no theme_arriving key at all) loads every stored key unchanged, resolves theme_arriving to None, and is not rewritten on disk by load_device_config() - no migration | ported | server/test_config_history.py::test_pre_phase_14_file_has_no_theme_arriving_migration |
| 40 | save_device_config()'s theme_arriving honours its three-state contract end to end: set, carry-forward-on-omission, CLEAR_THEME_ARRIVING clears to None without disturbing theme, and a non-member value raises ValueError leaving the file byte-identical | ported | server/test_config_history.py::test_theme_arriving_three_state_write_contract |
| 41 | normalise_display_enabled() degrades int 0, int 1, 'true', 'on', an empty string, None, an empty list, and an empty dict to DEFAULT_DISPLAY_ENABLED, and returns both real booleans unchanged | ported | server/test_config_history.py::test_normalise_display_enabled_only_accepts_real_bools |
| 42 | load_device_config() degrades a hand-written hostile display_enabled (0, "false", null, or a non-dict document) to True so a corrupted config can never darken the frame (D-09), while a legitimate display_enabled=false survives as False | ported | server/test_config_history.py::test_hand_written_hostile_display_enabled_yields_true_but_false_survives |
| 43 | save_device_config(display_enabled=False) round-trips through load_device_config() as False with all seven other fields still at their prior (default) values, and a subsequent theme-only save carries display_enabled=False forward unchanged | ported | server/test_config_history.py::test_save_display_enabled_false_round_trips_and_carries_forward |
| 44 | save_device_config() rejects display_enabled='on', 0, 1, 'false', and [] with ValueError and leaves a pre-existing, legitimately-saved file byte-identical across every rejection (display_enabled=None is not a rejection case - it means carry forward) | ported | server/test_config_history.py::test_save_display_enabled_rejects_non_bool_and_leaves_file_byte_identical |
| 45 | load_device_config() resolves calendar_theme_id to None both for a missing state dir and for a config saved before this key was ever supplied - a pre-existing deployed config must not break | ported | server/test_config_history.py::test_calendar_theme_id_absent_key_defaults_to_none |
| 46 | save_device_config(calendar_theme_id=<a registered THEME_IDS member>) round-trips through load_device_config() unchanged | ported | server/test_config_history.py::test_calendar_theme_id_round_trips |
| 47 | load_device_config() degrades a hand-written hostile calendar_theme_id (an unregistered string, the empty string, a JSON int, bool, list, dict, or null) to None, provably never to DEFAULT_THEME_ID (T-16-TAMPER) | ported | server/test_config_history.py::test_hand_written_hostile_calendar_theme_id_yields_none_never_default |
| 48 | save_device_config(calendar_theme_id=<non-member>) raises ValueError and leaves a pre-existing, legitimately-saved file byte-identical | ported | server/test_config_history.py::test_save_calendar_theme_id_rejects_non_member_byte_identical |
| 49 | a save that supplies only calendar_theme_id leaves theme/theme_arriving/tracked_runway/led_enabled/quiet-hours/wake_interval_s/display_enabled at their stored values, and a save that supplies only theme leaves an already-stored calendar_theme_id intact | ported | server/test_config_history.py::test_calendar_theme_id_carries_forward_in_both_directions |
| 50 | clearing theme_arriving through CLEAR_THEME_ARRIVING leaves a separately-set calendar_theme_id untouched - the two override keys are not coupled by the shared write path | ported | server/test_config_history.py::test_calendar_theme_id_independent_of_theme_arriving |
| 51 | normalise_screen_id() degrades a hostile/unknown/None/non-string value to DEFAULT_SCREEN_ID and passes a real member through unchanged | ported | server/test_config_history.py::test_normalise_screen_id_degrades_hostile_values_to_default |
| 52 | save_device_config(screen_id='nope') raises ValueError naming SCREEN_IDS and leaves a pre-existing file byte-identical | ported | server/test_config_history.py::test_save_device_config_rejects_unknown_screen_id_without_touching_file |
| 53 | a device_config.json written with no screen_id key loads with DEFAULT_SCREEN_ID and is never rewritten on read | ported | server/test_config_history.py::test_screen_id_absent_from_disk_resolves_to_default_with_no_migration |
| 54 | device_config.SCREEN_IDS/DEFAULT_SCREEN_ID stay pinned equal to companion.screens's own duplicated-not-imported values | ported | server/test_config_history.py::test_screen_id_registry_agrees_with_companion_screens |
| 55 | a device_config.json written before this phase (no notifications key at all) resolves notifications to DEFAULT_NOTIFICATIONS and is not rewritten on disk by load_device_config() - no migration | ported | server/test_config_history.py::test_notifications_absent_from_disk_resolves_to_default_with_no_migration |
| 56 | normalise_notifications() degrades a non-dict value ('x', [], 7, None) to a copy of DEFAULT_NOTIFICATIONS wholesale, and degrades each hostile sub-field ({'lang': 'de'}, or a wrong-typed topic_url/battery_low/frame_silent) independently while a real sibling field survives | ported | server/test_config_history.py::test_normalise_notifications_degrades_hostile_shapes_per_field |
| 57 | save_device_config(notifications={...}) round-trips through load_device_config() unchanged, leaving every sibling field at its prior (default) value | ported | server/test_config_history.py::test_save_notifications_round_trips |
| 58 | save_device_config() rejects a non-dict notifications value, a non-str topic_url, a non-bool battery_low/frame_silent, and a lang outside ('en', 'fr') with ValueError, leaving a pre-existing, legitimately-saved file byte-identical across every rejection | ported | server/test_config_history.py::test_save_notifications_rejects_every_malformed_shape |
| 59 | server/device_config.py introduces no print()/logging call for the notifications group, preserving this module's own print-free-by-design contract (T-20-12: a topic_url can never reach a log this module controls) | ported | server/test_config_history.py::test_saved_topic_url_never_appears_in_a_rejected_writes_bytes_or_this_modules_own_source |
| 60 | connect() creates history.db, sets WAL + busy_timeout, creates all three tables, and is idempotent | ported | server/test_config_history.py::test_connect_creates_db_with_wal_and_tables |
| 61 | record_runway_event() inserts one row; recent_runway_events(limit=2) returns the two newest, newest first | ported | server/test_config_history.py::test_record_and_recent_runway_events |
| 62 | route_source_counts(since=...) returns fresh_hit/cache_hit/miss with counts 1/2/1 | ported | server/test_config_history.py::test_route_source_counts_buckets_correctly |
| 63 | corroboration_counts(since=...) buckets True/None/False separately, never collapsing None into False | ported | server/test_config_history.py::test_corroboration_counts_keeps_none_distinct_from_false |
| 64 | a runway_events row written with corroborated unknown is readable back as the unknown value, distinct from false | ported | server/test_config_history.py::test_corroborated_unknown_is_readable_back_distinctly |
| 65 | a callsign containing an HTML angle bracket and a SQL quote round-trips byte-identically through recent_runway_events() | ported | server/test_config_history.py::test_hostile_callsign_round_trips_byte_identically |
| 66 | set_meta()/get_meta() round-trip, absent key reads None, a second set_meta() overwrites rather than duplicating | ported | server/test_config_history.py::test_meta_get_set_overwrites_not_duplicates |
| 67 | ingest_caddy_battery_log() inserts exactly 2 rows from a mixed fixture, and 0 more on an unchanged re-run | ported | server/test_config_history.py::test_ingest_caddy_battery_log_is_idempotent |
| 68 | daily_battery_averages() groups by Europe/Paris calendar day, rounds the mean, orders newest-first, honours since=, and excludes NULL-battery and unparseable-timestamp rows (22-06-PLAN.md Task 1) | ported | server/test_config_history.py::test_daily_battery_averages_groups_excludes_and_bounds_correctly |
| 69 | daily_battery_averages(): a 01:30+02:00 reading buckets to its Europe/Paris day, and two same-Paris-day readings on different UTC days form ONE bucket (D-12.3, 22-06-PLAN.md Task 1) | ported | server/test_config_history.py::test_daily_battery_averages_buckets_by_paris_day_not_utc_day |
| 70 | daily_battery_averages() buckets correctly across the CET-to-CEST March transition (the skipped hour), on the Paris day each reading actually fell on (D-12.3) | ported | server/test_config_history.py::test_daily_battery_averages_crosses_the_march_dst_forward_transition |
| 71 | daily_battery_averages() buckets correctly across the CEST-to-CET October transition (the repeated hour), counting each instant once on the correct Paris day (D-12.3) | ported | server/test_config_history.py::test_daily_battery_averages_crosses_the_october_dst_back_transition |
| 72 | check_in_gaps() returns the observed intervals between consecutive device_health check-ins, oldest-first, each carrying both timestamps, its length in seconds and its Paris day | ported | server/test_config_history.py::test_check_in_gaps_returns_consecutive_intervals_oldest_first |
| 73 | check_in_gaps() reads EVERY device_health row, including one whose battery_mv is NULL - a missing X-Battery-Mv header is not a missed wake (CFG-43) | ported | server/test_config_history.py::test_check_in_gaps_counts_a_null_battery_row_as_a_real_check_in |
| 74 | check_in_gaps() reports the spans an undatable ts bounds as UNKNOWN rather than merging them into one false long interval | ported | server/test_config_history.py::test_check_in_gaps_reports_an_undatable_span_as_unknown_not_merged |
| 75 | check_in_gaps() returns an empty list - never raising - for an empty table, a single check-in, and a window containing no rows, while an inclusive window still yields its interval | ported | server/test_config_history.py::test_check_in_gaps_degenerate_windows_return_empty_without_raising |
| 76 | check_in_gaps()'s docstring carries both 'cannot know' caveats - the ingest-missed log range and the UNIQUE(ts, battery_mv) collapse with its 60-second bound - plus why battery_mv is unfiltered | ported | server/test_config_history.py::test_check_in_gaps_docstring_states_what_it_cannot_know |
| 77 | server/history_db.py still contains no ALTER TABLE and no PRAGMA user_version - the whole migration story remains CREATE TABLE IF NOT EXISTS on every connection (CFG-43) | ported | server/test_config_history.py::test_history_db_introduces_no_migration_mechanism |
| 78 | classify_check_in_gap() is on-cadence below warn_s, late EXACTLY AT warn_s, still late below error_s and missing EXACTLY AT error_s, for a non-default 777 s cadence (CFG-43) | ported | server/test_config_history.py::test_classify_check_in_gap_boundaries_on_the_multiplier_path |
| 79 | classify_check_in_gap() with a None cadence degrades to the bare floors rather than refusing to classify - the same degradation device_staleness_thresholds(None) already performs | ported | server/test_config_history.py::test_classify_check_in_gap_none_cadence_uses_the_bare_floors |
| 80 | classify_check_in_gap() reports an unknowable gap (None, a non-number, a bool, a negative, NaN) as unknown - never as on-cadence | ported | server/test_config_history.py::test_classify_check_in_gap_reports_an_unknowable_gap_as_unknown |
| 81 | classify_check_in_gap()'s own source derives its thresholds from device_staleness_thresholds(), re-types none of the multipliers or floors, reads no config, and history_db.py holds no second copy | ported | server/test_config_history.py::test_classify_check_in_gap_reuses_the_one_threshold_function |
| 82 | the verdict vocabulary is four distinct observed terms and neither history_db.py nor wake.py uses the words 'honoured' or 'punctual' anywhere (24-RESEARCH.md Risk 1) | ported | server/test_config_history.py::test_the_verdict_vocabulary_is_observed_never_honoured |
| 83 | every CREATE TABLE in history_db.py is guarded by IF NOT EXISTS and there are exactly four - the fourth (wake_epochs) is the whole migration story for this phase | ported | server/test_config_history.py::test_every_create_table_is_guarded_and_there_are_exactly_four |
| 84 | a history.db created BEFORE wake_epochs existed opens cleanly, gains the table from the existing CREATE TABLE IF NOT EXISTS bootstrap, and keeps every pre-existing row intact (no migration needed) | ported | server/test_config_history.py::test_an_old_schema_database_gains_wake_epochs_without_losing_a_row |
| 85 | record_wake_epoch() inserts only when the effective interval differs from the newest stored row, treating an undeterminable cadence as a distinct value rather than a continuation | ported | server/test_config_history.py::test_record_wake_epoch_writes_only_when_the_interval_changes |
| 86 | no file under companion/ so much as mentions wake_epochs - the table accrues data for a later phase and nothing in phase 24 reads it (24-RESEARCH.md Risk 1, Option C) | ported | server/test_config_history.py::test_nothing_under_companion_reads_the_epoch_table |
| 87 | every history_db.py execute() call uses ? placeholders, never %-formatting or an f-string | ported | server/test_config_history.py::test_all_sql_uses_placeholders_not_string_formatting |
| 88 | effective_wake_interval_s(cfg, battery_critical=True) pins device_config.BATTERY_CRITICAL_SLEEP_S ahead of every other consideration, including display_enabled=False, and the default False leaves every existing result unchanged | ported | server/test_config_history.py::test_effective_wake_interval_s_battery_critical_pins_and_wins |
| 89 | read_battery_critical() returns True only for a literal JSON true under BATTERY_CRITICAL_STATE_KEY - a missing file, malformed JSON, a non-dict payload, the string 'true', and the int 1 all return False, and it never raises | ported | server/test_config_history.py::test_read_battery_critical_is_fail_open |
| 90 | next_wake_status(last_checkin, cfg, battery_critical=True) puts the next wake exactly BATTERY_CRITICAL_SLEEP_S (3600 s) after the check-in when no quiet-hours window applies | ported | server/test_config_history.py::test_next_wake_status_battery_critical_pins_3600s |

## server/test_dither.py

# Ledger: server/test_dither.py

Baseline: 32-BASELINE/server__test_dither.txt (6 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | panel_palette_image() carries exactly PALETTE_RGB's 6 entries, unpadded | ported | server/test_dither.py::test_panel_palette_image_is_unpadded |
| 2 | dither_to_full_panel_palette() on a hue-rich synthetic image yields indices that are a subset of {0,1,2,3,4,5} | ported | server/test_dither.py::test_full_palette_dither_stays_within_legal_indices |
| 3 | a flat single-color RGB source quantizes to exactly that color's own index | ported | server/test_dither.py::test_flat_source_quantizes_to_single_index |
| 4 | dither_to_full_panel_palette() is deterministic for the same input image | ported | server/test_dither.py::test_dither_to_full_panel_palette_is_deterministic_for_the_same_input_image |
| 5 | write_calibration_preview() writes exactly one palette-swatch PNG and returns its path | ported | server/test_dither.py::test_calibration_preview_writes_exactly_one_palette_swatch_png_and_returns_its_path |
| 6 | build_mood_background() no longer exists on server.plane.dither (D-21 retirement) | ported | server/test_dither.py::test_build_mood_background_no_longer_exists_on_server_plane_dither |

## server/test_enrich.py

# Ledger: server/test_enrich.py

Baseline: 32-BASELINE/server__test_enrich.txt (60 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | replaying the real recorded adsbdb hit (TVF16VB) yields the normalised route | ported | server/test_enrich.py::test_hit_fixture_normalises |
| 2 | replaying the real recorded adsbdb miss (EJU84YF, a genuine captured 404 - see fixtures/README.md) yields None | ported | server/test_enrich.py::test_real_recorded_miss_yields_none |
| 3 | a 500 response yields None without raising | ported | server/test_enrich.py::test_500_yields_none |
| 4 | a connection error (transport raises) yields None without raising | ported | server/test_enrich.py::test_connection_error_yields_none |
| 5 | a 200 response with a non-JSON body yields None without raising | ported | server/test_enrich.py::test_non_json_body_yields_none |
| 6 | a 200 body missing the 'response' key yields None | ported | server/test_enrich.py::test_missing_response_key_yields_none |
| 7 | a 200 body missing the 'flightroute' key yields None | ported | server/test_enrich.py::test_missing_flightroute_key_yields_none |
| 8 | a structurally incomplete 200 body (missing/non-string municipality) yields None - UI-SPEC has no partial state | ported | server/test_enrich.py::test_half_resolved_route_yields_none |
| 9 | two consecutive lookups for the same callsign invoke the transport exactly once (cached hit) | ported | server/test_enrich.py::test_hit_is_cached |
| 10 | a cached miss (a callsign that returned 404) is never re-queried on a later lookup | ported | server/test_enrich.py::test_miss_is_cached |
| 11 | the cache round-trips through json.dumps/json.loads and still honours both a cached hit and a cached miss | ported | server/test_enrich.py::test_cache_round_trips_through_json |
| 12 | callsigns are normalised (stripped, upper-cased) before both the cache key and the request URL are built | ported | server/test_enrich.py::test_normalisation_shares_one_cache_entry_and_request |
| 13 | to_sentence_case_city lower-cases interior connective particles but capitalises the first word and all other words | ported | server/test_enrich.py::test_sentence_case_lowercases_interior_particles |
| 14 | _primary_city_name() reduces a '/'-separated compound municipality name to its first segment, unchanged when there is no '/' | ported | server/test_enrich.py::test_primary_city_name_reduces_compound_municipality |
| 15 | _parse_route() applies _primary_city_name() to both origin_city and destination_city, not just one (D-09-style, Phase 9 09-04) | ported | server/test_enrich.py::test_parse_route_applies_primary_city_name_to_both_cities |
| 16 | a callsign containing non-alphanumeric characters is rejected before the outbound request is built (T-02-04-02) | ported | server/test_enrich.py::test_unsafe_callsign_never_queried |
| 17 | city_for_state() returns the destination city for departing and the origin city for arriving | ported | server/test_enrich.py::test_city_for_state_picks_correct_end |
| 18 | normalise_callsign strips and upper-cases, returning None for anything empty or non-string | ported | server/test_enrich.py::test_normalise_callsign_edge_cases |
| 19 | airline_from_callsign('TVF16VB') returns 'Transavia France' | ported | server/test_enrich.py::test_airline_from_callsign_tvf |
| 20 | airline_from_callsign(' tvf16vb ') normalises through normalise_callsign() before the prefix lookup | ported | server/test_enrich.py::test_airline_from_callsign_normalises |
| 21 | airline_from_callsign() returns None (never raises) for an unknown prefix, a bare 3-letter string, empty string, None, an int, and a path-separator payload | ported | server/test_enrich.py::test_airline_from_callsign_never_raises_battery |
| 22 | airline_only_route('Transavia France') carries _parse_route()'s exact key set, airline_name set and the other four keys None; airline_only_route(None) returns None | ported | server/test_enrich.py::test_airline_only_route_shape_and_none_handling |
| 23 | resolve_route('EJU84YF', ...) on the real recorded adsbdb miss yields an 'easyJet' airline-only route with source 'airline_only', identically on a second call, with exactly one transport invocation | ported | server/test_enrich.py::test_resolve_route_airline_only_on_real_miss |
| 24 | resolve_route('TVF16VB', ...) classifies fresh_hit on the first call and cache_hit on the second | ported | server/test_enrich.py::test_resolve_route_fresh_then_cache_hit |
| 25 | resolve_route() on a miss whose callsign prefix is absent from the table returns (None, 'miss') | ported | server/test_enrich.py::test_resolve_route_unknown_prefix_is_miss |
| 26 | every value in enrich._ICAO_AIRLINE_PREFIXES is a member of illustrations.target_airline_names() (D-07 drift guard) | ported | server/test_enrich.py::test_prefix_table_values_are_a_subset_of_illustration_targets |
| 27 | every key of enrich._ICAO_AIRLINE_PREFIXES is exactly 3 uppercase A-Z characters | ported | server/test_enrich.py::test_prefix_table_keys_are_three_uppercase_letters |
| 28 | airline_from_callsign('KMM466') returns 'KM Malta Airlines' (260827-jz6, real curled callsign) | ported | server/test_enrich.py::test_airline_from_callsign_km_malta |
| 29 | airline_from_callsign('JAF7521') returns 'TUIfly Belgium' (260827-jz6, real curled callsign, QT-jz6-D-02 override) | ported | server/test_enrich.py::test_airline_from_callsign_tuifly_belgium |
| 30 | correct_airline_name('AIA6412', 'Avies') returns 'Amelia' (260827-kih) | ported | server/test_enrich.py::test_correct_airline_name_aia_to_amelia |
| 31 | correct_airline_name('ZZZ1234', 'Avies') returns 'Avies' unchanged - a different prefix carrying the same string is never rewritten (QT-kih-D-01) | ported | server/test_enrich.py::test_correct_airline_name_is_prefix_scoped |
| 32 | resolve_route('AIA6412', ...) corrects the real recorded AIA/Avies misattribution to 'Amelia' on both a fresh_hit and a cache_hit, while the cache entry itself keeps the raw upstream string (QT-kih-D-02) | ported | server/test_enrich.py::test_resolve_route_corrects_aia_fresh_then_cached |
| 33 | airline_from_callsign('AIA6412') returns 'Amelia' (260827-kih, zero network call) | ported | server/test_enrich.py::test_airline_from_callsign_aia_amelia |
| 34 | every _AIRLINE_NAME_CORRECTIONS row agrees with _ICAO_AIRLINE_PREFIXES and its corrected value is a target_airline_names() member (QT-kih-D-03 cross-table invariant) | ported | server/test_enrich.py::test_correction_table_agrees_with_prefix_table_and_targets |
| 35 | correct_airline_name()/apply_airline_name_correction() never raise for a hostile callsign x airline_name/route battery (None, int, empty string, bare prefix, path-separator payload, non-dict route, a route whose .get() raises), and never return a value derived from the arguments other than the unchanged airline_name (T-kih-01) | ported | server/test_enrich.py::test_correction_seam_never_raises_battery |
| 36 | correct_airline_name() maps FPO/Europe Airpost -> ASL Airlines France, CRL/Corsairfly -> Corsair, CCM/CCM Airlines -> Air Corsica (260827-kih) | ported | server/test_enrich.py::test_correct_airline_name_three_stale_brand_pairs |
| 37 | resolve_route() corrects all three stale-brand carriers under their own prefix and leaves the same string untouched under an unrelated prefix; the corrected Air Corsica route selects the renamed air-corsica.png/air-corsica-atr72.png files (260827-kih) | ported | server/test_enrich.py::test_resolve_route_and_selection_for_three_stale_brand_carriers |
| 38 | airline_from_callsign('HOP4001') returns 'Air France Hop' (260827-lgt, real curled callsign) | ported | server/test_enrich.py::test_airline_from_callsign_hop_air_france |
| 39 | airline_from_callsign('WMT3001') returns 'Wizz Air' (260827-lgt, real curled callsign WMT3001; adsbdb itself resolves 'Wizz Air Malta', QT-lgt-D-01 deliberate brand consolidation) | ported | server/test_enrich.py::test_airline_from_callsign_wizz_air_malta |
| 40 | airline_from_callsign('KLJ123') returns 'KlasJet' (260827-lgt, KLJ123 is a SYNTHETIC shape-valid callsign - no real KLJ callsign was ever live-confirmed, QT-lgt-D-06) | ported | server/test_enrich.py::test_airline_from_callsign_klasjet |
| 41 | no _AIRLINE_NAME_CORRECTIONS row exists whose prefix element is HOP, WMT or KLJ (QT-lgt-D-07 guard) | ported | server/test_enrich.py::test_no_correction_row_for_new_lgt_prefixes |
| 42 | note_unresolved_prefix() on a first sighting records a well-formed entry (count 1, both timestamps set) | ported | server/test_enrich.py::test_note_unresolved_prefix_first_sighting |
| 43 | note_unresolved_prefix() on a second sighting increments count, updates last_seen/example_callsign, and never moves first_seen | ported | server/test_enrich.py::test_note_unresolved_prefix_second_sighting_updates_in_place |
| 44 | note_unresolved_prefix() returns None and records nothing for every single prefix already present in _ICAO_AIRLINE_PREFIXES (full-table invariant, not sampled) | ported | server/test_enrich.py::test_note_unresolved_prefix_never_records_a_covered_prefix |
| 45 | note_unresolved_prefix() never records and never raises for None/int/empty/whitespace/bare-prefix/lowercase/path-separator/dot-dot/digit-in-prefix/non-dict-registry input, rebuilds a malformed pre-existing entry, and bounds the stored example for a 900-character legitimate-shape callsign | ported | server/test_enrich.py::test_note_unresolved_prefix_hostile_input_battery |
| 46 | trim_unresolved_prefixes() evicts lowest-count-then-oldest-last-seen, so a recurring prefix survives a flood of one-off arrivals and the newer one-off wins the tie-break (QT-oz9-D-04) | ported | server/test_enrich.py::test_trim_unresolved_prefixes_favours_recurrence |
| 47 | note_unresolved_prefix() records exactly the callsigns resolve_route() classifies 'miss' and none it classifies 'airline_only', on the same real production condition (an adsbdb 404) | ported | server/test_enrich.py::test_note_unresolved_prefix_agrees_with_resolve_route |
| 48 | _parse_route() reads callsign_iata from two real fixtures (TVF16VB -> 'TO16VB', AIA6412 -> 'U36412') (D-09) | ported | server/test_enrich.py::test_callsign_iata_parsed_from_real_hits |
| 49 | callsign_iata is optional and never route-fatal: absent/empty/whitespace/non-string values all still resolve a full route with callsign_iata degraded to None (D-09, T-08-02-01/T-08-02-02) | ported | server/test_enrich.py::test_callsign_iata_optional_never_route_fatal |
| 50 | a callsign resolved twice, the second time with no transport available, agrees on callsign_iata both on the plain hit path (TVF16VB) and the airline-name-correction path (AIA6412, Avies->Amelia) - proving both _route_from_entry() and apply_airline_name_correction()'s shallow copy preserve the field (D-09) | ported | server/test_enrich.py::test_callsign_iata_cache_round_trip_parity |
| 51 | _parse_route(), _route_from_entry() and airline_only_route() all agree on one key set, derived from a real _parse_route() result rather than hardcoded (D-09) | ported | server/test_enrich.py::test_shape_parity_across_all_three_builders |
| 52 | _parse_route()'s returned dict has no 'callsign'/'callsign_icao' key and no value equal to the raw ICAO callsign string - the raw callsign is structurally absent, not just unused by the renderer (D-08) | ported | server/test_enrich.py::test_raw_icao_callsign_never_smuggled_in |
| 53 | airline_source_from_callsign() returns ('<name>', 'static') for a known static prefix, (None, None) for an unknown one, and (None, None) for the full 260827-hyy hostile-input battery, with no registry configured | ported | server/test_enrich.py::test_airline_source_from_callsign_static_path_parity |
| 54 | after manual_resolutions.set_manual_registry_state_dir(tmp) with a ZZZ->'Zephyr Air' entry, airline_source_from_callsign('ZZZ1234') returns ('Zephyr Air', 'manual') and airline_from_callsign() returns 'Zephyr Air'; clearing the state dir restores (None, None) | ported | server/test_enrich.py::test_airline_source_from_callsign_manual_path |
| 55 | a manual entry for a prefix already in the static table is never consulted (D-06): airline_source_from_callsign() still reports the static name and source 'static'; static_airline_name_for_prefix() returns the static name for that prefix, None for a manual-only prefix, and handles non-string/wrong-length input without raising | ported | server/test_enrich.py::test_d06_collision_and_static_airline_name_for_prefix |
| 56 | resolve_route() with a manual entry for a prefix absent from the static table and an adsbdb miss returns source 'manual' and an airline-only route whose airline_name is the operator's name and whose other five keys are None | ported | server/test_enrich.py::test_resolve_route_manual_source |
| 57 | a manual entry for a static-table prefix still yields 'airline_only' under an adsbdb miss (D-06), and adsbdb still wins by construction ('fresh_hit') even when a manual entry exists for that prefix | ported | server/test_enrich.py::test_resolve_route_static_and_adsbdb_precedence_over_manual |
| 58 | clear_resolved_unresolved_prefix() removes a manually-resolved prefix's entry and returns the prefix, a second call returns None, and a still-unresolved prefix's entry survives byte-identical (D-14) | ported | server/test_enrich.py::test_clear_resolved_unresolved_prefix_happy_path_and_idempotence |
| 59 | clear_resolved_unresolved_prefix() returns None and mutates nothing for a non-dict registry, None, an int, an empty string, a bare 3-letter callsign, and a path-separator payload - one check covering the whole hostile-input sweep | ported | server/test_enrich.py::test_clear_resolved_unresolved_prefix_hostile_input_sweep |
| 60 | the eleven 260921-v9c prefixes each resolve through airline_from_callsign() to their exact expected name (including TFV60HA and KAF001 as actually observed), the DJT correction seam rewrites 'Denver Jet' to 'La Compagnie' via correct_airline_name(), and DEF/QEM both resolve to None (QT-v9c-D-06 guard) | ported | server/test_enrich.py::test_v9c_eleven_new_prefixes_and_djt_correction_and_exclusions |

## server/test_illustrations.py

# Ledger: server/test_illustrations.py

Baseline: 32-BASELINE/server__test_illustrations.txt (60 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | normalise_airline_key('Air Algérie') == 'air-algerie' | ported | server/test_illustrations.py::test_normalise_airline_key_air_algerie |
| 2 | normalise_airline_key('Air Corsica') == 'air-corsica' (260827-kih) | ported | server/test_illustrations.py::test_normalise_airline_key_air_corsica |
| 3 | normalise_airline_key('') / (None) / (42) all return None without raising | ported | server/test_illustrations.py::test_normalise_airline_key_falsy_and_non_string_returns_none |
| 4 | classify_aircraft_type() maps real designators onto all seven SHAPE_SLUGS | ported | server/test_illustrations.py::test_classify_aircraft_type_all_seven_buckets |
| 5 | classify_aircraft_type() normalizes lowercase and leading/trailing whitespace | ported | server/test_illustrations.py::test_classify_aircraft_type_normalizes_case_and_whitespace |
| 6 | classify_aircraft_type() returns None for an unrecognized designator | ported | server/test_illustrations.py::test_classify_aircraft_type_unknown_designator_is_none |
| 7 | classify_aircraft_type() returns None for falsy and non-string inputs (None, '', 0, 42, [], {}) without raising | ported | server/test_illustrations.py::test_classify_aircraft_type_falsy_and_non_string_returns_none |
| 8 | classify_aircraft_type() returns None (never raises) for hostile path-separator/parent-dir inputs (T-03.1-03-01) | ported | server/test_illustrations.py::test_classify_aircraft_type_hostile_inputs_never_raise |
| 9 | every _TYPE_SHAPE_BUCKETS value is a member of SHAPE_SLUGS and every key is uppercase ASCII 3-4 chars | ported | server/test_illustrations.py::test_type_shape_buckets_contract |
| 10 | select_illustration({'airline_name': 'Air France'}) returns the real air-france.png path | ported | server/test_illustrations.py::test_select_illustration_real_air_france |
| 11 | select_illustration({'airline_name': 'Vueling Airlines'}) returns the real vueling-airlines.png path | ported | server/test_illustrations.py::test_select_illustration_real_vueling |
| 12 | select_illustration() with an unknown airline returns the generic fallback path | ported | server/test_illustrations.py::test_select_illustration_unknown_airline_falls_back |
| 13 | select_illustration(None) returns the generic fallback path | ported | server/test_illustrations.py::test_select_illustration_none_route_falls_back |
| 14 | select_illustration() with a route lacking airline_name returns the generic fallback path | ported | server/test_illustrations.py::test_select_illustration_route_missing_airline_name_falls_back |
| 15 | select_illustration() with a non-string airline_name returns the generic fallback path | ported | server/test_illustrations.py::test_select_illustration_route_non_string_airline_name_falls_back |
| 16 | select_illustration() never raises across a battery of malformed inputs | ported | server/test_illustrations.py::test_select_illustration_never_raises_malformed_battery |
| 17 | select_illustration() returns None only when even the generic fallback file is absent | ported | server/test_illustrations.py::test_select_illustration_returns_none_only_when_fallback_missing |
| 18 | select_illustration() Tier 1 returns the airline-and-shape file when it exists | ported | server/test_illustrations.py::test_select_illustration_tier1_airline_and_shape_match |
| 19 | select_illustration() Tier 2 (D-06) returns the airline-only file when the shape file is absent | ported | server/test_illustrations.py::test_select_illustration_tier2_airline_only_when_shape_absent |
| 20 | select_illustration() Tier 2 wins over Tier 3 when both an airline file and a matching generic-shape file exist | ported | server/test_illustrations.py::test_select_illustration_tier2_wins_over_tier3 |
| 21 | select_illustration() Tier 3 (D-07) returns the neutral shape file for an unrecognized airline with a known shape | ported | server/test_illustrations.py::test_select_illustration_tier3_neutral_shape_for_unrecognized_airline |
| 22 | select_illustration() Tier 3 also applies when route is None but a type is supplied | ported | server/test_illustrations.py::test_select_illustration_tier3_applies_when_route_is_none |
| 23 | select_illustration() Tier 4 (D-08) returns the universal fallback when neither key resolves | ported | server/test_illustrations.py::test_select_illustration_tier4_universal_fallback_when_neither_resolves |
| 24 | select_illustration() Tier 4 also returns when the shape classifies but no generic-{shape}.png file exists | ported | server/test_illustrations.py::test_select_illustration_tier4_when_shape_classifies_but_no_generic_file |
| 25 | select_illustration() never raises for a hostile aircraft_type x malformed-route matrix, and no returned path escapes ILLUSTRATION_DIR (T-03.1-03-01) | ported | server/test_illustrations.py::test_select_illustration_hostile_battery_never_raises_and_stays_confined |
| 26 | illustration_path_for_key() rejects any key containing a path separator or parent-dir segment | ported | server/test_illustrations.py::test_illustration_path_for_key_rejects_separator |
| 27 | generic_fallback_path() points at the real, vendored generic-fallback.png | ported | server/test_illustrations.py::test_generic_fallback_path_points_at_real_file |
| 28 | validate_illustration_file() rejects a non-PNG file with a distinct message | ported | server/test_illustrations.py::test_validate_illustration_file_rejects_non_png |
| 29 | validate_illustration_file() rejects a PNG with no alpha channel with a distinct message | ported | server/test_illustrations.py::test_validate_illustration_file_rejects_no_alpha |
| 30 | validate_illustration_file() rejects a PNG whose alpha channel is fully opaque with a distinct message | ported | server/test_illustrations.py::test_validate_illustration_file_rejects_opaque_alpha |
| 31 | validate_illustration_file() rejects a PNG narrower than the minimum width with a distinct message | ported | server/test_illustrations.py::test_validate_illustration_file_rejects_narrow_width |
| 32 | validate_illustration_file() rejects a portrait PNG with a distinct message | ported | server/test_illustrations.py::test_validate_illustration_file_rejects_portrait |
| 33 | validate_illustration_file() rejects a PNG whose pixel count exceeds the cap with a distinct message | ported | server/test_illustrations.py::test_validate_illustration_file_rejects_oversized_pixel_count |
| 34 | validate_illustration_file() accepts the real, vendored air-france.png with zero problems | ported | server/test_illustrations.py::test_validate_illustration_file_accepts_real_vendored_file |
| 35 | required_filenames() contains generic-fallback.png and every entry matches ^[a-z0-9-]+\.png$ | ported | server/test_illustrations.py::test_required_filenames_contract |
| 36 | every file in required_filenames() exists in the vendored set and passes validate_illustration_file() | ported | server/test_illustrations.py::test_all_required_files_pass_validation |
| 37 | target_filenames() contains one generic-{shape}.png per SHAPE_SLUGS plus the universal fallback, all matching ^[a-z0-9-]+\.png$ | ported | server/test_illustrations.py::test_targets_contain_all_generic_shapes_and_fallback |
| 38 | target_filenames() has no duplicate entries | ported | server/test_illustrations.py::test_targets_have_no_duplicates |
| 39 | every currently-vendored .png in ILLUSTRATION_DIR is a member of target_filenames() (widening the expected set did not orphan an existing asset) | ported | server/test_illustrations.py::test_every_vendored_png_is_a_target |
| 40 | required_filenames() is a subset of target_filenames() and still contains every baseline file | ported | server/test_illustrations.py::test_required_is_subset_of_targets_and_keeps_baseline |
| 41 | outstanding_filenames() is exactly target_filenames() minus the on-disk set, in target order | ported | server/test_illustrations.py::test_outstanding_is_targets_minus_on_disk |
| 42 | the four P-04 secondary variants appear in target_filenames() with the expected exact names, alongside their unsuffixed primaries | ported | server/test_illustrations.py::test_p04_secondary_variants_and_primaries_present |
| 43 | target_airline_names() contains the current-brand 'ASL Airlines France'/'Corsair'/'Air Corsica' strings, not the stale adsbdb-resolved names they replace (260827-kih, inverted from the 260827-hyy check) | ported | server/test_illustrations.py::test_target_airline_names_carries_current_brand_not_stale_names |
| 44 | target_airline_names()/target_filenames() carry 'KM Malta Airlines'/'TUIfly Belgium' and their derived filenames, and never 'Air Malta' or 'Jetairfly' (260827-jz6, QT-jz6-D-02 drift guard) | ported | server/test_illustrations.py::test_km_malta_and_tuifly_belgium_targets_present |
| 45 | target_filenames() contains 'amelia.png'/'amelia-embraer.png' (delivered on disk) and totals 52 entries (260827-kih baseline, updated by 260827-lgt, a parallel Air Caraïbes livery-audit session, and quick task 260921-v9c's nine new primaries) | ported | server/test_illustrations.py::test_amelia_targets_present_and_total_is_52 |
| 46 | the four renamed illustration files (air-corsica/air-corsica-atr72/asl-airlines-france/corsair) exist on disk; the four superseded filenames they replace do not (260827-kih) | ported | server/test_illustrations.py::test_renamed_files_exist_superseded_names_do_not |
| 47 | target_airline_names()/target_filenames() carry 'Air France Hop'/'KlasJet' (with 'Air France'/'Wizz Air' still present as distinct names) and the three new filenames (delivered on disk); the QT-lgt-D-01 Wizz Air Malta reuse guard holds - no Malta-specific Wizz entry exists in either list (260827-lgt) | ported | server/test_illustrations.py::test_lgt_targets_present_and_wizz_reuse_guard_holds |
| 48 | target_variants_by_airline() returns 36 pairs in the same order as target_airline_names() (260921-v9c: 27 -> 36 via nine new primaries) | ported | server/test_illustrations.py::test_variants_by_airline_matches_names_order_and_count |
| 49 | target_variants_by_airline()'s Air Caraïbes pair carries exactly ['a330', 'a350-1000', 'atr72'], in order | ported | server/test_illustrations.py::test_variants_air_caraibes_three_shapes_in_order |
| 50 | target_variants_by_airline()'s Air France pair (a single None-shape entry) carries an empty list, not [None] | ported | server/test_illustrations.py::test_variants_air_france_empty_list_not_none |
| 51 | no target_variants_by_airline() shape list contains None, and 'a350-1000' survives (not dropped by a SHAPE_SLUGS membership test) | ported | server/test_illustrations.py::test_variants_no_none_and_a350_1000_survives |
| 52 | target_variants_by_airline() is derived from _ILLUSTRATION_TARGETS directly (source assertion) | ported | server/test_illustrations.py::test_variants_derived_from_targets_no_second_table |
| 53 | resolved_illustration_path() returns the override path when it exists, and the vendored path when it does not (260902-v26) | ported | server/test_illustrations.py::test_resolved_path_override_vs_vendored |
| 54 | select_illustration(..., state_dir=tmp) returns the override path when one exists for that key; the same call with no state_dir still returns the vendored path (260902-v26) | ported | server/test_illustrations.py::test_select_illustration_state_dir_override_vs_no_state_dir |
| 55 | select_illustration() Tier 1 override wins over the vendored Tier 1 file; an override for the bare Tier 2 airline key does not displace a vendored Tier 1 file - tier precedence is unchanged, only the per-tier source changes (260902-v26) | ported | server/test_illustrations.py::test_tier1_override_precedence_and_tier_isolation |
| 56 | override_path_for_key() returns None for a battery of hostile keys, and select_illustration() with a state_dir never returns a path outside ILLUSTRATION_DIR or the override directory, across a hostile aircraft_type x malformed-route matrix (T-v26-01-01) | ported | server/test_illustrations.py::test_override_hostile_key_confinement |
| 57 | the vendored file's bytes are unchanged after a battery of override resolution calls (260902-v26, T-v26-01-02) | ported | server/test_illustrations.py::test_vendored_file_immutable_after_override_resolution |
| 58 | set_override_state_dir() round trip: setting it makes a bare select_illustration() call pick up the override; resetting to None restores the vendored path, and the reset is guaranteed by a finally block (260902-v26, T-v26-01-04) | ported | server/test_illustrations.py::test_set_override_state_dir_round_trip |
| 59 | target_filenames() contains all nine 260921-v9c filenames (delivered on disk), 'French Air Force' is a target_airline_names() member, and the deferred tenth file 'saudi-special-flight.png' is neither a target nor present on disk (QT-v9c-D-06 guard) | ported | server/test_illustrations.py::test_v9c_nine_new_targets_present_and_tenth_excluded |
| 60 | every airline in target_variants_by_airline() has an unsuffixed {slug}.png primary that is both a target_filenames() member and present on disk - protects the companion gallery card contract (airlines_page.py builds every card's <img src> from the primary key alone, 260921-v9c) | ported | server/test_illustrations.py::test_every_airline_has_an_unsuffixed_primary_file_on_disk |

## server/test_manual_resolutions.py

# Ledger: server/test_manual_resolutions.py

Baseline: 32-BASELINE/server__test_manual_resolutions.txt (23 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | load_manual_resolutions() on a nonexistent state dir returns {} without raising | ported | server/test_manual_resolutions.py::test_missing_state_dir_returns_empty_dict_without_raising |
| 2 | load_manual_resolutions() on a file containing invalid JSON returns {} | ported | server/test_manual_resolutions.py::test_invalid_json_returns_empty_dict |
| 3 | load_manual_resolutions() on a JSON list (non-dict top level) returns {} | ported | server/test_manual_resolutions.py::test_non_dict_top_level_returns_empty_dict |
| 4 | load_manual_resolutions() drops an entry whose value is not a dict | ported | server/test_manual_resolutions.py::test_non_dict_entry_value_dropped |
| 5 | load_manual_resolutions() normalises a lowercase key to uppercase and round-trips a valid entry | ported | server/test_manual_resolutions.py::test_lowercase_key_normalised_on_read_and_round_trips |
| 6 | load_manual_resolutions() drops a path-traversal-shaped key and a 4-letter key | ported | server/test_manual_resolutions.py::test_malformed_keys_dropped |
| 7 | load_manual_resolutions() drops a hand-edited entry whose airline_name slugs to a reserved key | ported | server/test_manual_resolutions.py::test_reserved_name_dropped_on_read |
| 8 | add_entry() returns ADD_OK and round-trips through load_manual_resolutions() with a non-empty created_at | ported | server/test_manual_resolutions.py::test_add_entry_round_trips_with_non_empty_created_at |
| 9 | add_entry() rejects a 2-letter prefix with ADD_REJECTED_PREFIX | ported | server/test_manual_resolutions.py::test_add_entry_rejects_2letter_prefix |
| 10 | add_entry() rejects a whitespace-only name with ADD_REJECTED_NAME_EMPTY | ported | server/test_manual_resolutions.py::test_add_entry_rejects_whitespace_only_name |
| 11 | add_entry() rejects a 101-char name with ADD_REJECTED_NAME_TOO_LONG | ported | server/test_manual_resolutions.py::test_add_entry_rejects_101char_name |
| 12 | add_entry() rejects 'Generic Fallback' and 'Generic A320' with ADD_REJECTED_NAME_RESERVED | ported | server/test_manual_resolutions.py::test_add_entry_rejects_reserved_names |
| 13 | add_entry() rejects a new prefix at MANUAL_RESOLUTION_MAX_ENTRIES (ADD_REJECTED_FULL) but allows overwriting an existing one | ported | server/test_manual_resolutions.py::test_add_entry_rejects_new_prefix_at_cap_but_allows_overwrite |
| 14 | delete_entry() returns True once then False on a repeated call, without raising | ported | server/test_manual_resolutions.py::test_delete_entry_returns_true_once_then_false |
| 15 | set_manual_registry_state_dir()/airline_name_for_prefix() cache round-trips and clears on reset to None | ported | server/test_manual_resolutions.py::test_state_dir_cache_round_trips_and_clears_on_reset |
| 16 | entry_rows() returns (prefix, airline_name, created_at) tuples sorted by prefix, skipping a malformed entry | ported | server/test_manual_resolutions.py::test_entry_rows_sorted_and_skips_malformed_entry |
| 17 | delete_entry() leaves the override PNG on disk untouched (D-08), pinned to illustrations.override_path_for_key() | ported | server/test_manual_resolutions.py::test_delete_entry_leaves_override_png_untouched |
| 18 | no manual_resolutions.json.tmp file remains after a successful add_entry() (atomicity proof) | ported | server/test_manual_resolutions.py::test_no_stray_tmp_file_after_successful_add |
| 19 | 20 concurrent add_entry() calls for 20 distinct prefixes (ThreadingHTTPServer's real concurrency shape) all persist durably with no lost update and no stray .tmp file left behind (WR-02) | ported | server/test_manual_resolutions.py::test_concurrent_add_entry_calls_lose_no_updates |
| 20 | load_manual_resolutions() prints a one-line drop-count message whenever it silently rejects an entry or truncates at the cap (naming the real count in both cases) and prints nothing when nothing is dropped (WR-03) | ported | server/test_manual_resolutions.py::test_load_prints_drop_count_for_rejected_and_capped_entries |
| 21 | add_entry() returns ADD_FAILED (never raises) when its state dir cannot be created because the parent directory is read-only — CR-01's exact reproduction case (WR-11) | ported | server/test_manual_resolutions.py::test_add_entry_on_uncreatable_state_dir_returns_failed |
| 22 | delete_entry() returns False (never raises) when the state dir goes read-only mid-write, and the existing entry survives untouched since the write never happened — CR-01's mirror case for delete (WR-11) | ported | server/test_manual_resolutions.py::test_delete_entry_on_unwritable_state_dir_returns_false |
| 23 | add_entry() rejects every name in the hostile-input sweep, leaving the registry empty | ported | server/test_manual_resolutions.py::test_add_entry_rejects_hostile_input_sweep |

Notes: rows 21/22 were the baseline's two root-sandbox FAILs (32-BASELINE/INDEX.md's
"## Notes" - both a pre-existing root-euid environment condition, not a regression). Both are
ported unchanged (same assertion, `tmp_path` in place of `tempfile.TemporaryDirectory()`) with
`@requires_non_root` added (MR-8): under euid 0 they are skipped with an explicit reason
(root ignores read-only directory permission bits, so the write the check expects to fail would
silently succeed instead); under a non-root euid they run and pass, confirmed here with
`runuser -u nobody`.

## server/test_notify.py

# Ledger: server/test_notify.py

Baseline: 32-BASELINE/server__test_notify.txt (8 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | send_notification() returns True for a 200 response from the injected transport | ported | server/test_notify.py::test_send_notification_returns_true_for_a_200_response_from_the_injected_transport |
| 2 | send_notification() returns False for a 500 response from the injected transport | ported | server/test_notify.py::test_send_notification_returns_false_for_a_500_response_from_the_injected_transport |
| 3 | send_notification() returns False (never raises) when the transport raises TimeoutError | ported | server/test_notify.py::test_send_notification_returns_false_never_raises_when_the_transport_raises_timeout_error |
| 4 | send_notification() returns False (never raises) when the transport raises an arbitrary Exception | ported | server/test_notify.py::test_send_notification_returns_false_never_raises_when_the_transport_raises_an_arbitrary_exception |
| 5 | send_notification() returns False for a loopback, a localhost, a link-local cloud-metadata, and a file:// topic URL, without the injected transport ever being called | ported | server/test_notify.py::test_ssrf_gate_refuses_before_ever_calling_the_transport |
| 6 | body_for_lang() returns the French form for 'fr' and the English source string unchanged for every other language argument | ported | server/test_notify.py::test_body_for_lang_returns_french_or_falls_back_to_english |
| 7 | default_notify_transport() builds a POST request with the body UTF-8 encoded, a Title header, and the caller's timeout, against a faked opener | ported | server/test_notify.py::test_default_transport_builds_the_expected_request |
| 8 | default_notify_transport()'s _NoRedirectHandler refuses a 302 pointing at an internal address (169.254.169.254) outright - send_notification() returns False and the redirect target is never fetched | ported | server/test_notify.py::test_no_redirect_handler_refuses_a_302_to_an_internal_address_and_never_fetches_it |

## server/test_panel_preview.py

# Ledger: server/test_panel_preview.py

Baseline: 32-BASELINE/server__test_panel_preview.txt (11 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | a drawn canvas, packed then unpacked, yields identical per-pixel index data | ported | server/test_panel_preview.py::test_a_drawn_canvas_packed_then_unpacked_yields_identical_per_pixel_index_data |
| 2 | a canvas containing all six legal indices, including a mixed odd/even column pair, round-trips exactly | ported | server/test_panel_preview.py::test_all_six_indices_with_mixed_pair_round_trips |
| 3 | unpack_panel() on wrong-length input raises PanelDecodeError, never AssertionError/IndexError | ported | server/test_panel_preview.py::test_wrong_length_raises_panel_decode_error |
| 4 | unpack_panel() on the one illegal nibble code raises PanelDecodeError naming the offending code | ported | server/test_panel_preview.py::test_illegal_nibble_raises_panel_decode_error_naming_the_offending_code |
| 5 | panel_png_bytes() returns real, Pillow-decodable PNG bytes at the expected dimensions | ported | server/test_panel_preview.py::test_panel_png_bytes_is_a_real_pillow_decodable_png_at_the_expected_dimensions |
| 6 | panel_png_bytes(max_width=240) returns a proportionally resized thumbnail | ported | server/test_panel_preview.py::test_thumbnail_is_proportionally_resized |
| 7 | read_panel_file() on a missing file returns None rather than raising | ported | server/test_panel_preview.py::test_read_panel_file_on_a_missing_file_returns_none_rather_than_raising |
| 8 | a row with two different legal indices at column 0 and column 1 round-trips without a high/low nibble swap | ported | server/test_panel_preview.py::test_mixed_pair_catches_nibble_transposition |
| 9 | the full getdata() sequence (not a sampled subset) matches exactly for an all-six-indices canvas | ported | server/test_panel_preview.py::test_full_getdata_sequence_matches_over_all_six_indices |
| 10 | a real production render.render_panel(None, 'empty') round-trips index-for-index against build_canvas() | ported | server/test_panel_preview.py::test_production_render_round_trips_exactly_against_build_canvas |
| 11 | the nearest-neighbour thumbnail's colour set is a strict subset of the full image's colour set | ported | server/test_panel_preview.py::test_thumbnail_colour_set_is_a_strict_subset_of_the_full_images_colour_set |

## server/test_pipeline_e2e.py

# Ledger: server/test_pipeline_e2e.py

Baseline: 32-BASELINE/server__test_pipeline_e2e.txt (7 checks)

All seven old checks shared one process-global state directory (setup ->
download -> empty-snapshot no-op -> battery poll -> battery-empty park and
recovery are a genuinely sequential scenario, each depending on state an
earlier check created - MR-4), so all seven map to the same single,
consolidated node id.

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | run_once(multi-aircraft fixture) writes panel.bin at exactly 960000 bytes | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |
| 2 | panel.bin bytes decompose into only the six legal nibble codes | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |
| 3 | byos_server.py setup->display returns a valid image_url and image_hash | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |
| 4 | downloaded image is 960000 bytes and SHA-256-verifies against image_hash | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |
| 5 | run_once(empty fixture) leaves panel.bin byte-identical (D-04) | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |
| 6 | a real authenticated poll carrying X-Battery-Mv:3400, followed by a run_once() cycle, changes the served panel.bin only inside the icon's byte columns/rows, and the packed ink nibble at (1520,70) matches whichever state run_once() actually reported | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |
| 7 | end to end through the real device protocol: a 3290 mV check-in followed by run_once() latches BATTERY EMPTY and serves its hash with sleep_s 3600; a second run_once() is a byte-identical hash-skip; a 4100 mV check-in's OWN reply anticipates recovery (sleep_s != 3600) before the next run_once() clears the park and serves a different hash | ported | server/test_pipeline_e2e.py::test_full_pipeline_end_to_end_through_the_real_device_protocol |

## server/test_plane_detection.py

# Ledger: server/test_plane_detection.py

Baseline: 32-BASELINE/server__test_plane_detection.txt (47 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | filter_in_geofence drops out-of-bbox and position-less records | ported | server/test_plane_detection.py::test_filter_in_geofence_drops_out_of_bbox_and_positionless |
| 2 | select_runway3_aircraft picks 39d300 (450ft beats 800ft) | ported | server/test_plane_detection.py::test_select_runway3_aircraft_picks_lowest_altitude |
| 3 | select_runway3_aircraft: on-ground beats 800ft airborne | ported | server/test_plane_detection.py::test_select_runway3_aircraft_on_ground_beats_airborne |
| 4 | select_runway3_aircraft returns None for an empty snapshot | ported | server/test_plane_detection.py::test_select_runway3_aircraft_empty_returns_none |
| 5 | selected record's callsign is stripped of trailing padding | ported | server/test_plane_detection.py::test_selected_record_callsign_is_stripped |
| 6 | select_runway3_aircraft is deterministic under input reordering | ported | server/test_plane_detection.py::test_select_runway3_aircraft_deterministic_under_shuffle |
| 7 | select_runway3_aircraft: multi-aircraft winner's aircraft_type is B738 | ported | server/test_plane_detection.py::test_multi_aircraft_winner_has_aircraft_type |
| 8 | select_runway3_aircraft: on-ground winner's aircraft_type is A320 | ported | server/test_plane_detection.py::test_on_ground_winner_has_aircraft_type |
| 9 | select_runway3_aircraft: a record with no t key yields aircraft_type None | ported | server/test_plane_detection.py::test_no_type_key_yields_none |
| 10 | select_runway3_aircraft: malformed type values all yield aircraft_type None without raising | ported | server/test_plane_detection.py::test_malformed_type_values_never_raise |
| 11 | real wrong-runway record 39de4a is in-bbox and below-ceiling (the pre-fix accept condition) | ported | server/test_plane_detection.py::test_wrong_runway_fixture_reproduces_the_precondition |
| 12 | select_runway3_aircraft rejects the real runway-20 departure 39de4a (the reported false positive) | ported | server/test_plane_detection.py::test_wrong_runway_is_rejected |
| 13 | select_runway3_aircraft still selects the real runway-25 arrival 347288 (no over-tightening) | ported | server/test_plane_detection.py::test_real_runway3_arrival_is_still_selected |
| 14 | with both real aircraft present, only the genuine runway-3 arrival is a candidate | ported | server/test_plane_detection.py::test_wrong_runway_loses_to_real_one |
| 15 | runway_axis/along_cross_track_m are derived from the published thresholds | ported | server/test_plane_detection.py::test_axis_is_derived_from_published_thresholds |
| 16 | track_axis_deviation_deg is bidirectional and rejects bools/non-numerics | ported | server/test_plane_detection.py::test_track_deviation_is_bidirectional_and_type_safe |
| 17 | runway 06/24's published thresholds are rejected by the corridor gate, not the track gate | ported | server/test_plane_detection.py::test_corridor_is_what_rejects_runway_06_24 |
| 18 | runway 02/20's centreline crossing is rejected by the track gate, not the corridor gate | ported | server/test_plane_detection.py::test_track_gate_is_what_rejects_runway_02_20 |
| 19 | a record with no track is corridor-gated rather than rejected outright | ported | server/test_plane_detection.py::test_missing_track_does_not_disqualify |
| 20 | default poll (no providers arg) queries adsb.fi then adsb.lol | ported | server/test_plane_detection.py::test_default_poll_queries_adsbfi_then_adsblol |
| 21 | airplaneslive remains a selectable opt-in, absent from the default order | ported | server/test_plane_detection.py::test_airplaneslive_still_opt_in |
| 22 | poll_current_aircraft: two agreeing providers yield corroborated=True | ported | server/test_plane_detection.py::test_agreeing_providers_are_corroborated |
| 23 | poll_current_aircraft: disagreeing providers select nothing (doubt -> D-04 hold) | ported | server/test_plane_detection.py::test_disagreeing_providers_yield_nothing |
| 24 | poll_current_aircraft: an unreachable provider is not scored as disagreement | ported | server/test_plane_detection.py::test_single_reachable_provider_is_uncorroborated_not_suppressed |
| 25 | poll_current_aircraft (default order): adsb.fi and adsb.lol agreeing yields corroborated=True with adsb.fi's record | ported | server/test_plane_detection.py::test_default_order_corroborates |
| 26 | poll_current_aircraft (default order): adsb.fi and adsb.lol disagreeing select nothing | ported | server/test_plane_detection.py::test_default_order_disagreement_yields_nothing |
| 27 | poll_current_aircraft (default order): adsb.lol unreachable degrades to single-source, not suppressed | ported | server/test_plane_detection.py::test_default_order_degrades_to_single_source |
| 28 | query_provider: adsb.fi and adsb.lol response keys are never interchanged (proven through the transport) | ported | server/test_plane_detection.py::test_provider_keys_are_not_interchanged |
| 29 | the taxiing masking record is in-bbox, track-aligned, inside the airborne corridor and outranks the real arrival (the pre-fix accept condition) | ported | server/test_plane_detection.py::test_masking_fixture_reproduces_the_precondition |
| 30 | select_runway3_aircraft: a taxiing aircraft off the pavement no longer masks a real runway-3 movement | ported | server/test_plane_detection.py::test_taxiing_aircraft_no_longer_masks_real_runway3_traffic |
| 31 | the on-ground gate keeps the real runway-3 ground record (+31m) and rejects the documented 150m residual | ported | server/test_plane_detection.py::test_ground_gate_keeps_real_runway3_ground_traffic |
| 32 | the pavement pair ties at effective altitude 0.0, is identical across both feeds except seen_pos, and the pre-fix key still splits them (the manufactured-disagreement precondition) | ported | server/test_plane_detection.py::test_pavement_pair_reproduces_the_precondition |
| 33 | select_runway3_aircraft: two feeds differing only in seen_pos select the SAME aircraft | ported | server/test_plane_detection.py::test_selection_is_provider_independent |
| 34 | poll_current_aircraft (default order): two feeds differing only in seen_pos are corroborated, not suppressed | ported | server/test_plane_detection.py::test_identical_sets_are_not_manufactured_into_disagreement |
| 35 | poll_current_aircraft (default order): unequal candidate sets corroborate the common aircraft instead of suppressing (a stable tie-break alone would not have) | ported | server/test_plane_detection.py::test_asymmetric_sets_corroborate_the_common_aircraft |
| 36 | poll_current_aircraft: genuinely disjoint candidate sets still suppress the cycle (D-04 intact) | ported | server/test_plane_detection.py::test_genuinely_disjoint_sets_still_suppress |
| 37 | poll_current_aircraft: the disagreement line names every candidate each feed saw, not just the winners | ported | server/test_plane_detection.py::test_disagreement_line_names_every_candidate |
| 38 | runways['3'] duplicates the legacy runway/corridor blocks exactly (drift guard) | ported | server/test_plane_detection.py::test_runways_3_matches_legacy_blocks |
| 39 | runway_axis(geofence) matches runway_axis(geofence, runway_id='3') | ported | server/test_plane_detection.py::test_default_runway_id_matches_explicit_default |
| 40 | runway_axis: 06-24/02-20 computed bearings match their published true headings | ported | server/test_plane_detection.py::test_neighbouring_runway_bearings_match_published_headings |
| 41 | runway_axis(runway_id='totally-unknown') falls back to the default runway's axis (T-06-02-01) | ported | server/test_plane_detection.py::test_unknown_runway_id_falls_back_to_default_axis |
| 42 | corridor_params(runway_id='02-20') matches the file, negative entries fall back to the default | ported | server/test_plane_detection.py::test_corridor_params_for_02_20_and_malformed_fallback |
| 43 | select_aircraft_for_runway positively tracks 06-24 and 02-20 on their own centrelines | ported | server/test_plane_detection.py::test_positive_tracking_on_neighbouring_runways |
| 44 | the real runway-3 fixture is excluded from runway 06-24's gate (exclusive both ways) | ported | server/test_plane_detection.py::test_real_runway3_fixture_excluded_from_06_24 |
| 45 | selected_runway equals the requested id, or the default id on an unrecognised request | ported | server/test_plane_detection.py::test_selected_runway_key_reports_effective_id |
| 46 | filter_in_geofence tags carry on_runway and the deprecated on_runway3 alias correctly | ported | server/test_plane_detection.py::test_on_runway_and_deprecated_alias_tags |
| 47 | poll_current_aircraft diagnostics distinguishes all-providers-failed from a real selection | ported | server/test_plane_detection.py::test_diagnostics_distinguishes_all_failed_from_no_selection |

## server/test_poll_loop.py

# Ledger: server/test_poll_loop.py

Baseline: 32-BASELINE/server__test_poll_loop.txt (110 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | the first-ever detection sets last_flight and leaves previous_flight as None | ported | server/test_poll_loop.py::test_two_deep_flight_history_sequence |
| 2 | re-detecting the same hex across consecutive cycles does not shift anything into previous_flight | ported | server/test_poll_loop.py::test_two_deep_flight_history_sequence |
| 3 | a genuinely new aircraft (different hex) shifts the old current flight into previous_flight/previous_confirmed_state | ported | server/test_poll_loop.py::test_two_deep_flight_history_sequence |
| 4 | a third distinct detection shifts again - previous_flight tracks only the immediately-preceding detection (two-deep) | ported | server/test_poll_loop.py::test_two_deep_flight_history_sequence |
| 5 | render.build_canvas() is actually called with the shifted previous_flight (not just recorded in poll_state.json) | ported | server/test_poll_loop.py::test_two_deep_flight_history_sequence |
| 6 | the unresolved-prefix registry accumulates across two separate run_once() cycles against the same state directory, read back from poll_state.json on disk between cycles (260827-oz9) | ported | server/test_poll_loop.py::test_unresolved_prefix_registry_accumulates_across_cycles |
| 7 | a cycle detecting a callsign whose prefix IS in _ICAO_AIRLINE_PREFIXES leaves unresolved_prefixes byte-identical - the registry is a list of gaps, not a log of every adsbdb miss (260827-oz9) | ported | server/test_poll_loop.py::test_recognized_airline_leaves_registry_untouched |
| 8 | the poll_loop: line's unknown_prefix= field names the recorded prefix on a miss cycle, reads None on a covered cycle, and every pre-existing field is still present (260827-oz9) | ported | server/test_poll_loop.py::test_journal_line_names_unknown_prefix |
| 9 | PRECONDITION: with pacing disabled (pre-fix), a 4-aircraft burst inside one device redraw window leaves at least one aircraft that NEVER appears on any frame the device could fetch (mechanism C) | ported | server/test_poll_loop.py::test_prefix_burst_loses_an_aircraft_entirely_pre_fix |
| 10 | REGRESSION (a): a burst of 4 distinct detections inside one ~90s device window puts every non-discarded aircraft on the glass, in first-detected (FIFO) order | ported | server/test_poll_loop.py::test_paced_burst_shows_every_undropped_aircraft |
| 11 | GUARD: no aircraft ever reaches the current display slot more than MAX_STALENESS_S (150s) after it was first detected - the bound that keeps this a real-time board rather than a backlog | ported | server/test_poll_loop.py::test_no_promotion_exceeds_the_staleness_bound |
| 12 | REGRESSION: a queued aircraft past MAX_STALENESS_S when its turn comes is discarded, named in the log line's dropped= field, and skipped PAST - a fresher entry behind it is promoted in the same cycle | ported | server/test_poll_loop.py::test_expired_entries_are_skipped_not_stalled_behind |
| 13 | REGRESSION (b): a sustained 12-aircraft burst never grows the pending queue past MAX_PENDING_FLIGHTS and never raises - it saturates at the cap and discards the OLDEST entries first | ported | server/test_poll_loop.py::test_sustained_burst_caps_the_queue_and_drops_oldest_first |
| 14 | GUARD (c): light traffic - one distinct aircraft every 120s - never queues, never discards, and shifts current->previous on the very same cycle it is detected (byte-identical to pre-fix behaviour) | ported | server/test_poll_loop.py::test_light_traffic_is_identical_to_pre_fix |
| 15 | REGRESSION: a deferred detection and its last_advance_at timestamp are written to poll_state.json and read back from disk by the next run_once() - the queue survives the systemd oneshot boundary (D-P2-02) | ported | server/test_poll_loop.py::test_queue_survives_the_process_boundary |
| 16 | GUARD: a malformed pending_flights / last_advance_at in poll_state.json degrades to an empty queue and completes the cycle, never raising (load_poll_state()'s never-a-crash discipline, D-P2-02) | ported | server/test_poll_loop.py::test_malformed_queue_state_degrades_instead_of_raising |
| 17 | GUARD: every poll line still carries all eleven pre-existing fields and additionally shown=, pending= and dropped= - on every cycle, not only on interesting ones | ported | server/test_poll_loop.py::test_log_line_contract_is_intact |
| 18 | REGRESSION: a cycle that defers a detection is legible in the log - hex= names the detection, shown= names the different aircraft on the panel, pending= is non-zero, and panel_changed=False | ported | server/test_poll_loop.py::test_a_deferred_cycle_is_distinguishable_in_the_log |
| 19 | apply_battery_hysteresis()'s truth table: threshold-inclusive disarm (<=3500 sets True), clear-inclusive re-arm (>=3600 clears True) - a reading strictly between the two constants holds the previous decision in both directions (Pitfall 5) | ported | server/test_poll_loop.py::test_hysteresis_truth_table |
| 20 | apply_battery_hysteresis(None, was_active) holds was_active unchanged - a device that has never reported must not spuriously show the icon, and an unreadable file must not spuriously clear a real warning | ported | server/test_poll_loop.py::test_never_reported_reading_holds |
| 21 | load_battery_state() returns None for a missing file, invalid JSON, a JSON list, a dict with no battery_mv key, and a battery_mv that is a string/bool/float/negative/zero - and the int for a well-formed state | ported | server/test_poll_loop.py::test_load_battery_state_degrades_never_raises |
| 22 | the battery decision survives run_once()'s process boundary in poll_state.json, and the D-04 hold branch re-renders panel.bin exactly when the battery decision genuinely flips (not on every hold cycle, and even with no aircraft detected at all) | ported | server/test_poll_loop.py::test_cross_cycle_persistence_and_hold_branch_rerender |
| 23 | device_config.RUNWAYS and the geofence file's own runway id set agree exactly | ported | server/test_poll_loop.py::test_runway_ids_agree_across_device_config_and_geofence |
| 24 | a default config against the FLIGHT1 fixture reproduces the pinned pre-06-10 panel.bin digest | ported | server/test_poll_loop.py::test_default_config_byte_identity |
| 25 | a saved non-default tracked runway reaches detect.select_aircraft_for_runway on the injected-snapshot branch | ported | server/test_poll_loop.py::test_non_default_runway_reaches_select_aircraft_for_runway |
| 26 | a saved non-default tracked runway reaches detect.poll_current_aircraft on the live branch | ported | server/test_poll_loop.py::test_non_default_runway_reaches_poll_current_aircraft |
| 27 | an all-providers-failed diagnostics report yields a true source_fault flag passed to render.build_canvas | ported | server/test_poll_loop.py::test_all_failed_diagnostics_yields_true_fault_flag |
| 28 | providers queried successfully with nothing selected leaves the source_fault flag false | ported | server/test_poll_loop.py::test_successful_query_no_selection_yields_false_fault_flag |
| 29 | the injected-snapshot branch never sets the source_fault flag, regardless of a previously-persisted true value | ported | server/test_poll_loop.py::test_injected_snapshot_branch_never_sets_fault |
| 30 | two consecutive cycles with an unchanged true fault flag and no new detection write panel.bin exactly once, not twice | ported | server/test_poll_loop.py::test_fault_transition_gated_not_value |
| 31 | the poll_loop: log line gains theme=/tracked_runway=/source_fault= fields and stays a single print() per cycle | ported | server/test_poll_loop.py::test_log_line_gains_theme_runway_fault_fields |
| 32 | detecting a new aircraft writes exactly one runway_events row; re-detecting it unchanged writes no further row | ported | server/test_poll_loop.py::test_history_row_written_only_on_hex_transition |
| 33 | a confirmed_state flip on the same hex writes a new runway_events row | ported | server/test_poll_loop.py::test_history_row_written_on_confirmed_state_flip |
| 34 | a corroboration flip on the same hex/confirmed_state writes a new runway_events row | ported | server/test_poll_loop.py::test_history_row_written_on_corroboration_flip |
| 35 | the pipeline-run meta timestamp updates on every cycle, including one that writes no runway_events row | ported | server/test_poll_loop.py::test_pipeline_run_meta_updated_every_cycle |
| 36 | a panel write with changed bytes saves one image into the gallery; an unchanged-bytes cycle saves none | ported | server/test_poll_loop.py::test_gallery_archives_only_changed_panels |
| 37 | the gallery never holds more than GALLERY_MAX_ENTRIES; the oldest entries are removed first | ported | server/test_poll_loop.py::test_gallery_prunes_to_max_entries |
| 38 | a read-only gallery directory does not fail the cycle - run_once() still returns and panel.bin is still written | ported | server/test_poll_loop.py::test_readonly_gallery_dir_does_not_fail_cycle |
| 39 | a history.db failure (open_db raising) is caught and logged without failing the cycle or leaving panel.bin unwritten | ported | server/test_poll_loop.py::test_history_write_failure_does_not_fail_cycle |
| 40 | device_config.json is byte-identical before and after a poll cycle - poll_loop.py reads it and never writes it | ported | server/test_poll_loop.py::test_poll_loop_never_writes_device_config |
| 41 | a corrupted device_config.json (invalid JSON) still yields a completed cycle using the documented defaults | ported | server/test_poll_loop.py::test_corrupted_device_config_falls_back_to_defaults |
| 42 | a run_once() cycle given --caddy-log ingests a real access-log line into device_health | ported | server/test_poll_loop.py::test_caddy_log_ingestion_wired_into_poll_cycle |
| 43 | omitting --caddy-log (the default) ingests nothing and raises nothing | ported | server/test_poll_loop.py::test_caddy_log_omitted_is_noop |
| 44 | the pinned-digest verdict is Linux-strict and non-Linux-informational - both branches proven by forcing platform.system() | ported | server/test_poll_loop.py::test_digest_verdict_is_linux_strict_and_non_linux_informational |
| 45 | window entry renders the QUIET HOURS canvas exactly once and persists hold_state='quiet_hours' | ported | server/test_poll_loop.py::test_quiet_hours_entry_renders_once_and_persists_flag |
| 46 | a second in-window cycle is a no-op - panel_changed is False and panel.bin's bytes are unchanged | ported | server/test_poll_loop.py::test_quiet_hours_hold_is_noop |
| 47 | the rendered panel is exactly render.build_canvas(None, 'quiet_hours', quiet_hours_until='07:00') | ported | server/test_poll_loop.py::test_quiet_hours_panel_matches_expected_canvas |
| 48 | an in-window cycle on the live path never calls detect.poll_current_aircraft or detect.load_geofence | ported | server/test_poll_loop.py::test_quiet_hours_skips_ads_b_detection |
| 49 | REGRESSION GUARD: the first cycle after window exit repaints the held live board and clears hold_state | ported | server/test_poll_loop.py::test_quiet_hours_exit_from_held_branch_repaints |
| 50 | the exit cycle's returned state is the ordinary held value, never 'quiet_hours' and never a new third state | ported | server/test_poll_loop.py::test_quiet_hours_exit_writes_no_transition_screen |
| 51 | quiet_hours_enabled=False takes the ordinary detection path regardless of stored start/end times | ported | server/test_poll_loop.py::test_quiet_hours_disabled_is_inert |
| 52 | toggle-off entry renders the DISPLAY OFF canvas exactly once, byte-identical to render.build_canvas(None, 'display_off') | ported | server/test_poll_loop.py::test_display_off_entry_renders_once_and_matches_canvas |
| 53 | toggle-off hold is a no-op - panel.bin unchanged and no gallery entry added, even across a battery hysteresis transition | ported | server/test_poll_loop.py::test_display_off_hold_is_noop_across_battery_transition |
| 54 | a toggle-off cycle on the live path never calls detect.poll_current_aircraft or detect.load_geofence - an off period is unbounded, so a query-and-discard loop would run against the free-tier aggregators indefinitely | ported | server/test_poll_loop.py::test_display_off_skips_ads_b_detection |
| 55 | the overlap: toggle off AND an active quiet-hours window renders DISPLAY OFF, not QUIET HOURS (D-05 display axis) | ported | server/test_poll_loop.py::test_display_off_wins_over_active_quiet_hours_window |
| 56 | D-07 hold-to-hold, direction one: switching the toggle off mid-window costs no e-ink refresh - the quiet screen stays up and the latch silently updates to 'display_off' | ported | server/test_poll_loop.py::test_toggle_off_mid_window_produces_no_refresh |
| 57 | D-07 hold-to-hold, direction two: switching the toggle back on while a quiet-hours window is already active costs no e-ink refresh - the off screen stays up and the latch silently updates to 'quiet_hours' | ported | server/test_poll_loop.py::test_toggle_back_on_during_window_produces_no_refresh |
| 58 | the first cycle after the toggle is switched back on repaints the held live board once, with no intermediate transition screen, and clears hold_state | ported | server/test_poll_loop.py::test_display_off_exit_repaints_once_with_no_transition_screen |
| 59 | a Phase-10-shaped poll_state.json (legacy quiet_hours_active=True, no hold_state) migrates without a spurious repaint and loses its stale key on disk | ported | server/test_poll_loop.py::test_legacy_poll_state_migrates_without_spurious_repaint |
| 60 | display_enabled=True takes the ordinary detection path - the display-off machinery costs nothing when unused | ported | server/test_poll_loop.py::test_display_enabled_is_inert |
| 61 | run_once() configures the manual-resolution registry from THIS cycle's own state_dir every cycle - a seeded prefix resolves after a cycle against its state dir, and a later cycle against a different, registry-less state dir leaves it unresolved again (D-01) | ported | server/test_poll_loop.py::test_manual_registry_loaded_once_per_cycle_from_its_own_state_dir |
| 62 | a detected flight whose callsign carries a manually-registered prefix, with adsbdb returning nothing, is recorded with route_source == 'manual' and a route carrying the operator's airline name (D-01/D-02, end to end through a real run_once() cycle) | ported | server/test_poll_loop.py::test_manual_resolution_reaches_route_source_end_to_end |
| 63 | a cycle detecting a flight whose prefix is now resolvable via the manual registry removes that prefix's entry from unresolved_prefixes and persists the removal, leaving a still-unresolvable prefix's entry byte-identical (D-14) | ported | server/test_poll_loop.py::test_clear_resolved_unresolved_prefix_removes_resolvable_entry |
| 64 | the D-14 cleanup removes a resolved prefix's entry even when this cycle's own route_source is 'fresh_hit' (adsbdb answered) - a route_source-gated implementation would fail exactly this check and no other (13-RESEARCH.md Pitfall 2) | ported | server/test_poll_loop.py::test_clear_resolved_unresolved_prefix_is_independent_of_route_source |
| 65 | a battery-icon repaint of the same flight (the held/re-render branch) reports the identical effective_theme the flight-detected branch already reported, and both are the matching rule's theme rather than the base theme - proving a battery-icon repaint can never flip the panel's colour (D-13, 15-VALIDATION.md row 8) | ported | server/test_poll_loop.py::test_battery_transition_never_flips_effective_theme_for_the_same_flight |
| 66 | the nothing-ever-detected empty-state call site reports effective_theme == the base theme, never consulting a configured rule or the arrivals override (D-09, 15-VALIDATION.md row 9) | ported | server/test_poll_loop.py::test_nothing_ever_detected_ignores_rule_and_override |
| 67 | the held branch's own empty-state call site (a persisted flight whose confirmed_state never resolved) reports effective_theme == the base theme, even though a rule configured to match that flight's own callsign is present (D-09, 15-VALIDATION.md row 9) | ported | server/test_poll_loop.py::test_held_branch_with_no_confirmed_state_ignores_rule_and_override |
| 68 | the hold early-return's result dict reports effective_theme == the base theme under display-off, even with a matching rule, an arrivals override, and a pre-hold flight persisted in poll_state.json (D-09, 15-VALIDATION.md row 9) | ported | server/test_poll_loop.py::test_hold_early_return_ignores_rule_and_override |
| 69 | with no rule but an arrivals override configured, run_once() reports the override as effective_theme for a detected arriving flight and the base theme for a detected departing flight (D-04, proved end to end through the real poll loop) | ported | server/test_poll_loop.py::test_direction_sensitivity_through_the_real_loop |
| 70 | a colour rule added to the state dir AFTER one run_once() cycle is picked up by the very next cycle - proving the registry is primed every cycle, not cached once per process (D-13/T-15-02) | ported | server/test_poll_loop.py::test_colour_rules_registry_reloaded_every_cycle_from_its_own_state_dir |
| 71 | a battery-icon repaint of a calendar-matched flight, hours after the calendar entry's own time window has closed, reports the identical effective_theme the flight-detected branch already reported - the calendar theme, not the base theme - proving the held branch reuses the persisted match rather than recomputing one against the moved clock (D-13, T-16-BRANCH, 16-VALIDATION.md poll-loop row) | ported | server/test_poll_loop.py::test_calendar_match_survives_a_battery_repaint_past_its_own_window |
| 72 | a calendar match beats a matching exact-callsign rule end to end through the real loop - the calendar's designated theme, not the rule's (D-02) | ported | server/test_poll_loop.py::test_calendar_match_beats_an_exact_callsign_rule |
| 73 | the nothing-ever-detected empty-state call site reports effective_theme == the base theme with a calendar theme configured - a calendar match must never reach an empty state | ported | server/test_poll_loop.py::test_nothing_ever_detected_ignores_calendar_match |
| 74 | the held branch's own empty-state call site (a persisted flight whose confirmed_state never resolved) reports effective_theme == the base theme, even though a calendar entry matching that flight's own route is present | ported | server/test_poll_loop.py::test_held_branch_with_no_confirmed_state_ignores_calendar_match |
| 75 | the hold early-return's result dict reports effective_theme == the base theme under display-off, even with a calendar theme configured, a matching entry present, and a pre-hold flight's own last_calendar_theme_id already persisted | ported | server/test_poll_loop.py::test_hold_early_return_ignores_calendar_match |
| 76 | an airline-only enrichment (no cached route, resolved only via the static ICAO-prefix table) never lets a calendar match fire, even with an entry that would otherwise match on airline and time - proving the narrowing end to end through the real loop (CORRECTION 1) | ported | server/test_poll_loop.py::test_airline_only_route_never_matches_the_calendar |
| 77 | a hand-edited poll_state.json whose stored last_calendar_theme_id is not a registered theme falls back to the base theme on the held branch's repaint, never reaching the panel (T-16-TAMPER) | ported | server/test_poll_loop.py::test_tampered_last_calendar_theme_id_falls_back_to_base_theme |
| 78 | with no calendar secret file present, a full run_once() cycle completes normally and never creates calendar_rules.json | ported | server/test_poll_loop.py::test_unconfigured_cycle_never_creates_the_registry_file |
| 79 | with a calendar secret file present and a fresh last_attempt_at already on disk, ten consecutive cycles inside the throttle interval leave calendar_rules.json's modification time and contents byte-identical - the throttled path performs no write (T-16-DOS) | ported | server/test_poll_loop.py::test_throttled_cycles_never_rewrite_the_registry_file |
| 80 | with no calendar configured, a matching-rule cycle, an arrivals-override cycle and a plain base-theme cycle all report exactly what they reported before this phase - an explicit regression fence | ported | server/test_poll_loop.py::test_feature_off_leaves_every_pre_phase_behaviour_unchanged |
| 81 | poll_loop.py's own run_once() call site, which passes no min_interval_s argument, still skips the calendar fetch 60 seconds after a recorded attempt - pinning refresh_calendar_registry()'s new parameter to default to None so today's pacing is unchanged (D-06) | ported | server/test_poll_loop.py::test_default_min_interval_s_preserves_poll_loops_pacing |
| 82 | a first cycle crossing into battery-low sends exactly one push whose body carries the millivolt reading and the SEED-006 curve percentage '(≈ 9%)', and records last_battery_sent=True (quick 260923-gaf) | ported | server/test_poll_loop.py::test_battery_low_transition_sends_once_with_mv |
| 83 | a second cycle still battery-low (last_battery_sent already True) sends nothing | ported | server/test_poll_loop.py::test_battery_low_second_cycle_sends_nothing |
| 84 | a cycle crossing back to normal sends exactly one recovery push (no interpolated arguments) and records last_battery_sent=False | ported | server/test_poll_loop.py::test_battery_recovery_transition_sends_once |
| 85 | D-26: a notifications group with battery_low: False sends nothing on a transition and records nothing | ported | server/test_poll_loop.py::test_battery_config_disabled_sends_nothing |
| 86 | D-26: a notifications group with no topic_url configured sends nothing | ported | server/test_poll_loop.py::test_battery_no_topic_url_sends_nothing |
| 87 | a sender returning False still flips the recorded state, so a persistently-failing endpoint does not repeat the push every cycle | ported | server/test_poll_loop.py::test_battery_sender_returning_false_still_flips_state |
| 88 | a raising send_notification() does not propagate out of the real run_once() battery-transition call site (T-20-17) | ported | server/test_poll_loop.py::test_battery_raising_sender_does_not_propagate_through_run_once |
| 89 | D-28: notifications.lang == "fr" produces the French battery-low body | ported | server/test_poll_loop.py::test_battery_french_lang_produces_french_body |
| 90 | a seeded device_health check-in older than the shared warn threshold sends exactly one frame-silent push and records last_silent_sent=True | ported | server/test_poll_loop.py::test_silence_transition_sends_once_past_warn_threshold |
| 91 | a second cycle reading the same stale device_health row sends nothing | ported | server/test_poll_loop.py::test_silence_transition_second_cycle_sends_nothing |
| 92 | a fresh device_health check-in after a reported silence sends exactly one recovery push | ported | server/test_poll_loop.py::test_silence_transition_recovery_sends_once |
| 93 | a seeded device_health check-in just inside the shared warn threshold sends nothing | ported | server/test_poll_loop.py::test_silence_transition_inside_threshold_sends_nothing |
| 94 | no device_health rows at all sends nothing and records nothing (a first-install state, not a silence transition) | ported | server/test_poll_loop.py::test_silence_transition_no_rows_sends_and_records_nothing |
| 95 | D-26: a notifications group with frame_silent: False sends nothing even for a very stale check-in | ported | server/test_poll_loop.py::test_silence_transition_config_disabled_sends_nothing |
| 96 | the silent threshold the helper actually used equals wake.device_staleness_thresholds(wake.effective_wake_interval_s(cfg))[0] for a non-default wake_interval_s (777s), not a phase-local re-tuning | ported | server/test_poll_loop.py::test_silence_threshold_matches_shared_wake_thresholds_for_nondefault_interval |
| 97 | WR-01 fix: a display_off hold with a stale device_health check-in still raises exactly one frame_silent push from the early-return hold branch, and persists last_silent_sent=True | ported | server/test_poll_loop.py::test_silence_transition_fires_during_display_off_hold |
| 98 | three consecutive poll cycles at an unchanged effective wake interval write exactly one wake_epochs row, and a fourth at a changed interval writes a second (CFG-43 Task 3) | ported | server/test_poll_loop.py::test_wake_epochs_accrue_only_when_the_effective_interval_changes |
| 99 | a wake_epochs write raising sqlite3.Error is contained by _record_history()'s existing handler - the poll cycle completes and the panel is still written (T-24-03-B) | ported | server/test_poll_loop.py::test_a_raising_epoch_write_cannot_break_a_poll_cycle |
| 100 | apply_battery_critical_hysteresis(battery_mv, was_active) holds every pinned boundary: (None, False)->False, (None, True)->True, (3300, False)->True, (3301, False)->False, (3699, True)->True, (3700, True)->False | ported | server/test_poll_loop.py::test_apply_battery_critical_hysteresis_boundaries |
| 101 | a 3290 mV reading on the live board enters BATTERY EMPTY: state, panel_changed, the persisted hold_state/battery_critical_active latch, byte-identical panel.bin, and detection skipped entirely (detect.poll_current_aircraft/load_geofence never called) | ported | server/test_poll_loop.py::test_battery_empty_entry_from_live_board_renders_and_skips_detection |
| 102 | display_enabled=False plus a 3290 mV reading yields battery_empty, not display_off - the battery axis outranks the operator's own toggle | ported | server/test_poll_loop.py::test_battery_empty_outranks_display_off |
| 103 | an active quiet-hours window plus a 3290 mV reading yields battery_empty, not quiet_hours - a flat pack cannot honour either standing condition | ported | server/test_poll_loop.py::test_battery_empty_outranks_quiet_hours |
| 104 | entering BATTERY EMPTY from an existing DISPLAY OFF hold repaints (the widened D-07 boundary), producing the byte-identical BATTERY EMPTY canvas | ported | server/test_poll_loop.py::test_battery_empty_entry_from_existing_display_off_hold_repaints |
| 105 | parked cycles at 3290/3400/3600/3699 mV, plus a display toggle and a theme change while still parked, are all no-ops - panel.bin unchanged and no gallery entry added | ported | server/test_poll_loop.py::test_battery_empty_parked_is_noop_across_readings_and_config_edits |
| 106 | a missing or corrupt battery_state.json can never enter BATTERY EMPTY, and deleting the file while already parked never clears the park - the persisted latch, not the file's presence, is authoritative | ported | server/test_poll_loop.py::test_battery_empty_missing_or_corrupt_reading_never_parks_and_never_clears |
| 107 | a 3700 mV reading clears the BATTERY EMPTY hold, repaints the live board (panel_changed=True), and the persisted battery_critical_active latch returns to False | ported | server/test_poll_loop.py::test_battery_empty_recovery_repaints_live_board |
| 108 | recovering while display_enabled=False repaints DISPLAY OFF, not the live board - the next hold in priority order | ported | server/test_poll_loop.py::test_battery_empty_recovery_with_display_off_repaints_display_off |
| 109 | 3400 mV sets the battery-low badge (battery_low_active=True) without parking the frame (battery_critical_active stays False) - the two axes are independent | ported | server/test_poll_loop.py::test_badge_threshold_reading_sets_badge_without_parking |
| 110 | parked with wake_interval_s=300 and a 20-minute-old check-in, no frame_silent push is sent - the identical setup without the park sends exactly one (issue 1's fix) | ported | server/test_poll_loop.py::test_silence_transition_parked_suppresses_false_alert |

## server/test_render.py

# Ledger: server/test_render.py

Baseline: 32-BASELINE/server__test_render.txt (140 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | render_panel(flight, 'departing', route) packs to exactly 960000 bytes with only legal nibbles | ported | server/test_render.py::test_departing_packs_correctly |
| 2 | render_panel(flight, 'arriving', route) packs to exactly 960000 bytes with only legal nibbles | ported | server/test_render.py::test_arriving_packs_correctly |
| 3 | departing render's dominant nibble is 0x1 (White) - D-01 White default theme background | ported | server/test_render.py::test_departing_dominant_is_white |
| 4 | arriving render's dominant nibble is also 0x1 (White) - D-01's single shared field colour for both states, not a copy-paste duplicate of the departing check above | ported | server/test_render.py::test_arriving_dominant_is_white |
| 5 | departing render contains White (text/frame) and Black (real illustration livery) nibbles - D-25 full-color compositing | ported | server/test_render.py::test_departing_has_white_and_black_from_real_livery |
| 6 | departing render's nibble set is a subset of the 6 legal Spectra 6 codes | ported | server/test_render.py::test_only_legal_indices_present |
| 7 | empty-state render is White-dominant and contains at least one Black nibble | ported | server/test_render.py::test_empty_state_white_dominant_with_black |
| 8 | rendering the same flight+route twice produces byte-identical output (determinism) | ported | server/test_render.py::test_rendering_is_deterministic |
| 9 | departing and arriving renders of the same flight differ in bytes (state changes output) | ported | server/test_render.py::test_departing_and_arriving_differ |
| 10 | the main illustration's opaque pixels are byte-identical between departing and arriving renders (D-24: never mirrored by state) | ported | server/test_render.py::test_illustration_not_mirrored_between_states |
| 11 | draw_illustration()/_resize_illustration() have no mirror/flip parameter (D-24) | ported | server/test_render.py::test_illustration_functions_have_no_mirror_param |
| 12 | draw_illustration() with a soft/gradient alpha source never produces an illegal in-between palette index (Pitfall 2 regression), and returns .rect (full placement) plus .content (tight painted bbox) as distinct boxes | ported | server/test_render.py::test_soft_alpha_illustration_stays_within_legal_palette |
| 13 | a route whose airline_name has no vendored file falls back to generic-fallback.png and renders different bytes than a route with real art | ported | server/test_render.py::test_different_airline_changes_the_rendered_bytes |
| 14 | departing render draws the top-left 'DEPARTING' label and the top-right runway tag, both tracked glyph-by-glyph, label first then tag | ported | server/test_render.py::test_departing_top_row_labels_present |
| 15 | the state label's first glyph sits at the MARGIN inset and the top-right tag's first glyph is positioned so its tracked run ends flush at WIDTH - MARGIN (D-26, spike 002a) | ported | server/test_render.py::test_top_labels_sit_at_the_margin_inset |
| 16 | draw_frame() draws a thin outline at the ~2.5%%-of-width inset (D-26) | ported | server/test_render.py::test_frame_outline_is_drawn |
| 17 | departing main flight text is '{identifier} to {destination_city}' / '{airline_name} · {type_label}' (D-10 tier 1) | ported | server/test_render.py::test_departing_main_text_uses_lowercase_to |
| 18 | arriving main flight text is '{identifier} from {origin_city}' (D-10 tier 1, lowercase sentence text) | ported | server/test_render.py::test_arriving_main_text_uses_lowercase_from |
| 19 | a full enrichment miss (route=None) draws 'Unknown flight' and ROUTE_FALLBACK_TEXT, never the raw callsign (D-08/D-10 tier 4) | ported | server/test_render.py::test_enrichment_miss_shows_unknown_flight |
| 20 | a supplied previous_flight/previous_route renders its own real two-line text block | ported | server/test_render.py::test_previous_flight_card_renders_its_own_text |
| 21 | no drawn text contains a 'PREVIOUS ·' prefix (D-26: explicitly removed after the live sketch pass) | ported | server/test_render.py::test_previous_flight_text_has_no_previous_prefix |
| 22 | the previous flight's text block is drawn right-aligned (anchor='ra', D-26) | ported | server/test_render.py::test_previous_flight_text_is_right_aligned |
| 23 | omitting previous_flight/previous_route renders a genuinely different (single-flight) panel | ported | server/test_render.py::test_no_previous_flight_omits_the_card |
| 24 | every active-state text role uses PTSerif-Bold.ttf (D-06); the empty-state heading keeps PTSerif-Bold.ttf and EMPTY_BODY_FONT keeps PTSerif-Regular.ttf | ported | server/test_render.py::test_pt_serif_bold_is_the_active_weight |
| 25 | PREVIOUS_LINE2_FONT's size is 20px (D-11) with its overflow floor unchanged | ported | server/test_render.py::test_previous_line2_font_grew_to_20px |
| 26 | the White theme's active-state roles request only PTSerif-Regular.ttf, never Bold (08-06 on-glass correction) | ported | server/test_render.py::test_white_theme_uses_only_regular_weight |
| 27 | every one of the 11 registry themes requests only its own declared weight (08-06 on-glass correction, widened same session) | ported | server/test_render.py::test_every_theme_uses_only_its_declared_weight |
| 28 | _paint_text_backing() no longer exists on server.plane.render (D-05) | ported | server/test_render.py::test_paint_text_backing_helper_is_gone |
| 29 | no rectangle filled with the state's own background index is painted, on any registered theme, in either active state (D-05) | ported | server/test_render.py::test_no_background_filled_rectangle_behind_text_on_any_theme |
| 30 | a genuinely long destination/origin city name (Santiago de Compostela) shrinks via fit_text_size() without crashing, drawn in full | ported | server/test_render.py::test_long_name_stress_case_shrinks_without_crashing |
| 31 | _flight_line2_text() renders the airline name alone for an unlabelled (unrecognized) type designator | ported | server/test_render.py::test_unlabelled_type_renders_airline_alone |
| 32 | _flight_line2_text() renders the airline name alone for aircraft_type=None | ported | server/test_render.py::test_none_type_renders_airline_alone |
| 33 | _flight_line2_text() renders the airline name alone when aircraft_type is omitted entirely | ported | server/test_render.py::test_one_argument_call_renders_airline_alone |
| 34 | the P-01 presentation-only airline alias renders the current brand name; a non-aliased airline is unchanged | ported | server/test_render.py::test_display_airline_name_applies_the_p01_alias_only_where_defined |
| 35 | _flight_line2_text() never raises across a battery of malformed routes x hostile aircraft types, and always returns a string | ported | server/test_render.py::test_flight_line2_text_never_raises_for_hostile_inputs |
| 36 | the longest real airline name combined with the longest type label still renders without tripping _assert_within_canvas | ported | server/test_render.py::test_long_name_plus_longest_label_fits_within_canvas |
| 37 | rendering with a main flight carrying a type calls select_illustration() with that exact type | ported | server/test_render.py::test_select_illustration_receives_main_flights_type |
| 38 | a main + previous flight makes two select_illustration() calls, each receiving its own flight's type (no crossover) | ported | server/test_render.py::test_select_illustration_calls_each_receive_their_own_flights_type |
| 39 | previous_flight=None still completes without raising and never fabricates a type for the omitted previous card | ported | server/test_render.py::test_no_previous_flight_never_raises_and_never_crosses_over |
| 40 | server/plane/render.py's comment-stripped source contains no stroke_width/stroke_fill text-outline usage | ported | server/test_render.py::test_render_source_never_uses_text_outline_arguments |
| 41 | a corrupt (byte-garbage) illustration file degrades to the generic fallback instead of raising out of render_panel() | ported | server/test_render.py::test_corrupt_illustration_degrades_to_generic_fallback |
| 42 | an oversized illustration is rejected on its PNG header, before any pixel data is decoded | ported | server/test_render.py::test_oversized_illustration_rejected_on_header |
| 43 | when the selected illustration and the generic fallback are both undecodable, the render skips the illustration and still returns a valid panel | ported | server/test_render.py::test_both_illustration_and_fallback_undecodable_still_renders |
| 44 | an airline-only route (adsbdb miss, prefix-resolved 'easyJet') omits line 1 entirely and draws only the airline name, never the raw callsign or ROUTE_FALLBACK_TEXT (D-08/D-10 tier 3) | ported | server/test_render.py::test_airline_only_route_shows_airline_not_fallback_text |
| 45 | an airline-only Transavia France + B738 route composes line 2 as '{airline} · {type label}' exactly like a full hit, line 1 omitted entirely, never the raw callsign or a to/from clause (D-08/D-10 tier 3) | ported | server/test_render.py::test_airline_only_route_composes_line2_like_a_full_hit |
| 46 | illustrations.select_illustration() on an airline-only Transavia France route resolves to 'transavia-france.png' - the airline's own art, not the generic fallback | ported | server/test_render.py::test_airline_only_route_selects_the_airlines_own_illustration |
| 47 | a route already corrected by enrich.correct_airline_name() renders its current brand name via _flight_line2_text(), and display_airline_name() is a no-op on the already-corrected string (260827-kih) | ported | server/test_render.py::test_corrected_route_renders_current_brand_and_display_alias_is_noop |
| 48 | _flight_line1_text() tier 1 (identifier + city) returns the exact '{identifier} to\|from {city}' string for both states (D-10) | ported | server/test_render.py::test_tier1_identifier_and_city_both_known |
| 49 | _flight_line1_text() tier 2 (city known, no identifier) returns the title-case direction word and city, with no identifier, for both states (D-10) | ported | server/test_render.py::test_tier2_city_known_no_identifier |
| 50 | _flight_line1_text() tier 3 (airline known, no city, no identifier) returns an empty string - the sentinel meaning line 1 is omitted (D-10) | ported | server/test_render.py::test_tier3_airline_only_returns_empty_string |
| 51 | _flight_line1_text() tier 4 (nothing resolved) returns the fixed string 'Unknown flight' for both route=None and a dict with no airline name, identical for both states (D-10) | ported | server/test_render.py::test_tier4_nothing_resolved_returns_unknown_flight |
| 52 | no drawn text on either card contains the raw callsign or hex, across all four content-ladder tiers (D-08 end-to-end guard) | ported | server/test_render.py::test_d08_no_raw_callsign_or_hex_anywhere_across_all_tiers |
| 53 | _flight_line1_text() degrades a tier rather than raising for hostile route shapes (non-string/empty/whitespace identifier, non-dict route) | ported | server/test_render.py::test_hostile_route_shapes_degrade_a_tier_without_raising |
| 54 | build_canvas() with no battery kwarg is pixel-identical to battery_low=False for departing/arriving/empty (default-off, no regression) | ported | server/test_render.py::test_battery_default_off_matches_explicit_false |
| 55 | battery_low=True differs from battery_low=False only inside the icon bounding box (64,1514,115,1536), for departing/arriving (with a real previous-flight card on the canvas) and empty | ported | server/test_render.py::test_battery_icon_conditional_draw_is_spatially_contained |
| 56 | with battery_low=True, the body outline corner/fill/nub read as the state's own ink (EMPTY_INK for the empty state), while the hollow interior right of the fill still reads as the state's background | ported | server/test_render.py::test_battery_icon_ink_and_hollow_interior |
| 57 | battery icon size constants are a uniform round(original * 0.7) reduction of the former spacing-scale values (260828-0qo on-glass correction) with the stroke never dropping below FRAME_STROKE_PX, position constants (BATTERY_ICON_LEFT/BOTTOM) unchanged, the nub centred to within one pixel, and a total bounding box of (64,1514,115,1536) | ported | server/test_render.py::test_battery_icon_geometry_derives_from_spacing_scale |
| 58 | the main flight text sits exactly MAIN_TEXT_GAP_PX below the aircraft's last actually-painted pixel row, identically for illustrations whose transparent bottom padding differs by over 100px (illustration-crop-text-margin: no full-rectangle anchoring) | ported | server/test_render.py::test_main_text_gap_is_constant_across_illustrations |
| 59 | the previous flight text sits exactly PREVIOUS_TEXT_GAP_PX below its aircraft's last actually-painted pixel row, identically across illustrations with very different transparent bottom padding | ported | server/test_render.py::test_previous_text_gap_is_constant_across_illustrations |
| 60 | _opaque_bbox() measures the hard-thresholded paste mask, never a naive Image.getbbox() - the soft drop-shadow band (alpha 1..127) that is never painted must not count as content | ported | server/test_render.py::test_opaque_bbox_uses_the_paste_threshold_not_a_naive_getbbox |
| 61 | draw_illustration() returns a placement whose .content is strictly contained in .rect, with a strictly higher bottom edge, for real vendored art (structural guard against restoring full-rectangle anchoring) | ported | server/test_render.py::test_placement_content_is_strictly_inside_its_rect |
| 62 | the main illustration's VISIBLE horizontal midpoint sits on the canvas centre (within rounding) for files with sharply different left/right padding asymmetry - centred by painted pixels, not by rectangle | ported | server/test_render.py::test_main_illustration_is_centred_on_its_visible_pixels |
| 63 | the previous aircraft's visible right edge lands exactly on the main aircraft's visible right edge, and the previous text right-aligns to that same shared line minus the D-12 optical offset, for two files with very different right padding | ported | server/test_render.py::test_previous_card_and_text_align_to_the_main_aircrafts_visible_right_edge |
| 64 | the previous card's two text lines share one anchor x, equal to the previous aircraft's measured opaque right edge minus PREVIOUS_TEXT_LEFT_OFFSET_PX (D-12) | ported | server/test_render.py::test_previous_card_both_lines_share_one_anchor_at_the_optical_offset |
| 65 | the main card's text lines stay centred on the canvas midpoint with anchor='ma', unaffected by the previous card's optical offset (D-12) | ported | server/test_render.py::test_main_card_text_remains_centred_not_offset |
| 66 | an airline-only route on the main card omits line 1 entirely, promoting line 2 to line 1's y-position, with no empty-string draw call (D-10 tier 3) | ported | server/test_render.py::test_tier3_promotion_on_main_card |
| 67 | an airline-only route on the previous card omits its own line 1, promoting line 2 to line 1's y-position using the previous card's own gap constant, with no empty-string draw call (D-10 tier 3) - the check that catches the change implemented in only one of the two functions | ported | server/test_render.py::test_tier3_promotion_on_previous_card |
| 68 | both cards independently omit line 1 and promote line 2 on a simultaneous airline-only render, without interfering with each other (D-10 tier 3) | ported | server/test_render.py::test_tier3_on_both_cards_simultaneously |
| 69 | the previous card's VISIBLE vertical midpoint sits on the PREVIOUS_ILLUSTRATION_CENTER_Y_FRAC line (within rounding) across illustrations with very different top/bottom padding - no per-file vertical drift | ported | server/test_render.py::test_previous_card_is_vertically_centred_on_its_visible_pixels |
| 70 | render_panel() with no theme_id is byte-identical to an explicit default theme_id (CFG-01) | ported | server/test_render.py::test_theme_default_matches_no_theme_arg |
| 71 | build_canvas(theme_id='white') matches the no-theme default (D-01), and every other registered theme genuinely differs from it - none is a silent no-op | ported | server/test_render.py::test_white_theme_canvas_matches_default_and_others_differ |
| 72 | build_canvas(theme_id='not-a-theme') produces the default theme's canvas rather than raising | ported | server/test_render.py::test_unknown_theme_degrades_to_default_canvas |
| 73 | build_canvas(flight, 'nonsense-state') still raises ValueError naming departing/arriving/empty | ported | server/test_render.py::test_unknown_state_still_raises_naming_all_three_states |
| 74 | _assert_legal_palette() (run internally by build_canvas()) passes for every registered theme | ported | server/test_render.py::test_legal_palette_holds_for_every_theme |
| 75 | for every registered theme, in both departing and arriving states, a real two-flight panel's dominant nibble is that theme's own background (D-01/D-02 - the flat-field guard rail against a large livery area) | ported | server/test_render.py::test_per_theme_dominant_background_holds_in_both_states |
| 76 | render.state_ink_index() agrees with device_config.theme_ink_index() for every registered theme, in both departing and arriving states (D-02) | ported | server/test_render.py::test_ink_index_matches_theme_registry_for_every_theme |
| 77 | runway_tag_text() with no argument returns exactly TOP_RIGHT_TAG_TEXT (default render unchanged) | ported | server/test_render.py::test_runway_tag_text_default_matches_top_right_tag |
| 78 | runway_tag_text('06-24')/('02-20') return the strings from device_config.RUNWAYS | ported | server/test_render.py::test_runway_tag_text_matches_registry_for_other_runways |
| 79 | runway_tag_text('unknown') returns the default runway's tag rather than raising | ported | server/test_render.py::test_runway_tag_text_unknown_id_degrades_to_default |
| 80 | build_canvas(None, 'empty', runway_id=...) draws that runway's heading as a tracked label, including the longest of the three, and passes the safe-box assertion | ported | server/test_render.py::test_empty_canvas_draws_selected_runways_heading |
| 81 | build_canvas(flight, 'departing', runway_id='06-24') draws that runway's tag glyph-by-glyph, passing the within-canvas assertion | ported | server/test_render.py::test_active_canvas_draws_selected_runways_tag |
| 82 | render_panel(..., source_fault=False) is byte-identical to the same call without the argument | ported | server/test_render.py::test_source_fault_false_matches_default |
| 83 | render_panel(..., source_fault=True) differs from the same call with the flag false | ported | server/test_render.py::test_source_fault_true_differs_from_false |
| 84 | the source-fault badge is drawn on both the active canvas and the empty canvas (visible in every state) | ported | server/test_render.py::test_badge_caption_present_on_active_and_empty_canvases |
| 85 | the badge caption text is absent from a normal render (source_fault defaults to False) | ported | server/test_render.py::test_badge_caption_absent_from_a_normal_render |
| 86 | _assert_legal_palette() (run internally by build_canvas()) passes with the badge drawn, in both active states, the empty state, and every theme | ported | server/test_render.py::test_legal_palette_holds_with_badge_across_states_and_themes |
| 87 | a fault-badged departing render still satisfies _assert_legal_palette() for the default theme (proven via build_canvas(), not a re-implementation) | ported | server/test_render.py::test_fault_badged_departing_render_satisfies_legal_palette_via_build_canvas |
| 88 | draw_source_fault_badge()'s bounding box stays inside the drawn frame | ported | server/test_render.py::test_badge_bbox_stays_inside_the_drawn_frame |
| 89 | the source-fault badge's caption respects its theme's declared weight, same as every other active-state role (code-review WR-01) | ported | server/test_render.py::test_badge_caption_uses_its_theme_declared_weight |
| 90 | the source-fault badge's exclamation-mark dot paints a visible multi-pixel area, not a single point (code-review WR-02) | ported | server/test_render.py::test_badge_exclamation_dot_paints_more_than_one_pixel |
| 91 | all three runway ids combined with the single theme id render without error across both active states (theme-addition regression guard) | ported | server/test_render.py::test_runway_and_theme_matrix_combines_without_error |
| 92 | the D-26 outline is genuinely absent from a real build_canvas() render - every sampled point on the former frame band reads the state's own background index or its dithered White speckle, in both active states | ported | server/test_render.py::test_no_frame_outline_on_real_active_renders |
| 93 | --airline/--city CLI flags reach the rendered captions (render.main(), D-04) | ported | server/test_render.py::test_cli_airline_and_city_flags_reach_captions |
| 94 | --no-route still overrides --airline/--city ('Unknown flight', ROUTE_FALLBACK_TEXT, never the raw callsign) | ported | server/test_render.py::test_cli_no_route_wins_over_airline_and_city |
| 95 | --calibration-preview writes exactly one file (palette-swatches.png, D-13) | ported | server/test_render.py::test_calibration_preview_writes_exactly_one_file |
| 96 | combining --no-route with --out prints the skypane-poll.timer restart reminder (T-07-01-01) | ported | server/test_render.py::test_synthetic_reminder_printed_when_override_combined_with_out |
| 97 | the default CLI preview (no forcing flags) draws a tier-1 line containing the preview route's real identifier (D-10 tier 1) | ported | server/test_render.py::test_cli_default_preview_draws_tier1_with_identifier |
| 98 | --no-identifier forces the default preview into tier 2 (title-case direction + city, no identifier) (D-10 tier 2) | ported | server/test_render.py::test_cli_no_identifier_flag_forces_tier2 |
| 99 | --no-identifier combined with --no-route still produces tier 4, and combined with --preview-airline-only still produces tier 3 with no line 1 - both are no-ops (D-10) | ported | server/test_render.py::test_cli_no_identifier_is_a_noop_with_no_route_and_airline_only |
| 100 | no CLI path at any of the four content-ladder tiers draws the raw callsign passed via --callsign (D-08 CLI-level guard) | ported | server/test_render.py::test_cli_never_draws_raw_callsign_across_all_four_tiers |
| 101 | the previous card's D-12 optical offset holds its shared-anchor and safe-box invariants across six airline illustrations with deliberately different airframe silhouettes (narrowbody x2, turboprop, small twin, regional jet, widebody) - not just the single pair it was tuned against | ported | server/test_render.py::test_previous_card_optical_offset_holds_across_diverse_illustration_sample |
| 102 | LABEL_TRACKING_PX == 6 and draw_tracked_text()/_tracked_text_width()/_tracked_text_bbox() exist with the original commit's public/private naming split | ported | server/test_render.py::test_label_tracking_constant_and_helpers_present |
| 103 | _tracked_text_width() arithmetic holds for empty/single-char/multi-char/zero-tracking, derived from font.getlength() rather than hardcoded pixel numbers | ported | server/test_render.py::test_tracked_text_width_arithmetic |
| 104 | draw_tracked_text() issues one text draw per character at anchor='la', with inter-glyph advance == font.getlength(previous_char) + tracking, returning the x immediately after the last glyph's advance | ported | server/test_render.py::test_draw_tracked_text_glyph_by_glyph |
| 105 | every consecutive pair of glyph origins within the state-label and runway-tag runs differs by exactly font.getlength(previous_char) + LABEL_TRACKING_PX | ported | server/test_render.py::test_top_row_inter_glyph_advance_matches_tracking |
| 106 | the tracked top row builds without an AssertionError and the computed tag start x is >= 0 for every registered runway id, both active states, and a flat and a dithered theme | ported | server/test_render.py::test_top_row_tracking_stays_within_canvas_across_runways_themes_states |
| 107 | for a full two-flight active render with source_fault=True, the main card's line 1, the previous card's line 1, and the source-fault caption are each still drawn as one whole-string call, and the total single-character draw count equals exactly len(label_text) + len(tag_text) | ported | server/test_render.py::test_tracking_confined_to_top_row_roles_only |
| 108 | every registered band theme (PHASE9-1) renders via build_canvas() in both departing and arriving states without exception | ported | server/test_render.py::test_band_themes_render_without_exception |
| 109 | draw_diagonal_band() paints only {IDX_WHITE, band_idx} on a fresh White canvas, flat and dithered | ported | server/test_render.py::test_draw_diagonal_band_paints_only_legal_two_colour_set |
| 110 | a band theme's top labels are genuinely split into a merged state-label/airport-code run (e.g. 'DEPARTING FROM ORY') and a standalone runway-tag run (e.g. 'RWY 3'), both derived from runway_tag_text().partition(' · ') (PHASE9-3) | ported | server/test_render.py::test_band_theme_top_labels_are_split |
| 111 | a non-band theme's (white, the default) top labels remain exactly STATE_LABEL_TEXT and the FULL runway tag, unsplit - both draw_top_labels()'s own default (called with no band_theme argument) and _build_active_canvas()'s wiring genuinely preserve today's behaviour | ported | server/test_render.py::test_non_band_theme_top_labels_are_unsplit |
| 112 | the default (white) theme's canvas is byte-identical to before this phase (getdata()/getcolors() computed fresh, and 'white' itself is confirmed not a band theme) | ported | server/test_render.py::test_default_theme_canvas_unchanged_by_band_port |
| 113 | every one of the 11 pre-band themes' full two-flight render is pixel-identical whether _build_active_canvas()'s band_idx=band_idx wiring runs (always None) or draw_main_text_block()/draw_previous_text_block() are called with no band_idx argument at all (PHASE9-4/PHASE9-6 regression guard) | ported | server/test_render.py::test_non_band_text_blocks_unaffected_by_band_idx_kwarg |
| 114 | a band theme's main card draws the big-number line as route['callsign_iata'] verbatim and the tracked route line as _flight_line1_text()'s real remainder, upper-cased (tier 1); and draws only the promoted airline·type line, with no number/dash/tracked-route draw at all, for a tier-3 (airline-only) route (PHASE9-4) | ported | server/test_render.py::test_band_main_card_tier_split_reuses_real_content |
| 115 | a band theme's main-card anchor='ma' draws (the number line and the airline·type line) all share exactly one x-coordinate - center_x is computed once per block, never recomputed per line (round-15 fix, PHASE9-4) | ported | server/test_render.py::test_band_center_x_computed_once_not_recomputed_per_line |
| 116 | every registered band theme's main card text samples as IDX_WHITE (never IDX_BLACK) inside its own drawn bboxes - the round-13 ink swap, widened on real glass to every band colour, is proven by actual pixel colour, not by absence of an exception (PHASE9-5) | ported | server/test_render.py::test_band_black_main_card_ink_swaps_to_white |
| 117 | the previous card's drawn text bboxes never overlap the diagonal band's own rightmost extent, at any registered band theme, in a full two-flight render (PHASE9-6 clearance guard) | ported | server/test_render.py::test_previous_card_never_collides_with_the_band |
| 118 | every registered band theme's full two-flight composition (three-tier main + previous card text, plus the source-fault badge when present) stays _assert_legal_palette()-legal across both active states (PHASE9-4/PHASE9-5/PHASE9-6 full sweep) | ported | server/test_render.py::test_band_themes_full_composition_stays_palette_legal |
| 119 | the main illustration's VISIBLE vertical centre sits on one fixed canvas line (within MAIN_VERTICAL_DRIFT_TOLERANCE_PX) across all vendored files, instead of drifting with each file's own transparent top padding (illustration-crop-text-margin's missed sixth anchor) | ported | server/test_render.py::test_main_illustration_vertical_centre_has_no_per_file_drift |
| 120 | render_panel(None, 'quiet_hours', quiet_hours_until='07:00') packs to exactly 960000 bytes, dominated by the dimmed Black field, with at least one White nibble (D-05/D-06, as revised on glass in 12-06) | ported | server/test_render.py::test_quiet_hours_packs_white_dominant_with_black |
| 121 | every pixel index in build_canvas(None, 'quiet_hours', quiet_hours_until='07:00') is one of the six legal palette indices | ported | server/test_render.py::test_quiet_hours_only_legal_indices |
| 122 | build_canvas(None, 'quiet_hours', ...) is pixel-identical across different theme_id values (theme is ignored for this screen) | ported | server/test_render.py::test_quiet_hours_ignores_theme_id |
| 123 | build_canvas(None, 'quiet_hours', quiet_hours_until=None) and quiet_hours_until='' both render without raising and differ from quiet_hours_until='07:00' (heading-only degradation, T-10-02-01) | ported | server/test_render.py::test_quiet_hours_missing_until_omits_body_without_raising |
| 124 | battery_low=True changes the quiet-hours canvas relative to battery_low=False (10-RESEARCH.md A2) | ported | server/test_render.py::test_quiet_hours_battery_low_changes_canvas |
| 125 | source_fault=True changes the quiet-hours canvas relative to source_fault=False (10-RESEARCH.md A2) | ported | server/test_render.py::test_quiet_hours_source_fault_changes_canvas |
| 126 | build_canvas()'s source lists the 'quiet_hours' dispatch branch before the 'empty' dispatch branch | ported | server/test_render.py::test_quiet_hours_branch_precedes_empty_branch_in_source |
| 127 | render.main() via build_parser() with ['--state', 'quiet_hours', '--quiet-hours-until', '07:00', '--out', <path>] returns 0 and writes exactly IMAGE_BYTES bytes | ported | server/test_render.py::test_cli_renders_quiet_hours_state |
| 128 | band_blue_field/band_red_field each render via build_canvas() in both active states with the state's own background index (never White) provably the single most common index on the panel, stated explicitly rather than merely implied by the absence of an exception | ported | server/test_render.py::test_tinted_field_band_themes_prove_dominance_explicitly |
| 129 | build_canvas(None, 'display_off', theme_id=...) renders the dimmed Black/White field (DIMMED_FIELD_IDX dominant, no third index), carrying the heading and body, byte-identical across the full THEME_IDS registry (D-03 as revised on glass in 12-06) | ported | server/test_render.py::test_display_off_flat_white_black_across_all_themes |
| 130 | build_canvas(None, 'display_off') is provably distinct from build_canvas(None, 'empty') and build_canvas(None, 'quiet_hours', ...) - all three pass flight=None, so a reordered dispatch branch is otherwise silent (the exact trap plan 10-02 documented) | ported | server/test_render.py::test_display_off_provably_distinct_from_empty_and_quiet_hours |
| 131 | build_canvas(None, 'display_off', quiet_hours_until='07:00') is byte-identical to the call without it - no return-time value can reach this screen (D-04) | ported | server/test_render.py::test_display_off_ignores_quiet_hours_until |
| 132 | DISPLAY_OFF_HEADING_TEXT == 'DISPLAY OFF' and DISPLAY_OFF_BODY_TEXT == the locked body string, asserted by exact equality (D-04) | ported | server/test_render.py::test_display_off_copy_constants_match_locked_strings |
| 133 | battery_low=True and source_fault=True each change the display-off canvas independently, and both may be set at once, mirroring the equivalent quiet-hours checks | ported | server/test_render.py::test_display_off_battery_and_fault_indicators_are_independent |
| 134 | build_canvas(None, 'display_off', ...) uses only legal palette indices and passes every internal _assert_in_safe_box() check across all four battery_low/source_fault combinations (neither / battery only / fault only / both) | ported | server/test_render.py::test_display_off_legal_palette_and_safe_box_across_indicator_combos |
| 135 | BATTERY_EMPTY_HEADING_TEXT == 'BATTERY EMPTY' and BATTERY_EMPTY_BODY_LINES == the two locked authored sentences, asserted by exact equality | ported | server/test_render.py::test_battery_empty_copy_constants_match_locked_strings |
| 136 | build_canvas(None, 'battery_empty') calls render._build_dimmed_hold_canvas exactly once, with draw_empty_battery_icon, EMPTY_BATTERY_ICON_HEIGHT_PX, BATTERY_EMPTY_HEADING_TEXT, BATTERY_EMPTY_BODY_LINES, source_fault=False and battery_low=False | ported | server/test_render.py::test_battery_empty_dispatches_through_shared_dimmed_composition |
| 137 | build_canvas(None, 'battery_empty') and build_canvas(None, 'display_off') are byte-identical in the top and bottom 400 rows (the same dithered field) and provably distinct overall (the centred glyph/label/body differs) | ported | server/test_render.py::test_battery_empty_shares_dimmed_field_with_display_off |
| 138 | draw_empty_battery_icon() returns EMPTY_BATTERY_ICON_HEIGHT_PX (== MOON_ICON_DIAMETER_PX), draws an ink bounding box exactly EMPTY_BATTERY_ICON_HEIGHT_PX tall and EMPTY_BATTERY_ICON_WIDTH_PX wide, a POWER_ICON_STROKE_PX-long ink run at body mid-height from the left edge, a hollow body centre, and only the field/ink indices anywhere on the canvas | ported | server/test_render.py::test_draw_empty_battery_icon_geometry |
| 139 | build_canvas(None, 'battery_empty', ...) bytes are byte-stable regardless of source_fault, battery_low or theme_id - the hash the whole parked episode relies on staying constant | ported | server/test_render.py::test_battery_empty_byte_stable_across_indicators_and_themes |
| 140 | build_canvas(None, 'battery_empty') uses only IDX_BLACK/IDX_WHITE, dominated by DIMMED_FIELD_IDX (Black) - the same dimmed-field family rule as DISPLAY OFF and QUIET HOURS | ported | server/test_render.py::test_battery_empty_black_white_black_dominant |

## server/test_runway_config.py

# Ledger: server/test_runway_config.py

Baseline: 32-BASELINE/server__test_runway_config.txt (15 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | real fixture track_arrival_440cb1: -640 seeded with no prior state -> arriving | ported | server/test_runway_config.py::test_real_track_first_observation_arriving |
| 2 | real fixture track_arrival_440cb1: first +48 flare reading holds arriving (real landing, not a bug) | ported | server/test_runway_config.py::test_real_track_first_flare_holds |
| 3 | real fixture track_arrival_440cb1: second +48 flare reading still holds arriving (real landing, not a bug) | ported | server/test_runway_config.py::test_real_track_second_flare_holds |
| 4 | +48 seeded with no prior confirmed state returns None (nothing to hold) | ported | server/test_runway_config.py::test_no_prior_state_inside_deadband_returns_none |
| 5 | SYNTHETIC (A-02-02-01, no real departure observed): +200 seeded None -> departing (inclusive boundary) | ported | server/test_runway_config.py::test_synthetic_climb_threshold_departs |
| 6 | SYNTHETIC (A-02-02-01, no real departure observed): +199 holds last confirmed state (just inside deadband) | ported | server/test_runway_config.py::test_synthetic_just_below_climb_threshold_holds |
| 7 | real-data-backed boundary: -200 seeded None -> arriving (inclusive boundary) | ported | server/test_runway_config.py::test_descend_threshold_arrives |
| 8 | real-data-backed boundary: -199 holds last confirmed state (just inside deadband) | ported | server/test_runway_config.py::test_just_above_descend_threshold_holds |
| 9 | None vertical_rate holds last confirmed state and never raises | ported | server/test_runway_config.py::test_none_vertical_rate_holds_last_confirmed_state_and_never_raises |
| 10 | 'ground' string vertical_rate holds last confirmed state and never raises | ported | server/test_runway_config.py::test_string_vertical_rate_holds_last_confirmed_state_and_never_raises |
| 11 | True (bool) vertical_rate holds last confirmed state, not read as int 1 | ported | server/test_runway_config.py::test_bool_vertical_rate_holds_last_confirmed_state_not_read_as_int_1 |
| 12 | dict vertical_rate holds last confirmed state and never raises | ported | server/test_runway_config.py::test_dict_vertical_rate_holds_last_confirmed_state_and_never_raises |
| 13 | SYNTHETIC (A-02-02-01, no real departure observed): +2400 large climb -> departing | ported | server/test_runway_config.py::test_synthetic_large_climb_departs |
| 14 | infer_from_flight() delegates on the flight dict's vertical_rate_fpm key | ported | server/test_runway_config.py::test_infer_from_flight_delegates_on_the_flight_dicts_vertical_rate_fpm_key |
| 15 | device_config.runway_label() returns an English label containing 'Runway ' and no 'Piste' for every RUNWAY_IDS member (D-11/A-29) | ported | server/test_runway_config.py::test_runway_labels_are_english_with_no_piste_vocabulary |

## stub-server/test_poll_cycle.py

# Ledger: stub-server/test_poll_cycle.py

Baseline: 32-BASELINE/stub-server__test_poll_cycle.txt (46 checks)

16 old checks were pure unit checks (no shared server/harness state) and map 1:1 to
16 independent pytest test functions. 2 old checks (the validate_display_response()
negative controls) needed no harness at all and became 2 more independent tests. The
remaining 28 old checks all ran against ONE long-lived byos_server.py subprocess,
sharing a device_token issued once, a served image hash later checks assert against,
and a battery_state.json persistence chain - a genuinely sequential scenario (MR-4),
so all 28 map to the same single consolidated node id.

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | seconds_until_quiet_hours_end() and _HHMM_RE are byte-for-byte identical between server/device_config.py and stub-server/byos_server.py | ported | stub-server/test_poll_cycle.py::test_quiet_hours_helpers_drift_guard_matches_device_config |
| 2 | read_quiet_hours() returns None and never raises for a missing, truncated, non-dict, disabled, or badly-shaped device_config.json | ported | stub-server/test_poll_cycle.py::test_read_quiet_hours_fail_open_never_raises |
| 3 | quiet_hours_sleep_s() returns 28000 inside an enabled 23:00-07:00 window and the unchanged base 300 once the window has ended | ported | stub-server/test_poll_cycle.py::test_quiet_hours_sleep_s_extends_inside_window_and_flat_after |
| 4 | quiet_hours_sleep_s() never returns less than the base sleep, even when the base already carries the device past the window's end | ported | stub-server/test_poll_cycle.py::test_quiet_hours_sleep_s_never_shorter_than_base |
| 5 | read_wake_interval_s() returns the caller's default and never raises for a missing, truncated, non-dict, key-absent, bool (true), string, float, or out-of-range device_config.json | ported | stub-server/test_poll_cycle.py::test_read_wake_interval_s_fail_open_never_raises |
| 6 | read_wake_interval_s() returns 120 for a stored 120 and returns the inclusive bounds 60 and 3600 unchanged | ported | stub-server/test_poll_cycle.py::test_read_wake_interval_s_happy_path |
| 7 | quiet_hours_sleep_s(read_wake_interval_s(...), ...) uses the configured 120 as its base with quiet hours disabled, and still returns 28000 (> 3600) inside an active quiet-hours window - the delivered value is deliberately not re-clamped | ported | stub-server/test_poll_cycle.py::test_wake_interval_layers_under_quiet_hours_without_reclamping |
| 8 | read_display_enabled() returns True and never raises for a missing, truncated, non-dict, key-absent, or non-bool (0, 1, "false", null) device_config.json, and a real display_enabled:false survives as False | ported | stub-server/test_poll_cycle.py::test_read_display_enabled_fail_open_never_raises |
| 9 | with display_enabled false and no quiet-hours window, quiet_hours_sleep_s(display_off_sleep_s(base, d), d) is exactly 300 for base in 60, 300, 900 and 3600 (base=3600 proves the pin REPLACES rather than bounds the configured interval) | ported | stub-server/test_poll_cycle.py::test_display_off_flat_pin_is_300_regardless_of_base |
| 10 | with the display off and quiet hours active, the served sleep_s is max(300, quiet_hours_remaining) in both directions: 28000s of remaining window time wins over the 300s off-state pin, and the 300s pin still floors the value when only 200s remain in the window - the device never wakes more often than quiet hours alone would have made it | ported | stub-server/test_poll_cycle.py::test_display_off_and_quiet_hours_overlap_unit |
| 11 | with display_enabled true, the composed sleep_s chain (including display_off_sleep_s()) equals the pre-existing Phase 10/11 quiet_hours_sleep_s(read_wake_interval_s(...)) chain across all four combinations of window active/inactive and wake_interval_s set/unset - this plan adds a branch, it does not alter the on-state behaviour | ported | stub-server/test_poll_cycle.py::test_display_on_state_matches_preexisting_chain |
| 12 | DISPLAY_OFF_SLEEP_S is numerically equal between server/device_config.py (read as plain text, never imported) and the loaded stub-server/byos_server.py module | ported | stub-server/test_poll_cycle.py::test_display_off_sleep_s_constant_parity |
| 13 | read_battery_critical() degrades to False and never raises for a missing file, malformed JSON, a non-dict payload, the string "true", the int 1, and a literal false, returns True only for a literal JSON true, and matches server.wake.read_battery_critical() on every one of those fixtures (behaviour parity) | ported | stub-server/test_poll_cycle.py::test_read_battery_critical_fail_open_matches_server_wake |
| 14 | battery_critical_sleep_s(base, d, fresh_mv): not parked returns base unchanged for every base; parked returns exactly BATTERY_CRITICAL_SLEEP_S (3600) for bases 60/300/3600; a fresh reading of 3800 mV (at or above BATTERY_CRITICAL_RECOVER_MV) anticipates recovery and returns base instead; and a fresh reading of 3500 mV or None stays parked | ported | stub-server/test_poll_cycle.py::test_battery_critical_sleep_s_unit |
| 15 | the composed sleep_s chain: parked with display_enabled=False yields exactly 3600s (the parked pin beats the 300s off-state pin), and parked inside a quiet-hours window with more than 3600s remaining yields the window's own remaining time (the longer pin always wins, D-05's sleep axis extended) | ported | stub-server/test_poll_cycle.py::test_battery_critical_composed_chain |
| 16 | BATTERY_CRITICAL_SLEEP_S is numerically equal between server/device_config.py and the loaded stub-server/byos_server.py module, and BATTERY_CRITICAL_RECOVER_MV is numerically equal between server/poll_loop.py and the loaded module - both read as plain text, never imported | ported | stub-server/test_poll_cycle.py::test_battery_critical_constant_parity |
| 17 | setup issues a 64-lowercase-hex device_token | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 18 | setup rejects a body missing mac with 422 | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 19 | display poll with no Authorization header returns 401 | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 20 | display poll with an unissued bearer returns 401 | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 21 | display poll returns a valid response shape (incl. firmware:null and led_enabled:true) | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 22 | download yields exactly 960000 bytes matching image_hash | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 23 | verify_panel_bytes rejects a flipped byte | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 24 | verify_panel_bytes rejects a one-byte truncation | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 25 | a second poll of an unchanged image returns the same image_hash (hash-skip) | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 26 | replacing the served image changes image_hash and the client re-downloads | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 27 | telemetry headers are accepted and echoed to server stdout | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 28 | log endpoint accepts a logs array and returns ok:true | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 29 | validate_display_response rejects sleep_s=0 | ported | stub-server/test_poll_cycle.py::test_validate_display_response_rejects_sleep_s_zero |
| 30 | validate_display_response rejects uppercase hex in image_hash | ported | stub-server/test_poll_cycle.py::test_validate_display_response_rejects_uppercase_image_hash |
| 31 | default --image-url-scheme (http) is served in image_url | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 32 | --image-url-scheme https serves image_url with https:// and an unchanged host/path/digest | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 33 | an authenticated poll carrying a plausible X-Battery-Mv persists {battery_mv, received_at} to battery_state.json, and a second poll with a different value overwrites it | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 34 | 10 hostile/malformed X-Battery-Mv values (non-digit, negative, float, whitespace, injection, out-of-range, the '0' unknown sentinel, empty, oversized, non-ASCII digits) all return 200 and persist nothing - the previously persisted value survives byte-identical | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 35 | a display poll with a bogus bearer token returns 401 and never writes battery_state.json (the write barrier sits strictly behind bearer_ok()) | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 36 | a device_config.json with led_enabled:false yields a 200 display response with led_enabled:false | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 37 | a device_config.json with a hostile string led_enabled yields a 200 display response with led_enabled:true | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 38 | a truncated/invalid device_config.json still yields a 200 display response with led_enabled:true and passes validate_display_response() | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 39 | a device_config.json with a currently-active quiet-hours window yields a 200 display response whose sleep_s is strictly greater than the base --sleep (300) and no greater than 7200 | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 40 | a hostile device_config.json (non-HH:MM quiet_hours_start, null quiet_hours_end) still yields a 200 display response with sleep_s exactly equal to the base --sleep (300) | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 41 | a device_config.json with wake_interval_s:120 yields a 200 display response with sleep_s exactly 120 - not the harness's base --sleep (300) | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 42 | a device_config.json with a below-floor wake_interval_s:30 yields a 200 display response with sleep_s exactly 300 (the fail-open CLI default), and still passes validate_display_response() | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 43 | with display_enabled false and quiet hours enabled for a window still ~1h from ending, a live GET /device/v1/display response's sleep_s is strictly greater than 300 (the remaining window time wins over the flat off-state pin, D-05's sleep axis) - this is the check the composition-order negative control targets | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 44 | with poll_state.json's battery_critical_active latched True, a live GET /device/v1/display carrying X-Battery-Mv:3290 returns sleep_s exactly 3600 (the parked pin) | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 45 | with poll_state.json's battery_critical_active still latched True, a live GET /device/v1/display carrying a recovering X-Battery-Mv:4100 returns the base sleep_s (300), not the stale 3600s parked pin - recovery is anticipated within THIS request | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |
| 46 | a poll against a stopped server is classified as a failed wake, not a crash | ported | stub-server/test_poll_cycle.py::test_device_protocol_end_to_end_over_real_http |

