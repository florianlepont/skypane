# Ledger: server/test_enrich.py

Baseline: 32-BASELINE/server__test_enrich.txt (60 checks)

| # | Old check label | Disposition | New node id / reason |
| --- | --- | --- | --- |
| 1 | replaying the real recorded adsbdb hit (TVF16VB) yields the normalised route | ported | server/test_enrich.py::test_hit_fixture_normalises |
| 2 | replaying the real recorded adsbdb miss (EJU84YF, a genuine captured 404 - see fixtures/README.md) yields None | ported | server/test_enrich.py::test_real_recorded_miss_yields_none |
| 3 | a 500 response yields None without raising | ported | server/test_enrich.py::test_500_yields_none |
| 4 | a connection error (transport raises) yields None without raising | ported | server/test_enrich.py::test_connection_error_yields_none |
| 5 | a 200 response with a non-JSON body yields None without raising | ported | server/test_enrich.py::test_non_json_body_yields_none |
| 6 | a 200 body missing the 'response' key yields None | ported | server/test_enrich.py::test_missing_response_key_yields_none |
| 7 | a 200 body missing the 'flightroute' key yields None | ported | server/test_enrich.py::test_missing_flightroute_key_yields_none |
| 8 | a structurally incomplete 200 body (missing/non-string municipality) yields None - UI-SPEC has no partial state | ported | server/test_enrich.py::test_half_resolved_route_yields_none |
| 9 | two consecutive lookups for the same callsign invoke the transport exactly once (cached hit) | ported | server/test_enrich.py::test_hit_is_cached |
| 10 | a cached miss (a callsign that returned 404) is never re-queried on a later lookup | ported | server/test_enrich.py::test_miss_is_cached |
| 11 | the cache round-trips through json.dumps/json.loads and still honours both a cached hit and a cached miss | ported | server/test_enrich.py::test_cache_round_trips_through_json |
| 12 | callsigns are normalised (stripped, upper-cased) before both the cache key and the request URL are built | ported | server/test_enrich.py::test_normalisation_shares_one_cache_entry_and_request |
| 13 | to_sentence_case_city lower-cases interior connective particles but capitalises the first word and all other words | ported | server/test_enrich.py::test_sentence_case_lowercases_interior_particles |
| 14 | _primary_city_name() reduces a '/'-separated compound municipality name to its first segment, unchanged when there is no '/' | ported | server/test_enrich.py::test_primary_city_name_reduces_compound_municipality |
| 15 | _parse_route() applies _primary_city_name() to both origin_city and destination_city, not just one (D-09-style, Phase 9 09-04) | ported | server/test_enrich.py::test_parse_route_applies_primary_city_name_to_both_cities |
| 16 | a callsign containing non-alphanumeric characters is rejected before the outbound request is built (T-02-04-02) | ported | server/test_enrich.py::test_unsafe_callsign_never_queried |
| 17 | city_for_state() returns the destination city for departing and the origin city for arriving | ported | server/test_enrich.py::test_city_for_state_picks_correct_end |
| 18 | normalise_callsign strips and upper-cases, returning None for anything empty or non-string | ported | server/test_enrich.py::test_normalise_callsign_edge_cases |
| 19 | airline_from_callsign('TVF16VB') returns 'Transavia France' | ported | server/test_enrich.py::test_airline_from_callsign_tvf |
| 20 | airline_from_callsign(' tvf16vb ') normalises through normalise_callsign() before the prefix lookup | ported | server/test_enrich.py::test_airline_from_callsign_normalises |
| 21 | airline_from_callsign() returns None (never raises) for an unknown prefix, a bare 3-letter string, empty string, None, an int, and a path-separator payload | ported | server/test_enrich.py::test_airline_from_callsign_never_raises_battery |
| 22 | airline_only_route('Transavia France') carries _parse_route()'s exact key set, airline_name set and the other four keys None; airline_only_route(None) returns None | ported | server/test_enrich.py::test_airline_only_route_shape_and_none_handling |
| 23 | resolve_route('EJU84YF', ...) on the real recorded adsbdb miss yields an 'easyJet' airline-only route with source 'airline_only', identically on a second call, with exactly one transport invocation | ported | server/test_enrich.py::test_resolve_route_airline_only_on_real_miss |
| 24 | resolve_route('TVF16VB', ...) classifies fresh_hit on the first call and cache_hit on the second | ported | server/test_enrich.py::test_resolve_route_fresh_then_cache_hit |
| 25 | resolve_route() on a miss whose callsign prefix is absent from the table returns (None, 'miss') | ported | server/test_enrich.py::test_resolve_route_unknown_prefix_is_miss |
| 26 | every value in enrich._ICAO_AIRLINE_PREFIXES is a member of illustrations.target_airline_names() (D-07 drift guard) | ported | server/test_enrich.py::test_prefix_table_values_are_a_subset_of_illustration_targets |
| 27 | every key of enrich._ICAO_AIRLINE_PREFIXES is exactly 3 uppercase A-Z characters | ported | server/test_enrich.py::test_prefix_table_keys_are_three_uppercase_letters |
| 28 | airline_from_callsign('KMM466') returns 'KM Malta Airlines' (260827-jz6, real curled callsign) | ported | server/test_enrich.py::test_airline_from_callsign_km_malta |
| 29 | airline_from_callsign('JAF7521') returns 'TUIfly Belgium' (260827-jz6, real curled callsign, QT-jz6-D-02 override) | ported | server/test_enrich.py::test_airline_from_callsign_tuifly_belgium |
| 30 | correct_airline_name('AIA6412', 'Avies') returns 'Amelia' (260827-kih) | ported | server/test_enrich.py::test_correct_airline_name_aia_to_amelia |
| 31 | correct_airline_name('ZZZ1234', 'Avies') returns 'Avies' unchanged - a different prefix carrying the same string is never rewritten (QT-kih-D-01) | ported | server/test_enrich.py::test_correct_airline_name_is_prefix_scoped |
| 32 | resolve_route('AIA6412', ...) corrects the real recorded AIA/Avies misattribution to 'Amelia' on both a fresh_hit and a cache_hit, while the cache entry itself keeps the raw upstream string (QT-kih-D-02) | ported | server/test_enrich.py::test_resolve_route_corrects_aia_fresh_then_cached |
| 33 | airline_from_callsign('AIA6412') returns 'Amelia' (260827-kih, zero network call) | ported | server/test_enrich.py::test_airline_from_callsign_aia_amelia |
| 34 | every _AIRLINE_NAME_CORRECTIONS row agrees with _ICAO_AIRLINE_PREFIXES and its corrected value is a target_airline_names() member (QT-kih-D-03 cross-table invariant) | ported | server/test_enrich.py::test_correction_table_agrees_with_prefix_table_and_targets |
| 35 | correct_airline_name()/apply_airline_name_correction() never raise for a hostile callsign x airline_name/route battery (None, int, empty string, bare prefix, path-separator payload, non-dict route, a route whose .get() raises), and never return a value derived from the arguments other than the unchanged airline_name (T-kih-01) | ported | server/test_enrich.py::test_correction_seam_never_raises_battery |
| 36 | correct_airline_name() maps FPO/Europe Airpost -> ASL Airlines France, CRL/Corsairfly -> Corsair, CCM/CCM Airlines -> Air Corsica (260827-kih) | ported | server/test_enrich.py::test_correct_airline_name_three_stale_brand_pairs |
| 37 | resolve_route() corrects all three stale-brand carriers under their own prefix and leaves the same string untouched under an unrelated prefix; the corrected Air Corsica route selects the renamed air-corsica.png/air-corsica-atr72.png files (260827-kih) | ported | server/test_enrich.py::test_resolve_route_and_selection_for_three_stale_brand_carriers |
| 38 | airline_from_callsign('HOP4001') returns 'Air France Hop' (260827-lgt, real curled callsign) | ported | server/test_enrich.py::test_airline_from_callsign_hop_air_france |
| 39 | airline_from_callsign('WMT3001') returns 'Wizz Air' (260827-lgt, real curled callsign WMT3001; adsbdb itself resolves 'Wizz Air Malta', QT-lgt-D-01 deliberate brand consolidation) | ported | server/test_enrich.py::test_airline_from_callsign_wizz_air_malta |
| 40 | airline_from_callsign('KLJ123') returns 'KlasJet' (260827-lgt, KLJ123 is a SYNTHETIC shape-valid callsign - no real KLJ callsign was ever live-confirmed, QT-lgt-D-06) | ported | server/test_enrich.py::test_airline_from_callsign_klasjet |
| 41 | no _AIRLINE_NAME_CORRECTIONS row exists whose prefix element is HOP, WMT or KLJ (QT-lgt-D-07 guard) | ported | server/test_enrich.py::test_no_correction_row_for_new_lgt_prefixes |
| 42 | note_unresolved_prefix() on a first sighting records a well-formed entry (count 1, both timestamps set) | ported | server/test_enrich.py::test_note_unresolved_prefix_first_sighting |
| 43 | note_unresolved_prefix() on a second sighting increments count, updates last_seen/example_callsign, and never moves first_seen | ported | server/test_enrich.py::test_note_unresolved_prefix_second_sighting_updates_in_place |
| 44 | note_unresolved_prefix() returns None and records nothing for every single prefix already present in _ICAO_AIRLINE_PREFIXES (full-table invariant, not sampled) | ported | server/test_enrich.py::test_note_unresolved_prefix_never_records_a_covered_prefix |
| 45 | note_unresolved_prefix() never records and never raises for None/int/empty/whitespace/bare-prefix/lowercase/path-separator/dot-dot/digit-in-prefix/non-dict-registry input, rebuilds a malformed pre-existing entry, and bounds the stored example for a 900-character legitimate-shape callsign | ported | server/test_enrich.py::test_note_unresolved_prefix_hostile_input_battery |
| 46 | trim_unresolved_prefixes() evicts lowest-count-then-oldest-last-seen, so a recurring prefix survives a flood of one-off arrivals and the newer one-off wins the tie-break (QT-oz9-D-04) | ported | server/test_enrich.py::test_trim_unresolved_prefixes_favours_recurrence |
| 47 | note_unresolved_prefix() records exactly the callsigns resolve_route() classifies 'miss' and none it classifies 'airline_only', on the same real production condition (an adsbdb 404) | ported | server/test_enrich.py::test_note_unresolved_prefix_agrees_with_resolve_route |
| 48 | _parse_route() reads callsign_iata from two real fixtures (TVF16VB -> 'TO16VB', AIA6412 -> 'U36412') (D-09) | ported | server/test_enrich.py::test_callsign_iata_parsed_from_real_hits |
| 49 | callsign_iata is optional and never route-fatal: absent/empty/whitespace/non-string values all still resolve a full route with callsign_iata degraded to None (D-09, T-08-02-01/T-08-02-02) | ported | server/test_enrich.py::test_callsign_iata_optional_never_route_fatal |
| 50 | a callsign resolved twice, the second time with no transport available, agrees on callsign_iata both on the plain hit path (TVF16VB) and the airline-name-correction path (AIA6412, Avies->Amelia) - proving both _route_from_entry() and apply_airline_name_correction()'s shallow copy preserve the field (D-09) | ported | server/test_enrich.py::test_callsign_iata_cache_round_trip_parity |
| 51 | _parse_route(), _route_from_entry() and airline_only_route() all agree on one key set, derived from a real _parse_route() result rather than hardcoded (D-09) | ported | server/test_enrich.py::test_shape_parity_across_all_three_builders |
| 52 | _parse_route()'s returned dict has no 'callsign'/'callsign_icao' key and no value equal to the raw ICAO callsign string - the raw callsign is structurally absent, not just unused by the renderer (D-08) | ported | server/test_enrich.py::test_raw_icao_callsign_never_smuggled_in |
| 53 | airline_source_from_callsign() returns ('<name>', 'static') for a known static prefix, (None, None) for an unknown one, and (None, None) for the full 260827-hyy hostile-input battery, with no registry configured | ported | server/test_enrich.py::test_airline_source_from_callsign_static_path_parity |
| 54 | after manual_resolutions.set_manual_registry_state_dir(tmp) with a ZZZ->'Zephyr Air' entry, airline_source_from_callsign('ZZZ1234') returns ('Zephyr Air', 'manual') and airline_from_callsign() returns 'Zephyr Air'; clearing the state dir restores (None, None) | ported | server/test_enrich.py::test_airline_source_from_callsign_manual_path |
| 55 | a manual entry for a prefix already in the static table is never consulted (D-06): airline_source_from_callsign() still reports the static name and source 'static'; static_airline_name_for_prefix() returns the static name for that prefix, None for a manual-only prefix, and handles non-string/wrong-length input without raising | ported | server/test_enrich.py::test_d06_collision_and_static_airline_name_for_prefix |
| 56 | resolve_route() with a manual entry for a prefix absent from the static table and an adsbdb miss returns source 'manual' and an airline-only route whose airline_name is the operator's name and whose other five keys are None | ported | server/test_enrich.py::test_resolve_route_manual_source |
| 57 | a manual entry for a static-table prefix still yields 'airline_only' under an adsbdb miss (D-06), and adsbdb still wins by construction ('fresh_hit') even when a manual entry exists for that prefix | ported | server/test_enrich.py::test_resolve_route_static_and_adsbdb_precedence_over_manual |
| 58 | clear_resolved_unresolved_prefix() removes a manually-resolved prefix's entry and returns the prefix, a second call returns None, and a still-unresolved prefix's entry survives byte-identical (D-14) | ported | server/test_enrich.py::test_clear_resolved_unresolved_prefix_happy_path_and_idempotence |
| 59 | clear_resolved_unresolved_prefix() returns None and mutates nothing for a non-dict registry, None, an int, an empty string, a bare 3-letter callsign, and a path-separator payload - one check covering the whole hostile-input sweep | ported | server/test_enrich.py::test_clear_resolved_unresolved_prefix_hostile_input_sweep |
| 60 | the eleven 260921-v9c prefixes each resolve through airline_from_callsign() to their exact expected name (including TFV60HA and KAF001 as actually observed), the DJT correction seam rewrites 'Denver Jet' to 'La Compagnie' via correct_airline_name(), and DEF/QEM both resolve to None (QT-v9c-D-06 guard) | ported | server/test_enrich.py::test_v9c_eleven_new_prefixes_and_djt_correction_and_exclusions |
