---
phase: 32-test-foundation-pytest-and-ci-you-can-trust
plan: 15
type: execute
wave: 4
depends_on: [32-13, 32-14]
files_modified:
  - pyproject.toml
  - .claude/CLAUDE.md
  - CONTRIBUTING.md
  - README.md
  - server/README.md
  - deploy/provision.sh
autonomous: true
requirements: [TST-09, TST-01, TST-04]

must_haves:
  truths:
    - "companion/app.py, stub-server/byos_server.py and stub-server/make_test_panel.py are no longer omitted and show measured, non-trivial coverage"
    - "fail_under equals the measured total rounded down (no margin), with the measurement documented in pyproject.toml"
    - "The full suite passes on Python 3.14 from a clean venv installed with --require-hashes"
    - "CLAUDE.md, CONTRIBUTING, README and server/README describe pytest as the way to run tests and Python 3.14 as the target"
  artifacts:
    - path: "pyproject.toml"
      provides: "omit list without the three subprocess-launched files; measured fail_under with derivation comment"
      contains: "fail_under ="
    - path: ".claude/CLAUDE.md"
      provides: "Tests / CI stack row describing pytest; Server row Python 3.14"
      contains: "pytest"
    - path: "CONTRIBUTING.md"
      provides: "How to run tests with pytest; lock regeneration"
      contains: "pytest"
    - path: "README.md"
      provides: "Requires Python 3.14; pytest-based Tests section"
      contains: "Python 3.14"
  key_links:
    - from: "pyproject.toml [tool.coverage.report] fail_under"
      to: "./scripts/run-all-tests.sh"
      via: "pytest-cov gate"
      pattern: "fail_under = [0-9]+"
---

<objective>
Finish TST-09 and the documentation parts of TST-01/TST-04: remove `companion/app.py`, `stub-server/byos_server.py` and `stub-server/make_test_panel.py` from the coverage omit list now that `patch = ["subprocess"]` measures them, run the full suite on Python 3.14 from a clean hash-locked venv, set `fail_under` to the measured floor (rounded down, no margin) with the derivation documented, and update CLAUDE.md, CONTRIBUTING, README and server/README.

Output: pyproject.toml coverage changes, updated docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-13-SUMMARY.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-12-SUMMARY.md
@pyproject.toml
@scripts/run-all-tests.sh

<planner_findings>
- CONTEXT.md TST-09: "Measure, then set fail_under to the measured floor (rounded down, no 4-point margin), and document the measurement in pyproject.toml." The existing `fail_under = 83` comment documents an older derivation with a 4-point margin; replace it with the new one (measured value, date, interpreter, scope, command).
- Measure on Python 3.14 (what CI runs): the sandbox has `uv` and a local cpython-3.14.0rc2 (RESEARCH Pitfall 4). Build a scratch venv with `uv venv --python 3.14 --seed`, install `server/requirements-dev.txt` with `--require-hashes`, install the Chromium headless shell (`python -m playwright install --only-shell chromium`, plus `--with-deps` if the sandbox lacks browser libraries) so the browser harnesses run instead of skipping, then `PYTHON=<scratch>/bin/python3 ./scripts/run-all-tests.sh`. A run where browser harnesses SKIP under-measures companion code; record Chromium availability.
- Other references to update: README "Requires Python 3.12." (line ~86) and its Tests section ("There is no pytest: each test_*.py file is a standalone..."), server/README.md "Running the tests" (says no pytest), CONTRIBUTING line 10, CLAUDE.md Server row ("Python 3.12") and Tests / CI row ("stdlib test harnesses"), deploy/provision.sh comment (~62-66) that cites server/README's "development target CPython 3.12".
- D-A3: everything in English; comments state why, not history.
- `.claude/CLAUDE.md` edits are a locked developer decision (D-A2: "CLAUDE.md Tests / CI stack row and CONTRIBUTING are updated"); change only the two stack-table rows named below, not the GSD or profile sections.
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subprocess-measured coverage on Python 3.14; fail_under at the measured floor</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml ([tool.coverage.run] omit list and comments; [tool.coverage.report] fail_under and its derivation comment)
    - scripts/run-all-tests.sh (plan 32-13 wrapper)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-13-SUMMARY.md (coverage total and whether the combine fallback was needed)
  </read_first>
  <action>
Per TST-09 and the CONTEXT.md coverage decision:
1. Remove `stub-server/byos_server.py`, `stub-server/make_test_panel.py` and `companion/app.py` (and their "structurally unmeasurable" comments) from `[tool.coverage.run] omit`; keep the three `test_*.py` globs and add `test-support/*` only if coverage ever reports it (it is outside `source`, so it should not).
2. Build the 3.14 scratch venv per planner findings and run `PYTHON=<scratch>/bin/python3 ./scripts/run-all-tests.sh --cov-report=term-missing`. All tests must pass. Read the per-file lines for the three files: each must be present with a non-zero percentage well above module-import level (companion/app.py was ~21% import-only in the old omit rationale; expect route bodies covered now). If any of the three shows 0% or import-only coverage, subprocess coverage is not reaching it: debug (child env, `patch = ["subprocess"]`, data-file combine) before setting any number.
3. Set `fail_under` to the measured TOTAL rounded down to an integer (e.g. 91.7 → 91). The gate only ratchets up: if the floor is below the current 83, stop and report instead of lowering it. Replace the old derivation comment with: measured total with one decimal, date, interpreter (`python --version` of the scratch venv), the command, the scope (source + omit as now configured), the rule "measured floor, no margin, only ratchets up; changing source/omit invalidates it" (D-A3: no plan history).
4. Re-run `./scripts/run-all-tests.sh` with the dev venv (server/.venv) and confirm the gate passes there too; if the dev venv's total differs from the 3.14 total by enough to fail, record both in the SUMMARY and keep the 3.14 figure (CI is 3.14) only if the dev-venv run still passes; otherwise set fail_under to the lower of the two floors and say why in the comment.
Remove the scratch venv afterwards.
  </action>
  <verify>
    <automated>! grep -qE '"(stub-server/byos_server.py|stub-server/make_test_panel.py|companion/app.py)"' pyproject.toml && grep -qE '^fail_under = [0-9]+$' pyproject.toml && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"(stub-server/byos_server.py|stub-server/make_test_panel.py|companion/app.py)"' pyproject.toml` prints 0
    - `grep -E '^fail_under = [0-9]+$' pyproject.toml` prints one line whose value is > 0 and equals floor(measured total) recorded in the comment above it
    - The comment above fail_under contains the measured percentage with one decimal, a 2026 date and "3.14"
    - The 3.14 run's coverage table (pasted in the SUMMARY) lists `companion/app.py`, `stub-server/byos_server.py` and `stub-server/make_test_panel.py` each with a non-zero percentage
    - `PYTHON=<scratch 3.14 venv>/bin/python3 ./scripts/run-all-tests.sh` exited 0 (recorded in the SUMMARY with the interpreter version and Chromium availability)
    - `./scripts/run-all-tests.sh` (server/.venv) exits 0
  </acceptance_criteria>
  <done>The three subprocess-launched files are measured; the gate sits at the measured floor, documented; the suite is green on 3.14.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation — pytest and Python 3.14</name>
  <files>.claude/CLAUDE.md, CONTRIBUTING.md, README.md, server/README.md, deploy/provision.sh</files>
  <read_first>
    - .claude/CLAUDE.md (Technology Stack table: Server and Tests / CI rows)
    - CONTRIBUTING.md ("Before you start" section)
    - README.md ("Running it locally" and "Tests" sections, lines ~80-125)
    - server/README.md ("Running the tests" section, lines ~27-40)
    - deploy/provision.sh lines 55-75
    - scripts/run-all-tests.sh and scripts/lock-deps.sh (the commands being documented)
  </read_first>
  <action>
Per D-A2 ("CLAUDE.md Tests / CI stack row and CONTRIBUTING are updated to describe pytest as the way to run tests") and D-A4 (Python 3.14):
- `.claude/CLAUDE.md`: Server row "Python 3.12" → "Python 3.14 (Ubuntu 26.04 distro python3)"; Tests / CI row → pytest + pytest-xdist + pytest-cov (`./scripts/run-all-tests.sh` wraps `pytest -n auto --cov`), pytest-socket non-loopback guard, companion harnesses run through a pytest shim until Phase 33, ruff, coverage gate at the measured floor, Playwright headless shell, hash-locked `server/requirements*.txt`, GitHub Actions with a reviewer-gated deploy, firmware host tests in firmware.yml. Touch no other part of the file.
- `CONTRIBUTING.md`: replace the test bullet with: set up `server/.venv` on Python 3.14 and `pip install --require-hashes -r server/requirements-dev.txt`; run `./scripts/run-all-tests.sh` (or `server/.venv/bin/python3 -m pytest <path> -k <expr>` for a subset); tests must not use the network (the guard fails them; use the `fake_providers` fixture) and must write only under `tmp_path`; dependency changes go in `server/requirements*.in` then `scripts/lock-deps.sh`.
- `README.md`: "Requires Python 3.12." → "Requires Python 3.14."; the venv install lines use `--require-hashes -r server/requirements.txt` (runtime) and mention `server/requirements-dev.txt` for tests; rewrite the Tests section: pytest via `./scripts/run-all-tests.sh` (what CI runs), `JOBS=1` for serial, extra args passed to pytest, coverage gate, firmware host tests command; remove "There is no pytest".
- `server/README.md`: rewrite "Running the tests" for pytest (`server/.venv/bin/python3 -m pytest server/test_plane_detection.py`), drop the "no pytest" convention text.
- `deploy/provision.sh`: update only the comment that cites "development target as CPython 3.12" to say the project targets the distro python3 (3.14 on Ubuntu 26.04); no code change.
All English, no plan history (D-A3).
  </action>
  <verify>
    <automated>grep -q "Requires Python 3.14" README.md && ! grep -q "There is no pytest" README.md && grep -q "pytest" CONTRIBUTING.md && grep -q "pytest" server/README.md && grep -q "Python 3.14" .claude/CLAUDE.md && bash -n deploy/provision.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Requires Python 3.12" README.md` prints 0 and `grep -c "Requires Python 3.14" README.md` prints 1
    - `grep -ci "no pytest" README.md server/README.md` prints 0 for both files
    - `grep -c "require-hashes" README.md CONTRIBUTING.md` shows at least 1 in each
    - `grep "| Tests / CI |" .claude/CLAUDE.md | grep -c pytest` prints 1 and `grep "| Server |" .claude/CLAUDE.md | grep -c "3.14"` prints 1
    - `git diff .claude/CLAUDE.md | grep -c '^[-+]|'` prints 4 (exactly two table rows changed)
    - `git diff deploy/provision.sh | grep '^[-+][^-+]' | grep -vc '^[-+]\s*#'` prints 0 (comment-only change); `bash -n deploy/provision.sh` exits 0
    - `grep -c "fake_providers" CONTRIBUTING.md` prints at least 1 and `grep -c "lock-deps.sh" CONTRIBUTING.md` prints at least 1
  </acceptance_criteria>
  <done>Docs describe pytest as the way to run tests, Python 3.14 as the target, and hash-locked dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge decision | A green CI claims "every check ran and coverage held" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-15-01 | Repudiation | coverage gate that no longer detects regressions | mitigate | fail_under at the measured floor (no margin), measured on the CI interpreter with subprocess coverage, documented in pyproject.toml |
| T-32-15-02 | Tampering | docs instructing unhashed installs | mitigate | README/CONTRIBUTING show `--require-hashes` installs and the lock regeneration script |
| T-32-15-03 | Information Disclosure | contributors adding network-dependent tests | mitigate | CONTRIBUTING states the no-network rule and the fake_providers fixture; the guard enforces it |
</threat_model>

<verification>
- `./scripts/run-all-tests.sh` exits 0 with the new gate
- The 3.14 full-suite run result, coverage table for the three files and Chromium availability are in the SUMMARY
</verification>

<success_criteria>
ROADMAP success criterion 4 (coverage measured for companion/app.py and byos_server.py; gate raised to the measured floor) and the documentation half of TST-01/TST-04.
</success_criteria>

<output>
Create `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-15-SUMMARY.md` when done
</output>
