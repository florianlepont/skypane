---
phase: 32-test-foundation-pytest-and-ci-you-can-trust
plan: 03
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - server/test_dither.py
  - server/test_runway_config.py
  - server/test_notify.py
  - server/test_panel_preview.py
  - server/test_pipeline_e2e.py
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_dither.md
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_runway_config.md
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_notify.md
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_panel_preview.md
  - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_pipeline_e2e.md
autonomous: true
requirements: [TST-02, TST-03]

must_haves:
  truths:
    - "`pytest server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py server/test_pipeline_e2e.py -n 4` passes"
    - "Every baseline check of these five harnesses maps to a collected node id or a justified deletion"
    - "The byos_server.py child launched by test_pipeline_e2e runs with the no-network guard (child_env)"
    - "No migrated test writes outside tmp_path or touches the network"
  artifacts:
    - path: "server/test_dither.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: "server/test_runway_config.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: "server/test_notify.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: "server/test_panel_preview.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: "server/test_pipeline_e2e.py"
      provides: "pytest tests migrated from the hand-rolled harness"
      contains: "def test_"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_dither.md"
      provides: "ledger rows for server/test_dither.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_runway_config.md"
      provides: "ledger rows for server/test_runway_config.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_notify.md"
      provides: "ledger rows for server/test_notify.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_panel_preview.md"
      provides: "ledger rows for server/test_panel_preview.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
    - path: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_pipeline_e2e.md"
      provides: "ledger rows for server/test_pipeline_e2e.py"
      contains: "| # | Old check label | Disposition | New node id / reason |"
  key_links:
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_dither.md"
      to: "server/test_dither.py"
      via: "every ported row names a collected node id in server/test_dither.py"
      pattern: "server/test_dither.py::test_"
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_runway_config.md"
      to: "server/test_runway_config.py"
      via: "every ported row names a collected node id in server/test_runway_config.py"
      pattern: "server/test_runway_config.py::test_"
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_notify.md"
      to: "server/test_notify.py"
      via: "every ported row names a collected node id in server/test_notify.py"
      pattern: "server/test_notify.py::test_"
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_panel_preview.md"
      to: "server/test_panel_preview.py"
      via: "every ported row names a collected node id in server/test_panel_preview.py"
      pattern: "server/test_panel_preview.py::test_"
    - from: ".planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_pipeline_e2e.md"
      to: "server/test_pipeline_e2e.py"
      via: "every ported row names a collected node id in server/test_pipeline_e2e.py"
      pattern: "server/test_pipeline_e2e.py::test_"
---

<objective>
Migrate the five smallest server-side harnesses — `server/test_dither.py` (6 checks), `server/test_runway_config.py` (14), `server/test_notify.py` (8), `server/test_panel_preview.py` (11) and `server/test_pipeline_e2e.py` (7; launches `stub-server/byos_server.py` as a subprocess) — to pytest. These establish the translation conventions on the cheapest files (RESEARCH batch A).

Purpose: TST-02 — server-side harnesses become real pytest tests with every pre-migration check accounted for in the migration ledger.
Output: migrated test module(s) and their ledger fragment(s) under `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-CONTEXT.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-PATTERNS.md
@server/test_dither.py
@server/test_runway_config.py
@server/test_notify.py
@server/test_panel_preview.py
@server/test_pipeline_e2e.py
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md
@conftest.py
@test-support/skypane_test_support.py

<migration_rules>
Identical in every migration plan (32-03..32-10). "Harness" = one of the hand-rolled files; "baseline" = `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/<key>.txt` captured by plan 32-02.

- MR-1 Translation: each old `check(label, fn)` becomes one pytest test function `test_<descriptive_snake_case>`, or one id of a `@pytest.mark.parametrize` when the old check was emitted from a loop. The old label is the test docstring verbatim (a parametrized or consolidated test lists its labels in the docstring or carries them in readable param ids). `return False, reason` becomes `assert <condition>, reason` (or `pytest.fail(reason)` where no single boolean exists); `return True, ""` disappears; exceptions propagate — never wrap a test body in try/except that turns an error into a pass.
- MR-2 Remove `check()`, the `results` list, every `EXPECTED_CHECK_COUNT`, `main()`, the "N/M checks pass" summary and the import-failure guard at the top of `main()` (pytest reports collection errors itself).
- MR-3 Legacy-runner bridge (removed by plan 32-13): keep the file's existing repo-root `sys.path` bootstrap and end the file with an `if __name__ == "__main__":` block that does `raise SystemExit(pytest.main([__file__, "-q", "-p", "no:cacheprovider"]))`, so `scripts/run_all_tests.py` (still CI's runner until plan 32-13) keeps running the file under coverage.
- MR-4 Independence: no test depends on another test's side effects or on execution order. Shared expensive setup → a `scope="module"` fixture built with `tmp_path_factory`. A genuinely sequential scenario (a later old check asserts on state an earlier one created) → one test function whose asserts cover those old checks (N old checks → 1 node id in the ledger). The module must pass with `-n 0` and with `-n 4`.
- MR-5 Filesystem: every path lives under `tmp_path` / `tmp_path_factory`; the migrated file contains no `tempfile.` call; a test run leaves no new untracked file in the repo.
- MR-6 Global state (module attributes, env vars, cwd, `socket.getaddrinfo`): patch with `monkeypatch`, never manual save/restore.
- MR-7 Network: no test opens a non-loopback socket or resolves a non-loopback name — the pytest-socket allow-hosts guard and the autouse DNS guard from plan 32-01 fail such a test. ADS-B/adsbdb HTTP goes through the `fake_providers` fixture (repo-root conftest.py) or through the file's own function-level stubs converted to `monkeypatch.setattr`. Never add `@pytest.mark.enable_socket`. A fallback to the "real" `socket.getaddrinfo` inside a test now reaches the DNS guard: that is intended.
- MR-8 Root safety: every check whose outcome depends on permission bits (`os.chmod`, read-only files or directories, unwritable paths) gets `@requires_non_root` (`from skypane_test_support import requires_non_root`), with every path under tmp_path. A baseline FAIL line caused only by euid 0 is ported this way (the check runs in CI, where the runner is non-root).
- MR-9 Subprocesses: env built with `skypane_test_support.child_env()` (guard active in the child), loopback ports only, fixture teardown terminate → wait(timeout=5) → kill → wait.
- MR-10 Comments: keep only comments that explain why a behaviour is checked; drop phase/plan/ticket history (D-A3); English only.
- MR-11 No production code change. If the guard exposes a real network access, stub it in the test. Anything surprising (a check that cannot be ported as-is) goes into the SUMMARY with the reason.
- MR-12 Ledger fragment `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/<key>.md`, format exactly as described in `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md`: `# Ledger: <harness>`, a `Baseline:` line, then one table row per PASS/FAIL line of the baseline, in baseline order. `deleted` only with a concrete reason (for example: the check only asserted that a module imports, which pytest collection now guarantees). Verify with `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py <harness>`.
- MR-13 Test names are unique in the module and read as behaviour ("test_empty_state_is_white_dominant"), not as numbers.
</migration_rules>

<interfaces>
From test-support/skypane_test_support.py (plan 32-01; importable in tests because pyproject.toml sets `pythonpath = [".", "test-support"]`):
- `requires_non_root` — pytest skipif marker for euid 0
- `child_env(base=None, *, fake_providers=None, state_dir=None) -> dict[str, str]` — env for child processes: guard on, PYTHONPATH includes test-support; `fake_providers` = None | "default" | FakeProviders (written to state_dir)
- `class FakeProviders` — `respond(name, body, status=200)`, `fail(name, exc)`, `calls` (list of {"provider", "url"}); provider names "adsbfi", "adsblol", "airplaneslive", "adsbdb"; defaults: adsbfi (200, {"aircraft": []}), adsblol/airplaneslive (200, {"ac": []}), adsbdb (404, {"response": "unknown callsign"})
- `REPO_ROOT`, `TEST_SUPPORT_DIR`

From repo-root conftest.py (plan 32-01):
- fixture `fake_providers(monkeypatch) -> FakeProviders` — installed on `requests.get` (reaches `detect.query_provider()` and `enrich.default_transport()`), `detect.MIN_SECONDS_BETWEEN_CALLS` set to 0
- autouse DNS guard: `socket.getaddrinfo`/`gethostbyname`/`gethostbyname_ex` raise `NetworkAccessBlocked` for non-loopback names

From pyproject.toml (plan 32-01): `--import-mode=importlib --strict-markers`, markers `slow`, `legacy_harness`; pytest-socket `--allow-hosts=127.0.0.1,::1,localhost --allow-unix-socket`.
</interfaces>

<planner_notes>
- Check counts above are the harnesses' own first `EXPECTED_CHECK_COUNT`; the baseline transcript is authoritative.
- `server/test_dither.py:124-136` uses `tempfile.mkdtemp()` + `shutil.rmtree` in a finally: becomes `tmp_path` (32-PATTERNS.md shows the exact before/after for this file).
- `server/test_pipeline_e2e.py` has its own Harness class (free port via bind 127.0.0.1:0, `subprocess.Popen` of byos_server.py, readiness poll with `socket.create_connection`, terminate→kill teardown, `urllib.request.urlopen` to 127.0.0.1) and calls `poll_loop.run_once(snapshot=...)` with an injected snapshot (no live network by design). Turn the Harness into a yield fixture; pass `env=child_env()` to Popen; keep loopback-only URLs.
- `server/test_notify.py` exercises notification dispatch: any outbound HTTP (for example to an ntfy URL) must stay stubbed via monkeypatch — the guard will fail an unstubbed call.
- Parallel execution: this plan shares wave 2 with other migration plans in the same working tree. Only touch the files in `files_modified`. Run only this plan's test files (never `./scripts/run-all-tests.sh`, which clears shared coverage data files). Do not reinstall or recreate `server/.venv`.
</planner_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test_dither, test_runway_config, test_notify, test_panel_preview</name>
  <files>server/test_dither.py, server/test_runway_config.py, server/test_notify.py, server/test_panel_preview.py, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_dither.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_runway_config.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_notify.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_panel_preview.md</files>
  <read_first>
    - server/test_dither.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_dither.txt (the labels to map, in order)
    - server/test_runway_config.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_runway_config.txt (the labels to map, in order)
    - server/test_notify.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_notify.txt (the labels to map, in order)
    - server/test_panel_preview.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_panel_preview.txt (the labels to map, in order)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md (fragment format)
    - conftest.py and test-support/skypane_test_support.py (fixtures and helpers from plan 32-01)
  </read_first>
  <action>
Convert the four small harnesses in place, smallest first (dither → runway_config → notify → panel_preview). Use `tmp_path` for every temporary directory (dither's calibration preview, panel_preview's output images). Group related tests in plain functions; no test classes unless a shared fixture set makes one clearer.

Apply MR-1..MR-13 (see `<migration_rules>`). Write the ledger fragment(s) .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_dither.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_runway_config.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_notify.md, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_panel_preview.md with one row per baseline line. Per TST-02 and the CONTEXT.md ledger decision, every baseline check maps to a node id or to a deletion with a reason.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py -q -p no:cacheprovider && server/.venv/bin/python3 -m pytest server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py -q -p no:cacheprovider -n 4 && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_dither.py && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_runway_config.py && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_notify.py && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_panel_preview.py && server/.venv/bin/ruff check server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py -q -p no:cacheprovider` exits 0 (0 failed, 0 errors)
    - `server/.venv/bin/python3 -m pytest server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py -q -p no:cacheprovider -n 4` exits 0
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_dither.py` exits 0 and prints `server/test_dither.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_dither.py` prints 0
    - `server/.venv/bin/python3 server/test_dither.py` exits 0 (legacy-runner bridge, MR-3)
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_runway_config.py` exits 0 and prints `server/test_runway_config.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_runway_config.py` prints 0
    - `server/.venv/bin/python3 server/test_runway_config.py` exits 0 (legacy-runner bridge, MR-3)
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_notify.py` exits 0 and prints `server/test_notify.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_notify.py` prints 0
    - `server/.venv/bin/python3 server/test_notify.py` exits 0 (legacy-runner bridge, MR-3)
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_panel_preview.py` exits 0 and prints `server/test_panel_preview.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_panel_preview.py` prints 0
    - `server/.venv/bin/python3 server/test_panel_preview.py` exits 0 (legacy-runner bridge, MR-3)
    - After this task's commit and a fresh test run, `git status --porcelain --untracked-files=all server/ stub-server/ | grep '^??' | grep -cvE 'server/requirements(-dev)?\.in$'` prints 0 (tests wrote nothing into the repo; the two `.in` files are plan 32-12's, created in the same wave)
    - `server/.venv/bin/ruff check server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py` exits 0
    - `git diff --quiet HEAD -- server/plane server/device_config.py server/history_db.py server/notify.py server/panel_format.py server/panel_preview.py server/poll_loop.py server/wake.py stub-server/byos_server.py stub-server/make_test_panel.py` exits 0 (no production code change, MR-11)
  </acceptance_criteria>
  <done>server/test_dither.py, server/test_runway_config.py, server/test_notify.py, server/test_panel_preview.py run as real pytest tests; the ledger fragment(s) map every baseline check.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate test_pipeline_e2e (byos subprocess fixture)</name>
  <files>server/test_pipeline_e2e.py, .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_pipeline_e2e.md</files>
  <read_first>
    - server/test_pipeline_e2e.py (the whole harness: every check() call, its setup, its helpers)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-BASELINE/server__test_pipeline_e2e.txt (the labels to map, in order)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md (fragment format)
    - conftest.py and test-support/skypane_test_support.py (fixtures and helpers from plan 32-01)
    - stub-server/test_poll_cycle.py lines 235-315 (the Harness this file mirrors)
    - server/poll_loop.py run_once signature (~line 989)
  </read_first>
  <action>
Convert `server/test_pipeline_e2e.py`: its Harness becomes a `byos_server` yield fixture (function scope unless the old checks shared one server across checks, in which case module scope with state reset documented) that launches `stub-server/byos_server.py` with `env=child_env()` and state under tmp_path, and always terminates the child. Each old check becomes a test using that fixture.

Apply MR-1..MR-13 (see `<migration_rules>`). Write the ledger fragment(s) .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger/server__test_pipeline_e2e.md with one row per baseline line. Per TST-02 and the CONTEXT.md ledger decision, every baseline check maps to a node id or to a deletion with a reason.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest server/test_pipeline_e2e.py -q -p no:cacheprovider && server/.venv/bin/python3 -m pytest server/test_pipeline_e2e.py -q -p no:cacheprovider -n 4 && server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_pipeline_e2e.py && server/.venv/bin/ruff check server/test_pipeline_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest server/test_pipeline_e2e.py -q -p no:cacheprovider` exits 0 (0 failed, 0 errors)
    - `server/.venv/bin/python3 -m pytest server/test_pipeline_e2e.py -q -p no:cacheprovider -n 4` exits 0
    - `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py server/test_pipeline_e2e.py` exits 0 and prints `server/test_pipeline_e2e.py: N/N baseline checks mapped` with 0 pending
    - `grep -cE 'EXPECTED_CHECK_COUNT|def check\(|tempfile\.' server/test_pipeline_e2e.py` prints 0
    - `server/.venv/bin/python3 server/test_pipeline_e2e.py` exits 0 (legacy-runner bridge, MR-3)
    - After this task's commit and a fresh test run, `git status --porcelain --untracked-files=all server/ stub-server/ | grep '^??' | grep -cvE 'server/requirements(-dev)?\.in$'` prints 0 (tests wrote nothing into the repo; the two `.in` files are plan 32-12's, created in the same wave)
    - `server/.venv/bin/ruff check server/test_pipeline_e2e.py` exits 0
    - `git diff --quiet HEAD -- server/plane server/device_config.py server/history_db.py server/notify.py server/panel_format.py server/panel_preview.py server/poll_loop.py server/wake.py stub-server/byos_server.py stub-server/make_test_panel.py` exits 0 (no production code change, MR-11)
  </acceptance_criteria>
  <done>server/test_pipeline_e2e.py run as real pytest tests; the ledger fragment(s) map every baseline check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → network | Migrated tests run under the pytest-socket allow-hosts guard and the DNS guard |
| old harness → new tests | Checks could be dropped or weakened during translation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Repudiation | server/test_dither.py, server/test_runway_config.py, server/test_notify.py, server/test_panel_preview.py, server/test_pipeline_e2e.py | mitigate | Ledger fragment checked by 32-ledger-check.py: row count and label multiset equal the baseline, every ported node id is collected, every deletion has a reason |
| T-32-03-02 | Information Disclosure | tests reaching live ADS-B/adsbdb/calendar hosts | mitigate | MR-7: guard fails any non-loopback connect or DNS lookup; provider HTTP only via fake_providers or monkeypatched stubs; no enable_socket marker |
| T-32-03-03 | Tampering | tests writing outside tmp_path (repo tree, shared /tmp paths) | mitigate | MR-5: tmp_path only, no `tempfile.`, untracked-file check after the run |
</threat_model>

<verification>
- `server/.venv/bin/python3 -m pytest server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py server/test_pipeline_e2e.py -q -p no:cacheprovider -n 4` exits 0; every `server/.venv/bin/python3 .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-ledger-check.py <harness>` for this plan exits 0
- `grep -rn "enable_socket" server/test_dither.py server/test_runway_config.py server/test_notify.py server/test_panel_preview.py server/test_pipeline_e2e.py` prints nothing
</verification>

<success_criteria>
server/test_dither.py, server/test_runway_config.py, server/test_notify.py, server/test_panel_preview.py, server/test_pipeline_e2e.py are pytest modules that pass under xdist, touch no network, write only under tmp_path, and every baseline check is mapped in the ledger fragment(s).
</success_criteria>

<output>
Create `.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-03-SUMMARY.md` when done (include per-harness: baseline count, ported, deleted with reasons, consolidations, any root-only skips)
</output>
