---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 06
type: execute
wave: 3
depends_on: ["20-01", "20-03", "20-04"]
files_modified:
  - companion/pages/home_page.py
  - companion/i18n_fr/home.py
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-13, CFG-14, CFG-18]
user_setup: []

must_haves:
  truths:
    - "D-16: Home has no Quick actions card — no screen switch, no quiet-hours switch, no Refresh button; the /quick/* routes and their flash keys are untouched"
    - "D-17: Home is a hero row (the current picture, with its 'Rendered HH:MM' caption and, when known, a 'CALLSIGN · Airline · ORY → TLS' one-liner) beside one status card, then the recent flights full width"
    - "D-17: the status card's headline is the next update — 'Next update ≈ 22:25', or 'Expected since 12:03' in the warn treatment when that time is already past"
    - "D-17: the Frame verdict appears exactly once — the card renders FRAME_STATE_TEXT itself and takes only the timestamp detail from the health state"
    - "D-17.2: each recent flight shows the airline's 40x40 artwork thumbnail when one exists, lazily loaded, and the dashed placeholder when it does not — never a broken image"
    - "D-20: Home adds no query beyond the ones it already runs plus the pure-string airline-key resolve"
    - "D-21: the three status rows are layout.status_row() calls, not stat_tile() calls"
    - "D-30: in simple mode the 'See details on Health' link is not emitted"
    - "D-05 (Home): every user-visible string on Home renders through t(), with its French entry in companion/i18n_fr/home.py"
  artifacts:
    - path: "companion/pages/home_page.py"
      provides: "the rebuilt hero row, status card and recent flights"
      contains: "layout.status_row"
    - path: "companion/i18n_fr/home.py"
      provides: "Home's French catalogue"
      contains: "CATALOG"
  key_links:
    - from: "companion/pages/home_page.py"
      to: "companion/layout.py"
      via: "the shared status-row primitive"
      pattern: "layout\\.status_row\\("
    - from: "companion/pages/home_page.py"
      to: "server/plane/illustrations.py"
      via: "the airline-key resolver the Airlines page already uses"
      pattern: "illustrations\\.normalise_airline_key"
---

<objective>
Rebuild Home as the glanceable page CFG-14 asks for: picture and one status card
side by side, the next update as the headline, the recent flights with artwork
thumbnails, no quick-action widgets, no duplicated verdict, and no past time
presented as a future one.

Purpose: Home is the landing page and the one page the second household member
sees most. Today it repeats the frame verdict twice, shows a "Next wake" that can
already be in the past, and carries three switches that belong on the pages that
own those settings.
Output: a rewritten `companion/pages/home_page.py`, its French catalogue, and
retargeted checks in `companion/test_view_pages.py` — the file that owns every
Home check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
Available from earlier waves:
  layout.status_row(label, verdict, detail, state)        # 20-03
  ctx["health_state"]["device_detail_html"]               # 20-03 — the
      verdict-free timestamp fragment; ctx["health_state"]["device_html"] still
      exists for Health's own tile and must NOT be used by Home any more
  layout.QUICK_STATE_FIELD / QUICK_STATE_ON / QUICK_STATE_OFF   # 20-01
  ctx["lang"], ctx["simple_mode"]                          # 20-01
  i18n.t(text)                                             # 20-01
  CSS: .home-hero, .preview-frame*, .status-card__headline[--warn],
       .status-card__rows, .status-row*, .recent-flight__thumb[--placeholder]  # 20-04

Home's own current shape — companion/pages/home_page.py (382 lines, read in full):
  _status_tiles_html(ctx)   # :166-226 — the three stat_tile() calls, the
      next-wake block at :184-192, and the device_html embed at :175-177
  _quick_actions_html(ctx)  # :240-311 and _toggle_form_html() :229-237 — deleted
  _now_showing_html(ctx)    # :314-338 — becomes the hero's picture half
  _recent_flights_html(ctx) # :340-369 — gains the thumbnail column
  FRAME_STATE_TEXT / DATA_STATE_TEXT / BATTERY_STATE_TEXT  # :99-113 — kept verbatim

The resolver to reuse — server/plane/illustrations.py:540 normalise_airline_key(
  airline_name): a pure string function returning "" for a falsy name (the exact
  call airlines_page.py:657 already makes — no new query, per D-20).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Quick actions card and rebuild the status card on status_row()</name>
  <files>companion/pages/home_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/home_page.py (the whole file, 382 lines)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md sections A and B (the status-card markup, the headline's two states, and the "verdict and detail must differ" contract)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-RESEARCH.md Pitfall 3 (the confirmed root cause of the duplicated verdict)
    - companion/layout.py: `status_row()`, `card_status_class()`, `parse_iso()`, `local_clock_text()` and the QUICK_STATE_* constants (all present from waves 1-2)
    - companion/wake.py / server/wake.py `next_wake_at_iso()` (unchanged behaviour behind the shim)
    - companion/test_view_pages.py: grep `home_page.` (13 references — this file owns every Home check) and read each one, plus the EXPECTED_CHECK_COUNT trail and the check() helper
  </read_first>
  <action>
    Delete from `companion/pages/home_page.py`: `_quick_actions_html()`,
    `_toggle_form_html()`, their call site in `render()`, and the constants only
    they used — `QUICK_ACTIONS_HEADING`, `QUICK_ACTIONS_CAPTION`,
    `SCREEN_WIDGET_LABEL`, `SCREEN_ON_TEXT`, `SCREEN_OFF_TEXT`,
    `SCREEN_TURN_ON_BUTTON`, `SCREEN_TURN_OFF_BUTTON`, `QUIET_WIDGET_LABEL`,
    `QUIET_ON_TEMPLATE`, `QUIET_OFF_TEXT`, `QUIET_TURN_ON_BUTTON`,
    `QUIET_TURN_OFF_BUTTON`, `REFRESH_WIDGET_LABEL`, `REFRESH_BUTTON_TEXT`,
    `REFRESH_HELP_TEXT`, `REFRESH_COOLDOWN_TEMPLATE`, `QUICK_DISPLAY_ROUTE`,
    `QUICK_QUIET_HOURS_ROUTE`, `POLL_ROUTE`, and Home's own copies of
    `QUICK_STATE_FIELD`/`QUICK_STATE_ON`/`QUICK_STATE_OFF` (20-01 moved those to
    `companion/layout.py`, which is where 20-07 reads them from — a page module
    may never import another page module). Leave a short comment naming D-16 and
    pointing at `config_page.py` (switches) and Device's Manual refresh section,
    so the relocation is greppable. The `/quick/*` routes, their flash keys and
    their handlers are NOT touched by this plan.

    Replace `_status_tiles_html()` with a `_status_card_html(ctx)` that keeps the
    existing state resolution verbatim (`device_state`, `pipeline_state`,
    `battery_state` off `ctx["health_state"]`, and the three
    `FRAME_STATE_TEXT`/`DATA_STATE_TEXT`/`BATTERY_STATE_TEXT` dicts unchanged in
    English) and changes only the output builder: three `layout.status_row()`
    calls — Frame, Battery, Flight data — inside
    `<div class="status-card__rows">`, wrapped in a
    `<div class="page-section status-card" aria-labelledby="home-status-heading">`
    with a visually-hidden `<h2 id="home-status-heading">`.

    The Frame row's `detail` is `ctx["health_state"]["device_detail_html"]` (the
    verdict-free fragment 20-03 added), never `device_html` — that embed is the
    confirmed cause of the duplicated verdict, and this is the only correct fix.
    The Battery row's detail keeps the existing "≈ NN% · NNNN mV" text; the
    Flight-data row's detail keeps its existing freshness clause.

    The card's headline sits above the rows: take the existing next-wake block's
    computation (`wake.next_wake_at_iso()` + `layout.local_clock_text()`,
    already in scope) and add one `is_past` branch comparing the parsed next-wake
    against `layout.parse_iso(now)`. Not past renders
    `NEXT_UPDATE_TEMPLATE = "Next update ≈ %s"`; past renders
    `EXPECTED_SINCE_TEMPLATE = "Expected since %s"` with the
    `status-card__headline--warn` modifier (or, if 20-04's contrast gate rejected
    warn-coloured body text, the non-colour treatment that plan shipped — read
    `style.css`'s comment there before choosing). When the frame has never
    checked in, render no headline at all, matching today's behaviour. Retire
    `NEXT_WAKE_LABEL`/`NEXT_WAKE_VALUE_TEMPLATE` and any tile that carried them.

    Keep the "See details on Health" link under the card, gated on
    `not ctx.get("simple_mode")` (D-30). Wrap every user-visible string in this
    task's output in `t()` at its interpolation site, leaving the constants'
    English values intact, and keep each `t()` result inside `escape_html()`.

    In `companion/test_view_pages.py`: retarget every existing Home check that
    asserted the quick-actions card, the three `stat_tile()` tiles or the
    "Next wake" label. Add checks: a seeded render contains no
    `quick-action` markup and no `/quick/` action anywhere; it contains exactly
    three `status-row` blocks with the Frame, Battery and Flight-data labels; the
    Frame state sentence appears exactly once in the whole page body (assert a
    count of 1, this is the bug's pinned regression test); a seeded next-wake in
    the future renders `Next update ≈` and no `--warn` modifier; one in the past
    renders `Expected since` with the warn treatment; a never-checked-in state
    renders neither; and `simple_mode` on removes the Health link while off
    restores it. Re-derive `EXPECTED_CHECK_COUNT` by running the harness and
    appending a NEW last assignment commented `20-06-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quick-action\|_quick_actions_html\|_toggle_form_html\|/quick/" companion/pages/home_page.py` outputs `0`
    - `grep -c "layout.status_row(" companion/pages/home_page.py` outputs `3`
    - `grep -c "device_html" companion/pages/home_page.py` outputs `0` and `grep -c "device_detail_html" companion/pages/home_page.py` outputs `1`
    - `grep -c "stat_tile(" companion/pages/home_page.py` outputs `0`
    - `grep -c "Expected since" companion/pages/home_page.py` outputs at least `1`
    - `grep -c "simple_mode" companion/pages/home_page.py` outputs at least `1`
    - `companion/test_view_pages.py` exits 0 and reports `M/M checks pass` with M equal to its last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Home carries one status card of three rows with a next-update headline that tells the truth about a past time, the frame verdict appears once, and the quick actions are gone.</done>
</task>

<task type="auto">
  <name>Task 2: the hero row and the recent-flights thumbnails</name>
  <files>companion/pages/home_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/home_page.py `_now_showing_html()`, `_recent_flights_html()`, `_recent_flights()`, `_route_text()`, `_direction_text()`, `_gallery_name_to_iso()` and `render()` (all in full)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section B (the hero markup, the caption's flight one-liner, the thumbnail markup and the placeholder rule)
    - companion/pages/airlines_page.py around line 657 (the exact `illustrations.normalise_airline_key()` call and the illustration URL shape it feeds)
    - companion/app.py: the `/illustration/` route (confirm the exact URL form and what it does for an unknown key) and the `/gallery/` route
    - companion/static/style.css: the `.home-hero`, `.preview-frame*` and `.recent-flight*` rules added by 20-04 (the class names this markup must match exactly)
  </read_first>
  <action>
    Rebuild `render()`'s body as two rows. The first is
    `<div class="home-hero">` containing `_now_showing_html()`'s output —
    restructured into a `<figure class="preview-frame">` with the image
    (`width`/`height` attributes as today, `decoding="async"`, the existing alt
    text through `t()`) and a `<figcaption class="preview-frame__caption
    text-label">` carrying the existing "Rendered HH:MM" caption plus, when the
    current flight is known, a `<span class="preview-frame__flight">` reading
    callsign in `.mono`, then airline, then the route, then the direction, joined
    by the existing separator. Callsigns, airline names and ICAO codes are data
    and are never translated. The second child of the hero is Task 1's status
    card. Below the hero, the recent flights section stays full width with its
    existing heading and "See all flights" link. The empty states keep their
    current headings and bodies, translated.

    In `_recent_flights_html()`, add the leading thumbnail cell: resolve the
    row's airline through `illustrations.normalise_airline_key()` (import
    `from server.plane import illustrations`, the same import `airlines_page.py`
    uses — this is a pure string function, so D-20's "no new query" holds), and
    when it returns a truthy key render
    `<img class="recent-flight__thumb" loading="lazy" width="40" height="40"
    src="/illustration/{key}.png" alt="{airline} illustration">` with the alt
    text through `t()` and the airline name escaped; when it returns falsy render
    `<span class="recent-flight__thumb recent-flight__thumb--placeholder"></span>`
    with no `<img>` at all, so a missing file can never produce a broken-image
    icon. The callsign stays in `.mono`; the airline, route and direction stay in
    the detail cell; the local time stays last.

    If confirming that an artwork file exists for a resolved key would require a
    filesystem stat per row, do NOT add one — render the `<img>` for any truthy
    key and let the existing `/illustration/` route's own not-found behaviour
    apply, exactly as the Airlines gallery already does. Record that choice in a
    comment naming D-20.

    In `companion/test_view_pages.py`: retarget the existing recent-flights row
    checks to the four-cell shape. Add checks: a row whose airline resolves
    renders exactly one `recent-flight__thumb` `<img>` with `loading="lazy"` and
    a `/illustration/` src; a row with a null or unrecognised airline renders the
    placeholder span and no `<img>`; the hero contains `.preview-frame` before
    `.status-card` in document order (picture-first stacking comes from source
    order); the flight one-liner appears when the current flight is known and is
    absent otherwise; and a French render shows the translated headings and alt
    text while callsigns and airline names stay untranslated. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-06-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "home-hero" companion/pages/home_page.py` outputs `1`
    - `grep -c "preview-frame" companion/pages/home_page.py` outputs at least `3`
    - `grep -c "illustrations.normalise_airline_key" companion/pages/home_page.py` outputs `1`
    - `grep -c 'loading="lazy"' companion/pages/home_page.py` outputs at least `1`
    - `grep -c "recent-flight__thumb--placeholder" companion/pages/home_page.py` outputs `1`
    - `grep -c "conn.execute\|history_db\." companion/pages/home_page.py` is unchanged from before this task (D-20: no new query)
    - `companion/test_view_pages.py` exits 0 with M equal to its last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>The hero row renders picture-then-status in source order with the flight one-liner, and every recent flight shows artwork or the dashed placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Home's French catalogue and a French end-to-end render check</name>
  <files>companion/i18n_fr/home.py, companion/pages/home_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/home_page.py, as rewritten by Tasks 1-2 — every remaining ALL_CAPS user-visible constant and every string-valued dict
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section A (the Home copy table: it gives the exact French for every headline, label, verdict, caption, link and empty state on this page — use it verbatim, do not re-translate)
    - companion/i18n_fr/nav.py and companion/i18n_fr/health.py (the catalogue module format and the Unicode conventions to copy)
    - 20-CONTEXT.md D-05 and D-09
  </read_first>
  <action>
    Create `companion/i18n_fr/home.py` with a `CATALOG` dict holding every key in
    20-UI-SPEC.md §A's table, using that table's French column verbatim:
    "Next update ≈ %s", "Expected since %s", "Frame", "Battery", "Flight data",
    the three frame verdicts, the three data verdicts, the two battery verdicts,
    "No reading yet", "See details on Health", "Rendered %s", "Recent flights",
    "See all flights", "No flights yet.", the empty-state body, and
    "%s illustration". Group them under comment banners matching the page's
    sections. Real U+2019 apostrophes; U+00A0 before `:` `;` `?` `!`.

    Sweep `home_page.py` for any remaining user-visible string not yet wrapped in
    `t()` at its interpolation site — including the page title, the page purpose
    sentence, both empty-state headings and bodies, and every `alt`/`title`
    attribute — and wrap them, keeping every constant's English value and every
    `escape_html()` wrap intact.

    In `companion/test_view_pages.py` add: a full Home render under
    `prefs.set_request_prefs(lang="fr")` contains "Accueil"-era French — at
    minimum the French page title, "Vols récents", "Voir tous les vols",
    "Prochaine mise à jour" (or "Attendue depuis" for a past wake), "Cadre",
    "Batterie" and "Données de vol" — and contains none of the English strings
    whose French differs; the same seeded render under `en` still contains every
    English string the pre-existing checks assert; and a check that every key in
    `companion/i18n_fr/home.py` is also a key of the merged
    `companion.i18n_fr.CATALOG` (proving the auto-merge picked the module up).
    Re-derive `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-06-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `PY=...; "$PY" -c "import companion.i18n_fr.home as m; print(m.CATALOG['Recent flights'], '|', m.CATALOG['Frame'])"` prints `Vols récents | Cadre`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Next update ≈ %s'])"` prints `Prochaine mise à jour ≈ %s`
    - `grep -c "PAGE_TITLE = \"Home\"" companion/pages/home_page.py` outputs `1` (the source constant is still English)
    - `companion/test_view_pages.py` and `companion/test_i18n.py` both exit 0 with M equal to their last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Home renders fully in French under a French request, byte-identically in English otherwise, and its catalogue module merges cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite (`history_db`) → Home render | runway events and device health are poll-loop data; callsigns and airline names are third-party API strings |
| resolved airline key → `/illustration/{key}.png` URL | a derived path component reaches an image route |
| health-state dict → Home markup | `device_detail_html` is pre-built markup, unlike every other value on this page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering (XSS) | callsign, airline, route, alt text and every `t()` result on Home | mitigate | every dynamic value crosses `layout.escape_html()` at interpolation; `status_row()` escapes its own verdict/detail slots; translated strings are not treated as pre-escaped |
| T-20-24 | Tampering (path) | `/illustration/{key}.png` built from an API-supplied airline name | mitigate | the key comes from `illustrations.normalise_airline_key()`, the same resolver the Airlines gallery already trusts, which reduces to a constrained slug and returns "" for anything unusable; a falsy key renders no `<img>` at all |
| T-20-25 | Information Disclosure | `device_detail_html` embedded as markup | accept | it is server-built markup from `health_page`, already escaped at its own construction site; Home passes it through unchanged, exactly as it does today, and no longer carries the verdict paragraph |
| T-20-16 | Information Disclosure | simple mode hiding the Health link | accept | presentation only — `/health` stays reachable and session-gated (D-30) |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed |
</threat_model>

<verification>
- `companion/test_view_pages.py` → `M/M checks pass`, exit 0
- `companion/test_i18n.py` → `M/M checks pass`
- `companion/test_companion_app.py` → exactly the two documented root-sandbox
  FAILs (the `/quick/*` routes still work and still redirect to Display)
- `ruff check .` clean
- `<human-check>` (end of phase, folded into 20-12's sweep): at 1280px the
  picture sits left of the status card and is capped at 60vh; at 390px the
  picture comes first and nothing overflows; in dark mode every row's dot and
  verdict are legible; the headline reads "Expected since" only when the frame is
  actually overdue.
</verification>

<success_criteria>
- No quick-action markup, route or constant remains in `home_page.py`; the `/quick/*` handlers are untouched
- The status card renders three `status_row()` rows and one headline
- The Frame state sentence appears exactly once in a full Home render
- A past next-wake renders "Expected since"; a future one renders "Next update ≈"; an unknown one renders neither
- Every recent flight renders either one lazy 40×40 thumbnail or the dashed placeholder
- Home issues no query it did not issue before
- In simple mode the Health link is absent from the markup
- A French request renders Home entirely in French; an English request is unchanged
- `EXPECTED_CHECK_COUNT` re-derived by running the harness, appended once per task citing this plan
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-06-SUMMARY.md` when done.
</output>
</content>
