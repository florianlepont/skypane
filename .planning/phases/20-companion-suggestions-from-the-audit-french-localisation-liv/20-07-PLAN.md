---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 07
type: execute
wave: 3
depends_on: ["20-01", "20-03", "20-04"]
files_modified:
  - companion/screens.py
  - companion/pages/config_page.py
  - companion/i18n_fr/display.py
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-13, CFG-15, CFG-18]
user_setup: []

must_haves:
  truths:
    - "D-10: Runway renders on Display, not Device — GROUP_RUNWAY moves from the plane frame's advanced_groups to its everyday_groups"
    - "D-11: Calendar and the flight-colour rules render on Display; the calendar-disconnect form and the rule add/delete forms return to Display"
    - "D-12: Display is three headed sections built from layout.section_intro_html() — Look (Theme, Flight colours, Calendar), What it watches (Runway), When it is on (Screen on/off, Quiet hours) — and its intro reads 'Everything about what the frame shows and when.'"
    - "D-13/Pitfall 1: the Screen on/off and Quiet hours cards render as siblings of <form id=settings-form>, their scheduled inputs bound back with form=settings-form, so the instant switches' own <form> is never nested inside another form"
    - "D-19: each of those two cards carries a quick-action switch row at its top, posting to /quick/display and /quick/quiet-hours, above a hairline, with one shared sentence 'Applies the next time the frame wakes up.'"
    - "D-36 (Device half): the 'Edit artwork' link is gone from the Device page"
    - "D-16 (Device half): Device keeps its existing Manual refresh section as the one place to refresh now"
    - "D-05 (Display/Device): every user-visible string in config_page.py renders through t(), with its French entry in companion/i18n_fr/display.py"
  artifacts:
    - path: "companion/screens.py"
      provides: "the moved group membership"
      contains: "everyday_groups"
    - path: "companion/pages/config_page.py"
      provides: "the three supersections, the restructured switch cards and the translated copy"
      contains: "layout.section_intro_html"
    - path: "companion/i18n_fr/display.py"
      provides: "the Display and Device pages' French catalogue"
      contains: "CATALOG"
  key_links:
    - from: "companion/pages/config_page.py"
      to: "companion/layout.py"
      via: "the promoted section-intro helper and the quick-action protocol constants"
      pattern: "layout\\.(section_intro_html|QUICK_STATE_)"
    - from: "companion/pages/config_page.py"
      to: "#settings-form"
      via: "form-associated controls outside the form"
      pattern: "form=\"%s\"|form=\"settings-form\""
---

<objective>
Regroup the settings pages: every everyday setting lands on Display, organised
as Look / What it watches / When it is on, with the two schedule cards carrying
their own instant switch — and resolve the HTML-validity conflict that switch
creates before it ships.

Purpose: CFG-15's whole point is that a household member finds every everyday
setting in one place, and that Device keeps only hardware, data and diagnostics.
The `<form>`-nesting conflict (20-RESEARCH.md Pitfall 1) is the one thing that
would make this page structurally broken while still looking right.
Output: the group move in `companion/screens.py`, a three-section Display
assembly, two restructured group cards with instant switches, a Device page
without the "Edit artwork" link, and `companion/i18n_fr/display.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-RESEARCH.md
@.claude/skills/sketch-findings-skypane/references/settings-page-patterns.md

<interfaces>
The registry to edit — companion/screens.py:44-62:
  SCREEN_TYPES["plane-frame"]["everyday_groups"] = (GROUP_THEME, GROUP_QUIET_HOURS, GROUP_DISPLAY)
  SCREEN_TYPES["plane-frame"]["advanced_groups"] = (GROUP_RUNWAY, GROUP_LED, GROUP_WAKE_INTERVAL, GROUP_CALENDAR)
  EVERYDAY_GROUPS / ADVANCED_GROUPS (module level, grep-confirmed unused by any
  production code or test — update for honesty, they gate nothing)

Reads them already, needs no change — companion/pages/config_page.py:119-133
  scope_groups(scope)

The assembly to restructure — config_page.py:2286 and :2348-2369:
  groups_html = "".join(builders[g]() for g in groups if g in builders)
  return header + '<form id="settings-form" ...>' + hidden_html + groups_html
         + save_button + '</form>' + calendar_disconnect_html + rules_section_html
         + poll_section_html + dirty_bar_html

The cross-DOM submission precedent — config_page.py:2242 (the dirty-bar Save
  button) and :2452-2456 (the screen-type <select>), both already shipping
  form="%s" % SETTINGS_FORM_ID from outside the form.

The two builders to restructure — config_page.py:1451-1513 display_group(),
  config_page.py:1221-1356 quiet_hours_group().

The flags to move — config_page.py:2306-2319: show_rules /
  show_calendar_disconnect (set only in the SCOPE_DEVICE branch today), the
  `_edit_artwork_link_html()` call site (deleted by this plan), and
  `_scope_fields_html(scope, layout.DEVICE_ROUTE)` (becomes DISPLAY_ROUTE).

From earlier waves: layout.section_intro_html(), layout.QUICK_STATE_FIELD /
  QUICK_STATE_ON / QUICK_STATE_OFF, i18n.t(), ctx["simple_mode"], and the
  .theme-status--nested / .quick-action-slot CSS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Runway, Calendar and the rules to Display, and build Display's three supersections</name>
  <files>companion/screens.py, companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    - companion/screens.py (the whole file, 88 lines)
    - companion/pages/config_page.py lines 110-140 (`scope_groups()`, `submitted_scope()`, `submitted_return_route()`) and lines 2270-2380 (`render()` in full — the builders dict, the groups_html join, the scope branches, the flags and the final concatenation)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section C (the exact supersection markup, the heading order, the `--nested` wrapper class on each grouped card and the spacing rule) and section B of the copy table (the three headings, their three intro sentences and Display's own purpose sentence)
    - companion/pages/health_page.py: how it calls `layout.section_intro_html()` after 20-03's promotion (the call shape to copy)
    - companion/test_config_page.py: grep `scope_groups`, `SCOPE_DEVICE`, `SCOPE_DISPLAY`, `show_rules`, `calendar`, `runway` and read every check they name, plus the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    In `companion/screens.py`, move `GROUP_RUNWAY` and `GROUP_CALENDAR` out of
    `SCREEN_TYPES["plane-frame"]["advanced_groups"]` and into
    `["everyday_groups"]`, and set the everyday tuple's order to the order
    Display renders in: theme, calendar, runway, display, quiet hours (the
    per-supersection grouping is `render()`'s concern, but the tuple should not
    contradict it). Update the module-level `EVERYDAY_GROUPS`/`ADVANCED_GROUPS`
    constants to match — grep first to confirm they are still referenced by
    nothing, and say so in a comment. `scope_groups()` itself needs no change.

    In `config_page.py`'s `render()`: move the `show_rules` and
    `show_calendar_disconnect` flags from the `elif scope == SCOPE_DEVICE:`
    branch to the `if scope == SCOPE_DISPLAY:` branch, and change
    `_scope_fields_html(scope, layout.DEVICE_ROUTE)` to
    `layout.DISPLAY_ROUTE` for the calendar-disconnect section and for the rule
    add/delete forms, so every one of those actions returns to Display.

    Branch the Display scope's assembly: instead of one flat
    `"".join(builders[g]() ...)`, build three `layout.section_intro_html()`
    headers interleaved with their group builders, in the locked order —
    "Look" (`display-look`) over Theme, Flight colours and Calendar;
    "What it watches" (`display-watches`) over Runway; "When it is on"
    (`display-on`) over Screen on/off and Quiet hours — with each grouped card's
    wrapper gaining the `--nested` modifier class (`theme-status--nested` for the
    `.theme-status` wrappers, `page-section--nested` for the `.page-section`
    ones) so 20-04's extended selector renders their `<h2>` at the bottom rung.
    Use the three headings and three em-dash intro sentences from the copy
    table verbatim, through `t()`. Leave the Device and all-scope paths on the
    existing flat join, untouched. Display's own purpose sentence becomes
    "Everything about what the frame shows and when."; Device's intro sentence
    and its "Screen: Plane frame" caption are explicitly unchanged.

    The Flight colours section and the Calendar card now render inside the "Look"
    supersection's run of cards rather than after the form — keep each of them in
    whatever DOM position its own `<form>` requirements demand (the rules add
    form and the calendar forms are their own forms and stay siblings of
    `#settings-form`), and place the supersection headers so the visual order is
    the locked one. 20-09 rebuilds those two cards' internals; this task only
    relocates and re-wraps them.

    In `companion/test_config_page.py`: retarget every check asserting Calendar,
    the rules section or Runway render on Device — they now assert those render
    on Display and are absent from Device. Add checks: `scope_groups(SCOPE_DISPLAY)`
    contains runway and calendar and `scope_groups(SCOPE_DEVICE)` contains
    neither; a Display render contains the three `section-intro` headings in the
    locked document order; every grouped card under a supersection carries a
    `--nested` class; the Device render contains no `section-intro`; the
    calendar-disconnect and rule-delete forms carry `return_to` values pointing
    at Display. Re-derive `EXPECTED_CHECK_COUNT` by running the harness and
    appending a NEW last assignment commented `20-07-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `PY=...; "$PY" -c "import companion.screens as s; d=s.SCREEN_TYPES['plane-frame']; print(sorted(d['everyday_groups'])); print(sorted(d['advanced_groups']))"` lists runway and calendar in the first tuple and neither in the second
    - `grep -c "layout.section_intro_html(" companion/pages/config_page.py` outputs `3`
    - `grep -c "DEVICE_ROUTE" companion/pages/config_page.py` is strictly lower than before this task
    - `grep -c "theme-status--nested\|page-section--nested" companion/pages/config_page.py` outputs at least `5`
    - `companion/test_config_page.py` exits 0 and reports `M/M checks pass` with M equal to its last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Display carries all six everyday groups under three headed sections in the locked order; Device carries none of them; every moved form returns to Display.</done>
</task>

<task type="auto">
  <name>Task 2: The instant switches, and the form restructure that makes them valid HTML</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py lines 1221-1356 (`quiet_hours_group()` in full) and lines 1451-1513 (`display_group()` in full) — every input, its name, and the wrapper markup
    - companion/pages/config_page.py line 2242 and lines 2440-2460 (the two shipped `form="%s" % SETTINGS_FORM_ID` precedents) and lines 2348-2369 (`render()`'s final concatenation — where a sibling of the form is emitted)
    - companion/pages/config_page.py: `calendar_disconnect_section()` (config_page.py:1690-1737) — the existing sibling-of-the-form placement to copy
    - companion/pages/home_page.py `_toggle_form_html()` as it was before 20-06 deleted it (read it from git: `git show HEAD:companion/pages/home_page.py | sed -n '229,311p'`) — the exact quick-action markup shape to relocate, including `.quick-action`, `.quick-action__text`, `.quick-action__state` and `.quick-action__form`
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section D (the full card markup with the quick-action slot, and the required structural decision) and .planning/.../20-RESEARCH.md Pitfall 1 (why an in-place insertion is invalid)
    - companion/static/dirty-state.js (how `data-dirty-section` and form-associated controls outside the form are tracked — confirm the restructured inputs still register)
  </read_first>
  <action>
    Restructure exactly two group builders, `display_group()` and
    `quiet_hours_group()`, and nothing else:

    1. Their returned card wrappers move OUT of `groups_html` and render as
       siblings of `<form id="settings-form">`, in the same slot
       `calendar_disconnect_section()` already occupies — while still appearing
       inside the "When it is on" supersection visually, which means `render()`
       emits that supersection's header and these two cards after the form closes,
       in the locked order. Give each builder a signature or return shape that
       makes this explicit (for example returning the card separately from the
       in-form fragment) rather than having `render()` string-slice anything.
    2. Every scheduled control inside those two cards gains
       `form="%s" % SETTINGS_FORM_ID` — the `display_enabled` checkbox, the
       `quiet_hours_enabled` checkbox and both `time` inputs. Reuse the existing
       constant; never retype the literal `"settings-form"`.
    3. Each card gains a `.quick-action-slot` at its top containing the
       relocated quick-action row — the label, the current state, and a real
       `<form method="post" action="/quick/display">` (respectively
       `/quick/quiet-hours"`) carrying
       `<input type="hidden" name="{layout.QUICK_STATE_FIELD}" value="on|off">`
       and a submit button — followed by the shared sentence
       "Applies the next time the frame wakes up." in `.text-label
       .section-caption`, followed by the slot's hairline. Import the three
       quick-action protocol constants from `companion/layout.py` (20-01 moved
       them there); a page module may never import `home_page`.
       The switch's button text and state words come from the copy table through
       `t()`: Screen / On / Off / Switch on / Switch off, and the quiet-hours
       equivalents.
    4. `data-dirty-section` on both cards is unchanged, so the dirty bar keeps
       naming them; the quick-action `<form>` is a separate form and must never
       be seen by the dirty-state script.

    `handle_post()` is NOT touched: its absent-checkbox carry-forward and the
    hidden `scope`/`return_to` fields keep working because only the DOM position
    changed, not the submitted field set (D-13).

    Immediately after this restructuring lands, run the whole
    `companion/test_config_page.py` suite BEFORE writing any new check, and treat
    any newly-failing check as a genuine signal to investigate — 20-RESEARCH.md
    Open Question 1 flags that a DOM-parse-based assertion could notice the move
    even though no check names the constraint. Record what you found in the
    summary.

    Then add checks: a Display render contains exactly two
    `action="/quick/display"` / `action="/quick/quiet-hours"` forms; neither is a
    descendant of `<form id="settings-form">` (assert by locating the form's
    closing tag and checking the offsets, not by eye); the page contains no
    `<form` inside another `<form` anywhere (a general nesting check over the
    whole rendered body — this is the pinned regression test for Pitfall 1);
    all four scheduled inputs carry `form="settings-form"`; the shared
    "Applies the next time the frame wakes up." sentence appears exactly twice;
    and a POST through `handle_post()` with the same field set as before still
    produces the same saved config. Re-derive `EXPECTED_CHECK_COUNT` and append a
    NEW last assignment commented `20-07-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'action="/quick/display"\|action="/quick/quiet-hours"' companion/pages/config_page.py` outputs `2`
    - `grep -c "SETTINGS_FORM_ID" companion/pages/config_page.py` is at least 4 higher than before this task
    - `grep -c 'form="settings-form"' companion/pages/config_page.py` outputs `0` (the constant is used, never the literal)
    - `grep -c "quick-action-slot" companion/pages/config_page.py` outputs `2`
    - `grep -c "home_page" companion/pages/config_page.py` outputs `0`
    - `companion/test_config_page.py` exits 0 with M equal to its last `EXPECTED_CHECK_COUNT`, and the rendered Display page contains no nested `<form>` (pinned by the new check)
  </acceptance_criteria>
  <done>Both schedule cards carry an instant switch above a hairline, their scheduled inputs still submit with the page-wide Save, and the page contains no nested form.</done>
</task>

<task type="auto">
  <name>Task 3: config_page through t(), its French catalogue, and the Device page's "Edit artwork" removal</name>
  <files>companion/pages/config_page.py, companion/i18n_fr/display.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py — every module-level ALL_CAPS user-visible constant (`grep -nE '^[A-Z][A-Z0-9_]+ *= *[("f]?"' companion/pages/config_page.py`, ~105 of them) and every string-valued dict, then each one's interpolation site
    - companion/pages/config_page.py lines 2300-2320 and 2461-2483 (`_edit_artwork_link_html()` and its Device action-slot call site — both go)
    - companion/pages/config_page.py: `poll_trigger_section()` (Device's Manual refresh — confirm it already offers the refresh Home is losing, and change nothing but its copy's `t()` wrapping)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md sections B and I of the copy table (Display's headings and intros; the Airlines button that replaces the Device link, which 20-10 builds)
    - companion/i18n_fr/health.py and companion/i18n_fr/home.py (catalogue module format and Unicode conventions)
    - 20-CONTEXT.md D-05, D-09, D-36
  </read_first>
  <action>
    Delete `_edit_artwork_link_html()` and its call site from the Device page's
    action slot, plus the constants only it used (D-36). Leave a comment naming
    D-36 and pointing at `airlines_page.py`, where 20-10 renders the replacement
    "Change pictures" button. Device's remaining groups — Diagnostic LED, Wake
    interval, Manual refresh, the screen selector seam, and (from 20-11) the new
    Notifications group — are otherwise unchanged; confirm in the summary that
    the Manual refresh section already provides the refresh button Home dropped
    (D-16), and change nothing about it beyond `t()`.

    Wrap every user-visible string in `config_page.py` in `t()` at its
    interpolation site — group headings and captions, field labels and hints,
    button labels, validation-error messages, empty states, the quiet-hours
    preset labels, the poll-cooldown `data-cooldown-template`/`data-submit-pending`
    attribute values, the confirm question on the calendar-disconnect form's
    `data-confirm` attribute, the disconnect confirmation page's own copy, and
    every `alt`/`aria-label`/`title`. Never mutate a constant's value; keep every
    `escape_html()` wrap. Theme display names shown to people are translated;
    theme ids, group ids, route strings, field names, flash keys and CSS classes
    are not.

    Create `companion/i18n_fr/display.py` holding one entry per English key you
    wrapped, grouped by card with comment banners, following D-09's rules and
    taking every string the UI-SPEC copy table specifies verbatim (Display's
    purpose sentence, the three supersection headings and intros, the shared
    instant-switch sentence, and the quick-action state words). For strings the
    copy table does not cover — the Runway, LED, wake-interval and manual-refresh
    copy — write plain French in the same voice ("Piste", "Heures calmes",
    "Écran", "Appareil"), sentence case, U+2019 apostrophes, U+00A0 before
    `:` `;` `?` `!`.

    In `companion/test_config_page.py`: retarget any check asserting the Device
    page's "Edit artwork" link to assert its absence. Add: a French Display
    render contains "Affichage"-era French, at minimum the three supersection
    headings, the purpose sentence and the instant-switch sentence, and none of
    their English counterparts; an English render still contains every English
    string the pre-existing checks assert; the Device render contains no
    `edit-artwork` markup and no `?edit=1` link; and every key of
    `companion/i18n_fr/display.py` is a key of the merged CATALOG. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-07-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_config_page.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_edit_artwork_link_html\|edit-artwork" companion/pages/config_page.py` outputs `0`
    - `grep -c "?edit=1" companion/pages/config_page.py` outputs `0`
    - `PY=...; "$PY" -c "import companion.i18n_fr.display as m; print(m.CATALOG['Look'], '|', m.CATALOG['When it is on'])"` prints `Aspect | Quand il est allumé`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Applies the next time the frame wakes up.'])"` prints the French sentence
    - `grep -c "i18n.t(\|[^a-z_]t(" companion/pages/config_page.py` is at least the constant count noted in read_first
    - both harnesses exit 0 with M equal to their last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Display and Device render entirely in French under a French request, the Device page no longer links to artwork editing, and the settings catalogue module exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /settings | the one batched settings form, whose in-scope field set decides absent-checkbox semantics |
| browser → POST /quick/display, /quick/quiet-hours | two instant, idempotent state changes now posted from Display |
| device config → Display render | stored values reach the page as pre-checked controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-26 | Tampering (silent state loss) | the scope/in-scope absent-checkbox semantics now that four groups share `scope=display` | mitigate | `handle_post()` is untouched and the scheduled inputs still submit with the full form via `form="settings-form"`; a POST with the same field set must save the same config, pinned by a round-trip check. The related connect-form hazard is 20-09's T-20-11 |
| T-20-27 | Tampering (invalid DOM) | the instant switch's `<form>` inside `#settings-form` | mitigate | both cards render as siblings of the form; pinned by a whole-body "no `<form>` inside a `<form>`" check |
| T-20-03 | Tampering (XSS) | every `t()` result and every stored value rendered on Display/Device | mitigate | `escape_html()` at every interpolation; translated strings are not treated as pre-escaped |
| T-20-01 | Elevation of Privilege | `/quick/*` posted from a new page | accept | the routes and their `require_session()` gate are unchanged; only the page that renders the form and the redirect target moved |
| T-20-16 | Information Disclosure | Device remaining reachable in simple mode | accept | D-30: simple mode hides the nav entry, not the route |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed |
</threat_model>

<verification>
- `companion/test_config_page.py` → `M/M checks pass`, exit 0
- `companion/test_i18n.py` → `M/M checks pass`
- `companion/test_companion_app.py` → exactly the two documented root-sandbox
  FAILs (the `/quick/*` and `/settings` routes are unchanged)
- `companion/test_view_pages.py`, `companion/test_status_pages.py` → unchanged results
- `ruff check .` clean
- `<human-check>` (end of phase, folded into 20-12's sweep): on Display, the
  three section headings read as one rung above their cards; switching the screen
  off from Display flashes and persists; saving the page afterwards does not undo
  the switch; the quiet-hours times still save.
</verification>

<success_criteria>
- `scope_groups(SCOPE_DISPLAY)` returns all six everyday groups and `scope_groups(SCOPE_DEVICE)` returns none of them
- Display renders exactly three `section-intro` headers, in the locked order, with every grouped card carrying a `--nested` wrapper class
- The rendered Display page contains no nested `<form>`, and all four scheduled inputs carry the `form` attribute via `SETTINGS_FORM_ID`
- Both instant switches post to their existing `/quick/*` routes and share one "applies on the next wake" sentence
- The Device page contains no "Edit artwork" link and keeps Manual refresh
- Display and Device render in French with no English sentence left
- `EXPECTED_CHECK_COUNT` re-derived by running the harness, appended once per task citing this plan
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-07-SUMMARY.md` when done.
</output>
</content>
