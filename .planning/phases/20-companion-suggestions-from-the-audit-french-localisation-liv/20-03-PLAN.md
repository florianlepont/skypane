---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - companion/layout.py
  - companion/pages/health_page.py
  - companion/i18n_fr/health.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-13, CFG-14, CFG-15]
user_setup: []

must_haves:
  truths:
    - "D-21: layout.status_row(label, verdict, detail, state) renders one dot + label + verdict + detail row, reusing _STATUS_DOT_CLASSES and card_status_class(); an empty label omits the <span> entirely"
    - "D-12 (prerequisite): Health's _section_intro_html() is promoted verbatim to layout.section_intro_html(), so config_page.py can build Display's three supersections without a page module importing another page module"
    - "D-17 (prerequisite): the health-state dict carries a verdict-free device_detail_html fragment, so Home can render the frame verdict once instead of embedding device_html's own verdict paragraph a second time"
    - "D-07: relative_age_text() and local_clock_text() follow the request language — 'just now' reads 'à l'instant', '1d ago' reads 'il y a 1 j', and month abbreviations are localised; times stay 24-hour Europe/Paris in both languages"
    - "D-05 (Health): every user-visible string on the Health page renders through t(), with its French entry in companion/i18n_fr/health.py"
  artifacts:
    - path: "companion/layout.py"
      provides: "status_row(), section_intro_html(), language-aware date and age text"
      contains: "def status_row("
    - path: "companion/pages/health_page.py"
      provides: "the detail-only device fragment and fully translated Health copy"
      contains: "def _device_timestamp_only"
    - path: "companion/i18n_fr/health.py"
      provides: "the Health page's French catalogue"
      contains: "CATALOG"
  key_links:
    - from: "companion/pages/health_page.py"
      to: "companion/layout.py"
      via: "the promoted section-intro helper"
      pattern: "layout\\.section_intro_html"
    - from: "companion/layout.py"
      to: "companion/i18n.py"
      via: "t() on the age and month text"
      pattern: "i18n\\.t\\("
---

<objective>
Add the two shared view primitives the rest of the phase builds on
(`status_row()`, the promoted `section_intro_html()`), make the date and
relative-age text follow the request language (D-07), give Home the detail-only
device fragment that fixes its duplicated verdict, and translate the Health page.

Purpose: 20-06 (Home) and 20-09 (Calendar) both consume `status_row()`; 20-07
(Display's three supersections) consumes `section_intro_html()`. Both must exist
and be tested before those waves start, and neither may be re-derived per page.
Output: two shared helpers in `companion/layout.py`, language-aware dates, a
`_device_timestamp_only()` fragment in `health_page.py`, and
`companion/i18n_fr/health.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
The status-class dict `status_row()` must reuse rather than copy —
  `companion/layout.py:151-156`: _STATUS_DOT_CLASSES / _DEFAULT_STATUS_DOT_CLASS,
  already consumed by status_dot() (:1214), stat_tile() (:1239) and
  card_status_class(base_class, status) (:1299).

The helper being promoted — `companion/pages/health_page.py:1639-1666`:
  _section_intro_html(section_id, heading, description) ->
    '<div class="section-intro"><h2 id="..." class="text-heading">…</h2>
     <p class="text-label section-caption">…</p></div>'

The fragment Home must stop embedding — `companion/pages/health_page.py:1669-1722`:
  _device_section(device_health, now) returns
    '<p class="text-body widget-verdict">{DEVICE_STATE_TEXT[state]}</p>'
    + layout.concise_timestamp_html(ts, now)
  DEVICE_STATE_TEXT (health_page.py:247-251) is byte-identical to
  home_page.FRAME_STATE_TEXT (home_page.py:99-103) — that is the duplication.

The two date helpers — `companion/layout.py:565-578` relative_age_text(age_seconds)
  and `companion/layout.py:651-670` local_clock_text(parsed, now_parsed=None),
  with `_MONTH_ABBR` at layout.py:31-32. Dozens of call sites pass exactly the
  existing positional arguments — do not change either signature's positional
  part.

New this phase (20-01, wave 1): companion/i18n.py `t()`/`t_lang()`,
  companion/prefs.py `current_lang()`/`simple_mode()`, the auto-merging
  companion/i18n_fr/ package.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: layout.status_row(), the promoted layout.section_intro_html(), and health_page's detail-only device fragment</name>
  <files>companion/layout.py, companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/layout.py lines 140-170 (`_STATUS_DOT_CLASSES` and its default) and lines 1214-1330 (`status_dot()`, `stat_tile()`, `card_status_class()` in full — `status_row()` sits beside them and reuses all three mechanisms)
    - companion/pages/health_page.py lines 1630-1730 (`_section_intro_html()` and `_device_section()` in full) and every call site of `_section_intro_html` in that file (grep it first)
    - companion/pages/health_page.py lines 240-260 (`DEVICE_STATE_TEXT`)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section A (the exact `status-row` markup, the empty-label rule, and the "verdict and detail must be two different pieces of information" contract) and section C (why the intro helper must move to the shared layer)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md (the `layout.status_row()` entry — it quotes the function body, including the instruction to route the outer class through `card_status_class("status-row", state)` rather than interpolating an unvalidated state)
    - companion/test_status_pages.py: grep `section-intro`, `device_html`, `stat_tile` and read every check they name, plus the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    In `companion/layout.py`, add `status_row(label, verdict, detail, state)`
    beside `stat_tile()`, returning
    `<div class="status-row status-row--{state}"><span class="dot {dot_class}"></span>[<span class="status-row__label text-label">…</span>]<span class="status-row__verdict">…</span><span class="status-row__detail">…</span></div>`.
    The dot class comes from `_STATUS_DOT_CLASSES.get(state, _DEFAULT_STATUS_DOT_CLASS)`
    and the outer modifier from `card_status_class("status-row", state)` — a third
    and fourth consumer of the one existing whitelist, never a second copy, and
    never a bare `%s` interpolation of an unvalidated `state`. An empty or falsy
    `label` omits the whole `<span>`, not merely its text (20-UI-SPEC.md §A: the
    Calendar status row passes `""` because its own `<h2>` names the subject).
    `verdict` and `detail` are both escaped here. Docstring: name D-21, state
    that `verdict` and `detail` must carry different information (the Home
    duplicated-verdict defect is the reason this primitive exists), and record
    that the caller passes already-translated text — `status_row()` itself calls
    no `t()`.

    Move `health_page._section_intro_html()` into `companion/layout.py` as the
    public `section_intro_html(section_id, heading, description)` — byte-identical
    markup, the same `.section-intro` class, only the module changes. Update every
    `_section_intro_html(` call site in `health_page.py` to
    `layout.section_intro_html(` and delete the private copy so there is exactly
    one definition. Docstring: name 20-UI-SPEC.md §C and the reason —
    `companion/pages/__init__.py` forbids a page module importing another page
    module, and 20-07 needs this helper from `config_page.py`.

    In `health_page.py`, add `_device_timestamp_only(device_health, now)`
    returning only the timestamp half of `_device_section()`'s output (the
    `layout.concise_timestamp_html(...)` line), with no verdict paragraph.
    `_device_section()` keeps its current behaviour unchanged and is refactored to
    call the new helper for its second half, so the two can never drift. Publish
    the fragment on the health-state dict as a new `"device_detail_html"` key,
    beside the existing `"device_html"` key `compute_health_state()` already
    returns (`health_page.py:1408-1450`) — Home reads it off `ctx["health_state"]`,
    because `companion/pages/__init__.py` forbids `home_page.py` from importing
    `health_page.py` directly. Docstring: name D-17 and 20-RESEARCH.md Pitfall 3 —
    Home embeds `device_html` wholesale today and therefore renders
    `FRAME_STATE_TEXT`'s sentence twice; 20-06 consumes the new key instead. Do
    NOT fix the duplication by editing either state-text dict's wording.

    In `companion/test_status_pages.py` add: `status_row("Frame", "Checking in
    normally", "Last check-in 2m ago", "ok")` contains `status-row--ok`,
    `dot--ok`, all three texts and exactly one `status-row__label`;
    `status_row("", …, "warn")` contains no `status-row__label`;
    `status_row(…, state="nonsense")` falls back to the default dot class and
    emits no `status-row--nonsense` class; a hostile `verdict`/`detail`
    containing `<script>` comes back escaped; `layout.section_intro_html()`
    output is byte-identical to the markup Health rendered before the move (pin
    the exact string); `health_page` no longer defines `_section_intro_html`;
    `_device_timestamp_only()` output contains no `widget-verdict` and no member
    of `DEVICE_STATE_TEXT.values()`, while `_device_section()` still contains
    exactly one of them; and `compute_health_state()`'s returned dict carries a
    `device_detail_html` key whose value is that verdict-free fragment. Re-derive `EXPECTED_CHECK_COUNT` by running the harness
    and appending a NEW last assignment commented `20-03-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def status_row(" companion/layout.py` outputs `1`
    - `grep -c "def section_intro_html(" companion/layout.py` outputs `1` and `grep -c "def _section_intro_html" companion/pages/health_page.py` outputs `0`
    - `grep -c "layout.section_intro_html(" companion/pages/health_page.py` equals the number of `_section_intro_html(` call sites counted before this task
    - `grep -c "def _device_timestamp_only" companion/pages/health_page.py` outputs `1`
    - `grep -c "device_detail_html" companion/pages/health_page.py` outputs at least `2`
    - `grep -c "card_status_class(\"status-row\"" companion/layout.py` outputs `1`
    - `companion/test_status_pages.py` reports exactly the one documented root-sandbox FAIL, with M equal to its last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Both shared primitives live in layout.py with a single definition each, and Home has a verdict-free device fragment to consume.</done>
</task>

<task type="auto">
  <name>Task 2: language-aware relative-age and clock text (D-07)</name>
  <files>companion/layout.py, companion/i18n_fr/health.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/layout.py lines 25-40 (`_MONTH_ABBR`) and lines 560-600 (`relative_age_text()` in full, including every unit branch)
    - companion/layout.py lines 640-700 (`local_clock_text()` in full, and `concise_timestamp_html()` if it is adjacent — note every format it produces)
    - grep -rn "relative_age_text\|local_clock_text" companion/ (every call site — none may need editing)
    - companion/i18n.py and companion/prefs.py (from 20-01)
    - 20-CONTEXT.md D-07 and D-09 (the exact French examples "il y a 1 j", "à l'instant", "10 sept. 11:53", and the typographic rules)
    - companion/test_status_pages.py: grep `relative_age_text`, `local_clock_text`, ` ago` and read every check they name
  </read_first>
  <action>
    Make both helpers language-aware without changing either positional
    signature: each gains a trailing `lang=None` keyword whose `None` resolves to
    `prefs.current_lang()`, so no existing call site changes and a French request
    gets French text automatically.

    `relative_age_text()`: keep the English branch byte-identical (every existing
    pinned check must still find its exact English string). Add a parallel French
    path built from two module tables in the same "fixed dict, membership lookup,
    documented fallback" shape `_STATUS_DOT_CLASSES` uses: a unit-suffix table
    (`s`→`s`, `m`→`min`, `h`→`h`, `d`→`j`) and the connector template, taken from
    the catalogue so the copy lives with the rest of the French — `"just now"` →
    `"à l'instant"` and `"%s ago"` → `"il y a %s"`. The French form of `1d ago`
    must read `il y a 1 j` (a real U+00A0 between the number and the unit, per
    D-09).

    `local_clock_text()`: add `_MONTH_ABBR_FR` as a tuple parallel to
    `_MONTH_ABBR` (`janv., févr., mars, avr., mai, juin, juil., août, sept.,
    oct., nov., déc.`) selected by the resolved language. The time itself stays
    24-hour Europe/Paris in both languages — no locale module, no `%p`, and no
    change to the time-zone handling.

    Add the handful of catalogue entries these two helpers need to
    `companion/i18n_fr/health.py` (this plan owns that module; it is the natural
    home since Health is the heaviest consumer of both helpers). Use real
    Unicode characters in source — U+2019 apostrophes and U+00A0 non-breaking
    spaces — matching this codebase's existing real-Unicode convention.

    In `companion/test_status_pages.py` add, for each helper, a French/English
    pair over the same input: `relative_age_text(30)` gives `just now` under
    `en` and `à l'instant` under `fr`; `relative_age_text(90000)` gives the
    existing English `1d ago` and the French `il y a 1 j`; `local_clock_text()`
    on a September timestamp gives `sept.` under `fr` and `Sep` under `en`, with
    an identical `HH:MM` in both. Add one check that every existing English
    output is unchanged for the default language (call each helper with no
    explicit `lang` after `prefs.set_request_prefs(lang="en")`). Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-03-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_MONTH_ABBR_FR" companion/layout.py` outputs at least `2`
    - `PY=...; "$PY" -c "import companion.layout as l, companion.prefs as p; p.set_request_prefs(lang='fr'); print(l.relative_age_text(30)); print(l.relative_age_text(90000))"` prints `à l'instant` then a string starting `il y a 1`
    - `PY=...; "$PY" -c "import companion.layout as l, companion.prefs as p; p.set_request_prefs(lang='en'); print(l.relative_age_text(30))"` prints `just now`
    - `grep -c "def relative_age_text(age_seconds" companion/layout.py` outputs `1` (the positional part of the signature is unchanged)
    - `companion/test_status_pages.py` reports exactly the one documented root-sandbox FAIL
    - `PY=...; "$PY" companion/test_view_pages.py` and `"$PY" companion/test_config_page.py` still report their unchanged `M/M checks pass` (English output moved nowhere)
  </acceptance_criteria>
  <done>Age and clock text follow the request language, English output is byte-identical to before, and no call site changed.</done>
</task>

<task type="auto">
  <name>Task 3: the Health page through t(), with its French catalogue</name>
  <files>companion/pages/health_page.py, companion/i18n_fr/health.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/pages/health_page.py — every module-level ALL_CAPS string constant and every string-valued dict (`grep -nE '^[A-Z][A-Z0-9_]+ *= *[("f]?"' companion/pages/health_page.py` and `grep -n "_TEXT = {" companion/pages/health_page.py` first, then read each one in place to see how it is rendered)
    - companion/pages/health_page.py: every `escape_html(` call site that interpolates one of those constants (this is where `t()` wraps, not at the constant)
    - 20-CONTEXT.md D-05 and D-09 (what is and is not translated: identifiers, route names, CSS classes, flash keys, ICAO codes, airline names and theme ids stay English; theme display names are translated)
    - companion/i18n_fr/nav.py (from 20-01 — the catalogue module format, the comment-banner-per-group convention and the Unicode rules to copy)
    - companion/test_status_pages.py: the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    Wrap every user-visible string on the Health page in `t()` at its render
    site — headings, captions, tile captions, the three verdict dicts, empty
    states, button labels, the freshness button's `data-pause-text`/
    `data-resume-text` attribute values, every `alt`/`aria-label`/`title`
    attribute, and the unresolved-prefix and resolution-statistics copy. Never
    mutate a constant's value: the constants keep their English text (D-01) so
    every existing pinned check keeps finding its literal, and `t()` is applied
    where the constant is interpolated. Every `t()` result stays wrapped in
    `escape_html()` — a translated string is not pre-escaped (T-20-03).

    Do not translate: route strings, CSS class names, flash keys, ICAO/IATA
    codes, airline names, theme ids, the `mono` data values, or anything the
    scanner in 20-12 will classify as an identifier.

    Add every English string you wrapped as a key in `companion/i18n_fr/health.py`
    with its French value, grouped under comment banners matching the page's own
    sections, following D-09's copy rules: sentence case, U+2019 apostrophes,
    U+00A0 before `:` `;` `?` `!`, and plain French over anglicisms ("État",
    "Batterie", "Pile de données"-free wording — prefer the plainest word a
    household member reads without a glossary). Templated strings keep their
    `%s`/`%d` placeholders inside the key exactly as they appear in source.

    In `companion/test_status_pages.py` add: a full `health_page.render(ctx)`
    under `prefs.set_request_prefs(lang="fr")` contains the French page title
    and at least three other French strings and contains none of a short,
    explicitly-listed set of English source strings that have distinct French
    forms; the same render under `en` is byte-identical to the pre-task output
    for a seeded state (pin a few representative substrings rather than the whole
    body); and a check that every key of `companion/i18n_fr/health.py`'s own
    CATALOG is a `str` and maps to a non-empty `str`. The exhaustive
    English-coverage check is D-08's and lands in 20-12 — do not hand-roll a
    partial version of it here. Re-derive `EXPECTED_CHECK_COUNT` and append a NEW
    last assignment commented `20-03-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "i18n.t(\|t(" companion/pages/health_page.py` is at least the number of ALL_CAPS user-visible constants counted in read_first
    - `grep -c "^DEVICE_STATE_TEXT" companion/pages/health_page.py` outputs `1` and its English values are unchanged: `PY=...; "$PY" -c "import sys; sys.path.insert(0,'.'); from companion.pages import health_page as h; print(h.DEVICE_STATE_TEXT['ok'])"` prints `Checking in normally`
    - `PY=...; "$PY" -c "import companion.i18n_fr.health as m; print(len(m.CATALOG) > 20)"` prints `True`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print('Checking in normally' in c.CATALOG)"` prints `True`
    - `companion/test_status_pages.py` reports exactly the one documented root-sandbox FAIL
    - `grep -c "onclick=\|onchange=" companion/pages/health_page.py` outputs `0`
  </acceptance_criteria>
  <done>Health renders entirely in French under a French request and byte-identically in English otherwise; its catalogue module is populated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite (`history_db`) → Health render | device_health and pipeline rows are poll-loop data; a hostile value must escape, never execute |
| catalogue → rendered HTML | translated text is data like any other and must cross `escape_html()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering (XSS) | `status_row()`'s verdict/detail slots and every `t()` call site on Health | mitigate | `status_row()` escapes both slots itself; every `t()` result stays inside an existing `escape_html()` wrap; pinned by a hostile-input check on `status_row()` |
| T-20-18 | Tampering | `status_row(state=...)` outer CSS class | mitigate | the class comes from `card_status_class()`'s whitelist, so an unrecognised state can never reach the class attribute; pinned by a `state="nonsense"` check |
| T-20-19 | Denial of Service | `relative_age_text()`/`local_clock_text()` on a malformed timestamp | mitigate | both keep their existing degrade-not-raise behaviour unchanged; the French path adds table lookups only, no parsing |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed |
</threat_model>

<verification>
- `companion/test_status_pages.py` → exactly the one documented root-sandbox FAIL
- `companion/test_view_pages.py`, `companion/test_config_page.py`,
  `companion/test_companion_app.py`, `companion/test_i18n.py` → unchanged results
  (this plan must not move any other page's English output)
- `ruff check .` clean
- `<human-check>` (end of phase, folded into 20-12's sweep): Health in French
  reads naturally and no English sentence is left on the page.
</verification>

<success_criteria>
- `status_row()` and `section_intro_html()` each have exactly one definition, in `companion/layout.py`
- `health_page._section_intro_html` no longer exists and every call site uses the shared helper
- `_device_timestamp_only()` emits no verdict text
- A French request renders `à l'instant`, `il y a 1 j` and `sept.`; an English request is byte-identical to before
- Health renders in French with no English sentence left, and `companion/i18n_fr/health.py` holds every key
- `EXPECTED_CHECK_COUNT` re-derived by running the harness, appended once per task citing this plan
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-03-SUMMARY.md` when done.
</output>
</content>
