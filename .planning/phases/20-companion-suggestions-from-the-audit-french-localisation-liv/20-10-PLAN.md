---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 10
type: execute
wave: 4
depends_on: ["20-01", "20-04", "20-07"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/pages/history_page.py
  - companion/i18n_fr/airlines.py
  - companion/i18n_fr/flights.py
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-13, CFG-18]
user_setup: []

must_haves:
  truths:
    - "D-36: Airlines carries a 'Change pictures' secondary button at the top of the gallery linking to ?edit=1, which reads 'Done' and links back without the query once edit mode is on, plus one sentence explaining what edit mode allows"
    - "D-36: the ?edit=1 gating from phase 19 is unchanged — only the entry point is new, and the Device page's old 'Edit artwork' link is already gone"
    - "D-30: in simple mode the button, its sentence and the ?edit=1 lightbox forms are not emitted; typing ?edit=1 by hand still works, because simple mode is presentation, not access control"
    - "D-05 (Airlines): every user-visible string on the Airlines page renders through t(), with its French entry in companion/i18n_fr/airlines.py"
    - "D-05 (Flights): every user-visible string on the Flights page renders through t(), with its French entry in companion/i18n_fr/flights.py"
  artifacts:
    - path: "companion/pages/airlines_page.py"
      provides: "the Change pictures / Done toggle and the translated gallery"
      contains: "airlines-edit-toggle"
    - path: "companion/i18n_fr/airlines.py"
      provides: "the Airlines page's French catalogue"
      contains: "CATALOG"
    - path: "companion/i18n_fr/flights.py"
      provides: "the Flights page's French catalogue"
      contains: "CATALOG"
  key_links:
    - from: "companion/pages/airlines_page.py"
      to: "/airlines?edit=1"
      via: "the styled-anchor toggle"
      pattern: "airlines-edit-toggle"
    - from: "companion/pages/history_page.py"
      to: "companion/i18n.py"
      via: "t() at every interpolation site"
      pattern: "t\\("
---

<objective>
Make artwork editing discoverable where it belongs — on the Airlines page, as a
button that says what it does — and finish the bilingual sweep across the two
remaining everyday pages, Airlines and Flights.

Purpose: the developer did not understand the Device page's "EDIT ARTWORK —
Opens Airlines with the artwork-editing forms available" link. 20-07 removed it;
this plan replaces it with a control on the page it acts on. These are also the
last two everyday pages still rendering English-only, and D-08's completeness
harness in 20-12 cannot pass until they are done.
Output: the toggle button, its explanatory sentence, its simple-mode gate, and
two catalogue modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
The gating that must not change — companion/pages/airlines_page.py:1217-1219:
  the `?edit=1` lightbox forms already render only when ctx["edit_mode"] is true
  (phase 19 D-22). ctx["edit_mode"] is set by companion/app.py from the query
  string; this plan touches neither.

The markup shape being relocated — companion/pages/config_page.py's deleted
  _edit_artwork_link_html() (read it from git:
  `git show HEAD~1:companion/pages/config_page.py | sed -n '2461,2483p'`, or from
  the phase-19 plan if 20-07 has already landed) — the
  `.page-header__screen` / `.text-label.section-caption` slot shape.

New this phase: ctx["simple_mode"] (20-01), i18n.t() (20-01), the
  `.airlines-edit-toggle` rule (20-04), the copy table's §I strings.

The page's own entry point — companion/pages/airlines_page.py:1981:
  layout.page_header("Airlines", purpose=GALLERY_PURPOSE_TEXT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The "Change pictures" / "Done" toggle on Airlines (D-36)</name>
  <files>companion/pages/airlines_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/airlines_page.py `render()` in full (its page_header call, the gallery section's opening markup, and where `ctx["edit_mode"]` is consulted)
    - companion/pages/airlines_page.py lines 1200-1240 (the `?edit=1`-gated lightbox forms — read them to confirm this task changes none of them)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section K and copy-table section I (the exact markup, the styled-anchor decision, the two label states and the explanatory sentence in both languages)
    - companion/static/style.css: the `.airlines-edit-toggle` rule added by 20-04 (match the class name exactly)
    - companion/test_view_pages.py: grep `airlines_page` and `edit=1`, read every check they name, plus the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    Render, as the first element inside the gallery section directly under the
    page's own heading/purpose block (not in the page header's action slot —
    Airlines has no such slot precedent):
    `<a href="/airlines?edit=1" class="airlines-edit-toggle">Change pictures</a>`
    when `ctx.get("edit_mode")` is falsy, and
    `<a href="/airlines" class="airlines-edit-toggle">Done</a>` when it is true —
    both labels through `t()`. Under it, one sentence in
    `.text-label.section-caption`: "Replace an airline's picture or add one for
    an airline that has none." Reuse the `.page-header__screen` wrapper shape the
    deleted Device link used, so the two visually match where the design contract
    expects them to.

    Gate the whole block — the anchor and the sentence — on
    `not ctx.get("simple_mode")` (D-30). Leave the `?edit=1` lightbox forms'
    existing `ctx["edit_mode"]` gate exactly as it is; in simple mode the forms
    remain reachable by typing the URL, which is the documented and intended
    behaviour (presentation, not access control). Record that in a comment beside
    the gate so nobody later "fixes" it into an access control.

    Do not add a script, a GET form or a query-string builder — this is one
    anchor with two literal hrefs.

    In `companion/test_view_pages.py` add: a default Airlines render contains
    exactly one `airlines-edit-toggle` anchor whose href is `/airlines?edit=1`
    and whose text is "Change pictures", plus the explanatory sentence; a render
    with `edit_mode` true shows "Done" linking to `/airlines` with no query; a
    render with `simple_mode` true contains no `airlines-edit-toggle` and no
    explanatory sentence; a render with `simple_mode` true AND `edit_mode` true
    still renders the lightbox forms (the URL still works); and a French render
    shows "Modifier les images" / "Terminé". Re-derive `EXPECTED_CHECK_COUNT` and
    append a NEW last assignment commented `20-10-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "airlines-edit-toggle" companion/pages/airlines_page.py` outputs at least `2`
    - `grep -c 'href="/airlines?edit=1"' companion/pages/airlines_page.py` outputs `1`
    - `grep -c "simple_mode" companion/pages/airlines_page.py` outputs at least `1`
    - `grep -c "<script\|onclick=" companion/pages/airlines_page.py` outputs `0`
    - `grep -c "edit_mode" companion/pages/airlines_page.py` is at least as high as before this task (the existing gating is untouched)
    - `companion/test_view_pages.py` exits 0 with M equal to its last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Airlines offers one obvious button to enter and leave picture-editing, hidden in simple mode, with the phase-19 gating untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The Airlines page through t(), with its French catalogue</name>
  <files>companion/pages/airlines_page.py, companion/i18n_fr/airlines.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/airlines_page.py — every module-level ALL_CAPS user-visible constant (`grep -nE '^[A-Z][A-Z0-9_]+ *= *[("f]?"' companion/pages/airlines_page.py`, ~56 of them) and each one's interpolation site
    - companion/pages/airlines_page.py — the filter bar, the card chips, the coverage/resolution copy, the upload and replace forms' labels, errors and confirmations, and every `alt`/`aria-label`/`title`
    - 20-CONTEXT.md D-05 and D-09 (airline names, ICAO codes and file names are data and are never translated)
    - companion/i18n_fr/home.py and companion/i18n_fr/display.py (the catalogue module format, comment banners and Unicode conventions)
  </read_first>
  <action>
    Wrap every user-visible string on Airlines in `t()` at its interpolation
    site, keeping each constant's English value and every `escape_html()` wrap:
    the page title and purpose, the gallery and coverage headings, the filter-bar
    labels and counts, the card chips and status words, the empty states, every
    upload/replace form label, hint, button and validation error, the manual
    resolution copy, and every `alt`/`aria-label`/`title` attribute. Airline
    names, ICAO/IATA codes, artwork file names and slugs are data — never
    translated.

    Create `companion/i18n_fr/airlines.py` with one entry per wrapped English
    string, grouped under comment banners matching the page's sections, following
    D-09's copy rules (sentence case, U+2019 apostrophes, U+00A0 before
    `:` `;` `?` `!`, "Compagnies" for the page, plain French over anglicisms).
    Templated strings keep their `%s`/`%d` placeholders inside the key.

    In `companion/test_view_pages.py` add: a French Airlines render contains the
    French page title and at least four other French strings and none of their
    English counterparts; an English render still satisfies every pre-existing
    Airlines check; airline names and ICAO codes are byte-identical in both
    renders (a seeded airline name appears unchanged under `fr`); and every key
    of `companion/i18n_fr/airlines.py` is a key of the merged CATALOG. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-10-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `PY=...; "$PY" -c "import companion.i18n_fr.airlines as m; print(len(m.CATALOG) > 30, m.CATALOG['Change pictures'])"` prints `True Modifier les images`
    - `grep -cE '^[A-Z][A-Z0-9_]+ *= *[("f]?"' companion/pages/airlines_page.py` is unchanged from before this task (no constant was deleted or reworded)
    - `grep -c "i18n.t(\|[^a-z_]t(" companion/pages/airlines_page.py` is at least the constant count noted in read_first
    - both harnesses exit 0 with M equal to their last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Airlines renders entirely in French under a French request, with airline names and codes untouched, and English unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: The Flights page through t(), with its French catalogue</name>
  <files>companion/pages/history_page.py, companion/i18n_fr/flights.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/history_page.py — every module-level ALL_CAPS user-visible constant (~16) and each one's interpolation site, plus the table headers, the filter bar, the empty state, the pagination copy and the copy-button markup
    - companion/pages/history_page.py: the `_copy_button_html()` markup (20-11 will add its `data-copied-text` attribute — this task only translates the strings already rendered there, and must leave the markup shape intact for that plan)
    - companion/static/list-filter.js and freshness.js: any `data-*` text attributes this page renders for a script to read (those values are user-visible and must be translated here, per D-06's narrowed scope — the scripts themselves need no change)
    - 20-CONTEXT.md D-05, D-07, D-09 (relative ages and month abbreviations already follow the language after 20-03; callsigns, ICAO codes, airport codes and airline names are data)
    - companion/i18n_fr/airlines.py from Task 2 (format to match)
  </read_first>
  <action>
    Wrap every user-visible string on the Flights page in `t()` at its
    interpolation site: the page title and purpose, the table's column headers,
    the filter-bar labels and the result count, the direction words, the empty
    state, the pagination controls, the copy button's label and its
    `data-*`-carried feedback text if this page renders one today, and every
    `alt`/`aria-label`/`title`. Keep each constant's English value and every
    `escape_html()` wrap. Callsigns, ICAO24 hexes, airport codes, airline names
    and timestamps are data — never translated; the relative ages and month
    abbreviations already follow the language through `layout` (20-03) and need
    no per-call change here.

    Create `companion/i18n_fr/flights.py` with one entry per wrapped string,
    following the same conventions, using "Vols" for the page and plain French
    for the columns ("Indicatif", "Compagnie", "Heure", "Sens" or equivalents in
    the same voice).

    In `companion/test_view_pages.py` add: a French Flights render contains the
    French page title, every translated column header and the French empty state,
    and none of their English counterparts; an English render still satisfies
    every pre-existing Flights check; a seeded callsign and airport code are
    byte-identical in both renders; and every key of
    `companion/i18n_fr/flights.py` is a key of the merged CATALOG. Re-derive
    `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-10-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_view_pages.py && "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `PY=...; "$PY" -c "import companion.i18n_fr.flights as m; print(len(m.CATALOG) >= 10)"` prints `True`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Flights'])"` prints `Vols`
    - `grep -c "def _copy_button_html" companion/pages/history_page.py` outputs `1` (the markup shape 20-11 extends is intact)
    - both harnesses exit 0 with M equal to their last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Flights renders entirely in French under a French request, with flight data untouched, and English unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string → ctx["edit_mode"] | the only thing that opens the artwork forms, unchanged from phase 19 |
| SQLite + third-party API strings → Airlines and Flights markup | airline names, callsigns and file names are attacker-influenceable data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-16 | Information Disclosure | simple mode hiding the artwork entry point | accept | D-30: the button is omitted, the route is not; a pinned check asserts the forms still render when `?edit=1` is typed in simple mode, so the behaviour is deliberate and documented |
| T-20-03 | Tampering (XSS) | airline names, callsigns, file names and every `t()` result on both pages | mitigate | `escape_html()` at every interpolation, unchanged; translated strings are not treated as pre-escaped |
| T-20-31 | Elevation of Privilege | the artwork upload/replace forms reachable from a new entry point | accept | the forms, their routes and their own validation are untouched by this plan; only a link to them moved pages, and every route stays session-gated |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed |
</threat_model>

<verification>
- `companion/test_view_pages.py` → `M/M checks pass`, exit 0
- `companion/test_i18n.py` → `M/M checks pass`
- `companion/test_companion_app.py` → exactly the two documented root-sandbox
  FAILs (the artwork routes are unchanged)
- `ruff check .` clean
- `<human-check>` (end of phase, folded into 20-12's sweep): the "Change
  pictures" button reads as a button rather than a link, in both themes; pressing
  it opens the editing forms and the button then reads "Done".
</verification>

<success_criteria>
- Airlines renders exactly one edit toggle, with the correct label and href in each state, and none in simple mode
- The `?edit=1` gating is byte-for-byte the phase-19 behaviour, including in simple mode
- Airlines and Flights both render entirely in French under a French request, with data strings untouched
- `companion/i18n_fr/airlines.py` and `companion/i18n_fr/flights.py` merge cleanly into CATALOG
- `EXPECTED_CHECK_COUNT` re-derived by running each touched harness, appended once per task citing this plan
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-10-SUMMARY.md` when done.
</output>
</content>
