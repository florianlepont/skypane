---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - companion/static/style.css
  - companion/contrast_check.py
  - companion/test_contrast_check.py
autonomous: true
requirements: [CFG-14, CFG-15, CFG-16]
user_setup: []

must_haves:
  truths:
    - "D-18: every new component this phase renders has its CSS in one place, using only the existing tokens — one 24px gap between sibling cards, the hairline card border, the 12px uppercase label voice, and status colour only through --color-ok/warn/error with a text verdict beside every dot"
    - "D-21: .status-row's label joins the existing shared label-voice declaration set as its seventh member, with no change to the six existing selectors' declarations"
    - "D-12: .theme-status--nested joins .page-section--nested in the nested-card-title selector, so a group card under a Display supersection renders its <h2> at the 16px sans-semibold bottom rung instead of the 22px serif tier"
    - "D-17: the Home hero is a one-column grid that becomes picture + status card at >=960px by source order, with the picture capped at 60vh on desktop only"
    - "D-14d/D-15b: .theme-chip--compact is a size-only additive modifier on the existing chip, inheriting every selected-state mechanism unchanged"
    - "D-17 (contrast gate): the warn-coloured 'Expected since' headline is proven to meet WCAG AA against the card surface in both themes by companion/contrast_check.py, not by eye"
  artifacts:
    - path: "companion/static/style.css"
      provides: "every new rule this phase's markup depends on"
      contains: ".status-row"
    - path: "companion/contrast_check.py"
      provides: "the warn-text-on-card-surface pair gate"
      contains: "status-warn"
  key_links:
    - from: "companion/static/style.css"
      to: "the design tokens"
      via: "var(--space-*)/var(--color-*) only, no new literal"
      pattern: "var\\(--"
    - from: "companion/test_contrast_check.py"
      to: "companion/contrast_check.py"
      via: "the new warn-on-card pair assertion"
      pattern: "contrast_ratio"
---

<objective>
Ship every stylesheet rule this phase's five markup plans depend on, in one
place, plus the contrast gate 20-UI-SPEC.md requires before the warn-coloured
"Expected since" headline may ship.

Purpose: `companion/static/style.css` is the one file every page plan would
otherwise contend for. Concentrating the phase's CSS here lets Home, Display,
Calendar, Flight colours, the live preview and Airlines run in parallel waves
with no stylesheet conflict, and it keeps the design contract reviewable as a
single diff against the skill.
Output: the new rules from 20-UI-SPEC.md §A–K, the nested-card-title selector
extension, and one new executable contrast assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.claude/skills/sketch-findings-skypane/SKILL.md
@.claude/skills/sketch-findings-skypane/references/control-density.md
@.claude/skills/sketch-findings-skypane/references/accessibility-contrast.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home hero, the status-row primitive, the nested-card-title extension and the instant-switch slot</name>
  <files>companion/static/style.css</files>
  <read_first>
    - companion/static/style.css — its header comment in full (the accent-reservation contract and the "no new token without extending this list" rule), the `:root` token blocks for both themes, the shared label-voice declaration block (the six selectors `.stat-tile__caption`, `.data-table th`, `.data-card__label`, `.filter-bar__count`, `.banner__pill`, `.airline-card__chip`), the `.page-section` / `.page-section--nested` rules, the `.section-intro` rule, `.dot` / `.dot--ok|warn|error`, `.widget-detail`, `.quick-action*`, `.recent-flight*`, `.now-showing*` and `.theme-status` rules
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md sections A, B, C and D (every declaration block in this task is quoted there; the naming-caution note about `.preview-frame` and `history_page` is in §B and must be honoured — keep the class name)
    - .claude/skills/sketch-findings-skypane/references/visual-direction-typography.md (the three-rung heading ladder the `--nested` selector belongs to)
    - companion/test_status_pages.py: grep `data-card__label` and `data-table th` — those two selectors' declaration text is pinned string-equal by that harness (owned by another plan this wave) and must not be edited
  </read_first>
  <action>
    Add to `companion/static/style.css`, using only existing tokens — no new
    custom property, no new colour literal, no new accent consumer:

    §A, the status-row primitive: `.status-row` (flex, baseline-aligned,
    wrapping, `var(--space-sm)` gap, `var(--space-xs) 0` padding),
    `.status-row__label` (`flex: 0 0 auto; min-width: 96px`, relaxed to
    `min-width: 0` below 960px), `.status-row__verdict`
    (`font-weight: var(--weight-semibold)`) and `.status-row__detail` (the
    existing `.widget-detail` colour mix, copied as the declaration, not as a
    new token). Register `.status-row__label` as the seventh selector in the
    shared 12px/semibold/uppercase/0.06em/70%-muted label-voice block, written
    out as its own selector in that same block exactly as the other six are —
    and change none of the six existing selectors' declaration text.

    §B, the Home hero: `.home-hero` (one-column grid, `var(--space-lg)` gap,
    becoming `minmax(0,1fr) minmax(320px,400px)` with `align-items: start` at
    `min-width: 960px`); `.preview-frame` / `.preview-frame__image` (full-width
    block, hairline border, `var(--radius-control)`, white backing, with the
    desktop-only `max-height: 60vh` and auto side margins);
    `.preview-frame__caption` and `.preview-frame__flight`;
    `.status-card__headline` (body size, semibold, `0 0 var(--space-md)` margin)
    with the `--warn` modifier colouring it `var(--color-status-warn)`;
    `.status-card__rows` (column flex, `var(--space-xs)` gap). Keep the class
    name `.preview-frame` — 20-UI-SPEC.md §B confirms the existing
    `history_page` guard is scoped to that page's own output and does not forbid
    it here. Phone stacking comes from source order; add no `order` property.
    `.recent-flight` gains the 40px leading column
    (`40px minmax(0,auto) minmax(0,1fr) auto`, collapsing to three columns below
    960px with `.recent-flight__detail` on row 2), plus `.recent-flight__thumb`
    (40×40, `object-fit: cover`, `var(--radius-control)`) and
    `.recent-flight__thumb--placeholder` reusing the existing dashed
    no-artwork treatment rather than inventing a second one.

    §C, the nested-card-title parity fix: extend the existing
    `.page-section--nested > h2, .battery-trend-section > h2` selector with
    `.theme-status--nested > h2` — one line, additive, so a group card placed
    under a Display supersection renders its heading at the 16px sans-semibold
    bottom rung. Do not convert any `.theme-status` wrapper to `.page-section`;
    that alternative is explicitly rejected in §C for its blast radius. Confirm
    the exact current selector list before editing and extend it rather than
    retyping it.

    §D, the instant-switch slot: `.quick-action-slot` (bottom padding and margin
    of `var(--space-md)`, hairline bottom border) — the divider between a card's
    instant switch and its scheduled controls. `.quick-action` itself is an
    existing component and gains no new rule.

    Record the phase number and the UI-SPEC section in a comment banner above
    each block, matching this file's existing per-phase comment convention.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_contrast_check.py && "$PY" companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.status-row\b\|^\.status-row__" companion/static/style.css` outputs at least `4`
    - `grep -c "\.theme-status--nested > h2" companion/static/style.css` outputs `1`
    - `grep -c "^\.home-hero\|^\.preview-frame\|^\.status-card__headline\|^\.quick-action-slot\|^\.recent-flight__thumb" companion/static/style.css` outputs at least `5`
    - `grep -c "max-height: 60vh" companion/static/style.css` outputs `1` and it sits inside a `@media (min-width: 960px)` block
    - `grep -cE "#[0-9a-fA-F]{3,6}" companion/static/style.css` is unchanged from before this task (no new colour literal)
    - `companion/test_status_pages.py` reports exactly the one documented root-sandbox FAIL (the pinned label-voice declarations still match)
  </acceptance_criteria>
  <done>Home's hero, the status-row primitive, the nested heading parity and the instant-switch divider are all styled from existing tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar's fused card, the flight-colour rows, the compact chip, the live preview and the Airlines toggle</name>
  <files>companion/static/style.css</files>
  <read_first>
    - companion/static/style.css — the `.theme-chip`, `.theme-chip__preview`, `.theme-chip__body`, `.theme-chip__name`, `.theme-chip__swatches`, `.theme-chip__dot`, `.theme-chip__check` rules in full; the `.theme-form` / `.theme-option` / `.theme-option--active` block; the `.rule-*` rules; `.banner__pill`; `.lightbox` (for its 480px max-width); the existing `@supports selector(:has(*))` block and every `:has()` usage, to match this file's progressive-enhancement convention
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md sections E, F, G, H and K (every declaration block in this task is quoted there, including the `:has()`-fused Calendar card and its documented no-`:has()` degradation)
    - .claude/skills/sketch-findings-skypane/references/control-density.md (the selected-state contract the compact chip must inherit without adding to it, and the 44px touch-target register the 28px segment buttons are already exempt from)
  </read_first>
  <action>
    Continue in `companion/static/style.css`, still using only existing tokens:

    §G, the compact chip: `.theme-chip--compact` as a size-only additive
    modifier — `width: 104px`, a 36px preview band, `var(--space-xs)` body
    padding, a 12px name, 2px swatch gap, 8px dots, and the check glyph inset to
    `var(--space-xs)` with a 14px icon. Add no selected-state rule of any kind:
    the base chip's border, wash, check glyph and `:has(input:checked)` layer all
    apply unchanged, which is the whole point of making this size-only. Reuse
    `.theme-chip__name`'s existing no-wrap/ellipsis rule rather than restating it.

    §F, the "Match by" control and the rule rows: style native radios as the
    existing segmented control — `.theme-form input[type="radio"]` visually
    hidden with its adjacent `<label>` carrying `.theme-option`'s exact 28px/13px
    geometry and the same 12%-accent active pill on `:checked`, plus the global
    `:focus-visible` outline moved onto the label so keyboard focus stays
    visible. This is the zero-degraded-state path 20-UI-SPEC.md Structural Note 5
    recommends and this phase adopts, which is why no `rule-form.js` exists.
    Then `.rule-add-form--inline` (wrapping flex, end-aligned, `var(--space-sm)`
    gap, the text input capped at 140px), `.rule-list` (unstyled column flex with
    `var(--space-sm)` gap), `.rule-row` (wrapping flex, `var(--space-sm)` gap,
    `var(--space-sm) var(--space-md)` padding, hairline border,
    `var(--radius-control)`, `var(--color-dominant)` surface), `.rule-row__key`
    (`min-width: 72px`), `.rule-row__theme` (`flex: 1 1 auto`), `.rule-row form`
    (`flex: none; margin: 0`), and `.rule-suggestions` / `.rule-suggestion-chip`.
    `.rule-row__kind` gets no rule of its own — it composes `.banner__pill`
    verbatim, and per the UI-SPEC it must NOT be colour-coded.

    §E, the fused Calendar card: `.page-section:has(+ .calendar-disconnect-form)`
    squaring the card's bottom corners and zeroing its bottom margin, plus
    `.calendar-disconnect-form` as the card's trailing action strip (matching
    surface and hairline, no top border, square top corners, right-aligned
    flex). Place it inside this file's existing `:has()` progressive-enhancement
    convention; the documented degradation without `:has()` is two separate
    cards with the normal gap, which is acceptable and must be left working.

    §H, the live preview: `.theme-live-preview` (`max-width: 480px`, bottom
    margin `var(--space-md)`), `.theme-live-preview__image` (block, full width,
    auto height, hairline border, `var(--radius-control)`) and its centred
    caption.

    §K, the Airlines toggle: `.airlines-edit-toggle` — an anchor carrying the
    base `button` element's own quiet visual treatment (inline-flex, 30px height,
    12px horizontal padding, 6px radius, the existing `color-mix` wash and
    border, no underline, inherited text colour, semibold), plus its `:hover`
    and its sub-960px 36px height, mirroring the base button block's values
    exactly. Note in a comment that an `<a>` cannot share the `button` element
    selector, which is why this one visual treatment is deliberately duplicated.

    No new custom property, no new colour literal, no new accent consumer
    anywhere in this task.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_contrast_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.theme-chip--compact" companion/static/style.css` outputs `1` and `sed -n '/^\.theme-chip--compact/,/^}/p' companion/static/style.css | grep -c "border-color\|background\|check"` shows no selected-state declaration in the base modifier block
    - `grep -c "^\.rule-list\|^\.rule-row\|^\.rule-add-form--inline\|^\.rule-suggestion-chip" companion/static/style.css` outputs at least `4`
    - `grep -c "\.calendar-disconnect-form" companion/static/style.css` outputs at least `2` and `grep -c ":has(+ \.calendar-disconnect-form)" companion/static/style.css` outputs `1`
    - `grep -c "^\.theme-live-preview\|^\.airlines-edit-toggle" companion/static/style.css` outputs at least `2`
    - `grep -c 'input\[type="radio"\]' companion/static/style.css` is greater than before this task (the native-radio segmented control shipped)
    - `grep -cE "#[0-9a-fA-F]{3,6}" companion/static/style.css` is unchanged from before this task
  </acceptance_criteria>
  <done>Every remaining component in the UI spec has its rules, all additive, all from existing tokens, with the native-radio segmented control chosen over a JS-driven one.</done>
</task>

<task type="auto">
  <name>Task 3: the contrast gate for the warn-coloured "Expected since" headline</name>
  <files>companion/contrast_check.py, companion/test_contrast_check.py</files>
  <read_first>
    - companion/contrast_check.py (the whole file — `relative_luminance()`, `contrast_ratio()`, `hue_separation()`, `perceptual_distance()` and the WCAG constants, plus however the module currently sources the token values it checks)
    - companion/test_contrast_check.py (the whole file — how it enumerates pairs, how it reads tokens out of style.css, and its EXPECTED_CHECK_COUNT)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md, the Typography and Colour sections (the required check is named there: the warn-coloured headline is the phase's one genuinely new text-on-surface pair) and the Accessibility Floors section
    - .claude/skills/sketch-findings-skypane/references/accessibility-contrast.md (why this gate is executable rather than prose)
  </read_first>
  <action>
    Add the warn-text-on-card-surface pair to the executable contrast contract:
    `--color-status-warn` as text against `--color-dominant` (the card surface
    `.status-card` sits on), in BOTH themes, asserted at
    `WCAG_AA_NORMAL_TEXT` (4.5) because the headline renders at body size, not
    large-text size. Follow whatever mechanism the module already uses to source
    token values — extend the existing pair table rather than adding a second,
    parallel one.

    If either theme's pair fails the 4.5 threshold, do NOT weaken the threshold
    and do NOT invent a new colour. Instead, record the measured ratio in a
    comment and make `.status-card__headline--warn` carry the warn signal
    without warn-coloured body text: keep the headline in `--color-text` and
    mark the overdue state with the existing warn dot plus the word "Expected"
    in the sentence itself (the text verdict is already required beside every
    dot by the design contract). Update 20-UI-SPEC.md's §B block in
    `companion/static/style.css` accordingly with a comment naming the measured
    ratio — the decision must be traceable to a number, not to taste.

    Add the checks to `companion/test_contrast_check.py` in the file's existing
    style: one per theme for the new pair, plus one asserting the pair is present
    in the module's own table (so a future token change cannot silently drop it).
    Re-derive `EXPECTED_CHECK_COUNT` by running the harness and appending a NEW
    last assignment commented `20-04-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_contrast_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status-warn" companion/contrast_check.py` outputs at least `1`
    - `grep -c "status-warn" companion/test_contrast_check.py` outputs at least `2` (one check per theme)
    - `companion/test_contrast_check.py` exits 0 and reports `M/M checks pass` with M equal to its last `EXPECTED_CHECK_COUNT`
    - `grep -c "WCAG_AA_NORMAL_TEXT = 4.5" companion/contrast_check.py` outputs `1` (the threshold was not weakened)
    - if the gate failed and the fallback treatment shipped, `grep -c "measured" companion/static/style.css` is at least `1` and the recorded ratio appears in that comment
  </acceptance_criteria>
  <done>The overdue headline's colour treatment is decided by a measured contrast ratio, pinned in both themes, with the threshold untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet → browser | `style.css` is served as a static asset under the site CSP; it introduces no script and no external reference |
| design tokens → components | a new component may consume tokens but may not introduce a colour literal or a new accent consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-20 | Information Disclosure | a CSS rule referencing an external URL (font, image, import) | mitigate | this file's zero-external-reference rule is unchanged; every rule added here uses local tokens only, pinned by the unchanged colour-literal count and by the absence of any `url(` addition |
| T-20-21 | Tampering (accessibility regression) | warn-coloured body text on the card surface | mitigate | gated by an executable contrast assertion in both themes at 4.5, with a documented non-colour fallback if it fails |
| T-20-15 | Tampering (CSP) | new interactive components | mitigate | every control added here is a native element styled by CSS; no rule depends on an inline style or an inline script |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; no registry component is pulled in (20-UI-SPEC.md Registry Safety: not applicable) |
</threat_model>

<verification>
- `companion/test_contrast_check.py` → `M/M checks pass`, exit 0
- `companion/test_status_pages.py` → exactly the one documented root-sandbox FAIL
  (the pinned label-voice declarations are untouched)
- `companion/test_view_pages.py`, `companion/test_config_page.py`,
  `companion/test_companion_app.py` → unchanged results
- `<human-check>` (end of phase, folded into 20-12's sweep): at 1280px and
  390px, in light and dark, no horizontal overflow and no component sitting on a
  colour it cannot be read against.
</verification>

<success_criteria>
- Every class named in 20-UI-SPEC.md §A–K exists in `companion/static/style.css`
- `.theme-status--nested > h2` joins the existing nested-card-title selector; no `.theme-status` wrapper was converted to `.page-section`
- `.theme-chip--compact` adds size declarations only and no selected-state rule
- The "Match by" control is native radios styled as the segmented control; no `rule-form.js` is created anywhere in this phase
- The colour-literal count in `style.css` is unchanged and no new custom property was introduced
- The warn-on-card pair is asserted in both themes at 4.5, or the documented non-colour fallback shipped with its measured ratio recorded
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-04-SUMMARY.md` when done.
</output>
</content>
