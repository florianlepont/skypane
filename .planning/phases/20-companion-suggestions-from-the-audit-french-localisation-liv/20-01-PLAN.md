---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/prefs.py
  - companion/i18n.py
  - companion/i18n_fr/__init__.py
  - companion/i18n_fr/common.py
  - companion/i18n_fr/nav.py
  - companion/auth.py
  - companion/app.py
  - companion/layout.py
  - companion/pages/__init__.py
  - companion/test_i18n.py
  - companion/test_companion_app.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-13, CFG-18]
user_setup: []

must_haves:
  truths:
    - "D-01: the companion ships in French and English; English stays the literal value of every source constant and French is a lookup catalogue, so every existing pinned check still finds its English literal"
    - "D-02: the nav footer carries a two-segment FR · EN control, built with the theme picker's own <form method=post> idiom, posting to a session-gated POST /ui-lang that sets a cookie with auth.secure_cookie_flag()'s flags and redirects to the referring tab"
    - "D-03: with no cookie the language comes from Accept-Language (fr* → French, anything else → English) and the resolved language is written into <html lang=...> by page_shell() and login_shell()"
    - "D-04: companion/i18n.py exposes t(text) and t_lang(text, lang); the catalogue is companion/i18n_fr's CATALOG dict keyed by the exact English source string; a missing key returns the English string and never raises"
    - "D-05 (layout/app/auth half): the nav labels, the footer switches, the login page and the 404 page render through t()"
    - "D-09: the French nav labels are Accueil, Affichage, Vols, Compagnies, Avancé, État, Appareil"
    - "D-16 (app.py half): POST /quick/display and POST /quick/quiet-hours redirect to the Display page, not Home"
    - "D-29: a Simple · Full control sits under the language and theme pickers, posting to a session-gated POST /ui-mode that sets the sp_ui_mode cookie"
    - "D-30 (nav half): in simple mode the Advanced nav group and the Health nav dot are not emitted at all; typing an advanced URL still works because simple mode is presentation only"
    - "D-32: no inline <script> and no on* attribute is introduced by any of this markup"
  artifacts:
    - path: "companion/prefs.py"
      provides: "per-request browser preferences (language, simple mode) as contextvars, set once by app.page_context()"
      exports: ["set_request_prefs", "current_lang", "simple_mode", "LANG_CHOICES", "MODE_CHOICES"]
    - path: "companion/i18n.py"
      provides: "t()/t_lang() over the French catalogue"
      exports: ["t", "t_lang"]
    - path: "companion/i18n_fr/__init__.py"
      provides: "CATALOG, merged from every sibling module in the package"
      contains: "CATALOG"
    - path: "companion/test_i18n.py"
      provides: "round-trip, fallback, duplicate-key and catalogue-merge checks"
      contains: "EXPECTED_CHECK_COUNT"
  key_links:
    - from: "companion/app.py"
      to: "companion/prefs.py"
      via: "page_context() resolves the cookie/Accept-Language once per request"
      pattern: "prefs\\.set_request_prefs"
    - from: "companion/i18n.py"
      to: "companion/i18n_fr"
      via: "CATALOG.get(text, text)"
      pattern: "CATALOG\\.get"
    - from: "companion/layout.py"
      to: "/ui-lang"
      via: "the nav footer language form"
      pattern: "action=\"/ui-lang\""
---

<objective>
Land the phase's foundation: the per-request language mechanism (D-01..D-04),
the two new nav-footer switches and their session-gated cookie routes (D-02,
D-29), `<html lang>` (D-03), the localised nav and shell strings (D-05, D-09),
the simple-mode nav suppression (D-30's nav half) and the `/quick/*` redirect
move (D-16's app.py half).

Purpose: every later plan in this phase rewrites a page and must call `t()` from
its first line and add its own French strings in the same task. That is only
possible if `companion/i18n.py`, the catalogue package, the cookie routes and
`page_shell()`'s `lang` are already merged.
Output: `companion/prefs.py`, `companion/i18n.py`, the `companion/i18n_fr/`
catalogue package, `POST /ui-lang`, `POST /ui-mode`, a three-switch nav footer,
and a new `companion/test_i18n.py` harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<!-- .planning/STATE.md is deliberately NOT referenced: it is far too large for
     an executor's context budget. 20-CONTEXT.md carries every decision. -->

<interfaces>
The idiom to copy for both new switches — `companion/layout.py:835-848`:
  def _theme_form_html(resolved_theme)  -> '<form class="theme-form" method="post"
      action="/ui-theme">' + one <button type="submit" name="ui_theme" value="..."
      class="theme-option[ theme-option--active]" aria-pressed="..."> per choice
  UI_THEME_CHOICES = ("auto", "light", "dark")   # layout.py:149

The route template — `companion/app.py:2479-2490` `_handle_theme_post()`:
  form = self.read_form(); submitted = form.get("ui_theme")
  cookie_header = "%s=%s; HttpOnly%s; SameSite=Strict; Path=/; Max-Age=%d" % (
      auth.UI_THEME_COOKIE_NAME, submitted, auth.secure_cookie_flag(),
      THEME_COOKIE_MAX_AGE_S)
  return self.redirect(self._referring_tab(), set_cookie=cookie_header)

Its gated dispatch — `companion/app.py:2523-2526`:
  if path == THEME_ROUTE:
      if not self.require_session(): return None
      return self._handle_theme_post()

Cookie helpers — `companion/auth.py:58` UI_THEME_COOKIE_NAME = "sp_ui_theme";
  `auth.py:228-246` secure_cookie_flag(); auth.parse_cookies(header).

The two shells that hard-code the language —
  `companion/layout.py:1095` page_shell(): '<html lang="en" data-ui-theme="%s">\n'
  `companion/layout.py:969`  login_shell(): the identical literal

The two footer assembly sites that must change together —
  `companion/layout.py:1058-1060` page_shell()'s sidebar_footer_html
  `companion/layout.py:928-930`   _mobile_nav_html()'s footer_html

The quick-action protocol constants app.py reads off home_page today —
  home_page.QUICK_STATE_FIELD / QUICK_STATE_ON / QUICK_STATE_OFF, used by
  `companion/app.py:2450-2477` `_handle_quick_toggle()`, which currently
  redirects to HOME_ROUTE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: companion/prefs.py, companion/i18n.py and the companion/i18n_fr/ catalogue package</name>
  <files>companion/prefs.py, companion/i18n.py, companion/i18n_fr/__init__.py, companion/i18n_fr/common.py, companion/i18n_fr/nav.py, companion/test_i18n.py</files>
  <read_first>
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-PATTERNS.md (the `companion/i18n.py` and `companion/i18n_fr.py` pattern entries — the module-docstring convention, the ContextVar shape, and the never-raises contract are quoted there verbatim)
    - companion/wake.py lines 1-40 and companion/auth.py lines 1-60 (the module-boundary docstring house style every shared, page-independent companion module carries)
    - companion/battery.py (the most recent example of a new shared companion module, created in phase 19)
    - companion/test_contrast_check.py lines 1-80 (the smallest existing self-executing harness — copy its file skeleton: the `check(name, fn)` helper, the `results` list, the `EXPECTED_CHECK_COUNT` assignment and the final `N/M checks pass` print)
    - server/device_config.py lines 416-432 (the RUNWAYS registry — the "one plain dict, no framework" idiom the catalogue copies)
  </read_first>
  <action>
    Create `companion/prefs.py`: a shared, page-independent module beside
    `auth.py`/`battery.py`/`layout.py`/`screens.py`, holding the two per-request
    browser preferences this phase introduces. Give it the house-style module
    docstring naming D-04/D-29, stating it is stdlib-only (`contextvars`) and
    that it must never import anything under `companion/pages/` or under
    `server/`. Contents: `LANG_CHOICES = ("fr", "en")`, `DEFAULT_LANG = "en"`,
    `MODE_CHOICES = ("simple", "full")`, `DEFAULT_MODE = "full"`, two module
    level `contextvars.ContextVar`s, and exactly three public functions —
    `set_request_prefs(lang=None, mode=None)` (membership-tested, anything
    unrecognised degrades to the default, never raises),
    `current_lang()` and `simple_mode()` (True only when the resolved mode is
    `"simple"`). One set path, two read paths, no other mutation path — the same
    discipline `auth.py`'s own module-level mutable state is guarded by.

    Rationale to record in the docstring: `page_shell()`, `sidebar_nav()` and
    `_mobile_nav_html()` are reached from ~40 existing call sites across every
    page module; threading two new keyword arguments through all of them would
    touch every file this phase deliberately splits across waves. `ctx["lang"]`
    and `ctx["simple_mode"]` are still published for page modules (Task 2), per
    `companion/pages/__init__.py`'s documented ctx contract — `prefs` is the
    layout layer's read path for the same single resolution, never a second one.

    Create `companion/i18n.py` with `t(text)` and `t_lang(text, lang)` exactly as
    20-PATTERNS.md quotes them: `t()` returns `i18n_fr.CATALOG.get(text, text)`
    when `prefs.current_lang() == "fr"` and `text` unchanged otherwise;
    `t_lang()` is the ContextVar-free test sibling. Both carry the
    "never raises, never logs — a missing key degrades to the English source
    string" docstring sentence, copied from `server/device_config.py`'s
    normaliser convention. State in the docstring that `t()` returns plain text,
    never markup, so every call site still wraps it in `layout.escape_html()`.

    Create the catalogue as a package, `companion/i18n_fr/`, not a single module
    — this phase's plans run in parallel worktrees per wave and each page's plan
    owns its own catalogue file. `companion/i18n_fr/__init__.py` builds one
    `CATALOG` dict by walking its own package with `pkgutil.iter_modules`,
    importing each sibling module and merging that module's own `CATALOG` dict.
    Auto-discovery is deliberate: no plan ever has to edit a shared registration
    list, so no two plans in one wave can conflict over it. Raise `ValueError`
    naming the duplicated key if two modules define the same English key —
    merging silently would make one translation unreachable. Consumers keep
    reading `companion.i18n_fr.CATALOG`, exactly the name D-04 specifies.

    Seed two modules. `companion/i18n_fr/common.py`: the login page's strings
    (`companion/auth.py`'s four user-visible constants), the 404 page's
    `NOT_FOUND_TITLE`/`NOT_FOUND_PURPOSE_TEXT` from `companion/app.py:557-558`,
    and the "Sign out" button label. `companion/i18n_fr/nav.py`: the nav labels
    fixed by D-09 — "Home"→"Accueil", "Display"→"Affichage", "Flights"→"Vols",
    "Airlines"→"Compagnies", "Advanced"→"Avancé", "Health"→"État",
    "Device"→"Appareil" — plus 20-UI-SPEC.md §F's footer strings: "Language"→
    "Langue", "Theme"→"Thème", "Simple mode"→"Mode simple", "Simple"→"Simple",
    "Full"→"Complet". "FR" and "EN" are identifiers and get no entry. Follow
    D-09's copy rules in every value: sentence case, the typographic apostrophe
    U+2019, and a non-breaking space U+00A0 before `:` `;` `?` `!`.

    Create `companion/test_i18n.py` on the existing harness skeleton (a
    `check(name, fn)` helper, `def _x(): ... return True/False, "reason"` per
    check, never a bare `assert`, an `EXPECTED_CHECK_COUNT` assignment and a
    final `N/M checks pass` line, exiting non-zero on any failure). Checks:
    `t_lang("Home", "fr") == "Accueil"`; `t_lang("Home", "en") == "Home"`;
    `t_lang("a string nobody translated", "fr")` returns that string unchanged;
    `t()` follows `prefs.set_request_prefs(lang="fr")` and back;
    `prefs.set_request_prefs(lang="de")` resolves to `"en"`;
    `prefs.set_request_prefs(mode="nonsense")` leaves `simple_mode()` False;
    `i18n_fr.CATALOG` contains every key defined in `common.py` and `nav.py`;
    every CATALOG value is a `str` and no value is byte-identical to its key
    except where the UI-SPEC copy table says the French is unchanged; and a
    source-level check that `companion/i18n.py` and `companion/prefs.py` contain
    neither `companion.pages` nor `from server`. Set
    `EXPECTED_CHECK_COUNT` to the number the harness actually prints.
    The D-08 completeness and dead-translation checks are NOT this task's — they
    land in 20-12, after every page has been rewritten.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `companion/i18n.py` defines exactly `def t(` and `def t_lang(`: `grep -c "^def t" companion/i18n.py` outputs `2`
    - `grep -v '^#' companion/i18n.py companion/prefs.py | grep -c "companion.pages\|from server"` outputs `0`
    - `PY=...; "$PY" -c "import companion.i18n_fr as c; print(c.CATALOG['Home'], c.CATALOG['Advanced'])"` prints `Accueil Avancé`
    - `PY=...; "$PY" -c "import companion.i18n as i; print(i.t_lang('Flights','fr'), i.t_lang('Flights','en'))"` prints `Vols Flights`
    - `PY=...; "$PY" -c "import companion.prefs as p; p.set_request_prefs(lang='de'); print(p.current_lang())"` prints `en`
    - `companion/test_i18n.py` exits 0 and its last line reports `M/M checks pass` with M equal to its `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>The language mechanism, the auto-merging catalogue package and its own harness exist; nothing else in the app consumes them yet.</done>
</task>

<task type="auto">
  <name>Task 2: the /ui-lang and /ui-mode routes, the request-preference resolution, and the /quick/* redirect move</name>
  <files>companion/auth.py, companion/app.py, companion/pages/__init__.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/app.py lines 2440-2540 (`_handle_quick_toggle()`, `_handle_theme_post()` and `do_POST()`'s gated dispatch chain — the exact three-line `if path == ...: if not self.require_session(): return None` shape to copy twice)
    - companion/app.py lines 1030-1060 (`_resolved_ui_theme()` and `_referring_tab()`)
    - companion/app.py lines 1115-1180 (`page_context()` in full — the place ctx["lang"]/ctx["simple_mode"] are added and the single place prefs.set_request_prefs() is called)
    - companion/app.py lines 1280-1340 and 1360-1380 (`_not_found_page()` and the login-page render, both of which render pre-session and so must resolve the language from Accept-Language)
    - companion/app.py lines 195-215 (the route-constant block where THEME_ROUTE lives) and lines 550-565 (NOT_FOUND_TITLE / NOT_FOUND_PURPOSE_TEXT)
    - companion/auth.py lines 40-70 and 220-250 (UI_THEME_COOKIE_NAME's neighbourhood, parse_cookies() and secure_cookie_flag())
    - companion/pages/__init__.py lines 120-172 (the documented ctx-key contract, including ctx["edit_mode"]'s "presentation only, never consulted by a POST handler" wording — the model for the two new keys)
    - companion/test_companion_app.py: grep for `ui_theme` and read every check it names (the theme-cookie round-trip family is the template), plus the file's EXPECTED_CHECK_COUNT trail and its check() helper
  </read_first>
  <action>
    In `companion/auth.py`, add `UI_LANG_COOKIE_NAME = "sp_ui_lang"` and
    `UI_MODE_COOKIE_NAME = "sp_ui_mode"` directly beside `UI_THEME_COOKIE_NAME`,
    with a comment stating all three share `secure_cookie_flag()` so the Secure
    flag can never drift between them. Wrap `auth.py`'s four user-visible login
    strings in `t()` at their render site (not at the constant — the constants
    keep their English values, per D-01).

    In `companion/app.py`:
    - Add `LANG_ROUTE = "/ui-lang"` and `MODE_ROUTE = "/ui-mode"` beside
      `THEME_ROUTE`, and `LANG_COOKIE_MAX_AGE_S`/`MODE_COOKIE_MAX_AGE_S` reusing
      `THEME_COOKIE_MAX_AGE_S`'s value and comment.
    - Add `_handle_lang_post()` and `_handle_mode_post()` as byte-for-byte
      siblings of `_handle_theme_post()`, reading `form.get("ui_lang")` against
      `prefs.LANG_CHOICES` and `form.get("ui_mode")` against `prefs.MODE_CHOICES`
      respectively, building the cookie header through
      `auth.secure_cookie_flag()` with `HttpOnly; SameSite=Strict; Path=/`, and
      redirecting to `self._referring_tab()`. An unrecognised value sets no
      cookie and still redirects, exactly as the theme route already behaves.
    - Register both in `do_POST()` with the same
      `if not self.require_session(): return None` guard THEME_ROUTE carries.
      Neither route may be reachable without a session (T-20-01).
    - Add `_lang_from_request()`: read `auth.parse_cookies(self.headers.get("Cookie"))`
      first; if `sp_ui_lang` holds a member of `prefs.LANG_CHOICES` use it;
      otherwise parse `self.headers.get("Accept-Language", "")` and return
      `"fr"` when the first supported entry is an `fr`-prefixed tag, else
      `"en"`. Split on `,`, strip `;q=` weights, lowercase, and never
      interpolate any header byte into the page — the return value is always one
      of the two whitelisted ids (T-20-04). Add `_mode_from_request()` the same
      way for `sp_ui_mode`, defaulting to `"full"`.
    - In `page_context()`, call `prefs.set_request_prefs(lang=self._lang_from_request(), mode=self._mode_from_request())`
      once, immediately where `ctx["ui_theme"]` is already computed, and publish
      `"lang": prefs.current_lang()` and `"simple_mode": prefs.simple_mode()` in
      the returned dict. Call `prefs.set_request_prefs(...)` on the pre-session
      paths too — the login render and `_not_found_page()` — so both shells get a
      resolved language before any string is looked up.
    - Wrap `NOT_FOUND_TITLE` and `NOT_FOUND_PURPOSE_TEXT` in `t()` at their
      `_not_found_page()` interpolation site.
    - D-16's app.py half: `_handle_quick_toggle()`'s two `self.redirect(...)`
      calls change from `HOME_ROUTE` to `layout.DISPLAY_ROUTE` (all three
      redirect targets in that method, including the invalid-state early return).
      The flash keys are unchanged. Also move the quick-action protocol constants
      `QUICK_STATE_FIELD`, `QUICK_STATE_ON`, `QUICK_STATE_OFF` into
      `companion/layout.py` (a shared module both `app.py` and, from 20-07,
      `config_page.py` may import — a page module may never import another page
      module) and point `_handle_quick_toggle()` at the layout copies.
      `home_page.py`'s own copies stay untouched in this plan; 20-06 deletes them
      with the rest of Home's quick-action code.

    In `companion/pages/__init__.py`, document `ctx["lang"]` and
    `ctx["simple_mode"]` in the ctx-key contract, copying `ctx["edit_mode"]`'s
    own wording: presentation only, never consulted by a POST handler, and in
    simple mode's case explicitly NOT an access control — every advanced route
    stays session-gated and reachable by URL (D-30).

    In `companion/test_companion_app.py`, add checks modelled on the existing
    theme-cookie family: `POST /ui-lang` with `ui_lang=fr` returns 303, sets
    `sp_ui_lang=fr` with `HttpOnly` and `SameSite=Strict`, and redirects to the
    referring tab; `ui_lang=de` sets no cookie; `POST /ui-lang` with no session
    returns the same unauthenticated response every other gated POST returns;
    the same three for `/ui-mode` with `simple`/`full`/garbage; a GET of any page
    with `Accept-Language: fr-FR,fr;q=0.9` and no cookie renders
    `<html lang="fr"`; with `Accept-Language: en-GB` renders `<html lang="en"`;
    the cookie beats the header; and `POST /quick/display` now redirects to
    `/display`. Retarget — never delete — the existing check that asserts the
    quick toggle redirects to `/`. Re-derive `EXPECTED_CHECK_COUNT` by running
    the harness and appending a NEW last assignment commented
    `20-01-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UI_LANG_COOKIE_NAME\|UI_MODE_COOKIE_NAME" companion/auth.py` outputs at least `2`
    - `grep -c "secure_cookie_flag()" companion/app.py` outputs at least `3` (theme, lang, mode)
    - `grep -A2 -E "path == (LANG_ROUTE|MODE_ROUTE)" companion/app.py | grep -c "require_session"` outputs `2`
    - `grep -c "HOME_ROUTE" companion/app.py` is strictly lower than before this task, and `sed -n '/def _handle_quick_toggle/,/def _handle_theme_post/p' companion/app.py | grep -c "HOME_ROUTE"` outputs `0`
    - `grep -c "QUICK_STATE_FIELD" companion/layout.py` outputs at least `1`
    - `companion/test_companion_app.py` reports exactly the two documented root-sandbox FAILs and no others; its last line's M equals the file's last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Both cookie routes exist, are session-gated, round-trip their cookie, and the language resolves from cookie-then-Accept-Language on every path including login and 404; the quick toggles land on Display.</done>
</task>

<task type="auto">
  <name>Task 3: &lt;html lang&gt;, the three-switch nav footer, localised nav labels and simple-mode nav suppression</name>
  <files>companion/layout.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/layout.py lines 25-70 (`_MONTH_ABBR`, the route constants, `ADVANCED_GROUP_LABEL`, `NAV_GROUPS`)
    - companion/layout.py lines 700-960 (`_nav_links()`, `sidebar_nav()`, `_theme_form_html()`, `_logout_form_html()`, `_mobile_nav_html()` — read `_mobile_nav_html()`'s docstring in full: it states why `theme_form_html` is passed as a parameter so the two footer copies cannot drift)
    - companion/layout.py lines 960-1010 (`login_shell()`, including its hard-coded `<html lang="en"` literal)
    - companion/layout.py lines 1003-1200 (`page_shell()` in full — its signature, its sidebar-footer assembly, its `<html lang="en"` literal and its script-tag tuple)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md section I (the exact footer markup, the resolved order language → theme → simple mode → Sign out, and the three `aria-label`s)
    - companion/prefs.py and companion/i18n.py (created in Task 1)
    - companion/test_status_pages.py: grep for `sidebar_footer`, `theme-form`, `nav`, `lang="en"` and read every check they name, plus the EXPECTED_CHECK_COUNT trail
  </read_first>
  <action>
    In `companion/layout.py`:
    - Add `import companion.i18n as i18n` and `import companion.prefs as prefs`
      at the top (layout imports nothing from `companion.app`, so there is no
      cycle; record that in the import comment).
    - `page_shell()` gains a `lang=None` keyword whose default resolves to
      `prefs.current_lang()`, and its `'<html lang="en" data-ui-theme="%s">'`
      literal becomes `'<html lang="%s" data-ui-theme="%s">'`. Do the same in
      `login_shell()`. A `None` default is deliberate: no existing page-module
      call site changes, and the value can only ever be one of the two ids the
      whitelist in `prefs` allows.
    - Add `_lang_form_html(resolved_lang)` and `_mode_form_html(resolved_mode)`
      as siblings of `_theme_form_html()`, identical in shape: one
      `<button type="submit" name="ui_lang" value="fr|en" class="theme-option[
      theme-option--active]" aria-pressed="...">` per option inside a
      `<form class="theme-form" method="post" action="/ui-lang">`, and the same
      for `ui_mode` with `/ui-mode` and the labels `t("Simple")`/`t("Full")`.
      "FR"/"EN" are identifiers and are NOT passed through `t()`. Each of the
      three footer forms gains an `aria-label` — `t("Language")`, `t("Theme")`,
      `t("Simple mode")` — per 20-UI-SPEC.md §I; `_theme_form_html()` gains its
      one-line `aria-label` in the same edit. Both new builders hard-code their
      own action literal for the same reason `_theme_form_html()`'s comment
      already documents.
    - Both footer assembly sites — `page_shell()`'s `sidebar_footer_html` and
      `_mobile_nav_html()`'s `footer_html` — become
      lang form + theme form + mode form + `_logout_form_html()`, in that order.
      They must change together; `_mobile_nav_html()` takes the two new
      fragments as parameters exactly as it already takes `theme_form_html`.
    - Nav labels: wrap every `NAV_GROUPS` label and every nav link label in
      `t()` at render time in `_nav_links()`/`sidebar_nav()`/`_mobile_nav_html()`
      — never by mutating the constants, which keep their English values.
      `escape_html()` still wraps the result of every `t()` call.
    - Simple mode (D-30's nav half): when `prefs.simple_mode()` is true,
      `_nav_links()`/`sidebar_nav()`/`_mobile_nav_html()` omit the
      `ADVANCED_GROUP_LABEL` group and its two links entirely, and emit no
      Health status dot anywhere in the nav. Omission is server-side, not a CSS
      `display:none` — 20-UI-SPEC.md Structural Note 6 requires this choice to be
      made explicitly, and server-side omission is the one that also lets 20-09's
      disclosures render *different text* in simple mode rather than two copies.
      Record that reasoning in a comment beside the gate, and state there that
      this is presentation only: `/health` and `/device` stay reachable by URL
      and stay session-gated.
    - Add the `QUICK_STATE_FIELD = "state"`, `QUICK_STATE_ON = "on"`,
      `QUICK_STATE_OFF = "off"` constants Task 2 pointed `app.py` at, with a
      comment naming D-16/D-19: the quick-action form protocol now has one home
      that both `app.py` and a page module may import.

    In `companion/test_status_pages.py` add: a check that `page_shell()` renders
    `<html lang="fr"` under `prefs.set_request_prefs(lang="fr")` and
    `<html lang="en"` otherwise; the same for `login_shell()`; a check that a
    rendered shell contains exactly three `class="theme-form"` forms whose
    actions are `/ui-lang`, `/ui-theme`, `/ui-mode` in that document order, each
    with an `aria-label`; a check that the French shell's nav reads
    `Accueil`/`Affichage`/`Vols`/`Compagnies`/`Avancé`/`État`/`Appareil`; a
    check that with `simple_mode` on, the shell contains neither
    `ADVANCED_GROUP_LABEL`'s label nor `/health` nor `/device` in its nav and
    carries no nav status dot; and a check that with simple mode off all of them
    return. Retarget — never delete — any existing check asserting the literal
    `lang="en"` or the footer's two-element composition. Re-derive
    `EXPECTED_CHECK_COUNT` by running the harness and appending a NEW last
    assignment commented `20-01-PLAN.md Task 3`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'action="/ui-lang"\|action="/ui-mode"' companion/layout.py` outputs `2`
    - `grep -c '<html lang="en"' companion/layout.py` outputs `0`
    - `grep -c "def _lang_form_html\|def _mode_form_html" companion/layout.py` outputs `2`
    - `grep -c "prefs.simple_mode()" companion/layout.py` outputs at least `1`
    - `grep -c "<script" companion/layout.py` is unchanged from before this task, and `grep -c "onclick=\|onchange=\|onsubmit=" companion/layout.py` outputs `0`
    - `companion/test_status_pages.py` reports exactly the one documented root-sandbox FAIL; its M equals the file's last `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>Every page renders with the resolved `<html lang>`, a three-switch footer in the order language/theme/simple mode/Sign out, French nav labels under `fr`, and no Advanced group or Health dot in simple mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /ui-lang, /ui-mode | untrusted form bodies reach two new state-changing routes |
| browser → Accept-Language / Cookie headers | untrusted header bytes decide a value interpolated into `<html lang>` |
| server-rendered HTML → browser | every dynamic value, including every `t()` return, crosses `escape_html()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Elevation of Privilege | `POST /ui-lang`, `POST /ui-mode` | mitigate | both dispatched behind `if not self.require_session(): return None`, the exact guard `THEME_ROUTE` carries; pinned by a no-session check per route in `test_companion_app.py` |
| T-20-02 | Tampering | the two new cookies | mitigate | header built through `auth.secure_cookie_flag()` with `HttpOnly; SameSite=Strict; Path=/`; never a second Secure-flag scheme |
| T-20-03 | Tampering (XSS) | `t()` output in nav labels, footer buttons, 404 and login copy | mitigate | `t()` returns plain text, never markup; every call site stays wrapped in `layout.escape_html()`; translated strings are never special-cased as pre-escaped |
| T-20-04 | Tampering (header injection) | `_lang_from_request()` / `_mode_from_request()` → `<html lang="%s">` | mitigate | the parser returns only a member of `prefs.LANG_CHOICES`/`MODE_CHOICES`; no header byte is ever interpolated into the page |
| T-20-16 | Information Disclosure | simple mode read as an access control | accept | simple mode omits markup only; `/health` and `/device` stay session-gated and reachable by URL, stated in `companion/pages/__init__.py` and in a comment beside the nav gate (D-30) |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | this plan installs zero packages; 20-RESEARCH.md's Package Legitimacy Audit records the phase-wide zero-install exemption |
</threat_model>

<verification>
- `companion/test_i18n.py` → `M/M checks pass`, exit 0
- `companion/test_companion_app.py` → exactly the two documented root-sandbox FAILs
- `companion/test_status_pages.py` → exactly the one documented root-sandbox FAIL
- `PY=...; "$PY" companion/test_config_page.py` and `companion/test_view_pages.py` → unchanged `M/M checks pass` (this plan must not move any page's English output)
- `ruff check .` clean
</verification>

<success_criteria>
- `companion/i18n.py` and `companion/prefs.py` import nothing from `companion/pages/` or `server/`
- `companion.i18n_fr.CATALOG` merges every module in the package and raises on a duplicate key
- `POST /ui-lang` and `POST /ui-mode` are session-gated, set their cookie with `secure_cookie_flag()`'s flags, and redirect to the referring tab
- A cookie-free request with `Accept-Language: fr-FR` renders `<html lang="fr"`
- The nav footer renders language, theme, simple mode and Sign out in that order, each segmented form carrying an `aria-label`
- In simple mode the Advanced group, `/health`, `/device` and the nav status dot are absent from the rendered nav markup
- `POST /quick/display` and `POST /quick/quiet-hours` redirect to `/display`
- `EXPECTED_CHECK_COUNT` re-derived by running each touched harness, appended as a new last assignment citing this plan
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-01-SUMMARY.md` when done.
</output>

<source_audit>
## Multi-Source Coverage Audit (phase-wide — recorded once, here, in plan 01)

### GOAL (ROADMAP.md Phase 20)
| Item | Plans | Status |
|---|---|---|
| Bilingual FR/EN, switchable per browser | 20-01, 20-03, 20-06, 20-07, 20-09, 20-10, 20-11, 20-12 | COVERED |
| Home redesigned; quick actions moved to Display/Device | 20-04, 20-06, 20-07 | COVERED |
| Display regrouped with every everyday setting | 20-04, 20-07, 20-09 | COVERED |
| Calendar + flight colours strongly improved | 20-04, 20-09 | COVERED |
| Live theme preview from the last real flight | 20-04, 20-08, 20-11 | COVERED |
| Push notifications for battery-low / frame-silent | 20-02, 20-05, 20-11 | COVERED |
| Simple mode hiding everything advanced | 20-01, 20-06, 20-09, 20-10, 20-12 | COVERED |

### REQ (phase requirement ids)
| ID | Plans | Status |
|---|---|---|
| CFG-13 | 20-01, 20-03, 20-06, 20-07, 20-09, 20-10, 20-11, 20-12 | COVERED |
| CFG-14 | 20-04, 20-06 | COVERED |
| CFG-15 | 20-04, 20-07, 20-09 | COVERED |
| CFG-16 | 20-04, 20-08, 20-11 | COVERED |
| CFG-17 | 20-02, 20-05, 20-11 | COVERED |
| CFG-18 | 20-01, 20-06, 20-09, 20-10, 20-12 | COVERED |

### RESEARCH (20-RESEARCH.md pitfalls, assumptions, open questions)
| Item | Plan | Status |
|---|---|---|
| Pitfall 1: the instant switches cannot nest inside `#settings-form` (A1) | 20-07 | COVERED |
| Pitfall 2: calendar-connect needs its own route (A2) | 20-09 | COVERED |
| Pitfall 3: the duplicated verdict's real cause is `health["device_html"]` (A3) | 20-03 (helper), 20-06 (consumer) | COVERED |
| Pitfall 4: only `copy-button.js` and `dirty-state.js` need JS changes (A4) | 20-11 | COVERED |
| Pitfall 5: D-08's harness must be an AST scan, not a hand-kept list | 20-12 | COVERED |
| Pitfall 6: `notifications.lang` is a save-time snapshot, never a form field (A5/A6) | 20-11 (write), 20-02 (validate), 20-05 (read) | COVERED |
| Pitfall 7: the live-preview cache key must fold in the event id | 20-08 | COVERED |
| A5: the topic URL is write-only, never masked | 20-11 | COVERED |
| A6: reuse `device_staleness_thresholds()`'s WARN threshold, no new constant | 20-05 | COVERED |
| A7 / Open Question 2: Home's checks are owned by `test_view_pages.py` | 20-06 | COVERED |
| Open Question 1: run `test_config_page.py` immediately after the form restructuring | 20-07 | COVERED |
| Open Question 3: `companion/wake.py` becomes a re-export shim | 20-02 | COVERED |
| Package Legitimacy Gate | n/a — zero package installs phase-wide | EXCLUDED (documented exemption) |
| UI-SPEC Structural Note 5: native radios, not `<button>` segments, for "Match by" | 20-09 | COVERED (native radios chosen; `rule-form.js` therefore never created) |
| UI-SPEC Structural Note 6: simple mode hides server-side | 20-01 | COVERED (server-side omission chosen explicitly) |

### CONTEXT (20-CONTEXT.md D-01..D-36)
| Decision | Plan(s) |
|---|---|
| D-01, D-02, D-03, D-04 | 20-01 |
| D-05 | 20-01 (layout/app/auth), 20-03 (health), 20-06 (home), 20-07 (config), 20-09 (calendar/rules), 20-10 (airlines/flights), 20-11 (notifications) |
| D-06 | 20-11 |
| D-07 | 20-03 |
| D-08 | 20-12 |
| D-09 | 20-01 (nav labels) + every catalogue task; audited in 20-12 |
| D-10, D-11, D-12, D-13 | 20-07 |
| D-14a–d | 20-09 |
| D-15a–e | 20-09 |
| D-16 | 20-01 (routes), 20-06 (Home deletion), 20-07 (Device refresh copy) |
| D-17 | 20-06 |
| D-18 | 20-04 (CSS), 20-06 (markup), 20-12 (sweep) |
| D-19 | 20-07 |
| D-20 | 20-06 |
| D-21 | 20-03 (primitive), 20-06 + 20-09 (consumers) |
| D-22, D-23 | 20-08 |
| D-24 | 20-08 (script/route), 20-11 (markup) |
| D-25 | 20-02 |
| D-26 | 20-02 (persistence), 20-11 (UI + test route) |
| D-27 | 20-02 (wake move + notify), 20-05 (poll loop) |
| D-28 | 20-02 (field), 20-11 (save-time snapshot), 20-05 (read) |
| D-29, D-30 (nav half) | 20-01 |
| D-30 (page halves) | 20-06, 20-09, 20-10 |
| D-31 | 20-12 (verified, no code change) |
| D-32 | 20-08, 20-11 (every new script via the six-touch-point contract) |
| D-33 | every plan (EXPECTED_CHECK_COUNT re-derivation) |
| D-34 | 20-12 |
| D-35 | 20-12 |
| D-36 | 20-07 (Device link removed), 20-10 (Airlines button) |

All 36 locked decisions are planned. No Deferred Idea (a second read-only
password, email/SMS notifications, a daily digest, firmware or panel-renderer
changes, a third language, per-screen state directories) appears in any plan.
</source_audit>
</content>
</invoke>
