---
phase: 20-companion-suggestions-from-the-audit-french-localisation-liv
plan: 12
type: execute
wave: 6
depends_on: ["20-01", "20-03", "20-06", "20-07", "20-09", "20-10", "20-11"]
files_modified:
  - companion/test_i18n.py
  - companion/test_companion_app.py
  - .claude/skills/sketch-findings-skypane/SKILL.md
  - .claude/skills/sketch-findings-skypane/references/control-density.md
  - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md
  - .planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md
autonomous: true
requirements: [CFG-13, CFG-18]
user_setup: []

must_haves:
  truths:
    - "D-08: a check renders every page in French against a seeded state and fails, listing the missing keys, if any user-visible constant of the page modules is absent from the French catalogue"
    - "D-08: a second check fails if any catalogue entry is never produced by any scanned module — no dead translations"
    - "D-08: the scan is mechanical (stdlib ast over the page modules, layout.py and auth.py), never a hand-kept list, so it cannot drift as later phases add strings"
    - "D-09: the French copy is audited once, end to end, against its own rules — sentence case, typographic apostrophes, non-breaking spaces before : ; ? !, no anglicism where a plain French word exists"
    - "D-31: simple mode keeps Home, Display's six groups, Flights and Airlines fully usable, and survives navigation across every page"
    - "D-18: the FR/EN sweep at 1280 and 390 px, light and dark, finds no overflow and no CSP violation"
    - "D-33: every harness this phase touched has an EXPECTED_CHECK_COUNT re-derived by running it, and the five checks that fail in a root sandbox are still untouched"
    - "D-34: the design-system skill records the status row, the compact chip, the Display supersections, the rule row, the nav-footer switches and the Home hero"
    - "D-35: S-01, S-03, S-05 and S-06 are marked shipped in 18-AUDIT.md, and the dashboard's findings.json update is handed to the orchestrator because that file lives outside this repo"
  artifacts:
    - path: "companion/test_i18n.py"
      provides: "the completeness and dead-translation harness"
      contains: "ast"
    - path: ".claude/skills/sketch-findings-skypane/SKILL.md"
      provides: "the phase-20 components in the design system of record"
      contains: "status-row"
    - path: ".planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md"
      provides: "the shipped status of the four suggestions"
      contains: "S-01"
  key_links:
    - from: "companion/test_i18n.py"
      to: "companion/pages/*.py, companion/layout.py, companion/auth.py"
      via: "an AST scan of ALL_CAPS string constants"
      pattern: "ast\\.(parse|walk)"
    - from: "companion/test_i18n.py"
      to: "companion/i18n_fr"
      via: "key-set comparison in both directions"
      pattern: "CATALOG"
---

<objective>
Close the phase: the completeness harness that keeps the two languages in step
for every future phase, the end-to-end simple-mode and bilingual sweep, the
design-system update, and the audit record.

Purpose: D-08 is what stops English leaking back into French the first time
someone adds a string in phase 21. It has to land last, after every page has been
rewritten, and it has to be mechanical — a hand-kept list would be stale within a
week.
Output: two new checks in `companion/test_i18n.py`, simple-mode coverage, a
recorded FR/EN headless sweep, an updated design-system skill, and four
suggestions marked shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-CONTEXT.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-UI-SPEC.md
@.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-RESEARCH.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
The scan target set D-05 names: companion/pages/*.py (home, config, health,
  history, airlines), companion/layout.py's nav-label and status-text dicts, and
  companion/auth.py's login-error strings. Every user-visible string in this
  codebase is an ALL_CAPS module constant or a value in an ALL_CAPS dict —
  confirmed across all five page modules.

The catalogue: companion/i18n_fr/ (a package whose __init__ merges every sibling
  module's CATALOG and raises on a duplicate key), read only through
  companion/i18n.py's t()/t_lang().

Headless tooling available in this environment: the Playwright CLI at
  /opt/node22/bin/playwright (Node). The Python playwright package is NOT
  installed. The sweep script is a scratch-directory artifact, never a repo file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08 — the mechanical completeness and dead-translation harness</name>
  <files>companion/test_i18n.py</files>
  <read_first>
    - companion/test_i18n.py (created by 20-01 — its check() helper, its imports and its EXPECTED_CHECK_COUNT)
    - .planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-RESEARCH.md Pitfall 5 (the AST strategy, and why a "search the French page for English words" check is the wrong shape — it false-passes on words identical in both languages and false-fails on identifiers)
    - each of companion/pages/home_page.py, config_page.py, health_page.py, history_page.py, airlines_page.py: the shape of their ALL_CAPS constants and ALL_CAPS string-valued dicts, so the scanner recognises both
    - companion/layout.py's NAV_GROUPS and its status-text dicts; companion/auth.py's login-error strings
    - companion/i18n_fr/__init__.py (the merge and its duplicate-key raise)
  </read_first>
  <action>
    Add to `companion/test_i18n.py` a scanner built on stdlib `ast`: parse each
    module in the D-05 set from source (never by importing and introspecting —
    source is what a future contributor edits), walk its top-level `Assign`
    nodes, and collect (a) every string-literal value assigned to an ALL_CAPS
    name and (b) every string literal appearing as a value inside an ALL_CAPS
    dict literal. Keep `%s`/`%d` placeholders in the collected string exactly as
    written — the catalogue key format is the source string verbatim.

    Exclude, by explicit and commented rule rather than by guesswork: route
    strings and path prefixes (values starting with `/` or ending in `.js`/
    `.png`/`.css`), CSS class names and attribute-name constants, flash keys and
    field names, and anything matching an identifier pattern (no spaces and no
    accented characters, e.g. theme ids, group ids, ICAO codes). Name each
    exclusion rule for what it excludes, and add an explicit opt-out list only as
    a last resort, documenting each entry — the point of the scan is that nobody
    has to remember to register a new string.

    Check 1 (completeness): every scanned literal that is not excluded must be a
    KEY of `companion.i18n_fr.CATALOG`. On failure, the check's reason lists the
    missing keys with the module each came from — a developer must be able to fix
    it without re-running anything. Do NOT implement this as "render the French
    page and assert the English is absent": some English words are legitimately
    identical in French, and some identifiers must stay English.

    Check 2 (no dead translations): every key of `CATALOG` must appear in the
    scanned set, with one documented exception list for the strings that are
    legitimately owned elsewhere — the notification bodies in `server/notify.py`
    (which the poll loop renders, not a page module) and any string built by a
    shared helper rather than a constant. Failure lists the orphaned keys.

    Check 3 (the render-level smoke test D-08 also asks for): render every page —
    Home, Display, Device, Flights, Airlines, Health, login, 404 and the calendar
    disconnect confirmation — in French against a seeded state, and assert each
    render succeeds, contains the page's own French title, and contains no
    Python-format artefact (`%s`, `%d`, a stray `{}`), which is the failure mode a
    mistyped catalogue key actually produces.

    Check 4 (D-09's mechanical half): every CATALOG value uses the typographic
    apostrophe U+2019 rather than `'`, and every value containing `:`/`;`/`?`/`!`
    preceded by a space uses U+00A0 for that space. Failure lists the offending
    keys. The rest of D-09 — voice, sentence case, anglicisms — is Task 2's
    human read, not something to fake mechanically.

    Re-derive `EXPECTED_CHECK_COUNT` by running the harness and appending a NEW
    last assignment commented `20-12-PLAN.md Task 1`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import ast" companion/test_i18n.py` outputs `1`
    - `companion/test_i18n.py` exits 0 and reports `M/M checks pass` with M equal to its last `EXPECTED_CHECK_COUNT`
    - Deleting any single entry from `companion/i18n_fr/home.py` makes the completeness check FAIL and name that key (verify by doing it in a scratch copy, then restoring — state the observed failure text in the summary)
    - Adding a bogus key to a catalogue module makes the dead-translation check FAIL and name it (same scratch verification)
    - `grep -c "in rendered_french_page\|not in body" companion/test_i18n.py` shows no check implementing completeness as an absence-of-English search
  </acceptance_criteria>
  <done>The catalogue is provably complete and provably has no orphans, by a scan that cannot go stale, and both failure modes were observed rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Simple-mode coverage and the FR/EN headless sweep</name>
  <files>companion/test_companion_app.py</files>
  <read_first>
    - companion/test_companion_app.py: the existing full-request checks (how a session is established and how a page is fetched over real HTTP), the cookie helpers, and the EXPECTED_CHECK_COUNT trail
    - 20-CONTEXT.md D-30 and D-31 (the exhaustive list of what simple mode hides and what it must keep)
    - companion/layout.py's nav gate, home_page's Health-link gate, airlines_page's toggle gate and config_page's disclosure gates (the four places simple mode is implemented — confirm each is reachable from a real request)
    - .planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-VERIFICATION.md (the ten browser-only human checks this phase must not regress)
    - .planning/phases/20-.../20-UI-SPEC.md Accessibility Floors (what the sweep is looking for)
  </read_first>
  <action>
    Add end-to-end simple-mode checks to `companion/test_companion_app.py`, all
    over real HTTP with the `sp_ui_mode=simple` cookie set: the nav on every page
    contains no Advanced group, no `/health` link, no `/device` link and no nav
    status dot; Home renders without the Health link; Airlines renders without
    the "Change pictures" button; Display renders both disclosures as their short
    single sentence; `GET /health` and `GET /device` still return 200 for a
    signed-in session (presentation, not access control); Display still renders
    all six everyday groups; Flights and Airlines still render their full
    content; and the three nav-footer switches are present on every page in both
    modes. Then the same set with `sp_ui_mode=full` asserting everything returns.
    Add one check that the mode survives navigation: three sequential requests
    with the cookie all render the simple nav.

    Then run the FR/EN sweep as a verification step, not as a repo artifact:
    start the companion against a seeded state directory, and drive
    `/opt/node22/bin/playwright` with a small script written into this session's
    scratch directory (never into the repository) that visits Home, Display,
    Device, Flights, Airlines and Health at 1280x900 and 390x844, in light and
    dark, in French and English — 24 screenshots — asserting for each page that
    `document.documentElement.scrollWidth <= window.innerWidth` (no horizontal
    overflow) and that no console message matches `Content Security Policy`.
    Record the resulting matrix in the plan summary: page, viewport, theme,
    language, overflow yes/no, CSP violations. If the Playwright CLI cannot drive
    a browser in this environment, say so explicitly in the summary and downgrade
    the sweep to the `<human-check>` below — do not silently skip it and do not
    record it as passed.

    Read the whole French catalogue once, end to end, against D-09's rules —
    sentence case, no anglicism where a plain French word exists, the nav labels
    exactly as D-09 fixes them — and fix what reads wrong. This is the half of
    D-09 that Task 1's mechanical check deliberately does not cover.

    Re-derive `EXPECTED_CHECK_COUNT` and append a NEW last assignment commented
    `20-12-PLAN.md Task 2`.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sp_ui_mode" companion/test_companion_app.py` outputs at least `6`
    - `companion/test_companion_app.py` reports exactly the two documented root-sandbox FAILs, with M equal to its last `EXPECTED_CHECK_COUNT`
    - The summary contains the 24-row sweep matrix, or an explicit statement that the headless browser could not run here plus the downgraded human check
    - No screenshot, script or report from the sweep is committed: `git status --porcelain` shows no new untracked file outside `.planning/`
  </acceptance_criteria>
  <done>Simple mode is proven end to end in both directions, and the bilingual layout sweep is either recorded as a matrix or explicitly downgraded.</done>
</task>

<task type="auto">
  <name>Task 3: The design-system skill (D-34) and the audit record (D-35)</name>
  <files>.claude/skills/sketch-findings-skypane/SKILL.md, .claude/skills/sketch-findings-skypane/references/control-density.md, .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md, .planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md</files>
  <read_first>
    - .claude/skills/sketch-findings-skypane/SKILL.md in full (its "Current as of" line, its Folded-In Work list, the label-voice paragraph listing six selectors, and its SUPERSEDED-in-place convention — that convention is how every change in this task must be written)
    - .claude/skills/sketch-findings-skypane/references/control-density.md (the chip, the segmented control and the touch-target register)
    - .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md (the card/group patterns this phase restructured)
    - companion/static/style.css as it now stands (read the phase-20 blocks 20-04 added — the skill must describe what actually shipped, not what was planned)
    - .planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md lines 60-75 (the suggestions table and its status column)
  </read_first>
  <action>
    Update the design-system skill to describe what this phase shipped, in its own
    voice and with its own SUPERSEDED-in-place convention — never deleting a
    superseded statement, always marking it:
    - `SKILL.md`: move the "Current as of" line to phase 20 and add this phase to
      the Folded-In Work list; add `.status-row__label` as the seventh member of
      the label voice; describe the `status-row` primitive (dot + label + verdict
      + detail, its three states, and the rule that verdict and detail must carry
      different information); note that `.theme-status--nested > h2` joined the
      nested-card-title selector; describe the Home hero (one column, two at
      >=960px, picture first by source order, 60vh desktop cap) and the
      nav-footer's three segmented switches with their `aria-label`s.
    - `references/control-density.md`: the compact theme chip (`--compact`,
      size-only, inheriting every selected-state mechanism), the native-radio
      segmented control used for "Match by", and the confirmation that neither
      needs a new touch-target register entry.
    - `references/settings-page-patterns.md`: Display's three supersections and
      why they reuse Health's `section-intro` rather than a new heading tier; the
      instant-switch slot and the `form="settings-form"` cross-DOM submission that
      makes it valid HTML; the two-form Calendar card fused with `:has()` and its
      documented degradation; and the `.rule-row` list replacing the rules table.
    Every entry names the decision id it came from (D-12, D-14, D-15, D-19, D-21,
    D-02/D-29) so a future reader can trace it.

    In `.planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md`, change
    S-01, S-03, S-05 and S-06's status from `open` to `fixed (phase 20)`, matching
    the exact wording S-02 and S-04 already use, and update the section heading
    that counts which suggestions remain open.

    The audit dashboard's `findings.json` lives outside this repository and cannot
    be edited from here. Record that explicitly in the plan summary as a handoff
    item for the orchestrator, naming the four finding ids and the status to set —
    do not create a placeholder file, and do not mark D-35 complete without that
    line in the summary.
  </action>
  <verify>
    <automated>PY=/home/user/skypane/server/.venv/bin/python; [ -x "$PY" ] || PY=$(command -v python3); "$PY" companion/test_contrast_check.py && grep -c "fixed (phase 20)" .planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fixed (phase 20)" .planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md` outputs `4`
    - `grep -c "open" .planning/phases/18-companion-audit-and-ux-refactor/18-AUDIT.md` is 4 lower than before this task
    - `grep -c "status-row" .claude/skills/sketch-findings-skypane/SKILL.md` outputs at least `2`
    - `grep -c "theme-chip--compact" .claude/skills/sketch-findings-skypane/references/control-density.md` outputs at least `1`
    - `grep -c "section-intro\|form=\"settings-form\"\|rule-row" .claude/skills/sketch-findings-skypane/references/settings-page-patterns.md` outputs at least `3`
    - `grep -c "SUPERSEDED" .claude/skills/sketch-findings-skypane/SKILL.md` is greater than or equal to its previous count (nothing was deleted in place)
    - the plan summary contains the findings.json handoff line naming S-01, S-03, S-05 and S-06
  </acceptance_criteria>
  <done>The design system of record describes this phase's six new patterns, the audit's four suggestions read as shipped, and the out-of-repo dashboard update is handed off in writing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → page modules | the AST scan reads source; it must never execute a page module's source as a side effect of scanning |
| headless browser → the running companion | the sweep drives a real session against a seeded state directory, never against production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-16 | Information Disclosure | simple mode mistaken for an access control | mitigate | pinned by checks asserting `/health` and `/device` still return 200 for a signed-in session in simple mode, so the documented behaviour is the tested behaviour |
| T-20-32 | Information Disclosure | the sweep's screenshots and the seeded state | mitigate | the sweep runs against a scratch state directory and writes its artefacts into the session scratch directory only; nothing is committed, pinned by a clean `git status` criterion |
| T-20-33 | Tampering | the AST scanner importing page modules | mitigate | the scanner parses source with `ast.parse` and never imports the module under scan, so a scan can have no side effect on the app |
| T-20-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; the Playwright CLI already present in the environment is used as-is, and no dependency is added to the repo |
</threat_model>

<verification>
- `companion/test_i18n.py` → `M/M checks pass`, exit 0, including the two D-08 checks
- `companion/test_companion_app.py` → exactly the two documented root-sandbox FAILs
- `PYTHON=/home/user/skypane/server/.venv/bin/python bash scripts/run-all-tests.sh`
  → no NEW failures beyond the five documented root-sandbox checks. This is the
  LAST plan of the phase: the full suite must be green here before
  `/gsd:verify-work`
- `ruff check .` clean
- `<human-check>` (end of phase, browser required): the FR/EN sweep at 1280 and
  390 px in both themes shows no overflow and no CSP violation (automate if the
  Playwright CLI can drive a browser here; otherwise this is the fallback);
  the native `confirm()` dialog still appears before disconnecting a calendar and
  before removing a rule; a screen reader announces the three nav-footer switch
  groups by their `aria-label`s; a real ntfy push arrives on the phone for a
  battery-low transition, a frame-silent transition and the "Send a test" button.
- `<human-check>`: the ten browser-only checks recorded in 19-VERIFICATION.md
  still pass — this phase must not regress them.
</verification>

<success_criteria>
- The completeness check fails, by name, for a deleted catalogue entry, and the dead-translation check fails, by name, for an orphaned one — both observed, not assumed
- The scan is source-based, mechanical, and documents every exclusion rule
- Every page renders in French with no Python-format artefact
- Simple mode is proven to hide exactly D-30's list and keep exactly D-31's, and to survive navigation
- The FR/EN sweep matrix is recorded, or its unavailability is stated and downgraded to a human check
- The design-system skill describes the status row, the compact chip, the native-radio segmented control, the Display supersections, the instant-switch slot, the fused Calendar card, the rule row, the nav-footer switches and the Home hero
- 18-AUDIT.md marks S-01, S-03, S-05 and S-06 as fixed (phase 20), and the out-of-repo findings.json update is handed to the orchestrator in the summary
- The full suite is green apart from the five documented root-sandbox checks
</success_criteria>

<output>
Create `.planning/phases/20-companion-suggestions-from-the-audit-french-localisation-liv/20-12-SUMMARY.md` when done.
</output>
</content>
