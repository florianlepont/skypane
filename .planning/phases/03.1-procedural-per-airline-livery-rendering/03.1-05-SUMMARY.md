---
phase: 03.1-procedural-per-airline-livery-rendering
plan: 05
subsystem: assets
tags: [illustrations, vendor-provenance, attribution, pillow, select_illustration]

# Dependency graph
requires:
  - phase: 03.1-procedural-per-airline-livery-rendering (plan 03)
    provides: "_ILLUSTRATION_TARGETS, target_filenames()/outstanding_filenames()/required_filenames(), the four-tier select_illustration() lookup"
provides:
  - "25 of 26 outstanding D-03 illustration target files, delivered and validated"
  - "Digest-backed provenance for all 25 files in server/assets/icons/illustrations/VENDOR.md"
  - "3 out-of-scope generated files registered under _unresolved/ with explicit non-target rationale"
  - "Live-run proof that Tiers 1/2/3 of select_illustration() actually reach the delivered art"
  - "Rewritten HANDOFF.md covering the full 34-file D-03 target set"
affects: [03.1-06 (if a future plan closes the last outstanding file), future-milestone-illustration-audits]

tech-stack:
  added: []
  patterns:
    - "Provenance-by-digest: every vendored PNG gets a real shasum -a 256 + Pillow-read dimensions row in VENDOR.md, never copied from another record"
    - "Named-partial-delivery discipline: outstanding_filenames() is the single source of truth for what's missing; VENDOR.md names the remainder explicitly rather than going silent"
    - "Non-target holding area (_unresolved/): generated-but-unreachable art is kept on disk with an explicit VENDOR.md explanation, satisfying scripts/check-attribution.sh without misrepresenting it as shipped"

key-files:
  created:
    - server/assets/icons/illustrations/easyjet.png
    - server/assets/icons/illustrations/wizz-air.png
    - server/assets/icons/illustrations/volotea.png
    - server/assets/icons/illustrations/ita-airways.png
    - server/assets/icons/illustrations/air-europa.png
    - server/assets/icons/illustrations/royal-air-maroc.png
    - server/assets/icons/illustrations/lot-polish-airlines.png
    - server/assets/icons/illustrations/air-caraibes.png
    - server/assets/icons/illustrations/french-bee.png
    - server/assets/icons/illustrations/europe-airpost.png
    - server/assets/icons/illustrations/tunisair.png
    - server/assets/icons/illustrations/pegasus-airlines.png
    - server/assets/icons/illustrations/chalair-aviation.png
    - server/assets/icons/illustrations/twin-jet.png
    - server/assets/icons/illustrations/corsairfly.png
    - server/assets/icons/illustrations/ccm-airlines-atr72.png
    - server/assets/icons/illustrations/transavia-france-a320.png
    - server/assets/icons/illustrations/air-caraibes-a330.png
    - server/assets/icons/illustrations/generic-a320.png
    - server/assets/icons/illustrations/generic-b737.png
    - server/assets/icons/illustrations/generic-atr72.png
    - server/assets/icons/illustrations/generic-beechcraft1900d.png
    - server/assets/icons/illustrations/generic-embraer.png
    - server/assets/icons/illustrations/generic-a330.png
    - server/assets/icons/illustrations/generic-a350.png
    - server/assets/icons/illustrations/_unresolved/amelia-international.png
    - server/assets/icons/illustrations/_unresolved/la-compagnie.png
    - server/assets/icons/illustrations/_unresolved/air-caraibes-atr72-unused.png
  modified:
    - server/assets/icons/illustrations/HANDOFF.md
    - server/assets/icons/illustrations/VENDOR.md
    - server/assets/icons/VENDOR.md

key-decisions:
  - "Kept the 3 out-of-scope generated files (amelia-international, la-compagnie, air-caraibes-atr72-unused) on disk under a new _unresolved/ subdirectory rather than deleting them, registering each with an explicit non-target explanation in VENDOR.md so check-attribution.sh passes without misrepresenting them as shipped, selectable art"
  - "royal-air-maroc-embraer.png left genuinely outstanding (not fabricated or substituted) - named by filename in VENDOR.md's Phase 3.1 coverage section, per the plan's must_haves"
  - "Airline files whose adsbdb-resolved name predates a real-world rebrand (europe-airpost.png/ASL Airlines France, corsairfly.png/Corsair International) recorded in VENDOR.md under their resolved (old) name, per the Naming rules 03.1-LIVE-RESOLUTION.md established - never renamed to the current public brand"

requirements-completed: [PLANE-01, PLANE-02]

coverage:
  - id: D1
    description: "HANDOFF.md rewritten to name and prompt every one of the 34 D-03 target files, sourced from illustrations.py's own --targets output"
    requirement: "PLANE-01"
    verification:
      - kind: unit
        ref: "server/.venv/bin/python3 server/plane/illustrations.py --targets | xargs -I NAME grep -q -- NAME server/assets/icons/illustrations/HANDOFF.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "25 of 26 outstanding illustration files generated, delivered, and human-confirmed for nose-left orientation and filename-matching aircraft type"
    requirement: "PLANE-01"
    verification:
      - kind: manual_procedural
        ref: "Task 2 blocking human-action checkpoint - developer confirmed nose-left + type-match by eye per delivered file, resume signal named this a partial batch (25/26)"
        status: pass
    human_judgment: true
    rationale: "Nose orientation and aircraft-type-matches-filename are explicitly not machine-checkable (RESEARCH.md Pitfall 4); closed only by the developer's own visual sign-off, already completed interactively before this agent resumed."
  - id: D3
    description: "Every delivered file passes validate_illustration_file() (format/dimensions/alpha/landscape)"
    requirement: "PLANE-01"
    verification:
      - kind: automated_ui
        ref: "server/.venv/bin/python3 server/plane/illustrations.py --validate (exit 0, 1 outstanding target reported as expected)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every delivered file digest-recorded (sha256 + Pillow dimensions) in VENDOR.md, including 3 out-of-scope files with explicit non-target rationale"
    requirement: "PLANE-02"
    verification:
      - kind: other
        ref: "shasum -a 256 re-run and spot-checked against VENDOR.md for easyjet.png, royal-air-maroc.png, generic-a350.png - exact match; scripts/check-attribution.sh exit 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "The real four-tier select_illustration() code reaches the delivered art across Tiers 1, 2, and 3"
    requirement: "PLANE-02"
    verification:
      - kind: integration
        ref: "server/.venv/bin/python3 -c snippet calling select_illustration() live: Tier1 (CCM Airlines, AT72) -> ccm-airlines-atr72.png; Tier2 (CCM Airlines, A320) -> ccm-airlines.png; Tier3 (Some Unrecognized Airline, A321) -> generic-a320.png"
        status: pass
    human_judgment: false
  - id: D6
    description: "The 1 genuinely outstanding target is named by filename, not silently dropped, and the full automated suite stays green throughout"
    requirement: "PLANE-01"
    verification:
      - kind: unit
        ref: "scripts/run-all-tests.sh (all 9 harnesses, incl. illustrations: 42/42, render: 35/35); server/.venv/bin/python3 -m ruff check ."
        status: pass
    human_judgment: false

patterns-established:
  - "VENDOR.md per-batch provenance sections: each delivery batch gets its own dated subsection (generation date + tool) appended alongside prior history, never overwriting it"

# Metrics
duration: ~35min (Task 3 portion of this session; Tasks 1-2 completed in prior sessions per the plan's own record)
completed: 2026-08-27
status: complete
---

# Phase 03.1 Plan 05: Illustration Hand-off, Provenance & Reachability Summary

**25 of 26 outstanding D-03 illustration targets delivered, digest-registered in VENDOR.md, and proven reachable through the live four-tier `select_illustration()` code across Tiers 1/2/3 — 1 file (`royal-air-maroc-embraer.png`) named as the sole remaining outstanding target.**

## Performance

- **Duration:** ~35 min for this session's Task 3 execution (provenance registration, reachability proof, verification suite, commits). Task 1 (HANDOFF.md rewrite) and Task 2 (the human illustration-generation gate) were completed in prior sessions of this same plan.
- **Completed:** 2026-08-27
- **Tasks:** 3/3 (Task 1 auto, Task 2 checkpoint:human-action, Task 3 auto)
- **Files modified/created:** 30 (25 new PNGs + 3 `_unresolved/` PNGs + `HANDOFF.md` + 2 `VENDOR.md` files)

## Accomplishments

- Rewrote `HANDOFF.md` around the code's own `--targets` output, covering the full 34-file D-03 plan (15 new airline primaries, 3 new secondary variants, 7 new neutral shape fallbacks, plus the 8 already-vendored baseline files), with the Naming-rules rename trap documented and cited against `03.1-LIVE-RESOLUTION.md`'s live evidence.
- Took delivery of a human-confirmed, explicitly-named partial batch: 25 of 26 outstanding target files, every one nose-left and type-matched by eye per the developer's own sign-off (Task 2's blocking gate, already closed before this agent resumed — not re-litigated here).
- Computed and recorded real `shasum -a 256` digests and Pillow-read pixel dimensions for all 25 delivered files (never copied from another record) in `server/assets/icons/illustrations/VENDOR.md`'s extended "Per-file digests" table, split into airline-primary, secondary-variant, and neutral-shape-fallback subsections.
- Registered the 3 generated-but-out-of-scope files (`_unresolved/amelia-international.png`, `_unresolved/la-compagnie.png`, `_unresolved/air-caraibes-atr72-unused.png`) with explicit per-file explanations of why each corresponds to no real `(airline, shape)` selection key — satisfying `scripts/check-attribution.sh`'s recursive scan without misrepresenting them as shipped, reachable art.
- Added a "Phase 3.1 coverage" section to `illustrations/VENDOR.md` stating target=34, delivered=33 (8 baseline + 25 this batch), outstanding=1 (`royal-air-maroc-embraer.png`, reason: not yet generated), and naming both D-03 airlines `03.1-LIVE-RESOLUTION.md` excluded (Amelia International, La Compagnie) with their reasons.
- Updated `server/assets/icons/VENDOR.md`'s top-level summary to describe the new neutral-shape-file category and defer per-file type detail to `illustrations/VENDOR.md` now that aircraft type is filename-dictated rather than freely chosen.
- Proved reachability live against the real `select_illustration()` code (not just presence on disk) across three fallback tiers — see "Reachability Proof" below.
- Re-ran and confirmed green: `scripts/check-attribution.sh`, `illustrations.py --validate` (25/26, 1 outstanding as expected), `scripts/run-all-tests.sh` (all 9 harnesses, `test_illustrations.py` at 42/42 unchanged since 03.1-03 — `required_filenames()`'s on-disk union automatically enforces the newly delivered files with zero code change), and `ruff check .`.

## Reachability Proof

Run via `server/.venv/bin/python3` importing `server.plane.illustrations.select_illustration` directly:

```python
# Tier 1: exact airline + shape match
select_illustration({"airline_name": "CCM Airlines"}, aircraft_type="AT72")
-> ccm-airlines-atr72.png

# Tier 2 (D-06): known airline, no exact-shape file for A320 -> falls to the airline's primary file
select_illustration({"airline_name": "CCM Airlines"}, aircraft_type="A320")
-> ccm-airlines.png

# Tier 3 (D-07): unrecognized airline, recognized+covered shape -> neutral shape file
select_illustration({"airline_name": "Some Unrecognized Airline"}, aircraft_type="A321")
-> generic-a320.png
```

All three outputs are relative to `ILLUSTRATION_DIR` and match exactly what the plan's acceptance criteria required, spanning a delivered secondary-variant file (Tier 1), a delivered airline-primary file (Tier 2), and a delivered neutral-shape file (Tier 3).

## Task Commits

1. **Task 1: Rewrite HANDOFF.md for the full D-03 target set** - `3b74b3b` (docs) — completed and committed in a prior session.
2. **Task 2: Illustration hand-off (developer generates and delivers the art)** - checkpoint:human-action, no separate commit (files landed as untracked working-tree additions; the developer's human sign-off was interactive, not code). The delivered PNGs were committed as part of Task 3 below, since Task 2 itself produces no repository-tracked change until registered.
3. **Task 3: Register provenance, prove reachability, record what remains** - `e0193e8` (feat) — includes the 25 delivered PNGs, the 3 `_unresolved/` files, and both `VENDOR.md` updates.

**Plan metadata:** this commit (docs: complete plan) — see below.

## Files Created/Modified

- `server/assets/icons/illustrations/HANDOFF.md` - rewritten for the full 34-file D-03 target set (Task 1, prior session)
- `server/assets/icons/illustrations/*.png` (25 new files) - the delivered art itself; see frontmatter `key-files.created` for the full list
- `server/assets/icons/illustrations/_unresolved/*.png` (3 files) - out-of-scope generated art, kept for future reference, registered but not selectable
- `server/assets/icons/illustrations/VENDOR.md` - extended per-file digest table, new Phase 3.1 batch provenance entry, new "Phase 3.1 coverage" section, updated Coverage note re: easyJet's two AOC prefixes
- `server/assets/icons/VENDOR.md` - updated selected-aircraft-types summary to describe the new neutral-shape-file category and defer per-file detail to `illustrations/VENDOR.md`

## Decisions Made

- The 3 out-of-scope generated files were kept on disk under a new `_unresolved/` subdirectory (not deleted) and registered in `VENDOR.md` with explicit non-target rationale, rather than either silently discarding real generation work or letting `check-attribution.sh`'s recursive scan fail on unregistered files.
- `royal-air-maroc-embraer.png` was left genuinely outstanding — not fabricated, not substituted with a different file, not silently dropped from the record. It is named by filename in `VENDOR.md`'s "Phase 3.1 coverage" section and in `illustrations.py --outstanding`'s live output.
- Airline files whose `adsbdb`-resolved name predates a real-world rebrand (`europe-airpost.png` for ASL Airlines France; `corsairfly.png` for Corsair International) are recorded in `VENDOR.md` under their resolved (pre-rebrand) name with a parenthetical note of the current brand — never renamed to the current public brand, per the Naming rules `03.1-LIVE-RESOLUTION.md` established and `HANDOFF.md` documents.

## Deviations from Plan

None beyond what the plan itself anticipated and explicitly permitted: Task 2's `<action>` and `<acceptance_criteria>` explicitly allow "Delivery may be incremental," and this plan's `must_haves` explicitly requires "Whatever remains undelivered is recorded by name in VENDOR.md and the SUMMARY, so no D-03 target is silently dropped" — both conditions are satisfied here rather than deviated from. No Rule 1/2/3/4 auto-fixes were needed during Task 3's execution; every acceptance criterion the plan specified was met on the first pass.

## Known Stubs

None. `royal-air-maroc-embraer.png` is not a stub in the render path — it is a named-outstanding target file. Any real Royal Air Maroc flight detected as the Embraer shape falls through cleanly to Tier 3 (`generic-embraer.png`, already delivered and validated) or Tier 4, per `select_illustration()`'s existing, tested fallback design — there is no broken or empty rendering path, only a lower-tier (but still correct and shipped) fallback illustration until this one file is generated.

## Issues Encountered

None. All three re-verification commands (`scripts/check-attribution.sh`, `illustrations.py --validate`, `scripts/run-all-tests.sh`) passed on the first run after the `VENDOR.md` updates; `ruff check .` reported zero findings; `git status --porcelain` was clean immediately after the Task 3 commit.

## User Setup Required

None - no external service configuration required. The one remaining external dependency (generating `royal-air-maroc-embraer.png`) is the same D-09 hand-off mechanism already documented in `HANDOFF.md`; no new setup step exists beyond running the same `--outstanding` / paste-the-prompt / `--validate` loop the developer already used for this batch.

## Next Phase Readiness

- Plan 03.1-05 is complete per its own `must_haves`: partial-but-named delivery is an explicitly accepted outcome ("Whatever remains undelivered is recorded by name..."), and every other acceptance criterion (validator green, attribution green, full suite green, reachability proven across 3 tiers) is met.
- Phase 3.1 itself has one open item carried forward: `royal-air-maroc-embraer.png` remains outstanding. No code change is required to close it later — dropping the file into `server/assets/icons/illustrations/` under that exact name and re-running `--validate` is sufficient; `required_filenames()`'s on-disk union means the automated suite will pick it up with zero further code changes.
- No blockers to closing Phase 3.1 as a whole; this was its final (5th) plan.

---
*Phase: 03.1-procedural-per-airline-livery-rendering*
*Completed: 2026-08-27*

## Self-Check: PASSED

- FOUND: `server/assets/icons/illustrations/easyjet.png`
- FOUND: `server/assets/icons/illustrations/_unresolved/amelia-international.png`
- FOUND: `server/assets/icons/illustrations/HANDOFF.md`
- FOUND: `server/assets/icons/illustrations/VENDOR.md`
- FOUND: `server/assets/icons/VENDOR.md`
- FOUND commit: `3b74b3b` (Task 1)
- FOUND commit: `e0193e8` (Task 3)
