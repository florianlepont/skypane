---
phase: 03.1-procedural-per-airline-livery-rendering
plan: 03
subsystem: illustration-selection
tags: [python, illustration-selection, fallback-tiers, icao-type-classification, asvs-v5]

requires:
  - phase: 03.1-procedural-per-airline-livery-rendering
    provides: "03.1-LIVE-RESOLUTION.md's 24-airline D-03 target table with verdict tokens, including the two adsbdb-rename cases (Europe Airpost, Corsairfly) and the two [UNRESOLVED] exclusions (Amelia International, La Compagnie)"
  - phase: 03.1-procedural-per-airline-livery-rendering
    provides: "detect.py's aircraft_type field (normalized ICAO type designator or None) on every flight dict, from 03.1-02"
provides:
  - "classify_aircraft_type(icao_type): pure, never-raising ICAO-type-designator -> one of SHAPE_SLUGS (seven D-03 shapes) or None"
  - "select_illustration(route, aircraft_type=None): four-tier lookup (exact airline+shape, airline-only/D-06, neutral shape/D-07, universal fallback/D-08), byte-compatible with every pre-existing single-argument call site"
  - "target_filenames()/required_filenames()/outstanding_filenames(): the full D-03 hand-off plan, the immovable must-exist-now baseline, and the machine-reportable remainder, respectively"
  - "--targets/--outstanding/--strict-targets CLI flags for plan 03.1-05 to drive its hand-off from"
affects: [03.1-04-render-caption, 03.1-05-illustration-handoff]

tech-stack:
  added: []
  patterns:
    - "classify_aircraft_type() mirrors normalise_airline_key()'s exact shape (isinstance guard, strip/upper, static-table lookup, None on miss, never raises) - a lookup against a fixed table only, never a pass-through of its argument"
    - "Every composite selection key (airline-shape, generic-shape) is built through the existing illustration_path_for_key() helper so the _UNSAFE_KEY_RE boundary check applies uniformly - no new direct os.path.join for path construction was added by either new tier"
    - "required_filenames() vs target_filenames() vs outstanding_filenames() split (P-05): 'must exist now' vs 'full plan' vs 'remaining work', so CI stays green mid-hand-off"

key-files:
  created: []
  modified:
    - server/plane/illustrations.py
    - server/test_illustrations.py

key-decisions:
  - "_ILLUSTRATION_TARGETS built exactly from 03.1-LIVE-RESOLUTION.md's 'Consequences for the target set': 16 baseline airlines + 6 newly live-resolved Step-C airlines (Europe Airpost, Tunisair, Pegasus Airlines, Chalair Aviation, Twin Jet, Corsairfly) + 4 P-04 secondary variants = 22 primary + 4 secondary + 7 generic-shape + 1 universal fallback = 34 target files today"
  - "Amelia International and La Compagnie deliberately excluded from _ILLUSTRATION_TARGETS per their [UNRESOLVED] verdicts - can be added later with zero other code change once a real callsign confirms their resolved name"
  - "Used the exact adsbdb-resolved strings for the two rename cases (Europe Airpost, not ASL Airlines France; Corsairfly, not Corsair International), documented in a new module-docstring section, per the same discipline as the existing CCM Airlines/Air Corsica case"

requirements-completed: [PLANE-01, PLANE-02]

coverage:
  - id: D1
    description: "classify_aircraft_type() maps every real D-03 ICAO type designator onto exactly one of the seven shape buckets; unknown, empty, non-string and hostile inputs all return None without raising"
    requirement: "PLANE-01"
    verification:
      - kind: unit
        ref: "server/test_illustrations.py#illustrations (42/42 checks, 6 dedicated to classify_aircraft_type())"
        status: pass
    human_judgment: false
  - id: D2
    description: "select_illustration(route, aircraft_type) resolves all four D-06/D-07/D-08 tiers correctly, in the documented precedence order, never raises, and never returns a path outside ILLUSTRATION_DIR"
    requirement: "PLANE-02"
    verification:
      - kind: unit
        ref: "server/test_illustrations.py#illustrations (8 checks: Tier 1-4 plus a hostile aircraft_type x malformed-route matrix with a realpath-containment assertion)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The full D-03 target file set is enumerable from code (target_filenames()), the pre-03.1 baseline still validates as required right now (required_filenames()), and the outstanding remainder is machine-reportable (outstanding_filenames(), --strict-targets)"
    verification:
      - kind: unit
        ref: "server/test_illustrations.py#illustrations (6 checks: generic-shape completeness, no duplicates, no orphaned vendored file, required subset of targets, outstanding == targets-minus-on-disk, P-04 variant presence)"
        status: pass
      - kind: integration
        ref: "server/plane/illustrations.py --targets/--required/--outstanding/--validate/--validate --strict-targets, all manually run this session with exact-count assertions matching the plan's acceptance criteria"
        status: pass
    human_judgment: false

duration: ~25min
completed: 2026-08-27
status: complete
---

# Phase 3.1 Plan 03: Illustration Selection Summary

**`illustrations.py` grew from a one-key (airline) lookup into a four-tier (airline, aircraft-type) selection engine, with the full 34-file D-03 target set now enumerable from code without breaking the eight-file baseline CI already enforces.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-27 (session start, reading context)
- **Completed:** 2026-08-27T05:59:44Z
- **Tasks:** 3 (each executed as a RED test commit followed by a GREEN implementation commit, per the task-level `tdd="true"` flag)
- **Files modified:** 2 (`server/plane/illustrations.py`, `server/test_illustrations.py`)

## Accomplishments

- `SHAPE_SLUGS` (the seven D-03 base shapes) and `_TYPE_SHAPE_BUCKETS` (a hand-curated ICAO-designator -> shape-slug static table, ~35 codes) added; `classify_aircraft_type()` mirrors `normalise_airline_key()`'s exact never-raises shape and is proven against real designators (`A20N`, `B38M`, `AT43`, `BE9L`, `E145`, `A339`, `A35K`), case/whitespace variants, unknown codes, falsy/non-string inputs, and a hostile path-separator battery (T-03.1-03-01)
- `select_illustration(route, aircraft_type=None)` rewritten in place as a four-tier lookup: Tier 1 exact `{airline}-{shape}.png`, Tier 2 (D-06) airline-only ("brand wins"), Tier 3 (D-07) neutral `generic-{shape}.png`, Tier 4 (D-08) the existing universal fallback - every tier reuses `illustration_path_for_key()` so the `_UNSAFE_KEY_RE` boundary check applies uniformly, and omitting `aircraft_type` reproduces pre-03.1 behaviour exactly (verified: `select_illustration({"airline_name": "Air France"})` still returns `air-france.png`)
- `_ILLUSTRATION_TARGETS` (22 primary airlines + 4 P-04 secondary variants), `target_filenames()`, `required_filenames()` (now a baseline-plus-on-disk union per P-05), and `outstanding_filenames()` implemented; `--targets`/`--outstanding`/`--strict-targets` added to the CLI
- `test_illustrations.py` grew from 22 to 42 checks across three RED/GREEN cycles (6 for `classify_aircraft_type()`, 8 for the four-tier `select_illustration()`, 6 for the target/required/outstanding split), all passing; `EXPECTED_CHECK_COUNT` bumped in lockstep each time
- Full 9-harness suite (`scripts/run-all-tests.sh`), `scripts/check-attribution.sh`, and `ruff check .` all green; coverage 78% (above the 75% floor); `git diff --stat` across all six of this plan's commits touches only the two files named in the plan's `files_modified`

## Task Commits

Each task was executed as an explicit RED (failing test) commit followed by a GREEN (implementation) commit, per the plan's task-level `tdd="true"` attribute:

1. **Task 1: `classify_aircraft_type()` and the seven-bucket shape table**
   - RED: `32d217c` (test) - 6 new checks added, confirmed failing (`AttributeError`, 22/28)
   - GREEN: `9a72ad9` (feat) - `SHAPE_SLUGS`, `_TYPE_SHAPE_BUCKETS`, `classify_aircraft_type()` implemented; 28/28 pass
2. **Task 2: Four-tier `select_illustration(route, aircraft_type=None)`**
   - RED: `60a4628` (test) - 8 new checks added, confirmed failing (`TypeError`, 28/36)
   - GREEN: `fbb746d` (feat) - four-tier rewrite implemented; 36/36 pass; `test_render.py` and the full suite re-confirmed green
3. **Task 3: The D-03 target file set, required/target/outstanding split, CLI**
   - RED: `c0bfdbf` (test) - 6 new checks added, confirmed failing (`AttributeError`, 36/42)
   - GREEN: `57f5cab` (feat) - `_ILLUSTRATION_TARGETS`, `target_filenames()`, `outstanding_filenames()`, widened `required_filenames()`/`_validate_directory()`, new CLI flags implemented; 42/42 pass

**Plan metadata:** (this commit, pending) `docs(03.1-03): complete plan`

## Files Created/Modified

- `server/plane/illustrations.py` - `SHAPE_SLUGS`, `_TYPE_SHAPE_BUCKETS`, `classify_aircraft_type()`, four-tier `select_illustration()`, `_ILLUSTRATION_TARGETS`, `target_filenames()`, `outstanding_filenames()`, widened `required_filenames()`/`_validate_directory()`, `--targets`/`--outstanding`/`--strict-targets` CLI flags, two new module-docstring sections (the `_TYPE_SHAPE_BUCKETS` discipline note, and the "filenames mirror the data source, never the current brand" rename-trap note citing `03.1-LIVE-RESOLUTION.md`)
- `server/test_illustrations.py` - 20 new checks across three RED/GREEN cycles; `EXPECTED_CHECK_COUNT` 22 -> 28 -> 36 -> 42

## Decisions Made

- Populated `_ILLUSTRATION_TARGETS` directly from `03.1-LIVE-RESOLUTION.md`'s "Consequences for the target set" section rather than re-deriving it from `03.1-RESEARCH.md`'s earlier (partially superseded) filename table - the LIVE-RESOLUTION document is the more recent, live-verified ground truth, and it explicitly flags the two rename cases (Europe Airpost, Corsairfly) and the two exclusions (Amelia International, La Compagnie) that RESEARCH.md's draft table did not yet have.
- Kept the four P-04 secondary-variant entries' primary/secondary pairing exactly as specified in the plan text (CCM Airlines A320 primary + ATR72 secondary, Transavia France B737 primary + A320 secondary, Royal Air Maroc B737 primary + Embraer secondary, Air Caraïbes A350 primary + A330 secondary) - no independent judgment call needed since the plan's own action text pinned all four pairs explicitly.

## Deviations from Plan

### Auto-fixed Issues

None - every task's implementation matched its `<action>` block directly; no bugs, missing critical functionality, or blocking issues were discovered during implementation.

### Noted Discrepancy (not a deviation, informational only)

**Task 2's acceptance criterion "`grep -v '^\s*#' server/plane/illustrations.py | grep -c 'os.path.join(ILLUSTRATION_DIR'` outputs at most `2`" does not hold literally** - the pre-existing (pre-plan) file already had **3** such occurrences before this plan touched anything: `generic_fallback_path()`, `illustration_path_for_key()`, and a third one already present inside the original `_validate_directory()` (`path = os.path.join(ILLUSTRATION_DIR, name)`, original line 242), which the plan's criterion text apparently did not count. Verified via `git show 713b4a4:server/plane/illustrations.py | grep -v '^\s*#' | grep -c 'os.path.join(ILLUSTRATION_DIR'` before starting this plan's edits, which also returns 3. **The criterion's actual intent - that neither new tier in Task 2's rewritten `select_illustration()` constructs a path directly instead of going through `illustration_path_for_key()` - is fully satisfied**: the grep count is unchanged at 3 immediately after Task 2's GREEN commit (confirmed), only increasing to include Task 3's two additional (expected, out-of-scope-for-Task-2) `os.path.join(ILLUSTRATION_DIR, ...)` calls in `required_filenames()`/`outstanding_filenames()`/`_validate_directory()`'s widened logic, which is Task 3's own territory, not Tier construction. No code change was made in response to this - it is a pre-existing plan-authoring miscount, not a defect.

---

**Total deviations:** 0 auto-fixed. One noted, non-blocking discrepancy in a single acceptance-criterion's literal expected number (documented above, intent satisfied).

## Issues Encountered

None beyond the discrepancy documented above.

## User Setup Required

None - no external service configuration required. This plan makes no network call of its own.

## Next Phase Readiness

- `classify_aircraft_type()` and the four-tier `select_illustration()` are ready for plan 03.1-04 (`render.py`'s `_flight_line2_text()` and its two `select_illustration()` call sites) to consume: `flight.get("aircraft_type")` is already available on every flight dict via 03.1-02's extraction.
- `target_filenames()`/`outstanding_filenames()`/`--targets`/`--outstanding` give plan 03.1-05 a single command (`server/.venv/bin/python3 server/plane/illustrations.py --outstanding`) to generate its hand-off list from, rather than hand-typing the 26 currently-outstanding filenames.
- No blockers. The two `[UNRESOLVED]` airlines (Amelia International, La Compagnie) remain excluded from `_ILLUSTRATION_TARGETS`, carried forward from 03.1-01/03.1-LIVE-RESOLUTION.md as a pre-existing open item, not new to this plan.

---
*Phase: 03.1-procedural-per-airline-livery-rendering*
*Completed: 2026-08-27*
