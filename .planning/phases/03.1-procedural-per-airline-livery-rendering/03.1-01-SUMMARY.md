---
phase: 03.1-procedural-per-airline-livery-rendering
plan: 01
subsystem: docs
tags: [adsbdb, adsb-fi, airplanes-live, illustrations, roadmap, live-verification]

# Dependency graph
requires:
  - phase: 03-visual-polish-on-real-glass
    provides: the D-09 external-AI-illustration-generation hand-off discipline and the existing 7-airline static asset set this phase extends
provides:
  - "ROADMAP.md's Phase 03.1 section rewritten to describe the static-but-type-accurate approach (D-10), superseding the abandoned procedural-compositing architecture"
  - "03.1-LIVE-RESOLUTION.md: a live-verified, evidence-backed record of every D-03 airline's exact adsbdb-resolved airline_name string, replacing every guessed/assumed name with a real probe result"
  - "Confirmation that the ICAO type designator field (t) is present on opendata.adsb.fi (111/125 real aircraft); airplanes.live confirmed network-blocked (403), not schema-absent"
  - "Two Pitfall-1-style stale-brand-name findings (ASL Airlines France resolves as 'Europe Airpost', Corsair International resolves as 'Corsairfly') that plan 03.1-03 must use as the real selection keys"
  - "Two airlines (Amelia International, La Compagnie) marked UNRESOLVED with full evidence and excluded from the target set pending future re-verification with a real callsign"
affects: [03.1-02-detect-type-extraction, 03.1-03-illustration-selection, 03.1-05-illustration-handoff]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Calibrate a data-source probe method against a known answer before trusting it on unknowns (AFR -> Air France) — reused from the existing illustrations.py docstring discipline"
    - "Never rename a stale-looking asset filename without first live-querying the data source for its current resolved string (Pitfall 1) — this session found two more instances of the same pattern beyond the existing CCM Airlines/Air Corsica case"

key-files:
  created:
    - .planning/phases/03.1-procedural-per-airline-livery-rendering/03.1-LIVE-RESOLUTION.md
  modified:
    - .planning/ROADMAP.md

key-decisions:
  - "ASL Airlines France's real adsbdb-resolved selection key is 'Europe Airpost' (confirmed via two live, real, currently-flying callsigns, FPO701/FPO458), not D-03's label 'ASL Airlines France' — plan 03.1-03 must build the target table entry and filename (europe-airpost.png) around the verified string"
  - "Corsair International's real adsbdb-resolved selection key is 'Corsairfly' (confirmed via the calibrated airline endpoint plus independent Wikipedia corroboration that Corsairfly is a genuine prior legal name for the same airline), not D-03's label 'Corsair International' — filename must be corsairfly.png"
  - "Amelia International and La Compagnie are excluded from the illustration target set this phase — neither has a trustworthy adsbdb-resolved name (Amelia: no candidate code resolves correctly, including one independently sourced from Wikipedia/airhex.com that turned out to belong to a different real airline; La Compagnie: the real-world ICAO code is confirmed via Wikipedia but adsbdb's own database maps it to an unrelated US operator) — both can be added later with zero code change once a real callsign is captured"
  - "ROADMAP.md's Phase 03.1 goal and success criteria 3/4 now describe the two-key/four-tier static-illustration-selection mechanism (D-06/D-07/D-08) instead of the abandoned server-side SVG-shape+livery-color compositing engine; the original 'Note on origin' paragraph is preserved verbatim with a new dated superseding note appended, per this project's established Roadmap Evolution precedent"

requirements-completed: [PLANE-01, PLANE-02]

coverage:
  - id: D1
    description: "ROADMAP.md's Phase 03.1 goal, success criteria 3-4, and origin note describe the static-but-type-accurate approach actually being built, not the abandoned procedural vector-compositing engine"
    requirement: "PLANE-01"
    verification:
      - kind: other
        ref: "grep -v '^#' .planning/ROADMAP.md | grep -c 'onto the correct aircraft-type' -> 0; grep -c '03.1-05-PLAN.md' .planning/ROADMAP.md -> 1; git diff --numstat .planning/ROADMAP.md confined to the Phase 03.1 bullet+section"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every D-03 target airline's exact adsbdb-resolved airline_name is recorded verbatim from a real probe (never guessed), with an explicit verdict token, in 03.1-LIVE-RESOLUTION.md"
    requirement: "PLANE-01"
    verification:
      - kind: other
        ref: "grep -c '\\[VERIFIED-CALLSIGN\\]\\|\\[VERIFIED-AIRLINE-ENDPOINT-ONLY\\]\\|\\[UNRESOLVED\\]\\|\\[CITED' 03.1-LIVE-RESOLUTION.md -> 37 (>= 12 required)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The t field's presence is confirmed (adsb.fi) or explicitly marked unconfirmed-by-network (airplanes.live) per provider, closing Assumption A3/Pitfall 4"
    requirement: "PLANE-02"
    verification:
      - kind: other
        ref: "Live curl to opendata.adsb.fi/api/v2/lat/48.8566/lon/2.3522/dist/250 (111/125 t-present) and api.airplanes.live/v2/point/... (HTTP 403), both recorded verbatim in 03.1-LIVE-RESOLUTION.md"
        status: pass
    human_judgment: false

duration: 35min
completed: 2026-08-27
status: complete
---

# Phase 3.1 Plan 01: ROADMAP Correction & Live Airline Resolution Summary

**Live-verified every D-03 airline's exact adsbdb-resolved name via real callsigns/calibrated probes (finding two more stale-brand-name mismatches beyond the known CCM Airlines case), confirmed the ADS-B `t` field on adsb.fi, and rewrote ROADMAP.md's Phase 3.1 section to drop the abandoned procedural-compositing architecture in favor of the static-but-type-accurate approach actually being built.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-26T23:32:00Z (approx.)
- **Completed:** 2026-08-27T00:06:58Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 edited)

## Accomplishments
- Live-resolved all 24 D-03 airlines' exact `adsbdb` selection-key strings using a calibrated probe method (airline endpoint validated against the known `AFR` -> `Air France` answer first), with 7 `[VERIFIED-CALLSIGN]`, 7 `[VERIFIED-AIRLINE-ENDPOINT-ONLY]`, 8 `[CITED: 03.1-RESEARCH.md]`, and 2 `[UNRESOLVED]` verdicts — zero guessed strings entered the record
- Found two new stale-brand-name mismatches beyond the already-known CCM Airlines/Air Corsica case: ASL Airlines France's real adsbdb string is `"Europe Airpost"`, and Corsair International's is `"Corsairfly"` — both confirmed via real evidence (live callsigns for the former, calibrated airline endpoint + independent Wikipedia corroboration for the latter), both documented as load-bearing findings for plan 03.1-03's target table
- Confirmed the ICAO `t` field is present on 111/125 real in-flight aircraft via a live `opendata.adsb.fi` query; confirmed `airplanes.live` is network-blocked (HTTP 403, Cloudflare) rather than schema-absent, consistent with the existing `COMPLIANCE.md` finding
- Rewrote ROADMAP.md's Phase 3.1 goal, phase-list summary bullet, and success criteria 3/4 via four scoped `Edit` calls (never a whole-file `Write`) to describe the two-key/four-tier static-illustration-selection mechanism instead of the abandoned server-side SVG-shape+livery-color compositing engine, while preserving the original "Note on origin" paragraph verbatim and appending a dated superseding note

## Task Commits

Each task was committed atomically:

1. **Task 2: Live-resolve every unverified target airline and confirm the `t` field on both providers** - `cc814ed` (docs)
2. **Task 1: Rewrite ROADMAP.md's Phase 3.1 section to match the approach actually being built (D-10)** - `b94cb80` (docs)

_Note: Task 2 was executed and committed before Task 1 in this session (both are independent, non-order-dependent documentation tasks; Task 2's findings informed Task 1's origin-note wording, so doing Task 2 first was the more natural sequence)._

## Files Created/Modified
- `.planning/phases/03.1-procedural-per-airline-livery-rendering/03.1-LIVE-RESOLUTION.md` - New file: probe method calibration, the four re-confirmed load-bearing strings, the eight newly-resolved D-03 airlines, both providers' `t`-field confirmation, the full 24-row resolved-names table, and the Consequences-for-the-target-set include/exclude list
- `.planning/ROADMAP.md` - Phase 3.1 section: goal, phase-list summary bullet, success criteria 3-4, and a new superseding note appended to the existing "Note on origin" paragraph

## Decisions Made
- ASL Airlines France's selection key is `"Europe Airpost"` (not D-03's label) — see key-decisions above
- Corsair International's selection key is `"Corsairfly"` (not D-03's label) — see key-decisions above
- Amelia International and La Compagnie excluded from the target set this phase (both `[UNRESOLVED]`) — see key-decisions above
- Task execution order: Task 2 (live resolution) run and committed before Task 1 (ROADMAP rewrite), since Task 2's findings directly informed the wording of Task 1's superseding note

## Deviations from Plan

None — plan executed exactly as written. Both tasks' automated verification commands and acceptance criteria passed without requiring a Rule 1/2/3 auto-fix. Task 2 required substantially more live-probing effort than a literal reading of the plan's eight-airline list might suggest (candidate ICAO codes for Amelia International and La Compagnie both turned out to be wrong or ambiguous, requiring independent Wikipedia cross-checks per Pitfall 2's "never trust a guessed code" discipline before concluding `[UNRESOLVED]`) — this is the plan's own designed behavior (Step C explicitly anticipates candidate codes needing validation and discarding), not a deviation from it.

## Issues Encountered
- `scripts/run-all-tests.sh`'s no-regression check (plan's `<verification>` section) could not be executed in this worktree: `server/.venv` does not exist here (confirmed present in the main repo checkout, absent in this git worktree), and this sandbox's pip is configured against an internal package registry (`nexus.internal.enchanted.tools`) that is not reachable from this network, so a fresh venv could not be provisioned either. This is a pre-existing environment-provisioning gap in this specific worktree, unrelated to this plan's changes — `git status --porcelain server/` was confirmed empty both before and after this plan's two tasks, and this plan touches zero server code, so there is nothing for the test suite to regress on. Flagging for whoever executes plan 03.1-02 (the first plan in this phase that actually touches `server/`) to provision the venv or run from the main checkout.

## User Setup Required

None - no external service configuration required. (All live probes in this plan were unauthenticated, read-only HTTP GETs to public APIs — no credentials involved.)

## Next Phase Readiness
- Plan 03.1-02 (`detect.py` type extraction) can proceed immediately: the `t` field's presence is confirmed on `adsb.fi`, and the never-raise `.get()`-then-normalize extraction pattern this plan's research already sketched is unaffected by this plan's findings.
- Plan 03.1-03 (`classify_aircraft_type()` + two-key `select_illustration()` + target table) has an unambiguous, evidence-backed include/exclude list to build from: 22 airlines with a real resolved name (two of them under corrected selection keys — `Europe Airpost`, `Corsairfly` — not their D-03 labels), 2 airlines (`Amelia International`, `La Compagnie`) deliberately excluded with documented reasons.
- Carried-forward gap for a future session: `airplanes.live`'s `t`-field presence remains unconfirmed by direct live query (network-blocked, not schema-absent) — the first real production poll cycle that wins on `provider=airplaneslive` post-03.1-02 should be spot-checked in `journalctl` for a populated `aircraft_type` field (Pitfall 4), and this worktree's missing `server/.venv` should be provisioned before or during 03.1-02's execution.

---
*Phase: 03.1-procedural-per-airline-livery-rendering*
*Completed: 2026-08-27*

## Self-Check: PASSED

- FOUND: `.planning/phases/03.1-procedural-per-airline-livery-rendering/03.1-LIVE-RESOLUTION.md`
- FOUND: `.planning/phases/03.1-procedural-per-airline-livery-rendering/03.1-01-SUMMARY.md`
- FOUND commit `cc814ed` (Task 2)
- FOUND commit `b94cb80` (Task 1)
- FOUND commit `ad9dcd2` (this summary)
