---
phase: 05
slug: low-battery-indicator
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-27
revised: 2026-08-28
amends: .planning/phases/03-visual-polish-on-real-glass/03-UI-SPEC.md (Revision 4, as-shipped record of `server/plane/render.py`'s two-flight poster layout — this document adds one new zone without touching any existing one)
supersedes: 03-CONTEXT.md's D-12 (Yellow reserved for the low-battery indicator) — 05-CONTEXT.md's D-04 confirms the indicator is White/Ivory, not Yellow; the D-12 reservation is now moot and Yellow stays free
---

# Phase 05 — UI Design Contract (Low-Battery Indicator)

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note (inherited from 02/03-UI-SPEC.md, restated for this revision):** This is not a web/React UI. The "frontend" is a Python/Pillow raster-rendering pipeline on the server that produces a single 960,000-byte, 4-bit-per-pixel indexed-color panel image (`docs/PROTOCOL.md` §1), blitted directly to a 13.3" E Ink Spectra 6 panel by firmware with no rendering logic of its own. There is no browser, no DOM, no component tree, no user input. The shadcn initialization gate does not apply.

**What this document covers:** Exactly one new visual element — a conditionally-rendered battery icon glyph, bottom-left — added to the already-locked Phase 3 two-flight poster. It does not restate or revise anything Phase 3 already locked (frame, top labels, main/previous illustrations and text blocks, typography, background colour). Every decision below is either lifted directly from `05-CONTEXT.md`'s D-01 through D-07 (cited inline) or resolves one of that document's explicitly named "Claude's Discretion" items (also cited inline) — nothing here reopens a decision `05-CONTEXT.md` already locked.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — custom Pillow drawing primitives, reusing `server/plane/render.py`'s existing `_font()`/`ImageDraw` conventions |
| Icon library | none — one hand-drawn glyph (`draw_battery_icon()`), built from `ImageDraw.rectangle()` calls only, no vector asset, no font glyph |
| Font | Not applicable to this element — the battery indicator is icon-only, no text (D-04) |

---

## Canvas and Geometry — new zone only

### Revision 2 — live on-glass size correction (2026-08-28)

The geometry originally specified below (Revision 1) shipped in plan 05-02 and was deployed to the real production hardware. The developer then saw the low-battery glyph rendered on the physical 13.3" Spectra 6 panel and judged it too large. Every size dimension (`BATTERY_ICON_BODY_W`/`BODY_H`/`NUB_W`/`NUB_H`/`STROKE_PX`) was therefore reduced to 70% of its original value (`round(original * 0.7)`); the two position constants (`BATTERY_ICON_LEFT`/`BATTERY_ICON_BOTTOM`) and `BATTERY_ICON_FILL_FRAC` were deliberately left untouched. The superseded total bounding box was `(64, 1504, 136, 1536)` — 72×32px; the new one is `(64, 1514, 115, 1536)` — 51×22px. This is a live on-glass correction in the same lineage as Phase 3's D-20/D-21/D-22 and D-26/D-27 on-glass corrections, recorded here per quick task **260828-0qo**. This section's own original instruction — "Confirm against a real render before treating as final" — is exactly what produced this revision: the process worked as designed.

Canvas unchanged: 1200×1600, portrait, full-bleed. `MARGIN = 64` (unchanged `server/plane/render.py` constant, reused here — see below).

**New zone: bottom-left battery icon.** This is the one area of the locked layout with no existing element (`05-CONTEXT.md` D-05) — the frame's top-left corner holds the state label, top-right holds the tag, bottom-right holds the previous-flight card; bottom-left is empty. Anchoring the icon at the same `MARGIN` inset the top labels already use makes it the visual mirror of those corners, and reads as the deliberate opposite-corner counterweight to the previous-flight card (`05-CONTEXT.md` D-05: "visually balances the previous-flight card on the opposite side").

| Constant | Value | Notes |
|---|---|---|
| `BATTERY_ICON_LEFT` | `MARGIN` = 64 | **Unchanged by Revision 2** — same left inset as the top-row labels and the previous-flight text block's `available_width` origin — one consistent left baseline across the whole panel |
| `BATTERY_ICON_BOTTOM` | `HEIGHT - MARGIN` = 1536 | **Unchanged by Revision 2** — mirrors the top labels' `MARGIN` inset on the opposite (bottom) edge |
| `BATTERY_ICON_BODY_W` | 45px | Icon body (outline rectangle) width — `round(64 * 0.7)` = `round(44.8)` (Revision 2; was 64px) |
| `BATTERY_ICON_BODY_H` | 22px | Icon body height — `round(32 * 0.7)` = `round(22.4)` (Revision 2; was 32px) |
| `BATTERY_ICON_NUB_W` | 6px | Terminal nub width, attached to the body's right edge — `round(8 * 0.7)` = `round(5.6)` (Revision 2; was 8px) |
| `BATTERY_ICON_NUB_H` | 11px | Terminal nub height, vertically centered on the body (to within one pixel — see below) — `round(16 * 0.7)` = `round(11.2)` (Revision 2; was 16px — note: this document's pre-Revision-2 text read 14px, but the shipped 05-02 code has actually used 16px since 05-02 resolved its own grid-alignment discretion item; 16px, not 14px, is the value genuinely superseded here) |
| `BATTERY_ICON_STROKE_PX` | 2px | Outline stroke width — `round(3 * 0.7)` = `round(2.1)` (Revision 2; was 3px). 2px is simultaneously where the proportional 0.7 reduction lands and the floor it stops at: it now equals the frame's own `FRAME_STROKE_PX`, not exceeds it — dropping further to a 1px hairline would cost the glyph its legibility as a distinct shape at e-ink resolution |
| `BATTERY_ICON_FILL_FRAC` | 0.22 | **Unchanged by Revision 2** — a ratio of the interior width, not a pixel size. Fraction of the body's *interior* width (inside the stroke) filled solid, left-aligned — a fixed "low" glyph, not a live gauge (resolves `05-CONTEXT.md`'s "fill-level rendering" discretion item: a single fixed near-empty fill, not proportional to the real mV reading) |

**Derived bounding boxes** (computed once, deterministic, no dependency on flight data) — **Revision 2 values, live on-glass corrected 2026-08-28:**

- Icon body: `(64, 1514, 109, 1536)` — left=`BATTERY_ICON_LEFT`, bottom=`BATTERY_ICON_BOTTOM`, top=`bottom - BATTERY_ICON_BODY_H`, right=`left + BATTERY_ICON_BODY_W`.
- Terminal nub: `(109, 1519, 115, 1530)` — attached to the body's right edge, centered to within one pixel: `nub_top = body_top + (BODY_H - NUB_H) // 2`.
- **Total icon bounding box: `(64, 1514, 115, 1536)` — 51×22px** (was `(64, 1504, 136, 1536)` — 72×32px before this revision). The pre-revision figure was itself compared against the session sketch's "~72×34px, sized like B" reference (`05-CONTEXT.md` D-04); that comparison is now superseded by direct observation of the real panel — the developer saw the shipped 72×32 glyph on the physical Spectra 6 hardware and judged it too large, which is the on-glass feedback this revision applies.
- Fill rectangle (inside the stroke, left-aligned): interior = `(66, 1516, 107, 1534)` (41×18px after inset by `BATTERY_ICON_STROKE_PX`); fill width = `round(41 * 0.22)` = 9px → fill box `(66, 1516, 75, 1534)`.

**Nub centring is no longer exactly symmetric (Revision 2).** `BODY_H - NUB_H` = `22 - 11` = `11`, an odd leftover, so floor division (`// 2`) puts a 5px gap above the nub and a 6px gap below it, rather than the 8px-above/8px-below exact centring the pre-revision 32/16 dimensions produced. This is the intended behaviour of the proportional reduction, not a defect — a future reader should not treat the one-pixel asymmetry as a bug to fix.

**Collision check against the existing locked layout** (verified by hand against `render.py`'s live geometry, not assumed) — **Revision 2: figures updated for the shrunk icon; conclusions unchanged, since a uniform shrink at a fixed anchor (`BATTERY_ICON_LEFT`/`BATTERY_ICON_BOTTOM` unmoved) cannot introduce a new collision — every clearance below can only have strictly increased relative to Revision 1:**

- **Frame** (`FRAME_INSET_FRAC = 0.025` → outline at `(30, 30, 1170, 1570)`): icon sits 34px inside the frame's left edge and 34px inside its bottom edge (unchanged — the anchor didn't move); the icon's own footprint shrank, so its top edge is now 10px further from any element above it than before.
- **Previous-flight card**: right-aligned to the main illustration's right edge (≈x 531–1096 for the default preview illustration; real per-airline files vary but are always right-anchored, never left-anchored) — zero horizontal overlap with the icon's x-range, now `64–115` (was `64–136`), under any real illustration file. Vertically, even a taller-than-average previous illustration's text block bottoms out well above y≈1514 (was y≈1504 pre-revision — the icon's top edge moved down 10px, widening this clearance) — no vertical collision under realistic aspect ratios.
- **Main illustration/text block**: horizontally centered, always spans a width narrower than the full canvas; at `MAIN_ILLUSTRATION_TOP_FRAC = 0.30` (y=480) it sits far above the icon's y-range regardless of state, unaffected by the resize.

**Guard rail:** wrap the icon's draw call in `_assert_within_canvas()` (the same looser guard every other active-state element outside the strict empty-state `SAFE_BOX` already uses — see `03-UI-SPEC.md`'s Canvas and Geometry section) — not `_assert_in_safe_box()`, since, like the frame and both illustrations, this element deliberately sits inside the old 64px band's bottom-left corner rather than being confined by it.

---

## Battery Icon — glyph contract

**Shape:** a simple outline rectangle (the battery "body") with a smaller rectangle "terminal nub" attached to its right edge, plus one solid-filled interior rectangle showing a fixed low level. **Square corners, no rounding** — matches the frame's own plain rectangle-outline aesthetic (`draw_frame()`), not a departure into a softer icon style (resolves `05-CONTEXT.md`'s "corner style" discretion item).

**Orientation:** horizontal, terminal nub on the right — the conventional battery-icon reading direction (matches how a real device battery gauge reads left-to-right, empty-to-full).

**Fill behaviour:** the interior fill is solid at `BATTERY_ICON_FILL_FRAC` (22%) width, left-aligned inside the stroke, in the same ink colour as the outline — the unfilled 78% of the interior shows the state background colour through it (Blue/Green/White depending on state), which is what visually reads as "mostly empty." **This is a single fixed "low" glyph, not a live gauge proportional to the actual mV reading** — resolves `05-CONTEXT.md`'s explicit discretion note that DEVICE-04's plain wording ("see a low-battery indicator") is satisfied by one fixed glyph, and a live/proportional gauge is out of scope (see `05-CONTEXT.md`'s Deferred Ideas).

**No text, no "LOW" label, no percentage** — icon-only per D-04's explicit correction mid-discussion ("tu me parles de texte alors que je pensais à un icône batterie tout simplement"). This is the poster's first and only icon-based element; every other element (state label, tag, both flight text blocks) stays pure text, per `05-CONTEXT.md`'s own flagged exception to the project's text-only visual language.

**Line weight:** `BATTERY_ICON_STROKE_PX = 2` for the outline (Revision 2; was 3). At 2px the stroke no longer exceeds the frame's own `FRAME_STROKE_PX` — it equals it. The rationale changed with the value: 2px is where the proportional `round(3 * 0.7)` reduction lands, and it is simultaneously the floor that reduction stops at, since dropping to a 1px hairline would cost the glyph its legibility as a distinct shape at e-ink resolution. Not related to any stroke-fill anti-aliasing rule (there is none here — all fills are flat integer-index rectangles, same discipline as every other shape in `render.py`).

**Rendering primitive:** `ImageDraw.rectangle(box, outline=ink_idx, width=BATTERY_ICON_STROKE_PX)` for the body and nub outlines, `ImageDraw.rectangle(fill_box, fill=ink_idx)` for the interior fill and the nub (drawn solid, not outlined — a real device's terminal nub reads as a small solid tab, not a second hollow rectangle). No `stroke_width`/`stroke_fill` text-drawing parameters are involved (this is not text) so the project's standing anti-aliasing prohibition on that specific API is not a relevant risk here — but the same "flat integer-index fills only" discipline applies throughout.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Icon ink, departing/arriving states | `IDX_WHITE` (White/Ivory) | Both the outline and the fill are drawn in the same ink colour already used for every other text/frame element in that state (`STATE_INK[state]` — currently `IDX_WHITE` for both `departing` and `arriving`). Matches `05-CONTEXT.md` D-04's explicit White/Ivory choice, and reuses the existing per-state ink dict rather than hardcoding a literal — if `STATE_INK` is ever revised, the icon follows automatically. |
| Icon ink, empty state | `IDX_BLACK` | **New resolution, not covered by `05-CONTEXT.md`** — the empty state's background is flat White (`IDX_WHITE`) with Black (`IDX_BLACK`) text; a White-filled icon would be invisible there. Since a battery-voltage reading is a device concern independent of whether a flight is currently detected, the icon renders in all three states (departing/arriving/empty) when the mV reading is low, using each state's own already-established ink colour rather than a single hardcoded value. Flag this for confirmation during planning if the empty state's low-battery behaviour turns out to need a different call. |
| Accent / secondary | not applicable | This element introduces no new palette colour — it reuses each state's existing single ink index, exactly like the frame and every text role already do |

**D-07 confirmed unaffected:** the existing Blue (`departing`)/Green (`arriving`) full-bleed state background is completely untouched by this element — the icon draws *on top of* the existing flat background fill, using only the state's ink colour, never altering `STATE_BACKGROUND`. The two signals (state = background colour, battery = bottom-left icon) coexist independently, per `05-CONTEXT.md` D-07.

**Yellow reservation retired:** `03-CONTEXT.md`'s D-12 ("Yellow reserved for Phase 4/5's low-battery indicator") is superseded — the indicator uses White/Ivory, not Yellow (`05-CONTEXT.md` D-04). Yellow remains fully free for illustration liveries with no reserved future consumer.

---

## Trigger and Data Contract

*(Not a visual property, but the render-time condition this document's "conditionally rendered" language depends on — included so the contract is self-sufficient without re-reading `05-CONTEXT.md`.)*

| Constant | Value | Source |
|---|---|---|
| `BATTERY_LOW_THRESHOLD_MV` | 3500 | `05-CONTEXT.md` D-01 — locked now, trivially adjustable once 05-01 Tasks 2-3 produce a real discharge curve |
| `BATTERY_LOW_CLEAR_MV` | 3600 | **New, resolves `05-CONTEXT.md`'s hysteresis discretion item** — a 100mV re-arm buffer above the threshold: once the warning has fired, it does not clear until a reading reaches this higher value, preventing flicker from a reading oscillating right at 3500mV across consecutive polls. Cheap, standard debounce practice per `05-CONTEXT.md`'s own instruction. |
| Comparison basis | Raw millivolts, not a derived percentage | `05-CONTEXT.md` D-02 |

**Conditional rendering (D-06):** the icon zone is drawn only when the current battery reading is at/below `BATTERY_LOW_THRESHOLD_MV` (or, once hysteresis has armed, until it clears at `BATTERY_LOW_CLEAR_MV`). At every other reading, this zone is entirely absent — the panel renders pixel-identical to today's Phase 3 output, with zero trace of the icon or its bounding box. Same conditional-element precedent already established for the previous-flight card (`03-CONTEXT.md` D-25, reused directly per `05-CONTEXT.md` D-06).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable — this is a passive ambient display, no interactive controls |
| Icon label / caption | **None — icon-only, zero new text introduced by this phase** (D-04). No "LOW", no percentage, no mV value ever rendered on the panel. |
| Empty state (existing, unaffected) | Unchanged from Phase 3: "Watching Runway 3" / "No aircraft detected yet — the display updates the moment one is." — this phase adds the battery icon on top when the reading is low, but does not alter this copy |
| Error state | Not applicable — there is no error-rendering path this phase adds; a missing/malformed battery reading is a firmware/plumbing concern (`05-CONTEXT.md`'s Claude's Discretion), not a new visual error state on the panel |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

---

## Rendering Rules

- **Flat integer-index fills only**, same discipline as every other element in `render.py` — the icon's outline and fill are drawn with plain `ImageDraw.rectangle()` integer-index calls, never composed in RGB and quantized afterward, never using antialiased strokes.
- **No dithering** — unlike the two aircraft illustrations (which are the panel's one deliberate full-color, Floyd-Steinberg-dithered exception), the battery icon is flat, single-ink, exactly like the frame, top labels, and both flight text blocks.
- **Square corners, no rounded-rectangle primitive** — consistent with `draw_frame()`'s plain rectangle outline.
- **Deterministic geometry** — every icon coordinate is a fixed constant derived from `MARGIN`/`HEIGHT`/`WIDTH`, with zero dependency on flight/illustration data (unlike the previous-flight card, whose exact height varies by illustration aspect ratio). This is what makes the bottom-left placement collision-safe against the previous-flight card by construction, not by a runtime check.
- **Integration point:** the new draw call (`draw_battery_icon()` or equivalent) branches into `_build_active_canvas()` (and, per the empty-state colour resolution above, `_build_empty_canvas()`) as one more conditional draw step, alongside the existing frame/labels/illustration/text calls — same integration point `05-CONTEXT.md`'s Integration Points section already names.
- **Preview before commit:** generate a real `render.py --preview` PNG showing the icon composited against both the Blue and Green backgrounds (and, given the empty-state colour resolution above, the White background too) before treating any of this document's pixel values as final — same discipline Phase 3's D-20/D-21/D-22 established, explicitly requested again in `05-CONTEXT.md`'s Claude's Discretion notes.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none | not applicable — no shadcn, no component registry, no React |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (zero new text confirmed — icon-only per D-04; existing empty-state copy confirmed unchanged)
- [ ] Dimension 2 Visuals: PASS (bottom-left zone, exact bounding boxes, square corners, fixed-fill glyph shape all match this document; collision-checked against frame and previous-flight card)
- [ ] Dimension 3 Color: PASS (White/Ivory ink for departing/arriving reusing `STATE_INK`; Black ink for empty state reusing existing empty-state ink; D-07's background non-interference confirmed; D-12's Yellow reservation correctly retired)
- [ ] Dimension 4 Typography: PASS (not applicable — no text in this element, confirmed intentional)
- [ ] Dimension 5 Spacing: PASS (all new constants derived from the existing `MARGIN`/`HEIGHT`/`WIDTH` constants, no ad hoc magic numbers unaccounted for)
- [ ] Dimension 6 Registry Safety: PASS (not applicable, unchanged)

**Approval:** pending
