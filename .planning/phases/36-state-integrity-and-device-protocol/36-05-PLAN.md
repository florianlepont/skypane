---
phase: 36-state-integrity-and-device-protocol
plan: 05
type: execute
wave: 2
depends_on: [36-01, 36-02]
files_modified:
  - server/plane/calendar_rules.py
  - server/notify.py
  - server/test_calendar_rules.py
  - server/test_notify.py
autonomous: true
requirements: [INT-02, INT-07, INT-12, INT-14]

must_haves:
  truths:
    - "The calendar fetch (every redirect hop) and the ntfy POST connect through http_fetch.pinned_request: the socket goes to an address that was checked, TLS verifies the URL hostname"
    - "fetch_ics has one total deadline across all hops (CALENDAR_FETCH_DEADLINE_S) checked between chunks and before each hop; the notify POST has NOTIFY_DEADLINE_S"
    - "refresh_calendar_registry does not hold calendar_rules.lock during the network fetch: a save_calendar_url() from another thread completes while a fetch is blocked"
    - "A URL saved or cleared during a fetch wins: the stale result is not written"
    - "last_synced_at is derived from the injected now, never datetime.now()"
    - "calendar_rules.json and calendar_url.secret are written through atomic_io (secret 0600 from creation); _calendar_registry_lock delegates to atomic_io.exclusive_lock"
  artifacts:
    - path: "server/plane/calendar_rules.py"
      provides: "pinned default transport, total deadline, split lock in refresh, injected clock, atomic writes"
      contains: "pinned_request"
    - path: "server/notify.py"
      provides: "pinned, deadline-bounded POST, no redirects"
      contains: "pinned_request"
  key_links:
    - from: "calendar_rules.default_calendar_transport"
      to: "http_fetch.pinned_request"
      via: "GET with the per-hop timeout clamped to the time left"
      pattern: "http_fetch\\.pinned_request"
    - from: "notify.default_notify_transport"
      to: "http_fetch.pinned_request"
      via: "POST, a 3xx is returned and treated as failure"
      pattern: "http_fetch\\.pinned_request"
    - from: "calendar_rules._calendar_registry_lock"
      to: "atomic_io.exclusive_lock"
      via: "delegation"
      pattern: "atomic_io\\.exclusive_lock"
---

<objective>
Close the SSRF check-then-connect gap on the two user-supplied outbound URLs (INT-14: pin, as
decided in CONTEXT, rather than only correcting the docstring), bound both calls by a total
deadline (INT-07), stop holding the calendar registry lock across the network fetch and use the
injected clock (INT-12), and move the calendar writers onto `atomic_io` (INT-02).

Purpose: today `_host_is_safe` checks DNS answers that `requests`/`urllib` do not reuse at
connect time, a slow feed can stall a cycle, and a companion calendar save can wait up to the
fetch time behind the poll's lock.
Output: `server/plane/calendar_rules.py`, `server/notify.py` and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-state-integrity-and-device-protocol/36-CONTEXT.md
@.planning/phases/36-state-integrity-and-device-protocol/36-RESEARCH.md
@.claude/CLAUDE.md

<conventions>
- English why-comments, no plan/ticket/phase IDs in code (`scripts/check_comment_history.py check`).
- No test reaches the network; pinned-path tests use the http_fetch seams (fake resolver via
  `socket.getaddrinfo` monkeypatch as `server/test_notify.py` already does, fake
  `socket.create_connection` and a fake SSL context via monkeypatching the attributes
  `http_fetch` looks up at call time). No test reads source text.
- Keep the names companion tests patch: `calendar_rules.default_calendar_transport` (replaced by
  name in `companion/test_companion_app_helpers.py` `stubbed_calendar_transport`) keeps the
  signature `(url, timeout)`, and `fetch_ics` keeps looking it up at call time.
- Never log a URL or an exception string, only the exception type (existing rule in both modules).
</conventions>

<interfaces>
From server/http_fetch.py (36-02): `pinned_request(method, url, *, headers=None, body=None,
timeout, deadline_s, resolver=None, create_connection=None, ssl_context=None, clock=None) ->
PinnedResponse` (`status_code`, `status`, `headers.get`, `is_redirect`, `iter_content`,
`getcode`, `close`), `address_is_public(ip_text)`, `resolve_public_addresses(hostname, port,
resolver=None)`, `DeadlineExceeded`, `UnsafeDestination`.
From server/atomic_io.py (36-01): `atomic_write`, `staged_write(path, data, mode=None)` (yields
`commit()`), `exclusive_lock(lock_path, timeout_s, blocking=True)`, `LockBusy(TimeoutError)`.

Current calendar_rules symbols (re-locate by name): `USER_AGENT`,
`CALENDAR_FETCH_TIMEOUT_S = 10.0`, `CALENDAR_MAX_RESPONSE_BYTES`, `CALENDAR_MAX_REDIRECTS`,
`FETCH_OK`, `FETCH_SKIPPED_UNCONFIGURED`, `FETCH_SKIPPED_THROTTLED`, `FETCH_REJECTED_URL`,
`FETCH_FAILED`, `_WRITE_LOCK`, `CALENDAR_REGISTRY_LOCK_TIMEOUT_S`,
`CALENDAR_REGISTRY_LOCK_POLL_S`, `_calendar_registry_lock(state_dir)`,
`write_calendar_registry(state_dir, entries, last_attempt_at, last_synced_at, now=None)`,
`save_calendar_url(state_dir, value, now=None)`, `configured_calendar_url(state_dir)`,
`load_calendar_registry(state_dir, now=None)`, `calendar_fetch_is_due`, `_address_is_public`,
`_host_is_safe`, `_url_is_safe`, `default_calendar_transport(url, timeout)`,
`fetch_ics(url, timeout=None, transport=None, max_redirects=None, max_bytes=None)`,
`refresh_calendar_registry(state_dir, now, transport=None, min_interval_s=None)` (`now` is
epoch seconds from `poll_loop.now_s()`).
Current notify symbols: `_NoRedirectHandler`, `_NO_REDIRECT_OPENER`,
`default_notify_transport(url, title, body, timeout)`, `send_notification(topic_url, title,
body, timeout=5, transport=None)`, `_response_status`.

New: `CALENDAR_FETCH_TIMEOUT_S` becomes 5.0 (per connect/read); `CALENDAR_FETCH_DEADLINE_S =
10.0` (all hops); `FETCH_SUPERSEDED = "fetch_superseded"`; `notify.NOTIFY_DEADLINE_S = 5.0`.
Worst case per call = deadline + one read timeout: calendar 15 s, notify 10 s (these feed the
90 s unit budget: 2 x 13 s providers + 1.1 s + 13 s adsbdb + 15 s calendar + 2 x 10 s notify
is about 75 s).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calendar pinned transport, total deadline, atomic writes, lock delegation (INT-14, INT-07, INT-02)</name>
  <files>server/plane/calendar_rules.py, server/test_calendar_rules.py</files>
  <read_first>
    - server/plane/calendar_rules.py (imports and constants; `_calendar_registry_lock`; `write_calendar_registry`; `save_calendar_url`; `_address_is_public` through `fetch_ics`)
    - server/http_fetch.py and server/atomic_io.py
    - server/test_calendar_rules.py (`_FakeCalendarResponse`, `make_calendar_transport`, the `_url_is_safe` and `fetch_ics` tests)
    - companion/test_companion_app_helpers.py (`stubbed_calendar_transport`, `fake_public_hostname`: read only)
  </read_first>
  <behavior>
    - Default transport path (no transport injected): socket.getaddrinfo answers a public address first and 127.0.0.1 afterwards; the captured create_connection address is the public one, the SSL server_hostname is the feed hostname, and the parsed body is returned
    - A redirect hop to a hostname whose (single) resolution includes a private address is refused without a connection to it
    - A fake transport whose iter_content yields chunks while an injected clock advances 3 s per chunk: fetch_ics returns None once CALENDAR_FETCH_DEADLINE_S has passed, logging only the exception/deadline type
    - The timeout handed to the transport on a later hop is min(CALENDAR_FETCH_TIMEOUT_S, time left)
    - write_calendar_registry and save_calendar_url leave no '*.tmp'; calendar_url.secret is 0600 and was 0600 at rename time; a failed registry erase still leaves the old secret and registry untouched (existing property, now through staged_write)
    - _calendar_registry_lock still raises TimeoutError when a child process holds calendar_rules.lock past the timeout
    - Existing _url_is_safe / fetch_ics tests stay green unchanged
  </behavior>
  <action>
RED then GREEN. (1) `_address_is_public` delegates to `http_fetch.address_is_public`. Correct
the `_host_is_safe` docstring: it is an early refusal only; the protection against a changed
answer is that the real connection goes through `http_fetch.pinned_request`, which resolves
once and connects to a checked address. (2) `default_calendar_transport(url, timeout)` returns
`http_fetch.pinned_request("GET", url, headers={"User-Agent": USER_AGENT}, timeout=timeout,
deadline_s=CALENDAR_FETCH_DEADLINE_S)`. (3) `fetch_ics` gains `deadline_s=None` (default
`CALENDAR_FETCH_DEADLINE_S`) and `clock=None` (default `time.monotonic`): one deadline for the
whole call, checked before each hop and after each chunk (returns None when passed), and each
transport call receives `min(timeout, time left)`. (4) `_calendar_registry_lock` delegates to
`atomic_io.exclusive_lock(_calendar_registry_lock_path(state_dir),
CALENDAR_REGISTRY_LOCK_TIMEOUT_S)`, keeping its name and `TimeoutError` contract; remove the
now-unused local `fcntl`/`errno` handling and `CALENDAR_REGISTRY_LOCK_POLL_S` if nothing else
uses them (grep tests first). (5) `write_calendar_registry` writes through
`atomic_io.atomic_write`; `save_calendar_url`'s set/replace branch uses
`atomic_io.staged_write(path, normalised_url, mode=0o600)`: erase the registry inside the block
and call `commit()` only if the erase succeeded, preserving today's order and failure
properties. Keep `requests` imported only if still used.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_calendar_rules.py && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0
    - `grep -cE 'getpid|get_ident\(\)' server/plane/calendar_rules.py` == 0 and `grep -c "requests.get(" server/plane/calendar_rules.py` == 0
    - `grep -c "http_fetch.pinned_request" server/plane/calendar_rules.py` == 1 and `grep -c "atomic_io.exclusive_lock" server/plane/calendar_rules.py` == 1
  </acceptance_criteria>
  <done>The calendar fetch is pinned and deadline-bounded; its writers and lock use atomic_io.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: refresh_calendar_registry without the lock across the fetch, injected clock (INT-12)</name>
  <files>server/plane/calendar_rules.py, server/test_calendar_rules.py</files>
  <read_first>
    - server/plane/calendar_rules.py (`refresh_calendar_registry` in full, `calendar_fetch_is_due`, `configured_calendar_url`)
    - companion/app.py (the two calendar handlers that call `refresh_calendar_registry` and compare with `FETCH_OK`: read only, to confirm every caller only tests `== FETCH_OK`)
    - server/poll_loop.py (`run_once`'s `refresh_calendar_registry` call: read only)
  </read_first>
  <behavior>
    - refresh with now=1_800_000_000.0 and a fake transport: last_synced_at == datetime.fromtimestamp(1_800_000_000, timezone.utc).isoformat(timespec="seconds"), on disk and in the returned registry
    - A fake transport blocks on a threading.Event; while the refresh thread waits in it, save_calendar_url(state_dir, <other https URL>) from the main thread returns True within 2 s; after the event is set, refresh returns FETCH_SUPERSEDED, and the registry on disk still has the entries the save left (empty), not the stale fetch result
    - Same, but the URL is cleared (CLEAR_CALENDAR_URL) during the fetch: no registry write from the refresh, the calendar stays unconfigured
    - last_attempt_at is recorded before the fetch: a second refresh started during the blocked fetch (normal throttle) returns FETCH_SKIPPED_THROTTLED without calling its transport
    - A failing fetch (transport raises) keeps the previous entries and last_synced_at, with last_attempt_at == now (existing property)
  </behavior>
  <action>
RED then GREEN. Restructure `refresh_calendar_registry` into three steps, keeping the outer
never-raise `try`: (a) under `_calendar_registry_lock`: load, read the configured URL, return
`FETCH_SKIPPED_UNCONFIGURED`/`FETCH_SKIPPED_THROTTLED` as today, else record the attempt by
`write_calendar_registry(state_dir, registry["entries"], now, registry["last_synced_at"],
now=now)`; release. (b) Without any lock: `fetch_ics`, then parse and window. (c) Under the
lock again: reload the registry and re-read the configured URL; if it differs from the URL
fetched (changed or cleared), write nothing and return `(FETCH_SUPERSEDED, reloaded)`; on a
failed fetch return `(FETCH_FAILED, reloaded)` (the attempt is already recorded); on success
write the windowed entries with `last_synced_at = datetime.fromtimestamp(now,
timezone.utc).isoformat(timespec="seconds")` and return as today. Keep the single result log
line (never the URL). Add `FETCH_SUPERSEDED` next to the other result codes and document that
callers compare with `FETCH_OK` only. Update the docstring and the comment that explained why the
whole sequence ran under the lock (now: why the fetch runs outside it and why the URL is
re-checked). Lock order stays poll.lock then calendar_rules.lock; nothing here takes poll.lock.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_calendar_rules.py companion/test_companion_app_05.py -k "calendar or refresh" && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0
    - `sed -n '/^def refresh_calendar_registry/,/^def match_calendar_theme/p' server/plane/calendar_rules.py | grep -c "datetime.now("` == 0
    - `grep -c 'FETCH_SUPERSEDED = "fetch_superseded"' server/plane/calendar_rules.py` == 1
  </acceptance_criteria>
  <done>A companion save never waits for a feed download, a stale fetch never overwrites a newer URL, and the sync time comes from the injected clock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: notify through the pinned primitive (INT-14, INT-07)</name>
  <files>server/notify.py, server/test_notify.py</files>
  <read_first>
    - server/notify.py (in full; 130 lines)
    - server/test_notify.py (tests that patch `_NO_REDIRECT_OPENER` or build `addinfourl` responses; the getaddrinfo fake)
    - server/http_fetch.py
  </read_first>
  <behavior>
    - Default transport: getaddrinfo answers public first and private afterwards; the captured create_connection address is the public one, server_hostname is the topic hostname, the request line is "POST /<topic> HTTP/1.1", the body bytes and the Title header are sent; a 200 makes send_notification return True
    - A 302 to 169.254.169.254 makes send_notification return False and no second connection is opened
    - A topic hostname resolving to a private address: send_notification returns False and create_connection is never called
    - A response whose status line arrives only after NOTIFY_DEADLINE_S on the injected clock: False, logging only the exception type
    - Tests that inject `transport=` keep passing unchanged
  </behavior>
  <action>
RED then GREEN. Replace `_NoRedirectHandler`, `_NO_REDIRECT_OPENER` and the urllib request
with `default_notify_transport(url, title, body, timeout)` returning
`http_fetch.pinned_request("POST", url, headers={"Title": title, "Content-Type": "text/plain;
charset=utf-8", "User-Agent": calendar_rules.USER_AGENT}, body=body.encode("utf-8"),
timeout=timeout, deadline_s=NOTIFY_DEADLINE_S)`. A 3xx is returned unfollowed and
`send_notification`'s existing 2xx check turns it into False. Keep `_url_is_safe` as the early
refusal in `send_notification`. Rewrite the module docstring accordingly (pinned connection,
no redirects because the primitive never follows them). Rewrite the tests that patched
`_NO_REDIRECT_OPENER` to use the http_fetch seams; do not weaken what they assert (redirect never
followed, internal target never contacted).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_notify.py server/test_calendar_rules.py && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - pytest and the full suite exit 0
    - `grep -cE "urllib\.request|_NO_REDIRECT_OPENER" server/notify.py` == 0 and `grep -c "http_fetch.pinned_request" server/notify.py` == 1
  </acceptance_criteria>
  <done>Push notifications connect only to a checked public address, verify TLS against the hostname, never follow redirects and finish within their deadline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-entered calendar URL / ntfy topic -> VPS outbound | the VPS can reach loopback, the private network and the provider metadata address |
| calendar feed response | untrusted size, pace and redirect targets |
| companion thread <-> poll process | both touch calendar_rules.json under calendar_rules.lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-23 | Elevation (SSRF / DNS rebinding) | `default_calendar_transport`, `default_notify_transport` | mitigate | `http_fetch.pinned_request`: one resolution, all answers public, connect to a checked address; tests with a rebinding resolver |
| T-36-24 | Elevation (SSRF via redirect) | `fetch_ics` hops, notify | mitigate | each calendar hop is re-checked and re-pinned; notify never follows a 3xx |
| T-36-25 | Spoofing | TLS | mitigate | default context, `server_hostname` = URL hostname |
| T-36-26 | Denial of service | slow feed / slow ntfy | mitigate | total deadline across hops (10 s + one 5 s read) and 5 s for notify |
| T-36-27 | Information disclosure | calendar secret file | mitigate | `staged_write(mode=0o600)`: 0600 from creation, test stats at rename |
| T-36-28 | Tampering (stale overwrite) | `refresh_calendar_registry` | mitigate | re-read the configured URL under the lock after the fetch; mismatch writes nothing |
| T-36-29 | Information disclosure (logs) | fetch/send failures | mitigate | exception type only, never URL or message (existing rule kept, asserted in tests via captured stderr) |
</threat_model>

<verification>
- `server/.venv/bin/python -m pytest -q server/test_calendar_rules.py server/test_notify.py` green
- `./scripts/run-all-tests.sh` green (companion calendar tests use the unchanged `default_calendar_transport` seam)
- ruff and the comment guard exit 0
</verification>

<success_criteria>
Both user-supplied outbound URLs are fetched through a pinned, deadline-bounded connection; the
calendar lock is never held across the network; `last_synced_at` follows the injected clock.
</success_criteria>

<output>
Create `.planning/phases/36-state-integrity-and-device-protocol/36-05-SUMMARY.md` when done. Record
the INT-14 decision (pin) and that `_url_is_safe` stays in calendar_rules until Phase 39 ARC-03.
</output>
