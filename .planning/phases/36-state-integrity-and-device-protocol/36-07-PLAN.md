---
phase: 36-state-integrity-and-device-protocol
plan: 07
type: execute
wave: 3
depends_on: [36-01, 36-05, 36-06]
files_modified:
  - server/poll_loop.py
  - companion/app.py
  - server/test_poll_lock.py
  - server/test_poll_loop.py
  - companion/test_poll_now_lock.py
autonomous: true
requirements: [INT-01, INT-02, INT-07, INT-08, INT-11, INT-13]

must_haves:
  truths:
    - "Only one poll cycle runs at a time across processes: run_once() holds exclusive_lock(<state_dir>/poll.lock) for its whole body"
    - "The reproduction (two OS processes x 200 locked read-increment-save cycles through poll_loop's lock and save_poll_state) ends at 400 with zero exceptions, and the same test failed on the pre-change code (recorded in SUMMARY)"
    - "The systemd oneshot waits up to POLL_LOCK_WAIT_S for the lock, then exits 1 with a clear message; the companion's POST /poll-now never waits: a lock held by another process gives the existing 'already running' flash"
    - "poll_state.json, panel.bin, gallery PNGs and illustration uploads are written through atomic_io / unique temp names; no fixed .tmp name remains in server/poll_loop.py or companion/app.py"
    - "main() prints a full traceback to stdout before returning 1 when a cycle fails"
    - "A cycle that detects an aircraft but queues it updates META_LAST_DETECTION"
    - "run_once passes its injected clock (now_s()) to enrich.resolve_route, so adsbdb cache TTLs follow the test clock"
  artifacts:
    - path: "server/poll_loop.py"
      provides: "poll_cycle_lock, PollBusy, POLL_LOCK_WAIT_S, run_once(lock_timeout_s=...), atomic writes, traceback in main, last-detection fix"
      contains: "poll_cycle_lock"
    - path: "server/test_poll_lock.py"
      provides: "two-process x 200 reproduction"
    - path: "companion/test_poll_now_lock.py"
      provides: "/poll-now with the lock held by another process answers poll_already_running without waiting"
  key_links:
    - from: "companion/app.py _handle_poll_now"
      to: "poll_loop.run_once(..., lock_timeout_s=0)"
      via: "except poll_loop.PollBusy -> FLASH_KEY_POLL_ALREADY_RUNNING"
      pattern: "PollBusy"
    - from: "server/poll_loop.py run_once"
      to: "atomic_io.exclusive_lock(<state_dir>/poll.lock)"
      via: "poll_cycle_lock"
      pattern: "poll\\.lock"
---

<objective>
Serialise poll cycles across processes (INT-01), finish the atomic-write migration in the poll
loop and the companion upload (INT-02), print full tracebacks (INT-11), record queued detections
as detections (INT-13), and wire the injected clock into the adsbdb cache (INT-08 wiring). The
poll unit's `TimeoutStartSec` landed in 36-02; this plan's lock wait stays well below it (INT-07).

Purpose: ROADMAP criterion 1 (two processes x 200 saves, zero exceptions, zero lost updates) and
the rest of criterion 2. Last because it consumes the helpers of every earlier plan.
Output: `server/poll_loop.py`, `companion/app.py`, the reproduction test and the companion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-state-integrity-and-device-protocol/36-CONTEXT.md
@.planning/phases/36-state-integrity-and-device-protocol/36-RESEARCH.md
@.claude/CLAUDE.md

<conventions>
- English why-comments, no plan/ticket/phase IDs in code (`scripts/check_comment_history.py check`).
- Behaviour tests only. Companion tests use `make_app_server` (companion/conftest.py),
  `login` and `http_request` from `test-support/companion_app_server.py`; they must satisfy
  `companion/test_suite_guards.py`.
- Server tests reuse `server/test_poll_loop.py`'s `clock` fixture, `_stub_adsbdb` and
  `_snapshot`. Child processes run `sys.executable -c ...` with `PYTHONPATH` = repo root.
</conventions>

<interfaces>
From server/atomic_io.py (36-01): `atomic_write(path, data, mode=None)`,
`exclusive_lock(lock_path, timeout_s, blocking=True)`, `LockBusy(TimeoutError)`.
From server/plane/enrich.py (36-06): `resolve_route(callsign, cache, transport=None,
timeout=DEFAULT_TIMEOUT, now=None)`.

Current poll_loop symbols (re-locate by name): `now_s()`, `load_poll_state`,
`save_poll_state(state_dir, state)` (fixed `.tmp`), `write_panel_atomic(state_dir, rendered)`
(fixed `.tmp`, returns whether the panel changed), `_record_history(state_dir, flight,
confirmed_state, route_source, route, tracked_runway_id, source_fault, record_event, now_iso,
caddy_log=None, wake_interval_s=_NO_WAKE_EPOCH)` (sets `META_LAST_DETECTION` only if `flight is
not None`), `_save_to_gallery(state_dir, canvas, now_iso)` (PIL save in place),
`run_once(snapshot=None, state_dir=None, geofence=None, caddy_log=None)`, the
`enrich.resolve_route(current_flight.get("callsign"), cache)` call, the held branch
(`elif current_flight is not None:`) that calls `_record_history(state_dir, None, ...)` while
`flight` (this cycle's detection) may be non-None, `build_parser()`, `main(argv=None)`.

Current companion/app.py symbols: `_POLL_LOCK = threading.Lock()` (kept as the in-process fast
path), `FLASH_KEY_POLL_ALREADY_RUNNING`, `_handle_poll_now` (non-blocking `_POLL_LOCK`, cooldown
check, `poll_loop.run_once(state_dir=..., geofence=...)`, `mark_poll_triggered`), the
illustration upload handler that builds `".%s.%d.upload.tmp"` / `".%s.%d.encoded.tmp"` names
from `os.getpid()` in `override_dir` and `os.replace`s the encoded PNG onto
`illustrations.override_path_for_key(key, state_dir)`.

New in poll_loop: `POLL_LOCK_FILENAME = "poll.lock"`, `POLL_LOCK_WAIT_S = 10.0` (a normal cycle
takes a few seconds; the wait plus a worst-case cycle stays under the unit's 90 s),
`class PollBusy(RuntimeError)`, `poll_cycle_lock(state_dir, timeout_s=None)` (context manager:
`os.makedirs(state_dir, exist_ok=True)`, then `atomic_io.exclusive_lock(<state_dir>/poll.lock,
timeout, blocking=timeout != 0)`, translating `LockBusy` into `PollBusy`; `None` means
`POLL_LOCK_WAIT_S`), and `run_once(snapshot=None, state_dir=None, geofence=None,
caddy_log=None, lock_timeout_s=None)`, whose existing body moves unchanged into a private
`_run_once_locked(...)` called inside `poll_cycle_lock`. Lock order: `_POLL_LOCK` (companion
thread lock), then `poll.lock`, then `calendar_rules.lock` (taken inside the cycle by
`refresh_calendar_registry`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cross-process poll lock, reproduction, companion busy path (INT-01)</name>
  <files>server/poll_loop.py, companion/app.py, server/test_poll_lock.py, server/test_poll_loop.py, companion/test_poll_now_lock.py</files>
  <read_first>
    - server/poll_loop.py (imports and bootstrap at the top, `save_poll_state`, `load_poll_state`, `run_once` signature and first lines, `main`)
    - server/atomic_io.py
    - companion/app.py (`_POLL_LOCK` comment block, `_handle_poll_now`, the two calendar handlers that also take `_POLL_LOCK`)
    - companion/test_companion_app_05.py (the overlapping /poll-now test and `test_poll_trigger_cooldown_sequence`, for the flash assertions)
    - companion/conftest.py (`make_app_server`) and test-support/companion_app_server.py (`AppServer.state_dir`, `login`, `http_request`)
  </read_first>
  <behavior>
    - server/test_poll_lock.py: two child processes each run 200 iterations of "with poll_loop.poll_cycle_lock(sd): state = poll_loop.load_poll_state(sd); state['counter'] = state.get('counter', 0) + 1; poll_loop.save_poll_state(sd, state)"; both exit 0 with empty stderr, poll_state.json has counter == 400, and no '*.tmp' remains
    - test_poll_loop.py: with a child process holding poll.lock, run_once(snapshot=..., state_dir=sd, lock_timeout_s=0) raises PollBusy within 0.5 s and creates neither poll_state.json nor panel.bin
    - test_poll_loop.py: a child holds poll.lock for 0.5 s; run_once(snapshot=..., lock_timeout_s=5) waits, then completes normally
    - test_poll_loop.py: with the lock held elsewhere and POLL_LOCK_WAIT_S monkeypatched to 0.2, main(["--state-dir", sd]) returns 1 and stdout names the busy lock
    - companion/test_poll_now_lock.py: make_app_server(fake_providers=True); the test process holds exclusive_lock(<server.state_dir>/poll.lock); POST /poll-now answers 303 with flash=poll_already_running within 3 s; after release, POST /poll-now answers flash=poll_triggered (the busy attempt did not consume the cooldown)
    - The existing overlapping /poll-now test and the cooldown sequence test stay green
  </behavior>
  <action>
RED first, and record the failing run: write `server/test_poll_lock.py` whose child code uses
`getattr(poll_loop, "poll_cycle_lock", None) or (lambda sd: contextlib.nullcontext())` for this
RED run only; run it against the unchanged code and paste the result (lost updates and/or
`FileNotFoundError` on the shared `poll_state.json.tmp`) into the SUMMARY. If the RED run happens
to pass, rerun up to 5 times and record every outcome. Then GREEN: in `poll_loop.py` add the
constants, `PollBusy`, `poll_cycle_lock` and the `lock_timeout_s` parameter per
`<interfaces>`; move `save_poll_state` onto `atomic_io.atomic_write(path, json.dumps(state,
indent=1))` in this task (the reproduction needs unique temp names); the lock is taken before any
state read, including the override/registry priming at the top of the old body. `main()`
catches `PollBusy` first and prints one line, "poll_loop: another poll cycle holds <path>;
this cycle was skipped after <n> s", then returns 1. In `companion/app.py`
`_handle_poll_now`: call `poll_loop.run_once(state_dir=..., geofence=..., lock_timeout_s=0)`
and catch `poll_loop.PollBusy` before the generic `except Exception`, setting
`FLASH_KEY_POLL_ALREADY_RUNNING` and not calling `mark_poll_triggered`; update the `_POLL_LOCK`
comment (in-process fast path; poll.lock covers other processes). Finally remove the
`getattr` fallback from the test so it calls `poll_loop.poll_cycle_lock` directly.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_poll_lock.py server/test_poll_loop.py companion/test_poll_now_lock.py companion/test_companion_app_05.py && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0; `server/test_poll_lock.py` passes 5 runs in a row (`for i in 1 2 3 4 5; do server/.venv/bin/python -m pytest -q server/test_poll_lock.py || exit 1; done`)
    - SUMMARY contains the RED output of the reproduction against the pre-change code
    - `grep -c "getattr(poll_loop" server/test_poll_lock.py` == 0
    - `grep -c "PollBusy" companion/app.py` >= 1 and `grep -c "_POLL_LOCK = threading.Lock()" companion/app.py` == 1
  </acceptance_criteria>
  <done>Two processes can no longer interleave poll cycles or lose a poll_state update; the companion never blocks a request thread on the lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: remaining atomic writes, traceback, last detection, clock wiring (INT-02, INT-11, INT-13, INT-08)</name>
  <files>server/poll_loop.py, companion/app.py, server/test_poll_loop.py</files>
  <read_first>
    - server/poll_loop.py (`write_panel_atomic`, `_record_history`, `_save_to_gallery`, `_prune_gallery`, the held branch and the `enrich.resolve_route` call in `_run_once_locked`, `main`)
    - companion/app.py (the illustration upload handler with the pid-named temps)
    - server/history_db.py (`META_LAST_DETECTION`, `get_meta`, `utc_now_iso`)
    - server/test_poll_loop.py (`clock`, `_stub_adsbdb`, `_snapshot`, existing gallery and history tests; grep for `panel.bin.tmp` or `poll_state.json.tmp` assumptions)
    - companion tests that exercise the illustration upload (grep "upload" in companion/test_*.py)
  </read_first>
  <behavior>
    - After a cycle that changes the panel: panel.bin holds the packed bytes, the gallery holds a PNG that PIL opens, and no '*.tmp' exists in the state dir or gallery/
    - atomic_io.atomic_write forced to raise inside _save_to_gallery: the cycle still completes (existing containment) and no '*.tmp' remains
    - main() with run_once monkeypatched to raise ValueError("boom"): returns 1, and captured stdout contains "Traceback (most recent call last)" and "ValueError: boom"
    - Cycle 1 detects A (displayed); cycle 2 within MIN_ADVANCE_INTERVAL_S detects B, which is queued (held branch); with history_db.utc_now_iso monkeypatched to return distinct values per cycle, META_LAST_DETECTION equals cycle 2's value
    - A held cycle with no detection leaves META_LAST_DETECTION unchanged
    - With the clock fixture at T, a cycle that enriches a callsign stores cached_at == T in poll_state's enrichment_cache (proves now_s() reaches resolve_route)
    - Illustration upload through the companion still replaces the override (existing upload tests green) and leaves no '*.tmp' in the override directory
  </behavior>
  <action>
RED then GREEN. `write_panel_atomic`: keep the unchanged-hash early return, then
`atomic_io.atomic_write(panel_path, rendered)`. `_save_to_gallery`: render the PNG into an
`io.BytesIO` and `atomic_io.atomic_write(path, buffer.getvalue())`, inside the existing
catch-and-log. `_record_history` gains a keyword `detected=False`; set `META_LAST_DETECTION`
when `flight is not None or detected`; the held branch passes `detected=flight is not None`.
Pass `now=now_s()` to `enrich.resolve_route`. `main()`: in the generic `except Exception`, print
the existing one-line summary, then `traceback.print_exc(file=sys.stdout)` (journald captures
stdout), then return 1. Update the `save_poll_state`/`write_panel_atomic` docstrings (they cite
byos's old `.tmp` scheme). In `companion/app.py` illustration upload: create the raw upload temp
with `tempfile.mkstemp(dir=override_dir, prefix="." + key + ".upload.", suffix=".tmp")` (the suffix stays exactly ".tmp" so the phase-wide fixed-name grep below stays meaningful) and
write the payload through the returned fd; encode the RGBA PNG into an `io.BytesIO` and
`atomic_io.atomic_write(override_path, buffer.getvalue())`; the `finally` removes only the raw
temp (initialised to None before the `try`). Keep every redirect and flash unchanged.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_poll_loop.py server/test_poll_lock.py && server/.venv/bin/python -m pytest -q companion -k "upload or illustration or poll" && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - pytest selections and the full suite exit 0; the coverage gate holds
    - `grep -cE '\+ *"\.tmp"|getpid' server/poll_loop.py companion/app.py` reports 0 for each file
    - `grep -c "traceback.print_exc(file=sys.stdout)" server/poll_loop.py` == 1
    - Phase-wide fixed-name check: `git grep -nE '\+ *"\.tmp"|\.tmp"[^)]*getpid|%d\.tmp|"\.[A-Za-z_-]+\.tmp"' -- '*.py' ':(exclude)**/test_*.py' ':(exclude)test-support/**'` prints nothing (on 9eb92a6 it prints the 16 old sites)
  </acceptance_criteria>
  <done>Every state write in the poll loop and the companion upload is atomic with a unique temp; failures log a traceback; queued detections count as detections; the adsbdb cache follows the injected clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| companion process <-> poll oneshot process | both run `run_once()` against one state/ directory |
| browser -> companion upload | authenticated owner uploads an illustration file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-35 | Tampering (lost update, torn file) | `run_once` from two processes | mitigate | `poll_cycle_lock` over `poll.lock` for the whole cycle; two-process x 200 reproduction |
| T-36-36 | Denial of service | companion request thread waiting on the poll lock | mitigate | `lock_timeout_s=0` from `/poll-now`, `PollBusy` -> "already running" flash |
| T-36-37 | Denial of service | oneshot waiting forever | mitigate | `POLL_LOCK_WAIT_S = 10` then exit 1, under the unit's `TimeoutStartSec=90s` |
| T-36-38 | Tampering | upload temp names | mitigate | mkstemp in the override dir (O_EXCL, random) instead of pid names |
| T-36-39 | Information disclosure | traceback in journald | accept | journald is readable by root and the adm group only; tracebacks contain code paths, and callers already avoid putting URLs or secrets in exception messages |
</threat_model>

<verification>
- `server/.venv/bin/python -m pytest -q server/test_poll_lock.py server/test_poll_loop.py companion/test_poll_now_lock.py` green
- `./scripts/run-all-tests.sh` green; ruff and the comment guard exit 0
- The phase-wide fixed-temp-name `git grep` in Task 2 prints nothing. Out of scope, recorded in SUMMARY: `deploy/backup/skypane_backup.py` (per-archive `.partial-*` names, per CONTEXT) and `deploy/activate.sh`'s `.current.tmp` symlink swap (a deploy-time symlink, not a state file; deploys are serialised by the CI deploy job's concurrency group)
</verification>

<success_criteria>
ROADMAP criteria 1 and 2 hold: the two-process x 200 reproduction ends with zero exceptions and
zero lost updates, and no fixed `.tmp` name remains in Python production code. `main()` failures
carry a traceback; queued detections update "Last aircraft detected".
</success_criteria>

<output>
Create `.planning/phases/36-state-integrity-and-device-protocol/36-07-SUMMARY.md` when done. Include
the RED run of the reproduction against the pre-change code, and the out-of-scope `.tmp` names
listed in `<verification>`.
</output>
