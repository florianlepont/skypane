---
phase: 36-state-integrity-and-device-protocol
plan: 04
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - server/device_config.py
  - server/plane/colour_rules.py
  - server/plane/manual_resolutions.py
  - server/test_state_writers.py
  - companion/theme_preview.py
  - companion/illustration_normalize.py
  - companion/test_preview_cache.py
  - deploy/backup/backup_gate.py
  - deploy/tests/test_backup_gate.py
autonomous: true
requirements: [INT-02, INT-03, INT-04]

must_haves:
  truths:
    - "save_device_config() runs its load-merge-write under a module threading.Lock and exclusive_lock(<state_dir>/device_config.lock); concurrent saves of disjoint fields from many threads lose no field"
    - "device_config.json, colour_rules.json and manual_resolutions.json are written through atomic_io.atomic_write (unique temp names, no leftovers), with unchanged JSON format and file mode"
    - "Two threads rendering the same cold theme preview never raise and leave one PNG; stale-signature files for that theme+event are removed; the preview cache directory is bounded to THEME_PREVIEW_CACHE_MAX_FILES, oldest first"
    - "_cached_normalized_png_bytes has a finite lru_cache maxsize (128)"
    - "backup_gate ack writes last-pull through a unique mkstemp temp in pulled_dir, mode 0644, and stays self-contained (no server import)"
  artifacts:
    - path: "server/device_config.py"
      provides: "locked save_device_config"
      contains: "exclusive_lock"
    - path: "companion/theme_preview.py"
      provides: "atomic, pruned, bounded preview cache"
      contains: "THEME_PREVIEW_CACHE_MAX_FILES"
    - path: "server/test_state_writers.py"
      provides: "no-lost-update test for save_device_config and no-leftover tests for the registries"
    - path: "companion/test_preview_cache.py"
      provides: "concurrent cold preview, prune and bound tests, lru bound"
  key_links:
    - from: "server/device_config.py save_device_config"
      to: "server/atomic_io.py"
      via: "exclusive_lock + atomic_write"
      pattern: "atomic_io\\.(exclusive_lock|atomic_write)"
    - from: "companion/theme_preview.py cached_preview_bytes"
      to: "server/atomic_io.py atomic_write"
      via: "unique temp write, then prune"
      pattern: "atomic_io\\.atomic_write"
---

<objective>
Migrate the configuration and cache writers onto `server/atomic_io.py`: `save_device_config`
gets a thread lock plus a file lock (INT-03); the colour-rule and manual-resolution registries
and `device_config.json` get unique temp names (INT-02); the theme preview cache is written
atomically, pruned and bounded, and the illustration cache is bounded (INT-04); the backup
gate's fixed `.last-pull.tmp` becomes a unique local temp (INT-02).

Purpose: success criterion 2 for these files, and no lost settings update when two browser
tabs save at once.
Output: the six production files above and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-state-integrity-and-device-protocol/36-CONTEXT.md
@.planning/phases/36-state-integrity-and-device-protocol/36-RESEARCH.md
@.claude/CLAUDE.md

<conventions>
- English why-comments, no plan/ticket/phase IDs in code (`scripts/check_comment_history.py check`).
- Tests assert behaviour, never source text. `companion/test_suite_guards.py` scans every
  `companion/test_*.py`: the new companion test must not read production files as text, use
  `inspect`/`ast`, read `__doc__`, or redefine the shared `Harness`/`http_request` machinery.
- `deploy/backup/backup_gate.py` runs as `/usr/bin/python3` outside the venv and the release:
  it must not import `server.*`.
</conventions>

<interfaces>
From server/atomic_io.py (36-01): `atomic_write(path, data, mode=None)`,
`exclusive_lock(lock_path, timeout_s, blocking=True)`, `LockBusy(TimeoutError)`,
`DEFAULT_FILE_MODE`.

Current symbols to re-locate on the tree:
- `server/device_config.py`: `save_device_config(state_dir, theme=None, ..., notifications=None)`
  (load current, merge, `path + ".tmp"` write near its end), `device_config_path`,
  `load_device_config`. Callers: `companion/app.py` and `companion/pages/config_page.py`
  (unchanged by this plan).
- `server/plane/colour_rules.py`: `_WRITE_LOCK`, `add_rule`, `delete_rule` (pid+tid temp, return
  `ADD_FAILED` / `False` on failure).
- `server/plane/manual_resolutions.py`: `_WRITE_LOCK`, `add_entry`, `delete_entry` (same shape);
  its module docstring describes the old temp scheme.
- `companion/theme_preview.py`: `THEME_PREVIEW_CACHE_DIRNAME`, `preview_signature` (12 hex),
  `cache_dir`, `cache_path` (file name `<theme>-<event id or "sample">-<signature>.png`),
  `cached_preview_bytes(state_dir, theme_id, live_event=None)`, `preview_png_bytes`.
- `companion/illustration_normalize.py`: `_cached_normalized_png_bytes(path, mtime_ns)` under
  `functools.lru_cache(maxsize=None)`, `cached_normalized_png_bytes(path)`.
- `deploy/backup/backup_gate.py`: `_cmd_ack(archive_dir, pulled_dir, name)`.

New constants: `device_config.DEVICE_CONFIG_LOCK_FILENAME = "device_config.lock"`,
`device_config.DEVICE_CONFIG_LOCK_TIMEOUT_S = 10.0`,
`theme_preview.THEME_PREVIEW_CACHE_MAX_FILES = 64`,
`illustration_normalize.NORMALIZED_CACHE_MAX_ENTRIES = 128` (above the 43 vendored assets plus
overrides).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: device_config lock + atomic registries (INT-03, INT-02)</name>
  <files>server/device_config.py, server/plane/colour_rules.py, server/plane/manual_resolutions.py, server/test_state_writers.py</files>
  <read_first>
    - server/device_config.py (`save_device_config` in full, imports, `device_config_path`)
    - server/plane/colour_rules.py (`add_rule`, `delete_rule`, `_WRITE_LOCK`)
    - server/plane/manual_resolutions.py (`add_entry`, `delete_entry`, `_WRITE_LOCK`, module docstring)
    - server/atomic_io.py
    - server/test_colour_rules.py and server/test_manual_resolutions.py (grep for `.tmp` or `getpid` assumptions)
  </read_first>
  <behavior>
    - 12 threads each call save_device_config(state_dir, <one distinct field>=<value>) 20 times (theme, tracked_runway, led_enabled, wake_interval_s, display_enabled, quiet_hours_* ...): no exception, and the final file holds every thread's last value (today a lost update is possible)
    - A child process holding exclusive_lock(<state_dir>/device_config.lock) makes save_device_config wait; once released, the save lands (proves the file lock is taken)
    - After save_device_config, add_rule/delete_rule and add_entry/delete_entry, the state dir holds no '*.tmp' file and each JSON file parses with the same content as before the change
    - atomic_io.atomic_write forced to raise (monkeypatched in the caller's module): add_rule returns ADD_FAILED, delete_rule returns False, add_entry returns ADD_FAILED, the old file is intact; save_device_config propagates the OSError as today
    - device_config.json mode after a save equals atomic_io.DEFAULT_FILE_MODE
  </behavior>
  <action>
RED then GREEN. In `server/device_config.py` add a module `_SAVE_LOCK = threading.Lock()` and
wrap the whole load-merge-write of `save_device_config` in `with _SAVE_LOCK:` then
`with atomic_io.exclusive_lock(os.path.join(state_dir, DEVICE_CONFIG_LOCK_FILENAME),
DEVICE_CONFIG_LOCK_TIMEOUT_S):` (create `state_dir` first); write with
`atomic_io.atomic_write(path, json.dumps(new_config, indent=1))`. A `LockBusy` propagates like
the `OSError` it replaces (a `TimeoutError` is an `OSError`), so companion callers keep their
error path; check that in `companion/app.py` and `companion/pages/config_page.py` (read only).
In `colour_rules` and `manual_resolutions` replace each pid+tid temp block with
`atomic_io.atomic_write(path, json.dumps(registry, indent=1))` inside the existing `try`, keeping
the existing return values; update docstrings that describe the old temp naming. Import as
`from server import atomic_io`.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q server/test_state_writers.py server/test_colour_rules.py server/test_manual_resolutions.py && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0
    - `grep -cE 'getpid|get_ident|\+ *"\.tmp"' server/device_config.py server/plane/colour_rules.py server/plane/manual_resolutions.py` reports 0 for each file
    - `grep -c "exclusive_lock" server/device_config.py` >= 1
  </acceptance_criteria>
  <done>Settings saves are serialised across threads and processes; the three registries use unique temp names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: theme preview and illustration caches (INT-04)</name>
  <files>companion/theme_preview.py, companion/illustration_normalize.py, companion/test_preview_cache.py</files>
  <read_first>
    - companion/theme_preview.py (in full; 208 lines)
    - companion/illustration_normalize.py (the cached pair at the end)
    - companion/app.py (the two call sites of `cached_preview_bytes` and `cached_normalized_png_bytes`, read only)
    - companion/conftest.py and companion/test_suite_guards.py (fixture names, rules a new companion test must obey)
  </read_first>
  <behavior>
    - With preview_png_bytes monkeypatched to a fake that waits on a 2-party threading.Barrier then returns fixed PNG bytes, two threads call cached_preview_bytes(state_dir, "white") on a cold cache: both return the bytes, no exception, exactly one "white-sample-*.png" file exists, no '*.tmp' remains
    - Pre-seeded "white-sample-<12 other hex>.png" and "white-5-<12 hex>.png" and "black-sample-<12 hex>.png": after cached_preview_bytes(state_dir, "white"), the stale white-sample file is gone and the other two remain
    - Directory pre-filled with THEME_PREVIEW_CACHE_MAX_FILES + 5 PNGs with increasing mtimes: after one cold write, the number of '*.png' files is THEME_PREVIEW_CACHE_MAX_FILES and the 6 oldest are gone; the file just written remains
    - A cache hit does not prune or write
    - illustration_normalize._cached_normalized_png_bytes.cache_info().maxsize == 128
  </behavior>
  <action>
RED then GREEN. In `cached_preview_bytes`: keep the read-on-hit path; on a miss, render, then
`atomic_io.atomic_write(path, payload)` (replacing the pid-named temp), then
`_prune_preview_cache(directory, keep_path=path)`, which never raises (`OSError` ignored) and
(1) removes `*.png` files whose name is `<same theme>-<same event part>-<12 hex>.png` with a
different signature (match with a regex built from `re.escape` of the prefix plus
`[0-9a-f]{12}\.png`, so theme `a` never matches files of a theme `a-5`), then (2) if more than
`THEME_PREVIEW_CACHE_MAX_FILES` `*.png` files not starting with "." remain, removes the oldest by
mtime, never `keep_path`. Update the docstring (unique temp, prune, bound). In
`illustration_normalize.py` add `NORMALIZED_CACHE_MAX_ENTRIES = 128` and use
`functools.lru_cache(maxsize=NORMALIZED_CACHE_MAX_ENTRIES)`; update its comment (bounded, why
128). Import `from server import atomic_io`.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q companion/test_preview_cache.py companion/test_suite_guards.py && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0 (the guard accepts the new module)
    - `grep -c "getpid" companion/theme_preview.py` == 0 and `grep -c "maxsize=None" companion/illustration_normalize.py` == 0
  </acceptance_criteria>
  <done>Preview writes are race-free, stale previews are removed, both caches are bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: backup gate ack through a unique temp (INT-02)</name>
  <files>deploy/backup/backup_gate.py, deploy/tests/test_backup_gate.py</files>
  <read_first>
    - deploy/backup/backup_gate.py (`_cmd_ack`, imports)
    - deploy/tests/test_backup_gate.py (`test_ack_valid_archive_writes_marker_atomically` asserts the old fixed temp name is absent)
  </read_first>
  <behavior>
    - A valid ack writes last-pull with the archive name, mode 0644, and pulled_dir holds no '*.tmp' afterwards
    - Two acks run concurrently as subprocesses (two different valid archives): both exit 0, last-pull holds one of the two names in full, no '*.tmp' remains
  </behavior>
  <action>
In `_cmd_ack` create the temp with `tempfile.mkstemp(dir=pulled_dir, prefix=".last-pull.",
suffix=".tmp")`, `os.fchmod(fd, 0o644)` before writing, write the name and newline, flush,
fsync, close, `os.replace` to `last-pull`; on `OSError` unlink the temp if it exists and keep the
existing `_fail("ack failed: ...")`. Stdlib only, no `server` import. In the test, replace the
`.last-pull.tmp` absence check with "no file in pulled_dir ends with .tmp" and add the
concurrent-ack test.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q deploy/tests/test_backup_gate.py && server/.venv/bin/ruff check . && server/.venv/bin/python scripts/check_comment_history.py check && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - pytest and the full suite exit 0
    - `grep -c '".last-pull.tmp"' deploy/backup/backup_gate.py` == 0 and `grep -cE "^(from|import) server" deploy/backup/backup_gate.py` == 0
  </acceptance_criteria>
  <done>The gate's ack marker uses a unique temp name and stays self-contained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> companion -> device_config.json | authenticated owner edits settings from several tabs/threads |
| Mac pull job -> backup_gate (SSH forced command) | the only remote writer of `last-pull` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-18 | Tampering (lost update) | `save_device_config` | mitigate | thread lock + `exclusive_lock(device_config.lock)` around load-merge-write; threaded test |
| T-36-19 | Denial of service (disk) | theme preview cache | mitigate | stale-signature prune plus a 64-file bound, oldest first |
| T-36-20 | Denial of service (memory) | illustration lru_cache | mitigate | `maxsize=128` |
| T-36-21 | Tampering | preview cache file names | accept | names are built from a THEMES key, an int event id or "sample", and a hash; no request string reaches a path (unchanged `cache_path` validation) |
| T-36-22 | Denial of service | device_config lock stuck | mitigate | bounded 10 s wait, then an error the companion already reports as a failed save |
</threat_model>

<verification>
- `server/.venv/bin/python -m pytest -q server/test_state_writers.py companion/test_preview_cache.py deploy/tests/test_backup_gate.py` green
- `./scripts/run-all-tests.sh` green; ruff and the comment guard exit 0
</verification>

<success_criteria>
No fixed or pid-only temp name remains in `server/device_config.py`, `server/plane/colour_rules.py`,
`server/plane/manual_resolutions.py`, `companion/theme_preview.py` or
`deploy/backup/backup_gate.py`; concurrent settings saves lose nothing; both companion caches are
bounded.
</success_criteria>

<output>
Create `.planning/phases/36-state-integrity-and-device-protocol/36-04-SUMMARY.md` when done.
</output>
