---
phase: 10-scheduled-quiet-hours
plan: 05
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - companion/pages/config_page.py
  - companion/static/style.css
  - companion/test_config_page.py
  - .claude/skills/sketch-findings-skypane/SKILL.md
autonomous: true
requirements: []   # unmapped backlog phase promoted from SEED-001 — see 10-RESEARCH.md <phase_requirements>
must_haves:
  truths:
    - "The Settings page renders a fourth group, Quiet hours, with an enable checkbox and two time inputs pre-filled from the saved config (D-03/D-04)"
    - "An unchecked enable checkbox still saves edited start/end times — the time inputs are never disabled, dimmed, or ignored (10-UI-SPEC.md's resolution of 10-RESEARCH.md Assumption A1 / Open Question 2)"
    - "An absent quiet_hours_enabled field resolves to False, never to leave-unchanged, matching the LED checkbox's established absent-means-False semantics"
    - "A malformed submitted HH:MM returns the existing generic save-failed flash and leaves the pre-existing device_config.json byte-identical — rejection stays all-or-nothing across every field in the merged form"
    - "Every interpolated current value passes through escape_html()"
    - "The native time-picker indicator follows the site's explicit data-ui-theme override rather than the OS colour scheme"
  artifacts:
    - companion/pages/config_page.py
    - companion/static/style.css
  key_links:
    - "handle_post() writes through the single save_device_config() call the merged Settings form already uses — one form, one write, all-or-nothing rejection"
    - "The saved fields are read back by poll_loop.py (plan 10-04) and, by JSON key name, by byos_server.py (plan 10-03); this page is their only writer"
---

<objective>
Add the Quiet hours fieldset to the companion Settings page: an enable checkbox plus a
start and an end time input, saved through the same merged form and the same single
`save_device_config()` call every other setting already uses.

Purpose: this is the only place a user ever sets the window. `10-UI-SPEC.md` locks the
group's placement, field order, copy, layout and unchecked-checkbox semantics, so this
plan is an implementation of an approved contract, not a design exercise.

Output: `quiet_hours_group()` and its three copy constants, the `render()` and
`handle_post()` extensions, the one required `color-scheme` CSS addition, the
`.settings-checkbox` generalisation now that two groups share that pattern, extended
coverage in `companion/test_config_page.py`, and the matching update to the project's
living design-system skill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-scheduled-quiet-hours/10-CONTEXT.md
@.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md
@.planning/phases/10-scheduled-quiet-hours/10-PATTERNS.md
@companion/pages/config_page.py
</context>

<!-- planner-discipline-allow: led-checkbox -->
<!-- planner-discipline-allow: device_config._HHMM_RE -->

## Artifacts this phase produces

Every symbol below is created by Phase 10 and does not exist in the codebase before it.
Do not treat any of them as drift against existing source.

**companion/pages/config_page.py** (this plan): `QUIET_HOURS_CHECKBOX_VALUE`,
`QUIET_HOURS_SECTION_HEADING`, `QUIET_HOURS_SECTION_CAPTION`, `quiet_hours_group()`, and
the `quiet_hours_enabled`/`quiet_hours_start`/`quiet_hours_end` form field names.
**companion/static/style.css** (this plan): the `.settings-checkbox` class (a rename of
the existing single-consumer checkbox-normalisation class) and three `color-scheme`
declarations.

**server/device_config.py** (plan 10-01, landed in Wave 1): `DEFAULT_QUIET_HOURS_ENABLED`,
`DEFAULT_QUIET_HOURS_START`, `DEFAULT_QUIET_HOURS_END`, `_HHMM_RE`, `QUIET_HOURS_TZ`,
`normalise_quiet_hours_enabled()`, `normalise_quiet_hours_time()`,
`seconds_until_quiet_hours_end()`, `quiet_hours_status()`, the three new
`load_device_config()` return keys and the three new `save_device_config()` keyword
arguments.

**server/plane/render.py** (plan 10-02): `QUIET_HOURS_HEADING_TEXT`,
`QUIET_HOURS_BODY_TEMPLATE`, `_build_quiet_hours_canvas()`, the `quiet_hours_until`
parameter, the `quiet_hours` `--state` choice and `--quiet-hours-until` flag.

**stub-server/byos_server.py** (plan 10-03): `QUIET_HOURS_TZ`, `_HHMM_RE`,
`seconds_until_quiet_hours_end()` (vendored duplicate), `read_quiet_hours()`,
`quiet_hours_sleep_s()`.

**server/poll_loop.py** (plan 10-04): the `poll_state.json` key `quiet_hours_active`
and the `"quiet_hours"` render-state string.

<tasks>

<task type="auto">
  <name>Task 1: Add quiet_hours_group() and wire it through render() and handle_post()</name>
  <files>companion/pages/config_page.py</files>
  <read_first>
    - `companion/pages/config_page.py` — read the whole file. Anchors: lines 34-47
      (`SETTINGS_ROUTE`, `SETTINGS_FORM_ID`, `LED_CHECKBOX_VALUE`), 49-99 (the
      one-caption-per-section rationale and the four `*_SECTION_CAPTION` constants plus
      `LED_SECTION_HEADING` and `DIRTY_SECTION_ATTR`), 348-400 (`led_group()` — the exact
      structural template), 596-671 (`render()`, its `device_cfg.get(...)` reads and the
      merged form's group order), and 674-750 (`handle_post()`, its docstring's
      absent-means-False and all-or-nothing contracts, and its single
      `save_device_config()` call).
    - `.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md` — the
      `## Interaction Contract (Companion Web App)` and `## Copywriting Contract`
      sections. Field order, the vertical stacking rule, the unchecked-checkbox semantics
      and every copy string are locked there; do not re-derive them.
    - `.planning/phases/10-scheduled-quiet-hours/10-PATTERNS.md`, the
      "`companion/pages/config_page.py`" section.
    - `server/device_config.py` as landed by plan 10-01 — the three `DEFAULT_QUIET_HOURS_*`
      constants and `save_device_config()`'s new keyword arguments and `ValueError`
      messages.
  </read_first>
  <action>
Add three module constants beside their LED counterparts:
`QUIET_HOURS_CHECKBOX_VALUE = "on"` (the sole accepted submitted value, defined once and
shared by the markup and the validator so the two cannot drift — same comment shape
`LED_CHECKBOX_VALUE` carries), `QUIET_HOURS_SECTION_HEADING = "Quiet hours"`, and
`QUIET_HOURS_SECTION_CAPTION = "Pauses the frame's wake, poll and display cycle
overnight. Applies on the next scheduled poll, which may now be hours away."` — that
caption text is locked verbatim by 10-UI-SPEC.md's Copywriting Contract. It deliberately
restates the duration caveat the other three captions do not, because this is the one
setting whose wait can stretch from minutes to hours (D-02); it is the reason no
special-cased flash message is added.

Add `quiet_hours_group(current_enabled, current_start, current_end)` immediately after
`led_group()`, following that function's exact structure: a `<div class="theme-status"
%s="%s">` wrapper carrying `DIRTY_SECTION_ATTR` and the escaped heading as its section
name, then `<h2 class="text-heading">`, then `<p class="text-label section-caption">`,
then the controls. No `<fieldset>`/`<legend>` — for the reason `led_group()`'s docstring
already documents (a `<legend>` only has accessible-name semantics inside a `<fieldset>`,
which these sibling groups deliberately do not have).

Controls, in this locked order:
  1. `<label class="settings-checkbox"><input type="checkbox" name="quiet_hours_enabled"
     value="{QUIET_HOURS_CHECKBOX_VALUE}"{checked}> Enable quiet hours</label>`, with
     `checked = " checked" if current_enabled else ""`, exactly as `led_group()` builds
     it. The class is `settings-checkbox`, the generalised name Task 2 introduces — write
     the new markup against the new name and let Task 2 land the CSS and rename the LED
     group's own attribute in the same wave.
  2. `<label>Start <input type="time" name="quiet_hours_start" value="{...}"></label>`
  3. `<label>End <input type="time" name="quiet_hours_end" value="{...}"></label>`
Each on its own line, full width, stacked vertically. Do NOT wrap them in
`.theme-status__row` or any other side-by-side flex row: 10-UI-SPEC.md rejects that
explicitly, both because two native time pickers plus a gap risk wrapping at 320-375px
and because 06.6.4.1 (D-01) just removed this page's two-column grid and this new group
must not reintroduce a side-by-side layout the rest of the page moved away from. Do NOT
add a `disabled` attribute or any dimmed/`.disabled` treatment tied to the checkbox — the
time inputs stay fully interactive whatever its state, because they save independently of
it. Route every interpolated value — the heading, the caption, the checkbox value, and
both current times — through `escape_html()`, matching this file's universal escaping
discipline.

In `render()`, read the three saved values next to the existing three, using the same
`device_cfg.get(key, device_config.DEFAULT_*)` shape: `current_quiet_enabled`,
`current_quiet_start`, `current_quiet_end`. Emit `quiet_hours_group(...)` as the FOURTH
group inside the existing merged `<form action="{SETTINGS_ROUTE}">` — after
`led_group(...)`, before the always-visible bottom "Save settings" button. Add one more
`"%s"` placeholder to the format string and one more argument to the tuple; do not
restructure the form, the dirty bar, or the Poll section.

In `handle_post()`, add `submitted_qh_enabled = form.get("quiet_hours_enabled")` beside
the three existing `form.get(...)` reads, and resolve exactly three shapes and no others,
mirroring `led_enabled` line for line: absent -> `False`; equal to
`QUIET_HOURS_CHECKBOX_VALUE` -> `True`; anything else -> `return FLASH_SAVE_FAILED`. The
absent-means-`False` rule is mandatory for the same reason it is for the LED: an unchecked
HTML checkbox is omitted from the POST body entirely, so carrying it forward would
silently re-enable a curfew the user just turned off.

Read `submitted_qh_start = form.get("quiet_hours_start")` and `submitted_qh_end =
form.get("quiet_hours_end")` and pass them STRAIGHT THROUGH to the existing single
`save_device_config(...)` call as `quiet_hours_start=` / `quiet_hours_end=` — `None` when
absent, which that function carries forward from the current on-disk value, exactly like
`theme`/`tracked_runway`. Do NOT add a separate pre-check against `device_config._HHMM_RE`
here: that is a private name in another module, and `save_device_config()` already
validates both fields strictly and raises `ValueError` before it touches the file, which
this handler's existing `except (ValueError, OSError): return FLASH_SAVE_FAILED` already
maps to the correct generic flash. All-or-nothing rejection is preserved because that
validation happens before any write. Add a short paragraph to `handle_post()`'s docstring
recording this deliberately — a reader must not conclude the time fields were left
unvalidated.

Pass `quiet_hours_enabled=` (the resolved boolean) in the same `save_device_config(...)`
call. Extend the docstring's opening sentence and its "exactly three shapes" paragraph to
name the new fields, and add a sentence recording the locked semantics from
10-UI-SPEC.md: an unchecked checkbox still persists edited start/end times, so a user can
pre-configure a window before ever turning it on. That resolves 10-RESEARCH.md's
Assumption A1 / Open Question 2 in the affirmative — it is a decision, not an oversight.

No new flash key, no new error copy: saving reuses `FLASH_SAVED` ("Saved — will apply on
the frame's next scheduled refresh") and failures reuse `FLASH_SAVE_FAILED` verbatim,
both locked by 10-UI-SPEC.md's Copywriting Contract.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py; echo "exit=$?"</automated>
    <automated>server/.venv/bin/ruff check companion/pages/config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at its current count of 64 (Task 3 adds the new checks and bumps it)
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; h = c.quiet_hours_group(True, '23:00', '07:00'); print('type=\"time\" name=\"quiet_hours_start\" value=\"23:00\"' in h, 'name=\"quiet_hours_end\" value=\"07:00\"' in h, h.count('checked') == 1, 'theme-status__row' not in h, 'disabled' not in h)"` prints `True True True True True`
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import config_page as c; print(c.quiet_hours_group(False, '23:00', '07:00').count('checked'))"` prints `0`
    - `grep -c 'Quiet hours' companion/pages/config_page.py` is at least 1, and `grep -c 'Enable quiet hours' companion/pages/config_page.py` is at least 1
    - `grep -n 'device_config\._HHMM_RE' companion/pages/config_page.py` returns no match — no private cross-module access
    - `server/.venv/bin/ruff check companion/pages/config_page.py` reports no findings
    - `git diff --name-only` after this commit lists exactly `companion/pages/config_page.py`
  </acceptance_criteria>
  <done>
The Settings page renders a fourth Quiet hours group with the locked copy and field order,
and one merged POST persists the enable flag plus both times through the existing single
`save_device_config()` call, rejecting all-or-nothing on any invalid field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generalise the checkbox class and add the color-scheme declarations</name>
  <files>companion/static/style.css, companion/pages/config_page.py, .claude/skills/sketch-findings-skypane/SKILL.md</files>
  <read_first>
    - `companion/static/style.css` — lines 55-70 (the theme-resolution header comment),
      140-200 (the `@media (prefers-color-scheme: dark)` block and the two
      `html[data-ui-theme="light"]` / `html[data-ui-theme="dark"]` override blocks),
      665-695 (the `.visually-hidden` comment that cross-references the checkbox
      normalisation rule by class name), and 1610-1660 (the two checkbox rules
      themselves and their long rationale comments).
    - `companion/pages/config_page.py` as changed by Task 1 — `led_group()`'s `<label>`
      class attribute and its docstring paragraph naming that class.
    - `.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md` — the "CSS reuse note"
      and the "One required CSS addition — `color-scheme`" blocks. Both are locked
      guidance; the rename is the spec's recommended option and this plan takes it, so do
      NOT fall back to duplicating the rule under a second class name.
    - `.claude/skills/sketch-findings-skypane/SKILL.md` — the "Controls and density"
      paragraph's touch-target floor register, which names the current class explicitly.
  </read_first>
  <action>
Rename the checkbox-normalisation class from `led-checkbox` to `settings-checkbox`, in
place, everywhere it appears — there are now two consumers of an identical four- and
nine-declaration pair, and duplicating them under a second name would create the
two-places-one-value failure mode this stylesheet's own comments already warn about.
A rename, not an alias: the old name must not survive as a second selector.

Sites to change:
  - `companion/static/style.css`: the `.led-checkbox` rule (~line 1619) and the
    `.led-checkbox input[type="checkbox"]` rule (~line 1645). Keep both rationale
    comments intact, updating only the class names inside them, and reword the rule's
    opening line so it no longer describes the control as LED-specific — it is now the
    shared Settings-page checkbox normalisation used by the Diagnostic LED group and the
    Quiet hours group.
  - `companion/static/style.css` ~line 681: the `.visually-hidden` comment
    cross-references the old selector by name; update that reference so the
    floor-clearing mechanism comment still points at a selector that exists.
  - `companion/pages/config_page.py`: `led_group()`'s `<label class="...">` attribute and
    the paragraph of its docstring that names the class.
Do not change any declaration value while renaming: the 44px `min-height` floor
relocation, the flex row, the 16px box, the cleared minimums and `flex: none` all stay
exactly as they are. This is a rename plus a comment reword, nothing else.

Add the three `color-scheme` declarations 10-UI-SPEC.md requires, placed alongside the
existing theme-attribute blocks (~lines 175-191), not as a new section:
`color-scheme: light` inside `html[data-ui-theme="light"]`, `color-scheme: dark` inside
`html[data-ui-theme="dark"]`, and `color-scheme: light dark` on `:root` for the default
`auto` case. No `color-scheme` property exists anywhere in this file today. It is required
because this site's dark/light mode is driven by an explicit `data-ui-theme` attribute
(CFG-09) that can disagree with the OS's own `prefers-color-scheme`, and without it a
browser paints the native time input's `::-webkit-calendar-picker-indicator` using the OS
scheme — producing a dark, hard-to-see icon on the dark-mode `--color-secondary` fill, or
the inverse, whenever the two disagree. Add a short comment above them recording that
reason, in the same register as the neighbouring CFG-09 comment.

No other CSS is needed for the new fieldset. `<input type="time">` inherits the global
`input, select` rule cleanly — every declaration in it (the 44px minimums, padding,
`--color-secondary` fill, border, radius, colour, `--font-body-size`, `--font-ui`,
`accent-color`) applies to `type="time"` exactly as it does to `type="text"`, and no
type-specific rule exists that would need extending. The 44px floor clears both WCAG 2.5.8
(AA, 24px) and 2.5.5 (AAA, 44px) with no trade. Do not add a width, height or padding
override for the time inputs.

Update `.claude/skills/sketch-findings-skypane/SKILL.md`, which is this project's living
design-system reference and names the old class explicitly in its touch-target floor
register ("Controls and density" paragraph, the "relocated" category). Rename it there to
the new class and reword the parenthetical so it describes both consumers rather than the
LED alone. Add the two facts this phase introduces to the same document, in its
established style: that `color-scheme` now exists on the two `data-ui-theme` blocks and
`:root`, and why; and that `<input type="time">` is now in use on the Settings page and
sits in the register's "kept" category via the unmodified global `input`/`select` 44px
floor. Follow whatever append-only log convention the file's own `<metadata>` section
uses for recording folded-in work.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py; echo "exit=$?"</automated>
    <automated>server/.venv/bin/python3 companion/test_contrast_check.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'settings-checkbox' companion/static/style.css` is at least 2 and `grep -c 'settings-checkbox' companion/pages/config_page.py` is at least 2
    - `grep -rn 'led-checkbox' companion/static/style.css companion/pages/config_page.py` returns no match — the rename left no stale selector or stale comment reference behind
    - `grep -c 'color-scheme' companion/static/style.css` is at least 3, and `grep -nE 'html\[data-ui-theme="dark"\]' companion/static/style.css` still matches (the declarations were added inside the existing blocks, not as new selectors)
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 at its unchanged count — no colour token changed
    - `grep -rn 'led-checkbox' .claude/skills/sketch-findings-skypane/SKILL.md` returns no match, and `grep -c 'settings-checkbox' .claude/skills/sketch-findings-skypane/SKILL.md` is at least 1
    - `git diff --name-only` after this commit lists exactly `companion/static/style.css`, `companion/pages/config_page.py`, and `.claude/skills/sketch-findings-skypane/SKILL.md`
  </acceptance_criteria>
  <done>
One shared checkbox-normalisation class serves both Settings groups with no duplicated
rule and no stale name anywhere, the native time picker follows the site's explicit theme
override, and the living design-system reference records both changes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cover the fieldset and the save path in test_config_page.py</name>
  <files>companion/test_config_page.py</files>
  <read_first>
    - `companion/test_config_page.py` — lines 118-130 (`EXPECTED_CHECK_COUNT = 64`),
      250-265 (`_write_device_config()`, the fixture helper), 284-345 (the render-shape
      check and the LED-group markup check whose assertions the rename affects), 770-820
      (the section-caption structural check that calls each group builder directly), and
      1130-1160 plus 1380-1440 (the `handle_post()` persistence and rejection checks whose
      fixtures the new fields extend).
    - `companion/pages/config_page.py` and `companion/static/style.css` as changed by
      Tasks 1 and 2.
    - `.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md` — the locked field order,
      copy and unchecked-checkbox semantics the new checks assert.
  </read_first>
  <action>
Retarget the existing checks that assert the old checkbox class name — the LED-group
markup check (~lines 316-340, which asserts the label class and the input's
name/value/checked attribute sequence) and the section-caption structural check
(~line 796, whose per-group tuple names the class as its control marker). Change only the
class literal; do not weaken or delete either assertion.

Add new `check(...)` calls covering at minimum:
  - **Markup.** `quiet_hours_group(True, "23:00", "07:00")` emits exactly one
    `<label class="settings-checkbox">`, one `name="quiet_hours_start"` input of
    `type="time"` carrying `value="23:00"`, one `name="quiet_hours_end"` input carrying
    `value="07:00"`, and exactly one `checked` flag; `quiet_hours_group(False, ...)`
    carries none. Assert the group contains no `theme-status__row` and no `disabled`
    attribute — the vertical-stacking and never-disabled rules are locked by
    10-UI-SPEC.md and are the two most likely things to drift.
  - **Field order.** Inside the returned markup, the `</h2>` position precedes the
    `section-caption` paragraph, which precedes the checkbox label, which precedes the
    start input, which precedes the end input — compare `str.index()` positions.
  - **Escaping.** `quiet_hours_group(True, '"><script>', "07:00")` produces markup with no
    raw `<script>` substring, proving the current-value interpolation goes through
    `escape_html()`.
  - **Render wiring.** `render(ctx)` with a `device_config` dict carrying
    `quiet_hours_enabled: True, quiet_hours_start: "22:30", quiet_hours_end: "06:15"`
    contains both values and the checked flag, and the Quiet hours group appears after the
    LED group and before the form's closing Save button.
  - **Save, checkbox on.** `handle_post({"quiet_hours_enabled": "on",
    "quiet_hours_start": "22:30", "quiet_hours_end": "06:15"}, ctx)` returns `FLASH_SAVED`
    and `device_config.load_device_config()` reads back all three values.
  - **Save, checkbox absent, times still persist.** `handle_post({"quiet_hours_start":
    "22:30", "quiet_hours_end": "06:15"}, ctx)` returns `FLASH_SAVED`, and the on-disk
    config has `quiet_hours_enabled` `False` with both times persisted as submitted. This
    is the check that pins 10-UI-SPEC.md's resolution of Assumption A1 — a user can
    pre-configure a window before enabling it — and must not be dropped or inverted.
  - **Reject, malformed time.** `handle_post({"quiet_hours_start": "24:00"}, ctx)` against
    a state directory holding a legitimately-saved config returns `FLASH_SAVE_FAILED` and
    leaves `device_config.json` byte-identical (read the bytes before and after).
  - **Reject, crafted checkbox value.** `handle_post({"quiet_hours_enabled": "yes"}, ctx)`
    returns `FLASH_SAVE_FAILED`, matching the LED field's third shape.
  - **All-or-nothing.** A post carrying a valid theme AND a malformed
    `quiet_hours_end` returns `FLASH_SAVE_FAILED` and persists neither — the theme on disk
    is unchanged.
Set `EXPECTED_CHECK_COUNT` (currently 64) to 64 plus the exact number of `check(...)`
calls added, in this same commit — the harness fails by design on a mismatch.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py; echo "exit=$?"</automated>
    <automated>server/.venv/bin/ruff check companion/test_config_page.py</automated>
    <human-check>
Start the companion service against a scratch state directory, sign in, and open
`/settings` in a real browser.

1. Desktop viewport: confirm the Quiet hours group renders as a fourth card below
   Diagnostic LED, with the heading, one muted caption sentence, the checkbox, then Start
   and End on their own full-width lines — not side by side.
2. Narrow the viewport to 320px and then 375px: confirm neither time input wraps,
   overflows, or crushes its label.
3. Confirm both time inputs match the page's other fields visually (same fill, border,
   radius, height) — 10-UI-SPEC.md asserts they inherit the global `input, select` rule
   cleanly, and this is the first `type="time"` input in the codebase, so it is the one
   claim in that spec that only a real browser can settle.
4. Toggle the site's own light/dark theme control while your OS is set to the OPPOSITE
   scheme, and confirm the native time-picker indicator icon stays visible in both — that
   disagreement is exactly the case the new `color-scheme` declarations exist for.
5. Leave the checkbox unchecked, edit both times, save, and reload: the times must come
   back as edited, with the checkbox still unchecked. Then tick the checkbox and save
   again: the flash must be the ordinary "Saved — will apply on the frame's next
   scheduled refresh" message, with no quiet-hours-specific copy.
6. Confirm the enable checkbox renders as a normal small checkbox with the accent tint,
   not an oversized filled box — that is what the shared normalisation class provides.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`
    - `server/.venv/bin/ruff check companion/test_config_page.py` reports no findings
    - `grep -c 'quiet_hours' companion/test_config_page.py` is at least 8
    - `server/.venv/bin/python3 companion/test_companion_app.py` and `server/.venv/bin/python3 companion/test_status_pages.py` and `server/.venv/bin/python3 companion/test_view_pages.py` each exit 0 at their unchanged counts — the rename and the new group touched no other page
    - `git diff --name-only` after this commit lists exactly `companion/test_config_page.py`
  </acceptance_criteria>
  <done>
The harness proves the markup, field order, escaping, render wiring, both save paths,
both rejection paths and the all-or-nothing rule, and a real browser has confirmed the
two things a stdlib check harness cannot: the time inputs' inherited sizing and the
theme-consistent picker icon.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser POST `/settings` -> `handle_post()` | Untrusted submitted quiet-hours strings cross into a persisted document |
| saved config -> rendered Settings HTML | A stored value is interpolated back into markup on the next page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Denial of Service | `handle_post()` -> `save_device_config()` | medium | mitigate | Every submitted quiet-hours value is validated by `save_device_config()` before the file is touched; the crafted-checkbox value is rejected in `handle_post()` itself by exact equality against `QUIET_HOURS_CHECKBOX_VALUE`. A malformed HH:MM raises `ValueError`, is caught by the existing `except (ValueError, OSError)`, and returns the generic save-failed flash — it never reaches a `datetime` constructor and never leaves a partial write |
| T-10-05-02 | Information Disclosure / Tampering | `quiet_hours_group()`'s value interpolation | medium | mitigate | Every interpolated current value — heading, caption, checkbox value, both times — goes through `escape_html()`, exactly as `led_group()`/`theme_fieldset()` already do; covered by a dedicated crafted-value check in Task 3 |
| T-10-05-03 | Elevation of Privilege | access to the new fieldset | low | accept | No new auth surface: the companion site's existing single shared-password gate (06-CONTEXT.md D-01/D-02) covers every Settings field uniformly, new or old, and this group is inside the same already-gated form and route |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages and zero new client-side dependencies — server-rendered HTML strings plus a native `<input type="time">`, no framework, no registry, no shadcn (`Tool: none`, confirmed in 10-UI-SPEC.md) |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` exits 0 at the bumped count
- The other four companion harnesses (`test_companion_app.py`, `test_status_pages.py`,
  `test_view_pages.py`, `test_contrast_check.py`) each exit 0 at their unchanged counts
- `server/.venv/bin/ruff check .` reports no findings
- `scripts/run-all-tests.sh` shows no NEW harness failure versus the pre-plan baseline
- The real-browser check's findings are recorded in the plan SUMMARY, including the
  narrow-viewport and OS-versus-site-theme results
</verification>

<success_criteria>
- A fourth Quiet hours group renders on `/settings` with the locked copy, field order and vertical stacking
- An unchecked checkbox still saves edited times; an absent checkbox resolves to False
- A malformed HH:MM or a crafted checkbox value returns the generic save-failed flash and persists nothing
- Every interpolated value is escaped
- One shared checkbox class serves both groups, with no stale name left anywhere
- The native time-picker indicator follows the site's explicit theme override
</success_criteria>

<output>
Create `.planning/phases/10-scheduled-quiet-hours/10-05-SUMMARY.md` when done
</output>
