---
phase: 10-scheduled-quiet-hours
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/plane/render.py
  - server/test_render.py
autonomous: true
requirements: []   # unmapped backlog phase promoted from SEED-001 — see 10-RESEARCH.md <phase_requirements>
must_haves:
  truths:
    - "build_canvas(None, \"quiet_hours\", quiet_hours_until=\"07:00\") returns a flat White canvas carrying the heading QUIET HOURS and the body Back at 07:00, in Black ink, regardless of theme_id (D-05/D-06)"
    - "Every drawn element on the quiet-hours canvas passes _assert_in_safe_box() and every pixel index is one of the six legal palette indices"
    - "The battery-low icon and the source-fault badge render on the quiet-hours screen when their flags are set, matching _build_empty_canvas()'s existing precedent (10-RESEARCH.md A2, locked by 10-UI-SPEC.md)"
    - "A missing or non-string quiet_hours_until renders the heading alone and never raises and never draws the literal text None"
    - "server/plane/render.py's CLI can render the quiet-hours state to a PNG preview, so the deferred visual review has a real artefact to look at"
  artifacts:
    - server/plane/render.py
    - server/test_render.py
  key_links:
    - "build_canvas()'s state == \"quiet_hours\" branch must be evaluated BEFORE the existing `flight is None or state == \"empty\"` branch, or every quiet-hours call silently renders the empty state instead"
    - "server/poll_loop.py (plan 10-04) is the only production caller of this render state"
---

<objective>
Add the dedicated "QUIET HOURS" panel render state (D-05) with its locked English copy
(D-06) to `server/plane/render.py`, plus the CLI flag that makes the deferred real-preview
review possible.

Purpose: this screen is a deliberate, confirmed exception to the project's "no on-screen
status text" rule — the same class of exception the Phase 5 low-battery icon already
carved out. It is drawn exactly once, by `poll_loop.py` (plan 10-04), at the poll that
first detects window entry.

Output: `_build_quiet_hours_canvas()`, a `quiet_hours` dispatch branch and
`quiet_hours_until` parameter on `build_canvas()`/`render_panel()`, a `--state quiet_hours`
/ `--quiet-hours-until` CLI path, and extended coverage in `server/test_render.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-scheduled-quiet-hours/10-CONTEXT.md
@.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md
@.planning/phases/10-scheduled-quiet-hours/10-PATTERNS.md
@server/plane/render.py
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 10 and does not exist in the codebase before it.
Do not treat any of them as drift against existing source.

**server/plane/render.py** (this plan): `QUIET_HOURS_HEADING_TEXT`,
`QUIET_HOURS_BODY_TEMPLATE`, `_build_quiet_hours_canvas()`, the `quiet_hours_until`
parameter on `build_canvas()` and `render_panel()`, the `quiet_hours` value of the CLI
`--state` choice list, and the `--quiet-hours-until` CLI flag.

**server/device_config.py** (plan 10-01): `DEFAULT_QUIET_HOURS_ENABLED`,
`DEFAULT_QUIET_HOURS_START`, `DEFAULT_QUIET_HOURS_END`, `_HHMM_RE`, `QUIET_HOURS_TZ`,
`normalise_quiet_hours_enabled()`, `normalise_quiet_hours_time()`,
`seconds_until_quiet_hours_end()`, `quiet_hours_status()`, and the three new
`load_device_config()`/`save_device_config()`/`device_config.json` keys.

**stub-server/byos_server.py** (plan 10-03): `QUIET_HOURS_TZ`, `_HHMM_RE`,
`seconds_until_quiet_hours_end()` (vendored duplicate), `read_quiet_hours()`,
`quiet_hours_sleep_s()`.

**server/poll_loop.py** (plan 10-04): the `poll_state.json` key `quiet_hours_active`
and the `"quiet_hours"` render-state string.

**companion/pages/config_page.py** (plan 10-05): `QUIET_HOURS_CHECKBOX_VALUE`,
`QUIET_HOURS_SECTION_HEADING`, `QUIET_HOURS_SECTION_CAPTION`, `quiet_hours_group()`.
**companion/static/style.css** (plan 10-05): the `.settings-checkbox` class and
`color-scheme` declarations.

<tasks>

<task type="auto">
  <name>Task 1: Add _build_quiet_hours_canvas() and the build_canvas() dispatch branch</name>
  <files>server/plane/render.py, server/test_render.py</files>
  <read_first>
    - `server/plane/render.py` lines 105-125 (`SPACE_SM`, `WIDTH`/`HEIGHT`, `SAFE_BOX`),
      155-200 (`EMPTY_HEADING_FONT`, `EMPTY_BODY_FONT`, `EMPTY_HEADING_MIN_SIZE`),
      290-305 (`EMPTY_INK`, `EMPTY_BODY_TEXT`), 1740-1799 (`_build_empty_canvas()` — the
      body this task copies), and 1987-2050 (`build_canvas()`'s docstring and dispatch)
      plus `render_panel()` immediately after it.
    - `.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md`, the
      `## Panel Screen Contract` section — the locked font/size/ink/layout table and the
      locked copy. That section, not the web-shaped tables above it, is this surface's
      contract.
    - `.planning/phases/10-scheduled-quiet-hours/10-PATTERNS.md`, the
      "`server/plane/render.py`" section — the condensed load-bearing shape of the
      canvas-building code and the dispatch snippet.
    - `server/test_render.py` lines 40-50 (`EXPECTED_CHECK_COUNT`), 360-375 (the
      `check()` harness), 450-470 (`_empty_state_white_dominant_with_black()` — the
      closest existing analog for the new palette/dominance check), and 1490-1600 (the
      per-state parametrised battery/source-fault checks).
  </read_first>
  <action>
Add two copy constants next to `EMPTY_BODY_TEXT` (~line 299):
`QUIET_HOURS_HEADING_TEXT = "QUIET HOURS"` and `QUIET_HOURS_BODY_TEMPLATE = "Back at %s"`.
Both are locked by D-06 and 10-UI-SPEC.md's Panel Screen Copywriting Contract — English,
all-caps heading, `Back at HH:MM` body. Do not localise to French despite the developer's
own French working example during the discussion; every other panel string
(`DEPARTING`/`ARRIVING`, `ORY · RWY 3`) is English and this one matches them.

Add `_build_quiet_hours_canvas(quiet_hours_until, source_fault=False, battery_low=False)`
immediately after `_build_empty_canvas()` (after line 1798). Copy `_build_empty_canvas()`'s
body structure verbatim — `pf.new_canvas(IDX_WHITE)`, `ImageDraw.Draw`, `_font(EMPTY_BODY_FONT)`,
`center_x = WIDTH // 2`, `safe_width = SAFE_BOX[2] - SAFE_BOX[0]`,
`fit_text_size(PT_SERIF_BOLD, EMPTY_HEADING_FONT[1], heading_text, safe_width, EMPTY_HEADING_MIN_SIZE)`,
the `getmetrics()` ascent+descent heights, `_wrap_text()` for the body,
the `total_height`/`start_y` vertical-centring formula, the `draw.textbbox(...)` +
`_assert_in_safe_box(...)` + `draw.text(..., fill=EMPTY_INK, anchor="ma")` pair for the
heading and for each body line, and the trailing `draw_source_fault_badge(canvas,
EMPTY_INK, weight="bold")` / `draw_battery_icon(canvas, draw, EMPTY_INK)` guards — with
these differences:

  - `heading_text` is the fixed `QUIET_HOURS_HEADING_TEXT`, not `empty_heading_text(runway_id)`.
    The function takes no `runway_id` and ignores `theme_id` entirely, exactly as the
    empty state ignores `theme_id` — this screen is always flat White with Black ink.
  - The body line is `QUIET_HOURS_BODY_TEMPLATE % quiet_hours_until` when
    `quiet_hours_until` is a non-empty string, and the body is OMITTED ENTIRELY otherwise
    (`body_lines = []`, so `total_height` reduces to `heading_height` and the heading
    centres alone). Never render the string `Back at None`. This is the same
    "an element exists visually only when it has real information to show" discipline
    03-CONTEXT.md D-25 established and 05-CONTEXT.md D-06 reused for the battery icon.
  - Pass the safe-box assertion labels `"quiet-hours heading"` and
    `"quiet-hours body line"` so a failure names this screen, not the empty state.
  - The `fit_text_size()` shrink path will not trigger for an 11-character heading at
    72px inside a 1072px safe width; keep it anyway, defensively, exactly as
    10-UI-SPEC.md's Panel Screen table specifies.

Add `quiet_hours_until=None` as a new trailing keyword parameter to `build_canvas()` and
insert its dispatch branch as the FIRST statement of the function body, BEFORE the
existing `if flight is None or state == "empty":` check — that existing check would
otherwise swallow every quiet-hours call (`flight` is always `None` for this state) and
silently render the empty screen. The branch reads
`if state == "quiet_hours": return _build_quiet_hours_canvas(quiet_hours_until=quiet_hours_until,
source_fault=source_fault, battery_low=battery_low)`. Extend `build_canvas()`'s docstring
with a paragraph naming the new state, citing D-05/D-06, and stating that `theme_id`,
`runway_id`, `route`, and the three `previous_*` arguments are all ignored for it.

Add the same `quiet_hours_until=None` parameter to `render_panel()` and pass it straight
through to `build_canvas()`, matching how that wrapper already forwards every other
argument. Extend its docstring's passthrough sentence to name it.

Add new `check(...)` calls to `server/test_render.py` covering, at minimum:
(1) `render.render_panel(None, "quiet_hours", quiet_hours_until="07:00")` returns exactly
`panel_format.IMAGE_BYTES` bytes, is White-dominant, and contains at least one Black
nibble — mirror `_empty_state_white_dominant_with_black()`'s structure;
(2) every pixel index in `build_canvas(None, "quiet_hours", quiet_hours_until="07:00")`
is a member of the six legal palette indices;
(3) the canvas is pixel-identical for two different `theme_id` values, proving the theme
is ignored;
(4) `build_canvas(None, "quiet_hours", quiet_hours_until=None)` and
`quiet_hours_until=""` both return a canvas without raising, and differ from the
`quiet_hours_until="07:00"` canvas (the body line is genuinely absent);
(5) `battery_low=True` and `source_fault=True` each change the canvas relative to the
same call with both `False`, proving both indicators reach this screen (10-RESEARCH.md
Assumption A2, locked by 10-UI-SPEC.md);
(6) a source assertion that `build_canvas()`'s `quiet_hours` branch precedes its
`state == "empty"` branch — e.g. compare the two `str.index()` positions of the two
branch literals inside `inspect.getsource(render.build_canvas)`.
Set `EXPECTED_CHECK_COUNT` (currently 119) to 119 plus the exact number of `check(...)`
calls added, in this same commit — the harness fails by design on a mismatch.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py; echo "exit=$?"</automated>
    <automated>server/.venv/bin/ruff check server/plane/render.py server/test_render.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`
    - `grep -c 'QUIET HOURS' server/plane/render.py` is at least 1
    - `grep -nE '^def _build_quiet_hours_canvas\(' server/plane/render.py` matches
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render; c = render.build_canvas(None, 'quiet_hours', quiet_hours_until='07:00'); print(c.size, sorted({i for i, _ in [(p, 1) for p in c.getdata()]}))"` prints `(1200, 1600)` followed by a list whose every entry is one of `0, 1, 2, 3, 4, 5`
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from server.plane import render; a = render.build_canvas(None, 'quiet_hours', quiet_hours_until=None).tobytes(); b = render.build_canvas(None, 'quiet_hours', quiet_hours_until='07:00').tobytes(); print(a != b)"` prints `True` and raises nothing
    - `server/.venv/bin/ruff check server/plane/render.py server/test_render.py` reports no findings
    - `git diff --name-only` after this commit lists exactly `server/plane/render.py` and `server/test_render.py`
  </acceptance_criteria>
  <done>
A `quiet_hours` render state exists, is dispatched before the empty-state branch, draws
the locked English copy in Black on flat White inside the safe box, carries the
battery/fault indicators, and degrades to a heading-only screen for a missing end time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose the quiet-hours state through render.py's preview CLI</name>
  <files>server/plane/render.py, server/test_render.py</files>
  <read_first>
    - `server/plane/render.py` lines 2113-2200 (`build_parser()` — the `--state` choices
      list, `--theme`, `--runway`, `--source-fault`, `--battery-low`, `--preview`,
      `--out`) and 2202-2319 (`main()` — in particular the `if args.state != "empty":`
      guard at ~line 2217 that builds a synthetic flight, and the single
      `build_canvas(...)` call at ~line 2263).
    - `.planning/phases/10-scheduled-quiet-hours/10-UI-SPEC.md`, the
      "Open item this spec deliberately does NOT resolve" block — the reason this CLI
      path has to exist at all.
    - `.planning/phases/10-scheduled-quiet-hours/10-VALIDATION.md`, the
      "Manual-Only Verifications" table.
  </read_first>
  <action>
Add `"quiet_hours"` to `build_parser()`'s `--state` choices list (currently
`["departing", "arriving", "empty"]`), and add a `--quiet-hours-until` argument
defaulting to `server.device_config.DEFAULT_QUIET_HOURS_END` — the module already imports
`device_config` for `--theme`/`--runway`'s own choice lists, so reuse that import rather
than hardcoding `"07:00"`. Its help text should say it is the local Europe/Paris
wall-clock end time the preview's "Back at" line shows, and that it is ignored for every
other `--state`.

Fix `main()`'s synthetic-flight guard. It currently reads `if args.state != "empty":` and
would therefore build a fake `{"hex": ..., "callsign": ...}` flight for the quiet-hours
state and drag the whole route/illustration preview machinery in with it. Change the
condition so the synthetic-flight block runs only for the two real aircraft states — test
membership against `(runway_config.STATE_DEPARTING, runway_config.STATE_ARRIVING)`, which
this module already imports and uses two lines below, rather than adding a second
negative list that a future third non-flight state would have to be remembered into.

Pass `quiet_hours_until=args.quiet_hours_until` through `main()`'s single
`build_canvas(...)` call, alongside the existing `theme_id`/`runway_id`/`source_fault`/
`battery_low` arguments.

Add one `check(...)` to `server/test_render.py` that drives `render.main()` through
`build_parser()` with `["--state", "quiet_hours", "--quiet-hours-until", "07:00", "--out",
<tmp path>]` and asserts it returns 0 and writes exactly `panel_format.IMAGE_BYTES` bytes
— the CLI path is what the manual preview review below depends on, so it needs a real
automated guard, not just the library-level checks from Task 1. Bump
`EXPECTED_CHECK_COUNT` by exactly the number of `check(...)` calls added, in the same
commit.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py; echo "exit=$?"</automated>
    <automated>server/.venv/bin/python3 server/plane/render.py --state quiet_hours --quiet-hours-until 07:00 --preview /tmp/skypane-quiet-hours-preview.png --out /tmp/skypane-quiet-hours.bin && ls -l /tmp/skypane-quiet-hours.bin</automated>
    <human-check>
Open `/tmp/skypane-quiet-hours-preview.png` (written by the command above) and compare it
side by side with the empty state, rendered with
`server/.venv/bin/python3 server/plane/render.py --state empty --preview /tmp/skypane-empty-preview.png`.

10-UI-SPEC.md deliberately leaves ONE question open for this review and this review only:
does "QUIET HOURS" read as meaningfully distinct from "Watching Runway 3" at a glance,
given both screens share the identical flat-White / Black / centred-heading structure?

The spec's locked default is option (1): change nothing, if the differing text alone is
enough. If it is not, the sanctioned follow-ups, least to most invasive, are (2) a
slightly larger or bolder heading treatment for this screen, still White/Black, or
(3) — last resort only — a distinct flat fill colour for this screen alone, still one of
the six legal palette indices. Do not ship (2) or (3) speculatively without having looked
at (1) first; that sequencing is the whole point of this check, and it mirrors the
real-preview discipline 05-CONTEXT.md used for the battery icon.

Also confirm the body line reads exactly `Back at 07:00` in English, and that the
heading and body both sit comfortably inside the panel's margins.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 server/plane/render.py --state quiet_hours --quiet-hours-until 07:00 --out /tmp/skypane-quiet-hours.bin` exits 0 and `/tmp/skypane-quiet-hours.bin` is exactly 960000 bytes
    - `server/.venv/bin/python3 server/plane/render.py --state quiet_hours --preview /tmp/skypane-quiet-hours-preview.png` exits 0 and writes a readable PNG
    - `server/.venv/bin/python3 server/plane/render.py --state departing --callsign AF1006 --out /tmp/skypane-departing.bin` still exits 0 and writes 960000 bytes — the reworked synthetic-flight guard did not break the two aircraft states
    - `server/.venv/bin/python3 server/plane/render.py --state nope` exits non-zero with an argparse choices error naming `quiet_hours` among the valid values
    - `server/.venv/bin/python3 server/test_render.py` exits 0 at the bumped `EXPECTED_CHECK_COUNT`
    - `git diff --name-only` after this commit lists exactly `server/plane/render.py` and `server/test_render.py`
  </acceptance_criteria>
  <done>
The preview CLI renders the quiet-hours screen to a `.bin` and a `.png`, the two aircraft
states are unaffected, and the deferred visual review has a real artefact to judge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `poll_loop.py` -> `build_canvas(quiet_hours_until=...)` | A config-derived string crosses into text drawn onto the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Denial of Service | `_build_quiet_hours_canvas()` | medium | mitigate | A missing/empty/non-string `quiet_hours_until` omits the body line instead of raising or drawing a `None` literal; the caller (plan 10-04) additionally only ever passes a value that has already cleared `_HHMM_RE`, so the string is bounded to five characters |
| T-10-02-02 | Tampering | panel palette legality | low | mitigate | The canvas is built exclusively from `IDX_WHITE` and `EMPTY_INK` (= `IDX_BLACK`) and every text bbox passes `_assert_in_safe_box()`; a legal-palette membership check over every pixel is an acceptance criterion on Task 1 |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing — it uses only Pillow primitives already imported by `render.py`. `server/requirements.txt` is untouched |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_render.py` exits 0 at the bumped count
- `server/.venv/bin/ruff check .` reports no findings
- `scripts/run-all-tests.sh` shows no NEW harness failure versus the pre-plan baseline
  (the pre-existing, unrelated `server/test_poll_loop.py` digest mismatch documented in
  `.planning/STATE.md` is not this plan's to fix)
- The deferred visual review (Task 2's `human-check`) is recorded in the plan SUMMARY with
  the option chosen — (1), (2) or (3) — and the reason
</verification>

<success_criteria>
- `build_canvas(None, "quiet_hours", quiet_hours_until="07:00")` renders the locked copy on a flat White canvas in Black ink, ignoring `theme_id`
- The quiet-hours branch is dispatched before the empty-state branch
- Battery-low and source-fault indicators render on this screen when set
- A missing end time degrades to a heading-only screen without raising
- The preview CLI can produce both a `.bin` and a `.png` for the new state
</success_criteria>

<output>
Create `.planning/phases/10-scheduled-quiet-hours/10-02-SUMMARY.md` when done
</output>
