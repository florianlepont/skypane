---
phase: 14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit
plan: 08
type: execute
wave: 4
depends_on: ["14-04", "14-05", "14-06", "14-07"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Every row in 14-VALIDATION.md's Manual-Only Verifications table has been exercised in a real browser and confirmed working, not merely inferred from source-level checks."
    - "Phase 13's inherited G-02 (the <datalist> popup, never verified in a browser across two phases) is finally exercised and confirmed."
    - "The full automated suite is green immediately before the manual pass begins, so any failure found manually is attributable to genuine browser-only behavior, never to a regression the harness would already have caught."
  artifacts:
    - "A completed manual verification record (in the SUMMARY this plan produces) covering all nine VALIDATION.md manual-only rows plus G-02, each with a pass/fail and any issue found."
  key_links:
    - "The human tester drives the exact same running companion service the automated suite already exercised — no separate staging build, no divergent code path"
---

<objective>
Close the phase with the one verification step nothing in this phase's toolchain can automate:
a real browser, a real click, a real keyboard tab order, against the real running companion
service. VALIDATION.md's own § Manual-Only Verifications table names nine rows this phase
cannot pass without one; Phase 13 left one more (G-02) unverified across two phases running.

Purpose: RESEARCH.md is explicit that the stdlib `check()` harness "cannot assert, at all...
anything that requires a JavaScript engine actually executing `panel-lookup.js` against a live
DOM." Every plan in this phase (14-04 through 14-07) has already proven its own server-rendered
half — markup shape, attribute values, source-level script contracts — as far as a harness
without a browser can. This plan is where the phase's actual interactive promise (the
developer's own "un clic dessus ouvre également la pop up") gets confirmed rather than assumed.

Output: a full green automated suite run immediately before the manual pass, and a completed,
recorded pass/fail for every one of VALIDATION.md's nine manual-only rows plus G-02, captured
in this plan's own SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-CONTEXT.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-UI-SPEC.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-VALIDATION.md
@.planning/phases/13-add-an-illustration-for-an-unidentified-flight-from-the-comp/13-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — full automated suite green, and a live service to test against</name>
  <read_first>
    - `scripts/run-all-tests.sh` — the canonical 17-harness list this task runs in full
    - `.planning/phases/14-.../14-VALIDATION.md` § Test Infrastructure — the exact commands, restated so the human tester in Task 2/3 inherits a known-good baseline
    - `companion/app.py` — how to start the companion service locally (its own `if __name__ == "__main__"` entry point / the module's documented run command) for the manual pass below
  </read_first>
  <files>none (verification only)</files>
  <action>
    Run `scripts/run-all-tests.sh` in full and confirm it exits 0, with every one of the 17
    harnesses reporting its own exact-count pass line — in particular
    `companion/test_status_pages.py` at 163/163, `companion/test_view_pages.py` at 63/63, and
    `companion/test_companion_app.py` at 159/159, the three counts plans 14-04/14-05/14-06/14-07
    left behind. If any count differs from what those plans' own SUMMARYs recorded, stop and
    report the discrepancy rather than proceeding to the manual pass — a red or drifted
    automated suite means the manual pass would be testing against an unknown baseline.

    Start `companion/app.py` locally against a state directory seeded with: at least 15
    unresolved prefixes at varying counts (some below `GAP_BLOCK_THRESHOLD`, at least 13 at or
    above it, so both the threshold exclusion and the `GAP_BLOCK_CAP`/overflow line are
    reachable in the browser); at least two manual-resolution entries, one superseded (a real
    static-table prefix such as `AFR`) and one active with no artwork yet; and the existing
    curated illustration set unchanged. Log in through the normal session flow. Leave the
    service running for Task 2/3.
  </action>
  <acceptance_criteria>
    - `scripts/run-all-tests.sh` exits 0
    - The seeded local service answers `GET /airlines` with 200 and visibly renders both gap
      cards and at least one manual-origin card when viewed in a browser
  </acceptance_criteria>
  <verify>
    <automated>scripts/run-all-tests.sh</automated>
  </verify>
  <done>The full automated suite is green at the exact counts this phase's plans recorded, and a real, seeded companion service is running and reachable for the manual pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-glass verification — imageless open, per-mode form toggling, in-dialog delete, keyboard focus, and the superseded card</name>

  <files>none — this task changes no file; it verifies Tasks built by plans 14-04/14-05/14-06</files>

  <read_first>
    - `.planning/phases/14-.../14-VALIDATION.md` § Manual-Only Verifications — rows 1 through 4 and row 6 (D-02, D-03, D-09, focus, and the superseded-card half of D-02/D-03) are this task's own scope
    - `.planning/phases/14-.../14-UI-SPEC.md` § Interaction Contract → the Visibility toggle table — the exact expected show/hide behavior per trigger this task confirms on real glass
    - `.planning/phases/14-.../14-CONTEXT.md` § D-09, D-09 amendment, D-10 — the locked decisions this task's steps 3 and 5 confirm
  </read_first>

  <action>
    Drive the session against the running service Task 1 left up, and capture the tester's
    report verbatim per step — do not normalise a step into a bare pass/fail with no detail.
    Walk steps 1 through 5 in order, in one browser session, so the dialog's DOM-reuse-across-
    clicks behavior (UI-SPEC's own "single highest-risk correctness detail") is actually
    exercised repeatedly rather than each mode being opened once from a fresh page load.
  </action>

  <what-built>
    Automated: gap cards (empty, imageless, sorted, thresholded, capped), a widened shared
    dialog carrying resolve-name/resolve-upload/delete forms and a manual-note, chips marking
    manual origin, a summary line, and a `panel-lookup.js` extended to open imagelessly, toggle
    the right form per trigger, and intercept `&lt;a&gt;` navigation.
  </what-built>
  <how-to-verify>
    Against the running service from Task 1, at `/airlines`:

    1. **D-02 (imageless open, no network call).** Open your browser's Network panel. Click an
       empty gap card. Confirm the dialog opens with no image area (no broken-image icon, no
       image slot at all) and confirm the Network panel shows **no** new request fired by the
       click — neither for an image nor for the page itself.
    2. **D-03 (right form per trigger).** Click an ordinary curated art card — confirm the
       dialog shows the replace-image form and nothing else optional. Close it. Click the gap
       card from step 1 again — confirm the dialog now shows the "Save airline name" form and
       nothing else optional (no upload zone, no replace form, no delete control). Click the
       active manual entry's card (no artwork yet) — confirm the dialog shows the upload form
       and, this time, a Delete control with its caption above it.
    3. **D-09 (delete from inside the dialog).** With the active manual entry's dialog still
       open from step 2, click Delete. Confirm the entry is gone from the grid on reload and
       that clicking the illustration's override file's own effect (if any artwork was
       uploaded) is unaffected — the delete only removes the name, never any uploaded image.
    4. **Focus (D-02/D-03, keyboard-only).** Using only Tab and Enter, reach and open the gap
       card from step 1 again. Confirm focus lands inside the dialog on a sensible control (the
       airline-name field, not Close and not lost to the page body), and confirm Escape closes
       the dialog and returns focus sensibly.
    5. **Superseded card (D-10).** Open the seeded superseded entry's card (`AFR`). Confirm the
       chip reads "Superseded", the dialog's note explains the built-in list now owns this
       prefix, and the image shown is the frame's real current artwork for that airline — not
       whatever the operator's own stored name would have implied.
  </how-to-verify>
  <verify>
    <human-check>Tester reports, per step: the Network-panel observation from step 1 (no request fired); which optional form(s) were visible for each of the three trigger types in step 2; the delete outcome and override-file survival from step 3; the keyboard-only focus target and Escape behavior from step 4; and the chip text, note content and image identity observed in step 5.</human-check>
    <automated>echo "manual browser step — recorded by tester report, see acceptance criteria"</automated>
  </verify>
  <acceptance_criteria>
    - All five steps report a concrete, specific outcome — a step reported as "worked fine" with no detail is re-asked, not accepted
    - Step 1 explicitly confirms zero new network requests fired by the click
    - Step 2 explicitly confirms exactly one optional form visible per trigger, never more than one
    - Step 3 explicitly confirms the override file (if any) survives the delete
    - Step 5 explicitly confirms the displayed image is the built-in airline's own current artwork, not an orphaned upload
  </acceptance_criteria>
  <resume-signal>Type "approved" once all five checks pass, or describe exactly which failed and what you observed instead.</resume-signal>
  <done>The dialog's imageless open, per-mode form toggling, in-dialog delete, keyboard focus behavior, and the superseded card's honest current-state rendering are all confirmed on real glass, not inferred from source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-glass verification — load-time auto-open, the no-JS fallback, the filterable summary line, and Phase 13's inherited G-02</name>

  <files>none — this task changes no file; it verifies Tasks built by plans 14-05/14-06 and closes Phase 13's G-02</files>

  <read_first>
    - `.planning/phases/14-.../14-VALIDATION.md` § Manual-Only Verifications — rows 4, 5, 7 and 8 (D-13, D-14, D-11, D-12), plus the table's own G-02 row
    - `.planning/phases/13-.../13-UAT.md` § G-02 — the exact, still-open question this task closes: does the `<datalist>` suggestion list actually drop down, dismiss, and stay clear of the form on a narrow viewport
    - `.planning/phases/14-.../14-CONTEXT.md` § D-13, D-14 — the auto-open decisions, including the recorded supersession the tester should not expect a "second click" alternative for
  </read_first>

  <action>
    Continue in the same or a fresh browser session against the still-running service. Run
    steps 1 through 5 in order — step 2 depends on step 1's redirect landing correctly, and
    step 4 requires disabling JavaScript, so it is deliberately placed after the JS-dependent
    steps rather than first.
  </action>

  <what-built>
    Automated: the `?resolve={prefix}` load-time auto-open (D-13/D-14), the no-JS fallback path
    (D-12) including its own delete form (D-09 amendment), the D-11 summary line's live filter
    hook, and — inherited from Phase 13 — the airline-name `&lt;datalist&gt;` suggestion popup
    (G-02).
  </what-built>
  <how-to-verify>
    Against the same running service:

    1. **D-13 (auto-open from Health).** Visit `/health`, find a coverage-gap row, click its
       Resolve link. Confirm you land on `/airlines` with the dialog **already open** — no
       second click needed — showing that prefix's own name form.
    2. **D-14 (auto-reopen after naming).** In that open dialog, type a brand-new airline name
       with no existing artwork and save. Confirm the redirect lands back on `/airlines` with
       the dialog **already open again**, this time on the upload step for the name you just
       saved.
    3. **D-11 (summary line drives the filter).** Scroll to the manual-resolutions summary line
       ("N manual resolutions..."). Click it. Confirm the grid filters down to exactly the
       manual-origin cards (the "N of M shown" count updates to match), with no page reload.
    4. **D-12 (no-JS fallback still resolves).** Disable JavaScript in your browser (or use a
       private/incognito profile with a content-blocker that kills scripts). Visit
       `/airlines?resolve={a-live-gap-prefix}`. Confirm the page-section form at the bottom of
       the page still lets you save a name and, separately, visit
       `/airlines?resolve={the-active-manual-entry-prefix}` and confirm its own Delete button
       is present and works with JavaScript off.
    5. **G-02 (Phase 13's inherited gap — the `&lt;datalist&gt;` popup).** With JavaScript back
       on, open a resolve-name form (any gap card) and start typing an airline name. Confirm
       the native suggestion list actually drops down while typing, that it is dismissible
       (Escape or clicking away), and that on a narrow/mobile-width viewport it does not
       obstruct the rest of the form.
  </how-to-verify>
  <verify>
    <human-check>Tester reports, per step: whether the dialog was already open on arrival in steps 1 and 2 (never requiring a second click); the filtered count and card set observed in step 3; confirmation the no-JS save and no-JS delete both worked in step 4; and the datalist's drop-down/dismiss/narrow-viewport behavior from step 5, closing G-02 either way.</human-check>
    <automated>echo "manual browser step — recorded by tester report, see acceptance criteria"</automated>
  </verify>
  <acceptance_criteria>
    - Steps 1 and 2 both explicitly confirm the dialog was open on page arrival, with no second click performed
    - Step 3 explicitly confirms the visible card count changed to match only manual-origin cards
    - Step 4 explicitly confirms both the no-JS save and the no-JS delete succeeded
    - Step 5 reports a definite outcome for G-02 (pass or fail with detail) — not skipped
  </acceptance_criteria>
  <resume-signal>Type "approved" once all five checks pass, or describe exactly which failed and what you observed instead — this closes Phase 13's G-02 either way, so record the outcome even if it fails.</resume-signal>
  <done>Load-time auto-open, the no-JS fallback's full round trip (save and delete), the summary line's live filter, and Phase 13's long-open G-02 are all confirmed on real glass.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no source artifact — it is a verification gate. Its own SUMMARY.md is the
artifact: a recorded pass/fail for each of VALIDATION.md's nine manual-only rows plus G-02.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester's browser → the running companion service | Identical to every other operator session on this site — no new boundary is introduced by the act of testing |

## STRIDE Threat Register

No new code ships in this plan, so no new STRIDE surface is introduced. The mitigations this
plan *confirms* were already registered by the plans that built them:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-28 | Repudiation | a manual-only behavior failing silently with no automated re-check | medium | mitigate | This plan's own `<resume-signal>` requires an explicit typed outcome for every row, recorded in the SUMMARY — a failure here cannot be silently skipped the way an unautomatable behavior otherwise could be (RESEARCH.md's own stated limitation for this phase). |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` exits 0 immediately before the manual pass begins
- All nine rows of `14-VALIDATION.md`'s Manual-Only Verifications table are exercised and recorded
- Phase 13's G-02 is exercised and recorded, closing a gap open since Phase 13
</verification>

<success_criteria>
- Every automated harness is green at the exact counts this phase's plans recorded
- Every manual-only behavior in VALIDATION.md is confirmed working in a real browser, not inferred
- G-02 is finally closed
- Any failure found is recorded with enough detail to become a gap-closure plan, not lost
</success_criteria>

<output>
Create `.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-08-SUMMARY.md` when done, recording the pass/fail outcome of every row exercised above.
</output>
