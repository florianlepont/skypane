---
phase: 14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - companion/test_view_pages.py
  - companion/test_status_pages.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The three harnesses this phase moves (view-pages, status-pages, companion-app) can be run from inside this worktree with the exact command VALIDATION.md names."
    - "A new data-view-panel-* attribute constant added to airlines_page.py cannot be forgotten by the cross-file dialog guard — the harness goes red until it is classified."
    - "A seeded manual-resolutions registry and a seeded unresolved-prefix registry are both reachable from one fixture helper each, so Waves 1-3 never invent a second seeding path."
  artifacts:
    - "server/.venv/bin/python3 resolvable from the worktree root"
    - "companion/test_view_pages.py: _LIGHTBOX_SHARED_TOKENS / _LIGHTBOX_AIRLINES_ONLY_TOKENS / _LIGHTBOX_RENDER_ONLY_TOKENS module-level tuples"
    - "companion/test_view_pages.py: _view_panel_attr_constants_all_classified() check"
    - "companion/test_status_pages.py: _seed_manual_resolutions() fixture helper"
  key_links:
    - "_lightbox_dom_contract_three_file_guard() reads its token list from the three module-level tuples, never from an inline literal tuple"
    - "_view_panel_attr_constants_all_classified() enumerates airlines_page's _VIEW_PANEL_*_ATTR names by reflection, not from a hand-copied list"
    - "_seed_manual_resolutions() writes through manual_resolutions.add_entry()/the registry's own on-disk shape, never a hand-written JSON literal"
---

<objective>
Stand up the validation scaffolding this phase's later waves depend on, and make the
cross-file dialog contract *self-enforcing* before a single new attribute exists.

Purpose: VALIDATION.md's Wave 0 has two items — the extended
`_lightbox_dom_contract_three_file_guard()` and seeded `poll_state.json`/manual-registry
fixtures. Extending the guard by hand-listing tokens is exactly the drift this codebase
keeps paying to eliminate, so this plan instead restructures the guard so that *any*
future `_VIEW_PANEL_*_ATTR` constant must be explicitly classified or the harness fails.
Waves 1-3 then cannot silently ship an attribute the script never learned to read.

Output: three token tuples plus one reflection-driven coverage check in
`companion/test_view_pages.py`; one manual-registry fixture helper plus one exercising
check in `companion/test_status_pages.py`; a runnable interpreter at
`server/.venv/bin/python3` inside this worktree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-CONTEXT.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-VALIDATION.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the documented harness commands runnable inside this worktree</name>
  <read_first>
    - `scripts/run-all-tests.sh` — the canonical 17-harness list, the `PYTHON` override contract, and the documented venv bootstrap in its own error message
    - `.planning/phases/14-.../14-VALIDATION.md` § Test Infrastructure — the exact commands every later task's acceptance criteria quote
    - `.gitignore` line 17 (`.venv/`) — confirms nothing created here is committable
  </read_first>
  <files>server/.venv (created, not committed)</files>
  <action>
    `server/.venv` does not exist in this worktree; it exists and is fully provisioned in the
    main checkout at `/Users/florian/Projects/skypane/server/.venv`. Every harness file
    computes `REPO_ROOT` from its own `__file__` and inserts it at `sys.path[0]`, so the
    main checkout's interpreter run against this worktree's test files resolves the
    `companion`/`server` packages from *this worktree* — verified. Make
    `server/.venv/bin/python3` resolvable from the worktree root so every later acceptance
    command in this phase reads verbatim as VALIDATION.md states it, with no `PYTHON=`
    prefix an executor might drop.

    Prefer the network-free route: create `server/.venv` as a symlink to the main
    checkout's already-provisioned venv directory. A venv's `bin/python3` is itself a
    symlink to the system interpreter and locates its own `pyvenv.cfg` through the resolved
    path, so a symlinked venv directory behaves identically to a real one for this purpose.

    Fallback, only if the symlink route is unavailable: the real bootstrap
    `scripts/run-all-tests.sh` documents in its own error message —
    `python3 -m venv server/.venv` followed by installing `server/requirements.txt` and
    `server/requirements-dev.txt` into it. Add no package that is not already pinned in
    those two files; this phase introduces no dependency of any kind (14-CONTEXT.md phase
    boundary).

    Then record the three pre-change baselines in the plan's commit message so every later
    `EXPECTED_CHECK_COUNT` move in this phase is arithmetic against a measured number, not
    a carried-forward one: view-pages 54, status-pages 149, companion-app 159.

    Commit nothing under `server/.venv` — `.gitignore` already covers `.venv/`. This task's
    commit carries the baseline record only; if it would otherwise be empty, fold it into
    Task 2's commit and note the baselines there instead.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 and its last line is `view-pages: 54/54 checks pass`
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and its last line is `status-pages: 149/149 checks pass`
    - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 and its last line is `companion-app: 159/159 checks pass`
    - `git status --short` shows no tracked change under `server/.venv`
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py &amp;&amp; server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <done>All three harnesses run from the worktree root with the verbatim VALIDATION.md command and report their documented baseline counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the cross-file dialog contract self-enforcing (test_view_pages.py 54 → 55)</name>
  <read_first>
    - `companion/test_view_pages.py:1712-1761` — `_lightbox_dom_contract_three_file_guard()`, the guard being restructured, including its inline `tokens` tuple
    - `companion/test_view_pages.py:1763-1786` — `_airlines_lightbox_constants_match_history()`, the duplicated-not-imported precedent and this file's `check(name, fn)` idiom
    - `companion/test_view_pages.py:75` — `EXPECTED_CHECK_COUNT` and its trailing running-arithmetic comment convention
    - `companion/pages/airlines_page.py:100-140` — the four existing `_VIEW_PANEL_*_ATTR` constants and `LIGHTBOX_REPLACE_FORM_CLASS`
    - `companion/pages/history_page.py:225,385-400` — History's own `_VIEW_PANEL_CLOSE_ATTR` and `_lightbox_html()`, proving which tokens History genuinely renders
    - `.planning/phases/14-.../14-UI-SPEC.md` § Interaction Contract → "Cross-file guard" and the attribute-vocabulary table — the token list this guard will grow to cover
    - `.planning/phases/14-.../14-RESEARCH.md` Open Question 3 — the "yes, they need the same guard" answer
  </read_first>
  <files>companion/test_view_pages.py</files>
  <behavior>
    - Given today's source, the restructured guard passes with the identical six tokens it pins now, plus `data-view-panel-close` (shared) and `data-view-panel-replace-action` + `lightbox__replace` (Airlines-only).
    - Given a hypothetical new `_VIEW_PANEL_FOO_ATTR` constant added to `airlines_page.py` and not added to any of the three tuples, the new coverage check fails with a message naming the unclassified constant.
    - Given a token placed in the Airlines-only tuple that *does* appear in the rendered History page, the guard fails — the exclusion is asserted, not assumed.
    - Given a token placed in the render-only tuple, the guard asserts it appears in the rendered Airlines page and does **not** assert anything about `panel-lookup.js`.
  </behavior>
  <action>
    Replace `_lightbox_dom_contract_three_file_guard()`'s inline `tokens` tuple with three
    module-level tuples declared next to it, each with a docstring-grade comment stating its
    contract:

    - `_LIGHTBOX_SHARED_TOKENS` — must appear in `companion/static/panel-lookup.js`'s
      source, in the rendered History page, and in the rendered Airlines page. Seed with
      today's six (`LIGHTBOX_DIALOG_ID`, `data-view-panel-src`, `data-view-panel-caption`,
      `lightbox__image`, `lightbox__caption`, `lightbox__note`) plus `data-view-panel-close`,
      which every one of the three already carries today.
    - `_LIGHTBOX_AIRLINES_ONLY_TOKENS` — must appear in `panel-lookup.js`'s source and in
      the rendered Airlines page, and must be **absent** from the rendered History page.
      Seed with `data-view-panel-replace-action` and `lightbox__replace`. Assert the History
      absence explicitly; the existing `_history_lightbox_carries_zero_replace_markup()`
      check stays as it is and is not folded into this guard.
    - `_LIGHTBOX_RENDER_ONLY_TOKENS` — server-rendered vocabulary the script does not read
      *yet*: asserted present in the rendered Airlines page only, with no assertion at all
      against `panel-lookup.js`. Seed **empty**. Its comment must state plainly that this
      tuple is a staging area for one wave at most and that plan 14-05 empties it again.

    Keep the guard a single `check()` — restructuring its data, not splitting it — so
    `EXPECTED_CHECK_COUNT` moves by exactly the one new check below. Update the guard's
    own `check()` name string to describe the three-tuple contract.

    Add one new `check()`, `_view_panel_attr_constants_all_classified()`: enumerate
    `airlines_page`'s module namespace by reflection for every name matching the exact
    shape `_VIEW_PANEL_` … `_ATTR`, and assert each constant's *value* appears in exactly
    one of the three tuples. Reflection, never a hand-copied list — a hand-copied list is
    the same drift this guard exists to catch, one level up. Report the offending constant
    name and value on failure. Additionally assert the three tuples are pairwise disjoint,
    so a token can never be classified twice with contradictory expectations.

    Move `EXPECTED_CHECK_COUNT` 54 → 55 in the same commit, extending the existing trailing
    running-arithmetic comment in this file's own established style (`+ 1 (phase 14 plan
    14-01 Task 2: …)`), never rewriting the historical arithmetic already there.

    Work test-first within this task: add the coverage check and watch it fail against a
    deliberately-unclassified temporary constant (or by temporarily emptying a tuple), then
    restore and land green — the harness's exact-count gate means the commit itself must be
    green.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 and reports `view-pages: 55/55 checks pass`
    - `grep -n "_LIGHTBOX_SHARED_TOKENS\|_LIGHTBOX_AIRLINES_ONLY_TOKENS\|_LIGHTBOX_RENDER_ONLY_TOKENS" companion/test_view_pages.py` returns at least 6 lines (three declarations plus three uses inside the guard)
    - The guard function body contains no inline literal token tuple: `sed -n '/def _lightbox_dom_contract_three_file_guard/,/^    check(/p' companion/test_view_pages.py | grep -c 'data-view-panel-src'` returns `0`
    - The coverage check is reflection-driven: `sed -n '/def _view_panel_attr_constants_all_classified/,/^    check(/p' companion/test_view_pages.py | grep -c 'dir(' ` returns at least `1`
    - `_LIGHTBOX_RENDER_ONLY_TOKENS` is declared and empty at the end of this plan
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <done>The dialog contract guard reads three classified token tuples; adding an unclassified `_VIEW_PANEL_*_ATTR` constant to `airlines_page.py` now fails the harness by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Manual-registry fixture helper (test_status_pages.py 149 → 150)</name>
  <read_first>
    - `companion/test_status_pages.py:326-366` — `_mkstate()`, `_seed_unresolved_prefixes()`, `_ctx()`; the fixture-helper block this joins
    - `companion/test_status_pages.py:285` — `EXPECTED_CHECK_COUNT` and its running-arithmetic comment convention
    - `server/plane/manual_resolutions.py:296-380` — `add_entry()`'s validation order and its `ADD_*` result codes; the only sanctioned write path into the registry
    - `companion/pages/airlines_page.py:992-1024` — `_manual_resolution_rows()`, whose `(prefix, airline_name, created_at, superseded, needs_artwork)` tuples Waves 2-3 consume
    - `.planning/phases/14-.../14-VALIDATION.md` § Wave 0 Requirements — "reuse that helper, do not invent a second one"
  </read_first>
  <files>companion/test_status_pages.py</files>
  <behavior>
    - `_seed_manual_resolutions(state_dir, entries)` given `[("XQZ", "Totally Novel Airline")]` produces a state dir where `manual_resolutions.load_manual_resolutions(state_dir)` returns exactly one entry keyed `"XQZ"`.
    - The helper raises (never silently no-ops) if `add_entry()` returns anything other than `ADD_OK`, so a fixture that would have seeded nothing fails loudly instead of producing a vacuously-passing check downstream.
    - A prefix seeded through the helper renders in today's management table with its airline name.
  </behavior>
  <action>
    Add `_seed_manual_resolutions(state_dir, entries)` to the fixture-helper block, directly
    below `_seed_unresolved_prefixes()`. `entries` is an iterable of
    `(prefix, airline_name)` pairs; optionally accept a third `created_at` element per pair
    and pass it as `add_entry()`'s injectable `now` so a harness can pin timestamps.

    Seed through `manual_resolutions.add_entry()` — never by writing a JSON literal. The
    registry's on-disk shape and its validation order are `manual_resolutions.py`'s to own
    (14-CONTEXT.md's phase boundary forbids this phase touching that file); a fixture that
    hand-wrote the file would silently drift from it and would additionally bypass the very
    `load_manual_resolutions()` rebuild-from-scratch discipline downstream checks rely on.
    Assert each call returns `manual_resolutions.ADD_OK` and raise `AssertionError` naming
    the prefix and the returned code otherwise.

    Note in the helper's own comment that a superseded fixture is produced by choosing a
    prefix `enrich.static_airline_name_for_prefix()` already answers for — never by
    mutating the registry — because `_manual_resolution_rows()` derives `superseded` from
    that oracle alone (RESEARCH.md Pitfall 6) and Waves 2-3 must consume that derivation
    rather than re-deriving it.

    Add one `check()` that exercises the helper end-to-end against today's page: seed two
    entries (one that `enrich.static_airline_name_for_prefix()` answers for, one it does
    not), render `airlines_page.render(_ctx(tmp))`, and assert both airline names appear
    and that `_manual_resolution_rows()` reports `superseded` `True` for exactly the first.
    This check is written against today's management table and must survive its retirement
    in plan 14-06 by asserting on `_manual_resolution_rows()`'s tuples and the rendered
    airline names — not on `<table>`/`<tr>` markup that plan deletes.

    Move `EXPECTED_CHECK_COUNT` 149 → 150, extending the trailing running-arithmetic
    comment in this file's own style.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `status-pages: 150/150 checks pass`
    - `grep -c "def _seed_manual_resolutions" companion/test_status_pages.py` returns `1`
    - The helper writes through the sanctioned API: `sed -n '/def _seed_manual_resolutions/,/^def /p' companion/test_status_pages.py | grep -c 'add_entry'` returns at least `1`
    - The helper writes no JSON literal: `sed -n '/def _seed_manual_resolutions/,/^def /p' companion/test_status_pages.py | grep -c 'json.dump'` returns `0`
    - The new check asserts on `_manual_resolution_rows()` output, not on `<tr>` markup — `grep -c "MANUAL_RESOLUTION_HEADERS" ` inside the new check's slice returns `0`
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>A single sanctioned manual-registry seeding path exists, is exercised by a check that outlives the management table, and the status-pages harness reports 150/150.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by **this plan** (the phase-wide list is assembled across all eight plans):

| Kind | Symbol | File |
|---|---|---|
| test constant | `_LIGHTBOX_SHARED_TOKENS` | `companion/test_view_pages.py` |
| test constant | `_LIGHTBOX_AIRLINES_ONLY_TOKENS` | `companion/test_view_pages.py` |
| test constant | `_LIGHTBOX_RENDER_ONLY_TOKENS` | `companion/test_view_pages.py` |
| test function | `_view_panel_attr_constants_all_classified()` | `companion/test_view_pages.py` |
| test helper | `_seed_manual_resolutions(state_dir, entries)` | `companion/test_status_pages.py` |
| count move | `EXPECTED_CHECK_COUNT` 54 → 55 | `companion/test_view_pages.py` |
| count move | `EXPECTED_CHECK_COUNT` 149 → 150 | `companion/test_status_pages.py` |

No CSS class, data attribute, route query parameter or runtime function is created by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → fixture helper | `_seed_manual_resolutions()` writes into a `tempfile.mkdtemp()` state dir only; no path is taken from any external source |
| package index → `server/.venv` | Only reached on Task 1's fallback route, and only for the repository's own already-pinned requirements files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-SC | Tampering | `server/.venv` bootstrap (Task 1) | medium | mitigate | Prefer the network-free symlink to the main checkout's already-provisioned venv. If `pip` is used at all it installs **only** `server/requirements.txt` and `server/requirements-dev.txt`, both already pinned in-repo since Phase 4. This phase adds **zero** new packages (14-RESEARCH.md § Package Legitimacy Audit: "not applicable"), so no `[ASSUMED]`/`[SUS]` package exists and no blocking legitimacy checkpoint is required. Adding any package not already in those two files is out of scope and a signal the plan was misread. |
| T-14-07 | Tampering | `_seed_manual_resolutions()` | low | mitigate | Seeds exclusively through `manual_resolutions.add_entry()`. A hand-written JSON fixture would bypass the registry's own validation order and let a later check pass against a shape production can never produce — the exact false-confidence failure a fixture is supposed to prevent. Pinned by the `json.dump` negative gate in the acceptance criteria. |
| T-14-08 | Repudiation | `EXPECTED_CHECK_COUNT` arithmetic | low | accept | The count is re-derived from the real on-disk `check()` count at merge time (this file's own documented convention), so a mis-stated running comment degrades to a loud harness failure, never a silent one. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_view_pages.py` → `view-pages: 55/55 checks pass`
- `server/.venv/bin/python3 companion/test_status_pages.py` → `status-pages: 150/150 checks pass`
- `server/.venv/bin/python3 companion/test_companion_app.py` → `companion-app: 159/159 checks pass` (untouched by this plan; run to prove no collateral)
- `scripts/run-all-tests.sh` exits 0
- Zero files under `companion/pages/`, `companion/static/` or `server/` are modified by this plan — `git diff --name-only` lists only the two harness files
</verification>

<success_criteria>
- Every documented harness command in VALIDATION.md runs verbatim from this worktree
- The dialog contract guard is data-driven over three classified token tuples with `_LIGHTBOX_RENDER_ONLY_TOKENS` empty
- An unclassified `_VIEW_PANEL_*_ATTR` constant fails `companion/test_view_pages.py`
- One sanctioned manual-registry seeding helper exists and is exercised
- Both moved `EXPECTED_CHECK_COUNT` values match their real on-disk check count
</success_criteria>

<output>
Create `.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-01-SUMMARY.md` when done
</output>

