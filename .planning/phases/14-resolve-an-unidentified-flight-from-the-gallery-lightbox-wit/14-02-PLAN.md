---
phase: 14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/test_view_pages.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The resolve-name form, the resolve-upload form, the sighting-context block and the delete form each come from exactly one Python rendering function, called once for the no-JS fallback and once for the dialog."
    - "With JavaScript disabled, an operator who lands on /airlines?resolve={prefix} for a prefix that has a manual entry can delete that entry — the capability D-12 promised would survive is literally intact (D-09 amendment)."
    - "The shared dialog carries every element the three card modes need, all server-rendered once, with no duplicate DOM id anywhere on the page."
    - "Every trigger on the Airlines page carries the full data-view-panel-* vocabulary, including empty values, so no attribute write can ever be conditionally skipped at click time."
  artifacts:
    - "companion/pages/airlines_page.py: _resolve_name_form_html(), _resolve_upload_form_html(), _manual_delete_form_html(), _resolve_context_html(row, now, id_suffix)"
    - "companion/pages/airlines_page.py: the eleven new _VIEW_PANEL_*_ATTR constants and their mode/manual value constants"
    - "companion/pages/airlines_page.py: the Phase 14 copy-deck constants (UI-SPEC Full Copy Deck)"
    - "An extended _lightbox_html() carrying heading, manual-note, resolve-context, resolve-name form, resolve-upload zone and delete form before Close"
  key_links:
    - "_resolve_section_html() calls the same four rendering functions the dialog calls — grep proves two call sites per function, never two definitions"
    - "Dialog-side calls pass id_suffix='-dialog'; fallback-side calls pass id_suffix='' so every Phase 13 element id is byte-identical"
    - "Every new _VIEW_PANEL_*_ATTR constant is classified into _LIGHTBOX_RENDER_ONLY_TOKENS by this plan and promoted by plan 14-05"
---

<objective>
Build the shared machinery every later wave consumes: the copy deck, the trigger attribute
vocabulary, the four one-definition-two-call-sites rendering functions, the extended
dialog, and the no-JS fallback's newly-required delete form.

Purpose: 14-CONTEXT.md's Claude's Discretion #1 is binding — "that they must not be two
independent copies is not optional" — and the D-09 amendment (2026-09-06) extends that rule
to the delete form. Every subsequent plan in this phase renders *through* these functions
or *reads* these attributes, so getting the factoring right here is what stops the two
rendering paths drifting for the rest of the phase.

Output: `airlines_page.py` gains ~11 attribute constants, ~10 copy constants, 4 shared
rendering functions, an extended `_lightbox_html()`, an attribute-complete
`_airline_card_html()`, and a `_resolve_section_html()` that renders delete in both of its
entry-bearing branches. `test_view_pages.py` classifies the new vocabulary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-CONTEXT.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-UI-SPEC.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-PATTERNS.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-RESEARCH.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Phase 14 copy deck and trigger attribute vocabulary</name>
  <read_first>
    - `companion/pages/airlines_page.py:100-140` — the four existing `_VIEW_PANEL_*_ATTR` constants and the `LIGHTBOX_*_CLASS` block this joins
    - `companion/pages/airlines_page.py:218-345` — the Phase 13 copy-constant block, including `RESOLVE_CAPTION_TEMPLATE`, `SUPERSEDED_MARKER_TEXT`, `DELETE_BUTTON_TEXT`, and the em-dash/apostrophe convention its own comment states
    - `companion/pages/airlines_page.py:650-656` — `_FILTER_LABEL_TEXT` and the filter-bar copy block
    - `companion/pages/airlines_page.py:188-207` — the eight existing `FLASH_MANUAL_*` constants
    - `.planning/phases/14-.../14-UI-SPEC.md` § Copywriting Contract → **Full Copy Deck** (the binding table) and § Interaction Contract → **Attribute vocabulary** (the binding table)
    - `.planning/phases/13-.../13-UAT.md` § G-01 — why one more flash-key constant is declared here
  </read_first>
  <files>companion/pages/airlines_page.py</files>
  <action>
    Add, in the module's existing constant blocks and in its existing voice (real U+2014 em
    dashes, real U+2019 apostrophes, one constant per string so the harness asserts against
    the constant and never a duplicated literal):

    **Attribute vocabulary** (UI-SPEC's binding table, verbatim attribute names), beside the
    four existing `_VIEW_PANEL_*_ATTR` constants: `_VIEW_PANEL_HEADING_ATTR`,
    `_VIEW_PANEL_MODE_ATTR`, `_VIEW_PANEL_MANUAL_ATTR`, `_VIEW_PANEL_SCOPE_ATTR`,
    `_VIEW_PANEL_RESOLVE_PREFIX_ATTR`, `_VIEW_PANEL_FIRST_SEEN_ATTR`,
    `_VIEW_PANEL_LAST_SEEN_ATTR`, `_VIEW_PANEL_COUNT_ATTR`,
    `_VIEW_PANEL_UPLOAD_ACTION_ATTR`, `_VIEW_PANEL_DELETE_ACTION_ATTR`,
    `_VIEW_PANEL_MANUAL_NOTE_ATTR`. Their values are exactly the `data-view-panel-*` names
    UI-SPEC's table gives.

    **Value vocabularies**, so no mode/manual string is ever a bare literal at a call site:
    `_VIEW_PANEL_MODE_ART = "art"`, `_VIEW_PANEL_MODE_GAP = "gap"`,
    `_VIEW_PANEL_MODE_NEEDS_ARTWORK = "needs-artwork"`, `_VIEW_PANEL_MANUAL_ACTIVE = "active"`,
    `_VIEW_PANEL_MANUAL_SUPERSEDED = "superseded"`.

    **Class constants** for the new dialog elements, and for the two existing generic
    classes the dialog now also consumes (promoted from bare literals so plan 14-01's guard
    can pin them): `LIGHTBOX_HEADING_CLASS`, `LIGHTBOX_MANUAL_NOTE_CLASS`,
    `LIGHTBOX_RESOLVE_NAME_CLASS`, `LIGHTBOX_DELETE_CLASS`, `RESOLVE_UPLOAD_ZONE_CLASS`
    (existing value `resolve-upload-zone`), `RESOLVE_CONTEXT_CLASS` (existing value
    `resolve-context`). Replace the corresponding bare literals in
    `_resolve_context_html()` and `_resolve_section_html()`'s upload zone with these
    constants in the same edit — a promoted constant that leaves its old literal behind is a
    second source of truth, which is the whole failure mode being closed.

    Also add `RESOLVE_CONTEXT_DD_CLASSES` — the five per-`<dd>` hook classes UI-SPEC's
    Component Inventory requires (`resolve-context__prefix`, `-first-seen`, `-last-seen`,
    `-count`, `-callsign`) as a 5-tuple aligned 1:1 with the existing
    `RESOLVE_CONTEXT_LABELS` tuple, so the two can only ever be zipped, never mismatched.

    **Copy deck** (UI-SPEC's Full Copy Deck, byte for byte):
    - `RESOLVE_CAPTION_TEMPLATE` — **reworded**, replacing its Phase 13 wording, to
      "Every flight using prefix %s will show as this airline once you save a name."
      One template with no callsign clause, so it stays true on the CR-02 row-gone path
      where no sighting data survives. This wording is what discharges D-01's obligation
      that the dialog state the per-prefix scope at the moment of acting.
    - `GAP_CARD_ARIA_TEMPLATE = "Resolve prefix %s — example callsign %s"` (two `%s`:
      prefix, then callsign).
    - `MANUAL_CHIP_ACTIVE_TEXT = "Resolved by hand"`.
    - `MANUAL_DELETE_CAPTION = "Deleting removes only this manual name — any uploaded
      artwork stays in place."`
    - `MANUAL_SUPERSEDED_NOTE_TEMPLATE` — UI-SPEC's four-`%s` sentence (prefix, built-in
      name, operator's name, built-in name again).
    - `MANUAL_OVERFLOW_TEMPLATE = "%d other unresolved prefixes — "` and
      `MANUAL_OVERFLOW_LINK_TEXT = "see the full list"`. Two constants rather than one,
      because only the tail is the `<a>`'s text; their concatenation must equal UI-SPEC's
      single deck string and a harness check in plan 14-04 asserts exactly that.
    - `MANUAL_SUMMARY_TEMPLATE = "%d manual resolutions, %d superseded"` and
      `MANUAL_SUMMARY_TEMPLATE_NONE = "%d manual resolutions"` — the with-superseded and
      no-superseded forms UI-SPEC specifies.
    - `_FILTER_LABEL_TEXT` — **reworded** to "Filter by airline or callsign".

    **One flash key, for Phase 13's gap G-01**: `FLASH_MANUAL_NAME_UNUSABLE =
    "manual_name_unusable"`, declared here beside the eight existing `FLASH_MANUAL_*`
    constants because this module is where every manual flash key is defined and re-exported
    from. Its comment must state plainly: (a) it is wired into `companion/app.py` by plan
    14-07, not by this plan; (b) it closes `13-UAT.md`'s G-01, where a name the operator
    genuinely typed but which the key-slugger could not use was reported back as an *empty*
    name; (c) it is a deliberate, recorded extension of `14-UI-SPEC.md`'s Copywriting
    Contract, whose Error-state row scoped this phase to "no new flash key" before
    `14-CONTEXT.md`'s Deferred Ideas elevated G-01 into this phase's scope. Declaring it
    here and wiring it in 14-07 is what lets G-01 be fixed without two plans contending for
    `airlines_page.py` in the same wave.

    Every value that is a template keeps its `%`-placeholder arity documented in a comment.
    Add no constant this task's list does not name; the deck is closed.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 (plan 14-01's coverage check will fail until Task 3 classifies the new attribute constants — this task and Task 3 may share one commit if that ordering is convenient, but the tree must be green at every commit)
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0
    - `grep -c "^_VIEW_PANEL_.*_ATTR = " companion/pages/airlines_page.py` returns `15` (4 existing + 11 new)
    - The promoted class constants leave no stray literal behind: `grep -c '"resolve-context"' companion/pages/airlines_page.py` returns `1` (the constant's own definition) and `grep -c '"resolve-upload-zone"' companion/pages/airlines_page.py` returns `1`
    - `python3 -c "import companion.pages.airlines_page as a; assert (a.MANUAL_OVERFLOW_TEMPLATE + a.MANUAL_OVERFLOW_LINK_TEXT) == '%d other unresolved prefixes — see the full list'"` succeeds
    - `python3 -c "import companion.pages.airlines_page as a; assert len(a.RESOLVE_CONTEXT_DD_CLASSES) == len(a.RESOLVE_CONTEXT_LABELS) == 5"` succeeds
    - `grep -c "FLASH_MANUAL_NAME_UNUSABLE" companion/pages/airlines_page.py` returns at least `1`
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>The full Phase 14 attribute vocabulary, class constants and copy deck exist as named module constants with no duplicated literals, and G-01's flash key is declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four shared rendering functions, and the no-JS fallback's delete form (D-09 amendment)</name>
  <read_first>
    - `companion/pages/airlines_page.py:809-947` — `_resolve_section_html()` in full: the four branches (stale/invalid, Step A, Step B, already-resolved), the inline Step A name-field block (~877-897), the inline Step B upload-zone block (~920-937), and its CR-02 docstring paragraph
    - `companion/pages/airlines_page.py:761-790` — `_resolve_context_html(row, now)`, the function gaining an `id_suffix` parameter and per-`<dd>` classes
    - `companion/pages/airlines_page.py:793-807` — `_known_airlines_datalist_html()`, whose `<datalist id>` must become suffixable
    - `companion/pages/airlines_page.py:956-961` and `:1046-1050`, `:1116-1120` — `_manual_delete_action()` and its **two existing call sites**: this codebase's own precedent for one string-builder feeding two differing markup contexts (14-PATTERNS.md names this as the exact analog)
    - `companion/pages/airlines_page.py:420-491` — `_lightbox_replace_form_html()`, and specifically its docstring's rule that `action=""` is a real, present placeholder the script overwrites, never an omitted attribute
    - `.planning/phases/14-.../14-UI-SPEC.md` § Component Inventory → the four-row function-signature table, the "**Why two ids, not one**" paragraph, and the `_manual_delete_form_html()` no-`id_suffix` paragraph
    - `.planning/phases/14-.../14-UI-SPEC.md` § Page Composition → `_resolve_section_html()`'s expanded responsibility table (which branches gain the delete form)
    - `.planning/phases/14-.../14-CONTEXT.md` § D-09 amendment (2026-09-06)
  </read_first>
  <files>companion/pages/airlines_page.py</files>
  <behavior>
    - `_resolve_name_form_html(prefix_value, id_suffix)` called with `id_suffix=""` produces markup whose input id is `manual-airline-name` and whose datalist id is `known-airlines` — byte-identical to what Phase 13's inline block produces today, so no existing check or `<label for>` association breaks.
    - The same function called with `prefix_value=""`, `id_suffix="-dialog"` produces `manual-airline-name-dialog` / `known-airlines-dialog`, and its hidden prefix input's `value` is empty.
    - Rendering `/airlines?resolve={prefix}` for a prefix with **no** manual entry (Step A) emits **zero** delete forms.
    - Rendering the same URL for a prefix that **has** a manual entry with no artwork (Step B) emits exactly one delete form, pointing at `_manual_delete_action(prefix)`.
    - Rendering the same URL for a prefix whose manual entry already has artwork (already-resolved branch) emits exactly one delete form.
    - Rendering a stale/invalid prefix emits zero delete forms.
    - `_resolve_context_html(row, now, id_suffix="")` and `..., id_suffix="-dialog")` produce structurally identical `<dl>`s — same number of `<dt>`/`<dd>` pairs, same five per-`<dd>` hook classes — differing only where an id is involved.
  </behavior>
  <action>
    Extract the three markup blocks currently inline in `_resolve_section_html()` into
    standalone functions, and add the fourth. Follow `_manual_delete_action()`'s own
    documented precedent exactly: one definition, two call sites, the differing value passed
    as an argument.

    - `_resolve_name_form_html(prefix_value, id_suffix)` — lifts the Step A `<form
      method="post" action="{RESOLVE_ROUTE}">` wrapper, its hidden `prefix` input, the
      `.resolve-name-field` block (label, text input with `list=`, `maxlength="100"`,
      `required`, `autocomplete="off"`, `autofocus`), the `<datalist>`, the hint caption and
      the submit button. `id_suffix` is appended to `MANUAL_NAME_INPUT_ID` and
      `MANUAL_DATALIST_ID` in **both** the `id`/`list`/`for` positions so the association
      survives; when `id_suffix` is `""` every emitted id is byte-identical to today's.
      `prefix_value` is interpolated into the hidden input's `value` through
      `escape_html()` exactly once.
    - `_resolve_upload_form_html(action, id_suffix)` — lifts the Step B
      `.resolve-upload-zone` block (upload icon via `layout.icon_html("icon-upload", ...)`,
      label, hint, the multipart `<form>`, the file input, the submit button). `action` is
      interpolated into the form's `action` attribute; the dialog call site passes `""`, and
      that empty string must be emitted as a **real, present** `action=""` attribute, never
      omitted — the same rule `_lightbox_replace_form_html()`'s docstring already states,
      for the same reason (`panel-lookup.js` overwrites an existing attribute rather than
      creating one). `id_suffix` is appended to `MANUAL_UPLOAD_INPUT_ID` in both the `id`
      and `for` positions.
    - `_manual_delete_form_html(action)` — new, and deliberately **without** an `id_suffix`
      parameter: its output names no id at all (`<form class="{LIGHTBOX_DELETE_CLASS}"
      method="post" action="…">`, a `.text-label section-caption` paragraph carrying
      `MANUAL_DELETE_CAPTION`, and a `<button type="submit">{DELETE_BUTTON_TEXT}</button>`),
      so two copies on one page cannot collide. UI-SPEC's Component Inventory states this
      reasoning; carry it into the function's own docstring so a later editor does not
      "fix" the asymmetry by adding a suffix parameter it does not need.
    - `_resolve_context_html(row, now, id_suffix="")` — extend the existing function rather
      than replacing it. Each `<dd>` additionally carries its own hook class from
      `RESOLVE_CONTEXT_DD_CLASSES`, in **both** call modes, so the dialog's copy has stable
      JS hooks and the two copies stay structurally identical. Accept `row=None` and emit
      five empty `<dd>`s in that case — that is how the dialog's placeholder copy is built.
      Preserve the existing discipline byte for byte: first-seen/last-seen go through
      `layout.concise_timestamp_html(value, now, fallback="")` and are interpolated verbatim
      as already-safe markup, never re-escaped; every other value goes through
      `escape_html()` exactly once.

    Rewire `_resolve_section_html()` to call all four with real values and `id_suffix=""`.
    Its four-branch derivation logic — the CR-02 fallthrough, the `prefix is None` guard,
    the corrupt-key guard — is **not** touched; only the markup emission moves. Its
    docstring gains a paragraph naming the four shared functions and stating that the dialog
    is their second call site.

    Then apply the D-09 amendment: `_resolve_section_html()` additionally renders
    `_manual_delete_form_html(_manual_delete_action(prefix))` in exactly the two branches
    that have reached an `entry` — Step B (beneath the upload zone and skip link) and
    already-resolved (beneath the body text) — and in **neither** of the two entry-less
    branches (stale/invalid, Step A). This is the same rule the dialog encodes via `manual`
    being `active` or `superseded`; state that equivalence in the code comment so the two
    render sites are visibly one rule.

    Do not change `_resolve_section_html()`'s position in `render()` yet — the page-order
    move (resolve section from top to bottom) belongs to plan 14-04, which owns the page
    composition.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 with its count unchanged from plan 14-01's 150 (every Phase 13 check must still pass unmodified — that is the proof the `id_suffix=""` path is byte-compatible; if a check genuinely needs retargeting, update it in place with no count change and say so in the SUMMARY)
    - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0 at 159 — the POST routes are untouched
    - Exactly one definition each: `grep -c "^def _resolve_name_form_html" companion/pages/airlines_page.py` → `1`; same for `_resolve_upload_form_html`, `_manual_delete_form_html`
    - Two call sites each after Task 3 lands: `grep -c "_resolve_name_form_html(" companion/pages/airlines_page.py` → `3` (one definition + two calls); same arithmetic for `_resolve_upload_form_html` and `_manual_delete_form_html`
    - `_manual_delete_form_html` takes no id suffix: `sed -n '/^def _manual_delete_form_html/p' companion/pages/airlines_page.py | grep -c 'id_suffix'` returns `0`
    - Behavioural, via a throwaway REPL against a seeded state dir: Step A renders 0 occurrences of `LIGHTBOX_DELETE_CLASS`; Step B renders 1; already-resolved renders 1; stale renders 0
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py &amp;&amp; server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <done>Four rendering functions have one definition each; `_resolve_section_html()` calls all four and renders delete in exactly its two entry-bearing branches; every Phase 13 element id is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the shared dialog and complete every trigger's attribute vocabulary</name>
  <read_first>
    - `companion/pages/airlines_page.py:601-648` — `_lightbox_html()` and its element-order docstring ("image, caption, note, replace form, Close … Close stays last")
    - `companion/pages/airlines_page.py:494-584` — `_airline_card_html()`, the `zoom_html` attribute block being widened, and its escape-once docstring
    - `.planning/phases/14-.../14-UI-SPEC.md` § Page Composition → the new top-to-bottom order block, and § Interaction Contract → the **Visibility toggle table** plus the **Correctness rule** paragraph ("never conditionally skipped")
    - `.planning/phases/14-.../14-UI-SPEC.md` § Component Inventory → the dialog-side argument column of the function table (`prefix_value=""`, `action=""`, `row=None`, `id_suffix="-dialog"`)
    - `companion/test_view_pages.py` — the three token tuples plan 14-01 created, and `_view_panel_attr_constants_all_classified()`
    - `.planning/phases/14-.../14-RESEARCH.md` Pitfall 3 — why each optional form's own primary control keeps its `autofocus`
  </read_first>
  <files>companion/pages/airlines_page.py, companion/test_view_pages.py</files>
  <behavior>
    - `airlines_page.render({})` emits exactly one `<dialog id="panel-lookup-dialog">` containing, in order: image, caption, note, heading, manual-note, resolve-context, resolve-name form, resolve-upload zone, replace form, delete form, Close.
    - The rendered page contains no duplicate DOM id: every id emitted inside the dialog carries the `-dialog` suffix, and every id emitted by the no-JS fallback section does not.
    - Every `.airline-card__zoom` trigger carries all fifteen `data-view-panel-*` attributes, with `data-view-panel-mode="art"` and every attribute that does not apply to a plain curated card present with an empty value.
    - `companion/test_view_pages.py`'s reflection coverage check passes: all eleven new attribute constants are classified, and all sit in `_LIGHTBOX_RENDER_ONLY_TOKENS` at the end of this plan.
  </behavior>
  <action>
    **Dialog.** Extend `_lightbox_html()`'s flat concatenation, keeping Close last (its
    docstring's stated invariant) and preserving the existing image/caption/note prefix
    byte-identically. Insert, in UI-SPEC's binding order, between the note and Close:

    1. `<h2 class="{LIGHTBOX_HEADING_CLASS}">` — empty; the script fills it via
       `textContent`. Its `:empty` collapse rule is CSS's job (plan 14-03), not this
       function's.
    2. `<p class="{LIGHTBOX_MANUAL_NOTE_CLASS} text-body">` — empty, same idiom as the
       existing `.lightbox__note:empty`.
    3. `_resolve_context_html(None, None, id_suffix="-dialog")` — five empty `<dd>`s
       carrying their five hook classes.
    4. `_resolve_name_form_html("", "-dialog")`.
    5. `_resolve_upload_form_html("", "-dialog")`.
    6. `_lightbox_replace_form_html()` — existing, unchanged, in its existing relative
       position ahead of Close.
    7. `_manual_delete_form_html("")`.

    Every form's `action` and every `<input>`'s value is the empty *placeholder*, present as
    a real attribute. Do not emit any element `hidden` from the server: the initial
    visibility state is the script's to set, and RESEARCH.md Pitfall 3 requires that state
    be final *before* `showModal()` runs, which is a runtime ordering concern plan 14-05
    owns. Update `_lightbox_html()`'s docstring to state the new element order and to name
    which of its children are optional-and-script-toggled.

    Keep each optional form's own primary control's `autofocus` attribute as the shared
    render functions emit it — do not strip it and do not add a second one. Pitfall 3's
    analysis depends on the browser skipping a non-rendered `autofocus` and falling through
    to the next focusable control; removing them would break the fallback path's focus
    behaviour for no gain.

    **Triggers.** Widen `_airline_card_html()`'s `zoom_html` attribute block so every
    trigger carries the complete vocabulary. For a plain curated art card the values are:
    `mode="art"`; `src`/`caption`/`replace-action` exactly as today; and
    `heading`, `manual`, `scope`, `resolve-prefix`, `first-seen`, `last-seen`, `count`,
    `upload-action`, `delete-action`, `manual-note` **all present and empty**. Emitting them
    empty rather than omitting them is not cosmetic: UI-SPEC's Correctness rule requires the
    script to copy every attribute on every open using the `attr || ""` idiom, and an
    omitted attribute is precisely how the previous click's content leaks onto the next
    card. Every interpolated value still goes through `escape_html()` exactly once at the
    point of interpolation (T-06.6.4.1-05); the existing already-escaped `image_url`
    variable must not be escaped a second time — its own comment says so.

    Leave `_airline_card_html()`'s outer element a `<div>` and its trigger a
    `<button type="button">` for now. The `<a>` generalisation applies only to cards
    carrying a real resolve prefix, which do not exist until plans 14-04/14-06.

    **Classification.** In `companion/test_view_pages.py`, add all eleven new
    `data-view-panel-*` values and the four new/promoted dialog class tokens
    (`lightbox__heading`, `lightbox__manual-note`, `lightbox__resolve-name`,
    `lightbox__delete`) to `_LIGHTBOX_RENDER_ONLY_TOKENS` — server-rendered now, not yet
    read by the script. Add `resolve-context` and `resolve-upload-zone` there too, since the
    dialog now renders both and plan 14-05 will query both. Add no new `check()` and do not
    move `EXPECTED_CHECK_COUNT`: this is a data change inside the existing guard.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 and reports `view-pages: 55/55 checks pass`
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0
    - `scripts/run-all-tests.sh` exits 0
    - Exactly one dialog: `python3 -c "import companion.pages.airlines_page as a; print(a.render({}).count('<dialog'))"` prints `1`
    - No duplicate id — a throwaway script that extracts every `id="..."` from `airlines_page.render(ctx)` for a state dir seeded with a live gap **and** `resolve_prefix` set (so the fallback section renders simultaneously with the dialog) finds zero repeated values
    - Every attribute present on every trigger: for the rendered page, each of the fifteen `data-view-panel-*` names occurs at least once inside every `<button class="airline-card__zoom"` … `>` slice
    - `_LIGHTBOX_RENDER_ONLY_TOKENS` is non-empty and every one of its members is absent from `companion/static/panel-lookup.js` (the tuple's contract — the script has not learned them yet)
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py &amp;&amp; server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>The shared dialog carries every element the three modes need, every trigger carries the complete attribute vocabulary, there are no duplicate DOM ids, and the new tokens are classified as render-only pending plan 14-05.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

| Kind | Symbol | File |
|---|---|---|
| data attribute | `data-view-panel-heading` (`_VIEW_PANEL_HEADING_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-mode` (`_VIEW_PANEL_MODE_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-manual` (`_VIEW_PANEL_MANUAL_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-scope` (`_VIEW_PANEL_SCOPE_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-resolve-prefix` (`_VIEW_PANEL_RESOLVE_PREFIX_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-first-seen` / `-last-seen` / `-count` | `airlines_page.py` |
| data attribute | `data-view-panel-upload-action` (`_VIEW_PANEL_UPLOAD_ACTION_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-delete-action` (`_VIEW_PANEL_DELETE_ACTION_ATTR`) | `airlines_page.py` |
| data attribute | `data-view-panel-manual-note` (`_VIEW_PANEL_MANUAL_NOTE_ATTR`) | `airlines_page.py` |
| value constant | `_VIEW_PANEL_MODE_ART` / `_MODE_GAP` / `_MODE_NEEDS_ARTWORK` | `airlines_page.py` |
| value constant | `_VIEW_PANEL_MANUAL_ACTIVE` / `_MANUAL_SUPERSEDED` | `airlines_page.py` |
| CSS class | `lightbox__heading` (`LIGHTBOX_HEADING_CLASS`) | `airlines_page.py` → `style.css` (14-03) |
| CSS class | `lightbox__manual-note` (`LIGHTBOX_MANUAL_NOTE_CLASS`) | `airlines_page.py` → `style.css` (14-03) |
| CSS class | `lightbox__resolve-name` (`LIGHTBOX_RESOLVE_NAME_CLASS`) | `airlines_page.py` → `style.css` (14-03) |
| CSS class | `lightbox__delete` (`LIGHTBOX_DELETE_CLASS`) | `airlines_page.py` → `style.css` (14-03) |
| CSS class | `resolve-context__prefix` / `-first-seen` / `-last-seen` / `-count` / `-callsign` (`RESOLVE_CONTEXT_DD_CLASSES`) | `airlines_page.py` |
| constant (promoted) | `RESOLVE_UPLOAD_ZONE_CLASS`, `RESOLVE_CONTEXT_CLASS` | `airlines_page.py` |
| function | `_resolve_name_form_html(prefix_value, id_suffix)` | `airlines_page.py` |
| function | `_resolve_upload_form_html(action, id_suffix)` | `airlines_page.py` |
| function | `_manual_delete_form_html(action)` | `airlines_page.py` |
| signature change | `_resolve_context_html(row, now, id_suffix="")` | `airlines_page.py` |
| DOM ids | `manual-airline-name-dialog`, `known-airlines-dialog`, `manual-illustration-input-dialog` | rendered dialog |
| copy constant | `GAP_CARD_ARIA_TEMPLATE`, `MANUAL_CHIP_ACTIVE_TEXT`, `MANUAL_DELETE_CAPTION`, `MANUAL_SUPERSEDED_NOTE_TEMPLATE`, `MANUAL_OVERFLOW_TEMPLATE`, `MANUAL_OVERFLOW_LINK_TEXT`, `MANUAL_SUMMARY_TEMPLATE`, `MANUAL_SUMMARY_TEMPLATE_NONE` | `airlines_page.py` |
| copy reworded | `RESOLVE_CAPTION_TEMPLATE`, `_FILTER_LABEL_TEXT` | `airlines_page.py` |
| flash key | `FLASH_MANUAL_NAME_UNUSABLE = "manual_name_unusable"` (wired by 14-07) | `airlines_page.py` |

No route, no query parameter and no server-side symbol is created by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `poll_state.json` → rendered dialog attributes | Sighting context (prefix, first/last seen, count, example callsign) originates in live ADS-B traffic and is operator-adjacent, not operator-authored; it crosses into HTML attribute values here |
| `manual_resolutions.json` → rendered markup | The airline name is genuinely operator-typed and crosses into both a form value and a note sentence |
| operator URL (`?resolve={prefix}`) → `_resolve_section_html()` | Unchanged from Phase 13: validate-then-join against the server-written registry, never sanitise-then-use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering (stored XSS) | `_resolve_name_form_html()`, `_resolve_upload_form_html()`, `_resolve_context_html()`, `_manual_delete_form_html()`, `_airline_card_html()`'s widened attribute block | high | mitigate | Every interpolated value passes `escape_html()` **exactly once, at the point of interpolation** (T-06.6.4.1-05). The two documented exceptions stay exceptions: `layout.concise_timestamp_html()`'s return is already-safe markup interpolated verbatim, and `image_url` is escaped at construction and must not be re-escaped. Pinned by the existing hostile-name escaping check in `test_status_pages.py` (quick 260902-v26), which plan 14-04 extends onto gap cards. |
| T-14-09 | Tampering (DOM integrity) | duplicate DOM ids when the dialog and the no-JS fallback both render | high | mitigate | The `-dialog` id suffix on every id-bearing child of the dialog, and `_manual_delete_form_html()` naming no id at all. Duplicate ids break `<label for>` association and make `<datalist>` resolution undefined — an accessibility defect, and a foothold for confusing which control a submit actually targets. Pinned by the no-duplicate-id acceptance criterion, rendered with `resolve_prefix` set so both surfaces are on the page simultaneously. |
| T-14-10 | Elevation of Privilege | the delete form newly rendered inside the no-JS fallback section, reachable at `GET /airlines?resolve={prefix}` | medium | mitigate | **No new route and no new gate.** `POST /airlines/manual-resolutions/{prefix}/delete` is Phase 13's route, unmodified; `GET /airlines` already sits behind the existing session gate; `SameSite=Strict` on the session cookie remains this site's single documented CSRF control (`companion/auth.py:132`), and a second mechanism must not be invented for this form alone. The change is *where the control is rendered*, not who may reach it. Confirm during execution that the rendered fallback section is unreachable without a session — the existing route-gating checks in `test_companion_app.py` already assert this for `GET /airlines` and must stay green. |
| T-14-11 | Information Disclosure | `MANUAL_SUPERSEDED_NOTE_TEMPLATE` interpolating the operator's own name beside the built-in name | low | accept | Both values are already displayed to the same single-operator session elsewhere on the page; the note adds no new disclosure, only an explanation. Escaped once at interpolation like every other value. |
| T-14-SC | Tampering | package installs | n/a | accept | This plan adds no dependency. `14-RESEARCH.md` § Package Legitimacy Audit: "not applicable — no `pip install`, no `npm install`, no new vendored library." No `[ASSUMED]`/`[SUS]` package exists, so no blocking legitimacy checkpoint applies. |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` exits 0
- `server/.venv/bin/python3 companion/test_view_pages.py` → 55/55
- `server/.venv/bin/python3 companion/test_status_pages.py` → 150/150
- `server/.venv/bin/python3 companion/test_companion_app.py` → 159/159 (proof no POST route moved)
- `git diff --name-only` lists only `companion/pages/airlines_page.py` and `companion/test_view_pages.py` — **zero** files under `server/`, and `companion/app.py` untouched
- Grep proof of the one-definition/two-call-sites contract for all four shared functions
</verification>

<success_criteria>
- Four shared rendering functions, one definition each, two call sites each
- Delete renders in the no-JS fallback's two entry-bearing branches and nowhere else (D-09 amendment)
- Every Phase 13 element id is byte-identical on the `id_suffix=""` path
- The dialog carries every mode's elements in UI-SPEC's binding order with Close last
- Every trigger carries all fifteen `data-view-panel-*` attributes, empties included
- No duplicate DOM id when the dialog and the fallback section coexist
- All new tokens classified into `_LIGHTBOX_RENDER_ONLY_TOKENS`
</success_criteria>

<output>
Create `.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-02-SUMMARY.md` when done
</output>
