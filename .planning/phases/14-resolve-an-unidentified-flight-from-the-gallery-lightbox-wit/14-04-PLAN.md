---
phase: 14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit
plan: 04
type: execute
wave: 2
depends_on: ["14-02", "14-03"]
files_modified:
  - companion/pages/airlines_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "A coverage gap seen at least 3 times renders as an empty, imageless card at the head of the gallery grid, labelled with its example callsign, sorted by sighting count descending, capped at 12 with an overflow line naming what the cap hides and linking to Health."
    - "A gap card's data-filter-group value can never collide with an existing airline card's raw enumerate() index (RESEARCH.md Pitfall 4)."
    - "The no-JS fallback resolve section now renders as the last element on the page, after the shared dialog, and every existing test that isolates its markup keeps passing against the new position."
    - "A hostile example callsign reaching a gap card is escaped exactly once, matching the discipline already proven for airline names and resolve-section values."
  artifacts:
    - "companion/pages/airlines_page.py: GAP_BLOCK_THRESHOLD, GAP_BLOCK_CAP, _gap_rows_for_grid(state_dir), _gap_card_html(index, row), _gap_overflow_html(overflow_count)"
    - "companion/pages/airlines_page.py: _gallery_grid_html(pairs, state_dir=None, gap_cards_html=\"\") widened to prepend gap cards inside the same .illustration-grid wrapper"
    - "companion/pages/airlines_page.py: render()'s composition reordered to page_header + filter_bar + [plan 14-06's manual-summary insertion point] + gap_overflow_line + gallery_grid + lightbox + resolve_section"
    - "companion/test_status_pages.py: _resolve_slice() retargeted to isolate everything after the shared dialog's closing tag"
  key_links:
    - "_gap_card_html()'s data-filter-group value is a \"gap%d\" string, never a bare integer, so list-filter.js's group-counting can never collide with the art grid's enumerate() sequence"
    - "_gap_rows_for_grid()'s sort key is (-count, prefix), duplicated verbatim from health_page.unresolved_rows(), never imported (airlines_page.py has no import path to a sibling page module)"
    - "_gap_card_html()'s trigger is the whole <a> element with no nested .airline-card__zoom button — panel-lookup.js's ancestor walk finds data-view-panel-src on the <a> itself"
    - "The gap block's threshold-and-cap shape (D-06) is applied inside _gap_rows_for_grid() before any card renders, so the shown count and the overflow count are always mutually consistent"
---

<objective>
Render coverage gaps as empty cards at the head of the Airlines gallery grid (D-01, D-02's
markup half, D-04, D-05, D-06, D-07), and move the no-JS resolve section from the top of the
page to the bottom, behind the shared dialog, matching UI-SPEC's new page composition.

Purpose: this is the phase's primary visual deliverable — "cases vides... un clic dessus
ouvre également la pop up" is the developer's own framing. Every later wave depends on this
plan's gap-card markup and page order: 14-05's script needs `data-view-panel-resolve-prefix`
to exist on a real trigger to auto-open against, and 14-06's manual-resolution absorption
needs the extended `pairs` composition and the gap-block's insertion point already in place
before it threads the D-11 summary line between the filter bar and the overflow line.

Output: gap cards render, sorted and capped, at the head of `.illustration-grid`; an overflow
line renders only when the cap bites; the resolve section moves to the bottom of the page;
five new checks plus one retargeted test helper land in `companion/test_status_pages.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-CONTEXT.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-UI-SPEC.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-PATTERNS.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-RESEARCH.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-02-SUMMARY.md
@.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap-card rendering and the gap block (D-01, D-02, D-04, D-05, D-06, D-07)</name>
  <read_first>
    - `companion/pages/airlines_page.py` `_airline_card_html()` (494-584) and `_gallery_grid_html()` (587-598) — the sibling card shape and the grid wrapper this plan widens
    - `companion/pages/airlines_page.py` `unresolved_row_for_prefix()` (700-758) — the exact five-field row shape (`prefix, count, first_seen, last_seen, example_callsign`) this plan's own gap-registry scan must reproduce for every eligible prefix, not just one
    - `companion/pages/health_page.py` `unresolved_rows()` (1860-1896) — the sort key (`(-row[1], row[0])`) and the malformed-entry-skip discipline to duplicate verbatim (airlines_page.py has no import path to health_page.py, a sibling page module)
    - The Phase 14 attribute/copy vocabulary 14-02 declared: `_VIEW_PANEL_HEADING_ATTR`, `_VIEW_PANEL_MODE_ATTR`, `_VIEW_PANEL_MANUAL_ATTR`, `_VIEW_PANEL_SCOPE_ATTR`, `_VIEW_PANEL_RESOLVE_PREFIX_ATTR`, `_VIEW_PANEL_FIRST_SEEN_ATTR`/`_LAST_SEEN_ATTR`/`_COUNT_ATTR`, `_VIEW_PANEL_MODE_GAP`, `GAP_CARD_ARIA_TEMPLATE`, `RESOLVE_CAPTION_TEMPLATE` (reworded), `RESOLVE_HEADING`, `MANUAL_OVERFLOW_TEMPLATE`, `MANUAL_OVERFLOW_LINK_TEXT`
    - `.planning/phases/14-.../14-UI-SPEC.md` § Page Composition → "Gap-card markup shape" (the exact `<a>` shape, attribute-for-attribute) and "Gap-block composition" (source/sort/cap/overflow-line rules)
    - `.planning/phases/14-.../14-RESEARCH.md` Pitfall 4 (the `data-filter-group` collision) and its "Don't Hand-Roll" sort-key row
    - `companion/test_status_pages.py:326-368` (`_mkstate`, `_seed_unresolved_prefixes`, `_ctx`) and `companion/test_status_pages.py:6011-6066` (`_replace_control_escapes_hostile_airline_name`, the hostile-value escaping idiom this task's own new check extends)
  </read_first>
  <files>companion/pages/airlines_page.py, companion/test_status_pages.py</files>
  <behavior>
    - A prefix seeded at `count=2` never renders a gap card; the identical prefix seeded at `count=3` does (the threshold is `>=`, not `>`).
    - Fifteen eligible prefixes (count >= 3) render exactly 12 gap cards, sorted `(-count, prefix)`, and `_gap_rows_for_grid()`'s own overflow count is exactly `3`.
    - A gap card's rendered markup contains zero `<img` tags and zero `<button class="airline-card__zoom"` — the whole `<a class="airline-card">` element is the trigger, carrying `data-view-panel-src=""`, `data-view-panel-mode="gap"`, `data-view-panel-manual=""`, `data-view-panel-resolve-prefix` equal to the prefix, and every other vocabulary attribute 14-02 declared, non-empty where UI-SPEC's snippet shows a value.
    - `_gap_card_html()`'s `data-filter-group` value matches `^gap\d+$` and is never equal, as a bare string, to any curated card's integer `data-filter-group` value on the same render.
    - With 12 or fewer eligible prefixes, `_gap_overflow_html()` returns the empty string; with more, it returns the templated line naming the exact overflow count, ending in a period, with `<a href="/health">` wrapping only `MANUAL_OVERFLOW_LINK_TEXT`.
    - An `example_callsign` containing `<`, `>`, `&` and `"` renders fully escaped in both the card's visible text and every attribute value that carries it.
  </behavior>
  <action>
    Add two module-level constants beside the existing Phase 13 route/copy blocks:
    `GAP_BLOCK_THRESHOLD = 3` and `GAP_BLOCK_CAP = 12` — D-06's locked shape, UI-SPEC's own
    numbers, each with a one-line comment naming D-06.

    Add `_gap_rows_for_grid(state_dir)`: read `poll_loop.load_poll_state(state_dir)`'s
    `unresolved_prefixes` dict directly (this module already imports `poll_loop` for
    `unresolved_row_for_prefix()`), duplicating `health_page.unresolved_rows()`'s own
    malformed-entry-skip discipline byte for byte (skip a non-dict entry, skip a non-int or
    bool `count`) — never import `health_page`, a sibling page module. Build the full eligible
    list (`count >= GAP_BLOCK_THRESHOLD`), sort it with the identical `(-count, prefix)` key
    `health_page.unresolved_rows()` uses (duplicated, not imported, matching this module's own
    established discipline for `LIGHTBOX_DIALOG_ID` et al.), then split into `(shown, overflow_count)`
    where `shown` is the first `GAP_BLOCK_CAP` rows and `overflow_count` is
    `max(0, len(eligible) - GAP_BLOCK_CAP)`. Return the five-field tuples in the exact shape
    `unresolved_row_for_prefix()` already returns them (`prefix, count, first_seen, last_seen,
    example_callsign`), so a future caller can treat both functions' rows identically.

    Add `_gap_card_html(index, row)`: build the `<a class="airline-card"
    href="/airlines?resolve={prefix}">` UI-SPEC's Gap-card markup shape gives verbatim,
    attribute for attribute. `data-view-panel-src=""`; `data-view-panel-caption` and the
    visible `<p class="airline-card__name mono">` text are both the escaped example callsign;
    `data-view-panel-heading` is `RESOLVE_HEADING`; `data-view-panel-mode` is
    `_VIEW_PANEL_MODE_GAP`; `data-view-panel-manual` is `""`; `data-view-panel-scope` is
    `RESOLVE_CAPTION_TEMPLATE % escaped_prefix`; `data-view-panel-resolve-prefix` is the escaped
    prefix; `data-view-panel-first-seen`/`-last-seen`/`-count` are the escaped raw registry
    values (not run through `layout.concise_timestamp_html()` — that helper returns markup
    unsuited to an attribute value, and `panel-lookup.js` can only ever textContent-copy
    whatever raw string the attribute carries; this is a deliberate, documented asymmetry
    with the no-JS fallback's own concise rendering, not a defect). `data-filter-text` is the
    lower-cased escaped callsign and prefix, space-joined; `data-filter-group` is `"gap%d" %
    index` — a string prefix, never a bare integer (RESEARCH.md Pitfall 4). `aria-label` is
    `GAP_CARD_ARIA_TEMPLATE % (escaped_prefix, escaped_callsign)`. The card's single child is
    `<span class="airline-card__placeholder" aria-hidden="true"></span>` (14-03's CSS already
    styles this class) followed by the name paragraph — no `.airline-card__zoom`, no
    `.airline-card__chip` container (a raw gap has no chip; chips are 14-06's manual-absorption
    concern). Every interpolated value passes `escape_html()` exactly once, at the point of
    interpolation (T-06.6.4.1-05) — reuse one `escaped_prefix`/`escaped_callsign` pair per call,
    never re-escape.

    Add `_gap_overflow_html(overflow_count)`: return `""` when `overflow_count` is falsy;
    otherwise `<p class="text-label section-caption">%d other unresolved prefixes — <a
    href="/health">%s</a>.</p> % (overflow_count, MANUAL_OVERFLOW_LINK_TEXT)` — matching
    UI-SPEC's Gap-block composition literal exactly, including the trailing period outside the
    anchor.

    Widen `_gallery_grid_html(pairs, state_dir=None, gap_cards_html="")`: prepend
    `gap_cards_html` inside the existing `.illustration-grid` wrapper, ahead of the curated
    cards' own joined string — one function still owns the grid's outer markup, matching this
    module's "one render function" discipline; do not have `render()` build a second wrapper.

    Rewire `render()`: compute `gap_shown, gap_overflow_count = _gap_rows_for_grid(state_dir)`;
    `gap_cards_html = "".join(_gap_card_html(i, row) for i, row in enumerate(gap_shown))`;
    `overflow_html = _gap_overflow_html(gap_overflow_count)`. `pairs =
    illustrations.target_variants_by_airline()` stays as today. `total = len(gap_shown) +
    len(pairs)` feeds `_filter_bar_html(total)` — every visible card, gap or art, per UI-SPEC's
    "Filter bar total" note (plan 14-06 further widens `pairs` additively; this plan's own
    arithmetic already accounts for whatever `pairs` holds at call time, so no further edit is
    needed here later). Guard `filter_html`/`lightbox_html` on `(pairs or gap_shown)` rather
    than `pairs` alone, so a state with gaps but (hypothetically) zero curated pairs still gets
    its filter bar and dialog.

    Reorder the final concatenation to: `page_header` + `filter_html` +
    **[a bare `""` placeholder position, immediately followed by `overflow_html` — do not
    concatenate the two into one expression; keep them as two distinct summands in the return
    tuple/expression so plan 14-06 can insert its own `manual_summary_html` summand between
    them without needing to re-parse a merged string]** + `_gallery_grid_html(pairs, state_dir,
    gap_cards_html)` + `lightbox_html` + `resolve_html`. State this exact insertion contract
    (UI-SPEC's binding order: filter_bar, then manual-summary, then gap-overflow, then grid) in
    a code comment directly above the return statement, naming plan 14-06 by number, so the
    next editor does not need to re-derive it from UI-SPEC.

    Do not touch `_resolve_section_html()`'s own derivation logic (Step A/B/already-done/stale)
    — only its position in `render()`'s composition moves.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `status-pages: 157/157 checks pass` (152 + 5)
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 (unaffected)
    - `grep -c "^GAP_BLOCK_THRESHOLD = 3$" companion/pages/airlines_page.py` returns `1`; same for `^GAP_BLOCK_CAP = 12$`
    - `grep -c "^def _gap_rows_for_grid\|^def _gap_card_html\|^def _gap_overflow_html" companion/pages/airlines_page.py` returns `3`
    - No nested zoom button in a gap card: a throwaway REPL rendering one seeded gap and slicing from `<a class="airline-card"` to the matching `</a>` finds zero occurrences of `airline-card__zoom`
    - `data-filter-group` never a bare integer for a gap card: `python3 -c "import re; s=open('companion/pages/airlines_page.py').read(); assert 'data-filter-group=\"gap%d\"' in s"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <done>Gap cards render at the head of the grid, sorted count-descending, thresholded at 3 and capped at 12, with an overflow line naming what the cap hides; five new checks pass at 157/157.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move the resolve section to the bottom of the page and retarget its test isolation helper</name>
  <read_first>
    - `companion/pages/airlines_page.py` `render()` (1167-1220, pre-Task-1 line numbers — re-read after Task 1 lands in this same plan) — the composition Task 1 just reordered
    - `companion/test_status_pages.py:6254-6261` (`_resolve_slice()`) — the helper four existing checks call, currently `rendered[:rendered.index("filter-bar")]`, which silently breaks the moment the resolve section moves below the grid
    - `companion/test_status_pages.py:6263-6516` — the four checks that call `_resolve_slice()`: `_resolve_section_four_states_render_correctly`, `_resolve_section_datalist_contract`, `_resolve_section_escapes_hostile_values_and_distrusts_query_string`, `_resolve_section_step_b_reachable_after_gap_cleared` — read enough of each to confirm none also asserts on the section's *position*, only its *content*
    - `companion/pages/airlines_page.py` `LIGHTBOX_DIALOG_ID` (100) and `_lightbox_html()` (601-639, pre-Task-1 numbers) — the dialog's own id, the new boundary marker
    - `companion/test_status_pages.py:6656-6679` (`_health_resolve_link_template_matches_airlines_route_constants`) — the pre-existing cross-module check proving Health's Resolve-link target already matches `airlines_page.AIRLINES_ROUTE`/`RESOLVE_QUERY_PARAM`; this task touches neither file it reads, only confirms the check still passes after the reorder
  </read_first>
  <files>companion/pages/airlines_page.py, companion/test_status_pages.py</files>
  <behavior>
    - `_resolve_slice(rendered)` isolates exactly the same resolve-section markup it did before the reorder — every one of the four existing checks that call it keeps passing with zero change to their own assertions.
    - A new check proves the page's top-to-bottom order: the filter bar's own marker precedes the gallery grid's own marker, which precedes the dialog's opening tag, which precedes the dialog's closing tag, which precedes the resolve section's own back-link text (when `resolve_prefix` is seeded).
    - `companion/pages/health_page.py` is not modified by this task (`git diff --name-only` never lists it), and the cross-module Resolve-link-target check stays green unchanged.
  </behavior>
  <action>
    Retarget `_resolve_slice(rendered)` to isolate everything the page renders *after* the
    shared dialog rather than everything before the filter bar: locate
    `'id="%s"' % airlines_page.LIGHTBOX_DIALOG_ID` inside `rendered`, then find the next
    `"</dialog>"` from that point forward, and return the slice starting immediately after it.
    This is robust regardless of the dialog's own contents (whatever 14-02 and later waves add
    inside it) since it anchors on the dialog's own id and its universal closing tag, never on
    a hardcoded index into a specific inner string. Do not change any of the four checks'
    assertion bodies — only this one shared helper moves, and every caller inherits the fix.

    Add one new check, `_page_composition_order_matches_ui_spec()`: render a state seeded with
    both a real gallery (the default curated pairs, always present) and `resolve_prefix` set
    to a live gap, then assert, via `rendered.index(...)` on four markers in strictly
    increasing order: `'class="filter-bar'`, `'class="illustration-grid'`,
    `'id="%s"' % airlines_page.LIGHTBOX_DIALOG_ID`, `"</dialog>"`, and (last)
    `airlines_page.RESOLVE_BACK_LINK_TEXT`. This is the one direct, permanent proof that
    UI-SPEC's new top-to-bottom order actually shipped, independent of what any individual
    section's own content-focused check already covers.

    Confirm (no code change, this file only) that
    `_health_resolve_link_template_matches_airlines_route_constants` (companion/test_status_pages.py:6656)
    still passes unmodified — it does not depend on page order, only on the two modules'
    route/query-param constants, which this plan does not touch. Its continued green run is
    this task's confirmation that D-13's auto-open target (14-05) and Health's Resolve link
    stay in agreement with no code change to `companion/pages/health_page.py` at all.

    Move `EXPECTED_CHECK_COUNT` 157 → 158, extending the file's own running-arithmetic comment.
  </action>
  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_status_pages.py` exits 0 and reports `status-pages: 158/158 checks pass`
    - `scripts/run-all-tests.sh` exits 0
    - `git diff --name-only` never lists `companion/pages/health_page.py`
    - `sed -n '/def _resolve_slice/,/^    def \|^    check(/p' companion/test_status_pages.py | grep -c '"</dialog>"'` returns at least `1`
    - `sed -n '/def _resolve_slice/,/return/p' companion/test_status_pages.py | grep -c '"filter-bar"'` returns `0` (the old, now-wrong boundary literal is gone)
  </acceptance_criteria>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py &amp;&amp; scripts/run-all-tests.sh</automated>
  </verify>
  <done>The resolve section renders last on the page, `_resolve_slice()` isolates it correctly at its new position, all four checks that depend on it still pass with no change to their own bodies, a new check proves the page order directly, and Health's Resolve-link-target contract stays green with zero changes to health_page.py.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

| Kind | Symbol | File |
|---|---|---|
| constant | `GAP_BLOCK_THRESHOLD = 3` | `airlines_page.py` |
| constant | `GAP_BLOCK_CAP = 12` | `airlines_page.py` |
| function | `_gap_rows_for_grid(state_dir)` | `airlines_page.py` |
| function | `_gap_card_html(index, row)` | `airlines_page.py` |
| function | `_gap_overflow_html(overflow_count)` | `airlines_page.py` |
| signature change | `_gallery_grid_html(pairs, state_dir=None, gap_cards_html="")` | `airlines_page.py` |
| composition change | `render()` reordered: filter_bar → [14-06's insertion point] → gap_overflow_line → gallery_grid (gap + art) → lightbox → resolve_section | `airlines_page.py` |
| test helper retarget | `_resolve_slice()` now anchors on `</dialog>`, not `"filter-bar"` | `test_status_pages.py` |
| count move | `EXPECTED_CHECK_COUNT` 152 → 158 | `test_status_pages.py` |

No route, no CSS class, and no server-side symbol is created by this plan. `.airline-card__placeholder` (styled by 14-03) and the `_VIEW_PANEL_*`/copy constants (declared by 14-02) are consumed, not created, here.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `poll_state.json` → gap-card markup | `example_callsign` is free-text, genuinely operator-adjacent (whatever a real aircraft transponder broadcasts), and crosses into both visible text and multiple attribute values on the same card |
| operator URL (`?resolve={prefix}`) → gap card's own `href` | The prefix interpolated into `href="/airlines?resolve={prefix}"` is always a value this function itself derived from the already-validated registry key (`_gap_rows_for_grid()`'s own dict keys), never a raw query-string value — the same validate-then-join posture `_resolve_section_html()` already holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-16 | Tampering (stored XSS) | `_gap_card_html()`'s multiple interpolation points (visible text, `data-view-panel-*` attributes, `data-filter-text`, `aria-label`) | high | mitigate | Every interpolated value passes `escape_html()` exactly once, at the point of interpolation (T-06.6.4.1-05), with one escaped variable reused across every attribute that needs it rather than re-escaping per site. Pinned by this plan's own hostile-callsign check, extending the existing `_replace_control_escapes_hostile_airline_name()` precedent onto a second, independent data source (live traffic rather than the curated airline list). |
| T-14-17 | Tampering (client-side filter collision) | `data-filter-group="gap%d"` vs. the curated grid's bare-integer `enumerate()` sequence | medium | mitigate | String-prefixed group values (`"gap0"`.."gap11"`) can never collide with a bare integer string from the art grid, since `list-filter.js`'s own `"g" + group` concatenation treats both as opaque strings — pinned by this plan's own collision-check acceptance criterion (RESEARCH.md Pitfall 4). |
| T-14-18 | Information Disclosure | raw (unformatted) `first_seen`/`last_seen`/`count` values copied verbatim into `data-view-panel-*` attributes | low | accept | These are the identical values `_resolve_context_html()` already displays to the same single-operator session via the no-JS fallback and Health's own registry table — no new disclosure, only a different (unformatted) presentation once the script copies them into the dialog. Escaped once at interpolation like every other value. |
| T-14-SC | Tampering | package installs | n/a | accept | This plan adds no dependency; both files are already-committed, first-party code. |
</threat_model>

<verification>
- `scripts/run-all-tests.sh` exits 0
- `server/.venv/bin/python3 companion/test_status_pages.py` → 158/158
- `server/.venv/bin/python3 companion/test_view_pages.py` → 55/55 (unaffected)
- `git diff --name-only` lists only `companion/pages/airlines_page.py` and `companion/test_status_pages.py` — `companion/pages/health_page.py` and every file under `server/` are untouched
- Gap block: threshold, sort, cap and overflow line all provable from a single seeded fixture without a live browser
</verification>

<success_criteria>
- Gap cards render at the head of the grid: no `<img>`, no `.airline-card__zoom` button, full attribute vocabulary, real `<a href>` trigger
- Threshold (≥3), cap (≤12) and count-descending sort all hold; the overflow line renders the exact hidden count only when the cap bites
- `data-filter-group` never collides between gap cards and curated cards
- The resolve section renders last on the page, behind the shared dialog, with zero change to its own derivation logic
- Health's Resolve-link-target contract stays green with zero changes to `health_page.py`
</success_criteria>

<output>
Create `.planning/phases/14-resolve-an-unidentified-flight-from-the-gallery-lightbox-wit/14-04-SUMMARY.md` when done
</output>
