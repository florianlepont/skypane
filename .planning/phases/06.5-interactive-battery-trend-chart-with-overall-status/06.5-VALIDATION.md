---
phase: 06.5
slug: interactive-battery-trend-chart-with-overall-status
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-28
---

# Phase 06.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Stdlib, hand-rolled `check(name, fn)` harness — no pytest |
| **Config file** | none — the harness is a plain executable script |
| **Quick run command** | `server/.venv/bin/python3 companion/test_status_pages.py` |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~20-30 seconds (automated portion) |

---

## Sampling Rate

- **After every task commit:** `server/.venv/bin/python3 companion/test_status_pages.py`
- **After every plan wave:** `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite green, **plus** a real browser/device preview check for the tap/hover interaction itself — this harness has no headless-browser/JS execution capability, so the actual click-triggered DOM update cannot be automated, matching `05-CONTEXT.md`'s "real preview renders before committing to visual decisions" precedent
- **Max feedback latency:** ~30 seconds (automated); the manual tap-test is a separate, explicit checkpoint

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.5-01-01 | 01 | 1 | D-01 (status badge) | T-06.5-01 | `_battery_section()` emits a `status_dot()` badge next to the heading; empty-history state renders `"ok"`, never triggering `collect_anomalies()`'s drop-anomaly banner on a fresh/empty deployment | unit | `server/.venv/bin/python3 companion/test_status_pages.py` | ✅ file exists, extend | ⬜ pending |
| 06.5-01-02 | 01 | 1 | D-02 (interactive markup) | T-06.5-02 | Each chart point carries `data-mv`/`data-ts` attributes; exactly one `<script` tag on the page; zero inline event-handler attributes (`onclick=` etc.) anywhere in the output | unit (markup-presence only — JS behavior itself is out of this harness's reach) | `server/.venv/bin/python3 companion/test_status_pages.py` | ✅ file exists, extend | ⬜ pending |
| 06.5-01-03 | 01 | 1 | D-02 (JS asset + route) | T-06.5-03 | New `companion/static/battery-trend.js` served via a pre-auth static route mirroring `_serve_stylesheet()` exactly (`text/javascript` per RFC 9239); uses `click`+`keydown` handlers and `textContent`, never `innerHTML` | unit | `server/.venv/bin/python3 companion/test_status_pages.py` (or `test_companion_app.py` if the route lives there — planner to confirm exact file) | ✅ file exists, extend | ⬜ pending |
| 06.5-02-01 | 02 | 2 | D-02 (real tap/hover) | — | Tapping/clicking a chart point actually reveals its exact mV + timestamp on a real device/browser, both mobile tap and desktop hover | manual (real preview render, per 05-CONTEXT.md precedent) | — no automated equivalent in this harness | N/A — behavior only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `companion/test_status_pages.py` — new check(s) for: (1) badge presence on the Battery section, (2) empty-history badge state not triggering the anomaly banner, (3) exactly-one-`<script>`-tag + no-inline-handlers page-level guard. `EXPECTED_CHECK_COUNT` must be bumped by the exact number of new checks added — the harness fails closed if this constant drifts.
- [ ] No new test framework or fixture infrastructure needed — extends the existing `check()` pattern already established in this file.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tap-to-reveal actually works on a real mobile browser | D-02 | This project's stdlib test harness has no headless-browser/JS-execution capability — the markup-presence checks above prove the *scaffolding* is correct but not that the interaction actually fires | On a real phone (or the Browser pane's mobile emulation), tap a chart point and confirm the exact mV + timestamp appears; confirm hover does the same on desktop; confirm the readout has a reserved fixed-height slot so nothing jumps when it appears |
| Empty-history badge doesn't read as alarming | D-01, Assumption A1 | Requires visually judging tone/color on a fresh-deployment fixture, not just asserting a CSS class string | With zero battery rows, confirm the badge reads as neutral/informational (not a red/error state) alongside the "no data yet" table |

---

## Validation Sign-Off

- [x] All non-manual tasks have automated verify (see table above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (only the final wave-2 task is manual-only, immediately preceded by 3 automated tasks)
- [ ] Wave 0 covers all MISSING references — 3 new checks still to be written by the planner/executor (see Wave 0 Requirements)
- [x] No watch-mode flags — all commands are one-shot script executions
- [x] Feedback latency < 30s (automated portion)
- [ ] `nyquist_compliant: true` set in frontmatter — left `false` pending planner's actual task breakdown matching this map exactly

**Approval:** pending — planner must confirm task IDs above match its actual PLAN.md breakdown before flipping `nyquist_compliant: true`.
