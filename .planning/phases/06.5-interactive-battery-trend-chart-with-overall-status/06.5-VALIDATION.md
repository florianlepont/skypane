---
phase: 06.5
slug: interactive-battery-trend-chart-with-overall-status
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-28
---

# Phase 06.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Stdlib, hand-rolled `check(name, fn)` harness — no pytest |
| **Config file** | none — the harness is a plain executable script |
| **Quick run command** | `server/.venv/bin/python3 companion/test_status_pages.py` |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~20-30 seconds (automated portion) |

---

## Sampling Rate

- **After every task commit:** `server/.venv/bin/python3 companion/test_status_pages.py`
- **After every plan wave:** `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite green, **plus** a real browser/device preview check for the tap/hover interaction itself — this harness has no headless-browser/JS execution capability, so the actual click-triggered DOM update cannot be automated, matching `05-CONTEXT.md`'s "real preview renders before committing to visual decisions" precedent
- **Max feedback latency:** ~30 seconds (automated); the manual tap-test is a separate, explicit checkpoint

---

## Per-Task Verification Map

> Confirmed against the actual plan breakdown on 2026-08-28: three plans across three waves. Task IDs below match `06.5-01-PLAN.md`, `06.5-02-PLAN.md` and `06.5-03-PLAN.md` exactly.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.5-01-01 | 01 | 1 | D-02 (JS asset + styles) | T-06.5-06 | `companion/static/battery-trend.js` writes the readout through `textContent` only — no HTML-parsing DOM sink, no dynamic evaluation, no network call anywhere in its non-comment body; wires `click`/`mouseenter`/`focus`/`keydown` and no raw touch listeners; returns early and harmlessly when no readout element exists | source-invariant grep + `node --check` syntax gate | `node --check companion/static/battery-trend.js` plus the task's grep battery; `scripts/run-all-tests.sh` proves nothing regressed | ✅ new file, created by this task | ⬜ pending |
| 06.5-01-02 | 01 | 1 | D-02 (static route) | T-06.5-04, T-06.5-05 | New pre-auth `/static/battery-trend.js` route mirroring `_serve_stylesheet()` exactly — `text/javascript` per RFC 9239, `cache_seconds=300`, `OSError` contained to the site's own 404 page, no client-supplied path segment and therefore no traversal surface | unit (live HTTP round trip through the existing subprocess harness) | `server/.venv/bin/python3 companion/test_companion_app.py` | ✅ file exists, extend (+1 check, `EXPECTED_CHECK_COUNT` +1) | ⬜ pending |
| 06.5-02-01 | 02 | 2 | D-01 (status badge) | T-06.5-08 | `_battery_section()` emits a `status_dot()` badge on every data-bearing branch; the empty-history state renders `"ok"`, so `collect_anomalies()` never tells a zero-reading deployment it suffered an abnormal drop (Assumption A1, locked) | unit (in-process render) | `server/.venv/bin/python3 companion/test_status_pages.py` | ✅ file exists, extend (+3 checks, 1 existing check corrected) | ⬜ pending |
| 06.5-02-02 | 02 | 2 | D-02 (interactive markup) | T-06.5-01, T-06.5-02, T-06.5-03, T-06.5-09 | Each point carries an enlarged transparent `data-mv`/`data-ts` hit target with a `<title>`; the page carries exactly one `<script` tag and zero inline event-handler attributes; the empty path carries none at all; a hostile timestamp is escaped; the Python/CSS/JS contract literals are drift-guarded | unit (markup-presence + cross-file contract — the JS *behaviour* itself remains out of this harness's reach) | `server/.venv/bin/python3 companion/test_status_pages.py` | ✅ file exists, extend (+5 checks; total `EXPECTED_CHECK_COUNT` +8 across plan 02) | ⬜ pending |
| 06.5-03-01 | 03 | 3 | D-01, D-02 (real tap/hover) | T-06.5-10, T-06.5-11, T-06.5-12 | A seeded live service proves the wire (badge, 12 hit targets, one script tag, pre-auth JS route) over real HTTP; the developer then confirms on a **named real phone** that a tap reveals the exact mV + timestamp, that targets are thumb-reachable, that nothing shifts on first reveal, that desktop hover and the JS-disabled tooltip both work, and that the empty badge reads informational | automated HTTP staging + `<human-check>` (`workflow.human_verify_mode` is `end-of-phase`, so this is not a blocking mid-execution checkpoint) | `scripts/run-all-tests.sh` + the task's staging assertions; the tap test itself has no automated equivalent | N/A — behavior only, recorded in `06.5-03-SUMMARY.md` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Resolved by the plan breakdown — every gap below is assigned to a named task, and no new test framework or fixture infrastructure is needed. All new checks extend the existing `check(name, fn)` pattern already established in each harness.
- [ ] `companion/test_companion_app.py` — 1 new check (pre-auth `/static/battery-trend.js` returns 200, `text/javascript`, non-empty, `max-age=300`). Assigned to **06.5-01-02**. `EXPECTED_CHECK_COUNT` +1.
- [ ] `companion/test_status_pages.py` — 3 new checks: badge present and healthy on a normal trend; empty history renders an ok badge with **no** anomaly banner and no abnormal-drop copy (the Assumption A1 regression guard); a real drop drives both the badge and the banner. Assigned to **06.5-02-01**. `EXPECTED_CHECK_COUNT` +3. One existing check (`_independent_thresholds_one_warn_one_ok`) must be corrected in the same commit — its `dot--ok` expectation moves from 1 to 2 because the Battery badge is a legitimate new healthy dot in that fixture.
- [ ] `companion/test_status_pages.py` — 5 new checks: per-point interactive markup; exactly-one-`<script>` + zero inline handlers at page level; the empty path stays script-free; hostile-timestamp escaping; and the Python/CSS/JS contract drift guard. Assigned to **06.5-02-02**. `EXPECTED_CHECK_COUNT` +5.
- [ ] `EXPECTED_CHECK_COUNT` bumps must be computed from each file's **current** value, never hard-coded — Phase 06.3 (planned, unexecuted) also adds checks to both harnesses, so whichever phase executes second inherits a different starting number. The constant is a fail-closed gate: the harness exits non-zero if it drifts from the real check count in either direction.
- [ ] Manual/preview verification: real-device tap test on the shipped `battery-trend.js`. Assigned to **06.5-03-01**, expressed as a `<human-check>` (not a blocking checkpoint) because `workflow.human_verify_mode` is `end-of-phase`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tap-to-reveal actually works on a real mobile browser | D-02 | This project's stdlib test harness has no headless-browser/JS-execution capability — the markup-presence checks above prove the *scaffolding* is correct but not that the interaction actually fires | On a real phone (or the Browser pane's mobile emulation), tap a chart point and confirm the exact mV + timestamp appears; confirm hover does the same on desktop; confirm the readout has a reserved fixed-height slot so nothing jumps when it appears |
| Empty-history badge doesn't read as alarming | D-01, Assumption A1 | Requires visually judging tone/color on a fresh-deployment fixture, not just asserting a CSS class string | With zero battery rows, confirm the badge reads as neutral/informational (not a red/error state) alongside the "no data yet" table |

---

## Validation Sign-Off

- [x] All non-manual tasks have automated verify (see table above) — every one of the four automated tasks carries an `<automated>` block, none is `MISSING`
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (only the final wave-3 task carries a human check, and it *also* carries an automated block, immediately preceded by 4 fully automated tasks)
- [x] Wave 0 covers all MISSING references — all 9 new checks are assigned to named tasks with their `EXPECTED_CHECK_COUNT` bumps stated (see Wave 0 Requirements)
- [x] No watch-mode flags — all commands are one-shot script executions
- [x] Feedback latency < 30s (automated portion)
- [x] `nyquist_compliant: true` — task IDs above match `06.5-01-PLAN.md` / `06.5-02-PLAN.md` / `06.5-03-PLAN.md` exactly

**Approval:** confirmed 2026-08-28 by the planner. Task IDs match the actual PLAN.md breakdown (3 plans, 3 waves, 5 tasks).
