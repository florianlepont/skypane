---
phase: 37-security-and-operations-hardening
plan: 11
type: execute
wave: 7
depends_on: ["37-09", "37-10"]
external_depends_on: ["Phase 36 (merged; modifies stub-server/byos_server.py)", "Phase 34 FW-08 if landed (per-device enrolment secret — follow its shape)"]
files_modified:
  - stub-server/byos_server.py
  - stub-server/test_byos_bind_secret.py
  - deploy/skypane-byos.service
  - deploy/tests/test_units.py
  - deploy/skypane.env.example
  - deploy/README.md
  - .planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md
autonomous: false
requirements: [SEC-06, SEC-07]
user_setup: []

must_haves:
  truths:
    - "byos listens on 127.0.0.1 only in production and its unit carries IPAddressDeny=any + IPAddressAllow=localhost — ROADMAP SC-5 'byos reachable on loopback only'"
    - "The byos secret reaches the process through the environment, never argv: pgrep -af byos_server.py shows no secret — ROADMAP SC-5 'no secret in ps'"
    - "byos --bind defaults to 0.0.0.0 and --secret still works for the LAN stub flow; the enrolment model is unchanged (D-01)"
    - "The frame still fetches its image through Caddy after the change"
  artifacts:
    - path: "stub-server/byos_server.py"
      provides: "--bind flag; secret default from SKYPANE_BYOS_SECRET"
      contains: "--bind"
    - path: "deploy/skypane-byos.service"
      provides: "--bind 127.0.0.1, IPAddressDeny=any, IPAddressAllow=localhost, no --secret"
      contains: "IPAddressDeny=any"
    - path: "stub-server/test_byos_bind_secret.py"
      provides: "loopback listen + no secret in /proc/<pid>/cmdline"
  key_links:
    - from: "deploy/skypane-byos.service EnvironmentFile"
      to: "byos_server.py os.environ.get('SKYPANE_BYOS_SECRET')"
      via: "argparse default read in build_parser/main at run time"
      pattern: "SKYPANE_BYOS_SECRET"
---

<objective>
Wave B — the SEC-06 and SEC-07 remainders on byos: `--bind 127.0.0.1` with
`IPAddressDeny=any`/`IPAddressAllow=localhost`, and the byos secret moved off the command line into
the environment; then the live proof (CP-11) and the removal of the old in-place layout (CP-7).

External dependency: Phase 36 (D-01) — it rewrites stub-server/byos_server.py. This plan must not
touch byos_server.py before Phase 36 is merged; Task 1 opens with that gate and re-reads the file as
Phase 36 left it. If Phase 34 FW-08 (per-device enrolment secret) has landed, keep its enrolment
model exactly and only change how the remaining secret reaches the process (env, not argv).
Depends on Plans 37-09 (release layout live) and 37-10 (keeps VPS checkpoints serial).

Output: byos flag/env change + tests, byos unit, docs, CP-11 and CP-7 evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-security-and-operations-hardening/37-CONTEXT.md
@.planning/phases/37-security-and-operations-hardening/37-RESEARCH.md
@.planning/phases/37-security-and-operations-hardening/37-03-SUMMARY.md
@.planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md

<interfaces>
Before Phase 36 (a2bf2db; re-read, Phase 36/34 will have changed it):
- ap.add_argument("--secret", default="", ...) compared against the enrolment request; ap.add_argument("--port", type=int, default=8642);
  server = ThreadingHTTPServer(("0.0.0.0", args.port), Handler); no --bind flag.
- skypane-byos.service ExecStart ... --secret ${SKYPANE_BYOS_SECRET} ...; EnvironmentFile=/opt/skypane/skypane.env already exports SKYPANE_BYOS_SECRET.
Pattern to mirror: companion/app.py --bind (Plan 37-01): add_argument("--bind", default="0.0.0.0", help=...), (args.bind, args.port), bind in the startup print.
Env read pattern: os.environ.get at run time (inside build_parser/main), never at import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 36 gate, then byos --bind and secret-from-environment (TDD)</name>
  <files>stub-server/byos_server.py, stub-server/test_byos_bind_secret.py</files>
  <read_first>
    - stub-server/byos_server.py as Phase 36 (and Phase 34, if landed) left it: argument parser, secret comparison / enrolment path, server construction, startup print, any outbound network calls (grep urllib|requests|http.client|socket.create_connection)
    - companion/app.py --bind implementation (Plan 37-01)
    - existing byos tests that spawn the server (grep "byos_server.py" in stub-server/ and server/ tests) and Phase 32 fixtures
  </read_first>
  <behavior>
    - parse defaults: bind == "0.0.0.0"; secret == value of SKYPANE_BYOS_SECRET when set, "" when unset; an explicit --secret overrides the environment
    - A byos subprocess started with --bind 127.0.0.1 --port <free> and SKYPANE_BYOS_SECRET=s3cret-test in its env: /proc/net/tcp shows its LISTEN socket on 0100007F:<port hex> only
    - /proc/<pid>/cmdline of that process does not contain "s3cret-test"
    - Enrolment with the env-provided secret succeeds and with a wrong secret fails, exactly as with --secret before (use the enrolment request shape the existing byos tests use)
  </behavior>
  <action>
    Gate first (D-01): confirm Phase 36 is merged on main — every .planning/phases/36-*/36-*-PLAN.md has a SUMMARY and ROADMAP marks Phase 36 complete. If not, stop and report "blocked: Phase 36 not merged". Also check Phase 34 status and note in the SUMMARY which secret model is live. If byos_server.py makes any outbound connection, stop and report (IPAddressDeny=any would break it).

    RED: stub-server/test_byos_bind_secret.py (pytest; Phase 32 fixtures; Linux /proc parsing, skip on non-Linux) for the behavior list. Must fail.

    GREEN: in byos_server.py change the secret option's default to os.environ.get("SKYPANE_BYOS_SECRET", "") evaluated when the parser is built at run time; update its help to say production supplies it through the environment and that --secret is visible in ps, kept only for the LAN stub flow. If FW-08 introduced a different secret option name, apply the same env-default rule to whatever secret remains, with an env var name documented in skypane.env.example. Add --bind (default "0.0.0.0", help as in the companion), construct the server with (args.bind, args.port), include the bind address in the startup print. Change nothing else (no enrolment logic change). Why-comments only, English (D-A3).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q stub-server/test_byos_bind_secret.py && server/.venv/bin/ruff check stub-server/ && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"--bind"' stub-server/byos_server.py` == 1 and `grep -c "args.bind" stub-server/byos_server.py` >= 1
    - `grep -c 'os.environ.get("SKYPANE_BYOS_SECRET"' stub-server/byos_server.py` == 1 (or the FW-08 equivalent, recorded in the SUMMARY)
    - `grep -c '("0.0.0.0", args.port)' stub-server/byos_server.py` == 0
    - pytest exits 0; full suite green (existing byos-spawning tests pass no --secret and keep working)
  </acceptance_criteria>
  <done>byos can bind loopback only and takes its secret from the environment, with tests proving both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: byos unit — loopback bind, IP filter, no secret in argv; tests and docs</name>
  <files>deploy/skypane-byos.service, deploy/tests/test_units.py, deploy/skypane.env.example, deploy/README.md</files>
  <read_first>
    - deploy/skypane-byos.service (as Plan 37-03 left it) and deploy/tests/test_units.py
    - deploy/skypane.env.example SKYPANE_BYOS_SECRET block; deploy/README.md byos/secret mentions (grep "--secret", "SKYPANE_BYOS_SECRET")
  </read_first>
  <behavior>
    - byos unit: ExecStart contains --bind 127.0.0.1 and does not contain --secret; IPAddressDeny=any and IPAddressAllow=localhost present; still EnvironmentFile=/opt/skypane/skypane.env
    - companion and poll units still have no IPAddressDeny (unchanged assertion)
    - offline systemd-analyze security --threshold=20 still passes for byos (expected ~0.9)
  </behavior>
  <action>
    Edit deploy/skypane-byos.service: remove the `--secret ${SKYPANE_BYOS_SECRET}` continuation line, add `--bind 127.0.0.1`, add IPAddressDeny=any and IPAddressAllow=localhost with a why-comment (byos makes no outbound calls; Caddy on loopback is its only client; the filter applies both directions), and rewrite the header comment lines that say the secret is on the command line or that byos binds 0.0.0.0. Update deploy/tests/test_units.py to assert the behavior list (replace any Wave A assertion that expected --secret in the byos ExecStart). Update the SKYPANE_BYOS_SECRET comment in skypane.env.example (read from the environment, never passed on the command line) and the README sentences that describe the byos secret or bind. Record the byos offline score after this change in 37-SEC-BASELINE.md "Wave B (CP-11)".
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q deploy/tests/test_units.py deploy/tests/test_docs.py && systemd-analyze security --offline=true --threshold=20 deploy/skypane-byos.service >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- "--secret" deploy/skypane-byos.service` == 0 and `grep -c -- "--bind 127.0.0.1" deploy/skypane-byos.service` == 1
    - `grep -c "^IPAddressDeny=any$" deploy/skypane-byos.service` == 1 and `grep -c "^IPAddressAllow=localhost$" deploy/skypane-byos.service` == 1
    - `grep -c "IPAddressDeny" deploy/skypane-companion.service deploy/skypane-poll.service` shows 0 for both
    - pytest exits 0; offline score passes --threshold=20
  </acceptance_criteria>
  <done>The byos unit is loopback-only, IP-filtered, and passes its secret only through the environment.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: CP-11 — deploy Wave B and prove loopback-only, no secret in ps, device still fetches</name>
  <files>.planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md</files>
  <read_first>
    - 37-RESEARCH.md CP-11
    - 37-SEC-BASELINE.md (byos before/after from CP-1/CP-6)
  </read_first>
  <action>After this plan's code is merged, the developer approves the production deploy and runs the checks. Claude records the results in 37-SEC-BASELINE.md "Wave B (CP-11)".</action>
  <what-built>byos bound to 127.0.0.1 with IPAddressDeny=any/IPAddressAllow=localhost; secret from EnvironmentFile only.</what-built>
  <how-to-verify>
    1. Approve the "Deploy to production" run for the merge commit in GitHub Actions; it must be green.
    2. Then:
    ```bash
    ssh ubuntu@<vps> 'pgrep -af byos_server.py'
    ssh ubuntu@<vps> 'sudo ss -ltnp | grep -E ":8642 "'
    curl -s -o /dev/null -w '%{http_code}\n' https://<public-host>/device/v1/display
    ssh ubuntu@<vps> 'sudo systemd-analyze security skypane-byos.service | tail -n 1'
    ssh ubuntu@<vps> 'sudo journalctl -u skypane-byos -f'    # leave running until the frame wakes, then Ctrl-C
    ```
    Expected: the pgrep line contains `--bind 127.0.0.1` and no `--secret` and no secret value; ss shows `127.0.0.1:8642` only; curl prints 401; the byos score is lower than its CP-6 value; at the next frame wake the journal shows the display request and image download succeeding (and the frame shows a fresh panel).
  </how-to-verify>
  <acceptance_criteria>
    - Developer pasted pgrep, ss, curl code, the byos score line, and confirmed the device fetch
    - 37-SEC-BASELINE.md "Wave B (CP-11)" filled
  </acceptance_criteria>
  <verify>
    <human-check>Developer runs the how-to-verify commands and pastes the output; Claude compares it with the Expected line.</human-check>
    <automated>grep -A6 "Wave B (CP-11)" .planning/phases/37-security-and-operations-hardening/37-SEC-BASELINE.md | grep -c "127.0.0.1:8642"</automated>
  </verify>
  <done>byos loopback-only and secret-free argv verified live and recorded.</done>
  <resume-signal>Type "waveb-ok" and paste outputs, or paste the failing job log</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: CP-7 — remove the old in-place code directories</name>
  <files>none (VPS action only; evidence goes into 37-11-SUMMARY.md)</files>
  <read_first>
    - 37-RESEARCH.md CP-7 and "Runtime State Inventory" (build artifacts row)
  </read_first>
  <action>Once production has run from the release layout for at least a few days without incident (true by the time Wave B lands), the developer deletes the old in-place code directories kept as a manual fallback. Claude records the date in the SUMMARY.</action>
  <what-built>Release layout in production since the Plan 37-09 cutover.</what-built>
  <how-to-verify>
    ```bash
    ssh ubuntu@<vps>
    readlink /opt/skypane/current
    grep -l '/opt/skypane/\(server\|stub-server\|companion\|config\)/' /etc/systemd/system/skypane-*.service; echo "grep-exit=$?"   # expect grep-exit=1 (no unit uses them)
    sudo rm -rf /opt/skypane/server /opt/skypane/stub-server /opt/skypane/companion /opt/skypane/config
    ls /opt/skypane
    systemctl is-active skypane-byos skypane-companion skypane-poll.timer caddy skypane-backup.timer
    exit
    ```
    Expected: grep-exit=1 before deletion; `ls` shows current, releases, state, venv, skypane.env; five "active".
  </how-to-verify>
  <acceptance_criteria>
    - Developer pasted grep-exit=1, the ls output and the is-active lines
  </acceptance_criteria>
  <verify>
    <human-check>Developer runs the how-to-verify commands and pastes the output; Claude compares it with the Expected line.</human-check>
  </verify>
  <done>Old in-place code directories removed with services still active.</done>
  <resume-signal>Type "old-layout-removed" and paste outputs</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network -> byos port 8642 | Must only be reachable from Caddy on loopback |
| Other local users -> /proc argv | Secret must not be in argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-50 | Information disclosure | byos secret in ps | mitigate | Secret from EnvironmentFile via os.environ; --secret removed from the unit; ProtectProc=invisible stays |
| T-37-51 | Elevation / bypass | byos reachable without Caddy | mitigate | --bind 127.0.0.1 + IPAddressDeny=any/IPAddressAllow=localhost; ufw deny 8642 unchanged |
| T-37-52 | Denial of service | IP filter blocking a needed outbound call | mitigate | Gate: stop if byos_server.py makes outbound connections; live device-wake check in CP-11 |
| T-37-53 | Tampering | enrolment model changed by accident | mitigate | Only argv/env plumbing changes; FW-08 shape followed; existing enrolment tests must pass |
| T-37-54 | Denial of service | deleting old dirs still in use | mitigate | grep of installed units before rm; is-active after |
</threat_model>

<verification>
- `pytest -q stub-server/test_byos_bind_secret.py deploy/tests/test_units.py` green; full suite green; CP-11 and CP-7 signed off
</verification>

<success_criteria>
ROADMAP SC-5 complete: before/after scores recorded (CP-1, CP-6, CP-11), byos reachable on loopback only, no secret in ps.
</success_criteria>

<output>
Create `.planning/phases/37-security-and-operations-hardening/37-11-SUMMARY.md` when done.
</output>
