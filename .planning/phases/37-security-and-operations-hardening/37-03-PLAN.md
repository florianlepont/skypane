---
phase: 37-security-and-operations-hardening
plan: 03
type: execute
wave: 1
depends_on: []
external_depends_on: ["Phase 32 (pytest infrastructure; Phase 32 TST-04/06/07 also edit ci.yml — re-read it)"]
files_modified:
  - pyproject.toml
  - deploy/Caddyfile
  - deploy/render_caddyfile.sh
  - deploy/skypane-byos.service
  - deploy/skypane-companion.service
  - deploy/skypane-poll.service
  - deploy/skypane-backup.service
  - deploy/skypane-backup.timer
  - .github/workflows/ci.yml
  - deploy/tests/test_caddyfile.py
  - deploy/tests/test_units.py
  - deploy/tests/test_ci_secrets.py
autonomous: true
requirements: [SEC-02, SEC-04, SEC-05, SEC-06, SEC-07]
user_setup: []

must_haves:
  truths:
    - "Both rendered Caddy site blocks send Strict-Transport-Security max-age=31536000 without preload (D-15) — ROADMAP SC-2 first half"
    - "The Caddyfile is rendered from skypane.env hostnames by one anchored script that rejects any hostname outside [A-Za-z0-9.-]"
    - "Every unit carries the SEC-06 directive set and scores <= 2.0 under systemd-analyze security --offline in CI"
    - "Every unit runs code from /opt/skypane/current/...; the companion unit passes --bind 127.0.0.1 (D-22); no IP filter on companion or poll (D-18/D-22)"
    - "A nightly skypane-backup.timer/service pair exists (oneshot, User=skypane, PrivateNetwork=true)"
    - "No ${{ secrets.* }} expression appears inside any run: script in ci.yml; DEPLOY_HOST_KEY and DEPLOY_SSH_TARGET arrive through env: (D-20)"
  artifacts:
    - path: "deploy/render_caddyfile.sh"
      provides: "anchored hostname render: render_caddyfile.sh <template> <public-host> <companion-host> > out"
    - path: "deploy/skypane-backup.service"
      provides: "nightly snapshot job unit"
      contains: "PrivateNetwork=true"
    - path: "deploy/skypane-backup.timer"
      provides: "OnCalendar 03:15 UTC, Persistent"
      contains: "Persistent=true"
    - path: "deploy/tests/test_units.py"
      provides: "directive-set + offline score tests"
  key_links:
    - from: "deploy/skypane-backup.service ExecStart"
      to: "/opt/skypane/current/deploy/backup/skypane_backup.py (Plan 37-04)"
      via: "CLI contract --state-dir --archive-dir --keep"
      pattern: "skypane_backup\\.py"
    - from: "deploy/skypane-companion.service"
      to: "companion/app.py --bind (Plan 37-01)"
      via: "ExecStart argument"
      pattern: "--bind 127\\.0\\.0\\.1"
    - from: ".github/workflows/ci.yml test job"
      to: "systemd-analyze security --offline=true --threshold=20"
      via: "CI step over deploy/*.service"
      pattern: "systemd-analyze security --offline=true"
---

<objective>
Wave A — deploy configuration: HSTS + shared Caddyfile renderer (SEC-02, SEC-05), systemd unit
hardening + release-layout paths + nightly backup unit (SEC-06, SEC-05, SEC-04), and CI secrets via
`env:` plus an offline systemd score gate (SEC-07, SEC-06).

External dependency: Phase 32 only (D-01); Task 1 opens with the Phase 32 gate. The byos
`--bind 127.0.0.1` and `IPAddressDeny=any`/`IPAddressAllow=localhost` are Wave B (Plan 37-11) — do NOT
make them here. The shared byos secret is not this phase's to move: Phase 34 plan 34-03 (FW-08,
D-34-01) retires it for a per-device registry (`<state-dir>/devices.json`) and deletes the
`--secret ${SKYPANE_BYOS_SECRET}` line from the byos unit. Leave that line exactly as found — if
34-03 has already removed it, never reintroduce it; if it is still there, keep it (34-03 removes it).

Nothing in this plan reaches production by itself: units and the Caddyfile are installed only by the
new activate.sh (Plan 37-06) at the supervised cutover (Plan 37-09). The old deploy.sh never ships
units, so merging these files is inert until then.

Output: edited Caddyfile/units/ci.yml, two new unit files, render script, three pytest files,
pytest testpaths/coverage updated for deploy/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-security-and-operations-hardening/37-CONTEXT.md
@.planning/phases/37-security-and-operations-hardening/37-RESEARCH.md
@.planning/phases/37-security-and-operations-hardening/37-PATTERNS.md

<interfaces>
deploy/Caddyfile: two site blocks whose opening lines sit at column 0 and must stay byte-identical
  (they are the render anchors): `203-0-113-10.nip.io {` (device, reverse_proxy 127.0.0.1:8642) and
  `config-203-0-113-10.nip.io {` (companion, reverse_proxy 127.0.0.1:8643).
Anchored sed today (provision.sh): s/^config-203-0-113-10\.nip\.io {/${COMPANION_HOST} {/ then s/^203-0-113-10\.nip\.io {/${PUBLIC_HOST} {/
Units today: ExecStart=/opt/skypane/venv/bin/python3 /opt/skypane/{stub-server/byos_server.py|companion/app.py|server/poll_loop.py} ...;
  companion and poll pass --geofence /opt/skypane/config/runway3.json; byos passes --secret ${SKYPANE_BYOS_SECRET} until Phase 34 plan 34-03 retires the shared secret and deletes that line (re-read the unit: it may already be gone);
  existing hardening: NoNewPrivileges, PrivateTmp, ProtectSystem=strict, ProtectHome, ReadWritePaths=/opt/skypane/state.
skypane_backup.py CLI contract (implemented by Plan 37-04):
  /opt/skypane/venv/bin/python3 /opt/skypane/current/deploy/backup/skypane_backup.py --state-dir /opt/skypane/state --archive-dir /var/lib/skypane-backup/archives --keep 14
ci.yml deploy job: "Trust the production host key" step echoes ${{ secrets.DEPLOY_HOST_KEY }} inside run:; "Deploy" step passes ${{ secrets.DEPLOY_SSH_TARGET }} inside run:.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 32 gate, HSTS in both site blocks, shared anchored renderer, pytest wiring for deploy/</name>
  <files>deploy/Caddyfile, deploy/render_caddyfile.sh, deploy/tests/test_caddyfile.py, pyproject.toml</files>
  <read_first>
    - deploy/Caddyfile (both site blocks, comments)
    - deploy/provision.sh (hostname validation loop and the anchored sed render)
    - pyproject.toml ([tool.pytest.ini_options] testpaths/import mode from Phase 32, [tool.coverage.run] source)
    - Phase 32 conftest.py (socket guard; import mode — if pytest runs in the default "prepend" import mode, test basenames must be unique across dirs or deploy/tests needs an __init__.py)
  </read_first>
  <behavior>
    - render_caddyfile.sh deploy/Caddyfile pub.example.org cfg.example.org prints a Caddyfile whose site lines are `pub.example.org {` and `cfg.example.org {`, and the companion line is not corrupted by the device substitution
    - Each rendered site block contains `header Strict-Transport-Security "max-age=31536000"`; the file contains neither `preload` nor `includeSubDomains`
    - Hostnames `a b`, `x;rm -rf /`, `$(id)`, `` `id` ``, empty string -> exit code != 0 and empty stdout
    - render_caddyfile.sh never sources or reads skypane.env (grep: no `source`, no `. ` include, no `skypane.env`)
  </behavior>
  <action>
    Gate first (D-01/D-02): confirm Phase 32 is on main (every 32-*-PLAN.md has a SUMMARY; pyproject has [tool.pytest.ini_options]). If not, stop: "blocked: Phase 32 not merged".

    deploy/Caddyfile: add `header Strict-Transport-Security "max-age=31536000"` inside each of the two site blocks (right after the reverse_proxy line), with a short why-comment: HSTS is a browser policy, the firmware's esp_http_client ignores it, so the device block is safe (D-15); no preload, no includeSubDomains. Do not touch the two anchor lines.

    Create deploy/render_caddyfile.sh (bash, set -euo pipefail, header comment: purpose, runs on the VPS as root from activate.sh, usage): args <template> <public-host> <companion-host>; validate both hosts non-empty and matching ^[A-Za-z0-9.-]+$ (same rule as provision.sh), error to stderr prefixed "render_caddyfile.sh:" and exit 1 before printing anything; then run the two anchored sed substitutions (companion line first) and print to stdout. chmod 755 (git update-index --chmod=+x).

    pyproject.toml: add deploy/tests to pytest testpaths (keep Phase 32's entries), add deploy/backup to coverage source if Phase 32's coverage config lists sources explicitly. If Phase 32 uses the default import mode and any deploy test basename could collide, add an empty deploy/tests/__init__.py and list it in the SUMMARY.

    Write deploy/tests/test_caddyfile.py covering the behavior list via subprocess.run(["bash", "deploy/render_caddyfile.sh", ...]) and text parsing of site blocks (split at lines ending in " {" at column 0, track brace depth).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q deploy/tests/test_caddyfile.py && bash -n deploy/render_caddyfile.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'header Strict-Transport-Security "max-age=31536000"' deploy/Caddyfile` == 2
    - `grep -v '^\s*#' deploy/Caddyfile | grep -Ec 'preload|includeSubDomains'` == 0
    - `grep -c '^203-0-113-10\.nip\.io {' deploy/Caddyfile` == 1 and `grep -c '^config-203-0-113-10\.nip\.io {' deploy/Caddyfile` == 1
    - `grep -Ec '(^|[^A-Za-z_])(source|\.) .*env|skypane\.env' deploy/render_caddyfile.sh` == 0
    - `test -x deploy/render_caddyfile.sh` succeeds; pytest command exits 0
  </acceptance_criteria>
  <done>HSTS is in both blocks and a tested, anchored renderer exists for activate.sh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden all units, move them to /opt/skypane/current, add the nightly backup unit and timer</name>
  <files>deploy/skypane-byos.service, deploy/skypane-companion.service, deploy/skypane-poll.service, deploy/skypane-backup.service, deploy/skypane-backup.timer, deploy/tests/test_units.py</files>
  <read_first>
    - deploy/skypane-byos.service, deploy/skypane-companion.service, deploy/skypane-poll.service, deploy/skypane-poll.timer (whole files, including header comments)
    - 37-RESEARCH.md §SEC-06 directive table and "UMask=0027 is safe here"; §SEC-04 "Snapshot job"
    - 37-PATTERNS.md "systemd units"
  </read_first>
  <behavior>
    - For byos, companion, poll and backup units: CapabilityBoundingSet= (empty), AmbientCapabilities= (empty), PrivateDevices=true, DevicePolicy=closed, ProtectKernelTunables=true, ProtectKernelModules=true, ProtectKernelLogs=true, ProtectControlGroups=true, ProtectClock=true, ProtectHostname=true, ProtectProc=invisible, ProcSubset=pid, RestrictNamespaces=true, RestrictRealtime=true, RestrictSUIDSGID=true, LockPersonality=true, SystemCallArchitectures=native, MemoryDenyWriteExecute=true, SystemCallFilter=@system-service, SystemCallErrorNumber=EPERM, UMask=0027, NoNewPrivileges=true, PrivateTmp=true, ProtectSystem=strict, ProtectHome=true
    - byos/companion/poll: RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX; backup: RestrictAddressFamilies=AF_UNIX and PrivateNetwork=true
    - companion and poll have no IPAddressDeny/IPAddressAllow (they make outbound calls)
    - Every ExecStart script path starts with /opt/skypane/current/; companion ExecStart contains --bind 127.0.0.1; --geofence points at /opt/skypane/current/adsb-test/runway3.json
    - Backup service: Type=oneshot, User=skypane, Group=skypane, ReadWritePaths contains /opt/skypane/state and /var/lib/skypane-backup/archives, no [Install] section; timer: OnCalendar=*-*-* 03:15:00 UTC, Persistent=true, RandomizedDelaySec=10m, Unit=skypane-backup.service, WantedBy=timers.target
    - When systemd-analyze supports --offline (skip otherwise), `systemd-analyze security --offline=true --threshold=20 <unit>` exits 0 for all four services
  </behavior>
  <action>
    Edit the three existing units: ExecStart script paths /opt/skypane/{stub-server,companion,server}/... -> /opt/skypane/current/{...}; --geofence /opt/skypane/config/runway3.json -> /opt/skypane/current/adsb-test/runway3.json; companion ExecStart gains --bind 127.0.0.1 (D-22); keep EnvironmentFile=/opt/skypane/skypane.env, the venv interpreter path, WorkingDirectory, Restart settings and ReadWritePaths=/opt/skypane/state. byos keeps its current bind in this plan (Wave B, D-01) and its `--secret` line exactly as found (present before Phase 34 plan 34-03 lands, absent after; never add it back). Append the directive set from the behavior list under the existing hardening comment, and rewrite that comment and each unit's header comment where the change makes them false (companion no longer "binds 0.0.0.0"; why UMask=0027 is safe; why no IP filter on companion/poll; why ProtectProc=invisible hides every service's argv from other users). English, why-only, no plan IDs (D-A3).

    Create deploy/skypane-backup.service modelled on skypane-poll.service: Type=oneshot, User=skypane, Group=skypane, EnvironmentFile not needed, ExecStart per the CLI contract in <interfaces>, the full directive set, PrivateNetwork=true, RestrictAddressFamilies=AF_UNIX, ReadWritePaths=/opt/skypane/state /var/lib/skypane-backup/archives (state must be writable because opening a WAL database needs its -shm). Create deploy/skypane-backup.timer modelled on skypane-poll.timer with the timer values from the behavior list and a why-comment (03:15 UTC is quiet for the frame; Persistent catches up after downtime).

    Write deploy/tests/test_units.py: parse each unit with configparser.ConfigParser(strict=False, interpolation=None) plus a small line scanner for repeated keys and backslash continuations of ExecStart; assert the behavior list; the offline score test uses shutil.which("systemd-analyze") and pytest.skip when missing or when `systemd-analyze security --help` lacks --offline. Record the offline score of each unit before (from `git show HEAD:deploy/<unit>` written to tmp) and after in the SUMMARY (D-17).
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q deploy/tests/test_units.py && for u in deploy/skypane-byos.service deploy/skypane-companion.service deploy/skypane-poll.service deploy/skypane-backup.service; do systemd-analyze security --offline=true --threshold=20 "$u" >/dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "SystemCallFilter=@system-service" deploy/*.service | wc -l` == 4 and `grep -l "UMask=0027" deploy/*.service | wc -l` == 4
    - `grep -c "IPAddressDeny" deploy/skypane-companion.service deploy/skypane-poll.service` shows 0 for both files
    - `grep -c "^ExecStart=/opt/skypane/venv/bin/python3 /opt/skypane/current/" deploy/skypane-byos.service deploy/skypane-companion.service deploy/skypane-poll.service deploy/skypane-backup.service` shows 1 for each file
    - `grep -c -- "--bind 127.0.0.1" deploy/skypane-companion.service` == 1
    - `grep -c "/opt/skypane/config/" deploy/*.service` totals 0
    - Each of the 4 units passes `systemd-analyze security --offline=true --threshold=20` locally (systemd 255 is available in the sandbox)
  </acceptance_criteria>
  <done>Four hardened units scoring <= 2.0 offline, release-layout paths, companion loopback bind, nightly backup timer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CI secrets through env: and an offline systemd-analyze gate</name>
  <files>.github/workflows/ci.yml, deploy/tests/test_ci_secrets.py</files>
  <read_first>
    - .github/workflows/ci.yml (whole file as Phase 32 left it; test job steps; deploy job steps and comments; every concurrency block)
    - 37-RESEARCH.md "ci.yml deploy job (D-20)" snippet and §SEC-07
  </read_first>
  <behavior>
    - test_ci_secrets: scanning ci.yml, no line inside a `run:` block (inline `run: ...` or a `run: |` block scalar until indentation returns) contains `${{ secrets.`
    - test_ci_secrets: the deploy job references DEPLOY_HOST_KEY and DEPLOY_SSH_TARGET under `env:` and uses "$DEPLOY_HOST_KEY" / "$DEPLOY_SSH_TARGET" in run:
    - test_ci_secrets: the test job has a step running `systemd-analyze security --offline=true --threshold=20`
  </behavior>
  <action>
    Edit the deploy job per D-20: "Trust the production host key" step gets env: DEPLOY_HOST_KEY: ${{ secrets.DEPLOY_HOST_KEY }} and its run becomes install -d -m 700 ~/.ssh; printf '%s\n' "$DEPLOY_HOST_KEY" >> ~/.ssh/known_hosts; chmod 644 ~/.ssh/known_hosts. "Deploy" step gets env: DEPLOY_SSH_TARGET: ${{ secrets.DEPLOY_SSH_TARGET }} and run: ./deploy/deploy.sh "$DEPLOY_SSH_TARGET". Keep the step comments but reword the Deploy comment (it will no longer be rsync: say deploy.sh ships the committed tree and activate.sh swaps, probes and rolls back). The webfactory/ssh-agent `with:` input stays. Do not change any concurrency block or group (D-13).

    Add to the test job, after the lint step, a step named "Score systemd units (offline, fail above 2.0)" whose run loops over deploy/*.service calling systemd-analyze security --offline=true --threshold=20 "$u" (fails the job on any unit above threshold). The shellcheck step is added later by Plan 37-07 once every deploy script exists.

    Write deploy/tests/test_ci_secrets.py (stdlib text scan; no PyYAML dependency) for the behavior list.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q deploy/tests/test_ci_secrets.py && git diff HEAD -- .github/workflows/ci.yml | grep -Ec '^[-+][^-+].*(concurrency|cancel-in-progress|group:)' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'echo "${{ secrets.DEPLOY_HOST_KEY }}"' .github/workflows/ci.yml` == 0
    - `grep -c 'deploy.sh "${{ secrets.DEPLOY_SSH_TARGET }}"' .github/workflows/ci.yml` == 0
    - `grep -c 'printf .%s\\n. "$DEPLOY_HOST_KEY"' .github/workflows/ci.yml` == 1
    - `grep -c "systemd-analyze security --offline=true --threshold=20" .github/workflows/ci.yml` == 1
    - No concurrency lines changed (verify command)
  </acceptance_criteria>
  <done>No secret is interpolated into a run: script, and CI fails when any unit's offline exposure exceeds 2.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Caddy (TLS) | First-visit downgrade risk without HSTS |
| skypane.env -> Caddyfile render | Hostnames are operator input used in a root-owned config |
| GitHub secrets -> runner shell | Secrets must not be spliced into script text |
| Service process -> host | A compromised Python process's reach into kernel, devices, other processes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Information disclosure | TLS downgrade on first visit | mitigate | HSTS max-age=31536000 on both site blocks (no preload, D-15) |
| T-37-11 | Tampering | Caddyfile render injection | mitigate | Hostname regex ^[A-Za-z0-9.-]+$ before sed; anchored substitutions; never sources the env file |
| T-37-12 | Elevation of privilege | service sandbox | mitigate | SEC-06 directive set on all units, CI offline score gate --threshold=20 |
| T-37-13 | Information disclosure | byos shared secret in argv (until Phase 34 plan 34-03 retires it) | mitigate | ProtectProc=invisible hides other users' /proc entries now; 34-03 replaces the shared secret with the per-device registry and drops `--secret` from the unit; Plan 37-11 verifies no secret in argv |
| T-37-14 | Tampering / injection | ci.yml `${{ }}` inside run: | mitigate | Secrets passed via env: and quoted "$VAR"; test_ci_secrets.py guards regressions |
| T-37-15 | Denial of service | MemoryDenyWriteExecute/ProcSubset breaking CPython on the VPS | mitigate | Smoke-tested at cutover (Plan 37-09); revert only the offending directive and record it |
</threat_model>

<verification>
- `pytest -q deploy/tests/test_caddyfile.py deploy/tests/test_units.py deploy/tests/test_ci_secrets.py` green
- Offline systemd-analyze <= 2.0 for four units; `ruff check .` clean; full suite green
</verification>

<success_criteria>
HSTS configured (SC-2, config side), unit hardening measurable offline (SC-5 "before/after" CI half), units/Caddyfile ready to be deployed by activate.sh (SC-4 prerequisite), CI secrets via env: (SEC-07).
</success_criteria>

<output>
Create `.planning/phases/37-security-and-operations-hardening/37-03-SUMMARY.md` with the offline before/after scores per unit.
</output>
