---
phase: 37-security-and-operations-hardening
plan: 05
type: execute
wave: 2
depends_on: ["37-01"]
external_depends_on: ["Phase 32 (pytest infrastructure, browser test policy)"]
files_modified:
  - companion/auth.py
  - companion/app.py
  - companion/i18n_fr/common.py
  - companion/test_post_origin.py
  - companion/test_browser_origin.py
autonomous: true
requirements: [SEC-03]
user_setup: []

must_haves:
  truths:
    - "A POST to any companion route (login included) with Origin from another site, Origin: null, or Sec-Fetch-Site cross-site/same-site is rejected with 403 and changes nothing — ROADMAP SC-2 second half"
    - "Same-origin browser POSTs (forms and the quick-switch fetch) and header-less clients behave exactly as before (D-16)"
    - "A real browser page on another origin submitting a form to the companion gets 403 and the device config is unchanged"
  artifacts:
    - path: "companion/auth.py"
      provides: "post_origin_ok(headers) -> bool"
      contains: "def post_origin_ok"
    - path: "companion/app.py"
      provides: "gate as the first statement of do_POST; 403 page"
    - path: "companion/test_post_origin.py"
      provides: "pytest HTTP tests over every POST route"
    - path: "companion/test_browser_origin.py"
      provides: "Playwright cross-origin form test"
  key_links:
    - from: "companion/app.py do_POST"
      to: "auth.post_origin_ok(self.headers)"
      via: "first statement, before routing and read_form()"
      pattern: "post_origin_ok\\(self\\.headers\\)"
---

<objective>
Wave A — SEC-03 Origin / Sec-Fetch-Site check on every POST (D-16), as defence in depth on top of
SameSite=Strict.

Depends on Plan 37-01 (same files: companion/auth.py, companion/app.py). External dependency:
Phase 32 only (D-01); Task 1 opens with the Phase 32 gate.

Output: `auth.post_origin_ok()`, the do_POST hook with a 403 page, French strings, pytest HTTP
tests over every POST route, one Playwright cross-origin test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-security-and-operations-hardening/37-CONTEXT.md
@.planning/phases/37-security-and-operations-hardening/37-RESEARCH.md
@.planning/phases/37-security-and-operations-hardening/37-PATTERNS.md
@.planning/phases/37-security-and-operations-hardening/37-01-SUMMARY.md

<interfaces>
companion/app.py (re-grep):
- Handler.do_POST(self): parsed = urlsplit(self.path); path = parsed.path; then a chain of route checks
  (LOGIN_ROUTE, SETTINGS_ROUTE, POLL_ROUTE, QUICK_DISPLAY_ROUTE, QUICK_QUIET_HOURS_ROUTE, QUICK_LED_ROUTE,
  THEME_ROUTE, LANG_ROUTE, LOGOUT_ROUTE, airlines_page.RESOLVE_ROUTE, manual-resolution delete prefix,
  illustration upload prefix, RULES_ADD_ROUTE, CALENDAR_DISCONNECT_ROUTE, CALENDAR_CONNECT_ROUTE,
  NOTIFICATIONS_TEST_ROUTE, rules delete prefix) and a final 404. No do_PUT/do_DELETE/do_PATCH.
- Handler._not_found_page(self): pre-auth-safe page (lang from cookie, health_alert only when authenticated)
- Handler.send_html(self, code, html_str): adds Cache-Control no-store + hardening headers
- NOT_FOUND_TITLE / NOT_FOUND_PURPOSE_TEXT constants pattern; FR "Not Found": "Introuvable" in companion/i18n_fr/common.py
- companion/static/quick-switch.js: the only fetch POST (method POST, credentials same-origin, redirect manual)
Rule (RESEARCH "SEC-03 gate" code example): reject Sec-Fetch-Site in {cross-site, same-site}; if Origin
present: reject "null"; else compare urlsplit(origin) netloc (default :443/:80 dropped) with Host (default port dropped), lowercase; neither header -> allow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 32 gate, post_origin_ok() + do_POST hook + 403 page, HTTP tests over every POST route</name>
  <files>companion/auth.py, companion/app.py, companion/i18n_fr/common.py, companion/test_post_origin.py</files>
  <read_first>
    - companion/app.py do_POST (all branches), _not_found_page, send_html, the NOT_FOUND_* constants
    - companion/auth.py after Plan 37-01 (append the new helper; do not disturb LoginThrottle)
    - companion/i18n_fr/common.py ("Not Found" entry and docstring typography rules)
    - companion/test_login_throttle.py (Plan 37-01) for the companion-server fixture used there; Phase 32/33 conftest fixtures
  </read_first>
  <behavior>
    - Unit: post_origin_ok({}) True; {"Origin": "https://skypane.algernon.ovh", "Host": "skypane.algernon.ovh"} True; {"Origin": "https://skypane.algernon.ovh:443", "Host": "skypane.algernon.ovh"} True; {"Origin": "http://127.0.0.1:8650", "Host": "127.0.0.1:8650"} True
    - Unit: {"Origin": "https://evil.example", "Host": "skypane.algernon.ovh"} False; {"Origin": "null", ...} False; {"Sec-Fetch-Site": "cross-site"} False; {"Sec-Fetch-Site": "same-site"} False; {"Sec-Fetch-Site": "same-origin", "Origin": matching} True; {"Sec-Fetch-Site": "none"} True; Origin present but Host missing -> False
    - HTTP (parametrised over every POST route path found in do_POST): with Origin https://evil.example -> 403; with Sec-Fetch-Site: cross-site -> 403
    - HTTP: POST /login with the right password and Origin: https://evil.example -> 403 and no Set-Cookie header
    - HTTP: an authenticated POST to the display quick-switch route with a cross-site Origin -> 403 and device_config.json unchanged on disk; the same POST with matching Origin + Sec-Fetch-Site: same-origin -> the route's normal status and the change applied
    - HTTP: POST /login with no Origin and no Sec-Fetch-Site behaves as before (303 on right password)
    - The 403 body is an HTML page through send_html (Cache-Control: no-store present)
  </behavior>
  <action>
    Gate first (D-01/D-02): confirm Phase 32 is on main; otherwise stop: "blocked: Phase 32 not merged".

    RED: companion/test_post_origin.py (unit tests of auth.post_origin_ok + HTTP tests on the same companion-server fixture Plan 37-01 used; build the route list by reading do_POST's route constants from the app module so a route added later is covered automatically, plus one concrete path for each prefix route). Must fail.

    GREEN: add to companion/auth.py a pure function post_origin_ok(headers) implementing the rule in <interfaces> (headers is any mapping with .get; compare case-insensitively; strip a default :443 from https origins and :80 from http origins, and the same default ports from Host). Docstring: why Origin/Fetch Metadata on top of SameSite=Strict, why header-less requests are allowed (non-browser clients still need the session cookie), why Host is a sound comparison (browsers set it; Caddy passes it through).

    In companion/app.py make the first statement of do_POST: if not auth.post_origin_ok(self.headers): return self.send_html(403, self._forbidden_page()) — before urlsplit routing and before any read_form(), so login and every future route are covered (D-16). Add _forbidden_page() modelled on _not_found_page (pre-auth safe, same shell), with constants FORBIDDEN_TITLE = "Request refused" and FORBIDDEN_PURPOSE_TEXT = "This request came from another site, so it was refused. Open SkyPane directly and try again." and the "Back to Home" link. Add FR entries to companion/i18n_fr/common.py: "Request refused" -> "Requête refusée"; the purpose sentence -> "Cette requête venait d’un autre site, elle a donc été refusée. Ouvrez SkyPane directement et réessayez." (U+2019 apostrophe). Keep the helper single so a later route-table refactor can move it trivially.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q companion/test_post_origin.py && server/.venv/bin/python -m pytest -q -k "i18n" companion/ && server/.venv/bin/ruff check companion/ && ./scripts/run-all-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def post_origin_ok" companion/auth.py` == 1
    - `awk '/def do_POST/{f=1;n=0} f&&NF{n++} f&&n<=3' companion/app.py | grep -c "post_origin_ok(self.headers)"` == 1 (hook within the first lines of do_POST)
    - `grep -c "def _forbidden_page" companion/app.py` == 1 and `grep -c '"Request refused": "Requête refusée"' companion/i18n_fr/common.py` == 1
    - pytest shows the parametrised every-route 403 tests passing; full suite exits 0 (existing tests send no Origin, so unchanged)
  </acceptance_criteria>
  <done>Every POST passes through one Origin/Fetch-Metadata gate; cross-site POSTs get a localized 403 and change nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright proof — cross-origin form rejected, same-origin UI unaffected</name>
  <files>companion/test_browser_origin.py</files>
  <read_first>
    - Phase 32/33 browser test fixtures and skip policy (grep conftest.py for playwright/chromium; TST rows on browser SKIP)
    - companion/test_browser_ux_helpers.py (login helper, harness start) and companion/static/quick-switch.js (the fetch POST)
    - companion/test_post_origin.py (Task 1) for the quick-switch route and form field names
  </read_first>
  <action>
    Create companion/test_browser_origin.py following Phase 32/33's browser-test conventions (use their fixtures and their skip-or-fail policy when Chromium is missing; do not invent a new policy). Scenario 1: start the companion on 127.0.0.1:<port A>; start a tiny http.server on 127.0.0.1:<port B> (a different origin, loopback so the socket guard allows it) serving an HTML page with an auto-submitting form whose action is http://127.0.0.1:<port A>/<quick display route> with the same fields the UI posts; log in on origin A in the same browser context (so the session cookie exists), open the page on origin B, wait for navigation; assert the response status is 403 and device_config.json on disk is unchanged. Scenario 2: on origin A, use the real quick-switch control (the fetch POST in quick-switch.js) and a normal Settings form save; assert both succeed and the value changes on disk. Each scenario uses its own isolated server/state dir.
  </action>
  <verify>
    <automated>server/.venv/bin/python -m pytest -q companion/test_browser_origin.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "403" companion/test_browser_origin.py` >= 1
    - pytest exits 0; in an environment with Chromium the two scenarios report "passed" (not skipped) — run once with Chromium available (Phase 32's CI image or the local Playwright shell) and record the result in the SUMMARY
  </acceptance_criteria>
  <done>A real browser proves the cross-origin rejection and that same-origin forms and fetch keep working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party page in the owner's browser -> companion POST | Ambient cookie could ride a cross-site request if SameSite were bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-22 | Tampering (CSRF) | every POST route incl. login | mitigate | post_origin_ok first in do_POST: Origin must equal Host; Sec-Fetch-Site cross-site/same-site rejected; Origin null rejected |
| T-37-23 | Spoofing | same-site sibling host (e.g. another *.nip.io name) | mitigate | same-site rejected, not only cross-site |
| T-37-24 | Denial of service | legitimate old browsers without headers | accept | Header-less requests allowed (D-16); SameSite=Strict still applies |
| T-37-25 | Information disclosure | 403 page leaking state pre-auth | mitigate | _forbidden_page mirrors _not_found_page (health data only when authenticated) |
</threat_model>

<verification>
- `pytest -q companion/test_post_origin.py companion/test_browser_origin.py` green; full suite green
</verification>

<success_criteria>
ROADMAP SC-2 second half: a cross-origin POST is rejected (HTTP and real-browser proof); same-origin flows unchanged.
</success_criteria>

<output>
Create `.planning/phases/37-security-and-operations-hardening/37-05-SUMMARY.md` when done.
</output>
