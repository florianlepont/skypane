---
phase: 16-calendar-linked-flight-highlighting-a-connected-calendar-sou
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/fixtures/calendar_crewwebplus_redacted.ics
  - server/fixtures/README.md
  - server/plane/calendar_rules.py
  - server/test_calendar_rules.py
  - scripts/run-all-tests.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "A redacted CrewWebPlus-shaped .ics fixture exists in the repo carrying every structural property the parser is asserted against — folded continuation lines, the STATUS:CANCELLED plus 1899-placeholder junk pair, the FLT/OFFD/CAHC/CPBL category mix, an escaped DESCRIPTION, and bare-UTC DTSTART/DTEND — and carries no real schedule, no real crew code and no real UID."
    - "An RFC 5545 content line folded across two or more physical lines (a continuation beginning with a single SPACE or HTAB) is rejoined into one logical line before any property is parsed, so a folded SUMMARY or DESCRIPTION can never be truncated or misparsed as a new property."
    - "A property line is split on its FIRST colon, with the parameter block (everything after the first semicolon of the name segment) separated from the bare property name, so DTSTART;VALUE=DATE-TIME:... is recognised as DTSTART."
    - "Only VEVENT blocks carrying CATEGORIES:FLT become match candidates; OFFD, CAHC and CPBL blocks never do, and any block carrying STATUS:CANCELLED is dropped before its dates are parsed."
    - "DTSTART/DTEND parse only the bare UTC YYYYMMDDTHHMMSSZ form (measured 63/63 in the real export, zero TZID). Any other date form — a TZID parameter, a VALUE=DATE all-day value, a truncated value — is REJECTED with a one-line warning and the event is dropped, never guessed into a fallback format."
    - "A malformed, truncated, empty or hostile feed body yields an empty candidate list and never raises, so one bad feed can never abort a poll cycle."
    - "A parsed entry carries only the airline IATA code, the two airport IATA codes and two epoch timestamps — never the flight number, never the UID, never the SUMMARY or DESCRIPTION text — keeping the persisted footprint of a named person's schedule to the minimum the matcher needs (D-03's privacy reasoning)."
    - "server/test_calendar_rules.py runs inside scripts/run-all-tests.sh's HARNESSES array, so the new harness is visible to the suite and to the coverage gate."
  artifacts:
    - server/fixtures/calendar_crewwebplus_redacted.ics
    - server/plane/calendar_rules.py
    - server/test_calendar_rules.py
  key_links:
    - "scripts/run-all-tests.sh's HARNESSES array must list server/test_calendar_rules.py or the new harness silently runs in no suite and is invisible to the coverage gate (16-VALIDATION.md Wave 0 requirement 3)."
    - "calendar_rules.py must stay a leaf importing stdlib + server.device_config + requests only. It must never import server.plane.colour_rules — D-02's precedence is wired the other way, via a keyword argument poll_loop.py computes and passes in — nor server.plane.enrich/detect/illustrations/manual_resolutions/render, or poll_loop -> calendar_rules -> X -> poll_loop becomes a real import cycle."
    - "Unfolding must run BEFORE the property split. Reversing that order silently corrupts exactly the SUMMARY the route regex depends on (16-RESEARCH.md Pitfall 1)."
    - "CATEGORIES:FLT filtering must run BEFORE date parsing. The OFFD fixture event deliberately carries a VALUE=DATE all-day date the date parser is required to reject; if the category filter ran second, a legitimate non-flight event would produce a rejection warning on every fetch."
  prohibitions:
    - "The fixture must not contain a real flight number, a real crew code, a real UID, a real person's name, or any real schedule taken from the supplied export."
    - "The parser must not implement any TZID resolution path, must not import zoneinfo, and must not fall back to a second date format when the bare-UTC pattern fails."
    - "A parsed entry dict must not carry a flight_number, uid, summary, description or status key."
    - "calendar_rules.py must not import server.plane.colour_rules, server.plane.enrich, server.plane.detect, server.plane.illustrations, server.plane.manual_resolutions or server.plane.render."
    - "No new entry may be added to server/requirements.txt or server/requirements-dev.txt."
    - "server/plane/render.py must not be modified by this plan."
---

<objective>
Land this phase's two Wave 0 items — the redacted `.ics` fixture and the new `server/test_calendar_rules.py` harness — together with the first half of `server/plane/calendar_rules.py`: the hand-rolled RFC 5545 subset parser that turns an untrusted iCal feed body into a bounded list of match candidates.

Purpose: every other plan in this phase asserts something against this harness and this fixture. `16-VALIDATION.md`'s Wave 0 Requirements list both as blocking, and the real CrewWebPlus export is a named person's work schedule that is deliberately not committed — so a structurally faithful redaction is the only thing the parser can ever be proved against. Shipping the parser first, alone, with its own checks, means the untrusted-input surface (T-16-INPUT) is provably correct before anything fetches, persists or matches.

Output: one committed fixture with a provenance record, one new leaf server module carrying constants plus the parser, one new test harness, one edited suite file list.

This plan carries **both** of `16-VALIDATION.md`'s Wave 0 requirements. Nothing in this phase depends on the harness before this plan creates it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-CONTEXT.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-PATTERNS.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the redacted CrewWebPlus-shaped .ics fixture and its provenance record</name>

  <files>server/fixtures/calendar_crewwebplus_redacted.ics, server/fixtures/README.md</files>

  <read_first>
    - `server/fixtures/README.md` — the whole provenance discipline: every fixture field is marked either **real** (copied verbatim from a captured record) or **synthetic** (invented, explicitly noted). This fixture is the first one in the directory that is entirely synthetic-by-design, and the README must say so in those terms.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md` § "Wave 0 Requirements" — the exact list of structural properties the fixture must preserve.
    - `.planning/ROADMAP.md` § "Phase 16" measured finding 1 — the real export's `PRODID`, the `SUMMARY` shape, the `CATEGORIES` vocabulary, and the two `STATUS:CANCELLED` 1899-placeholder junk events.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md` § "Corrections applied 2026-09-07" — CORRECTION 2, which fixes the `DTSTART`/`DTEND` form at bare UTC and requires an unrecognised form to be rejected rather than guessed.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-CONTEXT.md` § `<canonical_refs>` "Sample data" — why the real export is not committed.
  </read_first>

  <action>
Create `server/fixtures/calendar_crewwebplus_redacted.ics`, committed with LF line endings (the parser normalises CRLF to LF, and the harness proves the CRLF form parses identically by substituting the endings in memory — do not commit CRLF, git would mangle it).

Wrap everything in a single `BEGIN:VCALENDAR` / `END:VCALENDAR` block carrying `VERSION:2.0`, `CALSCALE:GREGORIAN` and the real producer's identifier `PRODID:-//Apm Technologies//CrewWebPlus//EN` (the PRODID string is the one genuinely real token in the file — it identifies the exporter, not a person, and the README must mark it as such).

Emit exactly eleven `VEVENT` blocks, in this order, using the fictional airline designator `XX`, the fictional far-end airport codes `AAA` and `BBB`, and `ORY` as the home airport. Every `UID` takes the form `fixture-00NN@skypane.invalid`. Every `DTSTAMP` is `20260901T000000Z`.

1. `SUMMARY:XX1001 ORY-AAA(+0200)`, `CATEGORIES:FLT`, `DTSTART;VALUE=DATE-TIME:20260901T060000Z`, `DTEND;VALUE=DATE-TIME:20260901T081500Z`. A departure from the home airport.
2. `SUMMARY:XX1002 AAA-ORY(+0200)`, `CATEGORIES:FLT`, `DTSTART;VALUE=DATE-TIME:20260901T090000Z`, `DTEND;VALUE=DATE-TIME:20260901T111500Z`. The return arrival.
3. `SUMMARY:XX2001 BBB-ORY(+0200)`, `CATEGORIES:FLT`, `DTSTART;VALUE=DATE-TIME:20260901T050000Z`, `DTEND;VALUE=DATE-TIME:20260901T065500Z`. This event carries the **folded** `DESCRIPTION`: one logical value over 75 octets, broken across three physical lines, where the second physical line begins with a single SPACE and the third begins with a single HTAB. The value contains at least one RFC 5545 escape sequence — a backslash followed by the letter n — so an escaped newline is present in the source text. The description text is generic operational filler (aircraft type, block minutes); it must name no person and no crew code.
4. `SUMMARY:XX2002 BBB-ORY(+0200)`, `CATEGORIES:FLT`, `DTSTART;VALUE=DATE-TIME:20260901T133000Z`, `DTEND;VALUE=DATE-TIME:20260901T152500Z`. Deliberately the **same route as event 3, roughly eight hours later** — the synthetic analogue of the measured NCE-ORY twice-daily rotation. Plan 16-06's ambiguity check consumes this pair; do not change the interval without updating that plan.
5. `SUMMARY:XX9001 ORY-AAA(+0200)`, `CATEGORIES:FLT`, `STATUS:CANCELLED`, `DTSTART;VALUE=DATE-TIME:18991230T000000Z`, `DTEND;VALUE=DATE-TIME:18991230T000000Z`. The first junk placeholder.
6. Identical in shape to 5 with `SUMMARY:XX9002 AAA-ORY(+0200)`. The second junk placeholder — the real export carries two, and the fixture carries two, because a parser that filters on `STATUS` only when it happens to be the first event is not actually filtering.
7. `CATEGORIES:OFFD`, `SUMMARY:Day off`, `DTSTART;VALUE=DATE:20260903`, `DTEND;VALUE=DATE:20260904`. An all-day, non-`DATE-TIME` value that the date parser is required to reject — its presence proves the category filter runs *before* date parsing, because a correct implementation never asks the date parser about this event at all.
8. `CATEGORIES:CAHC`, `SUMMARY:Standby`, bare-UTC `DTSTART`/`DTEND` on 20260902.
9. `CATEGORIES:CPBL`, `SUMMARY:Training`, bare-UTC `DTSTART`/`DTEND` on 20260902.
10. `CATEGORIES:FLT`, `SUMMARY:XX3001 unstructured duty text`, bare-UTC `DTSTART`/`DTEND` on 20260901. A flight-category event whose summary does not match the route shape — proves a shape-invalid summary is skipped rather than partially guessed.
11. `CATEGORIES:FLT`, `SUMMARY:XX4001 AAA-ORY(+0200)`, `DTSTART;TZID=Europe/Paris:20260901T180000`, `DTEND;TZID=Europe/Paris:20260901T201500`. The CORRECTION 2 tripwire: a well-formed flight event whose date form is not the bare-UTC one this producer emits. It must be rejected with a warning and produce no entry, and it must never be resolved through a timezone database.

Four of the eleven — events 1, 2, 3 and 4 — are the only ones that may become entries. Record that number in the fixture as a leading `X-SKYPANE-FIXTURE-EXPECTED-ENTRIES:4` property directly under `PRODID`, so a future editor who adds an event sees the ledger they have to update. (An `X-` property is valid RFC 5545 and the parser ignores unknown properties by construction.)

Then append a `## calendar_crewwebplus_redacted.ics` section to `server/fixtures/README.md`, following the existing per-fixture heading style. It must state: that this fixture is **entirely synthetic except for the `PRODID` string**, which is real and identifies the exporter rather than any person; that the real `CrewWebPlus` export it is modelled on is a named individual's work schedule and is deliberately never committed (`16-CONTEXT.md`'s Sample data note); the eleven-event inventory above with one line each saying what property that event exists to exercise; and the expected parsed-entry count of 4 with a pointer to the harness constant that pins it.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "p='server/fixtures/calendar_crewwebplus_redacted.ics'; t=open(p).read(); assert t.count('BEGIN:VEVENT')==11 and t.count('END:VEVENT')==11, t.count('BEGIN:VEVENT'); assert t.count('CATEGORIES:FLT')==7 and t.count('STATUS:CANCELLED')==2 and t.count('18991230')>=2; assert 'CATEGORIES:OFFD' in t and 'CATEGORIES:CAHC' in t and 'CATEGORIES:CPBL' in t; assert 'TZID=Europe/Paris' in t and 'VALUE=DATE:20260903' in t; assert 'X-SKYPANE-FIXTURE-EXPECTED-ENTRIES:4' in t; assert '\r' not in t, 'fixture must be committed LF-only'; ls=t.split('\n'); assert any(l.startswith(' ') for l in ls) and any(l.startswith('\t') for l in ls), 'fixture must fold with both SPACE and HTAB continuations'; print('fixture OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0.
    - `grep -c 'BEGIN:VEVENT' server/fixtures/calendar_crewwebplus_redacted.ics` returns exactly `11`.
    - `grep -c '^DESCRIPTION' server/fixtures/calendar_crewwebplus_redacted.ics` returns at least `1`, and the line immediately following one `DESCRIPTION` line begins with a single space, and a later line begins with a HTAB (asserted by the verify command above).
    - `server/.venv/bin/python3 -c "t=open('server/fixtures/calendar_crewwebplus_redacted.ics').read(); import re; assert re.search(r'DESCRIPTION[^\n]*\\\\n', t) or re.search(r'^[ \t][^\n]*\\\\n', t, re.M), 'no escaped newline present in a DESCRIPTION'"` exits 0.
    - `grep -ci 'transavia\|orly-ville\|crew\b' server/fixtures/calendar_crewwebplus_redacted.ics` returns `0` — no real carrier and no real crew vocabulary leaked into the redaction.
    - `git diff --stat -- server/fixtures/README.md` shows the README gained a section, and `grep -c 'calendar_crewwebplus_redacted.ics' server/fixtures/README.md` is at least `1`.
    - `git status --porcelain` shows no `.ics` file anywhere outside `server/fixtures/` — the real export was never staged.
  </acceptance_criteria>

  <done>A committed, entirely-synthetic eleven-event `.ics` fixture reproduces every structural property the parser will be asserted against, with a provenance section in the fixtures README stating what is real (the PRODID alone) and what is invented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create server/plane/calendar_rules.py — module contract, constants and the RFC 5545 subset parser</name>

  <files>server/plane/calendar_rules.py</files>

  <read_first>
    - `server/plane/colour_rules.py` **lines 1-120** — the module docstring's leaf-import contract (verbatim the clause this new module must carry, adapted to name itself), the redeploy/`state_dir` note, the compiled positive-allowlist regex block, and the result-constant vocabulary with its "these are NOT flash keys" comment.
    - `server/plane/manual_resolutions.py` — the never-raising load and the tmp-write idiom this module copies in plan 16-03 (read now so the file layout of this module anticipates it).
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-PATTERNS.md` § "`server/plane/calendar_rules.py` (NEW — service, leaf)" — the leaf-import rule, the allowlist-regex assignment and the result-code shape.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md` § "Common Pitfalls" Pitfalls 1, 2 and 3, and § "Code Examples" → the `_unfold` skeleton.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md` § "Corrections applied 2026-09-07" — CORRECTION 2 in full.
    - `server/fixtures/calendar_crewwebplus_redacted.ics` as created by Task 1.
  </read_first>

  <behavior>
    - `unfold_ics_lines()` on text where a line begins with a single SPACE returns that line joined onto the previous one, with the marker character removed and no other character lost.
    - The same holds for a HTAB continuation, and for a continuation of a continuation (three physical lines becoming one logical line).
    - `unfold_ics_lines()` on CRLF-terminated text returns exactly the same list it returns for the LF-terminated equivalent.
    - A continuation line at the very start of the text (no previous line to join onto) is kept as its own line rather than raising an IndexError.
    - `split_property()` on `DTSTART;VALUE=DATE-TIME:20260901T060000Z` yields the name `DTSTART` and the value `20260901T060000Z`, with the parameter block separated and the value's own colons (if any) left intact.
    - `parse_ics_datetime()` returns a float epoch for `20260901T060000Z` and `None` for a `VALUE=DATE` day value, a `TZID`-qualified local value, an empty string, a non-string, and a syntactically plausible but out-of-range value.
    - `parse_ics_events()` on the committed fixture returns exactly 4 entries, sorted ascending by `start_at`, each carrying exactly the five keys `airline_iata`, `origin_iata`, `destination_iata`, `start_at`, `end_at`.
    - `parse_ics_events()` on an empty string, on a truncated block that never closes, on a body of random bytes decoded as text, and on `None` returns an empty list and raises nothing.
    - `parse_ics_events()` on a body containing more `VEVENT` blocks than `CALENDAR_MAX_ENTRIES` returns at most `CALENDAR_MAX_ENTRIES` entries.
  </behavior>

  <action>
Create `server/plane/calendar_rules.py`. Imports: `json`, `os`, `re`, `sys`, `threading`, `datetime`/`timezone` from `datetime`, and `server.device_config`. Do not import `requests` yet — plan 16-04 adds it. Do not import any other `server.plane` module, ever.

**Module docstring.** Open with what the module owns across this phase (the calendar-sourced theme input: fetch, parse, the rolling-window registry, and the pure match function) and then carry an adapted copy of `colour_rules.py`'s leaf-import clause naming this module: it imports `server.device_config` plus stdlib (and, from plan 16-04, `requests`) and must never import `server.plane.colour_rules`, `server.plane.enrich`, `server.plane.detect`, `server.plane.illustrations`, `server.plane.manual_resolutions` or `server.plane.render`. State why the `colour_rules` direction specifically is forbidden: D-02's precedence is wired the other way round, as a keyword argument `poll_loop.py` computes here and passes into the resolver there, precisely so this module stays a leaf. Add the same redeploy note `colour_rules.py` carries — the registry lives at `{state_dir}/calendar_rules.json`, outside the rsynced tree. Add a short privacy clause: this module's persisted output describes a named person's near-term work schedule, so a record carries the minimum the matcher needs and nothing else, and D-03's rolling window is what bounds retention.

**Constants.** Define, each with a one-line comment explaining the number rather than restating it:
`CALENDAR_RULES_FILENAME = "calendar_rules.json"`;
`CALENDAR_URL_ENV_VAR = "SKYPANE_CALENDAR_ICS_URL"` (the phase's first runtime secret outside `companion/auth.py`; note that its *value* never leaves this module and never reaches a log line, a page or `state_dir`);
`CALENDAR_MAX_ENTRIES = 200` (mirrors `colour_rules.COLOUR_RULE_MAX_ENTRIES`; a real 48h roster window is under ten events, so this bounds a hostile feed rather than a plausible one);
`CALENDAR_FETCH_INTERVAL_S = 1800`, `CALENDAR_FETCH_TIMEOUT_S = 10.0`, `CALENDAR_MAX_RESPONSE_BYTES = 2 * 1024 * 1024`, `CALENDAR_MAX_REDIRECTS = 5`, `CALENDAR_WINDOW_FORWARD_S = 172800`, `CALENDAR_MATCH_TOLERANCE_S = 5400` — declare all seven here even though plans 16-03, 16-04 and 16-06 are the ones that consume them, so the module has one tunables block rather than four scattered ones.
`CATEGORY_FLIGHT = "FLT"` and `STATUS_CANCELLED = "CANCELLED"`.

**Compiled positive allowlists**, in one block styled exactly like `colour_rules.py`'s:
`_SUMMARY_ROUTE_RE` matching a whole stripped summary as `{flight token}{whitespace}{3-letter origin}-{3-letter destination}` with an optional trailing parenthesised signed four-digit offset, using named groups `flight`, `origin`, `destination`, `offset`. The flight token's own charset is `[A-Z0-9]{2,8}` — the same bound `colour_rules._CALLSIGN_RULE_RE` uses and for the same documented real-world reason.
`_AIRLINE_IATA_RE` matching exactly two characters from `[A-Z0-9]` with a lookahead requiring at least one letter, so an all-digit pair is not mistaken for an airline designator.
`_AIRPORT_IATA_RE` matching exactly three characters from `[A-Z]`.
`_ICAL_UTC_RE` matching exactly eight digits, the letter T, six digits and a trailing Z.
Comment the offset group explicitly: it is the far end's local UTC offset, informational only, captured so the regex anchors the whole summary but never used for matching — the authoritative times are `DTSTART`/`DTEND`, already UTC (CORRECTION 2).

**`unfold_ics_lines(raw_text)`.** Implement RFC 5545 §3.1 unfolding from the skeleton in `16-RESEARCH.md` § Code Examples: normalise CRLF to LF, then walk the physical lines and, whenever a line's first character is a SPACE or a HTAB **and at least one logical line already exists**, strip that one marker character and append the remainder to the previous logical line; otherwise start a new logical line. Return the list of logical lines. Non-string input returns an empty list. Comment that this must run before any property is split, naming the failure it prevents: a folded `SUMMARY` or `DESCRIPTION` otherwise arrives as a bogus new property and the route regex silently misses on exactly the long lines the real export produces.

**`split_property(line)`.** Partition on the FIRST colon into a name-and-parameters segment and a value; take the bare name as everything before the first semicolon of that segment, uppercased and stripped; return the name, the raw parameter segment and the value. A line with no colon returns `None` for the name. Comment that a colon inside a quoted parameter value is a real RFC 5545 possibility this exporter is not known to use and is explicitly out of the accepted subset — such a line is skipped, never guessed at.

**`parse_ics_datetime(value)`.** Accept only a string matching `_ICAL_UTC_RE`; parse it with `datetime.strptime` using the `%Y%m%dT%H%M%SZ` directive, attach `timezone.utc`, and return `.timestamp()` as a float. Everything else returns `None`. Wrap the `strptime` call so an out-of-range value returns `None` rather than raising. Do not add a second format, do not add a `TZID` branch, and do not import `zoneinfo` — CORRECTION 2 measured 63/63 events in the real export as bare UTC with zero `TZID`, and the required behaviour for anything else is a loud rejection so a future producer change fails visibly instead of silently mis-timing a match.

**`parse_ics_events(raw_text)`.** Never raises; returns a list of entry dicts. Unfold first. Then walk the logical lines accumulating one property dict per `BEGIN:VEVENT` .. `END:VEVENT` block, recording only the last value seen for `SUMMARY`, `CATEGORIES`, `STATUS`, `DTSTART` and `DTEND`, and ignoring every other property including unknown `X-` ones. A block that never closes is discarded. For each closed block, in this order: drop it unless its `CATEGORIES` value, uppercased and split on commas, contains `CATEGORY_FLIGHT`; drop it if its `STATUS` value uppercases to `STATUS_CANCELLED`; only then match its `SUMMARY` against `_SUMMARY_ROUTE_RE` and only then call `parse_ics_datetime()` on `DTSTART` and `DTEND`. Validate the two airport codes against `_AIRPORT_IATA_RE` and the first two characters of the matched flight token against `_AIRLINE_IATA_RE`. Drop the event if either timestamp is `None` or if `end_at` is earlier than `start_at`. Build each surviving entry from scratch as a five-key dict — `airline_iata`, `origin_iata`, `destination_iata`, `start_at`, `end_at` — never by reusing the accumulated property dict, and never carrying the flight number, the UID, the summary or the description forward. Stop accumulating at `CALENDAR_MAX_ENTRIES` surviving entries. Return the list sorted ascending by `start_at`.

Count rejections in two separate buckets — events dropped for a date form the parser does not accept, and events dropped for any other reason — and, when either is non-zero, print one line to `sys.stderr` naming the module, the two counts and the reason class. Print the *counts and the reason*, never a rejected value: a `DTSTART` is a named person's flight time and `journalctl` is readable by anyone with VPS access. The date-form bucket having its own count is what makes CORRECTION 2's rejection loud.

The ordering of those three gates is load-bearing and must be commented as such: the category filter runs before the date parser specifically so the fixture's all-day `OFFD` event never reaches `parse_ics_datetime()` and never inflates the rejection count on a perfectly healthy feed.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys; sys.path.insert(0,'.')
import server.plane.calendar_rules as cr
raw = open('server/fixtures/calendar_crewwebplus_redacted.ics').read()
e = cr.parse_ics_events(raw)
assert len(e)==4, len(e)
assert all(set(x)=={'airline_iata','origin_iata','destination_iata','start_at','end_at'} for x in e), e[0]
assert [x['start_at'] for x in e]==sorted(x['start_at'] for x in e)
assert cr.parse_ics_events(raw.replace('\n','\r\n'))==e
assert cr.parse_ics_events('')==[] and cr.parse_ics_events(None)==[] and cr.parse_ics_events('BEGIN:VEVENT')==[]
assert cr.parse_ics_datetime('20260901T060000Z') and cr.parse_ics_datetime('20260903') is None and cr.parse_ics_datetime('20260901T180000') is None
print('parser OK', len(e))
"</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 and prints `parser OK 4`.
    - The leaf-import contract holds structurally, not just in prose: `server/.venv/bin/python3 -c "import ast; t=ast.parse(open('server/plane/calendar_rules.py').read()); mods=[n.name for x in ast.walk(t) if isinstance(x,ast.Import) for n in x.names]+[x.module or '' for x in ast.walk(t) if isinstance(x,ast.ImportFrom)]; bad=[m for m in mods if any(k in m for k in ('colour_rules','plane.enrich','plane.detect','illustrations','manual_resolutions','plane.render'))]; assert not bad, bad; print('leaf OK')"` exits 0.
    - No timezone database is reachable: `server/.venv/bin/python3 -c "import ast; t=ast.parse(open('server/plane/calendar_rules.py').read()); mods=[n.name for x in ast.walk(t) if isinstance(x,ast.Import) for n in x.names]+[x.module or '' for x in ast.walk(t) if isinstance(x,ast.ImportFrom)]; assert not any('zoneinfo' in m or 'pytz' in m for m in mods), mods"` exits 0.
    - An entry never carries schedule text: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); import server.plane.calendar_rules as cr; e=cr.parse_ics_events(open('server/fixtures/calendar_crewwebplus_redacted.ics').read()); import json; blob=json.dumps(e); assert 'XX1001' not in blob and 'fixture-' not in blob and 'DESCRIPTION' not in blob, blob"` exits 0.
    - The `TZID` tripwire is rejected loudly: running the parser on the fixture with stderr captured emits a line naming a non-zero date-form rejection count, and the resulting entry list still has length 4.
    - The entry cap holds: parsing a body built by repeating fixture event 1 with distinct UIDs 400 times returns exactly `CALENDAR_MAX_ENTRIES` entries.
    - `git diff --stat -- server/requirements.txt server/requirements-dev.txt` is empty.
    - `git diff --stat -- server/plane/render.py` is empty.
  </acceptance_criteria>

  <done>`server/plane/calendar_rules.py` exists as a leaf module carrying the phase's tunables block and a hand-rolled RFC 5545 subset parser that unfolds before splitting, filters on category before parsing dates, accepts only the bare-UTC date form, bounds its output, and emits five-key entries carrying no schedule text.</done>
</task>

<task type="auto">
  <name>Task 3: Create server/test_calendar_rules.py with the parser checks, and register it in the suite</name>

  <files>server/test_calendar_rules.py, scripts/run-all-tests.sh</files>

  <read_first>
    - `server/test_colour_rules.py` **lines 1-80** — the exact preamble to copy: shebang, docstring naming the module under test and the validation item it discharges, the `HERE`/`REPO_ROOT` sys.path bootstrap, the `EXPECTED_CHECK_COUNT` ledger comment stating it is re-derived by RUNNING the harness, the local `check(name, fn)` accumulator with its never-swallow-an-exception clause, and the import-failure early return.
    - `server/test_colour_rules.py` **last 25 lines** — the terminating `total`/`passed`/`EXPECTED_CHECK_COUNT` comparison and the `sys.exit(main())` guard.
    - `server/test_enrich.py` **lines 20-40** — `FIXTURES_DIR` and `load_fixture()`, the established way a harness reaches `server/fixtures/`.
    - `scripts/run-all-tests.sh` **lines 34-60** — the `HARNESSES` array and the header comment's running harness count and per-phase provenance notes.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md` § "Per-Task Verification Map" rows 1-4 (Plan column `parser`) and § "Wave 0 Requirements".
  </read_first>

  <action>
Create `server/test_calendar_rules.py`, stdlib-only, no pytest, no `conftest.py`, mirroring `server/test_colour_rules.py`'s structure exactly. Its docstring names `server/plane/calendar_rules.py` as the module under test and states that it discharges `16-VALIDATION.md`'s Wave 0 harness requirement, that later plans in phase 16 extend it, and that every fixture directory is a `tempfile.TemporaryDirectory()`. Add a `FIXTURES_DIR`/`load_fixture()` pair in `test_enrich.py`'s shape, plus a module-level `FIXTURE_ICS = "calendar_crewwebplus_redacted.ics"` and `FIXTURE_EXPECTED_ENTRIES = 4` — the second is the ledger the fixture's own `X-SKYPANE-FIXTURE-EXPECTED-ENTRIES` property mirrors, and one check asserts the two agree so the fixture and the harness cannot drift apart silently.

Write these checks, one `check(...)` call each, each named so a failure reads as a sentence about the behaviour rather than about the function:

**Unfolding (16-VALIDATION row 1).** A SPACE continuation is rejoined with the marker removed and nothing else lost. A HTAB continuation is rejoined the same way. Three physical lines fold into one logical line. A synthetic `SUMMARY` longer than 75 octets, folded, round-trips to its exact original value. Text whose very first line is a continuation does not raise. The CRLF form of the committed fixture unfolds to the identical list as the LF form.

**Property splitting (16-RESEARCH Pitfall 2).** `DTSTART;VALUE=DATE-TIME:20260901T060000Z` yields the bare name `DTSTART` and the value with no parameter residue. A line whose value itself contains a colon keeps the whole value. A line with no colon at all yields no name and is skipped rather than raising.

**Junk filtering (16-VALIDATION row 2).** Parsing the committed fixture yields exactly `FIXTURE_EXPECTED_ENTRIES` entries. No entry's timestamps fall in the 19th century, proving the two `STATUS:CANCELLED` placeholder events were dropped. No entry derives from the `OFFD`, `CAHC` or `CPBL` blocks — assert this positively by checking that every returned entry's route matches one of the four flight events' routes and that the total count is exactly four, so an extra non-flight entry would fail on the count.

**Date form (16-VALIDATION row 3, CORRECTION 2).** `parse_ics_datetime()` returns a float for the bare-UTC form and `None` for each of: an all-day `VALUE=DATE` value, a `TZID`-qualified local value, a value with a lowercase trailing z, an empty string, a non-string, and a syntactically shaped but calendrically impossible value. Separately, parse the committed fixture with `sys.stderr` redirected to an in-memory buffer and assert the buffer contains a non-zero date-form rejection count — the `TZID` tripwire event is rejected *loudly*, not silently — while the entry count is still four. Assert also that the captured stderr contains no eight-digit date string, so the rejection is loud about the count without publishing a person's flight time to the journal.

**Never raises (16-VALIDATION row 4).** `parse_ics_events()` returns an empty list for: the empty string, `None`, a non-string, a `VCALENDAR` that opens a `VEVENT` and never closes it, a body of arbitrary punctuation, a body whose every line lacks a colon, and a body decoded from random bytes with replacement characters.

**Bounded output (T-16-DOS, partial — plan 16-03 completes it at the registry tier).** A synthetic body repeating one valid flight event more times than `CALENDAR_MAX_ENTRIES` returns exactly `CALENDAR_MAX_ENTRIES` entries.

**Record shape (privacy).** Every returned entry has exactly the five expected keys; serialising the whole list to JSON contains none of the fixture's flight-number tokens, none of its UID tokens, and none of its description text.

Then set `EXPECTED_CHECK_COUNT` by **running the harness and reading the printed total**, not by counting the `check(` calls by hand, and follow `test_colour_rules.py`'s ledger-comment style: the initial value, that phase 16 plan 01 introduced it, and that later plans in this phase raise it.

Finally register the harness in `scripts/run-all-tests.sh`: add `server/test_calendar_rules.py` to the `HARNESSES` array immediately after `server/test_colour_rules.py`, and update the header comment in two places — the "Runs all 18 harnesses" sentence becomes 19, and the "Canonical 18-file enumeration" provenance comment gains a clause in the file's own established style noting that phase 16 plan 01 added the calendar-rules harness. Do not reorder or remove any existing entry.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_calendar_rules.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_calendar_rules.py` exits 0 and prints a `calendar_rules: N/N checks pass` line whose two numbers are equal and equal to `EXPECTED_CHECK_COUNT`.
    - The ledger is genuinely enforced: `grep -c 'total == EXPECTED_CHECK_COUNT' server/test_calendar_rules.py` returns `1`.
    - The harness is hermetic: `server/.venv/bin/python3 -c "import ast; t=ast.parse(open('server/test_calendar_rules.py').read()); mods=[n.name for x in ast.walk(t) if isinstance(x,ast.Import) for n in x.names]+[x.module or '' for x in ast.walk(t) if isinstance(x,ast.ImportFrom)]; assert not any(m.startswith('pytest') or m.startswith('unittest') or m=='requests' for m in mods), mods"` exits 0.
    - `grep -c 'server/test_calendar_rules.py' scripts/run-all-tests.sh` returns at least `1`, and `grep -c 'server/test_colour_rules.py' scripts/run-all-tests.sh` still returns at least `1`.
    - `scripts/run-all-tests.sh` exits 0 and its output names 19 harnesses, not 18.
    - Deleting the fixture proves the checks are live rather than vacuous: temporarily rename `server/fixtures/calendar_crewwebplus_redacted.ics`, confirm the harness exits non-zero, then restore it.
    - `git diff --stat -- server/plane/render.py companion/` is empty for this plan's commits.
  </acceptance_criteria>

  <done>A registered, hermetic, ledger-enforcing harness proves the parser unfolds before splitting, filters junk and non-flight categories, accepts only bare UTC while rejecting anything else loudly and without leaking a date value, never raises on hostile input, bounds its output, and emits records carrying no schedule text.</done>
</task>

</tasks>

## Artifacts this phase produces

Introduced by **this plan** (16-01):

| Kind | Symbol / path |
|---|---|
| new file | `server/fixtures/calendar_crewwebplus_redacted.ics` |
| new file | `server/plane/calendar_rules.py` |
| new file | `server/test_calendar_rules.py` |
| constant | `calendar_rules.CALENDAR_RULES_FILENAME` |
| constant | `calendar_rules.CALENDAR_URL_ENV_VAR` (value `SKYPANE_CALENDAR_ICS_URL`) |
| constant | `calendar_rules.CALENDAR_MAX_ENTRIES` |
| constant | `calendar_rules.CALENDAR_FETCH_INTERVAL_S` |
| constant | `calendar_rules.CALENDAR_FETCH_TIMEOUT_S` |
| constant | `calendar_rules.CALENDAR_MAX_RESPONSE_BYTES` |
| constant | `calendar_rules.CALENDAR_MAX_REDIRECTS` |
| constant | `calendar_rules.CALENDAR_WINDOW_FORWARD_S` |
| constant | `calendar_rules.CALENDAR_MATCH_TOLERANCE_S` |
| constant | `calendar_rules.CATEGORY_FLIGHT`, `calendar_rules.STATUS_CANCELLED` |
| private regex | `calendar_rules._SUMMARY_ROUTE_RE`, `_AIRLINE_IATA_RE`, `_AIRPORT_IATA_RE`, `_ICAL_UTC_RE` |
| function | `calendar_rules.unfold_ics_lines(raw_text)` |
| function | `calendar_rules.split_property(line)` |
| function | `calendar_rules.parse_ics_datetime(value)` |
| function | `calendar_rules.parse_ics_events(raw_text)` |
| test constant | `test_calendar_rules.FIXTURE_ICS`, `FIXTURE_EXPECTED_ENTRIES`, `EXPECTED_CHECK_COUNT` |
| fixture property | `X-SKYPANE-FIXTURE-EXPECTED-ENTRIES` (inside the `.ics`) |
| suite entry | `server/test_calendar_rules.py` in `scripts/run-all-tests.sh`'s `HARNESSES` |

No new CSS class, route path, env var read, flash key or dependency is introduced by this plan. `SKYPANE_CALENDAR_ICS_URL` is *named* here as a constant but is not read by any code until plan 16-03.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied iCal feed body → `parse_ics_events()` | Fully untrusted third-party text becomes the candidate list that later decides what the physical panel renders. |
| a named person's work schedule → this process's stderr / `journalctl` | Rejection diagnostics run on every fetch and are readable by anyone with VPS access. |
| repository → the committed fixture | A redaction that keeps real content would publish a private schedule to a public GitHub repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-INPUT | Tampering / Elevation of Privilege | `parse_ics_events()` and its four compiled allowlists | high | mitigate | Every field extracted from the feed passes a compiled positive allowlist before it becomes an entry: `_SUMMARY_ROUTE_RE` anchors the whole summary, `_AIRPORT_IATA_RE` gates both airport codes, `_AIRLINE_IATA_RE` gates the airline designator, `_ICAL_UTC_RE` gates both timestamps. Entries are rebuilt from scratch, never by reusing the accumulated property dict, so no unvalidated key can survive. Plan 16-03 re-applies the identical gates on every read of the persisted file (`colour_rules.load_colour_rules()`'s re-validate-on-read discipline, T-15-01's direct precedent). Proved by Task 3's junk-filtering, date-form and record-shape checks. |
| T-16-DOS | Denial of Service | unbounded parse of a hostile feed | medium | mitigate | Accumulation stops at `CALENDAR_MAX_ENTRIES`; an unterminated `VEVENT` block is discarded rather than buffered to EOF as a live block; every parse failure degrades to a dropped event, never an exception out of the poll cycle. Proved by Task 3's bounded-output and never-raises checks. Plan 16-04 adds the response-size and timeout halves; plan 16-03 adds the persisted-registry half. |
| T-16-SECRET | Information Disclosure | parser rejection diagnostics on stderr | high | mitigate | The rejection log line carries two counts and a fixed reason class only — never a rejected value, never a `DTSTART`, never a summary. `journalctl -u skypane-poll` is readable by anyone with VPS access and `poll_loop.py`'s own docstring already binds this project to "never logs a bearer token or BYOS setup secret". Proved by Task 3's assertion that the captured stderr contains no eight-digit date string. |
| T-16-PRIV | Information Disclosure | the committed fixture; the entry record shape | medium | mitigate | The fixture is entirely synthetic bar the `PRODID` exporter string, asserted by a grep for real-carrier and crew vocabulary and by the fixtures README's real-versus-synthetic marking discipline. The entry record carries five keys and deliberately drops the flight number, UID, summary and description — the minimum the matcher needs, which is also the minimum retained on disk under D-03. Proved by Task 1's redaction grep and Task 2's record-shape criterion. |
| T-16-SC | Tampering | package installs | n/a | accept | This plan installs zero packages. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable (the phase adds no `server/requirements.txt` entry), and `./.claude/CLAUDE.md`'s stdlib-only server posture forbids adding one. Pinned by the empty-requirements-diff criterion on Task 2. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_calendar_rules.py` exits 0 with `N/N checks pass`.
- `scripts/run-all-tests.sh` exits 0 and runs 19 harnesses.
- **D-07 standing gate (checked in every plan of this phase):** `git diff --stat -- server/plane/render.py` is empty and `server/.venv/bin/python3 server/test_render.py` exits 0 with its `EXPECTED_CHECK_COUNT` unedited.
- **No new dependency (checked in every plan of this phase):** `git diff -- server/requirements.txt server/requirements-dev.txt` is empty.
- `git diff --stat -- companion/ server/poll_loop.py server/plane/colour_rules.py server/device_config.py` is empty for this plan's commits — this plan touches nothing any other plan owns.
- The real supplied `CrewWebPlus` export is not in the working tree or the index: `git ls-files '*.ics'` lists exactly one path, `server/fixtures/calendar_crewwebplus_redacted.ics`.
</verification>

<success_criteria>
- The fixture reproduces all six structural properties `16-VALIDATION.md` names, and carries no real schedule, crew code, carrier name or UID.
- `parse_ics_events()` on the fixture returns exactly four entries and nothing else, under both LF and CRLF line endings.
- An unrecognised date form is rejected with a visible non-zero count and produces no entry, and no timezone database is reachable from the module.
- The parser never raises on any of seven hostile bodies and never exceeds `CALENDAR_MAX_ENTRIES`.
- `calendar_rules.py`'s import list is provably leaf, checked by AST rather than by grep.
- The new harness runs inside `scripts/run-all-tests.sh` and enforces its own check-count ledger.
</success_criteria>

<output>
Create `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-01-SUMMARY.md` when done.
</output>
