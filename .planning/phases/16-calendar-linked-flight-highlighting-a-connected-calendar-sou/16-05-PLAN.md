---
phase: 16-calendar-linked-flight-highlighting-a-connected-calendar-sou
plan: 05
type: execute
wave: 3
depends_on:
  - 16-02
  - 16-03
files_modified:
  - companion/pages/config_page.py
  - companion/app.py
  - companion/test_config_page.py
  - deploy/skypane.env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "The Settings page gains one Calendar group as the last section inside the tracked settings form, carrying a heading, one caption, a status line and a theme select — no new button, no new flash key, no new CSS token, no new icon."
    - "The status line renders one of exactly three locked strings: not configured, connected but never yet synced, or connected with the last-synced relative timestamp — and never renders the calendar URL, any part of it, or anything about the calendar's contents."
    - "The companion process learns only whether the URL is set, never its value: the presence check goes through calendar_rules.calendar_is_configured(), read fresh on every request, and configured_calendar_url() is never called anywhere under companion/."
    - "The theme select is a native <select name=\"calendar_theme_id\"> carrying every registered theme id in THEME_IDS order with device_config.theme_label() text, pre-selected on the saved value or, when none is saved, on the theme the base Theme grid currently has selected."
    - "A submitted calendar_theme_id that is not a member of device_config.THEME_IDS is refused by handle_post() before anything is written, reusing the existing generic settings-save-failed flash — no new flash constant exists (T-16-TAMPER, the HTTP-layer half of the write gate plan 16-02 owns at the persistence layer)."
    - "The section works with JavaScript disabled: the status line is server-rendered plain text, the select is a native control, and saving goes through the existing static Save Settings fallback button."
    - "The last-synced timestamp is rendered through layout.concise_timestamp_html(), so it reads as an absolute time plus a relative age exactly like every other timestamp on this page — never a bare ISO string, and never a fabricated or estimated time when no successful sync has happened."
    - "deploy/skypane.env.example documents SKYPANE_CALENDAR_ICS_URL with the same never-committed discipline the file already applies to the other two secrets, and ships a placeholder rather than any real value."
  artifacts:
    - companion/pages/config_page.py
    - companion/app.py
    - deploy/skypane.env.example
  key_links:
    - "companion/ must call calendar_rules.calendar_is_configured() and never configured_calendar_url(). The second returns the secret, and any companion call site for it is a leak path into a rendered page (T-16-SECRET)."
    - "The Calendar group belongs INSIDE <form id=\"settings-form\"> (unlike Phase 15's Rules section, which had to sit outside because it nests its own forms). Placing it outside would silently stop the select from submitting with Save Settings."
    - "handle_post()'s membership test against device_config.THEME_IDS is the HTTP-layer half of a two-gate pair; save_device_config()'s ValueError (plan 16-02) is the other. Removing either leaves the write path single-gated."
    - "The three status strings are the only place the copy constraint from measured finding 3 is enforced on this surface. Paraphrasing them reopens the overstatement problem 16-UI-SPEC.md's 'what this section does NOT say' guard exists to close."
  prohibitions:
    - "The calendar URL, or any substring of it, must never be rendered into any page, any attribute, any flash message or any HTML comment."
    - "companion/ must not import or call configured_calendar_url()."
    - "No count of upcoming flights, no next-match preview, no flight number, time or route may appear anywhere on the Settings page (16-UI-SPEC.md § Configured / Not Configured State)."
    - "No test-connection button, no calendar-scoped cooldown, no second poll-trigger control."
    - "No staleness warning banner, no status colour, no status dot, no badge, no new icon — the three states are distinguished by text alone."
    - "No new flash-key constant, no new CSS custom property, no new accent-colour use, no new typographic role, no new spacing token."
    - "No copy may use track, watch, follow, monitor, notify, currently flying, in the air now, or on duty; and no copy may name a person, a role, an employer or a crew function."
    - "server/ must not be modified by this plan."
    - "No new entry may be added to server/requirements.txt or server/requirements-dev.txt."
---

<objective>
Ship the phase's entire user-visible surface: one Calendar group at the end of the Settings page, carrying a three-state configured/not-configured status line and a theme picker for calendar-matched flights, plus the env-file documentation for `SKYPANE_CALENDAR_ICS_URL`.

Purpose: this is a deliberately small surface and the smallness is the design, not an omission — `16-UI-SPEC.md` resolves the phase's one genuinely open UI question by rejecting an upcoming-flights preview, a flight count, a test-connection button and a staleness banner, each with its own recorded reasoning. The copy is the load-bearing part. Measured finding 3 is that the frame showed **none** of the calendar owner's three flights among 201 detections on her one recorded duty day, so a caption that implies the frame is watching for a flight makes a working feature look broken. Every string here is locked in `16-UI-SPEC.md`'s Copywriting Contract and is copied verbatim, not paraphrased.

Output: one modified page module, one modified controller, one extended harness, one documented env template.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-CONTEXT.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-UI-SPEC.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-PATTERNS.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md
@./.claude/skills/sketch-findings-skypane/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Calendar group to companion/pages/config_page.py — copy constants, renderer, form slot, POST gate</name>

  <files>companion/pages/config_page.py</files>

  <read_first>
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-UI-SPEC.md` **in full** — but especially § "Copywriting Contract" (the six locked strings, verbatim), § "What this section deliberately does NOT say", § "Configured / Not Configured State" (why there is no preview, no count, no test button and no staleness banner), § "Section Anatomy" (the exact markup skeleton, the `.page-section` versus `.theme-status` reasoning, and the placement recommendation), § "Theme Control", § "Control Density" and § "Accessibility Floors".
    - `./.claude/skills/sketch-findings-skypane/SKILL.md` — the companion's current design system; binding for anything under `companion/`. This plan introduces no new token, so read it to confirm the reuse claims rather than to pick anything new.
    - `companion/pages/config_page.py` **lines 118-200** — `THEME_SECTION_CAPTION`, `RUNWAY_SECTION_CAPTION`, `LED_SECTION_CAPTION` and `DIRTY_SECTION_ATTR`: the module-constant copy idiom and the dirty-section attribute name.
    - `companion/pages/config_page.py` **lines 270-305** — `RULES_SECTION_HEADING`, `RULES_SECTION_CAPTION`, `RULE_THEME_FIELD_LABEL`, `RULE_VALUE_HINT`: the locked-English block this plan's constants join, including the "verbatim, do not paraphrase" comment.
    - `companion/pages/config_page.py` **lines 797-840** — `display_group()`: the closest structural analog for a small group (a wrapper, a heading, one caption, one control) and its universal `escape_html()` discipline.
    - `companion/pages/config_page.py` **lines 1087-1130** — `_rule_add_form_html()`'s theme field: the `THEME_IDS` option-building loop and the `.rule-add-form__field` label-plus-select-plus-hint wrapper this plan reuses verbatim.
    - `companion/pages/config_page.py` **lines 1285-1415** — `render()`'s ctx reads and its return tuple, where the new group's slot goes.
    - `companion/pages/config_page.py` **lines 1533-1600** — `handle_post()`'s membership tests and the `FLASH_SAVE_FAILED` early returns, and its final `save_device_config()` call.
    - `companion/layout.py` **lines 536-580** — `concise_timestamp_html()`: its raw-markup-producing contract (callers interpolate the return value verbatim and must never re-escape it) and its `fallback` parameter.
    - `companion/static/style.css` around `.rule-add-form__field`, `.page-section`, `.text-label` and `.section-caption` — confirm each exists and needs no change.
  </read_first>

  <behavior>
    - With no calendar configured, `render()` emits the not-configured status string and no other status string.
    - With a calendar configured and no successful sync recorded, `render()` emits the pending status string.
    - With a calendar configured and a `last_synced_at` present, `render()` emits the synced status string with a timestamp fragment produced by `concise_timestamp_html()` — an absolute time plus a relative age, never a bare ISO string.
    - With a calendar configured and a `last_synced_at` that is present but unparseable, the pending string is used rather than a fabricated time.
    - The emitted markup contains exactly one `<select name="calendar_theme_id">`, carrying one option per member of `THEME_IDS` in that order, with `device_config.theme_label()` text.
    - With a saved `calendar_theme_id`, that option carries the selected attribute; with none saved, the option matching the currently-selected base theme does.
    - The Calendar group's markup appears after the Display group's and before the settings form's closing tag.
    - `handle_post()` with a `calendar_theme_id` that is a member of `THEME_IDS` persists it and carries every other field forward.
    - `handle_post()` with a `calendar_theme_id` that is not a member returns the generic save-failed flash key and writes nothing.
    - `handle_post()` with no `calendar_theme_id` field at all leaves an already-saved value untouched.
  </behavior>

  <action>
Four edits to `companion/pages/config_page.py`. Import `server.plane.calendar_rules` alongside the existing `colour_rules` import; `companion/app.py` already imports `server.poll_loop`, which imports `requests` transitively, so this adds no new dependency to the companion process.

**1. Locked copy constants**, added to the locked-English block beside the Rules constants, each a plain module-level string assigned once and escaped at render time, carrying the same "verbatim, do not paraphrase" comment the neighbouring block already has. Copy the values **character for character** from `16-UI-SPEC.md` § Copywriting Contract:

- `CALENDAR_SECTION_HEADING` — the single word the spec gives, which is deliberately not any wording that names a person, a role, an employer or a crew function.
- `CALENDAR_SECTION_CAPTION` — the spec's three-sentence caption. Preserve all three sentences including the middle one that states the feature can only colour a flight that happens to be on screen and does not track or announce anything on its own, and the closing next-scheduled-poll latency clause every sibling caption ends on.
- `CALENDAR_STATUS_NOT_CONFIGURED` — the spec's not-configured sentence. It names the environment variable and the env file, and it is the one place either string appears in the UI. Build it by interpolating `calendar_rules.CALENDAR_URL_ENV_VAR` rather than retyping the literal, so the copy and the constant can never drift.
- `CALENDAR_STATUS_CONFIGURED_PENDING` — the spec's never-yet-synced sentence.
- `CALENDAR_STATUS_CONFIGURED_SYNCED_PREFIX` — the spec's synced sentence with its relative-timestamp slot left open, so the renderer can interpolate `concise_timestamp_html()`'s raw markup into it without the escaping collision that a single format string would create.
- `CALENDAR_THEME_FIELD_LABEL` — the spec's one-word field label.
- `CALENDAR_THEME_HINT` — the spec's hint sentence, placed under the select. Add a comment recording the spec's own reasoning for the deliberate repetition: this is the single sentence most likely to be skimmed past if it appears only once at the top of the section.

Add a block comment above the group naming the constraint these strings exist to satisfy: `.planning/ROADMAP.md`'s Phase 16 measured finding 3 — on the one measured duty day, none of the calendar owner's three flights were among the frame's 201 detections — so no string here may imply the frame knows a flight is happening independently of what is on screen.

**2. `calendar_group(configured, last_synced_at, now, current_calendar_theme_id, current_theme_id)`.** Build the markup exactly as `16-UI-SPEC.md` § Section Anatomy specifies: an outer `div` with class `page-section` carrying `DIRTY_SECTION_ATTR` set to the section's own name; an `h2` with class `text-heading` carrying the heading; a `p` with classes `text-label section-caption` carrying the caption; a `p` with class `calendar-status` carrying the resolved status; then a `div` with class `rule-add-form__field` containing a `label` bound to the select's id, the select itself, and a `p` with classes `text-label section-caption` carrying the hint.

Resolve the status with an explicit three-branch decision: not configured wins first; then configured-with-a-usable-`last_synced_at`; then configured-otherwise, which is the pending string. For the synced branch, call `layout.concise_timestamp_html(last_synced_at, now, fallback="")` and treat an empty return as "not usable" so an unparseable stored value falls back to the pending string rather than producing a fabricated or blank time — this is `16-UI-SPEC.md` Open Question 3's stated fallback. Because that helper is a raw-markup-producing function, interpolate its return verbatim and do not re-escape it, and escape the surrounding sentence fragments separately; leave a comment saying so, since it is the one place in this function where the file's otherwise-universal escaping discipline is deliberately not applied.

Build the options with `_rule_add_form_html()`'s exact loop — one option per `device_config.THEME_IDS` member in order, text from `device_config.theme_label()`, both escaped — and mark selected the saved `calendar_theme_id` when it is set, otherwise the currently-selected base theme id, matching the arrivals grid's own default-selection precedent. Give the select the `required` attribute and no swatch inside any option. Emit `class="calendar-status"` on the status paragraph as a bare hook and **add no rule to `companion/static/style.css`** — that resolves `16-UI-SPEC.md` Open Question 4 in favour of the no-new-CSS option, since the spec states the class would declare nothing a bare paragraph does not already inherit.

**3. Wire it into `render()`.** Read `ctx.get("calendar_configured")` and `ctx.get("calendar_last_synced_at")` beside the existing `wake_interval_env_default` read, and read `device_cfg.get("calendar_theme_id")` with an explicit `.get()` and no `or` fallback — `None` is meaningful here, exactly as `current_theme_arriving`'s own read comment already explains for its key. Add one more format slot to the return tuple, immediately after `display_group(...)`'s and before the static save-fallback attribute, so the Calendar group is the last section inside the form. Add a comment recording that this position is `16-UI-SPEC.md` § Section Anatomy's Placement recommendation, taken as recommended: it is a pure zero-disruption append that leaves every existing group's relative order and DOM nesting untouched, where the considered alternative (directly after Theme) would insert into the middle of an already-verified sequence for a purely narrative benefit. Note explicitly that unlike Phase 15's Rules section this group belongs **inside** the form, because it nests no form of its own and submits with the shared Save Settings flow.

**4. Extend `handle_post()`.** Read `calendar_theme_id` from the form beside the existing reads. Add a membership test against `device_config.THEME_IDS` in the same shape and the same position as the `theme_arriving` one, returning the existing generic save-failed flash constant — no new flash key, matching `16-UI-SPEC.md` § Flash messages, which classifies a non-member value here as a hostile-request shape rather than a genuine user mistake. Pass the value through as one more keyword argument on the existing single `save_device_config()` call, leaving the all-or-nothing rejection contract unchanged. Extend the function's docstring with one short paragraph naming the new field and stating that it is a plain tracked field with no checkbox, so unlike `theme_arriving` it needs no clear sentinel and `None` keeps its "not supplied, carry forward" meaning.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>

  <acceptance_criteria>
    - The three status strings and both hints are byte-identical to `16-UI-SPEC.md`'s Copywriting Contract: for each of `CALENDAR_SECTION_CAPTION`, `CALENDAR_STATUS_CONFIGURED_PENDING`, `CALENDAR_THEME_HINT`, the constant's value appears as a contiguous substring of `16-UI-SPEC.md`. Assert it with a script that imports the module and searches the spec file, so a paraphrase fails rather than merely looking different.
    - The forbidden vocabulary is absent: `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); import companion.pages.config_page as cp; blob=' '.join([cp.CALENDAR_SECTION_HEADING,cp.CALENDAR_SECTION_CAPTION,cp.CALENDAR_STATUS_NOT_CONFIGURED,cp.CALENDAR_STATUS_CONFIGURED_PENDING,cp.CALENDAR_STATUS_CONFIGURED_SYNCED_PREFIX,cp.CALENDAR_THEME_FIELD_LABEL,cp.CALENDAR_THEME_HINT]).lower(); bad=[w for w in ('track','watch','follow','monitor','notify','currently flying','in the air','on duty','crew','roster','duty','pilot','she','her','his') if w in blob]; assert not bad, bad"` exits 0.
    - The URL never reaches the markup: rendering the Calendar group with the environment variable set to a URL carrying a distinctive token produces markup containing neither the token, the host, the path nor the query-parameter name.
    - Exactly one select, correctly populated: the rendered group contains exactly one occurrence of the `calendar_theme_id` field name, and exactly `len(device_config.THEME_IDS)` option elements, in `THEME_IDS` order.
    - The three status branches are exclusive: for each of the three input combinations, the rendered markup contains that branch's string and neither of the other two.
    - An unparseable stored sync timestamp falls back to the pending string and produces no timestamp markup.
    - Placement: in the rendered Settings page, the index of the Calendar heading is greater than the index of the Display heading and less than the index of the settings form's closing tag.
    - `grep -c 'configured_calendar_url' companion/pages/config_page.py companion/app.py` returns `0` for both files.
    - No new CSS: `git diff --stat -- companion/static/` is empty.
    - No new flash constant: `git diff -- companion/pages/config_page.py | grep -c '^+FLASH_'` returns `0`.
    - `server/.venv/bin/python3 companion/test_config_page.py` and `server/.venv/bin/python3 companion/test_companion_app.py` both exit 0 with their `EXPECTED_CHECK_COUNT` values still at their pre-edit values, proving this task broke nothing before Task 3 extends the ledger.
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 — no colour or contrast surface changed.
  </acceptance_criteria>

  <done>The Settings page carries a Calendar group as its last in-form section, with verbatim locked copy, a three-state text-only status line, a native theme select wired to `calendar_theme_id`, a membership-gated POST path reusing the existing flashes, and no new CSS, icon, button or flash key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the calendar presence and last-synced values through companion/app.py, and document the env var</name>

  <files>companion/app.py, deploy/skypane.env.example</files>

  <read_first>
    - `companion/app.py` **lines 100-110** — `SLEEP_ENV_VAR`'s declaration, the precedent for where an env-var name constant lives in this file, and why this plan deliberately references `calendar_rules.CALENDAR_URL_ENV_VAR` instead of declaring a second literal.
    - `companion/app.py` **lines 501-534** — `env_wake_interval_default()`: the fail-open, per-call, never-cached env read whose shape this plan reuses, and its docstring's explicit contrast with `configured_password()`'s fail-closed shape.
    - `companion/app.py` **lines 60-80** — the import block, including the existing `from server.plane import colour_rules, illustrations, manual_resolutions` line this plan extends.
    - `companion/app.py` **lines 908-1010** — `page_context()`: where every per-request key is built, including the `wake_interval_env_default`, `colour_rules` and `manual_resolutions` entries and their comments about reading fresh per request rather than through the poll cycle's process-scoped cache.
    - `companion/auth.py` **lines 40-80** — `PASSWORD_ENV_VAR` and `configured_password()`, the existing secret-handling precedent and the model for the calendar URL.
    - `deploy/skypane.env.example` **in full** — the never-committed discipline in its header, and the comment style each variable's block uses (what it is for, how to generate a real value, which units read it).
    - `server/plane/calendar_rules.py` as delivered by plan 16-03 — `calendar_is_configured()`, `load_calendar_registry()`, `CALENDAR_URL_ENV_VAR`.
  </read_first>

  <behavior>
    - `page_context()` returns a `calendar_configured` key that is a genuine `bool`, `True` exactly when the environment variable holds a non-empty value at the moment of the request.
    - Setting the environment variable between two requests changes the key's value on the second, because nothing is captured at import time.
    - `page_context()` returns a `calendar_last_synced_at` key holding the registry's stored ISO string, or `None` when no successful sync has been recorded, read fresh per request from the state directory.
    - Neither key ever holds the URL or any part of it.
    - A missing, unreadable or corrupt `calendar_rules.json` yields `calendar_last_synced_at` as `None` rather than an exception, so a broken registry cannot 500 an authenticated page.
  </behavior>

  <action>
Two files.

**`companion/app.py`.** Extend the existing `from server.plane import ...` line with `calendar_rules`. Add two keys to `page_context()`'s returned dict, positioned beside the `colour_rules` entry so the per-request-fresh-read group stays together:

- `calendar_configured`, from `calendar_rules.calendar_is_configured()`. Comment it in `env_wake_interval_default()`'s own vocabulary: read fresh on every request, never captured at import time, so a redeployed env file takes effect on the next service restart with nothing cached in between. Then state the security contract plainly, because it is the reason this key is a boolean and not a string: the calendar URL is a subscription secret, the companion process must never learn its value, and `calendar_rules.configured_calendar_url()` therefore has no call site anywhere under `companion/` — this key is the whole of what the web tier is allowed to know.
- `calendar_last_synced_at`, from `calendar_rules.load_calendar_registry(state_dir)`'s stored value. Comment it with the same reasoning the neighbouring `manual_resolutions` and `colour_rules` entries carry: read fresh per request from disk, never through the poll cycle's process-scoped cache, because this service is a long-running threading HTTP server and a sync landing mid-session must be visible on the very next request. Note that the loader is contractually never-raising, which is what makes it safe to call unconditionally on every authenticated page render.

Do not add a `SKYPANE_CALENDAR_ICS_URL` literal to this file. `SLEEP_ENV_VAR` is a local literal because no server module owns that name; this one is owned by `calendar_rules` and must be referenced from there so the not-configured copy string, the reader and the env template cannot drift apart.

**`deploy/skypane.env.example`.** Append a `SKYPANE_CALENDAR_ICS_URL` block in the file's established style, after the companion-host block. It must state: what the variable is (the private subscription URL of an iCal calendar feed, used to give flights that calendar lists their own panel theme); that the URL itself is a credential, because it grants read access to a private schedule to anyone holding it, so it carries the same never-committed, never-pasted-into-a-log-or-a-transcript discipline the file's header already states for the other two secrets; that it is optional and that leaving it unset is a designed state in which the feature is simply inactive; that only `skypane-poll.service` reads the value, while `skypane-companion.service` reads only whether it is set, so the Settings page shows a connected-or-not status and never the URL; and that the feed is fetched over HTTPS only, at most once every thirty minutes, with private and loopback addresses refused. Ship a placeholder value in the same shape the other secrets use — an obviously-fake value that could not be mistaken for real. Do not include a real calendar URL, a real host, or anything resembling a real token.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys,os,tempfile,json; sys.path.insert(0,'.')
import server.plane.calendar_rules as cr
src=open('companion/app.py').read()
assert 'calendar_configured' in src and 'calendar_last_synced_at' in src
assert 'configured_calendar_url' not in src, 'the companion must never call the value accessor'
assert 'SKYPANE_CALENDAR_ICS_URL' not in src, 'the env var name must be referenced from calendar_rules, not redeclared'
env=open('deploy/skypane.env.example').read()
assert 'SKYPANE_CALENDAR_ICS_URL=' in env
assert 'https://' not in env.split('SKYPANE_CALENDAR_ICS_URL=')[1].split(chr(10))[0].lower() or 'replace' in env.split('SKYPANE_CALENDAR_ICS_URL=')[1].split(chr(10))[0].lower()
with tempfile.TemporaryDirectory() as d:
    os.environ.pop(cr.CALENDAR_URL_ENV_VAR, None)
    assert cr.calendar_is_configured() is False
    assert cr.load_calendar_registry(d)['last_synced_at'] is None
    open(cr.calendar_rules_path(d),'w').write('garbage{')
    assert cr.load_calendar_registry(d)['last_synced_at'] is None
print('app wiring OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 and prints `app wiring OK`.
    - `grep -c 'configured_calendar_url' companion/` (recursively, across every file) returns `0`.
    - `grep -rc 'SKYPANE_CALENDAR_ICS_URL' companion/` returns `0` for every file — the name is referenced through `calendar_rules.CALENDAR_URL_ENV_VAR`, never retyped.
    - Both keys are genuinely per-request: an authenticated page render with the variable unset, followed by setting it and rendering again in the same process, shows `False` then `True`.
    - `calendar_configured` is a `bool` and `calendar_last_synced_at` is a string or `None`, asserted by type on a live `page_context()` call.
    - A corrupt registry file does not 500 the page: with `calendar_rules.json` containing non-JSON bytes, an authenticated GET of the Settings route returns 200 and renders the pending or not-configured status.
    - `deploy/skypane.env.example` carries a placeholder, not a URL: the value on the `SKYPANE_CALENDAR_ICS_URL` line contains no dot-separated hostname and no query string.
    - `git diff --stat -- server/` is empty for this plan's commits.
    - `server/.venv/bin/python3 companion/test_companion_app.py` exits 0.
  </acceptance_criteria>

  <done>The companion learns only whether a calendar is configured and when it last synced, both read fresh per request, with the URL's value having no call site anywhere in the web tier — and the env template documents the variable as the credential it is.</done>
</task>

<task type="auto">
  <name>Task 3: Extend companion/test_config_page.py with the Calendar group's render, copy, containment and POST checks</name>

  <files>companion/test_config_page.py</files>

  <read_first>
    - `companion/test_config_page.py` — its preamble and sys.path bootstrap; its existing `theme_arriving` render and `handle_post()` check groups (roughly eighty references, the direct template); its adversarial-payload membership-test checks; its one end-to-end HTTP round-trip check; its live `EXPECTED_CHECK_COUNT` and the terminating count comparison.
    - `companion/pages/config_page.py` and `companion/app.py` as modified in Tasks 1 and 2.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-UI-SPEC.md` § "Copywriting Contract" and § "What this section deliberately does NOT say" — the source of truth the copy checks compare against.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md` — the two rows whose Automated Command column names `companion/test_config_page.py`: the secret-never-reaches-a-page row and the D-01 no-calendar-row-in-the-rules-list row.
  </read_first>

  <action>
Extend `companion/test_config_page.py` in place, using its existing idioms and adding no new import beyond `os` if it is not already present. No pytest.

Write these checks:

**Three status states, mutually exclusive.** Render the Settings page three times — not configured; configured with no stored sync; configured with a stored ISO sync timestamp — and for each assert that the expected status string is present and that both other status strings are absent. In the third case additionally assert a relative-age fragment is present, proving `concise_timestamp_html()` was used rather than a bare ISO string being printed.

**Unparseable sync timestamp falls back honestly.** With a stored `last_synced_at` that is a non-ISO string, assert the pending string is rendered and no timestamp markup appears — never a fabricated time.

**Copy fidelity against the spec.** Read `16-UI-SPEC.md` from disk and assert each of the section caption, the two configured-state strings and the theme hint appears in it as a contiguous substring. This is what makes a paraphrase fail rather than merely look different, and it is the check that keeps measured finding 3's constraint enforced after this phase closes.

**Forbidden vocabulary.** Assert none of the phase's banned words appears anywhere in the rendered Calendar group's text: the verbs implying independent awareness, the real-time phrasings, and any person, role, employer or crew-function noun.

**The secret never reaches a page (16-VALIDATION, T-16-SECRET).** With the environment variable set to a URL carrying a distinctive token, render the full Settings page and assert the token, the hostname, the path segment and the query-parameter name each appear nowhere in the response body — not in text, not in an attribute, not in a comment. Repeat the assertion against a real authenticated HTTP round trip using this file's existing end-to-end idiom, so the check covers the served bytes and not only the render function's return.

**No preview, no count (16-UI-SPEC § Configured / Not Configured State).** With a populated `calendar_rules.json` in the state dir holding several entries with real-looking routes and times, render the Settings page and assert none of the entries' airport codes, none of their airline codes and no digit-bearing count phrase derived from them appears anywhere in the body. This is the check that stops a future well-meaning addition of an upcoming-flights preview from landing silently.

**D-01 on the page (16-VALIDATION `registry` row).** With both a populated `calendar_rules.json` and a `colour_rules.json` holding one hand-added rule, assert the rendered rules list shows exactly the one manual rule and no calendar-sourced row — the operator's editor never fills with entries they did not create.

**The theme select.** Exactly one `calendar_theme_id` field is emitted; it carries one option per `THEME_IDS` member in that order; with a saved value that option is selected; with no saved value the option matching the current base theme is selected; the select is `required`; no option carries a swatch.

**Placement and form membership.** The Calendar heading's index in the body is after the Display heading's and before the settings form's closing tag, and the group carries the dirty-section attribute.

**POST validation (T-16-TAMPER, HTTP-layer half).** A valid member persists and carries the other fields forward. Each of a non-member string, an empty string, a path-traversal-shaped payload and a SQL-shaped payload returns the generic save-failed flash key and writes nothing — reuse the adversarial payloads this file already defines for the theme field. A post omitting the field entirely leaves an already-saved value untouched.

**No-JS correctness.** Assert the rendered group contains no inline event-handler attribute and no script tag, and that the select sits inside the settings form's markup range.

Then raise `EXPECTED_CHECK_COUNT` by exactly the number of checks added, **re-derived by running the harness**, following the file's ledger-comment style with a note that phase 16 plan 05 added the Calendar group checks.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 and prints an `N/N checks pass`-shaped line whose `N` is strictly greater than the pre-edit `EXPECTED_CHECK_COUNT`.
    - `grep -c 'total == EXPECTED_CHECK_COUNT' companion/test_config_page.py` returns `1`.
    - The copy-fidelity check is live rather than vacuous: temporarily reword one character of `CALENDAR_SECTION_CAPTION` in `companion/pages/config_page.py`, confirm the harness exits non-zero and that the failing check is the spec-substring one, then restore it.
    - The secret-containment check is live rather than vacuous: temporarily render the environment variable's value into the status line, confirm the harness exits non-zero and that the failing check is a containment one, then restore it.
    - The no-preview check is live rather than vacuous: temporarily append a parsed-entry count to the status line, confirm the harness exits non-zero, then restore it.
    - `server/.venv/bin/python3 companion/test_companion_app.py`, `server/.venv/bin/python3 companion/test_status_pages.py`, `server/.venv/bin/python3 companion/test_view_pages.py` and `server/.venv/bin/python3 companion/test_contrast_check.py` all exit 0.
    - `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <done>The config-page harness proves the three status states are exclusive and honest, the copy is byte-identical to the spec and free of the banned vocabulary, the calendar URL reaches neither the rendered markup nor the served bytes, no calendar content or count is displayed, no calendar-sourced row appears in the rules editor, the theme select is correctly populated and placed inside the settings form, and every hostile submitted value is refused with the existing generic flash.</done>
</task>

</tasks>

## Artifacts this phase produces

Introduced by **this plan** (16-05):

| Kind | Symbol / path |
|---|---|
| copy constant | `config_page.CALENDAR_SECTION_HEADING` |
| copy constant | `config_page.CALENDAR_SECTION_CAPTION` |
| copy constant | `config_page.CALENDAR_STATUS_NOT_CONFIGURED` |
| copy constant | `config_page.CALENDAR_STATUS_CONFIGURED_PENDING` |
| copy constant | `config_page.CALENDAR_STATUS_CONFIGURED_SYNCED_PREFIX` |
| copy constant | `config_page.CALENDAR_THEME_FIELD_LABEL` |
| copy constant | `config_page.CALENDAR_THEME_HINT` |
| function | `config_page.calendar_group(configured, last_synced_at, now, current_calendar_theme_id, current_theme_id)` |
| form field name | `calendar_theme_id` (POST to the existing `/settings` route) |
| element id | `calendar-theme` (the select, and its label's `for` target) |
| CSS class name | `calendar-status` — emitted as a markup hook only; **no rule is added to `companion/static/style.css`** |
| dirty-section name | `Calendar` (the value of `data-dirty-section` on the new group) |
| ctx key | `page_context()` → `calendar_configured` (bool) |
| ctx key | `page_context()` → `calendar_last_synced_at` (ISO string or `None`) |
| env var (documented) | `SKYPANE_CALENDAR_ICS_URL` in `deploy/skypane.env.example` |

No new route path, no new flash key, no new CSS custom property, no new icon, no new button. Deliberately **not** introduced, each with recorded reasoning in `16-UI-SPEC.md`: an upcoming-flights preview, a parsed-flight count, a test-connection button, a staleness warning banner, and any status colour, dot or badge.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment → the companion's rendered HTML | The calendar URL is a subscription credential; the web tier is the one surface other people in the room can see. |
| authenticated operator → `POST /settings` → `save_device_config(calendar_theme_id=…)` | A submitted string becomes durable config consulted on every poll cycle. |
| `{state_dir}/calendar_rules.json` → an authenticated page render | A named person's near-term schedule sits behind the loader the Settings page calls on every request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-SECRET | Information Disclosure | the companion's access to the calendar URL | high | mitigate | The web tier is architecturally denied the value: `page_context()` carries a boolean from `calendar_is_configured()`, and `configured_calendar_url()` has no call site anywhere under `companion/` — pinned by a recursive zero-count grep across the whole directory, not by review. The not-configured copy names the variable, never a value. Proved by Task 3's containment checks, which set a tokenised URL and assert four distinct substrings are absent from both the render function's return and the served HTTP bytes, backed by a live-mutation criterion. |
| T-16-PRIV | Information Disclosure | what the Settings page says about the calendar's contents | medium | mitigate | The page shows a configured-or-not status and a last-synced age, and nothing about what was fetched — `16-UI-SPEC.md` § Configured / Not Configured State rejects an upcoming-flights preview, a next-match indicator and even a bare count, each with its own recorded reasoning, because flight-level detail and even a countable signal are identifying to anyone who knows the operator. Proved by Task 3's no-preview check, which populates a real-shaped registry and asserts none of its routes, airline codes or a derived count appears in the body — so a future well-meaning addition fails the suite rather than shipping. |
| T-16-TAMPER | Tampering | the submitted `calendar_theme_id` | high | mitigate | Two independent gates: `handle_post()`'s membership test against `device_config.THEME_IDS` before the value leaves the HTTP layer, returning the existing generic save-failed flash and writing nothing; and `save_device_config()`'s own `ValueError` before the file is touched (plan 16-02). The read path adds a third in `normalise_calendar_theme_id()`, and the resolver a fourth (plan 16-06). Proved by Task 3's four adversarial-payload checks, reusing the payloads this harness already defines for the theme field. |
| T-16-CSRF | Cross-site request forgery | the `calendar_theme_id` field's write path | medium | transfer | Transferred to the site-wide session gate plus the session cookie's strict same-site flag, whose coverage was verified route-by-route in `15-SECURITY.md` § Transfer Verification. The transfer target is genuinely applicable here and needs no new verification: this field adds **no new route** — it rides the already-gated `POST /settings` handler that `theme`, `theme_arriving`, `tracked_runway`, `led_enabled`, the quiet-hours fields, `wake_interval_s` and `display_enabled` all already share, and which `require_session()` already guards before dispatch. Phase 15's T-15-13 is the direct precedent for this exact disposition on this exact route. |
| T-16-DOS | Denial of Service | a corrupt registry file reached on every authenticated page render | low | mitigate | `load_calendar_registry()` is contractually never-raising (plan 16-03), which is what makes it safe to call unconditionally in `page_context()` on every authenticated page. Proved by Task 2's criterion that an authenticated GET of the Settings route returns 200 with `calendar_rules.json` containing non-JSON bytes. |
| T-16-SC | Tampering | package installs | n/a | accept | This plan installs zero packages, adds no component library, no CSS framework, no client script and no build step. `companion/pages/config_page.py` gains one import of an existing first-party server module. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable for the whole phase. |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with a raised ledger.
- `server/.venv/bin/python3 companion/test_companion_app.py`, `companion/test_status_pages.py`, `companion/test_view_pages.py` and `companion/test_contrast_check.py` all exit 0.
- `scripts/run-all-tests.sh` exits 0. The pre-existing load-sensitive `/poll-now` concurrency check in `companion/test_companion_app.py` is a known flake under suite load (`16-VALIDATION.md` § Sampling Rate) — a single failure of that one check that passes on re-run is not a Phase 16 regression; any other failure is.
- **D-07 standing gate (checked in every plan of this phase):** `git diff --stat -- server/plane/render.py` is empty and `server/.venv/bin/python3 server/test_render.py` exits 0 with its `EXPECTED_CHECK_COUNT` unedited.
- **No new dependency (checked in every plan of this phase):** `git diff -- server/requirements.txt server/requirements-dev.txt` is empty.
- `git diff --stat -- server/ companion/static/` is empty for this plan's commits — this plan runs in the same wave as 16-04 and touches no server file and no stylesheet.
- **Human check (`workflow.human_verify_mode` is `end-of-phase`, so this is recorded for the end-of-phase review rather than as a blocking checkpoint):**
  <human-check>
    Load the companion Settings page with `SKYPANE_CALENDAR_ICS_URL` unset, then with it set to a real feed, and confirm three things by eye. First, the URL appears nowhere — not in the page, not in the page source, not in a tooltip. Second, the copy promises only that the frame colours a flight that happens to be on screen, and nothing on the page suggests it is watching, tracking or announcing anything. Third, the theme picker looks and behaves like the one in the Rules add-form, and changing it dirties the save bar exactly like changing Wake interval does. `16-VALIDATION.md` § Manual-Only Verifications records this as the one manual item; the real end-to-end match is opportunistic and explicitly not a phase gate.
  </human-check>
</verification>

<success_criteria>
- One Calendar group renders as the last section inside the settings form, with the dirty-section attribute and no new CSS, icon, button or flash key.
- The status line renders exactly one of three locked strings, distinguished by text alone, with the synced state carrying an absolute-plus-relative timestamp and an unparseable stored value falling back to the pending string.
- Every user-facing string is byte-identical to `16-UI-SPEC.md` and free of the banned vocabulary, enforced by a check that fails on a paraphrase.
- The calendar URL reaches neither the rendered markup nor the served bytes, and `configured_calendar_url()` has zero call sites under `companion/`.
- No flight, route, time or count from the calendar appears anywhere on the page, and the rules editor shows only rules the operator typed.
- A hostile `calendar_theme_id` is refused at the HTTP layer with the existing generic flash and nothing is written.
- `deploy/skypane.env.example` documents the variable as a credential, with a placeholder value.
</success_criteria>

<output>
Create `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-05-SUMMARY.md` when done.
</output>
