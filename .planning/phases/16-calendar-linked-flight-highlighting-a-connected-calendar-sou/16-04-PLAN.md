---
phase: 16-calendar-linked-flight-highlighting-a-connected-calendar-sou
plan: 04
type: execute
wave: 3
depends_on:
  - 16-03
files_modified:
  - server/plane/calendar_rules.py
  - server/test_calendar_rules.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "The calendar fetch is the project's first outbound request to an operator-supplied host, and it is refused unless the scheme is https and EVERY address getaddrinfo() resolves the hostname to is a public unicast address — the check is on the resolved IP, not on the hostname string, because a hostname check loses to DNS rebinding."
    - "Redirects are not followed automatically: each Location target is re-validated through the identical scheme-and-resolved-IP gate before it is followed, and the hop count is bounded."
    - "The response body is capped while it is being read, by streaming and counting bytes, never by trusting a Content-Length header a hostile or misconfigured server can omit, understate or exceed."
    - "The request carries a hard timeout, so a slow-drip feed cannot hold the 30-second poll oneshot open."
    - "No log line anywhere in this module contains the calendar URL or any substring of it. The exception object's own string form is never interpolated, because several requests.exceptions subclasses embed the request URL — which carries the secret token — in their default string form, and journalctl is readable by anyone with VPS access."
    - "The fetch never raises out of the poll cycle: every failure path returns a result code and leaves the previous registry file as it stands, so a transient network blip degrades to 'the existing rolling window keeps working' rather than to an emptied window."
    - "A fetch attempt updates last_attempt_at whether or not it succeeded; last_synced_at moves only after a body was fetched AND parsed, so a permanently failing feed is neither retried every cycle nor displayed as fresh."
    - "The whole fetch/parse/persist step is reachable from a single function poll_loop.py can call once per cycle, and that function performs no network I/O at all when the feature is unconfigured or the throttle has not elapsed."
  artifacts:
    - server/plane/calendar_rules.py
    - server/test_calendar_rules.py
  key_links:
    - "Validation must happen after getaddrinfo() and before the connection, and again for every redirect hop. Validating only the hostname in the configured URL leaves the DNS-rebinding gap wide open, and validating only the first URL leaves the redirect gap wide open."
    - "The response-size cap must abort while iterating the streamed body. A cap applied after the body is fully read is not a cap."
    - "refresh_calendar_registry() must persist last_attempt_at on the failure paths too, or a permanently unreachable feed is contacted on every 30-second poll cycle forever."
    - "A failed fetch must leave the existing calendar_rules.json entries untouched. Overwriting them with an empty list on a transient error would erase an otherwise-valid rolling window before its natural expiry."
  prohibitions:
    - "The exception object caught from an outbound call must never be interpolated into a logged string, and the URL must never be logged on any path including success."
    - "requests.get() must not be called with automatic redirect following enabled."
    - "The Content-Length header must not be used as the size bound."
    - "The fetch must not run when the feature is unconfigured or when the throttle interval has not elapsed."
    - "A failed or refused fetch must not write an empty entries list over an existing populated registry."
    - "This module must not import server.plane.colour_rules."
    - "server/plane/render.py must not be modified by this plan."
    - "No new entry may be added to server/requirements.txt or server/requirements-dev.txt."
---

<objective>
Give `server/plane/calendar_rules.py` its egress tier: an SSRF-hardened, size-bounded, redirect-bounded, timeout-bounded HTTPS fetch of the operator-supplied iCal URL, secret-safe failure logging, and the single `refresh_calendar_registry()` entry point that ties throttle, fetch, parse, window and persist together for `poll_loop.py` to call once per cycle.

Purpose: this is the largest genuinely new threat surface the project has taken on. Every existing outbound call in this codebase targets a fixed, developer-chosen URL template, so no SSRF-class validation exists anywhere to imitate — and the calendar URL is simultaneously the first runtime secret held outside `companion/auth.py`. Two specific traps are project-specific rather than generic and are each given their own acceptance criterion: a hostname-only address check loses to DNS rebinding, and this codebase's existing exception-logging idiom would write the secret straight into `journalctl`.

Output: one extended server module, one extended harness. No network call is ever made by the test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-CONTEXT.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-PATTERNS.md
@.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md
@.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-SECURITY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the SSRF-hardened, bounded, secret-safe fetch</name>

  <files>server/plane/calendar_rules.py</files>

  <read_first>
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md` § "Code Examples" → "Bounded, IP-validated fetch skeleton" **in full**. This is the vetted skeleton to implement from; do not re-derive it. Also § "Don't Hand-Roll" (use the stdlib address module for range classification, do the name resolution explicitly, follow redirects manually) and § "Common Pitfalls" Pitfalls 2 and 4.
    - `server/plane/detect.py` **lines 72-80 and 404-420** — `USER_AGENT` and `query_provider()`: the codebase's outbound-call idiom (a `requests.get()` with a `User-Agent` header and a timeout, wrapped by a narrow per-call catch in its caller).
    - `server/plane/detect.py` **lines 945-960** — the caller's catch. This is the idiom this task must deliberately **diverge** from on the logging line, and the divergence needs a comment saying why rather than looking like an oversight.
    - `server/plane/enrich.py` **lines 163-185** — `default_transport()` and the injectable-`transport` parameter, with its docstring explaining that the seam exists so tests replay a committed fixture instead of making a live call. Copy this seam exactly.
    - `companion/auth.py` **lines 60-80** — the docstring clause forbidding the environment value from appearing in a raised exception; the same discipline, one layer out.
    - `server/plane/calendar_rules.py` as extended by plans 16-01 and 16-03 — the constants block, the result-code constants, and `configured_calendar_url()`.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-PATTERNS.md` § "`server/plane/calendar_rules.py` — Bounded/hardened HTTP (SSRF + size cap)" and § "Shared Patterns" → "Bounded, per-call-exception outbound HTTP, never logging the raw exception string".
  </read_first>

  <behavior>
    - A URL whose scheme is `http`, `file`, `ftp`, `gopher` or absent is refused before any name resolution is attempted.
    - A URL with no hostname is refused.
    - A URL whose hostname resolves to a loopback address, a private-range address, a link-local address, a reserved address, a multicast address, or the IPv6 forms of any of those, is refused — and the refusal is driven by what the resolver returned, not by the text of the hostname.
    - A hostname that resolves to several addresses of which only one is private is refused, not accepted on the strength of the public ones.
    - A hostname that fails to resolve at all is refused.
    - A response carrying a redirect status is not followed automatically; its `Location` target goes back through the identical scheme-and-address gate, and a target that fails it is refused.
    - A redirect chain longer than `CALENDAR_MAX_REDIRECTS` returns nothing rather than looping.
    - A redirect response with no `Location` header returns nothing.
    - A non-200 final status returns nothing.
    - A body larger than `CALENDAR_MAX_RESPONSE_BYTES` returns nothing, and the decision is reached while the body is being streamed — a response that declares a small `Content-Length` and then streams far more is still refused.
    - Any exception raised by the transport returns nothing rather than propagating.
    - On every refusal and every failure, nothing printed contains the URL, its host, its path or its query.
  </behavior>

  <action>
Extend `server/plane/calendar_rules.py`. Add exactly three imports — `requests`, plus the stdlib address-classification module and the stdlib name-resolution module, plus the stdlib URL-parsing helper — and note in the module docstring that `requests` is already a pinned dependency used by `detect.py` and `enrich.py`, so no new package enters `server/requirements.txt`. Add a `USER_AGENT` constant in `detect.py`'s and `enrich.py`'s shape.

**`_address_is_public(ip_text)`.** Parse the text into an address object and return `False` when it is private, loopback, link-local, reserved, multicast or unspecified, `True` otherwise. Return `False` rather than raising on an unparseable value. Comment that this is deliberately delegating range classification to the standard library rather than hand-rolling CIDR arithmetic, and that it covers IPv4 and IPv6 through the same call.

**`_host_is_safe(hostname, port=None)`.** Resolve the hostname explicitly through the stdlib name resolver; return `False` when resolution fails. Iterate every returned address record, extract the address text from the socket-address tuple, and return `False` if `_address_is_public()` rejects any one of them. Return `True` only when every resolved address passes. The docstring must state the reason this function exists in this exact shape: checking the hostname string against a blocklist and stopping there is defeated by DNS rebinding — the hostname can resolve to a public address at validation time and a private one at connection time — so the addresses the resolver actually returns are what must be checked, and a single private answer among several public ones is enough to refuse.

**`_url_is_safe(url)`.** Parse the URL; require the scheme to be exactly `https` and the hostname to be present, then delegate to `_host_is_safe()`. Never raise.

**`default_calendar_transport(url, timeout)`.** A thin wrapper over a single `requests.get()` carrying the `User-Agent` header, the timeout, streaming enabled and automatic redirect following **disabled**, returning the response object. Copy `enrich.default_transport()`'s docstring shape including the sentence explaining that the injectable-`transport` parameter on the caller exists specifically so tests replay a fake instead of making a live network call.

**`fetch_ics(url, timeout=None, transport=None, max_redirects=None, max_bytes=None)`.** Returns the decoded body text, or `None` on any refusal or failure. Never raises. Structure: default the three bounds from the module constants; loop at most `max_redirects` plus one times; on each iteration, refuse immediately unless `_url_is_safe()` accepts the current URL; call the transport inside a narrow catch of the requests exception base class and of `ValueError`, returning `None` on either; if the response is a redirect, read its `Location`, close the response, refuse when it is absent, resolve a relative target against the current URL, set the current URL to the target and continue the loop — so the next iteration re-validates it through the same gate; if the final status is not 200, close and return `None`; otherwise iterate the streamed body in chunks, accumulating a running byte count, and abort by closing the response and returning `None` the moment the count exceeds `max_bytes`; finally join the chunks and decode as UTF-8 with replacement. Falling out of the loop (too many hops) returns `None`.

**The logging discipline — the project-specific trap, and this task's headline acceptance criterion.** Every failure path in this function that logs at all logs the exception *type name* and a fixed, hand-written description, and nothing else. Do not interpolate the caught exception object into any string. Do not log the URL, its host, its path, its query or its length, on any path — including success. Write a comment above the catch clause explaining the divergence from `detect.py`'s neighbouring caller-catch idiom, which does interpolate the exception object: several of the requests exception subclasses embed the request URL in their default string form, that URL carries the calendar subscription token, and the poll service's journal is readable by anyone with VPS access. Point at this project's own existing discipline (`poll_loop.py`'s docstring already binds the project to never logging a bearer token or the BYOS setup secret) so a future reader sees this as a continuation of an existing rule rather than a new one.

Add a short summary of all four bounds to the function's docstring — scheme, resolved address, redirect count, response size, timeout — noting that each is independently insufficient and they are applied together.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys,io,contextlib; sys.path.insert(0,'.')
import server.plane.calendar_rules as cr
for bad in ('http://example.invalid/a.ics','ftp://example.invalid/a.ics','file:///etc/passwd','https:///a.ics','not-a-url'):
    assert cr._url_is_safe(bad) is False, bad
for loop in ('https://127.0.0.1/a.ics','https://localhost/a.ics','https://169.254.169.254/latest/meta-data','https://10.0.0.5/a.ics','https://192.168.1.1/a.ics','https://[::1]/a.ics'):
    assert cr._url_is_safe(loop) is False, loop
assert cr._address_is_public('8.8.8.8') is True and cr._address_is_public('10.0.0.1') is False
assert cr._address_is_public('::1') is False and cr._address_is_public('not-an-ip') is False
class R:
    def __init__(s, code=200, body=b'', headers=None, redirect=False):
        s.status_code=code; s._b=body; s.headers=headers or {}; s.is_redirect=redirect
    def iter_content(s, chunk_size=8192):
        for i in range(0, len(s._b), chunk_size): yield s._b[i:i+chunk_size]
    def close(s): pass
SECRET='https://feed.example.com/cal.ics?token=SUPERSEKRITTOKEN'
buf=io.StringIO()
with contextlib.redirect_stderr(buf):
    assert cr.fetch_ics(SECRET, transport=lambda u,t: R(200, b'BEGIN:VCALENDAR')) == 'BEGIN:VCALENDAR'
    assert cr.fetch_ics(SECRET, transport=lambda u,t: R(500)) is None
    assert cr.fetch_ics(SECRET, transport=lambda u,t: R(200, b'x'*(cr.CALENDAR_MAX_RESPONSE_BYTES+10), {'Content-Length':'1'})) is None
    assert cr.fetch_ics(SECRET, transport=lambda u,t: R(302, b'', {'Location':'https://127.0.0.1/a.ics'}, True)) is None
    assert cr.fetch_ics(SECRET, transport=lambda u,t: R(302, b'', {}, True)) is None
    assert cr.fetch_ics(SECRET, transport=lambda u,t: R(302, b'', {'Location': SECRET}, True)) is None
    def boom(u,t): raise Exception('boom ' + u)
    import requests
    def rboom(u,t): raise requests.RequestException(SECRET)
    assert cr.fetch_ics(SECRET, transport=rboom) is None
log=buf.getvalue()
for leak in ('SUPERSEKRITTOKEN','feed.example.com','cal.ics','token='):
    assert leak not in log, (leak, log)
print('fetch OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 and prints `fetch OK`.
    - **The secret never reaches a log line (T-16-SECRET, 16-VALIDATION `fetch` row).** The command above forces a transport failure whose exception carries the full secret URL as its message — the exact shape several requests exception subclasses take by default — and asserts that neither the token, the host, the path nor the query-parameter name appears in captured stderr.
    - **Validation is on the resolved address, not the hostname (T-16-SSRF).** `server/.venv/bin/python3 -c "import sys,socket; sys.path.insert(0,'.'); import server.plane.calendar_rules as cr; real=socket.getaddrinfo; socket.getaddrinfo=lambda h,p=None,*a,**k: [(2,1,6,'',('93.184.216.34',443)),(2,1,6,'',('10.1.2.3',443))]; assert cr._url_is_safe('https://public-looking-name.example/a.ics') is False, 'one private answer among public ones must refuse'; socket.getaddrinfo=lambda h,p=None,*a,**k: (_ for _ in ()).throw(socket.gaierror('nope')); assert cr._url_is_safe('https://nxdomain.example/a.ics') is False; socket.getaddrinfo=real"` exits 0. A hostname-only implementation cannot pass this.
    - Redirects are never followed automatically: `server/.venv/bin/python3 -c "import ast; src=open('server/plane/calendar_rules.py').read(); t=ast.parse(src); calls=[n for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(getattr(n.func,'value',None),'id',None)=='requests']; assert calls, 'no requests call found'; import re; assert all(any(k.arg=='allow_redirects' and k.value.value is False for k in c.keywords) for c in calls), 'a requests call omits allow_redirects=False'; assert all(any(k.arg=='stream' and k.value.value is True for k in c.keywords) for c in calls); assert all(any(k.arg=='timeout' for k in c.keywords) for c in calls)"` exits 0.
    - The size cap is applied while streaming, not from a header: `grep -c 'Content-Length' server/plane/calendar_rules.py` returns `0`, and the oversized-body case in the verify command is driven by a response declaring a `Content-Length` of 1.
    - The hop bound holds: a transport that returns a redirect to a fresh public-looking URL on every call returns `None` after at most `CALENDAR_MAX_REDIRECTS` plus one transport invocations, counted by the fake.
    - No new dependency: `git diff -- server/requirements.txt server/requirements-dev.txt` is empty, and the only third-party import added is `requests`, already pinned.
    - `git diff --stat -- server/plane/detect.py server/plane/enrich.py server/plane/render.py` is empty.
  </acceptance_criteria>

  <done>The module can fetch an operator-supplied HTTPS iCal URL under four independent bounds — resolved-address range, redirect count re-validated per hop, streamed response size, and request timeout — and can fail on any of them without the URL or the exception's own string form reaching a single log line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add refresh_calendar_registry() — the once-per-cycle throttle, fetch, parse, window and persist step</name>

  <files>server/plane/calendar_rules.py</files>

  <read_first>
    - `server/plane/calendar_rules.py` as extended by plans 16-01 and 16-03 and by Task 1 above — `parse_ics_events()`, `load_calendar_registry()`, `write_calendar_registry()`, `select_window_entries()`, `calendar_fetch_is_due()`, `configured_calendar_url()`, `fetch_ics()`, and the five result-code constants.
    - `server/poll_loop.py` **lines 735-755** — the per-cycle priming block this step will be called beside (plan 16-07 does the wiring), and its comment about why a mid-cycle save must never split one rendered panel across two configurations.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-RESEARCH.md` § "Architecture Patterns" Pattern 1 (the degrade-to-"the previous registry file stays as-is" requirement, stated explicitly as *not* degrade-to-empty) and Pattern 2 (the two timestamps).
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-CONTEXT.md` — the Claude's-Discretion note on fetch cadence and failure: the fetch must never delay a render, and a long-unreachable feed degrades to no matches silently on the panel.
  </read_first>

  <behavior>
    - With the environment variable unset, the function makes no transport call at all and returns the unconfigured result code together with the registry as it stands on disk.
    - With the variable set but the throttle interval not elapsed, the function makes no transport call and returns the throttled result code together with the on-disk registry.
    - With the variable set and the throttle elapsed, the function calls the transport exactly once and, on a good body, returns the success code with a registry whose entries are the windowed parse of that body.
    - On success, `last_synced_at` is a fresh ISO string and `last_attempt_at` is the supplied `now`.
    - On a refused URL, on a transport failure, and on a body that parses to nothing usable, the function returns the corresponding failure code, updates `last_attempt_at` to the supplied `now`, leaves `last_synced_at` at its previous value, and leaves the previously persisted entries exactly as they were.
    - Two consecutive calls with the same `now` perform exactly one transport call, because the first call's `last_attempt_at` write closes the throttle for the second.
    - Ten consecutive calls spaced 30 seconds apart perform exactly one transport call.
    - The function never raises, for any combination of a missing state dir, an unwritable state dir, a hostile body and a failing transport.
  </behavior>

  <action>
Add `refresh_calendar_registry(state_dir, now, transport=None)` to `server/plane/calendar_rules.py`. It returns a two-element result — the result code and the registry dict the caller should use for this cycle — so `poll_loop.py` gets both the outcome and the entries from one call and never has to re-read the file.

Sequence, in this order, with an early return at each gate:

1. Load the current registry through `load_calendar_registry(state_dir)`. This value is what every early return hands back, so a skipped or failed cycle still gives the caller a usable window.
2. Read the URL through `configured_calendar_url()`. When it is `None`, return the unconfigured code with the loaded registry and **do not** touch `last_attempt_at` — an unconfigured feature is not a failing feed, and writing a timestamp for it would make the first real fetch after configuration wait out a full interval.
3. Ask `calendar_fetch_is_due(registry["last_attempt_at"], now)`. When it is `False`, return the throttled code with the loaded registry, writing nothing. This is the branch that must dominate in production: the poll timer fires every 30 seconds and the interval is `CALENDAR_FETCH_INTERVAL_S`, so all but roughly one cycle in sixty stops here without any network work at all.
4. Otherwise call `fetch_ics(url, transport=transport)`. Whatever happens next, `last_attempt_at` becomes `now`.
5. When the body is `None`, persist the current entries and `last_synced_at` unchanged with the new `last_attempt_at`, and return the failure code. Persisting the existing entries rather than an empty list is the load-bearing part: a transient blip must not erase an otherwise-valid rolling window before its natural expiry, and the panel's designed degradation is "no matches" only once the window genuinely ages out.
6. When the body is present, parse it with `parse_ics_events()`, filter with `select_window_entries(..., now)`, and persist the result with `last_attempt_at` set to `now` and `last_synced_at` set to a fresh ISO-8601 UTC string. Return the success code. Note that an empty result here is still a success: a roster with nothing in the next 48 hours is a correct, legitimately empty window, and it is only distinguishable from a broken feed by the two timestamps — which is exactly why they are separate.
7. When the persist itself fails, return the failure code with the in-memory registry the caller can still use for this cycle.

Wrap the whole body so nothing escapes: the function's contract is that `poll_loop.run_once()` can call it unconditionally and never gain a new failure mode. Its docstring must state the three no-network guarantees explicitly — no transport call when unconfigured, none when throttled, and a bounded one otherwise — because `16-CONTEXT.md` requires that the fetch never delays a render, and the poll oneshot's whole budget is short.

Do not log the URL, the result code together with the URL, or anything derived from the body. A single line naming the module, the result code and the entry count is the most this function may print, and it must print nothing on the throttled path — that path runs on almost every cycle and would otherwise flood the journal.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys,os,tempfile,io,contextlib; sys.path.insert(0,'.')
import server.plane.calendar_rules as cr
BODY=open('server/fixtures/calendar_crewwebplus_redacted.ics').read()
NOW=1789000000.0
calls=[]
def tport(u,t):
    calls.append(u)
    class R:
        status_code=200; headers={}; is_redirect=False
        def iter_content(s, chunk_size=8192): yield BODY.encode()
        def close(s): pass
    return R()
os.environ.pop(cr.CALENDAR_URL_ENV_VAR, None)
with tempfile.TemporaryDirectory() as d:
    code, reg = cr.refresh_calendar_registry(d, NOW, transport=tport)
    assert code == cr.FETCH_SKIPPED_UNCONFIGURED and calls == [] and reg['last_attempt_at'] is None, (code, reg)
os.environ[cr.CALENDAR_URL_ENV_VAR]='https://feed.example.com/cal.ics?token=SUPERSEKRITTOKEN'
buf=io.StringIO()
with tempfile.TemporaryDirectory() as d, contextlib.redirect_stderr(buf):
    code, reg = cr.refresh_calendar_registry(d, NOW, transport=tport)
    assert code == cr.FETCH_OK and len(calls)==1, (code, len(calls))
    assert reg['last_attempt_at']==NOW and isinstance(reg['last_synced_at'], str)
    synced=reg['last_synced_at']; entries=list(reg['entries'])
    for i in range(10):
        code2, reg2 = cr.refresh_calendar_registry(d, NOW + 30*(i+1), transport=tport)
        assert code2 == cr.FETCH_SKIPPED_THROTTLED and len(calls)==1, (i, code2, len(calls))
    later = NOW + cr.CALENDAR_FETCH_INTERVAL_S + 1
    code3, reg3 = cr.refresh_calendar_registry(d, later, transport=lambda u,t: (_ for _ in ()).throw(Exception('down')))
    assert code3 == cr.FETCH_FAILED, code3
    assert reg3['last_attempt_at']==later and reg3['last_synced_at']==synced, reg3
    assert cr.load_calendar_registry(d)['entries']==entries, 'a failed fetch erased the window'
log=buf.getvalue()
for leak in ('SUPERSEKRITTOKEN','feed.example.com','cal.ics'):
    assert leak not in log, (leak, log)
os.environ.pop(cr.CALENDAR_URL_ENV_VAR, None)
print('refresh OK')"</automated>
  </verify>

  <acceptance_criteria>
    - The command above exits 0 and prints `refresh OK`.
    - **The throttle genuinely prevents a per-cycle fetch (16-VALIDATION `fetch` row).** The command above drives eleven calls across a five-minute span of simulated 30-second cycles and asserts exactly one transport invocation.
    - **A failed fetch does not erase the window.** The command above asserts that after a transport failure the persisted entries are byte-for-byte the ones the successful cycle wrote, and that `last_synced_at` did not move while `last_attempt_at` did.
    - **The unconfigured path writes nothing.** With the variable unset, no transport call occurs and `last_attempt_at` is still `None`, so configuring the feature does not force a full interval's wait before the first fetch.
    - The throttled path is silent: running twenty throttled cycles with stderr captured produces an empty buffer.
    - The function never raises: driving it against a read-only state directory, a body of random punctuation, a transport returning a redirect loop, and a transport raising, all return a result code rather than propagating.
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); import server.plane.calendar_rules as cr, inspect; s=inspect.signature(cr.refresh_calendar_registry); assert list(s.parameters)==['state_dir','now','transport'] and s.parameters['transport'].default is None"` exits 0.
    - `git diff --stat -- server/poll_loop.py server/plane/colour_rules.py server/plane/render.py companion/` is empty.
  </acceptance_criteria>

  <done>`poll_loop.py` has one function to call per cycle that does no network work when unconfigured or throttled, performs one bounded fetch otherwise, persists a windowed parse on success, keeps the existing window and moves only the attempt timestamp on failure, prints nothing on the dominant path, and never raises.</done>
</task>

<task type="auto">
  <name>Task 3: Extend server/test_calendar_rules.py with the fetch-hardening, secret-leak and refresh-orchestration checks</name>

  <files>server/test_calendar_rules.py</files>

  <read_first>
    - `server/test_calendar_rules.py` as extended by plans 16-01 and 16-03 — its preamble, its `check(name, fn)` accumulator, its fixture helpers, its live `EXPECTED_CHECK_COUNT` and its terminating count comparison.
    - `server/test_enrich.py` — `make_transport()`, the hermetic fake this file's fetch checks must mirror so no live network call ever enters the suite.
    - `server/plane/calendar_rules.py` as extended by Tasks 1 and 2.
    - `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-VALIDATION.md` § "Per-Task Verification Map" — every row whose Plan column reads `fetch`.
    - `.planning/phases/15-per-direction-themes-per-flight-colour-rules-and-roster-link/15-SECURITY.md` — the register's vocabulary and the standard this phase's threat rows are held to.
  </read_first>

  <action>
Extend `server/test_calendar_rules.py` in place. Add a local fake-response class and a local fake-transport factory in `test_enrich.make_transport()`'s shape — parameterised by status, body bytes, headers and a redirect flag, and recording the URLs it was called with. No live network call, no new third-party import.

Write these checks:

**Scheme and address gate (16-VALIDATION `fetch` row 1, T-16-SSRF).** Separate checks for: a non-https scheme is refused; a URL with no hostname is refused; each of loopback IPv4, loopback IPv6, three private IPv4 ranges, the link-local metadata address, and a reserved address is refused; a hostname that fails resolution is refused. Then the two that a hostname-only implementation would pass and this one must not: with the stdlib resolver monkeypatched to return one public and one private address for a public-looking hostname, the URL is refused; and with it monkeypatched to return only public addresses, the same URL is accepted. Restore the resolver in a `finally` so check order can never leak state, exactly as `test_colour_rules.py` does for its process-global cache.

**Redirects (16-VALIDATION `fetch` rows 1 and 2).** A redirect whose `Location` points at a loopback address is refused — proving the gate is re-applied per hop and not only to the configured URL. A redirect with no `Location` returns nothing. A relative `Location` is resolved against the current URL and then re-validated. A chain longer than `CALENDAR_MAX_REDIRECTS` returns nothing, asserted by counting the fake's invocations.

**Size, status and timeout (16-VALIDATION `fetch` row 2, T-16-DOS).** A body one byte over `CALENDAR_MAX_RESPONSE_BYTES` returns nothing. The same oversized body accompanied by a `Content-Length` header declaring one byte still returns nothing — the cap is on what was streamed, not on what was claimed. A body exactly at the cap is accepted. A non-200 final status returns nothing. The transport is invoked with a timeout argument, asserted by having the fake record its second positional argument and checking it equals `CALENDAR_FETCH_TIMEOUT_S`.

**Secret containment (16-VALIDATION `fetch` rows 3 and 4, T-16-SECRET) — the group this plan exists for.** With the environment variable set to a URL carrying a distinctive token, run every failure path in turn — refused scheme, refused address, refused redirect target, hop-limit exhaustion, oversized body, non-200 status, and a transport raising an exception **whose message is the full URL** (the shape several requests exception subclasses take by default) — each with stderr captured. Assert for each that the buffer contains none of: the token, the hostname, the path segment, the query-parameter name. Add one more check asserting the same four substrings are absent from the persisted `calendar_rules.json` after a full successful `refresh_calendar_registry()` cycle.

**Refresh orchestration (16-VALIDATION `fetch` rows on the throttle and the timestamps).** The unconfigured path performs no transport call and writes no timestamp. The throttled path performs no transport call and prints nothing. Eleven calls across simulated 30-second cycles perform exactly one transport call. A failure after a success moves `last_attempt_at`, leaves `last_synced_at`, and leaves the entries on disk unchanged. A success writes a windowed parse of the committed fixture. A successful fetch of a feed with nothing in the window is reported as success with an empty entry list, distinguishable from a failure only by `last_synced_at` having moved — assert both facts in the same check so the distinction is pinned.

**Never raises.** `refresh_calendar_registry()` against a nonexistent state dir, a body of random punctuation, a transport that raises, and a transport that redirects forever, each returns a result code and raises nothing.

Then raise `EXPECTED_CHECK_COUNT` by exactly the number of checks added, **re-derived by running the harness**, and extend the ledger comment noting that phase 16 plan 04 added the fetch-hardening and refresh checks.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_calendar_rules.py</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_calendar_rules.py` exits 0 and prints an `N/N checks pass`-shaped line whose `N` is strictly greater than plan 16-03's `EXPECTED_CHECK_COUNT`.
    - The suite makes no network call: `server/.venv/bin/python3 -c "import ast; t=ast.parse(open('server/test_calendar_rules.py').read()); mods=[n.name for x in ast.walk(t) if isinstance(x,ast.Import) for n in x.names]+[x.module or '' for x in ast.walk(t) if isinstance(x,ast.ImportFrom)]; assert 'requests' not in mods or True; import re; src=open('server/test_calendar_rules.py').read(); assert 'requests.get(' not in src, 'the harness must never call requests.get directly'"` exits 0.
    - The SSRF check is live rather than vacuous: temporarily change `_host_is_safe()` to classify the hostname string instead of the resolved addresses, confirm the harness exits non-zero and that the failing check is the mixed-address one, then restore it.
    - The secret check is live rather than vacuous: temporarily change one failure path to interpolate the caught exception object into its log line, confirm the harness exits non-zero and that the failing check is a secret-containment one, then restore it.
    - The throttle check is live rather than vacuous: temporarily set `CALENDAR_FETCH_INTERVAL_S` to zero, confirm the harness exits non-zero and that the failing check is the one-transport-call-across-eleven-cycles one, then restore it.
    - `grep -c 'total == EXPECTED_CHECK_COUNT' server/test_calendar_rules.py` returns `1`.
    - `scripts/run-all-tests.sh` exits 0, and the whole suite completes within its usual runtime — no check waits on a real socket.
  </acceptance_criteria>

  <done>The harness proves the fetch refuses a private resolved address even behind a public-looking name, re-validates every redirect hop, bounds hops and streamed bytes and time, and that across every failure path the calendar URL and its token reach neither stderr nor the persisted registry — and that the once-per-cycle refresh fetches once per interval, not once per poll.</done>
</task>

</tasks>

## Artifacts this phase produces

Introduced by **this plan** (16-04), all in `server/plane/calendar_rules.py`:

| Kind | Symbol / path |
|---|---|
| constant | `calendar_rules.USER_AGENT` |
| function | `calendar_rules._address_is_public(ip_text)` |
| function | `calendar_rules._host_is_safe(hostname, port=None)` |
| function | `calendar_rules._url_is_safe(url)` |
| function | `calendar_rules.default_calendar_transport(url, timeout)` |
| function | `calendar_rules.fetch_ics(url, timeout=None, transport=None, max_redirects=None, max_bytes=None)` |
| function | `calendar_rules.refresh_calendar_registry(state_dir, now, transport=None)` → `(result_code, registry)` |

No new file, CSS class, route path, env var or flash key is introduced by this plan. `requests` is imported for the first time in this module; it is an already-pinned dependency and no requirements entry changes.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied URL in `skypane.env` → outbound HTTPS request from the VPS | The project's first outbound request to a host the developer did not choose; the VPS's own loopback and private-network services sit behind it. |
| third-party redirect response → the next request the VPS makes | A `Location` header is attacker-controlled input that decides where the next connection goes. |
| third-party response body → process memory and the poll oneshot's time budget | An unbounded or slow-drip body is a denial-of-service vector against a 30-second oneshot. |
| caught exception object → `journalctl -u skypane-poll` | The exception's default string form carries the request URL, which carries the subscription token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-SSRF | Tampering / Information Disclosure | `_url_is_safe()`, `_host_is_safe()`, `fetch_ics()`'s redirect loop | high | mitigate | Scheme restricted to `https`; the hostname resolved explicitly and **every** returned address classified through the stdlib address module, refusing on private, loopback, link-local, reserved, multicast or unspecified — so a name that resolves to a mix of public and private answers is refused rather than accepted. Automatic redirect following is disabled and each `Location` target goes back through the identical gate before it is followed, with a bounded hop count. Proved by Task 3's mixed-address check (which a hostname-only implementation fails), the loopback-redirect check, and the AST criterion asserting every `requests` call disables redirect following. |
| T-16-SECRET | Information Disclosure | every logging path in the fetch and refresh functions | high | mitigate | No log line in this module interpolates the caught exception object or the URL, on any path including success; only the exception type name and a fixed description are emitted. This is a deliberate, commented divergence from `detect.py`'s neighbouring caller-catch idiom, which does interpolate the exception — safe there only because those URLs carry no secret. Proved by Task 3's seven-failure-path containment group, which forces an exception whose message *is* the full secret URL and asserts four distinct substrings are absent from stderr, plus the equivalent assertion against the persisted registry file. Backed by the mutation criterion that fails if any path is changed to interpolate. |
| T-16-DOS | Denial of Service | oversized body, slow feed, redirect loop, per-cycle fetching | medium | mitigate | Four independent bounds, none sufficient alone: the body is capped while it is streamed, with the header-declared length never consulted (pinned by a zero-count grep for that header name and by a test whose oversized body declares a length of one); the request carries a hard timeout asserted from the transport's recorded arguments; the redirect hop count is bounded and asserted by invocation counting; and `refresh_calendar_registry()` gates on the durable attempt timestamp so the operator's host is contacted at most once per `CALENDAR_FETCH_INTERVAL_S` rather than on every 30-second cycle. |
| T-16-INPUT | Tampering | the fetched body entering `parse_ics_events()` | high | transfer | Fully mitigated upstream in plan 16-01: the parser's four compiled allowlists, its never-raising contract and its entry cap are what stand between an untrusted body and durable state, and plan 16-03 re-applies the identical gates on every read of the persisted file. Transferred rather than re-implemented here — a second validation layer at the fetch tier would be the one place in this module that diverges from the established single-gate-per-tier discipline. Task 3's never-raises group confirms the transfer holds through this tier by driving a punctuation body end to end. |
| T-16-PRIV | Information Disclosure | the fetched body in process memory and on disk | medium | mitigate | The body is never logged, never echoed, and never persisted raw — only `parse_ics_events()`'s five-field entries, further reduced by D-03's window, reach disk. Proved by Task 3's assertion that the persisted registry after a full successful cycle contains none of the URL's identifying substrings and, from plan 16-01, none of the fixture's schedule text. |
| T-16-SC | Tampering | package installs | n/a | accept | This plan installs zero packages. `requests` is imported for the first time in this module but is already pinned in `server/requirements.txt` at 2.34.2 and already used by `detect.py` and `enrich.py`. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable for the whole phase, and the empty-requirements-diff criterion pins it. |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_calendar_rules.py` exits 0 with a raised ledger.
- `scripts/run-all-tests.sh` exits 0 with no measurable runtime increase — no check waits on a real socket.
- **D-07 standing gate (checked in every plan of this phase):** `git diff --stat -- server/plane/render.py` is empty and `server/.venv/bin/python3 server/test_render.py` exits 0 with its `EXPECTED_CHECK_COUNT` unedited.
- **No new dependency (checked in every plan of this phase):** `git diff -- server/requirements.txt server/requirements-dev.txt` is empty.
- `git diff --stat -- companion/ server/poll_loop.py server/device_config.py server/plane/colour_rules.py server/plane/detect.py server/plane/enrich.py` is empty for this plan's commits — this plan runs in the same wave as 16-05 and must not touch anything that plan owns.
- Manual, one-off, at review time: `grep -rn "$SKYPANE_CALENDAR_ICS_URL" state_dir/` on the deployed host returns nothing after a real fetch cycle. Recorded here because `16-VALIDATION.md` names it as the manual half of the secret-handling row; it is not a phase gate.
</verification>

<success_criteria>
- The fetch refuses every non-https scheme, every private or loopback resolved address, and a public-looking hostname that resolves to a mix including a private address.
- Every redirect hop is re-validated through the same gate, and the hop count is bounded.
- The response size cap is enforced on the streamed bytes with the declared length never consulted, and the request carries a hard timeout.
- Across seven distinct failure paths — including a transport exception whose message is the full URL — neither the token, the host, the path nor the query name reaches stderr or the persisted registry.
- Eleven simulated 30-second poll cycles produce exactly one transport call, and the throttled path prints nothing.
- A failed fetch moves only `last_attempt_at` and leaves the existing rolling window intact.
</success_criteria>

<output>
Create `.planning/phases/16-calendar-linked-flight-highlighting-a-connected-calendar-sou/16-04-SUMMARY.md` when done.
</output>
