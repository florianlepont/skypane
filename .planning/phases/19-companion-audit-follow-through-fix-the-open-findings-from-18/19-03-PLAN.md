---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - companion/pages/history_page.py
  - companion/static/copy-button.js
  - companion/static/style.css
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-06]
user_setup: []

must_haves:
  truths:
    - "D-19 (A-36): the Flights table is 6 columns, not 7 — the Runway column is gone, its value survives in the mobile card's More details and in the row's title attribute"
    - "D-19 (A-36): a keyboard user can reach and scroll the Flights table — the scroller is focusable and has an accessible name"
    - "D-19 (A-36): the Timestamp column shows a short local clock, with the full ISO still in the title attribute"
    - "D-20 (A-37): a sighted user sees a transient 'Copied' state on the button itself for about 1.5 s"
    - "D-20 (A-37): each copy button's accessible name names the row's callsign, so 50 rows no longer share one name"
    - "D-20 (A-37): success is only reported when the clipboard write actually succeeded"
  artifacts:
    - path: "companion/pages/history_page.py"
      provides: "6-column table, focusable scroller, clock-only timestamp cell, per-row copy labels"
      contains: "tabindex=\"0\""
    - path: "companion/static/copy-button.js"
      provides: "visible transient feedback gated on a real clipboard success"
      contains: "execCommand"
  key_links:
    - from: "companion/static/copy-button.js"
      to: "companion/pages/history_page.py"
      via: "the [data-copy-value] button / [data-copy-feedback] next-sibling span contract"
      pattern: "data-copy-feedback"
    - from: "companion/pages/history_page.py"
      to: "companion/layout.py"
      via: "parse_iso + local_clock_text for the shortened Timestamp cell"
      pattern: "layout\\.local_clock_text"
---

<objective>
Close A-36 (D-19) and A-37 (D-20) on the Flights page: make the wide table fit
and reachable, shorten the timestamp, and make the copy buttons honest —
individually named, visibly confirming, and only claiming success when the
clipboard write actually happened.

Purpose: at 1280px the 7-column table needed 1,305px inside an 880px column, so
it scrolled behind a 12px shadow that keyboard users could not reach at all; and
the copy confirmation was invisible to sighted users while all 50 rows shared one
accessible name.
Output: a 6-column table with a focusable, named scroller, a clock-only Timestamp
cell, and a copy button that swaps its own label to "Copied" for 1.5 s only on
real success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
Timestamp helpers — `companion/layout.py`:
  def concise_timestamp_html(ts, now_ts, fallback="no reading yet")
      # '<span class="mono" title="{full ISO}">{HH:MM} UTC ({relative})</span>'
      # the relative-age suffix is what makes this column too wide.
  def parse_iso(ts) -> datetime | None
  def local_clock_text(parsed, now_parsed=None) -> "HH:MM" | "D Mon HH:MM"
      # takes DATETIMES, not ISO strings — parse first. Europe/Paris (Phase 18 D-06).
  # NOTE: companion/layout.py is owned by plan 19-04 in this same wave. This plan
  # must NOT edit layout.py — compose the short cell inside history_page.py from
  # the three existing helpers above.

Table builders — `companion/pages/history_page.py`:
  _HEADERS = ("Timestamp","Callsign","Type","Route","State","Corroboration","Runway")  # :132
  def _history_table_html(formatted_rows, now=None)   # :668 — hand-rolled <td>s,
      # emits '<div class="data-table-wrap"><table class="data-table">…'
      # and '<tr class="{row|row-alt}" data-filter-text="…" data-filter-group="{i}">'
  def _history_cards_html(formatted_rows, now=None)   # the <960px card list
  def _copy_button_html(value, label)                 # :539 — escapes both args
  _COPY_CALLSIGN_LABEL / _COPY_HEX_LABEL / _COPY_TIMESTAMP_LABEL  # :194-196

Copy script — `companion/static/copy-button.js`:
  fallbackCopy(value)     # :38 — calls document.execCommand("copy") and DISCARDS
                          #       its boolean return value (the A-37 defect)
  showFeedback(button)    # :55 — writes FEEDBACK_TEXT into the next-sibling
                          #       [data-copy-feedback] span (which is
                          #       .visually-hidden, hence invisible to sighted users)
  handleClick(button)     # :66 — Clipboard API with a .then(resolve, reject)
                          #       split, then the fallback path
  # Standing constraint: no innerHTML, no other HTML-writing sink, ES5-safe
  # subset only (no let/const/arrow/backtick). Enforced by harness checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the Runway column, name and focus the scroller, shorten the Timestamp (D-19)</name>
  <files>companion/pages/history_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/history_page.py lines 120-200 (`_HISTORY_UNAVAILABLE_TEXT`,
      `_HEADERS` and its "7 entries, left-to-right order unchanged" comment, the
      three `_COPY_*_LABEL` constants, the cell class constants)
    - companion/pages/history_page.py lines 660-760 (`_history_table_html()` in
      full — the hand-rolled cells tuple, the `<tr>` open tag with
      `data-filter-text`/`data-filter-group`, and the `.data-table-wrap` wrapper)
    - companion/pages/history_page.py lines 760-860 (`_history_cards_html()` —
      confirm the mobile card's "More details" already carries the runway, so
      nothing is lost by dropping the desktop column)
    - companion/layout.py lines 595-665 (`concise_timestamp_html()`,
      `parse_iso()`, `local_clock_text()` — and the LOCAL_TZ constant)
    - companion/test_view_pages.py — grep for every check mentioning `_HEADERS`,
      `Runway`, a column count, or `data-table-wrap`, and read each in full
      before editing
    - .claude/skills/sketch-findings-skypane/SKILL.md (the "Data and tables"
      paragraph: sticky header scoped to `.data-table-wrap`, the retired zebra
      striping left as inert markup, the `.history-cards ~ .data-table-wrap`
      breakpoint toggle — none of which this task may disturb)
  </read_first>
  <action>
    In `companion/pages/history_page.py`:

    1. Drop `"Runway"` from `_HEADERS`, leaving 6 entries in the same
       left-to-right order. Update the comment above it to say 6 entries and to
       record WHY (A-36/D-19: one runway is tracked at a time, so the column
       carried the same value on every row while costing ~90px of a 1,305px
       table; the value now lives in the row's `title` and in the mobile card's
       More details).
    2. In `_history_table_html()`, delete the final
       `"<td>%s</td>" % escape_html(row["tracked_runway"])` cell, and add a
       `title="%s"` attribute to the `<tr>` open tag carrying the runway value,
       escaped through `escape_html()` at the point of interpolation. Keep
       `class`, `data-filter-text` and `data-filter-group` exactly as they are —
       `data-filter-group` in particular is load-bearing for
       `companion/static/list-filter.js`'s logical-row counting.
    3. Give the `.data-table-wrap` wrapper `tabindex="0"`, `role="region"` and an
       `aria-label` naming what it scrolls (e.g. "Recent flights table,
       scrollable"), so a keyboard user can focus it and arrow-scroll to the last
       columns. Add a module-level constant for that label rather than inlining
       the string, matching this file's constants-at-the-top convention.
    4. Add a private `_clock_cell_html(raw_ts, now)` to this file that returns
       `'<span class="mono" title="{escaped full ISO}">{escaped local clock}</span>'`,
       built from `layout.parse_iso()` + `layout.local_clock_text()` with
       `now_parsed` supplied so a same-day row renders bare "HH:MM" and an older
       row renders "D Mon HH:MM" (Europe/Paris, Phase 18 D-06). Degrade exactly
       the way `concise_timestamp_html()` does: a falsy `raw_ts` yields the same
       escaped fallback string that function uses, and an unparseable `raw_ts`
       yields a span with the raw value in both the title and the visible text —
       never a raise. Use it for the desktop table's Timestamp cell in place of
       `layout.concise_timestamp_html(row["raw_ts"], now)`. Do NOT edit
       `companion/layout.py`: plan 19-04 owns that file in this same wave.
    5. Leave `_history_cards_html()`'s own timestamp rendering alone — the mobile
       card is not width-constrained in the same way and its relative-age suffix
       is the only age cue it has.

    In `companion/test_view_pages.py`: retarget in place every check that pinned
    7 columns (same check name, expectation updated to 6), plus any check keyed on
    the Runway `<td>`'s position. Add new checks for: the rendered `<tr>` carrying
    a `title` attribute whose value is the row's runway; the `.data-table-wrap`
    carrying `tabindex="0"` and a non-empty `aria-label`; the desktop Timestamp
    cell containing no " ago" / relative-age substring while still carrying the
    full ISO in a `title`; and `"Runway"` being absent from the rendered
    `<thead>` while still present in the rendered mobile-card markup. Then
    re-derive `EXPECTED_CHECK_COUNT` by running the harness and appending a NEW
    last assignment citing 19-03-PLAN.md — never by arithmetic.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import history_page as h;print(len(h._HEADERS));print('Runway' in h._HEADERS)"`
      prints `6` then `False`
    - `grep -c 'tabindex="0"' companion/pages/history_page.py` outputs at least `1`
    - `grep -c "def _clock_cell_html" companion/pages/history_page.py` outputs `1`
    - `grep -c "layout.local_clock_text" companion/pages/history_page.py` outputs at least `1`
    - `git diff --stat companion/layout.py` shows no change
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 and its
      final line reports `M/M checks pass` matching the new last
      `EXPECTED_CHECK_COUNT`
  </acceptance_criteria>
  <done>The Flights table is 6 columns with a focusable, named scroller and a clock-only Timestamp; the runway value survives in the row title and the mobile card; harness green.</done>
</task>

<task type="auto">
  <name>Task 2: Name every copy button for its own row and only report real success (D-20)</name>
  <files>companion/pages/history_page.py, companion/static/copy-button.js, companion/test_view_pages.py</files>
  <read_first>
    - companion/static/copy-button.js (the whole file, 97 lines — the header
      comment's standing no-HTML-writing-sink / ES5-subset constraint, and all
      three of `fallbackCopy()`, `showFeedback()`, `handleClick()`)
    - companion/pages/history_page.py lines 190-200 and 530-600
      (`_COPY_*_LABEL`, `_copy_button_html()`, `_callsign_hex_cell()` and its
      three documented branches)
    - companion/pages/history_page.py lines 800-840 (the mobile card's three
      `_copy_button_html()` call sites, including the timestamp one)
    - companion/test_view_pages.py — grep for `Copy callsign`, `Copy hex ID`,
      `Copy timestamp` and `data-copy-feedback` and read each pinned check in full
  </read_first>
  <action>
    In `companion/pages/history_page.py`, turn the three bare label constants into
    templates that name the row, keeping the existing constant names so no
    unrelated reference breaks:
      `_COPY_CALLSIGN_LABEL = "Copy callsign %s"`
      `_COPY_HEX_LABEL = "Copy hex ID for %s"`
      `_COPY_TIMESTAMP_LABEL = "Copy timestamp for %s"`
    and add a small private helper that formats a template against the row's
    callsign, substituting a documented placeholder (e.g. the existing
    `NO_CALLSIGN_NOTE_TEXT`-adjacent wording, or the hex value when the callsign
    is absent) so the accessible name is never left with a dangling "for ".
    Update all five `_copy_button_html(...)` call sites (three in
    `_callsign_hex_cell()`'s branches, three in `_history_cards_html()` — read
    them and count exactly) to pass the formatted label. `_copy_button_html()`
    itself needs no signature change: it already escapes `label` through
    `escape_html()`, so there is no new escaping path.

    In `companion/static/copy-button.js`:
      - `fallbackCopy(value)` must `return document.execCommand("copy");` from
        inside its existing `try`, with the `finally` that removes the textarea
        preserved, so the boolean reaches the caller instead of being discarded.
      - `handleClick(button)` must call `showFeedback(button)` ONLY on real
        success: on the Clipboard-API path, only in the resolve callback; on the
        reject callback and the no-Clipboard-API path, only when
        `fallbackCopy(value)` returned true. When the copy genuinely failed,
        write nothing at all — do not show a false "Copied". Add a short comment
        recording that A-37's third half was exactly this discarded return value.
      - Keep the file inside its ES5-safe subset and its no-HTML-writing-sink
        constraint: `var`/`function` only, no `let`/`const`/arrow/backtick, and
        no `innerHTML`/`insertAdjacentHTML`/`document.write`/`eval`.

    Add to `companion/test_view_pages.py`: a check asserting the rendered Flights
    page contains at least two DIFFERENT `aria-label` values among its copy
    buttons (the 50-identical-names defect, closed); a check asserting each
    rendered copy button's `aria-label` contains its own row's callsign; a check
    reading `companion/static/copy-button.js` from disk and asserting it contains
    `return document.execCommand` (success is propagated) and none of
    `innerHTML`, `insertAdjacentHTML`, `document.write`, `eval(`, `let `,
    `const `, `=>`, backtick. Re-derive `EXPECTED_CHECK_COUNT` by running the
    harness and appending a new last assignment citing 19-03-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return document.execCommand" companion/static/copy-button.js` outputs `1`
    - `grep -c "let \|const \|=>" companion/static/copy-button.js` outputs `0`
    - `grep -c "innerHTML\|insertAdjacentHTML\|document.write\|eval(" companion/static/copy-button.js` outputs `0`
    - `grep -c "_COPY_CALLSIGN_LABEL = \"Copy callsign %s\"" companion/pages/history_page.py` outputs `1`
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 with
      `M/M checks pass`
  </acceptance_criteria>
  <done>Every copy button names its own row's callsign; a failed clipboard write reports nothing; the script stays ES5-safe and sink-free.</done>
</task>

<task type="auto">
  <name>Task 3: Make the copy confirmation visible for 1.5 s (D-20)</name>
  <files>companion/static/copy-button.js, companion/static/style.css, companion/test_view_pages.py</files>
  <read_first>
    - companion/static/copy-button.js (as modified by Task 2 — `showFeedback()`
      and `FEEDBACK_RESET_MS`)
    - companion/pages/history_page.py `_copy_button_html()` (the button plus its
      immediate-next-sibling `data-copy-feedback` span, already
      `role="status" aria-live="polite"` and `.visually-hidden`)
    - companion/static/style.css — the `.copy-btn` block (~line 1603-1650),
      including its `::before` synthesized 44x44 hit area, and the
      `.visually-hidden` utility rule
    - companion/static/style.css `[data-copy-value]` block (~line 4200-4220 — the
      print/reduced rule that keys on the attribute, not the class; read its
      comment so you do not break its discriminator)
    - .claude/skills/sketch-findings-skypane/SKILL.md (the touch-target register
      entry for `.copy-btn`, whose relocated 44x44 `::before` hit area must
      survive; and "no new tokens, no new accent consumer")
  </read_first>
  <action>
    Implement D-20's visible transient state as a TEXT SWAP on the button itself,
    per D-20's own wording, with the existing hidden live-region span kept for
    screen-reader announcement:
      - in `copy-button.js`, change `FEEDBACK_RESET_MS` to `1500` and extend
        `showFeedback(button)` to also, on success only: remember the button's
        current `textContent`, write the string "Copied" into the button via
        `textContent`, add a `copy-btn--copied` class via `classList` (with a
        `setAttribute("class", …)` fallback in the same defensive style
        `companion/static/battery-trend.js` already uses for SVG elements), and
        restore both the original text and the class in the same
        `window.setTimeout` that already clears the hidden span. Use one timer,
        not two, so the visible and announced states can never disagree. Guard
        against a double-click re-entrancy resetting the saved label to "Copied"
        by storing the original text on the first swap only.
      - the button's visible content today is an SVG icon
        (`layout.icon_html("icon-copy")`); writing `textContent` replaces it, and
        restoring `textContent` cannot restore an SVG child. Therefore the icon
        must be preserved by markup, not by JS: in
        `history_page._copy_button_html()`, wrap the icon in a
        `<span class="copy-btn__icon" aria-hidden="true">` and add an empty
        `<span class="copy-btn__label"></span>` sibling INSIDE the button, and
        have the script write into `.copy-btn__label` only — never into the
        button element itself. This keeps the no-HTML-writing-sink rule intact
        (only `textContent` on a leaf span) and keeps the icon untouched.
      - in `companion/static/style.css`, add a `.copy-btn__label` rule (empty by
        default, so the resting button is icon-only exactly as today) and a
        `.copy-btn--copied` rule that reveals the label text using the EXISTING
        `--color-status-ok` token and the existing label voice values already in
        the file — no new custom property, no new colour literal, no accent use.
        The rule must not change the button's box size in a way that breaks the
        `::before` 44x44 hit area or the table's column widths; prefer revealing
        the label inside the existing padding box.

    Add to `companion/test_view_pages.py`: a check that the rendered copy button
    markup contains exactly one `copy-btn__label` span and one
    `copy-btn__icon` span per button and that the `data-copy-feedback` sibling
    span still immediately follows the button (the contract `copy-button.js`
    depends on); a check reading `copy-button.js` from disk asserting it
    references the literal `copy-btn__label` and `copy-btn--copied` strings and
    contains `1500`; and a cross-file check reading `style.css` from disk
    asserting both class names are styled there (the same index-plus-window
    technique the file's existing cross-file CSS guards use — never a regex CSS
    parser). Re-derive `EXPECTED_CHECK_COUNT` by running the harness and
    appending a new last assignment citing 19-03-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "copy-btn__label" companion/static/copy-button.js` outputs at least `1`
    - `grep -c "copy-btn__label\|copy-btn--copied" companion/static/style.css` outputs at least `2`
    - `grep -c "1500" companion/static/copy-button.js` outputs at least `1`
    - `grep -c "innerHTML\|insertAdjacentHTML\|document.write\|eval(\|let \|const \|=>" companion/static/copy-button.js` outputs `0`
    - `grep -c "\-\-color-status-ok" companion/static/style.css` is greater than before this task, and `git diff companion/static/style.css | grep -c "^+.*--[a-z-]*:"` outputs `0` (no new custom property is declared)
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 with `M/M checks pass`
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0 (the colour-separation contract still holds)
  </acceptance_criteria>
  <done>A successful copy swaps a visible "Copied" label beside the icon for 1.5 s using existing tokens; the hidden live region still announces it; the icon and the 44x44 hit area are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite `runway_events` -> Flights render | callsign, hex, route and timestamp are values the poll loop wrote from ADS-B feeds; a hostile string must not escape the escaping choke point |
| server-rendered attribute values -> browser | the new `<tr title>`, `aria-label` and `data-copy-value` attributes all carry row data into attribute context |
| DOM -> clipboard | the copy button reads a server-escaped attribute and writes it to the OS clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering (XSS via attribute injection) | the new `<tr title="…">`, the per-row `aria-label`, the scroller's `aria-label` | mitigate | every interpolated value passes `layout.escape_html()` at the point of interpolation; `_copy_button_html()` already escapes both of its arguments, and the new label formatting happens BEFORE that escape, never after. Proven by Task 1/2's render-level checks plus the file's existing hostile-value checks |
| T-19-10 | Tampering (DOM sink) | `companion/static/copy-button.js` | mitigate | the script writes only `textContent` on a leaf `<span>` and only toggles a class; the forbidden-sink grep in Tasks 2-3's acceptance criteria keeps `innerHTML`/`insertAdjacentHTML`/`document.write`/`eval(` at zero |
| T-19-16 | Spoofing (false success signal) | `copy-button.js` `handleClick()` | mitigate | feedback is shown only when `execCommand` returned true or the Clipboard promise resolved; a failed write shows nothing, so the user is never told their data was copied when it was not (A-37) |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; no build step, no bundler, no new JS dependency |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_view_pages.py` -> `M/M checks pass`, exit 0
- `server/.venv/bin/python3 companion/test_contrast_check.py` -> `36/36`, exit 0
- `scripts/run-all-tests.sh` -> no NEW failures beyond the five pre-existing
  root-sandbox artifacts
- `<human-check>` (end-of-phase, per `workflow.human_verify_mode`): at 1280px the
  Flights table fits its column without horizontal scroll, or scrolls with a
  visible focus ring when tabbed to; clicking a copy button shows "Copied" beside
  the icon for about 1.5 s and the row's own callsign is announced.
</verification>

<success_criteria>
- `len(history_page._HEADERS) == 6` and `"Runway" not in _HEADERS`
- Every `<tr>` in the desktop table carries the runway in its `title`
- `.data-table-wrap` is focusable and has an accessible name
- The desktop Timestamp cell shows a local clock with no relative-age suffix and
  the full ISO in its `title`
- No two copy buttons on a multi-row render share an `aria-label`
- A failed clipboard write produces no feedback at all
- `EXPECTED_CHECK_COUNT` re-derived by running the harness
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-03-SUMMARY.md` when done.
</output>
