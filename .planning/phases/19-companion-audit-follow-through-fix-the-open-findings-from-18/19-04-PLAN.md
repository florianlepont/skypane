---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 04
type: execute
wave: 2
depends_on: ["19-02"]
files_modified:
  - companion/static/poll-cooldown.js
  - companion/app.py
  - companion/layout.py
  - companion/pages/config_page.py
  - companion/test_companion_app.py
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-01]
user_setup: []

must_haves:
  truths:
    - "D-18 (A-35): every response, redirects included, carries the hardening headers and a Content-Security-Policy"
    - "D-18 (A-35): the app serves no inline <script> anywhere — the poll countdown and the disable-on-submit affordance both run from companion/static/poll-cooldown.js"
    - "D-18 (A-35): POST /ui-theme and POST /logout require a valid session cookie"
    - "D-18 amended: the CSP is default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; form-action 'self'; frame-ancestors 'none' — script-src stays strict, style-src allows the server-controlled theme swatch attributes"
  artifacts:
    - path: "companion/static/poll-cooldown.js"
      provides: "the externalized cooldown countdown AND disable-on-submit affordance, data-attribute driven"
      min_lines: 60
    - path: "companion/app.py"
      provides: "CSP header, hardening headers on redirects, session-gated /ui-theme and /logout, the poll-cooldown static route"
      contains: "Content-Security-Policy"
    - path: "companion/layout.py"
      provides: "POLL_COOLDOWN_SCRIPT_SRC and its page_shell() script tag"
      contains: "POLL_COOLDOWN_SCRIPT_SRC"
  key_links:
    - from: "companion/app.py"
      to: "companion/layout.py"
      via: "POLL_COOLDOWN_SCRIPT_ROUTE must equal POLL_COOLDOWN_SCRIPT_SRC (duplicated-not-imported contract)"
      pattern: "POLL_COOLDOWN_SCRIPT_(ROUTE|SRC)"
    - from: "companion/static/poll-cooldown.js"
      to: "companion/pages/config_page.py"
      via: "data-cooldown / data-cooldown-text-id / data-cooldown-template / data-submit-pending attributes on the poll button"
      pattern: "data-cooldown"
    - from: "companion/app.py redirect()"
      to: "_send_hardening_headers()"
      via: "redirects now carry the same headers as HTML responses"
      pattern: "_send_hardening_headers"
---

<objective>
Close A-35 (D-18): add a Content-Security-Policy to every response, make
redirects carry the hardening headers they currently skip, session-gate the two
un-gated state-changing POST routes, and remove the app's last two inline
`<script>` elements so `script-src 'self'` can be strict.

Purpose: this is an authenticated admin panel reachable from the public
internet. It has no CSP at all today; its redirects send zero hardening headers;
and `POST /ui-theme` and `POST /logout` are reachable with no session.
Output: a new `companion/static/poll-cooldown.js` wired in through the six-point
static-script contract, a CSP + redirect-hardening change in `companion/app.py`,
and the two ungated routes gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md

<interfaces>
The six-touch-point static-script contract (verified live; copy it exactly):
  1. companion/app.py:108-135   — `<NAME>_SCRIPT_ROUTE = "/static/<file>.js"`
  2. companion/app.py           — `_<NAME>_JS_PATH = os.path.join(_HERE, "static", "<file>.js")`
  3. companion/app.py:1322-1333 — `def _serve_<name>_script(self): return self._serve_script_file(_<NAME>_JS_PATH)`
  4. companion/app.py:1915-1921 — one `elif path == <NAME>_SCRIPT_ROUTE: return self._serve_<name>_script()` in do_GET()
  5. companion/layout.py:113-137 — `<NAME>_SCRIPT_SRC = "/static/<file>.js"` with the
       "duplicated rather than imported" comment every sibling carries
  6. companion/layout.py:1110-1146 — one more `'<script src="%s" defer></script>'`
       line in page_shell()'s format string AND one more entry in its value tuple
  app.py cannot import layout.py's constant (import cycle); the two strings are
  retyped in both files and pinned equal by a dedicated harness check. Do not
  try to collapse this into a shared module — it is a deliberate, tested design.

Current header helpers — `companion/app.py`:
  def _send_hardening_headers(self)   # :855 — sends X-Content-Type-Options,
                                      #        X-Frame-Options, Referrer-Policy.
                                      #        NO CSP today.
  def send_html(self, code, html_str) # :868 — calls _send_hardening_headers()
  def send_bytes(...)                 # :882 — calls _send_hardening_headers()
  def redirect(self, location, set_cookie=None)  # :906 — calls it NOT AT ALL

Ungated routes — `companion/app.py:2252-2256`, against the gate-then-dispatch
pattern every sibling already uses at :2232-2235:
  if path == SETTINGS_ROUTE:
      if not self.require_session():
          return None
      return self._handle_settings_post()

The two inline scripts to externalize — `companion/pages/config_page.py`:
  def _poll_cooldown_script(cooldown_remaining)  # :1192 — the ticking countdown
  def _poll_submit_script()                      # :1245 — the "Polling…" disable
  def poll_trigger_section(cooldown_remaining)   # :1293 — emits one or the other
  POLL_TRIGGER_BUTTON_ID = "poll-trigger-btn"    # :291
  POLL_COOLDOWN_TEXT_ID  = "poll-cooldown-text"  # :292
  POLL_COOLDOWN_HELPER_TEXT = "Poll triggered recently — try again in {n}s."  # :285
  POLL_COOLDOWN_TEMPLATE_TOKEN = "__N__"         # :308
  POLL_SUBMIT_PENDING_TEXT = "Polling…"          # :298

The two pinned checks that WILL break and must be retargeted in place —
`companion/test_config_page.py:1603` and `:1620`:
  _poll_cooldown_script_has_no_forbidden_sink()   # regex-extracts <script>…</script>
  _poll_submit_script_has_no_forbidden_sink()     # from poll_trigger_section()'s output
  with _FORBIDDEN_SCRIPT_SINKS = ("innerHTML","outerHTML","insertAdjacentHTML",
  "document.write","eval(","fetch(","XMLHttpRequest") and
  _REQUIRED_SCRIPT_OPERATIONS = ("use strict","textContent","removeAttribute",
  "setInterval","clearInterval").

The seven theme-swatch inline style attributes that the CSP must NOT break —
`companion/pages/config_page.py:_theme_chip_grid_html()` (3 of them) plus the
single-theme and calendar-section `theme-swatch__chip` pairs. All are
server-controlled hex values from the fixed 18-member `device_config.THEMES`
registry, never user input. This is why the amended CSP carries
`style-src 'self' 'unsafe-inline'` and NOT a class-per-theme refactor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize both inline poll scripts into companion/static/poll-cooldown.js</name>
  <files>companion/static/poll-cooldown.js, companion/app.py, companion/layout.py, companion/pages/config_page.py, companion/test_companion_app.py, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py lines 1174-1380 (`_js_literal()`,
      `_poll_cooldown_script()`, `_poll_submit_script()`,
      `poll_trigger_section()` — read all four docstrings in full; they state the
      behaviour that must survive byte-for-byte and the reasons behind the `> 0`
      versus truthy comparison)
    - companion/static/dirty-state.js lines 1-55 (the header-comment convention
      and the guard-clause idiom to copy for a new static file)
    - companion/static/copy-button.js lines 1-40 (the closest structural analog:
      data-attribute driven, `setTimeout`-based transient state, ES5-safe)
    - companion/app.py lines 108-140 (the seven existing `*_SCRIPT_ROUTE` and
      `_*_JS_PATH` constants), lines 1315-1340 (a `_serve_*_script` delegate),
      lines 1905-1930 (the `do_GET()` static dispatch chain)
    - companion/layout.py lines 110-140 (the seven `*_SCRIPT_SRC` constants and
      their duplicated-not-imported comments) and lines 1100-1150 (`page_shell()`'s
      script-tag format string and its value tuple)
    - companion/test_companion_app.py lines 2540-2600 (`_static_script_public()`,
      `_four_new_static_routes_dom_contract_guard()`) and lines 2600-2690
      (`_panel_lookup_script_es5_safe_and_no_html_write()` and
      `_panel_lookup_script_route_src_agree()`) — the three checks every new
      static script gets
    - companion/test_config_page.py lines 1588-1645 (the two pinned inline-script
      sink checks that this task retargets)
  </read_first>
  <action>
    Create `companion/static/poll-cooldown.js` containing BOTH behaviours as two
    guarded IIFE bodies (or one IIFE with two guarded blocks), ported from
    `_poll_cooldown_script()` and `_poll_submit_script()` without re-deriving
    either: the countdown decrements once per second, clears its interval at
    zero, calls `btn.removeAttribute("disabled")` and clears the cooldown
    paragraph's `textContent`; the submit affordance attaches a `submit` listener
    on `btn.form` that sets `btn.disabled = true` and swaps
    `btn.textContent` to the pending label. 19-RESEARCH.md Assumption A3 is
    load-bearing here: moving only the countdown would let `script-src 'self'`
    silently strip the submit affordance.

    Values that used to be Python-interpolated through `_js_literal()` now come
    from data attributes on the poll button, which `poll_trigger_section()` emits:
      `data-cooldown` — the server-computed remaining seconds (absent or "0" on
        the enabled branch),
      `data-cooldown-text-id` — the id of the paragraph to write into,
      `data-cooldown-template` — `POLL_COOLDOWN_HELPER_TEXT` already formatted
        with `POLL_COOLDOWN_TEMPLATE_TOKEN`,
      `data-cooldown-token` — the token to `String.prototype.replace`,
      `data-submit-pending` — `POLL_SUBMIT_PENDING_TEXT`.
    Every one of those attribute values must be written through
    `escape_html()` at the point of interpolation in `poll_trigger_section()`.
    In the script, read them with `getAttribute()`, coerce the countdown with
    `parseInt(raw, 10)` and bail on `isNaN` or `<= 0` — preserving the existing
    `> 0`-not-truthy contract so a negative value cannot natively disable the
    button while the script inertly no-ops.

    Give the file the house header comment: name D-18/A-35 as the reason it
    exists, state the ES5-safe subset constraint (no `let`/`const`/arrow/
    backtick), name `POLL_COOLDOWN_SCRIPT_ROUTE` as the route it is served
    from, and restate the standing no-`innerHTML`/no-other-HTML-writing-sink
    constraint. Guard-clause first: look the button up by id and `return`
    immediately when it is absent, since this script is served to every page and
    only one page renders the markup.

    Delete `_poll_cooldown_script()` and `_poll_submit_script()` from
    `config_page.py` and remove their calls from `poll_trigger_section()`, so
    that function's output contains ZERO `<script>` elements on both branches.
    Leave `_js_literal()` in place only if another caller still needs it —
    grep first; if it has no remaining caller, delete it and say so in the
    SUMMARY rather than leaving dead code.

    Wire the new file through all six touch points listed in `<interfaces>`:
    `POLL_COOLDOWN_SCRIPT_ROUTE = "/static/poll-cooldown.js"` and
    `_POLL_COOLDOWN_JS_PATH` in `app.py`, a `_serve_poll_cooldown_script()`
    delegate, one `do_GET()` dispatch line, `POLL_COOLDOWN_SCRIPT_SRC` in
    `layout.py` with the sibling comment, and an eighth
    `<script src="%s" defer></script>` line plus tuple entry in `page_shell()`.

    Harness work:
      - in `companion/test_companion_app.py`, add the three checks every static
        script gets, copying the existing shapes: `_static_script_public(
        "/static/poll-cooldown.js")`; an ES5/forbidden-token check on the file's
        source read from disk (banning `let `, `const `, `=>`, backtick,
        `innerHTML`, `outerHTML`, `insertAdjacentHTML`, `document.write`,
        `eval(`, `fetch(`, `XMLHttpRequest`) that also asserts the presence of
        `use strict`, `textContent`, `removeAttribute`, `setInterval`,
        `clearInterval` and `addEventListener`; and a route/src agreement check
        in `_panel_lookup_script_route_src_agree()`'s single-constant shape.
      - in `companion/test_config_page.py`, RETARGET the two pinned inline-script
        checks in place, keeping their names recognisable and recording the
        retarget in a comment: each must now assert that
        `poll_trigger_section(17)` and `poll_trigger_section(0)` contain NO
        `<script` substring at all, and that the required data attributes are
        present with the right values on the respective branch. Move the
        forbidden-sink/required-operation assertions onto the static file's
        source (read from disk), so the coverage those checks provided is
        preserved rather than dropped.
      - re-derive `EXPECTED_CHECK_COUNT` in BOTH harnesses by running each one,
        reading its printed "N/M checks pass", and appending a NEW last
        assignment citing 19-04-PLAN.md. Remember `test_companion_app.py` has
        multiple historical assignments and only the last is live.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;print('<script' in c.poll_trigger_section(17), '<script' in c.poll_trigger_section(0))"`
      prints `False False`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;print('data-cooldown=' in c.poll_trigger_section(17))"`
      prints `True`
    - `grep -c "def _poll_cooldown_script\|def _poll_submit_script" companion/pages/config_page.py` outputs `0`
    - `grep -c "let \|const \|=>\|innerHTML\|document.write\|eval(\|fetch(\|XMLHttpRequest" companion/static/poll-cooldown.js` outputs `0`
    - `grep -c "setInterval\|clearInterval\|removeAttribute\|addEventListener" companion/static/poll-cooldown.js` outputs at least `4`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.app as a, companion.layout as l;print(a.POLL_COOLDOWN_SCRIPT_ROUTE == l.POLL_COOLDOWN_SCRIPT_SRC)"`
      prints `True`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.layout as l;print(l.page_shell(title='T',active='health',body='<p>b</p>').count('<script src='))"`
      prints `8`
    - both harnesses report `M/M` (config-page) and the two documented
      root-sandbox FAILs only (companion-app)
  </acceptance_criteria>
  <done>No inline script is emitted anywhere by config_page; poll-cooldown.js carries both behaviours, is served pre-auth, is wired through all six touch points, and the two retargeted pinned checks assert the absence of inline scripts plus the presence of the data attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Send a CSP on every response and harden redirects (D-18)</name>
  <files>companion/app.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/app.py lines 845-915 (`_send_hardening_headers()`, `send_html()`,
      `send_bytes()`, `redirect()` — read the WR-02 docstring: it already names
      "with no X-Frame-Options/CSP an authenticated page can be framed", so the
      CSP is the completion of a stated intent, not a new claim)
    - companion/pages/config_page.py `_theme_chip_grid_html()` (lines 489-545) —
      count the `style="background:%s"` attributes with your own eyes and confirm
      every value comes from `device_config.THEMES`, never from request data
    - 19-CONTEXT.md "Resolutions of the research's open questions" (the amended
      CSP string, verbatim) and 19-RESEARCH.md Pitfall 2
    - companion/test_companion_app.py — grep for `X-Frame-Options` and
      `Referrer-Policy` to find the existing hardening-header checks and copy
      their shape
  </read_first>
  <action>
    Add a module-level constant in `companion/app.py` holding the CSP value
    EXACTLY as the orchestrator amended it:
      `default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; form-action 'self'; frame-ancestors 'none'`
    Send it as a fourth `self.send_header("Content-Security-Policy", ...)` call
    inside `_send_hardening_headers()`. Document in a comment, in this codebase's
    explain-the-why style: `script-src` stays strict with no `'unsafe-inline'`
    and no nonce — that is where the XSS risk lives, and Task 1 removed the last
    inline script so nothing needs an exception; `style-src` carries
    `'unsafe-inline'` solely for the seven `style="background:…"` theme-swatch
    attributes, whose values come from the fixed 18-member `device_config.THEMES`
    registry and are never user input, and a class-per-theme refactor was
    rejected because it would churn dozens of pinned render checks for no
    security gain; `img-src` needs `data:` for the inline favicon/icon data URI;
    `frame-ancestors 'none'` is the modern companion to the existing
    `X-Frame-Options: DENY`, which is kept for older browsers.

    Add `self._send_hardening_headers()` inside `redirect()`, immediately before
    `self.end_headers()` and after the `Content-Length` header, matching
    `send_html()`'s and `send_bytes()`'s existing call position. Also add
    `Cache-Control: no-store` to `redirect()` if and only if it is absent: a 303
    carrying a `Set-Cookie` must not be cached — check the current code first and
    record the decision either way in a comment.

    Add to `companion/test_companion_app.py`, in the real-running-service style
    this file already uses:
      - a check that an authenticated HTML response carries a
        `Content-Security-Policy` header whose value equals the constant exactly
        (string equality against `app_module.<CONSTANT_NAME>`, not a substring
        test, so the directive set cannot silently drift);
      - a check that the CSP value contains `script-src 'self'` and does NOT
        contain `script-src` followed by `'unsafe-inline'` anywhere;
      - a check that a 303 redirect response (e.g. a legacy-route redirect, or
        the unauthenticated redirect to `/login`) carries all four hardening
        headers — `X-Content-Type-Options`, `X-Frame-Options`,
        `Referrer-Policy` and `Content-Security-Policy`;
      - a check that the static CSS response (`send_bytes()` path) also carries
        the CSP header.
    Re-derive `EXPECTED_CHECK_COUNT` by running the harness and appending a new
    last assignment citing 19-04-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Content-Security-Policy" companion/app.py` outputs at least `2`
      (the constant and the `send_header` call)
    - `grep -c "frame-ancestors 'none'" companion/app.py` outputs `1`
    - `grep -c "script-src 'self';" companion/app.py` outputs `1` and
      `grep -c "script-src 'self' 'unsafe-inline'" companion/app.py` outputs `0`
    - `grep -n -A10 "def redirect" companion/app.py | grep -c "_send_hardening_headers"` outputs `1`
    - `server/.venv/bin/python3 companion/test_companion_app.py` reports exactly
      the two documented root-sandbox FAILs and no others
  </acceptance_criteria>
  <done>Every HTML, byte and redirect response carries the four hardening headers including the amended CSP; script-src is strict; four new checks pin the header value by equality.</done>
</task>

<task type="auto">
  <name>Task 3: Session-gate POST /ui-theme and POST /logout (D-18)</name>
  <files>companion/app.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/app.py lines 2225-2265 (`do_POST()`'s route chain — the
      gate-then-dispatch pattern at the SETTINGS_ROUTE branch, and the two
      ungated THEME_ROUTE / LOGOUT_ROUTE branches)
    - companion/app.py lines 916-945 (`require_session()` — note it returns a
      bool and performs its own 303-to-login with the allowlisted `next`
      parameter, so a gated branch returns `None` after a False)
    - companion/app.py `_handle_theme_post()` (~line 2215) — confirm what it
      writes and why an unauthenticated caller setting another visitor's UI theme
      cookie is a real state change
    - 19-02-SUMMARY.md (plan 19-02 already added the `auth.revoke(token)` call to
      the LOGOUT_ROUTE branch — you are adding the gate ON TOP of that, not
      replacing it; read the summary and the current branch before editing)
    - companion/test_companion_app.py — grep for `THEME_ROUTE`, `/ui-theme` and
      `/logout` and read every existing check that posts to either route: several
      will now need a session cookie they do not currently send
  </read_first>
  <action>
    Insert the standard gate-then-dispatch guard into both branches of
    `do_POST()`, copying the SETTINGS_ROUTE branch's exact shape:
      `if not self.require_session(): return None`
    placed before `self._handle_theme_post()` and before the LOGOUT_ROUTE
    branch's body. The LOGOUT_ROUTE branch must keep plan 19-02's
    `auth.revoke(...)` call and its existing redirect+`Set-Cookie` response,
    with the gate running first.

    Add a comment on the logout branch explaining why gating a logout is correct
    rather than pedantic: an unauthenticated `POST /logout` is a CSRF-shaped
    forced-sign-out of whoever holds the session, and gating it costs a signed-out
    caller nothing — they are already signed out.

    Audit and update every existing harness check that posts to either route:
    any check that previously expected a 303 without a cookie must now either
    send the session cookie (if it is testing the happy path) or be retargeted to
    assert the unauthenticated 303-to-login (if that is the behaviour it is
    really pinning). Retarget in place, keeping each check's name recognisable,
    and record the retarget in a comment — never delete coverage.

    Add two new checks: `POST /ui-theme` with no cookie returns 303 to `/login`
    and does NOT emit a `ui_theme` `Set-Cookie` header; `POST /logout` with no
    cookie returns 303 to `/login`. Re-derive `EXPECTED_CHECK_COUNT` by running
    the harness and appending a new last assignment citing 19-04-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n -A4 "if path == THEME_ROUTE" companion/app.py | grep -c "require_session"` outputs `1`
    - `grep -n -A6 "if path == LOGOUT_ROUTE" companion/app.py | grep -c "require_session"` outputs `1`
    - `grep -n -A6 "if path == LOGOUT_ROUTE" companion/app.py | grep -c "auth.revoke"` outputs `1`
      (plan 19-02's revocation call survives the gating change)
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import re;s=open('companion/app.py').read();print(s.count('if not self.require_session():'))"`
      outputs a number two greater than it did before this task
    - `server/.venv/bin/python3 companion/test_companion_app.py` reports exactly
      the two documented root-sandbox FAILs and no others
  </acceptance_criteria>
  <done>Both previously ungated state-changing POST routes require a session; existing checks that posted anonymously are retargeted rather than dropped; two new checks pin the unauthenticated behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> every route | this is an authenticated admin panel reachable from the internet behind Caddy; every response's headers are a control surface |
| third-party page -> an authenticated companion page | framing / clickjacking / cross-origin form posting |
| any origin -> `POST /ui-theme`, `POST /logout` | the two state-changing POST routes that require no session today |
| server-rendered HTML -> browser script execution | the inline `<script>` elements that force a permissive `script-src` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering (XSS) | every HTML response; the two inline `<script>` bodies in `poll_trigger_section()` | mitigate | Task 1 removes both inline scripts so Task 2's `script-src 'self'` can be strict with no `'unsafe-inline'` and no nonce. Proven by the "no `<script` in `poll_trigger_section()` output" assertion plus the CSP string-equality check |
| T-19-05 | Tampering / Information Disclosure | `app.redirect()` | mitigate | Task 2 adds `_send_hardening_headers()` to `redirect()`, so a 303 (including the login bounce and the flash redirects) can no longer be framed, MIME-sniffed or leak a cross-origin referrer. Proven by the new redirect-carries-four-headers check |
| T-19-04 | Elevation of Privilege | `do_POST()` `THEME_ROUTE` and `LOGOUT_ROUTE` | mitigate | Task 3 adds `require_session()` before both, matching the gate-then-dispatch pattern every other state-changing route already uses. Proven by the two new unauthenticated-303 checks |
| T-19-17 | Tampering (clickjacking) | authenticated pages | mitigate | `frame-ancestors 'none'` in the CSP, alongside the retained `X-Frame-Options: DENY` for older browsers |
| T-19-18 | Tampering (cross-origin form post) | every `<form>` on the site | mitigate | `form-action 'self'` in the CSP, complementing the existing `SameSite=Strict` session cookie |
| T-19-19 | Tampering (CSP bypass via inline style) | the seven server-controlled `style="background:…"` swatch attributes | accept | `style-src 'self' 'unsafe-inline'` is the orchestrator's ruling: every value comes from the fixed 18-member `device_config.THEMES` registry and is never user input, so an inline-style allowance carries no injection path here, while a class-per-theme refactor would churn dozens of pinned render checks for no security gain. `script-src` — where the real risk lives — stays strict |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; no build step, no bundler, no new JS dependency |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` -> `M/M checks pass`
- `server/.venv/bin/python3 companion/test_companion_app.py` -> exactly the two
  documented root-sandbox FAILs
- `scripts/run-all-tests.sh` -> no NEW failures beyond the five pre-existing
  root-sandbox artifacts
- `<human-check>` (end-of-phase): with the service running, load the Device page
  during a poll cooldown and confirm the countdown still ticks down and re-enables
  the button, and that the browser console shows NO CSP violation; then click
  Trigger poll now on a zero-cooldown page and confirm the button still disables
  and reads "Polling…"; then confirm every theme chip on Display still shows its
  colour swatch.
</verification>

<success_criteria>
- `poll_trigger_section()` emits no `<script>` on either branch
- `/static/poll-cooldown.js` is served pre-auth with a `text/javascript` content
  type and `Cache-Control: max-age=300`
- `app.POLL_COOLDOWN_SCRIPT_ROUTE == layout.POLL_COOLDOWN_SCRIPT_SRC` and
  `page_shell()` emits eight deferred script tags
- Every HTML, byte and 303 response carries the four hardening headers
- The CSP value equals the amended string exactly; `script-src` has no
  `'unsafe-inline'`
- `POST /ui-theme` and `POST /logout` without a session both 303 to `/login`
- `EXPECTED_CHECK_COUNT` re-derived by running each touched harness
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-04-SUMMARY.md` when done.
</output>
