---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 11
type: execute
wave: 5
depends_on: ["19-08", "19-09", "19-10"]
files_modified:
  - companion/pages/config_page.py
  - companion/app.py
  - companion/layout.py
  - companion/static/confirm-submit.js
  - companion/test_config_page.py
  - companion/test_companion_app.py
autonomous: true
requirements: [CFG-01]
user_setup: []

must_haves:
  truths:
    - "D-08 (A-26): disconnecting the calendar is its own POST /settings/calendar/disconnect form on the Device page, outside #settings-form — the ordinary checkbox is gone"
    - "D-08 (A-26): a JS user gets one native confirm() dialog and no interim page; a no-JS user gets a server-rendered two-step confirmation page"
    - "D-08 (A-26): the server only disconnects when it receives confirm=yes — a bare POST renders the confirmation page instead of erasing anything"
    - "D-12 (A-30): the theme chip grids and the runway row are exposed as radiogroups via role=radiogroup + aria-labelledby pointing at the group's own <h2> — NOT <fieldset>/<legend>, which four pinned checks forbid on this page"
    - "D-12 (A-30): every hint paragraph has an id and its control an aria-describedby pointing at it"
  artifacts:
    - path: "companion/static/confirm-submit.js"
      provides: "the inline-free native confirm step for any [data-confirm] form"
      min_lines: 40
    - path: "companion/pages/config_page.py"
      provides: "the dedicated disconnect form, the two-step confirmation page, and the radiogroup/aria wiring"
      contains: "role=\"radiogroup\""
    - path: "companion/app.py"
      provides: "the POST /settings/calendar/disconnect route behind require_session()"
      contains: "CALENDAR_DISCONNECT_ROUTE"
  key_links:
    - from: "companion/app.py"
      to: "server/plane/calendar_rules.py"
      via: "save_calendar_url(state_dir, CLEAR_CALENDAR_URL) — the single existing disconnect writer, reused not reimplemented"
      pattern: "CLEAR_CALENDAR_URL"
    - from: "companion/static/confirm-submit.js"
      to: "companion/pages/config_page.py"
      via: "the [data-confirm] / [data-confirm-field] attribute contract on the disconnect form"
      pattern: "data-confirm"
    - from: "companion/pages/config_page.py"
      to: "each group's <h2>"
      via: "role=radiogroup + aria-labelledby pointing at that heading's id"
      pattern: "aria-labelledby"
---

<objective>
Close A-26 (D-08) and A-30 (D-12) on the Settings pages: give "disconnect this
calendar and delete the flights it supplied" a real, confirmed, dedicated action
instead of an ordinary checkbox buried in a nine-field form, and give the chip
grids and the runway row the group semantics a screen-reader user needs.

Purpose: A-26 is a destructive, hard-to-undo action reachable by a stray click
with no confirmation at all. A-30 means a screen-reader user hears eighteen
unrelated radio buttons with no group name and no connection to the hint
paragraph that explains them.
Output: a dedicated POST route with a JS-and-no-JS confirmation path, a new
`companion/static/confirm-submit.js`, and `role="radiogroup"` +
`aria-labelledby` + `aria-describedby` wiring that adds zero `<fieldset>`
elements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
THE CROSS-CUTTING WARNING THAT SHAPES D-12 (19-PATTERNS.md, "Critical
Cross-Cutting Warning for the Planner"): this codebase DELIBERATELY removed every
`<fieldset>`/`<legend>` from the Settings page, and FOUR pinned checks in
`companion/test_config_page.py` (~:457, :612, :855, :2825) each assert:
    if "<fieldset" in rendered:
        return False, "expected zero <fieldset> elements anywhere on the page, found one"
`config_page.py`'s own docstrings (~:560-565, :711-712, :835-839) give the reason:
a `<legend>` only has accessible-name semantics inside a `<fieldset>`, which these
sibling groups deliberately do not have. D-12 offers the compatible path and it is
the one to build: `role="radiogroup"` + `aria-labelledby` pointing at the group's
existing `<h2>`. Implementing D-12 with a literal `<fieldset>` fails four pinned
checks by name and must not be attempted.

Calendar disconnect, current in-form checkbox to REMOVE —
`companion/pages/config_page.py:1109-1128` inside `calendar_group()`:
  disconnect_checkbox_html = ""
  if configured or drift:
      disconnect_checkbox_html = (
          '<label class="settings-checkbox">'
          '<input type="checkbox" name="calendar_disconnect" '
          'id="calendar-disconnect" value="%s"> %s</label>'
      ) % (escape_html(CALENDAR_DISCONNECT_CHECKBOX_VALUE),
           escape_html(CALENDAR_DISCONNECT_CHECKBOX_LABEL))

The shared resolver — `companion/pages/config_page.py:1824-1885`
`submitted_calendar_signal(form)`: SIX resolution gates returning one of
CALENDAR_URL_SIGNAL_CARRY_FORWARD / _SET / _CLEAR / _INVALID. Gates 1-3 exist
solely to interpret a `calendar_disconnect` field submitted INSIDE the settings
form. 19-RESEARCH.md Pitfall 7: LEAVE gates 1-3 in place (harmless and still
correct if a hostile client crafts that field into a `/settings` POST, which
`handle_post()`'s all-or-nothing rejection still covers) unless a grep of every
call site proves them unreachable AND worth removing. Document the choice either
way. Its docstring names a second consumer ("plan 17-04's request handler") —
grep and verify before touching anything.

The disconnect writer to reuse — `server/plane/calendar_rules.py`:
  CLEAR_CALENDAR_URL = object()                            # :141 — an identity
      # sentinel; save_calendar_url() compares it by IDENTITY only, never by
      # truthiness or equality, so the literal string "CLEAR_CALENDAR_URL" takes
      # the ordinary URL path
  def save_calendar_url(state_dir, value, now=None) -> bool  # :1139
      # every successful call erases the fetched calendar registry (17-CONTEXT.md
      # D-04/D-05) — that erasure IS the disconnect, and it is why the ordinary
      # settings save must never call it on an unrelated change
  FLASH_KEY_CALENDAR_DISCONNECTED                          # already exists in app.py

The immediate-action route pattern to copy — `companion/app.py`:
  RULES_ADD_ROUTE = config_page.RULES_ADD_ROUTE            # :189 — rebound from
      # config_page, never retyped (app.py imports that module; the reverse would
      # be a cycle)
  def _handle_rule_add_post(self)                          # :1740 — read its
      # docstring: an immediate action OUTSIDE the settings form and its dirty
      # bar, with an explicit branch per result value and never a dict lookup, so
      # an unrecognised result cannot pass through with no flash at all
  do_POST()'s gate-then-dispatch shape                     # :2293-2310
      if path == RULES_ADD_ROUTE:
          if not self.require_session():
              return None
          return self._handle_rule_add_post()

The six-touch-point static-script contract for the new script — see
19-04-SUMMARY.md and `companion/app.py:108-140` / `:1322-1333` / `:1915-1921`,
`companion/layout.py:113-146` / `:1110-1146`. After plan 19-04 there are EIGHT
scripts; this plan adds the ninth. `app.py` cannot import `layout.py`'s constant
(import cycle) — the route string is retyped in both files and pinned equal by a
dedicated harness check. Do not try to collapse it.

The two radiogroup targets — `companion/pages/config_page.py`:
  def _theme_chip_grid_html(field_name, selected_theme_id, extra_class="", extra_attr="")  # :489
      # returns '<div class="theme-chip-grid"{attr}>{chips}</div>'; `extra_attr`
      # already exists as the attribute-injection seam (the arrivals grid uses it
      # for ARRIVAL_GRID_ATTR) — reuse it rather than adding a second seam
  def theme_fieldset(current_theme_id, current_theme_arriving=None)  # :546
      # emits the group's <h2> — the element aria-labelledby must point at.
      # NOTE the function NAME contains "fieldset" but it emits no <fieldset>
      # element; keep the name, it is referenced by tests
  def runway_fieldset(current_runway_id, images_available=())        # :673
  THEME_SECTION_CAPTION / RUNWAY_SECTION_CAPTION / LED_SECTION_CAPTION /
  QUIET_HOURS_SECTION_CAPTION / WAKE_INTERVAL_SECTION_CAPTION /
  DISPLAY_SECTION_CAPTION / CALENDAR_SECTION_CAPTION / RULES_SECTION_CAPTION
      # each rendered as '<p class="text-label section-caption">…</p>' — these are
      # the hint paragraphs that need ids

Plans 19-07 and 19-10 both changed `config_page.py` in earlier waves: 19-07 added
`errors`/`submitted` threading plus an `aria-describedby` for field errors, and
19-10 added the quiet-hours presets. A control that now needs BOTH a hint id and
an error id must carry a SPACE-SEPARATED `aria-describedby` list, not one
overwriting the other. Read 19-07-SUMMARY.md and 19-10-SUMMARY.md and the live
source before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give calendar disconnect its own route, its own form, and a server-side confirmation step (D-08)</name>
  <files>companion/pages/config_page.py, companion/app.py, companion/test_config_page.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/pages/config_page.py lines 1001-1174 (`calendar_group()` in FULL —
      the status branches, the disconnect checkbox block and its polarity comment,
      the theme select, and the section's own `<h2>`/caption)
    - companion/pages/config_page.py lines 1818-1888 (`submitted_calendar_signal()`
      in full — all six gates and the docstring naming a second consumer)
    - companion/pages/config_page.py lines 396-480 (every `CALENDAR_*` copy and
      limit constant, including `CALENDAR_DISCONNECT_CHECKBOX_LABEL/VALUE` and
      `CALENDAR_URL_MAX_LEN`)
    - companion/pages/config_page.py lines 340-395 (`RULES_ADD_ROUTE`,
      `RULES_DELETE_ROUTE_PREFIX/SUFFIX` — the route-constant naming convention
      for an immediate action)
    - companion/app.py lines 1740-1840 (`_handle_rule_add_post()` and
      `_handle_rule_delete()` in full — the explicit-branch-per-result discipline
      and the flash redirect shape) and lines 2285-2315 (their `do_POST()` dispatch)
    - companion/app.py `_handle_settings_post()` as modified by plan 19-07 (read
      19-07-SUMMARY.md first) — in particular where `submitted_calendar_signal()`
      is consulted and where `FLASH_KEY_CALENDAR_DISCONNECTED` is emitted
    - server/plane/calendar_rules.py lines 130-150 and 1139-1240
      (`CLEAR_CALENDAR_URL`, `save_calendar_url()` — its return value, its
      registry erasure, and the identity-only sentinel comparison)
    - companion/test_config_page.py — grep for `calendar_disconnect` and read
      every pinned check
    - companion/test_companion_app.py — grep for `calendar_disconnect`,
      `T-17-FLASH` and `calendar_disconnected` and read every pinned check
  </read_first>
  <action>
    Route and handler:
      - add `CALENDAR_DISCONNECT_ROUTE = "/settings/calendar/disconnect"` to
        `companion/pages/config_page.py` beside `RULES_ADD_ROUTE`, and rebind it
        in `companion/app.py` (`CALENDAR_DISCONNECT_ROUTE =
        config_page.CALENDAR_DISCONNECT_ROUTE`) rather than retyping the literal —
        that rebinding is this codebase's documented convention for a route whose
        single definition site is `config_page.py`;
      - add a `_handle_calendar_disconnect_post()` handler modelled on
        `_handle_rule_add_post()`: read the form, and if the submitted `confirm`
        field is not exactly the accepted value, render the two-step confirmation
        page at 200 and return WITHOUT touching anything. Only on an exact match
        call `calendar_rules.save_calendar_url(state_dir,
        calendar_rules.CLEAR_CALENDAR_URL)` — the single existing disconnect
        writer, never a reimplementation — and branch explicitly on its boolean
        result to `FLASH_KEY_CALENDAR_DISCONNECTED` or the existing generic
        calendar-failure flash, redirecting back to the Device page. Never a dict
        lookup, so an unexpected result cannot pass through with no flash;
      - wire it into `do_POST()` with the standard gate-then-dispatch shape
        (`if not self.require_session(): return None`), placed beside the rules
        routes;
      - define the accepted confirm value as a module constant in
        `config_page.py` (e.g. `CALENDAR_DISCONNECT_CONFIRM_VALUE = "yes"`) with
        the field name constant beside it, so the markup, the script and the
        handler all read one definition.

    The form and the confirmation page, both in `config_page.py`:
      - DELETE the `disconnect_checkbox_html` block from `calendar_group()` and
        every reference to it in that function's returned markup. Keep
        `CALENDAR_DISCONNECT_CHECKBOX_VALUE`/`_LABEL` only if they are still used;
        if not, retire them with a comment recording D-08 rather than deleting
        silently;
      - add a `calendar_disconnect_section()` builder emitting a standalone
        `<form method="post" action="{CALENDAR_DISCONNECT_ROUTE}">` — OUTSIDE
        `<form id="settings-form">`, which HTML requires anyway since forms cannot
        nest — rendered only when the calendar is `configured or drift` (the same
        condition the checkbox used, and for the same documented reason: a drifted
        file still holds a URL the disconnect promise must reach). It contains a
        hidden `<input type="hidden" name="{confirm field}" value="">` carrying a
        `data-confirm-field` attribute, a plain `<button type="submit">` with copy
        that names the consequence (the existing
        `CALENDAR_DISCONNECT_CHECKBOX_LABEL` wording is already explicit about
        deleting the supplied flights — reuse that voice), and a `data-confirm`
        attribute on the form whose value is the confirmation question the script
        will show;
      - add a `calendar_disconnect_confirm_page(ctx)` builder returning the
        two-step page body: a heading, one plain sentence naming exactly what will
        be erased, a `<form method="post">` posting back to the same route with the
        confirm field pre-set to the accepted value and a real submit button, and
        a plain link back to the Device page as the cancel path. Route every
        dynamic value through `escape_html()`. This page is what a no-JS browser
        sees, and it is the server-side control — the native `confirm()` added in
        Task 2 is a misclick guard, never a security control (19-CONTEXT.md's own
        D-08 resolution says so explicitly);
      - emit `calendar_disconnect_section()` from `render()` on the Device scope
        only, immediately after the calendar section, and never inside the
        settings form.

    `submitted_calendar_signal()`: grep every call site in `config_page.py` and
    `app.py` first. Per 19-RESEARCH.md Pitfall 7, LEAVE gates 1-3 in place — they
    become unreachable from the trimmed form but stay correct against a hostile
    client crafting `calendar_disconnect` into a `/settings` POST, which
    `handle_post()`'s all-or-nothing rejection still covers. Add a comment to the
    function recording that as a deliberate decision with its reason, and that the
    dedicated route does NOT call this resolver at all (it always means "clear",
    so signal resolution would be dead weight there).

    Harness work:
      - in `companion/test_config_page.py`, retarget in place every
        `calendar_disconnect`-checkbox check: each must now assert the checkbox is
        ABSENT from `render()`'s output at every scope, and assert the new
        standalone form is present on the Device scope (and absent when no
        calendar is configured). Add checks that the disconnect form is NOT a
        descendant of the settings form (assert its `<form` opening tag appears
        after the settings form's `</form>`), that it carries the hidden confirm
        field with an EMPTY value, and that
        `calendar_disconnect_confirm_page(ctx)` renders a form posting to the same
        route with the confirm field pre-set;
      - in `companion/test_companion_app.py`, add real-HTTP checks: a bare
        authenticated `POST /settings/calendar/disconnect` with no confirm field
        returns 200 containing the confirmation copy and leaves the calendar URL
        file untouched; the same POST with `confirm=yes` returns 303 with the
        disconnected flash and the calendar is actually gone; an unauthenticated
        POST returns 303 to `/login`; and a POST with `confirm=maybe` renders the
        confirmation page rather than disconnecting. Retarget any existing
        T-17-FLASH calendar-disconnect check that pointed at the old in-form path;
      - re-derive `EXPECTED_CHECK_COUNT` in BOTH harnesses by running each and
        appending a NEW last assignment citing 19-11-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;import companion.app as a;print(a.CALENDAR_DISCONNECT_ROUTE == c.CALENDAR_DISCONNECT_ROUTE)"`
      prints `True`
    - `grep -c 'name="calendar_disconnect"' companion/pages/config_page.py` outputs `0`
    - `grep -c "def calendar_disconnect_section\|def calendar_disconnect_confirm_page" companion/pages/config_page.py` outputs `2`
    - `grep -c "def _handle_calendar_disconnect_post" companion/app.py` outputs `1`
    - `grep -n -A4 "if path == CALENDAR_DISCONNECT_ROUTE" companion/app.py | grep -c "require_session"` outputs `1`
    - `grep -c "calendar_rules.CLEAR_CALENDAR_URL" companion/app.py` outputs at least `1`
    - `grep -c "def submitted_calendar_signal" companion/pages/config_page.py` outputs `1` and `git diff companion/pages/config_page.py | grep -c "^-.*gate 1\|^-.*gate 2\|^-.*gate 3"` outputs `0`
    - both harnesses green: `test_config_page.py` `M/M`, `test_companion_app.py`
      with exactly the two documented root-sandbox FAILs
  </acceptance_criteria>
  <done>Disconnect is its own session-gated route with a form outside the settings form; a bare POST renders a confirmation page and erases nothing; only confirm=yes disconnects; the shared resolver's gates 1-3 are preserved with the reason recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add the inline-free native confirm step (D-08, client half)</name>
  <files>companion/static/confirm-submit.js, companion/app.py, companion/layout.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/static/poll-cooldown.js (created by plan 19-04 — the most recent
      example of this codebase's new-static-script conventions; read its header
      comment shape and its data-attribute reads)
    - companion/static/dirty-state.js (the guard-clause idiom and, as of plan
      19-10, its `submit` listener — read 19-10-SUMMARY.md: you must not let the
      confirm dialog and the beforeunload guard fight each other)
    - companion/app.py lines 108-140 (now nine route constants after plan 19-04),
      lines 1315-1340 (a `_serve_*_script` delegate), lines 1905-1935 (the
      `do_GET()` static dispatch chain)
    - companion/layout.py lines 110-150 (the `*_SCRIPT_SRC` constants and their
      duplicated-not-imported comments) and lines 1100-1150 (`page_shell()`'s
      script-tag format string and value tuple — after 19-04 it emits eight)
    - companion/test_companion_app.py lines 2540-2700 (`_static_script_public()`,
      the route/src agreement check, and the per-file ES5/forbidden-token guard
      shape) plus whatever plan 19-04 and 19-09 appended
    - companion/pages/config_page.py's `calendar_disconnect_section()` from Task 1
      (the `data-confirm` / `data-confirm-field` attribute contract)
  </read_first>
  <action>
    Create `companion/static/confirm-submit.js`, wired through the six-touch-point
    static-script contract as the ninth script:
      `CONFIRM_SUBMIT_SCRIPT_ROUTE = "/static/confirm-submit.js"` and
      `_CONFIRM_SUBMIT_JS_PATH` in `app.py`; a `_serve_confirm_submit_script()`
      delegate; one `do_GET()` dispatch line; `CONFIRM_SUBMIT_SCRIPT_SRC` in
      `layout.py` with the sibling duplicated-not-imported comment; and a ninth
      `<script src="%s" defer></script>` line plus tuple entry in `page_shell()`.

    Behaviour, in the house ES5-safe subset (no `let`/`const`/arrow/backtick, no
    `innerHTML` or any other HTML-writing sink):
      - `document.querySelectorAll("form[data-confirm]")`; return immediately when
        the list is empty (this script is served to every page and only the Device
        page renders such a form — a load-bearing guard, not defensive noise);
      - on each form's `submit` event: read the question from the form's
        `data-confirm` attribute and call `window.confirm(question)`. On a decline,
        `evt.preventDefault()` and return, leaving the hidden field untouched. On
        an accept, find the form's own `[data-confirm-field]` input and set its
        `.value` to the value carried in a `data-confirm-value` attribute on the
        form (server-supplied, so the accepted token has exactly one definition
        site in `config_page.py`), then let the submit proceed;
      - never construct or dispatch a synthetic event; never navigate; never read
        a URL out of the DOM.
    Header comment in the house style: name D-08/A-26, the ES5 constraint, the
    route it is served from, the standing no-HTML-writing-sink rule, and — this
    one matters — state plainly that the native dialog is a MISCLICK GUARD, not a
    security control: the server independently requires the confirm field and
    renders its own two-step page for a no-JS client, and the route is behind
    `require_session()`. A reader must not mistake this file for an authorisation
    step.

    Interaction with plan 19-10's `beforeunload` guard: the disconnect form is
    NOT the settings form, so `dirty-state.js`'s submit listener does not fire for
    it and a user with unsaved settings edits will get BOTH the confirm dialog and
    the unload warning. That is correct and intended — they really are about to
    lose unsaved edits — and must be recorded in a comment in this file so a
    future reader does not "fix" it.

    Harness work in `companion/test_companion_app.py`: add the three checks every
    static script gets — `_static_script_public("/static/confirm-submit.js")`; a
    named ES5/forbidden-token guard on the file's source banning `let `, `const `,
    `=>`, backtick, `innerHTML`, `outerHTML`, `insertAdjacentHTML`,
    `document.write`, `eval(`, `fetch(`, `XMLHttpRequest`, `location.assign`,
    `location.replace` and asserting the presence of `use strict`,
    `addEventListener`, `preventDefault` and `confirm(`; and a route/src agreement
    check. Add a check that `page_shell()` now emits nine deferred script tags.
    Re-derive `EXPECTED_CHECK_COUNT` by running the harness and appending a new
    last assignment citing 19-11-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.app as a, companion.layout as l;print(a.CONFIRM_SUBMIT_SCRIPT_ROUTE == l.CONFIRM_SUBMIT_SCRIPT_SRC)"`
      prints `True`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.layout as l;print(l.page_shell(title='T',active='device',body='<p>b</p>').count('<script src='))"`
      prints `9`
    - `grep -c "let \|const \|=>\|innerHTML\|document.write\|eval(\|fetch(" companion/static/confirm-submit.js` outputs `0`
    - `grep -c "confirm(" companion/static/confirm-submit.js` outputs at least `1`
    - `grep -c "misclick" companion/static/confirm-submit.js` outputs at least `1`
      (the not-a-security-control statement is present)
    - `server/.venv/bin/python3 companion/test_companion_app.py` reports exactly
      the two documented root-sandbox FAILs and no others
  </acceptance_criteria>
  <done>A ninth static script provides the native confirm step for any [data-confirm] form, is served pre-auth, is wired through all six touch points, and documents that it is a misclick guard rather than an authorisation step.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the chip grids and the runway row as radiogroups, and link every hint (D-12)</name>
  <files>companion/pages/config_page.py, companion/test_config_page.py</files>
  <read_first>
    - 19-PATTERNS.md "Critical Cross-Cutting Warning for the Planner" (read it
      before anything else — it names the four pinned checks a `<fieldset>` would
      break, by line number)
    - companion/test_config_page.py lines 450-465, 605-620, 848-862, 2818-2832
      (all FOUR zero-fieldset checks — read each in full and confirm for yourself
      what they assert)
    - companion/pages/config_page.py lines 489-545 (`_theme_chip_grid_html()` —
      the `extra_class`/`extra_attr` seam and the `ARRIVAL_GRID_ATTR` precedent
      for injecting an attribute onto the grid wrapper)
    - companion/pages/config_page.py lines 546-673 (`theme_fieldset()` — where its
      `<h2>` is emitted and whether it already has an id) and lines 673-772
      (`runway_fieldset()` — the runway row wrapper and its `<h2>`)
    - companion/pages/config_page.py lines 560-565, 711-712, 835-839 (the three
      docstring paragraphs that explain WHY there is no `<fieldset>`/`<legend>`
      here — they must be extended, not contradicted)
    - the CURRENT source of every group builder (plans 19-07 and 19-10 changed
      several last wave — read 19-07-SUMMARY.md and 19-10-SUMMARY.md first),
      paying particular attention to the `aria-describedby` plan 19-07 added for
      field errors: a control needing both a hint id and an error id must carry a
      space-separated list
    - .claude/skills/sketch-findings-skypane/SKILL.md (the
      `input.visually-hidden` / `select.visually-hidden` exempt-by-delegation
      touch-target entry — the radios are visually hidden and delegate their hit
      area to the wrapping `.runway-card` / `.theme-chip` label; the aria wiring
      must not disturb that)
  </read_first>
  <action>
    Add `role="radiogroup"` plus `aria-labelledby` to the two chip grids and to the
    runway row, pointing at each group's own `<h2>`:
      - give each group's `<h2>` a stable `id` via a new module-level id constant
        per group (e.g. `THEME_GROUP_HEADING_ID`,
        `THEME_ARRIVING_GROUP_HEADING_ID`, `RUNWAY_GROUP_HEADING_ID`) — constants
        at the top, never literals at a render site, matching this file's
        convention;
      - pass the `role`/`aria-labelledby` pair into `_theme_chip_grid_html()`
        through its EXISTING `extra_attr` seam rather than adding a second seam;
        the arrivals grid must keep `ARRIVAL_GRID_ATTR` as well, so build a
        combined attribute string for it;
      - wrap the runway row the same way on its existing row container.
    Add ZERO `<fieldset>` and ZERO `<legend>` elements anywhere. Extend the three
    docstring paragraphs that explain the fieldset-free design with a note that
    D-12/A-30 supplies the missing group semantics via ARIA instead, for exactly
    the reason those paragraphs already give.

    Link every hint paragraph to its control:
      - give each `<p class="text-label section-caption">` hint an `id` from a new
        per-group id constant;
      - add `aria-describedby` pointing at that id to the group's primary control
        — the radiogroup container for the two chip grids and the runway row, and
        the individual `<input>`/`<select>` for the single-control groups (LED
        checkbox, both quiet-hours time inputs, the wake-interval number input,
        the display checkbox, the calendar theme select and the calendar URL
        input);
      - where plan 19-07 already added an error-message `aria-describedby`, the
        two ids must be combined into ONE space-separated `aria-describedby`
        value, hint first then error. Factor that into a small private helper
        (e.g. `_describedby_attr(*ids)`) that drops falsy ids and returns the
        empty string when none remain, so no control ever emits
        `aria-describedby=""` and no builder hand-concatenates ids.

    Harness work in `companion/test_config_page.py`: leave all four zero-fieldset
    checks passing UNMODIFIED — they are the guard that keeps this implementation
    honest. Add checks that: a full `render()` at each scope contains at least two
    `role="radiogroup"` occurrences on Display and at least one on Device; every
    `aria-labelledby` value on the page matches the `id` of an element that
    actually exists in the same output (parse the ids out and compare — a dangling
    reference is worse than none); every `aria-describedby` value's every
    space-separated token likewise resolves to an existing id; no element emits an
    empty `aria-describedby`; and a control carrying both a hint and an error
    (render with an `errors` dict for that field) has BOTH ids in its
    `aria-describedby`, in that order. Re-derive `EXPECTED_CHECK_COUNT` by
    running the harness and appending a new last assignment citing 19-11-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;h=c.render({'device_config':{},'state_dir':'/tmp'}, scope=c.SCOPE_DISPLAY);print('<fieldset' in h, h.count('role=\"radiogroup\"'))"`
      prints `False` followed by an integer of at least `2`
    - `grep -c "<fieldset\|<legend" companion/pages/config_page.py` outputs `0`
    - `grep -c "def _describedby_attr" companion/pages/config_page.py` outputs `1`
    - `grep -c 'aria-describedby=""' companion/pages/config_page.py` outputs `0`
    - `grep -c "aria-labelledby" companion/pages/config_page.py` outputs at least `3`
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with `M/M checks pass`
    - `scripts/run-all-tests.sh` shows no NEW failures beyond the five
      pre-existing root-sandbox artifacts
  </acceptance_criteria>
  <done>The two chip grids and the runway row are named radiogroups; every hint is linked to its control; hint and error ids coexist in one aria-describedby; the four zero-fieldset checks still pass unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any origin -> `POST /settings/calendar/disconnect` | a new state-changing, destructive route: a successful call erases the stored calendar URL AND the fetched calendar registry |
| browser -> the confirm field | the `confirm=yes` token is client-supplied and is the only thing standing between a POST and the erasure |
| client-side `confirm()` -> authorisation | a native dialog is trivially bypassable and must never be treated as a control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-41 | Elevation of Privilege | the new `POST /settings/calendar/disconnect` route | mitigate | `if not self.require_session(): return None` as the FIRST statement of the branch, copying the gate-then-dispatch shape every other state-changing route uses; pinned by the unauthenticated-303-to-login check |
| T-19-09 | Tampering (CSRF) | the new destructive route | accept | 19-CONTEXT.md's own D-08 resolution: no additional CSRF mechanism beyond the site's existing posture — `SameSite=Strict` session cookie, POST-only, session-gated, single origin, single operator. This matches every other state-changing route in the app; introducing a token here alone would create a false impression that the others are protected differently |
| T-19-42 | Tampering (accidental destructive action) | the confirm step | mitigate | the SERVER requires an exact `confirm=yes` and renders its own two-step page otherwise, so the guard holds with JavaScript disabled, with the script blocked by CSP, or against a hand-crafted form. The native `confirm()` is explicitly documented in `confirm-submit.js` as a misclick guard only. Pinned by the `confirm=maybe` and no-confirm-field checks, both of which assert the calendar file is untouched |
| T-19-43 | Denial of Service (data destruction on an unrelated save) | `submitted_calendar_signal()` gates 1-3 and `save_calendar_url()` | mitigate | the gates are DELIBERATELY retained (19-RESEARCH.md Pitfall 7) so a hostile `calendar_disconnect` field crafted into a `/settings` POST still resolves through `handle_post()`'s all-or-nothing rejection rather than into an unguarded code path; the dedicated route never consults the resolver because it always means clear |
| T-19-08 | Tampering (XSS) | the disconnect form, the confirmation page, the `data-confirm` question, the new id/aria attribute values | mitigate | every interpolated value passes `layout.escape_html()` at its interpolation point; the confirm question and the accepted token are module constants, never request data. The dangling-reference checks in Task 3 additionally prove no aria attribute points at an id the page does not emit |
| T-19-10 | Tampering (DOM sink) | `companion/static/confirm-submit.js` | mitigate | the file only reads attributes, calls `window.confirm`, sets one input's `.value` and may call `preventDefault()`; its named harness guard keeps every HTML-writing sink, `fetch(`, `eval(` and every URL-taking navigation form at zero |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; no build step, no bundler, no new JS dependency |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` -> `M/M checks pass`
- `server/.venv/bin/python3 companion/test_companion_app.py` -> exactly the two
  documented root-sandbox FAILs
- `scripts/run-all-tests.sh` -> no NEW failures beyond the five pre-existing
  root-sandbox artifacts
- `<human-check>` (end-of-phase): with a calendar connected, the Device page shows
  a standalone Disconnect button and no disconnect checkbox. Clicking it raises a
  native confirmation; declining changes nothing; accepting disconnects and shows
  the flash. With JavaScript disabled, clicking it lands on a confirmation page
  whose Cancel link returns to Device and whose confirm button disconnects. With a
  screen reader, the theme chips and the runway cards announce as named radio
  groups and each group's hint is read with its control.
</verification>

<success_criteria>
- `POST /settings/calendar/disconnect` exists, is session-gated, and erases
  nothing without an exact `confirm=yes`
- The `calendar_disconnect` checkbox no longer appears anywhere in `render()`'s
  output, at any scope
- The disconnect form is not a descendant of the settings form
- `submitted_calendar_signal()`'s six gates are intact, with the decision recorded
- A ninth static script provides the native confirm and `page_shell()` emits nine
  script tags
- `render()` emits zero `<fieldset>` and zero `<legend>` elements and at least one
  `role="radiogroup"` per scope that has one
- Every `aria-labelledby` and every `aria-describedby` token resolves to an id the
  same output emits; no empty `aria-describedby`
- `EXPECTED_CHECK_COUNT` re-derived by running each touched harness
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-11-SUMMARY.md` when done.
</output>
