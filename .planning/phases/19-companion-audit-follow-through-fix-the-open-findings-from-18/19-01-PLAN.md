---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/battery.py
  - companion/pages/home_page.py
  - companion/pages/health_page.py
  - companion/test_status_pages.py
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-03]
user_setup: []

must_haves:
  truths:
    - "D-01 (A-19): a household member reading Health sees the battery as a percentage ('≈ 62%') beside the millivolt figure, the same estimate Home already shows"
    - "D-01: battery_percent() lives in a shared non-page module, so neither home_page nor health_page imports the other"
    - "D-03 (A-21): every stat tile on Health states its verdict in words as well as in border colour (WCAG 1.4.1)"
  artifacts:
    - path: "companion/battery.py"
      provides: "shared clamped 0-100 battery estimate"
      exports: ["battery_percent", "BATTERY_FULL_MV", "BATTERY_EMPTY_MV"]
    - path: "companion/pages/health_page.py"
      provides: "battery percentage in the readout + text verdicts on Device/Pipeline/Corroboration tiles"
      contains: "DEVICE_STATE_TEXT"
    - path: "companion/pages/home_page.py"
      provides: "same estimate, now consumed from companion.battery"
      contains: "battery.battery_percent"
  key_links:
    - from: "companion/pages/health_page.py"
      to: "companion/battery.py"
      via: "import companion.battery as battery"
      pattern: "battery\\.battery_percent"
    - from: "companion/pages/home_page.py"
      to: "companion/battery.py"
      via: "import companion.battery as battery"
      pattern: "battery\\.battery_percent"
---

<objective>
Close A-19 (D-01) and A-21 (D-03) on the Health page: give the battery reading a
percentage a non-technical reader can act on, and give every Health stat tile a
short text verdict beside its colour.

Purpose: Health is an Advanced page, but the audit's audience framing still
applies — millivolts are not a verdict, and a colour-only status is invisible to
a colour-blind reader and to anyone reading a greyscale screenshot.
Output: a new shared `companion/battery.py`, a percentage in Health's battery
readout and in every sparkline point's tooltip, and word verdicts on Health's
three stat tiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<!-- .planning/STATE.md is deliberately NOT referenced: it is >500KB and would
     consume the executor's context budget. 19-CONTEXT.md carries every decision
     this plan needs. -->

<interfaces>
Current owner of the estimate — `companion/pages/home_page.py:106-124`:
  BATTERY_FULL_MV = 4200
  BATTERY_EMPTY_MV = 3300
  def battery_percent(mv) -> int | None   # clamped 0-100, None for non-numeric/<=0

Health's shared reading formatter — `companion/pages/health_page.py:701`:
  def _battery_reading_parts(mv, ts, now) -> (value_text, when_text)
  # value_text is "{mv} mV" today; it feeds the resting readout
  # (_battery_readout_block), every sparkline point's <title>/aria-label, and
  # every point's data-when attribute. One change here reaches all of them.

Health's tile wrapper — `companion/layout.py:1218`:
  def stat_tile(caption, content_html, status=None, icon=None)
  # `status` drives the coloured top border only; the verdict text must be
  # inside `content_html`. No stat_tile() change is needed.

Home's verdict-dict idiom to copy — `companion/pages/home_page.py:89-104`:
  FRAME_STATE_TEXT / DATA_STATE_TEXT / BATTERY_STATE_TEXT, each mapping
  "ok"/"warn"/"error" to a plain sentence, rendered as
  '<p class="text-body widget-verdict">…</p>'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move battery_percent() into a shared companion/battery.py</name>
  <files>companion/battery.py, companion/pages/home_page.py, companion/test_view_pages.py</files>
  <read_first>
    - companion/pages/home_page.py (the whole file — you must see the current
      owner of BATTERY_FULL_MV/BATTERY_EMPTY_MV/battery_percent and both call
      sites at lines ~191 and ~197)
    - companion/pages/__init__.py (the documented "no page module imports
      another page module" boundary that is the reason this module must exist)
    - companion/auth.py (lines 1-60 only — the module-docstring convention for a
      shared, page-independent companion module: state why it exists and what it
      must never import)
    - companion/test_view_pages.py lines 2580-2610 (the one existing pinned
      check that calls home_page.battery_percent directly)
    - companion/test_view_pages.py lines 60-90 (the EXPECTED_CHECK_COUNT
      assignment trail and the check() helper)
  </read_first>
  <action>
    Create `companion/battery.py` as a sibling of `companion/auth.py`,
    `companion/layout.py` and `companion/screens.py` — NOT inside
    `companion/pages/`. Give it a module docstring in this codebase's house
    style: name D-01/A-19 as the reason it exists, state that it is stdlib-only,
    and state the boundary explicitly — it must never import anything under
    `companion/pages/` and nothing under `server/`, because its whole purpose is
    to let `home_page.py` and `health_page.py` share one estimate without either
    importing the other.

    Move `BATTERY_FULL_MV = 4200`, `BATTERY_EMPTY_MV = 3300` and
    `battery_percent(mv)` into it verbatim, including the existing four-line
    comment block that explains the linear 3.3-4.2 V approximation and that the
    frame's own low-battery warning still uses the exact millivolt thresholds in
    `server/poll_loop.py`. Do not change the arithmetic, the clamping, or the
    None-returning behaviour for non-numeric and non-positive input.

    In `companion/pages/home_page.py`: add `import companion.battery as battery`
    alongside its existing imports, delete the three moved names, and retarget
    both call sites to `battery.battery_percent(...)`. Nothing else in that file
    changes — `BATTERY_STATE_TEXT`, `NO_READING_TEXT` and the "≈ %d%%"
    formatting all stay exactly where they are.

    In `companion/test_view_pages.py`: retarget the existing check at ~line 2593
    in place — same check name, same four assertions (4200 -> 100, 3300 -> 0,
    "x" -> None, 0 -> None) — to call `battery.battery_percent`, importing
    `companion.battery as battery` at the top of the file the same way the file
    already imports `home_page`. Add two new checks: one asserting
    `hasattr(home_page, "battery_percent") is False` (the move actually
    happened, nothing left behind), and one asserting that
    `companion/battery.py`'s source, read from disk, contains neither
    `companion.pages` nor `from server` (the documented boundary is enforced,
    not just promised).

    Then re-derive `EXPECTED_CHECK_COUNT`: run the harness, read its printed
    "N/M checks pass" line, and append a NEW last
    `EXPECTED_CHECK_COUNT = M` assignment with a short comment citing
    19-01-PLAN.md. Never compute M by arithmetic on an older comment, and never
    edit an older assignment's narration in place — this file's convention is an
    append-only derivation trail.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def battery_percent" companion/pages/home_page.py` outputs `0`
    - `grep -c "def battery_percent" companion/battery.py` outputs `1`
    - `grep -c "battery.battery_percent" companion/pages/home_page.py` outputs `2`
    - `grep -vc . /dev/null; grep -v '^#' companion/battery.py | grep -c "companion.pages"` outputs `0`
    - `server/.venv/bin/python3 companion/test_view_pages.py` exits 0 and its
      final line reports `M/M checks pass` with M equal to the file's last
      `EXPECTED_CHECK_COUNT` assignment
    - `server/.venv/bin/python3 -c "import companion.battery as b; print(b.battery_percent(3750))"`
      prints an integer in [0, 100]
  </acceptance_criteria>
  <done>companion/battery.py owns the estimate; home_page consumes it; the moved-function check and two new boundary checks pass; EXPECTED_CHECK_COUNT re-derived by running the harness.</done>
</task>

<task type="auto">
  <name>Task 2: Show the battery percentage beside the millivolt readout on Health (D-01)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/pages/health_page.py lines 690-760 (`_battery_reading_parts()` —
      the single helper that feeds the resting readout, every sparkline point's
      `<title>`/`aria-label`, and every point's `data-when` attribute)
    - companion/pages/health_page.py lines 1500-1565 (`_battery_readout_block()`
      and the two-span `battery-readout__value` / `battery-readout__detail`
      contract `companion/static/battery-trend.js` writes into)
    - companion/static/battery-trend.js (lines 25-115 — confirm it writes
      `textContent` into those two spans on hover, so the percentage must be
      part of the value TEXT the server builds, not extra markup)
    - companion/pages/home_page.py lines 183-205 (the exact `"≈ %d%%"` idiom and
      the `pct is None` fallback to reuse verbatim)
    - companion/battery.py (created in Task 1)
    - companion/test_status_pages.py lines 275-300 (EXPECTED_CHECK_COUNT trail)
      and every check whose name mentions `battery-readout`, `battery_readout`,
      or ` mV` (grep them first — they pin the current value-text shape)
  </read_first>
  <action>
    Add `import companion.battery as battery` to `companion/pages/health_page.py`.

    Change `_battery_reading_parts(mv, ts, now)` so its returned `value` text is
    `"≈ %d%% · %s mV" % (pct, mv)` when `battery.battery_percent(mv)` returns an
    int, and stays exactly `"%s mV" % mv` when it returns None. Use the real
    U+00B7 middle dot and the real U+2248 "≈" character, matching this module's
    existing real-Unicode-in-source convention. The literal `"{mv} mV"` must
    remain a substring of the result in both branches so no existing pinned
    substring check on the millivolt figure breaks.

    Doing it in this one helper — rather than in `_battery_readout_block()` — is
    deliberate and must be preserved: it makes the resting readout, each chart
    point's tooltip, each point's `aria-label` and each point's `data-when`
    attribute carry the same estimate BY CONSTRUCTION, and it needs no change to
    `companion/static/battery-trend.js`, whose `reveal()` writes the server-built
    value string through `textContent`.

    Update `_battery_reading_parts()`'s docstring to state that `value` is now
    "the estimate then the measurement", that the estimate is labelled "≈"
    because it is a linear approximation (D-01 keeps the linear estimate until
    Phase 5's discharge run yields real data), and that the exact millivolt
    thresholds still live in `server/poll_loop.py`.

    Add to `companion/test_status_pages.py`: a check calling
    `health_page._battery_reading_parts(3750, "<iso>", "<iso>")` and asserting
    the value part starts with "≈", contains "%" and still contains "3750 mV";
    a check asserting the None-percentage path (pass a non-numeric mv) leaves the
    value text free of "≈"; and a check on a full `health_page.render(ctx)` with
    a seeded battery reading asserting the rendered page contains both "≈" and
    " mV" inside the `battery-readout__value` span. Retarget — never delete — any
    existing check that asserted the value text equals a bare "{mv} mV" string.

    No new CSS class, no new design token, no new accent consumer: the percentage
    rides inside the existing `battery-readout__value mono` span, which the
    design contract already covers.

    Re-derive `EXPECTED_CHECK_COUNT` by running the harness and appending a new
    last assignment citing 19-01-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'.'); from companion.pages import health_page as h; print(h._battery_reading_parts(3750,'2026-09-11T10:00:00+00:00','2026-09-11T10:05:00+00:00')[0])"`
      prints a string containing `≈`, `%` and `3750 mV`
    - `grep -c "battery.battery_percent" companion/pages/health_page.py` outputs at least `1`
    - `grep -c "battery-readout__value mono" companion/pages/health_page.py` outputs `1` (the existing span survives unchanged)
    - `server/.venv/bin/python3 companion/test_status_pages.py` reports exactly
      one remaining FAIL, the documented root-sandbox `anomaly_active()`
      non-existent-state-dir case, and no other
    - `grep -c "location.reload\|innerHTML" companion/static/battery-trend.js` is unchanged from before this task (this task does not touch that file)
  </acceptance_criteria>
  <done>Health's battery readout and every sparkline point label read "≈ NN% · NNNN mV"; a non-numeric reading still renders millivolts alone with no stray "≈"; harness green except the one documented sandbox failure.</done>
</task>

<task type="auto">
  <name>Task 3: Give every Health stat tile a text verdict (D-03, WCAG 1.4.1)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/pages/health_page.py lines 1403-1480 (`_device_section()` and
      `_pipeline_section()` — read the quick-task-260901-tsa comment blocks in
      full: they record WHY the status dot's text was removed, and this task
      reverses that removal deliberately, so the comments must be updated, not
      contradicted silently)
    - companion/pages/health_page.py lines 2415-2445 (`render()`'s three
      `layout.stat_tile()` calls for Device / Pipeline / Corroboration /
      Resolution rate)
    - companion/pages/home_page.py lines 89-104 and 183-208 (FRAME_STATE_TEXT /
      DATA_STATE_TEXT / BATTERY_STATE_TEXT and the
      `<p class="text-body widget-verdict">` render shape — the tone and the
      markup idiom to mirror)
    - companion/layout.py lines 1210-1260 (`stat_tile()` — confirm `status`
      drives only the border class, so no change is needed there)
    - companion/pages/health_page.py lines 1023-1130 (`battery_status()`,
      `corroboration_status()`, `overall_severity()` — the state vocabulary)
    - .claude/skills/sketch-findings-skypane/SKILL.md (the label voice and the
      "no new tokens, no new accent consumer" contract)
  </read_first>
  <action>
    Add three module-level verdict dictionaries to `health_page.py`, modelled
    one-for-one on `home_page.py`'s `FRAME_STATE_TEXT`, mapping
    "ok"/"warn"/"error" to a short plain sentence, and name them for the signal
    they describe:
      DEVICE_STATE_TEXT — ok: "Checking in normally"; warn: "Has not checked in
        for a while"; error: "Has not checked in for a long time"
      PIPELINE_STATE_TEXT — ok: "Running on schedule"; warn: "A little behind";
        error: "Has not run for a long time"
      CORROBORATION_STATE_TEXT — ok: "Sources agree"; warn: "Sources disagreed
        recently"
    Keep the wording inside Home's register (a sentence a household member reads
    without a glossary); the exact copy beyond this mapping is Claude's
    Discretion per 19-CONTEXT.md.

    In `_device_section()` and `_pipeline_section()`, prepend the verdict
    paragraph to the existing timestamp row, using Home's exact markup shape
    `'<p class="text-body widget-verdict">%s</p>' % escape_html(...)`, keyed on
    the `state` each function already computes, with the same
    `.get(state, <warn entry>)` safe-fallback direction Home uses. The existing
    `stat-tile__value` timestamp row and (for pipeline) the
    `stat-tile__meta text-label section-caption` last-detection row both stay
    exactly as they are, after the verdict.

    In `render()`, the Corroboration tile's `content_html` gains the same verdict
    paragraph ahead of `corroboration_html`, keyed on
    `"warn" if disagreement_warn else "ok"` — the identical expression already
    passed as that tile's `status`, so the word and the colour can never
    disagree.

    The Resolution-rate tile is the one deliberate exception and must be
    documented as such in a comment: it is passed `status=None` and carries no
    pass/fail verdict anywhere in this module (no status function for it exists),
    so inventing a verdict word for it would assert a judgement the page does not
    make. Its rendered figure stays as-is.

    Update the quick-task-260901-tsa comment blocks in both section builders to
    record that D-03/A-21 reinstates a text verdict — and that this is NOT a
    revival of the duplicated-label defect that comment describes, because the
    verdict answers the tile's caption rather than repeating it (the caption
    names the signal, the verdict states the judgement, the value row gives the
    timestamp — three distinct rungs).

    Add to `companion/test_status_pages.py`: one check per tile asserting that a
    full `health_page.render(ctx)` at each of the three severities contains the
    matching verdict string inside a `widget-verdict` paragraph; one check
    asserting the Resolution-rate tile carries NO `widget-verdict` paragraph; and
    one check asserting every one of the three dictionaries has exactly the
    expected key set. Then re-derive `EXPECTED_CHECK_COUNT` by running the
    harness and appending a new last assignment citing 19-01-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^DEVICE_STATE_TEXT\|^PIPELINE_STATE_TEXT\|^CORROBORATION_STATE_TEXT" companion/pages/health_page.py` outputs `3`
    - `grep -c "widget-verdict" companion/pages/health_page.py` outputs at least `3`
    - `grep -v '^#' companion/pages/health_page.py | grep -c "stat-tile__value"` is unchanged from before this task
    - `server/.venv/bin/python3 companion/test_status_pages.py` reports exactly
      one remaining FAIL (the documented root-sandbox `anomaly_active()` case)
    - no new custom property, class name or colour literal is added to
      `companion/static/style.css` by this task: `git diff --stat companion/static/style.css` shows no change
  </acceptance_criteria>
  <done>Health's Device, Pipeline and Corroboration tiles each state their verdict in words; the Resolution-rate tile deliberately does not, with the reason recorded in source; harness green except the one documented sandbox failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite (`history_db`) -> Health render | device_health rows, including `battery_mv` and `ts`, are data the poll loop wrote; a corrupt or hostile value must degrade, never crash the page or escape unescaped |
| server-rendered HTML -> browser | every dynamic value must cross the single `escape_html()` choke point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering (XSS) | `_battery_reading_parts()` value text, the three verdict paragraphs | mitigate | every interpolated value goes through `layout.escape_html()` at the point of interpolation; the verdict text itself is a module constant, never request data. Proven by the existing hostile-timestamp harness check plus the new render-level checks in Tasks 2-3 |
| T-19-13 | Denial of Service | `battery.battery_percent()` on a non-numeric / non-positive / hostile `battery_mv` | mitigate | the moved function keeps its `try/except (TypeError, ValueError)` and `<= 0` guards verbatim and returns None rather than raising; Task 1's retargeted check pins all four degradation cases |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | this plan installs zero packages in any ecosystem; the Package Legitimacy Gate is skipped per its own no-external-packages exemption (19-RESEARCH.md, Standard Stack) |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_view_pages.py` -> `M/M checks pass`, exit 0
- `server/.venv/bin/python3 companion/test_status_pages.py` -> exactly one FAIL,
  the documented root-sandbox `anomaly_active()` non-existent-state-dir case
  (baseline was 162/163; do not "fix" it by weakening the test)
- `scripts/run-all-tests.sh` -> no NEW failures beyond the five pre-existing
  root-sandbox artifacts recorded in 18-AUDIT.md
</verification>

<success_criteria>
- `companion/battery.py` exists, exports `battery_percent`, and imports nothing
  under `companion/pages/` or `server/`
- Neither `home_page.py` nor `health_page.py` imports the other
- A seeded Health render shows "≈ NN% · NNNN mV"
- Health's Device, Pipeline and Corroboration tiles each render a
  `widget-verdict` paragraph whose word matches the `status` driving the border
- `EXPECTED_CHECK_COUNT` in both touched harnesses equals the count the harness
  actually printed, appended as a new last assignment citing this plan
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-01-SUMMARY.md` when done.
</output>

<source_audit>
## Multi-Source Coverage Audit (phase-wide — recorded once, here, in plan 01)

### GOAL (ROADMAP.md Phase 19)
| Item | Plan | Status |
|---|---|---|
| A-19..A-24 (Health defects) | 19-01, 19-05, 19-06, 19-09 | COVERED |
| A-25..A-31 (Settings defects) | 19-07, 19-10, 19-11, 19-12 | COVERED |
| A-32..A-35 (Auth & shell defects) | 19-02, 19-04 | COVERED |
| A-36, A-37 (Flights defects) | 19-03 | COVERED |
| A-38, A-39 (Airlines defects) | 19-08 | COVERED |
| A-40 remainder (screens seam) | 19-12 | COVERED |
| S-02 (next-wake countdown) | 19-12 | COVERED |
| S-04 (quiet-hours presets) | 19-10 | COVERED |

### REQ (phase requirement ids)
| ID | Plans | Status |
|---|---|---|
| CFG-01 | 19-02, 19-04, 19-07, 19-10, 19-11, 19-12 | COVERED |
| CFG-03 | 19-01, 19-05, 19-06, 19-09 | COVERED |
| CFG-04 | 19-06, 19-08 | COVERED |
| CFG-06 | 19-03 | COVERED |
| CFG-08 | 19-06 | COVERED |

### RESEARCH (19-RESEARCH.md constraints and pitfalls)
| Item | Plan | Status |
|---|---|---|
| Pitfall 1: re-derive EXPECTED_CHECK_COUNT by running the harness | every plan | COVERED |
| Pitfall 2: CSP vs inline swatch styles (resolved: `style-src 'self' 'unsafe-inline'`) | 19-04 | COVERED |
| Pitfall 3: revocation set needs a home + pruning + a lock | 19-02 | COVERED |
| Pitfall 4: D-15's fix is in `record_failure()`, not `locked_out()` | 19-02 | COVERED |
| Pitfall 5: D-02 must keep the nav dot, battery-trend.js and list-filter.js intact | 19-09 | COVERED |
| Pitfall 6: D-07 by signature widening, never a return-type change | 19-07 | COVERED |
| Pitfall 7: `submitted_calendar_signal()` gates 1-3 go dead, not deleted | 19-11 | COVERED |
| Assumption A3: BOTH inline poll scripts must move | 19-04 | COVERED |
| PATTERNS.md cross-cutting warning: D-12 must use `role="radiogroup"`, not `<fieldset>` | 19-11 | COVERED |
| `server/test_config_history.py` dict-equality checks gain `screen_id` | 19-12 | COVERED |
| Package Legitimacy Gate | n/a — zero package installs phase-wide | EXCLUDED (documented exemption) |

### CONTEXT (19-CONTEXT.md D-01..D-23)
| Decision | Plan |
|---|---|
| D-01, D-03 | 19-01 |
| D-04, D-05 | 19-05 |
| D-06 | 19-06 |
| D-02 | 19-09 |
| D-07 | 19-07 |
| D-08, D-12 | 19-11 |
| D-09, D-10, D-14 | 19-10 |
| D-11, D-13, D-23 | 19-12 |
| D-15, D-16, D-17 | 19-02 |
| D-18 | 19-04 |
| D-19, D-20 | 19-03 |
| D-21, D-22 | 19-08 (airlines side) + 19-12 (Device-page "Edit artwork" link) |

All 23 locked decisions are planned. No Deferred Idea (S-01, S-03, S-05, S-06,
calibrated battery curve, per-IP throttling) appears in any plan.
</source_audit>
