---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 06
type: execute
wave: 3
depends_on: ["19-05"]
files_modified:
  - companion/layout.py
  - companion/pages/health_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [CFG-03, CFG-04, CFG-08]
user_setup: []

must_haves:
  truths:
    - "D-06 (A-24): Health's tile labels and captions read in plain language — 'Do the two data sources agree?', 'Flight data last updated', 'Flights we could name'"
    - "D-06 (A-24): the corroboration rows read 'Both agree / Only one saw it / They disagree'"
    - "D-06 (A-24): 'adsbdb' is called 'the route database' in visible prose"
    - "D-06 (A-24): no requirement id (CFG-04) appears in any visible sentence on Health"
    - "D-06 (A-24): the technical term survives as a title tooltip on the label, so the page is still greppable/diagnosable by the developer"
  artifacts:
    - path: "companion/layout.py"
      provides: "stat_tile()'s optional caption_title tooltip, byte-identical output when unused"
      contains: "caption_title"
    - path: "companion/pages/health_page.py"
      provides: "plain-language labels, rows and prose with technical terms demoted to tooltips"
      contains: "Flight data last updated"
  key_links:
    - from: "companion/pages/health_page.py"
      to: "companion/layout.py"
      via: "stat_tile(..., caption_title=<technical term>)"
      pattern: "caption_title"
---

<objective>
Close A-24 (D-06): rewrite every piece of jargon on the Health page into
something a household member can parse, and keep the technical term reachable as
a `title` tooltip on the label rather than deleting it.

Purpose: Health is where the audit's audience framing bites hardest. Today it
says "Corroboration", "Single-source (uncorroborated)", "ADS-B pipeline last
ran", "adsbdb", "Resolution rate", and — in a visible sentence — "CFG-04's
registry". None of those are readable without the source code.
Output: plain-language labels, rows and prose on Health, with the technical
vocabulary preserved in tooltips, and a harness that pins the new copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
The tile wrapper to widen — `companion/layout.py:1218`:
  def stat_tile(caption, content_html, status=None, icon=None)
      # `caption` is escaped here; `content_html` is already-safe markup passed
      # through verbatim; `icon` is a whitelisted ICON_IDS member, never markup.
      # Its docstring states the deliberate rule that this function must never
      # grow a second free-form raw-markup parameter — a `title` ATTRIBUTE value
      # is not raw markup, so a fully-defaulted `caption_title` is compatible
      # with that rule. Output MUST stay byte-identical when it is not passed:
      # plan 19-07 owns test_companion_app.py in this same wave and several
      # checks there match stat_tile's markup literally.

Copy constants to rewrite — `companion/pages/health_page.py`:
  PIPELINE_FRESHNESS_LABEL = "ADS-B pipeline last ran"          # :195
  DEVICE_FRESHNESS_LABEL   = "Device last checked in"           # :194 — already plain, keep
  LAST_DETECTION_LABEL     = "Last aircraft detected"           # :200 — already plain, keep
  RESOLUTION_RATE_LABEL    = "Resolution rate"                  # :319
  UNRESOLVED_SECTION_HEADING = "Unresolved prefixes"            # :320
  STATS_SECTION_HEADING      = "Resolution statistics"          # :321
  _CORROBORATION_ROWS = (                                       # :136-149
      ("True",  "Agreement", "ok", "Both ADS-B sources independently selected the same aircraft."),
      ("None",  "Single-source (uncorroborated)", "ok", "Only one source returned a result this cycle — …"),
      ("False", "Disagreement", "warn", "The two sources named different aircraft, so nothing was selected …"),
  )
  _SOURCE_ROWS = (...)   # :382-398 — three of the five glosses say "adsbdb";
                         # the "miss" gloss says "this is exactly what CFG-04's
                         # registry above tracks."
  _READ_ONLY_NOTE = (...)                                       # :357
  SOURCE_FAULT_HEADING = "ADS-B source outage"                  # :162
  SCREEN_SECTION_DESCRIPTION / SERVER_DATA_SECTION_DESCRIPTION  # :337-340

Render sites — `companion/pages/health_page.py`:
  render()'s three `layout.stat_tile(...)` calls                # :2423-2441
      # the Corroboration tile passes the literal "Corroboration" as its caption
  def _corroboration_section(counts)                            # :1804 — the
      # three always-visible rows, each `layout.status_dot(status, label)`
  def _corroboration_details_html()                             # :1786 — the
      # <details> <dt>/<dd> pairs carrying each row's explanation
  def _section_intro_html(section_id, heading, description)      # :1380 — emits a
      # byte-pinned '<h2 id="…" class="text-heading">…</h2>'; do NOT change its shape

D-06's locked mapping (19-CONTEXT.md):
  "Corroboration"                      -> "Do the two data sources agree?"
  "Agreement"                          -> "Both agree"
  "Single-source (uncorroborated)"     -> "Only one saw it"
  "Disagreement"                       -> "They disagree"
  "ADS-B pipeline last ran"            -> "Flight data last updated"
  "adsbdb"                             -> "the route database"
  "Resolution rate"                    -> "Flights we could name"
  the "CFG-04's registry" sentence     -> rewritten with no requirement id
Everything beyond this mapping is Claude's Discretion, inside the SKILL.md label
voice and the tone Home's FRAME_STATE_TEXT/DATA_STATE_TEXT dictionaries set.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give stat_tile an optional caption tooltip, byte-identical when unused</name>
  <files>companion/layout.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/layout.py lines 1210-1260 (`stat_tile()` in full — its docstring's
      "never grow a second free-form raw-markup parameter" rule, and both the
      icon and no-icon caption branches)
    - companion/layout.py `escape_html()` and `icon_html()` (so you know exactly
      which values are already escaped)
    - companion/test_status_pages.py — grep for `stat_tile` and `stat-tile__caption`
      and read every pinned check in full
    - companion/test_companion_app.py — grep for `stat-tile` (read only; this
      plan must NOT edit that file, plan 19-07 owns it this wave). Confirm with
      your own eyes that every check there matches markup that a
      not-passed `caption_title` leaves unchanged.
  </read_first>
  <action>
    Widen `layout.stat_tile()` to
    `stat_tile(caption, content_html, status=None, icon=None, caption_title=None)`.
    When `caption_title` is falsy, the returned string must be BYTE-IDENTICAL to
    today's output on both the icon and no-icon branches — that is the
    compatibility contract with every existing pinned check, including the ones in
    `companion/test_companion_app.py` that this plan does not own.

    When `caption_title` is a non-empty string, add a
    `title="{escape_html(caption_title)}"` attribute to the
    `<p class="text-label stat-tile__caption">` element (the label itself, per
    D-06's "the technical term stays as a `title` tooltip on the label"), and
    nothing else changes. Extend the docstring: state that `caption_title` is an
    ATTRIBUTE VALUE, escaped here, and therefore does not violate the standing
    no-second-raw-markup-parameter rule; and state the D-06 purpose — the visible
    label is plain language for a household reader while the technical term stays
    one hover away for the developer.

    Add to `companion/test_status_pages.py`: a check that
    `stat_tile("C", "<p>x</p>", "ok", None)` equals
    `stat_tile("C", "<p>x</p>", "ok", None, caption_title=None)` and equals
    `stat_tile("C", "<p>x</p>", "ok", None, caption_title="")` (the
    byte-identical-when-unused contract); a check that a supplied
    `caption_title` appears as a `title="…"` attribute on the caption element and
    nowhere else; and a check that a `caption_title` containing `"` and `<` is
    escaped. Re-derive `EXPECTED_CHECK_COUNT` by running the harness and
    appending a NEW last assignment citing 19-06-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.layout as l;print(l.stat_tile('C','<p>x</p>','ok') == l.stat_tile('C','<p>x</p>','ok',None,None))"`
      prints `True`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.layout as l;print('title=\"Corroboration\"' in l.stat_tile('C','<p>x</p>','ok',None,'Corroboration'))"`
      prints `True`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.layout as l;print('&lt;' in l.stat_tile('C','<p>x</p>',None,None,'a<b'))"` prints `True`
    - `server/.venv/bin/python3 companion/test_companion_app.py` reports exactly
      the two documented root-sandbox FAILs (no stat_tile check regressed)
    - `git diff --stat companion/test_companion_app.py` shows no change
  </acceptance_criteria>
  <done>stat_tile accepts an escaped caption tooltip; its output is provably unchanged when the parameter is not passed; test_companion_app.py is untouched and still green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Health's tile labels and corroboration rows in plain language (D-06)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/pages/health_page.py lines 130-205 (`_CORROBORATION_ROWS` with its
      four-tuple shape and the comment recording that its explanations come from
      `detect.poll_current_aircraft()`'s documented three-outcome semantics;
      `SOURCE_FAULT_HEADING`, `DEVICE_FRESHNESS_LABEL`,
      `PIPELINE_FRESHNESS_LABEL`, `LAST_DETECTION_LABEL`)
    - companion/pages/health_page.py lines 310-345 (`RESOLUTION_RATE_LABEL`,
      `UNRESOLVED_SECTION_HEADING`, `STATS_SECTION_HEADING`, and the two
      section-description constants with their "keep the leading em-dash"
      instruction)
    - companion/pages/health_page.py lines 1786-1825 (`_corroboration_details_html()`
      and `_corroboration_section()` — where each row's label and explanation render)
    - companion/pages/health_page.py lines 2415-2445 (`render()`'s three
      `stat_tile()` calls, including the literal `"Corroboration"` caption)
    - companion/pages/home_page.py lines 89-104 (the tone to match)
    - .claude/skills/sketch-findings-skypane/SKILL.md (the unified label voice
      covering `.stat-tile__caption` — 12px uppercase semibold; a long
      question-shaped label must still fit that treatment at 240px, so keep it
      short)
    - companion/test_status_pages.py — grep for `Corroboration`, `Agreement`,
      `Single-source`, `Disagreement`, `pipeline last ran`, `Resolution rate`
      and read EVERY pinned check that asserts one of those literals
  </read_first>
  <action>
    Apply D-06's locked mapping, keeping every constant's NAME unchanged so no
    unrelated reference breaks — only the string values change:
      `PIPELINE_FRESHNESS_LABEL = "Flight data last updated"`
      `RESOLUTION_RATE_LABEL = "Flights we could name"`
      `_CORROBORATION_ROWS`' three labels become "Both agree", "Only one saw it",
        "They disagree", leaving each row's stored key ("True"/"None"/"False"),
        status and explanation position in the four-tuple untouched.
    Add sibling constants carrying the technical term for the tooltip, named for
    the label they annotate (e.g. `PIPELINE_FRESHNESS_TITLE = "ADS-B pipeline
    last ran"`, `CORROBORATION_TILE_TITLE = "Corroboration"`,
    `RESOLUTION_RATE_TITLE = "Route resolution rate"`), and a new
    `CORROBORATION_TILE_LABEL = "Do the two data sources agree?"` replacing the
    literal `"Corroboration"` currently inlined at the `stat_tile()` call site —
    this file's convention is constants at the top, never literals at a render
    site.

    Pass each tooltip through the `caption_title` parameter Task 1 added, at all
    three `stat_tile()` call sites in `render()`. The Device tile's label is
    already plain ("Device last checked in") — give it a `caption_title` only if
    there is a genuine technical term to preserve; if there is not, pass nothing
    and say so in a comment rather than inventing one.

    Also rewrite each corroboration row's EXPLANATION into the same register: the
    `<details>` block's `<dd>` text should say what happened in ordinary words
    ("Both of the frame's flight-data sources picked the same aircraft.") while
    keeping the substance the existing comment says it inherits from
    `detect.poll_current_aircraft()`'s three documented outcomes — do not change
    what the rows MEAN, only how they read. Keep the "Only one saw it" row's
    crucial clarification that this is not a disagreement.

    Extend the `_CORROBORATION_ROWS` comment to record the D-06 rewrite and that
    the stored keys are the on-disk vocabulary and must never be renamed to match
    the new labels.

    Harness work in `companion/test_status_pages.py`: retarget IN PLACE — same
    check names, updated expected literals, with a comment recording the D-06
    retarget — every check asserting one of the old strings. Add a check
    asserting a full `health_page.render(ctx)` contains none of "Corroboration"
    as visible text outside a `title="…"` attribute, none of
    "Single-source (uncorroborated)", and none of "pipeline last ran" outside a
    `title="…"` attribute. Add a check asserting each of the three tiles' caption
    elements carries a `title` attribute whose value is the matching technical
    constant. Re-derive `EXPECTED_CHECK_COUNT` by running the harness and
    appending a new last assignment citing 19-06-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import health_page as h;print(h.PIPELINE_FRESHNESS_LABEL, '|', h.RESOLUTION_RATE_LABEL, '|', [r[1] for r in h._CORROBORATION_ROWS])"`
      prints `Flight data last updated | Flights we could name | ['Both agree', 'Only one saw it', 'They disagree']`
    - `grep -c "Single-source (uncorroborated)" companion/pages/health_page.py` outputs `0`
    - `grep -c "caption_title=" companion/pages/health_page.py` outputs at least `2`
    - `server/.venv/bin/python3 companion/test_status_pages.py` reports exactly
      one FAIL, the documented root-sandbox `anomaly_active()` case
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0
  </acceptance_criteria>
  <done>Health's three tile labels and the three corroboration rows read in plain language; each technical term is one hover away; every pinned copy check is retargeted, none dropped.</done>
</task>

<task type="auto">
  <name>Task 3: De-jargon the registry and statistics prose (D-06, CFG-04/CFG-08 surfaces)</name>
  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/pages/health_page.py lines 345-400 (`_NO_GAPS_HEADING`,
      `_NO_GAPS_BODY`, `_READ_ONLY_NOTE`, `_NO_STATS_HEADING`, `_NO_STATS_BODY`,
      `RESOLUTION_WINDOW_DAYS`, and `_SOURCE_ROWS` in full — including the long
      comment explaining why "airline_only" and "manual" are deliberately
      distinct, which must survive the rewrite)
    - companion/pages/health_page.py lines 155-185 (`SOURCE_FAULT_HEADING`,
      `SOURCE_FAULT_BODY`, `_ADSB_PROVIDER_NAMES`, `ANOMALY_BANNER_TEXT` and its
      comment stating that constant is asserted as a literal SUBSTRING by several
      harness checks — so any edit to it must be deliberate and retargeted)
    - companion/pages/health_page.py `_registry_section()` and `_stats_table_html()`
      (the two render sites for this prose)
    - companion/pages/health_page.py lines 330-341 (the two section descriptions
      and their "keep the leading em-dash" instruction)
    - companion/test_status_pages.py — grep for `adsbdb`, `CFG-04`, `prefix`,
      `Unresolved`, `Resolution statistics`, `read-only` and read every pinned
      check in full
  </read_first>
  <action>
    Replace every visible occurrence of "adsbdb" in `_SOURCE_ROWS`' glosses and in
    any other visible prose with "the route database", keeping each gloss's
    meaning intact. Where the distinction between the live lookup, the cache and
    the static prefix table matters (it does — the existing comment says
    collapsing "airline_only" and "manual" would hide which mechanism did the
    work), phrase it in ordinary words rather than deleting the distinction. Keep
    the word "adsbdb" itself available to the developer via a `title` attribute on
    the affected row label, or in the source comment, and say which you chose.

    Rewrite the "miss" gloss so it no longer contains "CFG-04's registry" — no
    requirement id may appear in any visible sentence on this page. Point the
    reader at the card by its on-screen name instead (e.g. "…so the callsign shows
    up in the unidentified-airlines list above."). Then grep the whole module for
    `CFG-0` inside STRING LITERALS (not comments) and rewrite every hit the same
    way; requirement ids stay in comments and docstrings, where they are useful,
    and disappear from rendered text.

    Rename the two card headings into the same register while keeping their
    constant names: `UNRESOLVED_SECTION_HEADING` becomes something a household
    member parses (e.g. "Airlines we could not name"), and
    `STATS_SECTION_HEADING` likewise (e.g. "How well we name flights"). Keep
    both short enough for the nested-card-title tier
    (16px sans semibold per SKILL.md). `_READ_ONLY_NOTE`, `_NO_GAPS_BODY` and
    `_NO_STATS_BODY` get the same treatment: no "prefix", no "ICAO", no
    "registry" in the plain sentence, with the technical vocabulary demoted to a
    tooltip or a comment.

    Do NOT change `SERVER_DATA_SECTION_ID` or `SCREEN_SECTION_ID` — the id
    `#server-data` is a documented cross-page anchor `history_page.py` links to,
    and renaming it silently breaks that link. Do NOT change
    `ANOMALY_BANNER_TEXT` unless you also retarget every check that asserts it as
    a literal substring; if you do change it, say so explicitly in the SUMMARY.

    Harness work in `companion/test_status_pages.py`: retarget in place every
    check asserting one of the rewritten literals. Add a check asserting that a
    full `health_page.render(ctx)` — seeded with a non-empty unresolved registry
    AND with stats rows, so every branch renders — contains no occurrence of
    "adsbdb" and no occurrence of the regex `CFG-\d` outside a `title="…"`
    attribute. Re-derive `EXPECTED_CHECK_COUNT` by running the harness and
    appending a new last assignment citing 19-06-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adsbdb" companion/pages/health_page.py` counts only comment lines:
      `grep -v '^\s*#' companion/pages/health_page.py | grep -c '"[^"]*adsbdb'` outputs `0`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import re;from companion.pages import health_page as h;bad=[r for r in h._SOURCE_ROWS if 'CFG-' in r[2] or 'adsbdb' in r[2]];print(bad)"`
      prints `[]`
    - `grep -c "SERVER_DATA_SECTION_ID = \"server-data\"" companion/pages/health_page.py` outputs `1`
    - `server/.venv/bin/python3 companion/test_status_pages.py` reports exactly
      one FAIL, the documented root-sandbox `anomaly_active()` case
    - `scripts/run-all-tests.sh` shows no NEW failures beyond the five
      pre-existing root-sandbox artifacts
  </acceptance_criteria>
  <done>No visible sentence on Health names adsbdb, an ICAO prefix registry or a requirement id; the cross-page #server-data anchor is intact; every pinned copy check is retargeted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered HTML -> browser | the new `title` attribute values enter attribute context and must be escaped there |
| SQLite / `poll_state.json` -> Health prose | the registry and stats rows interpolated into the rewritten copy are still pipeline-written data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering (XSS via attribute injection) | `layout.stat_tile()`'s new `caption_title` | mitigate | the value is escaped inside `stat_tile()` with `escape_html()` before interpolation into the `title="…"` attribute, and every caller passes a module constant, never request data; pinned by Task 1's escaping check with a `"`/`<` payload |
| T-19-24 | Information Disclosure (internal detail in UI) | requirement ids and internal module names in visible prose | mitigate | Task 3 removes every `CFG-\d` and `adsbdb` occurrence from rendered text, demoting them to tooltips and comments; pinned by the render-level regex check. This is a small but real reduction in what an unauthenticated-but-phished screenshot reveals about the system's internals |
| T-19-25 | Tampering (silent broken cross-page link) | `SERVER_DATA_SECTION_ID` = `#server-data` | mitigate | the id is explicitly excluded from the rename and pinned by an equality check, because `history_page.py` links to that exact anchor |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_status_pages.py` -> exactly one FAIL,
  the documented root-sandbox `anomaly_active()` case
- `server/.venv/bin/python3 companion/test_companion_app.py` -> exactly the two
  documented root-sandbox FAILs (proving the `stat_tile()` widening broke nothing
  in the harness this plan does not own)
- `server/.venv/bin/python3 companion/test_contrast_check.py` -> `36/36`
- `scripts/run-all-tests.sh` -> no NEW failures
- `<human-check>` (end-of-phase): read the whole Health page aloud as a household
  member would; no sentence should require the source code. Hovering each tile
  label should reveal its technical term.
</verification>

<success_criteria>
- `stat_tile()` output is byte-identical when `caption_title` is not passed
- The three mapped labels and the three corroboration rows carry D-06's exact
  replacement strings
- No visible text on Health contains "adsbdb", "Single-source (uncorroborated)"
  or a `CFG-\d` requirement id
- Each rewritten label's technical term is reachable as a `title` attribute
- `#server-data` is unchanged
- `EXPECTED_CHECK_COUNT` re-derived by running the harness
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-06-SUMMARY.md` when done.
</output>
