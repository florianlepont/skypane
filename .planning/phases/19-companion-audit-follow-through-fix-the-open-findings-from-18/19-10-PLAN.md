---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 10
type: execute
wave: 4
depends_on: ["19-03", "19-07"]
files_modified:
  - companion/static/dirty-state.js
  - companion/static/style.css
  - companion/pages/config_page.py
  - companion/test_config_page.py
autonomous: true
requirements: [CFG-01]
user_setup: []

must_haves:
  truths:
    - "D-09 (A-27): the static 'Save settings' fallback button hides only once dirty-state.js has actually initialised the bar, never on the mere presence of the .js class"
    - "D-10 (A-28): navigating away with unsaved settings edits raises the browser's own confirmation; the bar's Save and Cancel clear it"
    - "D-14 (S-04): the Quiet hours group offers three preset buttons — Night (23:00-07:00), Work day (08:00-18:00), Always on (off) — that fill the inputs client-side and mark the form dirty"
    - "D-14: no server change — the presets submit through the same quiet_hours_start / quiet_hours_end fields the form already has"
  artifacts:
    - path: "companion/static/dirty-state.js"
      provides: "the dirty-ready marker, the beforeunload guard, and the quiet-hours preset fill"
      contains: "dirty-ready"
    - path: "companion/pages/config_page.py"
      provides: "the three preset buttons with data-preset-start / data-preset-end attributes"
      contains: "data-preset-start"
    - path: "companion/static/style.css"
      provides: "the fallback-hide rule keyed on .dirty-ready instead of .js"
      contains: "dirty-ready"
  key_links:
    - from: "companion/static/style.css"
      to: "companion/static/dirty-state.js"
      via: ".dirty-ready [data-static-save-fallback] { display: none } — the class the script adds only after the bar is confirmed present"
      pattern: "dirty-ready"
    - from: "companion/pages/config_page.py"
      to: "companion/static/dirty-state.js"
      via: "the data-preset-start / data-preset-end attribute pair on each preset button"
      pattern: "data-preset-"
---

<objective>
Close A-27 (D-09), A-28 (D-10) and S-04 (D-14): stop hiding the only working Save
button on a page whose save bar never initialised, warn before discarding unsaved
settings edits, and give Quiet hours three one-tap presets.

Purpose: today the fallback Save button hides whenever `nav-dropdown.js` sets the
`.js` class on `<html>` — a completely different file that says nothing about
whether `dirty-state.js` succeeded, so any JS hiccup in the bar leaves no way to
save at all. Separately, Add rule / Delete / Trigger poll all navigate away and
silently discard unsaved edits.
Output: a `dirty-ready` marker the stylesheet keys on, a `beforeunload` guard
gated on the existing diff count, and three server-rendered preset buttons filled
by the same script that already owns the form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
Current script — `companion/static/dirty-state.js` (186 lines). Structure:
  (function () { "use strict";
    var form = document.querySelector("form[data-dirty-form]");
    if (!form) { return; }                                   # :43-46
    var bar      = document.querySelector("[data-dirty-bar]");
    var countEl  = document.querySelector("[data-dirty-count]");
    var cancelBtn= document.querySelector("[data-dirty-cancel]");
    if (!bar || !countEl) { return; }                         # :50-53
    ... snapshotValues() / countDifferences() (:83) / dirtySectionLabels() / updateBar() (:140)
    form.addEventListener("change", updateBar);               # :172
    form.addEventListener("input", updateBar);                # :173
    if (cancelBtn) { cancelBtn.addEventListener("click", function () {
        form.reset(); bar.hidden = true; }); }                # :175-180
  })();
  # Its header comment (lines 1-39) explicitly states that hiding the bottom
  # Save button is NOT this file's job and is done by the `.js`-gated CSS rule
  # driven by nav-dropdown.js's unconditional class-add. D-09 reverses exactly
  # that split, so that paragraph must be rewritten, not contradicted silently.
  # Standing constraints: ES5-safe subset (no let/const/arrow/backtick), no
  # innerHTML, no other HTML-writing sink. Enforced by
  # test_config_page.py's `_dirty_state_js_references_dirty_section_attr_and_
  # has_no_forbidden_syntax()` (~:2320), whose ban list is
  # ("innerHTML", "let ", "const ", "=>", "`").

The CSS rule to retarget — `companion/static/style.css:704`:
  .js [data-static-save-fallback] { display: none; }
  # `.js` is added unconditionally by nav-dropdown.js. Pinned by
  # test_config_page.py's `_style_css_references_static_save_fallback_attr()`
  # (~:2332), which finds `config_page.STATIC_SAVE_FALLBACK_ATTR` in the file and
  # requires "display: none" within the following 120 characters — that check
  # survives a selector change, but its NAME says ".js-gated" and must be
  # retargeted with a comment.
  # Note `.js .mobile-nav` rules (:676, :682, :4821) also use `.js` — leave them.

The button being hidden — `companion/pages/config_page.py`:
  STATIC_SAVE_FALLBACK_ATTR = "data-static-save-fallback"     # :269
  render()'s '<button type="submit" %s>Save settings</button>' # ~:1780
  # test_config_page.py:1332 pins "exactly one data-static-save-fallback
  # occurrence" — do not change the markup, only what hides it.

Quiet hours group — `companion/pages/config_page.py:830-887` `quiet_hours_group()`:
  # emits '.theme-status' wrapper, an <h2 class="text-heading">, a
  # '<p class="text-label section-caption">', a '.settings-checkbox' label, then
  # '<label>Start <input type="time" name="quiet_hours_start" value="%s"></label>'
  # and the matching End input. Plan 19-07 (previous wave) widened this function
  # with errors/submitted parameters and added `required` to both inputs — read
  # 19-07-SUMMARY.md and the current source before editing.
  # Its docstring locks the control ORDER (checkbox, Start, End, each its own
  # full-width line) and states the inputs are never disabled. D-14 adds the
  # presets ABOVE the time inputs.
  DIRTY_SECTION_ATTR = "data-dirty-section"                   # :268 — the wrapper
  # attribute dirty-state.js reads by literal value

Design contract constraints (.claude/skills/sketch-findings-skypane/SKILL.md):
  - bare `button` is the quiet-button treatment: 30px tall at >=960px, 36px/14px
    below it, near-invisible wash at rest revealing on hover. Three preset
    buttons should use the bare element and inherit that, adding no new class
    geometry if avoidable.
  - no new design token, no new accent consumer.
  - `<input type="time">` keeps the 44px touch-target floor — do not let the
    preset row change that.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide the fallback Save button only once the bar really initialised (D-09)</name>
  <files>companion/static/dirty-state.js, companion/static/style.css, companion/test_config_page.py</files>
  <read_first>
    - companion/static/dirty-state.js lines 1-60 (the whole header comment plus
      both guard clauses — you must read the paragraph that says hiding the
      bottom Save button is not this file's job, because D-09 makes it this
      file's job)
    - companion/static/nav-dropdown.js (find its unconditional `.js` class-add and
      confirm with your own eyes that it says nothing about dirty-state.js
      succeeding — that is the A-27 defect)
    - companion/static/style.css line ~704 (`.js [data-static-save-fallback]`) and
      lines ~676, ~682, ~4821 (the three `.js .mobile-nav` rules that must NOT
      change)
    - companion/test_config_page.py lines 2305-2350 (both cross-file static-asset
      guards in full — the ban list and the index-plus-window technique)
    - companion/test_config_page.py line ~1332 (the "exactly one
      data-static-save-fallback occurrence" check)
    - 19-07-SUMMARY.md (plan 19-07 edited style.css and config_page.py in the
      previous wave)
  </read_first>
  <action>
    In `companion/static/dirty-state.js`, immediately AFTER the
    `if (!bar || !countEl) { return; }` guard succeeds — i.e. at the first point
    where the bar is proven present and this script is proven to have reached
    initialisation — add
    `document.documentElement.classList.add("dirty-ready");` with a
    `setAttribute("class", ...)` fallback only if you find evidence this codebase
    needs one for `<html>` (`nav-dropdown.js` is the precedent; match whatever it
    does rather than inventing a different approach).

    Rewrite the header paragraph that currently explains why hiding the bottom
    Save button is NOT this file's job: record it as SUPERSEDED by D-09/A-27,
    state the defect it caused (the `.js` class comes from a different file and
    is set unconditionally, so any failure in THIS file left the page with a
    hidden fallback button and no working bar — no way to save at all), and state
    the new contract: this file adds `dirty-ready` to `<html>` only after the bar
    is confirmed, and the stylesheet keys the hide on that class alone.

    In `companion/static/style.css`, change the selector at ~line 704 from
    `.js [data-static-save-fallback]` to
    `.dirty-ready [data-static-save-fallback]`, keeping `display: none;` and the
    rule's position. Add a comment naming D-09/A-27 and stating that `.js` is set
    by `nav-dropdown.js` unconditionally while `dirty-ready` is set by
    `dirty-state.js` only after the bar exists, so this rule now hides the
    fallback exactly when a replacement is actually available. Do not touch the
    three `.js .mobile-nav` rules.

    In `companion/test_config_page.py`: retarget
    `_style_css_references_static_save_fallback_attr()` in place — keep the check
    callable's name, update its check() description from ".js-gated" to
    ".dirty-ready-gated", add a comment recording the D-09 retarget, and extend
    its body to additionally assert that the 120-character window contains
    `dirty-ready` and does NOT contain the old `.js ` prefix for this attribute.
    Extend
    `_dirty_state_js_references_dirty_section_attr_and_has_no_forbidden_syntax()`
    to also assert the script source contains the literal `dirty-ready`, so the
    CSS and the script cannot drift. Add a check asserting the script adds
    `dirty-ready` only AFTER its bar guard — i.e. that the first occurrence of
    `dirty-ready` in the source comes after the first occurrence of
    `data-dirty-bar`, the same source-ordering technique
    `test_companion_app.py`'s `_panel_lookup_optional_replace_lookup_stays_
    outside_mandatory_guard` check already uses. Re-derive
    `EXPECTED_CHECK_COUNT` by running the harness and appending a NEW last
    assignment citing 19-10-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dirty-ready" companion/static/dirty-state.js` outputs at least `1`
    - `grep -c "dirty-ready" companion/static/style.css` outputs at least `1`
    - `grep -c "^\.js \[data-static-save-fallback\]" companion/static/style.css` outputs `0`
    - `grep -c "\.js \.mobile-nav" companion/static/style.css` is unchanged from before this task
    - `grep -c "let \|const \|=>\|innerHTML" companion/static/dirty-state.js` outputs `0`
    - `server/.venv/bin/python3 -c "src=open('companion/static/dirty-state.js').read();print(src.index('dirty-ready') > src.index('data-dirty-bar'))"`
      prints `True`
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with `M/M checks pass`
  </acceptance_criteria>
  <done>The fallback Save button hides only when dirty-state.js has proven the bar exists; the mobile-nav `.js` rules are untouched; the CSS and the script are pinned to the same class name and to the correct source ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Warn before discarding unsaved settings edits (D-10)</name>
  <files>companion/static/dirty-state.js, companion/test_config_page.py</files>
  <read_first>
    - companion/static/dirty-state.js lines 80-186 (`countDifferences()`,
      `dirtySectionLabels()`, `updateBar()`, the change/input listeners and the
      cancel handler — you must reuse `countDifferences()`, never reimplement the
      diff count)
    - companion/pages/config_page.py `render()`'s `dirty_bar_html` (~:1705-1715)
      and the long quick-task-260901-re6 comment above it, which explains that
      the bar's Save button lives OUTSIDE the form and submits via
      `form="{SETTINGS_FORM_ID}"` — so there is no JS click handler on Save to
      hook, only a native submit
    - companion/pages/config_page.py's rules-editor and poll sections (the
      Add rule / Delete / Trigger poll forms and links that navigate away — the
      exact A-28 trigger)
    - companion/test_config_page.py — grep for `beforeunload` (expect zero hits;
      this is new coverage)
  </read_first>
  <action>
    Add a `beforeunload` guard to `companion/static/dirty-state.js`, beside the
    existing `form.addEventListener("change"/"input", updateBar)` pair:
      `window.addEventListener("beforeunload", function (evt) { ... })`
    which, when `countDifferences() > 0` AND a `suppressGuard` flag is false,
    calls `evt.preventDefault()` and sets `evt.returnValue = ""` (both forms are
    needed for cross-browser coverage; the browser supplies its own copy — never
    try to set a custom message, modern browsers ignore it).

    Clear the guard on the two legitimate exits, using a module-scoped
    `suppressGuard` boolean set to true:
      - the form's own `submit` event — one
        `form.addEventListener("submit", ...)` covers BOTH the in-form bottom
        Save button and the bar's out-of-form Save button, because the latter
        submits this same form natively via its `form="{SETTINGS_FORM_ID}"`
        attribute. Say that in a comment, citing the 260901-re6 reasoning, so no
        future reader adds a second, redundant click handler on the bar's Save;
      - the existing cancel handler, immediately after its `form.reset()` and
        `bar.hidden = true` lines (a reset restores the snapshot values, so
        `countDifferences()` would already return 0 — set the flag anyway and
        explain that it is belt-and-braces against a browser whose `reset()`
        timing races the unload).

    Do NOT suppress the guard for the Add rule / Delete / Trigger poll forms:
    those are exactly the navigations A-28 is about, and the guard firing there is
    the intended behaviour.

    Extend the header comment with a short paragraph naming D-10/A-28, the one
    predicate the guard uses (`countDifferences() > 0`, reused not reimplemented),
    and the two exits that clear it.

    In `companion/test_config_page.py`, add checks reading
    `companion/static/dirty-state.js` from disk: that it registers a
    `beforeunload` listener; that the listener's body references
    `countDifferences` (the guard is keyed on the real diff count, not a separate
    flag that can drift); that `returnValue` and `preventDefault` both appear;
    that a `submit` listener exists on the form; and that the file still contains
    none of `innerHTML`, `let `, `const `, `=>`, backtick. Re-derive
    `EXPECTED_CHECK_COUNT` by running the harness and appending a new last
    assignment citing 19-10-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "beforeunload" companion/static/dirty-state.js` outputs at least `1`
    - `grep -c "returnValue" companion/static/dirty-state.js` outputs at least `1`
    - `grep -c 'addEventListener("submit"' companion/static/dirty-state.js` outputs at least `1`
    - `grep -c "let \|const \|=>\|innerHTML" companion/static/dirty-state.js` outputs `0`
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with `M/M checks pass`
  </acceptance_criteria>
  <done>Leaving the Settings page with unsaved edits raises the browser's own confirmation; a real Save or Cancel clears it; the guard reuses the existing diff count.</done>
</task>

<task type="auto">
  <name>Task 3: Add the three Quiet hours presets (D-14 / S-04)</name>
  <files>companion/pages/config_page.py, companion/static/dirty-state.js, companion/test_config_page.py</files>
  <read_first>
    - companion/pages/config_page.py lines 220-250 (the quiet-hours section
      constants) and the CURRENT source of `quiet_hours_group()` (plan 19-07
      changed it last wave — read the live file, not the pre-19-07 shape, and
      read 19-07-SUMMARY.md)
    - companion/pages/config_page.py lines 260-275 (`DIRTY_SECTION_ATTR`,
      `STATIC_SAVE_FALLBACK_ATTR` — the data-attribute naming convention to match)
    - companion/static/dirty-state.js (the whole file as modified by Tasks 1-2 —
      you need `snapshotValues()`, `countDifferences()`, `updateBar()` and both
      guard clauses to decide where the preset block goes)
    - server/device_config.py lines 54-60 (`DEFAULT_QUIET_HOURS_START` /
      `DEFAULT_QUIET_HOURS_END` — the "Night" preset must match the shipped
      defaults, not a retyped guess) and `normalise_quiet_hours_time()`
    - .claude/skills/sketch-findings-skypane/SKILL.md (the bare-`button` quiet
      treatment and its 30px/36px geometry; the `<input type="time">` 44px
      touch-target floor; "no new tokens, no new accent consumer")
    - companion/test_config_page.py — grep for `quiet_hours` and read every
      pinned check on that group's markup
  </read_first>
  <action>
    In `companion/pages/config_page.py`, add three copy/value constants for the
    presets, sourcing the Night values from
    `device_config.DEFAULT_QUIET_HOURS_START` / `DEFAULT_QUIET_HOURS_END` rather
    than retyping "23:00"/"07:00", so the preset and the shipped default can
    never drift:
      Night      -> label "Night (23:00–07:00)" built from those two constants,
                    `data-preset-start` = start, `data-preset-end` = end
      Work day   -> "Work day (08:00–18:00)", 08:00 / 18:00
      Always on  -> "Always on (off)", which must UNCHECK the enable checkbox and
                    leave both times untouched (that is what "off" means here —
                    the times stay configured, the curfew is disabled, exactly the
                    pre-configure-before-enabling behaviour `quiet_hours_group()`'s
                    docstring locks). Express it with a distinct attribute, e.g.
                    `data-preset-enabled="0"`, rather than overloading the time
                    attributes.
    Use real U+2013 en dashes in the ranges, matching this module's real-Unicode
    convention.

    Emit the three as `<button type="button" data-quiet-preset …>` elements inside
    a single wrapper, placed ABOVE the two `<input type="time">` labels and BELOW
    the enable checkbox, so the locked control order (checkbox, then the times) is
    preserved with the presets sitting between the checkbox and the times as an
    affordance for them. `type="button"` is load-bearing: a default-type button
    inside the form would submit it. Use the bare `button` element with no new
    class so it inherits the existing quiet-button treatment and this task adds
    NO CSS beyond what Task 1 already changed. Give the wrapper a plain class only
    if layout genuinely requires one, and if so reuse an existing flex/row class
    from the stylesheet rather than declaring a new rule.

    Escape every attribute value and every label at its interpolation point.
    Extend `quiet_hours_group()`'s docstring: record D-14/S-04, that the presets
    are a CLIENT-SIDE affordance only with no server change (they write into the
    same `quiet_hours_start`/`quiet_hours_end`/`quiet_hours_enabled` fields the
    form already posts, and `handle_post()`'s validation is untouched), and that a
    no-JS browser simply sees three inert buttons that do nothing — an acceptable
    degradation because the time inputs themselves remain fully usable.

    In `companion/static/dirty-state.js`, add a guarded preset block. Place it
    BEFORE the `if (!bar || !countEl) { return; }` guard so the presets keep
    working on a page whose save bar failed to initialise (that is the whole point
    of D-09), and route its dirty notification through a tiny `notifyDirty()`
    helper that calls `updateBar()` only when `bar` and `countEl` are both
    present. The block:
      - `document.querySelectorAll("[data-quiet-preset]")`; return early when the
        list is empty (this script is served to every page);
      - on click, read `data-preset-start` / `data-preset-end` /
        `data-preset-enabled` off the button and write them into
        `form.elements["quiet_hours_start"]` / `["quiet_hours_end"]` `.value` and
        `form.elements["quiet_hours_enabled"].checked`, each behind its own
        presence guard, and only for the attributes the button actually carries;
      - then call `notifyDirty()`. Do NOT dispatch a synthetic `change` event:
        constructing one in an ES5-safe way is awkward and unnecessary when the
        handler lives in this same file — call the existing `updateBar()` through
        `notifyDirty()` instead. Record that reasoning in a comment, and record
        that `countDifferences()` is reused, never reimplemented.
      - keep every write to `.value` / `.checked` — no `innerHTML`, no other
        HTML-writing sink.

    In `companion/test_config_page.py`: add checks that `quiet_hours_group()`
    renders exactly three `data-quiet-preset` buttons, all `type="button"`; that
    the Night preset's `data-preset-start`/`data-preset-end` equal
    `device_config.DEFAULT_QUIET_HOURS_START`/`DEFAULT_QUIET_HOURS_END`; that the
    Work day preset carries 08:00/18:00; that the Always-on preset carries
    `data-preset-enabled="0"` and NO time attributes; that the preset wrapper
    appears after the `.settings-checkbox` label and before the first
    `type="time"` input (index comparison); that `handle_post()`'s behaviour for
    the quiet-hours fields is byte-identical to before (no server change);
    and a cross-file check that `dirty-state.js`'s source references the literal
    `data-quiet-preset` and the three `data-preset-*` attribute names. Re-derive
    `EXPECTED_CHECK_COUNT` by running the harness and appending a new last
    assignment citing 19-10-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;h=c.quiet_hours_group(True,'23:00','07:00');print(h.count('data-quiet-preset'), h.count('type=\"button\"'))"`
      prints `3 3`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;from server import device_config as d;h=c.quiet_hours_group(True,'23:00','07:00');print(('data-preset-start=\"%s\"' % d.DEFAULT_QUIET_HOURS_START) in h)"`
      prints `True`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;h=c.quiet_hours_group(True,'23:00','07:00');print(h.index('data-quiet-preset') > h.index('settings-checkbox') and h.index('data-quiet-preset') < h.index('type=\"time\"'))"`
      prints `True`
    - `grep -c "data-quiet-preset" companion/static/dirty-state.js` outputs at least `1`
    - `grep -c "let \|const \|=>\|innerHTML" companion/static/dirty-state.js` outputs `0`
    - `git diff companion/static/style.css | grep -c "^+.*--[a-z-]\+:"` outputs `0` (no new custom property in this whole plan)
    - `server/.venv/bin/python3 companion/test_config_page.py` exits 0 with `M/M checks pass`
    - `server/.venv/bin/python3 companion/test_contrast_check.py` exits 0
  </acceptance_criteria>
  <done>Three inert-without-JS preset buttons sit between the enable checkbox and the time inputs, fill the fields client-side, mark the form dirty through the existing diff count, and change nothing on the server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM -> the settings form's values | the preset buttons write into form fields client-side; whatever they write is still submitted as untrusted input |
| client-side affordance -> server validation | `required`, the presets and the dirty bar are all conveniences; the server remains the sole validator (ASVS V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-38 | Tampering (client-side validation bypass) | the quiet-hours preset buttons | mitigate | the presets write into the same two fields the form already posts and are validated server-side exactly as a hand-typed value is — by `handle_post()`'s HH:MM pre-check (plan 19-07) and `save_device_config()`'s own `_HHMM_RE` gate. No new server code path and no new trusted client input; pinned by the "handle_post behaviour is byte-identical" check |
| T-19-10 | Tampering (DOM sink) | `dirty-state.js` | mitigate | every DOM write in the new code is `.value`, `.checked`, `classList.add` or `textContent`; the forbidden-token grep keeps `innerHTML` and the ES5-unsafe constructs at zero, and the existing harness guard is extended rather than bypassed |
| T-19-39 | Denial of Service (no way to save) | the `.js`-gated fallback-hide rule | mitigate | D-09 is itself the mitigation: the fallback Save button now hides only when `dirty-state.js` has proven the replacement bar exists, so a JS failure degrades to a working server-rendered form instead of an unsaveable page (A-27). Pinned by the source-ordering check and the cross-file CSS/script class agreement |
| T-19-40 | Denial of Service (silent data loss) | navigating away with unsaved edits | mitigate | D-10's `beforeunload` guard, keyed on the real diff count and cleared only by a genuine submit or cancel (A-28) |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; no build step, no bundler, no new JS dependency |
</threat_model>

<verification>
- `server/.venv/bin/python3 companion/test_config_page.py` -> `M/M checks pass`
- `server/.venv/bin/python3 companion/test_contrast_check.py` -> `36/36`
- `scripts/run-all-tests.sh` -> no NEW failures beyond the five pre-existing
  root-sandbox artifacts
- `<human-check>` (end-of-phase): on Display, tap "Work day" and confirm both time
  inputs fill and the save bar appears; tap "Always on" and confirm the enable
  checkbox unticks and the times stay; then click Trigger poll on Device with
  unsaved edits and confirm the browser asks before leaving; then Save and
  confirm it does not ask. With JavaScript disabled, confirm the bottom
  "Save settings" button is visible and works.
</verification>

<success_criteria>
- `dirty-state.js` adds `dirty-ready` to `<html>` only after its bar guard passes,
  and `style.css` keys the fallback-hide on that class
- The three `.js .mobile-nav` rules are unchanged
- A `beforeunload` guard keyed on `countDifferences()` fires while dirty and is
  cleared by a real submit or cancel
- Three `type="button"` presets render between the enable checkbox and the time
  inputs, with the Night values sourced from `device_config`'s defaults
- No server-side change to quiet-hours handling
- No new custom property in `style.css`
- `EXPECTED_CHECK_COUNT` re-derived by running the harness
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-10-SUMMARY.md` when done.
</output>
