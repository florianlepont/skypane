---
phase: 19-companion-audit-follow-through-fix-the-open-findings-from-18
plan: 12
type: execute
wave: 6
depends_on: ["19-08", "19-11"]
files_modified:
  - server/device_config.py
  - companion/wake.py
  - companion/app.py
  - companion/pages/config_page.py
  - companion/pages/home_page.py
  - server/test_config_history.py
  - server/test_runway_config.py
  - companion/test_config_page.py
  - companion/test_companion_app.py
  - companion/test_view_pages.py
autonomous: true
requirements: [CFG-01, CFG-04]
user_setup: []

must_haves:
  truths:
    - "D-11 (A-29): the runway cards read 'Runway 3 (07/25)', 'Runway 4 (06/24)', 'Runway 2 (02/20)' — no French 'Piste' vocabulary in the picker"
    - "D-23 (A-40 remainder): device_config carries a screen_id key, defaulting to plane-frame and normalised against the screen registry, threaded through ctx['screen_id']"
    - "D-23: the Display and Device headers render a screen selector <select> only when more than one screen type is registered"
    - "D-13 (A-31, S-02): Home and Device show 'Next wake ≈ HH:MM' in Paris local time, computed from the last device check-in plus the effective wake interval"
    - "D-13 (A-31): every 'Applies on the next scheduled poll' caption gains '(next wake ≈ HH:MM)' where the value is known"
    - "D-22 (A-39): the Device page carries an 'Edit artwork' link to /airlines?edit=1"
  artifacts:
    - path: "server/device_config.py"
      provides: "the three plain-English runway labels and the screen_id key with its own normaliser"
      contains: "normalise_screen_id"
    - path: "companion/wake.py"
      provides: "next_wake_at_iso() — the one definition of the next-wake arithmetic"
      exports: ["next_wake_at_iso"]
    - path: "companion/pages/config_page.py"
      provides: "the conditional screen selector, the next-wake caption suffixes, and the Edit artwork link"
      contains: "screen_id"
  key_links:
    - from: "companion/app.py page_context()"
      to: "companion/pages/config_page.py"
      via: "ctx['screen_id'] and ctx['last_checkin_ts'], both read fresh per request"
      pattern: "screen_id|last_checkin_ts"
    - from: "server/device_config.py"
      to: "companion/screens.py"
      via: "a duplicated-not-imported screen id list, pinned equal by a harness check (server/ must never import companion/)"
      pattern: "SCREEN_ID"
    - from: "companion/pages/home_page.py"
      to: "companion/wake.py"
      via: "next_wake_at_iso() + layout.local_clock_text() for the Next wake widget"
      pattern: "wake\\.next_wake_at_iso"
---

<objective>
Close A-29 (D-11), A-31/S-02 (D-13), the A-40 remainder (D-23) and the Device-page
half of A-39 (D-22): make the runway picker speak the same language as the rest of
the app, tell the user when the frame will next wake up, finish the screens seam
so the next screen type is a registry entry plus its groups, and put the artwork
editor behind a named link.

Purpose: "Piste 3 / 4 / 2" is French ADP vocabulary in an otherwise English app
whose other surfaces say "Runway 3 / 06-24 / 02-20"; "Applies on the next
scheduled poll" is unquantified on a page that already knows the wake interval
and the last check-in; and `screen_id` is the one piece of the screens seam Phase
18 left unbuilt, so the registry is still fed `None`.
Output: relabelled runways, a `screen_id` config key with its own normaliser and
a conditional selector, a next-wake figure on Home and Device, and an "Edit
artwork" link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-CONTEXT.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-RESEARCH.md
@.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-PATTERNS.md
@.claude/skills/sketch-findings-skypane/SKILL.md

<interfaces>
A HARD LAYERING CONSTRAINT 19-RESEARCH.md did not flag, verified by grep:
`companion/` imports `server/` (e.g. `companion/app.py:74` does
`from server import device_config, history_db`), and `server/` NEVER imports
`companion/`. Therefore `server/device_config.py` MUST NOT import
`companion/screens.py` for D-23's normalisation. Use this codebase's established
duplicated-not-imported contract instead: define the screen id list and the
default in `server/device_config.py` as literals, and pin them equal to
`companion.screens.SCREEN_IDS` / `DEFAULT_SCREEN_ID` with a dedicated harness
check (the same mechanism `app.py`'s `*_SCRIPT_ROUTE` / `layout.py`'s
`*_SCRIPT_SRC` pairs already use). Do not invert the dependency.

Runway registry — `server/device_config.py:400-435`:
  RUNWAYS = {
      "3":     {"label": "Piste 3", "tag_text": "ORY · RWY 3",     "empty_heading": "Watching Runway 3"},
      "06-24": {"label": "Piste 4", "tag_text": "ORY · RWY 06/24", "empty_heading": "Watching Runway 06/24"},
      "02-20": {"label": "Piste 2", "tag_text": "ORY · RWY 02/20", "empty_heading": "Watching Runway 02/20"},
  }
  RUNWAY_IDS = tuple(RUNWAYS)          # :435
  def runway_label(runway_id)          # :960 — needs NO change
  # `tag_text` and `empty_heading` are rendered onto the e-ink PANEL by
  # server/plane/render.py, not by the companion — changing them would move
  # pinned panel-render expectations in server/test_render.py. D-11's only
  # stated change is `label`; see the action for the reading to record.
  # test_view_pages.py:1441-1460 derives its expectation via a LIVE
  # device_config.runway_label("3") call, so it tracks the registry
  # automatically and needs no edit (19-RESEARCH.md, D-11 code example).

Config persistence — `server/device_config.py`:
  def load_device_config(state_dir) -> dict   # :598 — "Always returns all ten
      # keys"; every key goes through its own normalise_*(); never raises; a key
      # absent from an older file resolves via .get() with no migration
  def save_device_config(state_dir, theme=None, ..., calendar_theme_id=None)  # :646
      # every supplied value is membership/type-checked BEFORE anything is
      # written; None means "not supplied, carry forward"; writes via
      # tmp-write-then-os.replace()
  normalise_runway_id / normalise_theme_id / ... # :444-598 — the read-path
      # "degrade to the default" idiom to copy for normalise_screen_id()
  # server/test_config_history.py asserts the FULL config dict by equality in
  # multiple checks (~:100, 116, 131, 144, 193, 225 per 19-RESEARCH.md) — every
  # one must gain "screen_id": "plane-frame" or it fails the moment
  # load_device_config() returns an eleventh key. This is the single
  # highest-count mechanical retarget in the phase. Current count: 60/60.

Screen registry — `companion/screens.py` (87 lines, read it in full):
  DEFAULT_SCREEN_ID = "plane-frame"; SCREEN_TYPES = {...}; SCREEN_IDS = tuple(SCREEN_TYPES)
  def screen_type(screen_id=None)      # falls back to the default, never raises
  def current_screen_id(ctx=None)      # already reads ctx.get("screen_id") first
      # and falls back to DEFAULT_SCREEN_ID — THE SEAM IS ALREADY BUILT AND
      # ALREADY WIRED to config_page.render() (:1722) and handle_post() (:2056).
      # It is simply fed None today because nothing populates ctx["screen_id"]
      # or the on-disk key. D-23 is the plumbing, not a redesign.

Page context — `companion/app.py:1021-1120` `page_context()`. Plans 19-07 and
19-08 both changed this file in earlier waves (`errors` handling, `edit_mode`) —
read 19-07-SUMMARY.md and 19-08-SUMMARY.md and the live source first.
  def env_wake_interval_default()      # :610 — reads SKYPANE_SLEEP_S and clamps
      # it to [WAKE_INTERVAL_MIN_S, WAKE_INTERVAL_MAX_S] for a UI-prefill reason
      # its docstring explains in full. Plan 19-05 created
      # companion/wake.py's `env_sleep_s()` as the UNCLAMPED sibling for
      # threshold arithmetic and deliberately left this function alone; this plan
      # retargets it to delegate, keeping the clamp HERE.

Next-wake inputs — `companion/wake.py` (created by plan 19-05):
  SLEEP_ENV_VAR / MISSED_WAKES_WARN / MISSED_WAKES_ERROR / STALE_*_FLOOR_S
  def env_sleep_s()                          # unclamped SKYPANE_SLEEP_S or None
  def effective_wake_interval_s(device_cfg)  # DISPLAY_OFF_SLEEP_S while the
      # screen is off, else wake_interval_s, else env_sleep_s(), else None
  def device_staleness_thresholds(wake_interval_s)

Time formatting — `companion/layout.py`:
  def parse_iso(ts) -> datetime | None
  def local_clock_text(parsed, now_parsed=None) -> "HH:MM" | "D Mon HH:MM"
      # Europe/Paris (Phase 18 D-06). 19-CONTEXT.md requires "next wake" to use
      # THIS function.

Caption constants carrying the unquantified promise — `companion/pages/config_page.py`:
  THEME_SECTION_CAPTION, RUNWAY_SECTION_CAPTION, LED_SECTION_CAPTION,
  WAKE_INTERVAL_SECTION_CAPTION, QUIET_HOURS_SECTION_CAPTION (grep for
  "next scheduled poll" to find every one — D-13 says "gain '(next wake ≈ HH:MM)'
  WHERE THE VALUE IS KNOWN", so the suffix must be appended at RENDER time, never
  baked into the constant, and must be omitted entirely when unknown)
  DISPLAY_SECTION_CAPTION deliberately states its own ~5-minute latency instead
  of the generic clause (12-CONTEXT.md D-01) — leave it alone.

Home's widget idiom — `companion/pages/home_page.py:183-230` `_status_tiles_html()`
and `layout.stat_tile()` (widened by plan 19-06 with `caption_title`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel the runways and add the screen_id config key (D-11, D-23 persistence half)</name>
  <files>server/device_config.py, server/test_config_history.py, server/test_runway_config.py</files>
  <read_first>
    - server/device_config.py lines 395-440 (the `RUNWAYS` registry and its full
      comment block about ADP "Piste N" numbering and the explicit key->number
      mapping; also the sentence "The French 'Piste' vocabulary is scoped to the
      companion web picker alone" — which D-11 makes false and which must be
      rewritten)
    - server/device_config.py lines 440-600 (every `normalise_*()` — pick the
      closest analog for `normalise_screen_id()`) and lines 700-730 (the
      `RUNWAY_IDS` membership raise on the write path, the exact
      "membership test, raise on write, degrade on read" split to copy)
    - server/device_config.py lines 598-800 (`load_device_config()` and
      `save_device_config()` in FULL — the always-returns-all-keys docstring, the
      per-field validation block, the `new_config` dict assembly and the
      tmp-write-then-replace)
    - companion/screens.py (the whole file — `DEFAULT_SCREEN_ID`, `SCREEN_TYPES`,
      `SCREEN_IDS`)
    - server/test_config_history.py — grep for `"tracked_runway"` to find EVERY
      full-config dict literal, and read the `check()` helper and the
      `EXPECTED_CHECK_COUNT` assignment (~:52)
    - server/test_runway_config.py (the whole file, 14 checks — confirm whether
      any pins a runway label literally)
    - server/plane/render.py — grep for `tag_text` and `empty_heading` to confirm
      for yourself that they are panel-render values, not companion values
  </read_first>
  <action>
    D-11. Change ONLY the three `RUNWAYS[*]["label"]` values to
    `"Runway 3 (07/25)"`, `"Runway 4 (06/24)"` and `"Runway 2 (02/20)"` — typed by
    hand, because the "3" key's parenthetical "07/25" is not derivable from the
    key. Leave `tag_text` and `empty_heading` untouched: they are rendered onto the
    e-ink panel by `server/plane/render.py` and are pinned by
    `server/test_render.py`, and D-11's own parenthetical names
    `device_config.RUNWAYS[*]["label"]` as the thing that changes. Record that
    reading in a comment: the label now carries both the ADP number and the
    physical designator, `tag_text` keeps its existing parentheses-free form, and
    the panel's own vocabulary is out of scope. Rewrite the "The French 'Piste'
    vocabulary is scoped to the companion web picker alone" sentence — after this
    change the picker is English and the ADP number survives inside the label.
    `runway_label()` needs no change.

    D-23 persistence. Add to `server/device_config.py`:
      - `DEFAULT_SCREEN_ID = "plane-frame"` and `SCREEN_IDS = ("plane-frame",)` as
        LITERALS, with a comment stating plainly that these duplicate
        `companion/screens.py`'s own values, that the duplication is deliberate
        because `server/` must never import `companion/` (the dependency runs one
        way only), and that a harness check pins the two equal — the same
        duplicated-not-imported contract `app.py`/`layout.py`'s script-route pairs
        already use. Name the file and the constants to keep in sync;
      - `normalise_screen_id(value)` following the read-path idiom of its
        siblings: return `value` when it is a member of `SCREEN_IDS`, else
        `DEFAULT_SCREEN_ID`. Never raise;
      - a `"screen_id": normalise_screen_id(data.get("screen_id"))` entry in
        `load_device_config()`'s returned dict, and update that function's
        docstring from "all ten keys" to eleven, listing the new key and stating
        that an existing `device_config.json` with no `screen_id` resolves to the
        default with no migration and no rewrite — the same additive behaviour
        `theme_arriving` and `calendar_theme_id` already document;
      - a `screen_id=None` keyword parameter on `save_device_config()`, placed
        LAST so every existing positional/keyword call is unaffected, validated by
        the write-path membership raise (`if screen_id is not None and screen_id
        not in SCREEN_IDS: raise ValueError(...)` naming both the rejected value
        and `SCREEN_IDS`, matching `tracked_runway`'s message shape), and carried
        forward from the current on-disk value when None. Extend the docstring.

    Harness work:
      - `server/test_config_history.py`: add `"screen_id": "plane-frame"` to EVERY
        full-config dict literal (grep `"tracked_runway"` to find them all — do not
        rely on the line numbers in the research). Add checks that
        `normalise_screen_id()` degrades a hostile/unknown/None value to the
        default; that `save_device_config(screen_id="nope")` raises `ValueError`
        and leaves any pre-existing file byte-identical (the same property the
        `tracked_runway` checks already assert); that a `device_config.json`
        written WITHOUT a `screen_id` key loads with the default and is not
        rewritten on read; and the cross-file agreement check importing
        `companion.screens` and asserting
        `device_config.SCREEN_IDS == screens.SCREEN_IDS` and
        `device_config.DEFAULT_SCREEN_ID == screens.DEFAULT_SCREEN_ID`;
      - `server/test_runway_config.py`: if any check pins a runway label literally,
        retarget it in place; if none does (the likely case), add one check
        asserting `runway_label()` returns a string containing "Runway " and no
        "Piste" for every member of `RUNWAY_IDS`, so the French vocabulary cannot
        come back unnoticed;
      - re-derive `EXPECTED_CHECK_COUNT` in BOTH files by running each and
        appending a NEW last assignment citing 19-12-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 server/test_config_history.py && server/.venv/bin/python3 server/test_runway_config.py && server/.venv/bin/python3 companion/test_view_pages.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from server import device_config as d;print([d.runway_label(i) for i in d.RUNWAY_IDS])"`
      prints `['Runway 3 (07/25)', 'Runway 4 (06/24)', 'Runway 2 (02/20)']`
    - `grep -c '"Piste' server/device_config.py` outputs `0`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from server import device_config as d;import companion.screens as s;print(d.SCREEN_IDS == s.SCREEN_IDS, d.DEFAULT_SCREEN_ID == s.DEFAULT_SCREEN_ID)"`
      prints `True True`
    - `grep -c "import companion" server/device_config.py` outputs `0`
    - `server/.venv/bin/python3 -c "import sys,tempfile;sys.path.insert(0,'.');from server import device_config as d;t=tempfile.mkdtemp();c=d.load_device_config(t);print(len(c), c['screen_id'])"`
      prints `11 plane-frame`
    - `server/.venv/bin/python3 server/test_render.py` exits 0 (the panel's
      `tag_text`/`empty_heading` were not disturbed)
    - `server/test_config_history.py` and `server/test_runway_config.py` both
      report `M/M checks pass`
  </acceptance_criteria>
  <done>The runway picker speaks English with the ADP number in the label; device_config carries a validated screen_id as its eleventh key with no migration; the duplicated screen id list is pinned equal to the companion registry; the panel render is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Thread screen_id through ctx, render the conditional selector, and add the Edit artwork link (D-23, D-22)</name>
  <files>companion/app.py, companion/pages/config_page.py, companion/test_config_page.py, companion/test_companion_app.py</files>
  <read_first>
    - companion/screens.py (whole file) and companion/pages/__init__.py (the full
      ctx-key contract that must document the new key)
    - companion/app.py lines 1021-1120 (`page_context()` as it stands AFTER plans
      19-07 and 19-08 — read both SUMMARYs first; note where `device_config` is
      already loaded, so `screen_id` can be read from that same dict rather than
      loading the config twice)
    - companion/pages/config_page.py lines 1700-1800 (`render()`'s
      `screens.current_screen_id(ctx)` / `screens.screen_type(...)` calls,
      `scope_groups()`, the three scope branches and `_screen_caption_html()`)
    - companion/pages/config_page.py lines 2040-2070 (`handle_post()`'s own
      `screens.current_screen_id(ctx)` call and the surrounding scope gating) and
      its membership-test-before-use gates (as modified by plan 19-07 — read
      19-07-SUMMARY.md)
    - companion/pages/config_page.py lines 1793-1818 (`_screen_caption_html()` and
      `_scope_fields_html()` — the header slot the selector joins, and the
      existing hidden-field pattern)
    - companion/layout.py `page_header(title, purpose=None, action_html=None, ...)`
      (the `action_html` slot the caption already uses)
    - companion/pages/airlines_page.py's `EDIT_QUERY_PARAM` and `AIRLINES_ROUTE`
      (added/used by plan 19-08 — read 19-08-SUMMARY.md; the link must be built
      from those constants, never a retyped literal)
    - companion/test_config_page.py — grep for `screen_id`, `SCREEN_CAPTION`,
      `current_screen_id` and read every pinned check
  </read_first>
  <action>
    ctx threading:
      - in `page_context()`, add `"screen_id"` read from the `device_config` dict
        the method already loads (`device_config.load_device_config(state_dir)`),
        so the config file is read once per request, not twice. Document the key in
        `companion/pages/__init__.py`'s ctx contract;
      - `screens.current_screen_id(ctx)` already membership-tests the value and
        falls back to the default, so no second validation is needed at the page
        layer — say so in a comment rather than duplicating the gate.

    The conditional selector, in `config_page.py`:
      - add a `_screen_selector_html(current_screen_id)` builder returning the
        EMPTY STRING when `len(screens.SCREEN_IDS) <= 1` (D-23's explicit
        condition), and otherwise a `<select name="screen_id">` with one
        `<option value="{id}"{selected}>{label}</option>` per registry member,
        labels from `screens.screen_type(id)["label"]`, every value and label
        escaped at its interpolation point, plus a visually-hidden `<label>` or an
        `aria-label` so the control has an accessible name;
      - render it in the Display and Device header slots along
        `_screen_caption_html(screen)` (both already go through
        `layout.page_header(..., action_html=...)`), so with one registered screen
        the header is byte-identical to today and with two it gains a picker;
      - place the `<select>` INSIDE the settings form so it submits with the rest,
        and add a `screen_id` gate to `handle_post()` using the exact
        membership-test-before-use shape its siblings use
        (`if submitted_screen_id is not None and submitted_screen_id not in
        screens.SCREEN_IDS: _note_error(...); return FLASH_SAVE_FAILED`), passing
        the validated value through as one more keyword argument on the SAME
        single `save_device_config()` call — never a second write path. The
        all-or-nothing rejection contract is unchanged.

    The Edit artwork link (D-22's Device-page half):
      - add a copy constant (e.g. `EDIT_ARTWORK_LINK_TEXT = "Edit artwork"`) and
        render a `text-label` anchor to
        `"%s?%s=1" % (layout.AIRLINES_ROUTE or airlines_page's route constant,
        airlines_page.EDIT_QUERY_PARAM)` on the Device page only. Build the href
        from the existing constants, never a retyped `/airlines?edit=1` literal.
        `config_page.py` must not import `airlines_page` if that crosses the
        page-module import boundary in `companion/pages/__init__.py` — check that
        boundary FIRST; if it does, rebind the two constants in `layout.py` or
        pass the href through `ctx` from `app.py`, and record which you chose and
        why. Add one plain sentence beside the link saying what it opens, in the
        section-caption voice.

    Harness work:
      - `companion/test_config_page.py`: checks that `_screen_selector_html()`
        returns `""` with the real single-member registry; that with
        `screens.SCREEN_TYPES` monkeypatched to two members it emits exactly one
        `<select name="screen_id">` with two options, the current one `selected`,
        and a non-empty accessible name (restore the registry in a `finally`);
        that `render()` at Display and Device scope contains no `<select
        name="screen_id">` today; that `handle_post()` rejects a crafted
        `screen_id` with `FLASH_SAVE_FAILED` and notes a field error while writing
        nothing; that a valid `screen_id` round-trips through
        `save_device_config()`; and that the Device scope renders exactly one
        Edit-artwork anchor whose href contains `edit=1` while the Display scope
        renders none;
      - `companion/test_companion_app.py`: a real-HTTP check that an
        authenticated GET of the Device page contains the Edit-artwork href and
        that following it returns 200 with the artwork-editing forms present (the
        end-to-end link between plan 19-08's `?edit=1` gate and this link);
      - re-derive `EXPECTED_CHECK_COUNT` in BOTH harnesses by running each and
        appending a NEW last assignment citing 19-12-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_config_page.py && server/.venv/bin/python3 companion/test_companion_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "screen_id" companion/pages/__init__.py` outputs at least `1`
    - `grep -c '"screen_id"' companion/app.py` outputs at least `1`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from companion.pages import config_page as c;print(repr(c._screen_selector_html('plane-frame')))"`
      prints `''`
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.screens as s;from companion.pages import config_page as c;s.SCREEN_TYPES['x']={'label':'X','description':'d','everyday_groups':(),'advanced_groups':(),'has_colour_rules':False,'has_manual_poll':False};s.SCREEN_IDS=tuple(s.SCREEN_TYPES);h=c._screen_selector_html('plane-frame');print('<select name=\"screen_id\"' in h, h.count('<option'))"`
      prints `True 2`
    - `grep -c "edit=1\|EDIT_QUERY_PARAM" companion/pages/config_page.py` outputs at least `1`
    - both harnesses green: `test_config_page.py` `M/M`, `test_companion_app.py`
      with exactly the two documented root-sandbox FAILs
  </acceptance_criteria>
  <done>ctx carries a validated screen_id, the selector renders only for a multi-screen registry and posts through the existing single write path, and the Device page links to the artwork editor via the existing route constants.</done>
</task>

<task type="auto">
  <name>Task 3: Show "Next wake ≈ HH:MM" on Home and Device (D-13, S-02)</name>
  <files>companion/wake.py, companion/app.py, companion/pages/home_page.py, companion/pages/config_page.py, companion/test_view_pages.py, companion/test_config_page.py</files>
  <read_first>
    - companion/wake.py (created by plan 19-05 — read the whole file: its docstring
      boundary, `env_sleep_s()`, `effective_wake_interval_s()`,
      `device_staleness_thresholds()` and the comment explaining why it does NOT
      apply app.py's UI clamp)
    - companion/app.py lines 605-650 (`env_wake_interval_default()` in FULL,
      including the paragraph explaining the [60, 3600] clamp's HTML5 reason — the
      clamp stays HERE, only the raw read delegates)
    - companion/app.py lines 1021-1120 (`page_context()` as it stands after plans
      19-07, 19-08 and Task 2 of this plan) and the `_safe_query`-style helpers
      near it
    - server/history_db.py — `latest_device_health(conn)` (its return shape and
      the `ts` key) and `utc_now_iso()`
    - companion/pages/home_page.py lines 85-240 (the state-text dictionaries,
      `_safe_query()`, `_latest_battery()`, `_status_tiles_html()` and the tile
      assembly — the widget idiom the new figure joins)
    - companion/layout.py lines 595-665 (`parse_iso()`, `local_clock_text()`,
      `LOCAL_TZ`) and `stat_tile()` as widened by plan 19-06
    - companion/pages/config_page.py — grep for "next scheduled poll" and read
      EVERY caption constant that contains it, plus `DISPLAY_SECTION_CAPTION`
      (which must be left alone, per 12-CONTEXT.md D-01)
    - companion/test_view_pages.py and companion/test_config_page.py — grep for
      the caption literals you are about to append to
  </read_first>
  <action>
    One definition of the arithmetic, in `companion/wake.py`:
      add `next_wake_at_iso(last_checkin_ts, device_cfg)` returning an ISO-8601 UTC
      string for `last_checkin_ts + effective_wake_interval_s(device_cfg)`, or None
      when `last_checkin_ts` is falsy/unparseable or the effective interval is
      None. Parse with `datetime.fromisoformat`-equivalent handling that accepts
      the `history_db.utc_now_iso()` shape and treats a naive value as UTC, the
      same convention `layout.local_clock_text()` documents. Return an ISO STRING,
      not formatted text, and do NOT import `companion.layout` here — keep this
      module free of any view dependency so each page formats the value itself
      with `layout.local_clock_text()`. Never raise.

    Delegate the env read, in `companion/app.py`:
      change `env_wake_interval_default()` to obtain the raw value from
      `wake.env_sleep_s()` and then apply its existing
      `[device_config.WAKE_INTERVAL_MIN_S, WAKE_INTERVAL_MAX_S]` clamp, so there is
      one place that reads `SKYPANE_SLEEP_S` and one place that clamps it for the
      numeric input. Keep the whole docstring, adding one sentence recording the
      delegation and that the clamp deliberately stays here because it exists for
      an HTML5 `min="60"` reason that does not apply to threshold arithmetic.

    New ctx key, in `page_context()`:
      add `"last_checkin_ts"` from a fail-soft `history_db.latest_device_health()`
      read (None on any failure, matching how the other reads in this method
      already degrade), and document it in `companion/pages/__init__.py`'s ctx
      contract. Do not compute the next-wake string here — the pages format it, so
      the value stays data and the formatting stays in the view layer.

    Home, in `companion/pages/home_page.py`:
      add a "Next wake" figure using `wake.next_wake_at_iso(ctx["last_checkin_ts"],
      ctx["device_config"])`, formatted through
      `layout.local_clock_text(layout.parse_iso(iso), now_parsed=layout.parse_iso(ctx["now"]))`
      and prefixed with "≈" (real U+2248), rendered in Home's existing widget
      idiom — either as a second line on the frame tile or as its own small line,
      whichever matches the existing markup without inventing a new class. Omit the
      figure ENTIRELY when the value is None (no check-in yet, or no known
      interval): D-13 says "where the value is known", so there must be no
      placeholder, no "unknown", and no dangling label. Add a module constant for
      the label and one for the "≈ %s" template rather than inlining strings.

    Device/Display captions, in `companion/pages/config_page.py`:
      append " (next wake ≈ HH:MM)" to each caption that currently ends with the
      unquantified "Applies on the next scheduled poll" clause, at RENDER time —
      never baked into the constant — and omit the suffix entirely when the value
      is None. Add one private helper (e.g.
      `_with_next_wake(caption, next_wake_clock)`) that returns the caption
      unchanged for a falsy clock, so every caption site uses one implementation
      and none hand-concatenates. Leave `DISPLAY_SECTION_CAPTION` untouched: it
      deliberately states its own ~5-minute screen-off latency instead of the
      generic clause (12-CONTEXT.md D-01), and appending a wake-interval-derived
      figure there would contradict it. Record that exception in a comment.
      Also render a "Next wake ≈ HH:MM" line in the Device page header slot
      alongside the screen caption, per D-13's "Home and Device show" wording.

    Harness work:
      - in `companion/test_view_pages.py` (Home) and `companion/test_config_page.py`
        (Display/Device): checks that `wake.next_wake_at_iso()` returns None for a
        falsy ts, for an unparseable ts and for a config with no known interval;
        that it returns last_checkin + wake_interval_s for a screen-on config;
        that it returns last_checkin + `DISPLAY_OFF_SLEEP_S` for a screen-off
        config (the D-13 screen-off rule); that Home renders the "≈" figure when a
        check-in and an interval are both present and renders NO next-wake label at
        all when either is missing; that each affected caption gains the suffix
        when the value is known and is byte-identical to its constant when it is
        not; and that `DISPLAY_SECTION_CAPTION` never gains a suffix;
      - re-derive `EXPECTED_CHECK_COUNT` in BOTH harnesses by running each and
        appending a NEW last assignment citing 19-12-PLAN.md.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/python3 companion/test_config_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.wake as w;print(w.next_wake_at_iso('2026-09-11T10:00:00+00:00', {'wake_interval_s':900,'display_enabled':True}))"`
      prints an ISO timestamp 15 minutes later
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.wake as w;print(w.next_wake_at_iso('2026-09-11T10:00:00+00:00', {'wake_interval_s':900,'display_enabled':False}))"`
      prints an ISO timestamp 5 minutes later (DISPLAY_OFF_SLEEP_S wins)
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');import companion.wake as w;print(w.next_wake_at_iso(None,{}), w.next_wake_at_iso('nonsense',{'wake_interval_s':900}))"`
      prints `None None`
    - `grep -c "companion.layout\|import layout" companion/wake.py` outputs `0`
    - `grep -c "wake.env_sleep_s" companion/app.py` outputs at least `1`
    - `grep -c "def _with_next_wake" companion/pages/config_page.py` outputs `1`
    - both harnesses green: `test_view_pages.py` `M/M`,
      `test_config_page.py` `M/M`
    - `scripts/run-all-tests.sh` shows no NEW failures beyond the five
      pre-existing root-sandbox artifacts
  </acceptance_criteria>
  <done>One function owns the next-wake arithmetic with no view dependency; Home and Device show "≈ HH:MM" in Paris local time and show nothing at all when the value is unknown; the screen-off cadence is honoured; the SKYPANE_SLEEP_S read has a single definition site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `device_config.json` on disk -> `screen_id` | a new on-disk key whose value selects a registry entry and therefore which settings groups render |
| browser form POST -> `screen_id` | a new submitted field that reaches `save_device_config()` |
| process environment -> `SKYPANE_SLEEP_S` | now read through one function and clamped in one place |
| SQLite `device_health.ts` -> the next-wake figure | a pipeline-written timestamp used in arithmetic and rendered |
| `server/` module boundary | `server/device_config.py` must never import `companion/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Spoofing / Tampering (unvalidated registry key) | `screen_id` on both the read and the write path | mitigate | read path: `normalise_screen_id()` degrades any non-member (including a path-traversal string or a non-string) to `DEFAULT_SCREEN_ID`, matching every sibling normaliser; write path: an explicit membership test raises `ValueError` before anything is written, and `screens.screen_type()`/`current_screen_id()` independently fall back rather than raising. ASVS V5 membership-test-before-use, applied at both ends. Pinned by the hostile-value and write-raise checks |
| T-19-44 | Tampering (layering inversion) | `server/device_config.py` | mitigate | the screen id list and default are duplicated as literals with a harness check pinning them equal to `companion.screens`, and an acceptance criterion asserts `grep -c "import companion" server/device_config.py` is `0`. Importing the companion package into the server package would let a companion-side change break the poll loop |
| T-19-45 | Tampering (config corruption on a rejected write) | `save_device_config(screen_id=...)` | mitigate | validation happens before the file is touched and the write uses the existing tmp-write-then-`os.replace()`, so a rejected `screen_id` leaves any pre-existing file byte-identical — asserted by a check modelled on the existing `tracked_runway` equivalent |
| T-19-27 | Tampering (partial write) | `handle_post()`'s new `screen_id` gate | mitigate | the gate returns `FLASH_SAVE_FAILED` before `save_device_config()` is called and the value rides on the SAME single write call, so the all-or-nothing contract is unchanged |
| T-19-08 | Tampering (XSS) | the screen selector's option values and labels, the Edit-artwork href, the next-wake figures and caption suffixes | mitigate | option values and labels come from the fixed `screens.SCREEN_TYPES` registry and are escaped at their interpolation point; the href is composed from module route constants, never from request data; the next-wake string is derived from a server-side timestamp and formatted by `layout.local_clock_text()`, then escaped |
| T-19-46 | Denial of Service (crash on a hostile timestamp) | `wake.next_wake_at_iso()` | mitigate | never raises: a falsy, unparseable or non-string `last_checkin_ts` and an unknown interval all return None, and the caller omits the figure entirely — pinned by the None-returning checks |
| T-19-24 | Information Disclosure (internal detail in UI) | the runway labels | mitigate | incidental but real: the picker stops exposing internal ADP French vocabulary and reads as ordinary English |
| T-19-SC | Tampering (supply chain) | package-manager installs | n/a | zero packages installed; `datetime` is stdlib |
</threat_model>

<verification>
- `server/.venv/bin/python3 server/test_config_history.py` -> `M/M checks pass`
- `server/.venv/bin/python3 server/test_runway_config.py` -> `M/M checks pass`
- `server/.venv/bin/python3 server/test_render.py` -> exits 0 (the panel's
  `tag_text`/`empty_heading` are untouched)
- `server/.venv/bin/python3 companion/test_config_page.py` -> `M/M checks pass`
- `server/.venv/bin/python3 companion/test_view_pages.py` -> `M/M checks pass`
- `server/.venv/bin/python3 companion/test_companion_app.py` -> exactly the two
  documented root-sandbox FAILs
- `scripts/run-all-tests.sh` -> no NEW failures beyond the five pre-existing
  root-sandbox artifacts (this is the LAST plan of the phase: the full suite must
  be green here before `/gsd:verify-work`)
- `<human-check>` (end-of-phase): the runway cards read "Runway 3 (07/25)" etc.;
  Home shows "Next wake ≈ HH:MM" in Paris local time and shows nothing when the
  frame has never checked in; the Device page's "Applies on the next scheduled
  poll" captions carry the same figure; the Device page's "Edit artwork" link
  opens Airlines with the artwork forms available; no screen selector is visible
  with one registered screen type.
</verification>

<success_criteria>
- `runway_label()` returns the three English labels; no `"Piste` string literal
  remains in `server/device_config.py`
- `load_device_config()` returns eleven keys including a normalised `screen_id`,
  with no migration needed for an older file
- `device_config.SCREEN_IDS` equals `screens.SCREEN_IDS` and `server/` imports
  nothing from `companion/`
- `ctx["screen_id"]` and `ctx["last_checkin_ts"]` exist and are documented in
  `companion/pages/__init__.py`
- `_screen_selector_html()` returns `""` for a single-member registry and a real
  `<select name="screen_id">` for a multi-member one
- `wake.next_wake_at_iso()` is the only place the next-wake arithmetic lives, has
  no view dependency, and returns None rather than raising
- Home and Device show "≈ HH:MM" only when the value is known
- `DISPLAY_SECTION_CAPTION` gains no suffix
- The Device page links to `/airlines?edit=1` via existing constants
- `EXPECTED_CHECK_COUNT` re-derived by running every touched harness
</success_criteria>

<output>
Create `.planning/phases/19-companion-audit-follow-through-fix-the-open-findings-from-18/19-12-SUMMARY.md` when done.
</output>
