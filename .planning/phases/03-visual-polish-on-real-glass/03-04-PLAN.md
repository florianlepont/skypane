---
phase: 03-visual-polish-on-real-glass
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true

files_modified:
  - server/plane/render.py
  - server/test_render.py

requirements: [PLANE-01, PLANE-02]

must_haves:
  truths:
    - "A corrupt, truncated or otherwise undecodable illustration file that passes `os.path.isfile()` degrades to `generic-fallback.png`: `render_panel()` returns a full 960,000-byte panel and never raises. This restates and closes 03-03-PLAN.md's must-have verbatim — 'A missing, corrupt or oversized illustration file degrades to the fallback ... it never raises and never blanks the panel' — which 03-VERIFICATION.md scored ✗ FAILED (truth #6)."
    - "An illustration whose PNG header declares more pixels than `illustrations.ILLUSTRATION_MAX_PIXELS` is rejected before any pixel data is decoded, inside the live render path — not only inside the offline `--validate` CLI."
    - "When the selected illustration and the generic fallback are both undecodable, the render skips the illustration entirely and still returns a valid 960,000-byte panel — the identical degradation the already-working 'illustration directory renamed aside' path produces."
    - "One bad illustration file degrades that one flight's illustration only; it never propagates out of `render_panel()` into `poll_loop.py`'s outer handler, so panel updates keep flowing for every subsequent poll cycle."
    - "The seven already-verified Phase 3 truths are unchanged: byte-determinism of a repeated render, D-24 never-mirrored illustrations, the `_assert_legal_palette()` contract, and the missing-directory degradation all still hold."
  artifacts:
    - server/plane/render.py
    - server/test_render.py
  key_links:
    - "`_build_active_canvas()` -> `_load_illustration_safely()` -> `_resize_illustration()` — BOTH call sites (the D-25/D-26 main card and the previous card). A fix wired into only one of the two leaves the gap open for the second flight."
    - "`_load_illustration_safely()` -> `illustrations.generic_fallback_path()` — D-08's universal fallback is reused as the decode-failure fallback, not just the selection-miss fallback."
    - "`_illustration_over_pixel_cap()` -> `illustrations.ILLUSTRATION_MAX_PIXELS` — the existing 40M cap is imported, never redefined, so the render path and the offline validator can never drift apart."
---

<objective>
Close the single gap 03-VERIFICATION.md found in Phase 3 (7/8 must-haves verified): the live
render path has no error handling around Pillow decoding a vendored illustration, so a
corrupt or oversized `.png` raises `PIL.UnidentifiedImageError` straight out of
`render_panel()`.

Root cause, per 03-VERIFICATION.md: the try/except that 03-03-PLAN.md's must-have and its
threat T-03-03-02 both relied on lived inside `draw_silhouette()`, which the D-25/D-26
two-flight redesign deleted. Nothing replaced it. `validate_illustration_file()` — which
already contains exactly the right guard — is wired only into `illustrations.py --validate`,
never into `render.py`.

Purpose: `poll_loop.py`'s outer `except Exception` stops the crash-loop but not the damage —
one bad file among the 33+ now vendored freezes ALL future panel updates until a human
notices, instead of degrading that one flight's illustration to D-08's generic fallback and
carrying on. This restores per-illustration degradation as defence in depth.

Output: two new never-raises helpers in `server/plane/render.py`, both `_build_active_canvas()`
illustration call sites rewired through them, and three regression checks in
`server/test_render.py` that feed a real byte-garbage file and a real oversized PNG through
the actual `render_panel()` code path.

Scope discipline: this closes one gap. Nothing already covered by the seven passing
must-haves is re-planned or touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-visual-polish-on-real-glass/03-VERIFICATION.md
@.planning/phases/03-visual-polish-on-real-glass/03-PATTERNS.md
@server/plane/render.py
@server/plane/illustrations.py
@server/test_render.py
</context>

## Gap being closed

| Source | Item | Status before this plan |
|---|---|---|
| 03-VERIFICATION.md gap #1 | Corrupt-file case: `_resize_illustration()`'s `Image.open(path).convert("RGBA")` is unguarded across the whole chain `_build_active_canvas` -> `_resize_illustration` -> `draw_illustration`. Reproduced live: a byte-garbage `.png` raises `PIL.UnidentifiedImageError` out of `render_panel()`. | ✗ FAILED |
| 03-VERIFICATION.md gap #2 | Oversized case: `validate_illustration_file()`'s `ILLUSTRATION_MAX_PIXELS` header cap is called only from the offline `--validate` CLI, never from the render path. Same root cause. | ✗ FAILED |
| 03-VERIFICATION.md Key Link | `_resize_illustration()` / `draw_illustration()` -> error handling for a decode failure: **none** | ✗ NOT_WIRED |
| 03-VERIFICATION.md Key Link | `illustrations.validate_illustration_file()` -> the render-time illustration-loading path: **none** | ✗ NOT_WIRED |

## Multi-source coverage audit

| Source | Item | Covered by |
|---|---|---|
| GOAL (ROADMAP Phase 3 SC 1) | The selection and compositing *mechanism* is correct, verified by the automated suite, not by eye | Tasks 1+2 — the degradation branch of that mechanism is the only part the suite did not exercise |
| REQ | PLANE-01 (departing flight number / airline / destination visible) | Task 2 — a decode failure currently suppresses the whole main text block along with the illustration and then freezes every later cycle; degrading to the fallback keeps the flight card rendering |
| REQ | PLANE-02 (arriving flight number / airline / origin visible) | Task 2 — same call path, both states |
| RESEARCH (03-RESEARCH.md Pitfall 2) | Alpha mask must be hard-thresholded to binary before `paste()` | Preserved unchanged — `draw_illustration()` is not modified by this plan |
| CONTEXT D-08 | A single dithered generic illustration is the universal fallback for uncovered airlines and the Route-unavailable state | Task 2 — D-08's `generic_fallback_path()` is reused as the decode-failure fallback, so a decode miss and a selection miss produce the same visual outcome |
| CONTEXT D-24 | Every illustration renders nose-left in both states, never mirrored | Preserved — no mirroring is introduced; the fallback is loaded and composited through the same unmodified `draw_illustration()` |
| CONTEXT D-25 / D-26 | Two-flight poster composition: a main card and a previous card, each with its own illustration | Task 2 — BOTH `select_illustration()` call sites are rewired, not just the main one |
| 03-VERIFICATION.md | Gaps #1 and #2 | Tasks 1+2 |

No unplanned items. Phase 3's deferred item (hardware-verified on-glass legibility) belongs to
Phase 6 and is explicitly out of scope here.

## Artifacts this phase produces

| Artifact | Kind | Created / changed by |
|---|---|---|
| `_illustration_over_pixel_cap(path)` | new function (`server/plane/render.py`) | 03-04 |
| `_load_illustration_safely(path, target_w)` | new function (`server/plane/render.py`) | 03-04 |
| `_build_active_canvas()`'s two illustration-loading call sites | changed call sites (`server/plane/render.py`) | 03-04 |
| `EXPECTED_CHECK_COUNT` raised from `35` to `38` | changed module constant (`server/test_render.py`) | 03-04 |
| Check "a corrupt (byte-garbage) illustration file degrades to the generic fallback instead of raising out of render_panel()" | new harness check (`server/test_render.py`) | 03-04 |
| Check "an oversized illustration is rejected on its PNG header, before any pixel data is decoded" | new harness check (`server/test_render.py`) | 03-04 |
| Check "when the selected illustration and the generic fallback are both undecodable, the render skips the illustration and still returns a valid panel" | new harness check (`server/test_render.py`) | 03-04 |

Unchanged on purpose: `server/plane/illustrations.py` (its `validate_illustration_file()`,
`generic_fallback_path()` and `ILLUSTRATION_MAX_PIXELS` are consumed, not modified),
`server/poll_loop.py` (its outer handler stays the last-resort net, not the degradation
mechanism), and `draw_illustration()` (the alpha-threshold and no-remap dither discipline is
load-bearing and untouched).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regression checks that feed a real corrupt file and a real oversized PNG through render_panel() and fail against today's code</name>

  <files>server/test_render.py</files>

  <behavior>
    - Rendering with `select_illustration()` forced to return a byte-garbage `.png` produces a 960,000-byte panel that is **byte-identical** to the same render with `select_illustration()` forced to return `illustrations.generic_fallback_path()`. Byte-identity is the assertion that proves it degraded *to the fallback* rather than merely skipping the illustration.
    - Rendering with `select_illustration()` forced to return a **valid** PNG whose pixel count exceeds `illustrations.ILLUSTRATION_MAX_PIXELS` also produces the byte-identical generic-fallback panel. Using a genuinely decodable oversized file (not garbage) is what makes this check prove a header-only cap exists — a bare try/except around the decode cannot satisfy it, because the decode would succeed and paint a different panel.
    - Rendering with BOTH `select_illustration()` and `generic_fallback_path()` forced to a byte-garbage path returns a 960,000-byte panel without raising, and its bytes differ from the generic-fallback panel — proving the illustration was skipped entirely rather than silently drawn.
    - Against the current unfixed `render.py`, all three checks report `FAIL` (the first and third as `exception: UnidentifiedImageError(...)` surfaced by the harness's own `check()` wrapper, the second as a byte mismatch or a palette-contract exception). The other 35 checks still report `PASS`.
  </behavior>

  <read_first>
    - `server/test_render.py` — read the whole file. Specifically: the module docstring's "asserts on the rendered canvas and packed bytes only" discipline, `EXPECTED_CHECK_COUNT` (line 36, currently `35`), `TEST_FLIGHT` / `TEST_ROUTE` (lines 43-56), the `check(name, fn)` helper inside `main()` (lines 147-156) and its `except Exception` -> automatic `FAIL` behaviour, the local imports of `render` / `panel_format` / `illustrations` / `Image` at lines 158-166, `_SelectIllustrationSpy` (lines 115-141) as the canonical monkeypatch-and-restore shape, and the existing generic-fallback check around line 366 (`with_real_airline` vs `with_fallback`) as the closest existing "two renders must differ" comparison.
    - `server/plane/illustrations.py` — `validate_illustration_file()` (lines 380-428) is the closest existing analog for this whole gap: it is the codebase's proven "never let a Pillow decode error propagate" pattern AND the header-only decompression-bomb guard (`.size` and `.format` read before any `load()`/`convert()`, with the early `return` comment explaining why). Also read `ILLUSTRATION_MAX_PIXELS` (line 115, `40_000_000`) and `generic_fallback_path()` (lines 313-314).
    - `server/plane/render.py` — `_resize_illustration()` (lines 268-277, the unguarded `Image.open(path)` / `.convert("RGBA")` this plan guards) and `_build_active_canvas()` (lines 540-590, both illustration call sites at ~562-569 and ~574-584).
    - `.planning/phases/03-visual-polish-on-real-glass/03-PATTERNS.md` — the "Regression Test Pattern" section (harness conventions, the monkeypatch-return-value shape, the "no new committed binary fixture" style) and the "Data Flow Summary" diagram marking `_resize_illustration()` as the sole unguarded step.
    - `.planning/phases/03-visual-polish-on-real-glass/03-VERIFICATION.md` — the "Behavioral Spot-Checks" table, which records exactly how the crash was reproduced live (monkeypatched `select_illustration()` returning a byte-garbage `.png` path).
  </read_first>

  <action>
Write the harness changes FIRST and confirm they fail against the current unfixed `render.py`
before Task 2 touches any production code. The three checks are the regression pins for
03-VERIFICATION.md's two gaps.

**In `server/test_render.py`:**

- Add `import tempfile` alongside the existing stdlib imports at the top of the file.
- Raise `EXPECTED_CHECK_COUNT` from `35` to `38`.
- Add a module-level helper `_write_garbage_png()` that creates a `NamedTemporaryFile` with a
  `.png` suffix and `delete=False`, writes a short run of non-PNG bytes to it, closes it, and
  returns the path. Match the verifier's live repro: a file that passes `os.path.isfile()` but
  carries no valid PNG signature.
- Add a module-level helper `_write_oversized_png()` that builds a genuinely valid PNG whose
  pixel count exceeds `illustrations.ILLUSTRATION_MAX_PIXELS`, writes it to a `.png`
  `NamedTemporaryFile` with `delete=False`, and returns the path. Use single-band mode `"L"`
  at 7000x6000 (42,000,000 pixels, over the 40,000,000 cap, and comfortably under Pillow's own
  `Image.MAX_IMAGE_PIXELS` so no DecompressionBombWarning fires), filled with a uniform value,
  and save with `compress_level=1` so the fixture writes in about a second and lands as a few
  tens of KB on disk. Single-band keeps the in-memory fixture around 42 MB rather than the
  168 MB an RGBA buffer of that size would need.
- Add a module-level context manager or small helper `_forced_illustration(render_mod, path)`
  that monkeypatches `render_mod.illustrations.select_illustration` to a lambda accepting
  `(route, aircraft_type=None)` and returning `path`, and restores the original on exit —
  following `_SelectIllustrationSpy`'s exact monkeypatch-and-restore shape, but overriding the
  return value instead of recording arguments. It must also be able to override
  `render_mod.illustrations.generic_fallback_path` for the third check, restoring that too.

Add these three checks at the end of `main()`'s check list, each wrapped in `try/finally` that
`os.unlink()`s every temp file it created:

- **Check 36 — "a corrupt (byte-garbage) illustration file degrades to the generic fallback
  instead of raising out of render_panel()".** Force `select_illustration()` to the garbage
  path and call `render.render_panel(TEST_FLIGHT, "departing", route=TEST_ROUTE)`. Then force
  `select_illustration()` to `illustrations.generic_fallback_path()` and render the same
  arguments again. Return failure unless the first buffer is exactly
  `panel_format.IMAGE_BYTES` long AND the two buffers are byte-identical. Put the reason
  string in terms of what was expected — degrade to the fallback, not merely survive.
- **Check 37 — "an oversized illustration is rejected on its PNG header, before any pixel data
  is decoded".** First self-guard the fixture: open it with `Image.open()`, multiply `.size`,
  and return failure if the product is not greater than `illustrations.ILLUSTRATION_MAX_PIXELS`
  (so a future change to the cap turns this check into an honest failure rather than a
  vacuous pass). Then run the same forced-render / forced-fallback-render comparison as check
  36 and require byte-identity.
- **Check 38 — "when the selected illustration and the generic fallback are both undecodable,
  the render skips the illustration and still returns a valid panel".** Force BOTH
  `select_illustration()` and `generic_fallback_path()` to the same garbage path. Require the
  render to return exactly `panel_format.IMAGE_BYTES` and to differ from the
  generic-fallback-forced panel rendered with the same flight and route. This pins the tail of
  the ladder: exhausting every candidate degrades to "no illustration", the same outcome the
  already-working renamed-directory path produces, and still never raises.

Do not add a committed binary fixture — both fixtures are synthesized inline, matching the
harness's existing "real vendored assets or nothing" style.

After writing, run the harness and record the RED evidence in the plan's summary: exactly
three `FAIL` lines and 35 `PASS` lines.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py 2>&1 | tee /tmp/03-04-red.txt | tail -5; test "$(grep -c '^FAIL ' /tmp/03-04-red.txt)" = "3" &amp;&amp; test "$(grep -c '^PASS ' /tmp/03-04-red.txt)" = "35"</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py 2>&1 | grep -c '^FAIL '` outputs exactly `3` — all three new checks fail against the unfixed `render.py`, proving each one genuinely exercises the gap.
    - `server/.venv/bin/python3 server/test_render.py 2>&1 | grep -c '^PASS '` outputs exactly `35` — no pre-existing check was broken while adding the new ones.
    - `server/.venv/bin/python3 server/test_render.py 2>&1 | grep -c 'UnidentifiedImageError'` outputs at least `1` — the harness surfaces the exact exception class 03-VERIFICATION.md reproduced live.
    - `grep -c 'EXPECTED_CHECK_COUNT = 38' server/test_render.py` outputs `1`.
    - `grep -c 'ILLUSTRATION_MAX_PIXELS' server/test_render.py` outputs at least `1` — the oversized fixture is validated against the real cap constant, not a hardcoded number.
    - `server/.venv/bin/python3 -m ruff check server/test_render.py` reports no findings.
    - `git diff --name-only` lists exactly `server/test_render.py`.
  </acceptance_criteria>

  <done>Three regression checks exist that drive a real byte-garbage file and a real valid oversized PNG through `render_panel()`, all three fail today for the documented reason, and the other 35 checks are untouched and still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the live render path — a never-raises illustration loader with a header-only pixel cap, wired into both D-25/D-26 call sites</name>

  <files>server/plane/render.py</files>

  <behavior>
    - `_illustration_over_pixel_cap(path)` returns `False` for a real vendored illustration, `True` for a PNG whose header declares more than `illustrations.ILLUSTRATION_MAX_PIXELS` pixels, and `True` for a path whose header cannot be read at all (missing, garbage, unreadable) so the caller degrades rather than attempting a decode. It never raises, for any input.
    - `_load_illustration_safely(path, target_w)` returns a resized RGBA image loaded from `path`; from `illustrations.generic_fallback_path()` when `path` is missing, over-cap or undecodable; and `None` when neither candidate is usable. It never raises, for any input, including `None`.
    - `_load_illustration_safely(None, w)` behaves exactly as today's `select_illustration()` returning `None` did end to end: the generic fallback is attempted and, if it is absent, the illustration is skipped — so the already-verified renamed-directory degradation is preserved bit for bit.
    - After the fix all 38 harness checks pass, including the pre-existing determinism check (a repeated render is byte-identical) and the D-24 never-mirrored check.
  </behavior>

  <read_first>
    - `server/plane/render.py` — `_resize_illustration()` (lines 268-277: the `with Image.open(path) as source` / `.convert("RGBA")` / `.resize(..., Image.LANCZOS)` body that must stay functionally unchanged and become the guarded inner call), `draw_illustration()` (lines 280-303: read only to confirm it operates on already-decoded RGBA data and needs no guard of its own), `_build_active_canvas()` (lines 540-590: the two call sites — main at ~562-569 where `draw_main_text_block()` is nested inside the `if`, and previous at ~574-584 where `prev_w` is derived from `main_bbox` before the resize), `_assert_within_canvas()` / `_assert_legal_palette()` (lines 195-206, 510-537: guard rails that must keep passing), and the module's existing imports (line 61 `import os`, line 62 `import sys`, line 64 `from PIL import Image` — no new import is needed).
    - `server/plane/illustrations.py` — `validate_illustration_file()` (lines 380-428). This is the pattern to copy: `try: with Image.open(path) as img:` reading `.format`/`.size` first, the early `return` before any decode once the pixel cap is exceeded (with its comment explaining exactly why), and `except Exception as exc:` turning every Pillow error into a value instead of propagating. Also `ILLUSTRATION_MAX_PIXELS` (line 115), `generic_fallback_path()` (lines 313-314) and `select_illustration()`'s docstring (lines 317-342) for the "never raises for any input" API convention this module already establishes across `normalise_airline_key()`, `classify_aircraft_type()` and `illustration_path_for_key()`.
    - `.planning/phases/03-visual-polish-on-real-glass/03-PATTERNS.md` — "Closest Existing Analog #1" (the validator pattern to reuse), "Closest Existing Analog #2" (why the retired `draw_silhouette()` is architectural precedent only and has no try/except to restore verbatim), the illustrative `_load_illustration_safely()` sketch, and "Constants/Contracts to Preserve".
    - `.planning/phases/03-visual-polish-on-real-glass/03-VERIFICATION.md` — the Key Link table's two `NOT_WIRED` rows, which name precisely what this task wires.
    - `server/poll_loop.py` lines 305-320 — read only to confirm the outer `except Exception` stays untouched; it remains the last-resort net for genuinely unanticipated failures, not this gap's degradation mechanism.
  </read_first>

  <action>
Implement against Task 1's three failing checks until all 38 pass. Change nothing else in
`render.py`.

**Add `_illustration_over_pixel_cap(path)`** immediately above `_resize_illustration()`:

- Open `path` with `Image.open()` inside a `with` block and read `.size` only. Pillow's `open()`
  is lazy — it parses the header without decoding pixel data — which is the whole point.
- Return `True` when `width * height` exceeds `illustrations.ILLUSTRATION_MAX_PIXELS`. Import
  the constant from the `illustrations` module (already imported at line 78); never restate the
  number, so the render path and the offline validator can never drift apart.
- Wrap the whole body in `except Exception` and return `True` on any failure: a header that
  cannot even be parsed is not a file worth decoding, and the caller's next action for `True`
  is to move on to the next candidate, which is the correct response either way.
- Docstring: state that this is the render-path counterpart of
  `illustrations.validate_illustration_file()`'s header-first cap (T-03-03-01 / T-03.1-05-01),
  that it deliberately checks only the pixel count and not the validator's vendor-time rules
  (minimum width, landscape orientation, alpha presence) because those are hand-off quality
  gates and must never cause a live poll cycle to drop a legitimate illustration, and that it
  never raises.

**Add `_load_illustration_safely(path, target_w)`** immediately below
`_illustration_over_pixel_cap()`:

- Build an ordered candidate ladder: the requested `path` first, then
  `illustrations.generic_fallback_path()`. Skip any candidate that is falsy, that is not
  `os.path.isfile()`, or that has already been tried earlier in the ladder — the dedup matters
  because D-08's Tier 4 already returns the generic fallback as the selected path, and without
  it a Tier-4 selection would be attempted and reported twice.
- For each surviving candidate: skip it when `_illustration_over_pixel_cap()` says so;
  otherwise call `_resize_illustration(candidate, target_w)` inside `try` / `except Exception`
  and return its result on success, continuing to the next candidate on failure.
- Return `None` once the ladder is exhausted. Never raise.
- On each rejected candidate write exactly one diagnostic line to `sys.stderr` (`sys` is
  already imported) that names the candidate path and the reason — the exception's class name
  for a decode failure, or the measured pixel count for a cap rejection. Do not print image
  bytes. Silence here would mean a bad file degrades forever with nothing in journald;
  `poll_loop.py` already prints its own failures to the same place.
- Docstring: state that this restores the degradation ladder 03-03-PLAN.md's must-have and
  threat T-03-03-02 specified, whose original home (`draw_silhouette()`'s enclosing try/except)
  was removed by the D-25/D-26 two-flight redesign and never replaced — reference
  03-VERIFICATION.md. State that the last resort is no longer a redrawn silhouette but D-08's
  `generic-fallback.png`, and then "no illustration at all", matching the already-correct
  missing-directory behaviour.

**Rewire `_build_active_canvas()`** — both call sites, not one:

- Main card: keep the `illustrations.select_illustration(route, flight.get("aircraft_type"))`
  call exactly as it is. Replace the load with a single unconditional
  `_load_illustration_safely(main_path, main_w)` and branch the existing block on the returned
  image being non-`None` instead of on the path being non-`None`. This is behaviour-preserving
  for the `None`-path case: the loader's own ladder attempts the generic fallback and returns
  `None` when it is absent, which is exactly what a `None` path meant before. Everything inside
  the block — `main_left`, `draw_illustration()`, `_assert_within_canvas()`,
  `draw_main_text_block()` — stays as written.
- Previous card: keep the outer `previous_flight is not None and main_bbox is not None`
  condition and the `select_illustration()` call. Preserve the ordering — `prev_w` is derived
  from `main_bbox` and must still be computed before the load. Replace the resize with
  `_load_illustration_safely(prev_path, prev_w)` and branch on the returned image being
  non-`None` instead of on `prev_path` being non-`None`. Everything inside — `prev_h`,
  `prev_left`, `prev_top`, `draw_illustration()`, `_assert_within_canvas()`,
  `draw_previous_text_block()` — stays as written.
- After the change, `_build_active_canvas()`'s own source must contain no direct
  `_resize_illustration` call: every decode now goes through the guarded loader.

Leave `_resize_illustration()`, `draw_illustration()`, `_assert_legal_palette()`,
`server/plane/illustrations.py` and `server/poll_loop.py` untouched.
  </action>

  <verify>
    <automated>server/.venv/bin/python3 server/test_render.py &amp;&amp; bash scripts/run-all-tests.sh</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python3 server/test_render.py` exits `0` and its last line is exactly `render: 38/38 checks pass`.
    - Both call sites are wired and no unguarded decode remains inside the composer: `server/.venv/bin/python3 -c "import inspect,sys;sys.path.insert(0,'.');from server.plane import render as r;s=inspect.getsource(r._build_active_canvas);print(s.count('_resize_illustration'), s.count('_load_illustration_safely'))"` prints exactly `0 2`.
    - The header cap is read from the shared constant, inside the loader's own ladder: `server/.venv/bin/python3 -c "import inspect,sys;sys.path.insert(0,'.');from server.plane import render as r;print(inspect.getsource(r._illustration_over_pixel_cap).count('ILLUSTRATION_MAX_PIXELS'))"` prints at least `1`.
    - `server/.venv/bin/python3 -c "import sys;sys.path.insert(0,'.');from server.plane import render as r, illustrations as i;print(r._illustration_over_pixel_cap('/nonexistent/nope.png'), r._illustration_over_pixel_cap(i.generic_fallback_path()))"` prints `True False`.
    - The live repro from 03-VERIFICATION.md's Behavioral Spot-Checks now degrades instead of crashing: `server/.venv/bin/python3 -c "import sys,os,tempfile;sys.path.insert(0,'.');from server.plane import render as r;f=tempfile.NamedTemporaryFile(suffix='.png',delete=False);f.write(b'not a real image');f.close();r.illustrations.select_illustration=lambda route,aircraft_type=None:f.name;print(len(r.render_panel({'hex':'3985a7','callsign':'AF1380','aircraft_type':'B738'},'departing',route={'airline_name':'Air France','origin_city':'Paris','destination_city':'Nice'})));os.unlink(f.name)"` prints `960000`.
    - That same command emits a diagnostic: appending ` 2>&1 | grep -ci illustration` to it outputs at least `1`.
    - `bash scripts/run-all-tests.sh` exits `0` with all nine harnesses reporting and the coverage floor met.
    - `server/.venv/bin/python3 -m ruff check .` reports no findings.
    - `git diff --name-only` across both tasks lists exactly `server/plane/render.py` and `server/test_render.py`.
    - `git status --porcelain server/plane/illustrations.py server/poll_loop.py` outputs nothing — neither file was modified.
  </acceptance_criteria>

  <done>A corrupt or oversized illustration degrades to `generic-fallback.png` and then to no illustration, inside the live render path, for both the main and previous flight cards; `render_panel()` never raises for an undecodable file; all 38 render checks and the full nine-harness suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored PNG on disk → Pillow decode inside a live poll cycle | The only untrusted-input path this plan touches. 33+ opaque binaries delivered across two hand-off rounds (Phase 3's original 8, Phase 3.1's 25) are decoded by Pillow inside the production render process on every poll. Disk corruption, a bad file swap, or a hand-off that slips past a one-time `--validate` run all surface here, at render time, not at review time. |
| `illustrations.select_illustration()`'s returned path → `_resize_illustration()` | `select_illustration()` guarantees the path exists (`os.path.isfile()` at every tier). It guarantees nothing whatsoever about the bytes behind it. That unstated gap is this plan's subject. |
| A failed render → `poll_loop.py`'s outer `except Exception` | Crossing this boundary converts a single bad illustration into a frozen panel for every subsequent cycle. The fix's purpose is to stop the crossing, not to improve the handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Denial of Service | `_resize_illustration()`'s `Image.open(path).convert("RGBA")` reached from `_build_active_canvas()`, both call sites | high | mitigate | `_load_illustration_safely()` wraps every decode in `except Exception` and degrades candidate-by-candidate to D-08's `generic-fallback.png`, then to no illustration. This closes the render-path half of **T-03-03-02** (`03-03-PLAN.md`), whose claimed mitigation — "three-level degradation, all inside a broad `except Exception`" — was removed together with `draw_silhouette()` during the D-25/D-26 redesign and never replaced. 03-VERIFICATION.md reproduced the resulting uncaught `PIL.UnidentifiedImageError` live. Regression-pinned by Task 1's checks 36 and 38, both of which fail against the pre-fix code. |
| T-03-04-02 | Denial of Service | A decompression-bomb or otherwise oversized PNG decoded at render time | high | mitigate | `_illustration_over_pixel_cap()` reads `.size` from the PNG header via Pillow's lazy `Image.open()` and rejects anything above `illustrations.ILLUSTRATION_MAX_PIXELS` (40M) before `.convert()`/`.resize()` touch pixel data — the identical header-first discipline `validate_illustration_file()` already uses offline, reusing the same constant rather than restating it. This closes the render-path half of **T-03.1-05-01** (`.planning/phases/03.1-procedural-per-airline-livery-rendering/03.1-05-PLAN.md`, "Pillow decoding a delivered PNG", high/mitigate) and of its Phase 3 ancestor **T-03-03-01**. Both entries claimed the cap protected the render path; 03-VERIFICATION.md gap #2 established that `validate_illustration_file()` is invoked only from `illustrations.py --validate`, never from `render.py` or `poll_loop.py`. Regression-pinned by Task 1's check 37, which deliberately uses a genuinely valid oversized PNG so that a bare try/except cannot satisfy it — only a real header cap can. |
| T-03-04-03 | Information Disclosure | The one-line degradation diagnostic written to `sys.stderr` | low | accept | The line carries a local asset path and an exception class name only — no image bytes, no credentials, no third-party API payload. It lands in journald beside `poll_loop.py`'s existing `poll_loop: cycle failed: ...` line, which already prints exception text on the same host. Accepted at ASVS L1; the operational cost of a silent permanent degradation is the larger risk. |
| T-03-04-04 | Denial of Service | Re-attempting a persistently bad file on every poll cycle | low | accept | Each cycle costs at most one extra header-only `Image.open()` per bad candidate plus at most two decode attempts, bounded by the fixed two-candidate ladder and its dedup. No result cache is introduced deliberately: a cache would keep serving degraded output after the file is repaired, which is worse for a device polling on a tens-of-seconds cadence. The stderr line keeps the condition visible. |
| T-03-04-05 | Tampering | The guard silently masking a genuinely broken vendored asset set | medium | mitigate | Degradation is never silent: every rejection emits a stderr line, `illustrations.py --validate` (unchanged, and run by `scripts/check-attribution.sh`/CI) still fails loudly on a bad file at hand-off time, and check 38 pins the "everything failed" tail so an all-candidates-bad state still produces a valid panel rather than an exception that hides the cause. The guard is defence in depth behind the vendor-time gate, not a replacement for it. |
| T-03-04-SC | Tampering | npm/pip/cargo supply chain | n/a | accept | Not applicable — zero package-manager installs. This plan adds no dependency; Pillow 12.3.0 is already present in `server/.venv` and is unchanged. The illustration assets are not registry packages and are held to the equivalent bar by T-03-04-01/02 and by 03.1-05's blocking human hand-off gate, which is never auto-approvable. |
</threat_model>

<verification>
Run from the repository root, in order:

1. `server/.venv/bin/python3 server/test_render.py` — exits `0`, last line exactly `render: 38/38 checks pass`.
2. `server/.venv/bin/python3 server/test_illustrations.py` — exits `0` at `42/42`, confirming `illustrations.py` was not touched.
3. `bash scripts/run-all-tests.sh` — exits `0`, all nine harnesses report, coverage floor met.
4. `server/.venv/bin/python3 -m ruff check .` — no findings.
5. `git status --porcelain server/plane/illustrations.py server/poll_loop.py` — empty.
6. Re-check the two gap rows of 03-VERIFICATION.md's Key Link table by inspection: `_build_active_canvas()` → decode error handling is now WIRED via `_load_illustration_safely()`, and the render path now enforces `ILLUSTRATION_MAX_PIXELS` via `_illustration_over_pixel_cap()`.
</verification>

<success_criteria>
- 03-VERIFICATION.md gap #1 (corrupt file) is closed: a byte-garbage `.png` reaching the render path degrades to `generic-fallback.png` and `render_panel()` returns 960,000 bytes.
- 03-VERIFICATION.md gap #2 (oversized file) is closed: a PNG whose header declares more than `ILLUSTRATION_MAX_PIXELS` pixels is rejected in the live render path before any pixel data is decoded.
- The degradation is per-illustration, at both the main and previous call sites — one bad file no longer freezes all future panel updates through `poll_loop.py`'s outer handler.
- Three regression checks pin all of the above; they demonstrably failed against the pre-fix code (RED evidence recorded in the summary) and pass after it.
- Phase 3's seven already-verified must-haves are unaffected: 38/38 render checks and the full nine-harness suite pass, and only `server/plane/render.py` and `server/test_render.py` changed.
</success_criteria>

<output>
Create `.planning/phases/03-visual-polish-on-real-glass/03-04-SUMMARY.md` when done.

Record in it: the exact RED output from Task 1 (the three `FAIL` lines verbatim), the exact
GREEN output from Task 2 (`render: 38/38 checks pass`), the live repro command's `960000`
result, and an explicit statement that 03-VERIFICATION.md's gaps #1 and #2 and its two
`NOT_WIRED` key links are now closed — so the phase can be re-verified against a 8/8 score.
</output>
