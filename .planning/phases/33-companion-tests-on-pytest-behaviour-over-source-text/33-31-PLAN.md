---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 31
type: execute
wave: 8
depends_on: [33-30]
files_modified:
  - companion/test_status_pages_07.py
  - companion/test_status_pages_helpers.py
  - companion/test_status_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "Part 07 of companion/test_status_pages.py (original check() calls #293-#317) runs as native pytest tests in companion/test_status_pages_07.py"
    - "companion/test_status_pages.py no longer exists; its fragment has zero pending rows (317 plus any addendum)"
    - "The .tab-bar__pill margin check resolves --space-xs/--space-sm from custom_properties over the served stylesheet"
    - "The /illustration/{key}.png round trip runs against a function-scoped server and asserts served bytes"
  artifacts:
    - path: "companion/test_status_pages_07.py"
      provides: "part 07 status-pages tests"
      contains: "def test_"
  key_links:
    - from: ".planning/.../33-ledger/companion__test_status_pages.md"
      to: "33-ledger-check.py (no --allow-pending)"
      via: "0 pending, legacy file gone"
      pattern: "ported"
    - from: "companion/test_status_pages_07.py"
      to: "companion_markup.custom_properties"
      via: "token resolution"
      pattern: "custom_properties"
---

<objective>
Finish the status-pages chain: migrate part 07 into `companion/test_status_pages_07.py` and
delete the legacy `companion/test_status_pages.py`. This is the last legacy harness to migrate,
so after this plan the legacy set is empty (TST-14).

Output: the part-07 module, the legacy file removed, and the fragment complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-30-SUMMARY.md
@companion/test_status_pages_helpers.py

<slice>
- Harness: `companion/test_status_pages.py`.
- Part 07 = original check() calls #293-#317 (25 calls, the last of the file), original lines
  14439-16262:
  - FIRST anchor: "the .tab-bar__pill's horizontal margin, resolved from style.css's own --space-xs/--space-sm "
  - LAST anchor: "GET /illustration/{key}.png against a real running service serves normalized bytes that differ "
- Hotspots:
  - C: `.resolve-context[hidden]`, declared after its base rule. Rule order is observable in
    `css_rules` (list order), so assert the relative index;
  - D/J: freshness-line `<time data-relative>` and the breathing-class claims about
    freshness.js. The rendered attributes are D; the JS internals are J (served-asset tokens, or
    deleted with a reason naming the covering browser test).
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 07</name>
  <files>companion/test_status_pages_07.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_status_pages.py from the FIRST anchor for ~900 lines
  </read_first>
  <action>
Port the first half (by count) of part 07 into `companion/test_status_pages_07.py`, applying the
hotspot rules from `<slice>`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_07.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_07.py` exits 0
    - `grep -cE "tempfile|open\(|getsource" companion/test_status_pages_07.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 07 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 07 and deletion of the legacy file</name>
  <files>companion/test_status_pages_07.py, companion/test_status_pages_helpers.py, companion/test_status_pages.py</files>
  <read_first>
    - companion/test_status_pages.py from where Task 1 stopped through the LAST anchor (end of main())
  </read_first>
  <action>
(1) Port the remaining calls, through the LAST anchor. The illustration round trip:
- uploads or seeds an illustration into a function-scoped server's `tmp_path` state;
- GETs `/illustration/<key>.png`;
- asserts that the decoded pixel size and bytes differ from the vendored original, as the
  legacy check did.

(2) Confirm nothing imports the legacy module, then `git rm companion/test_status_pages.py`.

(3) Run every status-pages module together.
  </action>
  <verify>
    <automated>test ! -e companion/test_status_pages.py && server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_0*.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e companion/test_status_pages.py` succeeds
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_0*.py` exits 0
  </acceptance_criteria>
  <done>All status-pages checks run under pytest, and the legacy file is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Close the fragment, confirm the legacy set is empty, commit and push</name>
  <files>.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 4-6
  </read_first>
  <action>
(1) Flip the remaining rows, add `### Part 07 (plan 33-31)`, and run the ledger check WITHOUT
`--allow-pending`.

(2) Confirm that `skypane_test_support.legacy_companion_harnesses()` returns `()`, provided every
other chain has finished (33-04, 33-08, 33-13, 33-18, 33-20, 33-24). If some chain has not,
record which one is outstanding in the SUMMARY; do not fail this plan for it.

(3) Run `33-ledger-check.py --all` and record the result, which reports pending anywhere.

(4) Commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_status_pages.py && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py companion/test_legacy_harness_shim.py -k "(status_pages or detector or exemption or consistent) and not exits_zero" -q && server/.venv/bin/ruff check companion/test_status_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_status_pages.py` exits 0 (0 pending)
    - `grep -c "| pending |" .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md` prints 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>The status-pages harness is fully migrated; the last legacy harness is retired.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-31-01 | Tampering | illustration writes outside tmp_path | mitigate | a function-scoped server state under tmp_path; guard G6 |
| T-33-31-02 | Repudiation | a check lost when the legacy file is deleted | mitigate | ledger-check without --allow-pending before the commit |
| T-33-31-03 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env) |
</threat_model>

<verification>
- all status-pages modules green; ledger rc 0 with no pending; legacy file gone
</verification>

<success_criteria>
All 317 status-pages checks accounted for; no legacy companion harness remains once the other
chains have finished.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-31-SUMMARY.md` when done
</output>
