---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 29
type: execute
wave: 6
depends_on: [33-28]
files_modified:
  - companion/test_status_pages_05.py
  - companion/test_status_pages_05b.py
  - companion/test_status_pages_helpers.py
  - companion/test_status_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 05 of companion/test_status_pages.py (original check() calls #172-#244) runs as native pytest tests in companion/test_status_pages_05.py and _05b"
    - "The all-52-vendored-illustrations normalisation check is parametrised per illustration (so xdist spreads it) and writes only under tmp_path"
    - "French-rendering and i18n catalogue checks assert on rendered text and imported catalogue objects, not on source text"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_status_pages_05.py"
      provides: "part 05 status-pages tests (first half)"
      contains: "def test_"
    - path: "companion/test_status_pages_05b.py"
      provides: "part 05 status-pages tests (second half)"
      contains: "def test_"
  key_links:
    - from: "companion/test_status_pages_05.py"
      to: "companion.illustration_normalize"
      via: "parametrised per-illustration normalisation"
      pattern: "parametrize"
    - from: "companion/test_status_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 05 of `companion/test_status_pages.py` into `companion/test_status_pages_05.py`
and `_05b`. This is plan 5 of 7 in the status-pages chain; at 73 calls it is the largest
status slice.

Output: the part-05 modules, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-28-SUMMARY.md
@companion/test_status_pages_helpers.py

<slice>
- Harness: `companion/test_status_pages.py`.
- Part 05 = original check() calls #172-#244 (73 calls), original lines 9698-12048:
  - FIRST anchor: "Health's and Home's freshness lines are layout.freshness_line_html()'s own output "
  - LAST anchor: "the single lightbox replace form declares method=\"post\", enctype=\"multipart/form-data\" — a "
- Split: calls #172-#208 go into `companion/test_status_pages_05.py`, and #209-#244 into
  `companion/test_status_pages_05b.py`.
- Contents:
  - freshness lines, motion keyframes (C: `keyframes()` plus the declarations referencing
    `var(--motion-fast)`), illustration normalisation (52 vendored PNGs), `login_shell`
    `lang="fr"`, French reminders and relative-age texts, `status_row` escaping;
  - the "every key of companion/i18n_fr/health.py's own CATALOG" check becomes a comparison over
    the imported dicts (S → B);
  - the Airlines gallery, filters and the lightbox forms (D).
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calls #172-#208 into test_status_pages_05.py</name>
  <files>companion/test_status_pages_05.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_status_pages.py from the FIRST anchor for ~1200 lines
    - companion/illustration_normalize.py public API (grep -n "^def ")
  </read_first>
  <action>
Port calls #172-#208. The 52-illustration check becomes
`@pytest.mark.parametrize("key", <sorted vendored illustration keys discovered through the production registry API>)`.
Discover the keys through that API, not by listing files by suffix.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_05.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_05.py` exits 0
    - `grep -cE "tempfile|open\(|getsource" companion/test_status_pages_05.py` prints 0
  </acceptance_criteria>
  <done>Calls #172-#208 run under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Calls #209-#244 into test_status_pages_05b.py</name>
  <files>companion/test_status_pages_05b.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - companion/test_status_pages.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port calls #209-#244. Hostile-value escaping checks render with the script-shaped values and
assert that the parsed DOM contains no injected element, and that the text contains the escaped
literal.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_05.py companion/test_status_pages_05b.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_05b.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages` exits 0
  </acceptance_criteria>
  <done>All 73 part-05 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_status_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md</files>
  <read_first>
    - companion/test_status_pages.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows
and add `### Part 05 (plan 33-29)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py && server/.venv/bin/ruff check companion/test_status_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 05 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-29-01 | Tampering | normalised PNGs written outside tmp_path | mitigate | any output path under tmp_path; guard G6 |
| T-33-29-02 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env) |
| T-33-29-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- part-05 modules, shim (-k status_pages), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
73 more status-pages checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-29-SUMMARY.md` when done
</output>
