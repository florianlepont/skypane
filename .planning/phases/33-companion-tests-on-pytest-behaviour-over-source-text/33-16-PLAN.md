---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 16
type: execute
wave: 4
depends_on: [33-15]
files_modified:
  - companion/test_companion_app_03.py
  - companion/test_companion_app_helpers.py
  - companion/test_companion_app.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 03 of companion/test_companion_app.py (original check() calls #114-#168) runs as native pytest tests in companion/test_companion_app_03.py"
    - "The no-JS control registry checks assert on parsed native form controls in the rendered pages, not on source text"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_companion_app_03.py"
      provides: "part 03 companion-app tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_companion_app_03.py"
      to: "companion_markup.parse_html"
      via: "parsed native controls"
      pattern: "parse_html"
    - from: "companion/test_companion_app.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 03 of `companion/test_companion_app.py` into `companion/test_companion_app_03.py`.
This is plan 3 of 5 in the companion-app chain.

Output: the part-03 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-15-SUMMARY.md
@companion/test_companion_app_helpers.py

<slice>
- Harness: `companion/test_companion_app.py`. Keep its exported plumbing.
- Part 03 = original check() calls #114-#168 (55 calls), original lines 5076-7428:
  - FIRST anchor: "dirty-state.js animates the restored bar's own count element and never its word: "
  - LAST anchor: "every control in _NO_JS_CONTROL_REGISTRY holds its value in a native <input>/"
- Hotspots:
  - JS behaviour described via source (J): prefer the served-asset contract, or delete with code
    J when the claim is about implementation wording. Behaviour that only a browser can observe
    stays covered by the browser modules; name the covering browser test in the deletion reason
    when one exists;
  - `_NO_JS_CONTROL_REGISTRY` checks: render each registered page, parse it, and assert that every
    registered control is a native `input`/`select`/`textarea` inside a `form` with the expected
    name (D).
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 03</name>
  <files>companion/test_companion_app_03.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_companion_app.py from the FIRST anchor for ~1180 lines
  </read_first>
  <action>
Port the first half (by count) of part 03 into `companion/test_companion_app_03.py`, using the
rules' conventions and rubric codes.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_03.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_03.py` exits 0
    - `grep -cE "getsource|ast\.parse|tokenize|open\(" companion/test_companion_app_03.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 03 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 03</name>
  <files>companion/test_companion_app_03.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - companion/test_companion_app.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-03 calls, through the LAST anchor, handling the `_NO_JS_CONTROL_REGISTRY`
checks as described in `<slice>`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_03.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_03.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app` exits 0
  </acceptance_criteria>
  <done>All 55 part-03 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_companion_app.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md</files>
  <read_first>
    - companion/test_companion_app.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, keeping the exported plumbing. Set `EXPECTED_CHECK_COUNT` to the
remaining rows, flip the slice rows and add `### Part 03 (plan 33-16)`. Run section 5 of the
rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py && server/.venv/bin/ruff check companion/test_companion_app*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 03 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-16-01 | Information Disclosure | app.py children reaching providers | mitigate | fake_providers=True + child_env via shared fixtures |
| T-33-16-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-16-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k companion_app), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
55 more companion-app checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-16-SUMMARY.md` when done
</output>
