---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 32
type: execute
wave: 9
depends_on: [33-04, 33-08, 33-13, 33-18, 33-20, 33-24, 33-31]
files_modified:
  - companion/test_legacy_harness_shim.py
  - test-support/skypane_test_support.py
  - test-support/test_test_support.py
  - test-support/companion_app_server.py
  - conftest.py
  - pyproject.toml
  - companion/test_suite_guards.py
  - scripts/run-all-tests.sh
  - .github/workflows/ci.yml
  - .claude/CLAUDE.md
  - README.md
  - CONTRIBUTING.md
  - stub-server/test_devices_registry.py
autonomous: true
requirements: [TST-10, TST-11, TST-12, TST-13, TST-14]

must_haves:
  truths:
    - "companion/test_legacy_harness_shim.py, ORIGINAL_COMPANION_HARNESSES / LEGACY_* / legacy_companion_harnesses(), conftest.py's collect_ignore, the legacy_harness marker and the LegacyHarness transition alias no longer exist"
    - "No EXPECTED_CHECK_COUNT, check() runner or main() runner exists anywhere in companion/, test-support/, server/ or stub-server/ test files"
    - "The TST-10/12/13/14 guard is strict. It scans every companion/test_*.py and companion/conftest.py with no legacy exemption, and it fails on any EXPECTED_CHECK_COUNT / def check / def main"
    - "CI no longer re-includes .planning/ docs in its paths filter, because no test reads them; the browser step names the pytest-playwright suite"
    - "scripts/run-all-tests.sh is still a thin `pytest -n auto --cov` wrapper, with no shim or HARNESS_TIMEOUT_S reference"
    - "CLAUDE.md's Tests / CI row, README and CONTRIBUTING describe the pytest-playwright browser tests, the app-server fixtures and the behaviour-over-source rule, and no longer mention the shim"
  artifacts:
    - path: "companion/test_suite_guards.py"
      provides: "strict guard, no legacy exemption"
      contains: "def scanned_files"
    - path: "scripts/run-all-tests.sh"
      provides: "thin pytest wrapper"
      contains: "-m pytest -n"
    - path: ".claude/CLAUDE.md"
      provides: "updated Tests / CI stack row"
      contains: "pytest-playwright"
  key_links:
    - from: "conftest.py"
      to: "skypane_test_support"
      via: "no collect_ignore any more"
      pattern: "strip_proxy_env"
    - from: ".github/workflows/ci.yml"
      to: "scripts/run-all-tests.sh"
      via: "unchanged CI step with SKYPANE_REQUIRE_BROWSER=1"
      pattern: "SKYPANE_REQUIRE_BROWSER"
---

<objective>
Retire every piece of transition machinery now that no legacy harness remains (TST-14), make the
behaviour-over-source guard strict (TST-12), and update CI and the docs (TST-11). Also fold in the
cheap `stub-server/test_devices_registry.py` tmp_path fix, which CONTEXT's Deferred section
explicitly allows "if cheap".

Output: the shim and legacy lists deleted, the strict guard, a cleaned CI paths filter, and
updated docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-CONTEXT.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@companion/test_legacy_harness_shim.py
@conftest.py
@scripts/run-all-tests.sh

<planner_findings>
- Transition artefacts to delete:
  - `companion/test_legacy_harness_shim.py`;
  - in `test-support/skypane_test_support.py`: `ORIGINAL_COMPANION_HARNESSES`,
    `legacy_companion_harnesses()`, `LEGACY_COMPANION_HARNESSES`, `LEGACY_HELPER_MODULES`,
    `LEGACY_COMPANION_COLLECT_IGNORE`, and their tests in `test-support/test_test_support.py`;
  - `conftest.py` line 25 `collect_ignore = ...` and its comment (lines 21-24), plus the header
    mention in line 3;
  - the `pyproject.toml` marker `legacy_harness` (line ~88) and the coverage comments that name
    the shim (~99, ~113, ~125). Edit the comments only; do NOT change `source`, `omit`, `patch`
    or `sigterm`, since that would invalidate the measured floor;
  - the `LegacyHarness` class in `test-support/companion_app_server.py`, the only place tempfile
    is used in the shared fixtures;
  - the `ALWAYS_EXEMPT` shim entry and the legacy exemption in `companion/test_suite_guards.py`.
- `scripts/run-all-tests.sh` lines 11-24 and 54 describe the shim and `HARNESS_TIMEOUT_S`.
- `.github/workflows/ci.yml`:
  - lines 36-47 (header comment) and both `paths:` blocks re-include
    `06.6.3-CONTEXT.md`, `20-UI-SPEC.md` and `21-UI-SPEC.md` "the legacy companion harnesses
    assert on". Plans 33-12 and 33-28 removed those reads, so drop the three re-include lines
    per trigger and the sentence;
  - keep `server/assets/**/VENDOR.md`, which `check-attribution.sh` reads;
  - line 140's step name mentions `companion/test_browser_ux.py`; rename it to the browser test
    suite generically.
- `.claude/CLAUDE.md` line 36, the Tests / CI row, contains "companion's remaining hand-rolled
  harnesses run through a pytest shim until Phase 33". Replace that clause with
  "companion browser tests on pytest-playwright (a missing browser fails CI)". Keep the rest of
  the row.
- `stub-server/test_devices_registry.py` already uses `child_env()` at every launch site; only
  two `tempfile.mkdtemp` uses remain (lines ~102 and ~416). Converting them to `tmp_path` /
  `tmp_path_factory` is the cheap fold-in.
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the shim, legacy lists, collect_ignore, marker and LegacyHarness; make the guard strict</name>
  <files>companion/test_legacy_harness_shim.py, test-support/skypane_test_support.py, test-support/test_test_support.py, test-support/companion_app_server.py, conftest.py, pyproject.toml, companion/test_suite_guards.py</files>
  <read_first>
    - test-support/skypane_test_support.py (legacy section)
    - test-support/test_test_support.py (tests of legacy_companion_harnesses)
    - test-support/companion_app_server.py (LegacyHarness)
    - conftest.py (whole file)
    - pyproject.toml lines 60-130
    - companion/test_suite_guards.py (ALWAYS_EXEMPT, scanned_files, legacy exemption)
  </read_first>
  <action>
(1) Pre-condition: `legacy_companion_harnesses()` must return `()`, and
`33-ledger-check.py --all` must exit 0 with no pending rows. If either fails, stop: a migration
plan is incomplete.

(2) Delete the artefacts listed in `<planner_findings>`. Update the header comments of
`conftest.py` and `skypane_test_support.py` so they describe only what remains (network guard,
fake providers), in English and without plan IDs.

(3) In `companion/test_suite_guards.py`:
- remove the legacy exemption and the shim entry;
- `scanned_files()` becomes every `companion/test_*.py` plus `companion/conftest.py`, minus only
  the guard module itself;
- add `test_no_legacy_runner_anywhere`, which scans `server/test_*.py`, `stub-server/test_*.py`,
  `test-support/test_*.py` and `companion/test_*.py` for the G8 markers (EXPECTED_CHECK_COUNT,
  module-level `def check` / `def main`, `if __name__ == "__main__"`) and asserts there are none.
  If a server-side file legitimately keeps `if __name__ == "__main__": sys.exit(pytest.main(...))`,
  allow exactly that form and document it.

(4) Run `pytest -n auto -p no:cacheprovider companion test-support -q`. Everything is collected
normally, now that nothing is collect-ignored.
  </action>
  <verify>
    <automated>test ! -e companion/test_legacy_harness_shim.py && ! grep -rn "EXPECTED_CHECK_COUNT\|LEGACY_COMPANION\|legacy_harness\|collect_ignore\|LegacyHarness\|legacy_companion_harnesses" companion test-support conftest.py pyproject.toml scripts --include=*.py --include=*.toml --include=*.sh && SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion test-support -q</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e companion/test_legacy_harness_shim.py` succeeds
    - `grep -rn "EXPECTED_CHECK_COUNT\|LEGACY_COMPANION\|legacy_harness\|collect_ignore\|LegacyHarness" companion test-support conftest.py pyproject.toml scripts` prints nothing
    - `grep -c "tempfile" test-support/companion_app_server.py` prints 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -q` exits 0, and `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py --collect-only -q | grep -c "obeys_behaviour_over_source_rules"` equals `ls companion/test_*.py | wc -l` (every other test file plus conftest.py, minus the guard itself)
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion test-support` exits 0
  </acceptance_criteria>
  <done>No transition machinery remains, and the guard is strict over every companion test module.</done>
</task>

<task type="auto">
  <name>Task 2: run-all-tests.sh comments, the CI paths filter, and the docs</name>
  <files>scripts/run-all-tests.sh, .github/workflows/ci.yml, .claude/CLAUDE.md, README.md, CONTRIBUTING.md</files>
  <read_first>
    - scripts/run-all-tests.sh (whole file)
    - .github/workflows/ci.yml lines 1-160
    - .claude/CLAUDE.md (Technology Stack table)
    - README.md "## Tests" section; CONTRIBUTING.md "Before you start"
  </read_first>
  <action>
(1) `scripts/run-all-tests.sh`:
- rewrite the header so it says pytest discovers every test, including the companion suite and
  its pytest-playwright browser tests;
- remove the `HARNESS_TIMEOUT_S` usage line and every shim mention;
- the command line itself stays byte-identical.

(2) `.github/workflows/ci.yml`:
- remove the three `.planning/...` re-include lines from BOTH the `push` and `pull_request`
  `paths` blocks;
- rewrite the header sentence to say that only `server/assets/**/VENDOR.md` is re-included
  (`check-attribution.sh`), and that tests read no docs (a guard enforces it);
- rename step 140 to "Download the Chromium headless shell the browser tests drive";
- keep `SKYPANE_REQUIRE_BROWSER: "1"`, the cache key and the install command unchanged.

Validate the YAML with
`server/.venv/bin/python3 -c "import yaml"`, if PyYAML is available, or with
`python3 -c "import json,sys"` after `gh workflow view`; otherwise do a careful visual diff.
Record the method used.

(3) `.claude/CLAUDE.md`: make the Tests / CI row edit from `<planner_findings>`, and nothing else
in the file.

(4) `README.md` "## Tests": add a short paragraph.
- Browser tests use pytest-playwright and need
  `server/.venv/bin/python3 -m playwright install --only-shell chromium`.
- Locally, a missing browser is a visible skip; with `SKYPANE_REQUIRE_BROWSER=1` or in CI it
  fails.
- Run only them with `-m browser`.

(5) `CONTRIBUTING.md`: add two bullets.
- Companion tests use the `app_server` / `make_app_server` fixtures and the helpers in
  `test-support/companion_app_server.py` / `companion_markup.py`.
- Tests assert behaviour: rendered HTML, served CSS or JS, or a browser. They never read source
  files, comments or `.planning/`, and `companion/test_suite_guards.py` enforces this.

English only; no plan IDs in user-facing docs.
  </action>
  <verify>
    <automated>! grep -n "HARNESS_TIMEOUT_S\|shim" scripts/run-all-tests.sh && ! grep -n "UI-SPEC.md'\|06.6.3-CONTEXT.md'" .github/workflows/ci.yml && grep -q "SKYPANE_REQUIRE_BROWSER" .github/workflows/ci.yml && grep -q "pytest-playwright" .claude/CLAUDE.md && ! grep -q "until Phase 33" .claude/CLAUDE.md && grep -q "only-shell chromium" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "exec \"\${PYTHON}\" -m pytest -n" scripts/run-all-tests.sh` prints 1 (the command is unchanged)
    - `grep -c "\.planning/phases" .github/workflows/ci.yml` prints 0, and `grep -c "server/assets/\*\*/VENDOR.md" .github/workflows/ci.yml` prints 2
    - `grep -q "until Phase 33" .claude/CLAUDE.md` exits 1, and `grep -q "pytest-playwright" .claude/CLAUDE.md` exits 0
    - `grep -q "test_suite_guards" CONTRIBUTING.md && grep -q "\-m browser" README.md` exits 0
  </acceptance_criteria>
  <done>The runner, CI and docs describe the finished state.</done>
</task>

<task type="auto">
  <name>Task 3: The test_devices_registry.py tmp_path fold-in; full suite as root; commit and push</name>
  <files>stub-server/test_devices_registry.py</files>
  <read_first>
    - stub-server/test_devices_registry.py lines 90-200 and 400-440 (the two mkdtemp sites and their cleanup)
  </read_first>
  <action>
(1) Replace both `tempfile.mkdtemp(...)` uses with pytest temp dirs:
- the server-owning helper class takes a `state_dir` argument supplied from `tmp_path`, or from
  a module-scoped `tmp_path_factory` fixture if it is module-scoped;
- `test_devices_cli_add_list_remove_round_trip` takes `tmp_path`;
- remove the now-redundant `shutil.rmtree` cleanup and the `tempfile` import;
- no behavioural change.

(2) Run `./scripts/run-all-tests.sh` as root with the browser pre-flight
`PLAYWRIGHT_BROWSERS_PATH` exported and `SKYPANE_REQUIRE_BROWSER=1`. Expect green;
`requires_non_root` tests are skipped. Record the pass, skip and coverage numbers in the SUMMARY.
If coverage is below 93 %, investigate before continuing; plan 33-33 owns the gate decision.

(3) Commit and push.
  </action>
  <verify>
    <automated>! grep -n "tempfile" stub-server/test_devices_registry.py && server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider stub-server/test_devices_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tempfile" stub-server/test_devices_registry.py` prints 0
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider stub-server/test_devices_registry.py` exits 0
    - `SKYPANE_REQUIRE_BROWSER=1 ./scripts/run-all-tests.sh` exits 0 as root. The SUMMARY records passed/skipped counts and TOTAL coverage
    - `git status --porcelain` is empty after the commit
  </acceptance_criteria>
  <done>The fold-in is done, and the whole suite runs through one pytest entry point with no transition code left.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI paths filter → test job | a filter that skips runs must never hide a test-relevant change |
| test suite → host filesystem | root runs must stay inside tmp_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-32-01 | Tampering | CI skipping a run on a doc change the tests depend on | mitigate | the strict guard proves no test reads `.planning/` or docs, so dropping the re-includes is safe; VENDOR.md stays |
| T-33-32-02 | Repudiation | a legacy harness silently left behind | mitigate | pre-condition: `legacy_companion_harnesses() == ()` and `ledger-check --all` rc 0; the new `test_no_legacy_runner_anywhere` guard |
| T-33-32-03 | Tampering | writes outside tmp_path | mitigate | LegacyHarness (the last tempfile user in the fixtures) is deleted; devices_registry moves to tmp_path |
| T-33-32-04 | Information Disclosure | test credentials reaching production | accept | deploy/deploy.sh is unchanged; test-support/ and dev locks are never deployed |
</threat_model>

<verification>
- the full suite green as root through `./scripts/run-all-tests.sh`; the strict guard green; no transition artefacts left by grep
</verification>

<success_criteria>
TST-14 is complete: no run_all_tests / hand list / check count / shim remains, and pytest
discovery is the only mechanism.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-32-SUMMARY.md` when done
</output>
