---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 06
type: execute
wave: 3
depends_on: [33-05]
files_modified:
  - companion/test_view_pages_02.py
  - companion/test_view_pages_helpers.py
  - companion/test_view_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 02 of companion/test_view_pages.py (original check() calls #38-#96) runs as native pytest tests in companion/test_view_pages_02.py on the shared fixtures"
    - "The shrunk legacy test_view_pages.py's single EXPECTED_CHECK_COUNT equals the fragment's pending rows, and the harness still passes through the shim"
    - "Every source-text / CSS-text / JS-text check in the slice is rewritten as behaviour, parsed DOM or served asset, or deleted with a coded reason"
  artifacts:
    - path: "companion/test_view_pages_02.py"
      provides: "part 02 view-page tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_view_pages_02.py"
      to: "companion/conftest.py"
      via: "shared fixtures"
      pattern: "app_server"
    - from: "companion/test_view_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 02 of `companion/test_view_pages.py` into `companion/test_view_pages_02.py`
(TST-10, TST-12, TST-13, TST-15). This is plan 2 of 4 in the view-pages chain.

Output: the part-02 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-05-SUMMARY.md
@companion/test_view_pages_helpers.py

<slice>
- Harness: `companion/test_view_pages.py`.
- Part 02 = original check() calls #38-#96 (59 calls), original lines 2359-4667. It is now at the
  top of the remaining `main()`.
  - FIRST anchor: "the rendered Flights table carries zero visible More/Plus/Less/Moins button labels and"
  - LAST anchor: "the resolve dialog's data-view-panel-first-seen/-last-seen carry FORMATTED Europe/Paris"
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 02</name>
  <files>companion/test_view_pages_02.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_view_pages.py from the FIRST anchor (grep -n) for ~1150 lines
    - companion/test_view_pages_01.py (first 80 lines: conventions set by 33-05)
  </read_first>
  <action>
Port the first half (by count) of the part-02 check() calls into
`companion/test_view_pages_02.py`, following 33-MIGRATION-RULES.md sections 2-3:
- one `test_` per check, with the old label as docstring;
- loops are parametrised;
- each check is classified B/D/C/J/S/P/R/T;
- helpers needed by several parts go into `companion/test_view_pages_helpers.py`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_02.py` exits 0
    - `grep -cE "class Harness|def http_request|tempfile|open\(" companion/test_view_pages_02.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 02 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 02</name>
  <files>companion/test_view_pages_02.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - companion/test_view_pages.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-02 calls, through the LAST anchor, with the same conventions. Date and
time formatting checks assert on the rendered attribute values after `parse_html`, and on the
formatter's output for a fixed instant. They never read formatter source.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_02.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_02.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages` exits 0
  </acceptance_criteria>
  <done>All 59 part-02 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_view_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md</files>
  <read_first>
    - companion/test_view_pages.py (the single EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Delete the migrated calls and the closures only they used from the legacy `main()`, set
`EXPECTED_CHECK_COUNT` to the remaining rows, and flip this slice's rows (a coded reason on
every deletion). Add `### Part 02 (plan 33-06)`, run section 5 of the rules, then commit and
push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k view_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_view_pages.py && server/.venv/bin/ruff check companion/test_view_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_view_pages.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k view_pages` exits 0
    - `grep -c "^EXPECTED_CHECK_COUNT" companion/test_view_pages.py` prints 1
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 02 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-06-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-06-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k view_pages), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
59 more view-pages checks migrated or deleted with reasons; the legacy remainder is proven
consistent.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-06-SUMMARY.md` when done
</output>
