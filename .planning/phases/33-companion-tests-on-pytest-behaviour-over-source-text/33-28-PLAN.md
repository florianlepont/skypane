---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 28
type: execute
wave: 5
depends_on: [33-27]
files_modified:
  - companion/test_status_pages_04.py
  - companion/test_status_pages_helpers.py
  - companion/test_status_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 04 of companion/test_status_pages.py (original check() calls #140-#171) runs as native pytest tests in companion/test_status_pages_04.py"
    - "The check that read freshness.js for the ticket ID 260902-chc and opened .planning/phases/06.6.3-.../06.6.3-CONTEXT.md is deleted with a P/J-coded reason; no test reads either file"
    - "The .refresh-pill style.css rule checks assert on css_rules over the served stylesheet"
    - "freshness.js contract checks use the served asset after comment/string stripping, or are deleted with a J-coded reason when they asserted comment wording"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_status_pages_04.py"
      provides: "part 04 status-pages tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_status_pages_04.py"
      to: "companion_markup.css_rules / served_asset"
      via: "served CSS/JS assertions"
      pattern: "css_rules|served_asset|served_css_rules"
    - from: "companion/test_status_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 04 of `companion/test_status_pages.py` into `companion/test_status_pages_04.py`.
This is plan 4 of 7 in the status-pages chain. The slice holds the audit's named evidence sites
for "comment contains a ticket ID" and "reads .planning/…CONTEXT.md" (TST-12).

Output: the part-04 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-27-SUMMARY.md
@companion/test_status_pages_helpers.py

<slice>
- Harness: `companion/test_status_pages.py`.
- Part 04 = original check() calls #140-#171 (32 calls), original lines 7302-9697. The
  anomaly_active check inside this range already moved in 33-25.
  - FIRST anchor: "all three nested Health cards (Battery trend, Unresolved prefixes, Resolution statistics) show one "
  - LAST anchor: "freshness.js's new-row highlight is a DIFF over server-rendered row identity: its two "
- P/J evidence site (original ~8382-8423), the check labelled "the D-12 reversal (260902-chc) is
  written down at both prose sites it touches ...":
  - it greps `static/freshness.js` for "260902-chc" and opens
    `.planning/phases/06.6.3-.../06.6.3-CONTEXT.md`;
  - DELETE it with
    `P: asserted plan-history prose in a freshness.js header comment and in a .planning CONTEXT.md; no behaviour`.
  - Note for 33-32 that `.github/workflows/ci.yml` re-includes that CONTEXT.md only because of
    this read.
- C hotspots:
  - "style.css's .refresh-pill / .refresh-pill[hidden] / pill-scoped icon rules each carry the ..."
    → `declarations_for(css, ".refresh-pill")` / `(".refresh-pill[hidden]")`, etc.;
  - "the desktop-padding/mobile-density pair holds together" → declarations under the matching
    `@media` at_rules.
- J hotspots: freshness.js `swapNodes` / new-row-highlight claims. Prefer the served-asset token
  checks. Pure implementation-wording claims are deleted with code J, naming the browser test
  that covers the behaviour, if any.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 04, including the P/J deletion</name>
  <files>companion/test_status_pages_04.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_status_pages.py from the FIRST anchor for ~1200 lines (the 260902-chc check sits near the end of this range)
  </read_first>
  <action>
Port the first half (by count) of part 04 into `companion/test_status_pages_04.py`, applying the
P/J deletion and the C rewrites from `<slice>`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_04.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_04.py` exits 0
    - `grep -cE "\.planning|260902-chc|open\(|tempfile" companion/test_status_pages_04.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 04 runs under pytest; the planning-doc read is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 04</name>
  <files>companion/test_status_pages_04.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - companion/test_status_pages.py from where Task 1 stopped through the LAST anchor
    - test-support/companion_markup.py (declarations_for with at_rules, strip_js_comments_and_strings)
  </read_first>
  <action>
Port the remaining part-04 calls, through the LAST anchor, applying the C/J rules from `<slice>`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_04.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_04.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages` exits 0
  </acceptance_criteria>
  <done>All 32 part-04 calls are ported or deleted with reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_status_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md</files>
  <read_first>
    - companion/test_status_pages.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls; after this plan the legacy file contains no `.planning` string. Set
`EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows and add
`### Part 04 (plan 33-28)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py && server/.venv/bin/ruff check companion/test_status_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"\.planning"' companion/test_status_pages.py` prints 0
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 04 is migrated; after this plan no status-pages test reads a `.planning/` file.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-28-01 | Repudiation | losing coverage by deleting the P/J check | accept | it asserted only plan-history prose; the reason is recorded; the freshness behaviour is covered by the browser modules |
| T-33-28-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-28-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k status_pages), guard, ledger --allow-pending, ruff: all green; no `.planning` string in any status-pages test
</verification>

<success_criteria>
32 more status-pages checks migrated; the audit's `.planning` / ticket-ID evidence sites are
closed.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-28-SUMMARY.md` when done
</output>
