---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 21
type: execute
wave: 4
depends_on: [33-20]
files_modified:
  - companion/test_browser_ux_01.py
  - companion/test_browser_ux_helpers.py
  - companion/test_browser_ux.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md
autonomous: true
requirements: [TST-10, TST-11, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 01 of companion/test_browser_ux.py (original check() calls #1-#18) runs as per-test pytest-playwright tests in companion/test_browser_ux_01.py"
    - "The self-referential _ASPECT_REPIN_LEDGER check is deleted with an R-coded reason, together with its table in the legacy file"
    - "The shrunk legacy test_browser_ux.py still passes through the shim in a real browser, and its single EXPECTED_CHECK_COUNT equals the pending rows"
    - "With SKYPANE_REQUIRE_BROWSER=1 the new tests run with 0 skips"
  artifacts:
    - path: "companion/test_browser_ux_01.py"
      provides: "part 01 browser UX tests"
      contains: "pytest.mark.browser"
  key_links:
    - from: "companion/test_browser_ux_01.py"
      to: "companion/conftest.py page / new_context"
      via: "pytest-playwright fixtures"
      pattern: "page|new_context"
    - from: "companion/test_browser_ux.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Start the browser_ux chain (4 plans, 33-21..33-24): migrate part 01 of
`companion/test_browser_ux.py` into `companion/test_browser_ux_01.py`, and delete the
self-referential aspect-repin check.

Today one Chromium runs all 75 checks serially, which takes 202 s and dominates the suite's wall
time. Splitting them into per-test functions lets xdist parallelise them (TST-11).

Output: the part-01 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_browser_ux.txt
@companion/test_browser_ux_health_drawings.py

<slice>
- Harness: `companion/test_browser_ux.py`. 75 rows; module-level code at original lines 1-1210,
  with helpers, `_ASPECT_REPIN_LEDGER` and the `EXPECTED_CHECK_COUNT` history at ~140-215;
  `main()` at 1211.
- Legacy runner: ONE `Harness()` seeded with `seed_state_dir`, and ONE browser for all 75 checks.
  - Read-only checks share a module-scoped seeded server:
    `module_app_server_factory(seed=seed_state_dir, fake_providers=True)`.
  - Checks that save share nothing: they use function-scoped `make_app_server`.
  - Per-viewport and no-JS contexts come from the guarded `new_context` fixture.
- Part 01 = original check() calls #1-#18, original lines 1211-2806:
  - FIRST anchor: "_ASPECT_REPIN_LEDGER is non-empty, every row is well-formed (all five "
  - LAST anchor: "at 390px selecting a palette chip ANSWERS - the chip's border-colour changes "
- Rubric R: the first check opens this test file to grep def names. Delete it with
  `R: self-referential CFG-85 aspect-repin bookkeeping; opens test_browser_ux.py to grep def names; no behaviour`,
  and delete the table from the legacy file.
- Module-level helpers of this file that later parts need go into
  `companion/test_browser_ux_helpers.py`, the chain's helpers module, which this chain owns from
  now on.
- Chain-first duty: collapse `EXPECTED_CHECK_COUNT` to one line.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The R deletion and the first half of part 01</name>
  <files>companion/test_browser_ux_01.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 0-3
    - companion/test_browser_ux.py lines 1-140 and 215-1210 (module-level code) and from the FIRST anchor for ~800 lines
    - companion/test_browser_ux_health_drawings.py (the 33-19 conversion conventions)
  </read_first>
  <action>
Do the browser pre-flight. Create `companion/test_browser_ux_01.py` with
`pytestmark = pytest.mark.browser`, the module-scoped seeded server fixture, and the first half
(by count) of part 01, the R check excluded.

Width ladders and theme loops become parametrise ids, using the helpers' `VIEWPORT_*` constants.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_01.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_01.py -rs` exits 0 with 0 skipped
    - `grep -cE "_ASPECT_REPIN_LEDGER|sync_playwright|browser\.new_context|open\(" companion/test_browser_ux_01.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 01 runs per-test in a real browser.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 01</name>
  <files>companion/test_browser_ux_01.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - companion/test_browser_ux.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-01 calls, through the LAST anchor. Palette-chip interactions that SAVE
use a function-scoped server; hover and focus previews that do not save may share the module
server.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_01.py -q -rs && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux -q</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_01.py -rs` exits 0 with 0 skipped
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux` exits 0
  </acceptance_criteria>
  <done>All 18 part-01 calls are ported or deleted with reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify in a browser, commit and push</name>
  <files>companion/test_browser_ux.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md</files>
  <read_first>
    - companion/test_browser_ux.py lines 130-230 (EXPECTED_CHECK_COUNT history, _ASPECT_REPIN_LEDGER) and the main() region
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) In the legacy file: delete the migrated calls and the `_ASPECT_REPIN_LEDGER` table, and
collapse `EXPECTED_CHECK_COUNT` to one line.

(2) Flip the rows: the R row is `deleted`, and ported node ids carry the `[chromium...]` suffix.

(3) Run section 5 of the rules. The shim run uses `SKYPANE_REQUIRE_BROWSER=1`, so it proves the
shrunk legacy file really ran.

(4) Commit and push.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "exits_zero and test_browser_ux" -q -rs && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_browser_ux.py && server/.venv/bin/ruff check companion/test_browser_ux*.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^EXPECTED_CHECK_COUNT" companion/test_browser_ux.py` prints 1, and `grep -c "_ASPECT_REPIN_LEDGER = (" companion/test_browser_ux.py` prints 0
    - The shim run for test_browser_ux passes with 0 skipped under SKYPANE_REQUIRE_BROWSER=1
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_browser_ux.py` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 01 is migrated, the self-read ledger is gone, and the counts are consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-21-01 | Information Disclosure | browser reaching non-loopback hosts | mitigate | guarded fixtures only (G10); navigation to base_url only |
| T-33-21-02 | Repudiation | silent browser skip | mitigate | SKYPANE_REQUIRE_BROWSER=1, 0-skip acceptance, the same for the shim run |
| T-33-21-03 | Tampering | shared-server state leaking across xdist tests | mitigate | mutating tests use a function-scoped server |
</threat_model>

<verification>
- part-01 module and shrunk legacy harness both green in a real browser; ledger --allow-pending rc 0; guard green
</verification>

<success_criteria>
18 browser checks migrated and per-test parallel; the aspect-repin self-read is gone from the
whole companion suite.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-21-SUMMARY.md` when done
</output>
