---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 22
type: execute
wave: 5
depends_on: [33-21]
files_modified:
  - companion/test_browser_ux_02.py
  - companion/test_browser_ux_helpers.py
  - companion/test_browser_ux.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md
autonomous: true
requirements: [TST-10, TST-11, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 02 of companion/test_browser_ux.py (original check() calls #19-#37) runs as per-test pytest-playwright tests in companion/test_browser_ux_02.py"
    - "Any check in the slice that read style.css text asserts the computed style in the browser (_computed_paint / _resolved_property), or is deleted with a C-coded reason"
    - "The shrunk legacy harness still passes through the shim in a real browser, and its EXPECTED_CHECK_COUNT equals the pending rows"
  artifacts:
    - path: "companion/test_browser_ux_02.py"
      provides: "part 02 browser UX tests"
      contains: "pytest.mark.browser"
  key_links:
    - from: "companion/test_browser_ux_02.py"
      to: "companion/test_browser_ux_helpers.py _computed_paint / _resolved_property"
      via: "computed-style assertions"
      pattern: "_computed_paint|_resolved_property"
    - from: "companion/test_browser_ux.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 02 of `companion/test_browser_ux.py` into `companion/test_browser_ux_02.py`. The
slice covers dialog entrance motion, login lockout countdown, refresh regions and the no-JS
freshness line. This is plan 2 of 4 in the browser_ux chain.

Output: the part-02 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-21-SUMMARY.md
@companion/test_browser_ux_01.py

<slice>
- Harness: `companion/test_browser_ux.py`.
- Part 02 = original check() calls #19-#37 (19 calls), original lines 2807-5190:
  - FIRST anchor: "both <dialog>s FADE AND ZOOM in — measured mid-flight, two frames after the "
  - LAST anchor: "with scripts blocked at 360px, in BOTH languages, the freshness line still "
- Login-lockout checks mutate the throttle state: each gets a function-scoped server. Coordinate
  with Phase 37's auth throttle if main has changed login behaviour; see the pre-flight merge
  rule.
- Timing-sensitive motion checks keep their legacy measurement technique (frames after open);
  do not loosen tolerances.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 02</name>
  <files>companion/test_browser_ux_02.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 0-3
    - companion/test_browser_ux.py from the FIRST anchor for ~1200 lines
  </read_first>
  <action>
Do the browser pre-flight. Port the first half (by count) of part 02 into
`companion/test_browser_ux_02.py`, following the conventions from 33-21's module.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_02.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_02.py -rs` exits 0 with 0 skipped
    - `grep -cE "sync_playwright|browser\.new_context|open\(|tempfile" companion/test_browser_ux_02.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 02 runs per-test in a real browser.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 02</name>
  <files>companion/test_browser_ux_02.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - companion/test_browser_ux.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-02 calls, through the LAST anchor. No-JS checks use
`new_context(java_script_enabled=False, viewport=VIEWPORT_MIN_SUPPORTED)`, parametrised over
`lang` en/fr.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_02.py -q -rs && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux -q</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_02.py -rs` exits 0 with 0 skipped
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux` exits 0
  </acceptance_criteria>
  <done>All 19 part-02 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify in a browser, commit and push</name>
  <files>companion/test_browser_ux.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md</files>
  <read_first>
    - companion/test_browser_ux.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows
and add `### Part 02 (plan 33-22)`. Run section 5 of the rules, with the shim under
`SKYPANE_REQUIRE_BROWSER=1`. Then commit and push.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "exits_zero and test_browser_ux" -q -rs && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_browser_ux.py && server/.venv/bin/ruff check companion/test_browser_ux*.py</automated>
  </verify>
  <acceptance_criteria>
    - The shim run for test_browser_ux passes with 0 skipped under SKYPANE_REQUIRE_BROWSER=1
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_browser_ux.py` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 02 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-22-01 | Information Disclosure | browser reaching non-loopback hosts | mitigate | guarded fixtures only (G10) |
| T-33-22-02 | Repudiation | silent browser skip | mitigate | SKYPANE_REQUIRE_BROWSER=1, 0-skip acceptance |
| T-33-22-03 | Tampering | login-throttle state leaking between tests | mitigate | function-scoped server per lockout test |
</threat_model>

<verification>
- part-02 module and shrunk legacy harness green in a real browser; ledger --allow-pending rc 0
</verification>

<success_criteria>
19 more browser checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-22-SUMMARY.md` when done
</output>
