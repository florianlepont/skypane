---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 19
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - companion/test_browser_ux_helpers.py
  - companion/test_browser_ux_health_drawings.py
  - companion/test_browser_ux.py
  - companion/test_browser_ux_quiet_wake.py
  - test-support/skypane_test_support.py
  - companion/test_suite_guards.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux_health_drawings.md
autonomous: true
requirements: [TST-10, TST-11, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "companion/test_browser_ux_helpers.py is an importable helper module (__test__ = False). It takes TEST_PASSWORD from companion_app_server and page/context objects from pytest-playwright's guarded fixtures, and no longer depends on companion.test_companion_app"
    - "companion/test_browser_ux_health_drawings.py is a pytest-playwright module: each of its 11 checks is its own test (parametrised where the legacy check looped), so xdist distributes them"
    - "With SKYPANE_REQUIRE_BROWSER=1 the health-drawings tests run in a real Chromium, with 0 skips; the loopback route guard applies to every context they use"
    - "The still-legacy test_browser_ux.py and test_browser_ux_quiet_wake.py import their Harness from companion_app_server (LegacyHarness), not from test_companion_app, with no change to their checks"
    - "The helpers module leaves collect_ignore (LEGACY_HELPER_MODULES is empty) and passes the guard"
  artifacts:
    - path: "companion/test_browser_ux_health_drawings.py"
      provides: "health-drawing browser tests on pytest-playwright"
      contains: "pytest.mark.browser"
    - path: "companion/test_browser_ux_helpers.py"
      provides: "shared browser helpers usable from pytest"
      contains: "__test__ = False"
  key_links:
    - from: "companion/test_browser_ux_health_drawings.py"
      to: "companion/conftest.py new_context / page / browser override"
      via: "pytest-playwright fixtures"
      pattern: "def test_.*\\(page|new_context"
    - from: "companion/test_browser_ux.py"
      to: "test-support/companion_app_server.py LegacyHarness"
      via: "repointed import"
      pattern: "companion_app_server"
---

<objective>
Lay the browser-test foundation and migrate the first browser harness:

1. Convert `companion/test_browser_ux_helpers.py` into a pytest-usable helper module, which
   removes its dependency on `test_companion_app`.
2. Rewrite `companion/test_browser_ux_health_drawings.py` in place as pytest-playwright tests
   (TST-11: per-test, xdist-parallel, and a missing browser fails in CI).
3. Repoint the two still-legacy browser harnesses' `Harness` import to
   `companion_app_server.LegacyHarness`, so 33-18 can delete `test_companion_app.py`.

Output: the helpers module converted, health_drawings on pytest-playwright, two import lines
repointed, and the fragment complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_browser_ux_health_drawings.txt
@companion/conftest.py

<slice>
- `companion/test_browser_ux_helpers.py` (2712 lines, 41 helpers):
  - line 37 `from companion.test_companion_app import TEST_PASSWORD` → `from companion_app_server import TEST_PASSWORD`;
  - make sure `test-support` is on `sys.path` the same way `companion/conftest.py` does, so the
    legacy script harnesses that import this module still work;
  - add `__test__ = False`;
  - any helper that calls `browser.new_context(...)` / `browser.new_page(...)` takes a context
    factory argument instead (pass pytest-playwright's guarded `new_context` fixture). Legacy
    callers, still-legacy test_browser_ux.py and quiet_wake, pass their own
    `browser.new_context` until they migrate. Keep the two call styles compatible (e.g.
    `make_context=browser.new_context` as the argument). Guard G10 then passes for the helper,
    because the only direct call sites live in legacy files.
- `companion/test_browser_ux_health_drawings.py`: 2092 lines, 11 checks, all in one
  `sync_playwright()` block over ONE seeded `Harness` (`seed_state_dir(harness.tmpdir)`). Convert
  to:
  - a module-scoped seeded server via `module_app_server_factory(seed=seed_state_dir, fake_providers=True)`,
    for the read-only drawing checks; fake providers match the imported legacy Harness;
  - one `test_` per check, taking `page` or `new_context`, with `pytestmark = pytest.mark.browser`;
  - the theme / width / no-JS loops parametrised, e.g. `[light-360]`, `[dark-1280]`;
  - the 1 `style.css` mention checked for rubric C.
- Legacy importers to repoint (import line only, no check change):
  - `companion/test_browser_ux.py:98` `from companion.test_companion_app import Harness, TEST_PASSWORD`;
  - `companion/test_browser_ux_quiet_wake.py:49` `from companion.test_companion_app import Harness`.

  Both become `from companion_app_server import LegacyHarness as Harness` (plus `TEST_PASSWORD`
  where imported), after inserting `os.path.join(REPO_ROOT, "test-support")` into `sys.path`.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert test_browser_ux_helpers.py and repoint the legacy importers</name>
  <files>companion/test_browser_ux_helpers.py, companion/test_browser_ux.py, companion/test_browser_ux_quiet_wake.py, test-support/skypane_test_support.py, companion/test_suite_guards.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 0-2 (browser pre-flight)
    - companion/test_browser_ux_helpers.py lines 1-200 and every `def` that calls browser.new_context/new_page (grep -n "new_context\|new_page\|sync_playwright" companion/test_browser_ux_helpers.py)
    - companion/test_browser_ux.py lines 90-135 and companion/test_browser_ux_quiet_wake.py lines 40-70 (import blocks)
    - test-support/companion_app_server.py (LegacyHarness, TEST_PASSWORD)
    - test-support/skypane_test_support.py LEGACY_HELPER_MODULES; companion/test_suite_guards.py ALWAYS_EXEMPT
  </read_first>
  <action>
(1) Do the browser pre-flight in 33-MIGRATION-RULES.md section 0.

(2) Edit the helpers module as described in `<slice>`.

(3) Repoint the two legacy import lines.

(4) Set `LEGACY_HELPER_MODULES = ()` in `test-support/skypane_test_support.py`. If 33-03 put the
helpers module in `ALWAYS_EXEMPT` in `companion/test_suite_guards.py`, remove that entry, and fix
any remaining guard finding in the helpers:
- comments are fine;
- a helper that read `style.css` from disk is rewritten to computed style or deleted.

(5) Prove nothing regressed by running both still-legacy browser harnesses through the shim with
`SKYPANE_REQUIRE_BROWSER=1`: same PASS counts as their baselines, 75 and 9.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "test_browser_ux or quiet_wake" -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux_helpers -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_companion_app" companion/test_browser_ux_helpers.py companion/test_browser_ux.py companion/test_browser_ux_quiet_wake.py` prints 0 for each file
    - `grep -q "^__test__ = False" companion/test_browser_ux_helpers.py` exits 0
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "test_browser_ux or quiet_wake"` exits 0 with 0 skipped
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux_helpers` exits 0 (the helpers are scanned, not exempt)
  </acceptance_criteria>
  <done>The helpers are pytest-usable and scanned by the guard, and the legacy browser harnesses no longer import test_companion_app.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_browser_ux_health_drawings.py as pytest-playwright tests</name>
  <files>companion/test_browser_ux_health_drawings.py</files>
  <read_first>
    - companion/test_browser_ux_health_drawings.py (whole file, 2092 lines)
    - companion/conftest.py (browser, new_context, blocked_requests, module_app_server_factory)
    - companion/test_browser_ux_helpers.py (_login, _computed_paint, _resolved_property, _in_both_themes, _no_js_page signatures after Task 1)
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_browser_ux_health_drawings.txt (11 labels)
  </read_first>
  <action>
Rewrite the file in place.
- Delete `main()`, `check()`, `EXPECTED_CHECK_COUNT`, the SKIP try/except blocks and
  `sync_playwright()`.
- Add a module-scoped seeded server fixture and `pytestmark = pytest.mark.browser`.
- Write one `test_` per legacy check, with the old label as docstring. Each check's inner loops
  over theme, width and JS on/off become `parametrize` ids, so xdist spreads them.
- Each test logs in through `_login(page, server.base_url())` from the helpers, or uses
  `new_context(...)` for viewport or no-JS variants.
- Classify every check (mostly B/D in the browser); the one style.css mention follows rubric C.

Run the module with `SKYPANE_REQUIRE_BROWSER=1 -n auto`, and compare the wall time with the
baseline's 21.6 s serial; note both in the SUMMARY.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_health_drawings.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_health_drawings.py -rs` exits 0 with 0 skipped and at least 11 passed
    - `grep -cE "EXPECTED_CHECK_COUNT|def check\(|def main\(|sync_playwright|^SKIP|print\(\"SKIP" companion/test_browser_ux_health_drawings.py` prints 0
    - `server/.venv/bin/python3 -m pytest --collect-only -q -p no:cacheprovider companion/test_browser_ux_health_drawings.py | grep -c "\[chromium"` is at least 11
  </acceptance_criteria>
  <done>The health-drawing checks run as per-test pytest-playwright tests in a real browser.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger, guard, commit and push</name>
  <files>.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux_health_drawings.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux_health_drawings.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 4-6
  </read_first>
  <action>
Fill every row: ported node ids carry the `[chromium...]` suffix exactly as `--collect-only`
prints it; a parametrised check points at its first id. Add a part note.

Run the ledger check without `--allow-pending`, the guard, the shim consistency test and ruff.
Then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_browser_ux_health_drawings.py && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py companion/test_legacy_harness_shim.py -k "(browser_ux or detector or exemption or consistent) and not exits_zero" -q && server/.venv/bin/ruff check companion/test_browser_ux*.py test-support</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_browser_ux_health_drawings.py` exits 0 (11 rows, 0 pending)
    - `server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'test-support'); import skypane_test_support as s; assert 'companion/test_browser_ux_health_drawings.py' not in s.LEGACY_COMPANION_HARNESSES and s.LEGACY_HELPER_MODULES == ()"` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>The first browser harness is migrated, and the browser foundation is in place for 33-20..33-24.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chromium → network | browser network stack outside pytest-socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-19-01 | Information Disclosure | browser contexts reaching non-loopback hosts | mitigate | every context comes from the guarded `new_context` / `page` fixtures (guard G10); navigation only to the app server's base_url |
| T-33-19-02 | Repudiation | a browser test silently skipping | mitigate | SKYPANE_REQUIRE_BROWSER=1 in verify, 0-skip acceptance; conftest fails on launch error in CI |
| T-33-19-03 | Denial of Service | breaking still-legacy browser harnesses | mitigate | the shim run of both with SKYPANE_REQUIRE_BROWSER=1 in Task 1 verify |
</threat_model>

<verification>
- health_drawings green in a real browser under xdist; the legacy browser harnesses green; the ledger rc 0; the guard scans the helpers
</verification>

<success_criteria>
11 browser checks migrated. Browser helpers and the importers no longer depend on
test_companion_app.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-19-SUMMARY.md` when done
</output>
