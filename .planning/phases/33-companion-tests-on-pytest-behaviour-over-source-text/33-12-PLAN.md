---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 12
type: execute
wave: 5
depends_on: [33-11]
files_modified:
  - companion/test_config_page_04.py
  - companion/test_config_page_04b.py
  - companion/test_config_page_helpers.py
  - companion/test_config_page.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 04 of companion/test_config_page.py (original check() calls #146-#228) runs as native pytest tests in companion/test_config_page_04.py (with an optional _04a/_04b split)"
    - "The checks that opened .planning/.../20-UI-SPEC.md and 21-UI-SPEC.md no longer read any planning file. Copy-fidelity claims assert the rendered page against literal strings in the test; the rest are deleted with a P-coded reason"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_config_page_04.py"
      provides: "part 04 config-page tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_config_page_04.py"
      to: "rendered config/display pages"
      via: "parse_html text assertions against literal copy"
      pattern: "parse_html"
    - from: "companion/test_config_page.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 04 of `companion/test_config_page.py` into `companion/test_config_page_04.py`.
This is plan 4 of 5 in the config-page chain, and this slice holds one of the audit's named
evidence sites: the 20-UI-SPEC and 21-UI-SPEC reads.

Output: the part-04 module(s), the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-11-SUMMARY.md
@companion/test_config_page_helpers.py

<slice>
- Harness: `companion/test_config_page.py`.
- Part 04 = original check() calls #146-#228 (83 calls, the largest by count), original lines
  8362-11075:
  - FIRST anchor: "up to five suggestion chips render with data-kind/data-value from recent runway events, and "
  - LAST anchor: "with no check-in at all, the Frame strip carries the UNKNOWN delay sentence exactly "
- Hotspot (P), around original lines 8590-8630: `os.path.join(REPO_ROOT, ".planning", "phases", ..., "20-UI-SPEC.md")`
  and the matching `21-UI-SPEC.md` read.
  - If the check verifies that rendered copy equals a sentence in the spec: copy the sentence
    into the test as a literal constant, assert against the rendered page's text, and mark the
    row ported.
  - If it only checks spec prose, it is deleted with
    `P: asserted planning-document prose; no behaviour`.
  - Note in the SUMMARY that `.github/workflows/ci.yml` re-includes those two UI-SPEC paths only
    because of these reads; plan 33-32 removes them.
- Size: 83 calls. Split into `companion/test_config_page_04.py` (first ~42 calls) and
  `companion/test_config_page_04b.py` if that keeps modules readable. List both in the SUMMARY.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 04, including the UI-SPEC reads</name>
  <files>companion/test_config_page_04.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3 (rubric P)
    - companion/test_config_page.py from the FIRST anchor for ~1350 lines (the UI-SPEC reads sit near the start)
  </read_first>
  <action>
Port the first half (by count) of part 04 into `companion/test_config_page_04.py`, using the
rules' conventions.

For each UI-SPEC read, decide P-rewrite vs P-delete as described in `<slice>`, and record the
literal copy constant's provenance in the test docstring, e.g. "copy fixed by the Phase 20
Display redesign". The docstring cites no file path under `.planning/`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_04.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_04.py` exits 0
    - `grep -cE "\.planning|UI-SPEC|open\(" companion/test_config_page_04.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 04 runs under pytest, and no planning document is read.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 04</name>
  <files>companion/test_config_page_04.py, companion/test_config_page_04b.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - companion/test_config_page.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-04 calls, through the LAST anchor, into `test_config_page_04.py` or the
`_04b` split. Frame-strip delay-sentence checks assert on the parsed text of the strip cell for
each seeded check-in state, parametrised by state.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_04*.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_04*.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page` exits 0
  </acceptance_criteria>
  <done>All 83 part-04 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_config_page.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md</files>
  <read_first>
    - companion/test_config_page.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls; after this plan the legacy file must contain no `.planning` string.
Set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows and add
`### Part 04 (plan 33-12)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py && server/.venv/bin/ruff check companion/test_config_page*.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"\.planning"' companion/test_config_page.py` prints 0
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 04 is migrated, the UI-SPEC reads are gone, and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-12-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-12-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-12-03 | Repudiation | dropped checks / silent loss of copy-fidelity coverage | mitigate | each P row names its rewrite or deletion reason; staged ledger-check |
</threat_model>

<verification>
- part-04 modules, shim (-k config_page), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
83 more config-page checks migrated; the audit evidence site (config UI-SPEC reads) is closed.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-12-SUMMARY.md` when done
</output>
