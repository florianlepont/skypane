---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 30
type: execute
wave: 7
depends_on: [33-29]
files_modified:
  - companion/test_status_pages_06.py
  - companion/test_status_pages_helpers.py
  - companion/test_status_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 06 of companion/test_status_pages.py (original check() calls #245-#292) runs as native pytest tests in companion/test_status_pages_06.py"
    - "'style.css declares the new/extended selectors UI-SPEC's Component Inventory enumerates' becomes: every selector in a literal list in the test has a rule in css_rules over the served stylesheet. No UI-SPEC is read"
    - "Retired-symbol checks (e.g. SUPERSEDED_MARKER_*) assert not hasattr(module, name), or are deleted with an S-coded reason"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_status_pages_06.py"
      provides: "part 06 status-pages tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_status_pages_06.py"
      to: "companion_markup.css_rules"
      via: "selector inventory over served CSS"
      pattern: "css_rules|served_css_rules"
    - from: "companion/test_status_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 06 of `companion/test_status_pages.py` into `companion/test_status_pages_06.py`.
The slice covers upload inputs, the resolve section, retired machinery, the Frame strip and the
tab bar. This is plan 6 of 7 in the status-pages chain.

Output: the part-06 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-29-SUMMARY.md
@companion/test_status_pages_helpers.py

<slice>
- Harness: `companion/test_status_pages.py`.
- Part 06 = original check() calls #245-#292 (48 calls), original lines 12049-14438:
  - FIRST anchor: "the whole rendered page carries exactly one <input type=\"file\"> whose id equals "
  - LAST anchor: "the tab bar is display:none until the 959.98px boundary, then fixed to the viewport bottom at "
- Hotspots:
  - C: the selector inventory ("UI-SPEC's Component Inventory"). Copy the selector list as a
    literal into the test; no spec read;
  - C: tab-bar `display:none` then fixed at `@media (max-width: 959.98px)`, via `declarations_for`
    with `at_rules`;
  - S: "the retired D-06 supersession machinery's own symbols (SUPERSEDED_MARKER_TITLE, ...)"
    → `not hasattr`;
  - D: French tab-bar labels.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 06</name>
  <files>companion/test_status_pages_06.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_status_pages.py from the FIRST anchor for ~1200 lines
  </read_first>
  <action>
Port the first half (by count) of part 06 into `companion/test_status_pages_06.py`, applying the
hotspot rules from `<slice>`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_06.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_06.py` exits 0
    - `grep -cE "UI-SPEC\.md\"|\.planning|tempfile|open\(|getsource" companion/test_status_pages_06.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 06 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 06</name>
  <files>companion/test_status_pages_06.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - companion/test_status_pages.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-06 calls, through the LAST anchor. Frame-strip "one wrapper, one
three-row grid" checks parse the DOM for structure and read the grid declarations from the
served CSS.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_06.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_06.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages` exits 0
  </acceptance_criteria>
  <done>All 48 part-06 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_status_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md</files>
  <read_first>
    - companion/test_status_pages.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows
and add `### Part 06 (plan 33-30)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py && server/.venv/bin/ruff check companion/test_status_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 06 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-30-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-30-02 | Tampering | upload inputs writing outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-30-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k status_pages), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
48 more status-pages checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-30-SUMMARY.md` when done
</output>
