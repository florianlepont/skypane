---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 11
type: execute
wave: 4
depends_on: [33-10]
files_modified:
  - companion/test_config_page_03.py
  - companion/test_config_page_helpers.py
  - companion/test_config_page.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 03 of companion/test_config_page.py (original check() calls #85-#145) runs as native pytest tests in companion/test_config_page_03.py"
    - "The does-not-exist path check writes only under tmp_path; no tempfile.mkdtemp survives"
    - "The style.css rule checks in this slice (e.g. no live rule selector for a retired save-status attribute) assert on css_rules over the SERVED stylesheet, or are deleted with a C-coded reason"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_config_page_03.py"
      provides: "part 03 config-page tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_config_page_03.py"
      to: "companion_markup.css_rules / served_stylesheet"
      via: "structural CSS assertions"
      pattern: "css_rules|rules_with_selector"
    - from: "companion/test_config_page.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 03 of `companion/test_config_page.py` into `companion/test_config_page_03.py`.
This is plan 3 of 5 in the config-page chain.

Output: the part-03 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-10-SUMMARY.md
@companion/test_config_page_helpers.py

<slice>
- Harness: `companion/test_config_page.py`.
- Part 03 = original check() calls #85-#145 (61 calls), original lines 5651-8361:
  - FIRST anchor: "after a save that stored wake_interval_s 120, a later submission with wake_interval_s as the empty string, and a"
  - LAST anchor: "the empty state is muted sans copy in .empty-state-plain and carries no <h*> heading element, "
- Hotspots:
  - original ~6078: `nonexistent = os.path.join(tmpdir, "does-not-exist")` → `tmp_path / "does-not-exist"` (T, already safe in intent);
  - "style.css carries NO rule selector referencing STATIC_SAVE_FALLBACK_ATTR" and "style.css carries no live RULE selector for the retired .save-status" → code C: assert over the selectors of `css_rules(served_stylesheet(server))`;
  - the heaviest `tempfile.mkdtemp` user in the file (72 uses in the whole harness): every one becomes `tmp_path`.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 03</name>
  <files>companion/test_config_page_03.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_config_page.py from the FIRST anchor for ~1350 lines
  </read_first>
  <action>
Port the first half (by count) of part 03 into `companion/test_config_page_03.py`, using the
rules' conventions and rubric codes. Every legacy `tempfile.mkdtemp()` + `shutil.rmtree` pair
becomes `tmp_path` (or `tmp_path / "<name>"` when a check needs several dirs).
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_03.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_03.py` exits 0
    - `grep -cE "tempfile|shutil\.rmtree|open\(" companion/test_config_page_03.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 03 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 03</name>
  <files>companion/test_config_page_03.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - companion/test_config_page.py from where Task 1 stopped through the LAST anchor
    - test-support/companion_markup.py (css_rules / rules_with_selector)
  </read_first>
  <action>
Port the remaining part-03 calls, through the LAST anchor.

"No rule selector references X" becomes: no selector string in `css_rules(css)` contains X, in
any at-rule context. It is fetched from the served stylesheet.

Empty-state markup checks use `parse_html` (no `h1`-`h6` inside `.empty-state-plain`).
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_03.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_03.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page` exits 0
  </acceptance_criteria>
  <done>All 61 part-03 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_config_page.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md</files>
  <read_first>
    - companion/test_config_page.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice
rows and add `### Part 03 (plan 33-11)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py && server/.venv/bin/ruff check companion/test_config_page*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 03 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-11-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-11-02 | Tampering | writes outside tmp_path (mkdtemp-heavy slice) | mitigate | tmp_path everywhere; guard G6 rejects tempfile |
| T-33-11-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k config_page), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
61 more config-page checks migrated, with no tempfile use left in the new module.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-11-SUMMARY.md` when done
</output>
