---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 10
type: execute
wave: 3
depends_on: [33-09]
files_modified:
  - companion/test_config_page_02.py
  - companion/test_config_page_helpers.py
  - companion/test_config_page.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 02 of companion/test_config_page.py (original check() calls #34-#84) runs as native pytest tests in companion/test_config_page_02.py"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
    - "Every source-text / CSS-text check in the slice is rewritten as behaviour, parsed DOM or served CSS, or deleted with a coded reason"
  artifacts:
    - path: "companion/test_config_page_02.py"
      provides: "part 02 config-page tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_config_page_02.py"
      to: "companion/conftest.py"
      via: "shared fixtures"
      pattern: "app_server"
    - from: "companion/test_config_page.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 02 of `companion/test_config_page.py` (quiet window span, wake interval and
`handle_post` validation) into `companion/test_config_page_02.py`. This is plan 2 of 5 in the
config-page chain.

Output: the part-02 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-09-SUMMARY.md
@companion/test_config_page_helpers.py

<slice>
- Harness: `companion/test_config_page.py`.
- Part 02 = original check() calls #34-#84 (51 calls), original lines 2936-5650:
  - FIRST anchor: "the quiet window's span goes FORWARD through midnight — 23:00→07:00 is 480 minutes and"
  - LAST anchor: "handle_post rejects \"abc\"/\"1.5\" (handler's int() gate) and \"59\"/\"3601\"/\"-1\" (save_device_config()'s bo"
- Mostly behaviour (B) plus input-validation boundaries. Parametrise the boundary sets (e.g.
  "abc", "1.5", "59", "3601", "-1") with ids.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 02</name>
  <files>companion/test_config_page_02.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_config_page.py from the FIRST anchor for ~1350 lines
    - companion/test_config_page_01.py (first 80 lines: conventions)
  </read_first>
  <action>
Port the first half (by count) of part 02 into `companion/test_config_page_02.py`, using the
rules' conventions and rubric codes.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_02.py` exits 0
    - `grep -cE "class Harness|def http_request|tempfile|open\(" companion/test_config_page_02.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 02 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 02</name>
  <files>companion/test_config_page_02.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - companion/test_config_page.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-02 calls, through the LAST anchor. Each rejection assertion checks the
persisted state read back from the `tmp_path` state dir, and not only the response.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_02.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_02.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page` exits 0
  </acceptance_criteria>
  <done>All 51 part-02 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_config_page.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md</files>
  <read_first>
    - companion/test_config_page.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice
rows and add `### Part 02 (plan 33-10)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py && server/.venv/bin/ruff check companion/test_config_page*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 02 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-10-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-10-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k config_page), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
51 more config-page checks migrated; the remainder is proven consistent.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-10-SUMMARY.md` when done
</output>
