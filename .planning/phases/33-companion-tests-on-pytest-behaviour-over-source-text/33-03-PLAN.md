---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test-support/companion_markup.py
  - test-support/test_companion_markup.py
  - companion/test_suite_guards.py
  - test-support/skypane_test_support.py
  - test-support/test_test_support.py
  - companion/test_legacy_harness_shim.py
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-14]

must_haves:
  truths:
    - "Structural parsers exist for the rewrites. The HTML parser gives parsed-DOM queries; the CSS parser gives selector→declaration lookup, including @media/@supports context and comma selectors. The JS scanner strips comments and strings"
    - "A meta-test fails if any migrated companion test module opens .planning/ or a UI-SPEC, reads a production .py/.js/.css/.html file as text, uses inspect/ast/tokenize on source, or reads __doc__"
    - "The same meta-test fails if a migrated module redefines Harness, http_request or _NoRedirectHandler; uses tempfile or a /nonexistent literal; runs chmod outside requires_non_root; or still carries EXPECTED_CHECK_COUNT, check() or main()"
    - "The guard's exemption set is exactly the still-legacy harnesses, derived from disk, so it shrinks automatically with every migration plan"
    - "The legacy harness list is derived from disk, not hand-edited, so parallel migration plans never edit a shared list"
  artifacts:
    - path: "test-support/companion_markup.py"
      provides: "parse_html/Node.select/find_all/text, css_rules/declarations_for/keyframes/custom_properties, strip_js_comments_and_strings"
      contains: "def css_rules"
    - path: "companion/test_suite_guards.py"
      provides: "TST-10/12/13/14 AST guard with its own detector self-tests"
      contains: "def scan_source"
    - path: "test-support/skypane_test_support.py"
      provides: "ORIGINAL_COMPANION_HARNESSES, legacy_companion_harnesses(), LEGACY_HELPER_MODULES, derived LEGACY_COMPANION_HARNESSES / LEGACY_COMPANION_COLLECT_IGNORE"
      contains: "def legacy_companion_harnesses"
  key_links:
    - from: "companion/test_suite_guards.py"
      to: "skypane_test_support.legacy_companion_harnesses()"
      via: "exemption set = still-legacy harnesses"
      pattern: "legacy_companion_harnesses"
    - from: "conftest.py collect_ignore"
      to: "LEGACY_COMPANION_COLLECT_IGNORE (derived)"
      via: "unchanged name, derived value"
      pattern: "LEGACY_COMPANION_COLLECT_IGNORE"
---

<objective>
Build the toolkit and the gates that the migration plans depend on:

- stdlib structural parsers for HTML, CSS and JS, which replace the source-text idioms (TST-12);
- an AST guard meta-test, `companion/test_suite_guards.py`, which enforces TST-10, TST-12,
  TST-13 and TST-14 on every migrated module from day one;
- the legacy harness set, derived from disk, so the shim runs exactly the not-yet-migrated
  harnesses and no plan hand-edits a shared list.

Purpose: success criterion 3 must be provable mechanically. The guard starts strict for every
non-legacy module; its only exemption is the still-legacy set, which empties as the chains
finish. The closing plan (33-32) deletes that exemption. The disk-derived list keeps the shim
and `collect_ignore` right without parallel plans colliding on a tuple, and replaces the
per-plan one-line removals CONTEXT describes with an equivalent that cannot conflict.

Output: `companion_markup.py` and its tests, `test_suite_guards.py`, the derived legacy list,
and an updated shim meta-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-CONTEXT.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@test-support/skypane_test_support.py
@companion/test_legacy_harness_shim.py
@conftest.py

<planner_findings>
Known evidence sites the guard must catch, used as positive fixtures in its self-tests:
- `os.path.join(REPO_ROOT, ".planning", "phases", ..., "06.6.3-CONTEXT.md")` in test_status_pages.py around line 8402;
- the `20-UI-SPEC.md` / `21-UI-SPEC.md` joins in test_config_page.py around lines 8596-8624;
- `open(os.path.join(HERE, "test_config_page.py"))`, the aspect-repin self-read;
- `open(os.path.join(HERE, "static", "style.css"))`;
- `inspect.getsource(module)`;
- `health_page.anomaly_active("/nonexistent/definitely-not-here")`;
- `tempfile.mkdtemp(prefix=...)`;
- `class Harness:`;
- `def http_request(`.

Negatives the guard must NOT flag:
- a served route string `"/static/style.css"` passed to `http_request`/`get`;
- a docstring or comment that mentions `style.css` or `30-UI-SPEC.md` (docstrings are Expr
  statements; comments are not in the AST);
- `"/no-such-route"`;
- a PNG path under `server/assets`;
- `(tmp_path / "test_probe.py").write_text("...")` (33-02's browser-policy probe).

The legacy harnesses print their summary only via `main()`. A file is still legacy exactly while
it is one of the 9 originals AND contains a line starting `EXPECTED_CHECK_COUNT`
(33-MIGRATION-RULES.md section 1).
</planner_findings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: companion_markup.py — HTML, CSS and JS structural helpers</name>
  <files>test-support/companion_markup.py, test-support/test_companion_markup.py</files>
  <read_first>
    - companion/test_status_pages.py lines 1030-1110 (_css_without_comments and the substring-slicing idioms being replaced)
    - companion/static/style.css first 120 lines and one @media and one @supports block (grep -n "@media\|@supports\|@keyframes\|@starting-style" companion/static/style.css | head) — shapes the parser must handle
    - companion/test_browser_ux_helpers.py lines 560-630 (_computed_paint: the browser-side equivalent, for docstring cross-reference)
  </read_first>
  <behavior>
    - parse_html('<div class="a b"><p id="x">Hi <b>there</b></p><br><img src=y></div>'):
      - select(".a p#x") returns 1 node whose text() is "Hi there"
      - find_all("img")[0].attrs["src"] == "y"
      - void elements do not swallow their following siblings
    - select supports tag, .class, #id, [attr], [attr="v"], the descendant combinator and the child combinator ">"; an unsupported selector raises ValueError (never a silent empty list)
    - css_rules on the three shapes below gives the right selectors, declarations and at_rules:
      - "a{color:red} /* } */ @media (max-width: 959.98px){ .x, .y > z { margin: 0 !important } }"
      - "@supports selector(:has(*)) { .c:has(input:checked) { border-color: var(--color-accent) } }"
      - "@keyframes k { from {opacity:0} to {opacity:1} }"
    - declarations_for(css, ".x") → {} unless at_rules is given; declarations_for(css, ".x", at_rules=("@media (max-width: 959.98px)",)) → {"margin": "0 !important"}
    - a string value containing ";" or "}" (content: "};") does not split a declaration
    - custom-property values with nested parens stay intact: color-mix(in srgb, var(--a) 70%, transparent)
    - keyframes(css) returns {"k"}; custom_properties(css, ":root") returns a dict of --name → value; a selector that matches no rule raises KeyError from declarations_for
    - strip_js_comments_and_strings("var a = '//x'; // c\n/* d */ b = `t`;") keeps "var a = ;" and "b = ;" tokens and removes the comments and every string/template literal
  </behavior>
  <action>
Create `test-support/companion_markup.py`, stdlib only (`html.parser`, `re`):

- `Node(tag, attrs, parent)` with `children`, `text()` (descendant text, whitespace collapsed),
  `find_all(tag=None, cls=None, attrs=None)`, `find(...)` (raises LookupError when absent),
  `select(selector)` and `select_one(selector)`.
- `parse_html(text) -> Node`: the root is a synthetic `#document`; void-element set per HTML5;
  unclosed tags are closed at their parent's end.
- A CSS tokenizer that handles comments, quoted strings, nested blocks, and statement at-rules
  (`@import`, `@charset`).
- `css_rules(css_text) -> list[Rule]`: Rule is a NamedTuple of `selectors` (tuple of
  whitespace-normalised strings), `declarations` (list of `(prop, value)` with `!important`
  kept in the value), and `at_rules` (tuple of enclosing at-rule preludes, whitespace
  normalised). `@keyframes` blocks are recorded separately and are not returned as rules.
- `declarations_for(css_text, selector, at_rules=())`: merges, in source order, every rule
  whose selectors include `selector` exactly and whose `at_rules` tuple equals `at_rules`, with
  the last declaration winning. It raises KeyError when no rule matches.
- `rules_with_selector(css_text, selector)`: every rule in any context.
- `keyframes(css_text) -> set`.
- `custom_properties(css_text, selector=":root", at_rules=())`.
- `strip_js_comments_and_strings(js)`: a small state machine over `//`, `/* */`, quotes and
  backticks; regex literals are out of scope and stated so in the docstring.

Module docstring: these helpers operate on SERVED bytes, fetched over HTTP by the test, never on
files opened from disk (TST-12).

Write `test-support/test_companion_markup.py` covering every `<behavior>` line, RED then GREEN.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider test-support/test_companion_markup.py -v && server/.venv/bin/ruff check test-support/companion_markup.py test-support/test_companion_markup.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider test-support/test_companion_markup.py` exits 0 with at least 10 passed
    - `grep -qE "^def (parse_html|css_rules|declarations_for|keyframes|custom_properties|strip_js_comments_and_strings)\(" test-support/companion_markup.py` matches all six names (check each with its own grep)
    - `grep -c "^import\|^from" test-support/companion_markup.py` shows only stdlib modules (no tinycss2)
  </acceptance_criteria>
  <done>The rewrite toolkit exists, is stdlib only and self-tested, and covers every idiom the legacy harnesses use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The TST-10/12/13/14 AST guard meta-test</name>
  <files>companion/test_suite_guards.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md section 3 (rubric codes the guard enforces)
    - companion/test_status_pages.py lines 8195-8210 and 8385-8425; companion/test_config_page.py lines 1640-1700 and 8590-8630 (real positive shapes)
    - test-support/skypane_test_support.py (requires_non_root name and import path)
  </read_first>
  <behavior>
    - scan_source(src, filename) returns a list of Violation(rule, lineno, detail). Each positive snippet in planner_findings yields exactly its rule:
      - G1: a planning or UI-SPEC string
      - G2: inspect/ast/tokenize/linecache on source
      - G3: open/read_text/read_bytes/os.path.join/Path over a source-suffixed constant, or over __file__
      - G4: .__doc__
      - G5: Harness / _InProcessHarness / _NoRedirectHandler / http_request / HTTPRedirectHandler subclass / build_opener
      - G6: a "/nonexistent" literal, or tempfile.*
      - G7: os.chmod in a function without requires_non_root, unless the module pytestmark has it
      - G8: EXPECTED_CHECK_COUNT, a module-level def check/def main, or `if __name__ == "__main__"`
      - G10: a direct `browser.new_context(` / `browser.new_page(` call or a `sync_playwright(` call (browser tests must use the guarded `new_context` / `page` fixtures from companion/conftest.py)
    - every negative snippet yields []
    - a *_helpers.py source without a module-level `__test__ = False` yields rule G9
    - the per-file test is parametrised over every companion/test_*.py plus companion/conftest.py, minus this file, the shim and every path in legacy_companion_harnesses(); it passes on the current tree
  </behavior>
  <action>
Create `companion/test_suite_guards.py`. The module docstring states the rules in behaviour
terms, citing no plan IDs.

- Implement `scan_source(source, filename)` with `ast.NodeVisitor`, using rules G1–G10 as in
  `<behavior>`.
- G1 inspects `ast.Constant` str values that are NOT docstrings. A docstring is the first `Expr`
  statement of a module, class or function body.
- G3 treats `.py`, `.js`, `.css`, `.html` and `.htm` as source suffixes. It flags:
  (a) calls to `open`, `io.open` or `codecs.open` with no mode or a read mode, whose argument
      subtree contains a source-suffixed constant or `__file__`;
  (b) `.read_text()` / `.read_bytes()` / `.open()` whose receiver subtree contains the same;
  (c) any `os.path.join` / `Path(...)` / Path `/` BinOp that contains a source-suffixed constant
      AND refers to `__file__`, `HERE`, `REPO_ROOT`, or a name ending in `_DIR` or `_ROOT`
      (names starting with `tmp` excluded). This catches a path built in one statement and
      opened in another.

  A constant starting with `/` or `http` is a route, not a path component, and is never flagged.
  Writing a probe file under `tmp_path`, e.g. `(tmp_path / "test_probe.py").write_text(...)`, is
  a negative case.
- G5 allows nothing in a scanned module; the shared definitions live in
  `test-support/companion_app_server.py`, which is not scanned.
- `ALWAYS_EXEMPT` = this file and `companion/test_legacy_harness_shim.py`. The closing plan
  removes the shim entry together with the shim.
- `scanned_files()` = sorted `companion/test_*.py` plus `companion/conftest.py`, minus
  `ALWAYS_EXEMPT`, minus `skypane_test_support.legacy_companion_harnesses()` (Task 3).
- Tests:
  - `test_detector_flags_positive[<rule>-<case>]` and `test_detector_accepts_negative[<case>]`,
    parametrised over in-module snippet tables;
  - `test_module_obeys_behaviour_over_source_rules[<file>]`, parametrised over `scanned_files()`,
    whose assertion message lists every violation as `file:line rule detail`;
  - `test_exemption_set_is_only_legacy_harnesses`: every exempt path is one of the 9 originals or
    in `ALWAYS_EXEMPT`.

On today's tree the scanned set is:
- `companion/test_browser_ux_helpers.py`, which 33-03 does not exempt;
- `companion/conftest.py` and the 33-02 test modules, if 33-02 has landed.

If `test_browser_ux_helpers.py` has G-violations today, fix them only when the fix is a
non-behavioural rename (G9: add `__test__ = False`). Otherwise add it to `ALWAYS_EXEMPT` with the
comment "until 33-19 converts it", and record that in the SUMMARY; 33-19 removes the entry.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -v && server/.venv/bin/ruff check companion/test_suite_guards.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py` exits 0 with at least 20 passed
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k positive -v | grep -c PASSED` is at least 10 (one per rule G1-G10)
    - Deleting `legacy_companion_harnesses()` from the exemption computation in a scratch copy makes `test_module_obeys_behaviour_over_source_rules` fail for companion/test_status_pages.py. This is checked once by hand, and the SUMMARY records the violation count it reported
  </acceptance_criteria>
  <done>The guard is live and strict for every non-legacy module, and its detectors are proven against the audit's evidence shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Derive the legacy harness set from disk and update the shim's meta-test</name>
  <files>test-support/skypane_test_support.py, test-support/test_test_support.py, companion/test_legacy_harness_shim.py</files>
  <read_first>
    - test-support/skypane_test_support.py lines 385-410 (LEGACY_COMPANION_HARNESSES / LEGACY_COMPANION_COLLECT_IGNORE)
    - companion/test_legacy_harness_shim.py (whole file, 116 lines)
    - conftest.py line 25 (collect_ignore consumer; stays unchanged)
    - test-support/test_test_support.py (existing test style)
  </read_first>
  <action>
In `test-support/skypane_test_support.py`, replace the two hand tuples with:

- `ORIGINAL_COMPANION_HARNESSES`: the 9 paths, frozen, in the existing order, with a comment
  that this is the pre-Phase-33 set and never grows.
- `legacy_companion_harnesses()`: returns a tuple of those originals that exist under REPO_ROOT
  AND contain a line matching `^EXPECTED_CHECK_COUNT\s*=`. It reads TEST files only, never
  production source.
- `LEGACY_COMPANION_HARNESSES = legacy_companion_harnesses()`, computed at import.
- `LEGACY_HELPER_MODULES = ("companion/test_browser_ux_helpers.py",)`, with a comment that
  33-19 empties it.
- `LEGACY_COMPANION_COLLECT_IGNORE = LEGACY_COMPANION_HARNESSES + LEGACY_HELPER_MODULES`.

`conftest.py` needs no change.

In `companion/test_legacy_harness_shim.py`:
- keep `test_legacy_companion_harness_exits_zero` as is; it is already parametrised over
  `LEGACY_COMPANION_HARNESSES`. When the set is empty, pytest reports an empty-parameter-set
  skip, which is acceptable until 33-32 deletes the shim;
- replace `test_legacy_harness_list_matches_disk` with `test_legacy_set_is_consistent_with_disk`,
  asserting:
  (a) every legacy path exists;
  (b) every `ORIGINAL_COMPANION_HARNESSES` entry that is NOT legacy is either absent from disk or
      free of `EXPECTED_CHECK_COUNT`, `def check(` and `def main(`;
  (c) no `companion/test_*.py` outside the originals contains a line starting
      `EXPECTED_CHECK_COUNT`, so a new legacy-style file cannot hide.

Add unit tests to `test-support/test_test_support.py` for `legacy_companion_harnesses()`, using a
monkeypatched `REPO_ROOT` pointing at a `tmp_path` fake tree:
- a harness with the marker is legacy;
- a harness without it is not;
- a missing harness is not.

Confirm that today all 9 are still legacy, and that the shim runs `contrast_check` and `i18n`
green.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider test-support/test_test_support.py companion/test_legacy_harness_shim.py -k "legacy or consistent or contrast_check or i18n" -v && server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'test-support'); import skypane_test_support as s; assert len(s.LEGACY_COMPANION_HARNESSES)==9, s.LEGACY_COMPANION_HARNESSES"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def legacy_companion_harnesses" test-support/skypane_test_support.py && grep -q "ORIGINAL_COMPANION_HARNESSES" test-support/skypane_test_support.py` exits 0
    - `grep -q "test_legacy_harness_list_matches_disk" companion/test_legacy_harness_shim.py` exits 1 (replaced)
    - The python one-liner in verify reports 9 legacy harnesses on the current tree
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k consistent` exits 0
    - `git diff --quiet conftest.py` exits 0
  </acceptance_criteria>
  <done>The legacy set is derived from disk. The shim runs whatever is still legacy, and the consistency meta-test replaces the hand-list check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test modules → repo filesystem | a test reading `.planning/` or source text couples tests to prose (the TST-12 risk) |
| test modules → host filesystem | a root run writing outside tmp_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Tampering | host filesystem under root (`/nonexistent` mkdir) | mitigate | guard G6 forbids `/nonexistent` literals and `tempfile.*` in migrated modules; G7 forces `requires_non_root` on chmod tests |
| T-33-03-02 | Repudiation | a migrated check that silently still reads source | mitigate | guard G1–G4 on every non-legacy module; the detectors are self-tested against the audit's evidence shapes |
| T-33-03-03 | Elevation of Privilege | the exemption set growing silently | mitigate | `test_exemption_set_is_only_legacy_harnesses` + `test_legacy_set_is_consistent_with_disk` rule (c) |
</threat_model>

<verification>
- `pytest companion/test_suite_guards.py test-support companion/test_legacy_harness_shim.py -k "not exits_zero"` green
- all 9 harnesses still detected as legacy
</verification>

<success_criteria>
Every migration plan has parsers for its rewrites and a strict guard that proves its new
modules obey TST-10, TST-12 and TST-13. The legacy set shrinks automatically, with no shared
list edits.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-03-SUMMARY.md` when done
</output>
