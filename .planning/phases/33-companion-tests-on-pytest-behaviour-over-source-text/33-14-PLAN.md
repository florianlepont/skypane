---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 14
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - companion/test_companion_app_helpers.py
  - companion/test_companion_app_01.py
  - companion/test_companion_app.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 01 of companion/test_companion_app.py (original check() calls #1-#50) runs as native pytest tests in companion/test_companion_app_01.py on the shared fixtures, with fake providers on, as the legacy Harness had"
    - "The two root-unsafe WR-11 read-only-state-dir checks are pulled forward, ported with @requires_non_root on a tmp_path state dir, and removed from the legacy harness. The shim therefore runs test_companion_app green as root"
    - "The legacy file keeps Harness, TEST_PASSWORD, _InProcessHarness and http_request, because the still-legacy browser harnesses and i18n import them until 33-19 and 33-04"
    - "The shrunk legacy harness's single EXPECTED_CHECK_COUNT equals the pending rows"
  artifacts:
    - path: "companion/test_companion_app_01.py"
      provides: "part 01 + WR-11 root-safe tests"
      contains: "requires_non_root"
    - path: "companion/test_companion_app_helpers.py"
      provides: "calendar transport fakes, public-hostname fake, seeding helpers"
      contains: "__test__ = False"
  key_links:
    - from: "companion/test_companion_app_01.py"
      to: "skypane_test_support.requires_non_root"
      via: "decorator on the WR-11 chmod tests"
      pattern: "@requires_non_root"
    - from: "companion/test_companion_app.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Start the companion-app chain (5 plans, 33-14..33-18):
- create the helpers module;
- migrate part 01 of `companion/test_companion_app.py` into `companion/test_companion_app_01.py`;
- pull the two root-unsafe WR-11 `os.chmod` checks forward (TST-13), so the legacy harness is
  green as root from now on (32-REVIEW IN-05).

Output: the helpers module, the part-01 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_companion_app.txt

<slice>
- Harness: `companion/test_companion_app.py`:
  - 320 baseline rows from 302 check() calls, so some are loop-emitted → parametrise;
  - helpers at original lines 1-1566; `main()` at 1567;
  - the only legacy Harness with `child_env(fake_providers=FakeProviders())` → new tests use
    `make_app_server(fake_providers=True)`, or the module factory with `fake_providers=True`
    (RESEARCH Pitfall 4).
- Part 01 = original check() calls #1-#50, original lines 1567-2676:
  - FIRST anchor: "password_ok() accepts the correct password and rejects a wrong one"
  - LAST anchor: "there is exactly one error-signal colour token "
- Out-of-order pull (rubric T), original lines ~10325-10420, which use `manual_harness` (built
  around ~9494). Two checks:
  - "POST /airlines/resolve redirects with the manual_save_failed flash key (never a dropped connection) when add_entry() cannot write because the state dir is read-only — the exact failure mode CR-01 fixed, exercised end to end (WR-11)"
  - "POST /airlines/manual-resolutions/{prefix}/delete redirects with the manual_delete_failed flash key, leaving the entry in place, when delete_entry() cannot write because the state dir is read-only (WR-11)"

  Both are FAIL rows in the root baseline. Port them with `@requires_non_root`. Each test makes
  its own function-scoped server via `make_app_server(fake_providers=True)`, seeds the registry
  with `poll_loop.save_poll_state`, `os.chmod(state_dir, 0o500)` inside try/finally restoring
  `0o700`, then asserts the 303 plus flash key plus nothing persisted.
- The legacy module must keep exporting `Harness`, `TEST_PASSWORD`, `_InProcessHarness` and
  `http_request` untouched in this plan: 3 browser harnesses and `test_i18n.py` import them
  until 33-19 / 33-04 land. Plan 33-18 deletes the file.
- Chain-first duty: collapse the `EXPECTED_CHECK_COUNT` history (original lines ~100-300).
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers module and the first half of part 01</name>
  <files>companion/test_companion_app_helpers.py, companion/test_companion_app_01.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1-3
    - companion/test_companion_app.py lines 1-100 and 300-1566 (imports and helpers; the Harness/_InProcessHarness/http_request/_login block at 1160-1530 is replaced by companion_app_server; keep _FakeCalendarResponse, _stubbed_calendar_transport, _fake_public_hostname and similar)
    - companion/test_companion_app.py from the FIRST anchor for ~560 lines
    - companion/conftest.py and test-support/companion_app_server.py (grep "^def \|^class ")
  </read_first>
  <action>
(1) Create `companion/test_companion_app_helpers.py` with `__test__ = False`. Move copies of the
reusable non-plumbing helpers into it (calendar transport fakes, public-hostname fake, seeding
helpers). Do not copy `Harness`, `_InProcessHarness`, `http_request`, `_NoRedirectHandler`,
`_cookie_value`, `_login` or any tempfile use. The legacy file keeps its own originals for now.

(2) Create `companion/test_companion_app_01.py` and port the first half (by count) of part 01,
using the rules' conventions. Auth checks (`password_ok`, cookie, login throttling if present)
are B: they stay in-process or use `app_server` via `login()`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_01.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_01.py` exits 0
    - `grep -q "^__test__ = False" companion/test_companion_app_helpers.py` exits 0
    - `grep -cE "class Harness|class _InProcessHarness|def http_request|_NoRedirectHandler|tempfile" companion/test_companion_app_helpers.py companion/test_companion_app_01.py` prints 0 for both files
  </acceptance_criteria>
  <done>The helpers module exists and the first half of part 01 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 01 and the WR-11 root-safe pair</name>
  <files>companion/test_companion_app_01.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - companion/test_companion_app.py from where Task 1 stopped through the LAST anchor
    - companion/test_companion_app.py lines 9485-9560 and 10320-10425 (manual_harness setup and the two WR-11 checks)
    - test-support/skypane_test_support.py requires_non_root
  </read_first>
  <action>
(1) Port the remaining part-01 calls, through the LAST anchor. A colour-token claim ("exactly one
error-signal colour token") is code C: count the custom properties from `custom_properties` over
the served stylesheet whose values equal the error hex, in each theme block. A claim that is
only about the stylesheet header comment is deleted with code C.

(2) Port the two WR-11 checks, as described in `<slice>`, into the same module with
`@requires_non_root`.

Run the module once as root, where the 2 tests show as skipped, and once as nobody:
`runuser -u nobody -- env HOME=/tmp server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_companion_app_01.py -k WR_11_or_read_only`,
adjusting `-k` to the chosen names. If nobody cannot run the venv here, the reason is
permissions (see 32-15-SUMMARY): record that in the SUMMARY and rely on 33-33's non-root run.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_01.py -q -rs && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_01.py -rs` exits 0; as root it reports exactly the 2 WR-11 tests as skipped with the requires_non_root reason
    - `grep -c "@requires_non_root" companion/test_companion_app_01.py` is at least 2
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app` exits 0
  </acceptance_criteria>
  <done>Part 01 and the WR-11 pair are ported, and the pair is root-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify as root, commit and push</name>
  <files>companion/test_companion_app.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md</files>
  <read_first>
    - companion/test_companion_app.py lines 100-300 (EXPECTED_CHECK_COUNT history) and the main() regions being cut
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) In the legacy file:
- delete the part-01 calls and the two WR-11 checks, plus the closures only they use;
- keep `manual_harness` setup if later checks still need it;
- collapse `EXPECTED_CHECK_COUNT` to one line;
- leave `Harness` / `TEST_PASSWORD` / `_InProcessHarness` / `http_request` exported.

(2) Flip the rows: both WR-11 FAIL rows become `ported`, with the node ids of the new
requires_non_root tests. Add `### Part 01 (plan 33-14)`, noting the out-of-order T pull.

(3) Run section 5 of the rules. The shim run for companion_app must now pass AS ROOT.

(4) Commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py && server/.venv/bin/ruff check companion/test_companion_app*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app` exits 0 when run as root (id -u = 0)
    - `grep -c "os.chmod" companion/test_companion_app.py` prints 0
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py` exits 0
    - `grep -qE "^(class Harness|def http_request|class _InProcessHarness|TEST_PASSWORD = )" companion/test_companion_app.py` still matches for all four names (importers intact)
    - `server/.venv/bin/python3 -c "import sys; sys.path[:0]=['.','test-support']; from companion.test_companion_app import Harness, TEST_PASSWORD, _InProcessHarness, http_request"` exits 0. The importers' contract stays intact; 33-19 migrates health_drawings in parallel, so do not run the browser shim here
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 01 is migrated and the WR-11 root failures are gone from the legacy run.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14-01 | Information Disclosure | app.py children reaching providers | mitigate | fake_providers=True as in the legacy Harness, plus the child_env guard; guard G5 forbids local copies |
| T-33-14-02 | Tampering | chmod on a shared dir as root | mitigate | chmod only on the test's own tmp_path state dir, restored in finally; skipped under euid 0 (requires_non_root) |
| T-33-14-03 | Repudiation | dropped checks | mitigate | staged ledger-check; the FAIL rows map to real node ids |
</threat_model>

<verification>
- new module green (2 root skips); shim for companion_app green as root; guard, ledger --allow-pending, ruff green
</verification>

<success_criteria>
50 + 2 checks migrated, and 32-REVIEW IN-05's companion_app half is closed.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-14-SUMMARY.md` when done
</output>
