---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 33
type: execute
wave: 10
depends_on: [33-32]
files_modified:
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md
  - pyproject.toml
autonomous: true
requirements: [TST-11, TST-13, TST-15]

must_haves:
  truths:
    - "33-MIGRATION-LEDGER.md accounts for every pre-migration companion check (1250 measured, plus any Phase 37 addendum): every row is ported to a real node id or deleted with a coded reason, and 0 are pending"
    - "The closing-parity note reconciles Phase 32's 769 plus Phase 33's baseline to the audit's 2018, explains the +1 with its commit, and lists any addendum checks as 'added after audit baseline'"
    - "Coverage measured on a full non-root run is >= 93 % (the pre-migration figure); fail_under is raised to the new floor only if the measurement is higher, never lowered"
    - "The full suite passes as root AND as a non-root user, and after each run git status is clean and /nonexistent gains nothing"
    - "CI on the claude/phase-33 PR is green with browser tests actually executed (not skipped)"
  artifacts:
    - path: ".planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md"
      provides: "assembled ledger with closing parity"
      contains: "Closing parity"
  key_links:
    - from: "33-MIGRATION-LEDGER.md"
      to: "33-ledger-check.py --assemble"
      via: "generated from the 9 fragments"
      pattern: "phase33_total"
    - from: "pyproject.toml fail_under"
      to: "measured non-root full run"
      via: "derivation comment"
      pattern: "fail_under"
---

<objective>
Close Phase 33 with evidence:
- assemble the migration ledger and prove closing parity against the audit's 2018 checks
  (TST-15);
- measure coverage and ratchet the gate if it rose;
- prove the suite passes as root and as non-root without writing outside `tmp_path` (TST-13);
- drive CI green on the phase PR, with browser tests really running (TST-11).

Output: the final ledger, possibly a raised `fail_under`, a green PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-CONTEXT.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/INDEX.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md
@.planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-15-SUMMARY.md
@pyproject.toml

<planner_findings>
- Phase 32's ledger total is 769 (server + stub-server). The audit says 2018 in total, so it
  expects 1249 companion checks; research measured 1250. 33-01's INDEX.md Notes name the commit
  behind the +1.
- The non-root recipe is in 32-15-SUMMARY. `nobody` cannot traverse `/root` or the 0700
  scratchpad, so build the interpreter and venv under a world-readable path, e.g.
  `/tmp/skypane-scratch314`, and delete them afterwards.
- CI runs 3.14. If no 3.14 interpreter is available locally, measure on the best available
  interpreter and state which one in the derivation comment. Also record CI's own coverage line
  from the PR run.
- The `fail_under` derivation comment in pyproject.toml documents the method. Update it with the
  new measurement (date, interpreter, euid, test count, TOTAL %). Change `source`, `omit`, `patch`
  and `sigterm` NOT at all.
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the ledger and write the closing-parity note</name>
  <files>.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/INDEX.md (totals and the +1 note)
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/*.addendum.txt (if any)
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-MIGRATION-LEDGER.md (its totals section)
  </read_first>
  <action>
(1) Run `33-ledger-check.py --all`, which must exit 0, then `--assemble`. Capture the printed
`phase33_total`.

(2) Under the "Closing parity" heading, write:
- the baseline total per harness;
- the addendum count per harness, with commits;
- the ported vs deleted totals, and the deleted totals broken down by rubric code
  (B/D/C/J/S/P/R/T);
- the arithmetic: Phase 32 769 + Phase 33 baseline N = 769 + N, compared with the audit's
  2018, with the difference explained. Expected: +1 from `<sha>` per INDEX.md, plus the
  addendum checks "added after audit baseline";
- the distinct-node-id count and the collected-test count.

(3) Confirm every deleted row's reason starts with a rubric code:
`grep "| deleted |" 33-MIGRATION-LEDGER.md | grep -vcE "\| (B|D|C|J|S|P|R|T): "` must print 0.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --all && grep -q "Closing parity" .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md && ! grep -q "| pending |" .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `33-ledger-check.py --all` exits 0
    - `grep -c "| pending |" 33-MIGRATION-LEDGER.md` prints 0
    - The ledger contains the literal numbers 769 and 2018 and an explanation line naming a commit sha for the difference
    - `grep "| deleted |" .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md | grep -vcE "\| (B|D|C|J|S|P|R|T): "` prints 0
  </acceptance_criteria>
  <done>Every one of the audit's 2018 checks (plus addenda) is accounted for across the Phase 32 and Phase 33 ledgers.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage gate, and root plus non-root runs with a no-stray-writes proof</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml [tool.coverage.report] derivation comment
    - .planning/phases/32-test-foundation-pytest-and-ci-you-can-trust/32-15-SUMMARY.md (non-root measurement recipe and pitfalls)
  </read_first>
  <action>
(1) Record a snapshot: `ls -la /nonexistent 2>/dev/null > <scratch>/before.txt` and
`git status --porcelain` (must be empty).

(2) Root run:
`SKYPANE_REQUIRE_BROWSER=1 PLAYWRIGHT_BROWSERS_PATH=<pre-flight path> ./scripts/run-all-tests.sh`.
It must exit 0, and the only skips allowed are `requires_non_root` tests.

(3) Non-root run:
- build a world-readable interpreter and venv per 32-15, with
  `pip install --require-hashes -r server/requirements-dev.txt`;
- copy the repo to a scratch dir owned by nobody (`git worktree add` or
  `cp -a` + `chown -R nobody`), and install the headless shell into a nobody-readable
  `PLAYWRIGHT_BROWSERS_PATH`;
- run `runuser -u nobody -- env HOME=<scratch> SKYPANE_REQUIRE_BROWSER=1 PLAYWRIGHT_BROWSERS_PATH=<path> PYTHON=<venv>/bin/python3 ./scripts/run-all-tests.sh`
  there;
- it must exit 0 with 0 skips.

(4) After each run:
- `git status --porcelain` in the run's tree is empty; ignored `.coverage*` and `.pytest_cache`
  files may exist, since they are in `.gitignore`. Check with
  `git status --porcelain --ignored`, and list anything unexpected;
- `ls -la /nonexistent` matches the snapshot.

(5) Take TOTAL coverage from the non-root run.
- If it is >= 93.35 and its floor is above 93, raise `fail_under` to the floor (rounded down)
  and update the derivation comment.
- If it is between 93 and 93.99, leave 93 and add the new measurement to the comment.
- If it is below 93, STOP and report which files lost coverage. The gate is never lowered.

(6) Remove the scratch venv, worktree and browser copies, then commit and push.
  </action>
  <verify>
    <automated>grep -q "^fail_under = " pyproject.toml && git diff --quiet -- server/ companion/ stub-server/ test-support/</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records both runs: root (exit 0; skip count equal to the requires_non_root count) and nobody (exit 0, 0 skips), each with passed counts and TOTAL coverage
    - `grep "^fail_under = " pyproject.toml` shows a value >= 93, with the derivation comment updated to this measurement's date, interpreter and euid
    - The SUMMARY shows the `ls -la /nonexistent` before/after as identical, and `git status --porcelain` empty after both runs
  </acceptance_criteria>
  <done>TST-13 and TST-15's coverage clause are proven with measurements.</done>
</task>

<task type="auto">
  <name>Task 3: Drive CI green on the phase PR, with browser tests really executed</name>
  <files>.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-LEDGER.md</files>
  <read_first>
    - .github/workflows/ci.yml (test job steps)
  </read_first>
  <action>
(1) `git push origin claude/phase-33`. If no PR exists for the branch
(`gh pr list --head claude/phase-33`), create a draft PR to main with `gh pr create --draft`:
- title "Phase 33: companion tests on pytest — behaviour over source text";
- body summarising TST-10..TST-15 with the ledger totals, and ending with the attribution lines
  required by the session.

(2) `gh pr checks --watch` until the CI test job completes. If it fails, fix, push and repeat.

(3) From `gh run view --log` of the green run, extract and append under a "CI evidence" heading
in the ledger:
- the pytest summary line;
- the count of `[chromium` tests passed;
- the absence of "skipped" for browser tests;
- CI's own coverage TOTAL.

Commit and push that evidence.
  </action>
  <verify>
    <automated>N="$(gh pr list --head claude/phase-33 --json number -q '.[0].number')" && gh pr checks "$N" | grep -i "Lint, test" | grep -qi "pass" && ! gh pr checks "$N" | grep -i "Lint, test" | grep -qi "fail"</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --head claude/phase-33 --json number,isDraft` returns one PR
    - `gh pr checks <n>` shows the test job passing
    - The ledger's "CI evidence" section shows a non-zero count of `[chromium` tests passed and 0 browser skips, plus the CI coverage TOTAL
  </acceptance_criteria>
  <done>CI is green on the phase PR, and the browser tests ran on GitHub's runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local scratch non-root env → repo | the non-root run must not modify tracked files |
| branch → CI | test job only; deploy runs only on main after reviewer approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-33-01 | Repudiation | parity claimed without evidence | mitigate | the assembled ledger comes from the checker; arithmetic and commit-cited deltas are in the ledger |
| T-33-33-02 | Tampering | coverage gate lowered to pass | mitigate | the plan forbids lowering; stop-and-report below 93 |
| T-33-33-03 | Tampering | writes outside tmp_path in root/non-root runs | mitigate | /nonexistent snapshot comparison + clean git status after both runs |
| T-33-33-04 | Elevation of Privilege | a PR push triggering production deploy | accept | ci.yml deploy runs only on push to main behind a reviewer gate; this plan opens a draft PR only |
</threat_model>

<verification>
- ledger-check --all rc 0; ledger assembled with closing parity; coverage >= 93 measured non-root; root and non-root runs green; CI green with browser tests executed
</verification>

<success_criteria>
All five Phase 33 success criteria from ROADMAP.md are evidenced:
1. every former harness is a pytest module;
2. a missing browser fails CI;
3. no source, comment or `.planning` reads (strict guard);
4. root-safe with no stray writes;
5. 2018 checks accounted for, and coverage >= 93.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-33-SUMMARY.md` when done
</output>
