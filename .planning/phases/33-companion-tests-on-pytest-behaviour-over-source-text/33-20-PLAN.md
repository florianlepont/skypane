---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 20
type: execute
wave: 3
depends_on: [33-19]
files_modified:
  - companion/test_browser_ux_quiet_wake.py
  - companion/test_browser_ux_helpers.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux_quiet_wake.md
autonomous: true
requirements: [TST-10, TST-11, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "companion/test_browser_ux_quiet_wake.py is a pytest-playwright module: each of its 9 checks is its own test (parametrised where it looped), distributed by xdist"
    - "Tests that save quiet hours or wake settings use a function-scoped server; read-only geometry checks may share a module-scoped seeded server; no test depends on another's side effects"
    - "With SKYPANE_REQUIRE_BROWSER=1 all tests run in a real Chromium with 0 skips, and every context is loopback-guarded"
    - "Its ledger fragment has zero pending rows"
  artifacts:
    - path: "companion/test_browser_ux_quiet_wake.py"
      provides: "quiet-hours / wake browser tests"
      contains: "pytest.mark.browser"
  key_links:
    - from: "companion/test_browser_ux_quiet_wake.py"
      to: "companion/conftest.py page / new_context / make_app_server"
      via: "fixtures"
      pattern: "make_app_server|module_app_server_factory"
---

<objective>
Rewrite `companion/test_browser_ux_quiet_wake.py` (9 checks, 2073 lines) in place as
pytest-playwright tests (TST-11, TST-12, TST-14, TST-15).

Output: a pytest-playwright module and a complete fragment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-19-SUMMARY.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_browser_ux_quiet_wake.txt
@companion/test_browser_ux_health_drawings.py

<slice>
- 9 checks inside one `sync_playwright()` block over one seeded `Harness`, now
  `LegacyHarness` after 33-19.
- 3 `open(` calls: identify each one. A file written for upload goes under `tmp_path`; a source
  read follows the rubric.
- Two mentions of `EXPECTED_CHECK_COUNT` in comments or strings (~669, ~769) disappear with the
  rewrite.
- Several checks drive the quiet-hours dial and save. They MUTATE state, so give each one a
  function-scoped `make_app_server(seed=seed_state_dir, fake_providers=True)`.
- Follow the conversion style that 33-19 set in `companion/test_browser_ux_health_drawings.py`.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of the quiet-wake checks</name>
  <files>companion/test_browser_ux_quiet_wake.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 0-3
    - companion/test_browser_ux_quiet_wake.py lines 1-1100
    - companion/test_browser_ux_health_drawings.py (first 120 lines: fixture and parametrisation conventions from 33-19)
  </read_first>
  <action>
Do the browser pre-flight. Start the in-place rewrite:
- keep the legacy `main()` block below the new tests temporarily, while porting;
- port the first 4-5 checks as `test_` functions per the `<slice>` scoping rules, parametrising
  any theme and width loops;
- quiet-hours arc and caption geometry helpers already live in `test_browser_ux_helpers.py`; call
  them rather than copying them.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_quiet_wake.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_quiet_wake.py -rs` exits 0 with 0 skipped
  </acceptance_criteria>
  <done>The first half of the quiet-wake checks runs as pytest-playwright tests.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining checks; remove the legacy runner</name>
  <files>companion/test_browser_ux_quiet_wake.py</files>
  <read_first>
    - companion/test_browser_ux_quiet_wake.py lines 1100-2073
  </read_first>
  <action>
Port the remaining checks. Then delete `main()`, `check()`, `EXPECTED_CHECK_COUNT`, the SKIP
blocks, `sync_playwright()` and the `LegacyHarness` import. Add `pytestmark = pytest.mark.browser`.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_quiet_wake.py -q -rs && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k quiet_wake -q</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_quiet_wake.py -rs` exits 0 with 0 skipped and at least 9 passed
    - `grep -cE "EXPECTED_CHECK_COUNT|def check\(|def main\(|sync_playwright|LegacyHarness|tempfile" companion/test_browser_ux_quiet_wake.py` prints 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k quiet_wake` exits 0
  </acceptance_criteria>
  <done>All 9 checks are native pytest-playwright tests.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger, commit and push</name>
  <files>.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux_quiet_wake.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux_quiet_wake.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 4-6
  </read_first>
  <action>
Fill every row with `[chromium...]` node ids as `--collect-only` prints them. Run the ledger
check without `--allow-pending`, the shim consistency test and ruff. Then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_browser_ux_quiet_wake.py && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k consistent -q && server/.venv/bin/ruff check companion/test_browser_ux*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_browser_ux_quiet_wake.py` exits 0 (9 rows, 0 pending)
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>The quiet-wake harness is fully migrated.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-20-01 | Information Disclosure | browser reaching non-loopback hosts | mitigate | guarded new_context / page fixtures only (G10) |
| T-33-20-02 | Repudiation | silent browser skip | mitigate | SKYPANE_REQUIRE_BROWSER=1 and 0-skip acceptance |
| T-33-20-03 | Tampering | state leaking between xdist-distributed tests | mitigate | mutating tests use a function-scoped server |
</threat_model>

<verification>
- quiet_wake green in a real browser under xdist; ledger rc 0; guard green
</verification>

<success_criteria>
9 browser checks migrated; the file leaves the legacy set.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-20-SUMMARY.md` when done
</output>
