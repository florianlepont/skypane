---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 07
type: execute
wave: 4
depends_on: [33-06]
files_modified:
  - companion/test_view_pages_03.py
  - companion/test_view_pages_helpers.py
  - companion/test_view_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 03 of companion/test_view_pages.py (original check() calls #97-#134) runs as native pytest tests in companion/test_view_pages_03.py"
    - "The checks that grepped companion/static/*.js source (e.g. panel-lookup.js has no date API) now assert on the SERVED asset after comment/string stripping, or on browser-observable behaviour, or are deleted with a J-coded reason"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_view_pages_03.py"
      provides: "part 03 view-page tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_view_pages_03.py"
      to: "companion_app_server.served_asset + companion_markup.strip_js_comments_and_strings"
      via: "served JS contract checks"
      pattern: "served_asset"
    - from: "companion/test_view_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 03 of `companion/test_view_pages.py` into `companion/test_view_pages_03.py`. This
is plan 3 of 4 in the view-pages chain, and this slice holds most of the JS-source checks
(rubric J).

Output: the part-03 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-06-SUMMARY.md
@companion/test_view_pages_helpers.py

<slice>
- Harness: `companion/test_view_pages.py`.
- Part 03 = original check() calls #97-#134 (38 calls), original lines 4668-6981:
  - FIRST anchor: "companion/static/panel-lookup.js contains no date-parsing or date-formatting API at all"
  - LAST anchor: "under a French request Home's headings ('Vols récents'/'Voir tous les vols') and a"
- Hotspots: JS-source reads of `companion/static/*.js` (J) and French-rendering checks (D).
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 03 (the JS-contract checks)</name>
  <files>companion/test_view_pages_03.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md section 3 (rubric J and S)
    - companion/test_view_pages.py from the FIRST anchor for ~1150 lines
    - test-support/companion_markup.py strip_js_comments_and_strings docstring
  </read_first>
  <action>
Port the first half (by count) of the part-03 calls into `companion/test_view_pages_03.py`.

A JS delivery contract, for example "panel-lookup.js contains no date-parsing API" or "ES5 only",
is fetched through `served_asset(app_server, "/static/<name>.js")`, run through
`strip_js_comments_and_strings`, then checked for the forbidden identifiers as whole tokens
(`\bDate\b`, `toLocale...`, `Intl`), so a comment or string can no longer make the check pass
or fail.

A check whose real claim is that the server supplies a formatted value is better proven by
parsing the rendered attribute, which is D.

Assertions on a JS comment are deleted with code J.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_03.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_03.py` exits 0
    - `grep -cE "open\(|os\.path\.join\(HERE" companion/test_view_pages_03.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 03 runs under pytest; JS contracts are checked against served bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 03</name>
  <files>companion/test_view_pages_03.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - companion/test_view_pages.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-03 calls, through the LAST anchor. French-rendering checks render under
`lang="fr"`, either in-process via `prefs.set_request_prefs` as the legacy code does, or over
HTTP with the language cookie or query the app supports. They assert on the text of the parsed
headings and links.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_03.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_03.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages` exits 0
  </acceptance_criteria>
  <done>All 38 part-03 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_view_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md</files>
  <read_first>
    - companion/test_view_pages.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls from the legacy `main()` and set `EXPECTED_CHECK_COUNT` to the
remaining rows. Flip this slice's rows, add `### Part 03 (plan 33-07)`, run section 5 of the
rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k view_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_view_pages.py && server/.venv/bin/ruff check companion/test_view_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_view_pages.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k view_pages` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 03 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-07-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-07-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k view_pages), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
38 more checks migrated; no JS-source grep survives in the new module.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-07-SUMMARY.md` when done
</output>
