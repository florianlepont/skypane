---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 17
type: execute
wave: 5
depends_on: [33-16]
files_modified:
  - companion/test_companion_app_04.py
  - companion/test_companion_app_04b.py
  - companion/test_companion_app_helpers.py
  - companion/test_companion_app.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 04 of companion/test_companion_app.py (original check() calls #169-#248) runs as native pytest tests in companion/test_companion_app_04.py (and _04b)"
    - "JS_GATE_CLASS / selector-existence checks resolve the class against css_rules over the SERVED stylesheet, never style.css on disk"
    - "The login next= route check uses /no-such-route instead of a /nonexistent literal, with unchanged behaviour"
    - "The real-PNG upload round trip uses a function-scoped server with fake providers and writes only under tmp_path"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_companion_app_04.py"
      provides: "part 04 companion-app tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_companion_app_04.py"
      to: "companion_markup.css_rules"
      via: "JS gate class resolves to a served rule"
      pattern: "css_rules|rules_with_selector"
    - from: "companion/test_companion_app.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 04 of `companion/test_companion_app.py` into `companion/test_companion_app_04.py`
and `_04b`. This is plan 4 of 5 in the companion-app chain; at 80 calls it is the largest
companion-app slice.

Output: the part-04 modules, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-16-SUMMARY.md
@companion/test_companion_app_helpers.py

<slice>
- Harness: `companion/test_companion_app.py`. Keep its exported plumbing.
- Part 04 = original check() calls #169-#248 (80 calls), original lines 7429-9825:
  - FIRST anchor: "layout.JS_GATE_CLASS resolves to a real selector in companion/static/style.css on "
  - LAST anchor: "uploading a real PNG over real HTTP to a real companion/app.py subprocess changes "
- Hotspots:
  - original ~7829/7839: `base + "/login?next=/nonexistent-route"` → `/no-such-route`. The
    behaviour (not a real NAV_TABS member → safe default) is unchanged;
  - original ~7863-7987: 22-UI-SPEC.md mentions are comments or labels only; no file read;
  - `manual_harness` (~9494/9543) setups → function-scoped `make_app_server(fake_providers=True)`.
- Split: `companion/test_companion_app_04.py` takes calls #169-#208, and
  `companion/test_companion_app_04b.py` takes #209-#248.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calls #169-#208 into test_companion_app_04.py</name>
  <files>companion/test_companion_app_04.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_companion_app.py from the FIRST anchor for ~1200 lines
    - companion/layout.py JS_GATE_CLASS (grep -n JS_GATE_CLASS companion/layout.py)
  </read_first>
  <action>
Port calls #169-#208 into `companion/test_companion_app_04.py`.

"JS_GATE_CLASS resolves to a real selector" becomes: some rule in
`css_rules(served_stylesheet(server))` has a selector containing `"." + layout.JS_GATE_CLASS`.

Apply the `/no-such-route` rename. Everything else follows the rubric.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_04.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_04.py` exits 0
    - `grep -cE "/nonexistent|open\(|tempfile" companion/test_companion_app_04.py` prints 0
  </acceptance_criteria>
  <done>Calls #169-#208 run under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Calls #209-#248 into test_companion_app_04b.py</name>
  <files>companion/test_companion_app_04b.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - companion/test_companion_app.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port calls #209-#248 into `companion/test_companion_app_04b.py`. The upload round trip builds
its PNG in memory with Pillow or writes it under `tmp_path`, POSTs multipart through
`http_request` against a function-scoped `make_app_server(fake_providers=True)`, and asserts on
the served bytes afterwards.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_04.py companion/test_companion_app_04b.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_04b.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app` exits 0
  </acceptance_criteria>
  <done>All 80 part-04 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_companion_app.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md</files>
  <read_first>
    - companion/test_companion_app.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, keeping the exported plumbing. Set `EXPECTED_CHECK_COUNT` to the
remaining rows, flip the slice rows and add `### Part 04 (plan 33-17)`. Run section 5 of the
rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py && server/.venv/bin/ruff check companion/test_companion_app*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 04 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-17-01 | Information Disclosure | app.py children reaching providers | mitigate | fake_providers=True + child_env via shared fixtures |
| T-33-17-02 | Tampering | upload writes outside tmp_path | mitigate | the fixture state dir is under tmp_path; guard G6 |
| T-33-17-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- part-04 modules, shim (-k companion_app), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
80 more companion-app checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-17-SUMMARY.md` when done
</output>
