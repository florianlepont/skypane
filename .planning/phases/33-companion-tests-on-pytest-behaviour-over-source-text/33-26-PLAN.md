---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 26
type: execute
wave: 3
depends_on: [33-25]
files_modified:
  - companion/test_status_pages_02.py
  - companion/test_status_pages_helpers.py
  - companion/test_status_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 02 of companion/test_status_pages.py (original check() calls #38-#85) runs as native pytest tests in companion/test_status_pages_02.py"
    - "Regularity-grid, axis-label and health-state checks assert on parsed SVG/HTML and on function outputs, not on source text"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_status_pages_02.py"
      provides: "part 02 status-pages tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_status_pages_02.py"
      to: "companion_markup.parse_html"
      via: "parsed SVG/HTML assertions"
      pattern: "parse_html"
    - from: "companion/test_status_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 02 of `companion/test_status_pages.py` into `companion/test_status_pages_02.py`.
This is plan 2 of 7 in the status-pages chain.

Output: the part-02 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-25-SUMMARY.md
@companion/test_status_pages_helpers.py

<slice>
- Harness: `companion/test_status_pages.py`.
- Part 02 = original check() calls #38-#85 (48 calls), original lines 2611-4931:
  - FIRST anchor: "battery_sparkline_svg() emits exactly four aria-hidden axis-label text nodes carrying the FIXED "
  - LAST anchor: "compute_health_state()'s pipeline_detail_html key, for a never-ran pipeline, is the bare "
- Content: sparkline axis labels, the regularity grid (`wake.classify_check_in_gap`), daily chart
  points, `_axis_day_label` time zones, device tile verdicts, `layout.empty_state`, and
  `compute_health_state`. This is mostly B/D.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 02</name>
  <files>companion/test_status_pages_02.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_status_pages.py from the FIRST anchor for ~1160 lines
    - companion/test_status_pages_01.py (first 80 lines: conventions)
  </read_first>
  <action>
Port the first half (by count) of part 02 into `companion/test_status_pages_02.py`, following the
rules' conventions and rubric. Per-day and per-threshold loops become parametrised tests.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_02.py` exits 0
    - `grep -cE "tempfile|open\(|getsource" companion/test_status_pages_02.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 02 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 02</name>
  <files>companion/test_status_pages_02.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - companion/test_status_pages.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-02 calls, through the LAST anchor. A "byte-identical output" claim
compares two function outputs; it does not compare against source text.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_02.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_02.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages` exits 0
  </acceptance_criteria>
  <done>All 48 part-02 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_status_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md</files>
  <read_first>
    - companion/test_status_pages.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows
and add `### Part 02 (plan 33-26)`. Run section 5 of the rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py && server/.venv/bin/ruff check companion/test_status_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 02 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-26-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-26-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-26-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k status_pages), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
48 more status-pages checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-26-SUMMARY.md` when done
</output>
