---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 23
type: execute
wave: 6
depends_on: [33-22]
files_modified:
  - companion/test_browser_ux_03.py
  - companion/test_browser_ux_helpers.py
  - companion/test_browser_ux.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md
autonomous: true
requirements: [TST-10, TST-11, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 03 of companion/test_browser_ux.py (original check() calls #38-#62) runs as per-test pytest-playwright tests in companion/test_browser_ux_03.py"
    - "The zero-request check for uncommitted edits observes requests through Playwright's request events on the guarded context, not through source text"
    - "The shrunk legacy harness still passes through the shim in a real browser, and its EXPECTED_CHECK_COUNT equals the pending rows"
  artifacts:
    - path: "companion/test_browser_ux_03.py"
      provides: "part 03 browser UX tests"
      contains: "pytest.mark.browser"
  key_links:
    - from: "companion/test_browser_ux_03.py"
      to: "companion/conftest.py page / new_context"
      via: "pytest-playwright fixtures"
      pattern: "page|new_context"
    - from: "companion/test_browser_ux.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 03 of `companion/test_browser_ux.py` into `companion/test_browser_ux_03.py`. The
slice covers countdowns, Display-page edit and save flows, and palette chip previews. This is
plan 3 of 4 in the browser_ux chain.

Output: the part-03 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-22-SUMMARY.md
@companion/test_browser_ux_01.py

<slice>
- Harness: `companion/test_browser_ux.py`.
- Part 03 = original check() calls #38-#62 (25 calls), original lines 5191-7612:
  - FIRST anchor: "a countdown whose instant has already passed reads the server's own "
  - LAST anchor: "hovering or keyboard-focusing an unchecked palette chip previews that "
- Display-page save flows MUTATE state, so they use function-scoped servers. Request-count
  assertions use `page.on("request", ...)` on the guarded context.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 03</name>
  <files>companion/test_browser_ux_03.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 0-3
    - companion/test_browser_ux.py from the FIRST anchor for ~1200 lines
  </read_first>
  <action>
Do the browser pre-flight. Port the first half (by count) of part 03 into
`companion/test_browser_ux_03.py`, following the chain's conventions.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_03.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_03.py -rs` exits 0 with 0 skipped
    - `grep -cE "sync_playwright|browser\.new_context|open\(|tempfile" companion/test_browser_ux_03.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 03 runs per-test in a real browser.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 03</name>
  <files>companion/test_browser_ux_03.py, companion/test_browser_ux_helpers.py</files>
  <read_first>
    - companion/test_browser_ux.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-03 calls, through the LAST anchor. Hover and focus preview checks use
`_computed_paint` before and after the interaction; they never compare against stylesheet text.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_03.py -q -rs && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux -q</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_ux_03.py -rs` exits 0 with 0 skipped
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k browser_ux` exits 0
  </acceptance_criteria>
  <done>All 25 part-03 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify in a browser, commit and push</name>
  <files>companion/test_browser_ux.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md</files>
  <read_first>
    - companion/test_browser_ux.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_browser_ux.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, set `EXPECTED_CHECK_COUNT` to the remaining rows, flip the slice rows
and add `### Part 03 (plan 33-23)`. Run section 5 of the rules with the shim under
`SKYPANE_REQUIRE_BROWSER=1`. Then commit and push.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "exits_zero and test_browser_ux" -q -rs && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_browser_ux.py && server/.venv/bin/ruff check companion/test_browser_ux*.py</automated>
  </verify>
  <acceptance_criteria>
    - The shim run for test_browser_ux passes with 0 skipped under SKYPANE_REQUIRE_BROWSER=1
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_browser_ux.py` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 03 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-23-01 | Information Disclosure | browser reaching non-loopback hosts | mitigate | guarded fixtures only (G10) |
| T-33-23-02 | Repudiation | silent browser skip | mitigate | SKYPANE_REQUIRE_BROWSER=1, 0-skip acceptance |
| T-33-23-03 | Tampering | save flows leaking state across tests | mitigate | function-scoped server per mutating test |
</threat_model>

<verification>
- part-03 module and shrunk legacy harness green in a real browser; ledger --allow-pending rc 0
</verification>

<success_criteria>
25 more browser checks migrated.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-23-SUMMARY.md` when done
</output>
