---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 09
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - companion/test_config_page_helpers.py
  - companion/test_config_page_01.py
  - companion/test_config_page.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 01 of companion/test_config_page.py (original check() calls #1-#33) runs as native pytest tests in companion/test_config_page_01.py on the shared fixtures"
    - "The self-referential _ASPECT_REPIN_LEDGER check, which opens test_config_page.py to grep its own text, is deleted with an R-coded reason, and the _ASPECT_REPIN_LEDGER table is removed from the legacy file"
    - "The config-page helpers live in companion/test_config_page_helpers.py, with no Harness, http_request or _NoRedirectHandler copy"
    - "The shrunk legacy harness has one EXPECTED_CHECK_COUNT equal to the pending rows, and still passes through the shim"
  artifacts:
    - path: "companion/test_config_page_01.py"
      provides: "part 01 config-page tests"
      contains: "def test_"
    - path: "companion/test_config_page_helpers.py"
      provides: "config-page seeding/render helpers"
      contains: "__test__ = False"
  key_links:
    - from: "companion/test_config_page_01.py"
      to: "companion/conftest.py"
      via: "shared fixtures"
      pattern: "app_server"
    - from: "companion/test_config_page.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Start the config-page chain (5 plans, 33-09..33-13): create the helpers module, delete the
self-referential aspect-repin bookkeeping, and migrate part 01 of
`companion/test_config_page.py` into `companion/test_config_page_01.py`.

Output: the helpers module, the part-01 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_config_page.txt

<slice>
- Harness: `companion/test_config_page.py`. 276 baseline rows; helpers at original lines
  1-1630; `main()` at 1631.
- Legacy plumbing to drop in favour of the shared fixtures: `_NoRedirectHandler` (1474),
  `http_request` (1488) and `Harness` (1511, a plain env with no fake providers).
- Part 01 = original check() calls #1-#33, original lines 1631-2935:
  - FIRST anchor: "_ASPECT_REPIN_LEDGER is non-empty, every row is well-formed (all five keys present, "
  - LAST anchor: "the control's own semantics and the photographs survive the map's removal — the row"
- Rubric R: the first check (around original lines 1640-1700) and the `_ASPECT_REPIN_LEDGER`
  tuple (1230-~1330), with its history comments (1132-1230), are plan-history bookkeeping that
  opens the test file itself. Delete the check with reason
  `R: self-referential CFG-85 aspect-repin bookkeeping; opens test_config_page.py to grep def names; no behaviour`.
  Also delete the tuple and its comments from the legacy file. Before deleting the tuple, grep
  that no remaining legacy check references it; line ~7505 is only a comment.
- Chain-first duties: collapse the `EXPECTED_CHECK_COUNT` history (original lines ~150-560)
  into one line, and create `companion/test_config_page_helpers.py`.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers module, the R deletion, and the first half of part 01</name>
  <files>companion/test_config_page_helpers.py, companion/test_config_page_01.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1-3
    - companion/test_config_page.py lines 1-160 and 560-1630 (imports and module-level helpers; skip the EXPECTED_CHECK_COUNT history block)
    - companion/test_config_page.py from the FIRST anchor for ~650 lines
    - companion/conftest.py, test-support/companion_app_server.py, test-support/companion_markup.py (grep "^def \|^class ")
  </read_first>
  <action>
(1) Create `companion/test_config_page_helpers.py` with `__test__ = False`, holding the reusable
helpers: form-body builders, state seeding and `handle_post` wrappers.
- Exclude the legacy plumbing (`Harness`, `http_request`, `_NoRedirectHandler`) and every
  tempfile use.
- `tokenize` / `ast` helpers over production source are not carried over.

(2) Create `companion/test_config_page_01.py` and port the first half (by count) of part 01,
starting after the R-deleted first check. `handle_post`-level checks stay in-process on a
`tmp_path` state dir; HTTP round trips use `app_server`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_01.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_01.py` exits 0
    - `grep -q "^__test__ = False" companion/test_config_page_helpers.py` exits 0
    - `grep -cE "class Harness|def http_request|_NoRedirectHandler|tempfile|_ASPECT_REPIN_LEDGER" companion/test_config_page_helpers.py companion/test_config_page_01.py` prints 0 for both files
  </acceptance_criteria>
  <done>The helpers module exists and the first half of part 01 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 01</name>
  <files>companion/test_config_page_01.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - companion/test_config_page.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-01 calls, through the LAST anchor, with the rubric classification. The
quiet-dial and runway-control markup checks use `parse_html(...).select(...)`. The "every class
the emitted markup carries has a real rule" kind of check intersects the classes from the parsed
DOM with the selectors from `css_rules(served_stylesheet(server))`; it never greps style.css.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_01.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_01.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k "config_page_01 or config_page_helpers"` exits 0
  </acceptance_criteria>
  <done>All 33 part-01 calls are ported or deleted with reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_config_page.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md</files>
  <read_first>
    - companion/test_config_page.py lines 140-570 (EXPECTED_CHECK_COUNT history), 1132-1335 (_ASPECT_REPIN_LEDGER block) and the `def main():` region
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) In the legacy file:
- delete the migrated calls and their closures;
- delete the `_ASPECT_REPIN_LEDGER` tuple and its history comments;
- collapse `EXPECTED_CHECK_COUNT` to one line above `def main():` with the remaining count.

(2) Flip this slice's rows, add `### Part 01 (plan 33-09)`, run section 5 of the rules, then
commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py && server/.venv/bin/ruff check companion/test_config_page*.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^EXPECTED_CHECK_COUNT" companion/test_config_page.py` prints 1, and `grep -c "_ASPECT_REPIN_LEDGER = (" companion/test_config_page.py` prints 0
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_config_page.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k config_page` exits 0
    - The fragment row for the `_ASPECT_REPIN_LEDGER is non-empty...` label is `deleted` with a reason starting `R: `
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 01 is migrated, the self-referential ledger is gone, and the counts are consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-09-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-09-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify; the R deletion carries its reason |
</threat_model>

<verification>
- new module, shim (-k config_page), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
33 config-page checks accounted for, and the first TST-12 evidence site from the audit (the
self-read ledger) is gone.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-09-SUMMARY.md` when done
</output>
