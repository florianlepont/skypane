---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 18
type: execute
wave: 6
depends_on: [33-17, 33-04, 33-19]
files_modified:
  - companion/test_companion_app_05.py
  - companion/test_companion_app_helpers.py
  - companion/test_companion_app.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "Part 05 of companion/test_companion_app.py (original check() calls #249-#302, minus the two WR-11 checks already moved) runs as native pytest tests in companion/test_companion_app_05.py"
    - "The --geofence missing-file check passes a tmp_path absent path, not /nonexistent/no-such-geofence.json"
    - "companion/test_companion_app.py no longer exists, and nothing in the repo imports companion.test_companion_app"
    - "The companion_app ledger fragment has zero pending rows: all 320 baseline rows plus any addendum are accounted for"
  artifacts:
    - path: "companion/test_companion_app_05.py"
      provides: "part 05 companion-app tests"
      contains: "def test_"
  key_links:
    - from: ".planning/.../33-ledger/companion__test_companion_app.md"
      to: "33-ledger-check.py (no --allow-pending)"
      via: "0 pending, legacy file gone"
      pattern: "ported"
    - from: "companion/test_companion_app_05.py"
      to: "companion/conftest.py make_app_server"
      via: "extra_args geofence under tmp_path"
      pattern: "make_app_server"
---

<objective>
Finish the companion-app chain: migrate part 05 into `companion/test_companion_app_05.py` and
delete the legacy `companion/test_companion_app.py`. That file is the last source of the copied
`Harness` / `_InProcessHarness` / `http_request` / `_NoRedirectHandler` (TST-10, TST-14).

Depends on:
- 33-04: `test_i18n.py` no longer imports `_tca`;
- 33-19: the still-legacy browser harnesses and `test_browser_ux_helpers.py` import from
  `companion_app_server` instead.

Output: the part-05 module, the legacy file removed, and the fragment complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-17-SUMMARY.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-19-SUMMARY.md
@companion/test_companion_app_helpers.py

<slice>
- Harness: `companion/test_companion_app.py`.
- Part 05 = original check() calls #249-#302, the last of the file, minus the 2 WR-11 checks that
  33-14 already moved; original lines 9826-12243:
  - FIRST anchor: "the overridden air-france render and the vueling-airlines render (the same source "
  - LAST anchor: "the site-wide editorial floor (CFG-79): every non-exempt .section-caption element "
- Hotspots:
  - original ~10865: `Harness(extra_args=["--geofence", "/nonexistent/no-such-geofence.json"])` →
    `make_app_server(extra_args=["--geofence", str(tmp_path / "absent" / "no-such-geofence.json")], fake_providers=True)`.
    Assert the same startup behaviour the legacy check asserted;
  - the editorial-floor `.section-caption` check (C/D): render the pages, collect every
    `.section-caption` element from the parsed DOM, and check the served stylesheet's
    declarations for `.section-caption`. A claim about computed colour belongs to the browser
    modules; name the covering browser test if the rewrite relies on one.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 05</name>
  <files>companion/test_companion_app_05.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_companion_app.py from the FIRST anchor for ~1200 lines
  </read_first>
  <action>
Port the first half (by count) of part 05 into `companion/test_companion_app_05.py`, including
the geofence hotspot handling from `<slice>`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_05.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_05.py` exits 0
    - `grep -cE "/nonexistent|open\(|tempfile" companion/test_companion_app_05.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 05 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 05</name>
  <files>companion/test_companion_app_05.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - companion/test_companion_app.py from where Task 1 stopped through the LAST anchor (end of main())
  </read_first>
  <action>
Port the remaining calls, through the LAST anchor, following the rubric.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_05.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_05.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app` exits 0
  </acceptance_criteria>
  <done>All remaining part-05 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the legacy harness, close the fragment, verify, commit and push</name>
  <files>companion/test_companion_app.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) `grep -rn "test_companion_app\b\|companion.test_companion_app\|_tca\b" companion test-support conftest.py`
must show no importer. If one remains, stop and report it, because a 33-04 or 33-19 prerequisite
did not land.

(2) `git rm companion/test_companion_app.py`.

(3) Flip the remaining rows, add `### Part 05 (plan 33-18)`, and run the ledger check WITHOUT
`--allow-pending`.

(4) Run every companion-app module together, plus the guard, the shim consistency test, and the
still-legacy browser harnesses through the shim. They now import `LegacyHarness` from
`companion_app_server`; run them with the pre-flight `PLAYWRIGHT_BROWSERS_PATH` and
`SKYPANE_REQUIRE_BROWSER=1`.

(5) Commit and push.
  </action>
  <verify>
    <automated>test ! -e companion/test_companion_app.py && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_companion_app.py && server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_0*.py companion/test_suite_guards.py companion/test_legacy_harness_shim.py -k "(companion_app or detector or exemption or consistent) and not exits_zero" -q && server/.venv/bin/ruff check companion/test_companion_app*.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e companion/test_companion_app.py` succeeds
    - `grep -rln "companion.test_companion_app\|from companion import test_companion_app" companion test-support` prints nothing
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_companion_app.py` exits 0 (0 pending)
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "exits_zero and browser_ux"` exits 0. The still-legacy browser_ux harness imports LegacyHarness and stays green; other legacy harnesses belong to sibling plans in flight
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>The companion-app harness is fully migrated, and the last copied lifecycle plumbing in companion/ is gone.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-18-01 | Tampering | `--geofence` host path as root | mitigate | tmp_path absent path; guard G6 |
| T-33-18-02 | Denial of Service | breaking still-legacy importers | mitigate | importer grep before `git rm`; the shim run over every remaining legacy harness in verify |
| T-33-18-03 | Repudiation | a check lost when the legacy file is deleted | mitigate | ledger-check without --allow-pending passes before the commit |
</threat_model>

<verification>
- all companion-app modules green; ledger rc 0 with no pending; no importer of the legacy module; remaining legacy harnesses green
</verification>

<success_criteria>
All 320 companion-app baseline rows are accounted for, and the reference `Harness` copy is gone.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-18-SUMMARY.md` when done
</output>
