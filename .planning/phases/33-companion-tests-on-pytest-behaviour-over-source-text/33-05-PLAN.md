---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 05
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - companion/test_view_pages_helpers.py
  - companion/test_view_pages_01.py
  - companion/test_view_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 01 of companion/test_view_pages.py (original check() calls #1-#37) runs as native pytest tests in companion/test_view_pages_01.py on the shared fixtures"
    - "The view-pages helpers needed by later parts live in companion/test_view_pages_helpers.py, without a Harness, http_request or _NoRedirectHandler copy"
    - "The shrunk legacy test_view_pages.py has one EXPECTED_CHECK_COUNT line equal to the fragment's pending rows, and still passes through the shim"
    - "Every source-text / CSS-text check in the slice is rewritten as behaviour, parsed DOM or served CSS, or deleted with a coded reason"
  artifacts:
    - path: "companion/test_view_pages_01.py"
      provides: "part 01 view-page tests"
      contains: "def test_"
    - path: "companion/test_view_pages_helpers.py"
      provides: "seeding/render helpers for view-page tests"
      contains: "__test__ = False"
  key_links:
    - from: "companion/test_view_pages_01.py"
      to: "companion/conftest.py"
      via: "app_server / make_app_server / module_app_server_factory fixtures"
      pattern: "app_server"
    - from: "companion/test_view_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "single EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Start the view-pages chain (4 plans): create the chain's helpers module and migrate part 01 of
`companion/test_view_pages.py` into `companion/test_view_pages_01.py`.

Purpose: TST-10, TST-12, TST-13 and TST-15 for the first slice, plus the chain setup that the
later view-pages plans (33-06..33-08) reuse.

Output: the helpers module, the part-01 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_view_pages.txt

<slice>
- Harness: `companion/test_view_pages.py`. 169 baseline rows; module-level helpers at original
  lines 1-1041; `main()` at 1042.
- Part 01 = original check() calls #1-#37, original lines 1042-2358:
  - FIRST anchor: "an empty database renders the flight-history empty-state copy and no <table"
  - LAST anchor: "each summary row gets exactly one sibling detail row, matched by aria-controls/id,"
- Chain-first duties (33-MIGRATION-RULES.md section 1):
  - collapse every historical `EXPECTED_CHECK_COUNT = ...` reassignment (original lines
    ~93-353) and its history comments into ONE line above `def main():`;
  - create `companion/test_view_pages_helpers.py`.
- The legacy `Harness` in this file uses a plain env with NO fake providers. Keep
  `fake_providers=False`, the fixture default.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers module and the first half of part 01</name>
  <files>companion/test_view_pages_helpers.py, companion/test_view_pages_01.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md (sections 1-3)
    - companion/test_view_pages.py lines 1-1041 (module-level helpers: which are seeding/render helpers vs the Harness/http_request/_NoRedirectHandler plumbing to drop)
    - companion/test_view_pages.py from the FIRST anchor (grep -n) for ~660 lines (first half of the slice)
    - companion/conftest.py, test-support/companion_app_server.py, test-support/companion_markup.py (grep "^def \|^class " to get the exact API)
  </read_first>
  <action>
(1) Create `companion/test_view_pages_helpers.py` with `__test__ = False`, holding the legacy
module-level helpers that the migrated tests need: database/state seeding, row builders and
render wrappers.
- Do NOT copy `Harness`, `http_request`, `_NoRedirectHandler`, `_cookie_value`, `_login` or
  `tempfile` usage. Tests use `companion_app_server` and the conftest fixtures instead.
- Any helper that read CSS, JS or HTML source from disk is rewritten over served bytes and the
  `companion_markup` parsers, or dropped when nothing needs it.

(2) Create `companion/test_view_pages_01.py`. Port the first half (by count) of the part-01
check() calls, from the FIRST anchor onward:
- one `test_` function per old check, with the old label as docstring;
- parametrise the loops;
- classify each check with the rubric codes B/D/C/J/S/P/R/T and keep a running list of the
  codes for Task 3;
- read-only render checks that call page modules in-process need no server;
- HTTP checks use `app_server`, or a module-scoped seeded server built with
  `module_app_server_factory`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_01.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_01.py` exits 0
    - `grep -q "^__test__ = False" companion/test_view_pages_helpers.py` exits 0
    - `grep -cE "class Harness|def http_request|_NoRedirectHandler|tempfile" companion/test_view_pages_helpers.py companion/test_view_pages_01.py` prints 0 for both files
  </acceptance_criteria>
  <done>The helpers module exists and the first half of part 01 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 01</name>
  <files>companion/test_view_pages_01.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - companion/test_view_pages.py from where Task 1 stopped through the LAST anchor (grep -n "each summary row gets exactly one sibling detail row")
    - companion/test_view_pages_01.py (Task 1 output, for conventions)
  </read_first>
  <action>
Port the remaining part-01 check() calls, up to and including the LAST anchor, into
`companion/test_view_pages_01.py`, with the same conventions and rubric classification as
Task 1. Structural HTML assertions use `parse_html(...).select(...)`, for example the summary
and detail row pairing via `aria-controls` / `id`. Do not use regex over the HTML.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_01.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_01.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k "view_pages_01 or view_pages_helpers"` exits 0
  </acceptance_criteria>
  <done>All 37 part-01 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_view_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md</files>
  <read_first>
    - companion/test_view_pages.py lines 85-360 (the EXPECTED_CHECK_COUNT history block) and the `def main():` region
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4, 5, 6
  </read_first>
  <action>
(1) In `companion/test_view_pages.py`:
- delete the migrated check() calls and the closures only they used;
- keep any setup that later checks still need;
- replace every historical `EXPECTED_CHECK_COUNT = ...` line, with its history comments, by one
  `EXPECTED_CHECK_COUNT = <remaining rows>` line directly above `def main():`.

(2) Flip this slice's fragment rows to `ported` or `deleted` with a coded reason, and add the
`### Part 01 (plan 33-05)` note.

(3) Run every command in 33-MIGRATION-RULES.md section 5, using `--allow-pending`.

(4) Commit, then `git push origin claude/phase-33`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k view_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_view_pages.py && server/.venv/bin/ruff check companion/test_view_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^EXPECTED_CHECK_COUNT" companion/test_view_pages.py` prints 1
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_view_pages.py` exits 0; this proves remaining == pending and that every ported node id exists
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k view_pages` exits 0
    - The fragment has no `deleted` row without a rubric code prefix: `grep "| deleted |" <fragment> | grep -vcE "\| (B|D|C|J|S|P|R|T): "` prints 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 01 is migrated. The legacy harness shrinks consistently, and the ledger proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → companion/app.py child | started only through the shared AppServer (child_env) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Information Disclosure | app.py children reaching the network | mitigate | only the shared fixtures start servers (guard G5 forbids local copies) |
| T-33-05-02 | Tampering | writes outside tmp_path | mitigate | fixtures own every state dir; guard G6 |
| T-33-05-03 | Repudiation | dropped checks | mitigate | staged ledger-check (pending == remaining legacy count) in verify |
</threat_model>

<verification>
- new module green; shim green for view_pages; guard green; ledger --allow-pending rc 0; ruff clean
</verification>

<success_criteria>
37 check() calls are migrated or deleted with reasons. The rest still run legacy, and the
counts are proven equal.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-05-SUMMARY.md` when done
</output>
