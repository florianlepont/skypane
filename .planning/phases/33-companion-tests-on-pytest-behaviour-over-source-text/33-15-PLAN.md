---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 15
type: execute
wave: 3
depends_on: [33-14]
files_modified:
  - companion/test_companion_app_02.py
  - companion/test_companion_app_helpers.py
  - companion/test_companion_app.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 02 of companion/test_companion_app.py (original check() calls #51-#113) runs as native pytest tests in companion/test_companion_app_02.py"
    - "The _resolve_flash_text calls no longer use /nonexistent literals; the absent state dir is a tmp_path subpath, and whether that function can create directories is verified and recorded (RESEARCH assumption A3)"
    - "UI-SPEC flash-deck copy is asserted as literal strings against _resolve_flash_text / the rendered flash, never read from a spec file"
    - "The JS contract checks (dirty-state.js / submit-guard.js ES5-safe, sink-free) assert on the SERVED asset after comment/string stripping"
    - "The shrunk legacy harness's EXPECTED_CHECK_COUNT equals the pending rows, and it still passes through the shim"
  artifacts:
    - path: "companion/test_companion_app_02.py"
      provides: "part 02 companion-app tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_companion_app_02.py"
      to: "companion_app_server.served_asset + strip_js_comments_and_strings"
      via: "served JS contract"
      pattern: "served_asset"
    - from: "companion/test_companion_app.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Migrate part 02 of `companion/test_companion_app.py` into `companion/test_companion_app_02.py`.
This is plan 2 of 5 in the companion-app chain.

Output: the part-02 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-14-SUMMARY.md
@companion/test_companion_app_helpers.py

<slice>
- Harness: `companion/test_companion_app.py`. Keep its exported plumbing (see 33-14).
- Part 02 = original check() calls #51-#113 (63 calls), original lines 2677-5075:
  - FIRST anchor: "the Health notification dot appears inside the Health sidebar link and on the tab "
  - LAST anchor: "dirty-state.js stays ES5-safe (no let/const/arrow/backtick/innerHTML/outerHTML/"
- Hotspots:
  - original ~3846-3911: the six flash deck strings "from 13-UI-SPEC.md's Full Copy Deck".
    The strings are already literals in the test, so port them as literals asserted against
    `_resolve_flash_text` (B). Do not open the spec;
  - original 3873/3878: `_resolve_flash_text(key, "/nonexistent-state-dir-fixture")` and
    `_resolve_flash_text("not-a-real-flash-key", "/nonexistent")`. Read
    `companion/app.py::_resolve_flash_text`, and everything it calls, to settle RESEARCH A3: does
    it ever `os.makedirs` or open for writing? Either way, use `tmp_path / "absent" / "state"`
    and assert that the path still does not exist afterwards;
  - JS ES5 / sink checks (J) and `/static/submit-guard.js`-served checks (B);
  - `22-UI-SPEC.md` mentions (lines ~3058-3171) are comments only; drop them.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 02 (including the flash-deck and absent-state-dir checks)</name>
  <files>companion/test_companion_app_02.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_companion_app.py from the FIRST anchor for ~1200 lines
    - companion/app.py `_resolve_flash_text` and its callees (grep -n "def _resolve_flash_text" companion/app.py)
  </read_first>
  <action>
Port the first half (by count) of part 02 into `companion/test_companion_app_02.py`. Apply the
hotspot handling from `<slice>`. Record the A3 finding in the SUMMARY: "_resolve_flash_text
creates directories: yes/no (evidence: <function names>)". If it does create directories, that
is a production behaviour worth noting, but do not change production code for it.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_02.py` exits 0
    - `grep -cE "/nonexistent|UI-SPEC|tempfile|open\(" companion/test_companion_app_02.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 02 runs under pytest, and no host path literal remains.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 02 (the JS contract checks)</name>
  <files>companion/test_companion_app_02.py, companion/test_companion_app_helpers.py</files>
  <read_first>
    - companion/test_companion_app.py from where Task 1 stopped through the LAST anchor
    - test-support/companion_markup.py strip_js_comments_and_strings
  </read_first>
  <action>
Port the remaining part-02 calls, through the LAST anchor.

ES5-safety and sink checks fetch the asset with `served_asset(server, "/static/dirty-state.js")`,
strip comments and strings, and assert that the forbidden tokens are absent as whole tokens:
`let`, `const`, `=>`, backtick, `innerHTML`, `outerHTML`, `insertAdjacentHTML`,
`document.write`. Parametrise over the served JS assets the legacy check covered.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_02.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_companion_app_02.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k companion_app` exits 0
  </acceptance_criteria>
  <done>All 63 part-02 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify, commit and push</name>
  <files>companion/test_companion_app.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md</files>
  <read_first>
    - companion/test_companion_app.py (the EXPECTED_CHECK_COUNT line and the remaining main())
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_companion_app.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
Remove the migrated calls, keeping the exported plumbing. Set `EXPECTED_CHECK_COUNT` to the
remaining rows, flip the slice rows and add `### Part 02 (plan 33-15)`. Run section 5 of the
rules, then commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py && server/.venv/bin/ruff check companion/test_companion_app*.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_companion_app.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k companion_app` exits 0
    - `grep -c "/nonexistent-state-dir-fixture" companion/test_companion_app.py` prints 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 02 is migrated and the ledger is consistent.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-15-01 | Tampering | `_resolve_flash_text` given a host path as root | mitigate | tmp_path absent path, and the test asserts it stays absent; the A3 finding is recorded |
| T-33-15-02 | Information Disclosure | app.py children reaching providers | mitigate | fake_providers=True + child_env via shared fixtures |
| T-33-15-03 | Repudiation | dropped checks | mitigate | staged ledger-check in verify |
</threat_model>

<verification>
- new module, shim (-k companion_app), guard, ledger --allow-pending, ruff: all green
</verification>

<success_criteria>
63 more companion-app checks migrated; RESEARCH assumption A3 is resolved with evidence.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-15-SUMMARY.md` when done
</output>
