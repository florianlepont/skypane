---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 13
type: execute
wave: 6
depends_on: [33-12]
files_modified:
  - companion/test_config_page_05.py
  - companion/test_config_page_helpers.py
  - companion/test_config_page.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "Part 05 of companion/test_config_page.py (original check() calls #229-#276) runs as native pytest tests in companion/test_config_page_05.py"
    - "companion/test_config_page.py no longer exists; the shim no longer runs it; its ledger fragment has zero pending rows"
    - "Retired-wording checks assert on rendered output (the retired text is absent from the rendered page in both languages), never on source text"
  artifacts:
    - path: "companion/test_config_page_05.py"
      provides: "part 05 config-page tests"
      contains: "def test_"
  key_links:
    - from: ".planning/.../33-ledger/companion__test_config_page.md"
      to: "33-ledger-check.py (no --allow-pending)"
      via: "0 pending, legacy file gone"
      pattern: "ported"
    - from: "companion/test_config_page_05.py"
      to: "companion/conftest.py"
      via: "shared fixtures"
      pattern: "app_server"
---

<objective>
Finish the config-page chain: migrate part 05 into `companion/test_config_page_05.py` and
delete the legacy `companion/test_config_page.py` (TST-14 for this harness).

Output: the part-05 module, the legacy file removed, and the fragment complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-12-SUMMARY.md
@companion/test_config_page_helpers.py

<slice>
- Harness: `companion/test_config_page.py`.
- Part 05 = original check() calls #229-#276 (48 calls, the last of the file), original lines
  11076-13604:
  - FIRST anchor: "none of the three retired delay wordings ('Takes effect within about 5 minutes', "
  - LAST anchor: "the native submit carrying STATIC_SAVE_FALLBACK_ATTR is emitted UNCONDITIONALLY — "
- Retired-wording checks: if the legacy check grepped source for a retired string, rewrite it as
  "the retired string is absent from the rendered page's text", in EN and FR (D); a source-only
  claim is deleted with code S.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 05</name>
  <files>companion/test_config_page_05.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_config_page.py from the FIRST anchor for ~1270 lines
  </read_first>
  <action>
Port the first half (by count) of part 05 into `companion/test_config_page_05.py`, using the
rules' conventions and rubric codes.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_05.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_05.py` exits 0
    - `grep -cE "class Harness|def http_request|tempfile|open\(" companion/test_config_page_05.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 05 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 05</name>
  <files>companion/test_config_page_05.py, companion/test_config_page_helpers.py</files>
  <read_first>
    - companion/test_config_page.py from where Task 1 stopped through the LAST anchor (end of main())
  </read_first>
  <action>
Port the remaining calls, through the LAST anchor. The static-save-fallback submit checks assert
that the parsed form has a `button[type=submit]` carrying the attribute, in every render state,
parametrised by state.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_05.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_05.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k config_page` exits 0
  </acceptance_criteria>
  <done>All 48 part-05 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the legacy harness, close the fragment, verify, commit and push</name>
  <files>companion/test_config_page.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) Confirm no `check(` call is left beyond this slice, and that nothing imports the legacy
module (`grep -rn "companion.test_config_page import\|test_config_page as" companion test-support`).

(2) `git rm companion/test_config_page.py`.

(3) Flip the remaining rows, add `### Part 05 (plan 33-13)`, and run the ledger check WITHOUT
`--allow-pending`.

(4) Run all config-page modules together, plus the guard and the shim consistency test, then
commit and push.
  </action>
  <verify>
    <automated>test ! -e companion/test_config_page.py && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_config_page.py && server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_config_page_0*.py companion/test_suite_guards.py companion/test_legacy_harness_shim.py -k "(config_page or detector or exemption or consistent) and not exits_zero" -q && server/.venv/bin/ruff check companion/test_config_page*.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e companion/test_config_page.py` succeeds
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_config_page.py` exits 0 (276 rows plus any addendum, 0 pending)
    - `grep -c "| pending |" .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_config_page.md` prints 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>The config-page harness is fully migrated, and its legacy file and check count are retired.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-13-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-13-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-13-03 | Repudiation | a check lost when the legacy file is deleted | mitigate | ledger-check without --allow-pending passes before the commit |
</threat_model>

<verification>
- all config-page modules green; ledger rc 0 with no pending; legacy file gone; ruff clean
</verification>

<success_criteria>
All 276 config-page checks accounted for; the file leaves the legacy set.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-13-SUMMARY.md` when done
</output>
