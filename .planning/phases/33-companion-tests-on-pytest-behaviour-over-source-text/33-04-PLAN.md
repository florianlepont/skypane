---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 04
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - companion/test_contrast_check.py
  - companion/test_i18n.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_contrast_check.md
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_i18n.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "companion/test_contrast_check.py is a native pytest module; its colour tokens come from the stylesheet the app SERVES, parsed structurally, never from style.css opened on disk"
    - "companion/test_i18n.py is a native pytest module; the French-catalogue checks assert on imported catalogue objects and rendered output, never on an AST, regex or tokenize pass over i18n_fr/*.py or static/*.js"
    - "test_i18n.py no longer imports anything from companion.test_companion_app; its in-process server checks use app_server_in_process"
    - "Neither file contains EXPECTED_CHECK_COUNT, check() or main(); both leave the legacy set and are collected by pytest"
    - "Both ledger fragments account for every baseline row (49 + 24) with zero pending"
  artifacts:
    - path: "companion/test_contrast_check.py"
      provides: "WCAG contrast / hue-separation tests over served CSS tokens"
      contains: "def test_"
    - path: "companion/test_i18n.py"
      provides: "i18n catalogue and rendering tests"
      contains: "app_server_in_process"
  key_links:
    - from: "companion/test_contrast_check.py"
      to: "companion_markup.custom_properties / companion_app_server.served_stylesheet"
      via: "served CSS token extraction"
      pattern: "custom_properties|served_stylesheet"
    - from: "companion/test_i18n.py"
      to: "companion/conftest.py app_server_in_process"
      via: "fixture"
      pattern: "app_server_in_process"
---

<objective>
Rewrite the two smallest harnesses, test_contrast_check.py (49 checks) and test_i18n.py
(24 checks), in place as native pytest modules. Apply the TST-12 rubric to every check in
them.

Purpose:
- this is the first proof that the whole workflow (fixture, markup toolkit, guard, ledger)
  holds end to end;
- test_i18n.py stops importing `_InProcessHarness` / `http_request` from test_companion_app.py,
  which unblocks deleting that file in 33-18.

Output: both files native pytest, and both fragments complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_contrast_check.txt
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_i18n.txt
@.claude/skills/sketch-findings-skypane/SKILL.md

<slice>
Single-plan harnesses: each file is rewritten IN PLACE (33-MIGRATION-RULES.md section 1).

- companion/test_contrast_check.py:
  - 524 lines; 13 check() calls emit 49 rows, so several are loops → parametrise;
  - 7 `style.css` reads. The tokens under test (accent, status colours, surfaces, in both
    themes) must come from `custom_properties(served_stylesheet(server), ":root")` and from
    the dark-theme selector block(s), fetched from a running app (use `app_server_in_process`,
    no subprocess needed). The numeric WCAG math stays on `companion/contrast_check.py`'s
    imported functions.
- companion/test_i18n.py:
  - 1289 lines; 19 check() calls emit 24 rows;
  - 4 `open(` and 3 getsource/ast/tokenize uses (S / J candidates);
  - line ~1064 imports `companion.test_companion_app as _tca` for `_InProcessHarness` /
    `http_request`: replace it with the `app_server_in_process` fixture and
    `companion_app_server.http_request`;
  - its summary line has no name prefix (irrelevant once migrated).
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite test_contrast_check.py as pytest over served CSS tokens</name>
  <files>companion/test_contrast_check.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_contrast_check.md</files>
  <read_first>
    - companion/test_contrast_check.py (whole file, 524 lines)
    - companion/contrast_check.py (public functions and constants: hue_separation, perceptual_distance, MIN_SIGNAL_* and the ratio function names)
    - test-support/companion_markup.py (custom_properties, css_rules, declarations_for signatures)
    - test-support/companion_app_server.py (served_stylesheet) and companion/conftest.py (app_server_in_process)
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_contrast_check.txt (the 49 labels)
  </read_first>
  <action>
Follow 33-MIGRATION-RULES.md sections 0 and 2-4. Replace `main()` / `check()` /
`EXPECTED_CHECK_COUNT` with one `test_` function per check; loop-emitted checks become
`@pytest.mark.parametrize`, with ids naming the token pair and theme.

For every check that pulled a hex value out of `companion/static/style.css` text (rubric C):
- fetch the stylesheet with `served_stylesheet(app_server_in_process)`;
- read the light tokens with `custom_properties(css, ":root")` and the dark tokens from the
  dark-theme block. Grep style.css for `prefers-color-scheme: dark` and `data-theme` to learn
  the exact selector and at-rule, then pass them as `selector=` / `at_rules=`;
- assert the same numeric property through `companion.contrast_check`'s own functions.

A check that asserted a stylesheet comment is deleted with reason code C.

Keep the file's module docstring short and in behaviour terms, with no plan IDs.

Fill the fragment: every row is ported with a node id, or deleted with a coded reason. Add a
`### Part 01 (plan 33-04)` note with the counts per rubric code.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_contrast_check.py -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_contrast_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_contrast_check.py` exits 0
    - `grep -cE "EXPECTED_CHECK_COUNT|def check\(|def main\(" companion/test_contrast_check.py` prints 0
    - `grep -c 'style.css"' companion/test_contrast_check.py` prints 0 outside route strings (`grep 'style.css' companion/test_contrast_check.py | grep -v '/static/style.css' | grep -vc '^\s*#'` prints 0)
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_contrast_check.py` exits 0 (no --allow-pending)
  </acceptance_criteria>
  <done>The contrast checks run under pytest against served tokens; all 49 rows are accounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_i18n.py as pytest, catalogue behaviour over source scans</name>
  <files>companion/test_i18n.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_i18n.md</files>
  <read_first>
    - companion/test_i18n.py (whole file, 1289 lines)
    - companion/i18n.py (t, t_lang and the catalogue-loading API) and `ls companion/i18n_fr/` (the per-area CATALOG modules)
    - test-support/companion_markup.py (strip_js_comments_and_strings, parse_html) and test-support/companion_app_server.py (served_asset, http_request)
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_i18n.txt (the 24 labels)
  </read_first>
  <action>
Classify each of the 24 checks by the rubric in 33-MIGRATION-RULES.md section 3 and port it:

- AST, regex or tokenize scans of `companion/i18n_fr/*.py` (S) become assertions on the
  imported objects. Examples:
  - every English key has a French CATALOG entry: compare the imported dict key sets;
  - no value is empty;
  - placeholders match: compare the `%s` / `{name}` sets between the English and French values;
  - `t_lang(key, "fr")` returns the French value.
- Scans of `companion/static/*.js` for untranslated literals (J): fetch the asset through
  `served_asset(server, ...)`. When the property is a string-literal contract, assert on the
  served text after `strip_js_comments_and_strings` where that is meaningful. Otherwise prove
  the behaviour by rendering a page under `lang="fr"` and parsing it.
- A check that only asserted a comment or docstring is deleted.
- The in-process server checks take the `app_server_in_process` fixture. Delete the
  `import companion.test_companion_app as _tca` line and every `_tca.` reference.

Fill the fragment (zero pending) and add the part note with the rubric-code counts.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_i18n.py -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_i18n.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_i18n.py` exits 0
    - `grep -cE "test_companion_app|_tca\.|EXPECTED_CHECK_COUNT|def check\(|def main\(|getsource|ast\.parse|tokenize" companion/test_i18n.py` prints 0
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_i18n.py` exits 0
  </acceptance_criteria>
  <done>The i18n checks assert catalogue and rendering behaviour; all 24 rows are accounted for, and the file no longer depends on test_companion_app.</done>
</task>

<task type="auto">
  <name>Task 3: Guard, legacy-set and suite verification, then commit and push</name>
  <files>companion/test_contrast_check.py, companion/test_i18n.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 5-6
    - companion/test_suite_guards.py (what the guard enforces)
  </read_first>
  <action>
Run the checks in 33-MIGRATION-RULES.md section 5:
- the guard now scans both files, which are no longer legacy. Fix every violation it reports by
  rewriting; never by exempting;
- confirm `skypane_test_support.LEGACY_COMPANION_HARNESSES` no longer lists either file;
- confirm the shim's consistency test passes;
- run ruff;
- confirm `git status` is clean apart from this plan's files.

Commit (message e.g. `test(33-04): contrast_check and i18n on pytest, behaviour over source text`)
and `git push origin claude/phase-33`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py companion/test_legacy_harness_shim.py -k "(contrast_check or i18n or detector or exemption or consistent) and not exits_zero" -q && server/.venv/bin/ruff check companion/test_contrast_check.py companion/test_i18n.py && server/.venv/bin/python3 -c "import sys; sys.path.insert(0,'test-support'); import skypane_test_support as s; assert not any(h.endswith(('test_i18n.py','test_contrast_check.py')) for h in s.LEGACY_COMPANION_HARNESSES)"</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k "test_i18n or test_contrast_check"` exits 0 with both files scanned (2 passed)
    - The legacy set printed by `python3 -c` excludes both files
    - `server/.venv/bin/ruff check companion/test_contrast_check.py companion/test_i18n.py` exits 0
    - `git log -1 --format=%s` names 33-04, and `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Two harnesses are fully migrated, guarded and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → in-process app | the in-process server binds loopback only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Repudiation | dropped checks | mitigate | ledger-check with no pending, run in verify |
| T-33-04-02 | Tampering | writes outside tmp_path | mitigate | fixtures use tmp_path; guard G6 scans both files |
| T-33-04-03 | Information Disclosure | password env leaking between tests | mitigate | app_server_in_process restores `auth.PASSWORD_ENV_VAR` at teardown (33-02 tested) |
</threat_model>

<verification>
- both modules green under `pytest -n auto`; both ledger checks rc 0 without --allow-pending; guard green; ruff clean
</verification>

<success_criteria>
73 baseline checks accounted for, with zero source-text or CSS-text reads left in either file.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-04-SUMMARY.md` when done
</output>
