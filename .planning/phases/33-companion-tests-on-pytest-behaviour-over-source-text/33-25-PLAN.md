---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 25
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - companion/test_status_pages_helpers.py
  - companion/test_status_pages_01.py
  - companion/test_status_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-15]

must_haves:
  truths:
    - "Part 01 of companion/test_status_pages.py (original check() calls #1-#37) runs as native pytest tests in companion/test_status_pages_01.py on the shared fixtures"
    - "The anomaly_active() degrade-safely check is pulled forward and uses a tmp_path absent path. Running the suite no longer creates /nonexistent/definitely-not-here on the host, and the legacy harness no longer FAILs as root"
    - "'companion/wake.py's source never mentions the pages package or app.py' is proven by behaviour: importing companion.wake in a fresh child_env subprocess loads no companion.pages / companion.app module"
    - "The status-pages helpers live in companion/test_status_pages_helpers.py, with no Harness, http_request or _NoRedirectHandler copy; _css_without_comments is replaced by the served-stylesheet parser"
    - "The shrunk legacy harness's single EXPECTED_CHECK_COUNT equals the pending rows"
  artifacts:
    - path: "companion/test_status_pages_01.py"
      provides: "part 01 status-pages tests + anomaly_active root-safe test"
      contains: "def test_"
    - path: "companion/test_status_pages_helpers.py"
      provides: "history/poll-state seeding helpers"
      contains: "__test__ = False"
  key_links:
    - from: "companion/test_status_pages_01.py"
      to: "health_page.anomaly_active"
      via: "tmp_path absent path"
      pattern: "tmp_path"
    - from: "companion/test_status_pages.py"
      to: "33-ledger-check.py staged rule"
      via: "EXPECTED_CHECK_COUNT == pending rows"
      pattern: "^EXPECTED_CHECK_COUNT = "
---

<objective>
Start the status-pages chain (7 plans, 33-25..33-31), the largest harness at 16.3k lines and
317 checks:
- create the helpers module;
- migrate part 01 into `companion/test_status_pages_01.py`;
- pull forward the root-unsafe `anomaly_active("/nonexistent/...")` check, the audit's TST-13
  evidence site, so the host-filesystem side effect stops from this plan on.

Output: the helpers module, the part-01 module, the shrunk legacy file, and the fragment rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-BASELINE/companion__test_status_pages.txt

<slice>
- Harness: `companion/test_status_pages.py`. 317 rows; helpers at original lines 1-1290
  (`_NoRedirectHandler` 1174, `http_request` 1182, `Harness` 1203 with a plain env and no fake
  providers, `_css_without_comments` ~1036); `main()` at 1291.
- Part 01 = original check() calls #1-#37, original lines 1291-2610:
  - FIRST anchor: "render() shows two distinct, separately-labelled freshness signals"
  - LAST anchor: "battery_sparkline_svg() emits per-point interactive hit targets with data-mv/data-ts/<title>, in "
- Out-of-order pull (rubric T), original ~8190-8235, the check labelled
  "anomaly_active() runs on every page render and must never raise — missing/empty/file/corrupt-db inputs all degrade safely".
  - `anomaly_active("/nonexistent/definitely-not-here")` → `anomaly_active(str(tmp_path / "absent" / "nested"))`;
  - the empty-dir case uses `tmp_path / "empty"` instead of `tempfile.mkdtemp`;
  - the file and corrupt-db cases go under `tmp_path`;
  - the same assertions otherwise. This row is a FAIL in the root baseline; it maps to the new
    node id.
- S hotspot (original ~1416): "companion/wake.py's source never mentions the pages package or
  app.py". Rewrite it as a subprocess run with `env=child_env()` of
  `import sys, companion.wake; print(sorted(m for m in sys.modules if m.startswith(("companion.pages","companion.app"))))`,
  asserting an empty list. That is the observable property the source grep stood in for.
- 260902-chc mentions around original ~2272-2305 ("both the rendered page and the module itself"):
  the page half is D; the "module itself" half is S (use `not hasattr(module, "<retired name>")`,
  or delete with a reason).
- Chain-first duty: collapse the `EXPECTED_CHECK_COUNT` history (original lines ~150-500) into
  one line.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers module, the anomaly_active pull, and the first half of part 01</name>
  <files>companion/test_status_pages_helpers.py, companion/test_status_pages_01.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1-3
    - companion/test_status_pages.py lines 1-150 and 500-1290 (imports and helpers; skip the EXPECTED_CHECK_COUNT history)
    - companion/test_status_pages.py lines 8150-8240 (the anomaly_active check)
    - companion/test_status_pages.py from the FIRST anchor for ~650 lines
    - companion/pages/health_page.py anomaly_active (grep -n "def anomaly_active")
  </read_first>
  <action>
(1) Create `companion/test_status_pages_helpers.py` with `__test__ = False`, holding the seeding
helpers (history_db writers, `save_poll_state` wrappers, fixture builders).
- Do not copy `Harness`, `http_request`, `_NoRedirectHandler`, `_css_without_comments` or any
  tempfile use.
- Add `served_css_rules(server)`, a thin wrapper over `css_rules(served_stylesheet(server))`,
  for the later parts.

(2) Create `companion/test_status_pages_01.py`:
- the anomaly_active test, as described in `<slice>`;
- the first half (by count) of part 01, including the wake-import subprocess rewrite.

After running it, confirm that `ls -la /nonexistent/` shows no NEW entry. The pre-existing empty
dir from the baseline run may remain.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_01.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_01.py` exits 0 as root
    - `grep -cE "/nonexistent|tempfile|open\(|_css_without_comments|inspect\.getsource" companion/test_status_pages_01.py companion/test_status_pages_helpers.py` prints 0 for both files
    - `grep -q "^__test__ = False" companion/test_status_pages_helpers.py` exits 0
  </acceptance_criteria>
  <done>The helpers module exists; the root-unsafe check and the first half of part 01 run under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 01</name>
  <files>companion/test_status_pages_01.py, companion/test_status_pages_helpers.py</files>
  <read_first>
    - companion/test_status_pages.py from where Task 1 stopped through the LAST anchor
  </read_first>
  <action>
Port the remaining part-01 calls, through the LAST anchor.

Battery ring, chart and sparkline checks are D: parse the SVG with `parse_html` and assert on
element counts and attributes (`viewBox`, `data-mv`, `data-ts`, `<title>`). A drawn fraction is
recovered from the parsed attribute values, as the legacy check computed it.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_01.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k status_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_status_pages_01.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k "status_pages_01 or status_pages_helpers"` exits 0
  </acceptance_criteria>
  <done>All 37 part-01 calls plus the pulled check are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the legacy harness, fill the ledger, verify as root, commit and push</name>
  <files>companion/test_status_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md</files>
  <read_first>
    - companion/test_status_pages.py lines 150-500 (EXPECTED_CHECK_COUNT history) and the main() regions being cut
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_status_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) In the legacy file:
- delete the part-01 calls and the anomaly_active check, plus the closures only they use;
- collapse `EXPECTED_CHECK_COUNT` to one line.

(2) Flip the rows: the anomaly_active FAIL row becomes `ported`. Add `### Part 01 (plan 33-25)`,
noting the out-of-order T pull.

(3) Run section 5 of the rules. The shim run for status_pages must now pass AS ROOT, and
`/nonexistent` must gain nothing.

(4) Commit and push.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages -q && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py && server/.venv/bin/ruff check companion/test_status_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/nonexistent" companion/test_status_pages.py` prints 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k status_pages` exits 0 when run as root
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py --allow-pending companion/test_status_pages.py` exits 0
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>Part 01 is migrated; the status-pages half of 32-REVIEW IN-05 and the host mkdir are closed.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-25-01 | Tampering | `anomaly_active` creating `/nonexistent/...` as root | mitigate | tmp_path absent path; guard G6 forbids the literal; the legacy literal is removed in the same commit |
| T-33-25-02 | Information Disclosure | wake-import subprocess | mitigate | launched with `child_env()` (no-network guard) |
| T-33-25-03 | Repudiation | dropped checks | mitigate | staged ledger-check; the FAIL row maps to a node id |
</threat_model>

<verification>
- new module green as root; shim for status_pages green as root; no new /nonexistent entry; ledger --allow-pending rc 0
</verification>

<success_criteria>
37 + 1 status-pages checks migrated; the audit's TST-13 host-write evidence site is closed.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-25-SUMMARY.md` when done
</output>
