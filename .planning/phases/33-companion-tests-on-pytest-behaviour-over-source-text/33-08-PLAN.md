---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 08
type: execute
wave: 5
depends_on: [33-07]
files_modified:
  - companion/test_view_pages_04.py
  - companion/test_view_pages_helpers.py
  - companion/test_view_pages.py
  - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
autonomous: true
requirements: [TST-10, TST-12, TST-13, TST-14, TST-15]

must_haves:
  truths:
    - "Part 04 of companion/test_view_pages.py (original check() calls #135-#169) runs as native pytest tests in companion/test_view_pages_04.py"
    - "companion/test_view_pages.py no longer exists; the shim no longer runs it; its ledger fragment has zero pending rows"
    - "The style.css checks in this slice (@supports selector(:has(*)), @starting-style lightbox entrance) assert on the SERVED stylesheet through css_rules, or are deleted with a C-coded reason"
  artifacts:
    - path: "companion/test_view_pages_04.py"
      provides: "part 04 view-page tests"
      contains: "def test_"
  key_links:
    - from: "companion/test_view_pages_04.py"
      to: "companion_markup.css_rules / served_stylesheet"
      via: "structural CSS assertions"
      pattern: "css_rules|declarations_for|rules_with_selector"
    - from: ".planning/.../33-ledger/companion__test_view_pages.md"
      to: "33-ledger-check.py (no --allow-pending)"
      via: "0 pending, legacy file gone"
      pattern: "ported"
---

<objective>
Finish the view-pages chain: migrate part 04 into `companion/test_view_pages_04.py` and delete
the legacy `companion/test_view_pages.py`. This retires its `EXPECTED_CHECK_COUNT` / `check()`
/ `main()` (TST-14 for this harness).

Output: the part-04 module, the legacy file removed, and the fragment complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-07-SUMMARY.md
@companion/test_view_pages_helpers.py

<slice>
- Harness: `companion/test_view_pages.py`.
- Part 04 = original check() calls #135-#169 (35 calls, the last of the file), original lines
  6982-9332:
  - FIRST anchor: "a fully-seeded Home render under lang='fr' shows the French page title, section"
  - LAST anchor: "both <dialog>s arrive through ONE @starting-style entrance on .lightbox[open] —"
- Hotspots: CSS-text checks ("companion/static/style.css still carries exactly one @supports
  selector(:has(*))", the lightbox `@starting-style` / `[open]` entrance) are code C; French
  Home rendering is D.
</slice>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First half of part 04</name>
  <files>companion/test_view_pages_04.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 2-3
    - companion/test_view_pages.py from the FIRST anchor for ~1170 lines
    - test-support/companion_markup.py (css_rules Rule.at_rules semantics, rules_with_selector)
  </read_first>
  <action>
Port the first half (by count) of part 04 into `companion/test_view_pages_04.py`, following the
rules' conventions and rubric.

For CSS structure claims, fetch `served_stylesheet(server)` and query `css_rules`. For example,
"exactly one `@supports selector(:has(*))` block" becomes a count of the distinct `at_rules`
preludes equal to that text across `css_rules(css)`.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_04.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_04.py` exits 0
    - `grep -cE "open\(|os\.path\.join\(HERE" companion/test_view_pages_04.py` prints 0
  </acceptance_criteria>
  <done>The first half of part 04 runs under pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Second half of part 04</name>
  <files>companion/test_view_pages_04.py, companion/test_view_pages_helpers.py</files>
  <read_first>
    - companion/test_view_pages.py from where Task 1 stopped through the LAST anchor (end of main())
  </read_first>
  <action>
Port the remaining calls, through the LAST anchor.

`@starting-style` and `.lightbox[open]` entrance claims use `css_rules` with the matching
`at_rules` context, plus `keyframes()` when a named animation is involved.

A check that asserted a stylesheet comment is deleted with code C.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_04.py -q && server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_04.py` exits 0
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_suite_guards.py -k view_pages` exits 0
  </acceptance_criteria>
  <done>All 35 part-04 calls are ported.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the legacy harness, close the fragment, verify, commit and push</name>
  <files>companion/test_view_pages.py, .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md</files>
  <read_first>
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-MIGRATION-RULES.md sections 1, 4-6
  </read_first>
  <action>
(1) Confirm the legacy `main()` holds no `check(` call beyond this slice.

(2) Run `grep -rn "test_view_pages\b\|companion.test_view_pages import\|test_view_pages as" companion test-support`.
Only this plan's modules, and nothing importing the legacy file, may match.

(3) `git rm companion/test_view_pages.py`.

(4) Flip the remaining rows, add `### Part 04 (plan 33-08)`, and run
`33-ledger-check.py companion/test_view_pages.py` WITHOUT `--allow-pending`.

(5) Confirm `legacy_companion_harnesses()` no longer lists the file, and the shim's consistency
test passes.

(6) Run `pytest -n auto companion/test_view_pages_0*.py` for the whole chain, then commit and
push.
  </action>
  <verify>
    <automated>test ! -e companion/test_view_pages.py && server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_view_pages.py && server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_view_pages_01.py companion/test_view_pages_02.py companion/test_view_pages_03.py companion/test_view_pages_04.py companion/test_suite_guards.py companion/test_legacy_harness_shim.py -k "(view_pages or detector or exemption or consistent) and not exits_zero" -q && server/.venv/bin/ruff check companion/test_view_pages*.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e companion/test_view_pages.py` succeeds
    - `server/.venv/bin/python3 .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger-check.py companion/test_view_pages.py` exits 0 (0 pending; 169 rows plus any addendum)
    - `grep -c "| pending |" .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-ledger/companion__test_view_pages.md` prints 0
    - All four view-pages modules pass together under `-n auto`
    - `git status --porcelain -- <this plan's files_modified>` is empty after the commit (sibling plans in the same wave may have in-flight edits elsewhere)
  </acceptance_criteria>
  <done>The view-pages harness is fully migrated, and its legacy file and check count are retired.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08-01 | Information Disclosure | app.py children reaching the network | mitigate | shared fixtures only (child_env); guard G5 |
| T-33-08-02 | Tampering | writes outside tmp_path | mitigate | fixtures + guard G6 |
| T-33-08-03 | Repudiation | a check lost when the legacy file is deleted | mitigate | ledger-check without --allow-pending must pass before `git rm` is committed |
</threat_model>

<verification>
- all four view-pages modules green; ledger rc 0 with no pending; legacy file gone; guard green; ruff clean
</verification>

<success_criteria>
All 169 view-pages checks (plus any addendum) are accounted for, and the file leaves the legacy
set.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-08-SUMMARY.md` when done
</output>
