---
phase: 33-companion-tests-on-pytest-behaviour-over-source-text
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/requirements-dev.in
  - server/requirements-dev.txt
  - pyproject.toml
  - test-support/companion_app_server.py
  - companion/conftest.py
  - companion/test_app_server_fixture.py
  - companion/test_browser_policy.py
autonomous: true
requirements: [TST-10, TST-11]

must_haves:
  truths:
    - "One support module starts companion/app.py on a free loopback port. Its state dir is under the test's tmp_path, it runs under child_env(), and teardown kills the whole process group"
    - "A companion test gets a running server, a logged-in cookie and a non-redirect-following HTTP client from fixtures, with no Harness class of its own"
    - "Under pytest-playwright, a missing or unlaunchable Chromium fails with CI=true or SKYPANE_REQUIRE_BROWSER=1, and is a visible skip with a reason otherwise. It never passes"
    - "A browser test cannot reach a non-loopback host: the request is aborted and the test fails"
    - "pytest-playwright is in the hash-locked dev lock; the runtime lock is byte-identical"
  artifacts:
    - path: "test-support/companion_app_server.py"
      provides: "AppServer, InProcessAppServer, LegacyHarness (transition alias), http_request, cookie_value, login, get, served_stylesheet, served_asset, TEST_PASSWORD"
      contains: "class AppServer"
    - path: "companion/conftest.py"
      provides: "app_server, make_app_server, module_app_server_factory, app_server_in_process, browser (CI-fail/local-skip), new_context override with loopback-only route guard, blocked_requests"
      contains: "def browser("
    - path: "server/requirements-dev.txt"
      provides: "pytest-playwright pinned with hashes"
      contains: "pytest-playwright==0.9.0"
    - path: "companion/test_browser_policy.py"
      provides: "proof of the missing-browser policy and the loopback guard"
  key_links:
    - from: "test-support/companion_app_server.py"
      to: "skypane_test_support.child_env"
      via: "every Popen env"
      pattern: "child_env\\("
    - from: "companion/conftest.py"
      to: "pytest-playwright browser_type fixture"
      via: "session-scoped browser override"
      pattern: "browser_type"
---

<objective>
Build the ONE shared companion app-server fixture that replaces the 4 copied `Harness`
classes, `_InProcessHarness`, `http_request` and `_NoRedirectHandler` (TST-10). Add
pytest-playwright together with the missing-browser policy and a loopback-only browser guard
(TST-11).

Purpose: every migration plan (33-04..33-31) builds on these fixtures. Doing it once, before
any migration, removes the copy drift (only 1 of the 4 `Harness` copies has fake providers).
The legacy `LegacyHarness` alias lets 33-19 repoint the still-legacy browser harnesses away
from `test_companion_app.py`, so that file can be deleted in stages.

Output:
- the pytest-playwright lock
- `test-support/companion_app_server.py`
- `companion/conftest.py`
- two self-test modules
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-CONTEXT.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-RESEARCH.md
@.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md
@test-support/skypane_test_support.py
@conftest.py
@pyproject.toml

<interfaces>
From companion/test_companion_app.py (the reference copies to generalise; read, do not modify):
- TEST_PASSWORD = "companion-test-password-please-ignore" (line 83); APP_PATH = HERE/app.py; STARTUP_DEADLINE_S = 10.0
- class _NoRedirectHandler (1163) + http_request(url, method="GET", data=None, cookie=None, timeout=10, content_type=None, extra_headers=None) -> (status, headers_dict, body_bytes) (1187-1220)
- _cookie_value(headers) (1223); class Harness(extra_args=()) with tmpdir, port, start(), stop(), base_url(), fake_provider_calls(), read_stdout(), cleanup() (1233-1326)
- class _InProcessHarness (1329-1384): ThreadingHTTPServer over companion.app.Handler with Handler.args = argparse.Namespace(state_dir=..., geofence=None), password env set/restored
- _login(harness, password=TEST_PASSWORD) -> cookie; expects 303 (1507-1520)

From test-support/skypane_test_support.py:
- child_env(base=None, *, fake_providers=None, state_dir=None) -> env dict (fake_providers may be a FakeProviders instance or "default")
- FakeProviders(); FakeProviders.read_calls_log(spec_path); requires_non_root; REPO_ROOT; TEST_SUPPORT_DIR

companion.auth.PASSWORD_ENV_VAR — the env var the app reads its password from.
</interfaces>

<planner_findings>
- RESEARCH checked pytest-playwright 0.9.0 against the pinned stack (playwright 1.63.0, pytest
  9.1.1, Python 3.14) and under xdist, where each worker launches its own browser.
  - New transitive packages: pytest-base-url, python-slugify, text-unidecode.
  - The Package Legitimacy Audit rates every one [OK], with the official docs confirmed, so no
    [ASSUMED]/[SUS] package and no legitimacy checkpoint is needed.
- Do NOT pass `--browser-channel`: the default resolves to the headless shell that CI's
  `--only-shell` install provides.
- In allow-hosts mode, pytest-socket guards only `connect()` in THIS process. Chromium's own
  network stack is a separate process, hence the route-level guard in Task 3.
- `/opt/pw-browsers` is stale here. For every browser command, follow the pre-flight in
  33-MIGRATION-RULES.md section 0 (`PLAYWRIGHT_BROWSERS_PATH=/tmp/skypane-pw-browsers`).
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest-playwright to the dev lock and register the browser marker</name>
  <files>server/requirements-dev.in, server/requirements-dev.txt, pyproject.toml</files>
  <read_first>
    - server/requirements-dev.in (comment style of each pin)
    - scripts/lock-deps.sh (how both locks regenerate)
    - pyproject.toml [tool.pytest.ini_options] markers list
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-RESEARCH.md "Package Legitimacy Audit"
  </read_first>
  <action>
(1) Append to `server/requirements-dev.in`, after `pytest-socket`, the pin `pytest-playwright==0.9.0`,
with a one-line comment above it: pytest fixtures (`page`, `browser`) for the companion browser
tests; dev-only, never deployed.

(2) Run `scripts/lock-deps.sh`. Then:
- `git diff --quiet server/requirements.txt` must exit 0; if it does not, revert that file and
  stop — the runtime lock must not change (CONTEXT D-A2);
- confirm `playwright==1.63.0` is still pinned in the dev lock.

(3) Install into the local venv:
`server/.venv/bin/pip install --require-hashes -r server/requirements-dev.txt`.

(4) In `pyproject.toml` markers, add
`"browser: drives a real Chromium through pytest-playwright (needs playwright install --only-shell chromium)"`.
Change nothing else in that file.

(5) Confirm the plugin does not disturb the existing suite: run
`server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider server stub-server test-support -q`.
It must pass as before; `requires_non_root` skips as root are expected.
  </action>
  <verify>
    <automated>grep -q "^pytest-playwright==0.9.0" server/requirements-dev.in && grep -q "^pytest-playwright==0.9.0" server/requirements-dev.txt && git diff --quiet server/requirements.txt && server/.venv/bin/python3 -c "import pytest_playwright" && server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider server stub-server test-support -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sha256:" server/requirements-dev.txt` is greater than it was before the change, and `grep -q "^playwright==1.63.0" server/requirements-dev.txt` exits 0
    - `git diff --quiet server/requirements.txt` exits 0
    - `grep -q '"browser: ' pyproject.toml` exits 0
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider server stub-server test-support -q` exits 0
  </acceptance_criteria>
  <done>pytest-playwright is installed from the hash lock; the runtime lock is unchanged; the marker is registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared app-server support module and fixtures</name>
  <files>test-support/companion_app_server.py, companion/conftest.py, companion/test_app_server_fixture.py</files>
  <read_first>
    - companion/test_companion_app.py lines 60-100 and 1160-1530 (Harness, _InProcessHarness, http_request, _NoRedirectHandler, _cookie_value, _login — the behaviour to preserve)
    - companion/test_config_page.py, companion/test_status_pages.py, companion/test_view_pages.py: grep `class Harness` in each and read those ~90 lines (the no-fake-providers variant)
    - companion/app.py argparse section (grep `add_argument`) and the STYLE_ROUTE constant (grep `STYLE_ROUTE`)
    - test-support/skypane_test_support.py lines 211-420 (FakeProviders, child_env, requires_non_root)
  </read_first>
  <behavior>
    - app_server fixture: GET /login returns 200; POST /login with TEST_PASSWORD returns 303 with a Set-Cookie; login(app_server) returns a non-empty cookie
    - http_request never follows redirects: an unauthenticated GET / returns 303 with a Location header that contains /login
    - two app_server instances in one test (via make_app_server) run on different ports and have different state dirs, both under tmp_path
    - after AppServer.stop(), os.kill(pid, 0) raises ProcessLookupError; stop() also terminates a grandchild in the same process group
    - the child's environment carries the no-network var (read /proc/<pid>/environ) and PYTHONPATH starts with TEST_SUPPORT_DIR
    - make_app_server(fake_providers=True) writes fake-providers.json into its state dir, and fake_provider_calls() returns a list
    - make_app_server(seed=fn) calls fn(state_dir) before the process starts
    - app_server_in_process serves /login and restores the password env var after teardown
    - served_stylesheet(server) returns str with a Content-Type starting with text/css; served_asset(server, "/static/<name>.js") returns the served JS text
  </behavior>
  <action>
Create `test-support/companion_app_server.py`: stdlib only, plus `skypane_test_support`. It
contains:

- `TEST_PASSWORD` (the same literal), `APP_PATH`, `STARTUP_DEADLINE_S = 10.0` and
  `pick_free_port()`.
- `class AppServer(state_dir, *, extra_args=(), fake_providers=False, password=TEST_PASSWORD, env_overrides=None)`:
  - `start()`:
    - builds its env with `child_env(dict(os.environ), fake_providers=FakeProviders() if fake_providers is True else (fake_providers or None), state_dir=state_dir)`;
    - sets `auth.PASSWORD_ENV_VAR` and applies `env_overrides`;
    - launches `[sys.executable, APP_PATH, "--port", str(port), "--state-dir", state_dir, *extra_args]`
      with `start_new_session=True`, stdout+stderr going to `<state_dir>/app.stdout.log`;
    - polls the port until it accepts a connection. If the process exits early or the deadline
      passes, it raises `RuntimeError` including the log text.
  - `stop()`:
    - sends SIGTERM to the process group; SIGTERM lets coverage `sigterm=true` save data;
    - waits 5 s, then SIGKILLs the group, then waits;
    - is idempotent.
  - `base_url()`, `url(path)`, `read_stdout()`, `fake_provider_calls()`
    (`FakeProviders.read_calls_log` on the spec path), plus `pid`, `port`, `state_dir`, and
    `tmpdir` as an alias of `state_dir`.
- `class InProcessAppServer(state_dir)`: the `_InProcessHarness` logic, taking its state dir as
  an argument; `stop()` restores the env.
- `class LegacyHarness(AppServer)`, a transition alias for still-legacy script harnesses only:
  - no-arg constructor (`extra_args=()`), `state_dir = tempfile.mkdtemp(prefix="skypane-companion-")`,
    and `fake_providers=True`, matching `test_companion_app.Harness`;
  - `cleanup()` rmtrees the state dir;
  - its docstring states it is removed by the closing plan once no legacy harness remains.
- The HTTP functions:
  - `_NoRedirectHandler`, and `http_request` with the same signature and return shape;
  - `cookie_value(headers)`;
  - `login(server, password=TEST_PASSWORD)`, which raises `AssertionError` unless it gets a 303
    with a cookie;
  - `get(server, path, cookie=None)`;
  - `served_stylesheet(server)`: GETs the app's style route and asserts a 200 and a `text/css`
    content type;
  - `served_asset(server, path)`.

Create `companion/conftest.py`. Its first import makes sure `test-support` is on `sys.path`, as
root `conftest.py` does. It defines these fixtures:

- `app_server`: function-scoped; `AppServer(str(tmp_path / "state"))`, started and stopped;
  no fake providers.
- `make_app_server`: a function-scoped factory with signature
  `(seed=None, extra_args=(), fake_providers=False, env_overrides=None)`. Each server gets
  `tmp_path / "state-<n>"`, the seed is called before start, and every server is stopped at
  teardown.
- `module_app_server_factory`: the same factory, module-scoped, over `tmp_path_factory.mktemp("companion-module")`.
- `app_server_in_process`: function-scoped, over `tmp_path`.

Scope rationale in its docstring: function scope isolates each test by default, and module scope
is for read-only groups (33-MIGRATION-RULES.md section 2).

Write `companion/test_app_server_fixture.py`, one test per `<behavior>` line, RED first.
  </action>
  <verify>
    <automated>server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_app_server_fixture.py -v && server/.venv/bin/ruff check test-support companion/conftest.py companion/test_app_server_fixture.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_app_server_fixture.py` exits 0 with at least 9 passed
    - `grep -c "child_env(" test-support/companion_app_server.py` is at least 1, and `grep -q "start_new_session=True" test-support/companion_app_server.py` exits 0
    - `grep -qE "def (app_server|make_app_server|module_app_server_factory|app_server_in_process)\(" companion/conftest.py` matches all four names (check each with its own grep)
    - `grep -q "tempfile" companion/conftest.py companion/test_app_server_fixture.py` exits 1 (no tempfile use in the fixtures or their tests)
    - `server/.venv/bin/python3 -m pytest -p no:cacheprovider companion/test_legacy_harness_shim.py -k "contrast_check or i18n"` still exits 0 (legacy path untouched)
  </acceptance_criteria>
  <done>A single shared app-server implementation with fixtures exists, is self-tested, and uses child_env() and process-group teardown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Missing-browser policy and loopback-only browser guard</name>
  <files>companion/conftest.py, companion/test_browser_policy.py</files>
  <read_first>
    - companion/conftest.py (Task 2 output)
    - companion/test_legacy_harness_shim.py (_browser_required() semantics to preserve: SKYPANE_REQUIRE_BROWSER=="1" or CI.lower()=="true")
    - .planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-PATTERNS.md section "Browser tests — pytest-playwright wiring"
    - server/.venv/lib/python3*/site-packages/pytest_playwright/pytest_playwright.py (names and scopes of browser_type, browser_type_launch_args, context, new_context fixtures)
  </read_first>
  <behavior>
    - With a launchable Chromium, a @pytest.mark.browser test using `page` opens app_server's /login and reads its <title> or the password field
    - page.goto("http://example.invalid/") inside a test is aborted by the guard (raises a Playwright Error), and the blocked_requests list contains that URL; the same holds for a context made with new_context(viewport={"width": 360, "height": 844})
    - a test that triggers a non-loopback request and does not clear the recorded list fails at teardown with a message naming the URL
    - in a subprocess pytest run over a tiny browser test, with PLAYWRIGHT_BROWSERS_PATH pointing at an empty tmp dir:
      - with CI=true, rc != 0 and the output contains "could not launch"
      - with CI and SKYPANE_REQUIRE_BROWSER removed, rc == 0 and the output reports 1 skipped whose reason contains "could not launch"
  </behavior>
  <action>
Add to `companion/conftest.py`:

- `_browser_required()`, with the shim's exact semantics.
- A session-scoped `browser(browser_type, browser_type_launch_args)` override:
  - it tries `browser_type.launch(**browser_type_launch_args)`;
  - on any exception it calls `pytest.fail("Chromium could not launch (CI / SKYPANE_REQUIRE_BROWSER=1): %r" % exc)`
    when a browser is required, and otherwise
    `pytest.skip("Chromium could not launch: %r - run `playwright install --only-shell chromium`" % exc)`;
  - it yields the browser, then closes it.

  If `browser_type` itself errors because Playwright is missing, the same policy applies: import
  playwright lazily inside a try.
- A loopback-only guard on EVERY browser context. Override pytest-playwright's function-scoped
  `new_context` factory fixture in `companion/conftest.py`: a same-named fixture that requests the
  plugin's `new_context` (pytest resolves the parent definition). Its wrapper calls the original,
  then `ctx.route("**/*", handler)`, and returns the context. The plugin's own `context` / `page`
  fixtures are built on `new_context`, so they are covered too; confirm this in the plugin source
  listed in read_first.
  - The handler continues requests whose scheme is `data`, `blob` or `about`, or whose host is
    `127.0.0.1`, `localhost` or `[::1]`. It aborts everything else and appends the URL to a
    per-test list.
  - Teardown calls `pytest.fail` listing any recorded URLs.
  - Expose `blocked_requests` as a fixture returning that list, so the proof test can read and
    clear it.
  - Tests that need several viewports call the `new_context(viewport=...)` fixture, never
    `browser.new_context(...)` directly. The 33-03 guard rule G10 enforces this.

Create `companion/test_browser_policy.py` (`pytestmark = pytest.mark.browser` for its in-browser
tests) implementing `<behavior>`.

The subprocess cases write a probe project entirely inside `tmp_path`, so nothing is written to
the repo. It has three files:
- `pytest.ini`: `[pytest]` and nothing else, so the repo addopts do not apply;
- `conftest.py`: the single line `pytest_plugins = ["companion.conftest"]`, which loads the real
  override;
- `test_probe.py`: one test taking `page`.

Run it as `[sys.executable, "-m", "pytest", "-p", "no:cacheprovider", "-q", "-rs", str(probe_dir)]`
with `cwd=probe_dir`. The env is `child_env()` plus:
- `PYTHONPATH` extended with REPO_ROOT, so `companion.conftest` imports;
- `PLAYWRIGHT_BROWSERS_PATH` set to an empty `tmp_path` subdir;
- `CI` / `SKYPANE_REQUIRE_BROWSER` explicitly set or removed per case.

Mark these two subprocess tests `slow`.

Run the in-browser tests locally with the pre-flight `PLAYWRIGHT_BROWSERS_PATH` from
33-MIGRATION-RULES.md section 0 and `SKYPANE_REQUIRE_BROWSER=1`.
  </action>
  <verify>
    <automated>SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_policy.py companion/test_app_server_fixture.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `SKYPANE_REQUIRE_BROWSER=1 server/.venv/bin/python3 -m pytest -n auto -p no:cacheprovider companion/test_browser_policy.py` exits 0 with 0 skipped (the browser really ran; export PLAYWRIGHT_BROWSERS_PATH per the pre-flight if needed)
    - `grep -q "could not launch" companion/conftest.py && grep -q "def new_context(" companion/conftest.py && grep -q ".route(" companion/conftest.py` exits 0
    - `git status --porcelain companion/ | grep -v -E "conftest.py|test_browser_policy.py|test_app_server_fixture.py"` prints nothing after the run (no probe files left in the repo)
    - `server/.venv/bin/ruff check companion/conftest.py companion/test_browser_policy.py` exits 0
  </acceptance_criteria>
  <done>A missing browser fails in CI and skips locally, both proven by a subprocess test. Browser tests are confined to loopback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest process → companion/app.py child | the child must inherit the no-network guard |
| pytest process → Chromium process | Chromium's network stack is outside pytest-socket |
| PyPI → dev venv | new dev dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Information Disclosure | companion/app.py children reaching real ADS-B hosts | mitigate | `AppServer.start()` builds env only via `child_env()` (the sitecustomize guard); a test proves the no-network var in /proc/<pid>/environ |
| T-33-02-02 | Information Disclosure | Chromium navigating off-box | mitigate | `new_context` override installs a route guard on every context (including `context`/`page`); non-loopback requests are aborted and fail the test; proven by test_browser_policy; guard G10 forbids bypassing it via `browser.new_context` |
| T-33-02-03 | Denial of Service | orphaned app.py children holding ports | mitigate | `start_new_session=True` + killpg SIGTERM→SIGKILL teardown; tested with pid liveness |
| T-33-02-04 | Information Disclosure | TEST_PASSWORD reaching production | accept | test-support/ and requirements-dev.txt are never deployed (deploy/deploy.sh installs server/requirements.txt only) |
| T-33-SC | Tampering | pip install of pytest-playwright + transitive deps | mitigate | RESEARCH Package Legitimacy Audit: all [OK]/verified (no [ASSUMED]/[SUS], so no checkpoint); installed only via `--require-hashes` from the uv-generated lock |
</threat_model>

<verification>
- `pytest -n auto companion/test_app_server_fixture.py companion/test_browser_policy.py` green with SKYPANE_REQUIRE_BROWSER=1
- server/stub-server/test-support suite unchanged and green
- runtime lock untouched
</verification>

<success_criteria>
Every later plan can write `def test_x(app_server): ...` or `def test_y(page, app_server): ...`
without copying lifecycle plumbing. The browser policy of TST-11 is enforced by a fixture and
proven by a test.
</success_criteria>

<output>
Create `.planning/phases/33-companion-tests-on-pytest-behaviour-over-source-text/33-02-SUMMARY.md` when done
</output>
