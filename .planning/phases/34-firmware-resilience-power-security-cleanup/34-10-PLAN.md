---
phase: 34-firmware-resilience-power-security-cleanup
plan: 10
type: execute
wave: 4
depends_on: ["34-03", "34-05", "34-08", "34-09"]
files_modified:
  - firmware/VENDOR.md
  - firmware/tests/check_log_contract.sh
  - .github/workflows/firmware.yml
  - hardware/PHASE34-HARDWARE-SESSION.md
autonomous: true
requirements: [FW-01, FW-02, FW-03, FW-04, FW-06, FW-07, FW-08, FW-09, FW-10, FW-11, FW-12, FW-13, FW-15]
user_setup: []

must_haves:
  truths:
    - "firmware/VENDOR.md documents every new step= value (auth, enrol, secret, config, reset, deadline), the unchanged five line shapes, and every new diagnostic line as outside the contract."
    - "A CI check proves the five Log Line Contract format strings are still present byte-for-byte in the firmware sources and that every step token the code can emit is documented in VENDOR.md."
    - "VENDOR.md lists every file this phase added or modified, the ISRG-only bundle's operational risk, and why the 800 us row pacing was kept."
    - "hardware/PHASE34-HARDWARE-SESSION.md exists with pre-registered expectations for every scenario the hardware plan runs, so results are judged against thresholds written before the session."
    - "The full production build passes the built production check (https-only, no fault code, git-derived version)."
  artifacts:
    - path: "firmware/VENDOR.md"
      provides: "Updated Log Line Contract, file inventory, operational notes"
      contains: "deadline"
    - path: "firmware/tests/check_log_contract.sh"
      provides: "Contract byte-compatibility and step-token documentation check"
    - path: "hardware/PHASE34-HARDWARE-SESSION.md"
      provides: "Results template with scenario IDs H-00..H-20 and pre-registered thresholds"
  key_links:
    - from: ".github/workflows/firmware.yml"
      to: "firmware/tests/check_log_contract.sh"
      via: "CI step"
      pattern: "check_log_contract.sh"
    - from: "hardware/PHASE34-HARDWARE-SESSION.md"
      to: "hardware/logs/phase34/"
      via: "each scenario row names its capture file"
      pattern: "hardware/logs/phase34/"
---

<objective>
Close the autonomous part of the phase: bring firmware/VENDOR.md in line with
the code (Log Line Contract additions, file inventory, operational notes), add a
CI guard for the contract, prepare the hardware-session results document with
pre-registered expectations, and confirm the production build end to end.

Purpose: the Log Line Contract must stay byte-compatible and every new step
value must be documented (phase constraint); the single hardware session (plan
34-11) needs a results document whose pass criteria were written before the
measurements. English only, no plan/ticket history in the text (D-A3).
Output: VENDOR.md, check_log_contract.sh (+ CI step), PHASE34-HARDWARE-SESSION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-firmware-resilience-power-security-cleanup/34-CONTEXT.md
@.planning/phases/34-firmware-resilience-power-security-cleanup/34-RESEARCH.md
@.planning/phases/34-firmware-resilience-power-security-cleanup/34-11-PLAN.md
@firmware/VENDOR.md
@hardware/BACKOFF-OBSERVATION.md

<interfaces>
Contract format strings that must remain byte-identical (tag `skypane`):
  app_main.c:     "wake reason=%s boot_count=%" PRIu32
                  "poll ok sleep_s=%" PRIu32 " hash_skip=%u"
                  "poll fail step=%s backoff_n=%u sleep_s=%" PRIu32
                  "sleep enter sleep_s=%" PRIu32
  state_machine.c "blit ok bytes=960000 sha256_ok=1"
Step tokens the code can emit after plans 34-06/34-08: wifi, http, status, json, download, verify, blit, auth, enrol, secret, config, reset, deadline.
Diagnostic lines outside the contract: fp_boot "reset reason=<label>"; fp_diag "wake timing total_ms=... wifi_ms=... setup_ms=... display_ms=... download_ms=... draw_ms=..."; fp_api "http connects=<n> first_connect_ms=<n> tls_offered=<0|1> tls_saved_len=<n>" and "TLS session saved (<n> bytes)"; fp_batt "battery mv=<pack> pin_mv=<pin>" (now the mean of 8 samples); fp_fault "SKYPANE-FAULT-INJECT <name>" (dev fault builds only).
Hardware scenario IDs (shared with plan 34-11): H-00 .. H-20 as listed in Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VENDOR.md update and a CI guard for the Log Line Contract</name>
  <files>firmware/VENDOR.md, firmware/tests/check_log_contract.sh, .github/workflows/firmware.yml</files>
  <read_first>
    - firmware/VENDOR.md (whole file: vendored-files table, "Original To This Repository", "Log Line Contract", "Re-verification")
    - firmware/main/app_main.c, firmware/main/state_machine.c (final state after plans 34-06/34-08: contract strings and the step_for table)
    - firmware/main/api_client.c, tls_session.c, wake_guard.c, fault_inject.c, battery.c (diagnostic lines)
    - git log --stat for this phase's commits (`git log --stat --since=2026-09-23 -- firmware/`) to build the file inventory
    - .github/workflows/firmware.yml (as left by plan 34-04)
  </read_first>
  <action>
Update `firmware/VENDOR.md`:
- "Log Line Contract": keep the five-row table's shapes exactly; extend the `poll fail step=` row's token list to `wifi|http|status|json|download|verify|blit|auth|enrol|secret|config|reset|deadline` and add, under the table, a short list defining each NEW token (auth: server rejected the bearer token, token erased, next wake re-enrols; enrol: setup refused — secret not accepted or device not registered; secret: no valid enrolment secret in the secret partition — run firmware/provision.sh; config: API base URL rejected by this build, e.g. http:// in a production image; reset: previous wake ended in a panic/watchdog/brownout-class reset, no poll this wake; deadline: wake exceeded CONFIG_SKYPANE_WAKE_BUDGET_S). State that `wake reason=power-on` now appears only for a real power-on reset; abnormal resets report `other`. Add a "Diagnostic lines (outside the contract)" paragraph listing every line in the interfaces block with its tag.
- Vendored-files table: update the "no"-row descriptions for every upstream file this phase modified (api_client.c/.h, nvs_schema.h, state_machine.c/.h, app_main.c, epd13in3e.c, panel.c, wifi.c, CMakeLists.txt, sdkconfig.defaults, partitions.csv, Kconfig.projbuild, main/CMakeLists.txt) with a factual summary of the change; flip any row that was "yes" (verbatim) to "no" if its file changed (check api_base.c stays verbatim — it should).
- "Original To This Repository": add validate, reset_reason, wake_deadline, sleep_decision, wake_guard, nvs_util, enrol_secret, fault_inject, tls_session (each .c/.h), provision.sh, sdkconfig.dev.defaults, main/certs/, tests/check_production_config.sh, tests/check_log_contract.sh and the new test_*.c files, one line each; update the run_host_tests.sh and build.sh bullets (convention-based discovery; prod/dev profiles and git-derived version); update secrets.example.h's bullet (no setup secret; optional SKYPANE_API_BASE_DEV and static-IP macros).
- Add an "Operational notes" section: the TLS trust store holds only ISRG Root X1/X2 — if Let's Encrypt moves the VPS chain to another root, frames fail TLS until reflashed, so update main/certs first; the 800 us per-row pacing is kept because the panel datasheet gives no per-row timing and the value comes from the vendor reference driver; app rollback is disabled until OTA exists.
English only; describe what the code does, no plan IDs (D-A3).

Create `firmware/tests/check_log_contract.sh` (POSIX sh, set -eu, self-locating): (1) assert each of the five contract format strings in the interfaces block is present verbatim in app_main.c / state_machine.c (grep -F); (2) extract every string literal returned by state_machine.c's step mapping plus the literals passed to `fail_and_sleep(` in app_main.c, and assert each appears in VENDOR.md's `poll fail step=` row; (3) assert VENDOR.md's table still contains the five shapes. Print `log-contract: PASS` or the failing item and exit 1. Add a step `Check Log Line Contract` running it to `.github/workflows/firmware.yml` next to the production-config static check (before the build).
  </action>
  <verify>
    <automated>sh firmware/tests/check_log_contract.sh &amp;&amp; grep -q "auth|enrol|secret|config|reset|deadline" firmware/VENDOR.md &amp;&amp; grep -q "check_log_contract.sh" .github/workflows/firmware.yml &amp;&amp; grep -q "ISRG Root X1" firmware/VENDOR.md &amp;&amp; ! grep -n "SKYPANE_SETUP_SECRET" firmware/VENDOR.md</automated>
  </verify>
  <acceptance_criteria>
    - `sh firmware/tests/check_log_contract.sh` prints `log-contract: PASS` and exits 0.
    - Temporarily changing `hash_skip=%u` to `hash_skip=%d` in app_main.c makes the script exit 1 (then revert; `git diff firmware/main/app_main.c` empty afterwards).
    - VENDOR.md's `poll fail step=` row contains all 13 tokens; each new token has a one-line definition below the table.
    - `grep -c "SKYPANE_SETUP_SECRET" firmware/VENDOR.md` returns 0; `grep -c "tls_session\|wake_guard\|enrol_secret\|provision.sh" firmware/VENDOR.md` is at least 4.
    - firmware.yml runs check_log_contract.sh before `Build firmware image`.
  </acceptance_criteria>
  <done>VENDOR.md matches the code, and CI fails if a contract line changes shape or a step token goes undocumented.</done>
</task>

<task type="auto">
  <name>Task 2: Hardware-session results template with pre-registered expectations, and the end-to-end build gate</name>
  <files>hardware/PHASE34-HARDWARE-SESSION.md</files>
  <read_first>
    - hardware/BACKOFF-OBSERVATION.md (verdict-first structure, honest disclosure of partial results)
    - hardware/BATTERY-RUN.md lines 325-350 (the ~28 s overhead), hardware/logs/backoff-run.log (the ≈4.4 s / ≈3.0 s baseline)
    - .planning/phases/34-firmware-resilience-power-security-cleanup/34-11-PLAN.md (the script whose scenario IDs this template must match exactly)
    - .planning/ROADMAP.md Phase 34 success criteria 1-5
  </read_first>
  <action>
Create `hardware/PHASE34-HARDWARE-SESSION.md` (English, D-A3) with these sections, left for the developer to fill:
1. `## Verdict` — placeholder line `PENDING` plus one bullet per ROADMAP success criterion 1-5 with `PENDING`.
2. `## Build under test` — fields: commit (`git rev-parse HEAD`), production `Firmware version:` line, dev `Firmware version:` line, device MAC, date, network (LAN stub host, VPS host placeholder `<public-host>`).
3. `## Scenario results` — one table, columns `ID | Requirement | Pre-registered expectation | Observed | Capture file | Result (PASS/FAIL/N/A)`, one row per scenario H-00..H-20 exactly as defined in 34-11-PLAN.md, with the expectation column filled in now (e.g. H-05: "`poll fail step=auth backoff_n=<n> sleep_s=<n>` on the wake after the revoke; the next wake logs `setup accepted; device credential stored` and `poll ok`; server log shows 401 then `setup: <mac> enrolled`"; H-12: "`reset reason=panic`, `wake reason=other`, `poll fail step=reset backoff_n=0 sleep_s=300`, then after the 300 s sleep a second panic and `backoff_n=1 sleep_s=600`"; H-15: "`poll fail step=deadline` with the `wake timing total_ms=` value between 300000 and 360000"; H-17: "|X-Battery-Mv − multimeter| ≤ 60 mV on each of 3 readings, and the `fp_batt` line appears before the first Wi-Fi log line"). Capture file names follow `hardware/logs/phase34/<ID>-<slug>.log` (device) and `...-server.log` (server).
4. `## Wake duration (no-change wakes)` — table: configuration (LAN stub before = 4.4 s total / 3.0 s DHCP from hardware/logs/backoff-run.log; LAN stub after; VPS before; VPS after with tls_offered=0; VPS after with tls_offered=1; LAN after with static IP) × median `total_ms`, median `wifi_ms`, median `display_ms`, `http connects`, `first_connect_ms`, n wakes. Pre-registered expectation: LAN-after median wifi_ms below 3000; VPS-after connects=1 per no-change wake; tls_offered=1 wakes show a lower first_connect_ms than tls_offered=0 if Caddy resumes sessions (either outcome is recorded — resumption is best effort).
5. `## The ~28 s per-cycle overhead` — a structured explanation to fill: the three-way comparison (LAN before vs VPS before vs VPS after), the measured TLS connect cost, DHCP share, full-blit share, and a one-paragraph conclusion.
6. `## Battery vs multimeter` — 3-row table (time, multimeter mV at the JST connector, X-Battery-Mv from the server log, delta).
7. `## Deviations and not observed` — free text, with prompts for brownout (bench PSU unavailable), static IP (skipped if not needed) and anything that failed.
8. `## Checker output` — placeholders for `sh firmware/tests/check_production_config.sh built firmware/build-ee02` and `python3 hardware/logtools.py` outputs if used.

Then run the end-to-end gate: `sh firmware/tests/run_host_tests.sh`, `sh firmware/tests/check_production_config.sh static`, `sh firmware/tests/check_log_contract.sh`, `python3 stub-server/test_devices_registry.py`, and — if Docker is available — `./firmware/build.sh` followed by `sh firmware/tests/check_production_config.sh built firmware/build-ee02` and `SKYPANE_PROFILE=dev SKYPANE_FAULT=panic ./firmware/build.sh` (proves the dev fault build compiles; then `grep -a -c SKYPANE-FAULT-INJECT firmware/build-ee02-dev/skypane.bin` ≥ 1 while the production binary has 0). Record the outputs in the SUMMARY; if Docker is unavailable, push the branch and record the Firmware Build workflow run URL and result instead.
  </action>
  <verify>
    <automated>test -f hardware/PHASE34-HARDWARE-SESSION.md &amp;&amp; for id in H-00 H-05 H-12 H-15 H-17 H-20; do grep -q "| $id " hardware/PHASE34-HARDWARE-SESSION.md || exit 1; done &amp;&amp; grep -q "hardware/logs/phase34/" hardware/PHASE34-HARDWARE-SESSION.md &amp;&amp; sh firmware/tests/run_host_tests.sh &amp;&amp; sh firmware/tests/check_production_config.sh static &amp;&amp; sh firmware/tests/check_log_contract.sh &amp;&amp; python3 stub-server/test_devices_registry.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| H-" hardware/PHASE34-HARDWARE-SESSION.md` returns 21 (H-00..H-20) and every row's expectation column is non-empty.
    - The template contains the sections `## Verdict`, `## Build under test`, `## Scenario results`, `## Wake duration (no-change wakes)`, `## The ~28 s per-cycle overhead`, `## Battery vs multimeter`, `## Deviations and not observed`, `## Checker output`.
    - All four autonomous gates in verify exit 0.
    - The SUMMARY records either the local Docker build + `production-config built: PASS` + the dev fault marker count, or the CI run URL with a passing Firmware Build.
  </acceptance_criteria>
  <done>The phase is fully built and checked without hardware, and the developer has a results document whose expectations were fixed before the session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository docs -> future operators | Wrong docs lead to insecure operation (e.g. stale shared secret, stale CA roots) |
| CI -> merged firmware | Guards must fail on contract or production-config regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10-01 | Repudiation | Log Line Contract | mitigate | check_log_contract.sh in CI keeps captured-log evidence comparable across phases |
| T-34-10-02 | Denial of Service | CA root rotation | mitigate | Operational note in VENDOR.md tells the operator to update main/certs before a root change |
| T-34-10-03 | Information Disclosure | results document | mitigate | Template tells the developer to record MAC and hashes only as needed and never tokens or secrets (repeated in 34-11's scrub step) |
</threat_model>

<verification>
- check_log_contract.sh, check_production_config.sh static, run_host_tests.sh, test_devices_registry.py all pass.
- Production build + built check pass (locally or in CI).
</verification>

<success_criteria>
- Log Line Contract byte-compatible and every new step value documented (phase constraint).
- Success criterion 3 fully in CI (host tests + production checks + contract check).
- Hardware session has pre-registered expectations.
</success_criteria>

<output>
Create `.planning/phases/34-firmware-resilience-power-security-cleanup/34-10-SUMMARY.md` when done.
</output>
