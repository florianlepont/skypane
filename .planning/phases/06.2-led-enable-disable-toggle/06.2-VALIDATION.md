---
phase: 06.2
slug: led-enable-disable-toggle
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-28
---

# Phase 06.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Custom hand-rolled `check()`-based harnesses (no pytest/unittest) — this repo's established convention |
| **Config file** | none — `scripts/run-all-tests.sh` is the canonical runner |
| **Quick run command** | `server/.venv/bin/python3 server/test_config_history.py` / `companion/test_config_page.py` / `stub-server/test_poll_cycle.py` (each directly executable) |
| **Full suite command** | `scripts/run-all-tests.sh` |
| **Estimated runtime** | ~30 seconds (existing suite, no new harness added) |

---

## Sampling Rate

- **After every task commit:** Run the specific harness(es) touched by that task
- **After every plan wave:** Run `scripts/run-all-tests.sh`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.2-01-01 | 01 | 1 | D-01/D-02 (led field round-trip) | T-06.2-01 | `load_device_config()`/`save_device_config()` round-trip `led_enabled`, reject non-bool, default `True` on hostile/missing input | unit | `server/.venv/bin/python3 server/test_config_history.py` | ✅ file exists, extend | ⬜ pending |
| 06.2-01-02 | 01 | 1 | D-01 (fieldset UI) | T-06.2-02 | `led_fieldset()` renders checked/unchecked correctly; `handle_post()` validates and persists, rejecting non-bool submissions | unit | `server/.venv/bin/python3 companion/test_config_page.py` | ✅ file exists, extend | ⬜ pending |
| 06.2-01-03 | 01 | 1 | Cross-process read (flagged risk) | T-06.2-03 | `byos_server.py`'s `/display` response reflects a `false` value written to `device_config.json` in the harness's own `--state-dir`, defaults to `true` on missing/malformed config, never crashes the poll response on a read failure | integration (subprocess, real HTTP) | `server/.venv/bin/python3 stub-server/test_poll_cycle.py` | ✅ file exists, extend, bump `EXPECTED_CHECK_COUNT` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. `server/test_config_history.py`, `companion/test_config_page.py`, and `stub-server/test_poll_cycle.py` all already exist, are already registered in `scripts/run-all-tests.sh`, and already cover the exact pattern (theme/runway round-trip, fieldset render, device-protocol response shape) this phase extends. No new test file, no new framework install, no new fixture needed.

---

## Manual-Only Verifications

*All phase behaviors have automated verification.* The one manual/human step is confirming the LED itself visibly stays off on real hardware after a `false` save and the device's next poll — captured as a `checkpoint:human-verify` step in the plan, not a substitute for the automated coverage above.

---

## Validation Sign-Off

- [x] All tasks have automated verify (see table above)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (all 3 tasks in the phase's single wave have direct coverage)
- [x] Wave 0 covers all MISSING references — N/A, no gaps
- [x] No watch-mode flags — all commands are one-shot script executions
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** confirmed 2026-08-28 by `/gsd-plan-phase 06.2`. The three task IDs above map 1:1 onto `06.2-01-PLAN.md`'s three tasks (`06.2-01-01` device_config round-trip, `06.2-01-02` fieldset UI + route, `06.2-01-03` byos_server cross-process read), each carrying the exact automated command listed in the map. Each task additionally bumps its harness's `EXPECTED_CHECK_COUNT` to a pinned value, so a missing check fails the harness rather than passing silently: `server/test_config_history.py` 16→21, `companion/test_config_page.py` 15→23, `stub-server/test_poll_cycle.py` 20→23.

The manual step anticipated above is `06.2-02-PLAN.md`'s single `checkpoint:human-verify` task (`06.2-02-01`), split into its own non-autonomous plan in wave 2 so it never shares a context window with implementation. It covers the one failure mode no harness in this phase can see: two *separately launched* processes disagreeing on `--state-dir` (06.2-RESEARCH.md Pitfall 2), which fails open to enabled and is indistinguishable from a broken read. Its Part A step 11 negative control is what distinguishes them.
