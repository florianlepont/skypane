# Phase 06.2: LED enable/disable toggle - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-28
**Phase:** 06.2-led-enable-disable-toggle
**Areas discussed:** Control placement and default state

---

## Control placement and default state

| Option | Description | Selected |
|--------|-------------|----------|
| New section, defaults ON | Own small section on the config page, consistent with theme/runway; defaults on since that matches current always-on behavior | ✓ |
| Fold into existing settings, defaults ON | Add as one more field inside the existing theme/runway form instead of its own section | |
| New section, defaults OFF | Own section, but ships defaulting to off (opt-in) since the LED is a bring-up aid, not normal-use | |

**User's choice:** New section, defaults ON.
**Notes:** Codebase investigation (prior to this question) confirmed firmware and the wire protocol already fully support the toggle — only `device_config.py`, `byos_server.py`'s hardcoded `True`, and a companion UI control are missing.

---

## Claude's Discretion

- Exact toggle copy/label text and control type (checkbox vs. Yes/No radio pair).
- The persisted JSON field name in `device_config.py`.

## Deferred Ideas

None — discussion stayed within phase scope.
