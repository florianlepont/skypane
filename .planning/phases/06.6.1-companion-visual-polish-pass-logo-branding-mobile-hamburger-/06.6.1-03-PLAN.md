---
phase: 06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/pages/health_page.py
  - companion/test_status_pages.py
autonomous: true
requirements: [D-02, D-04]

must_haves:
  truths:
    - "The Health anomaly banner still appears exactly when collect_anomalies() returns a non-empty list, and its revised copy points the reader at the tiles rather than at a detail list that no longer exists (D-04)."
    - "The redundant per-anomaly detail list is gone from the rendered page — the Overview tiles already carry the same information through their status colours and dots."
    - "collect_anomalies() itself is unchanged: it still computes the same four-signal list, and its truthiness is still the only thing gating the banner."
    - "Battery trend renders in its own full-width section below the Overview grid, not as a 240px-floor grid tile, and keeps its status badge, its interactive chart, its readout line and its single script tag working exactly as 06.5 shipped them (D-02)."
    - "The Overview grid now holds exactly three stat tiles under one Overview heading."
    - "anomaly_active(state_dir) returns the same verdict the banner does, for the same inputs, and never raises for any state directory — missing, empty, locked or corrupt."
    - "Every database read still goes through _safe_query(), and each Health section still degrades independently rather than faulting the page."
  artifacts:
    - companion/pages/health_page.py    # revised ANOMALY_BANNER_TEXT, BATTERY_SECTION_HEADING/CLASS constants, _read_health_inputs(), _battery_trend_section_html(), public anomaly_active(), restructured render()
    - companion/test_status_pages.py    # 2 existing checks corrected, 6 new checks, EXPECTED_CHECK_COUNT + 6
  key_links:
    - "anomaly_active() is the cross-page signal plan 06.6.1-04 threads into ctx and renders as the Health nav-tab dot. It must be importable and callable with only a state_dir, because companion/app.py's page_context() runs it on EVERY page render, not just Health's."
    - "anomaly_active() and render() must reach the same verdict from the same inputs, or the nav dot and the banner will disagree on screen. Task 3 pins that agreement in both directions with a real seeded database."
    - "`battery-trend-section` is the class companion/static/style.css styles (plan 06.6.1-01, same wave). Task 2 adds a drift guard reading the stylesheet from disk."
    - "companion/test_status_pages.py exits non-zero unless passed == total == EXPECTED_CHECK_COUNT. It reads 36 today; sibling plan 06.6-01 also adds checks here. Bump by exactly +6 from the file's CURRENT on-disk value, never to a hard-coded total."
---

<objective>
Take the redundancy and the cramping out of the Health page: drop the anomaly banner's detail list (the Overview tiles below it already say the same thing in colour), give the battery-trend chart the full page width it needs instead of a 240px grid track, and expose the anomaly verdict as a cheap, never-raising function so plan 04 can surface it on the nav without any page module importing another.

Purpose: real-device testing during 06.5-03 found the Health page saying the same thing twice — once in prose under the banner, once in the tiles — while the one genuinely information-dense element on the page, the battery chart, was squeezed into the narrowest column on it.
Output: a simplified banner, a full-width battery section, one new public function, six new checks and two corrected ones.
</objective>

## Two existing checks this plan knowingly breaks (fix them, do not work around them)

Both are real, both are in `companion/test_status_pages.py`, and both are correct today. Each task fixes exactly the one it breaks:

1. **`_battery_drop_drives_both_badge_and_banner`** asserts `"A battery reading shows an abnormal drop."` appears in the rendered page — copy that is only ever rendered by the `<ul>` detail list Task 1 removes. Task 1 rewrites that one assertion.
2. **`_health_page_renders_one_dashboard_grid_of_four_tiles`** asserts exactly four `class="stat-tile` occurrences. Task 2 moves Battery trend out of the grid, making it three. Task 2 rewrites that check and its registered name.

Neither may be deleted, weakened past what the change requires, or "fixed" by keeping the old behaviour. Each is guarding a property this plan deliberately changes, so it must be updated to guard the new property with the same rigour.

## Soft conflict with sibling phase 06.6 — read before editing

`.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-01-PLAN.md` is **planned but not executed** and touches this same file plus `companion/test_status_pages.py`: it promotes the timestamp helpers (`_parse_iso`/`_age_seconds`/`_relative_age_text`) into `companion/layout.py` as public functions, rewires `_device_section()`/`_pipeline_section()` onto them, and reformats the Battery Trend **table's** Timestamp column inside `_battery_section()`.

The edits are disjoint by concern: 06.6-01 owns timestamp *formatting inside* the section builders; this plan owns the banner assembly, `render()`'s layout structure and a new module-level function. This plan does **not** modify `_device_section()`, `_pipeline_section()`, `_corroboration_section()`, `battery_sparkline_svg()`, `battery_status()`, `staleness_status()` or `collect_anomalies()` at all, and touches `_battery_section()` **not at all** — the battery section's relocation happens at its `render()` call site, not inside it.

**Whichever runs second must** read `render()`, `_battery_section()` and the harness fresh from disk, treat every line reference here as a symbol to search for rather than a coordinate, read the other phase's `-SUMMARY.md` if present, and recompute `EXPECTED_CHECK_COUNT` from the file's real current value.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-CONTEXT.md
@.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md
@.claude/skills/sketch-findings-skypane/references/visual-direction-typography.md
@.planning/phases/06.5-interactive-battery-trend-chart-with-overall-status/06.5-02-SUMMARY.md
</context>

## Artifacts this plan produces

| Symbol / path | Kind | Location | Notes |
|---|---|---|---|
| `ANOMALY_BANNER_TEXT` | changed value | `companion/pages/health_page.py` | Now points at the tiles, since the list it used to point at is gone |
| `BATTERY_SECTION_HEADING` | new module constant (`"Battery trend"`) | `companion/pages/health_page.py` | The `<h2>` text, defined once so plan 04 can attach an icon to a known heading |
| `BATTERY_SECTION_CLASS` | new module constant (`"battery-trend-section"`) | `companion/pages/health_page.py` | Contract value shared with `companion/static/style.css` |
| `_read_health_inputs(state_dir, now)` | new function | `companion/pages/health_page.py` | The five `_safe_query()` reads, single-sourced so `render()` and `anomaly_active()` cannot read different things |
| `_battery_trend_section_html(battery_html)` | new function | `companion/pages/health_page.py` | Wraps the existing battery markup in the full-width card section |
| `anomaly_active(state_dir, now=None)` | **new public function** | `companion/pages/health_page.py` | The cross-page signal plan 04 threads into `ctx`; never raises |
| `render(ctx)` | changed | `companion/pages/health_page.py` | Three-tile grid, no `<ul>`, battery section emitted after the grid |
| 6 new + 2 corrected `check()` calls | tests | `companion/test_status_pages.py` | Plus the `EXPECTED_CHECK_COUNT` bump |

<tasks>

<task type="auto" tdd="true" id="06.6.1-03-01">
  <name>Task 1: Drop the anomaly banner's redundant detail list and re-point its copy (D-04)</name>

  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` — the module docstring's D-14 paragraph (`collect_anomalies()` decides whether the banner appears at all; its absence *is* the all-clear); `ANOMALY_BANNER_TEXT` near line 131; `collect_anomalies()` at 375-391 in full, including all four of its item copy strings; `render()` at 530-575 in full, especially the `if anomalies:` block that builds `items_html` and concatenates a `<ul>` onto `banner_html`.
    - `companion/layout.py` — `anomaly_banner()`, so it is clear the banner builder itself needs no change.
    - `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md` — the Copywriting Contract's "Anomaly banner heading (revised)" and "Anomaly detail list (removed)" rows (which carry the exact replacement string), and the Layout Contract's "Anomaly banner — redundancy removed" section.
    - `companion/test_status_pages.py` — `_battery_drop_drives_both_badge_and_banner` in full (**the one check this task breaks** — it asserts the abnormal-drop item copy appears in the rendered page), plus the two checks that assert `ANOMALY_BANNER_TEXT` is *absent* on healthy fixtures, which must keep passing untouched.
    - This plan's "Two existing checks this plan knowingly breaks" block above.
  </read_first>

  <behavior>
    - A fixture with at least one unhealthy signal renders `ANOMALY_BANNER_TEXT` exactly once and renders zero `<ul>` and zero `<li` elements.
    - The same fixture no longer renders any of `collect_anomalies()`'s four item copy strings anywhere in the page.
    - A fully healthy fixture renders no banner at all — unchanged behaviour, and the two existing checks asserting it keep passing with no edits.
    - `collect_anomalies()` called directly still returns the same list it returns today for every combination of inputs; its four item strings are still defined and still reachable.
    - `render()`'s `collect_anomalies(...)` call and its four arguments are unchanged.
  </behavior>

  <action>
1. **Revise `ANOMALY_BANNER_TEXT`** to the exact string `06.6.1-UI-SPEC.md`'s Copywriting Contract specifies: the same leading warning glyph and the same "Something needs attention" opening, with the trailing clause re-pointed from the flagged items to the tiles below. The old wording referenced "the flagged item(s) below" — after this task there is nothing below it to point at, so leaving the string alone would ship copy that dangles. Add a trailing comment recording that the string changed *because* the list was removed, so the two edits stay visibly coupled.

2. **Simplify `render()`'s banner assembly.** Replace the whole `if anomalies:` block — the `items_html` generator expression and the `<ul>`/`<li>` concatenation — with a single conditional that sets `banner_html` to `layout.anomaly_banner(ANOMALY_BANNER_TEXT)` when `anomalies` is truthy and the empty string otherwise. Nothing else in `render()` changes in this task.

3. **Leave `collect_anomalies()` completely alone** — signature, docstring and all four item strings. Its return value is still what gates the banner, and (per plan 06.6.1-04) it is what will drive the nav-tab notification dot as well. Add one sentence to its docstring recording the new reality: the list's *contents* are no longer rendered anywhere, only its emptiness is consumed, and the item strings survive deliberately because they remain the readable, greppable definition of what counts as an anomaly. Without that sentence a future reader will correctly conclude the strings are dead code and delete them — which would delete the signal's own documentation.

4. **In `companion/test_status_pages.py`, fix the one check this breaks.** `_battery_drop_drives_both_badge_and_banner` currently requires `"A battery reading shows an abnormal drop."` to be present in the rendered page. Invert that assertion: the copy must now be **absent** from the rendered page, while `collect_anomalies()` called directly with an error battery state must still **return** it. Keep the check's other two assertions — the `dot--error` class and the banner copy appearing exactly once — exactly as they are; those carry the check's real meaning (badge and banner driven by one signal) and must not drift. Extend the registered name to say the detail copy is no longer rendered, so the tally line stays self-describing, and add a one-line comment above it pointing at `06.6.1-UI-SPEC.md`'s "Anomaly detail list (removed)" row as the reason the assertion flipped.

5. **Add two new checks** after it:
   a. **No list markup survives.** With an unhealthy fixture (reuse the stale-device shape an existing check already seeds), assert the rendered page contains zero `<ul` and zero `<li` occurrences, and that `ANOMALY_BANNER_TEXT` appears exactly once. This is the check that proves the redundancy is actually gone rather than merely restyled.
   b. **None of the four item strings is rendered.** With a fixture that trips *all four* signals at once (stale device, stale pipeline, an abnormal battery drop, and a disagreement recorded in the corroboration window), assert `collect_anomalies()` would return four items for those inputs, that the banner still renders exactly once, and that none of the four literal item strings appears in the page. Read the four strings from `collect_anomalies()`'s own behaviour rather than re-typing them where practical; where a literal is unavoidable, keep it in one tuple at the top of the check so a copy change touches one place.

6. **Bump `EXPECTED_CHECK_COUNT` by exactly 2** for this task, computed from the file's real current on-disk value (it reads `36` as of this plan's writing, but sibling plan 06.6-01 also adds checks here and either plan may land first). Tasks 2 and 3 bump it again by 2 each. **Never write a literal total that was not just read from the file.**
  </action>

  <acceptance_criteria>
    - Source: `companion/pages/health_page.py` contains zero occurrences of `<ul>` and zero of `<li`.
    - Source: `ANOMALY_BANNER_TEXT`'s value no longer contains the phrase about flagged items, and `collect_anomalies` still contains all four of its item strings.
    - Source: `render()` still calls `collect_anomalies(` with the same four arguments.
    - Source: `companion/test_status_pages.py` still contains the registered check name substrings for the two healthy-fixture "no banner" checks, unedited.
    - Behavior: an in-process assertion confirms an unhealthy render contains the banner copy once, no list markup, and none of the four anomaly item strings, while `collect_anomalies()` still returns them.
    - Behavior: `server/.venv/bin/python3 companion/test_status_pages.py` passes with `N` two greater than before this task; `server/.venv/bin/ruff check companion/` and `scripts/run-all-tests.sh` both exit 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/ruff check companion/ && server/.venv/bin/python3 -c "
import sys, os, shutil, tempfile, sqlite3; sys.path.insert(0, '.')
from companion.pages import health_page as h
src = open('companion/pages/health_page.py').read()
assert '<ul>' not in src and '<li' not in src, 'the anomaly detail list markup is still present'
items = h.collect_anomalies('warn', 'warn', 'error', True)
assert len(items) == 4, 'collect_anomalies must still compute all four items: %r' % items
tmp = tempfile.mkdtemp()
try:
    rendered = h.render({'state_dir': tmp, 'now': '2026-08-29T12:00:00+00:00'})
    assert rendered.count('<ul') == 0 and rendered.count('<li') == 0, 'rendered page still emits list markup'
    for item in items:
        assert item not in rendered, 'anomaly item copy is still rendered: %r' % item
finally:
    shutil.rmtree(tmp, ignore_errors=True)
print('ok')
" && scripts/run-all-tests.sh</automated>
  </verify>

  <done>The anomaly banner says one thing once. Its detail list is gone from the markup, its copy points at the tiles that actually carry the specifics, and `collect_anomalies()` is untouched — still computing the same four-signal list, still the sole gate on whether the banner appears, with a docstring line explaining why its now-unrendered strings deliberately survive. The one existing check this broke is fixed by inverting exactly one assertion, not by weakening the check, and two new checks pin the absence of both the list markup and the item copy.</done>
</task>

<task type="auto" tdd="true" id="06.6.1-03-02">
  <name>Task 2: Move the battery-trend chart out of the grid into its own full-width section (D-02)</name>

  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` — `render()` as modified by Task 1, particularly the `tiles_html` concatenation and the final return expression; `_battery_section()` in full (**this task does not modify it** — read it to confirm its return contract: `(markup, state)`, where the markup already contains the badge, the raw-mV table, the sparkline, the readout line and the single `<script>` tag); `BATTERY_TREND_SCRIPT_SRC` and the comment explaining why the script tag is emitted only when a chart exists.
    - `companion/layout.py` — `stat_tile()`'s docstring, to confirm what is being given up by not wrapping the battery markup in a tile (the status-coloured top border) and what is not (nothing else — `content_html` passes through byte-for-byte).
    - `companion/static/style.css` — the `.battery-trend-section` and `.battery-trend-section svg` rules added by plan 06.6.1-01 (same wave). If they are not there yet, this task still ships correctly; the drift guard in step 5 is what will catch a rename.
    - `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md` — the Layout Contract's "Battery-trend section — full-width" section and Component Patterns' "Battery-trend section (revised)" row.
    - `companion/test_status_pages.py` — `_health_page_renders_one_dashboard_grid_of_four_tiles` in full (**the one check this task breaks**), and the 06.5-era checks asserting exactly one `<script` tag on a chart-bearing page and zero on the empty path, which must keep passing unchanged.
    - This plan's "Two existing checks this plan knowingly breaks" block above.
  </read_first>

  <behavior>
    - A chart-bearing render emits exactly one `dashboard-grid` div containing exactly three `stat-tile` occurrences, and exactly one `battery-trend-section` element, in that document order.
    - The battery section carries an `<h2>` holding the battery heading text, and the `<h2>` appears exactly once on the page.
    - Everything `_battery_section()` produced before still appears: the status badge, the raw-mV table, the sparkline, the readout element and exactly one `<script>` tag whose src is `BATTERY_TREND_SCRIPT_SRC`.
    - The empty-history render still emits no `<svg`, no readout element and no `<script>` tag — and still emits the battery section wrapper with its heading, because a section that exists but has nothing to plot is honest, whereas a vanished section reads as a bug.
    - `_battery_section()`'s source is unchanged, and `battery_state` still reaches `collect_anomalies()` unchanged.
    - The battery section carries no `stat-tile` class and no status modifier class — it is a page section, not a status tile.
  </behavior>

  <action>
1. **Add two module constants** near the other Battery constants: `BATTERY_SECTION_HEADING` holding the heading text `Battery trend` (the same string `render()` passes to `stat_tile()` today — moving it into a constant is what lets plan 06.6.1-04 attach an icon to a known heading without re-typing the literal), and `BATTERY_SECTION_CLASS` holding the section's CSS class, with a trailing comment naming `companion/static/style.css` as its counterpart and noting that step 5 adds a drift guard.

2. **Add `_battery_trend_section_html(battery_html)`** next to `_battery_readout_block()`. It returns a `<section>` carrying `BATTERY_SECTION_CLASS`, containing an `<h2 class="text-heading">` holding `escape_html(BATTERY_SECTION_HEADING)` followed by `battery_html` interpolated **verbatim** — no `escape_html()` on it. `battery_html` is `_battery_section()`'s already-safe output (badge markup, an escaped table, an SVG, a script tag), exactly the same "already-built markup passes through untransformed" contract `stat_tile()` and `_source_fault_block()` already follow; re-escaping it would print the raw tags as visible text. Say that in the docstring, in the same terms `stat_tile()`'s docstring already uses, so the convention reads as one convention rather than three coincidences.

3. **Restructure `render()`'s return.** Three changes, nothing else:
   - Remove the `layout.stat_tile("Battery trend", battery_html, battery_state)` term from `tiles_html`, leaving three tiles: Device check-in, ADS-B pipeline, Corroboration. Keep their order and their arguments exactly as they are.
   - Append `_battery_trend_section_html(battery_html)` to the return expression, **after** the closing `</div>` of the dashboard grid. Document order matters here in a way a CSS-only change could not fix: the section must follow the grid in the DOM so it reads as "and here, in detail" rather than interrupting the four-signal summary.
   - Leave `battery_state` in the `collect_anomalies(...)` call untouched. The state value is still consumed — it just no longer paints a tile border. Add a comment saying so, because a reader seeing `battery_state` no longer passed to `stat_tile()` will otherwise assume it went unused.
   The heading (`<h1>`), the source-fault block, the banner and the `<h2>Overview</h2>` line are all unchanged.

4. **Do not modify `_battery_section()`.** Its badge, table, sparkline, readout and script-tag emission all compose unchanged inside a `<section>` exactly as they did inside a tile. Not touching it also keeps the collision surface with sibling plan 06.6-01 (which edits that function's table formatting) as small as possible. The deliberate absence of a status-coloured top border on the new section is not an oversight: the section already carries `_battery_section()`'s own `status_dot()` badge as its verdict, and plan 06.6.1-01's `.battery-trend-section` rule omits `border-top` for exactly that reason.

5. **In `companion/test_status_pages.py`:**
   - **Fix the broken check.** Rewrite `_health_page_renders_one_dashboard_grid_of_four_tiles` to expect **three** `stat-tile` occurrences, and rename both the function and its registered check name to say three. In the same check, add two assertions covering what moved: exactly one occurrence of `health_page.BATTERY_SECTION_CLASS`, and that its index in the rendered string is **greater** than the index of the grid's closing `</div>` — i.e. the section really follows the grid rather than merely existing. Keep the existing one-`dashboard-grid`, one-`Overview`-heading and no-`page-section` assertions exactly as they are.
   - **Add a check that nothing was lost in the move.** Seed two readings, render, and assert all of: `BATTERY_SECTION_HEADING` appears in an `<h2>`; the status badge label appears; the readout element id appears; exactly one `<script` occurrence is present with `BATTERY_TREND_SCRIPT_SRC`; and the battery section markup contains **no** `stat-tile` class. Slice the rendered string to the battery section's own boundaries for the last assertion rather than searching the whole page, or the three surviving tiles will make it trivially fail.
   - **Add a cross-file class agreement check.** Read `companion/static/style.css` from disk (build the path from the module-level `HERE` constant, the same way the existing source-reading checks do) and assert `health_page.BATTERY_SECTION_CLASS` appears in it. Same drift-guard pattern 06.5-02 established for the Python/CSS/JS trio, applied to the one new class this task emits.
   - Bump `EXPECTED_CHECK_COUNT` by exactly **2** for this task, on top of Task 1's +2, computed from the file's real current value.
  </action>

  <acceptance_criteria>
    - Source: `companion/pages/health_page.py` defines `BATTERY_SECTION_HEADING`, `BATTERY_SECTION_CLASS` and `_battery_trend_section_html`.
    - Source: `render()` contains exactly three `layout.stat_tile(` calls and still contains `collect_anomalies(` with `battery_state` among its arguments.
    - Source: `git diff companion/pages/health_page.py` shows no change inside `_battery_section()`.
    - Source: `companion/pages/health_page.py` still contains exactly one `<script` occurrence and exactly one `defer`.
    - Behavior: an in-process assertion confirms one grid with three tiles, one battery section positioned after the grid, one `<h2>` with the heading, and one script tag on a chart-bearing render.
    - Behavior: the empty-history render still emits zero `<script` and zero `<svg` while still emitting the battery section and its heading.
    - Behavior: `server/.venv/bin/python3 companion/test_status_pages.py` passes with `N` two greater than after Task 1; `server/.venv/bin/ruff check companion/` and `scripts/run-all-tests.sh` both exit 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/ruff check companion/ && [ "$(grep -c '<script' companion/pages/health_page.py)" = "1" ] && [ "$(grep -c 'layout.stat_tile(' companion/pages/health_page.py)" = "3" ] && server/.venv/bin/python3 -c "
import sys, shutil, tempfile; sys.path.insert(0, '.')
from companion.pages import health_page as h
css = open('companion/static/style.css').read()
assert h.BATTERY_SECTION_CLASS in css, 'battery section class %r is not styled' % h.BATTERY_SECTION_CLASS
tmp = tempfile.mkdtemp()
try:
    rendered = h.render({'state_dir': tmp, 'now': '2026-08-29T12:00:00+00:00'})
    assert rendered.count('dashboard-grid') == 1
    assert rendered.count('class=\"stat-tile') == 3, 'expected three tiles, got %d' % rendered.count('class=\"stat-tile')
    assert rendered.count(h.BATTERY_SECTION_CLASS) == 1, 'expected exactly one battery section'
    assert rendered.index(h.BATTERY_SECTION_CLASS) > rendered.index('dashboard-grid'), 'the battery section must follow the grid'
    assert rendered.count('>' + h.BATTERY_SECTION_HEADING + '<') == 1, 'expected exactly one battery heading'
    assert rendered.count('<script') == 0 and rendered.count('<svg') == 0, 'an empty history must still emit no chart and no script'
finally:
    shutil.rmtree(tmp, ignore_errors=True)
print('ok')
" && scripts/run-all-tests.sh</automated>
  </verify>

  <done>Battery trend has the whole page width instead of a 240px grid track, in its own card section below a three-tile Overview grid, with its badge, table, chart, readout and single script tag all intact and `_battery_section()` itself untouched. The check that pinned four tiles now pins three plus a positioned battery section, so the structural contract is still enforced rather than merely relaxed, and the section's CSS class is guarded against silent drift.</done>
</task>

<task type="auto" tdd="true" id="06.6.1-03-03">
  <name>Task 3: Expose the anomaly verdict as a cheap, never-raising cross-page signal</name>

  <files>companion/pages/health_page.py, companion/test_status_pages.py</files>

  <read_first>
    - `companion/pages/health_page.py` — `render()` as modified by Tasks 1 and 2, specifically its five `_safe_query()` calls and the four section calls that turn them into states; `_safe_query()` and the `_DB_UNAVAILABLE` sentinel with its comment; `_cutoff_iso()` and `_CORROBORATION_WINDOW_DAYS`; `collect_anomalies()`.
    - `companion/pages/__init__.py` — the `ctx` contract in full, and the standing rule that **no page module imports another page module**. That rule is precisely why this function must exist here and be consumed by `companion/app.py`, rather than a nav renderer reaching into Health's internals.
    - `companion/app.py` — `runway_images_available()` (added in Phase 06.4) and its `try/except OSError` "never raises" discipline, plus `page_context()`. This function follows the same precedent from the other side of the boundary: `page_context()` calls it on **every** page, so a raise here would fault every page in the app, not just Health.
    - `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md` — the Layout Contract's "Health nav-tab notification indicator" section, which names `ctx["health_anomaly_active"]` and the `ctx["runway_images"]` precedent.
    - This plan's soft-conflict block above.
  </read_first>

  <behavior>
    - `anomaly_active(state_dir)` returns `False` for a healthy seeded fixture and `True` for a fixture with any one of the four signals unhealthy.
    - For every fixture tested, `anomaly_active(state_dir, now)` and `ANOMALY_BANNER_TEXT in render(ctx)` agree — in both directions, on the same state directory and the same `now`.
    - `anomaly_active()` returns `False`, and does not raise, for: a non-existent directory path, an empty directory, a path that is a regular file rather than a directory, and a `history.db` whose contents are non-SQLite bytes.
    - `anomaly_active()` accepts `now=None` and resolves the current time itself.
    - `render()`'s rendered output for any given fixture is byte-identical before and after this task — the refactor moves reads into a helper without changing what is read or how it degrades.
  </behavior>

  <action>
1. **Extract the five reads into `_read_health_inputs(state_dir, now)`**, placed just above `render()`. It performs exactly the five `_safe_query()` calls `render()` performs today — latest device health, the last-pipeline-run meta value, the source-fault meta value, the battery trend rows, and the corroboration counts computed against `_cutoff_iso(now, _CORROBORATION_WINDOW_DAYS)` — and returns them in a dict keyed by name. Move the calls; do not change a single argument, sentinel or cutoff computation. Its docstring must state why it exists: `render()` and `anomaly_active()` must be looking at the same five values, or the nav dot and the banner can disagree on screen, and single-sourcing the *inputs* is what removes that whole class of drift. Rewrite `render()` to consume it, leaving everything downstream of the reads untouched.

2. **Add the public `anomaly_active(state_dir, now=None)`** immediately after `collect_anomalies()`. It:
   - resolves `now` to `history_db.utc_now_iso()` when not supplied;
   - calls `_read_health_inputs()`, then the same four section builders `render()` calls, keeping only their second return values (the states and the disagreement flag) and discarding the markup;
   - returns `bool(collect_anomalies(device_state, pipeline_state, battery_state, disagreement_warn))`.
   Building and discarding markup is deliberate, not waste: routing the verdict through the *same* functions `render()` uses is what makes it structurally impossible for the dot and the banner to disagree. A cheaper reimplementation that recomputed the states directly would be a second copy of the rules, and this file's whole D-14 design rests on there being one. State that in the docstring.

3. **Wrap the whole body in a broad guard that returns `False` on any exception.** This is a deliberate departure from the narrow `(sqlite3.Error, OSError)` catches used elsewhere in this file, and the docstring must justify it in these terms: `_safe_query()`'s narrow catch protects one *section* of one page, whereas this function is called from `companion/app.py`'s `page_context()` on **every** authenticated page render, so an unanticipated raise here would turn every page in the app into a 500 over a decorative nav dot. `runway_images_available()` established exactly this never-raises contract for exactly this reason in Phase 06.4. Failing closed to `False` is also the safe direction: a missing dot understates a problem the Health page itself will still report in full, whereas a crashed app reports nothing at all.

4. **Add a module-docstring sentence** recording that `anomaly_active()` is this module's one intentionally-public cross-page export, consumed by `companion/app.py`'s `page_context()`, and that it exists specifically so no nav renderer has to import a page module — the constraint `companion/pages/__init__.py` states.

5. **In `companion/test_status_pages.py`, add two checks:**
   a. **Agreement, both directions.** Over at least three fixtures — fully healthy, stale device, and abnormal battery drop — assert for each that `anomaly_active(tmp, now)` equals `ANOMALY_BANNER_TEXT in render(_ctx(tmp, now=now))`. Compare the two booleans against each other rather than against hard-coded expectations, so the check pins *agreement* (the property that matters on screen) rather than restating the anomaly rules a third time. Include at least one fixture of each verdict so the check cannot pass vacuously.
   b. **Never raises.** Call `anomaly_active()` against four hostile inputs — a path that does not exist, an empty temporary directory, a path pointing at a regular file, and a temporary directory whose `history.db` has been overwritten with non-SQLite bytes — and assert each returns `False` without raising. Name the check so it says this function runs on every page and therefore may never fault one, and add a one-line comment citing `runway_images_available()`'s precedent. Build the `history.db` filename from `server.history_db`'s own constant if it exposes one; otherwise derive it by opening the database once through `history_db.open_db()` and then overwriting whatever file appeared, rather than hard-coding a name this test would silently stop exercising if it changed.
   - Bump `EXPECTED_CHECK_COUNT` by exactly **2** for this task, on top of the +2 from each of Tasks 1 and 2 (+6 across the plan), computed from the file's real current value.
  </action>

  <acceptance_criteria>
    - Source: `companion/pages/health_page.py` defines `anomaly_active` and `_read_health_inputs`, and `render()` calls `_read_health_inputs(`.
    - Source: `anomaly_active`'s docstring mentions `page_context` (the caller whose blast radius justifies the broad guard).
    - Source: `companion/pages/health_page.py` still imports only its existing modules — no new import beyond what `_read_health_inputs` already needs.
    - Behavior: an in-process assertion confirms `anomaly_active()` returns `False` for a non-existent path, an empty directory, a file-not-directory path and a corrupt database, without raising.
    - Behavior: an in-process assertion confirms `anomaly_active()` and the banner's presence in `render()` agree on a healthy fixture and on an unhealthy one.
    - Behavior: `server/.venv/bin/python3 companion/test_status_pages.py` passes with `N` six greater than before this plan; `server/.venv/bin/ruff check companion/` and `scripts/run-all-tests.sh` both exit 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_status_pages.py && server/.venv/bin/ruff check companion/ && server/.venv/bin/python3 -c "
import sys, os, shutil, tempfile, inspect; sys.path.insert(0, '.')
from companion.pages import health_page as h
doc = inspect.getdoc(h.anomaly_active) or ''
assert 'page_context' in doc, 'anomaly_active must document why it never raises'
assert h.anomaly_active('/nonexistent/definitely-not-here') is False
empty = tempfile.mkdtemp()
try:
    assert h.anomaly_active(empty) is False
    rendered = h.render({'state_dir': empty, 'now': '2026-08-29T12:00:00+00:00'})
    assert h.anomaly_active(empty, '2026-08-29T12:00:00+00:00') == (h.ANOMALY_BANNER_TEXT in rendered), 'verdict and banner disagree'
    from server import history_db
    with history_db.open_db(empty) as conn:
        pass
    dbs = [f for f in os.listdir(empty) if f.endswith('.db')]
    assert dbs, 'expected a database file to have been created'
    open(os.path.join(empty, dbs[0]), 'wb').write(b'not a sqlite file at all')
    assert h.anomaly_active(empty) is False, 'a corrupt database must degrade to False, never raise'
finally:
    shutil.rmtree(empty, ignore_errors=True)
fd, path = tempfile.mkstemp()
os.close(fd)
try:
    assert h.anomaly_active(path) is False, 'a file-not-directory state_dir must degrade to False'
finally:
    os.unlink(path)
print('ok')
" && scripts/run-all-tests.sh</automated>
  </verify>

  <done>`health_page.anomaly_active(state_dir)` answers the one question the nav needs — is anything wrong right now — by running the exact same reads and the exact same section functions `render()` runs, so the dot plan 04 will draw and the banner the page draws can never contradict each other. It returns `False` rather than raising for a missing, empty, non-directory or corrupt state directory, because `companion/app.py` calls it on every page and a decorative dot must never be able to 500 the app. Two checks pin both properties, and `render()`'s output is unchanged by the refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `history.db` → every authenticated page (new) | `anomaly_active()` widens Health's database reads from one page to every page in the app, via `page_context()`. Nothing user-supplied crosses here — the inputs are all server-written rows — but the *blast radius* of a failure widens from one section to the whole app, which is what drives this plan's never-raises requirement. |
| browser → `/health` | Unchanged. The page stays behind `companion/auth.py`'s HMAC session gate; this plan adds no route and no authorization surface. |
| section markup → rendered page | Unchanged. `_battery_trend_section_html()` passes already-built, already-escaped markup through verbatim, the same contract `stat_tile()` and `_source_fault_block()` already follow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.1-09 | Denial of Service | `anomaly_active()` raising from `page_context()`, faulting **every** authenticated page over a decorative nav dot | high | mitigate | Task 3 requires a broad exception guard returning `False`, justified in the docstring against `runway_images_available()`'s Phase 06.4 precedent, and pins it with a check exercising four hostile inputs (missing path, empty directory, file-not-directory, corrupt database). Failing closed also fails in the safe direction: a missing dot understates a problem the Health page still reports in full. |
| T-06.6.1-10 | Repudiation / misinformation | The nav dot and the anomaly banner disagreeing — a user sees "attention needed" on the tab and an all-clear Health page, or the reverse | medium | mitigate | `_read_health_inputs()` single-sources the five reads and `anomaly_active()` routes its verdict through the same four section builders and the same `collect_anomalies()` call `render()` uses, so a second copy of the rules cannot exist. Task 3 check (a) asserts the two agree in both directions across healthy and unhealthy fixtures. |
| T-06.6.1-11 | Tampering (XSS via markup pass-through) | `_battery_trend_section_html()` interpolating `battery_html` without escaping | low | mitigate | `battery_html` is `_battery_section()`'s own output, every dynamic part of which is already escaped at its source (the mV table through `layout.data_table()`, timestamps through `escape_html()` in `battery_sparkline_svg()`). Escaping it again would double-encode and print raw tags as text. The heading is a module constant and is still routed through `escape_html()`. The convention is stated in the docstring in the same terms `stat_tile()` already uses. |
| T-06.6.1-12 | Repudiation / misinformation | Removing the anomaly detail list leaving the banner's copy pointing at items that no longer exist | medium | mitigate | Task 1 changes the copy in the same edit that removes the list and comments the two as coupled. Task 1's new check (b) trips all four signals at once and asserts none of the four item strings reaches the page, so a partial revert fails loudly. |
| T-06.6.1-13 | Denial of Service (resource) | Five extra SQLite reads added to every page render | low | accept | Accepted deliberately. This is a single-user service reading a small local SQLite file over a loopback-bound HTTP server; the added cost is microseconds against a request that already opens the same database for other `ctx` entries. The alternative — caching the verdict across requests — would introduce staleness in a signal whose entire purpose is freshness, which is a worse trade for this workload. |
| T-06.6.1-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this plan installs zero packages and adds zero imports. |
</threat_model>

<verification>
1. `scripts/run-all-tests.sh` exits 0 — all 15 harnesses green and the coverage threshold still met (`companion/pages/health_page.py` is measured; the six new checks exercise `anomaly_active()`'s guard branch and both new markup paths).
2. `companion/test_status_pages.py` reports a passing `N/N` tally six higher than before this plan, with `EXPECTED_CHECK_COUNT` matching its own `check(` count.
3. `git diff --stat` touches exactly the two files in `files_modified` — in particular **not** `companion/static/style.css` (plan 01 owns it), `companion/layout.py` or `companion/app.py` (plans 04/05 own those).
4. `git diff companion/pages/health_page.py` shows **no** change inside `_battery_section()`, `battery_sparkline_svg()`, `battery_status()`, `staleness_status()`, `_device_section()`, `_pipeline_section()` or `_corroboration_section()`, and no change to `collect_anomalies()` beyond one added docstring sentence.
5. The two knowingly-broken checks are corrected in place, keeping their non-affected assertions byte-identical, and neither is deleted.
</verification>

<success_criteria>
- D-04: the Health page states each anomaly once — in the tiles — and the banner's copy points there rather than at a list that no longer exists.
- D-02: the battery chart gets the full page width in its own card section, keeping every 06.5 behaviour (badge, hit targets, readout, single script) intact.
- The Overview grid's structural contract is still enforced by a check, at three tiles plus a positioned battery section, rather than relaxed.
- `anomaly_active()` gives plans 04 and 05 a cross-page signal that cannot disagree with the banner and cannot fault the app.
- `collect_anomalies()` and every section builder are untouched, keeping the collision surface with sibling phase 06.6 minimal.
- Full suite green, count gate in sync against the file's real value.
</success_criteria>

<output>
Create `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-03-SUMMARY.md` when done.
</output>
