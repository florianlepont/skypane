---
phase: 06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - companion/pages/history_page.py
  - companion/test_view_pages.py
autonomous: true
requirements: [D-02]

must_haves:
  truths:
    - "History's flight table renders exactly 7 header cells and exactly 7 body cells per row, down from 9 — Callsign+Hex and Aircraft type+Airline are each one column now."
    - "A merged cell puts both values on one line, separated by a middle dot, at today's row height — no stacked two-line cell, no <br>, no block-level child anywhere in the table body."
    - "Column order is unchanged left to right: Timestamp, Callsign, Type, Route, State, Corroboration, Runway."
    - "Both halves of every merged cell reach the HTML through companion.layout.escape_html(), the module's single escaping path — no second escaping route is introduced."
    - "A row missing its secondary value (no hex, or no airline) renders the primary value alone, with no dangling separator and no empty secondary span."
    - "The Corroboration column still embeds layout.status_dot()'s pre-built markup unescaped, and the table is still wrapped in .data-table-wrap — the two reasons this table is hand-built rather than delegated to layout.data_table() are both intact."
  artifacts:
    - companion/pages/history_page.py    # _merged_cell(), a 7-entry _HEADERS, a rewritten _history_table_html() cells tuple, revised module docstring
    - companion/test_view_pages.py       # 5 new checks, EXPECTED_CHECK_COUNT + 5
  key_links:
    - "The class names `cell-primary`, `cell-inline-sep` and `cell-secondary` are defined in companion/static/style.css by plan 06.6.1-01 (same wave, different file). If either side renames, the cell renders as unstyled inline text — Task 2 adds a drift guard that reads the stylesheet from disk and requires all three class names to appear in it."
    - "companion/test_view_pages.py exits non-zero unless passed == total == EXPECTED_CHECK_COUNT. Bump by exactly +5 from the file's CURRENT on-disk value, never to a hard-coded total — this project has hit stale literal counts three times already (06.3-03, 06.4-01, and 06.5-02's own note)."
    - "Sibling phase 06.6 plan 06.6-03 rewrites the SAME function's Timestamp cell. See the soft-conflict block below before assuming this file's shape."
---

<objective>
Cut History's flight table from 9 columns to 7 by merging Callsign+Hex and Aircraft type+Airline into single dot-separated cells, so the table fits a 13" laptop's ~1280px content column without reaching for horizontal scroll — while keeping today's row height, which is the property that makes scanning many rows fast.

Purpose: real-device testing during 06.5-03 found History the densest page in the app. `06.6.1-UI-SPEC.md`'s Layout Contract settles the fix as sketch 003 Variant B ("Inline compact"), chosen over the stacked-cell variant specifically because stacking grows row height and works against fast scanning.
Output: one new private helper, a 7-entry header tuple, a rewritten cells tuple, and five new automated checks.
</objective>

## Soft conflict with sibling phase 06.6 — read before editing

`.planning/phases/06.6-companion-ui-clarity-pass-timestamps-poll-trigger-cooldown-c/06.6-03-PLAN.md` is **planned but not executed** and rewrites the **same function** — `_history_table_html()` — to render the Timestamp column as an absolute ISO string followed by a relative age. There is no formal execution-order dependency between 06.6 and 06.6.1 (the same sibling arrangement 06.3/06.4/06.5 already used successfully on this project).

The two edits are disjoint by column: 06.6-03 owns the **Timestamp** cell; this plan owns the **Callsign/Hex** and **Type/Airline** cells and the `_HEADERS` tuple, and does not touch Timestamp's formatting at all.

**Whichever of the two runs second must:**
- Read `_history_table_html()` and `_HEADERS` fresh from disk before editing. Every line reference in this plan is a landmark to search for by symbol name, never a coordinate to trust.
- Read the other phase's `-SUMMARY.md` if it exists, before assuming the file's shape.
- Recompute `EXPECTED_CHECK_COUNT` from the file's real current value. 06.6-03 also adds checks to `companion/test_view_pages.py`.

If 06.6-03 has already run, the Timestamp cell will hold a formatted string rather than the raw `row["ts"]`. Leave it exactly as found — this plan changes column *count*, not Timestamp *content*.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-CONTEXT.md
@.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md
@.claude/skills/sketch-findings-skypane/references/data-density.md
</context>

## Artifacts this plan produces

| Symbol / path | Kind | Location | Notes |
|---|---|---|---|
| `_HEADERS` | changed value | `companion/pages/history_page.py` | 9 entries → 7: `Timestamp, Callsign, Type, Route, State, Corroboration, Runway` |
| `CELL_PRIMARY_CLASS`, `CELL_SECONDARY_CLASS`, `CELL_SEPARATOR_CLASS` | new module constants | `companion/pages/history_page.py` | Contract values shared with `companion/static/style.css`; a drift guard asserts they appear there |
| `CELL_SEPARATOR_TEXT` | new module constant | `companion/pages/history_page.py` | The middle-dot glyph, defined once |
| `_merged_cell(primary, secondary)` | new function | `companion/pages/history_page.py` | Builds one `<td>` holding both values on one line, both escaped |
| `_history_table_html(formatted_rows)` | changed output | `companion/pages/history_page.py` | 7 cells per row instead of 9 |
| 5 new `check()` calls | tests | `companion/test_view_pages.py` | Plus the `EXPECTED_CHECK_COUNT` bump |

<tasks>

<task type="auto" tdd="true" id="06.6.1-02-01">
  <name>Task 1: Merge Callsign+Hex and Type+Airline into single one-line cells (D-02)</name>

  <files>companion/pages/history_page.py</files>

  <read_first>
    - `companion/pages/history_page.py` — the whole file (173 lines). Note especially: the module docstring's four standing claims (reuses `server.plane.render`'s presentation mappings so page and panel never drift; every DB read goes through `_safe_query()`; `layout.data_table()` cannot host the Corroboration column's `status_dot()` markup, which is *why* this table is hand-built; the `.data-table-wrap` wrapper was the original defect 06.3-03 closed and must survive) — **the docstring's "9-column table" wording needs updating and its other claims must stay true**; `_HEADERS`; `format_event_row()` and the exact dict keys it returns; `_history_table_html()` in full, particularly the `cells` tuple and which cells carry `class="mono"`.
    - `.claude/skills/sketch-findings-skypane/references/data-density.md` — the Design Decisions list (especially the row-height rationale for picking B over A), the CSS Patterns block, the HTML Structures block, and the "What to Avoid" list, which forbids Variant C's further State-into-Route merge.
    - `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-UI-SPEC.md` — the Layout Contract's "History table density" section in full (including the accepted-interim-state caveat about Timestamp width) and Component Patterns' "Merged table cell" row.
    - `companion/layout.py` — `escape_html()` and `status_dot()`, so the escaping convention this file already follows is applied identically to the new spans.
    - This plan's soft-conflict block above.
  </read_first>

  <behavior>
    - `_merged_cell("AFR123", "39d301")` returns one `<td>` containing the primary span, the separator span and the secondary span, in that order, with both values present.
    - `_merged_cell("AFR123", "")` returns a `<td>` containing the primary span only — no separator span, no secondary span.
    - `_merged_cell("", "")` returns a `<td>` with an empty primary span and nothing else; it never raises.
    - `_merged_cell` escapes both arguments: a value carrying `<`, `>`, `&` or `"` appears in escaped form and never raw.
    - `_history_table_html()` on a one-row fixture emits exactly 7 `<th>` and exactly 7 `<td>`.
    - The emitted header text no longer contains standalone `Hex` or `Airline` header cells; it does contain `Timestamp`, `Callsign`, `Type`, `Route`, `State`, `Corroboration` and `Runway`, in that order.
    - The emitted table body contains no `<br` and no `<div` — the one-line/one-row-height contract that decided Variant B over Variant A.
    - The Corroboration cell still contains `dot--` markup rendered as real HTML, not escaped text, and the whole table is still wrapped in `.data-table-wrap`.
    - The empty-rows path is unchanged: `_history_table_html([])` still returns `layout.empty_state()`'s output.
  </behavior>

  <action>
Edit `companion/pages/history_page.py` only. `format_event_row()` is **not** modified — it already returns every value the merged cells need under stable keys, and leaving it untouched keeps the collision surface with sibling plan 06.6-03 as small as possible.

1. **Add four module constants** next to `_HEADERS`:
   - `CELL_PRIMARY_CLASS` = the primary-value class name, `CELL_SECONDARY_CLASS` = the secondary-value class name, `CELL_SEPARATOR_CLASS` = the separator class name. Their values must equal the class names `companion/static/style.css` styles (plan 06.6.1-01, same wave) — name that file in a trailing comment and note that Task 2 adds a drift guard reading the stylesheet from disk, so the two cannot silently diverge. They are duplicated rather than imported because a page module has no import path to the stylesheet.
   - `CELL_SEPARATOR_TEXT` = the middle-dot glyph, defined once so the separator can never be typed as a hyphen or a bullet in one call site and a middle dot in another.

2. **Replace `_HEADERS`** with the 7-entry tuple `Timestamp, Callsign, Type, Route, State, Corroboration, Runway`. Note in a comment that `Callsign` and `Type` are now merged columns carrying a secondary value each, and that the left-to-right order is deliberately the same as the 9-column version so a returning user's scanning habit still works.
   **Do not go further.** Sketch Variant C additionally merged State into Route; `06.6.1-UI-SPEC.md` and `data-density.md` both explicitly place that outside this phase's locked scope. Seven columns, not six.

3. **Add `_merged_cell(primary, secondary)`** immediately above `_history_table_html()`. It returns a complete `<td>` string:
   - always a `<span>` carrying `CELL_PRIMARY_CLASS` holding `escape_html(primary)`;
   - then, **only when `secondary` is truthy**, a `<span>` carrying `CELL_SEPARATOR_CLASS` holding `escape_html(CELL_SEPARATOR_TEXT)` followed by a `<span>` carrying `CELL_SECONDARY_CLASS` holding `escape_html(secondary)`.
   - The `<td>` carries no `class` attribute of its own — the monospace treatment now comes from the two span classes, so a `mono` class on the cell would be redundant and would fight the 12px secondary size.
   - Give it a docstring stating the falsy-secondary rule and its reason: `format_event_row()` legitimately yields an empty string for a missing hex or a missing aircraft type, and rendering a separator with nothing after it would read as truncated data rather than as absent data. That is the whole reason this is a function rather than an inline format string used twice.
   - Both arguments go through `escape_html()` here and nowhere else — do not pre-escape at the call site as well, or values would be double-encoded and print their entity forms as visible text (the same trap `stat_tile()`'s docstring already documents for `content_html`).

4. **Rewrite the `cells` tuple** inside `_history_table_html()` to seven entries, in this order:
   - Timestamp — **leave exactly as found** (see the soft-conflict block: sibling plan 06.6-03 owns this cell's content).
   - `_merged_cell(row["callsign"], row["hex"])`
   - `_merged_cell(row["aircraft_type_label"], row["airline_label"])`
   - Route, State — unchanged.
   - Corroboration — unchanged, still `layout.status_dot(...)` interpolated **without** `escape_html()`. This is the one deliberate non-escaping site in the file and 06.3-03 pinned it with a regression check; do not "fix" it.
   - Runway — unchanged.
   Everything else in the function — the empty-rows early return, the header-cell comprehension, the `row`/`row-alt` alternation, and the `.data-table-wrap` / `.data-table` wrapper block — stays byte-identical.

5. **Update the module docstring.** Two edits: the sentence describing "History's 9-column table" now describes a 7-column table, and add one sentence recording that Callsign/Hex and Type/Airline are rendered as merged one-line cells whose *row height is unchanged* — that property is the reason this variant was chosen over stacked cells, and it is the thing a future "improvement" is most likely to break. Leave every other claim in the docstring intact; all of them are still true, and Task 2 asserts two of them still hold.

6. Add no new import. `escape_html`, `layout` and `panel_render` are all already imported.
  </action>

  <acceptance_criteria>
    - Source: `companion/pages/history_page.py` defines `_merged_cell`, `CELL_PRIMARY_CLASS`, `CELL_SECONDARY_CLASS`, `CELL_SEPARATOR_CLASS` and `CELL_SEPARATOR_TEXT`.
    - Source: `_HEADERS` has exactly 7 entries and contains neither a standalone `Hex` nor a standalone `Airline` entry.
    - Source: `companion/pages/history_page.py` still contains `'<div class="data-table-wrap">'` and still interpolates `layout.status_dot(` without wrapping it in `escape_html(`.
    - Source: the file's import block is unchanged.
    - Behavior: an in-process assertion confirms a one-row render emits exactly 7 `<th>` and 7 `<td>`, that a merged cell holds both values with the separator between them, that a missing secondary value drops the separator, and that a hostile value is escaped.
    - Behavior: the rendered table body contains no `<br` and no `<div` (the row-height contract).
    - Behavior: `server/.venv/bin/ruff check companion/` exits 0 and `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 -c "
import sys; sys.path.insert(0, '.')
from companion.pages import history_page as h
assert len(h._HEADERS) == 7, 'expected 7 headers, got %d: %r' % (len(h._HEADERS), h._HEADERS)
assert 'Hex' not in h._HEADERS and 'Airline' not in h._HEADERS
assert list(h._HEADERS) == ['Timestamp', 'Callsign', 'Type', 'Route', 'State', 'Corroboration', 'Runway'], h._HEADERS
c = h._merged_cell('AFR123', '39d301')
assert h.CELL_PRIMARY_CLASS in c and h.CELL_SECONDARY_CLASS in c and h.CELL_SEPARATOR_CLASS in c
assert c.index(h.CELL_PRIMARY_CLASS) < c.index(h.CELL_SEPARATOR_CLASS) < c.index(h.CELL_SECONDARY_CLASS)
bare = h._merged_cell('AFR123', '')
assert h.CELL_SEPARATOR_CLASS not in bare and h.CELL_SECONDARY_CLASS not in bare, 'a missing secondary must drop the separator'
hostile = h._merged_cell('A<script>', 'x\"y')
assert '<script' not in hostile and 'x\"y' not in hostile, 'merged cell values must be escaped'
row = h.format_event_row({'ts': '2026-08-29T07:24:05+00:00', 'callsign': 'AFR123', 'hex': '39d301', 'aircraft_type': 'A320', 'airline': 'AFR', 'origin': 'ORY', 'destination': 'LHR', 'confirmed_state': 'DEPARTING', 'corroborated': 'True', 'tracked_runway': '06'})
tbl = h._history_table_html([row])
assert tbl.count('<th>') == 7 and tbl.count('<td>') == 7, 'expected 7/7 cells, got %d/%d' % (tbl.count('<th>'), tbl.count('<td>'))
assert '<br' not in tbl and '<div class=\"data-table-wrap\">' in tbl and tbl.count('<div') == 1, 'no stacked cells; the scroll wrapper is the only div'
assert 'dot--' in tbl, 'the Corroboration status_dot markup must still render unescaped'
assert h._history_table_html([]).startswith('<div class=\"empty-state\"'), 'the empty path must be unchanged'
print('ok')
" && server/.venv/bin/ruff check companion/ && scripts/run-all-tests.sh</automated>
  </verify>

  <done>History renders seven columns instead of nine. Callsign and hex share one line separated by a middle dot, as do aircraft type and airline, at exactly today's row height with no stacked cells anywhere. A row with no hex or no type degrades to its primary value alone rather than a dangling separator. Both merged values go through the module's one escaping helper, the Corroboration column's deliberate raw-markup embedding and the horizontal-scroll wrapper are both untouched, and the module docstring now describes the table that actually exists.</done>
</task>

<task type="auto" id="06.6.1-02-02">
  <name>Task 2: Pin the 7-column contract, the one-line row height and the CSS class agreement in the harness</name>

  <files>companion/test_view_pages.py</files>

  <read_first>
    - `companion/test_view_pages.py` — lines 50-70 (`EXPECTED_CHECK_COUNT`, currently `20` — **read the real value, do not trust this number**, sibling plan 06.6-03 also adds checks to this file), the module's fixture helpers (`_mkstate`, the `runway_events` seeding helper, `_ctx`), the `check(name, fn)` harness, the existing History checks — **especially the 06.3-03 check that pins `.data-table-wrap`** and the check that pins the Corroboration column's unescaped `status_dot()` embedding, both of which this task must leave untouched — and the tally block at the end of `main()`.
    - `companion/pages/history_page.py` as modified by Task 1.
    - `companion/test_status_pages.py` — the one existing check that reads a companion source file from disk (the pattern for building a repo-relative path from the module-level `HERE` constant), to copy that shape for this task's stylesheet drift guard.
    - This plan's soft-conflict block above.
  </read_first>

  <action>
Edit `companion/test_view_pages.py` only. Follow the file's existing check shape exactly: a nested function returning an `(ok, reason)` tuple, registered through `check(name, fn)`, using the file's own `_mkstate` + `try/finally shutil.rmtree` fixture discipline where a state directory is needed.

1. **Add five checks**, registered immediately after the existing History table checks:

   a. **Seven columns, named and ordered.** Seed one `runway_events` row, render the History page, and assert the response contains exactly seven `<th>` occurrences; that the seven header labels appear in the expected left-to-right order (compare against `history_page._HEADERS` rather than a hard-coded list, so the check reads the contract rather than restating it); and that the strings `<th>Hex</th>` and `<th>Airline</th>` are **absent**. This is the check that proves the 9→7 reduction shipped.

   b. **Both values survive the merge.** With the same seeded row, assert the callsign, the hex, the aircraft-type label and the airline label all still appear in the rendered output — the merge removed *columns*, not *data*. Then assert the callsign and hex appear inside the **same** `<td>`: locate the `<td>` boundaries around the callsign and require the hex to fall inside them. A check that only counted columns would happily pass on an implementation that dropped the hex entirely.

   c. **Row height contract (Variant A guard).** Assert the rendered History page's table region contains no `<br` and no block-level element inside a `<td>` (search for `<div` and `<p ` within the table body). Name the check so it says what it is protecting: the merged cells stay on one line, because a taller row was the exact reason the stacked-cell variant was rejected. Put a one-line comment above it citing `data-density.md`'s "What to Avoid" list — without it, a future contributor reads two-line cells as a readability improvement rather than as a rejected alternative.

   d. **Hostile values in both merged cells are escaped.** Seed a row whose callsign, hex, aircraft type and airline each carry a quote-and-tag-bearing fragment (mirror the existing hostile-value check's fixture shape in this file or in `companion/test_status_pages.py`). Assert none of the raw fragments appears in the response and that the escaped forms do. Because the values sit in element text rather than an attribute, assert specifically that no raw `<` from the fixture survived.

   e. **Cross-file class agreement.** Read `companion/static/style.css` from disk (build the path from the module-level `HERE` constant, the same way the existing source-reading check does) and assert all three of `history_page.CELL_PRIMARY_CLASS`, `history_page.CELL_SECONDARY_CLASS` and `history_page.CELL_SEPARATOR_CLASS` appear in it. Then assert all three also appear in the rendered History page. This is what makes the Python/CSS contract fail loudly instead of silently rendering the merged cell as plain unstyled text — the same drift-guard pattern 06.5-02 established for the Python/CSS/JS trio.

2. **Bump `EXPECTED_CHECK_COUNT` by exactly 5**, computed from the file's real current on-disk value. Read it first. It is `20` as of this plan's writing, but sibling plan 06.6-03 also adds checks to this file and either plan may land first — this project has shipped a stale hard-coded count three separate times (06.3-03, 06.4-01, and the drift 06.5-02's own plan warned about). **Never write a literal total that was not just read from the file.**

3. **Leave the two pinned History checks unedited**, including their registered names: the `.data-table-wrap` check 06.3-03 added, and the check pinning the Corroboration column's unescaped `status_dot()` embedding. Both still pass unchanged after Task 1, and both are guarding properties this plan explicitly preserves.
  </action>

  <acceptance_criteria>
    - Source: `companion/test_view_pages.py`'s `EXPECTED_CHECK_COUNT` equals its own count of `check(` invocations (the harness exits non-zero otherwise).
    - Source: the file still contains the two pre-existing History checks' registered names, unedited.
    - Source: the new checks reference `history_page._HEADERS` and the three `CELL_*_CLASS` constants by symbol, not by re-typed string literals.
    - Behavior: `server/.venv/bin/python3 companion/test_view_pages.py` prints a passing `N/N` tally and exits 0, with `N` five greater than before this task.
    - Behavior: deliberately reverting `_HEADERS` to its 9-entry form makes check (a) fail — confirm by reasoning about the assertion, not by actually reverting.
    - Behavior: `server/.venv/bin/ruff check companion/` exits 0 and `scripts/run-all-tests.sh` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>server/.venv/bin/python3 companion/test_view_pages.py && server/.venv/bin/ruff check companion/ && server/.venv/bin/python3 -c "
import re, sys; sys.path.insert(0, '.')
src = open('companion/test_view_pages.py').read()
declared = int(re.search(r'EXPECTED_CHECK_COUNT\s*=\s*(\d+)', src).group(1))
assert declared > 20, 'EXPECTED_CHECK_COUNT was not bumped: %d' % declared
from companion.pages import history_page as h
css = open('companion/static/style.css').read()
for name in (h.CELL_PRIMARY_CLASS, h.CELL_SECONDARY_CLASS, h.CELL_SEPARATOR_CLASS):
    assert name in css, 'class %r is emitted by history_page but not styled in style.css' % name
    assert name in src, 'the harness must reference %r' % name
assert 'data-table-wrap' in src, 'the 06.3-03 scroll-wrapper check must survive'
print('ok')
" && scripts/run-all-tests.sh</automated>
  </verify>

  <done>The 7-column shape, the presence of every merged value, the one-line row-height contract, escaping on both merged cells, and the Python-to-CSS class agreement are all pinned by automated checks that read the contract from its source constants rather than restating it. The count gate is back in sync against the file's real value, and the two pre-existing History checks 06.3-03 added are untouched and still passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `history.db` `runway_events` rows → rendered table cells | `callsign`, `hex`, `aircraft_type` and `airline` cross from SQLite into HTML element text. `airline` in particular originates upstream from adsbdb/ADS-B enrichment, not from this project's own code, so it is treated as untrusted regardless of the writer being `poll_loop.py`. |
| browser → `/history` | Unchanged. The page stays behind `companion/auth.py`'s existing HMAC session gate; this plan adds no route and no authorization surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.6.1-05 | Tampering (stored XSS) | `_merged_cell()`'s two new interpolation sites, which carry upstream-sourced airline names and ADS-B callsign/hex values into element text | medium | mitigate | Both arguments are escaped inside `_merged_cell()` through `companion.layout.escape_html()` — the module's single escaping choke point, and the one place a grep can audit. Task 1's gate asserts a tag-and-quote-bearing value comes back escaped; Task 2 check (d) makes it a permanent regression check driven through a real seeded database row. |
| T-06.6.1-06 | Tampering (double-encoding) | Escaping at both the call site and inside the helper, printing entity forms as visible text | low | mitigate | Task 1 states the single-escaping rule explicitly and forbids pre-escaping at the call site. The Corroboration column's deliberately-unescaped `status_dot()` embedding is called out as the one exception and is left untouched, so the two conventions stay distinguishable rather than blurring. |
| T-06.6.1-07 | Tampering (silent feature death) | Drift between the class names `history_page.py` emits and the rules `style.css` defines, leaving merged cells as unstyled run-together text | low | mitigate | The class names live in three module constants and Task 2 check (e) reads the shipped stylesheet from disk, requiring all three to appear there and in the rendered page. `EXPECTED_CHECK_COUNT`'s fail-closed gate stops the check from being quietly deleted. |
| T-06.6.1-08 | Repudiation / data loss | A merge implementation that drops the hex or airline value entirely while still passing a column-count check | medium | mitigate | Task 2 check (b) asserts every merged value still appears in the output **and** that both halves land inside the same `<td>`, which a count-only check cannot detect. |
| T-06.6.1-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this plan installs zero packages, adds zero imports, and introduces no registry artifact. Reaching for a table/grid library to deliver a two-column merge would be a scope violation to flag, not an option. |
</threat_model>

<verification>
1. `scripts/run-all-tests.sh` exits 0 — all 15 harnesses green and the coverage threshold still met (`companion/pages/history_page.py` is measured; the five new checks exercise the merged-cell branches including the falsy-secondary path).
2. `companion/test_view_pages.py` reports a passing `N/N` tally five higher than before this plan, with `EXPECTED_CHECK_COUNT` matching.
3. `git diff --stat` touches exactly the two files in `files_modified` — in particular **not** `companion/static/style.css` (plan 06.6.1-01 owns it), `companion/layout.py` or any other page module.
4. `git diff companion/pages/history_page.py` shows no change to `format_event_row()`, to `render()`, to the import block, or to the `.data-table-wrap` / `.data-table` wrapper block.
</verification>

<success_criteria>
- History fits a 13" laptop's content column at 7 columns instead of 9, with the same left-to-right reading order and the same row height.
- Callsign·Hex and Type·Airline read as one line each, with the separator suppressed when there is nothing to separate.
- No data was lost in the merge — every value that had a column still appears, proven by a check that inspects cell boundaries rather than counting columns.
- Both new interpolation sites go through the module's single escaping helper, and the file's two deliberate exceptions (the raw `status_dot()` embedding, the scroll wrapper) survive untouched.
- The Python-to-CSS class contract is guarded against silent drift.
- Full suite green, count gate in sync against the file's real value.
</success_criteria>

<output>
Create `.planning/phases/06.6.1-companion-visual-polish-pass-logo-branding-mobile-hamburger-/06.6.1-02-SUMMARY.md` when done.
</output>
